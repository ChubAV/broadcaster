# РЕЕСТР ДОЛГА: объявления денежного пути без исполняемого свидетеля.
#
# Это ЗАПИСАННЫЙ ДОЛГ, а не закрытие. Реестр НЕ утверждает, что записанные абзацы
# верны; он утверждает, что их ровно столько, что каждый существует, и что новый
# абзац без свидетеля в него молча не поместится: потолок объявлен константой
# WITHOUT_WITNESS_CEILING в tests/test_application/test_declared_invariants.py и
# может только снижаться.
#
# Формат строки — четыре части через ` :: `:
#   путь модуля :: владелец абзаца :: отпечаток :: причина
#
# Владелец — имя функции либо <module>. Отпечаток — первые 60 символов абзаца со
# схлопнутыми пробелами. ПРИЧИНА ОБЯЗАТЕЛЬНА: запись без причины есть способ пройти
# гейт, ничего не починив.
#
# В реестр НЕ ИМЕЮТ ПРАВА попасть: абзац, несущий маркер ⚠️, и любое из ПЕРЕЖИВШИХ
# мест находок раунда 6 — на это краснеют тесты храповика.
#
# ⚠️ ПЛАН 05.1-07 УБРАЛ ОТСЮДА ШЕСТНАДЦАТЬ ЗАПИСЕЙ И НИ ОДНОЙ НЕ ДОБАВИЛ. Все
# шестнадцать принадлежали абзацам, чей носитель снят вместе с матрицей тарифов:
# модулю правила перехода, пяти функциям доли месяца и веткам решения о плане в
# `_apply_extension`. Потолок понижен ТЕМ ЖЕ коммитом и в точности до числа
# оставшихся записей — иначе разница была бы запасом, то есть местом для нового
# ложного абзаца.
#
# ⚠️ ПЛАН 08-05 УБРАЛ ОТСЮДА ОДНУ ЗАПИСЬ И НИ ОДНОЙ НЕ ДОБАВИЛ: 21 → 20. Запись
# принадлежала абзацу `create_payment` «ЧТО ЗАКРЫЛО БЫ ОКНО СВОЙСТВОМ, А НЕ
# ФОРМУЛИРОВКОЙ», и причиной её было прямым текстом «описание НЕ сделанной
# работы: закреплять тестом нечего, пока работа не сделана». РАБОТА СДЕЛАНА:
# частичный уникальный индекс построен ревизией `0021` и объявлен
# `__table_args__` модели, абзац переписан НА СДЕЛАННОЕ и назвал своих
# свидетелей. Долг закрыт РАБОТОЙ, а не переформулированием, и потолок понижен
# ТЕМ ЖЕ коммитом в точности до числа оставшихся записей — иначе разница стала бы
# запасом, то есть местом для нового ложного абзаца.
# --- app/application/billing/ (план 05-25, задача 2) ---
app/application/billing/subscription_period.py :: <module> :: ГРАНИЦЫ МОДУЛЯ (по образцу app/application/analytics/send_an :: МАШИННО ПРОВЕРЯЕМО, свидетеля сегодня нет: утверждение «модуль ничего не пишет и не знает про БД» проверяется тестом по сырому тексту модуля, тест не написан; работа своего размера
app/application/billing/subscription_period.py :: <module> :: ПОЧЕМУ СТАНДАРТНАЯ БИБЛИОТЕКА, А НЕ СТОРОННИЙ `relativedelta :: довод о выборе зависимости, а не утверждение о поведении: почему стандартная библиотека вместо стороннего пакета
app/application/billing/subscription_period.py :: countdown_base :: Публичная, а не приватная, именно поэтому: журналу нужна ТА  :: довод о видимости символа, а не утверждение о поведении функции
# --- app/services/payment_service.py (план 05-25, задача 3) ---
app/services/payment_service.py :: PaymentCreationError :: ЗАЧЕМ ОТДЕЛЬНЫЙ ТИП. Вызывающий обязан отличить «платёж не с :: довод о выборе типа исключения, а не утверждение о поведении
app/services/payment_service.py :: PendingIntentCapError :: ЗАЧЕМ СВОЙ ТИП, А НЕ `PaymentCreationError`. Тот говорит «ЮK :: довод о выборе типа исключения, а не утверждение о поведении
app/services/payment_service.py :: create_payment :: ЧЕМ ИМЕННО КОНЧАЕТСЯ ЭТО ОКНО В ПЛОСКОЙ МОДЕЛИ, НАЗВАНО ЗДЕС :: описание записанного долга фазы (окно срока давности не закрыто), а не утверждение о сегодняшнем поведении; абзац переписан фазой 05.1 — запись ЗАМЕНЕНА, а не добавлена
app/services/payment_service.py :: _claim_payment :: ЭТО COMPARE-AND-SWAP, А НЕ ПРОВЕРКА С ПОСЛЕДУЮЩЕЙ ЗАПИСЬЮ. У :: МАШИННО ПРОВЕРЯЕМО, свидетеля сегодня нет: форма оператора (условие и запись в одном UPDATE) проверяется разбором дерева, тест не написан
app/services/payment_service.py :: _claim_payment :: РАБОТАЕТ ОДИНАКОВО НА PostgreSQL И НА SQLite, и это принципи :: свойство БОЕВОГО окружения: тест потребовал бы работы своего размера, свойство числится человеческой проверкой фазы (суита идёт на SQLite, боевой диалект PostgreSQL)
app/services/payment_service.py :: _mirror_claim :: Значения ставятся `set_committed_value`, а не присваиванием: :: МАШИННО ПРОВЕРЯЕМО, свидетеля сегодня нет: приём проверяется разбором тела функции, тест не написан
app/services/payment_service.py :: handle_webhook :: ПОРЯДОК ПРОВЕРОК ЗНАЧИМ И МЕНЯТЬСЯ НЕ ДОЛЖЕН: событие → нали :: МАШИННО ПРОВЕРЯЕМО, свидетеля сегодня нет: порядок проверяется AST-ловушкой того же вида, что у признака живости, тест не написан
app/services/payment_service.py :: handle_webhook :: ЗАЩИТА ОТ ДВОЙНОГО НАЧИСЛЕНИЯ ДЕРЖИТСЯ ДВУМЯ МЕХАНИЗМАМИ, И :: заголовок перечня: каждый из двух механизмов закреплён своим тестом в соседних абзацах
app/services/payment_service.py :: handle_webhook :: * `_claim_payment` — условный UPDATE, держит НАЛОЖИВШИЕСЯ до :: пункт перечня механизмов; сам механизм закреплён тестами наложившихся доставок
app/services/payment_service.py :: handle_webhook :: Проверка терминального статуса стоит ДО ветвления намеренно, :: МАШИННО ПРОВЕРЯЕМО, свидетеля сегодня нет: порядок проверяется AST-ловушкой, тест не написан
app/services/payment_service.py :: handle_webhook :: ПРЕДМЕТ ПОКУПКИ РЕШАЕТ КОЛОНКА `kind` ИЗ БД, никогда `metada :: МАШИННО ПРОВЕРЯЕМО, свидетеля сегодня нет: отсутствие чтения `metadata` на этом пути проверяется разбором дерева, тест не написан
app/services/payment_service.py :: _extend_subscription :: ЗАПРОС АКТИВНОЙ ПОДПИСКИ ПОВТОРЯЕТ `get_shell_context` ДОСЛО :: МАШИННО ПРОВЕРЯЕМО, свидетеля сегодня нет: дословное совпадение двух запросов проверяется сверкой деревьев, тест не написан
app/services/payment_service.py :: _extend_subscription :: ВТОРАЯ АКТИВНАЯ СТРОКА С РЕВИЗИИ 0018 НЕВОЗМОЖНА НА УРОВНЕ С :: свойство БОЕВОГО окружения: тест потребовал бы работы своего размера, свойство числится человеческой проверкой фазы (частичный уникальный индекс живёт в ревизии 0018, невыкаченной; записанный долг фазы)
app/services/payment_service.py :: <module> :: ТЕРМИНАЛЬНЫЕ СТАТУСЫ — те, из которых платёж больше не выход :: объяснение константы, а не утверждение о поведении функции
app/services/payment_service.py :: <module> :: СРОК ДАВНОСТИ НЕЗАКРЫТОГО ПОДПИСОЧНОГО НАМЕРЕНИЯ, В ЧАСАХ. З :: объяснение константы и записанного долга фазы, а не утверждение о поведении
app/services/payment_service.py :: <module> :: Подписка на событие `payment.canceled` в кабинете ЮKassa НЕ :: свойство БОЕВОГО окружения: тест потребовал бы работы своего размера, свойство числится человеческой проверкой фазы (состояние кабинета ЮKassa; D-27, записанный долг фазы)
app/services/payment_service.py :: <module> :: ЗНАКОМЫЕ СОБЫТИЯ — КОНСТАНТАМИ SDK, НИКОГДА СТРОКОВЫМИ ЛИТЕР :: МАШИННО ПРОВЕРЯЕМО, свидетеля сегодня нет: отсутствие строковых литералов событий проверяется разбором дерева, тест не написан
