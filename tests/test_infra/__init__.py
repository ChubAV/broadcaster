# ⚠️ ФАЙЛ СУЩЕСТВУЕТ РАДИ УНИКАЛЬНОСТИ ИМЁН МОДУЛЕЙ СУИТЫ, А НЕ РАДИ СТИЛЯ.
#
# Без него pytest импортирует тестовый файл ПО БАЗОВОМУ ИМЕНИ, и два файла с
# одинаковым именем в разных каталогах суиты сталкиваются: прогон падает на
# СБОРКЕ («import file mismatch»), не выполнив ни одного утверждения. Довод
# записан дословно в `tests/test_pages/__init__.py` и повторяется здесь потому,
# что он тот же.
#
# КАТАЛОГ ЗАВЕДЁН ОТДЕЛЬНЫЙ, А НЕ РАСТВОРЁН В СОСЕДНИХ. Гейты здесь читают
# АРТЕФАКТЫ РАЗВЁРТЫВАНИЯ — файлы, описывающие боевой стенд, — и не касаются ни
# приложения, ни его схемы, ни разметки. Ближайший по предмету сосед
# (`tests/test_migrations/test_deploy_applies_migrations_before_serving.py`)
# лежит в каталоге ревизий по своему поводу: он про очередь миграций. Класть
# сюда гейт приложения значило бы завести каталог, про который через фазу никто
# не скажет, что в нём лежит.
