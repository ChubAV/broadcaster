"""Правила, чей предмет есть ЗАПИСЬ проекта, а не его продукт.

⚠️ КАЖДЫЙ МОДУЛЬ КАТАЛОГА НЕСЁТ `pytestmark = pytest.mark.planning`, И ПОМЕЧЕНЫ ВСЕ
ДО ЕДИНОГО. Пропущенный модуль сделал бы отрицательный отбор ЛГУЩИМ: `-m "not
planning"` собрал бы правило записи, объявив его продуктовым, и следующий читатель
сделал бы вывод о продукте по отказу документа.

ЗАЧЕМ МАРКЕР. Затем, чтобы продуктовую суиту и суиту записи можно было ПРОГНАТЬ И
ДИАГНОСТИРОВАТЬ ПОРОЗНЬ. Правило записи, покрасневшее в общем прогоне, до маркера
давало один и тот же диагноз, что и отказ продукта, — и накопленное давление
кончается тем, что каталог добавляют в игнорируемые ЦЕЛИКОМ.

⚠️ ГРАНИЦА ГОДНОСТИ: МАРКЕР НЕ ЕСТЬ РАЗРЕШЕНИЕ НЕ ПРОГОНЯТЬ. Каталог держит
ЕДИНСТВЕННОЕ машинное принуждение регламента отметок требований (планы 10-14, 10-17)
и ЕДИНСТВЕННУЮ защиту обхода от самозаверения (план 10-20). Добавление каталога в
игнорируемые либо постоянный прогон под `-m "not planning"` означал бы потерю обоих
принуждений — это ЗАПРЕЩЕНО прохибицией плана 10-26. То же утверждение записано
рядом с объявлением маркера в `tests/conftest.py`: читатель приходит с обеих сторон,
и два места одного утверждения обязаны говорить одно.

Имя маркера объявлено ОДНИМ местом на проект — хуком `pytest_configure` в
`tests/conftest.py`; основание выбора места записано там же.

Источник: `WR-07` пятого круга ревизии кода Фазы 10 (`10-REVIEW.md`, 2026-09-07),
план 10-26.
"""
