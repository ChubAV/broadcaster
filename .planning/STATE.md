---
gsd_state_version: "1.0"
milestone: v2.1
milestone_name: HTMX-first
current_phase: 12
current_phase_name: Загрузка изображений без `fetch()`
status: executing
stopped_at: Completed 12-10-PLAN.md
last_updated: "2026-09-19T10:51:43.162Z"
last_activity: 2026-09-19
last_activity_desc: Phase 12 execution started
state_head: f149030c916a0529089a1cb34f9453d9ccdd3e2b
progress:
  total_phases: 9
  completed_phases: 5
  total_plans: 129
  completed_plans: 126
  percent: 56
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-08-29)

**Core value:** Надёжно выполнять периодические рекламные рассылки в группы нескольких мессенджеров по заданному пользователем расписанию.
**Current focus:** Phase 12 — Загрузка изображений без `fetch()`

**Пройдено в вехе v2.1: Фазы 7 и 8** (18 планов). Фаза 8 закрыта 2026-08-29 — 11/11 планов, верификация `passed`, UAT 9/9, `threats_open: 0`.

**Нумерация фаз вехи v2.1 — 7…15.** Веха v2.0 закончилась Фазой 6, и отсчёт продолжается, а не начинается заново. Это отличие от v2.0, которая сознательно начала с 1 (фазы v1.0 были ретроспективной документацией уже отгруженной системы, а не выполненными GSD-фазами).

**Следующий вход:** `/gsd-discuss-phase 9`. Состав и обоснования — `.planning/ROADMAP.md`, требования — `.planning/REQUIREMENTS.md`, разведка — `.planning/research/SUMMARY.md`.

## Current Position

Phase: 12 (Загрузка изображений без `fetch()`) — EXECUTING
Plan: 10 of 10
Total Plans in Phase: 10
Completed Plans in Phase: 6
Status: Ready to execute
Last activity: 2026-09-19 — 12-07 исполнен: гапы 2 и 3 верификации и находки CR-01/WR-04 закрыты РАБОТОЙ по решениям владельца D-16 и D-17. Ответ автосохранения перестал перерисовывать полосу вложений БЕЗ НУЖДЫ: `_autosave_response` принял `repaint_media` с ЛОЖНЫМ умолчанием, `_save_from_editor` ставит признак по ФАКТУ убирания ключа (а не по присутствию поля `remove_image`), включение полосы в `autosave_response.html` обёрнуто условием. Круг замкнут самим ответом загрузки — её заголовок `HX-Trigger-After-Swap: ads-image-attached` поднимает слушатель формы объявления, — и потому строка отказа смешанной партии не доживала до второго взгляда на экран: D-04 платил лживым кодом 200 впустую. Покупка плана 12-03 не потеряна — правило `test_autosave_response_carries_the_media_tray` ПЕРЕНАЦЕЛЕНО на путь убирания (три утверждения дословно, летопись в докстринге), а не ослаблено. Форма загрузки получила `sync='this:queue last'` — ту же стратегию наложения, что форма объявления; равенство держит НОВОЕ правило, читающее ДВА РАЗНЫХ способа записи одного свойства (литерал `hx-sync` тега `#ad-form` против аргумента вызова макроса-обёртки), с контролем зубов подстановкой `this:drop` с ОБЕИХ сторон. Запрет Фазы 8 на слово `queue` прочитан ПО ИСХОДНИКУ до правки: он проведён по маршрутам ДЕНЕЖНОГО периметра, и форма редактора объявлений названа там законным носителем очереди прямым текстом; `app/pages/ads.py` постановок задач и создания платежей не содержит. Впервые в суите заведены правила, рендерящие ДВА ФРАГМЕНТА ДРУГ ПРОТИВ ДРУГА по гапу 2: `test_a_refusal_row_survives_the_autosave_the_same_response_triggers` подаёт смешанную партию на `/ads/images`, вынимает из ответа строки отказа и скрытые поля и шлёт ровно их в `POST /ads/{id}/edit`. Кромка `concurrency` пробника на FETCH-01 закрыта НАЗВАННЫМ допущением (D-17): две записи `accepted-assumption` в `deferred-items.md` — объекты-сироты с межвкладочным счётом свободных мест (источник IN-05, настоящая квота — `own_image_keys` на сохранении) и потеря СРЕДНЕГО выбора файлов вытесняющей очередью (остаток выбранной D-16 стратегии). Обе улики RED замерены прогоном и проверены прибором (`RED_EVIDENCE_OK`, `target_test_failed`). Регрессия: `tests/test_pages` + `tests/test_templates` **2050 passed, 0 failed** (32:42), `tests/test_services` + `tests/test_routes` + `tests/test_planning` **618 passed**; инвентарь внеполосных блоков не сдвинулся. FETCH-01 по-прежнему НЕ отмечен — требование объявлено и планами 12-08…12-10.
Last activity (устарело, предмет — план 12-06; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-19 — 12-06 исполнен: гап 1 верификации (критерий 2 ROADMAP) и находка CR-02 закрыты РАБОТОЙ. Ветвь отказа сверки ключей `ads_images_upload` перестала отвечать авторитетно выглядящей ПУСТОЙ полосой и отвечает ПОДТВЕРЖДЁННЫМ подмножеством: `partition_own_image_keys` (`app/services/image_keys.py`) — единственный предикат принадлежности в дереве (грепом по `app/`: одно вхождение `match.group(1) != str(user_id)`), через него выражена и `own_image_keys`, поведение которой не сдвинуто ни на символ (отказ по длине первым, тот же `INACCESSIBLE_IMAGE_MESSAGE`, тот же список). Зонд А верификатора инвертирован: смешанная партия «свой + чужой» отдаёт свой ключ скрытым полем, чужого в тексте ответа нет, строка отказа одна, `mock_s3.call_count == 0` (T-12-06-02 жив). Зонд Б инвертирован: 11 своих законных ключей при потолке 10 остаются все одиннадцать, входящий файл получает `upload_limit_message(10)`, плитка «+ ФАЙЛ» скрыта, заголовка события нет; `free = max(0, …)` записывает разделение полномочий — потолок управляет НОВЫМИ файлами, а не судьбой прикреплённых. ⚠️ RED задачи 2 оказался НЕОЖИДАННО ЗЕЛЁНЫМ и РАССЛЕДОВАН: поведение приехало задачей 1 (разделяющий предикат снял с пути загрузки отказ по длине), поэтому коммит `refactor`, а непустота правила ДОКАЗАНА мутантом — восстановление прежнего отказа краснит его дословным «во фрагменте 0 скрытых полей вместо одиннадцати» при зелёном соседе про свободные места, порцелан после возврата пуст. Впервые в суите заведено правило на ДВА обработчика: ответ загрузки разбирается на скрытые поля, ровно они уходят в `POST /ads/{id}/edit`, `Ad.images` перечитывается — порознь оба обработчика были зелены, и работа терялась на стыке. Остаток (б) второго `missing` гапа НАЗВАН, а не закрыт (комментарий у расчёта свободных мест; `Ad.images` в срезе обработчика 0 → 1). RED задачи 1 замерен прогоном («2 failed, 14 passed», оба отказа — отказы утверждений, прибор дал `RED_EVIDENCE_OK`), GREEN — «22 passed». Регрессия: `tests/test_pages` **1824 passed, 0 failed** (32:19), `tests/test_services` + `tests/test_routes` **574 passed**. FETCH-01 НЕ отмечен: `requirements.ready-ids` даёт 0/1 — требование объявлено и планами 12-07…12-10.
Last activity (устарело, предмет — открытие исполнения фазы 12; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-19 — Phase 12 execution started
Last activity (устарело, предмет — план 11-21; строка восстановлена обходом `/gsd-verify-work 11` по идиоме D-30/D-32 — её затёрло закрытие фазы `phase.complete`): 2026-09-17 — 11-21 исполнен: гэп G-11-6 закрыт ОДНИМ местом. Скрытый индикатор `.form-busy` форм обёртки выведен из потока: макрос печатает литерал класса области `form-wrapper` сразу после адреса запроса, стили дают форме обёртки контекст позиционирования и кладут её индикатор в правый нижний угол коробки формы нулевыми смещениями, прозрачным для указателя (T-11-42: у тумблеров расписания цель блокировки пустая). Четырнадцать вызывающих не тронуты (D-14). Область задаёт ТРЕТЬЕ имя — не класс индикатора и не селектор поиска, — потому что панель подтверждения печатает ДОСЛОВНО тот же узел: её ~22 px приняты (10-UAT 3.5), и решение владельца 1 от 2026-09-17 вывело панели из объёма. Неподвижность панели ДОКАЗАНА: `test_the_confirmation_panel_indicator_keeps_its_accepted_place` с тремя подстановками (селектор без области, класс области на теге панели, позиция в базовом правиле) плюс два мутанта рабочей копии — А «2 failed, 2 passed» с отказом, назвавшим `form > .form-busy`, Б «2 failed, 2 passed» с отказом, назвавшим класс области в теге панели; `components/modal.html` не тронут ни на символ, порцелан после возврата пуст. Точка строки группы ОСТАЛАСЬ рядом с тумблером — второе исключение объёма и решение владельца 3, объявленное ОДНОЙ записью `IN_FLOW_INDICATOR_EXCEPTIONS` с обоснованием и числом `= 1`. Обоснование различия «имя класса формы» и абзац раздела сличения получили ПОКОЛЕНИЕ (D-30/D-32): прежний текст «обёртка класса формы не знает вовсе» верен для дерева до 11-21 и не вычёркивается. Числа не сдвинуты: базовое правило `.form-busy` одно, `PANEL_QUALITY_DIFFERENCES_ALLOWED = 2`, контролей файла 24; `pointer-events: none;` 1 → 2. RED задачи 1 замерен прогоном («2 failed», оба отказа — отказы утверждений, `RED_EVIDENCE_OK`), GREEN — «2 passed». Полный прогон БЕЗ отбора маркером: **3410 passed, 0 failed** (37:48) — ночное окно админ-обзора не сработало (прогон вне 00:00–05:00 UTC). ⚠️ Наблюдение проверки 6 `11-UAT.md` НЕ снято: оно за человеком в `/gsd-verify-work 11`, и таблица отметки оставлена пустой.
Last activity (устарело, предмет — план 11-20; строка восстановлена планом 11-21 по идиоме D-30/D-32 — её затёрла переустановка позиции перед партией закрытия гэпов): 2026-09-17 — 11-20 исполнен: GATE-02 закрыт числом ОБХОДА. `_post_302_assertions` разбирает дерево `tests/`, приписывает каждое утверждение `status_code == 302` о POST обработчику по таблице маршрутов собранного приложения и требует пары у каждого переведённого; `PAIRED_302_ASSERTIONS_DECLARED = 158`, 12 неразобранных поимённо в `UNATTRIBUTED_302_ASSERTIONS`. Докстринги `app/pages/htmx.py` перемерены (`15 1`). Летописи у критериев 1, 3, 4, 5, у FORM-03/FORM-04 и GATE-02; окно 51 `fixed`, окно 63 `open` с летописью D-08. Полная суита 3406 passed.
Last activity (устарело, предмет — план 11-19; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-17 — 11-19 исполнен: окно 51 закрыто РАБОТОЙ. `account_groups_toggle`/`account_groups_delete` (оба идентификатора пути) и `history_retry` на `PostIdPath`; `id_in_column` первым использованием до первой выборки; вне колонки — ветка «строки нет» с адресом из `account_id` пути у групп и «записи нет» (`/history`) у повтора. `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` 5 → 0; `test_no_page_post_input_keeps_a_framework_bound` сличает обход замера с `_post_handlers` и `POST_HANDLERS = 36`, синтетический вход с границей краснит помощник поимённо. Контроли реестра на синтетических входах (задача 1 до опустения). Реестр пар 43 → 48. Окно 51 в `.planning/WINDOWS.md` оставлено `open` для команды реестра плана 11-20.
Last activity (устарело, предмет — план 11-18; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-17 — 11-18 исполнен: последний обработчик фазы на слое ответа. `accounts_connect_max_start`: нет сессии `respond(redirect="/login")`, телефон из пробелов `respond_field_error` (страница мастера / фрагмент шага, 422 на обоих транспортах, эхо присланного значения только автоэкранированием поля — доказано на `_max_step_markup` с враждебным значением), финальный выход `respond(redirect="/accounts", fragment=…)`; `RedirectResponse` в обработчике 0, `asyncio.sleep(5)` сохранён. Шаг мастера — `accounts/includes/max_connect_step.html` (вынос отдельным коммитом, пять ветвей отрисовки совпали по токенам); `id="max-connect-step"` на самой колонке `.connect-shell`, потому что алерт ошибки стоит вне карточки. Адрес деградации `/accounts` сверен с кодом: только опрос переводит `connecting → syncing`, GET мастера удаляет `connecting`. Числа прогоном: 15 → 14, фрагментных 11 → 12, пар 41 → 43, скрытых вызывающих 21 → 22, параметрических 6 → 7, блоков вызова 13 → 14, вызовов перехода 71 → 72 (план его не назвал). Полная суита 3345 passed / 1 failed — ночное окно `test_the_overview_error_number_matches_the_users_own_dashboard`.
Last activity (устарело, предмет — план 11-17; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-17 — 11-17 исполнен: раздел `accounts` закрыл оба навигационных действия синхронизации. `accounts_sync_groups` отвечает `await respond(request, redirect=…)` на всех восьми выходах — «нет сессии» `/login`, «аккаунта нет» (чужой и вне колонки той же веткой) `/accounts`, остальные шесть на экран групп; `RedirectResponse` в обработчике 0. Заявка `_SYNC_IN_FLIGHT`: по AST между `_claim_sync_slot` и внешним `try` ноль операторов, `return` после `try` нет, `_release_sync_slot` один — в `finally`. Поведением: тест четырёх исходов (успех, отказ моста, неожиданное исключение, конфликт уникальности) требует пустой реестр И дошедшую до мессенджера следующую синхронизацию; мутант «освобождать только без htmx» краснит 4/4 при зелёных трёх случаях пар; мутант «выход «заявка занята» освобождает» краснит только новый тест занятой заявки. Форма шапки экрана групп — `form_wrapper` без цели, `data-syncing` на обёртке `<div>`, правила CSS `[data-acct-head] [data-syncing] .btn__icon`; атрибуты сняты рендером до/после (было только `method`, `action`, `data-syncing`). Три входа (`delete`, `retry-sync`, `sync-groups`) на `PostIdPath` с `id_in_column` первым использованием — окно 51 8 → 5. Числа прогонами: 16 → 15, пары 37 → 41, вызовы перехода 63 → 71, скрытые вызывающие 20 → 21, блоки обёртки 12 → 13, подставленные объявления экрана групп 60 → 61. RED задач 1 и 3 `RED_EVIDENCE_OK`. Широкий прогон pages/routes/templates/messengers: `1 failed, 2409 passed` — единственный отказ известное ночное окно админ-обзора
Last activity (устарело, предмет — план 11-16; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-17 — 11-16 исполнен: раздел `accounts` открыт первым из двух навигационных действий синхронизации. `accounts_retry_sync` отвечает `await respond(request, redirect=…)` на всех трёх выходах — «нет сессии» `/login`, «аккаунта нет» (чужой и не `wa`/`max` той же веткой, T-11-29) `/accounts`, успех `/accounts/{id}/groups`; порядок работы (мост, статус `syncing`, отправка задачи Celery) до ответа не сдвинут, `RedirectResponse` в обработчике 0. Заявку `_SYNC_IN_FLIGHT` этот вход не занимает (проверено по коду — её берёт только `accounts_sync_groups`, план 11-17), поэтому выходу `HX-Location` освобождать нечего. Три копии формы (`accounts/list.html`, `partial_cards.html`, `partials/sync_status_card.html`) — `form_wrapper` без цели (`hx-swap="none"`), в макрос не сведены (D-12(в)); сохранность атрибутов ЗАМЕРЕНА рендером трёх адресов до и после: у старой формы только `action` и `method`, потерь нет, кнопка байт в байт та же. Числа прогонами: отставание 17 → 16, пары 33 → 37 (четвёртый случай «аккаунт чужой» сверх §behavior), вызовы перехода 60 → 63, скрытые вызывающие обёртки 17 → 20, блоки вызова обёртки 9 → 12. RED `RED_EVIDENCE_OK` (7 tests, 6 failures). Широкий прогон pages/routes/messengers: `1 failed, 2178 passed` — единственный отказ известное ночное окно админ-обзора
Last activity (устарело, предмет — план 11-15; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-15 исполнен: денежный маршрут переведён на слой ответа. Третий выход `redirect_external` (`app/pages/htmx.py`) пишет `HX-Redirect` ТОЛЬКО на адрес, прошедший `_confirmation_url`: схема https, хост точным сравнением из `YOOKASSA_CONFIRMATION_HOSTS = frozenset({"yoomoney.ru"})` (константа, не настройка — OQ3), без порта, без `@` в узле, без обратной косой, ASCII, без управляющих; 18 написаний подделки — отдельные случаи. Провал — журнал `payment_confirmation_url_rejected` с хостом без адреса и `respond(/billing, payment_failed)`; без htmx прежний 302 без проверки (асимметрия в `SAFE_BY_NAME`). `subscribe_to_plan`: отказы на `respond()`, успех на `redirect_external`, `RedirectResponse` в обработчике 0, голый 403 — запись `OWN_RESPONSE_EXITS` в `DECISION_OWNER_D08`; форма на `form_wrapper` без цели и без `hx-sync`, `data-plan-cta` на обёртке. Пары на документированном хосте через настоящий `create_payment` (потолок PAY-01 вторым нажатием); фикстуры `yookassa.ru` путей без htmx не тронуты. Числа прогонами: записи заголовков 2 → 3, `SAFE_BY_NAME` 1 → 2, собственные выходы 11 → 12, отставание 18 → 17, пары 28 → 33, вызовы перехода 56 → 60 (+`/billing`), вызывающие обёртки 16 → 17, блоки 8 → 9, места записи кода 4 → 1 (последнее найдено полным прогоном исполнителя: 1 failed, 3320 passed). ⚠️ Ручной UAT критерия 2 (фактический хост `confirmation_url` на тестовом ключе) НЕ проведён — окно 87, до слияния
Last activity (устарело, предмет — план 11-14; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-14 исполнен: шестой частный микро-контракт адресной строки сведён в `?notice=` (D-10). Четыре исхода снятия задачи из очереди — коды `QUEUE_DROP_REMOVED/MISSING/UNAVAILABLE/NO_QUEUE` в `app/pages/notices.py` с текстами и вариантами, перенесёнными посимвольно; `admin_drop_task` отвечает `respond(notice=…)` на всех трёх выходах, включая ветку идентификатора вне колонки плана 11-11; `QUEUE_DROP_RESULTS`, параметр исхода `admin_queue` и место отрисовки в `admin/queue.html` сняты; транспорт остался переходом, `queue_row.html` не тронут. `?result=` в `app/`: 3 → 0 под `RETIRED_QUERY_KEYS` (5 → 6) с отрицательным контролем на новом ключе; RED гейта замерен на дереве до перевода во временном рабочем каталоге (`1 failed, 5 passed`, RED_EVIDENCE_OK). Записи с поколениями: комментарий `billing.py` (реестров было ЧЕТЫРЕ), пункт §Active `PROJECT.md` закрыт, шапка `includes/notice_area.html` и `PANEL_REFUSAL_CODES` (панель очереди выдаёт три кода отказа, два — предупреждения, едут в площадку; новый перечень `PANEL_POLITE_REFUSAL_CODES`). Числа прогонами: записей реестра 15 → 19, `MOVED_TEXTS` 11 → 15. Полный прогон 3340 passed
Last activity (устарело, предмет — план 11-13; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-13 исполнен: `admin_toggle_free_access` переведён на слой ответа — второй фрагментный обработчик раздела `admin`. На htmx успех отвечает 200 ТЕМ ЖЕ ответом, что блокировка: сборка вынесена в модульную `_user_actions_response`, которую зовут оба тумблера, шаблон `admin/partials/user_actions_response.html` один. `invalidate_access_cache` стоит ДО вызова слоя, фрагмент ленивый; порядок стережёт `test_free_access_fragment_is_assembled_after_the_access_cache_is_dropped` журналом вызовов — мутант «сборка до сброса» краснит его (`['access_view', 'invalidate:2']`), а тест одного значения `∞` на том же мутанте зеленел (плитка читает строку подписки, не кэш). «Пользователя нет» и «строки подписки нет» — 204 + `HX-Location`; без htmx прежний 302. Голый 403 не тронут; запись `OWN_RESPONSE_EXITS` в `DECISION_OWNER_D08`, девять прежних записей переведены туда же по тексту D-08 («к девяти записям»); окно 63 остаётся открытым. Поколения: `LIFTING_CONDITION_OWN_RESPONSE`, граница `DECISION_OWNER_D08`, докстринг `app/pages/htmx.py` (одиннадцать выходов). Форма бесплатного доступа на `form_wrapper(target='#user-actions', swap='innerHTML')`. Числа прогонами: 19 → 18, 10 → 11, 10 → 11, 54 → 56, пары 25 → 28, блоки обёртки 7 → 8
Last activity (устарело, предмет — план 11-12; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-12 исполнен: `admin_toggle_block` переведён на слой ответа — первый фрагментный обработчик раздела `admin`. На htmx успех отвечает 200 с содержимым `#user-actions` (подпись тумблера по новому состоянию) и двумя внеполосными узлами `innerHTML:#user-block-badge` и `innerHTML:#user-access-tile`, собранными включением тех же шаблонов плана 11-11; «пользователя нет», вне колонки и «себя» — 204 + `HX-Location`; без htmx прежний 302. Страница получила три постоянные обёртки (пустой бейдж — `display: contents`). Голый 403 остался (D-08 владельца) и объявлен записью `OWN_RESPONSE_EXITS` в состоянии `DECISION_OWNER_D08`; `_OWN_RESPONSE_PRICE` получил второе поколение — «плашка не рисуется» опровергнуто (пропускается только 422). Форма блокировки на `form_wrapper(target='#user-actions', swap='innerHTML')`; форма бесплатного доступа ждёт плана 11-13. Числа прогонами: 20 → 19, 9 → 10, 9 → 10, 52 → 54, OOB 17 → 19, пары 21 → 25, вызывающие обёртки 15 → 16, параметрические 5 → 6, блоки обёртки 6 → 7
Last activity (устарело, предмет — план 11-11; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-11 исполнен: раздел `admin` открыт ДВУМЯ шагами без смены транспорта. Содержимое блока «Действия», бейдж блокировки и содержимое плитки доступа карточки пользователя вынесены ДОСЛОВНО во включаемые шаблоны `admin/includes/user_actions.html`, `user_block_badge.html`, `user_access_tile.html` — единственный источник разметки для страницы и будущих ответов тумблеров (планы 11-12, 11-13). Невидимость выноса ИЗМЕРЕНА, а не заявлена: вывод `/admin/users/{id}` снят до и после в четырёх состояниях (обычный, заблокированный с льготой, закрытый доступ, своя карточка) и совпал байт в байт с точностью до минуты часов в дате пробного срока. Шесть входов (`admin_restart_worker`, `admin_drop_task`, `admin_toggle_free_access`, `admin_impersonate`, `admin_toggle_block`, `admin_delete_user`) перешли на `PostIdPath`: первое использование — `id_in_column` после сверки источника и до выборки, величина вне колонки идёт веткой «записи нет». Окно 51 14 → 8 поставлено отказом «стало 8, а объявлено 14»; строки матрицы границ остались и ждут ветку «нет» полем `outside`; `ROW_DELETE_SITES` называет новый файл формы удаления
Last activity (устарело, предмет — план 11-10; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-10 исполнен: форма профиля стала формой htmx НАД ОБЁРТКОЙ, и цель у неё ОДНА на два исхода. `profile.html` держит постоянную обёртку `#profile-settings`, включаемый шаблон — её содержимое; и успех (200 с сохранённым поясом), и ошибка поля (422 с присланным) подменяют СОДЕРЖИМОЕ этой обёртки, а не узел (D-12 Фазы 9). Совпадение целей — не удобство, а ОСНОВАНИЕ: ровно на нём перечень FORM-10/D-16 заведён ПУСТЫМ — перенацеливать заголовком нечего. Пустота не вакуумна: отрицательный контроль подставляет синтетический модуль с применением заголовка и требует, чтобы сканер НАШЁЛ его и назвал ключом `модуль::функция`; без контроля «применений нет» было бы неотличимо от «сканер ослеп». Обработчик не тронут НИ НА СТРОКУ — план 11-09 сделал его фрагментным заранее, и 45 тестов профиля и обезличивания зелены без правок. Три разметочных числа поставлены ПРОГОНАМИ покрасневших правил, каждое с летописью и дословным отказом: скрытые вызывающие 14 → 15, вызывающие параметрических целей 4 → 5, блоки вызова обёртки 5 → 6
Last activity (устарело, предмет — план 11-09; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-09 исполнен: своп правилу 422 ВЕРНУЛСЯ — ОДНИМ коммитом (`4ba02ef`) вместе с первым маршрутом, отдающим этот код АВТОРСКИМ фрагментом: сохранением часового пояса в профиле. Порядок доказан историей, а не обещанием: смягчение T-07-13 приземлилось ОТДЕЛЬНЫМ планом 11-07 (`cc72871`) РАНЬШЕ, поэтому дерева, на котором подмена включена без обработчика, в вехе не существует. Профиль стал ПЕРВЫМ потребителем ветки «код исхода на фрагменте» — приклейка внеполосного блока существовала с Фазы 8 и не использовалась НИ ОДНИМ обработчиком. Ошибка поля отвечает 422 на ОБОИХ транспортах, отсутствующее поле идёт веткой обработчика, а не отказом фреймворка. Счётчики поставлены ПРОГОНАМИ: отставание 21 → 20, фрагментные обработчики 8 → 9, вызовы перехода 51 → 52, пары 19 → 21, места записи кода исхода 5 → 4 (последнего план не называл — найден прогоном соседних модулей)
Last activity (устарело, предмет — план 11-08; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-08 исполнен: слой ответа получил `respond_field_error` — ЕДИНСТВЕННЫЙ литерал 422 приложения (`SERVER_SIDE_VALIDATION_RESPONSES` 1 → 2 поставлено покрасневшим прогоном); маршрутов, его зовущих, нет ни одного, и правило 422 по-прежнему `"swap": false` — своп приземляет план 11-09 ОДНИМ коммитом с первым вызывающим. Настройки профиля вынесены в `includes/profile_settings.html` — один источник формы для страницы и обоих будущих фрагментов, ошибка поля переехала от карточки К ФОРМЕ (D-12 Фазы 8), обработчик не тронут
Last activity (устарело, предмет — план 11-07; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-16 — 11-07 исполнен: смягчение T-07-13 приземлилось ОТДЕЛЬНЫМ планом ДО возврата свопа (коммит `cc72871`); отказ валидации страничного слоя на пути htmx — пустой 400 при любом месте ошибки, JSON-API байт-в-байт прежний на обоих транспортах
Last activity (устарело, предмет — закрытая Фаза 10; строка НЕ вычёркивается по идиоме D-30/D-32): 2026-09-11 — Phase 10: план 10-51 исполнен (RED→GREEN замерен переходом цвета правила безусловности подъёма — `1 failed, 229 deselected` при rc=1 с отказом, печатающим полученный селектор `.is-modal-open #htmx-failure-server, .is-modal-open #htmx-failure-network`, → `1 passed` при rc=0; улика проверена прибором `RED_EVIDENCE_OK`; модуль шелла `230 passed` при пороге сбора 223, полная суита `3182 passed`, rc=0, 38:20; продукт тронут ДВУМЯ строками преамбулы `app/static/css/app.css` — величины блока подъёма, правило блокировки прокрутки, рычаг и заготовки не тронуты)
Progress: [████████████████████] 116/116 plans ([██████░░░░] 56%) — счёт идёт по СПЛАНИРОВАННЫМ планам (Фазы 7, 8, 9, 10 и 11); Фазы 12…15 ещё не спланированы. По фазам вехи: **5/9 (56%)** — Фаза 11 ЗАСЧИТАНА 2026-09-18: верификация `passed`, обход 8/8 при восьми заполненных отметках, `threats_open: 0`, Nyquist `validated`.

✅ **Фаза 09 ЗАКРЫТА и ОТГРУЖЕНА (PR #47, 2026-09-02).** Все двадцать планов имеют сводки, суита зелена (2649 passed), проведены ЧЕТЫРЕ круга ручного обхода (последний — 2026-09-02, Chrome / macOS), верификация `passed` 18/18 круга 5, `09-SECURITY.md` несёт `threats_open: 0`.

**Оба расхождения обхода ЗАКРЫТЫ** (круг 5, коммит `042b8be`):

- **DIV-09-01** — `hx-include='#group-list-sentinel'` при отсутствующем сентинеле. Закрыто ветвью владельца `conditional-include`: объявление включаемых данных панели стало условным (параметр `has_sentinel`). Второе следствие вынесено именованной записью `INCLUDE_TARGET_EXCEPTIONS` с назначенной Фазой 15.
- **DIV-09-02** — `hx-disabled-elt="find button[type=submit]"` при кнопке, снятой Alpine. Закрыто решением владельца `accept-override` (`chubav`, 2026-09-01): защиту несёт `hx-sync="this:drop"`, отступление объявлено `DISABLED_ELT_EXCEPTIONS` с летописью. Запись `overrides` стои́т во frontmatter `09-VERIFICATION.md` и продублирована в `.planning/REQUIREMENTS.md`.

Круг 5 ВПЕРВЫЕ не внёс зеркального отказа — цепочка четырёх кругов «починка → зеркальный дефект» разорвана. **Следующий вход: `/gsd-discuss-phase 10`.**

**Порядок исполнения:** 7 → 8 → 9 → 10 → 11 → 14 → 15; Фаза 12 (загрузка изображений) идёт параллельно Фазам 9-11, Фаза 13 (QR-мастер) — после 10 и до 14.

**Фазам 12, 13 и 14 при планировании нужен `--research-phase`** (по §Research Flags разведки): загрузка — три поведенческих РЕШЕНИЯ, а не перенос; QR-мастер — машина состояний над живой сессией Telethon; авторизация — смена cookie личности через границу двух шеллов. Фазам 7, 9, 10, 11 и 15 разведка не нужна: паттерны сняты с действующего кода. Фазе 8 разведка не нужна, но денежный узел PAY-01/PAY-02 требует явного решения в `/gsd-discuss-phase`.

## Performance Metrics

**Velocity:**

- Total plans completed: 166 (milestone v2.0) — ⚠️ прежнее значение 70 расходилось с суммой по фазам (13+15+12+12+30+14+14 = 110) и с frontmatter `progress.completed_plans: 110`; исправлено при закрытии вехи
- Average duration: N/A
- Total execution time: N/A

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 1. Интерфейсный фундамент | 13/13 | - | - |
| 2. Объявления и расписания | 15/15 | - | - |
| 3. Группы аккаунта | 12/12 | - | - |
| 04 | 12 | - | - |
| 05.1 | 14 | - | - |
| 06 | 14 | - | - |
| 05 | 30 | - | - |
| 07 | 7 | - | - |
| 08 | 11 | - | - |
| 10 | 57 | - | - |
| 11 | 21 | - | - |

**Recent Trend:**

- Тренда пока нет: v1 был ретроспективной документацией без plan-артефактов, v2.0 — первый milestone, исполняемый через GSD.

**Per-Plan Metrics:**

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| Phase 05 P21 | 38min | 3 tasks | 3 files |
| Phase 05.1 P02 | 21min | 2 tasks | 2 files |
| Phase 09 P10 | 1h 33m | 4 tasks | 4 files |
| Phase 09 P11 | 6h 59m | 3 tasks | 6 files |
| Phase 09 P12 | 2h 31m | 3 tasks | 3 files |
| Phase 10 P05 | 65 min | 3 tasks | 2 files |
| Phase 10 P06 | 56 min | 2 tasks | 1 files |
| Phase 10 P07 | 73 min | 3 tasks | 4 files |
| Phase 10 P08 | 59 min | 3 tasks | 2 files |
| Phase 10 P09 | 85 min | 3 tasks | 12 files |
| Phase 10 P10 | 17 min | 3 tasks | 1 files |
| Phase 10 P11 | 52 min | 3 tasks | 3 files |
| Phase 10 P12 | 63 min | 3 tasks | 2 files |
| Phase 10 P13 | 54 min | 2 tasks | 2 files |
| Phase 10 P14 | 1h 3m | 2 tasks | 1 files |
| Phase 10 P15 | 60 min | 3 tasks | 1 files |
| Phase 10 P16 | 42 min | 2 tasks | 2 files |
| Phase 10 P24 | 1h 23m | 3 tasks | 5 files |
| Phase 10 P25 | 44 min | 3 tasks | 2 files |
| Phase 10 P26 | 21 min | 2 tasks | 7 files |
| Phase 10 P27 | 47 min | 2 tasks | 2 files |
| Phase 10 P28 | 1h 47m | 3 tasks | 6 files |
| Phase 10 P29 | 2h 5m | 2 tasks | 3 files |
| Phase 10 P30 | 1h 2m | 3 tasks | 4 files |
| Phase 10 P31 | 54 min | 3 tasks | 4 files |
| Phase 10 P32 | 1h 7m | 3 tasks | 7 files |
| Phase 10 P33 | 1h 40m | 3 tasks | 3 files |
| Phase 10 P34 | 45 min | 3 tasks | 2 files |
| Phase 10 P35 | 27 min | 3 tasks | 4 files |
| Phase 10 P36 | 92 min | 2 tasks | 1 files |
| Phase 10 P37 | 27 min | 3 tasks | 1 files |
| Phase 10 P40 | 21 min | 2 tasks | 1 files |
| Phase 10 P38 | 31 min | 2 tasks | 1 files |
| Phase 10 P39 | 35 min | 3 tasks | 2 files |
| Phase 10 P41 | 54 min | 3 tasks | 3 files |
| Phase 10 P42 | 45 min | 3 tasks | 1 files |
| Phase 10 P43 | 15 min | 3 tasks | 1 files |
| Phase 10 P44 | ~35m | 3 tasks | 2 files |
| Phase 10 P47 | ~25m | 3 tasks | 1 files |
| Phase 10 P46 | 1h 20m | 3 tasks | 9 files |
| Phase 10 P45 | 36 min | 3 tasks | 1 files |
| Phase 10 P48 | 52 min | 3 tasks | 2 files |
| Phase 10 P49 | 70 min | 3 tasks | 2 files |
| Phase 10 P50 | 1h 02m | 3 tasks | 5 files |
| Phase 10 P51 | 3h 05m | 3 tasks | 4 files |
| Phase 10 P52 | 52 min | 3 tasks | 1 files |
| Phase 10 P54 | 35 min | 2 tasks | 2 files |
| Phase 10 P53 | 71 min | 2 tasks | 3 files |
| Phase 10 P55 | 16 min | 3 tasks | 6 files |
| Phase 10 P56 | 67 min | 3 tasks | 3 files |
| Phase 10 P57 | 48 min | 3 tasks | 6 files |
| Phase 11 P01 | 1h 13m | 2 tasks | 9 files |
| Phase 11 P02 | 29 min | 2 tasks | 7 files |
| Phase 11 P03 | 29 min | 2 tasks | 7 files |
| Phase 11 P04 | 47 min | 2 tasks | 11 files |
| Phase 11 P05 | 1h 19m | 2 tasks | 9 files |
| Phase 11 P06 | 1h 5m | 2 tasks | 6 files |
| Phase 11 P07 | 1h 17m | 1 tasks | 6 files |
| Phase 11 P08 | 1h 46m | 2 tasks | 5 files |
| Phase 11 P09 | 1h 22m | 2 tasks | 11 files |
| Phase 11 P10 | 55 min | 2 tasks | 4 files |
| Phase 11 P11 | 25 min | 2 tasks | 8 files |
| Phase 11 P12 | 34 min | 2 tasks | 10 files |
| Phase 11 P13 | 34 min | 2 tasks | 8 files |
| Phase 11 P14 | 57 min | 2 tasks | 13 files |
| Phase 11 P15 | 67 min | 2 tasks | 11 files |
| Phase 11 P16 | 46 min | 2 tasks | 9 files |
| Phase 11 P17 | 69 min | 3 tasks | 10 files |
| Phase 11 P18 | 80 min | 2 tasks | 13 files |
| Phase 11 P19 | 67 min | 2 tasks | 7 files |
| Phase 11 P20 | 56 min | 3 tasks | 5 files |
| Phase 11 P21 | 63 min | 2 tasks | 4 files |
| Phase 12 P01 | 1h 44m | 2 tasks | 13 files |
| Phase 12 P02 | 45 min | 2 tasks | 3 files |
| Phase 12 P03 | 1h 45m | 2 tasks | 10 files |
| Phase 12 P04 | 1h 58m | 2 tasks | 5 files |
| Phase 12 P05 | 1h 30m | 2 tasks | 13 files |
| Phase 12 P06 | 46 min | 2 tasks | 4 files |
| Phase 12 P07 | 50 min | 3 tasks | 7 files |
| Phase 12 P08 | 9 min | 2 tasks | 3 files |
| Phase 12 P09 | 22 min | 3 tasks | 6 files |
| Phase 12 P10 | 18 min | 3 tasks | 7 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.

- Milestone v2.0: редизайн реализуется на Jinja2 + точечных HTMX/Alpine, без SPA.
- Milestone v2.0: настройка расписаний переезжает в редактор объявления; список расписаний становится сводным с включением и паузой.
- Milestone v2.0: адаптивность — критерий приёмки каждой фазы, а не отдельная фаза.
- Roadmap v2.0: нумерация фаз перезапущена с 1 (`--reset-phase-numbers`), так как фазы v1 были ретроспективной документацией уже отгруженной системы.
- Phase 1: htmx и Alpine вендорены файлами, Tailwind удалён — внешних ресурсов в проекте 0, build-шаг не вводится.
- Phase 1: браузерный `confirm()` заменён собственной панелью подтверждения с настоящей формой POST в 13 местах — путь удаления сохраняется без Alpine.
- Phase 2: долги Фазы 1 (CR-01, CR-02, WR-01/T-10-04) закрыты планом 02-02 — владение `ad_id`/`account_id`, сигнатурная проверка типа загрузки, владение ключом изображения.
- Phase 2: черновик объявления — колонка `ads.status` (миграция `0013`), планировщик пропускает черновики; выкат `0013` на целевую базу — решение владельца.
- Phase 2: автосохранение — create-or-update по `ad_id` на сервере + очередь `this:queue last` на форме; наложение с «Сохранить» удержано именованной регрессией.
- Phase 2: счётчик длины предупреждает (1024 с вложениями / 3686 без), но не блокирует сохранение и не режет текст.
- Phase 3: глобальный раздел «Группы» снесён — группы живут только на экране конкретного аккаунта; старые адреса перенаправляются на `/accounts`.
- Phase 3: GRP-08 (ручное добавление группы) снят, а не реализован — протокола синхронизации одной группы у воркеров нет, вход нёс дыру проверки владения.
- Phase 3: выключенная группа пропускается тихо — без записи в SendLog, `next_run_at` продолжает двигаться.
- Phase 3: повторный запуск синхронизации закрыт внутрипроцессной заявкой `_claim_sync_slot`, а не флагом в БД.
- Phase 3: R-03-09 — раскрытие текста стороннего httpx-исключения в плашке синхронизации принято владельцем как риск severity medium.
- Phase 5: семантика смены тарифа — `upgrade-only` (чекпойнт плана 05-10): повышение разрешено и оплаченный остаток не сжигает, понижение при действующей подписке не предлагается и не принимается. Правило объявлено ОДИН раз (`app/application/billing/plan_switch.py`) и читается ОБЕИМИ стадиями — гардом формы `POST /billing/subscribe` и `_apply_extension` (план 05-11). **Цена этого варианта после плана `05-18` названа ВЕЛИЧИНОЙ, а не эпитетом.** Форма границы — `convert-remainder` (решение владельца **D-30**, чекпойнт задачи 1 плана `05-18`): повышение СЧИТАЕТ ДОПЛАТУ — неистраченный остаток, купленный по цене младшего плана, конвертируется в дни старшего ПО ОТНОШЕНИЮ ЦЕН (`converted_remainder` в `app/application/billing/subscription_period.py`), и уже к получившейся точке добавляется оплаченный месяц (`next_expiry`). Повышение получает столько дней старшего тарифа, сколько СТОИЛ его остаток, не меньше одного дня у живого остатка: 25 дней Basic по 1490 ₽/мес дают 7 дней Pro по 4900 ₽/мес, а не 25. Формулировка совпадает с докстрингом `_extend_subscription` (`app/services/payment_service.py:648-656`) намеренно: два места одного утверждения обязаны говорить одно, и расхождение между ними — тот класс, за который фаза получила раунды 4 и 5. ⚠️ Цена, названная на чекпойнте плана `05-10`, говорила об ОСТАТКЕ младшего тарифа, отдаваемом при повышении бесплатно, и читалась У́ЖЕ того, что исполнял код: «остаток» читается как величина, ограниченная ОДНИМ периодом, тогда как код не ограничивал её ничем, и управлял ею покупатель. Прежняя формулировка здесь ПЕРЕСКАЗАНА, а не процитирована, и это осознанно: дословная цитата отменённой цены осталась бы в файле строкой, неотличимой машиной от живого утверждения, — а именно машинная проверка отсутствия отменённых формулировок и стоит на этой записи. Верификация раунда 5 воспроизвела расхождение числами: двенадцать предоплаченных месяцев `basic` плюс один платёж `pro` давали 395 дней Pro за 22 780 ₽ при прейскуранте 63 700 ₽. Расхождение закрыто планом `05-18` в названном направлении — те же деньги дают теперь ≈141 день Pro при прейскуранте ≈22 540 ₽. Запись о чекпойнте `05-10` НЕ стирается: она фиксирует, что было решено ТОГДА, и стереть её значило бы закрыть дефект удалением записи о нём. Подарком после `convert-remainder` остаётся ровно одно, и оно ограничено: применения тарифных лимитов в системе нет вовсе (D-08), поэтому «лимиты Pro» сегодня не дают доступа ни к чему сверх показа. **Исход на ИСТЁКШЕМ сроке — решение владельца, принятое чекпойнтом плана 05-13: вариант A, идентификатор `apply-after-expiry`.** Истёкший срок снимает отказ на ОБЕИХ стадиях, и оплаченное понижение ПРИМЕНЯЕТСЯ; ветка `refuse-always` (отказывать в продаже всегда) НЕ взята, шесть мест отзыва обещания не тронуты. Следствие для пользователя одной фразой: дождавшись окончания оплаченного срока, он может уйти на младший тариф — и получит именно тот тариф, за который заплатил, так что обещание «оплаченные дни не сгорают» исполняется, а не отзывается. Признак живости срока объявлен один раз (`subscription_is_live`, `app/application/billing/subscription_period.py`) и подаётся в правило обязательным keyword-only входом `period_is_live` (план 05-13). **Разрешение сделки записывается на сам платёж — решение владельца D-28** (гэп 1 раунда 4, вариант (а) из трёх названных отчётом): ответ гарда о разрешённом переходе пишется в колонку `payments.switch_authorized` (ревизия Alembic `0019`, план `05-15`), и стадия применения читает записанный ФАКТ, а не пересчитывает предикат по изменившейся строке подписки. Отвергнуты вариант (б) — считать оба решения по признаку, снятому в момент ПРОДАЖИ, — и вариант (в) — выдавать отвергнутому платежу дни уплаченного тарифа КАК ЕДИНСТВЕННУЮ меру; причина выбора названа прямо: корень не в форме аргументов и не в моменте выборки, а в том, что решение о сделке нигде не сохранялось, и оба отвергнутых варианта чинят следствие. ⚠️ Ревизия `0019` встаёт седьмой в невыкаченную очередь (D-26): на боевой базе колонки нет. **Цена отвергнутого платежа — решение владельца D-29:** отвергнутый платёж продолжает давать дни, но по УПЛАЧЕННОМУ тарифу — доля календарного месяца по отношению уплаченной суммы к цене действующего плана, не меньше одного дня, — а не полный месяц старшего; исход «взяли деньги и не дали ничего» не введён ни на одной ветке, отвергнуты сохранение безусловного сдвига как принятого размена и полный отказ в сдвиге. **ФОРМА потолка одновременных `pending`-подписочных намерений — ответ чекпойнта задачи 1 плана `05-15`: `cap-different-plan`, срок давности 24 часа** (исполнено планом `05-17`): отвергается второе незакрытое подписочное намерение с ДРУГИМ планом, повтор оплаты ТОГО ЖЕ тарифа остаётся разрешённым. Следствие для пользователя одной фразой: пока предыдущая оплата не завершилась, купить ДРУГОЙ тариф нельзя, и об этом говорят словами, а не молчаливым редиректом, — а через сутки незакрытое намерение мешать перестаёт. Срок давности существует из-за D-27 (подписка на `payment.canceled` в кабинете ЮKassa не подтверждена, поэтому отменённый платёж на проде остаётся `pending` навсегда), а не для удобства: потолок без него запер бы человека, закрывшего вкладку оплаты. Ответ «потолка нет» из перечня вариантов был СНЯТ разбором, а не забыт: без потолка не закрывается случай (2) третьего пункта `missing:` гэпа 1 — ни ветвью «выдан», ни ветвью «не продан», — и остаётся открытым путь случая (1), где ОБА платежа несут разрешение `True`, отказа не возникает ни у одного и доля месяца не исполняется вовсе (путь назван открытым в `05-15-SUMMARY.md` и закрыт ФОРМОЙ в плане `05-17`).
- Phase 5: D-30 (чекпойнт плана 05-18) — форма верхней границы `convert-remainder`: остаток при повышении конвертируется в дни старшего плана по отношению цен, а не переезжает днями. Отвергнуты `cap-horizon` и `apply-after-period`; записано в 05-CONTEXT.md по образцу D-28/D-29. ⚠️ Запись D-30 переписана планом `05-26` в двух местах — журнальный ключ ветки отката и цена этой ветки, названная числом; прежняя формулировка не стёрта, а названа ошибкой записи с указанием раунда, её обнаружившего.
- Phase 5: **D-31 (чекпойнт задачи 1 плана 05-22, ответ владельца `cap-one-month`)** — в ветке ОТКАТА, где цену любого из двух планов прочитать нельзя, перенос оплаченного остатка зажат верхней границей. Формулировка взята из докстринга ДОСЛОВНО, а не пересказана: «Верхней границей переноса объявлен ОДИН календарный месяц остатка: позднейшая из действующего срока и сегодняшнего дня, зажатая месяцем от сегодня» (`app/application/billing/subscription_period.py:337-338`). Правило объявлено один раз функцией `capped_carryover` и ЧИТАЕТСЯ денежным путём; `_apply_extension` своей формулы отсчёта не держит. Цена принята сознательно и названа числом: остаток короче месяца переносится целиком, часть остатка сверх месяца сгорает — исключение из прохибиции плана `05-01`, а не её соблюдение. Полная запись с отвергнутыми вариантами и рейтингом обратимости — `05-CONTEXT.md`, D-31.
- Phase 5: **D-32 (чекпойнт задачи 1 плана 05-24, ответ владельца `record-wins`)** — при конфликте объявления `upgrade-only` с записанным решением D-28 исполняется записанный ФАКТ сделки, а объявление перестаёт быть инвариантом обеих стадий. Формулировка взята из докстринга ДОСЛОВНО: «`upgrade-only` есть правило стадии НАМЕРЕНИЯ: оно определяет, что ПРОДАЁТСЯ, а не то, чем кончается любой подтверждённый платёж» (`app/application/billing/plan_switch.py:96-97`). Следствие для денег: остаток старшего тарифа конвертируется в дни младшего по отношению цен (30 дней Pro → 98 дней Basic), деньги примерно сохраняются, действующий тариф меняется вниз — и это теперь ВИДНО в журнале ключом `subscription_plan_downgraded`. Запись решения D-28 не правится: сужено объявление `upgrade-only`, а не D-28. Полная запись — `05-CONTEXT.md`, D-32.
- Phase 5: **D-33 (решение о МЕТОДЕ, план 05-26)** — прохибиции класса «объявление против кода» разведены по предмету: кодовая половина остаётся `verification: test` с настоящим машинным гейтом (план `05-25`), документная половина переводится в `verification: judgment` честно. Полная запись с причиной и рейтингом обратимости — `05-CONTEXT.md`, D-33; операционное следствие — §Deferred Items ниже.
- [Phase 05.1]: 05.1-02: словарь вердиктов разведён — VOID (05.1) при тексте требования, VOIDED-BY-MODEL-CHANGE в таблицах прослеживаемости
- [Phase 05.1]: 05.1-02: долг BILL-02 закрыт СНЯТИЕМ ПРЕДМЕТА (D-D), а не реализацией; статус Partial снят из всех действующих записей
- [Phase 05.1]: 05.1-02: VOID-требования выходят из счёта распределённых и объявляются отдельной строкой — прецедент GRP-08 применён без изменений
- [Phase 05.1]: 05.1-02: BILL-05 и BILL-07 переданы фазе 05.1; у фазы 5 в §Coverage ноль требований, её заголовок оставлен как история

- Phase 05.1: счётная модель тарификации снята целиком — три тарифных плана по осям лимитов И валюта сообщений с пакетами. На их месте единая подписка 3000 ₽/мес за доступ ко всей системе.
- Phase 05.1: доступ гейтится списком роутеров, а `billing` и `auth` включаются БЕЗ гейта явно — белый список исключений запрещён, по умолчанию открыто; перечень закреплён машинным гейтом по ИСХОДНИКУ (T-05.1-01, T-05.1-03).
- Phase 05.1: административный безлимит перенесён с оси сообщений на ось доступа (`has_free_access`); тумблер инвалидирует кэш вердикта в обе стороны, признак читается ровно в одном месте — предикате доступа (T-05.1-31).
- Phase 05.1: потеря журнала операций по сообщениям при ревизии `0020` ПРИНЯТА владельцем (T-05.1-30): утрачивается учётная история, а не оплаченное благо. Названа трижды — чекпойнт, докстринг ревизии, предупреждение внутри `downgrade()`.
- Phase 05.1: ревизия `0020` прогнана на КОПИИ боевой базы до выката; дамп снял владелец, единственный шаг к бою — чтение (T-05.1-48, T-05.1-49).
- Phase 05.1: security-ревизия закрыта — 56 записей реестра, `threats_open: 0` при ASVS L1 и `block_on: high` (`05.1-SECURITY.md`).

- Phase 5 (закрытие 2026-08-25): единственный пункт UAT — вёрстка раздела тарифов на 375px — закрыт ответом владельца `pass` при НАЗВАННОМ расхождении. ⚠️ Формально это «пункт закрыт», а НЕ «вёрстка увидена»: проверяемой вёрстки в коде нет, карточки тарифов вырезаны планами 05.1-05 и 05.1-07, а приёмку `/billing` на 375px закрыла фаза 05.1 своим UAT (3 из 3). Запись сделана так, потому что `result: pass` без этой оговорки прочитался бы следующим как визуальная проверка, которой не было.
- Phase 5 (безопасность 2026-08-25): `05-SECURITY.md` создан ретроспективно из `<threat_model>`-блоков тридцати планов — 221 уникальная угроза. ⚠️ `threats_open: 0` получен ТРЕМЯ разными способами, и смешивать их нельзя: 160 закрыты проверенной митигацией, 58 закрыты УДАЛЕНИЕМ защищаемого компонента фазой 05.1 (вернётся компонент — вернётся угроза), 2 приняты как риск владельцем. Одна (T-05-66, medium) остаётся открытой ниже порога блокировки `high`.
- Phase 5 (D-27 → AR-05-01, 2026-08-25): подписка на событие `payment.canceled` в кабинете ЮKassa принята как риск вместо включения. Цена названа: отменённый платёж остаётся `pending` навсегда, журнал платежей врёт про деньги. Код готов — снятие риска не требует правок.
- Phase 5 (WR-07 раунда 3 → AR-05-02, 2026-08-25): монтирование `/var/run/docker.sock` в контейнер `web` переведено из диспозиции `transfer` в `accept` с акцептором `chubav`. Причина перевода — не смена оценки риска, а то, что `transfer` семь раундов держался без названного получателя и потому передачей не был. Экспозиция жива.
- Phase 5 (T-05-66, открыт ниже порога): `Subscription.is_active` остался входом решения о доступе (`app/application/billing/subscription_period.py:148`), хотя объявленная митигация обещала перевести решение на `expires_at`. Обнаружено при этом, что НИ ОДИН путь в `app/` флаг не снимает — вход, который никакой код не может изменить.
- Quick 260826-ojg (2026-08-26): issue #45 — обёртка-форма внутри `.sched-card__body` объявлена flex-колонкой с промежутком РОДИТЕЛЯ (16px), а не своим: `gap` действует между детьми контейнера, а подблоки лежат внутри формы сохранения, поэтому ритм тела до них не доходил. Все три дефекта скриншота — один корень. Селектор несёт `:has(.sched-card__block)`: без него правило захватило бы форму удаления и `stretch` растянул бы «УДАЛИТЬ РАСПИСАНИЕ» во всю ширину карточки. ⚠️ Зрительный результат в браузере машинной проверки не имеет — проверяется ПРАВИЛО, а не вёрстка (D7 в SUMMARY, `human_judgment: true`).

- Phase 08 (2026-08-29): признак `HX-Request` читается РОВНО ОДИН раз (`app/pages/htmx.py:33`); `respond()` объявлен с обязательным `redirect=` — путь деградации нельзя забыть, его отсутствие роняет вызов.
- Phase 08: отказ зависимости едет своим типом `HtmxRefusal`, а не `HTTPException` — обработчик FastAPI отдаёт `JSONResponse` при ЛЮБОМ статусе, а FOUND-07 запрещает слою письма и редирект, и JSON. Ответ — 204 + `HX-Location` (`app/main.py:219`), у 204 тела нет по определению.
- Phase 08: пять микро-контрактов адресной строки сведены в один `?notice=` по закрытому реестру (`app/pages/notices.py`, 14 кодов); `notice_for` сравнивает ЦЕЛИКОМ, неизвестный код не рисует НИЧЕГО — владелец ссылки не может напечатать пользователю текст от имени приложения.
- Phase 08: параметр `?expired=1` гейта доступа НЕ читается разделом оплаты ни одной строкой (T-05.1-21) — плашка рисуется по состоянию с сервера; параметр оставлен живым для журналов и закреплён `test_the_access_redirect_flag_survives`.
- Phase 08: денежный потолок стал свойством СХЕМЫ — частичный уникальный индекс `uq_payments_open_subscription_intent` объявлен ДВАЖДЫ (ревизия `0021` и `Payment.__table_args__`), предикаты сверены разбором `ast`, зубы доказаны подделкой с изменённым пробелом.
- Phase 08: `_claim_payment` по-прежнему спрашивает ДОПОЛНЕНИЕ терминальных статусов — просроченное намерение осталось ОПЛАЧИВАЕМЫМ. Заведено второе множество `AWAITING_STATUSES` (`payment_service.py:104`), а не переписано первое: два вопроса — два множества (закрытие разрыва G-08-1).
- Phase 08: одиночность процесса приложения стала МАШИННЫМ фактом (`tests/test_infra/test_web_service_is_single_process.py`) — удержание `_RETRY_IN_FLIGHT` верно ровно при одном процессе, и это свойство развёртывания, а не кода.
- Phase 08 (UAT 2026-08-29): 9/9 пунктов ручной проверки закрыты `pass`; четыре из них — решения владельца об ОТКЛАДЫВАНИИ (плашки аварии не прячутся обратно, CR-01, четвёртый реестр `QUEUE_DROP_RESULTS`, граница пакета гейтов). Перечень — PROJECT.md §Active.
- Phase 08 (безопасность 2026-08-29): `08-SECURITY.md` собран из `<threat_model>` одиннадцати планов — 52 строки реестра, `threats_open: 0` при ASVS L1 и `block_on: high`, 7 принятых рисков (R-08-01…R-08-07).
- [Phase 09]: План 09-10: владелец выбрал ветвь record-kind — макрос-обёртка печатает цель блокировки только при переданном значении, вызов формы тумблера передаёт пустую цель — Тег макроса правится дешевле всего, пока его использует ОДНА форма; после Фазы 11, раздающей макрос сорока семи формам, та же правка задела бы каждую. Измеренная цена варианта real-target (отмена QUAL-06, измерение 3.3) не принята
- [Phase 09]: План 09-10: обе строки консоли расхождений DIV-09-01 и DIV-09-02 НАБЛЮДЕНЫ в живом браузере до правки (Chrome / macOS, 2026-08-30) — Чтение исходника при приёмке 09-09 подтверждено глазом: РАСХ-2 существует, вариант no-divergence отпал по факту. Повторное наблюдение ПОСЛЕ правки не проводилось и остаётся предметом приёмки фазы
- [Phase 09]: Решение владельца по DIV-09-01 — ветвь `conditional-include`: объявление включаемых данных панели подтверждения стало условным (параметр has_sentinel у макроса строки, умолчание «сентинела нет») — Правка мелкая и обратимая: контракт сентинела и арифметика починки курсора CR-01 не двигаются ни на символ. Вариант always-present-cursor отклонён владельцем как costly — он снимает совпадение «число отрисованных строк = смещение следующей порции», на котором стоит арифметика починки, шипнутой планом 09-05; вариант defer отклонён потому, что строка осталась бы в консоли на каждое удаление у большинства аккаунтов
- [Phase 09]: Второе следствие DIV-09-01 не закрывается сужением правила: оформлено именованным перечнем INCLUDE_TARGET_EXCEPTIONS по идиоме SP-1, назначенная фаза — Фаза 15 — Сужение правила до первого положения экрана владельцем отклонено прямым текстом с причиной «правило теряет зубы на втором положении навсегда, и следующее такое же расхождение там пройдёт молча». Запись несёт обоснование, положение экрана, назначенную фазу, отдельно утверждаемую константу и три утверждения идиомы, третье из которых краснеет при устаревании записи
- [Phase 10]: Механизм закрытия панели на транспорте HX-Location выбран владельцем: `location-branch` — первичным предикатом остаётся признак успешности самого рантайма, к нему прибавлен ЯВНЫЙ дизъюнкт транспорта перехода. Второго определения успеха проект не заводит; обе отвергающие формулировки D-12 сохранены, 422 остаётся неуспехом. — Ветвь (A), чтение диапазона кода ответа, отменила бы отвергающую половину D-12 поимённо и завела бы контракт, наследуемый FORM-08 Фазы 11 и всеми 18 местами подтверждения. Ветвь (B) не отменяет ни одной записанной формулировки — отменяется только ЦИТАТА выражения как полной его формы.
- [Phase 10]: Защита обращения к объекту запроса стережётся РАЗМЕТОЧНО, а не поведенчески, и основание записано: объект запроса присутствует на всех четырёх реальных формах события (измерено по бандлу), поэтому снятие защиты ни одной формы не покраснило бы. — Поведенческого различителя у защиты нет и синтезировать его нельзя: форма события без объекта запроса рантаймом не производится, а гарнир, подающий несуществующую форму, есть ровно предмет провала критерия 3. Механизм защиты — решение оркестратора, владелец назвал ветвь.
- [Phase 10]: Изъятия из правила неотличимости повтора выведены ЧТЕНИЕМ всех восьми обработчиков по ОДНОМУ предикату включения (действие уничтожает адресуемую строку — остаётся под правилом; повтор запускает действие заново либо строка не уничтожается — изымается), а не угадыванием по именам маршрутов
- [Phase 10]: admin_drop_task ИЗЪЯТ по предикату, а не по наблюдённому отказу: адресуемая ПУТЁМ строка (аккаунт) не уничтожается, снимается задача из ТЕЛА формы, и закрытый словарь исходов подраздела ОБЯЗАН различать drop_removed и drop_missing. Решение и его основание записаны окном 32, чтобы следующая фаза не приняла изъятие за недосмотр
- [Phase 10]: Отрицательный контроль фрагментного маршрута СЕЕТ ТРЕТЬЕ расписание сверх двух действующей расстановки: без него ответ на другой идентификатор уходил бы в ветку перехода и множество оказалось бы ПУСТЫМ — контроль доказывал бы смену ветки, а не чувствительность к идентификатору (окно 31)
- [Phase 10]: Сличение ответа повтора с ответом первого запроса живёт в ОДНОМ предикате _repeat_matches_the_first; его зовут и несущее правило, и его отрицательный контроль — второе сличение внутри контроля стерегло бы не то, что исполняет правило
- [Phase 10]: Негодная ВЕЛИЧИНА поля контекста `ad_id` ОТБРАСЫВАЕТСЯ до запроса, а не даёт отказ обработчика (`_AD_ID_MAX`, план 10-08) — Форма ответа маршрута подтверждённого удаления объявлена фазой ДВУМЯ транспортами. Третья форма, различимая по величине поля, вернула бы карту занятых идентификаторов, запрещённую свойством T-10-07. Основание совпадает с записанным у `_clean_ints`: испорченное поле КОНТЕКСТА не повод потерять само действие. Граница взята по ВЕРХНЕЙ ГРАНИЦЕ КОЛОНКИ (2_147_483_647) — величине, общей обоим драйверам проекта; нижняя граница — ЕДИНИЦА, потому что идентификаторы автоинкремента начинаются с неё, и адрес, собранный из `-1` или `0`, был бы вторым дефектом того же класса.
- [Phase 10]: Развилка формы ответа `schedules_delete` и адрес приземления читают ОДИН признак возврата, прочитанный ОДИН раз (план 10-08, `WR-01`) — `_editor_url` был заведён ровно затем, чтобы два независимо собранных адреса не разошлись молча. Адрес собирался один раз, а ВЕТКА — по-прежнему дважды: запрос с живым идентификатором строки без признака возврата давал 302 на сводный список пути деградации и 200 с фрагментом РЕДАКТОРА пути htmx, а все три внеполосных узла ответа целей на сводном списке не находят. Развилка Фазой 11 наследуется как образец для пятнадцати обработчиков класса A, поэтому расхождение размножилось бы раньше, чем стало бы видно хоть одному из двух транспортов.
- [Phase 10]: Ожидаемое число диалогов в обходе 10-UAT.md названо ИЗМЕРЕННЫМ, а не выведенным из намерения: редактор рисует панель удаления самого объявления сверх панелей расписаний, поэтому диалогов N+1, и несущий счёт — [id^="sched-del-"] — Обход, написанный по намерению, называет человеку то, чего он может не увидеть, и первым же несовпадением обесценивает себя целиком (записанный опыт Фазы 9)
- [Phase 10]: Способ вызова отказа для проверки панели дан двумя путями с названной разницей; путь через негодную ВЕЛИЧИНУ ad_id запрещён в обходе явно — Величина ad_id давала пятисотку (CR-01), но её закрыл план 10-08 границей величины — обход, зовущий отказ этим способом, не дождался бы его
- [Phase 10]: Phase 10 / план 10-11: решение о неотличимости повтора девятого маршрута подтверждения (удаление группы аккаунта) принято ПРАВИЛОМ, а не изъятием — объявленное число изъятий не двигается (4) — Выведено ЧТЕНИЕМ обработчика Фазы 9: он объявляет свойство о себе дословно — ответ у всех четырёх случаев (найденная, чужая, несуществующая, УЖЕ УДАЛЁННАЯ) собирается из group_id пути и совпадает побайтно. Предикат включения перечня изъятий не подходит ни одной половиной: действие уничтожает адресуемую строку, заново ничего не запускает и личности не меняет
- [Phase 10]: Phase 10 / план 10-11: вторая форма ответа девятого маршрута (переход на опустевшей выдаче) ИСХОДОМ перечня не объявлена — накрыта отдельным правилом, ограничение структуры записи передано окном 43 — Исход действия у маршрута один, кодов исхода он не выдаёт вовсе (notice не передаётся, D-10), а две формы ответа различает СОСТОЯНИЕ ЭКРАНА — то есть свойство транспорта, которое шапка модуля держит отдельными утверждениями. Поле htmx_form живёт у ЗАПИСИ, и второй исход с другой формой ответа уронил бы параметризованный обход; расширять структуру на ходу план запрещает прямо
- [Phase 10]: Phase 10 / план 10-11: в строку прогресса вехи записан СЧЁТ ПЛЮС СОБСТВЕННАЯ ОТМЕТКА плана 10-11 (11/11 при сыром счёте 10) — Отметку - [x] 10-11-PLAN.md ставит оркестратор ПОСЛЕ завершения плана; строка, записанная сырым счётом, разошлась бы с разделом фазы через минуту после собственной записи. Стерегущая команда принимает оба состояния и зелена на обоих
- [Phase 10]: Имя константы приведено к уже записанному основанию: _AD_ID_MAX → ID_MAX. Прежнее имя НЕ БЫЛО ОШИБКОЙ — оно точно называло единственный вход, который тогда ограничивался, и пережило появление ещё четырёх — Расхождение имени с основанием (основание называло предметом КОЛОНКУ, имя — ОБЪЯВЛЕНИЕ) есть половина причины, по которой граница осталась стоять на одном входе из пяти
- [Phase 10]: ТРИ типа-псевдонима ограниченного идентификатора, а не один: ScheduleIdPath (адрес запроса), AdIdForm (обязательное поле формы), AccountIdForm (необязательное поле формы) — Общей остаётся ВЕЛИЧИНА — единственное место, которого требует отчёт; различается СПОСОБ ПЕРЕДАЧИ, а он есть свойство МАРШРУТА. Один псевдоним на все входы вернул бы выбор транспорта в каждую сигнатуру либо молча принял бы поле формы там, где ожидается адрес
- [Phase 10]: Распоряжение негодной величиной различается ПО РОЛИ значения: обязательный вход маршрута отвечает отказом валидации 422, необязательное поле КОНТЕКСТА экрана по-прежнему ОТБРАСЫВАЕТСЯ без потери действия — Решение плана 10-08 не отменяется: испорченное поле контекста не повод потерять само действие, а третья форма ответа, различимая по величине поля, вернула бы карту занятых идентификаторов (T-10-07)
- [Phase 10]: Регламент записи требований получил ПРИНУЖДЕНИЕ: правило `tests/test_planning/test_requirement_completion_follows_verification.py` краснит прогон, если требование помечено завершённым без вердикта `passed` отчёта верификации своей фазы — Правило существовало прозой («условие перевода — вердикт `passed` повторной верификации»), принуждения не имело, и ровно поэтому коммит `0ea886d` пометил `FORM-06` при живом блокере `CR-01`; поймал это следующий круг верификации, а не прогон. Прецедент формы — `test_state_progress_matches_roadmap.py`.
- [Phase 10]: `promote`, а не `add-alongside`: граница величины идентификатора повышена в первичное представление — объявлена ОДИН раз на проект в нейтральном `app/pages/identifiers.py`, прежнее частное понижено до именованных псевдонимов страничного модуля расписаний — Вторая копия числа 2147483647 в соседнем файле разошлась бы с первой молча — тот класс отказа («запись шире дерева»), за который фаза получила круги ревизии 3, 4 и 5. Решение подкреплено машинным принуждением: правилом единственности объявления по разбору дерева всего `app/`, а не намерением
- [Phase 10]: Переезд границы сделан ПЕРЕНОСОМ ОБЪЯВЛЕНИЯ, а не переименованием потребителей: имя `ID_MAX` остаётся доступным по прежнему пути `app.pages.schedules` — Цена отката названа планом величиной: два модуля суиты ввозят `ID_MAX` из страничного модуля расписаний. Переезд эту цену не заплатил — оба файла не правились ни на символ и зелены (120 passed)
- [Phase 10]: Замер `_framework_bounded_post_inputs` разрешает псевдонимы границы по ВСЕМУ дереву `app/` неподвижной точкой, а не внутри одного модуля; охват ВХОДОВ при этом не расширен — Прежняя редакция была слепа к ограничению, приехавшему ввозом: после переезда она дала НОЛЬ измеренных входов при семи объявленных. Измеренная вселенная правила полноты стала бы пустой, и в направлении «найден замером, но не объявлен» правило зеленело бы вакуумом навсегда
- [Phase 10]: Решение о законности расхождения восьмой записи реестра с запертым D-01 планом НЕ ЧЕКАНИТСЯ: состояние остаётся `DECISION_WAITS_FOR_THE_OWNER` — Изъятие из запертого решения владельца принадлежит владельцу (прецедент окна 51 и формы окон 34/47). План делает расхождение ВИДИМЫМ, а снятие есть работа Фазы 11 по роадмапу
- [Phase 10]: Граница вселенных гейтов гарда происхождения объявлена ТРЕМЯ числами, а не двумя: 36 POST-обработчиков страничного слоя, 13 мест вызова гарда, 10 маршрутов внутри вселенной хотя бы одного гейта полноты — Отчёт ревизии (WR-05) назвал «13 из 36» покрытием вселенных; замер ast дал 10. Три маршрута (auth.py::stop_impersonation, billing.py::subscribe_to_plan, history.py::history_retry) НЕСУТ гард, не будучи стережимы ни одним гейтом — снятый с них гард не покраснит ничего. Объявив 13 покрытием, докстринг воспроизвёл бы ровно ту ошибку, которую WR-05 предъявил слову «полнота»
- [Phase 10]: Расширение серверного рубежа на 23 изменяющих маршрута страничного слоя НЕ выполнено, фаза-сниматель НЕ НАЗНАЧЕНА намеренно — Какие изменяющие маршруты продукта несут серверный рубеж — продуктовый выбор владельца вехи. Гейт, заведённый на маршруты входа и регистрации, которые гарда нести не могут, потребовал бы именованного изъятия из решения, которого никто не принимал. Прецедент разведения — окна 45, 47 и 51 реестра. План сделал расхождение ВИДИМЫМ числом вместо того, чтобы чеканить изъятие
- [Phase 10]: Каноническое утверждение истинной границы сверки источника заведено ИМЕНОВАННОЙ константой ORIGIN_CHECK_BOUNDARY в app/pages/common.py — раньше, чем на него сошлются четыре площадки вызова — На четырёх площадках стоят четыре дословные копии утверждения «до любого обращения к базе», опровергнутого WR-02: гард стоит после get_user_from_cookie, а тот уже сходил в базу один-два раза. Правка площадок раньше канона родила бы пятую копию; канон объявлен именем, чтобы площадки (план 10-32) сослались одной строкой
- [Phase 10]: Правило якорей наблюдения переехало из tests/test_planning/ в tests/test_templates/ ЦЕЛИКОМ — с реестром, объявленным числом и дословной летописью; сохранность доказана равенством сумм собранных правил 224 = 224, а не заявлена (план 10-26, WR-07)
- [Phase 10]: Маркер planning объявлен хуком pytest_configure в tests/conftest.py, а НЕ разделом [tool.pytest.ini_options]: раздела у проекта нет вовсе, и его появление сделало бы pyproject.toml якорем rootdir, изменив поведение всей суиты ради регистрации одного имени (план 10-26)
- [Phase 10]: Граница годности маркера записана В ДВУХ МЕСТАХ дерева (tests/conftest.py и tests/test_planning/__init__.py): маркер есть инструмент РАЗБОРА, а не разрешение не прогонять — каталог держит единственное принуждение регламента отметок требований и единственную защиту обхода от самозаверения (план 10-26)
- [Phase 10]: Планы ВОЛНЫ 1 фазы 10 отбирают продуктовую суиту ПУТЁМ (--ignore=tests/test_planning), а не маркером: отбор по отрицанию ещё не объявленного имени НЕ ПАДАЕТ — он молча собирает другое множество и даёт зелёный. Планы последующих волн пользуются -m 'not planning' свободно (план 10-26)
- [Phase 10]: Пометка написана ПО ЗАМЕРУ, а не по букве плана: замер опроверг вывод IN-02 пятого круга — вторая ветвь условия закрытия панели ЖИВАЯ и закрывает панель на 16 из 18 мест подтверждения, мёртв ПЕРВЫЙ дизъюнкт
- [Phase 10]: Оба исхода, предложенных IN-02, неприменимы: удаление терма — регрессия на 16 местах из 18, пометка подстраховкой — заведомо неверное утверждение в рычаге фазы. Терм на месте, поведение восемнадцати мест не тронуто
- [Phase 10]: Правило стережёт СОГЛАСИЕ ЦИТАТЫ ПОМЕТКИ С ДЕРЕВОМ, а не попадание статуса в успешный диапазон: диапазон на пути перехода не применяется вовсе, ранний возврат стоит до него
- [Phase 10]: 10-28: идентификаторы, приезжающие ПАРАМЕТРОМ ЗАПРОСА, закрыты вместе с идентификаторами адреса — Разведение их по способу передачи было бы разведением по ВНЕШНЕМУ признаку — ровно тот дефект, который ревизия предъявила на адресах: семья закрыта для тех своих членов, на которые смотрели
- [Phase 10]: 10-28: реестр расхождения вырос на ДЕВЯТЬ записей (8 → 17), а не на шесть — Ревизия воспроизвела отказ на шести МАРШРУТАХ, но два из них несут по два ограниченных идентификатора адреса, а один закрыт планом 10-24. Счёт ВХОДОВ, а не ИСХОДОВ — тот же способ, которым перечень вырос до семи вместо пяти на первом круге
- [Phase 10]: 10-28: признак раскрытого расписания закрыт по ОБЪЯВЛЕННОМУ предмету, а не по прослеженному пути — НАХОДКА замера: значение до сравнения по колонке НЕ ДОЕЗЖАЕТ — сличается с уже загруженным составом расписаний в памяти. Параметр закрыт всё равно (он объявлен идентификатором расписания), и разница записана прямо у параметра: молчаливое закрытие «на всякий случай» было бы записью шире дерева
- [Phase 10]: Вселенная гейта полноты объявлена ДВУМЯ признаками (местозаполнитель пути ЛИБО окончание имени на признак идентификатора) при обязательном объявлении целым: каждый признак порознь пропускает то, что накрывает второй, и оба названы вместе с тем, что каждый пропускает — Признак адреса один не накрыл бы курсор постраничного вывода групп (приезжает параметром запроса, в пути не стоит), закрытый планом 10-28. Признак имени один не накрыл бы идентификатор пути, названный будущей фазой иначе: имя параметра есть выбор автора, местозаполнитель пути — объявленный контракт маршрута.
- [Phase 10]: НАБЛЮДАЕМОЕ МНОЖЕСТВО СДЕЛАНО ШИРЕ ВСЕЛЕННОЙ: приложение накрывает и параметры, объявленные СТРОКОЙ, а не только целые вне обоих признаков, как предполагал ориентир плана — Замер предъявил ШЕСТЬ маршрутов, где параметр объявлен str, приводится к целому внутри обработчика и роняет его пятисоткой на величине вне диапазона колонки. При приложении, ограниченном целыми по объявлению, эти шесть не попали бы ни во вселенную, ни в приложение и выпали бы МОЛЧА — то есть правило объявляло бы полноту, имея слепую зону, и повторяло бы предмет WR-05 на другой оси.
- [Phase 10]: Перечень узнаваемых имён границы СОБИРАЕТСЯ ЧТЕНИЕМ нейтрального модуля и гонится до НЕПОДВИЖНОЙ ТОЧКИ по всему app/, а не выписывается литералом — Три корня нейтрального модуля разрешаются в шесть имён: app/pages/schedules.py заводит свои (ScheduleIdPath, AdIdForm, AccountIdForm). Разбор, слепой к границе, приезжающей ИМПОРТОМ, объявил бы семь закрытых входов модуля расписаний неограниченными — ровно так мерил пустую вселенную первый сбор плана 10-24.
- [Phase 10]: Разбор заведён РЯДОМ с прежним, а не расширен на месте: расширение прежнего разборщика починило бы слепоту ЦЕНОЙ ПОТЕРИ ПРАВИЛА — G-2 покраснел бы на девяти отказах, предъявив красный вместо измерения
- [Phase 10]: Сборщик фрагмента ИСКЛЮЧЁН из предмета разбора собственных выходов, и граница ИЗМЕРЕНА (12 мест у 10 обработчиков против 9): решение о форме ответа во вложенном _fragment принимает сам слой, позвавший его
- [Phase 10]: Условие снятия РАЗВЕДЕНО НА ДВУХ АДРЕСАТОВ машинно: правило требует в тексте И «Фаза 11» (адресат работы), И «ВЛАДЕЛЬЦУ» (адресат решения о форме); условие, назвавшее только фазу, читалось бы как назначенное решение
- [Phase 10]: Проверка статуса в приклейке поставлена ПЕРВОЙ по измеренному основанию: ответ 204 не несёт заголовка типа содержимого вовсе, и проверка типа, стоя выше, назвала бы не ту причину
- [Phase 10]: Механизм внеполосного кода исхода НЕ УДАЛЁН как неиспользуемый: сужение утверждения о механизме и его удаление — разные действия (0 удалённых строк кода)
- [Phase 10]: Пятая копия снятого утверждения ищется по ПРЕДМЕТУ, а не по совпадению слов: близкая находка записывается отдельно с замером, иначе строгий вердикт покупается умолчанием
- [Phase 10]: Правка ПОЛОВИНЫ вхождений одного утверждения отклоняется с замером числа вхождений: полупочиненное состояние есть тот же дефект, ради снятия которого правка затевалась
- [Phase 10]: Сведение записей об опровергнутом сохраняет ПРЕДМЕТ, а не ТЕКСТ, и проверяется ДВУМЯ числами сразу — строк меньше, названных предметов больше
- [Phase 10]: Несущий счёт проверки 1 обхода 10-UAT.md исправлен на `[id^="sched-del-"][role="dialog"]` в четырёх местах при ДОСЛОВНО перенесённых ожидаемых числах: правился ИНСТРУМЕНТ, а не ожидание. — `3`, `3` и `2`, `2` и `1`, `4 − N` и `3 − N` были ВЕРНЫМИ числами при неверном способе их снять — панелей было ровно три и тогда. Сдвинутое число означало бы подгонку ожидания под наблюдение.
- [Phase 10]: Команда посева артефакта обхода исправлена как ТЕКСТ и НЕ ЗАПУЩЕНА: `"%02d:00"` вместо `"0%d:00"`. — Три самостоятельно достаточных основания: рабочее дерево и прод смотрят в ОДНУ базу (замер `walkthrough.environment`); разрешение человека на прогон 2026-09-09 дано присутствующему за клавиатурой при `workflow.live_dom_uat: false` и автономным исполнителем не наследуется; годность исправленного выражения уже доказана замером Р-2 на последних двух стендах обхода.
- [Phase 10]: `FORM-06` НЕ отмечено выполненным, хотя `requirements.ready-ids` вернул `1/1`. — Артефакт обхода держит прямой запрет пунктом 3 своего регламента: `FORM-06` переводится в завершённое ТОЛЬКО после прохождения обхода И повторной верификации. Шаг 4.4 обхода ПРОВАЛЕН, в таблице прослеживаемости стои́т `Gaps Found`, и отметка `Complete`, поставленная планом, была бы самозаверением.
- [Phase 10]: Запись индекса 2 реестра якорей НЕ снята и объявленное число оставлено равным 21: реестр объявил своим предметом пары «шаг ↔ адресуемый узел», и снятие потеряло бы привязку признака роли к шагу 1.2. Различающую силу взяло на себя отдельное правило конъюнкции, а нулевая различающая сила записи названа числом в летописи (план 10-36, WR-01). — Ревизия предлагала альтернативу — снять запись и вернуть число к 20; она отвергнута с основанием: починила бы предъявленное ценой того, что ревизия не предъявляла.
- [Phase 10]: Разбор открывающего тега рычага ведётся С УЧЁТОМ КАВЫЧЕК, и основание записано ИСПОЛНЕНИЕМ: замер на HEAD — наивный кусок 710 символов обрывается внутри значения x-data и признака роли не содержит, кусок с учётом кавычек 2008 символов содержит оба признака. Пример починки, приведённый ревизией, разбирает наивно и на этом дереве краснел бы на работающей разметке (план 10-36). — Правило, краснеющее на работающей разметке, выключают вместе со свойством; контроль test_control_a_naive_first_bracket_scan_would_misread_this_markup держит основание машинно.

- [Phase 10]: **D-13 ПРИНЯТО ЗАПИСЬЮ, А НЕ ПРОЗОЙ — решение владельца `chubav` от 2026-09-09, ветвь `accept-reading`** (блокирующий останов задачи 1 плана 10-40). Прочтение критерия 3 роадмапа («критерий запрещает НОВЫЕ СУЩНОСТИ, а не правку существующих») получило машинно читаемое место — `overrides[0]` во frontmatter `10-VERIFICATION.md`, `overrides_applied` 0 → 1, форма унаследована от ЖИВОГО прецедента Фазы 9 (`accept-override`, `chubav`, 2026-09-01), второй формы не заведено. Формулировка критерия перенесена из `ROADMAP.md` ПОБУКВЕННО и сличена посимвольно программно. ⚠️ ОХВАТ ШИРЕ ПЕРЕЧНЯ ПЯТОГО КРУГА И ПОДТВЕРЖДЁН ВЛАДЕЛЬЦЕМ ПРЯМО: приняты все 51 строка JS, дописанные фазой в шаблоны на HEAD `c45d9c5` (`ads/form.html` 38 — исполняемых 2, комментария 36; `components/modal.html` 13, план 10-35), а не две строки обёртки. Дифф по `app/static/js/` ПУСТ; четыре числа сущностей найдено/объявлено 2/2, 1/1, 0/0, 24/24. Отклонена ветвь `reject-reading`. ⚠️ ДАТА ЗАПИСИ — ТА, КОТОРУЮ НАЗВАЛ ВЛАДЕЛЕЦ (2026-09-09), а не дата внесения (2026-09-10): расхождение названо в поле `accepted_by_provenance`, а не сглажено. Цена названа прямо: граница «новая сущность против правки существующей» становится действующим ПРАВИЛОМ ПРОЕКТА и переживает фазу; гейт четырёх чисел принуждает её ТОЛЬКО в четырёх измерениях. Вердикт отчёта, счёт истин, список гэпов и блок повторной верификации планом 10-40 НЕ ТРОНУТЫ — они принадлежат верификатору.
- [Phase 10]: Запись о починке гэпа идёт ОТДЕЛЬНЫМ ключом (`closed_by:` для дефекта артефакта, `closure_progress:` для продуктового), а поле `status` записи гэпа не правится и не вычёркивается — Замер словаря по дереву даёт у гэпов обхода ровно одно значение поля состояния, значения «починено» в словаре проекта нет ни одного; выдуманное новое не понял бы ни один потребитель, а значение отложенного сказало бы неправду. Поле есть запись того, что видел обход в свой день (идиома D-30/D-32)
- [Phase 10]: Ненаблюдённый шаг обхода разводится на наблюдённую и ненаблюдённую половины с названным методом, замером и владельцем, а не остаётся молчанием и не объявляется неприменимым — Неприменимость есть свойство СТЕНДА (как у шага 1.15); здесь предмет наблюдаем, не хватило инструмента. «Шаг не наблюдён» скрывает, что половина его снята другой проверкой, и следующий обход пролистает обе
- [Phase 10]: Владелец отложенной находки выбирается ЗАМЕРОМ по объявленному предмету фазы-кандидата, а не догадкой; раздел плана 10-39 впервые в этом файле владельца НАЗНАЧАЕТ, и отличие от трёх предыдущих разделов названо прямо — Пять требований Фазы 15 (FORM-01, FETCH-03, QUAL-04, GATE-09, GATE-10) не накрывают ни качества разборщика таблицы стилей в суите, ни отступа безопасной зоны — основание строкой роадмапа «фаза закрывает инвентари и обходы, новых механизмов не вводит»
- [Phase 10]: 10-41: конъюнкция «глагол применён К ЭТОЙ заготовке» утверждается ИСПОЛНЕНИЕМ метода открытия в интерпретаторе JS; подстрочное правило не удалено, а сужено до антивакуумного слоя и называет поимённо правило, утверждающее конъюнкцию
- [Phase 10]: 10-41: отказ «глагола нет» — расхождение НА ТЕЛО и спрашивается только при названных всех заготовках (WR-08); поломка разбора отличима от поломки снятия ПО ОСНОВАНИЮ через LEVER_PARSE_FAILURE_PREFIX, а не по числу находок (IN-03)
- [Phase 10]: Канон ловушек цепи предков ВЫПИСАН независимо, а не выведен из перечней гейта или из констант текстов отказа: источник, вычисленный из проверяемого, есть его второе имя, и правило поверх него зеленело бы по построению
- [Phase 10]: Прежний контроль-представитель ветвей гейта СНЯТ, а не оставлен рядом с перебором канона: читая перечень индексом, он учил бы следующего читателя, что так можно; оба его замера перенесены к правилу-преемнику
- [Phase 10]: Phase 10: **D-35 (чекпойнт задачи 2 плана 10-44, ответ владельца `waive-with-record`, принял `chubav` 2026-09-10)** — регламент безопасности для Фазы 10 ИЗЪЯТ записью с основанием, а не принуждён: `/gsd-secure-phase 10` НЕ ЗАПУСКАЕТСЯ, и артефакт `10-SECURITY.md` есть ЗАПИСЬ ИЗЪЯТИЯ (`status: waived` — ТРЕТЬЕ значение в словаре проекта, у девяти соседних фаз только `verified` и `pending-acceptance`). ⚠️ ОХВАТ РАЗОВЫЙ — ТОЛЬКО ФАЗА 10, и это записано дословно и продублировано машинно читаемыми полями (`waiver_scope: phase-10-only`, `waiver_applies_to_milestone: false`): цена ветви, названная на чекпойнте, состояла именно в наследуемом вехой прецеденте, и владелец наследование ОТКЛОНИЛ — для Фазы 11 и далее `security_enforcement: true` остаётся действующим, и настоящая запись их случая не решает. ⚠️ Поле `threats_open` получило ЗАМЕРЕННОЕ **12**, а не ноль (ПЕРВОЕ ненулевое значение этого поля в проекте): двенадцать строк `high`/`accept` существуют, и изъятие есть решение НЕ СВОДИТЬ их, а не утверждение, что их нет. Основание — замер `10-SECURITY-SUBJECT.md` по сорока исполненным планам: 250 строк реестра, 239 уникальных имён, 84/104/56/6 по тяжести, 195/55 по распоряжению; всё блокирующее множество есть ОДНА строка цепи поставок `T-10-12-SC`…`T-10-23-SC`, у которой ВНУТРИ той же фазы разошлась тяжесть (28 планов оценили её `low` либо `n/a`) при НУЛЕ коммитов ветви, тронувших `pyproject.toml`, `uv.lock`, `wa_worker/package.json` или `wa_bridge/package.json`. Отвергнута ветвь `run-secure-phase`. Вердикт `10-VERIFICATION.md` (`gaps_found`) не тронут, отметка `FORM-06` не поставлена, исполненные планы 10-01…10-40 не правлены ни на символ.
- [Phase 10]: D-36 — ветвь `permit-with-record`: 39 запретов седьмой партии (планы 10-35…10-40) получили ЯВНОЕ РАЗРЕШЕНИЕ владельца записью. Принял `chubav` 2026-09-10 на блокирующем чекпойнте задачи 2 плана 10-47 (`gate="blocking-human"`, `one-way`). ⚠️ ОБЛАСТЬ СУЖЕНА ВЛАДЕЛЬЦЕМ И ЗАПИСАНА МАШИННО ЧИТАЕМО: `permit_scope: seventh-batch-only`, `permit_applies_to_phase: false`, `permit_applies_to_milestone: false`. Разрешение НЕ покрывает ни 167 запретов планов 10-01…10-34 (163 без дескриптора), ни 33 запрета восьмой партии, ни какую-либо иную фазу вехи v2.1 — и это названо ЧИСЛАМИ в шапке артефакта, а не оставлено на догадку. ⚠️ ФОРМУЛИРОВКА ВЛАДЕЛЬЦА, ПЕРЕНЕСЁННАЯ ДОСЛОВНО: «РАЗРЕШЕНИЕ ДЕЛАЕТ ЗАПРЕТ НЕ БЛОКИРУЮЩИМ, НО НЕ СОБЛЮДЁННЫМ» — без неё следующий круг прочёл бы разрешение как ПРОВЕРКУ. — Основание — ЗАМЕР, а не мнение: из 25 запретов с дескриптором `verification: test` не принуждается ЦЕЛИКОМ НИ ОДИН (0 целиком, 8 частично, 17 не принуждается вовсе), а из 14 без дескриптора шесть НЕВЫРАЗИМЫ ПО СУЩЕСТВУ (свойство процесса, а не дерева) — то есть ветвь `enforce-each` в чистом виде недостижима, и это было названо ДО ответа. Поле `prohibitions_fully_enforced: 0` держит замер, который разрешением НЕ СДВИНУТ. ⚠️ ДВА ЗАПРЕТА НАЗВАНЫ ОТДЕЛЬНО ОТ ПРОЧИХ — они не «не принуждаются», а УЖЕ ОПРОВЕРГНУТЫ ДЕРЕВОМ, и `git blame` дал находку сверх задачи 1: ОБА нарушены ТЕМ ЖЕ ПЛАНОМ и ТЕМ ЖЕ КОММИТОМ, которым он исполнялся — 10-38 («`_template_chain` НЕ ПЕРЕИСПОЛЬЗУЕТСЯ») коммитом `2bfa658` (`test_shell.py:5916` ввоз, `:5918` применение), 10-37 («ЛИТЕРАЛОВ СЛОЯ В КОДЕ ПРАВИЛ НЕ ПОЯВЛЯЕТСЯ») коммитом `15b9e99` (`:5363`, `"z-index: 3"`). ⚠️ ТРИ УТОЧНЕНИЯ ЗАМЕРА ЗАПИСАНЫ, А НЕ СГЛАЖЕНЫ: (1) планы 10-01…10-32 несут 154 запрета, а не 165 — ни одна граница числа 165 не воспроизводит (154/159/167), вероятная причина арифметический вывод вместо счёта; прежняя формулировка помечена опровергнутой, не вычеркнута (идиома D-30/D-32); (2) оператор ввоза стои́т на `:5916`, а не на `:5918`; (3) найдено ВТОРОЕ место литерала слоя — `:4750`, автор `test(10-42)`, восьмая партия. Ни одного правила не заведено, ни один запрет не объявлен соблюдённым, `FORM-06` не отмечено (вердикт фазы `gaps_found`), продукт, суита, вердикт и исполненные планы не тронуты; `tests/test_planning/` — 28 зелёных.
- [Phase 10]: Согласие ПРОЗЫ роадмапа о числе планов с ОТМЕТКАМИ раздела фазы стало машинным: правило test_the_prose_plan_counts_agree_with_the_marks сличает строку **Plans** с отметками (не со сводками — отметку ставит оркестратор) и при первом же прогоне назвало расхождение 40 против 45 — Действующий гейт читает ОТМЕТКИ и прозы не видит ПО ПОСТРОЕНИЮ — ровно поэтому строка прожила устаревшей целую партию
- [Phase 10]: Ссылка прозы продуктового комментария на чужой файл даётся ИМЕНЕМ символа и файлом объявления, БЕЗ номера строки; разрешение имени в живое объявление принуждается реестром PROSE_SYMBOL_CITATIONS — Номер строки протухает МОЛЧА — доказано трижды: пометка modal.html пережила круги 5, 6, 8 и правку плана 10-35 в том же файле двадцатью строками выше
- [Phase 10]: Раздел плана вносится в deferred-items.md ПЕРЕД якорем DEFERRED_END_ANCHOR, а не дописыванием в конец; раздел на план РОВНО ОДИН, и обе половины формы принуждаются прогоном — Задача 3 плана 10-39 дописывала в конец: второй прогон завёл бы второй раздел, счёт подразделов вырос бы с 11 до 16. Латентно только потому, что план исполняется один раз — свойство держалось расписанием, а не формой файла (окно 70)
- [Phase 10]: Отбор владельцев заготовок вынесен из тела гейта в функции, принимающие карту шаблонов: контроль зубов подаёт СВОЮ карту вместо правки живого дерева — Дописать второй обработчик в app/templates/ ради зелени контроля значило бы завести в продукте ровно ту удвоенную реакцию, которую отверг D-01
- [Phase 10]: Две формы подключения макроса Jinja описываются ДВУМЯ образцами ребра, а не одним с чередованием — Слитый образец был бы шире каждой из форм: он совпал бы с формами, которых язык не знает, и завёл бы в граф достижимости звенья, которых рантайм не зовёт
- [Phase 10]: Утверждение уровня модуля о пути рычага переехало в именованное правило test_the_lever_path_lives_under_the_templates_root — Голый assert уровня модуля есть НЕ-проверка в обоих исходах: сработав, роняет сбор всего файла безымянно, а под python -O исчезает целиком
- [Phase 10]: Единственность ЯКОРЯ подстановки утверждается по дословному куску, единственность ОБЪЯВЛЕНИЯ — по блоку — Подстановке нужна однозначность в исходнике побайтово, а утверждению — предмет, а не проза о нём; прежде оба вопроса задавались куску, и различать их приходилось одним символом точки с запятой
- [Phase 10]: D-49-1: различение идёт по ПРИЗНАКУ УСПЕХА в детали события, а не по факту завершения обмена — событие завершения приходит и на пути отказа (замер вендоренного рантайма)
- [Phase 10]: D-49-2: проверяется ОТСУТСТВИЕ признака успеха, а не сравнение с ложью: на пути обрыва транспорта объект сведений признаков не получает вовсе
- [Phase 10]: D-49-4: заведён ОТДЕЛЬНЫЙ гарнир рассылки — гарнир регистраций не правлен, у него свой предмет (число)
- [Phase 10]: D-49-5: FAILURE_BANNER_HANDLERS_MEASURED переснято 2 -> 3 замером по дереву; прежнее значение ошибкой не было
- [Phase 10]: D-49-7: цена сужения принята сознательно — успешный ФОНОВЫЙ обмен гасит непрочитанный настоящий отказ (четвёртое окно, владельца нет)
- [Phase 10]: D-50-1: у сводки подменяется УЗЕЛ, а не содержимое — форма взята у ответа автосохранения, второго места отрисовки той же области, чтобы два ответа не свопали её по-разному
- [Phase 10]: D-50-2: счёт расписаний вычисляется в сборке фрагмента ОДИН раз и подаётся обеим областям — закрытие гэпа G-10-6 по построению, а не по совпадению
- [Phase 10]: D-50-3: ближайший момент запуска берётся минимумом в Python, как в _editor_context: агрегат SQL дал бы второй путь, и расхождение путей было бы неотличимо от расхождения данных
- [Phase 10]: D-50-6: мест включения разметки сводки ТРИ, а не два — правило заведено на ЗАМЕРЕННОМ числе дерева, а не на числе плана
- [Phase 10]: D-50-7: OOB_BLOCKS 12 → 13 поставлено ПРОГОНОМ покрасневшего правила: число, полученное арифметикой, зеленеет и на дереве, где прибавилось не то
- [Phase 10]: D-51-1: снято УСЛОВИЕ подъёма, а не заведён второй блок — величины блока не сдвинуты ни одна; вторая копия величин разошлась бы с первой молча
- [Phase 10]: D-51-2: отбор блоков подъёма переключён на имя ЗАГОТОВКИ ДО правки таблицы — отбор, привязанный к снимаемому условию, дал бы пустое множество, и пять зависимых правил зазеленели бы ВАКУУМОМ
- [Phase 10]: D-51-3: утверждение безусловности ПОЛОЖИТЕЛЬНОЕ и имени ни одного признака состояния не знает — правило, знающее одно имя, пропустило бы условность под другим
- [Phase 10]: D-51-4: антивакуумная половина правила порядка слоёв ПЕРЕЕХАЛА в собственное имя, а не снята вместе с условием — её второй предмет (прокрутка списка за открытым окном, план 09-13) условию подъёма не принадлежал никогда
- [Phase 10]: D-51-6: правило порядка слоёв переименовано — имя пережило предмет; прежнее названо в докстринге и в таблице стилей, ссылки в сводках 10-33 и 10-35 не правятся
- [Phase 10]: D-54-2: ожидание шага 2.8 обхода сформулировано через НЕЗАВИСИМОСТЬ прямоугольника плашки от положения прокрутки, а не через одно число `top`, и процедура ТРЕБУЕТ ненулевого `scrollY`, приведённого числом — Оба исхода 2026-09-11 ВЕРНЫ каждый для своего замера: `{top: 96}` при прокрутке у верхнего края и `{top: -701}` при `scrollY: 797`. Признак зависит от ПРОКРУТКИ, и один положительный замер его не закрывает — замер при нуле не снимает признака вовсе
- [Phase 10]: D-54-3: тело программы посева стенда в приписку НЕ переписано — правило `test_the_walkthrough_stand_is_seedable.py` утверждает `text.count(program) == 1` и краснеет на второй копии; названы РАЗДЕЛ и ШАГ единственной команды, а уборка и счёт приведены дословно — Требование плана «команды приводятся ДОСЛОВНО» и машинный инвариант единственности противоречат прямо; противоречие снято ЗАМЕРОМ (прогон дал FAILED ровно на вписанной второй копии), а не выбором по вкусу. Довод тождествен доктрине самого плана: вторая копия разошлась бы с первой молча
- [Phase 10]: D-54-6: `requirements-completed` сводки плана 10-54 оставлено ПУСТЫМ при `requirements: [FORM-06]` в шапке плана — требование закрывается ПОДПИСЬЮ ЧЕЛОВЕКА на приёмке обхода, и отметка его сводкой была бы самозаверением — Ради запрета этого живут `test_the_walkthrough_cannot_self_certify.py` и `test_no_requirement_is_marked_complete_before_its_phase_verification_passed`. Прецедент — сводка плана 10-51 той же фазы
- [Phase 10]: D-37 — ветвь `extend-override` (останов задачи 1 плана 10-53, `gate="blocking-human"`, `one-way`): область действующего отступления по критерию 3 РАСШИРЕНА второй записью блока `overrides` шапки `10-VERIFICATION.md`, называющей ТРЕТИЙ файл (`app/templates/includes/htmx_error_banner.html`) и ТРЕТЬЮ сущность (регистрацию `htmx:afterRequest` на узле тела документа, `:227-233`, коммит `4e24d61`) поимённо. Принял `chubav` 2026-09-12. ⚠️ ОБЛАСТЬ СУЖЕНА СЛОВАМИ ВЛАДЕЛЬЦА И ЗАПИСАНА МАШИННО ЧИТАЕМО: поле `covers` покрывает ТОЛЬКО ЭТУ регистрацию; БУДУЩИЕ регистрации в том же файле ею НЕ покрыты, требуют НОВОГО решения владельца и обязаны покраснить пятое утверждение гейта критерия 3 (заведено планом 10-52). Запись несёт сверх формы 2026-09-09 два поля: `price` — ссылку на запись 74 журнала окон (успешный ФОНОВЫЙ обмен гасит непрочитанный настоящий отказ; `T-10-49-05`, `accept`), и `enforced_by` — имя пятого утверждения гейта с путём модуля. Ветвь `criterion-stands-violated` НЕ выбрана, второй записи журнала окон не заводится. ⚠️ `overrides_applied: 0` и `status: gaps_found` планом НЕ СДВИНУТЫ: поле есть счёт КРУГА, а не счёт записей, и засчитывает запись СЛЕДУЮЩИЙ круг верификации, а не план. Цена расширения названа в самой записи: граница «новая сущность против правки существующей» растянута ВТОРЫМ частным разрешением, и читателю роадмапа для понимания критерия 3 нужно прочесть теперь ДВЕ записи блока.
- [Phase 10]: Защита от неисполнимой СОХРАНЁННОЙ строки поставлена ПОМОЩНИКОМ `next_run_or_none` рядом с `compute_next_run_at`, а не `try` внутри вычислителя — Внутри вычислителя `None` означает «моментов нет», и этот смысл утверждают правила `tests/test_services/test_schedule_service.py`; `try` внутри отнял бы у планировщика `app/worker/` возможность отличать пустое расписание от испорченного. Дифф `schedule_service.py` от базы плана ПУСТ.
- [Phase 10]: Перечень ловимых классов помощника закрыт ПЯТЬЮ именами замера; огульный перехват всех исключений запрещён — `ValueError`, `TypeError`, `IndexError`, `AttributeError`, `ZoneInfoNotFoundError` — замер шести форм двенадцатого круга. Огульный перехват проглотил бы отказ СУБД, отказ сети и ошибку программиста, превратив пятисотку с объяснением в четырёхсотку без него.
- [Phase 10]: Шесть форм неисполнимой строки объявлены ПЕРЕЧНЕМ `MALFORMED_STORED_FORMS`, из которого параметризуются правила всех трёх входов — Форма, добавленная в перечень, автоматически становится требованием ко ВСЕМ трём входам. Пятнадцать копий правил разъехались бы при первой правке — это ровно тот класс расхождения, ради которого существует модуль `schedule_rules`.
- [Phase 10]: Стопка двух заготовок сделана КЛАССОМ на существующих узлах плюс свойством-величиной, а НЕ элементом-обёрткой: обёртка встала бы предком вне объявленной вселенной FAILURE_BANNER_ANCESTORS, и правило чистоты цепи продолжало бы ЗЕЛЕНЕТЬ, смотря на ЧАСТЬ цепи — ослабление СЛЕПОТОЙ дороже открытой красноты (план 10-56)
- [Phase 10]: Смещение второй заготовки идёт через свойство-величину, а не через второе объявление положения по классу: объявление по классу проиграло бы объявлению по идентификатору по весу селектора МОЛЧА — таблица выглядела бы верной, а прямоугольник остался бы один (план 10-56)
- [Phase 10]: Шаг стопки объявлен числом, а его ДОСТАТОЧНОСТЬ проверяет правило, считающее минимум из объявлений .alert и --fs-md той же таблицы: ожидание, выписанное целиком, согласилось бы с любой правкой отступов плашки и молча перестало бы хватать (план 10-56)
- [Phase 10]: FORM-06 настоящим планом НЕ отмечен и 10-UAT.md не тронут ни символом: правка ОБЕСЦЕНИВАЕТ шаги 2.8 и 4.4 обхода, и это записано СЛЕДСТВИЕМ в самом файле заготовок, а не закрыто задачей — прямоугольника машина не видит (план 10-56)
- [Phase 10]: Форма ВЫХОДА у плашки отказа выбрана ОТВЕТОМ ВЛАДЕЛЬЦА (ветвь `A` — «снятие без регистрации», область записи `узко`), а не замером исполнителя: орган снятия есть элемент управления, состояние которого переключает браузер, скрытие выражено объявлением таблицы стилей, и ни одно из пяти чисел гейта критерия 3 не сдвинуто — Кнопка с обработчиком — форма, предложенная UI-ревизией ДОСЛОВНО, — есть ветвь `B`: она немедленно покраснила бы пятое утверждение гейта критерия 3 и потребовала бы сдвига объявленного числа плюс четвёртого имени события. Цена по критерию 3 принадлежит тому, кто вправе её принять, и машина не вправе выбрать за владельца. Запись — третья в блоке `overrides` `10-VERIFICATION.md`, `accepted_branch: dismiss-control-plus-reset`
- [Phase 10]: Орган снятия адресуется ДВУМЯ РАЗНЫМИ крючками по замеру: таблица стилей — КЛАССОМ (`.banner-dismiss`), сценарий — ИДЕНТИФИКАТОРОМ (`htmx-failure-*-close`) — Селектор `#htmx-failure-server-close` нёс бы подстроку `#htmx-failure-server`, попал бы в отбор `_selector_lifts_banner` и покрасил бы ПЯТЬ действующих правил подъёма за ФОРМУ правки; имя класса в сценарии сдвинуло бы `grep -c "banner-dismiss"` по разметке с 2 на 4, то есть сломало бы собственный акцептанс плана. Оба основания выписаны в комментарии рядом с константами, чтобы следующий читатель не «починил» расхождение
- [Phase 10]: Зазор стопки при снятии ПЕРВОЙ заготовки НЕ ПОЧИНЕН и записан окном 82 журнала, а не умолчан — Блок «отсутствия дыры» плана 10-56 ключáется на АТРИБУТ скрытия, а орган снятия атрибута не ставит — ставить его означало бы ветвь `B`. Обе мыслимые формы починки правят РАЗБОР РОЛЕЙ `_stack_blocks`: четвёртый блок с `--failure-banner-top` попадает в роль смещения (две части селектора без признака в скобках), а расширение селектора роли `reset` запятой разбивает разбор на три части и оставляет роль пустой. То есть починка есть предмет отдельного плана, а не строка в этом
- [Phase 11]: Phase 11 / 11-01: schedules_update over htmx returns only the card article plus OOB #ad-summary and innerHTML:#sched-del-N-text; the confirmation panel root never rides in the body or becomes a target
- [Phase 11]: Phase 11 / 11-01: GATE-02 pairs live in one parametrized module tests/test_pages/test_htmx_post_pairs.py; the closure counts a handler as covered by POST_PAIR_CASES or CONFIRMED_DELETE_ROUTES, so Phase 10 confirm-delete pairs are not duplicated
- [Phase 11]: Phase 11 P02: id_in_column (app/pages/identifiers.py) is the single in-handler identifier bound for POST handlers (D-07); PostIdPath/PostIdForm/OptionalPostIdForm carry no framework bound
- [Phase 11]: Phase 11 P02: catalogue rule requires the first read of a POST-alias parameter to be an id_in_column argument; POST alias allowed only on POST-only routes
- [Phase 11]: Phase 11 P02: _ownership_verdict takes ad_usable/account_usable flags computed by the handler; out-of-column values ride the not-found/foreign branch without a query
- [Phase 11]: Phase 11 P02: window 51 VALIDATION_REFUSAL_DIVERGENCES_DECLARED 23 -> 16, seven schedules entries lifted by work
- [Phase 11]: 11-03: schedules_toggle summary-list branch answers a transition to /schedules without a fragment; the row fragment belongs to plan 11-04
- [Phase 11]: 11-03: card toggle form takes empty blocking target and this:drop sync by the DIV-09-02 precedent under D-11, recorded as a second form_wrapper caller in DISABLED_ELT_EXCEPTIONS
- [Phase 11]: 11-03: editor tests read the label.toggle node by its for attribute because form_wrapper prints the toggle path twice
- [Phase 11]: 11-04: курсор /schedules/partial переведён на КЛЮЧ последней отрисованной строки — класс отказа CR-01 Фазы 9 стал невыразим формой контракта, а не ловится сверкой; курсор приземлился ДО фрагмента строки, как предписывает D-11
- [Phase 11]: 11-04: ветка сводного списка тумблера отвечает строкой своего экрана с серверным состоянием; линейка total намеренно НЕ перерисовывается до перезагрузки — названная цена того, что переключённая под фильтром строка не исчезает из-под руки
- [Phase 11]: 11-04: гейт подписи модуля расписаний признал ВСТРОЕННУЮ границу второй формой (как уже принято гейтом каталога), а курсор НЕ выведен изъятием — его величина подконтрольна отправителю; признание закреплено новым контролем на ПОЛОВИНУ границы
- [Phase 11]: 11-06: `_save_from_editor` перестал выбирать транспорт и возвращает ПАРУ «адрес деградации + нульарный сборщик фрагмента»; заголовок истории `HX-Push-Url` ставится ВНУТРИ сборщика на тот же объект, который сборщик возвращает, — утверждается по `response.headers`, а не чтением кода (D-13)
- [Phase 11]: 11-06: ветка отказа по вложениям редактора объявлений ОСТАЛАСЬ вне слоя ответа и вынесена в названный помощник `_attachment_refusal` — половина деградации отвечает `400`, формы которой у `respond()` нет, и восемь действующих утверждений суиты на ней стоят; расхождение НЕВИДИМО гейтам (они считают обработчиков, а не ветки) и потому записано окном 86, адресат — планировщик FORM-08
- [Phase 11]: 11-06: `ads_update` и `ads_delete` переведены на `PostIdPath` с проверкой первым использованием (`id_in_column`); величина вне колонки идёт веткой «объявления нет», реестр окна 51 — 16 → 14
- [Phase 11]: 11-07: смягчение T-07-13 приземлилось ОТДЕЛЬНЫМ планом ДО возврата свопа правилу 422 — дерева, на котором сток открыт, не существует ни на одном коммите; правило 422 остаётся `"swap": false`, своп возвращает план 11-09
- [Phase 11]: 11-07: ветвь htmx обработчика `RequestValidationError` выбирается ДВУМЯ признаками — `is_htmx` И принадлежностью сорвавшегося обработчика пакету `app.pages` (`scope["endpoint"]`, замерено доступным в момент обработчика для отказов пути, тела и строки запроса на FastAPI 0.129.0 / Starlette 0.52.1). Ветвь по одному заголовку превратила бы запрос к JSON-API с заголовком htmx в пустой 400 и сломала бы контракт 422 другого транспорта (D-07)
- [Phase 11]: 11-07: выход НЕ читает место ошибки (`exc.errors()`), поэтому путь, тело формы и строка запроса закрыты ОДНИМ выходом — расхождение по месту ошибки стало невыразимым, а не проверяемым; систематически обходятся только маршруты с целым параметром пути (27 пар), и основание границы записано в докстринге модуля обхода
- [Phase 11]: 11-07: `SERVER_SIDE_VALIDATION_RESPONSES` 0 → 1 поставлен прогоном; летопись называет ТРЕТИЙ исход, которого прежние две ветки не предусматривали, — место не отдаёт 422 слою письма вовсе, поэтому ни фрагмент, ни решение о правиле ему не требуются
- [Phase 11]: 11-08: слой ответа получил `respond_field_error(request, *, page, fragment)` — ошибка ПОЛЯ отвечает одним кодом на оба транспорта, а тело выбирается способом прихода: без признака htmx собирается страница целиком, с признаком — фрагмент формы; путь деградации здесь СТРАНИЦА, а не перенаправление, потому что действие не состоялось и уводить человека некуда, — поэтому выход не наследует обязательный адрес деградации `respond()`, у которого он был бы аргументом без предмета
- [Phase 11]: 11-08: ответ выхода собирается СВЕЖИЙ, а невыбранный сборщик не зовётся вовсе; и то и другое утверждается не совпадением тел, а тождеством объектов и списком состоявшихся вызовов — подмена статуса на объекте сборщика означала бы правку ответа, которым выход не владеет (граница, названная у `_glue_notice`), а лишний вызов собирал бы целый документ на каждой ошибке заполнения
- [Phase 11]: 11-08: `SERVER_SIDE_VALIDATION_RESPONSES` 1 → 2 поставлено ПОКРАСНЕВШИМ ПРОГОНОМ («мест в app/, отдающих ответ 422, стало 2, в летописи записано 1»: `app/main.py:249`, `app/pages/htmx.py:644`); летопись называет исполненным ПЕРВЫЙ из двух исходов, которых она требует, — тело такого ответа собирает вызывающий через окружение шаблонов, а не умолчание фреймворка
- [Phase 11]: 11-08: правило 422 в этом плане НЕ ТРОНУТО (`"swap": false`), и выход не зовёт ни один маршрут — недостижимость проверена грепом, а перевёрнутое утверждение `test_validation_rule_carries_both_swap_and_error` оставлено красным-по-замыслу; своп приземляет план 11-09 ОДНИМ коммитом вместе с первым вызывающим, и чинить эту инверсию надлежит МАРШРУТОМ, а не правкой строки конфигурации
- [Phase 11]: 11-08: содержимое карточки настроек профиля вынесено в `app/templates/includes/profile_settings.html` — ЕДИНСТВЕННЫЙ источник формы для страницы и обоих фрагментов плана 11-09; ошибка поля переехала из-над карточки «Аккаунт» К ФОРМЕ (D-12 Фазы 8), где стои́т поле, которое надо поправить
- [Phase 11]: 11-08: выбранный пояс берётся из `selected`, а умолчание («ключа нет — показать сохранённый») ставит ВЫЗЫВАЮЩАЯ СТРАНИЦА, а не включаемый шаблон: спрятанное во включение, решение об источнике значения было бы невидимо со страницы, а на ветке ошибки плана 11-09 обработчик подаст ПРИСЛАННОЕ значение, и введённое перестанет теряться на опечатке
- [Phase 11]: 11-08: разметки htmx и постоянной обёртки с `id` план НЕ заводит, `app/pages/profile.py` не тронут ни на строку (проверено пустым `git status` по файлу) — обёртка и атрибуты отправки принадлежат плану 11-10, ПОСЛЕ перевода обработчика: заведённые раньше, они целились бы в маршрут, фрагментов ещё не отдающий
- [Phase 11]: 11-09: своп правилу 422 ВЕРНУЛСЯ — значением существующего правила, а не шестым ключом конфигурации и не новым JS, — и приземлился ОДНИМ коммитом (`4ba02ef`) вместе с ПЕРВЫМ маршрутом, отдающим этот код АВТОРСКИМ фрагментом (сохранение часового пояса в профиле). Порядок доказан ИСТОРИЕЙ, а не обещанием: смягчение T-07-13 стои́т ОТДЕЛЬНЫМ планом 11-07 (`cc72871`) РАНЬШЕ, поэтому дерева, на котором подмена включена без обработчика, в вехе не существует
- [Phase 11]: 11-09: перевёрнутое утверждение `test_validation_rule_carries_both_swap_and_error` снято МАРШРУТОМ, а не правкой строки конфигурации: утверждение получило ТРЕТЬЕ поколение («своп не пропал» вместо «свопа нет»), литерал правила в `test_shell.py` приведён, и оба движения поставлены ПОКРАСНЕВШИМИ прогонами тем же коммитом, что и своп
- [Phase 11]: 11-09: поле пояса получило умолчание пустой строкой — отсутствующее поле идёт ВЕТКОЙ ОБРАБОТЧИКА, а не отказом фреймворка: обязательное поле срывало бы разбор СИГНАТУРЫ, и на пути письма человек получил бы пустой 400 смягчения 11-07 — ни формы, ни причины. Новых проверок ввода при этом не заведено, годность пояса решает та же одна сверка с закрытым перечнем (D-06)
- [Phase 11]: 11-09: профиль стал ПЕРВЫМ потребителем ветки «код исхода на фрагменте» — приклейка внеполосного блока существовала с Фазы 8 и не использовалась НИ ОДНИМ обработчиком приложения. Докстринг `respond` получил ЧЕТВЁРТОЕ поколение; прежняя формулировка «НИ ОДИН ОБРАБОТЧИК» названа опровергнутой, а не стёрта, и опровергает её не ошибка замера, а РАБОТА фазы (D-30/D-32). Исход при этом УСПЕХ, а не отказ, которого ждала прежняя редакция
- [Phase 11]: 11-09: эхо присланного пояса есть НЕДОСТИЖИМОСТЬ, а не только экранирование (T-11-13): поле выбора рисует ЗАКРЫТЫЙ список поясов, присланное значение лишь СРАВНИВАЕТСЯ с его элементами, и не совпавшее не попадает в документ ни одним путём. Утверждение снимается с ОТВЕТА, поэтому краснеет и на потерянном экранировании, и на попытке напечатать присланное «как есть»
- [Phase 11]: 11-09: граница сборщика в гейте собственных выходов расширена на ЧЕТВЁРТЫЙ выход слоя (`respond_field_error` берёт `page=`/`fragment=`) — без этого его сборщики объявлялись бы «выходом в обход слоя», то есть расхождением ровно для того пути, ради которого слой и заведён. Исключается ТОЛЬКО сборщик, поданный ИМЕНЕМ выходу слоя; собственный ответ в теле обработчика по-прежнему требует записи
- [Phase 11]: 11-09: `NOTICE_WRITE_PLACES` 5 → 4 — счётчик, которого план НЕ НАЗЫВАЛ: адрес с кодом исхода перестал склеиваться руками в обработчике и собирается единственной на проект сборкой. Найден ПРОГОНОМ соседних модулей, а не планом, — тот же класс, что стареющий счётчик плана 11-06
- [Phase 11]: 11-10: цель успеха и цель ошибки поля — ОДНА (`#profile-settings`), и это ОСНОВАНИЕ, а не удобство: ровно им перечень `HX-Retarget`/`HX-Reswap` (D-16, FORM-10) заведён ПУСТЫМ — перенацеливать заголовком нечего, пока цели совпадают. Разойдись они — основание пустоты исчезает вместе с совпадением, и это записано у самой записи перечня, а не оставлено на догадку
- [Phase 11]: 11-10: пустой перечень получил ЗУБЫ отрицательным контролем, потому что пустота сама по себе не значит ничего: пустое множество удовлетворяет правило полноты и тогда, когда сканер ослеп, а зелёный цвет сломанного сканера посимвольно совпадает с зелёным цветом соблюдённого правила (D-13 Фазы 8). Контроль подставляет синтетический модуль с применением заголовка и требует, чтобы сканер НАШЁЛ его и назвал ключом `модуль::функция`. Фаза уже записала один критерий, позеленевший на пустой выборке (окно 84), — второго не заведено
- [Phase 11]: 11-10: сканер применений идёт ПО ДЕРЕВУ РАЗБОРА, а не грепом, и основание измерено: оба имени заголовков НАЗЫВАЮТСЯ СЛОВАМИ в докстринге слоя ответа (`app/pages/htmx.py`), где записано, что применяются они поимённо. Греп посчитал бы эту прозу применением и покраснел бы на документации о запрете вместо его нарушения. Сравнивается ЗНАЧЕНИЕ строковой константы целиком, поэтому докстринг в множество не попадает
- [Phase 11]: 11-10: граница сканера названа честно — ВЕНДОРЕННЫЙ РАНТАЙМ НЕ СКАНИРУЕТСЯ. Оба имени набраны в `app/static/js/htmx.min.js` (исходник самого htmx, который их и читает); он не `*.py` и не `*.html`, то есть в обе области поиска не попадает ни одним путём. Включив его, правило краснело бы вечно по неустранимой причине — то есть учило бы игнорировать красное
- [Phase 11]: 11-10: признак выкладки формы (`data-form`) переехал на ВНУТРЕННИЙ узел, приёмом редактора объявлений: тег формы печатает теперь общий на сорок семь форм вехи макрос-обёртка, и раздать через него частную выкладку одной формы нельзя. Без переезда форма профиля потеряла бы колоночную раскладку молча — суита этого не видит, правило стилей смотрит на признак, а не на форму
- [Phase 11]: 11-10: обработчик не тронут НИ НА СТРОКУ, и это проверено, а не заявлено: план 11-09 сделал `profile_post` фрагментным заранее, поэтому разметка целится в маршрут, который фрагменты уже отдаёт. 45 тестов профиля и обезличивания зелены БЕЗ единой правки — то есть переведена форма, а не переписан контракт
- [Phase 11]: 11-11: вынесено СОДЕРЖИМОЕ, а не обёртки: `<div data-actions>` и карточка плитки (`card_open`/`card_close`) остались на странице, постоянных `id`, атрибутов htmx и макроса-обёртки в выносе нет. Обёртки с `id` и цели свопа — предмет планов 11-12 и 11-13; D-03 даёт `id` только блоку действий, `worker_row.html` и `queue_row.html` не тронуты
- [Phase 11]: 11-11: отступы и пробельные маркеры тегов перенесены с прежнего места, хотя в отдельном файле выглядят странно: они попадают в вывод, и только с ними вывод страницы совпал байт в байт. Шапка каждого шаблона говорит это прямо
- [Phase 11]: 11-11: строки матрицы границ шести входов НЕ СНЯТЫ (в отличие от плана 11-06), а получили поле `outside` — исход ветки «записи нет» кодом и адресом перехода пути деградации. Адрес деградации переводы транспорта 11-12…11-14 сохраняют, поэтому строки не переписываются при каждом из них. Правило смежности для этих входов не различает исходов по построению D-07 — названо у правила
- [Phase 11]: 11-11: контроль «шести записей административного модуля» потерял предмет вместе с записями и ПЕРЕНАЦЕЛЕН, а не снят: модуль выбирается замером перечня (самый населённый), поэтому правка не повторяется каждым планом, снимающим модуль целиком
- [Phase 11]: 11-12: голый 403 блокировки оставлен РЕШЕНИЕМ ВЛАДЕЛЬЦА — запись `OWN_RESPONSE_EXITS` стоит в `DECISION_OWNER_D08` (ссылка `D-08` проходит правило авторства); планом решение не чеканилось
- [Phase 11]: 11-12: `_OWN_RESPONSE_PRICE` не переписан, а получил второе поколение в КОНЦЕ строки: опровергнуто только «плашка не рисуется» (слушатель `htmx:responseError` пропускает лишь 422); префикс, по которому правило узнаёт цену, прежний
- [Phase 11]: 11-12: `id` получили блок действий и две области состояния с потребителем-свопом (прочтение D-03 рядом с D-02); `worker_row.html`/`queue_row.html` не тронуты; пустая обёртка бейджа — `display: contents`, иначе зазор во флекс-колонке личности
- [Phase 11]: 11-13: сборка ответа тумблеров карточки вынесена в модульную `_user_actions_response` — второй копии нет ни шаблона, ни сборки; вложенные ленивые `_fragment` оставлены, по ним гейты узнают фрагментный обработчик
- [Phase 11]: 11-13: порядок «сброс кэша вердикта до сборки плитки» утверждается ЖУРНАЛОМ ВЫЗОВОВ, а не значением `∞`: плитка читает строку подписки через `_access_view`, и тест значения зеленел на мутанте обратного порядка
- [Phase 11]: 11-13: девять прежних записей `OWN_RESPONSE_EXITS` переведены в `DECISION_OWNER_D08` по тексту D-08 («к девяти записям»); прежние формулировки сохранены с поколениями; окно 63 НЕ закрыто и НЕ снято — D-08 закрывает только часть «решение о форме»
- [Phase 11]: 11-14: исход «у аккаунта нет очереди» в `QUEUE_DROP_NOTICE_CODES` не входит — сервис его не выдаёт, код называет ветка обработчика до обращения к сервису
- [Phase 11]: 11-14: варианты предупреждения «задача уже ушла» и «у аккаунта нет очереди» НЕ сменены на ошибку, хотя теперь эти отказы едут в площадку приземления фокуса — D-10 переносит текст И вариант; шапка `includes/notice_area.html` и `PANEL_REFUSAL_CODES` получили поколение, новое утверждение измеряет `PANEL_POLITE_REFUSAL_CODES`
- [Phase 11]: 11-14: RED гейта снятого написания измерен на дереве до перевода во временном `git worktree` с новым файлом гейта, а не `git stash` (стек стэша общий на все рабочие каталоги)
- [Phase 11]: 11-15: проверка хоста адреса подтверждения отвергает ЛЮБОЙ `@` в узле (включая пустые данные пользователя) и обратную косую черту сверх перечня плана — `urlsplit` и браузер читают `https://evil.example\@yoomoney.ru` по-разному
- [Phase 11]: 11-15: асимметрия проверки принята и записана в `SAFE_BY_NAME`: путь без htmx отвечает прежним 302 на адрес SDK без проверки хоста; фикстуры `yookassa.ru` тестов без htmx не тронуты, пары оплаты — на `yoomoney.ru`
- [Phase 11]: 11-15: пары оформления доступа идут через НАСТОЯЩИЙ `create_payment` с подменой только сети ЮKassa — незакрытое намерение заводится первым нажатием, и потолок PAY-01 проверяется тем же прогоном
- [Phase 11]: 11-16: повторная синхронизация групп — навигационное действие (D-02): все три выхода `respond(redirect=…)`, на htmx 204 + `HX-Location` посимвольно на адреса 302; заявку `_SYNC_IN_FLIGHT` вход не занимает (проверено по коду), освобождать на выходах нечего
- [Phase 11]: 11-16: три копии формы повторной синхронизации на `form_wrapper` без цели, в макрос не сведены (D-12(в)); сохранность атрибутов замерена рендером до/после — у старой формы только `action` и `method`, переносить на обёртку-предка нечего
- [Phase 11]: 11-16: реестр пар получил четвёртый случай «аккаунт чужой» сверх трёх §behavior — край назван edge_coverage_fallback плана, T-11-29 требует неразличимости с «не найден» на обоих транспортах
- [Phase 11]: 11-17: все восемь выходов `accounts_sync_groups` — `respond(redirect=…)`; выходы после `_claim_sync_slot` ВНУТРИ внешнего `try` (AST: ноль операторов между занятием и `try`), освобождение заявки доказано поведением (следующая синхронизация доходит до мессенджера) и мутантом «освобождать только без htmx», на котором пары зеленели
- [Phase 11]: 11-17: `data-syncing` переехал на `<div>` вокруг `form_wrapper`, селектор CSS без имени тега; комментарий «кнопка не отключается» получил поколение — блокировка только на время запроса, guard серверный
- [Phase 11]: 11-17: три входа аккаунтов на `PostIdPath`, `id_in_column` первым использованием (у синхронизации групп до занятия заявки, у удаления после сверки источника); вне колонки — ветка «аккаунта нет»; окно 51 8 → 5
- [Phase 11]: 11-18: `id="max-connect-step"` на самой колонке `.connect-shell`, а не на обёртке внутри карточки — алерт ошибки стоит вне карточки и обязан входить в подменяемое содержимое; обёртка внутри карточки перенесла бы алерт в тело карточки, отдельная обёртка сняла бы flex-промежуток
- [Phase 11]: 11-18: адрес деградации старта MAX — `/accounts`, сверено с кодом (только опрос статуса переводит `connecting → syncing`, GET мастера удаляет `connecting`, список аккаунтов без фильтра статуса); отсутствующее поле телефона идёт той же веткой 422, авторский 422 на другие классы валидации не расширен
- [Phase 11]: 11-18: экранирование эха телефона доказано на `_max_step_markup` с враждебным значением — HTTP-путём враждебное значение до 422 не доезжает (ветка достижима только пробелами); перечень встроенных обработчиков отправки пуст
- [Phase 11]: 11-19: у тумблера и удаления группы величина вне колонки приземляется на `/accounts/{значение}/groups` (адрес ветки «строки нет» собран из `account_id` пути), а не на `/accounts` — ветки «аккаунта нет» у этих POST нет, и `/accounts` выдал бы ветку различием заголовка (D-04/D-13)
- [Phase 11]: 11-19: пустота реестра окна 51 доказывается не числом 0, а правилом пустоты со сличением обхода замера с независимым `_post_handlers` и `POST_HANDLERS` плюс синтетическим входом с границей; машинерия реестра оставлена
- [Phase 11]: 11-19: окно 51 в `.planning/WINDOWS.md` не закрыто этим планом — закрытие командой `windows fixed 51` принадлежит задаче 3 плана 11-20; пробная отметка откачена до коммита
- [Phase 11]: 11-20: GATE-02 = 158 поставлено прогоном обхода утверждений 302 (приписывание по `create_app().routes`); «160», «около 136» и 157 записаны прогнозами, пережитыми обходом
- [Phase 11]: 11-20: неразобранные обходом утверждения 302 не выпадают молча — перечень `UNATTRIBUTED_302_ASSERTIONS` по функциям с обработчиками, которые тоже обязаны иметь пару; мёртвая запись краснит
- [Phase 11]: 11-20: окно 63 остаётся `open` — D-08 закрыл только часть «решение о форме»; частичное решение записано летописью у Фазы 11 в ROADMAP, а не `waive`/`fixed`
- [Phase 12]: Целевым тестом улики RED выбран тест гейта доступа: он покраснел утверждением о поведении (404 вместо 204), тогда как два соседних краснели импортом ещё не существующего модуля — сбой загрузки не есть доказательство отсутствия поведения (#3770)
- [Phase 12]: Имя файлового поля объявлено ОДИН раз в app/services/image_upload.py и доезжает до разметки глобалом upload_field; в тестовом модуле оно названо литералом, потому что ввезённая константа утверждала бы имя саму о себе
- [Phase 12]: Фаза 12, план 12-02: задача 1 закоммичена как chore(12-02), а не feat — правка шаблонов конфига есть chore по таблице типов, и это же единственный тип, при котором рантайм-гейт TDD не срабатывает проверкой «feat раньше test». Тип выбран по таблице; совпадение с требованием гейта — следствие, а не подгонка
- [Phase 12]: Фаза 12, план 12-02: RED задачи 2 замерен на дереве ДО задачи 1 во временном каталоге сессии (идиома плана 11-14) — в готовом дереве модуль зеленеет сразу, и объявить это RED-фазой значило бы объявить измеренным то, что не исполнялось
- [Phase 12]: Фаза 12, план 12-02: ожидание берётся из Settings(_env_file=None, database_url=..., secret_key=...), а не из голого Settings() — боевой .env разработчика в тест протекать не должен (защита conftest.py), а два обязательных поля умолчаний не имеют и уронили бы сбор модуля там, где .env нет
- [Phase 12]: Фаза 12, план 12-02: правило разбирает исходник шаблона БЕЗ строк-комментариев — закомментированная директива предела не задаёт, но совпадению регулярного выражения неотличима от действующей
- [Phase 12]: Признак `data-media` переставлен первым атрибутом узла полосы: правило плитки режет разметку литералом, и после переезда полосы литералу негде находиться, кроме как во включаемом файле — Альтернатива — навесить признак ещё и на постоянную обёртку — завела бы вторую область раскладки: в app.css [data-media] есть display: flex, и обёртка стала бы flex-контейнером вокруг flex-строки
- [Phase 12]: Проза комментария не набирает имён, которые сличает гейт: прежняя редакция называется ОПИСАНИЕМ, а не цитатой — Гейты безопасности и критерии плана считают вхождения в ТЕКСТЕ файла и комментарий от кода не отличают: дословная цитата прежней редакции красила бы правило на объяснении того, что оно охраняет
- [Phase 12]: Утверждение об условных внеполосных блоках усилено с литерального числа до перечня ИМЁН (CONDITIONAL_OOB_TEMPLATES) — Число, поднятое до двух, не заметило бы ПОДМЕНЫ: условный блок взамен прежнего дал бы ту же двойку при полностью сменившемся предмете
- [Phase 12]: Свободные места партии вложений считаются ОДНОЙ строкой из результата own_image_keys — второго инструмента расчёта потолка не заводится — Два места, считающие потолок, разойдутся молча (Hyrum). Вызов закрывает и владение уже прикреплённых ключей, и предел их числа; расчёт свободных мест читает его же результат и потому разойтись с ним не может.
- [Phase 12]: Заголовок события на ответе загрузки выбран вариантом ПОСЛЕ ПОДМЕНЫ, а не обычным — Разница наблюдаема: обычный заголовок поднимает событие ДО подмены, форма объявления сериализовалась бы без новых скрытых полей, и первое же автосохранение сохранило бы объявление БЕЗ только что загруженной картинки. Решение владельца (заголовок плюс слушатель, ноль нового JavaScript) соблюдено дословно; уточнён вариант внутри того же механизма.
- [Phase 12]: Предикат принадлежности ключа вложения выражен ОДНИМ местом дерева — `partition_own_image_keys`; `own_image_keys` зовёт его и сравнения вторым разом не пишет (D-07, Hyrum). — Два авторитета по принадлежности разошлись бы молча и оставили бы дыру ровно там, где её не проверяли. Замерено грепом по `app/`: одно вхождение сравнения.
- [Phase 12]: Ветвь отказа сверки ключей на загрузке отвечает ПОДТВЕРЖДЁННЫМ подмножеством, а не пустой полосой. — Полоса вложений после плана 12-03 есть ЕДИНСТВЕННЫЙ источник списка ключей в документе, поэтому пустой список — не осторожность, а стирание: подмена выносит скрытые поля, и следующее автосохранение записывает `ad.images = []` (гап 1 верификации, CR-02).
- [Phase 12]: Вторая достижимость гапа 1 — ключ прежней формы, уже лежащий в `Ad.images`, — записана ОСТАТКОМ в коде и в сводке, а не выдана за закрытую. — Три альтернативы отвергнуты на существе: расширение образца сняло бы рубеж владения, спрашивать у базы не у кого (маршрут привязан к пользователю, а не к черновику, D-02), перенос данных вне рамки партии. Остаток меняет форму уже сломанного состояния и даёт из него выход, а не ломает исправное.
- [Phase 12]: Полоса вложений приезжает ответом автосохранения НЕ всегда, а только тем, который состав вложений изменил (`repaint_media`, умолчание ложно) — Полоса после плана 12-03 есть единственный источник списка ключей в документе, а внеполосная подмена заменяет узел целиком; ответ, рисующий её из базы, отстающей от документа на незаписанный ключ, уносит с экрана и свежую плитку, и строку отказа, за которую D-04 заплатил лживым кодом 200. Перерисовка поэтому стала исключением, которое надо заслужить, а не умолчанием, которое стирает (гап 2 верификации, CR-01).
- [Phase 12]: Форма загрузки объявляет ту же стратегию наложения, что форма объявления (`sync='this:queue last'`, D-16), и равенство держит правило суиты, а не совпадение — Очередь `hx-sync` действует на элемент, а формы — разные элементы: очередь одной на другую не распространяется. Запрет Фазы 8 на слово `queue` прочитан по исходнику до правки — он проведён по маршрутам денежного периметра, и форма редактора объявлений названа там законным носителем очереди прямым текстом (гап 3, WR-04).
- [Phase 12]: Кромка `concurrency`, выданная пробником на FETCH-01, закрыта записанным допущением (D-17), а не работой и не молчанием — Две записи в `deferred-items.md` со статусом `accepted-assumption`: объекты-сироты при обрыве партии с межвкладочным счётом свободных мест (D-17, источник наблюдения IN-05, настоящая квота — `own_image_keys` на сохранении) и потеря среднего выбора файлов вытесняющей очередью (D-16). Обе называют решение владельца по идентификатору и цену отвергнутой альтернативы.
- [Phase 12]: Авария хранилища посреди партии ловится ПОФАЙЛОВО в цикле обработчика, а `store_upload` не правится — Форму отказа выбирает тот, кто знает, есть ли ему что терять: у партии есть — ключи частей, уже записанных в хранилище, — у одиночного файла нет. Исключение, выпущенное наружу из цикла, оставляет партию без ответа-фрагмента, а на пятисотом рантайм разметки подмены не делает вовсе: документ ключей не узнаёт, объекты остаются сиротами. Контракт сервиса (HTTPException 502) при этом остаётся верен, и правило сервиса зелено без правки (D-16, находка ревью WR-05).
- [Phase 12]: Текст отказа при аварии хранилища говорит о СУДЬБЕ файла, а не о его пригодности — `STORAGE_UNAVAILABLE_MESSAGE` = «файл не сохранился — хранилище сейчас недоступно, попробуйте ещё раз через минуту». Сказать «ваш файл не подошёл» там, где не подошло хранилище, отправило бы человека искать другой файл вместо повторной попытки с этим же. Подробность исключения в закрытый набор текстов не входит и уходит только в журнал (T-12-08-01).
- [Phase 12]: Причина отказа сверяется правилом РАВЕНСТВОМ с ввезённой константой, а не вхождением слова — Подстрока утверждала бы, что человек читает ПОХОЖУЮ причину; равенство утверждает, что он читает ТУ САМУЮ, живущую в сервисе единственной копией. Литералами в модуле названо лишь то, что составляет контракт с браузером (имя поля, имя события, имя заголовка); текст для человека ввозится.
- [Phase 12]: Порядок задач плана 12-08 переставлен под рантайм-гейт TDD: правило задачи 2 написано ДО реализации задачи 1 — Обе задачи несут tdd="true" при TDD_MODE=true, а задача 1 behavior-adding; отдельного правила она не заводит — её поведение проверяет ровно то правило, которое инвертирует задача 2. Исполнить план в написанном порядке значило бы либо уронить гейт, либо завести одноразовое правило-дублёр. Задача 2 затем довела правило до формы, невозможной до задачи 1: ввоз константы, которой ещё не существовало.
- [Phase 12]: Отказ по числу частей составного запроса ДОПИСЫВАЕТ строку, а не подменяет полосу: разбор не состоялся, текущих ключей вложений сервер не знает, и подмена содержимого ответила бы полосой ПУСТОЙ, отцепив законные вложения (урок CR-02 плана 12-06 применён к новой ветви, а не повторён в ней) — Ветвь отказа сверки ключей (12-06) пересобирает полосу из ПОДТВЕРЖДЁННОГО подмножества, потому что разбор состоялся и подмножество известно. Здесь подтверждать нечего: ни одного поля images сервер не прочитал. Единственный честный ответ — дописать строку соседом узла полосы, оставив и её, и скрытые поля ключей нетронутыми.
- [Phase 12]: Отказ разбора составного запроса ловится типом Starlette, а не типом FastAPI: последний ПОТОМОК первого, и перехват по потомку пропустил бы отказ насквозь — request.form() поднимает starlette.exceptions.HTTPException; fastapi.HTTPException — его подкласс, и except по подклассу родительский экземпляр не ловит. Ошибка была бы молчаливой: ветвь просто не исполнялась бы, а человек продолжил бы получать JSON с кодом 400.
- [Phase 12]: Разбор строки отказа в тестах опирается на КОНТРАКТ нормализации имени (safe_filename не оставляет пробелов и не бывает пустым), а не на состав сегодняшних текстов отказа — Прежний разбор делил строку по первому вхождению разделителя и объявлял левую часть именем файла. Безымянный отказ, чья причина содержит тире как обычный знак препинания, читался бы как именованный — правило мерило бы разбор, а не предмет. Опора на контракт нормализации переживает появление новых текстов с тире.
- [Phase 12]: Сверка источника на маршруте загрузки стои́т ПОСЛЕ прав и ДО `request.form`, а не выше выборки строки — Канон `ORIGIN_CHECK_BOUNDARY` ставит сверку выше чтения ПРЕДМЕТА; предметом на этом маршруте является ТЕЛО запроса. `request.form()` вычитывает поток целиком и спуливает каждую файловую часть до первой строки логики, поэтому сверка ниже него отдала бы тот же 403, оплатив межсайтовому отправителю буферизацию всей партии до потолка тела на обратном прокси. Замеряется это не кодом ответа, а числом обращений к хранилищу (решение владельца D-15, находка ревью WR-03).
- [Phase 12]: Новая запись реестра собственных выходов получила СВОЁ состояние решения `DECISION_OWNER_D15`, а не `DECISION_OWNER_D08` — D-08 Фазы 11 закрывает вопрос о форме отказа по источнику для записей, которые называет — «девять записей» множеством и «три обработчика этой фазы» поимённо. Маршрута `POST /ads/images` в страничном слое в тот день не существовало вовсе (он переехал туда Фазой 12, D-01), поэтому присвоить ему D-08 значило бы дочеканить решение за владельца — ровно то, что модуль запрещает двумя абзацами. Форма у D-15 и D-08 совпала; это совпадение исходов двух решений, а не распространение одного на другое. По той же причине заведено второе условие снятия: массовый перевод Фазы 11 снять этот выход не может, потому что маршрут родился переведённым.
- [Phase 12]: Утверждение «предел размера картинки ограничивает ПРИНИМАЕМОЕ» снято, а ссылка на WR-02 сохранена историческим источником — На действующем транспорте вызывающий получает часть УЖЕ РАЗОБРАННОЙ, поэтому единственный предел ДО чтения — потолок тела запроса на обратном прокси (`nginx/*.conf.template`, сверяется `tests/test_nginx_body_limit.py`); `max_image_size_mb` ограничивает то, что декодируется и сохраняется. На прежнем транспорте — одна файловая часть, читаемая самим обработчиком — снятое утверждение было верно дословно, и неверным стало в день смены транспорта (D-01), а не изначально. Комментарий, утверждающий защиту, которой транспорт не даёт, хуже отсутствия комментария (находка ревью WR-01).

### Pending Todos

Пять открытых, все подтверждены как отложенные при закрытии вехи v2.0 (`.planning/todos/pending/`):

- `blocked-user-can-still-log-in.md` — **BLOCKER**, auth/доступ: блокировка пользователя не действует, вход через страничный маршрут открыт
- `password-reset-codes-are-predictable.md` — **BLOCKER**, auth/криптография: коды сброса пароля берутся из `random.randint`
- `session-cookie-without-secure-flag.md` — **BLOCKER**, auth/транспорт: cookie сессии выставляется без флага `secure`
- `plan-price-credibility-bound.md` — billing/денежный путь: правдоподобие цены в `PLAN_LIMITS` не проверяется нигде
- `full-suite-ads-editor-order-pollution.md` — tests/порядок исполнения сюиты: `test_image_base_url_comes_from_app_settings` красный только в полном прогоне

⚠️ Три первых заведены ревизией кода фазы 05.1 раунда 2 и НЕ закрыты — подтверждение при закрытии вехи подавляет их в аудите, а не чинит.

### Blockers/Concerns

- 🔴 [Phase 08] **Ревизия `0021` не выкачена, и у её выката есть ОБЯЗАТЕЛЬНЫЙ ПОРЯДОК** (R-08-06 `08-SECURITY.md`): бэкфилл переводит просроченные намерения в `expired` и следа об этом не оставляет. Накат `0021` РАНЬШЕ кода с `AWAITING_STATUSES` захлебнёт админский «Обзор» ложными `payment_stuck`, неотличимыми от настоящих. Штатная выкладка одним развёртыванием условие соблюдает; ручной накат миграции вперёд кода — нет.
- ⚠️ [Phase 08] Плашки аварии htmx не прячутся обратно: `removeAttribute('hidden')` односторонний, снятия на успешном `htmx:afterRequest` нет. После одного сбоя красное остаётся на экране до конца сессии. Отложено решением владельца (UAT 08, пункт 5).
- ⚠️ [Phase 08] CR-01: дозапись `yookassa_payment_id` и `commit` после успешного `YooPayment.create` стоят ВНЕ защиты `try/except` (`app/services/payment_service.py:721-808`). Деньги не теряются; цена — временная (≤ 24 ч) невозможность начать новую оплату для одного человека при редком сбое СУБД. Отложено решением владельца (UAT 08, пункт 8).
- ⚠️ [Phase 08] Запись утверждает больше, чем сделано: комментарий `app/pages/billing.py:93-95` («Три частных реестра… Копий не осталось ни одной») не учитывает ЧЕТВЁРТЫЙ реестр `QUEUE_DROP_RESULTS` и микро-контракт `?result=` на `/admin/queue` (`app/pages/admin.py:288`). Отложено решением владельца (UAT 08, пункт 9).
- ⚠️ [Phase 08] Митигация T-08-49 ЧАСТИЧНАЯ по названному поимённо остатку (R-08-05): `app/pages/billing.py` держит живые строки `Payment` и типа не называет, поэтому в область гейта закрытости колонки не входит. Гейт здоров потому, что присваиваний `.status` в этом модуле сегодня нет ни одного.
- ⚠️ [Phase 08] Гигиена реестра угроз: `T-08-30`, `T-08-32`, `T-08-33`, `T-08-34` переиспользованы разными планами под РАЗНЫЕ угрозы — при сводном чтении строка по одному идентификатору неоднозначна.
- ⚠️ [Phase 2 → Phase 05.1] Боевая база на ревизии `0019`; невыкачена одна — `0020` (единая подписка). **Прежняя редакция этой записи говорила «остаётся на `0012`; невыкачены `0013`…`0019`» и была НЕВЕРНА — исправлено при закрытии фазы 05.1 по свидетельству, а не по памяти:** план `05.1-14` прочитал ревизию боя КОМАНДОЙ (`SELECT version_num FROM alembic_version` на восстановленной копии боевого дампа, 2026-08-21T15:35Z), и это второй независимый источник против строки в `05.1-08-SUMMARY.md`. Следствие для прежней записи: описанный в ней механизм поломки КАЖДОГО чтения таблицы платежей (`Payment.__table__.columns` против схемы `0012`) БЕСПРЕДМЕТЕН — `kind`, `plan` и `switch_authorized` в схеме `0019` есть. Открытым остаётся выкат `0020`. Решение владельца D-26 (`defer-deploy`) на него распространяется: ревизия прогнана на КОПИИ боевой базы (пять условий сведены, популяции 0/0/4 из 4 пользователей, копия и дамп уничтожены), но на бой не накатывалась. До выката `0020` признак `subscriptions.has_free_access` в бою отсутствует, а `subscriptions.plan` — присутствует; модель их объявляет наоборот. Источник счёта — `alembic/versions/` против `alembic current` боевой базы, а не автор этой записи.
- ⚠️ [Phase 5] Подписка на событие `payment.canceled` в кабинете ЮKassa не подтверждена (D-27). Ветка `payment.canceled` из 05-02 зелёная в тестах и непроверенная в проде: пока подписка не включена (Интеграция → HTTP-уведомления, URL на `POST /api/billing/webhook` боевого домена), отменённые платежи навсегда остаются `pending`. **СТАТУС 2026-08-25: ПРИНЯТЫЙ РИСК `AR-05-01`, акцептор `chubav`** (`05-tarify/05-SECURITY.md`, угроза T-05-33, high, Repudiation). ⚠️ Принятие НЕ чинит: журнал платежей продолжает утверждать про деньги неправду. Снятие — одно действие в кабинете ЮKassa, без единой правки кода; ветка обработки готова и покрыта тестом `test_a_canceled_webhook_gives_a_pending_payment_a_terminal_status`.
- 🔴 [Phase 5, ревью раунда 2 — CR-02] `YooPayment.create()` — синхронный HTTP-вызов без таймаута внутри `async def` (`app/services/payment_service.py:128-141`), а `docker-compose.prod.yml:87` не задаёт `--workers`: воркер один, и зависшее соединение с ЮKassa вешает всё приложение, включая `/health`. Та же причина шла в раунде 1 половиной `WR-05`. **Отложено с обоснованием** (план 05-10): перенос в поток меняет модель исполнения платёжного вызова и требует своего наблюдения за временем ответа, а таймаута SDK всё равно не предоставляет — один `to_thread` проблему зависшего TLS-соединения перекладывает, а не решает.
- 🔴 [Phase 5, ревью раунда 3 — WR-07 (раунд 3)] `docker-compose.prod.yml:91-92` монтирует `/var/run/docker.sock` в контейнер `web` — это root-equivalent доступ к хосту у процесса, который терминирует `POST /api/billing/webhook`. Аутентификация этого маршрута — IP из заголовка, проставляемого прокси (`app/routes/billing.py:60-103`), и она отключается одной переменной окружения (`YOOKASSA_WEBHOOK_VERIFY_IP=false` — объявленный аварийный выход). Любая ошибка класса RCE на этом пути становится компрометацией ХОСТА, а не контейнера. Находка ПРЕДШЕСТВУЕТ фазе 05: монтирование обслуживает `app/services/wa_container_manager.py`. Фаза её не вносила, но УВЕЛИЧИЛА объём того, что доходит до этого процесса, — самая дорогая покупка продукта теперь идёт через него. Сегодняшний режим находки ревью назвало прямо: **принято никем конкретно**. **Распоряжение:** вынести управление контейнерами из `web` в отдельный воркер, владеющий сокетом, с обращением к нему через существующую очередь Redis; либо поставить перед сокетом фильтрующий прокси (например `tecnativa/docker-socket-proxy`) с разрешёнными только теми командами, которые `wa_container_manager` действительно использует, и отдать `web` адрес прокси вместо сырого сокета. Если ни то ни другое не входит в бюджет — принятый риск с НАЗВАННЫМ ВЛАДЕЛЬЦЕМ, по образцу записи R-03-09 Фазы 3 (§Deferred Items). Владелец: **chubav** (назначен 2026-08-25). **СТАТУС 2026-08-25: ПРИНЯТЫЙ РИСК `AR-05-02`** (`05-tarify/05-SECURITY.md`, угроза T-05-73, high, Elevation of Privilege): диспозиция переведена из `transfer` в `accept`, потому что передача без названного получателя семь раундов подряд передачей не являлась. ⚠️ Названный акцептор устраняет БЕСХОЗНОСТЬ, но НЕ устраняет экспозицию: сокет по-прежнему смонтирован rw (`docker-compose.prod.yml:108`, `:137`), и распоряжение выше остаётся в силе как способ снять риск. Кодом план 05-14 не трогает ни `docker-compose.prod.yml`, ни `wa_container_manager`: ревью само называет вынос работой своего размера и предлагает записью назначить владельца.
- 🟡 [Phase 5, код-ревью — сводное распоряжение по раундам 1 и 2] Имя `05-REVIEW.md` принадлежит нескольким разным документам: файл перезаписывается каждым раундом ревью, номера находок между раундами переназначены, и голый номер без раунда указывает не на ту находку. Их число эта запись не называет намеренно — оно растёт с каждым раундом; полный перечень, включая ещё не написанные раунды, отдаёт команда `git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md` (новейший сверху), а не автор записи. Каждый раунд адресуется своим неизменяемым коммитом: раунд 1 — `git show 4989f7a:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 2, warning: 10, info: 6`), раунд 2 — `git show 1d29360:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 2, warning: 9, info: 7`), раунд 3 — `git show 164d210:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 1, warning: 7, info: 5`), раунд 4 — `git show 5da8e82:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 1, warning: 4, info: 2`). Какой из раундов занимает файл сегодня, эта запись НЕ утверждает намеренно: ровно такое утверждение устаревало при каждой перезаписи, и следующая отберёт имя снова. Распоряжения раундов 1 и 2 — ниже в этой же записи; распоряжения каждого следующего раунда — отдельной записью следом, по одной записи на раунд. **Закрыты планами:** CR-01 раунда 1 → `05-07`, CR-02 раунда 1 → `05-08`, CR-01 раунда 2 → `05-11` (три строки §Deferred Items). **Приняты в работу и исполнены:** WR-01 раунда 1 (`format_amount` на `NaN`/`Infinity`) → `05-09`; WR-04 раунда 1 (константы предмета покупки) → ранее; половина WR-05 раунда 1 про обработку ошибок SDK и IN-06 раунда 1 (кнопка «Продлить» у истёкшего `free`) → `05-10`. **Отложены с обоснованием:** CR-02 раунда 2 — отдельной 🔴 записью выше, там же названа его половина из раунда 1; WR-10 (раунд 1) и WR-07 (раунд 2) — одна причина, два места отказа, ОДНО распоряжение: негодный `PLAN_LIMITS` роняет `axis_percent` сравнением строки с числом (негодный ТИП лимита — первый номер) и `json.loads` в `parsed_plan_limits`, отдавая 500 на весь раздел тарифов (негодный ФОРМАТ документа — второй номер, первым НЕ покрыт), ответ один — типизированная запись плана, проверяемая при построении `Settings`, чтобы негодный конфиг падал на старте, а не на клике покупателя, и это работа своего размера: контракт `parsed_plan_limits` меняется у трёх потребителей; WR-03, WR-06, WR-08, WR-09 раунда 1 (SQLite-каскад ревизии `0017`, ответ 200 на необработанное уведомление, неподтверждённые `limit`/`offset`, отсутствие пере-запроса платежа у ЮKassa) и IN-01…IN-05 раунда 1 — причины названы планами `05-07`, `05-08` и `05-10`; раунд 2 целиком: WR-01 (тот же lost-update в `reset_free_monthly`), WR-02 и WR-03 (гард переоткрывается одной непроверенной переменной, аварийный выключатель не оставляет следа), WR-04 (`"object": null` даёт 500 и цикл повторов ЮKassa), WR-05 (`limit`/`offset` у `GET /api/billing/transactions`), WR-06 (`∞` в сайдбаре при нулевом балансе), WR-08 («первый платёж»/«продление» решается по усечённой карте — тот же дефект нёс свой номер и в раунде 1), WR-09 (осиротевший `pending` при неожиданной форме ответа SDK), IN-01…IN-07 (в том числе IN-07 — платёж с пустым планом записывается как `free`). Ни одна находка не осталась без распоряжения; ни одна из перечисленных здесь отложенных находок не входит в гэпы `status: failed` отчёта `05-VERIFICATION.md`, а состав и число гэпов отдаёт сам отчёт.
- 🟡 [Phase 5, код-ревью раунда 3 — распоряжения] Раунд 3 читается командой `git show 164d210:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 1, warning: 7, info: 5`); каждый номер ниже квалифицирован раундом, потому что между раундами номера переназначены. **Закрыто планом:** `CR-01 (раунд 3)` — оплаченное понижение на истёкшем сроке списывалось, но не выдавалось, а понижение становилось недостижимым навсегда; закрыт планом `05-13`, отдельной строкой §Deferred Items. **Отложены с распоряжением, работа своего размера:** `WR-01 (раунд 3)` — отказ ПОВЫШЕНИЯ незнакомому плану сообщается пользователю словом «понижение»: `switch_is_refused` возвращает `True` с обеих сторон (намеренный fail-closed), а вызывающий отображает любой `True` в один код причины `downgrade`, и человеку говорят неправду плюс советуют дождаться конца срока, что не поможет; распоряжение — разделить код причины, а это требует различить два исхода отказа в самом правиле, владелец не назначен; `WR-07 (раунд 3)` — сокет Docker у веб-слоя: отдельной 🔴 записью выше, распоряжение и владелец там же; `WR-06 (раунд 3)` — план, выпавший из `PLAN_LIMITS`, рисует все четыре оси как «без ограничений», то есть безлимитный тариф, которого у пользователя нет: та же причина, что у отложенного распоряжения WR-10 (раунд 1) / `WR-07 (раунд 2)`, и распоряжение то же — типизированная запись плана, проверяемая при построении `Settings`; ВТОРОГО распоряжения не заводится, действует существующее. **Записаны с распоряжением, кода этим планом не требуют:** `WR-02 (раунд 3)` — умолчание `yookassa_return_url` строит адрес возврата из ОТОБРАЖАЕМОГО ИМЕНИ приложения (`settings.app_name`), и при незаданной переменной платёж создаётся с `return_url = "Broadcaster/billing"`; распоряжение — сделать настройку обязательной для создания платежа либо ввести явный `app_base_url`, и закрепить переменную в `docker-compose.prod.yml` так же, как план 05-07 закрепил `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER`; `WR-03 (раунд 3)` — константа `STATUS_PENDING` мертва (ноль ссылок), а путь записи пишет голый литерал `"pending"`: собственное правило модуля «константами, никогда строковыми литералами» нарушено ровно в том месте, которое оно защищает; распоряжение — либо `status=STATUS_PENDING`, либо удалить константу, но не держать обе формы одного значения; `WR-04 (раунд 3)` — `plan_card.html` зашивает `'free'`, хотя обработчик уже передаёт `free_plan_id` и `balance.html` им пользуется: распоряжение — передать идентификатор в карточку и сравнивать с ним; `WR-05 (раунд 3)` — журнал операций молча обрезается на 20 строках на том же экране, где журнал платежей свой потолок объявляет: распоряжение — объявить потолок или дать постраничный проход, инвариант применяется к обоим журналам или ни к одному; `IN-01 (раунд 3)` — неиспользуемый импорт `text` и ни разу не вызванный `logger` в `billing_service.py`, модуле, выполняющем КАЖДОЕ изменение баланса в продукте: распоряжение — убрать импорт и либо удалить `logger`, либо начать писать им `add_messages`/`reset_free_monthly`; `IN-02 (раунд 3)` — `app/application/billing/__init__.py` пуст, притом что каждый модуль пакета объявляет свои границы отдельно: распоряжение — объявить общее правило границ один раз на уровне пакета; `IN-03 (раунд 3)` — утверждение о границах в `subscription_period.py` верно для того, что модуль ИСПОЛЬЗУЕТ, и неверно для того, что он ВТЯГИВАЕТ транзитивно; у `plan_switch.py` то же утверждение держится AST-тестом `test_the_only_import_of_the_rule_is_the_declared_plan_order`, у этого модуля равноценного теста нет, и утверждение может протухнуть молча: распоряжение — либо равноценный AST-тест, либо ослабить формулировку до фактически гарантируемого; `IN-04 (раунд 3)` — `datetime.now(timezone.utc)` берётся трижды за один рендер (`app/pages/billing.py:214, 223, 331`), и два решения об одной подписке читают два разных момента: распоряжение — снимать момент один раз на входе обработчика и передавать его; `IN-05 (раунд 3)` — фраза отказа существует в двух формулировках, а комментарий рядом утверждает, что соседство удерживает их от расхождения: соседство спасает от ЗАБЫВЧИВОСТИ, но не от расхождения смысла; распоряжение — вывести одну из другой либо признать в комментарии два намеренных регистра. Находки прежних раундов, повторно НЕ открываемые, перечислены таблицей «Previously Dispositioned» ревью раунда 3; их распоряжения лежат в сводной записи по раундам 1 и 2 выше и в §Deferred Items — вторых распоряжений им не заводится.
- 🟡 [Phase 5, код-ревью раунда 4 — распоряжения] Раунд 4 читается командой `git show 5da8e82:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 1, warning: 4, info: 2`); каждый номер ниже квалифицирован раундом, потому что между раундами номера переназначены — под каждым из этих номеров в прежних раундах лежит ДРУГОЙ дефект. **Закрыто планами `05-15` и `05-17` — ни один из двух не закрывает находку в одиночку:** `CR-01 (раунд 4)` — подтверждённый платёж МЛАДШЕГО тарифа двигал срок ДЕЙСТВУЮЩЕГО СТАРШЕГО на полный календарный месяц, 9370 ₽ покупали 121 день Pro стоимостью 19 600 ₽, и число повторов не ограничивало ничто: деньги закрыты решением D-29 планом `05-15`, состояние «два разрешённых намерения разных тарифов» — потолком одновременных подписочных намерений планом `05-17`; имена колонки, ревизии и регрессий — отдельной строкой §Deferred Items. **Закрыто планом `05-15`:** `WR-01 (раунд 4)` — две стадии правила снимали признак живости в РАЗНЫЕ моменты из РАЗНЫХ источников, ответ гарда на платеже не сохранялся, а докстринг `plan_switch.py:48` утверждал обратное; тот же корень, что у `CR-01 (раунд 4)`, закрыт записью разрешения на сам платёж (`payments.switch_authorized`) и снятием утверждения «ОДНИМ НАБОРОМ ВЕЛИЧИН» из докстринга. **Отложены с распоряжением, работа своего размера, владелец не назначен:** `WR-02 (раунд 4)` — `app/pages/billing.py:195-198` держит рукописное отрицание признака живости в десяти строках от импортированного объявления, а комментарий `:100-107` существование копии отрицает, и расходятся они сегодня только на границе `expires_at == now`; распоряжение — выразить показ через то же объявление и снять второй снимок часов, это же закрывает `IN-04 (раунд 3)` в части `billing_page`; `WR-03 (раунд 4)` — ветка `if not period_is_live: return False` короткозамыкает fail-closed у плана без ранга, и пересечение двух правил не названо нигде; распоряжение — продуктовое решение о том, какое из двух правил сильнее, плюс зеркальная регрессия на стадии применения; ⚠️ после плана `05-15` путь СОХРАНЯЕТСЯ и называется прямо: незнакомый план, проданный на истёкшем сроке, получает записанное разрешение и применяется; `WR-04 (раунд 4)` — у `web`, `celery-beat` и обоих celery-воркеров боевого артефакта нет политики перезапуска, тогда как у `nginx`, `certbot` и `flower` она есть: упавший `web` не вернётся сам, а `nginx` вернётся и будет отдавать 502 на каждое уведомление ЮKassa; находка ПРЕДШЕСТВУЕТ фазе 05, но фаза материально увеличила цену отказа `web` — приём денег теперь идёт через него; распоряжение — `restart: unless-stopped` в `x-app-base` плюс проверка идемпотентности `entrypoint.sh` относительно повторного запуска, вне объёма кода фазы. **Закрыты планом `05-15` кодом, распоряжения не требуют:** `IN-01 (раунд 4)` — `app/constants.py:65` отправлял читателя `PLAN_ORDER` за правилом в один модуль из двух; закрыто задачей 3 плана `05-15` с названным там обоснованием: после плана потребителей правила стало больше, и строка стала бы неверной ещё раз; `IN-02 (раунд 4)` — AST-тест держал ПОРЯДОК двух операторов и не держал ВЕЛИЧИНУ, от которой снимается признак живости; закрыто задачей 2 плана `05-15`: план увеличил нагрузку на этот тест, и половина инварианта под возросшей нагрузкой стала бы долгом следующего раунда. Находки прежних раундов, повторно НЕ открываемые, перечислены таблицей «Previously Dispositioned» ревью раунда 4; их распоряжения лежат в записях выше и в §Deferred Items — вторых распоряжений им не заводится.
- 🟡 [Phase 5, код-ревью раунда 5 — распоряжения] Раунд 5 читается командой `git show 1ec9952:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 2, warning: 5, info: 2`); каждый номер ниже квалифицирован раундом, потому что между раундами номера ПЕРЕНАЗНАЧЕНЫ — под каждым из этих номеров в прежних раундах лежит ДРУГОЙ дефект. Ни одна находка раунда не осталась без распоряжения. **Закрыты планами:**
  - `CR-01 (раунд 5)` — блокер денежного пути: предоплаченный горизонт переводился на старший тариф целиком за цену ОДНОГО месяца (12 подтверждённых `basic` плюс один `pro` давали 395 дней Pro за 22 780 ₽ при прейскуранте 63 700 ₽). Закрыт планом `05-18` формой границы `convert-remainder` (решение владельца **D-30**, §Decisions и `05-CONTEXT.md`): остаток конвертируется в дни старшего плана по отношению цен (`converted_remainder`), те же деньги дают ≈141 день Pro. Именованные регрессии: `test_a_prepaid_horizon_is_not_converted_to_the_senior_plan_for_one_month` (падала до правки), `test_repeated_payments_of_one_plan_stay_priced_at_the_list`, `test_an_upgrade_with_one_paid_month_still_does_not_burn_the_remainder`, плюс переписанный по новой семантике с сохранением имени `test_an_upgrade_does_not_burn_the_paid_remainder` (падал до правки).
  - `CR-02 (раунд 5)` — модель `Payment` объявляет три колонки, которых нет в боевой схеме, и ломается КАЖДОЕ чтение таблицы платежей. Закрыт ДВУМЯ планами, ни один не закрывает находку в одиночку: кодовая половина — план `05-20` (фактический радиус поражения в докстринге ревизии `0019`, машинная сверка отображённых колонок с ГОЛОВНОЙ ревизией репозитория `tests/test_migrations/test_model_matches_head.py` с доказанной краснотой, закреплённое тестом свойство боевого старта `tests/test_migrations/test_deploy_applies_migrations_before_serving.py`); документная половина — план `05-21`, запись о D-26 выше по этой секции. ⚠️ Расхождение репозитория с БОЕВОЙ схемой ни одна из проверок не ловит и не может — это операционный факт D-26.
  - `WR-01 (раунд 5)` — обоснование «остаточное окно гонки денежно нейтрально благодаря D-29» неверно по построению. Закрыт планом `05-19` в докстринге `create_payment` и планом `05-21` в §Deferred Items: настоящий исход окна — ПОРЯДОК подтверждения, механизм, дающий нейтральность свойством, назван и назван несделанным.
  - `WR-03 (раунд 5)` — AST-тест порядка ходил по операторам верхнего уровня и видел один сдвиг срока из трёх. Закрыт планом `05-19`: обход `ast.walk`, множество двигающих имён ВЫВОДИТСЯ из импорта предметного модуля, второй инвариант по цели присваивания; краснота доказана двумя негативными контролями над синтетическими исходниками, живущими в суите постоянно.
  - `WR-04 (раунд 5)` — два докстринга одного денежного правила утверждали противоположное («по УПЛАЧЕННОМУ тарифу» над строкой, читающей цену ДЕЙСТВУЮЩЕГО). Закрыт планом `05-19`: обе формулировки названы выражением `paid / price(subscription.plan)`, и вторая стоит непосредственно над строкой, которая это выражение вычисляет.
  - `WR-05 (раунд 5)` — «потолок делает состояние недостижимым» при собственном ЗЕЛЁНОМ тесте, который его достигает. Закрыт планом `05-19` В НАЗВАННОМ НАПРАВЛЕНИИ: названы ОБА остаточных окна (гонка и срок давности), доказывающий тест назван по имени, а `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form` переименован в `test_a_second_intent_of_another_plan_is_refused_while_the_first_is_fresh` — по тому, что тело держит. ⚠️ Приведено к факту УТВЕРЖДЕНИЕ, а сам факт не изменён: второе окно ОТКРЫТО, пользователь, подождавший сутки, заводит второе оплачиваемое намерение другого тарифа гарантированно, и прохибиция плана `05-17` «MUST NOT позволять двум разрешённым намерениям разных тарифов висеть одновременно» остаётся ФАЛЬСИФИЦИРОВАННОЙ. Закрытие требует либо подтверждённой подписки на `payment.canceled` (D-27), либо снятия намерения С ОПЛАТЫ при истечении срока давности — обе работы своего размера, владелец не назначен.
  - **Отложено с распоряжением, владелец не назначен:** `WR-02 (раунд 5)` — записанный отказ (`switch_authorized = False`) чтится ровно на ОДНОЙ из двух ветвей `_extend_subscription`: ветвь ПЕРВОЙ ВСТАВКИ пишет план безусловно и выдала бы полный месяц на плане, в котором было отказано. Сегодня ЛАТЕНТНО ровно потому, что отрицательный ответ через форму не приходит (гард возвращает 302 до создания платежа), и латентность названа ПРИЧИНОЙ отсрочки, а не отсутствием дефекта. Признак, который снимет латентность, назван: момент, когда отказ перестанет возвращаться немедленно и на платёж приедет записанный ОТКАЗ, — это будущее прямо анонсировано комментарием гарда (`app/pages/billing.py:361-366`). Распоряжение: свести обе ветви к одному решению (вынести чтение записанного ответа выше развилки) либо закрепить инвариант тестом, который упадёт, когда отрицательный ответ станет достижим.
  - **Записаны с распоряжением, кода сегодня не требуют:** `IN-01 (раунд 5)` — `create_payment` не проверяет, что у подписочного платежа есть план: при `plan=` описание уезжает в ЮKassa строкой `Подписка «»`, а условие потолка считает такое намерение отдельным тарифом. Сегодня недостижимо — единственный вызывающий берёт идентификатор из выбранного плана, — но это ровно тот класс, который фаза закрывала у `kind` и `switch_authorized`; распоряжение — обязательная проверка рядом с потолком, тем же порядком, каким сделаны обязательными `kind` и `switch_authorized`.
  - `IN-02 (раунд 5)` — срок давности сравнивает часы ПРИЛОЖЕНИЯ (`datetime.now(timezone.utc)`) с часами СУБД (`server_default=func.now()`); на боевом артефакте это два разных контейнера, расхождение сдвигает срок давности в обе стороны и в суите не проявляется, потому что там обе стороны — один процесс; распоряжение — считать порог тем же источником времени либо проставлять время рождения приложением, как это уже делает время подтверждения.
  - Ни одна из перечисленных отложенных находок не входит в гэпы отчёта `05-VERIFICATION.md` раунда 5, а состав и число гэпов отдаёт САМ отчёт. Находки прежних раундов, повторно НЕ открываемые, перечислены таблицей «Previously Dispositioned» ревью раунда 5; их распоряжения лежат в записях выше и в §Deferred Items — вторых распоряжений им не заводится. ⚠️ Запись фиксирует ИСПОЛНЕННУЮ работу, а не пройденную проверку: вердикт даёт верификация раунда 6, и три judgment-tier прохибиции фазы (BILL-05, BILL-06, BILL-07) остаются `unresolved`.
- 🟡 [Phase 5, код-ревью раунда 6 — распоряжения] Раунд 6 читается файлом `.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 1, warning: 7, info: 4` — двенадцать находок). Каждый номер ниже квалифицирован строкой «(раунд 6)», потому что между раундами номера ПЕРЕНАЗНАЧЕНЫ: под каждым из этих номеров в прежних раундах лежит ДРУГОЙ дефект, и голый номер указал бы не на ту находку. Ни одна находка раунда не осталась без распоряжения; разнесены они на три группы, и сумма групп равна двенадцати (7 + 4 + 1). **Группа первая — закрыты планами волн 18-20:**
  - `CR-01 (раунд 6)` — блокер денежного пути: ветка ОТКАТА `_apply_extension` (`if price_from is or price_to is `) оставляла `base` связанной с `subscription.expires_at` и молча восстанавливала утечку неограниченного предоплаченного горизонта, которую сам план `05-18` закрывал в соседней ветке; ветку не проверял ни один тест. Закрыт планом `05-22` формой границы `cap-one-month` (решение владельца **D-31**, `05-CONTEXT.md`): правило объявлено чистой функцией `capped_carryover` и ЧИТАЕТСЯ денежным путём. Те же 22 780 ₽ дают теперь 60 дней Pro вместо 396. Именованные регрессии, доказанно красные на коде до правки: `test_an_unreadable_paid_plan_price_does_not_carry_the_whole_horizon` и `test_an_upgrade_from_free_does_not_carry_the_whole_horizon`, плюс модульная таблица `test_the_capped_carryover_never_exceeds_one_month_on_any_day_of_the_year` и `test_a_remainder_shorter_than_a_month_is_carried_whole`.
  - `WR-01 (раунд 6)` — обоснование длины месяца в докстринге `converted_remainder` опровергается тремя строчками арифметики: `month_days` сокращается и в ответ не входит вовсе. Закрыт планом `05-23` В НАЗВАННОМ НАПРАВЛЕНИИ — абзац приведён к ПОВЕДЕНИЮ, а не поведение к абзацу: величина названа свойством и закреплена характеризующим тестом на трёх базах с длинами месяца 28, 30 и 31.
  - `WR-02 (раунд 6)` — записанный ответ гарда (`switch_authorized = False`) чтился в обход правила истечения: утверждение «истечение снимает отказ на обеих стадиях» для него было ложным. Закрыт планом `05-24`: `app/services/payment_service.py:978` несёт `refused = period_is_live and not db_payment.switch_authorized`, рядом комментарий называет причину, регрессия `test_an_expired_period_lifts_the_recorded_refusal_and_leaves_no_trace` доказанно красная на коде до правки.
  - `WR-03 (раунд 6)` — объявленный инвариант «план только повышается» опровергнут прогоном: через окно срока давности действующий тариф идёт ВНИЗ, и до этой волны это происходило БЕЗ ЕДИНОЙ записи в журнале. Закрыт планом `05-24` по ответу владельца `record-wins` (решение **D-32**, `05-CONTEXT.md`): объявление снято из ЧЕТЫРЁХ мест деклараций (`app/services/payment_service.py`, `app/application/billing/plan_switch.py`) и заменено описанием трёх настоящих исходов, а понижение получило собственный ключ журнала уровня `warning` — `subscription_plan_downgraded` с полями `plan`, `paid_plan`, `decided_by`, `period_was_live`. Закреплено `test_a_demotion_by_the_recorded_answer_leaves_its_own_downgrade_trace` и машинной проверкой `test_neither_declaration_claims_the_plan_only_grows_at_the_apply_stage`. ⚠️ План называл ДВА места объявления; исполнитель насчитал на дереве ЧЕТЫРЕ и снял все четыре — расхождение записано здесь, а не молча исправлено.
  - `IN-01 (раунд 6)` — у `converted_remainder` не было прямых модульных тестов в файле, который владеет арифметикой. Закрыт планом `05-23`: семь тестов прямой таблицы решений в `tests/test_application/test_subscription_period.py`. ⚠️ Фактическая поправка к находке: символ встречался в `tests/` один раз (комментарием, без вызова), а не «ни разу», как утверждала находка.
  - **`IN-03 (раунд 6)` — ПРЕМИССА НАХОДКИ ОПРОВЕРГНУТА ПРОГОНОМ, И ЗАКРЫТИЕМ «КАК НАПИСАНО» ЭТО НЕ ЯВЛЯЕТСЯ.** Находка объявляла ветку `if base <= now_utc: return now_utc` в `converted_remainder` недостижимой на том основании, что «`countdown_base` уже вернул `now_utc` в этом случае». Рассуждение обратное: возврат `now_utc` из `countdown_base` не обходит ветку, а УДОВЛЕТВОРЯЕТ её условие — ветка берётся. План `05-23` разрешил вопрос прогоном ОБЕИХ форм функции до внесения правки: снятие ветки меняет ответ для отсутствующего и истёкшего срока с «сегодня» на «сегодня плюс сутки», то есть пускает нулевой остаток через нижнюю границу `max(prorated_days, 1)` и покупает ОДИН день тарифа, за который не заплачено. Ветка НЕСУЩАЯ. Взят второй путь, названный самой находкой дословно: сравнение переписано на `if base == now_utc`, а на месте ветки стоит комментарий, называющий `countdown_base` владельцем зажима (D-04) и два закрепляющих теста. **Распоряжение:** находка требует ПЕРЕФОРМУЛИРОВКИ, а не повторения. Если раунд 7 повторит требование «снять ветку», это будет требование правки ПОВЕДЕНИЯ денежного пути, и адресовать его следует владельцу, а не исполнителю. Критерий приёмки плана `05-23` «число вхождений `return now_utc` равно `0`» остался НЕВЫПОЛНЕННЫМ и невыполнимым — это записано его исполнителем прямо.
  - `IN-04 (раунд 6)` — ключ `subscription_prorating_skipped` испускался двумя ветвями с разными словарями `unreadable` и разными наборами полей, и по одному ключу исход был неразличим. Закрыт планом `05-22`: добавлено поле `stage` со значениями `prorate_refused` (ветка отказа, `:1143`) и `convert_remainder` (ветка отката, `:1230`), оба утверждает `test_the_refused_branch_names_its_own_stage_in_the_journal`. Этой же находкой исправлено НЕВЕРНОЕ НАЗВАНИЕ КЛЮЧА в записи решения D-30 (`05-CONTEXT.md`): там ключом отката значился `subscription_remainder_converted`, который пишется в противоположной ветке.
  - **Группа вторая — отложены с обоснованием, требуют кода вне объёма волны 21, владелец-решение назван у каждой:** `WR-04 (раунд 6)` — запись в БД, следующая за УСПЕШНЫМ `YooPayment.create`, не обёрнута ни обработкой ошибки, ни журналом: если коммит упадёт, у ЮKassa останется созданный платёж, которого нет в нашей таблице, и человек заплатит за строку, которую никто не подтвердит. Следствие названо, а не смягчено: это потеря денег на стороне покупателя без следа на нашей. Отложено потому, что закрытие требует решения о компенсирующем действии (пере-запрос статуса либо отмена платежа) — обе половины лежат в матрице `COVERAGE.md` со своими решениями, и включать половину механизма значило бы записать решение, которого никто не принимал. Распоряжение — владелец выбирает компенсирующее действие, после чего строка матрицы пересматривается вместе с планом.
  - `WR-05 (раунд 6)` — `handle_webhook` защищает ПАКЕТНУЮ ветку от отсутствующего `messages_count`, а у подписочной ветки эквивалентного гарда на `plan` нет: подписочный платёж с `plan IS NULL` пройдёт до записи. Сегодня недостижимо через форму (единственный вызывающий берёт идентификатор из выбранного плана), и латентность названа ПРИЧИНОЙ отсрочки, а не отсутствием дефекта. Распоряжение — обязательная проверка рядом с потолком, тем же порядком, каким сделаны обязательными `kind` и `switch_authorized`; это ровно тот же класс, что `IN-01 (раунд 5)`, и закрывать их следует одним планом.
  - `WR-06 (раунд 6)` — `GET /api/billing/transactions` принимает `limit` и `offset` без проверки границ. Следствие: отрицательный или огромный `limit` уходит в запрос как есть. Отложено: маршрут не имеет потребителя в интерфейсе после сноса `POST /api/billing/purchase` (D-24), и его судьба — отдельный пункт Claude's Discretion, не решённый фазой. Распоряжение — либо границы значений, либо снос маршрута вслед за `POST /purchase`; выбор за владельцем, потому что это решение о публичной поверхности API, а не о строке кода.
  - `WR-07 (раунд 6)` — `tests/test_migrations/test_model_matches_head.py` сверяет только ИМЕНА колонок: гейт выката проходит зелёным при расхождении типа или nullability. Следствие названо: тот самый класс дефекта, ради которого гейт заведён (`CR-02 (раунд 5)`), проходит его наполовину. Отложено потому, что сверка типов между SQLAlchemy и Alembic требует нормализации диалектных типов — работа своего размера. Распоряжение — расширить сверку на тип и nullability при следующем касании миграционного гейта; до тех пор гейт честно называется частичным.
  - **Группа третья — записана с распоряжением, кода сегодня не требует:** `IN-02 (раунд 6)` — всякая конверсия усекает неполный день оплаченного остатка. Закрыто планом `05-23` ФРАЗОЙ В ДОКСТРИНГЕ на месте обещания: абзац о нижней границе сам называет величину усечения, и утверждение перестало быть шире поведения. Кода это не потребовало и не должно было: усечение — свойство выбранной формы `convert-remainder` (D-30), а не дефект её реализации.
  - ⚠️ Владелец риска `WR-07 (раунд 3)` — монтирование `/var/run/docker.sock` в контейнер `web`, терминирующий вебхук ЮKassa, — НАЗНАЧЕН 2026-08-25 — `chubav`, принятый риск `AR-05-02` в `05-tarify/05-SECURITY.md`. Запись о шести раундах без имени сохранена намеренно: она объясняет, ПОЧЕМУ диспозиция переведена из `transfer` в `accept`. Распоряжение записано и проверено (вынос управления контейнерами в отдельный воркер либо фильтрующий прокси перед сокетом), не хватает только человека.
  - ⚠️ Запись фиксирует ИСПОЛНЕННУЮ работу, а не пройденную проверку: вердикт даёт верификация раунда 7, и три judgment-tier прохибиции фазы (BILL-05, BILL-06, BILL-07) остаются `unresolved`. Прохибиция BILL-05 в половине «MUST NOT менять действующий тариф вниз» остаётся `unresolved` **ПО РЕШЕНИЮ владельца (D-32), а не по недосмотру**: закрыть её значило бы отменить D-28. Вторая половина той же прохибиции — «не оставив в журнале ни одной записи» — ЗАКРЫТА ключом `subscription_plan_downgraded`.
  - ⚠️ Числа записи о `CR-01 (раунд 5)` выше по этой секции сняты прогоном раунда 5 от своего сегодняшнего дня; прогон раунда 6 воспроизвёл ту же величину на сутки большей, и в `05-CONTEXT.md` оставлено число раунда 6 — оно же стоит в докстринге `capped_carryover`. Это одна величина, измеренная в разные дни, а не два дефекта; сказано здесь, чтобы раунд 7 не прочитал разницу записей как новое расхождение.
- 🟡 [Phase 5, планирование раунда 7 — СУЖЕНИЕ ОБЪЁМА, решение владельца 2026-08-18] Объём закрытия гэпов раунда 7 **сужен владельцем**, и четыре уже спланированных плана СНЯТЫ ДО ИСПОЛНЕНИЯ: `05-27`, `05-29`, `05-30` и `05-32`. Они были написаны волнами 22-25 (коммиты `c133714` и `032de63`), прошли проверку планировщика, и **дефекта в них нет** — снятие есть решение об объёме, а не о качестве.
  - **Причина названа арифметикой, а не впечатлением.** Раунд 6 дал 17/20, раунд 7 — 19/24: две истины закрылись, а знаменатель вырос на четыре, и вырос ОТ АРТЕФАКТОВ ПРЕДЫДУЩЕГО РАУНДА ЗАКРЫТИЯ. План `05-22` внёс свой `must_have` №8 — он стал гэпом 2 раунда 7. План `05-25` построил гейт объявлений — из него вышли истины №18-20 И та объявленная щель, в которой раунд 7 нашёл гэп 1. То есть гэп 1 раунда 7 найден в тексте, который правили планы того же раунда, и в щели средства, построенного планом того же раунда. При этом BILL-05, BILL-06 и BILL-07 — все три `✓ SATISFIED`, критерии успеха 1-3 — `✓ VERIFIED`, а критерий 4 держится `PRESENT_BEHAVIOR_UNVERIFIED` только потому, что браузерного харнесса в проекте нет.
  - **Куда ушло содержимое снятых планов — поимённо, чтобы след не оборвался.** `05-27` (решение D-34, блок `overrides:`, строка C2 `05-UI-SPEC.md`) → `05-33`. `05-30`, задача 2 (три абзаца `payment_service.py` о ветке нечитаемой цены) → `05-33`; `05-30`, задача 1 (расширение гейта объявлений различением свидетеля по имени) → СНЯТА и записана ПРИНЯТЫМ ДОЛГОМ. `05-32` целиком (константа «60 дней» в D-30/D-31, исторический статус `05-22-SUMMARY.md:134`, сверка `COVERAGE.md`) → `05-33`. `05-29` целиком (расширение множества проверяемых гейтом модулей) → СНЯТ и записан ПРИНЯТЫМ ДОЛГОМ. Оба принятых долга и их причины записывает решение **D-34** в `.planning/phases/05-tarify/05-CONTEXT.md`.
  - **Тексты снятых планов сохранены ИСТОРИЕЙ GIT** (`c133714`, `032de63`) и никуда не копировались: копия завела бы второй источник истины о работе, которой не будет.
  - **Выжившие планы НЕ ПЕРЕНУМЕРОВАНЫ намеренно.** В работе остались `05-28`, `05-31` и новый `05-33`; разрыв 05-29…05-30 и 05-32 в нумерации ОБЪЯСНЁН этой записью и записью D-34. Сквозная перенумерация порвала бы соответствие PLAN↔SUMMARY и все ссылки §«Phase 5: Тарифы» ROADMAP — то есть закрыла бы дыру ценой более дорогой потери.
  - ⚠️ Запись фиксирует РЕШЕНИЕ ОБ ОБЪЁМЕ, а не пройденную проверку: вердикт по сокращённому закрытию даёт верификация раунда 8, и эта запись его не предвосхищает. Четыре находки код-ревью раунда 7 (`WR-02`, `IN-02`, `IN-03`, `IN-04`), которые закрывали снятые планы, получают распоряжения ПРИНЯТЫМ ДОЛГОМ со ссылкой на D-34 — их вносит план `05-31`, и до его исполнения они не записаны.
- 🟡 [Phase 5, гэп 4 раунда 7 — РЕГРЕССИЯ ТРЕКИНГА ЗАКРЫТА МЕХАНИЗМОМ, А НЕ ЗНАЧЕНИЕМ] Раунд 7 нашёл `.planning/STATE.md` разошедшимся с ROADMAP спустя ОДИН раунд после того, как то же расхождение было закрыто правилом «единственный источник счёта планов — ROADMAP» (гэп 3 раунда 5): frontmatter держал `completed_plans: 73` при `78` отмеченных планах ROADMAP. **Значения свёл обратно коммит `75fbdfa`** («docs(05): раунд 7 верификации — отчёт и сведение трекинга STATE с ROADMAP»), и одного этого было мало — что доказано тем же деревом: к исполнению плана `05-31` поле разошлось СНОВА (записано `78`, из ROADMAP выводится `80`), потому что волны 22 и 23 отметили исполненными планы `05-28` и `05-33`, а машинно читаемого поля не тронули. ⚠️ **ЗДЕСЬ ИСПРАВЛЕНА ОШИБКА ПРЕМИССЫ ПЛАНА `05-31`, А НЕ ПОВТОРЕНА.** План называл приводящим коммитом `6ade68e` — проверка по дереву (`git show --stat 6ade68e`) показала, что тот тронул ТОЛЬКО `.planning/ROADMAP.md` и потому есть ТРЕТИЙ обход, а не починка; STATE правил `75fbdfa`, отстоящий от него на семь секунд. Названо здесь, потому что запись, называющая не тот коммит, есть ровно тот класс дефекта, ради которого писана эта волна. **Причина установлена по дереву, а не угадана:** трекинг волны 18 (`4430ef4`) правил ОБА файла, а трекинг волн 19, 20 и 21 (`ada9500`, `1e80cb5`, `6ade68e`) правил ТОЛЬКО ROADMAP — расхождение вносит ИНСТРУМЕНТ трекинга, а не автор записи, и обход был возможен ровно потому, что обходить было нечего. Показательно и то, КЕМ расхождение чинилось: `75fbdfa` есть коммит ВЕРИФИКАЦИИ, то есть чинил не тот, кто вносил, — а такая починка механизма не заводит по построению. **Повторение закрыто механизмом (план `05-31`):** `tests/test_planning/test_state_progress_matches_roadmap.py` ВЫВОДИТ счёт из отметок ROADMAP и сверяет его с полем `progress`; прогон входит в `just test`, а рецепт `just tracking-check` даёт шагу трекинга способ проверить себя ДО коммита, а не после следующей верификации. Зубы доказаны воспроизведением САМОЙ регрессии на синтетической паре («ROADMAP на `78` исполненных / STATE на `73`») и ростом `total_plans` от добавленного неотмеченного плана — синтетическими текстами, а не правкой настоящих файлов. ⚠️ Цена механизма принята сознательно и названа: в окне между коммитом плана и коммитом трекинга гейт КРАСНЫЙ — это и есть его предмет, окно обязано быть видимым. ⚠️ Гейт прозы этого файла не читает ни одной строкой и решения D-33 не переоткрывает: предмет — одно машинно читаемое поле и один объявленный источник.
- 🟡 [Phase 5, код-ревью раунда 7 — распоряжения] Раунд 7 дал итог `critical: 1, warning: 6, info: 6` — ТРИНАДЦАТЬ находок. Какой раунд занимает имя `.planning/phases/05-tarify/05-REVIEW.md` сегодня, запись не утверждает намеренно: имя перезаписывается каждым раундом, и адресный коммит отдаёт `git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md`, а не автор записи. Каждый номер ниже квалифицирован строкой «(раунд 7)», потому что между раундами номера ПЕРЕНАЗНАЧЕНЫ: под тем же номером в прежних раундах лежит ДРУГАЯ находка, и голый номер указал бы не на тот дефект. Ни одна находка раунда не осталась без распоряжения; разнесены они на четыре названные группы, и сумма групп равна тринадцати (2 + 4 + 4 + 3). **Группа первая — закрыты этим сокращённым закрытием:**
  - `CR-01 (раунд 7)` — блокер денежного пути: `_plan_price` поднимал исключение вместо `` на испорченном перечне тарифов, то есть 5xx на уведомлении ЮKassa, цикл повторов и платёж, застрявший `pending` навсегда, — при том что собственный контракт функции обещал обратное с названной ценой. Закрыт планом `05-28`: обход `parsed_plan_limits` ушёл под защиту ЗАКРЫТЫМ перечнем типов (`InvalidOperation`, `TypeError`, `ValueError`, `AttributeError`), расхождение конфига с базой получило собственный журнальный ключ `plan_limits_unreadable` уровня `error`, испускаемый в модуле ровно один раз, а параметризованная регрессия `test_a_malformed_plan_list_does_not_break_the_notification` по четырём формам испорченного `PLAN_LIMITS` ПАДАЛА до правки — доказано отдельным RED-коммитом. ⚠️ **Остаток назван, а не умолчан.** `parsed_message_packages` (`app/config.py`) имеет ТУ ЖЕ форму отказа — голый `json.loads` строки окружения без схемы — и роняет `GET /api/billing/packages` и `POST /billing/purchase`. Раунд 7 назвал это прямо («Consider the same treatment») и вердикту НЕ вменил: цена там — страница, а не застрявший платёж, и план `05-28` его сознательно не исполнял, оставив распоряжение этой записи. Распоряжение: та же защита при следующем касании `app/config.py`, отдельной ревизии объёма не требующая; форсирующий признак возврата — находка того же класса на пакетной ветке либо отказ раздела пакетов в проде. Владелец не назначен.
  - `WR-01 (раунд 7)` — `_extend_subscription` объявлял прохибицию `05-01` СОБЛЮДЁННОЙ, тогда как тот же модуль пятью сотнями строк ниже объявлял ту же ветку ИСКЛЮЧЕНИЕМ из неё, а зелёный тест суиты доказывал именно исключение: три источника называли три разных статуса одной прохибиции. Закрыт планом `05-33`: статус сведён к ОДНОМУ каноническому прочтению — решению D-34 (`05-CONTEXT.md`), — и все его потребители приведены к нему ЦИТАТОЙ, а не второй формулировкой: три абзаца `app/services/payment_service.py` и строка C2 `05-UI-SPEC.md` ИСПРАВЛЕНЫ, а расхождение ЗАПИСЕЙ (текст исполненного плана `05-22` против ответа владельца на чекпойнт того же плана) закрыто блоком `overrides:` во frontmatter `05-VERIFICATION.md`.
  - **Группа вторая — ПРИНЯТЫ ДОЛГОМ решением владельца, а не оставлены молча. Ни одна из четырёх не записывается «закрытой»: они ПРИНЯТЫ, и это разные слова.** Все четыре закрывались планами, СНЯТЫМИ до исполнения при сужении объёма закрытия раунда 7; обоснование не заводится вторым рядом, оно записано решением **D-34** (`05-CONTEXT.md`) арифметикой: раунд 6 дал 17/20, раунд 7 — 19/24, то есть две истины закрылись, а знаменатель вырос на четыре и вырос ОТ АРТЕФАКТОВ ПРЕДЫДУЩЕГО РАУНДА ЗАКРЫТИЯ.
  - `WR-02 (раунд 7)` — гейт объявлений исключает половину НАМЕРЕНИЯ денежного пути: множество проверяемых модулей выводится из состава пакета, поэтому `app/pages/billing.py`, вход уведомления и общий гард источника в него не попадают. Закрывал снятый план `05-29` (расширение множества проверяемых гейтом модулей). ПРИНЯТ ДОЛГОМ, пункт (а) D-34; причина отказа от машинного закрытия названа там механикой, а не ленью: всякое такое закрытие вводит НОВУЮ поверхность проверки, а раунд 7 нашёл свой гэп 1 именно в щели средства, построенного тем же раундом. Форсирующий признак возврата: находка класса «модуль денежного пути вне гейта» в раунде, который сам этих мест не правил.
  - `IN-02 (раунд 7)` — поле `decided_by` в испускании `subscription_plan_downgraded` постоянно ПО ПОСТРОЕНИЮ (ветка достижима только при `refused is False`), и, в отличие от соседнего `period_was_live`, таковым не помечено, поэтому следующий читатель примет его за различающее. Закрывал снятый план `05-30`; в содержимое, перенесённое в `05-33`, не вошёл. ПРИНЯТ ДОЛГОМ со ссылкой на D-34. Форсирующий признак возврата: решение, принятое по значению этого поля как по различающему.
  - `IN-03 (раунд 7)` — мёртвый `render_ledger` (не вызывается ничем в дереве — это разовый печатник, которым засеян реестр долга) и избыточный алиас `Declaration.opening`, возвращающий `fingerprint` дословно, в модуле, весь предмет которого — «одно объявление, одно место». Закрывал снятый план `05-30`. ПРИНЯТ ДОЛГОМ со ссылкой на D-34. Форсирующий признак возврата: следующее касание гейта объявлений.
  - `IN-04 (раунд 7)` — разбор строки реестра долга падает ЗАКРЫТО при столкновении с разделителем `::`, роняя гейт целиком вместо отчёта об одной строке: отпечаток есть начало настоящего абзаца, а причина — свободный текст, и любой из двух однажды может содержать разделитель. Закрывал снятый план `05-30`. ПРИНЯТ ДОЛГОМ со ссылкой на D-34. Форсирующий признак возврата: первый настоящий отказ разбора реестра.
  - **Группа третья — отложены с обоснованием, и обоснование НАЗВАНО, а не подразумевается.** Все четыре суть переномерованные `WR-04`…`WR-07` раунда 6, уже отложенные записью раунда 6 выше по этой секции и названные `Gaps Summary` раунда 7 записанным долгом фазы, НЕ вменяемым вердикту. Вторых распоряжений им не заводится — действуют существующие.
  - `WR-03 (раунд 7)`, он же WR-04 раунда 6 — запись в БД, следующая за УСПЕШНЫМ `YooPayment.create`, не обёрнута ни обработкой ошибки, ни журналом: у ЮKassa остаётся созданный платёж, которого нет в нашей таблице. Обоснование отсрочки прежнее: закрытие требует РЕШЕНИЯ о компенсирующем действии (пере-запрос статуса либо отмена платежа), обе половины лежат в матрице `COVERAGE.md`, и включить половину механизма значило бы записать решение, которого никто не принимал. Владелец не назначен.
  - `WR-04 (раунд 7)`, он же WR-05 раунда 6 — у подписочной ветки `handle_webhook` нет гарда на `plan`, эквивалентного пакетному, а ветка первой вставки изобретает `"free"`. Обоснование прежнее: сегодня недостижимо через форму (единственный вызывающий берёт идентификатор из выбранного плана), и латентность названа ПРИЧИНОЙ отсрочки, а не отсутствием дефекта; это тот же класс, что `IN-01 (раунд 5)`, и закрывать их следует одним планом.
  - `WR-05 (раунд 7)`, он же WR-06 раунда 6 — `GET /api/billing/transactions` принимает `limit` и `offset` без проверки границ. Обоснование прежнее: у маршрута нет потребителя в интерфейсе после сноса `POST /api/billing/purchase` (D-24), и выбор между границами значений и сносом маршрута есть решение о ПУБЛИЧНОЙ ПОВЕРХНОСТИ API, а не о строке кода. Владелец не назначен.
  - `WR-06 (раунд 7)`, он же WR-07 раунда 6 — `tests/test_migrations/test_model_matches_head.py` сверяет только ИМЕНА колонок и только для `payments`: тот самый класс дефекта, ради которого гейт заведён (`CR-02 (раунд 5)`), проходит его наполовину. Обоснование прежнее: сверка типов и nullability между SQLAlchemy и Alembic требует нормализации диалектных типов — работа своего размера; до тех пор гейт честно называется частичным. Распоряжение — расширять при следующем касании миграционного гейта.
  - **Группа четвёртая — записаны с распоряжением, кода этим закрытием не требуют:**
  - `IN-01 (раунд 7)` — истёкший либо выпавший из `PLAN_LIMITS` план показывается действующим правом: `current_plan_id` берётся из строки подписки БЕЗ учёта истечения, а неизвестный идентификатор даёт пустую запись, отчего все четыре оси рисуются «без ограничений» — то есть безлимитным тарифом, которого у пользователя нет. Цена — ПОКАЗ, а не доступ: применения тарифных лимитов в системе нет вовсе по решению D-08 (пункт 5 записанного долга фазы), — но показ и есть предмет поставки этой фазы. Интерфейсная работа вынесена за границу объёма планами `05-11` и `05-13`. Распоряжение: решить семантику ЯВНО — подавать запись `FREE_PLAN_ID` при истёкшем сроке либо неизвестном идентификаторе, либо подписать состояние словами вместо «без ограничений»; владелец не назначен. Это же поднято зондом краевых случаев плана `05-31` как допущение E-02 (BILL-06), и прохибиция остаётся `unresolved`.
  - `IN-05 (раунд 7)` — `first_payment_at` называет ПРОДЛЕНИЕ «первым платежом», как только журнал усечён: контракт макроса («самая ранняя строка плана и есть его первый платёж») верен ТОЛЬКО В ОКНЕ, а `get_payment_history` режет журнал на `PAYMENT_LIST_CAP`, и страница уже знает об усечении признаком `payments_truncated`. Распоряжение: гасить подпись при `payments_truncated` либо назвать границу в докстринге макроса рядом с существующей фразой контракта — второе дешевле и закрывает ровно то, что неверно. Это же поднято зондом краевых случаев плана `05-31` как допущение E-03 (BILL-07), и прохибиция остаётся `unresolved`.
  - `IN-06 (раунд 7)` — монтирование `/var/run/docker.sock` в контейнер `web`, терминирующий уведомление ЮKassa: root-equivalent доступ к хосту у процесса, обрабатывающего запросы. Находка ПРЕДШЕСТВУЕТ фазе 05 и УЖЕ записана отдельной 🔴 записью выше по этой секции под номером WR-07 раунда 3 — с распоряжением (вынос управления контейнерами в отдельный воркер либо фильтрующий прокси перед сокетом). Здесь она КВАЛИФИЦИРУЕТСЯ раундом 7 как найденная ПОВТОРНО, и второй записи не заводится: два распоряжения об одном предмете расходятся, и расхождение обнаруживается позже, чем стоило бы. ⚠️ Владелец риска НАЗНАЧЕН 2026-08-25 — `chubav`; риск принят как `AR-05-02` (`05-tarify/05-SECURITY.md`, T-05-73). Счёт «седьмой раунд подряд без имени» сохранён как история, а не как текущее состояние.
  - ⚠️ **Вердикт раунда 7 записан, а не подразумевается:** `gaps_found`, счёт `19/24` (одна истина — `present, behavior-unverified`), прогон 2026-08-18T10:44:15Z. Что именно закрыто, одной фразой: верхняя граница предоплаченного горизонта поставлена на ОБЕИХ ветках денежного пути, объявлена ОДНИМ местом (`capped_carryover`), читается кодом и проверена ПЕРЕБОРОМ ВСЕХ ДНЕЙ ГОДА, а не образцом. Что осталось человеку — перечень `human_verification` того же отчёта; здесь он НЕ переписывается и называется ссылкой, потому что вторая копия перечня есть второй источник истины.
  - ⚠️ Запись фиксирует ИСПОЛНЕННУЮ работу, а не пройденную проверку: вердикт по сокращённому закрытию раунда 7 даёт верификация раунда 8, и настоящая запись его не предвосхищает. Находки прежних раундов, повторно НЕ открываемые, перечислены таблицей «Previously Dispositioned» ревью раунда 7; их распоряжения лежат в записях выше и в §Deferred Items — вторых распоряжений им не заводится.
- Brownfield-риск: система живая и покрыта тестами; протоколы отправки Telegram, WhatsApp и MAX трогать нельзя.
- ⚠️ [Phase 3] Браузерных/e2e-тестов в проекте нет: рантайм-поведение Alpine (гард повторной отправки в общем макросе подтверждения, 12 мест) держится на ручной проверке. Регрессия автотестами не поймается.
- Р-36-1 (открыто): одиннадцать записей реестра якорей несут в поле selector прозаический хвост, три — заполнитель, две — чистую прозу. Утверждение плана 10-36 «все 21 запись набираются в консоли» на замере не выполнено и планом 10-36 не достигнуто; занесено в .planning/WINDOWS.md как unmet-truth.
- ЧЕТВЁРТОЕ окно видимости отказа, внесённое планом 10-49: успешный ФОНОВЫЙ обмен (сентинель подгрузки, автосохранение, опрос) гасит НАСТОЯЩИЙ отказ, который человек не успел прочитать. Наблюдаемо только рантаймом браузера, ни одним правилом не стережётся, владельца НЕТ. T-10-49-05 accept; запись 74 .planning/WINDOWS.md

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 260803-l8h | обновить версию Baileys до v7.0.0-rc14 | 2026-08-03 | c9c2721 |  | [260803-l8h-baileys-v7-0-0-rc14](./quick/260803-l8h-baileys-v7-0-0-rc14/) |
| 260803-pb6 | переделать работу с MAX под последнюю версию библиотеки maxapi-python | 2026-08-03 | 0879a75 |  | [260803-pb6-max-maxapi-python](./quick/260803-pb6-max-maxapi-python/) |
| 260803-rqt | Исправить зависание MAX worker: Redis BLPOP timeout, корректный shutdown, heartbeat health-check контейнера и маскирование REDIS_URL | 2026-08-03 | 3f3b175 |  | [260803-rqt-max-worker-redis-blpop-timeout-shutdown-](./quick/260803-rqt-max-worker-redis-blpop-timeout-shutdown-/) |
| 260804-8wh | исправить проблемы контейнеров MAX: ложные Redis BLPOP timeout, падение синхронизации групп на CONTACT без contactId и утечку Redis credentials в логах | 2026-08-04 | 61c4121 |  | [260804-8wh-max-redis-blpop-timeout-contact-contacti](./quick/260804-8wh-max-redis-blpop-timeout-contact-contacti/) |
| 260807-pq7 | issue 35: при удалении аккаунта мессенджера расписания не удаляются, а переходят в статус приостановлено | 2026-08-07 | 95babd3 |  | [260807-pq7-issue-35](./quick/260807-pq7-issue-35/) |
| 260825-abl | issue 39: разрешить загрузку изображений только в форматах JPEG и PNG — WebP ломал отправку в Telegram | 2026-08-25 | 6b4b94d |  | [260825-abl-issue-39-razreshit-zagruzku-tolko-teh-fo](./quick/260825-abl-issue-39-razreshit-zagruzku-tolko-teh-fo/) |
| 260825-d2x | issue 40: сжимать изображения при загрузке и отдавать превью в интерфейс | 2026-08-25 | a33a62f |  | [260825-d2x-issue-40-szhimat-izobrazheniya-pri-zagru](./quick/260825-d2x-issue-40-szhimat-izobrazheniya-pri-zagru/) |
| 260825-hnf | скрипт обслуживания: сжать и создать превью для вложений, загруженных до issue #40 | 2026-08-25 | 8838179 | Verified | [260825-hnf-skript-obsluzhivaniya-szhat-i-sozdat-pre](./quick/260825-hnf-skript-obsluzhivaniya-szhat-i-sozdat-pre/) |
| 260825-m0b | страница ads не соответствует шаблону new_broadcaster_design.html | 2026-08-25 | ba7e1d8 |  | [260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-](./quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/) |
| 260825-of5 | страница accounts не соответствует шаблону new_broadcaster_design.html | 2026-08-25 | 13c6dd2 |  | [260825-of5-stranitsa-accounts-ne-sootvetstvuet-shab](./quick/260825-of5-stranitsa-accounts-ne-sootvetstvuet-shab/) |
| 260826-6jq | убрать со страницы dashboard блок «Воркеры аккаунтов» | 2026-08-26 | 884a06d |  | [260826-6jq-ubrat-so-stranitsy-dashboard-blok-otvech](./quick/260826-6jq-ubrat-so-stranitsy-dashboard-blok-otvech/) |
| 260826-9vv | убрать со страницы dashboard блок «Активность за неделю» | 2026-08-26 | a8cfebe |  | [260826-9vv-ubrat-so-stranitsy-dashboard-blok-aktivn](./quick/260826-9vv-ubrat-so-stranitsy-dashboard-blok-aktivn/) |
| 260826-dc0 | плитки дашборда считают календарные сутки в зоне пользователя вместо скользящих 24 часов | 2026-08-26 | a4654b9 |  | [260826-dc0-plitki-dashborda-schitayut-kalendarnye-s](./quick/260826-dc0-plitki-dashborda-schitayut-kalendarnye-s/) |
| 260826-jql | Telegram: разрешать peer перед отправкой картинок (PeerIdInvalidError на UploadMediaRequest) | 2026-08-26 | bf2a6b5 | Needs Review | [260826-jql-telegram-razreshat-peer-pered-otpravkoy-](./quick/260826-jql-telegram-razreshat-peer-pered-otpravkoy-/) |
| 260826-m5w | issue 44: на /ads/{id}/edit сжимаются и обрезаются иконки мессенджеров в шапке карточки расписания | 2026-08-26 | 41769e6 |  | [260826-m5w-posmotri-issue-44-i-isprav-problemmy-ver](./quick/260826-m5w-posmotri-issue-44-i-isprav-problemmy-ver/) |
| 260826-mwo | на /ads/{id}/edit тумблер включения/выключения расписания сворачивал/разворачивал карточки — редирект переписывал `?sched=` | 2026-08-26 | 2111a9f |  | [260826-mwo-na-stranitse-redaktirovaniya-obyavleniya](./quick/260826-mwo-na-stranitse-redaktirovaniya-obyavleniya/) |
| 260826-ojg | issue 45: на /ads/{id}/edit подблоки развёрнутой карточки расписания слипались — обёртка-форма гасила промежуток тела карточки | 2026-08-26 | 12f5302 |  | [260826-ojg-posmotri-issue-45-i-isprav-problemmy-ver](./quick/260826-ojg-posmotri-issue-45-i-isprav-problemmy-ver/) |

## Deferred Items

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| v2.1 | Email-уведомления NOTIF-01..04 | Отложено, вне roadmap v2.0 | 2026-08-08 |
| v2.1 | Прогрев аккаунтов и автопауза WARM-01..02 | Отложено, вне roadmap v2.0 | 2026-08-08 |
| Future scope | Reliability and hardening suggestions в research/SUMMARY.md (v1-era) | Не активно; требует отдельного решения по milestone | 2026-08-03 |
| Phase 2 | CR-01 — владение `ad_id`/`account_id` при постановке в расписание | ✓ Закрыто планом 02-02 (2026-08-11) | 2026-08-10 |
| Phase 2 | CR-02 — клиентский Content-Type при загрузке, SVG на origin хранилища | ✓ Закрыто планом 02-02 (2026-08-11) | 2026-08-10 |
| Phase 2 | WR-01 / T-10-04 — владение ключом изображения при сохранении объявления | ✓ Закрыто планом 02-02 (2026-08-11) | 2026-08-10 |
| Phase 5 | `billing/plans.html` не подключён ни к одному маршруту | Решение о маршруте или переносе содержимого — за Фазой 5 | 2026-08-09 |
| Phase 3 | R-03-09 / T-03-17 — текст стороннего httpx-исключения в плашке ошибки синхронизации | Принято владельцем как риск severity medium (2026-08-13); сужение текста — при желании отдельной задачей | 2026-08-13 |
| Phase 5 | CR-01 ревью раунда 1 — гард источника вебхука читал адрес пира при пустом заголовке | ✓ Закрыто планом 05-07 (2026-08-16): безопасное умолчание `X-Real-IP` стало свойством `app/config.py`, значение закреплено записью в `docker-compose.prod.yml`, ветка адреса пира удалена целиком; доказано регрессией через настоящий `ProxyHeadersMiddleware` (`tests/test_routes/test_billing_webhook_proxy_headers.py`, 10 тестов). Организационной мерой запись быть перестала; человеческим остаётся РОВНО ОДИН пункт — боевой nginx ставит настроенный заголовок именно на маршруте вебхука | 2026-08-16 |
| Phase 5 | CR-02 ревью раунда 1 — двойное зачисление, check-then-act без блокировки | ✓ Закрыто планом 05-08 (2026-08-16): `_claim_payment` — настоящий compare-and-swap с `rowcount == 1`, `with_for_update()` на выборке, приращение баланса на стороне СУБД, вторая активная строка подписки закрыта частичным уникальным индексом ревизии `0018`; доказано `test_payment_concurrency.py` и `test_0018_subscriptions_unique_user.py` (22 теста) | 2026-08-16 |
| Phase 5 | CR-01 ревью раунда 2 — молчаливое понижение тарифа подтверждённым платежом в `_apply_extension` | ⚠️ Закрыто планом 05-11 (2026-08-16) НАПОЛОВИНУ, завершено планом 05-13 (2026-08-17). Названное направление закрыто и подтверждено раундом 3 — шесть из семи must_haves плана 05-11 зелёные: правило `switch_is_refused` объявлено один раз (`app/application/billing/plan_switch.py`) и читается обеими стадиями, сохранение старшего тарифа пишет `subscription_plan_preserved` уровня `warning`, регрессия стадии применения — `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage`. Но объединено было ОБЪЯВЛЕНИЕ правила, а не его ВХОДНЫЕ ДАННЫЕ, и седьмой must_have (совпадение докстринга `_extend_subscription` с исполняемым кодом) раунд 3 признал фальсифицированным: стадии разошлись по члену о сроке, чем и была внесена регрессия — оплаченное понижение на ИСТЁКШЕМ сроке не выдавалось. Соседнее направление закрыто планом 05-13 отдельной строкой ниже. ⚠️ Вердикт `05-VERIFICATION.md` — `gaps_found` — действует до повторной верификации: запись фиксирует исполненную работу, а не пройденную проверку | 2026-08-16 |
| Phase 5 | CR-01 ревью раунда 3 — оплаченное понижение на ИСТЁКШЕМ сроке списывалось, но не применялось; понижение становилось недостижимым навсегда | ⚠️ Закрыто планом 05-13 (2026-08-17) в НАЗВАННОМ направлении — и раунд 4 это подтвердил независимым прогоном, а не по SUMMARY: все четыре обещанных механизма на месте. Признак живости объявлен ОДИН раз — `subscription_is_live` в `app/application/billing/subscription_period.py:53` (не рядом с правилом, как предлагало ревью: `plan_switch.py` держит AST-проверяемый инвариант единственного импорта, а признаку нужен `normalize_utc`) — и стал ОБЯЗАТЕЛЬНЫМ keyword-only входом `period_is_live` правила `switch_is_refused` БЕЗ значения по умолчанию, поэтому спросить правило, не подав признак, невозможно (`TypeError`, закреплено `test_the_rule_cannot_be_asked_without_the_liveness_term`). Признак снимается ДО сдвига срока; порядок закреплён AST-тестом `test_the_liveness_is_sampled_before_the_date_moves` (гейт `live=0 < move=1`). Регрессия стадии применения на истёкшем сроке существует и ПАДАЛА до правки: `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage` давал `assert 'pro' == 'basic'`; вместе с ней `test_the_expired_period_writes_no_preserved_plan_warning`. Оба операнда признака проходят через `normalize_utc` (отклонение Rule 2 плана 05-13), закреплено `test_a_naive_now_does_not_break_the_liveness_term`. Суита 1665 → 1681; прогон трёх относящихся файлов раундом 4 — 92 passed. **Но полным это закрытие не является:** раунд 4 нашёл дефект ТОГО ЖЕ корня, оставшийся после него — объединена была ФОРМА входных данных правила, а не МОМЕНТ их снятия, и решение ПРОДАТЬ на самом платеже не записывалось вовсе, отчего подтверждённый платёж младшего тарифа покупал месяц действующего старшего. Это `CR-01 (раунд 4)`, отдельной строкой ниже, закрытый планами 05-15 и 05-17. Соседство повторяет случай `CR-01 (раунд 2)`: закрытие верно в НАЗВАННОМ направлении и не полно — та же правка записи, которую план 05-14 сделал строкой выше. ⚠️ Вердикт `05-VERIFICATION.md` раунда 4 — `gaps_found` — действует до повторной верификации: запись фиксирует исполненную работу, а не пройденную проверку | 2026-08-17 |
| Phase 5 | CR-01 ревью раунда 4 — подтверждённый платёж МЛАДШЕГО тарифа двигал срок ДЕЙСТВУЮЩЕГО СТАРШЕГО на полный календарный месяц; 9370 ₽ покупали 121 день Pro стоимостью 19 600 ₽, число повторов не ограничивало ничто | ⚠️ Закрыто ДВУМЯ планами — ни один из них не закрывает находку в одиночку. **Планом 05-15 (2026-08-17):** ответ гарда о разрешённом переходе записывается на сам платёж колонкой `payments.switch_authorized` (решение владельца D-28), заведённой ревизией Alembic `0019` — `Boolean`, `nullable=True`, БЕЗ `server_default`, три значения вместо двух (`True` — спросили и разрешили, `False` — спросили и отвергли, `NULL` — не спрашивали); стадия применения читает записанный ФАКТ и спрашивает правило только при `NULL`, а не пересчитывает предикат по строке подписки, изменившейся за время сессии оплаты. Платёж, чей план не применён, покупает дни по УПЛАЧЕННОМУ тарифу — долю календарного месяца по отношению суммы к цене действующего плана, не меньше одного дня (`prorated_expiry`/`prorated_days` в `Decimal`, решение владельца D-29), а не месяц старшего. Регрессии, МЕНЯЮЩИЕ состояние подписки МЕЖДУ продажей и подтверждением, существуют и были КРАСНЫМИ до правки — оба названных отчётом случая: `test_a_deal_sold_on_an_expired_period_is_delivered_after_the_period_revives` (`plan == 'pro'` там, где ожидался `'basic'`) и `test_a_pro_deal_sold_before_any_subscription_is_not_erased_by_a_later_basic`; ⚠️ у второй красным было утверждение о СРОКЕ (61 день при пороге 45), а не о плане — прогона, в котором старый код давал `basic`, не существует, и приписывать его не станем. Суита 1681 → 1711. **Планом 05-17 (2026-08-17):** закрыт последний путь случая (1), где ОБА платежа заведены формой и оба несут разрешение `True` — там отказа не возникает ни у одного, доля месяца не исполняется, и последний подтверждённый младший переписывал уже выданный старший. Форма потолка — `cap-different-plan` (ответ чекпойнта задачи 1 плана 05-15): отвергается второе незакрытое подписочное намерение с ДРУГИМ планом, повтор ТОГО ЖЕ тарифа проходит; срок давности `PENDING_INTENT_TTL_HOURS = 24`; отказ несёт СВОЙ тип `PendingIntentCapError` и свой код причины `pending`, поднимается ПЕРВЫМ оператором `create_payment` — до единого обращения к ЮKassa (`YooPayment.create.call_count == 0`), и форма отвечает словами вместо молчаливого редиректа. Именованные регрессии: `test_a_second_subscription_intent_is_refused_before_the_money_moves`, `test_the_refusal_never_reaches_yookassa`, `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form`, `test_a_second_intent_for_the_same_plan_is_accepted`. Суита 1711 → 1722. ⚠️ Оговорки, которые запись НЕ сглаживает: потолок НЕ даёт атомарности — между проверкой и созданием платежа лежит сетевой вызов к ЮKassa, и это названо в докстринге `create_payment` ограничением, а не гарантией. ⚠️ Прежняя редакция принимала остаточный риск ссылкой на решение D-29 («платёж, чей план не применён, покупает долю месяца по уплаченной сумме, поэтому окно остаётся нейтральным») — то есть опиралась на ветку, которая на этом пути НЕ ИСПОЛНЯЕТСЯ НИ РАЗУ. Настоящий остаточный исход назван по значениям, а не ссылкой: у платежа, заведённого ФОРМОЙ, записанное разрешение ВСЕГДА положительно, потому что при отказе гард возвращает 302 ДО создания платежа (`app/pages/billing.py:348-349`), а сюда приезжает отрицание уже вычисленного ответа (`:367`), то есть `True`; значит на стадии применения `refused = not True = False` у ОБЕИХ строк, доля месяца не исполняется ни для одной, и исход решает **ПОРЯДОК подтверждения** — выдан тариф последнего подтверждённого платежа. D-29 остаётся живым только для строк со `switch_authorized IS NULL`, заведённых до ревизии `0019`, и после её выката это множество перестаёт пополняться навсегда. Формулировка совпадает с докстрингом `create_payment` (`app/services/payment_service.py:243-258`), переписанным планом `05-19`: два места одного утверждения обязаны говорить одно. Что дало бы нейтральность окна СВОЙСТВОМ, а не формулировкой, названо поимённо и названо НЕСДЕЛАННЫМ: частичный уникальный индекс «не более одного незакрытого подписочного намерения пользователя» по образцу `uq_subscriptions_active_user` ревизии `0018` — он НЕ ПОСТРОЕН и является работой своего размера (своя ревизия в невыкаченной очереди D-26, решение об уже существующих строках, round-trip-тест, обработка отказа ограничения), а не обещанием; ревизия `0019` встаёт СЕДЬМОЙ в невыкаченную очередь (D-26), на боевой базе колонки нет и не будет, пока очередь не пройдена; срок давности существует из-за D-27, а не для удобства. ⚠️ Вердикт `05-VERIFICATION.md` раунда 4 — `gaps_found` — действует до повторной верификации: запись фиксирует исполненную работу, а не пройденную проверку | 2026-08-17 |
| Phase 5 | **Тир прохибиций класса «объявление против кода» — решение D-33** | ✓ Решено планом `05-26` (2026-08-18). Граница проведена ПО ПРЕДМЕТУ, а не по удобству. **Кодовая половина остаётся `verification: test` и получила НАСТОЯЩЕЕ принуждение:** `tests/test_application/test_declared_invariants.py` (план `05-25`) требует, чтобы каждый помеченный абзац денежного пути называл закрепляющий тест по имени; множество модулей выводится из состава пакета, зубы доказаны двумя негативными и одним позитивным контролем, долг записан храповиком `declared_invariants_without_witness.txt` на 37 записей (14 из них помечены машинно-проверяемыми и стоят первыми на снятие). **Документная половина переводится в judgment-tier:** прохибиции о `.planning/STATE.md`, объявленные планами `05-16`, `05-19`, `05-20` и `05-21`, объявлялись `verification: test` четыре плана подряд при НУЛЕ тестов (проверено раундом 6 грепом по `tests/`), и пятое необеспеченное объявление было бы повторением ошибки, а не её исправлением. Причина перевода названа прямо и не как удобство: свойство «константа величины, которую документ не может держать истинной» механически неотделимо от законных чисел того же документа — дат, цен, номеров ревизий, номеров строк, чисел, квалифицированных раундом, — и гейт на него давал бы либо ложные срабатывания, либо проверку, зелёную всегда. ⚠️ **Перевод тира НЕ есть снятие прохибиции:** она остаётся `unresolved` и продолжает судиться человеком; меняется только честность обещания о СПОСОБЕ проверки. Форсирующий признак возврата: третий раунд подряд с находкой класса «константа величины в операционном документе» | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 1 из 9** — второе окно потолка одновременных намерений (СРОК ДАВНОСТИ, `PENDING_INTENT_TTL_HOURS`) | Открыто и названо открытым в трёх местах; опровергается собственным зелёным тестом проекта `test_a_stale_intent_does_not_block_a_new_one`. ⚠️ **Раунд 6 вскрыл ВТОРОЕ следствие этого окна:** через него достижимо изменение действующего тарифа ВНИЗ при живом старшем сроке. План `05-24` закрыл второе следствие ВИДИМОСТЬЮ, а не невозможностью (ответ владельца `record-wins`, решение D-32): понижение пишет `subscription_plan_downgraded` уровня `warning`, и разбирающий обращение может назвать человеку исход. Чем НЕ закрыто: само окно остаётся ОТКРЫТЫМ, понижение остаётся ДОСТИЖИМЫМ, и карточка младшего плана продолжает обещать «переход после окончания оплаченного срока», тогда как через окно он происходит раньше — интерфейсного ответа на это расхождение планы фазы не заводили. Закрытие требует либо подтверждённой подписки на `payment.canceled` (D-27), либо снятия намерения С ОПЛАТЫ при истечении срока давности; владелец не назначен | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 2 из 9** — частичный уникальный индекс «не более одного незакрытого подписочного намерения» | Не построен и назван несделанным. Потолок одновременных намерений держится проверкой в коде, а не свойством схемы; механизм, который дал бы нейтральность окна гонки СВОЙСТВОМ, назван (`WR-01 (раунд 5)`) и назван непостроенным. Распоряжение — строить вместе со следующей ревизией, касающейся `payments`, чтобы не заводить отдельную ревизию ради одного индекса | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 3 из 9** — `uq_subscriptions_active_user` ревизии `0018` не выкачена | Ревизия существует в репозитории и не применена к боевой базе; входит в ту же невыкаченную очередь, что и `0013`…`0019` (D-26). Следствие: гарантия «одна активная подписка на пользователя» в проде держится только кодом. Распоряжение — проходит вместе с очередью, отдельного решения не требует | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 4 из 9** — решение D-26: очередь ревизий не выкачена на боевую базу | Деплой-гейт владельца. Радиус поражения назван ФАКТИЧЕСКИЙ, а не заниженный (план `05-20`, докстринг `alembic/versions/0019_payment_switch_authorized.py`): отказывает КАЖДОЕ чтение таблицы платежей, раздел «Тарифы» отдаёт 500 любому вошедшему, обработчик уведомления падает ДО ветвления по предмету покупки и ломает приём ПАКЕТНЫХ платежей, работавших до этой фазы. Выкат кода и выкат очереди миграций разделить нельзя. Момент выката выбирает владелец | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 5 из 9** — применения тарифных лимитов в системе нет (D-08, BILL-02) | Фаза только ПОКАЗЫВАЕТ потребление и остаток; гейтов на создание не вводит — это записанное решение, а не пропуск. BILL-02 перепомечен честно (D-13), чтобы долг не растворился молча. Распоряжение — введение гейтов есть своя работа со своим ответом на вопрос «что делать с теми, кто уже за лимитом»; фазе не вменяется | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 6 из 9** — подписка на событие `payment.canceled` в кабинете ЮKassa не подтверждена (D-27) | Ветка отмены зелёная в тестах и непроверенная в проде: пока подписка не включена, отменённые платежи остаются `pending` навсегда, и критерий 3 на проде показывает неправду. Резервного пути у действия нет — состав рассылаемых событий кодом не проверяется (T-05-33). Требуется действие владельца в кабинете | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 7 из 9** — у планов `05-15` и `05-17` нет RED-коммитов | Раскрыто их собственными исполнителями и записано раундом 5; `tdd_gate` раунда 6 — `advisory_disclosed`, гейт неблокирующий, MVP mode выключен. Все планы прогона раунда 5 (`05-18`…`05-21`) и все планы волн 18-20, вводившие код, держали RED→GREEN. Распоряжение — задним числом не восстанавливается; факт остаётся записанным | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 8 из 9** — мобильная проверка на ширине 375px не проведена | Человеческая проверка, перенесённая через раунды; ни один план волны 21 разметки не трогает, поэтому предмет проверки этой волной не изменился. Распоряжение — проводится человеком перед сдачей фазы | 2026-08-18 |
| Phase 5 | **Записанный долг фазы, пункт 9 из 9** — нумерация угроз фазы НЕ УНИКАЛЬНА между планами | Вскрыт не раундом 6, а сверкой при перепланировании волны 20; кода не требует и исполненных планов не касается. Факт установлен командой `grep -ohE '^\| T-05-[0-9]+ \|' .planning/phases/05-tarify/05-*-PLAN.md \| tr -d '\| ' \| sort \| uniq -d`, её вывод — СЕМЬ идентификаторов: `T-05-30`, `T-05-31`, `T-05-32`, `T-05-33`, `T-05-34`, `T-05-35`, `T-05-51`. **Случаи ДВУХ РАЗНЫХ РОДОВ, и одна строка «семь дублей» слила бы их в неверное утверждение. (а) ШЕСТЬ настоящих столкновений** — один номер несёт РАЗНЫЕ угрозы в разных планах: `T-05-30` в `05-05` — подделка адреса пира в гарде уведомления (Spoofing, critical), а в `05-07` — молча обрезанный потолком список платежей (Repudiation, medium); тем же порядком разошлись `T-05-31` (`05-05` против `05-07`), `T-05-32`, `T-05-33` и `T-05-34` (`05-06` против `05-07`) и `T-05-35` (`05-06` против `05-08`). **(б) ОДИН случай законной преемственности** — `T-05-51` есть ОДНА И ТА ЖЕ угроза (повышение отдаёт лимиты старшего тарифа на оставшиеся дни младшего), перенесённая планами `05-11`, `05-13` и `05-15` из плана `05-10` дословно и с прямым указанием переноса в самой строке. Команда поиска дублей отличить перенос от столкновения не умеет, поэтому это названо словами — иначе следующий читатель примет исправную запись за дефект. **Распоряжение по обоим родам одно: ИСПОЛНЕННЫЕ ПЛАНЫ НЕ ПРАВЯТСЯ.** Переписывание реестра угроз исполненного плана задним числом есть правка истории — ровно то, что фазе запрещено и за что она уже получала раунд. Следствие долга: номер перестаёт адресовать угрозу однозначно в разговоре и в отчёте. Чего следствием НЕ является: ни одна мера защиты не отменяется и не теряется — у каждой строки своя мера на месте. Форсирующий признак возврата: следующая фаза заводит номера угроз сквозным счётчиком либо квалифицирует их номером плана | 2026-08-18 |
| Quick 260825-abl | Уже сохранённые вложения WebP/GIF — объявления, созданные ДО сужения, продолжат падать при отправке в Telegram | Открыто. Сужение действует на входе и только вперёд: ни один объект в хранилище не переписан, ни один ключ не мигрирован, пути отправки не тронуты. Затронутые объявления ищутся по `Ad.images` (`app/models/ad.py:19`) — по Content-Type СОХРАНЁННОГО объекта вне `{image/jpeg, image/png}`, а не по расширению имени, которое подконтрольно клиенту. Закрытие требует своего решения (перекодирование — от него `uploads.py` осознанно отказывается ради вектора decompression bomb — либо выборочная чистка ссылок с уведомлением владельца), поскольку меняет пользовательские данные | 2026-08-25 |

### Подтверждено при закрытии вехи v2.0 (2026-08-25)

Пункты, подтверждённые командой `audit-open acknowledge` и отложенные осознанно.
Подтверждение НЕ закрывает находку: оно подавляет её в предзакрывающем аудите и
самоаннулируется, как только состояние артефакта снова изменится.

| Category | Item | Status | Deferred At | Milestone |
|----------|------|--------|-------------|-----------|
| debug_sessions | knowledge-base | unknown | 2026-08-25 | v2.0 |
| debug_sessions | max-worker-import-after-update | awaiting_human_verify | 2026-08-25 | v2.0 |
| debug_sessions | telegram-schedule-not-due | investigating | 2026-08-25 | v2.0 |
| todos | blocked-user-can-still-log-in.md | (presence-only) | 2026-08-25 | v2.0 |
| todos | full-suite-ads-editor-order-pollution.md | (presence-only) | 2026-08-25 | v2.0 |
| todos | password-reset-codes-are-predictable.md | (presence-only) | 2026-08-25 | v2.0 |
| todos | plan-price-credibility-bound.md | (presence-only) | 2026-08-25 | v2.0 |
| todos | session-cookie-without-secure-flag.md | (presence-only) | 2026-08-25 | v2.0 |
| deferred_items | 01/deferred-items.md: План 06 — HTML-фрагменты статуса подключения с utility-классами удалённого Tailwind живут в Python (в тексте помечено ЗАКРЫТО планом 08) | acknowledged | 2026-08-25 | v2.0 |
| deferred_items | 01/deferred-items.md: План 08 — глобал шаблонов `s3_public_url` собирает `Settings()` в обход подмены зависимостей; `/ads/new` не рендерится без `.env` | acknowledged | 2026-08-25 | v2.0 |
| deferred_items | 01/deferred-items.md: План 07 — `app/templates/billing/plans.html` не подключён ни к одному маршруту | acknowledged | 2026-08-25 | v2.0 |
| deferred_items | 01/deferred-items.md: UAT тест 6 — CR-01/CR-02 вынесены в Фазу 2 (в основной таблице выше помечены закрытыми планом 02-02) | acknowledged | 2026-08-25 | v2.0 |
| deferred_items | 02/deferred-items.md: D-02-09-01 — `POST /api/groups` не проверяет владение аккаунтом | acknowledged | 2026-08-25 | v2.0 |
| deferred_items | 03/deferred-items.md: `GroupRepository` без потребителей — решение принято и исполнено планом 03-11 (в тексте помечено ЗАКРЫТО) | acknowledged | 2026-08-25 | v2.0 |
| deferred_items | 05.1/deferred-items.md: DEF-01 — `tests/test_planning` красен на счёте ПЛАНОВ, окно трекинга волны | acknowledged | 2026-08-25 | v2.0 |
| deferred_items | 05.1/deferred-items.md: расхождение дерева требований и таблицы прослеживаемости по GRP-04, GRP-05, GRP-06 | ✓ resolved — сверка проведена в этом же закрытии, см. footer `REQUIREMENTS.md` | 2026-08-25 | v2.0 |

⚠️ **Исходный аудит показал 19 пунктов; подтверждено 15, закрыт 1.** Разница — не пропуск.
Три «пункта» (`GRP-04 — Phase 3 — Pending`, `GRP-05 …`, `GRP-06 …`) были артефактом
сканера: `audit-open` читает ЛЮБУЮ строку, начинающуюся с `|`, как строку таблицы
отложенных пунктов, включая строки внутри ограждённого блока. Три такие строки —
процитированный фрагмент таблицы прослеживаемости ВНУТРИ записи 05.1 о расхождении
GRP-04…GRP-06, то есть тройной повторный счёт одной и той же записи. Ведущий `|` в
цитате снят, содержимое цитаты не изменено; сама запись НЕ подтверждена, а ЗАКРЫТА —
сверка прослеживаемости Фазы 3, которую она требовала, проведена в этом же закрытии
(GRP-04…GRP-06: `Pending` → `Complete` по проверенному коду).

⚠️ **Восемь записей `deferred_items` подтверждены ПРАВКОЙ ФАЙЛОВ, а не CLI-писателем.**
`audit-open acknowledge` отказал всем одиннадцати вызовам с `unsupported_heading_shape`:
файлы `deferred-items.md` этого проекта используют heading-delimited форму (#3457),
которую писатель не поддерживает. В каждую запись вписано поле `- **Status:** Phase 10 shipped — PR #48
— тот же маркер, который писатель поставил бы сам.

## Session Continuity

Last session: 2026-09-19T10:51:11.389Z
Stopped at: Completed 12-10-PLAN.md
Resume file: None

**Поправка к handoff, установленная проверкой на входе 2026-09-02:** субагент `a217c9b7b59eb5230` НЕ жив — он умер вместе с прошлой сессией. Его worktree цел: 2 коммита (`2f9875f` = RED_SHA, `c237d00` = сводка-останов) и НЕЗАКОММИЧЕННАЯ правка `modal.html` (+68) по ветви `destroy-guard`. `app/static/css/app.css` пункта 2 задачи 3 НЕ тронут. Устаревший `.planning/milestone.lock` (pid 941133 мёртв) снят.

**Отклонение диспатча, принятое владельцем 2026-09-02:** задача 3 доисполняется субагентом БЕЗ `isolation="worktree"`, работающим в СУЩЕСТВУЮЩЕМ worktree `agent-a217c9b7b59eb5230`. Основание: гейт `executor-isolation-dispatch` требует `isolation="worktree"` для `gsd-executor`, но свежий worktree форкается от `bb9113e`, где нет ни `2f9875f`, ни заведённого им тест-файла (+396) — гринить было бы нечего. Обе записанные развилки `.continue-here.md` при этом соблюдены: основное дерево не трогается, SUMMARY со `status: halted` в ветку фазы не сливается.

## Operator Next Steps

1. **Спланировать Фазу 9: `/gsd-discuss-phase 9`** — «Пилот на `account_groups`: сквозной контракт формы». Разведка ей не нужна (паттерны сняты с действующего кода). Фаза 8 построила ВСЁ, на что она опирается: `respond()`, реестр `?notice=`, две области уведомления, 13 гейтов.
2. **Денежный узел закрыт — снять его из «держать в виду».** PAY-01/PAY-02 исполнены Фазой 8: потолок стал частичным уникальным индексом, удержание пути рассылки подкреплено машинным фактом одиночности процесса. Индекс и удержание стоят ДО перевода форм `billing` в Фазе 11, как и требовалось.
3. **⚠️ Выкат `0021` имеет ОБЯЗАТЕЛЬНЫЙ ПОРЯДОК** (R-08-06): миграцию нельзя накатывать раньше кода с `AWAITING_STATUSES`, иначе админский «Обзор» захлебнётся ложными `payment_stuck`. Штатная выкладка одним развёртыванием условие соблюдает.
4. **Смёржить PR #43** (`gsd/phase-05-tarify` → `master`) — коммиты закрытия вехи v2.0 лежат поверх него и в `master` ещё не заведены.
5. **Поставить тег `v2.0`** ПОСЛЕ слияния, на коммит в `master`: `git tag -a v2.0 -m "..."`. Тег НЕ создан намеренно — при squash-мерже SHA ветки в `master` не попадает. Тегов в репозитории пока нет вовсе.
6. **Долг v2.0 в веху НЕ входит** — выкат очереди ревизий `0013`…`0021` (D-26), три BLOCKER-долга безопасности, подписка на `payment.canceled` (D-27), три debug-сессии, адаптив админки на 375px. Перечень с основаниями — §Deferred Items выше и `.planning/ROADMAP.md`.
7. **Четыре пункта отложены владельцем на UAT Фазы 8** — плашки аварии не прячутся обратно, CR-01, четвёртый реестр `QUEUE_DROP_RESULTS`, гигиена реестра угроз. Перечень — PROJECT.md §Active и §Blockers/Concerns выше.
