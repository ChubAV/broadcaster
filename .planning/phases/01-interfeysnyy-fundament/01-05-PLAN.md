---
phase: 01-interfeysnyy-fundament
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - app/templates/history/list.html
  - app/templates/history/includes/history_card.html
  - app/templates/history/partial_cards.html
  - app/templates/history/detail.html
  - app/templates/dashboard.html
  - app/templates/dashboard/includes/recent_send_card.html
  - app/templates/profile.html
  - app/static/css/app.css
  - tests/test_pages/test_responsive_markup.py
autonomous: true
requirements: [UI-04, UI-05, UI-06]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Записи истории отрисовываются примитивом data-hrow и перестраиваются в одну колонку на ширине 1080px"
    - "Блок метаданных записи истории на узкой ширине теряет левую границу и получает верхнюю — селектор адаптива переведён с инлайн-стиля на атрибут"
    - "Бесконечная прокрутка истории подгружает вторую страницу и сохраняет фильтры канала, статуса и периода"
    - "Страница детали отправки показывает текст ошибки неуспешной отправки"
    - "Дашборд и профиль отрисовываются в новом шелле из компонентов"
    - "Форма профиля сохраняет все атрибуты name, маршрут и выбор таймзоны"
    - "Все 393 существующих теста остаются зелёными"
    - statement: "Тело страницы профиля собрано из компонентов — сквозной срез Плана 01 доведён до конца страницы, а не только до шелла"
      verification: backstop
  artifacts:
    - path: "app/templates/history/includes/history_card.html"
      provides: "Запись истории на примитиве data-hrow с блоком метаданных, размеченным атрибутом data-area"
      contains: "data-hrow"
    - path: "app/templates/history/list.html"
      provides: "Список истории с фильтрами через макрос filters"
      contains: "components/filters.html"
    - path: "app/templates/dashboard.html"
      provides: "Дашборд в новом шелле — каркас без наполнения Фазы 4"
      contains: "block page_title"
    - path: "app/templates/profile.html"
      provides: "Профиль на макросах полей — завершение сквозного среза Плана 01"
  key_links:
    - from: "app/templates/history/includes/history_card.html"
      to: "app/static/css/app.css"
      via: "медиазапрос 1080px перестраивает data-hrow и его дочерний блок data-area=meta"
      pattern: "data-area"
    - from: "app/templates/history/list.html"
      to: "app/templates/history/partial_cards.html"
      via: "идентичный сентинел с протаскиванием фильтров"
      pattern: "filter_params"
  prohibitions:
    - statement: "MUST NOT прятать текст ошибки неуспешной отправки за сокращением без возможности прочитать его целиком — сообщение об ошибке это единственное, по чему пользователь понимает, почему его реклама не ушла"
      category: transparency
---

<objective>
Перевести на дизайн-систему разделы «История», «Дашборд» и «Профиль».

Purpose: история — единственный раздел с собственным адаптивным примитивом (`data-hrow`,
перестраивается раньше остальных, на 1080px) и единственное место, где дословный перенос CSS макета
делать нельзя. Профиль здесь закрывает сквозной срез Плана 01: шелл на этой странице уже доказан,
теперь достраивается её тело.

Output: 7 мигрированных шаблонов, исправленный селектор адаптива истории, дополненный
`test_responsive_markup.py`.

**Дашборд — только каркас.** Метрики за сутки, ближайшие отправки, живая лента, heatmap и индикатор
воркеров на самом дашборде относятся к Фазе 4 (DASH-01…DASH-05). Здесь дашборд переверстывается в том
объёме данных, который у него есть сегодня. Спарклайны и heatmap-ячейки как компоненты **не делаются** —
они принадлежат Фазе 4.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-interfeysnyy-fundament/01-CONTEXT.md
@.planning/phases/01-interfeysnyy-fundament/01-02-SUMMARY.md
@.planning/phases/01-interfeysnyy-fundament/01-03-SUMMARY.md
@.planning/phases/01-interfeysnyy-fundament/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Список истории и примитив data-hrow (UI-05, UI-06)</name>
  <files>app/templates/history/list.html, app/templates/history/includes/history_card.html, app/templates/history/partial_cards.html, app/static/css/app.css, tests/test_pages/test_responsive_markup.py</files>
  <read_first>
    - app/templates/history/list.html — целиком: блок фильтров (строки 10 и 13), список, сентинел с протаскиванием фильтров (строка 48)
    - app/templates/history/includes/history_card.html — целиком, 44 строки: берёт переменную цикла и не объявляет параметров
    - app/templates/history/partial_cards.html — целиком (6 строк)
    - app/static/css/app.css — медиазапрос 1080px, перенесённый Планом 01, и имя `data-area`, зафиксированное там же вместо селектора по инлайн-стилю
    - app/templates/components/filters.html — макрос Плана 04
    - app/templates/components/badge.html, table.html, empty_state.html, mono.html — сигнатуры Плана 02
    - app/pages/history.py — строки 65-115: состав фильтров и контекст партиала
    - design/new_broadcaster_design.unpacked.html — строки 807-876: раздел «История»; строка 834 — исходная разметка `data-hrow` с областями сетки
    - .planning/phases/01-interfeysnyy-fundament/01-RESEARCH.md — §Pattern 3 и Pitfall 7
  </read_first>
  <behavior>
    Дополнить `tests/test_pages/test_responsive_markup.py`:
    - `test_history_uses_hrow_primitive` — выдача `/history` содержит `data-hrow`
    - `test_history_meta_marked_by_attribute` — выдача содержит `data-area="meta"`; тем самым селектор
      адаптива 1080px имеет на чём сработать
    - `test_list_page_no_utility_classes[history]`
    - `test_history_card_renders_data` — выдача содержит значение созданной записи истории
    Регрессия: `test_infinite_scroll_chain[history]` и `test_infinite_scroll_keeps_filters[history]` из
    Плана 03 остаются зелёными.
  </behavior>
  <action>
Переверстать список истории на собственный адаптивный примитив.

**Единственное место фазы, где дословный перенос CSS запрещён.** Медиазапрос 1080px макета содержит
правило, чей селектор совпадает только при наличии у дочернего элемента инлайн-стиля с определённой
подстрокой. При переезде на классы это правило молча перестанет работать, и на узкой ширине блок
метаданных записи истории останется с левой границей и левым отступом вместо верхней границы. План 01
уже заменил селектор на атрибутный с именем `data-area`. Задача здесь — **разметить блок метаданных
этим атрибутом**, иначе исправленное правило по-прежнему не на чем сработать. Проверяется тестом
`test_history_meta_marked_by_attribute`.

Запись истории — `data-hrow` с областями сетки `head`, `body`, `meta`, `err`; на 1080px колонки
схлопываются в одну и порядок областей меняется. Разметка одна, перестраивает её CSS — переключателя
вида на JS не делать: он ломает подмену через HTMX.

`history_card.html` переводится в макрос с явными параметрами: сегодня он берёт переменную из цикла
вызывающего шаблона и параметров не объявляет. При потере контекста запись отрисуется пустой, а
страница вернёт 200 — поэтому тест проверяет наличие конкретного значения, а не факт отрисовки.

Блок фильтров (канал, статус, период) — на макрос `filters` из Плана 04. Сентинел: последний элемент
внутри того же контейнера, идентичен в `list.html` и `partial_cards.html`, неявная цель, параметр
компоновки убран, **цикл проброса фильтров перенесён дословно**.

Статусы отправки — через `badge` с явным вариантом; маппинг остаётся в шаблоне. Технические
идентификаторы и время — через `mono`.

Дату форматировать существующим глобалом, учитывающим таймзону пользователя, а не самостоятельно.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_preserved.py -x -q</automated>
    <automated>just test</automated>
    <human-check>Открыть /history и сузить окно до 1000px: запись перестраивается в одну колонку, блок метаданных получает верхнюю границу вместо левой и не остаётся с лишним левым отступом. Применить фильтр и прокрутить до подгрузки второй страницы — фильтр сохраняется.</human-check>
  </verify>
  <acceptance_criteria>
    - `app/templates/history/includes/history_card.html` содержит `data-hrow` и `data-area="meta"`
    - `app/templates/history/includes/history_card.html` содержит `{% macro `
    - `grep -c 'include "history/includes/history_card.html"' app/templates/history/list.html` == 0
    - `app/templates/history/list.html` содержит `components/filters.html`
    - `app/templates/history/list.html` и `app/templates/history/partial_cards.html` оба содержат `filter_params`, `hx-trigger="revealed"`, `hx-swap="outerHTML"`
    - `grep -c 'layout=cards' app/templates/history/list.html` == 0 и то же для `partial_cards.html`
    - `grep -rc 'bg-white\|text-gray\|rounded-lg\|border-gray\|hidden sm:' app/templates/history/list.html app/templates/history/includes/history_card.html app/templates/history/partial_cards.html | grep -v ':0' | wc -l` == 0
    - `grep -c 'style\*=' app/static/css/app.css` == 0 — селекторов по подстроке инлайн-стиля не осталось
    - Поведение: GET `/history` → 200, тело содержит значение созданной записи, `data-hrow` и `data-area="meta"`
    - Поведение: запрос партиала истории с фильтром и смещением 30 возвращает сентинел с тем же фильтром и смещением строго больше 30
    - `just test` завершается с кодом 0
  </acceptance_criteria>
  <done>История собрана из компонентов на примитиве `data-hrow`, блок метаданных размечен атрибутом, на
  который опирается исправленный медиазапрос, цепочка прокрутки и фильтры целы.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Деталь отправки (UI-04)</name>
  <files>app/templates/history/detail.html</files>
  <read_first>
    - app/templates/history/detail.html — целиком, 74 строки: карточки со снимком отправки, текстом ошибки и метаданными задачи
    - app/pages/history.py — обработчик детали: какие поля приходят в контекст
    - app/models/send_log.py — поля записи отправки: идентификатор задачи, текст ошибки, снимки
    - app/templates/components/card.html, badge.html, mono.html, button.html — сигнатуры Плана 02
    - design/new_broadcaster_design.unpacked.html — строки 807-876: оформление ошибок в разделе истории
  </read_first>
  <behavior>
    Регрессия — существующие тесты страницы детали остаются зелёными. Добавить в
    `tests/test_pages/test_responsive_markup.py`:
    - `test_history_detail_shows_error_text` — для записи с текстом ошибки выдача страницы детали
      содержит этот текст целиком, а не усечённо
  </behavior>
  <action>
Переверстать страницу детали отправки на компоненты: панели через `card_open` / `card_close`, статус
через `badge`, идентификатор задачи и временные метки через `mono`, кнопку возврата через `link_button`.
Заголовок раздела перенести в блоки шапки шелла.

**Текст ошибки выводить полностью и в читаемом виде.** Это единственное, по чему пользователь понимает,
почему его реклама не ушла. Оформлять моноширинным блоком с переносом длинных строк; не обрезать
многоточием и не прятать за раскрытием без возможности прочитать целиком.

Копирование ошибки одним действием и повтор отправки из записи истории — **это Фаза 4** (HIST-04),
здесь не делать. Правило фазы — «новый вид, старые действия».

Utility-классы вырезаются полностью.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/ -q</automated>
    <automated>just test</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/history/detail.html` содержит импорт из `components/`
    - `app/templates/history/detail.html` содержит `{% block page_title %}`
    - `grep -rc 'bg-white\|text-gray\|rounded-lg\|border-gray' app/templates/history/detail.html | grep -v ':0' | wc -l` == 0
    - `grep -c 'truncate' app/templates/history/detail.html` == 0
    - Поведение: GET страницы детали для записи с ошибкой возвращает 200 и содержит полный текст ошибки
    - Поведение: GET страницы детали для успешной отправки возвращает 200
    - `just test` завершается с кодом 0
  </acceptance_criteria>
  <done>Страница детали отправки собрана из компонентов и показывает полный текст ошибки.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Дашборд и профиль (UI-02, UI-04)</name>
  <files>app/templates/dashboard.html, app/templates/dashboard/includes/recent_send_card.html, app/templates/profile.html, tests/test_pages/test_responsive_markup.py</files>
  <read_first>
    - app/templates/dashboard.html — целиком, 42 строки
    - app/templates/dashboard/includes/recent_send_card.html — целиком, 20 строк: снова include без параметров
    - app/templates/profile.html — целиком, 29 строк: форма настроек пользователя и выбор таймзоны
    - app/pages/dashboard.py и app/pages/profile.py — контекст обеих страниц
    - tests/test_pages/test_profile.py — существующее покрытие профиля, включая утверждения на выбор таймзоны; их нельзя сломать
    - app/templates/components/card.html, field.html, select_field, button.html, avatar.html, progress.html — сигнатуры Плана 02
    - design/new_broadcaster_design.unpacked.html — строки 383-465 (дашборд), 991-1020 (профиль)
  </read_first>
  <behavior>
    Дополнить `tests/test_pages/test_responsive_markup.py`:
    - `test_dashboard_no_utility_classes` и `test_profile_no_utility_classes`
    - `test_profile_form_contract` — выдача `/profile` содержит `method="post"` и прежние атрибуты
      `name` полей настроек
    Регрессия: `tests/test_pages/test_profile.py` остаётся зелёным без правок, кроме утверждений на
    порядок атрибутов элемента выбора таймзоны.
  </behavior>
  <action>
Переверстать дашборд, карточку недавней отправки и профиль.

**Дашборд.** Только каркас в объёме сегодняшних данных: панели через `card_open` / `card_close`,
статусы через `badge`, заголовок в шапку шелла. Метрики за сутки, ближайшие отправки, живая лента,
heatmap и индикатор воркеров на самом дашборде — Фаза 4 (DASH-01…DASH-05), здесь не добавлять.
Спарклайны и heatmap-ячейки как компоненты не создавать. `recent_send_card.html` перевести в макрос с
явными параметрами — он тоже сегодня берёт переменную цикла и параметров не объявляет.

**Профиль.** План 01 доказал на этой странице сквозной путь до шелла; здесь достраивается её тело.
Форма настроек — на макросы `field` и `select_field`, кнопка сохранения — на `button`, аватар
пользователя — на макрос `avatar`.

**Осторожно — здесь самая плотная концентрация хрупких утверждений в тестах.** Существующее покрытие
профиля проверяет присутствие атрибута выбранного значения относительно конкретной подстроки таймзоны и
отсутствие этого атрибута в определённом фрагменте разметки. Такие проверки ломаются от изменения
порядка атрибутов в элементе выбора. Найти их **до** правки шаблона: либо `select_field` сохраняет
порядок атрибутов существующей разметки, либо тест правится синхронно в том же коммите с указанием
причины в SUMMARY. Также покрытие содержит утверждение на видимую подпись «Профиль» — эту подпись
сохранить, она же является пунктом навигации по D-11.

Атрибуты `name`, `method` и `action` формы профиля переносятся дословно — иначе сохранение настроек
перестанет работать молча.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_profile.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py -x -q</automated>
    <automated>just test</automated>
    <human-check>Открыть /dashboard и /profile: обе страницы в новом тёмном шелле, панели и поля выглядят как одна система с остальными разделами. Сменить таймзону в профиле и сохранить — значение сохраняется и подставляется при повторном открытии.</human-check>
  </verify>
  <acceptance_criteria>
    - `app/templates/dashboard.html` и `app/templates/profile.html` содержат `{% block page_title %}`
    - `app/templates/dashboard/includes/recent_send_card.html` содержит `{% macro `
    - `app/templates/profile.html` содержит импорт из `components/field.html`
    - `grep -rc 'bg-white\|text-gray\|rounded-lg\|border-gray\|lg:' app/templates/dashboard.html app/templates/profile.html app/templates/dashboard/includes/recent_send_card.html | grep -v ':0' | wc -l` == 0
    - Набор атрибутов `name` в `profile.html` совпадает с набором до правки
    - Поведение: GET `/profile` → 200, тело содержит подпись «Профиль» и сохранённую таймзону как выбранное значение
    - Поведение: POST сохранения профиля меняет настройки пользователя
    - Поведение: GET `/dashboard` → 200
    - `uv run pytest tests/test_pages/test_profile.py -x -q` завершается с кодом 0
    - `just test` завершается с кодом 0
  </acceptance_criteria>
  <done>Дашборд и профиль собраны из компонентов, форма профиля сохранила контракт полей, сквозной срез
  Плана 01 доведён до конца страницы профиля.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (План 05)

**Новые макросы**
- `app/templates/history/includes/history_card.html::history_card(...)` — бывший include, переведён в макрос с явными параметрами
- `app/templates/dashboard/includes/recent_send_card.html::recent_send_card(...)` — то же

**Новая разметочная конвенция**
- Атрибут `data-area="meta"` на блоке метаданных записи истории — точка опоры исправленного медиазапроса 1080px, заменившая селектор по подстроке инлайн-стиля

**Новые тесты (`tests/test_pages/test_responsive_markup.py`)**
- `test_history_uses_hrow_primitive`, `test_history_meta_marked_by_attribute`, `test_history_card_renders_data`, `test_history_detail_shows_error_text`, `test_dashboard_no_utility_classes`, `test_profile_no_utility_classes`, `test_profile_form_contract`
- Параметр `history` в `test_list_page_no_utility_classes`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → фильтры истории | Канал, статус и период приходят от клиента и возвращаются в URL сентинела |
| данные отправки → HTML | Текст ошибки приходит из внешнего мессенджера и не контролируется приложением |
| браузер → форма профиля | Настройки пользователя, включая таймзону |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | вывод текста ошибки отправки | high | mitigate | Текст ошибки — недоверенная строка из внешней системы, и задача требует показывать её целиком. Выводится **только** через autoescape Jinja2; отключение экранирования ради «читаемого форматирования» недопустимо. Покрыто `test_no_unsafe_escaping` из Плана 02 |
| T-05-02 | Information Disclosure | страница детали отправки | high | mitigate | Проверка владельца записи остаётся в обработчике, шаблон её не обходит. Регрессия ловится полной суитой |
| T-05-03 | Tampering | фильтры истории в URL сентинела | high | mitigate | Цикл проброса переносится дословно с экранированием значений для URL; значения попадают в атрибут под autoescape |
| T-05-04 | Spoofing | форма профиля | medium | mitigate | Атрибуты `name`, метод и маршрут переносятся дословно; логика сохранения настроек не трогается. Поведенческий критерий проверяет реальное сохранение |
| T-05-05 | Information Disclosure | снимки отправки на странице детали | low | accept | Состав отображаемых полей не меняется — существующее поведение приложения |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Пакеты не устанавливаются (D-02). Новых зависимостей нет; записей `[ASSUMED]`/`[SUS]`/`[SLOP]` нет |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_responsive_markup.py -x -q` — зелёный с новыми тестами
2. `uv run pytest tests/test_pages/test_htmx_preserved.py -x -q` — страховочная сетка цела
3. `uv run pytest tests/test_pages/test_profile.py -x -q` — зелёный
4. `just test` — после каждой задачи
5. Ручная проверка перестроения записи истории на 1080px и сохранения профиля
</verification>

<success_criteria>
- 7 шаблонов трёх разделов собраны из компонентов, utility-классов не осталось
- Примитив `data-hrow` применён, блок метаданных размечен атрибутом, исправленный медиазапрос срабатывает
- Цепочка прокрутки истории и её фильтры целы
- Текст ошибки отправки виден целиком
- Форма профиля сохранила контракт полей и работает
- Полная суита зелёная
</success_criteria>

<flagged_assumptions>
- **Edge-проб (UI-04, UI-05, UI-06)** — строки `unclassified` / `unresolved`; предикаты взяты из `01-RESEARCH.md` §Validation Architecture.
- **Дашборд остаётся каркасом.** Пять его содержательных элементов относятся к Фазе 4 и здесь сознательно не делаются. Это не сокращение объёма Фазы 1, а граница, зафиксированная ROADMAP: Фаза 1 даёт каркас, Фаза 4 — наполнение.
- **Хрупкие утверждения в покрытии профиля** — проверки на порядок атрибутов элемента выбора таймзоны. План требует найти их до правки и поправить синхронно, а не оставлять красными.
- **`app.css` может дополняться** правилами записи истории. Дополнения обязаны использовать токены Плана 01.
</flagged_assumptions>

<output>
Создать `.planning/phases/01-interfeysnyy-fundament/01-05-SUMMARY.md` по завершении.
Зафиксировать: разметку записи истории с областями сетки (Фаза 4 будет её расширять) и любые правки
существующих тестов с указанием причины.
</output>
