---
status: testing
phase: 01-interfeysnyy-fundament
source: [01-VERIFICATION.md]
started: 2026-08-09T21:17:54Z
updated: 2026-08-09T21:17:54Z
---

## Current Test

number: 1
name: Рантайм модального окна — порядок фокуса, ловушка, Esc
expected: |
  При открытии панели удаления начальный фокус стоит на «Отмена», а не на «Удалить».
  Tab не выводит фокус за пределы панели. Esc закрывает окно. После закрытия фокус
  возвращается на кнопку-триггер, с которой окно было открыто.
awaiting: user response

## Tests

### 1. Рантайм модального окна — порядок фокуса, ловушка, Esc
expected: Открыть `/ads`, нажать «Удалить» у объявления, нажать Tab несколько раз, затем Esc. Начальный фокус на «Отмена»; Tab не выходит за пределы панели; Esc закрывает окно; фокус возвращается на кнопку-триггер.
why_human: Инвариант порядка фокуса живёт целиком в рантайме Alpine. В проекте нет ни playwright, ни selenium, ни jsdom — ни один тест не исполняет JS. Разметка и обработчики присутствуют и подключены (`components/modal.html:54-62`), не исполнены только они.
result: [pending]

### 2. Карточное представление на 375px
expected: Открыть `/accounts`, `/ads`, `/schedules`, `/groups`, `/dashboard`, `/admin/users` при ширине окна 375px. Строки перестраиваются в карточки, шапка колонок исчезает, у каждого значения появляется подпись колонки, сайдбар сменяется нижними табами.
why_human: Визуальная отрисовка медиазапросов. Правила CSS (`app.css:479-487`, `1143-1149`) и атрибуты разметки проверены программно, фактический рендеринг — нет. Пункт покрытия D8.
result: [pending]

### 3. Самоочевидность неподписанных колонок (T-13-09)
expected: Открыть `/billing` и `/admin/groups-info` при ширине 375px. Значения колонок «Тип», «Описание», «Канал», «Обновлено» читаются без подписи (бейдж типа операции, свободный текст, бейдж канала, дата). Если нет — подписи дописываются в Фазе 5 и Фазе 6 соответственно.
why_human: Наблюдение T-13-09, переданное Планом 13 с явным запретом записывать его как принятую базовую линию. Суждение о самоочевидности значения — визуальное. Все числовые колонки в обоих шаблонах подписаны.
result: [pending]

### 4. Мастера подключения WhatsApp и MAX
expected: Пройти мастер подключения WhatsApp и MAX до появления QR-кода и дождаться смены статуса. Блок `#wa-status` / `#max-status` обновляется на месте без перезагрузки страницы, оформление совпадает с переверстанными мастерами.
why_human: `GET /accounts/connect/wa/status` и `/accounts/connect/max/status` требуют живых WA Bridge и MAX worker; HTTP-покрытия у них нет.
result: [pending]

### 5. Визуальное соответствие макету
expected: Просмотреть `/login`, `/register`, `/forgot-password` и внутренние страницы на предмет соответствия макету `new_broadcaster_design.html`. Единый визуальный язык, тёмная тема, кириллица в основной гарнитуре без системного фолбэка.
why_human: Визуальное соответствие макету программно не проверяется.
result: [pending]

### 6. Решение по двум BLOCKER'ам из 01-REVIEW.md
expected: |
  Принять решение по двум критическим находкам код-ревью:
    CR-01 — в `app/pages/schedules.py:204-213,314-315` не проверяется владение `ad_id` и
            `account_id`; можно поставить в расписание чужое объявление, при этом SendLog
            пишется с `user_id` владельца объявления, то есть счёт выставляется жертве.
    CR-02 — в `app/routes/uploads.py:48-52` клиентский Content-Type принимается на веру и
            сохраняется в S3; `image/svg+xml` проходит, а `history/detail.html:80` и
            `admin/user_history_detail.html:91` открывают объект по ссылке target="_blank",
            то есть SVG исполняется на origin хранилища.
  Решение: чинить в этой фазе, вынести в отдельную задачу или отнести в Фазу 2.
why_human: Гейт код-ревью открыт (`status: issues_found`, `critical: 2`). Цели этой фазы находки не блокируют — приложение отрисовывается через новый шелл и дизайн-систему независимо от них, — но ship блокируют. Решение продуктовое, не техническое.
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps
