---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 16
type: execute
wave: 16
depends_on: ["11-15"]
files_modified:
  - app/pages/accounts.py
  - tests/test_routes/test_wa_sync_status.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
  - app/templates/accounts/list.html
  - app/templates/accounts/partial_cards.html
  - app/templates/accounts/partials/sync_status_card.html
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [FORM-04]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: задача 1 плана 11-10 ревизии 1 (150000, 14 файлов на план), разрезанная на ответ обработчика и разметку трёх копий формы.
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Повторная синхронизация уводит с экрана — и на htmx отвечает 204 + `HX-Location` на `/accounts/{id}/groups` (успех) либо `/accounts` (аккаунт не найден), посимвольно на те же адреса, что уезжают 302 без htmx; тела нет (FORM-04, D-02)"
    - "Порядок работы обработчика не меняется: `retry_sync`, запись статуса `syncing` и отправка задачи Celery стоят до ответа на обоих транспортах"
    - "Три копии формы повторной синхронизации (разметка карточки аккаунта) отправляются через `form_wrapper` без цели подмены: новых свопов в шаблоны `accounts/*` план не приносит, тройная копия разметки карточки не сводится (D-12(в))"
    - "Ответ обработчика приземляется задачей 1 отдельным коммитом до разметки: пока формы не переведены задачей 2, они отправляются без htmx и получают прежний 302 — промежуточный коммит зелен и экрана не меняет"
    - "`NOT_YET_CONVERTED_COUNT` 17 → 16 — прогоном, летопись словами; разметочные числа и `HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном"
    - statement: "В браузере «Повторить» на карточке аккаунта переводит на экран групп без перезагрузки, Back не предлагает повторить POST; опрос `account-row-N` и панели удаления на /accounts работают после десятков действий, панели не множатся — ручной UAT фазы, пункты 5 и 7"
      verification: backstop
  artifacts:
    - "app/pages/accounts.py — accounts_retry_sync на respond()"
    - "app/templates/accounts/list.html, accounts/partial_cards.html, accounts/partials/sync_status_card.html — форма повторной синхронизации через form_wrapper"
    - "tests/test_routes/test_wa_sync_status.py — test_retry_sync_over_htmx_lands_by_a_location_header"
  key_links:
    - "форма повторной синхронизации (form_wrapper без цели) → accounts_retry_sync → respond(redirect=экран групп) → HX-Location: ломается — запрос htmx получает документ вместо заголовка перехода, и экран не сменяется"
  prohibitions:
    - statement: "удаление аккаунта из списка фрагментом не делается, смещённый курсор /accounts/partial не трогается (D-04)"
      status: flagged-unverified
      verification: none
    - statement: "тройная копия разметки карточки аккаунта в один макрос не сводится (D-12(в), Deferred Ideas)"
      status: flagged-unverified
      verification: none
    - statement: "фоновые пути синхронизации (Celery, статус syncing) не меняются — план меняет транспорт ответа"
      status: flagged-unverified
      verification: none
---

<objective>
Раздел `accounts` последним (порядок D-15): первое из двух навигационных действий синхронизации —
повторная синхронизация — отвечает `HX-Location` (FORM-04), а три копии её формы идут через обёртку
без цели подмены.

Purpose: действие уводит на экран групп аккаунта — фрагмент здесь был бы правкой на месте там, где её
нет (D-02: разведка относила действие к классу A, сегодняшнее поведение — навигация).

Output: переведённый `accounts_retry_sync`, три копии формы на `form_wrapper`, 17 → 16.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: разметка htmx трёх форм повторной синхронизации; тест
`test_retry_sync_over_htmx_lands_by_a_location_header`.
</artifacts_this_phase_produces>

<edge_coverage_fallback>
## Spec-less edge probe — явное допущение

- **FORM-04 — FLAGGED ASSUMPTION:** зонд краёв строку не классифицировал. Края навигации покрыты явными
  `truths` и случаями модуля пар: аккаунт не найден, чужой и отсутствие сессии — этим планом;
  синхронизация уже идёт, заявка занята и исключение моста — планом 11-17 (тест освобождения заявки).
  Машинного перечня краёв нет.
</edge_coverage_fallback>

<scope_note>
## Размер плана (ревизия 2)

Проекция 80000 в пределах смарт-зоны 100000. План 11-10 ревизии 1 (14 файлов, три задачи) разрезан по
обработчикам: этот план — повторная синхронизация, план 11-17 — синхронизация групп и три входа окна 51
модуля аккаунтов. Внутри плана ответ обработчика отделён от разметки: правила разметки перечня
непереведённых обработчиков не читают (замер ревизии 2: `NOT_YET_CONVERTED` встречается в
`tests/test_templates/test_htmx_markup_gates.py` только в комментариях), и правила обработчиков разметки
htmx у переведённого обработчика не требуют, поэтому ответ приземляется первым, а формы до задачи 2
получают прежний 302.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Повторная синхронизация на слое ответа — переход на экран групп на обоих транспортах</name>

  <files>
app/pages/accounts.py,
tests/test_routes/test_wa_sync_status.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/pages/accounts.py:712-750` — `accounts_retry_sync`: «нет сессии», выборка с `user_id` и типом,
  `retry_sync`, статус `syncing`, отправка задачи Celery, возврат на экран групп.
- `tests/test_routes/test_wa_sync_status.py`, `tests/test_messengers/test_max.py` — действующие тесты
  повторной синхронизации (подмена мессенджера).
- `tests/test_pages/test_htmx_post_pairs.py` — реестр случаев `POST_PAIR_CASES` и форма `_PairCase`.
- `tests/test_pages/test_hx_location_destinations.py:296-350` — `HX_LOCATION_DESTINATION_CALLS_DECLARED`
  и `ADDRESS_BUILDERS`.
- `tests/test_pages/test_htmx_gates.py:195-344` — `NOT_YET_CONVERTED` и летопись словами.
- `11-CONTEXT.md` D-02 (строка `accounts_retry_sync`, ⚠ разведка относила к классу A), D-12(в).
  </read_first>

  <behavior>
RED:
1. `test_retry_sync_over_htmx_lands_by_a_location_header` (в `tests/test_routes/test_wa_sync_status.py`
   рядом с действующими, с той же подменой мессенджера): htmx POST → 204, `HX-Location`
   `/accounts/{id}/groups`, тело пусто, статус аккаунта `syncing`, задача отправлена; без htmx → 302
   туда же.
2. Случаи модуля пар: успех (`LOCATION`), аккаунт не найден (`LOCATION`, `/accounts`), нет сессии
   (`LOCATION`, `/login`).
GREEN — ниже.
  </behavior>

  <action>
**1. Обработчик (D-02).** Все выходы — `respond`: «нет сессии» → `/login`; аккаунт не найден (либо чужой,
либо не `wa`/`max`) → `/accounts`; успех (после `retry_sync`, записи статуса и отправки задачи — порядок
не меняется) → `respond(request, redirect=f"/accounts/{account_id}/groups")`. Комментарий «Повторный
запуск нажимают с экрана групп аккаунта — туда же и возвращаем» сохраняется. Шаблоны в этой задаче не
трогаются: формы отправляются без htmx и получают прежний 302.

**2. Тесты и числа.** Тест и случаи — по §behavior. Ключ снимается из `NOT_YET_CONVERTED`; 17 → 16
прогоном, летопись словами («повторная синхронизация групп аккаунта») с текстом покрасневшего правила.
`HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном, с летописью.
  </action>

  <verify>
    <automated>uv run pytest tests/test_routes/test_wa_sync_status.py tests/test_messengers/test_max.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def accounts_retry_sync/,/^@/' app/pages/accounts.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 16$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_routes/test_wa_sync_status.py -q -k "location_header"` — зелено.
- Разметка в этой задаче не тронута: `git status --porcelain -- app/templates/accounts/` — пустой вывод.
  </acceptance_criteria>

  <done>
Повторная синхронизация отвечает переходом на экран групп на обоих транспортах; формы ещё без htmx и
получают прежний 302.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Три копии формы повторной синхронизации на обёртке — без цели подмены</name>

  <files>
app/templates/accounts/list.html,
app/templates/accounts/partial_cards.html,
app/templates/accounts/partials/sync_status_card.html,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/accounts/list.html:120-136`, `app/templates/accounts/partial_cards.html:75-95`,
  `app/templates/accounts/partials/sync_status_card.html:95-110` — три копии формы `retry-sync`.
- `app/templates/components/form_wrapper.html:138-148` — сигнатура обёртки.
- `tests/test_templates/test_htmx_markup_gates.py:3295-3320`, `:3768-3800`, `:4029-4046` — правила
  «каждая форма отправки несёт цель блокировки», «рождена макросом компонента», «объявляет цель либо
  её отсутствие».
- Результат задачи 1 (обработчик на слое ответа).
  </read_first>

  <action>
**1. Формы (D-12(в)).** Все три копии формы повторной синхронизации — вызов `form_wrapper(action='/accounts/' ~
account.id ~ '/retry-sync')` (в `sync_status_card.html` — с `account_id`) с прежней кнопкой; цели нет.
Копии правятся одинаково; сведения копий в макрос не делается. В `sync_status_card.html` форма стоит
внутри узла, подменяемого опросом, — это не новая цель свопа, и комментарий это называет.

**2. Числа разметки.** Правила `test_htmx_markup_gates.py` называют найденное и объявленное; двигаются
ровно те константы, которые краснеют (вызывающие макрос-обёртку, места отправки htmx), каждая — строкой
летописи «Фаза 11, план 11-16» с дословным текстом отказа.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_routes/test_wa_sync_status.py tests/test_pages/test_responsive_markup.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункты 5 и 7 (раздел accounts, повтор): на /accounts нажать «Повторить» на карточке аккаунта с ошибкой синхронизации — экран групп открывается без перезагрузки; Back не предлагает повторить POST; после десятков действий (повторить, удалить с отменой, ожидание опроса) панели не множатся и открываются. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -c 'form_wrapper' app/templates/accounts/list.html app/templates/accounts/partial_cards.html app/templates/accounts/partials/sync_status_card.html | grep -c ':0$'` возвращает `0`.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "disabled_elt or component_macro or swap_target"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Три копии формы повторной синхронизации отправляются через обёртку без цели подмены; разметочные числа
поставлены прогоном.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | `account_id` пути повторной синхронизации |
| обработчик → Celery | повторный запуск ставит фоновую задачу синхронизации |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-29 | Information Disclosure | повторная синхронизация чужого аккаунта | high | mitigate | Выборка с `MessengerAccount.user_id` не меняется; «не найден» уходит на `/accounts` на обоих транспортах (случаи модуля пар) |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Повторная синхронизация отвечает `HX-Location` на обоих транспортах на прежние адреса.
2. Три копии формы идут через обёртку без цели подмены; в `accounts/*` новых свопов нет.
3. `NOT_YET_CONVERTED_COUNT = 16`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-16-SUMMARY.md` when done.
</output>
