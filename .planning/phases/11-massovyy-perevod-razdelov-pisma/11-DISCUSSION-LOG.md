# Phase 11: Массовый перевод разделов письма - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-14
**Phase:** 11-Массовый перевод разделов письма
**Areas discussed:** отказ по источнику запроса (окно 63); остальные семь зон и вопрос о todo делегированы владельцем

---

## Ход обсуждения

Первое меню (две группы по четыре серые зоны плюс вопрос о todo) владелец **отклонил** и спросил
«что от меня нужно». После пересказа простым нумерованным списком ответил:
**«решай сам по умолчаниям»**. Лично задан один вопрос — тот, что записан как принадлежащий
владельцу (окно 63).

## Отказ по источнику запроса (окно 63)

| Option | Description | Selected |
|--------|-------------|----------|
| Оставить 403 (Рекомендуется) | Именованное изъятие «ветка недостижима из интерфейса»; 9 + 3 места остаются, гейт считает. Цена: при редком сбое прокси — неверная плашка «попробуйте через минуту» | ✓ |
| Понятная плашка с переходом | Отказ через слой ответа: новый код уведомления, `HX-Location` на ту же страницу, панель закрывается. Цена: новый код, отмена свойства плана 10-07, без JS редирект вместо 403 | |

**User's choice:** Оставить 403.
**Notes:** Записано как D-08 — решение владельца.

---

## Делегированные зоны (решения по умолчанию)

| Зона | Варианты, которые были в меню | Принятое умолчание |
|------|------------------------------|--------------------|
| «15 + 5» и граница класса A | накопительное прочтение / переоткрыть перезапуск и снятие задачи фрагментами / снять D-08 вместе с keyset | накопительное прочтение, 12 обработчиков, D-06/D-09/D-08 Фазы 10 не переоткрываются (D-01, D-03, D-04) |
| 422 + эхо и окно 51 | где форма перерисовывается; перенос границ внутрь обработчиков / именованное изъятие | 422 на двух формах с существующей ошибкой поля; границы переносятся, реестр → 0 (D-06, D-07) |
| Новое расписание `afterbegin` | буквальный `afterbegin` / порядок сервера / смена порядка редактора | по свойству — место из порядка сервера (конец), «было ноль» → `HX-Location` (D-05) |
| accounts: синхронизация и три копии | фрагмент карточки / `HX-Location`; сводить ли разметку | `HX-Location` для обоих действий, копии не сводятся (D-02, D-12) |
| Оплата: `HX-Redirect` и GATE-07 | проверка хоста + запись исключения / иная форма | отдельный выход, рантайм-проверка хоста, запись `SAFE_BY_NAME` (D-09) |
| GATE-02: пары и 204 | вселенная обхода; прочтение «200» | «не полный документ»: 200 фрагмент / 204 переход; вселенная — POST через `respond()`, число снимает обход (D-14) |
| admin: `?result=` и тумблеры | сводить / нет; фрагмент блока действий / `HX-Location` | свести в `?notice=`; тумблеры — фрагмент блока действий (D-10, D-02) |
| Совпавшие todo | не втягивать / втянуть order-pollution | не втягивать |

Попутно найдено при сверке с кодом и решено тем же порядком: курсор сводного списка расписаний на
keyset (D-11), прочтение критерия 5 (D-12), `hx-push-url` (D-13), перечень `HX-Retarget`/`HX-Reswap`
(D-16).

## Claude's Discretion

- Имя и сигнатура третьего выхода `htmx.py`; место множества хостов ЮKassa.
- Механизм перерисовки формы на 422.
- Способ сборки вселенной пар GATE-02.
- Имена скрытых полей и постоянных `id`; помощник проверки границы идентификатора.
- Состав побочных OOB-областей карточки пользователя.
- Нарезка на планы и волны.

## Deferred Ideas

- Фрагментное удаление аккаунтов/объявлений вместе с keyset (изъятие D-08 Фазы 10).
- Сведение тройной копии карточки аккаунта.
- `id`/макросы `worker_row`/`queue_row`, фрагментные перезапуск и снятие задачи.
- Проверки ввода в редакторе расписаний; порядок «новые сверху».
- Понятный текст отказа по источнику — возврат при наблюдении в журналах.
- `hx-push-url` по каждой форме — Фаза 15; отказ `stop_impersonation` — Фаза 14.
