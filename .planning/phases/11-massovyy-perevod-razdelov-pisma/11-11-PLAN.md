---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 11
type: execute
wave: 11
depends_on: ["11-10"]
files_modified:
  - app/templates/admin/user_detail.html
  - app/templates/admin/includes/user_actions.html
  - app/templates/admin/includes/user_block_badge.html
  - app/templates/admin/includes/user_access_tile.html
  - app/pages/admin.py
  - tests/test_pages/test_identifier_bounds.py
  - tests/test_pages/test_htmx_gates.py
autonomous: true
requirements: [FORM-03, FORM-08]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: вынос включаемых шаблонов из задачи 1 и задача 3 плана 11-07 ревизии 1 (175000, 13 файлов), разрезанного на 11-11, 11-12 и 11-13.
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Содержимое блока действий, бейдж блокировки и содержимое плитки доступа карточки пользователя живут во включаемых шаблонах `admin/includes/user_actions.html`, `user_block_badge.html`, `user_access_tile.html` (контекст `target_user`, `target_access`, `user`) — единственные источники разметки для страницы и ответов планов 11-12 и 11-13 (D-02)"
    - "Вынос дословный: без id, без атрибутов htmx и без `form_wrapper`; вывод страницы по содержанию прежний, суиты админки и разметки зелены без правки ожиданий"
    - "`admin/includes/worker_row.html` и `queue_row.html` не трогаются (D-03)"
    - "Шесть входов модуля администрирования сняты из окна 51: `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` 14 → 8; первое использование идентификатора — аргумент `id_in_column` после сверки источника и до первой выборки, величина вне колонки идёт веткой «нет» каждого обработчика (D-07)"
  artifacts:
    - "app/templates/admin/includes/user_actions.html, user_block_badge.html, user_access_tile.html (НОВЫЕ)"
    - "app/pages/admin.py — шесть обработчиков на PostIdPath с проверкой первым использованием"
  key_links:
    - "три включаемых шаблона → страница карточки пользователя и шаблон ответа тумблеров: ломается — подписи кнопок, бейдж и плитка на странице и во фрагменте расходятся"
    - "id_in_column → первое использование идентификатора до выборки: ломается — переполнение колонки снова даёт 500 на PostgreSQL"
  prohibitions:
    - statement: "admin/includes/worker_row.html и queue_row.html не получают id и макросов; перезапуск воркера и снятие задачи не становятся фрагментными (D-03, Deferred Ideas)"
      status: flagged-unverified
      verification: none
    - statement: "подтверждение для тумблеров админки не заводится (UI-контракт E5)"
      status: flagged-unverified
      verification: none
    - statement: "именованное изъятие из D-01 для входов окна 51 не чеканится — расхождение снимается работой (D-07)"
      status: flagged-unverified
      verification: none
---

<objective>
Раздел `admin` (порядок D-15) начинается двумя шагами без смены транспорта: области карточки
пользователя, печатающие состояние блокировки и доступа, выносятся во включаемые шаблоны — один
источник для страницы и фрагментов тумблеров (планы 11-12 и 11-13), — и шесть входов модуля снимаются из
окна 51 работой (D-07).

Purpose: тумблеры админки — правка на месте над ЧУЖОЙ учётной записью; ответ обязан показать новое
состояние всюду, где оно напечатано, и из той же разметки, что рисует страница.

Output: три включаемых шаблона, шесть входов на `PostIdPath`, 14 → 8.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `app/templates/admin/includes/user_actions.html`,
`user_block_badge.html`, `user_access_tile.html`.
</artifacts_this_phase_produces>

<scope_note>
## Размер плана (ревизия 2)

Проекция 80000 в пределах смарт-зоны. План 11-07 ревизии 1 (13 файлов, три задачи) разрезан по
обработчикам: этот план — вынос включаемых шаблонов и окно 51 раздела; план 11-12 — блокировка; план
11-13 — бесплатный доступ. Вынос отделён от переводов по форме плана 11-18 (мастер MAX): чистая
перестройка шаблона проверяется неизменностью действующих суит. Окно 51 раздела стоит до переводов:
ветки «нет» у обоих тумблеров сегодня отвечают 302 на те же адреса, которые переводы сохранят, поэтому
матрица `test_identifier_bounds.py` не меняется при последующем переводе; случай модуля пар для
блокировки с величиной вне колонки добавляет план 11-12 вместе с переводом.
</scope_note>

<tasks>

<task type="auto">
  <name>Задача 1: Состояние карточки пользователя во включаемых шаблонах — один источник, страница прежняя</name>

  <files>
app/templates/admin/user_detail.html,
app/templates/admin/includes/user_actions.html (NEW),
app/templates/admin/includes/user_block_badge.html (NEW),
app/templates/admin/includes/user_access_tile.html (NEW)
  </files>

  <read_first>
- `app/templates/admin/user_detail.html` целиком — бейдж блокировки `:53`, плитка доступа `:77-90`,
  блок `data-actions` `:132-180`, панели `modal()` ПОСЛЕ стека `:184-196`.
- `app/pages/admin.py:1318-1370` — `admin_user_detail`: `_active_subscriptions_by_user`, `_access_view`,
  ключ `target_access` и основание имени.
- `app/templates/account_groups/includes/group_row.html` — форма включаемого шаблона с шапкой-
  комментарием Jinja, называющей контекст.
- `tests/test_pages/test_admin_panel.py`, `tests/test_pages/test_blocked_user.py`,
  `tests/test_pages/test_free_access.py` — действующие утверждения страницы карточки.
- `11-CONTEXT.md` D-02, D-03.
  </read_first>

  <action>
Из `user_detail.html` ДОСЛОВНО выносятся: содержимое блока действий → `admin/includes/user_actions.html`;
бейдж блокировки → `admin/includes/user_block_badge.html`; содержимое плитки доступа →
`admin/includes/user_access_tile.html`. Шапка-комментарий Jinja каждого называет контекст
(`target_user`, `target_access`, `user`) и то, что шаблон — единственный источник разметки для страницы и
ответов тумблеров (планы 11-12 и 11-13). Страница включает их на прежних местах. В этой задаче НЕ
заводятся ни постоянные обёртки с id, ни атрибуты htmx, ни `form_wrapper`: формы тумблеров, формы-
триггеры входа под пользователем и удаления переносятся как есть; панели `modal()` остаются вне блока.
Обработчики не трогаются (D-03: строки воркеров и очереди — тоже).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_admin_panel.py tests/test_pages/test_blocked_user.py tests/test_pages/test_free_access.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_responsive_markup.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -cE 'admin/includes/(user_actions|user_block_badge|user_access_tile)\.html' app/templates/admin/user_detail.html` возвращает `3`.
- `grep -c 'hx-post' app/templates/admin/includes/user_actions.html` возвращает `0` (разметка htmx приходит планами 11-12 и 11-13).
- `git status --porcelain -- app/pages/admin.py` — пустой вывод (обработчики в этой задаче не тронуты).
- `git diff --numstat -- app/templates/admin/includes/worker_row.html app/templates/admin/includes/queue_row.html` — пустой вывод.
  </acceptance_criteria>

  <done>
Состояние карточки пользователя рисуется из трёх включаемых шаблонов; страница по содержанию прежняя,
суиты зелены без правки ожиданий.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Окно 51 — шесть входов модуля администрирования, 14 → 8</name>

  <files>
app/pages/admin.py,
tests/test_pages/test_identifier_bounds.py,
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `app/pages/admin.py` — сигнатуры `admin_drop_task` (`account_id`), `admin_toggle_block`,
  `admin_delete_user`, `admin_impersonate`, `admin_toggle_free_access` (`user_id`), `admin_restart_worker`
  (`account_id`) и их ветки «нет».
- `app/pages/identifiers.py` — `PostIdPath`, `id_in_column`.
- `tests/test_pages/test_htmx_gates.py` — шесть записей модуля администрирования в
  `VALIDATION_REFUSAL_DIVERGENCES`; `tests/test_pages/test_identifier_bounds.py:916-1048` — матрица.
- `tests/test_pages/test_confirm_delete_transport.py` — «missing identifier» случаи админских маршрутов.
  </read_first>

  <behavior>
RED: строки матрицы `test_identifier_bounds.py` для шести входов ожидают ветку «нет» обработчика
(`/admin/users` для пользовательских, ветку неизвестного аккаунта для воркера и очереди) вместо 422.
Сегодня 422. Случай модуля пар для блокировки с величиной вне колонки — план 11-12 (тумблер ещё не
переведён, и пара его транспортов не существует).
GREEN — ниже.
  </behavior>

  <action>
Шесть обработчиков переходят на `PostIdPath` (D-07); в каждом первое использование идентификатора —
аргумент `id_in_column`, стоящий после сверки источника (она идентификатора не читает) и до первой
выборки; ложный результат — ветка «нет» этого обработчика: «пользователя нет» у четырёх
пользовательских, «аккаунт неизвестен» у перезапуска воркера и снятия задачи (у снятия задачи —
та же ветка неизвестного аккаунта, что сегодня; её адрес переводит план 11-14). Шесть записей
модуля администрирования снимаются из реестра окна 51; `VALIDATION_REFUSAL_DIVERGENCES_DECLARED`
14 → 8 со строкой летописи (D-07, план 11-11) и текстом покрасневшего правила.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_admin_panel.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 8$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `grep -c 'entry="app/pages/admin.py::POST' tests/test_pages/test_htmx_gates.py` возвращает `0`.
- `uv run pytest tests/test_pages/test_identifier_bounds.py -q -k "checked_before_its_first_use"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Шесть входов администрирования проверяют идентификатор первым использованием; реестр окна 51 — 8.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| администратор → чужая учётная запись | карточка печатает блокировку и доступ ЧУЖОГО пользователя |
| браузер → страничный слой | `user_id` и `account_id` пути у шести админских маршрутов |
| обработчик → драйвер БД | целое вне int32 роняет запрос драйвером |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-18 | Elevation of Privilege | права на действия карточки пользователя | high | mitigate | `require_admin` и запрет под чужой личностью не трогаются; вынос шаблонов не меняет ни одного условия видимости форм (перенос дословный); `tests/test_pages/test_admin_panel.py` в verify |
| T-11-41 | Denial of Service | переполнение колонки на шести входах администрирования | high | mitigate | `id_in_column` — первое использование параметра после сверки источника и до выборки (правило каталога плана 11-02); матрица `test_identifier_bounds.py` |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_identifier_bounds.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Три включаемых шаблона состояния карточки пользователя; страница по содержанию прежняя; строки воркеров и очереди не тронуты.
2. `VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 8`; входов модуля администрирования в реестре нет.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-11-SUMMARY.md` when done.
</output>
