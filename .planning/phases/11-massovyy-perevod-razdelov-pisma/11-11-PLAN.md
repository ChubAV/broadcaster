---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 11
type: execute
wave: 11
depends_on: ["11-10"]
files_modified:
  - tests/test_pages/test_htmx_post_pairs.py
  - app/pages/htmx.py
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GATE-02, FORM-03, FORM-04]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Параметризованный обход дерева `tests/` находит каждое утверждение `status_code == 302` о ответе на POST к переведённому обработчику страничного слоя и утверждает, что у этого обработчика есть пара (случай модуля пар либо запись `CONFIRMED_DELETE_ROUTES`); существующие утверждения 302 не заменены и не размножены (GATE-02, D-14)"
    - "Вселенная обхода — утверждения 302 на POST-запросах к `@router.post`-обработчикам `app/pages/`, идущим через `respond()`; редиректы GET-страниц и обработчики, ещё стоящие в `NOT_YET_CONVERTED`, вне её и входят автоматически, покидая перечень (D-14)"
    - "Число таких утверждений объявлено константой `PAIRED_302_ASSERTIONS_DECLARED`, поставленной ПРОГОНОМ; летопись называет прогноз «160» (разведка 2026-08-26) и эвристические замеры 2026-09-14 («около 136» CONTEXT, 157 RESEARCH) прогнозами, пережитыми обходом — не ошибками (D-14, форма D-17 Фазы 10)"
    - "Отрицательные контроли обхода: синтетическое утверждение 302 на переведённый обработчик без пары краснеет и называет файл и строку; утверждение 302 на GET и на обработчик из `NOT_YET_CONVERTED` в счёт не входит; пустой корень тестов краснеет правилом числа"
    - "Докстринги слоя ответа перемерены и ссылаются на символы, а не на номера строк: число вызовов приложения, подающих фрагмент, и число подающих фрагмент вместе с кодом исхода сняты воспроизводимой командой разбора дерева, прежние формулировки («ТРИ сборщика», ссылка на строку удаления расписания) остаются названными устаревшими"
    - "Записи вехи приведены без смены статусов: у критериев 1, 3, 4, 5 Фазы 11 в `ROADMAP.md` стоят летописи прочтений D-01/D-03, D-05/D-06, D-14, D-12 и наблюдение D-13; у FORM-03/FORM-04 и GATE-02 в `REQUIREMENTS.md` — летописи «15/5» и «160 → N»; окно 51 отмечено исправленным, окно 63 — решённым владельцем (D-08); ни один флажок плана и ни одна клетка состояния требования не тронуты"
  artifacts:
    - "tests/test_pages/test_htmx_post_pairs.py — _post_302_assertions, PAIRED_302_ASSERTIONS_DECLARED, test_every_302_assertion_on_a_converted_handler_is_paired, test_the_number_of_paired_302_assertions_is_the_declared_one, test_control_an_unpaired_302_assertion_reddens_the_traversal, test_control_a_get_or_unconverted_302_stays_out_of_the_count"
    - "app/pages/htmx.py — перемеренные абзацы _glue_notice и respond"
    - ".planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md — летописи и окна"
  key_links:
    - "таблица маршрутов приложения (create_app().routes) → ключ обработчика `модуль::функция` → множество переведённых минус NOT_YET_CONVERTED: ломается — утверждение 302 приписано не тому обработчику, и пара «есть» у чужого"
  prohibitions:
    - statement: "флажки планов роадмапа и статусы требований не переводятся в Complete этим планом — это делает закрытие фазы после вердикта верификации"
      status: flagged-unverified
      verification: none
    - statement: "архив прогноза `.planning/research/ARCHITECTURE.md` («15/5») не правится — летопись пишется рядом с критерием (D-17 Фазы 10)"
      status: flagged-unverified
      verification: none
    - statement: "существующие утверждения 302 суиты не переписываются в пары и не удаляются (D-14)"
      status: flagged-unverified
      verification: none
---

<objective>
Критерий 4 закрывается числом, которое снимает обход (GATE-02, D-14), докстринги слоя ответа
перемеряются после двенадцати переводов, и записи вехи получают летописи прочтений, записанных
решениями фазы, — чтобы верификатор читал критерии роадмапа вместе с ними.

Purpose: пока пары живут реестром случаев без обхода утверждений 302, «суита перестаёт быть зелёной
по построению» остаётся обещанием; обход делает непарное утверждение красным с именем файла и строки.

Output: обход и константа в модуле пар, перемеренные докстринги, летописи `ROADMAP.md` и
`REQUIREMENTS.md`, окна 51 и 63.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-04-SUMMARY.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-10-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `_post_302_assertions`, `PAIRED_302_ASSERTIONS_DECLARED`,
`test_every_302_assertion_on_a_converted_handler_is_paired`,
`test_the_number_of_paired_302_assertions_is_the_declared_one`,
`test_control_an_unpaired_302_assertion_reddens_the_traversal`,
`test_control_a_get_or_unconverted_302_stays_out_of_the_count` (`tests/test_pages/test_htmx_post_pairs.py`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Обход утверждений 302 — число снимает дерево тестов, непарное утверждение краснеет с именем</name>

  <files>
tests/test_pages/test_htmx_post_pairs.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_post_pairs.py` целиком — реестр случаев и замыкание (планы 11-01…11-10).
- `tests/test_pages/test_htmx_gates.py:195-344` (`NOT_YET_CONVERTED`), `:683-800` (`_post_handlers`,
  `_converted`) — как ключ `модуль::функция` собирается у гейтов.
- `app/main.py` — `create_app(settings)` и подключение страничных роутеров с префиксами.
- `tests/test_pages/test_hx_location_destinations.py:280-330` — разбор дерева вместо грепа, основание.
- `11-RESEARCH.md` §Сверка дрейфа (строка «191 утверждение 302, ~136 на POST», замер 157) и §«Паттерн 4».
- `11-CONTEXT.md` D-14 целиком.
  </read_first>

  <behavior>
RED:
1. `test_control_an_unpaired_302_assertion_reddens_the_traversal`: синтетический корень тестов с
   функцией `resp = await client.post(f"/schedules/{x}/edit", …)` и `assert resp.status_code == 302`
   при реестре пар без случаев правки расписания → жалоба называет синтетический файл и строку.
2. `test_control_a_get_or_unconverted_302_stays_out_of_the_count`: синтетические `client.get(...)` с
   302 и POST на маршрут обработчика из `NOT_YET_CONVERTED` (авторизация) в счёт не входят.
3. `test_the_number_of_paired_302_assertions_is_the_declared_one` с заведомо неверным числом `0`
   краснеет и печатает найденное — число переписывается из текста отказа.
GREEN — обход и число.
  </behavior>

  <action>
**1. Помощник `_post_302_assertions(tests_root, route_table)`.** Разбор дерева каждого `tests/**/*.py`
(без модуля пар самого): в каждой тестовой функции (синхронной и асинхронной) — присваивания имени
результату вызова `<что-то>.post(<адрес>, …)` (с `await` и без); адрес — строковая константа либо
f-строка, подстановки которой приводятся к местозаполнителю, строка запроса и якорь отбрасываются.
Утверждения `assert <имя>.status_code == 302` (сравнение `Eq` с константой 302), где имя связано
последним предшествующим таким присваиванием в той же функции, собираются записями «файл, строка,
форма адреса». Таблица маршрутов строится из `create_app(settings).routes` (`APIRoute` с методом POST;
ключ обработчика — `endpoint.__module__` в виде пути `app/pages/<файл>.py` и `endpoint.__name__`), так
что префиксы роутеров учтены самим приложением. Форма адреса сопоставляется с путём маршрута
посегментно (местозаполнитель ↔ местозаполнитель или произвольный сегмент). В счёт входят записи,
чей обработчик переведён (`_converted` гейтов) и не стоит в `NOT_YET_CONVERTED`.

**2. Правила.** `test_every_302_assertion_on_a_converted_handler_is_paired`: обработчик каждой записи
входит в покрытые (ключи реестра модуля ∪ `route.key` записей `CONFIRMED_DELETE_ROUTES`); жалоба —
«файл:строка → обработчик». `test_the_number_of_paired_302_assertions_is_the_declared_one`: число
записей равно `PAIRED_302_ASSERTIONS_DECLARED`. Константа ставится ПРОГОНОМ (сначала `0`, число из
текста отказа); летопись: «160» — прогноз разведки 2026-08-26 о ВСЕЙ вехе; «около 136» (CONTEXT) и 157
(RESEARCH, функции с `.post(`) — эвристики 2026-09-14 разных единиц счёта; объявлено число, снятое
обходом этого правила, — прогнозы устарели, а не ошиблись (форма D-17 Фазы 10). Контроли — по
§behavior, на `tmp_path`. Докстринг модуля пополняется: вселенная D-14, и то, что утверждения
незатронутых обработчиков войдут в счёт автоматически, когда Фазы 13–14 снимут их из
`NOT_YET_CONVERTED` (правило числа краснеет — это движение, а не регрессия).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_post_pairs.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^PAIRED_302_ASSERTIONS_DECLARED = [1-9][0-9]*$' tests/test_pages/test_htmx_post_pairs.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q -k "unpaired_302 or stays_out_of_the_count or number_of_paired_302"` — зелено, 3 теста.
- `grep -nE '^def _post_302_assertions' tests/test_pages/test_htmx_post_pairs.py` печатает одну строку (обход — помощник модуля пар, а не копия в соседнем модуле).
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q --collect-only | tail -1` показывает не меньше 40 собранных тестов (случаи всех планов фазы).
  </acceptance_criteria>

  <done>
Каждое утверждение 302 о POST к переведённому обработчику имеет пару, число таких утверждений
снято обходом и объявлено; непарное утверждение краснеет с файлом и строкой.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Перемер докстрингов слоя ответа — символы вместо строк, числа воспроизводимой командой</name>

  <files>
app/pages/htmx.py
  </files>

  <read_first>
- `app/pages/htmx.py` целиком после планов 11-05 и 11-08: докстринг модуля (выходы, собственные
  выходы), `_glue_notice` (абзац «ГРАНИЦА БЕЗОПАСНОСТИ ПОДМЕНЫ ТЕЛА… Сборщиков этих сегодня ТРИ»),
  `respond` (абзац о коде исхода на ветке фрагмента и воспроизводимый вход разбора дерева).
- `app/templates/components/modal.html:720-750` — образец перехода ссылок на имена символов «без номеров
  строк» (план 10-46, `WR-03`).
  </read_first>

  <action>
Запустить воспроизводимую команду разбора дерева из докстринга `respond` (вызовы `respond` в `app/` с
именованным аргументом фрагмента и с фрагментом и кодом исхода) и перенести ОБА числа в абзацы
`_glue_notice` и `respond`. Ссылки на места вызовов записываются ИМЕНАМИ обработчиков и файлами их
объявления, без двоеточий с номерами строк. Прежние формулировки — «сборщиков ТРИ» с тремя ссылками на
строки, «код исхода не подаёт НИ ОДИН», ссылка на строку вызова удаления расписания — остаются
названными устаревшими (верными для дерева, на котором писались), номера строк в цитате пишутся словом
«строка», а не двоеточием. Граница безопасности `_glue_notice` («сборщик возвращает свежий ответ без
фоновых задач») перепроверяется чтением двенадцати новых сборщиков фазы и пишется с результатом
проверки. Докстринг модуля сверяется с деревом: выходы `respond`, `refuse`, `redirect_external`,
`respond_field_error`, `malformed_request_response`; собственных выходов переведённых обработчиков —
двенадцать, решение о форме — D-08.
  </action>

  <verify>
    <automated>uv run python -c "import ast,pathlib; c=[n for p in pathlib.Path('app').rglob('*.py') for n in ast.walk(ast.parse(p.read_text(encoding='utf-8'))) if isinstance(n,ast.Call) and getattr(n.func,'id',getattr(n.func,'attr',None))=='respond']; f=[n for n in c if 'fragment' in {k.arg for k in n.keywords}]; print(len(f), sum('notice' in {k.arg for k in n.keywords} for n in f))" && uv run pytest tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_notices_surface.py -q</automated>
    <fails_when>non-zero exit, or the printed pair of numbers is absent from the docstrings of app/pages/htmx.py, or "failed" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'schedules.py:1240' app/pages/htmx.py` возвращает `0`.
- `grep -cE 'account_groups\.py:[0-9]+' app/pages/htmx.py` возвращает `0`.
- Оба числа, напечатанные командой verify, встречаются в докстринге `respond`: `grep -n 'Фаза 11' app/pages/htmx.py` печатает хотя бы одну строку рядом с числами.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Докстринги слоя ответа говорят числа, снятые с дерева после фазы, и ссылаются на символы; опровергнутые
редакции названы, а не стёрты.
  </done>
</task>

<task type="auto">
  <name>Задача 3: Записи вехи — летописи прочтений у критериев и требований, окна 51 и 63</name>

  <files>
.planning/ROADMAP.md,
.planning/REQUIREMENTS.md,
.planning/WINDOWS.md
  </files>

  <read_first>
- `.planning/ROADMAP.md` §«Phase 11» (критерии 1–5, строка «Research: … `app/pages/ads.py:522` не
  проверял никто», порядок внутри фазы) и образец летописи `**Летопись числа мест: 16 → 18**` у Фазы 10.
- `.planning/REQUIREMENTS.md:20-65` — FORM-03, FORM-04, GATE-02 и образец летописи у FORM-06.
- `.planning/WINDOWS.md` — окна 51 и 63 (строки таблицы и JSON-блок) и заголовок с командами
  `windows waive` / `windows fixed`.
- Сводки планов 11-01…11-10 — числа, поставленные прогоном (двенадцать обработчиков, `PAIRED_302_ASSERTIONS_DECLARED`,
  наблюдение D-13 из 11-04).
- `tests/test_planning/` — правила записи (флажки и клетки состояний — предмет вердикта, не плана).
- `11-CONTEXT.md` D-01, D-03, D-05, D-06, D-08, D-12, D-13, D-14 и §specifics (летописи пишутся рядом с
  критерием).
  </read_first>

  <action>
**1. `ROADMAP.md`, раздел Фазы 11** — абзацы летописи ПОД списком критериев, форма «Летопись…» Фазы 10,
без правки текста самих критериев и без смены флажков:
- критерий 1: «15 класса A / 5 класса B» — прогноз разведки 2026-08-26 на всю веху; Фазы 9–10
  потребили все 5 класса B и 4 класса A; предмет Фазы 11 — двенадцать обработчиков (девять
  фрагментных, два навигационных, один внешний), `NOT_YET_CONVERTED_COUNT` 26 → 14; прогноз устарел, а
  не ошибся (D-01); запись «три файла `admin/includes/*` обязаны получить `id` и макросы» опиралась на
  тот же прогноз — id получил только блок действий карточки пользователя и две области состояния с
  потребителем-свопом (D-03);
- критерий 3: `afterbegin` прочтён по свойству — вставка без перерисовки контейнера в место, которое
  даёт порядок сервера (`beforeend`, D-05); 422 с эхом — ровно у профиля и мастера MAX (D-06), своп
  правилом 422 (Находка B);
- критерий 4: «160» — прогноз; объявлено число обхода `PAIRED_302_ASSERTIONS_DECLARED` = N (из сводки
  11-11); «с `HX-Request` → 200» прочтено как «htmx-путь не получает полного документа и редиректа»:
  фрагмент → 200 без `<!DOCTYPE`, навигация → 204 + `HX-Location`, оплата → 204 + `HX-Redirect` (D-14);
- критерий 5: прочтение по разделам — раскрытие карточки серверное, внутри целей только голый
  `x-data`, в шаблоны карточки аккаунта новых свопов нет (D-12); пункты 5 и 7 — ручной UAT;
- строка «Research» Фазы 11: адрес заголовка истории переехал на `app/pages/ads.py` строка 548 (было
  522); совместного поведения атрибута и заголовка нет — атрибут не ставится (D-13), заголовок
  переживает `respond()` (тест плана 11-04).

**2. `REQUIREMENTS.md`** — летописи под FORM-03 и FORM-04 (прогноз «15/5» пережит Фазами 9–10, D-01),
под GATE-02 («160 → N», D-14), той же формой, что «16 → 18» у FORM-06; флажки и клетки таблицы
состояний не трогаются.

**3. Окна.** Командами реестра, не правкой файла руками:
`node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 51` и
`node .claude/gsd-core/bin/gsd-tools.cjs windows waive 63 "D-08, решение владельца 2026-09-14: голый 403 на провале сверки источника остаётся именованным изъятием; двенадцать выходов объявлены OWN_RESPONSE_EXITS с состоянием решения, машинный гейт продолжает считать выходы"`.
Если команда отказывает (замок реестра, неизвестный вербатим) — остановиться и привести текст отказа
в сводке, а не править таблицу руками.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>non-zero exit, or "failed" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -n 'Летопись' .planning/ROADMAP.md | grep -c 'Фаза 11'` возвращает не меньше `3`.
- `grep -n 'PAIRED_302_ASSERTIONS_DECLARED' .planning/ROADMAP.md .planning/REQUIREMENTS.md` печатает хотя бы по одной строке на файл.
- `grep -nE '^\| 51 \|.*\| fixed \|' .planning/WINDOWS.md` печатает одну строку; `grep -nE '^\| 63 \|.*\| waived \|' .planning/WINDOWS.md` печатает одну строку.
- Ни один флажок не тронут: `git diff -U0 -- .planning/ROADMAP.md .planning/REQUIREMENTS.md > "${TMPDIR:-/tmp}/11-11-records.diff"` завершается с кодом `0`, после чего `grep -cE '^[-+].*\[[x ]\]' "${TMPDIR:-/tmp}/11-11-records.diff"` возвращает `0`.
- `uv run pytest tests/test_planning/ -q` — зелено.
  </acceptance_criteria>

  <done>
Верификатор читает критерии 1, 3, 4 и 5 роадмапа и требования FORM-03/04 и GATE-02 вместе с
летописями прочтений; окно 51 исправлено работой, окно 63 закрыто решением владельца.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| дерево тестов → гейт | обход читает исходники тестов разбором дерева, не исполняя их |
| реестр окон → отгрузка | `/gsd-ship` блокирует при открытых окнах (при `windows_enforce`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-35 | Repudiation | закрытие окна 63 решением, приписанным владельцу | medium | mitigate | Причина окна цитирует D-08 с датой и файлом `11-CONTEXT.md`, где решение записано как принятое владельцем лично; окно закрывается командой реестра с обязательной причиной |
| T-11-36 | Tampering | молчаливое ослабление обхода пар (утверждения, не попавшие в счёт) | medium | mitigate | Число объявлено константой и поставлено прогоном; отрицательные контроли непарного утверждения и исключения GET/непереведённых |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_gates.py tests/test_planning/ -q`.
- `uv run pytest tests/ -q` — полный прогон фазы без фильтра маркеров до `/gsd-verify-work` (известный красный `full-suite-ads-editor-order-pollution` отличать от регрессии).
- `graphify update .` в основном каталоге после слияния.
</verification>

<success_criteria>
1. `PAIRED_302_ASSERTIONS_DECLARED` поставлен обходом; каждое утверждение 302 о переведённом обработчике имеет пару.
2. Докстринги `htmx.py` перемерены и ссылаются на символы.
3. Летописи прочтений стоят у критериев и требований; окна 51 и 63 закрыты командами реестра; флажков и статусов план не трогал.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-11-SUMMARY.md` when done.
</output>
