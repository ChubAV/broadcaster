---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 12
type: execute
wave: 12
depends_on: ["11-11"]
files_modified:
  - app/pages/admin.py
  - app/templates/admin/user_detail.html
  - app/templates/admin/partials/user_actions_response.html
  - tests/test_pages/test_admin_panel.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_templates/test_htmx_markup_gates.py
  - app/templates/admin/includes/user_actions.html
autonomous: true
requirements: [FORM-03, FORM-04]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: перевод блокировки из задачи 1 плана 11-07 ревизии 1, разрезанный на ответ обработчика и разметку формы.
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Блокировка из карточки пользователя на htmx отвечает 200 и содержимым `#user-actions` (`innerHTML`) с подписью кнопки по новому состоянию, плюс внеполосной подменой содержимого `#user-block-badge` и `#user-access-tile`; без htmx — прежний 302 на `/admin/users/{id}` (FORM-03, D-02)"
    - "Места страницы, печатающие признак блокировки и безлимита, обновляются из тех же включаемых шаблонов, что рисуют полную страницу (план 11-11) — второй копии разметки нет"
    - "«Пользователя нет» (в том числе величина вне колонки) → переход на `/admin/users`, «нельзя заблокировать себя» → переход на `/admin/users/{id}` — на htmx 204 + `HX-Location` на тот же адрес, что и 302 (FORM-04, D-02, D-07)"
    - "Голый 403 на провале сверки источника у блокировки остаётся и объявлен записью `OWN_RESPONSE_EXITS` по решению владельца: состояние решения `DECISION_OWNER_D08` ссылается на D-08, основание называет недостижимость из интерфейса на htmx-пути и цену — плашку «Действие не выполнено. Попробуйте ещё раз через минуту.»; `OWN_RESPONSE_EXITS_DECLARED` 9 → 10 (D-08)"
    - "Постоянный id получают блок действий и две области состояния, у которых есть потребитель-своп; строки воркеров и очереди не трогаются (D-03)"
    - "Ответ обработчика приземляется задачей 1 до разметки: пока форма блокировки не переведена задачей 2, она отправляется без htmx и получает прежний 302; форма бесплатного доступа в этом плане не трогается — её обработчик переводит план 11-13"
    - "`NOT_YET_CONVERTED_COUNT` 20 → 19, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 9 → 10 — прогоном"
    - statement: "В браузере десяток нажатий «Заблокировать/Разблокировать» подряд без перезагрузки меняют подпись, бейдж и плитку доступа, панели входа под пользователем и удаления не множатся и открываются — ручной UAT фазы, пункт 5"
      verification: backstop
  artifacts:
    - "app/pages/admin.py — admin_toggle_block на respond()"
    - "app/templates/admin/user_detail.html — обёртки с id user-actions, user-block-badge, user-access-tile"
    - "app/templates/admin/partials/user_actions_response.html (НОВЫЙ)"
    - "tests/test_pages/test_htmx_gates.py — DECISION_OWNER_D08, запись OWN_RESPONSE_EXITS блокировки"
  key_links:
    - "форма блокировки (hx-target=#user-actions, innerHTML) → ответ с тем же включаемым шаблоном: ломается — подпись кнопки не совпадает с состоянием"
    - "OWN_RESPONSE_EXITS ↔ замер собственных выходов переведённых обработчиков: ломается — новый 403 без записи краснеет с именем"
  prohibitions:
    - statement: "отказ по источнику не переводится на слой ответа с новым кодом уведомления и переходом — отвергнуто владельцем (D-08)"
      status: flagged-unverified
      verification: none
    - statement: "admin/includes/worker_row.html и queue_row.html не получают id и макросов; перезапуск воркера и снятие задачи не становятся фрагментными (D-03, Deferred Ideas)"
      status: flagged-unverified
      verification: none
    - statement: "подтверждение для тумблеров админки не заводится (UI-контракт E5)"
      status: flagged-unverified
      verification: none
---

<objective>
Раздел `admin` (порядок D-15): блокировка пользователя из его карточки переходит на фрагмент блока
действий с внеполосными областями состояния, а её отказ по источнику объявляется изъятием по решению
владельца D-08.

Purpose: тумблер — правка на месте над ЧУЖОЙ учётной записью; ответ обязан показать новое состояние во
всех местах страницы, где оно напечатано, иначе администратор видит противоречащие друг другу подписи.

Output: переведённый `admin_toggle_block`, три постоянных id, шаблон ответа, `DECISION_OWNER_D08` и
запись D-08, форма блокировки на `form_wrapper`, 20 → 19, 9 → 10, 9 → 10.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-PATTERNS.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-11-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `app/templates/admin/partials/user_actions_response.html`;
id `user-actions`, `user-block-badge`, `user-access-tile`; `DECISION_OWNER_D08`
(`tests/test_pages/test_htmx_gates.py`); запись `OWN_RESPONSE_EXITS` блокировки; тест
`test_block_toggle_over_htmx_refreshes_every_place_of_the_state`.
</artifacts_this_phase_produces>

<decision_reading>
## Прочтение D-03 рядом с D-02

D-03: «Постоянный id получает только блок действий `admin/user_detail.html`. `admin/includes/worker_row.html`
и `queue_row.html` в этой фазе id и макросов НЕ получают… id без потребителя-свопа ничего не
добавляет». D-02 для этих же тумблеров требует «OOB для прочих мест страницы, печатающих то же
состояние», а §Claude's Discretion отдаёт состав этих областей планировщику. Внеполосная подмена
содержимого требует id цели. Прочтение: «только» в D-03 противопоставлено трём файлам
`admin/includes/*` роадмапа, а основание D-03 («id без потребителя-свопа») прямо разрешает id
областям, у которых потребитель-своп ЕСТЬ. Поэтому id получают ровно две области состояния и блок
действий, строки воркеров и очереди — нет. Запись повторяется шапкой шаблона ответа.
</decision_reading>

<scope_note>
## Размер плана (ревизия 2)

Проекция 90000 в пределах смарт-зоны. Один переведённый обработчик на план (разрез плана 11-07 ревизии 1
по обработчикам). Внутри плана ответ отделён от разметки формы: правила разметки перечня непереведённых
обработчиков не читают, поэтому ответ со своими целями (обёртки с id и шаблон ответа) приземляется
первым, а форма переходит на обёртку задачей 2. Отвергнутый шов — обе задачи отдельными планами: оба
стали бы планами из одной задачи.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Блокировка на слое ответа — блок действий, бейдж и плитка из одних шаблонов, изъятие D-08</name>

  <files>
app/pages/admin.py,
app/templates/admin/user_detail.html,
app/templates/admin/partials/user_actions_response.html (NEW),
tests/test_pages/test_admin_panel.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/pages/admin.py:1318-1370` — `admin_user_detail`: `_active_subscriptions_by_user`, `_access_view`,
  ключ `target_access`; `:1812-1850` — `admin_toggle_block` (сверка источника, «нет пользователя»,
  «себя нельзя», инверсия, проверка первым использованием плана 11-11).
- `app/templates/admin/user_detail.html` и три включаемых шаблона плана 11-11.
- `app/templates/ads/partials/sched_delete_response.html` — плоское тело с узлами `innerHTML:#…`.
- `tests/test_pages/test_htmx_gates.py:3736-3990` — `_OwnResponseExit`, `LIFTING_CONDITION_OWN_RESPONSE`,
  `_OWN_RESPONSE_PRICE`, `_OWN_RESPONSE_ORIGIN_GUARD`, девять записей, число, `:4086-4200` — правило
  авторства и `_DECISION_REFERENCE` (`D-\d{2}`).
- `app/templates/includes/htmx_error_banner.html:300-310` — текст плашки и исключение только для 422
  (цена D-08).
- `tests/test_pages/test_admin_panel.py` — действующие тесты блокировки; `tests/test_pages/test_blocked_user.py`.
- `11-CONTEXT.md` D-02, D-03, D-08 целиком.
  </read_first>

  <behavior>
RED:
1. `test_block_toggle_over_htmx_refreshes_every_place_of_the_state`: админ, htmx POST
   `/admin/users/{id}/block` → 200; тело начинается с содержимого блока действий (форма
   `/admin/users/{id}/block` с подписью «Разблокировать»); внеполосный узел `innerHTML:#user-block-badge`
   с бейджем «Заблокирован»; внеполосный `innerHTML:#user-access-tile`; нет `<!DOCTYPE`. Без htmx →
   302 `/admin/users/{id}`.
2. Случаи модуля пар: успех (`FRAGMENT`), пользователя нет (`LOCATION`, `/admin/users`), себя
   (`LOCATION`, `/admin/users/{admin}`), `user_id = 2147483648` (`LOCATION`, `/admin/users`, D-07).
3. Правило полноты `OWN_RESPONSE_EXITS` краснеет на переведённом тумблере до записи.
GREEN — ниже.
  </behavior>

  <action>
**1. Постоянные обёртки (D-02, D-03).** `user_detail.html` включает шаблоны плана 11-11 внутри
постоянных обёрток: существующий `<div data-actions>` получает `id="user-actions"`, бейдж — внутри
`<span id="user-block-badge">` в `data-identity-meta`, плитка — внутри обёртки с `id="user-access-tile"`
в карточке плитки. Вывод страницы по содержанию прежний; формы в `user_actions.html` в этой задаче не
трогаются.

**2. Шаблон ответа `admin/partials/user_actions_response.html` (НОВЫЙ).** Плоское тело: включение
`user_actions.html` (содержимое для `innerHTML`); безусловный узел `hx-swap-oob="innerHTML:#user-block-badge"`
с включением бейджа; безусловный узел `hx-swap-oob="innerHTML:#user-access-tile"` с включением плитки.
Шапка — перечень узлов и §decision_reading.

**3. `admin_toggle_block`.** Сверка источника с голым 403 остаётся выше и не трогается (D-08).
Выходы — `respond`: «пользователя нет» (и ложный `id_in_column`) → `/admin/users`; «себя» →
`/admin/users/{user_id}`; успех → `respond(request, redirect=f"/admin/users/{user_id}", fragment=…)`;
сборщик рендерит шаблон ответа с перечитанным пользователем и `target_access`, собранным ТЕМИ ЖЕ
функциями, что у `admin_user_detail`.

**4. Изъятие D-08.** В `test_htmx_gates.py` заводится константа `DECISION_OWNER_D08` — строка
состояния решения со ссылкой «решение владельца 2026-09-14, `11-CONTEXT.md` D-08: голый 403 на
провале сверки источника остаётся именованным изъятием». Запись `_OwnResponseExit(entry=
"app/pages/admin.py::admin_toggle_block", kind="Response(status_code=403)", …)`: основание — общие
`_OWN_RESPONSE_PRICE` и `_OWN_RESPONSE_ORIGIN_GUARD` плюс хвост «СВОЁ У ЭТОГО ВХОДА»: недостижимость
на htmx-пути (браузер сам ставит `Sec-Fetch-Site: same-origin`; `selfRequestsOnly: true`; поддельная
форма приходит полной навигацией и получает тот же 403) и цена словами — плашка «Действие не
выполнено. Попробуйте ещё раз через минуту.» (у тумблера панели нет). ⚠️ Если общий текст
`_OWN_RESPONSE_PRICE` («плашка не рисуется») расходится с измеренным поведением плашки
(`htmx_error_banner.html:305` исключает только 422), константа получает поколение, называющее
опровергнутую часть, а не переписывается молча. `lifting_condition` —
`LIFTING_CONDITION_OWN_RESPONSE`; `decision_state` — `DECISION_OWNER_D08`.
`OWN_RESPONSE_EXITS_DECLARED` 9 → 10 прогоном.

**5. Числа.** Ключ `admin_toggle_block` снимается из `NOT_YET_CONVERTED`, добавляется в
`FRAGMENT_RESPONSE_HANDLERS`; 20 → 19 и 9 → 10 прогоном, летопись словами («блокировка пользователя
из его карточки»). `OOB_BLOCKS` и прочие разметочные числа шаблона ответа и
`HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном, каждое с летописью «Фаза 11, план 11-12».
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_admin_panel.py tests/test_pages/test_blocked_user.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_origin_guard_on_destructive_routes.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def admin_toggle_block/,/^@router/' app/pages/admin.py | grep -c 'RedirectResponse'` возвращает `0`.
- `awk '/^async def admin_toggle_block/,/^@router/' app/pages/admin.py | grep -n 'Response(status_code=403)'` печатает одну строку (голый 403 остался).
- `grep -n 'id="user-actions"' app/templates/admin/user_detail.html` печатает одну строку.
- `git diff --numstat -- app/templates/admin/includes/worker_row.html app/templates/admin/includes/queue_row.html` — пустой вывод.
- `grep -nE '^OWN_RESPONSE_EXITS_DECLARED = 10$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 19$' tests/test_pages/test_htmx_gates.py` печатает одну строку; `grep -nE '^FRAGMENT_RESPONSE_HANDLERS_DECLARED = 10$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_admin_panel.py -q -k "refreshes_every_place_of_the_state"` — зелено.
  </acceptance_criteria>

  <done>
Блокировка отвечает фрагментом блока действий и обновляет бейдж и плитку из тех же шаблонов,
исходы вне экрана — переходом; голый 403 объявлен изъятием D-08.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Форма блокировки на обёртке — подмена содержимого блока действий</name>

  <files>
app/templates/admin/includes/user_actions.html,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/admin/includes/user_actions.html` — форма блокировки и форма бесплатного доступа
  (план 11-11), формы-триггеры входа под пользователем и удаления.
- `app/templates/components/form_wrapper.html:138-148`;
  `app/templates/account_groups/includes/group_row.html:193-215` — образец блочного вызова обёртки.
- `tests/test_templates/test_htmx_markup_gates.py:3295-3320`, `:4029-4200` — цель блокировки, цели
  свопа, параметрические цели.
  </read_first>

  <action>
Форма блокировки в `user_actions.html` — блочный вызов `form_wrapper(action=…, target='#user-actions',
swap='innerHTML')` с прежней подписью по состоянию. Форма бесплатного доступа в этом плане НЕ
переводится: её обработчик ещё отвечает перенаправлением, и запрос htmx вклеил бы в блок действий целый
документ — форму переводит план 11-13 вместе с обработчиком. Формы-триггеры входа под пользователем и
удаления (голый признак клиентского состояния) не меняются; панели `modal()` остаются вне блока.
Разметочные числа — прогоном, каждое с летописью «Фаза 11, план 11-12» и дословным текстом отказа.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py tests/test_pages/test_admin_panel.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункт 5 (раздел admin, блокировка): открыть карточку пользователя, десять раз подряд переключить блокировку без перезагрузки; подпись кнопки, бейдж и плитка согласованы после каждого нажатия; панели «Войти под пользователем» и «Удалить» открываются, в DOM по одной. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -c "target='#user-actions'" app/templates/admin/includes/user_actions.html` возвращает `1`.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "swap_target or two_roles or client_state"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Форма блокировки отправляется через обёртку и подменяет содержимое блока действий; форма бесплатного
доступа ждёт своего обработчика.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| администратор → чужая учётная запись | тумблер меняет блокировку ЧУЖОГО пользователя |
| сторонний сайт → изменяющий маршрут админки | cookie прикладывается браузером к межсайтовой форме |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-17 | Tampering (CSRF) | тумблер блокировки | high | mitigate | `is_same_origin` с голым 403 стоит после `require_admin` и до выборки, не меняется (D-08); запись `OWN_RESPONSE_EXITS` с правилом полноты краснеет на новом выходе без записи |
| T-11-18 | Elevation of Privilege | права на тумблер | high | mitigate | `require_admin` и запрет под чужой личностью не трогаются; `tests/test_pages/test_admin_panel.py` в verify |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Блокировка отвечает фрагментом блока действий с внеполосными областями состояния из тех же шаблонов.
2. `OWN_RESPONSE_EXITS_DECLARED = 10`, запись блокировки в состоянии `DECISION_OWNER_D08`.
3. `NOT_YET_CONVERTED_COUNT = 19`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 10`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-12-SUMMARY.md` when done.
</output>
