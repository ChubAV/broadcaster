---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 10
type: execute
wave: 10
depends_on: ["11-09"]
files_modified:
  - app/pages/accounts.py
  - app/templates/accounts/connect_max.html
  - app/templates/accounts/includes/max_connect_step.html
  - app/pages/account_groups.py
  - app/pages/history.py
  - tests/test_pages/test_max_connect_transport.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_identifier_bounds.py
  - tests/test_pages/test_htmx_response_contract.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_pages/test_responsive_markup.py
  - tests/test_pages/test_account_groups.py
  - tests/test_pages/test_history_retry.py
autonomous: true
requirements: [FORM-08, FORM-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  tokens: 165000
  raw_tokens: 165000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Старт подключения MAX на htmx отвечает 200 и содержимым контейнера шага мастера `#max-connect-step` (`innerHTML`): шаг QR с блоком опроса `#max-status` либо шаг с ошибкой подключения — без `<!DOCTYPE` (FORM-03, D-02)"
    - "Пустой телефон (в том числе из одних пробелов) отвечает 422 на обоих транспортах: htmx — фрагмент шага телефона с текстом «Введите номер телефона» и введённым значением в поле через автоэкранирование; без htmx — полная страница со статусом 422 (FORM-08, D-06)"
    - "Литерал статуса 422 в приложении по-прежнему один — выход `respond_field_error`; `SERVER_SIDE_VALIDATION_RESPONSES` не двигается"
    - "Без htmx успешный старт приземляет 302 на `/accounts`, где заведённый аккаунт в статусе подключения виден; адрес выбран по измерению: завершить подключение умеет только опрос htmx (`accounts_connect_max_status`), а GET шага мастера удаляет незавершённые аккаунты — приземление туда уничтожило бы только что заведённую запись"
    - "Встроенный обработчик отправки формы телефона снят: защиту от второй отправки несёт цель блокировки обёртки, индикатор ожидания виден всегда (запрос длится не меньше пяти секунд)"
    - "Пять последних входов окна 51 — четыре у групп аккаунта и один у повтора отправки — сняты работой: `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` 5 → 0, и гейт утверждает, что ни один вход POST-обработчика страничного слоя не несёт границы фреймворка — не вакуумно: синтетический вход с границей краснеет с именем (D-07)"
    - "`NOT_YET_CONVERTED_COUNT` 15 → 14 (остаются 4 обработчика QR-мастера Telegram и 10 авторизационных), `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 11 → 12 — прогоном"
    - statement: "В браузере старт подключения MAX показывает индикатор на время ожидания, затем QR и начинает опрос статуса без перезагрузки; пустой телефон из пробелов перерисовывает шаг с ошибкой и введённым значением — ручной UAT фазы, пункт 5 (accounts)"
      verification: backstop
  artifacts:
    - "app/pages/accounts.py — accounts_connect_max_start на respond() и respond_field_error()"
    - "app/templates/accounts/includes/max_connect_step.html (НОВЫЙ) — шаги мастера MAX; id max-connect-step в connect_max.html"
    - "app/pages/account_groups.py, app/pages/history.py — четыре и один вход на PostIdPath"
    - "tests/test_pages/test_max_connect_transport.py (НОВЫЙ)"
    - "tests/test_pages/test_htmx_gates.py — test_no_page_post_input_keeps_a_framework_bound и синтетические контроли реестра окна 51"
  key_links:
    - "форма телефона (hx-target=#max-connect-step, innerHTML) → фрагмент шага QR с hx-get every 3s → accounts_connect_max_status: ломается — QR виден, но подключение никогда не завершается"
    - "_framework_bounded_post_inputs == {} ↔ VALIDATION_REFUSAL_DIVERGENCES == () при DECLARED = 0: ломается — новая граница фреймворка проезжает без записи"
  prohibitions:
    - statement: "QR-мастер Telegram (connect_tg_user, четыре обработчика) не трогается — Фаза 13"
      status: flagged-unverified
      verification: none
    - statement: "авторизационные обработчики не трогаются — Фаза 14"
      status: flagged-unverified
      verification: none
    - statement: "новых проверок ввода номера телефона не заводится — ошибка поля ровно та, что существует сегодня (D-06)"
      status: flagged-unverified
      verification: none
---

<objective>
Последний обработчик фазы — старт подключения MAX — отвечает фрагментом шага мастера, а пустой
телефон — 422 с эхом (второй и последний авторский 422 фазы, FORM-08); пять последних входов окна
51 снимаются работой, и реестр расхождения формы отказа пустеет с не-вакуумным гейтом (D-07).

Purpose: после этого плана все двенадцать обработчиков фазы на слое ответа (`NOT_YET_CONVERTED_COUNT`
= 14 — ровно предмет Фаз 13 и 14), а окно 51 закрыто не изъятием, а работой.

Output: перевод `accounts_connect_max_start`, включаемый шаг мастера, модуль тестов транспорта MAX,
пять входов на `PostIdPath`, реестр окна 51 = 0.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-05-SUMMARY.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `app/templates/accounts/includes/max_connect_step.html`;
id `max-connect-step`; `tests/test_pages/test_max_connect_transport.py` с
`test_max_start_over_htmx_returns_the_step_container`,
`test_an_empty_phone_answers_422_with_the_echo_on_both_transports`,
`test_max_start_without_htmx_lands_on_the_accounts_list`;
`test_no_page_post_input_keeps_a_framework_bound`,
`test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule` (`tests/test_pages/test_htmx_gates.py`).
</artifacts_this_phase_produces>

<decision_reading>
## Два прочтения, записанные, чтобы верификатор не искал противоречия

**D-12(в) и шаблон мастера MAX.** D-12(в): «В шаблоны `accounts/*` фаза НОВЫХ свопов не приносит (D-02:
оба действия синхронизации — `HX-Location`), поэтому тройная копия разметки карточки аккаунта в этой
фазе не сводится». Предмет абзаца — три шаблона КАРТОЧКИ аккаунта и два действия синхронизации. D-02
для `accounts_connect_max_start` прямо назначает «фрагмент контейнера шага мастера (постоянный id)».
Шаблон мастера `accounts/connect_max.html` карточкой не является, и своп в его контейнер исполняет D-02,
не нарушая D-12(в).

**Адрес деградации старта MAX** (усмотрение планировщика — D-02 его не назначает). `respond()` требует
адрес перенаправления; сегодня путь без JavaScript получает страницу QR прямо в ответ на POST. Измерено
чтением кода: подключение завершает ТОЛЬКО опрос htmx `accounts_connect_max_status`, то есть без
JavaScript мастер не завершается и сегодня; GET `/accounts/connect/max` удаляет аккаунты в статусе
`connecting`. Приземление на `/accounts` сохраняет заведённую запись и не отнимает ни одной работающей
возможности. Решение обратимо (`reversible`) и названо в докстринге обработчика.
</decision_reading>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Старт подключения MAX — контейнер шага мастера и второй авторский 422</name>

  <files>
app/pages/accounts.py,
app/templates/accounts/connect_max.html,
app/templates/accounts/includes/max_connect_step.html (NEW),
tests/test_pages/test_max_connect_transport.py (NEW),
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_responsive_markup.py
  </files>

  <read_first>
- `app/pages/accounts.py:490-625` — GET мастера MAX (удаление незавершённых аккаунтов),
  `accounts_connect_max_start` целиком (пустой телефон, заведение/повторное использование аккаунта,
  `start_session`, `asyncio.sleep(5)`, `get_qr`, ошибка подключения, контекст шага `qr`);
  `:626-680` — `accounts_connect_max_status` (единственное место завершения подключения).
- `app/templates/accounts/connect_max.html` целиком — алерт ошибки, ветки `connected` / `phone` / `qr`
  с QR / `qr` без QR, форма телефона со встроенным обработчиком отправки, узлы `#max-status` с опросом.
- `app/templates/components/field.html:7` — `field(name, label, type, value, required, …)`.
- `app/pages/htmx.py` — `respond_field_error` (план 11-05).
- `tests/test_pages/test_responsive_markup.py` и `tests/test_pages/test_htmx_preserved.py` — правила
  разметки мастера MAX (узлы опроса «объявлены дважды»).
- `11-RESEARCH.md` Pitfall 9 (`asyncio.sleep(5)`, `reportValidityOfForms` и `required` — 422 достижим
  пробелами).
  </read_first>

  <behavior>
RED (модуль `tests/test_pages/test_max_connect_transport.py`, мессенджер и `asyncio.sleep` подменены):
1. `test_max_start_over_htmx_returns_the_step_container`: htmx POST с телефоном → 200, тело несёт
   `<img` QR и узел `id="max-status"` с опросом, не несёт `<!DOCTYPE`; подмена `start_session`,
   бросающая исключение → 200 и текст «Ошибка подключения к MAX».
2. `test_an_empty_phone_answers_422_with_the_echo_on_both_transports`: `phone="   "` под htmx → 422,
   текст «Введите номер телефона», нет `<!DOCTYPE`; значение `"><script>x</script>` из пробелов не
   собирается — отдельный случай: телефон `"   "` возвращается в поле; без htmx → 422 с `<!DOCTYPE`.
3. `test_max_start_without_htmx_lands_on_the_accounts_list`: без htmx → 302 `/accounts`, аккаунт в
   статусе `connecting` существует после приземления.
4. Случаи модуля пар: успех (`FRAGMENT`, метка `id="max-status"`), нет сессии (`LOCATION`, `/login`).
GREEN — ниже.
  </behavior>

  <action>
**1. Шаг мастера.** Содержимое карточки `connect_max.html` — алерт ошибки и ветки шагов — выносится в
`accounts/includes/max_connect_step.html` (контекст: `connected`, `step`, `qr_code`, `error`, `phone`);
страница включает его в постоянную обёртку `<div id="max-connect-step">` внутри карточки. Форма
телефона — блочный вызов `form_wrapper(action='/accounts/connect/max/start',
target='#max-connect-step', swap='innerHTML')`; встроенный обработчик отправки со сменой подписи
кнопки снимается (защита — цель блокировки обёртки, ожидание — индикатор обёртки); поле —
`field(name="phone", …, value=phone, required=true, …)`. Узлы опроса и их комментарий-якорь переносятся
дословно; «объявлен дважды» остаётся верным.

**2. Обработчик.** «Нет сессии» → `respond(request, redirect="/login")`. Пустой телефон после
`strip()` → `respond_field_error(request, page=…, fragment=…)`: страница — `connect_max.html` с шагом
`phone`, `error` «Введите номер телефона» (дословно) и `phone` = присланное значение; фрагмент — тот
же включаемый шаг. Остальной путь (аккаунт, сессия, ожидание, QR, ошибка) не меняется; финальный
выход → `respond(request, redirect="/accounts", fragment=…)`, сборщик рендерит включаемый шаг `qr` с
`qr_code`/`error`. Докстринг называет §decision_reading (адрес деградации и его основание).

**3. Числа.** Ключ снимается из `NOT_YET_CONVERTED` и добавляется в `FRAGMENT_RESPONSE_HANDLERS`;
15 → 14 и 11 → 12 прогоном; сводная летопись фазы у `NOT_YET_CONVERTED_COUNT` называет двенадцать
обработчиков СЛОВАМИ и остаток: четыре обработчика QR-мастера Telegram (Фаза 13) и десять
авторизационных (Фаза 14). `SERVER_SIDE_VALIDATION_RESPONSES` не двигается (утверждается прогоном
`test_htmx_response_contract.py`). Разметочные числа — прогоном.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_max_connect_transport.py tests/test_pages/test_responsive_markup.py tests/test_pages/test_htmx_preserved.py tests/test_pages/test_htmx_response_contract.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_templates/test_htmx_markup_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or tests/test_pages/test_max_connect_transport.py collects no tests</fails_when>
    <human-check>UAT фазы, пункт 5 (accounts, мастер MAX): на /accounts/connect/max ввести номер и нажать «Продолжить» — индикатор виден ~5 секунд, кнопка заблокирована, затем QR появляется без перезагрузки и во вкладке Network идут запросы статуса раз в 3 секунды; ввести номер из пробелов (сняв required в DevTools) — шаг перерисован с ошибкой и введённым значением, плашки аварии нет. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `awk '/^async def accounts_connect_max_start/,/^@/' app/pages/accounts.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -c 'onsubmit=' app/templates/accounts/connect_max.html app/templates/accounts/includes/max_connect_step.html | grep -v ':0$' | wc -l` возвращает `0`.
- `grep -n 'id="max-connect-step"' app/templates/accounts/connect_max.html` печатает одну строку.
- `grep -rn 'status_code=422' app/ | cut -d: -f1 | sort -u` печатает ровно `app/pages/htmx.py`.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 14$' tests/test_pages/test_htmx_gates.py` печатает одну строку; `grep -nE '^FRAGMENT_RESPONSE_HANDLERS_DECLARED = 12$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_max_connect_transport.py -q` — зелено, не меньше 3 тестов.
  </acceptance_criteria>

  <done>
Старт MAX отвечает фрагментом шага мастера, пустой телефон — авторским 422 с эхом на обоих
транспортах; все двенадцать обработчиков фазы на слое ответа.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Окно 51 закрыто работой — пять последних входов и не-вакуумная пустота реестра</name>

  <files>
app/pages/account_groups.py,
app/pages/history.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_identifier_bounds.py,
tests/test_pages/test_account_groups.py,
tests/test_pages/test_history_retry.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/pages/account_groups.py` — сигнатуры `account_groups_toggle` и `account_groups_delete`
  (`account_id`, `group_id` пути), ветки «аккаунта нет» (`respond(request, redirect="/accounts")`) и
  «группы нет», тройной `WHERE` и неотличимость четырёх случаев удаления.
- `app/pages/history.py:826-999` — `history_retry` (`log_id`), ветка «записи нет».
- `tests/test_pages/test_htmx_gates.py:2360-2430` (`_ValidationRefusalDivergence`,
  `DECISION_WAITS_FOR_THE_OWNER`, `LIFTING_CONDITION_VALIDATION_REFUSAL`), оставшиеся пять записей,
  `:2984-3172` (замер), `:3280-3520` — правила и три контроля, опирающиеся на БОЕВЫЕ записи
  (укороченный перечень, снятые записи админки, пустое основание).
- `tests/test_pages/test_identifier_bounds.py:916-1048` — матрица входов.
- `tests/test_pages/test_account_groups.py:3250-3272` — 422 GET-курсора (не трогается: это GET).
  </read_first>

  <behavior>
RED:
1. Строки матрицы для четырёх входов групп и входа повтора ожидают ветку «нет» обработчика вместо 422.
2. `test_no_page_post_input_keeps_a_framework_bound`: замер `_framework_bounded_post_inputs` на
   настоящем дереве пуст — краснеет, пока пять входов на ограниченных псевдонимах.
3. `test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule`: синтетический модуль с
   POST-обработчиком на `Path(ge=1)` даёт жалобу с именем входа.
GREEN — ниже.
  </behavior>

  <action>
**1. Входы.** `account_groups_toggle`, `account_groups_delete` (оба параметра пути) и `history_retry`
переходят на `PostIdPath`; первое использование каждого идентификатора — аргумент `id_in_column`,
стоящий после сверки источника и до первой выборки; ложный результат — ветка «нет» обработчика:
негодный `account_id` — ветка «аккаунта нет», негодный `group_id` — ветка «группы нет» (у удаления —
та же неотличимая форма, что у несуществующей группы; узлы снятия строятся из целого пути, как для
любого отсутствующего идентификатора), негодный `log_id` — ветка «записи нет» повтора.

**2. Реестр окна 51 → 0.** Пять записей снимаются; `VALIDATION_REFUSAL_DIVERGENCES` становится пустым
кортежем; `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` 5 → 0 со сводной летописью: 23 → 16 (11-02,
schedules) → 14 (11-04, ads) → 8 (11-06, admin) → 5 (11-09, accounts) → 0 (11-10, account_groups и
history) — расхождение снято РАБОТОЙ (D-07), изъятие не чеканилось, вопрос владельцу о законности
потерял предмет. `LIFTING_CONDITION_VALIDATION_REFUSAL` получает поколение «исполнено». Машинерия
реестра (тип записи, правила полноты, обоснований, авторства) остаётся — она ловит следующую границу
фреймворка на POST.

**3. Не-вакуумность.** `test_no_page_post_input_keeps_a_framework_bound` утверждает, что замер на
настоящем дереве пуст. Три контроля, опиравшиеся на боевые записи, переводятся на СИНТЕТИЧЕСКИЕ
входы: исходник с POST-обработчиком на `Path(ge=1, le=…)` и синтетические записи расхождения (полнота
— синтетический вход без записи называется поимённо; обоснования — синтетическая запись с пустым
полем; авторство — синтетическая запись с состоянием «решено» без `D-NN`). Новый контроль — по
§behavior, пункт 3. Правило «число равно объявленному» на пустом кортеже зелено ТОЛЬКО вместе с
не-вакуумным правилом пустоты — докстринг это называет (память проекта: «RED-гейт зеленеет вакуумом»).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_account_groups.py tests/test_pages/test_history_retry.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 0$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `awk '/^VALIDATION_REFUSAL_DIVERGENCES:/,/^\)/' tests/test_pages/test_htmx_gates.py | grep -c '_ValidationRefusalDivergence('` возвращает `0`.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -k "framework_bound or validation_refusal or divergence"` — зелено, включая `test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule`.
- `uv run pytest tests/test_pages/test_identifier_bounds.py -q -k "checked_before_its_first_use"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Ни один вход POST-обработчика страничного слоя не несёт границы фреймворка; реестр окна 51 пуст, и
пустота доказана синтетическим контролем.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → мастер MAX | номер телефона и сам факт старта сессии приходят от клиента |
| обработчик → мост MAX | старт сессии и QR — внешний вызов с ожиданием |
| браузер → входы групп и повтора | `account_id`, `group_id`, `log_id` пути |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-31 | Tampering (XSS) | эхо телефона во фрагменте и странице 422 | medium | mitigate | Только автоэкранирование `field(value=…)`; тест пустого телефона утверждает возврат значения и отсутствие неэкранированной разметки |
| T-11-32 | Information Disclosure | номер телефона в разметке | low | mitigate | Номер живёт только в поле формы (T-06-02) — в заголовки, атрибуты вне поля и журнал не переносится; перенос шаблона дословный |
| T-11-33 | Denial of Service | повторный старт сессии двойным нажатием | medium | mitigate | Цель блокировки обёртки на время запроса; повторное использование аккаунта в статусе `connecting` на сервере не меняется |
| T-11-34 | Denial of Service | переполнение колонки на входах групп и повтора | high | mitigate | `id_in_column` первым использованием (правило каталога); реестр окна 51 пуст с не-вакуумным гейтом |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_response_contract.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Старт MAX — фрагмент шага; пустой телефон — 422 с эхом на обоих транспортах; литерал 422 один.
2. `NOT_YET_CONVERTED_COUNT = 14`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 12`.
3. `VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 0` с не-вакуумным правилом пустоты.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-10-SUMMARY.md` when done.
</output>
