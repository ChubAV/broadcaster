---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 10
type: execute
wave: 10
depends_on: ["11-09"]
files_modified:
  - app/pages/accounts.py
  - app/templates/accounts/list.html
  - app/templates/accounts/partial_cards.html
  - app/templates/accounts/partials/sync_status_card.html
  - app/templates/account_groups/list.html
  - app/static/css/app.css
  - tests/test_routes/test_sync_groups.py
  - tests/test_routes/test_wa_sync_status.py
  - tests/test_pages/test_identifier_bounds.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_pages/test_responsive_markup.py
autonomous: true
requirements: [FORM-04]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Повторная синхронизация уводит с экрана — и на htmx отвечает 204 + `HX-Location` на `/accounts/{id}/groups` (успех) либо `/accounts` (аккаунт не найден), посимвольно на те же адреса, что уезжают 302 без htmx; тела нет (FORM-04, D-02)"
    - "Синхронизация групп заменяет список целиком — и на всех выходах отвечает на htmx 204 + `HX-Location` на тот же экран групп либо на `/accounts` (идиома D-09 Фазы 9); ни один выход, включая htmx-транспорт и ветку исключения, не минует освобождения заявки `_SYNC_IN_FLIGHT` (Pitfall 8)"
    - "Формы повторной синхронизации (три копии разметки карточки аккаунта) и форма синхронизации групп отправляются через `form_wrapper` без цели подмены (`hx-swap=none`): новых свопов в шаблоны `accounts/*` фаза не приносит, тройная копия разметки карточки не сводится (D-12(в))"
    - "Признак идущей синхронизации (`data-syncing`) сохраняет своё оформление: атрибут переезжает на обёртку формы, правило таблицы стилей адресуется обёрткой, число вхождений признака на экране прежнее"
    - "Три входа модуля аккаунтов сняты из окна 51: `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` 8 → 5; величина вне колонки идёт веткой «аккаунт не найден» (D-07)"
    - "`NOT_YET_CONVERTED_COUNT` 17 → 15 — прогоном"
    - statement: "В браузере «Повторить» на карточке аккаунта и «Синхронизировать всё» на экране групп переводят экран без перезагрузки, Back не предлагает повторить POST; опрос `account-row-N` и панели удаления на /accounts работают после десятков действий, панели не множатся — ручной UAT фазы, пункты 5 и 7"
      verification: backstop
  artifacts:
    - "app/pages/accounts.py — accounts_retry_sync, accounts_sync_groups на respond(); три обработчика на PostIdPath"
    - "app/templates/accounts/list.html, accounts/partial_cards.html, accounts/partials/sync_status_card.html — форма повторной синхронизации через form_wrapper"
    - "app/templates/account_groups/list.html и app/static/css/app.css — форма синхронизации через form_wrapper, признак data-syncing на обёртке"
  key_links:
    - "return await respond(...) ВНУТРИ try → finally _release_sync_slot: ломается — после первого htmx-запроса аккаунт навсегда «занят» до перезапуска процесса"
  prohibitions:
    - statement: "удаление аккаунта из списка фрагментом не делается, смещённый курсор /accounts/partial не трогается (D-04)"
      status: flagged-unverified
      verification: none
    - statement: "тройная копия разметки карточки аккаунта в один макрос не сводится (D-12(в), Deferred Ideas)"
      status: flagged-unverified
      verification: none
    - statement: "фоновые пути синхронизации (Celery, статус syncing) и серверный guard повторного запуска не меняются — фаза меняет транспорт ответа"
      status: flagged-unverified
      verification: none
---

<objective>
Раздел `accounts` последним (порядок D-15): два навигационных действия синхронизации отвечают
`HX-Location` (FORM-04), их формы идут через обёртку без цели подмены, и три входа модуля снимаются
из окна 51.

Purpose: оба действия уводят с экрана или заменяют список целиком — фрагмент здесь был бы правкой на
месте там, где её нет; а синхронизация групп держит внутрипроцессную заявку, которую ранний выход
транспорта не имеет права оставить занятой.

Output: переведённые `accounts_retry_sync` и `accounts_sync_groups`, четыре формы на `form_wrapper`,
перенос признака `data-syncing`, 17 → 15, 8 → 5.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-RESEARCH.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: разметка htmx трёх форм повторной синхронизации и
формы синхронизации групп; тесты `test_retry_sync_over_htmx_lands_by_a_location_header`,
`test_sync_groups_over_htmx_releases_the_claim_on_every_exit`.
</artifacts_this_phase_produces>

<edge_coverage_fallback>
## Spec-less edge probe — явное допущение

- **FORM-04 — FLAGGED ASSUMPTION:** зонд краёв строку не классифицировал. Края навигации (аккаунт
  не найден, чужой, синхронизация уже идёт, заявка занята, исключение моста, отсутствие сессии)
  покрыты явными `truths`, случаями модуля пар и тестом освобождения заявки; машинного перечня
  краёв нет.
</edge_coverage_fallback>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Повторная синхронизация — переход на экран групп, три копии формы на обёртке</name>

  <files>
app/pages/accounts.py,
app/templates/accounts/list.html,
app/templates/accounts/partial_cards.html,
app/templates/accounts/partials/sync_status_card.html,
tests/test_routes/test_wa_sync_status.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py
  </files>

  <read_first>
- `app/pages/accounts.py:712-750` — `accounts_retry_sync`: «нет сессии», выборка с `user_id` и типом,
  `retry_sync`, статус `syncing`, отправка задачи Celery, возврат на экран групп.
- `app/templates/accounts/list.html:120-136`, `app/templates/accounts/partial_cards.html:75-95`,
  `app/templates/accounts/partials/sync_status_card.html:95-110` — три копии формы `retry-sync`.
- `app/templates/components/form_wrapper.html:138-148`.
- `tests/test_templates/test_htmx_markup_gates.py:3295-3320`, `:3768-3800`, `:4029-4046` — правила
  «каждая форма отправки несёт цель блокировки», «рождена макросом компонента», «объявляет цель либо
  её отсутствие».
- `tests/test_routes/test_wa_sync_status.py`, `tests/test_messengers/test_max.py` — действующие тесты
  повторной синхронизации.
- `11-CONTEXT.md` D-02 (строка `accounts_retry_sync`, ⚠ разведка относила к классу A), D-12(в).
  </read_first>

  <behavior>
RED:
1. `test_retry_sync_over_htmx_lands_by_a_location_header` (в `tests/test_routes/test_wa_sync_status.py`
   рядом с действующими, с той же подменой мессенджера): htmx POST → 204, `HX-Location`
   `/accounts/{id}/groups`, тело пусто, статус аккаунта `syncing`, задача отправлена; без htmx → 302
   туда же.
2. Случаи модуля пар: успех (`LOCATION`), аккаунт не найден (`LOCATION`, `/accounts`), нет сессии
   (`LOCATION`, `/login`).
GREEN — ниже.
  </behavior>

  <action>
**1. Обработчик.** Все выходы — `respond`: «нет сессии» → `/login`; аккаунт не найден (либо чужой,
либо не `wa`/`max`) → `/accounts`; успех (после `retry_sync`, записи статуса и отправки задачи — порядок
не меняется) → `respond(request, redirect=f"/accounts/{account_id}/groups")`. Комментарий «Повторный
запуск нажимают с экрана групп аккаунта — туда же и возвращаем» сохраняется.

**2. Формы.** Все три копии формы повторной синхронизации — вызов `form_wrapper(action='/accounts/' ~
account.id ~ '/retry-sync')` (в `sync_status_card.html` — с `account_id`) с прежней кнопкой; цели нет.
Копии правятся одинаково; сведения копий в макрос не делается (D-12(в)). В
`sync_status_card.html` форма стоит внутри узла, подменяемого опросом, — это не новая цель свопа, и
комментарий это называет.

**3. Числа.** Ключ снимается из `NOT_YET_CONVERTED`; 17 → 16 прогоном, летопись словами («повторная
синхронизация групп аккаунта»); разметочные числа (вызывающие макрос-обёртку) — прогоном.
  </action>

  <verify>
    <automated>uv run pytest tests/test_routes/test_wa_sync_status.py tests/test_messengers/test_max.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_templates/test_htmx_markup_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def accounts_retry_sync/,/^@/' app/pages/accounts.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -c 'form_wrapper' app/templates/accounts/list.html app/templates/accounts/partial_cards.html app/templates/accounts/partials/sync_status_card.html | grep -c ':0$'` возвращает `0`.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 16$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_routes/test_wa_sync_status.py -q -k "location_header"` — зелено.
  </acceptance_criteria>

  <done>
Повторная синхронизация отвечает переходом на экран групп на обоих транспортах; три копии формы
отправляются через обёртку без цели подмены.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Синхронизация групп — переход на всех выходах и освобождение заявки на каждом</name>

  <files>
app/pages/accounts.py,
app/templates/account_groups/list.html,
app/static/css/app.css,
tests/test_routes/test_sync_groups.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py,
tests/test_pages/test_responsive_markup.py
  </files>

  <read_first>
- `app/pages/accounts.py:752-990` — `_SYNC_IN_FLIGHT`, `_claim_sync_slot`, `_release_sync_slot`,
  `accounts_sync_groups` ЦЕЛИКОМ: ранние выходы до заявки, внешний `try/finally`, узкий и широкий
  `except`, ветка `IntegrityError`, финальный возврат.
- `app/templates/account_groups/list.html:60-95` — форма синхронизации с условным `data-syncing` и
  комментарий «Кнопка при выполнении НЕ отключается».
- `app/static/css/app.css:2620-2632` и `:2720-2726` — два правила `[data-acct-head] form[data-syncing]`.
- `tests/test_routes/test_sync_groups.py` — 19 утверждений 302, в том числе на исключениях моста.
- `tests/test_pages/test_responsive_markup.py` — правила разметки шапки аккаунта (если есть).
- `11-RESEARCH.md` Pitfall 8.
  </read_first>

  <behavior>
RED:
1. `test_sync_groups_over_htmx_releases_the_claim_on_every_exit` (параметризован по исходам:
   успех, отказ получения групп мостом, неожиданное исключение, конфликт уникальности): htmx POST →
   204 и `HX-Location` `/accounts/{id}/groups`; после ответа `_SYNC_IN_FLIGHT` пуст; без htmx — 302
   туда же.
2. Случаи модуля пар: успех (`LOCATION`), синхронизация уже идёт (`LOCATION`, тот же экран), аккаунт
   не найден (`LOCATION`, `/accounts`).
GREEN — ниже.
  </behavior>

  <action>
**1. Обработчик.** Каждый выход — `return await respond(request, redirect=…)`: «нет сессии» → `/login`;
аккаунт не найден → `/accounts`; неподдерживаемый тип, статус `syncing`, заявка занята — экран групп;
все выходы ПОСЛЕ занятия заявки остаются ВНУТРИ внешнего `try`, освобождение в `finally` не
меняется, и сборка ответа не выносится до входа в `try` (Pitfall 8). Исключения моста и конфликт
уникальности пишутся на аккаунт как сегодня.

**2. Форма.** Форма синхронизации — вызов `form_wrapper(action='/accounts/' ~ account_id ~
'/sync-groups')` без цели, обёрнутый элементом, который несёт условный `data-syncing`; два правила
`app/static/css/app.css` меняют селектор с формы на обёртку (`[data-acct-head] [data-syncing]
.btn__icon`), в том числе правило отключения анимации. Комментарий шаблона «Кнопка при выполнении НЕ
отключается» получает поколение: цель блокировки обёртки выключает кнопку ТОЛЬКО на время запроса;
повторную отправку по-прежнему отклоняет серверный guard, и зависший статус способа повторить не
отнимает.

**3. Числа.** Ключ снимается из `NOT_YET_CONVERTED`; 16 → 15 прогоном, летопись словами
(«синхронизация групп аккаунта»). Переходные и разметочные числа — прогоном.
  </action>

  <verify>
    <automated>uv run pytest tests/test_routes/test_sync_groups.py tests/test_pages/test_account_groups.py tests/test_pages/test_responsive_markup.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py tests/test_templates/test_htmx_markup_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункты 5 и 7 (раздел accounts): на /accounts/{id}/groups нажать «Синхронизировать всё» — экран обновляется без перезагрузки, иконка крутится при статусе syncing; Back не предлагает повторить POST; на /accounts после десятков действий (повторить, удалить с отменой, ожидание опроса) панели не множатся и открываются. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `awk '/^async def accounts_sync_groups/,/^@/' app/pages/accounts.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -c 'form\[data-syncing\]' app/static/css/app.css` возвращает `0`; `grep -n '\[data-syncing\]' app/static/css/app.css` печатает хотя бы одну строку.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 15$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_routes/test_sync_groups.py -q -k "releases_the_claim_on_every_exit"` — зелено.
  </acceptance_criteria>

  <done>
Синхронизация групп отвечает переходом на всех выходах, заявка освобождается на каждом, признак идущей
синхронизации оформлен как прежде.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Окно 51 — три входа модуля аккаунтов, 8 → 5</name>

  <files>
app/pages/accounts.py,
tests/test_pages/test_identifier_bounds.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py
  </files>

  <read_first>
- `app/pages/accounts.py` — сигнатуры `accounts_delete`, `accounts_retry_sync`, `accounts_sync_groups`
  (`account_id: IdPath`) и их ветки «аккаунт не найден».
- `app/pages/identifiers.py` — `PostIdPath`, `id_in_column`.
- `tests/test_pages/test_htmx_gates.py` — три записи модуля аккаунтов в `VALIDATION_REFUSAL_DIVERGENCES`.
- `tests/test_pages/test_identifier_bounds.py:916-1048` — матрица; `tests/test_pages/test_confirm_delete_transport.py` — случаи удаления аккаунта.
  </read_first>

  <behavior>
RED: строки матрицы для трёх входов ожидают ветку «аккаунт не найден» (`/accounts`) вместо 422; случай
модуля пар — повторная синхронизация с `account_id = 2147483648` (`LOCATION`, `/accounts`). Сегодня 422.
GREEN — ниже.
  </behavior>

  <action>
Три обработчика переходят на `PostIdPath`; первое использование `account_id` — аргумент
`id_in_column` (у удаления — после сверки источника, она идентификатора не читает); ложный результат
— ветка «аккаунт не найден» каждого (у синхронизации групп — ДО занятия заявки). Три записи модуля
аккаунтов снимаются из реестра окна 51; `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` 8 → 5 со строкой
летописи (D-07, план 11-10) и текстом покрасневшего правила.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_htmx_post_pairs.py tests/test_routes/test_sync_groups.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 5$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `grep -c 'entry="app/pages/accounts.py::POST' tests/test_pages/test_htmx_gates.py` возвращает `0`.
- `uv run pytest tests/test_pages/test_identifier_bounds.py -q -k "checked_before_its_first_use"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Три входа аккаунтов проверяют идентификатор первым использованием; реестр окна 51 — 5 записей.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | `account_id` пути у трёх маршрутов |
| обработчик → внешний мост / Telethon | синхронизация обращается к мессенджеру и может длиться долго |
| процесс → внутрипроцессная заявка | `_SYNC_IN_FLIGHT` освобождается только в `finally` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-28 | Denial of Service | заявка `_SYNC_IN_FLIGHT`, оставленная занятой ранним выходом | high | mitigate | Все выходы после занятия заявки внутри `try`; тест освобождения по четырём исходам на htmx-транспорте |
| T-11-29 | Information Disclosure | синхронизация чужого аккаунта | high | mitigate | Выборка с `MessengerAccount.user_id` не меняется; «не найден» уходит на `/accounts` на обоих транспортах (случаи модуля пар) |
| T-11-30 | Denial of Service | двойной запуск синхронизации | medium | mitigate | Серверные ступени guard (статус `syncing`, заявка) не трогаются; цель блокировки кнопки защитой не объявляется (PAY-02) |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Оба действия синхронизации отвечают `HX-Location` на обоих транспортах; заявка освобождается на каждом выходе.
2. Четыре формы идут через обёртку без цели подмены; в `accounts/*` новых свопов нет.
3. `NOT_YET_CONVERTED_COUNT = 15`, `VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 5`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-10-SUMMARY.md` when done.
</output>
