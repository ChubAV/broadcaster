---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 10
type: execute
wave: 10
depends_on: ["11-09"]
files_modified:
  - app/templates/profile.html
  - app/templates/includes/profile_settings.html
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_htmx_gates.py
autonomous: true
requirements: [FORM-10, FORM-08]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: разметка формы из задачи 2 плана 11-06 ревизии 1 и задача 2 плана 11-05 ревизии 1 (перечень FORM-10).
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Форма профиля отправляется через `form_wrapper(action='/profile', target='#profile-settings', swap='innerHTML')`, страница держит постоянную обёртку `id=\"profile-settings\"` вокруг включаемого шаблона настроек; успех и ошибка поля подменяют ОДНУ цель — содержимое обёртки (D-12 Фазы 9, Находка B)"
    - "Заголовки перенацеливания и пересвопа применяются только поимённо: число их вхождений в `app/` равно числу записей перечня, перечень пуст, число объявлено нулём, и отрицательный контроль с синтетической записью заголовка краснеет (FORM-10, D-16)"
    - "Разметочные числа поставлены прогоном, каждое с летописью"
    - statement: "В браузере сохранение профиля показывает плашку «сохранено» в области шелла без перезагрузки; подложенный через DevTools неверный пояс перерисовывает форму с ошибкой без плашки аварии"
      verification: backstop
  artifacts:
    - "app/templates/profile.html — обёртка id profile-settings"
    - "app/templates/includes/profile_settings.html — форма через form_wrapper"
    - "tests/test_pages/test_htmx_gates.py — RETARGET_RESWAP_USES, RETARGET_RESWAP_USES_DECLARED, test_every_retarget_or_reswap_use_is_declared, test_the_number_of_retarget_or_reswap_uses_is_the_declared_one, test_control_an_undeclared_retarget_header_reddens_the_registry"
  key_links:
    - "форма профиля (hx-target=#profile-settings) → profile_post (план 11-09): 200 и 422 подменяют одну цель; ломается — ошибка поля без перенацеливания уезжает не туда, и перечень FORM-10 перестаёт быть пустым по основанию"
  prohibitions:
    - statement: "перенацеливание и пересвоп заголовками не применяются как умолчание для 422 (D-16)"
      status: flagged-unverified
      verification: none
    - statement: "атрибут записи адреса в историю на форму профиля не ставится (D-13)"
      status: flagged-unverified
      verification: none
---

<objective>
Форма профиля становится формой htmx над целью, общей для успеха и ошибки поля, и это же — основание, по
которому перечень заголовков перенацеливания/пересвопа заводится с нулём записей (FORM-10, D-16).

Purpose: переведённый планом 11-09 обработчик до этого плана исполнял фрагментные ветки только в тестах;
пользователь получает сохранение без перезагрузки и ошибку поля у формы. Цель 422 совпадает с целью
успеха — перенацеливать нечего, и перечень D-16 стережёт, чтобы так и оставалось.

Output: обёртка `#profile-settings`, форма на `form_wrapper`, перечень FORM-10 с зубами.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-RESEARCH.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-09-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: id `profile-settings`; `RETARGET_RESWAP_USES`,
`RETARGET_RESWAP_USES_DECLARED`, `test_every_retarget_or_reswap_use_is_declared`,
`test_the_number_of_retarget_or_reswap_uses_is_the_declared_one`,
`test_control_an_undeclared_retarget_header_reddens_the_registry`.
</artifacts_this_phase_produces>

<edge_coverage_fallback>
## Spec-less edge probe — явное допущение

- **FORM-10 — FLAGGED ASSUMPTION:** зонд краёв строку не классифицировал. Край «применение
  заголовка без записи» закрыт отрицательным контролем перечня; машинного перечня краёв нет.
</edge_coverage_fallback>

<scope_note>
## Размер плана (ревизия 2)

Проекция 65000 в пределах смарт-зоны. Разметка формы профиля отделена от коммита свопа (план 11-09),
потому что в одном плане с неделимым коммитом свопа она дала бы 12 файлов. Перечень FORM-10 перенесён сюда
из плана смягчения T-07-13 (ревизия 1: 11-05, задача 2): его нулевое основание — «цель 422 совпадает с
целью успеха» — становится фактом разметки именно этим планом, а план смягчения остаётся отдельным
планом до свопа, как того требует исправление ревизии 1.
</scope_note>

<tasks>

<task type="auto">
  <name>Задача 1: Форма профиля на обёртке — успех и ошибка поля подменяют одну цель</name>

  <files>
app/templates/profile.html,
app/templates/includes/profile_settings.html,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/profile.html` и `app/templates/includes/profile_settings.html` (план 11-08) — место
  включения и форма.
- `app/templates/components/form_wrapper.html:138-148`;
  `app/templates/account_groups/includes/group_row.html:193-215` — образец блочного вызова обёртки.
- `app/pages/profile.py` — `profile_post` после плана 11-09 (фрагмент успеха с кодом, 422 с фрагментом).
- `tests/test_templates/test_htmx_markup_gates.py:3969` (долгоживущая область заменяется содержимым),
  `:4029-4200` (цели свопа), `:4302` (одна величина в двух ролях).
  </read_first>

  <action>
`profile.html` включает шаблон настроек внутри постоянной обёртки `<div id="profile-settings">`
(долгоживущая область, её СОДЕРЖИМОЕ подменяется — D-12 Фазы 9). Форма во включаемом шаблоне — блочный
вызов `form_wrapper(action='/profile', target='#profile-settings', swap='innerHTML')` с прежними
`select_field(name="timezone", …, selected=selected, id='timezone')` и кнопкой «Сохранить». Шапка
включаемого шаблона получает строку: цель успеха и цель ошибки поля — одна (Находка B, D-06), поэтому
заголовки перенацеливания не нужны. Разметочные числа — прогоном, каждое с летописью «Фаза 11, план
11-10» и дословным текстом отказа.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_profile.py tests/test_pages/test_impersonation.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы (профиль): на /profile сменить часовой пояс и нажать «Сохранить» — плашка «сохранено» в области шелла без перезагрузки, выбранное значение в форме; подложить через DevTools неверное значение пояса и отправить — форма перерисована с ошибкой «Неверный часовой пояс» и введённым значением, плашки аварии нет. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -n 'id="profile-settings"' app/templates/profile.html` печатает одну строку.
- `grep -c "target='#profile-settings'" app/templates/includes/profile_settings.html` возвращает `1`.
- `grep -c 'hx-push-url' app/templates/includes/profile_settings.html` возвращает `0`.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "long_lived or swap_target or two_roles"` — зелено.
  </acceptance_criteria>

  <done>
Форма профиля отправляется через обёртку; успех и ошибка поля подменяют содержимое одной обёртки.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Перечень перенацеливания и пересвопа — ноль записей, число и зубы</name>

  <files>
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `tests/test_templates/test_htmx_markup_gates.py:2760-2870` и `:3178-3230` — форма перечня
  `DISABLED_ELT_EXCEPTIONS`: запись с обоснованием, число, правило числа.
- `tests/test_pages/test_htmx_gates.py:1531-1620` — `REQUEST_OBJECT_NAMES`, `_HeaderWrite`, обход
  записей заголовков `HX-*` (как разборщик видит ключ заголовка: подстрочная запись и словарь с
  константой); `:2102-2220` — форма отрицательных контролей на `tmp_path`.
- `11-CONTEXT.md` D-16; `11-RESEARCH.md` §Находка B, «Следствие для D-16/FORM-10».
  </read_first>

  <behavior>
RED: `test_control_an_undeclared_retarget_header_reddens_the_registry` — синтетический модуль с записью
заголовка перенацеливания в ответ даёт правилу жалобу с именем `модуль::функция`; правило на
настоящем дереве зелено при пустом перечне и числе ноль; антивакуум — сканер на синтетическом
модуле НАХОДИТ вхождение (иначе пустой перечень зеленел бы вакуумно).
GREEN — перечень и правила.
  </behavior>

  <action>
В `test_htmx_gates.py` заводятся (D-16): `RETARGET_RESWAP_USES: dict[str, str] = {}` (ключ
`app/pages/<файл>.py::<функция>`, значение — обоснование применения) и
`RETARGET_RESWAP_USES_DECLARED = 0` с летописью «0, Фаза 11, план 11-10: цель 422 совпадает с целью
успеха (Находка B; профиль — план 11-10, мастер MAX — план 11-18), ветка «было ноль» создания решена
переходом (D-05)». Сканер — по дереву разбора `app/**/*.py`: строковые константы, равные именам двух
заголовков, в любой позиции (ключ словаря заголовков, подстрочная запись, аргумент), собираются в
множество `модуль::функция`; шаблоны `app/templates` с вырезанными комментариями Jinja проверяются на
отсутствие этих имён. `test_every_retarget_or_reswap_use_is_declared` — найденное равно ключам
перечня, у каждой записи непустое обоснование; `test_the_number_of_retarget_or_reswap_uses_is_the_declared_one`
— длина перечня равна объявленному; контроль — по §behavior. Докстринг: D-16 — каждое применение
делает поведение невидимым в шаблоне, поэтому перечень, а не практика; Находка B — заголовки своп на
422 не включают.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -k "retarget"</automated>
    <fails_when>non-zero exit, or "failed" in the pytest summary line, or "deselected" with no "passed"</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^RETARGET_RESWAP_USES_DECLARED = 0$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -k "retarget" --collect-only | tail -1` показывает не меньше 3 собранных тестов.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q` — зелено целиком.
  </acceptance_criteria>

  <done>
Применение заголовков перенацеливания и пересвопа без записи краснит прогон с именем; перечень пуст
и объявлен нулём, пустота не вакуумна.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ 422 → DOM | с плана 11-09 тело авторского 422 подменяет цель запроса htmx; этим планом форма профиля становится таким запросом |
| слой ответа → разметка | заголовок перенацеливания подменил бы цель, объявленную шаблоном |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-16 | Tampering | перенацеливание ответа заголовком без записи | medium | mitigate | Перечень FORM-10 с числом 0 и отрицательным контролем; антивакуум сканера на синтетическом модуле |
| T-11-13 | Tampering (XSS) | эхо значения пояса, подменяемое в обёртку | medium | mitigate | Эхо — только автоэкранирование `select_field` (тест плана 11-09 с `"><script>alert(1)</script>` на обоих транспортах в verify через `test_profile.py`) |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_profile.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Форма профиля на обёртке; успех и 422 подменяют содержимое `#profile-settings`.
2. Перечень перенацеливания/пересвопа — 0 записей с зубами.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-10-SUMMARY.md` when done.
</output>
