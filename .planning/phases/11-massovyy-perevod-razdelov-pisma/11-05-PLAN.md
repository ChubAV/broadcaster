---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - app/pages/htmx.py
  - app/main.py
  - app/pages/profile.py
  - app/templates/profile.html
  - app/templates/includes/profile_settings.html
  - app/templates/includes/htmx_config.html
  - tests/test_pages/test_htmx_validation_sink.py
  - tests/test_pages/test_htmx_response_layer.py
  - tests/test_pages/test_htmx_response_contract.py
  - tests/test_pages/test_shell.py
  - tests/test_pages/test_profile.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
  - .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md
autonomous: true
requirements: [FORM-08, FORM-10, FORM-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  tokens: 190000
  raw_tokens: 190000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ни один запрос htmx не получает тела отказа валидации фреймворка: обработчик `RequestValidationError` на пути htmx отвечает пустым 400 (правило `[45]..` не свопает, плашка отказа поднимается), а на пути без htmx отдаёт прежний ответ фреймворка — контракт 422 JSON-API `app/routes/` не меняется (T-07-13, D-07)"
    - "Смягчение T-07-13 приземляется ОТДЕЛЬНЫМ коммитом ДО возврата свопа правилу 422: ни на одном коммите фазы своп 422 не включён без обработчика — порядок задач 1 → 2 несущий"
    - "Правило 422 блока конфигурации несёт `\"swap\": true` — значение, а не седьмой ключ; порядок пяти правил и шесть ключей не тронуты (Находка B, D-03 Фазы 7); своп возвращён ОДНОВРЕМЕННО с первым маршрутом, отдающим 422 с авторским фрагментом (профиль)"
    - "Сохранение профиля на htmx отвечает 200 и содержимым `#profile-settings` (`innerHTML`) плюс внеполосным блоком уведомления `profile_saved` — ПЕРВОЕ поведение ветки «код исхода на фрагменте»; без htmx — прежний 302 `/profile?notice=profile_saved` (D-02)"
    - "Неверный или отсутствующий часовой пояс отвечает 422 на обоих транспортах: htmx — фрагмент формы с текстом «Неверный часовой пояс» и выбором, возвращённым эхом через автоэкранирование, без `<!DOCTYPE`; без htmx — полная страница профиля со статусом 422 и тем же текстом (FORM-08, D-06)"
    - "Литерал статуса 422 записан в приложении в одном месте — выходе `respond_field_error` слоя ответа; `SERVER_SIDE_VALIDATION_RESPONSES` поставлен прогоном с летописью; утверждение `swap is False` перевёрнуто поколением, литерал правила в `test_shell.py` приведён"
    - "Заголовки перенацеливания и пересвопа применяются только поимённо: число их вхождений в `app/` равно числу записей перечня, перечень пуст, число объявлено нулём, и отрицательный контроль с синтетической записью заголовка краснеет (FORM-10, D-16)"
    - "`NOT_YET_CONVERTED_COUNT` 21 → 20, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 8 → 9 — прогоном"
    - statement: "В браузере сохранение профиля показывает плашку «сохранено» в области шелла без перезагрузки; подложенный через DevTools неверный пояс перерисовывает форму с ошибкой без плашки аварии; запрос htmx с испорченным идентификатором поднимает плашку «Действие не выполнено» и ничего не вклеивает в страницу"
      verification: backstop
  artifacts:
    - "app/pages/htmx.py — malformed_request_response, respond_field_error; докстринги модуля и respond с поколениями"
    - "app/main.py — регистрация обработчика RequestValidationError"
    - "app/pages/profile.py — profile_post на respond() и respond_field_error()"
    - "app/templates/includes/profile_settings.html (НОВЫЙ) — ошибка поля + форма через form_wrapper"
    - "app/templates/includes/htmx_config.html — правило 422 со свопом"
    - "tests/test_pages/test_htmx_validation_sink.py (НОВЫЙ) — VALIDATION_SINK_ROUTES_DECLARED и обход"
    - "tests/test_pages/test_htmx_gates.py — RETARGET_RESWAP_USES, RETARGET_RESWAP_USES_DECLARED, test_every_retarget_or_reswap_use_is_declared, test_the_number_of_retarget_or_reswap_uses_is_the_declared_one, test_control_an_undeclared_retarget_header_reddens_the_registry"
  key_links:
    - "обработчик RequestValidationError (main.py) → malformed_request_response (htmx.py) → пустой 400 на htmx: ломается — умолчание фреймворка с эхо ввода свопается в DOM через правило 422"
    - "правило 422 swap:true ↔ respond_field_error (авторский фрагмент): ломается — либо мёртвая кнопка без перерисовки, либо сток T-07-13"
    - "includes/profile_settings.html — один источник формы для страницы и обоих фрагментов: ломается — страница и фрагмент расходятся"
  prohibitions:
    - statement: "нового JS не заводится: ни слушателя htmx:beforeSwap, ни расширения response-targets, ни седьмого ключа конфигурации (Находка B, D-03 Фазы 7)"
      status: flagged-unverified
      verification: none
    - statement: "перенацеливание и пересвоп заголовками не применяются как умолчание для 422 (D-16)"
      status: flagged-unverified
      verification: none
    - statement: "новых проверок ввода, кроме существующей проверки часового пояса, не заводится (D-06)"
      status: flagged-unverified
      verification: none
    - statement: "история записи 07-SECURITY.md выше не переписывается — дописывается датированное добавление"
      status: flagged-unverified
      verification: none
    - statement: "исключение 422 в обработчике плашки отказа (`htmx_error_banner.html`) не трогается"
      status: flagged-unverified
      verification: none
---

<objective>
Главный риск фазы (RESEARCH Находка B) закрывается в одном плане в несущем порядке: сначала
смягчение стока T-07-13 на уровне приложения, затем — возврат свопа правилу 422 одновременно с
первым маршрутом, отдающим 422 с авторским фрагментом (сохранение профиля), и заведение перечня
заголовков перенацеливания/пересвопа числом ноль (FORM-10).

Purpose: в вендоренном htmx 2.0.10 решение «свопать ли» берётся только из `responseHandling`;
`HX-Retarget`/`HX-Reswap` его не меняют. Без `"swap": true` FORM-08 невыполним без нового JS; со
свопом, но без смягчения, тело 422 умолчания фреймворка с эхо ввода становится стоком исполнения
скрипта. Механизм в пределах усмотрения D-06 («собственным правилом… не расширяя шесть ключей»):
меняется значение, не ключ.

Output: обработчик отказа валидации, выход `respond_field_error`, профиль на слое ответа, своп 422,
обход стока, перечень FORM-10, добавление к `07-SECURITY.md`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-RESEARCH.md
@.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `malformed_request_response`, `respond_field_error`
(`app/pages/htmx.py`); регистрация `exception_handler(RequestValidationError)` в `app/main.py`;
`app/templates/includes/profile_settings.html`; id `profile-settings`;
`tests/test_pages/test_htmx_validation_sink.py` с `VALIDATION_SINK_ROUTES_DECLARED`,
`test_no_htmx_request_receives_a_framework_validation_body`,
`test_a_malformed_htmx_request_gets_an_empty_bad_request`,
`test_the_json_api_keeps_its_validation_contract`,
`test_control_without_the_handler_the_traversal_names_an_offender`,
`test_the_number_of_traversed_routes_is_the_declared_one`; `RETARGET_RESWAP_USES`,
`RETARGET_RESWAP_USES_DECLARED`, `test_every_retarget_or_reswap_use_is_declared`,
`test_the_number_of_retarget_or_reswap_uses_is_the_declared_one`,
`test_control_an_undeclared_retarget_header_reddens_the_registry`; тесты профиля
`test_profile_save_over_htmx_returns_the_form_and_the_notice`,
`test_an_invalid_timezone_answers_422_with_the_echo_on_both_transports`.
</artifacts_this_phase_produces>

<edge_coverage_fallback>
## Spec-less edge probe — явные допущения

- **FORM-08 — FLAGGED ASSUMPTION:** зонд краёв строку не классифицировал. Края ошибки заполнения
  (отсутствующее поле, враждебное значение в эхо, путь без htmx, запрос с испорченным
  идентификатором) покрыты явными `truths` и тестами этого плана; машинного перечня краёв нет.
- **FORM-10 — FLAGGED ASSUMPTION:** зонд краёв строку не классифицировал. Край «применение
  заголовка без записи» закрыт отрицательным контролем перечня; машинного перечня краёв нет.
</edge_coverage_fallback>

<decision_reading>
## Прочтение D-06 и Находки B (записывается, чтобы верификатор не искал заголовков)

D-06 назвал два возможных механизма перерисовки на 422: «точечным `HX-Retarget`/`HX-Reswap` либо
собственным правилом, выбор за планировщиком в рамках D-16», а §Claude's Discretion — «иное, не
расширяющее шесть ключей конфигурации». Находка B измерила по вендоренному файлу, что первый
механизм своп не включает. Выбран второй — значение `"swap": true` у существующего правила 422.
Цель 422 совпадает с целью успеха (`#profile-settings`, в плане 11-10 — `#max-connect-step`),
поэтому перечень D-16 заводится с нулём записей.
</decision_reading>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Смягчение T-07-13 — отказ валидации на пути htmx пуст и не свопается (своп 422 ещё выключен)</name>

  <files>
app/pages/htmx.py,
app/main.py,
tests/test_pages/test_htmx_validation_sink.py (NEW),
tests/test_pages/test_htmx_response_layer.py,
tests/test_pages/test_htmx_response_contract.py
  </files>

  <read_first>
- `app/main.py:200-250` — регистрация `HtmxRefusal` и доменных обработчиков, форма комментариев.
- `app/pages/htmx.py:1-110` — докстринг модуля («ДВА ВЫХОДА»), `is_htmx`; `:154-170` —
  `location_response` (форма сборки).
- `tests/test_pages/test_htmx_response_contract.py:40-190` — `VALIDATION_EXCEPTIONS`,
  `SERVER_SIDE_VALIDATION_RESPONSES = 0` и его летопись («что обязан сделать тот, кто увеличит»),
  `_status_code_literals` (считает и регистрацию обработчика исключения).
- `.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md:155-197` —
  запись T-07-13.
- `app/templates/includes/htmx_error_banner.html:300-325` — плашка отказа и исключение 422.
- `tests/test_pages/test_account_groups.py:895-910` — утверждения 422 на GET-курсоре БЕЗ htmx
  (обязаны остаться зелёными).
- `tests/test_routes/test_schedules_api_identifier_bounds.py` — контракт JSON-API.
- `tests/conftest.py:60-100` — `client`, `authed_client`, `htmx_client` складываются сигнатурой.
  </read_first>

  <behavior>
RED:
1. `test_a_malformed_htmx_request_gets_an_empty_bad_request`: авторизованный запрос htmx GET
   `/accounts/not-a-number/groups/partial` → 400, `content == b""`; POST `/schedules/new` без поля
   `ad_id` под htmx → 400, тело пусто. Сегодня 422 JSON — красный.
2. `test_no_htmx_request_receives_a_framework_validation_body` — параметризован по маршрутам
   приложения, у которых хотя бы один параметр пути объявлен целым
   (`route.dependant.path_params`); каждый местозаполнитель заменён токеном `not-a-number`, метод
   GET либо POST (пустое тело); утверждение: ответ НЕ является одновременно статусом 422 и типом
   `application/json`.
3. `test_the_json_api_keeps_its_validation_contract`: тот же испорченный вход на маршрут JSON-API
   без заголовка htmx → 422 JSON.
4. `test_control_without_the_handler_the_traversal_names_an_offender`: приложение, собранное
   `create_app`, с изъятым из `exception_handlers` обработчиком `RequestValidationError` — помощник
   обхода называет хотя бы один маршрут-нарушитель.
GREEN — обработчик.
  </behavior>

  <action>
**1. `malformed_request_response(request, exc)` в `app/pages/htmx.py`.** Асинхронная функция: без
признака htmx — `await` стандартного обработчика FastAPI `request_validation_exception_handler`
(импорт из `fastapi.exception_handlers`), то есть ответ фреймворка байт-в-байт прежний; с признаком —
`Response(status_code=400)` без тела. Докстринг: T-07-13 (своп на 422 — сток исполнения скрипта, пока
422 может быть умолчанием фреймворка с эхо ввода; `allowScriptTags`/`allowEval` — умолчания артефакта);
почему 400, а не 422 (с плана 11-05 правило 422 свопает, а `[45]..` — нет, и плашка отказа
поднимается — принятая цена: подделанный запрос получает общий совет); почему тело пустое (нечему
вклеиться); что путь без htmx и JSON-API не меняются (клиенты JSON-API признака не шлют). Чтение
признака — только через `is_htmx` (`HX_HEADER_READS` = 1).

**2. Регистрация в `app/main.py`.** `@app.exception_handler(RequestValidationError)` рядом с
`HtmxRefusal`, делегирующий в `malformed_request_response`; комментарий по форме соседних:
почему обработчик существует и что он не меняет для пути без htmx.

**3. Докстринг модуля `htmx.py`.** Перечень выходов пополняется третьим видом — «ответ приложения
на отказ валидации фреймворка» — с поколением: прежняя редакция «ДВА ВЫХОДА» не стирается.

**4. Счёт мест 422.** `SERVER_SIDE_VALIDATION_RESPONSES` в `test_htmx_response_contract.py` 0 → 1
прогоном (гейт считает регистрацию обработчика исключения местом); летопись: «Фаза 11, план 11-05,
задача 1 — регистрация обработчика `RequestValidationError`; его ветвь htmx отвечает пустым 400, не
422, ветвь без htmx — ответ фреймворка, до свопа не доходящий (путь без htmx не свопает); правило
422 в этом коммите ещё без свопа».

**5. Модуль обхода `tests/test_pages/test_htmx_validation_sink.py` (НОВЫЙ)** — по §behavior. Число
обойдённых пар «маршрут × метод» объявлено `VALIDATION_SINK_ROUTES_DECLARED` и ставится прогоном
(пустой обход краснеет — антивакуум). Докстринг называет границу: обходятся маршруты с целым
параметром пути, потому что только они гарантированно отказывают ДО тела обработчика (строковый
параметр пропустил бы токен в обработчик и исполнил бы действие). Тесты слоя ответа в
`test_htmx_response_layer.py`: вызов `malformed_request_response` с признаком и без — статус, тело,
тип.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_validation_sink.py tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_htmx_response_contract.py tests/test_pages/test_account_groups.py tests/test_routes/test_schedules_api_identifier_bounds.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or tests/test_pages/test_htmx_validation_sink.py collects no tests</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -n 'exception_handler(RequestValidationError)' app/main.py` печатает одну строку.
- `grep -nE '^async def malformed_request_response' app/pages/htmx.py` печатает одну строку.
- Правило 422 в этом коммите ещё без свопа: `grep -n '"code":"422", "swap": false, "error": true' app/templates/includes/htmx_config.html` печатает одну строку.
- `grep -nE '^SERVER_SIDE_VALIDATION_RESPONSES = 1$' tests/test_pages/test_htmx_response_contract.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_htmx_validation_sink.py -q -k "control_without_the_handler or empty_bad_request or json_api_keeps"` — зелено.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -k "read_in_exactly_one_place"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Отказ валидации фреймворка на пути htmx — пустой 400 на каждом маршруте с целым параметром пути;
путь без htmx и JSON-API прежние; смягчение закоммичено до возврата свопа.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Первый авторский 422 — профиль на слое ответа и своп правилу 422 одним коммитом</name>

  <files>
app/pages/htmx.py,
app/pages/profile.py,
app/templates/profile.html,
app/templates/includes/profile_settings.html (NEW),
app/templates/includes/htmx_config.html,
tests/test_pages/test_profile.py,
tests/test_pages/test_htmx_response_contract.py,
tests/test_pages/test_shell.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md
  </files>

  <read_first>
- `app/pages/profile.py` целиком — `profile_post`: обязательное поле пояса без умолчания, ветка
  успеха с кодом `PROFILE_SAVED`, ветка ошибки полной страницей со статусом 400.
- `app/templates/profile.html` целиком — алерт ошибки над картой, форма `method=post action=/profile`,
  `select_field(... selected=user.timezone ...)`.
- `app/templates/components/field.html:48` — сигнатура `select_field(name, label, options, selected,
  id, required, hint, error, disabled)`.
- `app/pages/htmx.py:285-354` (`_glue_notice`), `:394-485` (`respond`, абзац «КОД ИСХОДА СПОСОБЕН
  ДОЕХАТЬ И НА ВЕТКЕ ФРАГМЕНТА… НИ ОДИН ОБРАБОТЧИК»).
- `tests/test_pages/test_htmx_response_contract.py:193-270` — `test_validation_rule_carries_both_swap_and_error`
  и его докстринг-предписание.
- `tests/test_pages/test_shell.py:1270-1310` — `HTMX_CONFIG` с правилом 422 и комментарий.
- `tests/test_pages/test_notices_surface.py:117-160` — как утверждается приклейка кода к фрагменту.
- `tests/test_pages/test_profile.py`, `tests/test_pages/test_impersonation.py` (запрет правки
  профиля под чужой личностью — не сломать).
- Результат задачи 1.
  </read_first>

  <behavior>
RED:
1. `test_profile_save_over_htmx_returns_the_form_and_the_notice`: htmx POST `/profile` с годным
   поясом → 200, тело несёт форму `action="/profile"`, выбранный пояс с `selected`, внеполосный блок
   уведомления с текстом записи реестра `profile_saved`; нет `<!DOCTYPE`. Без htmx → 302
   `/profile?notice=profile_saved`.
2. `test_an_invalid_timezone_answers_422_with_the_echo_on_both_transports`: пояс
   `"><script>alert(1)</script>` под htmx → 422, текст «Неверный часовой пояс», нет `<!DOCTYPE`,
   подстрока `<script>alert(1)` отсутствует (экранирована); без htmx → 422, `<!DOCTYPE` есть, текст
   ошибки есть; запрос без поля пояса → тот же ответ 422 (не отказ фреймворка).
3. `test_validation_rule_carries_both_swap_and_error` переписан на `swap is True` — краснеет, пока
   правило не приведено.
GREEN — всё ниже ОДНИМ коммитом.
  </behavior>

  <action>
**1. Выход `respond_field_error(request, *, page, fragment)` в `htmx.py`.** Оба аргумента —
нульарные async-сборщики, как у `respond`. Без признака htmx ожидается `page()` (полный документ),
с признаком — `fragment()`; тело выбранного ответа возвращается в свежем `HTMLResponse` со
статусом 422, и литерал этого статуса записан в приложении ОДИН раз — здесь. Докстринг: ошибка
ПОЛЯ рисуется у формы (D-12 Фазы 8), статус 422 на обоих транспортах, эхо введённого — только через
автоэкранирование шаблона; своп правилом 422 (Находка B), а не заголовками (D-16); путь деградации
здесь — страница, а не перенаправление, потому что ошибка поля не есть исход действия.

**2. Форма профиля.** Содержимое карточки настроек — алерт ошибки поля и форма — выносится в
`app/templates/includes/profile_settings.html` (контекст: `timezone_choices`, `selected`, `error`);
`profile.html` включает его внутри постоянной обёртки `<div id="profile-settings">`, верхний алерт
страницы снимается (ошибка теперь у формы). Форма — блочный вызов `form_wrapper(action='/profile',
target='#profile-settings', swap='innerHTML')` с `select_field(name="timezone", …,
selected=selected, id='timezone')` и кнопкой «Сохранить».

**3. `profile_post`.** Поле пояса получает умолчание пустой строки (отсутствующее поле идёт веткой
ошибки обработчика, а не отказом фреймворка). «Нет сессии» → `respond(request, redirect="/login")`.
Годный пояс → запись → `respond(request, redirect="/profile", notice=notices.PROFILE_SAVED,
fragment=…)`, сборщик рендерит `includes/profile_settings.html` с сохранённым поясом без ошибки.
Негодный → `respond_field_error(request, page=…, fragment=…)`: страница — `profile.html` с `error`
«Неверный часовой пояс» (текст дословно) и `selected` = присланное значение; фрагмент — тот же
включаемый шаблон с тем же контекстом. Зависимость запрета правки под чужой личностью не трогается.

**4. Своп правилу 422 (Находка B).** В `includes/htmx_config.html` у правила
`{"code":"422", …, "error": true}` значение своп становится `true`; порядок правил и ключи не
меняются. `tests/test_pages/test_shell.py`: строка правила 422 в `HTMX_CONFIG` — `True`,
комментарий над константой получает поколение («со swap: False правило совпадало с [45]..; с плана
11-05 своп возвращён вместе с первым авторским маршрутом»).

**5. Гейты правила.** `test_validation_rule_carries_both_swap_and_error` утверждает своп и признак
ошибки; докстринг дописывает ТРЕТЬЕ поколение (своп возвращён 11-05 одновременно с первым
маршрутом, отдающим 422 с авторским фрагментом; смягчение заменено обработчиком задачи 1 и обходом
`test_htmx_validation_sink.py`), прежние поколения не стираются. `SERVER_SIDE_VALIDATION_RESPONSES`
— прогоном (ожидаемо 1 → 2: литерал выхода `respond_field_error`), летопись.

**6. `07-SECURITY.md`.** В раздел `07-05/T-07-13` дописывается датированное добавление «Фаза 11,
план 11-05»: своп возвращён вместе с первым авторским маршрутом (профиль); прежнее смягчение
«swap снят» заменено смягчением «на пути htmx отказ валидации фреймворка — пустой 400»
(`malformed_request_response`, обход по маршрутам); запись выше не правится.

**7. Докстринг `respond`.** Абзац о коде исхода на ветке фрагмента получает поколение: первым
обработчиком, подающим фрагмент и код, стало сохранение профиля (11-05); прежняя формулировка «НИ
ОДИН ОБРАБОТЧИК» остаётся названной опровергнутой.

**8. Числа.** Ключ `profile_post` снимается из `NOT_YET_CONVERTED` и добавляется в
`FRAGMENT_RESPONSE_HANDLERS`; 21 → 20 и 8 → 9 прогоном, летопись словами («сохранение часового
пояса в профиле»). Случаи модуля пар: успех (`FRAGMENT`, метка `id="timezone"`), нет сессии
(`LOCATION`, `/login`).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_profile.py tests/test_pages/test_htmx_response_contract.py tests/test_pages/test_shell.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_notices_surface.py tests/test_pages/test_impersonation.py tests/test_pages/test_htmx_validation_sink.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>non-zero exit, or "failed" in the pytest summary line (запись 07-SECURITY.md правится этой задачей)</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -n '"code":"422", "swap": true, "error": true' app/templates/includes/htmx_config.html` печатает одну строку.
- `grep -rn 'status_code=422' app/ | cut -d: -f1 | sort -u` печатает ровно `app/pages/htmx.py`.
- `awk '/^async def profile_post/,0' app/pages/profile.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -c 'Form(\.\.\.)' app/pages/profile.py` возвращает `0`.
- `grep -n 'id="profile-settings"' app/templates/profile.html` печатает одну строку.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 20$' tests/test_pages/test_htmx_gates.py` печатает одну строку; `grep -nE '^FRAGMENT_RESPONSE_HANDLERS_DECLARED = 9$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `grep -n 'Фаза 11' .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md` печатает хотя бы одну строку.
- `uv run pytest tests/test_pages/test_profile.py -q -k "returns_the_form_and_the_notice or answers_422_with_the_echo"` — зелено, 2 теста.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q` — зелено (разметочные числа поставлены прогоном).
  </acceptance_criteria>

  <done>
Профиль отвечает на htmx фрагментом формы с кодом исхода, на ошибку поля — 422 с эхом на обоих
транспортах; правило 422 свопает; все три запертых места (утверждение, литерал правила, счёт мест)
и запись безопасности приведены одним коммитом.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Перечень перенацеливания и пересвопа — ноль записей, число и зубы</name>

  <files>
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `tests/test_templates/test_htmx_markup_gates.py:2760-2870` и `:3178-3230` — форма перечня
  `DISABLED_ELT_EXCEPTIONS`: запись с обоснованием, число, правило числа.
- `tests/test_pages/test_htmx_gates.py:1531-1620` — `REQUEST_OBJECT_NAMES`, `_HeaderWrite`, обход
  записей заголовков `HX-*` (как разборщик видит ключ заголовка: подстрочная запись и словарь с
  константой); `:2102-2220` — форма отрицательных контролей на `tmp_path`.
- `11-CONTEXT.md` D-16; `11-RESEARCH.md` §Находка B, «Следствие для D-16/FORM-10».
  </read_first>

  <behavior>
RED: `test_control_an_undeclared_retarget_header_reddens_the_registry` — синтетический модуль с записью
заголовка перенацеливания в ответ даёт правилу жалобу с именем `модуль::функция`; правило на
настоящем дереве зелено при пустом перечне и числе ноль; антивакуум — сканер на синтетическом
модуле НАХОДИТ вхождение (иначе пустой перечень зеленел бы вакуумно).
GREEN — перечень и правила.
  </behavior>

  <action>
В `test_htmx_gates.py` заводятся: `RETARGET_RESWAP_USES: dict[str, str] = {}` (ключ
`app/pages/<файл>.py::<функция>`, значение — обоснование применения) и
`RETARGET_RESWAP_USES_DECLARED = 0` с летописью «0, Фаза 11, план 11-05: цель 422 совпадает с целью
успеха (Находка B), ветка «было ноль» создания решена переходом (D-05)». Сканер — по дереву разбора
`app/**/*.py`: строковые константы, равные именам двух заголовков, в любой позиции (ключ словаря
заголовков, подстрочная запись, аргумент), собираются в множество `модуль::функция`; шаблоны
`app/templates` с вырезанными комментариями Jinja проверяются на отсутствие этих имён.
`test_every_retarget_or_reswap_use_is_declared` — найденное равно ключам перечня, у каждой записи
непустое обоснование; `test_the_number_of_retarget_or_reswap_uses_is_the_declared_one` — длина
перечня равна объявленному; контроль — по §behavior. Докстринг: D-16 — каждое применение делает
поведение невидимым в шаблоне, поэтому перечень, а не практика; Находка B — заголовки своп на
422 не включают.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -k "retarget"</automated>
    <fails_when>non-zero exit, or "failed" in the pytest summary line, or "deselected" with no "passed"</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^RETARGET_RESWAP_USES_DECLARED = 0$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -k "retarget" --collect-only | tail -1` показывает не меньше 3 собранных тестов.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q` — зелено целиком.
  </acceptance_criteria>

  <done>
Применение заголовков перенацеливания и пересвопа без записи краснит прогон с именем; перечень пуст
и объявлен нулём, пустота не вакуумна.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | значения параметров пути, строки запроса и тела формы, в том числе враждебные, приходят на любой маршрут |
| ответ 422 → DOM | с этого плана тело ответа 422 подменяет цель запроса htmx |
| страничный слой → JSON-API | обработчик исключения регистрируется на приложении целиком |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Information Disclosure / XSS | своп тела 422 умолчания фреймворка с эхо ввода (переоткрыт возвратом свопа) | high | mitigate | Задача 1 (ОТДЕЛЬНЫЙ коммит до свопа): `malformed_request_response` отвечает на htmx пустым 400; `test_no_htmx_request_receives_a_framework_validation_body` обходит все маршруты с целым параметром пути; единственный источник 422 в приложении — `respond_field_error` с авторским фрагментом (утверждается `grep status_code=422`); `07-SECURITY.md` получает добавление |
| T-11-13 | Tampering (XSS) | эхо присланного пояса в фрагменте и странице 422 | medium | mitigate | Только автоэкранирование `select_field`; тест с `"><script>alert(1)</script>` утверждает отсутствие неэкранированной подстроки на обоих транспортах |
| T-11-14 | Repudiation | подделанный запрос htmx получает общую плашку «Действие не выполнено» | low | accept | Недостижимо из интерфейса (значения рисует сервер); цена названа в докстринге обработчика |
| T-11-15 | Elevation of Privilege | правка профиля под чужой личностью | high | mitigate | Зависимость `forbid_when_impersonating` не трогается; `tests/test_pages/test_impersonation.py` в verify |
| T-11-16 | Tampering | перенацеливание ответа заголовком без записи | medium | mitigate | Перечень FORM-10 с числом 0 и отрицательным контролем |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_response_contract.py tests/test_pages/test_shell.py tests/test_pages/test_htmx_validation_sink.py tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py -q` — после каждой задачи.
- `uv run pytest tests/test_planning/ -q` — после задачи 2.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Коммит задачи 1 предшествует коммиту свопа; на htmx-пути нет тела отказа фреймворка ни на одном обойдённом маршруте.
2. Правило 422 свопает; профиль отдаёт авторский 422 с эхом; литерал 422 в приложении один.
3. Перечень перенацеливания/пересвопа — 0 записей с зубами.
4. `NOT_YET_CONVERTED_COUNT = 20`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 9`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-05-SUMMARY.md` when done.
Сводка обязана привести хэши двух коммитов (смягчение и своп) в порядке приземления — это доказательство «сток не открыт ни на одном коммите».
</output>
