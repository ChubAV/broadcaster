---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - app/pages/schedules.py
  - app/templates/ads/partials/sched_create_response.html
  - tests/test_pages/test_editor_schedules.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_templates/test_htmx_markup_gates.py
  - app/templates/ads/form.html
autonomous: true
requirements: [FORM-07, FORM-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: задача 1 плана 11-04 ревизии 1 (180000, 11 файлов, три задачи), разрезанная на ответ обработчика и разметку контейнера и формы.
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Создание расписания в редакторе с непустым списком на htmx отвечает 200 и телом, чей верхний узел — новая карточка (статья `#sched-N` раскрытая и её панель рядом) для вставки `beforeend` в постоянный контейнер `#sched-list`; контейнер не перерисовывается, соседние карточки не трогаются (FORM-07, D-05)"
    - "Место вставки даёт порядок сервера: редактор сортирует расписания по `Schedule.id` по возрастанию, поэтому новая карточка встаёт В КОНЕЦ, и после F5 её место то же (прочтение `afterbegin` критерия 3 — по свойству, D-05)"
    - "Ответ создания подменяет содержимое линейки `#sched-count` и узел `#ad-summary` из ОДНОЙ величины числа расписаний (единственный источник разметки линейки — `ads/includes/sched_count_rule.html`)"
    - "Первое расписание («было ноль») на htmx отвечает 204 и `HX-Location` на `/ads/{ad_id}/edit?sched=N#sched-N` — тот же адрес, что уезжает 302 без htmx; второго механизма отрисовки пустого/непустого состояния не заводится (D-05, идиома D-09 Фазы 9)"
    - "Ответ обработчика приземляется задачей 1 до разметки: пока контейнер и форма не переведены задачей 2, форма создания отправляется без htmx и получает прежний 302"
    - "`NOT_YET_CONVERTED_COUNT` 24 → 23, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 5 → 6, `HX_HEADER_WRITES` остаётся 2 — прогоном"
    - statement: "В браузере при длинном списке расписаний и прокрутке к низу создание нового расписания не сбрасывает позицию прокрутки, карточка появляется в конце списка раскрытой — ручной UAT критерия 3"
      verification: backstop
  artifacts:
    - "app/templates/ads/form.html — контейнер `data-sched-list` с id `sched-list`; форма создания через form_wrapper (beforeend)"
    - "app/templates/ads/partials/sched_create_response.html (НОВЫЙ) — карточка + линейка + сводка"
    - "app/pages/schedules.py — schedules_create на respond(); прежние помощники перенаправления редактора удалены"
  key_links:
    - "форма создания (hx-target=#sched-list, beforeend) ↔ контейнер с id только при непустом списке: ломается — ответ фрагментом на пустой редактор даёт targetError; поэтому «было ноль» — переход"
    - "число расписаний, вычисленное один раз → линейка и сводка: ломается — линейка и сводка показывают разные числа до F5"
  prohibitions:
    - statement: "буквальный `afterbegin` не применяется, порядок редактора «новые сверху» не вводится (D-05, Deferred Ideas)"
      status: flagged-unverified
      verification: none
    - statement: "атрибут записи адреса в историю на форму создания не ставится (D-13)"
      status: flagged-unverified
      verification: none
---

<objective>
Последний обработчик раздела `schedules` (порядок D-15): создание расписания вставляет карточку в список,
не перерисовывая контейнер (FORM-07), а «было ноль» приземляет переходом.

Purpose: создание — единственное действие фазы с вставкой сущности; ошибка места вставки делает
экран расходящимся с сервером до F5 — ровно класс отказа, который закрывает веха.

Output: `schedules_create` через `respond()`; шаблон ответа создания; постоянный контейнер `#sched-list`
и форма создания на обёртке; прежние помощники перенаправления удалены; 24 → 23, 5 → 6.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: id `sched-list`;
`app/templates/ads/partials/sched_create_response.html`;
`test_schedule_create_over_htmx_appends_the_card_to_the_list`,
`test_the_first_schedule_over_htmx_lands_by_a_location_header`.
</artifacts_this_phase_produces>

<edge_coverage_fallback>
## Spec-less edge probe — явное допущение

- **FORM-07 — FLAGGED ASSUMPTION:** зонд краёв строку не классифицировал. Края создания (пустой
  список без контейнера, чужое объявление, недоступный аккаунт, отсутствие признака возврата) покрыты
  явными `truths` и случаями модуля пар этого плана; позиция прокрутки — `verification: backstop`.
  Машинного перечня краёв от зонда нет.
</edge_coverage_fallback>

<scope_note>
## Размер плана (ревизия 2)

Проекция 90000 в пределах смарт-зоны. План 11-04 ревизии 1 (11 файлов, три задачи) разрезан по
обработчикам: этот план — создание расписания, план 11-06 — модуль объявлений и его окно 51. Ответ
обработчика отделён от разметки: правила разметки перечня непереведённых обработчиков не читают, поэтому
ответ приземляется первым (его внеполосные узлы двигают разметочные числа в той же задаче), а контейнер
с id и форма на обёртке — задачей 2. Отвергнутый шов — обе задачи отдельными планами: оба стали бы
планами из одной задачи.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Создание расписания на слое ответа — карточка для вставки в конец, линейка и сводка из одной величины, «было ноль» переходом</name>

  <files>
app/pages/schedules.py,
app/templates/ads/partials/sched_create_response.html (NEW),
tests/test_pages/test_editor_schedules.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_hx_location_destinations.py,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/pages/schedules.py:900-991` — `schedules_create`: вердикт владения, чистка значений, запись,
  возврат; `:404-471`, `:659-672` — сборщики адреса и прежние помощники перенаправления (после этого
  плана у них не останется вызывающих — `grep -n` по имени).
- `app/pages/schedules.py:500-560` — `_ad_schedule_count`, `_ad_row`, `_ad_next_run_at`.
- `app/templates/ads/form.html:195-262` — обёртка `#sched-count`, контейнер `data-sched-list` внутри
  условия «расписания есть», ветка `empty_state`, форма создания `:241` и подпись кнопки.
- `app/templates/ads/partials/sched_delete_response.html` — узлы `innerHTML:#sched-count` и
  `#ad-summary`, включение линейки.
- Результат 11-01: макросы `sched_card`, `sched_card_article`; `ads/partials/sched_card_response.html`.
- `tests/test_templates/test_htmx_markup_gates.py:4404-4540` — правила единственного источника
  разметки линейки счётчика; `:4248` — `OOB_BLOCKS`.
- `11-CONTEXT.md` D-05 целиком и Landmine «data-sched-list сегодня без id, при нуле его нет вовсе».
  </read_first>

  <behavior>
RED:
1. `test_schedule_create_over_htmx_appends_the_card_to_the_list`: объявление с одним расписанием,
   htmx POST `/schedules/new` с `return_to=editor` → 200; тело начинается с `<article data-sched-card
   id="sched-{new}">` c формой правки (раскрыта), далее корень панели `id="sched-del-{new}"`;
   внеполосные `innerHTML:#sched-count` с текстом «2 расписания» и `#ad-summary`; нет `<!DOCTYPE`.
   Без htmx — 302 `/ads/{ad}/edit?sched={new}#sched-{new}`.
2. `test_the_first_schedule_over_htmx_lands_by_a_location_header`: объявление без расписаний →
   204, `HX-Location` `/ads/{ad}/edit?sched={new}#sched-{new}`, тело пусто; без htmx — 302 туда же.
3. Случаи модуля пар: успех (`FRAGMENT`), было ноль (`LOCATION`), чужое объявление (`LOCATION`,
   `/schedules`), недоступный аккаунт (`LOCATION`, код `schedule_account_gone`).
GREEN — ниже.
  </behavior>

  <action>
**1. Обработчик (D-02, D-05, D-06).** Все выходы — `respond`: «нет сессии» → `/login`; негодный `ad_id` либо
`OWNERSHIP_AD_DENIED` → `/schedules`; `OWNERSHIP_ACCOUNT_DENIED` → `/ads/{ad_id}/edit` с кодом
`notices.SCHEDULE_ACCOUNT_GONE`. После записи признак возврата читается один раз; без него →
`respond(request, redirect="/schedules")`. С ним — число расписаний объявления после записи
(`_ad_schedule_count`): при единице → `respond(request, redirect=_editor_url(True, ad_id,
schedule.id))` без фрагмента («было ноль» — контейнера на экране нет, D-05); иначе — тот же адрес и
нульарный сборщик, отдающий `ads/partials/sched_create_response.html`. Из модуля удаляются оба
прежних помощника перенаправления редактора, у которых не осталось вызывающих, а докстринг
`_editor_url` получает поколение («три вызывающих одевали адрес в перенаправление — с планов
11-01…11-05 адрес уезжает через слой ответа»). Шаблон редактора в этой задаче не трогается.

**2. Шаблон ответа (НОВЫЙ).** Плоское тело, три верхнеуровневые части: вывод `sched_card(...)`
(статья и панель — обе принадлежат контейнеру, как в полной отрисовке) с раскрытием новой карточки;
узел `hx-swap-oob="innerHTML:#sched-count"` с включением `ads/includes/sched_count_rule.html`; узел
`#ad-summary` с включением сводки. Число расписаний вычисляется ОДИН раз и уезжает в обе области.
Соседние карточки ответ не трогает: ранее раскрытая остаётся раскрытой до F5 (D-05). Шапка шаблона —
перечень узлов и основание `beforeend`.

**3. Числа.** Ключ `schedules_create` снимается из `NOT_YET_CONVERTED` и добавляется в
`FRAGMENT_RESPONSE_HANDLERS`; 24 → 23 и 5 → 6 прогоном, летопись словами («создание расписания в
редакторе»). `OOB_BLOCKS` и прочие разметочные числа шаблона ответа, `HX_LOCATION_DESTINATION_CALLS_DECLARED`
— прогоном, каждое с летописью «Фаза 11, план 11-05».
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_hx_location_destinations.py tests/test_templates/test_htmx_markup_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def schedules_create/,/^@router/' app/pages/schedules.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -cE '^def (_editor_redirect|_editor_error_redirect)\(' app/pages/schedules.py` возвращает `0`.
- `uv run pytest tests/test_pages/test_editor_schedules.py -q -k "appends_the_card or first_schedule_over_htmx"` — зелено, 2 теста.
- `uv run pytest tests/test_pages/test_schedule_ownership.py tests/test_pages/test_schedule_creation_path_exists.py tests/test_routes/test_schedules_profile_timezone.py -q` — зелено.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 23$' tests/test_pages/test_htmx_gates.py` печатает строк: 1.
- Разметка редактора в этой задаче не тронута: `git status --porcelain -- app/templates/ads/form.html` — пустой вывод.
  </acceptance_criteria>

  <done>
Создание отдаёт раскрытую карточку для вставки в конец контейнера и обновляет линейку и сводку одной
величиной; первое расписание приземляет переходом; прежние помощники перенаправления удалены.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Постоянный контейнер списка и форма создания на обёртке — вставка `beforeend`</name>

  <files>
app/templates/ads/form.html,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/ads/form.html:195-262` — контейнер `data-sched-list`, ветка `empty_state`, форма
  создания `:241` (поля `ad_id`, `return_to`, условный `account_id`, выражение подписи кнопки).
- `app/templates/components/form_wrapper.html:138-148`.
- `tests/test_templates/test_htmx_markup_gates.py:4029-4200` — цели свопа, их существование и
  параметрические цели.
- Результат задачи 1 (шаблон ответа создания).
  </read_first>

  <action>
**1. Контейнер и форма (D-05).** В `ads/form.html` контейнер `data-sched-list` получает постоянный
`id="sched-list"` (остаётся внутри условия «расписания есть» — основание ветки «было ноль»). Форма
создания переходит на блочный вызов `form_wrapper(action='/schedules/new', target='#sched-list',
swap='beforeend')`; поля `ad_id`, `return_to`, условный `account_id` и выражение подписи кнопки не
меняются. Комментарий над контейнером называет D-05: место вставки даёт порядок сервера
(`Schedule.id` по возрастанию, `app/pages/ads.py` `_editor_context`), поэтому `beforeend`.

**2. Числа разметки.** Двигаются ровно краснеющие константы (цели свопа, места отправки htmx,
вызывающие обёртку), каждая — строкой летописи «Фаза 11, план 11-05» с дословным текстом отказа.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py tests/test_pages/test_editor_schedules.py tests/test_pages/test_ads_editor.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT критерия 3: в редакторе объявления с 8+ расписаниями прокрутить к низу, нажать «+ РАСПИСАНИЕ»; позиция прокрутки не сброшена, новая карточка в конце списка раскрыта, линейка и сводка показывают новое число; на объявлении без расписаний первое создание переводит на редактор с раскрытой карточкой. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -c 'data-sched-list id="sched-list"' app/templates/ads/form.html` возвращает `1`.
- `grep -c "swap='beforeend'" app/templates/ads/form.html` возвращает `1`.
- `grep -c 'hx-push-url' app/templates/ads/form.html` возвращает `0`.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "counter_rule or schedule_counter or swap_target"` — зелено.
  </acceptance_criteria>

  <done>
Форма создания вставляет ответ `beforeend` в постоянный контейнер `#sched-list`; разметочные числа
поставлены прогоном.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | `ad_id` и `account_id` формы создания расписания |
| фрагмент → DOM | имена групп и подпись аккаунта новой карточки уезжают в разметку без перезагрузки |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Elevation of Privilege | создание расписания на чужое объявление / через чужой аккаунт | high | mitigate | `_ownership_verdict` стоит до записи и до развилки транспорта, не меняется; чужое → переход `/schedules` на обоих транспортах (случай модуля пар) |
| T-11-02 | Tampering | фрагмент новой карточки (XSS хранимым текстом) | medium | mitigate | Только автоэкранирование Jinja через тот же макрос `sched_card`, ни `|safe`, ни `Markup(` (правило GATE-07 разметочных гейтов зелено) |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Создание расписания вставляет карточку `beforeend` в `#sched-list`, «было ноль» — переходом.
2. Прежние помощники перенаправления редактора удалены.
3. `NOT_YET_CONVERTED_COUNT = 23`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 6`, `HX_HEADER_WRITES = 2`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-05-SUMMARY.md` when done.
</output>
