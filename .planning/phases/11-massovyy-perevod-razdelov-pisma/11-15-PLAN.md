---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 15
type: execute
wave: 15
depends_on: ["11-14"]
files_modified:
  - app/pages/htmx.py
  - app/pages/billing.py
  - app/templates/billing/balance.html
  - tests/test_pages/test_htmx_response_layer.py
  - tests/test_pages/test_billing_subscription.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_pages/test_responsive_markup.py
autonomous: true
requirements: [FORM-05, FORM-04]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: план 11-09 ревизии 1 перенумерован в 11-15 без изменения предмета. Смарт-зона 100000 превышена (advisory) — основание в <scope_note>.
  tokens: 150000
  raw_tokens: 150000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Оформление доступа на htmx при успешном создании платежа отвечает 204 и `HX-Redirect`, равным `confirmation_url` посимвольно; заголовка `HX-Location` в ответе нет, тела нет (FORM-05, D-09, Находка A: `HX-Location` читается первым)"
    - "Без htmx оформление отвечает прежним 302 на `confirmation_url`; проверка хоста на этом пути НЕ выполняется, и асимметрия записана основанием в `SAFE_BY_NAME`: источник адреса — ответ SDK ЮKassa по TLS, а не ввод пользователя, и правило границы фазы запрещает менять, ЧТО делает действие"
    - "Перед записью `HX-Redirect` адрес проходит рантайм-проверку: схема `https`, хост ТОЧНО из закрытого множества `YOOKASSA_CONFIRMATION_HOSTS = frozenset({\"yoomoney.ru\"})`, без порта и данных пользователя, только ASCII, без управляющих символов; суффиксные подделки (`evil-yoomoney.ru`, `yoomoney.ru.evil.com`) и недокументированный `yookassa.ru` отвергаются (D-09, Находка C)"
    - "Провал проверки: заголовок не пишется, журнал `payment_confirmation_url_rejected` несёт хост и НЕ несёт полного адреса, ответ — 204 и `HX-Location` на `/billing?notice=payment_failed`"
    - "Отказы оплаты (платежи выключены, незакрытое намерение, сбой создания) на htmx отвечают 204 и `HX-Location` на `/billing` с прежним кодом реестра; потолок незакрытых намерений и запрет синхронизации очередью на маршруте не тронуты (PAY-01, PAY-02)"
    - "`HX_HEADER_WRITES` 2 → 3, `SAFE_BY_NAME` — две записи; `OWN_RESPONSE_EXITS_DECLARED` 11 → 12 записью D-08 для голого 403 оплаты; `NOT_YET_CONVERTED_COUNT` 18 → 17 — прогоном"
    - "Пары оплаты тройные и стоят на документированном хосте: без заголовка → 302 на адрес подтверждения; с заголовком → 204 + `HX-Redirect`; адрес чужого хоста → заголовка нет, 204 + `HX-Location` на `/billing?notice=payment_failed`"
    - statement: "Оформление доступа на тестовом ключе ЮKassa в браузере с htmx уводит на страницу подтверждения; фактический хост `confirmation_url` записан в 11-UAT.md и совпадает с закрытым множеством — ручной UAT критерия 2, ДО слияния"
      verification: backstop
  artifacts:
    - "app/pages/htmx.py — HX_REDIRECT_HEADER, YOOKASSA_CONFIRMATION_HOSTS, _confirmation_url, redirect_external"
    - "app/pages/billing.py — subscribe_to_plan на respond() и redirect_external()"
    - "app/templates/billing/balance.html — форма оплаты через form_wrapper внутри обёртки data-plan-cta"
    - "tests/test_pages/test_htmx_gates.py — запись SAFE_BY_NAME для redirect_external; запись OWN_RESPONSE_EXITS для subscribe_to_plan"
  key_links:
    - "create_payment → result[confirmation_url] → redirect_external → _confirmation_url → HX-Redirect: ломается — htmx-оплата молча уходит в PAYMENT_FAILED у реального хоста (UAT обязателен)"
    - "HX-Redirect ставится на ВОЗВРАЩАЕМЫЙ ответ: ломается — тест кода 204 зелёный при потерянном заголовке (урок cookie D-05 Фазы 10)"
  prohibitions:
    - statement: "внешний адрес никогда не подаётся в redirect= и не уезжает HX-Location (ловушка D-15 Фазы 8)"
      status: flagged-unverified
      verification: none
    - statement: "множество хостов не выносится в настройки окружения — ценность проверки в закрытости (RESEARCH Open Question 3)"
      status: flagged-unverified
      verification: none
    - statement: "ответ не несёт одновременно HX-Location и HX-Redirect"
      status: flagged-unverified
      verification: none
    - statement: "перевод не ослабляет потолок незакрытых намерений оплаты и не допускает синхронизацию запросов очередью на маршруте оформления (PAY-01, PAY-02)"
      status: flagged-unverified
      verification: none
---

<objective>
Раздел `billing` (не первым — поверх построенного в Фазе 8 потолка, D-15): оформление доступа уходит
на внешнюю страницу подтверждения ЮKassa третьим выходом слоя ответа — заголовком `HX-Redirect`,
а не `HX-Location`, с рантайм-проверкой адреса перед записью заголовка (D-09).

Purpose: `selfRequestsOnly: true` блокирует межсайтовый переход по `HX-Location` — перевод формы
оплаты «как все» сломал бы работающий платёж; а заголовок увода с сайта без проверки есть открытый
редирект.

Output: третий выход с закрытым множеством хостов и тестами, перевод `subscribe_to_plan`, форма на
`form_wrapper`, записи `SAFE_BY_NAME` и D-08, 2 → 3, 11 → 12, 18 → 17.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-RESEARCH.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-13-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `HX_REDIRECT_HEADER`, `YOOKASSA_CONFIRMATION_HOSTS`,
`_confirmation_url`, `redirect_external` (`app/pages/htmx.py`); запись `SAFE_BY_NAME`
`"app/pages/htmx.py::redirect_external"`; запись `OWN_RESPONSE_EXITS` для
`app/pages/billing.py::subscribe_to_plan`; тесты
`test_an_external_address_leaves_by_the_redirect_header_only`,
`test_a_confirmation_address_off_the_closed_host_set_never_reaches_the_header`,
`test_subscription_over_htmx_answers_the_triple_pair`.
</artifacts_this_phase_produces>

<edge_coverage_fallback>
## Spec-less edge probe — явное допущение

- **FORM-05 — FLAGGED ASSUMPTION:** зонд краёв строку не классифицировал. Края внешнего перехода
  (суффиксные хосты, порт, данные пользователя, схема, не-ASCII, управляющие символы, одновременные
  заголовки, путь без htmx) перечислены явно в тестах задачи 1; фактический хост — `backstop` UAT.
  Машинного перечня краёв нет. Допущение A1 RESEARCH («`confirmation_url` при создании без
  `payment_method_data` всегда на `yoomoney.ru`») остаётся `[ASSUMED]` до UAT.
</edge_coverage_fallback>

<scope_note>
## Размер плана (ревизия 2)

Проекция 150000 НЕКАЛИБРОВАНА (замеров ноль) и выходит за смарт-зону 100000 (advisory). Отвергнуты два
шва. (1) Третий выход слоя ответа (задача 1) отдельным планом: план из одной задачи, а доказательство
безопасности записи заголовка (`SAFE_BY_NAME`) отделилось бы от своего единственного потребителя на
целую волну. (2) Разметка формы оплаты (обёртка, `data-plan-cta`, правила разметки) отдельным планом
после ответа обработчика: снова план из одной задачи на три файла, а денежный маршрут переводится одним
планом, чтобы потолок незакрытых намерений и запрет синхронизации очередью проверялись одним прогоном
периметра на одном дереве (PAY-01, PAY-02). План 11-09 ревизии 1 перенумерован в 11-15 без изменения
предмета.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Третий выход слоя ответа — `HX-Redirect` только на проверенный адрес ЮKassa</name>

  <files>
app/pages/htmx.py,
tests/test_pages/test_htmx_response_layer.py,
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `app/pages/htmx.py:1-44` (докстринг модуля: «Третий выход… сознательно отсутствует и вводится Фазой
  11»), `:110-170` (`_local_path` — классы отказа и правило «текст ошибки не подставляет значение»;
  `location_response`), `:394-485` (`respond`, абзац «ВНЕШНИЙ АДРЕС НИКОГДА НЕ ЕДЕТ В `redirect=`…
  ветку эту вводит Фаза 11»).
- `tests/test_pages/test_htmx_gates.py:159-165` (`HX_HEADER_WRITES = 2`), `:1531-1600` (`SAFE_BY_NAME`
  и основание формы записи), `:1797-1860` (правила числа и безопасности правого операнда).
- `tests/test_pages/test_htmx_response_layer.py:177-260` — форма тестов `_local_path` и выходов.
- `app/pages/accounts.py:690-700` — как модуль страниц берёт логгер (`structlog.get_logger()`).
- `11-RESEARCH.md` §Находка A, §Находка C, §Code Examples «Третий выход», Pitfalls 2 и 3.
  </read_first>

  <behavior>
RED:
1. `test_an_external_address_leaves_by_the_redirect_header_only`: запрос с признаком htmx и адрес
   `https://yoomoney.ru/checkout/payments/v2/contract?orderId=2c85a` → 204,
   `headers["HX-Redirect"]` равен адресу посимвольно, `"HX-Location" not in headers`, `content ==
   b""`; без признака → 302 и `location` равен адресу (для любого хоста).
2. `test_a_confirmation_address_off_the_closed_host_set_never_reaches_the_header` — параметризован по
   `http://yoomoney.ru/…`, `https://evil-yoomoney.ru/…`, `https://yoomoney.ru.evil.com/…`,
   `https://yookassa.ru/…`, `https://user@yoomoney.ru/…`, `https://yoomoney.ru:8443/…`, адресу с
   `\r\n`, адресу с кириллицей: 204, заголовка `HX-Redirect` нет, `HX-Location` =
   `/billing?notice=payment_failed`; запись журнала `payment_confirmation_url_rejected` несёт хост и
   не несёт полного адреса.
3. Правило `test_the_number_of_header_writes_is_the_declared_one` краснеет на третьей записи до
   движения числа; `test_every_header_write_has_a_safe_right_operand` краснеет до записи `SAFE_BY_NAME`.
GREEN — ниже.
  </behavior>

  <action>
**1. Выход (D-09, Claude's Discretion: имя и сигнатура).** В `app/pages/htmx.py`:
`HX_REDIRECT_HEADER = "HX-Redirect"` (константа, как `HX_LOCATION_HEADER`);
`YOOKASSA_CONFIRMATION_HOSTS = frozenset({"yoomoney.ru"})` с комментарием: множество снято с
документации ЮKassa (все официальные примеры `confirmation_url` — на `yoomoney.ru`; документация
говорит «страница ЮKassa или её партнёра», то есть закрытость ЯВЛЯЕТСЯ допущением A1 и подтверждается
UAT), хост `yookassa.ru` в документированных адресах подтверждения не найден, в настройки окружения
множество не выносится (OQ3). `_confirmation_url(value: str) -> str` — разбор
`urllib.parse.urlsplit`, отказ `ValueError`, если схема не `https`, `hostname` не входит в множество
ТОЧНЫМ сравнением, есть порт, есть данные пользователя или пароль, есть символ вне ASCII или
управляющий символ; текст ошибки значение не подставляет (правило `_local_path`).
`async def redirect_external(request, *, url: str, fallback: str, fallback_notice: str) -> Response`:
без признака htmx — прежнее перенаправление 302 на `url` (путь без JavaScript не меняется); с
признаком — проверка; при `ValueError` — журнал `payment_confirmation_url_rejected` с `host` из
разбора (логгер берётся внутри функции, как отложенные импорты модуля) и `return await
respond(request, redirect=fallback, notice=fallback_notice)`; при успехе — ответ 204 со словарём
заголовков, в котором ровно один ключ `HX_REDIRECT_HEADER`. Докстринг: Находка A (`HX-Redirect` —
`location.href`, полная навигация, `selfRequestsOnly` к ней не применяется; `HX-Location` читается
первым, поэтому ответ несёт ровно один заголовок перехода; 204 не мешает — заголовки читаются до
правил); асимметрия проверки на пути без htmx и её основание.

**2. Докстринги-поколения.** Абзац модуля «Третий выход… сознательно отсутствует и вводится Фазой 11»
и абзац `respond` «ветку эту вводит Фаза 11» получают поколение «введён планом 11-15 функцией
`redirect_external`», прежние формулировки остаются названными.

**3. Гейты заголовков.** `HX_HEADER_WRITES` 2 → 3 прогоном с летописью («третья запись —
`redirect_external`, D-09»). `SAFE_BY_NAME` получает вторую запись `"app/pages/htmx.py::redirect_external"`
по форме первой: значение приходит параметром, безопасность доказывается рантайм-проверкой
`_confirmation_url` (закрытое множество, точное сравнение хоста, схема, порт, данные пользователя,
ASCII, управляющие символы) и её тестами (план 11-15); путь без htmx заголовка не пишет и проверки
не несёт — асимметрия названа (источник — ответ SDK по TLS; правило границы фазы).

**4. Тесты** — по §behavior, в `tests/test_pages/test_htmx_response_layer.py`, через минимальное
приложение либо прямой вызов выхода с запросом, несущим признак (форма соседних тестов выходов).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^YOOKASSA_CONFIRMATION_HOSTS = frozenset\(\{"yoomoney.ru"\}\)$' app/pages/htmx.py` печатает одну строку.
- `grep -nE '^async def redirect_external' app/pages/htmx.py` печатает одну строку.
- `grep -nE '^HX_HEADER_WRITES = 3$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `grep -n '"app/pages/htmx.py::redirect_external"' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_htmx_response_layer.py -q -k "redirect_header_only or off_the_closed_host_set"` — зелено, не меньше 9 случаев.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -k "header_write"` — зелено.
  </acceptance_criteria>

  <done>
Третий выход существует, пишет `HX-Redirect` только на адрес закрытого множества хостов, на провале
уходит переходом с кодом `payment_failed`; гейты заголовков знают третью запись поимённо.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Оформление доступа — тройная пара, форма на обёртке, изъятие D-08 для голого 403</name>

  <files>
app/pages/billing.py,
app/templates/billing/balance.html,
tests/test_pages/test_billing_subscription.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_hx_location_destinations.py,
tests/test_pages/test_responsive_markup.py
  </files>

  <read_first>
- `app/pages/billing.py:268-352` — `subscribe_to_plan` целиком: «нет сессии», сверка источника с
  голым 403, «платежи выключены», `create_payment`, ветки `PendingIntentCapError` и
  `PaymentCreationError` с комментариями, возврат на `confirmation_url`.
- `app/templates/billing/balance.html:100-120` — форма `data-plan-cta` без полей и её комментарий.
- `app/static/css/app.css:1605-1613` — правило `[data-plan-cta] .btn` (адресуется предком).
- `tests/test_pages/test_responsive_markup.py:2305-2395` — правила атрибута `data-plan-cta` (число
  вхождений, привязка к форме оплаты).
- `tests/test_pages/test_money_perimeter_gate.py` — периметр опасных маршрутов, запрет синхронизации
  очередью.
- `tests/test_pages/test_billing_subscription.py:1-80` — `CONFIRMATION_URL` на `yookassa.ru` и способ
  подмены `create_payment`.
- Результат планов 11-12 и 11-13: `DECISION_OWNER_D08` и форма записи `OWN_RESPONSE_EXITS`.
  </read_first>

  <behavior>
RED:
1. `test_subscription_over_htmx_answers_the_triple_pair` (в `test_billing_subscription.py`) с подменой
   `create_payment`, возвращающей `https://yoomoney.ru/checkout/payments/v2/contract?orderId=2c85a`:
   без заголовка → 302 на адрес; с заголовком → 204, `HX-Redirect` равен адресу, `HX-Location` нет,
   тела нет; подмена с `https://evil.example/…` под htmx → 204, `HX-Redirect` нет, `HX-Location`
   `/billing?notice=payment_failed`.
2. Случаи модуля пар: успех (`EXTERNAL`), платежи выключены (`LOCATION`,
   `/billing?notice=payment_disabled`), незакрытое намерение (`LOCATION`,
   `/billing?notice=payment_pending`), сбой создания (`LOCATION`, `/billing?notice=payment_failed`),
   нет сессии (`LOCATION`, `/login`). Реализация транспорта `EXTERNAL` в параметризованном тесте: 204,
   `HX-Redirect` посимвольно, `HX-Location` отсутствует, тело пусто.
3. Правило полноты `OWN_RESPONSE_EXITS` краснеет на переведённой оплате до записи.
GREEN — ниже.
  </behavior>

  <action>
**1. `subscribe_to_plan`.** Порядок проверок и их комментарии сохраняются. «Нет сессии» →
`respond(request, redirect="/login")`; сверка источника с голым 403 не трогается (D-08); «платежи
выключены» → `respond(request, redirect="/billing", notice=notices.PAYMENT_DISABLED)`;
`PendingIntentCapError` → код `PAYMENT_PENDING`; `PaymentCreationError` → код `PAYMENT_FAILED`; успех
→ `return await redirect_external(request, url=result["confirmation_url"], fallback="/billing",
fallback_notice=notices.PAYMENT_FAILED)`. Собственной сборки перенаправления в обработчике не
остаётся. Комментарии веток «почему отдельная ветка незакрытого намерения» сохраняются дословно.

**2. Форма оплаты.** Форма переходит на вызов `form_wrapper(action='/billing/subscribe')` без цели
(ответ — переход); атрибут `data-plan-cta` переезжает на обёртку-предок формы (макрос обёртки
дополнительных атрибутов не принимает), правило `[data-plan-cta] .btn` в `app.css` адресуется предком
и работает без правки. Синхронизация очередью не ставится. Если правило
`test_responsive_markup.py` привязывает атрибут к тегу формы — оно получает поколение (атрибут на
единственной обёртке формы оплаты), число вхождений атрибута на шаблон остаётся одним.

**3. Изъятие D-08.** Запись `_OwnResponseExit(entry="app/pages/billing.py::subscribe_to_plan", …)` по
форме записей 11-12 и 11-13: основание — недостижимость из интерфейса на htmx-пути и цена словами (плашка
«Действие не выполнено. Попробуйте ещё раз через минуту.»); свой хвост — межсайтовый запрос не узнаёт
даже, включены ли платежи; `decision_state = DECISION_OWNER_D08`. `OWN_RESPONSE_EXITS_DECLARED` 11 → 12
прогоном; докстринг модуля `htmx.py` о собственных выходах — «двенадцать».

**4. Числа.** Ключ оплаты снимается из `NOT_YET_CONVERTED` (через `MONEY_HANDLER_KEY`, константа
остаётся для периметра); `NOT_YET_CONVERTED_COUNT` 18 → 17 прогоном, летопись словами («оформление
доступа с уходом на страницу ЮKassa»). В `HX_LOCATION_DESTINATION_TEMPLATES` добавляется `/billing` →
`billing/balance.html`, если правило назовёт адрес; числа переходов и разметки — прогоном.

**5. Фикстуры хоста.** Существующие тесты без заголовка на `yookassa.ru` не меняются (путь 302 хост не
проверяет); тройная пара и случаи модуля пар стоят на документированном `yoomoney.ru` (Находка C).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_subscription.py tests/test_pages/test_billing_payment_errors.py tests/test_pages/test_access_lifecycle.py tests/test_pages/test_money_perimeter_gate.py tests/test_pages/test_responsive_markup.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_hx_location_destinations.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT критерия 2 (ДО слияния): на стенде с тестовым ключом ЮKassa на /billing нажать кнопку оплаты при живом htmx — браузер уходит на страницу подтверждения ЮKassa; во вкладке Network у ответа POST /billing/subscribe статус 204 и заголовок HX-Redirect; записать фактический хост confirmation_url в 11-UAT.md и сверить с YOOKASSA_CONFIRMATION_HOSTS; если хост вне множества — остановить слияние и вернуть вопрос о множестве (Loki: payment_confirmation_url_rejected).</human-check>
  </verify>

  <acceptance_criteria>
- `awk '/^async def subscribe_to_plan/,/^@/' app/pages/billing.py | grep -c 'RedirectResponse'` возвращает `0`.
- `awk '/^async def subscribe_to_plan/,/^@/' app/pages/billing.py | grep -n 'Response(status_code=403)'` печатает одну строку.
- `grep -nE '^OWN_RESPONSE_EXITS_DECLARED = 12$' tests/test_pages/test_htmx_gates.py` печатает одну строку; `grep -nE '^NOT_YET_CONVERTED_COUNT = 17$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `grep -n 'hx-sync' app/templates/billing/balance.html | wc -l` возвращает `0`.
- `uv run pytest tests/test_pages/test_billing_subscription.py -q -k "triple_pair"` — зелено.
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q -k "subscribe"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Оформление доступа уходит на ЮKassa заголовком `HX-Redirect` только после проверки хоста; отказы и
провал проверки — переходом на `/billing` с кодом; голый 403 объявлен изъятием D-08; реестр
собственных выходов — 12.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK ЮKassa → слой ответа | `confirmation_url` приходит ответом внешнего сервиса и уезжает в заголовок |
| слой ответа → браузер | `HX-Redirect` исполняется как `location.href` — полная навигация, мимо `selfRequestsOnly` |
| сторонний сайт → денежный маршрут | cookie прикладывается к межсайтовой форме |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-23 | Spoofing (открытый редирект, OWASP A01 — канон, отмечен ссылкой) | `redirect_external` на htmx-пути | high | mitigate | Закрытое множество `{"yoomoney.ru"}`, точное сравнение `hostname`, `https`, без порта и данных пользователя; 8 параметризованных случаев отказа; запись `SAFE_BY_NAME` вместо разрешения классом |
| T-11-24 | Tampering / DoS | инъекция заголовка и падение на кодировании | medium | mitigate | Отказ на управляющих символах и не-ASCII до записи заголовка (классы `_local_path`); текст ошибки значение не подставляет |
| T-11-25 | Information Disclosure | журнал отвергнутого адреса | low | mitigate | Журнал несёт только хост, полный адрес (с `orderId`) не пишется — утверждается тестом записи журнала |
| T-11-26 | Tampering | второе незакрытое намерение оплаты из-за смены транспорта | high | mitigate | `create_payment` и `PendingIntentCapError` не трогаются, развилка транспорта стоит после создания; гейт периметра (`test_money_perimeter_gate.py`) в verify, синхронизации очередью на форме нет |
| T-11-27 | Tampering (CSRF) | голый 403 оплаты | high | mitigate | Сверка источника стоит до любого обращения к БД и не меняется; запись D-08 в `OWN_RESPONSE_EXITS` с правилом полноты |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет (`urllib.parse` — стандартная библиотека) |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
- Ручной UAT критерия 2 — ДО слияния ветки фазы (`<human-check>` задачи 2).
</verification>

<success_criteria>
1. На htmx оформление отвечает `HX-Redirect` на проверенный адрес ЮKassa и никогда — `HX-Location` на внешний адрес.
2. Адрес вне закрытого множества не попадает в заголовок; журнал называет хост без полного адреса.
3. `HX_HEADER_WRITES = 3`, `SAFE_BY_NAME` — 2 записи, `OWN_RESPONSE_EXITS_DECLARED = 12`, `NOT_YET_CONVERTED_COUNT = 17`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-15-SUMMARY.md` when done.
Сводка передаёт ручному обходу пункт «фактический хост confirmation_url на тестовом ключе» открытым, если UAT не пройден исполнителем.
</output>
