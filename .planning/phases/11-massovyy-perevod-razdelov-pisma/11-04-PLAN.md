---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - app/pages/schedules.py
  - app/templates/schedules/list.html
  - app/templates/schedules/partial_cards.html
  - app/templates/schedules/includes/schedule_row.html
  - app/templates/schedules/partials/schedule_row_response.html
  - tests/test_pages/test_schedules_list.py
  - tests/test_pages/test_identifier_bounds.py
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
autonomous: true
requirements: [FORM-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: задача 2 плана 11-03 ревизии 1 (170000, 13 файлов), разрезанная на ключевой курсор и тумблер сводного списка. Смарт-зона 100000 превышена (advisory) — основание в <scope_note>.
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/schedules/partial` листается ключом последней строки `after_id` вместо смещения: после тумблера под фильтром состояния следующая порция не пропускает ни одной строки (D-11, CR-01 Фазы 9); разметка сентинела байт-в-байт одинакова в `list.html` и `partial_cards.html`"
    - "Курсор приземляется задачей 1 ДО фрагмента строки: ни на одном коммите плана тумблер под фильтром не съедает строку следующей порции (D-11)"
    - "Тумблер на сводном списке на htmx отвечает 200 и строкой `#schedule-row-N` (`outerHTML`); экран узнаётся отсутствием признака редактора в существующем скрытом поле `return_to`, и адрес без htmx прежний (D-02) — ветка сводного списка, отвечавшая переходом с плана 11-03, становится фрагментом"
    - "Фрагмент строки ВСЕГДА отражает сервер: неполное расписание, которое нельзя возобновить, возвращает строку в прежнем состоянии, и оптимистично переключённый браузером флажок встаёт обратно (D-11, D-13/D-16 Фазы 9)"
    - "Форма тумблера строки отправляется `hx-trigger=change` с `hx-sync=this:drop`, прежний перехват Alpine по событию изменения снят, кнопка «Применить» остаётся для мира «Alpine жив, htmx мёртв»; вызывающий записан в `DISABLED_ELT_EXCEPTIONS` с основанием D-11 (прецедент DIV-09-02)"
    - "Счёт обработчиков не двигается (`schedules_toggle` переведён планом 11-03); `PAGINATION_EXCLUSION_DECLARED`, `CATALOGUE_UNIVERSE_DECLARED`, разметочные числа и `HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном"
    - statement: "В браузере тумблер строки переключается одним нажатием, двойное нажатие не отправляет два запроса, фокус остаётся на тумблере, строка под фильтром остаётся под рукой, прокрутка до второй порции не теряет строк — ручной UAT фазы, пункт 5"
      verification: backstop
  artifacts:
    - "app/pages/schedules.py — schedules_partial и schedules_list с after_id / next_after_id; ветка строки списка в schedules_toggle"
    - "app/templates/schedules/partials/schedule_row_response.html (НОВЫЙ)"
    - "app/templates/schedules/includes/schedule_row.html — тумблер через form_wrapper"
    - "tests/test_pages/test_schedules_list.py — test_the_next_portion_does_not_skip_a_row_after_a_toggle_under_the_state_filter"
  key_links:
    - "after_id сентинела → Schedule.id > after_id: ломается — тумблер под фильтром съедает строку следующей порции"
    - "return_to (скрытое поле) → выбор фрагмента: карточка редактора или строка списка; ломается — фрагмент редактора приезжает на сводный список, где цели нет"
  prohibitions:
    - statement: "при активном фильтре состояния переключённая строка не исчезает из-под руки переходом — отвергнуто D-11"
      status: flagged-unverified
      verification: none
    - statement: "атрибут записи адреса в историю на форму тумблера не ставится (D-13)"
      status: flagged-unverified
      verification: none
    - statement: "внутри целей свопа — только голый x-data (D-12(б), D-08 Фазы 9)"
      status: flagged-unverified
      verification: none
---

<objective>
Сводный список расписаний (раздел `schedules`, порядок D-15) получает ключевой курсор, без которого
фрагментный тумблер под фильтром воспроизвёл бы CR-01 Фазы 9 (D-11), а затем — фрагментный тумблер
строки на своём экране.

Purpose: тумблер — самое частое действие раздела; его ответ обязан подменять ровно строку того
экрана, откуда пришёл, и всегда показывать серверное состояние, а порция списка не имеет права терять
строку, выведенную тумблером из фильтра.

Output: keyset `/schedules/partial`, шаблон строки ответа, форма тумблера строки через `form_wrapper`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-PATTERNS.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-03-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `app/templates/schedules/partials/schedule_row_response.html`;
параметр `after_id` у `schedules_partial` и ключ контекста `next_after_id`;
`test_the_next_portion_does_not_skip_a_row_after_a_toggle_under_the_state_filter`.
</artifacts_this_phase_produces>

<scope_note>
## Размер плана (ревизия 2)

Проекция 110000 НЕКАЛИБРОВАНА (замеров ноль) и выходит за смарт-зону 100000 (advisory). План 11-03
ревизии 1 (13 файлов) разрезан по экранам тумблера: карточка редактора — план 11-03, сводный список —
этот план. Отвергнутый шов — курсор (задача 1) и тумблер строки (задача 2) отдельными планами: обе
стороны шва стали бы планами из одной задачи, а D-11 связывает их порядком — курсор обязан приземлиться
не позже фрагмента строки, и отдельный план курсора лишь разнёс бы по волнам одно решение.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Ключевой курсор `/schedules/partial` — порция не теряет строку после тумблера под фильтром</name>

  <files>
app/pages/schedules.py,
app/templates/schedules/list.html,
app/templates/schedules/partial_cards.html,
tests/test_pages/test_schedules_list.py,
tests/test_pages/test_identifier_bounds.py,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/pages/schedules.py:675-806` — `_summary_query`, `_apply_filters`, `_build_schedule_items`,
  `schedules_partial` (смещение `:765`, `.offset(offset)` `:788`, `next_offset` `:802`); `:808-866` —
  `schedules_list` (`next_offset: PAGE_SIZE`, `total`).
- `app/pages/account_groups.py:285-380` — образец keyset: `after_id: int | None = Query(None, ge=1,
  le=ID_MAX)`, `where(Group.id > after_id)`, `next_after_id`, комментарий T-09-13-01.
- `app/templates/schedules/list.html:40-63` и `app/templates/schedules/partial_cards.html` — сентинел,
  «идентичен побайтово»; `:48` — линейка `sched-count` с `total`.
- `tests/test_pages/test_identifier_bounds.py:1150-1190` — `PAGINATION_EXCLUSION_*`,
  `CATALOGUE_UNIVERSE_DECLARED`.
- `tests/test_pages/test_schedules_list.py` — действующие проверки порций и фильтров.
- `09-VERIFICATION.md` §CR-01 — дефект смещённого курсора.
  </read_first>

  <behavior>
RED: `test_the_next_portion_does_not_skip_a_row_after_a_toggle_under_the_state_filter`: 35 активных
расписаний, фильтр `state=active`; первая порция (30 строк) и адрес её сентинела сняты ДО тумблера →
тумблер паузы строки из первой порции запросом с заголовком htmx со сводного списка (без `return_to`) →
вторая порция по сохранённому адресу сентинела содержит все 5 оставшихся строк. Сегодня смещение
пропускает одну — красный (запись паузы исполняется при любом ответе тумблера, предмет теста — курсор).
GREEN — ниже.
  </behavior>

  <action>
**1. Ключевой курсор (D-11).** `schedules_partial`: параметр смещения заменяется на `after_id: int |
None = Query(None, ge=1, le=ID_MAX)`; к запросу при заданном ключе добавляется `Schedule.id >
after_id` ДО `order_by(Schedule.id).limit(limit + 1)`; контекст отдаёт `next_after_id` = идентификатор
последней строки порции (у кортежей — `page[-1].Schedule.id`) либо `None`. `schedules_list` отдаёт
`next_after_id` первой страницы. Сентинел в `list.html` и `partial_cards.html` — посимвольно
одинаковый адрес `/schedules/partial?after_id={{ next_after_id }}&limit=30` с тем же циклом проброса
фильтров. Комментарий: основание CR-01 Фазы 9, решение D-11; поддельный ключ сужает выборку, но не
отменяет ограничения пользователем (форма T-09-13-01).

**2. Числа.** `PAGINATION_EXCLUSION_DECLARED` и `CATALOGUE_UNIVERSE_DECLARED` в
`test_identifier_bounds.py` двигаются прогоном (ожидаемо 13 → 12 и 35 → 36: смещение ушло, ключ
вошёл во вселенную и несёт границу встроенно) — каждое с летописью. Разметочные числа сентинела —
прогоном.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_schedules_list.py tests/test_pages/test_identifier_bounds.py tests/test_templates/test_htmx_markup_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'offset=' app/templates/schedules/list.html app/templates/schedules/partial_cards.html | grep -c ':0$'` возвращает `2`.
- `diff <(grep 'hx-get="/schedules/partial' app/templates/schedules/list.html) <(grep 'hx-get="/schedules/partial' app/templates/schedules/partial_cards.html)` — пустой вывод.
- `awk '/^async def schedules_partial/,/^@router/' app/pages/schedules.py | grep -c '\.offset('` возвращает `0`.
- `uv run pytest tests/test_pages/test_schedules_list.py -q -k "does_not_skip_a_row"` — зелено.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "sentinel"` — зелено.
  </acceptance_criteria>

  <done>
Курсор сводного списка ключевой; порция не теряет строк после тумблера под фильтром.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Тумблер на сводном списке — строка своего экрана с серверным состоянием</name>

  <files>
app/pages/schedules.py,
app/templates/schedules/includes/schedule_row.html,
app/templates/schedules/partials/schedule_row_response.html (NEW),
tests/test_pages/test_schedules_list.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/pages/schedules.py` — `schedules_toggle` после плана 11-03 (ветка сводного списка отвечает
  переходом без фрагмента).
- `app/templates/schedules/includes/schedule_row.html:40-100` — `id="schedule-row-N"` и форма тумблера.
- `app/templates/account_groups/includes/group_row.html:95-215` — образец тумблера Фазы 9: снятие
  перехвата Alpine, `form_wrapper(... trigger='change', disabled_elt='', sync='this:drop')`, span
  «Применить» с голым `x-data` и `x-init`, основания в комментариях.
- `tests/test_templates/test_htmx_markup_gates.py:2760-2870` — `DISABLED_ELT_EXCEPTIONS` (запись
  `components/form_wrapper.html`, поле `callers`, вызывающий `ads/includes/sched_card.html` из плана 11-03).
- `tests/test_pages/test_htmx_post_pairs.py` — случай ветки сводного списка (`LOCATION`) из плана 11-03.
  </read_first>

  <behavior>
RED:
1. htmx-тумблер со сводного списка (без `return_to`) → 200, тело начинается с
   `<article class="sched-item" id="schedule-row-{id}">`, нет `<!DOCTYPE`; без htmx — 302 `/schedules`.
2. Заблокированное возобновление неполного расписания со списка → 200, строка с флажком без `checked`.
3. Случай модуля пар для строки списка: прежний `LOCATION` плана 11-03 заменяется на `FRAGMENT`, метка
   `id="schedule-row-{id}"`.
GREEN — ниже.
  </behavior>

  <action>
**1. Фрагмент строки списка (D-02, D-11).** Когда признак экрана не редактор, `schedules_toggle` отвечает
`respond(request, redirect=<прежний адрес>, fragment=…)`; сборщик отдаёт
`schedules/partials/schedule_row_response.html` (НОВЫЙ, плоское тело, один верхнеуровневый узел —
вывод `schedule_row(item, user)`); `item` строится ТЕМИ ЖЕ функциями, что и список:
`_summary_query(user.id)` с ограничением `Schedule.id == schedule_id`, `_group_names_for`,
`_build_schedule_items` — второй формы элемента не заводится. Докстринг сборщика называет
следствие D-11 прямо: при активном фильтре состояния переключённая строка остаётся на экране в
новом состоянии, линейка `total` не перерисовывается до перезагрузки — это принятая цена «строка не
исчезает из-под руки».

**2. Разметка строки.** Форма тумблера `schedule_row.html` — блочный вызов `form_wrapper(action=…,
target='#schedule-row-' ~ s.id, swap='outerHTML', trigger='change', disabled_elt='', sync='this:drop')`,
без поля `return_to`; внутри макрос `toggle` с прежним id и span с голым признаком клиентского состояния
и снятием себя при живом htmx, несущий кнопку «Применить»; прежний перехват Alpine по событию изменения
на форме снимается, комментарий называет снятие СЛОВАМИ, не набирая имя атрибута литералом (идиома
`group_row.html`). В `callers` записи `DISABLED_ELT_EXCEPTIONS` добавляется
`schedules/includes/schedule_row.html` строкой летописи D-11.

**3. Числа.** `HX_LOCATION_DESTINATION_CALLS_DECLARED` (ветка сводного списка перестала быть переходом)
и разметочные числа — прогоном, каждое с летописью «Фаза 11, план 11-04».
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_schedules_list.py tests/test_pages/test_htmx_post_pairs.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункт 5 (тумблер списка): на /schedules с фильтром «активные» прокрутить до второй порции, поставить на паузу строку первой порции, затем докрутить — ни одна строка не пропала; двойное быстрое нажатие тумблера даёт один запрос во вкладке Network; фокус остаётся на тумблере. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -c 'x-on:change="$el.submit()"' app/templates/schedules/includes/schedule_row.html` возвращает `0`.
- `grep -c 'schedules/includes/schedule_row.html' tests/test_templates/test_htmx_markup_gates.py` возвращает не меньше `1` (запись в `callers`).
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "disabled_elt"` — зелено.
- `uv run pytest tests/test_schedules_out_of_domain_resume.py tests/test_routes/test_schedules_toggle_detached.py -q` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Тумблер сводного списка подменяет свою строку с серверным состоянием; форма строки живёт по форме
Фазы 9.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | ключ курсора `after_id` и скрытое поле `return_to` подконтрольны клиенту |
| фрагмент → DOM | имена групп и название объявления уезжают в строку списка |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09 | Information Disclosure | поддельный `after_id` | low | mitigate | Ключ ограничен `Query(ge=1, le=ID_MAX)` и сужает выборку внутри ограничения пользователем, не расширяя её (форма T-09-13-01) |
| T-11-39 | Information Disclosure | строка списка по чужому идентификатору | high | mitigate | Элемент строки строится `_summary_query(user.id)` — ограничение пользователем стоит в самом запросе; «не найдено» по-прежнему уходит переходом на тот же адрес на обоих транспортах (план 11-03, случаи модуля пар) |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. `/schedules/partial` на ключе последней строки; строка после тумблера под фильтром не теряется.
2. Тумблер сводного списка отвечает фрагментом своей строки с серверным состоянием.
3. Форма тумблера строки — по форме Фазы 9, отступление цели блокировки записано с основанием D-11.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-04-SUMMARY.md` when done.
</output>
