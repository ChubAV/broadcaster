---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 09
type: execute
wave: 9
depends_on: ["11-08"]
files_modified:
  - app/pages/profile.py
  - app/templates/includes/htmx_config.html
  - tests/test_pages/test_profile.py
  - tests/test_pages/test_htmx_response_contract.py
  - tests/test_pages/test_shell.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
  - .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md
  - app/pages/htmx.py
autonomous: true
requirements: [FORM-08, FORM-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: неделимый коммит свопа из задачи 2 плана 11-06 ревизии 1; вынос формы — план 11-08, разметка формы — план 11-10.
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Правило 422 блока конфигурации несёт `\"swap\": true` — значение, а не седьмой ключ; порядок пяти правил и шесть ключей не тронуты (Находка B, D-03 Фазы 7); своп возвращён ОДНИМ коммитом с первым маршрутом, отдающим 422 с авторским фрагментом (профиль), и только поверх смягчения T-07-13 плана 11-07 — ни на одном коммите фазы своп 422 не включён без обработчика"
    - "Сохранение профиля на htmx отвечает 200 и выводом включаемого шаблона настроек (содержимое для `innerHTML` цели `#profile-settings`, которую ставит план 11-10) плюс внеполосным блоком уведомления `profile_saved` — ПЕРВОЕ поведение ветки «код исхода на фрагменте»; без htmx — прежний 302 `/profile?notice=profile_saved` (D-02)"
    - "Неверный или отсутствующий часовой пояс отвечает 422 на обоих транспортах: htmx — фрагмент формы с текстом «Неверный часовой пояс» и выбором, возвращённым эхом через автоэкранирование, без `<!DOCTYPE`; без htmx — полная страница профиля со статусом 422 и тем же текстом (FORM-08, D-06)"
    - "Утверждение `swap is False` перевёрнуто третьим поколением, литерал правила в `test_shell.py` приведён, и запись `07-05/T-07-13` в `07-SECURITY.md` получает датированное добавление — всё тем же коммитом, что и своп"
    - "Форма профиля в этом плане отправляется без htmx (обёртку ставит план 11-10): фрагментные ветки исполняются `htmx_client`, и промежуточный коммит зелен; путь без htmx меняет только статус ответа на ошибку поля (400 → 422)"
    - "`NOT_YET_CONVERTED_COUNT` 21 → 20, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 8 → 9 — прогоном"
    - "Докстринг `respond` называет первое поведение ветки «код исхода на фрагменте» — сохранение профиля (план 11-09); прежняя формулировка «НИ ОДИН ОБРАБОТЧИК» остаётся названной опровергнутой"
  artifacts:
    - "app/pages/profile.py — profile_post на respond() и respond_field_error()"
    - "app/templates/includes/htmx_config.html — правило 422 со свопом"
    - ".planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md — датированное добавление к T-07-13"
    - "tests/test_pages/test_profile.py — test_profile_save_over_htmx_returns_the_form_and_the_notice, test_an_invalid_timezone_answers_422_with_the_echo_on_both_transports"
  key_links:
    - "правило 422 swap:true ↔ respond_field_error (авторский фрагмент): ломается — либо мёртвая кнопка без перерисовки, либо сток T-07-13"
    - "обработчик RequestValidationError плана 11-07 ↔ правило 422 swap:true: ломается — умолчание фреймворка с эхо ввода свопается в DOM; поэтому своп стоит за предусловием"
    - "includes/profile_settings.html (план 11-08) — один источник формы для страницы, фрагмента успеха и фрагмента 422: ломается — страница и фрагмент расходятся"
  prohibitions:
    - statement: "нового JS не заводится: ни слушателя htmx:beforeSwap, ни расширения response-targets, ни седьмого ключа конфигурации (Находка B, D-03 Фазы 7)"
      status: flagged-unverified
      verification: none
    - statement: "перенацеливание и пересвоп заголовками не применяются как умолчание для 422 (D-16)"
      status: flagged-unverified
      verification: none
    - statement: "новых проверок ввода, кроме существующей проверки часового пояса, не заводится (D-06)"
      status: flagged-unverified
      verification: none
    - statement: "история записи 07-SECURITY.md выше не переписывается — дописывается датированное добавление"
      status: flagged-unverified
      verification: none
    - statement: "исключение 422 в обработчике плашки отказа (`htmx_error_banner.html`) не трогается"
      status: flagged-unverified
      verification: none
---

<objective>
Вторая половина Находки B: правило 422 возвращает своп ОДНИМ коммитом с первым маршрутом, отдающим 422 с
авторским фрагментом, — сохранением профиля. Смягчение T-07-13 уже стоит (план 11-07), выход
`respond_field_error` и включаемый шаблон настроек — план 11-08, поэтому своп не открывает стока.

Purpose: без `"swap": true` FORM-08 невыполним без нового JS; своп без авторского 422 и без смягчения
был бы стоком исполнения скрипта. Профиль — первый потребитель ветки «код исхода на фрагменте».

Output: профиль на слое ответа, своп 422, три запертых места (утверждение, литерал правила, счёт мест),
добавление к `07-SECURITY.md` и поколение докстринга `respond`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-RESEARCH.md
@.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-07-SUMMARY.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-08-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: тесты
`test_profile_save_over_htmx_returns_the_form_and_the_notice`,
`test_an_invalid_timezone_answers_422_with_the_echo_on_both_transports`.
</artifacts_this_phase_produces>

<edge_coverage_fallback>
## Spec-less edge probe — явное допущение

- **FORM-08 — FLAGGED ASSUMPTION:** зонд краёв строку не классифицировал. Края ошибки заполнения
  (отсутствующее поле, враждебное значение в эхо, путь без htmx) покрыты явными `truths` и тестами
  этого плана; край «запрос с испорченным идентификатором» — планом 11-07. Машинного перечня краёв нет.
</edge_coverage_fallback>

<decision_reading>
## Прочтение D-06 и Находки B (записывается, чтобы верификатор не искал заголовков)

D-06 назвал два возможных механизма перерисовки на 422: «точечным `HX-Retarget`/`HX-Reswap` либо
собственным правилом, выбор за планировщиком в рамках D-16», а §Claude's Discretion — «иное, не
расширяющее шесть ключей конфигурации». Находка B измерила по вендоренному файлу, что первый механизм
своп не включает. Выбран второй — значение `"swap": true` у существующего правила 422. Цель 422
совпадает с целью успеха (`#profile-settings` — план 11-10, `#max-connect-step` — план 11-18), поэтому
перечень D-16 заводится планом 11-10 с нулём записей. Кто подписывает своп: решение оркестратора
планирования 2026-09-14 — владельцу не эскалировалось; основание записано в `11-RESEARCH.md` §Open
Questions (RESOLVED), вопрос 1.
</decision_reading>

<scope_note>
## Размер плана (ревизия 2)

Проекция 85000 в пределах смарт-зоны. Задача 1 — неделимый коммит свопа (девять файлов): своп, первый
авторский 422, переписанное утверждение, литерал правила и добавление к записи безопасности обязаны
приземлиться вместе (истина ревизии 1). Разметка формы профиля отнесена к плану 11-10: в одном плане со
свопом она дала бы 12 файлов. Поколение докстринга `respond` — отдельная задача того же плана, потому что
пишется о поведении, уже приземлённом задачей 1.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Первый авторский 422 — профиль на слое ответа и своп правилу 422 одним коммитом</name>

  <files>
app/pages/profile.py,
app/templates/includes/htmx_config.html,
tests/test_pages/test_profile.py,
tests/test_pages/test_htmx_response_contract.py,
tests/test_pages/test_shell.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py,
.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md
  </files>

  <precondition>
Смягчение T-07-13 плана 11-07 и выход плана 11-08 на месте: `grep -n 'exception_handler(RequestValidationError)' app/main.py` печатает одну строку, `uv run pytest tests/test_pages/test_htmx_validation_sink.py -q` зелено, `grep -nE '^async def respond_field_error' app/pages/htmx.py` печатает одну строку и файл `app/templates/includes/profile_settings.html` существует.
  </precondition>

  <read_first>
- `app/pages/profile.py` целиком — `profile_post`: обязательное поле пояса без умолчания, ветка
  успеха с кодом `PROFILE_SAVED`, ветка ошибки полной страницей со статусом 400.
- `app/templates/includes/profile_settings.html` и `app/templates/profile.html` (план 11-08) — контекст
  `timezone_choices`, `selected`, `error`.
- `app/pages/htmx.py:285-354` (`_glue_notice`), `:394-485` (`respond`); `respond_field_error` (план 11-08).
- `tests/test_pages/test_htmx_response_contract.py:193-270` — `test_validation_rule_carries_both_swap_and_error`
  и его докстринг-предписание.
- `tests/test_pages/test_shell.py:1270-1310` — `HTMX_CONFIG` с правилом 422 и комментарий.
- `tests/test_pages/test_notices_surface.py:117-160` — как утверждается приклейка кода к фрагменту.
- `tests/test_pages/test_profile.py`, `tests/test_pages/test_impersonation.py` (запрет правки
  профиля под чужой личностью — не сломать).
- `.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md:155-197` — запись T-07-13.
- `11-07-SUMMARY.md` — хэш коммита смягчения.
  </read_first>

  <behavior>
RED:
1. `test_profile_save_over_htmx_returns_the_form_and_the_notice`: htmx POST `/profile` с годным
   поясом → 200, тело несёт форму `action="/profile"`, выбранный пояс с `selected`, внеполосный блок
   уведомления с текстом записи реестра `profile_saved`; нет `<!DOCTYPE`. Без htmx → 302
   `/profile?notice=profile_saved`.
2. `test_an_invalid_timezone_answers_422_with_the_echo_on_both_transports`: пояс
   `"><script>alert(1)</script>` под htmx → 422, текст «Неверный часовой пояс», нет `<!DOCTYPE`,
   подстрока `<script>alert(1)` отсутствует (экранирована); без htmx → 422, `<!DOCTYPE` есть, текст
   ошибки есть; запрос без поля пояса → тот же ответ 422 (не отказ фреймворка).
3. `test_validation_rule_carries_both_swap_and_error` переписан на `swap is True` — краснеет, пока
   правило не приведено.
GREEN — всё ниже ОДНИМ коммитом.
  </behavior>

  <action>
**1. `profile_post` (D-02, D-06).** Поле пояса получает умолчание пустой строки (отсутствующее поле идёт
веткой ошибки обработчика, а не отказом фреймворка). «Нет сессии» → `respond(request, redirect="/login")`.
Годный пояс → запись → `respond(request, redirect="/profile", notice=notices.PROFILE_SAVED,
fragment=…)`, сборщик рендерит `includes/profile_settings.html` с сохранённым поясом без ошибки.
Негодный → `respond_field_error(request, page=…, fragment=…)`: страница — `profile.html` с `error`
«Неверный часовой пояс» (текст дословно) и `selected` = присланное значение; фрагмент — тот же
включаемый шаблон с тем же контекстом. Зависимость запрета правки под чужой личностью не трогается.
Шаблоны страницы и настроек в этой задаче не трогаются.

**2. Своп правилу 422 (Находка B).** В `includes/htmx_config.html` у правила
`{"code":"422", …, "error": true}` значение своп становится `true`; порядок правил и ключи не
меняются. `tests/test_pages/test_shell.py`: строка правила 422 в `HTMX_CONFIG` — `True`,
комментарий над константой получает поколение («со swap: False правило совпадало с [45]..; с плана
11-09 своп возвращён вместе с первым авторским маршрутом»).

**3. Гейты правила.** `test_validation_rule_carries_both_swap_and_error` утверждает своп и признак
ошибки; докстринг дописывает ТРЕТЬЕ поколение (своп возвращён планом 11-09 одновременно с первым
маршрутом, отдающим 422 с авторским фрагментом; смягчение «swap снят» заменено обработчиком плана 11-07
— ветвь htmx у страничного слоя, JSON-API не тронут — и обходом `test_htmx_validation_sink.py`),
прежние поколения не стираются. `SERVER_SIDE_VALIDATION_RESPONSES` в этой задаче не двигается
(профиль зовёт выход, литерала не пишет) — утверждается прогоном.

**4. `07-SECURITY.md`.** В раздел `07-05/T-07-13` дописывается датированное добавление «Фаза 11, планы
11-07, 11-08 и 11-09»: своп возвращён вместе с первым авторским маршрутом (профиль, план 11-09); прежнее
смягчение «swap снят» заменено смягчением «на пути htmx у обработчиков страничного слоя отказ валидации
фреймворка — пустой 400; ответы JSON-API `app/routes/` не меняются ни на одном транспорте»
(`malformed_request_response`, обход по маршрутам обоих слоёв, гейт адресов шаблонов — план 11-07, хэш
коммита из его сводки); запись выше не правится.

**5. Числа.** Ключ `profile_post` снимается из `NOT_YET_CONVERTED` и добавляется в
`FRAGMENT_RESPONSE_HANDLERS`; 21 → 20 и 8 → 9 прогоном, летопись словами («сохранение часового
пояса в профиле»). Случаи модуля пар: успех (`FRAGMENT`, метка `id="timezone"`), нет сессии
(`LOCATION`, `/login`). `HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном, с летописью.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_profile.py tests/test_pages/test_htmx_response_contract.py tests/test_pages/test_shell.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_notices_surface.py tests/test_pages/test_impersonation.py tests/test_pages/test_htmx_validation_sink.py tests/test_pages/test_hx_location_destinations.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>non-zero exit, or "failed" in the pytest summary line (запись 07-SECURITY.md правится этой задачей)</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -n '"code":"422", "swap": true, "error": true' app/templates/includes/htmx_config.html` печатает одну строку.
- `grep -rn --include='*.py' 'status_code=422' app/ | cut -d: -f1 | sort -u` печатает ровно `app/pages/htmx.py`.
- `awk '/^async def profile_post/,0' app/pages/profile.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -c 'Form(\.\.\.)' app/pages/profile.py` возвращает `0`.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 20$' tests/test_pages/test_htmx_gates.py` печатает одну строку; `grep -nE '^FRAGMENT_RESPONSE_HANDLERS_DECLARED = 9$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `grep -nE '^SERVER_SIDE_VALIDATION_RESPONSES = 2$' tests/test_pages/test_htmx_response_contract.py` печатает одну строку.
- `grep -n 'Фаза 11' .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-SECURITY.md` печатает хотя бы одну строку.
- `uv run pytest tests/test_pages/test_profile.py -q -k "returns_the_form_and_the_notice or answers_422_with_the_echo"` — зелено, 2 теста.
  </acceptance_criteria>

  <done>
Профиль отвечает на htmx фрагментом формы с кодом исхода, на ошибку поля — 422 с эхом на обоих
транспортах; правило 422 свопает поверх смягчения плана 11-07; все три запертых места и запись
безопасности приведены одним коммитом.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Поколение докстринга `respond` — первое поведение ветки «код исхода на фрагменте»</name>

  <files>
app/pages/htmx.py
  </files>

  <read_first>
- `app/pages/htmx.py:394-485` — абзац «КОД ИСХОДА СПОСОБЕН ДОЕХАТЬ И НА ВЕТКЕ ФРАГМЕНТА… НИ ОДИН ОБРАБОТЧИК».
- Результат задачи 1 (`profile_post` зовёт `respond` с фрагментом и кодом исхода).
  </read_first>

  <action>
Абзац докстринга `respond` о коде исхода на ветке фрагмента получает поколение: первым обработчиком,
подающим фрагмент и код, стало сохранение профиля (план 11-09, `app/pages/profile.py::profile_post`);
прежняя формулировка «НИ ОДИН ОБРАБОТЧИК» остаётся названной опровергнутой (верной для дерева, на
котором писалась). Ссылка — именем обработчика и файлом объявления, без номера строки. Перемер чисел
вызовов после всех переводов фазы — план 11-20.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_notices_surface.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'profile_post' app/pages/htmx.py` возвращает не меньше `1`.
- `grep -c 'НИ ОДИН ОБРАБОТЧИК' app/pages/htmx.py` возвращает не меньше `1` (прежняя редакция названа, а не стёрта).
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Докстринг `respond` называет сохранение профиля первым потребителем ветки «код исхода на фрагменте»,
опровергнутая редакция названа.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ 422 → DOM | с этого плана тело ответа 422 подменяет цель запроса htmx |
| браузер → профиль | часовой пояс присылается клиентом, в том числе враждебным значением |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Information Disclosure / XSS | своп тела 422 (переоткрыт возвратом свопа) | high | mitigate | Предусловие задачи 1: смягчение плана 11-07 на месте (обработчик `RequestValidationError`, обход страничного слоя зелен); единственный литерал 422 в `app/*.py` — `respond_field_error` с авторским фрагментом (греп и гейт числа мест); добавление к `07-SECURITY.md` тем же коммитом, что и своп; сводка приводит хэши коммитов смягчения и свопа в порядке приземления |
| T-11-13 | Tampering (XSS) | эхо присланного пояса в фрагменте и странице 422 | medium | mitigate | Только автоэкранирование `select_field`; тест с `"><script>alert(1)</script>` утверждает отсутствие неэкранированной подстроки на обоих транспортах |
| T-11-15 | Elevation of Privilege | правка профиля под чужой личностью | high | mitigate | Зависимость `forbid_when_impersonating` не трогается; `tests/test_pages/test_impersonation.py` в verify |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_response_contract.py tests/test_pages/test_shell.py tests/test_pages/test_htmx_validation_sink.py tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py -q` — после каждой задачи.
- `uv run pytest tests/test_planning/ -q` — после задачи 1.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Своп 422 возвращён только поверх смягчения плана 11-07; ни на одном коммите фазы своп не включён без обработчика.
2. Правило 422 свопает; профиль отдаёт авторский 422 с эхом; литерал 422 в `app/*.py` один.
3. `NOT_YET_CONVERTED_COUNT = 20`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 9`, `SERVER_SIDE_VALIDATION_RESPONSES = 2`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-09-SUMMARY.md` when done.
Сводка обязана привести хэши двух коммитов — смягчения (план 11-07, задача 1) и свопа (этот план, задача 1) — в порядке приземления: это доказательство «сток не открыт ни на одном коммите».
</output>
