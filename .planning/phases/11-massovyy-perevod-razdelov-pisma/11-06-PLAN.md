---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 06
type: execute
wave: 6
depends_on: ["11-05"]
files_modified:
  - app/pages/ads.py
  - tests/test_pages/test_ads_editor.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_pages/test_identifier_bounds.py
autonomous: true
requirements: [FORM-03, FORM-08]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: задачи 2 и 3 плана 11-04 ревизии 1 (180000, 11 файлов, три задачи); создание расписания — план 11-05. Смарт-зона 100000 превышена (advisory) — основание в <scope_note>.
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Создание черновика объявления и его автосохранения идут через `respond()` без изменения поведения: на первом сохранении под htmx ответ несёт `HX-Push-Url: /ads/{id}/edit`, на последующих — не несёт; без htmx — прежние 302 (D-13, ⚠ пункт роадмапа о совместном поведении заголовка и атрибута)"
    - "Атрибут записи адреса в историю на формы редактора не ставится; совместного поведения атрибута и заголовка в фазе нет, и это наблюдение записано тестом по `response.headers`, а не прочтением кода (D-13)"
    - "Два входа модуля объявлений сняты из `VALIDATION_REFUSAL_DIVERGENCES`: 16 → 14; величина вне колонки на удалении и правке объявления идёт веткой «объявления нет» (D-07)"
    - "`NOT_YET_CONVERTED_COUNT` 23 → 21, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 6 → 8, `HX_HEADER_WRITES` остаётся 2 — прогоном"
    - statement: "После создания черновика объявления Back и F5 не предлагают повторить POST, адрес в строке — `/ads/{id}/edit` — ручной UAT фазы, пункт 7"
      verification: backstop
  artifacts:
    - "app/pages/ads.py — ads_create, ads_update на respond(); _save_from_editor отдаёт адрес деградации и сборщик фрагмента; ads_update и ads_delete на PostIdPath"
    - "tests/test_pages/test_ads_editor.py — test_created_draft_keeps_the_push_url_header_after_the_move_to_the_response_layer"
  key_links:
    - "сборщик фрагмента автосохранения → заголовок HX-Push-Url на ВОЗВРАЩАЕМОМ ответе: ломается — адрес не подменяется, и следующее автосохранение создаёт второй черновик"
  prohibitions:
    - statement: "атрибут записи адреса в историю не ставится ни на одну форму фазы (D-13; решение по каждой форме — QUAL-04, Фаза 15)"
      status: flagged-unverified
      verification: none
    - statement: "удаление объявления из списка фрагментом не делается, изъятие D-08 Фазы 10 остаётся в силе (D-04)"
      status: flagged-unverified
      verification: none
---

<objective>
Раздел `ads` (порядок D-15, вместе с `schedules`): создание и правка объявления переезжают на `respond()`
без изменения поведения, сохраняя заголовок `HX-Push-Url` (D-13), и два входа окна 51 модуля объявлений
снимаются работой (D-07).

Purpose: заголовок истории на создании черновика — единственное, что не даёт следующему автосохранению
создать второй черновик; переезд на слой ответа не имеет права его потерять, а совместное поведение с
атрибутом записи адреса роадмап назвал непроверенным.

Output: `ads_create`, `ads_update` через `respond()`; тест заголовка истории; 23 → 21, 6 → 8, 16 → 14.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-RESEARCH.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом:
`test_created_draft_keeps_the_push_url_header_after_the_move_to_the_response_layer`.
</artifacts_this_phase_produces>

<scope_note>
## Размер плана (ревизия 2)

Проекция 120000 НЕКАЛИБРОВАНА (замеров ноль) и выходит за смарт-зону 100000 (advisory). План 11-04
ревизии 1 (11 файлов, три задачи) разрезан по обработчикам: создание расписания — план 11-05, этот план —
модуль объявлений. Отвергнутый шов — окно 51 объявлений (задача 2) отдельным планом: обе стороны шва
стали бы планами из одной задачи, а обе задачи правят один модуль `app/pages/ads.py`, так что отдельный
план перечитывал бы те же обработчики.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Черновик и правка объявления на слое ответа — заголовок истории переживает переезд</name>

  <files>
app/pages/ads.py,
tests/test_pages/test_ads_editor.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_htmx_gates.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/pages/ads.py:405-560` — `_autosave_response` и `_save_from_editor` целиком: чтение признака
  htmx `:461`, ветка htmx с заголовком истории `:543-549`, путь без JavaScript `:551-556`.
- `app/pages/ads.py:584-660` — `ads_create`: создание по пустому полю, владение по `ad_id` из тела,
  ветка недоступного объявления с `INACCESSIBLE_AD_MESSAGE` на htmx; `:720-760` — `ads_update`.
- `app/templates/ads/form.html` — атрибуты формы редактора (`hx-post`, `hx-swap`, синхронизация
  очередью) — чтобы подтвердить, какие маршруты форма зовёт на htmx.
- `app/pages/htmx.py:474-485` — ответ фрагмента возвращается как есть при отсутствии кода исхода.
- `tests/test_pages/test_ads_editor.py` — действующие утверждения автосохранения и 302;
  `grep -n "HX-Push-Url" tests/test_pages/test_ads_editor.py tests/test_pages/test_htmx_gates.py`.
- `11-RESEARCH.md` §«Паттерн 3: HX-Push-Url» (приоритет заголовка над атрибутом, `Mn`).
  </read_first>

  <behavior>
RED:
1. `test_created_draft_keeps_the_push_url_header_after_the_move_to_the_response_layer`: htmx POST
   `/ads/new` с заголовком → 200, `response.headers["HX-Push-Url"] == f"/ads/{id}/edit"`; второй htmx
   POST с `ad_id` созданного → 200 и заголовка нет; без htmx первое сохранение → 302
   `/ads/{id}/edit`, «Сохранить» → 302 `/ads`. Тест пишется до перевода и зелен на сегодняшнем коде —
   RED этой задачи даёт правило отставания: после снятия двух ключей из `NOT_YET_CONVERTED` оно
   краснеет, пока обработчики не зовут слой ответа.
2. Случаи модуля пар: создание черновика (`FRAGMENT`, метка `id="ad-preview"`); правка своего
   (`FRAGMENT`); правка несуществующего (`LOCATION`, `/ads`).
GREEN — перевод.
  </behavior>

  <action>
**1. `_save_from_editor` перестаёт выбирать транспорт (D-02).** Он выполняет прежние шаги (владение
ключами вложений и лимит, снятие вложения, запись, коммит, перечитывание) и возвращает вызывающему
пару: адрес деградации (`"/ads"` при явном «Сохранить», иначе `/ads/{ad.id}/edit`) и нульарный
async-сборщик фрагмента. Сборщик строит `_autosave_response(...)` и, если запись создана этим
запросом, ставит заголовок истории `HX-Push-Url` значением `/ads/{ad.id}/edit` на ТОТ ЖЕ объект,
который возвращает (запись заголовка остаётся одной и в том же модуле — `HX_HEADER_WRITES` не
двигается). Чтения признака htmx и собственной сборки перенаправления в помощнике не остаётся.
Ветка отказа по вложениям (ответ автосохранения с ошибкой) — тоже сборщик фрагмента с адресом
деградации экрана редактора.

**2. `ads_create`.** Каждый выход — `return await respond(...)`: «нет сессии» → `/login`; создание
и обновление — пара от помощника; недоступное объявление → `respond(request, redirect="/ads",
fragment=…)`, где сборщик отдаёт автосохранение с `INACCESSIBLE_AD_MESSAGE` и `ad=None` — ровно
сегодняшний htmx-ответ (он обнуляет скрытое поле идентификатора).

**3. `ads_update`.** «Нет сессии» → `/login`; объявления нет → `respond(request, redirect="/ads")` без
фрагмента: оба транспорта приземляют на список — адрес тот же, что сегодня уезжает 302; вход из
интерфейса недостижим (идентификатор пути рисует сервер). Успех — пара от помощника.

**4. Тесты и числа.** Тест по §behavior; наблюдение D-13 записывается в докстринг теста: атрибута
записи адреса ни на одной форме нет, адрес меняет только заголовок, и он переживает `respond()`.
Ключи `ads_create`, `ads_update` снимаются из `NOT_YET_CONVERTED` и добавляются в
`FRAGMENT_RESPONSE_HANDLERS`; 23 → 21 и 6 → 8 прогоном, летопись словами («создание черновика
объявления», «правка объявления по адресу»). `HX_LOCATION_DESTINATION_CALLS_DECLARED` и разметочные
числа — прогоном.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_editor.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_hx_location_destinations.py tests/test_templates/test_htmx_markup_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункт 7: на /ads/new набрать заголовок (черновик создаётся автосохранением) — адрес в строке сменился на /ads/{id}/edit; нажать Back — браузер не предлагает повторно отправить форму; F5 на редакторе — страница перезагружается GET-ом. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `awk '/^async def ads_create/,/^@router/' app/pages/ads.py | grep -cE 'RedirectResponse|is_htmx'` возвращает `0`.
- `awk '/^async def ads_update/,/^@router/' app/pages/ads.py | grep -cE 'RedirectResponse|is_htmx'` возвращает `0`.
- `awk '/^async def _save_from_editor/,/^@router/' app/pages/ads.py | grep -cE 'RedirectResponse|is_htmx'` возвращает `0`.
- `grep -c 'HX-Push-Url' app/pages/ads.py` возвращает `1`.
- `grep -rn 'hx-push-url' app/templates | wc -l` возвращает `0`.
- `uv run pytest tests/test_pages/test_ads_editor.py -q -k "push_url_header_after_the_move"` — зелено.
- `grep -nE '^(NOT_YET_CONVERTED_COUNT = 21|FRAGMENT_RESPONSE_HANDLERS_DECLARED = 8|HX_HEADER_WRITES = 2)$' tests/test_pages/test_htmx_gates.py` печатает строк: 3.
- `uv run pytest tests/test_pages/test_ads_status.py tests/test_money_perimeter_gate.py -q 2>/dev/null || uv run pytest tests/test_pages/test_ads_status.py tests/test_pages/test_money_perimeter_gate.py -q` — зелено.
  </acceptance_criteria>

  <done>
Черновик и правка объявления идут через слой ответа, htmx-ответы байт-в-байт прежние, заголовок
истории выставлен на возвращаемом ответе при создании; наблюдение D-13 закреплено тестом.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Окно 51 — два входа модуля объявлений, 16 → 14</name>

  <files>
app/pages/ads.py,
tests/test_pages/test_identifier_bounds.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py
  </files>

  <read_first>
- `app/pages/ads.py` — сигнатуры `ads_update` (`ad_id: IdPath`) и `ads_delete` (`ad_id: IdPath`),
  выборка с `Ad.user_id`, ветки «объявления нет».
- `app/pages/identifiers.py` — `PostIdPath`, `id_in_column` (план 11-02).
- `tests/test_pages/test_htmx_gates.py` — две записи модуля объявлений в
  `VALIDATION_REFUSAL_DIVERGENCES` и `VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 16`.
- `tests/test_pages/test_identifier_bounds.py:149-275` —
  `test_ads_delete_refuses_a_value_no_driver_can_hold_with_a_validation_refusal`,
  `test_ads_delete_draws_the_column_boundary_on_both_of_its_sides`, `MISSING_ROW_REDIRECT`.
- `tests/test_pages/test_confirm_delete_transport.py` — маршрут удаления объявления и его случаи.
  </read_first>

  <behavior>
RED: тесты удаления объявления в `test_identifier_bounds.py` ожидают вместо отказа валидации ветку
«объявления нет» (302 `/ads` без htmx; 204 и `HX-Location` `/ads` с htmx) для `2147483648` и 26-значного
числа, а для `ID_MAX` — прежнюю ветку «нет строки»; случай модуля пар для правки с величиной вне
колонки (`LOCATION`, `/ads`). Сегодня 422 — красный.
GREEN — псевдонимы и проверка первым использованием.
  </behavior>

  <action>
`ads_update` и `ads_delete` переходят на `PostIdPath` (D-07); первое использование `ad_id` в каждом —
аргумент `id_in_column`; ложный результат — ветка «объявления нет» этого обработчика (у удаления —
через слой ответа, как у отсутствующей строки; сверка источника остаётся выше и не трогается).
Две записи модуля объявлений снимаются из реестра окна 51; `VALIDATION_REFUSAL_DIVERGENCES_DECLARED`
16 → 14 со строкой летописи (D-07, план 11-06) и текстом покрасневшего правила. Тесты
`test_identifier_bounds.py` переписываются по §behavior с поколением в докстрингах (прежнее
ожидание — отказ валидации, снятый D-07); имена тестов, говорящие «validation refusal», получают
имена, называющие ветку «нет строки», — файл не должен врать о предмете.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_htmx_post_pairs.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 14$' tests/test_pages/test_htmx_gates.py` печатает строк: 1.
- `grep -c 'entry="app/pages/ads.py::POST' tests/test_pages/test_htmx_gates.py` возвращает `0` (запись `OWN_RESPONSE_EXITS` удаления объявления маршрута POST в поле входа не несёт и остаётся).
- `grep -c 'with_a_validation_refusal' tests/test_pages/test_identifier_bounds.py` возвращает `0`.
- `uv run pytest tests/test_pages/test_identifier_bounds.py -q -k "checked_before_its_first_use"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Удаление и правка объявления проверяют идентификатор первым использованием; величина вне колонки
идёт веткой «объявления нет»; реестр окна 51 — 14 записей.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | `ad_id` скрытого поля и пути объявления; ключи вложений |
| слой ответа → история браузера | заголовок `HX-Push-Url` меняет адрес в строке без перезагрузки |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11 | Tampering | значение `HX-Push-Url` | medium | mitigate | Значение собирается из `ad.id` ПЕРЕЧИТАННОЙ записи внутри сборщика, не из запроса; правило `test_every_header_write_has_a_safe_right_operand` зелено, `HX_HEADER_WRITES` = 2 |
| T-11-12 | Tampering | подстановка чужого `ad_id` в автосохранение | high | mitigate | Владение по одному запросу с `Ad.user_id` не меняется; недоступное объявление отдаёт прежний ответ с обнулением идентификатора, записи не создаётся |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. `ads_create`/`ads_update` на `respond()`, заголовок истории на возвращаемом ответе, атрибута записи адреса нет.
2. `NOT_YET_CONVERTED_COUNT = 21`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 8`, `VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 14`, `HX_HEADER_WRITES = 2`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-06-SUMMARY.md` when done.
Сводка записывает наблюдение D-13 (заголовок истории переживает `respond()`, атрибута нет) дословно — его заберёт летопись роадмапа в плане 11-20.
</output>
