---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 13
type: execute
wave: 13
depends_on: ["11-12"]
files_modified:
  - app/pages/admin.py
  - app/pages/htmx.py
  - tests/test_pages/test_admin_panel.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
  - app/templates/admin/includes/user_actions.html
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [FORM-03, FORM-04]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: задача 2 плана 11-07 ревизии 1, разрезанная на ответ обработчика и разметку формы.
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Бесплатный доступ из карточки пользователя на htmx отвечает 200 и содержимым `#user-actions` (`innerHTML`) с подписью кнопки по новому состоянию, плюс внеполосной подменой `#user-block-badge` и `#user-access-tile` тем же шаблоном ответа, что у блокировки; без htmx — прежний 302 на `/admin/users/{id}` (FORM-03, D-02)"
    - "«Пользователя нет» → переход на `/admin/users`, «строки подписки нет» → переход на `/admin/users/{id}` — на htmx 204 + `HX-Location` на тот же адрес, что и 302 (FORM-04)"
    - "Плитка доступа во фрагменте собирается ПОСЛЕ `invalidate_access_cache`: выдача показывает `∞`, а не кэшированный вердикт"
    - "Голый 403 бесплатного доступа объявлен записью `OWN_RESPONSE_EXITS` с `DECISION_OWNER_D08`; девять записей, ждавших владельца, переведены в решённое состояние со ссылкой на D-08; `OWN_RESPONSE_EXITS_DECLARED` 10 → 11; правило авторства зелено (D-08)"
    - "Ответ обработчика приземляется задачей 1 до разметки: пока форма не переведена задачей 2, она отправляется без htmx и получает прежний 302"
    - "`NOT_YET_CONVERTED_COUNT` 19 → 18, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 10 → 11 — прогоном"
    - statement: "В браузере десяток нажатий «Заблокировать/Разблокировать» и «Выдать/Снять бесплатный доступ» подряд без перезагрузки меняют подписи, бейдж и плитку доступа, панели входа под пользователем и удаления не множатся и открываются — ручной UAT фазы, пункт 5"
      verification: backstop
  artifacts:
    - "app/pages/admin.py — admin_toggle_free_access на respond()"
    - "app/pages/htmx.py — докстринг модуля с поколением о собственных выходах"
    - "tests/test_pages/test_htmx_gates.py — запись OWN_RESPONSE_EXITS бесплатного доступа; девять записей в состоянии DECISION_OWNER_D08"
    - "tests/test_pages/test_admin_panel.py — test_free_access_toggle_over_htmx_refreshes_the_access_tile"
  key_links:
    - "invalidate_access_cache → сборка плитки доступа после сброса: ломается — плитка показывает кэшированный вердикт"
    - "форма бесплатного доступа (hx-target=#user-actions) → шаблон ответа плана 11-12: ломается — подпись кнопки не совпадает с состоянием"
  prohibitions:
    - statement: "отказ по источнику не переводится на слой ответа с новым кодом уведомления и переходом — отвергнуто владельцем (D-08)"
      status: flagged-unverified
      verification: none
    - statement: "admin/includes/worker_row.html и queue_row.html не получают id и макросов (D-03, Deferred Ideas)"
      status: flagged-unverified
      verification: none
    - statement: "подтверждение для тумблеров админки не заводится (UI-контракт E5)"
      status: flagged-unverified
      verification: none
---

<objective>
Раздел `admin` (порядок D-15): второй тумблер карточки пользователя — бесплатный доступ — отвечает тем же
фрагментом блока действий, что блокировка, с плиткой, собранной после сброса кэша, и решение владельца
D-08 закрывает вопрос о форме отказа по источнику для всего реестра собственных выходов.

Purpose: плитка печатает вердикт доступа, кэшируемый до минуты; фрагмент, собранный до сброса, показал бы
администратору старое состояние сразу после его изменения.

Output: переведённый `admin_toggle_free_access`, запись D-08 и решённое состояние девяти записей,
поколение докстринга `htmx.py`, форма на `form_wrapper`, 19 → 18, 10 → 11, 10 → 11.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-12-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: запись `OWN_RESPONSE_EXITS` для
`app/pages/admin.py::admin_toggle_free_access`; тест `test_free_access_toggle_over_htmx_refreshes_the_access_tile`.
</artifacts_this_phase_produces>

<scope_note>
## Размер плана (ревизия 2)

Проекция 80000 в пределах смарт-зоны. Один переведённый обработчик на план (разрез плана 11-07 ревизии 1
по обработчикам); ответ отделён от разметки формы по основанию плана 11-12. Отвергнутый шов — обе задачи
отдельными планами: оба стали бы планами из одной задачи.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Бесплатный доступ на слое ответа — плитка после сброса кэша, девять записей реестра получают решение владельца</name>

  <files>
app/pages/admin.py,
app/pages/htmx.py,
tests/test_pages/test_admin_panel.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/pages/admin.py:1596-1695` — `admin_toggle_free_access`: сверка источника, «нет пользователя»,
  «строки подписки нет» с журналом, инверсия, журнал, `invalidate_access_cache`.
- Результат плана 11-12 (шаблон ответа `admin/partials/user_actions_response.html`, `DECISION_OWNER_D08`).
- `tests/test_pages/test_htmx_gates.py:3736-3990` — девять записей и `LIFTING_CONDITION_OWN_RESPONSE`.
- `app/pages/htmx.py:1-44` — докстринг модуля о девяти выходах «ЖДЁТ ВЛАДЕЛЬЦА».
- `tests/test_pages/test_admin_panel.py` — `test_the_free_access_toggle_survives_a_user_without_a_subscription_row`.
  </read_first>

  <behavior>
RED:
1. `test_free_access_toggle_over_htmx_refreshes_the_access_tile`: htmx POST `/admin/users/{id}/unlimited`
   для пользователя со строкой подписки → 200; плитка во внеполосном узле показывает `∞`; подпись
   кнопки — «Снять бесплатный доступ»; нет `<!DOCTYPE`; без htmx — 302 `/admin/users/{id}`.
2. Случаи модуля пар: успех (`FRAGMENT`), строки подписки нет (`LOCATION`, `/admin/users/{id}`),
   пользователя нет (`LOCATION`, `/admin/users`).
3. Правило полноты реестра собственных выходов краснеет на втором тумблере до записи.
GREEN — ниже.
  </behavior>

  <action>
**1. `admin_toggle_free_access` (D-02).** Сверка источника с голым 403 не трогается. Выходы: «пользователя
нет» → `/admin/users`; «строки подписки нет» → журнал `free_access_toggle_without_subscription`
прежний, затем `respond(request, redirect=location)`; успех → запись, журнал `free_access_toggled`,
`invalidate_access_cache` — ДО сборки ответа; `respond(request, redirect=location, fragment=…)` с тем
же шаблоном ответа, что у блокировки (плитка собирается после сброса кэша). Форма в этой задаче не
трогается.

**2. Реестр D-08.** Запись для `admin_toggle_free_access` по форме записи блокировки (`DECISION_OWNER_D08`);
`OWN_RESPONSE_EXITS_DECLARED` 10 → 11 прогоном. Девять прежних записей получают `decision_state =
DECISION_OWNER_D08` (решение владельца D-08 закрывает вопрос о ФОРМЕ отказа по источнику для всего
реестра); `LIFTING_CONDITION_OWN_RESPONSE` получает поколение: вопрос о форме решён владельцем
2026-09-14 (D-08), выход остаётся именованным изъятием, машинный гейт продолжает считать выходы;
прежняя формулировка «принадлежит ВЛАДЕЛЬЦУ, а не фазе» остаётся как верная на своё время.
Константа `DECISION_WAITS_FOR_THE_OWNER` не удаляется — ей пользуется реестр окна 51.

**3. Докстринг модуля `htmx.py`.** Первый абзац получает поколение: собственных выходов — одиннадцать
(планы 11-12 и 11-13; двенадцатый приземлит 11-15), решение о форме принято владельцем (D-08); прежняя
редакция «ДЕВЯТЬ… ЖДЁТ ВЛАДЕЛЬЦА» остаётся названной устаревшей.

**4. Числа.** Ключ `admin_toggle_free_access` снимается из `NOT_YET_CONVERTED`, добавляется в
`FRAGMENT_RESPONSE_HANDLERS`; 19 → 18 и 10 → 11 прогоном, летопись словами («выдача и снятие
бесплатного доступа»). `HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_admin_panel.py tests/test_pages/test_free_access.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def admin_toggle_free_access/,/^@router/' app/pages/admin.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -nE '^OWN_RESPONSE_EXITS_DECLARED = 11$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `grep -c 'decision_state=DECISION_WAITS_FOR_THE_OWNER' <(awk '/^OWN_RESPONSE_EXITS:/,/^OWN_RESPONSE_EXITS_DECLARED/' tests/test_pages/test_htmx_gates.py)` возвращает `0`.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 18$' tests/test_pages/test_htmx_gates.py` печатает одну строку; `grep -nE '^FRAGMENT_RESPONSE_HANDLERS_DECLARED = 11$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -k "own_response or decision"` — зелено.
  </acceptance_criteria>

  <done>
Бесплатный доступ отвечает фрагментом с плиткой, собранной после сброса кэша; реестр собственных
выходов — одиннадцать записей в решённом состоянии по D-08.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Форма бесплатного доступа на обёртке — оба тумблера карточки на htmx</name>

  <files>
app/templates/admin/includes/user_actions.html,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/admin/includes/user_actions.html` — форма бесплатного доступа и блочный вызов обёртки
  у формы блокировки (план 11-12).
- `tests/test_templates/test_htmx_markup_gates.py:3295-3320`, `:4029-4200`.
  </read_first>

  <action>
Форма бесплатного доступа в `user_actions.html` — блочный вызов `form_wrapper(action=…,
target='#user-actions', swap='innerHTML')` с прежней подписью по состоянию, той же формы, что у
блокировки (план 11-12). Формы-триггеры входа под пользователем и удаления не меняются. Разметочные
числа — прогоном, каждое с летописью «Фаза 11, план 11-13» и дословным текстом отказа.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py tests/test_pages/test_admin_panel.py tests/test_pages/test_free_access.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункт 5 (раздел admin): открыть карточку пользователя, десять раз подряд переключить блокировку и бесплатный доступ без перезагрузки; подписи кнопок, бейдж и плитка согласованы после каждого нажатия; панели «Войти под пользователем» и «Удалить» открываются, в DOM по одной. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -c "target='#user-actions'" app/templates/admin/includes/user_actions.html` возвращает `2`.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "swap_target or two_roles or client_state"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Оба тумблера карточки пользователя отправляются через обёртку и подменяют содержимое блока действий.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| администратор → чужая учётная запись | тумблер меняет доступ ЧУЖОГО пользователя |
| сторонний сайт → изменяющий маршрут админки | cookie прикладывается браузером к межсайтовой форме |
| кэш вердикта доступа → плитка | плитка печатает вердикт, кэшируемый до минуты |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-17 | Tampering (CSRF) | тумблер бесплатного доступа | high | mitigate | `is_same_origin` с голым 403 стоит после `require_admin` и до выборки, не меняется (D-08); запись `OWN_RESPONSE_EXITS` с правилом полноты |
| T-11-19 | Repudiation | след привилегированной операции | medium | mitigate | Журналы `free_access_toggled` и `free_access_toggle_without_subscription` остаются перед ответом |
| T-11-20 | Information Disclosure | плитка после выдачи/снятия льготы | medium | mitigate | `invalidate_access_cache` вызывается ДО сборки фрагмента; тест утверждает `∞` в ответе выдачи |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Бесплатный доступ отвечает фрагментом блока действий с плиткой после сброса кэша.
2. `OWN_RESPONSE_EXITS_DECLARED = 11`, все записи в состоянии `DECISION_OWNER_D08`.
3. `NOT_YET_CONVERTED_COUNT = 18`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 11`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-13-SUMMARY.md` when done.
</output>
