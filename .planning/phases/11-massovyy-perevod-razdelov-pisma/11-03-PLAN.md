---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - app/pages/schedules.py
  - app/templates/ads/includes/sched_card.html
  - app/templates/schedules/includes/schedule_row.html
  - app/templates/schedules/list.html
  - app/templates/schedules/partial_cards.html
  - app/templates/schedules/partials/schedule_row_response.html
  - tests/test_pages/test_editor_schedules.py
  - tests/test_pages/test_schedules_list.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_identifier_bounds.py
  - tests/test_pages/test_hx_location_destinations.py
autonomous: true
requirements: [FORM-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  tokens: 170000
  raw_tokens: 170000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Тумблер расписания в редакторе на htmx отвечает 200 и карточкой `#sched-N` (`outerHTML`); раскрытие берётся из скрытого поля `keep_sched` — раскрытая соседняя карточка остаётся раскрытой, переключённая не разворачивается сама (D-12(а))"
    - "Тумблер на сводном списке на htmx отвечает 200 и строкой `#schedule-row-N` (`outerHTML`); экран узнаётся существующим скрытым полем `return_to` (редактор шлёт `editor`, строка списка — нет), и адрес без htmx на обоих экранах прежний (D-02)"
    - "Фрагмент тумблера ВСЕГДА отражает сервер: неполное расписание, которое нельзя возобновить, возвращает карточку или строку в прежнем состоянии, и оптимистично переключённый браузером флажок встаёт обратно (D-11, D-13/D-16 Фазы 9)"
    - "«Не найдено / чужое» и «значения вне области» отвечают на htmx 204 + `HX-Location` на тот же адрес и с тем же кодом `schedule_values_out_of_domain`, что уезжает 302 без htmx (D-11, D-06)"
    - "Обе формы тумблера отправляются `hx-trigger=change` с `hx-sync=this:drop`, прежний перехват Alpine по событию изменения снят, кнопка «Применить» остаётся для мира «Alpine жив, htmx мёртв» и снимает себя при живом htmx; отступление цели блокировки записано в `DISABLED_ELT_EXCEPTIONS` с основанием D-11 (прецедент DIV-09-02)"
    - "`/schedules/partial` листается ключом последней строки `after_id` вместо смещения: после фрагментного тумблера под фильтром состояния следующая порция не пропускает ни одной строки (D-11, CR-01 Фазы 9); разметка сентинела байт-в-байт одинакова в `list.html` и `partial_cards.html`"
    - "`NOT_YET_CONVERTED_COUNT` 25 → 24, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 4 → 5 — прогоном, летопись словами"
    - statement: "В браузере тумблер карточки и строки переключается одним нажатием, двойное нажатие не отправляет два запроса, фокус остаётся на тумблере, раскрытая соседняя карточка не схлопывается, строка под фильтром остаётся под рукой — ручной UAT фазы, пункт 5"
      verification: backstop
  artifacts:
    - "app/pages/schedules.py — schedules_toggle на respond() с двумя экранами; schedules_partial и schedules_list с after_id / next_after_id"
    - "app/templates/schedules/partials/schedule_row_response.html (НОВЫЙ)"
    - "app/templates/ads/includes/sched_card.html, app/templates/schedules/includes/schedule_row.html — тумблер через form_wrapper"
    - "tests/test_pages/test_schedules_list.py — test_the_next_portion_does_not_skip_a_row_after_a_fragment_toggle_under_the_state_filter"
  key_links:
    - "return_to (скрытое поле) → выбор фрагмента: карточка редактора или строка списка; ломается — фрагмент редактора приезжает на сводный список, где цели нет"
    - "after_id сентинела → Schedule.id > after_id: ломается — тумблер под фильтром съедает строку следующей порции"
  prohibitions:
    - statement: "при активном фильтре состояния переключённая строка не исчезает из-под руки переходом — отвергнуто D-11"
      status: flagged-unverified
      verification: none
    - statement: "атрибут записи адреса в историю на формы тумблера не ставится (D-13)"
      status: flagged-unverified
      verification: none
    - statement: "внутри целей свопа — только голый x-data; панели подтверждения остаются снаружи карточек (D-12(б), D-08 Фазы 9)"
      status: flagged-unverified
      verification: none
---

<objective>
Второй обработчик раздела `schedules` — тумблер — переходит на слой ответа на ОБОИХ своих экранах, а
сводный список получает ключевой курсор, без которого фрагментный тумблер под фильтром воспроизвёл
бы CR-01 Фазы 9 (D-11).

Purpose: тумблер — самое частое действие раздела; его ответ обязан подменять ровно строку того
экрана, откуда пришёл, и всегда показывать серверное состояние, иначе оптимистичный флажок врёт.

Output: `schedules_toggle` через `respond()`, две формы тумблера через `form_wrapper`, шаблон строки
ответа, keyset `/schedules/partial`, 25 → 24 и 4 → 5.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-PATTERNS.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-01-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `app/templates/schedules/partials/schedule_row_response.html`;
параметр `after_id` у `schedules_partial` и ключ контекста `next_after_id`;
`test_the_next_portion_does_not_skip_a_row_after_a_fragment_toggle_under_the_state_filter`,
`test_schedule_toggle_over_htmx_keeps_the_expanded_neighbour`,
`test_a_blocked_resume_over_htmx_returns_the_unchanged_card`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Тумблер в редакторе — карточка с серверным состоянием и раскрытием из формы</name>

  <files>
app/pages/schedules.py,
app/templates/ads/includes/sched_card.html,
tests/test_pages/test_editor_schedules.py,
tests/test_pages/test_htmx_gates.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_htmx_post_pairs.py
  </files>

  <read_first>
- `app/pages/schedules.py:1077-1164` — `schedules_toggle`: `resume_blocked`, пауза, ветка
  `next_run_or_none` с кодом `SCHEDULE_VALUES_OUT_OF_DOMAIN`, чтение формы ПОСЛЕ коммита (`:1157`),
  возврат с `_expanded_from_form`.
- `app/pages/schedules.py:275-299` — `_expanded_from_form` (коэрция `keep_sched`).
- `app/templates/ads/includes/sched_card.html` — форма тумблера (сегодня `:122-138`, поля `return_to`,
  `keep_sched`, макрос `toggle` с id `sched-toggle-N`); результат плана 11-01 (макрос статьи, шаблон
  ответа `ads/partials/sched_card_response.html`).
- `app/templates/account_groups/includes/group_row.html:95-215` — образец тумблера Фазы 9: снятие
  перехвата Alpine, `form_wrapper(... trigger='change', disabled_elt='', sync='this:drop')`, span
  «Применить» с голым `x-data` и `x-init`, основания в комментариях.
- `tests/test_templates/test_htmx_markup_gates.py:2760-2870` — `DISABLED_ELT_EXCEPTIONS` (запись
  `components/form_wrapper.html`, поле `callers`) и `:3178-3230` правила перечня.
- `tests/test_pages/test_htmx_gates.py:195-344`, `:1350-1400`.
- `09-CONTEXT.md` D-05, D-07, D-13, D-16; `11-CONTEXT.md` D-11, D-12.
  </read_first>

  <behavior>
RED:
1. `test_schedule_toggle_over_htmx_keeps_the_expanded_neighbour`: две карточки, раскрыта A; htmx-тумблер
   B с `keep_sched=A` → 200, тело — статья `sched-B` СВЁРНУТАЯ (нет формы `/schedules/B/edit`), нет
   `<!DOCTYPE`; без htmx — 302 на `/ads/{ad}/edit?sched=A#sched-A`.
2. `test_a_blocked_resume_over_htmx_returns_the_unchanged_card`: неполное выключенное расписание,
   htmx-тумблер → 200, в карточке флажок без `checked`, в базе `is_active` ложно.
3. Случаи модуля пар: успех (`FRAGMENT`); значения вне области (`LOCATION`,
   `/ads/{ad}/edit?notice=schedule_values_out_of_domain`); не найдено (`LOCATION`, `/schedules`).
GREEN — перевод.
  </behavior>

  <action>
**1. Обработчик.** Форма запроса разбирается ДО записи (разбор тела может поднять исключение, и
пятисотка после выполненной записи неотличима от «ничего не произошло» — основание `WR-07` у
удаления). Признак экрана читается ОДИН раз: `returns_to_editor = form_data.get("return_to") ==
RETURN_TO_EDITOR`; раскрытие — `_expanded_from_form(form_data)`. Выходы: «нет сессии» → `/login`;
расписание не найдено → `respond(request, redirect=_editor_url(returns_to_editor, None))` (переход,
D-11); значения вне области → `respond(request, redirect=f"/ads/{ad_id}/edit",
notice=notices.SCHEDULE_VALUES_OUT_OF_DOMAIN)` (исход, D-06); найдено (переключено ЛИБО
заблокированное возобновление) → `respond(request, redirect=_editor_url(returns_to_editor, ad_id,
expanded), fragment=…)`, где на экране редактора фрагмент — `ads/partials/sched_card_response.html`
с `expanded_id=expanded` (сборщик нульарный, контекст — как в 11-01), а экран списка — задача 2.
Заблокированное возобновление отдаёт фрагмент С НЕИЗМЕНЁННЫМ состоянием: это и возвращает
оптимистичный флажок (D-13/D-16 Фазы 9). Прежние помощники перенаправления редактора тумблером
больше не зовутся.

**2. Разметка тумблера карточки.** Форма тумблера переходит на блочный вызов
`form_wrapper(action='/schedules/' ~ s.id ~ '/toggle', target='#sched-' ~ s.id, swap='outerHTML',
trigger='change', disabled_elt='', sync='this:drop')`; внутри — `return_to`, условный `keep_sched`,
макрос `toggle` с прежним id (QUAL-06 ищет элемент по тому же id) и span с голым признаком
клиентского состояния и снятием себя при живом htmx, несущий кнопку «Применить». Прежний перехват
Alpine по событию изменения на форме СНИМАЕТСЯ; комментарий называет снятие СЛОВАМИ, не набирая имя
атрибута литералом (иначе греп снятия зеленеет на комментарии) — идиома `group_row.html`.

**3. Перечень `DISABLED_ELT_EXCEPTIONS`.** Поле `callers` записи `components/form_wrapper.html`
получает `ads/includes/sched_card.html`; основание пополняется строкой летописи: пустая цель
блокировки и отбрасывающая синхронизация взяты по прецеденту DIV-09-02 решением D-11 Фазы 11.
Число `DISABLED_ELT_EXCEPTIONS_DECLARED` считает записи, а не вызывающих, — двигается, только если
краснеет правило.

**4. Числа.** Ключ `schedules_toggle` снимается из `NOT_YET_CONVERTED` и добавляется в
`FRAGMENT_RESPONSE_HANDLERS`; 25 → 24 и 4 → 5 прогоном, летопись словами («переключение
расписания»). Разметочные числа (`MACRO_DEFINITION_SITES_CALLERS_DECLARED`,
`PARAMETRIC_SWAP_TARGETS_CALLERS_DECLARED`, `CLIENT_STATE_NODES` — голый признак переехал с формы на
span, минус один плюс один) — прогоном, каждое движение с летописью.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'x-on:change="$el.submit()"' app/templates/ads/includes/sched_card.html` возвращает `0`.
- `grep -c "trigger='change'" app/templates/ads/includes/sched_card.html` возвращает `1`; `grep -c "sync='this:drop'" app/templates/ads/includes/sched_card.html` возвращает `1`.
- `awk '/^async def schedules_toggle/,/^@router/' app/pages/schedules.py | grep -cE 'RedirectResponse|_editor_redirect|_editor_error_redirect'` возвращает `0`.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 24$' tests/test_pages/test_htmx_gates.py` печатает строк: 1; `grep -nE '^FRAGMENT_RESPONSE_HANDLERS_DECLARED = 5$' tests/test_pages/test_htmx_gates.py` печатает строк: 1.
- `grep -c 'ads/includes/sched_card.html' tests/test_templates/test_htmx_markup_gates.py` возвращает не меньше `1` (запись в `callers`).
- `uv run pytest tests/test_pages/test_editor_schedules.py -q -k "expanded_neighbour or blocked_resume"` — зелено, 2 теста.
- `uv run pytest tests/test_schedules_out_of_domain_resume.py tests/test_routes/test_schedules_toggle_detached.py -q` — зелено (исход вне области и отвязанное расписание не сломаны).
  </acceptance_criteria>

  <done>
Тумблер в редакторе отвечает карточкой с серверным состоянием и раскрытием из формы, исходы вне
экрана — переходом; форма тумблера живёт по форме Фазы 9; числа 24 и 5.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Тумблер на сводном списке и ключевой курсор — строка остаётся под рукой, порция не теряет строку</name>

  <files>
app/pages/schedules.py,
app/templates/schedules/includes/schedule_row.html,
app/templates/schedules/list.html,
app/templates/schedules/partial_cards.html,
app/templates/schedules/partials/schedule_row_response.html (NEW),
tests/test_pages/test_schedules_list.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_identifier_bounds.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/pages/schedules.py:675-806` — `_summary_query`, `_apply_filters`, `_build_schedule_items`,
  `schedules_partial` (смещение `:765`, `.offset(offset)` `:788`, `next_offset` `:802`); `:808-866` —
  `schedules_list` (`next_offset: PAGE_SIZE`, `total`).
- `app/pages/account_groups.py:285-380` — образец keyset: `after_id: int | None = Query(None, ge=1,
  le=ID_MAX)`, `where(Group.id > after_id)`, `next_after_id`, комментарий T-09-13-01.
- `app/templates/schedules/includes/schedule_row.html:40-100` — `id="schedule-row-N"` и форма тумблера.
- `app/templates/schedules/list.html:40-63` и `app/templates/schedules/partial_cards.html` — сентинел,
  «идентичен побайтово»; `:48` — линейка `sched-count` с `total`.
- `tests/test_pages/test_identifier_bounds.py:1150-1190` — `PAGINATION_EXCLUSION_*`,
  `CATALOGUE_UNIVERSE_DECLARED`.
- `tests/test_pages/test_schedules_list.py` — действующие проверки порций и фильтров.
- `09-VERIFICATION.md` §CR-01 — дефект смещённого курсора.
  </read_first>

  <behavior>
RED:
1. `test_the_next_portion_does_not_skip_a_row_after_a_fragment_toggle_under_the_state_filter`: 35
   активных расписаний, фильтр `state=active`; первая порция (30 строк) → htmx-тумблер паузы строки
   из первой порции → вторая порция по курсору сентинела первой страницы содержит все 5 оставшихся
   строк. Сегодня смещение пропускает одну — красный.
2. htmx-тумблер со сводного списка (без `return_to`) → 200, тело начинается с
   `<article class="sched-item" id="schedule-row-{id}">`, нет `<!DOCTYPE`; без htmx — 302 `/schedules`.
3. Случаи модуля пар для строки списка (`FRAGMENT`, метка `id="schedule-row-{id}"`).
GREEN — ниже.
  </behavior>

  <action>
**1. Фрагмент строки списка.** Когда признак экрана не редактор, сборщик тумблера отдаёт
`schedules/partials/schedule_row_response.html` (НОВЫЙ, плоское тело, один верхнеуровневый узел —
вывод `schedule_row(item, user)`); `item` строится ТЕМИ ЖЕ функциями, что и список:
`_summary_query(user.id)` с ограничением `Schedule.id == schedule_id`, `_group_names_for`,
`_build_schedule_items` — второй формы элемента не заводится. Докстринг сборщика называет
следствие D-11 прямо: при активном фильтре состояния переключённая строка остаётся на экране в
новом состоянии, линейка `total` не перерисовывается до перезагрузки — это принятая цена «строка не
исчезает из-под руки».

**2. Разметка строки.** Форма тумблера `schedule_row.html` — тот же блочный вызов, что в задаче 1, с
`target='#schedule-row-' ~ s.id`, без поля `return_to`; span «Применить» по той же форме; перехват
Alpine по событию изменения снимается, комментарий называет снятие словами. В `callers` записи
`DISABLED_ELT_EXCEPTIONS` добавляется `schedules/includes/schedule_row.html` строкой летописи D-11.

**3. Ключевой курсор (D-11).** `schedules_partial`: параметр смещения заменяется на `after_id: int |
None = Query(None, ge=1, le=ID_MAX)`; к запросу при заданном ключе добавляется `Schedule.id >
after_id` ДО `order_by(Schedule.id).limit(limit + 1)`; контекст отдаёт `next_after_id` = идентификатор
последней строки порции (у кортежей — `page[-1].Schedule.id`) либо `None`. `schedules_list` отдаёт
`next_after_id` первой страницы. Сентинел в `list.html` и `partial_cards.html` — посимвольно
одинаковый адрес `/schedules/partial?after_id={{ next_after_id }}&limit=30` с тем же циклом проброса
фильтров. Комментарий: основание CR-01 Фазы 9, решение D-11; поддельный ключ сужает выборку, но не
отменяет ограничения пользователем (форма T-09-13-01).

**4. Числа.** `PAGINATION_EXCLUSION_DECLARED` и `CATALOGUE_UNIVERSE_DECLARED` в
`test_identifier_bounds.py` двигаются прогоном (ожидаемо 13 → 12 и 35 → 36: смещение ушло, ключ
вошёл во вселенную и несёт границу встроенно) — каждое с летописью. Разметочные числа и
`HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_schedules_list.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_identifier_bounds.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункт 5 (тумблер): на /schedules с фильтром «активные» прокрутить до второй порции, поставить на паузу строку первой порции, затем докрутить — ни одна строка не пропала; двойное быстрое нажатие тумблера даёт один запрос во вкладке Network; фокус остаётся на тумблере. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -c 'offset=' app/templates/schedules/list.html app/templates/schedules/partial_cards.html | grep -c ':0$'` возвращает `2`.
- `diff <(grep 'hx-get="/schedules/partial' app/templates/schedules/list.html) <(grep 'hx-get="/schedules/partial' app/templates/schedules/partial_cards.html)` — пустой вывод.
- `grep -c 'x-on:change="$el.submit()"' app/templates/schedules/includes/schedule_row.html` возвращает `0`.
- `awk '/^async def schedules_partial/,/^@router/' app/pages/schedules.py | grep -c '\.offset('` возвращает `0`.
- `uv run pytest tests/test_pages/test_schedules_list.py -q -k "does_not_skip_a_row"` — зелено.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "sentinel or disabled_elt"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Тумблер сводного списка подменяет свою строку, оба экрана показывают серверное состояние, курсор
списка ключевой и не теряет строк после фрагментного тумблера под фильтром.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | `schedule_id` пути, скрытые поля `return_to` и `keep_sched`, ключ курсора `after_id` — подконтрольны клиенту |
| фрагмент → DOM | имена групп и название объявления уезжают в строку списка |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Information Disclosure | `schedules_toggle` по чужому идентификатору на двух экранах | high | mitigate | Выборка с `Ad.user_id` не меняется; «не найдено» уходит переходом на тот же адрес на обоих транспортах; признак экрана выбирает ТОЛЬКО форму фрагмента, не право на запись — утверждается случаями модуля пар |
| T-11-08 | Tampering | подделанный `keep_sched` / `return_to` | low | mitigate | `keep_sched` коэрцируется к целому и влияет только на раскрытие (редактор перепроверяет принадлежность); `return_to` сравнивается с константой и выбирает одну из двух выписанных форм — адрес не собирается из строки формы (T-02-23) |
| T-11-09 | Information Disclosure | поддельный `after_id` | low | mitigate | Ключ ограничен `Query(ge=1, le=ID_MAX)` и сужает выборку внутри ограничения пользователем, не расширяя её (форма T-09-13-01) |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Тумблер отвечает фрагментом своего экрана с серверным состоянием; «не найдено» и «вне области» — переходом.
2. Формы тумблера — по форме Фазы 9, отступление цели блокировки записано с основанием D-11.
3. `/schedules/partial` на ключе последней строки; строка после фрагментного тумблера под фильтром не теряется.
4. `NOT_YET_CONVERTED_COUNT = 24`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 5`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-03-SUMMARY.md` when done.
</output>
