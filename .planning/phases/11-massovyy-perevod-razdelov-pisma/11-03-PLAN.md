---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - app/pages/schedules.py
  - tests/test_pages/test_editor_schedules.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
  - app/templates/ads/includes/sched_card.html
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [FORM-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: задача 1 плана 11-03 ревизии 1 (170000, 13 файлов), разрезанная на ответ обработчика и разметку формы тумблера; сводный список и курсор — план 11-04.
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Тумблер расписания в редакторе на htmx отвечает 200 и карточкой `#sched-N` (`outerHTML`); раскрытие берётся из скрытого поля `keep_sched` — раскрытая соседняя карточка остаётся раскрытой, переключённая не разворачивается сама (D-12(а))"
    - "Экран узнаётся существующим скрытым полем `return_to` (редактор шлёт `editor`, строка списка — нет); ветка сводного списка в этом плане отвечает 204 + `HX-Location` на `/schedules` — посимвольно тот адрес, что уезжает 302 без htmx; фрагмент строки списка вместе с её разметкой приносит план 11-04, а до него форма строки запросов htmx не шлёт (D-02)"
    - "Фрагмент тумблера ВСЕГДА отражает сервер: неполное расписание, которое нельзя возобновить, возвращает карточку в прежнем состоянии, и оптимистично переключённый браузером флажок встаёт обратно (D-11, D-13/D-16 Фазы 9)"
    - "«Не найдено / чужое» и «значения вне области» отвечают на htmx 204 + `HX-Location` на тот же адрес и с тем же кодом `schedule_values_out_of_domain`, что уезжает 302 без htmx (D-11, D-06)"
    - "Форма тумблера карточки отправляется `hx-trigger=change` с `hx-sync=this:drop`, прежний перехват Alpine по событию изменения снят, кнопка «Применить» остаётся для мира «Alpine жив, htmx мёртв» и снимает себя при живом htmx; отступление цели блокировки записано в `DISABLED_ELT_EXCEPTIONS` с основанием D-11 (прецедент DIV-09-02)"
    - "Ответ обработчика приземляется задачей 1 до разметки: пока форма карточки не переведена задачей 2, она отправляется без htmx и получает прежний 302"
    - "`NOT_YET_CONVERTED_COUNT` 25 → 24, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 4 → 5 — прогоном, летопись словами"
    - statement: "В браузере тумблер карточки переключается одним нажатием, двойное нажатие не отправляет два запроса, фокус остаётся на тумблере, раскрытая соседняя карточка не схлопывается — ручной UAT фазы, пункт 5"
      verification: backstop
  artifacts:
    - "app/pages/schedules.py — schedules_toggle на respond() с двумя экранами"
    - "app/templates/ads/includes/sched_card.html — тумблер карточки через form_wrapper"
    - "tests/test_pages/test_editor_schedules.py — test_schedule_toggle_over_htmx_keeps_the_expanded_neighbour, test_a_blocked_resume_over_htmx_returns_the_unchanged_card"
  key_links:
    - "return_to (скрытое поле) → выбор ответа: карточка редактора или переход сводного списка; ломается — фрагмент редактора приезжает на сводный список, где цели нет"
    - "форма тумблера карточки (hx-target=#sched-N) → ads/partials/sched_card_response.html (план 11-01): ломается — htmx-путь получает целый документ либо панель задваивается"
  prohibitions:
    - statement: "атрибут записи адреса в историю на форму тумблера не ставится (D-13)"
      status: flagged-unverified
      verification: none
    - statement: "внутри целей свопа — только голый x-data; панели подтверждения остаются снаружи карточек (D-12(б), D-08 Фазы 9)"
      status: flagged-unverified
      verification: none
---

<objective>
Второй обработчик раздела `schedules` (порядок D-15) — тумблер — переходит на слой ответа: в редакторе
он отдаёт карточку с серверным состоянием и раскрытием из формы, а форма тумблера карточки живёт по форме
Фазы 9.

Purpose: тумблер — самое частое действие раздела; его ответ обязан подменять ровно карточку того
экрана, откуда пришёл, и всегда показывать серверное состояние, иначе оптимистичный флажок врёт.

Output: `schedules_toggle` через `respond()`, форма тумблера карточки через `form_wrapper`, 25 → 24 и
4 → 5.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-PATTERNS.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-01-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом:
`test_schedule_toggle_over_htmx_keeps_the_expanded_neighbour`,
`test_a_blocked_resume_over_htmx_returns_the_unchanged_card`; запись вызывающего
`ads/includes/sched_card.html` в `DISABLED_ELT_EXCEPTIONS`.
</artifacts_this_phase_produces>

<scope_note>
## Размер плана (ревизия 2)

Проекция 95000 в пределах смарт-зоны. План 11-03 ревизии 1 (13 файлов) разрезан по экранам тумблера:
карточка редактора — этот план, сводный список с ключевым курсором — план 11-04. Внутри плана ответ
обработчика отделён от разметки: правила разметки перечня непереведённых обработчиков не читают (замер
ревизии 2: `NOT_YET_CONVERTED` встречается в `tests/test_templates/test_htmx_markup_gates.py` только в
комментариях), и правила обработчиков разметки htmx у переведённого обработчика не требуют, поэтому ответ
приземляется первым, а форма до задачи 2 получает прежний 302. Отвергнутый шов — обе задачи отдельными
планами: оба стали бы планами из одной задачи.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Тумблер расписания на слое ответа — карточка редактора с серверным состоянием, исходы переходом</name>

  <files>
app/pages/schedules.py,
tests/test_pages/test_editor_schedules.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/pages/schedules.py:1077-1164` — `schedules_toggle`: `resume_blocked`, пауза, ветка
  `next_run_or_none` с кодом `SCHEDULE_VALUES_OUT_OF_DOMAIN`, чтение формы ПОСЛЕ коммита (`:1157`),
  возврат с `_expanded_from_form`; проверка первым использованием плана 11-02.
- `app/pages/schedules.py:275-299` — `_expanded_from_form` (коэрция `keep_sched`); `:404-471` — `_editor_url`.
- `app/pages/schedules.py:1167-1437` — `schedules_delete` и `schedules_update` (план 11-01): образец
  перевода и нульарного сборщика `ads/partials/sched_card_response.html`.
- `tests/test_pages/test_htmx_gates.py:195-344`, `:1350-1400`.
- `tests/test_pages/test_hx_location_destinations.py:296-350` — `HX_LOCATION_DESTINATION_CALLS_DECLARED`.
- `11-CONTEXT.md` D-02, D-06, D-11, D-12.
  </read_first>

  <behavior>
RED:
1. `test_schedule_toggle_over_htmx_keeps_the_expanded_neighbour`: две карточки, раскрыта A; htmx-тумблер
   B с `keep_sched=A` и `return_to=editor` → 200, тело — статья `sched-B` СВЁРНУТАЯ (нет формы
   `/schedules/B/edit`), нет `<!DOCTYPE`; без htmx — 302 на `/ads/{ad}/edit?sched=A#sched-A`.
2. `test_a_blocked_resume_over_htmx_returns_the_unchanged_card`: неполное выключенное расписание,
   htmx-тумблер из редактора → 200, в карточке флажок без `checked`, в базе `is_active` ложно.
3. Случаи модуля пар: успех в редакторе (`FRAGMENT`); значения вне области (`LOCATION`,
   `/ads/{ad}/edit?notice=schedule_values_out_of_domain`); не найдено (`LOCATION`, `/schedules`);
   тумблер со сводного списка без `return_to` (`LOCATION`, `/schedules`).
GREEN — перевод.
  </behavior>

  <action>
**1. Обработчик (D-02, D-06, D-11).** Форма запроса разбирается ДО записи (разбор тела может поднять
исключение, и пятисотка после выполненной записи неотличима от «ничего не произошло» — основание `WR-07`
у удаления). Признак экрана читается ОДИН раз: `returns_to_editor = form_data.get("return_to") ==
RETURN_TO_EDITOR`; раскрытие — `_expanded_from_form(form_data)`. Выходы: «нет сессии» → `/login`;
расписание не найдено → `respond(request, redirect=_editor_url(returns_to_editor, None))` (переход,
D-11); значения вне области → `respond(request, redirect=f"/ads/{ad_id}/edit",
notice=notices.SCHEDULE_VALUES_OUT_OF_DOMAIN)` (исход, D-06); найдено (переключено ЛИБО
заблокированное возобновление) на экране редактора → `respond(request, redirect=_editor_url(True, ad_id,
expanded), fragment=…)`, где сборщик нульарный и отдаёт `ads/partials/sched_card_response.html` с
`expanded_id=expanded` (контекст — как в 11-01); на сводном списке → `respond(request,
redirect=_editor_url(False, ad_id, expanded))` без фрагмента: форма строки в этом плане отправляется без
htmx, и фрагмент строки вместе с её разметкой приносит план 11-04. Заблокированное возобновление
отдаёт фрагмент С НЕИЗМЕНЁННЫМ состоянием: это и возвращает оптимистичный флажок (D-13/D-16 Фазы 9).
Прежние помощники перенаправления редактора тумблером больше не зовутся. Шаблоны в этой задаче не
трогаются.

**2. Числа.** Ключ `schedules_toggle` снимается из `NOT_YET_CONVERTED` и добавляется в
`FRAGMENT_RESPONSE_HANDLERS`; 25 → 24 и 4 → 5 прогоном, летопись словами («переключение
расписания»). `HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном, с летописью.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_hx_location_destinations.py tests/test_schedules_out_of_domain_resume.py tests/test_routes/test_schedules_toggle_detached.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def schedules_toggle/,/^@router/' app/pages/schedules.py | grep -cE 'RedirectResponse|_editor_redirect|_editor_error_redirect'` возвращает `0`.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 24$' tests/test_pages/test_htmx_gates.py` печатает строк: 1; `grep -nE '^FRAGMENT_RESPONSE_HANDLERS_DECLARED = 5$' tests/test_pages/test_htmx_gates.py` печатает строк: 1.
- `uv run pytest tests/test_pages/test_editor_schedules.py -q -k "expanded_neighbour or blocked_resume"` — зелено, 2 теста.
- Разметка в этой задаче не тронута: `git status --porcelain -- app/templates/` — пустой вывод.
  </acceptance_criteria>

  <done>
Тумблер в редакторе отвечает карточкой с серверным состоянием и раскрытием из формы, исходы вне
экрана и сводный список — переходом на прежние адреса; числа 24 и 5.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Форма тумблера карточки на обёртке — отправка по изменению, прежний перехват снят</name>

  <files>
app/templates/ads/includes/sched_card.html,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/ads/includes/sched_card.html` — форма тумблера (сегодня `:122-138`, поля `return_to`,
  `keep_sched`, макрос `toggle` с id `sched-toggle-N`); макрос статьи плана 11-01.
- `app/templates/account_groups/includes/group_row.html:95-215` — образец тумблера Фазы 9: снятие
  перехвата Alpine, `form_wrapper(... trigger='change', disabled_elt='', sync='this:drop')`, span
  «Применить» с голым `x-data` и `x-init`, основания в комментариях.
- `tests/test_templates/test_htmx_markup_gates.py:2760-2870` — `DISABLED_ELT_EXCEPTIONS` (запись
  `components/form_wrapper.html`, поле `callers`) и `:3178-3230` правила перечня; `:4302` — G-11.
- `09-CONTEXT.md` D-05, D-07, D-13, D-16.
  </read_first>

  <action>
**1. Разметка тумблера карточки (D-11, D-12(б)).** Форма тумблера переходит на блочный вызов
`form_wrapper(action='/schedules/' ~ s.id ~ '/toggle', target='#sched-' ~ s.id, swap='outerHTML',
trigger='change', disabled_elt='', sync='this:drop')`; внутри — `return_to`, условный `keep_sched`,
макрос `toggle` с прежним id (QUAL-06 ищет элемент по тому же id) и span с голым признаком
клиентского состояния и снятием себя при живом htmx, несущий кнопку «Применить». Прежний перехват
Alpine по событию изменения на форме СНИМАЕТСЯ; комментарий называет снятие СЛОВАМИ, не набирая имя
атрибута литералом (иначе греп снятия зеленеет на комментарии) — идиома `group_row.html`. ⚠️ G-11:
`sched-N` уже цель правки (план 11-01) — разрешение Фазы 9 у правила не снимается.

**2. Перечень `DISABLED_ELT_EXCEPTIONS`.** Поле `callers` записи `components/form_wrapper.html`
получает `ads/includes/sched_card.html`; основание пополняется строкой летописи: пустая цель
блокировки и отбрасывающая синхронизация взяты по прецеденту DIV-09-02 решением D-11 Фазы 11.
Число `DISABLED_ELT_EXCEPTIONS_DECLARED` считает записи, а не вызывающих, — двигается, только если
краснеет правило.

**3. Числа разметки.** `MACRO_DEFINITION_SITES_CALLERS_DECLARED`, `PARAMETRIC_SWAP_TARGETS_CALLERS_DECLARED`,
`CLIENT_STATE_NODES` (голый признак переехал с формы на span, минус один плюс один) — прогоном, каждое
движение с летописью «Фаза 11, план 11-03».
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py tests/test_pages/test_editor_schedules.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункт 5 (тумблер карточки): в редакторе раскрыть карточку A, переключить тумблер карточки B — A остаётся раскрытой; двойное быстрое нажатие тумблера даёт один запрос во вкладке Network; фокус остаётся на тумблере. Записать в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -c 'x-on:change="$el.submit()"' app/templates/ads/includes/sched_card.html` возвращает `0`.
- `grep -c "trigger='change'" app/templates/ads/includes/sched_card.html` возвращает `1`; `grep -c "sync='this:drop'" app/templates/ads/includes/sched_card.html` возвращает `1`.
- `grep -c 'ads/includes/sched_card.html' tests/test_templates/test_htmx_markup_gates.py` возвращает не меньше `1` (запись в `callers`).
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "disabled_elt or two_roles or client_state"` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Форма тумблера карточки отправляется через обёртку по изменению; отступление цели блокировки записано
с основанием D-11; разметочные числа поставлены прогоном.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | `schedule_id` пути, скрытые поля `return_to` и `keep_sched` — подконтрольны клиенту |
| фрагмент → DOM | имена групп и подпись аккаунта уезжают в карточку |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Information Disclosure | `schedules_toggle` по чужому идентификатору на двух экранах | high | mitigate | Выборка с `Ad.user_id` не меняется; «не найдено» уходит переходом на тот же адрес на обоих транспортах; признак экрана выбирает ТОЛЬКО форму ответа, не право на запись — утверждается случаями модуля пар |
| T-11-08 | Tampering | подделанный `keep_sched` / `return_to` | low | mitigate | `keep_sched` коэрцируется к целому и влияет только на раскрытие (редактор перепроверяет принадлежность); `return_to` сравнивается с константой и выбирает одну из двух выписанных форм — адрес не собирается из строки формы (T-02-23) |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Тумблер в редакторе отвечает карточкой с серверным состоянием; «не найдено», «вне области» и сводный список — переходом.
2. Форма тумблера карточки — по форме Фазы 9, отступление цели блокировки записано с основанием D-11.
3. `NOT_YET_CONVERTED_COUNT = 24`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 5`.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-03-SUMMARY.md` when done.
</output>
