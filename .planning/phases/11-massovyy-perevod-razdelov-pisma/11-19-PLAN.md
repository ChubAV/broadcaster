---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 19
type: execute
wave: 19
depends_on: ["11-18"]
files_modified:
  - tests/test_pages/test_htmx_gates.py
  - app/pages/account_groups.py
  - app/pages/history.py
  - tests/test_pages/test_identifier_bounds.py
  - tests/test_pages/test_account_groups.py
  - tests/test_pages/test_history_retry.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
autonomous: true
requirements: [FORM-08]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 1: задача 2 плана 11-10 прежней нарезки (165000, 15 файлов на план), разрезанная на две задачи.
  # Ревизия 2: план 11-12 ревизии 1 перенумерован в 11-19 без изменения предмета; проекция в пределах смарт-зоны.
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Три контроля реестра окна 51, опиравшиеся на БОЕВЫЕ записи (укороченный перечень, снятые записи админки, пустое основание), стоят на СИНТЕТИЧЕСКИХ входах и записях ДО того, как реестр опустеет: промежуточный коммит зелен при `VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 5`, и зубы каждого правила доказаны без опоры на живые записи"
    - "Пять последних входов окна 51 — четыре у групп аккаунта и один у повтора отправки — сняты работой: первое использование каждого идентификатора — аргумент `id_in_column`, стоящий после сверки источника и до первой выборки; величина вне колонки идёт веткой «нет» обработчика — посимвольно тем же адресом, что отсутствующая запись (неотличимость D-04 Фазы 9)"
    - "`VALIDATION_REFUSAL_DIVERGENCES_DECLARED` 5 → 0 со сводной летописью, и гейт утверждает, что ни один вход POST-обработчика страничного слоя не несёт границы фреймворка — не вакуумно: синтетический вход с границей краснеет с именем (D-07)"
    - "Машинерия реестра окна 51 (тип записи, правила полноты, обоснований, авторства) остаётся и ловит следующую границу фреймворка на POST"
  artifacts:
    - "app/pages/account_groups.py, app/pages/history.py — четыре и один вход на PostIdPath с проверкой первым использованием"
    - "tests/test_pages/test_htmx_gates.py — test_no_page_post_input_keeps_a_framework_bound, test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule, три контроля реестра окна 51 на синтетических входах"
  key_links:
    - "_framework_bounded_post_inputs == {} ↔ VALIDATION_REFUSAL_DIVERGENCES == () при DECLARED = 0: ломается — новая граница фреймворка проезжает без записи"
    - "синтетические контроли ↔ правила полноты, обоснований и авторства реестра: ломается — после опустения реестра правила зеленеют вакуумом (память проекта «RED-гейт зеленеет вакуумом»)"
  prohibitions:
    - statement: "именованное изъятие из D-01 для входов окна 51 не чеканится — расхождение снимается работой (D-07)"
      status: flagged-unverified
      verification: none
    - statement: "422 GET-курсора групп (`test_account_groups.py`) не трогается — это GET, вне окна 51"
      status: flagged-unverified
      verification: none
    - statement: "контракт 422 JSON-API app/routes/ не трогается (D-07)"
      status: flagged-unverified
      verification: none
---

<objective>
Окно 51 закрывается РАБОТОЙ: последние пять входов — у групп аккаунта и повтора отправки — переходят на
проверку первым использованием, реестр расхождения формы отказа пустеет, а его правила сохраняют зубы
на синтетических входах (D-07).

Purpose: после этого плана ни один вход POST-обработчика страничного слоя не отдаёт форму отказа
фреймворку; окно 51 закрыто не изъятием, а работой, и вопрос владельцу о законности расхождения теряет
предмет.

Output: контроли реестра на синтетических входах (задача 1), пять входов на `PostIdPath`, реестр окна
51 = 0 с не-вакуумным правилом пустоты (задача 2).
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `test_no_page_post_input_keeps_a_framework_bound`,
`test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule`, помощник правила пустоты
`_framework_bound_complaints` (`tests/test_pages/test_htmx_gates.py`).
</artifacts_this_phase_produces>

<scope_note>
## Почему закрытие окна 51 — отдельный план

Ревизия 1 планирования: план 11-10 прежней нарезки нёс 15 файлов (порог блокировки проверки планов).
Мастер MAX (план 11-11 ревизии 1, с ревизии 2 — 11-18) и закрытие окна 51 делят только два файла гейтов, поэтому разрезаны по задачам
с последовательными волнами. Внутри плана задача 1 переводит контроли на синтетические входы ДО
опустения реестра — иначе промежуточный коммит был бы красным (контроли опираются на боевые записи).

**Ревизия 2.** План 11-12 ревизии 1 перенумерован в 11-19 без изменения предмета. Три входа модуля
аккаунтов по-прежнему снимаются до этого плана — задачей 3 плана 11-17 (в ревизии 1 — задачей 3 плана
11-10), поэтому реестр к началу задачи 1 несёт те же пять записей.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Контроли реестра окна 51 на синтетических входах — зубы без опоры на живые записи</name>

  <files>
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py:2360-2430` (`_ValidationRefusalDivergence`,
  `DECISION_WAITS_FOR_THE_OWNER`, `LIFTING_CONDITION_VALIDATION_REFUSAL`), оставшиеся пять записей,
  `:2984-3172` (замер `_framework_bounded_post_inputs` — как он узнаёт границу), `:3280-3520` — правила
  и три контроля, опирающиеся на БОЕВЫЕ записи (укороченный перечень, снятые записи админки, пустое
  основание).
- `tests/test_pages/test_htmx_gates.py:2102-2220` — форма отрицательных контролей на синтетических
  модулях `tmp_path`.
  </read_first>

  <behavior>
RED:
1. `test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule`: синтетический модуль с
   POST-обработчиком на `Path(ge=1)` — помощник `_framework_bound_complaints(sources)` называет вход
   поимённо (модуль, маршрут, имя параметра); синтетический модуль с тем же обработчиком без границы
   жалоб не даёт.
2. Три контроля переписаны на синтетические входы и записи: полнота — синтетический вход с границей
   без записи называется поимённо; обоснования — синтетическая запись с пустым полем основания
   называется; авторство — синтетическая запись с состоянием «решено» без `D-NN` и без номера закрытого
   окна называется. Каждый контроль сначала прогоняется на заведомо правильной синтетической паре
   (жалоб нет) — антивакуум.
GREEN — помощник и контроли; реестр в этой задаче остаётся с пятью записями.
  </behavior>

  <action>
Помощник `_framework_bound_complaints(sources)` принимает исходники параметром и возвращает жалобы по
каждому входу POST-обработчика, несущему границу фреймворка (тот же разбор, что у
`_framework_bounded_post_inputs`, без дублирования — замер зовёт помощник либо наоборот). Три контроля,
опиравшиеся на боевые записи, переводятся на синтетические входы и записи по §behavior; их докстринги
получают поколение: «опирались на боевые записи реестра — план 11-19 переводит на синтетические, потому
что реестр пустеет задачей 2». Новый контроль — по §behavior, пункт 1. Правила реестра и число
`VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 5` в этой задаче не двигаются.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 5$' tests/test_pages/test_htmx_gates.py` печатает одну строку (реестр ещё не опустел).
- `grep -nE '^def _framework_bound_complaints' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -k "synthetic_bounded_input or validation_refusal or divergence"` — зелено, включая `test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule`.
- `uv run python -m compileall -q tests` — без ошибок.
  </acceptance_criteria>

  <done>
Правила реестра окна 51 доказывают зубы на синтетических входах и записях; реестр ещё несёт пять
записей, прогон зелен.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Окно 51 закрыто работой — пять последних входов и не-вакуумная пустота реестра</name>

  <files>
app/pages/account_groups.py,
app/pages/history.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_identifier_bounds.py,
tests/test_pages/test_account_groups.py,
tests/test_pages/test_history_retry.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/pages/account_groups.py` — сигнатуры `account_groups_toggle` и `account_groups_delete`
  (`account_id`, `group_id` пути), ветки «аккаунта нет» (`respond(request, redirect="/accounts")`) и
  «группы нет», тройной `WHERE` и неотличимость четырёх случаев удаления.
- `app/pages/history.py:826-999` — `history_retry` (`log_id`), ветка «записи нет».
- `app/pages/identifiers.py` — `PostIdPath`, `id_in_column` (план 11-02).
- Результат задачи 1 (помощник правила пустоты, синтетические контроли).
- `tests/test_pages/test_identifier_bounds.py:916-1048` — матрица входов.
- `tests/test_pages/test_account_groups.py:3250-3272` — 422 GET-курсора (не трогается: это GET).
  </read_first>

  <behavior>
RED:
1. Строки матрицы `test_identifier_bounds.py` для четырёх входов групп и входа повтора ожидают ветку
   «нет» обработчика вместо 422.
2. `test_no_page_post_input_keeps_a_framework_bound`: замер на настоящем дереве пуст — краснеет, пока
   пять входов на ограниченных псевдонимах.
3. Случай модуля пар — повтор отправки с `log_id = 2147483648` (`LOCATION`, адрес ветки «записи нет»
   повтора, посимвольно тот же, что уезжает 302 без htmx).
GREEN — ниже.
  </behavior>

  <action>
**1. Входы.** `account_groups_toggle`, `account_groups_delete` (оба параметра пути) и `history_retry`
переходят на `PostIdPath`; первое использование каждого идентификатора — аргумент `id_in_column`,
стоящий после сверки источника и до первой выборки; ложный результат — ветка «нет» обработчика:
негодный `account_id` — ветка «аккаунта нет», негодный `group_id` — ветка «группы нет» (у удаления —
та же неотличимая форма, что у несуществующей группы; узлы снятия строятся из целого пути, как для
любого отсутствующего идентификатора), негодный `log_id` — ветка «записи нет» повтора.

**2. Реестр окна 51 → 0.** Пять записей снимаются; `VALIDATION_REFUSAL_DIVERGENCES` становится пустым
кортежем; `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` 5 → 0 со сводной летописью: 23 → 16 (11-02,
schedules) → 14 (11-06, ads) → 8 (11-11, admin) → 5 (11-17, accounts) → 0 (11-19, account_groups и
history) — расхождение снято РАБОТОЙ (D-07), изъятие не чеканилось, вопрос владельцу о законности
потерял предмет. `LIFTING_CONDITION_VALIDATION_REFUSAL` получает поколение «исполнено». Машинерия
реестра (тип записи, правила полноты, обоснований, авторства) остаётся — она ловит следующую границу
фреймворка на POST.

**3. Не-вакуумность.** `test_no_page_post_input_keeps_a_framework_bound` утверждает, что помощник
правила пустоты на настоящем дереве не даёт жалоб. Правило «число равно объявленному» на пустом
кортеже зелено ТОЛЬКО вместе с не-вакуумным правилом пустоты и синтетическим контролем задачи 1 —
докстринг это называет (память проекта: «RED-гейт зеленеет вакуумом»). Переходные числа
(`HX_LOCATION_DESTINATION_CALLS_DECLARED`) — прогоном, если двинулись.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_account_groups.py tests/test_pages/test_history_retry.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 0$' tests/test_pages/test_htmx_gates.py` печатает одну строку.
- `awk '/^VALIDATION_REFUSAL_DIVERGENCES:/,/^\)/' tests/test_pages/test_htmx_gates.py | grep -c '_ValidationRefusalDivergence('` возвращает `0`.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -k "framework_bound or validation_refusal or divergence"` — зелено, включая `test_no_page_post_input_keeps_a_framework_bound` и `test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule`.
- `uv run pytest tests/test_pages/test_identifier_bounds.py -q -k "checked_before_its_first_use"` — зелено.
- `uv run pytest tests/test_routes/test_schedules_api_identifier_bounds.py -q` — зелено (JSON-API не тронут).
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Ни один вход POST-обработчика страничного слоя не несёт границы фреймворка; реестр окна 51 пуст, и
пустота доказана синтетическим контролем.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → входы групп и повтора | `account_id`, `group_id`, `log_id` пути — произвольные целые любой величины |
| обработчик → драйвер БД | целое вне int32 роняет запрос драйвером (OverflowError на SQLite, DataError на PostgreSQL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-34 | Denial of Service | переполнение колонки на входах групп и повтора | high | mitigate | `id_in_column` первым использованием (правило каталога `test_every_post_identifier_is_checked_before_its_first_use`); реестр окна 51 пуст с не-вакуумным гейтом и синтетическим контролем |
| T-11-39 | Information Disclosure | различимость «вне диапазона» и «нет строки» у удаления группы и повтора | medium | mitigate | Та же ветка того же обработчика; матрица `test_identifier_bounds.py` и случай модуля пар утверждают посимвольно тот же адрес приземления |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_post_pairs.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Контроли реестра окна 51 доказывают зубы на синтетических входах; промежуточный коммит зелен.
2. Пять последних входов проверяют идентификатор первым использованием; величина вне колонки ≡ «нет».
3. `VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 0` с не-вакуумным правилом пустоты.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-19-SUMMARY.md` when done.
</output>
