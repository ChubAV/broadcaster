---
status: partial
phase: 11-massovyy-perevod-razdelov-pisma
source: [11-VERIFICATION.md]
started: 2026-09-17T09:14:11Z
updated: 2026-09-18T12:00:00Z
checks_declared: 8
# ⚠️ ФОРМА ТЕКУЩЕЙ ЭПОХИ, А НЕ ТОЛЬКО ШАБЛОН GSD. Разделы `Current Test`/`Tests`/`Summary`/`Gaps`
# ведёт `/gsd-verify-work`; разделы «Проверка N» с таблицами отметок ставят артефакт ПОД
# `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` (как обходы фаз 9 и 10),
# а не в число изъятых. Терминальное `status` при пустой отметке краснит прогон.
---

## Current Test

[testing paused — 1 item outstanding]

Все восемь тестов имеют результат `pass`, но отметка проверки 1 пуста: путь оплаты ЮKassa не
пройден, фактический хост `confirmation_url` не записан. Владелец принял пункт без наблюдения
(2026-09-18), и шапка оставлена нетерминальной именно поэтому — см. решение под таблицей
«Отметка о закрытии проверки 1».

[round 2 после партии закрытия гэпов — 5 пунктов ждут человека: отметки проверок 1, 2, 3 пусты
(ответы «pass» без наблюдённого признака); проверка 6 переоткрыта на ПЕРЕНАБЛЮДЕНИЕ — дефект снят
в коде планом 11-21 и охраняется восемью гейтами, но отрисовка не наблюдена (окно 88 `unrun-verify`),
и пункт 5а теперь ОТДЕЛЬНЫЙ взгляд; проверка 8 заведена заново под побочные следствия той же правки]

[2026-09-18: ОТРИСОВКА НАБЛЮДЕНА агентом в Chrome по прямой реплике владельца — отметки проверок 6
и 8 заполнены признаками. Свежесть стенда сверена до замера. Шесть пунктов проверки 6 сошлись,
оба страха пункта 5а не воспроизвелись; найдено третье, чего проверка не ждала, и оно смыкается с
находкой (а) проверки 8. Вердикт «pass / issue» по обоим пунктам — за владельцем, агент только мерил]

## Tests

### 1. Оформление доступа на тестовом ключе ЮKassa при живом htmx
expected: На /billing нажать кнопку оплаты; во вкладке Network у POST /billing/subscribe — 204 и HX-Redirect. Браузер уходит на страницу подтверждения ЮKassa; фактический хост confirmation_url — ровно yoomoney.ru (иначе ответ уедет в /billing?notice=payment_failed). Записать фактический хост. Критерий 2, окно 87; план 11-15 требует этого до слияния.
result: pass

### 2. Прокрутка при создании расписания в длинном списке
expected: Редактор объявления с 8+ расписаниями — прокрутить вниз, нажать «+ РАСПИСАНИЕ». Позиция прокрутки не сброшена; новая карточка появилась в конце #sched-list раскрытой; линейка и сводка показывают новое число.
result: pass

### 3. Alpine после свапов без перезагрузки (UAT пункт 5)
expected: (а) редактор — десяток сохранений и тумблеров карточек при раскрытой соседней; (б) /schedules с фильтром — тумблер строки и докрутка второй порции; (в) карточка пользователя в админке — десяток переключений блокировки и бесплатного доступа; (г) /accounts — «Повторить», опрос account-row-N, панели удаления; (д) мастер MAX — индикатор ~5 с, QR, опрос статуса, пустой телефон из пробелов. Раскрытая карточка не схлопывается; в DOM ровно одна панель sched-del-N / user-imp / user-del на объект, панели открываются с актуальным текстом; подписи, бейдж и плитка согласованы; слушатели не копятся; двойное нажатие тумблера даёт один запрос; ошибка MAX перерисовывает шаг с введённым значением.
result: pass

### 4. Back и F5 после переходов (UAT пункт 7)
expected: /ads/new — набрать заголовок до создания черновика; «Повторить» на /accounts; «Синхронизировать всё» на экране групп; затем Back и F5. Адресная строка сменилась (/ads/{id}/edit, экран групп); Back и F5 не предлагают повторно отправить POST.
result: pass
note: "Части А и В сняты агентом в браузере по реплике владельца; часть Б («Повторить») не наблюдена — нет аккаунта в состоянии «Ошибка синхронизации». Владелец принял пункт без части Б репликой «1» (2026-09-17): тот же путь перехода, что у «Синхронизировать всё», покрыт тестами."

### 5. Профиль: сохранение и 422 в браузере
expected: Сохранить часовой пояс — плашка «сохранено» в области шелла без перезагрузки. Затем через DevTools подложить неверный пояс и отправить — на 422 форма #profile-settings перерисована с текстом «Неверный часовой пояс», без плашки аварии.
result: pass
note: "Снято агентом в браузере по реплике владельца «пятую проверь сам». Ожидаемое сошлось. Два побочных наблюдения, оба достижимы только подменой значения через DevTools: после 422 поле выбора показывает первый вариант (Калининград), а не присланное значение, — повторное «Сохранить» записало бы его; плашка «Настройки сохранены.» от предыдущего успеха остаётся видна рядом с ошибкой поля."

### 6. Лишний зазор под кнопками — и наложение точки на орган (пункт 5а)
expected: Chrome на боевых стилях, целиком: (1) /profile «Сохранить» — высота формы 111 px, не 132; (2) шаг phone мастера MAX «Продолжить» — 111 px, не 129,5; (3) раскрытая карточка расписания «Сохранить расписание» — без полосы 24 px; (4) ряды действий админки, /accounts, шапки групп и расписаний — промежутки ровные; (5) Slow 3G — точка через ~300 мс в правом нижнем углу формы, гаснет по ответу, у строки группы стои́т рядом с тумблером; **(5а) НАЛОЖЕНИЕ** — точка 8 px поверх угла обёрнутой кнопки ряда и поверх ручки ВКЛЮЧЁННОГО тумблера шапки высотой 40 px, нажатие по самому углу во время запроса доходит до органа; (6) панель удаления — точка видна, высота прежняя.
result: pass
resolution: "Закрыта репликой владельца «pass» 2026-09-18 после переотрисовки агентом в Chrome. Шесть пунктов сошлись; оба страха пункта 5а не воспроизвелись (зазор 1 px до дорожки тумблера, сетка проб нажатия побайтно совпала с сеткой без точки). Третья, не предвиденная проверкой находка — точка садится на скруглённый угол кнопки и выедает выемку из силуэта — предъявлена владельцу простыми словами вместе с находкой (а) проверки 8 и ПРИНЯТА той же репликой: чекпоинт называл два исхода, выбран первый."
history_reported: "[круг 1, на дереве ДО правки 11-21] Владелец поручил проверку агенту («шестую тоже проверь сам»). Замер агента в Chrome 152 / macOS на broadcaster.all-torgi.ru: зазор ЕСТЬ во всех трёх формах. Профиль «Сохранить» — форма 132 px, без .form-busy 111 px: лишние 21 px под кнопкой (скрытая точка inline-block открывает строчный бокс под flex-колонкой [data-form]). Правка расписания sched-101 «Сохранить расписание» — форма 1710 px, без точки 1686 px: лишние 24 px (точка — flex-элемент 8 px плюс gap 16 px). MAX «Продолжить» — лишние 18,5 px под кнопкой (форма 129,5 px против 111 px); разметку шага phone агент отрендерил из шаблона и вставил на живую страницу с боевым CSS, потому что /accounts/connect/max при активном MAX #29 уводит на /accounts."
severity: cosmetic
note: "ПЕРЕОТКРЫТА 2026-09-18 НЕ РЕГРЕССОМ, А ЗАКРЫТИЕМ: план 11-21 снял дефект в коде (`form_wrapper.html:163` печатает `class=\"form-wrapper\"`, `app.css:2207-2210` выводит точку из потока), и восемь гейтов краснеют на его возврате — верификатор прогнал их сам. Но ОТРИСОВКА не наблюдена ни разу: замер 2026-09-17 снят на дереве ДО правки. Пункт 5а — новый и отдельный: точка теперь стои́т ПОВЕРХ угла органа, а не после него. Арифметика `11-UI-REVIEW.md` говорит, что оба названных страха не воспроизводятся (тумблеры несут `min-height: 40px` против дорожки 22 px; `.btn { padding: 9px 16px }`), и сама называет себя арифметикой, а не наблюдением, — за наблюдение НЕ засчитывать."

### 7. Решение владельца по прочтениям критериев, принятым по умолчанию
expected: Владелец подтверждает летописи критериев 1, 3, 4, 5 в ROADMAP.md как контракт фазы — либо называет, что вернуть. Прочтения: D-01 (15/5 → 12 обработчиков), D-05 (afterbegin → beforeend в конце списка), D-06 (422 только у профиля и MAX), D-12/D-13 (критерий 5 по разделам, hx-push-url не ставится), D-14 (160 → 158, htmx-половина пары 200/204 по форме ответа). Лично владельцем принято только D-08; остальные написаны агентом по делегированию («решай сам по умолчаниям»), а буквальный текст FORM-07 («afterbegin») и критерия 4 («160», «→ 200») им не соответствует.
result: pass

### 8. Побочные следствия правки 11-21: контраст точки и её место в широкой форме
expected: (а) на шаге phone мастера MAX и на «Синхронизировать всё» экрана групп во время запроса — прочитать точку `--text-muted` `#6a6a78` на залитой `--accent-cta` кнопке; расчётный контраст ~2–3:1 при поле WCAG 1.4.11 в 3:1, замерить при необходимости. Точка либо различима, либо признаётся требующей правки. (б) В широких формах (профиль, форма правки расписания, оплата) — точка стои́т в правом нижнем углу КОРОБКИ ФОРМЫ, то есть в ширину карточки от нажатой кнопки. Признаётся приемлемой либо ставится в очередь на привязку к ряду действий.
result: pass
resolution: "Закрыта репликой владельца «pass» 2026-09-18. (а) Контраст ЗАМЕРЕН, а не оценён: точка `#6a6a78` на `--accent-cta` = rgb(207, 162, 255) даёт 2,6 : 1 при поле WCAG 1.4.11 в 3 : 1 — НИЖЕ порога, и это предъявлено владельцу прямым текстом («плохо различима, 2,6 : 1 при норме 3 : 1»). Владелец принял недобор: кнопка отправки на время запроса блокируется, поэтому точка не единственный признак ожидания. (б) Место в широкой форме воспроизведено (963 px правее кнопки на профиле, у правого края карточки) и признано приемлемым — в очередь на привязку к ряду действий не ставится."
note: "Заведена 2026-09-18 из переаудита `11-UI-REVIEW.md` (находки 4 и 5), а не из обхода: правка 11-21 сняла один дефект и внесла два следствия. Смягчающее обстоятельство ПРОВЕРЕНО ВЕРИФИКАТОРОМ, а не принято на слово: обе формы идут с умолчанием `disabled_elt='find button[type=submit]'`, и единственная кнопка отправки в них блокируется — точка НЕ единственный признак ожидания, вопреки формулировке Pillar 3 ревизии. Смещения точки — усмотрение планировщика, не решение владельца, поэтому пункт (б) в области."

## Проверка 1 — переход в ЮKassa на тестовом ключе (тест 1)

### Отметка о закрытии проверки 1

*(заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается; фактический хост `confirmation_url` — обязательная клетка)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак (код, заголовок, хост) |
|---|---|---|---|
|  |  |  |  |

Решение владельца по проверке 1 (2026-09-18, реплика «принять без наблюдения»): пункт **принят без
прохождения пути оплаты**. Прецедент формы — часть Б проверки 4, принятая владельцем 2026-09-17.

⚠️ **ОТМЕТКА ОСТАВЛЕНА ПУСТОЙ НАМЕРЕННО, И ЭТО НЕ НЕДОСМОТР.** Строка без фактического хоста
`confirmation_url` была бы ровно тем самозаверением, которое запрещает
`tests/test_planning/test_the_walkthrough_cannot_self_certify.py`: обязательная клетка отметки —
наблюдённый хост, а решение владельца наблюдением не является. Поэтому признак не выдуман, а
состояние шапки обхода возвращено в нетерминальное (`partial`) — это второй из двух выходов,
которые называет само правило.

**Почему путь не пройден агентом:** нажатие оплаты на боевом стенде уходит запросом наружу, к
ЮKassa; тестовость ключа агент подтвердить не мог — чтение `.env` закрыто стражем секретов.
Владельцу были предложены три исхода (пройти агентом с разрешения, пройти самому, принять без
наблюдения) — выбран третий.

**Что остаётся неснятым, названо прямо:** критерий 2 окна 87 и требование плана 11-15 («записать
фактический хост `confirmation_url`, иначе ответ уедет в `/billing?notice=payment_failed`») не
подтверждены живым прогоном. Заявка на хост держится кодом и правилами пар, а не наблюдением.

## Проверка 2 — прокрутка при создании расписания (тест 2)

### Отметка о закрытии проверки 2

*(заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| 2026-09-18 | Chrome / Linux, chrome-devtools MCP, broadcaster.all-torgi.ru, окно 1440×900 | агент | **Снято на `/ads/42/edit`.** Страница прокручена в самый низ (`scrollTop` **384** при `scrollHeight` 1284), нажата «+ РАСПИСАНИЕ». После ответа: `scrollTop` **384** — сдвиг **0 px**, позиция не сброшена; `scrollHeight` вырос до 1896. Новая карточка **`sched-191` стои́т ПОСЛЕДНИМ элементом `#sched-list`** (порядок детей: 186, 187, 188, 189, 190, **191**) и приходит **РАСКРЫТОЙ** — единственная форма `/schedules/191/edit` в документе. Линейка `#sched-count`: «5 расписаний» → «**6 расписаний**»; плитка предпросмотра «Расписания» → **6**. Панелей `sched-del-N` ровно по одной на объект (6, дублей нет). Перезагрузки не было: маркер на `window` жив, записей навигации **1**. ⚠️ Условие «8+ расписаний» из текста проверки НЕ выполнено — на стенде их 5 (стало 6); наблюдение снято на прокручиваемом списке из пяти. ⚠️ Признаки снял АГЕНТ, не человек: подписью приёмки эта строка не является. **За собой прибрано:** расписание 191 удалено, список вернулся к пяти |

## Проверка 3 — Alpine после свапов, UAT пункт 5 (тест 3)

### Отметка о закрытии проверки 3

*(заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается; по строке на раздел (а)–(д) допустимо)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| 2026-09-18 | Chrome / Linux, chrome-devtools MCP, broadcaster.all-torgi.ru | агент | **(а) РЕДАКТОР — СНЯТО.** `/ads/42/edit?sched=186`, карточка 186 раскрыта, соседние свёрнуты. Десять сохранений, разведённых по 2,2 с (первый прогон с интервалом 0,9 с дал ОДИН запрос — автосохранение гладит правки, поэтому интервал разведён за порог): **10 запросов `/ads/42/edit`, 50 свапов** (пять целей на ответ, внеполосные). Прирост узлов документа **0** (425 → 425), Alpine-корней **0** (8 → 8) — слушатели не копятся. **Раскрытая карточка не схлопнулась:** форма `/schedules/186/edit` на месте после всех десяти. Панелей `sched-del-N` ровно пять, дублей нет. Документ тот же: маркер жив, записей навигации 1. **(б) /schedules — ЧАСТИЧНО.** Тумблер строки `schedule-toggle-100`: **12 переключений → ровно 12 запросов**, прирост узлов 0, Alpine-корней 0, документ тот же. **Двойное нажатие в один тик → ровно ОДИН запрос.** Состояние тумблера возвращено в исходное (`false`). ⚠️ **Докрутка второй порции НЕ НАБЛЮДЕНА:** на стенде 7 расписаний, сентинел подгрузки не отрисован — наблюдать нечего. **(в), (г), (д) НЕ НАБЛЮДЕНЫ, причины названы поимённо:** (в) карточка пользователя в админке — десяток переключений блокировки и бесплатного доступа затронул бы учётную запись ЖИВОГО пользователя на боевом стенде (`/admin/users/2`), и без отдельного разрешения владельца агент этого не делал; (г) «Повторить» на `/accounts` — ни один из трёх аккаунтов не в состоянии «Ошибка синхронизации», кнопки на странице нет (та же причина, что у части Б проверки 4); (д) мастер MAX — `/accounts/connect/max` при активном MAX #29 уводит на `/accounts`, живьём шаг не открывается. ⚠️ Признаки снял АГЕНТ, не человек: подписью приёмки эта строка не является |

## Проверка 4 — Back и F5, UAT пункт 7 (тест 4)

### Отметка о закрытии проверки 4

*(заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| 2026-09-17 | Chrome 152 / macOS (агент, chrome-devtools MCP, broadcaster.all-torgi.ru; `workflow.live_dom_uat` = false — браузер вёл агент по прямой реплике владельца «проверь сам через mcp chrome dev») | агент | **(А) черновик:** на `/ads/new` набран заголовок → `POST /ads/new` 200 с `HX-Push-Url: /ads/66/edit`, `htmx:pushedIntoHistory`, адрес `/ads/66/edit` без перезагрузки, скрытое `ad_id` = 66; следующие автосохранения (ещё 4 POST) — в ту же запись, нового черновика нет. F5 → документ `GET /ads/66/edit` 200, заголовок на месте; Back → документ `GET /ads/new` 200; ни одного POST документом. Повтор на черновике 67 — то же. **(В) «Синхронизировать всё»** на MAX #29 (групп вне расписаний): `POST /accounts/29/sync-groups` 204 + `HX-Location: /accounts/29/groups` → `GET` 200, `pushedIntoHistory`, история 17 → 18, плашка «найдено 3, новых 0, обновлено имён 0, не найдено 1». F5 → документ `GET` 200; Back → документ `GET` 200; POST повторно не ушёл. **(Б) «Повторить» НЕ НАБЛЮДЕНА:** ни у одного из трёх аккаунтов нет состояния «Ошибка синхронизации», кнопки на `/accounts` нет. ⚠️ Признаки снял АГЕНТ, не человек: подписью приёмки эта строка не является. Побочные следы на стенде: черновики 66 и 67, у MAX #29 одна группа помечена «не найдено». В первом прогоне (А) заголовок в поле однажды пропал до второго набора; повтор с журналом `beforeinput`/`input` и OOB-свопов не воспроизвёл — узел поля не подменялся, выделение не ставилось; причина не установлена, признаком продукта не записано |

Решение владельца по части Б (2026-09-17, реплика «1»): пункт засчитан без наблюдения «Повторить» — на стенде нет аккаунта с ошибкой синхронизации; переход `HX-Location` у `accounts_retry_sync` тот же, что у наблюдённой части В, и покрыт тестами пар.

## Проверка 5 — профиль: сохранение и 422 (тест 5)

### Отметка о закрытии проверки 5

*(заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| 2026-09-17 | Chrome 152 / macOS (агент, chrome-devtools MCP, broadcaster.all-torgi.ru; `workflow.live_dom_uat` = false — браузер вёл агент по прямой реплике владельца «пятую проверь сам») | агент | **Сохранение:** «Сохранить» с тем же поясом `Europe/Moscow` → `POST /profile` 200, свап в `#profile-settings`, внеполосно в `#notice` — `alert--success` «Настройки сохранены.»; тот же документ (маркер на `<html>` жив, записей навигации 1). **422:** в `<select id="timezone">` добавлен и выбран `Mars/Olympus_Mons` → `POST /profile` 422, `htmx:beforeSwap` `shouldSwap: true`, `isError: true`; в `#profile-settings` перерисована форма с `alert--error` «Неверный часовой пояс»; `#htmx-failure-server` и `#htmx-failure-network` остались `hidden`; документ тот же. После F5 сохранённый пояс — `Europe/Moscow`, ошибок нет. ⚠️ Признаки снял АГЕНТ, не человек: подписью приёмки эта строка не является. Побочно: после 422 поле выбора стоит на первом варианте (`Europe/Kaliningrad`), присланного значения в списке нет; плашка успеха предыдущего сохранения видна рядом с ошибкой поля — оба случая достижимы только подменой через DevTools |

## Проверка 6 — зазор под кнопками (тест 6)

### Отметка о закрытии проверки 6

*(заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| 2026-09-18 | Chrome / Linux, chrome-devtools MCP, broadcaster.all-torgi.ru, окно 1440×900; `workflow.live_dom_uat` = false — браузер вёл агент по прямой реплике владельца «используй mcp chrome dev если можешь» | агент | **Свежесть стенда проверена ПЕРЕД замером:** `app.css?v=12346d8c0f1f` со стенда содержит `.form-wrapper { position: relative }` и `.form-wrapper > .form-busy { position: absolute; right: 0; bottom: 0; pointer-events: none }`, исключение `[data-group-row] form[action$="/toggle"] > .form-busy { position: static }` на месте; форма профиля печатает `class="form-wrapper"`. Замер 2026-09-17 снят до правки, этот — после. **Приём замера:** высота формы до и после удаления узла точки из DOM; «delta 0» = точка не занимает места. **(1) /profile «Сохранить»** — форма **111 px**, без точки 111 px, delta **0** (было 132). **(2) шаг phone мастера MAX «Продолжить»** — **111 px**, delta **0** (было 129,5; лишние 18,5 сняты). ⚠️ Шаг ВОСПРОИЗВЕДЁН, а не пройден живьём: `/accounts/connect/max` при активном MAX #29 уводит на `/accounts`, поэтому шаблон `accounts/includes/max_connect_step.html` отрисован проектным Jinja (`step="phone"`) и вставлен на живую страницу с боевым CSS — тот же приём, что в круге 1. Печатается `class="form-wrapper"`, форма блочная `position: relative`, колонка на внутреннем `.connect-step__form` (flex, gap 14). **(3) раскрытая карточка расписания** (`/ads/42/edit?sched=186`, форма `/schedules/186/edit`, flex-колонка gap 16) — **369 px**, без точки 369 px, delta **0**: полосы 24 px нет. **(4) ряды действий** — админка `/admin/users/2`: промежутки **9, 9, 9 px**, ширина обёрнутых форм от точки не растёт (dW **0**); `/schedules`: семь шапок `sched-item__head`, промежутки **12, 12 px** в каждой, dW **0**; шапки групп `/accounts/29/groups` — строки групп идут по объявленному исключению (точка `static`, рядом с тумблером), как принято отметкой 6.1.2 Фазы 9. **(5) Slow 3G, живой POST** `/accounts/29/groups/1175/toggle` с выборкой по кадрам: запрос ушёл на 3 мс, точка невидима до **298 мс**, проступила на **315 мс** (порог 300 мс + кадр), полная непрозрачность на 432 мс, `htmx:afterSwap` на 2085 мс, точка скрыта на **2088 мс** — гаснет по ответу, координата за весь полёт не сдвинулась (x 1240,1 / y 417,5, рядом с тумблером строки). **(5а) НАЛОЖЕНИЕ — снято раздельно, оба страха НЕ воспроизвелись, но найдено третье.** Ручка тумблера: у шапки расписания `label.toggle` 38×40 несёт видимую дорожку `.toggle__track` 38×**22** (y 634,1–656,1), точка стои́т y 657,1–665,1 — **зазор 1,0 px, пересечения с дорожкой нет**; арифметика `11-UI-REVIEW.md` подтвердилась наблюдением. Нажатие по углу: точка несёт `pointer-events: none`, и сетка 4×4 проб `elementFromPoint` по её площади **побайтно совпала с той же сеткой при удалённом из DOM узле точки** — где кнопка есть, проба возвращает `button.btn` в обоих случаях; недобор самого угла даёт `border-radius: 99px` кнопки-пилюли, а не индикатор. Подпись не закрыта, край карточки не срезает: на профиле карточка обрезает по x 1408, точка кончается на 1387 — запас 21 px. **ТРЕТЬЕ, чего проверка не ждала:** бокс точки 8×8 пересекает габарит обёрнутой кнопки **целиком (64 px², полное перекрытие)** и на глаз садится НА скруглённый край пилюли, выедая из силуэта заметную выемку — снято в увеличении 6× на «Синхронизировать всё» (`btn--primary`), 3,4× на ряду админки (`Снять бесплатный доступ`, `Заблокировать` — при этом третья, необёрнутая кнопка ряда «Войти под пользователем» такой бусины не несёт, асимметрия видна) и 2,6× на «Продолжить» шага MAX (`btn--block`). Читается это не признаком занятости, а дефектом края кнопки. **(6) панель удаления** (`/accounts`, `/accounts/5/delete`) — форма `modal__form`, НЕ `form-wrapper`: новое правило её не достаёт, точка осталась `position: static` в потоке и даёт те же **22 px** (8 px элемент + gap 14), высота панели 89,1 против 67,1 без точки. Принятая высота панели (решение владельца 1, отметка 3.5 обхода Фазы 10) не тронута, точка видна. ⚠️ Признаки снял АГЕНТ, не человек: подписью приёмки эта строка не является |

Решение владельца по проверке 6 (2026-09-18, реплика «pass»): пункт закрыт. Выемка точки на
скруглённом углу обёрнутой кнопки — третья находка, которой проверка не ждала, — предъявлена
владельцу простыми словами вместе с находкой (а) проверки 8 и принята. Признаки замера снял агент;
эта строка — приёмка владельцем результата, а не подпись под наблюдением.

## Проверка 7 — решение владельца по прочтениям критериев (тест 7)

### Отметка о закрытии проверки 7

*(заполняет владелец; отметка без названного решения по каждому из D-01, D-05, D-06, D-12/D-13, D-14 закрытием не считается)*

| Дата | Кто решал | Решение (подтверждено / что вернуть) |
|---|---|---|
| 2026-09-17 | владелец (`chubav`), реплика «pass» на чекпоинт проверки 7 в `/gsd-verify-work 11` | **Подтверждено, возвращать нечего:** D-01 (15/5 → 12 обработчиков) — подтверждено; D-05 (afterbegin → beforeend в конце списка) — подтверждено; D-06 (422 только у профиля и MAX) — подтверждено; D-12/D-13 (критерий 5 по разделам, hx-push-url не ставится) — подтверждено; D-14 (160 → 158, htmx-половина пары 200/204 по форме ответа) — подтверждено. Летописи критериев 1, 3, 4, 5 в ROADMAP.md приняты контрактом фазы. Чекпоинт предлагал два исхода — подтвердить либо назвать, что вернуть; выбран первый. Строку записал агент дословным переносом реплики владельца |

## Проверка 8 — контраст точки и её место в широкой форме (тест 8)

### Отметка о закрытии проверки 8

*(заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| 2026-09-18 | Chrome / Linux, chrome-devtools MCP, broadcaster.all-torgi.ru, окно 1440×900; браузер вёл агент по прямой реплике владельца | агент | **(а) КОНТРАСТ ЗАМЕРЕН, ОЦЕНКА ПРОВЕРКИ ПОДТВЕРДИЛАСЬ.** Наивный разбор здесь врёт: `--accent-cta` объявлен как `oklch(0.79 0.14 305)`, и выдёргивание трёх первых чисел дало бы 1,24 : 1 — мусор. Цвета приведены к sRGB самим браузером (`OffscreenCanvas` + `getImageData`): кнопка = **rgb(207, 162, 255)**, точка `--text-muted` = **rgb(106, 106, 120)**. Относительная яркость по WCAG → **2,6 : 1** при поле 1.4.11 в **3 : 1**. Это ниже порога; расчёт проверки («~2–3 : 1») сошёлся. Затронуты обе названные точки вызова: «Синхронизировать всё» шапки групп (`btn btn--primary`, 199×40) и «Продолжить» шага phone мастера MAX (`btn--primary btn--block`, 378×33). Для сравнения: на тёмной заливке карточки `#0e0e14` та же точка даёт **3,61 : 1** (проходит), и на кнопках ряда админки (`rgb(15, 15, 22)`) — тоже **3,59 : 1**. То есть недобор ровно на светлой залитой CTA-кнопке, а не везде. На глаз в увеличении 6×: точка читается тёмной выемкой на сиреневой пилюле, причём часть бусины выходит за скруглённый край на тёмный фон и там различается лучше, чем на самой кнопке. **(б) МЕСТО В ШИРОКОЙ ФОРМЕ — воспроизведено как описано.** `/profile`: форма 1072 px шириной, «Сохранить» кончается на x 415,7, точка стои́т на x 1379–1387 — **963,3 px правее кнопки**, у правого нижнего угла КОРОБКИ ФОРМЫ. Карточка обрезает по x 1408, точка не срезана (запас 21 px). На снимке страницы кнопка у левого края, точка — у правого края карточки; связь с нажатой кнопкой на глаз не читается. Та же картина у формы правки расписания (`/schedules/186/edit`: точка x 907,6 / y 513,1 при коробке формы до x 915,6 / y 521,1). ⚠️ Признаки снял АГЕНТ, не человек: подписью приёмки эта строка не является. Смягчающее обстоятельство прежнее и проверено: обе формы идут с `hx-disabled-elt="find button[type=submit]"`, кнопка отправки на время запроса блокируется, поэтому точка не единственный признак ожидания |

Решение владельца по проверке 8 (2026-09-18, реплика «pass»): оба пункта закрыты. Недобор контраста
(2,6 : 1 против поля 3 : 1) назван владельцу прямым текстом до решения и принят при смягчающем
обстоятельстве — блокировке кнопки отправки на время запроса. Место точки в широкой форме признано
приемлемым; в очередь на привязку к ряду действий не ставится. Признаки замера снял агент; эта
строка — приёмка владельцем результата, а не подпись под наблюдением.

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-11-6
  truth: "Под кнопками форм «Продолжить» (MAX), «Сохранить» (профиль) и «Сохранить расписание» нет лишнего вертикального зазора от скрытого .form-busy"
  status: closed
  closed_by: "План 11-21, первая партия закрытия гэпов (`ec33c8e` RED, `810eaa7` GREEN, `bf3cb65` гейты), 2026-09-17"
  closure_note: "ЗАКРЫТ В КОДЕ И ОХРАНЯЕТСЯ МАШИННО, НО ОТРИСОВКА НЕ НАБЛЮДЕНА. Взято ровно то `missing`, что записано ниже: точка выведена из потока ОДИН раз в макросе и таблице стилей (`form_wrapper.html:163` печатает `class=\"form-wrapper\"` — единственное вхождение в дереве шаблонов; `app.css:2207-2210` даёт форме контекст позиционирования и кладёт точку в правый нижний угол коробки формы с нулевыми смещениями и `pointer-events: none`), базовое правило `.form-busy` осталось единственным с этим селектором и сохранило `display:inline-block`, новое правило ОБЛАСТНОЕ. Область — ТРЕТЬЕ имя, не класс индикатора и не значение `hx-indicator`: панель подтверждения печатает тот же узел тем же селектором поиска, и правило по классу индикатора сдвинуло бы все 18 мест подтверждения, чьи ~22 px ПРИНЯТЫ (решение владельца 1, 10-UAT 3.5). Панель доказанно не тронута: `modal.html` без правок, `IN_FLOW_INDICATOR_EXCEPTIONS_DECLARED = 1` — объявленное исключение точки строки группы (решение владельца 3). Проверка 6 обхода переведена в `pending` на ПЕРЕНАБЛЮДЕНИЕ, окно 88 `unrun-verify` открыто; ревизия кода назвала четыре способа обойти новые гейты (WR-04…WR-07 `11-REVIEW.md`), правкой этой партии не охваченные."
  reason: "Agent measured (owner delegated: «шестую тоже проверь сам»): extra space under the button in all three forms — profile 21 px, schedule edit sched-101 24 px, MAX phone step 18.5 px (MAX markup rendered from the template and injected into the live page, since /accounts/connect/max redirects while MAX #29 is active). Taking .form-busy out of flow removes exactly that space."
  severity: cosmetic
  test: 6
  root_cause: "components/form_wrapper.html always prints <span class=\"form-busy\"> as the last IN-FLOW child of every wrapped form (after a whitespace node), and the base rule .form-busy (app.css:2154) gives it an 8x8 display:inline-block box hidden only by opacity/visibility — the box keeps its layout space, nothing takes it out of flow. In a block form whose content is a flex column (profile [data-form], MAX .connect-step__form) it opens an anonymous line box under the column (21 / 18.5 px); in a flex-column form (schedule edit form, gap 16) it is an 8 px flex item plus one gap (24 px); in flex action rows it widens each wrapped form by ~12 px, making gaps uneven. Harmless in Phase 9 (one inline-flex toggle caller); Phase 11 moved 13 more forms onto the macro and 11-10/11-18 moved the column from <form> to an inner div."
  artifacts:
    - path: "app/templates/components/form_wrapper.html"
      issue: "busy span always an in-flow last child of the form, whitespace before it"
    - path: "app/static/css/app.css"
      issue: ".form-busy (2154-2160) is an 8x8 inline-block box hidden by visibility/opacity only; no out-of-flow rule"
    - path: "app/templates/includes/profile_settings.html"
      issue: "column layout on inner [data-form], form itself is block — exposes the line box (21 px)"
    - path: "app/templates/accounts/includes/max_connect_step.html"
      issue: "column layout on inner .connect-step__form — same line box (18.5 px)"
    - path: "app/templates/ads/includes/sched_card.html"
      issue: "edit form (line 210) is a flex column with gap 16 — span adds 8 px item + 16 px gap (24 px); toggle form (167) in .sched-card__head widened ~12 px"
    - path: "app/templates/components/modal.html"
      issue: "prints its own .form-busy span in flex-column .modal__form (gap 14) — ~22 px in all 18 confirmation panels; caught by any global .form-busy fix (owner decision: height of an accepted panel)"
  missing:
    - "Take the busy indicator out of flow once, in the macro/CSS rather than per caller: positioning context on the wrapped form plus an absolutely positioned dot with explicit offsets, kept inside the form box (.card has overflow:hidden)"
    - "Keep the base .form-busy rule as the only rule with that exact selector and keep display:inline-block in it (test_the_indicator_class_is_self_sufficient reads the last matching rule); scope any new rule, e.g. form > .form-busy"
    - "Keep both transition strings, the .form-busy.htmx-request selector, the verbatim span markup in form_wrapper.html and modal.html, WRAPPER_QUALITY_ATTRS appearing once, 16 component files, and the schedule edit form's flex column with gap 16"
    - "If a class is added to the wrapper's <form>, update the allowed-quality-difference reason «имя класса формы» (test_every_allowed_quality_difference_carries_a_reason)"
    - "Preserve accepted observations: dot beside the group-row toggle (09-UAT 6.1.2) and inside the modal panel (10-UAT 3.5)"
    - "Row/header call sites with uneven gaps: admin/includes/user_actions.html:70,83; accounts/list.html:133; accounts/partial_cards.html:87; accounts/partials/sync_status_card.html:109; account_groups/list.html:104; ads/includes/sched_card.html:167; schedules/includes/schedule_row.html:106"
  debug_session: ".planning/debug/form-busy-extra-space-under-buttons.md"
