---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 08
type: execute
wave: 8
depends_on: ["11-07"]
files_modified:
  - app/pages/notices.py
  - app/pages/admin.py
  - app/templates/admin/queue.html
  - app/pages/billing.py
  - tests/test_pages/test_confirm_delete_transport.py
  - tests/test_pages/test_admin_panel.py
  - tests/test_pages/test_notices_channel.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_pages/test_htmx_post_pairs.py
  - .planning/PROJECT.md
autonomous: true
requirements: [FORM-04]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Снятие задачи из очереди приземляет на `/admin/queue` с кодом закрытого реестра `?notice=` на обоих транспортах (302 без htmx; 204 и `HX-Location` с htmx), и исход рисует общая область уведомлений шелла (D-10, D-12 Фазы 8)"
    - "Четыре исхода снятия задачи — снята, уже ушла, Redis недоступен, у аккаунта нет очереди — стали кодами `app/pages/notices.py` с текстами, перенесёнными ДОСЛОВНО, и тем же вариантом оформления (success / warning / error / warning); новых сообщений не заведено"
    - "Частный словарь исходов снятия задачи и собственное место его отрисовки на странице очереди сняты; страница очереди больше не читает параметр `result`"
    - "Вхождений ключа адресной строки `result` в форме `?result=` в `app/` — ноль, и это утверждает действующий гейт снятых написаний (`RETIRED_QUERY_KEYS`, идиома D-09 Фазы 8), пополненный шестым ключом с летописью"
    - "Комментарий `app/pages/billing.py` о частных реестрах говорит правду: частных реестров было ЧЕТЫРЕ (раздела оплаты, повтора, отказа перезапуска и исходов снятия задачи), и с плана 11-08 не осталось ни одного; прежняя формулировка названа устаревшей, а не стёрта"
    - "Пункт §Active `PROJECT.md` о четвёртом частном реестре закрыт ссылкой на план 11-08; статусы требований и флажки роадмапа не трогаются"
  artifacts:
    - "app/pages/notices.py — QUEUE_DROP_REMOVED, QUEUE_DROP_MISSING, QUEUE_DROP_UNAVAILABLE, QUEUE_DROP_NO_QUEUE и их записи в NOTICES"
    - "app/pages/admin.py — QUEUE_DROP_NOTICE_CODES (исход сервиса → код реестра); admin_drop_task с respond(notice=…)"
    - "tests/test_pages/test_notices_channel.py — RETIRED_QUERY_KEYS с шестым ключом"
  key_links:
    - "исход drop_task → QUEUE_DROP_NOTICE_CODES → respond(notice=код) → _with_notice → область шелла: ломается — исход снятия пропадает молча (незнакомый код не рисует ничего)"
  prohibitions:
    - statement: "текст сообщений исхода снятия задачи не переформулируется — переносится дословно (D-10)"
      status: flagged-unverified
      verification: none
    - statement: "admin/includes/queue_row.html не получает id и макросов, снятие задачи не становится фрагментным (D-03, WR-04)"
      status: flagged-unverified
      verification: none
---

<objective>
Шестой частный микро-контракт адресной строки — ключ исхода снятия задачи из очереди — сводится в
закрытый реестр уведомлений (D-10), его старое написание снимается сразу с гейтом на ноль, а записи,
утверждавшие больше сделанного, приводятся к истине.

Purpose: пока ключ живёт своим словарём и своим местом отрисовки, у одного исхода два владельца, а
комментарий раздела оплаты утверждает, что копий не осталось. Решение D-07 Фазы 10 отдало свод
разделу администрирования Фазы 11.

Output: четыре кода реестра, перевод `admin_drop_task`, снятие частного словаря и места отрисовки,
гейт снятых написаний, правка комментария и пункта §Active.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/PROJECT.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-CONTEXT.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `QUEUE_DROP_REMOVED`, `QUEUE_DROP_MISSING`,
`QUEUE_DROP_UNAVAILABLE`, `QUEUE_DROP_NO_QUEUE` (`app/pages/notices.py`); `QUEUE_DROP_NOTICE_CODES`
(`app/pages/admin.py`); шестой элемент `RETIRED_QUERY_KEYS` (`tests/test_pages/test_notices_channel.py`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Исходы снятия задачи — коды реестра, переход с кодом, снятие частного словаря и его отрисовки</name>

  <files>
app/pages/notices.py,
app/pages/admin.py,
app/templates/admin/queue.html,
tests/test_pages/test_confirm_delete_transport.py,
tests/test_pages/test_admin_panel.py,
tests/test_pages/test_hx_location_destinations.py,
tests/test_pages/test_htmx_post_pairs.py
  </files>

  <read_first>
- `app/pages/admin.py:295-320` — `QUEUE_DROP_RESULTS` (ключи `DROP_REMOVED`, `DROP_MISSING`,
  `DROP_UNAVAILABLE`, `"unknown_account"`; тексты и варианты); `:1040-1075` — `admin_queue` (параметр
  `result`, `drop_result`); `:1080-1180` — `admin_drop_task` и комментарий «ШЕСТОЙ частный
  микро-контракт… Свод отдан Фазе 11».
- `app/pages/notices.py:80-160` — форма констант с комментарием-основанием, запись `Notice(code,
  text, variant)`, реестр `NOTICES`, допустимые варианты.
- `app/templates/admin/queue.html:25-40` — место отрисовки по ключу.
- `tests/test_pages/test_confirm_delete_transport.py:552-615` и `:990-1010` — случаи снятия задачи
  (`outcome_key`, `missing_identifier_landing` с ключом `result`).
- `tests/test_pages/test_hx_location_destinations.py:670-690` — докстринг, называющий ключ.
- `tests/test_pages/test_notices_registry.py` — правила реестра (текст непуст, вариант из множества,
  коды уникальны).
- `11-CONTEXT.md` D-10.
  </read_first>

  <behavior>
RED: случаи снятия задачи в `test_confirm_delete_transport.py` переписываются на `outcome_key="notice"` и
коды реестра (успех, уже ушла, Redis недоступен), ветка отсутствующего аккаунта — на
`/admin/queue?notice=queue_drop_no_queue`; тест `test_admin_panel.py`: после снятия задачи без htmx
приземлённая страница очереди с `?notice=queue_drop_removed` рисует текст «Задача снята из очереди»
областью уведомлений шелла, а страница по старому ключу ничего не рисует. Сегодня ключ `result` —
красный.
GREEN — ниже.
  </behavior>

  <action>
**1. Коды реестра (D-10).** В `app/pages/notices.py` рядом с кодами админки заводятся
`QUEUE_DROP_REMOVED = "queue_drop_removed"`, `QUEUE_DROP_MISSING = "queue_drop_missing"`,
`QUEUE_DROP_UNAVAILABLE = "queue_drop_unavailable"`, `QUEUE_DROP_NO_QUEUE = "queue_drop_no_queue"` с
комментарием-основанием (перенесённым из комментария частного словаря: отсутствие задачи — тоже
исход, называемый словами). Записи `Notice` получают тексты ДОСЛОВНО из частного словаря и те же
варианты. Комментарий называет D-10: коды ЗАМЕЩАЮТ существующий частный реестр, а не добавляют
сообщения (D-03 Фазы 10 не связывает).

**2. `admin_drop_task`.** Отображение исхода сервиса в код — `QUEUE_DROP_NOTICE_CODES: dict[str, str]`
(`DROP_REMOVED` → `notices.QUEUE_DROP_REMOVED`, `DROP_MISSING` → `notices.QUEUE_DROP_MISSING`,
`DROP_UNAVAILABLE` → `notices.QUEUE_DROP_UNAVAILABLE`); три выхода — `respond(request,
redirect=location, notice=…)`: неизвестный аккаунт → `notices.QUEUE_DROP_NO_QUEUE`, неуспешный исход
→ код по отображению, успех → `notices.QUEUE_DROP_REMOVED`. Адрес с кодом собирает только
`_with_notice`. Комментарий «ШЕСТОЙ частный микро-контракт… не считается сведённым» получает
поколение: сведён планом 11-08 (D-10). Журналы `queue_task_drop_failed`/`queue_task_dropped` не
трогаются.

**3. Снятие частного реестра и его отрисовки.** Частный словарь исходов снятия задачи удаляется;
`admin_queue` перестаёт принимать параметр `result` и класть в контекст готовую пару «текст исхода, вариант оформления»; в
`admin/queue.html` собственное место отрисовки исхода снимается вместе с его комментарием, на месте
остаётся строка комментария Jinja: исход рисует общая область шелла (D-12 Фазы 8, D-10 Фазы 11).

**4. Тесты и числа.** Случаи и тесты — по §behavior; докстринг `test_hx_location_destinations.py`
приводится (пример ключа — код `?notice=`). `HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном, если
двинулся.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_admin_panel.py tests/test_pages/test_notices_registry.py tests/test_pages/test_notices_surface.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'QUEUE_DROP_RESULTS' app/pages/admin.py` возвращает `0`.
- `grep -c 'drop_result' app/templates/admin/queue.html app/pages/admin.py | grep -v ':0$' | wc -l` возвращает `0`.
- `grep -nE '^QUEUE_DROP_(REMOVED|MISSING|UNAVAILABLE|NO_QUEUE) = "queue_drop_' app/pages/notices.py` печатает 4 строки.
- `grep -n 'Не удалось снять задачу: Redis не отвечает' app/pages/notices.py` печатает хотя бы одну строку (текст перенесён).
- `awk '/^async def admin_drop_task/,/^@/' app/pages/admin.py | grep -c 'notice=notices.QUEUE_DROP_'` возвращает не меньше `1`.
- `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -k "drop"` — зелено.
  </acceptance_criteria>

  <done>
Исходы снятия задачи едут кодом закрытого реестра и рисуются шеллом; частного словаря и собственного
места отрисовки больше нет.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Гейт снятого написания и записи, утверждавшие больше сделанного</name>

  <files>
tests/test_pages/test_notices_channel.py,
app/pages/billing.py,
.planning/PROJECT.md
  </files>

  <read_first>
- `tests/test_pages/test_notices_channel.py:80-140` — `RETIRED_QUERY_KEYS` (пять снятых ключей) и
  правило, утверждающее ноль вхождений в `app/`, с его летописью.
- `app/pages/billing.py:85-100` — комментарий «Три частных реестра… Копий не осталось ни одной».
- `.planning/PROJECT.md:150-160` — пункт §Active «Четвёртый частный реестр… и микро-контракт».
- `tests/test_planning/` — модули записи (прогоняются этой задачей).
  </read_first>

  <behavior>
RED: добавленный в `RETIRED_QUERY_KEYS` шестой ключ краснит гейт на дереве ДО задачи 1 (прогон на
`git stash` не нужен: правило проверяется на текущем дереве после задачи 1 и обязано быть зелёным;
зубы доказывает существующий контроль правила — если его нет, добавить контроль с синтетическим
модулем, несущим старое написание, и утверждением, что правило его называет).
GREEN — ниже.
  </behavior>

  <action>
**1. Гейт.** В `RETIRED_QUERY_KEYS` добавляется ключ `result` в той же форме записи, что у пяти
снятых (`?<ключ>=`), со строкой летописи «Фаза 11, план 11-08, D-10: шестой частный микро-контракт
сведён в `?notice=`». Если у правила нет отрицательного контроля на синтетическом модуле — он
заводится по форме соседних контролей, и в нём используется именно новый ключ.

**2. Комментарий `billing.py`.** Абзац о частных реестрах получает поколение: реестров было ЧЕТЫРЕ
(четвёртый — исходов снятия задачи на странице очереди, заведён Фазой 6 и в свод Фазы 8 не входил);
прежняя формулировка «Три… Копий не осталось ни одной» остаётся названной — она утверждала больше
сделанного (UAT 08, пункт 9); с плана 11-08 копий действительно не осталось.

**3. `PROJECT.md` §Active.** Пункт «Четвёртый частный реестр…» закрывается: флажок пункта ставится
`[x]`, в конец пункта дописывается «— закрыт планом 11-08 (D-10): коды в `app/pages/notices.py`, гейт
`RETIRED_QUERY_KEYS`, комментарий `billing.py` приведён». Прочие пункты не трогаются; статусы
требований и флажки планов роадмапа не трогаются.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_notices_channel.py -q</automated>
    <fails_when>non-zero exit, or "failed" in the pytest summary line</fails_when>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>non-zero exit, or "failed" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -rn '?result=' app/ | wc -l` возвращает `0`.
- `grep -n "result=" tests/test_pages/test_notices_channel.py` печатает хотя бы одну строку (ключ в перечне снятых).
- `grep -n '11-08' .planning/PROJECT.md` печатает хотя бы одну строку.
- `grep -n 'ЧЕТЫРЕ' app/pages/billing.py` печатает хотя бы одну строку.
- `uv run pytest tests/test_pages/test_notices_channel.py tests/test_planning/ -q` — зелено.
  </acceptance_criteria>

  <done>
Старое написание ключа исхода очереди в `app/` запрещено гейтом; комментарий оплаты и пункт §Active
говорят то, что сделано.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| адресная строка → страница очереди | код исхода приходит параметром адреса и подконтролен владельцу ссылки |
| тело очереди Redis → ответ | исход снятия зависит от содержимого очереди |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-21 | Spoofing | подставленный код в адресе `/admin/queue` | low | mitigate | Закрытый реестр: незнакомый код не рисует ничего; по адресу едет КОД, текст берётся из исходника (`notice_for`) |
| T-11-22 | Tampering | адрес перехода с кодом | medium | mitigate | Код подаётся параметром `respond(notice=…)`, сверяется `_require_registered_notice`, адрес проходит `_local_path` |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_notices_channel.py -q` — после каждой задачи.
- `uv run pytest tests/test_planning/ -q` — после задачи 2.
- `uv run pytest tests/ -q` — на слиянии волны; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Исходы снятия задачи — коды реестра, рисуемые шеллом; частный словарь и его отрисовка сняты.
2. Гейт снятых написаний держит ноль вхождений старого ключа в `app/`.
3. Комментарий `billing.py` и пункт §Active `PROJECT.md` приведены к сделанному.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-08-SUMMARY.md` when done.
</output>
