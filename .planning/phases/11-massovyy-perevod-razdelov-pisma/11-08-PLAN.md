---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 08
type: execute
wave: 8
depends_on: ["11-07"]
files_modified:
  - app/pages/htmx.py
  - tests/test_pages/test_htmx_response_layer.py
  - tests/test_pages/test_htmx_response_contract.py
  - app/templates/profile.html
  - app/templates/includes/profile_settings.html
autonomous: true
requirements: [FORM-08]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ (sample_count 0, factor 1); confidence выведен из нуля замеров.
  # Ревизия 2: задача 1 плана 11-06 ревизии 1 (выход) и вынос формы профиля из его задачи 2; план 11-06 ревизии 1 (125000, 14 файлов) разрезан на 11-08, 11-09 и 11-10.
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Выход `respond_field_error(request, *, page, fragment)` существует в слое ответа: без признака htmx отдаёт тело полной страницы, с признаком — тело фрагмента, оба со статусом 422; литерал статуса 422 записан в `app/*.py` ровно в одном месте — этом выходе; `SERVER_SIDE_VALIDATION_RESPONSES` 1 → 2 поставлен прогоном с летописью"
    - "В этом плане правило 422 блока конфигурации остаётся `\"swap\": false`, и ни один маршрут выход не зовёт — своп и первый авторский 422 приземляет план 11-09 одним коммитом"
    - "Содержимое карточки настроек профиля — ошибка поля и форма — живёт во включаемом шаблоне `includes/profile_settings.html` (контекст `timezone_choices`, `selected`, `error`): один источник для страницы и обоих фрагментов плана 11-09; ошибка поля рисуется у формы, а не над карточкой (D-12 Фазы 8)"
    - "Форма профиля в этом плане отправляется без htmx, обработчик не тронут: суиты профиля и разметки зелены без правки ожиданий"
  artifacts:
    - "app/pages/htmx.py — respond_field_error"
    - "app/templates/includes/profile_settings.html (НОВЫЙ) — ошибка поля + форма часового пояса"
    - "app/templates/profile.html — включение настроек на месте формы"
  key_links:
    - "includes/profile_settings.html — один источник формы для страницы и фрагментов успеха и ошибки: ломается — страница и фрагмент расходятся"
  prohibitions:
    - statement: "нового JS не заводится: ни слушателя htmx:beforeSwap, ни расширения response-targets, ни седьмого ключа конфигурации (Находка B, D-03 Фазы 7)"
      status: flagged-unverified
      verification: none
    - statement: "правило 422 блока конфигурации в этом плане не трогается — своп возвращает план 11-09"
      status: flagged-unverified
      verification: none
    - statement: "новых проверок ввода, кроме существующей проверки часового пояса, не заводится (D-06)"
      status: flagged-unverified
      verification: none
---

<objective>
Вторая половина Находки B готовится двумя шагами, не открывающими стока: слой ответа получает выход
ошибки поля `respond_field_error` (своп ещё выключен, маршрутов, его зовущих, нет), а форма профиля
выносится во включаемый шаблон — единственный источник для страницы и фрагментов, которые отдаст
план 11-09.

Purpose: своп правилу 422 обязан приземлиться ОДНИМ коммитом с первым маршрутом, отдающим 422 с
авторским фрагментом (план 11-09); выход и один источник разметки — то, что этот коммит потребляет, и
приземлять их вместе с ним значило бы складывать четырнадцать файлов в один план.

Output: выход `respond_field_error` с тестами обоих транспортов, число мест 422, включаемый шаблон
настроек профиля.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-07-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень — `11-01-PLAN.md`. Этим планом: `respond_field_error` (`app/pages/htmx.py`);
`app/templates/includes/profile_settings.html`; тест
`test_a_field_error_answers_422_with_the_page_without_htmx_and_the_fragment_with_it`.
</artifacts_this_phase_produces>

<scope_note>
## Размер плана (ревизия 2)

Проекция 70000 в пределах смарт-зоны. План 11-06 ревизии 1 нёс 14 файлов и разрезан на три
последовательных плана: 11-08 (выход и вынос формы), 11-09 (перевод профиля и своп 422 одним коммитом),
11-10 (форма профиля на обёртке и перечень FORM-10). Коммит свопа неделим: своп, первый авторский 422,
переписанное утверждение `swap is False`, литерал правила в `test_shell.py` и добавление к
`07-SECURITY.md` обязаны приземлиться вместе (истина плана 11-09, ревизия 1), и уже этот коммит касается
девяти файлов; поэтому всё, что ему предшествует без поведения (выход, вынос), и всё, что следует за ним
(разметка формы), вынесено в соседние планы. Смягчение T-07-13 (план 11-07) по-прежнему приземляется до
свопа.
</scope_note>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Выход `respond_field_error` — единственный литерал 422 приложения (своп ещё выключен)</name>

  <files>
app/pages/htmx.py,
tests/test_pages/test_htmx_response_layer.py,
tests/test_pages/test_htmx_response_contract.py
  </files>

  <read_first>
- `app/pages/htmx.py:394-485` — `respond`: нульарные async-сборщики, обязательный путь деградации,
  абзац «КОД ИСХОДА СПОСОБЕН ДОЕХАТЬ И НА ВЕТКЕ ФРАГМЕНТА… НИ ОДИН ОБРАБОТЧИК»; `malformed_request_response`
  и признак страничного слоя (план 11-07).
- `tests/test_pages/test_htmx_response_contract.py:40-190` — `_status_code_literals`,
  `SERVER_SIDE_VALIDATION_RESPONSES` и его летопись (после плана 11-07 — `1`).
- `tests/test_pages/test_htmx_response_layer.py` — форма тестов выходов (прямой вызов с запросом,
  несущим признак).
  </read_first>

  <behavior>
RED:
1. `test_a_field_error_answers_422_with_the_page_without_htmx_and_the_fragment_with_it`: без признака
   htmx — статус 422, тело равно телу сборщика страницы, сборщик фрагмента не вызван; с признаком —
   статус 422, тело равно телу сборщика фрагмента, сборщик страницы не вызван; ответ — свежий
   `HTMLResponse` (`text/html`) без фоновой задачи.
2. Правило числа мест 422 краснеет на литерале выхода, пока `SERVER_SIDE_VALIDATION_RESPONSES` = 1.
GREEN — выход и число.
  </behavior>

  <action>
**1. Выход `respond_field_error(request, *, page, fragment)` в `htmx.py` (D-06).** Оба аргумента —
нульарные async-сборщики, как у `respond`. Без признака htmx ожидается `page()` (полный документ), с
признаком — `fragment()`; тело выбранного ответа возвращается в свежем `HTMLResponse` со статусом 422,
и литерал этого статуса записан в приложении ОДИН раз — здесь. Невыбранный сборщик не вызывается.
Докстринг: ошибка ПОЛЯ рисуется у формы (D-12 Фазы 8), статус 422 на обоих транспортах, эхо введённого
— только через автоэкранирование шаблона; своп правилом 422 (Находка B, возвращается планом 11-09), а не
заголовками (D-16); путь деградации здесь — страница, а не перенаправление, потому что ошибка поля не
есть исход действия; сток T-07-13 закрыт планом 11-07 (`malformed_request_response`).

**2. Счёт мест 422.** `SERVER_SIDE_VALIDATION_RESPONSES` 1 → 2 прогоном; летопись: «Фаза 11, план 11-08,
задача 1 — литерал выхода `respond_field_error`; маршрутов, его зовущих, в этом коммите нет, правило
422 ещё без свопа».
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_htmx_response_contract.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -nE '^async def respond_field_error' app/pages/htmx.py` печатает одну строку.
- `grep -rn --include='*.py' 'status_code=422' app/ | cut -d: -f1 | sort -u` печатает ровно `app/pages/htmx.py`.
- `grep -nE '^SERVER_SIDE_VALIDATION_RESPONSES = 2$' tests/test_pages/test_htmx_response_contract.py` печатает одну строку.
- Правило 422 ещё без свопа: `grep -n '"code":"422", "swap": false, "error": true' app/templates/includes/htmx_config.html` печатает одну строку.
- `uv run pytest tests/test_pages/test_htmx_response_layer.py -q -k "field_error"` — зелено.
  </acceptance_criteria>

  <done>
Выход ошибки поля существует и покрыт тестами обоих транспортов; литерал 422 один и посчитан; своп
ещё выключен.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Настройки профиля во включаемом шаблоне — один источник формы, ошибка у формы</name>

  <files>
app/templates/profile.html,
app/templates/includes/profile_settings.html (NEW)
  </files>

  <read_first>
- `app/templates/profile.html` целиком — алерт ошибки над картой (`:18`), форма `method=post
  action=/profile`, `select_field(... selected=user.timezone ...)`, кнопка «Сохранить».
- `app/templates/components/field.html:48` — сигнатура `select_field(name, label, options, selected,
  id, required, hint, error, disabled)`.
- `app/pages/profile.py` — контекст `profile_get` и ветки ошибки `profile_post` (`timezone_choices`,
  `error`; ключа `selected` в контексте сегодня нет).
- `tests/test_pages/test_profile.py` — утверждение текста «Неверный часовой пояс» на странице.
- `app/templates/account_groups/includes/group_row.html` — форма включаемого шаблона с шапкой-
  комментарием Jinja, называющей контекст.
  </read_first>

  <action>
Содержимое карточки настроек — вызов алерта ошибки поля и форма целиком (прежние `method`, `action`,
`select_field` с `id='timezone'`, кнопка «Сохранить») — выносится в
`app/templates/includes/profile_settings.html`; шапка-комментарий Jinja называет контекст
(`timezone_choices`, `selected`, `error`) и то, что шаблон — единственный источник формы для страницы и
обоих фрагментов плана 11-09. Выбранное значение поля берётся из `selected`. `profile.html` включает
шаблон на прежнем месте формы внутри карточки и подаёт `selected` из контекста страницы, когда ключ
задан, иначе — сохранённый пояс пользователя; прежний вызов алерта над карточкой снимается — ошибка
поля теперь рисуется у формы (D-12 Фазы 8). В этой задаче НЕ заводятся ни постоянная обёртка с id, ни
атрибуты htmx, ни `form_wrapper` — это план 11-10 после перевода обработчика; обработчики не трогаются.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_profile.py tests/test_pages/test_impersonation.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_responsive_markup.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'includes/profile_settings.html' app/templates/profile.html` возвращает `1`.
- `grep -c 'alert(error)' app/templates/profile.html` возвращает `0`; `grep -c 'alert(error)' app/templates/includes/profile_settings.html` возвращает `1`.
- `grep -c 'hx-post' app/templates/includes/profile_settings.html` возвращает `0` (разметка htmx приходит планом 11-10).
- `git status --porcelain -- app/pages/profile.py` — пустой вывод (обработчик в этом плане не тронут).
  </acceptance_criteria>

  <done>
Настройки профиля рисуются из включаемого шаблона, ошибка поля — у формы; суиты профиля и разметки
зелены без правки ожиданий.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| слой ответа → DOM | с плана 11-09 тело ответа 422 подменяет цель запроса htmx; этот план выход заводит, но своп не включает |
| браузер → профиль | часовой пояс присылается клиентом, в том числе враждебным значением |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Information Disclosure / XSS | тело 422 до свопа | high | mitigate | Правило 422 в этом плане остаётся без свопа (критерий приёмки); единственный литерал 422 в `app/*.py` — выход с авторским фрагментом (греп и гейт числа мест); смягчение плана 11-07 уже на месте |
| T-11-13 | Tampering (XSS) | эхо значения пояса во включаемом шаблоне | medium | mitigate | Выбранное значение уходит только в `select_field` с автоэкранированием; ни `|safe`, ни `Markup(` (правило GATE-07 разметочных гейтов зелено) |
| T-11-SC | Tampering | установки пакетов | low | accept | Установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_response_contract.py tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py -q` — после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны; `uv run python -m compileall -q app main.py tests`; `graphify update .` в основном каталоге.
</verification>

<success_criteria>
1. Выход `respond_field_error` существует; литерал 422 в `app/*.py` один; `SERVER_SIDE_VALIDATION_RESPONSES = 2`.
2. Правило 422 без свопа; маршрутов, зовущих выход, нет.
3. Форма профиля — один включаемый шаблон; страница по содержанию прежняя, ошибка поля у формы.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-08-SUMMARY.md` when done.
</output>
