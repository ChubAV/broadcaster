---
phase: 11-massovyy-perevod-razdelov-pisma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/schedules.py
  - app/templates/ads/includes/sched_card.html
  - app/templates/ads/partials/sched_card_response.html
  - app/templates/components/modal.html
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_editor_schedules.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_templates/test_htmx_markup_gates.py
  - tests/test_pages/test_hx_location_destinations.py
autonomous: true
requirements: [FORM-03, GATE-02]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: estimate-calibration даёт sample_count 0, factor 1,
  # поэтому tokens == raw_tokens, а confidence: low выведен из нуля замеров.
  # Смарт-зона 100000 превышена (advisory) — основание в <scope_note>.
  tokens: 160000
  raw_tokens: 160000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Правка расписания в редакторе объявления на транспорте htmx отвечает 200 и фрагментом, первым узлом которого стоит карточка `#sched-N`; карточка подменяет саму себя (`outerHTML`), приезжает раскрытой, и в теле нет `<!DOCTYPE`; без заголовка htmx — прежний 302 на `/ads/{ad_id}/edit?sched=N#sched-N` (FORM-03, D-02)"
    - "Панель подтверждения удаления остаётся снаружи подменяемой карточки: тело ответа правки не несёт корня панели `id=sched-del-N`, и после подмены в документе остаётся ровно одна панель на карточку (Landmine CONTEXT, D-12(б))"
    - "Текст панели подтверждения и сводка объявления не расходятся с сохранённым расписанием: ответ правки несёт внеполосный узел `#ad-summary` и подмену СОДЕРЖИМОГО абзаца текста панели по постоянному id `sched-del-N-text`; корень панели с состоянием Alpine целью не становится ни в какой роли"
    - "Исходы правки, не оставляющие карточку на экране — расписания нет при своём объявлении, аккаунт недоступен, объявление чужое или несуществующее, признак возврата не пришёл, — на транспорте htmx отвечают 204 и `HX-Location` на ПОСИМВОЛЬНО тот же адрес, что уезжает 302 без htmx, с кодом реестра ровно там, где он выдаётся сегодня (D-06)"
    - "Параметризованный модуль пар `tests/test_pages/test_htmx_post_pairs.py` существует: каждый случай утверждает ОБЕ половины пары на свежем состоянии, число случаев объявлено константой, поставленной прогоном, а каждый переведённый POST-обработчик страничного слоя имеет хотя бы один случай — в реестре модуля либо в `CONFIRMED_DELETE_ROUTES` (GATE-02, D-14)"
    - "`NOT_YET_CONVERTED_COUNT` 26 → 25 и `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 3 → 4 — оба числа поставлены прогоном покрасневшего правила, летопись называет обработчик СЛОВАМИ, а не ключом перечня"
    - "Разметка страницы редактора после разбиения макроса карточки байт-в-байт прежняя: суита компонентов и редактора зелена без правки её ожиданий"
    - "Новых мест записи заголовков `HX-*` план не заводит: `HX_HEADER_WRITES` остаётся 2"
    - statement: "В браузере раскрытая карточка после сохранения остаётся раскрытой, прокрутка не прыгает, панель удаления не задваивается и открывается с актуальным текстом после десятка сохранений без перезагрузки — ручной UAT фазы, пункт 5"
      verification: backstop
  artifacts:
    - "app/pages/schedules.py — schedules_update: все выходы через respond(); нульарный async-сборщик фрагмента"
    - "app/templates/ads/includes/sched_card.html — макросы sched_card_article и sched_card_panel_text; sched_card собран из них; форма правки через form_wrapper"
    - "app/templates/ads/partials/sched_card_response.html (НОВЫЙ) — карточка + #ad-summary + текст панели"
    - "app/templates/components/modal.html — абзац текста панели несёт id `{{ id }}-text`"
    - "tests/test_pages/test_htmx_post_pairs.py (НОВЫЙ) — _PairCase, POST_PAIR_CASES, POST_PAIR_CASES_DECLARED, FRAGMENT/LOCATION/EXTERNAL, test_every_pair_case_answers_both_transports, test_the_number_of_pair_cases_is_the_declared_one, test_every_converted_handler_has_a_pair, test_control_a_handler_without_a_case_reddens_the_closure"
  key_links:
    - "форма правки (hx-target=#sched-N) → schedules_update → respond(fragment=…) → sched_card_response.html: ломается — htmx-путь получает целый документ либо панель задваивается в списке"
    - "sched_card_panel_text → тело панели в modal() И внеполосный узел ответа: одна величина текста на два места; ломается — панель спрашивает про удалённые дни и группы"
    - "POST_PAIR_CASES ∪ CONFIRMED_DELETE_ROUTES → замыкание над переведёнными обработчиками: ломается — следующий переведённый обработчик уезжает без пары молча"
  prohibitions:
    - statement: "перевод не меняет того, ЧТО делает правка расписания: новых проверок ввода и ошибок поля редактор не получает, негодные значения по-прежнему тихо отбрасываются (D-06)"
      status: flagged-unverified
      verification: none
    - statement: "плашка на успешное сохранение расписания не выдаётся — исход виден подменённой карточкой (REQUIREMENTS Out of Scope, D-03 Фазы 10)"
      status: flagged-unverified
      verification: none
    - statement: "корень панели подтверждения (узел с состоянием Alpine) не становится ни целью подмены, ни внеполосной целью подмены узла"
      status: flagged-unverified
      verification: none
    - statement: "существующие утверждения 302 суиты не удаляются, не переписываются и не размножаются в функции-двойники — пары живут одним параметризованным модулем (D-14)"
      status: flagged-unverified
      verification: none
    - statement: "порядок карточек редактора не меняется на «новые сверху» (Deferred Ideas, D-05)"
      status: flagged-unverified
      verification: none
---

<objective>
Сквозной срез фазы на первом обработчике раздела `schedules` (порядок D-15): правка расписания
в редакторе объявления переходит на слой ответа и отдаёт на транспорте htmx фрагмент своей
карточки плюс побочные области, а суита получает параметризованный модуль пар — ту самую форму
критерия 4, в которую следующие планы фазы будут добавлять случаи.

Purpose: срез проходит все слои, которые фаза будет трогать дальше: обработчик → `respond()` →
разметка цели свопа через `form_wrapper` → шаблон ответа с внеполосными узлами → числовые гейты
(`NOT_YET_CONVERTED`, `FRAGMENT_RESPONSE_HANDLERS`, разметочные константы) → модуль пар.
Архитектурный тупик (панель, задваивающаяся при подмене карточки; цель, которую макрос не
печатает) обнаруживается здесь, на первом коммите, а не на двенадцатом обработчике.

Output: переведённый `schedules_update`, разбитый макрос карточки, шаблон ответа, модуль пар с
замыканием над переведёнными обработчиками, числа 26 → 25 и 3 → 4.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-RESEARCH.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-PATTERNS.md
@.planning/phases/11-massovyy-perevod-razdelov-pisma/11-VALIDATION.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов фазы (по планам):

- 11-01: `sched_card_article`, `sched_card_panel_text` (макросы `ads/includes/sched_card.html`);
  `app/templates/ads/partials/sched_card_response.html`; id `{{ id }}-text` в
  `components/modal.html`; `tests/test_pages/test_htmx_post_pairs.py` с `_PairCase`,
  `POST_PAIR_CASES`, `POST_PAIR_CASES_DECLARED`, `FRAGMENT`, `LOCATION`, `EXTERNAL`,
  `test_every_pair_case_answers_both_transports`, `test_the_number_of_pair_cases_is_the_declared_one`,
  `test_every_converted_handler_has_a_pair`, `test_control_a_handler_without_a_case_reddens_the_closure`,
  `_closure_complaints`; `test_schedule_edit_over_htmx_swaps_only_its_card`,
  `test_schedule_edit_over_htmx_refreshes_the_summary_and_the_panel_text`.
- 11-02: `PostIdPath`, `PostIdForm`, `OptionalPostIdForm`, `id_in_column` (`app/pages/identifiers.py`);
  `POST_ALIAS_ROOTS_DECLARED`, `test_every_post_identifier_is_checked_before_its_first_use`,
  `test_control_a_check_below_the_first_use_reddens_the_catalogue_rule`.
- 11-03: `app/templates/schedules/partials/schedule_row_response.html`; параметр `after_id` и
  контекст `next_after_id` у `/schedules/partial`;
  `test_the_next_portion_does_not_skip_a_row_after_a_fragment_toggle_under_the_state_filter`.
- 11-04: id `sched-list` у контейнера `data-sched-list`; `app/templates/ads/partials/sched_create_response.html`;
  `test_created_draft_keeps_the_push_url_header_after_the_move_to_the_response_layer`.
- 11-05: `malformed_request_response`, `_validated_by_the_page_layer` (`app/pages/htmx.py`);
  регистрация обработчика `RequestValidationError` в `app/main.py`;
  `tests/test_pages/test_htmx_validation_sink.py` с `VALIDATION_SINK_ROUTES_DECLARED`,
  `JSON_API_BOUNDARY_ROUTES_DECLARED`; `RETARGET_RESWAP_USES`, `RETARGET_RESWAP_USES_DECLARED`,
  `test_every_retarget_or_reswap_use_is_declared`.
- 11-06: `respond_field_error` (`app/pages/htmx.py`); `app/templates/includes/profile_settings.html`;
  id `profile-settings`.
- 11-07: `app/templates/admin/includes/user_actions.html`, `user_block_badge.html`,
  `user_access_tile.html`, `app/templates/admin/partials/user_actions_response.html`; id
  `user-actions`, `user-block-badge`, `user-access-tile`; записи `OWN_RESPONSE_EXITS` для двух тумблеров.
- 11-08: `QUEUE_DROP_REMOVED`, `QUEUE_DROP_MISSING`, `QUEUE_DROP_UNAVAILABLE`, `QUEUE_DROP_NO_QUEUE`
  (`app/pages/notices.py`); `QUEUE_DROP_NOTICE_CODES` (`app/pages/admin.py`); ключ `?result=` в
  `RETIRED_QUERY_KEYS`.
- 11-09: `HX_REDIRECT_HEADER`, `YOOKASSA_CONFIRMATION_HOSTS`, `_confirmation_url`,
  `redirect_external` (`app/pages/htmx.py`); запись `SAFE_BY_NAME` для `redirect_external`.
- 11-10: разметка htmx трёх форм повторной синхронизации и формы синхронизации групп.
- 11-11: `app/templates/accounts/includes/max_connect_step.html`, id `max-connect-step`;
  `tests/test_pages/test_max_connect_transport.py`.
- 11-12: `test_no_page_post_input_keeps_a_framework_bound`,
  `test_control_a_synthetic_bounded_input_reddens_the_emptiness_rule`.
- 11-13: `PAIRED_302_ASSERTIONS_DECLARED`, `_post_302_assertions`,
  `test_every_302_assertion_on_a_converted_handler_is_paired`,
  `test_the_number_of_paired_302_assertions_is_the_declared_one`,
  `test_control_an_unpaired_302_assertion_reddens_the_traversal`.
</artifacts_this_phase_produces>

<phase_source_audit>
## Multi-source coverage audit (набор планов фазы 11)

| SOURCE | ID | Предмет | Plan | Status |
|---|---|---|---|---|
| GOAL | — | действия во всех продуктовых и админских разделах без перезагрузки; суита перестаёт быть зелёной по построению | 01–11 | COVERED |
| REQ | FORM-03 | фрагмент по id + OOB (9 фрагментных обработчиков) | 01, 03, 04, 06, 07, 11 | COVERED |
| REQ | FORM-04 | навигационные → HX-Location | 01–04 (исходы), 07, 08, 10 | COVERED |
| REQ | FORM-05 | `/billing/subscribe` → HX-Redirect | 09 | COVERED |
| REQ | FORM-07 | создание вставляет сущность в список | 04 | COVERED |
| REQ | FORM-08 | 422 + перерисовка + эхо | 05 (сток T-07-13), 06, 11 (окно 51 — 02, 04, 07, 10, 12) | COVERED |
| REQ | FORM-10 | HX-Retarget/HX-Reswap поимённо | 05 | COVERED |
| REQ | GATE-02 | 302 → пары параметризованным обходом | 01 (модуль, замыкание), 13 (обход, число) | COVERED |
| RESEARCH | Находка A | HX-Redirect = location.href, ровно один заголовок | 09 | COVERED |
| RESEARCH | Находка B | своп 422 только правилом; T-07-13 | 05 (смягчение отдельным планом), 06 (своп) | COVERED |
| RESEARCH | Находка C | хосты ЮKassa `{yoomoney.ru}`, фикстуры на документированном хосте, UAT | 09 | COVERED |
| RESEARCH | дрейф | 157 против ~136; докстринги `htmx.py` `:1240`, «ТРИ сборщика» | 13 | COVERED |
| RESEARCH | Pitfalls 1–11 | 422 / потерянный HX-Redirect / хост / G-11 / панель / было ноль / чекбокс / `_SYNC_IN_FLIGHT` / sleep(5) / курсор / randomly | 06, 09, 09, 01, 01, 04, 03, 10, 11, 03, — (информационный) | COVERED |
| RESEARCH | Open Q 1–4 | подпись свопа 422; асимметрия хоста; место множества; POST-алиас | 05 и 06, 09, 09, 02 (`11-RESEARCH.md` §Open Questions — RESOLVED) | COVERED |
| CONTEXT | D-01 | 12 обработчиков, 26 → 14, летопись «15/5» | 01–11, 13 | COVERED |
| CONTEXT | D-02 | форма ответа по обработчику (9/2/1) | 01, 03, 04, 06, 07, 09, 10, 11 | COVERED |
| CONTEXT | D-03 | id только блоку действий, admin/includes не трогаются | 07, 13 (летопись) | COVERED |
| CONTEXT | D-04 | изъятие D-08 Фазы 10 в силе | 07, 10 (запреты) | COVERED |
| CONTEXT | D-05 | `beforeend` по порядку сервера; было ноль → HX-Location | 04, 13 (летопись) | COVERED |
| CONTEXT | D-06 | 422 ровно у профиля и MAX; исходы расписаний → HX-Location | 01, 03, 04, 06, 11 | COVERED |
| CONTEXT | D-07 | окно 51 работой, 23 → 0; JSON-API `app/routes/` не трогается | 02, 04, 07, 10, 12, 13 (окно); 05 (ветвь htmx обработчика отказа валидации только у страничного слоя, обход JSON-API на обоих транспортах) | COVERED |
| CONTEXT | D-08 | голый 403 — изъятие, 9 → 12; закрыта часть «решение о форме» окна 63 | 07, 09, 13 (окно 63 остаётся открытым, частичное решение записано летописью) | COVERED |
| CONTEXT | D-09 | третий выход, закрытое множество хостов, SAFE_BY_NAME, 2 → 3 | 09 | COVERED |
| CONTEXT | D-10 | `?result=` → `?notice=`, 0 вхождений, billing.py:93-95, PROJECT §Active | 08 | COVERED |
| CONTEXT | D-11 | keyset `/schedules/partial`, тумблер по Фазе 9 | 03 | COVERED |
| CONTEXT | D-12 | прочтение критерия 5 по разделам | 01, 03, 11, 13 (летопись) | COVERED |
| CONTEXT | D-13 | атрибут записи адреса не ставится; HX-Push-Url переживает respond() | 04, 13 | COVERED |
| CONTEXT | D-14 | прочтение критерия 4, число снимает обход | 01, 13 | COVERED |
| CONTEXT | D-15 | порядок schedules+ads → admin → accounts; billing не первым | волны 1–13 | COVERED |
| CONTEXT | D-16 | перечень HX-Retarget/HX-Reswap числом | 05 | COVERED |
</phase_source_audit>

<edge_coverage_fallback>
## Spec-less edge probe — явные допущения (никакой молчаливой потери)

У фазы нет SPEC.md; отчёт зонда краёв дал 7 строк `unclassified / unresolved` (тексты требований
русские, классификатор их не распознал). По §C каждая строка выносится явным флагом ровно в одном
плане: FORM-03 и GATE-02 — здесь; FORM-07 — 11-04; FORM-10 — 11-05; FORM-08 — 11-06; FORM-05 — 11-09;
FORM-04 — 11-10. Итог: 7 строк отчёта = 7 флагов (равенство «ни одна не потеряна»).

- **FORM-03 — FLAGGED ASSUMPTION:** зонд не выдал краевых предикатов. Края правки на месте
  покрыты явными `truths` этого и следующих планов (чужая/несуществующая запись → тот же адрес на
  обоих транспортах; панель вне цели; побочные области по одной величине), но машинно
  сгенерированного перечня краёв НЕТ, и верификатор не вправе считать его существующим.
- **GATE-02 — FLAGGED ASSUMPTION:** зонд не выдал краевых предикатов. Края обхода (пустой реестр,
  обработчик без случая, утверждение 302 без пары) закрыты антивакуумными и отрицательными
  контролями модуля пар (11-01, 11-13), а не отчётом зонда.
</edge_coverage_fallback>

<scope_note>
## Почему план не разрезан сильнее и почему волны строго последовательны

Смарт-зона 100000; проекция 160000 НЕКАЛИБРОВАНА (замеров ноль). Разрез глубже отклонён по двум
измеренным основаниям: (1) каждый план фазы двигает константы одного и того же файла
`tests/test_pages/test_htmx_gates.py` (`NOT_YET_CONVERTED`, `FRAGMENT_RESPONSE_HANDLERS`,
`VALIDATION_REFUSAL_DIVERGENCES`, `OWN_RESPONSE_EXITS`), поэтому параллельных волн у фазы нет, и
разрез увеличивает число последовательных волн, не уменьшая чтения на задачу; (2) промежуточный
коммит обязан быть зелёным: перевод обработчика без одновременного движения чисел краснит правило
отставания, поэтому перевод и числа живут в одной задаче. Задача 1 касается 6 файлов — это цена
учёта чисел, а не ширина предмета.
</scope_note>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: Сквозной срез — правка расписания от формы редактора до пары транспортов</name>

  <files>
app/pages/schedules.py,
app/templates/ads/includes/sched_card.html,
app/templates/ads/partials/sched_card_response.html (NEW),
tests/test_pages/test_htmx_post_pairs.py (NEW),
tests/test_pages/test_editor_schedules.py,
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `app/pages/schedules.py:994-1074` — `schedules_update` целиком: ветки «нет сессии», «записи нет /
  чужая» (с `_owns_ad` и кодом `SCHEDULE_AD_MISSING`), вердикт владения, запись, возврат.
- `app/pages/schedules.py:1167-1437` — `schedules_delete`: ОБРАЗЕЦ перевода в этом же модуле —
  `returns_to_editor` читается один раз через `RETURN_TO_EDITOR`, `screen_url = _editor_url(...)`,
  ветка без признака возврата уходит переходом, нульарный async `_fragment` с докстрингом.
- `app/pages/schedules.py:404-471` (`_editor_url`, `_editor_redirect`) и `:659-672`
  (`_editor_error_redirect`) — адреса приземления и коды.
- `app/pages/htmx.py:394-485` — `respond`: обязательный `redirect=`, `notice=`, `fragment=`.
- `app/pages/ads.py:267-400` — `_editor_context`: откуда берутся `accounts`, `groups`,
  `inactive_group_ids`, `schedules_count`, `next_run_at`; `grep -n "^from app.pages" app/pages/ads.py`
  — импортирует ли `ads.py` модуль расписаний (цикл импорта).
- `app/templates/ads/includes/sched_card.html:64-300` — макрос `sched_card`: вычисление `parts`,
  `complete`, `expanded`; `<article id="sched-N">`; форма правки `:156`; вызов `modal()` СНАРУЖИ
  статьи `:274`.
- `app/templates/ads/form.html:219-231` — цикл карточек и обёртка `#sched-count`.
- `app/templates/components/form_wrapper.html:138-148` — сигнатура `form_wrapper(action, target,
  swap, trigger, disabled_elt, sync)`; `app/templates/account_groups/includes/group_row.html:193-215`
  — образец блочного вызова обёртки.
- `app/templates/ads/partials/sched_delete_response.html` — плоское тело: узлы — прямые дети
  файла (`allowNestedOobSwaps: false`).
- `tests/test_pages/test_confirm_delete_transport.py:165-236` (`_Arranged`, `_Outcome`, `_Route`,
  `LOCATION`, `FRAGMENT`) и `:1223-1320` (два числа, антивакуум, обход обеих половин).
- `tests/conftest.py:60-100` — `client`, `htmx_client` (`follow_redirects=True` — часть предмета).
- `tests/test_pages/test_htmx_gates.py:195-344` (`NOT_YET_CONVERTED`, летопись словами),
  `:683-800` (`_post_handlers`, `_converted`), `:1350-1530` (`FRAGMENT_RESPONSE_HANDLERS`).
- `11-CONTEXT.md` D-02, D-06, D-12, D-14 и §Landmines (G-11, панель снаружи).
  </read_first>

  <precondition>
Состояние, оставленное Фазой 10, не сдвинуто: `grep -nE '^(NOT_YET_CONVERTED_COUNT = 26|FRAGMENT_RESPONSE_HANDLERS_DECLARED = 3|VALIDATION_REFUSAL_DIVERGENCES_DECLARED = 23|OWN_RESPONSE_EXITS_DECLARED = 9|HX_HEADER_WRITES = 2)$' tests/test_pages/test_htmx_gates.py` печатает строк: 5, и `uv run pytest tests/test_pages/test_htmx_gates.py -q` зелено.
  </precondition>

  <reversibility rating="costly">
Исполняет D-02 для первого обработчика: форма ответа задаёт разметку цели свопа в шаблоне
карточки, от которой зависят планы 11-03 и 11-04. Откат — обратная правка шаблона и обработчика,
данных и схемы не трогает; `one-way` не заслужено, чекпойнта решения нет.
  </reversibility>

  <behavior>
RED (пишется ДО перевода, прогон обязан покраснеть):
1. Модуль `tests/test_pages/test_htmx_post_pairs.py` со случаями `schedules_update`: успех
   (`FRAGMENT`, метка `id="sched-{id}"`); расписания нет при своём объявлении (`LOCATION`,
   `/ads/{ad}/edit?notice=schedule_ad_missing`); чужое объявление (`LOCATION`, `/schedules`);
   аккаунт недоступен (`LOCATION`, `/ads/{ad}/edit?notice=schedule_account_gone`); без признака
   возврата (`LOCATION`, `/schedules`). Половина htmx краснеет: сегодня приходит 302, за которым
   `htmx_client` следует, и в теле `<!DOCTYPE`.
2. Случай `account_groups_toggle` (успех, `FRAGMENT`, метка `id="group-row-{id}"`) — обработчик
   Фазы 9, которого нет в `CONFIRMED_DELETE_ROUTES`; замыкание без него краснеет и называет его.
3. `test_schedule_edit_over_htmx_swaps_only_its_card` в `test_editor_schedules.py`: статус 200,
   тело начинается с `<article data-sched-card id="sched-{id}">`, несёт форму `/schedules/{id}/edit`
   (карточка раскрыта), НЕ несёт `id="sched-del-{id}"`, не несёт `<!DOCTYPE`.
GREEN — перевод обработчика, разметка, числа.
  </behavior>

  <action>
**1. `schedules_update` на слой ответа (D-02, D-06).** Каждый выход — `return await respond(...)`:
«нет сессии» → адрес `/login`; записи нет при своём объявлении → адрес `/ads/{ad_id}/edit` и код
`notices.SCHEDULE_AD_MISSING`; записи нет иначе → `/schedules`; вердикт `OWNERSHIP_AD_DENIED` →
`/schedules`; `OWNERSHIP_ACCOUNT_DENIED` → `/ads/{ad_id}/edit` и код `notices.SCHEDULE_ACCOUNT_GONE`.
После записи признак возврата читается ОДИН раз выражением модуля (`form_data.get("return_to") ==
RETURN_TO_EDITOR`), адрес — `_editor_url(returns_to_editor, ad_id, schedule_id)`. Без признака
возврата — `respond(request, redirect=screen_url)` без фрагмента: на сводном списке карточки нет
(то же основание, что у ветки `WR-01` удаления). С признаком — `respond(request,
redirect=screen_url, fragment=_fragment)`. Прежние помощники перенаправления редактора (сборщик
перенаправления с раскрытием и сборщик перенаправления с кодом исхода) этим обработчиком больше не
зовутся; сами помощники живут до планов 11-03/11-04 (у них ещё два вызывающих). То, ЧТО обработчик пишет в базу, не меняется ни на символ.

**2. Сборщик `_fragment`.** Нульарный `async def`, возвращает СВЕЖИЙ `HTMLResponse`, собранный
`templates.env.get_template("ads/partials/sched_card_response.html").render(...)` (граница
`_glue_notice`: без `BackgroundTask`). Контекст карточки берётся у `_editor_context(db, ad,
settings, user, schedule_id)` — одна выборка на все поля; если импорт `_editor_context` на уровне
модуля замыкает цикл (`ads.py` импортирует модуль расписаний), импорт делается внутри сборщика с
комментарием, называющим цикл (идиома отложенных импортов `app/pages/htmx.py`). Раскрытие —
`expanded_id=schedule_id`: сегодняшний 302 приземляет с `?sched=N`, то есть правленая карточка
раскрыта (D-12(а) — раскрытие есть серверное состояние). Докстринг называет: почему функция
нульарная и отложенная (IN-03), почему корня панели нет в теле (Landmine), почему карточка раскрыта.

**3. Разбиение макроса карточки.** В `sched_card.html` заводятся `sched_card_article(s, ad,
accounts, groups, user, expanded_id=none, inactive_group_ids=none)` (только `<article>`) и
`sched_card_panel_text(s, accounts)` (строка частей подтверждения — ЕДИНСТВЕННЫЙ источник текста
панели; сегодня он собирается внутри `sched_card` через `parts`). `sched_card` остаётся с прежней
сигнатурой и собирается из статьи и вызова `modal(...)` с телом из `sched_card_panel_text` — вывод
редактора обязан остаться байт-в-байт прежним. Форма правки (`:156`) переходит на блочный вызов
`form_wrapper(action='/schedules/' ~ s.id ~ '/edit', target='#sched-' ~ s.id, swap='outerHTML')`;
внутри без изменений все поля и именованные кнопки отправки: `ad_id`, `return_to`, `timezone`,
`account_id`, `group_ids`, `groups_preset`, `days_of_week`, `days_preset`, `times_of_day`,
`remove_time`, `add_time`, `save_schedule`. Форма тумблера и форма-триггер удаления в этом плане не
трогаются. ⚠️ G-11: `sched-N` становится параметрической целью свопа при уже существующей
внеполосной цели удаления — если краснеет `test_no_id_is_both_a_swap_target_and_an_oob_target`,
применяется разрешение Фазы 9, записанное у самого правила; правило не снимается.

**4. Шаблон ответа `ads/partials/sched_card_response.html` (НОВЫЙ).** Плоское тело: первым
верхнеуровневым узлом — вывод `sched_card_article(...)`. Шапка-комментарий Jinja называет узлы
ответа, основание отсутствия панели и то, что внеполосные узлы добавит задача 2.

**5. Модуль пар `tests/test_pages/test_htmx_post_pairs.py` (НОВЫЙ).** Докстринг: почему пары живут
ОДНИМ модулем (критерий 4 роадмапа, D-14: «не 320 функций»); прочтение критерия 4 по D-14 —
фрагментная ветка → 200 и без `<!DOCTYPE`, навигационная → 204 и `HX-Location` без тела, внешняя →
204 и `HX-Redirect` без тела; почему существующие 302 не трогаются. Запись `_PairCase` (frozen
dataclass): `key` (`app/pages/<файл>.py::<обработчик>`), `name`, `identity` (обычный
пользователь / администратор), `arrange` (async-фабрика, возвращающая адрес, тело формы, ожидаемый
адрес приземления и фабрику подмен — форма `_Arranged` из `test_confirm_delete_transport.py`,
импорт разрешён), `transport` ∈ {`FRAGMENT`, `LOCATION`, `EXTERNAL`}, `fragment_mark`. Параметризованный
`test_every_pair_case_answers_both_transports` готовит состояние ЗАНОВО для каждой половины: без
заголовка — `client`, `follow_redirects=False`, 302 и `location` посимвольно; с заголовком —
`htmx_client`: `FRAGMENT` → 200, метки `<!DOCTYPE` нет, `fragment_mark` есть; `LOCATION` → 204,
`headers["HX-Location"]` посимвольно тот же адрес, `content == b""`; `EXTERNAL` объявлен константой
и получает первый случай в плане 11-09. `test_the_number_of_pair_cases_is_the_declared_one` сличает
`len(POST_PAIR_CASES)` с `POST_PAIR_CASES_DECLARED` (число ставится ПРОГОНОМ; пустой реестр
краснит). Замыкание `test_every_converted_handler_has_a_pair`: переведённые = ключи
`_post_handlers(_pages_sources())` с `calls_respond` (импорт помощников из
`tests.test_pages.test_htmx_gates`); покрытые = ключи реестра ∪ `route.key` записей
`CONFIRMED_DELETE_ROUTES`; жалоба называет обработчика поимённо. Помощник `_closure_complaints(cases,
sources=None)` принимает реестр параметром, и
`test_control_a_handler_without_a_case_reddens_the_closure` подаёт реестр без случаев
`schedules_update` и утверждает, что жалоба называет именно его.

**6. Тест редактора** `test_schedule_edit_over_htmx_swaps_only_its_card` — по §behavior, пункт 3.
Существующие утверждения 302 файла не трогаются.

**7. Числа гейтов.** Ключ `app/pages/schedules.py::schedules_update` снимается из `NOT_YET_CONVERTED`
и добавляется в `FRAGMENT_RESPONSE_HANDLERS` с комментарием записи. `NOT_YET_CONVERTED_COUNT` 26 → 25
и `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 3 → 4 — каждое со строкой летописи «Фаза 11, план 11-01»,
называющей обработчик СЛОВАМИ («правка расписания в редакторе объявления») и приводящей текст
покрасневшего правила. Складывать в уме запрещено.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_editor_schedules.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or tests/test_pages/test_htmx_post_pairs.py contributes no collected tests</fails_when>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q --collect-only | tail -1` показывает не меньше 7 собранных тестов (5 случаев правки, 1 тумблер группы, правила числа, замыкания и контроля).
- `awk '/^async def schedules_update/,/^@router/' app/pages/schedules.py | grep -cE 'RedirectResponse|_editor_redirect|_editor_error_redirect'` возвращает `0`.
- `grep -nE '^NOT_YET_CONVERTED_COUNT = 25$' tests/test_pages/test_htmx_gates.py` печатает строк: 1.
- `grep -nE '^FRAGMENT_RESPONSE_HANDLERS_DECLARED = 4$' tests/test_pages/test_htmx_gates.py` печатает строк: 1.
- `grep -c '"app/pages/schedules.py::schedules_update"' tests/test_pages/test_htmx_gates.py` возвращает `1` (ключ стоит только во множестве отдающих фрагмент; летопись набрана словами).
- `grep -c 'macro sched_card_article' app/templates/ads/includes/sched_card.html` возвращает `1`; `grep -c 'macro sched_card_panel_text' app/templates/ads/includes/sched_card.html` возвращает `1`.
- Разметка редактора не сдвинулась: `uv run pytest tests/test_templates/test_components.py tests/test_pages/test_ads_editor.py -q` — зелено.
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q -k "control_a_handler_without_a_case"` — зелено (отрицательный контроль замыкания).
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Правка расписания отвечает на htmx фрагментом своей карточки, а на всех исходах вне экрана —
переходом на тот же адрес, что и без htmx; корня панели в теле нет; модуль пар существует, считает
случаи числом и замыкает переведённые обработчики; числа 26 → 25 и 3 → 4 поставлены прогоном.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Побочные области правки — сводка объявления и текст панели по одной величине, числа разметки</name>

  <files>
app/templates/components/modal.html,
app/templates/ads/includes/sched_card.html,
app/templates/ads/partials/sched_card_response.html,
tests/test_pages/test_editor_schedules.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `app/templates/components/modal.html:693-820` — корень `<div class="modal" id="{{ id }}" x-data="{ open, opener, sending, ... }">` (узел С СОСТОЯНИЕМ) и абзац текста `:808` `<p class="modal__text">{{ body }}</p>`.
- `app/templates/ads/partials/sched_delete_response.html:100-125` — форма узла `#ad-summary`
  (подмена узла, включение `ads/includes/summary.html`) и форма `innerHTML:#sched-count`.
- `app/templates/ads/includes/summary.html:1-30` — объявленный контекст: `ad`, `user`, `editor`.
- `tests/test_templates/test_htmx_markup_gates.py:1446-1455` (`MACRO_DEFINITION_SITES_CALLERS_DECLARED`,
  `PARAMETRIC_SWAP_TARGETS_CALLERS_DECLARED`), `:1831` (`HX_TARGETS`), `:1900-1952` (`OOB_BLOCKS`,
  `CLIENT_STATE_NODES` и их летописи), правила `:3946`, `:3969`, `:4288`, `:4302`, `:4623`, `:4644`.
- `tests/test_pages/test_hx_location_destinations.py:296-350` — `HX_LOCATION_DESTINATION_CALLS_DECLARED`
  и `ADDRESS_BUILDERS`.
- Результат задачи 1 (шаблон ответа, макрос `sched_card_panel_text`).
  </read_first>

  <behavior>
RED: `test_schedule_edit_over_htmx_refreshes_the_summary_and_the_panel_text` — расписание с днями
«Пн», правка htmx на дни «Пн, Вт»: тело ответа несёт верхнеуровневый узел `id="ad-summary"` с
`hx-swap-oob`, узел с `hx-swap-oob="innerHTML:#sched-del-{id}-text"`, текст в нём содержит новые дни;
корня `id="sched-del-{id}"` в теле нет; полная страница редактора после перезагрузки печатает тот
же текст панели, что приехал внеполосно (одна величина). Сегодня узлов нет — прогон красный.
GREEN — разметка ниже и числа.
  </behavior>

  <action>
**1. Постоянный id абзаца текста панели (`components/modal.html`).** Абзац `modal__text` получает
атрибут `id="{{ id }}-text"`; корень панели и всё прочее не трогаются. Комментарий Jinja
объясняет: текст панели — долгоживущая область, её СОДЕРЖИМОЕ подменяется по постоянному id
(D-12 Фазы 9), поэтому внеполосная подмена никогда не касается узла с состоянием Alpine (D-12(б)
Фазы 11). Правило сличения свойств качества панели с `form_wrapper` обязано остаться зелёным.

**2. Внеполосные узлы ответа правки (`sched_card_response.html`).** Два БЕЗУСЛОВНЫХ
верхнеуровневых узла после статьи: узел `id="ad-summary"` с подменой узла и включением
`ads/includes/summary.html` (та же форма, что в ответе удаления, — единственный источник разметки
сводки), контекст `editor` из тех же значений, что получены у `_editor_context` в задаче 1 (одно
вычисление на обе области); и узел `hx-swap-oob="innerHTML:#sched-del-{{ s.id }}-text"` с выводом
`sched_card_panel_text(s, accounts)`. Условных внеполосных блоков не заводится — правило «условный
внеполосный блок ровно один» не двигается. Шапка шаблона пополняется перечнем узлов и основанием
каждого.

**3. Тест** `test_schedule_edit_over_htmx_refreshes_the_summary_and_the_panel_text` — по §behavior.

**4. Числа разметки и переходов — прогоном.** Правила `test_htmx_markup_gates.py` называют найденное
и объявленное; двигаются ровно те константы, которые краснеют (ожидаемо `OOB_BLOCKS` +2 и счёт
вызывающих параметрическую цель), каждая — строкой летописи «Фаза 11, план 11-01» с дословным
текстом отказа. `CLIENT_STATE_NODES` двигаться НЕ обязан (id — не признак клиентского состояния);
если двинулся — это дефект правки, а не повод менять число. `HX_LOCATION_DESTINATION_CALLS_DECLARED`
в `test_hx_location_destinations.py` двигается на число новых вызовов `respond` без фрагмента в
`schedules_update` — прогоном, с летописью.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/ tests/test_pages/test_htmx_gates.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_editor_schedules.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line</fails_when>
    <human-check>UAT фазы, пункт 5 (раздел schedules): в редакторе раскрыть карточку, десять раз подряд сменить дни и нажать «СОХРАНИТЬ РАСПИСАНИЕ» без перезагрузки; карточка остаётся раскрытой, прокрутка не прыгает, в DOM ровно одна панель `sched-del-N`, панель удаления открывается с актуальным текстом частей. Записать дату, браузер и наблюдение в 11-UAT.md.</human-check>
  </verify>

  <acceptance_criteria>
- `grep -c 'id="{{ id }}-text"' app/templates/components/modal.html` возвращает `1`.
- `uv run pytest tests/test_pages/test_editor_schedules.py -q -k "refreshes_the_summary_and_the_panel_text or swaps_only_its_card"` — зелено, 2 теста.
- `grep -nE '^CLIENT_STATE_NODES = 24$' tests/test_templates/test_htmx_markup_gates.py` печатает строк: 1.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "client_state or oob or long_lived or two_roles or quality"` — зелено.
- `uv run pytest tests/test_templates/test_components.py -q` — зелено (панели 18 мест не сломаны атрибутом).
- `grep -nE '^HX_HEADER_WRITES = 2$' tests/test_pages/test_htmx_gates.py` печатает строк: 1.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Ответ правки обновляет сводку объявления и текст панели из тех же величин, что печатает полная
страница, не касаясь узла панели с состоянием; разметочные и переходные числа поставлены прогоном.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | идентификатор расписания в пути, `ad_id` и `account_id` в теле формы приходят от недоверенного клиента |
| фрагмент → DOM | имена групп, подпись аккаунта и части подтверждения, сохранённые пользователем, уезжают в разметку, подменяемую без перезагрузки |
| слой ответа → адресная строка | адрес перехода уезжает заголовком `HX-Location` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `schedules_update` на транспорте htmx | high | mitigate | Выборка с соединением `Ad.user_id` и вердикт `_ownership_verdict` стоят ДО развилки транспорта и не меняются; чужая и несуществующая запись дают на htmx ТОТ ЖЕ адрес, что и без htmx — утверждается случаями модуля пар |
| T-11-02 | Tampering | фрагмент карточки и внеполосный текст панели (XSS хранимым текстом) | medium | mitigate | Только автоэкранирование Jinja, ни `|safe`, ни `Markup(` (правило GATE-07 разметочных гейтов зелено); текст панели — одна величина макроса, не строка ответа |
| T-11-03 | Tampering | заголовок `HX-Location` исходов правки | medium | mitigate | Адрес строит `respond(redirect=…)` → `_local_path`; новых мест записи заголовков нет (`HX_HEADER_WRITES` = 2 утверждается) |
| T-11-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не устанавливает пакетов; RESEARCH §Package Legitimacy Audit: «Фаза не устанавливает внешних пакетов» |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py tests/test_pages/test_htmx_post_pairs.py -q` — зелено после каждой задачи.
- `uv run pytest tests/ -q` — на слиянии волны (известный красный `full-suite-ads-editor-order-pollution` отличать от собственной регрессии).
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- `graphify update .` — в основном рабочем каталоге после слияния (в worktree графа нет).
</verification>

<success_criteria>
1. `schedules_update` отвечает на htmx фрагментом своей раскрытой карточки, а на исходах вне экрана — переходом на тот же адрес, что и 302.
2. Корень панели подтверждения не входит в тело ответа; текст панели и сводка обновляются по одной величине.
3. Модуль пар существует, считает случаи числом и замыкает все переведённые POST-обработчики.
4. `NOT_YET_CONVERTED_COUNT = 25`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 4`, `HX_HEADER_WRITES = 2`; разметочные числа поставлены прогоном.
</success_criteria>

<output>
Create `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-01-SUMMARY.md` when done.
Сводка обязана привести дословные тексты покрасневших правил, поставивших числа, и назвать, как разрешилось столкновение G-11 (если правило краснело).
</output>
