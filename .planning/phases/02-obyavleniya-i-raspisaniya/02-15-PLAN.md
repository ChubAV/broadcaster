---
phase: 02-obyavleniya-i-raspisaniya
plan: 15
type: execute
wave: 13
depends_on: ["02-13", "02-14"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [ADS-04, ADS-05]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Отметки ADS-04 и ADS-05 в REQUIREMENTS.md называют основанием работающее поведение ОБЕИХ половин контракта автосохранения: серверной (02-08) и браузерной (02-13, очередь вместо отмены + именованная регрессия наложения) — отметка не опережает состояние кода"
    - "Абзац-основание отметок Phase 2 в REQUIREMENTS.md несёт РЕАЛЬНОЕ число пройденных тестов из прогона этой сессии, а не унаследованное"
    - "Полный прогон uv run pytest tests/ -q завершается с exit 0 после смены контракта синхронизации (02-13) и ужесточения JSON-входа (02-14)"
    - "Ни один шаблон app/templates/ не несёт стратегию отмены синхронизации — очередь тотальна по поставляемой разметке"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Таблица прослеживаемости и абзац-основание, согласованные с состоянием кода после закрытия CR-04 и CR-03"
      contains: "02-13"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "tests/test_pages/test_ads_editor.py"
      via: "основание отметки ADS-04/ADS-05 называет именованную регрессию наложения, удерживающую браузерную половину"
      pattern: "02-13"
  prohibitions:
    - statement: "Отметка Complete в таблице прослеживаемости MUST NOT опережать наблюдаемое поведение кода: основанием отметки может быть только работающее поведение, удержанное тестом, а не факт исполненного плана."
      status: open
      verification: judgment
---

<objective>
Закрыть четвёртый гап верификации — «Отметки прослеживаемости соответствуют состоянию кода» — и дать фазе итоговый регрессионный гейт второго раунда.

Верификация установила: ADS-04 и ADS-05 отмечены Complete со ссылкой «подтверждено планом 02-08», а план 02-08 закрыл ровно половину механизма — серверную; браузерная была разомкнута (CR-04). Отметка снова опередила код — в ту же сторону, что и до предыдущей верификации. Missing-пункт гапа даёт развилку: снять Complete до закрытия CR-04 либо переписать основание. Поскольку волна 12 (план 02-13) закрывает CR-04, правильная ветка — переписать основание так, чтобы оно называло ОБЕ половины и удерживающие их тесты. Этот план исполняется строго после 02-13 и 02-14 (депенденсы волн), поэтому на момент правки основание становится истинным, а не авансовым.

Вторая задача — полный прогон суиты: смена контракта разметки (02-13) и ужесточение JSON-входа (02-14) обязаны не уронить ни один из существующих тестов фазы и проекта.

Purpose: верификация дважды поймала отметки, опережающие код; основание обязано стать проверяемо честным, и весь второй раунд — подтверждённым полным прогоном.
Output: согласованная таблица прослеживаемости с честным основанием и зелёный полный прогон.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-VERIFICATION.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-13-SUMMARY.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-14-SUMMARY.md
</context>

<!-- planner-discipline-allow: this:replace -->

<tasks>

<task type="auto">
  <name>Task 1: переписать основание отметок ADS-04 и ADS-05 на обе половины контракта</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>SUMMARY планов 02-13 и 02-14 существуют в .planning/phases/02-obyavleniya-i-raspisaniya/ (волна 12 исполнена — основание перестало быть авансовым)</precondition>
  <read_first>
    - .planning/REQUIREMENTS.md:199-252 — таблица прослеживаемости v2.0 (строки ADS-04 :209 и ADS-05 :210) и абзац-основание отметок Phase 2 (:243)
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-13-SUMMARY.md — что именно закрыло браузерную половину и как называется регрессия наложения
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-VERIFICATION.md — гап «Отметки прослеживаемости соответствуют состоянию кода» (missing-пункт)
  </read_first>
  <action>
    Сначала выполнить `uv run pytest tests/ -q` и зафиксировать фактическое число пройденных тестов — оно пойдёт в абзац-основание (унаследованное «884 passed» не переписывать вслепую: число изменилось за счёт новых регрессий 02-13 и 02-14; выдуманных чисел в документе быть не должно).

    Затем тремя точечными правками (Edit, не перезапись файла):
    1. Строка :209 (ADS-04): основание отметки — «Complete (2026-08-11, сервер: 02-08, браузер: 02-13)».
    2. Строка :210 (ADS-05): основание — «Complete (2026-08-11, подтверждено планами 02-08, 02-10 и 02-13)».
    3. Абзац-основание (:243): переписать так, чтобы он (а) называл ДВЕ половины контракта автосохранения — серверную маршрутизацию create/update (02-08) и браузерную стратегию очереди с именованной регрессией наложения `test_save_during_autosave_overlap_lands_in_one_published_ad` (02-13); (б) упоминал закрытие CR-03 планом 02-14 (правило JSON-входа не допускает нечитаемой записи; экраны SCH-04/ADS-07 живы при испорченной строке); (в) нёс фактическое число пройденных тестов из прогона этой сессии с датой. Принцип абзаца сохранить дословно: отметка ставится по факту работающего поведения, а не по факту исполненного плана.

    Смысловых требований не добавлять и не удалять; счётчики Complete/Pending (:250-251) не меняются — состав Complete тот же, меняется только основание.
  </action>
  <verify>
    <automated>grep -q '02-13' .planning/REQUIREMENTS.md && grep -q 'test_save_during_autosave_overlap_lands_in_one_published_ad' .planning/REQUIREMENTS.md && grep -q '02-14' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - строка ADS-04 таблицы содержит «02-08» и «02-13»
    - строка ADS-05 таблицы содержит «02-08», «02-10» и «02-13»
    - абзац-основание называет имя регрессии наложения и фактическое число пройденных тестов текущего прогона
    - счётчики v2.0 (39 требований, Complete: 13) не изменены
  </acceptance_criteria>
  <done>Основание отметок ADS-04/ADS-05 называет обе половины механизма и удерживающие их тесты; ни одна отметка не опережает код.</done>
</task>

<task type="auto">
  <name>Task 2: итоговый регрессионный гейт второго раунда</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-13-SUMMARY.md — какие тесты перевёрнуты
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-14-SUMMARY.md — какие тесты добавлены
  </read_first>
  <action>
    Прогнать полную суиту и три сквозные проверки контракта очереди — подтвердить, что второй раунд закрытия ничего не уронил:
    1. `uv run pytest tests/ -q` — exit 0, ни одного failed (число пройденных обязано совпасть с зафиксированным в задаче 1).
    2. Стратегии отмены синхронизации нет ни в одном шаблоне: негативный греп по app/templates/ (см. verify) — очередь тотальна по поставляемой разметке.
    3. Контракт перевёрнут и в тестах: tests/test_pages/test_ads_editor.py утверждает очередь (`this:queue last` присутствует в файле).
    Если хоть одна проверка падает — исправление в рамках файлов планов 02-13/02-14 и повторный прогон; в SUMMARY назвать причину и правку. Файл REQUIREMENTS.md этой задачей не меняется, если задача 1 уже записала верное число; при расхождении числа — поправить его на фактическое.
  </action>
  <verify>
    <automated>uv run pytest -q tests/ && ! grep -rq 'this:replace' app/templates/ && grep -q 'this:queue last' tests/test_pages/test_ads_editor.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q tests/` — exit 0
    - `! grep -rq 'this:replace' app/templates/` — стратегии отмены нет ни в разметке, ни в комментариях шаблонов
    - `grep -q 'this:queue last' tests/test_pages/test_ads_editor.py` — контракт очереди закреплён тестом
  </acceptance_criteria>
  <done>Полная суита зелёная с новым контрактом; очередь тотальна по шаблонам и закреплена тестом.</done>
</task>

</tasks>

## Artifacts this phase produces

- Обновлённые строки :209-:210 и абзац-основание :243 в `.planning/REQUIREMENTS.md` (основание ADS-04/ADS-05 называет 02-08 + 02-13; упомянут 02-14).
- Новых символов кода этот план не создаёт (документ + прогон).

## Flagged Assumptions (spec-less edge probe + UI-SPEC) — полный реестр раунда

Фаза не имеет SPEC-файла; edge-пробник по семи требованиям вернул ВСЕ строки `unclassified — review manually / unresolved`. Ни одна не разрешена автоматически; реестр раунда целиком (строки ADS-04/ADS-05 детализированы в 02-13, ADS-07/SCH-04/SCH-05 — в 02-14):

| Requirement | Probe | Disposition в раунде 2 |
|---|---|---|
| ADS-04 | unclassified — review manually | Наложение в одном окне закрыто регрессией 02-13; многовкладочный режим — явное допущение (две вкладки /ads/new легитимно дают два черновика) |
| ADS-05 | unclassified — review manually | Вложения при наложении закрыты 02-13; частично упавшая пачка — существующий backstop 02-08, не дублируется |
| ADS-06 | unclassified — review manually | Гапов нет; визуальное соответствие предпросмотра доставленному — human verification (02-VERIFICATION §Human Verification Required, п. 2) |
| ADS-07 | unclassified — review manually | Экран жив при испорченной строке (02-14); WR-09/WR-10 — предупреждения вне gaps-раздела, известны поимённо, в раунд не входят |
| ADS-08 | unclassified — review manually | Гапов нет; удержано именованной регрессией исполненного 02-05/02-09 |
| SCH-04 | unclassified — review manually | Экран жив при испорченной строке (02-14); empty/adjacency/ordering удержаны 02-11 |
| SCH-05 | unclassified — review manually | Тумблер испорченной строки ведёт себя как у неполного расписания (02-14, D-08) |

UI-SPEC `## UI Considerations` (102 позиции: 88 explicit, 12 backstop, 2 unresolved) поднят в must_haves исполненных планов 02-04/02-05/02-07/02-12; в этом раунде без дропов повторно фиксируются два ⚠ unresolved как явные допущения: **E1 `overflow`** (карточка «Содержание» при экстремальном тексте) и **E9 `zero-one-many`** (превью при большом числе вложений) — оба остаются в человеческих измерениях SC-5 (стенд 02-12, формулировки B-1…B-12, статус pending human verification — верификатор и исполнитель 02-12 сознательно не подменяли их автоматическим суждением).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | Новых границ доверия план не вводит: правится планировочный документ и выполняется прогон существующей суиты |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15-01 | Repudiation | .planning/REQUIREMENTS.md | low | mitigate | Основание отметки обязано называть удерживающий тест и фактическое число прогона — отметка перестаёт быть неподтверждаемым заявлением |

Кода, маршрутов и зависимостей план не меняет; установок пакетов нет — гейт легитимности пакетов не применим.
</threat_model>

<verification>
- `uv run pytest -q tests/` — exit 0.
- `grep -q '02-13' .planning/REQUIREMENTS.md` и `grep -q '02-14' .planning/REQUIREMENTS.md`.
- `! grep -rq 'this:replace' app/templates/`.
</verification>

<success_criteria>
- Гап «Отметки прослеживаемости соответствуют состоянию кода» закрыт: основание ADS-04/ADS-05 честно называет обе половины и удерживающие тесты.
- Полный прогон суиты зелёный после обоих фиксов раунда.
- Реестр допущений раунда (edge probe + UI-SPEC unresolved) зафиксирован без молчаливых дропов.
</success_criteria>

<output>
Create `.planning/phases/02-obyavleniya-i-raspisaniya/02-15-SUMMARY.md` when done. Указать фактическое число пройденных тестов и перечень правок REQUIREMENTS.md.
</output>
