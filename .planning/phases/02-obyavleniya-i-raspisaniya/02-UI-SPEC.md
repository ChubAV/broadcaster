---
phase: 2
slug: obyavleniya-i-raspisaniya
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-10
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for the ad editor and the summary schedule list.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope:** ADS-04, ADS-05, ADS-06, ADS-07, ADS-08, SCH-04, SCH-05.

**Screens in contract:**

| Screen | Route | Template | State |
|--------|-------|----------|-------|
| Ad editor | `/ads/new`, `/ads/{id}/edit` | `app/templates/ads/form.html` | rewritten |
| Ad list (status column only) | `/ads` | `app/templates/ads/includes/ad_card.html` | amended |
| Summary schedule list | `/schedules` | `app/templates/schedules/list.html`, `schedules/includes/schedule_row.html` | rewritten |
| Schedule create/edit pages | `/schedules/new`, `/schedules/{id}/edit` | `app/templates/schedules/form.html` | **deleted** (D-14) |

**Sources this contract was derived from (do not re-litigate):**

- `02-CONTEXT.md` D-01…D-21 — locked decisions
- `design/new_broadcaster_design.unpacked.html` lines 520–709 (editor) and 737–805 (summary list) — canonical visual reference
- `app/static/css/app.css` — the shipped design system from Phase 1 (tokens, 13 component classes)
- `app/templates/components/*.html` — the shipped macro library

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-written CSS, no build step (Phase 1 D-02) |
| Preset | not applicable |
| Component library | none — 13 Jinja2 macros in `app/templates/components/` (Phase 1 D-13) |
| Stylesheet | `app/static/css/app.css` — the single stylesheet of the project |
| Icon library | inline SVG only: `btn_icon(name)` in `components/button.html` (`plus`, `trash`, `check`, `arrow-right`, `pencil`, `refresh`) and `includes/messenger_icon.html` (`tg_user`, `wa`, `max`) |
| Font | `--font-sans` IBM Plex Sans (self-hosted, Cyrillic subsets present) · `--font-mono` IBM Plex Mono · `--font-display` Space Grotesk (logo/latin only) |
| Theme | dark only (Phase 1 D-10) — no light-mode variables exist and none are introduced |
| Behaviour layer | vendored htmx 1.9.10 + Alpine 3.13.3; zero external requests; no `innerHTML`/`outerHTML`/`insertAdjacentHTML` anywhere |

**Rule for this phase:** the token block, the macro library and the shell are **closed for redefinition**. Phase 2 adds a new numbered section (`8. Редактор объявления и сводный список расписаний`) to `app.css` and adds no new colours, radii or type steps beyond the ones declared below.

---

## Spacing Scale

Declared values for **all new Phase 2 markup** (multiples of 4 only):

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs | 4px | Gap inside a chip (icon ↔ label), badge inner gap, gap between a day cell's letter and its state |
| sm | 8px | Gaps between day cells, time pills, account chips, attachment tiles; padding of the chip strip |
| md | 12px | Vertical gap between controls inside one group (label ↔ control, group-row list); horizontal gap in the collapsed schedule row |
| lg | 16px | Gap between editor sections; gap between the two editor columns; gap between blocks inside an expanded schedule card; gap between cards in the summary list |
| xl | 24px | Padding of the expanded schedule card body; vertical rhythm between the lead paragraph, the filter bar and the list |
| 3xl | 48px | Empty-state padding — inherited from the shipped `.empty` class, not re-declared |

**Fixed control sizes (all multiples of 4):**

| Control | Size |
|---------|------|
| Attachment tile / «+ ФАЙЛ» tile | 88 × 88 |
| Day cell | 44 × 36 (≤400px: 40 × 36) |
| Time pill | height 36 |
| Account chip | height 44 |
| Group row (checkbox row) | height 40 |
| Preview media thumbnail | height 120 |
| Summary-list day cell | 40 × 32 |

**Exceptions (documented, deliberate):**

1. **Phase 1 macros keep their mockup-literal values.** `.card` (padding 18/20), `.field` (gap 7), `.toggle` (38×22 track), `.empty` (48/20), `.filters`, `.modal`, `.badge` (4/9) ship with odd values transcribed from the mockup. Phase 2 **reuses these macros unchanged and does not restyle them** — re-snapping them to the 4-scale would be a Phase 1 refactor outside this phase's boundary.
2. **Toggle track is 22px tall**, below the 36px touch minimum. Mitigation: the toggle is wrapped in a `<label class="toggle">` whose full row is the hit area (≥40px tall in both the editor card head and the summary card head). No bare 22px hit target ships.
3. Mockup values 9/11/13/14/19/22px in the two reference sections are **rounded to the nearest scale step** (9→8, 11→12, 13→12, 14→16, 19→20→`.card` padding, 22→24). This continues the Phase 1 precedent that smoothed the mockup's fractional type steps.

---

## Typography

Four steps for all new Phase 2 markup. Two weights only.

| Role | Size | Token | Weight | Line Height | Applies to |
|------|------|-------|--------|-------------|------------|
| Label / mono meta | 11px | `--fs-xs` | 400 | 1.3 | Day-cell letters, time values, group member counts, schedule counts, «след. запуск», the autosave indicator, `+ ФАЙЛ` / `+ РАСПИСАНИЕ` / `+ ВРЕМЯ` / `БУДНИ` / `КАЖДЫЙ ДЕНЬ` / `УДАЛИТЬ РАСПИСАНИЕ` captions. Uppercase + `letter-spacing: .12em` where it is a field label, `.08em` where it is an action caption. |
| Body | 14px | `--fs-lg` | 400 | **1.6** | Field values, textarea, group names, account names, preview title text and preview body, hints, the summary-list lead paragraph |
| Section heading | 15px | `--fs-xl` | 600 | 1.2 | «Содержание», «Расписания», «Предпросмотр»; ad title in a summary-list card |
| Page title | 22px | `--fs-h1` | 600 | 1.2 | Shell head — inherited from Phase 1, not re-declared |

**Line height 1.6 for Body is deliberate, not the 1.5 default:** the preview must reproduce the composer. `.field__input--area` already ships at 1.6, so the preview paragraph matches it exactly; at 1.5 the preview would visibly re-wrap differently from the textarea and break ADS-06 ("ровно в том виде, в каком оно уйдёт").

**Inherited steps used through unmodified macros (not new declarations):** `.field__label` 10px, `.field__hint` / `.field__error` 12px, `.badge` 11px / `.badge--mono` 10px. Phase 2 introduces **no other size**. Fractional steps from the mockup (9.5 / 10.5 / 11.5 / 12.5 / 13.5px) are not used.

---

## Color

| Role | Value | Token | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `#08080b` / `#0e0e14` | `--bg` / `--surface` | Page background and card surfaces — the editor's two columns and every summary card |
| Secondary (30%) | `#12121a` / `#0f0f16` | `--surface-input` / `--surface-pill` | Inputs, attachment tiles, schedule-card shells, preview frame, unselected chips, expanded-card body |
| Accent (10%) | `oklch(0.72 0.19 310)` | `--accent`, `--accent-cta` | See reserved-for list below |
| Destructive | `oklch(0.68 0.19 25)` | `--danger` | Destructive actions and error copy only |
| Warning | `oklch(0.82 0.16 85)` | `--warn` | Draft / not-filled-in signalling only |

**Accent reserved for — exhaustive list. Any accent use not on this list is a checker failure:**

1. The single primary CTA per screen — «Сохранить» in the editor (`.btn--primary`, `--accent-cta`).
2. The **on** state of a toggle track (`--accent`) — schedule enabled, in both the editor card and the summary card.
3. Selection state of a choice control: day cell selected, account chip selected, group checkbox checked, filter option selected.
4. Focus ring (`--focus-ring`) on every keyboard-focusable control.
5. The status dot on a summary-list card **only** when the card's channel colour is not applicable.

**Explicitly not accent:** «Отмена», «+ ФАЙЛ», «+ РАСПИСАНИЕ», «+ ВРЕМЯ», «БУДНИ», «КАЖДЫЙ ДЕНЬ», «РАЗВЕРНУТЬ», «ВЫБРАТЬ ВСЕ», the character counter in its normal range, the autosave indicator when idle, the preview channel caption, all mono metadata. These are `--text-muted` / `--text-secondary`, with `--text` on hover only.

**Destructive reserved for:** «УДАЛИТЬ ОБЪЯВЛЕНИЕ» and «УДАЛИТЬ РАСПИСАНИЕ» captions (muted at rest, `--danger` on hover — mockup behaviour), the confirm button of a delete modal (`.btn--danger`), `.alert--error`, `.field__error`, and the character counter once the limit is exceeded.

**Warning reserved for:** the «Черновик» ad-status badge, the «НЕ ЗАПОЛНЕНО» schedule badge, the «Заполните группы, дни и время» hint, the «Объявление в черновике» marker in the summary list, and the character counter at ≥90% of the limit.

**Channel colours** come from `messenger_icon` (`--info` Telegram, `--ok` WhatsApp, MAX keeps its own gradient). They mark channel identity only and never mark state.

---

## Copywriting Contract

All user-facing copy in Russian. Mono/uppercase captions are written in caps in the markup, not by `text-transform`, where they come verbatim from the mockup.

### Primary actions

| Element | Copy |
|---------|------|
| Primary CTA (ad editor) | **Сохранить** — there is no separate «Опубликовать»; the mockup has only Сохранить/Отмена (D-04) |
| Secondary | Отмена |
| Editor destructive | УДАЛИТЬ ОБЪЯВЛЕНИЕ |
| Add attachment | + ФАЙЛ |
| Add schedule | + РАСПИСАНИЕ |
| Add first schedule | + ДОБАВИТЬ ПЕРВОЕ |
| Add time | + ВРЕМЯ |
| Day presets | БУДНИ · КАЖДЫЙ ДЕНЬ |
| Group bulk | ВЫБРАТЬ ВСЕ / СНЯТЬ ВСЕ |
| Expand / collapse a schedule card | РАЗВЕРНУТЬ / СВЕРНУТЬ |
| Schedule destructive | УДАЛИТЬ РАСПИСАНИЕ |
| Summary list head action | К объявлениям (ghost) — «Создать» is removed from `/schedules`, creation lives in the ad editor (D-14) |
| Summary card row action | Открыть объявление |

### Labels and placeholders

| Element | Copy |
|---------|------|
| Section titles | Содержание · Расписания · Предпросмотр |
| Field labels | НАЗВАНИЕ · ТЕКСТ · ВЛОЖЕНИЯ · АККАУНТ · ГРУППЫ · ДНИ И ВРЕМЯ |
| Title placeholder | Например: Скидки на курсы Python |
| Text placeholder | Текст, который увидят участники групп |
| Search placeholder (summary list) | Поиск по объявлению и времени запуска |
| Filter labels | Канал · Статус |
| Filter options — channel | Все · Telegram · WhatsApp · MAX |
| Filter options — status | Все · Активные · На паузе |
| Filter actions | Применить · Сбросить |
| Group count caption | ГРУППЫ · выбрано {n} из {total} |
| Schedule count caption | {n} расписаний |
| Result count | {n} расписаний |
| Next run | след. запуск {дд.мм чч:мм} {tz} |
| Attachment remove button | `aria-label` / `title`: Убрать вложение |
| Time remove button | `aria-label` / `title`: Убрать время |
| Toggle title | Приостановить / Возобновить (existing copy, unchanged) |
| Timezone caption | Время по {tz из профиля} — read-only mono caption in the schedule card; the timezone is not selectable here |

### Status vocabulary

| Where | Copy | Variant |
|-------|------|---------|
| Ad list status cell, ad `status=draft` | Черновик | `badge('Черновик', 'warning')` |
| Ad list status cell, ad `status=published` | Опубликовано | `badge('Опубликовано', 'success')` |
| Editor summary block | Статус — Черновик / Опубликовано | mono, `--warn` / `--ok` |
| Schedule card in editor, incomplete (D-08) | НЕ ЗАПОЛНЕНО | `badge('Не заполнено', 'warning', mono=true)` |
| Schedule card in editor, incomplete hint | Заполните группы, дни и время | `--warn`, 14px |
| Summary card whose ad is a draft | Объявление в черновике | `badge('Объявление в черновике', 'warning')` |
| Summary card next-run cell when ad is a draft | отправок не будет | mono `--text-muted` |

> **Deliberate deviation from the mockup (D-UI-01).** The mockup labels an incomplete schedule card «ЧЕРНОВИК». In this phase «черновик» is a *load-bearing ad status* with a real send-pipeline effect (D-01/D-02). Reusing the same word for "this schedule has empty fields" would make two unrelated states indistinguishable in copy. The schedule badge therefore reads **«НЕ ЗАПОЛНЕНО»**; the amber colour, position and shape stay exactly as in the mockup.

### Autosave indicator

Placed at the right edge of the «Содержание» card head, mono 11px. Driven by htmx request state, no bespoke JS.

| State | Copy | Colour |
|-------|------|--------|
| Idle after a successful save | Сохранено | `--text-muted` |
| Request in flight | Сохраняем… | `--text-secondary` |
| Request failed | Не сохранено — проверьте соединение | `--danger` |
| Never saved yet (empty `/ads/new`) | Черновик создастся при первом изменении | `--text-muted` |

### Empty states

| Where | Copy |
|-------|------|
| Editor — no schedules yet | Title: **Расписаний пока нет — объявление не будет отправляться** · Action: + ДОБАВИТЬ ПЕРВОЕ |
| Editor — selected account has no groups | У выбранного аккаунта нет подключённых групп |
| Editor — user has no messenger accounts at all | Title: **Сначала подключите аккаунт мессенджера** · Hint: Без аккаунта расписание отправлять некуда · Action: Подключить аккаунт → `/accounts` |
| Editor — preview with nothing composed | Здесь появится объявление, каким его увидят участники групп |
| Summary list — user has no schedules at all | Title: **Расписаний пока нет** · Hint: Расписания создаются в редакторе объявления · Action: К объявлениям → `/ads` |
| Summary list — filters/search matched nothing | Title: **Расписания не найдены** · Action: СБРОСИТЬ ФИЛЬТРЫ |
| Ads list | unchanged from Phase 1 |

### Error states

Every error names the problem **and** the next step. Rendered with `alert(message, 'error')` unless stated otherwise.

| Trigger | Copy |
|---------|------|
| Upload rejected — wrong type (CR-02, server-side sniffing) | Не удалось загрузить: подойдут только изображения JPEG, PNG, WebP или GIF. Выберите другой файл. |
| Upload rejected — over the limit (D-13) | Можно прикрепить не больше 10 вложений. Удалите лишние и попробуйте снова. |
| Upload failed — network/storage | Не удалось загрузить файл. Проверьте соединение и попробуйте ещё раз. |
| Save rejected server-side — too many attachments (D-13) | Объявление не сохранено: вложений больше 10. Удалите лишние и нажмите «Сохранить». |
| Autosave failed | inline indicator: Не сохранено — проверьте соединение (see above). No modal, no page-level alert — the user keeps typing. |
| Schedule save rejected — ad or account not owned (CR-01/D-20) | Аккаунт или объявление недоступны. Обновите страницу и попробуйте снова. |
| User tries to enable an incomplete schedule (D-08) | Включить нельзя: выберите аккаунт, хотя бы одну группу, день и время. — rendered as the `--warn` hint inside the card, and the toggle is `disabled` |
| Empty required field on a no-JS full-form save (D-09) | `field(..., error='Заполните это поле')` — `.field--invalid` |

### Destructive confirmation

Uses the shipped `modal()` macro: a real `<form method="post">` inside the panel, an Alpine-intercepted trigger form outside it, so the delete path survives without Alpine (Phase 1 pattern, 13 existing sites).

| Action | Confirmation |
|--------|--------------|
| Удалить объявление | `modal(id='ad-del-{id}', title='Удалить объявление?', body={ad.title}, confirm_label='Удалить', variant danger)` — unchanged from Phase 1 |
| Удалить расписание | `modal(id='sched-del-{id}', title='Удалить расписание?', body='{канал} · {аккаунт} · {дни} · {времена}', confirm_label='Удалить', variant danger)` |
| Убрать вложение | **No confirmation, deliberately.** The action is cheap to undo (re-upload) and the S3 object is retained anyway (D-12), so a modal would cost more than the mistake. The removal is autosaved like any other edit and the indicator reports it. |
| Toggle a schedule on/off | No confirmation — reversible in one click, both in the editor and in the summary list (SCH-05). |

---

## Component Inventory

### Reused from Phase 1 unchanged — do not fork, do not restyle

| Macro | Used for |
|-------|----------|
| `card_open` / `card_close` | The four editor sections and each summary-list card |
| `field` / `textarea_field` / `select_field` | Title, text, filter selects |
| `button` / `link_button` | Сохранить, Отмена, all ghost actions |
| `badge` | Ad status, «Не заполнено», «Объявление в черновике» |
| `toggle` | Schedule enable/pause in both places (real `<input type=checkbox>`, focus ring, disabled state all present) |
| `empty_state` | Every empty state above |
| `modal` | Both delete confirmations |
| `alert` | Every error above |
| `mono` | Every mono caption |
| `filters` | The summary-list filter bar (collapsible on ≤860px, Alpine-enhanced, GET form) |
| `row_open` / `row_close` / `cell` | Ads list only — the summary schedule list leaves `data-row` for cards (see below) |
| `messenger_icon` | Channel identity in schedule cards and account chips |

### New CSS added by this phase — section 8 of `app.css`

| Selector | Purpose |
|----------|---------|
| `[data-editor]` | `display:grid; grid-template-columns: minmax(0,1.35fr) minmax(300px,.85fr); gap:16px; align-items:start` — **the ≤900px override already ships in `app.css:464`; only the base rule is missing** |
| `[data-editor-side]` | `position:sticky; top:96px; display:flex; flex-direction:column; gap:16px` — **the ≤900px `position:static` override already ships in `app.css:465`** |
| `[data-media]`, `.media-tile`, `.media-tile__remove`, `.media-tile--add` | 88×88 attachment tiles, wrapping strip, × button, dashed add tile |
| `.counter`, `.counter--warn`, `.counter--over` | Character counter in the ТЕКСТ label row |
| `.autosave`, `.autosave--busy`, `.autosave--error` | Autosave indicator; `--busy` driven by htmx's `.htmx-request` class, no timers |
| `[data-sched-card]`, `.sched-card__head`, `.sched-card__body` | Collapsible schedule card in the editor |
| `.chip-set`, `.chip`, `.chip--on` | Account chips, day cells, time pills, filter options — one primitive, three sizes via modifier |
| `.day-grid` | Wrapping 7-cell day grid |
| `.time-pill` | `input[type=time]` + remove button |
| `.group-pick`, `.group-pick__box`, `.group-pick__name`, `.group-pick__count` | Group checkbox rows |
| `.preview`, `.preview__frame`, `.preview__media`, `.preview__title`, `.preview__text`, `.preview__time` | Server-rendered preview |
| `.kv`, `.kv__k`, `.kv__rule`, `.kv__v` | Key–value summary block with the leader rule |
| `[data-sched-list]`, `.sched-item`, `.sched-item__head`, `.sched-item__days` | Summary-list card |

**Forbidden in this phase:** `.progress` inside a schedule card. The mockup's send-progress bar shows an invented percentage with no data source (D-17); the markup must not ship a placeholder bar.

---

## Interaction Contract

Baseline works with **JavaScript disabled** (D-09). htmx and Alpine are enhancement only.

| Interaction | No-JS baseline | Enhanced |
|-------------|----------------|----------|
| Save ad | `<form method="post" action="/ads/{id}/edit">` with «Сохранить» | htmx autosave on `keyup changed delay:2s, change delay:2s`; the response swaps the preview + summary + indicator block (D-05, D-06) |
| First save on `/ads/new` | Plain POST creates the draft and redirects to `/ads/{id}/edit` | First autosave creates the draft and the URL becomes `/ads/{id}/edit` (D-03) |
| Add attachment | `<input type="file" multiple>` inside the form, submitted with it | `POST /api/uploads/image` per file, tile appended via DOM nodes (never `innerHTML`) |
| Remove attachment | Named submit button `name="remove_image" value="{key}"` — a real POST | Alpine removes the tile and the hidden input, autosave persists |
| Add / edit / delete a schedule | Real `<form method="post">` per schedule to the existing page routes | htmx per-schedule POST — each schedule saves separately and immediately (D-07) |
| Expand a schedule card | Link `/ads/{id}/edit?sched={sid}#sched-{sid}` — the server renders that card expanded and the others collapsed | Alpine intercepts the click and toggles in place; DOM identical either way |
| Toggle a schedule (editor and summary list) | `<form method="post" action="/schedules/{id}/toggle">` wrapping the toggle | Alpine `x-on:change="$el.submit()"` — the existing Phase 1 pattern, route unchanged (D-15) |
| Day presets БУДНИ / КАЖДЫЙ ДЕНЬ | Named submit buttons on the schedule form | Alpine checks the day checkboxes client-side |
| ВЫБРАТЬ ВСЕ groups | Named submit button | Alpine checks all visible group checkboxes |
| Change the account in a schedule | Radio + submit — the server re-renders the group list for the new account and **clears the previous group selection** | htmx swaps the group block; selection cleared identically |
| Filter / search the summary list | GET form via the `filters` macro (`Применить`, `Сбросить`) | Collapsible on narrow widths via the existing Alpine binding |

**Autosave delay: 2s** — the midpoint of the 1.5–3s window left to discretion in CONTEXT. Trigger is htmx-native (`delay:` modifier); no bespoke timer, no `beforeunload` handler (D-05).

**Attachment order** = the order of `Ad.images`, which is upload order and is exactly the send order. Removal preserves the order of the rest. No drag-and-drop in this phase (reordering is not in ADS-05, and a pointer-only affordance would break the no-JS baseline).

**No cap on the number of schedules per ad.** The section caption shows the count; nothing is hidden or truncated.

**Attachment cap: 10**, enforced on the server (D-13). The browser check stays as convenience only, and the «+ ФАЙЛ» tile is hidden once 10 tiles are present.

---

## Responsive Contract

| Breakpoint | Behaviour |
|------------|-----------|
| > 900px | Editor is two columns (`1.35fr / 0.85fr`, gap 16); the right column is sticky at `top: 96px` |
| ≤ 900px | Editor collapses to one column, sticky is dropped — **both rules already ship in `app.css:463-466`**. DOM order becomes the reading order: Содержание → Расписания → Предпросмотр → Сводка → Сохранить/Отмена → УДАЛИТЬ ОБЪЯВЛЕНИЕ. The preview must therefore stay **after** the composer in the DOM. |
| ≤ 860px | Shell collapses to the existing mobile shell (tabs, no sidebar) — Phase 1, unchanged. The `filters` macro collapses behind its «Фильтры» button. |
| ≤ 400px | Day cells shrink 44 → 40 wide; the day grid wraps to two rows |
| All widths | Attachment strip, chip sets, day grid and time pills are wrapping flex containers — nothing horizontally scrolls, nothing is clipped |

**Summary list is card-based at every width** — it is not a `data-row` table, so no `[data-cell-label]` treatment is needed. The ads list keeps its Phase 1 `data-row` + `[data-cell-label]` behaviour; only its status cell copy changes.

**Touch targets:** every interactive control in new markup is ≥36px on its smallest axis, except the inherited 22px toggle track, which is wrapped in a ≥40px label row (see Spacing exception 2).

**Accessibility invariants:**

- Day cells, group rows and account chips are real `<input type="checkbox">` / `<input type="radio">` visually hidden behind `<label>` — keyboard-operable and submittable without JS.
- Every icon-only control (`×`, toggle) carries `aria-label`; decorative SVG carries `aria-hidden="true"`.
- Focus is visible on every control via `:focus-visible` + `--focus-ring` — never removed.
- The autosave indicator is `aria-live="polite"` so a screen reader announces «Сохранено» without stealing focus.
- Colour is never the sole carrier of state: draft/enabled/incomplete each pair colour with text (a badge, a hint or a toggle label).
- `prefers-reduced-motion` is already honoured project-wide; the new `rise` entrance on the editor grid must sit inside that guard.

---

## UI Considerations

Applicable state considerations resolved: **9 covered, 2 backstop, 2 unresolved**

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | schedules-in-editor | ✅ covered | An ad with zero schedules renders the «Расписаний пока нет — объявление не будет отправляться» state with the «+ ДОБАВИТЬ ПЕРВОЕ» action, not a bare empty section |
| empty | groups-for-selected-account | ✅ covered | Selecting an account with no connected groups renders «У выбранного аккаунта нет подключённых групп» inside the groups block, and the toggle stays disabled |
| empty | no-messenger-accounts | ✅ covered | A user with zero accounts sees «Сначала подключите аккаунт мессенджера» with a link to `/accounts` instead of an empty account chip strip |
| empty | preview-nothing-composed | ✅ covered | An untouched `/ads/new` preview renders «Здесь появится объявление, каким его увидят участники групп», never an empty frame |
| empty | summary-list-collection | ✅ covered | Zero schedules renders «Расписаний пока нет» pointing at `/ads`; zero *matches* renders «Расписания не найдены» with «СБРОСИТЬ ФИЛЬТРЫ» — the two are distinct states with distinct copy |
| loading | autosave-in-flight | ✅ covered | While the autosave request is in flight the indicator reads «Сохраняем…»; it is driven by htmx's `.htmx-request` class, so it cannot desync from the actual request |
| error | autosave-failure | ✅ covered | A failed autosave leaves the indicator at «Не сохранено — проверьте соединение» in `--danger` and does not clear or revert the user's typed text |
| error | upload-rejected | ✅ covered | Server rejection by type, count or transport each render their own `alert(..., 'error')` naming the next step; the already-attached tiles are untouched |
| partial | incomplete-schedule | ✅ covered | A schedule missing account, groups, days or times saves with `is_active=false`, shows the «Не заполнено» badge and the «Заполните группы, дни и время» hint, and its toggle is `disabled` (D-08) |
| overflow | long-ad-title, long-group-name, long-account-name | 🧪 backstop | Single-line ellipsis (`white-space:nowrap; overflow:hidden; text-overflow:ellipsis; min-width:0`) on the ad title in a summary card, on group names and on account chip labels, with the full value in `title`. Held-out visual UI-state test at 320px with a 200-character title. |
| long-text | preview-body | 🧪 backstop | The preview paragraph uses `white-space:pre-wrap; overflow-wrap:anywhere` so a 4000-character body or an unbroken URL wraps inside the frame instead of widening the sticky column. Held-out visual UI-state test. |
| zero-one-many | attachments-in-preview | ⚠ unresolved | The mockup shows exactly one 118px media block. The layout for 2…10 attachments in the preview (row of thumbnails vs. first-image-plus-count) is not specified by the mockup and is left as a planner assumption: default to a wrapping row of 120px-tall thumbnails in send order. |
| overflow | text-length-limit | ⚠ unresolved | The counter's limit is declared as 4096 with `--warn` at ≥90% and `--danger` above. Telegram applies a lower cap (~1024) to captions sent *with* media, which would make the counter lie for ads that have attachments. The planner must verify the real cap in `app/messengers/*` before wiring the threshold; the send protocols themselves must not be modified. |

---

## Registry Safety

Not applicable — no component registry is used. The project has no `components.json`, no `package.json` at the application level, no build step and zero external asset requests. Every component is a Jinja2 macro in `app/templates/components/`, authored in-repo.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable — no third-party UI code enters the build |

---

## Security Notes Carried Into the Visual Layer

These are Phase 1 debts closed in this phase (D-19…D-21) that have a rendering consequence, so the contract states them:

- Image keys come from user-controlled filenames and are stored in `Ad.images`. Any client-side rendering of a tile or preview thumbnail **must build DOM nodes and assign properties** — `innerHTML`, `outerHTML`, `insertAdjacentHTML` and `document.write` remain at zero in the project and the tests check the source.
- The attachment count limit is a **server** rule (D-13); the hidden «+ ФАЙЛ» tile is a convenience, never the enforcement point.
- The upload type check moves off the client-supplied `Content-Type` (CR-02); the error copy above is what the user sees when the server-side check rejects a file.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
