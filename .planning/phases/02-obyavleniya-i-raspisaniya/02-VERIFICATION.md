---
phase: 02-obyavleniya-i-raspisaniya
verified: 2026-08-11T13:55:00Z
status: passed
score: 4/5 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/5
  gaps_closed:

    - "SC-1 (CR-04): клиентская половина контракта автосохранения замкнута — hx-sync формы #ad-form переведён с отмены на `this:queue last` (form.html:66); семантика очереди подтверждена по поставляемому бандлу htmx.min.js (`m.indexOf(\"queue\")===0 → p=(V[1]||\"last\")`, `p===\"last\" → queuedRequests=[]; push(переиздание)` — переиздание идёт ПОСЛЕ завершения летящего запроса, ответ не выбрасывается); контракт разметки перевёрнут (test_ads_editor.py:1032 утверждает очередь, :1035 — отсутствие отмены); именованная регрессия наложения test_save_during_autosave_overlap_lands_in_one_published_ad зелёная — прогнана верификатором"
    - "SC-2: тот же корень, та же правка — регрессия наложения несёт два ключа images во втором запросе и утверждает одну строку ads с обоими ключами в порядке отправки; путь /ads/{id}/edit был выполнен ещё в прошлом раунде"
    - "CR-03: reject_explicit_null (@field_validator на group_ids/days_of_week/times_of_day/timezone, routes/schedules.py:49-56) даёт 422 ДО записи; вторая линия `or []` в schedule_row.html:110 и sched_card.html:66/68/173/183; 9 тестов двух новых файлов зелёные — прогнаны верификатором"
    - "Прослеживаемость ADS-04/ADS-05: основание переписано на ОБЕ половины (REQUIREMENTS.md:209-210 называют 02-08 и 02-13; абзац :243 называет именованную регрессию и фактическое число прогона); число 895 passed подтверждено НЕЗАВИСИМЫМ полным прогоном верификатора (exit 0, 1018.83s)"
  gaps_remaining: []
  regressions: []
behavior_unverified_items:

  - truth: "SC-5: Редактор объявления и список расписаний пригодны к использованию на мобильных ширинах"
    test: >-
      Открыть /ads/{id}/edit и /schedules на стенде одноразовой базы
      (docker start broadcaster-disposable-0013, рецепт в 02-12-SUMMARY) при
      ширинах 320px и 400px и пройти двенадцать формулировок B-1…B-12 из
      02-12-SUMMARY (индикатор автосохранения в ошибке; длинные имена аккаунта,
      группы и заголовка; карточка 7 дней × 6 времён; предпросмотр 4000 символов
      и неразрывная ссылка; экранирование в окне подтверждения).
    expected: >-
      Одна колонка на ≤900px, липкость правой колонки снята; порядок чтения
      содержание → расписания → предпросмотр → сводка → действия; «Сохранить» и
      «Отмена» в одном ряду, «УДАЛИТЬ ОБЪЯВЛЕНИЕ» отдельным рядом; горизонтального
      скролла нет; длинные значения обрезаются многоточием и не выталкивают тумблер.
    why_human: >-
      Все двенадцать формулировок объявлены verification: backstop и являются
      утверждениями о РЕНДЕРЕ. Медиазапросы присутствуют и предметны
      (app.css:463 ≤900px; :1470 ≤400px), 99 разметочных тестов зелёные, но
      правило может присутствовать и не давать заявленного рендера; браузерной
      оснастки в проекте нет. Исполнитель 02-12 сознательно оставил все
      двенадцать pending — верификатор поступает так же.
human_verification:

  - test: "Мобильные ширины 320px и 400px: двенадцать формулировок B-1…B-12 (см. behavior_unverified_items)"
    expected: "См. behavior_unverified_items"
    why_human: "Утверждения о рендере; браузерных измерений не выполнено ни одного"

  - test: >-
      Живой браузер, путь создания: открыть /ads/new, набрать текст, дождаться
      круга автосохранения и нажать «Сохранить» внутри него (или сразу после
      прикрепления вложения) — затем проверить список объявлений.
    expected: >-
      РОВНО одно объявление, опубликованное, с текстом на момент нажатия и всеми
      вложениями; сироты-черновика нет; индикатор автосохранения не зависает.
    why_human: >-
      Утверждение 02-13 о живом браузере («поставленный в очередь запрос
      сериализуется ПОСЛЕ внеполосной подмены #ad-id-field») объявлено
      verification: backstop. Верификатор подтвердил семантику очереди по коду
      поставляемого бандла и закрепил секвенс серверной регрессией, но живой
      браузерный прогон в проекте невыполним автоматически.

  - test: >-
      Предпросмотр объявления с текстом и 2-3 вложениями провести до РЕАЛЬНОЙ
      доставки в Telegram и WhatsApp и сравнить с рамкой предпросмотра.
    expected: "Внутри рамки — только текст и вложения в том же порядке; заголовок объявления в сообщение не входит"
    why_human: >-
      Действие в адрес живых получателей, которое нельзя отозвать, плюс суждение
      о визуальном соответствии. Половина ожидания (заголовок вне рамки на
      стороне предпросмотра) подтверждена на стенде исполнителем 02-12.

  - test: >-
      Судейский запрет 02-04: счётчик длины не вводит в заблуждение — порог 1024
      при вложениях, 3686 без них, maxlength нет, сохранение сверх порога проходит.
    expected: "Поведение подтверждено человеком"
    why_human: "Запрет объявлен verification: judgment; автономный вердикт модели НЕ авторитетен"

  - test: >-
      Судейский запрет 02-15 (unverified-prohibition — human review recommended):
      «отметка Complete не опережает наблюдаемое поведение кода». НЕАВТОРИТЕТНЫЙ
      вердикт верификатора: PASS — основание :243 называет удерживающие тесты и
      число прогона, число подтверждено независимым прогоном (895 passed), сами
      удерживающие тесты прогнаны и зелёные.
    expected: "Человек подтверждает, что основание отметок честно"
    why_human: "verification: judgment — в автономном прогоне вердикт LLM-судьи не авторитетен и обязан быть флагован"

  - test: >-
      Решение о ревизии 0013 на ЦЕЛЕВОЙ базе: она остаётся на 0012, колонка
      ads.status в живой схеме отсутствует.
    expected: "Владелец решает, когда и как выкатить 0013 на целевую базу"
    why_human: >-
      Человек выбрал apply-to-disposable, это исполнено корректно (прошлая
      верификация). Следствие остаётся: до выката ни один критерий фазы не
      наблюдаем в проде. Верификатор БД не мутирует.
---

# Phase 2: Объявления и расписания — Verification Report (третий заход: после второго раунда закрытия пробелов)

**Phase Goal:** Пользователь ведёт объявление и его расписания целиком из редактора объявления, а сводный список расписаний становится местом обзора и управления состоянием.
**Verified:** 2026-08-11T13:55:00Z
**Status:** human_needed
**Re-verification:** Да — после планов закрытия пробелов 02-13…02-15

## Вердикт — прямо и первым

**Все три пробела предыдущей верификации закрыты, и закрыты по существу, а не по отчёту.** Верификатор не принял ни один SUMMARY на веру: атрибут прочитан в шаблоне, семантика очереди прочитана в поставляемом бандле, валидатор прочитан в маршруте, все новые регрессии прогнаны собственноручно, и полная суита прогнана НЕЗАВИСИМО — **895 passed, 0 failed, exit 0 (1018.83s)** — число совпало с записанным в REQUIREMENTS.md байт-в-байт.

Автоматически проверяемых пробелов не осталось. Статус `human_needed`, а не `passed`, потому что остаются человеческие измерения: SC-5 (мобильный рендер, двенадцать backstop-формулировок B-1…B-12), живой браузерный прогон наложения (backstop 02-13), реальная доставка предпросмотра, два судейских запрета и решение владельца о выкате ревизии 0013 на целевую базу.

### Три точки закрытия CR-04, подтверждённые независимо

1. **Атрибут заменён.** `app/templates/ads/form.html:66` — `hx-sync="this:queue last"`, на той же форме `#ad-form`, тем же триггером (`hx-trigger="submit, keyup changed delay:2s, change delay:2s"`, :65 — не тронут). `grep -rn 'this:replace' app/templates/` — **ноль вхождений** (стратегии отмены нет ни в атрибутах, ни в комментариях).
2. **Семантика очереди подтверждена по поставляемому бандлу, а не по документации.** `app/static/js/htmx.min.js`: `else if(m.indexOf("queue")===0){var V=m.split(" ");p=(V[1]||"last").trim()}` — атрибут даёт `p="last"`; при летящем xhr `else if(p==="last"){f.queuedRequests=[];f.queuedRequests.push(function(){he(t,r,n,i,a)})}` — запрос кладётся в очередь и **переиздаётся из живой формы после завершения летящего** (колбэк `w` исполняет очередь после `f.xhr=null`), то есть уже после внеполосной подмены `#ad-id-field` (`autosave_response.html:34`, `hx-swap-oob="true"`). Ответ с идентификатором больше не выбрасывается — ровно та правка, которую называла верификация.
3. **Контракт перевёрнут и удержан тестами, прогнанными верификатором.** `tests/test_pages/test_ads_editor.py:1032` утверждает `'hx-sync="this:queue last"' in html`, `:1035` — `"this:replace" not in html`. Именованная регрессия наложения (`:480-535`) делает ровно то, что требовала верификация: первый POST без `ad_id`, идентификатор извлекается ИЗ ТЕЛА ОТВЕТА (внеполосный блок), второй POST с извлечённым `ad_id`, `save="1"`, вторым текстом и ДВУМЯ ключами вложений — и утверждает РОВНО одну строку `ads`, `AD_STATUS_PUBLISHED`, текст и вложения второй сериализации. Оба теста зелёные: **2 passed** (прогон верификатора).

Этим же закрыт SC-2 на пути создания: второй запрос регрессии несёт ключи `images` — путь `requestSave()` после загрузки вложения, тот же триггер той же формы.

### Закрытие CR-03, подтверждённое независимо

`app/routes/schedules.py:49-56`: `reject_explicit_null` — `@field_validator("group_ids", "days_of_week", "times_of_day", "timezone")`, `ValueError` при `v is None` → 422 силами FastAPI **до какого-либо setattr** (валидатор схемы отрабатывает до входа в обработчик). Валидаторы pydantic не запускаются на значениях по умолчанию — отсутствующий ключ по-прежнему значит «не трогать», контракт частичного патча (D-15) не сужен, что удержано контрольным тестом валидного патча.

Вторая линия — разметка тотальна по данным базы: `schedule_row.html:110` (`d in (s.days_of_week or [])`), `sched_card.html:66` (`s.group_ids or []` — существовавший образец), `:68` (`(s.days_of_week or [])|sort`), `:173`, `:183` (`(s.times_of_day or [])`).

Прогон верификатора: `tests/test_routes/test_schedules_api_null.py` + `tests/test_pages/test_schedules_poisoned_row.py` — **9 passed**. Покрытие предметно, не дымовое: параметризация по всем четырём полям с перечитыванием строки через ORM после отказа и `GET /api/schedules → 200`; страничная регрессия бьёт по РАЗВЁРНУТОЙ карточке (`?sched={id}` — адрес действия сводного списка, свёрнутый рендер не исполняет :173/:183) и утверждает маркеры тумблера по SCH-05/D-08 (на паузе `disabled` с подсказкой «Возобновить нельзя: расписание не заполнено», у активной — `checked`, пауза доступна).

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria — контракт)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Черновик сохраняется, правки автосохраняются без «Сохранить», черновик визуально отличается в списке | ✓ VERIFIED | Сервер: маршрутизация create/update по `ad_id` из тела (02-08, регрессии зелёные). Клиент: `hx-sync="this:queue last"` (form.html:66), семантика очереди подтверждена по бандлу, наложение удержано именованной регрессией (прогнана: passed). Бейдж: `ad_card.html:51` — «Черновик»/«Опубликовано» с безопасным дефолтом в черновик. `this:replace` в шаблонах — 0 вхождений. Оговорка: живой браузер — backstop 02-13, вынесен в human items |
| 2 | Несколько вложений, удаление любого, предпросмотр в том виде, в каком уйдёт | ✓ VERIFIED | Путь /ads/{id}/edit выполнен и удержан с прошлого раунда (удаление одного вхождения с порядком, предпросмотр из перечитанного объекта, целость SendLog — именованный тест). Путь создания: корень (отмена запроса) устранён 02-13; регрессия наложения несёт два ключа `images` и утверждает их в ТОЙ ЖЕ строке в порядке отправки. Весь test_ads_editor.py зелёный в прогоне верификатора |
| 3 | Создать, изменить и удалить расписание прямо в редакторе; путь создания жив в каждом коммите | ✓ VERIFIED | Регрессионная перепроверка: test_schedule_creation_path_exists.py + test_editor_schedules.py зелёные (в составе 119 passed прогона верификатора). Дополнительно устранена оговорка прошлого раунда: испорченная строка больше не выключает редактор — poisoned_row-регрессия утверждает 200 и присутствие карточки `sched-{id}` |
| 4 | Сводный список показывает объявление, каналы, группы, дни и время; включение/пауза прямо из списка | ✓ VERIFIED | Регрессионная перепроверка: test_schedules_list.py зелёный. Оговорка прошлого раунда снята: при строке с None в любом списочном поле GET /schedules → 200, карточка отрисована, тумблер размечен по SCH-05/D-08 (poisoned_row-регрессия, прогнана верификатором) |
| 5 | Редактор и список расписаний пригодны на мобильных ширинах | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Без изменений с прошлого раунда: медиазапросы присутствуют и предметны (app.css:463 ≤900px; :1470 ≤400px), 99 разметочных тестов зелёные, но двенадцать формулировок B-1…B-12 — backstop-утверждения о рендере; браузерных измерений не выполнено ни одного. Присутствует и связано — поведение не доказано |

**Score:** 4/5 truths verified (1 present, behavior-unverified; 0 failed)

### Required Artifacts (раунд закрытия 02-13…02-15)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/templates/ads/form.html` | `this:queue last` на #ad-form | ✓ VERIFIED | :66; остальные атрибуты формы не тронуты; комментарий описывает прежнее поведение словами, не литералом |
| `tests/test_pages/test_ads_editor.py` | `test_save_during_autosave_overlap_lands_in_one_published_ad` + перевёрнутый контракт | ✓ VERIFIED | :480 (регрессия, извлекает id из тела ответа, а не из базы) и :1032-1035 (очередь утверждается, отмена запрещена); оба passed |
| `app/routes/schedules.py` | `reject_explicit_null` до записи | ✓ VERIFIED | :49-56, схема-уровень, до setattr; существующий validate_timezone сохранён |
| `tests/test_routes/test_schedules_api_null.py` | 422 на 4 поля + контроль валидного патча | ✓ VERIFIED | 5 тестов, засев публичными маршрутами, перечитывание через ORM; passed |
| `tests/test_pages/test_schedules_poisoned_row.py` | Оба экрана 200, маркеры тумблера | ✓ VERIFIED | 4 теста (3 параметризованных на паузе + активная), развёрнутая карточка `?sched=`; passed |
| `app/templates/schedules/includes/schedule_row.html` | `days_of_week or []` | ✓ VERIFIED | :110 + комментарий «вторая линия CR-03» |
| `app/templates/ads/includes/sched_card.html` | `days_of_week or []`, `times_of_day or []` | ✓ VERIFIED | :66/:68/:173/:183 |
| `.planning/REQUIREMENTS.md` | Основание ADS-04/ADS-05 называет обе половины | ✓ VERIFIED | :209 «сервер: 02-08, браузер: 02-13»; :210 «02-08, 02-10 и 02-13»; :243 называет именованную регрессию, 02-14 и «895 passed» |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| form.html #ad-form | pages/ads.py | #ad-id-field внеполосно из ответа; очередь гарантирует сериализацию ПОСЛЕ подмены | ✓ WIRED | autosave_response.html:34 (`hx-swap-oob="true"`) → form.html:129 (живое поле); бандл: queue last переиздаёт после завершения летящего; регрессия закрепляет секвенс |
| routes/schedules.py | models/schedule.py | отказ валидации ДО setattr | ✓ WIRED | field_validator на схеме — отрабатывает при парсинге тела, до обработчика; тест перечитывает строку и утверждает равенство засеянной |
| schedule_row.html | pages/schedules.py | рендер тотален по данным строки | ✓ WIRED | poisoned_row-регрессия: 200 + карточка при None в каждом списочном поле |
| REQUIREMENTS.md | tests/test_pages/test_ads_editor.py | основание называет удерживающую регрессию | ✓ WIRED | :243 несёт имя `test_save_during_autosave_overlap_lands_in_one_published_ad`; тест существует и зелёный |

### Data-Flow Trace (Level 4)

Без изменений относительно прошлого раунда (сводный список рендерит имена групп одним запросом со связкой по владельцу; предпросмотр — из перечитанного объекта). Новый элемент раунда: идентификатор созданного черновика течёт `_save_from_editor` → autosave_response.html:34 (OOB) → живое поле #ad-id-field → следующий запрос очереди → ветка обновления. Цепочка замкнута; статических возвратов и моков на пути нет.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Контракт очереди + регрессия наложения (02-13) | `uv run pytest -q tests/test_pages/test_ads_editor.py::test_editor_form_carries_autosave_and_stays_a_real_form tests/test_pages/test_ads_editor.py::test_save_during_autosave_overlap_lands_in_one_published_ad` | 2 passed | ✓ PASS |
| 422 на явный null + живучесть при порче (02-14) | `uv run pytest -q tests/test_routes/test_schedules_api_null.py tests/test_pages/test_schedules_poisoned_row.py` | 9 passed | ✓ PASS |
| Регрессия ранее верифицированных SC-3/SC-4 + соседние контракты | `uv run pytest -q tests/test_pages/test_schedule_creation_path_exists.py tests/test_pages/test_schedules_list.py tests/test_pages/test_editor_schedules.py tests/test_pages/test_ads_editor.py tests/test_routes/test_schedules_api_ownership.py` | 119 passed | ✓ PASS |
| Полный прогон (ровно один на верификацию) | `uv run pytest tests/ -q` | **895 passed, 0 failed, exit 0** (1018.83s) | ✓ PASS |
| Отмены нет в шаблонах | `grep -rn 'this:replace' app/templates/` | 0 вхождений | ✓ PASS |

### Probe Execution

Пробников `scripts/*/tests/probe-*.sh` фаза не объявляет (ни в PLAN, ни в SUMMARY раунда) — N/A. Роль пробника прошлого раунда (два POST без ad_id → 2 строки) теперь исполняет постоянная регрессия наложения — воспроизводимо и зелено.

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|----------------|-------------|--------|----------|
| ADS-04 | 02-01…02-08, 02-13, 02-15 | Черновик + автосохранение | ✓ SATISFIED | Обе половины контракта закрыты и удержаны тестами; основание отметки честно (:209) |
| ADS-05 | 02-02…02-10, 02-13, 02-15 | Несколько вложений, удаление | ✓ SATISFIED | Оба пути (edit и create) закрыты; наложение с вложениями — именованная регрессия |
| ADS-06 | 02-04, 02-07 | Предпросмотр как уйдёт | ✓ SATISFIED (частично NEEDS HUMAN) | Предпросмотр из перечитанного объекта; визуальное соответствие ДОСТАВЛЕННОМУ — human item |
| ADS-07 | 02-05, 02-09, 02-14 | CRUD расписаний в редакторе | ✓ SATISFIED | Три настоящие формы; редактор жив при испорченной строке (02-14) |
| ADS-08 | 02-05, 02-09 | Расписание с группами/днями/временем | ✓ SATISFIED | Без изменений с прошлого раунда; регрессии зелёные |
| SCH-04 | 02-06, 02-11, 02-14 | Сводный список с составом | ✓ SATISFIED | Список полон; жив при испорченной строке |
| SCH-05 | 02-06, 02-11, 02-14 | Вкл/пауза из списка | ✓ SATISFIED | Тумблер настоящей формой; испорченная строка размечена как неполная (D-08) |

Orphaned requirements: нет — все семь ID фазы объявлены в планах и все семь Complete в REQUIREMENTS.md (:209-:215). Счётчики v2.0 сходятся (39 total, Complete: 13).

### Prohibitions (must-NOT)

| Запрет | Tier | Status | Evidence |
|--------|------|--------|----------|
| 02-13: автосохранение не уничтожает работу молча (сирота+дубль недопустимы) | test | ✓ VERIFIED | Удержан `test_save_during_autosave_overlap_lands_in_one_published_ad` — прогнан, passed |
| 02-14: один вызов собственного API не приводит интерфейс в невосстановимое состояние | test | ✓ VERIFIED | Удержан обоими новыми файлами (422 до записи + обе страницы 200 при порче) — прогнаны, passed |
| 02-15: отметка Complete не опережает код | judgment | ⚠ FLAGGED (unverified-prohibition — human review recommended) | НЕАВТОРИТЕТНЫЙ вердикт: PASS — основание называет тесты и число, число подтверждено независимым прогоном |
| 02-04: счётчик длины не вводит в заблуждение | judgment | ⚠ FLAGGED (перенесён) | Остаётся человеческим измерением |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | — | — | TBD/FIXME/XXX/TODO/HACK/placeholder-грепы по всем файлам раунда (form.html, routes/schedules.py, оба шаблона карточек, три тестовых файла) — чисто. Прежний блокер-паттерн (тест, закреплявший стратегию отмены как контракт, test_ads_editor.py:973 старой нумерации) устранён — утверждение перевёрнуто на очередь |

Named-известные предупреждения ВНЕ гапов фазы (перенос без изменений): WR-09 (`times_of_day: ["nope"]` на JSON-входе → 500), WR-10 (`POST /api/schedules` создаёт активное незаполненное), WR-11 (разметка переписывает предикат полноты inline), D-02-09-01 (deferred-items.md: `POST /api/groups` не проверяет владение аккаунтом — не дыра сегодня благодаря `owned_group_ids`, закрытие предложено). Все четыре — кандидаты следующего цикла, ни один не блокирует критерии фазы.

### Human Verification Required

См. frontmatter `human_verification` — шесть пунктов: (1) мобильные ширины B-1…B-12; (2) живой браузерный прогон наложения автосохранения (backstop 02-13); (3) предпросмотр до реальной доставки; (4) судейский запрет 02-04 о счётчике; (5) судейский запрет 02-15 о прослеживаемости (флаг unverified-prohibition, неавторитетный PASS); (6) решение о выкате ревизии 0013 на целевую базу — до выката ни один критерий фазы не наблюдаем в проде.

### Gaps Summary

Автоматически проверяемых пробелов нет. Оба блокера предыдущей верификации (CR-04 и CR-03) закрыты по существу и удержаны именованными регрессиями, прогнанными верификатором; прослеживаемость приведена к состоянию кода и подтверждена независимым полным прогоном (895 passed, exit 0). Регрессий по ранее верифицированным истинам SC-3/SC-4 не найдено — наоборот, обе оговорки прошлого раунда (испорченная строка выключала оба экрана) сняты второй линией 02-14. Фаза ждёт только человеческих измерений: мобильный рендер, живой браузер, реальная доставка, два судейских запрета и решение владельца о выкате миграции.

---

_Verified: 2026-08-11T13:55:00Z_
_Verifier: Claude (gsd-verifier)_
