# Phase 2: Объявления и расписания - Context

**Gathered:** 2026-08-10
**Status:** Ready for planning

<domain>
## Phase Boundary

Фаза 2 делает редактор объявления единственным местом настройки расписаний и вводит черновик как состояние объявления. В границе: статус `Ad.status` с миграцией схемы, автосохранение содержимого, несколько вложений, предпросмотр, полный CRUD расписаний внутри редактора, снос старых экранов создания/редактирования расписаний и превращение сводного списка расписаний в read-mostly обзор с включением и паузой.

Покрывает ADS-04, ADS-05, ADS-06, ADS-07, ADS-08, SCH-04, SCH-05.

**Правило границы.** Фаза меняет модель взаимодействия с объявлением и расписанием. Она НЕ трогает протоколы отправки Telegram/WhatsApp/MAX, НЕ меняет схему `Schedule`, НЕ вводит новых осей биллинга и НЕ переносит сюда работу Фаз 3–6 (экран групп аккаунта, дашборд, история, тарифы, админка).

**Единственное отступление от «интерфейс, а не функция»:** планировщик отправки получает новое условие — расписания объявлений в статусе черновика пропускаются (D-01). Это правка `app/worker/tasks.py` рядом с боевым пайплайном, и она обязательна: без неё бейдж «ЧЕРНОВИК» врёт пользователю.

**Долг Фазы 1, закрываемый здесь:** CR-01, CR-02, WR-01 (открытый гейт код-ревью `01-REVIEW.md`, `status: issues_found`, `critical: 2`) и тестовый пробел с глобалом `s3_public_url`.

</domain>

<decisions>
## Implementation Decisions

### Черновик и статус объявления

- **D-01:** Черновик не отправляется. Планировщик пропускает расписания объявлений в статусе черновика — статус имеет реальный эффект, а не только визуальный. Затрагивает запрос выбора наступивших расписаний в `app/worker/tasks.py`. — **Reversibility:** costly — условие уходит в боевой пайплайн отправки, откат означает повторную проверку всех тестов диспетчеризации.
- **D-02:** Вводится `Ad.status` (черновик / опубликовано), существующие записи мигрируют в «опубликовано». Поле `Ad.is_active` **удаляется** из модели, формы и миграции — сегодня оно переключается в форме и не проверяется нигде в коде отправки, то есть является мёртвым флагом. Одно состояние — одно поле. — **Reversibility:** one-way — требует миграции схемы (add `status`, drop `is_active`); откат — обратная миграция с восстановлением значений.
- **D-03:** `/ads/new` открывает пустой редактор и **не создаёт запись**. Первое автосохранение создаёт объявление в статусе черновика и подменяет URL на `/ads/{id}/edit`. Пустых записей от случайного захода в раздел не появляется.
- **D-04:** Публикация — это кнопка «Сохранить» из макета. Отдельной кнопки «Опубликовать» нет: макет содержит только «Сохранить» и «Отмена».

### Автосохранение

- **D-05:** Автосохранение по паузе ввода ~1.5–3 с, штатным HTMX-триггером (`hx-trigger="keyup changed delay:..."`), без собственного JS-таймера и без обработчика ухода со страницы. Продолжает линию Фазы 1: вендоренный htmx 1.9.10, своего JS минимум.
- **D-06:** Ответ автосохранения возвращает обновлённый блок предпросмотра — один запрос закрывает и сохранение, и обновление превью.

### Модель сохранения расписаний в редакторе

- **D-07:** Каждое расписание сохраняется **сразу и отдельно**, своим HTMX-запросом. Клиентского состояния, которое можно потерять при обрыве связи, нет. Это сознательное отступление от текста макета («сначала создастся объявление, затем привяжутся расписания») — оно возможно потому, что по D-03 объявление к моменту настройки расписаний уже существует как черновик.
- **D-08:** Неполное расписание **сохраняется, но выключенным** (`is_active=false`). Включение тумблера доступно только когда заполнены аккаунт, хотя бы одна группа, хотя бы один день и хотя бы одно время. Макетный бейдж «ЧЕРНОВИК» на карточке расписания и подсказка «Заполните группы, дни и время» означают именно это состояние — незаполненность, а не несохранённость.
- **D-09:** Базовый путь редактора работает **без JS**: сохранение объявления и удаление расписания выполняются обычной формой POST. Автосохранение, живое превью и сворачивание карточек — прогрессивное улучшение поверх работающей формы. Продолжает правило Фазы 1 и даёт тестам проверяемый серверный маршрут.

### Предпросмотр

- **D-10:** Предпросмотр рендерится **на сервере** и приходит ответом автосохранения (см. D-06). Превью показывает то, что реально лежит в БД, то есть то, что уйдёт в группы. Отставание на паузу автосохранения принято.
- **D-11:** Один вид предпросмотра для всех каналов. Подпись канала показывает, куда уйдёт объявление (по настроенным расписаниям), но визуал единый — все три адаптера (`telegram_user`, `whatsapp`, `max`) принимают один и тот же `text` + `images`. Переключателя каналов с разными рамками нет.

### Вложения

- **D-12:** Удаление вложения убирает ключ только из `Ad.images`; объект в S3 **остаётся**. Причина: `SendLog` хранит снапшоты отправленного контента, и удаление объекта из хранилища оставило бы историю с битыми картинками. Сироты в хранилище — принятая цена.
- **D-13:** Лимит вложений переносится **на сервер**: сервер отклоняет сохранение объявления с числом вложений сверх лимита. Браузерная проверка остаётся удобством, а не единственной защитой. Ложится в один ряд с CR-01/CR-02/WR-01 — клиентским данным в этой фазе перестают верить.

### Переезд создания расписаний

- **D-14:** Страницы `/schedules/new` и `/schedules/{id}/edit` и шаблон `app/templates/schedules/form.html` **удаляются целиком**. Один способ настроить расписание, мёртвого кода не остаётся.
- **D-15:** JSON-API `app/routes/schedules.py` (POST / PUT / DELETE / toggle) **остаётся как есть** по составу маршрутов и контракту — фаза меняет страничный слой. Единственная правка API — закрытие дыры владения из D-18.
- **D-16:** Порядок выката задаётся порядком планов: сначала отдельный план доводит расписания в редакторе объявления до рабочего состояния, и только следующий план удаляет `/schedules/new` и `/schedules/{id}/edit`. Между ними оба пути живы — критерий 3 фазы («ни в один момент выката пользователь не остаётся без возможности создать расписание») выполняется структурой планов, а не обещанием. — **Reversibility:** reversible.

### Сводный список расписаний

- **D-17:** Из макета берутся карточки, поиск по объявлению и фильтры-чипсы; **прогресс-бар отправок не делается** — в макете он показывает выдуманный процент, которому нет источника в данных. Макрос сворачиваемых фильтров существует с Фазы 1 (`01-04-PLAN.md`) и переиспользуется.
- **D-18:** Действия в списке: тумблер включения/паузы на месте плюс переход в редактор объявления к этому расписанию. Удаление расписания живёт **только** в редакторе — так требует ADS-08 и так устроен макет.

### Долги Фазы 1

- **D-19:** CR-01, CR-02 и WR-01 закрываются **отдельным первым планом** фазы, до того как начнётся переделка редактора. Гейт код-ревью закрывается рано, фиксы ложатся на стабильный код, а не на движущуюся цель.
- **D-20:** Фикс CR-01 покрывает **оба слоя**. Проверено по коду: страничный слой (`app/pages/schedules.py:204-213,314-315`) не проверяет ни `ad_id`, ни `account_id`; JSON-API (`app/routes/schedules.py:67-73`) проверяет владение объявлением через `AdRepository.get_by_id_and_user`, но **не проверяет владение `account_id`** — чужой аккаунт проходит и там. Проверка владения обоими идентификаторами вводится на обоих входах.
- **D-21:** Глобал `s3_public_url` в `app/pages/common.py:38` собирает `Settings()` в обход подмены зависимостей, из-за чего `/ads/new` и `/ads/{id}/edit` не рендерятся ни одним тестом суиты. Чинится **до переделки редактора**, отдельной задачей, вместе с первым рендер-тестом редактора — иначе вся фаза строится на непокрытом тестами экране. Дефект существует на базовом коммите и не внесён Фазой 1.

### Claude's Discretion

- **Расписания черновиков в сводном списке.** Вопрос поднят, но не разобран: черновик не отправляется (D-01), значит его расписания в списке никогда не сработают. Разумный дефолт — показывать их с явной пометкой, что объявление в черновике и отправки не идут; решение за планировщиком, если исследование не покажет иного.
- **Лимиты тарифа и черновики.** Считать ли черновик в лимит объявлений по тарифу — не решено. Дефолт: считать так же, как сейчас считаются объявления, новой оси биллинга не вводить (это Фаза 5).
- Индикатор состояния автосохранения («сохранено» / «сохраняем»), точное значение задержки в пределах 1.5–3 с, раскладка редактора и предпросмотра на мобильных ширинах (брейкпоинт редактора 900px из макета), порядок вложений и необходимость drag-n-drop, состав фильтров сводного списка, таймзона расписания (берётся из профиля пользователя как сейчас, в карточке расписания не выбирается), поведение выбранных групп при смене аккаунта в расписании, потолок числа расписаний на одно объявление.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Дизайн — источник истины
- `new_broadcaster_design.html` — канонический источник дизайна для всего milestone v2.0. Bundled-страница; читать **распакованную копию**, а не грепать оригинал.
- `design/new_broadcaster_design.unpacked.html` — распакованная рабочая копия (сделана в Фазе 1, D-20). Для этой фазы значимы: **редактор объявления** — строки 520–709 (две колонки, блок «Содержание» с счётчиком длины, «ВЛОЖЕНИЯ» плитками, секция «Расписания» со сворачиваемыми карточками, правая sticky-колонка с предпросмотром, сводкой и кнопками); **сводный список расписаний** — строки 737–805 (пояснение «Создание и настройка расписаний — на странице объявления», поиск, фильтры-чипсы, карточки с тумблером, сеткой дней и временами).
- `design/README.md` и `design/unpack.js` — как распаковывать, если понадобится пересобрать копию.

### Планирование milestone
- `.planning/ROADMAP.md` §«Phase 2: Объявления и расписания» — цель фазы, 5 критериев успеха, заметка про миграцию `Ad.status`; §«Жёсткие рамки milestone» — SPA исключён, протоколы отправки не трогаются, адаптивность сквозная.
- `.planning/REQUIREMENTS.md` §«Объявления» и §«Расписания» — формулировки ADS-04…ADS-08, SCH-04, SCH-05.
- `.planning/PROJECT.md` §Constraints, §Key Decisions — стек зафиксирован, интерфейс серверный на Jinja2 с точечным HTMX/Alpine.

### Решения Фазы 1, действующие здесь
- `.planning/phases/01-interfeysnyy-fundament/01-CONTEXT.md` — D-10 (только тёмная тема), D-13 (компоненты — макросы через `{% import %}`), D-15 (одна разметка `data-row`/`data-hrow`), D-02 (без build-шага), D-05 (htmx 1.9.10, Alpine 3.13.3 вендорены).
- `.planning/phases/01-interfeysnyy-fundament/01-03-SUMMARY.md` — эталонная миграция раздела «Объявления»: структура списочной страницы, сентинел бесконечной прокрутки, карточка как макрос.
- `.planning/phases/01-interfeysnyy-fundament/01-04-SUMMARY.md` — разделы «Расписания» и «Группы»: сигнатура макроса `filters`, раскладка колонок, как тумблер отправляет прежний запрос.

### Долги и находки безопасности
- `.planning/phases/01-interfeysnyy-fundament/01-REVIEW.md` — CR-01 и CR-02 с точными местами.
- `.planning/phases/01-interfeysnyy-fundament/01-SECURITY.md` — WR-01 / T-10-04, принята как риск R-01.
- `.planning/STATE.md` §Blockers/Concerns и §Deferred Items — решение UAT 2026-08-10 чинить всё три в Фазе 2, плюс запись про тестовый пробел `s3_public_url`.

### Карты кодовой базы
- `.planning/codebase/STRUCTURE.md` — расположение `app/pages/`, `app/routes/`, `app/templates/`.
- `.planning/codebase/CONVENTIONS.md`, `.planning/codebase/STACK.md` — принятые соглашения и стек.
- `.planning/codebase/TESTING.md` — как устроена суита (SQLite in-memory, httpx AsyncClient, фикстуры `client`/`db_session`/`auth_headers`).

Внешних ADR/спеков по объявлениям и расписаниям в репозитории нет — всё остальное зафиксировано в решениях выше.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `app/models/ad.py` — `Ad.images` уже JSON-список; `Ad.is_active` присутствует и подлежит удалению (D-02). Поля `status` нет — нужна миграция Alembic.
- `app/models/schedule.py` — `Schedule` уже привязан к `ad_id`, хранит `group_ids`, `days_of_week`, `times_of_day`, `timezone`, `is_active`, `next_run_at`. Схема расписаний **не меняется**. `account_id` nullable с `ON DELETE SET NULL` (issue #35), relationships `lazy="raise"`.
- `app/templates/ads/form.html` (122 строки) — текущий редактор: множественная загрузка до 10 файлов через `/api/uploads/image`, хранение ключей в скрытых инпутах `name="images"`, сборка разметки узлами DOM. Тесты `tests/test_templates/test_ads_form_security.py` фиксируют этот способ сборки — их контракт нельзя ломать.
- `app/routes/uploads.py` — `safe_filename()` уже нормализует клиентское имя файла (фикс Фазы 1). Проверка типа по клиентскому `content_type` (строки 48–52) — это и есть CR-02.
- `app/pages/schedules.py:327` — `/schedules/{schedule_id}/toggle` уже существует; SCH-05 опирается на готовый маршрут, а не пишет новый.
- Макрос сворачиваемых фильтров из `01-04-PLAN.md` — переиспользуется сводным списком (D-17).
- Все три адаптера `app/messengers/{telegram_user,whatsapp,max}.py` принимают `images: list[str] | None` — множественные вложения отправляются без правки протоколов.

### Established Patterns
- Списочные страницы отдают только `layout=cards`; шести файлов `*_rows.html` больше нет (удалены в Фазе 1). Диспетчеризация HTMX идёт через query-параметр, `hx-target` в шаблонах отсутствует.
- Подтверждение удаления — собственная панель с настоящей формой POST, без `confirm()`; 13 таких мест, тесты `*_degrades_without_alpine`.
- Клиентская разметка строится узлами DOM; `innerHTML` / `outerHTML` / `insertAdjacentHTML` / `document.write` — ноль по проекту, проверяется по исходнику.
- `active_page` проставляется всеми роутерами `app/pages/*.py` — механизм подсветки раздела существует.
- Тесты: `sqlite+aiosqlite:///:memory:`, полное создание схемы на тест, фикстуры в `tests/conftest.py`. 393 существующих теста обязаны остаться зелёными.

### Integration Points
- **`app/worker/tasks.py`** — запрос наступивших расписаний; сюда добавляется условие пропуска черновиков (D-01). Самое чувствительное место фазы: рядом идёт боевая диспетчеризация отправок.
- **Миграция Alembic** — `Ad.status` добавляется, `Ad.is_active` удаляется (D-02). Единственное изменение схемы в фазе.
- **`app/pages/ads.py`** — `ads_new`/`ads_create`/`ads_edit`/`ads_update` переписываются под черновик и автосохранение (D-03…D-06); здесь же WR-01 (строки 133–135, 183–187).
- **`app/pages/schedules.py`** — `schedules_new`/`schedules_edit` удаляются (D-14), `schedules_partial`/`schedules_list`/`schedules_toggle` остаются и переверстываются; здесь же CR-01 (строки 204–213, 314–315).
- **`app/routes/schedules.py:67-73`** — второй вход CR-01: владение `account_id` не проверяется (D-20).
- **`app/routes/uploads.py:48-52`** — CR-02, клиентский `Content-Type`.
- **`app/pages/common.py:38`** — глобал `s3_public_url`, из-за которого редактор непокрыт тестами (D-21).

</code_context>

<specifics>
## Specific Ideas

- Редактор — две колонки из макета: слева «Содержание» (название, текст со счётчиком длины, вложения плитками 86×86 с крестиком и плиткой «+ ФАЙЛ») и «Расписания», справа sticky-колонка с предпросмотром, блоком сводки «ключ — значение» и кнопками «Сохранить» / «Отмена» плюс «УДАЛИТЬ ОБЪЯВЛЕНИЕ» под ними.
- Карточка расписания в редакторе: свёрнутая строка — иконка канала, имя аккаунта, дни, времена, число групп, бейдж «ЧЕРНОВИК» при незаполненности, тумблер; развёрнутая — выбор аккаунта чипсами, список групп с чекбоксами и «выбрать все», сетка из 7 дней с пресетами «БУДНИ» и «КАЖДЫЙ ДЕНЬ», чипсы времени с `input type="time"` и «+ ВРЕМЯ», внизу «УДАЛИТЬ РАСПИСАНИЕ».
- Пустое состояние расписаний в редакторе: «Расписаний пока нет — объявление не будет отправляться» и кнопка «+ ДОБАВИТЬ ПЕРВОЕ».
- Сводный список открывается пояснением «Сводный список: здесь можно включить или поставить на паузу. Создание и настройка расписаний — на странице объявления.» — это текст макета, и он же объясняет пользователю переезд.
- Пустое состояние выбранного аккаунта: «У выбранного аккаунта нет подключённых групп».

</specifics>

<deferred>
## Deferred Ideas

- **Фоновая уборка файлов-сирот в S3** — удалённые вложения остаются в хранилище (D-12). Отдельная задача с учётом снапшотов `SendLog`; в v2.0 не входит.
- **Прогресс-бар отправок в карточке расписания** — визуал есть в макете, источника данных нет (D-17). Возвращаться к нему имеет смысл после Фазы 4, где появляются агрегации по `SendLog`.
- **Переключатель канала в предпросмотре** с разными рамками Telegram / WhatsApp / MAX — отклонено в этой фазе (D-11), требует знания реальных ограничений каждого канала.
- **Число вложений как ось тарифа** — новая ось биллинга, относится к Фазе 5.
- **Экран групп аккаунта** (GRP-04…GRP-08) и удаление пункта «Группы» из навигации — Фаза 3.
- **Обновление HTMX до 2.x** — отложено с Фазы 1, отдельная задача после v2.0.

</deferred>

---

*Phase: 2-Объявления и расписания*
*Context gathered: 2026-08-10*
