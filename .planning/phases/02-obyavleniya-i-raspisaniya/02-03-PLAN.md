---
phase: 02-obyavleniya-i-raspisaniya
plan: 03
type: tdd
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/constants.py
  - app/models/ad.py
  - alembic/versions/0013_ad_status.py
  - app/pages/ads.py
  - app/pages/dashboard.py
  - app/pages/schedules.py
  - app/routes/ads.py
  - app/application/scheduling/use_cases.py
  - app/templates/ads/includes/ad_card.html
  - app/templates/ads/form.html
  - tests/test_constants.py
  - tests/test_models/test_ad.py
  - tests/test_application/test_collect_due_draft.py
  - tests/test_pages/test_ads_status.py
  - tests/test_migrations/test_0013_ad_status.py
autonomous: false
requirements: [ADS-04]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Объявление имеет состояние «черновик» или «опубликовано»; существующие записи после миграции опубликованы (D-02)"
    - "Черновик визуально отличается в списке `/ads`: бейдж «Черновик» в варианте warning против «Опубликовано» в варианте success (SC-1, UI-SPEC E15)"
    - "Нераспознанное значение состояния отображается как «Черновик» — безопасный дефолт, потому что черновик не отправляется (UI-SPEC E15 error)"
    - "Планировщик не выбирает к отправке расписания объявлений в состоянии черновика (D-01)"
    - "Пропуск черновика сдвигает `next_run_at` вперёд, а не оставляет его в прошлом — публикация черновика не вызывает залпа пропущенных отправок"
    - "`/dashboard`, `/api/ads`, `/schedules/new` и `/schedules/{id}/edit` продолжают отвечать 200 после снятия старого флага активности — ни один из восьми читателей не забыт"
    - "Ревизия `0013` применяется и откатывается: `upgrade` и `downgrade` проверены автоматическим тестом, потому что суита строит схему через `Base.metadata.create_all` и миграции не применяет"
  artifacts:
    - path: "alembic/versions/0013_ad_status.py"
      provides: "Единственное изменение схемы фазы: добавление состояния объявления, снятие старого флага"
      contains: "down_revision = \"0012\""
    - path: "app/constants.py"
      provides: "Единственный источник допустимых значений состояния объявления"
      contains: "AD_STATUS_DRAFT"
    - path: "tests/test_application/test_collect_due_draft.py"
      provides: "Регрессия D-01 — самая чувствительная правка фазы, рядом с боевой диспетчеризацией"
      contains: "collect_due_schedules"
    - path: "tests/test_migrations/test_0013_ad_status.py"
      provides: "Единственная проверка ревизии 0013: round-trip upgrade/downgrade"
      contains: "downgrade"
  key_links:
    - from: "app/application/scheduling/use_cases.py"
      to: "app/models/ad.py"
      via: "ветка пропуска в collect_due_schedules читает состояние объявления и сдвигает next_run_at"
      pattern: "AD_STATUS_DRAFT"
    - from: "app/templates/ads/includes/ad_card.html"
      to: "app/constants.py"
      via: "ячейка состояния сопоставляет значение с константой, неизвестное значение падает в «Черновик»"
      pattern: "AD_STATUS_PUBLISHED"
  prohibitions:
    - statement: "Публикация черновика MUST NOT вызывать залп ранее пропущенных отправок: в момент перехода в опубликованное состояние расписание не должно иметь `next_run_at` в прошлом. Рассылка задним числом уходит в чужие Telegram/WhatsApp/MAX-группы от имени аккаунта пользователя — это спам, за который отвечает владелец аккаунта, и его нельзя отозвать."
      status: resolved
      verification: test
---

<objective>
**Сквозной срез фазы (tracer).** Провести состояние объявления через каждый слой, который эта фаза трогает, одним тонким путём: схема БД → модель → доменный подбор расписаний к отправке → страничный слой → публичный JSON-API → шаблон списка.

Это первый план, после которого пользователь получает новую наблюдаемую способность: объявление в состоянии черновика видно в списке как черновик и **не уходит в группы**. Всё последующее — редактор, вложения, предпросмотр, расписания в редакторе, сводный список — надстраивается над этим срезом.

Срез выбран не произвольно. `Ad.is_active` читается **в восьми местах**, два из которых `02-CONTEXT.md` не называет: `app/pages/dashboard.py:33` (счётчик объявлений на дашборде) и публичный контракт `/api/ads` (`AdResponse.is_active`, `UpdateAdRequest.is_active`). Снятие колонки без правки схем Pydantic превращает каждый вызов `/api/ads` в 500. Поэтому миграция, модель и все восемь читателей обязаны лечь **одним коммитом**, и этот коммит и есть трассирующая пуля.

Purpose: доказать архитектуру состояния объявления end-to-end до того, как поверх неё будут написаны четыре экрана — и закрыть D-01, единственное вторжение фазы в боевой пайплайн отправки.
Output: работающее состояние объявления во всех слоях, ревизия `0013`, пропуск черновиков планировщиком.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-CONTEXT.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-PATTERNS.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-UI-SPEC.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-02-SUMMARY.md
</context>

<ordering_contract>
**Почему трассирующая пуля не первый план фазы.** Порядок задан двумя запертыми решениями пользователя, а не предпочтением планировщика: D-21 требует починить шаблонный глобал «до переделки редактора, отдельной задачей», D-19 — закрыть CR-01/CR-02/WR-01 «отдельным первым планом фазы». Сверх того, `<verify>` трассирующей пули не имел бы силы на красной базовой линии (25 failed + 3 errors, `02-RESEARCH.md` §Pitfall 0). Планы 02-01 и 02-02 — это Wave 0 в терминах Nyquist: они делают результат проверяемым. Трассирующая пуля — первая **содержательная** задача фазы.

**Почему миграция идёт вместе с правкой `app/pages/schedules.py:131,240`, а не после сноса старых страниц.** `02-RESEARCH.md` §Pitfall 3: обе страницы создания и редактирования расписания фильтруют объявления по старому флагу. Если ревизия `0013` выкатится, пока страницы живы, обе упадут ошибкой SQL — и критерий 3 фазы («ни в один момент выката пользователь не остаётся без возможности создать расписание») нарушится **миграцией**, а не удалением. Вариант (B) — сначала снести страницы — конфликтует с D-16, потому что снос обязан идти после работающего редактора, а редактору нужно состояние объявления. Остаётся вариант (A), исполняемый здесь: страницы переводятся на новое состояние тем же коммитом и живут до плана 02-06.

Страховочная сетка `tests/test_pages/test_schedule_creation_path_exists.py` (создана в 02-01) обязана остаться зелёной после этого коммита.
</ordering_contract>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Подтвердить момент прохождения необратимой двери: ревизия `0013` снимает колонку старого флага активности объявлений, и `downgrade` не восстанавливает значения.</decision>
  <context>
**Что уже решено и не переспрашивается.** D-02 в `02-CONTEXT.md` — запертое решение пользователя: состояние объявления вводится, старый флаг удаляется, существующие записи мигрируют в опубликованное состояние. Пользователь сам присвоил этому решению оценку необратимости. Этот чекпойнт **не переоткрывает D-02**.

**Что подтверждается.** Операционная готовность к необратимому шагу. `upgrade` снимает колонку с боевыми данными; `downgrade` вернёт колонку, но не значения — какое объявление было черновиком, восстановить неоткуда, все строки вернутся активными. Миграции применяются к настоящей базе (`just upgrade`), а суита строит схему из модели и о ревизиях не знает, поэтому автоматической страховки у этого шага нет.

**Цена ошибки.** Потеря сведений о черновиках у всех существующих объявлений. На момент выката черновиков в базе быть не должно — поле состояния только вводится, — но это утверждение стоит подтвердить, а не предположить.
  </context>
  <options>
    <option id="proceed-with-backup">
      <name>Снять дамп таблицы объявлений и выполнить миграцию</name>
      <pros>Необратимость перестаёт быть необратимой на практике: значения восстановимы из дампа. Дёшево — одна таблица.</pros>
      <cons>Один дополнительный шаг перед выкатом.</cons>
    </option>
    <option id="proceed-without-backup">
      <name>Выполнить миграцию без дампа</name>
      <pros>Быстрее. Оправдано, если база разработческая или объявлений нет.</pros>
      <cons>Откат теряет сведения о черновиках безвозвратно.</cons>
    </option>
    <option id="defer">
      <name>Отложить выкат миграции, выполнив только авторство ревизии</name>
      <pros>Код готов, применение к базе откладывается до удобного момента.</pros>
      <cons>Задача 3 не может завершиться: приложение в среде разработки не увидит новую колонку, и планы 02-04…02-07 останутся непроверяемыми на живой базе.</cons>
    </option>
  </options>
  <resume-signal>Выбрать: proceed-with-backup, proceed-without-backup или defer</resume-signal>
</task>

<task type="tracer">
  <name>Задача 2: Состояние объявления сквозным срезом — схема, модель, восемь читателей, планировщик, бейдж (один коммит)</name>
  <reversibility rating="one-way">Ревизия `0013` снимает колонку старого флага активности: откат восстанавливает колонку, но не значения — какое объявление было черновиком, после `downgrade` восстановить неоткуда. Решение принято пользователем в D-02 с этой же оценкой, поэтому checkpoint не вставляется — дверь уже пройдена осознанно; оценка сохраняется для протокола.</reversibility>
  <files>app/constants.py, app/models/ad.py, alembic/versions/0013_ad_status.py, app/pages/ads.py, app/pages/dashboard.py, app/pages/schedules.py, app/routes/ads.py, app/application/scheduling/use_cases.py, app/templates/ads/includes/ad_card.html, app/templates/ads/form.html, tests/test_constants.py, tests/test_models/test_ad.py, tests/test_application/test_collect_due_draft.py, tests/test_pages/test_ads_status.py</files>
  <read_first>
    - `app/models/ad.py` целиком (22 строки) — поля модели; флаг активности на строке 19, `Boolean` станет неиспользуемым импортом
    - `app/constants.py` целиком (16 строк) — стиль объявления констант проекта (`TIMEZONE_CHOICES`, `VALID_TIMEZONES`)
    - `app/application/scheduling/use_cases.py` (строки 35-90) — `collect_due_schedules`: запрос на 48-56 с `joinedload(Schedule.ad)`, существующая ветка пропуска на 68-75 со сдвигом `next_run_at`
    - `app/application/scheduling/use_cases.py` (строки 140-170) — `send_message_once` и её каскад «нет объявления / аккаунт не активен → пишем SendLog и выходим»
    - `app/pages/dashboard.py` (строки 25-55) — счётчик объявлений (строка 33)
    - `app/pages/schedules.py` (строки 119-165 и 218-267) — `schedules_new` и `schedules_edit`, фильтры на 131 и 240
    - `app/pages/ads.py` (строки 160-195) — `ads_update`: параметр формы на 169, присваивание на 188
    - `app/routes/ads.py` (строки 14-90) — `CreateAdRequest`, `UpdateAdRequest` (строка 23), `AdResponse` (строка 31), `update_ad` с `repo.update(ad, **data.model_dump(exclude_unset=True))`
    - `app/templates/ads/includes/ad_card.html` (строки 40-50) — ячейка состояния 43-45
    - `app/templates/ads/form.html` (строки 14-40) — комментарий на 16 и тумблер на 35
    - `alembic/versions/0012_schedules_account_id_nullable_set_null.py` целиком — конвенция ревизии проекта
    - `alembic/versions/0003_add_is_blocked_to_users.py` (строки 15-30) — добавление NOT NULL-колонки в заполненную таблицу через `server_default`
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md` §Common Pitfalls 2, 3, 4, 5 и §Code Examples 1, 6
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-UI-SPEC.md` §Status vocabulary и §UI Considerations E15
  </read_first>
  <behavior>
    - Тест: только что созданное объявление получает состояние «опубликовано» — умолчание модели.
    - Тест: `/ads` для объявления-черновика содержит слово «Черновик»; для опубликованного — «Опубликовано».
    - Тест: `/ads` для объявления с нераспознанным значением состояния содержит «Черновик», а не сырое значение и не пустую ячейку.
    - Тест: `collect_due_schedules` не возвращает задачу для расписания объявления-черновика.
    - Тест: тот же вызов **сдвигает** `next_run_at` этого расписания вперёд относительно `now`, а не оставляет его в прошлом.
    - Тест: `collect_due_schedules` продолжает возвращать задачу для расписания опубликованного объявления с активным аккаунтом.
    - Тест: `GET /dashboard` отвечает 200 и считает объявления.
    - Тест: `GET /api/ads` отвечает 200 и элемент ответа содержит поле состояния.
    - Тест: `PATCH`/`PUT /api/ads/{id}` с недопустимым значением состояния отклоняется валидацией, а не записывает произвольную строку.
    - Тест: `GET /schedules/new` и `GET /schedules/{id}/edit` отвечают 200 — старый путь создания расписания жив.
  </behavior>
  <action>
Один коммит. Разделение этой работы на несколько коммитов оставляет промежуточное состояние, в котором `/api/ads` отвечает 500, а `/dashboard` и обе страницы расписаний падают ошибкой SQL.

<!-- planner-discipline-allow: is_active -->
<!-- planner-discipline-allow: Ad.is_active -->
<!-- planner-discipline-allow: ad.is_active -->

**1. Константы (`app/constants.py`).** Объявить `AD_STATUS_DRAFT = "draft"`, `AD_STATUS_PUBLISHED = "published"` и множество `AD_STATUSES`. Значения строковые: в проекте все статусы строковые (`MessengerAccount.status` сравнивается со строкой `"active"` прямо в доменном коде, `app/application/scheduling/use_cases.py:68`), а `sa.Enum` на PostgreSQL создал бы именованный тип БД, который `downgrade` обязан удалять отдельным шагом — прецедента в проекте нет. Стиль объявления — как у `TIMEZONE_CHOICES`/`VALID_TIMEZONES` в том же файле.

**2. Модель (`app/models/ad.py`).** Добавить `status: Mapped[str] = mapped_column(String(20), default=AD_STATUS_PUBLISHED, server_default=AD_STATUS_PUBLISHED, index=True)`. Снять булево поле активности (строка 19) и убрать ставший неиспользуемым импорт `Boolean`.

**3. Ревизия `alembic/versions/0013_ad_status.py`.** Форма — по `0012`: докстринг с описанием, пустая строка, затем `Revision ID: 0013` / `Revises: 0012`; идентификаторы — строки с ведущими нулями (`revision = "0013"`, `down_revision = "0012"`). `upgrade` добавляет колонку `status` типа `sa.String(20)` с `server_default` равным опубликованному состоянию и `nullable=False`, создаёт индекс `ix_ads_status` и снимает колонку старого флага. Отдельного `UPDATE` для заполнения не писать: `server_default` уже проставит всем существующим строкам опубликованное состояние — это ровно поведение, которого требует D-02, и ровно шаблон ревизии `0003`. `downgrade` возвращает булеву колонку с `server_default=sa.true()`, снимает индекс и колонку состояния; над ним обязателен комментарий в принятом стиле проекта (`0012:51-55`): «ВНИМАНИЕ: откат необратимо теряет данные» — какое объявление было черновиком, восстановить неоткуда, все строки вернутся активными. Если снятие колонки окажется неподдерживаемым на каком-то диалекте, применить `op.batch_alter_table` под условием `bind.dialect.name`, как это сделано в `0012:35-48`; на PostgreSQL (прод по `alembic.ini`) обычных `add_column`/`drop_column` достаточно.

**4. Восемь читателей — все в этом же коммите.** Полный список получен grep-ом по `app/` и `tests/` (`02-RESEARCH.md` §Pitfall 2):
- `app/models/ad.py:19` — снимается (пункт 2);
- `app/pages/dashboard.py:33` — счётчик объявлений; условие переводится на опубликованное состояние. Этого читателя нет ни в одном списке `02-CONTEXT.md`; без правки падает `/dashboard`;
- `app/pages/schedules.py:131` (`schedules_new`) и `:240` (`schedules_edit`) — фильтр выбора объявлений переводится на опубликованное состояние. Страницы **остаются живыми**: их сносит план 02-06, а не этот;
- `app/pages/ads.py:169` — параметр формы удаляется (D-04: в макете только «Сохранить» и «Отмена», отдельной кнопки публикации нет); `app/pages/ads.py:188` — присваивание заменяется записью состояния. Полная переработка обработчиков — задача плана 02-04; здесь правка минимальная, ровно чтобы обработчик работал с новым полем;
- `app/routes/ads.py:23` (`UpdateAdRequest`) и `:31` (`AdResponse`) — поле переименовывается в состояние. Тип входного поля — `Literal["draft", "published"] | None`, иначе через API записывается произвольная строка и объявление не отфильтруется ни как черновик, ни как опубликованное. `update_ad` присваивает поля напрямую из `model_dump(exclude_unset=True)`, поэтому валидация обязана стоять на схеме;
- `app/templates/ads/includes/ad_card.html:43-45` — ячейка состояния: `badge('Черновик', 'warning')` для черновика, `badge('Опубликовано', 'success')` для опубликованного, **нераспознанное значение падает в «Черновик»** (UI-SPEC E15 `error`: безопасный дефолт, потому что черновик не отправляется). Макросы `badge` и `cell` переиспользуются без изменений;
- `app/templates/ads/form.html:35` — тумблер удаляется (D-04), комментарий на строке 16, перечисляющий поля формы, приводится в соответствие.

После правки `grep -rn 'is_active' app/ tests/` должен показывать только `Group.is_active`, `Schedule.is_active` и `Subscription.is_active`.

**5. Пропуск черновиков планировщиком (D-01) — `app/application/scheduling/use_cases.py`.** `02-CONTEXT.md` D-01 называет `app/worker/tasks.py`; это неверно. `tasks.py:146` только **вызывает** `collect_due_schedules`, а сам запрос живёт в `app/application/scheduling/use_cases.py:48-56`.

Правка идёт в **существующую ветку пропуска** (строки 68-75), а не в `WHERE` запроса. Ветка дополняется условием «объявление в состоянии черновика» к уже имеющимся «нет объявления» и «аккаунт не активен». Выбор ветки вместо `WHERE` — не стилистический: фильтр в `WHERE` оставил бы `next_run_at` в прошлом, и в момент публикации черновика расписание немедленно выстрелило бы всеми пропущенными слотами — тихая рассылка задним числом в чужие группы. Ветка же пересчитывает `next_run_at` через `compute_next_run_at` и продолжает цикл. `schedule.ad` доступен без дополнительного запроса — он уже загружен `joinedload` на строке 50. Записать это решение комментарием «почему» со ссылкой на D-01.

Продублировать проверку состояния в `send_message_once` (строки 143-166), где уже есть каскад «нет объявления / аккаунт не активен → пишем SendLog и выходим»: задача может долететь до воркера уже после того, как объявление вернули в черновик. Это защита в глубину, а не замена основной ветке.

Протоколы отправки Telegram/WhatsApp/MAX (`app/messengers/*`) не трогаются — жёсткая рамка milestone.

**6. Тесты.** Создать `tests/test_application/test_collect_due_draft.py` (D-01, включая утверждение о сдвиге `next_run_at` — это и есть проверка прохибиции о залпе) и `tests/test_pages/test_ads_status.py` (бейдж, дефолт нераспознанного значения, живость `/dashboard`, `/api/ads`, `/schedules/new`, `/schedules/{id}/edit`). Переписать `tests/test_models/test_ad.py` (строки 33 и 58 утверждают старый флаг) и дополнить `tests/test_constants.py`. Форма тестов — по `tests/test_pages/test_responsive_markup.py:260-278`: посев через `db_session`, запрос через `authed_client`, утверждения на реальных строках.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application tests/test_pages/test_ads_status.py tests/test_models/test_ad.py tests/test_constants.py tests/test_routes/test_ads.py tests/test_pages/test_schedule_creation_path_exists.py tests/test_worker_tasks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/constants.py` содержит `AD_STATUS_DRAFT` и `AD_STATUS_PUBLISHED`.
    - `grep -c 'is_active' app/models/ad.py` возвращает `0`; `grep -c 'status' app/models/ad.py` возвращает значение не меньше `1`.
    - `grep -rn 'Ad\.is_active' app/` не возвращает ни одной строки.
    - `grep -rn 'ad\.is_active' app/templates/` не возвращает ни одной строки.
    - `alembic/versions/0013_ad_status.py` содержит `down_revision = "0012"` и `revision = "0013"`.
    - `alembic/versions/0013_ad_status.py` содержит комментарий со строкой `ВНИМАНИЕ: откат необратимо теряет данные`.
    - `app/routes/ads.py` содержит `Literal` для входного значения состояния.
    - `app/application/scheduling/use_cases.py` содержит `AD_STATUS_DRAFT` внутри ветки пропуска, а не в вызове `select(`.
    - `tests/test_application/test_collect_due_draft.py` содержит утверждение, что `next_run_at` расписания черновика после вызова строго больше переданного `now`.
    - Команда из `<verify>` завершается с кодом 0.
    - Полный прогон `uv run pytest tests/ -q` завершается с кодом 0.
  </acceptance_criteria>
  <done>Состояние объявления работает во всех шести слоях одним коммитом: черновик виден в списке бейджем, не выбирается планировщиком, его `next_run_at` сдвигается вперёд; `/dashboard`, `/api/ads` и обе старые страницы расписаний отвечают 200.</done>
</task>

<task type="auto">
  <name>Задача 3: Применить ревизию `0013` и закрепить её round-trip тестом</name>
  <precondition>`DATABASE_URL` указывает на достижимую базу, и `uv run alembic current` отвечает без ошибки соединения</precondition>
  <files>alembic/versions/0013_ad_status.py, tests/test_migrations/__init__.py, tests/test_migrations/test_0013_ad_status.py</files>
  <read_first>
    - `alembic/versions/0013_ad_status.py` — созданная в Задаче 2 ревизия
    - `alembic/env.py` — как ревизии получают соединение и метаданные
    - `alembic.ini` — целевой диалект
    - `tests/conftest.py` (строки 28-42) — фикстура `db_session`: схема строится `Base.metadata.create_all`, миграции не применяются
    - `justfile` (строки 31-37) — рецепты `migrate` и `upgrade`
  </read_first>
  <action>
**Часть A — применение.** Выполнить `just upgrade` (`uv run alembic upgrade head`) против настроенной базы. Это единственный способ довести новую колонку до работающего приложения: суита строит схему из модели и о существовании ревизий не знает. Шаг выполняется **после** авторства ревизии в Задаче 2 и **до** того, как приложение в среде разработки прочитает новую колонку. Если соединение недоступно — остановиться на непройденном предусловии, а не пропускать шаг молча.

**Часть B — единственная автоматическая проверка ревизии.** Создать `tests/test_migrations/test_0013_ad_status.py`. Аналога в проекте нет: ни один тест в `tests/` не импортирует и не запускает Alembic. Тест прогоняет `alembic upgrade head` и затем `alembic downgrade -1` против **временной файловой базы SQLite**, создаваемой самим тестом (`tmp_path`), с `sqlalchemy.url`, переопределённым программно через `alembic.config.Config`. Утверждения:
- после `upgrade` таблица объявлений имеет колонку состояния, и она `NOT NULL`;
- строка, вставленная **до** применения этой ревизии, после `upgrade` имеет опубликованное состояние — это и есть проверка D-02 «существующие записи мигрируют в опубликовано»;
- после `upgrade` колонки старого флага в таблице нет;
- после `downgrade` колонка состояния отсутствует, а колонка старого флага снова есть.

Файловая база, а не `:memory:`: Alembic открывает собственное соединение, и содержимое базы в памяти между соединениями не сохраняется.

Каталог `tests/test_migrations/` создать вместе с `__init__.py`, как в `tests/test_models/`.

Если какой-либо шаг ревизии на SQLite неприменим, тест обязан это **обнаружить**, а не обойти: единственный допустимый способ обхода — тот же условный `bind.dialect.name`, что уже стоит в `0012:35-48`, и тогда тест утверждает поведение для своего диалекта явно.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migrations/test_0013_ad_status.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_migrations/test_0013_ad_status.py` существует и содержит вызовы и `upgrade`, и `downgrade`.
    - Команда из `<verify>` завершается с кодом 0.
    - `uv run alembic current` печатает `0013` (или `0013 (head)`).
    - `uv run alembic heads` печатает ровно одну голову — ветвления ревизий не возникло.
    - Полный прогон `uv run pytest tests/ -q` завершается с кодом 0.
  </acceptance_criteria>
  <done>Ревизия `0013` применена к настроенной базе; её `upgrade` и `downgrade` покрыты автоматическим round-trip тестом, включая заполнение существующих строк опубликованным состоянием.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| клиент API → `PATCH`/`PUT /api/ads/{id}` | Значение состояния приходит из тела JSON и присваивается модели напрямую через `model_dump(exclude_unset=True)` |
| БД → планировщик отправки | `collect_due_schedules` читает состояние объявления и решает, уйдёт ли рассылка в чужие группы |
| ревизия `0013` → данные прода | `downgrade` необратимо теряет информацию о том, какие объявления были черновиками |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | `UpdateAdRequest` — запись произвольной строки в состояние объявления через JSON-API | high | mitigate | Тип входного поля `Literal["draft", "published"] \| None`; рендер дополнительно деградирует безопасно — нераспознанное значение показывается как «Черновик» (UI-SPEC E15) |
| T-02-12 | Denial of Service | Публикация черновика при `next_run_at` в прошлом — залп всех пропущенных слотов в группы | high | mitigate | Пропуск реализован веткой со сдвигом `next_run_at` через `compute_next_run_at`, а не условием в `WHERE`; закреплено тестом `tests/test_application/test_collect_due_draft.py` |
| T-02-13 | Tampering | Задача долетает до воркера после возврата объявления в черновик | medium | mitigate | Дублирующая проверка состояния в `send_message_once` рядом с существующим каскадом «нет объявления / аккаунт не активен» |
| T-02-14 | Denial of Service | Ревизия `0013` выкатывается, пока `app/pages/schedules.py:131,240` ещё читают старый флаг | high | mitigate | Вариант (A) из §Pitfall 3: миграция и правка обоих фильтров одним коммитом; страховочная сетка `test_schedule_creation_path_exists.py` держит SC-3 |
| T-02-15 | Denial of Service | Снятие колонки без правки схем Pydantic — 500 на четырёх маршрутах `/api/ads` | high | mitigate | `AdResponse` и `UpdateAdRequest` правятся тем же коммитом; живость закреплена тестом |
| T-02-16 | Repudiation | `downgrade` ревизии `0013` теряет, какое объявление было черновиком | medium | accept | Принято пользователем в D-02 (оценка `one-way`). Потеря объявлена комментарием в ревизии в принятом стиле проекта; альтернатива — держать мёртвую колонку вечно |
| T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов — `02-RESEARCH.md` §Package Legitimacy Audit пуст |
</threat_model>

<artifacts_this_phase_produces>
Символы, создаваемые **этим планом** (новые — не подлежат проверке на дрейф от источника):

| Вид | Символ / путь |
|-----|---------------|
| Константа | `AD_STATUS_DRAFT`, `AD_STATUS_PUBLISHED`, `AD_STATUSES` — `app/constants.py` |
| Поле модели | `Ad.status` — `app/models/ad.py` |
| Ревизия Alembic | `0013` — `alembic/versions/0013_ad_status.py` (`down_revision = "0012"`) |
| Индекс БД | `ix_ads_status` |
| Файл теста | `tests/test_application/test_collect_due_draft.py` |
| Файл теста | `tests/test_pages/test_ads_status.py` |
| Каталог и файл теста | `tests/test_migrations/__init__.py`, `tests/test_migrations/test_0013_ad_status.py` |

Удаляемые символы: поле активности модели `Ad` и одноимённая колонка таблицы `ads`, поле того же имени в `AdResponse` и `UpdateAdRequest`.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **ADS-04 (edge-проба, строка 1).** Вернулась `unclassified` → `unresolved`. Краевые случаи состояния объявления выведены из UI-SPEC E15 и D-01/D-02, а не из пробы. Полный роллап семи строк — в `02-01-PLAN.md` §flagged_assumptions.
- **A6 (`02-RESEARCH.md` §Assumptions Log).** Литералы значений состояния — `"draft"` и `"published"`. Источника правды в репозитории нет: поле не существовало. Любые другие литералы были бы одинаково валидны; выбор фиксируется один раз в `app/constants.py` и оттуда читается моделью, доменом, схемами API и шаблоном.
- **A8.** Список из восьми читателей старого флага получен grep-ом по `app/` и `tests/` и подтверждён в этой сессии планирования. Динамического доступа через `getattr` в проекте не найдено, но grep его и не нашёл бы. Приёмочный критерий с `grep -rn 'Ad\.is_active' app/` ловит статический случай; полная суита ловит остальное.
- **Расписания объявлений-черновиков.** По D-01 они никогда не сработают. Решение показывать их в сводном списке с пометкой (RESEARCH A3, UI-SPEC E13 `partial`) исполняется планом 02-07; здесь фиксируется только доменное поведение — задача не выдаётся.
</flagged_assumptions>

<verification>
- Черновик отличим в списке `/ads` и не выбирается к отправке; `next_run_at` сдвигается вперёд.
- `/dashboard`, `/api/ads`, `/schedules/new`, `/schedules/{id}/edit` отвечают 200 — ни один из восьми читателей не забыт.
- Ревизия `0013` применена и покрыта round-trip тестом; голова ревизий одна.
- Протоколы отправки не изменены: `git diff --stat app/messengers/` пуст.
- Полная суита зелёная; страховочная сетка SC-3 зелёная.
</verification>

<success_criteria>
- [ ] `app/constants.py` — единственный источник значений состояния объявления
- [ ] Ревизия `0013` создана по конвенции проекта, с предупреждением о необратимости отката
- [ ] Все восемь читателей старого флага переведены одним коммитом
- [ ] Пропуск черновика реализован веткой со сдвигом `next_run_at`, а не условием в `WHERE`
- [ ] Бейдж «Черновик»/«Опубликовано»; нераспознанное значение падает в «Черновик»
- [ ] `just upgrade` выполнен, `uv run alembic heads` даёт одну голову
- [ ] `uv run pytest tests/ -q` завершается с кодом 0
</success_criteria>

<output>
Create `.planning/phases/02-obyavleniya-i-raspisaniya/02-03-SUMMARY.md` when done
</output>
