---
phase: 02-obyavleniya-i-raspisaniya
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_config_s3.py
  - tests/test_e2e.py
  - tests/test_main.py
  - tests/test_pages/test_shell.py
  - tests/test_routes/test_groups_bulk.py
  - tests/test_routes/test_sync_groups.py
  - tests/test_routes/test_tg_user_auth.py
  - tests/test_routes/test_uploads.py
  - tests/test_routes/test_wa_sync_status.py
  - tests/test_services/test_messenger_factory.py
  - app/pages/common.py
  - app/main.py
  - tests/test_pages/test_ads_editor.py
  - tests/test_pages/test_schedule_creation_path_exists.py
autonomous: true
requirements: [ADS-04, ADS-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Полный прогон `uv run pytest tests/ -q` завершается без failed и без errors — базовая линия фазы измерима"
    - "Ни один тестовый модуль не наследует значения из `.env` разработчика: `Settings(...)` в тестах строится с `_env_file=None`"
    - "`GET /ads/new` и `GET /ads/{id}/edit` возвращают 200 под `authed_client` и это закреплено тестом (D-21)"
    - "Рендер редактора объявления не зависит от наличия файла `.env` в рабочем каталоге"
    - "В каждом коммите фазы существует хотя бы один рабочий путь создания расписания (страховочная сетка SC-3)"
  artifacts:
    - path: "tests/test_pages/test_ads_editor.py"
      provides: "Первый рендер-тест редактора объявления (D-21) — точка опоры ADS-04 и ADS-06"
      contains: "/ads/new"
    - path: "tests/test_pages/test_schedule_creation_path_exists.py"
      provides: "Страховочная сетка SC-3: путь создания расписания жив в любом коммите фазы"
      contains: "def test_"
    - path: "app/pages/common.py"
      provides: "Шаблонные глобалы изображений, привязанные к настройкам приложения, без вызова get_settings()"
      contains: "def bind_image_url_globals"
  key_links:
    - from: "app/main.py"
      to: "app/pages/common.py"
      via: "create_app(settings) вызывает bind_image_url_globals(settings)"
      pattern: "bind_image_url_globals"
    - from: "tests/conftest.py"
      to: "app/config.py"
      via: "Settings(_env_file=None, ...) — .env разработчика не протекает в тесты"
      pattern: "_env_file=None"
---

<objective>
Привести кодовую базу в состояние, в котором результаты тестов фазы 2 вообще что-то значат.

Исследование прогнало суиту на базовом коммите: **25 failed + 3 errors из 652** (`02-RESEARCH.md` §Common Pitfalls 0). Причина одна для всех 28: `app/config.py:78` объявляет `model_config = {"env_file": ".env", ...}`, и двенадцать тестовых модулей строят собственные `Settings(...)`, наследуя реальный `.env` разработчика. Одновременно `app/pages/common.py:36-38` регистрирует три шаблонных глобала, каждый из которых вызывает `get_settings()` в обход подмены зависимостей (D-21) — из-за чего `/ads/new` рендерится **только** при наличии `.env` рядом с процессом.

Это две стороны одного дефекта, и они взаимно исключают друг друга: в конфигурации, где редактор рендерится, суита красная; в конфигурации, где суита зелёная, редактор падает. Ни одна из пяти целей фазы не проверяема, пока обе половины не закрыты.

Purpose: без этого плана фаза не сможет отличить собственную регрессию от чужой, а D-21 прямо требует чинить глобал **до** переделки редактора, «отдельной задачей, вместе с первым рендер-тестом редактора».
Output: зелёная суита, рендер-тест редактора, страховочная сетка SC-3.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-CONTEXT.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-PATTERNS.md
</context>

<ordering_contract>
**Порядок выката фазы записан явно (SC-3, D-16, Pitfall 3).** Планы фазы исполняются строго последовательно; этот план — первый.

| План | Волна | Что делает | Почему именно здесь |
|------|-------|------------|---------------------|
| **02-01** | 1 | Зелёная база + рендер-тест редактора + сетка SC-3 | D-21; без этого `<verify>` любой последующей задачи ничего не доказывает |
| 02-02 | 2 | Долги Фазы 1: CR-01, CR-02, WR-01 | D-19 — фиксы ложатся на стабильный код, до переделки редактора |
| 02-03 | 3 | **Tracer:** `Ad.status` сквозным срезом + миграция `0013` | Вариант (A) из Pitfall 3: миграция и правка `app/pages/schedules.py:131,240` в одном коммите, старые страницы расписаний остаются живыми |
| 02-04 | 4 | Редактор: автосохранение, вложения, предпросмотр | Нужен `status` из 02-03 |
| 02-05 | 5 | Расписания внутри редактора | D-16: сначала новый путь работает |
| 02-06 | 6 | Снос `/schedules/new` и `/schedules/{id}/edit` | D-16: только следующим планом после 02-05 |
| 02-07 | 7 | Сводный список расписаний | Финальный экран SCH-04/SCH-05 |

Страховочная сетка `tests/test_pages/test_schedule_creation_path_exists.py` создаётся **в этом плане** и обязана оставаться зелёной в каждом последующем коммите фазы: именно она превращает «ни в один момент выката пользователь не остаётся без возможности создать расписание» из обещания в проверяемый факт.
</ordering_contract>

<tasks>

<task type="auto">
  <name>Задача 1: Изолировать тестовые настройки от `.env` разработчика</name>
  <files>tests/conftest.py, tests/test_config.py, tests/test_config_s3.py, tests/test_e2e.py, tests/test_main.py, tests/test_pages/test_shell.py, tests/test_routes/test_groups_bulk.py, tests/test_routes/test_sync_groups.py, tests/test_routes/test_tg_user_auth.py, tests/test_routes/test_uploads.py, tests/test_routes/test_wa_sync_status.py, tests/test_services/test_messenger_factory.py</files>
  <read_first>
    - `app/config.py` (строки 1-95) — определение `Settings`, `model_config` на строке 78, `get_settings` с `lru_cache`
    - `tests/conftest.py` (строки 1-30) — фикстура `test_settings`, единственная, что глушит SMTP (`smtp_host=""`)
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md` §Common Pitfalls 0 и §Code Examples 7 — воспроизведение дефекта и проверенное решение
  </read_first>
  <action>
Добавить именованный аргумент `_env_file=None` первым аргументом каждого конструирования `Settings(` во всех двенадцати тестовых модулях, которые его вызывают: `tests/conftest.py`, `tests/test_config.py`, `tests/test_config_s3.py`, `tests/test_e2e.py`, `tests/test_main.py`, `tests/test_pages/test_shell.py`, `tests/test_routes/test_groups_bulk.py`, `tests/test_routes/test_sync_groups.py`, `tests/test_routes/test_tg_user_auth.py`, `tests/test_routes/test_uploads.py`, `tests/test_routes/test_wa_sync_status.py`, `tests/test_services/test_messenger_factory.py`.

`_env_file` — штатный параметр `pydantic-settings`, отключающий чтение файла окружения для конкретного экземпляра. Он выбран вместо правки `app/config.py` намеренно: продовое поведение (`model_config` на `app/config.py:78`) остаётся нетронутым, меняется только тестовое окружение.

Исключение: `tests/test_config.py` может содержать тест, который проверяет само чтение файла окружения. Такой тест — единственное место, где параметр не добавляется; если он есть, снабдить его комментарием «why», называющим причину исключения.

Продовый код в этой задаче не трогается вообще.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py tests/test_config_s3.py tests/test_e2e.py tests/test_main.py tests/test_routes/test_groups_bulk.py tests/test_routes/test_sync_groups.py tests/test_routes/test_tg_user_auth.py tests/test_routes/test_uploads.py tests/test_routes/test_wa_sync_status.py tests/test_services/test_messenger_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Команда из `<verify>` завершается с кодом 0 (до правки в этих модулях 25 failed + 3 errors).
    - `grep -rn "Settings(" tests/ | grep -v "_env_file"` не возвращает ни одной строки, кроме явно закомментированного исключения в `tests/test_config.py`.
    - `uv run python -c "import sys; sys.path.insert(0,'.'); from app.config import Settings; print(repr(Settings(_env_file=None, database_url='sqlite+aiosqlite:///:memory:', secret_key='x').smtp_host))"` печатает `''`.
    - `git diff --name-only` не содержит ни одного файла вне каталога `tests/`.
  </acceptance_criteria>
  <done>Все двенадцать модулей строят `Settings` без чтения файла окружения; десять ранее красных модулей зелёные; продовый код не изменён.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Снять `get_settings()` с шаблонных глобалов изображений и закрепить рендер редактора тестом (D-21)</name>
  <files>app/pages/common.py, app/main.py, tests/test_pages/test_ads_editor.py, tests/test_pages/test_schedule_creation_path_exists.py, tests/test_pages/test_shell.py</files>
  <read_first>
    - `app/pages/common.py` (строки 20-55) — `_resolve_image_url` на 27-33 и три регистрации глобалов на 36-38, все три вызывают `get_settings()`
    - `app/pages/common.py` (строки 100-120) — `format_datetime_for_user`: образец глобала, который берёт данные запроса параметром
    - `app/main.py` (строки 62-90) — `create_app(settings: Settings | None = None)`, единственная точка, где приложение уже владеет своими настройками
    - `tests/conftest.py` (строки 44-60) — фикстура `client`: `create_app(settings=test_settings)` плюс `dependency_overrides[get_settings]`
    - `app/templates/ads/includes/ad_card.html` (строки 1-30) — комментарий макроса о том, что глобалы окружения макросу доступны, а контекст вызывающего — нет
    - `tests/test_pages/test_shell.py` (строки 80-115) — список страниц шелла и комментарий про 500 на `/ads/new`
    - `tests/test_pages/test_responsive_markup.py` (строки 260-290) — эталон рендер-теста: посев через `db_session`, запрос через `authed_client`, утверждения на реальных строках данных
  </read_first>
  <behavior>
    - Тест: `GET /ads/new` под `authed_client` возвращает 200 и содержит поля названия и текста.
    - Тест: `GET /ads/{id}/edit` под `authed_client` для своего объявления возвращает 200 и содержит его заголовок.
    - Тест: `GET /ads/{id}/edit` для чужого объявления не отдаёт 200 (редирект на список).
    - Тест: страница со ссылкой на изображение рендерится с базовым URL из настроек тестового приложения, а не из окружения процесса.
    - Тест (сетка SC-3): существует хотя бы один рабочий путь создания расписания.
  </behavior>
  <action>
**Часть A — `app/pages/common.py`.** Заменить три регистрации глобалов, выполняющиеся на импорте модуля, на функцию `bind_image_url_globals(settings: Settings) -> None`, которая регистрирует в `templates.env.globals` те же три имени — `get_image_url`, `resolve_image_url`, `s3_public_url` — замкнутыми на переданный `settings.s3_public_url`. Сигнатуры, видимые шаблонам, не меняются: `get_image_url(key)`, `resolve_image_url(key)`, `s3_public_url()`. Логика `_resolve_image_url` (вернуть значение как есть, если оно начинается с `http://` или `https://`, иначе собрать URL через `app.services.s3.get_image_url`) сохраняется дословно.

На импорте модуля зарегистрировать те же три имени с пустым базовым URL как безопасный дефолт. Требование жёсткое: **на импорте `app.pages.common` не должно происходить ни одного конструирования `Settings`**. Это и есть суть D-21 — сегодня импорт модуля падает `ValidationError`, если рядом нет файла окружения.

**Часть B — `app/main.py`.** Внутри `create_app`, сразу после разрешения `settings` (строки 62-65), вызвать `bind_image_url_globals(settings)`. Приложение уже получает настройки явно (`create_app(settings=test_settings)` в `tests/conftest.py:46`), поэтому этого достаточно: подмена зависимости больше не обходится.

**Почему не параметрическая инъекция из PATTERNS.md.** `02-PATTERNS.md` предлагал форму `format_datetime_for_user` — передавать базовый URL параметром через контекст ответа. Она отвергнута: `ad_card` — это макрос (`app/templates/ads/includes/ad_card.html:25`), а импортированным макросам Jinja контекст вызывающего не передаёт. Параметрическая форма потребовала бы менять сигнатуру макроса и всех его вызывающих в `ads/list.html` и `ads/partial_cards.html`, то есть трогать разметку списка ради починки настроек. Привязка на уровне `create_app` решает ровно заявленный дефект и не касается ни одного шаблона. Записать это решение комментарием «why» в `app/pages/common.py`.

**Часть C — тесты.** Создать `tests/test_pages/test_ads_editor.py` по образцу `tests/test_pages/test_responsive_markup.py:260-278`: посев через `db_session`, запрос через `authed_client`, утверждения на реальных строках, а не только на коде ответа. Покрыть поведение из блока `<behavior>`.

Создать `tests/test_pages/test_schedule_creation_path_exists.py` — страховочную сетку SC-3 на всю фазу. Тест утверждает дизъюнкцию: **хотя бы один** путь создания расписания доступен пользователю — либо `GET /schedules/new` отвечает 200, либо HTML `GET /ads/{id}/edit` содержит форму, чей `action` ведёт на маршрут создания расписания. Дизъюнкция обязательна: тест обязан быть зелёным и до переезда (план 02-05), и после сноса старых страниц (план 02-06), иначе он не сетка, а расписание работ. В докстринге назвать SC-3 и планы 02-05/02-06.

Привести в соответствие `tests/test_pages/test_shell.py`: комментарий на строках 105-112 утверждает, что `/ads/new` в тестовой среде отдаёт 500. После этой задачи это неверно — переписать комментарий и, если страница исключена из списка проверяемых, вернуть её туда. Строку 85 со ссылкой на страницу создания расписания **не трогать** — она снимается планом 02-06.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_editor.py tests/test_pages/test_schedule_creation_path_exists.py tests/test_pages/test_shell.py tests/test_pages/test_responsive_markup.py tests/test_templates -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/common.py` содержит `def bind_image_url_globals`.
    - `grep -n "get_settings()" app/pages/common.py` не возвращает ни одной строки внутри области регистрации шаблонных глобалов (строки 20-45 исходного файла).
    - `app/main.py` содержит вызов `bind_image_url_globals(settings)` внутри `create_app`.
    - `env -u DATABASE_URL -u SECRET_KEY uv run python -c "import app.pages.common"` завершается с кодом 0, будучи запущенным из каталога без файла `.env` (проверять во временном каталоге, не удаляя `.env` разработчика).
    - `tests/test_pages/test_ads_editor.py` существует и содержит утверждение на `/ads/new`.
    - `tests/test_pages/test_schedule_creation_path_exists.py` существует и его докстринг называет SC-3.
    - Команда из `<verify>` завершается с кодом 0.
    - Полный прогон `uv run pytest tests/ -q` завершается с кодом 0 — базовая линия фазы зафиксирована.
  </acceptance_criteria>
  <done>Шаблонные глобалы изображений привязаны к настройкам приложения и не конструируют `Settings` на импорте; `/ads/new` и `/ads/{id}/edit` покрыты рендер-тестом; страховочная сетка SC-3 существует; полная суита зелёная.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| окружение процесса → конфигурация приложения | Файл `.env` рабочего каталога попадает в `Settings` при **каждом** конструировании (`app/config.py:78`) |
| тестовый процесс → боевые учётные данные | Тестовые фикстуры сегодня наследуют реальные SMTP-параметры разработчика |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `Settings` в тестовых фикстурах (12 модулей) | medium | mitigate | `_env_file=None` в каждом конструировании: суита перестаёт запускаться с боевыми SMTP-параметрами и боевым `s3_public_url` разработчика (Задача 1) |
| T-02-02 | Denial of Service | `app/pages/common.py:36-38` — импорт модуля конструирует `Settings` | medium | mitigate | Привязка глобалов через `create_app`; импорт не падает `ValidationError` при отсутствии файла окружения (Задача 2) |
| T-02-03 | Tampering | Отсутствие тестового покрытия `/ads/new` и `/ads/{id}/edit` | high | mitigate | `tests/test_pages/test_ads_editor.py` — экран, на котором строится вся фаза, перестаёт быть непокрытым (Задача 2). Без этого правки планов 02-04/02-05 нечем удержать |
| T-02-04 | Elevation of Privilege | `GET /ads/{id}/edit` для чужого объявления | high | mitigate | Тест владения в `tests/test_pages/test_ads_editor.py` закрепляет существующую проверку `Ad.user_id == user.id` (`app/pages/ads.py:112-114`) как регрессионный контракт |
| T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | Фаза не устанавливает ни одного пакета — `02-RESEARCH.md` §Package Legitimacy Audit: таблица пуста, `[SLOP]`/`[SUS]` отсутствуют. Гейт легитимности неприменим |
</threat_model>

<artifacts_this_phase_produces>
Символы, создаваемые **этим планом** (новые — не подлежат проверке на дрейф от источника):

| Вид | Символ / путь |
|-----|---------------|
| Функция | `bind_image_url_globals(settings)` — `app/pages/common.py` |
| Файл теста | `tests/test_pages/test_ads_editor.py` |
| Файл теста | `tests/test_pages/test_schedule_creation_path_exists.py` |

Символы, создаваемые фазой целиком (полный перечень — в планах-владельцах): `app/constants.py: AD_STATUS_DRAFT`, `AD_STATUS_PUBLISHED`, `AD_STATUSES`; `Ad.status`; ревизия Alembic `0013`; `own_image_keys()`; `sniff_image()`; `app/templates/ads/includes/preview.html`, `summary.html`, `autosave.html`, `sched_card.html`; CSS-раздел 8 в `app/static/css/app.css`; маршрут-параметр `?sched={id}` у `/ads/{id}/edit`.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Пробы, не разрешённые автоматически — явные допущения фазы

**Edge-проба (спекless-fallback, §A).** Фаза не имеет обычного `-SPEC.md`, поэтому детерминированная edge-проба прогонялась по семи требованиям фазы. **Все 7 строк вернулись `unclassified` → `unresolved`.** Классификатор пробы работает по англоязычному лексикону, а тексты требований русские, — то есть `unclassified` здесь означает «классификатор не сработал», а не «фаза без краевых случаев». По §A такие строки **никогда** не закрываются backstop-ом автоматически и выносятся в план как явные допущения. Ниже — полный список из семи; равенство §C соблюдено: 7 строк пробы == 7 вынесенных допущений (и ноль молчаливых потерь).

| # | Требование | Строка пробы | Допущение планировщика | План-владелец |
|---|-----------|--------------|------------------------|---------------|
| 1 | ADS-04 | `unclassified` / `unresolved` | Краевые случаи черновика и автосохранения выведены не из пробы, а из UI-SPEC E1/E2 и D-03…D-06: пустой `/ads/new` без записи, потеря связи, гонка быстрых нажатий | 02-04 (+ 02-03 статус) |
| 2 | ADS-05 | `unclassified` / `unresolved` | Краевые случаи вложений выведены из UI-SPEC E3 и D-12/D-13: 0/1/10 вложений, частично неудавшаяся мультизагрузка, чужой ключ | 02-02, 02-04 |
| 3 | ADS-06 | `unclassified` / `unresolved` | Краевые случаи предпросмотра выведены из UI-SPEC E9: пустой предпросмотр, текст без медиа, медиа без текста, 4000 символов, 2…10 вложений (последнее — открытое допущение, см. 02-04) | 02-04 |
| 4 | ADS-07 | `unclassified` / `unresolved` | Краевые случаи расписания выведены из UI-SPEC E4…E8 и D-07/D-08: нет аккаунтов, аккаунт без групп, неполное расписание, смена аккаунта | 02-05 |
| 5 | ADS-08 | `unclassified` / `unresolved` | Краевые случаи удаления выведены из UI-SPEC E12 и правила деградации без Alpine: удаление незаполненного расписания, двойной клик по подтверждению | 02-05 |
| 6 | SCH-04 | `unclassified` / `unresolved` | Краевые случаи сводного списка выведены из UI-SPEC E13/E14: два разных пустых состояния, расписание объявления-черновика, отвязанный аккаунт | 02-07 |
| 7 | SCH-05 | `unclassified` / `unresolved` | Краевые случаи тумблера выведены из UI-SPEC E13 `error` и issue #35: отказ сервера, `resume_blocked` у расписания без аккаунта | 02-06, 02-07 |

`must_haves.truths` фазы выведены не из этой пробы, а из пяти критериев успеха ROADMAP и раздела `## UI Considerations` в `02-UI-SPEC.md` — оба источника конкретны и проверяемы.

**Прохибиции (§B, две стадии).** Стадия 1 дала ~12 сырых кандидатов, стадия 2 (точность) отбросила рутинную инженерию и уже специфицированное. Канон-отсылки, снятые по правилу ADR-550 D6 (breadcrumb, не минтим):
- IDOR по `ad_id`/`account_id` — канон, покрывается `/gsd-secure-phase` и блоком `<threat_model>` плана 02-02; не минтится.
- Загрузка SVG под видом изображения (XSS на origin хранилища) — канон OWASP, покрывается `/gsd-secure-phase` и `<threat_model>` плана 02-02; не минтится.

Выжившие три прохибиции авторизованы **без дескриптора проверки** (без `check_*`) в блоках `must_haves.prohibitions` планов-владельцев: 02-03 (залп пропущенных отправок), 02-04 (счётчик длины и целостность истории отправок).
</flagged_assumptions>

<verification>
- Полная суита `uv run pytest tests/ -q` зелёная (0 failed, 0 errors). Это новая базовая линия фазы; до этого плана база — 25 failed / 3 errors.
- `/ads/new` и `/ads/{id}/edit` покрыты тестом и не зависят от наличия `.env`.
- Страховочная сетка SC-3 существует и зелёная.
- Продовое поведение `app/config.py` не изменено.
</verification>

<success_criteria>
- [ ] Двенадцать тестовых модулей строят `Settings` с `_env_file=None`
- [ ] `app/pages/common.py` не конструирует `Settings` на импорте
- [ ] `bind_image_url_globals` вызывается из `create_app`
- [ ] `tests/test_pages/test_ads_editor.py` создан и зелёный
- [ ] `tests/test_pages/test_schedule_creation_path_exists.py` создан и зелёный
- [ ] `uv run pytest tests/ -q` завершается с кодом 0
</success_criteria>

<output>
Create `.planning/phases/02-obyavleniya-i-raspisaniya/02-01-SUMMARY.md` when done
</output>
