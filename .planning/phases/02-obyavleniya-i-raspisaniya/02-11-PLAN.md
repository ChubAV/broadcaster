---
phase: 02-obyavleniya-i-raspisaniya
plan: 11
type: execute
wave: 11
depends_on: ["02-08", "02-09", "02-10"]
files_modified:
  - tests/test_pages/test_attachment_history_integrity.py
  - tests/test_pages/test_schedules_list.py
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [ADS-04, ADS-05, ADS-06, ADS-07, ADS-08, SCH-04, SCH-05]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Каждый требовательный идентификатор фазы прослеживается в `.planning/REQUIREMENTS.md` согласованно с состоянием кода: отметка ставится по факту работающего поведения, а не по факту исполненного плана"
    - "Удаление вложения не разрушает историю отправленного: снапшот `SendLog` остаётся целым, и просмотр прошлой отправки не превращается в битую картинку — утверждение закреплено ИМЕНОВАННЫМ тестом, а не структурным доводом"
    - "Два расписания одного объявления с одинаковыми днями и одинаковыми временами рендерятся двумя отдельными карточками и не схлопываются в одну; счётчик найденных считает обе (SCH-04, `adjacency`)"
    - "Ноль расписаний и ноль совпадений дают ДВА РАЗНЫХ пустых состояния с разным текстом, и полоса фильтров рендерится в обоих (SCH-04, `empty`)"
    - "Порядок карточек сводного списка задан явной сортировкой по идентификатору расписания и стабилен между перезагрузками: расписания с равными значениями фильтрации не меняются местами, и порция бесконечной прокрутки не дублирует и не пропускает карточку (SCH-04, `ordering`)"
    - "Переключение расписания не меняет позицию его карточки в списке: после возврата порядок карточек тот же, что до переключения (SCH-05, `ordering`)"
  artifacts:
    - path: "tests/test_pages/test_attachment_history_integrity.py"
      provides: "Именованная регрессия запрета: снапшот отправленного переживает удаление вложения"
      contains: "SendLog"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Таблица прослеживаемости, согласованная с состоянием кода"
      contains: "ADS-07"
  key_links:
    - from: "tests/test_pages/test_attachment_history_integrity.py"
      to: "app/models/send_log.py"
      via: "снапшот вложений отправки — независимая колонка, правка списка ключей объявления её не касается"
      pattern: "ad_images"
  prohibitions:
    - statement: "Удаление вложения из объявления MUST NOT разрушать историю фактически отправленного: снапшоты `SendLog` остаются целыми, объект в хранилище не удаляется, и просмотр прошлой отправки не превращается в битую картинку."
      status: open
      verification: test
---

<objective>
Привести прослеживаемость требований в соответствие с состоянием кода и закрыть единственный
запрет фазы, помеченный верификатором как «выполнен структурно, но не удержан именованным тестом».

Прослеживаемость рассинхронизирована в ОБЕ стороны: ADS-07 и ADS-08 реализованы и закрыты зелёными
тестами (полный CRUD расписания из редактора, 92 теста профильных модулей), но в
`.planning/REQUIREMENTS.md` стоят как `Pending` и без отметки в списке; ADS-04 помечено `Complete`,
хотя механизм автосохранения был дефектен. Отметка должна ставиться по факту работающего поведения.

Запрет 02-04 «удаление вложения MUST NOT разрушать историю отправленного» помечен как
`⚠ FLAGGED — unverified enforcement`: структурно он выполнен (`SendLog.ad_images` — независимая
колонка снапшота, вызова удаления объекта в хранилище на пути убирания вложения нет вовсе), но
именованного теста, утверждающего целость снапшота после удаления вложения, в суите нет — ни одной
ссылки на `SendLog` в регрессии редактора. Fail-closed: помечено, не зелёное.

Purpose: снять последние два незакрытых пункта отчёта верификации, не относящиеся к человеческой
проверке.
Output: именованная регрессия целости снапшота; три недостающих утверждения о сводном списке;
согласованная таблица прослеживаемости.

**Почему план стоит волной 11, а не 10.** Он ставит отметку `Complete` требованию ADS-05, но две
его обязательные истины — граница `max_images_per_ad ±1` и точное правило порога счётчика —
доставляются задачей 3 плана 02-10, а не планами 02-08 и 02-09. В одной волне с 02-10 порядок
исполнения не задан, и отметка могла бы встать раньше, чем появится покрытие границы, — то есть в
нарушение собственного принципа этого плана «отметка ставится по факту работающего поведения, а не
по факту исполненного плана». Поэтому 02-10 добавлен в `depends_on`, а план сдвинут на волну 11.
Пересечений по записываемым файлам с 02-10 у него нет — единственная плата за сдвиг —
последовательное исполнение. План 02-12 остаётся на волне 11 рядом: он от 02-11 не зависит, и его
`files_modified` пуст, так что столкновения по записи между ними невозможно.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-VERIFICATION.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: именованная регрессия целости снапшота и три утверждения о сводном списке</name>
  <files>tests/test_pages/test_attachment_history_integrity.py, tests/test_pages/test_schedules_list.py</files>
  <read_first>
    - app/models/send_log.py (строка 21 — колонка снапшота вложений `ad_images`; остальные поля снапшота: текст объявления, имя группы, состояние)
    - app/pages/ads.py (`_save_from_editor` — обработка `remove_image`: ключ убирается ровно один раз, объект в хранилище не трогается)
    - tests/test_pages/test_ads_editor.py (состояние после плана 02-08: хелперы `image_key`, `form_body`, `_seed_ad`, `_user`, константы заголовков — образец для нового файла)
    - tests/test_pages/test_schedules_list.py (действующая регрессия SCH-04/SCH-05: состав карточки, два пустых состояния, склонения — новые утверждения дописываются в этот же файл)
    - app/pages/schedules.py (строки 351-465: `schedules_partial` и `schedules_list` — сортировка `.order_by(Schedule.id)` и разбиение на порции; строки 263-289 — `_summary_query` и `_summary_count_query`)
    - app/templates/schedules/list.html (два пустых состояния и полоса фильтров, рендерящаяся всегда)
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-VERIFICATION.md (раздел Prohibitions, строка про FLAGGED)
  </read_first>
  <behavior>
    - `test_send_log_snapshot_survives_attachment_removal`: посеяно объявление с двумя ключами вложений и строка `SendLog`, чей снапшот `ad_images` содержит оба ключа. Затем один ключ убирается из объявления через POST с полем `remove_image`. Ожидание: `Ad.images` содержит один ключ, а `SendLog.ad_images` — по-прежнему оба, в прежнем порядке; текст снапшота не изменился.
    - `test_removing_an_attachment_does_not_touch_storage`: тот же путь; ожидание — ни одна функция удаления объекта хранилища не вызывается. Утверждение вести подменой соответствующего вызова в `app/services/s3.py` на объект-счётчик и проверкой нулевого числа вызовов, а не грепом по исходнику.
    - `test_two_identical_schedules_render_as_two_cards`: два расписания одного объявления с одинаковыми днями и временами. Ожидание: в ответе `/schedules` два отдельных элемента карточки, счётчик найденных показывает два.
    - `test_two_empty_states_differ`: при нуле расписаний вовсе и при нуле совпадений фильтра тексты пустых состояний РАЗНЫЕ, и полоса фильтров присутствует в обоих ответах.
    - `test_card_order_is_stable_across_reloads_and_pages`: посеяно расписаний больше, чем помещается на одну порцию; порядок карточек первой страницы и порции прокрутки в сумме не содержит дубликатов и не пропускает ни одного расписания; повторный запрос даёт тот же порядок.
    - `test_toggling_does_not_move_the_card`: порядок идентификаторов карточек до и после переключения одного расписания совпадает.
  </behavior>
  <action>
    Создать `tests/test_pages/test_attachment_history_integrity.py` — отдельный файл, потому что
    утверждение принадлежит запрету фазы, а не регрессии редактора, и должно быть находимо по имени.
    Посев вести через `db_session` по образцу `tests/test_pages/test_ads_editor.py`; ключи вложений
    строить в форме `{user_id}/{32 hex}_{имя}`, иначе сохранение отклонится проверкой владения и тест
    померяет отказ, а не целость снапшота.

    Дописать четыре утверждения о сводном списке в `tests/test_pages/test_schedules_list.py`, рядом с
    существующими. Сортировка списка уже задана явно (`app/pages/schedules.py:376` и `:422` —
    `.order_by(Schedule.id)`), поэтому тест на стабильность порядка ЗАКРЕПЛЯЕТ существующее свойство,
    а не требует правки кода. Если прогон покажет обратное — остановиться и вынести находку, а не
    ослаблять утверждение.

    Для теста порций использовать существующий маршрут порции сводного списка с параметрами смещения
    и размера, как это делают действующие тесты бесконечной прокрутки в этом же файле.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_attachment_history_integrity.py tests/test_pages/test_schedules_list.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Файл `tests/test_pages/test_attachment_history_integrity.py` существует и содержит `def test_send_log_snapshot_survives_attachment_removal`
    - Файл содержит `def test_removing_an_attachment_does_not_touch_storage`
    - `tests/test_pages/test_schedules_list.py` содержит `def test_two_identical_schedules_render_as_two_cards`
    - `tests/test_pages/test_schedules_list.py` содержит `def test_two_empty_states_differ`
    - `tests/test_pages/test_schedules_list.py` содержит `def test_card_order_is_stable_across_reloads_and_pages`
    - `tests/test_pages/test_schedules_list.py` содержит `def test_toggling_does_not_move_the_card`
    - Поведение: после убирания одного ключа из объявления список ключей объявления короче на один, а снапшот отправки содержит прежний состав в прежнем порядке
    - Поведение: путь убирания вложения не вызывает ни одной функции удаления объекта хранилища
    - `uv run pytest tests/test_pages/test_attachment_history_integrity.py tests/test_pages/test_schedules_list.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Запрет о целости истории удерживается именованным тестом; три грани сводного списка и стабильность порядка при переключении закреплены.</done>
  <reversibility rating="reversible">Только тестовые файлы.</reversibility>
</task>

<task type="auto">
  <name>Task 2: привести таблицу прослеживаемости в соответствие с состоянием кода</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>Планы 02-08, 02-09 И 02-10 завершены, и их проверки зелёные. Три плана названы поимённо, потому что отметки опираются на все три: ADS-04 — на 02-08 (повторное автосохранение обновляет один черновик); SCH-05 — на 02-09 (единое определение полноты на обоих входах); ADS-05 — на 02-08 (текст и вложение в одном объявлении) И на 02-10, задача 3 (граница `max_images_per_ad ±1` и точное правило порога счётчика). Без 02-10 половина обязательных истин ADS-05 не существует, и отметка встала бы по факту исполненного плана, а не работающего поведения — ровно то, что этот план запрещает. Проверить: `uv run pytest tests/ -q` зелёный И файл `.planning/phases/02-obyavleniya-i-raspisaniya/02-10-SUMMARY.md` существует. Если хоть одно не выполнено — остановиться и не менять отметки.</precondition>
  <read_first>
    - .planning/REQUIREMENTS.md (строки 77-86 — чекбоксы ADS-04…ADS-08, SCH-04, SCH-05; строки 199-247 — таблица прослеживаемости v2.0 и блок покрытия)
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-VERIFICATION.md (раздел Requirements Coverage: ADS-04 BLOCKED, ADS-05 ЧАСТИЧНО, ADS-06 SATISFIED, ADS-07 SATISFIED, ADS-08 SATISFIED, SCH-04 SATISFIED, SCH-05 SATISFIED)
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-08-SUMMARY.md (результат закрытия автосохранения)
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-09-SUMMARY.md (результат закрытия межарендной дыры)
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-10-SUMMARY.md (результат закрытия границы лимита вложений и порога счётчика — вторая половина основания отметки ADS-05)
  </read_first>
  <action>
    Обновить `.planning/REQUIREMENTS.md` точечными правками — целиком файл не переписывать.

    В списке требований раздела «Объявления» и «Расписания» проставить отметку выполнения у ADS-04,
    ADS-05, ADS-06, ADS-07, ADS-08, SCH-04, SCH-05 — при условии, что предыдущая задача этого плана и
    планы 02-08, 02-09, 02-10 завершены зелёными. Сегодня отметки нет ни у одного из семи, при том
    что пять из них верификатор признал выполненными.

    В таблице прослеживаемости v2.0 заменить `Pending` на `Complete (<дата выполнения>)` у ADS-07 и
    ADS-08. У ADS-04 и ADS-05 запись `Complete` уже стоит — она была преждевременной на момент
    верификации; после закрытия дефекта планом 02-08 она становится верной, и её нужно
    ПОДТВЕРДИТЬ ссылкой на закрывающий план, а не оставить необъяснённой. Формат ссылки — тот же, что
    у соседних строк таблицы.

    Дописать под таблицей одну строку, фиксирующую основание отметок: какие планы фазы и какая
    проверка их подтверждают. Основание обязано назвать ВСЕ ТРИ закрывающих плана — 02-08, 02-09 и
    02-10, — потому что ADS-05 опирается на два из них: на 02-08 (текст и вложение попадают в одно
    объявление) и на 02-10 (граница `max_images_per_ad ±1` и точное правило порога счётчика).
    Записать основание только по 02-08 значило бы задокументировать отметку, половины оснований
    которой в документе нет. Блок «Coverage (v2.0)» пересчитать, если числа изменились; счётчики
    требований менять нельзя — ни одно требование не добавляется и не удаляется.

    Строку «Last updated» в конце файла дополнить записью об этом изменении, сохранив предыдущие.
  </action>
  <verify>
    <automated>grep -c "ADS-07 | Phase 2 | Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` содержит строку таблицы, в которой `ADS-07` соседствует с `Complete`
    - `.planning/REQUIREMENTS.md` содержит строку таблицы, в которой `ADS-08` соседствует с `Complete`
    - `grep -c '\[ \] \*\*ADS-0[45678]\*\*' .planning/REQUIREMENTS.md` равно 0 — непроставленных отметок среди требований объявлений фазы не осталось
    - `grep -c '\[ \] \*\*SCH-0[45]\*\*' .planning/REQUIREMENTS.md` равно 0
    - Число строк таблицы прослеживаемости v2.0 не изменилось: 39 требований, как было
    - `.planning/REQUIREMENTS.md` содержит упоминание закрывающих планов `02-08`, `02-09` и `02-10` рядом с основанием отметок
    - Файл `.planning/phases/02-obyavleniya-i-raspisaniya/02-10-SUMMARY.md` существует на момент правки — вторая половина основания ADS-05 доставлена, а не предположена
  </acceptance_criteria>
  <done>Таблица прослеживаемости и список требований согласованы с реальным состоянием кода; основание каждой отметки записано.</done>
  <reversibility rating="reversible">Документ планирования; откат — обратный коммит.</reversibility>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Артефакт | Вид | Файл |
|---|---|---|
| `tests/test_pages/test_attachment_history_integrity.py` | новый тестовый модуль | — |
| `test_send_log_snapshot_survives_attachment_removal` | тест | `tests/test_pages/test_attachment_history_integrity.py` |
| `test_removing_an_attachment_does_not_touch_storage` | тест | `tests/test_pages/test_attachment_history_integrity.py` |
| `test_two_identical_schedules_render_as_two_cards` | тест | `tests/test_pages/test_schedules_list.py` |
| `test_two_empty_states_differ` | тест | `tests/test_pages/test_schedules_list.py` |
| `test_card_order_is_stable_across_reloads_and_pages` | тест | `tests/test_pages/test_schedules_list.py` |
| `test_toggling_does_not_move_the_card` | тест | `tests/test_pages/test_schedules_list.py` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| правка объявления → снапшот отправки | Правка `Ad.images` не должна дотягиваться до независимой колонки снапшота `SendLog` |
| путь убирания вложения → хранилище объектов | Убирание ключа не должно вызывать удаление объекта: снапшоты истории ссылаются на тот же объект (D-12) |

## STRIDE Threat Register

ASVS level 1, блокировать на severity `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02G-17 | Repudiation | история отправленного могла бы измениться задним числом вслед за правкой объявления | medium | mitigate | Именованный тест утверждает, что снапшот `SendLog` не меняется при убирании вложения; структурного довода недостаточно (задача 1) |
| T-02G-18 | Denial of Service | удаление объекта хранилища оставило бы историю с битыми картинками | medium | mitigate | Тест подменяет функцию удаления объекта на счётчик и утверждает ноль вызовов (задача 1) |
| T-02G-19 | Tampering | отметка выполнения требования, не подтверждённая поведением | medium | mitigate | Предусловие задачи 2: отметка ставится только при зелёном полном прогоне и завершённых планах 02-08, 02-09 И 02-10 (последний доставляет границу лимита вложений и правило порога счётчика — половину обязательных истин ADS-05); зависимость закреплена в `depends_on` и волной 11, а не оставлена на удачу параллельного исполнения; основание записывается в документ |
| T-02G-20 | Information Disclosure | межарендная утечка через список расписаний | — | transfer | Уже закрыта планом 02-09 (T-02G-06); в этом плане только закрепляется тестами сводного списка |
| T-02G-SC | Tampering | установка пакетов менеджером пакетов | — | accept | План не устанавливает пакетов: `pyproject.toml` и `uv.lock` не входят в `files_modified`. Аудит легитимности пакетов не применим |
</threat_model>

<verification>
- `uv run pytest tests/ -q` — вся суита зелёная.
- Снапшот `SendLog` не меняется при убирании вложения; функция удаления объекта хранилища не вызывается.
- Два одинаковых расписания рендерятся двумя карточками; два пустых состояния различаются; порядок карточек стабилен между перезагрузками и порциями; переключение не двигает карточку.
- В `.planning/REQUIREMENTS.md` семь требований фазы отмечены выполненными, ADS-07 и ADS-08 больше не `Pending`, число требований не изменилось.
- Основание отметок называет все три закрывающих плана: 02-08, 02-09, 02-10.
</verification>

<success_criteria>
- Единственный `⚠ FLAGGED` запрет отчёта верификации закрыт именованным тестом.
- Все семь требований фазы прослеживаются согласованно с кодом, и основание отметки записано.
- Ни одно требование не добавлено и не удалено.
</success_criteria>

<output>
Create `.planning/phases/02-obyavleniya-i-raspisaniya/02-11-SUMMARY.md` when done
</output>
