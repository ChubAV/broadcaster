---
phase: 03-gruppy-akkaunta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/account_groups.py
  - app/pages/__init__.py
  - app/templates/account_groups/list.html
  - app/templates/account_groups/includes/group_row.html
  - app/templates/accounts/list.html
  - app/templates/accounts/partial_cards.html
  - app/templates/accounts/partials/sync_status_card.html
  - app/application/scheduling/use_cases.py
  - tests/test_pages/test_account_groups.py
  - tests/test_application/test_collect_due_inactive_group.py
autonomous: true
requirements: [GRP-04, GRP-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Пользователь открывает /accounts/{id}/groups своего аккаунта и видит на экране только группы этого аккаунта (D-02, GRP-04)"
    - "Пользователь переключает тумблер группы одним действием без панели подтверждения, и состояние сохраняется в БД (D-08, GRP-05)"
    - "Выключенная группа не получает задач отправки при диспетчеризации, даже если входит в расписание (D-05)"
    - "Тихий пропуск: при пропуске выключенной группы запись в SendLog не создаётся, след остаётся только в structlog (D-06)"
    - "Тумблер не меняет состав расписаний: Schedule.group_ids после переключения совпадает со значением до переключения (D-05)"
    - "Чужой аккаунт недоступен: страница и toggle чужого account_id не отдают данных и не меняют состояние"
    - "Порядок строк списка задан ORDER BY Group.id по возрастанию и одинаков при повторном открытии экрана"
    - "Две группы одного аккаунта с одинаковым name остаются двумя отдельными строками — слияния по имени нет, ключ строки это Group.id"
    - "Тумблер меняет ровно одну группу: is_active соседних групп того же аккаунта не изменяется"
    - "Двойная отправка формы тумблера возвращает группу в исходное состояние — маршрут инвертирует is_active, а не устанавливает его"
    - statement: "Группа, выключенная между сбором расписаний и постановкой задач, может получить одну уже собранную отправку; гарантия ограничена моментом сбора — новых задач для выключенной группы не создаётся"
      verification: backstop
    - "На /accounts каждая строка аккаунта во всех трёх копиях разметки несёт действие «Настроить группы», ведущее на /accounts/{id}/groups (E8 populated)"
    - "Действие «Настроить группы» рендерится для строки аккаунта в любом статусе — условной ветки, скрывающей вход, нет (E8 partial)"
    - "Ноль аккаунтов на /accounts — ноль строк и ноль входов; существующее пустое состояние экрана аккаунтов не меняется (E8 empty)"
    - "Одна ссылка на строку аккаунта, число ссылок масштабируется вместе с существующей постраничной загрузкой аккаунтов (E8 zero-one-many)"
    - "Метка «Настроить группы» — фиксированная строка внутри уже отгруженной раскладки строки аккаунта: перенос наследуется от этого экрана (E8 overflow)"
    - "В элемент входа не попадает пользовательский текст, длина метки постоянна (E8 long-text)"
  prohibitions:
    - "Операции над группами НЕ ДОЛЖНЫ молча менять пользовательскую настройку рассылки: выключение группы не удаляет её id из Schedule.group_ids и не меняет состав расписаний"
    - "Пропуск выключенной группы НЕ ДОЛЖЕН выглядеть для пользователя как попытка отправки: запись в SendLog не создаётся и новый статус журнала не вводится"
  artifacts:
    - path: "app/pages/account_groups.py"
      provides: "Роутер экрана групп аккаунта: страница и toggle с проверкой владения"
      min_lines: 60
    - path: "app/templates/account_groups/list.html"
      provides: "Страница экрана групп аккаунта"
      min_lines: 25
    - path: "app/templates/account_groups/includes/group_row.html"
      provides: "Макрос строки группы с тумблером в POST-форме"
      min_lines: 20
    - path: "tests/test_pages/test_account_groups.py"
      provides: "Поведенческие тесты экрана: владение, состав списка, тумблер"
      min_lines: 60
    - path: "tests/test_application/test_collect_due_inactive_group.py"
      provides: "Тесты D-05/D-06: выключенная группа не получает задач"
      min_lines: 50
  key_links:
    - from: "app/pages/__init__.py"
      to: "app/pages/account_groups.py"
      via: "include_router — без регистрации маршрут недостижим"
      pattern: "account_groups import router"
    - from: "app/templates/account_groups/includes/group_row.html"
      to: "app/pages/account_groups.py"
      via: "action формы тумблера ведёт на вложенный в аккаунт маршрут"
      pattern: "/accounts/.*/groups/.*/toggle"
    - from: "app/application/scheduling/use_cases.py"
      to: "app/models/group.py"
      via: "session.get(Group, group_id).is_active — условие пропуска"
      pattern: "is_active"
---

<objective>
Сквозной срез фазы: пользователь заходит с экрана «Аккаунты» на `/accounts/{id}/groups`, видит группы именно этого аккаунта и переключает тумблер одной из них — а выключенная группа впервые получает реальный эффект: диспетчеризация не ставит для неё задач отправки.

Purpose: доказать архитектуру экрана целиком (маршрут → владение → выборка → шаблон → POST-форма → БД → редирект) на первых, самых качественных токенах контекста, и сразу связать тумблер с его следствием (D-05), чтобы надпись макета «выключенные группы пропускаются при рассылке» стала правдой, а не обещанием. Закрывает GRP-04 в минимальном объёме и GRP-05 целиком по серверной части.
Output: новый роутер `app/pages/account_groups.py`, шаблоны `account_groups/list.html` и `account_groups/includes/group_row.html`, вход «Настроить группы» на `/accounts`, условие пропуска в `collect_due_schedules`, два новых тестовых файла.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gruppy-akkaunta/03-CONTEXT.md
@.planning/phases/03-gruppy-akkaunta/03-RESEARCH.md
@.planning/phases/03-gruppy-akkaunta/03-PATTERNS.md
@.planning/phases/03-gruppy-akkaunta/03-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Сквозной срез — экран групп аккаунта и рабочий тумблер</name>
  <reversibility rating="reversible">Новый модуль и новые шаблоны за существующим шелл-контрактом; откат — удаление файлов и одной строки регистрации роутера.</reversibility>
  <read_first>
    - app/pages/groups.py — полный контракт страницы, паршала, toggle и delete; источник структуры нового роутера
    - app/pages/accounts.py (строки 737-760) — образец проверки владения аккаунтом двойным WHERE
    - app/pages/__init__.py — как страничные роутеры включаются в общий router с зависимостью load_shell_context
    - app/templates/groups/list.html — шелл-контракт страницы, импорты макросов, сентинел
    - app/templates/groups/includes/group_row.html — макросная форма строки, тумблер в POST-форме, комментарии-инварианты
    - app/templates/accounts/list.html — три ветки статуса строки аккаунта и их actions-ячейка
    - app/templates/accounts/partial_cards.html — вторая синхронная копия разметки строки аккаунта
    - app/templates/accounts/partials/sync_status_card.html — третья копия; здесь идентификатор приходит как account_id, а не account.id
    - app/templates/components/toggle.html, components/button.html, components/card.html, components/empty_state.html — сигнатуры используемых макросов
    - tests/test_pages/test_schedules_list.py — образец структуры тестового файла страницы: докстринг-контракт, ORM-посев, фикстуры
    - tests/conftest.py — фикстуры db_session, client, auth_headers, authed_client
    - .planning/phases/03-gruppy-akkaunta/03-UI-SPEC.md — раздел Copywriting Contract и Interaction Contract
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ в `tests/test_pages/test_account_groups.py` и должны падать до реализации:
    - GET `/accounts/{id}/groups` своего аккаунта отвечает 200 и содержит имена групп именно этого аккаунта
    - на странице НЕТ имени группы, принадлежащей другому аккаунту того же пользователя
    - GET `/accounts/{id}/groups` чужого аккаунта не отдаёт имён его групп (редирект/404, тело без данных)
    - GET без cookie-сессии отвечает 302 на /login
    - POST `/accounts/{id}/groups/{gid}/toggle` своей группы отвечает 302 и инвертирует `is_active` в БД
    - POST toggle для группы, принадлежащей другому аккаунту, не меняет `is_active`
    - два последовательных POST toggle возвращают `is_active` к исходному значению
    - переключение одной группы не меняет `is_active` соседней группы того же аккаунта
    - Schedule.group_ids до и после переключения совпадают
    - порядок имён групп в HTML соответствует возрастанию Group.id
    - две группы с одинаковым name рендерятся двумя отдельными строками
    - GET /accounts содержит ссылку `/accounts/{id}/groups` для строки аккаунта
  </behavior>
  <files>app/pages/account_groups.py, app/pages/__init__.py, app/templates/account_groups/list.html, app/templates/account_groups/includes/group_row.html, app/templates/accounts/list.html, app/templates/accounts/partial_cards.html, app/templates/accounts/partials/sync_status_card.html, tests/test_pages/test_account_groups.py</files>
  <action>
Создать модуль `app/pages/account_groups.py` с `router = APIRouter(tags=["pages"])` и `PAGE_SIZE = 30`, преамбулой импортов по образцу `app/pages/groups.py` (`get_user_from_cookie`, `check_is_admin`, `templates`, `get_db`, `get_settings`, `Group`, `MessengerAccount`). Зарегистрировать роутер в `app/pages/__init__.py`: импорт `from app.pages.account_groups import router as account_groups_router` и `router.include_router(account_groups_router)` рядом с остальными страничными роутерами.

Два входа в этом плане.

`GET /accounts/{account_id}/groups` (`response_class=HTMLResponse`): аутентификация `get_user_from_cookie` — при её отсутствии `RedirectResponse("/login", 302)`; затем владение аккаунтом одним запросом `select(MessengerAccount).where(MessengerAccount.id == account_id, MessengerAccount.user_id == user.id)` с `scalar_one_or_none()`, при отсутствии — `RedirectResponse("/accounts", 302)`. Выборка групп: `select(Group).where(Group.user_id == user.id, Group.account_id == account_id).order_by(Group.id).limit(PAGE_SIZE + 1)`, срез на `PAGE_SIZE` и флаг `has_next` по длине (`limit+1`-приём из `groups_partial`). Контекст шаблона: `request`, `user`, `is_admin`, `account`, `groups`, `has_next`, `next_offset`, `active_page="accounts"` (D-02). Полный состав экрана — счётчики, поиск, шапка, плашка синка — план 03-05 и 03-06; здесь рендерится рабочий список строк и пустое состояние.

`POST /accounts/{account_id}/groups/{group_id}/toggle`: та же аутентификация; выборка группы ТРОЙНЫМ WHERE — `Group.id == group_id`, `Group.user_id == user.id`, `Group.account_id == account_id` (владение проверяется на каждом входе, правило Фазы 2; клиентскому `account_id` не верят). При найденной группе `group.is_active = not group.is_active` и `await db.commit()`; при ненайденной — состояние не меняется. Ответ в обоих случаях `RedirectResponse(f"/accounts/{account_id}/groups", 302)` (PRG). Состав расписаний не трогается: `Schedule.group_ids` этот маршрут не читает и не пишет (D-05 — тумблер обратим, D-08 — без панели подтверждения).

Шаблон `app/templates/account_groups/list.html`: `{% extends "base.html" %}`, `{% block title %}` и `{% block page_title %}` с именем аккаунта, импорт макросов `card_open`/`card_close`, `empty_state`, `link_button`, макроса строки из `account_groups/includes/group_row.html`. Тело — `card_open()`, цикл по `groups` с вызовом макроса строки, `card_close()`; при пустом `groups` — `empty_state` с копирайтингом из UI-SPEC (ветка «Групп пока нет» / «Все группы удалены» приходит в 03-05, здесь достаточно одной ветки «Групп пока нет» с подсказкой «Нажмите «Синхронизировать всё», чтобы подтянуть чаты аккаунта»). Действие шапки — `link_button('К аккаунтам', '/accounts', variant='ghost')`.

Шаблон `app/templates/account_groups/includes/group_row.html`: макрос `group_row(group, account_id)`, все данные приходят параметрами (импортируемый шаблон не видит контекста вызывающего). Разметка — карточная строка `[data-group-row]` (не `data-row`-таблица: UI-SPEC Responsive Contract), внутри — `avatar` инициалов, `<span>{{ group.name }}</span>` обычным выводом (имя группы — недоверенный внешний ввод, готовая разметка макросам не передаётся), и форма тумблера:
`<form method="post" action="/accounts/{{ account_id }}/groups/{{ group.id }}/toggle" x-data x-on:change="$el.submit()">` с вызовом макроса `toggle(name='is_active', checked=group.is_active, id='group-toggle-' ~ group.id, title='Отключить' if group.is_active else 'Включить')`. Перехват висит на САМОЙ форме — без Alpine остаётся настоящая POST-форма (базовый путь без JS, правило Фазы 2 D-09). Перенести в файл комментарий-инвариант о всплытии события change от чекбокса к форме — по образцу `groups/includes/group_row.html`.

Вход на экран: в actions-ячейку строки аккаунта добавить `link_button('Настроить группы', '/accounts/' ~ account.id ~ '/groups', variant='ghost')` — во ВСЕХ ТРЁХ синхронных копиях разметки строки и во всех ветках статуса внутри них: `app/templates/accounts/list.html` (ветки syncing, sync_failed и общая), `app/templates/accounts/partial_cards.html`, `app/templates/accounts/partials/sync_status_card.html`. В третьей копии идентификатор аккаунта приходит переменной `account_id`, а не `account.id` — ссылка собирается из неё. Вход рендерится безусловно, в любом статусе аккаунта.

CSS для `[data-group-row]` в этом плане не заводить: нумерованная секция `app.css` создаётся планом 03-05 целиком, разметка до неё остаётся работоспособной на наследуемых классах карточки. Новых utility-классов не вводить (`test_list_page_no_utility_classes`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py tests/test_templates/test_components.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/account_groups.py` содержит `async def account_groups_page(` и `async def account_groups_toggle(`
    - `grep -c 'Group.account_id == account_id' app/pages/account_groups.py` возвращает не менее 2 — владение аккаунтом проверено и на странице, и на toggle
    - `grep -q 'account_groups_router' app/pages/__init__.py` — роутер зарегистрирован
    - `grep -q 'active_page.*accounts' app/pages/account_groups.py` — D-02: экран живёт в разделе «Аккаунты»
    - GET `/accounts/{id}/groups` владельца отвечает 200 и содержит имя группы этого аккаунта; имени группы другого аккаунта в теле нет
    - POST toggle своей группы отвечает 302 и инвертирует `is_active`; POST toggle чужой группы `is_active` не меняет
    - Schedule.group_ids после переключения побайтово равен значению до переключения
    - `grep -c 'accounts/.*groups' app/templates/accounts/list.html` возвращает не менее 3 — ссылка стоит во всех ветках статуса
    - каждая из трёх копий разметки строки аккаунта содержит подстроку `/groups` в actions-ячейке
    - `uv run pytest tests/test_pages/test_account_groups.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Пользователь попадает на экран групп аккаунта кликом с `/accounts`, видит там только группы этого аккаунта и переключает тумблер одной из них; состояние сохраняется, расписания не меняются, чужие данные недостижимы.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Выключенная группа пропускается при диспетчеризации</name>
  <reversibility rating="costly">Условие уходит в боевой пайплайн диспетчеризации (D-05): откат означает повторную проверку всех тестов сбора расписаний, не правку одного файла.</reversibility>
  <read_first>
    - app/application/scheduling/use_cases.py (строки 60-150) — функция collect_due_schedules целиком: блок D-01 как эталон стиля комментария и per-group цикл как точка врезки
    - app/models/group.py — колонка is_active с default=True
    - tests/test_application/test_collect_due_draft.py — образец тестового файла: локальный движок, посев просроченного расписания, хелпер _as_utc
    - tests/test_application/test_scheduling_use_cases.py — существующие ожидания по составу задач
    - app/worker/tasks.py (строка 29) — стиль structlog: logger = structlog.get_logger(__name__)
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ в `tests/test_application/test_collect_due_inactive_group.py` и должны падать до правки:
    - расписание с двумя группами, одна из которых `is_active=False`: `collect_due_schedules` возвращает ровно одну задачу — для активной группы
    - выключенная группа в расписании для аккаунта типа `tg_user` — задачи нет
    - выключенная группа в расписании для аккаунта типа `wa` — задачи нет
    - выключенная группа в расписании для аккаунта типа `max` — задачи нет
    - после включения группы (`is_active=True`) тот же вызов возвращает задачу для неё
    - `Schedule.group_ids` после сбора не изменился — состав расписания не редактируется
    - `Schedule.next_run_at` продвинулся вперёд даже когда ВСЕ группы расписания выключены (расписание живо, пропущена только группа)
    - после сбора со всеми выключенными группами количество строк `SendLog` равно нулю (D-06 — тихий пропуск)
  </behavior>
  <files>app/application/scheduling/use_cases.py, tests/test_application/test_collect_due_inactive_group.py</files>
  <action>
Врезать пропуск выключенных групп в per-group цикл `collect_due_schedules` — в тот единственный цикл, где группа уже адресуется поштучно (D-05). Форма врезки: поднять `group = await session.get(Group, group_id)` ВЫШЕ ветвления по `account.type`, до создания `DispatchTask`; если группа найдена и `group.is_active` ложно — записать событие structlog `logger.info("group_skipped_inactive", group_id=group_id, schedule_id=schedule.id)` и `continue`. Ветка WA/MAX ниже переиспользует уже полученный объект вместо повторного `session.get` (identity map SQLAlchemy делает повторный вызов бесплатным, но лишний вызов из кода убрать).

Пропуск обязан быть тихим: `SendLog` в этой ветке не создаётся и новый статус журнала не вводится (D-06) — история отражает реальные попытки отправки. Расчёт `schedule.next_run_at` в конце итерации по расписанию не трогается: пропускается группа, а не расписание.

Рядом с условием обязателен комментарий в стиле файла — эталон стоит выше в том же файле у блока D-01 (строки 81-104): объяснить, ПОЧЕМУ условие стоит в этом цикле, а не в WHERE выборки расписаний (`group_ids` — JSON-список на расписании, WHERE-фильтр по составу групп там невозможен, а состав расписания при выключении группы не меняется — тумблер обратим), и сослаться на парный тестовый файл `tests/test_application/test_collect_due_inactive_group.py` как на спецификацию этого места.

Модуль-логгер: если `structlog.get_logger(__name__)` в файле ещё не объявлен — объявить на уровне модуля по образцу `app/worker/tasks.py:29`.

Прогон существующих тестов диспетчеризации обязателен: `session.get(Group, ...)` теперь выполняется и для TG-пути, где раньше не выполнялся, — тесты с мок-сессиями могут повести себя иначе, и это надо увидеть до коммита, а не после. Прогон адресуется поимённо файлу этой задачи плюс тестам воркера, а не каталогу `tests/test_application/` целиком: в первой волне параллельно идёт 03-02, который создаёт в том же каталоге собственный тестовый файл, и обход каталога собрал бы его наполовину написанным — падение было бы про чужую задачу. Обход каталога целиком выполняет 03-04 во второй волне, когда первая волна уже слита.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_collect_due_inactive_group.py -x -q</automated>
    <automated>uv run pytest tests/test_application/test_collect_due_inactive_group.py tests/test_worker/ tests/test_worker_tasks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'group_skipped_inactive' app/application/scheduling/use_cases.py` — событие пропуска логируется
    - `grep -q 'D-05' app/application/scheduling/use_cases.py` — комментарий-инвариант помечен решением, как это сделано для D-01
    - `collect_due_schedules` для расписания с одной активной и одной выключенной группой возвращает ровно одну задачу
    - для всех трёх типов аккаунта (`tg_user`, `wa`, `max`) выключенная группа не даёт задачи
    - количество строк `SendLog` после сбора с выключенными группами равно 0
    - `Schedule.next_run_at` продвинут вперёд даже когда все группы расписания выключены
    - `uv run pytest tests/test_application/test_collect_due_inactive_group.py tests/test_worker/ tests/test_worker_tasks.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Выключенная группа не получает задач отправки при диспетчеризации для всех трёх типов аккаунта; в SendLog при этом ничего не пишется, расписание продолжает жить и двигать next_run_at, включение группы немедленно возобновляет рассылку.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный маршрут `/accounts/{id}/groups` | `account_id` приходит из URL, то есть от недоверенного клиента |
| браузер → POST toggle | `account_id` и `group_id` приходят из URL; форма отправляется любым клиентом с валидной cookie |
| мессенджер → БД → шаблон | `Group.name` создан внешней системой и рендерится в HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | `GET /accounts/{account_id}/groups` | high | mitigate | Владение аккаунтом проверяется отдельным запросом `MessengerAccount.id == account_id AND user_id == user.id`; выборка групп дополнительно ограничена `Group.user_id` и `Group.account_id`. Поведенческий тест «чужой аккаунт не отдаёт имён групп» обязателен и пишется до реализации |
| T-03-02 | Elevation of Privilege | `POST /accounts/{account_id}/groups/{group_id}/toggle` | high | mitigate | Тройной WHERE (`Group.id`, `Group.user_id`, `Group.account_id`); при несовпадении состояние не меняется, ответ — тот же редирект, без различимого сигнала о существовании чужой группы. Тест «чужая группа не переключается» пишется до реализации |
| T-03-03 | Tampering | `account_groups/includes/group_row.html` | medium | mitigate | Имя группы выводится обычным `{{ group.name }}` под autoescape Jinja2; в макросы уходит текст, не разметка. Покрывается существующим `test_no_unsafe_escaping`, обходящим все шаблоны |
| T-03-04 | Repudiation | `collect_due_schedules` | low | accept | Пропуск выключенной группы фиксируется только structlog-событием `group_skipped_inactive`; запись в SendLog прямо запрещена D-06, и это осознанный размен: история обязана отражать реальные попытки отправки |
| T-03-05 | Tampering | POST-формы экрана | low | accept | CSRF-токенов в проекте нет ни на одной форме; новые формы не хуже существующих и новой поверхности класса не добавляют. Общесистемное решение — вне рамок фазы |

Установок пакетов (npm/pip/cargo) фаза не содержит — RESEARCH.md §Package Legitimacy Audit фиксирует нулевой список; строка `T-{phase}-SC` неприменима.
</threat_model>

## Artifacts this phase produces

Символы и файлы, создаваемые ЭТИМ планом (новые, не подлежащие проверке на дрейф):

- Файл `app/pages/account_groups.py`; в нём `router`, `PAGE_SIZE`, `account_groups_page`, `account_groups_toggle`
- Маршруты `GET /accounts/{account_id}/groups`, `POST /accounts/{account_id}/groups/{group_id}/toggle`
- Файлы шаблонов `app/templates/account_groups/list.html`, `app/templates/account_groups/includes/group_row.html`; макрос `group_row`
- Имя `account_groups_router` в `app/pages/__init__.py`
- structlog-событие `group_skipped_inactive` в `app/application/scheduling/use_cases.py`
- Файлы тестов `tests/test_pages/test_account_groups.py`, `tests/test_application/test_collect_due_inactive_group.py`
- HTML-атрибут раскладки `data-group-row`

## Planner assumptions (flagged, not resolved)

- Зондирование границ по GRP-07 вернуло строку `unclassified` («review manually»): движок не отнёс требование «повторно синхронизировать группы и увидеть результат» ни к одной категории. Это НЕ разрешено автоматически и не превращено в критерий: строка остаётся открытым предположением планировщика и должна быть просмотрена человеком при верификации фазы. Содержательная часть GRP-07 покрыта планами 03-02, 03-04 и 03-06; открытым остаётся именно вопрос «какие границы у требования, которого классификатор не понял».

<verification>
- `uv run pytest tests/test_pages/test_account_groups.py tests/test_application/test_collect_due_inactive_group.py -q` — зелено
- `uv run pytest tests/ -q` — суита не деградировала относительно базы 895 зелёных
- Экран достижим кликом с `/accounts` во всех трёх копиях разметки строки
- Ни одна проверка владения не пропущена: страница и toggle фильтруют по `user_id` И `account_id`
</verification>

<success_criteria>
- GRP-04 (частично): экран `/accounts/{id}/groups` существует, показывает только группы этого аккаунта и достижим с экрана «Аккаунты»
- GRP-05: группа включается и отключается одним действием; выключенная группа не получает задач отправки для всех трёх типов аккаунта
- D-02, D-05, D-06, D-08 реализованы и закреплены тестами
- Базовый путь без JS сохранён: тумблер остаётся настоящей POST-формой
</success_criteria>

<output>
Create `.planning/phases/03-gruppy-akkaunta/03-01-SUMMARY.md` when done
</output>
