---
phase: 03-gruppy-akkaunta
plan: 08
type: execute
wave: 4
depends_on: ["03-05", "03-06", "03-07"]
files_modified:
  - app/pages/groups.py
  - app/pages/__init__.py
  - app/pages/common.py
  - app/templates/groups/list.html
  - app/templates/groups/partial_cards.html
  - app/templates/groups/includes/group_row.html
  - app/static/css/app.css
  - tests/test_pages/test_shell.py
  - tests/test_pages/test_responsive_markup.py
  - tests/test_pages/test_htmx_preserved.py
  - tests/test_templates/test_components.py
  - tests/test_routes/test_groups_bulk.py
autonomous: true
requirements: [GRP-04, GRP-06]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Глобальный раздел групп снесён целиком: страниц, паршалов, массовых операций и фильтров старого раздела в приложении не осталось (D-01)"
    - "Пункт «Группы» отсутствует в боковом меню и в нижних табах на каждой странице (D-01)"
    - "Старый адрес раздела отвечает перенаправлением на экран аккаунтов — закладки пользователей не приводят к ошибке отсутствия страницы (D-01)"
    - "Общие параметризованные проверки адаптивной разметки, сохранности поведения подгрузки и состава шелла знают про новый экран вместо снесённого раздела"
    - "Поведенческие утверждения снесённого раздела — проверка владения при переключении и чистка расписаний при удалении — живут на новом экране, а не исчезли вместе с разделом"
    - "Стили снесённого раздела удалены из единственной таблицы стилей, а не остались сиротами"
    - "Переход по старому адресу и по любой старой глубокой ссылке — обычная навигация с ответом сервера, клиентского состояния ожидания на этом пути не существует (E8 loading)"
    - "Старый адрес и старые глубокие ссылки отвечают перенаправлением, а не ошибкой отсутствия страницы; пункта меню и его счётчика нет ни на одной странице (E8 error)"
    - "Полная суита зелена: снос не оставил ни одного висящего утверждения"
  artifacts:
    - path: "app/pages/groups.py"
      provides: "Заглушка-перенаправление со старого адреса раздела на экран аккаунтов"
      min_lines: 8
  key_links:
    - from: "app/pages/groups.py"
      to: "app/pages/accounts.py"
      via: "перенаправление старого адреса на экран аккаунтов"
      pattern: "RedirectResponse"
---

<objective>
Глобальный раздел «Группы» сносится целиком — страницы, паршалы, массовые операции, фильтры, шаблоны, стили и пункт навигации, — а старый адрес отвечает перенаправлением на экран аккаунтов. Общие параметризованные проверки переезжают со снесённого раздела на новый экран.

Purpose: закрывает отложенное решение Фазы 1 (пункт «Группы» был оставлен временно именно до этой фазы) и делает состав интерфейса честным: управление группами живёт на уровне аккаунта, и двух конкурирующих экранов групп в продукте быть не должно (D-01). Снос идёт последним, потому что до него новый экран обязан уметь всё, что умел старый раздел, — иначе снос отнимает возможность вместо того, чтобы её перенести.
Output: заглушка-перенаправление вместо раздела; удалённые шаблоны и стили; пункт навигации удалён; общие тестовые параметризации знают новый экран.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-gruppy-akkaunta/03-CONTEXT.md
@.planning/phases/03-gruppy-akkaunta/03-RESEARCH.md
@.planning/phases/03-gruppy-akkaunta/03-PATTERNS.md
@.planning/phases/03-gruppy-akkaunta/03-05-SUMMARY.md
@.planning/phases/03-gruppy-akkaunta/03-06-SUMMARY.md
@.planning/phases/01-interfeysnyy-fundament/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Заглушка-перенаправление, удаление шаблонов и пункта навигации</name>
  <reversibility rating="costly">Снос шаблонов, маршрутов и их тестов: возврат раздела означает восстановление его заново, а не откат одной правки.</reversibility>
  <precondition>Планы 03-05, 03-06 и 03-07 завершены: новый экран умеет список, поиск, подгрузку, переключение, удаление с чисткой расписаний и повторную синхронизацию с показом результата.</precondition>
  <read_first>
    - app/pages/groups.py — весь файл: какие входы удаляются и какое поведение уже перенесено на новый экран
    - app/pages/__init__.py — импорт и включение роутера раздела
    - app/pages/common.py (строки 105-120) — список пунктов навигации, выписанный один раз и используемый и боковым меню, и нижними табами; комментарий над списком объясняет, почему пункт групп дожил до этой фазы
    - app/templates/groups/list.html, app/templates/groups/partial_cards.html, app/templates/groups/includes/group_row.html — удаляемые шаблоны
    - app/pages/admin.py — обращения к адресам, начинающимся так же, как раздел групп: раздел админ-панели о сведениях по группам остаётся и не должен пострадать
  </read_first>
  <files>app/pages/groups.py, app/pages/__init__.py, app/pages/common.py, app/templates/groups/list.html, app/templates/groups/partial_cards.html, app/templates/groups/includes/group_row.html</files>
  <action>
Свести `app/pages/groups.py` к единственному входу — обработчику старого адреса раздела, возвращающему перенаправление на `/accounts` кодом 302. Все остальные входы файла (страница списка, паршал прокрутки, переключение, удаление, массовые операции) удаляются: их поведение уже живёт на маршрутах экрана групп аккаунта. Регистрация роутера в `app/pages/__init__.py` сохраняется — она нужна заглушке.

Причина именно перенаправления, а не удаления адреса: у пользователей остались закладки и старые ссылки, и ответ об отсутствии страницы на них — потеря без нужды. Записать это комментарием у заглушки.

Удалить каталог шаблонов раздела целиком — три файла: страницу, страницу подгрузки и макрос строки.

Удалить из списка пунктов навигации в `app/pages/common.py` строку пункта с ключом groups. Счётчика у этого пункта нет, поэтому правка состоит ровно в удалении одной строки списка и в приведении комментария над списком в соответствие: пояснение о том, что пункт сохраняется до Фазы 3, становится ложным и обязано уйти вместе с пунктом.

Раздел админ-панели со сведениями о группах живёт по другому адресу и в этой задаче не затрагивается — проверить это перед правкой и после неё.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/ -q</automated>
  </verify>
  <acceptance_criteria>
    - запрос старого адреса раздела отвечает кодом 302 с заголовком перенаправления на `/accounts`
    - каталога `app/templates/groups` не существует
    - `grep -c 'label": "Группы"' app/pages/common.py` возвращает 0
    - HTML любой страницы приложения не содержит пункта меню с подписью «Группы»
    - раздел админ-панели со сведениями о группах отвечает кодом 200 и не изменён
    - `app/pages/groups.py` не содержит ни одного обработчика метода POST
    - `uv run pytest tests/test_pages/test_shell.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Раздел снесён, старый адрес ведёт на экран аккаунтов, пункта «Группы» в навигации нет ни на одной странице, админ-раздел не задет.</done>
</task>

<task type="auto">
  <name>Task 2: Перевод общих параметризованных проверок на новый экран</name>
  <read_first>
    - tests/test_pages/test_responsive_markup.py — карта адресов разделов, список перенесённых разделов, ветка посева данных для раздела групп, собственные тесты раздела: карточный рендер, фильтры при подгрузке, сворачивание фильтров, проверка владения при переключении, панель подтверждения удаления, деградация формы удаления без Alpine, массовое удаление
    - tests/test_pages/test_htmx_preserved.py — параметризации по разделам, цепочка подгрузки, ветка посева для раздела групп, проброс фильтров
    - tests/test_pages/test_shell.py — список маршрутов шелла, список маршрутов паршалов, тест сохранности пункта навигации
    - tests/test_templates/test_components.py — набор файлов с массовыми действиями и перечень мест удаления строк
    - tests/test_routes/test_groups_bulk.py — тесты массовых операций снесённого раздела
    - tests/test_pages/test_account_groups.py — что уже покрыто на новом экране планами 03-01, 03-05 и 03-06
  </read_first>
  <files>tests/test_pages/test_responsive_markup.py, tests/test_pages/test_htmx_preserved.py, tests/test_pages/test_shell.py, tests/test_templates/test_components.py, tests/test_routes/test_groups_bulk.py</files>
  <action>
Раздел групп прошит в четырёх общих параметризованных файлах: слепое удаление маршрутов роняет десятки тестов, а слепое удаление тестов теряет проверки, которым новый экран обязан удовлетворять.

Порядок работы по каждому файлу — сначала сверка, потом правка. Сверка: выписать, что именно утверждает каждый тест раздела; отнести утверждение к одной из трёх категорий — переезжает на новый экран, исчезает вместе со снесённой возможностью, уже покрыто тестами нового экрана. Список сверки записать в SUMMARY: он и есть доказательство, что снос ничего не потерял.

В карте адресов разделов адаптивной проверки заменить запись раздела групп записью нового экрана: адрес экрана требует идентификатора аккаунта, поэтому ветка посева данных обязана создать аккаунт и вернуть его адрес. Новый экран включается в тот же класс проверок адаптивных примитивов и запрета utility-классов — он обязан их проходить.

В параметризациях проверки сохранности поведения подгрузки заменить раздел групп новым экраном: цепочка подгрузки и проброс параметра поиска проверяются на нём. Проброс фильтров по мессенджеру и по активности исчезает вместе с фильтрами (D-03) — вместо него проверяется проброс строки поиска.

В списках маршрутов шелла заменить адрес раздела адресом нового экрана, а адрес паршала раздела — адресом паршала нового экрана. Тест сохранности пункта навигации переписывается в свою противоположность: пункт обязан отсутствовать, и это утверждение обязано быть явным тестом, а не следствием отсутствия старого.

В перечнях мест массовых действий и мест удаления строк проверки шаблонов убрать записи снесённых шаблонов и добавить макрос строки нового экрана в перечень мест удаления строк. Записи о массовых действиях не заменяются: у нового экрана массовых операций нет (D-03).

Тесты массовых операций снесённого раздела удаляются файлом целиком: возможности, которую они проверяли, в продукте больше нет.

Проверки владения при переключении и чистки расписаний при удалении обязаны существовать на новом экране — они уже написаны планами 03-01 и 03-05. Сверить и, если какое-то утверждение старого файла на новом экране не воспроизведено, дописать его в тесты нового экрана прежде, чем удалить старое.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py tests/test_pages/test_htmx_preserved.py tests/test_pages/test_shell.py tests/test_templates/test_components.py -q</automated>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - файла `tests/test_routes/test_groups_bulk.py` не существует
    - карта адресов разделов в `tests/test_pages/test_responsive_markup.py` содержит запись нового экрана и не содержит записи снесённого раздела
    - параметризации по разделам в `tests/test_pages/test_htmx_preserved.py` содержат новый экран и не содержат снесённого раздела
    - `tests/test_pages/test_shell.py` содержит явный тест ОТСУТСТВИЯ пункта навигации с подписью «Группы»
    - перечень мест удаления строк в `tests/test_templates/test_components.py` содержит макрос строки нового экрана
    - `uv run pytest tests/ -q` завершается кодом 0
    - SUMMARY содержит список сверки утверждений снесённого раздела с их судьбой в трёх категориях
  </acceptance_criteria>
  <done>Общие проверки знают новый экран вместо снесённого раздела, ни одно поведенческое утверждение не потеряно, и это доказано списком сверки, а не заявлено.</done>
</task>

<task type="auto">
  <name>Task 3: Удаление стилей снесённого раздела и итоговый прогон</name>
  <read_first>
    - app/static/css/app.css — нумерация секций, секция снесённого раздела и секция нового экрана, добавленная планом 03-05
    - app/templates — какие шаблоны остались и какие классы из удаляемой секции ими используются
    - .planning/ROADMAP.md — пять критериев успеха фазы 3 в редакции после правки плана 03-07
  </read_first>
  <files>app/static/css/app.css</files>
  <action>
Удалить из единственной таблицы стилей проекта секцию снесённого раздела. Правило фазы прямое: стили снесённого раздела обязаны быть удалены, а не остаться сиротами.

Перед удалением каждого селектора убедиться, что он не используется оставшимися шаблонами: часть классов раздела могла быть общей. Селекторы, оказавшиеся общими, остаются, а их принадлежность фиксируется комментарием — иначе следующий снос удалит их вслепую. Нумерация остальных секций не сдвигается: перенумерация превратила бы диф в нечитаемый и спрятала бы содержательную правку.

Итоговый прогон фазы: полная суита зелена; главные пути пройдены вручную по критериям успеха фазы — открытие экрана групп аккаунта, переключение и удаление группы, повторная синхронизация с показом результата, работа на мобильных ширинах.

В SUMMARY записать итоговое число тестов суиты и разницу с базой в 895 зелёных с расшифровкой: сколько удалено вместе со снесённым разделом и удалённой JSON-поверхностью и сколько добавлено новым экраном. Без расшифровки изменившееся число неотличимо от потерянных тестов.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py tests/test_templates/ -q</automated>
    <human-check>Пройти пять критериев успеха фазы вручную: открыть экран групп аккаунта, включить и отключить группу, удалить группу через панель подтверждения, запустить повторную синхронизацию и увидеть её результат не покидая экрана, повторить на ширине 320 пикселей.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'groups-bulk' app/static/css/app.css` возвращает 0
    - каждый селектор, оставленный в таблице стилей как общий, встречается хотя бы в одном оставшемся шаблоне
    - нумерация секций таблицы стилей не изменилась для секций, не подвергшихся удалению
    - `uv run pytest tests/ -q` завершается кодом 0
    - SUMMARY содержит итоговое число тестов и расшифровку разницы с базой 895
  </acceptance_criteria>
  <done>Стили снесённого раздела удалены без сирот и без потери общих селекторов; суита зелена; расхождение с базовым числом тестов объяснено по составу.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → старый адрес раздела | закладки и старые ссылки пользователей приходят на адрес, за которым больше нет страницы |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-33 | Denial of Service | старый адрес раздела | medium | mitigate | Заглушка отвечает перенаправлением на экран аккаунтов: закладки продолжают работать, ответа об отсутствии страницы пользователь не видит |
| T-03-34 | Elevation of Privilege | потеря проверок владения вместе со снесёнными тестами | high | mitigate | Утверждения о владении при переключении и об удалении с чисткой расписаний сверяются со списком и обязаны существовать на новом экране ДО удаления старых; список сверки записывается в SUMMARY |
| T-03-35 | Tampering | заглушка перенаправления | low | accept | Заглушка не принимает параметров и не читает состояния: перенаправление безусловно |

Установок пакетов фаза не содержит — строка `T-{phase}-SC` неприменима.
</threat_model>

## Artifacts this phase produces

- Заглушка-перенаправление в `app/pages/groups.py` (единственный оставшийся обработчик модуля)
- Удаления: каталог `app/templates/groups` (три файла), пункт навигации с ключом groups в `app/pages/common.py`, файл `tests/test_routes/test_groups_bulk.py`, секция стилей снесённого раздела в `app/static/css/app.css`
- Записи нового экрана в общих параметризациях `tests/test_pages/test_responsive_markup.py`, `tests/test_pages/test_htmx_preserved.py`, `tests/test_pages/test_shell.py`, `tests/test_templates/test_components.py`

<verification>
- `uv run pytest tests/ -q` — зелено
- Старый адрес раздела отвечает перенаправлением, пункта навигации нет ни на одной странице
- Список сверки утверждений снесённого раздела записан и не содержит категории «потеряно»
</verification>

<success_criteria>
- D-01 выполнено целиком: раздел, его шаблоны, массовые операции, фильтры, стили и пункт навигации сняты, старый адрес перенаправляет
- Ни одна пользовательская возможность и ни одно поведенческое утверждение не потеряны при сносе
- Полная суита фазы зелена, расхождение с базовым числом тестов объяснено по составу
</success_criteria>

<output>
Create `.planning/phases/03-gruppy-akkaunta/03-08-SUMMARY.md` when done
</output>
