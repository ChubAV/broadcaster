---
phase: 03-gruppy-akkaunta
plan: 07
type: execute
wave: 3
depends_on: ["03-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - app/routes/groups.py
  - app/main.py
  - tests/conftest.py
  - tests/test_routes/test_groups.py
  - tests/test_routes/test_limits.py
  - tests/test_routes/test_history.py
  - tests/test_routes/test_schedules.py
  - tests/test_routes/test_schedules_api_null.py
  - tests/test_routes/test_schedules_api_ownership.py
  - tests/test_routes/test_schedules_toggle_detached.py
  - tests/test_e2e.py
  - tests/test_pages/test_schedules_detached_account.py
  - tests/test_application/test_account_deletion_schedules.py
autonomous: true
requirements: [GRP-08]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GRP-08 снято решением владельца: roadmap и требования говорят это согласованно, с указанной причиной и сохранённой прослеживаемостью (D-13)"
    - "Входа ручного создания группы в приложении не существует: JSON-маршрут создания удалён вместе с дырой проверки владения аккаунтом (D-14)"
    - "Тесты сеют группы прямой вставкой через ORM, а не через удалённый вход, и суита остаётся зелёной"
    - "Единственный источник появления групп — синхронизация: коллизия «добавленная вручную группа против пришедшей синком» невозможна по построению (GRP-08 adjacency)"
    - "Пустое состояние экрана не предлагает добавить группу вручную — призыва к действию из макета в разметке нет (GRP-08 empty)"
    - "Порядок списка задаётся синхронизацией и возрастанием идентификатора; ручных вставок в порядок не существует (GRP-08 ordering)"
    - "Тарифного лимита на количество групп в системе нет, и с удалением входа ничего не теряется — точки применения лимита за ним не стояло"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GRP-08 в разделе вне области с причиной снятия и сохранённой строкой прослеживаемости"
      contains: "GRP-08"
    - path: "tests/conftest.py"
      provides: "Фикстура-хелпер прямого посева групп через ORM"
      contains: "def "
  key_links:
    - from: "tests/conftest.py"
      to: "app/models/group.py"
      via: "посев групп прямой вставкой ORM вместо запроса к удалённому входу"
      pattern: "Group\\("
---

<objective>
Решение владельца о снятии GRP-08 доводится до конца в обеих плоскостях: документы фазы перестают обещать ручное добавление группы, а единственный вход ручного создания группы удаляется из приложения вместе с тестовым посевом, который на нём держался.

Purpose: снятое требование, оставшееся в roadmap и в требованиях, превращает верификацию фазы в спор о том, что именно обещано (D-13). А маршрут создания группы — не просто неиспользуемый вход: он не проверяет владение аккаунтом, то есть несёт дыру того же класса, что закрывалась в Фазе 2, и его удаление и есть митигация (D-14). Отдельная задача нужна потому, что этот вход де-факто работает посевом групп минимум в девяти тестовых файлах: удаление одной строкой означает красную суиту.
Output: согласованные ROADMAP.md и REQUIREMENTS.md; фикстура прямого посева групп в conftest; девять тестовых файлов, переведённых на неё; удалённый JSON-роутер групп.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-gruppy-akkaunta/03-CONTEXT.md
@.planning/phases/03-gruppy-akkaunta/03-RESEARCH.md
@.planning/phases/03-gruppy-akkaunta/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Согласованная правка roadmap и требований — GRP-08 снято</name>
  <reversibility rating="reversible">Решение о невключении возможности; возврат — отдельная фаза с собственным обсуждением форматов идентификаторов и валидации.</reversibility>
  <read_first>
    - .planning/ROADMAP.md — секция «Phase 3: Группы аккаунта»: строка цели, строка требований, пять критериев успеха
    - .planning/REQUIREMENTS.md — блок GRP-04…GRP-08 и таблица прослеживаемости в конце файла
    - .planning/phases/03-gruppy-akkaunta/03-CONTEXT.md — решение D-13 с датой и формулировкой причины
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Правка выполняется точечными заменами в существующих файлах — переписывание файлов целиком запрещено: в ROADMAP.md живут шесть фаз, в REQUIREMENTS.md — весь набор требований milestone.

В `.planning/ROADMAP.md`, секция «Phase 3: Группы аккаунта»:
- из строки требований убрать `GRP-08`, оставив `GRP-04, GRP-05, GRP-06, GRP-07`;
- критерий успеха 3 сегодня обещает две возможности сразу — удаление группы из списка аккаунта и добавление группы вручную; вторая половина обещания снимается, критерий остаётся об удалении;
- строка цели фазы правится только если содержит обещание ручного добавления; если не содержит — не трогать.

В `.planning/REQUIREMENTS.md`:
- строку требования GRP-08 перенести в раздел вне области milestone с явной причиной: снято решением владельца 2026-08-11 при обсуждении фазы 3 (D-13), возврат возможен отдельной фазой с обсуждением форматов идентификаторов и валидации;
- строку GRP-08 в таблице прослеживаемости сохранить, изменив состояние с ожидающего на «вне области, v2.0» со ссылкой на D-13 — прослеживаемость требования не удаляется, иначе через полгода никто не восстановит, было ли требование выполнено, отменено или потеряно.

Формулировка причины обязана называть, кто и когда принял решение, — не «не нужно», а «снято владельцем такого-то числа при обсуждении фазы».
  </action>
  <verify>
    <automated>grep -n 'GRP-08' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c 'GRP-08' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GRP-08' .planning/ROADMAP.md` возвращает 0
    - строка требований фазы 3 в `.planning/ROADMAP.md` перечисляет ровно GRP-04, GRP-05, GRP-06, GRP-07
    - критерий успеха 3 фазы 3 в `.planning/ROADMAP.md` не содержит слова «вручную»
    - `.planning/REQUIREMENTS.md` содержит GRP-08 в разделе вне области с причиной, включающей дату 2026-08-11 и пометку D-13
    - строка GRP-08 в таблице прослеживаемости `.planning/REQUIREMENTS.md` присутствует и не имеет состояния ожидания
    - остальные фазы `.planning/ROADMAP.md` не изменены — диф касается только секции фазы 3
  </acceptance_criteria>
  <done>Roadmap и требования говорят о ручном добавлении группы одно и то же: возможность снята владельцем, причина и дата записаны, прослеживаемость сохранена.</done>
</task>

<task type="auto">
  <name>Task 2: Фикстура прямого посева групп и перевод на неё девяти тестовых файлов</name>
  <read_first>
    - tests/conftest.py — фикстуры db_session, client, auth_headers, authed_client и их порядок
    - app/models/group.py — обязательные поля модели: user_id, account_id, messenger_type, group_external_id, name
    - tests/test_pages/test_schedules_list.py — образец локального ORM-посева аккаунта и групп в тестовом файле
    - tests/test_routes/test_schedules.py — типичный вызов создания группы через удаляемый вход и то, что тест делает с результатом
    - tests/test_routes/test_limits.py — тест, документирующий отсутствие тарифного лимита на группы
  </read_first>
  <files>tests/conftest.py, tests/test_routes/test_history.py, tests/test_routes/test_schedules.py, tests/test_routes/test_schedules_api_null.py, tests/test_routes/test_schedules_api_ownership.py, tests/test_routes/test_schedules_toggle_detached.py, tests/test_e2e.py, tests/test_pages/test_schedules_detached_account.py, tests/test_application/test_account_deletion_schedules.py, tests/test_routes/test_limits.py</files>
  <action>
Добавить в `tests/conftest.py` фикстуру-хелпер прямого посева группы через ORM: асинхронную функцию, принимающую сессию, идентификатор аккаунта, идентификатор пользователя и необязательные имя, тип мессенджера и внешний идентификатор, создающую строку `Group`, фиксирующую транзакцию и возвращающую созданный объект. Значения по умолчанию должны позволять вызвать её одной строкой — этим она заменяет одну строку запроса, которую заменяет.

Перевести на неё все вызовы создания группы через удаляемый JSON-вход в девяти файлах: `tests/test_routes/test_history.py`, `tests/test_routes/test_schedules.py`, `tests/test_routes/test_schedules_api_null.py`, `tests/test_routes/test_schedules_api_ownership.py`, `tests/test_routes/test_schedules_toggle_detached.py`, `tests/test_e2e.py`, `tests/test_pages/test_schedules_detached_account.py`, `tests/test_application/test_account_deletion_schedules.py`, `tests/test_routes/test_limits.py`. В восьми из них вызов единственный, в одном — два.

Замена механическая и обязана сохранять смысл каждого теста: там, где тест использовал идентификатор созданной группы из тела ответа, он берёт его из возвращённого объекта; там, где проверялся код ответа самого создания, — эта проверка исчезает вместе с входом, и это правильно, потому что она проверяла удаляемое поведение, а не поведение теста.

Тест, документирующий отсутствие тарифного лимита на группы, удаляется вместе со входом: за ним не стоит точки применения лимита, и после удаления входа утверждать ему нечего. Факт отсутствия лимита зафиксировать одной строкой в SUMMARY, чтобы он не потерялся вместе с тестом.

Порядок работы: сначала фикстура, затем по одному файлу с прогоном его тестов после каждой замены — иначе девять одновременных правок дают неотличимые друг от друга падения. Удаление самого входа выполняется задачей 3, поэтому после этой задачи суита обязана быть зелёной ДО удаления: это доказывает, что посев переведён корректно, а не что тесты просто перестали выполняться.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes/ tests/test_e2e.py tests/test_pages/test_schedules_detached_account.py tests/test_application/test_account_deletion_schedules.py -q</automated>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` содержит фикстуру-хелпер, создающую строку `Group` напрямую через сессию
    - ни один из девяти перечисленных файлов не обращается к удаляемому JSON-входу создания группы
    - `uv run pytest tests/ -q` завершается кодом 0 ДО удаления входа — посев переведён, а не сломан
    - тест, документировавший отсутствие лимита на группы, удалён, и факт отсутствия лимита записан в SUMMARY
    - число тестов в суите уменьшилось ровно на удалённые, а не на случайно пропущенные файлы
  </acceptance_criteria>
  <done>Тестовый посев групп больше не зависит от удаляемого входа: суита зелена на прямой вставке через ORM, и удаление входа стало безопасным.</done>
</task>

<task type="auto">
  <name>Task 3: Удаление JSON-маршрутов групп</name>
  <reversibility rating="costly">Снимается вся JSON-поверхность групп: возврат означает восстановление роутера, его регистрации и его тестов заново, а для гипотетического внешнего клиента вне репозитория — сломанный контракт, о котором мы узнаем не сразу.</reversibility>
  <read_first>
    - app/routes/groups.py — все четыре входа: создание без проверки владения аккаунтом, список, удаление, переключение; какое поведение из них обязано остаться на страничных маршрутах
    - app/main.py (строки 20-30 и 78-90) — импорт и регистрация роутера
    - tests/test_routes/test_groups.py — восемнадцать обращений к удаляемым входам: какие утверждения о поведении обязаны переехать на новый экран
    - app/pages/account_groups.py — страничные маршруты экрана, которые несут это поведение после фазы
  </read_first>
  <files>app/routes/groups.py, app/main.py, tests/test_routes/test_groups.py</files>
  <action>
Удалить `app/routes/groups.py` целиком вместе с его импортом и регистрацией в `app/main.py`.

Обоснование удаления всех четырёх входов, а не только создания (разрешение дискреции D-14 и допущения A4 из RESEARCH): вход создания снимается прямым решением владельца, а у остальных трёх нет ни одного потребителя в репозитории кроме тестов — ни разметка, ни статические файлы к ним не обращаются; публичного контракта JSON-API в документах проекта не зафиксировано. Оставлять три входа означало бы выравнивать в них проверку владения аккаунтом ради поверхности, которой никто не пользуется. Наименьшая поверхность закрывает класс дефекта целиком. Риск внешнего клиента вне репозитория признан и записан: он и есть содержание рейтинга обратимости этой задачи, и его следует назвать в SUMMARY явно, чтобы владелец мог возразить до слияния.

Всё поведение, которое несли удаляемые входы, к этому моменту уже живёт на страничных маршрутах экрана групп аккаунта: список — страница и паршал прокрутки, переключение — маршрут тумблера, удаление с чисткой расписаний — маршрут удаления. Ни одна возможность не теряется.

`tests/test_routes/test_groups.py` удаляется: его утверждения о владении, о переключении и об удалении с чисткой расписаний уже перенесены в тесты нового экрана планами 03-01 и 03-05. Перед удалением сверить список утверждений файла с тестами нового экрана и дописать недостающие в тесты экрана, а не оставлять их сиротами. Список сверки записать в SUMMARY.

<!-- planner-discipline-allow: /api/groups -->
  </action>
  <verify>
    <automated>test ! -f app/routes/groups.py</automated>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - файла `app/routes/groups.py` не существует
    - `grep -c 'routes.groups' app/main.py` возвращает 0
    - `grep -rc '/api/groups' app/ tests/ | grep -v ':0' | wc -l` возвращает 0 — обращений к удалённой поверхности не осталось нигде
    - файла `tests/test_routes/test_groups.py` не существует
    - `uv run pytest tests/ -q` завершается кодом 0
    - приложение поднимается: импорт `app.main` не падает
    - SUMMARY содержит сверку утверждений удалённого тестового файла с тестами нового экрана и явную запись о риске внешнего клиента
  </acceptance_criteria>
  <done>JSON-поверхность групп удалена целиком вместе с дырой проверки владения аккаунтом; ни одна пользовательская возможность не потеряна, суита зелена, риск записан явно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| внешний клиент → JSON-маршруты групп | поверхность, у которой в репозитории нет потребителей, но которая доступна любому обладателю токена |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-29 | Elevation of Privilege | вход создания группы в `app/routes/groups.py` | high | mitigate | Вход принимал идентификатор аккаунта и не проверял владение им — дыра того же класса, что закрывалась в Фазе 2. Митигация — удаление входа целиком (D-14), а не добавление проверки: у входа нет потребителя, и наименьшая поверхность надёжнее выровненной |
| T-03-30 | Elevation of Privilege | входы списка, удаления и переключения в том же файле | high | mitigate | Удаляются вместе с создающим: их поведение целиком воспроизведено страничными маршрутами экрана, где проверка владения выполняется на каждом входе |
| T-03-31 | Denial of Service | отсутствие тарифного лимита на группы | low | accept | Лимита на количество групп в системе не существует и точки его применения нет; удаление входа состояние не меняет. Введение лимита — решение вне рамок фазы, факт зафиксирован в SUMMARY |
| T-03-32 | Repudiation | снятие требования GRP-08 | low | mitigate | Причина, дата и автор решения записаны в требованиях, строка прослеживаемости сохранена: через полгода отличимо «отменено» от «потеряно» |

Установок пакетов фаза не содержит — строка `T-{phase}-SC` неприменима.
</threat_model>

## Artifacts this phase produces

- Фикстура-хелпер прямого посева групп в `tests/conftest.py`
- Запись GRP-08 в разделе вне области `.planning/REQUIREMENTS.md` с причиной и датой
- Удаления: файл `app/routes/groups.py`, его импорт и регистрация в `app/main.py`, файл `tests/test_routes/test_groups.py`, тест отсутствия лимита на группы

<verification>
- `uv run pytest tests/ -q` — зелено после каждой из трёх задач
- Roadmap и требования согласованы: ни один документ не обещает ручного добавления группы
- В репозитории не осталось обращений к удалённой JSON-поверхности групп
</verification>

<success_criteria>
- GRP-08 официально вне области milestone v2.0 с записанной причиной, датой и сохранённой прослеживаемостью (D-13)
- Вход ручного создания группы удалён вместе с дырой проверки владения аккаунтом (D-14)
- Суита зелена; тестовый посев групп не зависит от прикладных маршрутов
</success_criteria>

<output>
Create `.planning/phases/03-gruppy-akkaunta/03-07-SUMMARY.md` when done
</output>
