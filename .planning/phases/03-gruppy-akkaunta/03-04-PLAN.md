---
phase: 03-gruppy-akkaunta
plan: 04
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - app/pages/accounts.py
  - app/worker/tasks.py
  - tests/test_routes/test_sync_groups.py
  - tests/test_worker/test_tasks.py
autonomous: true
requirements: [GRP-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Синхронизация всех трёх типов аккаунта идёт через одну функцию переинвентаризации — трёх копий логики синка в коде больше нет (D-10, D-11)"
    - "После синхронизации TG-аккаунта на аккаунте заполнены last_synced_at и last_sync_result, и обработчик возвращает пользователя на экран групп этого аккаунта, а не на глобальный раздел"
    - "После фоновой синхронизации WA или MAX на аккаунте заполнены last_synced_at и last_sync_result, статус переходит в active"
    - "Провал фоновой синхронизации записывает на аккаунт результат с текстом ошибки и переводит статус в sync_failed — пользователю есть что показать"
    - "Повторный запуск синхронизации при статусе syncing отклоняется существующим guard-ом и не ставит второй задачи"
    - "Синхронизация не удаляет групп ни в одном из трёх путей: пропавшая группа получает пометку и остаётся в списке (D-11)"
    - "Синхронизация не переписывает is_active ни в одном из трёх путей (D-11)"
    - "Ранее удалённая пользователем группа возвращается следующим синком как новая, включённая, без связей с расписаниями (D-10)"
    - "Протоколы синхронизации Telegram, WhatsApp и MAX не изменены: состав запросов к мессенджерам и имена очередей прежние"
  prohibitions:
    - "Синхронизация НЕ ДОЛЖНА удалять данные пользователя: группа, не вернувшаяся из мессенджера, помечается, но остаётся в списке; удаление остаётся исключительно решением пользователя"
  artifacts:
    - path: "app/pages/accounts.py"
      provides: "TG-путь синхронизации через общий хелпер и редиректы на экран групп аккаунта"
    - path: "app/worker/tasks.py"
      provides: "Фоновые синхронизации WA и MAX через тот же хелпер, с записью результата"
  key_links:
    - from: "app/pages/accounts.py"
      to: "app/application/accounts/group_resync.py"
      via: "вызов apply_group_resync вместо встроенного блока only-add"
      pattern: "apply_group_resync"
    - from: "app/worker/tasks.py"
      to: "app/application/accounts/group_resync.py"
      via: "оба фоновых пути вызывают тот же хелпер"
      pattern: "apply_group_resync"
---

<objective>
Три копии логики синхронизации сводятся к трём вызовам одной функции, а результат синка впервые сохраняется на аккаунте: TG-путь в страничном обработчике и обе Celery-таски WA/MAX переходят на `apply_group_resync`, редиректы обработчиков ведут на экран групп аккаунта.

Purpose: сейчас блок «добавить только новые» скопирован в трёх местах с посимвольными расхождениями. Реализовать D-10/D-11/D-12 в каждой копии отдельно — гарантированное тихое расхождение WA- и MAX-путей (RESEARCH Pitfall 3). Одновременно закрываются висящие редиректы на сносимый раздел `/groups`.
Output: `app/pages/accounts.py` и `app/worker/tasks.py` без собственной логики состава групп; результат и время синка записываются во всех трёх путях; провал синка записывается текстом ошибки.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-gruppy-akkaunta/03-CONTEXT.md
@.planning/phases/03-gruppy-akkaunta/03-RESEARCH.md
@.planning/phases/03-gruppy-akkaunta/03-PATTERNS.md
@.planning/phases/03-gruppy-akkaunta/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TG-путь синхронизации через общий хелпер и редиректы на новый экран</name>
  <precondition>План 03-02 завершён: `app/application/accounts/group_resync.py` существует и экспортирует `apply_group_resync` и `record_sync_failure`, колонки `last_synced_at`/`last_sync_result`/`missing_since` объявлены в моделях.</precondition>
  <read_first>
    - app/pages/accounts.py (строки 700-820) — обработчик accounts_sync_groups целиком: проверка владения, guard по статусу syncing, три ветки получения групп, встроенный блок only-add, четыре редиректа
    - app/application/accounts/group_resync.py — сигнатуры apply_group_resync и record_sync_failure
    - app/application/accounts/dto.py — поля GroupResyncResult
    - tests/test_routes/test_sync_groups.py — существующие ожидания по входу синхронизации
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ (дополняют `tests/test_routes/test_sync_groups.py`) и должны падать до правки:
    - синхронизация TG-аккаунта с ответом из трёх групп создаёт три строки и заполняет `account.last_synced_at`
    - `account.last_sync_result` после успеха разбирается в словарь со счётчиками, `error` равен None
    - существующая группа с изменившимся именем переименовывается, счётчик переименований в результате равен 1
    - существующая группа, отсутствующая в ответе, получает `missing_since` и остаётся в БД
    - выключенная группа после синхронизации остаётся выключенной
    - обработчик отвечает 302 с адресом `/accounts/{account_id}/groups`
    - обработчик при статусе аккаунта `syncing` отвечает 302 и не выполняет запроса к мессенджеру
    - обработчик для чужого `account_id` отвечает 302 и не меняет состояния
    - падение `messenger.get_groups()` записывает результат с текстом ошибки и не роняет обработчик
  </behavior>
  <files>app/pages/accounts.py, tests/test_routes/test_sync_groups.py</files>
  <action>
В обработчике `accounts_sync_groups` удалить встроенный блок состава групп (выборка существующих внешних идентификаторов, множество увиденных, добавление новых) и заменить его вызовом `apply_group_resync(db, account, fetched_groups, messenger_type=messenger_type)` с последующим `await db.commit()`. Транзакцией по-прежнему управляет обработчик — хелпер `commit` не делает.

Получение групп от мессенджера обернуть так, чтобы отказ внешней системы записывался, а не терялся: при исключении вызвать `record_sync_failure(db, account, <текст ошибки>)`, сделать `commit` и вернуть тот же редирект. Протоколы обращения к Telethon, WhatsApp и MAX при этом НЕ меняются: состав и порядок вызовов `messenger.get_groups()` в трёх ветках остаётся прежним — жёсткая рамка milestone.

Перенаправления. Все четыре `RedirectResponse` этого файла, ведущие сегодня на глобальный раздел (ветка «аккаунт не найден или неподходящего типа», ветка guard-а по статусу `syncing`, ветка повторного запуска и финальный редирект успеха), должны вести на `/accounts/{account_id}/groups` — экран того аккаунта, с которым работал пользователь. Единственное исключение — ветка, где `account_id` не удалось разрешить в собственный аккаунт пользователя: она ведёт на `/accounts`, потому что адрес несуществующего экрана предлагать нечему.

Guard повторного запуска (`account.status == "syncing"`) сохраняется без изменений — он же закрывает гонку двойного запуска синхронизации.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes/test_sync_groups.py -x -q</automated>
    <automated>uv run pytest tests/test_routes/ tests/test_pages/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'apply_group_resync' app/pages/accounts.py`
    - `grep -v '^ *#' app/pages/accounts.py | grep -c 'RedirectResponse(url="/groups"' ` возвращает 0 — редиректов на сносимый раздел не осталось
    - `grep -c 'accounts/{account_id}/groups\|/accounts/" *+\|f"/accounts/{account_id}/groups"' app/pages/accounts.py` возвращает не менее 1
    - синхронизация TG-аккаунта заполняет `account.last_synced_at` и `account.last_sync_result`
    - переименованная в мессенджере группа получает новое `name`, счётчик переименований равен 1
    - пропавшая группа получает `missing_since` и остаётся в БД
    - запрос при статусе `syncing` отвечает 302 и не обращается к мессенджеру
    - `uv run pytest tests/test_routes/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Синхронный TG-синк выполняет полную переинвентаризацию через общий хелпер, записывает результат на аккаунт и возвращает пользователя на экран групп именно этого аккаунта.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Фоновые синхронизации WA и MAX через тот же хелпер</name>
  <read_first>
    - app/worker/tasks.py (строки 245-340) — _sync_wa_groups_async целиком: опрос моста, guard по статусу, встроенный блок only-add, переходы статуса, ветки failed и timeout
    - app/worker/tasks.py (строки 360-450) — MAX-аналог той же функции
    - app/application/accounts/group_resync.py — apply_group_resync и record_sync_failure
    - tests/test_worker/test_tasks.py — существующие ожидания фоновых задач и стиль моков моста
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ (дополняют `tests/test_worker/test_tasks.py`) и должны падать до правки:
    - фоновая синхронизация WA с ответом из трёх групп создаёт три строки, заполняет `last_synced_at` и `last_sync_result`, переводит статус в `active`
    - фоновая синхронизация MAX с тем же сценарием даёт тот же результат — обе копии ведут себя одинаково
    - существующая группа с изменившимся именем переименовывается в обоих путях
    - существующая группа, отсутствующая в ответе, получает `missing_since` и не удаляется в обоих путях
    - выключенная группа остаётся выключенной в обоих путях
    - ответ моста со статусом отказа переводит аккаунт в `sync_failed` и записывает результат с текстом ошибки
    - исчерпание попыток опроса переводит аккаунт в `sync_failed` и записывает результат с текстом ошибки
    - задача, запущенная для аккаунта не в статусе `syncing`, ничего не меняет (существующий guard)
  </behavior>
  <files>app/worker/tasks.py, tests/test_worker/test_tasks.py</files>
  <action>
Заменить встроенный блок состава групп в обеих фоновых функциях (WA и MAX) вызовом `apply_group_resync(session, account, groups, messenger_type=<"wa" либо "max">)`, оставив существующий `await session.commit()` на месте. Существующий guard «аккаунт не найден или не в статусе `syncing`» и перевод статуса в `active` после успеха сохраняются без изменений.

Ветки отказа — ответ моста со статусом провала, исчерпание попыток опроса и перехват исключения — дополнить вызовом `record_sync_failure(session, account, <текст>)` перед переводом статуса в `sync_failed`: без записанного результата экран не сможет показать пользователю причину провала, а сводка синка окажется пустой при явно неуспешном синке.

Структурное логирование сохранить в существующем стиле: событие завершения синка продолжает печатать общее число групп и число новых, дополнительно — число переименований и число помеченных как не найденные, полями kwargs.

Протоколы опроса моста НЕ трогать: интервал опроса, предельное число попыток, набор распознаваемых состояний ответа и имена Celery-задач остаются прежними — жёсткая рамка milestone. Правка касается ровно того участка, где ответ моста превращается в строки таблицы групп.

Обе функции обязаны получить идентичную обработку — это и есть цель плана. Тесты пишутся параметризованно либо парой симметричных сценариев так, чтобы расхождение WA- и MAX-путей падало тестом, а не обнаруживалось в проде.
  </action>
  <verify>
    <automated>uv run pytest tests/test_worker/test_tasks.py -x -q</automated>
    <automated>uv run pytest tests/test_worker/ tests/test_worker_tasks.py tests/test_application/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'apply_group_resync' app/worker/tasks.py` возвращает не менее 2 — обе фоновые функции переведены на хелпер
    - `grep -c 'record_sync_failure' app/worker/tasks.py` возвращает не менее 2
    - `grep -v '^ *#' app/worker/tasks.py | grep -c 'existing_ids = '` возвращает 0 — встроенного блока состава групп не осталось
    - фоновая синхронизация WA и фоновая синхронизация MAX на одинаковом сценарии дают одинаковые счётчики и одинаковое состояние строк
    - ответ моста со статусом отказа оставляет `sync_failed` и непустой текст ошибки в `last_sync_result`
    - выключенная группа после фоновой синхронизации остаётся выключенной
    - `uv run pytest tests/test_worker/ tests/test_worker_tasks.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Оба фоновых пути выполняют ту же переинвентаризацию, что и синхронный, записывают результат и время синка на аккаунт, а провал синка перестаёт быть безмолвным.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| мессенджер/мост → Celery-таска → БД | ответ внешней системы напрямую формирует состав строк таблицы групп |
| браузер → `POST /accounts/{id}/sync-groups` | `account_id` из URL, запуск дорогой операции |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Elevation of Privilege | `accounts_sync_groups` | high | mitigate | Существующая проверка владения `MessengerAccount.id == account_id AND user_id == user.id` сохраняется без изменений; `apply_group_resync` дополнительно скоупит выборку по `account.user_id`. Тест «чужой account_id не запускает синк» обязателен |
| T-03-15 | Denial of Service | повторный запуск синхронизации | medium | mitigate | Существующий guard по статусу `syncing` сохраняется и покрывается тестом: второй запрос не ставит второй Celery-задачи и не обращается к мессенджеру |
| T-03-16 | Tampering | ответ моста как источник состава групп | medium | mitigate | Хелпер не удаляет строк и не меняет `is_active`, поэтому враждебный или сломанный ответ моста способен добавить и переименовать строки, но не уничтожить пользовательские данные и не включить выключенное |
| T-03-17 | Information Disclosure | текст ошибки в `last_sync_result` | medium | mitigate | В результат пишется текст сообщения об ошибке, который затем рендерится пользователю под autoescape; секреты подключения в это поле не попадают — записывается сообщение исключения, не строка подключения |
| T-03-18 | Repudiation | расхождение WA- и MAX-путей | low | mitigate | Симметричные тесты обоих путей на одинаковом сценарии: расхождение падает тестом |

Установок пакетов фаза не содержит — строка `T-{phase}-SC` неприменима.
</threat_model>

## Artifacts this phase produces

- Вызовы `apply_group_resync` и `record_sync_failure` в `app/pages/accounts.py` и в обеих фоновых функциях `app/worker/tasks.py`
- Новый адрес перенаправления обработчиков синхронизации — `/accounts/{account_id}/groups`

<verification>
- `uv run pytest tests/test_routes/test_sync_groups.py tests/test_worker/test_tasks.py -q` — зелено
- `uv run pytest tests/ -q` — суита не деградировала
- В `app/pages/accounts.py` и `app/worker/tasks.py` не осталось собственной логики состава групп
</verification>

<success_criteria>
- GRP-07 (серверная половина): повторная синхронизация выполняет полную переинвентаризацию и сохраняет результат для всех трёх типов аккаунта
- D-09, D-10, D-11, D-12 реализованы во всех трёх путях синхронизации
- Протоколы отправки и синхронизации мессенджеров не изменены
</success_criteria>

<output>
Create `.planning/phases/03-gruppy-akkaunta/03-04-SUMMARY.md` when done
</output>
