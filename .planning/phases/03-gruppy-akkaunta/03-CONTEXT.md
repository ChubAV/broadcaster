# Phase 3: Группы аккаунта - Context

**Gathered:** 2026-08-11
**Status:** Ready for planning

<domain>
## Phase Boundary

Фаза 3 переносит управление группами с глобального раздела на экран конкретного messenger-аккаунта: `/accounts/{id}/groups` показывает только группы этого аккаунта с включением/отключением, удалением и повторной синхронизацией с видимым результатом. Глобальный раздел `/groups` и пункт «Группы» в навигации сносятся. Выключенная группа впервые получает реальный эффект — пропуск при рассылке.

Покрывает GRP-04, GRP-05, GRP-06, GRP-07. **GRP-08 (ручное добавление группы) снято решением владельца** — см. D-13; ROADMAP.md (критерий 3 фазы) и REQUIREMENTS.md требуют правки.

**Правило границы.** Протоколы синхронизации и отправки Telegram/WhatsApp/MAX не меняются — фаза меняет экран, операции над списком и одно условие в диспетчеризации (D-05). Дашборд, история, тарифы, админка — Фазы 4–6.

**Единственное отступление от «интерфейс, а не функция»:** диспетчер отправки получает новое условие — группы с `is_active=false` пропускаются (D-05). Это правка рядом с боевым пайплайном, аналогичная пропуску черновиков в Фазе 2 (D-01), и она обязательна: без неё надпись макета «выключенные группы пропускаются при рассылке» врёт пользователю.

</domain>

<decisions>
## Implementation Decisions

### Судьба раздела «Группы»

- **D-01:** Глобальный раздел `/groups` сносится целиком: страницы, паршалы, bulk-операции, фильтры и пункт «Группы» в навигации. `/groups` отвечает редиректом на «Аккаунты». Закрывает отложенное решение Фазы 1 (D-11: пункт был оставлен временно именно до этой фазы). — **Reversibility:** costly — снос шаблонов, роутов и их тестов; возврат означает восстановление раздела заново.
- **D-02:** Экран групп аккаунта живёт на `/accounts/{id}/groups`, `active_page=accounts`, вход — с экрана «Аккаунты» (клик по аккаунту).
- **D-03:** Состав экрана — макет плюс строка поиска по имени (у TG-аккаунта могут быть сотни чатов). Фильтры по мессенджеру/активности, статистика отправок по группе и bulk-операции старого раздела не переносятся.
- **D-04:** Длинные списки — бесконечная прокрутка сентинел-паттерном Фазы 1 (по 30 за запрос). Счётчик «N активных из M групп» считается отдельным запросом, а не по загруженной странице.

### Семантика «отключено»

- **D-05:** Выключенная группа **пропускается при отправке**: диспетчеризация не ставит задачи отправки для групп с `is_active=false`, даже если группа уже входит в расписания. Тумблер обратим — состав расписаний не меняется, включение возобновляет рассылку. — **Reversibility:** costly — условие уходит в боевой пайплайн диспетчеризации, откат означает повторную проверку тестов диспетчеризации.
- **D-06:** Пропуск тихий: записей в `SendLog` не создаётся, след — только structlog. История отражает реальные попытки отправки; новый статус журнала не вводится.
- **D-07:** В карточке расписания в редакторе объявления выключенные группы **видны только если уже выбраны в этом расписании** — с пометкой «отключена» (чекбокс недоступен для новых). Пользователь видит, почему группа из расписания не получает рассылку; список выбора не захламляется.
- **D-08:** Тумблер срабатывает мгновенно, без панели подтверждения — действие полностью обратимо. Показ «в N расписаниях» в подписи группы — на усмотрение исполнителя.

### Синхронизация и её результат

- **D-09:** Результат синка — сводка-плашка на экране, не покидая его: «найдено N, новых M, обновлено имён K» или текст ошибки. Для WA/MAX (фоновый синк через Celery) статус добирается самоостанавливающимся опросом — паттерн экрана аккаунтов из Фазы 1 (план 01-06). Результат последнего синка хранится на аккаунте и виден при перезаходе.
- **D-10:** Синк — полная переинвентаризация: **ручно удалённые группы возвращаются** при следующем синке (как новые, включённые, без старых связей с расписаниями). Так говорит пустое состояние макета («запустите синхронизацию, чтобы подтянуть чаты заново»). Если группа не нужна — её выключают, а не удаляют. Томбстоуны не вводятся.
- **D-11:** Синк обновляет имена существующих групп. Группы, которые мессенджер больше не вернул, помечаются («не найдена при синке»), но **не удаляются автоматически** — удаление остаётся решением пользователя. `is_active` синком не трогается.
- **D-12:** Миграция схемы: `MessengerAccount` получает `last_synced_at` и результат последнего синка (счётчики/ошибка — точная форма за планировщиком). Шапка «последняя синхронизация N назад» из макета становится честной. **Per-group кнопка синка ↻ из макета НЕ делается** — протокола синхронизации одной группы у воркеров не существует, а протоколы не трогаем (прецедент: Фаза 2 D-17 выкинула выдуманный прогресс-бар макета). Остаётся одна кнопка «Синхронизировать всё».

### Ручное добавление — отменено

- **D-13:** Ручного добавления групп **не будет**: GRP-08 снято решением владельца (2026-08-11, при обсуждении фазы). CTA «Добавить группу» из макета (строка ~1491) не реализуется. Требуется согласованная правка: ROADMAP.md — цель и критерий 3 фазы (убрать «добавить группу аккаунта вручную»), REQUIREMENTS.md — GRP-08 в Out of Scope с причиной. — **Reversibility:** reversible — решение о невключении; возврат возможен отдельной фазой.
- **D-14:** Вместе с отменой GRP-08 удаляется `POST /api/groups` — единственный вход ручного создания группы: UI-потребителя у него нет, а владение `account_id` он не проверяет (дыра класса CR-01 Фазы 2 закрывается удалением входа). Судьба остальных JSON-маршрутов групп (`GET`, `DELETE`, `PATCH toggle`) — за планировщиком: проверить потребителей и либо оставить с выравниванием поведения, либо снести как мёртвые.

### Claude's Discretion

- Точная форма сводки синка (плашка/тост, состав счётчиков) и форма пометки «не найдена при синке» (бейдж, цвет).
- Подпись строки группы: в макете «N участников», но источника данных в модели нет — заменить имеющимися данными или опустить.
- Показ «в N расписаниях» в подписи группы (D-08).
- Взаимодействие поиска с бесконечной прокруткой (по образцу существующих списочных страниц).
- Мобильная раскладка экрана в пределах брейкпоинтов макета (860/900/1080px); адаптивность — критерий приёмки фазы.
- Судьба `GET/DELETE/PATCH /api/groups` (см. D-14).
- Куда именно встраивается пропуск выключенных групп: выборка при диспетчеризации или фильтр при постановке задач — что даст более простой и тестируемый срез.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Дизайн — источник истины
- `new_broadcaster_design.html` — канонический источник дизайна milestone v2.0; читать **распакованную копию**, а не грепать оригинал.
- `design/new_broadcaster_design.unpacked.html` — распакованная рабочая копия. Для этой фазы значимы: **экран аккаунта** — строки ~906–964 (шапка аккаунта со статусом и «последняя синхронизация», линейка «N активных из M · ВЫКЛЮЧЕННЫЕ ГРУППЫ ПРОПУСКАЮТСЯ ПРИ РАССЫЛКЕ», строки групп с тумблером/удалением, пустое состояние «Все группы удалены»); **state-логика экрана** — строки ~1822–1857 (`acctGroups`: syncAll, спиннеры, подписи); **заголовок экрана и CTA** — строка ~1491 («Группы аккаунта: включение, удаление и повторная синхронизация»; CTA «Добавить группу» НЕ реализуется по D-13). Per-group кнопка ↻ (строка ~948) НЕ реализуется по D-12.
- `design/README.md`, `design/unpack.js` — как пересобрать распакованную копию при необходимости.

### Планирование milestone
- `.planning/ROADMAP.md` §«Phase 3: Группы аккаунта» — цель и 5 критериев успеха (**критерий 3 требует правки по D-13**); §«Жёсткие рамки milestone» — протоколы отправки не трогаются, адаптивность сквозная.
- `.planning/REQUIREMENTS.md` §«Аккаунты и группы» — GRP-04…GRP-07 (**GRP-08 снимается по D-13**).
- `.planning/PROJECT.md` §Constraints, §Key Decisions — стек зафиксирован, серверный Jinja2 + точечный HTMX/Alpine.

### Решения прошлых фаз, действующие здесь
- `.planning/phases/01-interfeysnyy-fundament/01-CONTEXT.md` — D-10 (тёмная тема), D-11 (пункт «Группы» временный — эта фаза его убирает), D-13 (компоненты-макросы), D-15 (одна разметка `data-row`), D-09/D-19 (контекст-провайдер шелла: счётчик групп у пункта меню уходит вместе с пунктом).
- `.planning/phases/01-interfeysnyy-fundament/01-04-SUMMARY.md` — миграция раздела «Группы» в Фазе 1: макрос фильтров, тумблер прежним запросом.
- `.planning/phases/01-interfeysnyy-fundament/01-06-SUMMARY.md` — экран «Аккаунты»: якоря подмены и **самоостанавливающийся опрос** — готовый паттерн для статуса синка (D-09).
- `.planning/phases/02-obyavleniya-i-raspisaniya/02-CONTEXT.md` — D-01 (прецедент нового условия в диспетчере — пропуск черновиков), D-09 (базовый путь без JS), правило «клиентским данным не верят: владение на каждом входе».

### Карты кодовой базы
- `.planning/codebase/STRUCTURE.md` — расположение `app/pages/`, `app/routes/`, `app/templates/`, правило добавления страниц.
- `.planning/codebase/CONVENTIONS.md`, `.planning/codebase/TESTING.md` — соглашения и устройство суиты (SQLite in-memory, фикстуры `client`/`db_session`/`auth_headers`).

Внешних ADR/спеков по группам в репозитории нет — всё остальное зафиксировано в решениях выше.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `app/pages/groups.py` — текущий глобальный раздел: `groups_toggle`/`groups_delete` (POST-формы с редиректом, удаление уже вычищает `group_ids` расписаний через `ScheduleRepository.remove_group_ids`) — логика переиспользуется экраном аккаунта; список/паршал/bulk сносятся (D-01, D-03).
- `POST /accounts/{account_id}/sync-groups` (`app/pages/accounts.py:737`) — существующий вход повторной синхронизации всех трёх типов: TG — синхронно через Telethon `get_dialogs`, WA/MAX — перевод в `syncing` + Celery-таски `sync_wa_groups`/`sync_max_groups`. Точка расширения для результата синка (D-09).
- `app/pages/accounts.py:661–696` — `sync-status` паршал с самоостанавливающимся опросом и `retry-sync` — паттерн для живого статуса синка на новом экране.
- `app/worker/tasks.py` `_sync_wa_groups_async` (~250–316) и MAX-аналог — место, где only-add меняется на «имена + пометка пропавших» и пишется результат синка (D-11, D-09).
- `app/models/group.py` — `is_active`, `last_error`/`error_at` уже есть; `app/models/messenger_account.py` — колонок `last_synced_at`/результата синка нет (миграция, D-12).
- Компоненты Фазы 1: сентинел бесконечной прокрутки (01-03), макрос фильтров/поиска (01-04), тумблер, панель подтверждения удаления (макетный мгновенный «×» заменяется панелью — правило Фазы 1).

### Established Patterns
- Списочные страницы: `layout=cards` через query-параметр, одна разметка `data-row`, HTMX `hx-get` с неявной целью.
- Базовый путь без JS: toggle/delete — настоящие формы POST; HTMX/Alpine — прогрессивное улучшение (тесты `*_degrades_without_alpine`).
- Владение проверяется на каждом входе (страничном и JSON) — правило Фазы 2.
- Тесты: SQLite in-memory, полная схема на тест; текущая суита (895) обязана остаться зелёной.

### Integration Points
- **`app/application/scheduling/use_cases.py` (~строка 65) / `app/worker/tasks.py`** — сюда встраивается пропуск `is_active=false` групп (D-05). Самое чувствительное место фазы: рядом боевая диспетчеризация.
- **Миграция Alembic** — `MessengerAccount.last_synced_at` + результат синка (D-12). ⚠ Целевая база всё ещё на ревизии `0012` (blocker из STATE.md): новая ревизия встаёт в очередь за невыкаченной `0013`.
- **`app/pages/ads.py:220`** — выборка групп для редактора (`is_active == True`): меняется под D-07 (выбранные-но-выключенные видны с пометкой).
- **Навигация/шелл** — пункт «Группы» и его счётчик в контекст-провайдере шелла удаляются (D-01).
- **`app/routes/groups.py`** — `POST /api/groups` удаляется (D-14); остальные маршруты — проверить потребителей.
- **Тесты старого раздела `/groups`** — переезжают на новый экран или удаляются вместе с разделом.

</code_context>

<specifics>
## Specific Ideas

- Шапка экрана — карточка аккаунта из макета: иконка мессенджера, имя, «{телефон} · последняя синхронизация {N} назад», бейдж статуса, справа «Синхронизировать всё» (спиннер ↻ во время синка) и «К аккаунтам».
- Линейка-разделитель над списком: слева «N активных из M групп», справа «ВЫКЛЮЧЕННЫЕ ГРУППЫ ПРОПУСКАЮТСЯ ПРИ РАССЫЛКЕ» — с D-05 эта надпись впервые правдива.
- Строка группы: аватар-инициалы в цвете мессенджера, имя, mono-подпись, статус синка, кнопка удаления «×» (через панель подтверждения), тумблер; выключенная строка — приглушённая (`opacity .55` в макете).
- Пустое состояние: «Все группы удалены — запустите синхронизацию, чтобы подтянуть чаты заново» (макет, строки 956–961) — согласуется с D-10.

</specifics>

<deferred>
## Deferred Ideas

- **Статистика отправок по группе** (попытки/успехи/последняя отправка со старого /groups) — уходит вместе с разделом; возвращать имеет смысл после Фазы 4, где появляются агрегации по `SendLog`.
- **Bulk-операции над группами** (массовое отключение/удаление) — не переносятся; при реальной потребности — отдельная задача.
- **Синк одной группы** — требует расширения протоколов воркеров; вне v2.0.
- **Ручное добавление группы (GRP-08)** — отменено владельцем, не отложено: снять с roadmap (D-13). Если потребность вернётся — отдельная фаза с собственным обсуждением форматов ID и валидации.

</deferred>

---

*Phase: 3-Группы аккаунта*
*Context gathered: 2026-08-11*
