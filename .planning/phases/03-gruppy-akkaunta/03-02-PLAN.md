---
phase: 03-gruppy-akkaunta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/messenger_account.py
  - app/models/group.py
  - alembic/versions/0014_sync_result_and_group_missing.py
  - app/application/accounts/group_resync.py
  - app/application/accounts/dto.py
  - tests/test_models/test_sync_result_columns.py
  - tests/test_application/test_group_resync.py
autonomous: true
requirements: [GRP-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Результат последней синхронизации хранится на аккаунте и переживает перезаход: MessengerAccount получает last_synced_at и last_sync_result (D-12)"
    - "Группа, не вернувшаяся из мессенджера, помечается временем пропажи в groups.missing_since и НЕ удаляется автоматически (D-11)"
    - "Синхронизация не трогает is_active: флаг группы после переинвентаризации равен значению до неё (D-11)"
    - "Синхронизация — полная переинвентаризация: ранее удалённая вручную группа возвращается как новая, включённая, без старых связей с расписаниями (D-10)"
    - "Хелпер переинвентаризации возвращает счётчики found/new/renamed/missing, по которым строится сводка синка (D-09)"
    - "Ревизия 0014 применима поверх 0013 одним прыжком и симметрично откатывается: колонки только additive nullable, data-migration нет"
    - "Код переживает last_synced_at IS NULL — состояние «синхронизация ещё не выполнялась» отличимо от «синхронизация дала ноль групп»"
    - "Повторный вызов хелпера с тем же ответом мессенджера не создаёт дублей: новых групп 0, переименований 0, помеченных 0"
    - "Пустой ответ мессенджера помечает ВСЕ существующие группы аккаунта как не найденные и не удаляет ни одной"
    - "Группа, вернувшаяся после пропажи, теряет пометку: missing_since снимается в None"
    - "Испорченная JSON-строка в last_sync_result читается защищённо и не приводит к исключению при рендере"
  prohibitions:
    - "Синхронизация НЕ ДОЛЖНА удалять данные пользователя: группа, не вернувшаяся из мессенджера, помечается, но остаётся в списке; удаление остаётся исключительно решением пользователя"
  artifacts:
    - path: "alembic/versions/0014_sync_result_and_group_missing.py"
      provides: "Ревизия схемы D-11/D-12: last_synced_at, last_sync_result, missing_since"
      contains: "down_revision"
      min_lines: 25
    - path: "app/application/accounts/group_resync.py"
      provides: "Единый хелпер переинвентаризации групп аккаунта для трёх мест вызова"
      min_lines: 60
    - path: "tests/test_application/test_group_resync.py"
      provides: "Тесты D-10/D-11/D-12 на хелпере переинвентаризации"
      min_lines: 80
  key_links:
    - from: "alembic/versions/0014_sync_result_and_group_missing.py"
      to: "app/models/messenger_account.py"
      via: "колонки ревизии обязаны совпадать с ORM-объявлениями"
      pattern: "last_sync(ed_at|_result)"
    - from: "app/application/accounts/group_resync.py"
      to: "app/models/group.py"
      via: "запись missing_since и обновление name"
      pattern: "missing_since"
---

<objective>
Схема и логика результата синхронизации: `MessengerAccount` получает время последнего синка и его результат, `Group` — пометку «не найдена при синке», а трижды скопированный блок only-add заменяется одним хелпером полной переинвентаризации.

Purpose: без места хранения результата шапка «последняя синхронизация N назад» и сводка «найдено N, новых M, обновлено имён K» врут (D-09, D-12), а без общего хелпера расширение логики синка втрое в трёх копиях гарантирует тихое расхождение WA- и MAX-путей (RESEARCH Pitfall 3). Этот план даёт обеим задачам одну точку истины ДО того, как её начнут вызывать три места (план 03-04) и рисовать экран (план 03-06).
Output: колонки `messenger_accounts.last_synced_at`, `messenger_accounts.last_sync_result`, `groups.missing_since`; ревизия `0014`, проверенная применением на одноразовой базе; модуль `app/application/accounts/group_resync.py` с DTO результата.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-gruppy-akkaunta/03-CONTEXT.md
@.planning/phases/03-gruppy-akkaunta/03-RESEARCH.md
@.planning/phases/03-gruppy-akkaunta/03-PATTERNS.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-12-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Колонки результата синка и ревизия 0014</name>
  <reversibility rating="costly">Форма хранения результата (`Text` с JSON-строкой против отдельных int-колонок) наследуется планами 03-04 и 03-06 и живёт в схеме боевой базы: смена формы после выката потребует новой ревизии и переписывания обоих потребителей.</reversibility>
  <read_first>
    - app/models/group.py — форма nullable-колонок с tz-aware datetime; last_error/error_at здесь про ошибки ОТПРАВКИ и под D-11 не переиспользуются
    - app/models/messenger_account.py — текущий состав колонок; ORM-relationship'ов модель не объявляет
    - alembic/versions/0013_ad_status.py — шапка ревизии, плоские revision/down_revision, симметричный downgrade
    - alembic/env.py — DATABASE_URL перекрывает sqlalchemy.url; миграции идут async-движком
    - .planning/STATE.md — блокер: целевая база стоит на ревизии 0012, ревизия 0013 не выкачена
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ в `tests/test_models/test_sync_result_columns.py` и должны падать до правки моделей:
    - только что созданный `MessengerAccount` имеет `last_synced_at is None` и `last_sync_result is None`
    - только что созданная `Group` имеет `missing_since is None`
    - `last_synced_at` принимает и возвращает tz-aware datetime
    - `last_sync_result` принимает произвольную строку длиной 1000+ символов (Text, не String)
    - `missing_since` принимает datetime и снимается обратно в None
  </behavior>
  <files>app/models/messenger_account.py, app/models/group.py, alembic/versions/0014_sync_result_and_group_missing.py, tests/test_models/test_sync_result_columns.py</files>
  <action>
Добавить в `app/models/messenger_account.py` две колонки ровно той формой, что уже используется в `app/models/group.py`:
`last_synced_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)` и
`last_sync_result: Mapped[str | None] = mapped_column(Text, nullable=True)`.
Импорт `DateTime`, `Text` в файле уже есть.

Добавить в `app/models/group.py` колонку
`missing_since: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)`
с комментарием: NULL означает «найдена при последней синхронизации»; колонка ставится и снимается синхронизацией (D-11), а `last_error`/`error_at` рядом относятся к ошибкам ОТПРАВКИ и под эту семантику не переиспользуются.

Форма хранения результата (разрешение дискреции D-12 и допущения A1 из RESEARCH): `last_sync_result` — `Text` с JSON-строкой вида `{"found": N, "new": M, "renamed": K, "missing": J, "error": null}`, где `error` — строка текста ошибки либо null. Причина выбора `Text` вместо `sa.JSON`: тесты идут на SQLite in-memory, прод на PostgreSQL, а `Text` ведёт себя одинаково в обеих СУБД; разбор выполняется на стороне читателя с защитой от мусора. Записать это обоснование комментарием рядом с колонкой.

Создать `alembic/versions/0014_sync_result_and_group_missing.py` по конвенции файла `0013_ad_status.py`: докстринг с обоснованием, плоские `revision = "0014"` и `down_revision = "0013"`. `upgrade()` — три `op.add_column`: `messenger_accounts.last_synced_at` (`sa.DateTime(timezone=True)`, `nullable=True`), `messenger_accounts.last_sync_result` (`sa.Text()`, `nullable=True`), `groups.missing_since` (`sa.DateTime(timezone=True)`, `nullable=True`). `downgrade()` — три симметричных `op.drop_column`.

Жёсткое ограничение ревизии (RESEARCH Pitfall 2): только additive nullable-колонки. Ни `server_default`, ни `nullable=False`, ни единой строки data-migration — целевая база стоит на `0012`, и прыжок `0012 → 0014` обязан быть одним безопасным шагом без переписывания существующих строк. Литералы типов выписываются в ревизии, а не импортируются из `app.models`: ревизия описывает схему на СВОЙ момент времени (правило, зафиксированное в шапке `0013`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_models/test_sync_result_columns.py -x -q</automated>
    <automated>uv run alembic heads 2>&1 | grep -E '0014.*\(head\)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'last_synced_at' app/models/messenger_account.py` и `grep -q 'last_sync_result' app/models/messenger_account.py`
    - `grep -q 'missing_since' app/models/group.py`
    - `alembic/versions/0014_sync_result_and_group_missing.py` содержит `revision = "0014"` и `down_revision = "0013"`
    - `grep -c 'op.add_column' alembic/versions/0014_sync_result_and_group_missing.py` возвращает 3
    - `grep -c 'op.drop_column' alembic/versions/0014_sync_result_and_group_missing.py` возвращает 3
    - `grep -v '^#' alembic/versions/0014_sync_result_and_group_missing.py | grep -c 'server_default'` возвращает 0 — ревизия строго additive nullable
    - `uv run alembic heads` печатает `0014` с пометкой `(head)`
    - `uv run pytest tests/test_models/test_sync_result_columns.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Три nullable-колонки объявлены в моделях и в ревизии `0014`; ревизия — единственная голова истории миграций, её downgrade симметричен upgrade.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Применение ревизии 0014 на одноразовой базе</name>
  <precondition>Docker доступен исполнителю (`docker version` завершается кодом 0), а порт 127.0.0.1:55432 свободен либо занят ранее созданным одноразовым контейнером; ни один контейнер работающего стека не останавливается и не перенастраивается.</precondition>
  <read_first>
    - .planning/phases/02-obyavleniya-i-raspisaniya/02-12-SUMMARY.md — прецедент одноразовой базы: контейнер postgres:18-alpine на 127.0.0.1:55432, программный guard по hostname/port/dbname, порядок проверки
    - .planning/STATE.md — блокер: целевая база на ревизии 0012, выкат остаётся решением владельца
    - alembic/env.py — переопределение адреса через переменную окружения DATABASE_URL
    - alembic/versions/0014_sync_result_and_group_missing.py — ревизия, применяемая в этой задаче
  </read_first>
  <files>alembic/versions/0014_sync_result_and_group_missing.py</files>
  <action>
Ревизия обязана быть ПРИМЕНЕНА к настоящей PostgreSQL до верификации фазы, иначе проверки зеленеют вхолостую: тестовая схема собирается из моделей через `Base.metadata.create_all`, и ошибка в самой ревизии в тестах невидима.

Применение выполняется ТОЛЬКО к одноразовой базе — по прецеденту плана 02-12. Поднять (или переиспользовать) контейнер `postgres:18-alpine`, слушающий исключительно `127.0.0.1:55432`, с отдельной базой и отдельной ролью. Перед командой миграции выполнить программный guard, отказывающийся исполнять её, если хост, порт и имя базы в эффективном `DATABASE_URL` не совпадают с одноразовыми: адрес целевой базы в этой задаче не используется ни в одной команде.

Порядок проверки:
1. `uv run alembic upgrade head` против одноразового `DATABASE_URL` — прогон всей цепочки до `0014`;
2. `uv run alembic current` печатает `0014` с пометкой `(head)`;
3. чтение `information_schema.columns` подтверждает наличие `messenger_accounts.last_synced_at`, `messenger_accounts.last_sync_result`, `groups.missing_since` и что все три nullable;
4. `uv run alembic downgrade 0013` проходит без ошибок и три колонки исчезают — симметрия доказана, а не заявлена;
5. `uv run alembic upgrade head` возвращает базу на `0014`.

Целевая (боевая) база в этой задаче НЕ трогается: она остаётся на ревизии `0012`, выкат `0013` и `0014` — решение владельца, зафиксированное блокером в STATE.md. В SUMMARY записать точные команды, вывод `alembic current` и результат сравнения колонок, чтобы владелец мог принять решение о выкате по фактам, а не по обещанию.

Ни один контейнер работающего стека (`web-broadcaster`, `nginx-broadcaster`, `celery-*`, `flower-broadcaster`, `wa-worker-*`, `max-worker-*`, `certbot-broadcaster`) не останавливается, не перезапускается и не перенастраивается.
  </action>
  <verify>
    <automated>uv run alembic heads 2>&1 | grep -E '0014.*\(head\)'</automated>
    <automated>uv run alembic upgrade 0013:0014 --sql 2>&1 | grep -Ei 'ALTER TABLE .*messenger_accounts.* ADD COLUMN last_synced_at'</automated>
    <automated>uv run alembic downgrade 0014:0013 --sql 2>&1 | grep -Ei 'ALTER TABLE .*groups.* DROP COLUMN missing_since'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` против одноразового адреса завершается кодом 0
    - `uv run alembic current` против одноразового адреса печатает `0014` и `(head)`
    - запрос к `information_schema.columns` одноразовой базы находит все три новые колонки со значением `is_nullable = YES`
    - `uv run alembic downgrade 0013` завершается кодом 0, после него все три колонки в `information_schema.columns` отсутствуют
    - оффлайн-генерация `uv run alembic upgrade 0013:0014 --sql` печатает три инструкции добавления колонок
    - в журнале выполнения нет ни одной команды, адресованной целевой базе или контейнерам работающего стека
    - SUMMARY плана содержит фактический вывод `alembic current` и итог сравнения колонок
  </acceptance_criteria>
  <done>Ревизия `0014` доказано применяется поверх `0013` на настоящей PostgreSQL и доказано откатывается; целевая база остаётся на `0012`, решение о её выкате передано владельцу с фактами на руках.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Хелпер полной переинвентаризации групп аккаунта</name>
  <read_first>
    - app/pages/accounts.py (строки 780-805) — текущий блок only-add TG-пути, который хелпер заменяет
    - app/worker/tasks.py (строки 278-305 и 380-405) — две посимвольно близкие копии того же блока
    - app/application/accounts/use_cases.py (строки 1-60) — стиль модуля application-слоя: from __future__, dataclass, прямые select
    - app/application/accounts/dto.py — где живут AccountInfo и SyncStatusView; сюда добавляется DTO результата
    - app/repositories/group.py — готовые list_by_account и get_external_ids, скоуп по аккаунту и пользователю
    - app/models/group.py — is_active с default=True, новая колонка missing_since
    - tests/test_application/test_collect_due_draft.py — образец теста application-слоя с локальным движком
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ в `tests/test_application/test_group_resync.py` и должны падать до реализации:
    - пустая база групп + три пришедшие группы → created=3, renamed=0, missing=0, found=3; все три `is_active=True`
    - существующая группа с тем же external_id и ДРУГИМ именем → renamed=1, created=0; `name` обновлён
    - существующая группа с тем же external_id и тем же именем → renamed=0, created=0
    - существующая группа, которой нет в ответе мессенджера → missing=1, строка НЕ удалена, `missing_since` заполнено
    - группа с ранее заполненным `missing_since`, вернувшаяся в ответе → `missing_since` снова None
    - выключенная группа (`is_active=False`), пришедшая в ответе → `is_active` остаётся False (синк не трогает флаг)
    - выключенная группа, НЕ пришедшая в ответе → `is_active` остаётся False, ставится `missing_since`
    - повторный вызов с тем же ответом → created=0, renamed=0, missing=0 (идемпотентность)
    - пустой ответ мессенджера при трёх существующих группах → missing=3, ни одна строка не удалена
    - дубли одного external_id в ответе мессенджера → создаётся ровно одна строка
    - группа, ранее удалённая пользователем, приходит в ответе → создаётся заново как новая, включённая, без связей с расписаниями (D-10)
    - после вызова `account.last_synced_at` заполнено, `account.last_sync_result` — разбираемая JSON-строка с ключами found/new/renamed/missing/error
  </behavior>
  <files>app/application/accounts/group_resync.py, app/application/accounts/dto.py, tests/test_application/test_group_resync.py</files>
  <action>
Создать `app/application/accounts/group_resync.py` — единственную реализацию логики синхронизации состава групп, которую вызовут все три места (TG-обработчик страницы и две Celery-таски; сами вызовы — план 03-04).

DTO результата положить в `app/application/accounts/dto.py` рядом с `AccountInfo`/`SyncStatusView`: `@dataclass(slots=True) class GroupResyncResult` с полями `found: int`, `created: int`, `renamed: int`, `missing: int`, `error: str | None = None`.

Публичная функция: `async def apply_group_resync(session, account, fetched, *, messenger_type) -> GroupResyncResult`, где `fetched` — последовательность словарей вида `{"id": ..., "name": ...}` в точности того формата, что уже отдают `messenger.get_groups()` и поле `groups` ответа `get_sync_status()`.

Алгоритм (D-10, D-11):
1. Загрузить существующие группы аккаунта в отображение по `group_external_id` — выборкой с двойным WHERE `Group.account_id == account.id` и `Group.user_id == account.user_id`.
2. Для каждой пришедшей записи с ранее не встреченным `id`: если группы нет — создать `Group(user_id=account.user_id, account_id=account.id, messenger_type=messenger_type, group_external_id=..., name=... or id)` и увеличить `created`; если есть — при отличии имени присвоить новое и увеличить `renamed`, а `missing_since` снять в `None`. Дубликаты `id` внутри одного ответа схлопываются множеством увиденных.
3. Для существующих групп, отсутствующих в ответе: проставить `missing_since` текущим UTC-временем, если оно ещё не стоит, и увеличить `missing`. Строка НЕ удаляется — удаление остаётся решением пользователя (D-11).
4. `is_active` не читается и не пишется ни в одной ветке — пользовательский выбор синхронизацией не перетирается (D-11). Ранее удалённая пользователем группа приходит по ветке «новая»: включённая, без связей с расписаниями (D-10) — томбстоунов нет.
5. Записать на аккаунт: `account.last_synced_at` — текущее UTC-время, `account.last_sync_result` — JSON-строка `{"found": ..., "new": ..., "renamed": ..., "missing": ..., "error": null}`. Ключ счётчика новых групп в JSON называется `new` (так его читает сводка UI-SPEC), поле DTO — `created`, потому что `new` в Python занято смыслом иначе.
6. Вернуть `GroupResyncResult`. `commit` хелпер НЕ делает: транзакцией управляет вызывающий (страничный обработчик и Celery-таска ведут её по-разному).

Отдельная функция `async def record_sync_failure(session, account, message: str) -> None`: пишет `account.last_synced_at` и `last_sync_result` с `error` равным тексту сообщения и нулевыми счётчиками — этой формой планы 03-04 и 03-06 показывают ошибку синка вместо сводки.

Функция чтения `def parse_sync_result(raw: str | None) -> dict | None`: разбирает строку с защитой от мусора — на пустом значении, невалидном JSON или несловарном верхнем уровне возвращает `None`, а не поднимает исключение (плашка результата обязана деградировать в отсутствие плашки, а не в стек-трейс).

Комментарий в шапке модуля обязан объяснить, почему хелпер один: до этой фазы блок синка был скопирован в трёх местах с посимвольными расхождениями, и расширение логики втрое в трёх копиях гарантировало тихое расхождение WA- и MAX-путей.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_group_resync.py -x -q</automated>
    <automated>uv run pytest tests/test_application/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/application/accounts/group_resync.py` содержит `async def apply_group_resync(`, `async def record_sync_failure(` и `def parse_sync_result(`
    - `app/application/accounts/dto.py` содержит `class GroupResyncResult`
    - `grep -v '^#' app/application/accounts/group_resync.py | grep -c 'session.delete'` возвращает 0 — хелпер не удаляет строк
    - `grep -v '^#' app/application/accounts/group_resync.py | grep -c 'is_active'` возвращает 0 — синк не трогает пользовательский флаг
    - вызов с тремя новыми группами возвращает `created=3, renamed=0, missing=0, found=3`
    - вызов с пустым `fetched` при трёх существующих группах возвращает `missing=3` и оставляет три строки в БД
    - повторный вызов с тем же `fetched` возвращает `created=0, renamed=0, missing=0`
    - после вызова `json.loads(account.last_sync_result)` даёт словарь с ключами `found`, `new`, `renamed`, `missing`, `error`
    - `parse_sync_result("{не json")` возвращает `None` и не поднимает исключения
    - `uv run pytest tests/test_application/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Существует одна функция переинвентаризации, покрытая тестами на все пять веток (новая, переименованная, неизменившаяся, пропавшая, вернувшаяся), не удаляющая данных и не трогающая `is_active`; результат и время синка записываются на аккаунт в разбираемой форме.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| мессенджер → хелпер переинвентаризации | `fetched` целиком приходит из внешней системы: и `id`, и `name` недоверенные |
| хелпер → БД | записываются строки, которые затем читает и рендерит вся фаза |
| одноразовая база ↔ целевая база | команды миграции не должны перепутать адреса |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | `apply_group_resync` — выборка существующих групп | high | mitigate | Выборка и запись скоупятся двойным условием `Group.account_id == account.id AND Group.user_id == account.user_id`; `account` передаётся вызывающим уже после проверки владения. Тест «чужая группа с тем же external_id не обновляется» обязателен |
| T-03-07 | Denial of Service | ответ мессенджера произвольного размера | medium | accept | Ограничения на длину ответа фаза не вводит: протоколы синхронизации трогать запрещено рамками milestone. Существующее ограничение — таймаут опроса воркера (40 попыток по 15 с) |
| T-03-08 | Tampering | `last_sync_result` как JSON-строка | medium | mitigate | Строка пишется только кодом (никогда не приходит от клиента), а читается через `parse_sync_result` с защитой от мусора: невалидное значение даёт `None`, а не исключение и не подстановку в разметку |
| T-03-09 | Destruction of data | ревизия `0014` | high | mitigate | Только additive nullable-колонки, без `server_default` и без data-migration; применение доказывается на одноразовой базе с программным guard по hostname/port/dbname, целевая база не адресуется ни одной командой |
| T-03-10 | Information Disclosure | имя группы во внешней системе | low | accept | Имена групп уже хранятся в БД с первой версии продукта; фаза не расширяет их видимость |

Установок пакетов фаза не содержит — строка `T-{phase}-SC` неприменима.
</threat_model>

## Artifacts this phase produces

- Колонки `messenger_accounts.last_synced_at`, `messenger_accounts.last_sync_result`, `groups.missing_since`
- Файл ревизии `alembic/versions/0014_sync_result_and_group_missing.py`, идентификатор ревизии `0014`
- Файл `app/application/accounts/group_resync.py`; в нём `apply_group_resync`, `record_sync_failure`, `parse_sync_result`
- Класс `GroupResyncResult` в `app/application/accounts/dto.py`
- Файлы тестов `tests/test_models/test_sync_result_columns.py`, `tests/test_application/test_group_resync.py`

## Planner assumptions (flagged, not resolved)

- Зондирование границ по GRP-07 вернуло строку `unclassified` («review manually»). Она НЕ разрешена автоматически и не превращена в критерий: категория границ этого требования остаётся открытым предположением планировщика и подлежит человеческому просмотру при верификации фазы.
- Разрешены дискреционные допущения RESEARCH A1 (`last_sync_result` — `Text` с JSON-строкой) и A2 (`groups.missing_since` — nullable `DateTime`). Обе формы отданы планировщику решением D-12; альтернативы (отдельные int-колонки, булев `is_missing`) равноценны, и цена смены после выката — одна дополнительная ревизия.

<verification>
- `uv run alembic heads` печатает единственную голову `0014`
- Применение и откат `0014` доказаны на настоящей PostgreSQL, целевая база не тронута
- `uv run pytest tests/test_application/test_group_resync.py tests/test_models/test_sync_result_columns.py -q` — зелено
- `uv run pytest tests/ -q` — суита не деградировала
</verification>

<success_criteria>
- GRP-07 (частично): результат синхронизации имеет место хранения, переживающее перезаход, и вычисляется одной функцией
- D-10, D-11, D-12 реализованы и закреплены тестами на всех пяти ветках переинвентаризации
- Ревизия `0014` безопасна для прыжка `0012 → 0014`: только additive nullable
</success_criteria>

<output>
Create `.planning/phases/03-gruppy-akkaunta/03-02-SUMMARY.md` when done
</output>
