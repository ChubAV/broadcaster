---
status: testing
phase: 03-gruppy-akkaunta
source: [03-VERIFICATION.md, 03-09-SUMMARY.md, 03-10-SUMMARY.md, 03-11-SUMMARY.md, 03-12-SUMMARY.md]
round: 2
previous_round: 03-UAT-round-1.md
started: 2026-08-13T16:20:00Z
updated: 2026-08-13T16:20:00Z
---

## Раунд 2 — волна закрытия пробелов

Первый раунд UAT (48 проверок, все `pass`, файл `03-UAT-round-1.md`) закрывал **только** планы 03-01..03-08 — это прямо видно в его поле `source`. Планы 03-09..03-12 легли **позже** и человеком не проверялись ни разу. Они изменили обработчик синхронизации (`app/pages/accounts.py`) и общий макрос подтверждения (`app/templates/components/modal.html`, 12 мест подтверждения во всём приложении).

Пять проверок ниже взяты из `human_verification` в `03-VERIFICATION.md`. Три из них проверяют рантайм-переход Alpine, который в проекте не исполняет ни один тест; одна — решение владельца о раскрытии текста ошибки; последняя — смоук-проход пяти критериев успеха фазы уже после волны.

## Current Test

number: 1
name: Состояние выполняющегося запроса на кнопке подтверждения
expected: |
  Кнопка гаснет (`.btn[disabled]`) и получает `aria-busy`, при этом удаление доходит до сервера: строка исчезает, браузер приходит на `/accounts/{id}/groups`.
awaiting: user response

## Tests

### 1. Состояние выполняющегося запроса на кнопке подтверждения
expected: Открыть `/accounts/{id}/groups` в браузере с поднявшимся Alpine, нажать «Удалить» у группы и подтвердить удаление, наблюдая кнопку подтверждения в момент отправки. Кнопка гаснет (`.btn[disabled]`) и получает `aria-busy`, при этом удаление ДОХОДИТ до сервера: строка исчезает, браузер приходит на `/accounts/{id}/groups`. Отдельный риск: отключение submit-кнопки внутри обработчика submit не должно погасить уже начатую отправку.
why_human: Переход `sending` false→true исполняет Alpine в браузере; браузерных/e2e-тестов в проекте нет. Тесты проверяют ТОЛЬКО разметку.
result: [pending]

### 2. Отмена повторной отправки
expected: Нажать кнопку подтверждения дважды подряд как можно быстрее (или Enter + клик). На сервер уходит ровно один POST удаления; второй отменяется обработчиком, а не проходит и не даёт вторую навигацию.
why_human: Рантайм-инвариант очерёдности, недоказуемый разметкой. Маршрут удаления идемпотентен, поэтому вреда от промаха не будет, но сам инвариант остаётся недоказанным.
result: [pending]

### 3. Наследование гарда остальными местами подтверждения
expected: Проверить в браузере ещё два места подтверждения помимо групп — удаление объявления (`ads/includes/ad_card.html`) и удаление расписания из редактора объявления (`ads/includes/sched_card.html`, блочный вызов со скрытым полем `return_to`). Подтверждённое удаление проходит; для расписания пользователь возвращается в редактор объявления, а не на сводный список (скрытое поле `return_to` доехало вместе с формой); кнопка «Отмена» остаётся нажимаемой во время отправки.
why_human: Правка легла в общий макрос и затрагивает 12 мест подтверждения во всём приложении. Тесты доказывают, что все потребители вызывают макрос, но не то, что отправка с блочным слотом реально доносит скрытое поле при активном гарде.
result: [pending]

### 4. РЕШЕНИЕ: раскрытие текста стороннего исключения в плашке ошибки синхронизации
expected: Вызвать сбой синхронизации WA/MAX так, чтобы поднялось httpx-исключение (остановить wa-worker или испортить его адрес в Redis), нажать «Синхронизировать всё» и прочитать плашку ошибки. РЕШЕНИЕ ВЛАДЕЛЬЦА — одно из двух: (а) принять раскрытие текста стороннего исключения как риск severity medium и записать его в регистр, либо (б) сузить текст узкой ветки до подконтрольного. См. CR-01 в `03-REVIEW.md`.
why_human: `app/messengers/whatsapp.py:130` и `max.py:120` конструируют `MessengerFetchError` как `f"{type(e).__name__}: {e}"`, а `app/pages/accounts.py` передаёт этот текст в `record_sync_failure` дословно; часть httpx-исключений встраивает в сообщение адрес внутреннего wa-worker. Тест `tests/test_routes/test_sync_groups.py:695` наоборот ЗАКРЕПЛЯЕТ сквозной проброс. Severity medium — ниже порога блокировки `high`, поэтому это не гейт фазы, но решение человек обязан принять явно.
result: [pending]

### 5. Смоук-проход пяти критериев успеха ПОСЛЕ волны закрытия пробелов
expected: Повторить проход пяти критериев успеха фазы на живом приложении. Все пять по-прежнему выполняются; синхронизация с экрана доходит до мессенджера и возвращает сводку; повторное нажатие кнопки синхронизации во время идущей синхронизации не запускает второй запрос к мессенджеру.
why_human: Раунд 1 закрывал только планы 03-01..03-08. Планы 03-09..03-12 изменили обработчик синхронизации и общий макрос подтверждения и человеком не проверялись.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

_Заполняется по ходу раунда._

## Открытая находка регистра угроз (не гейт фазы)

`03-SECURITY.md:53` держит T-03-17 в статусе `closed` на посылке «все конструкции — свой текст (`telegram_user.py:265`, `whatsapp.py:130,133`, `max.py:120,123`)». Проверка по файлам показала, что посылка верна только для `whatsapp.py:133` и `max.py:123`; `whatsapp.py:130`, `max.py:120` и `telegram_user.py:265` строят сообщение из стороннего исключения. Строку написал не план 03-12 (в его диффе она проходит контекстом), поэтому прохибиция плана формально не нарушена — но регистр в этой строке утверждает больше, чем делает код. Разрешается вместе с проверкой 4.
