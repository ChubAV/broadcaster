---
phase: 04-dashbord-i-istoriya
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/application/analytics/__init__.py
  - app/application/analytics/send_analytics.py
  - app/pages/dashboard.py
  - app/pages/history.py
  - app/pages/admin.py
  - app/templates/dashboard.html
  - app/templates/dashboard/includes/metric_tile.html
  - app/static/css/app.css
  - tests/test_application/test_send_analytics.py
  - tests/test_pages/test_dashboard.py
autonomous: true
requirements: [DASH-01, HIST-01]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Плитки дашборда считают отправки за скользящие 24 часа от момента запроса, а не от UTC-полуночи (D-02)"
    - "Запись со статусом account_disconnected попадает в плитку «Ошибок»; сумма «Успешно» и «Ошибок» равна плитке «Отправок за сутки» (D-01)"
    - "Дельта плитки считается к предыдущим суткам, и оба окна берутся одним запросом (D-03, D-38)"
    - "Плитка «Групп охвачено» не падает на записях с пустым group_id и не считает их за отдельную группу"
    - "Дашборд рендерит четыре плитки отправок и не рендерит в теле страницы счётчики объявлений, аккаунтов и групп (D-01)"
    - "Фильтры истории имеют единственное определение в app/application/analytics/send_analytics.py; его импортируют и история, и админка (D-35)"
    - "Период today отсчитывается от локальной полуночи пользователя, а не от UTC-полуночи (D-30)"
    - "history_count с данным набором фильтров возвращает ровно то число записей, которое отдаёт список с тем же набором (D-31)"
    - "tests/test_pages/test_htmx_preserved.py::test_infinite_scroll_keeps_filters остаётся зелёным после переноса фильтров"
    - "Агрегация окна метрик не использует диалект-специфичных SQL-функций календарной группировки, поэтому одинаково исполняется на SQLite и PostgreSQL"
    - "Кэша агрегатов в модуле нет: значения считаются на каждый рендер, и слоя инвалидации фаза не заводит (D-37)"
  prohibitions:
    - "MUST NOT silently exclude send records it cannot classify (missing messenger_type or group_id) from the tile counts — an unclassifiable record is still a send and must be counted, never dropped to make the numbers look clean"
  artifacts:
    - path: app/application/analytics/send_analytics.py
      provides: "Публичный контракт аналитики отправок для дашборда, истории и Фазы 6"
      contains: "async def send_metrics"
    - path: app/templates/dashboard/includes/metric_tile.html
      provides: "Макрос плитки метрики с дельтой (макет 387-392 минус спарклайн)"
      contains: "macro metric_tile"
    - path: tests/test_application/test_send_analytics.py
      provides: "Юнит-покрытие окна, трёх статусов и переносимости агрегации"
    - path: tests/test_pages/test_dashboard.py
      provides: "Интеграционное покрытие дашборда (собственного файла тестов у него не было)"
  key_links:
    - from: app/pages/dashboard.py
      to: app/application/analytics/send_analytics.py
      via: "импорт send_metrics — страница не считает агрегаты сама"
      pattern: "from app.application.analytics.send_analytics import"
    - from: app/pages/history.py
      to: app/application/analytics/send_analytics.py
      via: "импорт apply_history_filters/history_filter_params/history_count"
      pattern: "from app.application.analytics.send_analytics import"
    - from: app/pages/admin.py
      to: app/application/analytics/send_analytics.py
      via: "импорт фильтров вместо приватных имён страничного модуля истории"
      pattern: "from app.application.analytics.send_analytics import"
---

<objective>
Трассирующий срез Фазы 4: завести модуль аналитики отправок и провести один поток данных
сквозь все слои фазы — БД → чистая функция модуля → страничный обработчик → макрос Jinja →
CSS → тест. Слоем проверки выбраны четыре плитки метрик за сутки (DASH-01), потому что они
единственные затрагивают каждый слой, который дальше расширяют все остальные планы фазы.

Вторая задача плана переносит определение фильтров истории в тот же модуль и добавляет
`history_count` — контракт, от которого зависят чипсы (04-06), экспорт (04-08) и счётчик D-31.

Purpose: D-35 требует один модуль аналитики на дашборд, историю и Фазу 6. Его границу нужно
проверить на работающем срезе до того, как от неё начнут зависеть восемь остальных планов.
Output: `app/application/analytics/send_analytics.py` с публичным контрактом, четыре живые
плитки на дашборде, два новых файла тестов.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashbord-i-istoriya/04-CONTEXT.md
@.planning/phases/04-dashbord-i-istoriya/04-RESEARCH.md
@.planning/phases/04-dashbord-i-istoriya/04-PATTERNS.md
@.planning/phases/04-dashbord-i-istoriya/04-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Сквозной срез — модуль аналитики, метрики за сутки, плитки на дашборде</name>
  <reversibility rating="costly">Контракт модуля становится публичным для истории и Фазы 6 (D-35); смена сигнатур после того, как от них начнут зависеть остальные планы фазы, потребует согласованной правки во всех потребителях.</reversibility>
  <files>app/application/analytics/__init__.py, app/application/analytics/send_analytics.py, app/pages/dashboard.py, app/templates/dashboard.html, app/templates/dashboard/includes/metric_tile.html, app/static/css/app.css, tests/test_application/test_send_analytics.py, tests/test_pages/test_dashboard.py</files>
  <read_first>
    - `app/pages/dashboard.py` — что заменяется целиком: блок `stats` и расчёт `today_start` от UTC-полуночи
    - `app/application/accounts/group_resync.py` (строки 1-42 и 128-135) — форма модуля слоя application: докстринг-контракт, порядок импортов, сессия первым позиционным, остальное keyword-only
    - `app/application/scheduling/use_cases.py` (строки 35-60) — `@dataclass(slots=True)` и `collect_due_schedules` как образец сигнатуры
    - `app/pages/common.py` (строки 152-233 и 259-345) — `format_datetime_for_user`, `time_ago_for_user`, `_get_timezone_for_user`, `plural_ru`, нормализация naive→UTC, приём `int(... or 0)` и образец «много счётчиков одним round-trip»
    - `app/models/send_log.py` — состав колонок: `account_id` в модели НЕТ, есть `user_id`, `group_id`, `messenger_type`, `status`, `sent_at`
    - `app/templates/dashboard.html` — текущая обёртка `data-metrics` и заглушка Фазы 1, которую этот план снимает
    - `app/templates/history/includes/history_card.html` — образец макроса с докстрингом «почему макрос, а не include» и явными параметрами
    - `app/static/css/app.css` (строки 1000-1050 и 1370-1415) — примитивы `[data-longtext]`, `.chip`, `.day-grid`; сюда добавляются правила дельты
    - `design/new_broadcaster_design.unpacked.html` (строки 383-400) — плитка метрики: mono-метка, крупное число с tabular-nums, дельта цветом; блок спарклайна на строках 393-397 НЕ реализуется (D-03)
    - `tests/test_pages/test_responsive_markup.py` (строки 140-160) — фикстура `_seed_send_log`, образец посева записей журнала
    - `tests/conftest.py` — фикстуры `client`, `authed_client`, `db_session`, `test_settings`
  </read_first>
  <behavior>
    Юнит (`tests/test_application/test_send_analytics.py`):
    - `send_metrics` при `now` и посеве «5 записей внутри 24 ч, 3 записи в промежутке 24-48 ч» отдаёт `total=5`, `total_prev=3`.
    - Запись со статусом `account_disconnected` попадает в `failed`, не в `ok`; `ok + failed == total` при посеве из трёх статусов.
    - Запись, чей `sent_at` ровно на границе `now - 24h`, попадает в текущее окно, а не в предыдущее (граница включающая).
    - `groups` считает различные `group_id`; две записи по одной группе дают `groups=1`; запись с `group_id=None` не увеличивает `groups`, но увеличивает `total`.
    - Пустой набор данных даёт нули по всем восьми полям, а не `None`.
    - Чужие записи (другой `user_id`) не попадают ни в одно поле.
    Интеграция (`tests/test_pages/test_dashboard.py`):
    - `GET /dashboard` отдаёт 200 и содержит четыре mono-метки плиток отправок.
    - Ответ НЕ содержит меток счётчиков сущностей, снятых D-01 (проверяется по отсутствию соответствующих подписей в теле страницы).
    - Значение плитки «Отправок за сутки» равно числу посеянных записей за последние сутки.
    - Неавторизованный запрос `GET /dashboard` даёт редирект на `/login`.
  </behavior>
  <action>
Создать пакет `app/application/analytics/` с пустым `__init__.py` (по образцу `app/application/accounts/__init__.py`) и модулем `send_analytics.py`.

Модуль открывается докстринг-контрактом в форме `group_resync.py`: зачем модуль один (D-35 — его зовут дашборд, история и Фаза 6, ни один потребитель не владеет определением), и раздел «чего модуль не делает»: не читает `Request`, ничего не пишет в БД, не коммитит, не знает про Jinja и про шаблоны, не вызывает Docker SDK и НЕ кэширует результаты (D-37) — агрегаты считаются на каждый рендер, потому что кэш потребовал бы инвалидации из воркера на каждую отправку, а образец Redis-кэша в проекте существует и применён сознательно не здесь.

Выписать в модуле единственный на проект источник значений статуса журнала: `STATUS_OK = "ok"`, `STATUS_FAIL = "fail"`, `STATUS_ACCOUNT_DISCONNECTED = "account_disconnected"`, `FAILED_STATUSES = (STATUS_FAIL, STATUS_ACCOUNT_DISCONNECTED)`. Статусов три, а не два — плитка «Ошибок» обязана считать оба неуспешных (RESEARCH §Pitfall 3).

Добавить публичный хелпер `normalize_utc(value)`: если у datetime нет tzinfo — проставить `timezone.utc`. SQLite отдаёт колонку `DateTime(timezone=True)` naive, PostgreSQL — aware; приём копируется из `app/pages/common.py:161-162`. Все чтения `sent_at` в модуле проходят через него.

Объявить `@dataclass(slots=True) class SendMetrics` с полями `total`, `ok`, `failed`, `groups`, `total_prev`, `ok_prev`, `failed_prev`, `groups_prev` (все `int`) и вычисляемыми свойствами дельт `total_delta`, `ok_delta`, `failed_delta`, `groups_delta` — разность текущего и предыдущего окна.

Реализовать `async def send_metrics(session, *, user_id, now=None, window=timedelta(hours=24)) -> SendMetrics`. Окно берётся шириной `window * 2` одним `WHERE SendLog.sent_at >= now - window*2`, разделение на текущее и предыдущее — условными агрегатами `func.sum(case(...))` и `func.count(func.distinct(case(...)))`; один round-trip на все восемь чисел (D-38). Границей текущего окна служит `SendLog.sent_at >= now - window` — скользящие сутки, никакого приведения к полуночи (D-02). Все значения проходят через `int(x or 0)`: `func.sum` над пустым набором отдаёт NULL. Владение — `SendLog.user_id == user_id` в `WHERE`.

Бакетирование и группировка по календарным единицам в этом модуле выполняются в Python над проекцией значений; диалект-специфичных SQL-функций календарной группировки в модуле быть не должно — RESEARCH §Pitfall 2 перечисляет запрещённые имена и объясняет, почему ветка под PostgreSQL не исполнится тестами ни разу.

Переписать `app/pages/dashboard.py`: удалить три запроса счётчиков сущностей (`ads_count`, `accounts_count`, `groups_count`), удалить расчёт `today_start` от UTC-полуночи и запрос `sent_today`, удалить словарь `stats`. Вместо них — один вызов `send_metrics(db, user_id=user.id)`, результат кладётся в контекст под именем `metrics`. Блок `recent_sends` в этом плане остаётся как есть — его заменяет план 04-05. `active_page`, `is_admin` и гард `get_user_from_cookie` сохраняются дословно.

Создать `app/templates/dashboard/includes/metric_tile.html` с макросом `metric_tile(label, value, delta, tone='neutral')`: докстринг в форме `history_card.html` (почему макрос, а не include; параметры явные, потому что импортированный шаблон контекста вызывающего не получает). Разметка — по макету строки 387-392: mono-метка через существующий макрос `mono` с `upper=true` и тоном `muted`, крупное значение в `<span data-metric-value>`, дельта в отдельном `<span data-metric-delta data-tone="...">`. Спарклайн (макет 393-397) не реализуется по D-03. Utility-классов не вводить — их отсутствие проверяется существующим `test_no_utility_classes_anywhere`.

Переписать блок `data-metrics` в `app/templates/dashboard.html` на четыре вызова `metric_tile` с подписями «Отправок за сутки», «Успешно», «Ошибок», «Групп охвачено» и дельтами из `metrics`. Тон дельты: положительная — успех, отрицательная — опасность, ноль — приглушённый; для плитки «Ошибок» тон инвертируется (рост ошибок — не успех). Комментарий-заглушку Фазы 1 о том, что метрики принадлежат Фазе 4, снять — она перестала быть правдой.

В `app/static/css/app.css` добавить правила для `[data-metric-delta]` (mono-шрифт, размер 11px по макету, цвет по `data-tone`) и `font-variant-numeric: tabular-nums` для `[data-metric-value]`, если его там ещё нет. Правила ставить рядом с существующим блоком метрик, а не в конец файла.

Создать оба файла тестов из блока behavior. `tests/test_application/test_send_analytics.py` — асинхронные тесты с `@pytest.mark.asyncio` на фикстуре `db_session`; `tests/test_pages/test_dashboard.py` — на фикстурах `authed_client`/`client`. Посев записей журнала копировать с `_seed_send_log` (`tests/test_pages/test_responsive_markup.py:149`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_send_analytics.py tests/test_pages/test_dashboard.py -x -q</automated>
    <automated>! grep -vE '^\s*#' app/application/analytics/send_analytics.py | grep -qE 'func\.(strftime|date_trunc|extract|to_char|julianday)'</automated>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py tests/test_templates/test_components.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/application/analytics/send_analytics.py` содержит `async def send_metrics(` и `FAILED_STATUSES`
    - `app/pages/dashboard.py` содержит `from app.application.analytics.send_analytics import`
    - `app/pages/dashboard.py` не содержит `replace(` с обнулением часа (расчёт от UTC-полуночи снят)
    - `app/templates/dashboard/includes/metric_tile.html` содержит `macro metric_tile`
    - `GET /dashboard` авторизованным клиентом возвращает 200 и в теле присутствуют все четыре подписи плиток отправок
    - `GET /dashboard` неавторизованным клиентом возвращает 302 на `/login`
    - `uv run pytest tests/test_application/test_send_analytics.py tests/test_pages/test_dashboard.py -q` завершается кодом 0
    - `uv run pytest tests/test_pages/test_responsive_markup.py -q` завершается кодом 0 (utility-классы и элементы таблицы не появились)
  </acceptance_criteria>
  <done>Четыре плитки отправок за скользящие сутки с дельтой видны на дашборде, считаются модулем аналитики одним запросом и закреплены юнит- и интеграционными тестами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Фильтры истории и счётчик переезжают в модуль аналитики</name>
  <files>app/application/analytics/send_analytics.py, app/pages/history.py, app/pages/admin.py, tests/test_application/test_send_analytics.py</files>
  <read_first>
    - `app/pages/history.py` (строки 19-60) — `_parse_account_id`, `_history_filter_params`, `_apply_history_filters`: переносятся дословно
    - `app/pages/history.py` (строки 62-122 и 155-226) — оба вызова фильтров и построение `outerjoin(Group)`, без которого фильтр по аккаунту не работает
    - `app/pages/admin.py` (строки 1-20) — импорт приватных имён из страничного модуля истории, который обязан обновиться
    - `app/pages/common.py` (строки 140-170) — `_get_timezone_for_user`: нужен для локальной полуночи периода `today`
    - `tests/test_pages/test_htmx_preserved.py` (строки 200-250) — `test_infinite_scroll_keeps_filters`, гарантия, которую перенос не имеет права сломать
    - `tests/test_pages/test_responsive_markup.py` (строка 547 и окрестности) — `test_history_filters_survive_pagination`
    - `app/application/analytics/send_analytics.py` — файл из задачи 1, куда переезжают функции
  </read_first>
  <behavior>
    - `apply_history_filters(query, status=..., messenger_type=..., account_id=..., period=...)` при тех же аргументах строит тот же набор условий, что приватная функция до переезда: статус, тип мессенджера, `Group.account_id`, отсечка периода.
    - Период `7d` и `30d` ведут себя дословно как раньше.
    - Новый период `today` отсекает по локальной полуночи пользователя: для пользователя в UTC+3 запись, отправленная в 01:00 UTC того же дня, в выборку НЕ попадает, а запись в 22:00 UTC предыдущего дня (01:00 локального «сегодня») — попадает.
    - Неизвестное значение периода не поднимает исключение и не применяет отсечки.
    - `history_count(session, user_id=..., ...)` возвращает то же число, что длина полного списка с теми же фильтрами.
    - `history_filter_params` отдаёт тот же словарь ключей (`status`, `messenger`, `account_id`, `period`), что и раньше — разметка проброса фильтров в URL от него зависит.
  </behavior>
  <action>
Перенести `_history_filter_params` и `_apply_history_filters` из `app/pages/history.py` в `app/application/analytics/send_analytics.py` под публичными именами `history_filter_params` и `apply_history_filters`. Перенос обязан быть поведенчески нулевым: семантика и порядок условий сохраняются дословно, иначе краснеют `test_infinite_scroll_keeps_filters` и `test_history_filters_survive_pagination`.

Сигнатура `apply_history_filters(query, *, status=None, messenger_type=None, account_id=None, period=None, user=None)`. Параметр `user` нужен только периоду `today` и по умолчанию отсутствует; при `period == "today"` и заданном `user` отсечка берётся как локальная полночь пользователя, переведённая в UTC, через `_get_timezone_for_user`. Это единственный фильтр фазы, зависящий от таймзоны: без него повторится дефект, который D-02 чинит на дашборде.

Выписать в модуле кортеж допустимых периодов `HISTORY_PERIODS = ("today", "7d", "30d")` — их читают и фильтр, и разметка чипсов плана 04-06. Значение вне кортежа отсечки не применяет и исключения не поднимает (V5 из RESEARCH §Security Domain: неизвестное значение даёт «фильтр не применён», а не 500).

Добавить `async def history_count(session, *, user_id, status=None, messenger_type=None, account_id=None, period=None, user=None) -> int`. Внутри — `select(func.count()).select_from(SendLog).outerjoin(Group, SendLog.group_id == Group.id).where(SendLog.user_id == user_id)`, пропущенный через `apply_history_filters`. `outerjoin(Group)` обязателен даже когда фильтр по аккаунту не задан: без него условие по `Group.account_id` не построится, и фильтр молча развалится. Результат — `int(... or 0)`.

Обновить `app/pages/history.py`: импортировать `apply_history_filters` и `history_filter_params` из модуля аналитики, удалить локальные определения, заменить вызовы на именованные аргументы, передавать `user` в оба вызова. `_parse_account_id` остаётся в страничном модуле — это разбор HTTP-параметра, а не аналитика.

Обновить `app/pages/admin.py`: импорт приватных имён из `app.pages.history` заменить на импорт публичных имён из модуля аналитики. Поведение админской истории не меняется.

Дописать в `tests/test_application/test_send_analytics.py` тесты из блока behavior. Тест локальной полуночи ставит пользователю таймзону из `app.constants.VALID_TIMEZONES` со смещением +3 и сеет две записи по обе стороны локальной границы дня.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_send_analytics.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_preserved.py::test_infinite_scroll_keeps_filters tests/test_pages/test_responsive_markup.py -q</automated>
    <automated>uv run pytest tests/test_pages/ tests/test_templates/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/application/analytics/send_analytics.py` содержит `def apply_history_filters(`, `def history_filter_params(`, `async def history_count(` и `HISTORY_PERIODS`
    - `app/pages/history.py` не содержит `def _apply_history_filters(` и `def _history_filter_params(`
    - `app/pages/admin.py` содержит `from app.application.analytics.send_analytics import`
    - `grep -c 'apply_history_filters' app/application/analytics/send_analytics.py` больше нуля, и определение функции существует ровно в одном файле проекта
    - `uv run pytest tests/test_pages/test_htmx_preserved.py::test_infinite_scroll_keeps_filters -q` завершается кодом 0
    - `uv run pytest tests/test_pages/ tests/test_templates/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Определение фильтров истории единственное, лежит в модуле аналитики, знает период today от локальной полуночи и сопровождается счётчиком, который отдаёт то же число, что и список.</done>
</task>

</tasks>

## Artifacts this phase produces

Символы, создаваемые ЭТИМ планом (новые — не искать их в текущем дереве):

- Пакет `app/application/analytics/` и модуль `app/application/analytics/send_analytics.py`
- Константы: `STATUS_OK`, `STATUS_FAIL`, `STATUS_ACCOUNT_DISCONNECTED`, `FAILED_STATUSES`, `HISTORY_PERIODS`
- Датакласс `SendMetrics` со свойствами `total_delta`, `ok_delta`, `failed_delta`, `groups_delta`
- Функции: `normalize_utc`, `send_metrics`, `apply_history_filters`, `history_filter_params`, `history_count`
- Шаблон `app/templates/dashboard/includes/metric_tile.html` и макрос `metric_tile`
- Атрибуты разметки/CSS: `[data-metric-delta]`, `data-tone`
- Файлы тестов: `tests/test_application/test_send_analytics.py`, `tests/test_pages/test_dashboard.py`

Символы, создаваемые остальными планами фазы (перечислены здесь, чтобы источниковая сверка
не помечала их как дрейф): `activity_heatmap`, `HeatmapView`, `recent_feed`, `FeedRow`,
`upcoming_sends`, `UpcomingSend`, `RETRYABLE_STATUSES`, `build_dispatch_task`, `retry_send`,
`history_export`, `history_retry`, `retry_precheck`, `_claim_retry_slot`, `_release_retry_slot`,
`EXPORT_ROW_CAP`, `EXPORT_DELIMITER`, `EXPORT_HEADER`, `_csv_safe`, `FEED_LIMIT`,
`FEED_POLL_SECONDS`, `ix_send_logs_user_id_sent_at`, макросы `heatmap`, `upcoming_row`,
`feed_row`, `filter_chips`, шаблон `app/templates/dashboard/partial_feed.html`, модуль
`app/pages/dashboard_feed.py`, ревизия `alembic/versions/0016_send_logs_user_sent_at.py`.

## Probe Fallback — Flagged Assumptions

SPEC у фазы нет; `workflow.specless_probe_fallback` отработал и вернул 9 требований со
статусом `unclassified` / `unresolved` (классификатор англоязычный, тексты требований русские —
это промах классификации, а не отсутствие краевых случаев). По §A такие строки НЕ закрываются
backstop-ом автоматически и surface-ятся как явные допущения. Ни одно не отброшено: краевые
предикаты выведены вручную из `.planning/REQUIREMENTS.md`, критериев успеха ROADMAP и
`04-VALIDATION.md` §Per-Task Verification Map и разложены по планам.

| # | Требование | Статус пробы | Где живут выведенные предикаты |
|---|------------|--------------|-------------------------------|
| 1 | DASH-01 | unclassified / unresolved | `must_haves.truths` плана 04-01 |
| 2 | DASH-02 | unclassified / unresolved | `must_haves.truths` плана 04-04 |
| 3 | DASH-03 | unclassified / unresolved | `must_haves.truths` плана 04-05 |
| 4 | DASH-04 | unclassified / unresolved | `must_haves.truths` плана 04-04 |
| 5 | DASH-05 | unclassified / unresolved | `must_haves.truths` плана 04-05 |
| 6 | HIST-01 | unclassified / unresolved | `must_haves.truths` планов 04-01 и 04-06 |
| 7 | HIST-02 | unclassified / unresolved | `must_haves.truths` плана 04-07 |
| 8 | HIST-03 | unclassified / unresolved | `must_haves.truths` плана 04-08 |
| 9 | HIST-04 | unclassified / unresolved | `must_haves.truths` планов 04-03 и 04-09 |

Проверка «без молчаливых потерь»: 9 surface-нутых = 9 представленных (9 строк таблицы + все
выведенные предикаты в `must_haves.truths` перечисленных планов).

Прохибиции (§B, две стадии recall→precision, авторство descriptor-less): оставлено четыре —
P-04-01 (план 04-01), P-04-02 (план 04-07), P-04-03 и P-04-04 (план 04-09). Канонные кандидаты
не минтятся, а отмечаются хлебными крошками: CSV formula injection — канон injection, покрыт
`/gsd-secure-phase` и регистром угроз T-04-16, не минтится; доступ к чужим записям истории,
экспорту и повтору — канон access control, покрыт `/gsd-secure-phase` и T-04-01/T-04-13, не
минтится; раскрытие текста стороннего исключения владельцу — канон information disclosure,
унаследован от R-03-09, не минтится.

## Decisions resolved in this plan

- **Open Question 5 (судьба JSON-API `app/routes/history.py`) — решено «выровнять, не сносить».**
  `grep` по репозиторию показал: у `GET /api/history` и `GET /api/history/stats` нет ни одного
  потребителя, кроме тестов (`tests/test_e2e.py:165,172`, `tests/test_routes/test_history.py`).
  Допущение A5 при этом остаётся недоказуемым для внешних клиентов, а снос публичной
  JSON-поверхности — дверь в одну сторону для клиента, которого нельзя перечислить. Выравнивание
  обратимо и чинит реальный дефект (два разных ответа на вопрос «сколько было ошибок»).
  Исполняется планом 04-10.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный маршрут | значения фильтров (`status`, `messenger`, `account_id`, `period`) приходят из query-строки |
| приложение → PostgreSQL | агрегирующие запросы по `send_logs`, самой растущей таблице системы |
| данные мессенджера → агрегаты | `messenger_type`, `group_name` записаны воркером со слов стороннего сервиса |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `send_metrics`, `history_count`, `apply_history_filters` | high | mitigate | `user_id` — обязательный именованный параметр, условие `SendLog.user_id == user_id` стоит в базовом `WHERE` каждой функции; ветки «все пользователи» в модуле нет; тест на чужие записи в `test_send_analytics.py` |
| T-04-02 | Tampering | значения фильтров из query | low | accept | SQLAlchemy параметризует условия; неизвестное значение периода не применяет отсечки и не поднимает 500 — закреплено тестом |
| T-04-03 | Denial of Service | агрегация окна на большом `send_logs` | medium | mitigate | одно окно шириной 48 ч, один round-trip на восемь чисел (D-38); составной индекс `(user_id, sent_at)` приходит ревизией 0016 (план 04-02); запросы обязаны быть корректны и без индекса |
| T-04-04 | Tampering | вывод `group_name` / `messenger_type` в плитках | low | mitigate | значения выводятся обычным экранированным `{{ }}` без `|safe`; плитки показывают числа, не текст мессенджера |
| T-04-SC | Tampering | установка пакетов npm/pip/cargo | low | accept | фаза не устанавливает ни одного пакета; таблица `## Package Legitimacy Audit` в RESEARCH пуста по существу, гейт легитимности неприменим — если план всё же потребует зависимости, гейт обязан быть пройден до записи пакета |
</threat_model>

<verification>
- `uv run pytest tests/test_application/ tests/test_pages/test_dashboard.py -q` — зелено
- `uv run pytest tests/test_pages/ tests/test_templates/ -q` — зелено, инвентаризации сходятся
- Определение фильтров истории существует ровно в одном файле проекта
- В модуле аналитики нет диалект-специфичных SQL-функций календарной группировки
</verification>

<success_criteria>
- Модуль `app/application/analytics/send_analytics.py` существует, имеет докстринг-контракт с разделом «чего модуль не делает» и экспортирует `send_metrics`, `apply_history_filters`, `history_filter_params`, `history_count`
- Дашборд показывает четыре плитки отправок за скользящие сутки с дельтой к предыдущим суткам
- Счётчики сущностей из тела дашборда убраны; в сайдбаре они остаются нетронутыми
- Оба новых файла тестов существуют и зелены
- Вся существующая суита остаётся зелёной
</success_criteria>

<output>
Create `.planning/phases/04-dashbord-i-istoriya/04-01-SUMMARY.md` when done
</output>
