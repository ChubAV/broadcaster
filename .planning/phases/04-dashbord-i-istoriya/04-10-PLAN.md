---
phase: 04-dashbord-i-istoriya
plan: 10
type: execute
wave: 7
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06", "04-07", "04-08", "04-09"]
files_modified:
  - app/routes/history.py
  - app/repositories/send_log.py
  - tests/test_routes/test_history.py
  - tests/test_e2e.py
  - .planning/phases/04-dashbord-i-istoriya/04-SECURITY.md
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, HIST-01, HIST-02, HIST-03, HIST-04]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "На вопрос «сколько было ошибок» в проекте есть ровно один ответ: JSON-сводка считается тем же модулем аналитики, что и плитки дашборда"
    - "JSON-сводка считает все три статуса журнала, а не два из трёх: отправки, не ушедшие из-за отвалившегося аккаунта, перестали теряться"
    - "Устаревшего второго определения сводки по журналу в проекте не осталось"
    - "Вся суита проекта зелена целиком до передачи фазы на приёмку"
    - "Граф знаний проекта пересобран после правок кода, как того требуют инструкции проекта"
    - "Отчёт безопасности фазы существует отдельным файлом по образцу Фазы 3 и сводит регистры угроз всех десяти планов в одну таблицу"
    - "Принятые риски и остатки под смягчёнными угрозами названы в отчёте поимённо, а не унаследованы молча"
    - "Остаток по защите форм проекта от межсайтовой подделки выписан рекомендацией ОТДЕЛЬНОЙ задачи: фаза защищает только свою форму повтора и рамок не расширяет"
    - statement: "Дашборд и история пригодны к использованию на мобильных ширинах в живом браузере"
      verification: backstop
    - statement: "Опрос ленты продолжает тикать в открытой вкладке за пределами нескольких интервалов"
      verification: backstop
    - statement: "Панель подтверждения повтора при поднявшемся Alpine не даёт отправить повтор дважды"
      verification: backstop
    - statement: "Файл выгрузки открывается в табличном редакторе с целой кириллицей и полями по отдельным колонкам"
      verification: backstop
    - statement: "Сессия БД переживает поток выгрузки на боевом стеке приложения"
      verification: backstop
  artifacts:
    - path: app/routes/history.py
      provides: "JSON-сводка, считающая три статуса тем же модулем, что и дашборд"
      contains: "send_metrics"
    - path: .planning/phases/04-dashbord-i-istoriya/04-SECURITY.md
      provides: "Отчёт безопасности фазы: сводный регистр угроз, принятые риски, остатки и рекомендация отдельной задачи по защите форм проекта"
      contains: "T-04-38"
  key_links:
    - from: app/routes/history.py
      to: app/application/analytics/send_analytics.py
      via: "JSON-сводка зовёт модуль аналитики вместо собственного счёта по журналу"
      pattern: "send_metrics"
---

<objective>
Закрытие фазы: убрать второе определение сводки по журналу, прогнать суиту целиком, пересобрать
граф знаний, свести отчёт безопасности фазы и провести ручные проверки того, что автотестами в
этом проекте не проверяется.

Purpose: пять поведений фазы не покрываются автотестами по причинам среды (браузерных тестов в
проекте нет, табличного редактора нет, подмена сессии в тестах не наблюдает границу закрытия), и
их нельзя молча выдать за проверенные. Плюс требование ROADMAP «агрегации считаются один раз»
не выполнено, пока в проекте живёт второй счёт по журналу, знающий два статуса из трёх.
Output: выровненная JSON-сводка, зелёная суита, пересобранный граф, `04-SECURITY.md` со сводным
регистром угроз и принятыми рисками, отчёт ручной приёмки.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashbord-i-istoriya/04-CONTEXT.md
@.planning/phases/04-dashbord-i-istoriya/04-RESEARCH.md
@.planning/phases/04-dashbord-i-istoriya/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Одно определение сводки по журналу — JSON-API выравнивается по модулю аналитики</name>
  <files>app/routes/history.py, app/repositories/send_log.py, tests/test_routes/test_history.py, tests/test_e2e.py</files>
  <read_first>
    - `app/routes/history.py` — весь файл: схема ответа сводки, обработчик сводки, обработчик списка
    - `app/repositories/send_log.py` (строки 14-30) — устаревший счёт: окно жёстко тридцать суток и только два статуса из трёх, отправки с отвалившимся аккаунтом теряются
    - `app/application/analytics/send_analytics.py` — сигнатура `send_metrics` и её поля
    - `tests/test_routes/test_history.py` (строки 140-175) — существующие утверждения о числах сводки, которые сдвигаются вместе с семантикой
    - `tests/test_e2e.py` (строки 160-180) — сквозной сценарий, обращающийся к тем же адресам
    - `.planning/phases/03-gruppy-akkaunta/03-CONTEXT.md` — прецедент решения по мёртвым JSON-маршрутам раздела групп
  </read_first>
  <behavior>
    - Сводка по журналу считает окно в тридцать суток и три статуса: успешные, неуспешные (включая отправки с отвалившимся аккаунтом) и общее число.
    - Отправка со статусом отвалившегося аккаунта попадает в число неуспешных, а не теряется.
    - Сумма успешных и неуспешных равна общему числу.
    - Форма ответа сводки не изменилась: те же три поля с теми же именами.
    - Сводка отдаёт числа только по записям текущего пользователя.
    - Список истории через JSON-API продолжает отвечать в прежней форме.
    - Неавторизованный запрос к сводке по-прежнему отклоняется.
  </behavior>
  <action>
Переключить обработчик сводки в `app/routes/history.py` на модуль аналитики: вместо счёта через
репозиторий вызвать `send_metrics` с окном в тридцать суток и собрать ответ из его полей — общее
число, успешные, неуспешные. Форма ответа и имена полей не меняются, меняется только источник
чисел и семантика неуспешных: теперь их два статуса, а не один.

Основание решения (Open Question 5, отнесённого к Claude's Discretion): у обоих JSON-маршрутов
нет ни одного потребителя в репозитории, кроме тестов, но внешних клиентов перечислить нельзя, а
снос публичной JSON-поверхности для такого клиента — дверь в одну сторону. Выравнивание обратимо
и чинит настоящий дефект: сегодня на вопрос «сколько было ошибок» проект отвечает двумя разными
числами. Прецедент сноса мёртвых JSON-маршрутов раздела групп сюда не переносится именно из-за
недоказуемости отсутствия внешних потребителей.

Удалить устаревший метод счёта из `app/repositories/send_log.py` вместе с его импортами, ставшими
лишними: после переключения у него не остаётся потребителей, а второе определение сводки — ровно
то, что этот план убирает. Остальные методы репозитория не трогать: у них есть свои потребители.

Обновить утверждения о числах в `tests/test_routes/test_history.py`, добавив случай отправки с
отвалившимся аккаунтом и проверку, что сумма успешных и неуспешных сходится с общим числом.
Сквозной сценарий в `tests/test_e2e.py` привести в соответствие, если его утверждения о числах
сдвинулись; сами адреса и последовательность шагов не менять.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes/test_history.py tests/test_e2e.py -q</automated>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/routes/history.py` содержит `send_metrics`
    - `app/repositories/send_log.py` не содержит `def get_stats(`
    - Ответ сводки содержит те же три имени полей, что и до правки
    - Тест с посевом отправки с отвалившимся аккаунтом показывает её в числе неуспешных
    - `uv run pytest tests/test_routes/test_history.py tests/test_e2e.py -q` завершается кодом 0
    - `uv run pytest tests/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Сводка по журналу считается модулем аналитики, знает три статуса, а второго определения счёта в проекте не осталось.</done>
</task>

<task type="auto">
  <name>Task 2: Фазовый гейт — полная суита, отчёт безопасности фазы и пересборка графа знаний</name>
  <files>.planning/phases/04-dashbord-i-istoriya/04-SECURITY.md, .planning/phases/04-dashbord-i-istoriya/04-10-SUMMARY.md, graphify-out/graph.json</files>
  <read_first>
    - `.planning/phases/04-dashbord-i-istoriya/04-VALIDATION.md` — карта проверки по требованиям, частота сэмплирования и перечень ручных пунктов
    - `.planning/phases/03-gruppy-akkaunta/03-SECURITY.md` — образец отчёта безопасности фазы: фронтматтер (`threats_open`, `asvs_level`, `status`), таблица границ доверия, регистр угроз с колонкой статуса, раздел принятых рисков
    - Блоки `<threat_model>` всех десяти планов фазы (`04-01`…`04-10`) — источник строк сводного регистра
    - `justfile` — рецепты прогона тестов
    - `CLAUDE.md` — требование пересобирать граф знаний после правок кода
    - `.planning/STATE.md` — блокеры фазы, которые обязаны быть перенесены в отчёт в актуальном виде
  </read_first>
  <action>
Прогнать суиту целиком рецептом проекта. Базовый уровень до фазы — 1094 собираемых теста;
итоговое число собранных и пройденных тестов записать в SUMMARY фактическим значением из прогона,
а не оценкой.

Свести в SUMMARY карту требований фазы к тестам: для каждого из девяти требований назвать файл и
имя проверяющего теста. Требования, у которых закрывающего автотеста нет по причинам среды,
назвать явно и отправить в задачу 3, а не пометить закрытыми.

Пересобрать граф знаний проекта командой обновления графа — этого требуют инструкции проекта
после правок кода.

Зафиксировать в SUMMARY три состояния, которые фаза не меняет, но обязана передать дальше без
искажения: целевая база остаётся на ревизии 0012, и накат ревизий 0013-0016 — решение владельца;
браузерных и сквозных тестов в проекте нет, поэтому рантайм-поведение опроса, панели
подтверждения и кнопки копирования держится на ручной проверке; граница закрытия сессии при
потоковой выгрузке суитой не наблюдается, потому что подмена зависимости сессии в тестах не
является генератором.

Написать отчёт безопасности фазы `.planning/phases/04-dashbord-i-istoriya/04-SECURITY.md` по
образцу `03-SECURITY.md` — прецедент Фазы 3, отдельный файл, а не абзац в SUMMARY: регистр угроз
фазы живёт в десяти планах, и без сведения его в один документ принятые риски наследуются молча.
Состав:

(1) фронтматтер того же вида, что у Фазы 3: номер фазы, слаг, статус, уровень ASVS 1 и
`threats_open` — число ОТКРЫТЫХ угроз с severity не ниже порога блокировки (`high`). Число
получить счётом по сведённому регистру, а не прогнозом;

(2) таблица границ доверия — объединение блоков `<threat_model>` всех десяти планов без
дублирования строк;

(3) сводный регистр: каждая строка `T-04-XX` из каждого плана с колонкой статуса, где смягчение
подтверждается ссылкой на файл и строку реализации либо на имя закрывающего теста. Строк, чьё
смягчение не подтверждено ничем, быть не должно: такая строка остаётся открытой, а не
объявляется закрытой;

(4) раздел принятых рисков — поимённо, с обоснованием и severity: показ текста стороннего
исключения владельцу записи (T-04-27, унаследовано от R-03-09 Фазы 3), состав диагностического
блока в буфере обмена (T-04-28), смысл индикатора воркеров онлайн (T-04-21), значения фильтров из
адреса (T-04-02), а также перенос решения о накате ревизий владельцу (T-04-43, disposition
`transfer`);

(5) раздел остатков ПОД СМЯГЧЁННЫМИ угрозами — то, что смягчение закрывает не целиком:
(а) T-04-37 — заявка против двойной постановки живёт в памяти процесса и при нескольких рабочих
процессах гонка остаётся; (б) T-04-38 — сверка источника защищает ТОЛЬКО форму повтора, которую
вводит эта фаза, и пропускает запросы без обоих заголовков источника;

(6) ⚠️ РЕКОМЕНДАЦИЯ ОТДЕЛЬНОЙ ЗАДАЧИ — обязательный раздел, а не строка в таблице. Остальные
POST-формы проекта (13 мест удаления) защиты от межсайтовой подделки не имеют, и эта фаза их не
чинит: рамки фазы — дашборд и история. Записать рекомендацию явно, назвав объём (все изменяющие
состояние формы проекта), почему она не сделана здесь (расширение рамок фазы) и почему её нельзя
терять (аутентификация проекта идёт cookie). Рекомендация обязана быть выполнимым описанием
задачи для следующего планирования, а не сожалением.

Отчёт пишется ДО чекпоинта задачи 3: владелец должен принимать фазу, видя принятые риски.
  </action>
  <verify>
    <automated>just test</automated>
    <automated>uv run pytest tests/ -q --collect-only 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `just test` завершается кодом 0
    - Число собранных тестов после фазы не меньше базового уровня 1094
    - `graphify-out/graph.json` обновлён (время изменения файла новее начала фазы)
    - SUMMARY плана содержит карту «требование — файл теста — имя теста» для всех девяти требований фазы
    - SUMMARY плана содержит три перенесённых состояния
    - Файл `.planning/phases/04-dashbord-i-istoriya/04-SECURITY.md` существует и содержит фронтматтер с `asvs_level` и `threats_open`
    - Сводный регистр в `04-SECURITY.md` содержит каждый идентификатор `T-04-XX`, объявленный в планах фазы, и ни одного лишнего
    - `04-SECURITY.md` содержит `T-04-38` со статусом смягчения и названным остатком
    - `04-SECURITY.md` содержит раздел рекомендации отдельной задачи по защите POST-форм проекта от межсайтовой подделки, с названным объёмом
    - Каждый принятый риск в `04-SECURITY.md` имеет severity и обоснование, а не одну строку названия
  </acceptance_criteria>
  <done>Суита зелена целиком, граф пересобран, карта покрытия и переходящие состояния зафиксированы фактическими значениями, а отчёт безопасности фазы существует отдельным файлом с принятыми рисками, остатками и рекомендацией отдельной задачи.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ручная приёмка пяти поведений, не покрываемых автотестами</name>
  <what-built>
    Фаза 4 целиком: дашборд с четырьмя плитками отправок за скользящие сутки и дельтой, списком
    ближайших отправок с пометками причин, живой лентой на бессрочном опросе и heatmap 7×24 в
    таймзоне пользователя; история с чипсами-фильтрами, точным счётчиком найденного, выгрузкой
    отфильтрованного результата в файл, всегда видимым текстом ошибки с копированием
    диагностического блока и повтором неуспешной отправки через боевую очередь.
  </what-built>
  <how-to-verify>
    Пять пунктов ниже автотестами в этом проекте не проверяются: браузерных и сквозных тестов
    нет, табличного редактора в среде нет, а подмена зависимости сессии в тестах не наблюдает
    границу закрытия сессии.

    1. **Опрос ленты тикает бессрочно.** Запустить приложение (`just run`), открыть `/dashboard`,
       оставить вкладку открытой не меньше чем на пять интервалов опроса (интервал 20 секунд —
       около двух минут). Ожидаемо: блок ленты продолжает обновляться, запросы к `/dashboard/feed`
       идут во вкладке сети без остановки, лента не замирает.

    2. **Панель подтверждения повтора не даёт отправить дважды.** Открыть `/history`, найти
       неуспешную запись, нажать повтор, в открывшейся панели быстро нажать подтверждение дважды.
       Ожидаемо: подтверждение блокируется после первого нажатия, в очередь уходит одна задача,
       в истории появляется одна новая запись, а не две. Проверить также обновление страницы
       после повтора: повторной отправки не происходит.

    3. **Файл выгрузки открывается в табличном редакторе.** Отфильтровать историю любым набором
       фильтров, нажать выгрузку, открыть файл в Excel (или в LibreOffice с русской локалью).
       Ожидаемо: кириллица цела, поля разложены по отдельным колонкам, число строк совпадает со
       счётчиком, который был показан над списком. Отдельно: значение, начинающееся со знака
       равенства, отображается текстом и не вычисляется как формула.

    4. **Сессия БД переживает поток выгрузки на боевом стеке.** Прогнать выгрузку на реальном
       запуске приложения на выборке, достаточной, чтобы ответ шёл потоком (несколько тысяч
       строк). Ожидаемо: файл скачивается целиком, в логах нет ошибок о закрытой сессии или о
       работе вне контекста цикла событий.

    5. **Адаптивность дашборда и истории.** Открыть `/dashboard` и `/history` на ширинах 320,
       860, 900 и 1080 пикселей. Ожидаемо: сетка heatmap читаема или прокручивается по
       горизонтали и не схлопывается в нечитаемые полоски; плитки, строки ближайших отправок и
       карточки истории перестраиваются без горизонтальной прокрутки страницы; чипсы фильтров
       переносятся, а не обрезаются; кнопки повтора и копирования доступны.

    Перед ответом просмотреть `.planning/phases/04-dashbord-i-istoriya/04-SECURITY.md` — раздел
    принятых рисков, раздел остатков под смягчёнными угрозами и рекомендацию отдельной задачи по
    защите POST-форм проекта от межсайтовой подделки. Фаза защищает только собственную форму
    повтора; остальные формы остаются как были, и это принимается сознательно.

    Отдельно подтвердить или отложить решение по накату ревизий: целевая база на ревизии 0012,
    `alembic upgrade head` прогонит четыре ревизии, одна из которых удаляет строки и берёт
    исключительную блокировку на таблице групп, а ревизия индекса блокирует запись в журнал
    отправок на время построения. Это решение владельца, и фаза его не принимает.
  </how-to-verify>
  <resume-signal>Напишите «approved», если все пять пунктов прошли, либо перечислите, что именно не сошлось (номер пункта и наблюдаемое поведение)</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

Символы, изменяемые ЭТИМ планом:

- Обработчик JSON-сводки в `app/routes/history.py` переключается на `send_metrics`
- Устаревший метод счёта по журналу удаляется из `app/repositories/send_log.py`
- Отчёт безопасности фазы `.planning/phases/04-dashbord-i-istoriya/04-SECURITY.md` (образец — `03-SECURITY.md` Фазы 3)

Полный перечень символов, создаваемых фазой, приведён в разделе «Artifacts this phase produces»
плана 04-01.

## Manual-Only Verifications (перенесено из 04-VALIDATION.md и планов фазы)

| Поведение | Требование | Почему вручную | Где проверяется |
|-----------|------------|----------------|-----------------|
| Опрос ленты тикает бессрочно в открытой вкладке | DASH-03 | браузерных и сквозных тестов в проекте нет | пункт 1 чекпоинта |
| Панель подтверждения не даёт отправить повтор дважды | HIST-04 | рантайм Alpine проверяется руками | пункт 2 чекпоинта |
| Файл выгрузки открывается в табличном редакторе без искажений и одной колонкой | HIST-03 | редактора в среде нет | пункт 3 чекпоинта |
| Сессия БД переживает поток выгрузки на боевом стеке | HIST-03 | подмена зависимости сессии в тестах не является генератором | пункт 4 чекпоинта |
| Адаптивность дашборда и истории | сквозной критерий фазы | измерение рендера на реальных ширинах | пункт 5 чекпоинта |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| внешний клиент → JSON-API истории | адреса аутентифицированы и потенциально имеют неизвестных потребителей |
| фаза → следующая фаза | принятые риски и переходящие состояния передаются документом, а не устно |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-41 | Information Disclosure | JSON-сводка по журналу | high | mitigate | сводка считается по идентификатору текущего пользователя, полученному из зависимости аутентификации; условие владения стоит внутри модуля аналитики; существующий тест отказа неавторизованному сохраняется |
| T-04-42 | Repudiation | тихое наследование принятых рисков | medium | mitigate | отдельный файл `04-SECURITY.md` по образцу Фазы 3 сводит регистры десяти планов, называет принятые риски поимённо с severity (T-04-27, T-04-28, T-04-21, T-04-02, T-04-43) и остатки под смягчёнными угрозами (T-04-37 — гонка при нескольких рабочих процессах, T-04-38 — сверка источника закрывает только форму повтора), а остаток по остальным формам проекта выносится отдельным разделом-рекомендацией |
| T-04-43 | Denial of Service | накат четырёх ревизий на боевую базу разом | medium | transfer | решение и окно наката принадлежат владельцу; фаза выносит размен на чекпоинт и не применяет ревизии сама |
| T-04-SC | Tampering | установка пакетов npm/pip/cargo | low | accept | фаза не установила ни одного пакета; таблица аудита легитимности в исследовании пуста по существу |
</threat_model>

<verification>
- `just test` — зелено целиком
- Число собранных тестов не ниже базового уровня 1094
- Граф знаний пересобран
- Карта «требование — тест» покрывает все девять требований фазы
- `04-SECURITY.md` сводит регистры десяти планов, называет принятые риски и остатки и содержит рекомендацию отдельной задачи по защите форм проекта
- Пять ручных пунктов подтверждены человеком
</verification>

<success_criteria>
- В проекте один ответ на вопрос «сколько было ошибок»
- Суита зелена целиком
- Все девять требований фазы имеют либо названный автотест, либо названный ручной пункт — и ни одно не помечено закрытым без того и другого
- Переходящие состояния записаны явно, а принятые риски, остатки и рекомендация отдельной задачи по формам проекта живут в `04-SECURITY.md`
</success_criteria>

<output>
Create `.planning/phases/04-dashbord-i-istoriya/04-10-SUMMARY.md` when done
</output>
