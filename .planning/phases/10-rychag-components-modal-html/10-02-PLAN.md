---
phase: 10-rychag-components-modal-html
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/templates/components/modal.html
  - app/templates/includes/notice_area.html
  - tests/test_templates/test_components.py
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ Это НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а НЕ замер. Калибровки у проекта не
  # существует (estimate-calibration: sample_count 0, factor 1), поэтому
  # tokens и raw_tokens совпадают, а confidence: low ВЫВЕДЕН ИЗ ЧИСЛА
  # ЗАМЕРОВ (ноль), а не из самооценки планировщика.
  # Смарт-зона: 95000 при бюджете 100000 — в бюджете.
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Панель закрывается СУЩЕСТВУЮЩИМ Alpine: выражение x-on:htmx:after-request получает ветвь по успешности обмена, и новых сущностей JS не появляется — ни файла в app/static/js/, ни встроенного обработчика отправки, ни атрибута события htmx, ни узла клиентского состояния (критерий 3 роадмапа, D-13)"
    - "Панель закрывается ТОЛЬКО на успешном обмене: при 500 и при обрыве сети она ОСТАЁТСЯ открытой, а плашка QUAL-03 говорит, что произошло — повторить можно, не открывая панель заново (D-12)"
    - "Число путей снятия панели равно числу путей ухода её узла: узел остался в документе (все location-пути) → закрывает hide(); узел уехал вместе со строкой (фрагментный путь) → снимает внеполосный узел, а destroy() снимает блокировку прокрутки И приземляет фокус (D-10)"
    - "Приземление фокуса выбрано ОДИН раз на всех и живёт в ОБЕИХ ветвях — и в hide(), и в destroy(): площадка — область уведомлений #notice, получившая tabindex=\"-1\"; параметра площадки у макроса нет, 18 вызовов решения о ней не принимают (D-11)"
    - "Ветвление возврата фокуса спрашивает ПРИСУТСТВИЕ открывателя в документе, а не наличие у него метода фокусировки: у отсоединённого узла метод есть, вызов не падает и не делает НИЧЕГО, и фокус молча оставался бы на теле документа (D-11, Landmine 2)"
    - "Снос ЗАКРЫТОЙ соседней панели фокуса не трогает — страж собственного состояния в destroy() сохраняется и является различителем: подмена строки сносит закрытые панели постоянно, и без стража каждая из них уводила бы фокус у открытой (D-10)"
    - "Покрытие обеих ветвей ПОВЕДЕНЧЕСКОЕ, а не разметочное: правила ИСПОЛНЯЮТ объект x-data в интерпретаторе JS со стаб-документом и наблюдают, где оказался фокус, — разметочное правило зеленело бы на вхождении строки и в проекте уже признано недостаточным (план 09-17)"
    - "Копия свойств качества в modal.html получила ПРАВИЛО С ЗУБАМИ: гейт слицает набор свойств качества формы панели с тем, что печатает components/form_wrapper.html, и расхождение краснеет; допустимые различия (порядок узлов и имя класса формы) названы перечнем, а не подразумеваются (D-16)"
    - "Область приземления существует в шелле ОБОИХ приложений и на каждой странице — иначе фокус приземлялся бы в пустоту там, где панель как раз и открыта; отсутствие области краснеет отдельным правилом"
    - "Новых строк JS фаза не добавляет в смысле D-13: правятся ТЕЛА существующих методов и выражение существующего атрибута, и это прочтение критерия 3 записано решением владельца, а не выведено исполнителем"
    - statement: "Фокус после подтверждённого удаления действительно приземлился в область уведомлений, а не остался на теле документа: обход по Tab начинается с осмысленного места, и скринридер объявляет область — ручной UAT фазы, собственное наблюдение сверх пункта 4 перечня"
      verification: backstop
    - statement: "При отказе сервера (500) и при обрыве сети панель осталась открытой и поверх неё видна плашка аварии; повторное подтверждение уходит без повторного открытия панели — ручной UAT фазы, наблюдение к D-12"
      verification: backstop
  artifacts:
    - "app/templates/components/modal.html — ветвь по успешности обмена в x-on:htmx:after-request, приземление фокуса в hide() и в destroy()"
    - "app/templates/includes/notice_area.html — tabindex=\"-1\" на области #notice"
    - "tests/test_templates/test_components.py — сценарии гарнира жизненного цикла для фокуса, правила test_the_panel_closes_only_on_a_successful_exchange, test_the_panel_lands_the_focus_when_its_opener_left_the_document, test_the_teardown_lands_the_focus_too, test_the_teardown_of_a_closed_panel_never_moves_the_focus, test_control_negative_a_panel_landing_on_a_missing_region_reddens_the_gate, test_the_landing_region_exists_in_the_shell_of_both_apps"
    - "tests/test_templates/test_htmx_markup_gates.py — PANEL_QUALITY_PROPERTIES, PANEL_QUALITY_DIFFERENCES_ALLOWED, test_the_panel_quality_properties_match_the_form_wrapper, test_control_negative_a_diverged_quality_property_reddens_the_gate"
  key_links:
    - "выражение x-data панели → гарнир жизненного цикла: единственный способ, которым правила видят ПОВЕДЕНИЕ обоих методов; ломается — правила проверяют вхождение строки и зеленеют на панели, которая фокуса не двигает"
    - "#notice → hide() и destroy(): площадка, выбранная один раз на 18 мест; ломается — фокус после удаления остаётся на теле документа, и обход по Tab начинается сначала на каждом подтверждённом действии"
    - "PANEL_QUALITY_PROPERTIES → form_wrapper.html: единственная связь, по которой расхождение двух копий свойств качества краснеет; ломается — правка цели блокировки в обёртке Фазой 11 разойдётся с панелью молча и сразу на всех 18 местах"
  prohibitions:
    - statement: "оптимистичный UI не вводится: панель не закрывается при неуспешном обмене, потому что закрытая панель после невыполненного действия читается как «всё получилось» — веха это запретила поимённо"
      status: flagged-unverified
      verification: none
    - statement: "второе определение успеха в проекте не заводится: ветвь читает признак успешности события, а не собственное условие по коду ответа, которое молча разошлось бы с правилами responseHandling блока конфигурации Фазы 7"
      status: flagged-unverified
      verification: none
    - statement: "параметр площадки приземления у макроса не заводится — 18 вызовов получили бы по решению, ровно та форма, которую D-11 Фазы 9 уже отверг для цели свопа"
      status: flagged-unverified
      verification: none
    - statement: "форма панели не переводится на макрос-обёртку: у панели свой порядок узлов, своё имя класса формы и две привязки Alpine, а расширять обёртку ради одного потребителя значило бы уводить правку во второй файл, раздающийся остальным сорока шести формам (D-16)"
      status: flagged-unverified
      verification: none
    - statement: "pytest.skip при отсутствующем интерпретаторе JS ЗАПРЕЩЁН ни при какой причине: пропущенное правило неотличимо от зелёного (WARN-4 первого круга Фазы 9)"
      status: flagged-unverified
      verification: none
    - statement: "приземление фокуса на location-пути, где тело документа подменяется целиком, здесь НЕ решается — оно отложено сводному обходу доступности Фазы 15 (GATE-09), и площадка выбирается только для фрагментного пути"
      status: flagged-unverified
      verification: none
---

<objective>
Два механизма снятия панели разводятся по путям и получают ОБЩЕЕ приземление фокуса. Панель
закрывается только на успешном обмене; ветвь возврата фокуса перестаёт спрашивать наличие
метода и начинает спрашивать присутствие узла в документе; площадка приземления выбирается один
раз на все 18 мест — область уведомлений `#notice`. Копия свойств качества панели перестаёт
быть незащищённой копией: заводится правило, слицающее её с макросом-обёрткой.

Purpose: закрыть критерий 3 роадмапа в его прочтении D-13 — панель закрывается СУЩЕСТВУЮЩИМ
Alpine, правятся тела существующих методов, новых сущностей JS не появляется; и закрыть
Landmine 1 и Landmine 2 CONTEXT, на первом из которых уже споткнулся план 09-13.

Output: ветвь по успешности обмена, приземление фокуса в обеих ветвях ухода узла,
`tabindex="-1"` на области уведомлений, шесть исполняющих правил и гейт сличения свойств
качества с отрицательным контролем.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-PATTERNS.md
@.planning/phases/10-rychag-components-modal-html/10-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов фазы — в `10-01-PLAN.md` §«Artifacts this phase produces».
Этим планом создаются:

- `tabindex="-1"` на области `#notice` в `app/templates/includes/notice_area.html`
- сценарии гарнира жизненного цикла панели для наблюдения фокуса (`focus_after_hide`,
  `focus_after_teardown`, `focus_untouched_by_closed_sibling`) и расширение стаб-документа
  гарнира разрешением узла по идентификатору
- `PANEL_QUALITY_PROPERTIES`, `PANEL_QUALITY_DIFFERENCES_ALLOWED` —
  `tests/test_templates/test_htmx_markup_gates.py`
- имена правил: `test_the_panel_closes_only_on_a_successful_exchange`,
  `test_the_panel_lands_the_focus_when_its_opener_left_the_document`,
  `test_the_teardown_lands_the_focus_too`,
  `test_the_teardown_of_a_closed_panel_never_moves_the_focus`,
  `test_control_negative_a_panel_landing_on_a_missing_region_reddens_the_gate`,
  `test_the_landing_region_exists_in_the_shell_of_both_apps`,
  `test_the_panel_quality_properties_match_the_form_wrapper`,
  `test_every_allowed_quality_difference_carries_a_reason`,
  `test_control_negative_a_diverged_quality_property_reddens_the_gate`
</artifacts_this_phase_produces>

<tasks>

<task type="execute" tdd="true">
  <name>Задача 1: Закрытие по успеху, приземление фокуса в обеих ветвях ухода узла</name>

  <files>
app/templates/components/modal.html,
app/templates/includes/notice_area.html,
tests/test_templates/test_components.py
  </files>

  <read_first>
- `app/templates/components/modal.html` — **читать ЦЕЛИКОМ.** `:22-47` — изъятие контракта
  возврата фокуса и прямое назначение приземления ЭТОЙ фазе: абзац обязан быть помечен
  исполненным, а не удалён; `:39-47` — вторая половина изъятия и указание, что `destroy()`
  документного состояния касается, а фокуса не трогает ВОВСЕ — ровно это правится;
  `:185-241` — два пути ухода узла, роль стража собственного состояния в `destroy()` и
  запись «число путей снятия равно числу путей ухода»; `:270-278` — объект `x-data` со всеми
  тремя правящимися местами; `:290-291` — открывающий тег формы после плана 10-01.
- `app/templates/includes/notice_area.html:74-78` — обе области шелла: `#notice` (`role=
  "status"`, живость polite) и `#notice-alert` (`role="alert"`, живость assertive); шапка
  файла объясняет, почему ось одна — вариант записи, и почему областей ровно две.
- `app/templates/base.html` — оболочка: ориентира `<main>` в ней НЕТ (проверено при
  обсуждении), поэтому `#notice` не «один из», а единственный кандидат такого рода. Прочитать
  и убедиться самостоятельно, а не поверить записи.
- `tests/test_templates/test_components.py:1400-1500` — `SCROLL_LOCK_CLASS`, `MODAL_XDATA_RE`
  и `MODAL_LIFECYCLE_HARNESS` целиком: стаб-документ (`documentElement`, `activeElement`),
  `build()` с подставными `$nextTick` и `$refs`, три сегодняшних сценария и их возвращаемые
  поля.
- `tests/test_templates/test_components.py:1500-1585` — `DEAD_TEARDOWN`,
  `_modal_xdata_expression` с его границей «одно выражение на поданную разметку»,
  `_xdata_with_dead_teardown` с ДВОЙНЫМ ПРЕДОХРАНИТЕЛЕМ подстановки, `_run_modal_lifecycle`.
- `tests/test_templates/test_components.py:1584-1660` — обязательная структура правил:
  несущее + положительный контроль + проверка собственного состояния + отрицательный контроль +
  идемпотентность (`repeat_matches`).
- `tests/conftest.py` — `run_node_script` и записанная там причина, по которой отсутствие
  интерпретатора РОНЯЕТ правило, а не пропускает его.
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-17-SUMMARY.md` —
  как закрывалась предыдущая ветвь того же объекта: что именно было признано недостаточным
  покрытием и чем заменено.
- `10-CONTEXT.md` §Landmines — первые два пункта дословно: порядок событий на фрагментном пути
  (событие приходит ПОСЛЕ свопа, то есть после того, как внеполосный узел уже снял панель) и
  ветвление по присутствию узла вместо наличия метода.
  </read_first>

  <precondition>
План 10-01 приземлён: макрос печатает атрибуты безусловно, снятых параметров в сигнатуре нет.
Проверить `uv run pytest tests/test_templates/test_components.py tests/test_pages/test_editor_schedules.py -q`
— зелено. Интерпретатор Node доступен: `uv run pytest tests/test_templates/test_components.py -k "scroll_lock" -q`
проходит, а не пропускается.
  </precondition>

  <reversibility rating="costly">
Задача исполняет D-10 и D-11: площадка приземления раздаётся 18 местам одним значением, и
поменять её потом значит перечитать каждое место на предмет того, что там вообще есть область
уведомлений. `one-way` НЕ заслужено — правка живёт в одном файле и откатывается обратной
правкой; ни данных, ни внешнего состояния она не трогает. Чекпойнта решения перед задачей не
ставится: ни одно решение фазы не имеет рейтинга `one-way`.
  </reversibility>

  <behavior>
RED — до правки объекта клиентского состояния:

1. `test_the_panel_closes_only_on_a_successful_exchange` — разметочная половина: выражение
   атрибута `x-on:htmx:after-request` содержит признак успешности события и вызов закрытия;
   поведенческая половина: сценарий гарнира, который вызывает выражение с признаком успеха
   `true` (панель закрылась, признак блокировки прокрутки снят) и с `false` (панель ОСТАЛАСЬ
   открытой, признак блокировки на месте).
2. `test_the_panel_lands_the_focus_when_its_opener_left_the_document` — сценарий: панель
   открыта, открыватель ОТСОЕДИНЁН от документа (признак присутствия у него `false`, но метод
   фокусировки есть), вызывается закрытие → сфокусированным оказывается узел области
   приземления, а НЕ открыватель и не тело документа. Правило обязано отличать «фокус ушёл на
   площадку» от «фокус не двигался»: гарнир возвращает идентификатор последнего
   сфокусированного узла, а не булево.
3. `test_the_teardown_lands_the_focus_too` — то же на ВТОРОМ пути ухода узла: панель открыта,
   вызывается снос → фокус на площадке. Правило существует потому, что на фрагментном пути
   закрытие не вызывается ВОВСЕ, и обязательство, повешенное только на него, там не работает.
4. `test_the_teardown_of_a_closed_panel_never_moves_the_focus` — проверка собственного
   состояния: снос ЗАКРЫТОЙ соседней панели фокуса не трогает. Сценарий строит две панели,
   открывает одну, сносит другую и утверждает, что последний сфокусированный узел не изменился.
5. `test_control_negative_a_panel_landing_on_a_missing_region_reddens_the_gate` — отрицательный
   контроль: в стаб-документе площадки НЕТ, и несущие правила 2 и 3 на нём КРАСНЕЮТ.
6. `test_the_landing_region_exists_in_the_shell_of_both_apps` — область приземления с признаком
   программной фокусируемости присутствует в шелле; правило читает исходник включаемого файла
   области уведомлений и утверждает, что признак стои́т именно на области `role="status"`, а не
   на её соседке.
7. Идемпотентность: каждый новый сценарий прогоняется ДВАЖДЫ, и правила утверждают совпадение
   вердиктов (поле `repeat_matches`) — по действующей структуре правил панели.

GREEN — ветвь по успешности обмена, приземление в обеих ветвях, признак фокусируемости области.
  </behavior>

  <action>
**1. `app/templates/includes/notice_area.html` — площадка (D-11).**
Область `role="status"` (`:77`) получает признак программной фокусируемости `tabindex="-1"`.
Вторая область (`role="alert"`) его НЕ получает: площадка выбирается ОДНА, и вторая
фокусируемая область немедленно поставила бы вопрос, какая из них площадка. Рядом пишется
комментарий, называющий: кто и зачем сюда приземляется (панель подтверждения обоих путей ухода
узла, 18 мест), почему выбрана именно эта область (она присутствует на каждой странице обоих
шеллов, а ориентира `<main>` в оболочке нет), и ЦЕНУ выбора прямым текстом — на успехе область
пуста, потому что плашка на успех не выдаётся (D-03), то есть фокус приезжает в осмысленное
МЕСТО, но не в осмысленный ТЕКСТ; на отказном пути он приземляется точно на текст отказа.

**2. `app/templates/components/modal.html` — три точки правки объекта клиентского состояния.**

*Ветвь закрытия (D-12).* Выражение атрибута `x-on:htmx:after-request` на форме панели
дополняется условием по признаку успешности обмена, приходящему в подробностях события:
сброс признака отправки происходит ВСЕГДА, а закрытие — только при успехе. Собственного
условия по коду ответа не заводится: это было бы второе определение успеха в проекте, молча
расходящееся с правилами `responseHandling` блока конфигурации Фазы 7.

*Ветвь `hide()` (D-11, Landmine 2).* Проверка перед возвратом фокуса перестаёт спрашивать,
есть ли у сохранённого открывателя метод фокусировки, и начинает спрашивать, ПРИСУТСТВУЕТ ли он
в документе (свойство `isConnected`). Если присутствует — фокус возвращается на него, как
сегодня. Если нет — фокус приземляется на область `#notice`, найденную по идентификатору.
Идентификатор площадки записывается в объекте ОДИН раз и параметром макроса не становится.

*Ветвь `destroy()` (D-11, Landmine 1).* Приземление фокуса добавляется и сюда — это ВТОРОЙ
путь ухода узла, и на нём закрытие не вызывается вовсе, потому что событие после запроса
приходит ПОСЛЕ свопа, то есть после того, как внеполосный узел уже снял панель вместе с её
формой. Существующий страж собственного состояния сохраняется и является различителем: снос
ЗАКРЫТОЙ соседней панели — а он происходит при каждой подмене строки — фокуса трогать не должен.
Приземление ставится ВНУТРЬ стража, рядом со снятием признака блокировки прокрутки.

Выражение приземления объявляется в объекте один раз (например, отдельным методом объекта) и
зовётся из обеих ветвей: два независимо выписанных приземления разошлись бы молча, и половина
из 18 мест приземлялась бы в одно место, половина — в другое.

**3. Шапка `modal.html`.** Абзацы `:22-47` (изъятие контракта возврата фокуса с прямым
назначением приземления этой фазе) и абзац `:39-47` (утверждение, что снос документного
состояния фокуса не трогает) **НЕ вычёркиваются**: они помечаются исполненными с указанием
фазы и плана, и рядом пишется, ЧТО именно исполнено — площадка выбрана одна на всех,
приземление живёт в обеих ветвях ухода узла, число путей приземления равно числу путей ухода.
Прежние формулировки остаются читаемыми: стереть запись об отменённом обязательстве значило бы
закрыть его удалением следа (образец — записи D-30 и D-32 `.planning/STATE.md`).

**4. Гарнир жизненного цикла (`tests/test_templates/test_components.py`).**
Стаб-документ гарнира расширяется РОВНО настолько, насколько требуют новые сценарии: разрешение
узла по идентификатору, у каждого подставного узла — метод фокусировки, признак присутствия в
документе и опознаваемое имя; гарнир ведёт запись последнего сфокусированного имени и возвращает
её в вердикте. Расширение обязано остаться совместимым с тремя действующими сценариями:
их вердикты после правки гарнира не меняются ни на поле, и это утверждается прогоном
действующих правил блокировки прокрутки ДО и ПОСЛЕ.

Заводятся три сценария: закрытие при отсоединённом открывателе, снос открытой панели, снос
закрытой соседки при открытой панели. Каждый прогоняется дважды и возвращает поле совпадения
вердиктов. Отрицательный контроль исполняется на стаб-документе БЕЗ площадки и обязан доказать,
что подстановка что-то изменила и ИМЕННО то — двойной предохранитель по образцу подстановки
мёртвого пути сноса.

⚠️ Правила ИСПОЛНЯЮТ выражение, а не ищут в нём подстроку. Разметочная половина допустима
только как дополнение к поведенческой: правило на вхождении строки в проекте уже признано
недостаточным (план 09-17, WR-03 четвёртого круга).
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py -q</automated>
    <human-check>Ручной UAT фазы (собственное наблюдение сверх пункта 4 перечня): подтвердить удаление расписания в редакторе и нажать Tab — обход обязан начаться с области уведомлений, а не с начала документа. Отдельно: при отключённой сети подтвердить удаление — панель обязана ОСТАТЬСЯ открытой, а поверх неё появиться плашка аварии.</human-check>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_templates/test_components.py -k "panel_closes_only_on_a_successful_exchange or lands_the_focus_when_its_opener_left or teardown_lands_the_focus_too or teardown_of_a_closed_panel_never_moves_the_focus or landing_region_exists_in_the_shell" -x` — зелено, шесть правил.
- `uv run pytest tests/test_templates/test_components.py -k "control_negative" -x` — зелено: контроль краснит несущие правила на стаб-документе без площадки.
- Действующие правила блокировки прокрутки не задеты расширением гарнира:
  `uv run pytest tests/test_templates/test_components.py -k "scroll_lock or teardown_of_a_closed_panel_keeps_an_open_sibling_locked or delete_response_removes_the_very_node" -x` — зелено.
- `grep -c 'isConnected' app/templates/components/modal.html` возвращает не менее `1` — ветвление спрашивает присутствие узла.
- `grep -c 'detail.successful' app/templates/components/modal.html` возвращает `1` — ветвь закрытия читает признак успешности события ровно в одном месте.
- `grep -c 'tabindex="-1"' app/templates/includes/notice_area.html` возвращает `1`; `grep -c 'id="notice"' app/templates/includes/notice_area.html` возвращает `1`.
- Приземление объявлено ОДИН раз и зовётся дважды: в отрендеренном выражении клиентского состояния идентификатор площадки встречается ровно один раз — проверяется правилом, читающим отрендеренную разметку, а не грепом по шаблону.
- `uv run pytest tests/test_templates/ -q` — зелено целиком.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Панель закрывается только на успешном обмене и при отказе остаётся открытой; ветвление возврата
фокуса спрашивает присутствие открывателя в документе; приземление на область уведомлений живёт
в ОБЕИХ ветвях ухода узла и объявлено один раз; снос закрытой соседки фокуса не трогает;
область получила признак программной фокусируемости; всё шесть правил поведенческие и снабжены
отрицательным контролем и проверкой идемпотентности; отменённые абзацы шапки помечены, а не
стёрты.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: Копия свойств качества панели получает правило с зубами</name>

  <files>
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/components/form_wrapper.html:130-140` — что печатает макрос-обёртка: адрес
  отправки, способ подмены при отсутствии цели, селектор блокировки, селектор индикатора и
  узел индикатора; `:100-129` — шапка, в которой уже названа связь с умолчанием признака
  отправки панели подтверждения.
- `app/templates/components/modal.html:290-295` — форма панели ПОСЛЕ плана 10-01: тот же
  набор свойств, свой порядок узлов, своё имя класса формы, две привязки Alpine.
- `tests/test_templates/test_htmx_markup_gates.py:125-133` — `HX_POST_PLACES` как ближайшее по
  духу правило (счёт числом); `:2686-2796` — `DISABLED_ELT_EXCEPTIONS` и
  `DISABLED_ELT_EXCEPTIONS_DECLARED` как образец перечня с обоснованием на запись и счётом
  ЧИСЛОМ; `:2800+` — разборщик вызывающих `_macro_callers`; `:2855-2882` —
  `MACRO_BORN_EXCEPTIONS` и форма летописи.
- `tests/test_templates/test_htmx_markup_gates.py` §группа контроля (конец файла) — как в
  проекте пишутся отрицательные контроли с подменой одного модуля в словаре исходников и
  почему подмена не имеет права протечь через кэш.
- `10-CONTEXT.md` D-16 — выкладка, по которой рождение формы панели вызовом обёртки отвергнуто,
  и перечень допустимых различий.
- `10-PATTERNS.md` §«No Analog Found» — прямое указание, что операция сличения НАБОРОВ
  атрибутов двух макросов заводится в проекте ВПЕРВЫЕ, и форма — за планировщиком.
  </read_first>

  <precondition>
Задача 1 приземлена; `uv run pytest tests/test_templates/ -q` — зелено.
  </precondition>

  <behavior>
1. `test_the_panel_quality_properties_match_the_form_wrapper` — НЕСУЩЕЕ. Набор свойств
   качества, печатаемых формой панели подтверждения, СОВПАДАЕТ с набором, печатаемым
   макросом-обёрткой. Сравниваются: адрес отправки (обязан печатать ту же переменную, что и
   атрибут действия формы), способ подмены при отсутствии цели, селектор блокировки, селектор
   индикатора и присутствие узла индикатора. Отказ обязан называть РАЗОШЕДШЕЕСЯ свойство и оба
   значения, а не только факт расхождения.
2. Допустимые различия объявлены ПЕРЕЧНЕМ `PANEL_QUALITY_DIFFERENCES_ALLOWED` с обоснованием на
   запись и счётом ЧИСЛОМ: порядок узлов внутри формы и имя класса формы. Всё, что не в
   перечне, — расхождение.
3. `test_control_negative_a_diverged_quality_property_reddens_the_gate` — ОТРИЦАТЕЛЬНЫЙ
   КОНТРОЛЬ: подмена значения селектора блокировки в ОДНОЙ из двух копий краснит несущее
   правило. Подстановка идёт по образцу, встречающемуся в источнике ровно один раз, и
   результат отдельно проверяется на отличие от исходника — иначе контроль зелен по построению.
4. Антивакуум: набор `PANEL_QUALITY_PROPERTIES` непуст, и это утверждается ОТДЕЛЬНО. Пустой
   набор дал бы вакуумно зелёное сравнение, а зелёный цвет сломанного разборщика посимвольно
   совпадает с зелёным цветом соблюдённого правила (D-13 Фазы 8).
  </behavior>

  <action>
Правило заводится в `tests/test_templates/test_htmx_markup_gates.py`, рядом с прочими правилами
разметки, и читает ОБА исходника шаблонов, а не отрендеренную разметку: предмет — то, что
написано в файле, потому что расхождение вносится правкой файла.

Набор сличаемых свойств объявляется именованной константой `PANEL_QUALITY_PROPERTIES` — по
имени атрибута, а не по строке тега, чтобы порядок записи атрибутов различием не считался.
Каждое свойство сопровождается комментарием, называющим, ЧТО оно защищает: адрес отправки —
посимвольное совпадение с действием формы (правило G-4 сравнивает сырые строки шаблона);
способ подмены — отсутствие цели свопа у формы панели (D-11 Фазы 9); селектор блокировки —
QUAL-01 и запрет блокировать кнопку отказа; селектор и узел индикатора — QUAL-02.

Перечень допустимых различий пишется по форме `DISABLED_ELT_EXCEPTIONS`: запись, обоснование
на запись, и отдельная константа `PANEL_QUALITY_DIFFERENCES_ALLOWED` с ЧИСЛОМ, утверждаемым
правилом. Обоснование каждой записи обязано называть, почему различие не есть расхождение:
у панели свой порядок узлов, потому что слот скрытых полей стои́т между текстом и кнопками, а
обёртка печатает индикатор ПОСЛЕ слота; имя класса формы у панели своё, потому что таблица
стилей опирается на него, а обёртка класса формы не знает вовсе.

В докстринге правила записать основание D-16 целиком и назвать, ЧТО правило НЕ делает: оно не
требует рождения формы панели вызовом обёртки — этот вариант отвергнут по выкладке, — и не
запрещает обёртке расти параметрами; оно требует, чтобы ДВЕ КОПИИ ОДНОГО НАБОРА не разошлись
молча. Отдельной строкой назвать событие, ради которого правило заводится: правка цели
блокировки в обёртке Фазой 11 разошлась бы с панелью молча и сразу на всех 18 местах, потому
что до этой фазы копия касалась одного места, а теперь касается всех.

Отрицательный контроль пишется по действующей форме группы контроля файла: подменяется ОДИН
модуль в словаре прочитанных исходников, подмена не имеет права протечь в следующий тест через
кэш чтения.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "panel_quality_properties_match_the_form_wrapper" -x` — зелено.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "control_negative_a_diverged_quality_property" -x` — зелено: контроль краснит несущее правило на подделке.
- Антивакуум объявлен числом: `grep -v '^#' tests/test_templates/test_htmx_markup_gates.py | grep -cE 'PANEL_QUALITY_DIFFERENCES_ALLOWED = [0-9]+'` возвращает `1`.
- Каждая запись перечня допустимых различий несёт обоснование: число строк комментария внутри блока перечня не меньше числа записей — проверяется правилом `test_every_allowed_quality_difference_carries_a_reason`, а не глазами.
- `uv run pytest tests/test_templates/ -q` — зелено целиком, включая всю группу контроля.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Две копии набора свойств качества связаны машинным правилом; допустимые различия объявлены
перечнем с обоснованием на запись и утверждены числом; расхождение любого сличаемого свойства
краснеет, и это доказано отрицательным контролем на подделке, а не заявлено.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ сервера → выражение клиентского состояния панели | признак успешности обмена приходит подробностями события рантайма и решает, закрывать ли панель |
| подставная площадка приземления → фокус пользователя | идентификатор области зашит в макросе и подставляется в поиск узла в документе |
| шелл обоих приложений → область уведомлений | одна и та же область служит и площадкой фокуса, и целью внеполосной подмены исхода |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Spoofing | закрытие панели по подделанному признаку успеха | medium | mitigate | Признак берётся из подробностей события рантайма htmx, а не из тела ответа и не из собственного условия по коду: подделать его может только код, уже исполняющийся в документе пользователя. Второго определения успеха в проекте не заводится |
| T-10-10 | Denial of Service | панель, оставшаяся открытой при отказе | low | accept | Принято сознательно (D-12): открытая панель при 500 и при обрыве сети есть ЖЕЛАЕМОЕ поведение — повторить можно, не открывая её заново. Закрытая панель после невыполненного действия была бы дефектом, а не удобством |
| T-10-11 | Information Disclosure | приземление фокуса в область уведомлений | low | accept | Область на успехе пуста (плашка на успех не выдаётся, D-03), на отказе несёт код из ЗАКРЫТОГО реестра, а не текст владельца ссылки. Ничего сверх того, что уже видно на экране, фокус не раскрывает |
| T-10-12 | Tampering | идентификатор площадки, зашитый в макросе | low | mitigate | Величина литеральная, из параметров макроса не приходит и вызывающим не переопределяется; правило существования области в шелле краснеет, если область исчезнет или переедет |
| T-10-13 | Elevation of Privilege | снос закрытой соседней панели уводит фокус у открытой | medium | mitigate | Страж собственного состояния в ветви сноса сохраняется и утверждается отдельным правилом `test_the_teardown_of_a_closed_panel_never_moves_the_focus`; без него каждая подмена строки уводила бы фокус с открытой панели, включая административные подтверждения |
| T-10-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не выполняет ни одной установки: ни новой зависимости, ни build-шага, ни вендоренного файла. Разведки у фазы нет по решению роадмапа — предмета аудита легитимности не существует |
</threat_model>

<verification>
- `uv run pytest tests/test_templates/ -q` — зелено после каждой задачи.
- `uv run pytest tests/ -q` — зелено на слиянии волны, за вычетом известного красного
  `full-suite-ads-editor-order-pollution`.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- Правило блокировки прокрутки и его четыре соседа зелены ПОСЛЕ расширения гарнира — расширение
  не имеет права изменить вердикты действующих сценариев.
- `graphify update .` после правки кода.
</verification>

<success_criteria>
1. Панель закрывается только на успешном обмене; при отказе остаётся открытой.
2. Ветвление возврата фокуса спрашивает присутствие открывателя в документе, а не наличие
   метода.
3. Приземление фокуса живёт в ОБЕИХ ветвях ухода узла и объявлено в объекте ОДИН раз.
4. Снос закрытой соседней панели фокуса не трогает.
5. Область уведомлений получила признак программной фокусируемости; её присутствие в шелле
   утверждается правилом.
6. Все новые правила ПОВЕДЕНЧЕСКИЕ, с отрицательным контролем и проверкой идемпотентности.
7. Две копии набора свойств качества связаны правилом; допустимые различия объявлены перечнем
   с числом; расхождение доказано краснеющим на подделке контролем.
8. Отменённые абзацы шапки `modal.html` помечены исполненными, а не стёрты.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-02-SUMMARY.md` when done.

Сводка обязана:
- привести вердикты трёх новых сценариев гарнира дословно, включая поле совпадения повторных
  прогонов;
- назвать прочтение критерия 3 роадмапа (D-13) и указать, где оно записано в коде, — план
  10-04 опирается на эту запись;
- назвать оба собственных наблюдения фазы (приземление фокуса, поведение при отказе)
  НЕЗАКРЫТЫМИ машинно и передать их артефакту ручного обхода.
</output>
