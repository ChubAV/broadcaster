---
phase: 10-rychag-components-modal-html
plan: 51
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, ПО ТОЙ ЖЕ ПРИЧИНЕ, ЧТО У ПЛАНОВ 10-41…10-45.
wave: 2
# ⚠️ ВОЛНА 2 — ДВЕ ЗАВИСИМОСТИ ОТ ПЛАНА 10-49, И ОБЕ НАЗЫВАЮТСЯ ПОИМЁННО. ФАЙЛОВАЯ ЗДЕСЬ ВТОРАЯ ПО
# ВАЖНОСТИ, А НЕ ПЕРВАЯ.
# (1) СМЫСЛОВАЯ, И ОНА НЕСУЩАЯ. Настоящий план снимает с подъёма плашки условие открытой панели, то
#     есть делает плашку ПЛАВАЮЩЕЙ У ВЕРХНЕГО КРАЯ ВСЕГДА, пока она видима. До плана 10-49 у видимой
#     плашки был РОВНО ОДИН выход с экрана — открытие панели подтверждения. Снять условие ПЕРВЫМ
#     значило бы отгрузить полосу, висящую над каждым экраном до ближайшего открытия панели, — то
#     есть заплатить за достижимость записи отказа её несменяемостью. План 10-49 заводит второй и
#     главный выход: успешный обмен. Порядок «сначала выход, потом подъём» обязателен, и обратный
#     порядок отгрузил бы промежуточное состояние худшее обоих.
# (2) ФАЙЛОВАЯ. `tests/test_pages/test_shell.py` правит план 10-49 (волна 1); настоящий план правит
#     его вторым. Одновременное исполнение задело бы один файл двумя руками.
depends_on: ["10-49"]
gap_closure: true
gap_ids: [G-10-7]
files_modified:
  - app/static/css/app.css
  - tests/test_pages/test_shell.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ: `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3).
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ПОДЪЁМ ЗАГОТОВОК ОТКАЗА БЕЗУСЛОВЕН: он не зависит ни от одного признака состояния документа, и потому запись отказа достижима глазу И ТОГДА, КОГДА ПАНЕЛЬ УЖЕ УШЛА"
    - "ПРАВИЛО ЗНАЕТ ФОРМУ ВОЗВРАТА ДЕФЕКТА: блок подъёма, обусловленный ЛЮБЫМ признаком-предком, краснит прогон, а не проходит молча"
    - "БЛОК ПОДЪЁМА НЕ ОБЪЯВЛЯЕТ СПОСОБА ОТОБРАЖЕНИЯ: цена этого запрета выросла вместе с безусловностью — объявление перебило бы скрытость заготовок НА КАЖДОМ ЭКРАНЕ, а не только при открытой панели, и получило зубы именно сейчас"
    - "ГАРАНТИЯ, ДЕРЖАВШАЯСЯ АНТИВАКУУМНОЙ ПОЛОВИНОЙ ПРЕЖНЕГО ПРАВИЛА, НЕ ПОТЕРЯНА: рычаг по-прежнему обязан поднимать признак блокировки прокрутки ГЛАГОЛОМ, и это переехало в собственное именованное правило, а не исчезло вместе с условием подъёма"
    - statement: "CR-01 СЕДЬМОГО КРУГА НЕ ВОЗВРАЩАЕТСЯ: гашение обеих заготовок при открытии панели остаётся в рычаге нетронутым, и потому безусловный подъём не может вынести НАД ПАНЕЛЬЮ отказ, к ней отношения не имеющий"
      verification: test
  artifacts:
    - "`app/static/css/app.css` — блок подъёма без условия открытой панели и запись D-30/D-32 рядом с прежним основанием условности, а не вместо него"
    - "`tests/test_pages/test_shell.py` — переключённый на признак предмета отбор блоков подъёма, правило безусловности, правило отсутствия объявления способа отображения и вынесенное именованным правило подъёма признака блокировки прокрутки рычагом"
  key_links:
    - "отбор блоков подъёма по имени заготовки, а не по признаку состояния → работоспособность четырёх контролей: единственное, чем они продолжают находить свой предмет после снятия условия; ломается — четыре контроля зеленеют вакуумом на пустом отборе"
    - "правило безусловности → предмет гэпа: единственное, чем «подъём всегда» отличимо от «подъём при каком-то признаке»; ломается — условие возвращается под другим именем, и плашка снова улетает выше окна после ухода панели"
    - "нетронутое гашение в рычаге → незакрытие CR-01 повторно: единственное, чем безусловный подъём отличим от подъёма, выносящего чужой отказ над кнопкой необратимого удаления; ломается — возвращается дефект, которым фаза была провалена седьмым кругом"
  prohibitions:
    - statement: "`app/templates/` НЕ ПРАВИТСЯ НИ НА СИМВОЛ: предмет плана — ТАБЛИЦА СТИЛЕЙ и модуль суиты; правка рычага открыла бы CR-01, правка заготовок пересеклась бы с планом 10-49"
      status: flagged-unverified
      verification: test
    - statement: "ЧИСЛО СЛОЯ В ПРАВИЛО НЕ ВЫПИСЫВАЕТСЯ: оба слоя читаются из таблицы и сличаются между собой, и правка любого из двух не имеет права разойтись с правилом молча"
      status: flagged-unverified
      verification: test
    - statement: "ВТОРОГО БЛОКА ПОДЪЁМА НЕ ЗАВОДИТСЯ: вторая копия величин разошлась бы с первой молча — блок остаётся ОДИН, меняется его селектор"
      status: flagged-unverified
      verification: test
    - statement: "ПРАВИЛО БЛОКИРОВКИ ПРОКРУТКИ И САМ ПРИЗНАК `is-modal-open` НЕ СНИМАЮТСЯ: он держит собственный предмет (прокрутку списка за открытым окном) и снятие его было бы правкой шире гэпа"
      status: flagged-unverified
      verification: test
    - statement: "ВСЕЛЕННАЯ ПРЕДКОВ `FAILURE_BANNER_ANCESTORS` НЕ ПРАВИТСЯ: её состав снят замером по шеллам и к условию подъёма отношения не имеет"
      status: flagged-unverified
      verification: none
---

<objective>
**Запись отказа улетает выше окна ПОСЛЕ ТОГО, как панель ушла, и объяснения человеку на экране нет
ни одного.** Наблюдено 2026-09-11 обходом `walkthrough_3`, стенд ad_id=59 / sched-165, шаг 2.8, на
НАСТОЯЩЕМ обрыве транспорта (событие отказа отправки, статус 0), а не на подмене транспорта в
документе.

Замер дословно: плашка `htmx-failure-network` поднята верно и текст её верен, но прямоугольник
`{top: −701, left: 294, 944×63}` при `scrollY: 797` — она на **701 px ВЫШЕ окна**, `position:
static`, `z-index: auto`, `elementFromPoint` в её точке её не возвращает. **Глазной вердикт по снимку
экрана: объяснения на экране НЕТ.** Прокруткой вверх плашка достигается (`top: 96`), но повода
прокручивать человеку никто не дал — на экране устаревший редактор с нарисованной карточкой и сводкой
«РАСПИСАНИЯ 1» при `0` на сервере.

**Корень сверен с деревом.** Подъём заготовок, отгруженный планом 10-33, УСЛОВЕН: он включается
признаком блокировки прокрутки (блок `app/static/css/app.css`). На ПЕРЕХОДНОЙ ветке панель уходит по
завершении запроса удаления — то есть ДО того, как поднимется отказ отправки провалившегося
follow-up GET. К моменту показа плашки признак уже снят: подъём выключен, плашка падает обратно в
обычный поток документа, где её видно только при положении прокрутки у верхнего края.

⚠️ **ЭТО ЗЕРКАЛО ГЭПА G-10-4, А НЕ ЕГО ПОВТОР, И РАЗНИЦА НАЗВАНА ЗАПИСЬЮ ГЭПА.** G-10-4 — «плашка
СПРЯТАНА ПОД панелью, ПОКА панель открыта»; он снят подъёмом 10-33 и наблюдён закрытым ТРИЖДЫ.
Настоящая запись — «плашка УЛЕТЕЛА ВЫШЕ ОКНА ПОСЛЕ ТОГО, как панель ушла», и закрывающий G-10-4
подъём её НЕ ДОСТАЁТ ровно потому, что условен по признаку открытой панели. **Настоящий гэп G-10-4 НЕ
ПЕРЕОТКРЫВАЕТ:** его предмет — «пока панель открыта», и в этом окне признак снят.

⚠️ **ПОЧЕМУ ЭТОГО НЕ УВИДЕЛ ПЕРЕЗАМЕР ТОГО ЖЕ ДНЯ.** Он снял шаг 2.8 на стенде ad_id=57 и записал
прямоугольник `{top: 96, …}` — в окне. Та запись ВЕРНА и не вычёркивается: там положение прокрутки
случилось у верхнего края, и плашка попала в окно САМА. **Признак зависит от прокрутки, а не от
дерева, и потому один положительный замер его не закрывает.**

Purpose: сообщение, которое пишется ради случая отказа, обязано быть на экране в момент отказа —
независимо от того, где в документе стои́т прокрутка и ушла ли панель. Сегодня достижимость записи
отказа есть случайность положения прокрутки, и обход поймал обе стороны этой случайности за один
день.

Output: безусловный подъём обеих заготовок и правило, краснеющее на возврате условности в любой её
форме.

⚠️ **ОСНОВАНИЕ УСЛОВНОСТИ, ЗАПИСАННОЕ ПЛАНОМ 10-33, НЕ ВЫЧЁРКИВАЕТСЯ.** Оно было ВЕРНО для своего
дерева: пока снятую плашку не прятал никто, безусловный подъём оставил бы её висеть до конца сессии.
Условие своего пересмотра оно назвало само — и план 10-49 это условие снял, заведя гашение по успеху
обмена. Рядом с прежним основанием встаёт то, что его переросло (идиома D-30/D-32).
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-UAT.md
@.planning/phases/10-rychag-components-modal-html/10-33-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-49-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**`app/static/css/app.css`:** блок подъёма без условия открытой панели (блок ОДИН, меняется его
селектор) и запись D-30/D-32 рядом с заголовком блока и рядом с пунктом (в) о цене условности.

**Новые символы в `tests/test_pages/test_shell.py`:**

- `test_the_failure_banner_lift_is_unconditional` — селектор подъёма адресует заготовку САМ, без
  предка-признака;
- `test_the_failure_banner_lift_declares_no_display_mode` — блок подъёма не объявляет способа
  отображения;
- `test_the_lever_raises_the_scroll_lock_flag` — вынесенная гарантия прежней антивакуумной половины;
- `test_control_a_conditional_lift_reddens` — контроль возврата условности на доктóренной копии.

**Изменённые символы:** `_banner_elevation_rules` (отбор по имени заготовки вместо имени признака
состояния), `test_the_failure_banner_is_declared_above_the_panel_while_it_is_open` (имя выводится
исполнителем: предмет перестал быть «пока панель открыта»), четыре контроля, читающие отбор блоков
подъёма.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: правило безусловности подъёма — RED на сегодняшней таблице</name>

  <files>
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `tests/test_pages/test_shell.py:2994-3010` — `MODAL_PANEL_SELECTOR`, `MODAL_SCROLL_LOCK_FLAG`.
- `tests/test_pages/test_shell.py:3155-3212` — `_css_rules_of`, `_css_layer`, `_css_is_out_of_flow`,
  `_banner_elevation_rules`. ⚠️ ЧИТАТЬ ДОСЛОВНО: отбор блоков подъёма СЕГОДНЯ ключáется на ИМЯ
  ПРИЗНАКА СОСТОЯНИЯ, и это ровно то, что снимается. Отбор, оставленный как есть, после правки
  таблицы дал бы ПУСТОЕ множество, и четыре зависимых контроля зазеленели бы ВАКУУМОМ.
- `tests/test_pages/test_shell.py:3212-3280` — `_banner_elevation_findings` целиком, включая абзац о
  том, почему находки считаются по-разному для двух родов расхождений.
- `tests/test_pages/test_shell.py:3277-3335` — `test_the_failure_banner_is_declared_above_the_panel_
  while_it_is_open`, и ОСОБЕННО его вторая антивакуумная половина: сличение имени признака с
  исходником рычага. ⚠️ ЭТА ПОЛОВИНА ДЕРЖИТ ОТДЕЛЬНУЮ ГАРАНТИЮ, которая при снятии условия теряет
  своё место — и обязана ПЕРЕЕХАТЬ, а не исчезнуть.
- `tests/test_pages/test_shell.py:3337-3570` — четыре контроля и правило разбора блока, все читающие
  `_banner_elevation_rules`.
- `app/static/css/app.css:940-1130` — правило блокировки прокрутки, весь комментарий блока подъёма и
  сам блок. ⚠️ ЧИТАТЬ ДОСЛОВНО пункты (а), (б), (в), (г), (д) и последний абзац о способе
  отображения: настоящий план правит (в) и заголовок, а (а), (б), (г), (д) оставляет.
  </read_first>

  <behavior>
1. Отбор блоков подъёма находит СЕГОДНЯШНИЙ блок по имени заготовки — то есть работает и до правки
   таблицы, и после неё; на пустом отборе правила краснеют, а не зеленеют.
2. `test_the_failure_banner_lift_is_unconditional` СЕГОДНЯ КРАСЕН: селектор подъёма несёт предка.
3. `test_the_lever_raises_the_scroll_lock_flag` ЗЕЛЁН уже сегодня — гарантия перенесена, а не
   заведена заново, и это видно тем, что цвет она не меняет.
4. `test_the_failure_banner_lift_declares_no_display_mode` ЗЕЛЁН уже сегодня — запрет действует, но
   до сих пор держался прозой, а не правилом.
  </behavior>

  <action>
**(1) ОТБОР БЛОКОВ ПОДЪЁМА ПЕРЕКЛЮЧАЕТСЯ НА ПРИЗНАК ПРЕДМЕТА.** `_banner_elevation_rules` отбирает
блоки, чей селектор адресует ЗАГОТОВКУ (имя берётся из `FAILURE_BANNER_IDS`), и перестаёт спрашивать
про имя признака состояния. ⚠️ ЭТО ДЕЛАЕТСЯ ПЕРВЫМ ШАГОМ И ДО ПРАВКИ ТАБЛИЦЫ: четыре контроля и
`_banner_elevation_findings` читают этот отбор, и отбор, привязанный к снимаемому условию, превратил
бы их в зелёную пустоту ровно в момент правки. ⚠️ ОТБОР НЕ КЛЮЧÁЕТСЯ НА ПОЛОЖЕНИЕ ВНЕ ПОТОКА: это
одна из находок, и предмет, попавший в отбор, перестал бы быть находимым.
`_banner_elevation_findings` при этом сохраняет свою арифметику находок дословно: отсутствие блока —
находка на КАЖДУЮ заготовку, низкий слой — находка на блок.

**(2) RED — ПРАВИЛО БЕЗУСЛОВНОСТИ.** `test_the_failure_banner_lift_is_unconditional` разбирает
селектор блока подъёма на составные части и требует: часть, адресующая заготовку, стои́т в селекторе
ОДНА — ни предка перед ней, ни дополнительной простой части при ней. ⚠️ УТВЕРЖДЕНИЕ ФОРМУЛИРУЕТСЯ
ПОЛОЖИТЕЛЬНО («часть равна адресу заготовки»), а не отрицанием конкретного имени признака: правило,
знающее одно имя, пропустило бы условность, вернувшуюся под другим. Прогнать на нетронутом дереве и
записать RED замером: команда, код возврата, полученный селектор дословно.

**(3) ГАРАНТИЯ ПРЕЖНЕЙ АНТИВАКУУМНОЙ ПОЛОВИНЫ ПЕРЕЕЗЖАЕТ В СОБСТВЕННОЕ ИМЯ, А НЕ ИСЧЕЗАЕТ.**
`test_the_lever_raises_the_scroll_lock_flag` утверждает ровно то, что утверждала половина: рычаг
поднимает признак блокировки прокрутки ГЛАГОЛОМ, а не именем в прозе. ⚠️ ЗАЧЕМ ЭТО ОБЯЗАТЕЛЬНО.
Гарантия была ВТОРОСТЕПЕННОЙ ДЛЯ ПОДЪЁМА и ГЛАВНОЙ ДЛЯ ПРОКРУТКИ: признак, который рычаг перестал бы
поднимать, оставил бы список едущим за открытым окном (план 09-13, решение владельца `scroll-lock`).
Снять её вместе с условием подъёма значило бы починить одно и молча потерять другое. Докстринг
называет ОБА предмета и говорит, откуда правило переехало.

**(4) ЗАПРЕТ ОБЪЯВЛЕНИЯ СПОСОБА ОТОБРАЖЕНИЯ ПОЛУЧАЕТ ЗУБЫ.**
`test_the_failure_banner_lift_declares_no_display_mode`: блок подъёма не объявляет способа
отображения. ⚠️ ЦЕНА ЭТОГО ЗАПРЕТА РАСТЁТ ВМЕСТЕ С БЕЗУСЛОВНОСТЬЮ, И ИМЕННО ПОЭТОМУ ПРАВИЛО
ЗАВОДИТСЯ СЕЙЧАС. Скрытость заготовок держит атрибут скрытия через таблицу стилей пользовательского
агента; объявление способа отображения в блоке подъёма перебило бы её. До настоящего плана цена была
«две пустые подложки на каждом открытии панели», после — «две пустые подложки НА КАЖДОМ ЭКРАНЕ
ПРОЕКТА, всегда». Запрет до сих пор держался прозой блока и ничем больше.

**(5) ЧИСЛА СЛОЯ В НОВЫЕ ПРАВИЛА НЕ ВЫПИСЫВАЮТСЯ.** Действующее правило порядка слоёв читает оба
числа из таблицы и сличает их между собой; новые правила о слоях не говорят вовсе и этого свойства не
ослабляют.
  </action>

  <acceptance_criteria>
- `_banner_elevation_rules` отбирает блоки по имени заготовки; на СЕГОДНЯШНЕЙ таблице отбор даёт РОВНО
  ОДИН блок — число приведено в сводке.
- Четыре контроля, читающие отбор, зелены ДО правки таблицы — то есть переключение отбора их предмета
  не тронуло.
- `test_the_failure_banner_lift_is_unconditional` на нетронутой таблице КРАСЕН; полученный селектор
  приведён в сводке дословно.
- `test_the_lever_raises_the_scroll_lock_flag` и `test_the_failure_banner_lift_declares_no_display_
  mode` ЗЕЛЕНЫ уже до правки таблицы; оба состояния названы.
- Утверждение безусловности сформулировано ПОЛОЖИТЕЛЬНО и не знает имени ни одного признака состояния
  — проверяется чтением диффа.
- `git diff --numstat -- app` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "elevation or banner_layer or lift or scroll_lock"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" ПО ЛЮБОМУ правилу, кроме `test_the_failure_banner_lift_is_unconditional` — переключение отбора задело предмет действующих правил</fails_when>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "lift_is_unconditional"; test $? -ne 0</automated>
    <fails_when>команда завершается ненулевым кодом — то есть правило на нетронутой таблице ЗЕЛЕНО, и RED не измерен: правило зеленеет там, где обход замерил плашку на 701 px выше окна</fails_when>
    <automated>git diff --numstat -- app</automated>
    <fails_when>вывод НЕПУСТ — таблица стилей тронута раньше своей задачи, и переход цвета перестал быть измеримым</fails_when>
  </verify>

  <done>
Отбор блоков подъёма переключён на признак предмета и действующие правила от этого не пострадали;
правило безусловности заведено и красно; гарантия прежней антивакуумной половины переехала в
собственное имя и цвета не сменила; запрет объявления способа отображения получил зубы.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: подъём становится безусловным — GREEN, и прежнее основание условности остаётся рядом</name>

  <files>
app/static/css/app.css
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `app/static/css/app.css:1060-1130` — пункты (в), (г), (д), абзац о способе отображения и сам блок
  подъёма. ⚠️ ЧИТАТЬ ДОСЛОВНО ПУНКТ (в): он утверждает, что после закрытия панели слово замера
  «недостижима» перестаёт быть верным, потому что прокрутка живая. ИМЕННО ЭТО УТВЕРЖДЕНИЕ И
  ОПРОВЕРГНУТО обходом — и опровергнуто НЕ ПО ПОСЫЛКЕ (прокрутка действительно живая), а ПО ВЫВОДУ:
  повода прокручивать человеку никто не дал, и глазной вердикт по снимку — объяснения на экране нет.
- `app/static/css/app.css:940-965` — правило блокировки прокрутки. ⚠️ НЕ ТРОГАТЬ: у него собственный
  предмет.
- `tests/test_pages/test_shell.py:3277-3335` — правило порядка слоёв, из которого антивакуумная
  половина переехала задачей 1.
- `.planning/phases/10-rychag-components-modal-html/10-49-SUMMARY.md` — чем именно закрыт выход
  видимой плашки с экрана. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: запись D-30/D-32 настоящей задачи ССЫЛАЕТСЯ на это
  как на снятое условие пересмотра, и ссылка обязана быть верной, а не правдоподобной.
  </read_first>

  <behavior>
1. Блок подъёма адресует обе заготовки САМ; блок остаётся ОДИН, величины в нём не сдвинуты ни одна.
2. `test_the_failure_banner_lift_is_unconditional` становится ЗЕЛЁНЫМ — переход цвета, а не первый
   зелёный прогон.
3. Правило порядка слоёв и правило чистоты цепи предков остаются зелёными: предмет у них другой, и
   правка их не касается.
4. Правило блокировки прокрутки и признак `is-modal-open` остаются в таблице нетронутыми.
  </behavior>

  <action>
**(1) GREEN — ОДНА ПРАВКА СЕЛЕКТОРА, И БОЛЬШЕ НИЧЕГО.** У единственного блока подъёма снимается
предок-условие; величины блока — положение вне потока, отступ сверху, слой, ширина, поля,
скругление, подложка и тень — НЕ ТРОГАЮТСЯ НИ ОДНА. ⚠️ ВТОРОГО БЛОКА НЕ ЗАВОДИТСЯ: вторая копия
величин разошлась бы с первой молча, и это записанное основание самого блока.

**(2) ЗАГОЛОВОК БЛОКА ПРИВОДИТСЯ, И ЭТО ЕДИНСТВЕННАЯ ПРАВКА ЕГО ПЕРВОЙ СТРОКИ.** Прежний заголовок
называет подъём условным по открытой панели; он остаётся в тексте как запись своего дня и рядом с ним
встаёт новый — подъём безусловен. ⚠️ ПУНКТЫ (а), (б), (г) и (д) НЕ ПРАВЯТСЯ: замер обхода
2026-09-09, основание выбора слоя, замер цепи предков и перечень стерегущих правил верны и после
правки. Пункт (д) ДОПОЛНЯЕТСЯ именами новых правил — перечень стерегущего обязан оставаться полным.

**(3) ЗАПИСЬ D-30/D-32 ВСТАЁТ РЯДОМ С ПУНКТОМ (в), А НЕ ВМЕСТО НЕГО.** Прежний текст пункта (в)
остаётся ДОСЛОВНО. Рядом встаёт запись, называющая ЧЕТЫРЕ вещи и ни одной не смягчающая:
  - ВЫВОД пункта (в) ОПРОВЕРГНУТ ЗАМЕРОМ, а посылка его верна: прокрутка после ухода панели
    действительно живая, и плашка действительно достижима прокруткой — но повода прокручивать
    человеку никто не дал. Замер: прямоугольник `{top: −701, …}` при `scrollY: 797`, `position:
    static`, `z-index: auto`, `elementFromPoint` её не возвращает; глазной вердикт по снимку —
    объяснения на экране НЕТ. Стенд ad_id=59 / sched-165, шаг 2.8 обхода `walkthrough_3`, настоящий
    обрыв транспорта;
  - ПОЧЕМУ ЭТОГО НЕ ВИДЕЛ ПЕРЕЗАМЕР ТОГО ЖЕ ДНЯ: он записал прямоугольник в окне, и та запись ВЕРНА
    — там прокрутка случилась у верхнего края. Признак зависит от прокрутки, а не от дерева, и один
    положительный замер его не закрывает;
  - ЧТО СНЯЛО ОСНОВАНИЕ УСЛОВНОСТИ: план 10-49 завёл гашение обеих заготовок по успешному обмену, и
    страх «повиснет до конца сессии», которым условность и оправдывалась, перестал иметь предмет.
    ⚠️ БЕЗ ЭТОЙ ПОЛОВИНЫ ПРАВКА БЫЛА БЫ РАЗМЕНОМ, А НЕ ПОЧИНКОЙ, и порядок двух планов назван прямо;
  - НОВАЯ ЦЕНА, НАЗВАННАЯ ФАКТОМ: обе заготовки, показанные ОДНОВРЕМЕННО, встают в одну точку и
    накрывают друг друга — величины у них общие. Окно было и прежде (при открытой панели), настоящая
    правка его РАСШИРЯЕТ до «всегда, пока обе видимы». Предмета этого гэпа оно не касается, ПОЧИНКОЙ
    ЗДЕСЬ НЕ ЗАКРЫВАЕТСЯ и передаётся следующему кругу НАЗВАННЫМ, а не умолчанным.

**(4) ПЕРЕИМЕНОВАТЬ ПРАВИЛО, ЧЬЁ ИМЯ ПЕРЕЖИЛО СВОЙ ПРЕДМЕТ.** Правило порядка слоёв называется
«…пока панель открыта»; предмета «пока открыта» у подъёма больше нет. Имя выводится исполнителем по
дереву, и в сводке называются ОБА — прежнее и новое, — чтобы читатель, пришедший из сводки 10-33 со
старым именем, нашёл правило.

**(5) ПРОГНАТЬ ПРАВИЛА ЦЕПИ ПРЕДКОВ И ВСЕЛЕННОЙ ОТДЕЛЬНО.** `test_no_ancestor_of_the_failure_banner_
traps_the_fixed_layer` и `test_the_failure_banner_ancestor_universe_is_addressed_by_stylesheet_
selectors` предъявляются зелёными: их предмет — цепь предков точки включения, и настоящая правка её
не касается. ⚠️ ПРЕДЪЯВИТЬ, А НЕ ПРЕДПОЛОЖИТЬ: вселенная предков адресуется в том числе признаком
блокировки прокрутки, и читатель вправе заподозрить пересечение.
  </action>

  <acceptance_criteria>
- `test_the_failure_banner_lift_is_unconditional` ЗЕЛЕНО; названы ОБА состояния — красное до правки и
  зелёное после, с командами и кодами возврата.
- Блок подъёма в таблице ОДИН; ни одна его величина не сдвинута — проверяется чтением диффа.
- Правило блокировки прокрутки и признак `is-modal-open` в таблице нетронуты.
- Прежний текст пункта (в) и прежний заголовок блока присутствуют ДОСЛОВНО; рядом стои́т запись,
  называющая все четыре вещи перечня действия (3), включая новую цену без смягчения.
- Пункт (д) дополнен именами новых правил.
- Правила цепи предков и вселенной предъявлены зелёными отдельным прогоном.
- `git diff --numstat -- app/templates` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо отобрано меньше правил, чем 161 (замер модуля до партии), плюс новые правила планов 10-49 и настоящего</fails_when>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "ancestor"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — правка задела цепь предков или вселенную, чего она касаться не должна</fails_when>
    <automated>git diff --numstat -- app/templates</automated>
    <fails_when>вывод НЕПУСТ — тронута разметка: рычаг (закрытие CR-01) либо заготовки (предмет плана 10-49)</fails_when>
  </verify>

  <done>
Подъём обеих заготовок безусловен, блок остался один и величины его не сдвинуты; прежнее основание
условности стои́т дословно, рядом — запись с опровергнутым выводом, названным источником и новой
ценой; правило, чьё имя пережило предмет, переименовано с указанием обоих имён.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: зубы — контроль возврата условности и проверка, что CR-01 и G-10-4 не вернулись</name>

  <files>
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `tests/test_pages/test_shell.py:3337-3400` — `test_control_a_banner_layer_below_the_panel_reddens`:
  образец контроля на доктóренной копии таблицы, включая абзац о том, почему доктóриваний два.
- `tests/test_pages/test_shell.py:3858-3980` — `test_the_lever_clears_both_failure_banners_when_the_
  panel_opens` и его контроль. ⚠️ ЭТО ДЕЙСТВУЮЩЕЕ ЗАКРЫТИЕ CR-01, и задача обязана предъявить его
  зелёным, а не предположить.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md`, запись гэпа `G-10-4`, блоки
  `remeasured` и `remeasured_2` — три положительных замера признака. Настоящий план обязан оставить
  их верными, и это утверждение проверяется правилом порядка слоёв, а не словом.
  </read_first>

  <behavior>
1. Копия таблицы, где блоку подъёма возвращён предок-условие, КРАСНИТ правило безусловности.
2. Копия, где блок подъёма объявляет способ отображения, КРАСНИТ правило запрета — и это второе
   доктóривание, а не то же самое.
3. Правило порядка слоёв (предмет G-10-4) и правило закрытия CR-01 зелены, и рычаг не тронут.
  </behavior>

  <action>
**(1) ДВА ДОКТÓРИВАНИЯ, И ВТОРОЕ НЕ ИЗБЫТОЧНО.** Первое возвращает условность — доказывает, что
правило видит ВОЗВРАТ дефекта. Второе дописывает блоку способ отображения — доказывает, что правило
запрета видит форму отказа, чья цена ровно этой правкой и выросла. Копии готовятся во временном
каталоге; настоящая таблица не правится ни на символ. Ожидания выписываются ЛИТЕРАЛАМИ, а не
вычисляются из проверяемого источника.
⚠️ КОНТРОЛЬ ВОЗВРАТА УСЛОВНОСТИ ПОДАЁТ ПРЕДКОМ ПРОИЗВОЛЬНЫЙ ПРИЗНАК, А НЕ ТОТ, ЧТО СТОЯЛ ТАМ ДО
ПРАВКИ: правило обязано краснеть на условности КАК ТАКОВОЙ, а не на одном знакомом имени. Иначе
следующий круг вернёт условие под другим именем, и правило промолчит.

**(2) G-10-4 ПРЕДЪЯВЛЯЕТСЯ НЕПЕРЕОТКРЫТЫМ.** Прогнать правило порядка слоёв и правило чистоты цепи
предков; привести имена и коды возврата. Три положительных замера признака G-10-4 (2026-09-11,
стенды ad_id=55 и ad_id=59, слой 70 против 60, прямоугольник целиком в окне, глазной вердикт)
настоящей правкой обязаны остаться верными: слой не сдвинут, положение вне потока не снято, цепь
предков не тронута. ⚠️ ГРАНИЦА НАЗЫВАЕТСЯ ПРЯМО: правила читают ОБЪЯВЛЕНИЯ таблицы, а не отрисовку.
Отрисовку после настоящей правки снимет ЧЕЛОВЕК шагами 4.4 и 2.8 обхода; ни одно правило суиты этого
не утверждает и утверждать не вправе.

**(3) CR-01 ПРЕДЪЯВЛЯЕТСЯ НЕВЕРНУВШИМСЯ.** Прогнать правило закрытия CR-01 с его контролями и
привести пустой вывод `git diff` по рычагу. ⚠️ РАССУЖДЕНИЕ, ПОЧЕМУ БЕЗУСЛОВНЫЙ ПОДЪЁМ CR-01 НЕ
ВОЗВРАЩАЕТ, ЗАПИСЫВАЕТСЯ В СВОДКУ, А НЕ ЗАМЕНЯЕТ ПРОГОН: подъём безусловен, но плашка, погашенная
при открытии панели, НЕ ОТРИСОВЫВАЕТСЯ ВОВСЕ — поднимать нечего. Цепь «гашение при открытии панели →
CR-01 закрыт» проходит мимо условия подъёма и от его снятия не зависит.

**(4) ПОЛНЫЙ ПРОГОН СУИТЫ.** `uv run pytest tests/` целиком; итоговая строка — в сводку числом.

**(5) ЗАПИСАТЬ В СВОДКУ ОСТАТОК ЧЕТЫРЬМЯ ПУНКТАМИ, БЕЗ СМЯГЧЕНИЯ.**
  - две заготовки, показанные одновременно, накрывают друг друга — окно расширено настоящим планом,
    здесь не закрывается, владельца не имеет;
  - отказ ФОНОВОГО запроса, пришедший при открытой панели, по-прежнему встаёт поверх неё и говорит не
    об этом действии (окно, названное сводкой 10-35, настоящим планом не закрывается);
  - настоящий отказ, стёртый открытием панели либо успешным фоновым обменом, обратно не возвращается
    (окна, названные сводками 10-35 и 10-49);
  - ОТРИСОВКА после правки не снята никем: ни одно правило суиты её не утверждает, шаги 4.4 и 2.8
    обхода после настоящей партии обязаны быть ПЕРЕСНЯТЫ, и `status` артефакта обхода терминальным
    не становится — подписи человека на приёмке как не было, так и нет.
  </action>

  <acceptance_criteria>
- Оба доктóривания краснят то, что объявлено; ожидаемые состояния названы в сводке для каждого
  отдельно.
- Контроль возврата условности подаёт ПРОИЗВОЛЬНЫЙ признак-предок, а не тот, что стоял до правки —
  проверяется чтением диффа.
- Настоящий `app/static/css/app.css` контролями не правится: `git status --porcelain` по нему пуст
  после прогона.
- Правило порядка слоёв, правило чистоты цепи предков и правило закрытия CR-01 зелены; имена и коды
  возврата приведены.
- `git diff --numstat -- app/templates` пуст.
- `uv run pytest tests/` зелен; итоговая строка приведена числом.
- Остаток назван четырьмя пунктами, включая прямое утверждение о непереснятой отрисовке и о
  нетерминальном состоянии артефакта обхода.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "lift or elevation or ancestor or lever_clears"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed"</fails_when>
    <automated>uv run pytest tests/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed" — партия обязана оставить суиту зелёной целиком</fails_when>
    <automated>git status --porcelain app/static/css/app.css app/templates</automated>
    <fails_when>вывод показывает НЕЗАФИКСИРОВАННУЮ правку после прогона контролей — контроль тронул настоящую таблицу вместо копии, либо тронута разметка</fails_when>
  </verify>

  <done>
У правила безусловности и у правила запрета объявления способа отображения есть контроли,
краснеющие на возврате дефекта в общей форме, а не на одном знакомом имени; G-10-4 и CR-01
предъявлены незакрытыми заново; полная суита зелена; остаток назван четырьмя пунктами, включая
непереснятую отрисовку.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| таблица стилей → видимость канала отказа | объявления слоя и положения решают, увидит ли человек единственное объяснение аварии |
| признак состояния документа → правила стиля | класс, поднимаемый сценарием, участвовал в решении о видимости и перестаёт участвовать |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-51-01 | Repudiation | запись отказа, недостижимая глазу после ухода панели | high | mitigate | подъём становится безусловным; форма возврата дефекта краснится `test_the_failure_banner_lift_is_unconditional` и контролем, подающим ПРОИЗВОЛЬНЫЙ признак-предок |
| T-10-51-02 | Denial of Service | объявление способа отображения в блоке безусловного подъёма | high | mitigate | заведено `test_the_failure_banner_lift_declares_no_display_mode`: цена формы отказа выросла с «две пустые подложки при открытии панели» до «на каждом экране всегда», и запрет получил зубы |
| T-10-51-03 | Spoofing | плашка, утверждающая о текущем действии перед необратимым удалением (CR-01) | high | mitigate | гашение обеих заготовок при открытии панели остаётся в рычаге нетронутым; `git diff --numstat -- app/templates` пуст, правило закрытия CR-01 предъявлено зелёным прогоном |
| T-10-51-04 | Denial of Service | две заготовки, показанные одновременно, накрывают друг друга | low | accept | окно существовало и до правки (при открытой панели) и расширено ею; предмета гэпа не касается, названо прямо в прозе таблицы и в сводке, владельца не имеет |
| T-10-51-05 | Tampering | потеря гарантии подъёма признака блокировки прокрутки вместе со снятым условием | medium | mitigate | гарантия вынесена в `test_the_lever_raises_the_scroll_lock_flag` ДО правки таблицы и предъявлена зелёной на обоих деревьях |
| T-10-51-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_shell.py -q` — зелено; число отобранных правил не меньше
   замера 161 плюс новые правила планов 10-49 и 10-51.
2. `uv run pytest tests/ -q` — зелено целиком.
3. Переход цвета правила безусловности измерен и приведён обоими состояниями.
4. `git diff --numstat -- app/templates` пуст на всех трёх задачах.
5. Правило закрытия CR-01 и правило порядка слоёв (предмет G-10-4) предъявлены зелёными отдельно.
</verification>

<success_criteria>
- Подъём обеих заготовок безусловен, и запись отказа достижима глазу независимо от того, открыта
  панель или уже ушла.
- Правило краснеет на возврате условности В ЛЮБОЙ ФОРМЕ, а не на одном знакомом имени признака.
- Гарантия, державшаяся прежней антивакуумной половиной, не потеряна и предъявлена зелёной на обоих
  деревьях.
- G-10-4 и CR-01 не переоткрыты, и это показано прогоном, а не рассуждением.
- Граница доказанного названа прямо: правила читают объявления, отрисовку снимает человек шагами 4.4
  и 2.8, и `status` артефакта обхода терминальным не становится.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-51-SUMMARY.md` when done
</output>
