---
phase: 10-rychag-components-modal-html
plan: 34
subsystem: testing
tags: [uat, walkthrough, anchors, selector, seed, gap-closure]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "обход 2026-09-09 — три расхождения (Р-1, Р-2, Р-3), из них ДВА суть дефекты самого артефакта обхода, и реестр якорей `tests/test_templates/test_walkthrough_anchors.py` с двадцатью записями и объявленным числом"
  - phase: 10-rychag-components-modal-html
    provides: "план 10-33 — условный подъём обеих заготовок плашки отказа в слой выше панели и два стерегущих правила `tests/test_pages/test_shell.py`; по его СВОДКЕ написана приписка к расхождению Р-3"
provides:
  - "несущий счёт проверки 1 обхода — `[id^=\"sched-del-\"][role=\"dialog\"]` в четырёх местах артефакта (шапка, шаги 1.2, 1.5, 1.9, обе строки таблицы «что снимает эта проверка») при ДОСЛОВНО перенесённых ожидаемых числах"
  - "прежняя формулировка несущего счёта, ПОМЕЧЕННАЯ ОПРОВЕРГНУТОЙ по идиоме D-30/D-32: приведена дословно, названы замер `6` при трёх расписаниях, источник расхождения в разметке поимённо и дата обхода"
  - "двадцать первая запись реестра якорей — признак роли диалога у рычага панели со ссылкой на шаг 1.2; объявленное число `21` и летопись `20 → 21` с объяснением двух записей на один литерал разметки"
  - "команда посева артефакта с двузначным форматированием часа и запись раздела «Развилка посева» о том, каким выражением шёл дословно приведённый прогон, где отработало исправленное и что исполнитель плана посева НЕ ЗАПУСКАЛ — при ОДНОЙ ветви раздела"
  - "приписка к расхождению Р-3: выбранный кандидат (а), план 10-33, сделанное, чем стережётся и ПРЕДЕЛ доказанного — объявление слоя, а не отрисовка"
  - "ЗАМЕР: три счёта артефакта до и после правок — 6 / 6 / 6, не сдвинуты; состояние осталось `partial`"
  - "ЗАМЕР: строк файла, несущих исправленный несущий счёт, — было 2, стало 8 (порог гейта задачи 1 — не менее 8)"
  - "НАХОДКА: три из шести мест несли инструмент НЕ ЛИТЕРАЛОМ, а ССЫЛКОЙ (шаг 1.5 «Повторить 1.1 и 1.2», шаг 1.9 «повторить шаги 1.5-1.8», строка таблицы «то же выражение») — правка сделала ссылку явной, а не подменила литерал"
affects: [верификация Фазы 10, повторный ручной обход 10-UAT.md, Фаза 15 (GATE-09)]

actuals:
  tokens: 6395
  tasks: 3
  commits: 3
  plan_head_before: 1e83d3d3b5b6afea293a9c445bcfea3488a79b65

tech-stack:
  added: []
  patterns:
    - "ОБХОД, НАШЕДШИЙ ОШИБКУ В СВОЕЙ ЛИНЕЙКЕ, ПРАВИТ ЛИНЕЙКУ, А НЕ ЧИСЛА: ожидаемое число, сдвинутое заодно с правкой инструмента, есть подгонка ожидания под наблюдение — и она объявляется НАХОДКОЙ, а не исполняется"
    - "ЯКОРЬ ЗАВОДИТСЯ НА КАЖДУЮ ПОЛОВИНУ СОСТАВНОГО СЕЛЕКТОРА, А НЕ НА СЕЛЕКТОР ЦЕЛИКОМ: реестр ведётся по парам «шаг ↔ адресуемый узел», поэтому один литерал разметки может стоять в нём дважды — и причина двух записей пишется прямо, иначе следующий читатель примет её за описку копирования"
    - "КОМАНДА, ЧЬЯ ГОДНОСТЬ УЖЕ ЗАМЕРЕНА ЧУЖИМ ПРОГОНОМ, ПРАВИТСЯ КАК ТЕКСТ И НЕ ЗАПУСКАЕТСЯ: обязательство «исполнить, а не выписать» требует, чтобы записанная команда была ОТРАБОТАВШЕЙ, — и ссылка на замер, назвавший стенд и дату, исполняет его чужими руками, не покупая ноль знания ценой строк в боевой базе"

key-files:
  created: []
  modified:
    - .planning/phases/10-rychag-components-modal-html/10-UAT.md
    - tests/test_templates/test_walkthrough_anchors.py

key-decisions:
  - "Несущий счёт исправлен в четырёх местах, ожидаемые числа перенесены ДОСЛОВНО: `3` до удалений, `3` и `2` на шаге 1.5, `2` и `1` на шаге 1.9, `4 − N` и `3 − N` в таблице. Сдвинуть не захотелось ни одного — панелей было ровно три и тогда, ошибался способ снять число"
  - "Прежняя формулировка НЕ ВЫЧЕРКНУТА, а помечена опровергнутой по форме, уже действующей в этом же файле (летопись ожидания шагов 1.10-1.15): дословная цитата, замер, источник расхождения в разметке поимённо (`id=\"{{ id }}\"` при `role=\"dialog\"` на узле диалога и производный `id=\"{{ id }}-title\"` на заголовке — цель `aria-labelledby`), дата"
  - "Шаг 1.12 НЕ ПРАВЛЕН, и причина написана в тексте правки: его второе ожидаемое число `0` одинаково в обеих линейках. Без этой фразы непрáвленый шаг читался бы как пропущенный"
  - "Двадцать первая запись реестра стои́т на индексе 2, а не 0: индекс 0 подменяет отрицательный контроль своими руками и требует РОВНО ОДНОЙ находки — его жертва не тронута"
  - "Разметка НЕ тронута ни на строку: литерал признака роли взят из рычага таким, каким там стои́т. Дописать узел в шаблон ради зелени правила запрещено самим текстом его отказа"
  - "Команда посева исправлена как ТЕКСТ и НЕ ЗАПУЩЕНА — три самостоятельно достаточных основания названы в артефакте и повторены ниже; в живую базу не записано ни одной строки"
  - "Раздел «Развилка посева» остался при ОДНОЙ ветви — «ПОСЕВ ИСПОЛНЕН»: посев БЫЛ исполнен на четырёх стендах обхода, а неисполнение относится к исполнителю ПЛАНА и записано ВНУТРИ действующей ветви. Вторая ветвь сломала бы единственность, которую раздел держит своим же правилом"
  - "Приписка к Р-3 написана ПО СВОДКЕ 10-33, а не по замыслу: выбран кандидат (а), назван план, названо сделанное, названы стерегущие правила и ПРЕДЕЛ — объявление слоя и порядок объявленных слоёв, а не отрисовка браузером"
  - "Шаг 4.4 пройденным НЕ объявлен, состояние артефакта осталось `partial`: отметки снял агент, а не человек на приёмке, и снять признак Р-3 может только человек в браузере"
  - "`FORM-06` НЕ отмечено выполненным, хотя `requirements.ready-ids` вернул `1/1`: артефакт обхода держит прямой запрет пунктом 3 своего регламента, шаг 4.4 провален, а в таблице прослеживаемости стои́т `Gaps Found`. Отметка `Complete`, поставленная планом, была бы самозаверением"

patterns-established:
  - "Инструмент, названный шагом ССЫЛКОЙ («повторить 1.2», «то же выражение»), при правке инструмента делается ЯВНЫМ, а не оставляется ссылкой: ссылка верна ровно до тех пор, пока читатель дошёл до адресата, и первый же человек, начавший проверку с середины, снял бы число прежней линейкой"
  - "Летопись объявленного числа реестра называет ЧЕТЫРЕ вещи: прежнее значение, ПОЧЕМУ оно не было ошибкой, источник расхождения поимённо и план-правщик — а при появлении двух записей с одним литералом ещё и причину их различия"

requirements-completed: []

coverage:
  - id: D1
    description: "Несущий счёт проверки 1 меряет ровно панели расписаний и назван в четырёх местах артефакта; ни одно ожидаемое число не сдвинуто; прежняя формулировка стои́т помеченной опровергнутой"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_planning/test_the_walkthrough_cannot_self_certify.py → 5 passed, rc=0 (три счёта сходятся, состояние нетерминальное)"
        status: pass
      - kind: other
        ref: "гейт присутствия: grep -c '[id^=\"sched-del-\"][role=\"dialog\"]' 10-UAT.md → 8 при пороге 8 (до правки 2)"
        status: pass
      - kind: other
        ref: "git diff 06957ad — ни одно ожидаемое число не появляется среди изменённых величин: удалённых строк 6, и все шесть суть прежние формулировки инструмента, перенесённые с прежними числами дословно"
        status: pass
    human_judgment: false
  - id: D2
    description: "Вторая половина исправленного селектора заземлена в разметке двадцать первой записью реестра якорей; объявленное число приведено с летописью; отрицательный контроль по-прежнему находит ровно одну подмену"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py#test_every_walkthrough_anchor_exists_in_its_source_template"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py#test_the_number_of_walkthrough_anchors_is_declared"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py#test_control_a_missing_anchor_is_named_by_the_rule"
        status: pass
      - kind: other
        ref: "дословный красный правила числа при заведомо неверном значении снят ДО правки и приведён ниже в разделе «Дословные красные»"
        status: pass
    human_judgment: false
  - id: D3
    description: "Команда посева артефакта форматирует час двумя знаками; прочие значения полей расписания не тронуты; в живую базу не записано ни одной строки"
    requirement: "FORM-06"
    verification:
      - kind: other
        ref: "python3 -c 'print([\"%02d:00\" % (8 + i) for i in range(1, 4)])' → ['09:00', '10:00', '11:00']; прежнее выражение на тех же входах → ['09:00', '010:00', '011:00']"
        status: pass
      - kind: other
        ref: "git diff fb57601 — удалённых строк ОДНА, и это ровно строка `times_of_day`; список групп, день недели, часовой пояс и признак активности не тронуты"
        status: pass
      - kind: other
        ref: "команда посева НЕ ЗАПУСКАЛАСЬ: ни одного обращения к базе за исполнение плана не сделано; годность выражения взята у замера Р-2 обхода (последние два стенда 2026-09-09)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Раздел «Развилка посева» говорит, каким выражением шёл дословно приведённый прогон, где отработало исправленное и что исполнитель плана посева не запускал — при ОДНОЙ ветви; расхождение Р-3 несёт приписку с кандидатом, планом, сделанным и пределом доказанного"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_planning -q → 28 passed, rc=0 (три счёта сходятся, состояние осталось нетерминальным, единственность ветви развилки не нарушена)"
        status: pass
    human_judgment: true
    rationale: "Машинно доказаны ТОЛЬКО счета, состояние и единственность ветви. Читается ли приписка как «закрыто в дереве, но не на экране», а не как закрытие шага 4.4, — суждение, и снять его может только человек, который приписку прочтёт: ровно тот же предел, который приписка сама у себя и объявляет"
  - id: D5
    description: "Полная суита зелена без отбора, включая tests/test_planning/"
    verification:
      - kind: integration
        ref: "uv run pytest tests/ -q → 2902 passed, rc=0 (38:19); базовое число волны 1 — 2902 при коммите 1e83d3d, пол плана — 2897"
        status: pass
    human_judgment: false

duration: 45 min
completed: 2026-09-09
status: complete
---

# Phase 10 Plan 34: линейка обхода перестала мерить не то, что обещает

**Несущий счёт проверки 1 стал `[id^="sched-del-"][role="dialog"]` в четырёх местах артефакта при ДОСЛОВНО перенесённых числах, вторая половина этого селектора заземлена двадцать первой записью реестра якорей, команда посева сеет `09:00`/`10:00`/`11:00` вместо `09:00`/`010:00`/`011:00` — и не запускалась ни разу, — а расхождение Р-3 несёт приписку с выбранным кандидатом, планом 10-33 и пределом доказанного.**

## Performance

- **Duration:** 45 min
- **Started:** 2026-09-09T14:58:14Z
- **Completed:** 2026-09-09T15:43:23Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- **Р-1 закрыт правкой ИНСТРУМЕНТА, а не ожиданий.** Шапка проверки 1, шаги 1.2, 1.5, 1.9 и обе строки таблицы «что снимает эта проверка» называют несущим счётом `[id^="sched-del-"][role="dialog"]`. Ожидаемые числа перенесены дословно и не сдвинуты ни одно.
- **Прежняя формулировка помечена опровергнутой, а не вычеркнута** — по форме, уже действующей в этом же файле: дословная цитата, замер (`6` при трёх расписаниях), источник расхождения в разметке поимённо (`components/modal.html:694,710` — ключ панели на узле диалога; `:713` — производный ключ на заголовке, цель `aria-labelledby`), дата обхода.
- **Вторая половина несущего счёта заземлена в разметке.** Реестр якорей несёт двадцать первую запись — признак роли диалога у рычага панели со ссылкой на шаг 1.2, — объявленное число приведено к `21`, летопись `20 → 21` называет прежнее значение НЕОШИБОЧНЫМ и объясняет, почему литерал `role="dialog"` теперь стои́т в реестре дважды.
- **Р-2 закрыт: команда посева форматирует час двумя знаками.** Правка сделана как ТЕКСТ; команда не запускалась, и три основания запрета записаны в самом артефакте.
- **Р-3 получил приписку по сводке 10-33** — кандидат (а), план, сделанное, стерегущие правила и предел: объявление слоя и порядок объявленных слоёв в таблице стилей, а НЕ отрисовка браузером. Шаг 4.4 прямо объявлен проваленным до человека в браузере.

## Task Commits

1. **Задача 1: несущий счёт проверки 1 меряет то, что обещает (Р-1)** — `06957ad` (docs)
2. **Задача 2: вторая половина селектора заземлена в разметке (Р-1)** — `d02590d` (test)
3. **Задача 3: годное время в команде посева, записи о Р-2 и Р-3** — `fb57601` (docs)

## Files Created/Modified

- `.planning/phases/10-rychag-components-modal-html/10-UAT.md` — несущий счёт в четырёх местах, опровергнутая прежняя формулировка, причина непрáвленого шага 1.12, двузначное форматирование часа, запись раздела «Развилка посева», приписка к Р-3
- `tests/test_templates/test_walkthrough_anchors.py` — двадцать первая запись реестра, объявленное число `21`, летопись `20 → 21` с объяснением двух записей на один литерал

## Замеры — числами, а не словами

| Величина | ДО | ПОСЛЕ |
|---|---|---|
| объявленное число проверок (`checks_declared`) | 6 | 6 |
| разделов `## Проверка N` | 6 | 6 |
| таблиц `### Отметка` | 6 | 6 |
| состояние артефакта (`status`) | `partial` | `partial` |
| строк, несущих `[id^="sched-del-"][role="dialog"]` | 2 | 8 (порог гейта — не менее 8) |
| строк со старым инструментом без уточнения ролью | 4 | 2 (обе — записи о прежнем) |
| длина реестра якорей | 20 | 21 |
| объявленное число реестра | 20 | 21 |
| полная суита `uv run pytest tests/ -q` | 2902 passed (волна 1, `1e83d3d`) | **2902 passed, rc=0, 38:19** |
| строк, удалённых из 10-UAT.md за весь план | — | 7 (6 прежних формулировок инструмента + 1 строка `times_of_day`) |

**Строки со старым инструментом, оставшиеся намеренно:** дословная цитата в блоке «Прежняя формулировка» и «Что записано» расхождения Р-1. Обе суть ЗАПИСЬ О ПРЕЖНЕМ и правке не подлежат.

## Дословные красные

**Правило объявленного числа реестра при заведомо неверном значении** (снято ДО правки, значение возвращено немедленно; ни одного коммита с неверным числом не сделано):

```
E       AssertionError: якорей в реестре 20, а объявлено 99. ПОХУДЕВШИЙ реестр означает,
        что наблюдение СНЯЛИ, не тронув обхода; ВЫРОСШИЙ — что якорь добавили, не назвав
        шага, который его читает. Осознанная правка приводит ОБЪЯВЛЕННОЕ ЧИСЛО и
        дописывает летопись
E       assert 20 == 99
```

Без этого красного правка числа `20 → 21` была бы неотличима от правки числа ПОД правку.

## Команда посева НЕ ЗАПУСКАЛАСЬ — три основания

Запрет исполнен буквально: за весь план не сделано ни одного обращения к базе, и в живую базу не записано ни строки.

1. **Рабочее дерево и прод смотрят в ОДНУ базу — ЗАМЕР, а не опасение.** `walkthrough.environment` шапки артефакта: локальная уборка по `DATABASE_URL` из `.env` сняла строки, наблюдавшиеся на боевом адресе. Команда посева строит движок оттуда же (`get_settings().database_url`); запущенная здесь, она вставила бы в БОЕВУЮ базу аккаунт, объявление и три расписания.
2. **Разрешение человека на прогон 2026-09-09 не наследуется.** Оно было дано ПРИСУТСТВУЮЩЕМУ за клавиатурой на явно заданный вопрос о риске при `workflow.live_dom_uat` равном `false`. План 10-34 автономен.
3. **Прогон не нужен цели правки, и это главное из трёх.** Годность исправленного выражения записал сам обход: «Настоящий обход на последних двух стендах сеял верные значения и предупреждения не получал — то есть починка проверена, а не предложена» (Р-2). Повторение купило бы ноль знания ценой строк в боевой базе.

Обязательство «исполнить, а не выписать» при этом ИСПОЛНЕНО, а не обойдено: оно требует, чтобы записанная команда была ОТРАБОТАВШЕЙ, — исправленное выражение отработало на стендах обхода, и записана ссылка на этот замер, а не второй прогон. Проверка формы сделана без базы: `["%02d:00" % (8 + i) for i in range(1, 4)]` → `['09:00', '10:00', '11:00']`, прежнее выражение на тех же входах → `['09:00', '010:00', '011:00']`.

## Decisions Made

Полный список — во frontmatter (`key-decisions`). Несущие три:

1. **Правился ИНСТРУМЕНТ, а не ожидание.** Ни одно ожидаемое число не сдвинуто, и это проверено диффом, а не объявлено.
2. **Ветвь раздела «Развилка посева» осталась ОДНА.** Посев БЫЛ исполнен на четырёх стендах обхода; неисполнение относится к исполнителю ПЛАНА и записано ВНУТРИ действующей ветви.
3. **`FORM-06` не отмечено выполненным вопреки `1/1` от `requirements.ready-ids`.** Артефакт обхода запрещает это пунктом 3 своего регламента, шаг 4.4 провален, в таблице прослеживаемости стои́т `Gaps Found`.

## Deviations from Plan

### Находки, названные, а не исполненные

**1. [находка, не отступление] Ориентир планировщика по числу мест со старым инструментом разошёлся с замером, и разница предметная.**

- **Найдено при:** Задача 1, пункт (2) действия — «замерить, сколько мест файла несут старый инструмент».
- **Ориентир плана:** шапка, ТРИ шага и ДВЕ строки таблицы — шесть мест.
- **Измерено:** строк с литералом старого инструмента — ЧЕТЫРЕ, из них правке подлежали ТРИ (шапка, шаг 1.2, строка таблицы «панели расписаний ДО удалений»); четвёртая — «Что записано» расхождения Р-1, запись о прежнем.
- **Причина расхождения:** три из шести мест несли инструмент НЕ ЛИТЕРАЛОМ, А ССЫЛКОЙ — шаг 1.5 («Повторить 1.1 и 1.2»), шаг 1.9 («повторить шаги 1.5-1.8») и строка таблицы «панели расписаний ПОСЛЕ N удалений» («то же выражение»).
- **Как исполнено:** ссылка сделана ЯВНОЙ, а не подменена литералом — оба выражения выписаны в шаге целиком. Основание: ссылка верна ровно до тех пор, пока читатель дошёл до адресата, и первый же человек, начавший проверку с середины, снял бы число прежней линейкой. Порог гейта задачи 1 (`не менее 8` строк) при этом сошёлся точно — восемь, — что и подтверждает: планировщик имел в виду ровно эти шесть мест.
- **Числа не сдвинуты:** ни одно.

**2. [Rule 4 — решение вынесено, а не исполнено молча] `FORM-06` не отмечено выполненным.**

- **Найдено при:** шаг `update_requirements` регламента исполнения.
- **Что вернул инструмент:** `requirements.ready-ids` → `1/1 requirement(s) ready to mark complete` (все планы фазы имеют сводки).
- **Почему НЕ исполнено:** артефакт обхода держит прямой запрет пунктом 3 своего регламента — «`FORM-06` переводится в завершённое ТОЛЬКО после прохождения обхода И повторной верификации», — шаг 4.4 обхода ПРОВАЛЕН, а `.planning/REQUIREMENTS.md:136` несёт `Gaps Found`. Отметка `Complete`, поставленная планом поверх вердикта верификации, была бы ровно тем самозаверением, ради запрета которого живёт `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`.
- **Кому переходит:** `/gsd-verify-work` фазы 10 — сведение закрытого делает он, а не артефакт и не план.

---

**Всего отступлений:** 0 автоправок. **Находок, названных и не исполненных:** 2.
**Влияние на план:** нулевое. Обе находки суть записи о РАСХОЖДЕНИИ ОРИЕНТИРА С ЗАМЕРОМ и о ГРАНИЦЕ ПОЛНОМОЧИЙ, а не изменения предмета.

## Issues Encountered

None.

## Что этот план НЕ закрыл

- **Шаг 4.4 обхода остаётся ПРОВАЛЕННЫМ, состояние артефакта — `partial`.** Приписка к Р-3 говорит, чем расхождение закрыто В ДЕРЕВЕ, и ровно этим ограничивается: правила 10-33 утверждают объявление слоя и порядок объявленных слоёв, а не то, что браузер нарисовал плашку поверх панели. Снять признак может только человек в браузере на приёмке.
- **Раздел `## Gaps` не тронут и состояний гэпов не изменено.** Сведение закрытого делает `/gsd-verify-work`; артефакт, объявивший собственные гэпы закрытыми, самозаверяется.
- **Таблицы отметок не тронуты ни в одной клетке.** Они есть запись НАБЛЮДЁННОГО; артефакт приведён К отметке проверки 1, а не наоборот.
- **Продукт не правлен ни на строку.** Предмет плана — артефакт обхода и реестр якорей.
- **Правило, стерегущее ОСМЫСЛЕННОСТЬ наблюдения через якорь, не заведено.** Реестр объявил эту границу у себя дословно, а правила «селектор меряет то, что обещает» не бывает: осмысленность есть суждение, а не признак. Заземлены ОБЕ половины исправленного селектора — это максимум, доступный правилу.
- **Повторный прогон команды посева НЕ ОТЛОЖЕН, А ЗАПРЕЩЁН.** Разница названа: отложенное когда-нибудь делают. Захочет прогона человек — он делает его САМ, за своей клавиатурой, как и 2026-09-09.

## User Setup Required

None.

## Next Phase Readiness

- Обе половины несущего счёта проверки 1 заземлены в разметке; повторный обход снимет числа тем инструментом, который артефакт называет.
- Все три расхождения обхода 2026-09-09 несут записи о том, чем закрыты: Р-1 и Р-2 — в дереве настоящим планом, Р-3 — планом 10-33 с явно названным пределом.
- **Открытым остаётся ОДИН предмет и он адресован человеку:** шаг 4.4 — глазная проверка, что плашка аварии видна поверх панели. Вердикт фазы остаётся `gaps_found` до повторной верификации.
- Следующий вход: `/gsd-verify-work 10`.

## Self-Check: PASSED

- `FOUND: .planning/phases/10-rychag-components-modal-html/10-UAT.md`
- `FOUND: tests/test_templates/test_walkthrough_anchors.py`
- `FOUND: 06957ad`, `FOUND: d02590d`, `FOUND: fb57601`
- `git rev-list --count 1e83d3d..HEAD` → **3** (совпало с числом задач; счёт ИЗМЕРЕН по леднику, а не назван)
- `uv run pytest tests/ -q` → **2902 passed, rc=0** — прогон полный, без отбора и без исключения `tests/test_planning/`

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-09*
