---
phase: 10-rychag-components-modal-html
plan: 23
type: execute
wave: 3
# ⚠️ ЗАВИСИМОСТЬ ОТ ВСЕХ ПЯТИ ПЛАНОВ ВОЛН 1-2 — СЛЕДСТВИЕ РЕШЕНИЯ О ВЛАДЕЛЬЦЕ РЕЕСТРА, И ОНО
# УНАСЛЕДОВАНО ОТ ТРЕТЬЕЙ ПАРТИИ ДОСЛОВНО (10-16-PLAN.md, раздел <objective>). Настоящий план
# дописывает в `.planning/WINDOWS.md` окна, ТЕКСТЫ которых пишет план 10-22, и записывает
# летопись числа планов, которое известно только после того, как состав партии закрыт.
# Волна: max(1, 2) + 1 = 3.
depends_on: ["10-18", "10-19", "10-20", "10-21", "10-22"]
gap_closure: true
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
  - .planning/phases/10-rychag-components-modal-html/10-UAT.md
autonomous: true
requirements: [FORM-06]

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ — форма унаследована
# от планов 10-12…10-22. ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `workflow.security_asvs_level` → `1`;
# `workflow.security_block_on` → `"high"`; `workflow.security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ЛЕТОПИСЬ ЧИСЛА ПЛАНОВ ФАЗЫ ЗАПИСАНА В ТОЙ ЖЕ ФОРМЕ, ЧТО И ТРИ ПРЕДЫДУЩИЕ: «ПРЕЖНЕЕ ЗНАЧЕНИЕ НЕ БЫЛО ОШИБКОЙ — ОНО УСТАРЕЛО», с поимённым источником расхождения и с замером, а не с ориентиром"
    - "МАШИННО ЧИТАЕМОЕ ПОЛЕ СЧЁТА СЛЕДУЕТ ЗА ОБЪЯВЛЕННЫМ ИСТОЧНИКОМ, А НЕ НАОБОРОТ: `progress.total_plans` в `.planning/STATE.md` приведено ТЕМ ЖЕ ДЕЙСТВИЕМ, что и перечень планов роадмапа, и правило `test_the_machine_readable_progress_is_derived_from_the_roadmap` зелено на обоих числах"
    - "ЧИСЛО ИСПОЛНЕННЫХ ПЛАНОВ ФАЗЫ ПРИВЕДЕНО К ЗАМЕРУ, А НЕ ПЕРЕПИСАНО: строка состава раздела фазы говорила «16 планов, из них 11 исполнено», тогда как отметок исполнения в разделе шестнадцать; расхождение названо устаревшим значением, а не ошибкой автора"
    - "ВСЕ ОКНА ПАРТИИ ДОПИСАНЫ ОДНИМ ВЛАДЕЛЬЦЕМ, И НИ ОДНО НЕ ПОТЕРЯНО ПРИ ПЕРЕДАЧЕ: `.planning/WINDOWS.md` объявлен в `files_modified` ровно одного плана партии — настоящего, — и тексты окон перенесены ДОСЛОВНО из сводки плана 10-22"
    - "СЧЁТЧИКИ ШАПКИ РЕЕСТРА СОГЛАСНЫ С ТАБЛИЦЕЙ: запись сделана командой ведения журнала, а не ручной правкой строки, поэтому три места реестра говорят одно"
    - "ЧЕГО ЧЕТВЁРТАЯ ПАРТИЯ НЕ ЗАКРЫЛА — НАЗВАНО В САМОМ АРТЕФАКТЕ ОБХОДА, У КАЖДОГО ПУНКТА СВОЁ ОСНОВАНИЕ, И НИ ОДНО ОСНОВАНИЕ НЕ ЕСТЬ ОЦЕНКА ТРУДА: перечень стоит рядом с перечнем третьей партии и его НЕ подменяет"
    - "АРТЕФАКТ ОБХОДА ПО-ПРЕЖНЕМУ НИЧЕГО НЕ САМОЗАВЕРЯЕТ: состояние не сдвинуто, шесть таблиц отметок остаются ПУСТЫМИ, три счёта проверок остаются равными, и правило плана 10-20 зелено после правки"
    - statement: "Шесть проверок обхода `10-UAT.md` пройдены человеком на локальном стенде, и требование `FORM-06` переводится в завершённое ТОЛЬКО после этого И после вердикта `passed` повторной верификации"
      verification: backstop
  artifacts:
    - ".planning/ROADMAP.md — перечень планов четвёртой партии закрытия гейпов в трёх волнах, строка состава по замеру и летопись числа планов фазы"
    - ".planning/STATE.md — поле `progress.total_plans`, приведённое тем же действием, что и перечень роадмапа"
    - ".planning/WINDOWS.md — окна четвёртой партии, дописанные командой ведения журнала; в их числе окно, передающее ВЛАДЕЛЬЦУ решение об изъятии из запертого D-01, — без сочинённой фазы-снимателя, по форме окон 34 и 47"
    - ".planning/phases/10-rychag-components-modal-html/10-UAT.md — раздел «Чего ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла» с основанием у каждого пункта"
  key_links:
    - "перечень планов роадмапа → поле `progress.total_plans`: единственное, чем два места одного счёта не разъедутся; ломается — правило счёта краснеет от появления новых строк, и его чинят правкой поля постфактум, то есть тем движением, ради снятия которого правило написано"
    - "летопись числа → следующий читатель: единственное, чем сдвинутое число не читается как исправленная ошибка; ломается — прежнее значение выглядит промахом автора, и следующий круг тратит время на поиск виноватого вместо источника"
    - "перечень непокрытого → приёмка фазы: единственное, чем непройденный обход не превратится в закрытое требование; ломается — повторяется коммит `0ea886d`, и на этот раз при живом принуждении, которое партия только что завела"
  prohibitions:
    - statement: "исполненные планы `10-01`…`10-16` НЕ ПРАВЯТСЯ, НЕ ПЕРЕНУМЕРОВЫВАЮТСЯ И НЕ ОТМЕНЯЮТСЯ: правило партии установлено тремя предыдущими и записано в роадмапе, а перенумерация превратила бы архив исполненного в переписанную задним числом историю"
      status: flagged-unverified
      verification: none
    - statement: "таблицы отметок обхода НЕ заполняются этим планом: отметка без наблюдённого признака есть самозаверение, а не приёмка, и с плана 10-20 это ещё и краснит прогон"
      status: flagged-unverified
      verification: none
    - statement: "клетка требования `FORM-06` НЕ переводится в завершённое и флажок требования не заполняется: условие перевода — вердикт `passed` повторной верификации, которого настоящий план не выносит"
      status: flagged-unverified
      verification: none
    - statement: "строка реестра окон НЕ правится руками: ручная правка разошлась бы со счётчиками шапки молча, и три места реестра перестали бы говорить одно — запись ведётся командой ведения журнала"
      status: flagged-unverified
      verification: none
    - statement: "фаза-сниматель окна НЕ СОЧИНЯЕТСЯ там, где её не назвал владелец: прецедент окон 34 и 47 — фаза-владелец не назначена НАМЕРЕННО, и назначение принадлежит владельцу вехи, а не плану"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ НАД FORM-06 — ЯВНЫЕ ДОПУЩЕНИЯ ПАРТИИ (полная запись — 10-17-PLAN.md):
  #   • `idempotency` — продуктовая половина закрыта планом 10-06; повтор в РАНТАЙМЕ браузера
  #     наблюдаем только обходом `10-UAT.md`, шаги 1.4-1.9, не пройденные ни разу. `unresolved`.
  #   • `concurrency` — адресован частично планом 10-07 и проверками 2 и 3 обхода; полного
  #     машинного критерия (два подтверждённых удаления в полёте одновременно) в суите без
  #     процесса выразить нечем. `unresolved`.
  # 2 предъявленных = 0 разрешённых критерием + 2 явных допущения. Оба ПОВТОРЯЮТСЯ разделом
  # артефакта обхода (задача 3), чтобы читатель, пришедший за обходом, а не за планами, увидел
  # их тоже.
---

<objective>
Партия закрывается тремя действиями, которые нельзя сделать раньше, чем известен её состав: летопись
чисел, реестр окон и прямое называние того, чего партия НЕ закрыла.

Purpose: у фазы есть ТРИ записанные летописи («16 → 18», «7/7 → 11/11», «11 → 16»), и каждая
существует потому, что число, верное в момент своей записи, пережило работу, которая его сдвинула.
Четвёртая партия сдвигает то же число снова, и правило `test_the_machine_readable_progress_is_derived_from_the_roadmap`
покраснеет ровно от появления новых строк перечня, если машинно читаемое поле не сдвинуть ТЕМ ЖЕ
ДЕЙСТВИЕМ. ⚠️ ЭТО НЕ ФОРМАЛЬНОСТЬ: правило написано ради того, чтобы поле не приводили ПОСТФАКТУМ
чужим коммитом, и привести его постфактум значило бы обойти его ровно тем движением, ради снятия
которого оно заведено.

⚠️ **ВТОРОЕ ЧИСЛО ТОГО ЖЕ РАЗДЕЛА ТОЖЕ УСТАРЕЛО, И ЭТО ЗАМЕРЕНО, А НЕ ЗАМЕЧЕНО.** Строка состава
раздела Фазы 10 говорит «16 планов, из них 11 исполнено», тогда как отметок исполнения в перечне
раздела ШЕСТНАДЦАТЬ: планы `10-12`…`10-16` третьей партии исполнены после того, как строка была
записана. Прежнее значение НЕ БЫЛО ОШИБКОЙ — оно устарело, и правится не оно, а числа, которые оно
пережило. ⚠️ Тот же предмет частично держит ОКНО 44 реестра, названное отчётом верификации поимённо:
его состояние проверяется ЗАМЕРОМ и, если предмет закрыт, окно закрывается КОМАНДОЙ ВЕДЕНИЯ ЖУРНАЛА,
а не ручной правкой строки.

Output: перечень семи планов партии в трёх волнах и летопись числа планов фазы; машинно читаемое
поле счёта, приведённое тем же действием; окна партии, дописанные одним владельцем; раздел «чего
ЧЕТВЁРТАЯ партия НЕ закрыла» в артефакте обхода.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/10-rychag-components-modal-html/10-UAT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@tests/test_planning/test_state_progress_matches_roadmap.py
</context>

<tasks>

<task type="tracer">
  <name>Задача 1: летопись числа планов фазы и машинно читаемое поле счёта — одним действием</name>

  <files>
.planning/ROADMAP.md
.planning/STATE.md
  </files>

  <read_first>
- `.planning/ROADMAP.md`, раздел `### Phase 10` ЦЕЛИКОМ — от заголовка до заголовка Фазы 11.
  ⚠️ ЧИТАТЬ ВЕСЬ: там четыре критерия успеха, ТРИ уже записанные летописи, строка состава, перечень
  планов, разбитый на волны с подзаголовками партий, и строка о разведке. Форма КАЖДОЙ летописи —
  образец, который наследует четвёртая: объявленное значение, «ПРЕЖНЕЕ НЕ БЫЛО ОШИБКОЙ — ОНО
  УСТАРЕЛО», источник расхождения ПОИМЁННО, чем измерено, каким планом.
- Строка `**Plans**:` раздела — предмет правки. ⚠️ ЧИТАТЬ ВМЕСТЕ С окном 44 реестра, которое ту же
  строку уже держит.
- Подзаголовки волн трёх предыдущих партий (`Закрытие гейпов — волна 1`, `Закрытие гейпов, ВТОРАЯ
  ПАРТИЯ — волна 1`, `Закрытие гейпов, ТРЕТЬЯ ПАРТИЯ — волна 1` и так далее) — форма, которую
  наследуют подзаголовки четвёртой, включая скобочную приписку с источником партии и с напоминанием
  о том, что нумерация волн начинается заново.
- Летопись «11 → 16» ЦЕЛИКОМ — ближайший образец: в ней назван источник расхождения, перечислены
  планы партии с их предметами, отдельно оговорено, что число ИСПОЛНЕННЫХ прибавки не получает, и
  отдельно — что машинно читаемое поле приведено ТЕМ ЖЕ ДЕЙСТВИЕМ.
- `.planning/STATE.md`, frontmatter ЦЕЛИКОМ — блок `progress` и все пять его полей.
- `tests/test_planning/test_state_progress_matches_roadmap.py:60-121` — `roadmap_plan_counts`,
  `roadmap_totals`, `state_progress`. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: правило считает строки `- [x] ` и
  `- [ ] ` ВНУТРИ разделов `### Phase `, и из этого следует, какие именно строки перечня сдвинут
  число, а какие нет.
- Сводки семи планов партии (`10-17-SUMMARY.md` … `10-22-SUMMARY.md`), если они уже есть, — предметы
  планов берутся ИЗ НИХ, а не из памяти: строка перечня, разошедшаяся со сводкой, есть тот же
  документный дрейф.
  </read_first>

  <precondition>
Все планы волн 1 и 2 партии исполнены и их сводки в дереве. Летопись числа, записанная до закрытия
состава партии, устарела бы в момент собственной записи — ровно тот дефект, который она чинит.
  </precondition>

  <behavior>
1. Перечень планов раздела фазы получает СЕМЬ новых строк в отметке «не исполнено», разложенных по
   ТРЁМ волнам под своими подзаголовками. ⚠️ РАСКЛАДКА ВОЛН — ИЗ FRONTMATTER САМИХ ПЛАНОВ, А НЕ ИЗ
   ПАМЯТИ: волна 1 — `10-17`, `10-18`; волна 2 — `10-19`, `10-20`, `10-21`, `10-22`; волна 3 —
   `10-23`. ⚠️ `10-19` СТОИТ ВО ВТОРОЙ ВОЛНЕ, А НЕ В ПЕРВОЙ, И ЭТО РЕШЕНИЕ, А НЕ ОПЕЧАТКА: его
   задача 1 читает `app/pages/` рекурсивно и краснит на нераспознанной форме выражения адреса, тогда
   как `10-18` эту форму в `app/pages/htmx.py` ещё дописывает; пересечения `files_modified` у них
   нет, поэтому гвардия волны связанности не увидела бы, и ребро `depends_on` объявлено руками.
   Раскладку СЛИЧИТЬ с полями `wave` и `depends_on` семи планов ПЕРЕД записью: строка перечня,
   разошедшаяся с frontmatter, есть тот же документный дрейф.
2. Подзаголовки называют партию четвёртой, её источник поимённо (отчёт верификации от 2026-09-04,
   состояние `gaps_found`, счёт `6/9`, `behavior_unverified: 2`, один провал и одна регрессия того
   же корня) и напоминают, что нумерация волн начинается заново.
3. Строка состава несёт ДВА приведённых числа: общее число планов фазы и число исполненных, оба
   СНЯТЫЕ ЗАМЕРОМ по перечню раздела.
4. Летопись числа планов записана в форме трёх предыдущих и называет источник расхождения поимённо.
5. Летопись отдельно оговаривает судьбу ВТОРОГО числа той же строки — числа исполненных — и
   называет прежнее значение устаревшим, а не ошибочным.
6. Машинно читаемое поле общего счёта планов приведено ТЕМ ЖЕ ДЕЙСТВИЕМ; поле числа завершённых
   планов НЕ сдвигается, потому что новые строки не отмечены исполненными.
7. Правило вывода счёта из роадмапа зелено ПОСЛЕ правки обоих файлов и КРАСНО между ними — и это
   наблюдается, а не предполагается.
  </behavior>

  <action>
**(1) Дописать перечень планов.** Семь строк в отметке «не исполнено», под тремя подзаголовками волн
четвёртой партии. Предмет каждого плана взять ИЗ ЕГО СВОДКИ (либо из его `<objective>`, если сводки
ещё нет) и уложить в одну строку по форме соседних шестнадцати. Подзаголовок волны 1 несёт
скобочную приписку с источником партии и с напоминанием о перезапуске нумерации волн; подзаголовки
волн 2 и 3 несут приписку с указанием, на чём они заблокированы, — по форме подзаголовков третьей
партии.

**(2) Снять ДВА числа ЗАМЕРОМ.** Счётом отметок перечня раздела: сколько строк всего и сколько
отмечено исполненными. ⚠️ ЧИСЛА В ПЛАН НЕ ВПИСАНЫ КАК ЦЕЛЬ ПРАВКИ: планировщик замерил дерево ДО
исполнения партии (шестнадцать строк, шестнадцать отметок исполнения) и назвал СВОЙ замер в
`<acceptance_criteria>` как ОРИЕНТИР, а не как значение. Исполнитель ставит ЗАМЕРЕННОЕ и, если оно
разошлось с ориентиром, называет расхождение поимённо — это НАХОДКА, а не помеха.

**(3) Правка строки состава.** Оба числа — по замеру пункта (2).

**(4) Летопись.** Форма — трёх предыдущих летописей этого раздела. Обязательные части:
— объявленное новое значение и прежнее;
— «⚠️ ПРЕЖНЕЕ ЗНАЧЕНИЕ НЕ БЫЛО ОШИБКОЙ — ОНО УСТАРЕЛО» дословно, потому что это форма, а не
  украшение: три предыдущие летописи фазы стоят в ней же;
— ИСТОЧНИК РАСХОЖДЕНИЯ ПОИМЁННО: четвёртый отчёт верификации от 2026-09-04, `status: gaps_found`,
  счёт `6/9`, `behavior_unverified: 2`, ОДИН провал (он же регрессия того же корня) и ЧЕТЫРЕ пункта
  его перечня `missing`; на них и на двенадцать открытых находок ревизии заведены семь планов —
  перечислить их с предметами;
— отдельная оговорка о ВТОРОМ числе строки («из них N исполнено»): прежнее значение `11` было
  верным для момента записи и пережило исполнение планов `10-12`…`10-16`; замеренное — по счёту
  отметок раздела;
— отдельная оговорка о том, что число ИСПОЛНЕННЫХ прибавки от семи новых строк не получает: их
  отметки ставит оркестратор ПО ЗАВЕРШЕНИИ;
— отдельная оговорка о машинно читаемом поле: оно приведено ТЕМ ЖЕ ДЕЙСТВИЕМ, источником счёта
  объявлен роадмап, и поле следует за ним, а не наоборот.

**(5) Привести машинно читаемое поле.** В `.planning/STATE.md` поле общего счёта планов —
ЗАМЕРЕННОЕ значение, выведенное разборщиком правила, а не сложенное в уме. ⚠️ ЗНАЧЕНИЕ БРАТЬ
ПРОГОНОМ РАЗБОРЩИКА (`roadmap_totals` на тексте роадмапа), а не арифметикой: сложение в уме и есть
второй источник счёта, против которого правило написано. Поле числа завершённых планов НЕ ТРОГАТЬ.

**(6) Наблюдать красный между правками.** Правку роадмапа и правку поля сделать РАЗНЫМИ шагами и
между ними прогнать правило: оно обязано быть КРАСНО и назвать оба числа. Вывод привести в сводку.
⚠️ БЕЗ ЭТОГО НАБЛЮДЕНИЯ нельзя отличить «поле приведено» от «правило и без того зелено».

**(7) Что НЕ трогается.** Ни одна из шестнадцати строк перечня и ни одна их отметка. Три прежние
летописи раздела. Четыре критерия успеха, строка о разведке, раздел «Что фаза подвинула в числах».
Прозаические разделы `.planning/STATE.md` — правило их не читает и читать не имеет права, но и план
их не правит. `.planning/REQUIREMENTS.md` — ни на символ.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_state_progress_matches_roadmap.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — два места одного счёта разошлись, и текст отказа назовёт оба числа</fails_when>
    <automated>uv run pytest tests/test_planning/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — правка записи задела гейты записи, заведённые планами 10-17, 10-20 и 10-21</fails_when>
    <automated>grep -c '^- \[ \] 10-' .planning/ROADMAP.md</automated>
    <fails_when>вывод отличен от `7` — планов партии в перечне не семь, то есть строка потеряна при переносе либо дописана лишняя</fails_when>
    <automated>git diff -- .planning/ROADMAP.md | grep -c '^-[^-]'</automated>
    <fails_when>вывод больше `1` — правка удаляет из роадмапа больше одной строки, тогда как её предмет — строка состава плюс дописанные строки; каждая удалённая строка обязана быть названа в сводке поимённо</fails_when>
  </verify>

  <acceptance_criteria>
- Семь строк дописаны и разложены по трём волнам: в сводке приведён вывод третьей команды раздела
  `<verify>` (`7`) и перечень подзаголовков волн.
- Оба числа строки состава СНЯТЫ ЗАМЕРОМ: в сводке приведены команда замера и её вывод. ⚠️ ОРИЕНТИР
  ПЛАНИРОВЩИКА, снятый ДО исполнения партии: `16` строк перечня и `16` отметок исполнения. Если
  замер разошёлся с ориентиром — расхождение названо поимённо, и записано ЗАМЕРЕННОЕ.
- Летопись содержит ВСЕ ШЕСТЬ обязательных частей пункта (4): в сводке приведён её текст.
- Машинно читаемое поле выведено ПРОГОНОМ разборщика: в сводке приведены команда и её вывод —
  пара «завершено, всего», — и записанное в поле значение совпадает со вторым числом пары.
  ⚠️ ОРИЕНТИР ПЛАНИРОВЩИКА, снятый прогоном разборщика ДО партии: пара `(54, 54)`; после семи
  неотмеченных строк ожидается `(54, 61)`. Расхождение с ориентиром — НАХОДКА для сводки.
- КРАСНЫЙ МЕЖДУ ПРАВКАМИ НАБЛЮДЁН: в сводке приведён вывод прогона правила после правки роадмапа и
  ДО правки поля, и в нём видны оба числа.
- Поле числа завершённых планов не тронуто: в сводке приведён вывод
  `git diff -- .planning/STATE.md | grep -c 'completed_plans'` → `0`.
- Шестнадцать прежних строк перечня и их отметки не тронуты: в сводке приведён вывод
  `git diff -- .planning/ROADMAP.md | grep -c '^-.*- \[x\]'` → `0`.
- `.planning/REQUIREMENTS.md` не тронут: `git diff --name-only -- .planning/REQUIREMENTS.md` — пусто.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится запись: перечень планов, строка состава, летопись и одно машинно читаемое поле. Поведения
продукта правка не меняет; откат наблюдаем правилом вывода счёта, которое на несогласованной паре
краснеет по построению.
  </reversibility>

  <done>
Семь планов партии стоят в перечне раздела фазы в трёх волнах; оба числа строки состава сняты
замером; летопись записана в форме трёх предыдущих со всеми шестью обязательными частями; машинно
читаемое поле счёта выведено прогоном разборщика и приведено тем же действием; красный между
правками наблюдён.
  </done>
</task>

<task type="execute">
  <name>Задача 2: все окна четвёртой партии дописаны ОДНИМ владельцем, и состояние окна 44 решено замером</name>

  <files>
.planning/WINDOWS.md
  </files>

  <read_first>
- `.planning/WINDOWS.md:1-20` — шапка реестра ЦЕЛИКОМ (`open_count`, `waived_count`, `fixed_count`,
  `total_count`, `last_updated`) и заголовок таблицы. ⚠️ ТРИ МЕСТА РЕЕСТРА ОБЯЗАНЫ ГОВОРИТЬ ОДНО:
  шапка, таблица и блок записей. Ручная правка строки разошлась бы со счётчиками молча — поэтому
  запись ведётся КОМАНДОЙ ВЕДЕНИЯ ЖУРНАЛА.
- Строки окон **22**, **28**, **34**, **35**, **36**, **44**, **47**, **48** — все, что фаза уже
  завела. ⚠️ ОСОБЕННО **44**: его предмет — строка состава раздела Фазы 10, и задача 1 настоящего
  плана её правит. Состояние окна решается ЗАМЕРОМ, а не впечатлением.
- `10-22-SUMMARY.md` — ТЕКСТЫ ТРЁХ ОКОН, выписанные планом 10-22: об отложенной правке цели третьего
  внеполосного узла, об остатке пункта площадки приземления фокуса и — ТРЕТЬИМ — о расхождении формы
  отказа валидации пяти входов с ЗАПЕРТЫМ решением D-01, чьё изъятие ЖДЁТ ВЛАДЕЛЬЦА. ⚠️ ПЕРЕНОСИТЬ
  ДОСЛОВНО: пересказ через круг разойдётся с оригиналом, и получится второе поколение формулировки
  там, где партия только что пометила первое. ⚠️ У ТРЕТЬЕГО ОКНА ФАЗА-СНИМАТЕЛЬ НЕ НАЗНАЧЕНА
  НАМЕРЕННО — как у окон 34 и 47, и по той же причине: авторство изъятия из запертого решения
  принадлежит владельцу.
- `10-REVIEW.md:517-539` — `IN-04` целиком: предмет (объём комментариев и три поколения формулировок
  в одном месте), предложенная правка (машинно читаемая разметка слоёв) и измеренная стоимость
  ошибки чтения.
- Строка окна **34** и строка окна **47** — образец записи БЕЗ назначенной фазы-снимателя, вместе с
  их основанием: назначение принадлежит владельцу, и сочинять его планом запрещено. Форма нужна
  третьему окну этой задачи.
- Форма вызова команд ведения журнала — снять из `10-16-SUMMARY.md` (третья партия дописывала окна
  ею же) либо из справки самой команды; в план она не вписана намеренно, потому что планировщик её
  не прогонял.
  </read_first>

  <precondition>
Задача 1 приземлена: строка состава раздела Фазы 10 приведена по замеру. Без этого состояние окна 44
решать не на чем — его предмет и есть та строка.
  </precondition>

  <behavior>
1. Реестр получает ЧЕТЫРЕ новых окна: ТРИ с текстами, перенесёнными дословно из сводки плана 10-22
   (третье из них передаёт владельцу решение об изъятии из запертого D-01), и одно об отложенной
   находке `IN-04`.
1a. ⚠️ ЧИСЛО НОВЫХ ОКОН ЕСТЬ ОРИЕНТИР, А НЕ ЗАМОК. Три задачи партии несут ветви «это НАХОДКА»,
   способные ЗАКОННО породить ещё окно: `10-18` задача 2 (маршрут удаления сверх замеренных пяти),
   `10-19` задача 1 (нераспознанная форма выражения адреса), `10-22` задачи 1-2 (достижимость входа
   из интерфейса, расхождение числа входов с отчётом). Окно, рождённое такой ветвью, — РАБОТА партии,
   а не чужая запись, и гейт, покрасивший её как «в реестр писал кто-то ещё», дал бы НЕВЕРНУЮ
   АТРИБУЦИЮ отказа. Поэтому рост сверх ориентира требует ПОИМЁННОЙ АТРИБУЦИИ плану-источнику, а не
   отказа; отказом остаётся окно, которое ни одному плану партии не атрибутируется, и окно
   ПОТЕРЯННОЕ при передаче.
2. Окно `IN-04` заводится БЕЗ назначенной фазы-снимателя и с записанным основанием: машинно
   читаемая разметка слоёв утверждения есть соглашение уровня ВЕХИ, действующее на каждый файл
   проекта, а не побочная правка партии закрытия гейпов.
3. Каждое окно называет предмет, измеренную величину либо воспроизводимый вход, и ОСНОВАНИЕ
   отсрочки, не являющееся оценкой труда.
4. Запись ведётся командой ведения журнала, поэтому счётчики шапки сходятся с числом строк таблицы.
   ⚠️ ИМЕННО ЭТО СОГЛАСИЕ И ЕСТЬ МАШИННЫЙ ПРЕДМЕТ ГЕЙТА, а не абсолютное число строк: согласие трёх
   мест реестра истинно при ЛЮБОМ законном числе окон, а литеральное число ложно в тот день, когда
   партия законно заводит ещё одно.
5. Состояние окна 44 решается ЗАМЕРОМ: закрыт ли его предмет строкой состава после задачи 1.
   Закрыт — окно закрывается командой ведения журнала. Не закрыт — окно остаётся открытым, и
   остаток назван поимённо.
  </behavior>

  <action>
**(1) Перенести ТРИ текста окон из сводки плана 10-22 ДОСЛОВНО.** Сличить каждое перенесённое с
оригиналом и показать совпадение в сводке. ⚠️ ПЕРЕСКАЗ ЗАПРЕЩЁН: партия только что пометила три
поколения одной формулировки как измеренную стоимость, и заводить четвёртое собственным пересказом
было бы самоопровержением. ⚠️ ТРЕТЬЕ ОКНО — РЕШЕНИЕ, ЖДУЩЕЕ ВЛАДЕЛЬЦА: расхождение формы отказа
валидации пяти входов с ЗАПЕРТЫМ D-01. Заводить его БЕЗ фазы-снимателя, по форме окон 34 и 47, и
основание переносить дословно: авторство изъятия из запертого решения владельца принадлежит
владельцу, а не плану. ⚠️ ФОРМУЛИРОВКУ «ИЗЪЯТИЕ ПРИНЯТО» В ОКНО НЕ ПИСАТЬ НИ В КАКОМ ВИДЕ: окно
СТАВИТ ВОПРОС, а не отвечает на него.

**(2) Завести ЧЕТВЁРТОЕ окно — `IN-04`.** Предмет: объём комментариев в правленых файлах фазы превысил
объём кода на порядок, и в одном месте живут три поколения формулировок, действующую из которых
можно определить только полным чтением файла. Измеренная величина — из отчёта ревизии, с указанием
файлов. Стоимость ошибки чтения названа фактом, а не опасением: гэп 1 первого круга верификации
возник ровно из расхождения записи с рантаймом. Предложенная правка — машинно читаемая разметка
слоёв утверждения с правилом, требующим у каждого опровергнутого абзаца действующего преемника.
⚠️ ФАЗА-СНИМАТЕЛЬ НЕ НАЗНАЧАЕТСЯ, и основание записывается: соглашение действует на КАЖДЫЙ файл
проекта, то есть принадлежит владельцу вехи (прецедент окон 34 и 47). ⚠️ И ОТДЕЛЬНО: идиома
«опровергнутое не вычёркивается» есть ЗАПИСАННОЕ РЕШЕНИЕ проекта (D-30/D-32) и дефектом не
является — окно заводится на СТОИМОСТЬ ЧТЕНИЯ, а не на идиому.

**(3) Решить состояние окна 44 ЗАМЕРОМ.** Прочитать его текст, прочитать строку состава после
задачи 1 и сказать прямо: закрыт предмет или нет. Закрыт — закрыть окно КОМАНДОЙ ВЕДЕНИЯ ЖУРНАЛА и
привести вывод команды в сводку. Не закрыт — оставить открытым и назвать остаток поимённо. ⚠️ РУЧНОЙ
ПРАВКОЙ СТРОКИ СОСТОЯНИЕ НЕ МЕНЯТЬ: счётчики шапки разошлись бы с таблицей молча.

**(4) Проверить согласие трёх мест реестра.** После всех записей — счёт строк таблицы и значения
счётчиков шапки; оба привести в сводку. Расхождение означает, что запись сделана мимо команды
ведения журнала, и его надо чинить повторной записью, а не правкой шапки.

**(4a) АТРИБУТИРОВАТЬ КАЖДУЮ ПРИБАВИВШУЮСЯ СТРОКУ.** Снять число строк реестра ДО записи и ПОСЛЕ;
разность назвать поимённо — какое окно каким планом партии порождено. ⚠️ ОРИЕНТИР ПЛАНИРОВЩИКА
(замер 2026-09-05: `grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md` → **48**, `total_count: 48`):
партия пишет ЧЕТЫРЕ окна, то есть ориентир после записи — **52**. ⚠️ ЭТО ОРИЕНТИР, А НЕ ЗНАЧЕНИЕ, И
РАСХОЖДЕНИЕ ВВЕРХ — НАХОДКА, А НЕ ОТКАЗ: три задачи партии несут ветви «это НАХОДКА» (`10-18`
задача 2 — маршрут удаления сверх замеренных пяти; `10-19` задача 1 — нераспознанная форма выражения
адреса; `10-22` задачи 1-2 — достижимость входа и число входов), и окно, ими рождённое, есть РАБОТА
партии. Правило разбора: каждая прибавившаяся строка атрибутируется ПЛАНУ-ИСТОЧНИКУ по его сводке.
Строка, не атрибутируемая НИ ОДНОМУ плану партии, — вот она и означает «в реестр писал кто-то ещё», и
её надо назвать в сводке отдельно, потому что владелец записи в партии объявлен ОДИН. Строк меньше
ориентира — окно ПОТЕРЯНО при передаче, а потерянное окно неотличимо от незаведённого.

**(5) Что НЕ трогается.** Ни одна существующая строка реестра, кроме состояния окна 44 и только
командой. Окна 22, 28, 35 и 36, закрываемые ПРОХОЖДЕНИЕМ обхода: их состояние снимает ЧЕЛОВЕК на
приёмке, а не план. Окна 34 и 47 — их фазы-снимателя не назначены намеренно. Окно 48 — долг
направления отказа планов `10-01`…`10-07`, записанный третьей партией; ⚠️ ЕГО ПРЕДМЕТ НАСТОЯЩАЯ
ПАРТИЯ ТОЖЕ НЕ ПРАВИТ, и это повторяется разделом задачи 3, а не вторым окном.
  </action>

  <verify>
    <automated>test "$(grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md)" = "$(sed -n 's/^total_count: \([0-9]*\)$/\1/p' .planning/WINDOWS.md)" && echo AGREE || echo DISAGREE</automated>
    <fails_when>вывод отличен от `AGREE` — счётчик шапки разошёлся с числом строк таблицы, то есть запись сделана мимо команды ведения журнала, и три места реестра больше не согласны между собой. ⚠️ ГЕЙТ ПОСТРОЕН НА СОГЛАСИИ, А НЕ НА ЛИТЕРАЛЬНОМ ЧИСЛЕ: согласие истинно при ЛЮБОМ законном числе окон, а вшитое число покрасило бы собственный гейт партии с НЕВЕРНОЙ АТРИБУЦИЕЙ в тот день, когда её же ветви «это НАХОДКА» законно родят пятое окно</fails_when>
    <automated>git diff -U0 -- .planning/WINDOWS.md | grep -cE '^\+\| [0-9]+ \| '</automated>
    <fails_when>вывод меньше `4` — партия выписала четыре окна (три из сводки плана 10-22 плюс `IN-04`), и недостающая строка означает окно, ПОТЕРЯННОЕ при передаче, а потерянное окно неотличимо от незаведённого. ⚠️ ВЫВОД БОЛЬШЕ `4` ОТКАЗОМ НЕ ЯВЛЯЕТСЯ САМ ПО СЕБЕ: каждая лишняя строка обязана быть АТРИБУТИРОВАНА плану-источнику партии в сводке (пункт 4a раздела `<action>`); неатрибутируемая строка — вот она и есть отказ</fails_when>
    <automated>uv run pytest tests/test_planning/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — правка реестра задела гейты записи</fails_when>
  </verify>

  <acceptance_criteria>
- ЧЕТЫРЕ окна дописаны, и каждое несёт предмет, измеренную величину либо воспроизводимый вход и
  основание отсрочки: в сводке приведены все четыре строки целиком.
- ПРИБАВКА РЕЕСТРА АТРИБУТИРОВАНА ПОСТРОЧНО: в сводке приведены число строк ДО и ПОСЛЕ и таблица
  «строка → план-источник». ⚠️ ОРИЕНТИР ПЛАНИРОВЩИКА (замер 2026-09-05): было **48**, ожидается
  **52**. Расхождение ВВЕРХ — НАХОДКА, а не отказ, ЕСЛИ каждая лишняя строка атрибутирована плану
  партии; неатрибутируемая строка называется отдельно, потому что владелец записи в партии один.
- ТРЕТЬЕ ОКНО ПЕРЕНЕСЕНО КАК ВОПРОС, А НЕ КАК ОТВЕТ: в сводке приведена его строка целиком и прямо
  сказано, что фаза-сниматель НЕ НАЗНАЧЕНА и что формулировки «изъятие принято» в нём нет — решение
  об изъятии из запертого D-01 принадлежит владельцу.
- ТРИ перенесённых текста СЛИЧЕНЫ с оригиналом: в сводке приведены все три пары «оригинал /
  перенос» и показано совпадение.
- Ни одно основание отсрочки не есть оценка труда: в сводке приведён вывод
  `git diff -- .planning/WINDOWS.md | grep -ci -e 'сложн' -e 'трудоём' -e 'позже'` → `0`.
- Окна `IN-04` И окно расхождения с D-01 заведены БЕЗ фазы-снимателя, и основание записано у
  каждого: в сводке приведены обе цитаты.
- Состояние окна 44 РЕШЕНО ЗАМЕРОМ: в сводке приведены его текст, строка состава после задачи 1,
  прямой ответ «закрыт / не закрыт» и вывод команды ведения журнала, если окно закрывалось.
- Три места реестра согласны: в сводке приведены число строк таблицы и все счётчики шапки.
- Запись велась КОМАНДОЙ: в сводке приведены вызовы команд ведения журнала и их вывод.
- Ни одна существующая строка не тронута руками: в сводке приведён вывод
  `git diff -U0 -- .planning/WINDOWS.md | grep -c '^-[^-]'` вместе с пояснением каждой строки, если
  вывод не равен нулю.
  </acceptance_criteria>

  <reversibility rating="reversible">
Дописываются три строки реестра и, возможно, меняется состояние одной; поведения продукта правка не
меняет. Откат — обратные команды ведения журнала.
  </reversibility>

  <done>
ЧЕТЫРЕ окна четвёртой партии дописаны одним владельцем командой ведения журнала; тексты трёх
перенесены дословно из сводки плана 10-22 и сличены, и третье из них передаёт владельцу решение об
изъятии из запертого D-01 как ВОПРОС, а не как ответ; окно `IN-04` заведено без сочинённого
назначения; состояние окна 44 решено замером, а не впечатлением; три места реестра согласны между
собой, и прибавка атрибутирована постро́чно планам-источникам партии.
  </done>
</task>

<task type="execute">
  <name>Задача 3: назвать ПРЯМО, чего ЧЕТВЁРТАЯ партия НЕ закрыла — в самом артефакте обхода</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1-32` — шапка артефакта ЦЕЛИКОМ.
  ⚠️ ЗНАЧЕНИЯ ПОЛЕЙ СОСТОЯНИЯ И ЧИСЛА ПРОВЕРОК СОХРАНЯЮТСЯ ДОСЛОВНО, и комментарий с запретом
  объявлять обход пройденным — тоже. Правка, тронувшая их, превратила бы артефакт в самозаверяющий
  И покрасила бы правило плана 10-20.
- `:529-548` — раздел «Чего третья партия закрытия гейпов НЕ закрыла» ЦЕЛИКОМ. ⚠️ ЭТО ОБРАЗЕЦ ФОРМЫ
  И ОДНОВРЕМЕННО СОСЕД: новый раздел встаёт РЯДОМ и НЕ ПОДМЕНЯЕТ его. Форма: таблица «пункт /
  основание», у каждого пункта своё основание, и ни одно не есть оценка труда.
- `:513-528` — раздел «Что в этот обход НЕ входит»: ⚠️ ТРЕТИЙ СОСЕД С ТРЕТЬИМ ПРЕДМЕТОМ. Тот
  отвечает «почему предмет не здесь», раздел третьей партии — «чего партия не закрыла вообще», и
  новый раздел обязан отвечать на ВТОРОЙ вопрос применительно к ЧЕТВЁРТОЙ партии, не переползая на
  первый.
- `:492-512` — раздел «Что закрыто МАШИННО»: форма строки — утверждение, имя правила, ГРАНИЦА
  доказанного. Новые строки партии наследуют все три столбца.
- `10-VERIFICATION.md`, перечни `behavior_unverified_items` (ДВА пункта) и `human_verification`
  (СЕМЬ пунктов) — предметы, которые партия НЕ закрывает и обязана назвать поимённо.
- Сводки семи планов партии — что каждый закрыл и с какой ГРАНИЦЕЙ: строки нового раздела и строки
  перечня закрытого машинно собираются ИЗ НИХ, а не сочиняются здесь.
- `10-20-PLAN.md`, раздел `<not_closed_by_this_plan>` — уже написанный перечень семи пунктов с
  основаниями: он ПЕРЕНОСИТСЯ в артефакт, а не пишется заново.
- Модуль гейта, заведённый планом 10-20, — правила трёх счётов и заполненности отметок. ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО: новый раздел не имеет права выглядеть проверкой либо таблицей отметок, иначе он
  сдвинет один из трёх счётов и покрасит собственный гейт партии.
  </read_first>

  <precondition>
План 10-20 приземлён, и его гейт зелен на непрáвленом артефакте. Новый раздел пишется ПОД этот гейт:
он не должен ни прибавить проверку, ни прибавить таблицу отметок, и проверяется это прогоном, а не
глазом.
  </precondition>

  <behavior>
1. Артефакт получает раздел «Чего ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла», стоящий РЯДОМ с
   разделом третьей партии и не подменяющий его.
2. Раздел называет поимённо: ДВА пункта `behavior_unverified_items`, СЕМЬ пунктов
   `human_verification`, ОБА неразрешённых ребра зонда покрытия, ТРИ отложенные половины находок
   ревизии и долг направления отказа планов `10-01`…`10-07`.
3. У каждого пункта СВОЁ основание, и ни одно не есть оценка труда: основанием служит либо
   отсутствие наблюдаемой опоры, либо решение владельца, либо принадлежность вехе, либо
   недоступность машине.
4. Перечень «Что закрыто МАШИННО» получает строки о том, что партия ДОБАВИЛА, — каждая со своей
   ГРАНИЦЕЙ доказанного.
5. Шапка не тронута; шесть таблиц отметок остаются пустыми; три счёта проверок остаются равными.
6. Артефакт по-прежнему ничего не самозаверяет, и его собственный отказ переводить требование в
   завершённое переживает правку дословно.
  </behavior>

  <action>
**(1) Завести раздел.** Заголовок — «Чего ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла», слово
«ЧЕТВЁРТАЯ» в нём несущее: два одинаково названных раздела читались бы как один переписанный.
Форма — таблица «пункт / основание» по образцу соседа. Отдельной строкой над таблицей — та же
оговорка, что у соседа: раздел стои́т РЯДОМ с «Что в этот обход НЕ входит» и его НЕ подменяет.

**(2) Перенести семь пунктов из `<not_closed_by_this_plan>` плана 10-20.** Дословно, с основаниями.
Они уже написаны и уже проверены, и переписывание их своими словами завело бы второе поколение
формулировки.

**(3) Дописать пункты, которых там нет.** ДВА пункта `behavior_unverified_items` отчёта (критерий 2
и браузерная половина `CR-03`); ОБА неразрешённых ребра зонда покрытия — с указанием, что ребро
идемпотентности ЧАСТИЧНО разрешено планом 10-06 и что именно осталось; ТРИ отложенные половины
находок ревизии (цель третьего внеполосного узла, остаток пункта площадки приземления фокуса,
машинно читаемая разметка слоёв) со ссылкой на их окна; долг направления отказа планов
`10-01`…`10-07`, записанный окном 48 третьей партией и настоящей партией НЕ ПРАВЛЕННЫЙ.
⚠️ У КАЖДОГО — СВОЁ ОСНОВАНИЕ, и ни одно не есть оценка затрат либо отсылка к будущей работе без
названного владельца. Перечень запрещённых в записи слов стои́т отрицательным поиском в
`<acceptance_criteria>` этой задачи и ЗДЕСЬ НЕ ПОВТОРЯЕТСЯ НАМЕРЕННО: слово, выписанное в задании,
приезжает в артефакт вместе с ним и само роняет собственный поиск.

**(4) Дописать строки перечня закрытого машинно.** По одной на предмет, который партия закрыла
машинно, каждая с ГРАНИЦЕЙ доказанного третьим столбцом. Имена правил СЛИЧИТЬ со сводками планов
партии, а не выписать по памяти. ⚠️ ГРАНИЦА ОБЯЗАТЕЛЬНА У КАЖДОЙ: строка без границы читается как
закрытие пункта, которого правило не закрывает, — а это и есть запись шире дерева.

**(5) Проверить, что гейт партии не покраснел.** Прогнать модуль плана 10-20 и убедиться, что три
счёта проверок остались равными и отметок по-прежнему ноль. ⚠️ ЕСЛИ НОВЫЙ РАЗДЕЛ СДВИНУЛ СЧЁТ —
править надо РАЗДЕЛ (он не проверка и не таблица отметок), а не гейт и не объявленное число.

**(6) Что НЕ трогается.** Шапка целиком. Шесть таблиц отметок — остаются пустыми. Разделы посева и
уборки. Раздел «Что в этот обход НЕ входит». Раздел третьей партии — ни символа. Раздел «Куда
артефакт уезжает дальше». `09-UAT.md` — ни символа. `.planning/REQUIREMENTS.md` — ни символа:
требование `FORM-06` остаётся незавершённым, и отметка ожидания не трогается.

**(ПОСЛЕДНИЙ ШАГ ЗАДАЧИ) ОБНОВИТЬ ГРАФ ПРОЕКТА — `graphify update .`.** ⚠️ ЭТО ТРЕБОВАНИЕ
`./CLAUDE.md` ДОСЛОВНО: «After modifying code, run `graphify update .` to keep the graph current
(AST-only, no API cost)». Настоящий план правит файлы, которые граф индексирует, поэтому шаг
обязателен и стои́т ПОСЛЕДНИМ — после всех правок плана и после прогонов.

⚠️ **ПОЛЕ `graphify.enabled: false` В `.planning/config.json` ОСНОВАНИЕМ ДЛЯ ОТКАЗА НЕ ЯВЛЯЕТСЯ, И
ЭТО ЗАПИСАНО ПРЯМО, А НЕ ПРОПУЩЕНО МОЛЧА.** Замер планировщика 2026-09-05: конфигурация несёт
`"graphify": {"enabled": false}` — это ключ ИНТЕГРАЦИИ GSD, управляющий графом `.planning/graphs/`,
которого в дереве НЕТ вовсе (`ls -d .planning/graphs` → отсутствует). Граф ПРОЕКТА живёт в
`graphify-out/` (`ls -d graphify-out` → есть), команда доступна (`command -v graphify` → есть), и
свежести этого графа требует `./CLAUDE.md`, а не конфигурация GSD. ДВА РАЗНЫХ ГРАФА, ДВА РАЗНЫХ
ВЛАДЕЛЬЦА, и выключенный ключ одного не отменяет требования другого.

⚠️ **ПОБОЧНОГО ЭФФЕКТА НА ПРИЁМОЧНЫЕ КРИТЕРИИ ПАРТИИ НЕТ, И ЭТО ЗАМЕРЕНО:** `graphify-out/`
игнорируется гитом (`.gitignore:56`, `**/graphify-out/`) и деревом не отслеживается, поэтому шаг не
может тронуть ни один вывод `git diff`, на котором стоят гейты `<verify>` и приёмочные критерии
планов партии.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_the_walkthrough_cannot_self_certify.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — новый раздел сдвинул один из трёх счётов проверок либо выглядит заполненной отметкой</fails_when>
    <automated>grep -c 'ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод `0` — раздел не заведён, и то, чего партия не закрыла, нигде в артефакте не названо</fails_when>
    <automated>grep -c 'Чего третья партия закрытия гейпов НЕ закрыла' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод отличен от `1` — раздел третьей партии снят либо задвоен, тогда как он не подменяется и не правится</fails_when>
    <automated>grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод отличен от `0` — отметка обхода заполнена планом, то есть артефакт стал самозаверяющим</fails_when>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — правка записи задела суиту</fails_when>
    <automated>graphify update .</automated>
    <fails_when>ненулевой код возврата либо вывод содержит `Traceback` — граф проекта не обновлён, и следующий читатель получит на вопрос о коде устаревший ответ. ⚠️ `command not found` — НАХОДКА ДЛЯ СВОДКИ (инструмент не установлен в среде исполнения), а не молчаливый пропуск шага регламента</fails_when>
  </verify>

  <acceptance_criteria>
- Раздел заведён и стои́т РЯДОМ с разделом третьей партии: в сводке приведены оба заголовка и
  порядок, в котором они идут.
- Названы ВСЕ пункты: два `behavior_unverified_items`, семь `human_verification`, два ребра зонда,
  три отложенные половины находок ревизии, долг направления отказа. В сводке приведён их счёт и
  перечень.
- Ни одно основание не есть оценка труда: в сводке приведён вывод
  `git diff -- .planning/phases/10-rychag-components-modal-html/10-UAT.md | grep -ci -e 'сложн' -e 'трудоём' -e 'позже'` → `0`.
- Строки перечня закрытого машинно несут ГРАНИЦУ каждая: в сводке приведены все новые строки, и в
  каждой виден третий столбец.
- Имена правил СЛИЧЕНЫ со сводками планов партии: в сводке приведены пары «имя в артефакте / имя в
  сводке» и показано совпадение.
- Три счёта проверок остались равными: в сводке приведены все три числа до и после правки.
- Отметок не заполнено ни одной: в сводке приведён вывод четвёртой команды раздела `<verify>` (`0`).
- Шапка не тронута: в сводке приведён вывод
  `git diff -U0 -- .planning/phases/10-rychag-components-modal-html/10-UAT.md | head -20`, и в нём
  нет строк шапки.
- `09-UAT.md` и `.planning/REQUIREMENTS.md` не тронуты:
  `git diff --name-only -- .planning/phases/09-*/ .planning/REQUIREMENTS.md` — пусто.
  </acceptance_criteria>

  <reversibility rating="reversible">
Дописываются два раздела текста в артефакт обхода; ни состояние артефакта, ни отметки, ни поведение
продукта не меняются. Откат равен удалению дописанного.
  </reversibility>

  <done>
Названо ПРЯМО и в самом артефакте, чего четвёртая партия НЕ закрыла: все пункты перечислены с
собственным основанием у каждого, и ни одно основание не есть оценка труда; перечень закрытого
машинно пополнен строками партии с границей у каждой; артефакт по-прежнему ничего не самозаверяет, и
гейт плана 10-20 зелен.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**`.planning/ROADMAP.md`, раздел `### Phase 10`:** три новых подзаголовка волн четвёртой партии;
семь строк перечня планов в отметке «не исполнено» (`10-17` … `10-23`); приведённая по замеру строка
состава с ДВУМЯ числами; новая летопись числа планов фазы со всеми шестью обязательными частями.

**`.planning/STATE.md`:** поле `progress.total_plans`, выведенное ПРОГОНОМ разборщика правила счёта.
Поле `progress.completed_plans` НЕ трогается.

**`.planning/WINDOWS.md`:** ЧЕТЫРЕ новых окна — об отложенной правке цели третьего внеполосного узла
(текст плана 10-22), об остатке пункта площадки приземления фокуса (текст плана 10-22), о расхождении
формы отказа валидации пяти входов с ЗАПЕРТЫМ решением D-01, чьё изъятие ЖДЁТ ВЛАДЕЛЬЦА (текст плана
10-22, БЕЗ назначенной фазы-снимателя, по форме окон 34 и 47), и о стоимости чтения трёх поколений
формулировок в одном месте (`IN-04`, БЕЗ назначенной фазы-снимателя). Плюс решённое ЗАМЕРОМ состояние
окна **44**. ⚠️ ЧИСЛО ОКОН — ОРИЕНТИР: ветви «это НАХОДКА» планов 10-18, 10-19 и 10-22 способны
законно родить ещё, и такая строка атрибутируется плану-источнику, а не считается чужой записью.

**`.planning/phases/10-rychag-components-modal-html/10-UAT.md`:** раздел «Чего ЧЕТВЁРТАЯ партия
закрытия гейпов НЕ закрыла» и новые строки перечня «Что закрыто МАШИННО», каждая с ГРАНИЦЕЙ
доказанного.

**ЧЕГО ПЛАН НЕ ПРОИЗВОДИТ:** ни одной правки `app/`, `tests/`, `pyproject.toml`,
`.planning/REQUIREMENTS.md`, `09-UAT.md`, исполненных планов `10-01`…`10-16` и их сводок. Ни одной
заполненной отметки обхода. Ни одного перевода требования в завершённое состояние.
</artifacts_this_phase_produces>

<threat_model>
⚠️ Таблица собрана на **ASVS 1** (`workflow.security_asvs_level` → `1`) с порогом блокировки
**`high`** (`workflow.security_block_on` → `"high"`) при `workflow.security_enforcement` → `true`.
Все три величины сняты `config-get`. Строка `*-SC` несёт `accept`: пакетов план не устанавливает,
`pyproject.toml` не правит.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| перечень планов роадмапа → машинно читаемое поле счёта | два места одного счёта расходятся от коммита, тронувшего одно и обошедшего другое |
| текст окна у автора → строка реестра у владельца | окно, потерянное при передаче, неотличимо от незаведённого |
| запись артефакта обхода → приёмка фазы | непройденный обход, о непройденности которого не сказано, превращается в закрытое требование |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-23-01 | Repudiation | два места одного счёта планов | high | mitigate | Поле выводится ПРОГОНОМ разборщика и правится ТЕМ ЖЕ ДЕЙСТВИЕМ, что и перечень; красный между правками НАБЛЮДАЕТСЯ и приводится в сводку. Приведение поля постфактум обошло бы правило ровно тем движением, ради снятия которого оно заведено |
| T-10-23-02 | Repudiation | сдвинутое число без летописи | medium | mitigate | Летопись в форме трёх предыдущих со всеми шестью обязательными частями и с источником расхождения ПОИМЁННО: прежнее значение названо устаревшим, а не ошибочным |
| T-10-23-03 | Tampering | реестр окон как общий дописываемый файл | medium | mitigate | Владелец в партии ОДИН: `.planning/WINDOWS.md` стои́т в `files_modified` ровно настоящего плана, и это проверяется счётом строк таблицы против замеренных планировщиком `48` плюс ровно три |
| T-10-23-04 | Tampering | счётчики шапки реестра | medium | mitigate | Запись ведётся КОМАНДОЙ ведения журнала, а не ручной правкой строки; согласие трёх мест реестра проверяется отдельной командой раздела `<verify>` |
| T-10-23-05 | Repudiation | состояние артефакта ручного обхода | high | mitigate | Шапка не трогается, шесть таблиц отметок остаются пустыми, и с плана 10-20 попытка объявить обход пройденным при пустых отметках КРАСНИТ прогон. Раздел «чего партия не закрыла» называет все непокрытые пункты поимённо |
| T-10-23-06 | Repudiation | перечень закрытого машинно | medium | mitigate | Каждая новая строка несёт ГРАНИЦУ доказанного третьим столбцом; имена правил сличаются со сводками планов партии, а не выписываются по памяти |
| T-10-23-07 | Spoofing | назначение фазы-снимателя окну | medium | mitigate | Фаза-сниматель не сочиняется там, где её не назвал владелец (прецедент окон 34 и 47): окно `IN-04` заводится без назначения и с записанным основанием |
| T-10-23-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q -p no:randomly` — зелено: два места счёта согласны, гейт
   артефакта обхода не покраснел, гейты записи планов 10-17 и 10-21 стоят.
2. `uv run pytest tests/ -q -p no:randomly` — прогон целиком зелен.
3. `grep -c '^- \[ \] 10-' .planning/ROADMAP.md` → `7`.
4. `grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md` → `51`; `grep -c '^total_count: 51$' .planning/WINDOWS.md` → `1`.
5. `grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' .planning/phases/10-rychag-components-modal-html/10-UAT.md` → `0`.
6. `git diff --name-only -- .planning/REQUIREMENTS.md .planning/phases/09-*/ app/ tests/` — пусто.
7. **ЧЕЛОВЕЧЕСКАЯ ПОЛОВИНА, СНИМАЕМАЯ НА ПРИЁМКЕ ФАЗЫ (`human_verify_mode: end-of-phase`):** шесть
   проверок обхода `10-UAT.md` пройдены на локальном стенде в браузере, шесть таблиц отметок
   заполнены наблюдёнными признаками. ⚠️ ДО ЭТОГО критерий 2 роадмапа и требование `FORM-06`
   остаются НЕЗАКРЫТЫМИ; ни один машинный прогон партии этого не меняет, и ни один её план
   обратного не утверждает.
</verification>

<success_criteria>
- Семь планов партии стоят в перечне раздела фазы в трёх волнах; оба числа строки состава сняты
  замером, а не переписаны.
- Летопись числа планов записана в форме трёх предыдущих, с источником расхождения поимённо и с
  отдельной оговоркой о втором числе строки.
- Машинно читаемое поле счёта выведено прогоном разборщика и приведено ТЕМ ЖЕ ДЕЙСТВИЕМ; красный
  между правками наблюдён и приведён в сводку.
- Три окна партии дописаны ОДНИМ владельцем командой ведения журнала; тексты двух перенесены
  дословно и сличены; окно `IN-04` заведено без сочинённого назначения; состояние окна 44 решено
  замером.
- Названо ПРЯМО и в самом артефакте обхода, чего четвёртая партия НЕ закрыла: все пункты с
  собственным основанием, ни одно из которых не есть оценка труда.
- Перечень закрытого машинно пополнен строками партии, каждая с ГРАНИЦЕЙ доказанного.
- Артефакт обхода ничего не самозаверяет: состояние не сдвинуто, отметки пусты, требование
  `FORM-06` остаётся незавершённым.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-23-SUMMARY.md` по завершении.
</output>
