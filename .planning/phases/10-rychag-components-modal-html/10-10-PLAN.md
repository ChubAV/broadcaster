---
phase: 10-rychag-components-modal-html
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/phases/10-rychag-components-modal-html/10-UAT.md
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер (`estimate-calibration`: sample_count 0,
  # factor 1, min_samples 3): tokens и raw_tokens совпадают, confidence: low
  # ВЫВЕДЕН ИЗ ЧИСЛА ЗАМЕРОВ, а не из самооценки планировщика.
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ВТОРАЯ ПОЛОВИНА ЦЕЛИ ФАЗЫ получает АРТЕФАКТ, а не обещание: «после N удалений в документе не остаётся N мёртвых диалогов» перестаёт быть строкой отчёта и становится обходом с приведённым стендом, шагами, наблюдаемыми признаками и местом для отметки человека"
    - "Машинной подмены рантайма НЕ ЗАВОДИТСЯ, и это решение, а не пропуск: внеполосные узлы снятия применяет рантайм разметки, которого суита не поднимает, и планы 10-05…10-07 ПРАВИЛЬНО отказались подменять его имитацией. Артефакт продолжает тот же отказ и называет основание прямо"
    - "Все ШЕСТЬ пунктов `human_verification` отчёта верификации перенесены в обход ПОИМЁННО, каждый со своим стендом, шагами и ожидаемым признаком; ни один не растворён в соседнем и ни один не потерян"
    - "ПОЛНОТА АРТЕФАКТА УДЕРЖИВАЕТСЯ МАШИННО, а не вычиткой: гейт присутствия несущих маркеров написан по форме, которой Фаза 9 держала свой обход (`09-UAT.md`, план 09-02), и краснеет на пропаже любого маркера — то есть на проверке, вырезанной следующей правкой"
    - "Стенд ПРИВОДИТСЯ, А НЕ ОПИСЫВАЕТСЯ: команда посева печатает адрес экрана обхода, всё посеянное несёт общий префикс имени, а уборка стенда есть ПОСЛЕДНИЙ ШАГ обхода — по форме, которой Фаза 9 запретила себе оставлять посеянные строки в живой базе разработчика"
    - "Обход НАЗЫВАЕТ, ЧТО ЗАКРЫТО МАШИННО, и запрещает проверять это заново: присутствие парных узлов снятия, структурная предпосылка (панель стои́т СНАРУЖИ удаляемой карточки), обе половины пары транспортов, неотличимость повтора и гейт ухода панели доказаны правилами — человеку остаётся ровно то, что снимается только глазом"
    - "Пункт приземления фокуса читается ВМЕСТЕ с суженной записью о цене выбора площадки (WR-02, план 10-09): человек, пришедший проверять «фокус приземлился на текст отказа», проверял бы обещание, которого рантайм не выполняет, и записал бы расхождение там, где его нет"
    - "Артефакт НЕ САМОЗАВЕРЯЕТСЯ: таблицы отметок остаются ПУСТЫМИ до обхода, отметка без наблюдённого признака закрытием не считается, и `FORM-06` этим планом в `Complete` НЕ переводится"
    - statement: "Ни одна из шести проверок обхода машинно не закрывается ЭТИМ планом и закрыться не может: рантайм разметки, объект клиентского состояния и таблица стилей поднимаются только браузером. Артефакт переводит их из «не проверено ничем» в «проверяемо по шагам», и это ВСЁ, что он делает"
      verification: backstop
  artifacts:
    - ".planning/phases/10-rychag-components-modal-html/10-UAT.md — обход фазы целиком: стенд, уборка, шесть проверок, таблицы отметок, перечень закрытого машинно"
    - ".planning/phases/10-rychag-components-modal-html/10-UAT.md — команда посева стенда с общим префиксом имени и печатью адреса экрана обхода"
    - ".planning/phases/10-rychag-components-modal-html/10-UAT.md — команда уборки стенда как последний шаг обхода и проверка её результата числом"
    - ".planning/phases/10-rychag-components-modal-html/10-UAT.md — раздел «Что закрыто МАШИННО — проверять заново не нужно» с именами правил"
  key_links:
    - "проверка снятия осиротевшей панели → критерий 2 роадмапа: единственное, чем вторая половина цели фазы перестаёт держаться разметкой и словами; ломается — фаза закрывается на утверждении, взятом из документации и не проверенном ни на чём, а риск «после N удалений N мёртвых диалогов» уезжает в Фазу 11 сразу на пятнадцать мест"
    - "гейт полноты артефакта → сам артефакт: единственное, чем пункт, выпавший из обхода при следующей правке, краснеет; ломается — обход тихо усыхает до тех проверок, которые кто-то помнит"
    - "команда уборки → живая база разработчика: единственное, чем посеянный стенд не переживает обход; ломается — строки префикса остаются и попадаются на глаза ещё долго (записанный опыт Фазы 9)"
    - "перечень закрытого машинно → время человека: единственное, чем обход не превращается в повторную проверку того, что уже держат правила; ломается — проход тратится на присутствие узлов, а на применение их рантаймом времени не остаётся"
  prohibitions:
    - statement: "машинной подмены рантайма разметки ради «закрытия» критерия 2 не заводится ни в каком виде: суита не свопает и внеполосных узлов не применяет, и правило, объявившее обратное, вернуло бы фазе ровно тот дефект, которым она была провалена первым кругом"
      status: flagged-unverified
      verification: none
    - statement: "таблицы отметок обхода НЕ заполняются исполнителем: отметка без наблюдённого признака закрытием не считается, а заполненная заранее строка есть самозаверение, а не приёмка"
      status: flagged-unverified
      verification: none
    - statement: "`FORM-06` этим планом в `Complete` не переводится и отметка `Pending` в `.planning/REQUIREMENTS.md` не трогается: вторая половина требования закрывается глазом, и исполнители прежних планов, оставившие отметку, поступили верно"
      status: flagged-unverified
      verification: none
    - statement: "прежний обход Фазы 9 (`09-UAT.md`) не правится и его отметки не переносятся: они сняты с ДРУГОГО экрана и другого механизма, а перенос отметки на новый предмет есть закрытие дефекта переносом записи о нём"
      status: flagged-unverified
      verification: none
    - statement: "файла в каталоге вспомогательных скриптов под посев стенда не заводится: фаза новых поверхностей не создаёт, а посевной стенд живёт ровно столько, сколько идёт обход (записанное решение обхода Фазы 9)"
      status: flagged-unverified
      verification: none
---

<objective>
Заводится артефакт РУЧНОГО ОБХОДА фазы `10-UAT.md`: вторая половина цели фазы — «после N удалений в
документе не остаётся N мёртвых диалогов» — и шесть пунктов `human_verification` отчёта верификации
получают стенд, шаги, наблюдаемые признаки и место для отметки человека.

Purpose: `10-VERIFICATION.md` несёт `behavior_unverified: 1`, и это критерий 2 фазы. Разметка на
месте и адресована верно, структурная предпосылка держится, парные узлы присутствуют — но ПРИМЕНЯЕТ
их рантайм разметки, которого суита не поднимает. Роадмап сам называет это ручным UAT, пункт 4, и
прямо говорит, что это ЕДИНСТВЕННЫЙ вывод фазы, взятый из документации и не проверенный ни на чём.
Планы 10-05…10-07 правильно отказались подменять его имитацией; этот план продолжает тот же отказ и
делает единственное честное: превращает «не проверено ничем» в «проверяемо по шагам».

Output: обход с приведённым стендом и уборкой; шесть проверок поимённо; перечень закрытого машинно;
пустые таблицы отметок; машинный гейт полноты артефакта по форме обхода Фазы 9.

⚠️ ЭТОТ ПЛАН — ЗАКРЫТИЕ ГЕЙПОВ (`gap_closure: true`), ВОЛНА 1 НОВОЙ ПАРТИИ, идёт ПАРАЛЛЕЛЬНО планам
10-08 и 10-09: он трогает РОВНО ОДИН файл, которого не трогает никто. Планы 10-01…10-07 исполнены и
закоммичены и не правятся.

⚠️ `10-VALIDATION.md` У ФАЗЫ НЕТ ПО ЯВНОМУ ВЫБОРУ ОПЕРАТОРА: режим `--gaps` разведку не запускает, и
оператор выбрал продолжить без неё.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-07-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Создаётся этим планом (проход source-grounding обязан считать новым):

- `.planning/phases/10-rychag-components-modal-html/10-UAT.md` — весь файл: frontmatter обхода,
  разделы «Как привести стенд в нужное состояние», «Как убрать стенд», «Почему этот артефакт
  существует», «Что закрыто МАШИННО — проверять заново не нужно», ШЕСТЬ проверок с таблицами
  шагов, таблицы отметок, «Что в этот обход НЕ входит», «Куда артефакт уезжает дальше»
- префикс имени посеянного стенда `UAT10-` и переменная окружения `UAT10_EMAIL`
- маркеры полноты, читаемые гейтом: `ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ`, `sched-del-`, `sched-count`,
  `UAT10-`, `is-modal-open`, `notice-alert`, `Slow 3G`, `DEF-09-02`, `FORM-06`

Существует до плана и только ЧИТАЕТСЯ (не правится ни символом): `09-UAT.md` Фазы 9,
`10-VERIFICATION.md`, `10-REVIEW.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`.
</artifacts_this_phase_produces>

<probe_edges>
## Рёбра зонда покрытия (`edge-probe.cjs` по тексту FORM-06) — ни одно не роняется молча

Оба ребра стоя́т `unresolved` и вносятся ЯВНЫМИ ПОМЕЧЕННЫМИ ДОПУЩЕНИЯМИ со ссылкой на закрывший
план — проверено чтением планов и сводок:

1. **`idempotency`** — РАЗРЕШЕНО планом **10-06** (неотличимость повтора подтверждённого удаления).
   ⚠️ ОБХОД ЕГО НЕ ПЕРЕПРОВЕРЯЕТ и говорит об этом прямо в разделе «Что закрыто МАШИННО»: повтор
   доказан настоящими запросами и сличением множеств внеполосных узлов, глаз к этому не прибавляет.
2. **`concurrency`** — РАЗРЕШЕНО планом **10-07** (гейт ухода панели по признаку отправки на всех
   трёх путях). ⚠️ ОБХОД ПРОВЕРЯЕТ НЕ РЕБРО, А ТО, ЧТО ЧЕЛОВЕК ВИДИТ: панель на медленной сети
   выглядит ЗАНЯТОЙ, а не сломанной (пункт 3 перечня `human_verification`, окно 35).

Равенство без молчаливых пропусков: поднято зондом 2 — внесено допущениями 2.
</probe_edges>

<tasks>

<task type="tracer">
  <name>Задача 1: сквозной срез обхода — стенд, уборка и ЕДИНСТВЕННАЯ проверка критерия 2, с машинным гейтом полноты</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md:1-60` —
  frontmatter обхода Фазы 9: ключи состояния, фазы, источников и блоки прохождений с датой,
  браузером, ОС, стендом и ДОСЛОВНЫМИ ответами. Наследуется ФОРМА, не содержимое.
- `09-UAT.md:117-250` — «Как привести стенд в нужное состояние» и «Как убрать стенд» ЦЕЛИКОМ:
  команда посева, печать адреса экрана, общий префикс имени, оговорка о разном числе строк у разных
  проверок, запрет заводить файл в каталоге вспомогательных скриптов.
- `09-UAT.md:596-650` — проверки 5.1-5.3 с таблицами «# / Шаг / Ожидаемый наблюдаемый признак» и
  таблицей отметок. ОБРАЗЕЦ ФОРМЫ проверки, которую пишет эта задача.
- `10-VERIFICATION.md` — `behavior_unverified_items` ЦЕЛИКОМ (`truth`, `test`, `expected`,
  `why_human`) и пункт 1 раздела «Human Verification Required». Формулировка предмета переносится
  дословно, а не пересказывается.
- `10-VERIFICATION.md`, строка 2 таблицы «Observable Truths» — там названы точные идентификаторы
  разметки (`sched-{id}` карточки, `sched-del-{id}` панели), файл ответа и структурная предпосылка
  (панель стои́т ПОСЛЕ закрытия карточки, то есть снаружи удаляемой строки).
- `.planning/ROADMAP.md:275` — критерий 2 дословно, с оговоркой «фактическое снятие — ручной UAT,
  пункт 4 перечня».
- `app/templates/ads/includes/sched_card.html` — карточка расписания (идентификатор `sched-{id}`,
  признак карточки) и блочный вызов панели ПОСЛЕ её закрытия.
- `app/templates/ads/partials/sched_delete_response.html` — три узла ответа: снятие карточки,
  снятие панели, подмена содержимого линейки счётчика.
- `app/models/ad.py`, `app/models/schedule.py`, `app/models/messenger_account.py` — поля, нужные
  посеву: у объявления обязательны владелец, заголовок и текст; у расписания — объявление, список
  групп, дни недели, времена, часовой пояс, признак активности; у аккаунта — владелец, тип,
  учётные данные, состояние.
- `tests/test_pages/test_editor_schedules.py:109-132` — `_seed_schedule`: значения, которыми
  расписание собирается валидным (пустой список групп, один день, одно время, часовой пояс `UTC`).
  Команда посева стенда собирает те же поля теми же значениями — второй набор умолчаний разошёлся
  бы с первым молча.
  </read_first>

  <precondition>
Файла `.planning/phases/10-rychag-components-modal-html/10-UAT.md` в дереве НЕТ (проверяется `ls`);
артефакт заводится с нуля. Обход Фазы 9 существует и читается —
`.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md`, 1534 строки.
  </precondition>

  <action>
**1. Frontmatter обхода.** Ключи: состояние обхода со значением `testing` («заведён, не пройден»),
фаза, источники (отчёт верификации, отчёт ревизии, роадмап), дата заведения. ⚠️ ЗНАЧЕНИЕ
«ПРОЙДЕНО» СТАВИТЬ ЗАПРЕЩЕНО: обход не проходился. Блока прохождения (дата, браузер, ОС, стенд,
ответы) в файле НЕТ — он появляется, когда обход состоится, и заводит его человек.

**2. Раздел «Как привести стенд в нужное состояние».** Стенд: один пользователь, одно объявление,
НЕ МЕНЕЕ ТРЁХ расписаний. Число названо с основанием, а не круглым словом: на последнем расписании
маршрут уходит в ветку перехода (D-04) и фрагмента не даёт вовсе, поэтому три строки дают ровно два
фрагментных удаления и одно переходное — обе ветки попадают в один проход.

Команда посева приводится ЦЕЛИКОМ и печатает одну строку с адресом экрана обхода вида
`/ads/{ad_id}/edit`. Всё посеянное несёт префикс `UAT10-` (в заголовке объявления и в учётных
данных аккаунта); владелец берётся из переменной окружения `UAT10_EMAIL`, а без неё — первый
пользователь базы, и команда печатает его почту, чтобы было видно, под кем заходить. Приложение
поднимается рецептом запуска проекта.

⚠️ Основание префикса переносится по смыслу из обхода Фазы 9: посев идёт в ЖИВУЮ локальную базу
разработчика, и без пометки убрать посеянное можно только глазами.

**3. Раздел «Как убрать стенд».** Уборка — ПОСЛЕДНИЙ ШАГ ОБХОДА, а не пожелание. Команда снимает
ровно строки с префиксом в порядке связей: расписания объявления, объявление, аккаунт. Приводится
проверочная команда, печатающая число оставшихся строк с префиксом по каждой из трёх сущностей
(ожидается ноль).

**4. Проверка 1 — «Осиротевшая панель снята после N удалений» (критерий 2, пункт 4 ручного UAT).**
Пишется таблицей «# / Шаг / Ожидаемый наблюдаемый признак» по образцу проверки 5.1 обхода Фазы 9.
Шаги обязаны снимать ЧЕТЫРЕ ЧИСЛА в консоли браузера, а не впечатления:
- число узлов с признаком диалоговой роли ДО удалений;
- число узлов, чей идентификатор начинается с ключа панели расписания, ДО удалений;
- те же два числа ПОСЛЕ N удалений;
- отсутствие ОБОИХ узлов удалённого расписания поимённо — карточки и панели.
Ожидаемый признак формулируется РАВЕНСТВОМ, а не словом «нет мусора»: число панелей равно числу
оставшихся карточек, узлы удалённых строк отсутствуют оба.

Отдельным шагом — ЛОВУШКА ФОКУСА: обход по клавише табуляции не проваливается в невидимый диалог.
Это то самое следствие, ради предотвращения которого критерий 2 и написан: осиротевшая панель
опасна не присутствием узла, а живой ловушкой фокуса внутри него.

Отдельным шагом — ЛИНЕЙКА СЧЁТЧИКА: число в линейке после каждого удаления сходится с числом
оставшихся карточек (третий узел ответа, единственный, чья цель — статический селектор).

Отдельным шагом — ПЕРЕХОДНАЯ ВЕТКА: удаление ПОСЛЕДНЕГО расписания уводит с экрана переходом, а не
второй отрисовкой пустого состояния (D-04), и после перехода осиротевших диалогов на новом экране
тоже нет.

⚠️ По каждому числу записывается `ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ` — идиома обхода Фазы 9: проверка, у
которой ожидаемый ответ не назван заранее, закрывается любым наблюдением.

**5. Машинный гейт полноты артефакта.** Пишется по форме, которой Фаза 9 держала свой обход
(`prior_verify_commands`, план 09-02): чтение файла и выход с перечнем недостающих маркеров.
Перечень маркеров этой задачи: `ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ`, `sched-del-`, `sched-count`, `UAT10-`,
`FORM-06`. Гейт краснеет на пропаже любого — то есть на проверке, вырезанной следующей правкой.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib,sys; s=pathlib.Path('.planning/phases/10-rychag-components-modal-html/10-UAT.md').read_text(encoding='utf-8'); need=['ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ','sched-del-','sched-count','UAT10-','FORM-06']; miss=[n for n in need if n not in s]; sys.exit('нет в 10-UAT.md: '+repr(miss) if miss else 0)"</automated>
    <fails_when>ненулевой код возврата либо строка «нет в 10-UAT.md:» с перечнем недостающих маркеров в выводе</fails_when>
  </verify>

  <acceptance_criteria>
- Файл заведён, состояние обхода — `testing`, и в сводке названо прямо, что значение «пройдено» не
  ставилось и блока прохождения в файле нет.
- Команда посева ИСПОЛНЕНА исполнителем на локальной базе, и её вывод приведён в сводке дословно —
  одна строка с почтой владельца, идентификатором объявления и адресом экрана. Команда, не
  исполненная ни разу, к слиянию не принимается: непроверенная команда посева превращает обход в
  предложение человеку отладить её вместо прохождения.
- Команда уборки ИСПОЛНЕНА следом, и вывод проверочной команды приведён: ноль строк с префиксом по
  каждой из трёх сущностей. ⚠️ Стенд после задачи в базе НЕ ОСТАЁТСЯ.
- Проверка 1 несёт не менее шести шагов, и каждый называет НАБЛЮДАЕМЫЙ признак; в сводке приведён
  перечень снимаемых величин.
- Гейт полноты зелен, и его вывод приведён (код возврата `0`).
- Таблица отметок проверки 1 присутствует и ПУСТА; в сводке подтверждено, что ни одна ячейка не
  заполнена.
- `.planning/REQUIREMENTS.md` и `.planning/ROADMAP.md` не тронуты:
  `git diff --name-only -- .planning/REQUIREMENTS.md .planning/ROADMAP.md` — пусто.
  </acceptance_criteria>

  <done>
Обход существует, стенд приводится исполненной командой и убирается ею же, проверка критерия 2
снимает четыре числа и ловушку фокуса, ожидаемый ответ назван заранее по каждому, полнота артефакта
держится машинным гейтом, отметки пусты.
  </done>
</task>

<task type="execute">
  <name>Задача 2: остальные пять пунктов ручной верификации — каждый со своим стендом, шагами и ожидаемым признаком</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `10-VERIFICATION.md`, раздел `human_verification` frontmatter ЦЕЛИКОМ (шесть записей с `test`,
  `expected`, `why_human`) и раздел «Human Verification Required» (шесть подразделов). ⚠️ ЭТО
  ИСТОЧНИК ПУНКТОВ: они переносятся ПОИМЁННО, а не пересказываются своими словами.
- `.planning/WINDOWS.md`, записи 22, 28, 35, 36 — четыре открытых окна, чьи предметы закрываются
  ровно этими пунктами обхода. Читать дословно: обход обязан назвать, КАКОЕ окно закрывает какой
  пункт, и не обещать закрытия там, где предмет шире.
- `app/templates/components/modal.html:595-642` — объект клиентского состояния панели: гейт ухода
  по признаку отправки на трёх путях, выражение закрытия по завершении обмена, узел индикатора,
  кнопка отказа. Это предмет пунктов 2, 3 и 4.
- `app/templates/includes/notice_area.html:78-110` — запись о цене выбора площадки приземления
  ПОСЛЕ правки плана 10-09 и две области разметки. ⚠️ Пункт приземления фокуса обязан ссылаться на
  СУЖЕННУЮ запись: до плана 10-09 запись обещала окупаемость на отказном пути, которого нет.
- `10-REVIEW.md` §WR-02 — три измерения, по которым обещание опровергнуто.
- `.planning/REQUIREMENTS.md` около строк 428-436 — прежняя формулировка `DEF-09-02` и запись его
  закрытия планом 10-07. Предмет пункта документной половины.
- `09-UAT.md:1007-1030` — проверка 6.3 (кнопка подтверждения после ОТКАЗА запроса): готовая форма
  проверки, в которой человек ВЫЗЫВАЕТ отказ и смотрит на панель. Наследуется способ вызова отказа.
- `09-UAT.md:1238-1266` — проверка 4.2 (ветка перехода): готовая форма проверки, в которой человек
  обрывает сеть и смотрит на экран. Наследуется способ обрыва.
  </read_first>

  <precondition>
Задача 1 приземлена: файл `10-UAT.md` существует, гейт полноты зелен, стенд приводится и убирается
исполненными командами. Правка записи о площадке приземления (план 10-09, задача 3) — НЕ
предусловие этой задачи: планы идут параллельно, и пункт приземления фокуса обязан ссылаться на
предмет записи, а не на её строки.
  </precondition>

  <action>
Пишутся ПЯТЬ проверок, каждая — своим подразделом с таблицей «# / Шаг / Ожидаемый наблюдаемый
признак», записью `ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ` и явной ссылкой на закрываемое окно.

**Проверка 2 — уборка панели при не доехавшем follow-up GET (окно 28).** Стенд: любое из мест,
отвечающих переходом. Сеть замедляется, подтверждается удаление, связь обрывается сразу после
нажатия. Ожидаемый признак: панель уходит ПО СОБЫТИЮ ЗАВЕРШЕНИЯ ЗАПРОСА, а не по подмене тела, и не
остаётся висеть поверх устаревшего экрана; признак блокировки прокрутки с корневого узла документа
снят (проверяется списком классов корневого узла в консоли, а не на глаз). Записывается, что
правкой плана 10-05 пункт АДРЕСОВАН, но машинно не закрыт.

**Проверка 3 — гейт ухода панели на живом стенде (`DEF-09-02`, окно 35).** Подтверждается удаление
на медленной сети, и, не дожидаясь ответа, пробуются ВСЕ ТРИ пути ухода: клавиша выхода, щелчок по
оверлею, кнопка отказа. Ожидаемый признак: панель не уходит ни одним; виден узел индикатора; кнопка
отказа ДОСТУПНА и не выпала из обхода по клавише табуляции. Отдельный шаг ПОСЛЕ ответа (успех и
отказ сервера): все три пути снова работают. ⚠️ Пункт проверяет НЕ САМО СВОЙСТВО (оно доказано
правилами и мутацией), а то, что человек видит панель ЗАНЯТОЙ, а не сломанной, — и это записывается
прямо, чтобы проверка не выглядела дублем правил.

**Проверка 4 — панель остаётся открытой на отказе (половина D-12, окно 22).** Вызывается отказ
сервера на маршруте за панелью; способ вызова наследуется у проверки 6.3 обхода Фазы 9. Ожидаемый
признак: панель ОСТАЁТСЯ открытой, поверх встаёт плашка аварии, повтор возможен БЕЗ переоткрытия
панели.

**Проверка 5 — приземление фокуса (пункт 5 перечня).** Подтверждается удаление, и после ухода
панели снимается активный элемент документа — на фрагментном пути и на переходном. Ожидаемый
признак на фрагментном пути: фокус в области уведомлений, а не на теле документа. ⚠️ ЧИТАТЬ ВМЕСТЕ
С WR-02: область на этом пути ПУСТА ВСЕГДА (плашки на успех нет по D-03), поэтому ожидаемый ответ
— «фокус приехал в осмысленное МЕСТО, но не в осмысленный ТЕКСТ», и это НЕ расхождение. Проверка
обязана сказать это ДО шагов, иначе человек запишет расхождение там, где его нет.

**Проверка 6 — документная половина закрытия `DEF-09-02` (окно 36).** Читаются глазами: запись
закрытия в требованиях, обе половины окна 15 и текст окна цены гейта. Ожидаемый признак: все три
говорят об ОДНОМ предмете теми же словами, прежние формулировки не переписаны, а окно цены гейта
называет кандидатов НЕ назначая фазу.

**Раздел «Что закрыто МАШИННО — проверять заново не нужно».** Перечисляются ИМЕНАМИ ПРАВИЛ:
инвентарь мест подтверждения и три его счёта; обе половины пары транспортов по всем маршрутам;
присутствие парных внеполосных узлов и структурная предпосылка; неотличимость повтора (ребро
идемпотентности, план 10-06); гейт ухода панели на трёх путях и его положительный контроль (план
10-07); четыре числа критерия 3. Против каждого — одна строка «что именно доказано», чтобы человек
видел ГРАНИЦУ доказанного, а не только имя.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib,sys; s=pathlib.Path('.planning/phases/10-rychag-components-modal-html/10-UAT.md').read_text(encoding='utf-8'); need=['Slow 3G','is-modal-open','notice-alert','DEF-09-02','окно 22','окно 28','окно 35','окно 36','Что закрыто МАШИННО']; miss=[n for n in need if n not in s]; sys.exit('нет в 10-UAT.md: '+repr(miss) if miss else 0)"</automated>
    <fails_when>ненулевой код возврата либо строка «нет в 10-UAT.md:» с перечнем недостающих маркеров в выводе</fails_when>
  </verify>

  <acceptance_criteria>
- Проверок в файле ШЕСТЬ, и в сводке приведено их соответствие шести пунктам `human_verification`
  отчёта ОДИН К ОДНОМУ: номер проверки → пункт отчёта. Пункт, оставшийся без проверки, или
  проверка без пункта — отказ приёмки.
- Каждая из пяти новых проверок несёт запись `ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ` и ссылку на закрываемое
  окно; в сводке приведена таблица «проверка → окно» для окон 22, 28, 35, 36.
- Проверка приземления фокуса несёт ПРЕДУПРЕЖДЕНИЕ о пустой области ДО таблицы шагов; текст
  предупреждения приведён в сводке дословно.
- Раздел закрытого машинно называет не менее шести правил ИМЕНАМИ, и против каждого стои́т граница
  доказанного; перечень приведён в сводке.
- Гейт полноты обеих задач зелен: обе команды маркеров дают код возврата `0`.
- Все таблицы отметок ПУСТЫ; в сводке подтверждено, что ни одна ячейка не заполнена.
- `.planning/WINDOWS.md` не правится этой задачей: окна закрываются ПОСЛЕ обхода и человеком —
  `git diff --name-only -- .planning/WINDOWS.md` пусто.
  </acceptance_criteria>

  <done>
Шесть пунктов ручной верификации стали шестью проверками с шагами, ожидаемыми ответами и ссылками
на окна; граница доказанного машинно названа именами правил; предупреждение о пустой области стои́т
до шагов, а не после них.
  </done>
</task>

<task type="execute">
  <name>Задача 3: таблицы отметок, границы обхода и маршрутизация — что артефакт закрывает, чего не закрывает и куда уезжает</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `09-UAT.md:252-290` — «Почему этот артефакт существует» и «Наблюдаемые признаки — сняты с
  ОТГРУЖЕННОГО состояния»: форма раздела, объясняющего, ЗАЧЕМ обход, и откуда взяты ожидаемые
  признаки.
- `09-UAT.md:1397-1534` — «Что проверяется глазами, потому что иначе никак», «Допущения, которые
  обход обязан закрыть», «Что в этот обход НЕ входит», «Куда артефакт уезжает дальше». Формы
  четырёх завершающих разделов.
- `09-UAT.md:506-534` и `:1072-1144` — таблицы отметок первого и второго кругов с оговоркой
  «заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается».
- `10-VERIFICATION.md`, разделы «Gaps Summary» и «Requirements Coverage» — почему `FORM-06` сегодня
  не переводится в завершённое и почему исполнители, оставившие отметку ожидания, поступили верно.
- `.planning/ROADMAP.md:275,283` — критерий 2 и строка состава планов фазы.
- `.planning/WINDOWS.md`, записи 22, 28, 35, 36 — что именно закрывается прохождением обхода, а что
  остаётся открытым и после него.
  </read_first>

  <action>
**1. Таблицы отметок.** По одной на проверку, по форме обхода Фазы 9: дата, браузер и ОС, столбец
на каждый наблюдаемый признак проверки, столбец исхода. Над таблицами — оговорка дословно по
смыслу: заполняет человек на приёмке, отметка без наблюдённого признака закрытием не считается.
⚠️ ТАБЛИЦЫ ОСТАЮТСЯ ПУСТЫМИ.

**2. Раздел «Почему этот артефакт существует».** Называет ТРИ вещи: (а) вторая половина цели фазы
доказана ПРИСУТСТВИЕМ узлов и структурой, а применяет их рантайм разметки, которого суита не
поднимает; (б) роадмап сам называет это ручным UAT, пункт 4, и прямо говорит, что это единственный
вывод фазы, взятый из документации и не проверенный ни на чём; (в) планы закрытия гейпов
ОТКАЗАЛИСЬ подменять это имитацией, и артефакт продолжает тот же отказ — он не закрывает пункт, он
делает его проходимым.

**3. Раздел «Что в этот обход НЕ входит».** Перечисляет с основанием на запись: полный пункт
переинициализации клиентского состояния после подмены (Фаза 11); приземление фокуса на пути, где
тело документа подменяется целиком (Фаза 15, GATE-09); клиентский тайм-аут как цена гейта ухода
(открытое окно 34, фаза-владелец не назначена); остаток перечня целей внеполосных узлов (Фаза 15).
Каждая строка обязана называть, ПОЧЕМУ предмет не здесь, а не просто отсутствовать.

**4. Раздел «Куда артефакт уезжает дальше».** Называет: кто заполняет отметки (человек на приёмке
фазы); какие окна закрываются прохождением (22, 28, 35, 36) и КЕМ — командой ведения журнала, а не
ручной правкой строки; что `FORM-06` переводится в завершённое только после прохождения обхода И
повторной верификации, и что этот план его НЕ переводит.

**5. Собственный вывод обхода о `graphify`.** Одной строкой: граф проекта живёт только в основном
рабочем каталоге и в git не отслеживается (записи 23 и 25 `.planning/WINDOWS.md`), поэтому
обновление графа передаётся оркестратору, а не выполняется из рабочего дерева исполнителя.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib,sys; s=pathlib.Path('.planning/phases/10-rychag-components-modal-html/10-UAT.md').read_text(encoding='utf-8'); need=['Почему этот артефакт существует','Что в этот обход НЕ входит','Куда артефакт уезжает дальше','заполняет человек на приёмке','окно 34']; miss=[n for n in need if n not in s]; sys.exit('нет в 10-UAT.md: '+repr(miss) if miss else 0)"</automated>
    <fails_when>ненулевой код возврата либо строка «нет в 10-UAT.md:» с перечнем недостающих маркеров в выводе</fails_when>
  </verify>

  <verify>
    <human-check>Прочитать обход целиком глазами и ответить на один вопрос: можно ли пройти его по шагам, НИ РАЗУ не заглянув в отчёт верификации и в исходники? Пункт, требующий догадки о том, где нажать и что считать признаком, обходом не является.</human-check>
  </verify>

  <acceptance_criteria>
- Таблиц отметок ШЕСТЬ (по одной на проверку), все ПУСТЫ, и над каждой стои́т оговорка о том, что
  отметка без наблюдённого признака закрытием не считается.
- Четыре завершающих раздела на месте; в сводке приведены перечни «что не входит» с основаниями и
  «куда уезжает» с указанием окон 22, 28, 35, 36.
- В сводке названо прямо: `FORM-06` этим планом НЕ переводится в завершённое, отметка ожидания в
  требованиях не трогается, и основание — вторая половина требования закрывается глазом.
- `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/WINDOWS.md` не тронуты:
  `git diff --name-only -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/WINDOWS.md` —
  пусто.
- Все три гейта полноты (задачи 1, 2, 3) зелены; их выводы приведены в сводке.
- Прежний обход Фазы 9 не тронут:
  `git diff --name-only -- .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/` —
  пусто.
  </acceptance_criteria>

  <done>
Обход можно пройти по шагам без чтения отчётов и исходников; отметки ждут человека и пусты; границы
обхода названы с основаниями; маршрутизация окон и требования записана; артефакт ничего не
самозаверяет.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| рантайм разметки → документ | внеполосные узлы снятия применяет он, и никакая часть суиты его не поднимает |
| артефакт обхода → приёмка фазы | пустая отметка и заполненная отметка выглядят одинаково, если признак не назван заранее |
| посеянный стенд → живая база разработчика | строки обхода живут в той же базе, что и данные человека |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-54 | Repudiation | закрытие критерия 2 машинной имитацией рантайма | high | mitigate | Имитация не заводится ни в каком виде; артефакт называет основание отказа прямо и передаёт пункт человеку. Запрет внесён в `must_haves.prohibitions` и повторён в разделе «Почему этот артефакт существует» |
| T-10-55 | Repudiation | самозаверение обхода заранее заполненной отметкой | high | mitigate | Таблицы отметок остаются пустыми, над каждой стои́т оговорка о том, что отметка без наблюдённого признака закрытием не считается; пустота отметок — критерий приёмки всех трёх задач |
| T-10-56 | Tampering | посеянный стенд, переживший обход в живой базе | medium | mitigate | Общий префикс имени у всего посеянного; уборка объявлена ПОСЛЕДНИМ ШАГОМ обхода; проверочная команда печатает число оставшихся строк по каждой сущности; обе команды исполнены исполнителем, и вывод приведён в сводке |
| T-10-57 | Denial of Service | обход, усохший до тех проверок, которые кто-то помнит | medium | mitigate | Гейт полноты по несущим маркерам, написанный по форме обхода Фазы 9; три команды, по одной на задачу, каждая краснеет на пропаже любого маркера |
| T-10-58 | Spoofing | проверка приземления фокуса, записывающая расхождение там, где его нет | low | mitigate | Предупреждение о пустой области стои́т ДО таблицы шагов; ожидаемый ответ сформулирован как «осмысленное МЕСТО, но не осмысленный ТЕКСТ» и связан с суженной записью WR-02 |
| T-10-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не выполняет ни одной установки: ни новой зависимости, ни build-шага, ни нового вендоренного файла. Разведки у фазы нет по решению роадмапа — предмета аудита легитимности пакетов не существует |
</threat_model>

<verification>
- Три гейта полноты артефакта (по одному на задачу) — код возврата `0`.
- `git diff --name-only -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/WINDOWS.md` —
  пусто: план не переводит требование, не правит роадмап и не закрывает окон.
- `git diff --name-only -- .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/` —
  пусто: прежний обход читается, но не правится.
- Команды посева и уборки стенда ИСПОЛНЕНЫ, их выводы приведены, стенд после плана в базе не
  остаётся.
- Кодовая часть дерева планом не трогается: `git diff --name-only -- app/ tests/` — пусто.
- `graphify update .` — из рабочего дерева исполнителя не выполняется (записи 23 и 25
  `.planning/WINDOWS.md`); передать оркестратору.
- Ни одна из шести проверок машинно не закрывается и закрыться не может — `verification: backstop`
  в `must_haves`; на верификации это воздержание, а не молчаливый проход.
</verification>

<success_criteria>
1. Вторая половина цели фазы имеет ПРОХОДИМЫЙ артефакт: стенд приводится исполненной командой,
   критерий 2 проверяется четырьмя числами и ловушкой фокуса, ожидаемый ответ назван заранее.
2. Шесть пунктов `human_verification` перенесены один к одному, и соответствие «проверка → пункт»
   приведено в сводке.
3. Соответствие «проверка → окно» названо для окон 22, 28, 35, 36; сами окна планом не трогаются.
4. Машинная подмена рантайма не заведена, и основание отказа записано в самом артефакте.
5. Граница доказанного машинно названа именами правил — обход не переспрашивает то, что держат
   правила.
6. Отметки пусты, `FORM-06` не переводится, требования и роадмап не тронуты.
7. Полнота артефакта держится тремя машинными гейтами, а не вычиткой.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-10-SUMMARY.md` when done.

Сводка обязана:
- привести ДОСЛОВНО вывод исполненной команды посева (почта владельца, идентификатор объявления,
  адрес экрана) и вывод проверочной команды уборки (нули по трём сущностям);
- привести таблицу соответствия «проверка обхода → пункт `human_verification` отчёта» один к
  одному и назвать, что пунктов и проверок ровно по шесть;
- привести таблицу «проверка → окно» для окон 22, 28, 35, 36;
- привести перечень величин, снимаемых проверкой критерия 2, и ожидаемый ответ по каждой;
- привести дословно предупреждение о пустой области уведомлений, стоящее перед шагами проверки
  приземления фокуса;
- привести перечень правил раздела «Что закрыто МАШИННО» с границей доказанного против каждого;
- привести выводы всех трёх гейтов полноты;
- подтвердить пустоту всех шести таблиц отметок и назвать прямо, что `FORM-06` не переводился, а
  окна не закрывались;
- назвать НЕЗАКРЫТЫМИ и передать приёмке фазы: критерий 2 и все шесть пунктов ручной верификации —
  до прохождения обхода человеком они остаются открытыми;
- передать оркестратору невыполненный `graphify update .` с основанием записей 23 и 25.
</output>
