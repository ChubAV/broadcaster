---
phase: 10-rychag-components-modal-html
plan: 16
type: execute
wave: 2
# ⚠️ ЗАВИСИМОСТЬ РАСШИРЕНА ДО ДВУХ, И ЭТО СЛЕДСТВИЕ РЕШЕНИЯ О ВЛАДЕЛЬЦЕ РЕЕСТРА (записано в
# <objective>): настоящий план дописывает в `.planning/WINDOWS.md` окна, ТЕКСТ которых пишут
# планы 10-12 и 10-13, — значит он обязан идти строго ПОСЛЕ обоих, а не только после 10-13.
# Оба они стоят в волне 1, поэтому волна настоящего плана не двигается: max(1, 1) + 1 = 2.
depends_on: ["10-12", "10-13"]
gap_closure: true
files_modified:
  - .planning/phases/10-rychag-components-modal-html/10-UAT.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [FORM-06]
user_setup:
  - service: локальный стенд приложения и браузер
    why: "Вторая половина цели фазы («после N удалений в документе не остаётся N мёртвых диалогов») применяется РАНТАЙМОМ РАЗМЕТКИ. Суита идёт через транспорт без процесса на встроенной базе и не исполняет ни строчки JS: доказано ПРИСУТСТВИЕ узла в теле ответа, а не его ПРИМЕНЕНИЕ. Машинного пути закрыть это у проекта НЕТ — браузерных зависимостей в дереве нет ни одной (проверено планировщиком по `pyproject.toml`: ни `playwright`, ни `selenium`), и заводить их этой партией не предлагается."
    dashboard_config:
      - task: "Поднять приложение из корня репозитория, где лежит `.env`: `just run`; экран обхода — `http://localhost:8000/ads/{ad_id}/edit`"
        location: "локальная машина разработчика"
      - task: "Посеять стенд по разделу «Как привести стенд в нужное состояние» артефакта `10-UAT.md`: ОДНО объявление с НЕ МЕНЕЕ ЧЕМ ТРЕМЯ расписаниями, всё под префиксом имени `UAT10-`"
        location: "локальная база разработчика"
      - task: "Пройти шесть проверок артефакта `10-UAT.md`, заполнить шесть таблиц отметок наблюдёнными признаками и убрать стенд по разделу «Как убрать стенд»"
        location: "браузер с открытой консолью и вкладкой сетевых запросов"

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ, И ЭТО РЕШЕНИЕ,
# А НЕ ПРОПУСК. Основание СНЯТО ЗАМЕРОМ ПЛАНИРОВЩИКОМ, а не взято из памяти:
#  — СХЕМА ПЛАНА GSD ЭТИХ ПОЛЕЙ НЕ ЗНАЕТ. Обязательный перечень — `phase`, `plan`, `type`,
#    `wave`, `depends_on`, `files_modified`, `autonomous`, `must_haves`
#    (`gsd-core/bin/lib/frontmatter.cjs:1215`, он же продублирован в `verify.cjs:851`);
#    ни одного из двух имён в нём нет.
#  — ОБЕ ВЕЛИЧИНЫ ЖИВУТ В КОНФИГЕ и разрешаются ОРКЕСТРАТОРОМ на диспетче планировщика
#    (`gsd-core/workflows/plan-phase.md:420` и `:787`), а планом не объявляются.
#  — ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `config-get workflow.security_asvs_level` → `1`;
#    `config-get workflow.security_block_on` → `high`;
#    `config-get workflow.security_enforcement` → `true`.
#    Таблица STRIDE ниже собрана ИМЕННО НА ЭТОМ УРОВНЕ И С ЭТИМ ПОРОГОМ, и обе величины
#    названы в самой таблице, а не подразумеваются.
# ДОПИСАТЬ ОБА ПОЛЯ В ПЯТЬ НОВЫХ ПЛАНОВ — ОТВЕРГНУТО: их нет ни у одного из шестнадцати планов
# фазы и вообще нигде в `.planning/` (замер проверяющего), и заведённые здесь они дали бы
# конвенцию, расходящуюся с одиннадцатью предыдущими планами, вместо записи о том, где
# величины на самом деле живут. МОЛЧАНИЕ ОТВЕРГНУТО ТОЖЕ: оно неотличимо от пропуска — именно
# поэтому решение записано, а не оставлено умолчанием.

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер. Состояние калибровки СНЯТО
  # ПЛАНИРОВЩИКОМ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. Отсюда tokens == raw_tokens и confidence: low, ВЫВЕДЕННЫЙ ИЗ
  # ЧИСЛА ЗАМЕРОВ, а не из самооценки планировщика.
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ШАГ, ИДУЩИЙ ПО ПОЧИНЕННОЙ ВЕТКЕ, СПРАШИВАЕТ ТО, ЧТО ЧИНИЛОСЬ: переходная ветка обхода наблюдает не только смену экрана, но и ЖИВОСТЬ КЛИЕНТСКОГО СЛОЯ пришедшего экрана — счётчик символов, загрузку изображений и снятие вложения, то есть ровно те три вещи, которые умирали"
    - "ПРЕЖНЕЕ ОЖИДАНИЕ ПОМЕЧЕНО ОПРОВЕРГНУТЫМ, А НЕ ВЫЧЕРКНУТО: верификатор ожидал на этом шаге ОТКАЗ и был прав для дерева, на котором писал; после правки плана 10-13 ожидание меняется, и обе формулировки стоят рядом с названным источником расхождения"
    - "МАШИННАЯ ПОЛОВИНА `CR-03` ВНЕСЕНА В ПЕРЕЧЕНЬ ЗАКРЫТОГО МАШИННО ВМЕСТЕ СО СВОЕЙ ГРАНИЦЕЙ: доказано, что исходник скрипта переживает второе исполнение в одной области имён; НЕ доказано, что рантайм браузера ведёт себя по установленной цепи"
    - "АРТЕФАКТ ПО-ПРЕЖНЕМУ НИЧЕГО НЕ САМОЗАВЕРЯЕТ: `status: testing` сохраняется, таблицы отметок остаются пустыми, и собственный отказ артефакта переводить `FORM-06` в завершённое переживает эту правку дословно"
    - "ЧЕГО ТРЕТЬЯ ПАРТИЯ НЕ ЗАКРЫЛА — НАЗВАНО В САМОМ АРТЕФАКТЕ: критерий 2 машинного пути закрытия у проекта не имеет, браузерных зависимостей в дереве нет ни одной, и партия их не заводит; пункт остаётся человеческим и переходит в приёмку фазы"
    - "ВОСЬМОЕ ИЗМЕРЕНИЕ НАЙКВИСТА У ЭТОЙ ФАЗЫ НЕ ЗАКРЫТО ПО ЯВНОМУ ВЫБОРУ ОПЕРАТОРА, И ЭТО ЗАПИСАНО ОКНОМ, а не обнаружено следующим кругом: режим закрытия гейпов разведку не запускает, `10-RESEARCH.md` и `10-VALIDATION.md` у фазы нет"
    - "У РЕЕСТРА ОКОН В ТРЕТЬЕЙ ПАРТИИ ЕСТЬ РОВНО ОДИН ВЛАДЕЛЕЦ, И ЭТО НАЗНАЧЕНО РЕШЕНИЕМ, А НЕ ВЫШЛО УМОЛЧАНИЕМ: `.planning/WINDOWS.md` объявлен в `files_modified` ровно одного плана партии — настоящего, — и ни один другой план партии в него не пишет ни байта; проверяется по всем пяти планам разом"
    - "НИ ОДНО ОКНО ПАРТИИ НЕ ПОТЕРЯНО ПРИ ПЕРЕДАЧЕ: четыре окна дописаны ОДНИМ владельцем, тексты двух из них перенесены ДОСЛОВНО из сводок планов 10-12 и 10-13, и каждому присвоен номер, названный в сводке"
    - statement: "Фактическое снятие осиротевшей панели РАНТАЙМОМ разметки наблюдено человеком и записано в таблицу отметок проверки 1 наблюдёнными числами"
      verification: backstop
    - statement: "Клиентский слой редактора после перехода по заголовку `HX-Location` жив: консоль чиста, счётчик символов отзывается, вложение снимается — наблюдено человеком и записано в таблицу отметок"
      verification: backstop
  artifacts:
    - ".planning/phases/10-rychag-components-modal-html/10-UAT.md — переходная ветка проверки 1, дополненная наблюдением живости клиентского слоя, с летописью изменившегося ожидания"
    - ".planning/phases/10-rychag-components-modal-html/10-UAT.md — строка машинной половины `CR-03` в перечне закрытого машинно, со своей границей доказанного"
    - ".planning/phases/10-rychag-components-modal-html/10-UAT.md — раздел «Чего третья партия закрытия гейпов НЕ закрыла» с названными основаниями"
    - ".planning/WINDOWS.md — окно о незакрытом восьмом измерении Найквиста у Фазы 10"
    - ".planning/WINDOWS.md — окно плана 10-12 о непокрытых соседних страничных модулях (текст выписан планом 10-12 в его сводку, строку реестра дописывает НАСТОЯЩИЙ план)"
    - ".planning/WINDOWS.md — окно плана 10-13 об экране подключения (текст выписан планом 10-13 в его сводку, строку реестра дописывает НАСТОЯЩИЙ план)"
    - ".planning/WINDOWS.md — окно о семнадцати командах `<automated>` планов 10-01…10-07 без сформулированного направления отказа: долг записан, а не обнаружен следующим кругом"
  key_links:
    - "шаг переходной ветки → починка плана 10-13: единственное, чем глаз проверяет ровно то, что чинилось; ломается — обход идёт по починенной ветке и не спрашивает о ней ничего, как и прошлый раз"
    - "перечень закрытого машинно → границы доказанного: единственное, чем читатель не примет зелёное правило за закрытие пункта, которого оно не закрывает; ломается — запись становится шире дерева, а это тот класс расхождения, которым фаза провалена первым кругом"
    - "отказ артефакта самозаверяться → приёмка фазы: единственное, чем непройденный обход не превращается в закрытое требование; ломается — повторяется коммит `0ea886d`"
  prohibitions:
    - statement: "машинная подмена разметочного рантайма НЕ заводится ни в каком виде: правило, объявившее, что внеполосный узел ПРИМЕНЁН, вернуло бы фазе ровно тот дефект, которым она провалена первым кругом. Планы 10-05…10-07 и 10-10 от этого отказались сознательно, и настоящий план продолжает тот же отказ"
      status: flagged-unverified
      verification: none
    - statement: "браузерная зависимость НЕ заводится этой партией ради зелёной клетки: её нет в дереве (проверено), её появление есть решение ВЕХИ, а не побочная правка партии закрытия гейпов, и введённая наспех она дала бы новый класс непроверенного вместо закрытия старого"
      status: flagged-unverified
      verification: none
    - statement: "таблицы отметок НЕ заполняются заранее и не заполняются исполнителем плана: отметка без наблюдённого признака есть самозаверение, а не приёмка, и это записано в самом артефакте"
      status: flagged-unverified
      verification: none
    - statement: "отметки прежнего обхода Фазы 9 НЕ переносятся на новый предмет: перенос отметки есть закрытие дефекта переносом записи о нём"
      status: flagged-unverified
      verification: none
  # ⚠️ РЕБРА ЗОНДА ПОКРЫТИЯ — ЯВНЫЕ ДОПУЩЕНИЯ ЭТОЙ ПАРТИИ, А НЕ МОЛЧАЛИВЫЕ ПОТЕРИ.
  # Два из пяти рёбер зонда НЕ РАЗРЕШЕНЫ ни одним планом партии, и защитимого
  # машинного критерия приёмки у них нет:
  #   • `ordering` («когда элементы равны, задан ли порядок вывода и устойчив ли он») —
  #     предмет здесь есть ПОРЯДОК ПРИМЕНЕНИЯ трёх внеполосных узлов ответа рантаймом.
  #     Он наблюдаем только рантаймом, и ни одно правило проекта на него не опирается;
  #     формулировать критерий, которого никто не проверяет, значило бы завести запись
  #     шире дерева. Остаётся `unresolved` и НАЗВАН ЗДЕСЬ.
  #   • `concurrency` («что гарантировано при обрыве или параллельном запуске») —
  #     частично адресован гейтом ухода панели по признаку отправки (план 10-07,
  #     доказан в интерпретаторе и мутацией) и проверками 2 и 3 обхода; ПОЛНОГО
  #     машинного критерия — два подтверждённых удаления в полёте одновременно — в
  #     суите без процесса выразить нечем. Остаётся `unresolved` и НАЗВАН ЗДЕСЬ.
  # Остальные три ребра разрешены: `adjacency` и `idempotency` — планом 10-12,
  # `empty` — планом 10-13.
  # РАВЕНСТВО БЕЗ МОЛЧАЛИВЫХ ПОТЕРЬ ПО ПАРТИИ: 5 предъявленных = 3 разрешённых
  # критерием + 2 явных допущения.
---

<objective>
Закрывается машинно-подготовительная половина ДВУХ оставшихся человеческих пунктов: третьего
`missing` гэпа `CR-03` («пройти шаг 1.10 обхода `10-UAT.md`») и пункта
`behavior_unverified` — критерия 2 роадмапа, второй половины цели фазы, которую до сих пор не
наблюдал никто.

Purpose: обход `10-UAT.md` написан планом 10-10 ЧЕСТНО и идёт ровно по сломанной ветке — но его шаг
переходной ветки спрашивает только о СМЕНЕ ЭКРАНА и ничего не спрашивает о том, что на пришедшем
экране осталось живым. Именно поэтому регрессия `CR-03` дожила до верификации, даже имея готовый
шаг, который прошёл бы по ней. После правки плана 10-13 ожидание на этом шаге МЕНЯЕТСЯ: верификатор
ожидал здесь отказ и был прав для дерева, на котором писал.

Output: переходная ветка обхода, спрашивающая о живости клиентского слоя тремя наблюдениями;
летопись изменившегося ожидания с названным источником; машинная половина `CR-03` в перечне
закрытого машинно вместе со своей границей; раздел «чего третья партия НЕ закрыла»; ЧЕТЫРЕ окна
реестра — о непокрытых соседних страничных модулях (текст плана 10-12), об экране подключения (текст
плана 10-13), о незакрытом восьмом измерении Найквиста и о долге по направлению отказа планов
10-01…10-07.

⚠️ **РЕШЕНИЕ О ВЛАДЕЛЬЦЕ РЕЕСТРА ОКОН — ПРИНЯТО, А НЕ ВЫШЛО УМОЛЧАНИЕМ.** `.planning/WINDOWS.md`
в третьей партии закрытия гейпов пишет РОВНО ОДИН план — настоящий, задачей 2. Планы 10-12 и 10-13
тексты своих окон ПИШУТ (дословно, в свои сводки), а СТРОКУ реестра не пишут и файла в
`files_modified` не несут; план 10-15 делает то же на своём отказном пути останова.

Основание — тремя пунктами, и первый несущий:
(а) `WINDOWS.md` есть ОБЩИЙ ДОПИСЫВАЕМЫЙ РЕЕСТР (таблица на 894 строки, дописывается через
`gsd-tools windows append`). Два плана ОДНОЙ ВОЛНЫ, дописывающие в него, сталкиваются на файле,
которого гейт волны НЕ ВИДИТ, если пересечение их `files_modified` пусто ПО ОБЪЯВЛЕНИЮ, а не по
факту. Ровно это и было: план 10-12 писал окно задачей 3, НЕ объявив файла, а план 10-13 той же
волны 1 объявлял его честно.
(б) ПАРТИЯ ОТКРЫЛАСЬ ТРЕМЯ ПРЕТЕНДЕНТАМИ НА ОДИН РЕЕСТР (10-12 — молча, 10-13 и 10-16 — явно) И БЕЗ
НАЗНАЧЕННОГО ВЛАДЕЛЬЦА. Незаявленная запись в общий файл есть ровно та форма отказа, ради которой
партия существует: «объявлено там, куда смотрели, и не объявлено там, куда не смотрели». Воспроизводить
её внутри самой партии запрещено.
(в) ВЛАДЕЛЬЦЕМ НАЗНАЧЕН ИМЕННО ЭТОТ ПЛАН, потому что реестр непокрытого — его собственный предмет:
он и есть план «назвать, чего третья партия НЕ закрыла», он уже стои́т в ВОЛНЕ 2 после обоих авторов
текстов, и `.planning/WINDOWS.md` уже стоял в его `files_modified`.

ОТВЕРГНУТАЯ АЛЬТЕРНАТИВА НАЗВАНА: дописать `.planning/WINDOWS.md` в `files_modified` плана 10-12 и
развести 10-12 и 10-13 по волнам. Она чинит ОДНО столкновение и оставляет реестр БЕЗ ВЛАДЕЛЬЦА —
четвёртый претендент приехал бы так же молча, и следующая партия открылась бы с тем же дефектом.

СЛЕДСТВИЕ ДЛЯ ЭТОГО ПЛАНА: задача 2 дописывает ЧЕТЫРЕ окна, а не одно, и два из них — чужие тексты,
переносимые ДОСЛОВНО.

⚠️ ЭТОТ ПЛАН НЕ ЗАКРЫВАЕТ КРИТЕРИЙ 2 И НЕ ДЕЛАЕТ ВИДА, ЧТО ЗАКРЫВАЕТ. Машинного пути закрыть
применение внеполосного узла рантаймом у проекта НЕТ: браузерных зависимостей в дереве нет ни одной
(проверено планировщиком), и партия их не заводит. Пункт остаётся ЧЕЛОВЕЧЕСКИМ, переходит в приёмку
фазы и до прохождения обхода держит требование `FORM-06` незавершённым.

⚠️ РЕЖИМ ЧЕЛОВЕЧЕСКОЙ ПРОВЕРКИ ПРОЕКТА — `end-of-phase`, поэтому блокирующего останова внутри плана
не заводится: наблюдение снимается на приёмке фазы, и его требование выражено в `<verify>` как
человеческая проверка, а не как задача-останов.

⚠️ ЭТОТ ПЛАН — ЗАКРЫТИЕ ГЕЙПОВ (`gap_closure: true`), ТРЕТЬЯ ПАРТИЯ, ВОЛНА 2. Зависимость от плана
10-13 — СУЩЕСТВЕННАЯ: до его правки ожидание на переходной ветке ДРУГОЕ, и переписать его раньше
значило бы записать в обход ожидание, которого дерево не держит. Зависимость от плана 10-12
прибавлена решением о владельце реестра: настоящий план переносит в реестр ТЕКСТ, который пишет
10-12, и обязан идти после него. Волна от этого не двигается — оба автора стоят в волне 1.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-10-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-13-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Создаётся ЭТИМ планом (проход source-grounding обязан считать новым):

- шаги `1.13`, `1.14`, `1.15` переходной ветки проверки 1 — наблюдение живости клиентского слоя,
  `.planning/phases/10-rychag-components-modal-html/10-UAT.md`
- колонка `1.13-1.15 клиентский слой` в таблице отметок проверки 1, там же
- абзац летописи ожидания переходной ветки («ожидался отказ → ожидается живой слой»), там же
- строка машинной половины `CR-03` в таблице «Что закрыто МАШИННО», там же
- раздел «Чего третья партия закрытия гейпов НЕ закрыла», там же
- окно о незакрытом восьмом измерении Найквиста у Фазы 10, `.planning/WINDOWS.md`
- окно плана 10-12 о непокрытых соседних страничных модулях — СТРОКА реестра, там же (текст создан
  планом 10-12 и новым здесь не является; новой является строка)
- окно плана 10-13 об экране подключения — СТРОКА реестра, там же (то же основание)
- окно о семнадцати командах `<automated>` планов 10-01…10-07 без сформулированного направления
  отказа, там же

Существует ДО плана и только ПРАВИТСЯ либо СОХРАНЯЕТСЯ ДОСЛОВНО (новым не является): шапка
артефакта с `status: testing` и `checks_declared: 6`, шесть проверок, шесть таблиц отметок, разделы
посева и уборки стенда, раздел «Что в этот обход НЕ входит», раздел «Куда артефакт уезжает дальше».
</artifacts_this_phase_produces>

<tasks>

<task type="execute">
  <name>Задача 1: переходная ветка обхода спрашивает о том, что чинилось — живость клиентского слоя, и летопись изменившегося ожидания</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1-32` — шапка артефакта ЦЕЛИКОМ.
  ⚠️ `status: testing`, `checks_declared: 6` и комментарий о запрете ставить `passed` до обхода
  СОХРАНЯЮТСЯ ДОСЛОВНО. Правка, тронувшая их, превратила бы артефакт в самозаверяющий.
- `:238-286` — проверка 1 ЦЕЛИКОМ: предусловие, таблица из двенадцати шагов, раздел снимаемых
  величин, таблица отметок. Новые шаги встают ПОСЛЕ шага 1.12 и наследуют форму соседей: столбец
  шага и столбец ожидаемого наблюдаемого признака, ожидание набрано теми же словами.
- `:263-265` — шаги 1.10-1.12, переходная ветка. ⚠️ ЧИТАТЬ ДОСЛОВНО: шаг 1.10 ставит признак в окно
  браузера и наблюдает статус ответа и заголовок перехода; 1.11 читает признак обратно; 1.12
  считает диалоги на пришедшем экране. НИ ОДИН из трёх не спрашивает, что на пришедшем экране
  осталось РАБОТАТЬ, — и это единственная причина, по которой готовый шаг не предъявил регрессию.
- `:267-276` — раздел «Что снимает эта проверка» и таблица отметок с её колонками. Колонка
  прибавляется, а не переписывается.
- `10-VERIFICATION.md`, второй пункт `human_verification` — дословное ожидание верификатора на шаге
  1.10: «⚠️ ВЕРИФИКАТОР ОЖИДАЕТ ЗДЕСЬ ОТКАЗ». ⚠️ ЭТО И ЕСТЬ ПРЕЖНЯЯ ФОРМУЛИРОВКА, которую надо
  пометить опровергнутой, а не стереть: для дерева, на котором она писалась, она ВЕРНА.
- `10-REVIEW.md`, раздел `CR-03`, абзац «Observable result» — перечень того, что умирало: загрузка
  изображений, счётчик символов, перехват снятия изображения, управление плиткой добавления. Первые
  три и есть предмет новых шагов.
- `app/templates/ads/form.html` после плана 10-13 — узлы, за которые цепляются наблюдения:
  область текста и её счётчик, плитка добавления и поле выбора файла, кнопка снятия вложения. ⚠️
  ИМЕНА УЗЛОВ БРАТЬ ИЗ ШАБЛОНА, а не из памяти: шаг, называющий несуществующий узел, не проверяет
  ничего и обесценивает обход при первом же прохождении.
- `10-13-SUMMARY.md` — что именно правкой сделано и какова ГРАНИЦА доказанного машинной половиной.
  Строка перечня закрытого машинно собирается из неё, а не сочиняется здесь.
- `:462-478` — таблица «Что закрыто МАШИННО»: форма строки — утверждение, имя правила, ГРАНИЦА
  доказанного. Новая строка наследует все три столбца.
  </read_first>

  <precondition>
План 10-13 приземлён: правка `app/templates/ads/form.html` в дереве, правило двойного исполнения
зелено, и его вывод приведён в `10-13-SUMMARY.md`. Без этого ожидание переходной ветки менять
НЕЛЬЗЯ: обход, записавший ожидание, которого дерево не держит, хуже обхода, не спрашивающего
ничего, — он предъявит человеку отказ и заставит искать несуществующую причину.
  </precondition>

  <behavior>
1. Переходная ветка проверки 1 получает ТРИ новых шага, наблюдающих ровно то, что умирало:
   консоль пришедшего экрана чиста от ранней ошибки языка; счётчик символов отзывается на ввод;
   вложение снимается кнопкой снятия.
2. Каждый шаг называет ОЖИДАЕМЫЙ НАБЛЮДАЕМЫЙ ПРИЗНАК в той же форме, что и двенадцать соседних:
   что именно человек увидит, а не «работает».
3. Таблица отметок получает колонку под новые шаги. Колонок становится на одну больше; таблица не
   переписывается.
4. Летопись ожидания записана: прежнее ожидание («здесь ожидается ОТКАЗ») названо, помечено
   опровергнутым для сегодняшнего дерева, источник расхождения назван поимённо (правка плана 10-13),
   и прямо сказано, что прежнее ожидание НЕ БЫЛО ОШИБКОЙ — оно было верным для дерева, на котором
   писалось, и пережило правку.
5. Таблица «Что закрыто МАШИННО» получает строку машинной половины `CR-03` с её ГРАНИЦЕЙ: доказано,
   что исходник переживает второе исполнение в одной области имён; НЕ доказано, что рантайм
   браузера ведёт себя по установленной цепи, — и вот почему новые шаги нужны, несмотря на зелёное
   правило.
6. Шапка артефакта, шесть проверок, шесть таблиц отметок и все прочие разделы сохраняются: ни одна
   отметка не заполняется, `status` не двигается, `checks_declared` не двигается (проверок остаётся
   ШЕСТЬ — прибавлены ШАГИ внутри существующей проверки, а не проверка).
  </behavior>

  <action>
**1. Три новых шага переходной ветки.** Дописать в таблицу проверки 1 после шага 1.12:

— **1.13 — КОНСОЛЬ ПРИШЕДШЕГО ЭКРАНА.** Человек смотрит консоль сразу после перехода. Ожидаемый
наблюдаемый признак: ранней ошибки языка о повторном объявлении имени в консоли НЕТ. ⚠️ В тексте
шага НАЗВАТЬ, что именно искали бы до правки: сообщение о том, что имя уже объявлено, — чтобы
человек узнал признак, если он всё-таки появится, а не пролистал незнакомый текст.

— **1.14 — СЧЁТЧИК СИМВОЛОВ.** Человек ставит курсор в поле текста объявления и печатает несколько
символов. Ожидаемый наблюдаемый признак: число под полем МЕНЯЕТСЯ на каждый символ. Признак выбран
потому, что счётчик — самая дешёвая из трёх умиравших функций и не требует ни файла, ни сети.

— **1.15 — СНЯТИЕ ВЛОЖЕНИЯ.** Если у объявления есть вложения: человек нажимает кнопку снятия на
плитке. Ожидаемый наблюдаемый признак: плитка исчезает на месте, документ не перезагружается. Если
вложений нет — шаг помечается неприменимым и это записывается, а не пропускается молча.

⚠️ ИМЕНА И ПОЛОЖЕНИЯ УЗЛОВ БЕРУТСЯ ИЗ ШАБЛОНА ЧТЕНИЕМ, а не из памяти: имя узла, названное неверно,
обесценивает шаг при первом же прохождении, и человек запишет отказ, которого нет.

**2. Колонка отметок.** Прибавить к таблице отметок проверки 1 колонку `1.13-1.15 клиентский слой`.
Существующие колонки не трогать, строку отметок оставить пустой.

**3. Летопись ожидания.** Перед таблицей шагов (либо сразу после неё — там, где стоят прочие
предупреждения проверки 1) дописать абзац:
— прежнее ожидание названо дословно: отчёт верификации третьего круга ожидал на этом шаге ОТКАЗ и
называл его признак — раннюю ошибку языка и мёртвые загрузку изображений, счётчик символов и снятие
вложений;
— оно ПОМЕЧЕНО ОПРОВЕРГНУТЫМ ДЛЯ СЕГОДНЯШНЕГО ДЕРЕВА и НЕ ВЫЧЕРКНУТО;
— источник расхождения назван поимённо: правка плана 10-13, убравшая объявления верхнего уровня из
области имён документа;
— сказано прямо, что прежнее ожидание не было ошибкой: оно было верным для дерева, на котором
писалось, и правится не оно, а ожидание, которое оно пережило;
— и назван смысл шагов после правки: они предъявляют не отказ, а ЖИВОСТЬ, и потому остаются нужными
— зелёное машинное правило доказывает форму исходника, а не поведение рантайма.

Форма записи — идиома проекта: опровергнутое помечается рядом с истинным, а не стирается (D-30/D-32
`STATE.md`), и та же форма, которой фаза уже записала летописи «16 → 18» и «7/7 → 11/11».

**4. Строка в перечне закрытого машинно.** Дописать в таблицу «Что закрыто МАШИННО» строку:
утверждение — исходник инлайн-скрипта экрана редактора переживает второе исполнение в одной области
имён, и ни одна цель заголовка перехода не несёт объявлений верхнего уровня; имена правил — взять
из `10-13-SUMMARY.md` дословно; ГРАНИЦА доказанного — доказана ФОРМА ИСХОДНИКА и его поведение в
интерпретаторе; НЕ доказано, что рантайм браузера ведёт себя по цепи, установленной `CR-03`, и что
человек видит живой клиентский слой, — **это шаги 1.13-1.15**.

**5. Что НЕ трогается.** Шапка (`status`, `checks_declared`, комментарий о запрете `passed`), шесть
таблиц отметок (остаются пустыми), разделы посева и уборки, раздел «Что в этот обход НЕ входит»,
раздел «Куда артефакт уезжает дальше», отметки обхода Фазы 9 в `09-UAT.md`.
  </action>

  <verify>
    <automated>grep -c '^| 1\.1[3-5] ' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод отличен от `3` — три новых шага переходной ветки не заведены либо заведены не строками таблицы</fails_when>
    <automated>grep -c '^status: testing$' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод отличен от `1` — состояние артефакта сдвинуто, то есть обход объявлен пройденным без прохождения</fails_when>
    <automated>grep -c '^checks_declared: 6$' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод отличен от `1` — число проверок сдвинуто, хотя прибавлены ШАГИ внутри существующей проверки, а не проверка</fails_when>
    <automated>git diff -- .planning/phases/10-rychag-components-modal-html/10-UAT.md | grep -c '^-'</automated>
    <fails_when>вывод больше `4` — правка удаляет строки артефакта, а прибавление шагов и колонки удаления строк не требует; каждая удалённая строка обязана быть названа в сводке поимённо</fails_when>
  </verify>

  <acceptance_criteria>
- Три новых шага заведены и каждый называет ОЖИДАЕМЫЙ НАБЛЮДАЕМЫЙ ПРИЗНАК в форме соседних
  двенадцати; в сводке приведён текст всех трёх.
- Имена узлов, на которые ссылаются шаги, ВЗЯТЫ ИЗ ШАБЛОНА: в сводке названы файл и строка каждого
  узла, а также способ, которым это проверено.
- Летопись ожидания записана; в сводке приведён её текст, и в нём видны все пять обязательных
  частей: прежнее ожидание дословно, пометка опровергнутости, источник расхождения поимённо, прямое
  «прежнее не было ошибкой», смысл шагов после правки.
- Строка перечня закрытого машинно заведена, и её граница названа; имена правил СЛИЧЕНЫ с
  `10-13-SUMMARY.md` — в сводке приведены оба написания.
- Колонка отметок прибавлена, строка отметок ОСТАЛАСЬ ПУСТОЙ; в сводке приведён вид таблицы.
- Шапка не тронута: в сводке приведён вывод трёх проверок состояния (`status`, `checks_declared`,
  комментарий о запрете).
- Отметок не заполнено ни одной:
  `grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' .planning/phases/10-rychag-components-modal-html/10-UAT.md`
  → `0`.
- `09-UAT.md` не тронут: `git diff --name-only -- .planning/phases/09-*/` — пусто.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится только артефакт обхода; поведения продукта не меняется. Прежнее ожидание сохранено рядом с
новым, поэтому откат не требует восстановления утраченного текста.
  </reversibility>

  <done>
Переходная ветка обхода спрашивает о живости клиентского слоя тремя наблюдаемыми признаками;
изменение ожидания записано летописью с названным источником и без вычёркивания; машинная половина
`CR-03` внесена в перечень закрытого машинно вместе со своей границей; артефакт по-прежнему ничего
не самозаверяет.
  </done>
</task>

<task type="execute">
  <name>Задача 2: назвать, чего третья партия НЕ закрыла, и дописать ВСЕ ЧЕТЫРЕ окна партии — этот план владелец реестра</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
.planning/WINDOWS.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:33-63` — раздел «Почему этот артефакт
  существует» ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ДОСЛОВНО ЕГО ТРЕТИЙ ПУНКТ: планы закрытия гейпов ОТКАЗАЛИСЬ
  подменять применение рантайма имитацией, и артефакт продолжает тот же отказ. Новый раздел
  наследует этот тон и ту же прямоту.
- `:482-497` — раздел «Что в этот обход НЕ входит»: форма таблицы «пункт → почему не здесь». Новый
  раздел стоит РЯДОМ с ним и его не подменяет: тот отвечает «почему не здесь», новый — «чего партия
  не закрыла вообще».
- `:498-504` — раздел «Куда артефакт уезжает дальше»: кто заполняет отметки и что закрывается
  прохождением.
- `10-VERIFICATION.md`, `behavior_unverified_items` — дословная формулировка непроверенного
  поведения и его основание `why_human`. Новый раздел ссылается на неё, а не пересказывает по
  памяти.
- `.planning/WINDOWS.md`, записи 39-44 — форма записи окна: номер, фаза, тип, файл, строка, текст,
  состояние. Новые окна наследуют форму. ⚠️ ЭТОТ ПЛАН — ЕДИНСТВЕННЫЙ ПИШУЩИЙ В РЕЕСТР В ПАРТИИ
  (решение и его основание — в `<objective>`), поэтому форму читает он и никто больше.
- `.planning/phases/10-rychag-components-modal-html/10-12-SUMMARY.md`, раздел с ДОСЛОВНЫМ текстом
  окна о непокрытых соседних страничных модулях. ⚠️ ЧИТАТЬ ДОСЛОВНО И ПЕРЕНОСИТЬ ДОСЛОВНО: текст
  написан автором замера, и пересказ по памяти потерял бы именно то, ради чего окно заводится.
  Отсутствие раздела — ОСТАНОВ: значит, план 10-12 приземлился, не выписав переданного текста, и
  окно сочинять здесь запрещено.
- `.planning/phases/10-rychag-components-modal-html/10-13-SUMMARY.md`, раздел с ДОСЛОВНЫМ текстом
  окна об экране подключения. То же требование и тот же останов.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — раздел зонда направления
  отказа. ⚠️ ЧИСЛО `17` И ПЕРЕЧЕНЬ ПЛАНОВ `10-01…10-07` БЕРУТСЯ ОТТУДА, а не из настоящего плана:
  текст окна о долге обязан ссылаться на замер, а не на пересказ.
- `pyproject.toml`, блоки зависимостей ЦЕЛИКОМ. ⚠️ ПРОВЕРИТЬ СВОИМИ ГЛАЗАМИ: браузерных
  зависимостей в дереве нет ни одной. Планировщик это снял, но утверждение уезжает в артефакт и
  обязано быть перепроверено, а не унаследовано.
  </read_first>

  <precondition>
Сводки `10-12-SUMMARY.md` и `10-13-SUMMARY.md` существуют и несут ДОСЛОВНЫЕ тексты своих окон.
Проверяется перед началом задачи; отсутствие любого из двух — ОСТАНОВ С ЗАПИСЬЮ, а не повод сочинить
текст здесь: окно, пересказанное владельцем реестра вместо автора замера, теряет ровно ту точность,
ради которой заводится.
  </precondition>

  <behavior>
0. РЕЕСТР ОКОН ПИШЕТ ТОЛЬКО ЭТА ЗАДАЧА И ТОЛЬКО ОНА ВО ВСЕЙ ПАРТИИ. Дописываются ЧЕТЫРЕ окна: два
   чужих текста (планов 10-12 и 10-13), переносимых ДОСЛОВНО, и два своих (незакрытое восьмое
   измерение Найквиста и долг по направлению отказа планов 10-01…10-07).
1. Артефакт получает раздел, называющий поимённо, чего третья партия закрытия гейпов НЕ закрыла, и
   ПОЧЕМУ — с основанием у каждого пункта, а не перечислением.
2. Первым пунктом — критерий 2: машинного пути закрыть применение внеполосного узла рантаймом у
   проекта нет; браузерных зависимостей в дереве нет ни одной (перепроверено); заводить их партией
   закрытия гейпов — решение вехи, а не побочная правка, и введённые наспех они дали бы новый класс
   непроверенного вместо закрытия старого.
3. Вторым — два ребра зонда покрытия, оставшиеся неразрешёнными, с названным основанием каждого.
4. Третьим — незакрытое восьмое измерение Найквиста: у фазы нет ни разведки, ни валидационного
   артефакта, потому что режим закрытия гейпов разведку не запускает и оператор ЯВНО выбрал
   продолжить без неё.
5. Незакрытое восьмое измерение записывается ещё и ОКНОМ: раздел артефакта читает тот, кто пришёл за
   обходом, а окно читает тот, кто ведёт журнал фазы.
6. Долг по направлению отказа планов 10-01…10-07 записывается окном — и ТОЛЬКО окном: правке эти
   планы не подлежат, они исполнены и закоммичены, а переписывать историю ради контракта, которого
   на момент их написания не существовало, запрещено.
7. Ни одна отметка не заполняется; состояние артефакта не двигается.
  </behavior>

  <action>
**1. Раздел «Чего третья партия закрытия гейпов НЕ закрыла».** Дописать перед разделом «Куда
артефакт уезжает дальше». Форма — таблица «пункт → основание», по образцу соседнего раздела. Три
строки:

— **Критерий 2 (вторая половина цели фазы) остаётся человеческим.** Основание: суита идёт через
транспорт без процесса на встроенной базе и не исполняет ни строчки разметочного рантайма; доказано
ПРИСУТСТВИЕ парных внеполосных узлов в теле ответа, применение их рантаймом — нет. Машинного пути
закрыть это у проекта НЕТ: браузерных зависимостей в дереве нет ни одной (перепроверено чтением
блоков зависимостей). Заводить браузерную зависимость партией закрытия гейпов ОТВЕРГНУТО с
основанием: это решение ВЕХИ, действующее на весь проект, а не побочная правка партии, и введённая
наспех она дала бы новый класс непроверенного вместо закрытия старого. Закрывается ПРОХОЖДЕНИЕМ
проверки 1.

— **Два ребра зонда покрытия остаются неразрешёнными, и это названо, а не проглочено.** Ребро
порядка: предмет — порядок применения трёх внеполосных узлов ответа рантаймом; он наблюдаем только
рантаймом, ни одно правило проекта на него не опирается, и формулировать критерий, которого никто
не проверяет, значило бы завести запись шире дерева. Ребро одновременности: частично адресовано
гейтом ухода панели по признаку отправки (план 10-07, доказан в интерпретаторе и мутацией) и
проверками 2 и 3 обхода; полного машинного критерия — два подтверждённых удаления в полёте
одновременно — в суите без процесса выразить нечем.

— **Восьмое измерение Найквиста у фазы не закрыто.** Основание: режим закрытия гейпов разведку не
запускает, `10-RESEARCH.md` и `10-VALIDATION.md` у фазы нет, и оператор ЯВНО выбрал продолжить без
них. Это записано, а не обойдено; закрытие принадлежит владельцу вехи.

⚠️ КАЖДЫЙ ПУНКТ НАЗЫВАЕТ ОСНОВАНИЕ. Пункт, просто перечисленный, неотличим от забытого — форма и
основание взяты у соседнего раздела «Что в этот обход НЕ входит» дословно.

**2. ЧЕТЫРЕ ОКНА — И ЭТА ЗАДАЧА ЕДИНСТВЕННАЯ В ПАРТИИ, КТО ПИШЕТ В РЕЕСТР.** Дописывать
ИНСТРУМЕНТОМ, а не руками: `gsd_run windows append --kind <тип> --phase 10 --description "<текст>"
[--file <путь>]` — по одному вызову на окно; номера присваивает реестр. `gsd_run` поднимается
загрузчиком `execute-plan.md` (он уже в `<execution_context>`) и ищет инструмент по многим
расположениям — хардкодить путь `.claude/gsd-core/bin/` ЗАПРЕЩЕНО: каталог `.claude/` в игноре git
(`.gitignore:50`) и в worktree отсутствует — это уже записано окном 23, и повторять ту же ошибку
командой плана нельзя.

Порядок дописывания: сначала два ЧУЖИХ текста, затем два своих, — чтобы потеря переданного текста
обнаружилась первой, а не последней.

⚠️ ЕСЛИ `gsd_run` НЕДОСТУПЕН — это ОСТАНОВ С ЗАПИСЬЮ, а не повод дописать строку руками: реестр
держит одно и то же в ТРЁХ местах (счётчики шапки, строка таблицы, запись блока), инструмент правит
все три согласованно, а ручная правка разведёт их молча — и именно на это смотрит вторая команда
`<verify>`.

— **Окно 1, чужое, план 10-12, тип `todo`, файл — страничный модуль расписаний.** Текст ПЕРЕНОСИТСЯ
ДОСЛОВНО из раздела `10-12-SUMMARY.md`. Предмет — граница величины идентификатора на страничных
модулях за пределами расписаний; кандидат — Фаза 15; состояние `open`. ⚠️ ПЕРЕСКАЗ ЗАПРЕЩЁН: текст
написан тем, кто снял замер.

— **Окно 2, чужое, план 10-13, тип `todo`, файл — шаблон экрана подключения.** Текст ПЕРЕНОСИТСЯ
ДОСЛОВНО из раздела `10-13-SUMMARY.md`. Предмет — экран подключения несёт объявления верхнего уровня
и станет уязвим, как только окажется целью заголовка перехода; кандидат — Фаза 11; состояние `open`.

— **Окно 3, своё, тип `unrun-verify`, фаза `10`, файл — каталог фазы.** Текст: у Фазы 10 нет ни
`10-RESEARCH.md`, ни `10-VALIDATION.md`; режим закрытия гейпов разведку не запускает, и оператор явно
выбрал продолжить без неё; восьмое измерение Найквиста фазой НЕ ЗАКРЫТО, и три партии закрытия гейпов
этого не меняют. Состояние — `open`, фаза закрытия НЕ НАЗНАЧАЕТСЯ: назначение принадлежит владельцу
вехи, и сочинять его планом запрещено.

— **Окно 4, своё, тип `deviation`, фаза `10`, файл — каталог фазы.** Текст: детерминированный зонд
направления отказа вернул СЕМНАДЦАТЬ команд `<automated>` без сформулированного сигнала отказа, и все
семнадцать лежат в планах `10-01…10-07` — написанных ДО появления контракта `<fails_when>`,
исполненных и закоммиченных. ПРАВКЕ ОНИ НЕ ПОДЛЕЖАТ без переписывания истории, и настоящая партия их
не правит НАМЕРЕННО: контракт, которого на момент их написания не существовало, не есть претензия к
ним. Ни одна команда `<automated>` пяти новых планов партии блокером не признана. ⚠️ ЧИСЛА В ТЕКСТ
ОКНА СТАВЯТСЯ ПРОГОНОМ ЗОНДА, А НЕ ПЕРЕНОСЯТСЯ ОТСЮДА: число блокеров (`17`), перечень планов
(`10-01…10-07`) и число команд новой партии снимаются СВОИМ прогоном
(`gsd_run check verify-failure-directions 10`) и сличаются с `10-VERIFICATION.md`; расхождение —
ОСТАНОВ с записью, а не повод переписать ожидание. Ориентир круга проверки: `17` блокеров при `29`
командах новой партии ДО правок круга — после правок число команд партии выросло, и потому оно
СНИМАЕТСЯ, а не цитируется. Состояние — `open`, фаза
закрытия НЕ НАЗНАЧАЕТСЯ. ⚠️ ОКНО ЗАВОДИТСЯ ИМЕННО ПОТОМУ, ЧТО ПРОЕКТ ВЕДЁТ УЧЁТ ПО ЭТОЙ ОСИ: реестр
несёт 44 записи, и незаписанный долг здесь был бы неотличим от долга незамеченного.

**3. Проверить утверждение о зависимостях СВОИМИ ГЛАЗАМИ.** Прочитать блоки зависимостей
`pyproject.toml` целиком и убедиться, что браузерных зависимостей нет ни одной. Утверждение уезжает
в артефакт, который прочтёт человек на приёмке; унаследованное из плана и не перепроверенное, оно
было бы ровно тем классом записи, которым фаза уже провалена.

**4. Что НЕ трогается.** Шапка артефакта, шесть проверок, шесть таблиц отметок, разделы посева и
уборки, раздел «Что в этот обход НЕ входит», `09-UAT.md`, `.planning/REQUIREMENTS.md`.
  </action>

  <verify>
    <automated>grep -c 'Чего третья партия закрытия гейпов НЕ закрыла' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод `0` — раздел не заведён, и то, чего партия не закрыла, нигде в артефакте не названо</fails_when>
    <automated>grep -c '^status: testing$' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод отличен от `1` — состояние артефакта сдвинуто, то есть обход объявлен пройденным без прохождения</fails_when>
    <automated>grep -cE 'playwright|selenium|puppeteer' pyproject.toml</automated>
    <fails_when>вывод НЕ равен `0` — утверждение раздела о том, что браузерных зависимостей в дереве нет, ложно, и раздел обязан быть переписан по замеру, а не по этому плану</fails_when>
    <automated>grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md</automated>
    <fails_when>вывод отличен от `48` — реестр вырос не РОВНО на четыре строки от замеренных планировщиком `44`. Меньше — окно потеряно при передаче (потерянное окно неотличимо от незаведённого); больше — в реестр писал кто-то ещё, а владелец в партии объявлен один</fails_when>
    <automated>grep -c '^total_count: 48$' .planning/WINDOWS.md</automated>
    <fails_when>вывод отличен от `1` — счётчик шапки реестра разошёлся с числом строк таблицы, то есть запись сделана мимо `windows append` и три места реестра (шапка, таблица, блок записей) больше не согласны между собой</fails_when>
    <human-check>Пройти шесть проверок обхода `10-UAT.md` на локальном стенде в браузере, заполнить шесть таблиц отметок наблюдёнными признаками (включая новые шаги 1.13-1.15 переходной ветки) и записать исход. ⚠️ ДО ЭТОГО критерий 2 роадмапа и требование `FORM-06` остаются НЕЗАКРЫТЫМИ, и ни один машинный прогон этого не меняет. Режим человеческой проверки проекта — `end-of-phase`: наблюдение снимается на приёмке фазы.</human-check>
  </verify>

  <acceptance_criteria>
- Раздел заведён, несёт ТРИ пункта, и у каждого своё основание; в сводке приведён текст всех трёх.
- Утверждение о браузерных зависимостях ПЕРЕПРОВЕРЕНО исполнителем: в сводке названо, какие блоки
  `pyproject.toml` прочитаны и каков вывод команды поиска.
- **ЧЕТЫРЕ ОКНА ДОПИСАНЫ, И ВЛАДЕЛЕЦ ОДИН.** В сводке приведены НОМЕР и ТЕКСТ каждого из четырёх
  окон, и у окон 3 и 4 видно, что фаза закрытия НЕ назначена. Приведены ОБА замера роста реестра:
  `grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md` → `48` (было `44`, замер планировщика) и
  `grep -c '^total_count: 48$' .planning/WINDOWS.md` → `1`. Два независимых замера нужны потому, что
  реестр держит одно и то же в трёх местах, и расхождение между ними — признак записи мимо
  инструмента.
- **ЧУЖИЕ ТЕКСТЫ ПЕРЕНЕСЕНЫ ДОСЛОВНО, А НЕ ПЕРЕСКАЗАНЫ.** В сводке текст окна 1 приведён рядом с
  исходным абзацем `10-12-SUMMARY.md`, текст окна 2 — рядом с абзацем `10-13-SUMMARY.md`, и прямо
  сказано, что они совпадают. Расхождение — останов: пересказ владельцем реестра теряет точность
  автора замера.
- **ОБЪЯВЛЕНИЕ СОВПАДАЕТ С ФАКТОМ ПО ВСЕЙ ПАРТИИ.** В сводке приведён вывод счёта планов партии,
  объявивших реестр:
  `grep -l '^  - .planning/WINDOWS.md$' .planning/phases/10-rychag-components-modal-html/10-1[2-6]-PLAN.md`
  → РОВНО ОДИН файл, и это `10-16-PLAN.md`. Это и есть машинная форма решения о владельце: реестр
  объявлен ровно тем планом, который в него пишет, и ни одним другим.
- Отметок не заполнено ни одной:
  `grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' .planning/phases/10-rychag-components-modal-html/10-UAT.md`
  → `0`.
- Шапка не тронута; `checks_declared` по-прежнему `6`.
- `.planning/REQUIREMENTS.md` не тронут: `git diff --name-only -- .planning/REQUIREMENTS.md` —
  пусто. ⚠️ Перевод `FORM-06` в завершённое ЗАПРЕЩЁН этим планом прямо: правило плана 10-14 краснит
  такую отметку, и это не обход правила, а его основание.
- `09-UAT.md` не тронут: `git diff --name-only -- .planning/phases/09-*/` — пусто.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится только запись; поведения продукта не меняется. Решение НЕ заводить браузерную зависимость
этой партией зафиксировано с основанием и пересматривается владельцем вехи, а не следующим планом.
  </reversibility>

  <done>
Все четыре окна партии дописаны ОДНИМ владельцем, два чужих текста перенесены дословно, и реестр
объявлен ровно одним планом партии; чего третья партия не закрыла — названо в самом артефакте с
основанием у каждого пункта; незакрытое восьмое измерение записано ещё и окном, а долг по
направлению отказа планов 10-01…10-07 — четвёртым окном со снятыми, а не процитированными числами;
утверждение о зависимостях перепроверено, а не унаследовано;
артефакт по-прежнему ничего не самозаверяет, и требование остаётся незавершённым до прохождения
обхода.
  </done>
</task>

</tasks>

<threat_model>
⚠️ УРОВЕНЬ И ПОРОГ НАЗВАНЫ, А НЕ ПОДРАЗУМЕВАЮТСЯ. Таблица собрана на **ASVS 1**
(`workflow.security_asvs_level` → `1`) с порогом блокировки **`high`**
(`workflow.security_block_on` → `high`) при включённом принуждении
(`workflow.security_enforcement` → `true`). Все три величины СНЯТЫ `config-get`, а не объявлены
планом, и почему они не стоят во frontmatter — записано решением в самом frontmatter. Строка
`*-SC` несёт `high` как severity КЛАССА «цепочка поставки», а `accept` — потому что ПРЕДМЕТА НЕТ
(ни одна задача плана не устанавливает пакетов): открытой угрозой она не является и в
`threats_open` при пороге `high` не считается.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| артефакт обхода → человек на приёмке | текст шага есть инструкция: неверно названный узел заставит человека записать отказ, которого нет |
| запись артефакта → запись требований | состояние обхода есть условие перевода требования; отметка без наблюдения переводит запись шире дерева |
| зелёное машинное правило → читатель | правило, внесённое в перечень закрытого без своей ГРАНИЦЫ, читается как закрытие пункта, которого оно не закрывает |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-16-01 | Repudiation | `10-UAT.md`, таблицы отметок | high | mitigate | Отметки не заполняются ни планом, ни исполнителем; `status: testing` сохраняется и утверждается командой проверки. Правило плана 10-14 краснит перевод требования раньше вердикта — запись не может стать шире дерева молча |
| T-10-16-02 | Repudiation | строка перечня закрытого машинно | high | mitigate | Новая строка несёт ГРАНИЦУ доказанного третьим столбцом (задача 1): доказана форма исходника и его поведение в интерпретаторе, поведение рантайма браузера — нет. Без границы зелёное правило читалось бы как закрытие критерия 2 |
| T-10-16-03 | Tampering | текст новых шагов обхода | medium | mitigate | Имена и положения узлов берутся ЧТЕНИЕМ шаблона и называются в сводке с файлом и строкой: шаг, называющий несуществующий узел, обесценивает обход при первом же прохождении |
| T-10-16-04 | Information Disclosure | посев стенда в живую базу разработчика | low | mitigate | Весь посев несёт префикс имени `UAT10-` на двух полях, и раздел уборки снимает комплект по префиксу — приём и его основание уже записаны в артефакте (опыт обхода Фазы 9) |
| T-10-16-05 | Repudiation | незакрытое восьмое измерение Найквиста | medium | mitigate | Записано ДВАЖДЫ и в двух адресатах: разделом артефакта (для того, кто пришёл за обходом) и окном (для того, кто ведёт журнал фазы). Фаза закрытия не назначается — назначение принадлежит владельцу вехи |
| T-10-16-06 | Tampering | `.planning/WINDOWS.md` как общий дописываемый реестр | medium | mitigate | Владелец реестра в партии НАЗНАЧЕН и объявлен: файл стои́т в `files_modified` ровно одного плана — настоящего, — и ни один другой план партии в него не пишет. Без назначения два плана ОДНОЙ ВОЛНЫ дописывали бы строку в один файл, а гейт волны этого не видел бы: пересечение `files_modified` было бы пусто ПО ОБЪЯВЛЕНИЮ, а не по факту. Проверяется счётом планов партии, объявивших файл (задача 2) |
| T-10-16-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ, И ЭТО РЕШЕНИЕ: браузерная зависимость НЕ заводится этой партией с записанным основанием, `pyproject.toml` не правится, и отсутствие браузерных зависимостей ПЕРЕПРОВЕРЯЕТСЯ исполнителем командой поиска. Таблица легитимности пакетов не требуется |
</threat_model>

<verification>
1. `grep -c '^| 1\.1[3-5] ' .planning/phases/10-rychag-components-modal-html/10-UAT.md` → `3`.
2. `grep -c '^status: testing$' .planning/phases/10-rychag-components-modal-html/10-UAT.md` → `1`;
   `grep -c '^checks_declared: 6$' …` → `1`.
3. `grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' …10-UAT.md` → `0`: отметок не заполнено.
4. `git diff --name-only -- .planning/REQUIREMENTS.md .planning/phases/09-*/` — пусто.
5. `uv run pytest tests/ -q` — прогон целиком зелен (план правит только запись, и это утверждается
   прогоном, а не предполагается).
6. **ЧЕЛОВЕЧЕСКАЯ ПОЛОВИНА, СНИМАЕМАЯ НА ПРИЁМКЕ ФАЗЫ (`human_verify_mode: end-of-phase`):** шесть
   проверок обхода пройдены на локальном стенде в браузере, шесть таблиц отметок заполнены
   наблюдёнными признаками, включая новые шаги 1.13-1.15. ⚠️ ДО ЭТОГО критерий 2 роадмапа и
   требование `FORM-06` остаются НЕЗАКРЫТЫМИ; ни один машинный прогон этого не меняет, и настоящий
   план обратного не утверждает.
</verification>

<success_criteria>
- Переходная ветка обхода спрашивает ровно о том, что чинилось, тремя наблюдаемыми признаками.
- Изменение ожидания записано летописью: прежнее помечено опровергнутым, источник назван поимённо,
  и прямо сказано, что прежнее ожидание не было ошибкой.
- Машинная половина `CR-03` внесена в перечень закрытого машинно ВМЕСТЕ со своей границей.
- Чего третья партия не закрыла — названо в самом артефакте с основанием у каждого пункта; два
  неразрешённых ребра зонда покрытия названы, а не проглочены.
- Незакрытое восьмое измерение Найквиста записано и разделом, и окном, без назначения фазы.
- **РЕЕСТР ОКОН ИМЕЕТ РОВНО ОДНОГО ВЛАДЕЛЬЦА В ПАРТИИ, И ЭТО ЗАМЕРЕНО, А НЕ ОБЪЯВЛЕНО:**
  `.planning/WINDOWS.md` стои́т в `files_modified` ровно одного из пяти планов партии — настоящего;
  четыре окна дописаны им одним; тексты двух перенесены дословно из сводок планов 10-12 и 10-13, и
  совпадение показано. Долг по направлению отказа планов 10-01…10-07 записан четвёртым окном, а сами
  планы не тронуты.
- Артефакт по-прежнему ничего не самозаверяет: состояние не сдвинуто, отметки пусты, требование
  остаётся незавершённым до прохождения обхода человеком.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-16-SUMMARY.md` по завершении.
</output>
