---
phase: 10-rychag-components-modal-html
plan: 19
type: execute
wave: 2
# ⚠️ ЗАВИСИМОСТЬ ОТ 10-18 — НЕ ПО ФАЙЛУ, А ПО ОБЩЕМУ РЕСУРСУ, И ИМЕННО ПОЭТОМУ ОНА ОБЪЯВЛЕНА
# РУКАМИ. Пересечения `files_modified` у планов НЕТ: 10-18 пишет `app/pages/*.py`, настоящий план
# пишет только модули суиты. Гвардия волны, сверяющая `files_modified`, этой связанности НЕ УВИДИТ
# — а она есть: задача 1 настоящего плана ЧИТАЕТ `app/pages/` рекурсивно и делает нераспознанную
# форму выражения `redirect=` КРАСЯЩЕЙ прогон вместо тихого выпадения, тогда как 10-18 правит
# сборку адреса приземления в `app/pages/htmx.py` (задача 3) и вешает гард в четырёх модулях
# `app/pages/` (задача 2). В одной волне отказ был бы ПЕРЕМЕЖАЮЩИМСЯ и НЕАТРИБУТИРУЕМЫМ: гейт
# покраснел бы на форме, которую другой план в ту же волну ещё дописывает.
# Волна: max(1) + 1 = 2.
depends_on: ["10-18"]
gap_closure: true
files_modified:
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_templates/test_components.py
  - tests/test_pages/test_confirm_delete_transport.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ — форма унаследована
# от планов 10-12…10-18. ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `workflow.security_asvs_level` → `1`;
# `workflow.security_block_on` → `"high"`; `workflow.security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ГЕЙТ ЦЕЛЕЙ ПЕРЕХОДА ЧИТАЕТ ТО, ЧТО ИСПОЛНИТ РАНТАЙМ: разбор идёт по ЦЕПИ шаблона — сам файл, его база и все включаемые, — а не по одному файлу цели, поэтому объявление верхнего уровня в шелле краснит правило, а не проезжает мимо него"
    - "СПОСОБНОСТЬ ГЕЙТА УВИДЕТЬ ШЕЛЛ ПОКАЗАНА, А НЕ ЗАЯВЛЕНА: контроль сажает объявление верхнего уровня в БАЗОВЫЙ шаблон синтетической копии и показывает, что правило краснеет — на прежнем разборе оно оставалось бы зелёным при мёртвом клиентском слое ВСЕХ двенадцати целей"
    - "ОБХОД СТРАНИЧНОГО СЛОЯ РЕКУРСИВЕН: модуль, положенный в подкаталог `app/pages/`, попадает в счёт целей перехода, а не выпадает из него молча"
    - "НЕРАСПОЗНАННЫЙ АДРЕС ПРИЗЕМЛЕНИЯ КРАСНИТ ПРОГОН, А НЕ ВЫПАДАЕТ ИЗ СОПОСТАВЛЕНИЯ: вызов, чью форму адреса разборщик не разобрал, называется поимённо, потому что молча выпавший вызов неотличим от вызова, у которого цели нет"
    - "ГРАНИЦА ПОВЕДЕНЧЕСКОГО ГАРНИРА ПАНЕЛИ НАЗВАНА В НЁМ САМОМ: гарнир исполняет отложенный вызов немедленно и зовёт ветви ухода панели руками, поэтому утверждений О ПОРЯДКЕ ЗАДАЧ РАНТАЙМА он не проверяет — и зелень его правил не читается шире этого"
    - "ОПРОВЕРГНУТАЯ ФОРМУЛИРОВКА В СУИТЕ ПОМЕЧЕНА ТАК ЖЕ, КАК В ШАПКЕ РЫЧАГА: читатель, зашедший в суиту первой, видит пометку и действующего преемника, а не уносит опровергнутую версию"
    - "МЁРТВЫЙ КОД СНЯТ ИЗ ФАЙЛОВ, ЧЬЯ ДИСЦИПЛИНА СТРОИТСЯ НА ТОЧНОСТИ ОХВАТА: неиспользуемый импорт и неиспользуемое имя результата запроса не оставляют следующему читателю ложного признака того, что часть правил была удалена"
  artifacts:
    - "tests/test_pages/test_hx_location_destinations.py — разбор ЦЕПИ шаблона (файл, база, включаемые) вместо разбора одного файла, с объявленным числом звеньев"
    - "tests/test_pages/test_hx_location_destinations.py — контроль, сажающий объявление верхнего уровня в БАЗОВЫЙ шаблон синтетической копии"
    - "tests/test_pages/test_hx_location_destinations.py — рекурсивный обход страничного слоя и громкий отказ на нераспознанной форме адреса"
    - "tests/test_templates/test_components.py — названная ГРАНИЦА поведенческого гарнира в докстринге его запускающей функции и помеченная опровергнутой формулировка сценария сноса"
    - "tests/test_pages/test_confirm_delete_transport.py — снятое неиспользуемое имя результата первого запроса с однострочным основанием"
  key_links:
    - "цепь шаблона → рантайм перехода: единственное, чем гейт объявлений верхнего уровня совпадает с тем, что исполняет браузер; ломается — `const` в шелле убивает клиентский слой всех двенадцати целей при зелёном гейте, то есть возвращается дословно `CR-03`"
    - "названная граница гарнира → читатель зелёного правила: единственное, чем зелень не читается шире предмета; ломается — утверждения о порядке задач рантайма считаются проверенными, не будучи ими"
    - "пометка опровергнутой формулировки → действующий преемник: единственное, чем читатель суиты не унесёт отменённую версию; ломается — одно место несёт три поколения формулировок без признака действующего"
  prohibitions:
    - statement: "машинная подмена разметочного рантайма НЕ заводится ни в каком виде и настоящим планом в том числе: разбор ЦЕПИ шаблона утверждает ФОРМУ ИСХОДНИКА того, что рантайм исполнит вместе, и НЕ утверждает, что рантайм это исполнил, — правило, объявившее обратное, вернуло бы фазе тот дефект, которым она провалена первым кругом"
      status: flagged-unverified
      verification: none
    - statement: "браузерная зависимость НЕ заводится ради того, чтобы разобрать цепь шаблона настоящим рантаймом: её нет в дереве, её появление есть решение ВЕХИ, и введённая наспех она дала бы новый класс непроверенного вместо закрытия старого"
      status: flagged-unverified
      verification: none
    - statement: "опровергнутые формулировки НЕ ВЫЧЁРКИВАЮТСЯ: идиома D-30/D-32 `.planning/STATE.md` требует пометки с названным преемником, потому что стёртая формулировка лишает следующего читателя знания о том, что прочтение вообще понадобилось"
      status: flagged-unverified
      verification: none
    - statement: "поведенческий гарнир панели НЕ переписывается на асинхронный рантайм ради утверждений о порядке: гарнир существует ради свойств, наблюдаемых без рантайма, и переписанный он перестал бы отвечать за то, за что отвечает сегодня; названа ГРАНИЦА, а не снята"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ НАД FORM-06 — ЯВНЫЕ ДОПУЩЕНИЯ ПАРТИИ (полная запись — 10-17-PLAN.md):
  #   • `idempotency` — продуктовая половина закрыта планом 10-06; повтор в РАНТАЙМЕ браузера
  #     наблюдаем только обходом `10-UAT.md`, шаги 1.4-1.9, не пройденные ни разу. `unresolved`.
  #   • `concurrency` — адресован частично планом 10-07 и проверками 2 и 3 обхода; полного
  #     машинного критерия в суите без процесса выразить нечем. `unresolved`.
  # 2 предъявленных = 0 разрешённых критерием + 2 явных допущения.
---

<objective>
Закрываются ЧЕТЫРЕ находки ревизии кода, чей предмет — СУИТА: `WR-05` (гейт целей перехода читает 12
именованных файлов, а рантайм исполняет весь `<body>`), `WR-06` (поведенческий гарнир панели не
проверяет утверждений о порядке, а зелень его правил читается так, будто проверяет; плюс дословно
опровергнутая формулировка в комментарии сценария), `IN-01` и `IN-02` (мёртвый импорт и мёртвое имя
в файлах, чья дисциплина строится на точности охвата).

Purpose: главная из четырёх — `WR-05`, и её цена названа числом самим ревизором. Заголовок перехода
подменяет содержимое `<body>` ЦЕЛИКОМ и исполняет ВСЕ узлы сценария подменённого тела — включая
инлайн-скрипты базового шаблона и включаемых партиалов. Гейт заявлен как «ни один экран-цель не
несёт объявлений верхнего уровня», а разбирает ИСХОДНИК одного файла из примерно пяти, участвующих в
сборке каждой цели. Объявление верхнего уровня в `base.html` убило бы клиентский слой ВСЕХ
двенадцати целей, и гейт остался бы зелёным — то есть регрессия `CR-03`, на которую фаза потратила
круг, вернулась бы по единственному непокрытому пути.

⚠️ **СЕГОДНЯ АКТИВНОГО ДЕФЕКТА НЕТ, И ЭТО ИЗМЕРЕНО, А НЕ ПРЕДПОЛОЖЕНО:** ревизия прогнала разборщик
по всем шаблонам с инлайн-скриптами и получила пустой список у `base.html`, `auth_base.html` и обоих
`includes/*`. Предмет — СЛЕПАЯ ЗОНА ГЕЙТА, а не сегодняшний отказ, и правило заводится ровно потому,
что закрывать слепую зону после отказа дороже, чем до.

Output: разбор цепи шаблона вместо разбора файла, с контролем, сажающим объявление в БАЗУ;
рекурсивный обход страничного слоя и громкий отказ на нераспознанной форме адреса; названная граница
поведенческого гарнира и помеченная опровергнутой формулировка; снятые мёртвый импорт и мёртвое имя.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-13-SUMMARY.md
@tests/test_pages/test_hx_location_destinations.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: сквозной срез — гейт объявлений верхнего уровня читает ЦЕПЬ шаблона, а не один файл, и видит шелл</name>

  <files>
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `tests/test_pages/test_hx_location_destinations.py` ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ВЕСЬ: шапка модуля
  объявляет ГРАНИЦУ доказанного, и правка, разошедшаяся с ней, воспроизвела бы дефект фазы в её же
  гейте. Особенно:
  `:42-43` — `TEMPLATES_DIR`, `PAGES_DIR`;
  `:139-176` — `_run_twice`, `_inline_script_bodies`, `_editor_script_of`: приём исполнения
  исходника настоящим интерпретатором, заведённый планом 10-13;
  `:307` — `HX_LOCATION_DESTINATION_CALLS_DECLARED` и его летопись;
  `:381-434` — `_strip_js_comments`, `:436-468` — `_blank_string_literals`,
  `:470-505` — `_top_level_bindings`, `:506-521` — `_top_level_bindings_of_template`;
  `:515-536` — `HX_LOCATION_DESTINATION_TEMPLATES`: ДВЕНАДЦАТЬ пар «адрес → шаблон»;
  `:523-531` — `_page_modules()`: ⚠️ `PAGES_DIR.glob("*.py")`, НЕРЕКУРСИВНО — предмет смежной
  находки;
  `:532-559` — `_address_forms(node, scope)`: ⚠️ возвращает ПУСТОЕ множество на нераспознанном
  выражении `redirect=` — второй предмет смежной находки;
  `:569-609` — `_transition_destination_calls()`;
  `:627-650` — `test_every_transition_destination_has_a_declared_template`;
  `:651-670` — `test_no_transition_destination_declares_a_top_level_binding_inline`, ГЛАВНЫЙ
  ПРЕДМЕТ ЗАДАЧИ;
  `:671-728` — `test_a_reinstated_top_level_binding_reddens_the_static_rule` и
  `test_the_static_rule_does_not_cancel_itself`: ФОРМА ЗУБОВ, которую наследует новый контроль;
  `:729-748` — `test_every_exempt_template_carries_a_non_empty_rationale` и перечень изъятий.
- `app/templates/base.html` и `app/templates/auth_base.html` — базы, которые надо начать видеть:
  их блоки `{% block %}` и их инлайн-скрипты, если есть.
- `app/templates/includes/htmx_config.html`, `app/templates/includes/htmx_error_banner.html` —
  включаемые шеллом партиалы, названные ревизией поимённо.
- Одна из двенадцати целей ЦЕЛИКОМ (например `app/templates/dashboard.html`) — чтобы увидеть, чем
  именно `{% extends %}` и `{% include %}` записаны в этом проекте: форма выражения снимается
  ЧТЕНИЕМ, а не из памяти о синтаксисе Jinja вообще.
- `10-REVIEW.md:322-369` — `WR-05` целиком, включая предложенный образец `_template_chain` и обе
  смежные щели.
- `10-13-SUMMARY.md` — что именно план 10-13 объявил закрытым и какой границей: настоящая задача
  РАСШИРЯЕТ охват его гейта, а не отменяет его.
  </read_first>

  <precondition>
Прогон `uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly` на
непрáвленом дереве зелен, и в нём ровно столько правил, сколько оставил план 10-13. Число привести в
сводку ДО правки: без него нельзя будет отличить «правило прибавилось» от «правило переименовалось».
⚠️ И ВТОРОЕ: план 10-18 ПРИЗЕМЛЁН — сборка адреса приземления в `app/pages/htmx.py` уже несёт
параметр кода исхода перед якорем, и гард происхождения уже стои́т в четырёх модулях `app/pages/`.
Настоящая задача читает `app/pages/` рекурсивно и делает нераспознанную форму выражения `redirect=`
КРАСЯЩЕЙ; исполненная ДО 10-18, она покраснела бы на форме, которую другой план в ту же волну ещё
дописывает, и отказ был бы неатрибутируемым. Если правки 10-18 в дереве нет — ОСТАНОВИТЬСЯ.
  </precondition>

  <behavior>
1. Цепь шаблона — сам файл, его база (транзитивно, если баз несколько уровней) и все включаемые им
   файлы — собирается разборщиком по исходнику, и порядок звеньев значения не имеет: правило судит
   МНОЖЕСТВО.
2. Несущее правило обходит цепь КАЖДОЙ из двенадцати целей и находит ноль объявлений верхнего
   уровня. Изъятия перечня продолжают действовать пофайлово, а не поцепочно: изъят конкретный файл,
   а не всё, что его включает.
3. Отказ называет `файл:строка: ключевое слово имя (цель адрес)` — то есть звено цепи, а не только
   цель: читатель обязан узнать, В КАКОМ файле сидит объявление.
4. Цепь не вырождается: правило утверждает, что у каждой цели звеньев БОЛЬШЕ ОДНОГО, — иначе
   разборщик, не разобравший ни одного `extends`, зеленел бы, вернувшись к прежнему охвату молча.
5. Обход страничного слоя рекурсивен: модуль в подкаталоге `app/pages/` попадает в счёт вызовов
   перехода.
6. Вызов, чью форму адреса разборщик не распознал, КРАСНИТ прогон с названием модуля и строки.
  </behavior>

  <action>
**(1) Завести сборку цепи шаблона.** Функция принимает ИМЯ шаблона и возвращает множество имён
шаблонов, которые рантайм исполнит вместе с ним: сам файл, его базу (разворачивать транзитивно, пока
база есть) и все включаемые. Формы выражений `extends` и `include` снять ЧТЕНИЕМ живых шаблонов, а
не из памяти о Jinja вообще: в этом проекте включения встречаются и внутри блоков, и с аргументами.
⚠️ ЦИКЛЫ ОБЯЗАНЫ БЫТЬ НЕВЫРАЗИМЫ: разворот вести множеством уже посещённых, иначе взаимное включение
даст бесконечный обход, и правило отключат за то, что оно висит. Звено, которого в дереве нет,
краснит сборку ГРОМКО и с именем — молча пропущенное звено вернуло бы ровно ту слепую зону, ради
которой задача написана.

**(2) Переписать `test_no_transition_destination_declares_a_top_level_binding_inline`.** Вместо
чтения одного файла цели — обход её ЦЕПИ; `_top_level_bindings_of_template` прогоняется по каждому
звену. Изъятия (`TOP_LEVEL_BINDING_EXEMPT_TEMPLATES`) продолжают проверяться ПО ИМЕНИ ЗВЕНА: изъят
конкретный файл. Текст отказа расширить именем звена. Прибавить утверждение непустоты цепи: у каждой
цели звеньев больше одного.

**(3) Завести контроль `test_control_a_top_level_binding_in_the_shell_reddens_the_chain_rule`** —
ГЛАВНОЕ ДОКАЗАТЕЛЬСТВО ЗАДАЧИ. Подать разборщику СИНТЕТИЧЕСКУЮ цепь, в которой объявление верхнего
уровня сидит НЕ в файле цели, а в её БАЗЕ, и показать, что правило находит его и называет базу
поимённо. ⚠️ ВТОРОЙ ПОЛОВИНОЙ КОНТРОЛЯ — та же цепь БЕЗ объявления: находок ноль. Без второй
половины контроль не отличал бы работу от красноты всегда. Копии живут в памяти; файлы дерева не
правятся — приём взят у `test_a_reinstated_top_level_binding_reddens_the_static_rule`.

**(4) Закрыть две смежные щели, названные ревизией.** `_page_modules()` перевести на РЕКУРСИВНЫЙ
обход (`rglob`) с прежними исключениями (`__`-файлы и модуль слоя ответа); в сводке привести число
модулей до и после — если оно не изменилось, это НЕ повод не править: предмет в том, что модуль в
подкаталоге выпадет молча, а не в том, что он уже выпал. В разборе вызовов перехода — сделать
нераспознанную форму адреса ГРОМКОЙ: вызов, для которого `_address_forms` вернул пустое множество,
краснит прогон с именем модуля и номером строки, вместо того чтобы выпасть из сопоставления
`test_every_transition_destination_has_a_declared_template`.

**(5) Привести числа, которые сдвинутся.** `HX_LOCATION_DESTINATION_CALLS_DECLARED` — если
рекурсивный обход прибавил вызовы, число правится ВМЕСТЕ С ЛЕТОПИСЬЮ по форме, уже стоящей у этой
константы: прежнее значение НЕ БЫЛО ОШИБКОЙ — оно устарело, источник расхождения назван поимённо
(рекурсивный обход страничного слоя, план 10-19). Если не прибавил — число не трогать и сказать это
в сводке прямо.

**(6) Что НЕ трогается.** Правила исполнения исходника настоящим интерпретатором (`_run_twice`,
`test_the_first_execution_of_the_editor_script_succeeds`,
`test_the_editor_script_survives_a_second_execution_in_the_same_realm`,
`test_a_reinstated_top_level_binding_reddens_the_double_execution_rule`) — предмет плана 10-13,
доказан и зелен. Перечень `HX_LOCATION_DESTINATION_TEMPLATES` — двенадцать пар остаются двенадцатью,
цепь прибавляется К НИМ, а не вместо них. Перечень изъятий и правило обоснований.
`test_the_connect_screen_is_not_a_transition_destination_today`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` не выросло относительно числа, снятого предусловием, — новый контроль цепи не заведён</fails_when>
    <automated>uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly -k "top_level_binding_in_the_shell"</automated>
    <fails_when>вывод содержит `no tests ran` либо `failed` — контроль, сажающий объявление в базовый шаблон, не заведён под названным именем либо не зелен</fails_when>
    <automated>grep -c 'PAGES_DIR.glob(' tests/test_pages/test_hx_location_destinations.py</automated>
    <fails_when>вывод отличен от `0` — обход страничного слоя остался нерекурсивным, и модуль в подкаталоге по-прежнему выпадает молча</fails_when>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — расширение охвата гейта нашло в дереве объявление верхнего уровня, и это НАХОДКА: её надо назвать в сводке поимённо и закрыть, а не обойти сужением охвата</fails_when>
  </verify>

  <acceptance_criteria>
- Сборка цепи заведена, и её выход показан: в сводке приведено множество звеньев для ОДНОЙ названной
  цели, и в нём видны сама цель, её база и включаемые партиалы шелла.
- Циклы невыразимы: в сводке приведён вывод сборки на синтетической паре взаимно включающих
  шаблонов — обход завершается и не висит.
- Контроль базы КРАСНЕЕТ на посаженном объявлении и зеленеет без него: в сводке приведены оба
  вывода, и в тексте отказа видно ИМЯ БАЗОВОГО файла, а не только адрес цели.
- Непустота цепи утверждена: в сводке приведено число звеньев у каждой из двенадцати целей, и ни
  одно не равно единице.
- Обход страничного слоя рекурсивен: в сводке приведены числа модулей до и после правки и вывод
  третьей команды раздела `<verify>` (`0`).
- Нераспознанный адрес краснит: в сводке приведён вывод пробного прогона с намеренно
  нераспознаваемым выражением `redirect=` в синтетическом исходнике (ожидается отказ с именем модуля
  и строкой).
- Судьба `HX_LOCATION_DESTINATION_CALLS_DECLARED` названа прямо: сдвинуто с летописью либо не
  сдвинуто, и сказано, почему.
- Правила плана 10-13 не тронуты: в сводке приведён перечень имён функций, попавших в дифф, и ни
  одного из четырёх имён исполнения исходника в нём нет.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится ОДИН тестовый модуль, расширяя охват существующего правила; поведения продукта задача не
меняет ни на строку. Откат сужает охват обратно и наблюдаем прогоном нового контроля, который на
прежнем разборе краснеет по построению.
  </reversibility>

  <done>
Гейт объявлений верхнего уровня разбирает цепь шаблона — файл, базу и включаемые, — и это ПОКАЗАНО
контролем, сажающим объявление в базу; цепь не вырождается; обход страничного слоя рекурсивен;
нераспознанная форма адреса краснит прогон с именем модуля и строкой; числа, которые сдвинулись,
сдвинуты вместе с летописью.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: ГРАНИЦА поведенческого гарнира панели названа в нём самом, а опровергнутая формулировка сценария помечена</name>

  <files>
tests/test_templates/test_components.py
  </files>

  <read_first>
- `tests/test_templates/test_components.py:2000-2020` — сборка объекта клиентского состояния
  панели, и в ней строка `panel.$nextTick = function (fn) { fn(); };`. ⚠️ ЭТО И ЕСТЬ ГРАНИЦА:
  отложенный вызов исполняется НЕМЕДЛЕННО, то есть отложенности в гарнире нет вовсе.
- `:2205-2240` — `scenarioFocusAfterTeardown` ЦЕЛИКОМ, вместе с комментарием `:2219-2222`. ⚠️
  ЧИТАТЬ ДОСЛОВНО: комментарий говорит, что на фрагментном пути событие после запроса приходит
  ПОСЛЕ свопа, — ровно та формулировка, которую шапка `app/templates/components/modal.html`
  помечает ОПРОВЕРГНУТОЙ РАНТАЙМОМ.
- `app/templates/components/modal.html:100-130` — абзац с пометкой «АТРИБУЦИЯ ЭТИХ ДВУХ ВЕТВЕЙ …
  ОПРОВЕРГНУТА РАНТАЙМОМ» и ссылками на источники опровержения. ⚠️ ЭТО ДЕЙСТВУЮЩИЙ ПРЕЕМНИК, и
  пометка в суите обязана указывать НА НЕГО, а не пересказывать его своими словами: пересказ через
  круг разойдётся с оригиналом, и получится третье поколение формулировки.
- `app/templates/components/modal.html:110-125` — три утверждения о ПОРЯДКЕ, на которых держится
  исправление гейпа 1: внеполосное снятие происходит синхронно внутри свопа; событие завершения
  запроса летит в той же задаче; рантайм клиентского состояния сносит компонент микрозадачей позже.
  Ни одно гарниром не воспроизводится — это и есть содержание границы.
- `:2659-2700` — `_run_modal_lifecycle`: запускающая функция гарнира и её докстринг с УЖЕ
  названными границами. Новая граница встаёт РЯДОМ с ними, той же формой.
- `:2400-2420` — отображение имён сценариев (`focus_after_teardown` и соседи): имя сценария —
  часть контракта модуля, и переименование обязано пройти через все его упоминания.
- `10-REVIEW.md:370-410` — `WR-06` целиком.
- `:18` — `import pytest`, предмет `IN-01`. ⚠️ СНИМАЕТСЯ ЗАДАЧЕЙ 3, а не здесь: задача 2 правит
  ЗАПИСЬ, задача 3 — мёртвый код, и смешивать их в одном диффе значит потерять оба предмета.
  </read_first>

  <precondition>
Прогон `uv run pytest tests/test_templates/test_components.py -q -p no:randomly` на непрáвленом
дереве зелен. Задача правит ЗАПИСЬ и ИМЯ сценария, а не поведение гарнира, поэтому число правил до
и после обязано совпасть — снять его сейчас и привести в сводку.
  </precondition>

  <behavior>
1. Докстринг запускающей функции гарнира называет НОВУЮ границу рядом с уже названными: гарнир
   исполняет отложенный вызов немедленно и зовёт ветви ухода панели руками, поэтому утверждений о
   ПОРЯДКЕ задач рантайма он не воспроизводит и не проверяет.
2. Граница названа тремя утверждениями шапки рычага поимённо, а не общей фразой: читатель обязан
   узнать, КАКИЕ именно утверждения остаются вне охвата.
3. Комментарий сценария сноса несёт пометку «ОПРОВЕРГНУТО» с указанием ДЕЙСТВУЮЩЕГО преемника —
   шапки `components/modal.html`, — и прежняя формулировка остаётся рядом, а не стирается.
4. Сценарий назван тем, чем является: защитной ветвью сноса ОТКРЫТОЙ панели, а не «фрагментным
   путём».
5. Ни одно правило модуля не меняет своего исхода: правится запись и имя, а не поведение.
  </behavior>

  <action>
**(1) Дописать границу в докстринг `_run_modal_lifecycle`.** Новый абзац встаёт рядом с уже
названными границами и той же формой. Содержание: гарнир подменяет отложенный вызов немедленным и
зовёт `hide()` и `destroy()` РУКАМИ, в порядке, заданном самим сценарием, поэтому ТРИ утверждения о
порядке из шапки `app/templates/components/modal.html` — синхронность внеполосного снятия внутри
свопа, отправка события завершения запроса в той же задаче, снос компонента рантаймом клиентского
состояния микрозадачей позже — гарниром НЕ ВОСПРОИЗВОДЯТСЯ И НЕ ПРОВЕРЯЮТСЯ. Назвать все три
поимённо, а не общей фразой. Отдельной строкой — следствие для читателя: зелень правил гарнира
утверждает СВОЙСТВА ВЕТВЕЙ, а не их порядок; правило, утверждающее имя отработавшей ветви, стои́т на
порядке, заданном сценарием.

**(2) Пометить опровергнутую формулировку в `scenarioFocusAfterTeardown`.** К существующему
комментарию (`:2219-2222`) добавить пометку по идиоме D-30/D-32 `.planning/STATE.md`: формулировка
ОПРОВЕРГНУТА рантаймом, действующая версия — шапка `app/templates/components/modal.html`, источник
опровержения назван (ревизия и верификация Фазы 10, исправление планом 10-05). ⚠️ ПРЕЖНИЙ ТЕКСТ НЕ
СТИРАТЬ: он показывает, что прочтение вообще понадобилось, и стёртый лишает следующего читателя
именно этого знания.

**(3) Назвать сценарий тем, чем он является.** Переименовать `scenarioFocusAfterTeardown` в имя,
говорящее о ЗАЩИТНОЙ ВЕТВИ СНОСА ОТКРЫТОЙ панели, и провести переименование через ВСЕ упоминания —
включая ключ в отображении сценариев (`:2408`) и всех потребителей ключа в правилах модуля. ⚠️
ЕСЛИ ПЕРЕИМЕНОВАНИЕ ЗАДЕВАЕТ БОЛЬШЕ ТРЁХ МЕСТ — НЕ ПЕРЕИМЕНОВЫВАТЬ, а вписать новое имя предмета
ОДНОЙ СТРОКОЙ в комментарий сценария и сказать в сводке, сколько мест оно задело бы: правка записи
не имеет права превратиться в правку контракта модуля, а решение принимается по ЗАМЕРУ, не по вкусу.

**(4) Что НЕ трогается.** Тела сценариев и сама подмена отложенного вызова: гарнир существует ради
свойств, наблюдаемых без рантайма, и переписанный он перестал бы отвечать за то, за что отвечает
(объявлено прохибицией плана). Правила гейта ухода панели (`DEF-09-02`) и правила блокировки
прокрутки. Строка `import pytest` — предмет задачи 3.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` отличается от числа, снятого предусловием, — задача правит запись, а не поведение, и сдвиг числа означает, что тронуто правило</fails_when>
    <automated>grep -c 'ОПРОВЕРГНУТО' tests/test_templates/test_components.py</automated>
    <fails_when>вывод `0` — пометка опровергнутой формулировки не поставлена, и читатель суиты по-прежнему унесёт отменённую версию</fails_when>
    <automated>grep -c 'modal.html' tests/test_templates/test_components.py</automated>
    <fails_when>вывод `0` — пометка не называет действующего преемника, то есть читателю негде взять верную формулировку</fails_when>
  </verify>

  <acceptance_criteria>
- Новая граница вписана и называет ТРИ утверждения поимённо: в сводке приведён текст абзаца.
- Пометка опровергнутости поставлена и содержит ЧЕТЫРЕ части: прежняя формулировка на месте, слово
  пометки, имя действующего преемника, источник опровержения. В сводке приведён текст пометки.
- Прежний текст комментария НЕ УДАЛЁН: в сводке приведён вывод
  `git diff -- tests/test_templates/test_components.py | grep -c '^-'`, и каждая удалённая строка
  названа поимённо с основанием.
- Решение о переименовании принято ПО ЗАМЕРУ: в сводке приведено число мест, которые задевает
  переименование, и следствие — переименовано либо имя предмета вписано строкой.
- Поведение модуля не изменилось: числа правил до и после совпадают, оба приведены в сводке.
- Шапка `app/templates/components/modal.html` не тронута:
  `git diff --name-only -- app/templates/components/modal.html` — пусто.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится запись внутри одного тестового модуля и, возможно, одно внутреннее имя сценария; поведения
ни продукта, ни суиты задача не меняет. Откат равен обратной правке текста.
  </reversibility>

  <done>
Граница поведенческого гарнира названа в его собственном докстринге тремя утверждениями поимённо;
опровергнутая формулировка сценария помечена с указанием действующего преемника и источника
опровержения, а не стёрта; решение о переименовании сценария принято по замеру числа мест; исход ни
одного правила не изменился.
  </done>
</task>

<task type="execute">
  <name>Задача 3: мёртвый код снят из двух файлов, чья дисциплина строится на точности охвата</name>

  <files>
tests/test_templates/test_components.py
tests/test_pages/test_confirm_delete_transport.py
  </files>

  <read_first>
- `tests/test_templates/test_components.py:12-20` — блок импортов ЦЕЛИКОМ. ⚠️ ПЕРЕД СНЯТИЕМ
  `import pytest` ПРОВЕРИТЬ ПОИСКОМ по всему модулю, что имени `pytest` в нём нет ни в одном другом
  месте: маркеры, фикстуры и параметризация зовут его по имени, и снятый вслепую импорт роняет сбор
  тестов, а не одно правило.
- `tests/test_pages/test_confirm_delete_transport.py:1925-1960` — правило, внутри которого стои́т
  `first = await client.post(...)`, ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ВЕСЬ БЛОК: имя `first` может читаться ниже
  по телу, и ревизия снимала его статическим разбором, а не чтением. Проверить поиском в пределах
  функции.
- `10-REVIEW.md:486-504` — `IN-01` и `IN-02` целиком, вместе с предложенной формой правки для
  `IN-02` (запрос без присваивания плюс однострочный комментарий «запрос ради состояния, ответ не
  предмет»).
  </read_first>

  <behavior>
1. Модуль правил компонентов не импортирует имени, которого не использует.
2. Первый запрос правила транспорта исполняется по-прежнему — он нужен ради СОСТОЯНИЯ «строка уже
   удалена», — но его результат не связывается именем, а рядом стои́т однострочное основание.
3. Ни одно правило обоих модулей не меняет своего исхода.
  </behavior>

  <action>
**(1) `IN-01`.** Убедиться поиском, что имя `pytest` не встречается в
`tests/test_templates/test_components.py` нигде, кроме строки импорта; привести число вхождений в
сводку. Ноль прочих вхождений — снять строку импорта. Ненулевое — НЕ СНИМАТЬ и назвать место
поимённо: ревизия снимала находку статическим разбором, а разбор не видит имён, собранных строкой.

**(2) `IN-02`.** Снять связывание имени с результатом первого запроса: запрос исполняется, ответ не
именуется. Рядом поставить ОДНОСТРОЧНЫЙ комментарий, называющий, зачем запрос нужен, — запрос ради
СОСТОЯНИЯ «строка уже удалена», ответ предметом правила не является. ⚠️ КОММЕНТАРИЙ НЕСУЩИЙ, А НЕ
УКРАШЕНИЕ: без него следующий читатель примет снятое присваивание за забытую проверку и вернёт
имя обратно.

**(3) Что НЕ трогается.** Ни одно утверждение обоих правил, ни один заголовок запроса, ни адрес
контроля, ни порядок запросов. Прочие имена, помеченные статическим разбором в других файлах: их в
перечне ревизии нет, и снимать их «заодно» значит расширять предмет партии молча.

**(ПОСЛЕДНИЙ ШАГ ЗАДАЧИ) ОБНОВИТЬ ГРАФ ПРОЕКТА — `graphify update .`.** ⚠️ ЭТО ТРЕБОВАНИЕ
`./CLAUDE.md` ДОСЛОВНО: «After modifying code, run `graphify update .` to keep the graph current
(AST-only, no API cost)». Настоящий план правит файлы, которые граф индексирует, поэтому шаг
обязателен и стои́т ПОСЛЕДНИМ — после всех правок плана и после прогонов.

⚠️ **ПОЛЕ `graphify.enabled: false` В `.planning/config.json` ОСНОВАНИЕМ ДЛЯ ОТКАЗА НЕ ЯВЛЯЕТСЯ, И
ЭТО ЗАПИСАНО ПРЯМО, А НЕ ПРОПУЩЕНО МОЛЧА.** Замер планировщика 2026-09-05: конфигурация несёт
`"graphify": {"enabled": false}` — это ключ ИНТЕГРАЦИИ GSD, управляющий графом `.planning/graphs/`,
которого в дереве НЕТ вовсе (`ls -d .planning/graphs` → отсутствует). Граф ПРОЕКТА живёт в
`graphify-out/` (`ls -d graphify-out` → есть), команда доступна (`command -v graphify` → есть), и
свежести этого графа требует `./CLAUDE.md`, а не конфигурация GSD. ДВА РАЗНЫХ ГРАФА, ДВА РАЗНЫХ
ВЛАДЕЛЬЦА, и выключенный ключ одного не отменяет требования другого.

⚠️ **ПОБОЧНОГО ЭФФЕКТА НА ПРИЁМОЧНЫЕ КРИТЕРИИ ПАРТИИ НЕТ, И ЭТО ЗАМЕРЕНО:** `graphify-out/`
игнорируется гитом (`.gitignore:56`, `**/graphify-out/`) и деревом не отслеживается, поэтому шаг не
может тронуть ни один вывод `git diff`, на котором стоят гейты `<verify>` и приёмочные критерии
планов партии.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` отличается от суммы чисел, снятых до правки, — снят не мёртвый код, а живой</fails_when>
    <automated>grep -c '^import pytest$' tests/test_templates/test_components.py</automated>
    <fails_when>вывод отличен от `0` при нулевом числе прочих вхождений имени `pytest` в модуле — мёртвый импорт остался на месте</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — правка сломала разбор одного из двух модулей</fails_when>
    <automated>graphify update .</automated>
    <fails_when>ненулевой код возврата либо вывод содержит `Traceback` — граф проекта не обновлён, и следующий читатель получит на вопрос о коде устаревший ответ. ⚠️ `command not found` — НАХОДКА ДЛЯ СВОДКИ (инструмент не установлен в среде исполнения), а не молчаливый пропуск шага регламента</fails_when>
  </verify>

  <acceptance_criteria>
- Число вхождений имени `pytest` в модуле правил компонентов приведено в сводке, и решение (снят /
  не снят) названо вместе с ним.
- Число вхождений имени результата первого запроса в пределах правила приведено в сводке; снятие
  выполнено только при единственном вхождении.
- Однострочное основание при запросе поставлено: в сводке приведён его текст.
- Исход правил не изменился: числа правил каждого модуля до и после совпадают, все четыре числа
  приведены в сводке.
- Прочих имён, помеченных статическим разбором, план не трогал:
  `git diff --stat -- tests/` показывает ровно два файла этой задачи и файлы задач 1 и 2.
  </acceptance_criteria>

  <reversibility rating="reversible">
Снимаются две строки мёртвого кода; поведения ни продукта, ни суиты правка не меняет, и откат равен
их возврату.
  </reversibility>

  <done>
Неиспользуемый импорт снят после замера вхождений; имя результата первого запроса снято, а
однострочное основание запроса записано; исходы всех правил обоих модулей прежние.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Новые символы `tests/test_pages/test_hx_location_destinations.py`:**

| Символ | Что это |
|---|---|
| сборка цепи шаблона | множество имён «файл + база транзитивно + все включаемые», с невыразимыми циклами и громким отказом на отсутствующем звене |
| `test_control_a_top_level_binding_in_the_shell_reddens_the_chain_rule` | зубы двумя половинами: объявление посажено в БАЗУ синтетической копии — найдено; та же цепь без него — находок ноль |

**Изменённые символы того же файла:** `test_no_transition_destination_declares_a_top_level_binding_inline`
(обходит цепь, называет звено в отказе, утверждает непустоту цепи); `_page_modules` (рекурсивный
обход); разбор вызовов перехода (нераспознанная форма адреса краснит прогон);
`HX_LOCATION_DESTINATION_CALLS_DECLARED` — сдвигается ТОЛЬКО если замер прибавил вызовы, и тогда с
летописью.

**Изменённые символы `tests/test_templates/test_components.py`:** докстринг `_run_modal_lifecycle`
(новая граница тремя утверждениями поимённо); комментарий сценария сноса (пометка опровергнутости с
действующим преемником); имя сценария `scenarioFocusAfterTeardown` — переименовывается ТОЛЬКО если
замер показал не более трёх мест; снятая строка `import pytest`.

**Изменённый символ `tests/test_pages/test_confirm_delete_transport.py`:** правило повторного
удаления — связывание имени с результатом первого запроса снято, основание запроса записано строкой.

**Новых файлов, новых путей продукта, миграций и правок `.planning/` план НЕ производит.**
</artifacts_this_phase_produces>

<threat_model>
⚠️ Таблица собрана на **ASVS 1** (`workflow.security_asvs_level` → `1`) с порогом блокировки
**`high`** (`workflow.security_block_on` → `"high"`) при `workflow.security_enforcement` → `true`.
Все три величины сняты `config-get`. Строка `*-SC` несёт `accept`: пакетов план не устанавливает,
`pyproject.toml` не правит.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| гейт формы исходника → рантайм подмены тела | гейт читает файл цели, рантайм исполняет всю цепь: разница есть слепая зона, а не осторожность |
| зелёное правило гарнира → читатель | правило без названной границы читается как проверка утверждений о порядке, которых оно не проверяет |
| комментарий суиты → следующий автор | опровергнутая формулировка без пометки уносится читателем как действующая |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-19-01 | Tampering | гейт объявлений верхнего уровня у целей перехода | high | mitigate | Разбор переводится на ЦЕПЬ шаблона (задача 1), и способность гейта увидеть шелл показана контролем, сажающим объявление в БАЗУ. Без этого `const` в `base.html` убил бы клиентский слой всех двенадцати целей при зелёном гейте — дословный возврат `CR-03` |
| T-10-19-02 | Denial of Service | клиентский слой всех экранов-целей | high | mitigate | Тот же разбор цепи: предмет угрозы — одновременная смерть клиентского слоя двенадцати экранов от одной строки в шелле. Сегодня активного дефекта нет, и это ИЗМЕРЕНО ревизией, а не предположено; правило заводится ради слепой зоны, а не ради отказа |
| T-10-19-03 | Repudiation | зелень правил поведенческого гарнира | medium | mitigate | Граница названа в докстринге запускающей функции тремя утверждениями поимённо (задача 2): зелень перестаёт читаться шире предмета |
| T-10-19-04 | Repudiation | комментарий сценария сноса панели | medium | mitigate | Пометка опровергнутости с указанием действующего преемника и источника опровержения; прежний текст остаётся рядом по идиоме D-30/D-32 |
| T-10-19-05 | Tampering | обход страничного слоя и сопоставление адресов | medium | mitigate | Рекурсивный обход плюс громкий отказ на нераспознанной форме адреса: модуль в подкаталоге и вызов с неразобранным `redirect=` перестают выпадать молча |
| T-10-19-06 | Information Disclosure | мёртвый код в файлах точного охвата | low | mitigate | Снятие выполняется ТОЛЬКО после замера вхождений имени, и решение называется вместе с числом: снятый вслепую импорт роняет сбор тестов целиком |
| T-10-19-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
</threat_model>

<verification>
1. `uv run pytest tests/ -q -p no:randomly` — прогон целиком зелен. ⚠️ ЕСЛИ РАСШИРЕНИЕ ОХВАТА ГЕЙТА
   НАШЛО В ДЕРЕВЕ ОБЪЯВЛЕНИЕ ВЕРХНЕГО УРОВНЯ — это НАХОДКА, а не помеха: её называют поимённо и
   закрывают, а сужать охват обратно ЗАПРЕЩЕНО.
2. `uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly -k "top_level_binding_in_the_shell"`
   — контроль базы зелен.
3. `grep -c 'PAGES_DIR.glob(' tests/test_pages/test_hx_location_destinations.py` → `0`.
4. `grep -c 'ОПРОВЕРГНУТО' tests/test_templates/test_components.py` — не `0`.
5. `git diff --name-only -- .planning/ app/` — пусто: план правит только суиту.
6. `uv run python -m compileall -q app main.py tests` — сборка проходит.
</verification>

<success_criteria>
- Гейт целей перехода читает то, что исполнит рантайм: цепь шаблона, а не один файл; способность
  увидеть шелл ПОКАЗАНА контролем, а не заявлена.
- Цепь не вырождается, циклы невыразимы, отсутствующее звено краснит громко.
- Обход страничного слоя рекурсивен; нераспознанная форма адреса краснит прогон с именем и строкой.
- Граница поведенческого гарнира названа в нём самом тремя утверждениями поимённо.
- Опровергнутая формулировка помечена, а не стёрта, и у неё назван действующий преемник.
- Мёртвый код снят ПОСЛЕ замера вхождений, и решение названо вместе с числом.
- `.planning/` и `app/` не тронуты; требование `FORM-06` остаётся незавершённым.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-19-SUMMARY.md` по завершении.
</output>
