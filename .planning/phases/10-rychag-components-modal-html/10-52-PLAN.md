---
phase: 10-rychag-components-modal-html
plan: 52
type: tdd
# ⚠️ `type: tdd`, А НЕ `type: execute`, И ЭТО ОТЛИЧИЕ ОТ ПЛАНОВ 10-41…10-51. Основание — предмет:
# пятое утверждение гейта критерия 3 есть работа ровно тест-первая (правило → замер RED с печатью
# ПОЛУЧЕННОГО значения → правка объявления → замер GREEN → антивакуумные контроли), и `tdd_mode`
# конфигурации равен `true` (замерено `config-get workflow.tdd_mode` 2026-09-12).
wave: 1
depends_on: []
gap_closure: true
gap_ids: [GAP-1b]
# ⚠️ ИДЕНТИФИКАТОР ГЭПА ЗДЕСЬ НЕ ИЗ РЕЕСТРА `10-UAT.md`, И ЭТО НАЗВАНО, А НЕ УМОЛЧАНО. Записи
# `G-10-*` заводит обход; настоящий гэп заведён ОДИННАДЦАТЫМ КРУГОМ ВЕРИФИКАЦИИ и живёт в
# `10-VERIFICATION.md` разделом `gaps` с ДВУМЯ пунктами `missing`. Второй пункт («ПЯТОЕ УТВЕРЖДЕНИЕ
# ГЕЙТА ЧЕТЫРЁХ ЧИСЕЛ — счёт регистраций обработчиков в телах `<script>` всех шаблонов, объявленный
# числом») закрывается настоящим планом; первый (РЕШЕНИЕ ВЛАДЕЛЬЦА) — планом 10-53, и подменить
# один другим нельзя: машина не вправе принять чужой критерий приёмки за владельца.
files_modified:
  - tests/test_templates/test_components.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (`config-get`, 2026-09-12): `security_asvs_level` → `1`,
# `security_block_on` → `"high"`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ, поэтому
# протокол законности пакетов (`## Package Legitimacy Audit`) не требуется и чекпоинта не заводит.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ. Замер переснят 2026-09-12 на `9e6b859`: `estimate-calibration` →
  # `sample_count: 0`, `factor: 1`, `applied: false`, `min_samples: 3`, `confidence: "low"`.
  # `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена себе. Калибровка недостижима
  # и сегодня: образцов ноль при пороге три, и первые появятся лишь после исполнения этой партии.
  # Проекция остаётся СОВЕТОМ. Размер держится тем, что измеримо СЕЙЧАС: задач 3, правимый файл
  # ОДИН.
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "КРИТЕРИЙ 3 ПОЛУЧАЕТ ПРИНУЖДЕНИЕ ТАМ, ГДЕ ОНО БЫЛО ВАКУУМНЫМ: регистрация обработчика, добавленная в тело `<script>` шаблона `app/templates/**`, краснит ПРОГОН, а не ждёт круга верификации"
    - "ОТКАЗ НАЗЫВАЕТ ПОЛУЧЕННОЕ, А НЕ ТОЛЬКО НЕСОГЛАСИЕ: в выводе стои́т ФАЙЛ и ДВА ЧИСЛА при нём (найдено / объявлено), а у появившихся и исчезнувших файлов — РАЗНИЦА МНОЖЕСТВ, а не пара итогов"
    - "ЧИСЛО РЕГИСТРАЦИЙ ЗАГОТОВОК ПЛАШКИ НЕ ПОЛУЧАЕТ ВТОРОЙ КОПИИ: значение для `includes/htmx_error_banner.html` приходит ИМПОРТОМ единственного источника (`FAILURE_BANNER_HANDLERS_MEASURED`), поэтому расхождение двух копий невозможно ПО ПОСТРОЕНИЮ, а не по договорённости"
    - "ПЕРЕХОД, СДЕЛАННЫЙ ДЕВЯТОЙ ПАРТИЕЙ, ОТЛИЧИМ МАШИНОЙ: правило РАЗЛИЧАЕТ дерево до коммита `4e24d61` (две регистрации в заготовках) и дерево после него (три) — то есть гейт, существовавший бы тогда, эту правку бы увидел"
    - statement: "ЧЕТЫРЕ ДЕЙСТВУЮЩИХ УТВЕРЖДЕНИЯ НЕ ОСЛАБЛЕНЫ И НЕ ПЕРЕИМЕНОВАНЫ: имя `test_criterion_three_holds_by_the_numbers` сохранено (отбор `-k criterion_three` прежних планов продолжает его находить), четыре импорта единственных источников на месте, и их отрицательные контроли зелены"
      verification: test
    - statement: "ПЯТОЕ УТВЕРЖДЕНИЕ НЕ ПРЕДПОЛАГАЕТ НИ ОДНОГО ИЗ ДВУХ ОТВЕТОВ ВЛАДЕЛЬЦА: оно объявляет ЧИСЛО дерева и ловит НЕЗАЯВЛЕННОЕ, а ЗАПИСЬ об основании этого числа (расширенное отступление либо признанное нарушение) вносит план 10-53"
      verification: test
  artifacts:
    - "`tests/test_templates/test_components.py` — разборщик тел `<script>`, объявление числа регистраций, пятое утверждение внутри `test_criterion_three_holds_by_the_numbers` и два отрицательных контроля при нём"
  key_links:
    - "разборщик тел `<script>` → предмет пятого утверждения: единственное, чем блок сценария внутри шаблона отличим от прозы вокруг него; ломается — правило считает комментарии, и объяснение, набранное проверяемой строкой, роняет собственный гейт (урок `includes/htmx_config.html`)"
    - "импорт `FAILURE_BANNER_HANDLERS_MEASURED` → единственность числа заготовок: единственное, чем две копии числа лишены возможности разойтись молча; ломается — правило начинает проверять устаревшее значение при зелёном соседе"
    - "каталог, принимаемый ПАРАМЕТРОМ (`_all_templates(directory)` модуля разметочных гейтов) → выразимость отрицательных контролей: единственное, чем зубы правила показываются, а не заявляются; ломается — контроли невыразимы, и пятое утверждение зеленеет по построению ровно как четыре прежних не видели предмета"
  prohibitions:
    - statement: "ТРЕТИЙ ОБРАБОТЧИК В `app/templates/includes/htmx_error_banner.html` НЕ ОТКАТЫВАЕТСЯ И НЕ ПРАВИТСЯ НИ НА СИМВОЛ: он закрывает настоящий дефект `G-10-5`, стережётся правилами плана 10-49 с двумя отрицательными контролями, и предмет настоящего плана — ПРИНУЖДЕНИЕ ЗАЯВЛЕННОГО ЧИСЛА, а не ревизия правки"
      status: flagged-unverified
    - statement: "`app/` НЕ ПРАВИТСЯ НИ ОДНОЙ ЗАДАЧЕЙ: предмет плана — модуль суиты. Правка рычага `components/modal.html` открыла бы `CR-01`, правка заготовок пересеклась бы с планом 10-49 и сдвинула бы измеряемое число в том же прогоне, где оно объявляется, — переход цвета перестал бы быть измеримым"
      status: flagged-unverified
    - statement: "ИМЯ `test_criterion_three_holds_by_the_numbers` НЕ МЕНЯЕТСЯ: на него ключáются отборы прежних планов (`-k criterion_three` — планы 10-13, 10-41), и переименование сделало бы их отбор пустым, а пустой отбор зеленеет ВАКУУМОМ при коде возврата 0 (замер того же класса приведён планом 10-51)"
      status: flagged-unverified
    - statement: "ВТОРОЙ КОПИИ ЧИСЛА РЕГИСТРАЦИЙ ЗАГОТОВОК НЕ ЗАВОДИТСЯ: значение берётся ИМПОРТОМ из `tests/test_pages/test_shell.py`, а не переписывается литералом — вторая копия разошлась бы с первой молча, и это та же доктрина единственного источника, по которой утверждения 3 и 4 ввозят `INLINE_HANDLER_ATTR` и `CLIENT_STATE_NODES`"
      status: flagged-unverified
    - statement: "`10-UAT.md` НЕ ПРАВИТСЯ ВОВСЕ, поле `status` его шапки терминальным (`passed`/`complete`) НЕ СТАНОВИТСЯ ни от одного символа плана, и ни одна отметка прохождения в нём не ставится: ради запрета этого самозаверения живёт `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`"
      status: flagged-unverified
    - statement: "`FORM-06` НЕ ОТМЕЧАЕТСЯ ВЫПОЛНЕННЫМ: ни флажок `.planning/REQUIREMENTS.md:31`, ни клетка `:136` (`Gaps Found`) не двигаются — требование закрывается ПОДПИСЬЮ ЧЕЛОВЕКА на приёмке обхода, а не машинным утверждением, и правило `test_no_requirement_is_marked_complete_before_its_phase_verification_passed` стережёт именно это"
      status: flagged-unverified
    - statement: "ПРЕЖНЯЯ ПРОЗА ДОКСТРИНГА НЕ ВЫЧЁРКИВАЕТСЯ: запись «ЧЕТЫРЬМЯ ЧИСЛАМИ» была ВЕРНА для своего дерева и остаётся в тексте, а рядом с ней встаёт то, что её переросло (идиома D-30/D-32)"
      status: flagged-unverified
  assumptions:
    - statement: "РЕБРО `idempotency` ТРЕБОВАНИЯ FORM-06 («что произойдёт, если это исполнить дважды на том же входе») НАСТОЯЩИМ ПЛАНОМ НЕ РАЗРЕШАЕТСЯ. Предмет ребра у фазы двойной: (а) продуктовый — «после N удалений в документе не остаётся N мёртвых диалогов» и правило повторного удаления; (б) записной — повторяемость правки документа отложенного. План не трогает НИ ОДНОГО из двух: продуктовых строк в нём нет, `deferred-items.md` он не правит. Само пятое утверждение идемпотентно по построению (читает дерево, ничего в нём не меняет), и это свойство ПРАВИЛА, а не гарантия маршрута удаления — подменять одно другим запрещено"
      edge: idempotency
      status: unresolved
    - statement: "РЕБРО `concurrency` ТРЕБОВАНИЯ FORM-06 («что гарантировано при прерывании либо параллельном исполнении») НАСТОЯЩИМ ПЛАНОМ НЕ РАЗРЕШАЕТСЯ. Предмет ребра у фазы — третий обработчик, гасящий обе заготовки по признаку успеха ЛЮБОГО обмена (окно 74 / находка ревизии `IN-02`), и гонка «панель уходит по завершению POST раньше, чем поднимается отказ follow-up GET» (корень `G-10-7`). Ни то, ни другое планом не трогается: он ИЗМЕРЯЕТ число регистраций, а не порядок их срабатывания. Параллельность здесь есть только параллельность ВОЛН партии, и она разведена непересекающимися перечнями правимых файлов: настоящий план — единственный в волне 1"
      edge: concurrency
      status: unresolved
---

<objective>
**Гейт, объявленный машинным принуждением критерия 3, предмета своего критерия не измеряет — и
потому остался зелёным на дереве, где критерий нарушен.** Так говорит одиннадцатый круг, и это его
второй блокер, названный дословно: «принуждение вакуумно ровно в той точке, где понадобилось».

Тело `test_criterion_three_holds_by_the_numbers` (`tests/test_templates/test_components.py:4784`)
прочитано планировщиком ЦЕЛИКОМ. Четыре его утверждения суть: (1) имена файлов каталога
`app/static/js/`, (2) шаблоны со встроенным обработчиком отправки, (3) атрибуты событий рантайма
разметки, (4) ЧИСЛО узлов клиентского состояния. **Тело блока `<script>` внутри шаблона не
измеряется НИ ОДНИМ из четырёх.** Именно в тело блока `<script>` коммит `4e24d61` (план 10-49) и
добавил семь исполняемых строк — регистрацию ТРЕТЬЕГО обработчика в
`app/templates/includes/htmx_error_banner.html:227-233`.

**ЗАМЕР ПЛАНИРОВЩИКА, СНЯТЫЙ КОМАНДАМИ 2026-09-12 НА `9e6b859`, А НЕ ВЫПИСАННЫЙ ИЗ ОТЧЁТА:**

- вхождений вызова регистрации в шаблонах ДВА ФАЙЛА и одиннадцать мест: `ads/form.html` — 8,
  `includes/htmx_error_banner.html` — 3;
- на базе фазы (`7c1041d`) те же два файла давали 8 и **2** — то есть фаза сдвинула ровно одно
  число ровно в одном файле;
- в дереве СЕМЬ шаблонов несут блок `<script>` (`accounts/connect_tg_user.html`,
  `accounts/partials/connect_status.html`, `ads/form.html`, `auth_base.html`, `base.html`,
  `includes/htmx_config.html`, `includes/htmx_error_banner.html`), и пять из них не несут ни одной
  регистрации — то есть объявление ДОЛЖНО быть перечнем файлов с числами, а не одним итогом;
- свойства присваивания обработчика (`.on…=`) внутри тел `<script>` в дереве НОЛЬ; единственное
  вхождение такой формы — атрибут разметки `components/thumb.html:34`, то есть предмет утверждения
  3, а не пятого.

Purpose: сделать так, чтобы СЛЕДУЮЩАЯ такая правка краснила ПРОГОН. Сегодня её ловит круг
верификации — прибор, работающий раз в партию и читающий глазами; правило работает на каждом
прогоне и читает деревом. Одиннадцатый круг называет цену отсутствия правила прямо: «следующая
такая правка снова пройдёт молча».

Output: пятое утверждение гейта критерия 3, объявленное ПЕРЕЧНЕМ ФАЙЛОВ С ЧИСЛАМИ, с печатью
ПОЛУЧЕННОГО значения в отказе и двумя отрицательными контролями при нём.

⚠️ **ЭТО ТРАССИРУЮЩИЙ СРЕЗ ПАРТИИ, И ОН ИДЁТ ПЕРВЫМ НЕ ПО УДОБСТВУ.** Партия ведётся одним
срезом производственного качества, проверенным ДО любого расширения: правило → замер RED с печатью
полученного значения → правка объявления → замер GREEN → антивакуумный контроль. Срез проходит все
слои предмета за один проход: каталог шаблонов → тело блока сценария → форма регистрации →
объявленное число → отказ, называющий файл и оба числа. Человеческие замеры (план 10-54) и записи
решения (план 10-53) идут ПОСЛЕ него, потому что оба опираются на его числа: владельцу предъявляется
то, что ИЗМЕРЕНО правилом, а не то, что пересказано отчётом.

⚠️ **ЧЕГО ЭТОТ ПЛАН НЕ ДЕЛАЕТ, И ЭТО НАЗЫВАЕТСЯ ПЕРВЫМ, А НЕ ОБНАРУЖИВАЕТСЯ ЧИТАТЕЛЕМ СВОДКИ.**

1. **ОН НЕ ОТКАТЫВАЕТ ТРЕТИЙ ОБРАБОТЧИК.** Правка работает, закрывает настоящий дефект `G-10-5`
   (плашка отказа переживала успешный обмен) и снабжена двумя отрицательными контролями. Одиннадцатый
   круг говорит это своими словами: «Он не значит, что правка плоха».
2. **ОН НЕ РЕШАЕТ, ПРИНЯТ ЛИ КРИТЕРИЙ 3 НАРУШЕННЫМ.** Это решение владельца, и оно стои́т
   `checkpoint:decision` в плане 10-53 ПЕРЕД задачей, которая его записывает. Пятое утверждение
   объявляет ЧИСЛО СЕГОДНЯШНЕГО ДЕРЕВА и ловит НЕЗАЯВЛЕННОЕ; какое ОСНОВАНИЕ стои́т за этим числом —
   расширенное отступление или признанное нарушение — пишет план 10-53 в докстринг и в отчёт.
   ⚠️ Предполагать любой из двух ответов планировщик не вправе по тому же основанию, по которому
   верификатор не вправе толковать отступление шире, чем оно себя объявляет.
3. **ОН НЕ ЗАКРЫВАЕТ ЗАПИСЬ `G-10-7`, НЕ ДВИГАЕТ `FORM-06` И НЕ ПРАВИТ `10-UAT.md`.** Перечень
   непокрытого — раздел «Остаток» ниже, и ни один его пункт не смягчён.

⚠️ **ПОЧЕМУ RED ЗДЕСЬ МЕРЯЕТСЯ НА ОБЪЯВЛЕНИИ БАЗЫ ФАЗЫ, А НЕ НА НЕТРОНУТОМ ДЕРЕВЕ. ГРАНИЦА
НАЗЫВАЕТСЯ ЗДЕСЬ, А НЕ ОСТАВЛЯЕТСЯ СЛЕДУЮЩЕМУ ЧИТАТЕЛЮ.** Обычная форма RED этой фазы — «правило
красно на нетронутом дереве» — здесь НЕДОСТИЖИМА честно: предмет измерения уже стои́т в том
состоянии, которое владелец может принять, и правило, красное на настоящем дереве, пришлось бы
зеленить ОТКАТОМ третьего обработчика — а откат запрещён пунктом 1 выше. Поэтому RED снимается на
объявлении, равном ЗАМЕРУ БАЗЫ ФАЗЫ (`7c1041d`: заготовки — 2), и он не выдумка: это ровно тот
замер, при котором правило, существуй оно тогда, увидело бы правку девятой партии. GREEN снимается
переводом объявления на ИМПОРТ единственного источника. Переход цвета измерен ИСПОЛНЕНИЕМ правила с
печатью полученного значения, а не кодом возврата — конструкция вида `test $? -ne 0` объявляет отказ
измеренным там, где правило не исполнялось вовсе (ошибка сборки и пустой отбор дают тот же исход).
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-49-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**Новые символы в `tests/test_templates/test_components.py`:**

- `SCRIPT_HANDLER_REGISTRATIONS_DECLARED_HERE` — объявление числа регистраций ДЛЯ ТЕХ ФАЙЛОВ, У
  КОТОРЫХ ЕДИНСТВЕННОГО ИСТОЧНИКА ЕЩЁ НЕТ (сегодня — один файл, `ads/form.html`);
- `_declared_script_handler_registrations()` — сборка полного объявления: перечень выше плюс
  значение заготовок, приходящее ИМПОРТОМ;
- `HANDLER_REGISTRATION_FORMS` — перечень РАСПОЗНАВАЕМЫХ форм регистрации;
- `_script_bodies(source)` — тела блоков `<script>` шаблона и ничего кроме них;
- `_strip_js_comments(source)` — вырезание комментариев сценария (строчных и блочных);
- `_script_handler_registrations(templates)` — измеренный перечень «файл → число регистраций»;
- `test_control_negative_a_new_handler_registration_reddens_the_gate` — контроль на ЛИШНЮЮ
  регистрацию в копии каталога шаблонов;
- `test_control_negative_a_new_template_with_a_registration_reddens_the_gate` — контроль на НОВЫЙ
  файл шаблона, принёсший регистрацию.

**Изменённые символы:** `test_criterion_three_holds_by_the_numbers` — ПЯТОЕ утверждение в теле, пять
сообщений отказа с перенумерованным знаменателем, докстринг с записью D-30/D-32. Имя теста НЕ
меняется.

**Ввозимые символы (объявлений не заводят, копий не создают):** `_all_templates` из
`tests/test_templates/test_htmx_markup_gates.py` (обход, принимающий каталог ПАРАМЕТРОМ),
`_strip_comments` из `tests/test_templates/test_htmx_inventory.py` (уже ввезён),
`FAILURE_BANNER_HANDLERS_MEASURED` из `tests/test_pages/test_shell.py` (ввоз ФУНКЦИОНАЛЬНО-МЕСТНЫЙ,
как уже сделано в этом же модуле на строках 2869 и 4276 — модульный ввоз через границу пакетов этот
модуль не заводит).
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: трассирующий срез — пятое утверждение сквозь все слои предмета, RED на объявлении базы фазы и GREEN на импорте единственного источника</name>

  <files>
tests/test_templates/test_components.py
  </files>

  <read_first>
- `tests/test_templates/test_components.py:4746-4874` — раздел критерия 3 ЦЕЛИКОМ: `STATIC_JS_DIR`,
  `VENDORED_JS_FILES`, `_vendored_js_files` с его каталогом-параметром и
  `test_criterion_three_holds_by_the_numbers` со всеми четырьмя утверждениями и их сообщениями.
  ⚠️ ЧИТАТЬ ДОСЛОВНО докстринг: он записывает прочтение критерия 3 (D-13) и объявляет «четыре вещи,
  по одной на утверждение». Настоящая задача прибавляет пятую и ОСТАВЛЯЕТ прежний текст.
- `tests/test_templates/test_components.py:4875-4910` —
  `test_control_negative_a_new_vendored_script_reddens_the_gate`. ⚠️ ФОРМА ОТРИЦАТЕЛЬНОГО КОНТРОЛЯ
  НАСЛЕДУЕТСЯ ОТСЮДА, а не изобретается: три утверждения подряд — подмена приземлилась, правило её
  видит, боевое дерево не тронуто.
- `tests/test_templates/test_components.py:36-48` — блок ввозов и абзац о доктрине единственного
  источника («вторая копия разошлась бы с первой МОЛЧА»). ⚠️ ПЯТОЕ УТВЕРЖДЕНИЕ ОБЯЗАНО ЕЙ
  ПОДЧИНЯТЬСЯ.
- `tests/test_templates/test_components.py:1002-1008` — местный `_all_templates()` БЕЗ параметра
  каталога. ⚠️ ЕГО НЕ ПРАВИТЬ: он ввезён десятками правил модуля. Пятое утверждение берёт обход,
  принимающий каталог ПАРАМЕТРОМ, ввозом из модуля разметочных гейтов.
- `tests/test_templates/test_components.py:2860-2875` и `:4270-4285` — два ФУНКЦИОНАЛЬНО-МЕСТНЫХ
  ввоза из `tests/test_pages/test_shell.py`. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: форма ввоза через границу
  пакетов в этом модуле УЖЕ ЕСТЬ, и второй её формы не заводится.
- `tests/test_templates/test_htmx_markup_gates.py:319-340` — `_all_templates(directory)` с
  докстрингом о том, ПОЧЕМУ каталог принимается параметром («группа контроля обязана подать обходу
  ИЗМЕНЁННУЮ копию дерева»). Обход рекурсивный и отсортированный.
- `tests/test_templates/test_htmx_inventory.py`, `_strip_comments` целиком, включая абзац о том, что
  гейт, считающий прозу, «позволяет комментарию ЗАМЕНИТЬ собой пропавшее место, оставив число
  верным». ⚠️ ЗДЕСЬ ЖЕ ГРАНИЦА: он вырезает Jinja- и HTML-комментарии и НЕ вырезает комментариев
  СЦЕНАРИЯ — вторую половину заводит настоящая задача.
- `tests/test_pages/test_shell.py:2295-2312` — ЛЕТОПИСЬ константы `FAILURE_BANNER_HANDLERS_MEASURED`
  (переснята 2 → 3 планом 10-49) и сама константа. ⚠️ ЧИТАТЬ ДОСЛОВНО: это ЕДИНСТВЕННЫЙ ИСТОЧНИК
  числа регистраций заготовок, и пятое утверждение обязано его ВВЕЗТИ, а не переписать.
- `app/templates/includes/htmx_error_banner.html:213-235` — предмет измерения: обёртка признака
  однократности и ТРИ вызова регистрации внутри единственного блока `<script>`. ⚠️ ЧИТАТЬ И НЕ
  ПРАВИТЬ НИ НА СИМВОЛ.
- `app/templates/ads/form.html:307-541` — второй файл предмета: единственный блок `<script>`, 8
  вызовов регистрации и три десятка строк строчных комментариев внутри блока. ⚠️ ЧИТАТЬ И НЕ
  ПРАВИТЬ: этот файл и есть основание требовать вырезания комментариев сценария.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:270-325` — запись гэпа
  целиком: оба пункта `missing` и перечень артефактов с их претензиями.
- `.planning/ROADMAP.md`, раздел `### Phase 10`, критерий 3 ДОСЛОВНО. ⚠️ ФОРМУЛИРОВКА В ДОКСТРИНГ
  ПЕРЕНОСИТСЯ ПОБУКВЕННО, если переносится вообще: пересказанный критерий есть принуждение не того
  критерия.
  </read_first>

  <behavior>
1. Разборщик тел `<script>` на СЕГОДНЯШНЕМ дереве находит семь файлов с блоком сценария и
   одиннадцать вызовов регистрации в двух из них: `ads/form.html` — 8,
   `includes/htmx_error_banner.html` — 3.
2. Комментарии сценария из измеряемой строки вырезаны: перечень, снятый со вырезанием и без него,
   на сегодняшнем дереве СОВПАДАЕТ — то есть требование зубов пока не меняет, а дыру закрывает.
3. Пятое утверждение с объявлением, равным замеру базы фазы (заготовки — 2), КРАСНО, и в тексте
   отказа стои́т имя файла `includes/htmx_error_banner.html` и оба числа при нём.
4. Пятое утверждение с объявлением, где значение заготовок приходит ИМПОРТОМ, ЗЕЛЕНО — переход
   цвета, а не первый зелёный прогон.
5. Четыре действующих утверждения цвета не меняют ни на одном из двух деревьев объявления.
  </behavior>

  <action>
**(1) РАЗБОРЩИК ТЕЛ БЛОКА СЦЕНАРИЯ — `_script_bodies(source)`.** Возвращает содержимое каждого
блока `<script>` шаблона и ничего кроме него. Открывающий тег с атрибутами обязан приниматься:
шелл подключает вендоренный рантайм тегом с адресом, и разборщик, знающий только пустой тег,
потерял бы будущий блок молча. Незакрытый блок есть отказ разборщика, а не пустой результат —
молчаливый пропуск тут и есть предмет гэпа. Докстринг называет, ЗАЧЕМ разбирается тело, а не файл:
регистрация, лежащая в прозе вокруг блока, предметом критерия 3 не является, а регистрация внутри
блока — является.

**(2) ВЫРЕЗАНИЕ КОММЕНТАРИЕВ СЦЕНАРИЯ — `_strip_js_comments(source)`.** Строчные и блочные.
⚠️ ЗАЧЕМ ЭТО ОБЯЗАТЕЛЬНО, И ЭТО НЕ ГИГИЕНА РАЗБОРА. Ввозимый `_strip_comments` вырезает
Jinja- и HTML-комментарии, но внутри блока `<script>` шаблона `ads/form.html` лежат три десятка
строк строчных комментариев-обоснований — то есть ровно тот класс, на котором проект уже заплатил
дважды: гейт, считающий прозу, краснеет на правку документации и позволяет комментарию ЗАМЕНИТЬ
собой пропавшее место, оставив число верным. Порядок вырезания назвать в докстринге: сначала
Jinja и HTML ввозимым помощником, потом сценарные — HTML-комментарий, лежащий внутри блока,
до рантайма не доходит.

**(3) ПЕРЕЧЕНЬ РАСПОЗНАВАЕМЫХ ФОРМ — `HANDLER_REGISTRATION_FORMS`.** Две формы: вызов метода
регистрации слушателя на приёмнике и присваивание свойства-обработчика. Вторая формы в дереве
сегодня НОЛЬ вхождений внутри тел блоков (замер планировщика 2026-09-12), и включается она не
впрок, а потому что правило, знающее одну форму, пропустит регистрацию, вернувшуюся в другой.
⚠️ ГРАНИЦА ДОКАЗАННОГО НАЗЫВАЕТСЯ В ДОКСТРИНГЕ ПРЯМО: пятое утверждение НЕ ВИДИТ регистраций,
сделанных кодом из каталога вендоренных сценариев (предмет утверждения 1), атрибутов событий
разметки (предмет утверждения 3), узлов клиентского состояния (предмет утверждения 4) и
регистрации, собранной склейкой строк. Перечисление того, чего правило не видит, есть часть
правила, а не признание его слабости.

**(4) ИЗМЕРЕННЫЙ ПЕРЕЧЕНЬ — `_script_handler_registrations(templates)`.** Отображение «путь
шаблона относительно каталога → число регистраций», и в него попадают ТОЛЬКО файлы с ненулевым
числом. Пять шаблонов дерева несут блок сценария без единой регистрации, и нули в перечне были бы
величинами, которые надо сверять, — сторож завёл бы себе сторожа. Новый файл с регистрацией
приходит в перечень НОВЫМ КЛЮЧОМ, и разница множеств его называет.

**(5) ОБЪЯВЛЕНИЕ — ДВА СЛАГАЕМЫХ, И ВТОРОЕ НЕ КОПИРУЕТСЯ.**
`SCRIPT_HANDLER_REGISTRATIONS_DECLARED_HERE` объявляет ТОЛЬКО те файлы, у которых единственного
источника ещё нет: сегодня это один файл `ads/form.html` со значением 8. Значение
`includes/htmx_error_banner.html` НЕ ОБЪЯВЛЯЕТСЯ ЗДЕСЬ ВОВСЕ: его единственный источник —
`FAILURE_BANNER_HANDLERS_MEASURED` модуля правил шелла, и `_declared_script_handler_registrations()`
собирает полное объявление ВВОЗОМ этого имени (ввоз функционально-местный, по форме, уже применённой
в этом модуле дважды). ⚠️ ЛЕТОПИСЬ ОБОИХ ЧИСЕЛ ПИШЕТСЯ РЯДОМ С ОБЪЯВЛЕНИЕМ по форме
`TRANSITION_ASSEMBLY_PLACES` этого же модуля: чем и когда снято, и что означает движение числа В
КАЖДУЮ СТОРОНУ — вверх есть новая регистрация в дереве и предмет решения о критерии 3, вниз есть
ушедшая регистрация либо потерявший её разборщик.

**(6) RED — ОБЪЯВЛЕНИЕ, РАВНОЕ ЗАМЕРУ БАЗЫ ФАЗЫ.** Прежде ввоза значение заготовок ставится
ЛИТЕРАЛОМ 2 с комментарием, называющим замер: база фазы `7c1041d`, два обработчика Фазы 8
(коммит `6d2eaf2`). Прогнать и записать RED замером: команда, код возврата, полученное значение
ДОСЛОВНО. ⚠️ ЭТО НЕ «НЕВЕРНОЕ ЧИСЛО, ПОТОМ ВЕРНОЕ»: это единственный честный способ ИЗМЕРИТЬ, что
правило различает дерево до коммита `4e24d61` и дерево после него, — и ровно то измерение, которого
у фазы не было ни одного дня.

**(7) ПЯТОЕ УТВЕРЖДЕНИЕ В ТЕЛЕ ГЕЙТА.** Измеренный перечень сличается с объявленным. Текст отказа
несёт: имя утверждения с ЗНАМЕНАТЕЛЕМ ПЯТЬ, перечень файлов, где числа разошлись, с ОБОИМИ числами
у каждого, перечень появившихся файлов и перечень исчезнувших. ⚠️ ПЕЧАТЬ ДВУХ ИТОГОВ ВМЕСТО РАЗНИЦЫ
МНОЖЕСТВ ЗАПРЕЩЕНА ПРЯМО: одиннадцатый круг назвал этот класс находкой ревизии `IN-03`
(перечень внеполосных блоков поднят числом, отказ печатает два числа, а не какие файлы пришли и
какие ушли), и повторять его в правиле, заводимом ПРОТИВ молчаливых движений, было бы платой за
собственный урок. ⚠️ НАСТОЯЩЕЕ УТВЕРЖДЕНИЕ НАХОДКИ `IN-03` НЕ ЗАКРЫВАЕТ — предмет там другой файл
и другое число; закрыто здесь только повторение её КЛАССА.

**(8) GREEN — ЛИТЕРАЛ ЗАМЕНЯЕТСЯ ВВОЗОМ.** Значение заготовок начинает приходить из единственного
источника. Прогнать и записать GREEN тем же способом: команда, код возврата, итоговая строка.
Прежний комментарий о замере базы фазы НЕ ВЫЧЁРКИВАЕТСЯ — он становится летописью числа, и рядом с
ним встаёт запись о том, чем значение берётся теперь (идиома D-30/D-32).

**(9) ЗНАМЕНАТЕЛЬ ЧЕТЫРЁХ ДЕЙСТВУЮЩИХ СООБЩЕНИЙ ПРИВОДИТСЯ К ПЯТИ.** Каждое из четырёх сообщений
отказа называет себя номером из знаменателя; знаменатель, оставленный прежним при пяти
утверждениях, есть проза, разошедшаяся с деревом, — ровно окно 78, только внутри суиты. Правятся
ТОЛЬКО знаменатели и добавляется пятое сообщение; ни одно из четырёх утверждений не переформулируется
и ни один их ввоз не снимается.

**(10) ДОКСТРИНГ ПОЛУЧАЕТ ЗАПИСЬ D-30/D-32 И НАЗЫВАЕТ ПОВОД.** Прежний текст — «ЧЕТЫРЬМЯ ЧИСЛАМИ»
и перечень четырёх вещей — остаётся ДОСЛОВНО: он был верен для своего дерева. Рядом встаёт запись о
том, что его переросло: коммит `4e24d61` завёл регистрацию в теле блока сценария, ни одно из четырёх
утверждений её не измерило, и одиннадцатый круг назвал это блокером. ⚠️ ЗАПИСЬ О ТОМ, КАКОЕ
ОСНОВАНИЕ СТОИ́Т ЗА ЧИСЛОМ ТРИ (расширенное отступление владельца либо признанное нарушение
критерия), НАСТОЯЩЕЙ ЗАДАЧЕЙ НЕ ПИШЕТСЯ: ответа владельца ещё нет, и докстринг обязан сказать
ровно это — «основание вносит план 10-53», а не предположить любую из двух ветвей.
  </action>

  <acceptance_criteria>
- `_script_bodies` на сегодняшнем дереве находит блок сценария в СЕМИ шаблонах; число приведено в
  сводке, и приведён перечень семи путей.
- Измеренный перечень регистраций на сегодняшнем дереве равен двум записям —
  `ads/form.html` → 8 и `includes/htmx_error_banner.html` → 3; оба числа приведены в сводке и сняты
  ПРОГОНОМ, а не выпиской из настоящего плана.
- Перечень, снятый С ВЫРЕЗАНИЕМ комментариев сценария и БЕЗ него, на сегодняшнем дереве совпадает;
  равенство приведено в сводке как замер, а не как ожидание.
- Незакрытый блок сценария даёт ОТКАЗ разборщика, а не пустой результат — проверяется чтением диффа.
- Объявление числа заготовок в модуле `test_components.py` НЕ СОДЕРЖИТ ЛИТЕРАЛА после шага (8):
  значение приходит ввозом `FAILURE_BANNER_HANDLERS_MEASURED` — проверяется чтением диффа.
- ⚠️ ТЕКСТ ОТКАЗА ПЕЧАТАЕТ ПОЛУЧЕННОЕ ЗНАЧЕНИЕ: имя файла и ОБА числа при нём. Это не украшение —
  гейт RED ниже требует имени `includes/htmx_error_banner.html` В ВЫВОДЕ, и приезжает оно ровно из
  напечатанного перечня. Отказ, печатающий один вердикт, уронил бы собственный гейт.
- RED снят прогоном, в котором правило СОБРАЛОСЬ и ИСПОЛНИЛОСЬ: итоговая строка начинается с
  `1 failed`, а не `1 error` и не `N deselected`; полученное значение приведено в сводке дословно.
- GREEN снят тем же отбором; переход цвета назван ОБОИМИ состояниями.
- Пять сообщений отказа несут знаменатель ПЯТЬ; число вхождений приведено в сводке счётом.
- Имя `test_criterion_three_holds_by_the_numbers` не изменено; отбор `-k criterion_three` продолжает
  его находить — предъявлено прогоном.
- Прежний текст докстринга о четырёх числах в файле ПРИСУТСТВУЕТ и не вычеркнут; рядом стои́т запись
  о том, что его переросло, и указание, что основание числа вносит план 10-53.
- `git diff --numstat 9e6b859 -- app` пуст (ревизия закреплена базой партии и без `..HEAD`).
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо итоговая строка сообщает "88 deselected" (то есть отбор пуст и имя теста потеряно переименованием). ⚠️ ЭТА КОМАНДА ПРОГОНЯЕТСЯ ПОСЛЕ ШАГА (8): до него правило красно ПО ПОСТРОЕНИЮ, и отличить им «пятое утверждение красно на объявлении базы» от «сломано одно из четырёх» было бы невозможно</fails_when>
    <automated>OUT=$(uv run pytest tests/test_templates/test_components.py -q -k "criterion_three" 2>&1); printf '%s\n' "$OUT" | tail -4; printf '%s\n' "$OUT" | tail -1 | grep -qE '^1 passed'</automated>
    <fails_when>ненулевой код возврата — итоговая строка не начинается с "1 passed", то есть либо правило не было отобрано (`N deselected` при коде 0 у пустого отбора), либо прогон оборвался сборкой (`1 error`), либо собралось БОЛЬШЕ одного правила и отбор перестал быть точным. ⚠️ ПРОВЕРЯЕТСЯ ИМЕННО НАЧАЛО ИТОГОВОЙ СТРОКИ, А НЕ КОД ВОЗВРАТА: пустой отбор pytest даёт код 0, и гейт на коде возврата зеленел бы вакуумом — замер этого класса приведён планом 10-51 на модуле правил шелла</fails_when>
    <automated>git diff --numstat 9e6b859 -- app</automated>
    <fails_when>вывод НЕПУСТ — тронуто дерево приложения, а измеряемое число сдвинуто в том же прогоне, где объявляется, и переход цвета перестал быть измеримым. ⚠️ РЕВИЗИЯ ЗАКРЕПЛЕНА ЯВНО (`9e6b859` — база партии, HEAD на 2026-09-12), И БЕЗ `..HEAD`: сравнение идёт РАБОЧЕГО ДЕРЕВА с базой, поэтому команда видит правку и до коммита, и после него. Проверка без закреплённой ревизии была бы ПУСТА для уже зафиксированной правки и проходила бы ВАКУУМНО</fails_when>
  </verify>

  <done>
Разборщик тел блока сценария, вырезание комментариев сценария, перечень распознаваемых форм,
измеренный перечень и объявление заведены; пятое утверждение снято RED на объявлении базы фазы с
напечатанным полученным значением и GREEN на ввозе единственного источника; знаменатель четырёх
действующих сообщений приведён к пяти; имя гейта и все четыре ввоза не тронуты.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: два отрицательных контроля — правило ловит лишнюю регистрацию и новый файл, принёсший регистрацию</name>

  <files>
tests/test_templates/test_components.py
  </files>

  <read_first>
- `tests/test_templates/test_components.py:4875-4910` —
  `test_control_negative_a_new_vendored_script_reddens_the_gate` ЦЕЛИКОМ. ⚠️ ФОРМА НАСЛЕДУЕТСЯ
  ДОСЛОВНО: подмена пишется во временный каталог, утверждений три — подмена приземлилась, правило её
  видит, БОЕВОЕ дерево подменой не тронуто. Третье утверждение — граница контроля, и без него
  контроль мог бы править предмет, который меряет.
- `tests/test_templates/test_htmx_markup_gates.py:319-340` — `_all_templates(directory)`. ⚠️ ЧИТАТЬ
  ДОСЛОВНО абзац о том, что каталог-параметр есть НЕСУЩЕЕ решение гейта: без него группа контроля
  невыразима, и зубы правила пришлось бы ЗАЯВЛЯТЬ.
- Пятое утверждение и его помощники ПОСЛЕ задачи 1 — в том виде, в каком задача 1 их оставила.
  ⚠️ ЧИТАТЬ ПЕРЕД ПРАВКОЙ, а не по памяти о плане: контроль, написанный по плану, а не по дереву,
  меряет плановое ожидание.
- `tests/test_planning/test_deferred_items_are_idempotent.py:311-360` — форма отрицательных
  контролей на СИНТЕТИКЕ в этом проекте (раздел, написанный дважды; файл без якоря). ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО: ожидания контроля выписываются ЛИТЕРАЛАМИ, а не вычисляются из проверяемого
  источника, иначе контроль подтверждает сам себя.
  </read_first>

  <behavior>
1. Копия каталога шаблонов, в которой у одного файла на одну регистрацию БОЛЬШЕ, краснит пятое
   утверждение, и в тексте отказа стои́т имя этого файла с обоими числами.
2. Та же копия БЕЗ лишней регистрации утверждение отпускает — то есть правило меряет предмет, а не
   краснеет на любую подмену.
3. НОВЫЙ файл шаблона, принёсший одну регистрацию, краснит утверждение и попадает в перечень
   ПОЯВИВШИХСЯ, а не растворяется в итоговом числе.
4. Боевой каталог `app/templates/` ни одним контролем не тронут: измеренный перечень настоящего
   дерева после прогона контролей равен объявленному.
  </behavior>

  <action>
**(1) КОНТРОЛЬ ЛИШНЕЙ РЕГИСТРАЦИИ.** Копия каталога шаблонов во временном каталоге; в копию
`includes/htmx_error_banner.html` дописывается ЕЩЁ ОДНА регистрация внутри его блока сценария.
Утверждений три по наследуемой форме: подмена приземлилась (измеренное число у этого файла на
единицу больше объявленного), правило видит расхождение и называет ФАЙЛ, боевое дерево не тронуто.
⚠️ ДОПИСЫВАЕТСЯ РЕГИСТРАЦИЯ НА ПРОИЗВОЛЬНОЕ ИМЯ СОБЫТИЯ, А НЕ КОПИЯ ТРЕТЬЕГО ОБРАБОТЧИКА: правило
обязано краснеть на РЕГИСТРАЦИЮ КАК ТАКОВУЮ, а не на одно знакомое имя события — иначе следующая
правка заведёт слушателя другого события, и правило промолчит.

**(2) КОНТРОЛЬ НОВОГО ФАЙЛА.** В копию каталога кладётся шаблон, которого в дереве нет, с блоком
сценария и одной регистрацией. Утверждения: файл попал в измеренный перечень НОВЫМ ключом, правило
красно, имя нового файла стои́т в перечне появившихся. ⚠️ ЭТОТ КОНТРОЛЬ НЕ ИЗБЫТОЧЕН ПРИ ПЕРВОМ:
первый доказывает, что правило видит движение ЧИСЛА у известного файла, второй — что оно видит
ПОЯВЛЕНИЕ файла. Правило, отбирающее только объявленные ключи, прошло бы первый контроль и
провалило бы предмет: следующая фаза положит сценарий в новый шаблон, а не в старый.

**(3) ГРАНИЦА КОНТРОЛЯ — ТРЕТЬИМ УТВЕРЖДЕНИЕМ В КАЖДОМ.** Измеренный перечень БОЕВОГО каталога
после подмены равен объявленному. Без этого утверждения контроль, протёкший за свою границу,
правил бы предмет, который меряет, — и оба контроля стали бы зелёными по построению.

**(4) ОЖИДАНИЯ ВЫПИСЫВАЮТСЯ ЛИТЕРАЛАМИ.** Ни одно ожидание контроля не вычисляется из проверяемого
источника: вычисленное ожидание сходится с проверяемым всегда, включая день, когда оба неверны.
  </action>

  <acceptance_criteria>
- Оба контроля зелены; имена приведены в сводке.
- Первый контроль дописывает регистрацию на ПРОИЗВОЛЬНОЕ имя события, а не копию третьего
  обработчика — проверяется чтением диффа.
- Каждый контроль несёт ТРИ утверждения, и третье есть граница контроля (боевой каталог не тронут).
- Ожидания контролей выписаны литералами, а не вычислены из проверяемого источника — проверяется
  чтением диффа.
- `git diff --numstat 9e6b859 -- app/templates app/static` после прогона контролей пуст (ревизия
  закреплена базой партии и без `..HEAD`, поэтому проверка видит правку и до коммита, и после него —
  `git status --porcelain` показал бы чистоту у зафиксированной правки).
- `git diff --numstat 9e6b859 -- app` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py -q -k "criterion_three or handler_registration or vendored_script"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо собранных правил МЕНЬШЕ четырёх (гейт, контроль вендоренного каталога и два новых контроля) — меньшее число означает, что имя одного из новых контролей не попало в отбор и его цвет не меряется ничем</fails_when>
    <automated>git diff --numstat 9e6b859 -- app/templates app/static</automated>
    <fails_when>вывод НЕПУСТ — контроль тронул боевое дерево вместо копии, и оба контроля стали зелёными по построению. ⚠️ РЕВИЗИЯ ЗАКРЕПЛЕНА (`9e6b859`) И БЕЗ `..HEAD` — сравнение с РАБОЧИМ ДЕРЕВОМ: `git status --porcelain` показал бы чистоту, если бы контроль тронул дерево И правка оказалась зафиксированной, то есть проходил бы вакуумно ровно в худшем случае</fails_when>
  </verify>

  <done>
Пятое утверждение снабжено двумя отрицательными контролями: правило ловит лишнюю регистрацию у
известного файла и появление файла, принёсшего регистрацию; у каждого контроля есть граница, и
боевое дерево ни одним из них не тронуто.
  </done>
</task>

<task type="auto">
  <name>Задача 3: предъявление среза целиком — область прогона названа причиной, а остаток назван без смягчения</name>

  <files>
tests/test_templates/test_components.py
  </files>

  <read_first>
- `.planning/WINDOWS.md`, запись 79 ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ДОСЛОВНО, И ЭТО НЕСУЩЕЕ ДЛЯ НАСТОЯЩЕЙ
  ЗАДАЧИ: полная суита НЕ ЗЕЛЕНА НАДЁЖНО. Правило
  `tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard`
  недетерминировано по времени суток (катящееся окно 24 ч против календарного дня читателя) и
  краснеет между 00:00 и ~05:00 UTC. Замер владельца: 02:03 UTC red, 05:10 UTC green.
- `.planning/phases/10-rychag-components-modal-html/10-51-PLAN.md`, задача 3, действие (5) — форма
  записи остатка ПЯТЬЮ пунктами без смягчения. ⚠️ ФОРМА НАСЛЕДУЕТСЯ.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:640-700` — раздел
  «Human Verification Required» целиком, все четыре пункта. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: остаток
  настоящей задачи ССЫЛАЕТСЯ на эти пункты поимённо, и ссылка обязана быть верной, а не
  правдоподобной.
- `.planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md:1-40` — шапка
  реестра запретов с числами восьмой партии и областью разрешения (`permit_scope:
  seventh-batch-only`). ⚠️ ЧИТАТЬ И НЕ ПРАВИТЬ: реестр вне объёма партии, и правка его была бы
  вердиктом вместо владельца.
  </read_first>

  <action>
**(1) ПРОГНАТЬ ОБЛАСТЬ, КОТОРУЮ ПАРТИЯ КАСАЕТСЯ, И НАЗВАТЬ ОБЛАСТЬ ПРИЧИНОЙ, А НЕ УДОБСТВОМ.**
Модули: `tests/test_templates/test_components.py` (правимый), `tests/test_pages/test_shell.py`
(владелец ввозимой константы — правило, читающее её, обязано остаться зелёным),
`tests/test_planning/` ЦЕЛИКОМ. ⚠️ ПОСЛЕДНЕЕ — НЕ ПЕРЕСТРАХОВКА: исполнители деселектят
планировочные правила, и дефекты планировочных записей видит только явный прогон.

**(2) ПОЛНЫЙ ПРОГОН СУИТЫ ИДЁТ С ПОИМЁННЫМ ИЗЪЯТИЕМ ПРАВИЛА ЗАПИСИ 79, И ИЗЪЯТИЕ НАЗВАНО
ПРИЧИНОЙ.** Причина: правило требует равенства двух чисел, равных лишь часть суток, и между 00:00
и ~05:00 UTC краснеет БЕЗ ЕДИНОЙ ПРАВКИ. Гейт, опирающийся на его зелень, объявлял бы настоящий
план неисполнимым по часам, а не по дереву. ⚠️ ИЗЪЯТИЕ ПОИМЁННОЕ, А НЕ ПО СЛОВУ: изъятие по
подстроке унесло бы вместе с ним соседние правила админки, и их красноту партия перестала бы
видеть. ⚠️ ПРЕДМЕТ ЗАПИСИ 79 ВНЕ ФАЗЫ 10 и отложен владельцем; настоящий план его НЕ ЧИНИТ и
чинить не предлагает.

**(3) ЗАПИСАТЬ В СВОДКУ ЗАМЕР СРЕЗА ЧИСЛАМИ.** Число шаблонов с блоком сценария, измеренный
перечень регистраций, оба состояния перехода цвета с полученным значением RED дословно, число
собранных правил отбора `criterion_three`, итоговые строки всех прогонов.

**(4) ЗАПИСАТЬ В СВОДКУ ОСТАТОК, И НИ ОДИН ПУНКТ НЕ СМЯГЧАТЬ.** Восемь пунктов, каждый со
ссылкой на запись журнала окон либо на находку ревизии:
  - **пункт `missing[0]` записи гэпа одиннадцатого круга** — РЕШЕНИЕ ВЛАДЕЛЬЦА по критерию 3 —
    настоящим планом НЕ ЗАКРЫТ и закрыт быть не мог: его закрывает план 10-53 остановом на
    владельце, и машина не вправе выбрать за него ни одну из двух ветвей;
  - **три человеческих пункта** раздела «Human Verification Required» (шаг 2.8, шаг 4.4, подпись
    человека на приёмке) — настоящим планом не сдвинуты ни на волос; их адресат — план 10-54 и
    человек, а `FORM-06` закрывается ПОДПИСЬЮ, а не прогоном;
  - **запись 75 журнала окон** (`open`) — перечень ПРОЧИХ путей, оставляющих сводку объявления
    устаревшей, не снимался: план 10-50 закрыл только фрагментную ветвь удаления расписания
    (`G-10-6`), и отсутствие перечня за отсутствие путей не выдаётся;
  - **запись 76 журнала окон** (`open`, `T-10-51-04`, disposition `accept`, владельца нет) — две
    заготовки, показанные одновременно, накрывают друг друга ТЕПЕРЬ ВСЕГДА; тождественна находке
    ревизии `IN-03` о совмещённом блоке величин в таблице стилей;
  - **запись 78 журнала окон** (`open`) — проза `app/templates/includes/htmx_error_banner.html:130-149`
    разошлась с деревом ДВУМЯ утверждениями (условность подъёма; переименованное правило
    `…_above_the_panel_while_it_is_open` → `…_above_the_panel`); настоящий план `app/templates/`
    не правит, и потому расхождение остаётся;
  - **запись 79 журнала окон** (`open`) — недетерминированность суиты по времени суток; предмет
    вне Фазы 10, отложен владельцем, изъят из полного прогона поимённо по действию (2);
  - **запись 74 журнала окон** (`open`, `T-10-49-05`, `accept`) — четвёртое окно видимости отказа:
    успешный ФОНОВЫЙ обмен гасит непрочитанный настоящий отказ; тождественна находке ревизии
    `IN-02`, и она же есть предмет ребра `concurrency`, названного во frontmatter допущением;
  - **пункт 4 «Human Verification Required»** — решение владельца по запретам фазы:
    `prohibitions_unresolved_total: 200` при сегодняшних 221 плюс запреты настоящей партии,
    `prohibitions_fully_enforced: 0`, прежнее разрешение ограничено `seventh-batch-only`; реестр
    `10-PROHIBITIONS-SUBJECT.md` настоящим планом НЕ ПРАВИТСЯ, и его отставание в сводке названо
    числом.
  - **находка ревизии `IN-01`** (`tests/test_templates/test_htmx_markup_gates.py`, инвентарь
    внеполосных блоков поднят числом, отказ печатает два числа вместо разницы множеств) — вне
    объёма партии; настоящий план её КЛАСС в своём правиле не повторяет, но самой находки НЕ
    ЗАКРЫВАЕТ.

**(5) НАЗВАТЬ В СВОДКЕ ЗАМЕР, ОПРОВЕРГАЮЩИЙ ОДНО ЧИСЛО ВХОДНОГО КОНТЕКСТА, А НЕ ПРИНЯТЬ ЕГО
МОЛЧА.** Артефакт `10-SECURITY.md` на HEAD несёт `status: waived`, `threats_open: 12`,
`waiver_scope: phase-10-only`, `waived_by: chubav`, `waived_on: 2026-09-10` — а не `threats_open: 0`.
Замер снят планировщиком 2026-09-12 чтением шапки. Настоящий план этого артефакта НЕ ПРАВИТ:
поле изъятия принадлежит владельцу и команде безопасности фазы, а не плану закрытия гэпов. Запись
делается потому, что молчаливое согласие с неверным числом есть та же болезнь, против которой
заводится пятое утверждение.
  </action>

  <acceptance_criteria>
- Три названных прогона области зелены; итоговые строки приведены числами.
- Полный прогон суиты выполнен С ПОИМЁННЫМ изъятием правила записи 79, и изъятие названо в сводке
  ПРИЧИНОЙ (недетерминированность по времени суток), а не удобством.
- Замер среза приведён числами: семь шаблонов с блоком сценария, перечень регистраций двумя
  записями, полученное значение RED дословно, оба состояния перехода цвета.
- Остаток назван ВОСЕМЬЮ пунктами, у каждого — ссылка на запись журнала окон либо на находку
  ревизии; ни один не назван закрытым, частично закрытым или «по существу закрытым».
- В сводке прямо сказано: `G-10-7` остаётся `status: failed`, `FORM-06` не отмечен выполненным,
  `10-UAT.md` настоящим планом не правлен и его `status` не двигался.
- Замер шапки `10-SECURITY.md` приведён в сводке дословно, и названо, что план её не правит.
- `git diff --numstat 9e6b859 -- app .planning/REQUIREMENTS.md` пуст (ревизия закреплена базой
  партии и без `..HEAD`).
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py tests/test_pages/test_shell.py tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — правило-владелец ввозимой константы либо планировочный гейт сломаны правкой</fails_when>
    <automated>uv run pytest tests/ -q --deselect tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed", либо в выводе есть строка "ERROR: not found" (изъятое имя разошлось с деревом, и изъятие перестало быть поимённым)</fails_when>
    <automated>git diff --numstat 9e6b859 -- app .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод НЕПУСТ — тронуто дерево приложения, реестр требований либо артефакт обхода, то есть план вышел за свой предмет и подошёл к самозаверению. ⚠️ РЕВИЗИЯ ЗАКРЕПЛЕНА (`9e6b859`, база партии) И БЕЗ `..HEAD` — сравнение с рабочим деревом; проверка без закреплённой ревизии была бы пуста для зафиксированной правки</fails_when>
  </verify>

  <done>
Область прогона названа причиной, а не удобством; полный прогон выполнен с поимённым изъятием
правила записи 79; замер среза приведён числами; остаток назван восемью пунктами без смягчения;
опровергнутое число входного контекста названо замером, а не принято молча.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| дерево шаблонов → объявленное число | между правкой, вносящей исполняемый код в шаблон, и утверждением фазы «новых строк JS не добавлено» до сих пор нет ни одного прибора |
| суита → критерий приёмки роадмапа | зелень прогона читается следующим кругом как исполнение критерия; гейт, не покрывающий предмета, обращает эту зелень в утверждение о том, что не мерялось |
| модуль правил шелла → модуль правил компонентов | число регистраций заготовок пересекает границу пакетов суиты, и вторая его копия разошлась бы с первой молча |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-52-01 | Repudiation | зелень `test_criterion_three_holds_by_the_numbers` на дереве, где критерий 3 нарушен | high | mitigate | пятое утверждение считает регистрации в телах блоков `<script>` всех шаблонов и объявляет их ПЕРЕЧНЕМ ФАЙЛОВ С ЧИСЛАМИ; RED снят на объявлении базы фазы с печатью полученного значения, GREEN — на ввозе единственного источника |
| T-10-52-02 | Tampering | вторая копия числа регистраций заготовок в модуле правил компонентов | high | mitigate | значение приходит ввозом `FAILURE_BANNER_HANDLERS_MEASURED`; литерала в модуле нет, и это принуждается чтением диффа в акцептансе задачи 1 |
| T-10-52-03 | Spoofing | правило, зелёное по построению: сломанный обход тел блоков даёт пустой перечень, а пустое сходится с объявленным лишь в день, когда объявление тоже опустошат | high | mitigate | два отрицательных контроля на копии каталога (лишняя регистрация у известного файла; новый файл с регистрацией), у каждого третьим утверждением — граница контроля |
| T-10-52-04 | Information Disclosure | отказ, печатающий два итога вместо разницы множеств: читатель узнаёт, что число разошлось, и не узнаёт, где искать | medium | mitigate | текст отказа несёт файл и ОБА числа при нём плюс перечни появившихся и исчезнувших файлов; повторение класса находки `IN-03` запрещено прямо |
| T-10-52-05 | Denial of Service | правило, считающее прозу: комментарий-обоснование внутри блока сценария подменяет собой ушедшую регистрацию и оставляет число верным | medium | mitigate | комментарии Jinja и HTML вырезаются ввозимым помощником, сценарные — заведённым здесь; равенство перечней со вырезанием и без него на сегодняшнем дереве предъявлено замером |
| T-10-52-06 | Elevation of Privilege | план, выбравший за владельца одну из двух ветвей решения по критерию 3 | high | mitigate | пятое утверждение объявляет ЧИСЛО ДЕРЕВА и не несёт основания; основание вносит план 10-53 ПОСЛЕ останова на владельце, и докстринг говорит это прямо |
| T-10-52-07 | Repudiation | гейт, опирающийся на зелень полной суиты, недетерминированной по времени суток (запись 79) | medium | mitigate | область прогона сужена до касаемых модулей; полный прогон идёт с ПОИМЁННЫМ изъятием правила записи 79, и изъятие названо причиной |
| T-10-52-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера; протокол законности пакетов не применяется |
</threat_model>

<verification>
1. `uv run pytest tests/test_templates/test_components.py -q` — зелено целиком.
2. `uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"` — РОВНО ОДНО
   собранное правило (замер планировщика 2026-09-12: отбор `criterion_three or vendored_script` даёт
   2/88, из них `criterion_three` — одно); имя гейта не переименовано.
3. `uv run pytest tests/test_pages/test_shell.py tests/test_planning/ -q` — зелено: владелец
   ввозимой константы и планировочные гейты не тронуты.
4. `uv run pytest tests/ -q --deselect tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard`
   — зелено; изъятие названо причиной записи 79.
5. Переход цвета пятого утверждения измерен ИСПОЛНЕНИЕМ правила и приведён обоими состояниями с
   полученным значением RED дословно.
6. `git diff --numstat 9e6b859 -- app .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-UAT.md`
   пуст на всех трёх задачах.
</verification>

<success_criteria>
- Регистрация обработчика, добавленная в тело блока `<script>` любого шаблона `app/templates/**`,
  краснит ПРОГОН и называет файл с обоими числами.
- Правило различает дерево до коммита `4e24d61` и дерево после него — то есть гейт, существовавший
  бы тогда, правку девятой партии увидел бы; это ИЗМЕРЕНО, а не заявлено.
- Число регистраций заготовок не получило второй копии: значение приходит ввозом единственного
  источника.
- Оба отрицательных контроля зелены, и каждый несёт границу контроля; боевое дерево ни одним из них
  не тронуто.
- Четыре действующих утверждения не ослаблены, имя гейта не изменено, знаменатель их сообщений
  приведён к пяти.
- Ни одна из двух ветвей решения владельца по критерию 3 планом не предположена; основание числа
  вносит план 10-53, и докстринг говорит это прямо.
- Остаток назван восемью пунктами без смягчения; `G-10-7` остаётся `status: failed`, `FORM-06` не
  отмечен выполненным, `10-UAT.md` не правлен.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-52-SUMMARY.md` when done
</output>
