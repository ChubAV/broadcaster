---
phase: 10-rychag-components-modal-html
plan: 11
type: execute
wave: 2
depends_on: ["10-08"]
gap_closure: true
files_modified:
  - tests/test_pages/test_confirm_delete_transport.py
  - .planning/ROADMAP.md
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер (`estimate-calibration`: sample_count 0,
  # factor 1, min_samples 3): tokens и raw_tokens совпадают, confidence: low
  # ВЫВЕДЕН ИЗ ЧИСЛА ЗАМЕРОВ, а не из самооценки планировщика.
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ДЕВЯТЫЙ МАРШРУТ ПОДТВЕРЖДЕНИЯ ПРОХОДИТСЯ ОБХОДОМ, а не только числится в инвентаре: удаление группы аккаунта — маршрут за панелью подтверждения, переведённый Фазой 9, — до сих пор не проходил ни одной половины пары транспортов в модуле, который объявляет себя местом, где такой маршрут краснеет"
    - "Оба числа перечня ПОСТАВЛЕНЫ ПРОГОНОМ покрасневшего правила, а не сложением в уме, и летопись каждого продолжена своей формулировкой: число МАРШРУТОВ и число СЛУЧАЕВ ОБХОДА считают РАЗНЫЕ множества, и привести их к согласию правкой одного нельзя"
    - "Допущение о ЕДИНСТВЕННОСТИ фрагментного маршрута снято ЯВНО, а не обойдено: помощник выбора фрагментного маршрута утверждает единственность СВОИМ отказом и прямо говорит, что появление второго означает принятое решение о его форме ответа и НЕ ПРИНЯТОЕ — о неотличимости его повтора. Решение о повторе девятого маршрута ПРИНИМАЕТСЯ здесь и записывается"
    - "Свойство неотличимости повтора девятого маршрута ЛИБО утверждается правилом, ЛИБО вносится в перечень изъятий С ОСНОВАНИЕМ НА ЗАПИСЬ и двигает объявленное число изъятий — молчаливого третьего пути нет"
    - "Документный дрейф роадмапа закрыт ЗАМЕРОМ: строка прогресса вехи и раздел фазы говорят ОДНО И ТО ЖЕ число планов, и число получено СЧЁТОМ отметок в разделе фазы, а не памятью"
    - "Летопись расхождения записана, а не затёрта: прежняя строка прогресса называла число, верное для состава фазы ДО партии закрытия гейпов; правится не она, а числа, которые она пережила — та же формулировка, которой фаза уже записала летопись «16 → 18»"
    - statement: "Правило машинно читаемого прогресса (`test_the_machine_readable_progress_is_derived_from_the_roadmap`, окно 9) этим планом НЕ ЧИНИТСЯ и в мандат не входит: его предмет — способ вывода прогресса, а не согласие двух строк роадмапа. Его цвет ДО и ПОСЛЕ правки снимается и записывается честно, но критерием приёмки не является ни в одну сторону"
      verification: backstop
  artifacts:
    - "tests/test_pages/test_confirm_delete_transport.py — расстановки девятого маршрута (успех фрагментной ветки, опустевшая выдача, несуществующий идентификатор, чужой владелец)"
    - "tests/test_pages/test_confirm_delete_transport.py — запись девятого маршрута в перечне подтверждения с его ожидаемой формой ответа"
    - "tests/test_pages/test_confirm_delete_transport.py — оба объявленных числа со своими летописями, продолженными этим планом"
    - "tests/test_pages/test_confirm_delete_transport.py — принятое решение о неотличимости повтора девятого маршрута: правило либо запись перечня изъятий с основанием"
    - ".planning/ROADMAP.md — строка прогресса вехи, согласованная с разделом фазы, и летопись расхождения"
  key_links:
    - "перечень маршрутов подтверждения → продукт: единственное, чем маршрут за панелью не может остаться непройденным обходом; ломается — модуль объявляет себя местом, где девятый маршрут краснеет, и молчит о нём"
    - "помощник выбора фрагментного маршрута → решение о повторе: единственное, чем второй фрагментный маршрут не проходит мимо решения о неотличимости своего повтора; ломается — свойство, названное несущим для одного маршрута, для второго не принимается вовсе и не замечается"
    - "строка прогресса вехи → раздел фазы: единственное, чем читатель, поймавший число через фазу, не получает двух разных ответов; ломается — документный дрейф того же класса, что запись 26, и он уже один раз попал в отчёт верификации"
  prohibitions:
    - statement: "два числа перечня НЕ приводятся к согласию правкой одного: первое перестало бы отвечать за «сколько у продукта маршрутов подтверждения», второе — за «сколько различимых ответов эти маршруты дают», и это записано у самих констант"
      status: flagged-unverified
      verification: none
    - statement: "изъятие из правила неотличимости повтора НЕ заводится без основания НА ЗАПИСЬ: перечень без обоснований превращается в список того, до чего не дошли руки (форма перечня изъятий, принятая планом 10-06)"
      status: flagged-unverified
      verification: none
    - statement: "обработчик удаления группы аккаунта НЕ правится этим планом ни на символ: он переведён и обойдён руками Фазой 9, а предмет здесь — покрытие его ОБХОДОМ, а не его поведение"
      status: flagged-unverified
      verification: none
    - statement: "прежняя строка прогресса вехи не затирается молча: расхождение записывается летописью с названным источником, потому что прежнее число было верным для состава фазы, который оно пережило"
      status: flagged-unverified
      verification: none
    - statement: "предсуществующие красные `test_the_overview_error_number_matches_the_users_own_dashboard` (окно 27) и `test_the_machine_readable_progress_is_derived_from_the_roadmap` (окно 9) не чинятся и не втягиваются; критерии приёмки от них не зависят"
      status: flagged-unverified
      verification: none
---

<objective>
Закрываются два пункта `ℹ️ Info` таблицы Anti-Patterns отчёта верификации, оба — документного и
инвентарного класса: девятый маршрут подтверждения (удаление группы аккаунта, Фаза 9) не проходит
обход транспортов, объявляющий себя местом, где такой маршрут краснеет; и строка прогресса вехи в
роадмапе расходится с разделом фазы по числу планов.

Purpose: модуль обхода написан ОДНИМ файлом ровно затем, чтобы «маршрут, добавленный за панелью
подтверждения, не уронил бы ни одной из восьми функций, потому что своей функции у него ещё нет».
Маршрут Фазы 9 есть ровно такой случай, и он в перечне отсутствует — то есть заявленное свойство
модуля на нём не работает. Документный дрейф роадмапа — тот же класс, что запись 26: он уже попал в
отчёт верификации и попадёт в следующий, если его не закрыть замером.

Output: девятый маршрут в перечне с расстановками и обеими половинами пары; оба числа, поставленные
прогоном, с продолженными летописями; принятое и записанное решение о неотличимости его повтора;
согласованная строка прогресса с летописью расхождения.

⚠️ ЭТОТ ПЛАН — ЗАКРЫТИЕ ГЕЙПОВ (`gap_closure: true`), ВОЛНА 2 НОВОЙ ПАРТИИ. Зависимость от плана
10-08 — ПО ФАЙЛУ: оба правят `tests/test_pages/test_confirm_delete_transport.py`, и одновременная
правка одного файла двумя планами одной волны запрещена. Планы 10-01…10-07 исполнены и
закоммичены и не правятся.

⚠️ `10-VALIDATION.md` У ФАЗЫ НЕТ ПО ЯВНОМУ ВЫБОРУ ОПЕРАТОРА: режим `--gaps` разведку не запускает, и
оператор выбрал продолжить без неё.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-06-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-08-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Создаётся этим планом (проход source-grounding обязан считать новым):

- `_arrange_account_group_delete` — расстановка успеха фрагментной ветки девятого маршрута,
  `tests/test_pages/test_confirm_delete_transport.py`
- `_arrange_account_group_last` — расстановка ветки опустевшей выдачи, там же
- `_arrange_missing_account_group` — расстановка несуществующего идентификатора, там же
- `_arrange_foreign_account_group` — расстановка чужого владельца, там же
- запись девятого маршрута в `CONFIRMED_DELETE_ROUTES`, там же
- решение о неотличимости повтора девятого маршрута: ЛИБО
  `test_a_repeated_confirmed_delete_on_the_second_fragment_route_answers_the_same_shape`, ЛИБО
  запись в `REPEAT_EXEMPT_ROUTES` с основанием и сдвинутым `REPEAT_EXEMPT_ROUTES_DECLARED`
- летопись расхождения строки прогресса в `.planning/ROADMAP.md`

Существует до плана и только ПРАВИТСЯ (новым не является): `CONFIRMED_DELETE_ROUTES`,
`CONFIRMED_DELETE_ROUTES_DECLARED`, `CONFIRMED_DELETE_OUTCOME_CASES_DECLARED`,
`_the_fragment_route`, `_expected_oob_ids`, `_addressed_schedule_id`, `REPEAT_EXEMPT_ROUTES`,
`REPEAT_EXEMPT_ROUTES_DECLARED`, `_repeat_matches_the_first`, `_repeat_asserted_routes`,
`_Route`, `_Outcome`, `_Arranged`, `LOCATION`, `FRAGMENT`.
</artifacts_this_phase_produces>

<probe_edges>
## Рёбра зонда покрытия (`edge-probe.cjs` по тексту FORM-06) — ни одно не роняется молча

1. **`idempotency`** — РАЗРЕШЕНО планом **10-06**. ⚠️ ЭТОТ ПЛАН ЕГО РАСШИРЯЕТ: девятый маршрут
   получает СВОЁ решение о неотличимости повтора — правилом либо изъятием с основанием, — потому
   что перечень изъятий плана 10-06 его не содержал и содержать не мог.
2. **`concurrency`** — РАЗРЕШЕНО планом **10-07** (гейт ухода панели по признаку отправки). Этот
   план ни разметки, ни обработчиков не трогает и ребра не касается.

Равенство без молчаливых пропусков: поднято зондом 2 — внесено допущениями/расширениями 2.
</probe_edges>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: девятый маршрут подтверждения встаёт в обход — расстановки, запись перечня и обе половины пары</name>

  <files>
tests/test_pages/test_confirm_delete_transport.py
  </files>

  <read_first>
- `tests/test_pages/test_confirm_delete_transport.py:1-55` — модульный докстринг ЦЕЛИКОМ: две оси
  обхода, два числа, запрет сличать их между собой, основание «одним модулем, а не восемью
  функциями» и объяснение, почему половина htmx идёт с прослеживанием перенаправлений.
- `:111-185` — `_Arranged`, `_Outcome`, `_Route` и константы ожидаемой формы ответа. Новая запись
  собирается ИМИ; своих структур не заводится.
- `:339-400` — образцы расстановок соседних маршрутов: успех, несуществующий идентификатор, чужой
  владелец. Форма наследуется.
- `:643-656` — `_arrange_editor_schedule_delete`: единственная сегодня расстановка ФРАГМЕНТНОГО
  маршрута, включая приём «второе расписание оставляет экран непустым, иначе маршрут ушёл бы в
  ветку перехода». Девятый маршрут требует того же приёма по той же причине.
- `:666-700` и `:890-935` — начало перечня и ОБА объявленных числа со своими летописями. ⚠️ ЧИТАТЬ
  ЛЕТОПИСИ ДОСЛОВНО: они пишутся по форме «было → стало, фаза, план, задача, прибавка ПОИМЁННО, и
  число ПОСТАВЛЕНО ПРОГОНОМ покрасневшего правила».
- `:958-1020` — оба правила числа и правило «каждый случай обхода пройден».
- `:1022-1107` — `test_every_confirmed_delete_route_answers_both_transports`: как утверждаются обе
  половины пары, включая посимвольное сличение адреса и отсутствие целого документа.
- `tests/test_pages/test_account_groups.py:88-133` — `_seed_account(db, type_, user_id)` и
  `_seed_group(db, account, name, user_id, is_active)`. ⚠️ ИМЯ `_seed_account` УЖЕ ЗАНЯТО В МОДУЛЕ
  ОБХОДА (там это посев аккаунта мессенджера с другой сигнатурой) — ввоз обязан идти под
  псевдонимом, иначе второе имя молча перекроет первое и половина расстановок поедет не тем посевом.
- `app/pages/account_groups.py`, обработчик удаления группы ЦЕЛИКОМ — форма ответа: ветка
  опустевшей выдачи отвечает переходом, остальные — фрагментом; адрес приземления собирается с
  сохранённым фильтром поиска.
- `app/templates/account_groups/partials/delete_response.html` — внеполосные узлы ответа: пара
  снятия строки и панели плюс побочные области. Их идентификаторы нужны утверждению фрагментной
  половины.
  </read_first>

  <precondition>
План 10-08 приземлён: `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly`
зелено, и число прошедших названо в сводке 10-08 (база ДО партии — `41 passed`, измерено
планировщиком). Сегодняшнее состояние перечня: `CONFIRMED_DELETE_ROUTES_DECLARED` равно `8`,
`CONFIRMED_DELETE_OUTCOME_CASES_DECLARED` равно `18`, фрагментный маршрут в перечне ровно один.
  </precondition>

  <behavior>
СРЕЗ ВЕРТИКАЛЬНЫЙ: расстановка данных → настоящий POST по обоим транспортам → утверждение ОБЕИХ
половин пары для ОДНОГО нового маршрута, и только затем — числа и остальные исходы.

1. Запись девятого маршрута появляется в перечне, и ДВА правила числа КРАСНЕЮТ, называя фактическое
   и объявленное. Числа ставятся ИХ ВЫВОДОМ, дословно, а не сложением в уме.
2. `test_every_confirmed_delete_route_answers_both_transports` проходит новый маршрут БЕЗ ПРАВОК:
   ветвление по полю записи уже есть, и если новая запись потребовала бы правки обхода — это
   означало бы, что её форма ответа в структуру записи не укладывается, и ЭТО ОСТАНОВ с записью
   окном, а не расширение структуры на ходу.
3. Отдельные утверждения обхода, в счёт исходов НЕ входящие, работают на новом маршруте так же, как
   на восьми прежних: несуществующий идентификатор отвечает как успех, чужая строка не тронута и
   отвечает как успех, ветка «нет сессии» отвечает обеими половинами.
4. АНТИВАКУУМ: расстановка успеха оставляет выдачу НЕПУСТОЙ (иначе маршрут уходит в ветку перехода,
   и «фрагментная половина» проверяла бы переход), а расстановка опустевшей выдачи — наоборот. Обе
   утверждают своё состояние ЗАПРОСОМ К БАЗЕ до отправки, а не выводят его из ответа.
  </behavior>

  <action>
**1. Расстановки.** Четыре помощника по образцу соседних: успех фрагментной ветки (аккаунт и НЕ
МЕНЕЕ ДВУХ групп, адресуется одна), опустевшая выдача (аккаунт и РОВНО ОДНА группа),
несуществующий идентификатор группы, чужой владелец. Владелец берётся действующим помощником
текущей личности модуля обхода и передаётся посеву ЯВНО параметром владельца — посев Фазы 9
разрешает владельца сам, и молчаливое расхождение двух разрешений дало бы строки не тому
пользователю.

Ввоз помощников посева Фазы 9 идёт ПОД ПСЕВДОНИМАМИ (имя посева аккаунта в модуле обхода занято) и
сопровождается комментарием о причине псевдонима — ровно по форме уже стоящего в файле комментария
о ввозе двойников и посевов.

Тело формы расстановки несёт признак фильтра поиска в том виде, в каком его шлёт разметка строки
группы: контекст экрана приходит скрытым полем слота панели (D-02), и расстановка, отправившая
пустое тело, проверяла бы не тот путь, которым ходит продукт.

**2. Запись перечня.** Ключ записи — путь модуля и имя обработчика через двойное двоеточие, как у
восьми прежних. Форма ответа на транспорте htmx — фрагментная. Исходы: успех фрагментной ветки и
опустевшая выдача (адрес приземления — экран групп аккаунта с сохранённым фильтром). Отдельные
поля несуществующего идентификатора, чужого владельца и адреса ветки без сессии заполняются, а не
опускаются: маршрут, вошедший в перечень наполовину, объявляет себя пройденным там, где не пройден.

**3. Оба числа.** Ставятся ПРОГОНОМ покрасневших правил; вывод каждого приводится дословно. Летопись
каждого числа продолжается СВОЕЙ формулировкой — «маршруты подтверждения» у первого, «случаи
обхода, то есть пары маршрут × исход» у второго, — и называет прибавку ПОИМЁННО. ⚠️ Две летописи с
одинаковой формулировкой были бы приглашением следующей фазе свести числа в одно; это запрещено
прямо у самих констант.

Летопись первого числа обязана назвать и то, ПОЧЕМУ маршрут появляется только сейчас: он переведён
Фазой 9 и обойдён её собственным ручным обходом, а в перечень фазы, объявившей своими ВОСЕМЬ
маршрутов подтверждения, не попал — то есть прибавка не есть новая работа продукта, она есть
закрытие унаследованной дыры обхода.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "number_of_confirmed_delete_routes or outcome_case or answers_both_transports"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо в выводе отсутствует случай с именем обработчика удаления группы аккаунта</fails_when>
  </verify>

  <acceptance_criteria>
- **ОБА ЧИСЛА ПОСТАВЛЕНЫ ПРОГОНОМ.** В сводке приведены ДОСЛОВНО оба вывода покрасневших правил
  («маршрутов … а объявлено 8» и «случаев обхода … а объявлено 18») и только затем — записанные
  значения. Число, «посчитанное в уме», к слиянию не принимается.
- В сводке приведён перечень идентификаторов случаев обхода нового маршрута (вывод
  `--collect-only -q` по фильтру имени обработчика) — видно, сколько случаев он дал и какие.
- Обход обоих транспортов НЕ ПРАВИЛСЯ ради новой записи:
  `git diff -- tests/test_pages/test_confirm_delete_transport.py | grep -c '^[-+].*def test_every_confirmed_delete_route_answers_both_transports'`
  → `0`. Если правка потребовалась — это ОСТАНОВ и запись окном, а не расширение структуры на ходу.
- Антивакуум расстановок показан: в сводке приведены числа групп в выдаче ДО отправки для обеих
  расстановок (успех — не менее двух, опустевшая выдача — ровно одна).
- Полный прогон файла зелен, число прошедших названо и сопоставлено с числом после плана 10-08;
  прибавка объяснена числом заведённых случаев.
- Обработчик Фазы 9 не тронут: `git diff --name-only -- app/pages/account_groups.py` — пусто.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Девятый маршрут подтверждения проходит обход обеими половинами пары и всеми отдельными
утверждениями; оба числа поставлены прогоном и несут продолженные летописи со своими
формулировками; структура записи маршрута выдержала новую форму ответа без правки обхода.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: единственность фрагментного маршрута перестаёт быть допущением — решение о неотличимости повтора девятого маршрута ПРИНИМАЕТСЯ и записывается</name>

  <files>
tests/test_pages/test_confirm_delete_transport.py
  </files>

  <read_first>
- `tests/test_pages/test_confirm_delete_transport.py:1253-1270` — `_the_fragment_route` ЦЕЛИКОМ.
  ⚠️ ЕГО ОТКАЗ — ПРЕДМЕТ ЭТОЙ ЗАДАЧИ дословно: «появление второго означает, что решение о его форме
  ответа принято, а о неотличимости его повтора — нет». После задачи 1 фрагментных маршрутов ДВА, и
  помощник обязан покраснеть — это ожидаемо и это и есть красный, с которого задача начинается.
- `:1305-1410` — правило неотличимости повтора на фрагментном маршруте ЦЕЛИКОМ, включая оба
  антивакуума (строка есть ДО, строки нет ПОСЛЕ) и сличение множеств внеполосных узлов; и
  отрицательный контроль сдвинутого идентификатора.
- `:1543-1600` — `REPEAT_EXEMPT_ROUTES` и его объявленное число: форма записи изъятия
  (ключ маршрута → основание НА ЗАПИСЬ) и правило числа. ⚠️ Основания выведены ЧТЕНИЕМ
  обработчиков, а не угадыванием по именам — то же требование действует и здесь.
- `:1638-1800` — `_landing_answer`, `_repeat_matches_the_first`, `_repeat_asserted_routes` и
  параметризованное правило повтора на маршрутах перехода.
- `app/pages/account_groups.py`, обработчик удаления группы — ЧИТАТЬ, чтобы решение о повторе было
  ВЫВЕДЕНО, а не угадано: есть ли у него запасной источник контекста экрана, что происходит на
  повторном запросе, когда строки уже нет, и совпадает ли форма ответа с первой.
- `10-06-SUMMARY.md` — как принималось решение по каждому из восьми маршрутов и как записывались
  четыре изъятия.
  </read_first>

  <precondition>
Задача 1 приземлена: девятый маршрут в перечне, оба числа сведены, полный прогон файла зелен ЗА
ИСКЛЮЧЕНИЕМ правил, опирающихся на `_the_fragment_route`, — они краснеют его собственным отказом
про «второй фрагментный маршрут», и это ожидаемый красный, с которого начинается эта задача. Если
после задачи 1 он НЕ покраснел — помощник перестал утверждать единственность, и это ОСТАНОВ.
  </precondition>

  <behavior>
1. Помощник выбора фрагментного маршрута перестаёт молча означать «единственный»: он выбирает
   маршрут ПО КЛЮЧУ ЗАПИСИ (маршрут удаления расписания — предмет правила, написанного планом
   10-06), а его прежнее утверждение о единственности ЗАМЕНЯЕТСЯ утверждением о том, что КАЖДЫЙ
   фрагментный маршрут перечня имеет ПРИНЯТОЕ решение о неотличимости своего повтора: либо своё
   правило, либо запись изъятия с основанием. Отказ называет маршруты, у которых решения нет.
2. Решение по девятому маршруту ВЫВОДИТСЯ ЧТЕНИЕМ обработчика и принимается ОДНИМ ИЗ ДВУХ:
   - правило повтора по образцу правила плана 10-06 (два запроса подряд, антивакуумы до и после,
     сличение множеств внеполосных узлов и формы ответа), ЕСЛИ обработчик отвечает на повтор той же
     формой;
   - либо запись перечня изъятий С ОСНОВАНИЕМ НА ЗАПИСЬ и сдвинутым объявленным числом изъятий,
     ЕСЛИ форма ответа на повтор отличается по причине, названной в обработчике.
   ⚠️ ТРЕТЬЕГО ПУТИ НЕТ: маршрут без решения — ровно то состояние, которое отказ помощника и
   называет.
3. Если выбран путь правила — оно несёт СВОЙ отрицательный контроль по образцу контроля сдвинутого
   идентификатора: правило повтора без контроля заимствует зубы у соседнего.
4. Если выбран путь изъятия — основание обязано называть, ЧЕМ ответ на повтор отличается и ПОЧЕМУ
   это не есть нарушение неотличимости, а не «сюда не дошли руки».
  </behavior>

  <action>
**1. Правка помощника.** Выбор по ключу записи, а не по полю формы ответа; прежнее основание
единственности НЕ вычёркивается, а переписывается в утверждение о принятом решении: оно было верно
в момент, когда фрагментный маршрут был один, и остаётся верным по СУТИ (второй фрагментный маршрут
обязан принести своё решение о повторе), меняясь только механизмом.

**2. Правило «каждый фрагментный маршрут имеет принятое решение о повторе».** Множество фрагментных
маршрутов перечня сличается с объединением множества маршрутов, у которых есть своё правило
повтора, и множества ключей перечня изъятий. Разница в любую сторону — отказ, называющий ключи
поимённо. АНТИВАКУУМ: множество фрагментных маршрутов НЕ ПУСТО, и это утверждается отдельно.

**3. Решение по девятому маршруту.** Принимается ЧТЕНИЕМ обработчика Фазы 9, а не по аналогии с
маршрутом расписания: у них разные источники контекста экрана и разные побочные области ответа.
Выбранный путь и ОСНОВАНИЕ выбора записываются в сводке дословно, вместе с выдержкой из
обработчика, на которой решение основано.

**4. Число изъятий.** Если выбран путь изъятия — объявленное число двигается ПРОГОНОМ покрасневшего
правила, и его летопись продолжается со ссылкой на этот план и на маршрут поимённо.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "repeat or repeated or sameness or fragment_route"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке</fails_when>
  </verify>

  <acceptance_criteria>
- **ОЖИДАЕМЫЙ КРАСНЫЙ НАБЛЮДЁН И ПРИВЕДЁН.** В сводке — вывод отказа помощника выбора фрагментного
  маршрута на состоянии после задачи 1 (два фрагментных маршрута), дословно.
- Решение по девятому маршруту НАЗВАНО и ОБОСНОВАНО: в сводке приведена выдержка из обработчика
  Фазы 9, на которой оно основано, и назван выбранный путь (правило либо изъятие).
- Если выбран путь ПРАВИЛА: приведён его вердикт (формы ответа первого и повторного запросов,
  множества внеполосных узлов) и вердикт его отрицательного контроля.
- Если выбран путь ИЗЪЯТИЯ: приведена запись целиком с основанием, и объявленное число изъятий
  сдвинуто ПРОГОНОМ покрасневшего правила — его вывод приведён дословно.
- Правило «каждый фрагментный маршрут имеет принятое решение» зелено, и в сводке приведено
  множество фрагментных маршрутов и множество маршрутов с решением — видно, что они совпали, и что
  ни одно не пусто.
- Полный прогон файла зелен, число прошедших названо и сопоставлено с числом после задачи 1.
- `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly` — зелено (правила
  Фазы 9 на том же маршруте не сдвинуты).
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Единственность фрагментного маршрута больше не подразумевается: каждый фрагментный маршрут перечня
имеет ПРИНЯТОЕ решение о неотличимости своего повтора, решение по девятому выведено чтением
обработчика и записано, а правило, стерегущее это свойство, не пусто и краснеет на маршруте без
решения.
  </done>
</task>

<task type="execute">
  <name>Задача 3: строка прогресса вехи приводится в согласие с разделом фазы ЗАМЕРОМ, и расхождение записывается летописью</name>

  <files>
.planning/ROADMAP.md
  </files>

  <read_first>
- `.planning/ROADMAP.md:459-472` — таблица прогресса вехи ЦЕЛИКОМ, включая строку Фазы 10.
- `.planning/ROADMAP.md:267-308` — раздел Фазы 10 ЦЕЛИКОМ: цель, критерии, летопись «16 → 18»,
  строка состава планов и перечень планов с отметками по волнам. ⚠️ ЧИСЛО БЕРЁТСЯ ОТСЮДА СЧЁТОМ, а
  не из памяти и не из строки состава.
- `.planning/ROADMAP.md:279` — летопись «16 → 18» дословно: ФОРМА ЗАПИСИ, которую наследует
  летопись этой задачи («прежнее значение не было ошибкой — оно устарело; правится не оно, а числа,
  которые оно пережило», с ИСТОЧНИКОМ РАСХОЖДЕНИЯ ПОИМЁННО).
- `10-VERIFICATION.md`, строка таблицы Anti-Patterns о строке прогресса — формулировка предмета.
- `.planning/WINDOWS.md`, запись 26 — открытая дыра документного дрейфа того же класса; читать,
  чтобы НЕ ОБЕЩАТЬ её закрытия: предмет записи 26 шире одной строки таблицы.
- `.planning/WINDOWS.md`, запись 9 — предсуществующий красный правила машинно читаемого прогресса.
  Читать, чтобы знать границу: его предмет — СПОСОБ ВЫВОДА прогресса, а не согласие двух строк.
  </read_first>

  <action>
**1. Замер.** Число исполненных планов фазы берётся СЧЁТОМ отметок в разделе фазы, а общее число
планов — счётом строк перечня. Оба счёта делаются командой, а не глазами, и их вывод приводится в
сводке: строка прогресса, поставленная по памяти, есть тот же дрейф под новой датой.

**2. Правка строки прогресса.** Строка таблицы вехи для Фазы 10 приводится в согласие с замером.
Правка делается ТОЧЕЧНОЙ заменой этой строки, а не переписыванием файла: роадмап несёт разделы всех
девяти фаз вехи, и запись файла целиком снесла бы всё, что лежит вне окна правки.

**3. Летопись расхождения.** Пишется рядом с разделом фазы по форме летописи «16 → 18»: прежнее
значение НЕ БЫЛО ОШИБКОЙ — оно было верным для состава фазы ДО партии закрытия гейпов; источник
расхождения назван поимённо (партия `gap_closure` планов 10-05…10-07 прибавила три плана к
четырём базовым, а партия 10-08…10-11 — ещё четыре); новое значение получено СЧЁТОМ.

⚠️ ЗАКРЫТИЯ ЗАПИСИ 26 НЕ ОБЕЩАТЬ: её предмет — документный дрейф КЛАССА, а не эта строка. Задача
закрывает пункт `ℹ️ Info` отчёта верификации, и говорит об этом ровно так.
⚠️ Раздел фазы, критерии успеха, летопись «16 → 18» и перечни планов НЕ ПРАВЯТСЯ этой задачей —
кроме дописанной летописи расхождения.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib,re,sys; s=pathlib.Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); sec=s[s.index('### Phase 10:'):s.index('### Phase 11:')]; done=len(re.findall(r'^- \[x\] 10-', sec, re.M)); total=len(re.findall(r'^- \[[ x]\] 10-', sec, re.M)); row=re.search(r'^\| 10\. [^|]*\| v2\.1 \| (\d+)/(\d+) \|', s, re.M); sys.exit('строки прогресса Фазы 10 не найдено' if not row else (0 if (int(row.group(1)),int(row.group(2)))==(done,total) else f'строка прогресса {row.group(1)}/{row.group(2)} расходится со счётом по разделу фазы {done}/{total}'))"</automated>
    <fails_when>ненулевой код возврата, либо в выводе строка «расходится со счётом по разделу фазы», либо «строки прогресса Фазы 10 не найдено»</fails_when>
  </verify>

  <acceptance_criteria>
- Вывод команды-замера приведён в сводке ДО и ПОСЛЕ правки: видно прежнее значение строки, счёт по
  разделу фазы и новое значение.
- Правка ТОЧЕЧНАЯ: `git diff --stat -- .planning/ROADMAP.md` показывает изменение в пределах строки
  прогресса и дописанной летописи; число изменённых строк приведено в сводке.
- Летопись расхождения приведена в сводке дословно и содержит ИСТОЧНИК расхождения поимённо
  (партии закрытия гейпов) и утверждение о том, что прежнее значение не было ошибкой.
- Разделы прочих фаз не тронуты:
  `uv run python -c "import pathlib,sys; s=pathlib.Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); need=['### Phase 11:','### Phase 12:','### Phase 13:','### Phase 14:','### Phase 15:']; miss=[n for n in need if n not in s]; sys.exit('роадмап потерял разделы: '+repr(miss) if miss else 0)"`
  — код возврата `0`.
- Цвет правила машинно читаемого прогресса (окно 9) снят ДО и ПОСЛЕ правки и записан честно:
  `uv run pytest tests/test_planning/ -q -p no:randomly -k "machine_readable_progress"`. ⚠️ Его цвет
  критерием приёмки НЕ ЯВЛЯЕТСЯ ни в одну сторону, и правило этим планом не чинится; изменение
  цвета (в любую сторону) записывается в сводку строкой.
- `uv run pytest tests/ -q -p no:randomly` — зелено за вычетом ДВУХ известных предсуществующих
  красных, названных поимённо (окна 27 и 9).
  </acceptance_criteria>

  <done>
Строка прогресса вехи и раздел фазы говорят одно число, полученное счётом; расхождение записано
летописью с источником поимённо; правка точечная, прочие разделы роадмапа целы; предмет записи 26
закрытым не объявлен.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| перечень маршрутов обхода → продукт | перечень объявляет себя местом, где новый маршрут за панелью краснеет; маршрут вне перечня опровергает это молча |
| решение о неотличимости повтора → второй фрагментный маршрут | свойство, названное несущим для одного маршрута, для второго может не приниматься вовсе |
| строка прогресса вехи → раздел фазы | два числа об одном предмете в одном документе |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-59 | Repudiation | маршрут подтверждения, не пройденный обходом, который объявляет его своим | medium | mitigate | Девятый маршрут внесён в перечень со всеми исходами и отдельными утверждениями; оба числа поставлены прогоном покрасневших правил; обход не правился ради новой записи, и это утверждается диффом |
| T-10-60 | Information Disclosure | второй фрагментный маршрут без решения о неотличимости повтора | medium | mitigate | Помощник выбора перестаёт означать «единственный»; заведено правило «каждый фрагментный маршрут имеет ПРИНЯТОЕ решение» с антивакуумом; решение по девятому выведено ЧТЕНИЕМ обработчика и записано либо правилом с контролем, либо изъятием с основанием |
| T-10-61 | Tampering | запись роадмапа целиком вместо точечной правки строки | high | mitigate | Правка выполняется ТОЧЕЧНОЙ заменой; целость прочих разделов утверждается командой присутствия заголовков пяти соседних фаз; `git diff --stat` приводится в сводке |
| T-10-62 | Repudiation | затёртая прежняя строка прогресса без летописи | low | mitigate | Летопись пишется по форме летописи «16 → 18» с источником расхождения поимённо и с утверждением, что прежнее значение не было ошибкой, а устарело |
| T-10-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не выполняет ни одной установки: ни новой зависимости, ни build-шага, ни нового вендоренного файла. Разведки у фазы нет по решению роадмапа — предмета аудита легитимности пакетов не существует |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_account_groups.py -q -p no:randomly` — зелено.
- `uv run pytest tests/ -q -p no:randomly` — зелено за вычетом двух известных предсуществующих
  красных (окна 27 и 9), названных поимённо.
- `grep -cE '^CONFIRMED_DELETE_ROUTES_DECLARED = 9$' tests/test_pages/test_confirm_delete_transport.py` → `1`.
- Команда-замер строки прогресса — код возврата `0`.
- Команда присутствия заголовков соседних фаз роадмапа — код возврата `0`.
- `git diff --name-only -- app/` — пусто: план кода продукта не трогает.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- `graphify update .` — из рабочего дерева исполнителя не выполняется (записи 23 и 25
  `.planning/WINDOWS.md`); передать оркестратору.
</verification>

<success_criteria>
1. Девятый маршрут подтверждения проходит обход обеими половинами пары и всеми отдельными
   утверждениями; заявленное свойство модуля («маршрут за панелью краснеет здесь») стало верным.
2. Оба числа перечня поставлены прогоном покрасневших правил и несут продолженные летописи со
   своими, РАЗНЫМИ формулировками.
3. Структура записи маршрута выдержала новую форму ответа без правки параметризованного обхода —
   иначе останов и запись окном.
4. Единственность фрагментного маршрута перестала быть допущением: каждый фрагментный маршрут имеет
   ПРИНЯТОЕ решение о неотличимости повтора, и это утверждается правилом с антивакуумом.
5. Решение по девятому маршруту выведено ЧТЕНИЕМ обработчика, а не по аналогии, и записано вместе с
   выдержкой, на которой основано.
6. Строка прогресса вехи и раздел фазы говорят одно число, полученное СЧЁТОМ; правка точечная;
   прочие разделы роадмапа целы.
7. Правило машинно читаемого прогресса (окно 9) не чинилось; его цвет до и после записан честно и
   критерием не являлся.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-11-SUMMARY.md` when done.

Сводка обязана:
- привести ДОСЛОВНО оба вывода покрасневших правил числа и только затем — записанные значения;
- привести перечень идентификаторов случаев обхода нового маршрута (`--collect-only -q`);
- привести числа групп в выдаче ДО отправки для обеих расстановок (антивакуум);
- привести вывод диффа, доказывающий, что параметризованный обход не правился ради новой записи;
- привести ДОСЛОВНО ожидаемый красный помощника выбора фрагментного маршрута («второй фрагментный
  маршрут»);
- назвать выбранный путь решения о повторе девятого маршрута и привести выдержку из обработчика
  Фазы 9, на которой решение основано, а также вердикт правила с контролем ЛИБО запись изъятия с
  основанием и вывод покрасневшего правила числа изъятий;
- привести множество фрагментных маршрутов и множество маршрутов с принятым решением рядом;
- привести вывод команды-замера строки прогресса ДО и ПОСЛЕ, `git diff --stat` по роадмапу и
  дословно дописанную летопись расхождения;
- привести цвет правила машинно читаемого прогресса ДО и ПОСЛЕ правки и назвать прямо, что он не
  чинился и критерием не был;
- назвать НЕЗАКРЫТЫМИ и передать дальше: критерий 2 фазы и шесть пунктов ручной верификации
  (предмет плана 10-10, закрываются человеком на приёмке);
- назвать поимённо два предсуществующих красных (окна 27 и 9) и подтвердить, что ни один не чинился;
- передать оркестратору невыполненный `graphify update .` с основанием записей 23 и 25.
</output>
