---
phase: 10
slug: rychag-components-modal-html
# ⚠️ `status: subject` — ЭТО ПРЕДМЕТ РЕШЕНИЯ, А НЕ ВЕРДИКТ. Поля ЧИСЛА ОТКРЫТЫХ УГРОЗ здесь НЕТ
# НАМЕРЕННО, и его имя не набрано даже комментарием, чтобы подстрочный читатель не нашёл его
# ложно: оно есть машинно читаемое поле ВЕРДИКТА, и исполнитель, поставивший его сам, вынес бы
# вердикт вместо владельца. В ветви изъятия поле встаёт в `10-SECURITY.md`, а не здесь.
status: subject
asvs_level: 1
security_enforcement: true
security_block_on: high
measured: 2026-09-10
created: 2026-09-10
---

# Phase 10 — Security Subject

> Сведённый реестр угроз фазы как ПРЕДМЕТ РЕШЕНИЯ ВЛАДЕЛЬЦА. Не вердикт, не аудит, не замена
> `/gsd-secure-phase 10`.

## Почему этот файл существует

`10-SECURITY.md` не существует при `security_enforcement: true` — и этого не назвал НИ ОДИН из
девяти кругов верификации. Замер командой: `find .planning -name "*SECURITY*"` даёт **девять**
файлов (фазы 07, 08, 09 и все шесть фаз вехи v2.0). У Фазы 10 — **ни одного**.

Три значения принуждения, `.planning/config.json:48-50`, дословно:

| Ключ | Значение |
|------|----------|
| `security_enforcement` | `true` |
| `security_asvs_level` | `1` |
| `security_block_on` | `"high"` |

Пункт 5 перечня Human Verification девятого круга (`10-VERIFICATION.md:838-846`) называет решение
принадлежащим владельцу дословно: «прогнать `/gsd-secure-phase 10` — либо объявить фазу изъятой из
принуждения записью с основанием».

## Register origin

`register_authored_at_plan_time: true` — блок `<threat_model>` несут **ВСЕ СОРОК** исполненных
планов `10-01`…`10-40`, ни один не пропущен; раздел `## Trust Boundaries` — тоже все сорок.
Материал артефакта собран ЦЕЛИКОМ и авторован на планировании; отсутствует только СВЕДЕНИЕ и
решение о нём. Настоящий замер снят РАЗБОРОМ блоков, а не чтением, 2026-09-10.

⚠️ **Планы настоящей партии (`10-41`…`10-47`) в замер НЕ ВХОДЯТ**: они ещё не исполнены, и их
включение сделало бы предмет решения шире фазы, о которой решают.

## Итоги замера — семь величин числами

| # | Величина | Число |
|---|----------|-------|
| 1 | Разобранных исполненных планов (`10-01`…`10-40`) | **40** |
| 2 | Планов БЕЗ разбираемого блока `<threat_model>` | **0** |
| 3 | Строк реестра суммарно | **250** |
| 4 | Уникальных имён угроз | **239** |
| 5 | Распределение по тяжести | **84** `high` · **104** `medium` · **56** `low` · **6** `n/a` (`critical` — 0) |
| 6 | Распределение по распоряжению | **195** `mitigate` · **55** `accept` |
| 7 | Размер блокирующего множества при пороге `high` | **12** |

**Откуда берётся разница 250 − 239 = 11.** Два источника, оба замерены, ни один не есть ошибка
разбора:

- **10** — идентификатор `T-10-SC` повторён в одиннадцати планах `10-01`…`10-11` (одна и та же
  строка цепи поставок без номера плана в имени; считается один раз).
- **1** — в плане `10-22` идентификатор `T-10-22-04` носят **ДВЕ РАЗНЫЕ строки** («авторство
  изъятия из запертого решения D-01» и «правило суиты, утверждающее дефект нормой»). Это
  СТОЛКНОВЕНИЕ ИМЁН в исполненном плане. ⚠️ Оно **не чинится**: план исполнен, и правка задним
  числом переписала бы историю. Здесь оно записывается.

## Trust Boundaries

Сорок планов называют **106** формулировок границ (**98** различных дословно). После сведения —
**13** строк. Число исходных формулировок и число строк после сведения названы оба, как это
сделано в артефакте Фазы 09.

| # | Boundary | Формулировок сведено |
|---|----------|----------------------|
| B1 | недоверенный ввод браузера/клиента → страничный слой и далее в драйвер БД | 14 |
| B2 | сторонний сайт → маршрут, изменяющий состояние (граница происхождения) | 6 |
| B3 | вычисление вердикта о владении и административная граница | 5 |
| B4 | слой ответа → адресная строка браузера (примитив перехода) | 5 |
| B5 | ответ сервера → дерево документа и рантайм разметки в браузере | 13 |
| B6 | вызывающий компонента → форма панели и её 18 мест подтверждения | 6 |
| B7 | ответ сервера → клиентское состояние панели, её уборка и фокус пользователя | 5 |
| B8 | таблица стилей и шелл → достижимость сообщения об отказе для глаза человека | 6 |
| B9 | исходник продукта и суиты → разборщик правила (гейт читает дерево) | 7 |
| B10 | зелёное правило и запись в коде → следующий читатель | 9 |
| B11 | записи планирования (вердикт, требования, роадмап, окна) → следующий круг и следующая фаза | 18 |
| B12 | артефакт обхода → человек на приёмке; исполнитель → владелец фазы | 9 |
| B13 | посевной стенд и гарнир тестов → живая база и рантайм разработчика | 3 |

Сумма по группам — **106**, ни одна формулировка не осталась неразнесённой.

## Блокирующее множество — ПОИМЁННО, все 12 строк

Определение: тяжесть `high` либо `critical` при распоряжении, отличном от `mitigate`, при пороге
блокировки `high`. Строк ровно **12**, и все двенадцать — строка цепи поставок `T-10-NN-SC`
планов `10-12`…`10-23`.

| Имя угрозы | План | Тяжесть | Распоряжение | Существо (дословно из реестра, сокращённо) |
|------------|------|---------|--------------|--------------------------------------------|
| `T-10-12-SC` | 10-12 | high | accept | ПРЕДМЕТА НЕТ: ни одна задача плана не устанавливает пакетов ни одним пакетным менеджером; `pyproject.toml` не правится |
| `T-10-13-SC` | 10-13 | high | accept | ПРЕДМЕТА НЕТ: пакетов не ставится; интерпретатор JS в проекте уже есть (на нём собран `wa_worker/`) и ввозится действующим общим запуском |
| `T-10-14-SC` | 10-14 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и не правит `pyproject.toml` |
| `T-10-15-SC` | 10-15 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и не правит `pyproject.toml` |
| `T-10-16-SC` | 10-16 | high | accept | ПРЕДМЕТА НЕТ, И ЭТО РЕШЕНИЕ: браузерная зависимость НЕ заводится партией с записанным основанием, `pyproject.toml` не правится |
| `T-10-17-SC` | 10-17 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-18-SC` | 10-18 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-19-SC` | 10-19 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-20-SC` | 10-20 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-21-SC` | 10-21 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-22-SC` | 10-22 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-23-SC` | 10-23 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |

**Ни одной строки блокирующего множества ВНЕ цепи поставок нет.** Все 84 строки тяжести `high`,
кроме этих двенадцати, несут распоряжение `mitigate`.

**Замер, поверяющий существо всех двенадцати:** на ветви фазы против `master` — 287 коммитов, и
файлов `pyproject.toml`, `uv.lock`, `wa_worker/package.json`, `wa_bridge/package.json` среди
тронутых **ноль** (`git log --oneline master..HEAD -- …` — пусто). Фаза не ставит НИ ОДНОГО
пакета. То есть «предмета нет» не мнение планов, а факт дерева.

## Расхождение тяжести строки цепи поставок — находка замера

Одна и та же по существу строка («фаза не ставит пакетов, гейт легитимности предмета не имеет»)
несёт в фазе **три разные тяжести**, по диапазонам планов:

| Диапазон планов | Идентификатор | Тяжесть | Распоряжение | Число планов |
|-----------------|---------------|---------|--------------|--------------|
| `10-01`…`10-11` | `T-10-SC` (общий) | `low` | accept | 11 |
| `10-12`…`10-23` | `T-10-NN-SC` | **`high`** | accept | **12** |
| `10-24`…`10-34` | `T-10-NN-SC` | `low` | accept | 11 |
| `10-35`…`10-40` | `T-10-NN-SC` | `n/a` | accept | 6 |

⚠️ **РАСХОЖДЕНИЕ С ФОРМУЛИРОВКОЙ ПЛАНА 10-44, НАЗВАННОЕ, А НЕ СГЛАЖЕННОЕ.** `<objective>` плана
говорит, что та же строка в планах `10-24`…`10-40` несёт тяжесть `n/a`. ЗАМЕР даёт иначе:
`n/a` несут только шесть планов `10-35`…`10-40`, а одиннадцать планов `10-24`…`10-34` несут `low`.
В предмет решения идут СНЯТЫЕ числа. Вероятная причина расхождения: план 10-44 писан по образцу
`10-24` (там `low`) и по идиоме последней партии (там `n/a`), и обобщил вторую на весь диапазон.
На размер блокирующего множества расхождение НЕ ВЛИЯЕТ: оно равно 12 при любом из двух прочтений,
потому что `low` и `n/a` одинаково ниже порога `high`.

**Что из этого следует и что НЕ следует.** Следует: двенадцать строк суть РАСХОЖДЕНИЕ ТЯЖЕСТИ
одной и той же строки с идиомой той же фазы — двадцать восемь планов той же фазы, с тем же
существом и при тех же нулевых установках, оценили её `low` либо `n/a`. НЕ следует: что двенадцать
строк можно «починить».

🚫 **ПРАВКА ИСПОЛНЕННЫХ ПЛАНОВ `10-01`…`10-40` ЗАПРЕЩЕНА** — ни на символ, включая эти двенадцать
строк и включая столкновение имён `T-10-22-04` в плане `10-22`. Планы исполнены, сводки написаны,
коммиты стоя́т; правка задним числом переписала бы историю фазы и сделала бы сегодняшний замер
неповторимым. Расхождение ЗАПИСЫВАЕТСЯ здесь, а не чинится там.

## Ответ владельца

⚠️ **РАЗДЕЛ ВСТАЁТ РОВНО ОДИН РАЗ И ПО ЯКОРЮ, А НЕ ДОПИСЫВАНИЕМ В КОНЕЦ ФАЙЛА.** Счёт вхождений
заголовка второго уровня «Ответ владельца» до записи — **0**, после записи — **1**;
снимается он выражением `grep -c "^## Ответ вл" <файл>`, и самого литерала заголовка эта проза
намеренно не набирает — иначе счёт считал бы упоминание за вхождение. Это прямой урок `V9-06`:
задача, дописывающая раздел в конец, на втором прогоне заводит второй такой раздел, и следующий круг
читает два несогласных решения.

⚠️ **ВЫБОР СДЕЛАН ВЛАДЕЛЬЦЕМ НА БЛОКИРУЮЩЕМ ЧЕКПОЙНТЕ, А НЕ ИСПОЛНИТЕЛЕМ ВНУТРИ ЗАДАЧИ.** Замер
настоящего файла был предъявлен владельцу ЦЕЛИКОМ до ответа: семь величин, поимённый перечень
блокирующего множества, консолидированные границы доверия и раздел о расхождении тяжести. Обе ветви
предъявлены с их ценами дословно. Ни одна ветвь исполнителем не предвыбиралась.

| Поле | Значение |
|------|----------|
| Идентификатор ветви | **`waive-with-record`** |
| Принявший | **`chubav`** |
| Дата | **`2026-09-10`** |
| Область изъятия | **РАЗОВАЯ — ТОЛЬКО ФАЗА 10** (дословно ниже) |
| Артефакт ветви | `10-SECURITY.md` — создаётся задачей 3 настоящего плана |
| Поле числа открытых угроз в артефакте | **12** — ЗАМЕРЕННЫЙ размер блокирующего множества, подтверждено владельцем ПРЯМО, что не ноль |
| Отвергнутая ветвь | `run-secure-phase` — `/gsd-secure-phase 10` НЕ ЗАПУСКАЕТСЯ |

### Область изъятия — дословно, чтобы следующий круг не прочёл её шире принятого

> **Охват изъятия: РАЗОВЫЙ — только Фаза 10.** Изъятие НЕ объявляется действующим на веху v2.1.
> Следующая фаза вехи решает свой случай заново; прецедент не объявляется действующим по
> умолчанию.

⚠️ Формулировка перенесена ДОСЛОВНО по прямому требованию владельца: цена ветви `waive-with-record`,
названная на чекпойнте, состояла именно в прецеденте, наследуемом вехой, — и владелец наследование
ОТКЛОНИЛ. Читать эту запись как «Фаза 10 отменила регламент безопасности для вехи v2.1» — прочтение
ШИРЕ принятого. Для Фазы 11 и далее `security_enforcement: true` остаётся действующим, и настоящая
запись их случая не решает.

### Основание изъятия, подтверждённое владельцем — дословно

> Сорок реестров угроз авторованы на планировании, и ни один план не пропущен: 40 планов разобрано,
> 0 без разбираемого блока `<threat_model>`. Всё блокирующее множество при пороге `high` есть ОДНА И
> ТА ЖЕ строка цепи поставок — двенадцать строк `T-10-12-SC`…`T-10-23-SC`, все `high`/`accept`, — у
> которой ВНУТРИ ТОЙ ЖЕ ФАЗЫ разошлась тяжесть: планы 10-01…10-11 и 10-24…10-34 оценили её `low`,
> планы 10-35…10-40 — `n/a`. Предмета у неё нет ни в одном из двенадцати: ни одна задача этих планов
> не устанавливает пакетов ни одним пакетным менеджером и не правит `pyproject.toml`, а поверочный
> замер дерева даёт НОЛЬ коммитов ветви фазы (из 287), тронувших `pyproject.toml`, `uv.lock`,
> `wa_worker/package.json` или `wa_bridge/package.json`. Двенадцать строк суть РАСХОЖДЕНИЕ ТЯЖЕСТИ
> одной строки с идиомой той же фазы, а не двенадцать принятых высоких рисков.

**Каждое число основания сличено с замером настоящего файла — построчно, а не на веру:**

| Число основания | Источник в замере | Сходится |
|-----------------|-------------------|----------|
| 40 планов разобрано | Итоги замера, величина 1 | да |
| 0 без разбираемого блока | Итоги замера, величина 2 | да |
| 12 строк блокирующего множества | Итоги замера, величина 7; таблица блокирующего множества — 12 строк | да |
| `T-10-12-SC`…`T-10-23-SC`, все `high`/`accept` | таблица блокирующего множества, все 12 строк | да |
| 10-01…10-11 и 10-24…10-34 — `low` | таблица расхождения тяжести, строки 1 и 3 (11 + 11 планов) | да |
| 10-35…10-40 — `n/a` | таблица расхождения тяжести, строка 4 (6 планов) | да |
| НОЛЬ коммитов, тронувших четыре манифеста | поверочный замер дерева | да |
| 287 коммитов ветви | поверочный замер дерева, снятый ДО коммита задачи 1 | да, с поправкой ниже |

⚠️ **ПОПРАВКА К ЧИСЛУ 287, НАЗВАННАЯ, А НЕ СГЛАЖЕННАЯ.** Переснятый на записи решения замер даёт
**288** коммитов ветви против `master`, а не 287: разница ровно в один — коммит задачи 1 настоящего
плана, которым и был создан настоящий файл. Число 287 верно на момент снятия замера, 288 — на момент
записи решения. **На основание изъятия поправка не влияет ничем:** число коммитов, тронувших
`pyproject.toml`, `uv.lock`, `wa_worker/package.json` или `wa_bridge/package.json`, равно **НУЛЮ**
при обоих прочтениях, и именно оно, а не общий счёт, доказывает отсутствие предмета у строки цепи
поставок.

### Два расхождения, найденные задачей 1, ПЕРЕЖИВАЮТ решение — они не сглажены

**1. Формулировка `<objective>` плана 10-44 о диапазоне `n/a` — опровергнута замером.** `<objective>`
утверждает, что в планах 10-24…10-40 строка цепи поставок несёт `n/a`. ЗАМЕР даёт иначе: `low` у
одиннадцати планов 10-24…10-34 и `n/a` только у шести планов 10-35…10-40. **В запись идут СНЯТЫЕ
числа, а не числа плана.** На размер блокирующего множества расхождение НЕ ВЛИЯЕТ: оно равно **12**
при любом из двух прочтений, потому что `low` и `n/a` одинаково ниже порога `high`. Владелец
подтвердил это прочтение прямо.

**2. Столкновение имён `T-10-22-04` в исполненном плане 10-22.** Один идентификатор носят ДВЕ РАЗНЫЕ
строки реестра. Оно **ЗАПИСАНО, А НЕ ПОЧИНЕНО**: правка исполненных планов `10-01`…`10-40`
запрещена, план исполнен, сводка написана, коммиты стоя́т. Починка задним числом переписала бы
историю фазы и сделала бы сегодняшний замер неповторимым.

### Чего это решение НЕ объявляет и что оно НЕ разрешает делать

Названо прямо, потому что изъятие, прочитанное шире принятого, есть сокрытие:

- `/gsd-secure-phase 10` **НЕ ЗАПУСКАЕТСЯ** — ветвь `run-secure-phase` отвергнута владельцем.
- Ни один исполненный план `10-01`…`10-40` и ни одна их сводка **не правятся ни на символ**, включая
  двенадцать строк блокирующего множества и столкновение имён `T-10-22-04`.
- `10-VERIFICATION.md` **не трогается**: вердикт фазы остаётся `Gaps Found`, и настоящее решение его
  не выносит, не оспаривает и не отменяет.
- Отметка `FORM-06` в `.planning/REQUIREMENTS.md` **не ставится**: правило
  `test_requirement_completion_follows_verification` требует прежде вердикта `passed` у верификации
  фазы.
- Ни одна строка реестра **не переводится** из `accept` в `mitigate` и не помечается закрытой.
  Изъятие есть решение НЕ СВОДИТЬ двенадцать строк, а не утверждение, что их нет, — потому поле
  числа открытых угроз в `10-SECURITY.md` получает **12**, а не ноль.
- Продукт и суита **не правятся ни на строку**.

## Чего этот файл НЕ делает

Прямо и без оговорок:

- Он **не выносит вердикта фазе**: `10-VERIFICATION.md` держит `Gaps Found`, и настоящий файл
  этого не меняет и не оспаривает.
- Он **не объявляет ни одну угрозу закрытой**: ни одна строка реестра настоящим файлом не
  переводится из `accept` в `mitigate` и не помечается `closed`.
- Он **не заменяет `/gsd-secure-phase 10`**: артефакт регламента `10-SECURITY.md` производит
  команда регламента, и настоящий файл её не подменяет и её формы не наследует.
- Он **не двигает отметку требования**: `FORM-06` в `.planning/REQUIREMENTS.md` остаётся
  неотмеченным, и правило `test_requirement_completion_follows_verification` этим файлом не
  касается.
- Он **не принимает решения**: выбор между принуждением регламента и изъятием принадлежит
  владельцу. До ответа владельца раздела ответа в этом файле НЕ БЫЛО, и исполнитель ни одной
  ветви внутри задачи не выбирал. Решение принято ВЛАДЕЛЬЦЕМ на блокирующем чекпойнте и
  записано разделом «Ответ владельца» выше — идентификатором ветви, именем, датой и областью.

Без этого раздела следующий круг прочёл бы ПРЕДМЕТ решения как решение — ровно та подмена,
которой девять кругов и не заметили.
