---
phase: 10-rychag-components-modal-html
plan: 14
subsystem: testing
tags: [pytest, planning-gates, requirements, verification, traceability]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "отчёт верификации третьего круга (`10-VERIFICATION.md`, `gaps_found`) — источник вердикта, с которым сверяется новое правило"
  - phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
    provides: "прецедент формы — `tests/test_planning/test_state_progress_matches_roadmap.py`: правило записи, живущее в `tests/` и потому входящее в `just test`"
provides:
  - "правило соответствия «отметка завершённости требования ↔ вердикт отчёта верификации его фазы», входящее в общий прогон проекта"
  - "правило согласия двух записей одного факта: флажок требования в теле файла и клетка его состояния в таблице"
  - "индекс вердиктов отчётов верификации по номеру фазы, собираемый обходом ВСЕГО дерева записи (архив `.planning/milestones/` включён)"
  - "объявленное число строк таблицы состояний (44) с летописью и зубами"
  - "два отрицательных контроля на изменённых копиях записи — зубы обоих правил ПОКАЗАНЫ прогоном, а не заявлены"
affects: [10-15, 10-16, верификация Фазы 10, закрытие вехи v2.1, любой будущий перевод требования в завершённое состояние]

actuals:
  tokens: 7702
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - "правило записи (`tests/test_planning/`): регламент, записанный прозой, получает ПРИНУЖДЕНИЕ модулем в `tests/`, а не остаётся дисциплиной автора"
    - "разборщик принимает ИСХОДНИК параметром — без этого отрицательный контроль невыразим, и зубы правила пришлось бы заявлять вместо того, чтобы показывать"
    - "изменённая копия записи в памяти (`_with_status`, `_with_flag`, `_without_row`) вместо правки файла дерева"

key-files:
  created:
    - tests/test_planning/test_requirement_completion_follows_verification.py
  modified: []

key-decisions:
  - "Номер фазы для индекса отчётов берётся ИЗ ИМЕНИ ФАЙЛА, и способ выбран ОДИН: поле `phase` шапки несёт слог (`10-rychag-components-modal-html`), а у отчётов быстрых работ — вовсе не номер (`quick-260825-hnf-…`). Два независимых способа разошлись бы молча"
  - "Вселенная правила согласия — строки таблицы, НЕСУЩИЕ клетку фазы (требования вехи v2.1). Три строки, отложенные к v2.2 (`EDIT-01`, `UPLD-01`, `E2E-01`), флажка не несут по решению самой записи и из вселенной выпадают"
  - "Сужение вселенной стиранием фазы закрыто ОТДЕЛЬНЫМ правилом `test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself`: клетка без фазы законна ТОЛЬКО у отложенной строки"
  - "`COMPLETED_STATUS` и `PASSED_VERDICT` выписаны двумя независимыми константами; ни одна не выводится из другой — они живут в двух разных документах"
  - "Файл `.planning/REQUIREMENTS.md` не тронут ни на символ: возврат `FORM-06` в незавершённое состояние уже исполнен коммитом `c899f04`, и вторая правка того же места переписала бы историю задним числом"

patterns-established:
  - "Отказ показывает ВСЕ несогласные строки одним отображением, а не первую: утверждение на каждом шаге чинилось бы по одной строке за прогон"
  - "Три множества несогласий («нет клетки», «нет флажка», «значения разошлись») называются РАЗДЕЛЬНО; пустое множество в отказ не печатается вовсе"
  - "Число, объявленное константой, ставится ПРОГОНОМ покрасневшего правила и несёт летопись: откуда взялось, чем измерено, каким планом"

requirements-completed: []

coverage:
  - id: D1
    description: "Требование, помеченное завершённым при вердикте отчёта верификации своей фазы, отличном от `passed`, краснит общий прогон проекта"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_no_requirement_is_marked_complete_before_its_phase_verification_passed"
        status: pass
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_control_negative_a_premature_completion_reddens_the_rule"
        status: pass
    human_judgment: false
  - id: D2
    description: "Отказ несущего правила называет ВСЕ несогласные строки, а не первую, и по каждой — требование, фазу и вердикт отчёта (либо его отсутствие)"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_control_negative_two_premature_completions_are_both_named"
        status: pass
    human_judgment: false
  - id: D3
    description: "Индекс отчётов верификации собирается обходом ВСЕГО дерева записи: переезд фазы в архив не выключает правило молча"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_the_verification_index_is_built_by_walking_the_whole_record_tree"
        status: pass
    human_judgment: false
  - id: D4
    description: "Число строк таблицы состояний объявлено (44) и стережётся: ушедшая строка краснит так же, как пришедшая"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_the_number_of_requirement_rows_is_declared"
        status: pass
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself"
        status: pass
    human_judgment: false
  - id: D5
    description: "Флажок требования в теле файла и клетка его состояния в таблице говорят одно; три множества несогласий названы раздельно"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_the_flag_and_the_status_cell_of_every_requirement_agree"
        status: pass
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_control_negative_a_disagreeing_flag_reddens_the_agreement_rule"
        status: pass
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py#test_control_negative_the_three_kinds_of_disagreement_are_named_apart"
        status: pass
    human_judgment: false
  - id: D6
    description: "Границы предмета названы прямо тремя абзацами докстринга: прозу модуль не судит, верность отчёта не проверяет, решение D-33 не переоткрывает"
    verification: []
    human_judgment: true
    rationale: "Соблюдение границы — суждение о ТЕКСТЕ, а не машинно проверяемое свойство: правило, судящее собственный докстринг, было бы ровно тем гейтом на прозе, от которого отказалось решение D-33. Читает человек"

duration: 1h 3m
completed: 2026-09-04
status: complete
---

# Фаза 10 План 14: принуждение регламента «отметка завершённости не бывает шире вердикта» Summary

**Правило `tests/test_planning/test_requirement_completion_follows_verification.py` из девяти пунктов: требование, помеченное завершённым без вердикта `passed` отчёта верификации своей фазы, теперь КРАСНИТ `just test`, а флажок в теле записи и клетка таблицы обязаны говорить одно — оба правила с отрицательными контролями на изменённых копиях, файл записи не тронут ни на символ.**

## Performance

- **Duration:** 1h 3m (из них 35 мин 30 с — один прогон полной суиты)
- **Started:** 2026-09-04T14:46:52Z
- **Completed:** 2026-09-04T15:49:51Z
- **Tasks:** 2 (обе TDD, четыре коммита RED→GREEN)
- **Files modified:** 1 (создан)

## Accomplishments

- **Регламент проекта получил ПРИНУЖДЕНИЕ.** Собственная формулировка проекта — «условие перевода — вердикт `passed` повторной верификации» — существовала прозой и не имела зубов; ровно поэтому коммит `0ea886d` (план 10-11) пометил `FORM-06` завершённым при живом блокере `CR-01`, а поймал это СЛЕДУЮЩИЙ круг верификации, а не прогон. Теперь такой коммит краснит `just test`.
- **Индекс отчётов собран ОБХОДОМ всего дерева записи**, а не чтением одного каталога: переезд фазы в архив не выключает правило молча.
- **Два места одного факта сведены правилом согласия**: флажок требования и клетка его состояния. Событие `0ea886d` поставило ОБЕ отметки разом — потому они друг друга и не поймали; настоящее правило ловит случай, когда поедет ОДНО из двух, и это разделение предметов записано в докстринге, а не оставлено читателю.
- **Зубы обоих правил ПОКАЗАНЫ прогоном**, а не заявлены: три отрицательных контроля на изменённых копиях записи в памяти.

## Task Commits

1. **Задача 1 — RED:** `f3c430a` (`test`) — правило, индекс, контроли; `REQUIREMENT_ROWS_DECLARED = 0`, прогон краснеет, код возврата ровно `1`
2. **Задача 1 — GREEN:** `44ae988` (`feat`) — число `44` поставлено прогоном, летопись записана
3. **Задача 2 — RED:** `a987e66` (`test`) — правило согласия и два его контроля заведены ПЕРЕД разборщиком: `3 failed, 6 passed`, `NameError: name 'flag_cell_disagreements' is not defined`
4. **Задача 2 — GREEN:** `47c0f7c` (`feat`) — разборщик флажков, три множества несогласий, изменённые копии

Ступени REFACTOR не было: правила зелены с первой правки, а чистка без изменения — коммит без предмета.

## Files Created/Modified

- `tests/test_planning/test_requirement_completion_follows_verification.py` (создан, 558 строк) — разбор записи требований, индекс вердиктов отчётов, несущее правило, правило числа строк, правило законности клетки без фазы, правило согласия двух записей и три отрицательных контроля.

`.planning/REQUIREMENTS.md` **НЕ ТРОНУТ**: `git diff --name-only -- .planning/REQUIREMENTS.md` — пусто; `sed -n '31p;136p'` печатает ровно то состояние, что было до плана (флажок `FORM-06` не заполнен, клетка `Gaps Found`).

## Замеры — СНЯТЫ СВОИМ ПРОГОНОМ, ориентиры плана приведены рядом

| Величина | Замер | Ориентир плана | Объяснение расхождения |
|---|---|---|---|
| Строк таблицы состояний | **44** | 41 | 41 — число требований ВЕХИ v2.1, и ровно столько строк несут клетку фазы. Таблица держит СВЕРХ них ТРИ строки, заведённые 2026-08-29 и объявленные отложенными к v2.2: `EDIT-01`, `UPLD-01`, `E2E-01` (клетка фазы читается `— (v2.2)`). Ориентир не был ошибкой — он верно называл вселенную v2.1, но не число СТРОК, а стережётся здесь именно число строк |
| Строк с клеткой фазы | **41** | 41 | совпало |
| Строк, помеченных `Complete` | **23** | 23 | совпало |
| Флажков в теле файла | **41** | 41 | совпало |
| Флажков заполненных | **23** | 23 | совпало |
| Отчётов в индексе | **11** (фазы 1, 2, 3, 4, 5, 5.1, 6, 7, 8, 9, 10) | 4 (фазы 7, 8, 9, 10) | ориентир считал только `.planning/phases/`; ОБХОД ВСЕГО ДЕРЕВА добавляет **7 архивных** отчётов из `.planning/milestones/v2.0-phases/`. Это и есть требуемое свойство: переезд фазы в архив не выключает правило. Два отчёта быстрых работ (`quick/…`) в индекс НЕ попадают — их имя не несёт номера фазы, и таблица состояний на них не ссылается |

**Правило зелено на сегодняшнем дереве:** все **23** завершённые строки принадлежат фазам **7, 8 и 9**, вердикты которых — `passed`, `passed`, `passed`. Единственная строка Фазы 10 (`FORM-06`) помечена `Gaps Found` и правилом не судится вовсе.

Обход, которым найдены отчёты: `Path('.planning').rglob('*-VERIFICATION.md')` с отбором по форме имени `NN-VERIFICATION.md` / `NN.N-VERIFICATION.md`; номер нормализуется (`07` и `7` — одна фаза, потому что имя файла дополнено нулём, а клетка таблицы нет).

## ЧИСЛО ПОСТАВЛЕНО ПРОГОНОМ — дословный вывод покрасневшего правила

```
E       AssertionError: строк таблицы состояний в `.planning/REQUIREMENTS.md` найдено 44, а объявлено 0 — если строка добавлена или снята осознанно, привести надо ОБЪЯВЛЕННОЕ ЧИСЛО и записать летопись; если нет — привести надо таблицу
E       assert 44 == 0
```

`1 failed, 5 passed` (коммит `f3c430a`, код возврата `1`). Только после этого число `44` записано константой (`44ae988`).

## ЗУБЫ ПОКАЗАНЫ — дословный вывод отрицательных контролей

**Несущее правило, одна преждевременная отметка** (копия записи, где `FORM-06` помечен `Complete`):

```
требование `FORM-06` (Phase 10) помечено `Complete` в таблице состояний `.planning/REQUIREMENTS.md`, а у его фазы вердикт `gaps_found` — условие перевода записано самим проектом: вердикт `passed` повторной верификации
```

**ОТКАЗ ПОКАЗЫВАЕТ ВСЕ СТРОКИ, А НЕ ПЕРВУЮ** — две преждевременные отметки, два РАЗНЫХ случая (`FORM-06` — фаза с вердиктом `gaps_found`; `FORM-03` — Фаза 11, у которой отчёта нет вовсе):

```
требование `FORM-03` (Phase 11) помечено `Complete` в таблице состояний `.planning/REQUIREMENTS.md`, а у его фазы отчёта верификации этой фазы НЕТ ВОВСЕ — условие перевода записано самим проектом: вердикт `passed` повторной верификации
требование `FORM-06` (Phase 10) помечено `Complete` в таблице состояний `.planning/REQUIREMENTS.md`, а у его фазы вердикт `gaps_found` — условие перевода записано самим проектом: вердикт `passed` повторной верификации
```

Названы ОБЕ.

**Правило согласия, одно расхождение** (копия, где у `FORM-01` заполнен только флажок):

```
значения разошлись: `FORM-01`: флажок ЗАПОЛНЕН, клетка `Pending`
```

**ТРИ МНОЖЕСТВА НЕСОГЛАСИЙ РАЗДЕЛЬНЫ** — копия, где `QUAL-04` потерял строку таблицы, а `FORM-01` разошёлся значениями:

```
нет клетки: `QUAL-04`: флажок ПУСТ, клетки нет
значения разошлись: `FORM-01`: флажок ЗАПОЛНЕН, клетка `Pending`
```

Множество «нет флажка» пусто и в отказ НЕ ПЕЧАТАЕТСЯ — иначе читатель искал бы событие, которого не было.

Все три копии собраны В ПАМЯТИ (`_with_status`, `_with_flag`, `_without_row`); файл дерева не правился.

## Абзац докстринга о решении D-33 — целиком

> ПОЧЕМУ ЭТО НЕ ОТМЕНА РЕШЕНИЯ D-33. Решение D-33 отказало машинному гейту на ПРОЗЕ операционного документа с названной причиной: отличить «константу величины, которую документ не может держать истинной» от законного числа того же документа (даты, цены, номера ревизии, числа, квалифицированного раундом) машине нечем, и такой гейт давал бы либо ложные отказы, либо зелень всегда. Здесь предмет ДРУГОЙ: ДВА машинно читаемых поля (клетка состояния требования и поле `status` шапки отчёта) и ОДИН объявленный между ними источник соответствия. Суждения этот предмет не требует, поэтому D-33 настоящим модулем НЕ ПЕРЕОТКРЫВАЕТСЯ и НЕ ОТМЕНЯЕТСЯ; документная половина прохибиций по-прежнему судится человеком.

Два прочих обязательных абзаца — «ЗАЧЕМ» (событие `0ea886d` названо поимённо, вместе с возвратом `c899f04`) и «⚠️ ЧЕГО ЭТОТ ФАЙЛ НЕ ДОКАЗЫВАЕТ» (прозу не судит; верность отчёта не проверяет; своевременности отметки не знает; строк, не помеченных завершёнными, не судит вовсе) — стоят на месте, `:1-46` модуля.

## Абзац правила согласия — чего оно НЕ утверждает

> ⚠️ ЧЕГО ЭТО ПРАВИЛО НЕ УТВЕРЖДАЕТ. Оно НЕ утверждает, что ЛЮБОЕ из двух значений ВЕРНО: два согласных места могут быть согласно неверными, и именно так предмет и был заведён — коммит `0ea886d` поставил ОБЕ отметки разом, поэтому друг друга они не поймали. Утверждение о ВЕРНОСТИ значения принадлежит несущему правилу выше (`…_before_its_phase_verification_passed`), которое сверяет клетку с вердиктом отчёта. Настоящее правило ловит ДРУГОЙ случай: когда поедет ОДНО из двух. Разделение предметов записано здесь, а не оставлено читателю.

## Verification

| Проверка | Результат |
|---|---|
| `uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly` | **9 passed** |
| `uv run pytest tests/test_planning/…py -q -p no:randomly -k "agree or agreement or control"` | **5 passed, 4 deselected** — фильтр задачи 2 выбирает правила |
| `uv run pytest tests/test_planning -q -p no:randomly` | **14 passed** (базовый замер до плана — `5 passed`; прибавка `+9` равна числу заведённых правил) |
| `uv run pytest tests/ -q` | **2822 passed** за 35 мин 30 с (базовый после 10-13 — 2813; прибавка `+9` та же) |
| `uv run python -m compileall -q app main.py tests` | без ошибок |
| `git diff --name-only -- .planning/REQUIREMENTS.md` | пусто |
| `sed -n '31p;136p' .planning/REQUIREMENTS.md` | флажок `FORM-06` НЕ заполнен; клетка — `Gaps Found`. Ровно то состояние, что было ДО плана |

Дифф всех четырёх коммитов плана (`git diff --name-only f3c430a~1..HEAD`) содержит РОВНО один файл — созданный модуль.

## Decisions Made

Решения перечислены во frontmatter (`key-decisions`). Отдельно стоит отметить два, которые могли быть приняты молча:

1. **Номер фазы — из имени файла, и способ ОДИН.** Поле `phase` в шапке отчёта несёт слог, а у отчётов быстрых работ (`.planning/quick/…`) — вовсе не номер. Второй, независимый способ разошёлся бы с первым молча, поэтому основание выбора записано у самой функции.
2. **Вселенная правила согласия сужена до строк с клеткой фазы — и сужение застраховано.** Иначе несущее правило обходилось бы стиранием фазы. Поэтому заведено отдельное правило: клетка без фазы законна ТОЛЬКО у строки со статусом `Deferred`.

## Deviations from Plan

None — план исполнен ровно как написан.

Отдельно: шаг `requirements mark-complete` для `FORM-06` НЕ выполнялся, и это не отступление, а работа гейта общих идентификаторов. `requirements.ready-ids` вернул `0/1 requirement(s) ready to mark complete` — `FORM-06` объявлен ВСЕМИ шестнадцатью планами Фазы 10, а планы 10-15 и 10-16 сводок ещё не имеют. Сверх того: перевод `FORM-06` в `Complete` при вердикте `10-VERIFICATION.md`, равном `gaps_found`, теперь КРАСНИТ прогон — правилом, которое этот план и завёл. Именно поэтому `requirements-completed` в шапке пуст: план закрывает механизм, а не требование.

## Issues Encountered

Единственная особенность прогона — полная суита идёт **35 мин 30 с**, и первый её запуск пришлось повторить: вывод, пропущенный через `tail`, не попал в файл, а процесс был снят по тайм-ауту. Повторный прогон писал в файл напрямую. К предмету плана отношения не имеет.

## User Setup Required

None — внешних служб план не касается.

## Next Phase Readiness

- **Четвёртый гэп отчёта верификации третьего круга закрыт целиком.** Первая половина (возврат `FORM-06` в незавершённое состояние) была исполнена коммитом `c899f04` до начала плана и НЕ переделывалась; вторая половина — принуждение — заведена этим планом.
- **Готово к планам 10-15 и 10-16** (волна 1 третьей партии). Новое правило на них не влияет: оно судит только запись требований.
- **Предупреждение будущему кругу верификации.** Когда Фаза 10 получит вердикт `passed`, перевод `FORM-06` в `Complete` потребует ДВУХ согласованных правок — флажка `:31` и клетки `:136`; расхождение любой одной из них теперь краснит прогон, а перевод раньше вердикта не пройдёт вовсе.

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-04*

## Self-Check: PASSED

- `tests/test_planning/test_requirement_completion_follows_verification.py` — **FOUND** на диске (558 строк, 30 809 байт)
- Коммиты `f3c430a`, `44ae988`, `a987e66`, `47c0f7c` — **FOUND** все четыре в `git log`
- Все `<acceptance_criteria>` обеих задач перепроверены и приведены выше замерами и дословными выводами
- Плановая `<verification>` перезапущена целиком: `tests/test_planning` — 14 passed; `tests/` — 2822 passed; дифф записи требований пуст; `sed -n '31p;136p'` даёт доплановое состояние
