---
phase: 10-rychag-components-modal-html
plan: 53
subsystem: testing
tags: [verification-record, override, criterion-3, deferred-items, docstring]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "план 10-52 — пятое утверждение гейта критерия 3 (прибор, печатающий измеренный перечень регистраций в телах блоков `<script>`)"
  - phase: 10-rychag-components-modal-html
    provides: "одиннадцатый круг верификации — запись гэпа по критерию 3 с обоими пунктами `missing`"
provides:
  - "`overrides[1]` шапки `10-VERIFICATION.md` — ответ владельца машинно читаемым полем: ветвь `extend-override`, `chubav`, 2026-09-12, с полями `covers`, `price`, `enforced_by`"
  - "основание числа трёх регистраций в докстринге `test_criterion_three_holds_by_the_numbers` — тремя величинами и местом, где решение живёт"
  - "раздел `## План 10-53` в `deferred-items.md` — последствия ответа и перечень непокрытого"
affects: [10-54, следующий круг верификации Фазы 10, следующие фазы вехи v2.1]

actuals:
  tokens: 5132
  tasks: 2
  commits: 1
  plan_head_before: 4f58cffc5b9c2c307bf8bf81276908bb193bea98

tech-stack:
  added: []
  patterns:
    - "запись отступления с полями `price` и `enforced_by` — цена ссылкой на запись журнала окон, принуждение именем утверждения гейта"

key-files:
  created: []
  modified:
    - .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
    - tests/test_templates/test_components.py
    - .planning/phases/10-rychag-components-modal-html/deferred-items.md

key-decisions:
  - "Ветвь `extend-override` (ответ владельца `chubav`, 2026-09-12): область действующего отступления РАСШИРЕНА второй записью блока `overrides`, называющей третий файл (`app/templates/includes/htmx_error_banner.html`) и третью сущность (регистрацию `htmx:afterRequest` на узле тела документа, `:227-233`, коммит `4e24d61`) поимённо"
  - "Область записи СУЖЕНА словами владельца до ЭТОЙ регистрации: будущие регистрации в том же файле НЕ покрыты, требуют НОВОГО решения владельца и обязаны покраснить пятое утверждение гейта критерия 3"
  - "Ветвь `criterion-stands-violated` не выбрана — записи журнала окон настоящим планом не заводится: цена уже записана окном 74 и принята девятой партией открыто"
  - "`overrides_applied: 0` не сдвинут: поле есть счёт КРУГА, а не счёт записей, и засчитать запись вправе следующий круг, а не план"

patterns-established:
  - "Ответ владельца на `gate=\"blocking-human\"` живёт ПОЛЕМ шапки отчёта, а не прозой: следующий круг читает поле, а не выводит решение из диффа"
  - "Запись отступления несёт ЦЕНУ ссылкой на запись журнала окон и ПРИНУЖДЕНИЕ именем конкретного утверждения гейта — расширение формы записи 2026-09-09"

requirements-completed: []

coverage:
  - id: D1
    description: "Ответ владельца стои́т машинно читаемым местом — `overrides[1]` шапки `10-VERIFICATION.md` с ветвью, именем, датой, `covers`, `price`, `enforced_by`; первая запись, комментарий круга и поля вердикта не тронуты"
    verification:
      - kind: other
        ref: "grep -c -e '^status: gaps_found$' -e '^score: 8/10 must-haves verified$' -e '^behavior_unverified: 1$' -e '^overrides_applied: 0$' 10-VERIFICATION.md → 4"
        status: pass
      - kind: other
        ref: "git diff --numstat -- 10-VERIFICATION.md → 62 вставки, 0 удалений (первая запись и комментарий круга не тронуты ни символом)"
        status: pass
      - kind: other
        ref: "uv run python — yaml.safe_load шапки: overrides == 2 записи, поля[1] == [must_have, reason, accepted_by, accepted_at, accepted_branch, covers, price, enforced_by]"
        status: pass
      - kind: unit
        ref: "tests/test_planning/ (44 passed) — гейт отметки требования читает эту шапку и не уронен"
        status: pass
    human_judgment: true
    rationale: "Соответствие записи СЛОВАМ владельца машина не проверяет ни одним правилом: она проверяет форму, а не верность пересказа. Подтвердить, что `covers`, `accepted_by` и `accepted_at` говорят то, что сказал владелец, может только владелец"
  - id: D2
    description: "Число трёх регистраций в гейте критерия 3 получило ОСНОВАНИЕ: докстринг называет ветвь, имя, дату и поле шапки, где решение живёт; ссылка «вносит план 10-53» из текста ушла"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_components.py#test_criterion_three_holds_by_the_numbers (1 passed, 89 deselected) — правка докстринга не задела тела правила и его отбора"
        status: pass
      - kind: other
        ref: "grep по файлу на форму ссылки «вносит … план 10-53 после/ПОСЛЕ останова» → 0 вхождений; оставшиеся два упоминания 10-53 указывают на план, ВНЁСШИЙ основание"
        status: pass
    human_judgment: true
    rationale: "Правило доказывает только собственную несломанность; что абзац НАЗЫВАЕТ основание верно и полно — свойство прозы, которого не меряет ни одно правило суиты"
  - id: D3
    description: "Раздел `## План 10-53` внесён в `deferred-items.md` ПЕРЕД якорем, разделов на план ровно один, якорь в файле один и последний"
    verification:
      - kind: unit
        ref: "tests/test_planning/test_deferred_items_are_idempotent.py (7 passed) — единственность раздела на план, существование, единственность и положение якоря"
        status: pass
      - kind: other
        ref: "grep -c '^## План 10-53$' deferred-items.md → 1; tail файла показывает якорь последним элементом"
        status: pass
    human_judgment: false
  - id: D4
    description: "Запреты плана соблюдены: `app/`, `.planning/REQUIREMENTS.md`, `10-PROHIBITIONS-SUBJECT.md` не тронуты; формулировка критерия 3 в роадмапе и шапка `10-UAT.md` стоя́т нетронутыми"
    verification:
      - kind: other
        ref: "git diff --numstat 9e6b859 -- app .planning/REQUIREMENTS.md .../10-PROHIBITIONS-SUBJECT.md → ПУСТО"
        status: pass
      - kind: other
        ref: "grep -c 'новых строк JS фаза не добавляет (машинно: диффом по `app/static/js/` и шаблонам)' .planning/ROADMAP.md → 1"
        status: pass
      - kind: other
        ref: "grep -c -e '^status: partial$' -e '^checks_declared: 6$' 10-UAT.md → 2"
        status: pass
      - kind: unit
        ref: "uv run pytest tests/ -q --deselect tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard → 3215 passed, 1 deselected, rc=0"
        status: pass
    human_judgment: false

duration: 71 min
completed: 2026-09-12
status: complete
---

# Phase 10 Plan 53: ответ владельца по критерию 3 — Summary

**Ветвь `extend-override` владельца `chubav` (2026-09-12) записана полем `overrides[1]` шапки
`10-VERIFICATION.md` с узкой областью «только эта регистрация», ценой ссылкой на окно 74 и
принуждением именем пятого утверждения гейта — и число трёх регистраций в гейте критерия 3
получило основание тремя величинами вместо ссылки на будущий план.**

## Performance

- **Duration:** 71 min
- **Tasks:** 2 (задача 1 — останов на владельце, по построению не правит ничего; задача 2 — запись)
- **Files modified:** 3

## Accomplishments

- Ответ владельца получил МАШИННО ЧИТАЕМОЕ МЕСТО: следующий круг читает поле, а не выводит
  принятое отступление из диффа. Это и был предмет пункта `missing[0]` записи гэпа.
- Область расширения СУЖЕНА словами владельца и записана узкой: покрыта РОВНО одна регистрация;
  следующая в том же файле требует нового решения и обязана покраснить пятое утверждение гейта.
- Число трёх регистраций в `test_criterion_three_holds_by_the_numbers` перестало стоять без
  причины: докстринг называет ветвь, имя, дату и запись шапки, где решение живёт.
- Ссылка на будущее («основание вносит план 10-53»), оставленная планом 10-52 в ДВУХ местах
  модуля, заменена внесённым основанием — окно 78 в миниатюре не заведено.

## Task Commits

1. **Задача 1: РЕШЕНИЕ ВЛАДЕЛЬЦА (`checkpoint:decision`, `gate="blocking-human"`)** — коммита НЕТ
   по построению: задача предъявляет замеры и останавливается, не правя ни одного файла.
   Подтверждено: `git diff --numstat 9e6b859` по перечню запретов пуст, а грепа по scope
   `^[a-z]+\((0*10)-(0*53)\):` до настоящей партии не давал ни одного коммита.
2. **Задача 2: ответ становится записью, число получает основание** — `ca946b9` (docs)

**Plan metadata:** см. коммит `docs(10-53): complete …` ниже по истории.

## ОТВЕТ ВЛАДЕЛЬЦА — ДОСЛОВНО, ЧЕТЫРЬМЯ ВЕЛИЧИНАМИ

| Величина | Значение |
|---|---|
| Ветвь | `extend-override` |
| Область (`covers`) | **ТОЛЬКО ЭТА РЕГИСТРАЦИЯ.** Запись охватывает ровно регистрацию обработчика `htmx:afterRequest` на узле тела документа в `app/templates/includes/htmx_error_banner.html:227-233` (коммит `4e24d61`, 7 исполняемых строк). На БУДУЩИЕ регистрации в том же файле НЕ распространяется: всякая следующая требует НОВОГО решения владельца и обязана покраснить пятое утверждение гейта критерия 3 (план 10-52) |
| `accepted_by` | `chubav` |
| `accepted_at` | `2026-09-12` |

Ветвь `criterion-stands-violated` владельцем НЕ выбрана. Следствие, названное прямо: записи
журнала `.planning/WINDOWS.md` настоящий план не заводит — цена расширения уже записана окном 74 и
принята девятой партией открыто (`T-10-49-05`, disposition `accept`, тождественна находке ревизии
`IN-02`). Это ровно то, что говорит и frontmatter плана: `.planning/WINDOWS.md` правится ТОЛЬКО в
ветви `criterion-stands-violated`. Противоречия между текстом плана и указанием владельца нет.

## ШЕСТЬ ЗАМЕРОВ — ВЫВОДОМ КОМАНД, А НЕ ПЕРЕСКАЗОМ

⚠️ Замеры сняты КОМАНДАМИ на HEAD. Предъявление владельцу сделал предыдущий исполнитель до
останова; настоящий (продолжающий) исполнитель ПЕРЕСНЯЛ все шесть на HEAD десятой партии перед
записью — вывод приведён ниже дословно.

**1. Первая половина критерия держится.**
```
$ git diff --numstat 7c1041d..HEAD -- app/static/js/
[пусто]
```

**2. Вторая половина пала, и чем именно.**
```
$ git diff --numstat 7c1041d..HEAD -- app/templates/includes/htmx_error_banner.html
120	0	app/templates/includes/htmx_error_banner.html
```
```
$ git diff 4e24d61^..4e24d61 -- app/templates/includes/htmx_error_banner.html
@@ -170,5 +224,12 @@
     var network = document.getElementById('htmx-failure-network');
     if (network) { network.removeAttribute('hidden'); }
   });
+  document.body.addEventListener('htmx:afterRequest', function (event) {
+    if (!event.detail || !event.detail.successful) { return; }
+    var done_server = document.getElementById('htmx-failure-server');
+    if (done_server) { done_server.setAttribute('hidden', ''); }
+    var done_network = document.getElementById('htmx-failure-network');
+    if (done_network) { done_network.setAttribute('hidden', ''); }
+  });
   }
 </script>
```
(в том же коммите — 54 строки прозы обоснования выше по файлу; исполняемых строк СЕМЬ, целиком
новая регистрация.)

**3. Летопись файла.**
```
$ git log --follow --oneline -- app/templates/includes/htmx_error_banner.html
4e24d61 feat(10-49): третий обработчик гасит обе заготовки по признаку успеха обмена
d5bdf61 docs(10-35): довод (б) помечен опровергнутым, допущение Фазы 8 сужено, цена названа
f27b0e8 docs(10-33): владелец заготовок называет, чем держится их достижимость
efe9cb6 fix(09-19): wire the failure banner handlers once per live body (WR-05, GREEN)
37cff81 feat(09-04): name the screen-server divergence in the network failure banner
6d2eaf2 feat(08-04): плашки отказа сервера и обрыва связи, два обработчика, гейт G-23
```

**4. Вывод ПРИБОРА, заведённого волной 1 (план 10-52) — перечнем, измеренным ПРАВИЛОМ, а не грепом.**
```
$ uv run python -c "…ввоз _script_handler_registrations / _declared_script_handler_registrations…"
ИЗМЕРЕНО ПРАВИЛОМ: {'ads/form.html': 8, 'includes/htmx_error_banner.html': 3}
ОБЪЯВЛЕНО:         {'ads/form.html': 8, 'includes/htmx_error_banner.html': 3}

$ uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"
1 passed, 89 deselected, 1 warning in 0.28s
```

**5. Партия не назвала критерий ни разу.**
```
10-49-PLAN.md    | критерий 3 -> 0
10-49-PLAN.md    | D-13       -> 0
10-49-SUMMARY.md | критерий 3 -> 0
10-49-SUMMARY.md | D-13       -> 0
```
Замер планировщика 2026-09-12 воспроизведён: НОЛЬ вхождений в каждом из четырёх сочетаний.

**6. Область действующего отступления — его собственным текстом.**
`covers` записи `overrides[0]`: «51 строка JS в `app/templates/ads/form.html` и
`app/templates/components/modal.html`. Граница отступления — правка ТЕЛА метода уже существующего
объекта клиентского состояния и правка выражения УЖЕ СУЩЕСТВУЮЩЕГО атрибута события; заведение
НОВОЙ сущности настоящей записью НЕ принимается.»
`not_reached_by_this_override`: «`app/templates/includes/htmx_error_banner.html` — 7 исполняемых
строк, РЕГИСТРАЦИЯ ТРЕТЬЕГО обработчика `document.body.addEventListener('htmx:afterRequest', …)`
(план 10-49, коммит `4e24d61`). Файл в поле `covers` не назван; форма правки — не тело
существующего метода и не выражение существующего атрибута.»

## СПОСОБ СЛИЧЕНИЯ ФОРМУЛИРОВКИ КРИТЕРИЯ — ПРОГРАММНЫЙ, ПОСИМВОЛЬНЫЙ

`must_have` новой записи сличён с `.planning/ROADMAP.md` не глазом: шапка отчёта разобрана
`yaml.safe_load`, строка роадмапа взята поиском подстроки, нумерующий префикс снят
`re.sub(r'^\s*3\.\s*', '', …)`, и результаты сравнены оператором равенства строк Python.

```
СЛИЧЕНИЕ ПОСИМВОЛЬНО (must_have[1] == критерий 3 роадмапа): True 151 151
```
Обе длины — 151 символ; равенство строк в Python есть посимвольное равенство, поэтому отдельного
обхода по символам не заводилось.

## Files Created/Modified

- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — вторая запись блока
  `overrides` (62 вставки, 0 удалений: ни первая запись, ни комментарий круга, ни поля вердикта,
  ни `re_verification`, ни `gaps`/`deferred`/`advisory` не тронуты)
- `tests/test_templates/test_components.py` — основание числа в докстринге гейта критерия 3 и в
  модульном комментарии над разбором тел блоков `<script>`; тела правил и отбор не тронуты
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md` — раздел `## План 10-53`
  перед якорем

## Decisions Made

См. `key-decisions` шапки. Сверх них — одно решение о форме, названное здесь прямо: **ссылка на
будущий план заменена в ДВУХ местах модуля, а не в одном.** План называл одно место (докстринг);
на дереве та же ссылка стояла и в модульном комментарии над разбором (`:4800-4803`), внесённом тем
же планом 10-52. Оставить вторую значило бы завести окно 78 в миниатюре ровно того класса, против
которого записан `T-10-53-05`.

## Deviations from Plan

### 1. [Правка сверх буквы плана] Ссылка на будущий план снята в двух местах вместо одного

- **Найдено при:** задаче 2, действие (3)
- **Что:** план говорит «ФРАЗА ПЛАНА 10-52 „основание вносит план 10-53“ ЗАМЕНЯЕТСЯ ВНЕСЁННЫМ
  ОСНОВАНИЕМ, и это единственное место, где текст плана 10-52 правится» — в единственном числе.
  На дереве фраза стояла ДВАЖДЫ: в докстринге правила и в модульном комментарии над разборщиком
  тел блоков (`# … вносит его план 10-53 после останова на нём.`).
- **Как поступлено:** заменены обе; акцептанс плана требует, чтобы фраза «из текста ушла», и
  грепом по файлу форм ссылки на будущее остаётся НОЛЬ.
- **Файлы:** `tests/test_templates/test_components.py`
- **Проверка:** `-k "criterion_three"` — 1 passed; полная суита — 3215 passed
- **Коммит:** `ca946b9`

### 2. [Расхождение числа в тексте плана и в дереве — НАЗВАНО, а не сглажено]

- **Найдено при:** задаче 2, действие (4)(г)
- **Что:** план говорит «`prohibitions_unresolved_total: 200` при 221». Реестр
  `10-PROHIBITIONS-SUBJECT.md` несёт `prohibitions_unresolved_total: 200` при **239** запретах
  планов `10-01`…`10-47`; число **221** — счёт ОДИННАДЦАТОГО КРУГА («200 записанных плюс 21
  внесённый девятой партией»), то есть иная величина, а не та же.
- **Как поступлено:** в раздел отложенного внесены ОБА числа с их основаниями, а не одно вместо
  другого. Реестр не правлен ни символом.
- **Файлы:** `.planning/phases/10-rychag-components-modal-html/deferred-items.md`
- **Коммит:** `ca946b9`

---

**Всего отступлений:** 2 (1 правка сверх буквы плана ради его же акцептанса, 1 названное
расхождение чисел). **Влияние:** объём плана не расширен, ни один запрет не нарушен.

## ОСТАТОК — ВОСЕМЬЮ ПУНКТАМИ, БЕЗ СМЯГЧЕНИЯ

1. **Три человеческих пункта раздела «Human Verification Required»** — шаг 2.8 обхода
   (ПРЯМОУГОЛЬНИК плашки `htmx-failure-network` при рабочем положении прокрутки; пункт
   `missing[1]` записи `G-10-7`, окно 77), шаг 4.4 (плашка поверх открытой панели после
   безусловного подъёма плана 10-51) и ПОДПИСЬ ЧЕЛОВЕКА на приёмке `10-UAT.md`. Адресат — план
   10-54 и ЧЕЛОВЕК. **`FORM-06` закрывается именно ПОДПИСЬЮ**, и настоящим планом он не отмечен:
   ни флажок `.planning/REQUIREMENTS.md:31`, ни клетка `:136` (`Gaps Found`) не тронуты.
2. **Запись 75 журнала** — перечень ПРОЧИХ путей, оставляющих сводку объявления устаревшей, не
   снимался; план 10-50 закрыл только ФРАГМЕНТНУЮ ветвь удаления (`G-10-6`). Открыто.
3. **Запись 76 журнала** — две заготовки, показанные одновременно, накрывают друг друга ВСЕГДА
   (после безусловного подъёма); `T-10-51-04`, disposition `accept`, владельца нет; тождественна
   находке ревизии `IN-03`. Открыто.
4. **Запись 78 журнала** — проза заготовок разошлась с деревом ДВУМЯ утверждениями; не починена,
   потому что `app/` запрещён настоящему плану прямым перечнем прохибиций. Настоящий план
   добавляет к ней ТРЕТЬЕ следствие: проза не знает и о решении владельца от 2026-09-12.
5. **Запись 79 журнала** — суита недетерминирована по времени суток (`test_the_overview_error_
   number_matches_the_users_own_dashboard` требует равенства двух чисел, равных лишь часть суток;
   замер владельца: 02:03 UTC red, 05:10 UTC green). Предмет вне Фазы 10, отложено владельцем; в
   прогоне плана правило ИЗЪЯТО ПОИМЁННО и изъятие названо причиной.
6. **Запись 74 журнала** — успешный ФОНОВЫЙ обмен гасит непрочитанный настоящий отказ;
   `T-10-49-05`, disposition `accept`; тождественна находке ревизии `IN-02`. Это ЦЕНА принятой
   владельцем ветви, и она остаётся открытой записью, а не закрывается ответом.
7. **Пункт 4 «Human Verification Required»** — 221 запрет открыт (200 реестра плюс 21 девятой
   партии), `prohibitions_fully_enforced: 0`, разрешение владельца ограничено машинно читаемой
   областью `permit_scope: seventh-batch-only`. Сверх этого — запреты десятой партии, в реестр не
   внесённые. ОТДЕЛЬНОЕ решение владельца.
8. **Находка ревизии `IN-01`** — инвентарь внеполосных блоков поднят ЧИСЛОМ, и отказ печатает два
   числа вместо разницы множеств. Вне объёма партии.

Ни один из восьми пунктов настоящим планом не закрыт и не закрыт частично.

## Issues Encountered

Полный прогон суиты занял 39 минут (3215 passed, 1 deselected, rc=0) — это свойство проекта, а не
плана; изъятие одного правила названо причиной записи 79 и сделано поимённо.

## User Setup Required

None — внешних сервисов план не трогает.

## Next Phase Readiness

- Пункт `missing[0]` записи гэпа по критерию 3 (РЕШЕНИЕ ВЛАДЕЛЬЦА) закрыт записью; пункт
  `missing[1]` закрыт планом 10-52. Оба требуют ВЕРДИКТА следующего круга: `overrides_applied: 0`
  и `status: gaps_found` планом не сдвинуты намеренно — засчитывает запись круг, а не план.
- `G-10-7` остаётся `status: failed`; `10-UAT.md` остаётся `status: partial`; `FORM-06` остаётся
  незакрытым и ждёт подписи человека (адресат — план 10-54 и человек).

## Self-Check: PASSED

- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — НАЙДЕН, `overrides` = 2
- `tests/test_templates/test_components.py` — НАЙДЕН, гейт критерия 3 зелен
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md` — НАЙДЕН, раздел один
- Коммит `ca946b9` — НАЙДЕН в `git log`
- Все семь проверок раздела `<verify>` плана переисполнены и зелены

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-12*
