---
phase: 10-rychag-components-modal-html
plan: 48
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, ПО ТОЙ ЖЕ ПРИЧИНЕ, ЧТО У ПЛАНОВ 10-41…10-45: форма
# `type: tdd` описывается блоком `<feature>` без полей `read_first` и `acceptance_criteria`,
# которых потребитель выхода требует на КАЖДОЙ задаче. Дисциплина RED→GREEN держится задачами.
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-10-A3]
files_modified:
  - .planning/phases/10-rychag-components-modal-html/10-UAT.md
  - tests/test_planning/test_the_walkthrough_stand_is_seedable.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ: `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ, поэтому
# гейт легитимности пакетов неприменим, и это названо, а не умолчано.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена себе.
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "КОМАНДА ПОСЕВА АРТЕФАКТА ИСПОЛНИМА НА СЕГОДНЯШНЕЙ СХЕМЕ: набор полей, которым она строит расписание, проходит ограничение `ck_schedules_active_requires_next_run`, и это показано ВСТАВКОЙ В НАСТОЯЩУЮ СХЕМУ, а не чтением текста"
    - "РАСХОЖДЕНИЕ АРТЕФАКТА СО СХЕМОЙ ПЕРЕСТАЁТ БЫТЬ НЕВИДИМЫМ: правило краснеет, когда команда посева строит строку, которую схема отвергает, — и краснеет ЗА ЧАС, а не следующим обходом"
    - "ПРАВИЛО ЗНАЕТ НЕ ТОЛЬКО СЕГОДНЯШНЕЕ ОГРАНИЧЕНИЕ: ограничение, добавленное схеме завтра на поле, которого команда посева не называет, краснит статическую половину правила, а не проходит молча"
    - "ТРИ ЧИСЛА, СВЯЗАННЫЕ `test_the_walkthrough_cannot_self_certify.py`, НЕ СДВИНУТЫ НИ ОДНО: `checks_declared` остаётся 6, разделов вида «Проверка N» — 6, таблиц отметок — 6"
    - statement: "ПОЛЕ `status` АРТЕФАКТА ОБХОДА ОСТАЁТСЯ НЕТЕРМИНАЛЬНЫМ: подписи человека на приёмке не появилось, и настоящий план её не ставит ни одним символом"
      verification: test
  artifacts:
    - "`.planning/phases/10-rychag-components-modal-html/10-UAT.md` — команда посева § «Как привести стенд в нужное состояние», вычисляющая момент следующего запуска, и запись D-30/D-32 рядом с прежней, а не вместо неё"
    - "`tests/test_planning/test_the_walkthrough_stand_is_seedable.py` — поведенческое правило (вставка построенного артефактом набора полей в настоящую схему) плюс статическая половина (покрытие полей ограничений) плюс отрицательный контроль"
  key_links:
    - "разбор команды посева → предмет правила: единственное, чем «артефакт исполним» отличимо от «в артефакте есть слово»; ломается — правило зеленеет на тексте, который база отвергнет"
    - "вставка в настоящую схему → сила правила: единственное, чем поле, выставленное в `None` ради статической половины, отличимо от вычисленного момента; ломается — починка проходит гейт и роняет следующий обход"
    - "антивакуумное утверждение о непустоте ограничений модели → неваку́умность: единственное, чем «набор полей покрывает ограничения» отличимо от «ограничений нет вовсе»; ломается — правило зеленеет на модели без единого ограничения"
  prohibitions:
    - statement: "ПОЛЕ `status` ШАПКИ `10-UAT.md` НЕ ПРАВИТСЯ НИ НА СИМВОЛ, значения `passed`/`complete` не ставятся ни при каком объёме сделанного: подписи человека на приёмке нет"
      status: flagged-unverified
      verification: test
    - statement: "ТРИ ЧИСЛА, СВЯЗАННЫЕ `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`, НЕ ДВИГАЮТСЯ, И ЗАГОЛОВКОВ ВИДА «### N.» В РАЗДЕЛ «ТРИ ПУНКТА» НЕ ДОБАВЛЯЕТСЯ: предикат `phase uat-passed` читает там ровно три"
      status: flagged-unverified
      verification: test
    - statement: "ПРЕЖНИЕ ЗАПИСИ АРТЕФАКТА НЕ ВЫЧЁРКИВАЮТСЯ И НЕ ПЕРЕПИСЫВАЮТСЯ: дословно приведённый прогон посева, раздел расхождения Р-2 и блоки `walkthrough`/`walkthrough_2`/`walkthrough_3` остаются как есть (идиома D-30/D-32)"
      status: flagged-unverified
      verification: none
    - statement: "КОМАНДА ПОСЕВА НЕ ЗАПУСКАЕТСЯ ИСПОЛНИТЕЛЕМ НА ЖИВОЙ БАЗЕ: рабочее дерево и прод смотрят в ОДНУ базу (замер `walkthrough.environment`), план автономен, и разрешение, унаследованное молча неприсутствующим, разрешением не является. Годность правки показывается ПРАВИЛОМ СУИТЫ на схеме тестовой базы"
      status: flagged-unverified
      verification: test
    - statement: "ПРОДУКТ НЕ ПРАВИТСЯ НИ НА СИМВОЛ: `app/` этим планом не трогается вовсе"
      status: flagged-unverified
      verification: test
---

<objective>
**Обход Фазы 10 третий раз подряд начался с отказа собственного прибора, и на этот раз прибор не
соврал в числах, а НЕ ДАЛ ОБХОДУ НАЧАТЬСЯ.** Команда посева § «Как привести стенд в нужное
состояние» упала первым же действием:

```
CheckViolationError: new row for relation "schedules" violates check constraint
"ck_schedules_active_requires_next_run"
```

Команда сеет `is_active=True` при `next_run_at=None`. Ограничение, отвергающее ровно эту пару,
отгружено коммитом `d46780b` В ТОТ ЖЕ ДЕНЬ, что и обход. Команда написана планом 10-10 (2026-09-03)
и взяла набор полей у `_seed_schedule` тогдашней суиты, где поля `next_run_at` в наборе не было
вовсе. **Дерево ушло вперёд артефакта; артефакт этого не узнал, потому что стерегущего правила у
команды посева нет ни одного.**

⚠️ **ЭТО ДЕФЕКТ АРТЕФАКТА, КАК G-10-A1 И G-10-A2, НО ТЯЖЕЛЕЕ ОБОИХ, И РАЗНИЦА НАЗВАНА ЗАПИСЬЮ ГЭПА.**
Те два врали в ЧИСЛАХ и в формате времени — обход с ними проходился. Этот не даёт снять НИ ОДНОЙ из
шести проверок: без стенда обхода нет. Отсюда `severity: major` у записи дефекта артефакта.

Purpose: следующий обход не должен упираться в то же падение, и — важнее — расхождение артефакта со
схемой обязано краснеть ПРОГОНОМ, а не третьим обходом на боевой базе. G-10-A1 и G-10-A2 закрывались
ПРАВКОЙ ТЕКСТА без стерегущего правила; настоящий гэп показывает цену этого приёма, и потому
закрывается ОБЕИМИ половинами.

Output: исполнимая команда посева и правило, которое исполняет построенный ею набор полей на
НАСТОЯЩЕЙ схеме тестовой базы.

⚠️ **НИ ОДНОЙ ПРАВКИ ПРОДУКТА.** Дефект живёт в записи; `app/` этим планом не трогается.

⚠️ **ПОДПИСИ ЧЕЛОВЕКА НА ПРИЁМКЕ ЭТОТ ПЛАН НЕ СТАВИТ И СТАВИТЬ НЕ МОЖЕТ.** Пункт 3 перечня «ТРИ
ПУНКТА» остаётся ОТКРЫТЫМ; поле `status` шапки терминальным не становится.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-UAT.md
@.planning/phases/10-rychag-components-modal-html/10-34-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**Новый модуль `tests/test_planning/test_the_walkthrough_stand_is_seedable.py`:**

- `pytestmark = pytest.mark.planning` — предмет модуля есть ЗАПИСЬ проекта (обязательство
  `tests/test_planning/__init__.py`: помечены ВСЕ модули каталога до единого);
- `_seed_program(text)` — извлекает тело программы `uv run python -c '…'` из раздела посева;
- `_seed_schedule_loop(program)` — находит в разобранном дереве РОВНО ОДИН цикл, чьё тело строит
  `Schedule(...)`, и возвращает его вместе с набором имён-ключей;
- `test_the_seed_program_parses_and_builds_exactly_one_schedule` — антивакуум разбора;
- `test_the_seeded_row_satisfies_todays_schema` — ПОВЕДЕНЧЕСКОЕ: цикл артефакта исполняется, и
  построенные им строки вставляются в настоящую схему тестовой базы;
- `test_every_checked_column_is_named_by_the_seed_program` — СТАТИЧЕСКАЯ половина: каждое поле,
  названное ограничением модели, названо ключом в вызове `Schedule(...)`;
- `test_control_a_seed_program_without_the_next_run_reddens` — отрицательный контроль на
  доктóренной копии.

**Изменённый артефакт:** `10-UAT.md` — команда посева § «Как привести стенд в нужное состояние» и
приписка D-30/D-32 рядом с прежними записями раздела «Развилка посева».
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: правило исполнимости команды посева на сегодняшней схеме — RED на нетронутом артефакте</name>

  <files>
tests/test_planning/test_the_walkthrough_stand_is_seedable.py
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:265-341` — раздел «Как привести стенд
  в нужное состояние» целиком, включая оба предупреждения об общем префиксе и о числе «три», и
  команду посева дословно. ⚠️ ЧИТАТЬ ДОСЛОВНО АБЗАЦ ПРО ЗНАЧЕНИЯ ПОЛЕЙ: они взяты у `_seed_schedule`
  суиты и НЕ ВЫВЕДЕНЫ ЗАНОВО — «второй набор умолчаний разошёлся бы с первым молча». Основание
  верно и остаётся; сломалось не оно, а то, что источник набора с тех пор пополнился полем.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md`, запись гэпа `G-10-A3` в разделе
  `## Gaps` — поля `reason`, `root_cause`, `missing`, `workaround_used`. Диагноз ЗАМЕРЕН обходом;
  переставлять его заново ЗАПРЕЩЕНО — правило строится НА нём.
- `tests/test_pages/test_editor_schedules.py:113-140` — `_seed_schedule` суиты СЕГОДНЯ, вместе с
  комментарием о том, почему момент запуска ставится только включённой строке. Это и есть тот
  источник, который ушёл вперёд артефакта.
- `app/models/schedule.py:16-80` — `ACTIVE_REQUIRES_NEXT_RUN`, `ACTIVE_REQUIRES_NEXT_RUN_NAME`,
  `__table_args__` и объявления колонок. ⚠️ ЧИТАТЬ ШАПКУ ЦЕЛИКОМ: там записано, почему запрещена
  ровно ОДНА пара из четырёх, и это определяет, чем правило имеет право быть удовлетворено.
- `app/services/schedule_service.py:1-50` — сигнатура и возврат `compute_next_run_at`; пустой список
  дней либо времён даёт `None`, непустые — момент в UTC.
- `tests/test_planning/__init__.py` — обязательство маркера и граница его годности.
- `tests/conftest.py`, фикстура `db_session` — движок и создание схемы на прогон.
  </read_first>

  <behavior>
1. Разбор находит в разделе посева РОВНО ОДНУ программу и РОВНО ОДИН цикл, строящий `Schedule(...)`;
   на артефакте, где программы нет или циклов два, правило краснеет, а не выбирает молча.
2. Вставка строк, построенных циклом артефакта, в настоящую схему тестовой базы СЕГОДНЯ ПАДАЕТ на
   `ck_schedules_active_requires_next_run` — это и есть измеренный RED.
3. Множество полей, названных ограничениями модели, непусто (антивакуум), и сегодня оно НЕ покрыто
   ключами вызова `Schedule(...)` артефакта.
  </behavior>

  <action>
**(1) RED ПЕРВЫМ, И ОН ЗАПИСЫВАЕТСЯ ЗАМЕРОМ, А НЕ ОБЕЩАНИЕМ.** Завести модуль
`tests/test_planning/test_the_walkthrough_stand_is_seedable.py` с `pytestmark = pytest.mark.planning`
и прогнать его на НЕТРОНУТОМ артефакте. В сводку попадают: команда, код возврата, имя правила, текст
отказа базы дословно. Без этого замера «правило работает» неотличимо от «правило зеленеет вакуумом».

**(2) РАЗБОР — ПО ФОРМЕ, А НЕ ПО ТЕКСТУ ШАГА.** `_seed_program(text)` вырезает раздел между
заголовком «Как привести стенд в нужное состояние» и следующим заголовком того же уровня, берёт из
него блоки в тройных кавычках, отбирает тот, который несёт вызов интерпретатора с ключом `-c`, и
возвращает тело программы. Программ в разделе обязано быть РОВНО ОДНА — иначе отказ с именем числа.
⚠️ ХРУПКОСТЬ ЭТОГО РАЗБОРА НАЗЫВАЕТСЯ В ДОКСТРИНГЕ ПРЯМО, А НЕ ОСТАВЛЯЕТСЯ ЧИТАТЕЛЮ: правило
привязано к ФОРМЕ раздела, и перестройка раздела уронит его отказом о разборе. Это принято сознательно
— альтернатива (правило, выбирающее блок молча) вернула бы ровно тот класс, который гэп и есть.

**(3) РАЗБОР ПРОГРАММЫ — ДЕРЕВОМ, А НЕ ОБРАЗЦОМ СТРОКИ.** `_seed_schedule_loop(program)` разбирает
тело в дерево разбора языка, обходит его и находит циклы, чьё тело содержит вызов с именем `Schedule`.
Циклов обязан быть РОВНО ОДИН. Возвращаются: сам узел цикла и множество имён-ключей вызова.

**(4) ПОВЕДЕНЧЕСКАЯ ПОЛОВИНА — ИСПОЛНЕНИЕ ЦИКЛА АРТЕФАКТА НА НАСТОЯЩЕЙ СХЕМЕ.**
`test_the_seeded_row_satisfies_todays_schema` (асинхронное, фикстура `db_session`):
  - сеет настоящего пользователя, аккаунт и объявление теми же моделями, что и суита;
  - собирает пространство имён из НАСТОЯЩИХ символов: класс `Schedule`, `compute_next_run_at`,
    простые объекты-носители идентификаторов под именами `ad` и `acc`, и накопитель под именем `s`
    с методом добавления;
  - компилирует и исполняет РОВНО узел цикла, добытый шагом (3);
  - кладёт накопленные строки в сессию и фиксирует запись.
⚠️ **ИСПОЛНЕНИЕ ТЕКСТА АРТЕФАКТА НАЗЫВАЕТСЯ СВОИМ ИМЕНЕМ В ДОКСТРИНГЕ, А НЕ ПРЯЧЕТСЯ.** Исполняется
файл, лежащий в том же дереве и проходящий ту же ревизию, что исходники; исполняется РОВНО ОДИН узел
цикла, а не программа целиком (иначе правило построило бы движок по адресу боевой базы — ровно то,
что прохибиция плана запрещает). Граница названа: никакой строки, кроме тела цикла, правило не
исполняет.

**(5) АНТИВАКУУМ ПЕРЕД ПОЛОЖИТЕЛЬНОЙ ПОЛОВИНОЙ.** До вставки утверждается, что накопитель получил
НЕ НОЛЬ строк: цикл, не построивший ничего, дал бы зелёную вставку пустоты, и «команда исполнима»
было бы неотличимо от «команда ничего не сеет».

**(6) ЧТО ПРАВИЛО НЕ ДОКАЗЫВАЕТ — ЗАПИСАТЬ В ДОКСТРИНГ.** Оно не доказывает, что команда посева
отработает НА БОЕВОЙ БАЗЕ: схема тестовой базы создаётся из моделей, а боевая живёт ревизиями. Оно
доказывает ровно одно: набор полей, которым артефакт строит расписание, СЕГОДНЯШНЕЙ СХЕМЕ МОДЕЛЕЙ не
противоречит. Совпадение двух половин схемы держат другие правила
(`tests/test_models/test_schedule_active_requires_next_run.py`,
`tests/test_migrations/test_0022_schedules_active_requires_next_run.py`) — и они названы здесь, а не
подразумеваются.
  </action>

  <acceptance_criteria>
- Модуль несёт `pytestmark = pytest.mark.planning` — иначе отрицательный отбор `-m "not planning"`
  собрал бы правило записи, объявив его продуктовым.
- `test_the_seeded_row_satisfies_todays_schema` на НЕТРОНУТОМ артефакте КРАСЕН, и в сводке приведены
  команда, код возврата и текст отказа базы дословно.
- `test_the_seed_program_parses_and_builds_exactly_one_schedule` ЗЕЛЁН на нетронутом артефакте: разбор
  находит одну программу и один цикл — иначе красен был бы разбор, а не предмет.
- Антивакуумное утверждение о непустом накопителе стои́т ДО вставки, и это видно чтением диффа.
- Докстринг называет: (а) хрупкость разбора по форме раздела, (б) границу исполнения (только узел
  цикла), (в) чего правило не доказывает, с именами двух правил-соседей.
- `git diff --numstat -- app` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/test_the_walkthrough_stand_is_seedable.py -q -k "parses"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed"</fails_when>
    <automated>uv run pytest tests/test_planning/test_the_walkthrough_stand_is_seedable.py -q -k "todays_schema"; test $? -ne 0</automated>
    <fails_when>команда завершается ненулевым кодом — то есть правило на нетронутом артефакте ЗЕЛЕНО, и RED не измерен: правило зеленеет там, где обход упал</fails_when>
    <automated>git diff --numstat -- app</automated>
    <fails_when>вывод НЕПУСТ — продукт тронут, а задача правит только каталог правил записи</fails_when>
  </verify>

  <done>
Правило заведено и на нетронутом артефакте КРАСНО ровно тем отказом, на котором упал обход; разбор
зелен и антивакуумен; границы правила названы в докстринге; продукт не тронут.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: команда посева вычисляет момент следующего запуска — GREEN, и прежняя запись остаётся рядом</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:301-341` — команда посева дословно и
  строка ожидаемого вывода под ней.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:411-458` — раздел «Развилка посева —
  ПОСЕВ ИСПОЛНЕН» ЦЕЛИКОМ, включая приписку о правке выражения форматирования часа (план 10-34) и
  ТРИ основания запрета на запуск команды исполнителем плана. ⚠️ ЭТИ ТРИ ОСНОВАНИЯ ОСТАЮТСЯ В СИЛЕ
  ДОСЛОВНО и настоящим планом НЕ ПЕРЕСМАТРИВАЮТСЯ: дерево и прод смотрят в одну базу.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1-30` — шапка и оба предупреждения о
  запрете терминального значения поля состояния.
- `app/pages/schedules.py:880-910` — образец боевого вызова `compute_next_run_at` при создании
  расписания. Набор аргументов берётся ОТСЮДА, а не выводится заново.
- `tests/test_planning/test_the_walkthrough_cannot_self_certify.py:100-115` — три образца, которыми
  связаны три числа артефакта.
  </read_first>

  <behavior>
1. Программа посева импортирует вычислитель момента из службы расписаний и передаёт вычисленное
   значение полем `next_run_at` в тот же вызов `Schedule(...)`.
2. Правило задачи 1 становится ЗЕЛЁНЫМ, и это переход цвета, а не первый зелёный прогон.
3. Три числа, связанные защитой обхода от самозаверения, остаются 6, 6 и 6; заголовков вида «### N.»
   в разделе «ТРИ ПУНКТА» остаётся три.
  </behavior>

  <action>
**(1) GREEN — ПРАВКА КОМАНДЫ ПОСЕВА.** В программу § «Как привести стенд в нужное состояние»
добавляется импорт вычислителя момента из службы расписаний (`app.services.schedule_service`), а в
тело цикла — вычисленное значение поля момента следующего запуска. Набор дней, набор времён и часовой
пояс, поданные вычислителю, обязаны быть ТЕМИ ЖЕ ЗНАЧЕНИЯМИ, что уезжают в одноимённые поля строки:
второй независимый набор разошёлся бы с первым молча — ровно то основание, которым артефакт уже
объяснил, почему значения полей взяты у суиты, а не выведены заново. Практически это значит, что
список времён вычисляется ОДИН РАЗ и подаётся обоим потребителям.

**(2) ЗАПИСЬ D-30/D-32 ВСТАЁТ РЯДОМ, А НЕ ВМЕСТО.** В раздел «Развилка посева — ПОСЕВ ИСПОЛНЕН»
дописывается приписка по форме, которой в том же разделе уже записана правка выражения форматирования
часа. Приписка обязана назвать ЧЕТЫРЕ вещи и ни одной из них не смягчить:
  - дословно приведённый прогон посева шёл НАБОРОМ ПОЛЕЙ БЕЗ момента следующего запуска, и на
    сегодняшней схеме он упал бы — то есть приведённый прогон верен ДЛЯ СВОЕГО ДНЯ и устарел;
  - источник расхождения поимённо: ограничение `ck_schedules_active_requires_next_run`, коммит
    `d46780b`, 2026-09-11, тот же день, что и обход;
  - прежнее значение ОШИБКОЙ НЕ БЫЛО — оно УСТАРЕЛО: набор полей взят у `_seed_schedule` суиты
    2026-09-03, где поля момента в наборе не было вовсе;
  - ЧЕМ ТЕПЕРЬ СТЕРЕЖЁТСЯ: именем правила из задачи 1, а не бдительностью читателя. ⚠️ ЭТО И ЕСТЬ
    РАЗНИЦА С G-10-A1 И G-10-A2: те закрыты правкой текста без стерегущего правила, и настоящий гэп
    показывает цену того приёма.
⚠️ **НИ ОДНА ПРЕЖНЯЯ СТРОКА РАЗДЕЛА НЕ ВЫЧЁРКИВАЕТСЯ И НЕ ПРАВИТСЯ**, включая дословный вывод
прогона и три основания запрета на запуск команды.

**(3) ЗАПИСЬ В ПОЛЕ `workaround_used` ГЭПА НЕ СТИРАЕТСЯ.** Строка «текст команды в § „Как привести
стенд“ НЕ ПРАВЛЕН настоящей записью, и следующий обход упрётся в то же падение, пока гэп не закрыт
планом» была ВЕРНА в момент записи. Рядом с ней встаёт короткая приписка: команда правлена настоящим
планом, и обещание, данное записью гэпа, исполнено. Прежняя строка остаётся дословно.

**(4) КОМАНДА ПОСЕВА НЕ ЗАПУСКАЕТСЯ.** Годность правки показывается ПРАВИЛОМ СУИТЫ (задача 1), а не
прогоном по `DATABASE_URL` из `.env`: три основания запрета, записанные самим артефактом, действуют
дословно, и план автономен. ⚠️ Захочет прогона человек — он делает его САМ, за своей клавиатурой.

**(5) ТРИ ЧИСЛА ПЕРЕСНИМАЮТСЯ ПОСЛЕ ПРАВКИ, А НЕ ОБЕЩАЮТСЯ.** Значение `checks_declared`, счёт
разделов вида «Проверка N», счёт таблиц отметок и счёт заголовков вида «### N.» внутри раздела «ТРИ
ПУНКТА» снимаются командами и приводятся в сводке числами. Замер ДО правки: 6, 6, 6 и 3.
  </action>

  <acceptance_criteria>
- Правило `test_the_seeded_row_satisfies_todays_schema` ЗЕЛЕНО; в сводке названы ОБА состояния —
  красное до правки и зелёное после, с командами и кодами возврата.
- В разделе посева вычислитель момента назван РОВНО ОДИН РАЗ в импорте и РОВНО ОДИН РАЗ в вызове;
  список времён строится один раз и подаётся обоим потребителям — проверяется чтением диффа.
- Приписка D-30/D-32 присутствует и называет все четыре вещи, перечисленные в действии (2); ни одна
  прежняя строка раздела «Развилка посева» не вычеркнута и не переписана.
- `grep -c '^checks_declared: 6$'` по артефакту даёт 1; счёт разделов вида «Проверка N» даёт 6; счёт
  таблиц отметок даёт 6; счёт заголовков вида «### N.» внутри раздела «ТРИ ПУНКТА» даёт 3.
- Поле `status` шапки не тронуто.
- `git diff --numstat -- app tests` пуст: задача правит ТОЛЬКО артефакт обхода.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — защита обхода от самозаверения либо правило исполнимости стенда красно</fails_when>
    <automated>uv run python -c "import pathlib,re; t=pathlib.Path('.planning/phases/10-rychag-components-modal-html/10-UAT.md').read_text(encoding='utf-8'); print(len(re.findall(r'^checks_declared: 6$',t,re.M)), len(re.findall(r'^## Проверка \d+',t,re.M)), len(re.findall(r'^### Отметка\b',t,re.M)), len(re.findall(r'^### \d+\.', t[t.index('## ТРИ ПУНКТА'):t.index('## Gaps')], re.M)))"</automated>
    <fails_when>напечатано что-либо, кроме `1 6 6 3` — сдвинуто одно из трёх связанных чисел либо добавлен заголовок в раздел, где предикат фазы читает ровно три</fails_when>
    <automated>git diff --numstat -- app tests</automated>
    <fails_when>вывод НЕПУСТ — тронут продукт или суита, а задача правит только артефакт обхода</fails_when>
  </verify>

  <done>
Команда посева вычисляет момент следующего запуска и проходит ограничение схемы, что показано
переходом цвета правила; прежние записи раздела «Развилка посева» стоя́т дословно, рядом с ними —
приписка с названным источником расхождения; три связанных числа не сдвинуты.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: зубы правила — покрытие полей ограничений и отрицательный контроль</name>

  <files>
tests/test_planning/test_the_walkthrough_stand_is_seedable.py
  </files>

  <read_first>
- `tests/test_planning/test_the_walkthrough_stand_is_seedable.py` — собственный модуль после задачи 1.
- `app/models/schedule.py:46-80` — текст условия ограничения и имена колонок. ⚠️ Имена полей в
  правило ВЫПИСЫВАТЬСЯ НЕ ДОЛЖНЫ: они ДОБЫВАЮТСЯ из `Schedule.__table__.constraints`, иначе правило
  согласится с любой правкой модели — тот же приём, которым `test_shell.py` читает оба слоя из
  таблицы стилей и не знает наперёд ни одного.
- `tests/test_pages/test_shell.py:3337-3400` — образец ОТРИЦАТЕЛЬНОГО КОНТРОЛЯ на доктóренной копии:
  копия готовится в памяти либо во временном каталоге, настоящий файл не правится.
  </read_first>

  <behavior>
1. Множество полей, названных ограничениями модели, непусто (антивакуум) и СЕГОДНЯ покрыто ключами
   вызова `Schedule(...)` артефакта — после задачи 2.
2. Копия программы посева, из которой изъят ключ момента следующего запуска, КРАСНИТ обе половины:
   и статическую (поле не названо), и поведенческую (вставка отвергнута схемой).
3. Копия, где ключ момента назван, но его значением стои́т пустое значение, КРАСНИТ ПОВЕДЕНЧЕСКУЮ
   половину и ПРОХОДИТ статическую — и это показано, а не заявлено: ровно здесь статическая половина
   в одиночку была бы дырой.
  </behavior>

  <action>
**(1) СТАТИЧЕСКАЯ ПОЛОВИНА — ПОЛЯ ОГРАНИЧЕНИЙ ДОБЫВАЮТСЯ, А НЕ ВЫПИСЫВАЮТСЯ.**
`test_every_checked_column_is_named_by_the_seed_program` собирает имена колонок, упомянутых
ограничениями таблицы модели, и требует, чтобы каждое было названо ключом вызова `Schedule(...)` в
программе артефакта. ⚠️ АНТИВАКУУМ ПЕРВЫМ: множество имён обязано быть НЕПУСТЫМ, иначе «все поля
покрыты» неотличимо от «ограничений нет вовсе».
⚠️ ЗАЧЕМ ЭТА ПОЛОВИНА ПРИ ЖИВОЙ ПОВЕДЕНЧЕСКОЙ. Затем, что она краснеет на ограничении, добавленном
ЗАВТРА на поле, которого команда не называет, — даже если сегодняшние значения случайно его
удовлетворяют. Поведенческая половина ловит нарушение СЕГОДНЯШНИХ значений, статическая — умолчание о
НОВОМ поле. Ни одна из двух не покрывает предмет другой, и это записывается в докстринги обеих.

**(2) ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ — ДВА ДОКТÓРИВАНИЯ, И ВТОРОЕ НЕ ИЗБЫТОЧНО.**
`test_control_a_seed_program_without_the_next_run_reddens` готовит копию текста артефакта В ПАМЯТИ
(настоящий файл не правится ни на символ) и прогоняет по ней обе половины:
  - доктóривание (а): ключ момента изъят из вызова целиком → красны ОБЕ половины;
  - доктóривание (б): ключ момента назван, значением стои́т пустое → статическая половина ЗЕЛЕНА,
    поведенческая КРАСНА.
⚠️ ВТОРОЕ ДОКТÓРИВАНИЕ ЕСТЬ ГРАНИЦА МЕЖДУ «ПОЛЕ НАЗВАНО» И «СТРОКА ЗАКОННА», и без него правило
приняло бы починку, которая роняет следующий обход ровно тем же отказом. Ожидания контролей
выписываются ЛИТЕРАЛАМИ, а не вычисляются из проверяемого источника.

**(3) ПОЛНЫЙ ПРОГОН СУИТЫ.** Прогнать `uv run pytest tests/` целиком и привести в сводке итоговую
строку. ⚠️ ЭТО НЕ ФОРМАЛЬНОСТЬ: правило каталога `tests/test_planning/` читает ОБЩЕЕ дерево записи, и
правка артефакта обхода способна задеть соседние правила того же каталога.

**(4) ЗАПИСАТЬ В СВОДКУ ЦЕНУ, КОТОРУЮ ПРАВИЛО НЕ ЗАКРЫВАЕТ.** Артефакт обхода — не единственная
запись проекта, несущая исполнимый текст по живым моделям. Настоящее правило стережёт РОВНО ОДНУ
команду РОВНО ОДНОГО артефакта; остальные записи с исполнимым текстом не стережёт никто. Это
называется прямо и передаётся следующему кругу, а не выдаётся за общее решение.
  </action>

  <acceptance_criteria>
- Имена полей ограничений добываются из таблицы модели; литералов имён колонок в правиле НЕТ —
  проверяется чтением диффа.
- Антивакуумное утверждение о непустом множестве полей стои́т ДО положительного утверждения.
- Оба доктóривания контроля краснят то, что объявлено: (а) обе половины, (б) только поведенческую.
  Ожидаемые состояния названы в сводке для каждого доктóривания отдельно.
- Настоящий `10-UAT.md` контролем НЕ ПРАВИТСЯ: копия готовится в памяти — проверяется тем, что
  `git status --porcelain .planning/` после прогона контроля пуст.
- `uv run pytest tests/` зелен; итоговая строка приведена в сводке числом.
- В сводке названа граница: стережётся одна команда одного артефакта, остальные записи с исполнимым
  текстом остаются без правила.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/test_the_walkthrough_stand_is_seedable.py -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо отобрано меньше правил, чем перечислено в разделе артефактов плана</fails_when>
    <automated>uv run pytest tests/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed" — партия обязана оставить суиту зелёной целиком</fails_when>
    <automated>git status --porcelain .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод показывает НЕЗАФИКСИРОВАННУЮ правку после прогона контроля — контроль тронул настоящий артефакт вместо копии в памяти</fails_when>
  </verify>

  <done>
У правила две половины с названными непересекающимися предметами и отрицательный контроль с двумя
доктóриваниями, второе из которых закрывает дыру статической половины; полная суита зелена; граница
покрытия названа.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| текст артефакта → интерпретатор правила | правило исполняет узел дерева разбора, добытый из файла записи |
| запись проекта → живая база | команда посева строит движок по `DATABASE_URL`, и дерево смотрит в ту же базу, что прод |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-48-01 | Elevation of Privilege | `tests/test_planning/test_the_walkthrough_stand_is_seedable.py` — исполнение текста артефакта | medium | mitigate | исполняется РОВНО узел цикла, добытый разбором дерева, а не программа целиком; движок по `DATABASE_URL` правилом не строится; граница названа в докстринге и проверяется чтением диффа |
| T-10-48-02 | Tampering | `10-UAT.md` — запись состояния обхода | high | mitigate | поле `status` не правится; три числа, связанные `test_the_walkthrough_cannot_self_certify.py`, переснимаются командой в `<verify>` задачи 2 |
| T-10-48-03 | Denial of Service | боевая база через команду посева | high | mitigate | прохибиция плана запрещает запуск команды исполнителем; годность показывается правилом на схеме тестовой базы, гейт `git diff --numstat -- app` держит границу правки |
| T-10-48-04 | Information Disclosure | вывод команды посева (почта владельца) | low | accept | поле печатается артефактом сознательно — человеку надо знать, под кем заходить; команда исполнителем не запускается вовсе |
| T-10-48-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера — гейт легитимности пакетов неприменим, и это названо, а не умолчано |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q` — зелено; правило исполнимости стенда и защита обхода от
   самозаверения стоя́т рядом и не спорят.
2. `uv run pytest tests/ -q` — зелено целиком.
3. Три числа артефакта переснято и равны `1 6 6 3`.
4. `git diff --numstat -- app` пуст на всех трёх задачах: продукт не тронут.
5. Поле `status` шапки `10-UAT.md` не тронуто; подписи человека на приёмке план не ставит.
</verification>

<success_criteria>
- Команда посева § «Как привести стенд в нужное состояние» исполнима на сегодняшней схеме, и это
  показано ВСТАВКОЙ построенных ею строк в настоящую схему, а не чтением текста.
- Переход цвета правила измерен и приведён в сводке обоими состояниями.
- Отрицательный контроль краснеет на обеих формах негодной починки, включая ту, что называет поле и
  оставляет его пустым.
- Прежние записи артефакта стоя́т дословно; рядом с ними — приписка с названным источником расхождения.
- Полная суита зелена.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-48-SUMMARY.md` when done
</output>
