---
phase: 10-rychag-components-modal-html
plan: 20
type: execute
wave: 2
# ⚠️ ЗАВИСИМОСТЬ ОТ 10-17 — НЕ ПО ФАЙЛУ, А ПО ПРАВИЛУ, И ЭТО НАЗВАНО ПРЯМО. Настоящий план
# заводит НОВЫЙ модуль в каталоге `tests/test_planning/`, а план 10-17 заводит правило,
# ОБХОДЯЩЕЕ этот каталог и судящее форму его модулей. План, чья приёмка ограничена правилом
# другого плана, обязан идти ПОСЛЕ него: иначе оба приземлятся в одну волну, и нарушение
# обнаружится прогоном волны, а не чтением плана.
depends_on: ["10-17"]
gap_closure: true
files_modified:
  - tests/test_planning/test_the_walkthrough_cannot_self_certify.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ — форма унаследована
# от планов 10-12…10-19. ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `workflow.security_asvs_level` → `1`;
# `workflow.security_block_on` → `"high"`; `workflow.security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "АРТЕФАКТ РУЧНОГО ОБХОДА НЕ МОЖЕТ САМОЗАВЕРИТЬСЯ: состояние `passed` в шапке обхода требует ЗАПОЛНЕННОЙ отметки у КАЖДОЙ проверки, и артефакт, объявивший себя пройденным при пустых таблицах, краснит прогон"
    - "ПРАВИЛО ЗЕЛЕНО ВО ВСЕХ ЗАКОННЫХ СОСТОЯНИЯХ ДЕРЕВА, А НЕ В СЕГОДНЯШНЕМ: `testing` с пустыми таблицами и `passed` с заполненными проходят ОБА, и это ЗАМЕРЕНО прогоном на синтетике, а не объявлено, — урок блокера четвёртого круга применён к правилу, которое его же и закрывает"
    - "ЧИСЛО ПРОВЕРОК ОБХОДА СХОДИТСЯ ТРЕМЯ СЧЁТАМИ: объявленное шапкой, число разделов проверок и число таблиц отметок равны между собой, поэтому проверка не может исчезнуть из обхода молча"
    - "ШАГ ОБХОДА НЕ МОЖЕТ ССЫЛАТЬСЯ НА УЗЕЛ, КОТОРОГО В РАЗМЕТКЕ НЕТ: якоря наблюдения объявлены реестром с указанием файла-источника, и якорь, пропавший из шаблона, краснит прогон — иначе обход обесценивается при первом же прохождении, и человек ищет несуществующую причину"
    - "РЕЕСТР ЯКОРЕЙ НЕСЁТ ОБЪЯВЛЕННОЕ ЧИСЛО С ЛЕТОПИСЬЮ: молча похудевший реестр означает, что наблюдение сняли, не тронув обхода; молча выросший — что якорь добавили, не назвав, какой шаг его читает"
    - "ОБА ПРАВИЛА ПОКАЗЫВАЮТ ЗУБЫ НА СИНТЕТИЧЕСКОМ ИСТОЧНИКЕ: артефакт с состоянием `passed` при пустой таблице и шаблон без объявленного якоря краснят разборщик, и файлы дерева при этом не правятся"
    - statement: "Шесть проверок обхода `10-UAT.md` пройдены человеком на локальном стенде в браузере, и шесть таблиц отметок несут НАБЛЮДЁННЫЕ признаки, а не заполнены заранее"
      verification: backstop
    - statement: "Критерий 2 роадмапа — «после N удалений в документе не остаётся N мёртвых диалогов» — наблюдён рантаймом браузера, а не выведен из присутствия узлов в теле ответа"
      verification: backstop
  artifacts:
    - "tests/test_planning/test_the_walkthrough_cannot_self_certify.py — НОВЫЙ модуль: правило «состояние обхода не шире его собственных отметок», правило трёх счётов и правило якорей наблюдения"
    - "tests/test_planning/test_the_walkthrough_cannot_self_certify.py — реестр якорей наблюдения с объявленным числом и указанием файла-источника у каждого"
    - "tests/test_planning/test_the_walkthrough_cannot_self_certify.py — три контроля зубов на синтетических источниках"
  key_links:
    - "состояние шапки обхода → его собственные таблицы отметок: единственное машинное принуждение того правила, которое артефакт записал о себе сам («отметка без наблюдённого признака закрытием не считается»); ломается — непройденный обход превращается в закрытое требование, то есть повторяется коммит `0ea886d`"
    - "реестр якорей → шаблоны продукта: единственное, чем шаг обхода не обесценится при первом прохождении; ломается — человек ищет несуществующий узел и записывает отказ, которого нет"
    - "три счёта проверок → перечень `human_verification` отчёта верификации: единственное, чем проверка не исчезнет из обхода молча; ломается — обход зеленеет, обойдя меньше, чем объявил"
  prohibitions:
    - statement: "правило НЕ утверждает, что обход ПРОЙДЕН, и не может этого утверждать: оно утверждает, что ЗАПИСЬ о прохождении не шире собственных отметок артефакта. Правило, объявившее прохождение, было бы самозаверением на один уровень выше — ровно тем, что оно призвано запретить"
      status: flagged-unverified
      verification: none
    - statement: "правило НЕ вшивает сегодняшнее состояние обхода: литерала `testing` в утверждении нет, и зелень не держится на том, что обход ещё не пройден, — иначе повторился бы дословно блокер четвёртого круга, закрываемый планом 10-17"
      status: flagged-unverified
      verification: none
    - statement: "таблицы отметок НЕ заполняются ни планом, ни исполнителем, ни правилом: отметка без наблюдённого признака есть самозаверение, а не приёмка, и это записал сам артефакт"
      status: flagged-unverified
      verification: none
    - statement: "машинная подмена разметочного рантайма НЕ заводится: правило судит ЗАПИСЬ обхода и ПРИСУТСТВИЕ якорей в разметке, и ни одной строкой не утверждает, что рантайм что-либо применил"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ НАД FORM-06 — ЯВНЫЕ ДОПУЩЕНИЯ ПАРТИИ (полная запись — 10-17-PLAN.md):
  #   • `idempotency` — продуктовая половина закрыта планом 10-06; повтор в РАНТАЙМЕ браузера
  #     наблюдаем только обходом `10-UAT.md`, шаги 1.4-1.9, не пройденные ни разу. `unresolved`.
  #   • `concurrency` — адресован частично планом 10-07 и проверками 2 и 3 обхода; полного
  #     машинного критерия в суите без процесса выразить нечем. `unresolved`.
  # 2 предъявленных = 0 разрешённых критерием + 2 явных допущения.
---

<objective>
Закрывается МАШИННАЯ ПОЛОВИНА третьего гэпа партии — перечня `human_verification` из СЕМИ пунктов,
все из которых живут шагами `10-UAT.md`.

⚠️ **ЧТО ИМЕННО ЗАКРЫВАЕТСЯ, А ЧТО НЕТ — СКАЗАНО ПРЯМО, ПОТОМУ ЧТО СМЕШАТЬ ЭТИ ДВЕ ВЕЩИ И ЕСТЬ
ГЛАВНАЯ ОПАСНОСТЬ ПЛАНА.** Сам обход человеческий и остаётся человеческим: артефакт заведён планом
10-10, полон, дополнен планом 10-16 и **НИ РАЗУ НЕ ПРОЙДЕН** (`status: testing`, шесть таблиц
отметок пусты). Настоящий план обхода НЕ ПРОХОДИТ и пройденным НЕ ОБЪЯВЛЯЕТ. Он закрывает ровно то,
что машине доступно и что до сих пор не стерёг никто:

1. **ЗАПИСЬ О ПРОХОЖДЕНИИ НЕ МОЖЕТ СТАТЬ ШИРЕ ОТМЕТОК.** Артефакт записал о себе правило своими
   словами — «отметка без наблюдённого признака закрытием не считается», «значение `status: passed`
   до этого ставить ЗАПРЕЩЕНО», — и ПРИНУЖДЕНИЯ у этого правила нет ни одного. Ровно так и выглядел
   предмет плана 10-14 месяцем раньше: регламент был, принуждения не было, и его обошёл коммит
   `0ea886d`.
2. **СОГЛАСИЕ ОЖИДАНИЙ ОБХОДА С ДЕРЕВОМ.** Шаги называют узлы разметки поимённо
   (`[id^="sched-del-"]`, `#sched-count`, `#text-counter`, `[data-sched-card]` и другие). Шаг,
   называющий узел, которого в шаблоне нет, не проверяет НИЧЕГО и обесценивает обход при первом же
   прохождении — человек записывает отказ, которого нет, и ищет несуществующую причину.

Purpose: у этих двух свойств есть машинный критерий, они не требуют ни рантайма, ни браузера, и
сегодня их не стережёт ничто. Всё остальное содержание перечня `human_verification` машине
недоступно и настоящим планом НЕ ЗАКРЫВАЕТСЯ — это записано отдельным разделом ниже и повторено
планом 10-23 в самом артефакте.

Output: новый модуль гейтов записи с тремя правилами (состояние не шире отметок; три счёта проверок
сходятся; якоря наблюдения существуют в разметке) и тремя контролями зубов на синтетических
источниках.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-UAT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@tests/test_planning/test_requirement_completion_follows_verification.py
</context>

<not_closed_by_this_plan>
## Чего этот план НЕ закрывает — названо прямо, а не оставлено умолчанию

| Пункт перечня `human_verification` | Почему не закрывается здесь |
|---|---|
| Критерий 2 — счёт диалогов и ловушка фокуса после N удалений (шаги 1.1-1.9) | Применяет внеполосные узлы РАНТАЙМ разметки. Суита идёт через транспорт без процесса на встроенной базе и не исполняет ни строчки JS; браузерных зависимостей в дереве нет ни одной. Закрывается ПРОХОЖДЕНИЕМ проверки 1 человеком |
| Переходная ветка и живость клиентского слоя (шаги 1.10-1.15) | Наблюдается только рантаймом браузера: интерпретатор не свопает содержимое узла и не пересоздаёт узел скрипта. Машинная половина `CR-03` доказана планом 10-13 ИСПОЛНЕНИЕМ исходника дважды в одной области имён — и её граница записана там же |
| Уборка панели при не доехавшем follow-up GET (проверка 2, окно 28) | Наблюдение отличается от прежнего ИМЕННО рантаймом: правкой плана 10-05 пункт адресован, машинно не закрыт |
| Панель ЗАНЯТА, а не сломана, на медленной сети (проверка 3, окно 35) | Свойство доказано в интерпретаторе и мутацией (план 10-07); что человек ВИДИТ панель занятой — снимается глазом |
| Панель остаётся открытой на отказе (проверка 4, окно 22) | В суите доказана на реальной форме события отказа; на живом стенде — глазом |
| Приземление фокуса после ухода панели (проверка 5) | Поднимает клиентское состояние и настоящий своп. ⚠️ Читать вместе с `WR-04` ревизии: на фрагментном пути область уведомлений пуста ВСЕГДА, и это записано планом 10-22 пометкой в шапке рычага |
| Документная половина закрытия `DEF-09-02` (проверка 6, окно 36) | Прецедент разведения половин D-33 Фазы 5: документная половина судится человеком |

⚠️ **ОТМЕТКА БЕЗ НАБЛЮДЁННОГО ПРИЗНАКА ЗАКРЫТИЕМ НЕ СЧИТАЕТСЯ** — правило записал сам артефакт, и
настоящий план его ПРИНУЖДАЕТ, а не заменяет.
</not_closed_by_this_plan>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: сквозной срез — состояние обхода не шире его собственных отметок, и правило зелено во ВСЕХ законных состояниях</name>

  <files>
tests/test_planning/test_the_walkthrough_cannot_self_certify.py
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1-32` — шапка артефакта ЦЕЛИКОМ.
  ⚠️ ЧИТАТЬ ДОСЛОВНО: поля `status: testing`, `checks_declared: 6`, комментарий с запретом ставить
  `passed` до прохождения. Разбор ведётся ПО ФОРМЕ ПОЛЯ, а не по сегодняшнему значению.
- `:238-316` — проверка 1 ЦЕЛИКОМ: заголовок раздела проверки, таблица шагов, раздел снимаемых
  величин, подзаголовок отметки и ТАБЛИЦА ОТМЕТКИ с её строкой-разделителем и ПУСТОЙ строкой
  значений. ⚠️ ФОРМА ПУСТОЙ ОТМЕТКИ СНИМАЕТСЯ ОТСЮДА, а не из памяти: строка состоит из разделителей
  и пробелов.
- `:318-491` — проверки 2-6 и их таблицы отметок: убедиться, что форма подзаголовка отметки и
  форма таблицы у всех шести ОДНА. Разное написание сломало бы счёт, и это надо увидеть до правила,
  а не после.
- `.planning/phases/09-*/09-UAT.md` — шапка ПРОЙДЕННОГО обхода: ключи `walkthrough` /
  `walkthrough_2` и вид ЗАПОЛНЕННОЙ таблицы отметок. ⚠️ ЭТО ВТОРОЕ ЗАКОННОЕ СОСТОЯНИЕ ДЕРЕВА, и
  правило обязано быть зелено на нём тоже — иначе оно вшивает сегодняшнее состояние ровно так, как
  это сделал модуль плана 10-14.
- `tests/test_planning/test_requirement_completion_follows_verification.py` ПОСЛЕ плана 10-17 —
  форма модуля-соседа: шапка «зачем / чего файл не доказывает / почему это не отмена D-33»,
  разборщики с исходником в параметре, синтетические сборки, контроли зубов. Настоящий модуль
  наследует ВСЁ перечисленное.
- `tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py` ПОСЛЕ плана
  10-17 — правило, которое настоящий модуль обязан НЕ НАРУШИТЬ: он живёт в том же каталоге.
  ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: если разборщик настоящего модуля назовёт живой корень записи И понесёт
  вердиктный литерал в утверждении, волна покраснеет.
- `10-VERIFICATION.md`, перечень `human_verification` целиком — семь пунктов и их `why_human`:
  чтобы модуль не пообещал закрыть ни одного из них.
  </read_first>

  <precondition>
План 10-17 приземлён: правило независимости гейтов записи от вердикта живого дерева в каталоге
`tests/test_planning/` и зелено. Настоящий модуль встаёт в тот же каталог и обязан пройти под этим
правилом; писать его до появления правила — значит узнать о нарушении на прогоне волны.
  </precondition>

  <behavior>
1. Разборщик принимает ИСХОДНИК артефакта обхода ТЕКСТОМ и возвращает три величины: объявленное
   шапкой число проверок, число разделов проверок и число ЗАПОЛНЕННЫХ строк отметок.
2. Несущее правило: если состояние шапки объявляет обход пройденным, то заполненных отметок должно
   быть не меньше числа проверок. Если состояние иное — правило о заполненности МОЛЧИТ.
3. Правило зелено в ОБОИХ законных состояниях: непройденный обход с пустыми таблицами и пройденный
   обход с заполненными.
4. Второе правило: три счёта проверок сходятся — объявленное шапкой число, число разделов проверок
   и число таблиц отметок равны между собой.
5. Правило обходит ВСЕ артефакты обхода дерева, а не один: обход, выродившийся в чтение одного
   файла, зеленел бы от переезда артефакта, и это тот же класс, что переезд отчёта в архив.
6. Зубы: синтетический артефакт, объявивший себя пройденным при пустых отметках, краснит правило;
   синтетический артефакт с числом проверок, не сходящимся с числом таблиц, краснит второе.
  </behavior>

  <action>
Завести НОВЫЙ модуль `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`. Ни одного
существующего файла не трогать; `10-UAT.md` НЕ ПРАВИТЬ ни на символ.

**(1) Шапка модуля.** Докстринг по форме двух соседей каталога. ЗАЧЕМ — правило записано САМИМ
артефактом («отметка без наблюдённого признака закрытием не считается», «значение `status: passed`
до этого ставить ЗАПРЕЩЕНО»), принуждения у него нет, и это ровно та конструкция, которую месяцем
раньше обошёл коммит `0ea886d` у регламента отметок требований. ЧЕГО ФАЙЛ НЕ ДОКАЗЫВАЕТ — он НЕ
утверждает, что обход пройден, и не может: он утверждает, что ЗАПИСЬ о прохождении не шире
собственных отметок артефакта; он не судит прозы шагов, не проверяет верности ожиданий и ничего не
говорит о рантайме. ПОЧЕМУ ЭТО НЕ ОТМЕНА D-33 — предмет здесь ДВА машинно читаемых поля и ОДНО
объявленное между ними соответствие, суждения он не требует.

**(2) Константы.** Каталог фаз записи, вычисленный от `__file__`. `PASSED_STATE` — литерал
состояния, которым артефакт обхода объявляет себя пройденным; взять его из собственного запрета
артефакта, а не из вердиктов отчётов верификации: это ДРУГОЙ словарь, и смешение двух словарей —
готовое расхождение. ⚠️ ЛИТЕРАЛ НЕПРОЙДЕННОГО СОСТОЯНИЯ В МОДУЛЕ НЕ ОБЪЯВЛЯТЬ ВОВСЕ: правило,
знающее его, вшило бы сегодняшнее состояние дерева — дословно блокер четвёртого круга.

**(3) Разборщик `walkthrough_counts(source)`.** Вход — исходник артефакта ТЕКСТОМ (идиома каталога).
Выход — структура с полями: состояние шапки; объявленное число проверок; число разделов проверок;
число таблиц отметок; число ЗАПОЛНЕННЫХ строк отметок. Форму каждого признака снять ЧТЕНИЕМ
`10-UAT.md` и `09-UAT.md`, а не из памяти: заголовок раздела проверки, подзаголовок отметки, строка
таблицы с одними разделителями и пробелами. Разбор вести ПО ФОРМЕ СТРОКИ, а не по тексту: правка
формулировки шага не имеет права ронять правило — хрупкое правило отключают вместе со свойством.

**(4) Сборка `walkthrough_sources()`** — пары «путь, исходник» для ВСЕХ `*-UAT.md` дерева записи,
собранные РЕКУРСИВНЫМ обходом. Рекурсивным по той же причине, по какой рекурсивен обход отчётов
верификации у соседа: переезд артефакта в архив не имеет права молча выключить правило.

**(5) Несущее правило `test_no_walkthrough_declares_itself_passed_with_empty_marks`.** Для каждого
артефакта: если состояние равно `PASSED_STATE`, то число заполненных отметок не меньше числа
проверок. Иначе — не утверждать НИЧЕГО о заполненности. Отказ называет путь артефакта, его
состояние, число проверок и число заполненных отметок. Плюс утверждение непустоты: артефактов
найдено больше нуля.

**(6) Правило трёх счётов `test_the_number_of_walkthrough_checks_agrees_three_ways`.** Для каждого
артефакта: объявленное шапкой число проверок равно числу разделов проверок и равно числу таблиц
отметок. Отказ называет все три числа и путь.

**(7) Контроли зубов.** `test_control_a_walkthrough_claiming_passage_with_empty_marks_reddens` —
СИНТЕТИЧЕСКИЙ исходник артефакта: состояние `PASSED_STATE`, две проверки, обе таблицы отметок пусты;
правило находит нарушение и называет оба числа. Вторая половина — тот же исходник с ЗАПОЛНЕННЫМИ
отметками: нарушений ноль. Третья половина — тот же исходник с состоянием, ОТЛИЧНЫМ от `PASSED_STATE`,
при пустых отметках: нарушений ноль. ⚠️ ТРЕТЬЯ ПОЛОВИНА НЕСУЩАЯ: она и есть доказательство того, что
правило зелено в ОБОИХ законных состояниях, а не вшило одно из них.
`test_control_a_walkthrough_with_a_lost_mark_table_reddens_the_count` — синтетический исходник, где
проверок объявлено две, разделов два, а таблица отметок одна: второе правило краснеет.

**(8) Что НЕ трогается.** `10-UAT.md` — ни символа: он предмет плана 10-23, и владелец у него один.
`09-UAT.md` — ни символа: его отметки сняты с другого экрана и другого механизма.
`.planning/REQUIREMENTS.md` и все отчёты верификации.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_the_walkthrough_cannot_self_certify.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `4` — четыре правила модуля (несущее, три счёта, два контроля) не заведены либо не зелены</fails_when>
    <automated>uv run pytest tests/test_planning/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — новый модуль нарушил правило независимости гейтов записи, заведённое планом 10-17, либо разошёлся с соседями каталога</fails_when>
    <automated>git diff --name-only -- .planning/</automated>
    <fails_when>вывод не пуст — план тронул запись, а его предмет — суита; артефакт обхода правит план 10-23, и владелец у него один</fails_when>
  </verify>

  <acceptance_criteria>
- Правило зелено в ОБОИХ законных состояниях, и это ЗАМЕРЕНО, а не объявлено: в сводке приведены
  выводы трёх половин контроля зубов — `passed` с пустыми отметками (нарушение найдено), `passed` с
  заполненными (нарушений ноль), НЕ-`passed` с пустыми (нарушений ноль).
- Литерала непройденного состояния в модуле НЕТ:
  `grep -c 'testing' tests/test_planning/test_the_walkthrough_cannot_self_certify.py` → `0`, и
  вывод приведён в сводке.
- Разборщик принимает исходник ТЕКСТОМ: в сводке приведена сигнатура `walkthrough_counts`.
- Три счёта сняты с живого артефакта и приведены в сводке ЧИСЛАМИ: объявленное шапкой, число
  разделов проверок, число таблиц отметок; все три равны.
- Число заполненных отметок живого артефакта приведено в сводке и равно `0` — обход не пройден, и
  правило это ПРОПУСКАЕТ, потому что состояние не объявляет прохождения.
- Обход не выродился: в сводке приведено число найденных артефактов обхода, оно больше единицы
  (в дереве есть как минимум обход Фазы 9 и обход Фазы 10).
- Новый модуль проходит под правилом плана 10-17: в сводке приведён вывод прогона всего каталога.
- `.planning/` не тронут: в сводке приведён вывод третьей команды раздела `<verify>` (пусто).
  </acceptance_criteria>

  <reversibility rating="reversible">
Заводится ОДИН новый тестовый модуль; ни одного существующего файла задача не правит, поведения
продукта не меняет. Откат — удаление файла, и он возвращает дерево в состояние до задачи.
  </reversibility>

  <done>
Запись о прохождении обхода не может стать шире собственных отметок артефакта, и это принуждается
машинно; правило зелено в обоих законных состояниях дерева, и это замерено тремя половинами
контроля; три счёта проверок сходятся; обход артефактов рекурсивен и непуст.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: якоря наблюдения обхода существуют в разметке — шаг не может ссылаться на узел, которого нет</name>

  <files>
tests/test_planning/test_the_walkthrough_cannot_self_certify.py
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:279-296` — таблица шагов проверки 1
  ЦЕЛИКОМ. ⚠️ ВЫПИСАТЬ ИЗ НЕЁ КАЖДЫЙ УЗЕЛ, КОТОРЫЙ ШАГ ЧИТАЕТ ИЛИ НАЖИМАЕТ, вместе с файлом и
  строкой, которые артефакт сам называет источником: обход уже несёт эти ссылки, и реестр
  собирается ИЗ НИХ, а не сочиняется.
- `:318-491` — шаги проверок 2-6: те же выписки. ⚠️ ЧАСТЬ ССЫЛОК УКАЗЫВАЕТ НА ФАЙЛЫ, А НЕ НА УЗЛЫ
  (например «признак блокировки прокрутки на корневом элементе»): такие в реестр якорей НЕ
  ЗАНОСЯТСЯ, и это решение записывается — реестр, смешавший узлы с признаками, стерёг бы два разных
  предмета одним числом.
- `app/templates/ads/includes/sched_card.html`, `app/templates/ads/form.html`,
  `app/templates/ads/partials/sched_delete_response.html`, `app/templates/includes/notice_area.html`,
  `app/templates/components/modal.html` — файлы-источники якорей. ⚠️ КАЖДЫЙ ЯКОРЬ ПРОВЕРЯЕТСЯ
  ЧТЕНИЕМ ФАЙЛА: якорь, вписанный в реестр по памяти о шаге обхода, превратил бы правило в
  проверку самого себя.
- `tests/test_pages/test_htmx_gates.py:410-473` — форма реестра с обоснованием и объявленным числом
  (`OFFSET_CURSOR_EXCEPTIONS`, `OFFSET_CURSOR_EXCEPTIONS_DECLARED` и его летопись). Реестр якорей
  наследует эту форму: у каждой записи есть НАЗВАННОЕ ОСНОВАНИЕ, а у перечня — число с летописью.
- Результат задачи 1 в дереве: сборки и правила модуля. Новое правило встаёт РЯДОМ и наследует их
  форму; второго разборщика артефакта не заводится.
  </read_first>

  <precondition>
Задача 1 приземлена: модуль в дереве и его правила зелены. Реестр якорей встаёт в тот же модуль,
потому что предмет один — «обход обязан быть проходимым»: артефакт, объявивший себя пройденным без
отметок, и шаг, ссылающийся на несуществующий узел, суть две половины одного отказа.
  </precondition>

  <behavior>
1. Реестр перечисляет якоря наблюдения обхода: у каждого — селектор либо имя узла, файл-источник и
   ОСНОВАНИЕ, то есть какой шаг обхода его читает.
2. Каждый якорь реестра ПРИСУТСТВУЕТ в названном файле-источнике; отсутствующий краснит прогон с
   именем якоря, именем файла и номером шага.
3. Число якорей объявлено константой с летописью; расхождение числа с длиной реестра краснит
   прогон.
4. Файл-источник, которого в дереве нет, краснит прогон ГРОМКО, а не пропускается: реестр,
   ссылающийся на несуществующий файл, стерёг бы пустоту.
5. Зубы: реестр, у которого один якорь заменён на заведомо отсутствующий, краснит правило и
   называет ровно его.
  </behavior>

  <action>
**(1) Собрать реестр `WALKTHROUGH_ANCHORS`.** Пройти шаги ВСЕХ ШЕСТИ проверок `10-UAT.md` и выписать
каждый узел, который шаг читает либо нажимает, вместе с файлом-источником, который артефакт сам
называет. ⚠️ КАЖДУЮ ЗАПИСЬ ПРОВЕРИТЬ ЧТЕНИЕМ ФАЙЛА, а не памятью о шаге: если якорь в файле не
найден, это НАХОДКА — назвать её в сводке поимённо и записать отдельной строкой, потому что обход
уже несёт шаг, который не проверит ничего. Форму записи взять у `OFFSET_CURSOR_EXCEPTIONS`: поле
селектора, поле файла-источника, поле основания (какой шаг обхода читает якорь). Признаки, не
являющиеся узлами (состояния корневого элемента, заголовки ответа, коды состояния), в реестр НЕ
заносить и записать это решение отдельной строкой: реестр, смешавший узлы с признаками, стерёг бы
два предмета одним числом.

**(2) Объявить число.** `WALKTHROUGH_ANCHORS_DECLARED` с ЛЕТОПИСЬЮ по форме
`OFFSET_CURSOR_EXCEPTIONS_DECLARED`: объявленное число, откуда взялось (выписка из шагов обхода),
чем измерено (чтение шести проверок и проверка каждого якоря чтением файла-источника), каким планом
(10-20, четвёртая партия закрытия гейпов Фазы 10). ⚠️ ЧИСЛО В ПЛАН НЕ ВПИСАНО НАМЕРЕННО: планировщик
выписку из всех шести проверок не делал, и вписанное «примерно столько» было бы ориентиром, выданным
за замер.

**(3) Правило `test_every_walkthrough_anchor_exists_in_its_source_template`.** Для каждой записи
реестра: файл-источник существует, и якорь в нём находится. Отказ называет `якорь → файл (шаг N)`
для каждого отсутствующего и говорит, что делать: либо шаг обхода ссылается не туда и правится шаг,
либо узел ушёл из разметки и это регрессия продукта — различить эти два случая обязан ЧЕЛОВЕК, и
правило говорит об этом прямо, вместо того чтобы предлагать одну починку.

**(4) Правило `test_the_number_of_walkthrough_anchors_is_declared`.** Длина реестра равна
объявленному числу; отказ говорит, что молча похудевший реестр означает снятое наблюдение, а молча
выросший — якорь, добавленный без названного шага-читателя.

**(5) Контроль зубов `test_control_a_missing_anchor_is_named_by_the_rule`.** Подать правилу КОПИЮ
реестра, в которой один якорь заменён на заведомо отсутствующий в его файле: правило находит ровно
его и называет по имени. Вторая половина — непрáвленый реестр: находок ноль. Копия живёт в памяти;
ни реестр в модуле, ни файлы разметки не правятся.

**(6) Что НЕ трогается.** `10-UAT.md` — ни символа (владелец у него один, это план 10-23). Шаблоны
разметки — ни символа: если якорь не найден, это НАХОДКА для сводки, а не повод дописать узел в
шаблон «чтобы правило позеленело». ⚠️ ЭТО НЕСУЩИЙ ЗАПРЕТ ЗАДАЧИ: дописанный ради зелени узел есть
подгонка кода под тест, зеркальная подгонке теста под код, которой партия и занимается.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_the_walkthrough_cannot_self_certify.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `7` — четыре правила задачи 1 плюс три правила задачи 2 не заведены либо не зелены</fails_when>
    <automated>uv run pytest tests/test_planning/test_the_walkthrough_cannot_self_certify.py -q -p no:randomly -k "anchor"</automated>
    <fails_when>вывод содержит `no tests ran` либо `failed` — правила якорей и контроль зубов не заведены под названными именами либо не зелены</fails_when>
    <automated>git diff --name-only -- .planning/ app/templates/</automated>
    <fails_when>вывод не пуст — план тронул запись обхода либо разметку; узел, дописанный в шаблон ради зелени, есть подгонка кода под тест</fails_when>
  </verify>

  <acceptance_criteria>
- Реестр собран ВЫПИСКОЙ из шагов всех шести проверок: в сводке приведён перечень якорей с
  файлом-источником и номером шага-читателя у каждого.
- Каждый якорь проверен ЧТЕНИЕМ файла: в сводке приведена команда проверки и её вывод по каждому
  файлу-источнику.
- Якоря, НЕ найденные в дереве, названы поимённо отдельной строкой сводки — либо сказано, что таких
  нет. ⚠️ Молчание об этом не допускается: ненайденный якорь есть шаг обхода, который ничего не
  проверит.
- Решение о том, что признаки-не-узлы в реестр не заносятся, записано в модуле и процитировано в
  сводке.
- `WALKTHROUGH_ANCHORS_DECLARED` снят замером: в сводке приведены длина реестра и объявленное число,
  они равны.
- Контроль зубов КРАСНЕЕТ на подменённом якоре и зеленеет на непрáвленом реестре: в сводке приведены
  оба вывода.
- Ни `10-UAT.md`, ни шаблоны не тронуты: в сводке приведён вывод третьей команды раздела `<verify>`
  (пусто).
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится ОДИН новый тестовый модуль, заведённый задачей 1; ни разметка, ни запись обхода не
меняются. Откат — удаление реестра и трёх правил.
  </reversibility>

  <done>
Якоря наблюдения обхода объявлены реестром с файлом-источником и шагом-читателем у каждого; каждый
проверен чтением файла; число объявлено с летописью; правило краснеет на подменённом якоре; ни
артефакт обхода, ни разметка не тронуты.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Новый файл `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`:**

| Символ | Что это |
|---|---|
| `PASSED_STATE` | литерал состояния, которым артефакт обхода объявляет себя пройденным; взят из собственного запрета артефакта, а не из словаря вердиктов отчётов |
| `walkthrough_counts(source)` | разборщик: состояние шапки, объявленное число проверок, число разделов, число таблиц отметок, число ЗАПОЛНЕННЫХ отметок; исходник приходит ТЕКСТОМ |
| `walkthrough_sources()` | пары «путь, исходник» для всех `*-UAT.md` дерева записи, рекурсивным обходом |
| `test_no_walkthrough_declares_itself_passed_with_empty_marks` | несущее правило плюс утверждение непустоты обхода |
| `test_the_number_of_walkthrough_checks_agrees_three_ways` | правило трёх счётов |
| `test_control_a_walkthrough_claiming_passage_with_empty_marks_reddens` | зубы ТРЕМЯ половинами, третья доказывает зелень в обоих законных состояниях |
| `test_control_a_walkthrough_with_a_lost_mark_table_reddens_the_count` | зубы правила трёх счётов |
| `WALKTHROUGH_ANCHORS` | реестр якорей наблюдения: селектор, файл-источник, шаг-читатель |
| `WALKTHROUGH_ANCHORS_DECLARED` | число якорей с летописью по форме `OFFSET_CURSOR_EXCEPTIONS_DECLARED` |
| `test_every_walkthrough_anchor_exists_in_its_source_template` | правило существования якорей |
| `test_the_number_of_walkthrough_anchors_is_declared` | правило числа якорей |
| `test_control_a_missing_anchor_is_named_by_the_rule` | зубы двумя половинами |

**ЧЕГО ПЛАН НЕ ПРОИЗВОДИТ:** ни одной правки `10-UAT.md`, `09-UAT.md`, `.planning/REQUIREMENTS.md`,
`.planning/ROADMAP.md`, шаблонов разметки, `app/` и `pyproject.toml`. Ни одной заполненной отметки
обхода. Ни одного утверждения о том, что обход пройден.
</artifacts_this_phase_produces>

<threat_model>
⚠️ Таблица собрана на **ASVS 1** (`workflow.security_asvs_level` → `1`) с порогом блокировки
**`high`** (`workflow.security_block_on` → `"high"`) при `workflow.security_enforcement` → `true`.
Все три величины сняты `config-get`. Строка `*-SC` несёт `accept`: пакетов план не устанавливает.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| запись артефакта обхода → запись требований | состояние обхода есть условие перевода требования; запись, ставшая шире отметок, переводит требование шире дерева |
| шаг обхода → человек на приёмке | текст шага есть инструкция: неверно названный узел заставит человека записать отказ, которого нет |
| правило о записи → сегодняшнее состояние дерева | правило, вшившее одно из двух законных состояний, краснеет от штатного продвижения проекта |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-20-01 | Repudiation | состояние артефакта ручного обхода | high | mitigate | Несущее правило (задача 1): состояние `passed` требует заполненных отметок у каждой проверки. Правило, записанное самим артефактом, получает принуждение — та же конструкция, которую месяцем раньше обошёл коммит `0ea886d` у регламента отметок требований |
| T-10-20-02 | Repudiation | само новое правило | high | mitigate | Литерала непройденного состояния в модуле НЕТ, и зелень в ОБОИХ законных состояниях доказана третьей половиной контроля зубов. Без этого план воспроизвёл бы в собственном гейте блокер, который партия закрывает планом 10-17 |
| T-10-20-03 | Tampering | число проверок обхода | medium | mitigate | Правило трёх счётов: объявленное шапкой, число разделов и число таблиц отметок равны. Проверка не может исчезнуть из обхода молча |
| T-10-20-04 | Tampering | якоря наблюдения шагов обхода | medium | mitigate | Реестр якорей с файлом-источником и шагом-читателем; каждый проверен чтением файла; число объявлено с летописью. Шаг, называющий несуществующий узел, краснит прогон, а не обесценивает обход при прохождении |
| T-10-20-05 | Tampering | разметка продукта под зелень правила | high | mitigate | Дописывание узла в шаблон ради зелени объявлено НЕСУЩИМ ЗАПРЕТОМ задачи 2 и проверяется командой: `git diff --name-only -- app/templates/` обязан быть пуст. Подгонка кода под тест зеркальна подгонке теста под код, которой партия и занимается |
| T-10-20-06 | Denial of Service | обход артефактов записи | low | mitigate | Утверждение непустоты (артефактов найдено больше нуля) плюс рекурсивный обход: переезд артефакта в архив не выключает правило молча |
| T-10-20-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q -p no:randomly` — зелено; новый модуль проходит под
   правилом независимости плана 10-17.
2. `uv run pytest tests/ -q -p no:randomly` — прогон целиком зелен.
3. `grep -c 'testing' tests/test_planning/test_the_walkthrough_cannot_self_certify.py` → `0`:
   сегодняшнее состояние обхода в правиле не вшито.
4. `git diff --name-only -- .planning/ app/` — пусто.
5. **ЧЕЛОВЕЧЕСКАЯ ПОЛОВИНА, СНИМАЕМАЯ НА ПРИЁМКЕ ФАЗЫ (`human_verify_mode: end-of-phase`):** шесть
   проверок обхода `10-UAT.md` пройдены на локальном стенде в браузере и шесть таблиц отметок
   заполнены наблюдёнными признаками. ⚠️ ДО ЭТОГО критерий 2 роадмапа и требование `FORM-06`
   остаются НЕЗАКРЫТЫМИ; ни один машинный прогон этого не меняет, и настоящий план обратного не
   утверждает — раздел «Чего этот план НЕ закрывает» перечисляет все семь пунктов поимённо.
</verification>

<success_criteria>
- Запись о прохождении обхода не может стать шире собственных отметок, и это машинное принуждение
  правила, которое артефакт записал о себе сам.
- Правило зелено в ОБОИХ законных состояниях дерева, и это ЗАМЕРЕНО тремя половинами контроля, а не
  объявлено; литерала непройденного состояния в модуле нет.
- Три счёта проверок сходятся; обход артефактов рекурсивен и непуст.
- Якоря наблюдения объявлены реестром, каждый проверен чтением файла-источника, число объявлено с
  летописью, правило краснеет на подменённом якоре.
- Ненайденные якоря, если они есть, названы поимённо, а не закрыты дописыванием узла в шаблон.
- Названо ПРЯМО, чего план не закрывает: все семь пунктов перечня `human_verification` перечислены
  с основанием у каждого, и ни один не объявлен закрытым машинно.
- `.planning/` и `app/` не тронуты; требование `FORM-06` остаётся незавершённым.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-20-SUMMARY.md` по завершении.
</output>
