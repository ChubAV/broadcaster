---
phase: 10-rychag-components-modal-html
plan: 39
type: execute
wave: 3
# ⚠️ ВОЛНА 3 — ЗАВИСИМОСТИ ПРЕДМЕТНЫЕ, А НЕ ФАЙЛОВЫЕ, И ОБЕ НАЗВАНЫ. Пересечения по файлам нет ни с
# одним планом партии: 10-35 правит рычаг, таблицу стилей, включение заготовок и модуль шелла;
# 10-36 — реестр якорей; 10-37 и 10-38 — модуль шелла; 10-40 — отчёт верификации. Настоящий план
# правит артефакт обхода и перечень отложенного.
# (1) ЗАВИСИМОСТЬ ОТ 10-35: запись о ходе починки продуктового гэпа говорит, ЧЕМ закрыт дефект
# подъёма и что осталось человеку. Запись о починке, сделанная ДО починки, есть обещание — ровно
# тот класс, за который фаза получила круги 3, 4 и 5.
# (2) ЗАВИСИМОСТЬ ОТ 10-37: запись `IN-02` обязана назвать, какая ПОЛОВИНА находки осталась
# открытой. До исполнения 10-37 открыты обе, и запись, сделанная раньше, назвала бы владельцем не
# того.
depends_on: ["10-35", "10-37"]
gap_closure: true
gap_ids: [G-10-A1, G-10-A2, G-10-4, IN-01, IN-02, IN-03, IN-04, IN-05]
files_modified:
  - .planning/phases/10-rychag-components-modal-html/10-UAT.md
  - .planning/phases/10-rychag-components-modal-html/deferred-items.md
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (полей во frontmatter не заводится, форма унаследована от
# планов 10-12…10-34): `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена автором.
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "БЛОК ГЭПОВ ПЕРЕСТАЁТ ПРОТИВОРЕЧИТЬ ТЕЛУ СОБСТВЕННОГО ФАЙЛА: два дефекта артефакта, ПОЧИНЕННЫЕ В ДЕРЕВЕ планом 10-34, несут запись о закрытии с воспроизводимой командой замера, а не читаются следующим кругом как открытые"
    - "МАШИННО ЧИТАЕМОЕ ПОЛЕ СОСТОЯНИЯ ГЭПА НЕ ПОДМЕНЯЕТСЯ ВЫДУМАННЫМ ЗНАЧЕНИЕМ: замер словаря по дереву даёт для гэпов обхода ровно одно значение, и запись о закрытии живёт ОТДЕЛЬНЫМ ключом — прежнее поле остаётся записью того, что видел обход в свой день"
    - "ПРОДУКТОВЫЙ ГЭП ЗАКРЫТЫМ НЕ ОБЪЯВЛЯЕТСЯ: у него записан ХОД починки, а не её итог — отрисовку браузером снимает шаг 4.4, и он человеком не пройден"
    - "ШАГ, НЕ ЗАСЧИТАННЫЙ НИ В ОДНУ СТОРОНУ, ПОЛУЧАЕТ ОДНО ЯВНОЕ РАСПОРЯЖЕНИЕ: названо, какая его половина наблюдена другой проверкой на НАСТОЯЩЕМ отказе, какая не наблюдена, каким методом она наблюдаема и почему выбранный метод её не снял"
    - "ПРИЧИНА НЕНАБЛЮДЕНИЯ НАЗВАНА ЧИСЛОМ, А НЕ ВПЕЧАТЛЕНИЕМ: окно между ответом POST и приходом follow-up GET на медленном канале замерено обходом в миллисекундах, и именно оно объясняет отказ от переключения сети снаружи"
    - "НИ ОДНА НАХОДКА СЕДЬМОГО КРУГА НЕ ИСЧЕЗАЕТ БЕЗ ВЛАДЕЛЬЦА: все пять замечаний внесены в перечень отложенного с файлом, находкой, причиной неправки здесь и НАЗВАННЫМ владельцем"
    - "ЧАСТИЧНО ЗАКРЫТАЯ НАХОДКА НАЗВАНА ЧАСТИЧНОЙ: у второго замечания записано, какая половина закрыта планом 10-37 ПО НЕОБХОДИМОСТИ и какая осталась, — иначе следующий круг прочтёт находку закрытой целиком"
    - "ТРИ СЧЁТА АРТЕФАКТА ОБХОДА НЕ СДВИНУТЫ: объявленное число проверок, число разделов проверок и число таблиц отметок равны шести до и после, а состояние остаётся нетерминальным"
    - "ПОВТОРНОЕ ИСПОЛНЕНИЕ ПРАВОК НЕОТЛИЧИМО ОТ ПЕРВОГО: каждая приписка вносится по якорю, который после первой правки уже её несёт, поэтому вторая правка того же файла записи не удваивает — свойство проверяется счётом вхождений до и после"
    - statement: "СЛЕДУЮЩИЙ ПРОГОН ЗАКРЫТИЯ ГЭПОВ УВИДИТ ЗАПИСЬ О ЗАКРЫТИИ, А НЕ ТОЛЬКО ПОЛЕ СОСТОЯНИЯ: машинного разбора нового ключа у планировщика нет, и прочтение записи остаётся за читателем блока целиком"
      verification: backstop
  artifacts:
    - "10-UAT.md — записи о закрытии у двух гэпов-дефектов артефакта, с командой замера, планом-исполнителем и датой"
    - "10-UAT.md — запись о ходе починки у продуктового гэпа, с названным пределом доказанного"
    - "10-UAT.md — распоряжение по шагу 2.8: наблюдённая половина, ненаблюдённая половина, метод и причина"
    - "deferred-items.md — раздел «План 10-39» с пятью подразделами по числу замечаний седьмого круга, у каждого назван владелец"
  key_links:
    - "запись о закрытии внутри записи гэпа → следующая партия закрытия гэпов: единственное, чем починенный дефект артефакта не будет починен повторно; ломается — седьмая партия заводит план на то, что сделала шестая"
    - "распоряжение по шагу 2.8 → следующий обход: единственное, чем ненаблюдённый шаг не превращается в молча пропущенный; ломается — шаг остаётся без ответа третий круг подряд и перестаёт быть предметом"
    - "перечень отложенного → пять замечаний седьмого круга: единственное, чем находка без плана не исчезает вместе с отчётом; ломается — замечания живут только в отчёте ревизии, который следующий круг перезапишет, как шестой перезаписал пятый"
  prohibitions:
    - statement: "ЗАПОЛНЕННЫЕ СТРОКИ ТАБЛИЦ ОТМЕТОК НЕ ПРАВЯТСЯ НИ В ОДНОЙ КЛЕТКЕ: они есть запись НАБЛЮДЁННОГО, и правка наблюдения под новое знание была бы подгонкой замера под вывод"
      status: flagged-unverified
      verification: none
    - statement: "СОСТОЯНИЕ АРТЕФАКТА ТЕРМИНАЛЬНЫМ НЕ СТАВИТСЯ И ШАГ 4.4 ПРОЙДЕННЫМ НЕ ОБЪЯВЛЯЕТСЯ: отметки снял агент, а не человек на приёмке, и отрисовку браузером не доказывает ни одно правило суиты"
      status: flagged-unverified
      verification: test
    - statement: "ПОЛЕ СОСТОЯНИЯ НИ У ОДНОГО ГЭПА НЕ ПРАВИТСЯ И НЕ ВЫЧЁРКИВАЕТСЯ: замер словаря по дереву даёт для гэпов обхода ровно одно значение, и выдуманное второе не понял бы ни один потребитель; запись о закрытии идёт ОТДЕЛЬНЫМ ключом"
      status: flagged-unverified
      verification: test
    - statement: "ТЕКСТ ШАГА 2.8 И ЕГО ОЖИДАЕМЫЙ ПРИЗНАК НЕ ПРАВЯТСЯ: предмет задачи — РАСПОРЯЖЕНИЕ по ненаблюдённому шагу, а не переписывание того, что шаг спрашивает; правка ожидания под неудавшийся метод была бы подгонкой предмета под инструмент"
      status: flagged-unverified
      verification: none
    - statement: "ПЯТЬ ЗАМЕЧАНИЙ НЕ ЧИНЯТСЯ НИ ОДНО: их владелец назван, и правка `tests/test_pages/test_shell.py` либо `app/static/css/app.css` настоящим планом запрещена — этих файлов нет в его `files_modified`"
      status: flagged-unverified
      verification: test
    - statement: "ПРОДУКТ И СУИТА НЕ ПРАВЯТСЯ НИ НА СТРОКУ: предмет плана — ЗАПИСИ, и правка кода под запись зеркальна правке записи под код"
      status: flagged-unverified
      verification: test
    - statement: "КОМАНДА ПОСЕВА НЕ ЗАПУСКАЕТСЯ: рабочее дерево и прод смотрят в ОДНУ базу (замер `walkthrough.environment`), разрешение человека от 2026-09-09 было дано присутствующему и автономным исполнителем не наследуется, `workflow.live_dom_uat` равен `false`"
      status: flagged-unverified
      verification: none
    - statement: "РАЗДЕЛЫ РАСХОЖДЕНИЙ Р-1, Р-2 И Р-3 НЕ ПРАВЯТСЯ: у приписки Р-3 владелец — план 10-34, и вторая приписка из настоящего плана развела бы двух владельцев по одному тексту"
      status: flagged-unverified
      verification: none
---

<objective>
Записи фазы перестают противоречить дереву, ненаблюдённый шаг перестаёт быть молча пропущенным, и ни
одна находка седьмого круга не уходит из фазы без владельца.

**Три предмета, и все три — ЗАПИСИ, а не код.**

**(1) Блок гэпов артефакта обхода протух в двух записях из трёх.** Дефект несущего счёта шага 1.2
(счёт ловил заголовки панелей наравне с панелями) и дефект команды посева (однозначное
форматирование часа давало трёхзначные значения времени) ПОЧИНЕНЫ В ДЕРЕВЕ планом 10-34 — это
проверено на HEAD, а не принято по сводке: шаги 1.2, 1.5 и 1.9 и таблица «что снимает эта проверка»
несут уточнённый ролью счёт, команда посева читает двузначное форматирование часа, и прежние
формулировки помечены опровергнутыми по идиоме D-30/D-32. При этом блок гэпов того же файла
по-прежнему объявляет обоих провалившимися — то есть артефакт противоречит собственному телу, а
следующая партия закрытия гэпов, читающая блок, завела бы планы на уже сделанное. ⚠️ ЭТО ИМЕННО
БУХГАЛТЕРСКАЯ ПОЛОВИНА: сами дефекты НЕ ПЕРЕЧИНЯЮТСЯ ни одним символом.

**(2) Шаг 2.8 не засчитан НИ В ОДНУ СТОРОНУ, и распоряжения по нему нет.** Обрыв follow-up GET был
сделан подменой транспорта в документе, а не переключением сети снаружи: переключение не укладывалось
в окно — POST на медленном канале отвечает за 2073 мс, а GET доезжал раньше вызова. Прерывание
транспорта поднимает НЕ ТО событие, что настоящий обрыв, поэтому отрицательный ответ здесь
принадлежит методу, а не продукту. Сам обработчик на НАСТОЯЩЕМ отказе сработал — это наблюдено
проверкой 4 на переключении сети, — и текст шага прямо говорит, что перепроверять обработчик Фазы 8
не нужно. Шаг, оставленный без ответа, перестаёт быть предметом: следующий обход пролистает его как
«известно ненаблюдаемый», и половина, которая наблюдаема, уйдёт вместе с половиной, которая нет.

**(3) Пять замечаний седьмого круга ревизии остаются вне партии — по решению владельца, — и обязаны
не исчезнуть.** Отчёт ревизии перезаписывается каждым кругом (это уже случилось: шестой круг
перезаписал пятый), поэтому находка, живущая только в нём, исчезает вместе с ним. Перечень
отложенного фазы заведён ровно против этого и уже несёт восемь записей трёх планов.

Purpose: фаза получила круги 3, 4, 5 и 6 за один класс — «запись шире дерева» либо «запись у́же
дерева». Все три предмета настоящего плана принадлежат этому классу с разными знаками: блок гэпов
ШИРЕ дерева (называет открытым закрытое), запись шага 2.8 У́ЖЕ (молчит о том, что половина его
наблюдена другой проверкой), а пять замечаний рискуют исчезнуть вовсе. Закрывать их порознь в трёх
партиях значило бы отдать следующему кругу ровно ту работу, которую он делал уже четырежды.

Output: две записи о закрытии с воспроизводимой командой замера; одна запись о ходе починки, не
объявляющая её итога; одно распоряжение по ненаблюдённому шагу; пять записей отложенного с
названными владельцами.

⚠️ **ПОЛЕ СОСТОЯНИЯ ГЭПА НЕ ПРАВИТСЯ, И ЭТО РЕШЕНИЕ С ЗАМЕРОМ, А НЕ ОСТОРОЖНОСТЬ.** Замер словаря по
дереву записи: у гэпов артефактов обхода поле состояния принимает РОВНО ОДНО значение (три
вхождения, все три в этом файле); у отложенного в отчётах верификации — своё, тоже одно. Значения,
означающего «закрыто», в словаре проекта НЕТ НИ ОДНОГО. Выдуманное новое значение не понял бы ни один
потребитель, а подстановка значения отложенного сказала бы неправду: гэп не отложен, он починен.
Поэтому запись о закрытии идёт ОТДЕЛЬНЫМ ключом, а поле остаётся тем, чем является, — записью того,
что видел обход в свой день. Это и есть идиома D-30/D-32 в её машинной половине: прежнее не
вычёркивается, рядом с ним встаёт то, что его переросло.

⚠️ **ЦЕНА ЭТОГО ВЫБОРА НАЗЫВАЕТСЯ ПРЯМО.** Планировщик закрытия гэпов машинного разбора нового ключа
не имеет: он читает блок целиком. Значит запись о закрытии обязана стоять ВНУТРИ записи гэпа, а не в
конце файла, и называть исход первой же строкой. Если следующая партия всё же заведёт план на
закрытое — это находка о ФОРМЕ записи, и чинить надо форму, а не гэп.

⚠️ **О ПРОДУКТОВОМ ГЭПЕ ПИШЕТСЯ ХОД, А НЕ ИТОГ, И ИМЯ КЛЮЧА ЭТО ГОВОРИТ.** План 10-33 отгрузил
условный подъём; седьмой круг нашёл в нём дефект; план 10-35 дефект закрыл. Ни одно правило суиты
при этом не доказывает, что БРАУЗЕР нарисовал плашку поверх панели, — это шаг 4.4, и его снимает
человек. Слова «закрыт» и равносильных в этой записи быть не должно ни в какой форме.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-34-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-35-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-37-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**Записи в `10-UAT.md`** (все — ДОПИСЫВАНИЕ, ни одного удаления):

- ключ `closed_by:` внутри записи гэпа `G-10-A1` — вложенными полями: `plan`, `date`, `measurement`
  (команда замера дословно), `expected` (ожидаемый исход команды), `status_note` (почему поле
  состояния оставлено).
- такой же ключ `closed_by:` внутри записи гэпа `G-10-A2` — со своей командой замера.
- ключ `closure_progress:` внутри записи гэпа `G-10-4` — вложенными полями: `shipped`, `guarded_by`,
  `limit`. ⚠️ ИМЯ КЛЮЧА ОТЛИЧАЕТСЯ ОТ ДВУХ ПРЕДЫДУЩИХ НАМЕРЕННО И ЯВЛЯЕТСЯ ЧАСТЬЮ ПРЕДМЕТА: гэп не
  закрыт, и ключ `closed_by:` у него стоять НЕ ИМЕЕТ ПРАВА.
- приписка под таблицей отметок проверки 2 — распоряжение по шагу 2.8.

**Записи в `deferred-items.md`:**

- раздел `## План 10-39` с пятью подразделами по числу замечаний седьмого круга ревизии.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Задача 1: блок гэпов перестаёт противоречить телу своего файла — два закрытия и один ход починки</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `10-UAT.md:827-900` — блок гэпов целиком: три записи, их поля и форма. ⚠️ ЧИТАТЬ ДОСЛОВНО: предмет
  правки — ЭТИ записи, и структура блока обязана остаться разбираемой.
- `10-UAT.md:833-855` — запись продуктового гэпа. ⚠️ ОСОБО: её список `artifacts` уже несёт
  ИСПРАВЛЕННЫЙ адрес включения заготовок плашки и приписку о том, что прежний адрес был неверен;
  прежний адрес НЕ наследуется ни в одну новую строку.
- `10-UAT.md:303-320` — шапка проверки 1 ПОСЛЕ правки плана 10-34: несущий счёт с уточнением ролью
  и помеченная опровергнутой прежняя формулировка. ⚠️ ЭТО ДОКАЗАТЕЛЬСТВО ЗАКРЫТИЯ ПЕРВОГО ДЕФЕКТА
  АРТЕФАКТА — читать, а не принимать по сводке.
- `10-UAT.md:107-185` — раздел «Как привести стенд в нужное состояние» с командой посева ПОСЛЕ
  правки плана 10-34. ⚠️ ЭТО ДОКАЗАТЕЛЬСТВО ЗАКРЫТИЯ ВТОРОГО ДЕФЕКТА АРТЕФАКТА.
- `10-UAT.md:771-826` — расхождение Р-3 целиком, включая приписку плана 10-34 о выбранном кандидате.
  ⚠️ ЧИТАТЬ И НЕ ПРАВИТЬ.
- `10-UAT.md:1-60` — шапка: поле состояния, блок `walkthrough` и его списки провалов, неприменимых и
  ненаблюдённых шагов. ⚠️ ЧИТАТЬ И НЕ ПРАВИТЬ: состояние остаётся нетерминальным, шаг 4.4 остаётся
  в списке провалов.
- `.planning/phases/10-rychag-components-modal-html/10-35-SUMMARY.md` — чем именно закрыт дефект
  подъёма и где записан предел доказанного.
- `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` — что именно правило считает и
  какие поля читает. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: правка блока гэпов не имеет права его уронить.
- `.planning/STATE.md` — записи D-30/D-32.
  </read_first>

  <behavior>
1. Запись первого дефекта артефакта получает ключ записи о закрытии: назван план-исполнитель
   (10-34), названа дата, названа ВОСПРОИЗВОДИМАЯ команда замера по дереву и её ожидаемый исход.
2. Запись второго дефекта артефакта получает такой же ключ со своей командой замера.
3. Поле состояния ни у одной записи не тронуто; ни одна строка причины, корня, артефактов или
   недостающего не удалена.
4. Рядом с каждым новым ключом стои́т одна фраза о том, почему поле состояния оставлено как есть.
5. Запись продуктового гэпа получает ключ с ИНЫМ именем — о ХОДЕ починки, не о закрытии: названо,
   что отгружено планами 10-33 и 10-35, чем это стережётся и каков ПРЕДЕЛ — отрисовку браузером не
   доказывает ни одно правило суиты, её снимает шаг 4.4, и он человеком не пройден.
6. Блок гэпов остаётся разбираемым: отступы, дефисы записей и порядок ключей внутри записи не
   ломаются, число записей остаётся равным трём.
7. Три счёта артефакта равны шести до и после; состояние шапки остаётся нетерминальным.
  </behavior>

  <action>
**(1) ЗАМЕРИТЬ ЗАКРЫТОСТЬ ДВУХ ДЕФЕКТОВ АРТЕФАКТА ПО ДЕРЕВУ, А НЕ ПО СВОДКЕ.** Для первого — счёт
строк файла, несущих несущий счёт с уточнением ролью, и проверка, что шаги 1.2, 1.5 и 1.9 его
несут. Для второго — чтение выражения форматирования часа в команде посева. ⚠️ ЕСЛИ ЗАМЕР ГОВОРИТ,
ЧТО ДЕФЕКТ НЕ ЗАКРЫТ, ЗАПИСЬ О ЗАКРЫТИИ НЕ ПИШЕТСЯ, а расхождение называется в сводке поимённо: план
исходит из того, что 10-34 сделал заявленное, и обязан это ПРОВЕРИТЬ, а не принять.

**(2) ВНЕСТИ ЗАПИСЬ О ЗАКРЫТИИ КЛЮЧОМ `closed_by:` ВНУТРИ КАЖДОЙ ИЗ ДВУХ ЗАПИСЕЙ.** Имя ключа
задано ЛИТЕРАЛЬНО и выбору исполнителя не подлежит: на нём стои́т гейт проверки настоящей задачи.
Вложенные поля: `plan`, `date`, `measurement` (команда замера ДОСЛОВНО, так, чтобы её можно было
вставить в оболочку), `expected` (ожидаемый исход команды), `status_note` (фраза пункта (3)).
⚠️ КЛЮЧ СТОИ́Т ВНУТРИ ЗАПИСИ ГЭПА, НА ТОМ ЖЕ УРОВНЕ ОТСТУПА, ЧТО `missing:` И `artifacts:`, а не в
конце файла: планировщик закрытия гэпов читает запись, а не файл.

**(3) НАЗВАТЬ, ПОЧЕМУ ПОЛЕ СОСТОЯНИЯ ОСТАВЛЕНО.** Одна фраза на запись: замер словаря по дереву
даёт для гэпов обхода ровно одно значение, значения «закрыто» в словаре проекта нет, а выдуманное не
понял бы ни один потребитель; поле есть запись того, что видел обход в свой день, и стирать её
значило бы закрыть дефект удалением записи о нём (идиома D-30/D-32).

**(4) ПРОДУКТОВЫЙ ГЭП — ЗАПИСЬ О ХОДЕ КЛЮЧОМ `closure_progress:`, А НЕ О ЗАКРЫТИИ.** Имя ключа тоже
задано литерально и тоже несёт гейт; вложенные поля — `shipped`, `guarded_by`, `limit`. Ключ
`closed_by:` у этой записи стоять не имеет права. Содержание: план 10-33 отгрузил условный
подъём и два правила; седьмой круг ревизии нашёл в нём дефект — подъём выносил над КАЖДОЙ следующей
панелью отказ, к ней отношения не имеющий, и текст плашки утверждал о текущем действии неправду
перед необратимым удалением; план 10-35 закрыл дефект снятием обеих заготовок при открытии панели и
завёл правило по исходнику рычага. ПРЕДЕЛ называется последней строкой. ⚠️ СЛОВА «ЗАКРЫТ» И
РАВНОСИЛЬНЫХ В ЭТОЙ ЗАПИСИ БЫТЬ НЕ ДОЛЖНО НИ В КАКОЙ ФОРМЕ: гэп продуктовый, и его закрытие снимает
человек шагом 4.4.

**(5) НИЧЕГО НЕ УДАЛЯТЬ.** Ни поля, ни строки, ни пункта перечня недостающего. Правка ТОЛЬКО
ДОПИСЫВАЕТ. Причина названа: перечень недостающего есть часть записи о дефекте и остаётся ею и
после починки.

**(6) ПРОВЕРИТЬ РАЗБИРАЕМОСТЬ И СЧЁТЫ.** Блок гэпов обязан остаться разбираемым, число записей — три;
три счёта артефакта до и после — по шесть; поле состояния шапки остаётся нетерминальным. Все замеры
— в сводку числами.

**(7) НЕ ТРОГАТЬ ТАБЛИЦЫ ОТМЕТОК, РАЗДЕЛЫ Р-1/Р-2/Р-3 И БЛОК `walkthrough` ШАПКИ.** У приписки Р-3
свой владелец — план 10-34, и вторая приписка из настоящего плана развела бы двух владельцев по
одному тексту.
  </action>

  <acceptance_criteria>
- Записи двух дефектов артефакта несут ключ записи о закрытии с четырьмя частями: план, дата,
  дословная команда замера, ожидаемый исход.
- Обе команды замера ИСПОЛНЕНЫ исполнителем и их вывод приведён в сводку — запись о закрытии
  опирается на прогон, а не на сводку плана 10-34.
- Значение поля состояния у всех трёх записей совпадает с состоянием до правки (диффом); ни одна
  строка блока не удалена (дифф содержит только добавления).
- Запись продуктового гэпа несёт ключ с ИНЫМ именем, и в её тексте нет слова «закрыт» ни в одной
  форме (счёт вхождений равен нулю).
- Число записей блока равно трём; три счёта артефакта равны шести до и после; поле состояния шапки
  нетерминально.
- `uv run pytest tests/test_planning/ -q` завершается нулём.
  </acceptance_criteria>

  <verify>
    <!-- ⚠️ ПРОГОН ОБЯЗАН ВКЛЮЧАТЬ КАТАЛОГ ПРАВИЛ ЗАПИСИ, И ЭТО НЕ ПРЕДПОЧТЕНИЕ, А УРОК ФАЗЫ:
         исполнители этого проекта сужают прогон отбором и вымывают `tests/test_planning/`, после
         чего дефект В ЗАПИСЯХ невидим для собственной команды плана и ловится только полной
         суитой. Правило самозаверения читает ТРИ СЧЁТА и состояние — и ни одной буквы блока гэпов,
         поэтому вторая половина команды считает ЗАПИСИ О ЗАКРЫТИИ. Гейт положительный: он требует
         присутствия, а не отсутствия. -->
    <automated>uv run pytest tests/test_planning/ -q && test "$(grep -c 'closed_by:' .planning/phases/10-rychag-components-modal-html/10-UAT.md)" -ge 2 && test "$(grep -c 'closure_progress:' .planning/phases/10-rychag-components-modal-html/10-UAT.md)" -ge 1</automated>
    <fails_when>ненулевой код возврата (его даёт и красный прогон каталога правил записи, и счёт записей о закрытии меньше двух, и отсутствие записи о ходе починки), либо в итоговой строке прогона есть слово "failed"</fails_when>
  </verify>

  <done>
Обе записи дефектов артефакта несут ключ записи о закрытии с планом, датой, дословной командой замера
и её ожидаемым исходом; закрытость обоих ПРОВЕРЕНА по дереву, а не принята по сводке, и замер
приведён в сводку; поле состояния ни у одной записи не тронуто, и причина этого названа фразой у
каждой; запись продуктового гэпа несёт ключ о ХОДЕ починки с названным пределом и без единого слова
о закрытии; ни одна строка блока не удалена; блок разбираем, записей три; три счёта артефакта равны
шести до и после; состояние остаётся нетерминальным.
  </done>
</task>

<task type="auto">
  <name>Задача 2: ненаблюдённый шаг 2.8 получает одно явное распоряжение вместо молчания</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `10-UAT.md:408-431` — проверка 2 целиком: предусловие, восемь шагов и ожидаемые признаки. ⚠️ ТЕКСТ
  ШАГА 2.8 И ЕГО ОЖИДАЕМЫЙ ПРИЗНАК ЧИТАТЬ ДОСЛОВНО И НЕ ПРАВИТЬ.
- `10-UAT.md:432-440` — заполненная строка отметки проверки 2, где записаны и метод обрыва, и
  замер окна (POST на медленном канале отвечает за 2073 мс, GET доезжал раньше вызова), и
  собственный вывод наблюдателя о том, что отрицательный ответ здесь принадлежит методу. ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО И НЕ ПРАВИТЬ НИ В ОДНОЙ КЛЕТКЕ: распоряжение пишется ПОД таблицей и опирается на неё.
- `10-UAT.md:476-519` — проверка 4 целиком, включая раздел «Как вызвать отказ — ДВА способа, и
  разница между ними названа» и заполненную отметку. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: именно там обработчик
  наблюдён на НАСТОЯЩЕМ отказе (переключение сети), и именно это делает половину шага 2.8
  наблюдённой.
- `10-UAT.md:41-50` — блок `walkthrough` шапки со списком ненаблюдённых шагов. ⚠️ ЧИТАТЬ И НЕ
  ПРАВИТЬ: шаг 2.8 остаётся в списке — распоряжение объясняет его, а не отменяет.
- `10-UAT.md:295-320` — образец формы «прежнее ожидание опровергнуто, источник назван поимённо», уже
  применённый этим файлом. ⚠️ ФОРМА НАСЛЕДУЕТСЯ.
- `app/templates/includes/htmx_error_banner.html:132-139` — два обработчика: какой из них поднимает
  какая заготовка. Читать ради того, чтобы распоряжение называло событие правильно.
- `.planning/phases/10-rychag-components-modal-html/10-35-SUMMARY.md` — момент снятия заготовок после
  правки: он приходится на ОТКРЫТИЕ панели, тогда как в проверке 2 панель уходит на шаге 2.4, то
  есть ДО отказа. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: без этого распоряжение может назвать окно наблюдения
  неверно.
  </read_first>

  <behavior>
1. Под таблицей отметок проверки 2 стои́т приписка, называющая шаг 2.8 ненаблюдённым и разбирающая
   его НА ДВЕ ПОЛОВИНЫ.
2. Первая половина — «обработчик поднимает заготовку на настоящем отказе» — названа НАБЛЮДЁННОЙ, и
   назван замер, которым она наблюдена: проверка 4, переключение сети, заготовка в документе есть и
   несёт верный текст.
3. Вторая половина — «на транспорте перехода, при устаревшем экране и ушедшей панели, человек ВИДИТ
   плашку и читает вторую фразу» — названа НЕнаблюдённой, и названа причина: прерывание транспорта в
   документе поднимает не то событие, что настоящий обрыв.
4. Назван метод, которым вторая половина наблюдаема, и названо, чего он требует: настоящий обрыв
   транспорта, попадающий в окно между ответом POST и приходом follow-up GET; окно названо числом
   из отметки.
5. Назван артефакт-владелец: следующий проход обхода человеком либо агентом с управлением сетевыми
   условиями браузера, а не суита, — и сказано прямо, что суита эту половину не снимает ни одним
   правилом.
6. Назван эффект правки плана 10-35 на это наблюдение — снятие заготовок происходит при ОТКРЫТИИ
   панели, а к шагу 2.8 панель уже ушла, поэтому окно наблюдения не сдвинуто. ⚠️ ЕСЛИ ЗАМЕР ГОВОРИТ
   ИНОЕ — это находка, и она называется вместо утверждения.
7. Текст шага 2.8 и его ожидаемый признак не тронуты; таблица отметок не тронута; список
   ненаблюдённых шагов в шапке не тронут.
  </behavior>

  <action>
**(1) ПРОЧИТАТЬ ОБЕ ОТМЕТКИ И ВЫПИСАТЬ, ЧТО ИМЕННО НАБЛЮДЕНО ГДЕ.** Из отметки проверки 2 —
метод обрыва, замер окна и вывод наблюдателя. Из отметки проверки 4 — способ вызова отказа
(переключение сети) и то, что было наблюдено о плашке. Выписку привести в сводку: распоряжение
опирается на неё, и распоряжение без выписки было бы мнением.

**(2) РАЗВЕСТИ ШАГ НА ДВЕ ПОЛОВИНЫ И НАЗВАТЬ КАЖДУЮ.** Первая — срабатывание обработчика на
настоящем отказе; она наблюдена проверкой 4, и артефакт САМ говорит, что перепроверять обработчик
Фазы 8 не нужно. Вторая — видимость плашки и чтение второй фразы ИМЕННО в состоянии проверки 2
(панель ушла, экран устарел, удалённая карточка ещё нарисована); она не наблюдена. ⚠️ ЭТО РАЗВЕДЕНИЕ
И ЕСТЬ ПРЕДМЕТ ЗАДАЧИ: «шаг не наблюдён» скрывает, что половина его наблюдена, и следующий обход
пролистает обе.

**(3) НАЗВАТЬ ПРИЧИНУ НЕНАБЛЮДЕНИЯ МЕХАНИЗМОМ, А НЕ НЕУДАЧЕЙ.** Прерывание транспорта в документе
поднимает событие прерывания, а не событие отказа отправки; заготовку обрыва связи поднимает второй,
и первый её не поднимает. Поэтому отрицательный ответ принадлежит методу.

**(4) НАЗВАТЬ МЕТОД, КОТОРЫМ ПОЛОВИНА НАБЛЮДАЕМА, И ЕГО ЦЕНУ.** Настоящий обрыв транспорта,
попадающий в окно между ответом POST и приходом follow-up GET. Окно замерено: на медленном канале
POST отвечает за 2073 мс, а GET доезжает раньше, чем успевает сработать переключение снаружи, —
значит метод требует УПРАВЛЕНИЯ сетевыми условиями изнутри инструмента, а не руками. Назвать это
прямо; назвать также, что суита эту половину не снимает ни одним правилом и снять не может.

**(5) НАЗВАТЬ ВЛАДЕЛЬЦА.** Следующий проход обхода — человеком на приёмке фазы либо агентом с
управлением сетевыми условиями браузера при явном разрешении человека. ⚠️ НЕ ОБЪЯВЛЯТЬ ШАГ
НЕПРИМЕНИМЫМ: неприменимость есть свойство стенда (как у шага 1.15, где вложения не было), а здесь
предмет наблюдаем — не хватило инструмента.

**(6) ПРОВЕРИТЬ ЭФФЕКТ ПРАВКИ ПЛАНА 10-35 НА ЭТО НАБЛЮДЕНИЕ ЗАМЕРОМ, А НЕ ДОПУЩЕНИЕМ.** Снятие
заготовок происходит при открытии панели; в проверке 2 панель уходит на шаге 2.4, то есть ДО отказа
follow-up GET, — значит заготовка, поднятая отказом, снятию уже не подлежит и окно наблюдения не
сдвинуто. ⚠️ ЕСЛИ ЧТЕНИЕ СВОДКИ 10-35 ГОВОРИТ ИНОЕ — записать НАЙДЕННОЕ, а не ожидаемое: это
находка о взаимодействии двух правок, и она дороже гладкой записи.

**(7) НЕ ПРАВИТЬ НИ ШАГА, НИ ОТМЕТКИ, НИ СПИСКА НЕНАБЛЮДЁННЫХ ШАГОВ В ШАПКЕ.** Распоряжение
ОБЪЯСНЯЕТ ненаблюдённость, а не отменяет её. Шаг остаётся в списке: снять его оттуда значило бы
объявить наблюдённым то, чего никто не видел.
  </action>

  <acceptance_criteria>
- Под таблицей отметок проверки 2 присутствует приписка, называющая шаг 2.8 и разводящая его на
  две половины с явными словами «наблюдена» и «не наблюдена».
- В приписке названа проверка, которой наблюдена первая половина, и способ вызова отказа в ней
  (переключение сети).
- В приписке названа причина ненаблюдения второй половины МЕХАНИЗМОМ события, а не неудачей.
- В приписке присутствует замер окна в миллисекундах, взятый из отметки проверки 2.
- В приписке назван владелец, и слова «неприменим» в отношении шага 2.8 нет.
- Текст шага 2.8, все шесть таблиц отметок и список ненаблюдённых шагов в шапке совпадают с
  состоянием до правки (диффом).
- `uv run pytest tests/test_planning/ -q` завершается нулём.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed" — в частности от правила самозаверения, если правка сдвинула один из трёх счётов артефакта или его состояние</fails_when>
  </verify>

  <done>
Под таблицей отметок проверки 2 стои́т распоряжение по шагу 2.8, разводящее его на наблюдённую и
ненаблюдённую половины с указанием, чем наблюдена первая; причина ненаблюдения второй названа
МЕХАНИЗМОМ (прерывание транспорта поднимает не то событие); назван метод, которым она наблюдаема, с
замером окна в миллисекундах и с прямым указанием, что суита её не снимает; назван владелец, и шаг
НЕ объявлен неприменимым; эффект правки плана 10-35 на окно наблюдения проверен чтением сводки и
записан НАЙДЕННЫМ; текст шага, таблица отметок и список ненаблюдённых шагов не тронуты.
  </done>
</task>

<task type="auto">
  <name>Задача 3: пять замечаний седьмого круга уходят в перечень отложенного с названными владельцами</name>

  <files>
.planning/phases/10-rychag-components-modal-html/deferred-items.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md:1-5` — правило файла: «сюда
  пишется НАЙДЕННОЕ ВНЕ ГРАНИЦЫ ЗАДАЧИ; правка здесь не делается — граница области правки есть
  предмет, а не осторожность».
- `deferred-items.md:6-41` — раздел плана 10-25: образец формы записи (файл, что найдено, почему не
  правится здесь, владелец). ⚠️ ФОРМА НАСЛЕДУЕТСЯ, А НЕ ИЗОБРЕТАЕТСЯ.
- `deferred-items.md:42-77` и `:78-151` — разделы планов 10-30 и 10-32: образцы записи с ЗАМЕРОМ и с
  явным «владелец не назначен». ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: настоящий план владельца НАЗНАЧАЕТ, и это
  отличие от трёх предыдущих разделов надо назвать прямо.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md:428-499` — раздел `Info` целиком:
  все пять замечаний с файлами, строками, воспроизведёнными свойствами и предложенными починками.
  ⚠️ ЧИТАТЬ ДОСЛОВНО: записи переносятся ПО СОДЕРЖАНИЮ, а не по памяти о нём.
- `.planning/phases/10-rychag-components-modal-html/10-37-SUMMARY.md` — что именно план 10-37 закрыл
  из второго замечания и что осталось. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: без этого запись назовёт замечание
  открытым целиком либо закрытым целиком, и оба утверждения будут ложны.
- `.planning/ROADMAP.md` — раздел Фазы 15: что она объявляет своим предметом. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО:
  владелец назначается ЗАМЕРОМ по предмету фазы, а не догадкой.
  </read_first>

  <behavior>
1. В файле появляется раздел плана 10-39 с ПЯТЬЮ подразделами — по одному на каждое замечание
   седьмого круга.
2. Каждый подраздел несёт четыре обязательные части формы файла: файл (с строками), что найдено
   (с воспроизведённым свойством, а не пересказом), почему не правится здесь, владелец.
3. Владелец НАЗВАН у каждого из пяти, и это отличие от трёх предыдущих разделов файла названо прямо:
   там владелец не назначался, здесь назначается.
4. Причина неправки — ОДНА И ТА ЖЕ у всех пяти и названа один раз в шапке раздела: решение
   владельца оставить замечания следующей партии; плюс, где применимо, файл вне `files_modified`
   любого плана партии.
5. Запись второго замечания названа ЧАСТИЧНОЙ: сказано, какая половина закрыта планом 10-37 по
   необходимости (сличение простой части сделано строгим, потому что без него не выполним `WR-07`) и
   какая осталась (разрыв селекторов регулярным выражением комбинаторов внутри скобок).
6. Запись пятого замечания несёт замер конвенции файла: другое правило той же таблицы уже берёт
   отступ безопасной зоны, то есть конвенция существует и в блоке подъёма не применена.
7. Ни одно из пяти замечаний не чинится: ни `tests/test_pages/test_shell.py`, ни
   `app/static/css/app.css` не тронуты ни на символ.
8. Три существующих раздела файла не тронуты ни на символ.
  </behavior>

  <action>
**(1) ЗАВЕСТИ РАЗДЕЛ И НАЗВАТЬ ОБЩУЮ ПРИЧИНУ ОДИН РАЗ.** Шапка раздела говорит: пять замечаний
седьмого круга ревизии оставлены вне партии РЕШЕНИЕМ ВЛАДЕЛЬЦА, а не недосмотром и не нехваткой
времени; отчёт ревизии перезаписывается каждым кругом (шестой уже перезаписал пятый), поэтому
находка, живущая только в нём, исчезает вместе с ним, — и настоящий раздел есть единственное место,
где она переживёт перезапись.

**(2) ПЕРЕНЕСТИ КАЖДОЕ ЗАМЕЧАНИЕ ПО СОДЕРЖАНИЮ, А НЕ ПО ПАМЯТИ.** Для каждого: файл и строки из
отчёта; воспроизведённое свойство дословно, включая числа и выражения, которые отчёт приводит;
почему свойство сегодня латентно (у четырёх из пяти оно латентно, и это часть записи, а не смягчение);
чем чинится по мнению ревизии. ⚠️ ЕСЛИ ЧТЕНИЕ ДЕРЕВА РАСХОДИТСЯ С ОТЧЁТОМ — записать НАЙДЕННОЕ и
назвать расхождение: это тот же класс, что «запись шире дерева», и переносить его дальше нельзя.

**(3) ВТОРОЕ ЗАМЕЧАНИЕ — ЗАПИСЬ О ПОЛОВИНЕ.** Прочитать сводку плана 10-37 и записать: одна половина
(сличение простой части селектора подстрокой) ЗАКРЫТА планом 10-37 ПО НЕОБХОДИМОСТИ — без неё
невыполним `WR-07`, потому что имя тега тела документа есть подстрока имени атрибутного селектора
обёртки тела; вторая половина (разрыв селекторов регулярным выражением комбинаторов внутри скобок:
признак включения списка классов, аргумент позиционного псевдокласса) НЕ закрыта и остаётся здесь.
⚠️ БЕЗ ЭТОГО РАЗВЕДЕНИЯ ЗАПИСЬ ЛОЖНА В ЛЮБУЮ СТОРОНУ: «закрыто» неверно, «открыто» тоже.

**(4) НАЗНАЧИТЬ ВЛАДЕЛЬЦА КАЖДОМУ.** Владелец: следующая партия закрытия гэпов Фазы 10 либо Фаза 15.
⚠️ ВЫБОР МЕЖДУ ДВУМЯ ДЕЛАЕТСЯ ЗАМЕРОМ ПО ПРЕДМЕТУ ФАЗЫ 15, ПРОЧИТАННОМУ В РОАДМАПЕ, А НЕ НА ГЛАЗОК:
если предмет Фазы 15 накрывает замечание — назвать её и процитировать основание одной строкой; если
нет — назвать следующую партию. Назвать также прямо, что три предыдущих раздела этого файла владельца
НЕ НАЗНАЧАЛИ, а настоящий назначает, — иначе читатель примет разницу форм за небрежность.

**(5) НЕ ЧИНИТЬ НИ ОДНОГО.** Файлы `tests/test_pages/test_shell.py` и `app/static/css/app.css` в
`files_modified` настоящего плана не входят. Правка любого из них — нарушение границы, ради записи о
которой этот файл и существует.

**(6) ЗАМЕРИТЬ ДО И ПОСЛЕ.** Число разделов файла и число подразделов. ⚠️ ОРИЕНТИР ПЛАНИРОВЩИКА, СНЯТЫЙ ЗАМЕРОМ 2026-09-09: 3
раздела и 6 подразделов → 4 раздела и 11 подразделов. Расхождение с ориентиром — находка.
  </action>

  <acceptance_criteria>
- В `deferred-items.md` присутствует раздел плана 10-39; счёт подразделов файла вырос с 6 до 11.
- У каждого из пяти подразделов присутствуют все четыре части формы файла, и часть «владелец»
  непуста.
- Подраздел второго замечания содержит слова о ПОЛОВИНЕ и называет план 10-37 и основание
  необходимости.
- Подраздел пятого замечания содержит замер конвенции файла (другое правило той же таблицы уже
  берёт отступ безопасной зоны).
- В шапке раздела назван факт, что три предыдущих раздела владельца не назначали, а этот назначает.
- `tests/` и `app/` не изменены ни байтом (диффом); три существующих раздела файла не тронуты.
- `uv run pytest tests/test_planning/ -q` завершается нулём.
  </acceptance_criteria>

  <verify>
    <!-- Гейт положительный и по ДВУМ файлам сразу: каталог правил записи ловит дефект в записях
         фазы, счёт подразделов ловит потерянную запись. Замер до правки: 6 подразделов; пять новых дают порог 11. -->
    <automated>uv run pytest tests/test_planning/ -q && test "$(grep -c '^### ' .planning/phases/10-rychag-components-modal-html/deferred-items.md)" -ge 11</automated>
    <fails_when>ненулевой код возврата (его даёт и красный прогон каталога правил записи, и счёт подразделов меньше 11), либо в итоговой строке прогона есть слово "failed"</fails_when>
  </verify>

  <done>
Раздел плана 10-39 несёт пять подразделов по числу замечаний седьмого круга; у каждого названы файл
со строками, воспроизведённое свойство, причина неправки и ВЛАДЕЛЕЦ, выбранный замером по предмету
Фазы 15, а не догадкой; отличие от трёх предыдущих разделов (там владелец не назначался) названо
прямо; запись второго замечания разведена на закрытую планом 10-37 половину и оставшуюся, с
основанием необходимости; ни один файл продукта или суиты не тронут; замер числа разделов и
подразделов до и после приведён в сводку числами.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| записи фазы → следующая партия закрытия гэпов | блок гэпов читается как перечень того, что осталось сделать; протухшая запись направляет работу на уже сделанное |
| артефакт обхода → человек на приёмке | распоряжение по шагу определяет, будет ли шаг наблюдён в следующий раз или пролистан |
| отчёт ревизии → перечень отложенного | отчёт перезаписывается каждым кругом; находка без переноса исчезает бесследно |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-39-01 | Repudiation | блок гэпов `10-UAT.md` | medium | mitigate | Артефакт противоречит собственному телу: два починенных дефекта объявлены провалившимися. Закрывается записями о закрытии с воспроизводимой командой замера (задача 1) |
| T-10-39-02 | Spoofing | запись о продуктовом гэпе | high | mitigate | Запись, объявляющая продуктовый гэп закрытым по зелени правил суиты, есть самозаверение: отрисовку браузером не доказывает ни одно правило. Закрывается ключом о ХОДЕ починки и запретом слова «закрыт» в этой записи |
| T-10-39-03 | Repudiation | шаг 2.8 | medium | mitigate | Шаг, не засчитанный ни в одну сторону и не разобранный, пролистывается следующим обходом вместе с наблюдаемой половиной. Закрывается распоряжением, разводящим половины (задача 2) |
| T-10-39-04 | Information disclosure | перечень отложенного | low | accept | Записи называют пути файлов и строки исходников — сведения, доступные любому читателю репозитория; наружу не выносятся |
| T-10-39-05 | Tampering | поле состояния гэпа | medium | mitigate | Подмена поля выдуманным значением сломала бы разбор у любого потребителя и стёрла бы запись о наблюдённом. Закрывается прямым запретом и отдельным ключом |
| T-10-39-SC | Tampering | npm/pip/cargo installs | n/a | accept | ЗАМЕР: настоящий план не ставит ни одного пакета — правятся два документа записи. Гейт легитимности пакетов неприменим |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q` — зелено (правило самозаверения, правило регламента отметок требований, правило независимости гейтов от вердикта).
2. Блок гэпов разбираем, записей три, ни одна строка не удалена (диффом).
3. Поле состояния ни у одной записи гэпа не тронуто (диффом).
4. Слов «закрыт» и равносильных в записи продуктового гэпа нет (чтением записи).
5. Текст шага 2.8, все шесть таблиц отметок и блок `walkthrough` шапки не тронуты (диффом).
6. Три существующих раздела `deferred-items.md` не тронуты (диффом); новых подразделов пять.
7. `app/` и `tests/` не тронуты ни одним байтом (диффом).
</verification>

<success_criteria>
- Следующая партия закрытия гэпов, прочитав блок гэпов, не заведёт плана на уже починенное и не
  объявит закрытым то, что ждёт человека.
- Ненаблюдённый шаг имеет одно явное распоряжение с названным методом, замером и владельцем.
- Все пять замечаний седьмого круга переживут перезапись отчёта ревизии.
- Частично закрытая находка названа частичной.
- Ни один файл продукта или суиты не правлен; три счёта артефакта и его состояние не сдвинуты.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-39-SUMMARY.md` when done
</output>
