---
phase: 10-rychag-components-modal-html
plan: 45
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, ПО ТОЙ ЖЕ ПРИЧИНЕ, ЧТО У ПЛАНОВ 10-41…10-43: форма
# `type: tdd` описывается блоком `<feature>` без полей `read_first` и `acceptance_criteria`, которых
# потребитель выхода требует на КАЖДОЙ задаче. Дисциплина RED→GREEN соблюдается на уровне задач.
wave: 4
# ⚠️ ВОЛНА 4 — ДВЕ ЗАВИСИМОСТИ РАЗНОГО РОДА, И ОБЕ НАЗЫВАЮТСЯ ПОИМЁННО.
# (1) ФАЙЛОВАЯ. `tests/test_pages/test_shell.py` правят планы 10-41 (волна 1) и 10-42 (волна 2);
#     настоящий план правит его третьим. Одновременное исполнение задело бы один файл двумя руками.
# (2) ОБЛАСТЬ КОМАНДЫ ПРОВЕРКИ. Все три задачи утверждают ПУСТОЙ ДИФФ ПО `app`, а план 10-46
#     ЗАКОННО правит `app/templates/components/modal.html` и `app/pages/schedules.py`. Пересечения
#     `files_modified` у пары НЕТ, поэтому файловый гейт волны молчит; пересекается ОБЛАСТЬ КОМАНДЫ.
#     При одновременном исполнении проверка настоящего плана отказала бы на работе соседа
#     НЕАТРИБУТИРУЕМО — ровно тот класс отказа, ради закрытия которого партия и существует.
#
# ⚠️ ИЗ ТРЁХ МАРШРУТОВ ВЫБРАН «ОБЪЯВИТЬ РЕБРО», И ОСНОВАНИЕ ЗАПИСЫВАЕТСЯ ЗДЕСЬ, ЧТОБЫ СЛЕДУЮЩИЙ КРУГ
# ВИДЕЛ РЕШЕНИЕ, А НЕ РЕЗУЛЬТАТ.
#   • Сужение области команды (исключение двух файлов соседа из пути `git diff`) сохранило бы
#     параллелизм волны, но НЕ ПОКРЫЛО БЫ ТОГО, ЧТО НАСТОЯЩИЙ ПЛАН ОБЯЗУЕТСЯ НЕ ТРОГАТЬ:
#     `app/templates/components/modal.html` есть РЫЧАГ ФАЗЫ и лежит внутри `app/templates/`, то есть
#     внутри дословно заявленного запрета этого плана, а искушение править его названо прямо в
#     задаче 1 («рычаг обязан остаться вне множества владельцев» чинится либо двумя предикатами,
#     либо правкой самого рычага). Исключение подменило бы ложную красноту ЛОЖНОЙ ЗЕЛЕНЬЮ ровно на
#     том файле, ради которого гейт и стои́т, — то есть заплатило бы за атрибутируемость дырой в
#     покрытии, а не выиграло бы её.
#   • Пометка `coupling_justified` неприменима ПО ОПРЕДЕЛЕНИЮ: два порядка исполнения дают РАЗНЫЙ
#     результат. Сосед первым и зафиксированным → `git diff` сличает дерево с HEAD и его правок не
#     видит вовсе, гейт остаётся ЦЕЛЫМ и покрывающим. Настоящий план первым → его гейт красен на
#     чужой незафиксированной работе.
#   • Развод по волнам БЕЗ объявленного ребра дал бы ту же сериализацию, но СКРЫЛ БЫ НАПРАВЛЕНИЕ, а
#     направление здесь несущее: пишет сосед, утверждает пустоту настоящий план.
# Цена принята и называется: заводится волна 4, и последние два плана партии идут друг за другом, а
# не рядом. Гейт `git diff --numstat -- app` при этом НЕ СУЖАЕТСЯ ни на один путь.
depends_on: ["10-42", "10-46"]
gap_closure: true
gap_ids: [REVIEW-8/WR-01, REVIEW-8/WR-04, REVIEW-8/WR-05, REVIEW-8/WR-07, REVIEW-8/IN-01, REVIEW-8/IN-02]
files_modified:
  - tests/test_pages/test_shell.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ: `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3).
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ЕДИНСТВЕННОСТЬ ИСТОЧНИКА ЗАГОТОВОК ПЕРЕСТАЁТ ЗАВИСЕТЬ ОТ РОДА КАВЫЧЕК: второй шаблон, несущий сценарий и обращающийся к заготовке ЛЮБЫМ родом кавычек либо без них, краснит гейт"
    - "ПРАВИЛО ДОСТИЖИМОСТИ РЫЧАГА ЗНАЕТ ОБЕ ФОРМЫ ПОДКЛЮЧЕНИЯ МАКРОСА: подключение с переименованием модуля перестаёт быть невидимым, и это показано на доктóренной копии"
    - "КОНТРОЛЬ ДОСТИЖИМОСТИ ПОДМЕНЯЕТ И СОСТАВ НАСЛЕДНИКОВ, А НЕ ТОЛЬКО ЧИТАТЕЛЯ: сценарий «восьмой экран входа, добавленный без правки суиты», которым докстринг оправдывает чтение дерева, ПОКАЗАН исполнением"
    - "УТВЕРЖДЕНИЕ УРОВНЯ МОДУЛЯ ПЕРЕЕЗЖАЕТ В ИМЕНОВАННОЕ ПРАВИЛО: срабатывание перестаёт ронять СБОР всего файла безымянным отказом и перестаёт исчезать под ключом оптимизации интерпретатора"
    - "ТРЕТИЙ ЭЛЕМЕНТ ТРОЙКИ РАЗБОРЩИКА ТАБЛИЦЫ НАЗЫВАЕТСЯ ТЕМ, ЧТО ОН ЕСТЬ, и подсчёт объявлений перестаёт вестись по куску, вбирающему прозу перед блоком"
    - "СЧЁТ ВЛОЖЕННОСТИ ТЕЛА МЕТОДА ЗНАЕТ СТРОКОВЫЕ ЛИТЕРАЛЫ: фигурная скобка внутри литерала JS больше не двигает глубину, и это показано на синтетике"
    - "НИ ОДНА ЗАДАЧА НЕ ПРАВИТ НИ ПРОДУКТА, НИ РАЗМЕТКИ: все шесть находок живут в модуле суиты, и живое дерево не трогается ни на символ"
    - statement: "ДОБАВЛЕНИЕ ВТОРОЙ ФОРМЫ ПОДКЛЮЧЕНИЯ МАКРОСА НЕ РАСШИРЯЕТ ЖИВОГО ГРАФА: замер 2026-09-10 даёт НОЛЬ вхождений этой формы в `app/templates/`, поэтому граф корня шелла авторизации остаётся из пяти звеньев — проверяемо прогоном, а не разбором"
      verification: backstop
  artifacts:
    - "tests/test_pages/test_shell.py — `_declares_banner_id`, `_names_banner_literally` и квотонезависимые половины гейта единственности источника вместе с отрицательным контролем на копии с другим родом кавычек"
    - "tests/test_pages/test_shell.py — `TEMPLATE_MACRO_IMPORT_AS_RE` и её включение в перечень образцов рёбер графа"
    - "tests/test_pages/test_shell.py — `_auth_shell_heirs`, принимающий читателя и состав имён параметрами, и контроль «новый наследник»"
    - "tests/test_pages/test_shell.py — `test_the_lever_path_lives_under_the_templates_root` вместо утверждения уровня модуля"
    - "tests/test_pages/test_shell.py — `_css_rule_block(raw)` и приведённый докстринг `_css_rules`"
    - "tests/test_pages/test_shell.py — счёт вложенности тела метода с учётом строковых литералов и контроль на синтетике"
  key_links:
    - "квотонезависимость обеих половин → предмет гейта единственности: единственное, чем ВТОРОЙ источник отличим от единственного; ломается — литеральная копия сценария в другом шелле проходит обе половины, и D-01 нарушается молча"
    - "вторая форма подключения макроса → неваку́умность правила достижимости: единственное, чем «рычаг не достижим» отличимо от «правило не умеет видеть ребро»; ломается — панель подтверждения на экране входа приезжает при двух зелёных правилах"
    - "подмена состава наследников → показанность заявленного сценария: единственное, чем «восьмой экран, добавленный без правки суиты» перестаёт быть обещанием; ломается — докстринг оправдывает чтение дерева сценарием, ни разу не исполненным"
  prohibitions:
    - statement: "РАЗМЕТКА `app/templates/` И ТАБЛИЦА СТИЛЕЙ НЕ ПРАВЯТСЯ НИ НА СИМВОЛ: все шесть находок живут в модуле суиты, и подключение рычага, дописанное в живой шаблон ради зелени контроля, завело бы панель подтверждения на экране входа"
      status: flagged-unverified
      verification: test
    - statement: "ГРАНИЦА `_template_chain` ПЛАНА 10-19 НЕ ТРОГАЕТСЯ: изъятие подключения макросов там имеет своё записанное основание, и правка настоящего плана касается ТОЛЬКО сборщика графа достижимости"
      status: flagged-unverified
      verification: none
    - statement: "ВЕРДИКТ `10-VERIFICATION.md` НЕ ПРАВИТСЯ, ОТМЕТКА `FORM-06` В `.planning/REQUIREMENTS.md` НЕ ТРОГАЕТСЯ"
      status: flagged-unverified
      verification: test
    - statement: "ОЖИДАНИЯ КОНТРОЛЕЙ НЕ ВЫЧИСЛЯЮТСЯ ИЗ ПРОВЕРЯЕМОГО ИСТОЧНИКА: доктóренные тексты и ожидаемые числа выписываются литералами"
      status: flagged-unverified
      verification: none
---

<objective>
Шесть находок восьмого круга ревизии, живущих в модуле шелла и не попавших в ведущий срез партии,
закрываются одним планом: все шесть — о правилах, которые видят у́же собственного предмета.

**`REVIEW-8/WR-01` — единственность источника стала зависеть от рода кавычек.** План 10-35 заменил
квотонезависимую проверку на две, обе с кавычками: разметочная половина ищет объявление
идентификатора ТОЛЬКО в двойных кавычках, сценарная — литерал заготовки ТОЛЬКО в одинарных.
Воспроизведено исполнением: второй шаблон, несущий ТОЛЬКО сценарий и обращающийся к заготовке
ДВОЙНЫМИ кавычками, проходит ОБЕ половины. То есть гейт, заведённый против второго источника, второй
источник и пропускает — при условии, что тот набран не той кавычкой.

**`REVIEW-8/WR-04` — правило достижимости не знает второй формы подключения макроса.** Образец
покрывает форму с перечислением имён и не покрывает форму с переименованием модуля. Воспроизведено на
доктóренной копии экрана восстановления пароля. ⚠️ ЗАМЕР 2026-09-10: вхождений второй формы в
`app/templates/` — НОЛЬ (первой — 277), то есть находка ЛАТЕНТНА, и правка графа живого дерева не
двигает. Это надо предъявить числом, а не обещанием.

**`REVIEW-8/WR-07` — контроль подменяет читателя, но состав наследников берёт с диска.** Сценарий,
которым докстринг оправдывает чтение дерева вместо списка имён, — «восьмой экран входа, добавленный
без правки суиты» — исполнением НЕ ПОКАЗАН ни разу. Сегодня наследников семь, и все семь наследуют
корню напрямую.

**`REVIEW-8/IN-01` — утверждение уровня модуля.** Срабатывание уронит СБОР всего файла (4793 строки,
**161 правило** — оба числа переснято прогоном 2026-09-10) одним безымянным отказом, а под ключом
оптимизации интерпретатора исчезнет целиком. В модуле, где каждый отказ обязан называть починку, ни
одного текста при нём не стои́т.
⚠️ **ПРЕЖНЕЕ ЧИСЛО 168 НЕ ВЫЧЁРКИВАЕТСЯ, А ПОМЕЧАЕТСЯ УСТАРЕВШИМ С НАЗВАННЫМ ИСТОЧНИКОМ РАСХОЖДЕНИЯ**
(идиома D-30/D-32, та же, что применена к замеру подключений в задаче 2). 168 есть счёт ДВУХ модулей
ВМЕСТЕ: замер 4 отчёта `10-VERIFICATION.md` («168 passed прогоном верификатора») прогонял
`tests/test_pages/test_shell.py` и `tests/test_templates/test_walkthrough_anchors.py` ОДНОЙ командой,
и двухмодульное число было приписано ОДНОМУ модулю. Пересъёмка на HEAD: `test_shell.py` — **161**,
`test_walkthrough_anchors.py` — **7**, вместе — 168. Ошибкой прежнее значение не было: оно
принадлежало ДРУГОМУ МНОЖЕСТВУ, и в этом вся находка. Число 4793 переснято и верно.
⚠️ И ЭТО ЖЕ ЕСТЬ УКОР НАСТОЯЩЕМУ ПЛАНУ: он требует пересъёмки унаследованных чисел прямым текстом
(`read_first` задачи 2 и её шаг (6)) и к собственному числу правил файла эту дисциплину не применил.
Ни одна автоматическая команда проверки и ни один объявленный при ней признак отказа от этого числа
не зависят, то есть исполнение оно не ломало, — это запись шире дерева ровно того класса, против
которого партия работает.

**`REVIEW-8/WR-05` — третий элемент тройки разборщика таблицы не блок.** Он есть всё с конца
предыдущего блока, то есть вбирает стоящую перед блоком прозу. Замер: у блока подъёма в десять строк
длина куска 7487 символов, первые два символа — открывающий комментарий; счёт словосочетания
положения даёт 2, а счёт того же словосочетания с точкой с запятой — 1. Отрицательный контроль
держится ОДНИМ символом.

**`REVIEW-8/IN-02` (половина о фигурных скобках) — счёт вложенности не знает строковых литералов.**
Фигурная скобка внутри литерала JS двигает глубину. Сегодня таких литералов в теле метода нет,
находка ЛАТЕНТНА.

Purpose: пять из шести — регрессии или дыры, внесённые самой седьмой партией, и все шесть суть один
класс: правило видит у́же собственного предмета, а зелёный прогон читается как «предмет держится».

Output: шесть закрытых находок, у каждой — контроль, краснеющий на форме, которую правило сегодня
принимает.

⚠️ **НИ ОДНОЙ ПРАВКИ ПРОДУКТА И РАЗМЕТКИ.** Все шесть находок живут в `tests/test_pages/test_shell.py`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-35-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**Новые символы в `tests/test_pages/test_shell.py`:**

- `_BANNER_ID_ATTR_RE`, `_declares_banner_id(text, banner_id)` — объявление идентификатора заготовки
  в разметке, независимо от рода кавычек и включая незакавыченную форму;
- `_names_banner_literally(text, banner_id)` — обращение к заготовке литералом любого рода кавычек;
- `test_control_a_second_script_with_the_other_quote_reddens_the_single_source_gate` — контроль
  квотонезависимости;
- `TEMPLATE_MACRO_IMPORT_AS_RE` — образец второй формы подключения макроса;
- `test_control_an_auth_template_that_imports_the_lever_with_an_alias_reddens` — контроль второй
  формы;
- `test_control_a_new_auth_heir_that_reaches_the_lever_reddens` — контроль подменённого состава
  наследников;
- `test_the_lever_path_lives_under_the_templates_root` — именованное правило вместо утверждения
  уровня модуля;
- `_css_rule_block(raw)` — блок из дословного куска, без стоящей перед ним прозы;
- `_JS_STRING_QUOTES` и счёт вложенности с учётом строковых литералов в `_lever_show_body`;
- `test_control_a_brace_inside_a_js_string_does_not_move_the_depth` — контроль на синтетике.

**Изменённые символы:** `test_notice_area_and_failure_banner_have_a_single_source` (имя уточняется
исполнителем по дереву), `TEMPLATE_MACRO_IMPORT_RE` и перечень образцов в `_template_graph_from`,
`_auth_shell_heirs`, `_auth_shell_lever_findings`, `MODAL_LEVER_TEMPLATE` (выводится без утверждения
уровня модуля), докстринг `_css_rules`, контроли, считающие объявления по дословному куску,
`_lever_show_body`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: единственность источника заготовок перестаёт зависеть от рода кавычек</name>

  <files>
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `tests/test_pages/test_shell.py:2560-2640` — гейт единственности источника ЦЕЛИКОМ: сборка карты
  шаблонов, разметочная половина (объявление идентификатора), сценарная половина (`addEventListener`
  плюс литерал заготовки) и половина включений. ⚠️ ЧИТАТЬ ДОСЛОВНО ВЕСЬ АБЗАЦ «⚠️ ПРЕЖНЯЯ ФОРМА
  МЕМБЕРШИПА СУЖЕНА, А НЕ СНЯТА»: там записано, ПОЧЕМУ предмет утверждается прямо и дважды, и это
  основание ОСТАЁТСЯ ВЕРНЫМ — правится не оно, а зависимость обеих половин от рода кавычек.
- `tests/test_pages/test_shell.py:2044-2051` — `FAILURE_BANNER_OWNER`, `FAILURE_BANNER_SHELLS`,
  `FAILURE_BANNER_IDS`.
- `app/templates/includes/htmx_error_banner.html` — единственный владелец заготовок: каким родом
  кавычек он объявляет идентификаторы и каким обращается к ним в сценарии. Читать глазом на факт.
- `app/templates/components/modal.html:699-704` — тело метода открытия рычага: обе заготовки названы
  ОДИНАРНЫМИ кавычками, регистрации обработчика в файле нет. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: рычаг обязан
  остаться ВНЕ множества владельцев после правки, и это проверяется прогоном, а не рассуждением.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, находка `WR-01` — протокол
  воспроизведения исполнением.
  </read_first>

  <behavior>
1. Копия дерева шаблонов, где ВТОРОЙ файл несёт регистрацию обработчика и обращается к заготовке
   ДВОЙНЫМИ кавычками, краснит сценарную половину гейта. Сегодня она зелена.
2. Копия, где второй файл объявляет идентификатор заготовки ОДИНАРНЫМИ кавычками, краснит
   разметочную половину. Сегодня она зелена.
3. Копия, где второй файл объявляет идентификатор БЕЗ кавычек, краснит разметочную половину.
4. На НЕТРОНУТОМ дереве обе половины по-прежнему дают ровно одного владельца, и рычаг в него НЕ
   входит.
  </behavior>

  <action>
**(1) RED. НАПИСАТЬ КОНТРОЛЬ КВОТОНЕЗАВИСИМОСТИ ПЕРВЫМ.**
`test_control_a_second_script_with_the_other_quote_reddens_the_single_source_gate` собирает КАРТУ
ШАБЛОНОВ В ПАМЯТИ (не правя дерево) и прогоняет по ней обе половины отбора. ⚠️ ЧТОБЫ ЭТО СТАЛО
ВОЗМОЖНЫМ, ОТБОР ОБЕИХ ПОЛОВИН ВЫНОСИТСЯ ИЗ ТЕЛА ПРАВИЛА В ФУНКЦИИ, ПРИНИМАЮЩИЕ ТЕКСТ, — по той же
причине и той же формой, что параметр пути у `_lever_clearing_findings`: без этого контроль зубов
невыразим. Три случая контроля (другой род кавычек в сценарии, другой род в разметке, разметка без
кавычек) сегодня красны; записать команду, код возврата, имя правила, ожидаемое и полученное в сводку
как свидетельство RED.

**(1a) НАЗВАТЬ ПРЕДМЕТ ГЕЙТА ЕГО РЕШЕНИЕМ.** Гейт единственности источника есть машинное
принуждение D-01 в его половине об одном источнике на одну сущность: два обработчика на одну
заготовку дают удвоенную реакцию, и именно этот исход D-01 отверг. Приписка об этом ставится в
докстринг правила, чтобы следующий круг не выводил предмет заново.

**(2) GREEN — ДВА КВОТОНЕЗАВИСИМЫХ ПРЕДИКАТА.** `_declares_banner_id(text, banner_id)` истинен,
когда текст объявляет признак идентификатора со значением, равным имени заготовки, при ЛЮБОМ роде
кавычек и БЕЗ кавычек; образец собирается вокруг константы имени, а не выписывается второй копией.
`_names_banner_literally(text, banner_id)` истинен, когда имя заготовки встречается как строковый
литерал ЛЮБОГО рода кавычек. Сценарная половина остаётся КОНЪЮНКЦИЕЙ регистрации обработчика и
литерала — это её предмет, и он не ослабляется.

**(3) GREEN — ДОКСТРИНГ ПРИВОДИТСЯ, А ПРЕЖНЕЕ ОСНОВАНИЕ ОСТАЁТСЯ.** Абзац «⚠️ ПРЕЖНЯЯ ФОРМА
МЕМБЕРШИПА СУЖЕНА, А НЕ СНЯТА» остаётся дословно: его посылка верна. К нему дописывается приписка:
сужение внесло ЗАВИСИМОСТЬ ОТ РОДА КАВЫЧЕК, найденную восьмым кругом (`WR-01`), и она закрыта двумя
предикатами; кавычка перестала быть частью предмета, потому что предмет есть ИСТОЧНИК, а не его
набор.

**(4) ПОДТВЕРДИТЬ ПРОГОНОМ, ЧТО РЫЧАГ ОСТАЛСЯ ВНЕ МНОЖЕСТВА ВЛАДЕЛЬЦЕВ.** Сценарная половина требует
регистрации обработчика; рычаг её не несёт. ⚠️ ЭТО ЗАМЕР, А НЕ ВЫВОД: предъявить в сводке множество
владельцев по обеим половинам после правки и убедиться, что оно состоит из одного файла.
  </action>

  <acceptance_criteria>
- Три случая контроля (другой род кавычек в сценарии, другой род в разметке, разметка без кавычек)
  краснят гейт; до правки все три зелены — оба состояния названы в сводке.
- На нетронутом дереве обе половины дают ровно одного владельца, и `components/modal.html` в него не
  входит; множество названо в сводке поимённо.
- Оба предиката собираются вокруг `FAILURE_BANNER_IDS`, а не выписывают имена заготовок второй
  копией — проверяется чтением диффа.
- Абзац «⚠️ ПРЕЖНЯЯ ФОРМА МЕМБЕРШИПА СУЖЕНА, А НЕ СНЯТА» присутствует дословно и дополнен припиской.
- `git diff --numstat -- app` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "single_source or notice_area or failure_banner"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо отобрано меньше правил, чем до правки плюс один новый контроль</fails_when>
    <automated>git diff --numstat -- app</automated>
    <fails_when>вывод НЕПУСТ — продукт или разметка тронуты, а задача правит только модуль суиты</fails_when>
  </verify>

  <done>
Обе половины гейта единственности источника не зависят от рода кавычек и от их наличия; три формы
второго источника краснят гейт; рычаг остался вне множества владельцев и это подтверждено прогоном;
прежнее основание сужения сохранено и дополнено припиской.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: правило достижимости рычага видит обе формы подключения, подменяет состав наследников и не роняет сбор файла</name>

  <files>
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `tests/test_pages/test_shell.py:4495-4520` — `AUTH_SHELL_ROOT`, `_TEMPLATES_ROOT_RELATIVE`,
  утверждение уровня модуля, вывод `MODAL_LEVER_TEMPLATE` и `TEMPLATE_MACRO_IMPORT_RE` вместе с
  абзацем замера подключений. ⚠️ ЗАМЕР В АБЗАЦЕ (246 подключений, 278 сырых вхождений, 32 в прозе)
  ПЕРЕСНИМАЕТСЯ, а не переписывается: он снят до правок соседних планов.
- `tests/test_pages/test_shell.py:4520-4560` — `_auth_shell_template_source`, `_template_reader_with`,
  `_auth_shell_heirs` целиком, включая оба абзаца оснований («выписать семь имён было бы отказом» и
  «наследование берётся транзитивно»). ⚠️ ИМЕННО ЗДЕСЬ ЖИВЁТ `WR-07`: параметра читателя у сборщика
  наследников нет.
- `tests/test_pages/test_shell.py:4560-4650` — `_template_graph_from` целиком: перечень образцов
  рёбер, громкий отказ на отсутствующем звене, карта ссылающихся.
- `tests/test_pages/test_shell.py:4650-4740` — `_template_reach_path`, `_auth_shell_lever_findings`,
  `test_no_auth_shell_template_reaches_the_modal_lever` с тремя антивакуумными половинами.
- `tests/test_pages/test_shell.py:4740-4793` — `test_control_an_auth_template_that_imports_the_lever_reddens`
  целиком: форма доктóривания читателем.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, находки `WR-04`, `WR-07`, `IN-01` —
  протоколы воспроизведения.
  </read_first>

  <behavior>
1. Доктóренная копия наследника, подключающая рычаг ФОРМОЙ С ПЕРЕИМЕНОВАНИЕМ МОДУЛЯ, краснит правило
   достижимости ровно одной находкой, и в её тексте назван и наследник, и рычаг, и путь. Сегодня
   правило зелено.
2. Подменённый состав наследников, куда добавлено имя НОВОГО экрана входа, наследующего корню и
   подключающего рычаг, краснит правило ровно одной находкой, называющей ЭТОТ новый экран. Сегодня
   выразить это нечем: сборщик наследников читает дерево напрямую.
3. `test_the_lever_path_lives_under_the_templates_root` краснеет ИМЕНЕМ ПРАВИЛА и текстом с починкой,
   если путь рычага перестал лежать под корнем разметки. Сегодня то же условие роняет СБОР файла
   безымянно.
4. На нетронутом дереве правило достижимости по-прежнему зелено, наследников семь, граф корня — пять
   звеньев.
  </behavior>

  <action>
**(1) RED. НАПИСАТЬ ДВА КОНТРОЛЯ ПЕРВЫМИ.** Контроль второй формы подключения и контроль нового
наследника. Оба сегодня красны — первый потому, что ребро невидимо, второй потому, что состав
наследников подменить нечем. Записать команду, код возврата, имена правил, ожидаемое и полученное в
сводку как свидетельство RED.

**(2) GREEN — ВТОРАЯ ФОРМА РЕБРА.** `TEMPLATE_MACRO_IMPORT_AS_RE` покрывает форму с переименованием
модуля, с подрезкой и обоими родами кавычек, той же формой, что уже действующий образец. Она
добавляется в перечень образцов `_template_graph_from` ТРЕТЬИМ ребром подключения, а не заменяет
первое: две формы Jinja различны, и один образец на обе был бы шире каждой.
⚠️ ЗАМЕР ПРЕДЪЯВЛЯЕТСЯ ЧИСЛОМ: вхождений второй формы в `app/templates/` сегодня НОЛЬ (первой — 277),
поэтому живой граф не расширяется. Это надо СНЯТЬ ПРОГОНОМ и назвать в сводке, а не унаследовать из
плана: план писан до правок соседних планов партии.

**(3) GREEN — ЧИТАТЕЛЬ И СОСТАВ ИМЁН ПАРАМЕТРАМИ У СБОРЩИКА НАСЛЕДНИКОВ.** `_auth_shell_heirs`
получает два необязательных входа: читателя исходника и состав относительных имён дерева. Умолчания —
сегодняшние чтение с диска и обход дерева, и все действующие вызовы остаются побайтово прежними.
`_auth_shell_lever_findings` пробрасывает читателя И состав имён в сборщик наследников, а не только в
сборщик графа. Абзац основания пишется дословной формой абзаца о параметре пути у
`_lever_clearing_findings`.

**(4) GREEN — КОНТРОЛЬ НОВОГО НАСЛЕДНИКА.** `test_control_a_new_auth_heir_that_reaches_the_lever_reddens`
подаёт состав имён, равный настоящему ПЛЮС одно имя нового экрана, и читателя, который на это имя
отдаёт текст, наследующий корню шелла авторизации и подключающий рычаг. Ожидание: ровно одна находка,
и в ней назван НОВЫЙ экран. ⚠️ ЭТО И ЕСТЬ СЦЕНАРИЙ, КОТОРЫМ ДОКСТРИНГ СБОРЩИКА ОПРАВДЫВАЕТ ЧТЕНИЕ
ДЕРЕВА; до настоящей задачи он был обещанием. Приписка об этом ставится в докстринг сборщика.

**(5) GREEN — УТВЕРЖДЕНИЕ УРОВНЯ МОДУЛЯ ПЕРЕЕЗЖАЕТ В ПРАВИЛО.** Вывод `MODAL_LEVER_TEMPLATE`
перестаёт опираться на утверждение уровня модуля: имя собирается из хвоста пути рычага после корня
разметки БЕЗ утверждения, а условие «путь рычага лежит под корнем разметки» утверждается ИМЕНОВАННЫМ
правилом `test_the_lever_path_lives_under_the_templates_root`, чей текст отказа называет обе
величины и починку. ⚠️ ОСНОВАНИЕ ПЕРЕЕЗДА ЗАПИСЫВАЕТСЯ У ПРАВИЛА: утверждение уровня модуля роняет
СБОР файла безымянно и исчезает под ключом оптимизации интерпретатора, то есть в обоих исходах
перестаёт быть проверкой. Абзац «имя ВЫВЕДЕНО из пути, а не выписано второй копией» остаётся
дословно — его основание верно.

**(6) ПЕРЕСНЯТЬ ЗАМЕР ПОДКЛЮЧЕНИЙ.** Числа абзаца (подключений первой формы, сырых вхождений, вхождений
в прозе) пересчитываются прогоном по дереву и приводятся; прежние значения помечаются устаревшими, а
не ошибочными, если разошлись. Идиома D-30/D-32.

⚠️ **ЖИВОЕ ДЕРЕВО НЕ ПРАВИТСЯ НИ БАЙТОМ.** Оба контроля доктóрят В ПАМЯТИ. Основание записано в
докстринге действующего контроля и переносится ссылкой, а не второй копией.
  </action>

  <acceptance_criteria>
- Контроль второй формы подключения краснит правило ровно одной находкой, называющей наследника,
  рычаг и путь; до правки правило на той же копии зелено — оба состояния названы в сводке.
- Контроль нового наследника краснит правило ровно одной находкой, называющей новый экран.
- `test_the_lever_path_lives_under_the_templates_root` существует; утверждения уровня модуля в файле
  не осталось — проверяется отсутствием безымянного падения сбора при прогоне и чтением диффа.
- На нетронутом дереве наследников семь, граф корня — пять звеньев, правило достижимости зелено; все
  три числа названы в сводке.
- Число вхождений второй формы подключения в `app/templates/` снято прогоном и названо в сводке.
- `git diff --numstat -- app` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "auth_shell or reaches_the_modal_lever or imports_the_lever or new_auth_heir or lever_path_lives"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо отобрано меньше пяти правил (несущее, три контроля и правило пути рычага)</fails_when>
    <automated>uv run pytest tests/test_pages/test_shell.py -q</automated>
    <fails_when>ненулевой код возврата, либо в выводе есть строка "error" рядом со словом "collect" — сбор файла упал, что и есть предмет находки о переезде утверждения уровня модуля</fails_when>
  </verify>

  <done>
Граф достижимости знает обе формы подключения макроса; сборщик наследников принимает читателя и
состав имён параметрами, и сценарий «новый экран входа» показан исполнением; утверждение уровня
модуля переехало в именованное правило с текстом починки; замер подключений переснят; живое дерево не
тронуто.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: два разборщика называются тем, что они есть, и перестают держаться на одном символе</name>

  <files>
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `tests/test_pages/test_shell.py:2869-2935` — `_css_rules` целиком, включая абзац о третьем элементе
  тройки и обход с раскрытием медиазапросов. ⚠️ ЧИТАТЬ ДОСЛОВНО: `rule_start` присваивается равным
  началу преамбулы, и преамбула начинается СРАЗУ ЗА закрывающей скобкой предыдущего блока — отсюда и
  вбирание прозы.
- `tests/test_pages/test_shell.py:3240-3280` — контроль блока подъёма, считающий вхождения объявления
  положения В ДОСЛОВНОМ КУСКЕ. ⚠️ ИМЕННО ОН ДЕРЖИТСЯ ОДНИМ СИМВОЛОМ.
- `tests/test_pages/test_shell.py:4157-4180` — `_stylesheet_with_extra_declaration`: второй
  потребитель дословного куска.
- `tests/test_pages/test_shell.py:3330-3378` — `_lever_show_body` ПОСЛЕ правок плана 10-41: счёт
  вложенности и вырезание комментариев. ⚠️ ЗДЕСЬ ЖИВЁТ ВТОРАЯ ПОЛОВИНА `IN-02`.
- `app/static/css/app.css:1070-1090` — блок подъёма и стоящая перед ним проза. Читать глазом на факт:
  словосочетание положения в прозе действительно есть.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, находки `WR-05` и `IN-02` —
  протоколы с числами.
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md`, раздел «План 10-39»,
  подраздел о разборщике таблицы стилей — записанный владелец находки о качестве разборщика.
  ⚠️ НАСТОЯЩАЯ ЗАДАЧА ЕГО ЗАКРЫВАЕТ ЧАСТИЧНО, и это надо назвать в сводке: запись правится планом
  10-46.
  </read_first>

  <behavior>
1. `_css_rule_block(raw)` на дословном куске блока подъёма возвращает текст, начинающийся с
   ПРЕАМБУЛЫ БЛОКА, а не с открывающего комментария; его длина заметно меньше длины куска, и оба
   числа называются.
2. Счёт вхождений словосочетания положения в блоке (а не в куске) равен единице И БЕЗ точки с
   запятой: контроль перестаёт держаться одним символом.
3. Докстринг `_css_rules` называет третий элемент тройки тем, что он есть, — дословным куском ОТ
   КОНЦА ПРЕДЫДУЩЕГО БЛОКА, — и объясняет, зачем такая граница нужна подстановке.
4. `_lever_show_body` на синтетическом теле, где фигурная скобка стои́т внутри строкового литерала
   JS, возвращает тело целиком и отказа «тело не замкнуто» не поднимает. Сегодня поднимает.
5. `_lever_show_body` на теле, где фигурная скобка действительно не закрыта, отказ по-прежнему
   поднимает: правка сужает счёт, а не отменяет его.
  </behavior>

  <action>
**(1) RED. НАПИСАТЬ ДВА УТВЕРЖДЕНИЯ ПЕРВЫМИ.** Утверждение о счёте вхождений В БЛОКЕ без точки с
запятой (сегодня оно даёт двойку на куске) и контроль на синтетике с фигурной скобкой внутри
строкового литерала (сегодня даёт отказ «тело не замкнуто»). Записать команду, код возврата, имена
правил, ожидаемые и полученные ЧИСЛА в сводку как свидетельство RED.

**(2) GREEN — `_css_rule_block(raw)`.** Возвращает часть дословного куска, начинающуюся с преамбулы
блока: проза перед блоком отсекается по последней закрывающей фигурной скобке либо по последнему
закрывающему комментарию, что стои́т ближе к преамбуле. ⚠️ ГРАНИЦА НАЗЫВАЕТСЯ В ДОКСТРИНГЕ: функция
работает по КУСКУ, а не по исходнику, и отсекает то, что в кусок попало ПО ПОСТРОЕНИЮ разборщика,
а не по разметке таблицы.

**(3) GREEN — КОНТРОЛИ СЧИТАЮТ ПО БЛОКУ, А ПОДСТАВЛЯЮТ ПО КУСКУ.** Разделение прямое и записывается
прямо: единственность ЯКОРЯ подстановки утверждается по КУСКУ (потому что подстановка идёт по нему и
обязана быть однозначной), а единственность ОБЪЯВЛЕНИЯ — по БЛОКУ (потому что предмет утверждения
есть объявление, а не проза о нём). Точка с запятой перестаёт быть частью сличаемого — сличается имя
свойства и его значение в разобранных объявлениях блока.

**(4) GREEN — ДОКСТРИНГ `_css_rules` ПРИВОДИТСЯ.** Прежняя формулировка о третьем элементе приводится
дословно и помечается опровергнутой с указанием чем (`REVIEW-8/WR-05`), когда (2026-09-10) и чем
закрыто. Приводятся ЗАМЕРЕННЫЕ числа: длина куска у блока подъёма и длина блока после отсечения.
⚠️ ЧИСЛА СНИМАЮТСЯ ПРОГОНОМ, А НЕ ПЕРЕПИСЫВАЮТСЯ ИЗ ПЛАНА: план писан до правок соседних задач.
Заодно там же называются две уже записанные границы разборщика (уплощение медиазапросов и
раскрытие всего, чья преамбула начинается со знака правила) — они остаются, и их владелец
по-прежнему записан в `deferred-items.md`.

**(5) GREEN — СЧЁТ ВЛОЖЕННОСТИ ЗНАЕТ СТРОКОВЫЕ ЛИТЕРАЛЫ.** В `_lever_show_body` счёт глубины ведёт
состояние строкового литерала: одинарная, двойная и обратная кавычка открывают литерал, парная того
же рода закрывает, обратная косая черта экранирует следующий знак, а фигурные скобки внутри литерала
глубины не двигают. Оба текста отказа остаются прежними.
⚠️ ГРАНИЦА НАЗЫВАЕТСЯ: подстановка внутри шаблонного литерала JS счётом не разбирается; таких
литералов в теле метода сегодня нет, и число вхождений обратной кавычки приводится замером.

**(6) ПОДТВЕРДИТЬ, ЧТО ОБЕ ПРАВКИ НИЧЕГО НЕ ОТНЯЛИ.** Прогон всего модуля зелен; контроль блока
подъёма и контроль вырезанного снятия по-прежнему краснеют на своих доктóриваниях — это проверяется
исполнением, а не рассуждением.
  </action>

  <acceptance_criteria>
- `_css_rule_block` на куске блока подъёма возвращает текст, начинающийся с преамбулы блока; длина
  куска и длина блока названы в сводке обоими числами.
- Счёт объявления положения ПО БЛОКУ равен единице и не зависит от точки с запятой; замер до правки
  (2 на куске без точки с запятой, 1 с ней) назван в сводке.
- Прежняя формулировка о третьем элементе тройки приведена дословно и помечена опровергнутой.
- `_lever_show_body` на синтетике с фигурной скобкой внутри строкового литерала возвращает тело и
  отказа не поднимает; на теле с действительно незакрытой скобкой отказ поднимает. Обе стороны
  утверждены.
- Число вхождений обратной кавычки в теле метода снято прогоном и названо в сводке.
- Контроль блока подъёма и контроль вырезанного снятия по-прежнему краснеют на своих доктóриваниях.
- `git diff --numstat -- app` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "elevation or css_rule_block or brace_inside_a_js_string or lever_clears or keeps_a_stale_banner"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо отобрано меньше правил, чем до правки плюс один новый контроль</fails_when>
    <automated>uv run pytest tests/test_pages/test_shell.py tests/test_templates/test_components.py -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed" — правка разборщиков задела правила, читающие их выход в обоих модулях</fails_when>
  </verify>

  <done>
Третий элемент тройки разборщика таблицы назван тем, что он есть, и контроли считают объявления по
блоку, а подставляют по куску; сличение перестало держаться точкой с запятой; счёт вложенности тела
метода знает строковые литералы и это показано на синтетике; обе границы названы замерами; продукт не
тронут.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| дерево разметки → сборщик графа достижимости | правило читает файлы `app/templates/`; невидимое ребро делает «не достижим» утверждением ни о чём |
| исходник шаблонов → гейт единственности источника | гейт решает, кто ВЛАДЕЕТ заготовками; предикат, зависящий от рода кавычек, пропускает второй источник |
| текст таблицы стилей → разборщик правил | дословный кусок вбирает прозу перед блоком, и утверждения по нему считают не то |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-45-01 | Spoofing | гейт единственности источника заготовок | high | mitigate | Второй источник, набранный другим родом кавычек, проходит обе половины: решение D-01 нарушается молча, и два обработчика на одну заготовку дают удвоенную реакцию. Закрывается двумя квотонезависимыми предикатами и тремя случаями контроля |
| T-10-45-02 | Spoofing | правило достижимости рычага | high | mitigate | Панель подтверждения приезжает в шелл авторизации формой подключения, которой правило не видит, и подъём заготовок молча перестаёт работать при обоих зелёных правилах. Закрывается третьим образцом ребра и контролем на доктóренной копии |
| T-10-45-03 | Denial of service | утверждение уровня модуля | medium | mitigate | Срабатывание роняет сбор всего файла (161 правило — замер 2026-09-10; прежние 168 суть счёт ДВУХ модулей вместе, разбор в `<objective>`) безымянным отказом, а под ключом оптимизации исчезает целиком — в обоих исходах перестаёт быть проверкой. Закрывается переездом в именованное правило с текстом починки |
| T-10-45-04 | Repudiation | докстринг сборщика наследников | medium | mitigate | Сценарий, которым докстринг оправдывает чтение дерева, ни разу не исполнен: запись шире того, что показано. Закрывается параметрами читателя и состава имён плюс контролем нового наследника |
| T-10-45-05 | Tampering | подсчёт объявлений по дословному куску | medium | mitigate | Отрицательный контроль держится одним символом точки с запятой; проза перед блоком удваивает счёт. Закрывается отсечением прозы и разделением «якорь по куску, объявление по блоку» |
| T-10-45-06 | Tampering | счёт вложенности тела метода | low | mitigate | Фигурная скобка внутри строкового литерала двигает глубину, и разбор вернул бы кусок с неизвестными границами. Латентна сегодня. Закрывается состоянием строкового литерала и контролем на синтетике |
| T-10-45-SC | Tampering | npm/pip/cargo installs | n/a | accept | ЗАМЕР: план не ставит ни одного пакета — правится один модуль суиты. Гейт легитимности пакетов неприменим |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_shell.py -q` — зелено, сбор файла не падает.
2. `uv run pytest tests/test_templates/test_components.py -q` — зелено.
3. `uv run pytest tests/test_planning/ -q` — зелено.
4. `uv run python -m compileall -q app main.py tests` — код возврата ноль.
5. `git diff --numstat -- app` — пусто.
6. Числа замеров (вхождения второй формы подключения, наследники, звенья графа, длина куска и блока,
   вхождения обратной кавычки) названы в сводке.
</verification>

<success_criteria>
- Все шесть находок восьмого круга, живущих в модуле шелла, закрыты, и у каждой есть контроль,
  краснеющий на форме, которую правило сегодня принимает.
- Ни одна правка не расширила живого графа и не тронула дерева разметки.
- Утверждение уровня модуля перестало ронять сбор файла безымянно.
- Записи разборщиков не шире того, что они делают.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-45-SUMMARY.md` when done
</output>
