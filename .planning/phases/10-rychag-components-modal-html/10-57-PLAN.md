---
phase: 10-rychag-components-modal-html
plan: 57
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, И ЭТО ОТЛИЧИЕ ОТ ПЛАНОВ 10-55 И 10-56 ТОЙ ЖЕ ПАРТИИ.
# Основание — предмет: ПЕРВАЯ задача плана есть ОСТАНОВ ВЛАДЕЛЬЦА, и форма правки до его ответа
# НЕИЗВЕСТНА. Правило, написанное до ответа, было бы правилом одной из трёх ветвей, то есть
# исполнитель выбрал бы ветвь за владельца САМИМ ФАКТОМ написания правила. Внутри выбранной ветви
# задача 2 идёт тест-первой (RED с печатью полученного → правка → GREEN → отрицательный контроль), и
# это записано её собственным блоком `<behavior>`; `workflow.tdd_mode` равен `true` (замерено
# 2026-09-12), и эвристика соблюдена, а не обойдена.
wave: 3
depends_on: ["10-55", "10-56"]
# ⚠️ ВОЛНА 3, А НЕ 2, И ЭТО СЛЕДСТВИЕ, А НЕ ПЕРЕОЦЕНКА ПРЕДМЕТА. Ревизия плана 2026-09-12 объявила
# ребро 10-56 → 10-55 (разделяемый изменяемый `deferred-items.md`: пишет 10-55, читает гейтом
# `tests/test_planning/` 10-56), и 10-56 перешёл из волны 1 в волну 2. Номер волны настоящего плана
# СЧИТАЕТСЯ, а не назначается: `max(волна 10-55 = 1, волна 10-56 = 2) + 1 = 3`. Прежнее `wave: 2`
# ОШИБКОЙ НЕ БЫЛО и здесь не вычёркивается — оно было верно для дерева, где 10-56 стоял в волне 1.
gap_closure: true
gap_ids: [UI-1]
# ⚠️ ПРОИСХОЖДЕНИЕ ИДЕНТИФИКАТОРА НАЗЫВАЕТСЯ ЗДЕСЬ, ПОТОМУ ЧТО У ЭТОЙ ФАЗЫ УЖЕ БЫЛА
# ДВУСМЫСЛЕННОСТЬ ИМЕНИ ГЭПА (блок комментария `gap_ids` плана 10-52). `UI-1` — это ПЕРВЫЙ БЛОКЕР
# перечня «Top 3 Priority Fixes» отчёта UI-ревизии `10-UI-REVIEW.md` (`audited: 2026-09-12`,
# `score: 15/24`, Pillar 6 Experience Design 2/4). Источник назван прямо:
#   • в разделе `gaps` отчёта `10-VERIFICATION.md` этой записи НЕТ — UI-ревизия снята в 15:45, то
#     есть ПОСЛЕ круга верификации 15:00, и верификатор её не видел;
#   • поток `--gaps` доносит до планировщика только `10-VERIFICATION.md` и `10-UAT.md`, поэтому
#     находка передана партии РУКАМИ, и это записано, а не умолчано;
#   • с `UI-2` (план 10-56) она НЕ СЛИВАЕТСЯ: там предмет — ДВА прямоугольника вместо одного, здесь
#     — ВЫХОД у показанного прямоугольника. Разные дефекты, разные правки, разная цена.
#
# ⚠️ ЗАВИСИМОСТЬ ОТ 10-56 — ПО ФАЙЛАМ И ПО СМЫСЛУ, А НЕ ПО ПОРЯДКУ НОМЕРОВ. Настоящий план правит
# те же два продуктовых файла (`htmx_error_banner.html`, `app.css`) и тот же модуль правил
# (`tests/test_pages/test_shell.py`), поэтому в ОДНОЙ волне с 10-56 стоять не имеет права. Кроме
# того, орган управления заводится ВНУТРИ узла заготовки, у которого класс стопки появляется именно
# планом 10-56. Зависимость от 10-55 — по файлу `deferred-items.md`: разделы двух планов вносятся
# последовательно, а не в один прогон.
files_modified:
  - app/templates/includes/htmx_error_banner.html
  - app/static/css/app.css
  - tests/test_pages/test_shell.py
  - .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
  - .planning/phases/10-rychag-components-modal-html/deferred-items.md
  - .planning/WINDOWS.md
autonomous: false
requirements: [FORM-06]

# ⚠️ `autonomous: false` — В ПЛАНЕ СТОИ́Т `checkpoint:decision` С `gate="blocking-human"`.
# Автопродвижение (`workflow.auto_advance`) на этот гейт НЕ ДЕЙСТВУЕТ НИ В ОДНОМ РЕЖИМЕ: предмет
# останова — ЦЕНА ПО КРИТЕРИЮ 3, а действующая запись отступления владельца объявляет СВОЮ ГРАНИЦУ
# дословно и на настоящую правку не достаёт.

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (`config-get`, 2026-09-12): `security_asvs_level` → `1`,
# `security_block_on` → `"high"`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ. Двенадцать
# открытых угроз `10-SECURITY.md` живут под областью отказа `phase-10-only` и НЕ ПЕРЕСМАТРИВАЮТСЯ.

# ⚠️ `workflow.human_verify_mode` = `end-of-phase`, поэтому `checkpoint:human-verify` в плане НЕТ НИ
# ОДНОГО. `checkpoint:decision` — ДРУГОЙ тип: его предмет не проверка стенда, а РЕШЕНИЕ, которое
# машина не вправе принять за владельца. Человеческие ЗАМЕРЫ выражены блоком `<verify><human-check>`.

# ⚠️ ВОСЕМНАДЦАТЬ ЗАПЕРТЫХ РЕШЕНИЙ `10-CONTEXT.md` (D-01…D-18) НАСТОЯЩИМ ПЛАНОМ НЕ РЕАЛИЗУЮТСЯ
# ЗАНОВО: все восемнадцать доставлены планами 10-01…10-54. Из них план ЗАДЕВАЕТ ДВА, и оба названы
# поимённо, потому что оба суть предмет останова задачи 1 либо его границы:
#   • **D-13** — прочтение критерия 3 ПО ЧИСЛАМ. Именно оно делает ЛЮБУЮ форму выхода у плашки
#     предметом РЕШЕНИЯ ВЛАДЕЛЬЦА, а не выбора исполнителя: пять объявленных чисел и есть тот
#     критерий, который правка способна сдвинуть. Ни одно из них не двигается без ответа владельца.
#   • **D-12** — панель закрывается ТОЛЬКО на успешном обмене. Третий обработчик заготовок ключáется
#     на тот же признак успеха, и настоящий план его НЕ ТРОГАЕТ НИ НА СИМВОЛ: орган снятия убирает
#     ПРОЧИТАННОЕ СООБЩЕНИЕ, а не меняет правило закрытия панели, и смешать эти два предмета
#     запрещено — снятая плашка обязана вернуться на новом отказе.
# Решения D-30/D-32 — ИДИОМА ЗАПИСИ («прежний текст дословно, рядом — что его переросло»), и ею
# набрана третья запись блока `overrides` задачи 3.

# ⚠️ `$PLAN_BASE` — ИМЯ ДЕРЕВА ДО ПЕРВОГО КОММИТА ПЛАНА, И ОНО СНИМАЕТСЯ ПЕРВЫМ ЖЕ ДЕЙСТВИЕМ
# ИСПОЛНИТЕЛЯ: `PLAN_BASE=$(git rev-parse HEAD)` ДО правки чего-либо (то есть ДО останова задачи 1);
# снятое значение приводится в сводке. Все проверки вида «этот файл планом не тронут» набраны в форме
# `git diff "$PLAN_BASE"..HEAD -- <путь>`, И ЭТО НЕСУЩЕЕ СВОЙСТВО, А НЕ СЛОГ: голый `git diff` без
# диапазона на УЖЕ ЗАКОММИЧЕННОЙ правке печатает пустоту ВСЕГДА, то есть проверка проходила бы
# ВАКУУМНО — ровно тот класс ложной зелени, который эта фаза преследует у себя. Диапазон пришпилен,
# и потому пустой вывод есть ЗАМЕР, а не тишина.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ. Замер 2026-09-12: `estimate-calibration` → `sample_count: 0`,
  # `factor: 1`, `applied: false`, `min_samples: 3`, `confidence: "low"`. `confidence` ВЫВЕДЕНА ИЗ
  # ЧИСЛА ОБРАЗЦОВ (0 < 3). Размер держится измеримым: задач 3, файлов 6, из них продуктовых 2;
  # ветвление задачи 2 по ответу владельца в проекцию ВХОДИТ по самой дорогой из трёх ветвей.
  #
  # ⚠️ ПЕРЕБОР БЮДЖЕТА СМАРТ-ЗОНЫ НАЗВАН, А НЕ УМОЛЧАН, И ПРИНЯТ ОСОЗНАННО. Проекция 115000 против
  # бюджета 100000 — 1.15x. Проверщик плана 2026-09-12 отметил это предупреждением и порекомендовал
  # вынести задачу 3 (записи) отдельным планом; рекомендация СОВЕЩАТЕЛЬНА (ADR-2629, решение 5), и
  # планировщик выбрал ПРИНЯТИЕ. Проверщик при этом назвал КОНКРЕТНЫЙ довод, и он здесь отвечается
  # ЗАМЕРОМ, а не отводится: «контекст плана переживает останов владельца неопределённой длины, а
  # задача 3 несёт запись `reversibility: one-way` — деградация упадёт ровно на задачу, которой
  # нельзя ошибаться».
  #   (а) ПОСЫЛКА О ПЕРЕЖИВАНИИ ОСТАНОВА НЕВЕРНА ПО ЗАМЕРУ УСТРОЙСТВА, И ЭТО ЧИТАЕТСЯ ДОСЛОВНО:
  #       `gsd-core/references/checkpoints.md` объявляет, что «every mid-flight halt costs a full
  #       executor cold-start (CLAUDE.md, MEMORY.md, STATE.md, plan re-read on respawn) because
  #       SUBAGENT CONTEXT IS DISCARDED ACROSS THE PAUSE». Останов задачи 1 есть, стало быть, ГРАНИЦА
  #       КОНТЕКСТА, а не его продолжение: самая дорогая часть проекции (предъявление владельцу —
  #       четырнадцать мест чтения, пять замеров, три ветви с ценой каждой) в ОДНОМ окне с задачей 3
  #       НЕ ЖИВЁТ. Проекция 115000 есть ПОЛНЫЙ расход плана; ПИКОВОЕ ОДНО ОКНО — расход задач 2 и 3
  #       после холодного старта, и он бюджета не перебирает. Поле `tokens` не занижается ни на
  #       единицу: оно объявляет полный расход, каким он и есть.
  #   (б) РАСПИЛ УХУДШИЛ БЫ РОВНО ТО, ЧТО ПРОВЕРЩИК ЗАЩИЩАЕТ. Запись `overrides` обязана нести ОТВЕТ
  #       ВЛАДЕЛЬЦА ДОСЛОВНО и ПЯТЬ ЗАМЕРОВ, ПРЕДЪЯВЛЕННЫХ ЕМУ ДО правки (действие 1 задачи 3:
  #       «пять замеров задачи 1 приводятся здесь же выводом, а не ссылкой»). Эти замеры СНЯТЫ НА
  #       ДЕРЕВЕ ДО ЗАДАЧИ 2 и после неё НЕВОСПРОИЗВОДИМЫ (для ветви `B` число регистраций уже 4, а
  #       не 3). Отдельный план писал бы односторонне-необратимую запись ВТОРЫМ ПЕРЕСКАЗОМ — из
  #       чужой сводки, а не из живого ответа. Для записи, которую следующие круги читают как
  #       ПРАВИЛО ПРОЕКТА, первоисточник дороже свежести контекста.
  #   (в) ЦИФРА НЕ ОТКАЛИБРОВАНА: `sample_count: 0` при `min_samples: 3`. 1.15x — перебор проекции,
  #       не замера.
  # ⚠️ И ПОТОМУ ПРИНЯТИЕ НЕ ГОЛОЕ: риск, названный проверщиком, снимается ДЕШЁВЫМ ЗУБОМ, а не
  # отрицанием. Акцептанс задачи 1 расширен: ответ владельца И все пять замеров переносятся в СВОДКУ
  # ДОСЛОВНО, слово в слово, ДО правки чего-либо. Тем самым первоисточник записи живёт в артефакте
  # плана, а не только в окне исполнителя, — и задача 3 остаётся первоисточниковой даже если её
  # придётся возобновлять в холодном контексте. Факт перебора и выбранный маршрут называются в
  # сводке ЧИСЛОМ.
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "У ПОКАЗАННОЙ ПЛАШКИ ОТКАЗА ЕСТЬ ВЫХОД, И ЧЕЛОВЕК ВПРАВЕ ИМ ВОСПОЛЬЗОВАТЬСЯ: на экране, который больше не делает ни одного успешного обмена, прямоугольник во всю ширину перестаёт быть пришпиленным к верху до перезагрузки страницы"
    - "ЦЕНА ПО КРИТЕРИЮ 3 НАЗВАНА ВЛАДЕЛЬЦУ ЗАМЕРАМИ И ПРИНЯТА ИМ, А НЕ ИСПОЛНИТЕЛЕМ: форма правки выбрана ОТВЕТОМ на останов, и выбранная ветвь названа в сводке идентификатором"
    - "ОРГАН УПРАВЛЕНИЯ ДОСТИЖИМ С КЛАВИАТУРЫ И НЕСЁТ ДОСТУПНОЕ ИМЯ: это настоящий элемент управления, попадающий в порядок обхода, а не украшенный узел без роли"
    - "СНЯТАЯ ЧЕЛОВЕКОМ ПЛАШКА ВОЗВРАЩАЕТСЯ НА НОВОМ ОТКАЗЕ: следующая авария того же рода снова показывает плашку, и человек не остаётся в тишине — исход «снял один раз и больше не увижу никогда» есть регрессия ХУЖЕ исходного дефекта, и он запрещён прямо"
    - "ЧИСЛА ГЕЙТА КРИТЕРИЯ 3 НЕ ДВИНУТЫ МОЛЧА НИ ОДНО: если какое-то из пяти сдвинулось, рядом с ним стои́т ЗАПИСЬ ОСНОВАНИЯ — ответ владельца, а не замер исполнителя; правка одного лишь числа под найденное есть ровно то молчание, против которого пятое утверждение заведено"
    - "ТРЕТИЙ ОБРАБОТЧИК НЕ ТРОНУТ НИ НА СИМВОЛ: он закрывает настоящий дефект `G-10-5`, стережётся двумя отрицательными контролями плана 10-49, и его цена уже принята именованной записью отступления от 2026-09-12"
    - "ВСЕ ШЕСТНАДЦАТЬ ВНЕОБЛАСТНЫХ НАХОДОК РАСПОРЯЖЕНЫ ПОИМЁННО: одиннадцать ревизии — разделом плана 10-55, пять UI-ревизии (`UI-3`…`UI-7`) — разделом настоящего плана, у каждой назван владелец и одна строка причины; находки, отсутствующей и в задаче, и в записи, нет ни одной"
    - "ОБЕСЦЕНИВАНИЕ ЧЕЛОВЕЧЕСКИХ ЗАМЕРОВ ЗАПИСАНО, А НЕ ЗАКРЫТО: шаги 2.8 и 4.4 обхода после правок этой партии требуют СВЕЖЕГО прохода, и запись об этом долге заведена КОМАНДОЙ ведения журнала, а не отметкой о прохождении"
    - "СНЯТИЕ ОДНОЙ ПЛАШКИ НЕ СНИМАЕТ ВТОРУЮ: орган управления адресует СВОЙ узел заготовки, поэтому при двух одновременных авариях человек снимает прочитанное и оставляет непрочитанное — ребро `concurrency` требования FORM-06 разрешено ЯВНЫМ критерием приёмки, снимаемым сличением области селектора и исполнением сценария на стабах"
    - "ПОВТОРНОЕ НАЖАТИЕ ОРГАНА УПРАВЛЕНИЯ НЕ МЕНЯЕТ ИСХОДА, А ПОВТОРНЫЙ ОТКАЗ ПОСЛЕ СНЯТИЯ ПОКАЗЫВАЕТ ПЛАШКУ СНОВА: два состояния органа замкнуты сами на себя, и число узлов заготовок в документе не растёт ни от одного из двух действий — ребро `idempotency` требования FORM-06 разрешено ЯВНЫМ критерием приёмки"
  artifacts:
    - "`app/templates/includes/htmx_error_banner.html` — орган снятия внутри КАЖДОГО узла заготовки, с доступным именем; идентификаторы, тексты плашек и строка с идентификатором заготовки обрыва связи не сдвинуты"
    - "`app/static/css/app.css` — блоки внешнего вида органа снятия и правило скрытия снятой заготовки; блок подъёма и его селектор не тронуты"
    - "`tests/test_pages/test_shell.py` — правило существования и достижимости органа снятия, правило возврата плашки на новом отказе, правило области снятия (одна плашка, не обе), правило значений способа отображения у узлов заготовок и отрицательные контроли при них"
    - "`.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — ТРЕТЬЯ запись блока `overrides` с ответом владельца, стоящая РЯДОМ с двумя действующими; поля вердикта круга не тронуты"
    - "`.planning/phases/10-rychag-components-modal-html/deferred-items.md` — раздел `## План 10-57` с распоряжением по `UI-3`…`UI-7` и ссылкой на раздел плана 10-55"
    - "`.planning/WINDOWS.md` — запись о долге свежего прохода шагов 2.8 и 4.4 и о принятой цене по критерию 3, заведённая КОМАНДОЙ ведения журнала"
  key_links:
    - "ответ владельца → форма правки: единственное, чем цена по критерию 3 остаётся решением того, кто вправе её принять; ломается — исполнитель выбирает ветвь сам, и следующий круг читает как принятое то, что никто не принимал (класс `T-10-53-01`)"
    - "сброс состояния органа снятия в теле действующего обработчика отказа → возврат плашки: единственное, чем снятие остаётся снятием ОДНОГО сообщения, а не отключением канала до конца сессии; ломается — человек снял плашку однажды и больше не узнает ни об одной аварии, то есть тишина вместо пришпиленного прямоугольника"
    - "область селектора скрытия (узел заготовки, а не оба узла) → независимость двух плашек; ломается — снятие одной уносит вторую, и человек теряет инструкцию, которую как раз собирался прочитать"
    - "значение способа отображения у узлов заготовок → скрытость через таблицу стилей пользовательского агента: единственное, чем правило скрытия снятой заготовки не превращается в две пустые подложки на каждом экране; ломается — объявление автора перебивает атрибут скрытия, и цена, названная пунктом (д) прозы таблицы, платится немедленно"
  prohibitions:
    - statement: "ЧЕТВЁРТАЯ РЕГИСТРАЦИЯ ОБРАБОТЧИКА В `app/templates/includes/htmx_error_banner.html` НЕ ЗАВОДИТСЯ БЕЗ ОТВЕТА ВЛАДЕЛЬЦА НА ОСТАНОВЕ ЗАДАЧИ 1, И ЧИСЛО `FAILURE_BANNER_HANDLERS_MEASURED` НЕ ПРАВИТСЯ ИСПОЛНИТЕЛЕМ НИ ПРИ КАКОМ ЗАМЕРЕ: действующая запись отступления (`extend-override`, `chubav`, 2026-09-12) объявляет свою границу ДОСЛОВНО — «⚠️ НА БУДУЩИЕ РЕГИСТРАЦИИ В ТОМ ЖЕ ФАЙЛЕ ЗАПИСЬ НЕ РАСПРОСТРАНЯЕТСЯ: всякая следующая регистрация требует НОВОГО решения владельца». Правка одного лишь объявленного числа под найденное есть ровно то молчание, против которого пятое утверждение гейта заведено"
      status: flagged-unverified
    - statement: "АТРИБУТА СОБЫТИЯ РАНТАЙМА РАЗМЕТКИ (`hx-on`, `hx-on::…`) НЕ ЗАВОДИТСЯ НИ ОДНОГО: третье утверждение гейта критерия 3 объявляет вхождений НОЛЬ, а выражение `INLINE_HANDLER_ATTR` есть выражение ПО `hx-on` — то есть такая форма правки покраснит гейт немедленно, и никакое решение владельца этого не отменяет, потому что предмет запрета — второй слой экранирования поверх слоя разметки, которого автоматическое экранирование шаблонизатора не касается"
      status: flagged-unverified
    - statement: "НОВОГО УЗЛА КЛИЕНТСКОГО СОСТОЯНИЯ НЕ ЗАВОДИТСЯ: четвёртое утверждение гейта критерия 3 объявляет их число (`CLIENT_STATE_NODES`), и новый узел `x-data` есть новая СУЩНОСТЬ в смысле D-13 — то есть ТА ЖЕ цена, что у новой регистрации, и тот же предмет решения владельца. Заводить его молча запрещено ровно как молча править число"
      status: flagged-unverified
    - statement: "ТРЕТИЙ ОБРАБОТЧИК (`:227-233`) НЕ ОТКАТЫВАЕТСЯ, НЕ ПЕРЕНОСИТСЯ И НЕ ПРАВИТСЯ НИ НА СИМВОЛ: он закрывает настоящий дефект `G-10-5`, стережётся двумя отрицательными контролями плана 10-49, и предмет настоящего плана — ВЫХОД у плашки, а не ревизия чужой правки"
      status: flagged-unverified
    - statement: "СНЯТИЕ ПЛАШКИ НЕ СТАНОВИТСЯ ОТКЛЮЧЕНИЕМ КАНАЛА ДО КОНЦА СЕССИИ: исход «человек снял плашку один раз и больше не узнает ни об одной аварии» есть регрессия ХУЖЕ исходного дефекта — тишина вместо пришпиленного прямоугольника, — и ветвь, дающая такой исход, принимается только ПРЯМЫМ решением владельца с названной ценой"
      status: flagged-unverified
    - statement: "ПОЛЯ ВЕРДИКТА ОТЧЁТА `10-VERIFICATION.md` НЕ ПРАВЯТСЯ: `status`, `score`, `overrides_applied`, `behavior_unverified`, `covered_digest` и разделы `gaps`/`advisory`/`human_verification` принадлежат КРУГУ, а не плану. Планом вносится РОВНО ОДНА запись в блок `overrides`, встающая РЯДОМ с двумя действующими; засчитать её вправе СЛЕДУЮЩИЙ круг, и это прямое повторение решения, записанного комментарием того же блока"
      status: flagged-unverified
    - statement: "ДВЕ ДЕЙСТВУЮЩИЕ ЗАПИСИ `overrides` НЕ ТРОГАЮТСЯ НИ СИМВОЛОМ И НЕ ОБЪЯВЛЯЮТСЯ ОТМЕНЁННЫМИ: каждая верна для своего дня и своей области, и третья встаёт РЯДОМ, а не на их место (идиома D-30/D-32, ровно как вторая встала рядом с первой)"
      status: flagged-unverified
    - statement: "ТЕКСТЫ ОБЕИХ ПЛАШЕК НЕ ПРАВЯТСЯ, И СТРОКА, НЕСУЩАЯ ИДЕНТИФИКАТОР ЗАГОТОВКИ ОБРЫВА СВЯЗИ, ОСТАЁТСЯ ОДНОЙ СТРОКОЙ ВМЕСТЕ С ВЫЗОВОМ МАКРОСА ПЛАШКИ: `_network_banner_line` читает ровно одну строку исходника и ищет в ней четыре основы слов о расхождении экрана с сервером. Находка `UI-3` о формулировках плашек отложена, и текст поэтому не трогается вовсе"
      status: flagged-unverified
    - statement: "БЛОК ПОДЪЁМА И ЕГО СЕЛЕКТОР НЕ ПРАВЯТСЯ, И СПОСОБ ОТОБРАЖЕНИЯ В НЁМ НЕ ОБЪЯВЛЯЕТСЯ НИ В КАКОМ ВИДЕ: пять действующих правил подъёма плюс правила плана 10-56 держат его форму, а запрет способа отображения защищает скрытость заготовок через таблицу стилей пользовательского агента"
      status: flagged-unverified
    - statement: "ПРАВИЛО СКРЫТИЯ СНЯТОЙ ЗАГОТОВКИ ОБЪЯВЛЯЕТ СПОСОБ ОТОБРАЖЕНИЯ ТОЛЬКО ЗНАЧЕНИЕМ «НЕТ»: любое иное значение у узла заготовки перебило бы атрибут скрытия и дало бы пустые подложки на каждом экране проекта. Запрет получает СОБСТВЕННОЕ правило со сличением значения, а не держится осторожностью читателя"
      status: flagged-unverified
    - statement: "НАХОДКИ `UI-3`…`UI-7` НЕ СТАНОВЯТСЯ ЗАДАЧАМИ НИ ОДНОЙ СТРОКОЙ: решение владельца от 2026-09-12 ограничило партию тремя находками. В частности НЕ ПРАВЯТСЯ: тела диалогов десяти потребителей (`UI-4`), ранний выход по коду ответа валидации (`UI-5`), связь описания диалога (`UI-6`), возврат фокуса на пути внеполосного снятия панели (`UI-7`) и оповещение вспомогательных технологий (`UI-3`). Каждая распоряжается ЗАПИСЬЮ, и молчаливого пропуска нет ни у одной"
      status: flagged-unverified
    - statement: "`10-UAT.md` НЕ ПРАВИТСЯ ВОВСЕ И НИ ОДНА ОТМЕТКА ПРОХОЖДЕНИЯ В НЁМ НЕ СТАВИТСЯ: настоящая партия ОБЕСЦЕНИВАЕТ шаги 2.8 и 4.4, и долг свежего прохода записывается журналом окон, а не отметкой. Принуждение — `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`"
      status: flagged-unverified
    - statement: "`FORM-06` НЕ ОТМЕЧАЕТСЯ ВЫПОЛНЕННЫМ: ни флажок `.planning/REQUIREMENTS.md:31`, ни клетка `:136` (`Gaps Found`) не двигаются — требование закрывается ПОДПИСЬЮ ЧЕЛОВЕКА на приёмке обхода, и ответ владельца на останове задачи 1 этой подписью НЕ ЯВЛЯЕТСЯ: смешать два решения в одном ответе значило бы получить один ответ на два вопроса"
      status: flagged-unverified
    - statement: "РЕЕСТР ЗАПРЕТОВ `10-PROHIBITIONS-SUBJECT.md` НЕ ПРАВИТСЯ НИ ОДНИМ СИМВОЛОМ: расхождение его шапки (200) с деревом (285 записей) есть СОВЕЩАТЕЛЬНАЯ пометка двенадцатого круга и отдельное решение владельца, а не предмет этой партии. Запреты настоящего плана несут `flagged-unverified` и принуждения НЕ ЗАЯВЛЯЮТ"
      status: flagged-unverified
---

<objective>
**У плашки отказа нет выхода: она гаснет ТОЛЬКО от следующего УСПЕШНОГО обмена — и на экране, где
такого обмена больше не будет, пришпиленный к верху прямоугольник во всю ширину остаётся до
перезагрузки страницы.**

Замер прочитан планировщиком в дереве. `app/templates/includes/htmx_error_banner.html:227-233` —
третий обработчик — начинается с раннего выхода `if (!event.detail || !event.detail.successful) {
return; }`. То есть атрибут скрытия возвращается на место только когда обмен УДАЛСЯ. Человек, у
которого оборвалась связь на экране, не делающем больше ни одного обмена письма (либо делающем
только отказывающие), получает фиксированный прямоугольник поверх верха КАЖДОГО экрана до
перезагрузки. Это первый блокер UI-ревизии 2026-09-12 и главное, чем держится её оценка Pillar 6
(2/4): «a stuck full-width overlay is a task-blocking outcome, not a cosmetic one». Проза самой
таблицы стилей называет эту цену прямо и признаёт её лишь ЧАСТИЧНО оплаченной третьим обработчиком.

Purpose: отдать человеку действие, которым он убирает прочитанное сообщение, — и сделать это так,
чтобы следующая настоящая авария снова была ему показана. Плата за отсутствие выхода берётся в
худший момент его дня; плата за выход, отключающий канал навсегда, берётся ТИШИНОЙ в следующий такой
момент, и второе хуже первого.

⚠️ **ПОЧЕМУ ПЕРВОЙ ЗАДАЧЕЙ СТОИ́Т ОСТАНОВ ВЛАДЕЛЬЦА, А НЕ ПРАВКА.** Критерий 3 роадмапа гласит, что
фаза не добавляет новых строк JS, и он зелен СЕГОДНЯ ТОЛЬКО ПОД ДВУМЯ ПОДПИСАННЫМИ ЗАПИСЯМИ
ОТСТУПЛЕНИЯ (`10-VERIFICATION.md`, блок `overrides`). Вторая из них (`extend-override`, `chubav`,
2026-09-12) покрывает РОВНО семь строк третьего обработчика и объявляет свою границу ДОСЛОВНО:
«⚠️ НА БУДУЩИЕ РЕГИСТРАЦИИ В ТОМ ЖЕ ФАЙЛЕ ЗАПИСЬ НЕ РАСПРОСТРАНЯЕТСЯ: всякая следующая регистрация
в `includes/htmx_error_banner.html` требует НОВОГО решения владельца». Первая (`accept-reading`,
`chubav`, 2026-09-09) принимает ПРАВКУ ТЕЛА уже существующего метода и выражения УЖЕ СУЩЕСТВУЮЩЕГО
атрибута — но её поле `covers` называет ДВА ДРУГИХ ФАЙЛА (`ads/form.html`, `components/modal.html`),
и до настоящей правки она не достаёт.

**Значит ЛЮБАЯ форма выхода у плашки стои́т чего-то по критерию 3, и цену вправе принять только
владелец.** Планировщик замерил три формы и их цены (замеры приведены в задаче 1 дословно), но
ВЫБОР между ними машине не принадлежит: «машина не вправе выбрать за владельца, ровно как
верификатор не вправе толковать отступление шире, чем оно себя объявляет» — это записано в
докстринге самого гейта.

Output: орган снятия внутри каждой заготовки, выбранный ОТВЕТОМ владельца; правило возврата плашки
на новом отказе; правило области снятия; правило значений способа отображения у узлов заготовок;
третья запись блока `overrides` с ответом; распоряжение по пяти находкам UI-ревизии; запись журнала
о долге свежего прохода шагов 2.8 и 4.4.

⚠️ **ЧЕГО ЭТОТ ПЛАН НЕ ЗАКРЫВАЕТ.** Он не закрывает `UI-3` (плашки не оповещают вспомогательные
технологии) — орган снятия этого НЕ ЧИНИТ, и находка распоряжается записью. Он не закрывает
`FORM-06`: требование закрывается ПОДПИСЬЮ ЧЕЛОВЕКА на приёмке обхода, и ответ владельца на останове
задачи 1 этой подписью не является. Он не закрывает ПРЯМОУГОЛЬНИКА: суита читает объявления и
исполняет сценарий на стабах узлов.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-UI-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-53-PLAN.md
@app/templates/includes/htmx_error_banner.html
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Задача 1: останов — цена ВЫХОДА у плашки по критерию 3, тремя замеренными ветвями, и выбор принадлежит владельцу</name>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:204-262` — блок `overrides`
  ЦЕЛИКОМ: ОБЕ действующие записи, комментарий круга над блоком и поле `covers` каждой записи. ⚠️
  ЧИТАТЬ ДОСЛОВНО ГРАНИЦУ ВТОРОЙ ЗАПИСИ и поле `covers` первой: именно ими определяется, что
  настоящая правка НЕ ПОКРЫТА ни одной из двух.
- `tests/test_pages/test_shell.py:2295-2312` — константа `FAILURE_BANNER_HANDLERS_MEASURED` и её
  летопись (переснята 2 → 3 планом 10-49). ⚠️ ЭТО ЕДИНСТВЕННЫЙ ИСТОЧНИК ЧИСЛА, и пятое утверждение
  гейта критерия 3 ввозит его.
- `tests/test_pages/test_shell.py:2044-2075` — `FAILURE_BANNER_EVENTS` (три имени) и `:2552-2660` —
  `test_the_failure_banner_carries_every_measured_handler`,
  `test_the_failure_banner_registers_its_handlers_once_per_body`,
  `test_the_failure_banner_guard_lives_on_the_node_it_wires`. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: число
  регистраций СЛИЧАЕТСЯ с числом имён в перечне событий, поэтому четвёртая регистрация тянет за
  собой и четвёртое имя.
- `tests/test_templates/test_components.py:4936-5028` — разборщик тел блоков `<script>`
  (`_script_bodies`, `_strip_js_comments`, `HANDLER_REGISTRATION_FORMS`,
  `_script_handler_registrations`) и ОБЪЯВЛЕНИЕ (`SCRIPT_HANDLER_REGISTRATIONS_DECLARED_HERE`,
  `_declared_script_handler_registrations`). ⚠️ ЧИТАТЬ ДОСЛОВНО ДВЕ ФОРМЫ, КОТОРЫЕ СЧИТАЮТСЯ
  РЕГИСТРАЦИЯМИ, и назвать в предъявлении, какие формы правки разборщик НЕ считает.
- `tests/test_templates/test_components.py:5164-5195` — ПЯТОЕ утверждение внутри
  `test_criterion_three_holds_by_the_numbers` и текст его отказа. ⚠️ ЧИТАТЬ ВМЕСТЕ С утверждениями 3
  и 4 (`:5140-5163`): третье объявляет вхождений `hx-on` НОЛЬ, четвёртое объявляет число узлов
  клиентского состояния — обе формы правки, кажущиеся «бесплатными», покраснят гейт.
- `tests/test_templates/test_htmx_markup_security.py:172` — выражение `INLINE_HANDLER_ATTR`. ⚠️
  ЧИТАТЬ ОБЯЗАТЕЛЬНО: оно есть выражение ПО `hx-on`, а не по `on*`-атрибутам HTML, и именно поэтому
  ветвь с `hx-on::before-request` отпадает ДО останова, а не обсуждается на нём.
- `tests/test_templates/test_htmx_markup_gates.py:1952` — `CLIENT_STATE_NODES` и абзац летописи над
  ним.
- `app/templates/includes/htmx_error_banner.html:213-235` — разметка обеих заготовок и блок сценария
  ЦЕЛИКОМ: обёртка признака однократности, три обработчика, ранний выход по коду ответа валидации.
- `app/static/css/app.css:1150-1200` — проза блока подъёма, включая абзац «пункт (д)» о запрете
  объявления способа отображения.
- `.planning/phases/10-rychag-components-modal-html/10-UI-REVIEW.md`, пункт 1 перечня «Top 3
  Priority Fixes» и раздел Pillar 6 ЦЕЛИКОМ.
- `.planning/phases/10-rychag-components-modal-html/10-53-PLAN.md` — ФОРМА ОСТАНОВА И ФОРМА ЗАПИСИ
  ОТВЕТА наследуются отсюда: как предъявлялись замеры, как формулировался вопрос, как ответ
  превращался в запись, встающую РЯДОМ с прежней.
- `.planning/WINDOWS.md`, записи 74, 76, 77, 78.
  </read_first>

  <decision>
**ВОПРОС ВЛАДЕЛЬЦУ ОДИН: какой ценой по критерию 3 плашка отказа получает ВЫХОД?**

Предъявление собирается ЗАМЕРАМИ НА HEAD, командами, а не пересказом плана. Исполнитель обязан
снять и привести ДОСЛОВНО:

1. `grep -c "addEventListener" app/templates/includes/htmx_error_banner.html` — сегодняшнее число
   регистраций в файле.
2. `grep -n "FAILURE_BANNER_HANDLERS_MEASURED = " tests/test_pages/test_shell.py` — объявленное
   число и его место.
3. `uv run pytest tests/test_templates/test_components.py -k criterion_three -q` — сегодняшний цвет
   гейта критерия 3.
4. `sed -n '/^overrides:/,/^covered_files:/p' .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md`
   либо равнозначное чтение блока `overrides` — обе действующие записи с их полями `covers` и
   границей второй записи ДОСЛОВНО.
5. `grep -c "x-data" app/templates/includes/htmx_error_banner.html` — ноль узлов клиентского
   состояния в файле сегодня.

**ВЕТВЬ A — `dismiss-control-plus-reset` (форма, которую планировщик считает наименьшей по цене).**
Орган снятия — настоящий элемент управления БЕЗ регистрации обработчика: узел, чьё состояние
переключает сам браузер, с доступным именем, а скрытие снятой заготовки выражено объявлением таблицы
стилей по её состоянию. Возврат плашки на новом отказе обеспечивается **ДВУМЯ строками, добавленными
в ТЕЛА ДВУХ УЖЕ СУЩЕСТВУЮЩИХ обработчиков отказа** — по одной на обработчик: состояние органа
сбрасывается перед снятием атрибута скрытия.
- Цена: **регистраций не прибавляется** — пятое утверждение гейта остаётся `{ads/form.html: 8,
  includes/htmx_error_banner.html: 3}`, `FAILURE_BANNER_HANDLERS_MEASURED` НЕ правится,
  `FAILURE_BANNER_EVENTS` остаётся тремя именами, узлов клиентского состояния не прибавляется,
  вхождений `hx-on` остаётся ноль. **Все пять чисел гейта критерия 3 не двигаются.**
- Чего она ВСЁ РАВНО стои́т: **ДВЕ ИСПОЛНЯЕМЫЕ СТРОКИ** внутри тел действующих обработчиков этого
  файла. Первая запись отступления принимает ровно такую ФОРМУ правки («правка ТЕЛА метода уже
  существующего объекта… и выражения УЖЕ СУЩЕСТВУЮЩЕГО атрибута события»), но её `covers` называет
  ДВА ДРУГИХ ФАЙЛА; вторая покрывает этот файл, но РОВНО семь строк третьего обработчика. **Значит
  требуется РАСШИРЕНИЕ ПРОЧТЕНИЯ владельцем — но НЕ сдвиг ни одного объявленного числа.**

**ВЕТВЬ B — `new-registration` (форма, предложенная отчётом UI-ревизии дословно).** Кнопка снятия с
обработчиком нажатия плюс гашение плашки обрыва связи на следующем событии НАЧАЛА обмена, чтобы
повторная попытка видимо убирала прежний отказ.
- Цена, названная числами: **пятое утверждение гейта покраснеет немедленно** и напечатает
  `includes/htmx_error_banner.html: найдено 4, объявлено 3` (зубы измерены двенадцатым кругом
  мутацией). Требуется: (i) НОВОЕ РЕШЕНИЕ ВЛАДЕЛЬЦА — действующая запись объявляет, что на будущие
  регистрации в этом файле она НЕ РАСПРОСТРАНЯЕТСЯ; (ii) сдвиг `FAILURE_BANNER_HANDLERS_MEASURED`
  3 → 4 с записью основания; (iii) ЧЕТВЁРТОЕ ИМЯ в `FAILURE_BANNER_EVENTS`, иначе правило сличения
  числа регистраций с числом имён событий краснеет вторым; (iv) третья запись блока `overrides`.
- Что она даёт СВЕРХ ветви A: видимое гашение прежнего отказа при ПОВТОРНОЙ ПОПЫТКЕ до получения
  ответа. ⚠️ ЗАМЕР ПЛАНИРОВЩИКА ГОВОРИТ, ЧТО ЭТА ПРИБАВКА МАЛА: действующий третий обработчик уже
  гасит обе заготовки на УСПЕШНОМ обмене, поэтому удавшаяся повторная попытка убирает плашку и без
  ветви B; разница видна только в промежутке между началом запроса и его ответом.

**ВЕТВЬ C — `css-only` (ноль исполняемых строк).** Орган снятия и скрытие целиком в разметке и
таблице стилей, тел обработчиков не касаемся вовсе.
- Цена по критерию 3: **НОЛЬ.** Все пять чисел не двигаются, новых записей отступления не требуется,
  критерий соблюдён БУКВАЛЬНО.
- Чего она стои́т ЧЕЛОВЕКУ, и планировщик обязан назвать это прямо: состояние снятия замкнуто на
  себя, а сценарий умеет ровно одно — снять атрибут скрытия. Значит **снятая однажды плашка не
  вернётся до перезагрузки страницы**, и следующая настоящая авария того же рода останется человеку
  НЕ ПОКАЗАННОЙ. Это обмен пришпиленного прямоугольника на ТИШИНУ, и планировщик рекомендует ветвь C
  НЕ ВЫБИРАТЬ — но записывает её, потому что она единственная, чья цена по критерию 3 равна нулю, и
  скрыть её значило бы решить за владельца.

**ОТКЛОНЕНО ДО ОСТАНОВА, И ОСНОВАНИЕ — ЗАМЕР, А НЕ ВКУС:** форма с атрибутом события рантайма
разметки (`hx-on::…`). Третье утверждение гейта объявляет вхождений НОЛЬ, а выражение
`INLINE_HANDLER_ATTR` (`tests/test_templates/test_htmx_markup_security.py:172`) есть выражение
ПО `hx-on` — правка покраснит гейт немедленно, и предмет запрета (второй слой экранирования поверх
слоя разметки) решением о критерии 3 не снимается. Форма с новым узлом клиентского состояния
отклонена по той же арифметике: она двигает ЧЕТВЁРТОЕ объявленное число, то есть стои́т РОВНО
столько же, сколько ветвь B, и не даёт ничего сверх ветви A.
  </decision>

  <options>
- **A — `dismiss-control-plus-reset`:** расширить прочтение критерия 3 на ДВЕ строки в телах двух
  действующих обработчиков этого файла. Ни одно объявленное число не двигается. Плашка возвращается
  на новом отказе. **РЕКОМЕНДАЦИЯ ПЛАНИРОВЩИКА.**
- **B — `new-registration`:** принять четвёртую регистрацию: новая запись отступления ПЛЮС сдвиг
  объявленного числа 3 → 4 ПЛЮС четвёртое имя события. Прибавка к исходу для человека — видимое
  гашение в промежутке между началом запроса и ответом.
- **C — `css-only`:** ноль исполняемых строк, критерий соблюдён буквально; цена платится тем, что
  снятая плашка не вернётся до перезагрузки, и следующая авария останется непоказанной.
- **D — `defer`:** отложить `UI-1` целиком с названным владельцем и записью в `deferred-items.md`;
  плашка остаётся без выхода, и это записывается как принятое, а не как незамеченное.
  </options>

  <resume-signal>
Ответ владельца обязан назвать **идентификатор ветви** (`A`, `B`, `C` либо `D`) и — для ветвей `A` и
`B` — **ОБЛАСТЬ записи отступления** одним из двух прочтений, потому что второй раз этот вопрос
обойдётся дороже:
- **узко:** запись покрывает РОВНО эту правку (для `A` — две названные строки в телах двух
  названных обработчиков; для `B` — одну названную новую регистрацию), и всякая следующая правка
  того же файла требует НОВОГО решения;
- **широко:** запись покрывает КЛАСС правки в этом файле (для `A` — правку тел уже существующих
  обработчиков вообще), и следующая правка того же класса нового решения не требует.

⚠️ ОБЛАСТЬ СПРАШИВАЕТСЯ ОТДЕЛЬНО ОТ ВЕТВИ, И ЭТО НЕ ФОРМАЛЬНОСТЬ: у второй действующей записи
именно УЗОСТЬ есть её предмет, и запись, охват которой шире принятого владельцем, читалась бы
следующим кругом как правило проекта, переживающее фазу (класс угрозы `T-10-53-02`).

⚠️ ЧЕГО ЭТОТ ОТВЕТ НЕ ЗАКРЫВАЕТ, И ЭТО НАЗЫВАЕТСЯ ВЛАДЕЛЬЦУ ВМЕСТЕ С ВОПРОСОМ: он НЕ закрывает
`FORM-06` — требование закрывается ПОДПИСЬЮ на приёмке обхода `10-UAT.md`, и это ДРУГОЙ вопрос;
он НЕ закрывает решения о 285 запретах фазы и о двенадцати открытых угрозах `10-SECURITY.md`
(область отказа `phase-10-only`); он НЕ закрывает долга свежего прохода шагов 2.8 и 4.4. Смешать эти
решения в одном ответе значило бы получить один ответ на четыре вопроса.
  </resume-signal>

  <acceptance_criteria>
- В предъявлении владельцу приведены ВСЕ ПЯТЬ замеров дословным выводом команд, а не пересказом; у
  каждого назван снятый HEAD.
- Граница второй действующей записи отступления приведена ДОСЛОВНОЙ цитатой, а не изложением.
- Ответ владельца записан в сводке идентификатором ветви И идентификатором области (`узко`/`широко`)
  либо, для ветви `D`, названным владельцем отсрочки.
- **ПЕРВОИСТОЧНИК ЗАПИСИ ПЕРЕНЕСЁН В СВОДКУ ДО ПРАВКИ ЧЕГО-ЛИБО, И ЭТО АКЦЕПТАНС, А НЕ ПОЖЕЛАНИЕ:**
  в сводку переносятся (а) ДОСЛОВНЫЙ вывод всех пяти замеров — слово в слово, с их HEAD, — и
  (б) ДОСЛОВНЫЙ текст ответа владельца, а не его пересказ идентификаторами. ⚠️ ОСНОВАНИЕ ЗАМЕРЕНО, А
  НЕ ПРЕДПОЛОЖЕНО: устройство останова объявляет, что контекст подагента ПРИ ПАУЗЕ ОТБРАСЫВАЕТСЯ
  (`gsd-core/references/checkpoints.md`, «subagent context is discarded across the pause»), а задача
  3 обязана внести эти пять замеров в запись `overrides` ВЫВОДОМ, А НЕ ССЫЛКОЙ. После задачи 2 они
  НЕВОСПРОИЗВОДИМЫ: для ветви `B` число регистраций станет 4 вместо 3, и повторный прогон тех же
  команд дал бы ДРУГИЕ числа — то есть запись отступления описывала бы дерево ПОСЛЕ правки как
  основание решения, принятого ДО неё. Сводка и есть тот артефакт, в котором первоисточник живёт
  независимо от окна исполнителя.
- Перебор бюджета назван в сводке ЧИСЛОМ: проекция 115000 против бюджета 100000 (1.15x),
  `sample_count: 0`, маршрут — ПРИНЯТИЕ с основанием из блока `estimate`, и приведён фактический
  расход как первый ОБРАЗЕЦ для калибровки.
- Ни один файл дерева до получения ответа не тронут: `git status --porcelain` на момент останова
  показывает только артефакты планирования, если показывает что-либо.
  </acceptance_criteria>

  <done>
Владелец назвал ветвь и — для ветвей A и B — область записи отступления; ответ записан дословно и
стал единственным основанием формы правки задачи 2.
  </done>

  <reversibility rating="one-way">
Запись отступления, внесённая в отчёт верификации, читается следующими кругами и следующими фазами
как ПРАВИЛО ПРОЕКТА и переживает партию: отозвать её означает объявить ошибочным подписанное решение
владельца, а не откатить коммит. Область записи поэтому спрашивается ОТДЕЛЬНО от ветви, и `one-way`
здесь есть свойство ЗАПИСИ, а не кода: две строки сценария откатываются одним коммитом, подпись — не
откатывается ничем.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: выход у плашки — форма ветви, названной владельцем, с возвратом плашки на новом отказе и правилом области снятия</name>

  <files>
app/templates/includes/htmx_error_banner.html
app/static/css/app.css
tests/test_pages/test_shell.py
  </files>

  <read_first>
- ОТВЕТ ВЛАДЕЛЬЦА, записанный задачей 1: идентификатор ветви и идентификатор области. ⚠️ ЭТО
  ЕДИНСТВЕННОЕ ОСНОВАНИЕ ФОРМЫ ПРАВКИ. Ветвь, не названная владельцем, не исполняется ни одной
  строкой, даже если кажется лучше.
- `app/templates/includes/htmx_error_banner.html:213-235` в состоянии ПОСЛЕ плана 10-56: обе
  заготовки несут класс стопки, разметка и сценарий прочитаны. ⚠️ СЦЕНАРИЙ ЧИТАТЬ ЦЕЛИКОМ и
  запомнить, что тела обоих обработчиков отказа заканчиваются снятием атрибута скрытия у своего
  узла.
- `app/static/css/app.css` в состоянии ПОСЛЕ плана 10-56: блок подъёма со свойством-величиной и три
  блока по классу стопки. ⚠️ БЛОК ПОДЪЁМА НЕ ПРАВИТСЯ, И СПОСОБ ОТОБРАЖЕНИЯ В НЁМ НЕ ОБЪЯВЛЯЕТСЯ.
- `app/static/css/app.css:1150-1200` — проза блока подъёма, абзац «пункт (д)». ⚠️ ЧИТАТЬ ДОСЛОВНО:
  запрет относится к БЛОКУ ПОДЪЁМА, и настоящая задача его СОБЛЮДАЕТ, а объявление скрытия снятой
  заготовки ставит ДРУГИМ блоком по классу — при этом со значением «нет», которое атрибут скрытия
  перебить не способно НИ В КАКУЮ сторону. Это различие выписывается в прозу рядом, чтобы следующий
  читатель не прочёл запрет снятым.
- `app/static/css/app.css:1330-1333` и `:2224-2233` — действующие применения условного селектора по
  состоянию вложенного элемента управления (`:has(…)`) в этой же таблице. ⚠️ ФОРМА НАСЛЕДУЕТСЯ
  ОТСЮДА, а не изобретается: у проекта эта форма уже есть, и вторая её редакция была бы вторым
  определением.
- `app/templates/components/alert.html` — макрос плашки ЦЕЛИКОМ. ⚠️ ЧИТАТЬ: орган снятия встаёт
  ВНУТРИ узла заготовки, а НЕ внутрь макроса плашки — макрос обслуживает десятки мест проекта, и
  правка его тела вынесла бы орган снятия во все.
- `tests/test_pages/test_shell.py:2245-2270` — `_network_banner_line`. ⚠️ СТРОКА С ИДЕНТИФИКАТОРОМ
  ЗАГОТОВКИ ОБРЫВА СВЯЗИ ОБЯЗАНА ОСТАТЬСЯ ОДНОЙ СТРОКОЙ вместе с вызовом макроса плашки.
- `tests/test_pages/test_shell.py:2316-2420` — `FAILURE_BANNER_REGISTRATION_HARNESS` и форма
  исполнения сценария на стабах узлов. ⚠️ ФОРМА ПРАВИЛ, ИСПОЛНЯЮЩИХ СЦЕНАРИЙ, НАСЛЕДУЕТСЯ ОТСЮДА:
  стаб неймитирующий ровно там, где предмет.
- `tests/test_pages/test_shell.py:2703-2970` — `test_the_failure_banner_is_cleared_by_a_successful_exchange`,
  `test_a_failed_exchange_does_not_clear_the_failure_banner` и ДВА отрицательных контроля при них.
  ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: это правила третьего обработчика, и настоящая задача обязана оставить их
  зелёными — «гасить на ЛЮБОМ завершении обмена» они краснят намеренно.
- `tests/test_pages/test_shell.py:3936-3990` — `_banner_lift_display_findings` и
  `test_the_failure_banner_lift_declares_no_display_mode`.
- `tests/test_templates/test_components.py:5164-5195` — пятое утверждение гейта критерия 3.
  </read_first>

  <behavior>
1. Внутри КАЖДОГО узла заготовки есть орган снятия: он попадает в порядок обхода с клавиатуры и
   несёт доступное имя (для ветви `B` — настоящая кнопка; для ветвей `A` и `C` — элемент управления,
   чьё состояние переключает браузер).
2. Приведённый в действие орган снятия убирает СВОЮ заготовку с экрана и НЕ трогает вторую.
3. Для ветвей `A` и `B`: следующий отказ того же рода показывает заготовку СНОВА — состояние снятия
   сброшено. Для ветви `C` это НЕ ТАК, и правило ветви `C` утверждает ровно наблюдаемое, а не
   желаемое (иначе оно зеленело бы ложью).
4. Повторное приведение органа в то же состояние исхода не меняет, и число узлов заготовок в
   документе не растёт ни от одного действия.
5. Способ отображения у узлов заготовок объявляется ТОЛЬКО значением «нет» — иначе атрибут скрытия
   перебит и человек получает пустые подложки на каждом экране.
6. Пять чисел гейта критерия 3 равны объявленным, и объявление сдвинуто ТОЛЬКО если это предписал
   ответ владельца (ветвь `B`).
  </behavior>

  <action>
**(0) ВЕТВЛЕНИЕ ПО ОТВЕТУ.** Исполняется РОВНО ОДНА ветвь — та, которую назвал владелец. Её
идентификатор пишется первой строкой сводки. Неисполненные ветви в дерево не попадают ни строкой.
Если назван `D` (`defer`), задача 2 НЕ ИСПОЛНЯЕТСЯ ВОВСЕ: правки продукта нет, и задача 3
записывает отсрочку `UI-1` с названным владельцем наравне с пятью находками UI-ревизии.

**(1) ВЕТВЬ `A` — ОРГАН БЕЗ РЕГИСТРАЦИИ ПЛЮС СБРОС В ТЕЛАХ ДВУХ ДЕЙСТВУЮЩИХ ОБРАБОТЧИКОВ.**
- *Разметка.* Внутрь каждого узла заготовки, ПЕРЕД вызовом макроса плашки у заготовки отказа сервера
  и на ТОЙ ЖЕ СТРОКЕ у заготовки обрыва связи, ставится элемент управления с собственным классом,
  доступным именем на русском («Скрыть сообщение») и без всякого атрибута события. Его состояние
  переключает браузер, поэтому регистрации обработчика он не требует. ⚠️ РАЗМЕТКА ОБЕИХ ЗАГОТОВОК
  ОСТАЁТСЯ ПО ОДНОЙ СТРОКЕ НА ЗАГОТОВКУ — основание записано запретом во frontmatter и правилом
  `_network_banner_line`.
- *Таблица стилей.* Два блока: внешний вид органа снятия (положение внутри уже поднятой заготовки,
  снятие вида по умолчанию, знак крестика генерируемым содержимым, видимый признак фокуса по образцу
  действующих `:focus-visible` таблицы) и правило скрытия заготовки, чей орган приведён в действие.
  Второй блок адресует УЗЕЛ ЗАГОТОВКИ ПО КЛАССУ СТОПКИ через условный селектор по состоянию
  вложенного органа и объявляет способ отображения значением «нет». ⚠️ АДРЕСУЕТ ПО КЛАССУ, А НЕ ПО
  ИДЕНТИФИКАТОРУ: блок с идентификатором заготовки попал бы в отбор `_banner_elevation_rules`, и
  пять действующих правил подъёма покраснели бы за ФОРМУ правки. Проза рядом называет, почему
  значение «нет» запрета пункта (д) не нарушает: оно способно только ДОБАВИТЬ скрытости и не
  способно её отнять, тогда как предмет запрета — объявление, ПЕРЕБИВАЮЩЕЕ атрибут скрытия.
- *Сценарий.* В тела ДВУХ действующих обработчиков отказа добавляется по ОДНОЙ строке: состояние
  органа снятия своей заготовки возвращается в исходное ПЕРЕД снятием атрибута скрытия. ⚠️ ТЕЛО
  ТРЕТЬЕГО ОБРАБОТЧИКА НЕ ТРОГАЕТСЯ НИ НА СИМВОЛ. Ни одной новой регистрации не заводится; форма
  добавленных строк не есть ни вызов метода регистрации слушателя, ни присваивание
  свойства-обработчика, и это проверяется прогоном гейта критерия 3, а не утверждается.

**(2) ВЕТВЬ `B` — НОВАЯ РЕГИСТРАЦИЯ, И ЦЕНА ПЛАТИТСЯ ЗАПИСЬЮ, А НЕ ПРАВКОЙ ЧИСЛА.**
- *Разметка и таблица стилей.* Кнопка снятия внутри каждого узла заготовки (`<button
  type="button">` с доступным именем) и блоки её внешнего вида; правило скрытия — как в ветви `A`
  либо снятием атрибута скрытия из обработчика, по выбору исполнителя, с записью основания.
- *Сценарий.* Новая регистрация на узле тела документа ВНУТРИ действующей обёртки признака
  однократности — иначе число слушателей на живом узле росло бы всю сессию (основание записано в
  прозе файла и стережётся `test_the_failure_banner_registers_its_handlers_once_per_body`).
- *Объявления.* `FAILURE_BANNER_HANDLERS_MEASURED` сдвигается 3 → 4, и РЯДОМ С НИМ пишется летопись
  по форме действующей (что снято, когда, чем, и что означает движение числа в каждую сторону), со
  ССЫЛКОЙ НА ОТВЕТ ВЛАДЕЛЬЦА как на основание. В `FAILURE_BANNER_EVENTS` дописывается ЧЕТВЁРТОЕ имя
  В КОНЕЦ перечня — порядок первых трёх значим, второй элемент читается по позиции соседними
  правилами. ⚠️ ПРАВКА ОДНОГО ЛИШЬ ЧИСЛА БЕЗ ЗАПИСИ ОСНОВАНИЯ ЗАПРЕЩЕНА ПРЯМО: это ровно то
  молчание, против которого пятое утверждение гейта заведено, и его собственный текст отказа это
  говорит.

**(3) ВЕТВЬ `C` — БЕЗ ЕДИНОЙ ИСПОЛНЯЕМОЙ СТРОКИ.** Разметка и таблица стилей как в ветви `A`, тела
обработчиков НЕ ТРОГАЮТСЯ ВОВСЕ. ⚠️ ПРАВИЛО ВОЗВРАТА ПЛАШКИ В ЭТОЙ ВЕТВИ УТВЕРЖДАЕТ НАБЛЮДАЕМОЕ, А
НЕ ЖЕЛАЕМОЕ: оно фиксирует, что снятая заготовка до перезагрузки страницы не возвращается, и его
докстринг называет это ПРИНЯТОЙ ЦЕНОЙ с указанием на ответ владельца. Правило, утверждающее возврат
там, где возврата нет, зеленело бы ложью — а правило, фиксирующее цену, делает её видимой следующему
кругу.

**(4) ПРАВИЛО ОРГАНА СНЯТИЯ — `test_every_failure_banner_carries_a_dismiss_control`.** Читает
исходник включения (форма и путь параметром — как у `_failure_banner_source`) и утверждает: у
КАЖДОГО из двух узлов заготовок внутри есть орган снятия; у каждого органа есть доступное имя;
орган есть элемент, попадающий в порядок обхода с клавиатуры БЕЗ приписанного числа порядка. Отказ
печатает ПОЛУЧЕННОЕ: для какой заготовки орган не найден и что найдено вместо него.

**(5) ПРАВИЛО ОБЛАСТИ СНЯТИЯ — `test_dismissing_one_banner_leaves_the_other_shown`.** Утверждает по
ТАБЛИЦЕ СТИЛЕЙ, что селектор скрытия адресует узел заготовки, содержащий приведённый в действие
орган, а не всех носителей класса стопки: отказ печатает полученный селектор ДОСЛОВНО. ⚠️ ЭТО
РАЗРЕШЕНИЕ РЕБРА `concurrency` ТРЕБОВАНИЯ FORM-06 ЯВНЫМ КРИТЕРИЕМ: при двух одновременных авариях
человек снимает прочитанное и оставляет непрочитанное.

**(6) ПРАВИЛО ВОЗВРАТА — `test_a_dismissed_banner_returns_on_the_next_failure`.** Для ветвей `A` и
`B`: исполняется СЦЕНАРИЙ на стабах узлов существующей оснасткой модуля — событие отказа после
снятия обязано вернуть заготовку, и утверждается, что состояние органа сброшено. Для ветви `C`
правило имеет ту же роль, но фиксирует цену (см. действие 3). Отказ печатает полученное состояние
органа и полученное значение атрибута скрытия.

**(7) ПРАВИЛО ЗНАЧЕНИЙ СПОСОБА ОТОБРАЖЕНИЯ — `test_no_banner_rule_declares_a_display_mode_that_shows`.**
Собирает ВСЕ блоки таблицы, чей селектор адресует узел заготовки по идентификатору ИЛИ по классу
стопки, и утверждает: единственное значение способа отображения, встречающееся в них, — «нет».
Отказ печатает селектор и полученное значение. ⚠️ ЭТО РАСШИРЕНИЕ ЗУБОВ ПУНКТА (д), А НЕ ЕГО
ОСЛАБЛЕНИЕ: действующее правило стережёт БЛОК ПОДЪЁМА, а это — все блоки, достигающие узла
заготовки, и оба остаются в силе. Отрицательный контроль при нём: подстановка показывающего значения
способа отображения КРАСНИТ правило.

**(8) ОТРИЦАТЕЛЬНЫЕ КОНТРОЛИ — ВЫЗЫВАЮТ САМ ГЕЙТ.** Формой действующих контролей модуля: доктóренная
копия во временном каталоге, подстановка по ДОСЛОВНОМУ куску, вызов ТОЙ ЖЕ функции находок, которую
вызывает правило. ⚠️ ЭТО ПРЯМОЕ ИСПОЛНЕНИЕ НАХОДКИ `WR-01` И СОВЕЩАТЕЛЬНОЙ ПОМЕТКИ ДВЕНАДЦАТОГО
КРУГА: контроль, повторяющий логику гейта своим выражением, остаётся зелёным при обезоруженном гейте.
Контролей два: снятый орган снятия у одной заготовки; показывающее значение способа отображения.

**(9) ЗАМЕР ПЕРЕХОДА ЦВЕТА.** Правила (4) и (6) прогоняются ДО правки продукта, и в сводку
переносятся ДОСЛОВНЫЕ строки их отказов с ПОЛУЧЕННЫМИ значениями. ⚠️ КОД ВОЗВРАТА САМ ПО СЕБЕ
ЗАМЕРОМ RED НЕ ЯВЛЯЕТСЯ: ошибка сбора, описка в отборе `-k` и отказ ввоза дают тот же ненулевой код
при НЕИСПОЛНЕННОМ правиле.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed"; или число `passed` меньше 240 — модуль на HEAD давал 230 passed, план 10-56 прибавил не менее пяти правил, настоящая задача — не менее пяти; недобор означает потерю сбора, а не зелень</fails_when>
    <automated>uv run pytest tests/test_pages/test_shell.py -k "dismiss or dismissed or display_mode or banner" -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed"; или в выводе есть "no tests ran"; или число `passed` меньше пяти — пустой либо неполный отбор зеленеет вакуумом при коде возврата 0</fails_when>
    <automated>uv run pytest tests/test_templates/test_components.py -k criterion_three -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed"; или в выводе есть "no tests ran"; или в выводе присутствует строка "УТВЕРЖДЕНИЕ 5 ИЗ 5" либо "УТВЕРЖДЕНИЕ 4 ИЗ 5" либо "УТВЕРЖДЕНИЕ 3 ИЗ 5" — печать любого из трёх означает, что объявленное число сдвинулось не по ответу владельца</fails_when>
    <automated>uv run pytest tests/test_templates/ tests/test_pages/test_confirm_delete_transport.py -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed"; или число `passed` меньше 300 — набор шаблонных и транспортных гейтов на HEAD давал 323 passed вместе с `tests/test_planning/`</fails_when>
  </verify>

  <acceptance_criteria>
- Первой строкой сводки стои́т идентификатор ИСПОЛНЕННОЙ ветви и идентификатор области, названные
  владельцем; иной ветви в дереве нет ни строкой.
- Орган снятия есть у КАЖДОЙ заготовки: вывод `grep -c "<класс органа снятия>"
  app/templates/includes/htmx_error_banner.html` равен 2 (имя класса подставляет исполнитель и
  называет в сводке), и у каждого органа есть доступное имя (положительный греп по имени, 2
  вхождения).
- Разметка каждой заготовки осталась ОДНОЙ строкой: вывод `grep -c 'id="htmx-failure-server"'` и
  `grep -c 'id="htmx-failure-network"'` равен 1 каждый, и в строке заготовки обрыва связи
  присутствуют все четыре основы слов о расхождении (`экран`, `серв`, `обнов`, `настоящ`) — четыре
  положительных грепа.
- Третий обработчик не тронут: дифф файла разметки не содержит изменённых строк в диапазоне его тела,
  и `test_the_failure_banner_is_cleared_by_a_successful_exchange`,
  `test_a_failed_exchange_does_not_clear_the_failure_banner` и оба их контроля зелены (приводится
  выводом отбора).
- Число регистраций в файле: вывод `grep -c "addEventListener"
  app/templates/includes/htmx_error_banner.html` приводится в сводке ЧИСЛОМ и сличается с
  `FAILURE_BANNER_HANDLERS_MEASURED`. Для ветвей `A` и `C` оба равны трём и объявление НЕ ПРАВЛЕНО;
  для ветви `B` оба равны четырём, и РЯДОМ с объявлением стои́т летопись со ссылкой на ответ
  владельца.
- Узлов клиентского состояния в файле по-прежнему ноль: `grep -c "x-data"
  app/templates/includes/htmx_error_banner.html` равен 0.
- Вхождений атрибута события рантайма разметки в файле ноль: `grep -c "hx-on"
  app/templates/includes/htmx_error_banner.html` равен 0.
- Блок подъёма не тронут: дифф `app/static/css/app.css` не содержит изменённых строк внутри блока,
  чей селектор несёт адреса заготовок, и `test_the_failure_banner_lift_declares_no_display_mode`
  зелено.
- Единственное значение способа отображения во всех блоках, достигающих узла заготовки, — «нет»:
  подтверждается зеленью правила (7) и выводом его отрицательного контроля.
- В сводке приведены ДОСЛОВНЫЕ строки отказов правил (4) и (6) на дереве ДО правки, с полученными
  значениями. Утверждение «правило покраснело» без этих строк акцептансом не считается.
  </acceptance_criteria>

  <done>
Каждая заготовка несёт достижимый с клавиатуры орган снятия с доступным именем; снятие убирает свою
заготовку и не трогает вторую; для выбранной ветви записано и проверено поведение при новом отказе;
единственное значение способа отображения у узлов заготовок — «нет»; пять чисел гейта критерия 3
равны объявленным, и объявление сдвинуто только по ответу владельца; третий обработчик не тронут.
  </done>

  <reversibility rating="costly">
Код ветви откатывается одним обратным коммитом, но ЗАПИСЬ ОСНОВАНИЯ, внесённая задачей 3, —
не откатывается: она есть подписанное решение владельца. Для ветви `B` цена выше: сдвинутое
объявленное число и четвёртое имя события становятся частью контракта модуля правил, на который
ключáются отборы прежних планов. Поэтому ветвь исполняется ТОЛЬКО названная, и ни одна строка
неисполненной ветви в дерево не попадает.
  </reversibility>
</task>

<task type="auto">
  <name>Задача 3: записи — третья запись отступления рядом с двумя действующими, распоряжение по пяти находкам UI-ревизии и долг свежего прохода двух шагов обхода</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
.planning/phases/10-rychag-components-modal-html/deferred-items.md
.planning/WINDOWS.md
  </files>

  <read_first>
- **ПЕРВОИСТОЧНИК ОТВЕТА ВЛАДЕЛЬЦА И ПЯТИ ЗАМЕРОВ — СВОДКА ЗАДАЧИ 1 ЭТОГО ЖЕ ПЛАНА**
  (`.planning/phases/10-rychag-components-modal-html/10-57-SUMMARY.md` в состоянии после задачи 1):
  дословный текст ответа и дословный вывод всех пяти замеров с их HEAD. ⚠️ ЧИТАТЬ ОТСЮДА, А НЕ
  ПЕРЕСНИМАТЬ КОМАНДАМИ: пять замеров сняты на дереве ДО задачи 2 и после неё НЕВОСПРОИЗВОДИМЫ —
  для ветви `B` число регистраций уже 4, а не 3. Пересъёмка дала бы ДРУГИЕ числа, и запись
  отступления описывала бы дерево ПОСЛЕ правки как основание решения, принятого ДО неё. Если в
  сводке дословного вывода либо дословного ответа НЕТ — настоящая задача НЕ ИСПОЛНЯЕТСЯ, а
  останавливается с этим сообщением: односторонне-необратимая запись по пересказу не заводится.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:196-262` — комментарий круга
  над блоком `overrides` и ОБЕ действующие записи ЦЕЛИКОМ, со всеми полями (`must_have`, `reason`,
  `accepted_by`, `accepted_at`, `accepted_branch`, `covers`, `not_reached_by_this_override`). ⚠️
  ФОРМА ТРЕТЬЕЙ ЗАПИСИ НАСЛЕДУЕТСЯ ОТСЮДА ПОЛЕ В ПОЛЕ, а не изобретается. ⚠️ И ЧИТАТЬ ДОСЛОВНО
  КОММЕНТАРИЙ: «Засчитать запись вправе СЛЕДУЮЩИЙ круг верификации, а не план» — поле
  `overrides_applied` настоящей задачей НЕ ДВИГАЕТСЯ.
- `.planning/phases/10-rychag-components-modal-html/10-53-PLAN.md`, задача записи ответа владельца
  ЦЕЛИКОМ. ⚠️ ЭТО ПРЕЦЕДЕНТ: та же операция уже выполнялась в этой фазе, и её форма (запись встаёт
  РЯДОМ, прежние не тронуты, поля вердикта не тронуты) переносится, а не переоткрывается.
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md:1-24` — ФОРМА ФАЙЛА ЦЕЛИКОМ:
  якорь `<!-- deferred-items:end -->`, «РОВНО ОДИН РАЗДЕЛ НА ПЛАН», вставка ПЕРЕД якорем и
  основание замера (задача 3 плана 10-39).
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md`, раздел `## План 10-55`
  (внесён планом 10-55 той же партии) — на него ссылается новый раздел, чтобы читатель видел, что
  одиннадцать находок ревизии распоряжены, а не забыты.
- `tests/test_planning/test_deferred_items_are_idempotent.py` ЦЕЛИКОМ — четыре принуждаемых свойства
  формы файла. ⚠️ ИСПОЛНИТЕЛИ ОТБИРАЮТ `tests/test_planning/` ИЗ ПРОГОНОВ, поэтому команда проверки
  НАЗЫВАЕТ ЭТОТ КАТАЛОГ ЯВНО.
- `.planning/phases/10-rychag-components-modal-html/10-UI-REVIEW.md`, раздел «Further findings (the
  list does not stop at three)» ЦЕЛИКОМ плюс пункт 3 перечня «Top 3 Priority Fixes». ⚠️ ЧИТАТЬ
  КАЖДУЮ ИЗ ПЯТИ НАХОДОК: распоряжение обязано называть находку ПОИМЁННО и её файл, а не отсылать к
  диапазону строк отчёта.
- `.planning/WINDOWS.md` — последние записи и ФОРМА ведения журнала. ⚠️ ЗАПИСЬ ЗАВОДИТСЯ КОМАНДОЙ
  ведения (`windows append` с явными `--kind`, `--phase`, `--file`, `--description`), а НЕ правкой
  файла руками: счёта шапки разошлись бы молча, и прецедент этого решения — задача плана 10-53.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:542-600` — блок
  `human_verification` ЦЕЛИКОМ: три открытых человеческих пункта и четвёртый о решении владельца по
  запретам. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: настоящая задача НИ ОДНОГО из них не закрывает и ни одного не
  правит — она добавляет к двум первым ОСНОВАНИЕ свежего прохода.
  </read_first>

  <action>
**(1) ТРЕТЬЯ ЗАПИСЬ БЛОКА `overrides` — РЯДОМ, А НЕ ВМЕСТО.** В блок `overrides` отчёта
`10-VERIFICATION.md` дописывается ТРЕТЬЯ запись, по форме двух действующих, поле в поле:
`must_have` — формулировка критерия 3 ДОСЛОВНО, как она стои́т у двух действующих записей (сличается
посимвольно, а не пересказывается); `reason` — ответ владельца с ПРЕДЪЯВЛЕННЫМИ ЕМУ ЗАМЕРАМИ (пять
замеров задачи 1 приводятся здесь же выводом, а не ссылкой), и отдельным предложением — «чего эта
запись НЕ говорит», по форме второй действующей записи; `accepted_by`, `accepted_at`,
`accepted_branch` — из ответа; `covers` — ОБЛАСТЬ из ответа владельца, выписанная так, чтобы её
нельзя было прочесть шире (для области `узко` — перечень названных строк и обработчиков; для
`широко` — класс правки с явным указанием, что он покрывает, и с явным указанием, чего НЕ покрывает);
`not_reached_by_this_override` — что записью НЕ покрыто.
⚠️ **ДВЕ ДЕЙСТВУЮЩИЕ ЗАПИСИ И КОММЕНТАРИЙ КРУГА НАД БЛОКОМ НЕ ТРОГАЮТСЯ НИ СИМВОЛОМ.** ⚠️ **ПОЛЯ
ВЕРДИКТА НЕ ПРАВЯТСЯ:** `status`, `score`, `overrides_applied`, `behavior_unverified`,
`covered_digest`, разделы `gaps`, `advisory`, `human_verification`, `deferred` — они принадлежат
КРУГУ. Засчитать третью запись вправе СЛЕДУЮЩИЙ круг, и это прямое повторение решения, записанного
комментарием того же блока.
⚠️ Если владелец назвал ветвь `D`, третья запись НЕ ЗАВОДИТСЯ ВОВСЕ: отступления от критерия 3 не
происходит, и записывать нечего. Вместо неё отсрочка `UI-1` встаёт в раздел файла отложенного
наравне с пятью находками UI-ревизии.

**(2) РАЗДЕЛ ОТЛОЖЕННОГО — ПЯТЬ НАХОДОК UI-РЕВИЗИИ.** В `deferred-items.md` вносится РОВНО ОДИН
новый раздел `## План 10-57`, вставленный **ПЕРЕД** якорем `<!-- deferred-items:end -->`, с
подразделом на каждую из пяти находок: `UI-3` (плашки отказа не оповещают вспомогательные технологии
— узел с ролью сообщения присутствует в документе с загрузки, и переключается лишь атрибут скрытия
обёртки; `notice_area.html` в этом же проекте делает правильно), `UI-4` (тела диалогов расходятся у
десяти потребителей: шесть передают предложение о последствии, двое — опознаватель), `UI-5` (код
ответа валидации гасится молча на пути без подмены: ни свопа, ни плашки, ни уведомления), `UI-6`
(у диалога нет связи с его описанием), `UI-7` (возврат фокуса имеет названную дыру на пути
внеполосного снятия панели — честно раскрыт и не починен). Каждый подраздел несёт: ФАЙЛ находки,
одну строку о том, ЧТО найдено, **НАЗВАННОГО ВЛАДЕЛЬЦА** и одну строку причины отсрочки.
⚠️ ОСНОВАНИЕ ОТСРОЧКИ ОДНО И НАЗЫВАЕТСЯ ПРЯМО: решение владельца от 2026-09-12 ограничило партию
ТРЕМЯ находками (`CR-01`, `UI-1`, `UI-2`); находка, отсутствующая и в задаче, и в записи, была бы
МОЛЧАЛИВЫМ ПРОПУСКОМ. ⚠️ `UI-3` ПОЛУЧАЕТ ОТДЕЛЬНОЕ ПРЕДЛОЖЕНИЕ О ТОМ, ЧЕГО НЕ СДЕЛАЛ НАСТОЯЩИЙ
ПЛАН: орган снятия плашки оповещения вспомогательных технологий НЕ ЧИНИТ, и прочесть его как
частичную починку `UI-3` нельзя. Раздел заканчивается ссылкой на раздел `## План 10-55`, где
распоряжены одиннадцать находок ревизии, — чтобы шестнадцать читались как ОДНО распоряжение, а не
как два обрывка.

**(3) ЗАПИСЬ ЖУРНАЛА ОКОН — ДОЛГ СВЕЖЕГО ПРОХОДА И ПРИНЯТАЯ ЦЕНА.** Командой ведения журнала
заводится запись, называющая ТРИ вещи: (а) партия сдвинула СТОПКУ заготовок (план 10-56) и ВЫХОД у
плашки (настоящий план), поэтому прежние наблюдения шагов 2.8 и 4.4 обхода ОБЕСЦЕНЕНЫ и человек
должен пройти их ЗАНОВО — с указанием, что у шага 4.4 прибавился предмет: ЧИСЛО ВИДИМЫХ ЗАГОТОВОК;
(б) цена по критерию 3 принята ветвью, названной владельцем, и живёт третьей записью блока
`overrides` (либо не принята вовсе — для ветви `D`); (в) `FORM-06` остаётся ОТКРЫТЫМ, и закрывает
его ПОДПИСЬ ЧЕЛОВЕКА на приёмке обхода, а не настоящая партия. Идентификатор заведённой записи
приводится в сводке. ⚠️ ЗАПИСЬ ЗАВОДИТСЯ КОМАНДОЙ, А НЕ ПРАВКОЙ ФАЙЛА РУКАМИ: счёта шапки журнала
разошлись бы молча, и прецедент этого решения — задача плана 10-53.

**(4) ОБРАТНАЯ ССЫЛКА В СВОДКЕ.** В сводке приводится перечень ШЕСТНАДЦАТИ внеобластных находок с их
владельцами — одиннадцать из раздела плана 10-55 и пять из раздела настоящего плана, — чтобы
распоряжение читалось без открытия файла отложенного, и чтобы равенство «шестнадцать найдено =
шестнадцать распоряжено» было ВИДИМЫМ ЧИСЛОМ.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed"; или в выводе есть "no tests ran" — каталог назван ЯВНО потому, что исполнители его отбирают из прогонов, и дефект формы файла отложенного либо самозаверение обхода иначе доезжают до гейта оркестратора вместо задачи</fails_when>
    <automated>grep -c "^## План 10-57$" .planning/phases/10-rychag-components-modal-html/deferred-items.md</automated>
    <fails_when>вывод не равен 1 — ноль означает, что раздел не вставлен, два и более означают, что правка дописала второй раздел того же плана</fails_when>
    <automated>grep -n "deferred-items:end" .planning/phases/10-rychag-components-modal-html/deferred-items.md | tail -1</automated>
    <fails_when>вывод пуст (якорь потерян); или номер строки якоря МЕНЬШЕ номера строки заголовка раздела 10-57, полученного командой `grep -n "^## План 10-57$"` — раздел обязан стоя́ть ПЕРЕД якорем</fails_when>
    <automated>grep -c "accepted_branch:" .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md</automated>
    <fails_when>вывод меньше 3 для ветвей A/B (третья запись не заведена) либо не равен 2 для ветви D (запись заведена там, где отступления не происходит); значение более 3 означает, что записей внесено больше одной</fails_when>
    <automated>git diff "$PLAN_BASE"..HEAD -- .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-UAT.md .planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md</automated>
    <fails_when>вывод НЕПУСТ — ни флажок требования, ни клетка охвата, ни один символ обхода, ни реестр запретов настоящей задачей не трогаются</fails_when>
  </verify>

  <acceptance_criteria>
- Обе действующие записи блока `overrides` не тронуты: дифф отчёта не содержит изменённых строк
  внутри них и внутри комментария круга над блоком; проверяется предъявлением диффа в сводке.
- Поля вердикта круга не тронуты: дифф отчёта не содержит изменённых строк, начинающихся с
  `status:`, `score:`, `overrides_applied:`, `behavior_unverified:`, `covered_digest:` — приводится
  выводом.
- Формулировка `must_have` третьей записи совпадает с формулировкой двух действующих ПОСИМВОЛЬНО:
  сличение приводится в сводке (например, счётом вхождений этой формулировки в файле — их обязано
  стать три при двух до правки).
- Поле `covers` третьей записи выписано так, что его нельзя прочесть шире ответа: в нём назван либо
  перечень строк и обработчиков (область `узко`), либо класс правки ВМЕСТЕ с явным указанием, чего
  он НЕ покрывает (область `широко`).
- Все пять идентификаторов находок UI-ревизии присутствуют в новом разделе: `grep -o "UI-[3-7]"` по
  участку файла от заголовка `## План 10-57` до якоря даёт пять РАЗЛИЧНЫХ значений.
- У каждого из пяти подразделов назван владелец, и `UI-3` несёт отдельное предложение о том, что
  орган снятия его НЕ ЧИНИТ — положительный греп.
- Новый раздел ссылается на раздел `## План 10-55` — положительный греп.
- Идентификатор заведённой записи журнала окон приведён в сводке, и запись заведена КОМАНДОЙ ведения
  (вывод команды приводится).
- В сводке приведено равенство ЧИСЛОМ: шестнадцать внеобластных находок найдено, шестнадцать
  распоряжено (одиннадцать + пять), и ни одна не осталась ни в задаче, ни в записи.
  </acceptance_criteria>

  <verify>
    <human-check>
⚠️ ДОЛГ ЧЕЛОВЕКА, ЗАПИСАННЫЙ, А НЕ ЗАКРЫТЫЙ. Настоящая партия сдвинула И стопку заготовок (план
10-56), И выход у плашки (настоящий план), поэтому ДВА шага обхода `10-UAT.md` требуют СВЕЖЕГО
прохода, и машина их закрыть не вправе:
- **шаг 2.8** — прямоугольник плашки обрыва связи при рабочем (ненулевом) положении прокрутки: шесть
  величин по приписке `10-UAT.md:1224-1564`;
- **шаг 4.4** — плашка поверх ОТКРЫТОЙ панели подтверждения: четыре величины ПЛЮС ЧИСЛО ВИДИМЫХ
  ЗАГОТОВОК, и к ним прибавляется НОВЫЙ предмет — работает ли орган снятия мышью и с клавиатуры, и
  возвращается ли плашка на следующем отказе.

Основание непокрытия перезамерено: суита читает ОБЪЯВЛЕНИЯ таблицы стилей и исполняет сценарий на
СТАБАХ узлов; движка раскладки нет, браузерного привода нет ни одного
(`playwright|selenium|puppeteer|splinter` → 0 вхождений по дереву). Правило об объявлении и шаг о
прямоугольнике — РАЗНЫЕ предметы, и подмена одного другим уже стоила фазе окна 77.

⚠️ И ГЛАВНОЕ, ЧЕГО НЕ ЗАКРЫВАЕТ НИ ОДИН ПЛАН ЭТОЙ ПАРТИИ: **ПОДПИСЬ ЧЕЛОВЕКА на приёмке обхода
`10-UAT.md`.** Ею и ТОЛЬКО ею закрывается `FORM-06` — флажок `.planning/REQUIREMENTS.md:31` пуст,
клетка `:136` несёт `Gaps Found`, и ни один символ партии их не двигает. Ответ владельца на останове
задачи 1 этой подписью НЕ ЯВЛЯЕТСЯ. Открытыми также остаются решения владельца по 285 запретам фазы
и по двенадцати угрозам `10-SECURITY.md` (область отказа `phase-10-only`).
    </human-check>
  </verify>

  <done>
Третья запись блока `overrides` стои́т рядом с двумя действующими, ни одна из них не тронута, поля
вердикта круга не сдвинуты; пять находок UI-ревизии распоряжены поимённо с названным владельцем в
разделе перед якорем; запись журнала о долге свежего прохода и о принятой цене заведена командой;
`tests/test_planning/` зелен; `FORM-06` не тронут.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| решение владельца → форма правки | между ответом человека и строками, которые ложатся в дерево, стои́т исполнитель, и всякая его вольность становится решением, которого владелец не принимал |
| решение владельца → запись отступления | принятая запись читается следующими кругами и следующими фазами как ПРАВИЛО ПРОЕКТА и переживает партию, поэтому её ОБЛАСТЬ есть решение о вехе, а не о правке |
| план закрытия гэпов → вердикт круга | шапка отчёта несёт и записи, вносимые планами, и поля вердикта, принадлежащие кругу; граница между ними не принуждается ничем, кроме перечня запретов плана |
| человек → канал видимости аварии | орган снятия есть единственное действие, которым человек убирает сообщение; орган, отключающий канал до конца сессии, обменивает видимую помеху на ТИШИНУ |
| объявление таблицы стилей → скрытость заготовок | скрытость держит атрибут через таблицу стилей пользовательского агента, и любое показывающее объявление автора её перебивает В ЛЮБОМ ОРИГИНЕ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-57-01 | Elevation of Privilege | исполнитель, выбравший ветвь за владельца, потому что одна кажется очевидной | high | mitigate | `checkpoint:decision` с `gate="blocking-human"` ПЕРЕД задачей правки; автопродвижение на гейт не действует ни в одном режиме; акцептанс требует идентификатора ветви первой строкой сводки; неисполненные ветви в дерево не попадают ни строкой |
| T-10-57-02 | Tampering | объявленное число регистраций, поправленное исполнителем под найденное | high | mitigate | запрет выписан отдельной записью; сдвиг числа допускается ТОЛЬКО по ответу владельца и ТОЛЬКО с летописью основания рядом; акцептанс сличает греп по файлу с объявлением и требует обоих чисел в сводке |
| T-10-57-03 | Spoofing | запись отступления, охват которой шире принятого владельцем | high | mitigate | `covers` заполняется ОТВЕТОМ на уточняющий вопрос `<resume-signal>` об ОБЛАСТИ; для области `широко` требуется явное указание, чего запись НЕ покрывает; форма наследуется у двух действующих записей поле в поле |
| T-10-57-04 | Tampering | правка полей вердикта отчёта планом (`status`, `score`, `overrides_applied`) | high | mitigate | перечень непрáвимого выписан запретом; акцептанс требует предъявления диффа отчёта без изменённых строк этих полей |
| T-10-57-05 | Denial of Service | снятая плашка не возвращается: следующая авария остаётся человеку НЕ ПОКАЗАННОЙ | high | mitigate | правило возврата плашки исполнением сценария на стабах; запрет на превращение снятия в отключение канала; для ветви `C` цена называется владельцу ДО выбора и фиксируется правилом, а не умалчивается |
| T-10-57-06 | Denial of Service | показывающее объявление способа отображения у узла заготовки даёт пустые подложки на каждом экране | high | mitigate | правило значений способа отображения по ВСЕМ блокам, достигающим узла заготовки, плюс отрицательный контроль; действующее правило блока подъёма остаётся в силе и не ослабляется |
| T-10-57-07 | Tampering | правка либо откат третьего обработчика как побочное следствие | high | mitigate | запрет выписан отдельной записью; акцептанс требует отсутствия изменённых строк в диапазоне его тела и зелени двух его правил с двумя контролями |
| T-10-57-08 | Information Disclosure | снятие одной плашки уносит вторую, и человек теряет непрочитанную инструкцию | medium | mitigate | правило области снятия печатает полученный селектор дословно; орган адресует СВОЙ узел заготовки |
| T-10-57-09 | Repudiation | RED, объявленный по ненулевому коду возврата при неисполненном правиле | high | mitigate | акцептанс требует ДОСЛОВНЫХ строк отказов двух правил с полученными значениями; кода возврата одного недостаточно, и это записано лессоном партии |
| T-10-57-10 | Denial of Service | раздел отложенного, дописанный в конец файла: второй прогон удвоил бы его | medium | mitigate | вставка ПЕРЕД якорем, ровно один раздел на план; принуждение — `tests/test_planning/test_deferred_items_are_idempotent.py`, и каталог назван в команде ЯВНО |
| T-10-57-11 | Repudiation | одна из шестнадцати внеобластных находок, не попавшая ни в задачу, ни в запись | medium | mitigate | равенство «шестнадцать найдено = шестнадцать распоряжено» приводится ЧИСЛОМ в сводке; пять идентификаторов считаются грепом по участку раздела; ссылка на раздел плана 10-55 связывает два обрывка в одно распоряжение |
| T-10-57-12 | Elevation of Privilege | машинная отметка `FORM-06` выполненным либо прочтение ответа владельца как приёмки обхода | high | mitigate | запрет выписан отдельной записью; `<resume-signal>` называет владельцу ПРЯМО, чего его ответ не закрывает; акцептанс требует ПУСТОГО диффа `.planning/REQUIREMENTS.md` и `10-UAT.md`; принуждение — два правила `tests/test_planning/` |
| T-10-57-13 | Repudiation | запись журнала окон, внесённая руками в обход команды ведения: счёта шапки разошлись бы молча | medium | mitigate | запись заводится командой ведения с явными параметрами; идентификатор записи приводится в сводке |
| T-10-57-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера |

⚠️ ДВЕНАДЦАТЬ ОТКРЫТЫХ УГРОЗ `10-SECURITY.md` НАСТОЯЩИМ ПЛАНОМ НЕ ПЕРЕСМАТРИВАЮТСЯ: область отказа
объявлена `phase-10-only`, решение принадлежит владельцу и стои́т отдельным пунктом перечня
человеческой проверки двенадцатого круга.
</threat_model>

## Artifacts this phase produces

**Новые узлы и имена разметки (`app/templates/includes/htmx_error_banner.html`):**
- орган снятия внутри КАЖДОГО из двух узлов заготовок — свой класс и доступное имя на русском
  (имя класса подставляет исполнитель и называет в сводке)

**Новые блоки `app/static/css/app.css`:**
- внешний вид органа снятия (положение внутри поднятой заготовки, снятие вида по умолчанию, знак
  генерируемым содержимым, видимый признак фокуса)
- скрытие заготовки, чей орган снятия приведён в действие (селектор по КЛАССУ стопки, значение
  способа отображения — «нет»)

**Новые символы правил (`tests/test_pages/test_shell.py`):**
- `test_every_failure_banner_carries_a_dismiss_control`
- `test_dismissing_one_banner_leaves_the_other_shown`
- `test_a_dismissed_banner_returns_on_the_next_failure`
- `test_no_banner_rule_declares_a_display_mode_that_shows`
- `test_control_a_banner_without_a_dismiss_control_reddens`
- `test_control_a_showing_display_mode_on_a_banner_reddens`
- функция находок органа снятия, принимающая путь (вызывается и правилами, и контролями)
- **ТОЛЬКО ДЛЯ ВЕТВИ `B`:** сдвинутое значение `FAILURE_BANNER_HANDLERS_MEASURED` (3 → 4) с
  летописью основания и ЧЕТВЁРТОЕ имя в конце `FAILURE_BANNER_EVENTS`

**Новые записи:**
- ТРЕТЬЯ запись блока `overrides` в `10-VERIFICATION.md` (ветви `A`/`B`)
- раздел `## План 10-57` в `deferred-items.md` с пятью подразделами `UI-3`…`UI-7`
- запись `.planning/WINDOWS.md` о долге свежего прохода шагов 2.8 и 4.4 и о принятой цене

**НОВЫХ ФАЙЛОВ, НОВЫХ ИДЕНТИФИКАТОРОВ ЗАГОТОВОК, НОВЫХ УЗЛОВ КЛИЕНТСКОГО СОСТОЯНИЯ, НОВЫХ АТРИБУТОВ
СОБЫТИЙ РАНТАЙМА РАЗМЕТКИ И НОВЫХ ФУНКЦИЙ ПРОДУКТА ПЛАН НЕ ЗАВОДИТ НИ ОДНОГО. НОВАЯ РЕГИСТРАЦИЯ
ОБРАБОТЧИКА ПОЯВЛЯЕТСЯ ТОЛЬКО В ВЕТВИ `B` И ТОЛЬКО ПО ОТВЕТУ ВЛАДЕЛЬЦА.**

<verification>
1. `uv run pytest tests/test_pages/test_shell.py -q` — зелено, число `passed` не меньше 240.
2. `uv run pytest tests/test_templates/ tests/test_planning/ tests/test_pages/test_confirm_delete_transport.py`
   — зелено (замер на HEAD: 323 passed).
3. `uv run pytest tests/test_templates/test_components.py -k criterion_three -q` — зелено, и в
   выводе НЕТ ни одной строки «УТВЕРЖДЕНИЕ N ИЗ 5».
4. `uv run python -m compileall -q app main.py tests` — без отказов.
5. `git diff "$PLAN_BASE"..HEAD -- .planning/REQUIREMENTS.md
   .planning/phases/10-rychag-components-modal-html/10-UAT.md
   .planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md` — ПУСТ.
6. `git diff "$PLAN_BASE"..HEAD -- app/pages app/routes app/services alembic` — ПУСТ (предмет плана 10-55).
7. Идентификатор исполненной ветви, её область, оба числа регистраций и идентификатор записи журнала
   приведены в сводке.

⚠️ ПОЛНАЯ СУИТА (`just test`, замер 2026-09-12: 3216 passed, ~39:27) в петлю обратной связи задач НЕ
ВХОДИТ по длительности и снимается один раз гейтом оркестратора. При её прогоне помнить:
`tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard`
НЕДЕТЕРМИНИРОВАН ПО ВРЕМЕНИ СУТОК (скользящее окно 24 ч против календарного дня) и отложен решением
владельца — его краснота настоящей партии НЕ ПРИНАДЛЕЖИТ, и называть её надлежит именем правила и
причиной, а не отказом плана.
</verification>

<success_criteria>
- Владелец назвал ветвь и область; правка исполнена РОВНО по названной ветви, и её идентификатор
  стои́т первой строкой сводки.
- У каждой заготовки есть достижимый с клавиатуры орган снятия с доступным именем; снятие убирает
  свою заготовку и не трогает вторую.
- Для ветвей `A` и `B` снятая плашка возвращается на новом отказе; для ветви `C` цена
  зафиксирована правилом, а не умолчана.
- Единственное значение способа отображения во всех блоках, достигающих узла заготовки, — «нет».
- Все пять чисел гейта критерия 3 равны объявленным; объявление сдвинуто только по ответу владельца
  и только с летописью основания рядом.
- Третий обработчик не тронут ни на символ; блок подъёма и его селектор не тронуты.
- Третья запись блока `overrides` стои́т рядом с двумя действующими; поля вердикта круга не сдвинуты.
- Шестнадцать внеобластных находок распоряжены поимённо: одиннадцать разделом плана 10-55, пять
  разделом настоящего плана.
- Долг свежего прохода шагов 2.8 и 4.4 записан журналом; `FORM-06` не отмечен; `10-UAT.md` не тронут.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-57-SUMMARY.md` when done
</output>
