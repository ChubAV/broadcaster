---
phase: 10-rychag-components-modal-html
plan: 16
subsystem: testing
tags: [uat, broken-windows, htmx, hx-location, nyquist, gap-closure]

# Dependency graph
requires:
  - phase: 10-12
    provides: "граница ID_MAX на страничном модуле расписаний и ДОСЛОВНЫЙ текст окна о непокрытых соседних страничных модулях, выписанный в сводку для переноса владельцем реестра"
  - phase: 10-13
    provides: "обёртка инлайн-скрипта редактора немедленно вызываемым выражением (машинная половина CR-03), шесть правил двойного исполнения и ДОСЛОВНЫЙ текст окна об экране подключения"
  - phase: 10-10
    provides: "артефакт ручного обхода 10-UAT.md — шесть проверок, шесть пустых таблиц отметок, перечень закрытого машинно"
  - phase: 10-14
    provides: "правило, краснящее перевод требования в Complete раньше вердикта отчёта верификации своей фазы"
provides:
  - "переходная ветка проверки 1 спрашивает о ЖИВОСТИ клиентского слоя тремя наблюдаемыми признаками (шаги 1.13-1.15), а не только о смене экрана"
  - "летопись изменившегося ожидания: прежнее «здесь ожидается ОТКАЗ» помечено опровергнутым для сегодняшнего дерева и НЕ вычеркнуто, источник расхождения назван поимённо"
  - "машинная половина CR-03 в перечне закрытого машинно ВМЕСТЕ со своей границей доказанного"
  - "раздел «Чего третья партия закрытия гейпов НЕ закрыла» — три пункта, у каждого своё основание"
  - "четыре окна реестра (45, 46, 47, 48), дописанные ОДНИМ владельцем; два из них — чужие тексты, перенесённые дословно"
affects: [phase-11, phase-15, приёмка Фазы 10, повторная верификация Фазы 10]

# Actuals (#2632)
actuals:
  tokens: 48634
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - "ОДИН НАЗНАЧЕННЫЙ ВЛАДЕЛЕЦ ОБЩЕГО ДОПИСЫВАЕМОГО РЕЕСТРА НА ПАРТИЮ: `.planning/WINDOWS.md` объявлен в `files_modified` ровно одного плана партии; проверяется счётом планов, объявивших файл"
    - "ЧУЖОЙ ТЕКСТ ОКНА ПЕРЕНОСИТСЯ ИЗВЛЕЧЕНИЕМ ИЗ СВОДКИ, А НЕ ПЕРЕПЕЧАТКОЙ: совпадение утверждается сличением длин и байтов, а не глазом"
    - "ЧИСЛА ОКНА СНИМАЮТСЯ СВОИМ ПРОГОНОМ ЗОНДА, А НЕ ЦИТИРУЮТСЯ ИЗ ПЛАНА"

key-files:
  created:
    - .planning/phases/10-rychag-components-modal-html/10-16-SUMMARY.md
  modified:
    - .planning/phases/10-rychag-components-modal-html/10-UAT.md
    - .planning/WINDOWS.md

key-decisions:
  - "`FORM-06` в завершённое НЕ переводится и `.planning/REQUIREMENTS.md` не тронут ни байтом: отчёт верификации фазы читает `gaps_found`, и правило плана 10-14 покраснело бы на такой отметке. Штатный шаг исполнителя `requirements.mark-complete` пропущен НАМЕРЕННО — это не обход правила, а его основание"
  - "`requirements-completed` во фронтматтере оставлен ПУСТЫМ, а объявленное планом требование вынесено в отдельное поле `requirements-declared`: механический перенос `FORM-06` в поле с именем «завершённые» дал бы запись ШИРЕ дерева — ровно тот дефект, которым фаза провалена первым кругом (коммит `0ea886d`)"
  - "Пятое окно НЕ заводится, хотя человеческая половина обхода остаётся непройденной: команда `<verify>` плана утверждает РОВНО `48` строк реестра и краснеет на большем («в реестр писал кто-то ещё»). Непройденный обход записан там, где его прочтут, — шапкой `10-UAT.md`, разделом «Куда артефакт уезжает дальше» и перечнем `human_verification` отчёта верификации"
  - "Шаг 1.15 назван НЕПРИМЕНИМЫМ по умолчанию и способ сделать его применимым записан: объявление посевного стенда создаётся с `images=[]`, поэтому вердикта по снятию вложения без прикреплённого файла быть не может — умолчание записано, а не оставлено человеку на догадку"
  - "Признаком живости в шаге 1.15 выбрана НЕ пропажа плитки сама по себе, а ОТСУТСТВИЕ ПЕРЕЗАГРУЗКИ: серверная плитка несёт `type=\"submit\"`, и мёртвый скрипт даёт визуально похожее исчезновение через POST — признак, не различающий живое и мёртвое, обесценил бы шаг"

patterns-established:
  - "Владелец общего реестра назначается решением с записанным основанием и проверяется машинно (счёт планов партии, объявивших файл), а не соблюдается по договорённости"
  - "Опровергнутое ожидание ставится РЯДОМ с новым и помечается, а не стирается; при этом прямо сказано, что прежнее ожидание НЕ БЫЛО ошибкой (идиома D-30/D-32)"

requirements-declared: [FORM-06]
requirements-completed: []  # ПУСТО НАМЕРЕННО — см. key-decisions и раздел «Требования»

coverage:
  - id: D1
    description: "Переходная ветка проверки 1 обхода спрашивает о живости клиентского слоя тремя шагами (1.13 консоль, 1.14 счётчик символов, 1.15 снятие вложения) с названным ожидаемым наблюдаемым признаком у каждого"
    requirement: FORM-06
    verification:
      - kind: other
        ref: "grep -c '^| 1\\.1[3-5] ' .planning/phases/10-rychag-components-modal-html/10-UAT.md → 3"
        status: pass
    human_judgment: false
  - id: D2
    description: "Летопись изменившегося ожидания переходной ветки: прежнее ожидание отказа приведено дословно, помечено опровергнутым для сегодняшнего дерева, источник расхождения назван поимённо (правка 10-13, be81324), прямо сказано «прежнее не было ошибкой», назван смысл шагов после правки"
    verification:
      - kind: other
        ref: "git show 6191349 -- .planning/phases/10-rychag-components-modal-html/10-UAT.md (пять обязательных частей приведены в теле сводки)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Машинная половина CR-03 внесена в перечень закрытого машинно вместе со своей ГРАНИЦЕЙ: доказана форма исходника и его поведение в интерпретаторе, поведение рантайма браузера — нет"
    verification:
      - kind: other
        ref: "имена семи правил сличены с 10-13-SUMMARY.md; строка несёт все три столбца таблицы"
        status: pass
    human_judgment: false
  - id: D4
    description: "Раздел «Чего третья партия закрытия гейпов НЕ закрыла» — три пункта с основанием у каждого; утверждение о браузерных зависимостях перепроверено исполнителем"
    verification:
      - kind: other
        ref: "grep -c 'Чего третья партия закрытия гейпов НЕ закрыла' … → 1; grep -cE 'playwright|selenium|puppeteer' pyproject.toml → 0"
        status: pass
    human_judgment: false
  - id: D5
    description: "Четыре окна реестра дописаны ОДНИМ владельцем (45, 46, 47, 48); тексты окон 45 и 46 перенесены из сводок планов 10-12 и 10-13 ДОСЛОВНО"
    verification:
      - kind: other
        ref: "grep -cE '^\\| [0-9]+ \\| ' .planning/WINDOWS.md → 48; grep -c '^total_count: 48$' → 1; побайтовое сличение описаний окон 45/46 с исходными абзацами сводок"
        status: pass
    human_judgment: false
  - id: D6
    description: "Артефакт по-прежнему ничего не самозаверяет: status: testing, checks_declared: 6, все шесть таблиц отметок пусты, REQUIREMENTS.md не тронут"
    verification:
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py (в составе 2826 passed)"
        status: pass
    human_judgment: false
  - id: D7
    description: "Фактическое снятие осиротевшей панели РАНТАЙМОМ разметки после N удалений — критерий 2 роадмапа, вторая половина цели фазы"
    requirement: FORM-06
    verification: []
    human_judgment: true
    rationale: "Машинного пути закрыть применение внеполосного узла рантаймом у проекта НЕТ: суита идёт через транспорт без процесса и не исполняет ни строчки JS, браузерных зависимостей в дереве нет ни одной (перепроверено). Наблюдается только человеком на приёмке фазы"
  - id: D8
    description: "Клиентский слой редактора после перехода по заголовку HX-Location жив: консоль чиста, счётчик символов отзывается, вложение снимается на месте (шаги 1.13-1.15)"
    requirement: FORM-06
    verification: []
    human_judgment: true
    rationale: "Переисполнение скрипта и его последствия наблюдаются только рантаймом браузера. Зелёное правило плана 10-13 доказывает ФОРМУ исходника, а не поведение рантайма — граница названа в самой строке перечня закрытого машинно"

# Metrics
duration: 42 min
completed: 2026-09-04
status: complete
---

# Phase 10 Plan 16: Живость клиентского слоя в обходе, чего партия не закрыла, и четыре окна одним владельцем — Summary

**Переходная ветка обхода `10-UAT.md` перестала спрашивать только о смене экрана и получила три шага, наблюдающих ровно то, что умирало по `CR-03`; прежнее ожидание отказа помечено опровергнутым с названным источником; в реестр окон дописаны четыре записи (45-48) ОДНИМ назначенным владельцем, из них две — чужие тексты, перенесённые побайтово.**

## Performance

- **Duration:** 42 min
- **Started:** 2026-09-04T17:02:06Z
- **Completed:** 2026-09-04T17:44:44Z
- **Tasks:** 2
- **Files modified:** 2 (плюс созданная сводка)

## Accomplishments

- Переходная ветка проверки 1 спрашивает о **живости** тремя наблюдаемыми признаками — это единственное, чем глаз проверяет ровно то, что чинил план 10-13.
- Изменение ожидания записано **летописью**, а не подменой: обе формулировки стоя́т рядом, источник расхождения назван поимённо.
- Машинная половина `CR-03` внесена в перечень закрытого машинно **вместе с границей доказанного** — зелёное правило не читается как закрытие критерия 2.
- Названо поимённо, **чего третья партия НЕ закрыла**, с основанием у каждого пункта.
- Реестр окон вырос **ровно на четыре строки** (44 → 48) рукой одного владельца; долг по направлению отказа записан числами СВОЕГО прогона зонда.

## Task Commits

1. **Задача 1: переходная ветка обхода спрашивает о живости клиентского слоя, летопись изменившегося ожидания** — `6191349` (docs)
2. **Задача 2: чего третья партия НЕ закрыла + четыре окна партии одним владельцем** — `aae7e49` (docs)

**Plan metadata:** см. коммит `docs(10-16): complete …` ниже по истории.

## Файлы

- `.planning/phases/10-rychag-components-modal-html/10-UAT.md` — три шага переходной ветки, колонка отметок, летопись ожидания, строка перечня закрытого машинно, раздел «чего партия не закрыла»
- `.planning/WINDOWS.md` — четыре новые записи (45, 46, 47, 48); счётчики шапки правит инструмент, а не рука

---

## 1. Три новых шага — текст приведён целиком

| # | Шаг | Ожидаемый наблюдаемый признак (сокращённо; в артефакте — полностью) |
|---|---|---|
| 1.13 | **КОНСОЛЬ ПРИШЕДШЕГО ЭКРАНА.** Сразу после перехода, НЕ перезагружая документ, прочитать консоль целиком | ранней ошибки языка о повторном объявлении имени **НЕТ**; что искали бы до правки — названо дословно: `SyntaxError: Identifier 'IMAGE_BASE_URL' has already been declared` (`10-REVIEW.md:231`); если строка ЕСТЬ — выписать дословно, наблюдения 1.14-1.15 к предмету уже не относятся |
| 1.14 | **СЧЁТЧИК СИМВОЛОВ.** Поставить курсор в поле текста и напечатать несколько символов подряд | число слева в линейке `#text-counter` **МЕНЯЕТСЯ на КАЖДЫЙ символ**; признак отказа — число стои́т |
| 1.15 | **СНЯТИЕ ВЛОЖЕНИЯ.** Если у объявления ЕСТЬ вложения — нажать крестик на плитке | плитка исчезает **НА МЕСТЕ**, документ **НЕ перезагружается**; вложений нет — шаг помечается **НЕПРИМЕНИМЫМ** и это ЗАПИСЫВАЕТСЯ |

### Имена узлов ВЗЯТЫ ИЗ ШАБЛОНА ЧТЕНИЕМ — файл, строка и способ проверки

| Узел, названный шагом | Файл : строка | Чем проверено |
|---|---|---|
| `#text` — поле текста объявления | `app/templates/ads/form.html:91` (вызов `textarea_field(name='text', …)`) + `app/templates/components/field.html:31,34` (`field_id = id or name` → `id="text"`) | чтение шаблона и макроса; сходится с `document.getElementById('text')` в теле скрипта, `ads/form.html:371` |
| `#text-counter` — линейка счётчика | `app/templates/ads/form.html:99` (`<span class="counter…" id="text-counter">`) | чтение; сходится с `ads/form.html:370` |
| подключение счётчика | `app/templates/ads/form.html:516-518` (`textArea.addEventListener('input', updateCounter)`) | чтение хвоста скрипта |
| `.media-tile__remove` — крестик снятия вложения | `app/templates/ads/form.html:116` (серверная плитка, `type="submit" name="remove_image"`), `:413-420` (плитка, создаваемая скриптом) | чтение обеих ветвей |
| `renderImages()` последней строкой тела | `app/templates/ads/form.html:537` | чтение хвоста; отсюда и различающий признак шага 1.15 |
| перехват `event.preventDefault()` на крестике | `app/templates/ads/form.html:423` | чтение |
| плитка добавления «+ ФАЙЛ» / `#file-input` | `app/templates/ads/form.html:121` / `:161` | чтение |
| обёртка плана 10-13 | `app/templates/ads/form.html:308` (`(function () {`) / `:538` (`})();`) | чтение |

⚠️ **ЗАМЕЧЕНО ПРИ ЧТЕНИИ И ЗАПИСАНО В САМ ШАГ, А НЕ ОСТАВЛЕНО ЧЕЛОВЕКУ.** Объявление посевного стенда создаётся с `images=[]` (команда посева, раздел «Как привести стенд в нужное состояние»), поэтому шаг 1.15 по умолчанию НЕПРИМЕНИМ. В текст шага вписан способ сделать его применимым: прикрепить ОДИН файл плиткой «+ ФАЙЛ» ДО шага 1.10. Без этой записи человек либо пролистал бы шаг молча, либо записал бы отказ, которого нет.

## 2. Летопись изменившегося ожидания — все пять обязательных частей

Текст, дописанный в проверку 1 перед таблицей шагов (приведён с сохранением выделений):

> ⚠️ **ОЖИДАНИЕ НА ПЕРЕХОДНОЙ ВЕТКЕ (ШАГИ 1.10-1.15) ИЗМЕНИЛОСЬ, И ПРЕЖНЕЕ ПОМЕЧЕНО ОПРОВЕРГНУТЫМ, А НЕ ВЫЧЕРКНУТО.**
>
> **Прежнее ожидание — дословно.** Отчёт верификации третьего круга ожидал на этом шаге не подтверждение, а ОТКАЗ: «⚠️ ВЕРИФИКАТОР ОЖИДАЕТ ЗДЕСЬ ОТКАЗ: по цепи, установленной в `CR-03`, ожидается `SyntaxError: Identifier 'IMAGE_BASE_URL' has already been declared` и мёртвые загрузка изображений, счётчик символов и снятие вложений» (`10-VERIFICATION.md`, второй пункт перечня `human_verification`, строки 186-191).
>
> **Сегодня это ожидание ОПРОВЕРГНУТО ДЛЯ СЕГОДНЯШНЕГО ДЕРЕВА, и источник расхождения назван поимённо:** правка **плана 10-13** (коммит `be81324`) обернула инлайн-скрипт редактора немедленно вызываемым выражением-функцией (`app/templates/ads/form.html:308` — `(function () {`, `:538` — `})();`), и ни одного из восемнадцати объявлений верхнего уровня в области имён документа больше не живёт.
>
> **ПРЕЖНЕЕ ОЖИДАНИЕ НЕ БЫЛО ОШИБКОЙ.** Оно было ВЕРНЫМ для дерева, на котором писалось: на HEAD круга верификации скрипт действительно падал ранней ошибкой языка при переисполнении, и верификатор измерил это своими руками. Правится не оно, а ожидание, которое оно ПЕРЕЖИЛО, — и обе формулировки стоя́т рядом ровно затем, чтобы читатель видел, ЧТО изменилось и ЧЕМ (идиома проекта D-30/D-32 `STATE.md`; той же формой фаза уже записала летописи «16 → 18» и «7/7 → 11/11»).
>
> **СМЫСЛ ШАГОВ ПОСЛЕ ПРАВКИ.** Шаги 1.13-1.15 предъявляют теперь не ОТКАЗ, а **ЖИВОСТЬ**, и потому остаются нужными: зелёное машинное правило доказывает ФОРМУ ИСХОДНИКА и его поведение в интерпретаторе, а не поведение рантайма браузера. Ветка, спрашивающая ТОЛЬКО о смене экрана, пропустила бы `CR-03` второй раз — ровно так регрессия и дожила до верификации, имея готовый шаг 1.10, идущий прямо по сломанной ветке.

Пять обязательных частей: (1) прежнее ожидание дословно — абзац 2; (2) пометка опровергнутости — заголовок и абзац 3; (3) источник расхождения поимённо — абзац 3 (план 10-13, коммит `be81324`, строки шаблона); (4) прямое «прежнее не было ошибкой» — абзац 4; (5) смысл шагов после правки — абзац 5.

## 3. Строка перечня закрытого машинно и СЛИЧЕНИЕ ИМЁН ПРАВИЛ

Дописанная строка (три столбца, как у соседей):

- **Утверждение:** «**Машинная половина `CR-03`:** исходник инлайн-скрипта экрана редактора переживает ВТОРОЕ исполнение в одной области имён (обёртка немедленно вызываемым выражением, `ads/form.html:308` и `:538`, план **10-13**), и НИ ОДНА из целей заголовка перехода `HX-Location` не несёт объявлений верхнего уровня в инлайн-скрипте».
- **Правила:** `test_the_first_execution_of_the_editor_script_succeeds`, `test_the_editor_script_survives_a_second_execution_in_the_same_realm`, `test_a_reinstated_top_level_binding_reddens_the_double_execution_rule`, `test_no_transition_destination_declares_a_top_level_binding_inline`, `test_a_reinstated_top_level_binding_reddens_the_static_rule`, `test_the_static_rule_does_not_cancel_itself`, `test_the_connect_screen_is_not_a_transition_destination_today`.
- **ГРАНИЦА доказанного:** «Доказана ФОРМА ИСХОДНИКА и его поведение В ИНТЕРПРЕТАТОРЕ (двойное исполнение одного источника плюс обе мутации). **НЕ доказано, что рантайм БРАУЗЕРА ведёт себя по цепи, установленной `CR-03`, и что человек видит ЖИВОЙ клиентский слой, — это шаги 1.13-1.15**, и зелёное правило их не заменяет».

**Сличение написаний имён — оба приведены, расхождения нет.** Имена сняты со сводки командой
`grep -oE 'test_[a-z_0-9]+' .planning/phases/10-rychag-components-modal-html/10-13-SUMMARY.md | sort -u`
и перенесены посимвольно:

| написание в `10-13-SUMMARY.md` | написание в строке перечня |
|---|---|
| `test_the_first_execution_of_the_editor_script_succeeds` | `test_the_first_execution_of_the_editor_script_succeeds` |
| `test_the_editor_script_survives_a_second_execution_in_the_same_realm` | `test_the_editor_script_survives_a_second_execution_in_the_same_realm` |
| `test_a_reinstated_top_level_binding_reddens_the_double_execution_rule` | `test_a_reinstated_top_level_binding_reddens_the_double_execution_rule` |
| `test_no_transition_destination_declares_a_top_level_binding_inline` | `test_no_transition_destination_declares_a_top_level_binding_inline` |
| `test_a_reinstated_top_level_binding_reddens_the_static_rule` | `test_a_reinstated_top_level_binding_reddens_the_static_rule` |
| `test_the_static_rule_does_not_cancel_itself` | `test_the_static_rule_does_not_cancel_itself` |
| `test_the_connect_screen_is_not_a_transition_destination_today` | `test_the_connect_screen_is_not_a_transition_destination_today` |

## 4. Таблица отметок проверки 1 — колонка прибавлена, строка ПУСТА

```
| Дата | Браузер / ОС | 1.1-1.2 числа ДО | 1.5 числа ПОСЛЕ | 1.6 оба узла | 1.7 ловушка фокуса | 1.8 линейка | 1.10-1.12 переход | 1.13-1.15 клиентский слой | Исход |
|---|---|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |  |  |
```

**ТРИ УДАЛЁННЫЕ СТРОКИ ПРАВКИ НАЗВАНЫ ПОИМЁННО, КАК ТРЕБУЕТ КРИТЕРИЙ ПРИЁМКИ.** Команда
`git diff -- …10-UAT.md | grep -c '^-'` дала на задаче 1 **`4`** (порог — «больше `4`»), и это:

1. заголовок диффа `--- a/.planning/phases/10-rychag-components-modal-html/10-UAT.md` (не строка артефакта);
2. прежняя **шапка** таблицы отметок проверки 1 (без колонки `1.13-1.15 клиентский слой`);
3. прежняя **строка-разделитель** таблицы (девять колонок вместо десяти);
4. прежняя **пустая строка отметок** (девять пустых клеток вместо десяти).

Всё остальное правки — чистая вставка. Ни одна строка содержания не удалена; прибавление колонки удаления трёх строк требует по устройству формата.

## 5. Шапка артефакта не тронута — вывод трёх проверок состояния

| проверка | команда | вывод |
|---|---|---|
| состояние обхода | `grep -c '^status: testing$' …10-UAT.md` | `1` |
| число проверок | `grep -c '^checks_declared: 6$' …10-UAT.md` | `1` |
| комментарий о запрете `passed` | чтение строк 11-14 шапки | стои́т дословно: «Значение `status: passed` до этого ставить ЗАПРЕЩЕНО» |
| отметок заполнено | `grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' …10-UAT.md` | `0` |

## 6. Раздел «Чего третья партия закрытия гейпов НЕ закрыла» — три пункта

1. **Критерий 2 роадмапа остаётся ЧЕЛОВЕЧЕСКИМ.** Основание: суита идёт через транспорт без процесса и не исполняет ни строчки разметочного рантайма — доказано ПРИСУТСТВИЕ парных внеполосных узлов, применение их рантаймом нет; машинного пути закрыть это у проекта НЕТ; браузерная зависимость ОТВЕРГНУТА с основанием (решение ВЕХИ, а не побочная правка партии; введённая наспех — новый класс непроверенного вместо закрытия старого). Закрывается ПРОХОЖДЕНИЕМ проверки 1.
2. **Два ребра зонда покрытия остаются неразрешёнными, и это названо.** Ребро `ordering` — предмет есть ПОРЯДОК применения трёх внеполосных узлов рантаймом, наблюдаем только рантаймом, ни одно правило проекта на него не опирается, критерий без проверяющего был бы записью шире дерева. Ребро `concurrency` — адресовано частично (гейт плана 10-07 плюс проверки 2 и 3 обхода), полного критерия «два подтверждённых удаления в полёте одновременно» в суите без процесса выразить нечем. Остальные три ребра разрешены: `adjacency` и `idempotency` — планом 10-12, `empty` — планом 10-13.
3. **Восьмое измерение Найквиста у фазы НЕ ЗАКРЫТО.** У Фазы 10 нет ни `10-RESEARCH.md`, ни `10-VALIDATION.md`; режим закрытия гейпов разведку не запускает, оператор ЯВНО выбрал продолжить без неё. Записано ДВАЖДЫ и в двух адресатах — разделом артефакта и окном 47. Фаза-владелец НЕ назначена.

Отдельным абзацем — долг по направлению отказа (17 команд планов 10-01…10-07), записанный окном 48 и ТОЛЬКО окном.

### Утверждение о браузерных зависимостях ПЕРЕПРОВЕРЕНО, а не унаследовано

Прочитаны блоки зависимостей `pyproject.toml` **целиком**:

- `[project].dependencies` — **25 записей**: `aiobotocore`, `aiosmtplib`, `alembic`, `asyncpg`, `bcrypt`, `celery[redis]`, `docker`, `email-validator`, `fastapi`, `flower`, `httpx`, `jinja2`, `passlib[bcrypt]`, `pillow`, `prometheus-fastapi-instrumentator`, `pydantic-settings`, `python-jose[cryptography]`, `python-multipart`, `qrcode[pil]`, `sqlalchemy[asyncio]`, `structlog`, `telethon`, `uvicorn[standard]`, `yookassa` (и `readme`/`requires-python` вне перечня);
- `[dependency-groups].dev` — **6 записей**: `aiosqlite`, `httpx`, `maxapi-python`, `pytest`, `pytest-asyncio`, `pytest-cov`.

Браузерной зависимости среди них НЕТ НИ ОДНОЙ. Вывод команды поиска: `grep -cE 'playwright|selenium|puppeteer' pyproject.toml` → **`0`**.

## 7. ЧЕТЫРЕ ОКНА — номера, тексты, владелец

Порядок дописывания — сначала ЧУЖИЕ тексты, затем свои, чтобы потеря переданного текста обнаружилась первой. Инструментом (`gsd-tools windows append`), по одному вызову на окно; номера присвоил реестр.

### Окно 45 — чужое, текст плана 10-12 (`kind: todo`, `file: app/pages/schedules.py`, `status: open`)

> ГРАНИЦА ВЕЛИЧИНЫ ИДЕНТИФИКАТОРА ЗАКРЫТА ТОЛЬКО НА ОДНОМ СТРАНИЧНОМ МОДУЛЕ (план 10-12). ПРЕДМЕТ: общая граница `ID_MAX` и три типа-псевдонима (`ScheduleIdPath`, `AdIdForm`, `AccountIdForm`) живут в `app/pages/schedules.py` и стерегутся статическим гейтом `test_every_identifier_input_of_the_schedule_routes_carries_the_shared_bound`, который читает сигнатуры ЭТОГО ОДНОГО ФАЙЛА. СОСЕДНИЕ СТРАНИЧНЫЕ МОДУЛИ (`app/pages/ads.py`, `app/pages/accounts.py`, `app/pages/groups.py`, `app/pages/history.py` и прочие) несут идентификаторы ТОГО ЖЕ КЛАССА — приходящие адресом запроса и телом формы и уезжающие операндом сравнения SQL — и НИ ГРАНИЦЕЙ, НИ ГЕЙТОМ НЕ ПОКРЫТЫ ВОВСЕ. […] КАНДИДАТ НА ЗАКРЫТИЕ: Фаза 15 как сводный обход надёжности […] ФАЗА-ВЛАДЕЛЕЦ НЕ НАЗНАЧЕНА НАМЕРЕННО: назначение чужой фазе — решение вехи, а не плана (форма окна 34).

**Совпадение с исходным абзацем `10-12-SUMMARY.md` (раздел «ТЕКСТ ОКНА — ДОСЛОВНО, ДЛЯ ПЕРЕНОСА ПЛАНОМ 10-16», поле `description`) — ПОБАЙТОВОЕ.** Текст не перепечатывался: он ИЗВЛЕЧЁН из сводки, склеен в одну строку (реестр — таблица) и сличён программно: `got == want` → **True**, длины **1694 == 1694**. Многоточия выше — сокращение ЭТОЙ СВОДКИ ради читаемости; в реестре текст стои́т целиком.

### Окно 46 — чужое, текст плана 10-13 (`kind: todo`, `file: app/templates/accounts/connect_tg_user.html`, `status: open`)

> ЭКРАН ПОДКЛЮЧЕНИЯ TELEGRAM НЕСЁТ ОБЪЯВЛЕНИЯ ВЕРХНЕГО УРОВНЯ В ИНЛАЙН-СКРИПТЕ И СТАНЕТ УЯЗВИМ, КАК ТОЛЬКО ОКАЖЕТСЯ ЦЕЛЬЮ ЗАГОЛОВКА ПЕРЕХОДА (план 10-13). ПРЕДМЕТ: два объявления верхнего уровня — `let currentSessionId` и `let pollInterval` (app/templates/accounts/connect_tg_user.html:72-73), то есть ТА ЖЕ ФОРМА, которой экран редактора объявления падал РАННЕЙ ошибкой языка при переисполнении после свопа по `HX-Location`. […] КАНДИДАТ НА ЗАКРЫТИЕ: Фаза 11 […] ФАЗА-ВЛАДЕЛЕЦ НЕ НАЗНАЧЕНА НАМЕРЕННО: назначение чужой фазе есть решение вехи, а не плана (форма окон 34 и 41).

**Совпадение с исходным абзацем `10-13-SUMMARY.md` (раздел «ТЕКСТ ОКНА ДЛЯ РЕЕСТРА — ДОСЛОВНО, ВЛАДЕЛЬЦУ (план 10-16)», поле `description`) — ПОБАЙТОВОЕ:** `got == want` → **True**, длины **1564 == 1564**.

### Окно 47 — своё, восьмое измерение Найквиста (`kind: unrun-verify`, `file: .planning/phases/10-rychag-components-modal-html/`, `status: open`)

> ВОСЬМОЕ ИЗМЕРЕНИЕ НАЙКВИСТА У ФАЗЫ 10 НЕ ЗАКРЫТО, И ЭТО ЯВНЫЙ ВЫБОР ОПЕРАТОРА, А НЕ ПРОПУСК. ПРЕДМЕТ: у фазы нет ни `10-RESEARCH.md`, ни `10-VALIDATION.md` — замер каталога фазы даёт НОЛЬ артефактов разведки и валидации при ШЕСТНАДЦАТИ планах. ПОЧЕМУ ИХ НЕТ: три партии закрытия гейпов (10-08…10-11 и 10-12…10-16) шли режимом `gap_closure`, который разведку НЕ ЗАПУСКАЕТ, и оператор ЯВНО выбрал продолжить без неё. ЧТО ЭТО ЗНАЧИТ: измерение остаётся НЕЗАКРЫТЫМ, и ни одна из партий этого не меняет — партия закрытия гейпов закрывает НАЗВАННЫЕ гэпы, а не заводит недостающие измерения; объявлять измерение закрытым по числу исполненных планов значило бы завести запись шире дерева. ЗАПИСАНО ДВАЖДЫ И В ДВУХ АДРЕСАТАХ НАМЕРЕННО: разделом «Чего третья партия закрытия гейпов НЕ закрыла» артефакта `10-UAT.md` — для того, кто пришёл за обходом, — и настоящим окном — для того, кто ведёт журнал фазы; одного адресата не хватило бы, потому что эти двое читают разные файлы. **ФАЗА-ВЛАДЕЛЕЦ НЕ НАЗНАЧЕНА НАМЕРЕННО:** назначение чужой фазе есть решение ВЕХИ, а не плана (форма окон 34 и 41), и закрытие принадлежит владельцу вехи.

Замер, стоящий за числами окна: `ls` каталога фазы → 16 файлов `*-PLAN.md`, 15 файлов `*-SUMMARY.md` (шестнадцатая — настоящая), `ls … | grep -cE 'RESEARCH|VALIDATION'` → **`0`**.

### Окно 48 — своё, долг по направлению отказа (`kind: deviation`, `file: .planning/phases/10-rychag-components-modal-html/`, `status: open`)

> СЕМНАДЦАТЬ КОМАНД `<automated>` ПЛАНОВ 10-01…10-07 НЕ НЕСУТ СФОРМУЛИРОВАННОГО НАПРАВЛЕНИЯ ОТКАЗА, И ТРЕТЬЯ ПАРТИЯ НЕ ПРАВИТ ИХ НАМЕРЕННО. ЧИСЛА СНЯТЫ СВОИМ ПРОГОНОМ, А НЕ ПРОЦИТИРОВАНЫ (`gsd-tools check verify-failure-directions 10`): `status: blocked`, `counts.total = 60`, `counts.blocker = 17`, `counts.warning = 0`. Все СЕМНАДЦАТЬ блокеров лежат в планах 10-01…10-07 и НИГДЕ БОЛЬШЕ — 10-01: 3, 10-02: 2, 10-03: 3, 10-04: 2, 10-05: 2, 10-06: 2, 10-07: 3. ПОЧЕМУ НЕ ПРАВИТСЯ: эти планы написаны ДО появления контракта `<fails_when>`, ИСПОЛНЕНЫ и ЗАКОММИЧЕНЫ; правка требовала бы переписывания истории, а контракт, которого на момент их написания не существовало, претензией к ним не является. ЧТО ИЗМЕРЕНО ПО НОВОЙ ПАРТИИ: ТРИДЦАТЬ ОДНА команда `<automated>` планов 10-12…10-16 (10-12: 7, 10-13: 5, 10-14: 4, 10-15: 6, 10-16: 9), из них блокеров НОЛЬ […] Ориентир круга проверки называл `29` команд партии ДО правок круга; после правок число выросло до `31`, и потому оно СНЯТО прогоном, а не перенесено из плана. […] **ФАЗА-ВЛАДЕЛЕЦ НЕ НАЗНАЧЕНА НАМЕРЕННО.**

**СЛИЧЕНИЕ С ОРИЕНТИРОМ ПЛАНА — РАСХОЖДЕНИЯ, ТРЕБУЮЩЕГО ОСТАНОВА, НЕТ.** План называл ориентиром `17` блокеров при `29` командах партии ДО правок круга и прямо предписывал СНЯТЬ число команд, а не цитировать. Снято: блокеров **17** (сходится с ориентиром и с перечнем планов 10-01…10-07), команд новой партии **31** (выросло с 29 ровно так, как план и предупреждал). `10-VERIFICATION.md` отдельного раздела зонда направления отказа НЕ НЕСЁТ — сличать было не с чем, и числа взяты СВОИМ прогоном, что план и предписывает первым.

### Рост реестра — ДВА независимых замера

| замер | вывод | было |
|---|---|---|
| `grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md` | **48** | `44` (замер планировщика) |
| `grep -c '^total_count: 48$' .planning/WINDOWS.md` | **1** | `total_count: 44` |

Счётчики шапки после дописывания: `open_count: 42`, `waived_count: 1`, `fixed_count: 5`, `total_count: 48`. Два замера нужны потому, что реестр держит одно и то же в трёх местах; их согласие — признак того, что запись сделана ИНСТРУМЕНТОМ, а не рукой.

### Объявление совпадает с фактом по всей партии

```
$ grep -l '^  - .planning/WINDOWS.md$' .planning/phases/10-rychag-components-modal-html/10-1[2-6]-PLAN.md
.planning/phases/10-rychag-components-modal-html/10-16-PLAN.md
```

**РОВНО ОДИН файл, и это `10-16-PLAN.md`.** Ни один другой план партии в реестр не писал: `git show --stat` обоих коммитов задачи 2 и предыдущих планов подтверждает, что `.planning/WINDOWS.md` в третьей партии тронут только настоящим планом.

## Требования

**`FORM-06` НЕ ПЕРЕВЕДЕНО В ЗАВЕРШЁННОЕ, и `.planning/REQUIREMENTS.md` не тронут ни байтом**
(`git diff --name-only -- .planning/REQUIREMENTS.md` → пусто; отметка остаётся `- [ ]`, клетка таблицы — `Gaps Found`).

Основание, а не забывчивость: `10-VERIFICATION.md` читает `status: gaps_found`, и правило плана 10-14 `test_no_requirement_is_marked_complete_before_its_phase_verification_passed` покраснело бы на такой отметке — тем самым, которое воспроизводит событие коммита `0ea886d`. Штатный шаг исполнителя `requirements.mark-complete` пропущен НАМЕРЕННО; это и есть исполнение правила, а не его обход.

## Decisions Made

См. `key-decisions` фронтматтера. Существенное к прочтению:

1. **Владелец реестра — один, и это замерено, а не объявлено.** Файл стои́т в `files_modified` ровно одного из пяти планов партии; проверка — счёт планов, объявивших файл.
2. **Чужие тексты не перепечатывались.** Оба извлечены из сводок программно и сличены побайтово. Перепечатка владельцем реестра потеряла бы ровно ту точность, ради которой окно заводится.
3. **Числа окна 48 сняты своим прогоном.** Ориентир плана (`17` блокеров при `29` командах) подтвердился по блокерам и разошёлся по командам партии в предсказанную планом сторону — снято `31`.
4. **Пятое окно не заводится.** Команда `<verify>` плана утверждает ровно `48` строк; непройденный обход записан там, где его прочтут (см. «Broken-windows ledger» ниже).

## Deviations from Plan

### 1. [Rule 2 — Missing Critical] Шаг 1.15 получил записанное умолчание о неприменимости и способ его снять

- **Found during:** Задача 1
- **Issue:** План предписывал шагу ветвление «есть вложения / нет вложений», но объявление ПОСЕВНОГО СТЕНДА создаётся с `images=[]` — то есть по умолчанию шаг НЕПРИМЕНИМ ВСЕГДА, и человек, идущий по обходу буквально, никогда не получил бы вердикта по третьей из трёх умиравших функций. Шаг, который нельзя пройти, неотличим от шага, который забыли.
- **Fix:** в текст шага вписано, что стенд идёт без вложений, и назван способ сделать шаг применимым — прикрепить ОДИН файл плиткой «+ ФАЙЛ» (`ads/form.html:121`) ДО шага 1.10. Ветвление «неприменимо → ЗАПИСЫВАЕТСЯ, а не пропускается молча» сохранено дословно.
- **Files modified:** `.planning/phases/10-rychag-components-modal-html/10-UAT.md`
- **Verification:** `grep -c '^| 1\.1[3-5] '` → `3`; текст шага приведён в разделе 1 сводки.
- **Committed in:** `6191349`

### 2. [Rule 2 — Missing Critical] Признаком шага 1.15 выбрано ОТСУТСТВИЕ ПЕРЕЗАГРУЗКИ, а не пропажа плитки

- **Found during:** Задача 1
- **Issue:** План называл признаком «плитка исчезает на месте, документ не перезагружается». Чтение шаблона показало, что первая половина признака САМА ПО СЕБЕ не различает живое и мёртвое: серверная плитка несёт крестик `type="submit" name="remove_image"` (`ads/form.html:116`), и на мёртвом скрипте нажатие уходит POST-ом — плитка тоже пропадает, только вместе с перезагрузкой документа. Шаг, опирающийся на неразличающий признак, записал бы «работает» там, где всё умерло.
- **Fix:** в текст шага вынесен РАЗЛИЧАЮЩИЙ признак с механизмом: живой скрипт перерисовывает полосу сам (`renderImages()`, `:537`) и вешает перехват (`event.preventDefault()`, `:423`); мёртвый оставляет серверную плитку и даёт перезагрузку.
- **Files modified:** `.planning/phases/10-rychag-components-modal-html/10-UAT.md`
- **Verification:** оба узла прочитаны в шаблоне, файл и строка названы в разделе 1 сводки.
- **Committed in:** `6191349`

### 3. [Rule 3 — Blocking] `requirements-completed` оставлен ПУСТЫМ вопреки шаблону сводки

- **Found during:** оформление сводки
- **Issue:** шаблон сводки требует скопировать `requirements` плана (`[FORM-06]`) в поле `requirements-completed` ВЕРБАТИМ. План при этом ПРЯМО запрещает переводить `FORM-06` в завершённое, а правило плана 10-14 краснит такую отметку при вердикте `gaps_found`. Механическое исполнение шаблона завело бы в машиночитаемое поле с именем «завершённые» требование, которое НЕ завершено, — ровно тот класс расхождения записи с деревом, которым фаза провалена первым кругом.
- **Fix:** `requirements-completed: []`, а объявленное планом требование вынесено в соседнее поле `requirements-declared: [FORM-06]` с комментарием; `requirements.mark-complete` не запускалась.
- **Files modified:** `.planning/phases/10-rychag-components-modal-html/10-16-SUMMARY.md`
- **Verification:** `git diff --name-only -- .planning/REQUIREMENTS.md` → пусто; полный прогон суиты (2826 passed) держит правило плана 10-14 зелёным.
- **Committed in:** метаданный коммит плана

---

**Total deviations:** 3 auto-fixed (2 missing critical, 1 blocking)
**Impact on plan:** оба отступления 1 и 2 усиливают предмет плана ровно по его собственной оси — «шаг спрашивает то, что чинилось, и различает живое от мёртвого»; ни одно не расширяет предмет. Отступление 3 есть исполнение прямого запрета плана против умолчания шаблона. Разрастания объёма нет.

## Broken-windows ledger

Дефектов класса `stub`, `todo`, `fixme`, `skipped-test` правка не заводит: правится ЗАПИСЬ, кода она не трогает.

⚠️ **ПЯТОЕ ОКНО О НЕПРОЙДЕННОМ ОБХОДЕ НЕ ЗАВОДИТСЯ, И ЭТО РЕШЕНИЕ С ОСНОВАНИЕМ, А НЕ ПРОПУСК.** Команда `<verify>` плана утверждает РОВНО `48` строк реестра и краснеет на большем с формулировкой «в реестр писал кто-то ещё, а владелец в партии объявлен один». Непройденный обход и без пятого окна записан в ТРЁХ местах, которые прочтут: шапкой `10-UAT.md` (`status: testing`, блока прохождения нет), разделом «Куда артефакт уезжает дальше» (сопоставление проверок 2/3/4/6 окнам **28**, **35**, **22**, **36**) и перечнем `human_verification` отчёта `10-VERIFICATION.md` (семь пунктов). Шестое место — новый раздел «Чего третья партия НЕ закрыла».

## Threat Flags

Новой поверхности правка не заводит: изменены два файла записи, ни одного файла продукта. Сетевых входов, путей аутентификации, обращений к файловой системе продукта и изменений схемы нет. Строка `T-10-16-SC` таблицы угроз («установки пакетов», disposition `accept`) исполнена: `pyproject.toml` не правился, отсутствие браузерных зависимостей ПЕРЕПРОВЕРЕНО исполнителем.

## Known Stubs

Нет.

## Issues Encountered

- **Раздела зонда направления отказа в `10-VERIFICATION.md` НЕТ.** План отсылал за числом `17` и перечнем планов к такому разделу отчёта, но поиск по всем 388 строкам отчёта (`зонд`, `automated`, `fails_when`, `17`, `направлени`) совпадений не дал: единственный «зонд» отчёта — зонд величины идентификатора на фикстурах. Останова это не потребовало, потому что план ПЕРВЫМ ЖЕ требованием предписывает снимать числа СВОИМ прогоном (`check verify-failure-directions 10`), а сличение с отчётом — вторым; сличать оказалось не с чем, и это записано здесь, а не проглочено.
- **Полный прогон суиты идёт 35 минут** (`2074.48s`). Внешнего прерывания в этот раз не случилось; прогон доведён до конца одной попыткой.

## Verification (плановая)

| # | проверка | результат |
|---|---|---|
| 1 | `grep -c '^\| 1\.1[3-5] ' …10-UAT.md` | **3** ✓ |
| 2 | `grep -c '^status: testing$' …10-UAT.md` / `grep -c '^checks_declared: 6$' …` | **1** / **1** ✓ |
| 3 | `grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' …10-UAT.md` | **0** — отметок не заполнено ✓ |
| 4 | `git diff --name-only -- .planning/REQUIREMENTS.md .planning/phases/09-*/` | **пусто** ✓ |
| 5 | `uv run pytest tests/ -q` | **2826 passed, 964 warnings in 2074.48s (0:34:34)**, код возврата **0** — ровно база после плана 10-15 ✓ |
| 6 | ЧЕЛОВЕЧЕСКАЯ ПОЛОВИНА | **НЕ СНЯТА и снята быть не могла:** режим человеческой проверки проекта — `end-of-phase`, наблюдение принадлежит приёмке фазы. До неё критерий 2 роадмапа и требование `FORM-06` остаются НЕЗАКРЫТЫМИ, и настоящий план обратного не утверждает |

Задачные `<verify>`: задача 1 — `3` / `1` / `1` / `4` (порог «больше 4») ✓; задача 2 — `1` / `1` / `0` / `48` / `1` ✓.

## Self-Check

- `.planning/phases/10-rychag-components-modal-html/10-UAT.md` — FOUND
- `.planning/WINDOWS.md` — FOUND
- `6191349`, `aae7e49` — FOUND в `git log`
- Все `<acceptance_criteria>` обеих задач перепроверены прогонами; выводы приведены выше
- Побайтовое совпадение окон 45 и 46 с исходными абзацами сводок — утверждено сличением, а не глазом

## Self-Check: PASSED

## User Setup Required

**Да — и она есть человеческая половина плана.** `user_setup` фронтматтера называет локальный стенд и браузер: поднять приложение (`just run`), посеять стенд по разделу «Как привести стенд в нужное состояние» (ОДНО объявление с НЕ МЕНЕЕ ЧЕМ ТРЕМЯ расписаниями, префикс `UAT10-`), пройти шесть проверок `10-UAT.md`, заполнить шесть таблиц отметок наблюдёнными признаками и убрать стенд. Машинного пути закрыть это у проекта нет.

## Next Phase Readiness

- **Фаза 10 машинно исчерпана этой партией.** Шестнадцать планов исполнены, шестнадцать сводок на месте; суита зелена на 2826.
- **ЧТО ОСТАЁТСЯ И КОМУ.** Критерий 2 роадмапа и требование `FORM-06` закрываются ПРОХОЖДЕНИЕМ обхода `10-UAT.md` человеком плюс повторной верификацией фазы. До этого `FORM-06` остаётся `Pending`, а `10-VERIFICATION.md` — `gaps_found`; ни один машинный прогон этого не меняет.
- **Обход готов к прохождению:** шесть проверок, стенд посева отлажен исполнителем плана 10-10 и убран, переходная ветка теперь спрашивает о живости клиентского слоя.
- **Фазе 11 приезжают два предупреждения:** экран подключения Telegram (окно 46) и слоты панели с живым содержимым (окно 41).
- **Фазе 15 приезжают:** граница величины идентификатора на соседних страничных модулях (окно 45), приземление фокуса на переходном пути (GATE-09), остаток перечня целей внеполосных узлов.
- **Владельцу вехи приезжают:** незакрытое восьмое измерение Найквиста (окно 47) и долг по направлению отказа планов 10-01…10-07 (окно 48). Фазы-владельцы у обоих НЕ НАЗНАЧЕНЫ намеренно.

---
*Фаза: 10-rychag-components-modal-html*
*Завершено: 2026-09-04*
*Артефакт обхода по-прежнему ничего не самозаверяет: `status: testing`, шесть таблиц отметок пусты, `FORM-06` — `Pending`*
