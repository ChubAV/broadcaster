---
phase: 10-rychag-components-modal-html
plan: 30
type: execute
wave: 4
# ⚠️ ВОЛНА 4 — СЛЕДСТВИЕ ПРЕДМЕТА, А НЕ ОСТОРОЖНОСТИ. Гейт полноты границы по ВСЕМУ каталогу
# страничного слоя обязан встать ПОСЛЕ того, как закрыт последний вход: заведённый раньше, он
# краснел бы по построению всю волну 2 и всю волну 3 и был бы выключен раньше, чем начал стеречь.
# Файловая связность даёт то же число: 10-29 (волна 3) правит `tests/test_pages/test_identifier_bounds.py`.
depends_on: ["10-28", "10-29"]
gap_closure: true
files_modified:
  - tests/test_pages/test_identifier_bounds.py
  - tests/test_pages/test_editor_schedules.py
  - .planning/WINDOWS.md
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (поля во frontmatter не заводятся, форма унаследована от
# планов 10-12…10-29): `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  # `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < min_samples 3), а не назначена автором.
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "СЛЕДУЮЩИЙ НЕОГРАНИЧЕННЫЙ ИДЕНТИФИКАТОР НЕ УЕДЕТ В ДЕРЕВО МОЛЧА: правило разбирает ВЕСЬ каталог страничного слоя, а не один файл, и краснеет в момент введения такого входа, называя его модулем, маршрутом и именем параметра"
    - "ВСЕЛЕННАЯ ПРАВИЛА ОБЪЯВЛЕНА ЧИСЛОМ, ПОЭТОМУ СЛОМАВШИЙСЯ РАЗБОР НЕ ЗЕЛЕНЕЕТ: утверждение «каждый найденный ограничен» истинно и для ПУСТОГО множества найденных, и без объявленного числа правило зеленело бы навсегда, ничего не стерегя"
    - "ВСЕЛЕННАЯ НАЗВАНА ДВУМЯ ПРИЗНАКАМИ, А НЕ ОДНИМ: параметр входит в неё, если его имя стои́т местозаполнителем в пути маршрута ЛИБО оканчивается на признак идентификатора; вход, не покрытый ни одним признаком, живёт в ИМЕНОВАННОМ ПРИЛОЖЕНИИ с обоснованием и объявленным числом, а не выпадает молча"
    - "ПРЕЖНЕЕ ПРАВИЛО ОДНОГО ФАЙЛА НЕ СНЕСЕНО И НЕ ПОДМЕНЕНО: его предмет — инвариант модуля расписаний, объявленный шапкой того модуля, и он остаётся; новое правило стои́т РЯДОМ и накрывает каталог"
    - "ПРАВИЛО ПОКАЗАЛО ЗУБЫ МУТАЦИЕЙ, А НЕ ЗАЯВИЛО ИХ: разбор поданного параметром исходника с неограниченным идентификатором даёт красный, называющий этот вход поимённо"
    - "ОКНО 45 РЕЕСТРА ЗАКРЫТО ЗАМЕРОМ, А НЕ РЕШЕНИЕМ О ЧУЖОЙ ФАЗЕ: окно само назвало наблюдаемое условие своего снятия — отсутствие воспроизведённого отказа у соседних модулей, — условие наступило, и это записано с указанием, чем именно наступило"
    - "СЧЁТЧИКИ ШАПКИ РЕЕСТРА ОКОН СОГЛАСНЫ С ТАБЛИЦЕЙ: запись сделана командой ведения журнала, а не ручной правкой строки"
  artifacts:
    - "tests/test_pages/test_identifier_bounds.py — правило полноты границы по всему каталогу страничного слоя: объявленное число вселенной, именованное приложение с обоснованием и объявленным числом, два разведённых текста отказа и антивакуум"
    - "tests/test_pages/test_editor_schedules.py — отсылка от прежнего правила одного файла к новому правилу каталога с объяснением, чем их предметы различаются"
    - ".planning/WINDOWS.md — окно 45 в состоянии закрытого, записанное командой ведения журнала"
  key_links:
    - "правило каталога → следующий добавленный маршрут: единственное, чем неограниченный идентификатор не уедет в дерево молча; ломается — повторяется `CR-01` пятого круга через фазу, и корень у повторения тот же самый — граница стояла там, куда смотрели"
    - "объявленное число вселенной → сломавшийся разбор: единственное, чем правило не станет вакуумно-зелёным; ломается — переименование каталога или смена формы объявления маршрута делает правило вечно зелёным, и обнаруживается это следующим отказом обработчика на бою"
    - "именованное приложение → вход вне обоих признаков: единственное, чем непокрытый вход не выпадет молча; ломается — правило объявляет полноту, имея слепую зону, то есть повторяет предмет `WR-05` на другой оси"
    - "команда ведения журнала → счётчики шапки реестра: единственное, чем три места реестра не разъедутся; ломается — ручная правка строки, и шапка молча говорит не то, что таблица"
  prohibitions:
    - statement: "прежнее правило одного файла НЕ СНОСИТСЯ и не ослабляется: его предмет — объявленный инвариант модуля расписаний, а не подмножество нового правила, и снос заменил бы два утверждения одним"
      status: flagged-unverified
      verification: none
    - statement: "строка реестра окон НЕ ПРАВИТСЯ РУКАМИ: ручная правка разошлась бы со счётчиками шапки молча — запись ведётся командой ведения журнала"
      status: flagged-unverified
      verification: none
    - statement: "фаза-сниматель окна НЕ СОЧИНЯЕТСЯ там, где её не назвал владелец: окна 39, 47, 49, 51 и 52 остаются открытыми со своими основаниями, и ни одному из них настоящий план фазы-владельца не назначает"
      status: flagged-unverified
      verification: none
    - statement: "объявленное число вселенной НЕ ПОДГОНЯЕТСЯ под зелёный прогон: оно ставится прогоном ПОКРАСНЕВШЕГО правила, а вписанное по итогу зелёного прогона число не стережёт ничего"
      status: flagged-unverified
      verification: none
    - statement: "исполненные планы `10-01`…`10-23` и их сводки НЕ ПРАВЯТСЯ и не перенумеровываются"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ — ПОЛНАЯ ЛЕТОПИСЬ ПАРТИИ СТОИТ В `10-24-PLAN.md`. Настоящий план не
  # сдвигает ни одного ребра: его предмет — стерегущее правило, а не поведение границы.
---

<objective>
Граница величины идентификатора получает гейт полноты по ВСЕМУ каталогу страничного слоя, и окно 45
реестра закрывается замером.

Предмет — вторая половина предписания `CR-01` пятого круга: «widen the gate so the next unbounded
input cannot appear silently». Сегодня правило иметь границу требует от пяти входов ОДНОГО файла,
перечисленных руками; ревизия просит заменить перечень разбором всего каталога в той форме, которую
фаза уже применила к гарду происхождения.

Purpose: без этого правила партия закрыла бы двадцать девять входов и оставила бы способ, которым
тридцатый появится молча, — то есть починила бы следствие, оставив корень. Корень назван самим
окном 45: «граница стои́т там, куда смотрели, и молчит там, куда не смотрели».

⚠️ **ЧИСЛО ДВАДЦАТЬ ДЕВЯТЬ ПЕРЕСЧИТАНО ПО САМИМ ПЛАНАМ ПАРТИИ, А НЕ ПЕРЕПИСАНО, И СЛАГАЕМЫЕ НАЗВАНЫ.**
Прежняя редакция настоящего плана и плана 10-32 говорила «двадцать восемь»; сумма по планам её не
подтвердила. Слагаемые: **10-24** — один вход (`ad_id` маршрута удаления объявления); **10-28** —
семнадцать (пятнадцать идентификаторов адреса: семь в модуле групп аккаунта, четыре в модуле
аккаунтов, два в модуле объявлений, два в модуле журнала — плюс два параметра запроса: курсор
постраничного вывода групп и признак раскрытого расписания редактора); **10-29** — одиннадцать
идентификаторов адреса административного модуля. **1 + 17 + 11 = 29.** ⚠️ ЭТО ОРИЕНТИР ПЛАНИРОВЩИКА,
СНЯТЫЙ ЧТЕНИЕМ ПЛАНОВ; ИСТИНОЙ ЕГО ДЕЛАЕТ ЗАМЕР ИСПОЛНИТЕЛЯ по сводкам трёх планов. Расхождение с
ориентиром есть НАХОДКА и называется поимённо — какой вход не попал ни в одно слагаемое, — а не
поводом подогнать число под ожидание. Число это ЖИВЁТ В ТРЁХ МЕСТАХ (настоящий `<objective>`,
задача 2 настоящего плана и `<objective>` плана 10-32), и все три обязаны говорить одно.

⚠️ **ОКНО 45 ЗАКРЫВАЕТСЯ ЗАМЕРОМ, А НЕ ПЕРЕСМОТРОМ РЕШЕНИЯ ВЛАДЕЛЬЦА, И РАЗНИЦА НАЗЫВАЕТСЯ ПРЯМО.**
Окно называет фазу-владельца НЕ НАЗНАЧЕННОЙ и кандидатом — Фазу 15. Но основанием отсрочки оно
назвало НАБЛЮДАЕМОЕ УСЛОВИЕ: «у остальных модулей нет НИ воспроизведённого отказа, НИ объявленного
инварианта уровня модуля, и гейт, заведённый на весь каталог без замера, краснел бы на месте, о
котором никто ничего не измерил». Пятый круг ревизии предъявил воспроизведённый отказ на ШЕСТИ
соседних маршрутах поимённо, с таблицей снятых кодов. Условие, которое окно назвало само, наступило.
Закрытие окна есть ФИКСАЦИЯ НАСТУПИВШЕГО УСЛОВИЯ, а не назначение работы чужой фазе.

Output: правило каталога с объявленной вселенной и именованным приложением, отсылка от прежнего
правила и закрытое окно.
</objective>

<reversibility>
  <decision rating="reversible">
Заведение правила полноты по каталогу. Откат — удаление правила; поведение продукта не меняется ни на
байт.
  </decision>
  <decision rating="costly">
Закрытие окна 45. **`costly`, а не `one-way`:** реестр ведётся командой, состояние переводится
обратно тем же способом, и текст окна сохраняется. Цена отката названа: заново открытое окно
потеряет запись о том, каким замером условие его снятия наступило, если эта запись не будет вписана в
сводку плана — поэтому она вписывается ТУДА, а не только в реестр.
  </decision>
</reversibility>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/WINDOWS.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-28-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-29-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: правило полноты границы разбирает ВЕСЬ каталог страничного слоя — вселенная объявлена числом, слепая зона названа приложением</name>

  <files>
tests/test_pages/test_identifier_bounds.py
  </files>

  <read_first>
- `tests/test_pages/test_origin_guard_on_destructive_routes.py:30-60` и `:190-340` — ФОРМА, которую
  правило наследует целиком: разбор ДЕРЕВА (поиск строки считал бы вхождение в докстринге), реестр,
  собранный чтением, объявленное число вселенной, объявленное число изъятий, замыкающее требование
  «каждый найденный», антивакуум и отрицательные контроли, доказывающие, что правило КРАСНЕЕТ.
  ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО И ЦЕЛИКОМ: ревизия предписала «в той форме, которую фаза уже применяет», и
  форма эта — здесь.
- `tests/test_pages/test_editor_schedules.py:2160-2380` — прежнее правило одного файла
  (`BOUND_ALIASES`, статический гейт сигнатур). ⚠️ ЧИТАТЬ: чем его предмет отличается от предмета
  нового правила и почему оно НЕ СНОСИТСЯ.
- `app/pages/identifiers.py` — имена ограниченных псевдонимов: правило узнаёт их ПО ИМЕНИ, и перечень
  узнаваемых имён обязан жить в одном месте.
- `10-28-SUMMARY.md` и `10-29-SUMMARY.md` — перечни закрытых входов и их числа. ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО: объявленное число вселенной снимается ПРОГОНОМ, но сличается с суммой закрытого
  партией, и расхождение есть находка.
- `app/pages/*.py` — все модули каталога. ⚠️ ВЫПИСАТЬ, есть ли параметры, целые по объявлению и
  уезжающие операндом сравнения по колонке идентификатора, чьё имя НЕ стои́т местозаполнителем в пути
  и НЕ оканчивается на признак идентификатора. Такие параметры и есть содержимое ПРИЛОЖЕНИЯ, и найти
  их надо ДО написания правила, а не после первого красного.
  </read_first>

  <behavior>
1. Правило разбирает все модули каталога страничного слоя и собирает вселенную: параметры
   обработчиков маршрутов, объявленные целым, чьё имя стои́т местозаполнителем в пути маршрута ЛИБО
   оканчивается на признак идентификатора.
2. Каждый параметр вселенной объявлен через ограниченный псевдоним либо несёт границу встроенной
   записью.
3. Число параметров вселенной объявлено константой; расхождение с замером краснит прогон.
4. Параметры, целые по объявлению и уезжающие в сравнение по колонке, но не покрытые ни одним
   признаком вселенной, перечислены ИМЕНОВАННЫМ ПРИЛОЖЕНИЕМ со своим обоснованием и своим объявленным
   числом.
5. Правило падает на ПУСТОЙ вселенной.
6. Два текста отказа разведены: (а) найден параметр вселенной без границы; (б) число вселенной
   разошлось с замером.
7. Правило краснеет на поданном параметром исходнике с неограниченным идентификатором и называет
   его модулем, маршрутом и именем параметра.
8. Величины постраничного вывода (смещение и размер порции) во вселенную не входят, и это объявлено
   изъятием с обоснованием, а не умолчанием.
  </behavior>

  <action>
**(1) RED ПЕРЕД GREEN, НА СИНТЕТИЧЕСКОМ ИСХОДНИКЕ.** Сначала написать правило так, чтобы исходники
приходили ПАРАМЕТРОМ, и прогнать его на синтетическом модуле с неограниченным идентификатором пути —
оно обязано дать КРАСНЫЙ с текстом (а), называющим вход. Дословный вывод привести в сводку.
⚠️ ПОДАЧА ИСХОДНИКОВ ПАРАМЕТРОМ ОБЯЗАТЕЛЬНА: без неё зубы правила пришлось бы ЗАЯВЛЯТЬ, а боевой файл
— править ради доказательства. Приём взят у гейта гарда происхождения.

**(2) Объявить ВСЕЛЕННУЮ ДВУМЯ ПРИЗНАКАМИ и записать, почему их два.** Признак адреса (имя параметра
стои́т местозаполнителем в пути) машинно чёткий, но он один не накрыл бы курсор постраничного вывода
групп, закрытый планом 10-28, — тот приезжает параметром запроса. Признак имени (окончание на
признак идентификатора) накрывает его. Оба признака назвать в комментарии вместе с тем, что каждый
без другого пропускает.

**(3) Завести ПРИЛОЖЕНИЕ для входов вне обоих признаков.** Форма — действующая форма перечня изъятий
проекта: запись несёт ВХОД, ОБОСНОВАНИЕ и УСЛОВИЕ СНЯТИЯ, а число записей объявлено отдельно.
⚠️ ПЛАНИРОВЩИК ЗНАЕТ ОДНОГО КАНДИДАТА — признак раскрытого расписания редактора объявлений: он
объявлен идентификатором расписания, но имя его на признак идентификатора не оканчивается и
местозаполнителем в пути не стои́т. Исполнитель проверяет ЗАМЕРОМ, единственный ли он, и вписывает
СВОЙ перечень; расхождение с ориентиром называется поимённо.
⚠️ ПУСТОЕ ПРИЛОЖЕНИЕ ТОЖЕ ЗАКОННО, но тогда объявленное число равно нулю ЯВНО, а не отсутствует:
именованный ноль стережёт появление первой записи, отсутствующее объявление — нет.

**(4) Объявить ИЗЪЯТИЕ величин постраничного вывода** отдельной записью с обоснованием, взятым из
действующего гейта дословно: они не есть идентификатор, и отказ по ним ничего не говорит о владении
строкой. Изъятие названо, а не подразумевается — иначе правило имело бы слепую зону, о которой
читатель узнал бы, только заглянув в разбор.

**(5) Развести ДВА текста отказа.** Один текст на два события лгал бы в одном из них. Текст (а) ведёт
к сигнатуре и называет псевдоним, которым вход надо объявить; текст (б) ведёт к объявленному числу и
называет, что означает движение числа в каждую сторону.

**(6) АНТИВАКУУМ.** Отдельным утверждением: вселенная непуста и число разобранных модулей каталога
больше нуля. Без него переименование каталога или смена формы объявления маршрута дали бы вечно
зелёное правило.

**(7) Поставить объявленное число ПРОГОНОМ ПОКРАСНЕВШЕГО ПРАВИЛА.** Вписать заведомо неверное,
прогнать, прочитать замер из текста отказа, вписать прочитанное. ⚠️ Число, вписанное по итогу
зелёного прогона, не стережёт ничего: оно равно тому, что есть, по построению.

**(8) Сличить с суммой закрытого партией.** Объявленное число вселенной обязано сойтись с суммой
входов, закрытых планами 10-24, 10-28 и 10-29, плюс входы, закрытые планом 10-12. ⚠️ ОРИЕНТИР
ПЛАНИРОВЩИКА ПО ПАРТИИ: **1 (10-24) + 17 (10-28) + 11 (10-29) = 29**; слагаемые разобраны в
`<objective>` настоящего плана. Вклад плана 10-12 в это число НЕ ВХОДИТ и берётся отдельно.
Расхождение — это НАХОДКА, и она называется поимённо: какой вход не попал ни в один план партии.

**(9) Чего задача НЕ делает.** Не правит ни одного модуля `app/`. Не сносит прежнего правила одного
файла (задача 2). Не трогает реестра окон (задача 3).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_identifier_bounds.py -q</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — правило не собрано, либо найден параметр вселенной без границы, либо объявленное число разошлось с замером</fails_when>
    <automated>uv run pytest tests/test_pages/test_identifier_bounds.py -q -k "universe or catalogue or every_identifier or declared"</automated>
    <fails_when>ненулевой код возврата, либо `no tests ran` в выводе — новое правило названо так, что его не найти по предмету, и следующий читатель не отличит его от матрицы входов</fails_when>
    <automated>uv run pytest tests/ -q -m "not planning"</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — новое правило разошлось с продуктовой суитой</fails_when>
  </verify>

  <acceptance_criteria>
- КРАСНЫЙ НА СИНТЕТИЧЕСКОМ ИСХОДНИКЕ НАБЛЮДЁН ДО ЗЕЛЁНОГО: дословный вывод приведён в сводке, и в
  нём назван вход модулем, маршрутом и именем параметра.
- Объявленное число вселенной поставлено прогоном ПОКРАСНЕВШЕГО правила: красный вывод с заведомо
  неверным числом приведён в сводке, и из него видно замеренное значение.
- Объявленное число сличено с суммой входов, закрытых партией и планом 10-12: обе величины приведены
  в сводке, и расхождение, если оно есть, названо поимённо.
- Правило падает на пустой вселенной: вывод прогона с поданным пустым набором исходников приведён в
  сводке.
- Приложение объявлено с числом — ненулевым с обоснованиями либо явным нулём: содержимое и число
  приведены в сводке; ориентир планировщика (один кандидат — признак раскрытого расписания редактора)
  назван рядом, и расхождение с ним объяснено.
- Изъятие величин постраничного вывода объявлено записью с обоснованием, а не отсутствием в разборе:
  в сводке названо, где это объявление стои́т.
- Оба текста отказа РАЗЛИЧНЫ и оба приведены в сводке.
- Правило узнаёт ограниченный псевдоним по имени из ОДНОГО перечня узнаваемых имён: в сводке названо,
  сколько имён в перечне и совпадает ли оно с числом псевдонимов нейтрального модуля.
  </acceptance_criteria>

  <done>
Правило разбирает весь каталог, объявленное число поставлено прогоном красного и сличено с суммой
партии, приложение и изъятие объявлены с числами, антивакуум и зубы показаны прогонами.
  </done>
</task>

<task type="auto">
  <name>Задача 2: прежнее правило одного файла остаётся и получает отсылку — два предмета, а не один в двух копиях</name>

  <files>
tests/test_pages/test_editor_schedules.py
  </files>

  <read_first>
- `tests/test_pages/test_editor_schedules.py:1-40` — шапка модуля, где объявлен ИНВАРИАНТ УРОВНЯ
  МОДУЛЯ: «прямой POST мимо браузера обязан давать отказ валидации, а не 500». ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО:
  это и есть предмет, которого у нового правила НЕТ, и по которому прежнее правило не сносится.
- `tests/test_pages/test_editor_schedules.py:1960-2380` — `UNBOUNDED_ROUTE_CASES`, правило прогона
  пяти входов и статический гейт сигнатур файла.
- `tests/test_pages/test_identifier_bounds.py` — новое правило каталога, заведённое задачей 1.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `### CR-01`, абзац
  «replace the list with an `ast` walk». ⚠️ ЧИТАТЬ ВНИМАТЕЛЬНО: ревизия говорит о ГЕЙТЕ ОХВАТА
  («Gate scope»), а не о правиле прогона запросов. Прогон пяти реальных запросов — не то же, что
  статическая проверка сигнатур, и подмена одного другим потеряла бы наблюдение.
  </read_first>

  <behavior>
1. Прежнее правило прогона пяти входов модуля расписаний остаётся и остаётся зелёным.
2. Прежний статический гейт сигнатур файла остаётся и остаётся зелёным.
3. Оба несут отсылку к новому правилу каталога с объяснением, чем предметы различаются.
4. Новое правило каталога несёт встречную отсылку к инварианту модуля расписаний.
5. Ни одно из трёх правил не является подмножеством другого по УТВЕРЖДЕНИЮ, и это записано.
  </behavior>

  <action>
**(1) Записать РАЗЛИЧИЕ ПРЕДМЕТОВ, а не факт соседства.** Три утверждения различаются так:
— прежнее правило прогона: ПЯТЬ РЕАЛЬНЫХ ЗАПРОСОВ доезжают до отказа валидации, то есть наблюдается
  ПОВЕДЕНИЕ маршрута целиком, включая порядок в теле;
— прежний статический гейт сигнатур: ШЕСТОЙ вход ЭТОГО ФАЙЛА не появится без границы, то есть
  стережётся инвариант, объявленный шапкой ЭТОГО модуля;
— новое правило каталога: НИ ОДИН вход НИ ОДНОГО модуля страничного слоя не появится без границы,
  то есть стережётся свойство ПРОЕКТА.
⚠️ ПЕРВОЕ НЕ ВЫВОДИТСЯ ИЗ ТРЕТЬЕГО: статическая проверка сигнатуры не наблюдает, что запрос
действительно доехал до отказа, — она наблюдает объявление. Снос прежнего правила ради «неповторения»
заменил бы наблюдение объявлением.

**(2) Дописать отсылки в ОБЕ стороны.** У прежних правил — одна строка: свойство проекта стережёт
такое-то правило такого-то модуля; предмет здешних правил — инвариант ЭТОГО модуля и ПОВЕДЕНИЕ его
маршрутов. У нового правила — встречная: инвариант модуля расписаний объявлен шапкой того модуля и
проверяется прогоном настоящих запросов там же.

**(3) Прогнать оба прежних правила и убедиться, что они зелены** после всех правок партии. Их зелень
после закрытия двадцати девяти входов (1 у плана 10-24 + 17 у плана 10-28 + 11 у плана 10-29,
ориентир планировщика, сличаемый с замером по сводкам) не самоочевидна: планы партии трогали
`app/pages/schedules.py` только переносом объявления, но перенос — тоже правка.

**(4) Чего задача НЕ делает.** Не сносит и не ослабляет ни одного прежнего правила. Не меняет
`UNBOUNDED_ROUTE_CASES` и не сдвигает его числа. Не правит `app/pages/schedules.py` — его записи
правит план 10-32.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py -q</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — прежние правила модуля расписаний перестали собираться либо покраснели после правок партии</fails_when>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py tests/test_pages/test_identifier_bounds.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — два правила, стерегущие смежные предметы, разошлись</fails_when>
    <automated>grep -c "test_identifier_bounds" tests/test_pages/test_editor_schedules.py</automated>
    <fails_when>вывод равен `0` — отсылки к новому правилу каталога в прежнем модуле нет, и читатель, поймавший красное здесь, не найдёт соседнего утверждения</fails_when>
  </verify>

  <acceptance_criteria>
- Оба прежних правила зелены после всех правок партии, и это подтверждено прогоном, приведённым в
  сводке.
- Отсылки стоят в ОБЕ стороны: `grep -c "test_identifier_bounds" tests/test_pages/test_editor_schedules.py`
  и `grep -c "test_editor_schedules" tests/test_pages/test_identifier_bounds.py` — оба вывода не
  меньше `1`, и оба приведены в сводке.
- Различие предметов записано ТРЕМЯ утверждениями, а не фразой о соседстве: текст приведён в сводке.
- `UNBOUNDED_ROUTE_CASES` не правился: `git diff` по файлу не содержит изменений внутри его
  объявления, и число его записей приведено в сводке до и после.
  </acceptance_criteria>

  <done>
Прежние правила модуля расписаний целы и зелены, отсылки стоят в обе стороны, различие трёх предметов
записано утверждениями.
  </done>
</task>

<task type="auto">
  <name>Задача 3: окно 45 реестра закрывается ЗАМЕРОМ, и разница между «условие наступило» и «решением о чужой фазе» записывается прямо</name>

  <files>
.planning/WINDOWS.md
  </files>

  <read_first>
- `.planning/WINDOWS.md`, шапка со счётчиками и строка окна **45** ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ВЕСЬ ТЕКСТ
  ОКНА: там названы ЧТО закрыто, ЧТО остаётся, КАНДИДАТ на закрытие и — главное — ОСНОВАНИЕ отсрочки,
  сформулированное НАБЛЮДАЕМЫМ УСЛОВИЕМ («нет НИ воспроизведённого отказа, НИ объявленного инварианта
  уровня модуля»). Именно это условие и наступило.
- Строки окон **39**, **47**, **49**, **51**, **52** — они остаются ОТКРЫТЫМИ, и настоящий план ни
  одному из них фазы-владельца не назначает. ⚠️ ЧИТАТЬ, чтобы не закрыть соседнее окно по
  невнимательности: у окна 39 предмет — боевой драйвер, и партия его не трогала.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `### CR-01`, таблица
  воспроизведённых исходов — это и есть замер, которым условие наступило.
- `10-24-SUMMARY.md`, `10-28-SUMMARY.md`, `10-29-SUMMARY.md` — числа закрытых входов по планам.
- Сводки задач 1 и 2 настоящего плана — правило каталога и его объявленное число.
  </read_first>

  <behavior>
1. Окно 45 переведено в состояние закрытого КОМАНДОЙ ВЕДЕНИЯ ЖУРНАЛА.
2. Счётчики шапки реестра согласны с таблицей после перевода.
3. Текст окна не переписан: закрывается СОСТОЯНИЕ, а не редактируется описание.
4. В сводке плана записано, ЧЕМ именно условие снятия наступило, — так, чтобы заново открытое окно не
   потеряло этой записи.
5. Ни одно другое окно не тронуто.
  </behavior>

  <action>
**(1) Снять состояние реестра ДО.** Три числа шапки (открытых, отложенных, закрытых) и общее.
Привести в сводку.

**(2) Перевести окно 45 КОМАНДОЙ ВЕДЕНИЯ ЖУРНАЛА** — `gsd-tools windows fixed 45`. ⚠️ РУЧНАЯ ПРАВКА
СТРОКИ ЗАПРЕЩЕНА: она разошлась бы со счётчиками шапки молча, и три места реестра перестали бы
говорить одно. Это прохибиция `must_haves` настоящего плана, и два места одного утверждения обязаны
говорить одно.

**(3) Записать В СВОДКУ ПЛАНА, ЧЕМ УСЛОВИЕ НАСТУПИЛО** — четырьмя утверждениями, каждое с числом:
— воспроизведённый отказ у соседних модулей ПОЯВИЛСЯ: пятый круг ревизии предъявил его на шести
  маршрутах таблицей снятых кодов (привести число маршрутов и дату круга);
— объявленный инвариант уровня модуля БОЛЬШЕ НЕ ТРЕБУЕТСЯ как условие: правило заведено на СВОЙСТВО
  ПРОЕКТА, и его вселенная объявлена числом (привести число);
— гейт по каталогу НЕ КРАСНЕЕТ на местах, о которых ничего не измерено: все входы вселенной закрыты
  планами партии (привести сумму), а входы вне признаков вселенной живут в именованном приложении с
  обоснованием (привести число);
— чего закрытие НЕ УТВЕРЖДАЕТ: поведение боевого драйвера на неограниченном входе (окно 39) осталось
  ненаблюдённым, и второе окно закрытым НЕ объявляется.

**(4) Записать РАЗНИЦУ ПРЯМО.** Отдельным абзацем сводки: окно называло фазу-владельца НЕ
НАЗНАЧЕННОЙ и кандидатом — Фазу 15. Настоящий план фазы-владельца НЕ НАЗНАЧАЕТ и Фазе 15 работы НЕ
передаёт: он фиксирует, что НАБЛЮДАЕМОЕ УСЛОВИЕ, названное самим окном, наступило, и предмет окна
исчерпан замером. ⚠️ Это отличается от закрытия окна решением о чужой фазе, и отличие названо, а не
подразумевается: решение о чужой фазе плану запрещено (форма окон 34 и 47), фиксация наступившего
условия — нет.

**(5) Проверить, что не тронуто больше ничего — ОТ ЗАМЕРЕННОЙ ОПОРЫ, А НЕ ОТ СЧЁТА СТРОК.** Диффом
по реестру: изменённых строк ровно столько, сколько ставит команда ведения журнала, и все они
относятся к окну 45 и к шапке.
⚠️ **ПРЕДСУЩЕСТВУЮЩЕЕ СОСТОЯНИЕ КАТАЛОГА ЗАПИСИ ЗАМЕРЕНО И НАЗВАНО ПОИМЁННО, ПОТОМУ ЧТО БЕЗ НЕГО
ГЕЙТ ЧИСТОТЫ ОТЛИЧИТЬ РАБОТУ ПЛАНА ОТ СОСТОЯНИЯ ДЕРЕВА НЕ МОЖЕТ.** На дереве ДО начала партии
`git status --porcelain -- .planning/` даёт РОВНО ДВЕ строки, и обе неотслеживаемы:
`?? .planning/milestone.lock` и `?? .planning/state.json`. Это состояние дерева, а не работа партии,
и ни один план партии их не заводил. Отсюда форма гейта: он сличает МНОЖЕСТВО путей с названным
перечнем допустимого, а не считает строки, — счёт строк на нетронутом дереве даёт два и краснеет
там, где ничего не случилось. Исполнитель снимает эти же две строки СВОИМ прогоном и приводит их в
сводку рядом с перечнем допустимого; расхождение с ориентиром есть НАХОДКА и называется поимённо.
⚠️ **ГЛАГОЛЫ РЕЕСТРА НАЗВАНЫ ЗАМЕРОМ НАБОРА, А НЕ ПО ПАМЯТИ:** у `windows` есть `status`, `append`,
`waive` и `fixed`. Глагола `list` НЕТ, и вызов его вернул бы диагностику в поток ошибок при НУЛЕВОМ
коде возврата — то есть гейт, построенный на нём, зеленел бы всегда и ничего не стерёг. Состояние
окна снимается `status` и читается УТВЕРЖДЕНИЕМ о поле записи 45, перевод делается `fixed`.

**(6) Снять состояние реестра ПОСЛЕ** и сличить: открытых на одно меньше, закрытых на одно больше,
общее не изменилось.

**(7) Чего задача НЕ делает.** Не закрывает окон 39, 47, 49, 51 и 52 — у каждого своё основание, и
ни одно из них настоящей партией не исчерпано. Не назначает фазы-владельца ни одному открытому окну.
Не заводит новых окон — окна партии заведёт замыкающий план партии, если они появятся.
  </action>

  <verify>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs query windows status | uv run python -c "import json,sys; d=json.load(sys.stdin)['ledger']; e={x['id']: x for x in d['entries']}; w=e.get(45); print('окно 45:', (w or {}).get('status'), '| открытых:', d['open_count'], 'отложенных:', d['waived_count'], 'закрытых:', d['fixed_count'], 'всего:', d['total_count'], '| записей в таблице:', len(d['entries'])); sys.exit(0 if w is not None and w['status'] == 'fixed' and len(d['entries']) == d['total_count'] else 'окно 45 не переведено в закрытое либо число записей реестра разошлось с общим счётчиком; снятое состояние окна 45: '+repr((w or {}).get('status')))"</automated>
    <fails_when>ненулевой код возврата — состояние окна 45 в реестре не равно закрытому (команда ведения журнала не отработала), записи с номером 45 в реестре нет вовсе, либо число строк таблицы разошлось с общим счётчиком шапки. ⚠️ ГЕЙТ ЧИТАЕТ СОСТОЯНИЕ ИМЕННО ЗАПИСИ 45, А НЕ ОТСУТСТВИЕ ЕЁ В УСЕЧЁННОМ ВЫВОДЕ, И ЭТО РАЗНИЦА, А НЕ СТИЛЬ: при `open_count` под полсотни любой вывод, обрезанный по числу строк, зеленеет УСЕЧЕНИЕМ, а не закрытием окна. Проверяется УТВЕРЖДЕНИЕ (`status == fixed`), а не непопадание строки в окно просмотра. ⚠️ ГЛАГОЛ ВЗЯТ ИЗ ДЕЙСТВУЮЩЕГО НАБОРА: у `windows` есть `status`, `append`, `waive`, `fixed` — и только они; `list` среди них НЕТ, и вызов несуществующего глагола вернул бы пустой вывод при нулевом коде, то есть зеленел бы всегда</fails_when>
    <automated>uv run python -c "import re,pathlib,sys; s=pathlib.Path('.planning/WINDOWS.md').read_text(encoding='utf-8'); head=s.split('---')[1]; o=int(re.search(r'open_count:\s*(\d+)',head).group(1)); f=int(re.search(r'fixed_count:\s*(\d+)',head).group(1)); w=int(re.search(r'waived_count:\s*(\d+)',head).group(1)); t=int(re.search(r'total_count:\s*(\d+)',head).group(1)); sys.exit(0 if o+f+w==t else f'счётчики шапки реестра не сходятся: {o}+{f}+{w} != {t}')"</automated>
    <fails_when>ненулевой код возврата — сумма счётчиков шапки не равна общему числу, то есть строка правилась руками и шапка разошлась с таблицей</fails_when>
    <automated>git status --porcelain -uall -- .planning/ | grep -vE '(WINDOWS\.md|STATE\.md|ROADMAP\.md|state\.json|milestone\.lock|10-[0-9]{2}-(PLAN|SUMMARY)\.md)$'; test -z "$(git status --porcelain -uall -- .planning/ | grep -vE '(WINDOWS\.md|STATE\.md|ROADMAP\.md|state\.json|milestone\.lock|10-[0-9]{2}-(PLAN|SUMMARY)\.md)$')"</automated>
    <fails_when>ненулевой код возврата, и напечатанные строки называют каждый лишний путь ПОИМЁННО — правка задела в каталоге записи что-то за пределами перечисленного. ⚠️ ПЕРЕЧЕНЬ ДОПУСТИМОГО ЕСТЬ ЗАМЕР, А НЕ УДОБСТВО, И КАЖДЫЙ ЕГО ЧЛЕН ИМЕЕТ СВОЁ ОСНОВАНИЕ: `WINDOWS.md` — работа настоящей задачи; `STATE.md` и `ROADMAP.md` — бухгалтерия исполняющего механизма, ведущаяся помимо плана; `10-NN-PLAN.md` и `10-NN-SUMMARY.md` — артефакты партии; `state.json` и `milestone.lock` — ПРЕДСУЩЕСТВОВАВШЕЕ СОСТОЯНИЕ ДЕРЕВА, снятое замером ДО начала партии (обе строки неотслеживаемы и обе были на месте до первого коммита партии), и потому работой плана НЕ ЯВЛЯЮТСЯ. Прежняя редакция гейта считала СТРОКИ и краснела на нетронутом дереве, потому что не отличала работу плана от того, что там уже лежало</fails_when>
    <automated>git status --porcelain -uall -- .planning/state.json .planning/milestone.lock; test "$(git status --porcelain -uall -- .planning/state.json .planning/milestone.lock | grep -c '^?? ')" = 2</automated>
    <fails_when>вывод второй команды отличен от `2` — предсуществующее состояние дерева перестало быть тем, чем было замерено ДО партии. Меньше двух означает, что план втянул `state.json` либо `milestone.lock` в ЗАПИСЬ проекта (добавил под наблюдение либо удалил), то есть стёр саму границу между работой партии и тем, что лежало в дереве до неё; больше двух здесь невозможно — путей ровно два и названы они поимённо</fails_when>
  </verify>

  <acceptance_criteria>
- Три числа шапки реестра приведены в сводке ДО и ПОСЛЕ; открытых на одно меньше, закрытых на одно
  больше, общее не изменилось.
- Перевод сделан КОМАНДОЙ ВЕДЕНИЯ ЖУРНАЛА: в сводке приведена сама команда и её вывод.
- Четыре утверждения о том, чем условие снятия наступило, записаны в сводку, и КАЖДОЕ несёт своё
  число: маршрутов с воспроизведённым отказом, вселенной правила, суммы закрытых партией входов и
  записей приложения.
- Отдельным абзацем сводки записана РАЗНИЦА между фиксацией наступившего условия и решением о чужой
  фазе; в абзаце названо, что фаза-владелец окна планом НЕ НАЗНАЧАЕТСЯ.
- Записано ПРЯМО, чего закрытие не утверждает: окно 39 остаётся открытым, и поведение боевого
  драйвера на неограниченном входе ненаблюдённым.
- Ни одно другое окно не тронуто: в сводке приведён перечень изменённых строк реестра, и все они
  относятся к окну 45 и к шапке.
- Текст окна 45 не переписан: в сводке приведено, сколько символов описания изменилось, и это число
  равно нулю.
- ⚠️ ПРЕДСУЩЕСТВУЮЩЕЕ СОСТОЯНИЕ КАТАЛОГА ЗАПИСИ ОТДЕЛЕНО ОТ РАБОТЫ ПЛАНА ЗАМЕРОМ, А НЕ СЛОВОМ: в
  сводке приведён вывод `git status --porcelain -uall -- .planning/` с разметкой по каждой строке —
  работа настоящей задачи (`WINDOWS.md`), бухгалтерия исполняющего механизма (`STATE.md`,
  `ROADMAP.md`), артефакты партии (`10-NN-PLAN.md`, `10-NN-SUMMARY.md`) и ПРЕДСУЩЕСТВОВАВШЕЕ
  состояние дерева (`state.json`, `milestone.lock`, обе неотслеживаемы). Ориентир планировщика — две
  предсуществующие неотслеживаемые строки; расхождение с ним названо поимённо.
- Состояние окна 45 снято УТВЕРЖДЕНИЕМ о поле записи, а не отсутствием строки в усечённом выводе: в
  сводке приведён вывод гейта со снятым состоянием записи 45 и с четырьмя счётчиками шапки.
  </acceptance_criteria>

  <done>
Окно 45 закрыто командой ведения журнала, счётчики шапки сходятся, чем именно наступило условие —
записано четырьмя числами, разница с решением о чужой фазе названа, соседние окна не тронуты.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| будущая правка страничного слоя → продукт | новый маршрут с неограниченным идентификатором уезжает в дерево, и без стерегущего правила это видно только следующим отказом обработчика на бою |
| запись проекта → читатель | окно, закрытое без указания, чем наступило условие его снятия, неотличимо от окна, закрытого по усталости |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-30-01 | Denial of Service | будущие маршруты `app/pages/` | high | mitigate | ⚠️ ПРЕДМЕТ — ВОЗВРАТ ЗАКРЫТОГО ДЕФЕКТА ЧЕРЕЗ ФАЗУ, а не сегодняшний отказ. Без правила каталога партия закрыла бы двадцать девять входов (1 + 17 + 11 по планам 10-24, 10-28 и 10-29) и оставила бы способ, которым тридцатый появится молча. Митигация — разбор всего каталога с объявленной вселенной, именованным приложением, двумя разведёнными текстами отказа и антивакуумом (задача 1) |
| T-10-30-02 | Tampering | правило полноты, зеленеющее вакуумом | high | mitigate | Утверждение «каждый найденный ограничен» истинно и для ПУСТОГО множества найденных: сломайся разбор декораторов или переименуйся каталог, и правило зеленело бы навсегда. Митигация — объявленное число вселенной плюс отдельное утверждение о непустоте разобранных модулей; число ставится прогоном ПОКРАСНЕВШЕГО правила (задача 1, пункты 6 и 7) |
| T-10-30-03 | Repudiation | утверждение полноты со слепой зоной | medium | mitigate | Правило, объявляющее полноту по двум признакам, имеет слепую зону — вход, не покрытый ни одним. Митигация — ИМЕНОВАННОЕ ПРИЛОЖЕНИЕ с обоснованием и объявленным числом, включая явный ноль; изъятие величин постраничного вывода тоже объявлено записью, а не отсутствием в разборе |
| T-10-30-04 | Tampering | замена наблюдения объявлением | medium | mitigate | Снос прежнего правила прогона пяти реальных запросов ради «неповторения» заменил бы НАБЛЮДЕНИЕ поведения СТАТИЧЕСКОЙ ПРОВЕРКОЙ объявления. Митигация — прежние правила остаются, различие трёх предметов записано утверждениями, отсылки стоят в обе стороны (задача 2) |
| T-10-30-05 | Repudiation | реестр окон | medium | mitigate | Ручная правка строки разошлась бы со счётчиками шапки молча. Митигация — перевод командой ведения журнала плюс машинная проверка равенства суммы счётчиков общему числу (задача 3) |
| T-10-30-SC | Tampering | установки пакетов | low | accept | Установок нет: `pyproject.toml` не в `files_modified`, новых зависимостей не вводится |
</threat_model>

## Артефакты, которые создаёт этот план

- правило полноты границы по всему каталогу страничного слоя с объявленной вселенной, именованным
  приложением, объявленным изъятием, двумя текстами отказа и антивакуумом
  (`tests/test_pages/test_identifier_bounds.py`)
- перечень узнаваемых имён ограниченных псевдонимов, объявленный одним местом
- встречные отсылки между правилом каталога и правилами модуля расписаний
- состояние `fixed` у окна 45 реестра

Полный перечень артефактов ПАРТИИ — `10-24-PLAN.md`, раздел `## Артефакты, которые создаёт эта партия`.

## Что этот план НЕ закрывает, и основание у каждого пункта своё

- **Окно 39 — поведение боевого драйвера на неограниченном входе.** Основанием служит ОТСУТСТВИЕ
  ВТОРОГО ДРАЙВЕРА У СУИТЫ: она идёт на встроенной базе в памяти, второго драйвера у неё нет, и
  величина `2147483648` проходит в неё молча, тогда как asyncpg на ней даёт отказ данных. Партия
  сделала вопрос МЕНЕЕ важным (отказ ставится ДО драйвера на каждом входе), но не ответила на него.
  Окно остаётся открытым, и **фаза-владелец ему НЕ НАЗНАЧАЕТСЯ**.
- **Окно 47 — восьмое измерение Найквиста.** Пятая партия шла тем же режимом закрытия гейпов, который
  разведку НЕ ЗАПУСКАЕТ, и ни `10-RESEARCH.md`, ни `10-VALIDATION.md` у фазы не появилось. Объявлять
  измерение закрытым по числу исполненных планов значило бы завести запись шире дерева — при девяти
  новых планах ровно так же, как при семи. **Фаза-владелец НЕ НАЗНАЧЕНА НАМЕРЕННО.**
- **Окна 49, 51 и 52.** Каждое несёт своё записанное основание, и ни одно из них не есть отсутствие
  замера: 49 — отсутствие наблюдаемой опоры (шаг обхода не пройден ни разу); 51 — изъятие из
  запертого решения владельца, чеканить которое плану запрещено; 52 — соглашение, действующее на
  КАЖДЫЙ файл проекта, то есть принадлежащее владельцу вехи. Ни одному из трёх настоящий план
  фазы-владельца НЕ НАЗНАЧАЕТ.
- **Окно 48 — долг направления отказа семнадцати команд планов `10-01`…`10-07`.** Партия его НЕ
  ПРАВИТ по основанию четвёртой партии: планы написаны ДО появления контракта, ИСПОЛНЕНЫ и
  ЗАКОММИЧЕНЫ, и правка требовала бы переписывания истории. Ни одна команда девяти планов пятой
  партии к этому долгу не прибавлена.

<verification>
- `uv run pytest tests/test_pages/test_identifier_bounds.py tests/test_pages/test_editor_schedules.py -q` — зелено.
- `uv run pytest tests/ -q -m "not planning"` — зелено.
- Красный правила каталога на синтетическом исходнике приведён в сводке дословно.
- Красный правила с заведомо неверным объявленным числом приведён в сводке дословно.
- Вывод прогона на пустом наборе исходников приведён в сводке.
- Шесть чисел шапки реестра (три ДО, три ПОСЛЕ) приведены в сводке, и переходы сходятся.
- Четыре утверждения о наступлении условия окна 45, каждое со своим числом, приведены в сводке.
</verification>

<success_criteria>
Правило полноты границы разбирает весь каталог страничного слоя, его вселенная объявлена числом,
поставленным прогоном покрасневшего правила и сличённым с суммой закрытого партией; слепая зона
названа именованным приложением с обоснованием и числом; прежние правила модуля расписаний целы и
связаны с новым встречными отсылками; окно 45 закрыто командой ведения журнала с записью о том, чем
именно наступило названное им самим условие, и без назначения фазы-владельца какому-либо открытому
окну.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-30-SUMMARY.md` по завершении.
</output>
