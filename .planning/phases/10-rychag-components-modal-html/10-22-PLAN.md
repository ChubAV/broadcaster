---
phase: 10-rychag-components-modal-html
plan: 22
type: execute
wave: 2
# ⚠️ ЗАВИСИМОСТЬ ОТ 10-18 — ПО ФАЙЛУ: оба плана правят `app/pages/schedules.py`. План 10-18
# меняет там сигнатуру сборки адреса и вешает гард происхождения; настоящий план правит
# докстринг сборки фрагмента. Две правки одного файла в одной волне столкнулись бы на файле.
depends_on: ["10-18"]
gap_closure: true
files_modified:
  - tests/test_pages/test_htmx_gates.py
  - app/pages/schedules.py
  - app/templates/ads/partials/sched_delete_response.html
  - app/templates/components/modal.html
autonomous: true
requirements: [FORM-06]

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ — форма унаследована
# от планов 10-12…10-21. ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `workflow.security_asvs_level` → `1`;
# `workflow.security_block_on` → `"high"`; `workflow.security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ОТКАЗ ВАЛИДАЦИИ СЫРЫМ ТЕЛОМ МИМО СЛОЯ ОТВЕТА ОБЪЯВЛЕН РАСХОЖДЕНИЕМ, А НЕ ОСТАВЛЕН УМОЛЧАНИЕМ: пять входов границы величины идентификатора несут ЗАЯВЛЕННОЕ расхождение с правилом «форму ответа выбирает слой ответа», с названной ценой, условием снятия и состоянием решения"
    - "АВТОРСТВО ИЗЪЯТИЯ ИЗ ЗАПЕРТОГО D-01 ОСТАЁТСЯ У ВЛАДЕЛЬЦА, И ЭТО ПРИНУЖДАЕТСЯ МАШИННО: единственный прецедент изъятия в проекте (D-08) записан ВЛАДЕЛЬЦЕМ на этапе обсуждения, поэтому план заводит РЕЕСТР РАСХОЖДЕНИЯ и передаёт решение окном по форме окон 34 и 47, а запись, объявившая себя решённой без ссылки на решение владельца либо номер закрытого окна, краснит прогон"
    - "МОЛЧАЛИВОЕ УМОЛЧАНИЕ ОТВЕРГНУТО ПРЯМО, И ЭТО ЗАПИСАНО: из трёх возможных исходов — перенести границу внутрь обработчика, объявить изъятие, промолчать — худшим назван третий, потому что он читается как исполненный инвариант"
    - "РЕЕСТР РАСХОЖДЕНИЯ НЕСЁТ ОБЪЯВЛЕННОЕ ЧИСЛО И ПРОВЕРЯЕТСЯ ЗАМЕРОМ: вход, у которого граница величины стои́т на уровне фреймворка и который в перечне не назван, краснит прогон — перечень не может стать списком того, до чего не дошли руки"
    - "УТВЕРЖДЕНИЕ «ПРЕДМЕТ ЕСТЬ ЗАПИСЬ, А НЕ ПОВЕДЕНИЕ» ПОМЕЧЕНО ОПРОВЕРГНУТЫМ: у предмета есть воспроизводимый вход, он назван в записи поимённо, и читатель видит, ЧЕМ прочтение опровергнуто"
    - "ЦЕНА ОТЛОЖЕННОЙ ПРАВКИ НАЗВАНА, А НЕ УМОЛЧАНА: цель третьего внеполосного узла остаётся статической, и что это значит для документа, открытого на другом объявлении, сказано в записи прямо"
    - "УТВЕРЖДЕНИЕ О ПЛОЩАДКЕ ПРИЗЕМЛЕНИЯ ФОКУСА СУЖЕНО ДО ИЗМЕРЕННОГО: к названной решением D-11 цене прибавлены ДВА измеренных факта — у площадки нет доступного имени, и приземление фокуса в область живого объявления даёт расходящееся поведение рантаймов"
    - "ЧТО ИМЕННО ОСТАЛОСЬ ОТ ПУНКТА ПРИЗЕМЛЕНИЯ И КОМУ ОНО ПРИНАДЛЕЖИТ — НАЗВАНО: остаток передан сводному обходу доступности Фазы 15 (GATE-09), и это НЕ новое назначение, а уже записанная отложенная идея"
  artifacts:
    - "tests/test_pages/test_htmx_gates.py — реестр заявленных РАСХОЖДЕНИЙ формы отказа валидации с объявленным числом, условием снятия и состоянием решения «ЖДЁТ ВЛАДЕЛЬЦА» у каждой записи"
    - "tests/test_pages/test_htmx_gates.py — правило полноты расхождения по замеру, правило авторства решения и два контроля зубов"
    - "10-22-SUMMARY.md — ТЕКСТ ТРЕТЬЕГО ОКНА, передающего владельцу решение об изъятии из D-01, выписанный дословно для переноса задачей 2 плана 10-23"
    - "app/pages/schedules.py — докстринг сборки фрагмента с ПОМЕЧЕННЫМ опровергнутым утверждением и названной ценой отложенной правки"
    - "app/templates/ads/partials/sched_delete_response.html — шапка ответа с той же пометкой и тем же названным входом"
    - "app/templates/components/modal.html — шапка рычага: утверждение о площадке приземления сужено до измеренного, остаток назван и передан"
  key_links:
    - "заявленное расхождение → инвариант «форму ответа выбирает слой ответа»: единственное, чем нарушение инварианта отличается от исполненного инварианта; ломается — читатель принимает молчание за исполнение, и это худший из трёх исходов"
    - "состояние решения записи → окно реестра, ждущее владельца: единственное, чем расхождение не превратится в изъятие правкой одной строки без владельца; ломается — план чеканит изъятие из запертого решения чужой властью, и следующий читатель принимает это за решение владельца"
    - "пометка опровергнутого утверждения → воспроизводимый вход: единственное, чем следующий читатель узнает, что предмет — поведение; ломается — тест, утверждающий дефектное поведение нормой, остаётся единственной записью о нём"
    - "суженное утверждение о площадке → измеренные факты: единственное, чем пункт не читается исполненным без остатка; ломается — сводный обход доступности Фазы 15 получит пункт, объявленный закрытым"
  prohibitions:
    - statement: "граница величины идентификатора НЕ переносится внутрь обработчиков: план 10-12 свёл её к ОДНОМУ владельцу на все пять входов, и перенос внутрь пяти обработчиков развёл бы одно правило на пять мест — то есть закрыл бы находку ценой возврата предмета, ради которого партия 10-12 существовала"
      status: flagged-unverified
      verification: none
    - statement: "цель третьего внеполосного узла НЕ становится динамической этим планом: правка меняет цель узла у всех потребителей и наблюдается ЛИНЕЙКОЙ СЧЁТЧИКА — шагом 1.8 обхода `10-UAT.md`, который не пройден ни разу, поэтому наблюдаемой опоры для такой правки сегодня нет ни одной"
      status: flagged-unverified
      verification: none
    - statement: "площадка приземления фокуса НЕ меняется этим планом: её выбрало решение владельца D-11 с названной ценой, а остаток пункта уже передан отложенной идеей Фазы 15 (GATE-09); партия закрытия гейпов не пересматривает решений владельца и не сочиняет назначений"
      status: flagged-unverified
      verification: none
    - statement: "опровергнутые формулировки НЕ ВЫЧЁРКИВАЮТСЯ: идиома D-30/D-32 требует пометки с названным преемником; стёртая формулировка лишает читателя знания о том, что прочтение вообще понадобилось, — а стоимость такой ошибки чтения эта фаза уже измерила гейпом 1 первого круга"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ НАД FORM-06 — ЯВНЫЕ ДОПУЩЕНИЯ ПАРТИИ (полная запись — 10-17-PLAN.md):
  #   • `idempotency` — продуктовая половина закрыта планом 10-06; повтор в РАНТАЙМЕ браузера
  #     наблюдаем только обходом `10-UAT.md`, шаги 1.4-1.9, не пройденные ни разу. `unresolved`.
  #   • `concurrency` — адресован частично планом 10-07 и проверками 2 и 3 обхода; полного
  #     машинного критерия в суите без процесса выразить нечем. `unresolved`.
  # 2 предъявленных = 0 разрешённых критерием + 2 явных допущения.
---

<objective>
Закрываются ТРИ находки ревизии кода, у которых ОДИН предмет: **запись утверждает больше, чем
исполняет код**, — и все три ревизия сама предлагает закрыть ЛИБО правкой поведения, ЛИБО ЯВНОЙ
записью, назвав молчание худшим из исходов.

`WR-03` — отказ валидации пяти входов уходит сырым телом мимо слоя ответа, что противоречит
объявленному инвариантом «форму ответа выбирает слой ответа, а не обработчик». Достижимость из
интерфейса нулевая, поэтому это WARNING; но умолчание читается как исполненный инвариант.
`WR-02` — докстринг сборки фрагмента и шапка шаблона объявляют предмет третьего внеполосного узла
«ЗАПИСЬЮ, а не поведением», тогда как у предмета есть воспроизводимый вход.
`WR-04` — шапка рычага объявляет пункт площадки приземления фокуса исполненным, тогда как на
фрагментном пути область пуста ВСЕГДА, доступного имени у неё нет, а приземление фокуса в область
живого объявления даёт расходящееся поведение рантаймов.

⚠️ **ПОЧЕМУ ЗДЕСЬ ЗАКРЫВАЕТСЯ ЗАПИСЬ, А НЕ ПОВЕДЕНИЕ, — ОСНОВАНИЕ НАЗВАНО ТРЕМЯ ПУНКТАМИ И НИ ОДИН
ИЗ НИХ НЕ «ЭТО ТРУДНО».**
(а) `WR-03`: перенос границы внутрь обработчиков РАЗВЁЛ БЫ одно правило на пять мест, тогда как
план 10-12 только что свёл его к ОДНОМУ владельцу ради закрытия блокеров `CR-01` и `CR-02`. Правка
закрыла бы находку ценой возврата предмета, ради которого существовала предыдущая партия.
(б) `WR-02`: правка меняет цель внеполосного узла у ВСЕХ потребителей, и наблюдается она ЛИНЕЙКОЙ
СЧЁТЧИКА — шагом 1.8 обхода `10-UAT.md`. Обход не пройден НИ РАЗУ, то есть наблюдаемой опоры,
относительно которой правку можно было бы судить, сегодня НЕ СУЩЕСТВУЕТ.
(в) `WR-04`: площадку выбрало РЕШЕНИЕ ВЛАДЕЛЬЦА **D-11** с названной там же ценой, а остаток пункта
уже передан отложенной идеей «приземление фокуса на location-пути — сводный обход доступности Фазы
15 (GATE-09)». Партия закрытия гейпов решений владельца не пересматривает и назначений не сочиняет.

Все три отложенные половины получают ОКНО реестра; ТЕКСТЫ окон пишет настоящий план в свою сводку,
а СТРОКУ реестра дописывает план 10-23 — владелец реестра в партии один, и это то же решение, которым
третья партия развела авторов и владельца.

Output: заявленное РАСХОЖДЕНИЕ формы отказа валидации с числом, условием снятия и состоянием
решения; текст окна, передающего владельцу решение об изъятии из D-01;
две помеченные опровергнутыми формулировки с названными воспроизводимыми входами; суженное до
измеренного утверждение о площадке приземления фокуса с названным владельцем остатка.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-12-SUMMARY.md
@app/pages/schedules.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: сквозной срез — форма отказа валидации пяти входов объявлена РАСХОЖДЕНИЕМ с числом и обоснованием, а РЕШЕНИЕ об изъятии из D-01 передано владельцу</name>

  <files>
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py:400-480` — реестр `OFFSET_CURSOR_EXCEPTIONS` ЦЕЛИКОМ:
  структура записи (`_OffsetCursorException`), поля `section`, `templates`, `handler`, `reason`,
  `lifting_phase`, `lifting_condition`; константы `LIFTING_CONDITION`, `LIFTING_PHASE`;
  `OFFSET_CURSOR_EXCEPTIONS_DECLARED = 2` вместе с ЛЕТОПИСЬЮ. ⚠️ ЭТО ОБРАЗЕЦ ФОРМЫ, которую
  наследует новый реестр целиком: поля, обоснование у каждой записи, число с летописью.
- `tests/test_pages/test_htmx_gates.py` — правила, стерегущие тот реестр (число, непустота
  обоснований): их имена и форма отказа. Новые правила встают рядом и наследуют её.
- `app/pages/schedules.py:85-91` — `ScheduleIdPath`, `AdIdForm`, `AccountIdForm`: три псевдонима, на
  которых стои́т граница величины. ⚠️ ЧИТАТЬ ВМЕСТЕ С `:58-68` — летописью имени `ID_MAX` и
  основанием, по которому граница взята по верхней границе колонки.
- `app/pages/schedules.py:805`, `:894`, `:1022` — места применения псевдонимов. ⚠️ ПЯТЬ ВХОДОВ —
  ЭТО НЕ ТРИ ПСЕВДОНИМА: перечень пяти снять ЗАМЕРОМ по модулю и привести в сводку, а не переписать
  из отчёта верификации. Отчёт называет их результатом («delete, edit, toggle, new(ad_id),
  new(account_id)»), а реестр обязан стоять на замере.
- `tests/test_pages/test_confirm_delete_transport.py` — правило
  `test_no_schedule_route_answers_with_a_handler_failure_on_an_out_of_range_identifier` и константа
  ожидаемого исхода отказа валидации. ⚠️ ЭТОТ ФАЙЛ НЕ ПРАВИТСЯ настоящим планом: он предмет плана
  10-19 в этой же партии. Читать — да, править — нет.
- `10-REVIEW.md:236-279` — `WR-03` целиком, включая три названных исхода и прямое «молчаливое
  умолчание — худший из трёх вариантов».
- `10-12-SUMMARY.md` — что именно план 10-12 свёл к одному владельцу и почему: основание пункта (а)
  раздела `<objective>` берётся оттуда, а не из памяти.
- `.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md:52-58`, **D-01** — формулировка
  инварианта «форма ответа выбирается ПО КЛАССУ ДЕЙСТВИЯ». ⚠️ ЭТО ЗАПЕРТОЕ РЕШЕНИЕ ВЛАДЕЛЬЦА, а не
  вывод плана.
- `.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md:124-135`, **D-08** — ЕДИНСТВЕННЫЙ
  прецедент изъятия из D-01 в этом проекте. ⚠️ ЧИТАТЬ ВМЕСТЕ С D-01 И УВИДЕТЬ ГЛАВНОЕ: он назван
  «**ИМЕНОВАННОЕ ИЗЪЯТИЕ ИЗ D-01**» и записан **ВЛАДЕЛЬЦЕМ НА ЭТАПЕ ОБСУЖДЕНИЯ**, с измеренным
  основанием. Из формы прецедента следует авторство: изъятие из запертого решения владельца чеканит
  ВЛАДЕЛЕЦ, а не план. Настоящая задача поэтому НЕ ОБЪЯВЛЯЕТ изъятия — она заводит РЕЕСТР
  РАСХОЖДЕНИЯ и передаёт решение владельцу окном (пункт 3 раздела `<action>`).
- Строки окон **34** и **47** `.planning/WINDOWS.md` — форма записи решения, ЖДУЩЕГО ВЛАДЕЛЬЦА:
  без назначенной фазы-снимателя и с записанным основанием, почему назначение принадлежит владельцу.
  Ту же форму наследует третье окно настоящего плана.
  </read_first>

  <precondition>
Прогон `uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly` на непрáвленом дереве
зелен, и число его правил снято ДО правки. Без этого числа нельзя будет отличить «правило
прибавилось» от «правило переименовалось».
  </precondition>

  <behavior>
1. Реестр перечисляет входы, у которых граница величины идентификатора стои́т на уровне фреймворка и
   потому отказ уходит мимо слоя ответа. У каждой записи — вход, псевдоним, названная ЦЕНА, условие
   снятия и СОСТОЯНИЕ РЕШЕНИЯ.
1a. ⚠️ РЕЕСТР ОБЪЯВЛЯЕТ РАСХОЖДЕНИЕ С D-01, А НЕ ИЗЪЯТИЕ ИЗ НЕГО, И ЭТО РАЗНЫЕ ВЕЩИ. D-01 —
   ЗАПЕРТОЕ решение владельца («форма ответа выбирается ПО КЛАССУ ДЕЙСТВИЯ»); единственный прецедент
   изъятия из него, D-08, назван в том же документе «ИМЕНОВАННЫМ ИЗЪЯТИЕМ ИЗ D-01» и записан
   ВЛАДЕЛЬЦЕМ на этапе обсуждения. Авторство изъятия принадлежит владельцу, и план его не чеканит.
   Каждая запись реестра несёт поле состояния решения со значением «ЖДЁТ ВЛАДЕЛЬЦА», а само решение
   передаётся окном реестра `.planning/WINDOWS.md` по форме окон 34 и 47 — без сочинённой
   фазы-снимателя.
1b. МАШИННЫЙ ГЕЙТ ОТ ЭТОГО НЕ СЛАБЕЕТ, И В ЭТОМ ВЕСЬ СМЫСЛ РАЗВЕДЕНИЯ: ревизия назвала молчаливое
   умолчание ХУДШИМ из трёх исходов, и реестр расхождения запрещает его ровно так же, как запрещал
   бы реестр изъятия — вход, разошедшийся с D-01 и не названный, краснит прогон. Отложено РЕШЕНИЕ о
   легитимности расхождения, а не его ВИДИМОСТЬ.
2. Цена названа ДВУМЯ следствиями, а не словом: тело отказа не есть страница, и на пути разметочного
   рантайма код отказа не несёт заголовка перехода, поэтому ветвь закрытия панели не срабатывает и
   панель остаётся открытой без объяснения.
3. Число записей объявлено константой с летописью; расхождение краснит прогон.
4. Правило полноты: вход, у которого граница стои́т на уровне фреймворка и который в реестре не
   назван, краснит прогон с именем входа.
5. Правило обоснований: пустое обоснование, пустое состояние решения или пустое условие снятия
   краснят прогон — перечень без обоснований есть список того, до чего не дошли руки.
5a. Правило состояния решения: запись, объявившая себя РЕШЁННОЙ, обязана называть, ЧЕМ решена —
   идентификатором решения владельца (по форме `D-NN`) либо номером закрытого окна реестра. Запись,
   переведённая в «решено» без ссылки на решение владельца, краснит прогон: план, дочеканивший
   изъятие сам, покраснит собственный гейт, а не пройдёт его молча.
6. Зубы: реестр без одной записи краснит правило полноты; запись с пустым обоснованием краснит
   правило обоснований.
  </behavior>

  <action>
**(1) Снять перечень входов ЗАМЕРОМ.** Обойти `app/pages/schedules.py` и найти ВСЕ места, где
граница величины идентификатора выражена псевдонимом с ограничением на уровне фреймворка. Привести
в сводку перечень и его число. ⚠️ ЕСЛИ ЗАМЕР ДАСТ НЕ ПЯТЬ — это НАХОДКА: назвать расхождение с
отчётом верификации поимённо и записать реестр ПО ЗАМЕРУ, а не по отчёту. Отчёт называл ИСХОДЫ пяти
запросов, а реестр стоит на входах, и совпадение этих двух чисел — предположение, пока не измерено.

**(2) Завести реестр `VALIDATION_REFUSAL_DIVERGENCES`.** ⚠️ ИМЯ НЕСЁТ ПРЕДМЕТ: реестр объявляет
РАСХОЖДЕНИЕ с D-01, а не ИЗЪЯТИЕ из него (основание — пункт 1a раздела `<behavior>`). Структуру
записи и её поля взять у `_OffsetCursorException` по форме: вход (маршрут либо поле формы), псевдоним
границы, обоснование, условие снятия — и ДОБАВИТЬ поле `decision_state` со значением
`ЖДЁТ ВЛАДЕЛЬЦА` у каждой записи, потому что решение об изъятии из запертого D-01 планом не
чеканится. Обоснование каждой записи обязано называть ЦЕНУ ДВУМЯ
СЛЕДСТВИЯМИ (см. пункт 2 раздела `<behavior>`) и достижимость — нулевую из интерфейса, с
основанием: идентификаторы приходят из разметки, поле выбора аккаунта шлётся радиокнопками и при
снятом выборе не шлётся вовсе. ⚠️ ОБОСНОВАНИЕ ПИШЕТСЯ ПО ЗАМЕРУ, а не пересказом ревизии: если
достижимость окажется ненулевой хоть у одного входа, это НАХОДКА для сводки и повод остановиться,
а не строка обоснования.

**(3) ПЕРЕДАТЬ РЕШЕНИЕ ВЛАДЕЛЬЦУ ОКНОМ, А НЕ ЧЕКАНИТЬ ЕГО ПЛАНОМ.** Условие снятия расхождения:
оно снимается вместе с переносом границы внутрь обработчиков, а перенос осмыслен только вместе с
массовым переводом маршрутов на слой ответа. Фазу-сниматель взять ИЗ РОАДМАПА, а не назначить.
⚠️ ЕСЛИ В РОАДМАПЕ ТАКОГО РАЗДЕЛА НЕТ — фазу НЕ НАЗНАЧАТЬ и записать это прямо: назначение
принадлежит владельцу, и сочинять его планом запрещено (прецедент окна 34).

⚠️ **И ГЛАВНОЕ — ВЫПИСАТЬ ТЕКСТ ТРЕТЬЕГО ОКНА** (первые два выписывают задачи 2 и 3 настоящего
плана). Предмет окна: пять входов страничного слоя расходятся с ЗАПЕРТЫМ решением D-01 — форма
отказа выбирается не по классу действия, а фреймворком, — и ВОПРОС, объявить ли это ИМЕНОВАННЫМ
ИЗЪЯТИЕМ ИЗ D-01 по прецеденту D-08 либо снять расхождение переносом границы, **ЖДЁТ ВЛАДЕЛЬЦА**.
Обязательные части текста: (а) названные ПЯТЬ входов по замеру пункта (1); (б) цена, названная ДВУМЯ
следствиями (тело отказа не есть страница; на пути разметочного рантайма код отказа не несёт
заголовка перехода, ветвь закрытия панели не срабатывает, панель остаётся открытой без объяснения);
(в) измеренная достижимость из интерфейса; (г) ссылка на прецедент D-08 как на ФОРМУ, которой
решение обязано быть записано, если владелец его примет; (д) ФАЗА-СНИМАТЕЛЬ НЕ НАЗНАЧЕНА, и основание
записано: изъятие из запертого решения владельца принадлежит владельцу (прецедент окон 34 и 47).
Текст уходит в сводку ДОСЛОВНО — переносит его в реестр задача 2 плана 10-23, ОДИН владелец записи.

⚠️ **ПОЧЕМУ НЕ `checkpoint:decision`, И ЭТО НАЗВАНО ПРЯМО.** Чекпойнт решения остановил бы волну 2 до
ответа владельца, тогда как предмет ЗАДАЧИ — не изъятие, а ВИДИМОСТЬ расхождения, и она нужна
независимо от того, каким будет решение: ревизия назвала молчаливое умолчание ХУДШИМ из трёх исходов.
Окно передаёт решение владельцу, ничего не чеканя, и это ровно та форма, которой партия уже
пользуется (окна 34 и 47, план 10-23). ⚠️ ЕСЛИ ВЛАДЕЛЕЦ ОТВЕТИТ ДО ИСПОЛНЕНИЯ — записать его ответ
идентификатором решения в поле `decision_state`, и правило пункта (5a) это примет; сочинять ответ за
него ЗАПРЕЩЕНО.

**(4) Объявить число.** `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` с ЛЕТОПИСЬЮ по форме
`OFFSET_CURSOR_EXCEPTIONS_DECLARED`: объявленное число, откуда взялось (замер по модулю), чем
измерено, каким планом (10-22, четвёртая партия закрытия гейпов Фазы 10).

**(5) Правила.** `test_the_number_of_validation_refusal_divergences_is_declared` — длина реестра
равна объявленному числу, и отказ говорит, что молча выросший перечень означает
принятое-но-неназванное расхождение, а молча похудевший — снятое расхождение при нетронутой границе.
`test_every_framework_bounded_input_is_declared_as_a_divergence` — правило ПОЛНОТЫ: вход, найденный
замером и не названный реестром, краснит прогон с именем.
`test_every_validation_refusal_divergence_carries_a_non_empty_rationale` — обоснование, состояние
решения и условие снятия непусты у каждой записи. ⚠️ ЕСЛИ ФАЗА-СНИМАТЕЛЬ НЕ НАЗНАЧЕНА (пункт 3),
правило требует непустого ОБЪЯСНЕНИЯ вместо непустого имени фазы, и это записывается в его
докстринге, а не обходится ослаблением утверждения.

**(5a) Правило авторства решения `test_no_divergence_claims_a_decision_the_owner_did_not_make`.**
Запись, чьё состояние решения ОТЛИЧНО от «ЖДЁТ ВЛАДЕЛЬЦА», обязана называть ЧЕМ решена —
идентификатором решения владельца по форме `D-NN` либо номером закрытого окна реестра. Отказ говорит
прямо: D-01 есть ЗАПЕРТОЕ решение владельца, единственный прецедент изъятия из него (D-08) записан
ВЛАДЕЛЬЦЕМ на этапе обсуждения, и изъятие, дочеканенное планом либо исполнителем, есть присвоение
чужого авторства. ⚠️ ЭТО ПРАВИЛО СТЕРЕЖЁТ НЕ КОД, А ГРАНИЦУ ПОЛНОМОЧИЙ, и оно единственное, чем
следующий круг не превратит «ждёт владельца» в «решено» одной правкой строки.

**(6) Два контроля зубов.** `test_control_a_shortened_exception_list_reddens_the_completeness_rule` —
подать правилу полноты КОПИЮ реестра без одной записи: правило краснеет и называет пропущенный вход.
`test_control_an_empty_rationale_reddens_the_rationale_rule` — копия реестра с опустошённым
обоснованием одной записи: правило краснеет и называет её. Копии живут в памяти; боевой реестр не
правится — приём взят у контроля перечня осей
(`tests/test_pages/test_confirm_delete_transport.py:2992`).

**(7) Что НЕ трогается.** `app/pages/schedules.py` — настоящей задачей НИ НА СИМВОЛ: она объявляет
РАСХОЖДЕНИЕ, а не меняет границу (перенос границы внутрь обработчиков объявлен прохибицией плана).
`.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md` — НИ НА СИМВОЛ: решения владельца
правит владелец, и план, дописавший себе изъятие в запертый документ, закрыл бы вопрос авторства
подделкой источника. `.planning/WINDOWS.md` — не настоящей задачей: её дело ВЫПИСАТЬ текст окна в
сводку, а пишет реестр ОДИН владелец, задача 2 плана 10-23.
`tests/test_pages/test_confirm_delete_transport.py` — предмет плана 10-19 той же партии.
`OFFSET_CURSOR_EXCEPTIONS` и его число.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` не выросло относительно числа, снятого предусловием, — шесть новых правил (число, полнота, обоснования, авторство решения, два контроля зубов) не заведены</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly -k "validation_refusal or framework_bounded or divergence"</automated>
    <fails_when>вывод содержит `no tests ran` либо `failed` — правила реестра расхождения не заведены под названными именами либо не зелены</fails_when>
    <automated>git diff --name-only -- app/ | grep -v -e '^app/pages/schedules.py$' -e '^app/pages/htmx.py$' -e '^app/pages/accounts.py$' -e '^app/pages/ads.py$' -e '^app/pages/account_groups.py$' -e '^app/pages/common.py$' || true</automated>
    <fails_when>вывод не пуст — тронут путь `app/`, НЕ ПРИНАДЛЕЖАЩИЙ законному следу плана 10-18, тогда как предмет настоящей задачи — ОБЪЯВЛЕНИЕ расхождения, а не смена границы. ⚠️ ШЕСТЬ ВЫЧТЕННЫХ ПУТЕЙ — `files_modified` ПЛАНА 10-18, ВОЛНА 1: он завершается ДО волны 2, поэтому голая форма (`app/` целиком) к этой точке заведомо непуста и краснела бы от чужой ЗАКОННОЙ работы отказом с неверной атрибуцией — идиома взята у пункта 4 раздела `<verification>` плана 10-17. СОБСТВЕННЫЙ СЛЕД НАСТОЯЩЕГО ПЛАНА В `app/` НЕ ВЫЧТЕН НАМЕРЕННО: он появляется в задачах 2-3, то есть ПОСЛЕ настоящей, и оба шаблона (`app/templates/ads/partials/sched_delete_response.html`, `app/templates/components/modal.html`) остаются ПРЕДМЕТОМ гейта — вычет собственного следа превратил бы гейт в тавтологию, зелёную и молчаливую</fails_when>
  </verify>

  <acceptance_criteria>
- Перечень входов снят ЗАМЕРОМ: в сводке приведена команда замера, её вывод и получившееся число.
  Расхождение с числом `5` отчёта верификации, если оно есть, названо поимённо.
- Достижимость каждого входа из интерфейса проверена и названа: в сводке для каждого входа сказано,
  откуда приходит его значение, и на чём основана оценка достижимости.
- Реестр несёт у КАЖДОЙ записи пять полей, и ни одно не пусто: в сводке приведена одна запись
  целиком.
- Фаза-сниматель взята ИЗ РОАДМАПА либо НЕ НАЗНАЧЕНА с записанным основанием: в сводке сказано,
  какой из двух случаев, и приведена цитата из роадмапа либо цитата основания.
- РЕШЕНИЕ ОБ ИЗЪЯТИИ ИЗ D-01 НЕ ЧЕКАНИТСЯ ПЛАНОМ: у каждой записи реестра состояние решения равно
  «ЖДЁТ ВЛАДЕЛЬЦА», и в сводке приведён вывод прогона правила
  `test_no_divergence_claims_a_decision_the_owner_did_not_make` вместе с выводом его пробного
  прогона на копии записи, переведённой в «решено» БЕЗ ссылки (ожидается КРАСНЫЙ).
- ТЕКСТ ТРЕТЬЕГО ОКНА ВЫПИСАН ЦЕЛИКОМ И НЕСЁТ ВСЕ ПЯТЬ ОБЯЗАТЕЛЬНЫХ ЧАСТЕЙ пункта (3): в сводке
  приведён его текст дословно — его переносит в реестр задача 2 плана 10-23, и пересказ через круг
  запрещён.
- `10-CONTEXT.md` не тронут: `git diff --name-only -- .planning/phases/10-rychag-components-modal-html/10-CONTEXT.md` — пусто.
- Оба контроля зубов КРАСНЕЮТ на своём предмете: в сводке приведены оба вывода.
- Продукт не тронут: в сводке приведён вывод третьей команды раздела `<verify>` (пусто).
- Прогон целиком зелен: в сводке приведён вывод `uv run pytest tests/ -q -p no:randomly`.
  </acceptance_criteria>

  <reversibility rating="reversible">
Заводится реестр и пять правил в ОДНОМ тестовом модуле; поведения продукта задача не меняет ни на
строку. Откат возвращает молчаливое умолчание — то есть худший из трёх названных ревизией исходов, и
это наблюдаемо, а не тихо.
  </reversibility>

  <done>
Форма отказа валидации объявлена ЗАЯВЛЕННЫМ РАСХОЖДЕНИЕМ с D-01 — с числом, обоснованием, условием
снятия и состоянием решения у каждой записи; полнота расхождения проверяется замером по модулю;
РЕШЕНИЕ о том, объявить ли расхождение именованным изъятием из D-01, передано ВЛАДЕЛЬЦУ окном по
прецеденту D-08 и окон 34/47, а не дочеканено планом, и присвоение этого авторства принуждено
отдельным правилом; оба контроля показали зубы; продукт не тронут, и молчание, названное ревизией
худшим исходом, исключено.
  </done>
</task>

<task type="execute">
  <name>Задача 2: утверждение «предмет есть ЗАПИСЬ, а не поведение» помечено опровергнутым, и цена отложенной правки названа</name>

  <files>
app/pages/schedules.py
app/templates/ads/partials/sched_delete_response.html
  </files>

  <read_first>
- `app/pages/schedules.py:1112-1180` — сборка фрагмента ответа удаления ЦЕЛИКОМ с докстрингом.
  ⚠️ ЧИТАТЬ ДОСЛОВНО ДВА МЕСТА: абзац «ЦЕНА НЕОТЛИЧИМОСТИ НАЗЫВАЕТСЯ ЧИСЛОМ» и заключительная
  формулировка «третий узел стои́т там по решению D-12 Фазы 9, и предмет `WR-05` есть ЗАПИСЬ, а не
  поведение». ВТОРАЯ И ЕСТЬ ПРЕДМЕТ ЗАДАЧИ. ⚠️ ИМЯ НАХОДКИ В ЭТОЙ СТРОКЕ — `WR-05` ПРЕЖНЕГО круга
  ревизии; в четвёртом круге тот же предмет назван `WR-02`. Пометка обязана назвать ОБА имени, иначе
  следующий читатель не свяжет строку кода с отчётом.
- `:1174` — строка, где число расписаний считается по `ad_id`: вход, который делает предмет
  поведением, а не записью.
- `app/templates/ads/partials/sched_delete_response.html:1-30` — шапка шаблона ЦЕЛИКОМ и `:73` —
  третий внеполосный узел со СТАТИЧЕСКИМ селектором цели. ⚠️ Шапка перечисляет три узла и говорит,
  чем является третий; пометка встаёт туда же.
- `app/templates/ads/form.html` — узел линейки счётчика и его `id`: чтобы пометка называла
  ПОЛОЖЕНИЕ статической цели, а не только её имя.
- `10-REVIEW.md:195-235` — `WR-02` четвёртого круга целиком, включая воспроизводимый вход («запрос
  называет адресом расписание объявления B, а полем контекста — экран объявления A») и обе
  предложенные развилки.
- `tests/test_pages/test_confirm_delete_transport.py:3231-3311` —
  `test_the_counter_node_belongs_to_the_ad_named_by_the_request`: правило, УТВЕРЖДАЮЩЕЕ сегодняшнее
  поведение нормой. ⚠️ ЭТОТ ФАЙЛ НЕ ПРАВИТСЯ настоящим планом — он предмет плана 10-19 той же партии.
  Читать обязательно: пометка обязана назвать это правило поимённо, потому что оно и есть вторая
  запись о том же предмете.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:296` — шаг 1.8 обхода, линейка
  счётчика: наблюдаемая опора, которой сегодня нет, и на которую ссылается основание отсрочки.
  </read_first>

  <precondition>
План 10-18 приземлён: правки `app/pages/schedules.py` этой партии сделаны, и настоящая задача пишет
поверх них. Иначе две правки одного файла разъедутся, и вторая перепишет докстринг, отредактированный
первой.
  </precondition>

  <behavior>
1. Формулировка «предмет есть ЗАПИСЬ, а не поведение» помечена ОПРОВЕРГНУТОЙ и остаётся на месте.
2. Пометка называет ЧЕТЫРЕ вещи: воспроизводимый вход; чем и когда опровергнуто (`WR-02`, ревизия
   2026-09-04, при том что прежнее имя того же предмета — `WR-05`); почему это НЕ регрессия
   привилегий (выборки скоуплены владельцем объявления); и ПОЧЕМУ правка отложена.
3. Цена отложенной правки названа ДВУМЯ следствиями: число расписаний ЧУЖОГО объявления попадает в
   документ, открытый на своём, и переживает запрос до перезагрузки.
4. Основание отсрочки названо НАБЛЮДАЕМОЙ ОПОРОЙ, а не трудностью: правка меняет цель внеполосного
   узла у всех потребителей и наблюдается шагом 1.8 обхода, который не пройден ни разу.
5. Шапка шаблона несёт ту же пометку с тем же названным входом: два места одного факта говорят
   одно.
6. Правило суиты, утверждающее сегодняшнее поведение нормой, названо ПОИМЁННО в обеих пометках.
  </behavior>

  <action>
**(1) Пометить формулировку в докстринге сборки фрагмента.** К заключительной формулировке добавить
пометку по идиоме D-30/D-32 `.planning/STATE.md`, содержащую ЧЕТЫРЕ части раздела `<behavior>`.
⚠️ ПРЕЖНЮЮ ФОРМУЛИРОВКУ НЕ СТИРАТЬ: она показывает, каким было прочтение, и стёртая лишает читателя
знания о том, что прочтение понадобилось. Назвать ОБА имени находки (`WR-05` прежнего круга и
`WR-02` четвёртого) и правило суиты, утверждающее поведение нормой, — поимённо.

**(2) Назвать цену.** Отдельным абзацем — два следствия пункта 3 раздела `<behavior>`, и рядом
достижимость: из интерфейса случай недостижим, потому что обе формы пути шлют идентификатор
объявления СВОЕГО экрана; границ привилегий случай не пересекает, потому что выборки скоуплены
владельцем объявления. ⚠️ ОБЕ ОЦЕНКИ ПЕРЕПРОВЕРИТЬ ЧТЕНИЕМ КОДА, а не переписать из ревизии: если
хоть одна не подтвердится, это НАХОДКА для сводки и повод остановиться, а не строка записи.

**(3) Назвать основание отсрочки.** Отдельной строкой: правка меняет цель внеполосного узла у ВСЕХ
потребителей, а наблюдается она линейкой счётчика — шагом 1.8 обхода `10-UAT.md`, не пройденным ни
разу; наблюдаемой опоры для суждения о правке сегодня нет. ⚠️ ОСНОВАНИЕМ СЛУЖИТ ОТСУТСТВИЕ
НАБЛЮДАЕМОЙ ОПОРЫ, А НЕ ОЦЕНКА ЗАТРАТ. Перечень запрещённых в записи слов стои́т отрицательным
поиском в `<acceptance_criteria>` этой задачи и ЗДЕСЬ НЕ ПОВТОРЯЕТСЯ НАМЕРЕННО: слово, выписанное в
задании, приезжает в файл вместе с ним и само роняет собственный поиск.

**(4) Повторить пометку в шапке шаблона.** Тем же содержанием и с тем же названным входом. Шапка уже
перечисляет три узла и говорит, чем является третий; пометка встаёт туда, где сказано о третьем.

**(5) Выписать ТЕКСТ ОКНА в сводку.** Окно об отложенной правке цели третьего внеполосного узла:
предмет, воспроизводимый вход, цена двумя следствиями, достижимость, основание отсрочки и
фаза-владелец. ⚠️ СТРОКУ РЕЕСТРА `.planning/WINDOWS.md` НАСТОЯЩИЙ ПЛАН НЕ ПИШЕТ И ФАЙЛА В
`files_modified` НЕ НЕСЁТ: владелец реестра в партии один — план 10-23, — и это то же решение,
которым третья партия развела авторов текстов и владельца реестра.

**(6) Что НЕ трогается.** Цель третьего узла и её статический селектор. `app/templates/ads/form.html`.
Правило `test_the_counter_node_belongs_to_the_ad_named_by_the_request` и весь
`tests/test_pages/test_confirm_delete_transport.py`. Абзац «ЦЕНА НЕОТЛИЧИМОСТИ НАЗЫВАЕТСЯ ЧИСЛОМ» —
он верен и остаётся.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_editor_schedules.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — правка записи задела поведение, чего задача не предполагает вовсе</fails_when>
    <automated>grep -c 'ОПРОВЕРГНУТО' app/pages/schedules.py app/templates/ads/partials/sched_delete_response.html</automated>
    <fails_when>для любого из двух файлов выведено `0` — пометка поставлена только в одном месте, и два места одного факта снова говорят разное</fails_when>
    <automated>git diff --name-only -- .planning/</automated>
    <fails_when>вывод не пуст — план тронул запись; текст окна уезжает в сводку, а строку реестра пишет план 10-23</fails_when>
  </verify>

  <acceptance_criteria>
- Пометка содержит ВСЕ ЧЕТЫРЕ части: в сводке приведён её текст, и в нём видны воспроизводимый вход,
  оба имени находки с датой, оценка привилегий и основание отсрочки.
- Прежняя формулировка НЕ УДАЛЕНА: в сводке приведён вывод
  `git diff -- app/pages/schedules.py app/templates/ads/partials/sched_delete_response.html | grep -c '^-'`,
  и каждая удалённая строка названа поимённо с основанием.
- Обе оценки (недостижимость из интерфейса, отсутствие пересечения границ привилегий)
  ПЕРЕПРОВЕРЕНЫ чтением кода: в сводке названы файл и строка, на которых основана каждая.
- Правило суиты, утверждающее поведение нормой, названо поимённо в обеих пометках: в сводке
  приведены обе цитаты.
- Основание отсрочки не содержит оценок труда:
  `git diff -- app/pages/schedules.py app/templates/ads/partials/sched_delete_response.html | grep -ci -e 'сложн' -e 'трудоём' -e 'позже'`
  → `0`.
- Текст окна выписан в сводку целиком и несёт фазу-владельца.
- `.planning/` не тронут: в сводке приведён вывод третьей команды раздела `<verify>` (пусто).
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится только текст двух записей; ни поведения, ни разметки, ни правил суиты задача не меняет.
Откат равен обратной правке текста и наблюдаем командой поиска пометки.
  </reversibility>

  <done>
Утверждение «предмет есть ЗАПИСЬ, а не поведение» помечено опровергнутым в ОБОИХ местах, где оно
стои́т; названы воспроизводимый вход, оба имени находки, оценка привилегий и цена двумя следствиями;
основание отсрочки названо отсутствием наблюдаемой опоры, а не оценкой труда; текст окна выписан в
сводку для владельца реестра.
  </done>
</task>

<task type="execute">
  <name>Задача 3: утверждение о площадке приземления фокуса сужено до ИЗМЕРЕННОГО, остаток назван и передан</name>

  <files>
app/templates/components/modal.html
  </files>

  <read_first>
- `app/templates/components/modal.html:30-75` — шапка рычага, блок «ЧТО ИМЕННО ИСПОЛНЕНО, ТРЕМЯ
  УТВЕРЖДЕНИЯМИ» ЦЕЛИКОМ, и особенно пункт 1 («ПЛОЩАДКА ВЫБРАНА ОДИН РАЗ НА ВСЕХ — область
  уведомлений»). ⚠️ ЧИТАТЬ ДОСЛОВНО: предмет задачи — прочтение этого пункта как исполненного БЕЗ
  ОСТАТКА, а не сам выбор площадки.
- `:100-145` — пункт 2 с уже стоящей пометкой «АТРИБУЦИЯ ЭТИХ ДВУХ ВЕТВЕЙ … ОПРОВЕРГНУТА
  РАНТАЙМОМ», и абзац «ЧТО ЗДЕСЬ НЕ РЕШАЕТСЯ: приземление фокуса на пути ПЕРЕХОДА … отложено
  сводному обходу доступности Фазы 15 (GATE-09)». ⚠️ ЭТО ОБРАЗЕЦ ФОРМЫ ПОМЕТКИ, стоящий в том же
  файле, и ВТОРОЙ — уже записанный владелец остатка. Новая пометка наследует форму первого и
  ссылается на второй, а не заводит третье назначение.
- `:665` — выражение приземления `land(back)`: куда именно приземляется фокус, когда открывавшего
  узла в документе больше нет.
- `app/templates/includes/notice_area.html` — узел области уведомлений: его роль, признак живого
  объявления, признак программной фокусируемости и ОТСУТСТВИЕ доступного имени. ⚠️ ВСЕ ТРИ ФАКТА
  ПЕРЕПРОВЕРИТЬ ЧТЕНИЕМ, а не переписать из ревизии: пометка, стоящая на непроверенном факте, есть
  ровно тот дефект, который она чинит.
- `.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md`, **D-11** целиком — решение
  владельца о площадке приземления и НАЗВАННАЯ ТАМ ЖЕ ЦЕНА («на успехе область пуста»). ⚠️ ЦЕНА УЖЕ
  БЫЛА НАЗВАНА: задача прибавляет к ней ДВА измеренных факта, а не открывает решение заново.
- `.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md`, раздел `Deferred Ideas`, пункт
  «Приземление фокуса на location-пути … Фаза 15 (GATE-09)» — уже записанный владелец остатка.
- `10-REVIEW.md:280-321` — `WR-04` целиком, включая оба измеренных факта и предложенную правку.
- `10-VERIFICATION.md`, последний пункт `human_verification` — он прямо велит читать себя вместе с
  `WR-04`.
  </read_first>

  <precondition>
Прогон `uv run pytest tests/test_templates/ tests/test_pages/test_shell.py -q -p no:randomly` на
непрáвленом дереве зелен, и число правил снято. Задача правит ТОЛЬКО комментарий шаблона, поэтому
число обязано совпасть после правки; расхождение означало бы, что тронута разметка.
  </precondition>

  <behavior>
1. Пункт 1 блока исполненного получает пометку: он исполнен В ЧАСТИ выбора площадки и её
   единственности, и НЕ исполнен в части «фокус приезжает в осмысленное место».
2. К названной решением D-11 цене прибавлены ДВА ИЗМЕРЕННЫХ факта: у площадки нет доступного имени,
   и приземление фокуса в область живого объявления даёт расходящееся поведение рантаймов чтения с
   экрана.
3. Пометка называет, что D-11 НЕ ПЕРЕСМАТРИВАЕТСЯ: площадку выбрало решение владельца, цена там же
   названа, и настоящая правка сужает ПРОЧТЕНИЕ, а не решение.
4. Владелец остатка назван и НЕ ИЗОБРЕТЁН: сводный обход доступности Фазы 15 (GATE-09), уже
   записанный отложенной идеей в том же файле и в записи решений фазы.
5. Ни одна строка разметки не меняется: правка живёт в комментарии.
  </behavior>

  <action>
**(1) Перепроверить ТРИ факта чтением.** Область уведомлений: несёт ли она признак живого
объявления; несёт ли признак программной фокусируемости; есть ли у неё доступное имя. Привести все
три в сводку с файлом и строкой. ⚠️ ЕСЛИ ХОТЬ ОДИН НЕ ПОДТВЕРДИТСЯ — это НАХОДКА: остановиться,
назвать расхождение с ревизией и НЕ ПИСАТЬ пометку на непроверенном факте. Пометка, стоящая на
переписанном утверждении, есть дословно тот дефект, который она чинит.

**(2) Поставить пометку к пункту 1.** Форму взять у уже стоящей в этом же файле пометки пункта 2.
Содержание — четыре части раздела `<behavior>`: что исполнено, что не исполнено, два измеренных
факта, и прямое «решение D-11 не пересматривается — сужается ПРОЧТЕНИЕ». ⚠️ ТЕКСТ ПУНКТА 1 НЕ
СТИРАТЬ: он верен в своей части, и стёртый он унёс бы с собой основание, по которому площадка не
стала параметром макроса.

**(3) Назвать владельца остатка ссылкой, а не назначением.** Сослаться на уже стоящий в этом же
файле абзац об отложенном сводном обходе доступности Фазы 15 (GATE-09) и на пункт `Deferred Ideas`
записи решений фазы. ⚠️ НОВОГО НАЗНАЧЕНИЯ НЕ ЗАВОДИТЬ: партия закрытия гейпов назначений не
сочиняет, а два независимых назначения одного остатка разошлись бы молча.

**(4) Выписать ТЕКСТ ОКНА в сводку.** Окно об остатке пункта площадки: предмет, два измеренных
факта, что именно НЕ закрыто, и почему правка не делается партией (решение владельца D-11 плюс уже
записанный владелец остатка). ⚠️ СТРОКУ РЕЕСТРА `.planning/WINDOWS.md` НАСТОЯЩИЙ ПЛАН НЕ ПИШЕТ:
владелец реестра в партии один — план 10-23.

**(5) Что НЕ трогается.** Выражение приземления `land`, обе ветви ухода панели, `notice_area.html`
целиком, все атрибуты разметки панели, пункты 2 и 3 блока исполненного и уже стоящие в файле
пометки. Решение D-11 в записи фазы.

**(ПОСЛЕДНИЙ ШАГ ЗАДАЧИ) ОБНОВИТЬ ГРАФ ПРОЕКТА — `graphify update .`.** ⚠️ ЭТО ТРЕБОВАНИЕ
`./CLAUDE.md` ДОСЛОВНО: «After modifying code, run `graphify update .` to keep the graph current
(AST-only, no API cost)». Настоящий план правит файлы, которые граф индексирует, поэтому шаг
обязателен и стои́т ПОСЛЕДНИМ — после всех правок плана и после прогонов.

⚠️ **ПОЛЕ `graphify.enabled: false` В `.planning/config.json` ОСНОВАНИЕМ ДЛЯ ОТКАЗА НЕ ЯВЛЯЕТСЯ, И
ЭТО ЗАПИСАНО ПРЯМО, А НЕ ПРОПУЩЕНО МОЛЧА.** Замер планировщика 2026-09-05: конфигурация несёт
`"graphify": {"enabled": false}` — это ключ ИНТЕГРАЦИИ GSD, управляющий графом `.planning/graphs/`,
которого в дереве НЕТ вовсе (`ls -d .planning/graphs` → отсутствует). Граф ПРОЕКТА живёт в
`graphify-out/` (`ls -d graphify-out` → есть), команда доступна (`command -v graphify` → есть), и
свежести этого графа требует `./CLAUDE.md`, а не конфигурация GSD. ДВА РАЗНЫХ ГРАФА, ДВА РАЗНЫХ
ВЛАДЕЛЬЦА, и выключенный ключ одного не отменяет требования другого.

⚠️ **ПОБОЧНОГО ЭФФЕКТА НА ПРИЁМОЧНЫЕ КРИТЕРИИ ПАРТИИ НЕТ, И ЭТО ЗАМЕРЕНО:** `graphify-out/`
игнорируется гитом (`.gitignore:56`, `**/graphify-out/`) и деревом не отслеживается, поэтому шаг не
может тронуть ни один вывод `git diff`, на котором стоят гейты `<verify>` и приёмочные критерии
планов партии.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/ tests/test_pages/test_shell.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` отличается от числа, снятого предусловием, — правка вышла за пределы комментария</fails_when>
    <automated>git diff -U0 -- app/templates/components/modal.html | grep -c '^+[^+]' </automated>
    <fails_when>вывод `0` — пометка не поставлена вовсе</fails_when>
    <automated>git diff --name-only -- .planning/ app/templates/includes/notice_area.html</automated>
    <fails_when>вывод не пуст — план тронул запись решений либо область уведомлений, тогда как его предмет — прочтение утверждения в шапке рычага</fails_when>
    <automated>graphify update .</automated>
    <fails_when>ненулевой код возврата либо вывод содержит `Traceback` — граф проекта не обновлён, и следующий читатель получит на вопрос о коде устаревший ответ. ⚠️ `command not found` — НАХОДКА ДЛЯ СВОДКИ (инструмент не установлен в среде исполнения), а не молчаливый пропуск шага регламента</fails_when>
  </verify>

  <acceptance_criteria>
- Три факта об области уведомлений ПЕРЕПРОВЕРЕНЫ чтением: в сводке приведены файл, строка и вывод
  по каждому.
- Пометка содержит все четыре части: в сводке приведён её текст.
- Текст пункта 1 НЕ УДАЛЁН: в сводке приведён вывод
  `git diff -- app/templates/components/modal.html | grep -c '^-'`, и каждая удалённая строка
  названа поимённо с основанием.
- Владелец остатка назван ССЫЛКОЙ на уже записанное назначение: в сводке приведены обе цитаты — из
  шапки рычага и из записи решений фазы.
- Разметка не тронута: в сводке приведён вывод
  `git diff -- app/templates/components/modal.html | grep -c '^[-+][^-+]*<'` вместе с пояснением,
  какие строки он посчитал.
- Текст окна выписан в сводку целиком.
- Число правил суиты до и после совпадает, оба приведены в сводке.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится текст комментария в одном шаблоне; ни разметки, ни поведения, ни решения владельца задача
не меняет. Откат равен обратной правке текста.
  </reversibility>

  <done>
Утверждение о площадке приземления фокуса сужено до измеренного: названо, что исполнено, что нет, и
какие два факта измерены; решение D-11 не пересмотрено, а его прочтение — сужено; владелец остатка
назван ссылкой на уже записанное назначение; текст окна выписан в сводку для владельца реестра.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Новые символы `tests/test_pages/test_htmx_gates.py`:**

| Символ | Что это |
|---|---|
| структура записи расхождения формы отказа валидации | поля: вход, псевдоним границы, обоснование, условие снятия, состояние решения |
| `VALIDATION_REFUSAL_DIVERGENCES` | реестр заявленных РАСХОЖДЕНИЙ, собранный ЗАМЕРОМ по `app/pages/schedules.py` |
| `VALIDATION_REFUSAL_DIVERGENCES_DECLARED` | число с летописью по форме `OFFSET_CURSOR_EXCEPTIONS_DECLARED` |
| `test_the_number_of_validation_refusal_divergences_is_declared` | правило числа |
| `test_every_framework_bounded_input_is_declared_as_a_divergence` | правило ПОЛНОТЫ по замеру |
| `test_every_validation_refusal_divergence_carries_a_non_empty_rationale` | правило непустых обоснований |
| `test_no_divergence_claims_a_decision_the_owner_did_not_make` | правило АВТОРСТВА: «решено» без ссылки на решение владельца либо номер закрытого окна краснит прогон |
| `test_control_a_shortened_exception_list_reddens_the_completeness_rule` | зубы правила полноты |
| `test_control_an_empty_rationale_reddens_the_rationale_rule` | зубы правила обоснований |

**Изменённые записи (только текст, без строк кода и разметки):** докстринг сборки фрагмента в
`app/pages/schedules.py` (пометка опровергнутости с четырьмя частями и названная цена); шапка
`app/templates/ads/partials/sched_delete_response.html` (та же пометка); шапка
`app/templates/components/modal.html` (суженное до измеренного утверждение о площадке приземления).

**ТЕКСТЫ ТРЁХ ОКОН, ВЫПИСАННЫЕ В СВОДКУ, А НЕ В РЕЕСТР:** (1) окно об отложенной правке цели
третьего внеполосного узла (задача 2); (2) окно об остатке пункта площадки приземления фокуса
(задача 3); (3) окно о расхождении формы отказа валидации пяти входов с ЗАПЕРТЫМ решением D-01,
чьё изъятие ЖДЁТ ВЛАДЕЛЬЦА, — заводится БЕЗ фазы-снимателя по форме окон 34 и 47 и ставит ВОПРОС, а
не отвечает на него (задача 1, пункт 3 раздела `<action>`). ⚠️ ЧИСЛО ЗДЕСЬ ТРИ, А НЕ ДВА, И ЭТО
НЕСУЩЕЕ: третье окно — единственный путь, которым авторство изъятия из запертого решения остаётся у
владельца, и его тексты переносит ДОСЛОВНО задача 2 плана 10-23, читающая эту сводку. Реестр,
объявивший два, отдал бы владельцу реестра на одно окно меньше, чем партия выписала, а потерянное
окно неотличимо от незаведённого. Строки реестра `.planning/WINDOWS.md` дописывает план 10-23 —
владелец реестра в партии один.

**Новых путей продукта, миграций, зависимостей и правок `.planning/` план НЕ производит.**
</artifacts_this_phase_produces>

<threat_model>
⚠️ Таблица собрана на **ASVS 1** (`workflow.security_asvs_level` → `1`) с порогом блокировки
**`high`** (`workflow.security_block_on` → `"high"`) при `workflow.security_enforcement` → `true`.
Все три величины сняты `config-get`. Строка `*-SC` несёт `accept`: пакетов план не устанавливает.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| объявленный инвариант → фактическая форма отказа | нарушение, о котором не написано, читается как исполненный инвариант |
| запись о предмете находки → следующий читатель | «предмет есть запись» при поведенческом предмете отправляет следующего чинить не то |
| утверждение об исполненном пункте → сводный обход доступности | пункт, объявленный исполненным без остатка, не попадёт в сводный обход вовсе |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-22-01 | Repudiation | форма отказа валидации пяти входов | medium | mitigate | Изъятие ОБЪЯВЛЕНО с ценой, фазой-снимателем и условием (задача 1), полнота проверяется замером. Молчаливое умолчание названо ревизией худшим из трёх исходов и исключено |
| T-10-22-02 | Information Disclosure | тело отказа валидации на пути без разметочного рантайма | low | accept | Отказ несёт тело формата обмена вместо страницы. Достижимость из интерфейса НУЛЕВАЯ и перепроверяется задачей 1 по каждому входу; идентификаторы приходят из разметки. Принято с записанным РАСХОЖДЕНИЕМ, а не молча; РЕШЕНИЕ о переводе расхождения в именованное изъятие из D-01 передано владельцу окном |
| T-10-22-04 | Repudiation | авторство изъятия из запертого решения D-01 | medium | mitigate | Реестр объявляет расхождение и несёт состояние решения «ЖДЁТ ВЛАДЕЛЬЦА»; правило `test_no_divergence_claims_a_decision_the_owner_did_not_make` краснит на записи, объявившей себя решённой без ссылки на решение владельца. Прецедент формы — D-08, записанный ВЛАДЕЛЬЦЕМ; прецедент передачи — окна 34 и 47 |
| T-10-22-03 | Information Disclosure | число расписаний ЧУЖОГО объявления в документе | medium | mitigate | Цена названа ДВУМЯ следствиями в обеих записях (задача 2), воспроизводимый вход выписан, оценка привилегий перепроверена чтением: выборки скоуплены владельцем объявления, границ привилегий случай не пересекает. Правка поведения отложена с названной наблюдаемой опорой, а не с оценкой труда |
| T-10-22-04 | Repudiation | правило суиты, утверждающее дефект нормой | medium | mitigate | Правило названо ПОИМЁННО в обеих пометках: читатель, пришедший в суиту первой, видит, что зелёное правило закрепляет предмет, а не закрывает его |
| T-10-22-05 | Repudiation | пункт площадки приземления фокуса | medium | mitigate | Утверждение сужено до измеренного двумя перепроверенными фактами (задача 3); владелец остатка назван ССЫЛКОЙ на уже записанное назначение Фазы 15 (GATE-09), а не новым назначением |
| T-10-22-06 | Tampering | решение владельца D-11 | high | mitigate | D-11 НЕ ПЕРЕСМАТРИВАЕТСЯ: правка сужает прочтение, а не решение, и это сказано в самой пометке прямо. Партия закрытия гейпов решений владельца не открывает — объявлено прохибицией плана |
| T-10-22-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
</threat_model>

<verification>
1. `uv run pytest tests/ -q -p no:randomly` — прогон целиком зелен.
2. `uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly -k "validation_refusal or framework_bounded or divergence"` — правила реестра зелены.
3. `grep -c 'ОПРОВЕРГНУТО' app/pages/schedules.py app/templates/ads/partials/sched_delete_response.html`
   — ни для одного файла не `0`.
4. `git diff --name-only -- .planning/ app/templates/includes/notice_area.html app/templates/ads/form.html`
   — пусто: план не трогает ни записи, ни чужих предметов партии.
   ⚠️ `tests/test_pages/test_confirm_delete_transport.py` ИЗ ЭТОГО ПЕРЕЧНЯ СНЯТ НАМЕРЕННО: файл —
   предмет задачи 3 плана **10-19**, стоящего в ТОЙ ЖЕ ВОЛНЕ 2, и при общей рабочей копии
   утверждение о его нетронутости падало бы от чужой ЗАКОННОЙ работы отказом, который гвардия волны
   не атрибутирует (файла нет в `files_modified` настоящего плана). Предмет утверждения сохранён
   ПОЛОЖИТЕЛЬНО: собственный след плана исчерпывается четырьмя путями его `files_modified`, и это
   проверяется командой
   `git diff --name-only -- tests/ | grep -v -e '^tests/test_pages/test_htmx_gates.py$' -e '^tests/test_pages/test_hx_location_destinations.py$' -e '^tests/test_templates/test_components.py$' -e '^tests/test_pages/test_confirm_delete_transport.py$' -e '^tests/test_planning/test_the_walkthrough_cannot_self_certify.py$' -e '^tests/test_planning/test_requirement_completion_follows_verification.py$' || true`
   — пусто (первый путь — свой, остальные пять — законный след соседей по волне 10-19, 10-20 и
   10-21).
5. `uv run python -m compileall -q app main.py tests` — сборка проходит.
6. **ЧЕЛОВЕЧЕСКАЯ ПОЛОВИНА, СНИМАЕМАЯ НА ПРИЁМКЕ ФАЗЫ:** шаг 1.8 обхода `10-UAT.md` (линейка
   счётчика) и проверка 5 (приземление фокуса) остаются НЕПРОЙДЕННЫМИ, и настоящий план обратного
   не утверждает — он объявляет отсутствие этой опоры ОСНОВАНИЕМ отсрочки, а не её следствием.
</verification>

<success_criteria>
- Форма отказа валидации объявлена заявленным РАСХОЖДЕНИЕМ с числом, обоснованием, состоянием решения и
  условием; полнота проверяется замером; молчание исключено.
- Утверждение «предмет есть ЗАПИСЬ, а не поведение» помечено опровергнутым в ОБОИХ местах, с
  воспроизводимым входом, обоими именами находки и названной ценой.
- Утверждение о площадке приземления сужено до измеренного; решение владельца D-11 не пересмотрено;
  владелец остатка назван ссылкой, а не новым назначением.
- Ни одна прежняя формулировка не стёрта, и каждая удалённая строка (если такие есть) названа
  поимённо с основанием.
- Ни одно основание отсрочки не содержит оценки труда: все три стоят на отсутствии наблюдаемой опоры
  либо на решении владельца.
- Тексты ТРЁХ окон выписаны в сводку — два прежних плюс окно, передающее владельцу решение об
  изъятии из запертого D-01; строки реестра пишет план 10-23, владелец записи один.
- Решение об изъятии из D-01 планом НЕ ЧЕКАНИТСЯ: все записи реестра несут состояние
  «ЖДЁТ ВЛАДЕЛЬЦА», правило авторства зелено и показало красный на копии записи, переведённой в
  «решено» без ссылки.
- `.planning/` не тронут; требование `FORM-06` остаётся незавершённым.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-22-SUMMARY.md` по завершении.
</output>
