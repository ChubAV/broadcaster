---
phase: 10-rychag-components-modal-html
plan: 33
type: execute
wave: 1
# ⚠️ ВОЛНА 1 — ЗАВИСИМОСТЕЙ НЕТ НИ ОДНОЙ, И ЭТО ЗАМЕР, А НЕ УМОЛЧАНИЕ. Партия шестая, и в ней два
# плана. Файлы настоящего плана — таблица стилей, модуль суиты шелла и докстринг включения плашек;
# файлы плана 10-34 — артефакт обхода и модуль реестра якорей. Пересечения ноль, но 10-34 всё равно
# стои́т волной ниже: он ЗАПИСЫВАЕТ, чем закрыто расхождение Р-3, а запись о починке пишется ПОСЛЕ
# починки, а не вместо неё.
depends_on: []
gap_closure: true
gap_ids: [G-10-4]
files_modified:
  - tests/test_pages/test_shell.py
  - app/static/css/app.css
  - app/templates/includes/htmx_error_banner.html
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (полей во frontmatter не заводится, форма унаследована от
# планов 10-12…10-32): `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена автором.
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ЧЕЛОВЕК, НАЖАВШИЙ «УДАЛИТЬ» БЕЗ СВЯЗИ, ВИДИТ ОБЪЯСНЕНИЕ, А НЕ ПУСТОТУ: плашка обрыва связи объявлена ВНЕ ПОТОКА и СЛОЕМ ВЫШЕ панели ровно в том состоянии, ради которого она и пишется, — при открытой панели, снявшей прокрутку с корневого узла"
    - "ПОДЪЁМ ДЕЙСТВУЕТ ТОЛЬКО ПОКА ПАНЕЛЬ ОТКРЫТА, И ЭТО ВЫБРАНО, А НЕ ПОЛУЧИЛОСЬ: безусловный подъём превратил бы плашку, которую сценарий обратно не прячет, в вечно висящий поверх экрана прямоугольник — цену этого артефакт включения назвал допущением ещё Фазой 8"
    - "ПОРЯДОК СЛОЁВ УТВЕРЖДАЕТСЯ ПРАВИЛОМ, А НЕ ГЛАЗОМ: слой плашки и слой панели ЧИТАЮТСЯ ИЗ ТАБЛИЦЫ СТИЛЕЙ и сличаются между собой — число в правиле не выписано ни одно, поэтому правка любого из двух слоёв не может разойтись с правилом молча"
    - "ПРАВИЛО КЛЮЧÁЕТСЯ НА ТОТ ЖЕ ПРИЗНАК, КОТОРЫЙ ПОДНИМАЕТ САМА ПАНЕЛЬ: имя признака блокировки прокрутки сличается с исходником рычага, и панель, перестав его поднимать, роняет правило — а не тихо забирает у плашки подъём"
    - "НИ ОДИН ПРЕДОК ТОЧКИ ВКЛЮЧЕНИЯ В ОСНОВНОМ ШЕЛЛЕ НЕ ЗАПИРАЕТ ФИКСИРОВАННЫЙ СЛОЙ В СЕБЕ: вселенная предков объявлена, замерена по шеллу и стережётся правилом — свойство, порождающее содержащий блок или собственный контекст наложения, краснеет на предке, а не обнаруживается следующим обходом"
    - "ГРАНИЦА ДОКАЗАННОГО НАЗВАНА В САМИХ ПРАВИЛАХ: они утверждают ОБЪЯВЛЕНИЕ слоя в таблице стилей и порядок объявленных слоёв, а НЕ то, что браузер нарисовал плашку поверх панели, — последнее остаётся шагу 4.4 обхода `10-UAT.md`"
  artifacts:
    - "app/static/css/app.css — правило подъёма обеих заготовок плашки при поднятом признаке блокировки прокрутки, стоящее РЯДОМ с правилом самой блокировки, с замерами цепи предков и названной ценой"
    - "tests/test_pages/test_shell.py — правило порядка слоёв с положительной половиной и двумя отрицательными контролями; правило чистоты цепи предков с объявленной вселенной и своим отрицательным контролем"
    - "app/templates/includes/htmx_error_banner.html — запись о том, что достижимость заготовок при открытой панели держит правило стиля в другом файле, и чего эта пара НЕ доказывает"
  key_links:
    - "признак блокировки прокрутки → селектор подъёма: единственное, чем подъём включается ровно в том состоянии, где измерена недостижимость; ломается — панель перестаёт поднимать признак, подъём не включается никогда, и правило задачи 1 краснеет ИМЕННО ЭТИМ, а не молчит"
    - "слой панели в таблице стилей → слой плашки: единственное, чем «выше» остаётся истиной после правки любого из двух; ломается — кто-то поднимает слой панели, плашка снова уезжает под неё, и правило называет ОБА числа"
    - "цепь предков точки включения → фиксированный слой: единственное, чем подъём остаётся подъёмом ДО ОКНА, а не до ближайшего предка с трансформацией; ломается — свойство появляется на предке, плашка позиционируется внутри него, и глазом это неотличимо от работающего подъёма"
  prohibitions:
    - statement: "БЕЗУСЛОВНЫЙ подъём плашки не заводится: сценарий включения снятую плашку обратно НЕ ПРЯЧЕТ (записанное допущение Фазы 8), и подъём вне состояния открытой панели оставил бы человеку вечный прямоугольник поверх экрана после одного обрыва связи"
      status: flagged-unverified
      verification: none
    - statement: "третий обработчик НЕ ЗАВОДИТСЯ и число зарегистрированных обработчиков не сдвигается: `FAILURE_BANNER_HANDLERS_MEASURED` остаётся равным 2, сценарий включения не правится ни на строку"
      status: flagged-unverified
      verification: test
    - statement: "разметка включения плашек НЕ ПРАВИТСЯ: ни обёртки, ни идентификаторов, ни текстов — предмет плана есть СЛОЙ, а не содержимое, и правка разметки задела бы гейты владельца заготовок"
      status: flagged-unverified
      verification: test
    - statement: "область `#notice-alert` в подъём НЕ ВКЛЮЧАЕТСЯ: замер обхода (проверка 5, `WR-02`) говорит, что оба кода отказа панели подтверждения едут туда ТОЛЬКО транспортом перехода, на котором панель закрывается, — правило подъёма для неё было бы шире дерева"
      status: flagged-unverified
      verification: none
    - statement: "состояние обхода `10-UAT.md` терминальным не становится и шаг 4.4 пройденным не объявляется: браузера у исполнителя нет, а правило доказывает ОБЪЯВЛЕНИЕ слоя, а не отрисовку"
      status: flagged-unverified
      verification: none
---

<objective>
Плашка обрыва связи перестаёт быть недостижимой ровно в том состоянии, ради которого она написана.

Обход 2026-09-09 провалил шаг 4.4 и записал расхождение **Р-3** замерами, а не впечатлением: текст
плашки в документе ЕСТЬ и верен, но плашка лежит в обычном потоке (`position: static`,
`z-index: auto`) на 322 px выше окна (`top: -322`, `bottom: -259` при `scrollY: 418`), под панелью
(`position: fixed`, `z-index: 60`, высота 1267 — во всё окно); в точке плашки верхним узлом стои́т
`DIV.modal__overlay` диалога `sched-del-143`; прокрутка корня снята `overflow: hidden`. Человеку,
нажавшему «Удалить» без связи, не показано НИ ОДНОГО объяснения, почему ничего не произошло.

Purpose: это ВТОРАЯ ПОЛОВИНА D-12, оставшаяся без первой. Панель СОЗНАТЕЛЬНО остаётся открытой,
чтобы человек не потерял контекст того, что подтверждал (шаг 4.3 — сошёлся), кнопка снова нажимаема
(4.5 — сошёлся), — и при этом причина не показана. Контекст сохранён, причина утеряна.

Output: правило стиля, поднимающее обе заготовки плашки над панелью ровно при открытой панели; два
правила суиты, стерегущие порядок слоёв и чистоту цепи предков; запись о границе доказанного.

⚠️ **ВЫБРАН КАНДИДАТ (а), И ВЫБОР НАЗВАН СО ВСЕМИ ТРЕМЯ ОСНОВАНИЯМИ.** Расхождение Р-3 записало
трёх кандидатов и владельца им не назначило. Настоящий план назначает — и говорит, почему не двое
других.

- **(а) Поднять область записей отказа в свой слой выше панели — ВЗЯТ.** Он правит ровно
  измеренное: положение в потоке и порядок слоёв. Он не трогает ни разметки, ни сценария, ни числа
  обработчиков. Он действует на ОБЕ заготовки и на ОБА шелла одним объявлением, потому что владелец
  заготовок один (`FAILURE_BANNER_OWNER`).
- **(б) Отрисовывать запись отказа ВНУТРИ панели — ОТВЕРГНУТ ТРЕМЯ СЛЕДСТВИЯМИ.** (1) Оба
  обработчика канала — ОБЩИЕ на документ и о панели не знают ничего: чтобы отрисовать внутри,
  сценарию пришлось бы искать открытую панель, то есть завести знание о рычаге в общем канале
  обеих оболочек и всех сорока семи форм вехи. (2) Текст плашки пришлось бы либо продублировать в
  рычаге, либо переносить узлами — первое разошлось бы с оригиналом молча (ровно доктрина
  единственного источника, ради которой заготовки и живут в одном файле), второе есть сборка
  разметки, запрещённая гейтом стоков (`T-08-18`). (3) Канал перестал бы работать там, где панели
  нет вовсе, — а форм у вехи сорок семь, и панелей среди них восемнадцать.
- **(в) Отнести к сводному обходу доступности Фазы 15 (GATE-09) — ОТВЕРГНУТ.** Предмет GATE-09 —
  приземление фокуса на пути перехода, где тело документа подменяется целиком. Предмет Р-3 — слой
  видимости на ФРАГМЕНТНОМ пути при открытой панели; общего у них ровно то, что оба про
  доступность. Отложить дефект тяжести `major`, чинящийся объявлением стиля, в фазу, отстоящую на
  пять номеров, значило бы отложить ЕДИНСТВЕННОЕ объяснение отказа на пять фаз.

⚠️ **ПОДЪЁМ УСЛОВЕН ПРИЗНАКОМ ОТКРЫТОЙ ПАНЕЛИ, И ЦЕНА БЕЗУСЛОВНОГО НАЗВАНА ЧИСЛОМ ЧУЖОГО РЕШЕНИЯ.**
Артефакт включения говорит дословно: «Снятую плашку сценарий обратно не прячет: автоматическое
сокрытие потребовало бы третьего обработчика, а рамка вехи ограничивает число новых сценариев;
решение записано допущением плана и возвращается к обсуждению, если накопление плашек станет
заметно». Безусловный подъём сделал бы накопление заметным НЕМЕДЛЕННО — то есть переоткрыл бы чужое
записанное решение боковым следствием правки стиля, не назвав его. Условный подъём не трогает его
вовсе: вне открытой панели плашка ведёт себя ровно как сегодня, а внутри — становится достижимой.
Цена условности названа прямо и не смягчается: ПОСЛЕ закрытия панели плашка возвращается в поток и
может снова оказаться выше окна. Но там прокрутка ЖИВАЯ, и «недостижима» — слово замера Р-3 —
перестаёт быть верным: недостижимой её делала пара «фиксированная панель поверх» + `overflow:
hidden`, и подъём снимает обе.

⚠️ **ПЕРЕЧЕНЬ `artifacts` ГЭПА G-10-4 НАЗЫВАЕТ НЕ ТОТ ФАЙЛ, И ЭТО НАХОДКА, А НЕ ОПЕЧАТКА
ПЛАНИРОВЩИКА.** Гэп записал `app/templates/includes/notice_area.html` с пометкой «область записей
отказа без собственного контекста наложения». Замеренный текст — «Запрос не дошёл до сервера…» —
живёт в ДРУГОМ файле: `app/templates/includes/htmx_error_banner.html:101`, узел
`#htmx-failure-network`, и в дереве этот литерал встречается РОВНО ОДИН РАЗ. Ошибся адрес записи, а
не измерение: измерены положение, слой и верхний узел в точке — всё это верно и относится к
заготовке канала отказа. Исполнитель обязан ПЕРЕПРОВЕРИТЬ адрес своим прогоном (задача 1, пункт 1) и
привести замер в сводку: настоящий план правит `htmx_error_banner.html`, а `notice_area.html` не
трогает ни на символ — и второе есть решение, а не пропуск (см. прохибиции).
</objective>

<reversibility>
  <decision rating="reversible">
Условный подъём заготовок плашки в свой слой. Откат — снятие одного блока объявлений из таблицы
стилей; разметка, сценарий и число обработчиков не тронуты, откатывать в них нечего.
  </decision>
  <decision rating="reversible">
Два правила суиты и объявленная вселенная предков. Откат — снятие правил. ⚠️ Цена названа: вселенная
предков ЗАМЕРЕНА по шеллу, а не выведена, и новая обёртка, вставленная в шелл между корнем и точкой
включения, в неё сама не попадёт — это граница правила, названная в его же докстринге.
  </decision>
</reversibility>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-rychag-components-modal-html/10-UAT.md
@.planning/phases/10-rychag-components-modal-html/10-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: правило порядка слоёв краснеет на сегодняшнем дереве, правило стиля делает его зелёным</name>

  <files>
tests/test_pages/test_shell.py
app/static/css/app.css
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md`, раздел `### Р-3` целиком (строки
  724-751): таблица из шести замеров и абзац «Следствие для человека, названное без смягчения».
  ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: числа правила берутся из ЗАМЕРА, а не из памяти о нём.
- `app/templates/includes/htmx_error_banner.html:99-115` — обе заготовки, их идентификаторы и
  сценарий. ⚠️ ЧИТАТЬ РАДИ ГРАНИЦЫ: этот файл в задаче 1 НЕ ПРАВИТСЯ ни на символ.
- `app/static/css/app.css:937-986` — раздел панели подтверждения: слой панели (`:942`), правило
  блокировки прокрутки (`:961-964`), оверлей (`:965-968`), сама панель (`:969-977`).
- `app/static/css/app.css:844-852` — общий макрос плашки: своих внешних отступов у неё нет, фон
  полупрозрачный, радиус — `var(--r-lg)`.
- `app/templates/components/modal.html:693-711` — объект состояния панели: подъём признака
  блокировки на корневом узле (`:699`) и два пути его снятия (`:700`, `:701`).
- `tests/test_pages/test_shell.py:2042-2060` — действующий реестр канала отказа:
  `FAILURE_BANNER_OWNER`, `FAILURE_BANNER_SHELLS`, `FAILURE_BANNER_IDS`, `FAILURE_BANNER_EVENTS`.
  ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: идентификаторы заготовок правило берёт ОТСЮДА, второй копии не заводится.
- `tests/test_pages/test_shell.py:2150-2183` — форма помощников `_network_banner_line` и
  `_scratch_banner`: подмена ради отрицательного контроля идёт ЧЕРЕЗ ФАЙЛОВУЮ СИСТЕМУ, тем же путём
  чтения, каким гейт ходит по боевому дереву. Форма наследуется, а не изобретается.
  </read_first>

  <behavior>
Правило `test_the_failure_banner_is_declared_above_the_panel_while_it_is_open`:
- ПОЛОЖИТЕЛЬНАЯ ПОЛОВИНА ИДЁТ ПЕРВОЙ: на непрáвленом дереве находок ноль.
- Слой панели читается из таблицы стилей: блок селектора панели существует РОВНО ОДИН, объявляет
  положение вне потока и ЧИСЛОВОЙ слой. Ни одно из двух чисел в правиле не выписано — без этой
  половины правило зеленело бы вакуумом на таблице, где слоёв нет вовсе.
- Имя признака блокировки прокрутки, на который ключáется подъём, присутствует в исходнике рычага
  панели: панель, перестав его поднимать, роняет правило.
- Для КАЖДОГО идентификатора из `FAILURE_BANNER_IDS`: блок подъёма существует ровно один, его
  селектор несёт И признак блокировки, И идентификатор заготовки; блок объявляет положение вне
  потока; его слой СТРОГО БОЛЬШЕ слоя панели.
- Отказ называет ОБА числа и идентификатор заготовки, на которой найден.

Отрицательный контроль (а) `test_control_a_banner_layer_below_the_panel_reddens`: доктóренная копия
таблицы стилей, где слой подъёма опущен до слоя панели, — правило находит РОВНО ОДНО расхождение и
называет оба числа.

Отрицательный контроль (б) `test_control_a_banner_without_an_elevation_rule_reddens`: доктóренная
копия, где блок подъёма снят целиком, — правило находит по расхождению на КАЖДУЮ заготовку и
называет их идентификаторы.

Правило стиля в `app/static/css/app.css` после правки:
- поднимает ОБЕ заготовки одним блоком, ключáясь на признак блокировки прокрутки;
- объявляет положение вне потока, слой строго выше слоя панели, привязку к верху окна, ширину с
  ограничением сверху и центрирование, радиус и непрозрачную подложку с тенью;
- НЕ объявляет способа отображения ни в одном виде — иначе скрытые заготовки перестали бы быть
  скрытыми, и человек получил бы две пустые рамки на каждом открытии панели.
  </behavior>

  <action>
**(1) ПЕРЕПРОВЕРИТЬ АДРЕС ЗАМЕРА СВОИМ ПРОГОНОМ, А НЕ ПОВЕРИТЬ ПЛАНУ.** Найти по дереву `app/`, в
скольких файлах встречается первая фраза текста плашки обрыва связи, и в каком. ⚠️ ОРИЕНТИР
ПЛАНИРОВЩИКА: ровно одно вхождение, `app/templates/includes/htmx_error_banner.html`. Перечень
`artifacts` гэпа G-10-4 называет `app/templates/includes/notice_area.html` — это НАХОДКА, она
приводится в сводку числом вхождений в обоих файлах, а не поминается вскользь. Расхождение с
ориентиром — тоже находка, и тогда правится план, а не число.

**(2) Замерить цепь предков точки включения В ОБОИХ ШЕЛЛАХ и объявленные слои.** Выписать: (а)
последовательность узлов от тела документа до узла включения в `app/templates/base.html` и в
`app/templates/auth_base.html`; (б) все объявленные слои таблицы стилей числами. ⚠️ ОРИЕНТИРЫ
ПЛАНИРОВЩИКА, снятые до исполнения: основной шелл — тело документа, затем узлы с признаками
`data-shell` (`base.html:79`), `data-main` (`:209`), `data-body` (`:232`), включение на `:242`;
шелл авторизации — тело документа, узел `data-auth-shell` (`auth_base.html:33`), узел карточки
(`:34`), включение на `:49`; объявленных слоёв четыре — 60 у панели (`app.css:942`), 20 у нижней
полосы вкладок (`:637`), 5 у липкой шапки (`:653`), 1 у самой панели внутри её же контекста
(`:970`); наибольший — 60. ⚠️ ЛИПКАЯ ШАПКА СО СЛОЕМ 5 — СОСЕД, А НЕ ПРЕДОК: она закрывается на
`base.html:230`, а узел `data-body` открывается на `:232`. Числа привести в сводку.

**(3) НАПИСАТЬ ПРАВИЛО ПЕРВЫМ И УБЕДИТЬСЯ, ЧТО ОНО КРАСНОЕ.** Правило и оба его отрицательных
контроля ставятся в `tests/test_pages/test_shell.py` РЯДОМ с действующим реестром канала отказа —
идентификаторы заготовок берутся из `FAILURE_BANNER_IDS`, второй копии перечня не заводится.
Объявить константами: путь таблицы стилей; имя признака блокировки прокрутки с пометкой, что оно
ЗАМЕРЕНО по рычагу панели, а не взято из памяти; селектор слоя панели; путь исходника рычага.
Разбор таблицы стилей — по парам «список селекторов + блок объявлений»; вложенные медиазапросы
разбираются как обычные блоки со своими селекторами, и это свойство разбора назвать в докстринге
ПРЯМО, а не оставить следующему читателю. Прогнать до правки стиля: обе половины обязаны быть
КРАСНЫМИ, а текст отказа — называть, чего именно нет. Дословный красный привести в сводку.

**(4) ПОСТАВИТЬ ПРАВИЛО СТИЛЯ РЯДОМ С ПРАВИЛОМ БЛОКИРОВКИ ПРОКРУТКИ** (`app.css`, сразу после
блока `:961-964`), а не в конец файла: два правила одного состояния, разнесённые по файлу,
читаются как два механизма. Объявления блока: положение `fixed`; отступ сверху `12px`; привязки
слева и справа по нулю; слой `70`; ширина `min(560px, calc(100% - 24px))`; горизонтальные внешние
отступы `auto` (пара «обе привязки + определённая ширина + авто-отступы» и есть центрирование);
радиус `var(--r-lg)` — тот же, что у общего макроса плашки; подложка `var(--surface)`; тень
`0 24px 60px rgba(8, 8, 11, .6)` — та же, что у самой панели. ⚠️ СПОСОБ ОТОБРАЖЕНИЯ НЕ ОБЪЯВЛЯТЬ
НИ В КАКОМ ВИДЕ: скрытость заготовок держит атрибут скрытия через таблицу стилей пользовательского
агента, и любое объявление способа отображения в этом блоке перебило бы её — на каждом открытии
панели человек получал бы две пустые подложки.

**(5) ЧИСЛО СЛОЯ `70` ВЫБРАНО ЗАМЕРОМ, А НЕ КРУГЛЫМ СЛОВОМ, и это записать в комментарии:**
наибольший объявленный слой дерева — 60 у панели, и 70 стои́т выше него с запасом на одно
промежуточное значение. ⚠️ ЧИСЛО В ПРАВИЛЕ СУИТЫ НЕ ПОВТОРЯЕТСЯ: правило СЛИЧАЕТ два слоя,
прочитанных из таблицы, и не знает ни одного из них наперёд.

**(6) КОММЕНТАРИЙ БЛОКА НЕСЁТ ПЯТЬ ЧАСТЕЙ, И НИ ОДНА НЕ УКРАШЕНИЕ:** (а) что измерено обходом —
шесть величин Р-3 с датой и стендом; (б) почему подъём УСЛОВЕН — цена безусловного, названная
записанным допущением Фазы 8 о том, что снятую плашку сценарий обратно не прячет; (в) цена самой
условности — после закрытия панели плашка возвращается в поток; (г) замер цепи предков пункта (2) с
перечнем свойств, на которые она проверена, и прямое предупреждение, что свойство, порождающее
содержащий блок, появившееся на предке, отняло бы у подъёма окно; (д) чем это стережётся — имена
двух правил задач 1 и 2.

**(7) ПРОГНАТЬ ПРАВИЛО ПОСЛЕ ПРАВКИ СТИЛЯ: зелено.** Прогнать оба отрицательных контроля: красны на
доктóренных копиях, зелены на дереве. Все три перехода привести в сводку.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q</automated>
  </verify>

  <done>
Правило порядка слоёв зелено на дереве и красно на обеих доктóренных копиях; слой подъёма и слой
панели ЧИТАЮТСЯ из таблицы стилей, ни одно из двух чисел в правиле не выписано; блок подъёма стои́т
рядом с блоком блокировки прокрутки, ключáется на признак, поднимаемый самим рычагом, и способа
отображения не объявляет; дословный красный «до» и два замера (адрес текста плашки, цепь предков и
объявленные слои) приведены в сводку.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: цепь предков точки включения не запирает поднятый слой в себе</name>

  <files>
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `app/static/css/app.css:533-541` (`data-shell`), `:651` (`data-main`), `:681` (`data-body`),
  `:469-473` и `:482` — те же три селектора внутри медиазапросов узких экранов. ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО: правило обязано видеть ВСЕ блоки каждого селектора, а не первый.
- `app/static/css/app.css:992-1005` — цепь шелла авторизации и анимация карточки; `:438` —
  ключевые кадры этой анимации, последний кадр снимает трансформацию.
- `app/templates/base.html:232, 242` и `app/templates/auth_base.html:33-34, 49` — сами точки
  включения и их предки.
- Задача 1 настоящего плана, пункт (2) — замер цепи предков, уже приведённый в сводку.
  </read_first>

  <behavior>
Правило `test_no_ancestor_of_the_failure_banner_traps_the_fixed_layer`:
- ПОЛОЖИТЕЛЬНАЯ ПОЛОВИНА ПЕРВОЙ: на непрáвленом дереве находок ноль.
- Вселенная предков объявлена константой и содержит ТРИ селектора основного шелла. Пустая
  вселенная роняет правило отдельным отказом — иначе оно зеленело бы вакуумом.
- Ни один блок объявлений, чей селектор несёт любой из трёх, не объявляет свойств, порождающих
  содержащий блок для фиксированного потомка или собственный контекст наложения. Перечень свойств
  объявлен константой и назван по частям в докстринге: одни отнимают у фиксированного потомка окно,
  другие запирают его слой внутри предка.
- Отказ называет: селектор предка, свойство, значение и следствие — «поднятая плашка позиционируется
  внутри предка, а не по окну, и глазом это неотличимо от работающего подъёма».

Отрицательный контроль `test_control_a_transforming_ancestor_reddens`: доктóренная копия таблицы
стилей, где одному из трёх предков дописано свойство трансформации, — правило находит РОВНО ОДНО
расхождение и называет селектор и свойство.

Граница правила, объявленная в его же докстринге ДВУМЯ пунктами:
- вселенная ЗАМЕРЕНА по шеллу, а не выведена из него разбором: обёртка, вставленная в шелл между
  корнем и точкой включения, в неё сама не попадёт;
- цепь шелла авторизации в неё НЕ ВХОДИТ, и это решение с основанием, а не пропуск: карточка того
  шелла несёт анимацию, последний кадр которой трансформацию снимает, а во время самой анимации
  содержащий блок существует, — и панелей подтверждения в том шелле нет НИ ОДНОЙ (замер: все
  потребители рычага живут на страницах основного шелла).
  </behavior>

  <action>
**(1) Замерить вселенную предков ПО ШЕЛЛАМ, а не переписать из задачи 1.** Прогоном по
`app/templates/base.html` и `app/templates/auth_base.html`: какие узлы стоя́т между телом документа
и строкой включения заготовок. Замер привести в сводку — он и есть основание объявленной константы.

**(2) Замерить, в каком шелле живут потребители рычага панели.** Прогоном по дереву шаблонов: у
скольких файлов, зовущих рычаг, цепь наследования приходит в шелл авторизации. ⚠️ ОРИЕНТИР
ПЛАНИРОВЩИКА: ноль — все потребители приходят в основной шелл. Это число и есть основание для
исключения второй цепи из вселенной; расхождение с ориентиром отменяет исключение и есть находка.

**(3) Объявить перечень свойств ДВУМЯ ГРУППАМИ, а не одним списком без объяснения.** Первая группа
отнимает у фиксированного потомка окно и делает его содержащим блоком сам предок; вторая заводит
предку собственный контекст наложения, внутри которого слой потомка перестаёт сравниваться со слоем
панели. Каждую группу назвать в докстринге СЛОВАМИ и перечислить её свойства константой.

**(4) НАПИСАТЬ ПРАВИЛО ПЕРВЫМ И УБЕДИТЬСЯ, ЧТО ОНО ЗЕЛЕНО НА ДЕРЕВЕ, А ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ —
КРАСЕН.** Порядок здесь обратный задаче 1 и назван прямо: предмет правила есть ОТСУТСТВИЕ свойства,
и «красное до правки» у него не существует — зубы ему даёт только доктóренная копия. Дословный
красный контроля привести в сводку: без него правило неотличимо от разборщика, не находящего
ничего никогда.

**(5) НЕ ПРАВИТЬ НИ ОДНОГО ПРЕДКА.** Замер говорит, что сегодня ни один из трёх запрещённых свойств
не объявляет; правка «на всякий случай» была бы подгонкой дерева под правило.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q</automated>
  </verify>

  <done>
Правило чистоты цепи предков зелено на дереве и красно на доктóренной копии с трансформацией у
предка; вселенная объявлена тремя селекторами и замерена по шеллу; перечень свойств объявлен двумя
группами со словесным объяснением каждой; обе границы правила названы в его докстринге; два замера
(цепь предков, число потребителей рычага в шелле авторизации) приведены в сводку числами.
  </done>
</task>

<task type="auto">
  <name>Задача 3: владелец заготовок называет, чем держится их достижимость и чего эта пара НЕ доказывает</name>

  <files>
app/templates/includes/htmx_error_banner.html
  </files>

  <read_first>
- `app/templates/includes/htmx_error_banner.html:1-98` — докстринг целиком. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО
  ДО ПРАВКИ: файл сам объявляет три собственные дисциплины, и правка, их нарушившая, роняет гейты
  своего же владельца — имена событий набраны СЛОВАМИ, а не литералами; комментарий обязан быть
  комментарием шаблонизатора, а не разметки; стоков сборки разметки в файле ноль, и считаются они
  по исходнику ВМЕСТЕ с комментариями.
- `tests/test_pages/test_shell.py:2042-2060` и `:2186-2209` — реестр канала и объявленное число
  зарегистрированных обработчиков. ⚠️ ЧИСЛО НЕ СДВИГАЕТСЯ: третьего обработчика план не заводит.
- `app/static/css/app.css` — блок подъёма, поставленный задачей 1: запись обязана ссылаться на
  него, а не пересказывать его своими числами.
  </read_first>

  <behavior>
1. Докстринг несёт запись о том, что достижимость обеих заготовок ПРИ ОТКРЫТОЙ ПАНЕЛИ держит
   правило стиля в другом файле, и называет этот файл.
2. Запись называет ПОВОД: обход 2026-09-09, шаг 4.4, расхождение Р-3, и следствие для человека
   одной фразой — на отказе ему не было показано ни одного объяснения.
3. Запись называет ГРАНИЦУ ДОКАЗАННОГО: правила суиты утверждают ОБЪЯВЛЕНИЕ слоя и порядок
   объявленных слоёв, а не отрисовку браузером; отрисовка остаётся шагу 4.4 обхода.
4. Запись называет, что число зарегистрированных обработчиков НЕ СДВИНУТО и сценарий не тронут.
5. Прежний текст докстринга не вычеркнут ни в одном месте: правка ДОПИСЫВАЕТ, а не переписывает.
6. Ни имён событий литералами, ни стоков сборки разметки, ни разметочного комментария новая запись
   не вносит.
  </behavior>

  <action>
**(1) Дописать запись ОДНИМ абзацем в докстринг**, рядом с абзацем о том, что снятую плашку сценарий
обратно не прячет: там же стои́т и записанное допущение, которое условность подъёма НЕ переоткрывает.

**(2) НЕ ПЕРЕСКАЗЫВАТЬ ЧИСЛА ПРАВИЛА СТИЛЯ.** Запись ссылается на файл таблицы стилей и на имена
двух правил суиты; слой числом здесь не называется — вторая копия числа разошлась бы с первой
молча, и это ровно тот класс дефекта, за который фаза получила пятый круг ревизии.

**(3) НАЗВАТЬ, ЧТО ПОДЪЁМ УСЛОВЕН, И НАЗВАТЬ ЦЕНУ УСЛОВНОСТИ** одной фразой: вне открытой панели
поведение заготовок сегодняшнее, включая то, что обратно они не прячутся.

**(4) НЕ ТРОГАТЬ РАЗМЕТКУ И СЦЕНАРИЙ.** Ни обёрток, ни идентификаторов, ни текстов, ни строк
сценария: предмет задачи — запись.

**(5) Прогнать гейты владельца заготовок** и убедиться, что ни один не сдвинулся: число
зарегистрированных обработчиков, ноль стоков сборки разметки, оба имени событий на месте, единство
подключения в обоих шеллах.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py tests/test_templates -q</automated>
  </verify>

  <done>
Докстринг владельца заготовок называет: чем держится достижимость при открытой панели и где это
живёт; повод с датой, шагом и именем расхождения; границу доказанного двумя половинами; что число
обработчиков не сдвинуто. Прежний текст не вычеркнут ни в одном месте, новых литералов имён событий
и стоков сборки разметки не внесено, гейты владельца зелены.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| канал отказа → глаз человека | единственный путь, которым человек узнаёт, что действие НЕ произошло; отказ этого канала неотличим от сломанной кнопки |
| таблица стилей → рантайм наложения браузера | порядок слоёв объявляется в одном файле, а применяется контекстами наложения, которые заводят ЧУЖИЕ правила на предках |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-33-01 | Repudiation | `includes/htmx_error_banner.html` + `components/modal.html` (пара «канал отказа ↔ панель») | high | mitigate | ИЗМЕРЕННОЕ состояние: необратимое действие отказало, человеку не показано ни одного объяснения, и он повторяет нажатие вслепую либо считает продукт сломанным. Митигация — подъём заготовок в слой выше панели при поднятом признаке блокировки прокрутки (задача 1) и два правила, стерегущие порядок слоёв и чистоту цепи предков (задачи 1, 2) |
| T-10-33-02 | Tampering | фиксированный слой поверх панели подтверждения | medium | mitigate | Узел, положенный поверх модального окна, есть классическая поверхность перекрытия нажатия. Митигация ФОРМОЙ: обе заготовки не несут ни ссылок, ни кнопок, ни полей — только текст; они скрыты атрибутом скрытия и открываются ТОЛЬКО двумя обработчиками самого приложения; блок подъёма способа отображения не объявляет, поэтому скрытая заготовка остаётся невидимой и нажатий не принимает. ⚠️ ГРАНИЦА НАЗВАНА: перекрытие ВЕРХНЕЙ полосы панели видимой плашкой возможно и является ценой подъёма — панель центрирована, её кнопки стоя́т внизу |
| T-10-33-03 | Denial of Service | безусловный подъём (ОТВЕРГНУТЫЙ вариант) | medium | mitigate | Плашка, поднятая безусловно, никогда не прячется обратно (записанное допущение Фазы 8) и после одного обрыва связи заняла бы верх окна до конца сессии, закрывая содержимое. Митигация — подъём УСЛОВЕН признаком открытой панели; чужое записанное решение о третьем обработчике не переоткрывается боковым следствием правки стиля |
| T-10-33-04 | Repudiation | правила суиты над таблицей стилей | medium | mitigate | Правило, доказывающее ОБЪЯВЛЕНИЕ, читается как доказательство ОТРИСОВКИ, и следующий круг объявляет шаг 4.4 закрытым машинно. Митигация — граница названа в докстринге обоих правил, в комментарии блока стиля и в записи владельца заготовок (задача 3); состояние обхода терминальным не становится |
| T-10-33-05 | Tampering | предок точки включения в шелле | medium | mitigate | Свойство, порождающее содержащий блок, дописанное предку, отнимает у поднятой плашки окно и запирает её слой внутри предка — глазом неотличимо от работающего подъёма. Митигация — правило чистоты цепи предков с объявленной вселенной и отрицательным контролем (задача 2). ⚠️ ГРАНИЦА: вселенная ЗАМЕРЕНА, а не выведена разбором шелла, и новая обёртка в неё сама не попадёт — названо в докстринге правила |
| T-10-33-SC | Tampering | установки пакетов | low | accept | Установок нет: `pyproject.toml` не в `files_modified`, новых зависимостей не вводится, разведка пакетов не требуется |
</threat_model>

## Артефакты, которые создаёт этот план

- условный блок подъёма обеих заготовок плашки в таблице стилей, стоящий рядом с блоком блокировки
  прокрутки, с пятичастным комментарием и замерами
- правило порядка слоёв с положительной половиной и двумя отрицательными контролями
- правило чистоты цепи предков с объявленной вселенной, двумя группами свойств и отрицательным
  контролем
- запись владельца заготовок о том, чем держится их достижимость и чего пара правил НЕ доказывает

## Что этот план НЕ закрывает, и основание у каждого пункта своё

- **Шаг 4.4 обхода `10-UAT.md` пройденным НЕ объявляется, и состояние артефакта терминальным не
  становится.** Основание прямое: браузера у исполнителя нет, а правила утверждают ОБЪЯВЛЕНИЕ слоя,
  а не отрисовку. Запись о том, чем расхождение Р-3 закрыто и что осталось человеку, заводит план
  10-34 — ПОСЛЕ починки, а не вместо неё.
- **Область `#notice-alert` (`includes/notice_area.html`) в подъём не включается.** НЕ ОТЛОЖЕНА, А
  НЕ ВЗЯТА, и основание замерено, а не предположено: оба кода отказа, которые панель подтверждения
  способна выдать, едут в эту область ТОЛЬКО транспортом перехода (`WR-02`, три измерения, шапка
  `includes/notice_area.html`), а на этом транспорте панель ЗАКРЫВАЕТСЯ вторым дизъюнктом условия
  (`10-27`, замер бандла). Состояния «плашка в этой области при открытой панели» на сегодняшнем
  дереве не существует, и правило подъёма для неё было бы шире дерева — ровно тот класс записи, за
  который фаза получила круги 3, 4 и 5.
- **Автоматическое сокрытие снятой плашки (третий обработчик).** Не заводится: это ЧУЖОЕ записанное
  решение (`includes/htmx_error_banner.html`, допущение плана Фазы 8), и переоткрывать его боковым
  следствием правки стиля запрещено. Условность подъёма выбрана в том числе затем, чтобы решение
  осталось нетронутым: вне открытой панели поведение заготовок сегодняшнее.
- **Отсутствие клиентского тайм-аута у гейта ухода панели (окно 34 `.planning/WINDOWS.md`).**
  Предмет соседний, но ДРУГОЙ, и разводить их требует проверка 6 самого обхода: окно 34 — цена
  гейта, Р-3 — видимость сообщения об отказе рядом с ним. Окно 34 остаётся открытым и
  фазы-владельца не получает.

<verification>
- `uv run pytest tests/test_pages/test_shell.py -q` — зелено.
- `uv run pytest tests/ -q` — зелено, число пройденных НЕ МЕНЬШЕ базового `2897` (прибавка равна
  числу заведённых правил и контролей). ⚠️ ПРОГОН ПОЛНЫЙ, БЕЗ ОТБОРА: правила записи живут в
  `tests/test_planning/`, и прогон, их отбросивший, не увидел бы расхождения записей с деревом.
- `uv run python -m compileall -q app main.py tests` — код возврата ноль.
- Дословные красные приведены в сводке: правило порядка слоёв ДО правки стиля, оба его отрицательных
  контроля, отрицательный контроль правила цепи предков.
- Замеры приведены в сводке числами: вхождения текста плашки по двум файлам (находка о перечне
  `artifacts` гэпа), цепь предков в обоих шеллах, все объявленные слои таблицы стилей, число
  потребителей рычага в шелле авторизации.
- Число зарегистрированных обработчиков канала отказа не сдвинуто: `FAILURE_BANNER_HANDLERS_MEASURED`
  остаётся равным 2.
</verification>

<success_criteria>
Обе заготовки канала отказа объявлены вне потока и слоем строго выше панели ровно при поднятом
признаке блокировки прокрутки — то есть в том состоянии, где обход измерил недостижимость; порядок
слоёв и чистота цепи предков утверждаются двумя правилами, читающими оба числа из дерева и имеющими
зубы на доктóренных копиях; выбор кандидата (а) назван вместе с тремя основаниями отказа от (б) и
одним от (в); цена условности подъёма названа прямо; граница доказанного — «объявление, а не
отрисовка» — стои́т в трёх местах, и шаг 4.4 обхода пройденным не объявляется.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-33-SUMMARY.md` по завершении.
</output>
