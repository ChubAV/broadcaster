---
phase: 10-rychag-components-modal-html
plan: 57
subsystem: ui
tags: [css, jinja2, htmx, pytest, accessibility, has-selector, checkbox]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "план 10-49 — третий обработчик (гашение обеих заготовок по признаку успеха) и гарнир рассылки событий на стабах узлов; план 10-51 — БЕЗУСЛОВНЫЙ подъём обеих заготовок и пять действующих правил подъёма; план 10-56 — класс стопки `failure-stack` на обоих узлах и свойство-величина `--failure-banner-top`; план 10-55 — раздел `## План 10-55` файла отложенного; план 10-52 — пятое утверждение гейта критерия 3"
provides:
  - "ВЫХОД у показанной плашки отказа: орган снятия внутри КАЖДОГО узла заготовки — элемент управления, состояние которого переключает браузер, с доступным именем «Скрыть сообщение» и без приписанного числа порядка обхода"
  - "возврат плашки НА НОВОМ ОТКАЗЕ — двумя строками в телах двух УЖЕ СУЩЕСТВУЮЩИХ обработчиков, по одной на обработчик"
  - "область снятия: скрытие адресует заготовку с ПРИВЕДЁННЫМ органом условным селектором `:has()`, а не всех носителей класса стопки (ребро `concurrency` требования FORM-06 разрешено ЯВНЫМ критерием)"
  - "правило значений способа отображения у ВСЕХ блоков, достигающих узла заготовки — расширение зубов пункта (д) прозы таблицы, а не его ослабление"
  - "шесть новых символов правил в `tests/test_pages/test_shell.py`: три функции находок, четыре правила, два отрицательных контроля"
  - "ТРЕТЬЯ запись блока `overrides` отчёта верификации — ответ владельца ДОСЛОВНО и пять предъявленных ему замеров ВЫВОДОМ, с областью `узко`"
  - "распоряжение по пяти находкам UI-ревизии (`UI-3`…`UI-7`) разделом `## План 10-57` файла отложенного"
  - "две записи журнала окон: долг свежего прохода шагов 2.8 и 4.4 (№ 81) и НАЗВАННОЕ следствие ветви `A` — зазор стопки при снятии первой заготовки (№ 82)"
affects: [11-otvet-sloy-formy, UAT шаги 2.8 и 4.4, следующий круг верификации Фазы 10]

actuals:
  tokens: 12072
  tasks: 3
  commits: 4
plan_head_before: 3abac559417bf17321dffa0c47c784136cee16c0

tech-stack:
  added: []
  patterns:
    - "орган управления БЕЗ регистрации обработчика: состояние переключает браузер, скрытие выражено объявлением таблицы стилей по состоянию вложенного органа (`:has(> … :checked)`) — форма унаследована у `.chip:has(.chip__input:checked)` той же таблицы"
    - "два разных крючка на один орган ПО ЗАМЕРУ, а не по небрежности: таблица адресует его КЛАССОМ (идентификатор попал бы в отбор блоков подъёма подстрокой), сценарий — ИДЕНТИФИКАТОРОМ (класс в сценарии сдвинул бы счёт грепа по разметке)"
    - "гарнир стенда РАСШИРЯЕТСЯ новым предметом, а прежний вход остаётся ПРОЕКЦИЕЙ полного состояния: шесть действующих правил гашения не переписаны ни строкой"

key-files:
  created: []
  modified:
    - app/templates/includes/htmx_error_banner.html
    - app/static/css/app.css
    - tests/test_pages/test_shell.py
    - .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
    - .planning/phases/10-rychag-components-modal-html/deferred-items.md
    - .planning/WINDOWS.md

key-decisions:
  - "Форма правки выбрана ОТВЕТОМ ВЛАДЕЛЬЦА (ветвь `A`, область `узко`), а не исполнителем: цена по критерию 3 принадлежит тому, кто вправе её принять, и ни одна строка неисполненных ветвей `B`, `C`, `D` в дерево не попала"
  - "Орган снятия — флажок, а не кнопка с обработчиком: кнопка (форма, предложенная UI-ревизией ДОСЛОВНО) есть ветвь `B`, и она немедленно покраснила бы пятое утверждение гейта критерия 3, потребовав сдвига объявленного числа и четвёртого имени события"
  - "Скрытие снятой заготовки адресует узел ПО КЛАССУ СТОПКИ, а не по идентификатору: блок с адресом заготовки попал бы в отбор `_banner_elevation_rules`, и пять действующих правил подъёма покраснели бы за ФОРМУ правки"
  - "Возврат плашки утверждается НАЧАЛЬНЫМ состоянием органа «снято»: без него сценарий, сбрасывающий орган, неотличим от сценария, не знающего о нём вовсе — оба оставили бы орган в исходном положении"
  - "Зазор стопки при снятии ПЕРВОЙ заготовки НЕ ПОЧИНЕН и НАЗВАН окном 82: обе мыслимые формы починки правят разбор ролей `_stack_blocks` и красят правила стопки плана 10-56 за форму правки, а не за дефект"
  - "`FORM-06` НЕ отмечен и `10-UAT.md` не тронут ни символом: ответ владельца на останове ПОДПИСЬЮ на приёмке обхода не является, и смешать два решения в одном ответе значило бы получить один ответ на два вопроса"

patterns-established:
  - "Улика RED переводится в форму верификатора МЕХАНИЧЕСКИМ адаптером над настоящим прогоном, а не набирается руками (наследовано у плана 10-56 и подтверждено вторым применением)"
  - "Односторонне-необратимая запись несёт ПЕРВОИСТОЧНИК — ответ владельца дословно и предъявленные ему замеры ВЫВОДОМ, — потому что после правки замеры невоспроизводимы"

requirements-completed: []

coverage:
  - id: D1
    description: "У КАЖДОЙ заготовки внутри есть орган снятия: элемент управления, попадающий в порядок обхода с клавиатуры, с доступным именем «Скрыть сообщение» и без приписанного числа порядка"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_every_failure_banner_carries_a_dismiss_control"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_a_banner_without_a_dismiss_control_reddens"
        status: pass
    human_judgment: false
  - id: D2
    description: "Снятие одной плашки не снимает вторую — селектор скрытия адресует заготовку с ПРИВЕДЁННЫМ органом (ребро `concurrency` требования FORM-06)"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_dismissing_one_banner_leaves_the_other_shown"
        status: pass
    human_judgment: false
  - id: D3
    description: "Снятая человеком плашка ВОЗВРАЩАЕТСЯ на новом отказе того же рода — состояние органа сброшено телом действующего обработчика"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_a_dismissed_banner_returns_on_the_next_failure"
        status: pass
    human_judgment: false
  - id: D4
    description: "Единственное значение способа отображения во ВСЕХ блоках, достигающих узла заготовки, — «нет»; блок подъёма по-прежнему не объявляет его ни в каком виде"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_no_banner_rule_declares_a_display_mode_that_shows"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_a_showing_display_mode_on_a_banner_rule_reddens"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_the_failure_banner_lift_declares_no_display_mode"
        status: pass
    human_judgment: false
  - id: D5
    description: "Все пять чисел гейта критерия 3 равны объявленным; объявление не сдвинуто, потому что ответ владельца этого не предписал"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_components.py#test_criterion_three_holds_by_the_numbers"
        status: pass
      - kind: other
        ref: 'grep -c "addEventListener|x-data|hx-on" app/templates/includes/htmx_error_banner.html → 3 / 0 / 0'
        status: pass
    human_judgment: false
  - id: D6
    description: "Третья запись блока `overrides` стои́т рядом с двумя действующими; обе действующие и комментарий круга не тронуты, поля вердикта круга не сдвинуты"
    verification:
      - kind: other
        ref: 'grep -c "^    accepted_branch:" 10-VERIFICATION.md → 3; git diff "$PLAN_BASE"..HEAD -- 10-VERIFICATION.md → 104 добавленных, 0 удалённых'
        status: pass
    human_judgment: false
  - id: D7
    description: "Пять находок UI-ревизии (`UI-3`…`UI-7`) распоряжены поимённо с названным владельцем в разделе перед якорем; шестнадцать внеобластных находок читаются одним распоряжением"
    verification:
      - kind: unit
        ref: "tests/test_planning/test_deferred_items_are_idempotent.py"
        status: pass
      - kind: other
        ref: 'grep -o "UI-[3-7]" по участку раздела → пять РАЗЛИЧНЫХ значений; `- **Владелец:**` → 5'
        status: pass
    human_judgment: false
  - id: D8
    description: "ПРЯМОУГОЛЬНИК плашки при рабочем положении прокрутки и работа органа снятия мышью и с клавиатуры — шаги 2.8 и 4.4 обхода после правок этой партии"
    verification: []
    human_judgment: true
    rationale: "Суита читает ОБЪЯВЛЕНИЯ таблицы стилей и исполняет сценарий на СТАБАХ узлов; движка раскладки нет, браузерного привода нет ни одного (`playwright|selenium|puppeteer|splinter` → 0 вхождений по дереву). Правило об объявлении и шаг о прямоугольнике — РАЗНЫЕ предметы, и подмена одного другим уже стоила фазе окна 77. Долг записан окном 81, а не закрыт отметкой"
  - id: D9
    description: "ПОДПИСЬ ЧЕЛОВЕКА на приёмке обхода `10-UAT.md`, которой и только которой закрывается `FORM-06`"
    verification: []
    human_judgment: true
    rationale: "Ответ владельца на останове задачи 1 этой подписью НЕ ЯВЛЯЕТСЯ: его предмет — цена формы правки по критерию 3, а не приёмка шести проверок обхода. Флажок `.planning/REQUIREMENTS.md:31` пуст, клетка `:136` несёт `Gaps Found`, и ни один символ партии их не двигает"

duration: 48 min
completed: 2026-09-13
status: complete
---

# Phase 10 Plan 57: Выход у плашки отказа — Summary

**Ветвь `A` (`dismiss-control-plus-reset`), область записи `узко`: орган снятия-флажок внутри каждой заготовки, скрытие условным селектором `.failure-stack:has(> .banner-dismiss:checked)`, возврат плашки двумя строками в телах двух действующих обработчиков — и НИ ОДНО из пяти чисел гейта критерия 3 не сдвинуто.**

## Performance

- **Duration:** 48 min
- **Started:** 2026-09-13T13:27:16Z (холодный старт агента-продолжения после останова владельца)
- **Completed:** 2026-09-13T14:15:00Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- У показанной плашки отказа появился ВЫХОД, и следующая настоящая авария человеку всё равно показывается
- Цена формы правки по критерию 3 названа владельцу замерами, принята ИМ и записана третьей записью блока `overrides` — узко, ровно на две названные строки
- Пять находок UI-ревизии распоряжены поимённо; шестнадцать внеобластных находок партии читаются одним распоряжением
- Долг свежего прохода шагов 2.8 и 4.4 записан журналом окон, а не закрыт отметкой

---

## ЗАДАЧА 1 — ОСТАНОВ ВЛАДЕЛЬЦА: ПЕРВОИСТОЧНИК, ПЕРЕНЕСЁННЫЙ ДОСЛОВНО

Задача 1 есть `checkpoint:decision` с `gate="blocking-human"` и `reversibility: one-way`. Коммита она не несёт и нести не имела права: её последний пункт акцептанса требует, чтобы до ответа владельца ни один файл дерева не был тронут. Ноль коммитов у неё — ИСПОЛНЕНИЕ акцептанса, а не пропуск.

### 1.1. Ответ владельца — ДОСЛОВНО, а не пересказом идентификаторами

> **Ветвь: `A` — снятие без регистрации**
> **Область записи отступления: `узко`**

В словаре плана это ветвь `A` (`dismiss-control-plus-reset`) с УЗКИМ `covers`: третья запись `overrides` покрывает РОВНО эту правку — две названные строки в телах двух названных уже существующих обработчиков, — и всякая следующая правка `app/templates/includes/htmx_error_banner.html` требует НОВОГО решения владельца. Широкое (классовое) прочтение предъявлялось отдельным идентификатором и владельцем НЕ выбрано.

Ветви `B`, `C` и `D` не выбраны, и **ни одна их строка в дерево не попала**: ни новой регистрации, ни четвёртого имени события, ни сдвига `FAILURE_BANNER_HANDLERS_MEASURED`. Вся цена ветви `A` в том и состоит, что ни одно из пяти чисел гейта не двигается.

### 1.2. Пять замеров, предъявленных владельцу ДО правки — ДОСЛОВНЫМ ВЫВОДОМ

Сняты на HEAD `3abac55` 2026-09-13T10:48Z, на дереве ДО первого коммита плана. **После задачи 2 они невоспроизводимы, и потому перенесены сюда, а не пересняты:** запись отступления, набранная пересъёмкой, описывала бы дерево ПОСЛЕ правки как основание решения, принятого ДО неё.

**(1)** `grep -c "addEventListener" app/templates/includes/htmx_error_banner.html`
```
3
```

**(2)** `grep -n "FAILURE_BANNER_HANDLERS_MEASURED = " tests/test_pages/test_shell.py`
```
2311:FAILURE_BANNER_HANDLERS_MEASURED = 3
```

**(3)** `uv run pytest tests/test_templates/test_components.py -k criterion_three -q`
```
.                                                                        [100%]
1 passed, 89 deselected, 1 warning in 0.33s
```

**(4)** Поле `covers` ВТОРОЙ действующей записи (`extend-override`, `chubav`, `2026-09-12`) — ДОСЛОВНО:
```
    covers: >-
      ТОЛЬКО ЭТА РЕГИСТРАЦИЯ, И ГРАНИЦА НАЗВАНА ОТВЕТОМ ВЛАДЕЛЬЦА ДОСЛОВНО. Запись
      охватывает РОВНО регистрацию обработчика `htmx:afterRequest` на узле тела документа в
      `app/templates/includes/htmx_error_banner.html:227-233` (коммит `4e24d61`, план
      10-49, 7 исполняемых строк JS в теле единственного блока `<script>` файла). ⚠️ НА
      БУДУЩИЕ РЕГИСТРАЦИИ В ТОМ ЖЕ ФАЙЛЕ ЗАПИСЬ НЕ РАСПРОСТРАНЯЕТСЯ: всякая следующая
      регистрация в `includes/htmx_error_banner.html` требует НОВОГО решения владельца и
      обязана покраснить пятое утверждение гейта критерия 3 (заведено планом 10-52).
      Область настоящей записи узка НАМЕРЕННО — ради узости владелец и отвечал.
```
и поле `covers` ПЕРВОЙ записи (`accept-reading`, `chubav`, `2026-09-09`) — ДОСЛОВНО:
```
    covers: >-
      51 строка JS в `app/templates/ads/form.html` и `app/templates/components/modal.html`.
      Граница отступления — правка ТЕЛА метода уже существующего объекта клиентского
      состояния и правка выражения УЖЕ СУЩЕСТВУЮЩЕГО атрибута события; заведение НОВОЙ
      сущности настоящей записью НЕ принимается.
```

**(5)** `grep -c "x-data" app/templates/includes/htmx_error_banner.html`
```
0
```

**Дерево на момент останова**, `git status --porcelain`:
```
?? .planning/milestone.lock
?? .planning/state.json
```
`git diff --numstat "$PLAN_BASE"..HEAD` по всем шести путям `files_modified` — ПУСТ. Оба артефакта планирования предшествуют плану и ему не принадлежат.

### 1.3. Адреса строк, названные планом, СДВИНУТЫ — и это замер, а не поломка

Планы 10-55 и 10-56 сдвинули адреса, которые текст плана 10-57 называет. Правка шла по ФАКТИЧЕСКИМ адресам на `3abac55`, и это говорится здесь, а не умалчивается:

| Предмет | План называет | Замер на `3abac55` |
|---|---|---|
| блок `overrides` | `10-VERIFICATION.md:204-262` | `:204-295` |
| разметка и сценарий заготовок | `htmx_error_banner.html:213-235` | `:274-298` |
| третий обработчик | `:227-233` | `:290-296` |
| `FAILURE_BANNER_HANDLERS_MEASURED` | `test_shell.py:2295-2312` | константа на `:2311` (диапазон плана — её летопись) |

Каждый предмет найден; ни один не подменён соседним.

### 1.4. Перебор бюджета смарт-зоны — ЧИСЛОМ

Проекция `115000` против бюджета `100000` = **1.15x**, `sample_count: 0` при `min_samples: 3`, `confidence: low`, маршрут — **ПРИНЯТИЕ** (ADR-2629, решение 5: рекомендация проверщика совещательна). Основание принятия — блок `estimate` плана: останов есть ГРАНИЦА контекста, а не его продолжение, поэтому пиковое одно окно бюджета не перебирает, а распил плана лишил бы задачу 3 первоисточника.

**ФАКТИЧЕСКИЙ РАСХОД — ПЕРВЫЙ ОБРАЗЕЦ ДЛЯ КАЛИБРОВКИ, И ОН НЕ ОКРУГЛЁН В СВОЮ ПОЛЬЗУ:** `12072` (шкала `estimateTokens` — знаков реализованного диффа `"$PLAN_BASE"..HEAD` делить на 4: 47344 добавленных знаков плюс 944 удалённых). То есть проекция завышена в **9.5 раза**. Число подаётся следующему кругу как есть; подгонять его к проекции значило бы испортить каждую следующую проекцию.

---

## ЗАДАЧА 2 — ВЫХОД У ПЛАШКИ, ВЕТВЬЮ `A`

### 2.1. Первая строка сводки по акцептансу: **исполнена ветвь `A`, область записи `узко`**

Иной ветви в дереве нет ни строкой.

### 2.2. RED — ЗАМЕРЕН, А НЕ ОБЪЯВЛЕН

Код возврата замером RED не считался. Требовались ТРИ конъюнкта в ОДНОМ прогоне, и все три приведены.

Команда: `uv run pytest tests/test_pages/test_shell.py -k "test_every_failure_banner_carries_a_dismiss_control or test_a_dismissed_banner_returns_on_the_next_failure" -q`, rc=1.

Конъюнкт 1 — строки `FAILED`:
```
FAILED tests/test_pages/test_shell.py::test_every_failure_banner_carries_a_dismiss_control
FAILED tests/test_pages/test_shell.py::test_a_dismissed_banner_returns_on_the_next_failure
```

Конъюнкт 2 — итоговая строка:
```
2 failed, 243 deselected, 1 warning in 1.34s
```

Конъюнкт 3 — ДОСЛОВНЫЙ причинный текст с ПОЛУЧЕННЫМИ значениями.

Правило (4), `test_every_failure_banner_carries_a_dismiss_control`:
```
E       AssertionError: includes/htmx_error_banner.html:
E           — #htmx-failure-server: органов снятия (класс `banner-dismiss`) найдено 0, ожидался РОВНО ОДИН
E               получено: <div id="htmx-failure-server" class="failure-stack" hidden>{{ alert('Действие не выполнено. Попробуйте ещё раз через минуту.', 'error') }}</div>
E               следствие: у показанной плашки нет выхода — на экране, который больше не делает удавшихся обменов, прямоугольник остаётся до перезагрузки страницы (UI-1)
E           — #htmx-failure-network: органов снятия (класс `banner-dismiss`) найдено 0, ожидался РОВНО ОДИН
```

Правило (6), `test_a_dismissed_banner_returns_on_the_next_failure`:
```
E           AssertionError: #htmx-failure-server: состояние органа снятия НЕ СБРОШЕНО новым отказом
E                   получено:  снято=True, скрыто=False
E                   ожидалось: снято=False
E                   следствие: человек, убравший прочитанное сообщение, больше НЕ УЗНАЕТ НИ ОБ ОДНОЙ аварии этого рода до перезагрузки страницы — тишина вместо пришпиленного прямоугольника, то есть регрессия ХУЖЕ исходного дефекта
```

Улика переведена в форму верификатора **МЕХАНИЧЕСКИМ адаптером над настоящим прогоном** (имена читаются из строк `FAILED …::<имя>`, числа — из итоговой строки; адаптер живёт в каталоге сеанса и в дерево не коммитится). Вердикт `gsd-tools check tdd-red-evidence` по ОБОИМ целевым правилам:
```
RED_EVIDENCE_OK / target_test_failed
```

### 2.3. Форма правки

**Разметка** (`app/templates/includes/htmx_error_banner.html`). Внутрь каждого узла заготовки, ПЕРЕД вызовом макроса плашки и на ТОЙ ЖЕ СТРОКЕ, встал `<input type="checkbox" id="htmx-failure-{server|network}-close" class="banner-dismiss" aria-label="Скрыть сообщение">`. Ни одного атрибута события он не несёт, числа порядка обхода — тоже: в порядок обхода он попадает САМ. Разметка каждой заготовки осталась ОДНОЙ строкой.

**Таблица стилей** (`app/static/css/app.css`). Четыре блока внешнего вида органа (положение внутри уже поднятой заготовки, снятие вида по умолчанию, крестик генерируемым содержимым, признак фокуса по образцу действующих `:focus-visible`) и ОДИН блок скрытия:
```css
.failure-stack:has(> .banner-dismiss:checked) { display: none; }
```

**Сценарий.** В тела ДВУХ действующих обработчиков добавлено по ОДНОЙ строке:
```js
var server_close = document.getElementById('htmx-failure-server-close'); if (server_close) { server_close.checked = false; }
var network_close = document.getElementById('htmx-failure-network-close'); if (network_close) { network_close.checked = false; }
```
Форма этих строк не есть ни вызов метода регистрации слушателя, ни присваивание свойства-обработчика — и это ПРОВЕРЕНО прогоном гейта критерия 3, а не заявлено.

### 2.4. ДВА РАЗНЫХ КРЮЧКА НА ОДИН ОРГАН — ЗАМЕР, А НЕ НЕБРЕЖНОСТЬ

Таблица адресует орган **классом**, сценарий — **идентификатором**, и оба выбора вынуждены:
- селектор `#htmx-failure-server-close` нёс бы подстроку `#htmx-failure-server`, попал бы в отбор `_selector_lifts_banner` и покрасил бы **пять действующих правил подъёма** за ФОРМУ правки;
- имя класса в сценарии сдвинуло бы `grep -c "banner-dismiss"` по разметке с 2 на 4, то есть сломало бы собственный акцептанс плана.

Оба основания выписаны в комментарии рядом с объявлением констант, чтобы следующий читатель не «починил» расхождение.

### 2.5. Акцептанс — ЗАМЕРАМИ

| Пункт | Команда | Получено | Ожидалось |
|---|---|---|---|
| орган у каждой заготовки | `grep -c "banner-dismiss" …/htmx_error_banner.html` | `2` | 2 |
| доступное имя у каждого | `grep -c 'aria-label="Скрыть сообщение"' …` | `2` | 2 |
| разметка — одна строка | `grep -c 'id="htmx-failure-server"'` / `…-network"'` | `1` / `1` | 1 / 1 |
| регистраций в файле | `grep -c "addEventListener" …` | `3` | 3, равно объявлению |
| объявление числа | `grep -n "FAILURE_BANNER_HANDLERS_MEASURED = "` | `2311:… = 3` | не правлено |
| узлов клиентского состояния | `grep -c "x-data" …` | `0` | 0 |
| атрибутов события рантайма | `grep -c "hx-on" …` | `0` | 0 |

**Третий обработчик не тронут — двусторонне.** `git diff "$PLAN_BASE"..HEAD -- app/templates/includes/htmx_error_banner.html` не содержит ни одной изменённой строки в диапазоне его тела (грепы по `afterRequest`, `done_server`, `done_network`, `successful` в изменённых строках — НОЛЬ вхождений), а ПАРА к этому пункту непуста:
```
$ git diff --numstat "$PLAN_BASE"..HEAD -- app/templates/includes/htmx_error_banner.html
28	2	app/templates/includes/htmx_error_banner.html
```
Обе его группы правил и оба их контроля зелены (`test_the_failure_banner_is_cleared_by_a_successful_exchange`, `test_a_failed_exchange_does_not_clear_the_failure_banner` и соседние — `6 passed, 239 deselected` в отборе).

**Блок подъёма не тронут — тоже двусторонне.** Дифф таблицы стилей содержит **НОЛЬ удалённых строк**, то есть ни одна строка блока подъёма и ни одна строка трёх блоков стопки не изменена; пара к пункту непуста:
```
$ git diff --numstat "$PLAN_BASE"..HEAD -- app/static/css/app.css
62	0	app/static/css/app.css
```
`test_the_failure_banner_lift_declares_no_display_mode` зелено.

### 2.6. Прогоны задачи 2

| Команда | Результат |
|---|---|
| `uv run pytest tests/test_pages/test_shell.py -q` | `245 passed` (порог плана — не меньше 240; на `3abac55` было `239 passed`) |
| `uv run pytest tests/test_pages/test_shell.py -k "dismiss or dismissed or display_mode or banner" -q` | `39 passed, 206 deselected` (порог — не меньше пяти) |
| `uv run pytest tests/test_templates/test_components.py -k criterion_three -q` | `1 passed, 89 deselected`; строк «УТВЕРЖДЕНИЕ N ИЗ 5» в выводе НЕТ НИ ОДНОЙ |
| `uv run pytest tests/test_templates/ tests/test_planning/ tests/test_pages/test_confirm_delete_transport.py -q` | `323 passed` |
| `uv run python -m compileall -q app main.py tests` | без отказов |

---

## ЗАДАЧА 3 — ЗАПИСИ

### 3.1. Третья запись блока `overrides` — РЯДОМ, а не вместо

`must_have` подшит к **КРИТЕРИЮ 3** (ветвь `A` отступает именно от него), и формулировка совпадает с двумя действующими ПОСИМВОЛЬНО. `reason` несёт ответ владельца дословно и все пять замеров ВЫВОДОМ. `covers` выписано так, что шире ответа его прочесть нельзя. `accepted_by: "chubav"`, `accepted_at: "2026-09-13"`, `accepted_branch: "dismiss-control-plus-reset"`.

Пункт акцептанса, который **не замеряется одним числом**, замерен двумя:

| Греп | Получено | Ожидалось при ветви `A` |
|---|---|---|
| `grep -c "^      Панель закрывается существующим Alpine"` | `3` | 3 (было 2) |
| `grep -c "^      СНЯТАЯ ЧЕЛОВЕКОМ ПЛАШКА"` | `0` | 0 — маст-хэв возврата плашки отступившим НЕ объявлен |
| `grep -c "^    accepted_branch:"` | `3` | 3 |

Две действующие записи и комментарий круга **не тронуты ни символом**, поля вердикта круга не сдвинуты — замерено диффом:
```
$ git diff --numstat "$PLAN_BASE"..HEAD -- .../10-VERIFICATION.md
104	0	.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
$ git diff "$PLAN_BASE"..HEAD -- .../10-VERIFICATION.md | grep -E "^\+(status:|score:|overrides_applied:|behavior_unverified:|covered_digest:)"
(пусто)
```
Ноль удалённых строк и есть та половина, без которой «записи не тронуты» не утверждало бы ничего; непустой диапазон — вторая половина пары.

### 3.2. Пять находок UI-ревизии — поимённо

Раздел `## План 10-57` вставлен **ПЕРЕД** якорем (`заголовок: строка 577`, `якорь: строка 626`), ровно один:
```
$ grep -c "^## План 10-57$" …/deferred-items.md          → 1
$ grep -o "UI-[3-7]" <участок раздела> | sort -u          → UI-3 UI-4 UI-5 UI-6 UI-7
$ grep -c "^- \*\*Владелец:\*\*" <участок раздела>        → 5
$ grep -c "НЕ ЧИНИТ" <участок раздела>                    → 1  (отдельное предложение при UI-3)
$ git diff --numstat "$PLAN_BASE"..HEAD -- …/deferred-items.md → 49	0
```

**⚠️ ЯКОРЬ В ФАЙЛЕ ВСТРЕЧАЕТСЯ ДВАЖДЫ, И ЭТО ЗАМЕР, ПЕРЕДАВАЕМЫЙ ДАЛЬШЕ.** Второе вхождение — ПРОЗА шапки файла (`:8`), объясняющая читателю форму. Правило `tests/test_planning/test_deferred_items_are_idempotent.py` считает только строки, СОСТОЯЩИЕ из якоря целиком, и потому зелено; наивная подстановка по строке якоря вставила бы раздел в шапку. Вставка шла по номеру строки-якоря, а не по подстроке.

### 3.3. Обратная ссылка: ШЕСТНАДЦАТЬ найдено — ШЕСТНАДЦАТЬ распоряжено

**Одиннадцать** находок седьмого круга ревизии — раздел `## План 10-55` (`grep -c "^### "` по участку → `11`): `WR-01`…`WR-0n`, `IN-01`, `IN-02`, `IN-03`, `IN-04`, `IN-05` и прочие того же перечня; владелец у каждой — следующий круг закрытия гэпов Фазы 10.

**Пять** находок UI-ревизии — раздел `## План 10-57`:

| Находка | Файл | Владелец |
|---|---|---|
| `UI-3` — плашки отказа не оповещают вспомогательные технологии | `includes/htmx_error_banner.html`, `components/alert.html:10` | следующий круг закрытия гэпов Фазы 10 |
| `UI-4` — тела диалогов расходятся у десяти потребителей | `ads/form.html:301`, `accounts/list.html:190` | следующий круг закрытия гэпов Фазы 10 |
| `UI-5` — код ответа валидации гасится молча на пути без подмены | `includes/htmx_error_banner.html`, `components/modal.html:807` | следующий круг закрытия гэпов Фазы 10 |
| `UI-6` — у диалога нет связи с его описанием | `components/modal.html:715`, `:808` | следующий круг закрытия гэпов Фазы 10 |
| `UI-7` — возврат фокуса имеет названную дыру | `components/modal.html:22-30` | следующий круг закрытия гэпов Фазы 10 |

**11 + 5 = 16.** Находки, отсутствующей и в задаче, и в записи, нет ни одной.

### 3.4. Записи журнала окон — КОМАНДОЙ ведения

| № | kind | Предмет |
|---|---|---|
| **81** | `unrun-verify` | Долг СВЕЖЕГО прохода шагов 2.8 и 4.4; у шага 4.4 прибавился предмет — ЧИСЛО ВИДИМЫХ ЗАГОТОВОК и работа органа снятия мышью и с клавиатуры. Цена принята ветвью `A` ПО КРИТЕРИЮ 3 и живёт третьей записью `overrides`. `FORM-06` остаётся ОТКРЫТЫМ |
| **82** | `deviation` | НАЗВАННОЕ следствие ветви `A`: снятая органом первая заготовка оставляет вторую на смещённом месте (см. «Известные следствия» ниже) |

| **83** | `deviation` | `state advance-plan` правит строку ВНУТРИ дословной цитаты чужого файла в `STATE.md`, и подлинник цитаты уже невосстановим по истории (см. отступление 3 ниже) |

Шапка журнала после трёх записей: `open_count: 72`, `total_count: 83`.

⚠️ **ТЕКСТ ЗАПИСИ 83 ЗАВЕДЁН КОМАНДОЙ, А ПОЧИНЕН РУКАМИ, И ЭТО НАЗЫВАЕТСЯ.** Первый вызов `windows append` прошёл через оболочку, которая съела обратные кавычки описания (подстановка команд в двойных кавычках): запись создалась с выпавшими кусками. Счёта шапки при этом верны, поэтому починен ТОЛЬКО текст описания — в табличной строке и в блоке JSON разом, — а `windows status` перечитан после починки и здоров (`open_count: 72`). Заводить вторую запись взамен испорченной значило бы сдвинуть счёт на запись, которой нет.

---

## Task Commits

1. **Задача 1 (останов владельца)** — коммита НЕТ, и это исполнение акцептанса: до ответа владельца ни один файл дерева не тронут.
2. **Задача 2, RED** — `76348e6` (`test(10-57)`)
3. **Задача 2, GREEN** — `1753c10` (`feat(10-57)`)
4. **Задача 3 (записи)** — `67e1e9e` (`docs(10-57)`)

REFACTOR-коммита нет: чистить было нечего — четыре правила и две функции находок написаны в форме соседних, ни одного дубля разбора не заведено.

5. **Метаданные плана** — `3d4b272` (`docs(10-57)`: сводка, `STATE.md`, `ROADMAP.md`, `WINDOWS.md`)

`plan_head_before: 3abac559417bf17321dffa0c47c784136cee16c0`; `git rev-list --count ${PLAN_HEAD_BEFORE}..HEAD` → **4** (измерено, не пересказано; коммит метаданных включён — так же считал план 10-56, у которого пять коммитов с областью `(10-56)`).

## Files Created/Modified

- `app/templates/includes/htmx_error_banner.html` — орган снятия внутри каждой заготовки, две строки сброса в телах двух действующих обработчиков, проза о цене и её основании
- `app/static/css/app.css` — четыре блока внешнего вида органа и один блок скрытия снятой заготовки; блок подъёма и три блока стопки не тронуты
- `tests/test_pages/test_shell.py` — три функции находок, четыре правила, два отрицательных контроля, расширенный гарнир рассылки
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — третья запись блока `overrides`
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md` — раздел `## План 10-57`
- `.planning/WINDOWS.md` — окна 81 и 82

## TDD Gate Compliance

| Гейт | Коммит | Вердикт |
|---|---|---|
| RED | `76348e6` `test(10-57)` | ✓ улика проверена механическим адаптером: `check tdd-red-evidence` → `RED_EVIDENCE_OK` / `target_test_failed` по ОБОИМ целевым правилам |
| GREEN | `1753c10` `feat(10-57)` | ✓ RED предшествует GREEN; четыре новых правила и оба контроля зелены |
| REFACTOR | — | не потребовался; тесты зелены без него |

Гейт `execute-mvp-tdd` (TDD_MODE=true, `workflow.tdd_mode: true`) проверен ПЕРЕД шагом реализации задачи 2: коммит `test(10-57)` существует и трогает `tests/`, улика RED признана намеренной, `feat(10-57)`-коммита до него нет. **Гейт НЕ сработал.**

## Decisions Made

Перечислены в `key-decisions` frontmatter. Главное: **форма правки принадлежит ответу владельца, а не замеру исполнителя** — три неисполненные ветви не вошли в дерево ни строкой, и в частности не заведено ни одной новой регистрации обработчика, хотя именно её предлагала UI-ревизия ДОСЛОВНО.

## Deviations from Plan

### 1. [Rule 3 — Blocking] Гарнир рассылки расширен органом снятия, а не оставлен как был

- **Найдено при:** задача 2, RED (написание правила возврата).
- **Расхождение:** действующий гарнир `FAILURE_BANNER_DISPATCH_HARNESS` стабит `document.getElementById` перечнем ЗАГОТОВОК и возвращает `null` на любое другое имя. Две строки сброса, добавляемые ветвью `A`, ищут орган ИМЕННО через `getElementById` — на нетронутом гарнире они получали бы `null`, и правило возврата было бы НЕВЫРАЗИМО: «сценарий сбросил орган» неотличимо от «сценарий об органе не знает».
- **Правка:** гарнир получил параметры `__CONTROLS__` (перечень «заготовка → её орган») и `__DISMISSED__` (НАЧАЛЬНОЕ состояние органа) и ключ `dismissed` в вердикте. `_failure_banner_hidden_after` оставлена ПРОЕКЦИЕЙ полного состояния на прежнее — шесть действующих правил гашения не переписаны ни строкой и зелены.
- **Проверка:** отбор шести действующих правил гашения и однократности — `6 passed, 239 deselected`.
- **Коммит:** `76348e6`.

### 2. [Rule 2 — Missing Critical] Вторая запись журнала окон — названное следствие ветви `A`

- **Найдено при:** задача 2, разбор взаимодействия с блоками стопки плана 10-56.
- **Расхождение:** план заводит РОВНО одну запись журнала (долг обхода). Но ветвь `A` имеет НАЗВАННОЕ следствие, которого план не предвидел: блок «отсутствия дыры» ключáется на АТРИБУТ скрытия, а орган снятия атрибута не ставит (ставить его означало бы ветвь `B`).
- **Правка:** заведена ВТОРАЯ запись (`№ 82`, `deviation`) — следствие названо числом (зазор в шаг стопки 96px), названо условие его наступления (две одновременные аварии плюс снятие первой) и названо, почему починка есть предмет отдельного плана.
- **Основание правки:** собственный контракт исполнителя требует записывать в журнал каждый известный дефект — иначе он исчезает из виду вместе со сводкой. Запись ничего в плане не нарушает: `<verify>` задачи 3 не ограничивает ЧИСЛА записей журнала.
- **Коммит:** `67e1e9e`.

### 3. [Rule 1 — Bug] `state advance-plan` испортил ДОСЛОВНУЮ ЦИТАТУ чужого файла в `STATE.md`

- **Найдено при:** шаг трекинга, построчное чтение `git diff .planning/STATE.md`.
- **Расхождение:** верб матчит строки вида `- **Status:** …` по ВСЕМУ телу `STATE.md`, а не только в блоке `## Current Position`. В разделе о находке `audit-open acknowledge` (`:470-473`) стои́т ДОСЛОВНАЯ цитата поля, которое писатель вписывает в записи отложенного, — и значение ВНУТРИ цитаты подменилось текущим значением Status фазы: `Ready to execute` → `Phase complete — ready for verification`.
- **Правка:** значение восстановлено. ⚠️ **ВОССТАНОВЛЕНО ПО СМЫСЛУ, А НЕ ПО БАЙТАМ, И ЭТО НАЗВАНО ПРЯМО:** `git log -S` доводит только до коммита `3d1e672` (архивирование вехи v2.0), где строка читалась УЖЕ ПОДМЕНЁННОЙ (`v2.0 milestone complete`), то есть порча СТАРШЕ первого появления строки в истории и подлинник из неё невосстановим. Вписано `resolved` — именно его вписывает `audit-open acknowledge`, именно его читает сканер отложенного, и именно о нём говорит соседнее предложение самой цитаты («тот же маркер, который писатель поставил бы сам»).
- **Проверка:** `git diff .planning/STATE.md` прочитан ПОСТРОЧНО целиком; иных правок вне блока позиции, таблицы метрик, раздела решений и блока непрерывности сеанса верб не внёс.
- **Записано:** окно журнала **83** — вместе с тем, что починка есть правка ВЕРБА, а не четырнадцатая правка прозы.
- **Коммит:** тот же, что у метаданных плана.

### 4. [Rule 1 — Bug] `state add-decision` УДВОИЛ префикс фазы у трёх записей

- **Найдено при:** том же построчном чтении диффа.
- **Расхождение:** верб приписывает `- [Phase 10]: ` сам, а текст решения уже нёс этот префикс — вышло `- [Phase 10]: [Phase 10]: …` у всех трёх записей.
- **Правка:** удвоение снято у трёх строк.
- **Проверка:** `grep -n "\[Phase 10\]: \[Phase 10\]:" .planning/STATE.md` → ноль вхождений.
- **Коммит:** тот же, что у метаданных плана.

### 5. [Rule 3 — Blocking] `state add-decision` молча отказывает на путях вне корня проекта

- **Найдено при:** первой попытке записать три решения файлами из каталога сеанса.
- **Расхождение:** верб вернул `added: false` с `reason: "Path escapes allowed directory"` и НУЛЕВОЙ код возврата — то есть `tail -1` и проверка кода возврата показали бы успех при незаписанном решении.
- **Правка:** файлы решений скопированы во временный каталог ВНУТРИ корня проекта, вызовы повторены, поле `added` проверено у КАЖДОГО вызова (`true`, `true`, `true`), временный каталог снят.
- **Коммит:** тот же, что у метаданных плана.

---

**Total deviations:** 5 auto-fixed (2 blocking, 1 missing critical, 2 bugs).
**Impact on plan:** первые две необходимы по предмету плана, три последние — починки инструментов трекинга, найденные ПОСТРОЧНЫМ чтением диффа, а не угаданные. Первая — условие выразимости правила, которое план сам и предписал; вторая — условие того, чтобы следствие правки не исчезло вместе со сводкой. Разрастания области нет: продуктовых файлов по-прежнему два, и ни один из них не правлен сверх ветви `A`.

## Известные следствия (не стабы, а названная цена)

**Зазор стопки при снятии ПЕРВОЙ заготовки** — `app/static/css/app.css`, окно журнала **82**. При ДВУХ одновременных авариях человек, снявший первую плашку, видит вторую на смещённом месте, и над ней остаётся пустой зазор в шаг стопки (96px). Ни одна плашка при этом не теряется и ни одна не накрывает другую.

**Почему не починено — ЗАМЕР, А НЕ ЛЕНЬ.** Обе мыслимые формы починки красят правила стопки плана 10-56 за ФОРМУ правки:
- ЧЕТВЁРТЫЙ блок, объявляющий `--failure-banner-top` по состоянию вложенного органа, разбор `_stack_blocks` относит к роли СМЕЩЕНИЯ (две части селектора, без признака в квадратных скобках) — два блока смещения ломают `_banner_stacking_findings`;
- расширение селектора роли `reset` запятой разбивает его разбор на ТРИ части, роль остаётся ПУСТОЙ, и краснеет `test_a_single_shown_banner_keeps_the_base_offset`.

То есть починка есть правка РАЗБОРА РОЛЕЙ, а не правка таблицы, — предмет отдельного плана, и он назван журналом.

## Issues Encountered

**Предмета плана — ни одного.** Ни одно правило не потребовало второй попытки; ни один прогон не дал незапланированной красноты.

**Трекинга — четыре, и все четыре ПРЕДСКАЗАНЫ, а не обнаружены** (переданы исполнителю оркестратором дословно и подтверждены замером):

1. **Тринадцатое срабатывание `test_the_prose_plan_counts_agree_with_the_marks`.** `roadmap update-plan-progress` поставил отметку плану и НЕ тронул прозу `**Plans**: 57 планов, из них 56 исполнено`. Правило покраснело ровно там, где его ждали: `ProseDivergence(section='10: Рычаг `components/modal.html`', quantity='исполнено планов', prose=56, marks=57)`. Приведена ПРОЗА (отметки — объявленный источник счёта), тринадцатое срабатывание записано в летопись самой строки в идиоме соседних записей, гейт перепрогнан НЕПОСРЕДСТВЕННО перед коммитом — `44 passed`.
2. **`state advance-plan` не трогает прозу `Completed Plans in Phase:`** — строка осталась на `56` и приведена к `57` рукой.
3. **`state advance-plan` испортил дословную цитату чужого файла** — отступление 3 выше.
4. **`state add-decision` молча отказал на путях вне корня проекта** — отступление 5 выше.

Все четыре — свойства инструментов трекинга, а не следствия предмета плана, и каждое найдено ПОСТРОЧНЫМ чтением диффа, а не зеленью шага.

## User Setup Required

Нет — внешней настройки план не требует.

## Next Phase Readiness

**Готово:** у плашки отказа есть выход, и он не отключает канал до конца сессии. Третья запись блока `overrides` стои́т рядом с двумя действующими и ждёт СЛЕДУЮЩЕГО круга верификации — засчитать её план не вправе и не засчитывал.

**Открыто и названо:**
- **`FORM-06` НЕ закрыт.** Закрывает его ПОДПИСЬ ЧЕЛОВЕКА на приёмке обхода `10-UAT.md`; флажок `.planning/REQUIREMENTS.md:31` пуст, клетка `:136` несёт `Gaps Found`, и ни один символ партии их не двигает.
- **Шаги 2.8 и 4.4 обхода требуют СВЕЖЕГО прохода** (окно 81), и у шага 4.4 прибавился предмет.
- **Пять находок UI-ревизии** (`UI-3`…`UI-7`) распоряжены записью, а не задачей.
- **Зазор стопки** (окно 82) — предмет отдельного плана.
- Решения владельца по 285 запретам фазы и по двенадцати открытым угрозам `10-SECURITY.md` (область отказа `phase-10-only`) настоящим планом не затрагиваются.

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-13*

## Self-Check: PASSED

Шесть правленых файлов и сама сводка найдены на диске проверкой `[ -f ]`; три коммита плана
(`76348e6`, `1753c10`, `67e1e9e`) найдены в `git log --oneline --all`. Все пункты акцептанса
трёх задач перепрогнаны и приведены выше ВЫВОДОМ, а не пересказом.
