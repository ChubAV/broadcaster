---
phase: 10-rychag-components-modal-html
plan: 01
subsystem: ui
tags: [htmx, jinja2, fastapi, oob-swap, progressive-enhancement, inventory-gates]

requires:
  - phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
    provides: "Опциональные параметры макроса панели, шипнутый фрагментный путь удаления группы, образец ответа с внеполосными узлами, единственный источник линейки счётчика, перечень OOB_TARGET_EXCEPTIONS"
  - phase: 08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy
    provides: "respond() с обязательным redirect=, ветка location_response (204 + HX-Location), фикстура htmx_client, идиома инвентарного гейта собственным числом"
provides:
  - "Безусловные атрибуты htmx и узел индикатора в макросе панели подтверждения — все 18 мест получают отправку одной правкой одного файла"
  - "Сигнатура modal() без двух снятых параметров; ни один вызывающий их не передаёт"
  - "ads/partials/sched_delete_response.html — три внеполосных узла верхнего уровня, среди них ОТДЕЛЬНЫЙ узел снятия осиротевшей панели"
  - "ads/includes/sched_count_rule.html — единственный источник разметки линейки счётчика редактора"
  - "_editor_url() — один источник адреса приземления на обоих транспортах"
  - "_ad_has_a_schedule() и _ad_schedule_count() — предикат ветвления и счёт, оба под скоупом Ad.user_id"
  - "_ad_id_from_form() — контекст экрана, переживающий исчезновение строки (неотличимость повтора)"
  - "Переведённый schedules_delete: фрагмент при оставшихся расписаниях, 204 + HX-Location на последнем, 302 без htmx, ноль собственных редиректов"
  - "Четвёртый счёт инвентаря мест: аргументы вызывающих ⊆ сигнатура макроса"
  - "test_the_schedule_counter_markup_has_exactly_one_source — гейт единственного источника линейки редактора"
affects: [10-02, 10-03, 10-04, "Фаза 11 — массовый перевод разделов письма"]

actuals:
  tokens: 32455
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Безусловная раздача свойств качества макросом вместо опционального параметра (promote, а не add-alongside)"
    - "Контекст экрана скрытым полем, переживающий исчезновение строки, — условие неотличимости повтора на фрагментном пути"
    - "Летопись НЕПОДВИЖНОСТИ числа как полноправная запись наравне с летописью движения"

key-files:
  created:
    - app/templates/ads/includes/sched_count_rule.html
    - app/templates/ads/partials/sched_delete_response.html
  modified:
    - app/templates/components/modal.html
    - app/templates/account_groups/includes/group_row.html
    - app/templates/components/form_wrapper.html
    - app/templates/ads/form.html
    - app/templates/ads/includes/sched_card.html
    - app/pages/schedules.py
    - tests/test_pages/test_editor_schedules.py
    - tests/test_pages/test_htmx_gates.py
    - tests/test_templates/test_components.py
    - tests/test_templates/test_htmx_inventory.py
    - tests/test_templates/test_htmx_markup_gates.py

key-decisions:
  - "CONDITIONAL_PLACES НЕ двинут 2 → 1: константа инвентаризует условные входы ЧТЕНИЯ (hx-get), а панель условием собирала вход ЗАПИСИ. Подгонка снесла бы два работающих утверждения."
  - "Контекст экрана (идентификатор объявления) заведён вторым скрытым полем обеих форм пути удаления: без него повтор был отличим от первого удаления и кодом, и адресом."
  - "Фикстура htmx_client не запрашивается тестом пары транспортов — она ставит признак на ОБЩИЙ объект клиента и молча превращала половину деградации во вторую половину htmx."
  - "Имена снятого параметра и включаемого файла не набраны прозой в дереве шаблонов: греп снятия зеленел бы на собственном комментарии."

patterns-established:
  - "Доказательство покраснения расширенного правила без git stash: исходники пред-фазового коммита берутся `git show`, утверждения применяются к ним напрямую, рабочее дерево не трогается"
  - "Летопись неподвижности числа с ОСНОВАНИЕМ — там, где читатель вправе ожидать движения (FRAGMENT_ROUTES_DECLARED, HX_POST_PLACES)"

requirements-completed: []

coverage:
  - id: D1
    description: "Атрибуты htmx и узел индикатора печатает макрос панели БЕЗУСЛОВНО; двух снятых параметров в сигнатуре нет, условия шаблонизатора внутри открывающего тега нет, ни один вызывающий признака отправки не передаёт"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_templates/test_components.py#test_the_panel_hands_the_sending_state_to_every_call_site"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_components.py#test_modal_guard_is_inherited_by_every_consumer"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_components.py#test_modal_site_inventory"
        status: pass
    human_judgment: false
  - id: D2
    description: "Удаление расписания из редактора отвечает фрагментом из трёх внеполосных узлов верхнего уровня, среди которых ОТДЕЛЬНЫЙ узел снятия осиротевшей панели по её id"
    requirement: FORM-06
    verification:
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_editor_delete_returns_oob_nodes"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_the_orphaned_panel_is_removed_by_its_own_oob_node"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_editor_schedules.py#test_control_negative_a_conditional_removal_node_reddens_the_gate"
        status: pass
    human_judgment: false
  - id: D3
    description: "Последнее расписание объявления уводит переходом 204 + HX-Location; второй отрисовки пустого состояния не заведено"
    requirement: FORM-06
    verification:
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_the_last_schedule_goes_to_location"
        status: pass
    human_judgment: false
  - id: D4
    description: "Без заголовка htmx маршрут отвечает прежним 302 на прежний адрес; собственного редиректа у обработчика не осталось ни в одной ветке; адрес приземления совпадает на обоих транспортах"
    requirement: FORM-06
    verification:
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_editor_delete_degrades_without_htmx"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_the_editor_delete_address_is_the_same_on_both_transports"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_htmx_gates.py#test_no_converted_handler_keeps_its_own_redirect"
        status: pass
    human_judgment: false
  - id: D5
    description: "Повтор безвреден и неотличим; чужое расписание не удаляется и его ответ неотличим от ответа на несуществующее"
    requirement: FORM-06
    verification:
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_repeated_editor_delete_is_harmless"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_editor_delete_does_not_trust_the_schedule_of_another_owner"
        status: pass
    human_judgment: false
  - id: D6
    description: "Разметка линейки счётчика редактора имеет ровно один источник, и правило это стережёт; сводный список /schedules в предмет правила не входит"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py#test_the_schedule_counter_markup_has_exactly_one_source"
        status: pass
    human_judgment: false
  - id: D7
    description: "Обе формы пути удаления шлют один набор имён полей; ни одна форма-триггер в дереве не несёт признака отправки htmx (18 форм, число утверждено константой)"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_editor_schedules.py#test_both_editor_delete_forms_post_the_same_field_names"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_editor_schedules.py#test_the_trigger_form_never_carries_the_htmx_post"
        status: pass
    human_judgment: false
  - id: D8
    description: "Инвентарные числа сдвинуты прогоном покрасневшего правила: OOB_BLOCKS 9→12, NOT_YET_CONVERTED_COUNT 34→33, sched-count в перечне долгоживущих областей; одиннадцать чисел подтверждены неподвижными"
    verification:
      - kind: unit
        ref: "uv run pytest tests/test_templates/ tests/test_pages/test_htmx_gates.py -q — 186 passed"
        status: pass
    human_judgment: false
  - id: D9
    description: "hx-swap-oob=\"delete\" ДЕЙСТВИТЕЛЬНО снимает осиротевшую панель со страницы: после N удалений расписаний в документе не остаётся N узлов role=\"dialog\" с живыми ловушками фокуса — ручной UAT, пункт 4 перечня роадмапа, на карточке внутри редактора объявления"
    verification: []
    human_judgment: true
    rationale: "httpx внеполосных узлов НЕ ПРИМЕНЯЕТ и не свопает: машинного эквивалента у этого наблюдения не существует и не заводится. Тесты доказывают, что узел УЕХАЛ В ОТВЕТЕ; что рантайм по нему снял элемент, наблюдается только в браузере."
  - id: D10
    description: "Своп фрагмента приземлился ИМЕННО туда: карточка #sched-N исчезла, линейка счётчика в #sched-count обновилась на месте и не уехала из документа, соседние карточки не сдвинулись и не задвоились — ручной UAT, пункт 3 перечня"
    verification: []
    human_judgment: true
    rationale: "Предмет — визуальный результат применения внеполосных узлов в живом документе; тестовый клиент их не применяет."

duration: 55min
completed: 2026-09-02
status: complete
---

# Phase 10 Plan 01: Рычаг `components/modal.html` и фрагментное удаление расписания — Summary

**Оба опциональных параметра сняты с макроса панели, атрибуты htmx печатаются безусловно — 18 мест подтверждения получили отправку одной правкой одного файла; тот же план провёл рычаг насквозь на единственном фрагментном пути фазы: удаление расписания из редактора отвечает тремя внеполосными узлами, среди которых отдельный узел снятия осиротевшей панели.**

## Performance

- **Duration:** 55 min
- **Started:** 2026-09-02T19:24:00Z
- **Completed:** 2026-09-02T20:19:00Z
- **Tasks:** 3
- **Files modified:** 13 (2 created, 11 изменены)

## Accomplishments

- **Рычаг дёрнут.** Признак отправки и селектор включаемых данных сняты с сигнатуры `modal()`; `hx-post` / `hx-swap="none"` / `hx-disabled-elt` / `hx-indicator`, привязка возврата состояния и узел `form-busy` печатаются БЕЗУСЛОВНО. Условия шаблонизатора внутри открывающего тега формы не осталось ни одного.
- **Единственный сегодняшний носитель признака перестал его передавать**, а отменённые обоснования в трёх файлах помечены, а не вычеркнуты.
- **Фрагментный путь доехал от разметки до внеполосных узлов**: `schedules_delete` переведён на слой ответа, отвечает тремя узлами верхнего уровня при оставшихся расписаниях и 204 + `HX-Location` на последнем; собственного редиректа не осталось ни в одной ветке, включая «нет сессии».
- **Линейка счётчика редактора получила единственный источник** и постоянную обёртку `#sched-count`, а правило единственного источника — гейт с антивакуумной половиной.
- **Пять чисел подвинуты прогоном покрасневшего правила, одиннадцать подтверждены неподвижными**; одно из ожидавшихся движений оказалось ошибкой плана и разобрано записью, а не подгонкой.

## Task Commits

1. **Задача 1 (RED): падающие правила рычага и фрагментного пути** — `c5b1933` (test)
2. **Задача 1 (GREEN): безусловный рычаг и фрагментное удаление** — `14face5` (feat)
3. **Задача 2: числа, которые двигает эта правка** — `96ef0b2` (test)
4. **Задача 3: пары транспортов и безусловность узлов снятия** — `5766b03` (test)

_Задача 1 исполнена как TDD-задача и потому несёт два коммита (RED → GREEN)._

## Files Created/Modified

- `app/templates/components/modal.html` — сигнатура без двух параметров, безусловные атрибуты, шапка с прочтением критерия 3 и пометкой отменённых абзацев
- `app/templates/account_groups/includes/group_row.html` — снятие аргумента у единственного вызывающего, три переписанных абзаца летописи
- `app/templates/components/form_wrapper.html` — имя снятого параметра убрано из прозы (греп снятия)
- `app/templates/ads/includes/sched_count_rule.html` (НОВЫЙ) — единственный источник разметки линейки
- `app/templates/ads/partials/sched_delete_response.html` (НОВЫЙ) — три внеполосных узла верхнего уровня
- `app/templates/ads/form.html` — постоянная обёртка `#sched-count`, включение общего источника, снятие двух осиротевших импортов
- `app/templates/ads/includes/sched_card.html` — второе поле контекста в ОБЕИХ формах пути удаления
- `app/pages/schedules.py` — `_editor_url`, `_ad_id_from_form`, `_ad_has_a_schedule`, `_ad_schedule_count`, переведённый `schedules_delete`
- `tests/test_pages/test_editor_schedules.py` — одиннадцать новых правил
- `tests/test_templates/test_components.py` — новое рендерное правило, расширение двух правил критерия 1, разборщики сигнатуры и аргументов
- `tests/test_templates/test_htmx_markup_gates.py` — `OOB_BLOCKS` 9→12, `sched-count`, новое правило единственного источника, две летописи неподвижности
- `tests/test_pages/test_htmx_gates.py` — `NOT_YET_CONVERTED_COUNT` 34→33, комментарий фазовой сцепки
- `tests/test_templates/test_htmx_inventory.py` — летопись НЕподвижности `CONDITIONAL_PLACES`

## Числа: вывод каждого покрасневшего правила ДОСЛОВНО

### `OOB_BLOCKS` 9 → 12

Вывод `test_the_number_of_oob_blocks_is_the_declared_one` на дереве после задачи 1:

```
AssertionError: внеполосных блоков найдено 12, объявлено 9: ['account_groups/partials/count_rule_oob.html', 'account_groups/partials/delete_response.html', 'ads/includes/autosave.html', 'ads/includes/autosave_response.html', 'ads/partials/sched_delete_response.html', 'includes/notice_oob.html']
assert 12 == 9
```

Прибавка ровно три — снятие карточки, снятие панели, подмена содержимого области счётчика. Файл линейки в счёт не входит: он СОДЕРЖИМОЕ, признака внеполосной подмены не несёт. Ожидаемое планом направление (`+3`) подтвердилось.

### `NOT_YET_CONVERTED_COUNT` 34 → 33

Вывод `test_the_backlog_matches_the_declared_count`:

```
AssertionError: счётчик отставания краснеет на неизменённом дереве
assert 33 == 34
```

**Это движение СЧЁТЧИКА ПРОГРЕССА ВЕХИ, а не служебной константы.** Снятый обработчик — **удаление расписания из редактора объявления** (названо словами; ключ перечня в летописи не набран, иначе греп снятия зеленел бы на собственном комментарии). Число убывающее: рост означает регрессию, падение — проделанную работу. Фаза 10 ведёт его к 26; семь оставшихся шагов делает план 10-03.

### `HX_POST_PLACES` — пересчитан, 3 → 3

Гейт зелен без правки: место отправки у панели было и осталось ОДНО, изменилось лишь то, что оно печатается всегда, а не под условием. Ожидание плана («значение 3; подтвердить прогоном») подтверждено. Прибавка означала бы второй тег отправки в макросе, то есть ошибку формы.

### `LONG_LIVED_REGION_IDS` — пополнен значением `sched-count`

Область переживает сколько угодно ответов удаления; унесённая подменой узла один раз, она лишила бы цели все последующие и завела бы по строке в консоли на каждый.

### `FRAGMENT_ROUTES_DECLARED` — 11 → 11, неподвижность ЗАПИСАНА

Основание: множество считает маршруты, у которых пути деградации нет ВОВСЕ. Переведённый обработчик отдаёт фрагмент ЧЕРЕЗ слой ответа, то есть путь деградации у него есть, и в это множество он не входит по построению.

### Подтверждены неподвижными (утверждением, а не молчанием)

`MODAL_PLACES` 18, `MODAL_EVENT_NAMES` 9, `MODAL_IMPORTERS` 11, `MODAL_CONSUMERS` 10 записей, `HX_TARGETS` 1, `CLIENT_STATE_NODES` 24, `HX_HEADER_READS` 1, `HX_HEADER_WRITES` 2, `FRAGMENT_ROUTES_DECLARED` 11, `MACRO_BORN_EXCEPTIONS_DECLARED` 1, `DISABLED_ELT_EXCEPTIONS_DECLARED` 2.

## Наблюдение, без которого доказательства расхождения одной правки на всех нет

**Правило `test_the_panel_hands_the_sending_state_to_every_call_site` было КРАСНЫМ до правки макроса.** Вывод прогона на дереве до задачи 1, дословно:

```
AssertionError: макрос панели раздаёт свойства качества не всем вызывающим — без аргументов не приехало: ['hx-post="/ads/1/delete"', 'hx-swap="none"', 'hx-disabled-elt="find button[type=submit]"', 'hx-indicator="find .form-busy"']; тег формы: '<form class="modal__form" method="post" action="/ads/1/delete"\n          x-on:submit="if (sending) { $event.preventDefault(); return; } sending = true">'
```

Правило рендерит макрос БЕЗ единственного дополнительного аргумента. Красное оно означало, что htmx приходил только тому вызывающему, который его ПОПРОСИЛ; зелёное — что он приходит из макроса всем.

## Расширение двух правил критерия 1: что расширено и покрасневший прогон на `9f3c59e`

Прогон выполнен БЕЗ `git stash` — он в worktree запрещён (стек сташей общий на все worktree, и `pop` способен принести чужой WIP). Форма другая, предмет тот же: исходники пред-фазового коммита взяты `git show`, утверждения применены к ним напрямую, рабочее дерево не тронуто.

### `test_modal_guard_is_inherited_by_every_consumer` — КРАСНОЕ на `9f3c59e`

Расширено: прибавлено утверждение БЕЗУСЛОВНОСТИ унаследованного — набор свойств качества стои́т в исходнике макроса ВНЕ какого-либо условия шаблонизатора.

```
открывающий тег формы панели на 9f3c59e:
  '<form class="modal__form" method="{{ method }}" action="{{ action }}"\n          x-on:submit="if (sending) { $event.preventDefault(); return; } sending = true"\n          {%- if hx_post %} x-on:htmx:after-request="sending = false" hx-post="{{ action }}" hx-swap="none" hx-disabled-elt="find button[type=submit]" hx-indicator="find .form-busy"{% if hx_include %} hx-include="{{ hx_include }}"{% endif %}{% endif %}>'

свойств качества НЕ найдено безусловно: []
конструкция шаблонизатора в теге: True
ВЕРДИКТ: КРАСНОЕ
```

⚠️ **Наблюдение, стоящее записи:** проверка вхождением подстроки на пред-фазовом теге ЗЕЛЕНА (`[]`) — свойства там присутствуют текстуально, только под условием. Краснит правило второе утверждение, про конструкцию шаблонизатора. Одного утверждения о вхождении было бы недостаточно, и это ровно тот класс отказа, на котором Фаза 9 споткнулась дважды.

### `test_modal_site_inventory`, четвёртый счёт — ЗЕЛЁНОЕ на `9f3c59e`, и это честная запись

Расширено: прибавлен ЧЕТВЁРТЫЙ счёт — множество именованных аргументов всех вызывающих ⊆ имена сигнатуры.

```
сигнатура на 9f3c59e: ['action', 'body', 'cancel_label', 'confirm_label', 'confirm_variant', 'hx_include', 'hx_post', 'id', 'method', 'title']
передаёт единственный вызывающий: ['action', 'body', 'confirm_label', 'hx_post', 'id', 'method', 'title']
лишних имён на СОГЛАСОВАННОМ пред-фазовом дереве: []
```

**План требовал, чтобы покраснели ОБА правила; покраснело одно, и это записано как есть, а не подогнано.** Основание: на пред-фазовом дереве сигнатура и вызывающий были СОГЛАСОВАНЫ — оба знали снятый параметр, — поэтому подмножество выполнялось. Четвёртый счёт стережёт РАСХОЖДЕНИЕ, а его тогда не существовало. Невакуумность доказана контролем на смешанном дереве (пред-фазовый вызывающий против фазовой сигнатуры):

```
сигнатура после правки: ['action', 'body', 'cancel_label', 'confirm_label', 'confirm_variant', 'id', 'method', 'title']
лишние имена: ['hx_post']
ВЕРДИКТ: КРАСНОЕ
```

**Числа при этом НЕ ДВИНУЛИСЬ** — импортёров 11, имён события 9, мест 18, потребителей 10, — и именно неподвижность доказывает, что рычаг инвентаря не тронул.

## ⚠️ Промежуточное состояние волн: КОММИТ ВЫКАТКЕ НЕ ПОДЛЕЖИТ

После этого плана **все 18 панелей подтверждения шлют запрос через htmx**, а **семь из восьми обработчиков ещё отвечают перенаправлением**. Слой письма идёт по редиректу ПРОЗРАЧНО и выбрасывает полученное тело, потому что способ подмены у формы панели — «ничего». Наблюдаемый исход для человека на этих семи: **открытая панель и тишина** — действие ВЫПОЛНЯЕТСЯ, но об этом не сообщается ничем.

Состояние снимается приземлением **плана 10-03** (волна 2).

**Альтернатива «перенести 10-03 в волну 1» отвергнута по двум измеренным причинам:**
1. `files_modified` планов 10-01 и 10-03 ПЕРЕСЕКАЮТСЯ на `tests/test_pages/test_htmx_gates.py`; правило волн запрещает им стоять рядом.
2. Пересечение — СЦЕПКА ПО ЗНАЧЕНИЮ: этот план опускает счётчик прогресса вехи 34 → 33, а предусловие 10-03 требует ИМЕННО 33, откуда ведёт 33 → 31 → 27 → 26. В одной волне одно из движений потерялось бы либо ставилось вычитанием в уме.

Красного правила на промежуточное состояние не заведено сознательно: правило, красное по замыслу, в дереве запрещено — красный цвет обязан означать дефект, а не расписание работ. Стражем становится параметризованный обход из 10-03, приземляющийся в той же волне. В перечне отставания рядом с семью оставшимися ключами стои́т комментарий фазовой сцепки, называющий наблюдаемый исход словами.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 — Missing Critical] Повтор удаления был ОТЛИЧИМ от первого удаления**

- **Found during:** Задача 1, GREEN-фаза (`test_repeated_editor_delete_is_harmless` остался красным после реализации плана дословно)
- **Issue:** План велел собирать контекст экрана из `ad_id`, снятого С НАЙДЕННОЙ СТРОКИ. На повторном запросе строки уже нет, `ad_id` становится `None`, и обработчик уходил в ветку перехода (204 → `/schedules`) вместо ветки фрагмента (200). То есть неотличимость, объявленная планом свойством ШАБЛОНА («ветви по факту удаления в файле нет»), ломалась ВЫШЕ шаблона — в обработчике, — и безусловность узлов её не спасала. Прогон: `assert 204 == 200`.
- **Fix:** Заведён `_ad_id_from_form()`; ОБЕ формы пути удаления несут второе скрытое поле контекста (механизм D-02, рядом с признаком возврата). Найденная строка остаётся ПЕРВЫМ источником, поле — запасным и только там, где строки нет. Величина коэрцится к целому (T-02-23), владение по ней не презюмируется: и предикат ветвления, и счёт ограничены связью `Ad.user_id`.
- **Files modified:** `app/pages/schedules.py`, `app/templates/ads/includes/sched_card.html`, `tests/test_pages/test_editor_schedules.py`
- **Verification:** `test_repeated_editor_delete_is_harmless` зелено; `test_both_editor_delete_forms_post_the_same_field_names` (задача 3) стережёт равенство наборов полей обеих форм
- **Committed in:** `14face5`

**2. [Rule 3 — Blocking] Проза `form_wrapper.html` набирала имя снятого параметра**

- **Found during:** Задача 1, прогон критерия приёмки
- **Issue:** Критерий требует НОЛЬ вхождений имени снятого параметра в `app/templates/` вне самого компонента. `form_wrapper.html` называл его в объяснении своего умолчания — третий файл, планом не названный.
- **Fix:** Абзац переписан без набора имени, с явной записью о том, ПОЧЕМУ имя не набрано (та же дисциплина, которой в этом файле уже не набрано имя охраняемого атрибута).
- **Files modified:** `app/templates/components/form_wrapper.html`
- **Verification:** `grep -rn 'hx_post' app/templates/ | grep -v modal.html` → 0
- **Committed in:** `14face5`

**3. [Rule 3 — Blocking] Действующее правило рендерило макрос снятым параметром**

- **Found during:** Задача 1, RED-фаза
- **Issue:** `test_modal_hands_the_sending_state_back_on_the_htmx_path` рендерил панель с признаком отправки. Jinja роняет макрос на неизвестном именованном аргументе, то есть правило упало бы вместе со снятием параметра. Его второе утверждение («признак просочился в УМОЛЧАНИЕ») после D-14 стало прямо ЛОЖНЫМ.
- **Fix:** Рендер без снятого аргумента; отменённое утверждение снято, а не оставлено красным.
- **Files modified:** `tests/test_templates/test_components.py`
- **Committed in:** `c5b1933`

**4. [Rule 1 — Bug] Комментарии инфлировали греп единственного источника**

- **Found during:** Задача 1, прогон критерия приёмки (`grep -c 'sched_count_rule.html' ads/form.html` вернул 3 вместо 1)
- **Issue:** Мои собственные комментарии называли включаемый файл по имени — ровно тот приём, от которого план предупреждает («греп зеленеет на собственном комментарии»), только в обратную сторону: греп КРАСНЕЛ на прозе.
- **Fix:** Имя файла убрано из обеих прозаических записей, с объяснением, почему оно не набрано.
- **Committed in:** `14face5`

**5. [Rule 1 — Bug] Осиротевшие импорты в `ads/form.html`**

- **Found during:** Задача 1
- **Issue:** После переезда разметки линейки страница продолжала импортировать подпись счётчика и макрос моноширинного текста, не используя ни того ни другого. Импорт утверждал бы, что страница собирает линейку сама, — та самая вторая копия, ради снятия которой файл и заведён.
- **Fix:** Оба импорта сняты, снятие объяснено комментарием.
- **Committed in:** `14face5`

### Отклонения от плана, НЕ являющиеся автоправкой

**6. `CONDITIONAL_PLACES` НЕ двинут 2 → 1 — инструкция плана ошибочна**

План (задача 2, пункт 1; `must_haves`; критерий успеха 7) требовал опустить `CONDITIONAL_PLACES` 2 → 1 со ссылкой на снятие условия шаблонизатора вокруг htmx-атрибутов панели. **Требование измерено и отклонено:**

- Константа живёт в `tests/test_templates/test_htmx_inventory.py` и инвентаризует **условные входы ЧТЕНИЯ (`hx-get`)**. Её перечень — карточка состояния синхронизации и ответ синхронизации групп; ни то ни другое правкой не задето.
- Панель условием собирала вход **ЗАПИСИ**. Снятие того условия сдвинуть это число не могло: `test_htmx_inventory.py` остался ЗЕЛЁНЫМ (19 passed) и после правки.
- Поставленная в единицу, константа покраснила бы ДВА работающих утверждения: сумму перечня механизма 3 и тождество «сумма трёх механизмов == объявленное число входов чтения» (21).

То есть число, подогнанное под рассказ плана, снесло бы живой инвариант — ровно тот исход, ради предотвращения которого числа в этом дереве ставятся прогоном покрасневшего правила. Отклонение записано ЛЕТОПИСЬЮ НЕПОДВИЖНОСТИ прямо у константы, с разбором и с указанием, где выигрыш фазы меряется на самом деле.

**Выигрыш при этом настоящий и не потерян:** условие шаблонизатора внутри открывающего тега формы панели действительно исчезло, и стережёт это `test_modal_guard_is_inherited_by_every_consumer`. Слепая зона UAT-пункта 9 сокращена на рычаге всех 18 мест — но в ЗАПИСИ и в правиле, а не в этой константе.

**7. Критерий приёмки задачи 1 про счётчик отставания недостижим в её границах**

Задача 1 требует зелёного `pytest tests/test_pages/test_htmx_gates.py -k "own_redirect or backlog"`, тогда как задача 2 существует затем, чтобы поставить число ПРОГОНОМ ПОКРАСНЕВШЕГО ПРАВИЛА. Оба одновременно невыполнимы по построению. Половина `own_redirect` зелена сразу после задачи 1 (0 собственных редиректов); половина `backlog` зеленеет коммитом задачи 2. Предусловие самой задачи 2 (`test_editor_schedules.py` зелено) на конце задачи 1 выполнено, поэтому порядок плана соблюдён, а внутреннее противоречие критериев записано здесь.

**8. `git stash` для доказательства покраснения заменён на `git show`**

Критерий задачи 2 предписывал `git stash && git checkout 9f3c59e -- …`. В worktree это запрещено: стек сташей ОБЩИЙ на все worktree, и `pop` способен принести WIP соседа. ⚠️ **Запись честности: сташ был выполнен ОДИН раз по невнимательности и немедленно возвращён** — верхняя запись стека была проверяемо своей (метка ветки совпадала с веткой этого worktree), дерево восстановлено полностью, суита перепрогнана (105 passed), стек пуст. Дальше доказательство сделано без него: исходники пред-фазового коммита взяты `git show`, утверждения применены напрямую.

**9. `graphify update .` НЕ выполнен**

Критерий задачи 3 требует прогона после правки кода (правило `CLAUDE.md`). Граф `graphify-out/` существует ТОЛЬКО в основном рабочем каталоге и в worktree отсутствует (в git не отслеживается). Прогон здесь построил бы граф с нуля в каталоге, который оркестратор удаляет после возврата; запись же в `/source/broadcaster/graphify-out` — обращение ЗА ПРЕДЕЛЫ worktree и запрещена защитой путей. **Передаётся оркестратору: выполнить `graphify update .` в основном каталоге после слияния волны.** (В `.planning/config.json` `graphify.enabled: false`.)

---

**Total deviations:** 5 auto-fixed (1 missing critical, 3 blocking, 2 bug — с перекрытием категорий) + 4 записанных отклонения от текста плана.
**Impact on plan:** Все автоправки необходимы для корректности заявленных планом свойств; расширения предмета нет. Отклонение 6 — единственное, меняющее заявленный результат фазы: критерий успеха 7 в части `CONDITIONAL_PLACES` НЕ выполнен и выполнен быть не мог; остальные семь критериев успеха выполнены.

## Issues Encountered

- **Фикстура `htmx_client` непригодна для пары «оба транспорта в одном тесте».** Она ставит признак на ОБЩИЙ объект клиента — свойство несущее, оно и позволяет складывать её с фикстурами аутентификации, — но запрошенная рядом, она помечает и тот запрос, который обязан прийти БЕЗ признака. Поймано прогоном: половина деградации вернула 200 вместо 302. Признак ставится в самом тесте, ровно на один запрос из двух; причина выписана в докстринг правила, чтобы следующий читатель не «починил» это обратно.
- Известный красный `full-suite-ads-editor-order-pollution` планом не втягивался и не чинился; `tests/test_pages/` целиком — **1417 passed**.

## Known Stubs

Нет. Заглушек, недописанных ветвей и пропущенных тестов план не оставляет.

## Threat Flags

Новой поверхности сверх заявленной планом одна, и она разобрана выше как автоправка 1:

| Flag | File | Description |
|------|------|-------------|
| threat_flag: input-surface | `app/templates/ads/includes/sched_card.html`, `app/pages/schedules.py` | Второе поле формы (идентификатор объявления) стало входом контекста экрана. Митигация на месте: коэрция к целому (адрес структурно неизменяем, T-02-23), владение НЕ презюмируется — предикат ветвления и счёт ограничены связью `Ad.user_id`, приоритет у найденной строки, редактор перепроверяет владение на приземлении. Подстановка чужого номера даёт ветку перехода и ноль строк. |

## User Setup Required

None — внешней конфигурации план не требует. Ни build-шага, ни новой зависимости, ни нового вендоренного файла не заведено.

## Ручной UAT — что машинно НЕ ЗАКРЫТО

**Пункт 4 перечня роадмапа** (`hx-swap-oob="delete"` действительно снимает осиротевшую панель) закрыт машинно ЛИШЬ НАПОЛОВИНУ: тесты доказывают, что второй узел снятия УЕХАЛ В ОТВЕТЕ отдельным узлом верхнего уровня со своим `id`. Что рантайм по нему снял элемент со страницы — **не проверено ничем**: httpx внеполосных узлов не применяет и не свопает. Наблюдение проводится в браузере: открыть редактор объявления с тремя расписаниями, удалить два подряд без перезагрузки, убедиться в инструментах разработчика, что узлов `role="dialog"` от удалённых карточек в документе не осталось. Передаётся артефакту обхода фазы (`coverage` D9, D10).

## Next Phase Readiness

- **Готово К СЛЕДУЮЩЕЙ ВОЛНЕ, но НЕ К ВЫКАТКЕ** — см. §«Промежуточное состояние волн».
- Плану **10-03** передан счётчик прогресса вехи в значении **33** (его предусловие требует именно этого), перечень отставания с комментарием фазовой сцепки и семь непереведённых обработчиков.
- Планам **10-02** и **10-04** передан макрос с безусловными атрибутами: сличение свойств качества панели с макросом-обёрткой (D-16) и ветвь закрытия панели теперь имеют предмет на ВСЕХ 18 местах, а не на одном.
- Приведение записей «16 → 18» в `ROADMAP.md` и `REQUIREMENTS.md` (D-17) этим планом НЕ сделано — оно за планом 10-04; числа гейта (18 / 9 / 11 / 10) объявлены и утверждены здесь.
- `FORM-06` НЕ отмечено выполненным: требование объявляют все четыре плана фазы, и общий идентификатор не имеет права читаться «Complete», пока не финишировал последний из них.

## Self-Check: PASSED

- `app/templates/ads/includes/sched_count_rule.html` — FOUND
- `app/templates/ads/partials/sched_delete_response.html` — FOUND
- Коммиты `c5b1933`, `14face5`, `96ef0b2`, `5766b03` — FOUND
- `uv run pytest tests/test_pages/ -q` — 1417 passed
- `uv run pytest tests/test_templates/ tests/test_pages/test_htmx_gates.py -q` — 186 passed
- `uv run python -m compileall -q app main.py tests` — без ошибок

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-02*
