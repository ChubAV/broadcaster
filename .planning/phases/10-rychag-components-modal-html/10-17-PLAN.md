---
phase: 10-rychag-components-modal-html
plan: 17
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - tests/test_planning/test_requirement_completion_follows_verification.py
  - tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ, И ЭТО РЕШЕНИЕ,
# А НЕ ПРОПУСК — форма унаследована от планов 10-12…10-16 дословно. Обе величины живут в
# конфиге и разрешаются оркестратором; ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `config-get
# workflow.security_asvs_level` → `1`; `workflow.security_block_on` → `"high"`;
# `workflow.security_enforcement` → `true`. Таблица STRIDE ниже собрана ИМЕННО НА ЭТОМ
# УРОВНЕ И С ЭТИМ ПОРОГОМ, и обе величины названы в самой таблице.

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер. Состояние калибровки СНЯТО ПЛАНИРОВЩИКОМ:
  # `estimate-calibration` → sample_count 0, factor 1, applied false, min_samples 3.
  # Отсюда tokens == raw_tokens и confidence: low, ВЫВЕДЕННЫЙ ИЗ ЧИСЛА ЗАМЕРОВ.
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ОТРИЦАТЕЛЬНЫЕ КОНТРОЛИ ЗАДАЮТ СВОЙ ПРЕДМЕТ САМИ: оба контроля модуля принуждения питаются СИНТЕТИЧЕСКОЙ записью и СИНТЕТИЧЕСКИМ корнем в `tmp_path`, и ни один из них не читает `PLANNING_ROOT` ни одной строкой"
    - "ПРОГОН НЕ ЗАВИСИТ ОТ ВЕРДИКТА СВОЕЙ ФАЗЫ НИ НА ОДНОМ ИЗ ЧЕТЫРЁХ ЕГО ЗНАЧЕНИЙ: `passed`, `gaps_found`, `human_needed` и отсутствие отчёта дают ОДИН И ТОТ ЖЕ зелёный прогон модуля — предмет замера 3 отчёта верификации снят полностью, а не наполовину"
    - "ПРЕДМЕТ ЗАКРЫТ ШИРЕ, ЧЕМ НАЗВАЛА РЕВИЗИЯ, И ЭТО НАЗВАНО РАСШИРЕНИЕМ: `CR-01` назвал предметом вердикт `passed`, замер верификатора показал вшитый ЛИТЕРАЛ `gaps_found`; правится литерал, а не один вердикт из двух"
    - "ЗУБЫ ОБОИХ КОНТРОЛЕЙ ПОКАЗАНЫ МУТАЦИЕЙ, А НЕ ЗАЯВЛЕНЫ: синтетическому разборщику подаётся ИЗМЕНЁННЫЙ вход, и находка меняется ровно так, как обязана, — иначе синтетика заменила бы вшитое состояние на непроверенное"
    - "НЕСУЩИЕ ПРАВИЛА МОДУЛЯ НЕ ТРОНУТЫ: `test_no_requirement_is_marked_complete_before_its_phase_verification_passed`, `test_the_number_of_requirement_rows_is_declared`, `test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself`, `test_the_verification_index_is_built_by_walking_the_whole_record_tree`, `test_the_flag_and_the_status_cell_of_every_requirement_agree` продолжают стоять на ЖИВОМ дереве — они и должны на нём стоять"
    - "СЛЕДУЮЩИЙ ГЕЙТ РЕГЛАМЕНТА НЕ ПОВТОРИТ РАСХОЖДЕНИЯ НИ ОДНОЙ ИЗ ДВУХ ВЕТВЕЙ КЛАССА: отдельное правило утверждает, что ни одна тестовая функция каталога `tests/test_planning/`, касающаяся живого корня записи, не судит вердикт отчёта СВОЕЙ фазы в утверждении — ни сличением с литералом (признак A), ни утверждением о ЕГО ОТСУТСТВИИ (признак B); правило непусто по построению и показывает зубы на синтетическом исходнике четырьмя раздельными половинами"
    - "ВЕТВЬ «УТВЕРЖДЕНИЕ ОБ ОТСУТСТВИИ ОТЧЁТА» ЗАКРЫТА НАРАВНЕ С ЛИТЕРАЛЬНОЙ, И ЭТО ЗАМЕРЕНО, А НЕ ОБЪЯВЛЕНО: второй отрицательный контроль вердиктного литерала не несёт ВОВСЕ (`assert absent.verdict is None`), и правило, определённое конъюнкцией «живой корень И литерал», не увидело бы его никогда — определение есть ДИЗЪЮНКЦИЯ двух признаков, и каждый стережётся своей половиной контроля зубов"
    - "ЛЕТОПИСЬ ЧИСЛА СТРОК ТАБЛИЦЫ (`REQUIREMENT_ROWS_DECLARED = 44`) НЕ СДВИГАЕТСЯ ЭТИМ ПЛАНОМ: строк таблицы состояний план не прибавляет и не снимает, а сдвинутое без причины число обесценило бы правило, которое его стережёт"
  artifacts:
    - "tests/test_planning/test_requirement_completion_follows_verification.py — вспомогательная сборка синтетического корня записи `_fake_planning_root`"
    - "tests/test_planning/test_requirement_completion_follows_verification.py — вспомогательная сборка синтетической записи требований `_synthetic_requirements`"
    - "tests/test_planning/test_requirement_completion_follows_verification.py — оба переписанных отрицательных контроля и два правила зубов к ним"
    - "tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py — НОВЫЙ модуль: правило независимости гейтов записи от вердикта живого дерева с зубами на синтетическом исходнике-нарушителе"
  key_links:
    - "второй вход `premature_completions(requirements_text, planning_root)` → синтетический корень: единственное, чем предмет контроля задаётся самим контролем; ломается — прогон снова зелен ровно при условии, что фаза не достигла цели"
    - "зубы контроля → мутация входа: единственное, чем синтетика отличается от контроля, зеленеющего на чём угодно; ломается — вшитое состояние заменено непроверенным, и это хуже, потому что незаметно"
    - "правило независимости → каталог `tests/test_planning/`: единственное, чем следующий гейт регламента не повторит того же расхождения; ломается — предмет закрывается по одному модулю за круг"
  prohibitions:
    - statement: "несущие правила модуля НЕ переводятся на синтетический корень: отчёт верификации прямо говорит, что они корректны, зелены и предмет свой стерегут, а правило регламента, сверяющее синтетику с синтетикой, не сверяет НИЧЕГО и зеленеет вакуумом"
      status: flagged-unverified
      verification: none
    - statement: "утверждения контролей НЕ подгоняются под сегодняшнее дерево вторым заходом: подгонка через круг превращает модуль в тест, который принято править, — ровно тот класс отказа, который сам файл осуждает в своей шапке"
      status: flagged-unverified
      verification: none
    - statement: "поле `status` отчёта `10-VERIFICATION.md` НЕ правится этим планом ни на символ: вердикт ставит верификатор, а план, подвинувший вердикт ради зелени, закрыл бы дефект правкой записи о нём"
      status: flagged-unverified
      verification: none
    - statement: "клетка требования `FORM-06` в `.planning/REQUIREMENTS.md` НЕ переводится в `Complete`: условие перевода — вердикт `passed` повторной верификации, и настоящий план его не выносит"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ — ЯВНЫЕ ДОПУЩЕНИЯ ПАРТИИ, А НЕ МОЛЧАЛИВЫЕ ПОТЕРИ.
  # Детерминированный отчёт рёбер предъявил над FORM-06 ДВА ребра, оба `unresolved`:
  #   • `idempotency` («что будет, если это исполнится дважды над тем же входом»)
  #     — ЧАСТИЧНО РАЗРЕШЁН РАНЕЕ, И ДУБЛЬ НЕ ЗАВОДИТСЯ: план 10-06 закрыл продуктовую
  #     половину ребра над FORM-06 — неотличимость повтора подтверждённого удаления на
  #     фрагментном маршруте и на маршрутах перехода, настоящими запросами и сличением
  #     множеств внеполосных узлов. ЧТО ОСТАЛОСЬ: повтор в РАНТАЙМЕ браузера наблюдаем
  #     только обходом, и его наблюдатель — шаги 1.4-1.9 `10-UAT.md`, не пройденные ни разу.
  #   • `concurrency` («что гарантировано при обрыве или параллельном запуске»)
  #     — АДРЕСОВАН ЧАСТИЧНО: гейтом ухода панели по признаку отправки (план 10-07,
  #     доказан в интерпретаторе и мутацией) и проверками 2 и 3 обхода. ПОЛНОГО машинного
  #     критерия — два подтверждённых удаления в полёте ОДНОВРЕМЕННО — в суите без
  #     процесса выразить нечем. Остаётся `unresolved` и НАЗВАН ЗДЕСЬ.
  # РАВЕНСТВО БЕЗ МОЛЧАЛИВЫХ ПОТЕРЬ: 2 предъявленных = 0 разрешённых критерием
  # + 2 явных допущения, каждое с названным основанием и названным наблюдателем.
---

<objective>
Закрывается ЕДИНСТВЕННЫЙ БЛОКЕР четвёртого круга верификации (`gaps[0]`, `status: failed`, он же
`regressions[0]`) — целиком, всеми четырьмя пунктами его перечня `missing`.

Purpose: модуль `tests/test_planning/test_requirement_completion_follows_verification.py`, заведённый
планом 10-14 РАДИ ПРИНУЖДЕНИЯ РЕГЛАМЕНТА отметок требований, вшил в свои ДВА отрицательных контроля
ПЕРЕХОДНОЕ состояние живого дерева `.planning/`. Прогон `just test` — команду, которую в проекте
запускают все, — держит зелёной совпадение вердикта этой фазы с литералом `gaps_found`, то есть тем,
что фаза НЕ достигла цели. Дойти до `passed` можно только сломав собственный гейт проекта; чинить его
пришлось бы правкой утверждений, что через круг превращает модуль в тест, «который принято
подгонять», — ровно тот класс отказа, который сам файл осуждает в своей шапке.

⚠️ **ПРЕДМЕТ ШИРЕ, ЧЕМ НАЗВАЛА РЕВИЗИЯ, И ЭТО РАСШИРЕНИЕ, А НЕ ПОВТОР.** `10-REVIEW.md` (`CR-01`)
называет предметом вердикт `passed`. Замер верификатора прогоном В ОБЕ СТОРОНЫ показал, что правило
вшито в ЛИТЕРАЛ `gaps_found` и краснеет на ЛЮБОМ ином вердикте: `passed` → **2 failed, 7 passed**;
`human_needed` → **1 failed, 8 passed** (отказ дословно `+ human_needed` против ожидаемого
`gaps_found`); возврат в `gaps_found` → **9 passed**. Ревизия не ошиблась — она измерила ОДИН вердикт
из двух и назвала его верно. Настоящий план закрывает ШИРОКИЙ предмет: вшит ЛИТЕРАЛ, и правится
литерал.

Output: оба отрицательных контроля питаются синтетической записью и синтетическим корнем в `tmp_path`;
у обоих показаны зубы мутацией входа; новым модулем утверждено, что ни один гейт каталога
`tests/test_planning/` не сличает вердикт живого дерева с литералом в утверждении.

⚠️ **ЧТО В ЭТОМ ФАЙЛЕ НЕ ТРОГАЕТСЯ — НАЗВАНО ПОИМЁННО, А НЕ ОСТАВЛЕНО УМОЛЧАНИЮ.** Отчёт верификации
прямо говорит, что несущие правила модуля корректны, зелены и предмет свой стерегут. Правится ТОЛЬКО
ИСТОЧНИК ВХОДА двух отрицательных контролей. Пять правил, продолжающих стоять на ЖИВОМ дереве:
`test_no_requirement_is_marked_complete_before_its_phase_verification_passed`,
`test_the_number_of_requirement_rows_is_declared`,
`test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself`,
`test_the_verification_index_is_built_by_walking_the_whole_record_tree`,
`test_the_flag_and_the_status_cell_of_every_requirement_agree`. Правило регламента, сверяющее синтетику
с синтетикой, не сверяет НИЧЕГО: оно зеленело бы вакуумом, и это второй способ отключить свойство —
после хрупкости.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-14-SUMMARY.md
@tests/test_planning/test_requirement_completion_follows_verification.py
@tests/test_planning/test_state_progress_matches_roadmap.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: сквозной срез — синтетический корень записи и ПЕРВЫЙ отрицательный контроль, задающий свой предмет сам</name>

  <files>
tests/test_planning/test_requirement_completion_follows_verification.py
  </files>

  <read_first>
- `tests/test_planning/test_requirement_completion_follows_verification.py` ЦЕЛИКОМ (558 строк).
  ⚠️ ЧИТАТЬ ВЕСЬ, а не отрывок: шапка модуля объявляет, чего файл НЕ доказывает, и правка,
  разошедшаяся с этим объявлением, воспроизвела бы дефект фазы в её же гейте. Особенно:
  `:44-72` — `PROJECT_ROOT`, `PLANNING_ROOT`, `REQUIREMENTS_PATH`, `COMPLETED_STATUS`,
  `PASSED_VERDICT`, `REQUIREMENT_ROWS_DECLARED = 44` с летописью числа;
  `:74-79` — выражения `_ROW_RE`, `_PHASE_RE`, `_VERIFICATION_FILE_RE`, `_STATUS_FIELD_RE` и
  константа `_FRONTMATTER_FENCE`;
  `:82-87` — `_normalised_phase`;
  `:107-125` — `_requirement_rows(text)`: ИСХОДНИК УЖЕ ПРИХОДИТ ПАРАМЕТРОМ, и докстринг объясняет,
  почему («без параметра отрицательный контроль был бы невыразим»);
  `:145-156` — `_report_verdict(text)`: читает поле `status` ПЕРВОГО блока frontmatter;
  `:159-184` — `_verification_status_by_phase(root)`: индекс, собранный `root.rglob("*-VERIFICATION.md")`;
  `:209-235` — `premature_completions(requirements_text, planning_root)`: ОБА входа параметрами;
  `:288-299` — `_with_status(text, requirement, status)`: приём подмены КОПИИ записи.
- `:304-328` — `test_control_negative_a_premature_completion_reddens_the_rule`, ПРЕДМЕТ ЗАДАЧИ.
  ⚠️ ЧИТАТЬ ДОСЛОВНО: он читает `REQUIREMENTS_PATH.read_text()`, подменяет клетку `FORM-06` и
  вызывает `premature_completions(doctored, PLANNING_ROOT)` — второй вход ЖИВОЙ. Утверждения
  `only.verdict == "gaps_found"` и `"Phase 10" in message` и есть вшитое состояние.
- `tests/test_planning/test_state_progress_matches_roadmap.py:189-298` — ПРЕЦЕДЕНТ ФОРМЫ, которую
  надо воспроизвести: `_synthetic_roadmap(executed=…, planned=…)` и `_synthetic_state(completed=…,
  total=…)` собирают ОБА входа целиком, числа стоят ТОЛЬКО внутри синтетических текстов, и контроли
  `test_the_helper_catches_the_round_seven_regression`,
  `test_a_newly_added_unchecked_plan_raises_the_total`,
  `test_an_agreeing_pair_shows_no_divergence` сверяют синтетику с синтетикой. ⚠️ ЭТО ТОТ ЖЕ АВТОР И
  ТА ЖЕ ИДИОМА — новая форма не изобретается.
- `tests/test_pages/test_confirm_delete_transport.py:2992-3019` —
  `test_control_a_shortened_axis_list_reddens_the_axis_count`: ОБРАЗЕЦ ФОРМЫ ЗУБОВ. Разборщику
  подаётся ИЗМЕНЁННЫЙ вход, и утверждается, что находка изменилась ровно на ожидаемую величину.
- `10-VERIFICATION.md`, блок `gaps[0]` целиком: три замера, четыре пункта `missing`, обе записи
  `artifacts` с номерами строк. ⚠️ ЧИТАТЬ ВМЕСТЕ С полем `re_verification.corrections`: верификатор
  РАСШИРИЛ предмет `CR-01`, а не повторил его.
- `10-REVIEW.md:78-145` — `CR-01` целиком, включая предложенный образец `_fake_planning_root`.
  ⚠️ ОБРАЗЕЦ РЕВИЗИИ — ПОДСКАЗКА ФОРМЫ, А НЕ ТЕКСТ ДЛЯ ВСТАВКИ: он собирает frontmatter не так, как
  его читает `_report_verdict`, поэтому проверять надо ПРОГОНОМ, а не сличением с образцом.
- `10-14-SUMMARY.md` — что именно план 10-14 объявил предметом модуля и какие числа он замерил.
  </read_first>

  <precondition>
Прогон `uv run pytest tests/test_planning/ -q -p no:randomly` на непрáвленом дереве даёт
**14 passed** (9 правил модуля принуждения плюс 5 правил модуля счёта планов). Если число иное —
дерево изменилось после планирования, и замеры отчёта верификации надо переснять прежде, чем
править: план исходит из ЭТОГО числа и называет его прямо.
  </precondition>

  <behavior>
1. Синтетический корень записи собирается функцией, принимающей отображение «номер фазы → вердикт»,
   где `None` означает «отчёта нет вовсе», и раскладывающей отчёты по подкаталогам временного
   каталога так, чтобы их находил РЕКУРСИВНЫЙ обход `_verification_status_by_phase`.
2. Отчёт синтетического корня открывается блоком frontmatter в той же форме, в какой его читает
   `_report_verdict`: первая строка — ограда, поле `status` — внутри первого блока.
3. Синтетическая запись требований собирается функцией, принимающей тройки «имя требования, текст
   клетки фазы, состояние», и даёт текст, который разбирает `_ROW_RE` без единой правки выражения.
4. Первый отрицательный контроль питается ОБОИМИ синтетическими входами: имя требования и номер
   фазы взяты из диапазона, которого в проекте нет и не будет, а вердикт задан САМИМ КОНТРОЛЕМ.
5. Контроль по-прежнему утверждает ТРИ вещи, ради которых он написан: находка ровно одна, у неё
   назван вердикт, и текст отказа называет требование, фазу и вердикт.
6. Зубы: тот же вход с вердиктом `passed` даёт ПУСТОЙ список находок. Без этой половины контроль
   зеленел бы и на разборщике, возвращающем находку всегда.
  </behavior>

  <action>
Завести в модуле две вспомогательные сборки и переписать ИСТОЧНИК ВХОДА первого отрицательного
контроля. Ни одного несущего правила не трогать.

**(1) `_fake_planning_root(tmp_path, verdicts)`** — сборка синтетического корня записи. Вход:
`tmp_path` (фикстура pytest) и отображение `dict[str, str | None]` «номер фазы → вердикт», где `None`
означает «отчёта у фазы нет вовсе». На каждую фазу с непустым вердиктом создать подкаталог
`phases/{phase}-synthetic` и записать в него файл имени `{phase}-VERIFICATION.md`, чьё тело
открывается оградой `_FRONTMATTER_FENCE`, несёт строку поля `status` со значением вердикта и
закрывается той же оградой. Каталоги создавать с `parents=True`, кодировку `utf-8` указывать явно —
тем же приёмом, что и остальные чтения модуля. Вернуть `tmp_path`. В докстринге назвать, ЗАЧЕМ
корень синтетический: предмет контроля обязан задаваться самим контролем, а не состоянием проекта на
день прогона.

**(2) `_synthetic_requirements(rows)`** — сборка синтетической записи требований. Вход:
последовательность троек «имя требования, текст клетки фазы, состояние». Выход: текст, в котором
каждая тройка стои́т строкой таблицы формы `| ИМЯ | КЛЕТКА | СОСТОЯНИЕ |`, разбираемой `_ROW_RE` без
правки самого выражения. ⚠️ ФОРМУ СТРОКИ СНЯТЬ С `_ROW_RE` И С ЖИВОЙ ЗАПИСИ, А НЕ ИЗ ПАМЯТИ:
разборщик требует ровно трёх столбцов и конца строки сразу после третьего разделителя. Сборку
проверить утверждением ВНУТРИ контроля: `_requirement_rows` на её выходе возвращает ровно столько
строк, сколько подано, — иначе синтетика молча дала бы пустой вход, и контроль зеленел бы вакуумом.

**(3) Переписать `test_control_negative_a_premature_completion_reddens_the_rule`.** Функция получает
параметр `tmp_path`. Вместо `REQUIREMENTS_PATH.read_text(...)` — вызов `_synthetic_requirements` с
ОДНОЙ тройкой: имя `SYN-01`, клетка `Phase 91`, состояние `COMPLETED_STATUS`. Вместо `PLANNING_ROOT`
— `_fake_planning_root(tmp_path, {"91": "gaps_found"})`. ⚠️ НОМЕР ФАЗЫ `91` И ИМЯ `SYN-01` ВЫБРАНЫ
С ОСНОВАНИЕМ, А НЕ ПРОИЗВОЛЬНО: фаз с номером 91 у проекта нет и роадмап их не планирует, поэтому
столкновение с реальной фазой невыразимо; префикс `SYN` не совпадает ни с одним префиксом реестра
требований (`FORM`, `FOUND`, `GATE`, `QUAL`, `PAY`, `FETCH`, `EDIT`, `UPLD`, `E2E`). Оба выбора
назвать в докстринге. Утверждения контроля сохранить ПО СМЫСЛУ, сменив предмет на синтетический:
находка ровно одна; её имя требования — `SYN-01`; её вердикт — `gaps_found`; текст отказа содержит
имя требования, текст клетки фазы и вердикт. Положительный контроль на непрáвленой ЖИВОЙ записи из
этой функции УБРАТЬ: его предмет дословно совпадает с несущим правилом
`test_no_requirement_is_marked_complete_before_its_phase_verification_passed`, которое живого дерева
не покидает. Вместо него — синтетический положительный контроль пункта (4).

**(4) Завести `test_control_a_passed_verdict_in_the_synthetic_root_empties_the_finding`** — зубы
первого контроля мутацией входа, по образцу `test_control_a_shortened_axis_list_reddens_the_axis_count`.
Та же синтетическая запись, тот же синтетический корень, но вердикт фазы `91` подменён на
`PASSED_VERDICT`. Утверждать: список находок ПУСТ. В докстринге назвать, что доказывает мутация:
находка рождена ВЕРДИКТОМ поданного корня, а не присутствием строки в записи.

**(5) Что НЕ трогается.** Ни одна строка правил
`test_no_requirement_is_marked_complete_before_its_phase_verification_passed`,
`test_the_number_of_requirement_rows_is_declared`,
`test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself`,
`test_the_verification_index_is_built_by_walking_the_whole_record_tree`,
`test_the_flag_and_the_status_cell_of_every_requirement_agree`,
`test_control_negative_a_disagreeing_flag_reddens_the_agreement_rule`,
`test_control_negative_the_three_kinds_of_disagreement_are_named_apart`. Константа
`REQUIREMENT_ROWS_DECLARED` и её летопись не сдвигаются: строк таблицы план не прибавляет. Помощники
`_with_status`, `_with_flag`, `_without_row` в этой задаче остаются.

**(6) Порядок работы — красный до зелёного.** Сначала завести обе сборки и НОВЫЙ контроль зубов
(пункт 4) на ещё не переписанном контроле, прогнать и убедиться, что модуль зелен; затем переписать
источник входа первого контроля и прогнать снова. Оба промежуточных прогона привести в сводку
числами.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `10` — переписанный контроль и новый контроль зубов дают минимум на одно правило больше прежних девяти</fails_when>
    <automated>grep -c 'PLANNING_ROOT' tests/test_planning/test_requirement_completion_follows_verification.py</automated>
    <fails_when>вывод больше `6` — упоминаний живого корня стало больше, чем у пяти несущих правил плюс объявление самой константы, то есть отрицательный контроль снова читает живое дерево</fails_when>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly -k "control_a_passed_verdict_in_the_synthetic_root"</automated>
    <fails_when>вывод содержит `no tests ran` либо `failed` — правило зубов не заведено под названным именем либо не зелено</fails_when>
  </verify>

  <acceptance_criteria>
- `_fake_planning_root` заведён и его выход НАХОДИТСЯ рекурсивным обходом: в сводке приведён вывод
  `_verification_status_by_phase` на синтетическом корне `{"91": "gaps_found"}` — отображение с
  единственным ключом `91` и вердиктом `gaps_found`.
- `_synthetic_requirements` заведён, и в сводке приведено число строк, которое `_requirement_rows`
  вернул на его выходе, рядом с числом поданных троек; числа совпадают.
- Тело `test_control_negative_a_premature_completion_reddens_the_rule` не называет ни
  `PLANNING_ROOT`, ни `REQUIREMENTS_PATH`: в сводке приведён вывод
  `sed -n '/def test_control_negative_a_premature_completion_reddens_the_rule/,/^def /p' tests/test_planning/test_requirement_completion_follows_verification.py | grep -c -e PLANNING_ROOT -e REQUIREMENTS_PATH`
  → `0`.
- Правило зубов заведено под именем `test_control_a_passed_verdict_in_the_synthetic_root_empties_the_finding`
  и КРАСНЕЕТ от возврата вердикта `gaps_found`: в сводке приведён вывод одного пробного прогона с
  подменённым обратно вердиктом (ожидается отказ) и вывод штатного прогона (зелено).
- Пять несущих правил не тронуты: в сводке приведён перечень имён функций, попавших в диапазоны
  `git diff -U0 -- tests/test_planning/test_requirement_completion_follows_verification.py`, и ни
  одного из пяти имён в перечне нет.
- `grep -c '^REQUIREMENT_ROWS_DECLARED = 44$' tests/test_planning/test_requirement_completion_follows_verification.py`
  → `1`.
- Основание выбора номера фазы `91` и имени `SYN-01` записано в докстринге контроля и процитировано
  в сводке.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится ОДИН тестовый модуль; поведения продукта план не меняет ни на строку. Прежние утверждения
восстановимы из истории git, несущие правила не тронуты вовсе, поэтому откат не оставляет за собой
дыры в принуждении регламента.
  </reversibility>

  <done>
Сборки синтетической записи и синтетического корня заведены; первый отрицательный контроль задаёт
свой предмет сам и не читает живое дерево ни одной строкой; его зубы показаны мутацией входа; пять
несущих правил стоят на живом дереве нетронутыми; прогон модуля зелен.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: ВТОРОЙ отрицательный контроль — два случая, и ОТСУТСТВИЕ отчёта задано корнем, а не состоянием роадмапа</name>

  <files>
tests/test_planning/test_requirement_completion_follows_verification.py
  </files>

  <read_first>
- `tests/test_planning/test_requirement_completion_follows_verification.py:330-350` —
  `test_control_negative_two_premature_completions_are_both_named`, ПРЕДМЕТ ЗАДАЧИ. ⚠️ ЧИТАТЬ
  ДОСЛОВНО: он подменяет ДВЕ клетки живой записи (`FORM-06` и `FORM-03`) и утверждает
  `named == {"FORM-06", "FORM-03"}` плюс `absent.verdict is None`. ВТОРОЕ УТВЕРЖДЕНИЕ ОПАСНЕЕ
  ПЕРВОГО: оно вшивает ОТСУТСТВИЕ отчёта Фазы 11 и падёт от появления ЛЮБОГО отчёта Фазы 11 —
  события, стоящего в роадмапе ближайшей работой после настоящей фазы.
- `:209-235` — `premature_completions`: ветвь, где `report is None`, и ветвь, где `row.phase is None`.
  Разница несущая: первая означает «отчёта у фазы нет», вторая — «фаза у строки не названа», и
  второй случай судится ДРУГИМ правилом.
- `:186-206` — `PrematureCompletion.__str__`: литерал `НЕТ ВОВСЕ` печатается ровно в ветви
  `verdict is None`. Утверждение контроля о нём сохраняется по смыслу.
- Результат задачи 1 в дереве: `_fake_planning_root` и `_synthetic_requirements` вместе с их
  докстрингами. ⚠️ ВТОРАЯ ФОРМА ИХ НЕ ЗАВОДИТСЯ: второй экземпляр одной сборки разошёлся бы с первым
  при первой же правке — то самое основание, которым сам модуль объясняет параметризацию входа.
- `10-VERIFICATION.md`, второй пункт `artifacts` и второй пункт `missing` гейпа 1 — дословная
  формулировка требуемого: фиктивный корень в `tmp_path` с ДВУМЯ отчётами известных вердиктов
  (`gaps_found` и отсутствие отчёта).
  </read_first>

  <precondition>
Задача 1 приземлена: `_fake_planning_root` и `_synthetic_requirements` в дереве, прогон модуля
зелен. Без них второй контроль пришлось бы переводить на СВОЮ сборку, и два экземпляра одной сборки
разошлись бы при первой правке.
  </precondition>

  <behavior>
1. Второй отрицательный контроль питается синтетической записью из ДВУХ строк и синтетическим
   корнем, в котором у первой фазы отчёт с известным вердиктом ЕСТЬ, а у второй — нет вовсе.
2. Отказ по-прежнему называет ОБЕ строки, а не первую: множество имён находок равно множеству
   поданных имён.
3. Находка фазы без отчёта несёт вердикт `None` и печатается формулировкой «отчёта верификации этой
   фазы НЕТ ВОВСЕ».
4. Находка фазы с отчётом несёт ИМЕННО тот вердикт, который задан корнем, — иначе контроль не
   отличал бы двух своих случаев друг от друга, а написан он ровно ради их различения.
5. Зубы: тот же вход, но в корне ЕСТЬ отчёт второй фазы с вердиктом `passed`, — находка остаётся
   ровно одна, и это первая строка.
  </behavior>

  <action>
Переписать ИСТОЧНИК ВХОДА второго отрицательного контроля и показать его зубы. Несущих правил не
трогать; сборки задачи 1 переиспользовать, а не дублировать.

**(1) Переписать `test_control_negative_two_premature_completions_are_both_named`.** Функция получает
параметр `tmp_path`. Запись собирается `_synthetic_requirements` из ДВУХ троек: `SYN-01` / `Phase 91`
/ `COMPLETED_STATUS` и `SYN-02` / `Phase 92` / `COMPLETED_STATUS`. Корень собирается
`_fake_planning_root(tmp_path, {"91": "gaps_found", "92": None})` — фаза `92` отчёта не получает
вовсе, и это ЗАДАНО КОРНЕМ, а не состоянием роадмапа. Утверждения сохранить по смыслу: множество
имён находок равно `{"SYN-01", "SYN-02"}`; находка `SYN-02` несёт вердикт `None`; её текст содержит
литерал `НЕТ ВОВСЕ`; находка `SYN-01` несёт вердикт `gaps_found`. В докстринге назвать, ЧТО именно
сменилось по сравнению с прежней формой: прежний контроль утверждал отсутствие отчёта У ФАЗЫ 11
проекта, то есть держался на том, что следующая фаза ещё не верифицирована.

**(2) Завести `test_control_a_synthetic_report_for_the_second_phase_leaves_one_finding`** — зубы
второго контроля мутацией входа. Та же запись из двух строк, но корень собран
`{"91": "gaps_found", "92": PASSED_VERDICT}`. Утверждать: находка ровно одна и её имя — `SYN-01`.
В докстринге назвать, что доказывает мутация: вторая находка рождена ОТСУТСТВИЕМ отчёта В ПОДАННОМ
КОРНЕ, а не состоянием дерева проекта; именно этого различения не было у прежней формы контроля.

**(3) Снять `_with_status`, если после переписи у него не осталось потребителей.** Проверить поиском
по модулю и привести в сводке ЧИСЛО оставшихся вызовов. Ноль вызовов — снять функцию вместе с
докстрингом и назвать это в сводке отдельной строкой: помощник заведён ради подмены клетки ЖИВОЙ
записи, и после переезда обоих контролей на синтетику он есть мёртвый код, который следующий
читатель примет за действующий приём. Ненулевое число — НЕ СНИМАТЬ и назвать потребителя поимённо.
⚠️ `_with_flag` и `_without_row` НЕ ТРОГАТЬ ни в каком случае: их потребители — два контроля правила
согласия флажка с клеткой, которые живого дерева не покидают и предметом настоящей партии не
являются.

**(4) Что НЕ трогается.** Всё, названное пунктом (5) задачи 1, плюс результат самой задачи 1.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `11` — после двух задач правил в модуле минимум одиннадцать</fails_when>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly -k "control_a_synthetic_report_for_the_second_phase"</automated>
    <fails_when>вывод содержит `no tests ran` либо `failed` — правило зубов второго контроля не заведено под названным именем либо не зелено</fails_when>
  </verify>

  <acceptance_criteria>
- Тело `test_control_negative_two_premature_completions_are_both_named` не называет ни
  `PLANNING_ROOT`, ни `REQUIREMENTS_PATH`, ни имён `FORM-06` и `FORM-03`: в сводке приведён вывод
  `sed -n '/def test_control_negative_two_premature_completions_are_both_named/,/^def /p' tests/test_planning/test_requirement_completion_follows_verification.py | grep -c -e PLANNING_ROOT -e REQUIREMENTS_PATH -e FORM-06 -e FORM-03`
  → `0`.
- Оба случая контроля различены УТВЕРЖДЕНИЯМИ, а не порядком: в сводке приведены обе строки
  утверждений — о вердикте `gaps_found` у `SYN-01` и о вердикте `None` у `SYN-02`.
- Правило зубов заведено под именем
  `test_control_a_synthetic_report_for_the_second_phase_leaves_one_finding` и краснеет от возврата
  корня к `{"91": "gaps_found", "92": None}`: в сводке приведён вывод пробного прогона (ожидается
  отказ) и вывод штатного прогона (зелено).
- Судьба `_with_status` названа числом: в сводке приведено число оставшихся вызовов и следствие —
  снят либо оставлен с названным потребителем.
- `_with_flag` и `_without_row` не тронуты: в сводке приведён вывод
  `git diff -U0 -- tests/test_planning/test_requirement_completion_follows_verification.py | grep -c -e '_with_flag' -e '_without_row'` → `0`.
- Прогон обоих модулей каталога зелен: в сводке приведён вывод
  `uv run pytest tests/test_planning/ -q -p no:randomly` с числом.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится один тестовый модуль; поведения продукта задача не меняет. Прежняя форма контроля
восстановима из истории git, и восстановление вернуло бы ровно ту хрупкость, ради снятия которой
задача написана, — то есть откат наблюдаем, а не тих.
  </reversibility>

  <done>
Второй отрицательный контроль задаёт оба своих случая сам: вердикт первой фазы и ОТСУТСТВИЕ отчёта у
второй заданы поданным корнем, а не состоянием роадмапа; зубы показаны мутацией; судьба помощника
подмены клетки названа числом вызовов, а не решена молча.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 3: ЧЕТВЁРТЫЙ `missing` — отдельным правилом утвердить, что гейт записи не сличает вердикт живого дерева с литералом</name>

  <files>
tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py
  </files>

  <read_first>
- `10-VERIFICATION.md`, ЧЕТВЁРТЫЙ пункт `missing` гейпа 1 дословно: «Отдельным правилом утвердить,
  что ни один модуль `tests/test_planning/` не читает поле `status` отчёта СВОЕЙ фазы в утверждении
  — иначе следующий гейт регламента повторит расхождение». ⚠️ ЭТО ТЗ, а не пожелание: предмет
  правила — КЛАСС отказа, а не два конкретных контроля, которые чинят задачи 1 и 2.
- `tests/test_planning/test_requirement_completion_follows_verification.py:304-328` — ПЕРВЫЙ
  отрицательный контроль ДО задачи 1: он называет `PLANNING_ROOT` И несёт литерал `"gaps_found"`.
  Это ПРИЗНАК A в чистом виде.
- `tests/test_planning/test_requirement_completion_follows_verification.py:330-351` — ВТОРОЙ
  отрицательный контроль ДО задачи 2 ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ДОСЛОВНО И СЛИЧИТЬ С ПРИЗНАКОМ A: он
  называет `PLANNING_ROOT`, но ВЕРДИКТНОГО ЛИТЕРАЛА НЕ НЕСЁТ ВОВСЕ — его строковые константы суть
  `"FORM-06"`, `"FORM-03"` и `"НЕТ ВОВСЕ"`, а опасное утверждение выражено как
  `assert absent.verdict is None`. ЗАМЕР СНЯТ ПЛАНИРОВЩИКОМ 2026-09-05 и приводится здесь потому,
  что из него следует ФОРМА ПРАВИЛА: конъюнкция «живой корень И литерал» этот контроль не увидела
  бы НИКОГДА, а он ОПАСНЕЕ первого — вшивает не вердикт, а ОТСУТСТВИЕ отчёта Фазы 11.
- `tests/test_planning/test_requirement_completion_follows_verification.py` ПОСЛЕ задач 1 и 2 —
  положительный материал правила: пять несущих правил называют живой корень, НЕ несут вердиктных
  литералов и поля `verdict` в утверждении не касаются. Правило обязано пропускать их и ловить обе
  ветви класса.
- `tests/test_planning/test_requirement_completion_follows_verification.py:354-370` —
  `test_the_verification_index_is_built_by_walking_the_whole_record_tree`: несущее правило, которое
  называет `PLANNING_ROOT` и судит НЕПУСТОТУ индекса. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: это ГРАНИЦА признака B
  — поля `verdict` оно не касается, и признак B обязан его ПРОПУСКАТЬ.
- `tests/test_planning/test_requirement_completion_follows_verification.py:271-289` —
  `test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself`: читает `row.status` КЛЕТКИ
  ТРЕБОВАНИЯ. ⚠️ ВТОРАЯ ГРАНИЦА признака B: имя `status` в перечень полей вердикта НЕ ВХОДИТ, иначе
  правило покраснело бы на несущем правиле, читающем СОВСЕМ ДРУГОЕ поле совсем другой записи.
- `tests/test_planning/test_state_progress_matches_roadmap.py:44-49` — имена, которыми модуль
  каталога адресует ЖИВОЕ дерево (`PROJECT_ROOT`, `ROADMAP_PATH`, `STATE_PATH`), и `:189-232` —
  как тот же автор строит синтетические входы вместо живых.
- `tests/test_planning/test_requirement_completion_follows_verification.py:44-60` — имена
  `PROJECT_ROOT`, `PLANNING_ROOT`, `REQUIREMENTS_PATH` и форма летописи числа у константы
  `REQUIREMENT_ROWS_DECLARED`: ОБЪЯВЛЕННОЕ ЧИСЛО, откуда взялось, чем измерено, каким планом. Ту же
  форму наследует летопись перечня вердиктов настоящего модуля.
- `tests/test_pages/test_confirm_delete_transport.py:2992-3019` — форма зубов на СИНТЕТИЧЕСКОМ
  входе, которую наследует контроль настоящего модуля.
- `.planning/phases/*/[0-9]*-VERIFICATION.md` — поле `status` шапки каждого отчёта дерева: перечень
  вердиктов СНИМАЕТСЯ ЗАМЕРОМ по дереву, а не берётся из памяти, и замер приводится в сводке.
  </read_first>

  <precondition>
Задачи 1 и 2 приземлены: оба отрицательных контроля переведены на синтетические входы и прогон
модуля принуждения зелен. До этого настоящее правило КРАСНО ПО ПОСТРОЕНИЮ и называет обоих
нарушителей поимённо — это и есть его красный, и его надо УВИДЕТЬ, а не обойти порядком задач.
  </precondition>

  <behavior>
1. Разборщик принимает ИСХОДНИК модуля ТЕКСТОМ и его имя, а не путь: без параметра отрицательный
   контроль был бы невыразим — то же основание, которым сам каталог объясняет параметризацию входа
   у `_requirement_rows` и `_requirement_flags`.
2. Нарушителем считается тестовая функция, которая называет имя, адресующее живое дерево записи, И
   ЛИБО несёт строковый литерал из объявленного перечня вердиктов (ПРИЗНАК A), ЛИБО делает
   УТВЕРЖДЕНИЕ О ВЕРДИКТЕ отчёта — ВКЛЮЧАЯ УТВЕРЖДЕНИЕ О ЕГО ОТСУТСТВИИ (ПРИЗНАК B). Определение
   есть ДИЗЪЮНКЦИЯ ДВУХ ПРИЗНАКОВ, а не конъюнкция одного. Имя живого корня ПОРОЗНЬ нарушением не
   является, и это записано: пять несущих правил каталога называют живой корень законно.
3. ПРИЗНАК B ЗАВЕДЁН ЗАМЕРОМ, А НЕ ИЗ ОСТОРОЖНОСТИ, И БЕЗ НЕГО ПРАВИЛО НЕВЫПОЛНИМО ПО ПОСТРОЕНИЮ.
   Тело второго отрицательного контроля (`:330-351`) вердиктного литерала НЕ НЕСЁТ ВОВСЕ: его
   строковые константы суть `"FORM-06"`, `"FORM-03"`, `"НЕТ ВОВСЕ"`, а опасное утверждение выражено
   как `assert absent.verdict is None`. Четвёртый `missing` требует правила против КЛАССА — «ни один
   модуль `tests/test_planning/` не читает поле `status` отчёта СВОЕЙ фазы в утверждении», — а
   признак A накрывает лишь подслучай сличения с литералом.
4. ПРИЗНАК B НЕ НАКРЫВАЕТ ЗАКОННОГО, И ЕГО ГРАНИЦЫ ЗАМЕРЕНЫ, А НЕ ПРЕДПОЛОЖЕНЫ: ни одно несущее
   правило каталога поля вердикта в утверждении не касается. `test_the_verification_index_is_built_by_walking_the_whole_record_tree`
   называет живой корень и судит НЕПУСТОТУ индекса; `test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself`
   читает `row.status` КЛЕТКИ ТРЕБОВАНИЯ, а не поле отчёта, — и поэтому имя `status` в перечень
   полей вердикта НЕ ВХОДИТ.
5. Несущее правило обходит ВСЕ модули каталога `tests/test_planning/`, кроме самого себя, и находит
   ноль нарушителей. Отказ называет, КАКИМ ПРИЗНАКОМ пойман каждый: без этого «названы оба» не
   отличить от «оба пойманы одной ветвью», и вторая ветвь осталась бы непроверенной.
6. Обход НЕ ВЫРОЖДАЕТСЯ В ПУСТОЙ: правило утверждает, что модулей найдено больше нуля и тестовых
   функций разобрано больше нуля, — иначе оно зеленело бы вакуумом при первом же переименовании
   каталога.
7. ОБА СЛОВАРЯ ОБЪЯВЛЕНЫ и сняты замером, а не выдуманы: перечень вердиктов — замером по дереву,
   перечень полей вердикта — чтением каталога; число элементов КАЖДОГО утверждается вместе с
   летописью, откуда оно взялось.
8. Зубы: ЧЕТЫРЕ половины, по одной на каждую ветвь определения и её границу. (а) Исходник с одной
   функцией, называющей живой корень И несущей литерал вердикта, — нарушитель ровно один, назван по
   имени, признак A назван в отказе. (б) Тот же исходник БЕЗ литерала и БЕЗ утверждения о вердикте —
   нарушителей ноль. (в) Исходник с литералом, но БЕЗ имени живого дерева, — нарушителей ноль.
   (г) ⚠️ ЧЕТВЁРТАЯ ПОЛОВИНА, СТЕРЕГУЩАЯ ИМЕННО ПРИЗНАК B: исходник, называющий живой корень и
   несущий `assert <что-то>.verdict is None` БЕЗ ЕДИНОГО вердиктного литерала, — нарушитель ровно
   один, признак B назван в отказе. Без (г) признак B вошёл бы в правило НЕПРОВЕРЕННЫМ.
  </behavior>

  <action>
Завести НОВЫЙ модуль `tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py`.
Существующих модулей каталога не трогать.

**(1) Шапка модуля.** Докстринг по форме двух соседей (`test_state_progress_matches_roadmap.py` и
`test_requirement_completion_follows_verification.py`): ЗАЧЕМ — событие названо поимённо (модуль
принуждения плана 10-14 вшил в два отрицательных контроля переходное состояние живого дерева;
поймал это ЧЕТВЁРТЫЙ круг верификации прогоном в обе стороны, а не прогон суиты, потому что суита
зеленела ровно на том, что фаза не закрыта); ЧЕГО ФАЙЛ НЕ ДОКАЗЫВАЕТ — он не судит прозу ни одного
документа, не утверждает верности ни одного вердикта и ничего не говорит о функциях, живого дерева
не касающихся; ПОЧЕМУ ЭТО НЕ ОТМЕНА D-33 — предмет здесь ФОРМА ИСХОДНИКА суиты, а не проза
операционного документа, и суждения он не требует.

**(2) Константы.** `PLANNING_GATES_DIR` — каталог `tests/test_planning/`, вычисленный от
`__file__` тем же приёмом, что и `PROJECT_ROOT` у соседей. `LIVE_RECORD_NAMES` — множество имён,
которыми модуль каталога адресует живое дерево записи: `PROJECT_ROOT`, `PLANNING_ROOT`,
`REQUIREMENTS_PATH`, `ROADMAP_PATH`, `STATE_PATH`. Каждое имя взято ЧТЕНИЕМ соседних модулей, и это
названо в комментарии. `VERIFICATION_VERDICTS` — перечень значений, которые поле `status` шапки
отчётов верификации принимает В ДЕРЕВЕ; снять его ЗАМЕРОМ (обход `*-VERIFICATION.md`, чтение поля
`status` первого блока frontmatter) и записать полученные значения константой. `VERDICTS_DECLARED` —
их число, с ЛЕТОПИСЬЮ по форме `REQUIREMENT_ROWS_DECLARED`: объявленное число, откуда взялось (замер
по дереву), чем измерено (собственный обход, а не счёт глазом), каким планом (10-17, четвёртая
партия закрытия гейпов Фазы 10). ⚠️ ЧИСЛО В ПЛАНЕ НЕ НАЗЫВАЕТСЯ НАМЕРЕННО: планировщик замер не
делал, и вписанное сюда число было бы ориентиром, выданным за замер, — исполнитель ставит своё и
приводит его в сводку вместе с командой замера.

⚠️ **ВТОРОЙ СЛОВАРЬ — `REPORT_VERDICT_ATTRS`, И ОН НЕСЁТ ПРИЗНАК B.** Множество имён ПОЛЕЙ, которыми
каталог адресует вердикт отчёта верификации. Снять ЧТЕНИЕМ каталога: поле `verdict` несут ОБА
класса записи модуля принуждения — `VerificationReport.verdict` (`:137`) и
`PrematureCompletion.verdict` (`:194`). ⚠️ ИМЯ `status` В ЭТОТ ПЕРЕЧЕНЬ НЕ ВХОДИТ, И ОСНОВАНИЕ
ЗАПИСЫВАЕТСЯ РЯДОМ С КОНСТАНТОЙ: `RequirementRow.status` — КЛЕТКА ТРЕБОВАНИЯ, а не поле отчёта, и
несущее правило `test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself` читает именно
её, законно и на живом дереве. Перечень, включивший `status`, покраснел бы на несущем правиле — то
есть на работе, а не на дефекте. `VERDICT_ATTRS_DECLARED` — число элементов второго словаря, с
летописью той же формы (объявленное число, откуда взялось — чтение каталога, чем измерено, каким
планом). ⚠️ ЧИСЛО В ПЛАНЕ ОПЯТЬ НЕ НАЗЫВАЕТСЯ: замер делает исполнитель и приводит команду в сводку.

**(3) Разборщик `offending_functions(module_name, source)`.** Вход — имя модуля и его ИСХОДНИК
ТЕКСТОМ. Разбор через `ast.parse`. Обходятся определения функций верхнего уровня, чьё имя начинается
с `test_`. Функция считается нарушителем, если множество имён (`ast.Name`) в её теле пересекается с
`LIVE_RECORD_NAMES` И выполняется ХОТЯ БЫ ОДНО из двух:

— **ПРИЗНАК A (сличение с литералом).** Среди строковых констант (`ast.Constant` со строковым
  значением) тела функции есть значение из `VERIFICATION_VERDICTS`.
— **ПРИЗНАК B (утверждение о вердикте, ВКЛЮЧАЯ утверждение о его ОТСУТСТВИИ).** Внутри хотя бы
  одного узла `ast.Assert` тела функции встречается обращение к полю (`ast.Attribute`), чьё имя
  (`attr`) принадлежит `REPORT_VERDICT_ATTRS`. ⚠️ ОБЛАСТЬ СУЖЕНА ДО `ast.Assert` НАМЕРЕННО И
  ДОСЛОВНО ПО ТЗ: четвёртый `missing` говорит «читает поле `status` отчёта СВОЕЙ фазы **В
  УТВЕРЖДЕНИИ**». Функция, ПЕРЕДАЮЩАЯ `.verdict` во вспомогательную сборку и ничего о нём не
  утверждающая, нарушителем не является, и без этого сужения правило покраснело бы на построении
  отчёта, а не на суждении о нём.

Возврат — список записей «имя функции, номер строки, ПРИЗНАК (A или B), найденный предмет (литерал
для A, выражение поля для B)», чтобы отказ называл предмет И ВЕТВЬ, а не факт. ⚠️ БЕЗ НАЗВАННОЙ
ВЕТВИ приёмочный критерий «названы ОБА нарушителя» не отличает работу правила от того, что оба
пойманы одной ветвью, а вторая вошла непроверенной. ⚠️ ЛИТЕРАЛЫ ВНУТРИ СТРОК РАЗБОРУ НЕ ПОДЛЕЖАТ ПО
ПОСТРОЕНИЮ: имя, упомянутое внутри строкового литерала, узлом `ast.Name` не является, — назвать это
в докстринге, потому что от этого свойства зависит выразимость контроля пункта (7).

**(4) Сборка `planning_gate_sources()`** — пары «имя модуля, исходник» для всех `*.py` каталога,
кроме `__init__.py` и кроме САМОГО настоящего модуля. Исключение себя назвать в докстринге с
основанием: настоящий модуль держит перечень вердиктов ДАННЫМИ и подаёт их разборщику, а не
сличает с ними вердикт живого дерева в утверждении, — то есть предметом правила он не является, и
включённый в обход он объявил бы нарушителем собственный контроль зубов.

**(5) Несущее правило `test_no_planning_gate_asserts_a_live_verdict_literal`.** Обходит
`planning_gate_sources()`, собирает нарушителей со всех модулей, утверждает, что их нет. Текст
отказа перечисляет `модуль:строка: имя функции — ПРИЗНАК (A|B) — предмет` для каждого и называет,
ЧТО делать: питать контроль синтетическим корнем, а не живым деревом. ⚠️ ИМЯ ПРАВИЛА ОСТАВЛЕНО
ПРЕЖНИМ НАМЕРЕННО, а его предмет в докстринге РАСШИРЕН до класса: «утверждение о вердикте отчёта
живого дерева, включая утверждение о его ОТСУТСТВИИ». Имя, гоняющееся за каждым расширением
предмета, ломает ссылки соседних планов на него; расхождение имени с предметом закрывается
докстрингом, а не переименованием. Отдельными утверждениями — непустота:
модулей найдено больше нуля, тестовых функций разобрано больше нуля. Без них правило зеленело бы
вакуумом.

**(6) Правило числа `test_the_number_of_known_verdicts_is_declared`.** Утверждает ДВА равенства —
по одному на КАЖДЫЙ словарь: длина `VERIFICATION_VERDICTS` равна `VERDICTS_DECLARED`, и длина
`REPORT_VERDICT_ATTRS` равна `VERDICT_ATTRS_DECLARED`. В тексте отказа: вердикт, ПРИШЕДШИЙ в дерево,
обязан быть внесён в перечень с летописью, иначе правило перестанет ловить функции, сличающие ИМЕННО
его; поле вердикта, ПЕРЕИМЕНОВАННОЕ в каталоге, обязано быть внесено туда же, иначе признак B молча
перестанет видеть ветвь отсутствия. ⚠️ ВТОРОЕ РАВЕНСТВО НЕ ЗАВОДИТ ТРЕТЬЕГО ПРАВИЛА: предмет у обоих
ОДИН — объявленный словарь вердикта, — и разводить его на два имени значило бы завести второе место
одной летописи.

**(7) Контроль зубов `test_control_a_synthetic_offender_is_named_by_the_rule` — ЧЕТЫРЕ ПОЛОВИНЫ.**
Подать разборщику СИНТЕТИЧЕСКИЕ исходники, по одному на половину:

— **(а) ПРИЗНАК A ловится.** Одна функция `test_…`, называющая имя из `LIVE_RECORD_NAMES` и несущая
  литерал вердикта. Утверждать: нарушитель ровно один, назван по имени, в отказе виден литерал И
  буква признака `A`.
— **(б) Чистый исходник пропускается.** Тот же исходник БЕЗ литерала И БЕЗ утверждения о поле
  вердикта: нарушителей ноль.
— **(в) Литерал без живого корня пропускается.** Исходник с литералом, но БЕЗ имени живого дерева:
  нарушителей ноль.
— **(г) ⚠️ ЧЕТВЁРТАЯ ПОЛОВИНА — ПРИЗНАК B ЛОВИТСЯ, И ОНА НЕСУЩАЯ.** Исходник, где функция называет
  имя из `LIVE_RECORD_NAMES` и несёт `assert <имя>.verdict is None` — БЕЗ ЕДИНОГО вердиктного
  литерала, дословной формой второго отрицательного контроля (`:350`). Утверждать: нарушитель ровно
  один, назван по имени, в отказе видна буква признака `B`. ⚠️ БЕЗ (г) признак B вошёл бы в правило
  НЕПРОВЕРЕННЫМ, а именно он закрывает ту ветвь класса, ради которой написан четвёртый `missing`, —
  и именно он делает приёмочный критерий «названы ОБА» достижимым, а не невыполнимым по построению.
— **(д) ГРАНИЦА ПРИЗНАКА B, ТОЙ ЖЕ ПОЛОВИНОЙ (г), ВТОРЫМ УТВЕРЖДЕНИЕМ.** Исходник, где функция
  называет живой корень и УПОМИНАЕТ `.verdict` ВНЕ утверждения (передаёт во вспомогательную
  сборку), — нарушителей ноль. Без этого утверждения признак B было бы неотличимо от «любое
  упоминание поля», и он покраснел бы на построении отчёта.

Половины названы и разведены отдельно, потому что определение есть ДИЗЪЮНКЦИЯ двух признаков, каждый
из которых сам является конъюнкцией с именем живого корня: контроль, проверивший одну ветвь, зеленел
бы на разборщике, читающем только её.

**(8) Что НЕ делается.** Модуль не читает и не правит `.planning/` ни одного файла: его предмет —
исходник суиты. Перечень `LIVE_RECORD_NAMES` не расширяется «на будущее» именами, которых в каталоге
нет: перечень, называющий несуществующее, неотличим от перечня, до которого не дошли руки.

**(ПОСЛЕДНИЙ ШАГ ЗАДАЧИ) ОБНОВИТЬ ГРАФ ПРОЕКТА — `graphify update .`.** ⚠️ ЭТО ТРЕБОВАНИЕ
`./CLAUDE.md` ДОСЛОВНО: «After modifying code, run `graphify update .` to keep the graph current
(AST-only, no API cost)». Настоящий план правит файлы, которые граф индексирует, поэтому шаг
обязателен и стои́т ПОСЛЕДНИМ — после всех правок плана и после прогонов.

⚠️ **ПОЛЕ `graphify.enabled: false` В `.planning/config.json` ОСНОВАНИЕМ ДЛЯ ОТКАЗА НЕ ЯВЛЯЕТСЯ, И
ЭТО ЗАПИСАНО ПРЯМО, А НЕ ПРОПУЩЕНО МОЛЧА.** Замер планировщика 2026-09-05: конфигурация несёт
`"graphify": {"enabled": false}` — это ключ ИНТЕГРАЦИИ GSD, управляющий графом `.planning/graphs/`,
которого в дереве НЕТ вовсе (`ls -d .planning/graphs` → отсутствует). Граф ПРОЕКТА живёт в
`graphify-out/` (`ls -d graphify-out` → есть), команда доступна (`command -v graphify` → есть), и
свежести этого графа требует `./CLAUDE.md`, а не конфигурация GSD. ДВА РАЗНЫХ ГРАФА, ДВА РАЗНЫХ
ВЛАДЕЛЬЦА, и выключенный ключ одного не отменяет требования другого.

⚠️ **ПОБОЧНОГО ЭФФЕКТА НА ПРИЁМОЧНЫЕ КРИТЕРИИ ПАРТИИ НЕТ, И ЭТО ЗАМЕРЕНО:** `graphify-out/`
игнорируется гитом (`.gitignore:56`, `**/graphify-out/`) и деревом не отслеживается, поэтому шаг не
может тронуть ни один вывод `git diff`, на котором стоят гейты `<verify>` и приёмочные критерии
планов партии.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `3` — три правила модуля (несущее, число вердиктов, зубы) не заведены либо не зелены</fails_when>
    <automated>uv run pytest tests/test_planning/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `17` — 14 правил каталога до партии плюс минимум 3 новых</fails_when>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — партия правит только суиту и запись, и это утверждается прогоном, а не предполагается</fails_when>
    <automated>graphify update .</automated>
    <fails_when>ненулевой код возврата либо вывод содержит `Traceback` — граф проекта не обновлён, и следующий читатель получит на вопрос о коде устаревший ответ. ⚠️ `command not found` — НАХОДКА ДЛЯ СВОДКИ (инструмент не установлен в среде исполнения), а не молчаливый пропуск шага регламента</fails_when>
  </verify>

  <acceptance_criteria>
- Правило ВИДЕЛО СВОЙ КРАСНЫЙ, И В КРАСНОМ НАЗВАНЫ ОБА НАРУШИТЕЛЯ С ИХ ПРИЗНАКАМИ: в сводке
  приведён вывод прогона модуля на дереве, где обе задачи ОТКАЧЕНЫ (`git stash` либо прогон на копии
  контролей до правки), и в выводе названы ОБА —
  `test_control_negative_a_premature_completion_reddens_the_rule` (ожидается ПРИЗНАК A: он несёт
  литерал `"gaps_found"`) и `test_control_negative_two_premature_completions_are_both_named`
  (ожидается ПРИЗНАК B: вердиктного литерала он не несёт вовсе, опасное утверждение —
  `assert absent.verdict is None`). ⚠️ ЭТОТ КРИТЕРИЙ ДОСТИЖИМ ТОЛЬКО ПРИ ЗАВЕДЁННОМ ПРИЗНАКЕ B: при
  одном признаке A второй нарушитель не будет назван НИКОГДА, и это ЗАМЕРЕНО планировщиком по телу
  контроля (`:330-351`, замер 2026-09-05). Если вывод назвал только одного — правило собрано
  конъюнкцией, и его надо чинить, а не сужать критерий.
- ⚠️ ЕСЛИ ПРИЗНАК B ОКАЗАЛСЯ НЕПОДЪЁМНЫМ (единственная законная развилка): критерий сужается до
  ОДНОГО нарушителя, и тем же действием ЗАПИСЫВАЕТСЯ ПРЯМО — в `<not_closed_by_this_plan>` сводки и
  окном в `.planning/WINDOWS.md` с названным владельцем, — что ветвь класса «утверждение об
  ОТСУТСТВИИ отчёта» остаётся БЕЗ ГЕЙТА. МОЛЧАЛИВОЕ СУЖЕНИЕ КРИТЕРИЯ ЗАПРЕЩЕНО: четвёртый `missing`
  говорит о КЛАССЕ, и закрытие подслучая, выданное за закрытие класса, есть ровно то расхождение
  записи с кодом, против которого написана вся партия.
- ОБА СЛОВАРЯ сняты ЗАМЕРОМ: в сводке приведены команда замера `VERIFICATION_VERDICTS`, её вывод и
  перечень значений; и отдельно — команда замера `REPORT_VERDICT_ATTRS` по каталогу, её вывод и
  перечень имён полей. `VERDICTS_DECLARED` и `VERDICT_ATTRS_DECLARED` равны длинам своих перечней.
- ИМЯ `status` В `REPORT_VERDICT_ATTRS` НЕ ВОШЛО, И ОСНОВАНИЕ ПРИВЕДЕНО: в сводке приведена цитата
  комментария у константы и вывод прогона, показывающий, что
  `test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself` нарушителем не назван.
- Разборщик принимает исходник ТЕКСТОМ и возвращает ПРИЗНАК: в сводке приведена сигнатура
  `offending_functions` и одна запись возврата целиком.
- ЧЕТЫРЕ половины контроля зубов заведены и различены: в сводке приведены имена всех утверждений и
  результат каждого, включая (г) — синтетический исходник с `assert <имя>.verdict is None` БЕЗ
  вердиктного литерала, на котором нарушитель ровно один и назван признак `B`, — и (д), где
  `.verdict` упомянут ВНЕ утверждения и нарушителей ноль.
- Обход не выродился: в сводке приведены число разобранных модулей каталога и число разобранных
  тестовых функций, оба больше нуля.
- Настоящий модуль исключён из собственного обхода, и основание записано в докстринге сборки: в
  сводке приведена цитата основания.
- `.planning/` не тронут: `git diff --name-only -- .planning/` — пусто.
  </acceptance_criteria>

  <reversibility rating="reversible">
Заводится один НОВЫЙ тестовый модуль; ни одного существующего файла задача не правит, поведения
продукта не меняет. Откат — удаление файла, и он возвращает дерево ровно в состояние после задачи 2.
  </reversibility>

  <done>
Правило независимости гейтов записи от вердикта живого дерева заведено, зелено, показало свой
красный на откаченных задачах 1 и 2 и назвало обоих прежних нарушителей поимённо — ПЕРВОГО признаком
A, ВТОРОГО признаком B, — то есть закрыт КЛАСС, а не подслучай сличения с литералом; зубы показаны
на синтетическом исходнике ЧЕТЫРЬМЯ раздельными половинами, из которых четвёртая стережёт именно
ветвь «утверждение об ОТСУТСТВИИ отчёта»; оба словаря объявлены числами с летописями и сняты
замером.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Новые символы в `tests/test_planning/test_requirement_completion_follows_verification.py`:**

| Символ | Что это |
|---|---|
| `_fake_planning_root(tmp_path, verdicts)` | сборка синтетического корня записи: «номер фазы → вердикт», `None` — отчёта нет вовсе |
| `_synthetic_requirements(rows)` | сборка синтетической записи требований из троек «имя, клетка фазы, состояние» |
| `test_control_a_passed_verdict_in_the_synthetic_root_empties_the_finding` | зубы ПЕРВОГО отрицательного контроля мутацией вердикта |
| `test_control_a_synthetic_report_for_the_second_phase_leaves_one_finding` | зубы ВТОРОГО отрицательного контроля мутацией наличия отчёта |

**Переписанные символы того же файла (имена сохраняются, меняется ИСТОЧНИК ВХОДА):**
`test_control_negative_a_premature_completion_reddens_the_rule`,
`test_control_negative_two_premature_completions_are_both_named`.

**Возможно снимаемый символ:** `_with_status` — снимается ТОЛЬКО если после задачи 2 у него ноль
вызовов; решение принимается замером и называется в сводке числом.

**Новый файл:** `tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py`.

| Символ | Что это |
|---|---|
| `PLANNING_GATES_DIR` | каталог гейтов записи, вычисленный от `__file__` |
| `LIVE_RECORD_NAMES` | имена, которыми модули каталога адресуют живое дерево записи |
| `VERIFICATION_VERDICTS` | перечень вердиктов поля `status`, СНЯТЫЙ ЗАМЕРОМ по дереву |
| `VERDICTS_DECLARED` | их число с летописью по форме `REQUIREMENT_ROWS_DECLARED` |
| `offending_functions(module_name, source)` | разборщик: исходник приходит ТЕКСТОМ |
| `planning_gate_sources()` | пары «имя модуля, исходник» для каталога, без себя и без `__init__.py` |
| `test_no_planning_gate_asserts_a_live_verdict_literal` | несущее правило плюс два утверждения непустоты |
| `test_the_number_of_known_verdicts_is_declared` | правило числа вердиктов |
| `test_control_a_synthetic_offender_is_named_by_the_rule` | зубы тремя раздельными половинами |

**Новых путей продукта, констант продукта и миграций план НЕ производит:**
`app/`, `alembic/`, `pyproject.toml` и `.planning/` этим планом не правятся ни на символ.
</artifacts_this_phase_produces>

<threat_model>
⚠️ УРОВЕНЬ И ПОРОГ НАЗВАНЫ, А НЕ ПОДРАЗУМЕВАЮТСЯ. Таблица собрана на **ASVS 1**
(`workflow.security_asvs_level` → `1`) с порогом блокировки **`high`**
(`workflow.security_block_on` → `"high"`) при включённом принуждении
(`workflow.security_enforcement` → `true`). Все три величины СНЯТЫ `config-get`, а не объявлены
планом. Строка `*-SC` несёт `high` как severity КЛАССА «цепочка поставки», а `accept` — потому что
ПРЕДМЕТА НЕТ: ни одна задача плана не устанавливает пакетов, `pyproject.toml` не правится.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| гейт суиты → регламент проекта | правило, зеленеющее при условии «фаза не закрыта», отключается вместе со свойством, которое стережёт |
| синтетический вход → предмет контроля | контроль, чей предмет задан деревом, меняет смысл от чужого коммита |
| разборщик исходника суиты → каталог гейтов | обход, выродившийся в пустой, зеленеет вакуумом и неотличим от работающего |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-17-01 | Repudiation | принуждение регламента отметок требований | high | mitigate | Оба отрицательных контроля переезжают на синтетические входы (задачи 1, 2), поэтому зелень модуля перестаёт держаться на вердикте своей фазы. Свойство, которое иначе отключилось бы вместе с «подогнанным» правилом, — единственное машинное принуждение регламента отметок во всём дереве |
| T-10-17-02 | Tampering | утверждения отрицательных контролей | high | mitigate | Правится ИСТОЧНИК ВХОДА, а не утверждения: смысл каждого контроля сохраняется, и это проверяется тем, что оба продолжают называть свои случаи раздельно. Подгонка утверждений под сегодняшнее дерево объявлена прохибицией плана |
| T-10-17-03 | Repudiation | синтетика вместо вшитого состояния | high | mitigate | У обоих контролей заведены зубы мутацией входа (задачи 1, 2): без них вшитое состояние было бы заменено НЕПРОВЕРЕННЫМ, что хуже, потому что незаметно |
| T-10-17-04 | Denial of Service | обход каталога гейтов записи | medium | mitigate | Несущее правило нового модуля утверждает непустоту обхода двумя величинами — числом модулей и числом разобранных функций; вырожденный обход краснит правило, а не зеленеет вакуумом |
| T-10-17-05 | Spoofing | синтетические имена и номера фаз | low | mitigate | Имя `SYN-01`/`SYN-02` и номера фаз `91`/`92` выбраны с записанным основанием: ни один префикс реестра требований и ни один номер фазы роадмапа с ними не совпадает, поэтому синтетика не может быть принята за живую запись |
| T-10-17-06 | Tampering | перечень вердиктов нового модуля | medium | mitigate | Перечень снимается ЗАМЕРОМ по дереву, а его число объявлено с летописью: вердикт, пришедший в дерево и не внесённый в перечень, краснит правило числа, а не проходит молча |
| T-10-17-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит. Таблица легитимности пакетов не требуется |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q -p no:randomly` — зелено, число правил каталога выросло с
   14 до минимум 17.
2. `uv run pytest tests/ -q -p no:randomly` — прогон целиком зелен.
3. **ЗАМЕР НЕЗАВИСИМОСТИ ОТ ВЕРДИКТА СВОЕЙ ФАЗЫ — ПОВТОРЕНИЕ ЗАМЕРА 3 ОТЧЁТА ВЕРИФИКАЦИИ.**
   Исполнитель повторяет прогон модуля принуждения при поле `status` отчёта
   `10-VERIFICATION.md`, подменённом на `passed`, затем на `human_needed`, затем возвращённом в
   `gaps_found`, и приводит ТРИ числа в сводку. Ожидание: три ОДИНАКОВЫХ зелёных прогона там, где
   прежде было `2 failed / 1 failed / 0 failed`. ⚠️ ПОДМЕНА ДЕЛАЕТСЯ И ОТКАТЫВАЕТСЯ В ОДНОМ
   ДЕЙСТВИИ, а состояние файла после замера проверяется `git diff --name-only -- .planning/` — пусто.
4. `git diff --name-only -- .planning/ app/ pyproject.toml` — пусто: план правит только суиту.
5. `uv run python -m compileall -q app main.py tests` — сборка проекта проходит.
</verification>

<success_criteria>
- Оба отрицательных контроля модуля принуждения задают свой предмет САМИ и не читают живое дерево
  ни одной строкой; смысл каждого сохранён, а не срезан.
- У обоих показаны зубы мутацией входа — синтетика не заменила вшитое состояние непроверенным.
- Прогон модуля ОДИНАКОВ на всех четырёх состояниях вердикта своей фазы, и это ЗАМЕРЕНО, а не
  объявлено: три числа приведены в сводке.
- Расширение предмета названо расширением: в сводке сказано, что ревизия назвала вердикт `passed`,
  а замер верификатора — литерал `gaps_found`, и что правится литерал.
- Четвёртый `missing` закрыт ОТДЕЛЬНЫМ правилом, которое видело свой красный и назвало обоих
  прежних нарушителей поимённо.
- Пять несущих правил модуля принуждения стоят на живом дереве нетронутыми.
- Ни `.planning/`, ни `app/`, ни `pyproject.toml` не тронуты; требование `FORM-06` остаётся
  незавершённым, и настоящий план обратного не утверждает.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-17-SUMMARY.md` по завершении.
</output>
