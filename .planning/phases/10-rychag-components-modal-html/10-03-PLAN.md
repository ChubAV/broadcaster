---
phase: 10-rychag-components-modal-html
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/pages/accounts.py
  - app/pages/ads.py
  - app/pages/history.py
  - app/pages/admin.py
  - tests/test_pages/test_confirm_delete_transport.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_impersonation.py
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ confidence: low при нулевом числе замеров — калибровки проекта не
  # существует (estimate-calibration: sample_count 0, factor 1).
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Семь оставшихся обработчиков за панелями подтверждения отвечают через слой ответа: без HX-Request — прежний 302 на прежний адрес, с HX-Request — 204 и заголовок HX-Location на тот же адрес, тела нет (FORM-06, критерий 4 роадмапа, D-01)"
    - "Форма ответа выбрана ПО КЛАССУ ДЕЙСТВИЯ, а не одна на всех: эти семь либо уводят с экрана, либо меняют личность, либо не имеют строки на остающемся экране — ни одно из них строки с экрана не убирает, поэтому ни одно не отдаёт фрагмента (D-01, D-06)"
    - "Изъятие по смещённому курсору ОБЪЯВЛЕНО, а не подразумевается: удаление аккаунта и удаление объявления уходят в ветку перехода, хотя по классу действия принадлежали бы фрагментному пути; основание ИЗМЕРЕНО — оба списка листаются смещённым курсором, и фрагментное удаление воспроизвело бы CR-01 Фазы 9 дословно (D-08)"
    - "Перечень изъятий написан по форме DISABLED_ELT_EXCEPTIONS: запись + обоснование на запись + счёт ЧИСЛОМ; каждое обоснование называет ФАЗУ-СНИМАТЕЛЯ (Фаза 11) и УСЛОВИЕ снятия («изъятие снимается только вместе с переводом курсора на ключ последней строки»)"
    - "Вход под пользователем отвечает переходом на /dashboard, и cookie личности ставится на ТОТ ЖЕ объект, который уезжает человеку: пара утверждений ТРОЙНАЯ — без заголовка 302 и cookie; с заголовком 204, заголовок перехода И cookie (D-05, ловушка CONTEXT)"
    - "Установка cookie идёт ЕДИНСТВЕННОЙ функцией установки (план 06-02): собственного set_cookie в обработчике не появляется, второго набора атрибутов рядом с первым не заводится"
    - "Ключ ?result= раздела очереди НЕ сводится в реестр ?notice=: адрес проезжает через слой ответа частью строки перехода, код исхода не передаётся, и путь работает на обоих транспортах; шестой микро-контракт назван и адресован Фазе 11, но не объявлен сведённым (D-07)"
    - "Четыре действующих кода реестра выдаются ровно там, где выдаются сегодня: повтор поставлен в очередь, повтор занят, повтор невозможен, доступ закрыт — и ещё два в разделе воркеров; новых кодов план не заводит (D-03)"
    - "Ни у одного из семи обработчиков не осталось собственного RedirectResponse ни в одной ветке, включая «нет сессии» и «сущность не найдена» (G-2)"
    - "Число маршрутов подтверждения удаления, обойдённых парами, объявлено КОНСТАНТОЙ и равно восьми: молчаливое исчезновение маршрута из обхода краснеет, а параметризованный обход не превращает 8 пар в 16 функций"
    - "NOT_YET_CONVERTED_COUNT опущен 33 → 26 снятием семи ключей; летопись называет семь обработчиков СЛОВАМИ, а не ключами перечня — иначе греп снятия зеленеет на собственном комментарии"
    - "HX_HEADER_READS и HX_HEADER_WRITES не двинулись (1 и 2): заголовки пишет слой ответа, и семь маршрутов новых мест записи не заводят"
    - "Повторное подтверждённое действие остаётся безвредным на всех семи маршрутах: ни один из них не создаёт второй записи при втором нажатии — перезапуск воркера идемпотентен по построению, снятие задачи по отсутствующему идентификатору не находит её, удаление по несуществующей строке уходит той же веткой (FORM-06, идемпотентность)"
    - "Параллельные или прерванные подтверждения не оставляют системы в несогласованном виде: единица записи каждого из семи — один commit или один вызов менеджера; hx-disabled-elt серверной защитой НЕ является и таковой не объявляется (FORM-06, конкурентность)"
    - statement: "Переход по заголовку HX-Location действительно приводит человека на нужный экран, а вход под пользователем действительно СМЕНИЛ личность — панель админа показывает чужую учётную запись, а не прежнюю: ручной UAT фазы, собственное наблюдение к D-05. httpx устанавливает cookie, но переход по заголовку не исполняет"
      verification: backstop
  artifacts:
    - "app/pages/accounts.py — accounts_delete через слой ответа"
    - "app/pages/ads.py — ads_delete через слой ответа"
    - "app/pages/history.py — history_retry через слой ответа с четырьмя действующими кодами"
    - "app/pages/admin.py — admin_restart_worker, admin_drop_task, admin_impersonate, admin_delete_user через слой ответа"
    - "tests/test_pages/test_confirm_delete_transport.py (НОВЫЙ) — CONFIRMED_DELETE_ROUTES, CONFIRMED_DELETE_ROUTES_DECLARED, test_every_confirmed_delete_route_answers_both_transports, test_the_number_of_confirmed_delete_routes_is_the_declared_one"
    - "tests/test_pages/test_htmx_gates.py — OFFSET_CURSOR_EXCEPTIONS, OFFSET_CURSOR_EXCEPTIONS_DECLARED, test_every_offset_cursor_exception_carries_a_reason, test_the_number_of_offset_cursor_exceptions_is_the_declared_one, test_control_negative_an_undeclared_offset_cursor_list_reddens_the_gate"
    - "tests/test_pages/test_impersonation.py — test_impersonation_over_htmx_keeps_both_the_location_and_the_cookie"
  key_links:
    - "respond(redirect=…) → семь обработчиков: первый вызов слоя ответа в пяти файлах страничного слоя; ломается — обработчик, забывший путь деградации, НЕ СОБИРАЕТСЯ как вызов, потому что адрес объявлен обязательным ключевым аргументом"
    - "set_session_cookie(результат respond(), …) → вход под пользователем: единственная связь, которой cookie личности доезжает до браузера на транспорте htmx; ломается — вход молча не состоится, а тест, проверяющий только заголовок перехода, останется зелёным"
    - "OFFSET_CURSOR_EXCEPTIONS → Фаза 11: единственная запись, по которой следующая фаза узнает, что перевод этих списков на фрагментное удаление ОБЯЗАН взять курсор вместе с ними; ломается — Фаза 11 переводит списки и воспроизводит CR-01 на двух новых экранах"
  prohibitions:
    - statement: "курсоры порций аккаунтов и объявлений на ключ последней строки здесь НЕ переводятся — это работа Фазы 11, и порознь с переводом этих списков на фрагментное удаление она смысла не имеет (D-08)"
      status: flagged-unverified
      verification: none
    - statement: "ключ ?result= раздела очереди в реестр ?notice= здесь не сводится — свод отдан Фазе 11, разделу admin (D-07)"
      status: flagged-unverified
      verification: none
    - statement: "снятие задачи из очереди фрагментным не делается: ключ панели собран из ПОЗИЦИИ строки, а перевести его на идентификатор задачи НЕЛЬЗЯ — это отгруженное решение безопасности WR-04, ключ становится ИМЕНЕМ атрибута, а величина приходит из тела очереди"
      status: flagged-unverified
      verification: none
    - statement: "идентификаторы строкам админских включений здесь не присваиваются — роадмап назначил эти три файла Фазе 11 прямым текстом"
      status: flagged-unverified
      verification: none
    - statement: "третий выход слоя ответа (внешний адрес отдельным заголовком) здесь НЕ открывается: докстринг модуля отдал его Фазе 11 вместе с формой оплаты, и открыть его тут значило бы ввести ветку без её предмета"
      status: flagged-unverified
      verification: none
    - statement: "собственный set_cookie в обработчике входа под пользователем не заводится — установка идёт единственной функцией установки (план 06-02)"
      status: flagged-unverified
      verification: none
    - statement: "предикат отказа и ответ тому, кто пришёл без слоя письма, не меняются ни на символ: фаза меняет ТРАНСПОРТ, а не то, ЧТО действие делает (FOUND-04)"
      status: flagged-unverified
      verification: none
---

<objective>
Семь маршрутов, стоящих за панелями подтверждения и НЕ убирающих строки с остающегося экрана,
переходят на слой ответа: удаление аккаунта, удаление объявления, повтор отправки, перезапуск
воркера, снятие задачи из очереди, вход под пользователем и удаление пользователя. Вместе с
единственным фрагментным маршрутом плана 10-01 это все восемь маршрутов фазы.

Purpose: без этой части правка разметки дала бы человеку открытую панель и тишину — форма
панели уже несёт признак отправки htmx, а обработчик ответил бы редиректом, за которым слой
письма пошёл бы прозрачно и вклеил бы целый документ. Здесь же ОБЪЯВЛЯЕТСЯ изъятие по
смещённому курсору: два списка уходят в ветку перехода не потому, что «не дошли руки», а по
измерению, и запись называет фазу-снимателя и условие снятия.

Output: семь переведённых обработчиков, перечень изъятий с числом, параметризованный обход
восьми маршрутов парами, тройная пара для входа под пользователем, `NOT_YET_CONVERTED_COUNT`
33 → 26.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-PATTERNS.md
@.planning/phases/10-rychag-components-modal-html/10-01-SUMMARY.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-CONTEXT.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов фазы — в `10-01-PLAN.md` §«Artifacts this phase produces».
Этим планом создаются:

- `tests/test_pages/test_confirm_delete_transport.py` — НОВЫЙ файл с `CONFIRMED_DELETE_ROUTES`,
  `CONFIRMED_DELETE_ROUTES_DECLARED`, `test_every_confirmed_delete_route_answers_both_transports`,
  `test_the_number_of_confirmed_delete_routes_is_the_declared_one`
- `OFFSET_CURSOR_EXCEPTIONS`, `OFFSET_CURSOR_EXCEPTIONS_DECLARED`,
  `test_every_offset_cursor_exception_carries_a_reason`,
  `test_the_number_of_offset_cursor_exceptions_is_the_declared_one`,
  `test_control_negative_an_undeclared_offset_cursor_list_reddens_the_gate` —
  `tests/test_pages/test_htmx_gates.py`
- `test_impersonation_over_htmx_keeps_both_the_location_and_the_cookie` —
  `tests/test_pages/test_impersonation.py`
</artifacts_this_phase_produces>

<tasks>

<task type="execute" tdd="true">
  <name>Задача 1: Удаление аккаунта и удаление объявления — переход и ОБЪЯВЛЕННОЕ изъятие по курсору</name>

  <files>
app/pages/accounts.py,
app/pages/ads.py,
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_confirm_delete_transport.py (NEW)
  </files>

  <read_first>
- `app/pages/accounts.py:985-996` — `accounts_delete` целиком: ветка «нет сессии», вызов
  удаления аккаунта, редирект на раздел аккаунтов.
- `app/pages/ads.py:716-760` — `ads_delete` целиком: ветка «нет сессии», выборка объявления с
  ограничением владельцем, удаление, редирект на раздел объявлений. Обратить внимание, что
  ОДИН обработчик обслуживает ДВА места подтверждения — карточку списка и панель редактора.
- `app/pages/account_groups.py:588-600` — образец ветки «нет сессии», переведённой на слой
  ответа: собственного редиректа не остаётся (G-2).
- `app/pages/htmx.py:305-367` — `respond` с обязательным ключевым адресом деградации и веткой
  `fragment is None`; `:140-146` — `location_response` (204 без тела); `_local_path` и его
  проверки адреса.
- `app/templates/accounts/list.html:181-195` — вызов панели удаления аккаунта И сентинел
  смещённого курсора (`?offset=`); `app/templates/accounts/partial_cards.html:131`, `:139` —
  то же во второй копии разметки порции.
- `app/templates/ads/list.html:61` и `app/templates/ads/partial_cards.html:7` — смещённый
  курсор раздела объявлений; `app/templates/ads/includes/ad_card.html:131` и
  `app/templates/ads/form.html:272` — ДВА места подтверждения одного маршрута.
- `tests/test_pages/test_htmx_gates.py:199-283` — `NOT_YET_CONVERTED`, форма ключа
  `путь/файл.py::имя_функции`, `NOT_YET_CONVERTED_COUNT` с летописью и предупреждением, что
  это счётчик прогресса вехи; `:355-465` — разборщик `_Handler`, `_calls_response_layer`,
  `_builds_own_redirect` (узнаёт и сборку редиректа, и статус перехода в обеих формах записи).
- `tests/test_templates/test_htmx_markup_gates.py:2686-2796` — `DISABLED_ELT_EXCEPTIONS` и
  `DISABLED_ELT_EXCEPTIONS_DECLARED`: форма перечня изъятий, по которой пишется D-08;
  `:2855-2882` — `MACRO_BORN_EXCEPTIONS` и форма летописи с назначенной фазой-снимателем.
- `tests/conftest.py:70-90` — фикстура `htmx_client` и форма пары.
- `10-CONTEXT.md` D-08 целиком — измерение, отвергнутые варианты и названная сцепка для Фазы 11.
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md`
  §CR-01 — четыре круга закрытия дефекта смещённого курсора и то, чем он кончился.
  </read_first>

  <precondition>
План 10-01 приземлён: макрос печатает признак отправки безусловно, то есть панели этих двух
маршрутов УЖЕ шлют запрос через htmx, а обработчики ещё отвечают редиректом. Проверить
`uv run pytest tests/test_pages/test_htmx_gates.py -q` — зелено, и
`grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'NOT_YET_CONVERTED_COUNT = 33'`
возвращает `1`.
  </precondition>

  <reversibility rating="costly">
Задача исполняет D-08 — ИМЕНОВАННОЕ ИЗЪЯТИЕ из правила выбора формы ответа. Снятие изъятия
требует правки ОБОИХ курсоров и потому дороже, чем сама запись: снять его порознь с переводом
списков на фрагментное удаление нельзя. `one-way` НЕ заслужено — ни данных, ни схемы, ни
внешнего состояния изъятие не трогает; оно есть запись в перечне с числом. Ни одно решение фазы
не имеет рейтинга `one-way`, поэтому чекпойнта решения не ставится.
  </reversibility>

  <behavior>
RED — до перевода обработчиков:

1. `NOT_YET_CONVERTED_COUNT` 33 → 31 со снятием ключей удаления аккаунта и удаления
   объявления; правило отставания краснеет на непереведённых обработчиках.
2. Заводится `OFFSET_CURSOR_EXCEPTIONS` с двумя записями и
   `OFFSET_CURSOR_EXCEPTIONS_DECLARED = 2`; правила перечня краснеют до появления записей.
3. Заводится файл обхода `tests/test_pages/test_confirm_delete_transport.py` с перечнем
   маршрутов подтверждения и константой их числа; на этой задаче в нём живут ДВА маршрута,
   и число объявляется равным двум — задачи 2 и 3 доводят его до восьми, каждая своим
   движением с летописью.

Ожидаемые случаи (оба маршрута):
- POST без заголовка htmx, `follow_redirects=False` → `302`, `location` равен прежнему адресу
  раздела посимвольно.
- Тот же POST под `htmx_client` → `204`, `response.headers["HX-Location"]` равен тому же
  адресу, `response.text` пуст и `"<!DOCTYPE" not in response.text`.
- POST по чужому или несуществующему идентификатору под `htmx_client` → ответ той же формы,
  что и у успешного, строка в базе не удалена.
- Ветка «нет сессии» под `htmx_client` → `204` и заголовок перехода на страницу входа; без
  заголовка → `302` туда же.

GREEN — перевод обоих обработчиков на слой ответа и заполнение перечня изъятий.
  </behavior>

  <action>
**1. `app/pages/accounts.py::accounts_delete`.** Ветка «нет сессии» и ветка успеха переводятся
на слой ответа: адрес деградации — прежний раздел аккаунтов, код исхода не передаётся (на успех
плашка не выдаётся, D-03). Собственной сборки редиректа в теле обработчика не остаётся ни в
одной ветке — этого требует G-2, и разборщик правила узнаёт как сборку ответа-редиректа, так и
статус перехода в обеих формах записи. Импорт слоя ответа заводится в файле ВПЕРВЫЕ: сегодня
слой зовут только из модуля групп аккаунта. Логика удаления, ограничение владельцем и всё, что
делает действие, не трогается ни на символ.

**2. `app/pages/ads.py::ads_delete`.** То же: обе ветки на слой ответа, адрес деградации —
прежний раздел объявлений, кода исхода нет. В докстринг обработчика вписать измеренный факт:
ОДИН маршрут обслуживает ДВА места подтверждения — карточку в списке и панель в редакторе, — и
оба уходят одной веткой перехода; по классу действия панель редактора принадлежала бы переходу
и без изъятия (действие уводит с экрана, экрана после него нет), а карточка списка — изъятию по
курсору.

**3. Перечень изъятий `OFFSET_CURSOR_EXCEPTIONS` (`tests/test_pages/test_htmx_gates.py`).**
Пишется по форме `DISABLED_ELT_EXCEPTIONS`: тип записи с полями «место», «обоснование»,
«фаза-сниматель», «условие снятия», плюс отдельная константа числа записей. Две записи —
раздел аккаунтов и раздел объявлений.

Обоснование каждой записи обязано:
- называть ИЗМЕРЕНИЕ, а не мнение: обе порции листаются смещённым курсором, и адреса файлов
  разметки, где это видно, приводятся поимённо;
- называть ПОСЛЕДСТВИЕ фрагментного удаления числом: список сдвигается, и следующая порция
  пропускает ровно одну карточку — дословно CR-01, дефект, на закрытие которого Фаза 9
  потратила четыре круга;
- называть ФАЗУ-СНИМАТЕЛЯ (Фаза 11) и УСЛОВИЕ снятия дословно: «изъятие снимается только
  вместе с переводом курсора на ключ последней строки; порознь эти две работы смысла не
  имеют»;
- читаться как заявленное изъятие, а НЕ как «сюда не дошли руки» — формулировка это предмет
  записи, а не стиль.

Три правила перечня: каждая запись несёт непустое обоснование и названную фазу-снимателя;
число записей равно объявленному; отрицательный контроль — список на смещённом курсоре, не
объявленный записью, КРАСНИТ правило. Контроль пишется по действующей форме группы контроля
файла: подменяется один модуль в словаре прочитанных исходников, подмена не протекает через кэш.

**4. Файл обхода `tests/test_pages/test_confirm_delete_transport.py` (НОВЫЙ).**
Докстринг обязан назвать, почему пары живут ОДНИМ модулем, а не восемью: свойство одно
(транспорт подтверждённого необратимого действия), маршрутов восемь, и параметризованный обход
делает молчаливое исчезновение одного из них КРАСНЫМ — тогда как восемь отдельных функций в
восьми файлах молчали бы. Форма прямо наследует критерий 4 Фазы 11 («пары параметризованным
обходом, а не 320 функций») и идиому счёта числом D-13 Фазы 8.

Перечень `CONFIRMED_DELETE_ROUTES` — записи с полями: адрес маршрута, тело формы, ожидаемый
адрес приземления, признак «ожидается 204» либо «ожидается 200 с фрагментом», и фикстура
клиента, под которой маршрут доступен (обычный пользователь либо администратор). Константа
`CONFIRMED_DELETE_ROUTES_DECLARED` объявляется числом и на этой задаче равна ДВУМ; её летопись
пополняется задачами 2 и 3.

Обход утверждает ОБЕ половины пары для каждой записи и обязан различать «пришёл заголовок
перехода» и «в теле нет документа»: одиночное утверждение о редиректе зеленело бы ровно в том
состоянии продукта, которое фаза и чинит.

**5. Летопись `NOT_YET_CONVERTED_COUNT`** пополняется строкой 33 → 31 с названием ОБОИХ
обработчиков СЛОВАМИ («удаление аккаунта мессенджера», «удаление объявления»), а не ключами
перечня, и с указанием, что число есть счётчик прогресса вехи.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_htmx_gates.py -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_confirm_delete_transport.py -x` — зелено.
- `uv run pytest tests/test_pages/test_htmx_gates.py -k "own_redirect or backlog or offset_cursor" -x` — зелено, включая отрицательный контроль перечня изъятий.
- `awk '/^async def accounts_delete/,/^@router/' app/pages/accounts.py | grep -c 'RedirectResponse'` возвращает `0`.
- `awk '/^async def ads_delete/,/^@router/' app/pages/ads.py | grep -c 'RedirectResponse'` возвращает `0`.
- `grep -c 'from app.pages.htmx import' app/pages/accounts.py` возвращает `1`; то же для `app/pages/ads.py`.
- `grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'NOT_YET_CONVERTED_COUNT = 31'` возвращает `1`.
- `grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'OFFSET_CURSOR_EXCEPTIONS_DECLARED = 2'` возвращает `1`.
- Каждая запись изъятия называет фазу-снимателя: `uv run pytest tests/test_pages/test_htmx_gates.py -k "every_offset_cursor_exception_carries_a_reason" -x` — зелено.
- Существующие утверждения этих двух маршрутов не сломаны: `uv run pytest tests/test_pages/test_ads_editor.py tests/test_pages/test_ads_status.py tests/test_pages/test_responsive_markup.py -q` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Оба маршрута отвечают через слой ответа на обоих транспортах, собственного редиректа у них не
осталось, изъятие по смещённому курсору объявлено перечнем с обоснованием, фазой-снимателем,
условием снятия и числом, а отрицательный контроль доказывает, что необъявленный список красит
правило. Файл параметризованного обхода заведён и несёт две первые записи.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: Повтор отправки, перезапуск воркера, снятие задачи и удаление пользователя — четыре маршрута с кодами исхода</name>

  <files>
app/pages/history.py,
app/pages/admin.py,
tests/test_pages/test_confirm_delete_transport.py,
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `app/pages/history.py:826-999` — `history_retry` ЦЕЛИКОМ, включая докстринг: серверное
  удержание PAY-02 уже стои́т и переживает ответ; восемь точек выхода, из них четыре несут коды
  реестра (повтор поставлен в очередь, повтор занят, повтор невозможен, доступ закрыт), две —
  редирект на раздел без кода, одна — отказ статусом, одна — ветка «нет сессии».
- `app/pages/admin.py:867-957` — `admin_restart_worker`: пять точек выхода, из них две несут
  коды реестра (контейнера нет, перезапуск не удался); адрес раздела собран переменной
  `location` один раз.
- `app/pages/admin.py:1061-1144` — `admin_drop_task`: три редиректа, все несут ключ `?result=`
  ЧАСТЬЮ строки адреса; `:288-300` — закрытый словарь исходов `QUEUE_DROP_RESULTS`;
  `app/templates/admin/queue.html:34-36` — место отрисовки плашки по этому ключу.
- `app/pages/admin.py:1818-1861` — `admin_delete_user`: четыре точки выхода, одна из них —
  запрет удалить себя с возвратом на карточку пользователя.
- `app/pages/notices.py:87-112` — действующие коды реестра и `:128` — сам реестр; `_with_notice`
  в `app/pages/htmx.py:295-302` — как код приклеивается к адресу.
- `app/pages/htmx.py:305-367` — `respond`: код исхода едет ПАРАМЕТРОМ, а не склейкой строки
  адреса руками; `_require_registered_notice` роняет вызов на незарегистрированном коде.
- `10-PATTERNS.md` §2 — форма вызова для ветки перехода, и прямое предупреждение: для снятия
  задачи из очереди — ОБРАТНОЕ, ключ остаётся частью строки адреса перехода, а код исхода не
  передаётся.
- `10-CONTEXT.md` D-06 и D-07 целиком.
- `tests/test_pages/test_history_retry.py` — действующие утверждения повтора отправки; форма
  проверки кодов исхода в адресе.
- `tests/test_pages/test_admin_panel.py:2955` —
  `test_the_drop_button_and_its_confirmation_panel_still_match`: действующее утверждение
  соответствия кнопки и панели очереди, которое правка не имеет права сломать.
  </read_first>

  <precondition>
Задача 1 приземлена: файл параметризованного обхода существует и несёт две записи,
`NOT_YET_CONVERTED_COUNT` равен 31.
  </precondition>

  <behavior>
RED:
1. `NOT_YET_CONVERTED_COUNT` 31 → 27 со снятием четырёх ключей.
2. `CONFIRMED_DELETE_ROUTES_DECLARED` 2 → 6 с четырьмя новыми записями обхода.

Ожидаемые случаи:
- Повтор отправки: без заголовка → `302` на раздел истории с прежним кодом исхода в адресе; с
  заголовком → `204` и заголовок перехода на ТОТ ЖЕ адрес с тем же кодом, тела нет. Проверить
  ВСЕ ЧЕТЫРЕ действующих кода, каждый своим случаем: успешная постановка в очередь, занятость,
  невозможность повтора, закрытый доступ.
- Перезапуск воркера: то же для обоих действующих кодов и для успеха без кода.
- Снятие задачи: без заголовка → `302` на раздел очереди со СВОИМ ключом в адресе; с
  заголовком → `204` и заголовок перехода на посимвольно тот же адрес. Ключ едет ЧАСТЬЮ строки
  адреса, а код реестра НЕ передаётся — иначе слой ответа уронил бы вызов на
  незарегистрированном коде.
- Удаление пользователя: без заголовка → `302` на список пользователей; с заголовком → `204` и
  заголовок перехода; ветка «нельзя удалить себя» → тот же транспорт, адрес карточки
  пользователя.
- Отказ по правам (не администратор) на всех трёх админских маршрутах отвечает прежним статусом
  на ОБОИХ транспортах — предикат отказа фаза не трогает (FOUND-04).

GREEN — перевод четырёх обработчиков.
  </behavior>

  <action>
**1. `history_retry`.** Каждая точка выхода, собиравшая редирект, переводится на слой ответа.
Адрес деградации — прежний раздел истории; код исхода передаётся ПАРАМЕТРОМ кода, а не
приклеивается к строке адреса руками (адрес собирает `_with_notice`). Четыре действующих кода
сохраняются дословно и по тем же условиям; новых кодов НЕ заводится. Отказ статусом и ветка
«нет сессии» — первая остаётся как есть (это не редирект), вторая переводится на слой ответа.
Докстринг обработчика пополняется одной строкой: серверное удержание, о котором он говорит,
стои́т ДО развилки транспорта и переживает обе ветки — фаза меняет форму ответа, а не предикат.

**2. `admin_restart_worker`.** То же: адрес деградации — переменная раздела воркеров, уже
собранная в обработчике один раз; два действующих кода передаются параметром. Ветки «аккаунт
неизвестен» и «успех» отвечают без кода. Записи в журнал не трогаются: они предмет D-24, а не
транспорта.

**3. `admin_drop_task` — ИМЕННО НАОБОРОТ (D-07).** Ключ исхода очереди остаётся ЧАСТЬЮ строки
адреса перехода, а код реестра НЕ передаётся. Причина пишется комментарием прямо в обработчике:
этот ключ — ШЕСТОЙ частный микро-контракт адресной строки, уцелевший после свода Фазы 8; в
пятёрку, названную сводом, он не входил, свод отдан Фазе 11 разделу администрирования, а Фаза
10 остаётся рычагом, а не уборкой канала уведомлений. Комментарий обязан отдельно сказать, что
ключ не считается сведённым — иначе следующий читатель примет умолчание за завершённую работу.
Все три точки выхода переводятся, закрытый словарь исходов не трогается.

**4. `admin_delete_user`.** Четыре точки выхода на слой ответа: список пользователей для
успеха и для «пользователь не найден», карточка пользователя для «нельзя удалить себя», отказ
статусом остаётся. Кодов исхода этот маршрут сегодня не выдаёт, и новых не заводится.

**5. Обход.** Четыре записи добавляются в перечень маршрутов подтверждения; каждая несёт свою
фикстуру клиента (администратор для трёх админских, обычный пользователь для повтора) и свой
ожидаемый адрес приземления. Записи с кодами исхода добавляются ПО РАЗУ НА КОД: маршрут,
выдающий четыре кода, даёт четыре случая, а не один, — иначе обход утверждал бы транспорт
только на одной из веток. Число `CONFIRMED_DELETE_ROUTES_DECLARED` двигается прогоном, а не
сложением в уме, и его летопись называет добавленное СЛОВАМИ.

**6. Летопись `NOT_YET_CONVERTED_COUNT`** пополняется строкой 31 → 27 с названием четырёх
обработчиков СЛОВАМИ («повтор отправки из журнала», «перезапуск контейнера воркера», «снятие
задачи из очереди отправки», «удаление пользователя из его карточки»).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_history_retry.py tests/test_pages/test_admin_panel.py tests/test_pages/test_htmx_gates.py -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_confirm_delete_transport.py -x` — зелено; обход накрывает шесть маршрутов и все их коды исхода.
- `awk '/^async def history_retry/,/^@router/' app/pages/history.py | grep -c 'RedirectResponse'` возвращает `0`.
- `awk '/^async def admin_restart_worker/,/^@router/' app/pages/admin.py | grep -c 'RedirectResponse'` возвращает `0`; то же для `admin_drop_task` и `admin_delete_user`.
- Ключ исхода очереди остался частью строки адреса и кодом реестра не стал: `awk '/^async def admin_drop_task/,/^@router/' app/pages/admin.py | grep -c 'notice='` возвращает `0`.
- Новых кодов реестра не заведено: `grep -c '^[A-Z_]* = "' app/pages/notices.py` совпадает со значением на предыдущем коммите (`git show HEAD~1:app/pages/notices.py | grep -c '^[A-Z_]* = "'`).
- `grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'NOT_YET_CONVERTED_COUNT = 27'` возвращает `1`.
- `uv run pytest tests/test_pages/test_admin_panel.py -k "drop_button_and_its_confirmation_panel_still_match" -x` — зелено: соответствие кнопки и панели очереди не задето.
- `uv run pytest tests/test_pages/test_history_retry.py tests/test_pages/test_admin_users.py tests/test_pages/test_impersonation_gate.py -q` — зелено: предикаты отказа не тронуты.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Четыре маршрута отвечают через слой ответа на обоих транспортах; четыре действующих кода
реестра и два кода раздела воркеров выдаются там же, где выдавались; ключ исхода очереди
остался частью строки адреса и назван шестым микро-контрактом с адресованной фазой; новых кодов
не заведено; собственных редиректов у четырёх обработчиков не осталось.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 3: Вход под пользователем — cookie личности переезжает на ответ 204</name>

  <files>
app/pages/admin.py,
tests/test_pages/test_impersonation.py,
tests/test_pages/test_confirm_delete_transport.py,
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `app/pages/admin.py:1670-1770` — `admin_impersonate` ЦЕЛИКОМ: отказ статусом при отсутствии
  прав, две ветки «не найден» / «нельзя», создание токена, `:1752-1756` — комментарий
  «ЕДИНСТВЕННАЯ ФУНКЦИЯ УСТАНОВКИ» и сама связка «собрать редирект → навесить cookie на него»,
  `:1757-1769` — обязательный след в журнале с обоими идентификаторами, `:1770` — возврат
  ТОГО ЖЕ объекта, на который навешена cookie.
- `app/pages/auth.py:505-520` — `impersonation_stop`: та же связка «редирект + установка
  cookie на возвращаемый объект». ⚠️ ИЗМЕРЕНО И ЗАПИСАНО: этот маршрут сегодня заголовка
  перехода НЕ отдаёт — прецедент вехи записан, но В КОДЕ его нет. Читать ради связки, а не
  ради заголовка; сам маршрут этой фазой НЕ трогается.
- `app/pages/htmx.py:140-146` — `location_response` собирает НОВЫЙ ответ со статусом 204:
  cookie, навешенная на выброшенный редирект, не уедет никуда.
- `app/services/auth.py` (или модуль, где объявлена `set_session_cookie`) — единственная
  функция установки, её набор атрибутов и то, что она принимает ответ первым аргументом.
- `tests/test_pages/test_impersonation.py` — действующие утверждения входа под пользователем и
  помощник `_enter`, который УДОСТОВЕРЯЕТСЯ, что вход состоялся; `IMPERSONATION_REFUSAL_MARK`
  и `_detail_of`, импортируемые слоем ответа.
- `tests/test_pages/test_htmx_response_layer.py:275-335` — образец пары для действия под чужой
  личностью на обоих транспортах.
- `10-CONTEXT.md` D-05 целиком и §Landmines, третий пункт: «тест, проверяющий только заголовок
  перехода, останется зелёным при потерянной cookie — утверждать оба факта одним тестом».
- `10-CONTEXT.md` §specifics, последняя строка: «У входа под пользователем пара пишется
  ТРОЙНОЙ».
  </read_first>

  <precondition>
Задача 2 приземлена: `NOT_YET_CONVERTED_COUNT` равен 27, обход накрывает шесть маршрутов.
  </precondition>

  <reversibility rating="costly">
Задача исполняет D-05 — САМУЮ ПРИВИЛЕГИРОВАННУЮ операцию продукта. Ошибка здесь не роняет
запрос, а МОЛЧА не меняет личность: администратор остаётся собой, а экран выглядит так, будто
вход состоялся. Именно поэтому пара утверждений тройная, а не двойная. `one-way` НЕ заслужено:
правка не трогает ни схемы, ни данных, ни выданных токенов, откат — обратная правка одного
обработчика. Ни одно решение фазы не имеет рейтинга `one-way`, чекпойнта решения не ставится.
  </reversibility>

  <behavior>
RED:
1. `NOT_YET_CONVERTED_COUNT` 27 → 26 со снятием ключа входа под пользователем — последнее
   движение счётчика в фазе, суммарно 34 → 26.
2. `CONFIRMED_DELETE_ROUTES_DECLARED` 6 → 8: вход под пользователем и — записью, а не правкой
   обработчика — единственный фрагментный маршрут фазы (удаление расписания из редактора),
   у которого вторая половина пары ожидает `200` и фрагмент, а не `204`.

`test_impersonation_over_htmx_keeps_both_the_location_and_the_cookie` — ТРОЙНАЯ пара, ОДНИМ
тестом:
- без заголовка htmx → `302`, `location == "/dashboard"`, И в ответе присутствует cookie
  сессии с новым значением;
- с заголовком htmx → `204`, `response.headers["HX-Location"] == "/dashboard"`, тела нет,
  И в ответе присутствует ТА ЖЕ cookie сессии с новым значением;
- следующий запрос ПОД ПОЛУЧЕННОЙ cookie видит ЧУЖУЮ личность — то есть вход действительно
  состоялся, а не только заявлен заголовком.

Третья половина существует потому, что первые две зелены и при cookie, установленной с
негодными атрибутами: пара, не проверившая ДЕЙСТВИЕ под новой cookie, доказывает передачу
заголовка, а не смену личности.

Ветки «пользователь не найден» и «нельзя войти» — обе половины пары, cookie в них НЕ
появляется.
  </behavior>

  <action>
**1. `admin_impersonate`.** Все точки выхода, собиравшие редирект, переводятся на слой ответа:
раздел пользователей для «не найден», карточка пользователя для «нельзя», панель управления
для успеха. Отказ статусом остаётся как есть.

На успешной ветке порядок ОБЯЗАТЕЛЬНО такой: сначала берётся результат вызова слоя ответа, и
ТОЛЬКО ПОТОМ на этот полученный объект навешивается cookie единственной функцией установки.
Причина пишется комментарием прямо здесь, а не оставляется на догадку: ветка перехода собирает
НОВЫЙ ответ, и cookie, навешенная на выброшенный редирект, не уедет никуда — вход молча не
состоится, а тест, проверяющий только заголовок перехода, останется зелёным. Комментарий
`:1752-1756` о единственной функции установки сохраняется и дополняется этой строкой.

Обязательный след в журнале с обоими идентификаторами не трогается ни на символ и остаётся
ПЕРЕД возвратом: он предмет D-24, и порядок «сначала след, потом ответ» сохраняется.

**2. Обход.** Добавляются две записи: вход под пользователем (ожидается 204 и заголовок
перехода) и удаление расписания из редактора объявления (ожидается 200 и фрагмент). Вторая
запись НЕ правит обработчика — она включает единственный фрагментный маршрут фазы в общий
обход, чтобы число маршрутов подтверждения было ПОЛНЫМ: перечень, накрывающий семь из восьми,
молчал бы о восьмом ровно так же, как молчали бы восемь отдельных функций. Признак ожидаемой
формы ответа — поле записи, и обход ветвится по нему, а не по имени маршрута.

Число `CONFIRMED_DELETE_ROUTES_DECLARED` доводится до восьми ПРОГОНОМ покрасневшего правила;
летопись называет обе добавленные записи словами и отдельно фиксирует, что восемь — это ВСЕ
маршруты фазы, и появление девятого без записи краснеет.

**3. Летопись `NOT_YET_CONVERTED_COUNT`** пополняется строкой 27 → 26 с названием обработчика
СЛОВАМИ («вход администратора под личностью пользователя») и СВОДНОЙ строкой фазы: 34 → 26,
восемь обработчиков, названных восемью словосочетаниями. Сводная строка обязана перечислить их
СЛОВАМИ, а не ключами перечня.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_impersonation.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_htmx_gates.py -q</automated>
    <human-check>Ручной UAT фазы (собственное наблюдение к D-05): в браузере войти под пользователем из его карточки. Панель обязана перейти на главную и показать ЧУЖУЮ учётную запись, а не прежнюю; выход из-под личности обязан вернуть администратора. httpx перехода по заголовку не исполняет и доказать этого не может.</human-check>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_impersonation.py -k "impersonation_over_htmx_keeps_both_the_location_and_the_cookie" -x` — зелено; тест утверждает ТРИ факта и падает при удалении любого из них (проверить вручную, временно закомментировав установку cookie, и записать наблюдение в сводку).
- `awk '/^async def admin_impersonate/,/^@router/' app/pages/admin.py | grep -c 'RedirectResponse'` возвращает `0`.
- Установка идёт единственной функцией: `awk '/^async def admin_impersonate/,/^@router/' app/pages/admin.py | grep -c 'set_session_cookie'` возвращает `1`; `awk '/^async def admin_impersonate/,/^@router/' app/pages/admin.py | grep -c '\.set_cookie('` возвращает `0`.
- `grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'NOT_YET_CONVERTED_COUNT = 26'` возвращает `1`; `grep -c 'admin_impersonate' tests/test_pages/test_htmx_gates.py` возвращает `0`.
- `grep -v '^#' tests/test_pages/test_confirm_delete_transport.py | grep -cE 'CONFIRMED_DELETE_ROUTES_DECLARED = 8'` возвращает `1`.
- `uv run pytest tests/test_pages/test_htmx_gates.py -k "own_redirect or backlog" -x` — зелено: ни у одного из восьми обработчиков собственного редиректа не осталось.
- Число мест записи заголовков не двинулось: `grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'HX_HEADER_WRITES = 2'` возвращает `1`; то же для `HX_HEADER_READS = 1`.
- `uv run pytest tests/test_pages/ -q` — зелено, за вычетом известного красного `full-suite-ads-editor-order-pollution`.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- `graphify update .` выполнен после правки кода.
  </acceptance_criteria>

  <done>
Вход под пользователем отвечает переходом на панель управления на обоих транспортах, и cookie
личности ставится на ТОТ объект, который уезжает человеку; тройная пара утверждает заголовок,
cookie и фактическую смену личности одним тестом. Счётчик прогресса вехи опущен 34 → 26 со
сводной летописью, называющей восемь обработчиков словами. Параметризованный обход накрывает
все восемь маршрутов фазы и объявляет их число.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | идентификаторы аккаунта, объявления, записи журнала, воркера, задачи и пользователя приходят сегментами URL и телом формы от недоверенного клиента |
| администратор → чужая учётная запись | вход под пользователем переводит ВСЕ последующие действия администратора в чужую учётную запись; это единственная операция продукта, где действия одного выглядят как действия другого |
| слой ответа → адресная строка браузера | адрес перехода уезжает заголовком и обязан остаться локальным |
| тело очереди → адресная строка | ключ исхода снятия задачи собран из закрытого словаря исходов, а не из тела очереди |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Elevation of Privilege | `admin_impersonate` — cookie личности, потерянная при смене формы ответа | high | mitigate | Установка применяется к РЕЗУЛЬТАТУ вызова слоя ответа, а не к выброшенному редиректу; пара утверждений ТРОЙНАЯ и проверяет фактическую смену личности следующим запросом, а не только заголовок перехода |
| T-10-15 | Repudiation | след входа под пользователем | high | mitigate | Обязательная запись в журнал с ОБОИМИ идентификаторами (D-24) не трогается и остаётся перед возвратом ответа; порядок «сначала след, потом ответ» сохраняется |
| T-10-16 | Tampering | адрес перехода в заголовке на семи маршрутах | high | mitigate | Адрес строит `respond(redirect=…)`; `_local_path` отвергает схему, `//`, `\`, управляющие символы и не-ASCII. Новых мест записи заголовков не заводится — число мест записи подтверждается неподвижным |
| T-10-17 | Information Disclosure | ключ исхода очереди в адресной строке | medium | mitigate | Ключ приходит из ЗАКРЫТОГО словаря исходов страничного модуля, а не из тела очереди; по адресной строке едет КЛЮЧ, а не текст, поэтому владелец ссылки не может напечатать пользователю текст от имени приложения |
| T-10-18 | Elevation of Privilege | отказ по правам на трёх админских маршрутах | high | mitigate | Предикат отказа вычисляется ДО развилки транспорта и фазой не трогается (FOUND-04); ответ отказа остаётся прежним статусом на ОБОИХ транспортах, что утверждается прогоном действующих правил гейта прав |
| T-10-19 | Information Disclosure | удаление чужого или несуществующего аккаунта / объявления через новый транспорт | high | mitigate | Ограничение владельцем в выборке не трогается; ответ чужого и несуществующего идентификатора неотличим от успешного, что утверждается обходом |
| T-10-20 | Denial of Service | повтор отправки, поставленный дважды | medium | mitigate | Серверное удержание PAY-02 стои́т ДО развилки транспорта и переживает обе ветки; код «повтор занят» продолжает выдаваться там же, где выдаётся сегодня |
| T-10-21 | Spoofing | подделка заголовка htmx на привилегированных маршрутах | low | accept | Вердикт доступа вычисляется ДО развилки транспорта (T-08-09): подделанный заголовок меняет только форму ответа, но не право его получить |
| T-10-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не выполняет ни одной установки: ни новой зависимости, ни build-шага, ни вендоренного файла. Разведки у фазы нет по решению роадмапа — предмета аудита легитимности не существует |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/ -q` — зелено после каждой задачи, за вычетом известного
  красного `full-suite-ads-editor-order-pollution`.
- `uv run pytest tests/ -q` — зелено на слиянии волны.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- `graphify update .` после правки кода.
- Ручной UAT фазы: фактическая смена личности после перехода по заголовку — вынесена
  `<human-check>` задачи 3 и `verification: backstop` в `must_haves`.
</verification>

<success_criteria>
1. Семь маршрутов отвечают через слой ответа на обоих транспортах; форма ответа выбрана по
   классу действия, и ни один из семи фрагмента не отдаёт.
2. Изъятие по смещённому курсору объявлено перечнем с обоснованием, названной фазой-снимателем,
   условием снятия и числом; необъявленный список красит правило.
3. Вход под пользователем сохраняет cookie на ответе 204, и это утверждается ТРОЙНОЙ парой
   одним тестом.
4. Ключ исхода очереди не сведён в реестр кодов и назван шестым микро-контрактом с адресованной
   фазой.
5. Новых кодов реестра не заведено; шесть действующих выдаются там же, где выдавались.
6. Ни у одного из восьми обработчиков фазы не осталось собственного редиректа ни в одной ветке.
7. `NOT_YET_CONVERTED_COUNT` 34 → 26 со сводной летописью, называющей восемь обработчиков
   словами; число мест чтения и записи заголовков подтверждено неподвижным.
8. Параметризованный обход накрывает все восемь маршрутов и объявляет их число константой.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-03-SUMMARY.md` when done.

Сводка обязана:
- назвать `NOT_YET_CONVERTED_COUNT` 34 → 26 движением счётчика прогресса вехи и перечислить
  восемь обработчиков СЛОВАМИ;
- привести наблюдение «тройная пара краснеет при снятой установке cookie» — иначе доказательства
  зубов у неё нет;
- назвать ключ исхода очереди НЕ сведённым и передать его Фазе 11 записью, а не умолчанием;
- назвать наблюдение фактической смены личности незакрытым машинно и передать его артефакту
  ручного обхода.
</output>
