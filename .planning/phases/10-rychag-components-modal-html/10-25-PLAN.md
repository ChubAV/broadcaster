---
phase: 10-rychag-components-modal-html
plan: 25
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/pages/common.py
  - tests/test_pages/test_origin_guard_on_destructive_routes.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (поля во frontmatter не заводятся, форма унаследована от
# планов 10-12…10-24): `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  # `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < min_samples 3), а не назначена автором.
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ГАРД ПРОИСХОЖДЕНИЯ НЕ ПРОПУСКАЕТ ЗАПРОС ПО СОВПАДЕНИЮ ДВУХ ОТСУТСТВИЙ: заголовок `Origin: null` (песочница iframe, документ `data:`, часть цепочек межсайтовых переходов) при отсутствующем имени узла у самого запроса больше не читается как совпадение источников"
    - "ОБОСНОВАНИЕ ГЕЙТА ПЕРЕСТАЛО УТВЕРЖДАТЬ ЛОЖНОЕ О СОБСТВЕННОМ ПРОДУКТЕ: признак `samesite` ВЫСТАВЛЕН продуктом явно и в одном месте, и довод гарда переписан с «умолчание не выставлено» на ГЛУБИНУ — один рубеж снимается правкой одной строки набора атрибутов и не различает одноимённый источник иной схемы или иного порта"
    - "ГРАНИЦА ВСЕЛЕННЫХ ОБОИХ ГЕЙТОВ ПОЛНОТЫ НАЗВАНА ЧИСЛОМ, А НЕ СЛОВОМ «ПОЛНОТА»: сколько изменяющих маршрутов страничного слоя они накрывают из скольких — снято ЗАМЕРОМ дерева, а не переписано из отчёта, и стережётся правилом, которое краснеет от расхождения числа с деревом"
    - "У ОДНОГО ПРЕДИКАТА ОДНА ДЕЙСТВУЮЩАЯ ЗАПИСЬ ОБ ОДНОМ ОПРОВЕРГНУТОМ ПЕРЕЧНЕ: три перекрывающихся абзаца об одном и том же снятом перечне потребителей сведены в ОДНУ датированную запись, и это сведение, а не стирание — предмет записи сохранён"
    - "ИСТИННАЯ ГРАНИЦА ГАРДА ОБЪЯВЛЕНА ОДИН РАЗ И РЯДОМ С САМИМ ГАРДОМ: сверка стои́т ПОСЛЕ ПРАВ и ДО ЧТЕНИЯ ЦЕЛЕВОЙ СТРОКИ, а чтение субъекта к этому моменту уже произошло внутри разбора cookie — и это записано ТАМ, откуда четыре места смогут на это сослаться вместо четвёртой копии"
    - statement: "Расширение вселенной серверного рубежа на изменяющие маршруты за пределами подтверждённого удаления и админки есть решение ВЛАДЕЛЬЦА вехи: настоящий план называет границу числом и НЕ расширяет её"
      verification: backstop
  artifacts:
    - "app/pages/common.py — `is_same_origin` с исправленным сравнением по имени узла, с ОДНОЙ действующей записью об опровергнутом перечне потребителей и с именованным каноническим утверждением истинной границы сверки"
    - "app/pages/common.py — абзац `⚠️ ГРАНИЦА ВСЕЛЕННЫХ ОБОИХ ГЕЙТОВ НАЗВАНА ЧИСЛОМ` с двумя замеренными числами и с указанием, что расширение принадлежит владельцу"
    - "tests/test_pages/test_origin_guard_on_destructive_routes.py — исправленное обоснование гейта, правило границы вселенных с объявленными числами и правило безголового сравнения с отрицательным контролем"
  key_links:
    - "каноническое утверждение границы в `common.py` → четыре места вызова гарда: единственное, чем утверждение не разойдётся четырьмя копиями; ломается — правится одна копия из четырёх, и через круг читатель держит в голове два несогласных утверждения об одном порядке (ровно то, что предъявил `WR-01` пятого круга)"
    - "объявленное число вселенной → дерево: единственное, чем слово «полнота» не прочитается как закрытая поверхность; ломается — запись объявляет закрытым то, что закрыто для одного суффикса пути, и следующий круг предъявляет это снова"
    - "сравнение имени узла с явной проверкой на отсутствие → ветвь заголовка: единственное, чем два отсутствия не сложатся в совпадение; ломается — запрос из песочницы проезжает ветвью заголовка вместо объявленной ветви без заголовков, то есть тихо расширяет границу, которую модуль описывает точно"
  prohibitions:
    - statement: "запертые решения владельца (D-01, D-11 и прочие) НЕ ПЕРЕСМАТРИВАЮТСЯ планом: сузить ПРОЧТЕНИЕ записи можно, отменить РЕШЕНИЕ — нет, и единственный прецедент изъятия (D-08) записан владельцем"
      status: flagged-unverified
      verification: none
    - statement: "опровергнутые формулировки НЕ СТИРАЮТСЯ: сведение трёх перекрывающихся абзацев в один сохраняет ПРЕДМЕТ записи и дату, а не удаляет запись о дефекте"
      status: flagged-unverified
      verification: none
    - statement: "вселенная гейта полноты НЕ РАСШИРЯЕТСЯ этим планом: какие изменяющие маршруты несут серверный рубеж — продуктовое решение владельца вехи, и фаза-владелец не назначена"
      status: flagged-unverified
      verification: none
    - statement: "граница гарда «запрос без обоих заголовков пропускается» НЕ ПЕРЕСМАТРИВАЕТСЯ: она есть записанное закрытие прежнего круга, и ревизия пятого круга её намеренно не переоткрывала"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ — ПОЛНАЯ ЛЕТОПИСЬ ПАРТИИ СТОИТ В `10-24-PLAN.md`, раздел
  # `## Разрешение пяти рёбер зонда покрытия`. Здесь предмет плана рёбер `adjacency`/`empty`/
  # `idempotency`/`ordering` не касается; ребро `concurrency` касается ОДНИМ следствием и оно
  # названо: `is_same_origin` есть чистая функция запроса и состояния между вызовами не держит,
  # поэтому параллельные обращения влиять одно на другое не могут.
---

<objective>
`app/pages/common.py` перестаёт утверждать больше, чем исполняет, — по четырём находкам пятого круга
ревизии сразу, потому что все четыре живут в одном предикате и в его обосновании.

Предмет: `IN-01` (два отсутствия складываются в совпадение), `WR-03` (обоснование гейта утверждает
ложное о собственном продукте), `WR-05` (слово «полнота» прочитывается как закрытая поверхность там,
где закрыт один суффикс пути) и `IN-03` (три перекрывающихся абзаца об одном опровергнутом перечне).

Purpose: `WR-01` того же круга показал ЦЕНУ накопления: читатель, держащий в голове три
перекрывающиеся записи об одном снятом перечне, — это ровно тот читатель, который пропустил
четвёртую копию, живущую в соседнем файле. Партия чинит и копию (план 10-32), и накопление, которое
её спрятало (настоящий план), и порядок между ними назван: сначала заводится КАНОНИЧЕСКОЕ место, на
которое четыре площадки смогут сослаться, потом правятся площадки.

Output: один предикат с одним действующим обоснованием, две измеренные величины вселенной и два
новых правила, которые краснеют от расхождения записи с деревом.
</objective>

<reversibility>
  <decision rating="reversible">
Исправление сравнения имён узлов (`IN-01`). Откат — возврат одного выражения. Поведение сужается, а
не расширяется: единственный класс запросов, переставший проезжать, — тот, у которого имя узла
отсутствует С ОБЕИХ сторон.
  </decision>
  <decision rating="reversible">
Правка обоснований и сведение записей. Откат — правка текста.
  </decision>
</reversibility>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-18-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: два отсутствия перестают складываться в совпадение источников (`IN-01`)</name>

  <files>
app/pages/common.py
tests/test_pages/test_origin_guard_on_destructive_routes.py
  </files>

  <read_first>
- `app/pages/common.py:660-760` ЦЕЛИКОМ — докстринг `is_same_origin` и семь строк его тела.
  ⚠️ ЧИТАТЬ ВЕСЬ ДОКСТРИНГ: там объявлены ТРИ ветви (заголовок площадки запроса; заголовок
  источника; НИ ОДНОГО заголовка — пропускается) и записана граница защиты. Правка касается ВТОРОЙ
  ветви и только её.
- `tests/test_pages/test_origin_guard_on_destructive_routes.py:1-45` — шапка модуля, где объявлено,
  чего модуль НЕ доказывает. Новое правило обязано встать РЯДОМ с этой границей, а не поверх неё.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `### IN-01` — предъявленный
  случай и предложенная форма исправления.
  </read_first>

  <behavior>
1. Запрос с заголовком источника, чьё имя узла не разбирается (значение `null`), при отсутствующем
   имени узла у самого запроса совпадением источников НЕ признаётся.
2. Запрос с заголовком источника, чьё имя узла совпадает с именем узла запроса, совпадением
   признаётся по-прежнему.
3. Запрос с заголовком источника, чьё имя узла отличается, отвергается по-прежнему.
4. Запрос БЕЗ обоих заголовков пропускается по-прежнему — объявленная граница не сдвигается.
5. Ветвь заголовка площадки запроса имеет приоритет над ветвью источника по-прежнему.
  </behavior>

  <action>
**(1) RED ПЕРЕД GREEN.** Сначала правило: собрать объект запроса, у которого заголовок источника
несёт значение без разбираемого имени узла, а у самого запроса имя узла отсутствует, и потребовать
от предиката ответа «не совпадает». Прогнать, получить КРАСНЫЙ, привести дословный вывод в сводку.

**(2) Исправить вторую ветвь.** Разобранное имя узла источника сравнивается с именем узла запроса
ТОЛЬКО после явной проверки на его наличие: отсутствие имени узла у источника есть «не совпадает», а
не «сравним отсутствие с отсутствием». Ветви первая и третья не трогаются ни на символ.

**(3) Дописать в докстринг предиката ОДНО предложение** о том, почему сравнение несёт явную проверку
наличия: значение `null` в заголовке источника шлют песочница iframe, документ схемы `data:` и часть
цепочек межсайтовых переходов, и без явной проверки два отсутствия сложились бы в совпадение,
пропустив запрос ветвью ЗАГОЛОВКА вместо объявленной ветви БЕЗ ЗАГОЛОВКОВ. Назвать и достижимость:
за действующим обратным прокси проекта имя узла у запроса есть всегда, поэтому случай не достижим
сегодня — правится ТИХОЕ РАСШИРЕНИЕ границы, которую модуль описывает точно, а не воспроизведённый
отказ.

**(4) Правило и его ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ.** Рядом с правилом пункта (1) поставить проверки на
совпадающее имя узла (пропускается), на отличающееся (отвергается) и на отсутствие обоих заголовков
(пропускается). Без них зелёное правило пункта (1) выполнялось бы и предикатом, отвергающим ВСЁ.

**(5) Чего задача НЕ делает.** Не трогает ветвь без заголовков и её основание. Не трогает обоснований
гейта (задача 2) и записей об опровергнутом перечне (задача 3).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py -q</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — правила не собраны, и зелёный получен пустым множеством</fails_when>
    <automated>uv run pytest tests/test_pages/ -q --deselect tests/test_pages/test_htmx_gates.py::test_every_framework_bounded_input_is_declared_as_a_divergence</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — сужение предиката задело действующие правила страничного слоя, среди которых тринадцать мест вызова гарда. ⚠️ СНЯТО РОВНО ОДНО ПРАВИЛО, И СНЯТО ПО ЗАМЕРУ ПАРАЛЛЕЛИЗМА, А НЕ ПО ОСТОРОЖНОСТИ: `.planning/config.json` несёт `parallelization: true`, настоящий план стои́т в ВОЛНЕ 1 рядом с планом 10-24, и между задачей 1 плана 10-24 (вход `ad_id` маршрута удаления объявления получает границу) и его задачей 3 (запись об этом входе появляется в реестре) правило ПОЛНОТЫ реестра расхождения красно ПО ПОСТРОЕНИЮ У СОСЕДА — окно длиной во всю его задачу 2. Предмет настоящего плана — предикат происхождения; ни одной сигнатуры маршрута он не трогает, поэтому долг реестра принадлежать ему не может. Реестр НЕ ослабляется: предмет снятого узла измеряется следующей командой ЯВНО, и она отличает переходное состояние соседа от поломки реестра</fails_when>
    <automated>uv run python -c "import sys; from tests.test_pages.test_htmx_gates import _validation_refusal_completeness_complaints as complaints, VALIDATION_REFUSAL_DIVERGENCES as declared, _framework_bounded_post_inputs as measure, _pages_sources as sources; scanned=measure(sources()); found=complaints(declared); print('объявлено записей:', len(declared), '| замерено входов:', len(scanned), '| жалоб правила полноты:', len(found)); print(*found, sep='\n'); mine=[f for f in found if 'app/pages/common.py::' in f]; alien=[f for f in found if not f.startswith('НАЙДЕН ЗАМЕРОМ, НО НЕ ОБЪЯВЛЕН')]; sys.exit(0 if declared and scanned and not mine and not alien else 'предмет снятого узла не сводится к переходному состоянию соседа: '+repr({'объявлено': len(declared), 'замерено': len(scanned), 'свои': mine, 'иного вида': alien}))"</automated>
    <fails_when>ненулевой код возврата — и каждая из четырёх причин ДЕТЕРМИНИРОВАНА, то есть не зависит от того, где сейчас идёт сосед. Пустое `объявлено` либо пустое `замерено` — АНТИВАКУУМ: реестр не разобрался либо разбор страничного слоя сломался, и снятый узел зеленел бы пустым множеством. Непустое `свои` — жалоба назвала `app/pages/common.py`, то есть файл НАСТОЯЩЕГО плана вошёл во вселенную реестра: это дефект здесь, а не долг соседа. Непустое `иного вида` — жалоба вида «ОБЪЯВЛЕН, НО ЗАМЕРОМ НЕ НАЙДЕН» либо «ПСЕВДОНИМ РАЗОШЁЛСЯ С ЗАМЕРОМ»: переходное состояние плана 10-24 таких жалоб порождать не может (он множество замеренного только РАСТИТ), поэтому любая из них есть поломка реестра и останавливает исполнение независимо от волны. ⚠️ ОСТАТОЧНАЯ ГОНКА НАЗВАНА, А НЕ ЗАМОЛЧАНА: план 10-24 держит `tests/test_pages/test_htmx_gates.py` в `files_modified`, поэтому побайтовое окно в секунды — сбор файла в момент его записи — снятием узла не закрывается. Правило повтора КОНЕЧНО: команда прогоняется повторно ОДИН раз; если тот же узел красен и после завершения плана 10-24, это дефект, а не гонка</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — исходник не собирается</fails_when>
  </verify>

  <acceptance_criteria>
- КРАСНЫЙ ДО ПРАВКИ НАБЛЮДЁН: дословный вывод прогона правила пункта (1) на дереве ДО правки
  предиката приведён в сводке, и в нём видно, что предикат ответил совпадением.
- Четыре ветви поведения пунктов 1-4 `<behavior>` покрыты правилами, и каждое правило называет свой
  случай именем — прогон с фильтром по имени модуля собирает не менее четырёх правил, и число
  собранных приведено в сводке замером.
- Тело предиката осталось короче десяти строк: правка есть уточнение сравнения, а не второй
  механизм. Проверка — счёт строк тела, приведённый в сводке.
- Докстринг предиката называет достижимость случая ПРЯМО: за действующим обратным прокси случай не
  достижим, и это записано как основание severity, а не как повод не чинить.
  </acceptance_criteria>

  <done>
Предикат отвечает «не совпадает» на паре отсутствий; три прежние ветви ведут себя как прежде; красный
до правки наблюдён и приведён.
  </done>
</task>

<task type="auto">
  <name>Задача 2: обоснование гейта перестаёт утверждать ложное о собственном продукте, а слово «полнота» получает ЧИСЛО (`WR-03`, `WR-05`)</name>

  <files>
tests/test_pages/test_origin_guard_on_destructive_routes.py
app/pages/common.py
  </files>

  <read_first>
- `tests/test_pages/test_origin_guard_on_destructive_routes.py:305-340` — докстринг
  `test_every_destructive_route_checks_the_origin`. ⚠️ ПРЕДМЕТ ПРАВКИ — несущее предложение о том,
  что продукт умолчания «не выставляет и не проверяет».
- `app/pages/auth.py:80-100` — `_session_cookie_attrs`. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: там видно, что набор
  атрибутов cookie объявлен ОДИН раз и служит источником и постановке, и снятию cookie сессии, то
  есть признак есть выставленный продуктом атрибут, а не умолчание браузера.
- `app/pages/common.py:695-760` — абзацы `РАМКИ` и `ГЕЙТОВ ПОЛНОТЫ ТЕПЕРЬ ДВА` докстринга гарда.
  ⚠️ Читать вместе с `tests/test_pages/test_origin_guard_on_destructive_routes.py:33-45`, где
  объявлен критерий второй вселенной (метод и суффикс пути), и с
  `tests/test_pages/test_htmx_gates.py:153`, где стои́т независимо снятое число POST-обработчиков
  страничного слоя.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, разделы `### WR-03` и `### WR-05`
  целиком — включая таблицу маршрутов, оставшихся вне обеих вселенных.
  </read_first>

  <behavior>
1. Обоснование гейта называет признак cookie ВЫСТАВЛЕННЫМ продуктом и указывает место, где он
   выставлен, а довод гарда формулируется через ГЛУБИНУ, а не через отсутствие умолчания.
2. Докстринг гарда называет ЧИСЛОМ, сколько изменяющих маршрутов страничного слоя накрывают обе
   вселенные вместе и сколько их всего.
3. Оба числа СНЯТЫ ЗАМЕРОМ дерева на момент правки, а не переписаны из отчёта ревизии.
4. Новое правило краснеет, если любое из двух чисел разошлось с деревом, и текст его отказа называет
   оба числа.
5. Запись прямо говорит, что это ПРИНЯТОЕ СОСТОЯНИЕ, а не свойство полноты, и что расширение
   вселенной принадлежит владельцу, фаза-владелец которому не назначена.
  </behavior>

  <action>
**(1) Снять ОБА числа замером ДО правки текста.** Разбором дерева `app/pages/*.py`: сколько
обработчиков объявлены маршрутом метода POST (это первое число) и сколько из них зовут гард
происхождения (это второе). ⚠️ ЧИСЛА В ПЛАН ВПИСАНЫ КАК ОРИЕНТИР, А НЕ КАК ЦЕЛЬ ПРАВКИ: планировщик
снял на дереве до исполнения партии тринадцать мест вызова гарда в восьми файлах при тридцати шести
POST-обработчиках. Исполнитель ставит СВОЙ замер и, если он разошёлся с ориентиром, называет
расхождение поимённо — это НАХОДКА, а не помеха. ⚠️ ЧИСЛО POST-ОБРАБОТЧИКОВ СНИМАЕТСЯ СВОИМ
РАЗБОРОМ, а не копируется из соседнего гейта: две независимые меры одного множества, обязанные
совпасть, сильнее одного числа, переписанного из файла в файл.

**(2) Переписать несущее предложение докстринга гейта.** Новая формулировка утверждает ТРИ вещи и
ни одной сверх: признак cookie ВЫСТАВЛЕН продуктом явно и в одном месте (назвать путь и имя
функции); межсайтовый POST он действительно не пропускает; довод гарда есть ГЛУБИНА — одна политика
снимается сменой набора атрибутов в одной строке и не различает одноимённый источник иной схемы или
иного порта, который сам предикат пропускает по записанному решению. ⚠️ ВЫВОД ГЕЙТА НЕ МЕНЯЕТСЯ:
второй рубеж остаётся правильным, менялся ФАКТ, на котором вывод стоял.

**(3) Пометить прежнюю формулировку опровергнутой, а не стереть.** По действующей идиоме проекта:
одной строкой с указанием круга, который её опроверг, и с замером, которым опровергнута. Стирание
превратило бы запись о дефекте в её отсутствие.

**(4) Дописать в докстринг гарда абзац о ГРАНИЦЕ ВСЕЛЕННЫХ.** Содержание: оба числа из пункта (1);
что именно накрыто (все изменяющие маршруты админского модуля плюс POST-маршруты с путём на суффикс
подтверждённого удаления); что НЕ накрыто — перечислить классы поимённо (правка объявления, тумблеры
расписания и группы, синхронизация групп, профиль, вход и регистрация); что оставшиеся держатся
одной политикой cookie; и что это ПРИНЯТОЕ СОСТОЯНИЕ ВЕХИ, а не свойство полноты.
⚠️ **ФАЗА-СНИМАТЕЛЬ НЕ СОЧИНЯЕТСЯ.** Форма предложена ревизией с местозаполнителем «отложен фазой N
с основанием X» — местозаполнитель НЕ ЗАПОЛНЯЕТСЯ выдуманной фазой. Записывается прямо: расширение
вселенной есть решение владельца вехи (какие изменяющие маршруты несут серверный рубеж — продуктовый
выбор), фаза-владелец НЕ НАЗНАЧЕНА НАМЕРЕННО, прецедент — окна 45, 47 и 51 реестра.

**(5) Завести правило границы вселенных.** Оно объявляет оба числа константами со своей летописью
(форма — действующие объявленные числа этого же модуля), меряет дерево и краснеет с текстом,
называющим ОБА числа и разошедшееся множество поимённо. ⚠️ ОБЯЗАТЕЛЕН АНТИВАКУУМ: правило падает
и в том случае, когда измеренное множество POST-обработчиков ПУСТО, — иначе сломавшийся разбор
декораторов зеленел бы навсегда.

**(6) Показать зубы правила МУТАЦИЕЙ.** Сдвинуть одно из объявленных чисел на единицу, прогнать,
привести КРАСНЫЙ в сводку, вернуть число.

**(7) Чего задача НЕ делает.** Не расширяет вселенной ни одного гейта. Не снимает и не пересматривает
границы «запрос без обоих заголовков пропускается». Не правит ни одного обработчика.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py -q</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — правила модуля не собраны, и зелёный получен пустым множеством</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -k "post_handlers or handlers"</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — независимая мера того же множества разошлась с новой, то есть одно из двух чисел снято неверно</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — исходник не собирается</fails_when>
  </verify>

  <acceptance_criteria>
- Оба числа в докстринге гарда РАВНЫ замеру дерева, и замер приведён в сводке выводом прогона, а не
  утверждением. Ориентир планировщика (тринадцать из тридцати шести) назван в сводке рядом с
  замером; расхождение, если оно есть, названо поимённо.
- Правило границы вселенных краснеет на мутации объявленного числа: дословный красный вывод приведён
  в сводке, и в нём названы оба числа.
- Правило падает и на ПУСТОМ измеренном множестве — антивакуум проверен подачей пустого набора
  исходников, и вывод приведён в сводке.
- Докстринг гейта называет путь и имя функции, где выставлен набор атрибутов cookie. Проверка:
  `grep -c "_session_cookie_attrs" tests/test_pages/test_origin_guard_on_destructive_routes.py`
  даёт значение не меньше `1`.
- Абзац о границе вселенных содержит слова о том, что фаза-сниматель не назначена, и НЕ содержит
  имени какой-либо фазы в роли снимателя. Проверка — чтением, и результат чтения назван в сводке
  прямо: какая фаза упомянута и в какой роли.
- Прежняя формулировка о невыставленном умолчании ПОМЕЧЕНА опровергнутой с указанием круга, а не
  удалена; в сводке приведено, сколько строк дифф удалил из докстринга гейта и почему каждая.
  </acceptance_criteria>

  <done>
Обоснование гейта стои́т на измеренном факте, а не на опровергнутом; граница вселенных названа двумя
замеренными числами и стережётся правилом, показавшим красный на мутации и на пустом множестве.
  </done>
</task>

<task type="auto">
  <name>Задача 3: у одного опровергнутого перечня — ОДНА действующая запись, и рядом с гардом встаёт каноническое утверждение его истинной границы (`IN-03`, канон для `WR-02`)</name>

  <files>
app/pages/common.py
  </files>

  <read_first>
- `app/pages/common.py:666-760` ЦЕЛИКОМ — докстринг `is_same_origin` со всеми абзацами.
  ⚠️ ЧИТАТЬ ЦЕЛИКОМ И СЧИТАТЬ: предмет задачи — ТРИ абзаца об ОДНОМ и том же снятом перечне
  потребителей, два из которых говорят одно разными словами. Перед правкой выписать в сводку, какие
  именно абзацы найдены, их первые строки и их предмет — сведение вслепую потеряло бы запись, чей
  предмет отличается.
- `app/pages/accounts.py:1020-1035` — образец заголовка сверки на площадке вызова. ⚠️ ЧИТАТЬ:
  каноническое утверждение задачи заводится ровно затем, чтобы четыре такие площадки план 10-32
  перевёл на ССЫЛКУ, а не на четвёртую копию.
- `app/pages/common.py:565-585` — `get_user_from_cookie` и обращения к базе внутри него.
  ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: именно они делают ложным написанное на площадках утверждение «до любого
  обращения к базе», и именно их надо назвать в каноническом утверждении поимённо.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, разделы `### WR-02` и `### IN-03`.
  </read_first>

  <behavior>
1. В докстринге предиката остаётся ОДНА действующая запись об опровергнутом перечне потребителей —
   датированная, с указанием круга и плана, которым перечень снят.
2. Записи, чей предмет ОТЛИЧАЕТСЯ от снятого перечня, не сводятся и не трогаются.
3. Рядом с предикатом стои́т ИМЕНОВАННОЕ каноническое утверждение истинной границы сверки: она стои́т
   после прав и ДО ЧТЕНИЯ ЦЕЛЕВОЙ СТРОКИ, а чтение субъекта (и действующего лица) к этому моменту
   уже произошло внутри разбора cookie.
4. Каноническое утверждение объявлено так, что на него можно СОСЛАТЬСЯ из площадки вызова одной
   строкой: у него есть имя, а не только место.
5. Число строк докстринга предиката уменьшилось, а число НАЗВАННЫХ в нём предметов не уменьшилось.
  </behavior>

  <action>
**(1) Инвентаризовать перед сведением.** Выписать все абзацы докстринга, чей предмет — снятый
перечень потребителей гарда: первую строку каждого и его предмет одной фразой. Инвентарь привести в
сводку. Сводить можно ТОЛЬКО абзацы, у которых предмет совпал; абзац с иным предметом остаётся
нетронутым, и это называется в сводке отдельно.

**(2) Свести совпавшие в ОДНУ датированную запись.** Она обязана сохранить: (а) что перечень
потребителей здесь больше не ведётся; (б) ПОЧЕМУ — перечень, который надо не забыть исправить,
забывают; (в) чем полнота держится вместо перечня — двумя машинными гейтами, названными по имени;
(г) дату и круг, на котором перечень снят. Пункты (а)…(г) есть ПРЕДМЕТ записи, и сведение, потерявшее
любой из них, есть стирание, а не сведение.

**(3) Завести каноническое утверждение границы сверки** — именованной строковой константой уровня
модуля рядом с предикатом, а не абзацем внутри докстринга: площадка вызова должна иметь возможность
сослаться на ИМЯ. Содержание утверждения: сверка стои́т после прав и ДО ЧТЕНИЯ ЦЕЛЕВОЙ СТРОКИ; чтение
субъекта и действующего лица к этому моменту уже произошло внутри разбора cookie (назвать функцию и
оба обращения); отказ по происхождению не имеет права стать признаком существования строки.
⚠️ ФОРМУЛИРОВКА «ДО ЛЮБОГО ОБРАЩЕНИЯ К БАЗЕ» В КАНОН НЕ ПОПАДАЕТ: она и есть опровергнутое
утверждение, а канон существует затем, чтобы четыре площадки перестали его повторять.

**(4) Дописать к докстрингу предиката ОДНУ строку**, называющую имя канонической константы: читатель,
пришедший за порядком проверок, находит его с первой ссылки.

**(5) Замерить результат.** Снять число строк докстринга предиката ДО и ПОСЛЕ и назвать оба в сводке;
снять число НАЗВАННЫХ предметов ДО и ПОСЛЕ (по инвентарю пункта (1)) и назвать оба. Второе число
уменьшиться не имеет права — это и есть отличие сведения от стирания.

**(6) Чего задача НЕ делает.** Не правит ни одной площадки вызова — их переводит на ссылку план
10-32 в волне 4, и порядок этот назван: канон обязан существовать РАНЬШЕ, чем на него сошлются. Не
трогает `_ad_id_from_form` — его докстринг правит план 10-32, ЕГО ЗАДАЧА 3 (и она же сводит две
записи об опровергнутом): владелец назван поимённо, потому что указатель на чужого владельца стои́т
ровно того же, что и число, разошедшееся с деревом. Не трогает тела предиката (задача 1).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка докстринга задела правило, читающее исходник страничного слоя</fails_when>
    <automated>uv run python -c "import app.pages.common as m; names=[n for n in vars(m) if 'ORIGIN' in n.upper() and isinstance(getattr(m,n),str)]; print(names); assert names, 'именованного канонического утверждения границы сверки в модуле нет'"</automated>
    <fails_when>ненулевой код возврата либо пустой список в выводе — канон заведён абзацем внутри докстринга, а не именем, и сослаться на него из площадки вызова нечем</fails_when>
    <automated>uv run pytest tests/test_pages/ -q --deselect tests/test_pages/test_htmx_gates.py::test_every_framework_bounded_input_is_declared_as_a_divergence</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — правка задела правила страничного слоя за пределами гарда. ⚠️ СНЯТО РОВНО ОДНО ПРАВИЛО, И ОСНОВАНИЕ ТО ЖЕ, ЧТО У ЗАДАЧИ 1, СНЯТОЕ ЗАМЕРОМ: `parallelization: true`, волна 1, сосед 10-24 между своей задачей 1 и своей задачей 3 держит правило полноты реестра красным ПО ПОСТРОЕНИЮ. Настоящая задача правит ОДИН докстринг и заводит ОДНУ константу — сигнатур маршрутов не касается вовсе, поэтому долг реестра ей принадлежать не может. Предмет снятого узла измеряется следующей командой ЯВНО</fails_when>
    <automated>uv run python -c "import sys; from tests.test_pages.test_htmx_gates import _validation_refusal_completeness_complaints as complaints, VALIDATION_REFUSAL_DIVERGENCES as declared, _framework_bounded_post_inputs as measure, _pages_sources as sources; scanned=measure(sources()); found=complaints(declared); print('объявлено записей:', len(declared), '| замерено входов:', len(scanned), '| жалоб правила полноты:', len(found)); print(*found, sep='\n'); mine=[f for f in found if 'app/pages/common.py::' in f]; alien=[f for f in found if not f.startswith('НАЙДЕН ЗАМЕРОМ, НО НЕ ОБЪЯВЛЕН')]; sys.exit(0 if declared and scanned and not mine and not alien else 'предмет снятого узла не сводится к переходному состоянию соседа: '+repr({'объявлено': len(declared), 'замерено': len(scanned), 'свои': mine, 'иного вида': alien}))"</automated>
    <fails_when>ненулевой код возврата — четыре ДЕТЕРМИНИРОВАННЫЕ причины, ни одна из которых не зависит от места соседа. Пустое `объявлено` либо пустое `замерено` — АНТИВАКУУМ: снятый узел зеленел бы пустым множеством, а именно этим он и опасен. Непустое `свои` — жалоба назвала `app/pages/common.py`: заведённая настоящей задачей константа попала во вселенную реестра, и это дефект ЗДЕСЬ. Непустое `иного вида` — «ОБЪЯВЛЕН, НО ЗАМЕРОМ НЕ НАЙДЕН» либо «ПСЕВДОНИМ РАЗОШЁЛСЯ С ЗАМЕРОМ»: сосед такого породить не может, значит реестр сломан, и прогон останавливается независимо от волны. ⚠️ ОСТАТОЧНАЯ ГОНКА ТА ЖЕ, ЧТО У ЗАДАЧИ 1, И ПРАВИЛО ПОВТОРА ТО ЖЕ И КОНЕЧНОЕ: один повтор; красное после завершения плана 10-24 есть дефект, а не гонка</fails_when>
  </verify>

  <acceptance_criteria>
- Инвентарь абзацев ДО сведения приведён в сводке: первая строка каждого и его предмет одной фразой.
- Число НАЗВАННЫХ предметов после сведения не меньше, чем до: оба числа приведены в сводке замером.
- Число строк докстринга предиката после сведения МЕНЬШЕ, чем до: оба числа приведены в сводке
  замером (`uv run python -c "..."` либо `sed -n` по диапазону — способ назван в сводке).
- Каноническое утверждение доступно по ИМЕНИ из модуля: прогон проверки выше печатает непустой
  список имён.
- Каноническое утверждение называет функцию разбора cookie и ОБА её обращения к базе — читатель
  видит, ЧТО именно уже произошло к моменту сверки.
- Абзац, чей предмет отличается от снятого перечня, назван в сводке отдельно и не тронут: строки его
  в диффе отсутствуют.
  </acceptance_criteria>

  <done>
Один опровергнутый перечень описан одной датированной записью; предметы не потеряны; каноническое
утверждение истинной границы сверки объявлено именем и доступно площадкам вызова.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| сторонний сайт → страничный слой | межсайтовый POST с приложенной браузером cookie неотличим от своего |
| песочница iframe / документ схемы `data:` → страничный слой | заголовок источника несёт значение без разбираемого имени узла |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-25-01 | Spoofing | `app/pages/common.py::is_same_origin`, ветвь заголовка источника | low | mitigate | Сравнение имён узлов несёт явную проверку наличия: отсутствие имени у источника есть «не совпадает». Достижимость за действующим обратным прокси нулевая (имя узла у запроса есть всегда) — отсюда severity; чинится тихое расширение границы, а не воспроизведённый отказ (задача 1) |
| T-10-25-02 | Repudiation | обоснование гейта подтверждённого удаления | medium | mitigate | Обоснование стои́т на опровергнутом факте о собственном продукте, из-за чего читатель переоценивает, что даёт гард. Правится ФАКТ, вывод (глубина) остаётся; прежняя формулировка помечается опровергнутой, а не стирается (задача 2) |
| T-10-25-03 | Spoofing | двадцать три изменяющих маршрута страничного слоя вне обеих вселенных | medium | mitigate | ⚠️ МИТИГАЦИЯ ЕСТЬ ВИДИМОСТЬ, А НЕ ПОКРЫТИЕ, И ЭТО НАЗВАНО. Граница вселенных объявляется ДВУМЯ ЗАМЕРЕННЫМИ ЧИСЛАМИ и стережётся правилом; классы непокрытых маршрутов перечисляются поимённо. Расширение серверного рубежа на них есть решение владельца вехи, фаза-владелец НЕ НАЗНАЧЕНА (прецедент окон 45, 47, 51). Остаток держится политикой cookie, выставленной продуктом явно, и лежит ниже порога блокировки `high` |
| T-10-25-04 | Repudiation | докстринг гарда с тремя перекрывающимися записями об одном снятом перечне | low | mitigate | Три записи сводятся в одну датированную с сохранением всех четырёх предметов. Цена накопления предъявлена тем же кругом: читатель, держащий три перекрытия, пропустил четвёртую копию в соседнем файле (задача 3) |
| T-10-25-SC | Tampering | установки пакетов | low | accept | Установок нет: `pyproject.toml` не в `files_modified`, новых зависимостей не вводится |
</threat_model>

## Артефакты, которые создаёт этот план

- именованная константа канонического утверждения границы сверки в `app/pages/common.py`
- объявленные числа границы вселенных гейтов полноты и их летопись в
  `tests/test_pages/test_origin_guard_on_destructive_routes.py`
- правило границы вселенных с антивакуумом
- правила безголового сравнения источников и три отрицательных контроля к нему

Полный перечень артефактов ПАРТИИ — `10-24-PLAN.md`, раздел `## Артефакты, которые создаёт эта партия`.

## Что этот план НЕ закрывает, и основание у каждого пункта своё

- **Расширение вселенной серверного рубежа на 23 изменяющих маршрута (`WR-05` целиком).**
  Основанием служит ПРИНАДЛЕЖНОСТЬ РЕШЕНИЯ, а не оценка труда: какие изменяющие маршруты продукта
  несут серверный рубеж — продуктовый выбор владельца вехи, а гейт, заведённый планом на маршруты
  входа и регистрации, которые гарда нести не могут, потребовал бы именованного изъятия из решения,
  которого никто не принимал. Прецедент разведения: окно 51, где партия 4 сделала расхождение
  ВИДИМЫМ и не стала чеканить изъятие. Настоящий план делает то же: называет границу числом.
  **Фаза-владелец НЕ НАЗНАЧЕНА, и назначение чужой фазе есть решение вехи, а не плана.**
- **Граница «запрос без обоих заголовков пропускается».** Это ЗАПИСАННОЕ ЗАКРЫТИЕ прежнего круга,
  которое ревизия пятого круга намеренно не переоткрывала («recorded closures … are not
  re-reported»). Переоткрывать закрытое планом — тот же дрейф в другую сторону.
- **Четыре площадки вызова с опровергнутым заголовком (`WR-02` на местах).** Их правит план 10-32
  волны 4, и порядок назван: канон обязан существовать раньше, чем на него сошлются. Разведение —
  не отсрочка: правка площадок раньше канона родила бы пятую копию утверждения.

<verification>
- `uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py -q` — зелено, собрано не
  менее четырёх новых правил.
- `uv run pytest tests/test_pages/ -q --deselect tests/test_pages/test_htmx_gates.py::test_every_framework_bounded_input_is_declared_as_a_divergence`
  — зелено; предмет снятого узла закрыт парным замером задач 1 и 3, и его вывод (`объявлено`,
  `замерено`, `жалоб`) приведён в сводке.
- `uv run python -m compileall -q app main.py tests` — код возврата ноль.
- Три наблюдённых красных приведены в сводке дословно: правило безголового сравнения ДО правки,
  правило границы вселенных на мутации числа, правило границы вселенных на пустом множестве.
</verification>

<success_criteria>
Предикат происхождения не складывает два отсутствия в совпадение; обоснование гейта стои́т на
измеренном факте о собственном продукте; граница вселенных обоих гейтов названа двумя замеренными
числами со стерегущим правилом и с прямым указанием, что расширение принадлежит владельцу; один
опровергнутый перечень описан одной датированной записью, и истинная граница сверки объявлена
именем, на которое смогут сослаться площадки вызова.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-25-SUMMARY.md` по завершении.
</output>
