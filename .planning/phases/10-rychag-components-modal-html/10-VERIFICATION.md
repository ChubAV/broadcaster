---
phase: 10-rychag-components-modal-html
verified: 2026-09-10T08:30:00Z
status: gaps_found
score: 8/14 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/10-rychag-components-modal-html/10-01-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-01-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-02-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-02-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-03-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-03-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-04-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-04-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-05-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-05-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-06-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-06-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-07-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-07-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-08-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-08-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-09-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-09-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-10-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-10-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-11-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-11-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-12-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-12-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-13-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-13-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-14-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-14-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-15-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-15-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-16-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-16-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-17-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-17-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-18-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-18-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-19-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-19-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-20-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-20-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-21-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-21-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-22-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-22-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-23-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-23-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-24-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-24-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-25-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-25-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-26-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-26-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-27-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-27-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-28-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-28-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-29-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-29-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-30-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-30-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-31-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-31-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-32-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-32-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-33-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-33-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-34-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-34-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-35-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-35-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-36-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-36-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-37-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-37-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-38-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-38-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-39-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-39-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-40-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-40-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-CONTEXT.md"
  - ".planning/phases/10-rychag-components-modal-html/10-REVIEW.md"
  - ".planning/phases/10-rychag-components-modal-html/10-UAT.md"
  - ".planning/phases/10-rychag-components-modal-html/deferred-items.md"
  - "app/pages/account_groups.py"
  - "app/pages/accounts.py"
  - "app/pages/admin.py"
  - "app/pages/ads.py"
  - "app/pages/common.py"
  - "app/pages/history.py"
  - "app/pages/htmx.py"
  - "app/pages/identifiers.py"
  - "app/pages/schedules.py"
  - "app/static/css/app.css"
  - "app/templates/account_groups/includes/group_row.html"
  - "app/templates/accounts/list.html"
  - "app/templates/accounts/partial_cards.html"
  - "app/templates/ads/form.html"
  - "app/templates/ads/includes/ad_card.html"
  - "app/templates/ads/includes/sched_card.html"
  - "app/templates/ads/includes/sched_count_rule.html"
  - "app/templates/ads/partials/sched_delete_response.html"
  - "app/templates/components/form_wrapper.html"
  - "app/templates/components/modal.html"
  - "app/templates/history/includes/history_card.html"
  - "app/templates/includes/htmx_error_banner.html"
  - "app/templates/includes/notice_area.html"
  - "tests/conftest.py"
  - "tests/test_pages/test_confirm_delete_transport.py"
  - "tests/test_pages/test_editor_schedules.py"
  - "tests/test_pages/test_history_retry.py"
  - "tests/test_pages/test_htmx_gates.py"
  - "tests/test_pages/test_htmx_response_layer.py"
  - "tests/test_pages/test_hx_location_destinations.py"
  - "tests/test_pages/test_identifier_bounds.py"
  - "tests/test_pages/test_impersonation.py"
  - "tests/test_pages/test_notices_channel.py"
  - "tests/test_pages/test_origin_guard_on_destructive_routes.py"
  - "tests/test_pages/test_shell.py"
  - "tests/test_planning/__init__.py"
  - "tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py"
  - "tests/test_planning/test_requirement_completion_follows_verification.py"
  - "tests/test_planning/test_state_progress_matches_roadmap.py"
  - "tests/test_planning/test_the_walkthrough_cannot_self_certify.py"
  - "tests/test_templates/test_components.py"
  - "tests/test_templates/test_htmx_inventory.py"
  - "tests/test_templates/test_htmx_markup_gates.py"
  - "tests/test_templates/test_walkthrough_anchors.py"
behavior_unverified: 1
overrides_applied: 1
covered_digest: "v1:sha256:ca6a4f4c1504fb4a1bfb858a01186080469307a44578c0ab6ce1bc894ed57822"
re_verification:
  previous_status: human_needed
  previous_score: 7/8
  previous_verified: 2026-09-08T21:55:00Z
  round: 9
  gaps_closed:
    - >-
      ⚠️ САМОЕ КРУПНОЕ ИЗМЕНЕНИЕ КРУГА, И ОНО НЕ ИЗ ПАРТИИ 10-35…10-40: ВТОРАЯ
      ПОЛОВИНА ЦЕЛИ ФАЗЫ ВПЕРВЫЕ НАБЛЮДЕНА В НАСТОЯЩЕМ БРАУЗЕРЕ. Пять кругов
      подряд отчёт писал «не наблюдалась НИКЕМ И НИ РАЗУ». Обход `10-UAT.md`
      состоялся 2026-09-09 (Chrome/Linux, прод, ad_id=50, sched-139/140/141), и
      отметка проверки 1 несёт ЧИСЛА, а не слово «сошлось»: `[role="dialog"]`
      4 → 3, несущий счёт `[id^="sched-del-"][role="dialog"]` 3 → 2 (оба упали
      РОВНО на единицу), `getElementById('sched-139')` и
      `getElementById('sched-del-139')` — ОБА `null`, 18 нажатий Tab / 17
      перемещений фокуса / заходов в диалог **0**, на ветви перехода `1` и `0`
      при живом `window.__uat10`. Положительный контроль ловушки снят отдельно
      (проверка 5: в ЖИВОЙ панели Tab держит фокус внутри `sched-del-142`) — то
      есть «ловушки нет» отличимо от «Tab не работает».
    - >-
      `CR-03`, БРАУЗЕРНАЯ ПОЛОВИНА, ТОЖЕ НАБЛЮДЕНА: шаг 1.13 — ранней ошибки
      языка (`SyntaxError: Identifier 'IMAGE_BASE_URL' has already been
      declared`) в консоли пришедшего экрана НЕТ; шаг 1.14 — `#text-counter`
      30→31→32→33→34, на КАЖДЫЙ символ. Шаг 1.15 объявлен НЕПРИМЕНИМЫМ с
      названной причиной (вложение на проде не заводилось: S3-остаток вне
      досягаемости команды уборки), а не пропущен молча.
    - >-
      ПРОЧТЕНИЕ D-13 КРИТЕРИЯ 3 ПЕРЕСТАЛО ЖИТЬ ПРОЗОЙ: пункт 8 перечня
      `human_verification` прежнего круга — единственный, который был не
      проверкой стенда, а решением владельца, — закрыт записью `overrides[0]`
      ниже (план 10-40, ветвь `accept-reading`). Настоящий круг эту запись НЕ
      ПЕРЕОТКРЫВАЕТ и НЕ ПЕРЕСУЖИВАЕТ; она перенесена ДОСЛОВНО.
  gaps_remaining: []
  regressions:
    - >-
      ⚠️ РЕГРЕССИЯ ОХВАТА, ВНЕСЁННАЯ САМОЙ ПАРТИЕЙ И ВОСПРОИЗВЕДЁННАЯ
      ВЕРИФИКАТОРОМ ИСПОЛНЕНИЕМ: план 10-37 заменил подстрочное сличение простой
      части селектора (`ancestor in last`) строгим (`last in selectors`) — и
      снял с гейта чистоты цепи предков класс, который прежняя форма ЛОВИЛА.
      Замер верификатора на боевом дереве: `_selector_targets('body.night')`
      → `()`, `_selector_targets(':root.dark')` → `()`,
      `_selector_targets('[data-shell].collapsed')` → `()`,
      `_selector_targets('[data-body].wide')` → `()`. Любой квалификатор на
      селекторе предка прячет его от гейта; тема оформления (`:root.dark`,
      `body.night`) — самая ходовая правка ровно этих двух селекторов.
  corrections:
    - >-
      ⚠️ ИСТИНА 2 ПРЕЖНЕГО КРУГА ПЕРЕВЕДЕНА ИЗ `PRESENT_BEHAVIOR_UNVERIFIED` В
      `VERIFIED` НЕ ПО СВОДКЕ, А ПО ОТМЕТКЕ ОБХОДА С ЧИСЛАМИ. ЦЕНА ПЕРЕВОДА
      НАЗВАНА ПРЯМО И НЕ СГЛАЖИВАЕТСЯ: наблюдал АГЕНТ через chrome-devtools MCP,
      а не человек на приёмке; ключ `workflow.live_dom_uat` равен `false`, и
      право драйвить браузер дано репликой в сессии — отступление названо самим
      артефактом, а не вскрыто здесь. Сам `10-UAT.md` терминальным себя НЕ
      объявляет (`status: partial`). Подпись человека на приёмке остаётся
      НЕЗАКРЫТЫМ пунктом перечня `human_verification` — но ПОВЕДЕНИЕ наблюдено,
      и держать истину в `behavior_unverified` шестой круг подряд значило бы
      сказать о дереве неправду.
overrides:
  - must_have: >-
      Панель закрывается существующим Alpine (`x-on:htmx:after-request`) — новых строк JS
      фаза не добавляет (машинно: диффом по `app/static/js/` и шаблонам).
    reason: >-
      ⚠️ ПРОЧТЕНИЕ D-13, ЗАПИСАННОЕ В `10-CONTEXT.md:197-210` ДО ПРАВКИ, А НЕ ЗАДНИМ
      ЧИСЛОМ: «критерий запрещает НОВЫЕ СУЩНОСТИ, а не правку существующих». Буквальное
      прочтение отвергнуто там же и не по удобству — оно запретило бы починку теми же
      критериями, которые эту починку просят (критерий 2 той же фазы и приземление
      фокуса); амендмент формулировки в `ROADMAP.md` отвергнут тем же решением («документ
      вехи держится фиксированным, а прочтение — предмет CONTEXT.md»), и настоящая запись
      работает с ПРОЧТЕНИЕМ, а не с текстом критерия. ЗАМЕР 1 (`git diff 7c1041d..HEAD --
      app/static/js/`, HEAD `c45d9c5`): ПУСТ — ни файла, ни строки. ЗАМЕР 2 —
      ПЕРЕЗАМЕРЕННЫЙ НА HEAD ПОЛНЫЙ ПЕРЕЧЕНЬ строк JS, дописанных фазой в шаблоны: 51
      строка в ДВУХ файлах. `app/templates/ads/form.html` — 38 строк, из них ИСПОЛНЯЕМЫХ
      ДВЕ (`:308` `(function () {` и `:538` `})();` — обёртка немедленно вызываемым
      выражением, план 10-13, чинит регрессию `CR-03`, заведённую этой же фазой) и 36
      строк комментария (`:309-342`, `:539-540`); тело скрипта не правлено ни на символ.
      `app/templates/components/modal.html` — 13 строк выражений Alpine внутри
      СУЩЕСТВУЮЩЕГО узла `x-data` и СУЩЕСТВУЮЩИХ атрибутов события: `:699-707` (тело
      `show()` со снятием обеих заготовок плашки отказа — план 10-35, чинит `CR-01` — и
      тела `hide()`, `destroy()`, `land()`), `:713`, `:716`, `:777`, `:782`. Ни одного
      файла в `app/static/js/` и ни одного узла `<script>` фаза не завела. ЗАМЕР 3 —
      четыре числа сущностей на HEAD, найдено/объявлено: файлов в каталоге сценариев 2/2
      (`alpine.min.js`, `htmx.min.js`); шаблонов со встроенным обработчиком отправки
      (`onsubmit`) 1/1 (`accounts/connect_max.html`); файлов с атрибутом события рантайма
      разметки (`hx-on`) 0/0; узлов клиентского состояния (`x-data`) 24/24. Гейт
      `test_criterion_three_holds_by_the_numbers` (`tests/test_templates/test_components.py`)
      на HEAD зелен, отбор непуст (`1 passed, 83 deselected`), и при каждом из четырёх
      утверждений стои́т свой отрицательный контроль.
    accepted_by: "chubav"
    accepted_by_provenance: >-
      ИМЯ И ДАТА ПРИШЛИ ОТВЕТОМ ВЛАДЕЛЬЦА НА БЛОКИРУЮЩИЙ ОСТАНОВ задачи 1 плана 10-40, а
      не сняты `git config user.name` и заглушкой не подставлены: провизорной эта
      атрибуция не является ни одного круга — в отличие от истории записи Фазы 9, где имя
      два круга подряд стояло провизорным. ⚠️ ДАТА — ТА, КОТОРУЮ НАЗВАЛ ВЛАДЕЛЕЦ
      (`2026-09-09`), и она НЕ ПРАВИТСЯ ПОД ДАТУ ВНЕСЕНИЯ ЗАПИСИ (`2026-09-10`): запись
      принявшего есть запись того, что сказал он, а не того, когда исполнитель дошёл до
      файла. Расхождение названо здесь прямо, а не сглажено.
    accepted_at: "2026-09-09"
    accepted_branch: "accept-reading"
    accepted_branches_rejected: ["reject-reading"]
    covers: >-
      ПРИНИМАЕТСЯ ВЕСЬ ПЕРЕЗАМЕРЕННЫЙ ПЕРЕЧЕНЬ ЗАМЕРА 2 — все 51 строка JS в
      `app/templates/ads/form.html` и `app/templates/components/modal.html`, — А НЕ ТОЛЬКО
      ДВЕ СТРОКИ ОБЁРТКИ, НАЗВАННЫЕ ПЯТЫМ КРУГОМ: охват подтверждён владельцем ПРЯМО при
      ответе на останов задачи 1 плана 10-40. Граница отступления — ровно та, которую
      принуждает гейт четырёх чисел: правка ТЕЛА метода уже существующего объекта
      клиентского состояния и правка выражения УЖЕ СУЩЕСТВУЮЩЕГО атрибута события.
      Заведение НОВОЙ сущности — файла сценария, встроенного обработчика отправки,
      атрибута события рантайма разметки или узла `x-data` — настоящей записью НЕ
      принимается и краснит гейт. ⚠️ ЦЕНА НАЗВАНА ПРЯМО И НЕ СМЯГЧАЕТСЯ: граница «новая
      сущность против правки существующей» становится действующим ПРАВИЛОМ ПРОЕКТА, а не
      разовым разрешением, и всякая следующая правка JS в шаблонах будет опираться на
      настоящую запись как на прецедент. Гейт четырёх чисел принуждает границу ТОЛЬКО в
      четырёх измерениях; пятое, если оно появится, прочтение не закроет и не заметит.
      Формулировка критерия в `ROADMAP.md` остаётся буквальной — то есть документ вехи и
      отчёт фазы говорят разное, и согласует их только эта запись. Код настоящим решением
      НЕ ПРАВИТСЯ ни строкой, и ветвь `reject-reading` его тоже не откатывала бы: откат
      вернул бы `CR-03` и `CR-01`.
gaps:
  - gap_id: V9-01
    truth: >-
      `must_haves.truths[3]` плана 10-35: «правило по исходнику рычага ТРЕБУЕТ
      СНЯТИЯ ОБЕИХ заготовок по перечню `FAILURE_BANNER_IDS`»
    status: failed
    reason: >-
      ВОСПРОИЗВЕДЕНО ИСПОЛНЕНИЕМ ВЕРИФИКАТОРА, а не принято по `10-REVIEW.md`.
      `_lever_clearing_findings` ведёт ДВА НЕЗАВИСИМЫХ подстрочных поиска по телу
      `show()` (имя заготовки — отдельно, глагол `setAttribute('hidden'` —
      отдельно) и конъюнкцию «глагол применён К ЭТОЙ заготовке» не выражает.
      Матрица мутаций верификатора на доктóренной копии рычага (боевой файл
      открывался только на чтение):
      (A) снятие вырезано целиком, то есть ПРЕЖНЕЕ поведение → 2 находки,
      КРАСНО — эта половина истины держится;
      (B) обе заготовки НАЗВАНЫ в живом коде, глагол применён к `#notice` →
      **0 находок, ЗЕЛЕНО** — то есть тело, в котором не снимается НИ ОДНА
      заготовка, правило принимает;
      (C) снимается только одна, вторая названа комментарием → 1 находка, КРАСНО;
      (D) глагол на `#notice`, заготовки в комментарии → 2 находки, КРАСНО.
      Случай (B) и есть предмет: правило требует УПОМИНАНИЯ обеих заготовок и
      ПРИСУТСТВИЯ глагола где угодно, а не снятия обеих.
      ⚠️ ПРОДУКТ НА HEAD ВЕРЕН — это проверено чтением, а не выведено из зелени:
      `components/modal.html:702` применяет `setAttribute('hidden','')` К КАЖДОЙ
      из двух заготовок через `forEach`. Дефект в ЗАЩИТЕ свойства, а не в самом
      свойстве.
      ⚠️ ОТЯГЧАЮЩЕЕ: утверждение о стережении записано ПАРТИЕЙ В ПРОДУКТОВЫЙ
      ФАЙЛ — `app/static/css/app.css:1016-1017` («Свойство стережётся правилом по
      ИСХОДНИКУ рычага в модуле шелла»). Замер показывает, что правило стережёт
      не свойство, а два независимых упоминания. Это ровно класс «запись шире
      дерева», за который фаза получила круги 3, 4, 5, 6 и 8.
    artifacts:
      - path: "tests/test_pages/test_shell.py"
        issue: "`_lever_clearing_findings` (:3381-3425) — два независимых `in body` вместо одного выражения"
      - path: "app/static/css/app.css"
        issue: ":1016-1017 — утверждение о стережении шире того, что правило принуждает"
    missing:
      - "Сличать ПАРУ, а не два вхождения: перечень идентификаторов и глагол снятия обязаны принадлежать ОДНОМУ выражению тела `show()` (приём «оба признака на одном узле» партия уже написала в `tests/test_templates/test_walkthrough_anchors.py:255-257` — урок применён в одном модуле и не применён в другом в тот же день)"
      - "Отрицательный контроль РОВНО на воспроизведённую форму (B): глагол на чужом узле при названных заготовках обязан КРАСНЕТЬ"
      - "Привести утверждение `app.css:1016-1017` к тому, что правило действительно принуждает, — или расширить правило до утверждения (прежний текст не вычёркивается, идиома D-30/D-32)"
  - gap_id: V9-02
    truth: >-
      `must_haves.key_links[2]` плана 10-37: «вселенная предков → цепь до окна:
      единственное, чем гейт смотрит туда же, куда смотрит браузер; ломается —
      правка тела документа отнимает у подъёма окно при обоих зелёных правилах»
    status: failed
    reason: >-
      ИМЕННО НАЗВАННАЯ ПЛАНОМ ПОЛОМКА И ВОСПРОИЗВЕДЕНА, причём собственным
      исполнением верификатора: `body.night { filter: blur(2px); }` — правка ТЕЛА
      ДОКУМЕНТА, отнимающая у подъёма окно ЦЕЛИКОМ, — даёт `_selector_targets`
      → `()` и, следовательно, НОЛЬ находок при обоих зелёных правилах. То же для
      `:root.dark` и `[data-shell].collapsed`. Причина — переход на строгое
      равенство (`last in selectors`), сделанный планом 10-37 ради `WR-07` с
      ВЕРНЫМ основанием (`body` есть подстрока `[data-body]`), но без разбора
      простой части по её ПЕРВОМУ КОМПОНЕНТУ: квалификатор сужает селектор, но
      предка не меняет. Прежняя подстрочная форма все три случая ЛОВИЛА.
      ⚠️ ЭТО НЕ НАЗВАННАЯ ГРАНИЦА: докстринг `_selector_targets` называет ОДНУ
      границу — нечиненую половину `IN-02` про комбинаторы внутри скобок — и о
      цене перехода на равенство не говорит ни слова, а
      `test_the_failure_banner_ancestor_universe_is_addressed_by_stylesheet_selectors`
      утверждает строгость ТОЛЬКО в ту сторону, где она помогает.
      ⚠️ ЭТО РЕГРЕССИЯ ПАРТИИ, А НЕ НАСЛЕДСТВО: файл правлен планами 10-35/37/38
      2026-09-09…10, то есть ПОСЛЕ отметки `verified` прежнего круга.
    artifacts:
      - path: "tests/test_pages/test_shell.py"
        issue: "`_selector_targets` (:3838-3848) — строгое равенство всей простой части вместо её головы"
    missing:
      - "Сличать простую часть ПО ПЕРВОМУ КОМПОНЕНТУ (голова до первого `.`/`:`/`[`/`#`), сохранив строгость, ради которой переход и делался"
      - "Отрицательный контроль на КАЖДЫЙ из трёх воспроизведённых случаев (`body.night`, `:root.dark`, `[data-shell].collapsed` со свойством, порождающим содержащий блок), иначе следующая правка сличения снимет их так же молча"
  - gap_id: V9-03
    truth: >-
      `must_haves.truths[0]` плана 10-36: «все 21 запись реестра якорей
      набираются в консоли» (окно 68 `.planning/WINDOWS.md`, вердикт `open`)
    status: failed
    reason: >-
      ВЗВЕШЕНО, А НЕ ПЕРЕОТКРЫТО: расхождение уже записано ледже́ром как
      `unmet-truth` самим планом 10-36 (`89a9fbc`/`fdaf052`), и восьмой круг
      ревизии его перезамерил — прозаический хвост несут **11** записей, а не 12
      (величина окна завышена на единицу). Здесь оно названо ГЭПОМ, а не
      «известным окном», по одной причине: это `must_haves.truths[0]` плана
      СЕДЬМОЙ партии, то есть невыполненная истина закрытия, а не долг,
      унаследованный фазой.
    artifacts:
      - path: "tests/test_templates/test_walkthrough_anchors.py"
        issue: "поле `selector` 11 записей реестра несёт прозаический хвост; три — заполнитель, две — чистую прозу"
    missing:
      - "Либо привести поле `selector` к набираемому в консоли виду во всех записях, либо СУЗИТЬ формулировку истины плана до того, что реестр действительно принуждает (окно 68 закрывается вместе с этим)"
  - gap_id: V9-04
    truth: >-
      `must_haves.truths[0]` плана 10-38: «описка в имени свойства ВНУТРИ группы
      больше не проходит зелёной» (окно 69 `.planning/WINDOWS.md`, вердикт `open`)
    status: failed
    reason: >-
      ВЗВЕШЕНО ПО ЛЕДЖЕ́РУ: контроль и гейт читают ОДНУ константу, поэтому прогон
      с опиской `isolation` → `isolatiom` оставил весь отбор зелёным. Это
      КРУГОВОЙ контроль в терминах гейта качества тестов
      (`verifier-phase-gates.md`, «Circular test detection»): он доказывает
      согласованность правила с самим собой, а не соответствие правила предмету.
      Гейт называет этот класс блокирующим прямо, и настоящий отчёт с ним не
      спорит.
    artifacts:
      - path: "tests/test_pages/test_shell.py"
        issue: "`test_control_a_trapping_ancestor_reddens` и гейт читают один и тот же перечень имён свойств"
    missing:
      - "Питать контроль ЛИТЕРАЛЬНЫМ именем свойства (или независимым источником), чтобы описка в перечне краснила прогон; владелец состава перечней — план 10-37, и разводить владельцев по одному контролю партия запретила себе сама"
  - gap_id: V9-05
    truth: >-
      `10-SECURITY.md` фазы существует при `workflow.security_enforcement: true`
    status: failed
    reason: >-
      ЗАМЕР ВЕРИФИКАТОРА, А НЕ ЧТЕНИЕ РЕГЛАМЕНТА: `.planning/config.json:48-50` —
      `security_enforcement: true`, `security_asvs_level: 1`,
      `security_block_on: "high"`. `find .planning -name "*SECURITY*"` даёт ДЕВЯТЬ
      файлов — по одному у каждой соседней фазы (07, 08, 09 и все фазы вехи
      v2.0) — и НИ ОДНОГО у Фазы 10. То есть гейт безопасности для этой фазы не
      отработал НИ РАЗУ за девять кругов, и его отсутствие ни один прежний отчёт
      не назвал. ⚠️ ЦЕЛИ ФАЗЫ ЭТОТ ПУНКТ НЕ КАСАЕТСЯ и ни одной из четырёх
      формулировок роадмапа не противоречит — он касается ЗАКРЫТИЯ фазы по
      объявленному самим проектом регламенту. Каждый план партии, кроме того,
      выписал у себя в шапке `security_enforcement → true` замеренным значением.
    artifacts:
      - path: ".planning/phases/10-rychag-components-modal-html/"
        issue: "артефакта `10-SECURITY.md` в каталоге фазы нет"
    missing:
      - "Прогнать `/gsd-secure-phase 10` и получить `10-SECURITY.md` — либо явной записью объявить фазу изъятой из принуждения и назвать основание (изъятие молчанием здесь неотличимо от пропуска)"
  - gap_id: V9-06
    truth: >-
      `must_haves.truths` плана 10-39: «ПОВТОРНОЕ ИСПОЛНЕНИЕ ПРАВОК НЕОТЛИЧИМО ОТ
      ПЕРВОГО» (окно 70 `.planning/WINDOWS.md`, вердикт `open`)
    status: partial
    reason: >-
      ВЗВЕШЕНО ПО ЛЕДЖЕ́РУ: достигнуто ДВУМЯ правками из ТРЁХ, третья названа
      самим планом. Раздел «## План 10-39» внесён в `deferred-items.md`
      дописыванием в конец БЕЗ якоря — второй прогон дал бы ВТОРОЙ такой раздел
      (счёт подразделов вырос бы с 11 до 16). Латентно: план исполняется один
      раз. Названо здесь `partial`, а не `failed`, ровно поэтому.
    artifacts:
      - path: ".planning/phases/10-rychag-components-modal-html/deferred-items.md"
        issue: "у файла нет ни якоря конца перечня, ни правила единственности раздела на план"
    missing:
      - "Якорь конца перечня либо правило единственности раздела на план — владелец у формы файла не назван, и это часть гэпа"
advisory: []
deferred:
  - truth: >-
      Сеанс QR-входа Telegram привязан к пользователю, а подставленный чужой
      `session_id` отвергается (`CR-01` ШЕСТОГО круга ревизии)
    addressed_in: "Phase 13"
    evidence: >-
      Критерий 3 Фазы 13 роадмапа дословно: «Состояние мастера живёт на сервере,
      `session_id` едет скрытым полем, и серверная проверка владения сессией
      СОХРАНЕНА — подставленный чужой `session_id` отвергается (машинно)».
      Совпадение прямое и предметное, а не тангенциальное. ⚠️ ДЕФЕКТ
      ПОДТВЕРЖДЁН ЗАМЕРОМ ВЕРИФИКАТОРА, А НЕ ПРИНЯТ ПО ОТЧЁТУ: `_qr_sessions`
      живёт в `app/messengers/telegram_user.py:42` и ключуется ОДНИМ токеном
      (`:69`, `:79`, `:102`, `:117`, `:139`, `:157`, `:176`); ни один из пяти
      входов `app/pages/accounts.py` (`:226`, `:253`, `:267`, `:289`, `:329`)
      личности к сеансу не привязывает. ⚠️ ФАЗОЙ 10 НЕ СОЗДАН И ЕЮ НЕ ТРОНУТ:
      `git diff 7c1041d..HEAD -- app/pages/accounts.py` не содержит ни одного
      куска в диапазоне `:226-358`. То же говорит шапка `10-REVIEW.md`.
  - truth: >-
      Девять отказов по происхождению, собранных помимо слоя ответа, приведены к
      единой форме (`WR-04` ПЯТОГО круга, окно 63 `.planning/WINDOWS.md`)
    addressed_in: "Phase 11"
    evidence: >-
      Окно 63 разводит снятие на двух адресатов ЯВНО: «Фаза 11 „Массовый перевод
      разделов письма“ (критерий 1 роадмапа) снимает выходы РАБОТОЙ; ФОРМА,
      которой отказ по происхождению поедет после перевода, есть решение
      ВЛАДЕЛЬЦА». Машинный гейт от отсрочки не слабеет — реестр
      `OWN_RESPONSE_EXITS` с объявленным числом 9 краснеет С ИМЕНЕМ.
behavior_unverified_items:
  - truth: >-
      `G-10-4`: плашка аварии ВИДНА поверх панели подтверждения, а не спрятана
      под ней (шаг 4.4 обхода — единственный ПРОВАЛЕННЫЙ шаг `10-UAT.md`)
    test: >-
      Проверка 4 обхода, шаги 4.1-4.6 на живом стенде: открыть панель
      подтверждения удаления расписания, вызвать отказ (Offline), нажать
      «Удалить» и посмотреть, ЧТО ВСТАЛО ПОВЕРХ.
    expected: >-
      Плашка аварии видна поверх панели; панель остаётся открытой (D-12); кнопка
      подтверждения снова нажимаема; повтор возможен без переоткрытия панели.
    why_human: >-
      ⚠️ ПРОДУКТОВАЯ ПРАВКА В ДЕРЕВЕ ЕСТЬ И ПРОВЕРЕНА ЧТЕНИЕМ: блок подъёма
      `app/static/css/app.css:1075-1085` — `.is-modal-open #htmx-failure-server,
      .is-modal-open #htmx-failure-network { position: fixed; z-index: 70; … }`
      при наибольшем прочем слое таблицы 60 (`.modal`), плюс снятие обеих
      заготовок при открытии панели (`components/modal.html:702`). НО ОБА
      СТЕРЕГУЩИХ ПРАВИЛА УТВЕРЖДАЮТ ОБЪЯВЛЕНИЕ СЛОЯ И ПОРЯДОК ОБЪЯВЛЕННЫХ СЛОЁВ,
      А НЕ ОТРИСОВКУ — это записано в самой таблице стилей (`:1063-1070`) и не
      смягчено. Одно из двух правил, сверх того, потеряло охват (гэп `V9-02`), а
      второе не выражает конъюнкции (гэп `V9-01`). Замер прода 2026-09-09 давал
      плашку `position: static`, `z-index: auto`, прямоугольник ЦЕЛИКОМ выше окна
      (`top: -322` при `scrollY: 418`), верхним узлом в её точке —
      `DIV.modal__overlay`, прокрутку корня `overflow: hidden`. Что правка это
      перевернула, не наблюдал никто.
coincidental_reliance_items:
  - truth: >-
      Критерий 1: все 18 мест подтверждения РАБОТАЮТ через htmx одной правкой
      `components/modal.html`
    reason: undeclared-precondition
    harden: >-
      Связь «имя события ТРИГГЕРА == имя события панели-СЛУШАТЕЛЯ» сегодня цела —
      проверено скриптом верификатора прежнего круга (18 диспетчеризаций, 9
      префиксов, у каждого префикса парное выражение `id=`), — но НИЧЕМ НЕ
      ПРИНУЖДЕНА: инвентарь считает ЧИСЛО диспетчеризаций и ЧИСЛО различных
      имён, а правила, СЛИЧАЮЩЕГО имя триггера с именем слушателя, в дереве нет
      (`ℹ️ IN-A` прежнего круга, `MODAL_EVENT_RE` @834
      `tests/test_templates/test_components.py`). Триггер, диспетчеризующий имя,
      которого не слушает ни одна панель, прошёл бы ВСЕ гейты инвентаря молча, а
      место подтверждения было бы мёртвым. Ужесточение: правило парности имён.
      ⚠️ ПОМЕТКА СОВЕЩАТЕЛЬНАЯ — ни счёта, ни вердикта она не меняет и пунктом
      человеческой проверки не становится.
human_verification:
  - test: >-
      Шаг 4.4 обхода `10-UAT.md` — ЕДИНСТВЕННЫЙ ПРОВАЛЕННЫЙ ШАГ, перезамер после
      правок планов 10-33 и 10-35.
    expected: "Плашка аварии видна ПОВЕРХ панели; панель открыта; повтор без переоткрытия."
    why_human: "Правила утверждают ОБЪЯВЛЕНИЕ слоя, а не ОТРИСОВКУ; у исполнителей планов браузера нет, и сводка 10-33 записала это сама."
  - test: >-
      Шаг 2.8 того же обхода — плашка обрыва связи при не доехавшем follow-up GET
      (`unobserved_steps`).
    expected: "Плашка появилась и называет расхождение экрана с сервером второй фразой."
    why_human: >-
      Половина (A) уже наблюдена проверкой 4 на НАСТОЯЩЕМ обрыве и записана
      распоряжением плана 10-39; ненаблюдённой осталась половина при обрыве,
      сделанном подменой `XMLHttpRequest`: `abort()` поднимает не то событие, что
      настоящий обрыв транспорта.
  - test: >-
      ПОДПИСЬ ЧЕЛОВЕКА НА ПРИЁМКЕ под обходом 2026-09-09 — не перепрохождение, а
      приёмка уже снятых признаков.
    expected: >-
      Либо человек подтверждает отметки шести проверок и артефакт получает
      терминальное состояние, либо называет, какие проверки требуют
      перепрохождения человеческими руками.
    why_human: >-
      ⚠️ ОТСТУПЛЕНИЕ НАЗВАНО САМИМ АРТЕФАКТОМ, А НЕ ВСКРЫТО ЗДЕСЬ: наблюдал АГЕНТ
      через chrome-devtools MCP; `workflow.live_dom_uat` в `.planning/config.json`
      равен `false`; право дано репликой в сессии. `status: partial` держится
      именно поэтому, и `test_the_walkthrough_cannot_self_certify.py` этот запрет
      принуждает машинно.
  - test: >-
      РЕШЕНИЕ ВЛАДЕЛЬЦА, А НЕ ПРОВЕРКА СТЕНДА: 39 запретов
      (`must_haves.prohibitions`) шести планов седьмой партии стоя́т
      `status: flagged-unverified` — ВСЕ БЕЗ ЕДИНОГО ИСКЛЮЧЕНИЯ.
    expected: >-
      По каждому запрету — либо предъявленное принуждение (для 25 запретов с
      `verification: test`), либо явное человеческое разрешение (для 14 с
      `verification: none`).
    why_human: >-
      Регламент фазовых гейтов велит закрывать такие пункты FAIL-CLOSED: запрет
      уровня `test`, доехавший до верификации без принуждения, считается
      НЕПРОВЕРЕННЫМ и зелёным не становится. Выборочный замер верификатора
      показывает, что часть из них ДЕРЖИТСЯ по дереву (планы 10-37 и 10-38
      тронули РОВНО `tests/test_pages/test_shell.py` — проверено
      `git show --stat` по десяти коммитам; правка 10-35 в `app.css` и
      `htmx_error_banner.html` — 40 и 34 строки ДОБАВЛЕНО, 0 удалено, все внутри
      комментария), но ни один из 39 не имеет ПРИНУЖДЕНИЯ, а флаг снимается не
      замером верификатора, а решением.
  - test: "Артефакт безопасности фазы (`10-SECURITY.md`) — см. гэп `V9-05`."
    expected: "Либо артефакт заведён прогоном `/gsd-secure-phase 10`, либо изъятие объявлено записью с основанием."
    why_human: "Решение о принуждении регламента безопасности принадлежит владельцу, а не верификатору."
---

# Phase 10: Рычаг `components/modal.html` — Verification Report (ДЕВЯТЫЙ круг)

**Phase Goal:** подтверждение удаления идёт через htmx во всех 18 местах одной правкой одного
файла (`app/templates/components/modal.html`), и после N удалений в документе не остаётся N
мёртвых диалогов
**Verified:** 2026-09-10T08:30:00Z
**Status:** gaps_found
**Score:** 8/14 (8 истин контракта роадмапа — все закрыты; 6 истин контракта СЕДЬМОЙ партии —
закрыто 0)
**Re-verification:** Да, круг 9 — после СЕДЬМОЙ партии закрытия гейпов (планы 10-35…10-40) и
ВОСЬМОГО круга ревизии. Заменяет отчёт от 2026-09-08T21:55 (`human_needed`, 7/8). HEAD `188f34b`.

## Вердикт одной фразой, и он неудобен обеим сторонам

**ЦЕЛЬ ФАЗЫ ДОСТИГНУТА. ФАЗА НЕ ЗАКРЫТА.** Это не смягчение и не игра словами: обе половины
цели роадмапа впервые в истории фазы имеют доказательство — первая машинное, вторая
браузерное, — а гэпы круга лежат ЦЕЛИКОМ в том, чем СЕДЬМАЯ партия свои закрытия ЗАКРЕПИЛА.
Из шести истин, которые партия объявила своим предметом, НЕ ЗАКРЫТА НИ ОДНА: две опровергнуты
собственным исполнением верификатора, три записаны невыполненными в `.planning/WINDOWS.md`
самими планами партии, шестая достигнута двумя правками из трёх.

## Что этот круг измерил СВОИМИ РУКАМИ

Ни одно число ниже не взято из SUMMARY, из `10-REVIEW.md` и из отметок обхода — кроме тех
случаев, где источник назван прямо.

**Замер 1 — блокер `CR-01` восьмого круга ВОСПРОИЗВЕДЁН, и матрица шире, чем у ревизии.**
Доктóренная копия рычага в каталоге на прогон, боевой файл только на чтение:

| Мутация тела `show()` | Находок `_lever_clearing_findings` | Вердикт |
| --- | --- | --- |
| (A) снятие вырезано целиком — ПРЕЖНЕЕ поведение | 2 | КРАСНО |
| (B) обе заготовки НАЗВАНЫ, глагол применён к `#notice` | **0** | **ЗЕЛЕНО** |
| (C) снимается одна, вторая названа комментарием | 1 | КРАСНО |
| (D) глагол на `#notice`, заготовки в комментарии | 2 | КРАСНО |

Строка (A) — то, чего требует вторая половина истины плана 10-35, и она ДЕРЖИТСЯ; ревизия
этого не мерила. Строка (B) — то, чего требует первая половина («правило требует СНЯТИЯ
обеих»), и она НЕ ДЕРЖИТСЯ. Строки (C) и (D) показывают, что де-комментирование исходника
работает. Вывод точнее ревизионного: правило не вакуумно, оно НЕВЕРНО СФОРМУЛИРОВАНО —
требует упоминания и наличия глагола, а не их связи.

**Замер 2 — регрессия охвата `CR-02` ВОСПРОИЗВЕДЕНА исполнением на боевом дереве.**

```
_selector_targets('body')                  -> ('body',)
_selector_targets('body.night')            -> ()
_selector_targets(':root')                 -> (':root',)
_selector_targets(':root.dark')            -> ()
_selector_targets('[data-shell]')          -> ('[data-shell]',)
_selector_targets('[data-shell].collapsed')-> ()
_selector_targets('[data-body].wide')      -> ()
```

Любой квалификатор на селекторе предка прячет предка от гейта. Это ИМЕННО та поломка, которую
`must_haves.key_links[2]` плана 10-37 назвал признаком отказа связи.

**Замер 3 — продукт на HEAD ПРОВЕРЕН ЧТЕНИЕМ, а не выведен из зелени правил.**
`components/modal.html:702`:

```
['htmx-failure-server', 'htmx-failure-network'].forEach(function (id) {
  var node = document.getElementById(id); if (node) { node.setAttribute('hidden', ''); } });
```

Глагол применён К КАЖДОЙ из двух заготовок. Свойство, которое правило стережёт плохо, В ДЕРЕВЕ
ВЕРНО. Блок подъёма (`app/static/css/app.css:1075-1085`) на месте: `position: fixed`,
`z-index: 70` при наибольшем прочем слое таблицы 60.

**Замер 4 — прогоны верификатора, а не оркестратора.**

| Отбор | Результат |
| --- | --- |
| `tests/test_templates/test_components.py -k "modal_site_inventory or modal_guard_is_inherited or panel_closes_on_the_location_transport or successful_only_branch_is_dead or criterion_three_holds_by_the_numbers"` | **5 passed, 79 deselected** |
| `tests/test_pages/test_shell.py` + `tests/test_templates/test_walkthrough_anchors.py` | **168 passed** (690 с) |
| `tests/test_planning/` | **28 passed** |

**Замер 5 — принадлежность правок партии по дереву.** `git show --stat` по четырнадцати
коммитам кода партии: планы 10-36, 10-37 и 10-38 тронули РОВНО `tests/test_pages/test_shell.py`
и `tests/test_templates/test_walkthrough_anchors.py`; продуктовых правок у партии ДВЕ —
`b3a3fa3` (`modal.html`, пять строк `show()`) и `d5bdf61` (`app.css` + `htmx_error_banner.html`,
40 и 34 строки ДОБАВЛЕНО, 0 удалено, все внутри комментария). Дисциплина RED→GREEN видна в
самом леджере коммитов (`test:` с красным контролем, затем `feat:`) — и это стои́т отдельного
слова, потому что гейт `tdd.review-checkpoint` в этой фазе проверил НОЛЬ планов
(`"No type:tdd plans found in phase 10"`, все планы `type: execute`).

**Замер 6 — артефакт безопасности.** `.planning/config.json:48-50` —
`security_enforcement: true`. `find .planning -name "*SECURITY*"` → девять файлов, по одному у
фаз 07, 08, 09 и у всех фаз вехи v2.0; у Фазы 10 — НИ ОДНОГО. Девять кругов верификации этого
не назвали.

## Goal Achievement

### Observable Truths

#### Группа A — контракт роадмапа (перенесён из круга 5, переизмерен)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | **Критерий 1.** Все 18 мест подтверждения работают через htmx одной правкой `modal.html`; инвентарь утверждён ЧИСЛОМ | ✓ VERIFIED (coincidental-reliance) | Оба названных роадмапом гейта зелены прогоном ВЕРИФИКАТОРА (5 selected, 5 passed) и не вакуумны (антивакуумный барьер `test_modal_site_inventory`). Макрос (`modal.html:770-772`) печатает БЕЗУСЛОВНО семь свойств качества. ⚠️ Совещательно: парность имени события триггера и панели-слушателя ничем не принуждена — см. `coincidental_reliance_items`. |
| 2 | **Критерий 2 и ВТОРАЯ ПОЛОВИНА ЦЕЛИ ФАЗЫ.** Ответ снимает осиротевшую панель вторым OOB-узлом `hx-swap-oob="delete"`; после N удалений не остаётся N мёртвых диалогов | ✓ VERIFIED | **ПЯТЬ КРУГОВ `PRESENT_BEHAVIOR_UNVERIFIED` ЗАКОНЧИЛИСЬ.** Разметка проверена чтением: `ads/partials/sched_delete_response.html:101-102` и `account_groups/partials/delete_response.html:99-100` — ПАРНЫЕ узлы (строка + панель). ПОВЕДЕНИЕ наблюдено в настоящем Chrome (обход 2026-09-09, прод, ad_id=50): `[role="dialog"]` 4 → 3, несущий счёт `[id^="sched-del-"][role="dialog"]` 3 → 2 (оба на единицу), `sched-139` и `sched-del-139` — ОБА `null`, 18 нажатий Tab / заходов в диалог **0**, на ветви перехода `1` и `0` при живом `window.__uat10`. Ловушка снята С ПОЛОЖИТЕЛЬНЫМ КОНТРОЛЕМ (проверка 5: в живой панели Tab держит фокус внутри `sched-del-142`). ⚠️ ЦЕНА: наблюдал АГЕНТ, не человек на приёмке; подпись остаётся пунктом 3 перечня Human Verification. |
| 3 | **Критерий 3, половина «новых строк JS фаза не добавляет»** | PASSED (override) | Override: прочтение D-13 («критерий запрещает НОВЫЕ СУЩНОСТИ, а не правку существующих»), записанное в `10-CONTEXT.md:197-210` ДО правки — принято владельцем `chubav` 2026-09-09, ветвь `accept-reading`, отклонена `reject-reading`. Запись перенесена ДОСЛОВНО и настоящим кругом НЕ ПЕРЕСУЖИВАЕТСЯ. Гейт четырёх чисел `test_criterion_three_holds_by_the_numbers` зелен прогоном верификатора (в отборе из 5). ⚠️ ⚠️ ОДНО НАБЛЮДЕНИЕ, КОТОРОЕ ЗАПИСЬ НЕ ОТМЕНЯЕТ И КОТОРОЕ ЗДЕСЬ НАЗЫВАЕТСЯ, А НЕ ПРЯЧЕТСЯ: охват записи (`covers`) объявляет границей «правка ТЕЛА метода существующего `x-data`», и правка плана 10-35 (пять строк в `show()`) в эту границу попадает — то есть запись, принятая 2026-09-09, накрыла и правку, доехавшую 2026-09-09 вечером. Формулировка критерия в `ROADMAP.md` остаётся буквальной, документ вехи и отчёт фазы говорят разное, и согласует их только эта запись — ровно как она сама и предупреждает. |
| 4 | **Критерий 3, половина «панель закрывается существующим Alpine (`x-on:htmx:after-request`)»** | ✓ VERIFIED | ПОВЕДЕНЧЕСКИ: `test_the_panel_closes_on_the_location_transport` исполняет выражение `x-data` в НАСТОЯЩЕМ интерпретаторе и утверждает ПЕРЕХОД СОСТОЯНИЯ (`open/locked/sending` → False, `threw is None`), при нём отрицательный контроль. ⚠️ ДОБАВЛЕНО ЭТИМ КРУГОМ: свойство подтверждено и БРАУЗЕРОМ — проверка 2 обхода, `panelVisible: false` при `cardDrawn: true` и ОБОРВАННОМ follow-up GET, `is-modal-open` → `false`, `overflow` → `visible`. То есть панель ушла ПО СОБЫТИЮ ЗАВЕРШЕНИЯ, а не по свопу тела: тело так и не пришло. Прежний круг держал это пунктом 4 перечня человеческой проверки — пункт закрыт. |
| 5 | **Критерий 4.** Все 10 потребителей продолжают удалять без JS: панель остаётся настоящей формой POST с непустым `action` | ✓ VERIFIED | `method="post"` — литерал; `action` и `hx-post` выписаны ТЕМ ЖЕ выражением; `MODAL_CONSUMERS` — ровно 10 имён; гейты обходят ВСЕХ вызывающих при отрицательных контролях. У трёх потребителей без правила `*_degrades_without_*` строчная форма прямого POST есть и так (`admin/includes/worker_row.html:108`, `admin/includes/queue_row.html:87`, `history/includes/history_card.html:162`). |
| 6 | `CR-03`, БРАУЗЕРНАЯ ПОЛОВИНА: после перехода `HX-Location` человек видит ЖИВОЙ клиентский слой редактора | ✓ VERIFIED | Машинная половина держится (обёртка `ads/form.html:308`/`:538`, исходник исполняется ДВАЖДЫ в ОДНОЙ `vm`-области настоящим Node, мутация краснит). ⚠️ ДОБАВЛЕНО ЭТИМ КРУГОМ: браузерная половина наблюдена — шаг 1.13 ранней ошибки языка в консоли НЕТ; шаг 1.14 `#text-counter` 30→31→32→33→34, на КАЖДЫЙ символ, то есть обработчики живы на пришедшем документе. Шаг 1.15 объявлен НЕПРИМЕНИМЫМ с названной причиной (вложений на посевном объявлении не заводилось), а не пропущен молча. |
| 7 | Граница величины идентификатора держится на семье подтверждённого удаления | ✓ VERIFIED | Воспроизведено прежним кругом зондом **422 × 7**; корень объявлен ровно один раз во всём `app/` (`app/pages/identifiers.py`), при правиле — отрицательный контроль на ВТОРОЕ объявление. Каталожный гейт полноты **17 passed**, вселенная/приложение/псевдонимы объявлены числами (35/10/3). Регрессии в этом круге нет: файл не правлен партией. |
| 8 | Правило, принуждающее регламент отметок требований, не краснеет от штатного продвижения проекта — включая вердикт, которым фаза достигает цели | ✓ VERIFIED | `tests/test_planning/` — **28 passed** прогоном ВЕРИФИКАТОРА на живом вердикте. Отметка требования соответствует дереву: `.planning/REQUIREMENTS.md:31` — `- [ ] **FORM-06**`, `:136` — `Gaps Found`. Попытка плана 10-39 отметить требование закрытым уронила правило и была откачена — то есть правило и НЕ вакуумно, и НЕ зависит от вердикта. |

**Итог группы A: 8/8.** Ни одной FAILED, ни одной `PRESENT_BEHAVIOR_UNVERIFIED`.

#### Группа B — контракт СЕДЬМОЙ партии (истины, объявленные предметом планов 10-35…10-40)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 9 | `G-10-4`: плашка аварии видна ПОВЕРХ панели подтверждения (шаг 4.4 обхода) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Правка в дереве ЕСТЬ и проверена чтением (блок подъёма `app.css:1075-1085`, слой 70 против наибольшего прочего 60; снятие обеих заготовок `modal.html:702`). Оба стерегущих правила утверждают ОБЪЯВЛЕНИЕ и ПОРЯДОК слоёв, а НЕ отрисовку, — и это записано в самой таблице (`:1063-1070`), не смягчено. Сверх того одно из правил потеряло охват (`V9-02`), второе не выражает конъюнкции (`V9-01`). → человеку. |
| 10 | 10-35 `truths[3]`: правило требует СНЯТИЯ ОБЕИХ заготовок по перечню `FAILURE_BANNER_IDS` | ✗ FAILED | **Замер 1, строка (B): 0 находок, ЗЕЛЕНО** на теле, где не снимается ни одна заготовка. Гэп `V9-01`. |
| 11 | 10-36 `truths[0]`: все 21 запись реестра якорей набираются в консоли | ✗ FAILED | Окно **68** `.planning/WINDOWS.md`, вердикт `open`, тип `unmet-truth`, заведено САМИМ планом 10-36 (`89a9fbc`). Восьмой круг перезамерил: прозаический хвост несут 11 записей, не 12. Гэп `V9-03`. |
| 12 | 10-37 `key_links[2]`: гейт цепи предков смотрит туда же, куда смотрит браузер | ✗ FAILED | **Замер 2**: `body.night { filter: blur(2px) }` — правка ТЕЛА ДОКУМЕНТА, отнимающая у подъёма окно, — даёт НОЛЬ находок при обоих зелёных правилах. Это дословный признак поломки, названный самим планом. Гэп `V9-02`. |
| 13 | 10-38 `truths[0]`: описка в имени свойства ВНУТРИ группы больше не проходит зелёной | ✗ FAILED | Окно **69**, вердикт `open`: контроль и гейт читают ОДНУ константу, прогон с `isolation` → `isolatiom` оставил отбор зелёным. Круговой контроль в терминах гейта качества тестов. Гэп `V9-04`. |
| 14 | 10-39: повторное исполнение правок неотличимо от первого | ⚠️ PARTIAL | Окно **70**, вердикт `open`: достигнуто ДВУМЯ правками из ТРЁХ; третья (раздел в `deferred-items.md` без якоря) удвоилась бы на втором прогоне. Латентно. Гэп `V9-06`. |

**Итог группы B: 0/6** — 4 FAILED, 1 PARTIAL, 1 PRESENT_BEHAVIOR_UNVERIFIED.

**Score:** 8/14 must-haves verified (1 present, behavior-unverified; 1 из восьми — `PASSED (override)`)

⚠️ **ЧЕСТНОЕ СЛОВО О ФОРМЕ СЧЁТА.** Знаменатель вырос с 8 до 14 не потому, что круг ужесточился,
а потому, что седьмая партия ДОБАВИЛА в фазу шесть собственных истин. Прятать их за счётом
роадмапа значило бы отчитаться «8/8» за фазу, чья последняя партия не закрыла ничего из
объявленного. Роадмапный контракт при этом НЕ ослаблен и НЕ сужен: все четыре критерия
проверены и все четыре держатся.

### Deferred Items

| # | Item | Addressed In | Evidence |
| --- | --- | --- | --- |
| 1 | `CR-01` шестого круга: сеанс QR-входа Telegram не привязан к пользователю | Phase 13 | Критерий 3 Фазы 13 дословно требует, чтобы подставленный чужой `session_id` отвергался (машинно). Фазой 10 дефект не создан и ею не тронут. |
| 2 | `WR-04` пятого круга: девять отказов по происхождению помимо слоя ответа | Phase 11 | Окно 63 разводит снятие на двух адресатов явно; критерий 1 Фазы 11 снимает выходы работой. |

⚠️ **ОТЛОЖЕННЫМИ НЕ ОБЪЯВЛЕНЫ И НЕ БУДУТ:** шесть неограниченных входов (`CATALOGUE_APPENDIX`,
вердикт `open`) — ни одна последующая фаза вехи (11-15) предметом их не называет; и ни один из
шести гэпов настоящего круга — все шесть принадлежат Фазе 10 и адресата в поздних фазах не
имеют (проверено по разделам фаз 11-15).

### Advisory (New Scope, Unevidenced)

**Нет ни одной.** Все шесть гэпов круга подкреплены детерминированным свидетельством: два —
собственным исполнением верификатора (замеры 1 и 2), три — записями `unmet-truth` в
`.planning/WINDOWS.md`, заведёнными самими планами партии, один — командой `find` и чтением
`.planning/config.json`. Ни одно новое мнение об архитектуре в блокирующий набор не внесено, и
ни одна закрытая истина этим кругом не отменена.

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `app/templates/components/modal.html` | Рычаг: безусловные htmx-атрибуты формы панели + снятие обеих заготовок при открытии | ✓ VERIFIED | `:770-772` — семь свойств качества вне условия шаблонизатора; `:702` — `forEach` применяет `setAttribute('hidden','')` К КАЖДОЙ из двух заготовок (проверено чтением). 10 потребителей, 11 импортёров, 18 мест. |
| `app/static/css/app.css` | Блок подъёма заготовок плашки выше панели | ✓ VERIFIED (объявление) | `:1075-1085` — `position: fixed`, `z-index: 70` при наибольшем прочем слое 60. Способа отображения блок не объявляет — это несущее свойство, а не пропуск. ⚠️ `:1016-1017` несёт утверждение о стережении ШИРЕ того, что правило принуждает (гэп `V9-01`). |
| `app/templates/includes/htmx_error_banner.html` | Запись допущения Фазы 8, суженная, а не вычеркнутая | ✓ VERIFIED | `:51-73` и `:100-110` — прежний текст стои́т дословно, ниже пометка опровержения; исполняемых строк не добавлено (диффом: 34 добавлено, 0 удалено, все в Jinja-комментарии). |
| `app/templates/ads/partials/sched_delete_response.html` | Парные OOB-узлы снятия строки и панели | ✓ VERIFIED | `:101-102` — `id="sched-{{ schedule_id }}"` и `id="sched-del-{{ schedule_id }}"`, оба `hx-swap-oob="delete"`; `:103` — третий узел на линейку счётчика. Применение наблюдено обходом (истина 2). |
| `app/templates/account_groups/partials/delete_response.html` | То же для группы аккаунта | ✓ VERIFIED | `:99-100`. |
| `app/templates/ads/form.html` | Обёртка инлайн-скрипта | ✓ VERIFIED | `:308` / `:538`; тело скрипта не правлено ни на символ. |
| `tests/test_pages/test_shell.py` | Правила снятия заготовок и чистоты цепи предков | ⚠️ ORPHANED (правила есть, предмета не закрывают) | 168 passed прогоном верификатора — и при этом `_lever_clearing_findings` не выражает конъюнкции (`V9-01`), `_selector_targets` потерял охват (`V9-02`), контроль перечня цикличен (`V9-04`). Три из четырёх правил партии в этом файле стерегут не то, что объявлено. |
| `tests/test_templates/test_walkthrough_anchors.py` | Реестр якорей обхода | ⚠️ ORPHANED (реестр есть, истина не достигнута) | Модуль зелен; поле `selector` 11 записей не набирается в консоли (окно 68, `V9-03`). |
| `.planning/phases/10-…/10-UAT.md` | Обход второй половины цели фазы | ⚠️ ЧАСТИЧНО ЗАКРЫТ | `status: partial`, обход 2026-09-09 состоялся, отметки шести проверок заполнены ЧИСЛАМИ. `failed_steps: ["4.4"]`, `unobserved_steps: ["2.8"]`, `not_applicable_steps: ["1.15"]`, `divergences: 3`. Самозаверения артефакт не допускает и терминальным себя не объявляет — проверено правилом `test_the_walkthrough_cannot_self_certify.py` (28 passed). |
| `.planning/phases/10-…/10-SECURITY.md` | Артефакт безопасности фазы при `security_enforcement: true` | ✗ MISSING | Файла нет; у девяти соседних фаз он есть. Гэп `V9-05`. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Кнопка-триггер (18 мест) | Панель `modal()` (11 выражений `id=`) | событие окна `modal-open-<id>` | ✓ WIRED | 18 диспетчеризаций / 9 префиксов / 11 выражений `id=`, каждый префикс имеет пару. ⚠️ Правила, сличающего ИМЯ триггера с ИМЕНЕМ панели, в дереве нет — связь цела, но не принуждена (`coincidental_reliance_items`). |
| Форма панели | Маршрут подтверждённого удаления | `hx-post="{{ action }}"` == `action="{{ action }}"` | ✓ WIRED | Три правила обходят всех вызывающих при отрицательных контролях; зелены в прогоне верификатора. |
| Ответ удаления расписания | Документ | второй OOB-узел по `id` панели | ✓ WIRED | ⚠️ ПЕРЕВЕДЕНО ИЗ `HOLLOW`: узлы есть и различны (машинно), а СВОП наблюдён браузером — оба узла удалённой строки `null`, счёт диалогов упал на единицу. |
| Событие завершения запроса htmx | `hide()` панели | `x-on:htmx:after-request` | ✓ WIRED | Подтверждено ТРИЖДЫ: исполнением выражения в интерпретаторе, замером бандла прежнего круга (`afterRequest` летит из `g.onload` ПОСЛЕ `M(r,T)`) и браузером на ОБОРВАННОМ follow-up GET. |
| Тело `show()` рычага | Правило `_lever_clearing_findings` | подстрочный поиск по де-комментированному исходнику | ✗ NOT_WIRED | Связи «эта заготовка ↔ этот глагол» нет ни одной; форма (B) замера 1 проходит зелёной. Гэп `V9-01`. |
| Селекторы предков заготовки | `_ancestor_declarations` | `_selector_targets` (строгое равенство простой части) | ✗ NOT_WIRED | Квалификатор на селекторе предка рвёт связь: `body.night`, `:root.dark`, `[data-shell].collapsed` → 0 находок. Гэп `V9-02`. |
| Отметка `FORM-06` | Вердикт отчёта верификации | `tests/test_planning/test_requirement_completion_follows_verification.py` | ✓ WIRED | 28 passed; попытка отметить требование при непройденной верификации уронила правило и была откачена планом 10-39. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `modal.html` | `action` | Именованный аргумент вызывающего; у всех 11 выражений — конкатенация с идентификатором сущности | ✓ | ✓ FLOWING — пустого литерала нет ни у одного вызывающего |
| `sched_delete_response.html` | `schedule_id` | Идентификатор удалённой строки из обработчика `app/pages/schedules.py` | ✓ | ✓ FLOWING — подтверждён браузером: `sched-139` и `sched-del-139` исчезли ОБА |
| `sched_count_rule.html` | `schedules_count` | `editor.schedules_count`, один включаемый файл на ОБА места отрисовки | ✓ | ✓ FLOWING — линейка показала `2`, затем `1`, сойдясь с числом карточек |
| `modal.html` (панель) | `sending` | `x-data`, сбрасывается `x-on:htmx:after-request` | ✓ | ✓ FLOWING — переход состояния исполнен в интерпретаторе И наблюдён браузером |
| `app.css` подъём | `z-index: 70` | Литерал таблицы; правило сличает ДВА прочитанных слоя, числа в правиле не выписаны | ✓ | ⚠️ STATIC — объявление есть, отрисовка не наблюдалась (истина 9) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Правило снятия заготовок ловит misapplication | скрипт верификатора, 4 мутации доктóренной копии рычага | (A) 2, (B) **0**, (C) 1, (D) 2 находки | ✗ FAIL на (B) |
| Гейт цепи предков ловит квалифицированный селектор | скрипт верификатора, `_selector_targets` на 7 входах | 3 из 3 квалифицированных → `()` | ✗ FAIL |
| Пять гейтов критериев 1 и 3 | `uv run pytest tests/test_templates/test_components.py -k "…"` | **5 passed, 79 deselected** | ✓ PASS |
| Модули, правленные партией | `uv run pytest tests/test_pages/test_shell.py tests/test_templates/test_walkthrough_anchors.py -q` | **168 passed** (690 с) | ✓ PASS |
| Гейты записей | `uv run pytest tests/test_planning/ -q` | **28 passed** | ✓ PASS |
| Долговые маркеры в файлах партии | `grep -nE "\bTBD\b\|\bFIXME\b\|\bXXX\b"` по пяти файлам | НОЛЬ во всех пяти | ✓ PASS |
| Принадлежность правок партии | `git show --stat` по 14 коммитам кода | продуктовых правок ДВЕ; 10-36/37/38 тронули только суиту | ✓ PASS |
| Артефакт безопасности фазы | `find .planning -name "*SECURITY*"` | 9 файлов, Фазы 10 среди них НЕТ | ✗ FAIL |
| Отрисовка плашки поверх панели | — | нельзя без браузера | ? SKIP → человеку (истина 9) |

### Probe Execution

| Probe | Command | Result | Status |
| --- | --- | --- | --- |
| Конвенциональный каталог `scripts/*/tests/probe-*.sh` | `find scripts -path '*/tests/probe-*.sh'` | в проекте не заведён; ни один план фазы его не объявляет | SKIP |
| Граница величины на семье подтверждённого удаления | зонд прежнего круга, 7 входов | **422 × 7** | PASS (не переизмерялся: файл партией не тронут) |

### Test Quality Audit

| Test File | Linked Req | Active | Skipped | Circular | Assertion Level | Verdict |
| --- | --- | --- | --- | --- | --- | --- |
| `tests/test_pages/test_shell.py` | FORM-06 | 168 (с модулем якорей) | 0 | **ДА** — `test_control_a_trapping_ancestor_reddens` и гейт читают одну константу (окно 69) | Value | 🛑 BLOCKER (`V9-04`) |
| `tests/test_pages/test_shell.py` (`_lever_clearing_findings`) | FORM-06 | 1 правило + контроли | 0 | Нет | Existence (два независимых `in body`) | 🛑 BLOCKER (`V9-01`) — INSUFFICIENT: утверждение конъюнкции не выражено |
| `tests/test_pages/test_shell.py` (`_selector_targets`) | FORM-06 | 1 правило + 3 контроля | 0 | Нет | Value, но охват сужен | 🛑 BLOCKER (`V9-02`) — контроли утверждают строгость только в ту сторону, где она помогает |
| `tests/test_templates/test_walkthrough_anchors.py` | FORM-06 | входит в 168 | 0 | Нет | Value | ⚠️ истина реестра не достигнута (`V9-03`) |
| `tests/test_templates/test_components.py` | FORM-06 | 84 | 0 | Нет | Behavioral (`_run_modal_lifecycle` исполняет `x-data` настоящим Node) | ✓ |
| `tests/test_planning/` | FORM-06 | 28 | 0 | Нет | Behavioral (мутация роняет правило) | ✓ |

**Отключённых тестов на требовании:** 0. **Круговых образцов:** 1 (окно 69). **Недостаточных
утверждений:** 2 (`V9-01`, `V9-02`). ⚠️ Отдельно: гейт `tdd.review-checkpoint` отработал
ВАКУУМНО (`tddPlans: 0` — все планы фазы `type: execute`), то есть дисциплину RED→GREEN в этой
фазе не проверил никто, кроме леджера коммитов, где она видна.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| FORM-06 | ВСЕ 40 планов фазы (единственный ID во всех шапках, включая 10-35…10-40) | подтверждение удаления работает через htmx во всех 18 местах одной правкой `components/modal.html`, и ответ снимает осиротевшую панель вторым OOB-узлом с `hx-swap-oob="delete"` | ✗ BLOCKED (предмет требования закрыт; фаза — нет) | ⚠️ ОБЕ ПОЛОВИНЫ САМОГО ТРЕБОВАНИЯ ЗАКРЫТЫ: первая машинно (истины 1/3/4/5), вторая наблюдена браузером (истина 2). Требование НЕ отмечается закрытым только потому, что вердикт фазы — `gaps_found`, а правило `test_no_requirement_is_marked_complete_before_its_phase_verification_passed` это принуждает. Отметка в дереве вердикту соответствует: `.planning/REQUIREMENTS.md:31` — `- [ ]`, `:136` — `Gaps Found`. |

**Сирот нет.** Извлечение `requirements:` из шапок всех 40 планов даёт РОВНО `FORM-06` —
проверено по всем шести планам седьмой партии отдельно. `.planning/REQUIREMENTS.md:491`
отображает Фазу 10 в одно требование (`| 10. Рычаг components/modal.html | FORM-06 | 1 |`).
Требований, ожидаемых от фазы и не заявленных ни одним планом, нет.

⚠️ **ЧТО ЭТО ЗНАЧИТ ДЛЯ ОТМЕТКИ.** План 10-39 попытался перевести `FORM-06` в `Complete`,
получил красное правило и правку откатил — это записано в его сводке и подтверждено деревом.
Настоящий вердикт (`gaps_found`) означает, что откат был верен и отметка остаётся `- [ ]` ещё
один круг. Требование закроется на первом круге, где группа B будет закрыта; продукта это не
касается.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `tests/test_pages/test_shell.py` | 3381-3425 | **`V9-01` — единственный машинный страж продуктового свойства принимает зелёным тело, где свойство ложно.** Воспроизведено исполнением (замер 1, строка B) | 🛑 Blocker | Регрессия по признаку «файл правлен партией после отметки `verified` прежнего круга» — гейт свидетельства пройден без оговорок. Продукт на HEAD верен; защита бумажная |
| `tests/test_pages/test_shell.py` | 3838-3848 | **`V9-02` — регрессия ОХВАТА: квалификатор на селекторе предка прячет предка от гейта.** Воспроизведено исполнением (замер 2) | 🛑 Blocker | Прежняя подстрочная форма все три случая ЛОВИЛА. Тема оформления (`:root.dark`, `body.night`) — самая ходовая правка ровно этих селекторов; `filter` на теле документа отнимает у подъёма окно ЦЕЛИКОМ |
| `tests/test_pages/test_shell.py` | контроль перечня | **`V9-04` — круговой отрицательный контроль: контроль и гейт читают одну константу** (окно 69) | 🛑 Blocker | Доказывает согласованность правила с собой, а не соответствие предмету. Гейт качества тестов называет этот класс блокирующим прямо |
| `tests/test_templates/test_walkthrough_anchors.py` | поле `selector` 11 записей | **`V9-03` — истина реестра не достигнута** (окно 68) | 🛑 Blocker | `must_haves.truths[0]` плана СЕДЬМОЙ партии; расхождение записано самим планом, вердикт `open` |
| каталог фазы | — | **`V9-05` — `10-SECURITY.md` отсутствует при `security_enforcement: true`** | 🛑 Blocker | Гейт безопасности не отработал ни разу за девять кругов; у девяти соседних фаз артефакт есть. Цели фазы не касается, закрытия — касается |
| `app/static/css/app.css` | 1016-1017 | **Утверждение о стережении ШИРЕ того, что правило принуждает**, и это ПРОДУКТОВЫЙ файл | ⚠️ Warning | Класс «запись шире дерева», за который фаза получила круги 3, 4, 5, 6 и 8, — отгружен ТОЙ ЖЕ партией, что заводила механизмы против него. Поведения не трогает |
| `app/templates/components/modal.html` | 721, 723 | **`WR-03` ПЯТОГО КРУГА НЕ ЗАКРЫТ И ПРОТУХ ЕЩЁ СИЛЬНЕЕ.** Перезамерено этим кругом: пометка называет `HX_LOCATION_HEADER` «на :148» и `respond()` «(:375)»; на дереве `HX_LOCATION_HEADER` объявлен на `app/pages/htmx.py:59` и читается на `:169`, а `:357` занят `_with_notice` | ⚠️ Warning | ⚠️ ОТЯГЧАЮЩЕЕ ПРОТИВ ПРЕЖНЕГО КРУГА: план 10-35 правил ЭТОТ ЖЕ ФАЙЛ (`b3a3fa3`, двадцатью строками выше) и протухшую пометку не тронул. Номера в реестре гэпов партии не значились — и потому не были замечены |
| `app/pages/schedules.py` | 28 (опровергается 352) | **`WR-01` ПЯТОГО КРУГА НЕ ЗАКРЫТ.** Перезамерено: комментарий гласит «`ID_MAX` ВВОЗИТСЯ И НЕ ЗОВЁТСЯ В ЭТОМ ФАЙЛЕ НИ РАЗУ», а `:352` несёт `if value < 1 or value > ID_MAX:` в исполняемом коде | ⚠️ Warning | Читатель, снявший ввоз при уборке транзитных имён, уронит `_ad_id_from_form` на `NameError` — на маршруте подтверждённого удаления расписания, в проде |
| `.planning/ROADMAP.md` | строка `**Plans**` раздела Фазы 10 | Строка состава читает «40 планов, из них **34** исполнено», тогда как замер даёт **40 из 40** `- [x]` при **40** файлах `*-SUMMARY.md` | ⚠️ Warning | Собственная идиома фазы («прежнее значение не было ошибкой — оно устарело») САМА устарела ровно на ту партию, которая её дописывала. Гейт `test_state_progress_matches_roadmap` этого не ловит по построению: он читает ОТМЕТКИ, а не прозу (и потому зелен: `progress.completed_plans: 78` сходится с отметками) |
| `app/pages/{history,admin,ads}.py` | шесть входов | **`WR-04-A` — 500 на неограниченном идентификаторе** (зонд прежнего круга: `500 × 6`) | ⚠️ Warning | Вне предмета цели фазы. ДОЛГ БЕЗ ВЛАДЕЛЬЦА: ни одна фаза 11-15 предметом его не называет |
| `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` | 55, 67 | **`IN-03` ПЯТОГО КРУГА НЕ ЗАКРЫТ** — синтетический артефакт СЛЕДУЮЩЕЙ фазы роняет `test_the_declared_vocabularies_and_exemptions_agree` | ⚠️ Warning | Цели Фазы 10 не мешает: краснеет на первом артефакте фазы 11 |
| файлы, правленные партией | — | Долговые маркеры `TBD`/`FIXME`/`XXX` | ℹ️ Info | **НОЛЬ** во всех пяти файлах партии — проверено грепом |
| шесть планов партии | `must_haves.prohibitions` | **39 запретов, ВСЕ `status: flagged-unverified`** (25 с `verification: test`, 14 с `verification: none`) | ℹ️ Info → человеку | Ни одного зелёного, ни одного принуждённого. Fail-closed по регламенту: запрет уровня `test` без принуждения считается НЕПРОВЕРЕННЫМ. Выборочный замер показывает, что часть держится по дереву, но снимает флаг решение, а не замер |

### Decision Coverage

Гейт `check.decision-coverage-verify` — **не блокирующий по построению**. Решения
`10-CONTEXT.md`, на которые опирается этот круг, в отгруженных артефактах присутствуют:
D-13 (прочтение критерия 3) — записан `overrides[0]` шапки; D-12 (панель остаётся открытой на
отказе) — наблюдён шагами 4.3/4.5 обхода; D-03 (плашки на успех нет) — держится; D-30/D-32
(прежние формулировки не вычёркиваются) — соблюдён во всех правках партии, проверено диффом
(`d5bdf61`: 74 строки добавлено, 0 удалено). Решений, переведённых в планы и исчезнувших при
исполнении, не найдено.

### Human Verification Required

Пять пунктов. Прежний круг называл восемь; три закрыты обходом 2026-09-09 (осиротевшая панель,
ветвь перехода, живость клиентского слоя), один — записью решения владельца (прочтение D-13),
один — тем же обходом наполовину (проверка 2: несущие шаги 2.3-2.7 сошлись, ненаблюдённым
остался 2.8).

#### 1. Шаг 4.4 — ЕДИНСТВЕННЫЙ ПРОВАЛЕННЫЙ ШАГ ОБХОДА, перезамер после правок

**Тест:** открыть панель подтверждения удаления расписания, вызвать отказ (Network → Offline),
нажать «Удалить», посмотреть, ЧТО ВСТАЛО ПОВЕРХ; затем нажать «Удалить» повторно, не
переоткрывая панель.
**Ожидание:** плашка аварии ВИДНА поверх панели; панель осталась открытой; кнопка нажимаема;
повтор прошёл без переоткрытия.
**Почему человек:** оба стерегущих правила утверждают ОБЪЯВЛЕНИЕ слоя и ПОРЯДОК объявленных
слоёв, а НЕ отрисовку — это записано в самой таблице стилей (`app.css:1063-1070`) и здесь не
смягчается. Одно из двух правил, сверх того, потеряло охват (`V9-02`). Замер прода 2026-09-09
давал плашку `position: static`, `z-index: auto`, прямоугольник целиком выше окна, прокрутку
корня `overflow: hidden`.

#### 2. Шаг 2.8 — плашка обрыва связи при не доехавшем follow-up GET

**Тест:** повторить проверку 2 с НАСТОЯЩИМ обрывом транспорта (Network → Offline), а не
подменой `XMLHttpRequest`, и посмотреть на плашку.
**Ожидание:** плашка появилась и называет расхождение экрана с сервером второй фразой.
**Почему человек:** половина (A) уже наблюдена проверкой 4 на настоящем обрыве и записана
распоряжением плана 10-39; `abort()` поднимает не то событие, что настоящий обрыв.

#### 3. ПОДПИСЬ ЧЕЛОВЕКА НА ПРИЁМКЕ под обходом 2026-09-09

**Тест:** не перепрохождение, а приёмка: подтвердить отметки шести проверок либо назвать те,
что требуют человеческих рук.
**Ожидание:** артефакт получает терминальное состояние — либо получает названный перечень
перепрохождения.
**Почему человек:** ⚠️ отступление названо САМИМ артефактом, а не вскрыто здесь: наблюдал
АГЕНТ через chrome-devtools MCP; `workflow.live_dom_uat` равен `false`; право дано репликой в
сессии; обход шёл на ПРОДЕ против боевой базы. `status: partial` держится именно поэтому.
⚠️ Настоящий отчёт ПЕРЕВЁЛ истины 2 и 6 в `VERIFIED` по этим отметкам — то есть цена подписи
названа прямо: без неё вторая половина цели фазы стои́т на наблюдении агента.

#### 4. РЕШЕНИЕ ВЛАДЕЛЬЦА: 39 запретов партии, все `flagged-unverified`

**Тест:** по каждому запрету — предъявить принуждение (25 с `verification: test`) либо дать
явное разрешение (14 с `verification: none`).
**Ожидание:** ни один запрет не остаётся молча зелёным.
**Почему человек:** регламент велит закрывать такие пункты fail-closed. Выборочный замер
верификатора показывает, что часть ДЕРЖИТСЯ по дереву (10-37 и 10-38 тронули ровно
`tests/test_pages/test_shell.py`; правка 10-35 в `app.css` и `htmx_error_banner.html` —
комментарий без единой исполняемой строки), но принуждения нет ни у одного.

#### 5. РЕШЕНИЕ ВЛАДЕЛЬЦА: артефакт безопасности фазы

**Тест:** прогнать `/gsd-secure-phase 10` — либо объявить фазу изъятой из принуждения записью
с основанием.
**Ожидание:** `10-SECURITY.md` заведён либо изъятие названо.
**Почему человек:** решение о принуждении регламента принадлежит владельцу. ⚠️ Изъятие
молчанием здесь неотличимо от пропуска — девять кругов это и доказали.

### Gaps Summary

**Шесть гэпов, и ни один не в продукте.** Это главное, что следует прочесть из отчёта, и это же
делает его неудобным: цель фазы, которую восемь кругов подряд отчёты называли недостигнутой,
ДОСТИГНУТА — а фаза закрыться всё равно не может.

**Что закрылось.** Вторая половина цели («после N удалений в документе не остаётся N мёртвых
диалогов») впервые наблюдена в настоящем браузере, с числами и с положительным контролем
ловушки фокуса. Вместе с ней закрылись `CR-03` (браузерная половина), ветвь перехода и уборка
панели на оборванном follow-up GET. Прочтение D-13 критерия 3 перестало жить прозой и получило
запись владельца. Все четыре критерия роадмапа держатся.

**Что не закрылось — и почему это не придирка.** Седьмая партия была партией ЗАКРЕПЛЕНИЯ: пять
её планов из шести правили не продукт, а правила, которыми продукт стережётся. Из шести истин,
объявленных предметом этих планов, не закрыта ни одна. Две опровергнуты исполнением
верификатора: правило снятия заготовок принимает зелёным тело, где не снимается НИ ОДНА
заготовка; гейт цепи предков перестал видеть предка, к селектору которого дописан любой
квалификатор — то есть ровно ту правку темы оформления, которая в этом проекте вероятнее всего.
Три записаны невыполненными в `.planning/WINDOWS.md` САМИМИ планами партии (окна 68, 69, 70) —
и это, надо сказать прямо, ЗАСЛУГА партии: она свои промахи назвала, а не спрятала. Шестая
достигнута двумя правками из трёх.

**Класс дефекта один, и фаза знает его наизусть.** «Запись шире дерева» — то, за что фаза
получила круги 3, 4, 5, 6 и 8. На этом круге он лежит в ПРОДУКТОВОМ файле (`app.css:1016-1017`
утверждает стережение, которого правило не даёт) и в двух пометках, не тронутых с пятого круга,
одна из которых — в самом рычаге фазы, в двадцати строках от правки, которую партия туда же и
внесла.

**Отдельно и не как гэп: `10-SECURITY.md` не существует при `security_enforcement: true`.**
Девять соседних фаз артефакт имеют. Девять кругов верификации этой фазы этого не назвали —
включая восемь предыдущих и, до сегодняшнего замера, настоящий. Названо здесь именно потому,
что молчание о пропуске неотличимо от изъятия.

**Чего следующему кругу делать НЕ НАДО.** Трогать продукт. Все шесть гэпов чинятся в суите и в
записях; ни одна продуктовая строка фазы под ними не стои́т, и правка продукта под правило была
бы ровно тем, что планы партии сами себе запретили.

---

_Verified: 2026-09-10T08:30:00Z_
_Verifier: Claude (gsd-verifier), ДЕВЯТЫЙ круг_
