---
phase: 10-rychag-components-modal-html
verified: 2026-09-10T21:40:00Z
status: human_needed
score: 14/14 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/10-rychag-components-modal-html/10-01-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-01-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-02-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-02-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-03-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-03-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-04-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-04-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-05-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-05-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-06-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-06-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-07-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-07-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-08-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-08-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-09-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-09-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-10-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-10-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-11-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-11-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-12-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-12-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-13-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-13-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-14-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-14-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-15-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-15-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-16-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-16-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-17-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-17-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-18-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-18-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-19-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-19-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-20-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-20-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-21-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-21-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-22-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-22-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-23-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-23-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-24-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-24-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-25-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-25-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-26-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-26-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-27-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-27-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-28-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-28-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-29-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-29-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-30-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-30-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-31-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-31-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-32-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-32-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-33-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-33-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-34-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-34-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-35-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-35-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-36-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-36-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-37-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-37-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-38-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-38-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-39-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-39-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-40-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-40-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-41-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-41-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-42-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-42-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-43-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-43-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-44-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-44-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-45-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-45-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-46-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-46-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-47-PLAN.md"
  - ".planning/phases/10-rychag-components-modal-html/10-47-SUMMARY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-CONTEXT.md"
  - ".planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md"
  - ".planning/phases/10-rychag-components-modal-html/10-REVIEW.md"
  - ".planning/phases/10-rychag-components-modal-html/10-SECURITY-SUBJECT.md"
  - ".planning/phases/10-rychag-components-modal-html/10-SECURITY.md"
  - ".planning/phases/10-rychag-components-modal-html/10-UAT.md"
  - ".planning/phases/10-rychag-components-modal-html/deferred-items.md"
  - "app/pages/account_groups.py"
  - "app/pages/accounts.py"
  - "app/pages/admin.py"
  - "app/pages/ads.py"
  - "app/pages/common.py"
  - "app/pages/history.py"
  - "app/pages/htmx.py"
  - "app/pages/identifiers.py"
  - "app/pages/schedules.py"
  - "app/static/css/app.css"
  - "app/templates/account_groups/includes/group_row.html"
  - "app/templates/accounts/list.html"
  - "app/templates/accounts/partial_cards.html"
  - "app/templates/ads/form.html"
  - "app/templates/ads/includes/ad_card.html"
  - "app/templates/ads/includes/sched_card.html"
  - "app/templates/ads/includes/sched_count_rule.html"
  - "app/templates/ads/partials/sched_delete_response.html"
  - "app/templates/components/form_wrapper.html"
  - "app/templates/components/modal.html"
  - "app/templates/history/includes/history_card.html"
  - "app/templates/includes/htmx_error_banner.html"
  - "app/templates/includes/notice_area.html"
  - "tests/conftest.py"
  - "tests/test_pages/test_confirm_delete_transport.py"
  - "tests/test_pages/test_editor_schedules.py"
  - "tests/test_pages/test_history_retry.py"
  - "tests/test_pages/test_htmx_gates.py"
  - "tests/test_pages/test_htmx_response_layer.py"
  - "tests/test_pages/test_hx_location_destinations.py"
  - "tests/test_pages/test_identifier_bounds.py"
  - "tests/test_pages/test_impersonation.py"
  - "tests/test_pages/test_notices_channel.py"
  - "tests/test_pages/test_origin_guard_on_destructive_routes.py"
  - "tests/test_pages/test_prose_names_live_symbols.py"
  - "tests/test_pages/test_shell.py"
  - "tests/test_planning/__init__.py"
  - "tests/test_planning/test_deferred_items_are_idempotent.py"
  - "tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py"
  - "tests/test_planning/test_requirement_completion_follows_verification.py"
  - "tests/test_planning/test_state_progress_matches_roadmap.py"
  - "tests/test_planning/test_the_walkthrough_cannot_self_certify.py"
  - "tests/test_templates/test_components.py"
  - "tests/test_templates/test_htmx_inventory.py"
  - "tests/test_templates/test_htmx_markup_gates.py"
  - "tests/test_templates/test_walkthrough_anchors.py"
behavior_unverified: 1
overrides_applied: 1
covered_digest: "v1:sha256:db7f5c8629de50be38f4bdd0ddb88327dd4446272b50daa7f6ce5937288c0e20"
re_verification:
  previous_status: gaps_found
  previous_score: 8/14
  previous_verified: 2026-09-10T08:30:00Z
  round: 10
  gaps_closed:
    - >-
      `V9-01` — КОНЪЮНКЦИЯ «ГЛАГОЛ ПРИМЕНЁН К ЭТОЙ ЗАГОТОВКЕ» ТЕПЕРЬ УТВЕРЖДАЕТСЯ
      ИСПОЛНЕНИЕМ, И ЭТО ПРОВЕРЕНО СОБСТВЕННЫМ ПРОГОНОМ ВЕРИФИКАТОРА, А НЕ ПРИНЯТО
      ПО СВОДКЕ 10-41. Правило
      `test_the_lever_clears_both_failure_banners_by_execution`
      (`tests/test_templates/test_components.py:4455`) исполняет метод `show()`
      ЖИВОГО рычага в интерпретаторе JS через общий гарнир проекта
      (`_run_modal_lifecycle` → `run_node_script`) и спрашивает вердикт У КАЖДОГО
      УЗЛА отдельно. ЗАМЕР ВЕРИФИКАТОРА НА ФОРМЕ, ЭКВИВАЛЕНТНОЙ «СНЯТИЕ ПЕРЕЕХАЛО
      В `hide()`» (кусок снятия вырезан из выражения `x-data`): вердикт
      `cleared = {'htmx-failure-server': False, 'htmx-failure-network': False}`,
      `raised: True` — то есть метод отработал, а заготовки НЕ сняты, и правило
      КРАСНЕЕТ. Форма (B) матрицы девятого круга (обе заготовки названы, глагол на
      площадке приземления) несёт СВОЙ отдельный отказ и свой контроль
      `test_control_a_lever_that_targets_the_wrong_node_reddens` — прогон
      верификатора по отбору правил снятия: `3 passed, 85 deselected`.
    - >-
      `V9-01`, ВТОРАЯ ПОЛОВИНА: УТВЕРЖДЕНИЕ В ПРОДУКТОВОМ ФАЙЛЕ ПРИВЕДЕНО К ТОМУ,
      ЧТО ПРАВИЛО ДЕЙСТВИТЕЛЬНО ПРИНУЖДАЕТ. `app/static/css/app.css:1018-1035`
      прочитано верификатором целиком: прежняя формулировка «свойство стережётся
      правилом по ИСХОДНИКУ рычага» помечена опровергнутой по идиоме D-30/D-32, а
      действующая называет ЧЕТЫРЬМЯ пунктами — что принуждается (исполнение),
      где живёт (правило поимённо), что осталось от подстрочного слоя
      (антивакуум, конъюнкции НЕ утверждает) и что не принуждается ничем машинным
      (ОТРИСОВКА, шаг 4.4 обхода). Докстринг `_lever_clearing_findings`
      (`tests/test_pages/test_shell.py:3771`) сужен теми же словами: «утверждает
      РОВНО ДВЕ вещи, а не три». Класс «запись шире дерева» здесь снят.
    - >-
      `V9-02` — РЕГРЕССИЯ ОХВАТА ЦЕПИ ПРЕДКОВ СНЯТА, ЗАМЕР ВЕРИФИКАТОРА НА БОЕВОМ
      ДЕРЕВЕ ИСПОЛНЕНИЕМ: `_selector_targets('body.night')` → `('body',)`,
      `(':root.dark')` → `(':root',)`, `('[data-shell].collapsed')` →
      `('[data-shell]',)`, `('[data-body].wide')` → `('[data-body]',)`,
      `('body:hover')` → `('body',)`, `('div body.night')` → `('body',)`. ВСЕ ТРИ
      ВОСПРОИЗВЕДЁННЫХ ДЕВЯТЫМ КРУГОМ СЛУЧАЯ ОТДАЮТ ПРЕДКА. ⚠️ И СТРОГОСТЬ, РАДИ
      КОТОРОЙ ПЕРЕХОД ДЕЛАЛСЯ, СОХРАНЕНА — ЭТО ПРОВЕРЕНО ОТДЕЛЬНО, А НЕ ПРИНЯТО
      НА ВЕРУ: `('bodyish')` → `()`, `('[data-bodyx]')` → `()`, `('#body')` →
      `()`, `('.body')` → `()`, а `('[data-body]')` по-прежнему НЕ есть `body`
      (половина `WR-07` седьмого круга цела). Ожидания выписаны ЛИТЕРАЛЬНОЙ
      таблицей `_SELECTOR_TARGET_EXPECTATIONS` (15 строк) с замером «до правки»
      комментарием у каждой строки, и параметризованный контроль
      `test_control_a_qualified_ancestor_is_still_addressed` перебирает все 15.
    - >-
      `V9-03` — ВСЕ 21 ЗАПИСЬ РЕЕСТРА ЯКОРЕЙ НАБИРАЮТСЯ В КОНСОЛИ, И ЭТО СНЯТО
      ЗАМЕРОМ ВЕРИФИКАТОРА ПО КАЖДОЙ ЗАПИСИ, А НЕ ПО ЗЕЛЕНИ ПРАВИЛА. Консольные
      формы (после подстановки объявленного заполнителя цифрой) выписаны
      верификатором ЦЕЛИКОМ: `[role="dialog"]`, `[id^="sched-del-"]`,
      `[id^="sched-del-"][role="dialog"]`, `[data-sched-card]`, `#sched-1` (×2),
      `#sched-del-1`, `#sched-count` (×2), `#ad-del-1`, `#text`, `#text-counter`,
      `.media-tile__remove`, `#file-input`, `.modal__panel`, `.modal__overlay`,
      `[x-ref="cancel"]`, `.modal__panel button[type="submit"]`, `.form-busy`,
      `#notice`, `#notice-alert` — 21 из 21, ни одного прозаического хвоста, ни
      одной фигурной скобки. Заполнитель перестал быть прозой: он объявляется
      полем `placeholder` и подставляется `PLACEHOLDER_SUBSTITUTE = "1"`, а
      необъявленный заполнитель есть ОТДЕЛЬНОЕ основание отказа. Окно 68
      `.planning/WINDOWS.md` несёт вердикт `fixed` (снят 2026-09-10T13:17).
    - >-
      `V9-04` — КРУГОВОЙ КОНТРОЛЬ РАЗВЕДЁН НА ДВА ИСТОЧНИКА. Прочитан
      `ANCESTOR_TRAP_CANON` (`tests/test_pages/test_shell.py:5095`, 19 записей):
      НИ ОДНО его поле не вычисляется из `CONTAINING_BLOCK_PROPERTIES`,
      `STACKING_CONTEXT_PROPERTIES`, `LAYER_PROPERTY` или `INERT_DECLARED_VALUES`
      — все имена, начальные и ловящие значения выписаны литералами. Контроль
      `test_control_a_typo_inside_a_group_list_leaves_the_trap_unseen_and_reddens_the_agreement`
      предъявляет цикличность ТРЕМЯ половинами: (а) на настоящих перечнях правило
      согласия пусто; (б) на испорченном — РОВНО ДВЕ находки, называющие ОБЕ
      стороны; (в) гейт с тем же испорченным перечнем на отравленной таблице даёт
      НОЛЬ находок. Довод плана 10-38 против литеральной копии не отменён, а
      закрыт: расхождение сделано ГРОМКИМ с обеих сторон. Окно 69 — `fixed`
      (2026-09-10T12:57).
    - >-
      `V9-05` — АРТЕФАКТ БЕЗОПАСНОСТИ ФАЗЫ СУЩЕСТВУЕТ, И ЭТО ЧЕСТНОЕ ИЗЪЯТИЕ, А НЕ
      ИМИТАЦИЯ АУДИТА. `10-SECURITY.md` несёт `status: waived` (третье значение в
      словаре проекта, заведённое этой фазой), `threats_open: 12` — ПЕРВОЕ
      НЕНУЛЕВОЕ значение поля в проекте, `waiver_scope: phase-10-only`,
      `waiver_applies_to_milestone: false`, `waived_by: chubav`,
      `secure_phase_command_run: false`. Гэп требовал ОДНО ИЗ ДВУХ — прогон
      `/gsd-secure-phase 10` ЛИБО явную запись изъятия с основанием; взята вторая
      ветвь, и взята она владельцем на блокирующем чекпойнте задачи 2 плана
      10-44. ⚠️ ОСНОВАНИЕ ИЗЪЯТИЯ ПЕРЕПРОВЕРЕНО ВЕРИФИКАТОРОМ СВОИМ ЗАМЕРОМ:
      `git rev-list --count 7c1041d..HEAD` → 311 коммитов ветви фазы;
      `git log 7c1041d..HEAD -- pyproject.toml uv.lock wa_worker/package.json
      wa_bridge/package.json` → НОЛЬ. То есть двенадцать строк цепи поставок
      действительно не имеют предмета в этой фазе, и ноль в `threats_open` был бы
      ложью, а двенадцать — замером.
    - >-
      `V9-06` — ПОВТОРЯЕМОСТЬ ПРАВКИ ОТЛОЖЕННОГО ДОСТИГНУТА ФОРМОЙ ФАЙЛА, А НЕ
      РАСПИСАНИЕМ. Замер верификатора по дереву: `deferred-items.md` несёт якорь
      `<!-- deferred-items:end -->` РОВНО ОДИН РАЗ (строка 420), разделов,
      именованных планом, ПЯТЬ (`10-25`, `10-30`, `10-32`, `10-39`, `10-46`), и
      ни один не удвоен. Правило
      `tests/test_planning/test_deferred_items_are_idempotent.py` принуждает три
      признака (удвоение раздела, отсутствие якоря, удвоение якоря) семью
      правилами, из которых четыре — контроли зубов; граница «якорь требуется у
      ЖИВОГО отложенного, а удвоение судится у ВСЕХ файлов» названа в докстринге
      модуля. Окно 70 — `fixed` (2026-09-10T17:32).
  gaps_remaining: []
  regressions: []
  corrections:
    - >-
      ⚠️ БЛОКЕР `CR-01` ДЕВЯТОГО КРУГА РЕВИЗИИ ВОСПРОИЗВЕДЁН ВЕРИФИКАТОРОМ
      ИСПОЛНЕНИЕМ И ПОНИЖЕН ДО ПРЕДУПРЕЖДЕНИЯ — ПОНИЖЕНИЕ ОБОСНОВАНО ЗАМЕРОМ, А НЕ
      СНИСХОДИТЕЛЬНОСТЬЮ. Дефект настоящий: одна фигурная скобка в строчном
      комментарии тела `show()` уводит границу разбора в соседний метод, и
      `_lever_clearing_findings` отдаёт `()` на теле, где снятие целиком живёт в
      `hide()`. Воспроизведено дословно синтетикой ревизии. НО тот же прогон
      верификатора показал, что ПРЕДМЕТ гэпа `V9-01` стережёт ВТОРОЙ слой, и он
      на этой самой мутации КРАСНЕЕТ (вердикт `cleared` обеих заготовок `False`).
      Правило по регламенту гейтов качества тестов гласит: блокер, если вакуумное
      правило ЕДИНСТВЕННОЕ, покрывающее предмет; предупреждение, если предмет
      покрыт и другим действующим правилом. Здесь — второе. Сама ревизия это
      смягчающее назвала («Дефектен антивакуумный слой, а не оба слоя сразу»), и
      настоящий отчёт с ней не спорит, а перепроверяет её.
    - >-
      ⚠️ ЦЕНА ПОНИЖЕНИЯ НАЗЫВАЕТСЯ ПРЯМО И НЕ СГЛАЖИВАЕТСЯ: ДОКСТРИНГ
      `_lever_show_body` ГОВОРИТ О ДЕРЕВЕ НЕПРАВДУ. Он утверждает: «Сегодня таких
      скобок в теле нет, и контроль строчного комментария утверждает это прямо, а
      не полагается на удачу». Замер верификатора: контроль
      `test_control_a_lever_that_hides_its_clearing_in_a_line_comment_reddens`
      утверждает `"{" not in line_comment` про СОБСТВЕННУЮ синтетическую строку
      (`tests/test_pages/test_shell.py:4174`), а о живом рычаге не говорит
      ничего. Свойство держится тем, что строчных комментариев в теле `show()`
      живого рычага сегодня НОЛЬ (замер верификатора исполнением), — то есть
      РАСПИСАНИЕМ, ровно как это уже случилось с окном 70. Это тот же класс
      «запись шире дерева», за который фаза получила круги 3, 4, 5, 6, 8 и 9, и
      он записан ниже предупреждением `W-01`, а не растворён в зелени.
overrides:
  - must_have: >-
      Панель закрывается существующим Alpine (`x-on:htmx:after-request`) — новых строк JS
      фаза не добавляет (машинно: диффом по `app/static/js/` и шаблонам).
    reason: >-
      ⚠️ ЗАПИСЬ ПЕРЕНЕСЕНА ДОСЛОВНО ИЗ ДЕВЯТОГО КРУГА И НАСТОЯЩИМ КРУГОМ НЕ
      ПЕРЕОТКРЫВАЕТСЯ И НЕ ПЕРЕСУЖИВАЕТСЯ. Прочтение D-13, записанное в
      `10-CONTEXT.md:197-210` ДО правки: «критерий запрещает НОВЫЕ СУЩНОСТИ, а не
      правку существующих». ПЕРЕЗАМЕР ВЕРИФИКАТОРА НА HEAD ПОДТВЕРЖДАЕТ ОБЕ
      половины записи: `git diff 7c1041d..HEAD -- app/static/js/` ПУСТ; диффом по
      `app/` восьмая партия (`188f34b..HEAD`) тронула ТРИ продуктовых файла и
      НИ ОДНОЙ исполняемой строки — только тела комментариев (105 строк
      добавлено, 13 снято, все внутри комментария; проверено построчным разбором
      диффа). Гейт `test_criterion_three_holds_by_the_numbers` на HEAD зелен,
      отбор непуст.
    accepted_by: "chubav"
    accepted_by_provenance: >-
      ИМЯ И ДАТА ПРИШЛИ ОТВЕТОМ ВЛАДЕЛЬЦА НА БЛОКИРУЮЩИЙ ОСТАНОВ задачи 1 плана
      10-40. ⚠️ ДАТА — ТА, КОТОРУЮ НАЗВАЛ ВЛАДЕЛЕЦ (`2026-09-09`), и она не
      правится под дату внесения записи. Расхождение названо, а не сглажено.
    accepted_at: "2026-09-09"
    accepted_branch: "accept-reading"
    accepted_branches_rejected: ["reject-reading"]
    covers: >-
      ПРИНИМАЕТСЯ ВЕСЬ ПЕРЕЗАМЕРЕННЫЙ ПЕРЕЧЕНЬ — 51 строка JS в
      `app/templates/ads/form.html` и `app/templates/components/modal.html`.
      Граница отступления — правка ТЕЛА метода уже существующего объекта
      клиентского состояния и правка выражения УЖЕ СУЩЕСТВУЮЩЕГО атрибута
      события; заведение НОВОЙ сущности настоящей записью НЕ принимается и
      краснит гейт четырёх чисел. ⚠️ ЦЕНА: граница «новая сущность против правки
      существующей» есть действующее ПРАВИЛО ПРОЕКТА, а не разовое разрешение.
      Восьмая партия эту границу НЕ СДВИНУЛА: исполняемых строк она не тронула
      вовсе.
deferred:
  - truth: >-
      Сеанс QR-входа Telegram привязан к пользователю, а подставленный чужой
      `session_id` отвергается (`CR-01` ШЕСТОГО круга ревизии)
    addressed_in: "Phase 13"
    evidence: >-
      Критерий 3 Фазы 13 роадмапа дословно: «Состояние мастера живёт на сервере,
      `session_id` едет скрытым полем, и серверная проверка владения сессией
      СОХРАНЕНА — подставленный чужой `session_id` отвергается (машинно)».
      Совпадение прямое и предметное. ⚠️ ФАЗОЙ 10 НЕ СОЗДАН И ЕЮ НЕ ТРОНУТ:
      `app/pages/accounts.py` в диффе фазы диапазона `:226-358` не несёт;
      восьмая партия этого файла не касалась вовсе.
  - truth: >-
      Девять отказов по происхождению, собранных помимо слоя ответа, приведены к
      единой форме (`WR-04` ПЯТОГО круга, окно 63 `.planning/WINDOWS.md`)
    addressed_in: "Phase 11"
    evidence: >-
      Окно 63 разводит снятие на двух адресатов ЯВНО: «Фаза 11 „Массовый перевод
      разделов письма“ (критерий 1 роадмапа) снимает выходы РАБОТОЙ; ФОРМА,
      которой отказ по происхождению поедет после перевода, есть решение
      ВЛАДЕЛЬЦА». Машинный гейт от отсрочки не слабеет — реестр
      `OWN_RESPONSE_EXITS` с объявленным числом 9 краснеет С ИМЕНЕМ.
  - truth: >-
      Семь входов страничного слоя расходятся с запертым решением D-01: форму
      отказа выбирает фреймворк, а не класс действия (окно 51
      `.planning/WINDOWS.md`, вердикт `open`)
    addressed_in: "Phase 11"
    evidence: >-
      Окно 51 дословно: «оно снимается вместе с переносом границы внутрь
      обработчиков, а перенос осмыслен только вместе с массовым переводом
      маршрутов на слой ответа — Фаза 11, критерий 3 („ошибка валидации
      возвращает 422, перерисовывает форму и возвращает введённое эхом“), порядок
      внутри фазы ставит `schedules` первым». ⚠️ РЕШЕНИЕ О ЛЕГИТИМНОСТИ
      расхождения остаётся у ВЛАДЕЛЬЦА и отсрочкой не снимается; машинный гейт
      `test_no_divergence_claims_a_decision_the_owner_did_not_make` держится.
advisory: []
behavior_unverified_items:
  - truth: >-
      `G-10-4`: плашка аварии ВИДНА поверх панели подтверждения, а не спрятана
      под ней (шаг 4.4 обхода — единственный ПРОВАЛЕННЫЙ шаг `10-UAT.md`)
    test: >-
      Проверка 4 обхода, шаги 4.1-4.6 на живом стенде: открыть панель
      подтверждения удаления расписания, вызвать отказ (Offline), нажать
      «Удалить» и посмотреть, ЧТО ВСТАЛО ПОВЕРХ.
    expected: >-
      Плашка аварии видна поверх панели; панель остаётся открытой (D-12); кнопка
      подтверждения снова нажимаема; повтор возможен без переоткрытия панели.
    why_human: >-
      ⚠️ ОБА ПРЕЖНИХ ОСНОВАНИЯ ДЕРЖАТЬ ЭТОТ ПУНКТ ЗА ЧЕЛОВЕКОМ СНЯТЫ ВОСЬМОЙ
      ПАРТИЕЙ, А ТРЕТЬЕ — НЕТ, И ИМЕННО ОНО НЕСУЩЕЕ. Девятый круг называл три
      причины: правила утверждают ОБЪЯВЛЕНИЕ слоя, а не отрисовку; одно из двух
      правил потеряло охват (`V9-02`); второе не выражает конъюнкции (`V9-01`).
      Второе и третье закрыты и перепроверены настоящим кругом исполнением.
      ПЕРВОЕ ОСТАЁТСЯ И НЕПОДВИЖНО: ни одно машинное правило дерева не
      отрисовывает документ. Блок подъёма
      `app/static/css/app.css:1075-1085` объявляет `position: fixed; z-index: 70`
      при наибольшем прочем слое 60, снятие обеих заготовок при открытии панели
      утверждено ИСПОЛНЕНИЕМ — но ЧТО БРАУЗЕР НАРИСОВАЛ, не наблюдал никто ПОСЛЕ
      правок планов 10-33 и 10-35. Замер прода 2026-09-09 (ДО них) давал плашку
      `position: static`, `z-index: auto`, прямоугольник целиком выше окна
      (`top: -322` при `scrollY: 418`), верхним узлом в её точке —
      `DIV.modal__overlay`.
coincidental_reliance_items:
  - truth: >-
      Критерий 1: все 18 мест подтверждения РАБОТАЮТ через htmx одной правкой
      `components/modal.html`
    reason: undeclared-precondition
    harden: >-
      Связь «имя события ТРИГГЕРА == имя события панели-СЛУШАТЕЛЯ» цела, но
      НИЧЕМ НЕ ПРИНУЖДЕНА: инвентарь считает ЧИСЛО диспетчеризаций и ЧИСЛО
      различных имён, а правила, СЛИЧАЮЩЕГО имя триггера с именем слушателя, в
      дереве нет (`MODAL_EVENT_RE`, `tests/test_templates/test_components.py`).
      Триггер, диспетчеризующий имя, которого не слушает ни одна панель, прошёл
      бы ВСЕ гейты инвентаря молча, а место подтверждения было бы мёртвым.
      Ужесточение: правило парности имён. ⚠️ ПОМЕТКА СОВЕЩАТЕЛЬНАЯ — ни счёта, ни
      вердикта она не меняет и пунктом человеческой проверки не становится.
      ВОСЬМАЯ ПАРТИЯ ЭТОГО НЕ ТРОНУЛА, и пометка переносится ТРЕТИЙ круг подряд.
human_verification:
  - test: >-
      Шаг 4.4 обхода `10-UAT.md` — ЕДИНСТВЕННЫЙ ПРОВАЛЕННЫЙ ШАГ, перезамер после
      правок планов 10-33 и 10-35.
    expected: "Плашка аварии видна ПОВЕРХ панели; панель открыта; повтор без переоткрытия."
    why_human: >-
      Правила утверждают ОБЪЯВЛЕНИЕ слоя и ИСПОЛНЕНИЕ снятия, а не ОТРИСОВКУ; у
      исполнителей планов браузера нет, и сводка 10-33 записала это сама. Правка
      таблицы стилей в дереве есть и проверена чтением — ПЕРЕВЕРНУЛА ЛИ ОНА
      наблюдение 2026-09-09, не знает никто.
  - test: >-
      Шаг 2.8 того же обхода — плашка обрыва связи при не доехавшем follow-up GET
      (`unobserved_steps`).
    expected: "Плашка появилась и называет расхождение экрана с сервером второй фразой."
    why_human: >-
      Половина (A) уже наблюдена проверкой 4 на НАСТОЯЩЕМ обрыве; ненаблюдённой
      осталась половина при обрыве, сделанном подменой `XMLHttpRequest`:
      `abort()` поднимает не то событие, что настоящий обрыв транспорта.
  - test: >-
      ПОДПИСЬ ЧЕЛОВЕКА НА ПРИЁМКЕ под обходом 2026-09-09 — не перепрохождение, а
      приёмка уже снятых признаков.
    expected: >-
      Либо человек подтверждает отметки шести проверок и артефакт получает
      терминальное состояние, либо называет, какие проверки требуют
      перепрохождения человеческими руками.
    why_human: >-
      ⚠️ ОТСТУПЛЕНИЕ НАЗВАНО САМИМ АРТЕФАКТОМ: наблюдал АГЕНТ через
      chrome-devtools MCP; `workflow.live_dom_uat` равен `false`; право дано
      репликой в сессии. `10-UAT.md` на HEAD по-прежнему несёт `status: partial`
      (проверено верификатором чтением шапки), и
      `test_the_walkthrough_cannot_self_certify.py` этот запрет принуждает
      машинно. План 10-46 приписал к артефакту три незакрытых человеком пункта
      БЕЗ единой отметки прохождения — то есть партия расширила ЧЕСТНОСТЬ записи,
      а не закрыла пункт.
  - test: >-
      РЕШЕНИЕ ВЛАДЕЛЬЦА, А НЕ ПРОВЕРКА СТЕНДА: 200 запретов фазы из 239 стоя́т
      НЕРАЗРЕШЁННЫМИ, и это ЗАМЕР партии, а не оценка верификатора.
    expected: >-
      По каждому — либо предъявленное принуждение, либо явное человеческое
      разрешение с названной областью, либо явное признание неразрешённым.
    why_human: >-
      ⚠️ ПУНКТ СУЖЕН ВОСЬМОЙ ПАРТИЕЙ, НО НЕ ЗАКРЫТ, И РАЗНИЦУ НАДО ЧИТАТЬ ЧИСЛАМИ.
      План 10-47 предъявил 39 запретов СЕДЬМОЙ партии поимённо, и владелец на
      блокирующем чекпойнте дал РАЗРЕШЕНИЕ ветвью `permit-with-record` с
      формулировкой «РАЗРЕШЕНИЕ ДЕЛАЕТ ЗАПРЕТ НЕ БЛОКИРУЮЩИМ, НО НЕ СОБЛЮДЁННЫМ»
      и машинно читаемой областью (`permit_scope: seventh-batch-only`,
      `permit_applies_to_phase: false`, `permit_applies_to_milestone: false`).
      ОСТАЛЬНОЕ НАЗВАНО ЧИСЛАМИ В ТОЙ ЖЕ ШАПКЕ И НЕ СМЯГЧЕНО:
      `prohibitions_unresolved_outside_batch: 167`,
      `prohibitions_unresolved_eighth_batch: 33`,
      `prohibitions_unresolved_total: 200`, и —
      `prohibitions_fully_enforced: 0`, то есть НИ ОДИН из 25 запретов с
      дескриптором `test` не принуждается целиком. Регламент фазовых гейтов велит
      закрывать такие пункты FAIL-CLOSED: дескриптор `test` без принуждения есть
      НЕПРОВЕРЕННЫЙ запрет, и зелёным он не становится. Флаг снимается решением,
      а не замером верификатора.
---

# Phase 10: Рычаг `components/modal.html` — Verification Report (ДЕСЯТЫЙ круг)

**Phase Goal:** подтверждение удаления идёт через htmx во всех 18 местах одной правкой одного
файла (`app/templates/components/modal.html`), и после N удалений в документе не остаётся N
мёртвых диалогов
**Verified:** 2026-09-10T21:40:00Z
**Status:** human_needed
**Score:** 14/14 (8 истин контракта роадмапа — перенесены закрытыми; 6 гэпов девятого круга —
закрыты ВСЕ ШЕСТЬ)
**Re-verification:** Да, круг 10 — после ВОСЬМОЙ партии закрытия гэпов (планы 10-41…10-47) и
ДЕВЯТОГО круга ревизии. Заменяет отчёт от 2026-09-10T08:30 (`gaps_found`, 8/14). HEAD `5832701`.

## Вердикт одной фразой, и он неудобен обеим сторонам

**ЦЕЛЬ ФАЗЫ ДОСТИГНУТА. ВСЕ ШЕСТЬ ГЭПОВ ЗАКРЫТЫ. ФАЗА ВСЁ ЕЩЁ НЕ ЗАКРЫТА — И ОСТАВШЕЕСЯ
ПРИНАДЛЕЖИТ ЧЕЛОВЕКУ, А НЕ ИСПОЛНИТЕЛЮ.** Это первый круг фазы, где красного нет вовсе:
машинных гэпов не осталось ни одного, и никакая следующая партия закрытия гэпов их не найдёт,
потому что находить нечего. Оставшиеся четыре пункта — три шага стенда и одно решение владельца
о запретах — суть ровно то, чего машина не умеет, и записаны они не как «почти закрыто», а как
незакрытое.

⚠️ **ЧТО ЭТОТ КРУГ ПОНИЗИЛ, И ПОНИЖЕНИЕ НАЗЫВАЕТСЯ ПРЯМО.** Девятый круг ревизии
(`10-REVIEW.md`, `5832701`) поднял блокер `CR-01`. Верификатор его ВОСПРОИЗВЁЛ — и понизил до
предупреждения `W-01`, потому что собственный прогон показал: предмет, ради которого правило
существует, стережёт ВТОРОЙ слой, и он на той же мутации краснеет. Основание понижения —
регламент гейтов качества тестов, а не снисходительность; развёрнуто в разделе
«Anti-Patterns Found».

## Что этот круг измерил СВОИМИ РУКАМИ

Ни одно число ниже не взято из SUMMARY, из `10-REVIEW.md` и из отметок обхода — кроме тех
случаев, где источник назван прямо.

**Замер 1 — блокер `CR-01` ВОСПРОИЗВЕДЁН исполнением, и рядом воспроизведено то, чего ревизия не
мерила: компенсирующий слой.**

```
_lever_clearing_findings(<тело со скобкой в строчном комментарии, снятие в hide()>) -> ()          ← ЗЕЛЕНО, дефект
_lever_clearing_findings(<то же тело БЕЗ скобки в комментарии>)                    -> 2 находки    ← КРАСНО
_banner_clearing_verdict(<выражение x-data, снятие вырезано из show()>)
    -> cleared={'htmx-failure-server': False, 'htmx-failure-network': False}, raised=True           ← КРАСНО
```

Третья строка и есть предмет: тело, где снятие переехало из `show()`, правило ИСПОЛНЕНИЯ
краснит. Дефектен антивакуумный слой, а не свойство и не оба слоя.

**Замер 2 — регрессия охвата цепи предков (`V9-02`) СНЯТА, и строгость при этом цела.**

```
_selector_targets('body')                  -> ('body',)          _selector_targets('bodyish')      -> ()
_selector_targets('body.night')            -> ('body',)          _selector_targets('[data-bodyx]') -> ()
_selector_targets(':root.dark')            -> (':root',)         _selector_targets('#body')        -> ()
_selector_targets('[data-shell].collapsed')-> ('[data-shell]',)  _selector_targets('.body')        -> ()
_selector_targets('[data-body].wide')      -> ('[data-body]',)   _selector_targets('[data-body]')  -> ('[data-body]',)
```

Левая колонка — три случая, которыми девятый круг опроверг связь; все три отдают предка.
Правая — половина `WR-07`, ради которой сличение делалось строгим; она держится.

**Замер 3 — все 21 консольные формы реестра якорей выписаны и прочитаны поштучно.**
Ни фигурной скобки, ни прозаического хвоста; четыре заполнителя объявлены полем и подставляются
цифрой. Перечень целиком — в записи `gaps_closed` шапки.

**Замер 4 — основание изъятия безопасности перепроверено на дереве, а не принято по записи.**
`git rev-list --count 7c1041d..HEAD` → **311**; `git log 7c1041d..HEAD -- pyproject.toml uv.lock
wa_worker/package.json wa_bridge/package.json` → **0**. Двенадцать строк цепи поставок предмета
в этой фазе не имеют.

**Замер 5 — восьмая партия ПРОДУКТА НЕ ТРОНУЛА, и это несущее для сохранности цели.**
`git diff --stat 188f34b..HEAD -- app/` → три файла, 105 строк добавлено, 13 снято. Построчный
разбор диффа: **ни одной исполняемой строки** — только тела комментариев. То есть проверенное
девятым кругом поведение продукта настоящей партией не сдвинуто ни на символ, и цель фазы
переносится закрытой не по доверию, а потому, что менять её было нечем.

**Замер 6 — собственный прогон четырёх стерегущих модулей на HEAD.**
`uv run pytest tests/test_pages/test_shell.py tests/test_templates/test_walkthrough_anchors.py
tests/test_planning/ tests/test_pages/test_prose_names_live_symbols.py -q` → **282 passed**,
exit 0, 683 с.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Критерий 1: все 18 мест подтверждения работают через htmx одной правкой `components/modal.html`; инвентарь утверждён ЧИСЛОМ | ✓ VERIFIED (coincidental-reliance) | `MODAL_PLACES = 18` снято исполнением; `test_modal_site_inventory` и `test_modal_guard_is_inherited_by_every_consumer` зелены на HEAD (`3 passed, 85 deselected`). Пометка совещательная — см. `coincidental_reliance_items` |
| 2 | Критерий 2, машинная половина: ответ несёт второй OOB-узел `hx-swap-oob="delete"` по `id` панели | ✓ VERIFIED | Замер верификатора по дереву: `ads/partials/sched_delete_response.html:101-102` и `account_groups/partials/delete_response.html:99-100` — по два узла на ответ (строка + панель) |
| 3 | Критерий 2, поведенческая половина: после N удалений в документе не остаётся N мёртвых диалогов | ✓ VERIFIED | Перенесено закрытым из круга 9 по отметке обхода 2026-09-09 С ЧИСЛАМИ: `[role="dialog"]` 4 → 3, несущий счёт `[id^="sched-del-"][role="dialog"]` 3 → 2, `getElementById('sched-139')` и `getElementById('sched-del-139')` — ОБА `null`, заходов в диалог 0 при 18 нажатиях Tab. Восьмая партия продукта не тронула (замер 5) |
| 4 | Критерий 3: панель закрывается существующим Alpine, новых строк JS фаза не добавляет | PASSED (override) | `Override: прочтение D-13 из 10-CONTEXT.md:197-210 — accepted by chubav on 2026-09-09`. Перезамер на HEAD: `git diff 7c1041d..HEAD -- app/static/js/` пуст; гейт четырёх чисел зелен |
| 5 | Критерий 4: все 10 потребителей продолжают удалять без JS — панель остаётся формой POST с непустым `action` | ✓ VERIFIED | Гейты деградации Фазы 8 держат наследование; прогон модуля компонентов на HEAD зелен, отбор непуст |
| 6 | `DEF-09-02` закрыт на всех 18 местах: «Отмена» панели отменяет и летящий запрос | ✓ VERIFIED | Перенесено закрытым из круга 9; файл `app/templates/components/modal.html` восьмой партией правлен только в комментариях |
| 7 | `CR-03`: инлайн-скрипт редактора переживает переисполнение после перехода `HX-Location` | ✓ VERIFIED | Перенесено закрытым из круга 9 (наблюдено в браузере: ранней `SyntaxError` нет, `#text-counter` 30→31→32→33→34) |
| 8 | Граница величины идентификатора одна и с одним владельцем на пять входов `app/pages/schedules.py` | ✓ VERIFIED | Перенесено закрытым из круга 9; `app/pages/identifiers.py` на дереве, восьмой партией не тронут |
| 9 | `V9-01`: правило требует СНЯТИЯ ОБЕИХ заготовок, а не двух независимых упоминаний | ✓ VERIFIED | Замер 1. Конъюнкция утверждается ИСПОЛНЕНИЕМ метода `show()` живого рычага; на форме «снятие переехало» вердикт `cleared` обеих заготовок `False`. Запись в `app.css:1018-1035` сужена до факта четырьмя пунктами |
| 10 | `V9-02`: квалификатор на селекторе предка больше не прячет предка от гейта | ✓ VERIFIED | Замер 2. Все три воспроизведённых случая отдают предка; строгость `WR-07` цела; литеральная таблица из 15 строк с параметризованным контролем |
| 11 | `V9-03`: все 21 запись реестра якорей набираются в консоли | ✓ VERIFIED | Замер 3. 21 из 21 консольных форм — законные выражения селектора; заполнитель объявляется полем, а не угадывается. Окно 68 — `fixed` |
| 12 | `V9-04`: описка в имени свойства внутри группы больше не проходит зелёной | ✓ VERIFIED | `ANCESTOR_TRAP_CANON` (19 записей) выписан ЛИТЕРАЛАМИ и ни одним полем не вычисляется из проверяемых перечней; контроль предъявляет цикличность тремя половинами, включая «гейт с испорченным перечнем даёт НОЛЬ». Окно 69 — `fixed` |
| 13 | `V9-05`: `10-SECURITY.md` фазы существует при `security_enforcement: true` | ✓ VERIFIED | Артефакт на дереве: `status: waived`, `threats_open: 12` (первое ненулевое в проекте), `waiver_scope: phase-10-only`, `waived_by: chubav`, `secure_phase_command_run: false`. Гэп требовал ЛИБО прогона, ЛИБО явного изъятия с основанием — взята вторая ветвь владельцем. Основание перепроверено замером 4 |
| 14 | `V9-06`: повторное исполнение правки отложенного неотличимо от первого | ✓ VERIFIED | Якорь `<!-- deferred-items:end -->` в файле РОВНО один (строка 420); разделов, именованных планом, пять, ни один не удвоен; семь правил, из них четыре — контроли зубов. Окно 70 — `fixed` |
| — | `G-10-4`: плашка аварии ВИДНА поверх панели, а не спрятана под ней | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Правка в дереве есть и проверена чтением (`app.css:1075-1085`, слой 70 при наибольшем прочем 60); снятие обеих заготовок утверждено исполнением. НО ОТРИСОВКУ не воспроизводит ни одно правило, и после правок 10-33/10-35 её не наблюдал никто. В счёт НЕ входит |

**Score:** 14/14 истин верифицировано (1 — принята записью отступления; 1 — присутствует, но
поведение не проверено, и в счёт не входит)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Сеанс QR-входа Telegram привязан к пользователю (`CR-01` шестого круга ревизии) | Phase 13 | Критерий 3 Фазы 13 дословно требует серверной проверки владения сессией; фазой 10 предмет не создан и не тронут |
| 2 | Девять отказов по происхождению приведены к единой форме (`WR-04` пятого круга, окно 63) | Phase 11 | Окно 63 разводит снятие: Фаза 11 снимает выходы РАБОТОЙ, форму отказа решает ВЛАДЕЛЕЦ. Гейт `OWN_RESPONSE_EXITS` с числом 9 краснеет с именем |
| 3 | Семь входов страничного слоя расходятся с D-01 (окно 51) | Phase 11 | Окно 51 называет Фазу 11 и её критерий 3 дословно; решение о легитимности остаётся у владельца, гейт от отсрочки не слабеет |

### Advisory (New Scope, Unevidenced)

**None.** Каждая находка раздела Anti-Patterns ниже воспроизведена ИСПОЛНЕНИЕМ верификатора и
несёт команду с выводом — то есть неевидентных новоохватных находок в этом круге нет вовсе.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/templates/components/modal.html` | Рычаг всех 18 мест, снятие обеих заготовок при открытии | ✓ VERIFIED | Восьмой партией правлены ТОЛЬКО тела комментариев; исполняемых строк 0 |
| `app/static/css/app.css` | Условный подъём плашки над панелью + честная запись о том, чем свойство стережётся | ✓ VERIFIED | `:1075-1085` блок подъёма; `:1018-1035` — запись сужена до факта планом 10-41 |
| `tests/test_templates/test_components.py` | Правило снятия ИСПОЛНЕНИЕМ + три отрицательных контроля | ✓ VERIFIED | `:4455` несущее правило; контроли на формах (B), (C), (D) матрицы; прогон отбора зелен |
| `tests/test_pages/test_shell.py` | Разбор селекторов по составным/простым частям, канон ловушек, антивакуумный слой рычага | ✓ VERIFIED | `_selector_compounds`/`_compound_simple_selectors`/`_selector_targets` замерены исполнением; `ANCESTOR_TRAP_CANON` литерален. ⚠️ `_lever_show_body` несёт дефект `W-01` |
| `tests/test_templates/test_walkthrough_anchors.py` | 21 запись, набираемая в консоли, + правило набираемости | ✓ VERIFIED | 21/21 консольных форм законны. ⚠️ предикат правила различает алфавит — `W-03` |
| `tests/test_planning/test_deferred_items_are_idempotent.py` | Правило повторяемости правки отложенного | ✓ VERIFIED | 7 правил, из них 4 — контроли зубов; граница требования якоря названа в докстринге |
| `tests/test_pages/test_prose_names_live_symbols.py` | Разрешение имени прозы в живое объявление | ✓ VERIFIED | Реестр `PROSE_SYMBOL_CITATIONS`; 6 правил. ⚠️ охват — одно тело комментария, `W-06` |
| `.planning/.../10-SECURITY.md` | Запись изъятия с основанием, именем, датой и охватом | ✓ VERIFIED | 12 полей шапки, охват машинно читаем, `secure_phase_command_run: false` не скрыт |
| `.planning/.../10-PROHIBITIONS-SUBJECT.md` | Состояние 39 запретов седьмой партии как ПРЕДМЕТ решения + ответ владельца | ✓ VERIFIED | `permit_scope: seventh-batch-only`; непокрытое названо числами (167 + 33 = 200); `prohibitions_fully_enforced: 0` |
| `.planning/.../deferred-items.md` | Якорь конца перечня, раздел на план — один | ✓ VERIFIED | Якорь один (строка 420), пять разделов планов, удвоений нет |
| `.planning/.../10-UAT.md` | Обход с незакрытыми пунктами, названными незакрытыми | ✓ VERIFIED | `status: partial` держится; три пункта человека приписаны БЕЗ отметки прохождения |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `test_components.py` | `app/templates/components/modal.html` | `_modal_block()` → `_modal_xdata_expression()` → гарнир JS | WIRED | Правило читает ЖИВОЙ файл и исполняет его выражение; замер 1 |
| `test_components.py` | `tests/conftest.py` | `run_node_script` — единственный запуск интерпретатора на проект | WIRED | Ленивый ввоз `FAILURE_BANNER_HIDDEN_ATTR` из модуля шелла; ⚠️ связь модулей — `W-07` |
| `test_shell.py` | `app/static/css/app.css` | `_css_rules_of` → `_selector_targets` → `_ancestor_trap_findings` | WIRED | Квалифицированный предок снова виден (замер 2) |
| `ANCESTOR_TRAP_CANON` | `CONTAINING_BLOCK_PROPERTIES` / `STACKING_CONTEXT_PROPERTIES` | `_canon_agreement_findings` — сличение ДВУХ НЕЗАВИСИМЫХ источников | WIRED | Расхождение громкое с обеих сторон; цикличность снята |
| `WALKTHROUGH_ANCHORS` | шаблоны `app/templates/` | `missing_anchors` + `typeability_findings` | WIRED | 21 якорь разрешается в живой узел; консольная форма законна |
| `test_deferred_items_are_idempotent.py` | `.planning/**/deferred-items.md` | рекурсивный обход дерева планирования | WIRED | Живой файл найден и судится; архив вехи изъят с названным основанием |
| `10-SECURITY.md` | `.planning/config.json` | `security_enforcement: true` → запись изъятия | WIRED | Изъятие отвечает НА действующее принуждение, а не подменяет его |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `test_components.py` | `verdict["cleared"]` | вывод интерпретатора JS над ЖИВЫМ выражением `x-data` | Да — карта из двух ключей, значения меняются на мутациях | ✓ FLOWING |
| `test_shell.py` | `_selector_targets(...)` | посимвольный разбор селектора боевой таблицы стилей | Да — 15 различных ответов на 15 входов | ✓ FLOWING |
| `test_walkthrough_anchors.py` | `_console_form(anchor)` | поле `selector` реестра + объявленный `placeholder` | Да — 21 различная строка | ✓ FLOWING |
| `10-SECURITY.md` | `threats_open: 12` | разбор реестров `<threat_model>` сорока исполненных планов | Да — первое ненулевое значение поля в проекте | ✓ FLOWING |
| `10-PROHIBITIONS-SUBJECT.md` | `prohibitions_fully_enforced: 0` | поиск принуждающего правила по каждому из 25 запретов уровня `test` | Да — ноль есть ЗАМЕР, а не заглушка | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Правило снятия исполнением зелено на живом рычаге | `uv run pytest tests/test_templates/test_components.py -k "clears_both_failure_banners_by_execution or targets_the_wrong_node_reddens" -q` | `3 passed, 85 deselected` | ✓ PASS |
| Правило краснеет на переехавшем снятии | `_banner_clearing_verdict(<снятие вырезано из show()>)` | `cleared` обеих заготовок `False`, `raised: True` | ✓ PASS |
| Квалифицированный предок виден гейту | `_selector_targets('body.night')` и ещё 14 выражений | `('body',)`; 15/15 совпали с литеральной таблицей | ✓ PASS |
| 21 консольная форма якорей законна | перечисление `_console_form(a)` по реестру | 21 законное выражение, 0 скобок, 0 прозы | ✓ PASS |
| Четыре стерегущих модуля зелены на HEAD | `uv run pytest tests/test_pages/test_shell.py tests/test_templates/test_walkthrough_anchors.py tests/test_planning/ tests/test_pages/test_prose_names_live_symbols.py -q` | `282 passed`, exit 0 | ✓ PASS |
| Гейты трёх критериев роадмапа зелены | `uv run pytest tests/test_templates/test_components.py -k "modal_site_inventory or modal_guard_is_inherited or criterion_three_holds_by_the_numbers" -q` | `3 passed, 85 deselected` | ✓ PASS |
| Восьмая партия не тронула исполняемого продукта | `git diff 188f34b..HEAD -- app/` + построчный разбор | 3 файла, 105+/13−, исполняемых строк 0 | ✓ PASS |
| Отрисовка плашки поверх панели | — | стенда у верификатора нет; ни одно правило дерева документ не отрисовывает | ? SKIP → человек, шаг 4.4 |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| — | `find scripts -path '*/tests/probe-*.sh'` | ничего не найдено; ни один план и ни одна сводка партии проб не объявляет | SKIPPED (фаза проб не заводит) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| FORM-06 | 10-01…10-47 (все планы фазы) | подтверждение удаления работает через htmx во всех 18 местах одной правкой `components/modal.html`, и ответ снимает осиротевшую панель вторым OOB-узлом `hx-swap-oob="delete"` | ✓ SATISFIED, НО НЕ ОТМЕЧЕНО | Обе половины подтверждены: машинная — `MODAL_PLACES = 18` плюс два OOB-узла в двух ответах; поведенческая — отметка обхода 2026-09-09 с числами. ⚠️ КЛЕТКА `.planning/REQUIREMENTS.md:31` ОСТАЁТСЯ ПУСТОЙ, И ЭТО ВЕРНО: `test_no_requirement_is_marked_complete_before_its_phase_verification_passed` разрешает отметку ТОЛЬКО при `status: passed`, а настоящий вердикт — `human_needed`. Каждый план партии это соблюл |

**Orphaned requirements:** нет. `.planning/REQUIREMENTS.md:491` сводит Фазе 10 ровно одно
требование — `FORM-06`, и оно объявлено в шапках планов. Клетка состояния (`:136`) несёт
`Gaps Found` и подлежит переводу в `Human Needed` следующим трекингом.

⚠️ **МОЖНО ЛИ ТЕПЕРЬ ОТМЕЧАТЬ `FORM-06`.** Нет — и не потому, что предмет требования не
исполнен, а потому, что вердикт не `passed`. Отметка станет законной ровно тогда, когда четыре
пункта перечня человеческой проверки будут закрыты человеком: три шага стенда и одно решение о
запретах. Никакой партии закрытия гэпов между этим состоянием и отметкой нет.

### Anti-Patterns Found

⚠️ **ВСЕ ДЕВЯТЬ НАХОДОК НИЖЕ ВОСПРОИЗВЕДЕНЫ ВЕРИФИКАТОРОМ ИСПОЛНЕНИЕМ ЛИБО ЗАМЕРОМ ПО ДЕРЕВУ, А
НЕ ПЕРЕНЕСЕНЫ ИЗ `10-REVIEW.md`.** Файлы, их несущие, правлены восьмой партией, то есть по
регламенту эвиденс-гейта они «регрессионного охвата» и в понижении по неевидентности не
нуждаются. Ни одна не поднята до блокера, и основание у каждой названо.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tests/test_pages/test_shell.py` | 3726-3741 | `W-01` Счёт вложенности идёт ДО вырезания строчного комментария: одна `{` в `// …` уводит границу тела `show()` в соседний метод, и `_lever_clearing_findings` отдаёт `()` на теле, где снятие переехало в `hide()`. ⚠️ ОТЯГЧАЮЩЕЕ: докстринг утверждает, что контроль стережёт отсутствие таких скобок на живом рычаге; замер показал, что контроль утверждает это про СВОЮ синтетику (`:4174`) | ⚠️ Warning | Предмет `V9-01` стережёт ВТОРОЙ слой, и он на той же мутации краснеет (замер 1). По регламенту гейтов качества тестов вакуумное правило есть блокер, только если оно ЕДИНСТВЕННОЕ по предмету; здесь — не единственное. Латентность подтверждена замером: строчных комментариев в теле `show()` живого рычага **0** |
| `tests/test_pages/test_shell.py` | 3726-3735 | `W-02` Состояние строкового литерала заводится до вырезания комментариев: апостроф в `// don't forget` роняет разбор `ValueError` на ИСПРАВНОМ теле. Воспроизведено исполнением | ⚠️ Warning | Отказ громкий и с префиксом разбора, то есть от поломки снятия отличим; но краснит он на исправном дереве, и первый читатель пойдёт чинить комментарий рычага. Чинится тем же одним движением, что `W-01` |
| `app/templates/components/modal.html` | 743-752 | `W-03` Абзац «ЗАМЕР 2026-09-10 НАЗВАЛ ДЕЙСТВИТЕЛЬНЫЕ МЕСТА», внесённый планом 10-46 в ПРОДУКТОВЫЙ файл, несёт два неверных утверждения. Замер верификатора: `_with_notice` объявлен на `app/pages/htmx.py:357` — но цитата числа 357 НЕ называет (она называет 148, 375, 226); `HX_LOCATION_HEADER` объявлен на `:59`, а единственное чтение — на `:169`, то есть НИЖЕ названного числа 148, а не выше | ⚠️ Warning | ⚠️ ЭТО ТОТ ЖЕ КЛАСС «ЗАПИСЬ ШИРЕ ДЕРЕВА», ЗА КОТОРЫЙ ФАЗА ПОЛУЧИЛА КРУГИ 3-6, 8 И 9, И ОН ВНЕСЁН ПАРТИЕЙ, КОТОРАЯ ЕГО ЗАКРЫВАЛА. Не поднят до блокера потому, что цена ошибки чтения здесь мала: весь абзац существует ради тезиса «номера строк ненадёжны». Оба правила, заведённые тем же планом, к нему СЛЕПЫ: одно ищет форму `:NNN`, второе разрешает имена — числа, набранные словом «строка», не видит ни одно |
| `tests/test_templates/test_walkthrough_anchors.py` | 493-520, 557-618 | `W-04` Предикат набираемости различает АЛФАВИТ, а не выражение. Замер исполнением: `typeability_findings` на `#sched-1 - the schedule card`, `#notice sched card`, `button cancel of the panel` → `[]` во всех трёх | ⚠️ Warning | Первый вход в консоли даёт `DOMException`, ради которого правило и заведено. Тринадцать записей поймались потому, что проза была КИРИЛЛИЧЕСКОЙ. `V9-03` этим НЕ переоткрывается: сами 21 запись законны (замер 3) — слабо СТЕРЕЖЕНИЕ, а не свойство |
| `tests/test_planning/test_state_progress_matches_roadmap.py` | 111-140, 296-334 | `W-05` Правило согласия прозы с отметками опознаёт ОДНУ форму строки и стережёт один раздел из девяти. ⚠️ ЖИВОЕ РАСХОЖДЕНИЕ В НЕСТЕРЕЖЁННОМ РАЗДЕЛЕ ПОДТВЕРЖДЕНО ЗАМЕРОМ ВЕРИФИКАТОРА: `.planning/ROADMAP.md:180` объявляет «16 планов», а отметок в разделе Фазы 9 — **20** | ⚠️ Warning | Правило, заведённое против устаревшей прозы о числе планов, стои́т рядом с устаревшей прозой о числе планов и молчит. Предмет — запись Фазы 9, а не контракт Фазы 10; на цель и на `V9-06` не влияет |
| `tests/test_planning/test_state_progress_matches_roadmap.py` | 111-113, 133 | `W-06` `_PLANS_PROSE_RE` берёт ПЕРВОЕ совпадение в строке, а строка `ROADMAP.md:283` по идиоме D-30/D-32 несёт и опровергнутую цитату «40 планов, из них 34 исполнено» | ⚠️ Warning | Сегодня живое утверждение стои́т первым; ничто этого не удерживает. Летопись, дописанная ПЕРЕД действующим числом — а именно так её и дописывают в этом файле, — молча переведёт гейт на сверку исторического числа |
| `tests/test_pages/test_shell.py` | 2105-2123 | `W-07` `_BANNER_ID_ATTR_RE` не отделяет `id=` от признака, чьё имя им кончается. Замер: `_declares_banner_id('<div data-id="htmx-failure-server">', …)` → `True`, то же для `hx-target-id=` | ⚠️ Warning | Ложноположительный: ССЫЛКА на заготовку из соседнего шаблона объявится вторым ИСТОЧНИКОМ, и гейт единственности покраснеет там, где второго источника нет. Истину плана 10-45 («второй шаблон краснит гейт») не опровергает |
| `tests/test_templates/test_components.py` | 2869, 4276 | `W-08` Предметный модуль шаблонов получил ввоз из модуля страниц (`tests.test_pages.test_shell`). Партия при этом сама зафиксировала запрет такой связи в обратную сторону, и в том же дереве `test_shell.py:6127/6191` ввозит из `test_hx_location_destinations` | ⚠️ Warning | Ленивость ввоза названа мерой против падения сборки; мера неполна — ввоз исполняется в момент вызова, и поломка уровня чужого модуля станет отказом правил этого. Просится нейтральный модуль (`tests/support/`) |
| `tests/test_templates/test_components.py`, `tests/test_planning/test_state_progress_matches_roadmap.py` | 4436-4439, 501-503 | `W-09` Две вакуумные половины утверждений: `"\n" not in chunk.strip()` истинно по построению; `"1" in message` истинно всегда, потому что имя синтетического раздела начинается с `1` | ℹ️ Info | Половины не могут покраснеть никогда; правила, их несущие, имеют и действующие утверждения |
| `tests/test_planning/test_state_progress_matches_roadmap.py` | 106, 356, 484, 527 | Скан маркеров долга дал четыре вхождения `TBD` | ℹ️ Info | ⚠️ НАЗЫВАЕТСЯ ЯВНО, А НЕ ПРОПУСКАЕТСЯ МОЛЧА: это не маркеры отложенной работы, а ЛИТЕРАЛЫ формы документа — синтетическая строка `**Plans**: TBD` в фикстуре роадмапа и её цитаты в прозе правила. Отложенной работы ни одно из четырёх не объявляет, и блокером гейта маркеров долга они не являются. `FIXME`/`XXX` в файлах партии — ноль |

⚠️ **ЧЕГО В ЭТОЙ ТАБЛИЦЕ НЕТ, И ОТСУТСТВИЕ НАЗЫВАЕТСЯ.** Ни одного отключённого правила
(`pytest.mark.skip`, `xfail`) в суите — замер грепом дал НОЛЬ. Ни одного правила, сличающего
вывод системы со значением, порождённым той же системой: `ANCESTOR_TRAP_CANON`,
`_SELECTOR_TARGET_EXPECTATIONS` и записи контроля набираемости выписаны литералами, и каждое
несёт объявленное основание, почему оно не вычислено из проверяемого. Круговой контроль, который
девятый круг назвал гэпом `V9-04`, снят и на дереве не воспроизводится.

### Human Verification Required

#### 1. Шаг 4.4 обхода `10-UAT.md` — ЕДИНСТВЕННЫЙ ПРОВАЛЕННЫЙ ШАГ, перезамер после планов 10-33 и 10-35

**Test:** открыть панель подтверждения удаления расписания, вызвать отказ (Offline), нажать
«Удалить» и посмотреть, ЧТО ВСТАЛО ПОВЕРХ.
**Expected:** плашка аварии видна ПОВЕРХ панели; панель остаётся открытой; кнопка подтверждения
снова нажимаема; повтор возможен без переоткрытия панели.
**Why human:** правила утверждают ОБЪЯВЛЕНИЕ слоя и ИСПОЛНЕНИЕ снятия, а не ОТРИСОВКУ. ⚠️ Два
из трёх оснований девятого круга держать этот пункт за человеком СНЯТЫ восьмой партией и
перепроверены настоящим кругом (`V9-01`, `V9-02`); третье — «ни одно правило дерева документ не
отрисовывает» — неподвижно и снятию не подлежит.

#### 2. Шаг 2.8 того же обхода — плашка обрыва связи при не доехавшем follow-up GET

**Test:** довести follow-up GET до обрыва подменой `XMLHttpRequest` и посмотреть на плашку.
**Expected:** плашка появилась и называет расхождение экрана с сервером второй фразой.
**Why human:** половина (A) уже наблюдена на НАСТОЯЩЕМ обрыве; ненаблюдённой осталась половина
при обрыве, сделанном подменой: `abort()` поднимает не то событие, что настоящий обрыв
транспорта.

#### 3. ПОДПИСЬ ЧЕЛОВЕКА НА ПРИЁМКЕ под обходом 2026-09-09

**Test:** прочитать отметки шести проверок `10-UAT.md` и либо подтвердить их, либо назвать те,
что требуют перепрохождения человеческими руками.
**Expected:** артефакт получает терминальное состояние, либо перечень перепрохождения назван.
**Why human:** наблюдал АГЕНТ через chrome-devtools MCP при `workflow.live_dom_uat: false`;
право дано репликой в сессии. `status: partial` держится именно поэтому, и
`test_the_walkthrough_cannot_self_certify.py` запрет самозаверения принуждает машинно. План
10-46 приписал к артефакту три незакрытых человеком пункта БЕЗ единой отметки прохождения — то
есть партия расширила честность записи, а не закрыла пункт.

#### 4. РЕШЕНИЕ ВЛАДЕЛЬЦА: 200 запретов фазы из 239 стоя́т неразрешёнными

**Test:** по каждому — либо предъявленное принуждение, либо явное разрешение с названной
областью, либо явное признание неразрешённым.
**Expected:** ни один запрет не остаётся зелёным молча.
**Why human:** ⚠️ ПУНКТ СУЖЕН ВОСЬМОЙ ПАРТИЕЙ, НО НЕ ЗАКРЫТ, И РАЗНИЦА ЧИТАЕТСЯ ЧИСЛАМИ. План
10-47 предъявил 39 запретов СЕДЬМОЙ партии поимённо, и владелец на блокирующем чекпойнте дал
разрешение ветвью `permit-with-record` с формулировкой «РАЗРЕШЕНИЕ ДЕЛАЕТ ЗАПРЕТ НЕ БЛОКИРУЮЩИМ,
НО НЕ СОБЛЮДЁННЫМ» и машинно читаемой областью. Остальное названо числами в той же шапке и не
смягчено: 167 запретов планов 10-01…10-34, 33 запрета восьмой партии, итого 200 — и
`prohibitions_fully_enforced: 0`. Регламент фазовых гейтов велит закрывать такие пункты
FAIL-CLOSED: дескриптор `test` без принуждения есть НЕПРОВЕРЕННЫЙ запрет.

### Gaps Summary

**Гэпов нет.** Впервые за десять кругов раздел пуст, и пустота эта — не смягчение, а замер: все
шесть истин, объявленных девятым кругом невыполненными, закрыты, и каждая перепроверена
собственным исполнением верификатора, а не принята по сводке. Три окна журнала
(`.planning/WINDOWS.md` 68, 69, 70), стоявшие `open`, несут вердикт `fixed`, снятый командой
ведения журнала при зелёном прогоне закрывшего правила.

**Чего партия НЕ закрыла и не обещала закрыть.** Отрисовку браузером — её не воспроизводит ни
одно правило дерева, и шаг 4.4 обхода остаётся за человеком. 200 запретов фазы — по ним владелец
принял разрешение на 39 и оставил 200 названными нерешёнными, что честнее любого зелёного.
Подпись человека на приёмке обхода — артефакт держит `status: partial` сам, и машинное правило
это принуждает.

**Что этот круг ДОБАВИЛ к списку долгов, и добавка неприятна.** Девять предупреждений выше,
из которых три (`W-01`, `W-02`, `W-03`) заведены ВОСЬМОЙ ПАРТИЕЙ — той самой, что закрывала
класс «запись шире дерева». `W-03` есть буквальное его повторение в ПРОДУКТОВОМ файле: абзац
«ЗАМЕР 2026-09-10 НАЗВАЛ ДЕЙСТВИТЕЛЬНЫЕ МЕСТА» говорит о дереве неправду в двух местах, и оба
правила, заведённые тем же планом ради борьбы с протухающими ссылками, к нему слепы. Ни одно из
девяти не поднято до блокера — основание у каждого названо в таблице, а не подразумевается, — но
и раствориться в зелени им не дано.

**Чем этот круг отличается от девяти предыдущих.** Тем, что следующим действием фазы не является
партия закрытия гэпов. Ей нечего закрывать: очередь перешла к человеку.

---

_Verified: 2026-09-10T21:40:00Z_
_Verifier: Claude (gsd-verifier)_
