---
phase: 10-rychag-components-modal-html
verified: 2026-09-08T21:55:00Z
status: human_needed
score: 7/8 must-haves verified
behavior_unverified: 2
overrides_applied: 1
re_verification:
  previous_status: gaps_found
  previous_score: 6/9
  previous_verified: 2026-09-04T18:30:00Z
  gaps_closed:
    - >-
      `gaps[0]` ЧЕТВЁРТОГО КРУГА — вшитый вердиктный литерал в двух отрицательных
      контролях модуля принуждения регламента отметок. ЗАКРЫТ И ВОСПРОИЗВЕДЁН
      ЗАМЕРОМ ЭТОГО КРУГА В ОБЕ СТОРОНЫ, а не принят по `10-17-SUMMARY.md`.
      Подмена поля `status` настоящего отчёта на все три значения словаря даёт
      ОДИН И ТОТ ЖЕ прогон `tests/test_planning/`: `passed` → **28 passed**,
      `human_needed` → **28 passed**, `gaps_found` → **28 passed**. Прежний круг
      на том же месте получил `2 failed` / `1 failed` / `9 passed`. Оба
      отрицательных контроля питаются СИНТЕТИЧЕСКИМ корнем
      (`_fake_planning_root(tmp_path, …)`), как и требовал пункт `missing`.
      Четвёртый пункт `missing` тоже исполнен: заведён отдельный модуль
      `tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py`
      с правилом «гейт записи не судит вердикт отчёта СВОЕЙ фазы» и
      синтетическими нарушителями по двум признакам.
    - >-
      ЗУБЫ ПРАВИЛА ПОКАЗАНЫ МУТАЦИЕЙ ВЕРИФИКАТОРА, а не заявлены: `FORM-06`,
      подменённый на `- [x]` и клетку `Complete` при вердикте `gaps_found`,
      РОНЯЕТ `test_no_requirement_is_marked_complete_before_its_phase_verification_passed`
      (**1 failed, 27 passed**). То есть правило одновременно НЕ зависит от
      вердикта и НЕ вакуумно — два свойства, которые прежний круг требовал
      порознь.
    - >-
      ДОКУМЕНТНЫЙ ОТКАЗ ВОЛНЫ 3 ПЯТОЙ ПАРТИИ ЗАКРЫТ И ПРОВЕРЕН НА HEAD, а не
      принят по слову оркестратора. Коммит `dad5bc1` перевёл `FORM-06` в
      `Complete` при живом вердикте `gaps_found`; откат `06c9fc8` держится:
      `.planning/REQUIREMENTS.md:31` — `- [ ] **FORM-06**`, `:136` —
      `| FORM-06 | Phase 10 | Gaps Found |`, `tests/test_planning/` — 28 passed.
      Мутация выше воспроизводит ИМЕННО тот отказ, который поймал гейт волны 3.
  gaps_remaining: []
  regressions: []
  corrections:
    - >-
      ⚠️ ЗАПИСЬ `IN-02` ПЯТОГО КРУГА РЕВИЗИИ ОПРОВЕРГНУТА, И ЭТО ПОДТВЕРЖДЕНО
      СОБСТВЕННЫМ ЗАМЕРОМ ВЕРИФИКАТОРА ПО ВЕНДОРЕННОМУ БАНДЛУ, а не унаследовано
      от плана 10-27. Разбор `app/static/js/htmx.min.js` (htmx 2.0.10): `Vn` на
      смещении 47067, ветка `HX-Location` кончается `Nn("get",e,s);return`;
      единственное в бандле присваивание `e.successful=` (`count == 1`) стои́т на
      смещении 48145, то есть ПОСЛЕ раннего возврата. ДОБАВЛЕНО ЭТИМ КРУГОМ И В
      ЗАПИСЯХ ФАЗЫ ОТСУТСТВУЕТ: событие завершения запроса при этом НЕ ТЕРЯЕТСЯ —
      `const M=i.handler||Vn` (смещение 41186), а `g.onload` (смещение 44737)
      зовёт `M(r,T)` и ТОЛЬКО ЗАТЕМ `ae(r,"htmx:afterRequest",T)`. Ранний возврат
      из `Vn` слушателя не отменяет. Следовательно на транспорте перехода панель
      закрывает ВТОРОЙ дизъюнкт, а мёртв ПЕРВЫЙ — ровно как записал план 10-27.
    - >-
      ⚠️ ПОРУЧЕНИЕ «ЗАПИСИ ТРЕБУЮТ ПРАВКИ» ИСПОЛНЕНО НАПОЛОВИНУ, И ЗАМЕР
      ПОКАЗЫВАЕТ, КАКАЯ ПОЛОВИНА. `10-REVIEW.md` на HEAD есть ШЕСТОЙ круг
      (`reviewed: 2026-09-08T21:40`), и опровергнутого вывода `IN-02` он НЕ
      НЕСЁТ — его шапка прямо называет замер плана 10-27 прочитанным и
      оставленным как есть. Эта половина поручения закрыта самим перезаписанным
      отчётом. НО САМА ПОМЕТКА, КОТОРОЙ ФАЗА ЗАКРЫЛА `IN-02`, УЖЕ ПРОТУХЛА В ДВУХ
      КООРДИНАТАХ — см. `⚠️ WR-03` таблицы Anti-Patterns.
overrides:
  - must_have: >-
      Панель закрывается существующим Alpine (`x-on:htmx:after-request`) — новых строк JS
      фаза не добавляет (машинно: диффом по `app/static/js/` и шаблонам).
    reason: >-
      ⚠️ ПРОЧТЕНИЕ D-13, ЗАПИСАННОЕ В `10-CONTEXT.md:197-210` ДО ПРАВКИ, А НЕ ЗАДНИМ
      ЧИСЛОМ: «критерий запрещает НОВЫЕ СУЩНОСТИ, а не правку существующих». Буквальное
      прочтение отвергнуто там же и не по удобству — оно запретило бы починку теми же
      критериями, которые эту починку просят (критерий 2 той же фазы и приземление
      фокуса); амендмент формулировки в `ROADMAP.md` отвергнут тем же решением («документ
      вехи держится фиксированным, а прочтение — предмет CONTEXT.md»), и настоящая запись
      работает с ПРОЧТЕНИЕМ, а не с текстом критерия. ЗАМЕР 1 (`git diff 7c1041d..HEAD --
      app/static/js/`, HEAD `c45d9c5`): ПУСТ — ни файла, ни строки. ЗАМЕР 2 —
      ПЕРЕЗАМЕРЕННЫЙ НА HEAD ПОЛНЫЙ ПЕРЕЧЕНЬ строк JS, дописанных фазой в шаблоны: 51
      строка в ДВУХ файлах. `app/templates/ads/form.html` — 38 строк, из них ИСПОЛНЯЕМЫХ
      ДВЕ (`:308` `(function () {` и `:538` `})();` — обёртка немедленно вызываемым
      выражением, план 10-13, чинит регрессию `CR-03`, заведённую этой же фазой) и 36
      строк комментария (`:309-342`, `:539-540`); тело скрипта не правлено ни на символ.
      `app/templates/components/modal.html` — 13 строк выражений Alpine внутри
      СУЩЕСТВУЮЩЕГО узла `x-data` и СУЩЕСТВУЮЩИХ атрибутов события: `:699-707` (тело
      `show()` со снятием обеих заготовок плашки отказа — план 10-35, чинит `CR-01` — и
      тела `hide()`, `destroy()`, `land()`), `:713`, `:716`, `:777`, `:782`. Ни одного
      файла в `app/static/js/` и ни одного узла `<script>` фаза не завела. ЗАМЕР 3 —
      четыре числа сущностей на HEAD, найдено/объявлено: файлов в каталоге сценариев 2/2
      (`alpine.min.js`, `htmx.min.js`); шаблонов со встроенным обработчиком отправки
      (`onsubmit`) 1/1 (`accounts/connect_max.html`); файлов с атрибутом события рантайма
      разметки (`hx-on`) 0/0; узлов клиентского состояния (`x-data`) 24/24. Гейт
      `test_criterion_three_holds_by_the_numbers` (`tests/test_templates/test_components.py`)
      на HEAD зелен, отбор непуст (`1 passed, 83 deselected`), и при каждом из четырёх
      утверждений стои́т свой отрицательный контроль.
    accepted_by: "chubav"
    accepted_by_provenance: >-
      ИМЯ И ДАТА ПРИШЛИ ОТВЕТОМ ВЛАДЕЛЬЦА НА БЛОКИРУЮЩИЙ ОСТАНОВ задачи 1 плана 10-40, а
      не сняты `git config user.name` и заглушкой не подставлены: провизорной эта
      атрибуция не является ни одного круга — в отличие от истории записи Фазы 9, где имя
      два круга подряд стояло провизорным. ⚠️ ДАТА — ТА, КОТОРУЮ НАЗВАЛ ВЛАДЕЛЕЦ
      (`2026-09-09`), и она НЕ ПРАВИТСЯ ПОД ДАТУ ВНЕСЕНИЯ ЗАПИСИ (`2026-09-10`): запись
      принявшего есть запись того, что сказал он, а не того, когда исполнитель дошёл до
      файла. Расхождение названо здесь прямо, а не сглажено.
    accepted_at: "2026-09-09"
    accepted_branch: "accept-reading"
    accepted_branches_rejected: ["reject-reading"]
    covers: >-
      ПРИНИМАЕТСЯ ВЕСЬ ПЕРЕЗАМЕРЕННЫЙ ПЕРЕЧЕНЬ ЗАМЕРА 2 — все 51 строка JS в
      `app/templates/ads/form.html` и `app/templates/components/modal.html`, — А НЕ ТОЛЬКО
      ДВЕ СТРОКИ ОБЁРТКИ, НАЗВАННЫЕ ПЯТЫМ КРУГОМ: охват подтверждён владельцем ПРЯМО при
      ответе на останов задачи 1 плана 10-40. Граница отступления — ровно та, которую
      принуждает гейт четырёх чисел: правка ТЕЛА метода уже существующего объекта
      клиентского состояния и правка выражения УЖЕ СУЩЕСТВУЮЩЕГО атрибута события.
      Заведение НОВОЙ сущности — файла сценария, встроенного обработчика отправки,
      атрибута события рантайма разметки или узла `x-data` — настоящей записью НЕ
      принимается и краснит гейт. ⚠️ ЦЕНА НАЗВАНА ПРЯМО И НЕ СМЯГЧАЕТСЯ: граница «новая
      сущность против правки существующей» становится действующим ПРАВИЛОМ ПРОЕКТА, а не
      разовым разрешением, и всякая следующая правка JS в шаблонах будет опираться на
      настоящую запись как на прецедент. Гейт четырёх чисел принуждает границу ТОЛЬКО в
      четырёх измерениях; пятое, если оно появится, прочтение не закроет и не заметит.
      Формулировка критерия в `ROADMAP.md` остаётся буквальной — то есть документ вехи и
      отчёт фазы говорят разное, и согласует их только эта запись. Код настоящим решением
      НЕ ПРАВИТСЯ ни строкой, и ветвь `reject-reading` его тоже не откатывала бы: откат
      вернул бы `CR-03` и `CR-01`.
gaps: []
deferred:
  - truth: >-
      Сеанс QR-входа Telegram привязан к пользователю, а подставленный чужой
      `session_id` отвергается (`CR-01` ШЕСТОГО круга ревизии)
    addressed_in: "Phase 13"
    evidence: >-
      Критерий 3 Фазы 13 роадмапа дословно: «Состояние мастера живёт на сервере,
      `session_id` едет скрытым полем, и серверная проверка владения сессией
      СОХРАНЕНА — подставленный чужой `session_id` отвергается (машинно)».
      Совпадение прямое и предметное, а не тангенциальное. ⚠️ ДЕФЕКТ
      ПОДТВЕРЖДЁН ЗАМЕРОМ ВЕРИФИКАТОРА, А НЕ ПРИНЯТ ПО ОТЧЁТУ: `_qr_sessions`
      живёт в `app/messengers/telegram_user.py:42` и ключуется ОДНИМ токеном
      (`:69`, `:79`, `:102`, `:117`, `:139`, `:157`, `:176`); ни один из пяти
      входов `app/pages/accounts.py` (`:226`, `:253`, `:267`, `:289`, `:329`)
      личности к сеансу не привязывает. ⚠️ ФАЗОЙ 10 НЕ СОЗДАН И ЕЮ НЕ ТРОНУТ:
      `git diff 7c1041d..HEAD -- app/pages/accounts.py` не содержит ни одного
      куска в диапазоне `:226-358`. То же говорит шапка `10-REVIEW.md`.
  - truth: >-
      Девять отказов по происхождению, собранных помимо слоя ответа, приведены к
      единой форме (`WR-04` ПЯТОГО круга, окно 63 `.planning/WINDOWS.md`)
    addressed_in: "Phase 11"
    evidence: >-
      Окно 63 разводит снятие на двух адресатов ЯВНО: «Фаза 11 „Массовый перевод
      разделов письма“ (критерий 1 роадмапа) снимает выходы РАБОТОЙ; ФОРМА,
      которой отказ по происхождению поедет после перевода, есть решение
      ВЛАДЕЛЬЦА». Машинный гейт от отсрочки не слабеет — реестр
      `OWN_RESPONSE_EXITS` с объявленным числом 9 краснеет С ИМЕНЕМ.
behavior_unverified_items:
  - truth: >-
      КРИТЕРИЙ 2 И ВТОРАЯ ПОЛОВИНА ЦЕЛИ ФАЗЫ: ответ снимает осиротевшую панель
      подтверждения вторым OOB-узлом `hx-swap-oob="delete"` по её `id` — после N
      удалений в документе не остаётся N мёртвых диалогов
    test: >-
      Проверка 1 обхода `10-UAT.md` (шаги 1.1-1.8): открыть редактор объявления с
      ≥3 расписаниями, удалить расписание панелью подтверждения, повторить N раз;
      затем в консоли `document.querySelectorAll('[role="dialog"]').length` и
      `document.querySelectorAll('[id^="sched-del-"]').length`.
    expected: >-
      Число панелей равно числу оставшихся карточек; `#sched-N` и `#sched-del-N`
      удалённого расписания отсутствуют ОБА; живых ловушек фокуса не осталось.
    why_human: >-
      ПРИСУТСТВИЕ и РАЗЛИЧНОСТЬ обоих узлов доказаны машиной и проверены этим
      кругом (`app/templates/ads/partials/sched_delete_response.html:101-102` —
      парные `hx-swap-oob="delete"`;
      `account_groups/partials/delete_response.html:99-100` — то же; правило
      `test_the_orphaned_panel_is_removed_by_its_own_oob_node`,
      `tests/test_pages/test_editor_schedules.py:1539`, утверждает, что узел
      карточки и узел панели — РАЗНЫЕ узлы, то есть тест на присутствие тела его
      бы не заменил). ПРИМЕНЯЕТ узлы рантайм разметки; суита идёт через
      `ASGITransport` на in-memory SQLite и свопов не делает. Роадмап сам
      называет пункт ручным («фактическое снятие — ручной УАТ, пункт 4»).
      `10-UAT.md` на HEAD — `status: testing`, `checks_declared: 6`, таблицы
      отметок ПУСТЫ; обход не проходился ни разу.
  - truth: >-
      Транспорт, заведённый фазой, не убивает уже работающий JavaScript экрана, на
      который сам же приземляет (`CR-03`, БРАУЗЕРНАЯ ПОЛОВИНА)
    test: >-
      Проверка 1 обхода `10-UAT.md`, шаги 1.13-1.15: удалить ПОСЛЕДНЕЕ расписание;
      НЕ перезагружая документ, прочитать консоль пришедшего экрана; напечатать
      символы в поле `#text`; нажать крестик на плитке вложения.
    expected: >-
      Ранней ошибки языка о повторном объявлении имени в консоли НЕТ; число в
      `#text-counter` меняется на КАЖДЫЙ символ; плитка исчезает НА МЕСТЕ,
      документ НЕ перезагружается.
    why_human: >-
      МАШИННАЯ ПОЛОВИНА ДЕРЖИТСЯ (обёртка `ads/form.html:308` / `:538`, исходник
      исполняется ДВАЖДЫ в ОДНОЙ `vm`-области настоящим Node). Но интерпретатор
      не свопает `innerHTML`, не пересоздаёт узел сценария и не вешает
      обработчики на новые узлы документа. ⚠️ ЭТО ЖЕ ГРАНИЦА, ЧТО У ГАРНИРА
      ПАНЕЛИ: `_run_modal_lifecycle` сам объявляет вне охвата ТРИ утверждения о
      ПОРЯДКЕ задач рантайма (синхронность внеполосного снятия внутри свопа;
      событие завершения в той же задаче; снос компонента микрозадачей позже) —
      `tests/test_templates/test_components.py:2693`. Их человеческая половина
      закрывается проверкой 2 обхода, а не суитой.
coincidental_reliance_items: []
human_verification:
  - test: >-
      Проверка 1 обхода `10-UAT.md` (шаги 1.1-1.8) — счёт `[role="dialog"]` и
      `[id^="sched-del-"]` после N удалений через панель.
    expected: "После N удалений осиротевших диалогов с живыми ловушками фокуса в документе нет."
    why_human: >-
      OOB-снятие применяет рантайм htmx; httpx свопов не делает. КРИТЕРИЙ 2
      РОАДМАПА — ВТОРАЯ ПОЛОВИНА ЦЕЛИ ФАЗЫ, и это единственный вывод, взятый
      роадмапом из документации и не проверенный ни на чём.
  - test: >-
      Шаги 1.10-1.12 того же обхода: `window.__uat10 = 'жив'`, удалить ПОСЛЕДНЕЕ
      расписание, затем прочитать `window.__uat10` и повторить счёт диалогов.
    expected: "Экран сменился переходом (204 + `HX-Location`), `window.__uat10` === `'жив'`, счёт диалогов `1` и `0`."
    why_human: "Ветвь перехода наблюдается только рантаймом браузера."
  - test: "Шаги 1.13-1.15: консоль пришедшего экрана, счётчик символов, снятие вложения."
    expected: "Ранней ошибки языка нет; счётчик меняется на каждый символ; плитка снимается на месте без перезагрузки документа."
    why_human: >-
      ⚠️ ЛЕТОПИСЬ ОЖИДАНИЯ СОХРАНЯЕТСЯ: третий отчёт ОЖИДАЛ ЗДЕСЬ ОТКАЗ
      (`SyntaxError: Identifier 'IMAGE_BASE_URL' has already been declared`).
      После правки плана 10-13 ожидание ПЕРЕВЁРНУТО и спрашивает о ЖИВОСТИ.
      Прежнее ожидание не вычёркивается — оно было верным для дерева, на котором
      записано.
  - test: >-
      Проверка 2 обхода: замедлить сеть (Slow 3G), подтвердить удаление на месте,
      отвечающем 204 + `HX-Location`, и оборвать follow-up GET (Offline сразу
      после нажатия).
    expected: >-
      Панель уходит ПО СОБЫТИЮ ЗАВЕРШЕНИЯ ЗАПРОСА, а не по свопу тела: не
      остаётся висеть поверх устаревшего экрана с `is-modal-open` на `<html>`.
    why_human: >-
      ⚠️ ЭТОТ ПУНКТ ПОДКРЕПЛЁН ЗАМЕРОМ ЭТОГО КРУГА, НО НЕ ЗАКРЫТ ИМ: бандл
      показывает, что `htmx:afterRequest` летит из `g.onload` ПОСЛЕ возврата из
      `Vn`, то есть механизм есть. Но `_run_modal_lifecycle` сам объявляет
      ПОРЯДОК задач рантайма вне своего охвата, а именно порядок здесь и есть
      предмет: что панель ушла ПО СОБЫТИЮ, а не по свопу, различимо только на
      оборванном follow-up GET.
  - test: >-
      Проверка 3 обхода: подтвердить удаление на медленной сети и, не дожидаясь
      ответа, нажать Esc, щёлкнуть по оверлею и нажать «Отмена».
    expected: >-
      Панель НЕ уходит ни одним из трёх путей, пока запрос летит; виден узел
      индикатора (`.form-busy`); «Отмена» остаётся доступной в обходе по Tab.
      После ответа все три пути снова работают.
    why_human: "Гейт `DEF-09-02` доказан поведенчески и мутацией, но что человек видит панель ЗАНЯТОЙ, а не сломанной, снимается только глазом — окно 35."
  - test: "Проверка 4 обхода: вызвать 500 на маршруте за панелью подтверждения и наблюдать панель."
    expected: "Панель ОСТАЁТСЯ открытой, поверх встаёт плашка аварии, повтор возможен без переоткрытия."
    why_human: "Половина D-12: в суите доказана на реальной форме события отказа, на живом стенде — глазом. Окно 22."
  - test: "Проверка 5 обхода: подтвердить удаление и проверить `document.activeElement` после ухода панели на обоих путях."
    expected: "На фрагментном пути фокус приземлился в `#notice`, а не на `<body>`."
    why_human: >-
      Приземление фокуса поднимает Alpine и настоящий своп. ⚠️ Читать вместе с
      записанной границей: на фрагментном пути `#notice` ПУСТА всегда (плашки на
      успех нет по D-03).
  - test: >-
      РЕШЕНИЕ ВЛАДЕЛЬЦА, А НЕ ПРОВЕРКА СТЕНДА: принять или отклонить прочтение
      D-13 критерия 3 ЯВНОЙ ЗАПИСЬЮ `overrides` в шапке настоящего отчёта.
    expected: >-
      Либо в шапку добавлено `overrides[]` с формулировкой критерия 3, причиной
      («критерий запрещает НОВЫЕ СУЩНОСТИ, а не правку существующих»), именем
      принявшего и датой; либо прочтение отклонено, и две строки обёртки
      `ads/form.html:308` / `:538` объявлены нарушением критерия.
    why_human: >-
      ⚠️ ЭТО ЧЕТВЁРТЫЙ КРУГ, НА КОТОРОМ РАСХОЖДЕНИЕ НЕСЁТСЯ ПРОЗОЙ. Критерий 3
      называет предметом дифф по `app/static/js/` **И ШАБЛОНАМ**. Замер этого
      круга: `git diff 7c1041d..HEAD -- app/static/js/` ПУСТ, а `ads/form.html`
      получил ДВЕ строки JS (`(function () {` на `:308`, `})();` на `:538`).
      Дословно критерий этим тронут. Держится он прочтением D-13, записанным ДО
      правки и переведённым в машинный гейт, который считает СУЩНОСТИ. Прочтение
      защитимо и заведено не задним числом — но три круга подряд оно живёт
      прозой, тогда как в шапке отчёта для него есть машинно читаемое место.
    resolved_by: >-
      ⚠️ ПРИПИСКА, А НЕ ПРАВКА: текст выше стои́т ДОСЛОВНО и не вычёркивается — он есть
      запись того, СКОЛЬКО кругов вопрос стоял, и стереть её значило бы стереть
      основание, по которому он наконец решён (идиома D-30/D-32 `.planning/STATE.md`).
      РАЗРЕШЕНО 2026-09-09 РЕШЕНИЕМ ВЛАДЕЛЬЦА `chubav`, ветвь `accept-reading` (останов
      задачи 1 плана 10-40): прочтение D-13 ПРИНЯТО явной записью `overrides[0]` в шапке
      настоящего отчёта, `overrides_applied` приведён к 1. Запись внесена планом 10-40
      2026-09-10. ⚠️ ОХВАТ ЗАПИСИ ШИРЕ, ЧЕМ ОПИСЫВАЕТ ЗАМЕР ВЫШЕ: пятый круг назвал две
      строки обёртки `ads/form.html:308` / `:538`, а запись накрывает ПЕРЕЗАМЕРЕННЫЙ на
      HEAD `c45d9c5` полный перечень — 51 строку в `ads/form.html` (38) и
      `components/modal.html` (13), появившуюся планами 10-13 и 10-35. Отклонённая ветвь
      — `reject-reading`. ⚠️ ВЕРДИКТ ОТЧЁТА, СЧЁТ ИСТИН, СПИСОК ГЭПОВ И БЛОК ПОВТОРНОЙ
      ВЕРИФИКАЦИИ ПЛАНОМ 10-40 НЕ ТРОНУТЫ: они принадлежат верификатору, и запись
      отступления есть основание для следующего круга, а не сам вердикт.
---

# Phase 10: Рычаг `components/modal.html` — Verification Report (ПЯТЫЙ круг)

**Phase Goal:** подтверждение удаления идёт через htmx во всех 18 местах одной правкой одного
файла (`app/templates/components/modal.html`), и после N удалений в документе не остаётся N
мёртвых диалогов
**Verified:** 2026-09-08T21:55:00Z
**Status:** human_needed
**Re-verification:** Да — после ПЯТОЙ партии закрытия гейпов (планы 10-24…10-32). Заменяет
отчёт от 2026-09-04T18:30 (`gaps_found`, 6/9). HEAD `4b1f75c`.

## Что этот круг измерил СВОИМИ РУКАМИ

Ни одно число ниже не взято из SUMMARY. Перечень замеров с командами — таблицы
«Behavioral Spot-Checks» и «Probe Execution».

**Замер 1 — блокер прежнего круга, проверенный в ОБЕ стороны и ЗАКРЫТЫЙ.** Прежний круг
показал, что гейт регламента отметок зелен РОВНО ПРИ УСЛОВИИ, ЧТО ФАЗА НЕ ЗАКРЫТА. Этот круг
повторил ту же подмену на том же файле:

| Поле `status` настоящего отчёта | Прогон `tests/test_planning/` |
| --- | --- |
| `gaps_found` | **28 passed** |
| `human_needed` | **28 passed** |
| `passed` (цель фазы) | **28 passed** |

Прежний круг на том же месте получил `9 passed` / `1 failed` / `2 failed`. **Гэп закрыт, и это
воспроизведение, а не отчёт.** Зубы показаны отдельной мутацией: `FORM-06`, подменённый на
`- [x]` / `Complete` при вердикте `gaps_found`, роняет правило (**1 failed, 27 passed**). Тот
же замер ВОСПРОИЗВОДИТ отказ гейта волны 3 пятой партии и подтверждает, что откат `06c9fc8`
верен и держится на HEAD.

**Замер 2 — опровержение `IN-02` подтверждено НЕЗАВИСИМО и ДОСТРОЕНО.** Разбор вендоренного
`app/static/js/htmx.min.js` собственным скриптом верификатора:

```
len(bundle) = 51238
s.count('e.successful=') = 1                                   offset = 48145
Vn(t,e){…if(T(n,/HX-Location:/i)){…Nn("get",e,s);return}…}      offset = 47067
const M = i.handler || Vn;                                     offset = 41186
g.onload=function(){try{…M(r,T);…ae(r,"htmx:afterRequest",T);…}} offset = 44737
```

Ранний возврат из `Vn` стои́т ДО единственного присваивания признака успешности — значит
`$event.detail.successful` есть `undefined`, и панель закрывает ВТОРОЙ дизъюнкт. **ДОБАВЛЕНО
ЭТИМ КРУГОМ И В ЗАПИСЯХ ФАЗЫ ОТСУТСТВУЕТ:** ранний возврат НЕ отменяет слушателя — событие
летит из `g.onload` ПОСЛЕ возврата `M(r,T)`. Без этого второго полузамера утверждение «панель
закрывается на 16 из 18 мест» держалось бы на допущении, что событие вообще приходит. Оно
приходит.

**Замер 3 — находка партии о шести неограниченных входах ВОСПРОИЗВЕДЕНА, а не унаследована.**
Зонд верификатора на фикстурах проекта, величина `99999999999999999999999999`; зонд снят и в
дереве не оставлен:

```
GET /history                          500
GET /history/partial                  500
GET /history/export                   500
GET /admin/users/1/history            500
GET /admin/users/1/history/partial    500
POST /ads/new (ad_id)                 500
```

**Замер 4 — регрессия границы на семье подтверждённого удаления держится и ШИРЕ прежней.**
Прежний круг мерил ПЯТЬ входов; этот взял СЕМЬ, добавив два продуктовых маршрута, которых
прежний круг не касался:

```
POST /schedules/<big>/delete   422     POST /schedules/new (ad_id)       422
POST /schedules/<big>/edit     422     POST /schedules/new (account_id)  422
POST /schedules/<big>/toggle   422     POST /ads/<big>/delete            422
                                       POST /accounts/<big>/delete       422
```

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | **Критерий 1.** Все 18 мест подтверждения работают через htmx одной правкой `modal.html`; инвентарь утверждён ЧИСЛОМ | ✓ VERIFIED | Макрос (`modal.html:770-772`) печатает БЕЗУСЛОВНО `method="post"`, `action="{{ action }}"`, `hx-post="{{ action }}"`, `hx-swap="none"`, `hx-disabled-elt`, `hx-indicator`, `x-on:htmx:after-request` и узел `.form-busy`; конструкций шаблонизатора в открывающем теге формы нет. Оба названных роадмапом гейта зелены прогоном верификатора (5 selected, 5 passed) и НЕ ВАКУУМНЫ: `test_modal_site_inventory` несёт явный антивакуумный барьер («разборщик вызовов панели не нашёл НИ ОДНОГО именованного аргумента — счёт вакуумен»). ⚠️ ЧИСЛА СВЕРЕНЫ С ДЕРЕВОМ СОБСТВЕННЫМ СКРИПТОМ ВЕРИФИКАТОРА, а не приняты по константам: 18 мест диспетчеризации, 9 различных имён, 11 импортёров — совпадает с `MODAL_PLACES=18` / `MODAL_EVENT_NAMES=9` / `MODAL_IMPORTERS=11`. Прогон `tests/test_templates/` по файлам: **200 passed** (84+77+19+12+5+3). |
| 2 | **Критерий 2.** Ответ снимает осиротевшую панель вторым OOB-узлом `hx-swap-oob="delete"` по её `id`; после N удалений не остаётся N мёртвых диалогов | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Разметка на месте, адресована верно и проверена этим кругом: `ads/partials/sched_delete_response.html:101-102` и `account_groups/partials/delete_response.html:99-100` несут ПАРНЫЕ узлы (строка + панель). Структурная предпосылка держится — `{% call modal(id='sched-del-' ~ s.id …) %}` на `sched_card.html:274` стои́т ПОСЛЕ `</article>` на `:269`. Правило `test_the_orphaned_panel_is_removed_by_its_own_oob_node` утверждает РАЗЛИЧНОСТЬ узлов, то есть тест на присутствие тела его бы не заменил. НО СНЯТИЕ ПРИМЕНЯЕТ РАНТАЙМ: суита идёт через `ASGITransport` и свопов не делает; роадмап сам называет пункт ручным. `10-UAT.md` — `status: testing`, таблицы ПУСТЫ, обход не проходился ни разу. → человеку. |
| 3 | **Критерий 3, половина «новых строк JS фаза не добавляет»** (диффом по `app/static/js/` И ШАБЛОНАМ) | ✓ VERIFIED (по прочтению D-13; требуется явная запись `overrides`) | `git diff 7c1041d..HEAD -- app/static/js/` **ПУСТ** — проверено этим кругом. Гейт четырёх чисел (`test_criterion_three_holds_by_the_numbers`, `tests/test_templates/test_components.py:4243`) зелен и НЕ ВАКУУМЕН — при нём отрицательные контроли на каждое из четырёх утверждений. ⚠️ ДОСЛОВНО КРИТЕРИЙ ТРОНУТ, И ЭТО НАЗЫВАЕТСЯ ЗДЕСЬ ЧЕТВЁРТЫЙ КРУГ ПОДРЯД: `ads/form.html` получил ДВЕ строки JS (`:308`, `:538`). Держится критерий прочтением владельца D-13 («запрещены НОВЫЕ СУЩНОСТИ, а не правка существующих»), записанным ДО правки и переведённым в гейт, который считает СУЩНОСТИ. Прочтение защитимо; ему пора получить машинно читаемую отметку `overrides` — пункт 8 перечня Human Verification. ⚠️ **ПРИПИСКА ПЛАНА 10-40 (2026-09-10), ТЕКСТ ВЫШЕ НЕ ВЫЧЁРКНУТ И НЕ ПРАВЛЕН НИ НА СИМВОЛ:** расхождение РАЗРЕШЕНО 2026-09-09 решением владельца `chubav`, ветвь `accept-reading` — прочтение D-13 принято явной записью `overrides[0]` в шапке настоящего отчёта, `overrides_applied` приведён к 1, отклонена ветвь `reject-reading`. Охват записи ШИРЕ названных здесь двух строк: перезамер на HEAD `c45d9c5` даёт 51 строку JS в шаблонах — `ads/form.html` 38 (исполняемых 2: `:308`, `:538`; комментария 36: `:309-342`, `:539-540`) и `components/modal.html` 13 (`:699-707`, `:713`, `:716`, `:777`, `:782`, план 10-35). Четыре числа сущностей на HEAD найдено/объявлено: 2/2, 1/1, 0/0, 24/24. Вердикт этой клетки, счёт истин и список гэпов планом 10-40 НЕ ТРОНУТЫ. |
| 4 | **Критерий 3, половина «панель закрывается существующим Alpine (`x-on:htmx:after-request`)»** | ✓ VERIFIED | ПОВЕДЕНЧЕСКИ, А НЕ ПРИСУТСТВИЕМ. `test_the_panel_closes_on_the_location_transport` ИСПОЛНЯЕТ выражение `x-data` и выражение завершения запроса в НАСТОЯЩЕМ интерпретаторе (`_run_modal_lifecycle` → `tests/conftest.py::run_node_script`, `pytest.skip` запрещён) с формой события транспорта перехода и утверждает ПЕРЕХОД СОСТОЯНИЯ: `open is False`, `locked is False`, `sending is False`, `threw is None`, плюс поле вердикта `successful_key_present is False` — то есть гарнир не подсовывает форму, которой рантайм не производит. При нём отрицательный контроль `test_control_negative_the_successful_only_branch_is_dead_on_the_location_transport` (прежнее выражение оставляет панель ОТКРЫТОЙ). МЕХАНИЗМ ПОДТВЕРЖДЁН НЕЗАВИСИМО замером бандла (см. «Замер 2»). ⚠️ ОСТАТОК НЕ ПОГЛОЩЁН: ПОРЯДОК задач рантайма гарнир не воспроизводит и сам это объявляет — уходит проверкой 2 обхода (истина 6 и пункт 4 перечня). |
| 5 | **Критерий 4.** Все 10 потребителей продолжают удалять без JS: панель остаётся настоящей формой POST с непустым `action` | ✓ VERIFIED | `method="post"` — ЛИТЕРАЛ, `action="{{ action }}"` и `hx-post="{{ action }}"` выписаны ТЕМ ЖЕ выражением; `MODAL_CONSUMERS` — ровно 10 имён. Гейты `test_every_such_form_keeps_its_method_and_action`, `test_the_post_attribute_matches_the_action_character_for_character`, `test_every_caller_action_path_is_a_declared_route` обходят ВСЕХ вызывающих, при них отрицательные контроли (`…an_action_off_by_one_character_reddens_the_gate`). ⚠️ ДЕГРАДАЦИЯ ПРОВЕРЕНА ЭТИМ КРУГОМ ШИРЕ, ЧЕМ ЕЁ ПОКРЫВАЮТ ИМЕНОВАННЫЕ ТЕСТЫ: у трёх потребителей без правила `*_degrades_without_*` строчная форма прямого POST всё равно ЕСТЬ — `admin/includes/worker_row.html:108`, `admin/includes/queue_row.html:87`, `history/includes/history_card.html:162`. Панель есть УСИЛЕНИЕ поверх формы, а не единственный путь. |
| 6 | `CR-03`, БРАУЗЕРНАЯ ПОЛОВИНА: после перехода `HX-Location` человек видит ЖИВОЙ клиентский слой редактора | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Обёртка на месте (`ads/form.html:308`, `:538`), тело скрипта не правлено ни на символ. Свойство доказано ИСПОЛНЕНИЕМ: исходник рендеренной разметки прогоняется дважды в ОДНОЙ `vm`-области настоящим Node, мутация «вернуть объявление верхнего уровня» краснит правило. ЧЕГО НЕ ДОКАЗАНО: интерпретатор не свопает `innerHTML` и не вешает обработчики на новые узлы. Шаги 1.13-1.15 обхода не пройдены. Против прежнего круга состояние НЕ ИЗМЕНИЛОСЬ. → человеку. |
| 7 | Граница величины идентификатора держится на семье подтверждённого удаления (`CR-01`/`CR-02` третьего круга) | ✓ VERIFIED | **ВОСПРОИЗВЕДЕНО И РАСШИРЕНО.** Зонд верификатора: **422 × 7** (прежний круг мерил 5). Корень один и объявлен один раз во всём `app/` — `test_the_identifier_bound_is_declared_exactly_once_in_the_whole_app` с отрицательным контролем на ВТОРОЕ объявление и контролем «правило не читает прозу». Каталожный гейт полноты: **17 passed**; вселенная объявлена числом (35), приложение непокрытого — числом (10), корневые псевдонимы — числом (3), при них два отрицательных контроля. |
| 8 | Правило, принуждающее регламент отметок требований, не краснеет от штатного продвижения проекта — в том числе от вердикта, которым эта фаза достигает цели (`gaps[0]` ЧЕТВЁРТОГО круга) | ✓ VERIFIED | **ЗАКРЫТО И ВОСПРОИЗВЕДЕНО В ОБЕ СТОРОНЫ.** См. «Замер 1»: три вердикта — три раза **28 passed**; мутация `FORM-06` — **1 failed**. Оба отрицательных контроля питаются `_fake_planning_root(tmp_path, …)`, как требовал пункт `missing`. Четвёртый пункт `missing` исполнен отдельным модулем `test_planning_gates_are_independent_of_the_live_verdict.py`. Отметка требования соответствует дереву: `.planning/REQUIREMENTS.md:31` — `- [ ] **FORM-06**`, `:136` — `Gaps Found`. |

**Score:** 7/8 truths verified (2 present, behavior-unverified)

⚠️ **ЧТО ИЗМЕНИЛОСЬ ПРОТИВ ПРЕЖНЕГО КРУГА, ЧЕСТНЫМ СЧЁТОМ.** Единственный провал прежнего
круга закрыт, и закрыт ВОСПРОИЗВЕДЕНИЕМ ВЕРИФИКАТОРА В ОБЕ СТОРОНЫ, а не по
`10-17-SUMMARY.md`. Знаменатель опущен с 9 до 8: прежние истины 6 и 7 («пять входов» и
«соседние маршруты») сведены в одну (корень один), а критерий 3 РАЗВЕДЁН на две истины — его
половины проверяются РАЗНЫМИ способами и имеют РАЗНЫЙ статус доказанности; склеенные, они
прятали бы поведенчески доказанную половину за прозаически защищаемой. **Ни одной FAILED
истины на этом круге нет.** Фаза не закрыта по ОДНОЙ причине, и причина ровно та, которую
роадмап назвал заранее: вторая половина цели фазы наблюдается только человеком, и человек её
ещё не наблюдал.

### Deferred Items

| # | Item | Addressed In | Evidence |
| --- | --- | --- | --- |
| 1 | `CR-01` ШЕСТОГО круга: сеанс QR-входа Telegram не привязан к пользователю | Phase 13 | Критерий 3 Фазы 13 дословно требует, чтобы «подставленный чужой `session_id` отвергался (машинно)». Дефект подтверждён замером (`app/messengers/telegram_user.py:42` — ключ один токен; пять входов `app/pages/accounts.py:226…329` личности не привязывают), фазой 10 НЕ СОЗДАН и ею не тронут (`git diff` по `accounts.py` кусков в `:226-358` не даёт). |
| 2 | `WR-04` ПЯТОГО круга: девять отказов по происхождению помимо слоя ответа | Phase 11 | Окно 63 `.planning/WINDOWS.md` разводит снятие на двух адресатов явно; критерий 1 Фазы 11 снимает выходы работой. Машинный гейт `OWN_RESPONSE_EXITS` (число 9) от отсрочки не слабеет. |

⚠️ **ЧТО НЕ ОТЛОЖЕНО И ОТЛОЖЕННЫМ ОБЪЯВЛЕНО НЕ БУДЕТ.** Шесть неограниченных входов
(`CATALOGUE_APPENDIX`, вердикт `open`) НИ ОДНА последующая фаза вехи предметом не называет —
проверено по разделам фаз 11-15. Они остаются НАЗВАННЫМ ДОЛГОМ БЕЗ ВЛАДЕЛЬЦА, и это записано
в таблице Anti-Patterns, а не спрятано в `deferred`.

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `app/templates/components/modal.html` | Рычаг: безусловные htmx-атрибуты формы панели | ✓ VERIFIED | `:770-772` — все семь свойств качества вне условия шаблонизатора; 10 потребителей, 11 импортёров, 18 мест. Правка 10-27 (`9ec3f1d`): **56 added, 0 deleted**, все — комментарии шаблонизатора; отдаваемые байты не тронуты. |
| `app/templates/ads/partials/sched_delete_response.html` | Парные OOB-узлы снятия строки и панели | ✓ VERIFIED (присутствие) | `:101-102` — `id="sched-{{ schedule_id }}"` и `id="sched-del-{{ schedule_id }}"`, оба `hx-swap-oob="delete"`. Применение — истина 2. |
| `app/templates/account_groups/partials/delete_response.html` | То же для группы аккаунта | ✓ VERIFIED (присутствие) | `:99-100`. |
| `app/templates/ads/form.html` | Обёртка инлайн-скрипта, `#sched-count` как долгоживущая область | ✓ VERIFIED | `:308` / `:538` — обёртка; `<div id="sched-count">` с включаемым `sched_count_rule.html`. |
| `app/pages/identifiers.py` | Нейтральный модуль границы величины | ✓ VERIFIED | Единственное объявление `ID_MAX` во всём `app/` (правило + отрицательный контроль); 3 корневых псевдонима. |
| `tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py` | Правило «гейт записи не судит вердикт СВОЕЙ фазы» | ✓ VERIFIED | Существует, зелено, несёт синтетических нарушителей по двум признакам и границу признака B. |
| `tests/test_pages/test_identifier_bounds.py` | Гейт полноты границы по каталогу страничного слоя | ✓ VERIFIED | **17 passed**; вселенная 35, приложение 10, псевдонимы 3 — все объявлены числами, при них отрицательные контроли (`…a_synthetic_unbounded_identifier_reddens_the_catalogue_rule`, `…an_empty_catalogue_reddens_the_universe_rule`). |
| `.planning/phases/10-…/10-UAT.md` | Обход второй половины цели фазы | ⚠️ ORPHANED (не пройден) | `status: testing`, `checks_declared: 6`, таблицы отметок пусты. Артефакт существует и самозаверения не допускает (замер: подмена на `status: passed` при пустых таблицах РОНЯЕТ правило), но предмета своего не закрывает. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Кнопка-триггер (18 мест) | Панель `modal()` (11 выражений `id=`) | событие окна `modal-open-<id>` | ✓ WIRED | СВЕРЕНО СКРИПТОМ ВЕРИФИКАТОРА: 18 диспетчеризаций, 9 префиксов, и КАЖДЫЙ префикс имеет парное выражение `id=` у панели. `acc-del-` диспетчеризуется в трёх файлах, включая `accounts/partials/sync_status_card.html`, панелью не являющийся, — это объявленный приём «триггер где угодно, панель отдельно». ⚠️ ПРАВИЛА, СЛИЧАЮЩЕГО ИМЯ ТРИГГЕРА С ИМЕНЕМ ПАНЕЛИ, В ДЕРЕВЕ НЕТ (см. `ℹ️ IN-A`). |
| Форма панели | Маршрут подтверждённого удаления | `hx-post="{{ action }}"` == `action="{{ action }}"` | ✓ WIRED | `test_the_post_attribute_matches_the_action_character_for_character`, `test_every_caller_action_path_is_a_declared_route`, `test_no_caller_action_path_leads_to_a_fragment_route` + отрицательные контроли. |
| Ответ удаления расписания | Документ | второй OOB-узел по `id` панели | ⚠️ HOLLOW (присутствует, применение не наблюдалось) | Узлы есть и различны (машинно); своп применяет рантайм, которого суита не поднимает. → истина 2. |
| Событие завершения запроса htmx | `hide()` панели | `x-on:htmx:after-request` | ✓ WIRED | Подтверждено ДВАЖДЫ: исполнением выражения в настоящем интерпретаторе (переход состояния) и замером бандла (`g.onload` зовёт `ae(r,"htmx:afterRequest",T)` ПОСЛЕ `M(r,T)`, поэтому ранний возврат `Vn` слушателя не отменяет). |
| Отметка `FORM-06` | Вердикт отчёта верификации | `tests/test_planning/test_requirement_completion_follows_verification.py` | ✓ WIRED | Замер в обе стороны: независимость от вердикта (3 × 28 passed) и зубы (мутация → 1 failed). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `modal.html` | `action` | Именованный аргумент вызывающего; у всех 11 выражений — конкатенация с идентификатором сущности | ✓ | ✓ FLOWING — пустого литерала нет ни у одного вызывающего; `test_every_caller_action_path_is_a_declared_route` сличает путь с таблицей маршрутов |
| `sched_delete_response.html` | `schedule_id` | Идентификатор удалённой строки из обработчика `app/pages/schedules.py` | ✓ | ✓ FLOWING |
| `sched_count_rule.html` | `schedules_count` | `editor.schedules_count`, один включаемый файл на ОБА места отрисовки | ✓ | ✓ FLOWING — второй копии счётчика в дереве нет |
| `modal.html` (панель) | `sending` | `x-data`, сбрасывается `x-on:htmx:after-request` | ✓ | ✓ FLOWING — переход состояния исполнен в интерпретаторе, а не выведен из разметки |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Сборка дерева | `uv run python -m compileall -q app main.py tests` | RC=0 | ✓ PASS |
| Гейты разметки и инвентаря | `uv run pytest tests/test_templates/<файл> -q` (по шести файлам) | 84+77+19+12+5+3 = **200 passed** | ✓ PASS |
| Два гейта критерия 1 плюс три гейта транспорта | `uv run pytest tests/test_templates/test_components.py -k "modal_site_inventory or modal_guard_is_inherited or transition_response_is_assembled or panel_closes_on_the_location_transport or successful_only_branch_is_dead"` | 5 passed, 79 deselected | ✓ PASS |
| Гейт полноты границы по каталогу | `uv run pytest tests/test_pages/test_identifier_bounds.py -q` | **17 passed** | ✓ PASS |
| Гейты записи, вердикт `gaps_found` | `uv run pytest tests/test_planning/ -q` | **28 passed** | ✓ PASS |
| Гейты записи, вердикт подменён на `passed` | то же, после `sed` по полю `status` | **28 passed** | ✓ PASS (независимость доказана) |
| Гейты записи, вердикт подменён на `human_needed` | то же | **28 passed** | ✓ PASS (независимость доказана) |
| ЗУБЫ правила регламента | `FORM-06` → `- [x]` / `Complete` при `gaps_found` | **1 failed, 27 passed** (`test_no_requirement_is_marked_complete_before_its_phase_verification_passed`) | ✓ PASS (правило не вакуумно) |
| ЗУБЫ правила самозаверения обхода | `10-UAT.md` → `status: passed` при пустых таблицах | **1 failed, 27 passed** (`test_no_walkthrough_declares_itself_passed_with_empty_marks`) | ✓ PASS (правило не вакуумно) |
| Замер рантайма панели | скрипт верификатора по `app/static/js/htmx.min.js` | `count('e.successful=')==1` @48145; `Vn` @47067 с ранним возвратом; `M=i.handler||Vn` @41186; `afterRequest` из `g.onload` @44737 ПОСЛЕ `M(r,T)` | ✓ PASS |
| Сверка инвентаря с деревом | скрипт верификатора по `app/templates/**/*.html` | 18 диспетчеризаций / 9 префиксов / 11 выражений `id=`; каждый префикс имеет пару | ✓ PASS |
| Дифф вендоренных сценариев | `git diff 7c1041d..HEAD -- app/static/js/` | ПУСТ | ✓ PASS |
| Полный прогон суиты (ОДИН раз за верификацию) | `uv run pytest tests/ -q` | **2897 passed**, RC=0, 2197.52s (0:36:37) — ВОСПРОИЗВЕДЕНО ВЕРИФИКАТОРОМ, число оркестратора совпало | ✓ PASS |
| Второй OOB-узел применяется рантаймом | — | нельзя без браузера | ? SKIP → человеку (истина 2) |
| Клиентский слой жив после свопа | — | нельзя без браузера | ? SKIP → человеку (истина 6) |

### Probe Execution

| Probe | Command | Result | Status |
| --- | --- | --- | --- |
| Граница величины на семье подтверждённого удаления | зонд верификатора на фикстурах проекта, 7 входов, величина `9999999999999999999999999` | **422 × 7** | PASS |
| Шесть входов вне признака вселенной | зонд верификатора, величина `99999999999999999999999999` | **500 × 6** | FAILED (см. `⚠️ WR-04-A` — открытый долг вне предмета цели фазы) |
| Конвенциональный каталог `scripts/*/tests/probe-*.sh` | `find scripts -path '*/tests/probe-*.sh'` | в проекте не заведён; фаза его не объявляет | SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| FORM-06 | все 32 плана фазы (единственный ID во всех шапках) | подтверждение удаления работает через htmx во всех 18 местах одной правкой `components/modal.html`, и ответ снимает осиротевшую панель вторым OOB-узлом с `hx-swap-oob="delete"` | ? NEEDS HUMAN | ПЕРВАЯ ПОЛОВИНА требования (18 мест, одна правка) — ✓ машинно, истины 1/3/4/5. ВТОРАЯ (снятие осиротевшей панели) — присутствие узлов машинно, СНЯТИЕ не наблюдал никто (истина 2). Отметка в дереве этому соответствует: `- [ ]` и `Gaps Found`, и правило это принуждает. |

**Сирот нет.** Извлечение ID из шапок всех 32 планов даёт РОВНО `FORM-06`;
`.planning/REQUIREMENTS.md:491` отображает Фазу 10 в одно требование
(`| 10. Рычаг components/modal.html | FORM-06 | 1 |`). Требований, ожидаемых от фазы и не
заявленных ни одним планом, нет.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `app/templates/components/modal.html` | 721, 723 | **`WR-03` — ПОМЕТКА, ОБЪЯВИВШАЯ СЕБЯ СТЕРЕЖЁННОЙ, УЖЕ ПРОТУХЛА, И ЭТО В РЫЧАГЕ ФАЗЫ.** Воспроизведено замером верификатора: пометка называет `HX_LOCATION_HEADER` «на **:148**» и `respond()` «(**:375**)»; на самом деле `app/pages/htmx.py:169` и `:475`, а по названным номерам лежат текст `ValueError` про latin-1 и проза докстринга `_with_notice`. Та же пометка (`:764-769`) объявляет себя стерегомой правилом `test_the_transition_response_is_assembled_in_one_declared_place`, которое сличает ЧИСЛО мест сборки и СТАТУС и номеров строк не читает вовсе | ⚠️ Warning | Половина пометки объявлена стерегомой, будучи нестерегомой, — и протухла ОДНОВРЕМЕННО с собственной отгрузкой. Ровно класс «запись шире дерева», за который фаза получила круги 3, 4, 5 и 6, и на этот раз он лежит В ФАЙЛЕ, НАЗВАННОМ ЦЕЛЬЮ ФАЗЫ. Поведения не трогает |
| `app/pages/schedules.py` | 28 (опровергается 352) | **`WR-01` — ЗАПИСЬ, ОПРОВЕРГАЕМАЯ СВОИМ ЖЕ ФАЙЛОМ.** Воспроизведено грепом: комментарий гласит «`ID_MAX` ВВОЗИТСЯ И НЕ ЗОВЁТСЯ В ЭТОМ ФАЙЛЕ НИ РАЗУ, И ЭТО НАМЕРЕННО», а `:352` несёт `if value < 1 or value > ID_MAX:` в исполняемом коде `_ad_id_from_form` | ⚠️ Warning | Цена названа ревизией и признана верной: запись объявляет ввоз ЧИСТО ТРАНЗИТНЫМ, то есть удаляемым. Читатель, снявший ввоз при уборке транзитных имён, уронит `_ad_id_from_form` на `NameError` — на маршруте подтверждённого удаления расписания, в проде, а не на прогоне |
| `app/pages/history.py`, `app/pages/admin.py`, `app/pages/ads.py` | `history.py:1057`, `:586`; `ads.py:629`; `admin.py` через `parse_account_id` | **`WR-04-A` — ШЕСТЬ ВХОДОВ ОТДАЮТ 500 НА НЕОГРАНИЧЕННОМ ИДЕНТИФИКАТОРЕ.** ВОСПРОИЗВЕДЕНО ЗОНДОМ ВЕРИФИКАТОРА: `500 × 6` | ⚠️ Warning | ВНЕ ПРЕДМЕТА ЦЕЛИ ФАЗЫ (это не места подтверждения удаления), и потому не гэп. НО ДОЛГ НАЗВАН БЕЗ ВЛАДЕЛЬЦА: записи `CATALOGUE_APPENDIX` с вердиктом `open` и `deferred-items.md` делают его видимым, а `test_every_appendix_entry_is_still_outside_the_universe` — машинно стерегомым; ни одна последующая фаза вехи (11-15) его предметом не называет |
| `app/routes/{ads,accounts,schedules}.py` | 32 вхождения `_id: int` | **`WR-04` — тот же класс живёт в слое маршрутов, и вселенная гейта этого не объявляет числом** | ⚠️ Warning | Замер подтверждает наличие класса. Утверждение уникальности при этом НЕ ЛОЖНО: `test_the_identifier_bound_is_declared_exactly_once_in_the_whole_app` говорит об ОБЪЯВЛЕНИИ величины по всему `app/`, а не о её ПРИМЕНЕНИИ. Расходится не запись с деревом, а ОХВАТ гейта полноты (`CATALOGUE_DIRECTORY = APP_DIRECTORY / "pages"`) с ожиданием читателя |
| `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` | 55, 67 | **`IN-03` — ТОТ ЖЕ КЛАСС, ЧТО БЛОКЕР ПРЕЖНЕГО КРУГА, В СОСЕДНЕМ МОДУЛЕ.** ВОСПРОИЗВЕДЕНО: синтетический `.planning/phases/99-…/99-UAT.md` (штатное продвижение проекта) роняет `test_the_declared_vocabularies_and_exemptions_agree` — `assert 12 == 11` | ⚠️ Warning | ⚠️ ЧЕМ ЭТО ОТЛИЧАЕТСЯ ОТ ЗАКРЫТОГО БЛОКЕРА И ПОЧЕМУ НЕ ГЭП: прежний вшитый литерал делал зелень условной на том, что ФАЗА НЕ ЗАКРЫТА, — он блокировал СОБСТВЕННУЮ цель. Этот краснеет на ПЕРВОМ артефакте СЛЕДУЮЩЕЙ фазы, то есть цели Фазы 10 не мешает. Правило, заведённое планом 10-17 против рецидива, стережёт ВЕРДИКТ, а не разность счётов, и этот случай в его признак не попадает |
| `.planning/ROADMAP.md` | строка `**Plans**` раздела Фазы 10 | Строка состава читает «32 плана, из них **16** исполнено», тогда как замер счётом отметок раздела даёт **32 из 32** `- [x]` при 32 файлах `*-SUMMARY.md` и `progress.total_plans == completed_plans == 70` | ⚠️ Warning | Собственная идиома фазы («прежнее значение не было ошибкой — оно устарело») в момент отгрузки САМА устарела. Гейт `test_state_progress_matches_roadmap` этого не ловит по построению: он читает ОТМЕТКИ, а не прозу строки состава |
| `tests/test_templates/test_components.py` | `MODAL_EVENT_RE` @834 | **`IN-A` (находка настоящего круга, ревизией не предъявлена):** инвентарь считает ЧИСЛО диспетчеризаций и ЧИСЛО различных имён, но правила, сличающего имя события ТРИГГЕРА с именем панели-СЛУШАТЕЛЯ, в дереве нет | ℹ️ Info | Связь сегодня цела — проверено скриптом верификатора, все 9 префиксов имеют пару. Но триггер, диспетчеризующий имя, которого не слушает ни одна панель, прошёл бы ВСЕ гейты инвентаря молча, а место подтверждения было бы мёртвым — то есть критерий 1 («все 18 мест РАБОТАЮТ») машиной проверен не сквозь |
| файлы фазы под `app/` | — | Долговые маркеры `TBD`/`FIXME`/`XXX` | ℹ️ Info | НОЛЬ. Единственное вхождение `TBD` во всём диффе фазы — синтетическая строка внутри поддельного тела роадмапа (`tests/test_planning/test_state_progress_matches_roadmap.py:212`), то есть данные фикстуры, а не долг. `TODO`/`HACK`/`PLACEHOLDER` в `app/` — ноль |

### Human Verification Required

Восемь пунктов. Семь — обход `10-UAT.md` (шесть объявленных проверок; проверка 1 разбита на три
наблюдения, потому что закрывает три РАЗНЫЕ истины). Восьмой — не проверка стенда, а решение
владельца.

#### 1. Осиротевшая панель после N удалений (проверка 1, шаги 1.1-1.8) — КРИТЕРИЙ 2

**Тест:** открыть редактор объявления с ≥3 расписаниями, удалить расписание панелью
подтверждения, повторить N раз; затем в консоли посчитать
`document.querySelectorAll('[role="dialog"]').length` и
`document.querySelectorAll('[id^="sched-del-"]').length`.
**Ожидание:** число панелей равно числу оставшихся карточек; `#sched-N` и `#sched-del-N`
удалённого расписания отсутствуют ОБА.
**Почему человек:** это ВТОРАЯ ПОЛОВИНА ЦЕЛИ ФАЗЫ. Присутствие и различность узлов доказаны;
их ПРИМЕНЕНИЕ делает рантайм разметки, которого суита не поднимает, а браузерных зависимостей
в проекте нет.

#### 2. Ветвь перехода (проверка 1, шаги 1.10-1.12)

**Тест:** `window.__uat10 = 'жив'`, удалить ПОСЛЕДНЕЕ расписание, затем прочитать
`window.__uat10` и повторить счёт диалогов.
**Ожидание:** экран сменился переходом (204 + `HX-Location`), `window.__uat10` === `'жив'`,
счёт диалогов `1` и `0`.
**Почему человек:** ветвь перехода наблюдается только рантаймом браузера.

#### 3. Живость клиентского слоя после свопа (проверка 1, шаги 1.13-1.15) — `CR-03`

**Тест:** консоль пришедшего экрана; напечатать символы в `#text`; нажать крестик на плитке
вложения.
**Ожидание:** ранней ошибки языка нет; счётчик меняется на КАЖДЫЙ символ; плитка исчезает НА
МЕСТЕ.
**Почему человек:** ⚠️ ЛЕТОПИСЬ ОЖИДАНИЯ СОХРАНЯЕТСЯ: третий отчёт ОЖИДАЛ ЗДЕСЬ ОТКАЗ
(`SyntaxError: Identifier 'IMAGE_BASE_URL' has already been declared`); после правки плана
10-13 ожидание ПЕРЕВЁРНУТО. Прежнее ожидание не вычёркивается — оно было верным для дерева, на
котором записано.

#### 4. Уборка панели при не доехавшем follow-up GET (проверка 2)

**Тест:** Slow 3G, подтвердить удаление на месте, отвечающем 204 + `HX-Location`, и оборвать
follow-up GET (Offline сразу после нажатия).
**Ожидание:** панель уходит ПО СОБЫТИЮ ЗАВЕРШЕНИЯ ЗАПРОСА, а не по свопу тела; не остаётся
висеть поверх устаревшего экрана с `is-modal-open` на `<html>`.
**Почему человек:** ⚠️ ЗАМЕР ЭТОГО КРУГА ПОДКРЕПИЛ МЕХАНИЗМ, НО НЕ ПОРЯДОК. Бандл показывает,
что `htmx:afterRequest` летит из `g.onload` ПОСЛЕ возврата из `Vn`, — значит слушатель
приходит. Но `_run_modal_lifecycle` САМ объявляет три утверждения о ПОРЯДКЕ задач рантайма вне
своего охвата, а порядок здесь и есть предмет.

#### 5. Гейт ухода панели на живом стенде, `DEF-09-02` (проверка 3)

**Тест:** подтвердить удаление на медленной сети и, не дожидаясь ответа, нажать Esc, щёлкнуть
по оверлею и нажать «Отмена».
**Ожидание:** панель НЕ уходит ни одним из трёх путей, пока запрос летит; виден `.form-busy`;
«Отмена» остаётся в обходе по Tab. После ответа все три пути снова работают.
**Почему человек:** окно 35 — что человек видит панель ЗАНЯТОЙ, а не сломанной, снимается
только глазом.

#### 6. Панель остаётся открытой на отказе (проверка 4)

**Тест:** вызвать 500 на маршруте за панелью подтверждения и наблюдать панель.
**Ожидание:** панель ОСТАЁТСЯ открытой, поверх встаёт плашка аварии, повтор возможен без
переоткрытия.
**Почему человек:** половина D-12; окно 22.

#### 7. Приземление фокуса (проверка 5)

**Тест:** подтвердить удаление и проверить `document.activeElement` после ухода панели на обоих
путях.
**Ожидание:** на фрагментном пути фокус приземлился в `#notice`, а не на `<body>`.
**Почему человек:** приземление поднимает Alpine и настоящий своп. Читать вместе с записанной
границей: на фрагментном пути `#notice` ПУСТА всегда (плашки на успех нет по D-03).

#### 8. РЕШЕНИЕ ВЛАДЕЛЬЦА: прочтение D-13 критерия 3 — записью, а не прозой

**Тест:** добавить в шапку настоящего отчёта запись `overrides` либо отклонить прочтение.

```yaml
overrides:
  - must_have: >-
      Панель закрывается существующим Alpine (`x-on:htmx:after-request`) — новых
      строк JS фаза не добавляет (машинно: диффом по `app/static/js/` и шаблонам)
    reason: >-
      Прочтение D-13, записанное в `10-CONTEXT.md` ДО правки: критерий запрещает НОВЫЕ
      СУЩНОСТИ, а не правку существующих. Буквальное прочтение запретило бы починку,
      которую просят критерий 2 и критерий приземления фокуса той же фазы. Дифф по
      `app/static/js/` ПУСТ; две строки в `ads/form.html` (`:308`, `:538`) — обёртка
      немедленно вызываемым выражением, чинящая регрессию, заведённую этой же фазой,
      и тело скрипта не правлено ни на символ. Прочтение переведено в машинный гейт
      `test_criterion_three_holds_by_the_numbers` (четыре числа сущностей, при каждом
      отрицательный контроль).
    accepted_by: "<имя>"
    accepted_at: "<ISO 8601>"
```

**Почему человек:** прочтение чужого критерия есть решение владельца, а не исполнителя.
Формулировка критерия в роадмапе намеренно НЕ амендирована — но три круга подряд расхождение
живёт прозой отчёта, тогда как для него есть машинно читаемое место. Пока записи нет, у
следующего круга нет способа отличить принятое отступление от незамеченного.

⚠️ **РАЗРЕШЕНО. ПРИПИСКА ПЛАНА 10-40 ОТ 2026-09-10; ВСЁ, ЧТО ВЫШЕ, ВКЛЮЧАЯ ОБРАЗЕЦ БЛОКА,
СТОИ́Т ДОСЛОВНО И НЕ ВЫЧЁРКИВАЕТСЯ** — это запись того, сколько кругов вопрос стоял, и
стереть её значило бы стереть основание, по которому он наконец решён (идиома D-30/D-32,
`.planning/STATE.md`).

**Чем разрешено:** решением владельца `chubav` от **2026-09-09**, ветвь **`accept-reading`**
(блокирующий останов задачи 1 плана 10-40). Прочтение D-13 ПРИНЯТО явной записью
`overrides[0]` в шапке настоящего отчёта; `overrides_applied` приведён к `1` — числу
записей блока. Отклонённая ветвь — `reject-reading`; её заготовки в файле не оставлено.

**Чем запись ШИРЕ образца выше:** образец накрывал две строки обёртки `ads/form.html:308` /
`:538`, названные пятым кругом. Владелец подтвердил при ответе, что запись накрывает ВЕСЬ
ПЕРЕЗАМЕРЕННЫЙ на HEAD `c45d9c5` перечень — **51 строку JS в двух файлах**:
`ads/form.html` 38 (исполняемых 2 — `:308`, `:538`; комментария 36 — `:309-342`,
`:539-540`) и `components/modal.html` 13 (`:699-707`, `:713`, `:716`, `:777`, `:782`,
дописаны планом 10-35). Дифф по `app/static/js/` на том же HEAD **ПУСТ**. Четыре числа
сущностей, найдено/объявлено: файлов сценариев **2/2**, встроенных `onsubmit` **1/1**,
`hx-on` **0/0**, узлов `x-data` **24/24**; гейт `test_criterion_three_holds_by_the_numbers`
зелен, отбор непуст.

**Чего приписка НЕ делает:** поле `status` шапки, счёт истин, список гэпов и блок повторной
верификации планом 10-40 не тронуты ни байтом — они принадлежат верификатору. Запись
отступления есть ОСНОВАНИЕ для следующего круга, а не сам вердикт. Код не правился ни
строкой ни одной ветвью.

### Gaps Summary

**Гэпов нет.** Единственный провал прежнего круга закрыт и ВОСПРОИЗВЕДЁН верификатором в обе
стороны; ни одна из восьми истин на этом круге не FAILED.

Фаза стои́т в одном шаге от цели, и шаг этот — не код. **Вторая половина цели фазы — «после N
удалений в документе не остаётся N мёртвых диалогов» — не наблюдалась НИКЕМ И НИ РАЗУ.**
Роадмап предупредил об этом заранее, назвав пункт «единственным выводом исследования, взятым из
документации и не проверенным НИ НА ЧЁМ». Присутствие и различность парных OOB-узлов доказаны
машиной; их применение делает рантайм разметки, которого суита не поднимает, а браузерных
зависимостей в проекте нет ни одной. `10-UAT.md` заведён, самозаверения не допускает (проверено
мутацией) — и не пройден.

Отдельно и НЕ как гэпы названы шесть предупреждений таблицы Anti-Patterns. Два из них —
`WR-03` в самом рычаге фазы и `WR-01` в `app/pages/schedules.py` — принадлежат ровно тому
классу («запись шире дерева»), за который фаза получила круги 3, 4, 5 и 6, и оба ОТГРУЖЕНЫ ЭТОЙ
ЖЕ ПАРТИЕЙ вместе с механизмами, заведёнными против этого класса. Поведения они не трогают и
цели не мешают, но следующий круг закрытия гейпов, если он будет, начинается именно с них, а не
с продукта.

---

_Verified: 2026-09-08T21:55:00Z_
_Verifier: Claude (gsd-verifier), ПЯТЫЙ круг_
