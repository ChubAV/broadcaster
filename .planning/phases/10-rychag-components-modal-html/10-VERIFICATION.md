---
phase: 10-rychag-components-modal-html
verified: 2026-09-04T18:30:00Z
status: gaps_found
score: 6/9 must-haves verified
behavior_unverified: 2
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/7
  previous_verified: 2026-09-03T23:05:00Z
  gaps_closed:
    - >-
      `CR-01` — прямой POST мимо браузера на маршрут подтверждённого удаления даёт отказ
      валидации, а не 500. ЗАКРЫТ И ПРОВЕРЕН НЕЗАВИСИМЫМ ЗОНДОМ ВЕРИФИКАТОРА, а не принят
      по отчёту: `POST /schedules/<25 девяток>/delete` → **422** там, где прежний круг
      своими руками получил **500**.
    - >-
      `CR-02` — четыре соседних маршрута того же файла. ЗАКРЫТ ТЕМ ЖЕ ЗОНДОМ ОДНИМ
      ПРОГОНОМ: все пять входов отвечают **422**, ни одного `OverflowError`. Корень
      действительно один: `ID_MAX` + три псевдонима на границе приложения.
    - >-
      `CR-03` — МАШИННАЯ ПОЛОВИНА. Механизм отказа (ранняя ошибка языка о повторном
      объявлении) устранён обёрткой немедленно вызываемым выражением и доказан
      ИСПОЛНЕНИЕМ исходника ДВАЖДЫ В ОДНОЙ ОБЛАСТИ ИМЁН в НАСТОЯЩЕМ интерпретаторе
      (`node -e`, `vm.createContext`), с мутационным контролем. Это не разбор текста —
      это прогон. ⚠️ БРАУЗЕРНАЯ ПОЛОВИНА НЕ ЗАКРЫТА и не заявлена закрытой.
    - >-
      Документный гэп — отметка `FORM-06` возвращена в незавершённое состояние
      (`- [ ]` и клетка `Gaps Found`), и у правила появилось ПРИНУЖДЕНИЕ: девять правил
      `tests/test_planning/test_requirement_completion_follows_verification.py`, зелёные.
  gaps_remaining: []
  regressions:
    - >-
      НОВАЯ, ЗАВЕДЁННАЯ ТРЕТЬЕЙ ПАРТИЕЙ: модуль принуждения плана 10-14 вшил в свои
      отрицательные контроли ПЕРЕХОДНОЕ состояние живого дерева `.planning/`. Прогон
      `just test` зелен ТОЛЬКО пока вердикт этой фазы дословно `gaps_found`.
      Воспроизведено верификатором подменой поля и прогоном, в обе стороны.
  corrections:
    - >-
      РАСШИРЕНИЕ НАХОДКИ РЕВИЗИИ, А НЕ ПОВТОР ЕЁ. `10-REVIEW.md` (`CR-01`) называет
      предметом вердикт `passed`. ЗАМЕР ЭТОГО КРУГА ПОКАЗЫВАЕТ, ЧТО ПРЕДМЕТ ШИРЕ:
      правило вшито в ЛИТЕРАЛ `gaps_found`, поэтому краснеет на ЛЮБОМ ином вердикте —
      `passed` роняет ДВА правила, `human_needed` роняет ОДНО. Ревизия не ошиблась: она
      измерила один вердикт из двух и назвала его верно.
gaps:
  - truth: >-
      Правило, принуждающее регламент отметок требований, не краснеет от штатного
      продвижения проекта — в том числе от вердикта, которым эта фаза достигает цели
    status: failed
    reason: >-
      ВОСПРОИЗВЕДЕНО ВЕРИФИКАТОРОМ ПРОГОНОМ В ОБЕ СТОРОНЫ, а не унаследовано от ревизии.
      Оба отрицательных контроля читают ЖИВОЕ дерево (`premature_completions(doctored,
      PLANNING_ROOT)`) и утверждают его СЕГОДНЯШНЕЕ переходное значение дословно:
      `assert only.verdict == "gaps_found"` (`:323`) и `assert named == {"FORM-06",
      "FORM-03"}` (`:348`).
      ЗАМЕР 1 — поле `status` подменено на `passed`, прогон модуля:
      **2 failed, 7 passed** (`test_control_negative_a_premature_completion_reddens_the_rule`,
      `test_control_negative_two_premature_completions_are_both_named`).
      ЗАМЕР 2 — поле подменено на `human_needed`, прогон того же модуля:
      **1 failed, 8 passed**, отказ дословно `+ human_needed` против ожидаемого
      `gaps_found`.
      ЗАМЕР 3 — поле возвращено в `gaps_found`: **9 passed**.
      ⚠️ ПОЧЕМУ ЭТО БЛОКЕР, А НЕ ПРИДИРКА. Модуль заведён РАДИ ПРИНУЖДЕНИЯ РЕГЛАМЕНТА, и
      он же делает `just test` — команду, которую в проекте запускают все, — зелёной
      РОВНО ПРИ УСЛОВИИ, ЧТО ФАЗА НЕ ДОСТИГЛА ЦЕЛИ. Дойти до `passed` можно только
      сломав собственный гейт проекта; починить его придётся правкой утверждений, что
      через круг превращает его в тест, «который принято подгонять», — ровно тот класс
      отказа, который сам файл осуждает в своей шапке («хрупкое правило отключают, и
      вместе с ним отключается свойство»). Свойство, которое при этом отключится, —
      единственное машинное принуждение регламента отметок требований во всём дереве.
      ⚠️ НАСТОЯЩИЙ ОТЧЁТ НЕСЁТ `gaps_found` И ПОТОМУ СУИТУ НЕ РОНЯЕТ. Это НЕ смягчающее
      обстоятельство, а сам предмет: зелень держится совпадением вердикта с вшитым
      литералом, то есть тем, что фаза НЕ закрыта.
    artifacts:
      - path: "tests/test_planning/test_requirement_completion_follows_verification.py:304-328"
        issue: "`test_control_negative_a_premature_completion_reddens_the_rule` читает `PLANNING_ROOT` и утверждает `only.verdict == \"gaps_found\"` — вердикт живого дерева"
      - path: "tests/test_planning/test_requirement_completion_follows_verification.py:330-350"
        issue: "`test_control_negative_two_premature_completions_are_both_named` утверждает ОТСУТСТВИЕ отчёта Фазы 11 (`absent.verdict is None`) — падёт от появления любого отчёта Фазы 11"
    missing:
      - "Питать ОБА отрицательных контроля СИНТЕТИЧЕСКИМ деревом записи, а не `PLANNING_ROOT`: функция `premature_completions(requirements_text, planning_root)` уже принимает корень ПАРАМЕТРОМ — подменить второй вход так же, как уже подменяется первый (`_with_status`)"
      - "Собрать фиктивный корень в `tmp_path` с двумя отчётами известных вердиктов (`gaps_found` и отсутствие отчёта), чтобы предмет контроля задавался самим контролем, а не состоянием проекта на день прогона"
      - "После правки — показать зубы обоих контролей мутацией, как это сделано у оси адреса (`test_control_a_shortened_axis_list_reddens_the_axis_count`), иначе синтетика заменит вшитое состояние на непроверенное"
      - "Отдельным правилом утвердить, что ни один модуль `tests/test_planning/` не читает поле `status` отчёта СВОЕЙ фазы в утверждении — иначе следующий гейт регламента повторит расхождение"
deferred: []
behavior_unverified_items:
  - truth: >-
      Критерий 2: ответ снимает осиротевшую панель подтверждения вторым OOB-узлом
      `hx-swap-oob="delete"` по её `id` — после N удалений в документе не остаётся
      N мёртвых диалогов
    test: >-
      Шаги 1.1-1.8 обхода `10-UAT.md`: открыть редактор объявления с ≥3 расписаниями,
      удалить расписание через панель подтверждения, повторить N раз; затем в консоли
      `document.querySelectorAll('[role="dialog"]').length` и
      `document.querySelectorAll('[id^="sched-del-"]').length`.
    expected: >-
      Число панелей равно числу оставшихся карточек; `#sched-N` и `#sched-del-N`
      удалённого расписания отсутствуют ОБА; живых ловушек фокуса не осталось.
    why_human: >-
      Это ВТОРАЯ ПОЛОВИНА ЦЕЛИ ФАЗЫ и единственный вывод, взятый роадмапом из
      документации и не проверенный ни на чём (критерий 2, «фактическое снятие — ручной
      UAT, пункт 4»). Доказано ПРИСУТСТВИЕ обоих узлов в теле ответа
      (`sched_delete_response.html:71-72`) и структурная предпосылка (панель СНАРУЖИ
      удаляемой строки — `{% call modal %}` на `:274` ПОСЛЕ `</article>` на `:269`).
      ПРИМЕНЕНИЕ узлов делает рантайм разметки; суита идёт через `ASGITransport` на
      in-memory SQLite и не исполняет ни строчки JS. Браузерных зависимостей в проекте
      нет (ни playwright, ни selenium). `10-UAT.md` — `status: testing`, таблицы отметок
      ПУСТЫ, обход не проходился ни разу.
  - truth: >-
      `CR-03`, БРАУЗЕРНАЯ ПОЛОВИНА: после перехода `HX-Location` человек видит ЖИВОЙ
      клиентский слой редактора, а не молча умерший
    test: >-
      Шаги 1.13-1.15 обхода `10-UAT.md`: удалить ПОСЛЕДНЕЕ расписание; НЕ перезагружая
      документ, прочитать консоль пришедшего экрана; напечатать символы в поле `#text`;
      нажать крестик на плитке вложения.
    expected: >-
      Ранней ошибки языка о повторном объявлении имени в консоли НЕТ; число в
      `#text-counter` меняется на КАЖДЫЙ символ; плитка исчезает НА МЕСТЕ, документ НЕ
      перезагружается.
    why_human: >-
      Машинная половина ДОКАЗАНА ПРОГОНОМ, и это честная граница, а не заявление:
      исходник исполнен ДВАЖДЫ В ОДНОЙ ОБЛАСТИ ИМЁН настоящим интерпретатором
      (`vm.createContext`, `node -e`), оба прогона зелены, мутация «вернуть объявление
      верхнего уровня» краснит правило. Но интерпретатор — не браузер: он не свопает
      `innerHTML`, не пересоздаёт узел скрипта и не вешает обработчики на новые узлы
      документа. Что после НАСТОЯЩЕГО свопа клиентский слой ЖИВ, не наблюдал никто.
      План 10-13 этого и не заявляет.
human_verification:
  - test: >-
      Шаги 1.1-1.8 обхода `10-UAT.md` — счёт `[role="dialog"]` и `[id^="sched-del-"]`
      после N удалений через панель.
    expected: "После N удалений осиротевших диалогов с живыми ловушками фокуса в документе нет."
    why_human: "OOB-снятие применяет рантайм htmx; httpx свопов не делает. Критерий 2 роадмапа — вторая половина цели фазы."
  - test: >-
      Шаги 1.10-1.12 обхода `10-UAT.md`: `window.__uat10 = 'жив'`, удалить ПОСЛЕДНЕЕ
      расписание, затем прочитать `window.__uat10` и повторить счёт диалогов.
    expected: "Экран сменился переходом (204 + `HX-Location`), `window.__uat10` === `'жив'`, счёт диалогов `1` и `0`."
    why_human: "Ветвь перехода наблюдается только рантаймом браузера."
  - test: >-
      Шаги 1.13-1.15 обхода `10-UAT.md`: консоль пришедшего экрана, счётчик символов,
      снятие вложения.
    expected: >-
      Ранней ошибки языка нет; счётчик меняется на каждый символ; плитка снимается на
      месте без перезагрузки документа.
    why_human: >-
      ⚠️ ЛЕТОПИСЬ ОЖИДАНИЯ: прежний отчёт ОЖИДАЛ ЗДЕСЬ ОТКАЗ (`SyntaxError: Identifier
      'IMAGE_BASE_URL' has already been declared`). После правки плана 10-13 ожидание
      ПЕРЕВЁРНУТО и спрашивает о ЖИВОСТИ. Прежнее ожидание не вычёркивается — оно было
      верным для дерева, на котором записано.
  - test: >-
      Замедлить сеть (Slow 3G), подтвердить удаление на месте, отвечающем 204 +
      `HX-Location`, и оборвать follow-up GET (Offline сразу после нажатия).
    expected: >-
      Панель уходит ПО СОБЫТИЮ ЗАВЕРШЕНИЯ ЗАПРОСА, а не по свопу тела: не остаётся
      висеть поверх устаревшего экрана с `is-modal-open` на `<html>`.
    why_human: "Правкой плана 10-05 пункт АДРЕСОВАН, но машинно не закрыт — окно 28 `.planning/WINDOWS.md`. Шаг 2 обхода."
  - test: >-
      Подтвердить удаление на медленной сети и, не дожидаясь ответа, нажать Esc,
      щёлкнуть по оверлею и нажать «Отмена».
    expected: >-
      Панель НЕ уходит ни одним из трёх путей, пока запрос летит; виден узел индикатора
      (`.form-busy`); «Отмена» остаётся доступной в обходе по Tab. После ответа все три
      пути снова работают.
    why_human: "Гейт `DEF-09-02` доказан поведенчески и мутацией, но что человек видит панель ЗАНЯТОЙ, а не сломанной, снимается только глазом — окно 35."
  - test: "Вызвать 500 на маршруте за панелью подтверждения и наблюдать панель."
    expected: "Панель ОСТАЁТСЯ открытой, поверх встаёт плашка аварии, повтор возможен без переоткрытия."
    why_human: "Половина D-12: в суите доказана на реальной форме события отказа, на живом стенде — глазом. Окно 22."
  - test: "Подтвердить удаление и проверить `document.activeElement` после ухода панели на обоих путях."
    expected: "На фрагментном пути фокус приземлился в `#notice`, а не на `<body>`."
    why_human: >-
      Приземление фокуса поднимает Alpine и настоящий своп. ⚠️ Читать вместе с WR-04
      ревизии: на фрагментном пути `#notice` ПУСТА всегда (плашки на успех нет по D-03).
---

# Phase 10: Рычаг `components/modal.html` — Verification Report (четвёртый круг)

**Phase Goal:** подтверждение удаления идёт через htmx во всех 18 местах одной правкой одного файла (`app/templates/components/modal.html`), и после N удалений в документе не остаётся N мёртвых диалогов
**Verified:** 2026-09-04T18:30:00Z
**Status:** gaps_found
**Re-verification:** Да — после ТРЕТЬЕЙ партии закрытия гейпов (планы 10-12…10-16). Заменяет отчёт от 2026-09-03T23:05 (`gaps_found`, 4/7). HEAD `abedc0e`.

## Что этот круг измерил своими руками

Три замера, каждый — собственным прогоном верификатора, ни один не принят по SUMMARY.

**Замер 1 — пять входов, ради которых заведена третья партия.** Верификатор поднял
приложение на фикстурах проекта и послал те же пять запросов, которыми прежний круг
получил пять пятисоток. Зонд заведён, прогнан и снят; в дереве не оставлен. Результат
дословно:

```
delete(path): 422    edit(path): 422    toggle(path): 422
new(ad_id form): 422    new(account_id form): 422
```

Прежний круг на том же месте получил `500 × 5` с `OverflowError` в каждом. **Гэпы
`CR-01` и `CR-02` закрыты, и это подтверждено воспроизведением, а не отчётом.**

**Замер 2 — машинная половина `CR-03`.** Правило исполняет исходник инлайн-скрипта
редактора ДВАЖДЫ в ОДНОЙ области имён настоящим интерпретатором (`node -e` +
`vm.createContext`, `tests/conftest.py:384`), и отсутствие интерпретатора роняет правило
ГРОМКО, а не пропускает его. Прогон модуля — **10 passed**. Это НЕ разбор текста: это
прогон, и мутационный контроль (`test_a_reinstated_top_level_binding_reddens_the_double_execution_rule`)
показывает зубы.

**Замер 3 — блокер ревизии, проверенный в ОБЕ стороны и оказавшийся ШИРЕ заявленного.**

| Поле `status` отчёта | Прогон `tests/test_planning/test_requirement_completion_follows_verification.py` |
| --- | --- |
| `gaps_found` (сегодня) | **9 passed** |
| `human_needed` | **1 failed**, 8 passed |
| `passed` (цель фазы) | **2 failed**, 7 passed |

⚠️ **ГЛАВНЫЙ ВЫВОД КРУГА.** Ревизия назвала предметом вердикт `passed`. Замер показывает,
что правило вшито в ЛИТЕРАЛ `gaps_found` и краснеет на ЛЮБОМ ином вердикте. То есть
собственный гейт проекта зелен РОВНО ПРИ УСЛОВИИ, ЧТО ЭТА ФАЗА НЕ ЗАКРЫТА. Настоящий
отчёт несёт `gaps_found` и потому суиту не роняет — но это не смягчающее обстоятельство,
а сам предмет: зелень держится совпадением вердикта с вшитым литералом.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | **Критерий 1.** Все 18 мест подтверждения работают через htmx одной правкой `modal.html`; инвентарь утверждён ЧИСЛОМ | ✓ VERIFIED | Регрессия держится. Макрос печатает БЕЗУСЛОВНО `hx-post="{{ action }}"`, `hx-swap="none"`, `hx-disabled-elt`, `hx-indicator`, `x-on:htmx:after-request` и узел `.form-busy`; конструкций шаблонизатора в открывающем теге формы нет. Прогон трёх файлов гейтов этим кругом: **218 passed** (было 214 — четыре новых правила третьей партии). |
| 2 | **Критерий 2.** Ответ снимает осиротевшую панель вторым OOB-узлом `hx-swap-oob="delete"` по её `id` | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Разметка на месте и адресована верно (`sched_delete_response.html:71-72` — оба узла); структурная предпосылка держится — `{% call modal(id='sched-del-' ~ s.id …) %}` на `sched_card.html:274` стои́т ПОСЛЕ `</article>` на `:269`, панель СНАРУЖИ удаляемой строки. ПРИМЕНЕНИЕ узлов рантаймом не наблюдал никто: `10-UAT.md` по-прежнему `status: testing`, таблицы отметок ПУСТЫ. Состояние по существу то же, что кругом раньше. → человеку. |
| 3 | **Критерий 3.** Панель закрывается существующим Alpine — новых строк JS фаза не добавляет | ✓ VERIFIED (с оговоркой) | `git diff 7c1041d..HEAD -- app/static/js/` **ПУСТ** (проверено этим кругом). Гейт четырёх чисел зелен. ⚠️ ОГОВОРКА, НАЗВАННАЯ ПРЯМО: правка плана 10-13 добавила в ШАБЛОН две строки JS (`(function () {` на `ads/form.html:308` и `})();` на `:538`), а формулировка критерия называет предметом диффом «`app/static/js/` **и шаблоны**». Дословно критерий этим тронут. Держится он ПРОЧТЕНИЕМ ВЛАДЕЛЬЦА D-13 («критерий запрещает НОВЫЕ СУЩНОСТИ, а не правку существующих»), записанным планом 10-04 ДО этой правки и переведённым в числа гейтом, который считает СУЩНОСТИ (набор вендоренных файлов, файлы с инлайн-обработчиком отправки, атрибуты событий), а не строки. Прочтение защитимо и заведено не задним числом, но названо здесь, а не проглочено — см. `⚠️ Warning` таблицы Anti-Patterns. |
| 4 | **Критерий 4.** Все 10 потребителей продолжают удалять без JS: панель остаётся настоящей формой POST с непустым `action` | ✓ VERIFIED | `method="post"` — ЛИТЕРАЛ, `action="{{ action }}"` и `hx-post="{{ action }}"` выписаны ТЕМ ЖЕ выражением. Гейты деградации Фазы 8 держат наследование; все зелены в прогоне 218. |
| 5 | `DEF-09-02` — «Отмена» панели во время летящего необратимого запроса | ✓ VERIFIED | Регрессия держится. Гейт `if (sending) return; hide()` на всех трёх путях ухода по воле человека; `DISMISSAL_PATHS_DECLARED = 3` краснит четвёртый негейтированный путь. |
| 6 | Прямой POST мимо браузера на маршрут подтверждённого удаления даёт отказ валидации, а не 500 (`CR-01`), и соседние маршруты того же файла держат тот же инвариант (`CR-02`) | ✓ VERIFIED | **ЗАКРЫТО И ВОСПРОИЗВЕДЕНО НЕЗАВИСИМО.** Корень один и снят в одном месте: `ID_MAX = 2_147_483_647` (`schedules.py:68`) и три псевдонима `ScheduleIdPath`/`AdIdForm`/`AccountIdForm` (`:85-91`) на ГРАНИЦЕ ПРИЛОЖЕНИЯ — значение отвергается ДО тела обработчика, то есть ДО `_ownership_verdict`, внутри которого падали два из пяти отказов. Применены во всех пяти входах (`:798`, `:805`, `:890-894`, `:971`, `:1022`). Зонд верификатора: **422 × 5** против **500 × 5** прежнего круга. Слепая зона гейта закрыта второй осью: ось АДРЕСА заведена, число осей объявлено (`UNUSABLE_VALUE_AXES_DECLARED = 2`), величина адреса ОТБИРАЕТСЯ ИЗ ПЕРЕЧНЯ по свойству «вне `ID_MAX`», а не выписывается вторым литералом — то есть прежняя ошибка выбора константы внутри диапазона повториться молча не может. |
| 7 | Транспорт, заведённый фазой, не ломает уже работающий JavaScript экрана, на который сам же приземляет (`CR-03`) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | **МАШИННАЯ ПОЛОВИНА ЗАКРЫТА ПРОГОНОМ, БРАУЗЕРНАЯ — НЕТ, И ЭТО ГРАНИЦА, А НЕ УКЛОНЕНИЕ.** Обёртка на месте (`ads/form.html:308`, `:538`), тело скрипта не правлено ни на символ, все 18 объявлений `const`/`let` (`:346-374`) ушли внутрь области функции. Свойство доказано ИСПОЛНЕНИЕМ: исходник рендеренной разметки прогоняется дважды в ОДНОЙ `vm`-области настоящим Node, оба прогона зелены, мутация «вернуть объявление верхнего уровня» краснит правило, отсутствие интерпретатора роняет правило громко (`pytest.skip` запрещён). Плюс статическое правило по ВСЕМ целям перехода (32 вызова слоя ответа без фрагмента) и изъятие экрана подключения с основанием (окно 46). ⚠️ ЧЕГО НЕ ДОКАЗАНО: интерпретатор не свопает `innerHTML`, не пересоздаёт узел скрипта и не вешает обработчики на новые узлы. Что после НАСТОЯЩЕГО свопа клиентский слой ЖИВ — шаги 1.13-1.15 обхода, не пройденные. План 10-13 этого и не заявляет. → человеку. |
| 8 | Запись требования соответствует состоянию дерева: `FORM-06` не объявляется завершённым, пока цель фазы не достигнута, и у правила есть ПРИНУЖДЕНИЕ | ✓ VERIFIED | Отметка возвращена и держится: `.planning/REQUIREMENTS.md:31` — `- [ ] **FORM-06**`, `:136` — `| FORM-06 | Phase 10 | Gaps Found |` (коммит `c899f04`). Принуждение заведено планом 10-14: девять правил, читающих ДВА машинных поля (клетка состояния и поле `status` шапки отчёта) и не судящих прозу ни строкой; число строк таблицы объявлено (`REQUIREMENT_ROWS_DECLARED = 44`), строка без фазы законна только у отложенной, индекс собирается ОБХОДОМ всего дерева (архивные фазы в нём есть). Прогон: **9 passed**. ⚠️ Ни один план третьей партии `FORM-06` завершённым не пометил — при живом блокере это верное поведение, и оно наблюдается в дереве, а не заявляется. |
| 9 | Правило, принуждающее регламент отметок, не краснеет от штатного продвижения проекта — в том числе от вердикта, которым эта фаза достигает цели | ✗ FAILED | **НОВЫЙ ГЭП, ЗАВЕДЁННЫЙ ТРЕТЬЕЙ ПАРТИЕЙ. ВОСПРОИЗВЕДЁН В ОБЕ СТОРОНЫ.** Оба отрицательных контроля читают ЖИВОЕ дерево и утверждают его сегодняшнее переходное значение: `only.verdict == "gaps_found"` (`:323`), `absent.verdict is None` для Фазы 11 (`:348`). Замер: `passed` → **2 failed**; `human_needed` → **1 failed**; `gaps_found` → **9 passed**. Гейт проекта зелен ровно пока фаза не закрыта. Разбор — `gaps[0]`. |

**Score:** 6/9 truths verified (2 present, behavior-unverified; 1 failed)

⚠️ **ЧТО ИЗМЕНИЛОСЬ ПРОТИВ ПРЕЖНЕГО КРУГА, ЧЕСТНЫМ СЧЁТОМ.** Три провала прежнего круга
закрыты, и два из трёх — воспроизведением верификатора, а не по отчёту. Знаменатель вырос
с 7 до 9, потому что прежние истины 6 и «соседние маршруты» сведены в ОДНУ запись (корень
оказался один) и добавлены две новые: документная (истина 8, закрыта) и дефект самого
принуждения (истина 9, открыта). Фаза продвинулась существенно; она не закрыта по ОДНОЙ
причине, и причина эта — не в предмете фазы, а в инструменте, который третья партия завела
ради регламента.

### Deferred Items

Проверено по всем последующим фазам вехи (11, 12, 13, 14, 15). **Гэп `gaps[0]` не отложен
ни одной из них.** Ни одна последующая фаза не называет предметом ни модули
`tests/test_planning/`, ни регламент отметок требований, ни синтетические корни записи.
Фаза 15 («Упрочнение и сводный обход 47 форм») пятью своими критериями говорит о формах
письма, а не о гейтах записи. Совпадения нет — по правилу консервативности гэп остаётся
действующим.

*(Отдельно и НЕ как гэпы: окна 45 и 46 `.planning/WINDOWS.md` — расширение границы
величины на соседние страничные модули и сужение экрана подключения — записаны фазой САМОЙ
с названными кандидатами (15 и 11) и основанием, почему не сделано здесь. Это учтённый
долг, а не пропуск.)*

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `app/templates/components/modal.html` | Рычаг: безусловные htmx-атрибуты на все 18 мест | ✓ VERIFIED | Глагол — литерал; наследуется 10 потребителями; 218 гейтов зелены |
| `app/templates/ads/partials/sched_delete_response.html` | Три внеполосных узла, два с `hx-swap-oob="delete"` | ⚠️ HOLLOW (доставка доказана, применение — нет) | `:71-72` оба узла адресованы верно; применение рантаймом не наблюдал никто (истина 2) |
| `app/pages/schedules.py` | Граница величины идентификатора на всех входах | ✓ VERIFIED | `ID_MAX:68`, три псевдонима `:85-91`, применены в пяти входах; зонд даёт 422 × 5 |
| `app/templates/ads/form.html` | Инлайн-скрипт переживает переисполнение | ✓ VERIFIED (машинно) | Обёртка `:308`/`:538`, тело не правлено; двойное исполнение в Node зелено, мутация краснит |
| `tests/test_pages/test_hx_location_destinations.py` | Гарнир двойного исполнения + статическое правило целей | ✓ VERIFIED | 762 строки, 10 правил, все зелены; интерпретатор общий (`run_node_script`), пропуск запрещён |
| `tests/test_planning/test_requirement_completion_follows_verification.py` | Принуждение регламента отметок | ✗ ДЕФЕКТ | Несущие правила корректны и зелены; ДВА отрицательных контроля вшили переходное состояние живого дерева (истина 9) |
| `app/pages/htmx.py` | Один выход обработчика на оба транспорта | ✓ VERIFIED | `respond` / `location_response`; `_local_path` отвергает внешний адрес |
| `.planning/phases/10-.../10-UAT.md` | Артефакт ручного обхода со стендом и шагами | ✓ VERIFIED (как АРТЕФАКТ) | Шаги 1.13-1.15 добавлены планом 10-16, ожидание переходной ветки ПЕРЕВЁРНУТО с отказа на живость с летописью; ⚠️ `status: testing` — обход НЕ ПРОЙДЕН |
| `.planning/WINDOWS.md` | Окна третьей партии | ✓ VERIFIED | Окна 45, 46, 47, 48 на месте, каждое с основанием и кандидатом; фаза-владелец не назначена намеренно |
| `app/static/js/` | Не тронут | ✓ VERIFIED | `git diff 7c1041d..HEAD` пуст |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `components/modal.html` | маршрут удаления | `hx-post="{{ action }}"` | ✓ WIRED | Тот же символ, что и `action=`; сличение по сырой строке шаблона |
| `sched_card.html` | `modal.html` | `{% call modal(…) %}` ПОСЛЕ `</article>` | ✓ WIRED | `:274` после `:269` — панель СНАРУЖИ удаляемой строки; предпосылка критерия 2 держится |
| `schedules_delete` | `sched_delete_response.html` | `_fragment()` → `HTMLResponse` | ✓ WIRED | Счёт берётся `_ad_schedule_count`, реальный запрос |
| `schedules_delete` | `/ads/{id}/edit` | 204 + `HX-Location` | ✓ WIRED (вредоносность СНЯТА машинно) | Связь работает; механизм, которым она убивала скрипт цели, устранён обёрткой и доказан прогоном (истина 7) |
| `ScheduleIdPath`/`AdIdForm`/`AccountIdForm` | пять входов `schedules.py` | `Annotated[int, Path/Form(ge=1, le=ID_MAX)]` | ✓ WIRED | Статический гейт читает сигнатуры файла и краснит ШЕСТОЙ вход того же класса в момент введения |
| `premature_completions` | `10-VERIFICATION.md` | поле `status` шапки | ⚠️ WIRED, НО ВШИТО | Связь работает и именно поэтому роняет прогон при смене вердикта (истина 9) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `sched_delete_response.html` | `schedules_count` | `_ad_schedule_count(db, user.id, ad_id)` — `select(func.count())` | ✓ | ✓ FLOWING |
| `sched_delete_response.html` | `schedule_id` | Идентификатор ПУТИ запроса | ✓ | ✓ FLOWING (ограничен `ScheduleIdPath`) |
| `modal.html` | `action` | Параметр вызова макроса у 10 потребителей | ✓ | ✓ FLOWING |
| `test_requirement_completion…` | `only.verdict` | `PLANNING_ROOT` — ЖИВОЕ дерево записи | ✓ | ⚠️ FLOWING ИЗ ИЗМЕНЧИВОГО ИСТОЧНИКА — предмет истины 9 |
| `10-UAT.md` | таблицы отметок | ЧЕЛОВЕК | ✗ | ✗ DISCONNECTED (по замыслу — обход не пройден) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Пять входов `schedules.py` негодной величиной | зонд верификатора на фикстурах проекта (заведён, прогнан, снят) | **422 × 5** (прежний круг: 500 × 5) | ✓ PASS |
| Двойное исполнение инлайн-скрипта редактора в одной области имён | `pytest tests/test_pages/test_hx_location_destinations.py` | **10 passed** (настоящий Node + `vm.createContext`) | ✓ PASS |
| Гейты рычага, разметки и транспорта | `pytest tests/test_templates/test_components.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_confirm_delete_transport.py` | **218 passed** | ✓ PASS |
| Принуждение регламента отметок, вердикт `gaps_found` | `pytest tests/test_planning/test_requirement_completion_follows_verification.py` | **9 passed** | ✓ PASS |
| То же, поле `status` подменено на `human_needed` | тот же прогон | **1 failed**, 8 passed | ✗ FAIL |
| То же, поле `status` подменено на `passed` | тот же прогон | **2 failed**, 7 passed | ✗ FAIL |
| Состав суиты | `pytest tests/ -q --collect-only` | **2826 tests collected** — совпадает с числом партии | ✓ PASS |
| Фаза не трогала вендоренный JS | `git diff 7c1041d..HEAD -- app/static/js/` | пусто | ✓ PASS |
| Долги в изменённых файлах | `grep -nE "TBD\|FIXME\|XXX"` по 28 файлам диффа | ни одного | ✓ PASS |
| Снятие осиротевшей панели рантаймом | — | нечем: суита не поднимает рантайм разметки, браузерных зависимостей в проекте нет | ? SKIP → человеку |
| Живость клиентского слоя после свопа | — | нечем: `vm` — не браузер | ? SKIP → человеку |

⚠️ **О ПОДМЕНЕ ПОЛЯ В ДВУХ ЗАМЕРАХ.** Файл `10-VERIFICATION.md` правился временно, прогон
снимался, файл возвращался `git checkout` немедленно; состояние дерева проверено после
каждого возврата. Иначе утверждение ревизии осталось бы рассуждением: правило, о котором
спорят, обязано быть ПРОГНАНО в обоих состояниях.

### Probe Execution

Пробников конвенции `scripts/*/tests/probe-*.sh` в дереве нет; ни один из 16 планов фазы
их не объявляет. **SKIPPED** — предмета нет. Роль пробника исполнили три замера выше,
каждый прогнан верификатором и описан командой.

### Test Quality Audit

| Test File | Linked Req | Active | Skipped | Circular | Assertion Level | Verdict |
| --- | --- | --- | --- | --- | --- | --- |
| `test_hx_location_destinations.py` | FORM-06 (`CR-03`) | 10 | 0 | нет | Поведенческий (исполнение в интерпретаторе) + мутационные контроли | ✓ ГОДЕН |
| `test_confirm_delete_transport.py` | FORM-06 (`CR-01`) | — | 0 | нет | Значение (коды ответа) по ДВУМ осям, число осей объявлено | ✓ ГОДЕН |
| `test_components.py` / `test_htmx_markup_gates.py` | FORM-06 (крит. 1, 3, 4) | — | 0 | нет | Значение + числа инвентаря | ✓ ГОДЕН |
| `test_requirement_completion_follows_verification.py` | документный гэп | 9 | 0 | нет | Значение | ⚠️ НЕСУЩИЕ ПРАВИЛА ГОДНЫ, ОТРИЦАТЕЛЬНЫЕ КОНТРОЛИ ХРУПКИ |

**Отключённых правил на требовании:** 0.
**Круговых образцов:** 0 — ожидаемые величины ни в одном модуле не порождаются системой под
испытанием; величины негодных идентификаторов и коды ответов заданы независимо.
**Пропусков, неотличимых от зелёного:** 0 — `run_node_script` запрещает `pytest.skip`
явно и роняет правило при отсутствии интерпретатора.
**Хрупкие утверждения:** 2 → истина 9, `gaps[0]`.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| FORM-06 | 10-01…10-16 | Подтверждение удаления через htmx во всех 18 местах одной правкой `modal.html`; ответ снимает осиротевшую панель вторым OOB-узлом `hx-swap-oob="delete"` | ✗ BLOCKED (первая половина машинно закрыта, вторая — ЧЕЛОВЕЧЕСКАЯ и не наблюдалась) | `.planning/REQUIREMENTS.md:31` — `- [ ]`, `:136` — `Gaps Found`. Отметка НЕ поставлена, и это ВЕРНО: цель фазы не достигнута (истина 9 открыта, истины 2 и 7 ждут обхода). Ни один план третьей партии её не ставил. |

**Осиротевших требований нет:** `.planning/REQUIREMENTS.md:491` отображает на Фазу 10
ровно одно требование (`FORM-06`), и ровно оно объявлено во frontmatter планов.

### Decision Coverage

Гейт неблокирующий. Решения третьей партии наблюдаются в дереве поимённо: D-13 (прочтение
критерия 3 — «новые сущности, а не правка существующих») живёт в шапке `modal.html` и в
докстринге правила; решение «три псевдонима, а не один» записано у `ID_MAX` с основанием
(способ передачи есть свойство маршрута); решение «обёртка, а не правка тела скрипта»
записано у самой обёртки; решение «не чинить экран подключения сразу» — окно 46 с
названной ценой (три кнопки на `onclick` из атрибутов разметки). Потерянных при исполнении
решений не обнаружено.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `tests/test_planning/test_requirement_completion_follows_verification.py` | 304-350 | Утверждение вшивает изменчивое состояние живого дерева вместо синтетики | 🛑 Blocker | Прогон краснеет от штатного продвижения проекта; чинится правкой утверждений, что через круг превращает правило в «подгоняемое» — истина 9 |
| `app/templates/ads/form.html` | 308, 538 | Две строки JS добавлены в ШАБЛОН, тогда как критерий 3 называет предметом диффом «`app/static/js/` **и шаблоны**» | ⚠️ Warning | Держится прочтением D-13, записанным ДО правки и переведённым в числа; гейт четырёх чисел считает СУЩНОСТИ, а не строки, и потому расхождения не видит. Прочтение защитимо; названо, чтобы не проглотить |
| `tests/test_planning/test_requirement_completion_follows_verification.py` | `_verification_status_by_phase` | Индекс отчётов молча перезаписывается при совпадении номеров фаз между вехами | ⚠️ Warning | WR-08 ревизии; сегодня безвреден (совпадений нет), но переезд фазы в архив при одноимённом номере новой вехи выключит правило молча |
| `app/pages/schedules.py` | псевдонимы | Отказ валидации новых псевдонимов уходит сырым JSON мимо слоя ответа | ⚠️ Warning | WR-03 ревизии. Истину 6 не роняет (она требует «отказ валидации, а не 500» — получен 422), но на пути БЕЗ JS человек видит сырой JSON вместо экрана. Кандидат в окно |
| маршруты удаления | — | Пользовательские маршруты удаления без гарда происхождения, тогда как административные его несут | ⚠️ Warning | WR-07 ревизии; состояние УНАСЛЕДОВАННОЕ, не заведено этой фазой; предметом ни одного критерия фазы не является |
| `.planning/ROADMAP.md` | 287 | Строка состава «11 планов, из них 7 исполнено» пережила две партии | ℹ️ Info | Записано окном 44 как устаревшая летопись; план 10-11 правку раздела запрещает и требует брать число счётом отметок |

### Human Verification Required

Семь пунктов, из них ДВА — прямые следствия истин 2 и 7 (см. `behavior_unverified_items`).
Все семь живут шагами артефакта `10-UAT.md`, который заведён, полон и НИ РАЗУ НЕ ПРОЙДЕН.

**Порядок предлагается такой:** сначала шаги 1.1-1.8 (критерий 2 — вторая половина цели
фазы), затем 1.10-1.15 (переходная ветка и живость клиентского слоя), затем четыре пункта
качества панели. Обход снимается на приёмке фазы; отметка без наблюдённого признака
закрытием не считается — это правило записал сам артефакт.

### Gaps Summary

**Фаза продвинулась существенно, и три из четырёх гэпов прежнего круга закрыты по-настоящему.**
Два из трёх верификатор закрыл собственным воспроизведением, а не принял по отчёту: пять
маршрутов, отвечавших `500`, отвечают `422`; механизм, убивавший скрипт редактора, устранён
и доказан исполнением исходника дважды в одной области имён настоящим интерпретатором с
мутационным контролем. Документная запись возвращена в согласие с деревом, и у правила
впервые появилось машинное принуждение. Корень `CR-01`/`CR-02` оказался ОДИН и снят в
ОДНОМ месте — ровно так, как требовал прежний отчёт.

**Фаза не закрыта по одной причине, и причина лежит не в предмете фазы, а в инструменте,
который третья партия завела ради регламента.** Модуль
`tests/test_planning/test_requirement_completion_follows_verification.py` вшил в два своих
отрицательных контроля переходное состояние живого дерева `.planning/`. Замер в обе
стороны показывает, что предмет ШИРЕ, чем назвала ревизия: правило вшито в литерал
`gaps_found` и краснеет на ЛЮБОМ ином вердикте — `passed` роняет два правила,
`human_needed` роняет одно. То есть `just test` зелен ровно при условии, что эта фаза не
достигла цели, а единственный вердикт, оставляющий суиту зелёной, — тот, который говорит
«не готово». Настоящий отчёт несёт `gaps_found` и потому суиту не роняет; это не
смягчающее обстоятельство, а сам предмет.

Починка дешёвая и не требует переписывания замысла: функция
`premature_completions(requirements_text, planning_root)` уже принимает корень ПАРАМЕТРОМ —
второй вход нужно подменить синтетическим деревом так же, как первый уже подменяется
`_with_status`. Несущие правила модуля при этом не трогаются: они корректны, зелены и
предмет свой стерегут.

**Сверх этого фаза остаётся ЧЕЛОВЕЧЕСКОЙ по второй половине своей цели.** Критерий 2 —
«после N удалений в документе не остаётся N мёртвых диалогов» — по-прежнему доказан
ПРИСУТСТВИЕМ узлов и СТРУКТУРОЙ, а не применением: суита идёт через `ASGITransport` на
in-memory SQLite и не исполняет ни строчки JS, браузерных зависимостей у проекта нет, и
`10-UAT.md` не проходился ни разу. То же касается браузерной половины `CR-03`: машинная
доказана прогоном честно и с названной границей, браузерная ждёт шагов 1.13-1.15. Ни то,
ни другое не является провалом — это две записи о том, чего машина здесь увидеть не может,
и обе адресованы обходу, который заведён и ждёт человека.

**`FORM-06` этим отчётом в завершённое НЕ переводится** — и по вердикту, и по существу:
вторая половина требования (снятие осиротевшей панели) не наблюдалась никем.

---

_Verified: 2026-09-04T18:30:00Z_
_Verifier: Claude (gsd-verifier), четвёртый круг_
