---
phase: 10-rychag-components-modal-html
verified: 2026-09-03T23:05:00Z
status: gaps_found
score: 4/7 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/6
  previous_verified: 2026-09-03T13:05:00Z
  gaps_closed: []
  gaps_remaining:
    - >-
      Прямой POST мимо браузера на маршрут подтверждённого удаления даёт отказ валидации,
      а не 500 (T-02-24, T-02-25). ЗАКРЫТ ЧАСТИЧНО И ПОТОМУ НЕ ЗАКРЫТ: граница поставлена
      на ПОЛЕ ФОРМЫ, а идентификатор ПУТИ, доезжающий до того же сравнения на 26 строк
      раньше, остался неограниченным. Воспроизведено верификатором на HEAD.
  regressions:
    - >-
      `CR-03`: транспорт `HX-Location`, заведённый ЭТОЙ фазой на месте прежнего 302,
      переисполняет инлайн-скрипт редактора объявления и убивает ВЕСЬ его JavaScript
      после штатного удаления последнего расписания. До фазы маршрут отвечал 302
      (`git show 7c1041d:app/pages/schedules.py` — `_editor_redirect`), то есть браузер
      строил документ заново и скрипт исполнялся один раз.
  new_gaps:
    - >-
      `CR-02`: четыре соседних маршрута того же файла несут тот же неограниченный
      идентификатор и отвечают 500 (воспроизведено верификатором).
    - >-
      `FORM-06` помечен `Complete` в `.planning/REQUIREMENTS.md` коммитом `0ea886d`
      (план 10-11) при живом блокере и НИ РАЗУ не пройденном обходе `10-UAT.md`.
  corrections:
    - >-
      УТОЧНЕНИЕ ПРЕЖНЕГО ОТЧЁТА, А НЕ ОТМЕНА ЕГО. Прежний отчёт назвал предметом гэпа
      помощник `_ad_id_from_form` и поле формы. Замер этого круга показывает, что предмет
      ШИРЕ: величина приезжает в то же сравнение ДВУМЯ путями, и закрытие одного из них
      оставило маршрут в том же состоянии, в каком гэп был открыт, — 500 на том же
      запросе. Прежняя формулировка не вычёркивается: в части поля формы она верна и
      правка по ней исполнена корректно.
gaps:
  - truth: >-
      Прямой POST мимо браузера на маршрут подтверждённого удаления даёт отказ валидации,
      а не 500 (T-02-24, T-02-25 — записанный инвариант модуля правил самой фазы)
    status: failed
    reason: >-
      ВОСПРОИЗВЕДЕНО ВЕРИФИКАТОРОМ НЕЗАВИСИМО НА HEAD `a6fc9a5`, а не унаследовано от
      ревизии. План 10-08 завёл `_AD_ID_MAX = 2_147_483_647` (`app/pages/schedules.py:47`)
      и границу `if value < 1 or value > _AD_ID_MAX: return None` (`:305`) — на ПОЛЕ ФОРМЫ.
      Идентификатор ПУТИ (`schedule_id: int`, `:954`) границы не имеет и доезжает до
      `Schedule.id == schedule_id` (`:967`) НЕПОСРЕДСТВЕННО, минуя помощник целиком.
      Замер клиентом с cookie обычного пользователя:
      `POST /schedules/9999999999999999999999999/delete`, `HX-Request: true`,
      тело `return_to=editor&ad_id=1` → **HTTP 500**,
      `OverflowError: Python int too large to convert to SQLite INTEGER`,
      кадр `app/pages/schedules.py:967, in schedules_delete`.
      ⚠️ ПОЧЕМУ ГЕЙТ ЭТОГО НЕ ЛОВИТ, И ЭТО НЕ СЛУЧАЙНОСТЬ: обход построен на
      `MISSING_SCHEDULE_URL = "/schedules/999999/delete"`
      (`tests/test_pages/test_confirm_delete_transport.py:2272`) — величина внутри int32,
      поэтому ВСЯ пятизначная матрица `UNUSABLE_AD_ID_VALUES` прогоняется по заведомо
      годному пути. Гейт стережёт ровно ту половину, которая починена, и слеп ровно к той,
      которая осталась. Зелёная суита (2798 passed) опровержением не является: правило,
      неспособное дойти до дефекта, зеленеет одинаково и на починенном дереве, и на
      сломанном.
    artifacts:
      - path: "app/pages/schedules.py:954,967"
        issue: "`schedule_id: int` из пути не ограничен и уезжает в `Schedule.id == schedule_id` без проверки величины"
      - path: "tests/test_pages/test_confirm_delete_transport.py:2272"
        issue: "`MISSING_SCHEDULE_URL` внутри int32 — обход негодных величин физически не достигает пути запроса"
    missing:
      - "Ограничить величину идентификатора ПУТИ тем же `_AD_ID_MAX` до запроса — там же, где уже ограничено поле формы"
      - "Прогнать матрицу `UNUSABLE_AD_ID_VALUES` ещё и по АДРЕСУ запроса, а не только по телу: добавить параметризацию пути значением `\"9\" * 25`"
      - "Утвердить границу ОДНИМ местом на оба источника, иначе следующий источник того же значения повторит расхождение"
  - truth: >-
      Соседние маршруты того же файла держат тот же инвариант: неограниченный
      идентификатор не роняет обработчик (`CR-02`)
    status: failed
    reason: >-
      ВОСПРОИЗВЕДЕНО ВЕРИФИКАТОРОМ ОДНИМ ПРОГОНОМ, все пять маршрутов — 500,
      `OverflowError` в каждом:
      `POST /schedules/<25 девяток>/delete` → 500 (кадр `:967`);
      `POST /schedules/<25 девяток>/edit` → 500 (кадр `:834`);
      `POST /schedules/<25 девяток>/toggle` → 500 (кадр `:911`);
      `POST /schedules/new` с `ad_id=<25 девяток>` → 500 (кадр `:759` → `_ownership_verdict:469`);
      `POST /schedules/new` с `account_id=<25 девяток>` → 500 (кадр `:759` → `_ownership_verdict:476` → `_owns_ad:447`).
      Два из пяти падают ВНУТРИ `_ownership_verdict`, то есть отказ происходит на пути
      вычисления вердикта о владении.
    artifacts:
      - path: "app/pages/schedules.py:730,822-823,903"
        issue: "`ad_id: int = Form(...)`, `schedule_id: int`, `account_id` — ни один не ограничен по величине"
      - path: "app/pages/schedules.py:447,469,476"
        issue: "`_owns_ad` и `_ownership_verdict` принимают неограниченное значение и роняют драйвер БД"
    missing:
      - "Одна общая граница величины идентификатора, применяемая ДО любого запроса, на всех пяти входах"
      - "Правило, утверждающее «не 500» на всех пяти маршрутах перечнем, а не по одному"
  - truth: >-
      Транспорт, заведённый фазой, не ломает уже работающий JavaScript экрана,
      на который сам же приземляет (`CR-03`)
    status: failed
    reason: >-
      МЕХАНИЗМ УСТАНОВЛЕН ПО КОДУ ПОЛНОСТЬЮ, ЗВЕНО ЗА ЗВЕНОМ, и каждое звено измерено, а
      не предположено. (1) Ветка удаления ПОСЛЕДНЕГО расписания уходит в
      `respond(request, redirect=screen_url)` без фрагмента (`app/pages/schedules.py:1035`),
      то есть в `location_response` → **204 + `HX-Location: /ads/{ad_id}/edit`**
      (`app/pages/htmx.py:147-149`). (2) Вендоренный рантайм на этот заголовок вызывает
      `Nn("get", e, s)` — обычный ajax-переход с целью по умолчанию `body` и подменой
      `innerHTML` (`app/static/js/htmx.min.js`, ветка `HX-Location")`). (3) `allowScriptTags:true`
      в бандле, и мета-блок `includes/htmx_config.html` его НЕ переопределяет; рантайм
      пересоздаёт узел скрипта (`createElement("script")`), то есть **исполняет скрипт
      подменённого тела заново**. (4) `app/templates/ads/form.html:307` — обычный
      `<script>` (не `type="module"`, не обёрнут в IIFE) с **18 объявлениями `const`/`let`
      верхнего уровня** (`IMAGE_BASE_URL`, `THUMB_KEY_PREFIX`, `MAX_IMAGES`, `TEXT_LIMIT`,
      `TEXT_WARN_AT`, `CAPTION_LIMIT`, три `UPLOAD_*_ERROR`, `imagePaths`, `adForm`,
      `mediaStrip`, `imageInputs`, `fileInput`, `errorBox`, `counter`, `textArea`,
      `addTile`). Лексические привязки верхнего уровня переживают снос узла скрипта, и
      повторное объявление — РАННЯЯ ошибка спецификации языка: скрипт падает
      `SyntaxError: Identifier 'IMAGE_BASE_URL' has already been declared` ЦЕЛИКОМ, до
      исполнения первой строки. Умирают загрузка изображений, счётчик символов и снятие
      вложений — молча, без единого признака на сервере.
      ⚠️ ЭТО РЕГРЕССИЯ, ЗАВЕДЁННАЯ ЭТОЙ ФАЗОЙ, А НЕ УНАСЛЕДОВАННОЕ СОСТОЯНИЕ: до фазы
      обработчик заканчивался `_editor_redirect(form_data, ad_id)` — обычным 302
      (`git show 7c1041d:app/pages/schedules.py`), браузер строил документ заново, и
      скрипт исполнялся РОВНО ОДИН РАЗ.
      ⚠️ ГРАНИЦА ДОКАЗАННОГО НАЗЫВАЕТСЯ ПРЯМО: верификатор браузера не поднимал. Доказана
      ЦЕПЬ по коду и версии рантайма; наблюдение глазом принадлежит обходу `10-UAT.md`,
      шаг 1.10 которого идёт РОВНО по этой ветке — и обход не проходился ни разу.
    artifacts:
      - path: "app/templates/ads/form.html:307-339"
        issue: "18 объявлений `const`/`let` верхнего уровня в обычном `<script>` — переисполнение невозможно без SyntaxError"
      - path: "app/pages/schedules.py:1035"
        issue: "Ветка перехода приземляет на экран, чей скрипт переисполнения не переживает"
    missing:
      - "Либо изолировать скрипт редактора (IIFE или `type=\"module\"`), либо не приземлять на него через `HX-Location`"
      - "Правило, утверждающее, что ни один экран, служащий целью `HX-Location`, не несёт объявлений верхнего уровня в инлайн-скрипте"
      - "Пройти шаг 1.10 обхода `10-UAT.md` — он идёт ровно по этой ветке и предъявил бы отказ глазом"
  - truth: >-
      Запись требования соответствует состоянию дерева: `FORM-06` не объявляется
      завершённым, пока цель фазы не достигнута
    status: failed
    reason: >-
      `.planning/REQUIREMENTS.md:31` несёт `- [x] **FORM-06**`, `:136` — `| FORM-06 | Phase 10 | Complete |`.
      Отметка поставлена коммитом `0ea886d` (план 10-11, последний в партии) — то есть
      ПОСЛЕ того, как блокер `CR-01` был открыт прежним отчётом, и ДО того, как он был
      закрыт (он не закрыт и сейчас). Собственный текст `FORM-06` называет ВТОРОЙ
      половиной требования именно снятие осиротевшей панели OOB-узлом — то, что
      `10-UAT.md` прямо объявляет непройденным: `status: testing`, таблицы отметок пусты,
      шапка артефакта говорит дословно «требование `FORM-06` этим артефактом в
      завершённое НЕ переводится». Запись в этом проекте есть несущий контракт, и здесь
      она ШИРЕ поведения — ровно тот класс расхождения, которым фаза была провалена
      первым кругом.
    artifacts:
      - path: ".planning/REQUIREMENTS.md:31,136"
        issue: "`FORM-06` помечен завершённым при живом блокере и непройденном обходе"
    missing:
      - "Вернуть `FORM-06` в незавершённое состояние до закрытия блокеров и прохождения обхода"
      - "Правило или пункт регламента, запрещающий отметку требования раньше `status: passed` отчёта верификации"
behavior_unverified_items:
  - truth: >-
      Критерий 2: ответ снимает осиротевшую панель подтверждения вторым OOB-узлом
      `hx-swap-oob="delete"` по её `id` — после N удалений в документе не остаётся
      N мёртвых диалогов
    test: >-
      В браузере: открыть редактор объявления с ≥3 расписаниями, удалить расписание через
      панель подтверждения; повторить N раз. Затем в консоли:
      `document.querySelectorAll('[role="dialog"]').length` и
      `document.querySelectorAll('[id^="sched-del-"]').length`.
    expected: >-
      Число панелей равно числу оставшихся карточек; `#sched-N` и `#sched-del-N`
      удалённых расписаний отсутствуют ОБА; живых ловушек фокуса в документе не осталось.
    why_human: >-
      Роадмап называет это ЕДИНСТВЕННЫМ выводом фазы, взятым из документации и не
      проверенным ни на чём (ручной UAT, пункт 4). Суита идёт через `ASGITransport` на
      in-memory SQLite и не исполняет ни строчки JS: доказано ПРИСУТСТВИЕ узла в теле
      ответа, а не то, что рантайм его применил. План 10-10 завёл артефакт обхода
      `10-UAT.md` — и это ПРАВИЛЬНЫЙ шаг, но обход `status: testing` и не проходился ни
      разу, таблицы отметок пусты. Состояние по существу то же, что кругом раньше.
human_verification:
  - test: >-
      Шаг 1 обхода `10-UAT.md`: удалить расписание через панель N раз, считать
      `document.querySelectorAll('[role="dialog"]').length` и `[id^="sched-del-"]`.
    expected: "После N удалений осиротевших диалогов с живыми ловушками фокуса в документе нет."
    why_human: "OOB-снятие применяет рантайм htmx; httpx свопов не делает. Критерий 2, ручной UAT пункт 4 роадмапа."
  - test: >-
      Шаг 1.10 обхода `10-UAT.md`: перед удалением выполнить в консоли
      `window.__uat10 = 'жив'`, затем удалить ПОСЛЕДНЕЕ расписание и наблюдать консоль.
    expected: >-
      Экран сменился переходом (204 + `HX-Location`) И консоль чиста. ⚠️ ВЕРИФИКАТОР
      ОЖИДАЕТ ЗДЕСЬ ОТКАЗ: по цепи, установленной в `CR-03`, ожидается
      `SyntaxError: Identifier 'IMAGE_BASE_URL' has already been declared` и мёртвые
      загрузка изображений, счётчик символов и снятие вложений.
    why_human: "Переисполнение скрипта наблюдается только рантаймом браузера. Это ПОДТВЕРЖДЕНИЕ гэпа `CR-03`, а не его проверка на существование."
  - test: >-
      Замедлить сеть (Slow 3G), подтвердить удаление на месте, отвечающем 204 +
      `HX-Location`, и оборвать follow-up GET (Offline сразу после нажатия).
    expected: >-
      Панель уходит ПО СОБЫТИЮ ЗАВЕРШЕНИЯ ЗАПРОСА, а не по свопу тела: не остаётся
      висеть поверх устаревшего экрана с `is-modal-open` на `<html>`.
    why_human: "Правкой плана 10-05 пункт АДРЕСОВАН, но машинно не закрыт — окно 28 `.planning/WINDOWS.md`. Шаг 2 обхода `10-UAT.md`."
  - test: >-
      Подтвердить удаление на медленной сети и, не дожидаясь ответа, нажать Esc,
      щёлкнуть по оверлею и нажать «Отмена».
    expected: >-
      Панель НЕ уходит ни одним из трёх путей, пока запрос летит; виден узел индикатора
      (`.form-busy`); «Отмена» остаётся доступной и в обходе по Tab. После ответа все три
      пути снова работают.
    why_human: "Гейт `DEF-09-02` доказан поведенчески в интерпретаторе и мутацией, но что человек видит панель ЗАНЯТОЙ, а не сломанной, снимается только глазом — окно 35."
  - test: "Вызвать 500 на маршруте за панелью подтверждения и наблюдать панель."
    expected: "Панель ОСТАЁТСЯ открытой, поверх встаёт плашка аварии, повтор возможен без переоткрытия."
    why_human: "Половина D-12: в суите доказана на реальной форме события отказа, на живом стенде — глазом. Окно 22."
  - test: "Подтвердить удаление и проверить `document.activeElement` после ухода панели на обоих путях."
    expected: "На фрагментном пути фокус приземлился в `#notice`, а не на `<body>`."
    why_human: >-
      Приземление фокуса поднимает Alpine и настоящий своп. ⚠️ Читать вместе с WR-03
      ревизии: на фрагментном пути `#notice` ПУСТА всегда (плашки на успех нет по D-03).
  - test: >-
      Прочитать глазами запись закрытия `DEF-09-02` в `.planning/REQUIREMENTS.md:429,436`,
      обе половины окна 15 и текст окна 34 `.planning/WINDOWS.md`.
    expected: "Все три говорят об ОДНОМ предмете теми же словами; окно 34 называет кандидатов, не назначая фазу."
    why_human: "Документная половина закрытия `DEF-09-02` машинно не закрывается — окно 36."
---

# Phase 10: Рычаг `components/modal.html` — Verification Report (третий круг)

**Phase Goal:** подтверждение удаления идёт через htmx во всех 18 местах одной правкой одного файла, и после N удалений в документе не остаётся N мёртвых диалогов
**Verified:** 2026-09-03T23:05:00Z
**Status:** gaps_found
**Re-verification:** Да — после ВТОРОЙ партии закрытия гейпов (планы 10-08, 10-09, 10-10, 10-11). Заменяет отчёт от 2026-09-03T13:05:00Z (`gaps_found`, 4/6). HEAD `a6fc9a5`.

## Что этот круг измерил своими руками

Верификатор поднял приложение на фикстурах проекта и послал пять настоящих запросов
собственным зондом (заведён, прогнан и снят; в дереве не оставлен). Результат прогона
дословно:

```
delete: 500        edit: 500        toggle: 500
new_ad_id: 500     new_account_id: 500
```

Все пять — `OverflowError: Python int too large to convert to SQLite INTEGER`.

⚠️ **ЗЕЛЁНАЯ СУИТА ЭТОМУ НЕ ПРОТИВОРЕЧИТ, И ЭТО ГЛАВНЫЙ ВЫВОД КРУГА.** `2798 passed`
верно; правило, стерегущее этот инвариант, построено на адресе
`/schedules/999999/delete` — величине внутри int32. Вся пятизначная матрица негодных
величин прогоняется по заведомо годному пути запроса. Гейт слеп к дефекту не потому, что
дефект хитрый, а потому, что его константа выбрана внутри диапазона, за пределами
которого дефект и живёт.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | **Критерий 1.** Все 18 мест подтверждения работают через htmx одной правкой `modal.html`; инвентарь утверждён ЧИСЛОМ | ✓ VERIFIED | Регрессия держится. Макрос печатает БЕЗУСЛОВНО `hx-post="{{ action }}"`, `hx-swap="none"`, `hx-disabled-elt`, `hx-indicator`, `x-on:htmx:after-request` и узел `.form-busy` (`modal.html:677-679`), конструкций шаблонизатора в открывающем теге формы нет. Инвентарь: `MODAL_IMPORTERS=11`, `MODAL_EVENT_NAMES=9`, `MODAL_PLACES=18` (`test_components.py:984-986`), `MODAL_CONSUMERS` — 10 имён со сличением обоих счётов (`:1460-1473`). Прогон верификатора по трём файлам гейтов: **214 passed**. |
| 2 | **Критерий 2.** Ответ снимает осиротевшую панель вторым OOB-узлом `hx-swap-oob="delete"` по её `id` | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Разметка на месте и адресована верно (`ads/partials/sched_delete_response.html:71-72`); структурная предпосылка держится — `{% call modal(id='sched-del-' ~ s.id …) %}` стои́т ПОСЛЕ `</article>`, панель СНАРУЖИ удаляемой строки. План 10-10 завёл артефакт обхода `10-UAT.md` и сделал это ЧЕСТНО: `status: testing`, шапка прямо говорит «обход не проходился ни разу» и «требование `FORM-06` этим артефактом в завершённое НЕ переводится», машинной подмены рантайма не заведено. Но артефакт переводит пункт из «не проверено ничем» в «проверяемо по шагам» — и это ВСЁ. Фактическое снятие рантаймом по-прежнему не наблюдал никто. → человеку. |
| 3 | **Критерий 3.** Панель закрывается существующим Alpine (`x-on:htmx:after-request`) — новых строк JS фаза не добавляет | ✓ VERIFIED | `git diff 7c1041d..HEAD -- app/static/js/` **ПУСТ** (проверено на этом круге). Выражение живо и читает величину, выставляемую рантаймом на обоих транспортах: `sending = false; if ($event.detail.successful \|\| ($event.detail.xhr && …getResponseHeader('HX-Location'))) hide()` (`modal.html:679`). Гейт четырёх чисел зелен. ⚠️ Критерий сформулирован узко — «фаза не ДОБАВЛЯЕТ строк JS», — и в этой формулировке верен дословно. Что фаза при этом СЛОМАЛА уже существующий JS, критерием 3 не измеряется и вынесено отдельной истиной 7. |
| 4 | **Критерий 4.** Все 10 потребителей продолжают удалять без JS: панель остаётся настоящей формой POST с непустым `action` | ✓ VERIFIED | `method="post"` теперь ЛИТЕРАЛ, а не параметр (правка плана 10-09, WR-03), `action="{{ action }}"` и `hx-post="{{ action }}"` выписаны ТЕМ ЖЕ выражением (`modal.html:677-679`). Гейты деградации Фазы 8 держат наследование: `test_every_such_form_keeps_its_method_and_action`, `test_the_post_attribute_matches_the_action_character_for_character`, `test_the_panel_verb_is_a_literal_and_not_a_parameter` — все зелены в прогоне 214. |
| 5 | `DEF-09-02` — «Отмена» панели во время летящего необратимого запроса | ✓ VERIFIED | Регрессия держится. Гейт `if (sending) return; hide()` стои́т на всех трёх путях ухода по воле человека: Esc (`modal.html:670`), оверлей (`:674`), кнопка отказа (`:684`) — одной формой условия. `DISMISSAL_PATHS_DECLARED = 3` (`test_components.py:2556`) краснит четвёртый негейтированный путь. Положительный контроль после 500 на месте. |
| 6 | Прямой POST мимо браузера на маршрут подтверждённого удаления даёт отказ валидации, а не 500 (T-02-24, T-02-25) | ✗ FAILED | **ГЭП ПРЕЖНЕГО КРУГА НЕ ЗАКРЫТ.** Граница поставлена на ПОЛЕ формы (`_AD_ID_MAX`, `:47`, `:305`) — правка сама по себе корректная. Но в то же сравнение величина приезжает ВТОРЫМ путём: `schedule_id: int` из пути (`:954`) → `Schedule.id == schedule_id` (`:967`), минуя помощник целиком. Замер верификатора: `POST /schedules/<25 девяток>/delete` → **500**, кадр `:967`. Гейт этого не достигает: `MISSING_SCHEDULE_URL = "/schedules/999999/delete"` — внутри int32. Разбор — `gaps[0]`. |
| 7 | Транспорт, заведённый фазой, не ломает уже работающий JavaScript экрана, на который сам же приземляет (`CR-03`) | ✗ FAILED | **РЕГРЕССИЯ, ЗАВЕДЁННАЯ ЭТОЙ ФАЗОЙ.** Цепь установлена по коду звено за звеном: 204 + `HX-Location: /ads/{id}/edit` (`htmx.py:147-149`) → рантайм зовёт `Nn("get", …)` с целью `body` и подменой `innerHTML` → `allowScriptTags:true` и `createElement("script")` → скрипт `ads/form.html:307` с **18 объявлениями `const`/`let` верхнего уровня** исполняется ВТОРОЙ раз → ранняя ошибка языка, скрипт падает целиком. До фазы маршрут отвечал 302 (`git show 7c1041d:app/pages/schedules.py`), и переисполнения не было. Разбор — `gaps[2]`. |

**Score:** 4/7 truths verified (1 present, behavior-unverified; 3 failed — из них 1 унаследован незакрытым, 1 новый того же корня, 1 регрессия)

### Deferred Items

Проверено по всем последующим фазам вехи (12, 13, 14, 15). **Ни один гэп не отложен.**

- Фаза 12 снимает из `ads/form.html` `fetch()` и массив `imagePaths` («~70 строк JS минус»), но НЕ снимает блок скрипта целиком — счётчик символов и часть объявлений остаются, а сама Фаза 12 идёт ПОСЛЕ Фазы 11. Совпадение частичное и тангенциальное → по правилу консервативности `CR-03` остаётся действующим гэпом.
- Фаза 15 («Упрочнение и сводный обход 47 форм») ни одним из пяти критериев не называет ни границ величины идентификаторов, ни пятисоток на маршрутах. Совпадения нет.

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `app/templates/components/modal.html` | Рычаг: безусловные htmx-атрибуты на все 18 мест | ✓ VERIFIED | 677-679; глагол — литерал; наследуется 10 потребителями; 214 гейтов зелены |
| `app/templates/ads/partials/sched_delete_response.html` | Три внеполосных узла, два с `hx-swap-oob="delete"` | ⚠️ HOLLOW (доставка доказана, применение — нет) | Узлы в теле ответа есть и адресованы верно; применение рантаймом не наблюдал никто (истина 2) |
| `app/pages/schedules.py` | Маршрут подтверждённого удаления на слое ответа | ✗ ДЕФЕКТ | Граница величины стоит на одном из двух источников; пять маршрутов файла отвечают 500 |
| `app/pages/htmx.py` | Один выход обработчика на оба транспорта | ✓ VERIFIED | `respond` / `location_response` (147-149); `_local_path` отвергает внешний адрес |
| `.planning/phases/10-.../10-UAT.md` | Артефакт ручного обхода: стенд, шаги, гейт полноты | ✓ VERIFIED (как АРТЕФАКТ) | `checks_declared: 6`, шесть шагов, стенд `UAT10-`; ⚠️ `status: testing` — обход НЕ ПРОЙДЕН, требование им не закрывается (сам артефакт это и говорит) |
| `app/static/js/` | Не тронут | ✓ VERIFIED | `git diff 7c1041d..HEAD` пуст |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `components/modal.html` | маршрут удаления | `hx-post="{{ action }}"` | ✓ WIRED | Тот же символ, что и `action=`; сличение по сырой строке шаблона |
| `sched_card.html` | `modal.html` | `{% call modal(id='sched-del-' ~ s.id …) %}` ПОСЛЕ `</article>` | ✓ WIRED | Панель СНАРУЖИ удаляемой строки — предпосылка критерия 2 держится |
| `schedules_delete` | `sched_delete_response.html` | `_fragment()` → `HTMLResponse` | ✓ WIRED | Счёт берётся `_ad_schedule_count`, реальный запрос |
| `schedules_delete` | `/ads/{id}/edit` | 204 + `HX-Location` | ⚠️ WIRED, НО ВРЕДОНОСНО | Связь работает и именно поэтому убивает скрипт цели (истина 7) |
| ответ 204 | панель | `x-on:htmx:after-request` читает `HX-Location` | ✓ WIRED | Ветвь оживлена планом 10-05, подтверждена мутациями прежнего круга |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `sched_delete_response.html` | `schedules_count` | `_ad_schedule_count(db, user.id, ad_id)` — `select(func.count())` | ✓ | ✓ FLOWING |
| `sched_delete_response.html` | `schedule_id` | Идентификатор ПУТИ запроса | ✓ | ✓ FLOWING (⚠️ неограничен — истина 6) |
| `modal.html` | `action` | Параметр вызова макроса у 10 потребителей | ✓ | ✓ FLOWING |
| `10-UAT.md` | таблицы отметок | ЧЕЛОВЕК | ✗ | ✗ DISCONNECTED (по замыслу — обход не пройден) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Гейты рычага, разметки и транспорта | `pytest tests/test_templates/test_components.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_confirm_delete_transport.py -q` | `214 passed` | ✓ PASS |
| Фаза не трогала JS | `git diff 7c1041d..HEAD -- app/static/js/` | пусто | ✓ PASS |
| Долги в изменённых файлах | `grep -nE "TBD\|FIXME\|XXX"` по 26 файлам диффа | ни одного | ✓ PASS |
| `POST /schedules/<25 девяток>/delete` | зонд верификатора на фикстурах проекта | **500**, `OverflowError`, кадр `:967` | ✗ FAIL |
| Четыре соседних маршрута с той же величиной | тот же зонд | **500 × 4** | ✗ FAIL |
| Снятие осиротевшей панели рантаймом | — | нечем: суита не поднимает рантайм разметки | ? SKIP → человеку |
| Переисполнение скрипта редактора | — | нечем: требуется браузер | ? SKIP → человеку (подтверждение `CR-03`) |

### Probe Execution

Пробников конвенции `scripts/*/tests/probe-*.sh` в дереве нет; ни один план фазы их не объявляет. **SKIPPED** — предмета нет. Роль пробника этого круга исполнил зонд на фикстурах проекта, прогнанный и снятый (см. «Что этот круг измерил своими руками»).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| FORM-06 | 10-01…10-11 (все объявляют `requirements: [FORM-06]`) | Подтверждение удаления через htmx во всех 18 местах одной правкой `components/modal.html`; ответ снимает осиротевшую панель вторым OOB-узлом `hx-swap-oob="delete"` | ✗ BLOCKED | **ПЕРВАЯ половина** (18 мест одной правкой) — выполнена и стережётся числом. **ВТОРАЯ половина** (снятие осиротевшей панели) — доказана ПРИСУТСТВИЕМ узла, не применением; обход `10-UAT.md` не пройден. Плюс живой блокер на самом маршруте. ⚠️ При этом `.planning/REQUIREMENTS.md:31,136` помечает требование `Complete` — расхождение записи с деревом, вынесено отдельным гэпом |

Осиротевших требований, назначенных Фазе 10 в `REQUIREMENTS.md` и не заявленных ни одним планом, **не найдено**: `REQUIREMENTS.md:491` называет Фазе 10 ровно один пункт — `FORM-06`, и его заявляют все 11 планов.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `app/pages/schedules.py` | 954, 967 | Неограниченный идентификатор пути уезжает в сравнение SQL | 🛑 Блокер | 500 любому аутентифицированному пользователю на маршруте, который фаза сделала нормальным |
| `app/pages/schedules.py` | 730, 759, 822-823, 834, 903, 911, 447, 469, 476 | Тот же дефект на четырёх соседних маршрутах | 🛑 Блокер | 500 × 4, два — внутри вычисления вердикта о владении |
| `app/templates/ads/form.html` | 307-339 | 18 объявлений верхнего уровня в переисполняемом инлайн-скрипте | 🛑 Блокер | Весь JS редактора умирает после штатного удаления последнего расписания |
| `.planning/REQUIREMENTS.md` | 31, 136 | `FORM-06` помечен `Complete` при живом блокере и непройденном обходе | 🛑 Блокер | Запись шире поведения — тот класс расхождения, которым фаза провалена первым кругом |
| `tests/test_pages/test_confirm_delete_transport.py` | 2272 | Константа гейта выбрана внутри диапазона, за которым живёт дефект | ⚠️ Warning | Правило зеленеет одинаково на починенном и на сломанном дереве в половине своего предмета |
| `app/pages/schedules.py` | 1067 | Докстринг адресует остаток перечню `OOB_TARGET_EXCEPTIONS` | ⚠️ Warning | ПРОВЕРЕНО: перечень несёт ровно две записи экрана групп (`group-row-*`, `group-del-*`), закреплён `OOB_TARGET_EXCEPTIONS_DECLARED = 2`, и НИ ОДНОГО из трёх узлов расписаний в нём нет. Указатель ведёт не туда |
| `app/templates/components/modal.html` | 679 | `x-on:htmx:after-request` на форме, слот `{{ caller() }}` внутри неё | ⚠️ Warning | Обработчик не сужен — план 10-09 говорит об этом ПРЯМО («сужение здесь НЕ делается намеренно»). Взамен заведён инвентарь с зубами (`MODAL_BLOCK_CALLS_DECLARED = 3`, положительный + отрицательный контроль): конкретная опасность машинно заперта, сужение отложено окном. Компенсирующий контроль, а не починка |
| `app/pages/schedules.py` | 202-226 | `_expanded_from_form` остаётся неограниченным | ⚠️ Warning | До БД не доезжает (уезжает только в строку адреса), поэтому пятисотки не даёт; но именно на аналогии с ним построен довод соседнего помощника, и аналогия уже один раз сломалась |
| `app/pages/schedules.py` | — | Ноль `is_same_origin` на разрушающих маршрутах при шести в `admin.py` | ⚠️ Warning | ПРОВЕРЕНО: состояние ПРЕДСУЩЕСТВУЮЩЕЕ (0 до фазы, 0 после) — фазой не заведено, но фазой и не закрыто, а доктрину фаза утверждает |

Долговых меток (`TBD`, `FIXME`, `XXX`) в 26 изменённых файлах — **ни одной**.

### Human Verification Required

Семь пунктов, все в `human_verification` шапки. Шесть из них — перечень обхода `10-UAT.md`,
не пройденного ни разу. Седьмой заведён ЭТИМ кругом и стои́т особняком:

**Шаг 1.10 обхода — ожидается ОТКАЗ, а не подтверждение.** Верификатор ожидает на этом шаге
`SyntaxError` в консоли и мёртвый JS редактора. Шаг ценен именно этим: он предъявит `CR-03`
глазом. То, что обход был написан планом 10-10 и содержит шаг, идущий ровно по сломанной
ветке, но не был пройден, — и есть причина, по которой регрессия дожила до верификации.

### Gaps Summary

**Партия 10-08…10-11 сделала четыре настоящие вещи и не достигла цели.**

Сделано по существу: граница величины на поле формы (правка корректная, докстринг сужен до
правдивого рядом с помеченной опровергнутой прежней формулировкой — идиома проекта соблюдена);
глагол формы панели переведён в литерал; инвентарь блочных слотов с положительным и
отрицательным контролем; честный артефакт ручного обхода, который НЕ пытается подменить
рантайм имитацией и прямо отказывается самозаверяться. Последнее стои́т отметить отдельно:
план 10-10 имел все возможности объявить критерий 2 закрытым и не сделал этого.

Не достигнуто — тремя разными способами, и все три сводятся к ОДНОЙ форме отказа: **правка
закрыла предмет там, где на него смотрели, и не закрыла там, куда не смотрели.**

1. **Блокер `CR-01` закрыт наполовину и потому не закрыт.** Величина приезжает в сравнение
   двумя путями; ограничен один. Маршрут отвечает 500 на тот же запрос, которым гэп был
   открыт. Стерегущее правило слепо к этому по конструкции — его константа лежит внутри
   диапазона, за которым дефект и начинается.
2. **Тот же корень даёт ещё четыре пятисотки** на соседних маршрутах того же файла, две — на
   пути вычисления вердикта о владении.
3. **Фаза завела регрессию, которой до неё не было.** Переведя маршрут с 302 на
   204 + `HX-Location`, она сделала переисполнение инлайн-скрипта редактора неизбежным, а
   скрипт к переисполнению не готов ни на строчку. Штатное удаление последнего расписания
   убивает загрузку изображений, счётчик символов и снятие вложений — молча.
4. **Вторая половина цели фазы по-прежнему не наблюдалась никем**, а требование `FORM-06`
   при этом помечено завершённым. Артефакт обхода это отрицает собственной шапкой.

⚠️ **О ЗЕЛЁНОЙ СУИТЕ.** `2798 passed` на этом дереве — правда, и она ничего из
перечисленного не опровергает. Три из четырёх гэпов лежат ровно там, куда ни одно правило
не дотягивается: за границей int32, за пределами файла `schedules.py` и за пределами
процесса без браузера. Это не упрёк суите — это измеренная граница того, что она
утверждает, и записывать её надо здесь, а не обнаруживать следующим кругом.

---

_Verified: 2026-09-03T23:05:00Z_
_Verifier: Claude (gsd-verifier)_
