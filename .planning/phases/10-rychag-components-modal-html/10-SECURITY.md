---
phase: 10
slug: rychag-components-modal-html
status: audited
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: 2026-09-10
# ⚠️ ПОЛЯ НИЖЕ ИДУТ СВЕРХ ШЕСТИ КАНОНИЧЕСКИХ И ПОСЛЕ НИХ. Шесть полей выше — те же и в том же
# порядке, что у артефакта Фазы 09; поля ниже заводит ВЕТВЬ ИЗЪЯТИЯ, которой у соседних фаз не
# было, и без них охват изъятия остался бы читаемым только прозой. Владелец потребовал записать
# охват ЯВНО, чтобы следующий круг не прочёл изъятие шире принятого.
waiver_scope: phase-10-only
waiver_applies_to_milestone: false
waived_by: chubav
waived_on: 2026-09-10
waiver_basis: 10-SECURITY-SUBJECT.md
secure_phase_command_run: true
secure_phase_command_run_on: 2026-09-14
# ⚠️ ШАПКА ИЗМЕНЕНА 2026-09-14 АУДИТОМ, И ТРИ ПОЛЯ СМЕНИЛИСЬ. Прежние значения НЕ
# ВЫЧЁРКИВАЮТСЯ и называются здесь (идиома D-30/D-32): `status: waived` → `audited`,
# `threats_open: 12` → `0`, `secure_phase_command_run: false` → `true`. ВСЕ ТРИ БЫЛИ ВЕРНЫ ДЛЯ
# СВОЕГО ДНЯ: 2026-09-10 владелец решил аудит НЕ ПРОИЗВОДИТЬ, и запись изъятия ниже
# ОСТАЁТСЯ ЦЕЛИКОМ — она есть запись того решения, а не утверждение о сегодняшнем дереве.
# 2026-09-14 владелец решил иначе — «Прогнать /gsd-secure-phase 10», и аудит был произведён.
---

# Phase 10 — Security (ИЗЪЯТИЕ ИЗ ПРИНУЖДЕНИЯ, записанное с основанием)

> Per-phase security contract. У Фазы 10 он есть ЗАПИСЬ ИЗЪЯТИЯ, а не сведённый аудит: владелец
> решил регламент безопасности для этой фазы НЕ ПРИНУЖДАТЬ, и настоящий файл записывает это
> решение с основанием, именем, датой и охватом.

## ⚠️ Два поля шапки, которые читаются НЕ как у девяти соседних фаз

Названо первым разделом, потому что машинный потребитель прочтёт шапку раньше любой прозы.

**`status: waived` — ЗНАЧЕНИЕ, КОТОРОГО В СЛОВАРЕ ПРОЕКТА ДО СЕГОДНЯ НЕ БЫЛО.** Замер по всем
девяти существующим артефактам даёт два значения и только два: `verified` (восемь фаз) и
`pending-acceptance` (Фаза 04). Фаза 10 заводит третье. Оно НЕ означает ни «проверено», ни «ждёт
приёмки»: оно означает, что аудита не было и не будет, и это РЕШЕНО, а не забыто.

**`threats_open: 12` — ПЕРВОЕ НЕНУЛЕВОЕ ЗНАЧЕНИЕ ЭТОГО ПОЛЯ В ПРОЕКТЕ.** У всех девяти соседних
артефактов оно равно нулю. Двенадцать — ЗАМЕРЕННЫЙ размер блокирующего множества, а не оценка и не
округление.

🚫 **НОЛЬ ЗДЕСЬ БЫЛ БЫ ЛОЖЬЮ, И ЭТО НАЗВАНО ПРЯМО.** Двенадцать строк тяжести `high` с
распоряжением `accept` СУЩЕСТВУЮТ в реестрах исполненных планов; изъятие есть решение НЕ СВОДИТЬ
их, а не утверждение, что их нет. Поле читает команда отгрузки, и ноль в нём сказал бы ей, что
блокирующего множества нет вовсе. Значение `12` подтверждено владельцем ПРЯМО.

## Запись изъятия

| Поле | Значение |
|------|----------|
| Идентификатор ветви решения | **`waive-with-record`** |
| Принявший | **`chubav`** |
| Дата | **`2026-09-10`** |
| Охват | **РАЗОВЫЙ — ТОЛЬКО ФАЗА 10** (дословно ниже) |
| Основание | замер `10-SECURITY-SUBJECT.md`, снятый 2026-09-10 разбором сорока реестров |
| Отвергнутая ветвь | `run-secure-phase` — `/gsd-secure-phase 10` НЕ ЗАПУСКАЛСЯ и запускаться не будет |
| Где решение принято | блокирующий чекпойнт задачи 2 плана `10-44` (`gate="blocking-human"`, `one-way`) |

### Охват изъятия — ДОСЛОВНО

> **Охват изъятия: РАЗОВЫЙ — только Фаза 10.** Изъятие НЕ объявляется действующим на веху v2.1.
> Следующая фаза вехи решает свой случай заново; прецедент не объявляется действующим по
> умолчанию.

⚠️ **ПРОЧТЕНИЕ ШИРЕ ПРИНЯТОГО ЗАПРЕЩЕНО ЯВНО.** Цена ветви `waive-with-record`, названная на
чекпойнте, состояла именно в прецеденте «реестры на планировании плюс изъятие вместо сведения»,
наследуемом последующими фазами вехи. **Владелец наследование ОТКЛОНИЛ.** Для Фазы 11 и далее
`security_enforcement: true` остаётся действующим в полную силу; настоящая запись их случая не
решает и основанием для их изъятия не служит. Фаза, сославшаяся на этот файл как на прецедент,
сошлётся на него ПРОТИВ его собственного текста.

### Основание изъятия — ДОСЛОВНО, подтверждено владельцем

> Сорок реестров угроз авторованы на планировании, и ни один план не пропущен: 40 планов
> разобрано, 0 без разбираемого блока `<threat_model>`. Всё блокирующее множество при пороге
> `high` есть ОДНА И ТА ЖЕ строка цепи поставок — двенадцать строк `T-10-12-SC`…`T-10-23-SC`, все
> `high`/`accept`, — у которой ВНУТРИ ТОЙ ЖЕ ФАЗЫ разошлась тяжесть: планы 10-01…10-11 и
> 10-24…10-34 оценили её `low`, планы 10-35…10-40 — `n/a`. Предмета у неё нет ни в одном из
> двенадцати: ни одна задача этих планов не устанавливает пакетов ни одним пакетным менеджером и
> не правит `pyproject.toml`, а поверочный замер дерева даёт НОЛЬ коммитов ветви фазы (из 287),
> тронувших `pyproject.toml`, `uv.lock`, `wa_worker/package.json` или `wa_bridge/package.json`.
> Двенадцать строк суть РАСХОЖДЕНИЕ ТЯЖЕСТИ одной строки с идиомой той же фазы, а не двенадцать
> принятых высоких рисков.

Каждое число основания сличено с замером построчно; таблица сличения — `10-SECURITY-SUBJECT.md`,
раздел ответа владельца. ⚠️ Единственная поправка, названная там же: переснятый на записи решения
счёт даёт **288** коммитов ветви против `master`, а не 287, — разница ровно в коммит, которым
создан сам файл замера. Число коммитов, тронувших четыре манифеста пакетов, равно **НУЛЮ** при
обоих прочтениях, и доказывает отсутствие предмета именно оно, а не общий счёт.

## Register origin

`register_authored_at_plan_time: true` — блок `<threat_model>` несут **ВСЕ СОРОК** исполненных
планов `10-01`…`10-40`, ни один не пропущен; раздел границ доверия — тоже все сорок. Замер снят
РАЗБОРОМ блоков 2026-09-10.

**Итоги замера:** 250 строк реестра, 239 уникальных имён угроз. По тяжести: 84 `high` · 104
`medium` · 56 `low` · 6 `n/a` (`critical` — 0). По распоряжению: 195 `mitigate` · 55 `accept`.
Размер блокирующего множества при пороге `high` — **12**.

⚠️ **ЭТОТ ФАЙЛ НЕ ЕСТЬ АУДИТ И НЕ ПОДМЕНЯЕТ ЕГО.** У соседних фаз артефакт ПРОВЕРЯЛ объявленные
меры; здесь не проверено ничего. Материал аудита собран целиком и авторован на планировании —
отсутствует именно СВЕДЕНИЕ, и решение изъятия есть решение его не производить.

## Trust Boundaries

Сорок планов называют **106** формулировок границ (**98** различных дословно), сводимых к **13**
строкам. **Сведённая таблица живёт в `10-SECURITY-SUBJECT.md`, раздел `## Trust Boundaries`, и
сюда НЕ КОПИРУЕТСЯ.** Причина названа, а не подразумевается: копия завела бы второе место одного
утверждения, расходящееся с первым молча при любой правке, — тот самый класс, за который фаза
получила отдельные круги верификации. Ссылка на один источник честнее двух согласованных сегодня
копий.

## Блокирующее множество — ПОИМЁННО, все 12 строк, ни одна не растворена

Определение: тяжесть `high` либо `critical` при распоряжении, отличном от `mitigate`, при пороге
блокировки `security_block_on: high`. Строк ровно **12** — ровно то число, что стоит в поле
`threats_open` шапки.

| Имя угрозы | План | Тяжесть | Распоряжение | Существо (из реестра, сокращённо) |
|------------|------|---------|--------------|-----------------------------------|
| `T-10-12-SC` | 10-12 | high | accept | ПРЕДМЕТА НЕТ: ни одна задача плана не устанавливает пакетов ни одним пакетным менеджером; `pyproject.toml` не правится |
| `T-10-13-SC` | 10-13 | high | accept | ПРЕДМЕТА НЕТ: пакетов не ставится; интерпретатор JS в проекте уже есть (на нём собран `wa_worker/`) и ввозится действующим общим запуском |
| `T-10-14-SC` | 10-14 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и не правит `pyproject.toml` |
| `T-10-15-SC` | 10-15 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и не правит `pyproject.toml` |
| `T-10-16-SC` | 10-16 | high | accept | ПРЕДМЕТА НЕТ, И ЭТО РЕШЕНИЕ: браузерная зависимость НЕ заводится партией с записанным основанием, `pyproject.toml` не правится |
| `T-10-17-SC` | 10-17 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-18-SC` | 10-18 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-19-SC` | 10-19 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-20-SC` | 10-20 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-21-SC` | 10-21 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-22-SC` | 10-22 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
| `T-10-23-SC` | 10-23 | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |

**Ни одной строки блокирующего множества ВНЕ цепи поставок нет.** Все 84 строки тяжести `high`,
кроме этих двенадцати, несут распоряжение `mitigate`. Поверочный замер дерева: **НОЛЬ** коммитов
ветви фазы тронули `pyproject.toml`, `uv.lock`, `wa_worker/package.json` либо
`wa_bridge/package.json`. «Предмета нет» — факт дерева, а не мнение планов.

### Расхождение тяжести — почему двенадцать строк не есть двенадцать принятых высоких рисков

Одна и та же по существу строка несёт в фазе три разные тяжести, по диапазонам планов:

| Диапазон планов | Идентификатор | Тяжесть | Распоряжение | Число планов |
|-----------------|---------------|---------|--------------|--------------|
| `10-01`…`10-11` | `T-10-SC` (общий) | `low` | accept | 11 |
| `10-12`…`10-23` | `T-10-NN-SC` | **`high`** | accept | **12** |
| `10-24`…`10-34` | `T-10-NN-SC` | `low` | accept | 11 |
| `10-35`…`10-40` | `T-10-NN-SC` | `n/a` | accept | 6 |

Двадцать восемь планов ТОЙ ЖЕ фазы, с тем же существом и при тех же нулевых установках, оценили
эту строку `low` либо `n/a`. Двенадцать — расхождение тяжести с идиомой собственной фазы.

## Два расхождения замера, которые ПЕРЕЖИВАЮТ решение

Названы здесь, а не только в предмете решения: запись, оставившая их позади, дала бы следующему
кругу изъятие более гладким, чем оно есть.

**1. Формулировка `<objective>` плана 10-44 опровергнута замером.** `<objective>` утверждает, что
в планах 10-24…10-40 строка цепи поставок несёт `n/a`. Замер даёт `low` у одиннадцати планов
10-24…10-34 и `n/a` только у шести 10-35…10-40. **В запись идут СНЯТЫЕ числа.** На размер
блокирующего множества это не влияет: 12 при любом прочтении, потому что `low` и `n/a` одинаково
ниже порога `high`.

**2. Столкновение имён `T-10-22-04` в исполненном плане 10-22.** Один идентификатор носят ДВЕ
РАЗНЫЕ строки реестра. Оно **ЗАПИСАНО, А НЕ ПОЧИНЕНО**.

🚫 **ПРАВКА ИСПОЛНЕННЫХ ПЛАНОВ `10-01`…`10-40` ЗАПРЕЩЕНА** — ни на символ, включая двенадцать
строк блокирующего множества и включая это столкновение имён. Планы исполнены, сводки написаны,
коммиты стоя́т; правка задним числом переписала бы историю фазы и сделала бы сегодняшний замер
неповторимым.

## Чего этот файл НЕ делает

Прямо и без оговорок — потому что изъятие, прочитанное как вердикт, есть ровно та подмена, которой
девять кругов верификации и не заметили:

- Он **не выносит вердикта фазе**: `10-VERIFICATION.md` держал `Gaps Found`, и настоящий файл
  ⚠️ **ПОПРАВКА 2026-09-14, НАЙДЕННАЯ АУДИТОРОМ: НА HEAD ЭТО УЖЕ НЕВЕРНО.** `10-VERIFICATION.md:4`
  несёт `status: passed` — шестнадцатый круг, вердикт переведён по подписи `chubav` от 2026-09-14.
  Прежняя фраза НЕ вычёркивается: она была верна для 2026-09-10, когда писалось изъятие. На
  блокирующее множество поправка не влияет.
  этого не меняет, не оспаривает и не отменяет. Артефакт безопасности не есть верификация.
- Он **не объявляет ни одну угрозу закрытой**: ни одна строка реестра не переводится из `accept` в
  `mitigate` и не помечается закрытой. Двенадцать строк остаются открытыми, и поле `threats_open`
  говорит это числом.
- Он **не двигает отметку требования**: `FORM-06` в `.planning/REQUIREMENTS.md` остаётся
  неотмеченным, и правило `test_requirement_completion_follows_verification` требует прежде
  вердикта `passed` у верификации фазы.
- Он **не является аудитом и не заменяет его результата**: `/gsd-secure-phase 10` не запускался.
  Если веха или отгрузка потребуют сведённого аудита Фазы 10, его придётся произвести — настоящая
  запись объявляет лишь, что фаза закрывается БЕЗ него, и называет цену этого.
- Он **не распространяется на веху**: охват разовый, и Фаза 11 решает свой случай заново.
- Он **не правит ни продукта, ни суиты, ни исполненных планов** — ни на строку.

---

## Security Audit 2026-09-14

| Metric | Count |
|--------|-------|
| Threats found | 250 (диапазон 10-01…10-40, заказанный объём) |
| Closed | 118 |
| Open | 132 — **ни одной блокирующей** |
| **threats_open** | **0** |

Прогон: `/gsd-secure-phase 10`, `gsd-security-auditor`, ASVS L1, `block_on: high`. Решение
произвести аудит вместо изъятия — `chubav`, 2026-09-14, репликой «Прогнать /gsd-secure-phase 10»
на ship-гейте: изъятие 2026-09-10 до гейта отгрузки НЕ ДОСТАВАЛО, потому что гейт читает
`threats_open`, а не `status`. Это расхождение формы записи с механизмом названо здесь, а не
умолчано — оно того же класса, что отсрочка с адресатом без контракта в `deferred-items.md`.

### Двенадцать закрыты как БЕСПРЕДМЕТНЫЕ, а не как принятый риск

⚠️ **РАЗЛИЧИЕ НЕСУЩЕЕ И ВЫБРАНО СОЗНАТЕЛЬНО.** Принятый риск есть реальная подверженность,
которую кто-то решил нести. Здесь нести нечего: фаза не устанавливает ни одного пакета.
Распоряжение `accept` было в этих двенадцати строках **неверным глаголом для «неприменимо»**;
собственная поздняя идиома фазы (`n/a`) — верный.

Замер дерева, снятый аудитором ЗАНОВО и оказавшийся ШИРЕ замера оркестратора:

| Команда | Полученное |
|---|---|
| `git merge-base master HEAD` | `576cef0727e5422d374e00c31d7e2f4665c96b7a` |
| `git rev-list --count 576cef0..HEAD` | **418** |
| `git log --oneline 576cef0..HEAD -- pyproject.toml uv.lock wa_worker/package.json wa_bridge/package.json` | **пусто** |
| `git diff --name-only 576cef0..HEAD` через `grep -Ei 'package(-lock)?\.json|uv\.lock|pyproject\.toml|requirements.*\.txt|Pipfile|poetry\.lock|yarn\.lock|pnpm-lock'` | **exit 1, ноль совпадений** |

Последняя строка шире: ни одного манифеста и ни одного файла блокировки **любого** пакетного
менеджера ветвь не тронула — не только четырёх названных прежней записью.

### Расхождение тяжести: выброс — ДВЕНАДЦАТЬ, а не двадцать восемь

Прежняя запись этого файла говорила «28 против 12». Аудитор померил по ВСЕМ 57 планам, и счёт
оказался сильнее:

| Диапазон | Тяжесть | Планов |
|---|---|---|
| `10-01`…`10-11` | `low` | 11 |
| `10-12`…`10-23` | **`high`** | **12** |
| `10-24`…`10-34` | `low` | 11 |
| `10-35`…`10-57` | `n/a` | **23** |

**45 планов против 12.** С плана `10-35` идиома сошлась на `n/a` и держится **23 плана подряд
без единого возврата**. Двенадцать есть переходная ошибка оценки внутри одного окна партии, а не
взвешенная позиция фазы. Прежняя формулировка «28 против 12» ОШИБКОЙ НЕ БЫЛА: она верна для
замера 2026-09-10, который планов `10-41`…`10-57` видеть не мог.

### Реестр воспроизведён построчно

Разбор `<threat_model>` всех сорока планов заказанного диапазона совпал с замером 2026-09-10
**БЕЗ ЕДИНОГО РАСХОЖДЕНИЯ**: 250 строк, 239 уникальных имён, 84 `high` · 104 `medium` · 56 `low` ·
6 `n/a`, `critical` — 0; 195 `mitigate` · 55 `accept`; блокирующих при пороге `high` — 12;
столкновения имён `T-10-22-04` ×2 и `T-10-SC` ×11. Это стои́т назвать: `10-SECURITY-SUBJECT.md`
мерил честно.

**Чего тот замер видеть не мог.** Планы `10-41`…`10-57` — все **17** несут разбираемый
`<threat_model>`: **133** строки, 133 уникальных имени, 64 `high` · 45 `medium` · 17 `n/a` ·
7 `low`; 110 `mitigate` · 23 `accept`. **Блокирующих среди них НОЛЬ.** Полный реестр фазы
`10-01`…`10-57` — **383** строки, блокирующих по-прежнему ровно 12, и все двенадцать закрыты выше.

### Что проверено у 195 строк `mitigate` — прямыми уликами, а не «похоже на проверку»

Прогнано **758 правил, 0 отказов** шестью прогонами. **Все 72 строки `high`/`mitigate`
заказанного диапазона закрыты.** Улики — найденный вызов в продуктовом коде:

- `T-10-04`, `T-10-16` — мест записи `HX-Location` в Python ровно ОДНО (`app/pages/htmx.py:169`),
  и оно идёт через `_local_path` (`:110`), отвергающий не-`/`, `//`, `\`, `ord<0x20`/`0x7F` и
  не-ASCII. Это проверка «все входы», а не один греп.
- `T-10-02`, `T-10-19` — `Ad.user_id == user.id` в `WHERE` на `schedules.py:1012,1091,1208` и те же
  связи в `_ad_has_a_schedule`, `_ad_schedule_count`, `_owns_ad` (`:494,:514,:621`).
- `T-10-12-01/02`, `T-10-24-01`, `T-10-28-01`, `T-10-29-01/02` — `IdPath` (`identifiers.py:97`) на
  **43 площадках в 8 модулях**; `ID_MAX = 2_147_483_647` — истинный предел INT4, мутации границы
  в дереве нет.
- `T-10-24-02`, `T-10-28-02` — `is_same_origin` на **13 площадках в 8 модулях**, полноту держат два
  машинных гейта, оба зелены.
- `T-10-14`, `T-10-15`, `T-10-18` — порядок `respond` → `set_session_cookie` (`admin.py:1789-1791`),
  запись `impersonation_start` с обоими идентификаторами ДО `return` (`:1803-1807`),
  `require_admin` + `forbid_when_impersonating` в сигнатуре.
- `T-10-38`, `T-10-39`, `T-10-49` — гейт `sending` на всех трёх путях ухода панели
  (`modal.html:713,716,812`), безусловный сброс на `htmx:after-request` (`:807`), `method="post"`
  литералом (`:805`).

Из 123 строк `medium`/`low` с `mitigate` продуктового кода касаются **только шесть** — остальные
суть угрозы записи и суиты. Все шесть проверены поимённо.

### ЧЕГО АУДИТ НЕ ДОСТИГ — названо, а не умолчано

⚠️ **ЭТОТ РАЗДЕЛ ЕСТЬ ГЛАВНОЕ, ЧТО ОТЛИЧАЕТ АУДИТ ОТ ЧИСТОГО ЛИСТА.** Молчаливый частичный обход,
поданный как полный, есть ровно та подмена, за которую фаза платила кругами верификации.

1. **117 из 123 строк `medium`/`low` с `mitigate` НЕ прослежены построчно.** Проверено, что
   именованные модули правил существуют и зелены; НЕ проверено, что каждая отдельная строка
   реестра отвечает конкретному утверждению внутри них. Улика уровня МОДУЛЯ, а не СТРОКИ.
2. **110 строк `mitigate` планов `10-41`…`10-57`, из них 64 тяжести `high`, НЕ ПРОВЕРЕНЫ ВОВСЕ.**
   Они вне заказанного объёма, и блокирующих среди них ноль — но «объявлено митигированным» у них
   так же не есть «митигировано». **Рекомендация аудитора: второй прогон на этот диапазон.**
   ⚠️ Это ДОЛГ, а не закрытый пункт, и владелец у него не назначен.
3. **15 строк `accept` вне цепи поставок** (3 `medium`, 12 `low`) не значатся поимённо ни в одном
   журнале принятых рисков. Журнал проекта — `.planning/WINDOWS.md` (63 записи фазы 10), а не этот
   файл; сличить построчно нечем, потому что строки реестра номеров записей не несут.
4. **Половина рантайма браузера не проверяема суитой ни в каком объёме** — выражения `modal.html`
   проверены чтением исходника и правилами, исполняющими `x-data` в интерпретаторе JS со
   стаб-документом, а не наблюдением в браузере.

### Вне реестра — одно наблюдение, отделённое явно

Запись **54** журнала `.planning/WINDOWS.md` (`status: open`, владелец не назначен): докстринг
`is_same_origin` утверждает, что присутствие имени `test_every_destructive_route_checks_the_origin`
принуждается машинной проверкой, считающей вхождения, — такого правила в `tests/` НЕТ. Аудитор
подтвердил это своим поиском. Сам гард при этом полон и зелен; дефект — в записи о собственной
оснастке, не в защите. Уже зафиксировано проектом; новой находкой не объявляется.

⚠️ **ТОТ ЖЕ КЛАСС, ЧТО ВТОРАЯ ОТСРОЧКА ПРИЁМКИ** (докстринг `schedule_rules.py`, обещающий
несуществующий греп-гейт, `deferred-items.md`, раздел «Приёмка Фазы 10», пункт 2). Два независимых
прибора нашли в этой фазе один и тот же образец: **утверждение о принуждении, которого нет.**

### Одно объявление новой поверхности

Из 57 сводок `## Threat Flags` несут 23; сверх реестра объявлено **одно** место:
`10-01-SUMMARY.md` → `threat_flag: input-surface` (`ads/includes/sched_card.html`,
`app/pages/schedules.py`). Отображается на существо `T-10-02`, приходит с названной митигацией
(связь `Ad.user_id`), подтверждённой аудитором в коде. Информационно, не блокер.
