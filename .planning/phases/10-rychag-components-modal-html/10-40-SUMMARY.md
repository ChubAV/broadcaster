---
phase: 10-rychag-components-modal-html
plan: 40
subsystem: infra
tags: [verification, overrides, roadmap, gsd, planning-records]

requires:
  - phase: 10-rychag-components-modal-html (план 10-35)
    provides: "строки JS, дописанные в тело метода открытия рычага `components/modal.html` — ТРЕТЬЕ место, из-за которого перечень пятого круга стал неполон и предмет решения потребовал перезамера"
  - phase: 10-rychag-components-modal-html (план 10-13)
    provides: "обёртка немедленно вызываемым выражением в `ads/form.html` (`:308`, `:538`), чинящая регрессию `CR-03` — две строки, которыми критерий 3 тронут дословно"
  - phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
    provides: "ЖИВОЙ ПРЕЦЕДЕНТ формы записи отступления в этом проекте — блок `overrides` во frontmatter `09-VERIFICATION.md` (решение `accept-override`, `chubav`, 2026-09-01) с полями `must_have`/`reason`/`accepted_by`/`accepted_at`/`accepted_branch`/`accepted_branches_rejected`/`covers`"
provides:
  - "машинно читаемая запись отступления `overrides[0]` во frontmatter `10-VERIFICATION.md`: расхождение, четыре круга жившее ПРОЗОЙ отчёта, получило место, по которому следующий круг отличит ПРИНЯТОЕ отступление от НЕЗАМЕЧЕННОГО"
  - "`overrides_applied` приведён в согласие с числом записей блока: 0 → 1"
  - "ПЕРЕЗАМЕРЕННЫЙ на HEAD `c45d9c5` полный перечень строк JS, дописанных фазой в шаблоны — 51 строка в двух файлах, вместо двух строк, названных пятым кругом"
  - "приписки о разрешении расхождения к пункту 8 перечня ручной верификации (шапка `resolved_by` и тело отчёта) и к клетке таблицы критериев по критерию 3 — прежняя проза четырёх кругов стои́т дословно"
affects: [10-VERIFICATION.md следующего круга, /gsd-verify-phase Фазы 10, всякая следующая правка JS в шаблонах проекта]

actuals:
  # ⚠️ ЧИСЛО НЕ ПОДГОНЯЕТСЯ ПОД ОЦЕНКУ. Проекция плана была `tokens: 45000` при
  # `confidence: low` (sample_count 0, калибровка не применялась). Реализованный дифф
  # оказался ВОСЬМИКРАТНО меньше: план правит ОДИН документ, а объём оценки закладывался
  # под чтение отчёта, роадмапа, контекста, прецедента Фазы 9 и четыре замера на дереве —
  # то есть под ЧТЕНИЕ, которого шкала `chars/4 по диффу` не считает вовсе.
  tokens: 4365
  tasks: 2
  commits: 1
  plan_head_before: c45d9c5fb15e8c81526f189a34a9081bc3b60a4a

tech-stack:
  added: []
  patterns:
    - "Запись отступления наследует форму ЖИВОГО прецедента проекта, а не изобретает вторую: вторая форма разошлась бы с первой молча"
    - "Формулировка чужого критерия переносится в запись ПОБУКВЕННО и сличается посимвольно программно — пересказанный критерий есть отступление не от того критерия"
    - "Предмет решения ПЕРЕЗАМЕРЯЕТСЯ на HEAD в момент решения, а не выписывается из отчёта: отчёт писан до планов, изменивших предмет"
    - "Расхождение даты ответа и даты внесения записи НАЗЫВАЕТСЯ отдельным полем (`accepted_by_provenance`), а не сглаживается подстановкой сегодняшнего числа"

key-files:
  created:
    - .planning/phases/10-rychag-components-modal-html/10-40-SUMMARY.md
  modified:
    - .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
    - .planning/ROADMAP.md
    - .planning/STATE.md

key-decisions:
  - "РЕШЕНИЕ ВЛАДЕЛЬЦА `chubav` от 2026-09-09, ветвь `accept-reading`: прочтение D-13 критерия 3 ПРИНЯТО явной записью `overrides` в шапке отчёта верификации; отклонена ветвь `reject-reading`"
  - "Охват записи — ВЕСЬ перезамеренный перечень (51 строка JS в шаблонах), а не две строки обёртки: подтверждено владельцем прямо при ответе на останов"
  - "Форма записи унаследована от блока `overrides` Фазы 9 (`accept-override`, `chubav`, 2026-09-01); второй формы в проекте не заведено"
  - "Дата записи — та, которую назвал владелец (2026-09-09), а НЕ дата внесения (2026-09-10); расхождение названо полем `accepted_by_provenance`"
  - "Вердикт отчёта, счёт истин, список гэпов и блок повторной верификации НЕ ТРОНУТЫ: они принадлежат верификатору, и запись отступления есть основание для следующего круга, а не сам вердикт"
  - "`FORM-06` НЕ отмечается выполненным: вердикт фазы `human_needed`, и правило `test_no_requirement_is_marked_complete_before_its_phase_verification_passed` запрещает отметку до прохождения верификации"

patterns-established:
  - "Приписка, а не правка: разрешённое расхождение получает приписку с ветвью, датой и принявшим, а прежняя проза, называвшая расхождение открытым, стои́т дословно (идиома D-30/D-32)"
  - "Отклонённая ветвь называется ИМЕНЕМ в поле `accepted_branches_rejected`, но её заготовки в файле не оставляется: заготовка прочиталась бы следующим кругом как второе, несогласное решение"

requirements-completed: [FORM-06]
# ⚠️ ПОЛЕ ЗАПОЛНЕНО ПО FRONTMATTER ПЛАНА ДОСЛОВНО, НО ОТМЕТКА В `.planning/REQUIREMENTS.md`
# НЕ СТАВИТСЯ И `REQUIREMENTS.md` НЕ ПРАВИТСЯ: вердикт фазы — `human_needed`, а регламент
# отметок (`tests/test_planning/test_requirement_completion_follows_verification.py`)
# запрещает `Complete` до прохождения верификации. Та же граница соблюдена планами
# 10-33…10-37 этой же фазы.

coverage:
  - id: D1
    description: "В шапке `10-VERIFICATION.md` стои́т РОВНО ОДИН блок `overrides` ветви `accept-reading` с формулировкой критерия 3, перенесённой из `ROADMAP.md` ПОБУКВЕННО, причиной из перезамеренных величин, именем принявшего и датой; `overrides_applied` равен числу записей"
    requirement: "FORM-06"
    verification:
      - kind: other
        ref: "python: yaml.safe_load первого блока frontmatter → len(overrides)==1, overrides_applied==1, СОГЛАСИЕ True"
        status: pass
      - kind: other
        ref: "python: посимвольное сличение overrides[0].must_have со строкой .planning/ROADMAP.md:276 (критерий 3) → СОВПАДАЕТ"
        status: pass
      - kind: unit
        ref: "uv run pytest tests/test_planning/ -q → 28 passed"
        status: pass
      - kind: unit
        ref: "uv run pytest tests/test_templates/test_components.py -q -k criterion_three → 1 passed, 83 deselected"
        status: pass
    human_judgment: false
  - id: D2
    description: "К пункту 8 перечня ручной верификации (шапка и тело отчёта) и к клетке таблицы критериев по критерию 3 приписано, ЧЕМ и КОГДА расхождение разрешено; прежние тексты четырёх кругов стоя́т дословно"
    requirement: "FORM-06"
    verification:
      - kind: other
        ref: "git diff .planning/.../10-VERIFICATION.md — ни одной строки удаления в трёх правленых местах, только приписки"
        status: pass
    human_judgment: true
    rationale: "Что приписка называет разрешение ТОЧНО (ветвь, дата, принявший, охват) и что прежняя проза читается как летопись, а не как живое возражение, — суждение читателя следующего круга, а не свойство диффа"
  - id: D3
    description: "Ответ владельца записан дословно: идентификатор ветви `accept-reading`, имя `chubav`, дата `2026-09-09`, подтверждение охвата на ВЕСЬ перечень 51 строки"
    requirement: "FORM-06"
    verification: []
    human_judgment: true
    rationale: "Совпадение записанного с СКАЗАННЫМ владельцем не проверяется ничем, кроме самого владельца: машина видит только то, что записано. Это ровно та граница, ради которой задача 1 была блокирующим остановом (`gate=\"blocking-human\"`, угроза T-10-40-02)"

duration: 21min
completed: 2026-09-10
status: complete
---

# Phase 10 Plan 40: Прочтение D-13 критерия 3 принято записью `overrides` — решение владельца

**Расхождение, четыре круга жившее ПРОЗОЙ отчёта, получило машинно читаемое место: `overrides[0]` во frontmatter `10-VERIFICATION.md` по ветви владельца `accept-reading` (`chubav`, 2026-09-09), охватом на ВЕСЬ перезамеренный перечень — 51 строку JS в шаблонах, а не две строки обёртки.**

## Performance

- **Duration:** 21 min
- **Started:** 2026-09-10 (продолжение после ответа владельца на останов задачи 1)
- **Completed:** 2026-09-10
- **Tasks:** 2 (задача 1 — чекпойнт-останов, кода не производит; задача 2 — запись)
- **Files modified:** 1 продуктовый артефакт записи (`10-VERIFICATION.md`) + два общих (`ROADMAP.md`, `STATE.md`)

## Accomplishments

- **Четыре замера сняты КОМАНДАМИ на HEAD `c45d9c5` заново, а не унаследованы от предыдущего исполнителя и не выписаны из отчёта.**
- **Ответ владельца записан дословно** — ветвь `accept-reading`, имя `chubav`, дата `2026-09-09`, подтверждение охвата на весь перечень.
- **Блок `overrides` встал в шапку по форме ЖИВОГО прецедента Фазы 9**, `overrides_applied` приведён 0 → 1.
- **Формулировка критерия перенесена ПОБУКВЕННО и сличена ПОСИМВОЛЬНО программно** — совпадает.
- **Приписки о разрешении** встали в трёх местах; ни одна строка прежней прозы не вычеркнута.
- **Вердикт остался за верификатором:** `status`, `score`, `behavior_unverified`, `gaps` и `re_verification` в диффе отсутствуют.

## Четыре замера — ДОСЛОВНО, как сняты на HEAD `c45d9c5`

**Замер 1 — дифф каталога сценариев.**

```
$ git diff 7c1041d..HEAD -- app/static/js/
$ git diff --stat 7c1041d..HEAD -- app/static/js/
(вывод обеих команд ПУСТ — 0 строк)
```

**Замер 2 — ВСЕ строки JS, дописанные фазой в шаблоны (перезамер, а не выписка).** Снят разбором `git diff -U0 7c1041d..HEAD` по каждому шаблону с пересчётом номеров строк на HEAD; классификация — по попаданию строки внутрь `<script>` (для `ads/form.html`) и по несению выражения Alpine (для `components/modal.html`, где узла `<script>` нет вовсе). Исчерпывающий обход всех 13 тронутых фазой шаблонов по признакам `x-on:` / `x-data` / `x-init` / `onsubmit=` / `onclick=` / `onchange=` / `<script` / `hx-on` дал ровно ДВА файла — прочие одиннадцать не несут ни одной добавленной строки JS.

**ИТОГО 51 строка в двух файлах:**

| Файл | Строк | Разбивка |
| --- | --- | --- |
| `app/templates/ads/form.html` | **38** | ИСПОЛНЯЕМЫХ **2**: `:308` `(function () {`, `:538` `})();` — обёртка немедленно вызываемым выражением (план 10-13, чинит регрессию `CR-03`). КОММЕНТАРИЯ **36**: `:309-342` (34) и `:539-540` (2). Тело скрипта не правлено ни на символ |
| `app/templates/components/modal.html` | **13** | `:699-707` (9) — тело `show()` со снятием обеих заготовок плашки отказа (план 10-35, чинит `CR-01`) и тела `hide()`, `destroy()`, `land()`; `:713`, `:716`, `:777`, `:782` (4) — выражения существующих атрибутов события |

⚠️ **Перечень ШИРЕ перечня пятого круга:** тот называл ДВЕ строки (`ads/form.html:308`, `:538`). Разница — 49 строк и один файл, появившийся планом 10-35, то есть после того, как отчёт был написан.

**Замер 3 — четыре числа сущностей на HEAD, найдено/объявлено** (снято помощниками самого гейта — `_all_templates`, `_vendored_js_files`, `_client_state_sites`, `INLINE_SUBMIT_HANDLER`, `INLINE_HANDLER_ATTR`):

| # | Сущность | Найдено | Объявлено |
| --- | --- | --- | --- |
| 1 | файлов в `app/static/js/` | 2 (`alpine.min.js`, `htmx.min.js`) | 2 (`VENDORED_JS_FILES`) |
| 2 | шаблонов со встроенным `onsubmit` | 1 (`accounts/connect_max.html`) | 1 (`KNOWN_SUBMIT_HANDLER_FILES`) |
| 3 | файлов с атрибутом события рантайма разметки (`hx-on`) | 0 | 0 |
| 4 | узлов клиентского состояния (`x-data`) | 24 | 24 (`CLIENT_STATE_NODES`) |

Вердикт гейта прогоном: `uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"` → **`1 passed, 83 deselected`** (отбор НЕПУСТ, «no tests ran» не сообщается).

**Замер 4 — записи блока отступлений ДО правки.** `overrides_applied: 0` (строка 7). Записей блока `overrides` во frontmatter — **НОЛЬ**: разбор первого блока frontmatter (он кончался строкой 210) ключа `overrides` не содержал вовсе. Вхождение `overrides:` на строке 485 стои́т ВНУТРИ ОГОРОЖЕННОГО yaml-ОБРАЗЦА в ТЕЛЕ документа (пункт 8 перечня Human Verification) и записью не является.

✅ **Все четыре замера воспроизвели значения, снятые предыдущим исполнителем до останова, — но сняты заново своими командами, а не приняты по прозе диспатча.**

## Ответ владельца — записан дословно

| Поле | Значение |
| --- | --- |
| Ветвь | `accept-reading` |
| Имя для поля принявшего | `chubav` |
| Дата для записи | `2026-09-09` |
| Подтверждение охвата | запись отступления охватывает ВЕСЬ перезамеренный перечень строк JS замера 2 — все 51 строку в `ads/form.html` и `components/modal.html`, — а НЕ только две строки обёртки, названные пятым кругом |
| Отклонённая ветвь | `reject-reading` |

⚠️ **Расхождение дат названо, а не сглажено.** Владелец назвал дату `2026-09-09`; запись внесена `2026-09-10`. В запись пошло ТО, ЧТО НАЗВАЛ ВЛАДЕЛЕЦ, а расхождение объявлено полем `accepted_by_provenance` прямым текстом. Подставить дату внесения значило бы записать не ответ владельца, а собственное удобство.

## Task Commits

1. **Задача 1: РЕШЕНИЕ ВЛАДЕЛЬЦА (чекпойнт `gate="blocking-human"`)** — коммита не производит по построению. Останов был предъявлен владельцу предыдущим исполнителем; настоящий исполнитель ПЕРЕСНЯЛ все четыре замера своими командами и записал ответ.
2. **Задача 2: запись ответа в отчёт** — `415b0cf` (docs)

**Plan metadata:** коммит сводки идёт следом за настоящим замером `commits` и в число `actuals.commits: 1` не входит — оно снято `git rev-list --count c45d9c5..HEAD` В МОМЕНТ НАПИСАНИЯ СВОДКИ, как того требует протокол, а не досчитано вперёд.

## Files Created/Modified

- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — блок `overrides[0]` в шапке; `overrides_applied` 0 → 1; поле `resolved_by` у пункта 8 перечня `human_verification`; приписка к разделу `#### 8.` тела отчёта; приписка к клетке таблицы критериев по критерию 3
- `.planning/ROADMAP.md` — отметка исполнения плана 10-40 (`- [ ]` → `- [x]`, строка 384); ни одной правки текста критериев
- `.planning/STATE.md` — `progress.completed_plans` 75 → 76 (выведено из ROADMAP, а не набрано руками), запись решения, строка метрик `Phase 10 P40`, `stopped_at` и блок Session Continuity
- `.planning/phases/10-rychag-components-modal-html/10-40-SUMMARY.md` — настоящая сводка

## Decisions Made

См. `key-decisions` frontmatter. Отдельно, потому что это решение ИСПОЛНИТЕЛЯ, а не владельца:

- **Отклонённая ветвь названа ИМЕНЕМ, но её заготовки не оставлено.** `reject-reading` встречается в файле пять раз — один раз полем `accepted_branches_rejected: ["reject-reading"]` (форма прецедента Фазы 9) и четырежды прозой приписок, называющей, что именно отклонено. Ни одного блока-заготовки ветви отказа в файле нет: критерий «счёт вхождений ключа заготовки равен нулю» исполнен — ключа заготовки (`must_have` второй записи, поля нарушения критерия, перечня долга) в шапке не заведено, записей блока ровно одна.
- **`REQUIREMENTS.md` НЕ ПРАВИТСЯ.** План объявляет `requirements: [FORM-06]`, но вердикт фазы `human_needed`, и регламент отметок запрещает `Complete` до прохождения верификации. Отметка поставлена НЕ была; `requirements.mark-complete` не запускался. Это та же граница, которую держали планы 10-33…10-37.
- **Место блока в шапке — после `re_verification` и до `gaps`**, ровно как в `09-VERIFICATION.md`. Порядок существующих ключей не нарушен: `phase, verified, status, score, behavior_unverified, overrides_applied, re_verification, overrides, gaps, deferred, …`.

## Deviations from Plan

**None — план исполнен ровно так, как написан.** Ни одного правила отступлений (1-4) применять не потребовалось: план правит один документ записи, пакетов не ставит, кода не трогает.

Отдельно называется то, что деviaцией НЕ является и было предписано планом прямо: `.planning/ROADMAP.md` и `.planning/STATE.md` правлены НЕ как предмет плана (его `files_modified` называет один файл), а как общие артефакты, которые в последовательном режиме обновляет сам исполнитель. Ни одна строка критериев роадмапа при этом не тронута — правлена ровно одна строка отметки исполнения (`384`).

## Issues Encountered

**None.** Оба прогона верификации зелены с первого раза, шапка отчёта разбирается, посимвольное сличение критерия совпало без правок.

## Проверка `<verification>` плана — построчно

| # | Проверка | Результат |
| --- | --- | --- |
| 1 | `uv run pytest tests/test_planning/ -q` | **28 passed** (зелено ДО и ПОСЛЕ правки ROADMAP/STATE) |
| 2 | `uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"` | **1 passed, 83 deselected** — зелено, отбор непуст |
| 3 | Ровно один блок по выбранной ветви; заготовки отвергнутой ветви нет | записей `overrides` — **1**, `overrides_applied` — **1**, СОГЛАСИЕ True; заготовки ветви `reject-reading` нет |
| 4 | Формулировка критерия совпадает с роадмапом посимвольно | **СЛИЧЕНО ПРОГРАММНО — СОВПАДАЕТ** (`overrides[0].must_have` == `.planning/ROADMAP.md:276` без префикса нумерации) |
| 5 | Поле состояния, счёт истин, список гэпов и блок повторной верификации не тронуты | `git diff` по отчёту не содержит НИ ОДНОЙ правки строк `status:`, `score:`, `behavior_unverified:`, `gaps:`, `re_verification:` |
| 6 | `ROADMAP.md`, `10-CONTEXT.md`, `app/`, `tests/` не тронуты ни байтом | `10-CONTEXT.md`, `app/`, `tests/` — **не тронуты**; `ROADMAP.md` тронут РОВНО отметкой исполнения плана 10-40 (обязанность исполнителя в последовательном режиме), формулировки критериев не правлены |

## User Setup Required

None — внешней конфигурации не требуется.

## Next Phase Readiness

- **Расхождение по критерию 3 закрыто для следующего круга машинно.** У него появился способ отличить ПРИНЯТОЕ отступление от НЕЗАМЕЧЕННОГО: `overrides_applied: 1` и запись с четырьмя полями. Поднимать вопрос пятый раз оснований нет.
- **Остаток седьмой партии:** планы 10-38 и 10-39 не исполнены (отметки в роадмапе пусты). Фаза остаётся в `human_needed` — вторая половина её цели наблюдается только человеком, и это НЕ трогается настоящим планом.
- **Долг, который запись СОЗДАЛА и который назван прямо:** граница «новая сущность против правки существующей» стала действующим правилом проекта и переживает фазу; гейт четырёх чисел принуждает её только в четырёх измерениях. Пятое измерение, если оно появится, прочтение не закроет и не заметит — это записано в поле `covers`.
- **`FORM-06` остаётся неотмеченным** до вердикта верификации.

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-10*

## Self-Check: PASSED

- `10-40-SUMMARY.md` — FOUND на диске
- `10-VERIFICATION.md` — FOUND на диске, шапка разбирается, `overrides` = 1 запись
- Коммит задачи 2 `415b0cf` — FOUND в `git log --oneline --all`
- Коммит метаданных — ЭТОТ ЖЕ коммит (HEAD ветви фазы); хеш здесь НЕ НАЗЫВАЕТСЯ намеренно: назвать его внутри него самого нельзя, а названный до `--amend` хеш умер бы вместе с ним — ровно тот класс «запись шире дерева», за который фаза получила круги 3-6
- `uv run python -m pytest tests/test_planning/ -q` — **28 passed**
- `uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"` — **1 passed, 83 deselected**
- Рабочее дерево чисто: незакоммиченных правок нет (остаются только преджившие
  неотслеживаемые `.planning/milestone.lock` и `.planning/state.json`, которых план не
  касался)
