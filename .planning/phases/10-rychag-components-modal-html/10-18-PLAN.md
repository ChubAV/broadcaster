---
phase: 10-rychag-components-modal-html
plan: 18
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/pages/schedules.py
  - app/pages/htmx.py
  - app/pages/accounts.py
  - app/pages/ads.py
  - app/pages/account_groups.py
  - app/pages/common.py
  - tests/test_pages/test_origin_guard_on_destructive_routes.py
  - tests/test_pages/test_htmx_response_layer.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ — форма унаследована
# от планов 10-12…10-17. ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `workflow.security_asvs_level` → `1`;
# `workflow.security_block_on` → `"high"`; `workflow.security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ПРИЗНАК ВОЗВРАТА В РЕДАКТОР ЧИТАЕТСЯ ОДИН РАЗ, И ЭТО СВОЙСТВО СИГНАТУРЫ, А НЕ ПАМЯТЬ ЧИТАТЕЛЯ: `_editor_url` принимает уже вычисленный предикат, а не форму запроса, поэтому второе независимое чтение поля `return_to` в сборке адреса становится НЕВЫРАЗИМЫМ"
    - "КОММЕНТАРИЙ ОБРАБОТЧИКА ПЕРЕСТАЁТ ОБЕЩАТЬ ГАРАНТИЮ, КОТОРОЙ В КОДЕ НЕТ: утверждение «признак читается один раз и участвует в обоих решениях» становится истинным по построению, а не по совпадению"
    - "НЕОБРАТИМОЕ УДАЛЕНИЕ ПОЛЬЗОВАТЕЛЬСКИХ ДАННЫХ СВЕРЯЕТ ИСТОЧНИК ЗАПРОСА ТАК ЖЕ, КАК ЭТО ДЕЛАЮТ АДМИНИСТРАТИВНЫЕ МАРШРУТЫ: четыре маршрута подтверждённого удаления несут `is_same_origin`, и асимметрия, названная ревизией Фазы 6 под именем `CR-02`, закрыта, а не воспроизведена"
    - "ПОЛНОТА ГАРДА УТВЕРЖДАЕТСЯ ЗАМЕРОМ, А НЕ ПЕРЕЧНЕМ: маршрут, чей путь оканчивается на `/delete`, найденный в `app/pages/` и отсутствующий в объявленном реестре, краснит прогон — то есть пятый маршрут удаления не сможет появиться без принятого решения о гарде"
    - "ГЕЙТ ПОЛНОТЫ НЕ ЛОВУШКА: он показывает зубы на синтетическом исходнике со снятым гардом и на синтетическом исходнике с незаявленным маршрутом, по образцу отрицательного контроля админского гейта"
    - "ДВОЙНОЕ ВЛАДЕНИЕ РАЗВЕДЕНО ЯВНО, А НЕ ОСТАВЛЕНО СОВПАДЕНИЮ: пятый маршрут подтверждённого удаления (`app/pages/admin.py`, POST `/users/{user_id}/delete`) ПОПАДАЕТ под критерий разборщика и ПРИНАДЛЕЖИТ админскому гейту со своим объявленным числом; он изъят из нового реестра ПОИМЁННО, с записанным основанием, и изъятие само стережётся правилом — ссылка на чужого владельца, переставшая существовать, краснит прогон"
    - "ЗАПИСЬ О ГАРДЕ ПЕРЕСТАЁТ УТВЕРЖДАТЬ БОЛЬШЕ, ЧЕМ ИСПОЛНЯЕТ КОД: устаревшее перечисление «потребителей три» в докстринге `is_same_origin` снято ТЕМ ЖЕ ДЕЙСТВИЕМ, которым число потребителей растёт до восьми, — партия, существующая ради класса «запись утверждает больше, чем исполняет код», не расширяет его собственный экземпляр"
    - "ПАРАМЕТР КОДА ИСХОДА ВСТАЁТ ПЕРЕД ЯКОРЕМ, А НЕ ВНУТРЬ НЕГО: адрес приземления с якорем и кодом исхода собирается так, что код остаётся частью строки запроса, и плашка на пришедшем экране рисуется"
    - "ЛАТЕНТНАЯ ЛОВУШКА ЗАКРЫТА ДО ПЕРВОГО ПОТРЕБИТЕЛЯ, А НЕ ПОСЛЕ: сегодня адрес с якорем в сборку кода исхода не попадает, и правило заводится ИМЕННО поэтому — предмет, доказанный отсутствием потребителя, назавтра доказывается его появлением"
  artifacts:
    - "app/pages/schedules.py — `_editor_url` с предикатом возврата в сигнатуре и оба его потребителя, приведённые к одному вычислению признака"
    - "app/pages/accounts.py, app/pages/ads.py, app/pages/schedules.py, app/pages/account_groups.py — гард происхождения на четырёх маршрутах подтверждённого удаления"
    - "tests/test_pages/test_origin_guard_on_destructive_routes.py — НОВЫЙ модуль: реестр маршрутов удаления с объявленным числом, объявленное изъятие админского модуля с числом и основанием, правило полноты по замеру, правило владения изъятого маршрута и два контроля зубов"
    - "app/pages/common.py — докстринг `is_same_origin`: устаревшее перечисление потребителей снято, полнота передана машинным гейтам поимённо; объявленная ГРАНИЦА ЗАЩИТЫ не тронута ни словом"
    - "app/pages/htmx.py — сборка адреса приземления, ставящая параметр кода исхода ПЕРЕД якорем"
    - "tests/test_pages/test_htmx_response_layer.py — правило о положении параметра кода исхода относительно якоря и его отрицательный контроль"
  key_links:
    - "предикат в сигнатуре `_editor_url` → развилка формы ответа: единственное, чем ветка ответа и адрес приземления не разъедутся молча; ломается — возвращается дословно тот отказ, который абзац обработчика описывает как измеренное следствие расхождения"
    - "гард происхождения → четыре маршрута удаления: единственный серверный рубеж между сторонней страницей и необратимым удалением данных пользователя; ломается — рубежом остаётся одно умолчание браузера, которого продукт не выставляет и не проверяет"
    - "правило полноты по замеру → реестр маршрутов: единственное, чем пятый маршрут удаления не появится без принятого решения; ломается — перечень превращается в список того, до чего дошли руки"
  prohibitions:
    - statement: "гард происхождения НЕ вешается на маршруты ЧТЕНИЯ и не расширяется на весь `app/pages/` подряд: правило, отказывающее там, где нечего защищать, отключают целиком вместе со свойством, а объявленная граница гарда (запрос без обоих заголовков пропускается) записана в его собственном докстринге и настоящим планом не сдвигается"
      status: flagged-unverified
      verification: none
    - statement: "форма ответа на отказ гарда НЕ изобретается заново: она берётся у действующих потребителей `is_same_origin` дословно, потому что второй вид отказа на то же событие развёл бы два места одного правила"
      status: flagged-unverified
      verification: none
    - statement: "тестовая суита НЕ переучивается слать заголовки происхождения ради этой правки: гард по построению пропускает запрос без обоих заголовков, это записано его докстрингом как ПРИНЯТАЯ ГРАНИЦА, и правка фикстуры транспорта задела бы около 1700 правил ради свойства, которого гард не утверждает"
      status: flagged-unverified
      verification: none
    - statement: "сборка адреса приземления НЕ получает второго экземпляра: параметр кода исхода собирается ОДНИМ местом, и второе место разошлось бы с первым при первой же правке — основание записано докстрингом самой сборки"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ НАД FORM-06 — ЯВНЫЕ ДОПУЩЕНИЯ ПАРТИИ (полная запись — 10-17-PLAN.md):
  #   • `idempotency` — продуктовая половина закрыта планом 10-06; повтор в РАНТАЙМЕ браузера
  #     наблюдаем только обходом `10-UAT.md`, шаги 1.4-1.9, не пройденные ни разу. `unresolved`.
  #   • `concurrency` — адресован частично планом 10-07 и проверками 2 и 3 обхода; полного
  #     машинного критерия (два подтверждённых удаления в полёте одновременно) в суите без
  #     процесса выразить нечем. `unresolved`.
  # 2 предъявленных = 0 разрешённых критерием + 2 явных допущения.
---

<objective>
Закрываются ТРИ находки ревизии кода, чей предмет — СТРАНИЧНЫЙ СЛОЙ: `WR-01` (запись обещает
механическую гарантию, которой в коде нет), `WR-07` (необратимое удаление пользовательских данных
без серверного рубежа, тогда как соседние административные маршруты его несут) и `IN-03` (латентная
ловушка сборки адреса с кодом исхода при наличии якоря).

Purpose: все три — один класс, и он же тот, за который фаза получила четыре круга верификации:
**запись шире кода**. `WR-01` — комментарий утверждает единственность чтения там, где чтений два.
`WR-07` — проект в трёх соседних местах требует явной серверной проверки, а на удалении данных
пользователя её нет, и обработчики правились ЭТОЙ ЖЕ фазой. `IN-03` — докстринг сборки называет себя
«ЕДИНСТВЕННОЙ сборкой адреса с кодом исхода», а якоря не учитывает.

⚠️ **ПОЧЕМУ `WR-07` ЗАКРЫВАЕТСЯ, А НЕ ОТКЛАДЫВАЕТСЯ, — ОСНОВАНИЕ ЗАМЕРЕНО, А НЕ ВЗЯТО ИЗ ПАМЯТИ.**
Довод «правка гарда покрасит суиту» ПРОВЕРЕН ЧТЕНИЕМ ИСХОДНИКА гарда: `app/pages/common.py:686-694`
объявляет ПРИНЯТОЙ ГРАНИЦЕЙ, что запрос, не приславший НИ ОДНОГО из двух заголовков, пропускается —
дословно «в том числе тестовую суиту проекта, которая их не шлёт». То есть цена правки равна нулю
прогонов, и единственное, что стояло между отсрочкой и закрытием, — предположение, которое никто не
измерил. Оно измерено.

Output: `_editor_url` принимает предикат возврата; четыре маршрута подтверждённого удаления несут
`is_same_origin`; полнота гарда утверждается ЗАМЕРОМ по `app/pages/` с двумя контролями зубов;
параметр кода исхода встаёт перед якорем и стережётся правилом.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@app/pages/schedules.py
@app/pages/common.py
@app/pages/htmx.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: сквозной срез — признак возврата вычисляется ОДИН раз и уезжает в сборку адреса ПАРАМЕТРОМ</name>

  <files>
app/pages/schedules.py
  </files>

  <read_first>
- `app/pages/schedules.py:370-410` — `_editor_url(form_data, ad_id, schedule_id=None)` ЦЕЛИКОМ,
  вместе с докстрингом. ⚠️ СТРОКА `:402` — ВТОРОЕ ЧТЕНИЕ: `form_data.get("return_to") ==
  RETURN_TO_EDITOR`. Именно оно есть предмет задачи.
- `:410-430` — `_editor_redirect(form_data, ad_id, schedule_id=None)`: ВТОРОЙ потребитель сборки
  адреса. ⚠️ ЕГО НАДО ПРИВЕСТИ ТЕМ ЖЕ ДВИЖЕНИЕМ: правка одного потребителя оставила бы второй с
  прежней сигнатурой, и разъезд просто переехал бы на строку ниже.
- `:1055-1075` — `schedules_delete`: комментарий, объявляющий предмет `WR-01` закрытым, вычисление
  `returns_to_editor` и вызов `screen_url = _editor_url(form_data, ad_id)`. ⚠️ ЧИТАТЬ ДОСЛОВНО:
  комментарий и есть та запись, которая шире кода.
- `10-REVIEW.md:148-194` — `WR-01` целиком, включая предложенную сигнатуру. ⚠️ ОБРАЗЕЦ РЕВИЗИИ —
  ПОДСКАЗКА ФОРМЫ, а не текст для вставки: в дереве у `_editor_url` может быть иной порядок
  параметров и иные умолчания, и сигнатура собирается ЧТЕНИЕМ, а не копированием.
- Все вызовы `_editor_url` и `_editor_redirect` в дереве — снять поиском ДО правки и привести число
  в сводку: правка сигнатуры, пропустившая вызов, роняет импорт модуля, а не один маршрут.
- `10-08-SUMMARY.md` — что план 10-08 уже сделал по прежнему `WR-01` того же имени: ⚠️ ИМЕНА
  НАХОДОК В РАЗНЫХ КРУГАХ РЕВИЗИИ СОВПАДАЮТ, А ПРЕДМЕТЫ РАЗНЫЕ. Прежний `WR-01` был о развилке
  ответа; настоящий — о СБОРКЕ АДРЕСА, читающей форму второй раз. Не принять одно за другое.
  </read_first>

  <precondition>
Прогон `uv run pytest tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_editor_schedules.py -q -p no:randomly` на непрáвленом дереве зелен. Эти два модуля и есть
поведенческая опора обеих веток ответа удаления расписания; правка сигнатуры сборки адреса
проверяется ими, а не рассуждением.
  </precondition>

  <behavior>
1. Сборка адреса приземления принимает ПРЕДИКАТ возврата в редактор, а не форму запроса, и поля
   `return_to` не читает вовсе.
2. Оба потребителя сборки вычисляют предикат ОДНИМ И ТЕМ ЖЕ выражением и передают его дальше.
3. Поведение обеих веток ответа не меняется: возврат в редактор по-прежнему даёт адрес редактора с
   параметром и якорем выбранного расписания, отсутствие признака — сводный список.
4. Утверждение комментария обработчика становится ИСТИННЫМ ПО ПОСТРОЕНИЮ: второе независимое чтение
   поля в сборке адреса невыразимо, потому что формы у сборки больше нет.
  </behavior>

  <action>
**(1) Сменить сигнатуру `_editor_url`.** Первым параметром — булев предикат возврата в редактор
(имя выбрать совпадающим с именем локальной переменной обработчика, `returns_to_editor`, чтобы два
места одного факта звались одинаково), далее `ad_id` и `schedule_id` в прежнем порядке и с прежними
умолчаниями. Тело: условие читает ПАРАМЕТР, а не `form_data`. Из докстринга убрать всякое упоминание
чтения формы и вписать НОВОЕ утверждение: признак приходит вычисленным, поэтому второе чтение поля
`return_to` в этой функции невыразимо. ⚠️ ПРЕЖНЮЮ ФОРМУЛИРОВКУ НЕ ВЫЧЁРКИВАТЬ МОЛЧА: там, где
докстринг объяснял, почему признак читается ЗДЕСЬ, поставить пометку по идиоме D-30/D-32
`.planning/STATE.md` — прежнее утверждение названо и помечено опровергнутым с указанием находки
`WR-01` четвёртого круга ревизии как источника расхождения.

**(2) Привести `_editor_redirect`.** Он обязан вычислять предикат ТЕМ ЖЕ выражением
(`form_data.get("return_to") == RETURN_TO_EDITOR`) и передавать его в `_editor_url`. Литерал
`RETURN_TO_EDITOR` второй раз в модуле не выписывать — использовать существующую константу.

**(3) Привести `schedules_delete`.** Вызов сборки адреса получает уже вычисленную локальную
переменную `returns_to_editor` вместо `form_data`. Комментарий, объявляющий предмет закрытым,
переписать так, чтобы он называл МЕХАНИЗМ, а не обещание: признак стои́т в СИГНАТУРЕ сборки, поэтому
второе чтение невыразимо. ⚠️ Прежнюю формулировку пометить, а не стереть, по той же идиоме.

**(4) Привести ВСЕ прочие вызовы обеих функций.** Найти поиском по дереву до правки, привести число
в сводку, править все. Пропущенный вызов роняет импорт модуля целиком — это громкий отказ, и он
предпочтительнее тихого, но искать его надо до прогона, а не по трассировке.

**(5) Что НЕ трогается.** Границы величины идентификатора (`ID_MAX`, `ScheduleIdPath`, `AdIdForm`,
`AccountIdForm`) и `_ad_id_from_form` — предмет исполненных планов 10-08 и 10-12. Формы ответа,
коды исхода, содержимое фрагмента и заголовки перехода. Гард происхождения — предмет задачи 2.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_editor_schedules.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — обе ветки ответа удаления расписания разъехались с адресом приземления</fails_when>
    <automated>sed -n '/^def _editor_url/,/^def /p' app/pages/schedules.py | grep -c 'form_data'</automated>
    <fails_when>вывод отличен от `0` — сборка адреса по-прежнему видит форму запроса, то есть второе независимое чтение признака осталось выразимым</fails_when>
    <automated>grep -c 'RETURN_TO_EDITOR' app/pages/schedules.py</automated>
    <fails_when>вывод больше `3` — литерал признака выписан больше чем в объявлении константы и двух её законных чтениях, то есть источников одного решения снова несколько</fails_when>
  </verify>

  <acceptance_criteria>
- Сигнатура `_editor_url` приведена в сводке дословно, и первый её параметр — булев предикат.
- Тело `_editor_url` не называет `form_data`: в сводке приведён вывод второй команды раздела
  `<verify>` (`0`).
- Число найденных вызовов `_editor_url` и `_editor_redirect` ДО правки приведено в сводке, и все они
  правлены: в сводке приведён вывод `grep -n '_editor_url\|_editor_redirect' app/pages/schedules.py`
  после правки.
- Прежние формулировки докстринга и комментария ПОМЕЧЕНЫ опровергнутыми, а не стёрты: в сводке
  приведён текст пометки, и в нём назван источник расхождения (`WR-01`, ревизия 2026-09-04).
- Поведение обеих веток не изменилось: в сводке приведён вывод первой команды раздела `<verify>` с
  числом.
- Прогон целиком зелен: в сводке приведён вывод `uv run pytest tests/ -q -p no:randomly`.
  </acceptance_criteria>

  <reversibility rating="reversible">
Меняется сигнатура ДВУХ внутренних функций одного модуля и три места их вызова; внешнего контракта
(адресов, кодов ответа, заголовков) правка не касается, поэтому откат равен обратной правке
сигнатуры и наблюдаем прогоном тех же двух модулей суиты.
  </reversibility>

  <done>
Признак возврата в редактор вычисляется одним выражением и уезжает в сборку адреса параметром;
второе независимое чтение поля `return_to` в сборке НЕВЫРАЗИМО; утверждение комментария стало
свойством сигнатуры; прежние формулировки помечены опровергнутыми с названным источником.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: гард происхождения на четырёх маршрутах подтверждённого удаления и гейт ПОЛНОТЫ по замеру</name>

  <files>
app/pages/accounts.py
app/pages/ads.py
app/pages/schedules.py
app/pages/account_groups.py
app/pages/common.py
tests/test_pages/test_origin_guard_on_destructive_routes.py
  </files>

  <read_first>
- `app/pages/common.py:666-706` — `is_same_origin(request)` ЦЕЛИКОМ с докстрингом. ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО ВЕСЬ: там объявлено правило (сначала `Sec-Fetch-Site`, затем ХОСТ заголовка `Origin`),
  и там же — НАЗВАННАЯ ГРАНИЦА ЗАЩИТЫ: запрос, не приславший НИ ОДНОГО из двух заголовков,
  ПРОПУСКАЕТСЯ, «в том числе тестовую суиту проекта, которая их не шлёт». Из этой границы следует
  цена настоящей задачи для суиты, и она равна нулю прогонов.
- `app/pages/billing.py:305`, `app/pages/history.py:917`, `app/pages/admin.py:903` — ТРИ ДЕЙСТВУЮЩИХ
  ПОТРЕБИТЕЛЯ гарда. ⚠️ ФОРМУ ОТКАЗА СНЯТЬ ОТСЮДА ДОСЛОВНО: позиция вызова относительно проверки
  прав, вид возвращаемого ответа и код. Второй вид отказа на то же событие развёл бы два места
  одного правила.
- `tests/test_pages/test_admin_panel.py:2650-2790` — админский гейт полноты целиком:
  `_admin_mutating_handlers`, `ADMIN_MUTATING_ROUTE_COUNT`,
  `test_every_mutating_admin_route_checks_the_origin`,
  `_admin_routes_without_the_origin_guard`, `test_control_negative_a_mutating_admin_route_without_the_guard_reddens`.
  ⚠️ ЭТО ОБРАЗЕЦ, КОТОРЫЙ ВОСПРОИЗВОДИТСЯ, А НЕ ПРАВИТСЯ: файл админского гейта настоящим планом НЕ
  ТРОГАЕТСЯ, у него свой предмет и своё объявленное число.
- Четыре обработчика — предмет задачи, каждый ЦЕЛИКОМ от декоратора до первого обращения к базе:
  `app/pages/accounts.py:985-1030` (`accounts_delete`), `app/pages/ads.py:716-770` (`ads_delete`),
  `app/pages/schedules.py:1019-1075` (`schedules_delete`), и обработчик удаления группы аккаунта в
  `app/pages/account_groups.py` — его позицию снять поиском, номер строки в план не вписан
  намеренно: планировщик его не замерял, и вписанный «примерно» он увёл бы исполнителя не туда.
- `10-REVIEW.md:411-445` — `WR-07` целиком, включая прецедент `CR-02` ревизии Фазы 6.
- `app/pages/auth.py:93` — умолчание `samesite="lax"`, то самое ЕДИНСТВЕННОЕ, что стоит между
  сторонней страницей и этими четырьмя действиями сегодня.
- `app/pages/admin.py:1840-1870` — **ПЯТЫЙ** маршрут подтверждённого удаления,
  `@router.post("/users/{user_id}/delete")`, и вызов гарда на нём (`:1867`). ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО
  ДО НАПИСАНИЯ РАЗБОРЩИКА: он ПОПАДАЕТ под критерий «POST + путь оканчивается на `/delete`»,
  гард на нём УЖЕ СТОИТ, и стережёт его ЧУЖОЙ гейт со СВОИМ объявленным числом
  (`test_admin_panel.py`). Из этого следует изъятие пункта (3a) ниже — двойное владение разводится
  РЕШЕНИЕМ, а не совпадением критериев.
- ⚠️ **ЗАМЕР ПЛАНИРОВЩИКА, СНЯТЫЙ 2026-09-05, — ОРИЕНТИР, А НЕ ЗНАЧЕНИЕ.** Команда и вывод:
  `grep -rn '@router.post(.*delete' app/pages/` → **ПЯТЬ** маршрутов в ПЯТИ модулях:
  `accounts.py:989`, `admin.py:1840`, `account_groups.py:587`, `schedules.py:1019`, `ads.py:718`.
  Исполнитель переснимает своей командой; расхождение — НАХОДКА для сводки.
- `app/pages/common.py:694-706` — абзац «⚠️ РАМКИ» докстринга `is_same_origin` ЦЕЛИКОМ и следующий
  за ним абзац «⚠️ ПЕРЕЧЕНЬ ПОТРЕБИТЕЛЕЙ ЗДЕСЬ БОЛЬШЕ НЕ ВЕДЁТСЯ». ⚠️ ЧИТАТЬ ОБА И УВИДЕТЬ, ЧТО ОНИ
  ПРОТИВОРЕЧАТ ДРУГ ДРУГУ: первый утверждает «Потребителей три, и все зовут эту функцию», второй
  объявляет, что перечень больше не ведётся. ЗАМЕР ПЛАНИРОВЩИКА (2026-09-05,
  `grep -rn 'is_same_origin' app/`): потребителей **ЧЕТЫРЕ файла** и **ДЕВЯТЬ мест вызова**
  (`auth.py:476`, `billing.py:305`, `history.py:917`, `admin.py:903/1100/1637/1738/1819/1867`);
  после настоящей задачи станет **восемь файлов**. Предмет пункта (1a) ниже.
  </read_first>

  <precondition>
Гард `is_same_origin` в дереве и его докстринг содержит абзац о принятой границе (запрос без обоих
заголовков пропускается). Если абзац отсутствует — граница сдвинулась после планирования, и цену
правки для суиты надо переснять прогоном ДО того, как вешать гард: весь расчёт задачи стои́т на этом
абзаце.
  </precondition>

  <behavior>
1. Четыре маршрута подтверждённого удаления пользовательских данных отвечают отказом на запрос,
   пришедший НЕ со страницы самого приложения, — тем же видом отказа и в той же позиции, что и три
   действующих потребителя гарда.
2. Позиция вызова — ПОСЛЕ проверки прав и ДО любого чтения данных: отказ по происхождению не имеет
   права стать признаком существования строки.
3. Реестр маршрутов удаления объявлен и несёт число; маршрут, найденный в `app/pages/` замером и
   отсутствующий в реестре, краснит прогон.
4. Каждый маршрут реестра несёт вызов гарда; маршрут без вызова назван поимённо в тексте отказа.
5. Оба правила показывают зубы на СИНТЕТИЧЕСКОМ исходнике: снятый гард даёт ровно одно имя, а
   незаявленный маршрут `/delete` краснит правило полноты.
6. Суита проекта продолжает получать прежние ответы: она не шлёт ни `Origin`, ни `Sec-Fetch-Site`,
   и гард по объявленной границе её пропускает.
7. ПЯТЫЙ маршрут — административное удаление пользователя — попадает под критерий разборщика и
   ИЗЪЯТ из реестра ПОИМЁННО, потому что владелец у него УЖЕ ЕСТЬ: админский гейт полноты со своим
   объявленным числом. Изъятие несёт основание и число, а ссылка на чужого владельца САМА
   стережётся правилом: исчез гейт-владелец — изъятие потеряло основание, и прогон краснеет.
8. Докстринг гарда перестаёт называть число потребителей, разошедшееся с кодом: перечисление
   снимается, полнота передаётся ДВУМ машинным гейтам поимённо. Объявленная ГРАНИЦА ЗАЩИТЫ (запрос
   без обоих заголовков пропускается) НЕ ТРОГАЕТСЯ: на ней стои́т весь расчёт цены задачи для суиты.
  </behavior>

  <action>
**(1) Повесить гард на четыре обработчика.** В каждом из `accounts_delete`, `ads_delete`,
`schedules_delete` и обработчика удаления группы аккаунта вставить проверку `is_same_origin(request)`
и отказ — ФОРМОЙ, снятой с действующих потребителей (`app/pages/billing.py:305`,
`app/pages/history.py:917`), а не изобретённой. Позиция — после проверки прав пользователя и ДО
любого запроса к базе. Импорт `is_same_origin` из `app.pages.common` добавить там, где его ещё нет.
Рядом с каждым вызовом — ОДНОСТРОЧНЫЙ комментарий, называющий предмет (`WR-07`, ревизия
2026-09-04) и прецедент (`CR-02` ревизии Фазы 6), чтобы следующий читатель видел, что асимметрия
закрыта решением, а не совпадением.

**(1a) СНЯТЬ УСТАРЕВШЕЕ ПЕРЕЧИСЛЕНИЕ ПОТРЕБИТЕЛЕЙ В ДОКСТРИНГЕ `is_same_origin`** (`app/pages/common.py`,
абзац «⚠️ РАМКИ»). ⚠️ ЭТО НЕ ПОБОЧНАЯ ПРАВКА, А ПРЕДМЕТ ПАРТИИ: партия существует ради класса
«запись утверждает больше, чем исполняет код», а предложение «Потребителей три, и все зовут эту
функцию: форма покупки тарифа, форма покупки пакета сообщений (обе — `app/pages/billing.py`) и
повтор отправки (`app/pages/history.py`)» разошлось с деревом ЕЩЁ ДО настоящего плана (замер:
четыре файла, девять мест вызова) и разойдётся ВДВОЕ сильнее ПОСЛЕ него (восемь файлов). Оставить
его — значит штатно расширить собственный экземпляр класса, который партия помечает у других.

Заменить ПРЕДЛОЖЕНИЕ С ПЕРЕЧИСЛЕНИЕМ на утверждение, которое код исполняет: гард живёт публичным
именем здесь, чтобы у правила был ОДИН источник на проект; ЧИСЛО И ПЕРЕЧЕНЬ ПОТРЕБИТЕЛЕЙ ЗДЕСЬ НЕ
ВЕДУТСЯ, и полноту держат ДВА машинных гейта, названных поимённо, —
`test_every_mutating_admin_route_checks_the_origin` (`tests/test_pages/test_admin_panel.py`,
изменяющие маршруты админки) и `test_every_destructive_route_checks_the_origin`
(`tests/test_pages/test_origin_guard_on_destructive_routes.py`, маршруты подтверждённого удаления
пользовательских данных). ⚠️ СЛЕДУЮЩИЙ АБЗАЦ ДОКСТРИНГА («⚠️ ПЕРЕЧЕНЬ ПОТРЕБИТЕЛЕЙ ЗДЕСЬ БОЛЬШЕ НЕ
ВЕДЁТСЯ, И ЭТО ОСОЗНАННО») УЖЕ ГОВОРИТ РОВНО ЭТО — правка не заводит нового решения, а приводит
первый абзац к решению, записанному вторым, и дописывает в него ИМЯ ВТОРОГО ГЕЙТА.
⚠️ **ЧТО В ДОКСТРИНГЕ НЕ ТРОГАЕТСЯ НИ СЛОВОМ:** абзац «⚠️ НАЗВАННАЯ ГРАНИЦА ЗАЩИТЫ» и всё правило
сверки заголовков. На этой границе стои́т расчёт цены задачи для суиты, и сдвинутая мимоходом, она
обесценила бы предусловие задачи.
⚠️ **И ПРЕЖНИЙ ТЕКСТ НЕ ВЫЧЁРКИВАЕТСЯ БЕЗ ПРЕЕМНИКА:** по идиоме D-30/D-32 снятое утверждение
получает пометку опровергнутости с указанием ЗАМЕРА (дата, команда, полученные числа) и ДЕЙСТВУЮЩЕГО
преемника — двух названных гейтов.

**(2) Завести НОВЫЙ модуль `tests/test_pages/test_origin_guard_on_destructive_routes.py`.** Шапка —
докстринг по форме админского гейта: ЗАЧЕМ (аутентификация идёт cookie, браузер приложит её к
межсайтовой форме сам; ревизия Фазы 6 закрыла ту же асимметрию у административного удаления
пользователя, а на удалении данных САМОГО пользователя рубежа не было, при том что обработчики
правились Фазой 10); ЧЕГО МОДУЛЬ НЕ ДОКАЗЫВАЕТ (он не утверждает, что гард ЗАЩИЩАЕТ, — граница
гарда объявлена его собственным докстрингом и здесь не пересматривается; он утверждает, что гард
СТОИТ).

**(3) Реестр и его число.** `DESTRUCTIVE_ROUTE_HANDLERS` — отображение «имя модуля страничного слоя
→ множество имён обработчиков подтверждённого удаления», собранное ЧТЕНИЕМ дерева. Рядом —
`DESTRUCTIVE_ROUTES_DECLARED` с ЛЕТОПИСЬЮ по форме `OFFSET_CURSOR_EXCEPTIONS_DECLARED`
(`tests/test_pages/test_htmx_gates.py:466-473`): объявленное число, откуда взялось, чем измерено,
каким планом. ⚠️ ⚠️ **ЗАМЕР ПЛАНИРОВЩИКА ТЕПЕРЬ ЕСТЬ, И ОН ОРИЕНТИР, А НЕ ЗНАЧЕНИЕ**
(`grep -rn '@router.post(.*delete' app/pages/`, 2026-09-05): **ПЯТЬ** маршрутов в ПЯТИ модулях —
`accounts.py:989`, `ads.py:718`, `schedules.py:1019`, `account_groups.py:587` и
`admin.py:1840`. Исполнитель ПЕРЕСНИМАЕТ своей командой и приводит вывод в сводку; расхождение с
ориентиром — НАХОДКА, а не помеха, и записывается ЗАМЕРЕННОЕ. Маршрут, не попавший ни в реестр, ни в
объявленное изъятие, называется поимённо, и решение о гарде на нём принимается ЯВНО, а не молча.

**(3a) РАЗВЕСТИ ДВОЙНОЕ ВЛАДЕНИЕ — ЯВНЫМ ИЗЪЯТИЕМ С ОСНОВАНИЕМ И ЧИСЛОМ.** ⚠️ ПЯТЫЙ МАРШРУТ ЗАМЕРА,
`app/pages/admin.py` POST `/users/{user_id}/delete`, попадает под критерий разборщика ДОСЛОВНО, гард
на нём УЖЕ СТОИТ (`:1867`), и стережёт его ЧУЖОЙ гейт со СВОИМ объявленным числом —
`test_every_mutating_admin_route_checks_the_origin` / `ADMIN_MUTATING_ROUTE_COUNT`
(`tests/test_pages/test_admin_panel.py`), заведённый ревизией Фазы 6 под именем `CR-02`. Два правила
над одним маршрутом означают ДВА объявленных числа, которые придётся не забыть править вместе, —
и забытая половина выглядела бы работающей. Это ровно тот дефект, ради которого гард в своё время
свели к одному источнику.

Завести `DESTRUCTIVE_ROUTE_OWNERSHIP_EXEMPTIONS` — отображение «имя модуля страничного слоя → имя
ГЕЙТА-ВЛАДЕЛЬЦА и его константы числа», с обоснованием у каждой записи, и
`OWNERSHIP_EXEMPTIONS_DECLARED` с летописью той же формы (ориентир планировщика: **1** —
`admin.py`). Разборщик реестра исключает модули изъятия ИЗ СВОЕЙ ВСЕЛЕННОЙ, и правило числа
объявляет ДВА числа: сколько маршрутов найдено ВСЕГО по `app/pages/` и сколько из них в реестре
после изъятия. ⚠️ ИЗЪЯТИЕ ПО МОДУЛЮ, А НЕ ПО ПУТИ МАРШРУТА: админский гейт стережёт ВСЕ изменяющие
маршруты своего модуля — шире, чем `/delete`, — и изъятие по модулю совпадает с его вселенной, а
изъятие по одному пути оставило бы будущий второй `/delete` админки в двойном владении снова.

⚠️ **ИЗЪЯТИЕ НЕ ПЕРЕЖИВЁТ СВОЕГО ОСНОВАНИЯ, И ЭТО ПРИНУЖДАЕТСЯ.** Завести правило
`test_every_ownership_exemption_names_a_living_gate`: для каждой записи изъятия ПРОЧИТАТЬ исходник
названного гейта-владельца и утвердить, что имя правила и имя его константы числа В НЁМ ЕСТЬ.
Переименовали, снесли или переписали чужой гейт — изъятие потеряло основание, и прогон краснеет с
именем модуля и именем пропавшего правила. Без этого правила изъятие есть ссылка на обещание, а
маршрут необратимого удаления оказался бы не стережём НИКЕМ при двух зелёных гейтах.
⚠️ **ЧИТАТЬ — ДА, ПРАВИТЬ — НЕТ:** `tests/test_pages/test_admin_panel.py` настоящим планом НЕ
ТРОГАЕТСЯ ни на символ; правило читает его ИСХОДНИК ТЕКСТОМ, как всякий разборщик каталога.

**(4) Разборщики.** `destructive_route_handlers(module_name, source)` — имена функций-обработчиков,
чей декоратор объявляет метод POST и чей путь оканчивается на `/delete`; исходник приходит ТЕКСТОМ
(идиома каталога: без параметра отрицательный контроль невыразим). `handlers_without_the_origin_guard(module_name, source)` —
из них те, в чьём теле нет вызова `is_same_origin`. Разбор вести через `ast`, а не по строкам:
разбор по строкам сломался бы от переноса декоратора, и хрупкое правило отключают вместе со
свойством.

**(5) Правила.** `test_the_number_of_destructive_routes_is_declared` — ДВА равенства: число
маршрутов, найденных замером по ВСЕМУ `app/pages/`, равно объявленному общему, и число модулей
изъятия равно `OWNERSHIP_EXEMPTIONS_DECLARED`. Отказ говорит, что делать: маршрут добавлен или снят
— решение о гарде принимается явно, а число исправляется вместе с ним; изъятие выросло — второй
модуль ушёл под чужого владельца, и это решение, а не правка числа. `test_every_destructive_route_checks_the_origin` — множество
маршрутов без гарда пусто, отказ называет `модуль: обработчик` каждого. Плюс утверждение непустоты:
модулей разобрано больше нуля и маршрутов найдено больше нуля, — иначе оба правила зеленели бы
вакуумом на первом же переименовании каталога.

**(6) Два контроля зубов.** `test_control_a_route_without_the_guard_is_named` — синтетический
исходник с одним обработчиком `/delete` БЕЗ вызова гарда: разборщик называет ровно его.
`test_control_an_undeclared_destructive_route_reddens_the_count` — синтетический исходник с ДВУМЯ
обработчиками `/delete` там, где реестр знает одного: счёт расходится с объявленным. Оба контроля
питаются СИНТЕТИЧЕСКИМ исходником, боевые файлы не правятся — приём и его основание взяты у
`test_control_negative_a_mutating_admin_route_without_the_guard_reddens`.

**(7) Что НЕ трогается.** `tests/test_pages/test_admin_panel.py` и админский гейт полноты — у них
свой предмет и своё число; настоящий план его ЧИТАЕТ (правило пункта 3a) и не правит.
**ОБЪЯВЛЕННАЯ ГРАНИЦА ЗАЩИТЫ** в докстринге `is_same_origin` (абзац «⚠️ НАЗВАННАЯ ГРАНИЦА ЗАЩИТЫ»)
и правило сверки заголовков — ни словом. ⚠️ **ПОПРАВКА К ПРЕЖНЕЙ РЕДАКЦИИ ЭТОГО ПУНКТА:** докстринг
целиком из-под правки ВЫВЕДЕН НЕ БЫЛ БЫ ЧЕСТНО — устаревшее ПЕРЕЧИСЛЕНИЕ потребителей («Потребителей
три») правится пунктом (1a), потому что настоящая задача доводит их число до восьми файлов, и запись,
оставленная как есть, утверждала бы больше, чем исполняет код, — то есть ровно тот класс, ради
которого написана партия. Правится ПЕРЕЧИСЛЕНИЕ; граница — нет.
Маршруты ЧТЕНИЯ. Фикстуры транспорта суиты: заголовков происхождения они не шлют и переучиваться
не будут, что объявлено прохибицией плана.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `5` — пять правил модуля (число и изъятие, полнота, живость гейта-владельца, два контроля) не заведены либо не зелены</fails_when>
    <automated>grep -c 'is_same_origin' app/pages/accounts.py app/pages/ads.py app/pages/schedules.py app/pages/account_groups.py</automated>
    <fails_when>для любого из четырёх файлов выведено значение меньше `2` — в файле нет либо импорта, либо самого вызова гарда</fails_when>
    <automated>grep -c 'test_every_destructive_route_checks_the_origin' app/pages/common.py</automated>
    <fails_when>вывод отличен от `1` — докстринг гарда не назвал ВТОРОГО машинного гейта полноты, то есть перечисление потребителей снято, а действующий преемник утверждения не назван; ⚠️ ГЕЙТ ПОСТРОЕН НА ПРИСУТСТВИИ ПРЕЕМНИКА, А НЕ НА ОТСУТСТВИИ ПРЕЖНЕГО ЛИТЕРАЛА, потому что идиома D-30/D-32 ОСТАВЛЯЕТ опровергнутый текст на месте с пометкой, и отрицательный греп по нему красил бы работу, а не дефект</fails_when>
    <automated>grep -c 'НАЗВАННАЯ ГРАНИЦА ЗАЩИТЫ' app/pages/common.py</automated>
    <fails_when>вывод отличен от `1` — правка докстринга задела объявленную границу защиты, на которой стои́т предусловие задачи и весь расчёт её цены для суиты</fails_when>
    <automated>uv run pytest tests/test_pages -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — гард отказал запросу суиты, то есть объявленная граница гарда сдвинулась и цену правки надо пересчитать</fails_when>
  </verify>

  <acceptance_criteria>
- Гард стои́т на всех четырёх маршрутах: в сводке приведён вывод
  `grep -n 'is_same_origin' app/pages/accounts.py app/pages/ads.py app/pages/schedules.py app/pages/account_groups.py`.
- ДОКСТРИНГ ГАРДА БОЛЬШЕ НЕ УТВЕРЖДАЕТ ЧИСЛА, КОТОРОГО НЕ ИСПОЛНЯЕТ: в сводке приведён правленый
  абзац «⚠️ РАМКИ» ЦЕЛИКОМ вместе с пометкой опровергнутости — с датой замера, командой
  (`grep -rn 'is_same_origin' app/`), полученными числами (ориентир планировщика 2026-09-05:
  четыре файла и девять мест вызова ДО задачи, восемь файлов ПОСЛЕ) и ИМЕНАМИ ДВУХ действующих
  гейтов-преемников.
- ГРАНИЦА ЗАЩИТЫ НЕ СДВИНУТА: в сводке приведён вывод команды раздела `<verify>` по
  `НАЗВАННАЯ ГРАНИЦА ЗАЩИТЫ` (`1`) и прямое утверждение, что абзац не правился ни словом.
- ДВОЙНОЕ ВЛАДЕНИЕ РАЗВЕДЕНО, И ОБА ЧИСЛА НАЗВАНЫ: в сводке приведены число маршрутов, найденных
  замером по ВСЕМУ `app/pages/`, число модулей изъятия и число маршрутов реестра после изъятия.
  ⚠️ ОРИЕНТИР ПЛАНИРОВЩИКА (2026-09-05): найдено **5** маршрутов в **5** модулях, изъят **1** модуль
  (`admin.py`), в реестре остаётся **4**. Расхождение с ориентиром — НАХОДКА, и записывается
  ЗАМЕРЕННОЕ.
- ИЗЪЯТИЕ НЕ ПЕРЕЖИВЁТ ОСНОВАНИЯ, И ЭТО ПОКАЗАНО: в сводке приведён вывод правила
  `test_every_ownership_exemption_names_a_living_gate` и вывод его пробного прогона с намеренно
  искажённым именем чужого гейта (ожидается КРАСНЫЙ с именем модуля и именем пропавшего правила).
- `app/pages/admin.py` НЕ ТРОНУТ: `git diff --name-only -- app/pages/admin.py` — пусто; гард на его
  маршруте удаления стоял ДО партии (`:1867`), и настоящий план его не переставляет.
- Позиция вызова названа: для каждого из четырёх в сводке сказано, какая проверка стои́т ВЫШЕ него и
  какое обращение к базе — НИЖЕ.
- Форма отказа СЛИЧЕНА с действующими потребителями: в сводке приведены две строки — из
  `app/pages/billing.py` и из нового места — и показано, что они совпадают по виду ответа и коду.
- `DESTRUCTIVE_ROUTES_DECLARED` снят ЗАМЕРОМ: в сводке приведена команда замера, её вывод и
  получившееся число. Маршрут, не попавший ни в реестр, ни в объявленное изъятие, назван поимённо, и
  решение о гарде на нём записано ЯВНО.
- Оба контроля зубов заведены и КРАСНЕЮТ на своём предмете: в сводке приведён вывод пробного
  прогона каждого с намеренно исправленным разборщиком (ожидается отказ) и вывод штатного прогона.
- Непустота обхода утверждена: в сводке приведены число разобранных модулей и число найденных
  маршрутов, оба больше нуля.
- Суита не покраснела: в сводке приведён вывод `uv run pytest tests/ -q -p no:randomly` с числом.
- `tests/test_pages/test_admin_panel.py` не тронут: `git diff --name-only -- tests/test_pages/test_admin_panel.py` — пусто.
  </acceptance_criteria>

  <reversibility rating="costly">
Гард меняет НАБЛЮДАЕМОЕ поведение четырёх боевых маршрутов для браузерных запросов: страница,
пришедшая со стороннего происхождения, получает отказ там, где прежде получала удаление. Откат
возвращает экспозицию, названную ревизией, поэтому он не бесплатен и требует решения, а не правки.
⚠️ ПОРОГ БЛОКИРОВКИ НЕ ПРЕВЫШЕН: severity предмета — medium (см. `T-10-18-02`), порог
`security_block_on` — `high`, поэтому чекпойнт решения не вставляется.
  </reversibility>

  <done>
Четыре маршрута подтверждённого удаления пользовательских данных сверяют источник запроса той же
формой, что и действующие потребители гарда; полнота утверждается ЗАМЕРОМ по страничному слою, а не
перечнем; пятый маршрут разведён ЯВНЫМ изъятием с основанием, числом и правилом, стерегущим живость
чужого гейта-владельца; устаревшее перечисление потребителей в докстринге снято с пометкой
опровергнутости и указанием двух преемников, а объявленная граница защиты не тронута; оба контроля
показали зубы на синтетическом исходнике; суита осталась зелёной, и это подтверждено прогоном, а не
выведено из докстринга.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 3: параметр кода исхода встаёт ПЕРЕД якорем — латентная ловушка закрывается до первого потребителя</name>

  <files>
app/pages/htmx.py
tests/test_pages/test_htmx_response_layer.py
  </files>

  <read_first>
- `app/pages/htmx.py:285-303` — `_with_notice(redirect, notice)` ЦЕЛИКОМ с докстрингом. ⚠️ ЧИТАТЬ
  ДОСЛОВНО: докстринг объявляет функцию «ЕДИНСТВЕННОЙ сборкой адреса с кодом исхода» и объясняет
  выбор разделителя наличием строки запроса; про якорь не сказано ничего, и это и есть предмет.
- `app/pages/htmx.py` — `_local_path` и `NOTICE_QUERY_KEY`: через что проходит собранный адрес и
  каким ключом ставится код. ⚠️ ПРОВЕРКА `_local_path` ОБЯЗАНА ОСТАТЬСЯ НА ВЫХОДЕ: адрес, собранный
  кодом, обязан выдерживать ту же проверку, что и адрес, объявленный константой, — это записано
  докстрингом и настоящей задачей не сдвигается.
- `app/pages/schedules.py:395-408` — `_editor_url`, ЕДИНСТВЕННЫЙ в дереве производитель адреса с
  якорем (`/ads/{id}/edit?sched=N#sched-N`). ⚠️ ПОСЛЕ ЗАДАЧИ 1 У НЕГО ДРУГАЯ СИГНАТУРА — читать
  после её приземления, а не до.
- `10-REVIEW.md:505-516` — `IN-03` целиком: почему адрес с якорем сегодня в сборку не попадает
  (`schedule_id=None` в `schedules_delete`) и что соберётся у первого же вызова с якорем.
- `app/pages/schedules.py:1065-1072` — комментарий, ссылающийся на `IN-03` ПРЕЖНЕГО круга ревизии.
  ⚠️ ИМЯ СОВПАДАЕТ, ПРЕДМЕТ ДРУГОЙ: прежний `IN-03` был об отложенной сборке фрагмента. Не принять
  одно за другое и не править чужой комментарий.
- `tests/test_pages/test_htmx_response_layer.py` — форма правил слоя ответа и имена, которыми модуль
  зовёт сборку адреса: новое правило встаёт рядом с существующими и наследует их форму.
  </read_first>

  <precondition>
Задача 1 приземлена: `_editor_url` уже принимает предикат возврата. Иначе правило, собирающее адрес
с якорем через живого производителя, пришлось бы писать под сигнатуру, которая через час изменится.
  </precondition>

  <behavior>
1. Адрес без строки запроса и без якоря получает код исхода первым параметром через `?`.
2. Адрес со строкой запроса и без якоря получает код исхода через `&` — прежнее поведение
   сохраняется дословно.
3. Адрес С ЯКОРЕМ получает код исхода ПЕРЕД якорем: якорь остаётся последней частью адреса, а код
   остаётся частью строки запроса и читается страницей приземления.
4. Адрес с якорем И строкой запроса собирается тем же правилом: разделитель выбирается по наличию
   строки запроса В ЧАСТИ ДО якоря, а не во всём адресе.
5. Собранный адрес по-прежнему проходит `_local_path`.
  </behavior>

  <action>
**(1) Править `_with_notice`.** Разделить входной адрес по первому символу якоря на путь-с-запросом
и якорь. Разделитель кода исхода выбирать по наличию `?` В ЧАСТИ ДО ЯКОРЯ. Собрать обратно в порядке
«часть до якоря + разделитель + ключ кода + значение + якорь». Адрес без якоря собирается ровно как
прежде — это отдельно проверяется правилом, чтобы правка не оказалась сменой поведения там, где
поведения менять не просили. Выход по-прежнему проходит `_local_path`.

**(2) Дописать докстринг.** Назвать ТРЕТЬЮ причину, которой у него не было: якорь не есть часть
строки запроса, и параметр, поставленный после него, уезжает ВНУТРЬ фрагмента — страница
приземления такого кода не видит и плашку не рисует. ⚠️ ПРЕЖНЮЮ ФОРМУЛИРОВКУ О ВЫБОРЕ РАЗДЕЛИТЕЛЯ
НЕ СТИРАТЬ: она верна и остаётся, к ней добавляется вторая ось разбора. Отдельной строкой записать,
что предмет закрыт ДО появления первого потребителя, и назвать источник (`IN-03`, ревизия
2026-09-04), — иначе следующий читатель примет правило за ответ на случившийся отказ и пойдёт искать
его в журнале.

**(3) Завести правила в `tests/test_pages/test_htmx_response_layer.py`.**
`test_the_outcome_code_is_placed_before_the_anchor` — таблица входов и ожидаемых выходов из ЧЕТЫРЁХ
случаев поведения выше, каждый со своим ожидаемым адресом, выписанным ПОСИМВОЛЬНО. Посимвольно
потому, что предмет здесь — ПОРЯДОК частей адреса, и правило, проверяющее вхождение подстроки,
зеленело бы на обоих порядках сразу.
`test_control_negative_an_anchor_unaware_assembly_puts_the_code_inside_the_fragment` — зубы: подать
ПРЕЖНЮЮ форму сборки (выражение, выбирающее разделитель по всему адресу) на адресе с якорем и
показать, что код уезжает внутрь фрагмента. ⚠️ ПРЕЖНЯЯ ФОРМА ЖИВЁТ ВНУТРИ КОНТРОЛЯ, а не в боевом
модуле: контроль обязан показать, ЧТО было бы, а не заставлять читателя это воображать.

**(4) Что НЕ трогается.** `_local_path`, реестр кодов исхода, `respond()` и его развилки, ни один
вызов сборки в боевых модулях: у сборки один вход и один выход, и порядок частей адреса внутри неё
— единственное, что меняется.

**(ПОСЛЕДНИЙ ШАГ ЗАДАЧИ) ОБНОВИТЬ ГРАФ ПРОЕКТА — `graphify update .`.** ⚠️ ЭТО ТРЕБОВАНИЕ
`./CLAUDE.md` ДОСЛОВНО: «After modifying code, run `graphify update .` to keep the graph current
(AST-only, no API cost)». Настоящий план правит файлы, которые граф индексирует, поэтому шаг
обязателен и стои́т ПОСЛЕДНИМ — после всех правок плана и после прогонов.

⚠️ **ПОЛЕ `graphify.enabled: false` В `.planning/config.json` ОСНОВАНИЕМ ДЛЯ ОТКАЗА НЕ ЯВЛЯЕТСЯ, И
ЭТО ЗАПИСАНО ПРЯМО, А НЕ ПРОПУЩЕНО МОЛЧА.** Замер планировщика 2026-09-05: конфигурация несёт
`"graphify": {"enabled": false}` — это ключ ИНТЕГРАЦИИ GSD, управляющий графом `.planning/graphs/`,
которого в дереве НЕТ вовсе (`ls -d .planning/graphs` → отсутствует). Граф ПРОЕКТА живёт в
`graphify-out/` (`ls -d graphify-out` → есть), команда доступна (`command -v graphify` → есть), и
свежести этого графа требует `./CLAUDE.md`, а не конфигурация GSD. ДВА РАЗНЫХ ГРАФА, ДВА РАЗНЫХ
ВЛАДЕЛЬЦА, и выключенный ключ одного не отменяет требования другого.

⚠️ **ПОБОЧНОГО ЭФФЕКТА НА ПРИЁМОЧНЫЕ КРИТЕРИИ ПАРТИИ НЕТ, И ЭТО ЗАМЕРЕНО:** `graphify-out/`
игнорируется гитом (`.gitignore:56`, `**/graphify-out/`) и деревом не отслеживается, поэтому шаг не
может тронуть ни один вывод `git diff`, на котором стоят гейты `<verify>` и приёмочные критерии
планов партии.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_notices_surface.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — сборка адреса приземления разошлась с правилами слоя ответа либо с поверхностью плашек</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_response_layer.py -q -p no:randomly -k "before_the_anchor or anchor_unaware"</automated>
    <fails_when>вывод содержит `no tests ran` либо `failed` — правило порядка частей адреса и его отрицательный контроль не заведены под названными именами либо не зелены</fails_when>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — правка сборки адреса задела потребителя, которого план не назвал</fails_when>
    <automated>graphify update .</automated>
    <fails_when>ненулевой код возврата либо вывод содержит `Traceback` — граф проекта не обновлён, и следующий читатель получит на вопрос о коде устаревший ответ. ⚠️ `command not found` — НАХОДКА ДЛЯ СВОДКИ (инструмент не установлен в среде исполнения), а не молчаливый пропуск шага регламента</fails_when>
  </verify>

  <acceptance_criteria>
- Четыре случая поведения проверены ПОСИМВОЛЬНО: в сводке приведена таблица «вход → ожидаемый
  выход» из правила, все четыре строки.
- Поведение адреса БЕЗ якоря не изменилось: в сводке приведены два случая (со строкой запроса и без
  неё) и показано, что ожидаемые адреса совпадают с прежними.
- Отрицательный контроль КРАСНЕЕТ на новой сборке и зеленеет на прежней: в сводке приведён вывод
  обоих прогонов.
- Докстринг дополнен, а не переписан: в сводке приведён дифф докстринга, и в нём нет удалённых строк
  прежнего обоснования.
- Источник расхождения назван в коде: в сводке процитирована строка, называющая `IN-03` ревизии
  2026-09-04 и то, что предмет закрыт до появления первого потребителя.
- Чужой комментарий о ПРЕЖНЕМ `IN-03` в `app/pages/schedules.py` не тронут:
  `git diff -- app/pages/schedules.py | grep -c 'IN-03'` → `0`.
  </acceptance_criteria>

  <reversibility rating="reversible">
Меняется порядок сборки строки внутри ОДНОЙ внутренней функции; сегодня у ветви с якорем нет ни
одного потребителя (замер ревизии), поэтому наблюдаемого поведения правка не меняет вовсе, а откат
равен обратной правке двух строк.
  </reversibility>

  <done>
Параметр кода исхода встаёт перед якорем; четыре случая сборки проверены посимвольно; отрицательный
контроль показывает, что делала прежняя форма; докстринг получил вторую ось разбора без стирания
первой; предмет закрыт до появления первого потребителя, и это записано.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Изменённые символы `app/pages/schedules.py`:** `_editor_url` (новая сигнатура — первым параметром
булев предикат возврата), `_editor_redirect` (вычисляет предикат и передаёт его), `schedules_delete`
(передаёт вычисленную локальную переменную; получает вызов гарда происхождения).

**Изменённые символы `app/pages/accounts.py`, `app/pages/ads.py`, `app/pages/account_groups.py`:**
`accounts_delete`, `ads_delete` и обработчик удаления группы аккаунта — каждый получает импорт
`is_same_origin` (где его ещё нет) и вызов гарда после проверки прав, до обращения к базе.

**Изменённый символ `app/pages/htmx.py`:** `_with_notice` — сборка ставит параметр кода исхода перед
якорем; разделитель выбирается по наличию строки запроса в части ДО якоря.

**Новый файл `tests/test_pages/test_origin_guard_on_destructive_routes.py`:**

| Символ | Что это |
|---|---|
| `DESTRUCTIVE_ROUTE_HANDLERS` | реестр «модуль страничного слоя → имена обработчиков удаления» |
| `DESTRUCTIVE_ROUTES_DECLARED` | их число с летописью по форме `OFFSET_CURSOR_EXCEPTIONS_DECLARED` |
| `DESTRUCTIVE_ROUTE_OWNERSHIP_EXEMPTIONS` | отображение «модуль страничного слоя → имя ГЕЙТА-ВЛАДЕЛЬЦА и его константы числа», с обоснованием у каждой записи; изъятие ПО МОДУЛЮ, а не по пути маршрута (ориентир: 1 — админский модуль, стережённый чужим гейтом ревизии Фазы 6) |
| `OWNERSHIP_EXEMPTIONS_DECLARED` | число изъятий с летописью той же формы: изъятие, выросшее молча, означает модуль, ушедший под чужого владельца без решения |
| `destructive_route_handlers(module_name, source)` | разбор `ast`: POST-маршруты с путём на `/delete`, исходник ТЕКСТОМ; модули изъятия исключаются ИЗ ВСЕЛЕННОЙ |
| `handlers_without_the_origin_guard(module_name, source)` | из них те, в чьём теле нет вызова гарда |
| `test_the_number_of_destructive_routes_is_declared` | ДВА равенства: число маршрутов замером по всему `app/pages/` равно объявленному общему, и число модулей изъятия равно `OWNERSHIP_EXEMPTIONS_DECLARED`; плюс утверждения непустоты обхода |
| `test_every_ownership_exemption_names_a_living_gate` | ИЗЪЯТИЕ НЕ ПЕРЕЖИВЁТ СВОЕГО ОСНОВАНИЯ: исходник названного гейта-владельца ЧИТАЕТСЯ, и имя правила вместе с именем его константы числа обязано в нём быть; переименованный либо снесённый гейт краснит прогон. Чужой модуль при этом ЧИТАЕТСЯ, а не правится |
| `test_every_destructive_route_checks_the_origin` | несущее правило полноты гарда |
| `test_control_a_route_without_the_guard_is_named` | зубы: синтетический маршрут без гарда назван |
| `test_control_an_undeclared_destructive_route_reddens_the_count` | зубы: незаявленный маршрут краснит счёт |

**Новые символы `tests/test_pages/test_htmx_response_layer.py`:**
`test_the_outcome_code_is_placed_before_the_anchor` (четыре случая, ожидания посимвольно),
`test_control_negative_an_anchor_unaware_assembly_puts_the_code_inside_the_fragment` (зубы на
прежней форме сборки, живущей внутри контроля).

**Новых миграций, новых зависимостей и правок `.planning/` план НЕ производит.**
</artifacts_this_phase_produces>

<threat_model>
⚠️ Таблица собрана на **ASVS 1** (`workflow.security_asvs_level` → `1`) с порогом блокировки
**`high`** (`workflow.security_block_on` → `"high"`) при `workflow.security_enforcement` → `true`.
Все три величины сняты `config-get`. Строка `*-SC` несёт `accept`, потому что предмета нет: пакетов
план не устанавливает, `pyproject.toml` не правит.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| сторонняя страница → маршрут удаления пользовательских данных | межсайтовая форма несёт cookie сама; серверного рубежа на четырёх маршрутах не было |
| форма запроса → две связанные развилки обработчика | два независимых чтения одного поля управляют веткой ответа и адресом приземления |
| собранный адрес приземления → страница приземления | параметр, уехавший внутрь фрагмента, страницей не читается, и признака отказа нет ни одного |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-18-01 | Spoofing | `accounts_delete`, `ads_delete`, `schedules_delete`, удаление группы аккаунта | medium | mitigate | Гард `is_same_origin` после проверки прав и до чтения данных (задача 2), форма отказа снята с трёх действующих потребителей. Severity `medium`, а не `high`: умолчание `samesite="lax"` межсайтовый POST у современных браузеров не пропускает, то есть рубеж был один, но был |
| T-10-18-02 | Tampering | полнота гарда на страничном слое | medium | mitigate | Полнота утверждается ЗАМЕРОМ по `app/pages/` (POST-маршруты с путём на `/delete`), число объявлено с летописью, и незаявленный маршрут краснит прогон. Перечень без замера был бы списком того, до чего дошли руки |
| T-10-18-03 | Information Disclosure | позиция вызова гарда относительно чтения данных | medium | mitigate | Гард ставится ПОСЛЕ проверки прав и ДО любого обращения к базе: отказ по происхождению не имеет права стать признаком существования строки. Позиция проверяется приёмочным критерием поимённо для каждого из четырёх |
| T-10-18-04 | Tampering | два независимых чтения признака возврата | medium | mitigate | Признак уезжает в сборку адреса ПАРАМЕТРОМ (задача 1), поэтому второе чтение поля становится невыразимым, а утверждение комментария — свойством сигнатуры |
| T-10-18-05 | Repudiation | плашка исхода на адресе с якорем | low | mitigate | Параметр кода исхода встаёт перед якорем (задача 3): исход перестаёт молча теряться внутри фрагмента. Предмет закрыт ДО появления первого потребителя, и это записано в докстринге |
| T-10-18-06 | Denial of Service | гейт полноты гарда | low | mitigate | Два контроля зубов на синтетическом исходнике плюс утверждения непустоты обхода: правило, зеленеющее вакуумом, отличается от работающего только в день настоящего пропуска |
| T-10-18-07 | Tampering | ИЗЪЯТИЕ админского модуля из нового реестра (двойное владение) | medium | mitigate | Изъятие объявлено ПО МОДУЛЮ, с числом, летописью и основанием (маршрут стережёт `test_every_mutating_admin_route_checks_the_origin` со своим объявленным числом, ревизия Фазы 6, `CR-02`), и САМО стережётся правилом `test_every_ownership_exemption_names_a_living_gate`: переименованный либо снесённый гейт-владелец краснит прогон. Без этого правила маршрут необратимого удаления пользователя оказался бы не стережём НИКЕМ при двух зелёных гейтах |
| T-10-18-08 | Repudiation | докстринг `is_same_origin` как источник числа потребителей | low | mitigate | Устаревшее перечисление снимается с пометкой опровергнутости (идиома D-30/D-32) и указанием ДВУХ действующих гейтов-преемников; присутствие преемника принуждается командой раздела `<verify>`. Объявленная ГРАНИЦА ЗАЩИТЫ не правится, и её сохранность принуждается отдельной командой того же раздела |
| T-10-18-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит. Таблица легитимности пакетов не требуется |
</threat_model>

<verification>
1. `uv run pytest tests/ -q -p no:randomly` — прогон целиком зелен. ⚠️ ЭТО НЕСУЩАЯ ПРОВЕРКА ЗАДАЧИ 2:
   расчёт цены гарда стои́т на объявленной границе (запрос без обоих заголовков пропускается), и
   подтверждается он прогоном, а не чтением докстринга.
2. `grep -c 'is_same_origin' app/pages/accounts.py app/pages/ads.py app/pages/schedules.py app/pages/account_groups.py`
   — у каждого файла не меньше `2` (импорт и вызов).
3. `sed -n '/^def _editor_url/,/^def /p' app/pages/schedules.py | grep -c 'form_data'` → `0`.
4. `uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py tests/test_pages/test_htmx_response_layer.py -q -p no:randomly`
   — зелено.
5. `git diff --name-only -- .planning/ tests/test_pages/test_admin_panel.py` — пусто: план не правит
   ни записи, ни чужого гейта.
6. `uv run python -m compileall -q app main.py tests` — сборка проходит.
</verification>

<success_criteria>
- Признак возврата в редактор читается ОДИН раз, и второе чтение в сборке адреса НЕВЫРАЗИМО.
- Четыре маршрута подтверждённого удаления пользовательских данных несут гард происхождения той же
  формой, что и три действующих потребителя; асимметрия, названная прецедентом `CR-02` Фазы 6,
  закрыта.
- Полнота гарда утверждается ЗАМЕРОМ, а не перечнем; число объявлено с летописью; оба контроля
  зубов показали свой красный.
- Цена правки для суиты ЗАМЕРЕНА прогоном, а не выведена из докстринга гарда.
- Параметр кода исхода встаёт перед якорем; четыре случая сборки проверены посимвольно.
- Все опровергнутые формулировки ПОМЕЧЕНЫ, а не стёрты, и у каждой назван источник расхождения.
- `.planning/` не тронут; требование `FORM-06` остаётся незавершённым.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-18-SUMMARY.md` по завершении.
</output>
