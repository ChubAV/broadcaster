---
phase: 10-rychag-components-modal-html
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/pages/schedules.py
  - tests/test_pages/test_confirm_delete_transport.py
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер. Калибровки у проекта нет
  # (`estimate-calibration`: sample_count 0, factor 1, min_samples 3), поэтому
  # tokens и raw_tokens совпадают, а confidence: low ВЫВЕДЕН ИЗ ЧИСЛА ЗАМЕРОВ
  # (ноль), а не из самооценки планировщика.
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "БЛОКЕР `CR-01` ЗАКРЫТ: прямой POST мимо браузера на маршрут подтверждённого удаления с негодной ВЕЛИЧИНОЙ поля `ad_id` больше не роняет обработчик пятисоткой. Записанный инвариант модуля правил самой фазы (T-02-24, T-02-25, `tests/test_pages/test_editor_schedules.py`) — «прямой POST мимо браузера даёт отказ валидации, а не 500» — снова верен, и это утверждается ПРАВИЛОМ, а не заявлением"
    - "Величина ограничена ТАМ, ГДЕ ДОКСТРИНГ УЖЕ УТВЕРЖДАЛ, ЧТО ГРАНИЦА ЕСТЬ: значение вне диапазона колонки отбрасывается ДО запроса и не доезжает до драйвера БД. Отбрасывание, а не отказ, — по той же причине, что названа у `_clean_ints` и `_expanded_from_form`: испорченное поле контекста не повод потерять само действие"
    - "Докстринг `_ad_id_from_form` СУЖЕН ДО ПРАВДИВОГО: коэрция ограничивает ТИП, а не ВЕЛИЧИНУ, и разница между ними и есть предмет `CR-01`. Прежняя формулировка НЕ ВЫЧЕРКНУТА — она помечена опровергнутой рядом с верной (идиома D-30/D-32 `.planning/STATE.md`, тот же приём, которым план 10-05 исправил перевёрнутую атрибуцию ветвей)"
    - "Негодная величина покрыта ОБХОДОМ, а не единичным правилом: случай встал в гейт транспорта рядом с правилом неотличимости повтора, параметризацией по ПЯТИ значениям, и утверждает не только «не 500», но и ФОРМУ ответа — одну из двух объявленных фазой, а не четвёртую"
    - "ИЗМЕРЕННОЕ ПОБОЧНОЕ СЛЕДСТВИЕ ГРАНИЦЫ НАЗВАНО, А НЕ ПРОГЛОЧЕНО: до правки значения `-1` и `0` отвечали 204 с адресом приземления `/ads/-1/edit` и `/ads/0/edit` — бессмысленный адрес, собранный из непроверенной величины; после правки оба приземляются на сводный список. Это ИЗМЕНЕНИЕ ПОВЕДЕНИЯ на двух из пяти значений, оно УТВЕРЖДАЕТСЯ правилом, а не обнаруживается следующим кругом"
    - "`WR-01` ЗАКРЫТ: развилка «фрагмент / переход» решается ТЕМ ЖЕ признаком, что и адрес приземления. Запрос с живым идентификатором строки, но без признака возврата, больше не приземляет два транспорта на РАЗНЫЕ экраны — путь деградации и путь htmx сходятся на одном адресе посимвольно"
    - "`WR-05` ЗАКРЫТ: перечень изъятий докстринга сборки фрагмента накрывает ТРЕТИЙ узел ответа — линейку счётчика, — а не два из трёх. Утверждение о безвредности снова УЖЕ поведения, а не шире его, и это стережёт правило, а не дисциплина читателя"
    - "Свойство неотличимости повтора (`T-10-01`, `T-10-07`), закрытое планом 10-06, НЕ СЛОМАНО ни границей величины, ни гейтом развилки: ответ на негодную величину совпадает с ответом ветки перехода, и по нему нельзя узнать, занят ли идентификатор чужой записью. Прогон правил повтора приводится в сводке ДО и ПОСЛЕ"
    - statement: "Поведение на PostgreSQL (`DataError` вне диапазона int32 вместо `OverflowError` SQLite) машинно этим планом не снимается: суита исполняется на `sqlite+aiosqlite:///:memory:`, и второго драйвера у неё нет. Граница выбрана по ВЕРХНЕЙ ГРАНИЦЕ КОЛОНКИ, то есть по величине, общей для обоих драйверов, — но подтверждение на боевом драйвере остаётся наблюдением, а не замером"
      verification: backstop
  artifacts:
    - "app/pages/schedules.py — модульная константа верхней границы идентификатора объявления и её основание (верхняя граница колонки), рядом с `RETURN_TO_EDITOR`"
    - "app/pages/schedules.py — `_ad_id_from_form` с границей величины и суженным докстрингом; прежняя формулировка помечена опровергнутой, а не вычеркнута"
    - "app/pages/schedules.py — `schedules_delete`: развилка формы ответа спрашивает признак возврата тем же чтением, которым его спрашивает адрес"
    - "app/pages/schedules.py — докстринг `_fragment`: перечень изъятий накрывает ТРИ узла ответа, включая линейку счётчика"
    - "tests/test_pages/test_confirm_delete_transport.py — `UNUSABLE_AD_ID_VALUES` и правило `test_an_unusable_ad_id_never_reaches_the_database`"
    - "tests/test_pages/test_confirm_delete_transport.py — правило `test_the_fragment_branch_needs_the_editor_flag` и его антивакуум"
    - "tests/test_pages/test_confirm_delete_transport.py — правило `test_the_counter_node_belongs_to_the_ad_named_by_the_request`"
  key_links:
    - "тело запроса → `_ad_id_from_form` → сравнение `Schedule.ad_id == ad_id`: единственная связь, по которой непроверенная величина доезжает до драйвера БД; ломается — HTTP 500 любому аутентифицированному пользователю на маршруте подтверждённого удаления, и это ровно тот отказ, который фаза себе запретила запиской модуля правил"
    - "`_ad_id_from_form` → `_editor_url`: та же величина собирает АДРЕС приземления; ломается — человек уезжает на `/ads/-1/edit`, адрес которого собран из его же испорченного поля"
    - "признак возврата в редактор → развилка формы ответа И адрес приземления ОДНОВРЕМЕННО: единственное, чем два транспорта не могут приземлиться на разные экраны; ломается — путь деградации уходит на сводный список, а путь htmx остаётся с фрагментом редактора, и ни один из двух не видит расхождения в одиночку"
    - "перечень изъятий докстринга `_fragment` → три узла ответа: единственное, чем запись остаётся УЖЕ поведения; ломается — читатель, поверивший перечислению, считает линейку счётчика безопасной, а она переписывается числом чужого объявления"
  prohibitions:
    - statement: "прежняя формулировка докстринга `_ad_id_from_form` НЕ вычёркивается и не переписывается задним числом: она названа опровергнутой рядом с верной, потому что вычеркнутая запись не оставляет следующему читателю причины, по которой аналогия с полем разворота была неверной"
      status: flagged-unverified
      verification: none
    - statement: "`_expanded_from_form` этим планом НЕ правится: его результат уезжает ТОЛЬКО в строку адреса и до сравнения в SQL не доходит ни на одном пути — перенос границы туда был бы правкой по аналогии, ровно той, которой `CR-01` и был порождён"
      status: flagged-unverified
      verification: none
    - statement: "граница величины НЕ вводится отказом обработчика (4xx/5xx на негодное поле): форма ответа маршрута объявлена фазой ДВУМЯ транспортами, и третья, различимая по величине поля, вернула бы карту занятых идентификаторов, которую свойство T-10-07 запрещает"
      status: flagged-unverified
      verification: none
    - statement: "перечень маршрутов подтверждения и оба его числа этим планом НЕ двигаются: девятый маршрут и его записи — предмет плана 10-11, и правка перечня здесь столкнула бы два плана на одном файле в одной волне"
      status: flagged-unverified
      verification: none
    - statement: "предсуществующие красные `test_the_overview_error_number_matches_the_users_own_dashboard` (окно 27, `DEF-09-01`) и `test_the_machine_readable_progress_is_derived_from_the_roadmap` (окно 9) не чинятся и не втягиваются; критерии приёмки от них не зависят"
      status: flagged-unverified
      verification: none
---

<objective>
Закрывается БЛОКЕР `CR-01` отчёта верификации: помощник `_ad_id_from_form`, заведённый ЭТОЙ
фазой, ограничивает ТИП поля и не ограничивает его ВЕЛИЧИНУ, а результат уезжает прямо в сравнение
`Schedule.ad_id == ad_id`. Вместе с ним закрываются два предупреждения ревизии на том же
обработчике: `WR-01` (развилка формы ответа решается другим предикатом, чем адрес приземления) и
`WR-05` (перечень изъятий докстринга накрывает два узла ответа из трёх).

Purpose: `10-VERIFICATION.md` несёт `status: gaps_found` и ЕДИНСТВЕННЫЙ `gaps[0].status: failed` —
это он. Дефект лежит на ветке, которую фаза СДЕЛАЛА нормальной (запасной источник контекста, когда
строки нет: путь повтора и чужого идентификатора), достижим ЛЮБЫМ аутентифицированным
пользователем и прямо опровергает инвариант, записанный докстрингом модуля правил самой фазы. В
проекте, где запись есть несущий контракт, и в фазе, которая первый круг верификации проиграла
ровно на расхождении записи с рантаймом, это блокер, а не предупреждение.

Output: граница величины в коэрции; суженный докстринг с помеченной прежней формулировкой; случай
негодной величины в гейте обоих транспортов; развилка, спрашивающая тот же признак, что и адрес,
с правилом и антивакуумом; перечень изъятий, накрывающий третий узел, с правилом, которое его
стережёт.

⚠️ ЭТОТ ПЛАН — ЗАКРЫТИЕ ГЕЙПОВ (`gap_closure: true`), ВОЛНА 1 НОВОЙ ПАРТИИ. Планы 10-01…10-07
исполнены и закоммичены: они не правятся, не перенумеровываются, не отменяются и не помечаются
`superseded`. Нумерация волн партии начинается заново с 1 — ровно так, как это уже сделала партия
10-05…10-07.

⚠️ `10-VALIDATION.md` У ФАЗЫ НЕТ ПО ЯВНОМУ ВЫБОРУ ОПЕРАТОРА: режим `--gaps` разведку не запускает,
и оператор выбрал продолжить без неё. Основание записано здесь одной строкой, чтобы у Dimension 8
проверки планов было основание, а не молчание.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Символы, которые СОЗДАЁТ этот план (проход source-grounding обязан считать их новыми, а не
дрейфом существующих имён):

- `_AD_ID_MAX` — модульная константа верхней границы идентификатора объявления,
  `app/pages/schedules.py`
- `UNUSABLE_AD_ID_VALUES` — перечень негодных значений поля контекста,
  `tests/test_pages/test_confirm_delete_transport.py`
- `test_an_unusable_ad_id_never_reaches_the_database` — правило границы величины, там же
- `test_the_fragment_branch_needs_the_editor_flag` — правило согласия развилки и адреса, там же
- `test_the_counter_node_belongs_to_the_ad_named_by_the_request` — правило, стерегущее перечень
  изъятий докстринга сборки фрагмента, там же
- `_seed_second_ad_with_schedules` — расстановка второго объявления того же владельца для
  кросс-объявленческого случая, там же

Символы, СУЩЕСТВУЮЩИЕ до плана и только ПРАВЯЩИЕСЯ (новыми не являются): `_ad_id_from_form`,
`_editor_url`, `_ad_has_a_schedule`, `_ad_schedule_count`, `schedules_delete`, `_fragment`,
`RETURN_TO_EDITOR` (`app/pages/schedules.py`); `_arrange_editor_schedule_delete`,
`_addressed_schedule_id`, `_expected_oob_ids`, `_oob_node_ids`, `_identify`, `HTMX_HEADERS`,
`DOCUMENT_MARK`, `_the_fragment_route` (`tests/test_pages/test_confirm_delete_transport.py`).
</artifacts_this_phase_produces>

<probe_edges>
## Рёбра зонда покрытия (`edge-probe.cjs` по тексту FORM-06) — ни одно не роняется молча

Зонд поднял ДВА ребра, оба в состоянии `unresolved`. Оба вносятся сюда ЯВНЫМИ ПОМЕЧЕННЫМИ
ДОПУЩЕНИЯМИ со ссылкой на закрывший их план — проверено чтением планов и сводок, а не принято со
слов оркестратора:

1. **`idempotency`** («What happens if this runs twice on the same input?») — РАЗРЕШЕНО планом
   **10-06**: правило `test_a_repeated_confirmed_delete_on_the_fragment_route_answers_the_same_shape`
   называет это ребро своим предметом дословно в собственном докстринге, а
   `test_a_repeated_confirmed_delete_is_indistinguishable_from_the_first` разносит его на маршруты
   перехода с перечнем изъятий `REPEAT_EXEMPT_ROUTES`. ⚠️ ЭТОТ ПЛАН ОБЯЗАН НЕ СЛОМАТЬ ЕГО: и
   граница величины, и гейт развилки стоя́т ровно на той ветке, которой неотличимость держится.
   Прогон правил повтора ДО и ПОСЛЕ — критерий приёмки задач 1 и 2.
2. **`concurrency`** («If interrupted or run in parallel, what is guaranteed?») — РАЗРЕШЕНО планом
   **10-07** (гейт ухода панели по признаку отправки: прерывание летящего запроса волей человека
   невозможно по построению) и серверной единицей записи `schedules_delete` (один `commit` одной
   строки под условием владения). Этот план ни того, ни другого не трогает.

Равенство без молчаливых пропусков: поднято зондом 2 — внесено допущениями 2.
</probe_edges>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: сквозной срез границы величины — тело запроса → коэрция → сравнение в SQL → форма ответа</name>

  <files>
tests/test_pages/test_confirm_delete_transport.py,
app/pages/schedules.py
  </files>

  <reversibility rating="reversible">
Граница отбрасывает значения, которые не могут принадлежать ни одной строке; снятие границы
возвращает ровно сегодняшнее наблюдаемое поведение и ничего, кроме него. Форма ответа маршрута,
раздаваемая 18 местам, не меняется — меняется величина, доезжающая до драйвера.
  </reversibility>

  <read_first>
- `app/pages/schedules.py:216-252` — `_ad_id_from_form` ЦЕЛИКОМ: докстринг (четыре абзаца, включая
  абзац об аналогии с полем разворота) и три строки коэрции. Читать ДО правки: сужается ИМЕННО
  этот докстринг, и прежняя формулировка обязана остаться в файле помеченной.
- `app/pages/schedules.py:190-213` — `_expanded_from_form` и его докстринг: аналогия, на которую
  ссылается помощник выше, и место, где она ВЕРНА. Читать, чтобы сузить аналогию, а не отменить
  её: результат этого помощника уезжает только в строку адреса и до сравнения в SQL не доходит.
- `app/pages/schedules.py:302-345` — `_ad_has_a_schedule` и `_ad_schedule_count`: обе функции
  подставляют величину в сравнение, обе ограничивают выборку связью `Ad.user_id`, и обе принимают
  `None` штатно (ранний возврат). Это и есть приёмник границы.
- `app/pages/schedules.py:930-1010` — `schedules_delete` ЦЕЛИКОМ: снятие `ad_id` с найденной
  строки, запасной источник из формы, сборка адреса, развилка, отложенная сборка фрагмента.
- `app/middleware.py:20-45` — обработчик, который превращает исключение обработчика в HTTP 500 с
  телом `{"detail": "Internal server error"}`. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: он и есть причина, по
  которой правило может утверждать КОД ОТВЕТА, а не ловить исключение — в суите запрос
  возвращается пятисоткой, а не роняет клиента.
- `tests/test_pages/test_confirm_delete_transport.py:1253-1410` — `_the_fragment_route`,
  `_addressed_schedule_id`, `_expected_oob_ids` и правило неотличимости повтора ЦЕЛИКОМ. Новое
  правило встаёт РЯДОМ и наследует его форму: антивакуум до, антивакуум после, отказ, называющий
  предмет.
- `tests/test_pages/test_confirm_delete_transport.py:643-656` — `_arrange_editor_schedule_delete`:
  расстановка сеет объявление, аккаунт и ДВА расписания (второе оставляет экран непустым, иначе
  маршрут ушёл бы в ветку перехода). Своей расстановки не заводится.
- `tests/test_pages/test_confirm_delete_transport.py:80-104` — `USER_EMAIL`, `HTMX_HEADERS`,
  `DOCUMENT_MARK` и записанное основание, по которому фикстура `htmx_client` в этом модуле НЕ
  запрашивается, а признак ставится в самом обходе.
- `tests/test_pages/test_editor_schedules.py` — модульный докстринг: инвариант T-02-24 / T-02-25
  («прямой POST мимо браузера обязан давать отказ валидации, а не 500») дословно. Это и есть
  нарушенная запись.
- `10-VERIFICATION.md` — `gaps[0]` ЦЕЛИКОМ (`truth`, `reason`, три записи `artifacts`, три пункта
  `missing`) и раздел «Adjudication: CR-01».
- `10-REVIEW.md` §CR-01 (строки 66-140) — независимый разбор с выписанной трассой и с названной
  причиной, по которой аналогия с полем разворота ломается.
  </read_first>

  <precondition>
Дерево на HEAD воспроизводит отказ. Проверяется ОДНИМ прогоном временного правила либо, что
дешевле, чтением уже снятого замера: планировщик измерил на HEAD пятью значениями поля
(`POST /schedules/999999/delete`, признак htmx, тело `return_to=editor&ad_id=<значение>`):
`9`×25 → **500** (тело 34 байта), `-1` → 204 с адресом `/ads/-1/edit`, `0` → 204 с `/ads/0/edit`,
`1e3` → 204 с `/schedules`, `1_0` → 204 с `/ads/10/edit`. Если первый прогон нового правила даёт
иное распределение — состояние дерева разошлось с замером, и это ОСТАНОВ: расхождение записывается
окном, а не подгоняется правилом.
  </precondition>

  <behavior>
СРЕЗ ВЕРТИКАЛЬНЫЙ И ПРОИЗВОДСТВЕННЫЙ: тело запроса → коэрция поля контекста → сравнение в SQL →
форма ответа, — и делает это одним путём (маршрут удаления расписания), без правки соседних
помощников и без второй ветки.

1. `test_an_unusable_ad_id_never_reaches_the_database` — параметризация по `UNUSABLE_AD_ID_VALUES`
   (`"9" * 25`, `"-1"`, `"0"`, `"1e3"`, `"1_0"` — перечень взят из `gaps[0].missing` дословно).
   На каждом значении шлётся ПРЯМОЙ POST мимо браузера на адрес НЕСУЩЕСТВУЮЩЕЙ строки с признаком
   htmx и телом, несущим признак возврата и негодное поле. Три утверждения на случай:
   - код ответа НЕ равен 500 — дословный предмет гейпа;
   - форма ответа есть ОДНА ИЗ ДВУХ объявленных фазой (204 с заголовком перехода либо 200 с
     фрагментом без целого документа), а не третья и не четвёртая;
   - в теле нет метки целого документа.
2. Отдельное утверждение на ВЕЛИЧИНУ, а не на форму: значение, заведомо выходящее за диапазон
   колонки, приземляется на СВОДНЫЙ СПИСОК, а не на адрес, собранный из него же. Именно это
   отличает «ошибку проглотили» от «величину отбросили»: адрес `/ads/<25 девяток>/edit` был бы
   вторым дефектом того же класса.
3. АНТИВАКУУМ: тот же запрос с ГОДНЫМ значением поля (идентификатор объявления из расстановки)
   отвечает ТОЙ ЖЕ формой, что и до правки, и адрес приземления несёт этот идентификатор. Без
   этого утверждения правило зеленело бы и на помощнике, отбрасывающем ВСЁ.
4. Правило ИЗМЕРЯЕТ ИЗМЕНЕНИЕ ПОВЕДЕНИЯ, а не прячет его: для значений `-1` и `0` до правки адрес
   приземления собирался ИЗ НИХ (`/ads/-1/edit`, `/ads/0/edit`), после — сводный список. Оба
   значения входят в параметризацию, и ожидаемый адрес у них ПОСЛЕ правки утверждается явно.

ПЕРЕХОД ЦВЕТА ОБЯЗАН БЫТЬ НАБЛЮДЁН И ЗАПИСАН SHA-АМИ: правило ПЕРВЫМ коммитом на непрáвленом
обработчике (RED — ожидается `1 failed, 4 passed` с сообщением `assert 500 != 500`), граница
величины и суженный докстринг — ВТОРЫМ (GREEN).
  </behavior>

  <action>
**1. Перечень негодных значений и правило (`tests/test_pages/test_confirm_delete_transport.py`).**

`UNUSABLE_AD_ID_VALUES` — модульный кортеж из пяти строк, взятых из `gaps[0].missing` дословно:
двадцать пять девяток, минус единица, ноль, запись с показателем степени и запись с подчёркиванием
внутри числа. Перечень несёт комментарий, называющий, ЧТО именно каждое значение ломает: первое —
ВЕЛИЧИНУ (доезжает до драйвера), второе и третье — ДОМЕН (в колонке автоинкремента таких
идентификаторов не бывает, но адрес приземления до правки собирался из них), четвёртое —
ФОРМУ записи (коэрция отбрасывает его уже сегодня), пятое — ЛОВУШКУ ЧИТАТЕЛЯ: подчёркивание внутри
числа коэрция принимает, и значение доезжает до запроса ГОДНЫМ.

Правило пишется по составу `<behavior>` и встаёт РЯДОМ с правилом неотличимости повтора, в том же
разделе файла. Расстановка берётся действующая (`_arrange_editor_schedule_delete`), личность
ставится действующим помощником; адрес — НЕСУЩЕСТВУЮЩАЯ строка, потому что предмет правила есть
ЗАПАСНОЙ источник контекста, а он вступает в дело только там, где строки нет. Тело формы шлётся
СЫРОЙ строкой с заголовком типа содержимого формы, а не отображением: значение `9`×25 в отображении
пришлось бы приводить к строке дважды, и первое приведение спрятало бы предмет.

Отказ каждого утверждения приводит ЗНАЧЕНИЕ ПОЛЯ, код ответа и адрес приземления — иначе
покрасневшая параметризация не говорит, какое из пяти значений её уронило.

**2. Граница величины (`app/pages/schedules.py`).**

Заводится модульная константа `_AD_ID_MAX` со значением верхней границы знакового
тридцатидвухбитного целого (`2_147_483_647`) рядом с `RETURN_TO_EDITOR`. Её комментарий называет
ОСНОВАНИЕ, а не число: это верхняя граница колонки идентификатора, то есть величина, вне которой
значение не может принадлежать НИ ОДНОЙ строке ни на одном из двух драйверов проекта; поэтому оно
отбрасывается ДО запроса, а не роняет запрос драйвером.

`_ad_id_from_form` после коэрции возвращает `None` для значений меньше единицы и больше границы.
Нижняя граница — единица, а не ноль: идентификаторы автоинкремента начинаются с единицы, и ноль с
отрицательными в домене поля не лежат. Отбрасывание, а не отказ обработчика, — по основанию,
записанному у `_clean_ints` и повторённому здесь: испорченное поле КОНТЕКСТА не повод потерять
само действие, а третья форма ответа, различимая по величине поля, вернула бы карту занятых
идентификаторов, запрещённую свойством T-10-07.

**3. Сужение докстринга.**

Абзац об аналогии с помощником разворота ПОМЕЧАЕТСЯ ОПРОВЕРГНУТЫМ и остаётся в файле; рядом
встаёт верная формулировка. Она обязана назвать ЧЕТЫРЕ вещи: (а) коэрция ограничивает ТИП, а не
ВЕЛИЧИНУ; (б) аналогия ломается ровно там, где результат перестаёт быть частью строки адреса и
становится операндом сравнения в SQL; (в) граница взята по верхней границе колонки и стоит ДО
запроса; (г) предмет назван поимённо — `CR-01` отчёта верификации, воспроизведённый пятисоткой на
маршруте подтверждённого удаления.

⚠️ Вычёркивать прежнюю формулировку ЗАПРЕЩЕНО: вычеркнутая запись не оставляет следующему читателю
причины, по которой аналогия была неверной, и он повторит её на следующем поле, взятом из формы.
⚠️ `_expanded_from_form` не правится ни на символ — его результат уезжает только в строку адреса.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "unusable_ad_id"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо число прошедших меньше пяти (параметризация недобрала значений)</fails_when>
  </verify>

  <acceptance_criteria>
- **ПЕРЕХОД ЦВЕТА НАБЛЮДЁН, А НЕ ОБЕЩАН.** В сводке записаны два SHA: `RED_SHA` — коммит с
  правилом и БЕЗ правки обработчика, с дословным выводом
  `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "unusable_ad_id"`
  и кодом прогона РОВНО `1`; `GREEN_SHA` — следующий коммит с границей величины и зелёным прогоном
  того же фильтра.
- Красный прогон приведён ДОСЛОВНО и совпал с замером планировщика: `1 failed, 4 passed`, отказ
  несёт `assert 500 != 500` на значении из двадцати пяти девяток. Иное распределение красного —
  ОСТАНОВ и запись окном, а не подгонка правила.
- В сводке приведена таблица «значение → код и адрес приземления ДО / ПОСЛЕ» на все пять значений.
  Ожидается ПОСЛЕ: `9`×25 → 204 и сводный список; `-1` → 204 и сводный список (ДО было
  `/ads/-1/edit`); `0` → 204 и сводный список (ДО было `/ads/0/edit`); `1e3` → 204 и сводный список
  (не изменилось); `1_0` → 204 и `/ads/10/edit` (не изменилось — значение годное).
- Изменение поведения на двух значениях НАЗВАНО в сводке отдельной строкой, а не спрятано в
  таблице: адрес, собранный из непроверенной величины, перестал собираться.
- Прежняя формулировка докстринга ОСТАЛАСЬ в файле и помечена:
  `uv run python -c "import pathlib,sys; s=pathlib.Path('app/pages/schedules.py').read_text(encoding='utf-8'); sys.exit(0 if 'ОПРОВЕРГНУТО' in s and '_expanded_from_form' in s and '_AD_ID_MAX' in s else 'нет пометки опровержения либо константы границы')"`
  — код возврата `0`.
- Правила неотличимости повтора (ребро идемпотентности, план 10-06) НЕ СДВИНУТЫ:
  `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "repeat or repeated or sameness"`
  — зелено, и число прошедших равно тому же числу, снятому ДО правки (снять и записать оба вывода).
- Инвариант модуля правил редактора держится:
  `uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly` — зелено.
- База прогона файла обхода: ДО плана `41 passed` (измерено планировщиком). После задачи число
  названо в сводке, и прибавка объяснена числом заведённых случаев.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Негодная ВЕЛИЧИНА поля контекста отбрасывается до запроса; прямой POST мимо браузера отвечает одной
из двух объявленных фазой форм, а не пятисоткой; адрес приземления больше не собирается из
непроверенной величины; докстринг сужен до правдивого, а прежняя формулировка помечена, а не
вычеркнута. Переход цвета записан двумя SHA, красный прогон приведён дословно.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: развилка формы ответа спрашивает ТОТ ЖЕ признак, что и адрес приземления (WR-01)</name>

  <files>
tests/test_pages/test_confirm_delete_transport.py,
app/pages/schedules.py
  </files>

  <reversibility rating="costly">
Развилка формы ответа наследуется Фазой 11 как образец для пятнадцати обработчиков класса A: она
прямо названа роадмапом как та, что «возвращает фрагмент по `id` конкретной строки». Разошедшиеся
предикат ветвления и предикат адреса, оставленные образцом, размножились бы на пятнадцать мест
раньше, чем расхождение стало бы видно хоть одному из двух транспортов.
  </reversibility>

  <read_first>
- `app/pages/schedules.py:930-1010` ПОСЛЕ задачи 1 — обработчик целиком: сборка адреса
  (`screen_url`), развилка (`_ad_has_a_schedule`), докстринг защитной ветки с абзацем о её
  недостижимости из интерфейса, отложенная сборка фрагмента.
- `app/pages/schedules.py:254-284` — `_editor_url`: единственное место, где читается признак
  возврата, и записанное основание «два независимо собранных адреса разошлись бы молча». Читать
  дословно: задача переносит ТО ЖЕ основание на ветвление.
- `app/pages/schedules.py:35` — `RETURN_TO_EDITOR`: значение признака берётся отсюда, второй
  литерал в обработчике был бы вторым источником того же решения.
- `10-REVIEW.md` §WR-01 (строки 143-215) — разбор с выписанным замером: запрос с живым
  идентификатором строки без признака возврата даёт 302 на сводный список на пути деградации и 200
  с фрагментом РЕДАКТОРА на пути htmx, а три внеполосных узла ответа на сводном списке целей не
  находят.
- `tests/test_pages/test_confirm_delete_transport.py:1022-1107` —
  `test_every_confirmed_delete_route_answers_both_transports`: форма, которой обход утверждает ОБЕ
  половины пары, включая посимвольное сличение адреса и отсутствие целого документа. Новое правило
  наследует её утверждения, а не изобретает свои.
- `tests/test_pages/test_confirm_delete_transport.py:1253-1305` — `_the_fragment_route`,
  `_addressed_schedule_id`, `_expected_oob_ids`.
- `app/templates/schedules/` — убедиться ЧТЕНИЕМ, а не памятью: форм удаления в разделе нет, то
  есть ветка, которую правило делает наблюдаемой, из интерфейса недостижима и сегодня.
  </read_first>

  <precondition>
Задача 1 приземлена: `grep -c '_AD_ID_MAX' app/pages/schedules.py` возвращает не менее `2`, и
`uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "unusable_ad_id"`
зелено. Сегодняшнее поведение ветки, которую правит задача (измерено планировщиком на HEAD): POST с
ЖИВЫМ идентификатором строки и ПУСТЫМ телом, с признаком htmx → **200** и фрагмент редактора с
узлами `sched-N`, `sched-del-N` и линейкой счётчика; тот же путь без признака htmx → **302** на
сводный список.
  </precondition>

  <behavior>
1. `test_the_fragment_branch_needs_the_editor_flag` — несущее правило. POST на ЖИВУЮ строку с
   ПУСТЫМ телом (признака возврата нет), признак htmx есть. Вердикт ПОСЛЕ правки: 204 и заголовок
   перехода на сводный список; целого документа в теле нет; внеполосных узлов в ответе нет ВОВСЕ.
   ДО правки то же обращение даёт 200 и фрагмент — это и есть наблюдаемый переход цвета.
2. ВТОРАЯ ПОЛОВИНА ПАРЫ В ТОМ ЖЕ ПРАВИЛЕ: то же обращение БЕЗ признака htmx отвечает 302, и адрес
   перенаправления сличается с адресом заголовка перехода ПОСИМВОЛЬНО. Предмет правила — не «код
   204», а СХОЖДЕНИЕ ДВУХ ТРАНСПОРТОВ НА ОДНОМ ЭКРАНЕ; утверждение об одном коде зеленело бы и
   при двух разных адресах.
3. АНТИВАКУУМ: то же обращение С признаком возврата и годным полем контекста по-прежнему даёт 200 и
   ТРИ внеполосных узла, названных действующим помощником ожидаемых идентификаторов. Без него
   правило зеленело бы на обработчике, у которого фрагментной ветки не осталось вовсе, — то есть на
   удалении предмета вместо его починки.
4. Правило не заводит своей расстановки и своего разбора внеполосных узлов: и то, и другое в файле
   есть, и второй экземпляр разошёлся бы с первым молча.
  </behavior>

  <action>
**1. Правило.** Пишется по составу `<behavior>`, встаёт рядом с правилом неотличимости повтора, в
разделе фрагментного маршрута. Идентификатор живой строки берётся действующим помощником чтения
идентификатора из адреса расстановки. Отказ приводит код, адрес и множество внеполосных
идентификаторов — все три величины, потому что расхождение может прийти любой из них.

**2. Правка развилки (`app/pages/schedules.py`, обработчик `schedules_delete`).**

Признак возврата в редактор читается ОДИН раз, в локальную величину, и участвует В ОБОИХ решениях:
и в сборке адреса (через `_editor_url`, как сегодня), и в выборе формы ответа. Ветка фрагмента
требует ОБОИХ условий: признак возврата пришёл И у объявления осталась хоть одна строка расписания.
Порядок проверки — сначала признак, потом запрос к базе: запрос, чей результат заведомо не влияет
на исход, выполнять незачем (то же основание, которым отложена сборка фрагмента, IN-03).

Литерал значения признака НЕ ВЫПИСЫВАЕТСЯ второй раз — берётся модульная константа, та же, которую
читает сборщик адреса.

**3. Правка докстринга защитной ветки.**

Абзац о недостижимости ветки из интерфейса ОСТАЁТСЯ (он верен и измерен), но к нему добавляется
названная причина правки: страж ветки есть ПРЕДИКАТ, а не инвентарь шаблонов, и до этой правки
предикат ветвления спрашивал не то, что спрашивает адрес. Записывается ИЗМЕРЕННОЕ следствие
прежнего расхождения: три внеполосных узла ответа целей на сводном списке не находят (в разметке
сводного списка линейка счётчика печатается другим узлом), то есть путь htmx получал ответ, который
некуда приземлить, а удалённая строка оставалась на экране.

⚠️ Ветка «список опустел» (D-04) НЕ ТРОГАЕТСЯ: опустевший редактор по-прежнему закрывается
переходом, и второго экземпляра пустого состояния не заводится.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "editor_flag or fragment_branch"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке</fails_when>
  </verify>

  <acceptance_criteria>
- **ПЕРЕХОД ЦВЕТА НАБЛЮДЁН.** В сводке два SHA: `RED_SHA` (правило на непрáвленом обработчике, код
  прогона ровно `1`, отказ приводит `200` там, где ожидалось `204`) и `GREEN_SHA` (правка развилки,
  зелено).
- В сводке приведены ОБА адреса дословно — заголовок перехода на пути htmx и адрес перенаправления
  на пути деградации — и названо, что они совпали ПОСИМВОЛЬНО.
- Антивакуум показан числом: в сводке приведено множество внеполосных идентификаторов ответа с
  признаком возврата (ожидается три узла) и без него (ожидается пустое множество).
- Обход обоих транспортов всех маршрутов не сдвинут:
  `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly` — зелено;
  число прошедших названо и сопоставлено с числом после задачи 1.
- Правила неотличимости повтора остаются зелёными (ребро идемпотентности не сломано):
  `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "repeat or repeated or sameness"`
  — зелено.
- Редактор объявления не сдвинут:
  `uv run pytest tests/test_pages/test_editor_schedules.py tests/test_pages/test_htmx_gates.py -q -p no:randomly`
  — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Развилка формы ответа и адрес приземления читают ОДИН признак; запрос без признака возврата
приземляет оба транспорта на один экран, и это утверждается посимвольным сличением адресов;
антивакуум доказывает, что фрагментная ветка не удалена, а загейтирована.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 3: перечень изъятий докстринга накрывает ТРЕТИЙ узел ответа, и его стережёт правило (WR-05)</name>

  <files>
tests/test_pages/test_confirm_delete_transport.py,
app/pages/schedules.py
  </files>

  <read_first>
- `app/pages/schedules.py:964-1006` ПОСЛЕ задачи 2 — докстринг `_fragment` ЦЕЛИКОМ, особенно абзац
  «И УТВЕРЖДЕНИЕ О БЕЗВРЕДНОСТИ СУЖЕНО ДО ПРАВДИВОГО»: он перечисляет ДВА узла (`#sched-N` и
  `#sched-del-N`) и заканчивается принципом, который сам же нарушает — «дефект был бы в том, чтобы
  утверждать прозой ШИРЕ поведения».
- `app/templates/ads/partials/sched_delete_response.html` ЦЕЛИКОМ (73 строки) — три узла ответа и
  записанное основание, по которому третий подменяет СОДЕРЖИМОЕ линейки, а не сам узел.
- `app/pages/schedules.py:329-345` — `_ad_schedule_count`: число считается для объявления,
  ВЛАДЕЮЩЕГО удалённой строкой, а цель третьего узла — статический селектор, то есть та линейка,
  которая есть в живом документе.
- `10-REVIEW.md` §WR-05 (строки 320-357) — разбор с выписанной формулировкой замены.
- `tests/test_pages/test_confirm_delete_transport.py:1288-1302` — `_expected_oob_ids` и записанное
  основание, по которому третий идентификатор есть СТАТИЧЕСКИЙ селектор.
- `tests/test_pages/test_confirm_delete_transport.py:363-380` — `_seed_ad`, `_arrange_ad_delete`:
  форма посева объявления, которую наследует расстановка второго объявления.
  </read_first>

  <behavior>
1. `test_the_counter_node_belongs_to_the_ad_named_by_the_request` — правило, превращающее прозу в
   ИЗМЕРЕННЫЙ факт. У ОДНОГО владельца два объявления: A с двумя расписаниями и B с тремя.
   Посылается удаление строки объявления B с признаком возврата и полем контекста, называющим B.
   Вердикт: ответ несёт линейку счётчика с числом расписаний ОБЪЯВЛЕНИЯ B (после удаления — два), а
   не A; цель третьего узла — статический селектор, то есть в документе, открытом на объявлении A,
   этот узел приземлится в ЕГО линейку.
2. АНТИВАКУУМ ЧИСЛОМ: числа расписаний у A и B РАЗНЫЕ по построению расстановки, и правило это
   утверждает ДО отправки запроса. Равные числа сделали бы вердикт неотличимым от вердикта на
   правильном объявлении, и правило зеленело бы независимо от предмета.
3. Правило утверждает ФАКТ, а не осуждает его: кросс-объявленческое удаление границ привилегий не
   пересекает (оба объявления принадлежат одному владельцу) и из интерфейса после задачи 2
   недостижимо. Предмет правила — соответствие ЗАПИСИ поведению, и отказ обязан говорить именно
   это.
  </behavior>

  <action>
**1. Расстановка второго объявления (`_seed_second_ad_with_schedules`).**

Помощник сеет ВТОРОЕ объявление того же владельца и заданное число расписаний к нему, пользуясь
действующими помощниками посева объявления, аккаунта и расписания. Своих моделей и своего посева не
заводится. Возвращает объявление и идентификатор одной из его строк — обе величины нужны правилу.

**2. Правило.** Пишется по составу `<behavior>`. Число расписаний читается из ответа разбором
разметки линейки, а не подсчётом подстрок: линейка печатает число СЛОВОМ И ЦИФРОЙ через правило
склонения (`ads/includes/sched_count_rule.html`), и сличение по подстроке зеленело бы на «12»,
найденной внутри «12 расписаний» другого объявления. Достаточное и честное утверждение — сличение
ЦЕЛОГО текста линейки с текстом, отрисованным тем же шаблоном на ожидаемом числе.

**3. Расширение перечня изъятий (`app/pages/schedules.py`, докстринг `_fragment`).**

Абзац о безвредности расширяется с ДВУХ узлов до ТРЁХ. Третий назван и его вред назван измеренно:
линейка счётчика адресована статическим селектором, число считается для объявления, ВЛАДЕЮЩЕГО
удалённой строкой, поэтому в документе, открытом на другом объявлении того же владельца, встанет
чужое число и переживёт запрос до перезагрузки.

К перечню добавляется СТРУКТУРНОЕ СУЖЕНИЕ, ставшее верным задачей 2: собрать такое обращение из
интерфейса нельзя вовсе — признак возврата и поле контекста несут ОБЕ формы пути удаления, и
развилка теперь требует признака возврата; остаётся только собранное руками тело запроса. Прежняя
формулировка перечня НЕ вычёркивается: она была верна в части двух узлов и стала УЖЕ поведения не
ошибкой, а неполнотой.

⚠️ Разметка ответа (`ads/partials/sched_delete_response.html`) НЕ ПРАВИТСЯ: предмет `WR-05` —
запись, а не поведение, и третий узел стоит там по решению D-12 Фазы 9, перенесённому дословно.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "counter_node"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке</fails_when>
  </verify>

  <acceptance_criteria>
- Правило зелено, и его вердикт приведён в сводке ЧИСЛАМИ: число расписаний объявления A до и после
  запроса, число объявления B до и после, текст линейки в ответе дословно.
- Антивакуум показан: числа расписаний двух объявлений РАЗНЫЕ, и это утверждение приведено в сводке
  отдельно от несущего.
- ⚠️ ПРАВИЛО ЗЕЛЕНО ПЕРВЫМ ЖЕ ПРОГОНОМ, И ЭТО ОЖИДАЕМО: оно утверждает СЕГОДНЯШНЕЕ поведение, а
  предмет задачи — запись. Зубы ему даёт мутация, а не переход цвета: в сводке приведён вывод
  правила на мутации обработчика (счёт по объявлению, взятому из найденной строки, заменён на счёт
  по полю формы) — правило обязано покраснеть. Мутация снимается, дерево восстанавливается,
  `git diff --stat -- app/pages/schedules.py` после снятия приведён пустым.
- Перечень изъятий накрывает три узла:
  `uv run python -c "import pathlib,sys; s=pathlib.Path('app/pages/schedules.py').read_text(encoding='utf-8'); need=['sched-count','ТРЕТИЙ','#sched-del-']; miss=[n for n in need if n not in s]; sys.exit('нет в докстринге: '+repr(miss) if miss else 0)"`
  — код возврата `0`.
- Полный прогон файла обхода зелен, число прошедших названо:
  `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly`.
- `uv run pytest tests/ -q -p no:randomly` — зелено за вычетом ДВУХ известных предсуществующих
  красных, названных поимённо: `test_the_overview_error_number_matches_the_users_own_dashboard`
  (окно 27) и `test_the_machine_readable_progress_is_derived_from_the_roadmap` (окно 9). Ни один из
  них этим планом не чинится, и ни один критерий приёмки от них не зависит.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Перечень изъятий докстринга называет ТРИ узла ответа и измеренный вред третьего; утверждение о
безвредности снова уже поведения; правило стережёт запись и доказало зубы мутацией, а не переходом
цвета; структурное сужение, ставшее верным задачей 2, записано рядом.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| тело запроса → драйвер БД | величина из формы становится операндом сравнения в SQL и уезжает в драйвер без границы |
| тело запроса → адрес приземления | та же величина собирает адрес, на который уезжает человек |
| признак возврата → форма ответа И адрес | два решения об одном экране, принятые разными предикатами, расходятся молча |
| экран объявления A → внеполосные узлы ответа об объявлении B | цель узла разрешает рантайм в ЖИВОМ документе, а число считает сервер по другому объявлению |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-44 | Denial of Service | `_ad_id_from_form` → `_ad_has_a_schedule` → драйвер БД | high | mitigate | Верхняя и нижняя границы величины в коэрции, ДО запроса; значение вне домена колонки отбрасывается и даёт ветку перехода. Утверждается параметризацией по пяти значениям в гейте обоих транспортов, с наблюдённым переходом цвета (`1 failed, 4 passed`, `assert 500 != 500`) |
| T-10-45 | Tampering | адрес приземления, собранный из непроверенной величины | medium | mitigate | Отброшенная величина не доезжает до сборщика адреса: приземление уходит на сводный список вместо `/ads/<величина>/edit`. Изменение поведения на двух значениях утверждается правилом и названо в сводке отдельной строкой |
| T-10-46 | Information Disclosure | различимость форм ответа как карта занятых идентификаторов (T-10-01 / T-10-07) | high | mitigate | Ответ на негодную величину совпадает с ответом ветки перехода — третьей формы не заводится. Правила неотличимости повтора (план 10-06, ребро идемпотентности) прогоняются ДО и ПОСЛЕ, оба вывода в сводке |
| T-10-47 | Spoofing | два транспорта одного действия, приземляющиеся на разные экраны | medium | mitigate | Развилка формы ответа читает ТОТ ЖЕ признак, что и сборщик адреса, и читает его ОДИН раз; правило сличает адрес заголовка перехода с адресом перенаправления посимвольно, а антивакуум доказывает, что фрагментная ветка не удалена |
| T-10-48 | Repudiation | запись о безвредности ШИРЕ поведения | medium | mitigate | Перечень изъятий расширен до трёх узлов; правило кросс-объявленческого счёта делает третий узел измеренным фактом, а не прозой, и доказывает зубы мутацией обработчика |
| T-10-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не выполняет ни одной установки: ни новой зависимости, ни build-шага, ни нового вендоренного файла. Разведки у фазы нет по решению роадмапа — предмета аудита легитимности пакетов не существует |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_editor_schedules.py tests/test_pages/test_htmx_gates.py -q -p no:randomly` — зелено.
- `uv run pytest tests/ -q -p no:randomly` — зелено за вычетом двух известных предсуществующих
  красных (окна 27 и 9), названных поимённо.
- `grep -c '_AD_ID_MAX' app/pages/schedules.py` — не менее `2` (объявление и применение).
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- Числа-базы, снятые планировщиком на дереве ДО плана и подлежащие сличению в сводке:
  `tests/test_pages/test_confirm_delete_transport.py` → `41 passed`;
  `tests/test_templates/test_components.py` → `76 passed` (этот план его не трогает и не двигает).
- `graphify update .` — из рабочего дерева исполнителя не выполняется по тому же основанию, что у
  планов 10-01…10-04 (записи 23 и 25 `.planning/WINDOWS.md`); передать оркестратору.
- Поведение на боевом драйвере (`DataError` вместо `OverflowError`) остаётся наблюдением:
  `verification: backstop` в `must_haves`, машинного эквивалента в суите нет.
</verification>

<success_criteria>
1. Блокер `CR-01` закрыт: негодная ВЕЛИЧИНА поля контекста не доезжает до драйвера БД, и прямой
   POST мимо браузера отвечает одной из двух объявленных фазой форм, а не пятисоткой.
2. Граница стои́т ТАМ, где докстринг уже утверждал её наличие, и взята по верхней границе колонки —
   то есть по величине, общей для обоих драйверов проекта.
3. Докстринг сужен до правдивого; прежняя формулировка помечена опровергнутой и не вычеркнута.
4. Случай негодной величины стои́т в обходе обоих транспортов параметризацией по пяти значениям и
   утверждает ФОРМУ ответа, а не только отсутствие пятисотки.
5. Изменение поведения на значениях `-1` и `0` (адрес приземления перестал собираться из
   непроверенной величины) утверждается правилом и названо в сводке, а не обнаруживается следующим
   кругом.
6. `WR-01` закрыт: развилка формы ответа и адрес приземления читают один признак, и два транспорта
   сходятся на одном экране посимвольно.
7. `WR-05` закрыт: перечень изъятий накрывает три узла ответа, третий узел стережёт правило,
   доказавшее зубы мутацией.
8. Ребро идемпотентности (план 10-06) не сломано ни границей, ни гейтом развилки — прогон правил
   повтора приведён ДО и ПОСЛЕ.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-08-SUMMARY.md` when done.

Сводка обязана:
- привести `RED_SHA` и `GREEN_SHA` перехода цвета задач 1 и 2 вместе с кодом прогона на красном и
  дословным текстом отказа;
- привести таблицу «значение → код и адрес приземления ДО / ПОСЛЕ» на все пять значений и назвать
  ОТДЕЛЬНОЙ СТРОКОЙ изменение поведения на `-1` и `0`;
- привести оба адреса задачи 2 дословно и подтвердить их посимвольное совпадение;
- привести множества внеполосных идентификаторов с признаком возврата и без него;
- привести вердикт правила третьего узла ЧИСЛАМИ и вывод его МУТАЦИИ (правило обязано было
  покраснеть), а также подтвердить пустой `git diff --stat` после снятия мутации;
- привести прогон правил неотличимости повтора ДО и ПОСЛЕ и назвать, что ребро идемпотентности
  плана 10-06 не сломано;
- назвать НЕЗАКРЫТЫМИ и передать дальше: критерий 2 фазы (снятие осиротевшей панели — предмет
  плана 10-10) и шесть пунктов `human_verification` отчёта верификации;
- назвать поимённо два предсуществующих красных (окна 27 и 9) и подтвердить, что ни один из них не
  чинился и ни один критерий приёмки от них не зависел;
- передать оркестратору невыполненный `graphify update .` с основанием записей 23 и 25
  `.planning/WINDOWS.md`.
</output>
