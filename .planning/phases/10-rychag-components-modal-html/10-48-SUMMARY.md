---
phase: 10-rychag-components-modal-html
plan: 48
subsystem: testing
tags: [uat, pytest, sqlalchemy, ast, schema-constraint, planning-records]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "артефакт обхода 10-UAT.md с § «Как привести стенд в нужное состояние» (план 10-10), запись гэпа G-10-A3 (обход walkthrough_3), ограничение схемы ck_schedules_active_requires_next_run (коммит d46780b)"
provides:
  - "Исполнимая на сегодняшней схеме команда посева стенда обхода: next_run_at вычисляется compute_next_run_at"
  - "Правило tests/test_planning/test_the_walkthrough_stand_is_seedable.py — две половины (поведенческая вставка в настоящую схему + статическое покрытие полей ограничений) и отрицательный контроль с двумя доктóриваниями"
  - "Запись D-30/D-32 в § «Развилка посева»: источник расхождения назван поимённо, прежние строки не вычеркнуты"
affects: [обход Фазы 10, закрытие гэпа G-10-A3, будущие записи проекта с исполнимым текстом]

actuals:
  tokens: 9529
  tasks: 3
  commits: 3
plan_head_before: 354e9de3bb59b2b02810bb532598a823bad9b1f1

tech-stack:
  added: []
  patterns:
    - "Правило записи ИСПОЛНЯЕТ текст артефакта — но РОВНО ОДИН узел дерева разбора (тело цикла), с явно перечисленным в докстринге пространством имён и ничем сверх него"
    - "Две половины одного правила с НЕПЕРЕСЕКАЮЩИМИСЯ предметами: поведенческая ловит нарушение сегодняшними значениями, статическая — умолчание о поле, добавленном схеме завтра"
    - "Имена колонок ДОБЫВАЮТСЯ из Schedule.__table__.constraints сличением с table.columns, а не выписываются литералами: правило не согласится с любой правкой модели"

key-files:
  created:
    - tests/test_planning/test_the_walkthrough_stand_is_seedable.py
  modified:
    - .planning/phases/10-rychag-components-modal-html/10-UAT.md

key-decisions:
  - "D-48-1: статическая половина изымает из множества полей колонки, значение которых пишет САМА БАЗА (автонумерованный первичный ключ). Без изъятия правило требовало бы подать `id` ключом вызова — требование невозможного, красное всегда. Признак изъятия ДОБЫВАЕТСЯ у колонки (`primary_key` + `autoincrement`), а не выписан именем"
  - "D-48-2: имена колонок, названных CheckConstraint-ом, добываются СЛИЧЕНИЕМ текста условия со списком колонок таблицы. Основание — ЗАМЕР: у CheckConstraint, заданного СТРОКОЙ условия, коллекция `.columns` ПУСТА (SQLAlchemy 2.0, замер 2026-09-11)"
  - "D-48-3: команда посева вычисляет список времён, набор дней и часовой пояс ВНУТРИ тела цикла, а не над ним. Это не стиль, а исполнимость: правило исполняет РОВНО узел цикла, и имя, вычисленное над циклом, уронило бы его отказом о НЕИЗВЕСТНОМ ИМЕНИ — отказом ПРИБОРА, выданным за отказ предмета"
  - "D-48-4: команда посева исполнителем НЕ ЗАПУСКАЛАСЬ. Три основания запрета, записанные самим артефактом (одна база у дерева и прода, автономность плана, разрешение неунаследуемо), действуют дословно. Годность показана ПЕРЕХОДОМ ЦВЕТА правила на схеме тестовой базы"

patterns-established:
  - "Дефект артефакта закрывается ОБЕИМИ половинами: правкой текста И стерегущим правилом. G-10-A1 и G-10-A2 закрыты только первой — настоящий гэп показал цену того приёма"
  - "Отрицательный контроль с ДВУМЯ доктóриваниями, второе из которых отделяет «поле названо» от «строка законна»"

requirements-completed: [FORM-06]

coverage:
  - id: D1
    description: "Команда посева § «Как привести стенд в нужное состояние» исполнима на сегодняшней схеме — показано ВСТАВКОЙ построенных ею строк в настоящую схему тестовой базы, а не чтением текста"
    requirement: FORM-06
    verification:
      - kind: integration
        ref: "tests/test_planning/test_the_walkthrough_stand_is_seedable.py#test_the_seeded_row_satisfies_todays_schema"
        status: pass
    human_judgment: false
  - id: D2
    description: "Расхождение артефакта со схемой перестало быть невидимым: правило краснеет, когда команда посева строит строку, которую схема отвергает — и краснеет за прогон, а не следующим обходом"
    requirement: FORM-06
    verification:
      - kind: integration
        ref: "tests/test_planning/test_the_walkthrough_stand_is_seedable.py#test_control_a_seed_program_without_the_next_run_reddens"
        status: pass
    human_judgment: false
  - id: D3
    description: "Статическая половина знает не только сегодняшнее ограничение: ограничение, добавленное схеме завтра на неназванное командой поле, краснит её"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_planning/test_the_walkthrough_stand_is_seedable.py#test_every_checked_column_is_named_by_the_seed_program"
        status: pass
      - kind: unit
        ref: "tests/test_planning/test_the_walkthrough_stand_is_seedable.py#test_the_seed_program_parses_and_builds_exactly_one_schedule"
        status: pass
    human_judgment: false
  - id: D4
    description: "Три числа, связанные test_the_walkthrough_cannot_self_certify.py, не сдвинуты ни одно: checks_declared 6, разделов «Проверка N» 6, таблиц отметок 6; заголовков «### N.» в разделе «ТРИ ПУНКТА» — 3"
    verification:
      - kind: other
        ref: "uv run python -c \"...\" → напечатано `1 6 6 3` (замер ДО правки и ПОСЛЕ правки — оба раза)"
        status: pass
      - kind: unit
        ref: "tests/test_planning/test_the_walkthrough_cannot_self_certify.py (весь модуль, 40 passed в каталоге)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Поле `status` артефакта обхода осталось нетерминальным: подписи человека на приёмке не появилось, и план её не поставил ни одним символом"
    verification:
      - kind: other
        ref: "git diff -U0 10-UAT.md | grep -E '^[+-]status:' → пусто (строка не тронута)"
        status: pass
      - kind: unit
        ref: "tests/test_planning/test_the_walkthrough_cannot_self_certify.py"
        status: pass
    human_judgment: false
  - id: D6
    description: "Продукт не тронут ни на символ: app/ этим планом не правился вовсе"
    verification:
      - kind: other
        ref: "git diff --numstat -- app → пусто на всех трёх задачах"
        status: pass
    human_judgment: false

duration: 52 min
completed: 2026-09-11
status: complete
---

# Фаза 10 План 48: исполнимость команды посева стенда обхода — Сводка

**Команда посева артефакта обхода снова заводит стенд (момент следующего запуска вычисляется `compute_next_run_at`), и расхождение команды со схемой с этого дня краснит ПРОГОН за секунду, а не третий обход на боевой базе: правило исполняет цикл артефакта на настоящей схеме тестовой базы и держит вторую, статическую половину против ограничений, которых схема ещё не завела.**

## Performance

- **Duration:** 52 мин (3163 с)
- **Started:** 2026-09-11T13:09:51Z
- **Completed:** 2026-09-11T14:02:34Z
- **Tasks:** 3 из 3
- **Files modified:** 2 (1 создан, 1 изменён)

⚠️ Из 52 минут **40 занял ОДИН прогон полной суиты** (`uv run pytest tests/ -q` → 39:46). Это замер, а не оценка, и он передаётся следующему кругу: полный прогон в этом проекте стоит сорок минут, и план, требующий его дважды, стои́т полутора часов сам по себе.

## Accomplishments

- **G-10-A3 закрыт ОБЕИМИ половинами, а не одной.** Текст команды правлен И заведено стерегущее правило. G-10-A1 и G-10-A2 закрывались ПРАВКОЙ ТЕКСТА без правила — настоящий гэп показал цену того приёма: расхождение прожило от `d46780b` до третьего обхода невидимым.
- **Переход цвета ЗАМЕРЕН, а не обещан.** До правки: `1 failed`, `CHECK constraint failed: ck_schedules_active_requires_next_run`. После: `1 passed`. Обе команды и оба кода возврата приведены ниже.
- **У правила две половины с названными НЕПЕРЕСЕКАЮЩИМИСЯ предметами** и отрицательный контроль с двумя доктóриваниями, второе из которых отделяет «поле названо» от «строка законна».
- **Прежние записи артефакта стоя́т дословно.** Дословный вывод прогона посева, раздел расхождения Р-2, три основания запрета на запуск, блоки `walkthrough`/`walkthrough_2`/`walkthrough_3` — ни одна строка не вычеркнута и не переписана (идиома D-30/D-32).

## Task Commits

1. **Задача 1: правило исполнимости — RED на нетронутом артефакте** — `333e02c` (test)
2. **Задача 2: команда посева вычисляет момент следующего запуска — GREEN** — `7776580` (feat)
3. **Задача 3: зубы правила — покрытие полей ограничений и отрицательный контроль** — `44d4b0d` (test)

**Plan metadata:** см. следующий коммит `docs(10-48)`.

## TDD Gate Compliance

| Гейт | Коммит | Состояние |
|------|--------|-----------|
| RED | `333e02c` `test(10-48)` | ✅ правило заведено и КРАСНО на нетронутом артефакте |
| GREEN | `7776580` `feat(10-48)` | ✅ правка артефакта делает правило ЗЕЛЁНЫМ |
| REFACTOR | `44d4b0d` `test(10-48)` | ✅ зубы правила (вторая половина + контроль); тесты зелены |

Последовательность RED → GREEN соблюдена: `test(10-48)` предшествует `feat(10-48)` в истории ветки.

### RED-запись, проверенная прибором (не обещанием)

`gsd-tools check tdd-red-evidence` → **`RED_EVIDENCE_OK`** (`reason: target_test_failed`). Запись целиком:

```json
{
  "command": "uv run pytest tests/test_planning/test_the_walkthrough_stand_is_seedable.py -q -k \"todays_schema\"",
  "exit_code": 1,
  "failing_test": "test_the_seeded_row_satisfies_todays_schema",
  "target_test": "test_the_seeded_row_satisfies_todays_schema",
  "expected": "строки, построенные телом цикла посева артефакта 10-UAT.md, вставляются в настоящую схему тестовой базы без отказа",
  "actual": "sqlite3.IntegrityError: CHECK constraint failed: ck_schedules_active_requires_next_run — команда посева строит is_active=True при next_run_at=None",
  "verdict": "RED_EVIDENCE_OK",
  "reason": "target_test_failed"
}
```

⚠️ **Прибор читает TAP, а суита говорит на pytest, и переходник назван, а не умолчан.** Вывод прогона переведён в TAP из `--junitxml` ТОГО ЖЕ прогона (файлы в каталоге черновиков, не в дереве) — то есть переведён ЗАМЕР, а не сочинён. Код возврата взят у настоящего `pytest` (`1`), а не назначен.

## Переход цвета — оба состояния с командами и кодами возврата

| Состояние | Команда | Итоговая строка | rc |
|---|---|---|---|
| **RED** (артефакт нетронут, коммит `333e02c`) | `uv run pytest …test_the_walkthrough_stand_is_seedable.py -q -k "todays_schema"` | `1 failed, 1 deselected` + `FAILED …::test_the_seeded_row_satisfies_todays_schema` + `CHECK constraint failed: ck_schedules_active_requires_next_run` | **1** |
| **GREEN** (после правки, коммит `7776580`) | та же команда | `1 passed, 1 deselected, 1 warning in 0.12s` | **0** |

Прочие прогоны:

| Что | Итоговая строка | rc |
|---|---|---|
| `uv run pytest tests/test_planning/test_the_walkthrough_stand_is_seedable.py -q` | `4 passed, 1 warning in 0.29s` | 0 |
| `uv run pytest tests/test_planning/ -q` | `40 passed, 1 warning in 0.49s` | 0 |
| **`uv run pytest tests/ -q` (ПОЛНАЯ СУИТА)** | **`3167 passed, 974 warnings in 2386.75s (0:39:46)`** | **0** |
| `git diff --numstat -- app` | пусто (все три задачи) | 0 |
| `git diff --numstat -- app tests` (задача 2) | пусто | 0 |
| `git status --porcelain …/10-UAT.md` после прогона контроля | пусто — контроль тронул КОПИЮ В ПАМЯТИ, а не файл | 0 |

## Три связанных числа — переснято, а не обещано

Команда: `uv run python -c "import pathlib,re; …"`.

| Замер | `checks_declared: 6` | разделов «Проверка N» | таблиц отметок | «### N.» в «ТРИ ПУНКТА» |
|---|---|---|---|---|
| **ДО правки** | 1 | 6 | 6 | 3 |
| **ПОСЛЕ правки** | 1 | 6 | 6 | 3 |

Ни одно не сдвинуто. Поле `status` шапки — `partial`, не тронуто ни на символ (`git diff -U0 … \| grep '^[+-]status:'` → пусто).

## Отрицательный контроль — оба доктóривания с объявленным ожиданием

Копия текста артефакта готовится **В ПАМЯТИ**: разбирается программа, правится дерево, печатается обратно `ast.unparse`, подставляется в копию строки. Настоящий файл не правится ни на символ — показано пустым `git status --porcelain` по нему.

| Доктóривание | Статическая половина | Поведенческая половина | Ожидание выписано |
|---|---|---|---|
| **(а)** ключ `next_run_at` изъят из вызова целиком | **КРАСНА** — `uncovered == {"next_run_at"}` | **КРАСНА** — отказ базы несёт `ck_schedules_active_requires_next_run` | литералом |
| **(б)** ключ назван, значением стои́т пустое | **ЗЕЛЕНА** — `uncovered == frozenset()` | **КРАСНА** — отказ базы тот же | литералом |

⚠️ **Второе доктóривание не избыточно, и это ровно та дыра, из-за которой статическая половина в одиночку была бы негодна:** починка, НАЗЫВАЮЩАЯ поле и оставляющая его пустым, прошла бы статический гейт и уронила бы следующий обход ТЕМ ЖЕ отказом, на котором остановился `walkthrough_3`.

## Замер, определивший форму статической половины

| Ограничение таблицы `schedules` | `.columns` | Откуда взяты имена |
|---|---|---|
| `ForeignKeyConstraint` (×2) | `['ad_id']`, `['account_id']` | из коллекции как есть |
| `PrimaryKeyConstraint` | `['id']` | **изъято** — значение пишет сама база |
| `CheckConstraint ck_schedules_active_requires_next_run` | **ПУСТА** | сличением текста условия со списком колонок таблицы |

Итоговое множество полей ограничений сегодня: `['account_id', 'ad_id', 'is_active', 'next_run_at']`. Ключи вызова `Schedule(...)` артефакта: `['account_id', 'ad_id', 'days_of_week', 'group_ids', 'is_active', 'next_run_at', 'times_of_day', 'timezone']`. Непокрытых — **ноль**.

## Files Created/Modified

- `tests/test_planning/test_the_walkthrough_stand_is_seedable.py` (**создан**, 490 строк) — правило: разбор раздела и программы по ФОРМЕ, разбор программы ДЕРЕВОМ, исполнение РОВНО узла цикла на настоящей схеме, статическое покрытие полей ограничений, отрицательный контроль. Несёт `pytestmark = pytest.mark.planning`.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md` (**изменён**, +52/−2) — команда посева § «Как привести стенд», приписка D-30/D-32 к ⚠️ абзацу о наборе полей, приписка D-30/D-32 в § «Развилка посева», приписка к `workaround_used` гэпа `G-10-A3`.

## Decisions Made

См. `key-decisions` шапки (D-48-1 … D-48-4). Коротко:

1. **Изъятие колонок, которые пишет база** — иначе статическая половина требовала бы невозможного.
2. **Имена колонок CheckConstraint-а добываются сличением** — потому что `.columns` у строкового условия пуста (ЗАМЕР).
3. **Все читаемые телом цикла имена вычисляются ВНУТРИ тела** — иначе правило падает отказом ПРИБОРА, неотличимым для читателя от отказа предмета.
4. **Команда посева не запускалась** — три основания запрета артефакта действуют дословно.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocker] Строка импорта `Schedule` встречается в артефакте ТРИЖДЫ, а не однажды**

- **Found during:** Задача 2
- **Issue:** Правка планировалась подстановкой по строке импорта. Строка `from app.models.schedule import Schedule` живёт в ТРЁХ командах артефакта — посева, уборки и счёта остатков. Глобальная подстановка добавила бы импорт вычислителя во все три, включая те, что расписаний не строят.
- **Fix:** Подстановка выполняется ТОЛЬКО внутри раздела посева (границы — заголовок раздела и следующий заголовок того же уровня), а утверждение `count == 1` внутри раздела стои́т ДО подстановки. Промах пойман утверждением, а не наблюдением в дальнейшем.
- **Files modified:** `.planning/phases/10-rychag-components-modal-html/10-UAT.md`
- **Verification:** `compute_next_run_at` в разделе посева — ровно дважды (импорт + вызов), в программе — ровно дважды; в двух других командах артефакта — ноль раз.
- **Committed in:** `7776580`

**2. [Rule 1 - Bug] `TypeError: Boolean value of this clause is not defined` в статической половине**

- **Found during:** Задача 3
- **Issue:** Выражение `str(getattr(constraint, "sqltext", "") or "")` вызывает `__bool__` у объекта условия SQLAlchemy, которому истинность НЕ ОПРЕДЕЛЕНА. Оба правила задачи 3 падали отказом ПРИБОРА.
- **Fix:** Явная проверка `is None` вместо `or`.
- **Files modified:** `tests/test_planning/test_the_walkthrough_stand_is_seedable.py`
- **Verification:** `uv run pytest …test_the_walkthrough_stand_is_seedable.py -q` → `4 passed`.
- **Committed in:** `44d4b0d`

**3. [Rule 2 - Missing critical] Изъятие колонок, которые пишет сама база**

- **Found during:** Задача 3
- **Issue:** План велит собрать «имена колонок, упомянутых ограничениями таблицы модели». Буквальное исполнение включает `PrimaryKeyConstraint`, то есть требует подать `id` ключом вызова `Schedule(...)`. Писатель этого сделать не может — ключ назначает база, — и правило было бы КРАСНО ВСЕГДА, то есть негодно.
- **Fix:** Из множества изымаются колонки с `primary_key` и `autoincrement`. Признак ДОБЫВАЕТСЯ у колонки, а не выписан именем. Основание записано докстрингом рядом.
- **Files modified:** `tests/test_planning/test_the_walkthrough_stand_is_seedable.py`
- **Verification:** множество сегодня — `['account_id', 'ad_id', 'is_active', 'next_run_at']`, непокрытых ноль; контроль (а) краснеет ровно на `next_run_at`.
- **Committed in:** `44d4b0d`

**4. [Rule 2 - Missing critical] Приписка к ⚠️ абзацу о наборе полей ВНУТРИ раздела посева**

- **Found during:** Задача 2
- **Issue:** Абзац «Значения полей расписания взяты у `_seed_schedule` … пустой список групп, один день недели, одно время, часовой пояс `UTC`» ПЕРЕЧИСЛЯЕТ набор полей. После правки набор пополнился моментом следующего запуска, и абзац читался бы ЛОЖНО — тот же класс расхождения записи с деревом, который план и закрывает.
- **Fix:** Рядом с прежним абзацем (дословно нетронутым) встала приписка D-30/D-32: момент вычисляется, источник вычислителя назван, названо и стерегущее правило. План помещал приписку в § «Развилка посева»; она там есть — настоящая приписка ДОПОЛНИТЕЛЬНАЯ, в месте, которое иначе врало бы.
- **Files modified:** `.planning/phases/10-rychag-components-modal-html/10-UAT.md`
- **Verification:** прежний абзац присутствует дословно; три связанных числа не сдвинуты (`1 6 6 3`).
- **Committed in:** `7776580`

**5. [Rule 3 - Blocker] Формулировка приписки переписана, чтобы критерий приёмки остался однозначным**

- **Found during:** Задача 2
- **Issue:** Критерий задачи 2 требует, чтобы «в разделе посева вычислитель момента был назван РОВНО ОДИН РАЗ в импорте и РОВНО ОДИН РАЗ в вызове». Первая редакция приписки называла `compute_next_run_at` ещё и прозой — счёт по разделу давал три, и критерий становился спорным.
- **Fix:** Проза говорит «вычислитель службы расписаний (`app/services/schedule_service.py`)» без повтора имени. Счёт по разделу — 2 (импорт + вызов).
- **Files modified:** `.planning/phases/10-rychag-components-modal-html/10-UAT.md`
- **Verification:** импорт — 1, вызов — 1, замерено разбором самого правила.
- **Committed in:** `7776580`

---

**Total deviations:** 5 auto-fixed (Rule 1 — 1, Rule 2 — 2, Rule 3 — 2)
**Impact on plan:** Ни одна правка не расширила предмет. Две (№1, №5) — точность исполнения плана; две (№3, №4) — корректность, без которой правило либо всегда красно, либо артефакт врал бы; одна (№2) — отказ прибора. Продукт не тронут ни одной.

## Issues Encountered

**Полный прогон суиты стои́т 40 минут.** Это не отказ и не дефект — это ЦЕНА, и она замерена: `3167 passed, 974 warnings in 2386.75s (0:39:46)`. Прогонов полной суиты в этом плане был ОДИН (задача 3), и это сознательно: задачи 1 и 2 обходились каталогом `tests/test_planning/` (0.5 с), потому что их предмет — правило записи, а полный прогон нужен затем, чтобы увидеть, не задела ли правка артефакта соседние правила ТОГО ЖЕ каталога.

## Граница покрытия — что этот план НЕ закрывает

⚠️ **Настоящее правило стережёт РОВНО ОДНУ КОМАНДУ РОВНО ОДНОГО АРТЕФАКТА.** Артефакт обхода Фазы 10 — не единственная запись проекта, несущая ИСПОЛНИМЫЙ ТЕКСТ по живым моделям: команда уборки и команда счёта остатков того же артефакта строят движок и читают `Schedule`, `Ad`, `MessengerAccount`; артефакты обходов прошлых фаз несут свои. **Их не стережёт никто.** Класс дефекта, которым был `G-10-A3` (дерево ушло вперёд записи молча), закрыт для одной команды и открыт для всех остальных. Это передаётся следующему кругу прямо, а не выдаётся за общее решение.

⚠️ **Правило НЕ доказывает, что команда отработает НА БОЕВОЙ БАЗЕ.** Схема тестовой базы создаётся из моделей (`Base.metadata.create_all`), боевая живёт ревизиями Alembic. Совпадение двух половин держат `tests/test_models/test_schedule_active_requires_next_run.py` и `tests/test_migrations/test_0022_schedules_active_requires_next_run.py` — они названы в докстринге, а не подразумеваются.

⚠️ **Хрупкость разбора принята сознательно.** Правило привязано к ФОРМЕ раздела (заголовок, блок в тройных кавычках, вызов интерпретатора с `-c`). Перестройка раздела уронит его ОТКАЗОМ О РАЗБОРЕ. Альтернатива — правило, выбирающее блок молча, — вернула бы ровно тот класс, который гэп и есть.

## Known Stubs

Нет. Заглушек, пропущенных правил (`skip`/`xfail`) и непрогнанных команд `<verify>` план не оставил: все команды верификации всех трёх задач исполнены, итоговые строки приведены выше числами.

## User Setup Required

Нет — внешних служб план не трогает.

## Next Phase Readiness

- **Гэп `G-10-A3` закрыт.** Следующий обход Фазы 10 начинается с ИСПОЛНИМОГО посева; на том падении он больше не упрётся, а расхождение команды со схемой в будущем краснит прогон за 0.3 с.
- **Открытым остаётся ПУНКТ 3 перечня «ТРИ ПУНКТА» — ПОДПИСЬ ЧЕЛОВЕКА НА ПРИЁМКЕ.** Настоящий план её не ставит и поставить не может: поле `status` шапки остаётся `partial`.
- **Открытыми остаются прочие гэпы фазы** (`G-10-5`, `G-10-6`, `G-10-7` и др.) — настоящий план их не касался ни строкой.

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-11*

## Self-Check: PASSED

- `tests/test_planning/test_the_walkthrough_stand_is_seedable.py` — FOUND на диске
- `.planning/phases/10-rychag-components-modal-html/10-48-SUMMARY.md` — FOUND на диске
- Коммиты `333e02c`, `7776580`, `44d4b0d` — FOUND в истории ветки
- `uv run pytest tests/test_planning/ -q` после правки STATE.md и ROADMAP.md → `42 passed`, rc=0

### Трекинг: четвёртое срабатывание правила согласия прозы с отметками

⚠️ **ПРАВИЛО СРАБОТАЛО НА ЭТОМ ПЛАНЕ, И ЭТО ЗАПИСЫВАЕТСЯ, А НЕ УМАЛЧИВАЕТСЯ.** После
`roadmap update-plan-progress 10` правило `test_the_prose_plan_counts_agree_with_the_marks`
покраснело дословно: «проза строки `**Plans**` говорит 47 (исполнено планов), а ОТМЕТОК перечня —
48». Проза `ROADMAP.md` приведена к отметкам (47 → 48), запись перехода встала РЯДОМ с прежними
(идиома D-30/D-32), прежнее «47» не вычеркнуто.

⚠️ **`state advance-plan` записал «Plan: 2 of 51» вместо «48 of 51»** — верб ведёт свой внутренний
индекс, а не сквозную нумерацию планов фазы, и заодно затёр прозу блока `Current Position`. Число и
проза выправлены рукой; это ЧЕТВЁРТЫЙ раз, когда расхождение вносит ИНСТРУМЕНТ трекинга, а не автор
записи.

⚠️ **Требование `FORM-06` НЕ отмечено выполненным.** `requirements.ready-ids` вернул `0/1`: тем же
требованием владеют планы 10-49…10-51, у которых сводок ещё нет. Гейт разделяемого идентификатора
сработал как задуман — отметка встанет, когда закончится ПОСЛЕДНИЙ план партии.
