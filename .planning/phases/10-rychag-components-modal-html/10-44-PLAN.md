---
phase: 10-rychag-components-modal-html
plan: 44
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`: предмет плана — ЗАМЕР по документам записи и РЕШЕНИЕ
# ВЛАДЕЛЬЦА. Кода, для которого выразимо «ожидаемый выход при заданном входе», план не производит ни
# строки; дисциплина RED→GREEN здесь была бы формой без предмета. Это тот случай, который
# `gsd-core/references/tdd.md` называет прямо: конфигурация и записи идут стандартным планом.
wave: 2
# ⚠️ ВОЛНА 2 — ТОЛЬКО ТРАССИРУЮЩИЙ ПОРЯДОК. Пересечения по файлам с планами 10-42, 10-43 и 10-47 нет
# ни одного: настоящий план правит два артефакта каталога фазы и ни строки суиты или продукта.
# ⚠️ СЦЕПКА С ПЛАНОМ 10-47 (оба — писатели в один каталог фазы, оба читают его командой
# `git status --porcelain`) объявлена порядконезависимой ЗАМЕРОМ и записана пометкой
# `coupling_justified` во фронтматере плана 10-47; ребра между парой намеренно нет.
depends_on: ["10-41"]
gap_closure: true
gap_ids: [V9-05]
files_modified:
  - .planning/phases/10-rychag-components-modal-html/10-SECURITY-SUBJECT.md
  - .planning/phases/10-rychag-components-modal-html/10-SECURITY.md
autonomous: false
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (`.planning/config.json:48-50`): `security_asvs_level` → `1`,
# `security_block_on` → `"high"`, `security_enforcement` → `true`. ИМЕННО ОНИ И ЕСТЬ ПРЕДМЕТ ЭТОГО
# ПЛАНА: принуждение включено, а артефакта фазы нет.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3).
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ОТСУТСТВИЕ АРТЕФАКТА БЕЗОПАСНОСТИ ПРИ ВКЛЮЧЁННОМ ПРИНУЖДЕНИИ ПЕРЕСТАЁТ БЫТЬ НЕЗАМЕЧЕННЫМ: девять кругов верификации его не назвали, и после настоящего плана состояние читается машинно"
    - "ПРЕДМЕТ РЕШЕНИЯ ПРЕДЪЯВЛЕН ЗАМЕРОМ, А НЕ ОЦЕНКОЙ: реестры угроз всех сорока исполненных планов сведены в один перечень числами — сколько строк, сколько уникальных имён, как распределены тяжесть и распоряжение"
    - "БЛОКИРУЮЩЕЕ МНОЖЕСТВО НАЗВАНО ПОИМЁННО: двенадцать строк тяжести `high` несут распоряжение `accept` при пороге блокировки `high`, и все двенадцать — строка цепи поставок; перечень предъявлен владельцу целиком"
    - "РЕШЕНИЕ ПРИНИМАЕТ ВЛАДЕЛЕЦ, А НЕ ИСПОЛНИТЕЛЬ: выбор между принуждением регламента и изъятием с основанием есть решение о том, чем фаза закрывается, и внутри задачи он не делается ни одной ветвью"
    - "ИСПОЛНЕННЫЕ ПЛАНЫ НЕ ПРАВЯТСЯ НИ ОДНОЙ ВЕТВЬЮ: двенадцать строк несут тяжесть, разошедшуюся с более поздней идиомой фазы, и правка исполненного плана задним числом переписала бы историю"
    - "ЛЮБАЯ ИЗ ДВУХ ВЕТВЕЙ ЗАПИСЫВАЕТСЯ ОДИНАКОВО ЧЕСТНО: обе несут имя принявшего, дату, основание и ссылку на замер, и ни одна не оставляет вопрос открытым молча"
    - "ВЕРДИКТ ВЕРИФИКАЦИИ И ОТМЕТКА ТРЕБОВАНИЯ НЕ ДВИГАЮТСЯ: артефакт безопасности не есть верификация фазы, и запись о нём ничего не объявляет пройденным"
  artifacts:
    - "10-SECURITY-SUBJECT.md — сведённый реестр угроз сорока исполненных планов: числа строк, уникальных имён, распределение тяжести и распоряжения, перечень границ доверия, поимённый перечень блокирующего множества и перечень планов без разбираемого блока"
    - "10-SECURITY-SUBJECT.md — запись ответа владельца: идентификатор ветви, имя, дата, основание"
    - "10-SECURITY.md — ТОЛЬКО в ветви изъятия: запись изъятия с основанием, именем, датой и ссылкой на замер, во frontmatter-форме соседних фаз"
  key_links:
    - "сведённый реестр → предмет решения владельца: единственное, чем решение принимается о том, что есть, а не о том, что помнится; ломается — владелец решает вслепую, и решение переоткроется следующим кругом"
    - "поимённый перечень блокирующего множества → честность обеих ветвей: единственное, чем «изъятие безвредно» отличимо от «изъятие не глядя»; ломается — изъятие становится сокрытием"
    - "запись ответа машинно читаемым местом → следующий круг: единственное, чем ПРИНЯТОЕ решение отличимо от НЕЗАМЕЧЕННОГО; ломается — десятый круг поднимает вопрос заново, как девять предыдущих его не подняли"
  prohibitions:
    - statement: "РЕШЕНИЕ НЕ ПРИНИМАЕТСЯ ВНУТРИ ЗАДАЧИ НИ ОДНОЙ ВЕТВЬЮ: исполнитель предъявляет замер и обе ветви с их ценами и ОСТАНАВЛИВАЕТСЯ"
      status: flagged-unverified
      verification: none
    - statement: "ИСПОЛНЕННЫЕ ПЛАНЫ 10-01…10-40 НЕ ПРАВЯТСЯ НИ НА СИМВОЛ, включая двенадцать строк тяжести, разошедшихся с более поздней идиомой фазы: правка исполненного плана задним числом есть переписанная история"
      status: flagged-unverified
      verification: test
    - statement: "`10-VERIFICATION.md` НЕ ПРАВИТСЯ: поле состояния, счёт истин и список гэпов принадлежат верификатору, и артефакт безопасности вердикта фазы не выносит"
      status: flagged-unverified
      verification: test
    - statement: "ОТМЕТКА `FORM-06` В `.planning/REQUIREMENTS.md` НЕ ТРОГАЕТСЯ: правило `test_requirement_completion_follows_verification` требует прежде `passed` у верификации фазы"
      status: flagged-unverified
      verification: test
    - statement: "ИМЯ ПРИНЯВШЕГО И ДАТА НЕ ВЫДУМЫВАЮТСЯ И НЕ ПОДСТАВЛЯЮТСЯ ЗАГЛУШКОЙ: они приходят из ответа на чекпойнт, и запись с незаполненным именем есть решение без принявшего"
      status: flagged-unverified
      verification: none
    - statement: "ПРОДУКТ И СУИТА НЕ ПРАВЯТСЯ НИ НА СТРОКУ НИ ОДНОЙ ВЕТВЬЮ"
      status: flagged-unverified
      verification: test
---

<objective>
`10-SECURITY.md` не существует при включённом принуждении безопасности — и этого не назвал НИ ОДИН
из девяти кругов верификации. План предъявляет владельцу полный замер и обе ветви и останавливается.

**Что измерено, командами, а не чтением.** `find .planning -name "*SECURITY*"` даёт девять файлов:
по одному у фаз 07, 08, 09 и у всех шести фаз вехи v2.0. У Фазы 10 — НИ ОДНОГО.
`.planning/config.json:48-50`: `security_enforcement` истинно, `security_asvs_level` равен 1,
`security_block_on` равен `high`.

**Что при этом ЕСТЬ, и это делает предмет решения нетривиальным.** Замер 2026-09-10 по всем сорока
исполненным планам фазы: блок `<threat_model>` несут **все сорок**, ни один не пропущен; раздел
границ доверия — тоже все сорок; строк реестра — **250**, уникальных имён угроз — **239**;
распределение тяжести — 84 `high`, 104 `medium`, 56 `low`, 6 `n/a`; распоряжения — 195 `mitigate`,
55 `accept`. То есть материал для артефакта собран ЦЕЛИКОМ и авторован на планировании; отсутствует
только СВЕДЕНИЕ и решение о нём.

**Что найдено сверх отсутствия артефакта, и это НАСТОЯЩАЯ находка.** При пороге блокировки `high`
строк тяжести `high` с распоряжением `accept` — **двенадцать**, и ВСЕ ДВЕНАДЦАТЬ суть строка цепи
поставок `T-10-NN-SC` планов 10-12…10-23. Их существо совпадает с существом той же строки в планах
10-24…10-40, где она несёт тяжесть `n/a`: фаза не ставит НИ ОДНОГО пакета. То есть двенадцать строк
суть РАСХОЖДЕНИЕ ТЯЖЕСТИ с более поздней идиомой той же фазы, а не двенадцать принятых высоких
рисков. Правка их невозможна: планы исполнены, и правка исполненного плана задним числом переписала
бы историю. Значит, любая ветвь решения обязана их НАЗВАТЬ, а не растворить.

**Почему решает владелец, а не план.** Выбор между принуждением регламента (`/gsd-secure-phase 10`) и
изъятием записью с основанием есть решение о том, чем фаза закрывается. Оно названо пунктом 5 перечня
Human Verification девятого круга прямо. План готовит предмет и останавливается.

Purpose: цели фазы `V9-05` не касается — закрытия касается. Отступление, которого никто не принял и
никто не отклонил, есть худший из трёх исходов: оно не даёт ни права идти дальше, ни повода чинить.

Output: сведённый замер как предмет решения; ответ владельца, записанный машинно читаемым местом.

⚠️ **НИ ОДНА ВЕТВЬ НЕ ПРАВИТ НИ ПРОДУКТА, НИ СУИТЫ, НИ ИСПОЛНЕННЫХ ПЛАНОВ.**
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

- `.planning/phases/10-rychag-components-modal-html/10-SECURITY-SUBJECT.md` — НОВЫЙ файл: сведённый
  реестр угроз фазы как ПРЕДМЕТ РЕШЕНИЯ. Несёт числа замера, консолидированный перечень границ
  доверия, поимённый перечень блокирующего множества, перечень планов без разбираемого блока (сегодня
  пуст) и, после чекпойнта, запись ответа владельца.
- `.planning/phases/10-rychag-components-modal-html/10-SECURITY.md` — НОВЫЙ файл, создаётся ТОЛЬКО в
  ветви изъятия: запись изъятия с основанием, именем принявшего, датой и ссылкой на замер, во
  frontmatter-форме соседних фаз (`phase`, `slug`, `status`, `threats_open`, `asvs_level`, `created`).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Задача 1: реестры угроз сорока исполненных планов сводятся в ОДИН предмет решения — числами, а не оценкой</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-SECURITY-SUBJECT.md
  </files>

  <read_first>
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-SECURITY.md:1-60` — ЖИВОЙ
  ОБРАЗЕЦ формы в этом проекте: frontmatter с полями фазы, состояния, числа открытых угроз и уровня;
  строка происхождения реестра; блок итогов числами; консолидированная таблица границ доверия;
  таблица реестра с колонками. ⚠️ ФОРМА НАСЛЕДУЕТСЯ ОТСЮДА, а не изобретается: вторая форма
  разошлась бы с первой молча.
- `.planning/config.json:48-50` — три значения принуждения безопасности ДОСЛОВНО.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md`, раздел перечня Human
  Verification, пункт про `10-SECURITY.md` — дословная формулировка того, что требуется от человека.
- `.planning/phases/10-rychag-components-modal-html/10-12-PLAN.md`, блок `<threat_model>`, строка
  `T-10-12-SC` — образец строки цепи поставок с тяжестью `high` и распоряжением `accept`.
- `.planning/phases/10-rychag-components-modal-html/10-24-PLAN.md`, блок `<threat_model>`, строка
  `T-10-24-SC` — та же строка по существу, но с тяжестью `n/a`. ⚠️ ЧИТАТЬ ОБЕ: расхождение тяжести
  между ними и есть находка задачи, и предъявлять его надо сличением, а не пересказом.
  </read_first>

  <action>
**(1) СНЯТЬ ЗАМЕР КОМАНДОЙ, А НЕ ЧТЕНИЕМ.** Разобрать блок `<threat_model>` КАЖДОГО из сорока
исполненных планов (`10-01`…`10-40`) и собрать: число планов; число планов БЕЗ разбираемого блока;
число строк реестра; число уникальных имён угроз; распределение по тяжести; распределение по
распоряжению; поимённый перечень строк тяжести `high` или `critical` с распоряжением, отличным от
`mitigate`. ⚠️ РАЗБИРАЮТСЯ ТОЛЬКО ПЛАНЫ `10-01`…`10-40`: планы настоящей партии ещё не исполнены, и
включение их в замер сделало бы предмет решения шире фазы, о которой решают.

**(2) СЛИЧИТЬ СНЯТОЕ С ЧИСЛАМИ, НАЗВАННЫМИ В `<objective>`.** Ожидание: 40 планов, 0 без блока, 250
строк, 239 уникальных имён, 84/104/56/6 по тяжести, 195/55 по распоряжению, 12 строк блокирующего
множества, и все двенадцать оканчиваются на признак цепи поставок. ⚠️ РАСХОЖДЕНИЕ НЕ СГЛАЖИВАЕТСЯ:
если снятое отличается, в предмет решения идут СНЯТЫЕ числа, а расхождение с планом называется в
сводке отдельной строкой вместе с вероятной причиной.

**(3) СОБРАТЬ ПЕРЕЧЕНЬ ГРАНИЦ ДОВЕРИЯ.** Взять разделы границ доверия всех сорока планов и свести
формулировки к неповторяющемуся перечню, назвав ЧИСЛО исходных формулировок и ЧИСЛО строк после
сведения — ровно так, как это сделано в артефакте Фазы 09.

**(4) НАПИСАТЬ `10-SECURITY-SUBJECT.md`.** Frontmatter по образцу соседней фазы, но с полем
состояния `subject` и БЕЗ поля числа открытых угроз: настоящий файл есть ПРЕДМЕТ решения, а не
вердикт, и поле вердикта в нём было бы вердиктом, вынесенным исполнителем. Тело: строка
происхождения реестра (все сорок планов несут блок, авторованный на планировании); блок итогов
числами; консолидированная таблица границ доверия; таблица блокирующего множества ПОИМЁННО с
колонками «имя угрозы, план, тяжесть, распоряжение, существо»; отдельный раздел о расхождении
тяжести строки цепи поставок между планами 10-12…10-23 и 10-24…10-40 с прямым указанием, что правка
исполненных планов ЗАПРЕЩЕНА и потому расхождение записывается, а не чинится.

**(5) НАЗВАТЬ ПРЯМО, ЧЕГО ЭТОТ ФАЙЛ НЕ ДЕЛАЕТ.** Он не выносит вердикта фазе, не объявляет угрозы
закрытыми, не заменяет `/gsd-secure-phase 10` и не двигает отметку требования. Без этого абзаца
следующий круг прочтёт предмет решения как решение.
  </action>

  <acceptance_criteria>
- `10-SECURITY-SUBJECT.md` существует и содержит все семь величин замера числами: планов, планов без
  блока, строк реестра, уникальных имён, распределение тяжести, распределение распоряжения, размер
  блокирующего множества.
- Блокирующее множество предъявлено ПОИМЁННО: столько строк таблицы, сколько дал замер, и у каждой
  названы имя угрозы, план, тяжесть, распоряжение и существо.
- Раздел о расхождении тяжести строки цепи поставок называет оба диапазона планов и прямо запрещает
  правку исполненных планов.
- Frontmatter несёт поле состояния `subject` и НЕ несёт поля числа открытых угроз.
- Присутствует абзац о том, чего файл не делает.
- `git status --short .planning/phases/10-rychag-components-modal-html/` показывает ровно один новый
  файл и ни одного изменённого плана.
  </acceptance_criteria>


  <verify>
    <automated>uv run python -c "import pathlib,sys; p=pathlib.Path('.planning/phases/10-rychag-components-modal-html/10-SECURITY-SUBJECT.md'); s=p.read_text(encoding='utf-8') if p.is_file() else ''; need=['status: subject','Trust Boundaries','цепи поставок','10-01','10-40']; miss=[n for n in need if n not in s]; sys.exit('нет в 10-SECURITY-SUBJECT.md: '+repr(miss) if miss else 0)"</automated>
    <fails_when>ненулевой код возврата — файла нет либо в нём отсутствует одна из названных строк; вывод перечисляет отсутствующие</fails_when>
    <automated>git status --porcelain -- .planning/phases/10-rychag-components-modal-html/</automated>
    <fails_when>в выводе есть строка, оканчивающаяся на "-PLAN.md" или "-SUMMARY.md" с номером плана не выше 40 — исполненный план тронут, что запрещено прямо</fails_when>
  </verify>

  <done>
`10-SECURITY-SUBJECT.md` существует, несёт семь величин замера числами, поимённый перечень
блокирующего множества, консолидированные границы доверия, раздел о расхождении тяжести строки цепи
поставок с запретом правки исполненных планов и абзац о том, чего файл НЕ делает; ни один исполненный
план не тронут.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Задача 2: РЕШЕНИЕ ВЛАДЕЛЬЦА — принудить регламент безопасности либо изъять его записью с основанием</name>

  <decision>Закрывается ли отсутствие `10-SECURITY.md` ПРИНУЖДЕНИЕМ РЕГЛАМЕНТА (владелец запускает
`/gsd-secure-phase 10`, и артефакт производит он) — или регламент ИЗЫМАЕТСЯ для Фазы 10 записью с
основанием, именем и датой.</decision>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-SECURITY-SUBJECT.md` ЦЕЛИКОМ — предмет
  решения, собранный задачей 1. ⚠️ ЧИТАТЬ ДОСЛОВНО И ПЕРЕД ОТВЕТОМ: решение принимается по нему.
- `.planning/config.json:48-50` — три значения принуждения.
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-SECURITY.md:1-20` — что
  именно производит регламент: frontmatter с состоянием, числом открытых угроз и уровнем, плюс
  строка происхождения реестра.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md`, пункт 5 перечня Human
  Verification — дословная формулировка того, что решение принадлежит владельцу.
- `.planning/STATE.md`, записи D-30/D-32 — форма записи решения: опровергнутое и разрешённое
  помечается, а не стирается.
  </read_first>

  <context>
**ЧТО ПРЕДЪЯВЛЯЕТСЯ ВЛАДЕЛЬЦУ ДО ОТВЕТА — ДОСЛОВНО ИЗ `10-SECURITY-SUBJECT.md`:** семь величин
замера; поимённый перечень блокирующего множества; консолидированные границы доверия; раздел о
расхождении тяжести строки цепи поставок между планами 10-12…10-23 и 10-24…10-40.

**Ветвь (A) — `run-secure-phase`.** Регламент принуждается. Артефакт `10-SECURITY.md` производит
`/gsd-secure-phase 10`, который запускает ВЛАДЕЛЕЦ; настоящий план его НЕ АВТОРУЕТ — второй
производитель одного артефакта дал бы вторую форму, разошедшуюся с первой молча. Задача 3 в этой
ветви записывает ответ в `10-SECURITY-SUBJECT.md` и называет, что артефакт остаётся за командой
регламента.
**Цена ветви, названная прямо:** гэп `V9-05` НАСТОЯЩИМ ПЛАНОМ НЕ ЗАКРЫВАЕТСЯ — он закрывается
прогоном команды, и до него состояние остаётся прежним. Сверх того команда регламента столкнётся с
двенадцатью строками блокирующего множества и, при пороге `high`, вправе объявить фазу
заблокированной; починка их невозможна (планы исполнены), и разрешать расхождение придётся отдельной
записью — то есть ветвь (A) с высокой вероятностью приводит к ЭТОМУ ЖЕ решению вторым заходом.

**Ветвь (B) — `waive-with-record`.** Регламент изымается для Фазы 10 записью. Задача 3 создаёт
`10-SECURITY.md` с состоянием изъятия, основанием, именем принявшего, датой и ссылкой на замер.
Основание в записи приводится ЗАМЕРОМ, а не оценкой: все сорок планов несут авторованные реестры;
двенадцать строк блокирующего множества суть расхождение тяжести одной и той же строки цепи поставок
с более поздней идиомой той же фазы; установок пакетов фаза не производит ни одной.
**Цена ветви, названная прямо:** фаза объявляет, что артефакт безопасности у неё заменён записью об
изъятии, и запись переживает фазу. Всякая следующая фаза вехи получит прецедент «реестры на
планировании плюс изъятие вместо сведения», и его придётся либо повторять, либо явно отменять.
Сверх того сведения угроз по фазе не появится, и следующая фаза, ищущая, что Фаза 10 оставила
принятым, будет читать сорок блоков вместо одного.

⚠️ **ТРЕТЬЕЙ ВЕТВИ — «ОСТАВИТЬ КАК ЕСТЬ» — В ПЕРЕЧНЕ НЕТ, И ЭТО НЕ УПУЩЕНИЕ.** Она и есть
сегодняшнее состояние, и именно оно прожило девять кругов незамеченным.
  </context>

  <options>
    <option id="run-secure-phase">
      <name>(A) Регламент ПРИНУЖДАЕТСЯ: артефакт производит `/gsd-secure-phase 10`, запускаемый владельцем</name>
      <pros>Форма артефакта в проекте одна и та же у всех девяти фаз, где он есть, и десятая не заводит своей. Сведение угроз по фазе появляется, и следующая фаза читает один документ вместо сорока блоков. Двенадцать строк блокирующего множества попадают под глаз регламента, а не под запись, написанную тем, кому удобно их разрешить. Прецедента «изъятие вместо сведения» не возникает</pros>
      <cons>Гэп `V9-05` настоящим планом НЕ ЗАКРЫВАЕТСЯ: до прогона команды состояние прежнее, и десятый круг увидит его открытым. Команда регламента при пороге `high` вправе объявить фазу заблокированной двенадцатью строками, которые ПОЧИНИТЬ НЕЛЬЗЯ — планы исполнены; разрешать расхождение придётся отдельной записью, то есть ветвь с высокой вероятностью приводит к решению об изъятии вторым заходом, потратив круг</cons>
    </option>
    <option id="waive-with-record">
      <name>(B) Регламент ИЗЫМАЕТСЯ для Фазы 10 записью с основанием, именем и датой</name>
      <pros>Состояние становится машинно читаемым НЕМЕДЛЕННО, и десятый круг отличает ПРИНЯТОЕ изъятие от НЕЗАМЕЧЕННОГО отсутствия — то, чего девять кругов сделать не могли. Основание не мнение, а замер: сорок реестров авторованы на планировании, ни один не пропущен, а всё блокирующее множество есть одна и та же строка цепи поставок с разошедшейся тяжестью при нулевых установках пакетов. Ни продукта, ни суиты, ни исполненных планов не трогается</pros>
      <cons>Фаза объявляет, что артефакт безопасности у неё заменён записью, и запись переживает фазу: следующая фаза вехи получает прецедент «реестры на планировании плюс изъятие вместо сведения», который придётся либо повторять, либо явно отменять. Сведения угроз по фазе не появится, и следующая фаза, ищущая принятое Фазой 10, будет читать сорок блоков. Двенадцать строк блокирующего множества остаются в исполненных планах с прежней тяжестью, и разрешает их запись, а не пересмотр</cons>
    </option>
  </options>

  <reversibility rating="one-way">
Обе ветви объявляют, ЧЕМ фаза закрывается по части безопасности, и переживают фазу: следующие круги,
следующие фазы и команда отгрузки читают запись как основание. Ветвь (B) сверх того публикует в
проекте ПРЕЦЕДЕНТ обращения с регламентом безопасности, наследуемый последующими фазами вехи.
Отмена любой из двух записей задним числом есть переоткрытие уже принятого владельцем решения, а не
правка строки. Поэтому дверь гейтируется человеком до единого символа записи.
  </reversibility>

  <resume-signal>Ответить РОВНО одним идентификатором: `run-secure-phase` либо `waive-with-record`, и
назвать ИМЯ для поля принявшего и ДАТУ (обе величины идут в запись дословно и заглушкой не
подставляются).
При `run-secure-phase` — подтвердить, что `10-SECURITY.md` производит команда регламента, запускаемая
владельцем, и что настоящий план его не авторует.
При `waive-with-record` — подтвердить основание изъятия дословно и назвать, считается ли изъятие
разовым (только Фаза 10) либо действующим на всю веху.</resume-signal>

  <acceptance_criteria>
- Владельцу предъявлены семь величин замера, поимённый перечень блокирующего множества и раздел о
  расхождении тяжести — в формулировках `10-SECURITY-SUBJECT.md` дословно.
- Обе ветви предъявлены с их ценами в формулировках раздела `<context>` дословно.
- Ответ записан ровно одним идентификатором из двух вместе с именем и датой; выбор внутри задачи не
  сделан ни при каких условиях.
- При `waive-with-record` записано, разовое изъятие или на всю веху.
  </acceptance_criteria>

  <verify>
    <human-check>Владелец назвал ОДНУ из двух ветвей (`run-secure-phase` либо `waive-with-record`), назвал имя для записи и дату, а при изъятии — его область (Фаза 10 либо веха). Замер предъявлен ему ДО ответа и приведён в сводку дословно.</human-check>
  </verify>

  <done>
Замер предъявлен целиком; обе ветви и их цены названы дословно; выбор внутри задачи не сделан; ответ
владельца записан идентификатором ветви, именем и датой, а при изъятии — областью.
  </done>
</task>

<task type="auto">
  <name>Задача 3: ответ владельца записывается машинно читаемым местом, и ни одна ветвь не выносит вердикта фазе</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-SECURITY-SUBJECT.md
.planning/phases/10-rychag-components-modal-html/10-SECURITY.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-SECURITY-SUBJECT.md` ЦЕЛИКОМ — куда встаёт
  запись ответа. ⚠️ ЯКОРЬ: раздел ответа встаёт ОДИН раз, по имени раздела, а не дописыванием в
  конец файла (урок `V9-06` и окна 70).
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-SECURITY.md:1-20` — форма
  frontmatter, наследуемая ветвью изъятия ДОСЛОВНО (поля фазы, слага, состояния, числа открытых
  угроз, уровня и даты создания).
- `.planning/STATE.md`, записи D-30/D-32 — разрешённое помечается, а не стирается.
- `tests/test_planning/test_requirement_completion_follows_verification.py` — что именно правило
  читает в артефактах фазы. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: новый файл не имеет права его уронить.
  </read_first>

  <behavior>
1. В `10-SECURITY-SUBJECT.md` встаёт РОВНО ОДИН раздел ответа, с идентификатором ветви, именем
   принявшего, датой и основанием.
2. Повторное исполнение задачи не заводит второго такого раздела: запись идёт по имени раздела, и
   счёт его вхождений до и после равен единице.
3. Ветвь `run-secure-phase`: `10-SECURITY.md` НЕ СОЗДАЁТСЯ; раздел ответа прямо называет, что
   артефакт производит команда регламента, запускаемая владельцем, и что гэп `V9-05` до её прогона
   остаётся открытым.
4. Ветвь `waive-with-record`: `10-SECURITY.md` создаётся с состоянием изъятия, числом открытых угроз,
   равным ЗАМЕРЕННОМУ размеру блокирующего множества, уровнем из настроек, основанием, именем, датой
   и областью изъятия; тело несёт ссылку на замер и поимённый перечень блокирующего множества.
5. Ни в одной ветви не правятся `10-VERIFICATION.md`, `.planning/REQUIREMENTS.md`, исполненные планы,
   продукт и суита.
  </behavior>

  <action>
**(1) ВЗЯТЬ ОТВЕТ ЧЕКПОЙНТА И ИСПОЛНИТЬ РОВНО ОДНУ ВЕТВЬ.** Заготовка отвергнутой ветви в файлах не
оставляется: она прочиталась бы следующим кругом как второе, несогласное решение.

**(2) ЗАПИСЬ ИДЁТ ПО ЯКОРЮ, А НЕ ДОПИСЫВАНИЕМ В КОНЕЦ.** Раздел ответа имеет собственный заголовок;
перед записью считается число его вхождений, после — тоже, и оба числа называются в сводке. ⚠️ ЭТО
ПРЯМОЙ УРОК `V9-06`: задача, дописывающая раздел в конец файла, на втором прогоне заводит второй
такой раздел, и обнаруживает это только следующий круг.

**(3) ОСНОВАНИЕ СОБИРАЕТСЯ ИЗ ЗАМЕРА, А НЕ ИЗ ПАМЯТИ.** Числа берутся из
`10-SECURITY-SUBJECT.md`, а не переписываются из настоящего плана: план писан до прогона, файл — по
прогону.

**(4) ВЕТВЬ ИЗЪЯТИЯ ПИШЕТ `10-SECURITY.md` ФОРМОЙ СОСЕДНЕЙ ФАЗЫ.** Frontmatter — те же поля и в том
же порядке; поле состояния несёт изъятие, поле числа открытых угроз — ЗАМЕРЕННЫЙ размер блокирующего
множества, а не ноль. ⚠️ НОЛЬ ЗДЕСЬ БЫЛ БЫ ЛОЖЬЮ: строки существуют, и изъятие есть решение НЕ
СВОДИТЬ их, а не утверждение, что их нет. Тело: основание, имя, дата, область изъятия, ссылка на
замер и поимённый перечень блокирующего множества.

**(5) НЕ ВЫНОСИТЬ ВЕРДИКТА.** Ни один файл настоящего плана не объявляет фазу пройденной, не двигает
отметку требования и не правит отчёт верификации. Абзац об этом ставится в обоих исходах прямо.

**(6) ПРОВЕРИТЬ, ЧТО КАТАЛОГ ПРАВИЛ ЗАПИСИ ЗЕЛЕН.** Новый файл в каталоге фазы не имеет права
уронить ни одно из двадцати восьми правил.
  </action>

  <acceptance_criteria>
- Раздел ответа в `10-SECURITY-SUBJECT.md` встречается РОВНО ОДИН раз; число вхождений до и после
  названо в сводке.
- Раздел ответа несёт идентификатор ветви, имя принявшего, дату и основание; ни одно поле не
  заполнено заглушкой.
- В ветви `run-secure-phase` файла `10-SECURITY.md` в каталоге фазы НЕТ, и раздел ответа прямо
  называет, что гэп `V9-05` до прогона команды регламента остаётся открытым.
- В ветви `waive-with-record` `10-SECURITY.md` существует, его frontmatter несёт те же поля и в том
  же порядке, что у артефакта Фазы 09, поле числа открытых угроз равно замеренному размеру
  блокирующего множества, и тело несёт поимённый перечень этого множества.
- `git diff --numstat -- .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md app tests`
  пуст.
- `uv run pytest tests/test_planning/ -q` завершается нулём.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed", либо число зелёных меньше 28 — новый артефакт каталога фазы уронил правило записи</fails_when>
    <automated>git diff --numstat -- .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md app tests</automated>
    <fails_when>вывод НЕПУСТ — тронут отчёт верификации, отметка требования, продукт либо суита, что запрещено прямо</fails_when>
  </verify>

  <done>
Ответ владельца записан ровно одним разделом по якорю, с идентификатором ветви, именем, датой и
основанием, собранным из замера; ветвь изъятия создала артефакт формой соседней фазы с ЗАМЕРЕННЫМ
числом открытых угроз и поимённым перечнем блокирующего множества, ветвь принуждения артефакта не
создала и назвала гэп открытым до прогона команды; вердикт, отметка требования, продукт и суита не
тронуты; каталог правил записи зелен.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| исполнитель плана → владелец фазы | решение о том, чем фаза закрывается по части безопасности, принадлежит владельцу; задача, принявшая его сама, присваивает чужое полномочие |
| запись изъятия → следующая фаза вехи | запись читается как прецедент обращения с регламентом безопасности и наследуется |
| сведённый реестр → решение владельца | замер, снятый чтением вместо разбора, дал бы решение о том, что помнится, а не о том, что есть |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-44-01 | Elevation of privilege | задача записи решения | high | mitigate | Исполнитель, изъявший регламент безопасности сам, объявляет фазу закрытой вместо владельца. Закрывается блокирующим чекпойнтом перед задачей записи и прямым запретом выбора внутри задачи |
| T-10-44-02 | Repudiation | запись изъятия без имени и даты | high | mitigate | Изъятие без принявшего неотличимо от незамеченного отсутствия — ровно то, что прожило девять кругов. Закрывается требованием четырёх полей и запретом заглушек |
| T-10-44-03 | Repudiation | поле числа открытых угроз, записанное нулём | high | mitigate | Ноль при двенадцати существующих строках блокирующего множества есть ложь в машинно читаемом поле, которое читает команда отгрузки. Закрывается требованием ЗАМЕРЕННОГО значения и поимённым перечнем в теле |
| T-10-44-04 | Tampering | исполненные планы 10-12…10-23 | high | mitigate | Соблазн «починить» двенадцать строк правкой тяжести в исполненных планах переписал бы историю фазы. Закрывается прямым запретом и утверждением о том, что ни один файл плана с номером не выше 40 не изменён |
| T-10-44-05 | Tampering | `10-VERIFICATION.md` и отметка требования | high | mitigate | Артефакт безопасности не есть верификация; план, тронувший вердикт или отметку, объявил бы фазу пройденной в обход верификатора и правила регламента отметок. Закрывается запретом и утверждением о пустом диффе |
| T-10-44-06 | Repudiation | раздел ответа, дописанный в конец файла | medium | mitigate | Повторное исполнение завело бы второй раздел ответа, и следующий круг прочёл бы два несогласных решения — дословный класс `V9-06`. Закрывается записью по якорю и счётом вхождений до и после |
| T-10-44-SC | Tampering | npm/pip/cargo installs | n/a | accept | ЗАМЕР: план не ставит ни одного пакета — правятся два документа записи. Гейт легитимности пакетов неприменим. ⚠️ Тяжесть `n/a` соответствует идиоме планов 10-24…10-40 и НЕ является исправлением двенадцати строк планов 10-12…10-23: те не правятся |
</threat_model>

<verification>
1. `10-SECURITY-SUBJECT.md` существует, несёт семь величин замера и поимённый перечень блокирующего
   множества.
2. Раздел ответа встречается ровно один раз; идентификатор ветви, имя и дата заполнены не заглушкой.
3. В ветви изъятия `10-SECURITY.md` существует формой соседней фазы с замеренным числом открытых
   угроз; в ветви принуждения его нет, и это названо в записи.
4. `uv run pytest tests/test_planning/ -q` — зелено (28 правил).
5. `git diff --numstat -- .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md app tests` — пусто.
6. Ни один файл `10-01`…`10-40` не изменён.
</verification>

<success_criteria>
- Отсутствие артефакта безопасности при включённом принуждении перестало быть незамеченным.
- Предмет решения предъявлен замером по сорока реестрам, а не оценкой.
- Блокирующее множество названо поимённо, и ни одна ветвь его не растворяет.
- Решение принял владелец, а не исполнитель, и оно записано машинно читаемым местом.
- Вердикт верификации, отметка требования, исполненные планы, продукт и суита не тронуты.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-44-SUMMARY.md` when done
</output>
