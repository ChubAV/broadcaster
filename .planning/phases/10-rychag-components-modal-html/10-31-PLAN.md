---
phase: 10-rychag-components-modal-html
plan: 31
type: execute
wave: 4
# ⚠️ ВОЛНА 4 — СЛЕДСТВИЕ ОБЩЕГО ФАЙЛА. План 10-29 (волна 3) правит `tests/test_pages/test_htmx_gates.py`;
# настоящий план правит его же. Волна: 3 + 1 = 4. Пересечения с планом 10-30 той же волны нет:
# он правит `tests/test_pages/test_identifier_bounds.py`, `tests/test_pages/test_editor_schedules.py`
# и реестр окон.
depends_on: ["10-29"]
gap_closure: true
files_modified:
  - app/pages/htmx.py
  - tests/test_pages/test_htmx_gates.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (поля во frontmatter не заводятся, форма унаследована от
# планов 10-12…10-30): `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  # `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < min_samples 3), а не назначена автором.
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ГЕЙТ ЕДИНСТВЕННОГО РЕШЕНИЯ О ФОРМЕ ОТВЕТА ВИДИТ ВТОРОЙ ВЫХОД, А НЕ ТОЛЬКО РЕДИРЕКТ: разбор узнаёт ЛЮБУЮ собственную сборку ответа переведённого обработчика, а не одну её разновидность, и слепота гейта к собственноручному отказу с кодом снята"
    - "КАЖДЫЙ СОБСТВЕННЫЙ ВЫХОД ПЕРЕВЕДЁННОГО ОБРАБОТЧИКА НАЗВАН В ИМЕНОВАННОМ РЕЕСТРЕ С ОБОСНОВАНИЕМ, УСЛОВИЕМ СНЯТИЯ И СОСТОЯНИЕМ РЕШЕНИЯ, А ЧИСЛО ЗАПИСЕЙ ОБЪЯВЛЕНО: молча уцелевший выход неотличим от принятого решения"
    - "УТВЕРЖДЕНИЕ СЛОЯ ОТВЕТА О ЕДИНСТВЕННОМ МЕСТЕ РЕШЕНИЯ СУЖЕНО ДО ИЗМЕРЕННОГО: сколько выходов в обход слоя есть сегодня — названо числом, и утверждение больше не читается как исполненный инвариант"
    - "УТВЕРЖДЕНИЕ О ЗАКРЫТОЙ ГРАНИЦЕ КАНАЛА ИСХОДА НА ФРАГМЕНТНОЙ ВЕТКЕ СУЖЕНО ДО ИЗМЕРЕННОГО: механизм заведён и покрыт суитой, но ПОВЕДЕНИЕМ он не является — ни один вызов приложения не подаёт одновременно фрагмент и код исхода, и это снято прогоном замера"
    - "ПРИКЛЕЙКА ВНЕПОЛОСНОГО БЛОКА НЕ МОЛЧИТ НА ОТВЕТЕ, КОТОРОМУ ТЕЛО ЗАПРЕЩЕНО ЕГО СТАТУСОМ: попытка дописать тело к ответу без тела падает громко, а не рождает нарушение протокола"
    - "ГРАНИЦА БЕЗОПАСНОСТИ ПОДМЕНЫ ТЕЛА ОТВЕТА НАЗВАНА, А НЕ ПОДРАЗУМЕВАЕТСЯ: подмена безопасна ровно потому, что все сегодняшние сборщики фрагмента возвращают свежесобранный ответ, и это условие записано там, где его прочтёт следующий сборщик"
  artifacts:
    - "tests/test_pages/test_htmx_gates.py — расширенный разбор собственной сборки ответа, именованный реестр собственных выходов с объявленным числом и правила полноты, обоснований и авторства над ним"
    - "app/pages/htmx.py — суженное утверждение о единственном месте решения о форме ответа, суженное утверждение о канале исхода на фрагментной ветке, проверка статуса в приклейке внеполосного блока и названная граница безопасности подмены тела"
  key_links:
    - "расширенный разбор → реестр собственных выходов: единственное, чем второй выход не останется невидимым для гейта, построенного ради единственности; ломается — утверждение «единственное место» живёт рядом с выходами, его обходящими, и читатель верит утверждению"
    - "суженное утверждение слоя → следующий автор обработчика: единственное, чем «граница закрыта» не прочитается как исполненный инвариант; ломается — автор подаёт фрагмент с кодом исхода, полагая путь обкатанным, и обнаруживает, что он не исполнялся ни разу"
    - "проверка статуса в приклейке → протокол ответа: единственное, чем ответ без тела не уедет с телом; ломается — нарушение протокола вместо громкого отказа, и разбирается оно на стороне клиента, а не в суите"
  prohibitions:
    - statement: "форма отказа по происхождению НЕ МЕНЯЕТСЯ этим планом: перевод её на слой ответа есть решение о ФОРМЕ ОТВЕТА, а форма ответа заперта решением владельца D-01 — чеканить изъятие из запертого решения плану запрещено, прецедент — окно 51"
      status: flagged-unverified
      verification: none
    - statement: "новых кодов реестра уведомлений план НЕ ЗАВОДИТ (D-03), в том числе кода для неподтверждённого источника запроса: перевод отказа на реестр потребовал бы такого кода"
      status: flagged-unverified
      verification: none
    - statement: "записанное свойство «панель остаётся открытой на отказе» НЕ ОТМЕНЯЕТСЯ: оно проверено суитой на реальной форме события отказа (план 10-07) и стои́т отдельным пунктом ручного обхода с собственным окном"
      status: flagged-unverified
      verification: none
    - statement: "механизм внеполосного кода исхода на фрагментной ветке НЕ УДАЛЯЕТСЯ как неиспользуемый: он покрыт суитой заранее и станет поведением с первым фрагментом, несущим отказ; удаление заведённого впрок механизма и сужение утверждения о нём — разные действия"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ — ПОЛНАЯ ЛЕТОПИСЬ ПАРТИИ СТОИТ В `10-24-PLAN.md`. Настоящий план
  # касается ребра `empty` ОДНОЙ стороной и она названа: ответ, которому тело запрещено статусом,
  # есть пустой случай приклейки внеполосного блока, и он получает громкий отказ вместо тихого
  # нарушения протокола. Состояние ребра при этом не меняется — оно уже `resolved / explicit`.
---

<objective>
Слой ответа перестаёт утверждать больше, чем исполняет, — по двум находкам пятого круга сразу.

`WR-04`: модуль слоя ответа открывается словами «единственное место, где приложение решает, ЧЕМ
отвечать», и гейт `G-2` существует, чтобы это оставалось верным. Его разбор узнаёт РЕДИРЕКТ и только
его. Четвёртая партия дописала в переведённые обработчики собственную сборку ответа с кодом отказа —
второе, независимое решение о форме ответа, невидимое гейту.

`WR-06`: сборка ответа объявляет, что код исхода «доезжает и на ветке фрагмента» и «граница
закрыта». Замер показывает, что ни один вызов приложения не подаёт одновременно фрагмент и код: путь
достижим только из суиты. Утверждение описывает СПОСОБНОСТЬ, а читается как ПОВЕДЕНИЕ.

Purpose: обе находки суть один класс — запись шире дерева, — и обе живут в модуле, который эта веха
построила ради того, чтобы решение о форме ответа было ОДНО. Утверждение о единственности, стоящее
рядом с выходами, его обходящими, стоит дороже отсутствия утверждения: читатель верит ему и не ищет.

⚠️ **РЕВИЗИЯ ТРЕБУЕТ ВЫБРАТЬ ЯВНО ИЗ ДВУХ ИСХОДОВ, И ВЫБОР СДЕЛАН: ИСХОД (1) — РАСШИРИТЬ РАЗБОР И
ЗАВЕСТИ ИМЕНОВАННЫЙ РЕЕСТР РАСХОЖДЕНИЯ.** Исход (2) — провести отказ через слой ответа — ОТВЕРГНУТ, и
основание названо тремя следствиями, а не оценкой труда: (а) это решение о ФОРМЕ ОТВЕТА, а форма
ответа заперта решением владельца D-01, и чеканить изъятие плану запрещено (прецедент — окно 51,
где четвёртая партия сделала расхождение видимым и решения не приняла); (б) перевод потребовал бы
НОВОГО кода реестра уведомлений, а новых кодов фаза не заводит по D-03 с основанием, записанным
вехой; (в) он отменил бы записанное свойство «панель остаётся открытой на отказе», доказанное планом
10-07 и стоящее отдельным пунктом ручного обхода со своим окном.

Output: гейт, видящий второй выход; реестр, называющий каждый; и два суженных утверждения слоя.
</objective>

<reversibility>
  <decision rating="reversible">
Расширение разбора и заведение реестра собственных выходов. Откат — сужение разбора обратно;
поведение продукта не меняется ни на байт.
  </decision>
  <decision rating="reversible">
Проверка статуса в приклейке внеполосного блока. Откат — снятие условия. Путь сегодня достижим только
из суиты, поэтому изменение поведения продукта равно нулю по замеру.
  </decision>
  <decision rating="reversible">
Сужение двух утверждений докстринга. Откат — правка текста.
  </decision>
</reversibility>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-22-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: гейт единственного решения о форме ответа перестаёт быть слепым к собственноручной сборке ответа (`WR-04`, исход 1)</name>

  <files>
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py:630-690` — `_builds_own_redirect` и соседний разбор вызова
  слоя ответа. ⚠️ ЧИТАТЬ ВНИМАТЕЛЬНО: разбор узнаёт сборку ответа-редиректа и обе формы записи кода
  перехода, целочисленную и символьную, — и НИЧЕГО больше.
- `tests/test_pages/test_htmx_gates.py:1020-1055` — правило `G-2` и его вселенная: обработчики,
  ходящие через слой ответа. ⚠️ ВСЕЛЕННАЯ ЗДЕСЬ УЖЕ ВСЕГО СТРАНИЧНОГО СЛОЯ, и это существенно: не
  каждый из тринадцати обработчиков с собственным отказом переведён на слой, а правило говорит только
  о переведённых. Число попадающих в реестр снимается ЗАМЕРОМ, а не равно тринадцати по умолчанию.
- `tests/test_pages/test_htmx_gates.py:2340-2790` — раздел реестра расхождения формы отказа
  валидации после правок планов 10-24, 10-28, 10-29. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: он есть ОБРАЗЕЦ ФОРМЫ
  для нового реестра — пять полей записи, константы ждущего состояния и условия снятия, объявленное
  число с летописью, правила полноты, обоснований и авторства, приём подачи укороченного перечня
  параметром для показа зубов.
- `app/pages/accounts.py:1025-1035`, `app/pages/ads.py:767-777`,
  `app/pages/account_groups.py:686-695`, `app/pages/schedules.py:1060-1070`,
  `app/pages/history.py:913-920` — площадки собственного отказа внутри обработчиков, ходящих через
  слой ответа.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `### WR-04` целиком,
  включая оба предложенных исхода и абзац о поведенческом следствии.
  </read_first>

  <behavior>
1. Разбор узнаёт собственную сборку ответа переведённого обработчика в любой её разновидности, а не
   только сборку редиректа.
2. Замер по действующим исходникам находит переведённые обработчики, несущие собственную сборку
   ответа; их число снято прогоном.
3. Каждый найденный назван записью реестра с пятью непустыми полями.
4. Объявленное число реестра равно длине перечня и снято прогоном.
5. Правило полноты краснеет, если найденный замером выход не объявлен, и называет его поимённо.
6. Правило обоснований краснеет на записи с пустым полем.
7. Правило `G-2` в прежней формулировке остаётся зелёным: собственный редирект у переведённых
   обработчиков по-прежнему отсутствует.
8. Ни одно правило не зеленеет на пустом множестве.
  </behavior>

  <action>
**(1) RED ПЕРЕД GREEN.** Сначала расширить разбор и прогнать правило `G-2` БЕЗ реестра — оно обязано
покраснеть и назвать найденные выходы поимённо. Дословный вывод привести в сводку: это и есть
доказательство, что гейт был слеп, а стал зряч.

**(2) Расширить разбор ОДНОЙ функцией, не трогая прежней.** Прежний разбор редиректа остаётся: его
предмет — редирект, и правило `G-2` в прежней формулировке продолжает его стеречь. Новая функция
узнаёт собственную сборку ответа ЛЮБОГО вида — по имени вызываемого. Перечень узнаваемых имён
объявляется КОНСТАНТОЙ с обоснованием, а не набирается в выражении: перечень внутри выражения нельзя
ни сосчитать, ни расширить осознанно.

**(3) Снять ЗАМЕРОМ, сколько переведённых обработчиков несут собственную сборку.** ⚠️ ЧИСЛО В ПЛАН
НЕ ВПИСАНО КАК ЦЕЛЬ: планировщик замерил ТРИНАДЦАТЬ мест собственного отказа во ВСЁМ страничном слое
(восемь файлов), но вселенная правила `G-2` — только ПЕРЕВЕДЁННЫЕ обработчики, и сколько из
тринадцати в неё попадает, обязан снять исполнитель прогоном. Расхождение с ориентиром — НАХОДКА, и
она называется числом и именами.

**(4) Завести именованный реестр собственных выходов** по форме реестра расхождения формы отказа
валидации: запись несёт ВХОД (модуль и имя обработчика), ВИД собственной сборки, ОБОСНОВАНИЕ (что
именно этот выход делает и какова его цена), УСЛОВИЕ СНЯТИЯ и СОСТОЯНИЕ РЕШЕНИЯ.
⚠️ ЦЕНА НАЗЫВАЕТСЯ СЛЕДСТВИЕМ, А НЕ СЛОВОМ: на пути разметочного рантайма ответ без тела не свопается,
обработчик завершения запроса видит признак неуспеха, и панель остаётся открытой БЕЗ СООБЩЕНИЯ —
человек видит кнопку, на которую нажал впустую.
⚠️ УСЛОВИЕ СНЯТИЯ НАЗЫВАЕТ, ЧТО ИМЕННО ЕГО СНИМЕТ: решение владельца о форме отказа по происхождению.
**ФАЗА-СНИМАТЕЛЬ НЕ СОЧИНЯЕТСЯ.** Если роадмап называет фазу, которая эти обработчики трогает, — она
называется как фаза РАБОТЫ; решение о ФОРМЕ остаётся за владельцем, и это записывается двумя
адресатами, как это сделала четвёртая партия для окна 51.
⚠️ СОСТОЯНИЕ РЕШЕНИЯ — ЖДУЩЕЕ. Действующая константа модуля переиспользуется, новой не заводится.

**(5) Объявить число реестра с ЛЕТОПИСЬЮ:** 0 → замеренное, партия 5, настоящий план; чем измерено
(разбор дерева страничного слоя, вселенная правила `G-2`); что означает движение числа в каждую
сторону. Число ставится ПРОГОНОМ ПОКРАСНЕВШЕГО правила.

**(6) Завести три правила над реестром** по образцу соседнего: полноты (сличение множеств в обе
стороны), обоснований (пять полей непусты, условие снятия называет адресата, обоснование называет
цену следствием) и авторства (запись ссылается на решение либо на окно). Перечень подаётся
ПАРАМЕТРОМ, чтобы зубы показывались, а не заявлялись.

**(7) Показать зубы всех трёх** укороченным перечнем и записью с пустым полем; оба вывода привести
в сводку.

**(8) Записать ОТВЕРГНУТЫЙ ИСХОД И ЕГО ОСНОВАНИЕ** в шапке нового реестра — тремя следствиями из
раздела `<objective>`, а не ссылкой на план. Читатель реестра обязан узнать, почему выходы объявлены,
а не сняты, не выходя из файла.

**(9) Чего задача НЕ делает.** Не правит ни одного обработчика. Не меняет формы отказа по
происхождению. Не трогает реестра расхождения формы отказа валидации. Не трогает докстринга слоя
ответа (задачи 2 и 3).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — правила модуля не собраны, либо найденный замером собственный выход не объявлен реестром</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -k "own_response or builds_own or divergence"</automated>
    <fails_when>ненулевой код возврата, либо `no tests ran` в выводе — фильтр не собрал новых правил, то есть они названы так, что их не найти по предмету</fails_when>
    <automated>uv run pytest tests/ -q -m "not planning"</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — новые правила разошлись с продуктовой суитой</fails_when>
  </verify>

  <acceptance_criteria>
- КРАСНЫЙ РАСШИРЕННОГО РАЗБОРА БЕЗ РЕЕСТРА НАБЛЮДЁН: дословный вывод приведён в сводке, и в нём
  перечислены найденные выходы поимённо. Это и есть доказательство снятой слепоты.
- Число найденных снято ПРОГОНОМ и приведено в сводке рядом с ориентиром планировщика (тринадцать
  мест собственного отказа во всём страничном слое); названо, сколько из них попало во вселенную
  правила и почему остальные не попали.
- Перечень узнаваемых имён сборки объявлен КОНСТАНТОЙ с обоснованием: в сводке названо, сколько имён
  в нём и по какому признаку они выбраны.
- Объявленное число реестра поставлено прогоном покрасневшего правила: красный вывод с заведомо
  неверным числом приведён в сводке.
- Зубы всех трёх правил показаны: два вывода (укороченный перечень и запись с пустым полем)
  приведены в сводке.
- Обоснование каждой записи называет цену СЛЕДСТВИЕМ (ответ не свопается, признак неуспеха, панель
  без сообщения), а не словом «хуже»: в сводке приведена одна запись целиком как образец.
- Отвергнутый исход и три его основания записаны в шапке реестра: текст приведён в сводке.
- Правило `G-2` в прежней формулировке зелено: его исход приведён в сводке отдельно от нового.
  </acceptance_criteria>

  <done>
Разбор видит собственную сборку ответа любого вида; каждый найденный выход назван записью реестра с
пятью полями и ждущим состоянием; объявленное число поставлено прогоном красного; зубы трёх правил
показаны; отвергнутый исход записан с тремя основаниями.
  </done>
</task>

<task type="auto">
  <name>Задача 2: два утверждения слоя ответа сужаются до измеренного (`WR-04` половина записи, `WR-06`)</name>

  <files>
app/pages/htmx.py
  </files>

  <read_first>
- `app/pages/htmx.py:1-45` — докстринг модуля целиком, включая утверждение о единственном месте
  решения о форме ответа.
- `app/pages/htmx.py:340-380` — докстринг сборки ответа, включая абзац о коде исхода на фрагментной
  ветке и слова о закрытой границе. ⚠️ ЧИТАТЬ ВЕСЬ АБЗАЦ: там же записана ПРЕЖНЯЯ редакция,
  объявлявшая приклейку границей, которую функция не пересекает, — и форма её опровержения есть
  образец для настоящей правки.
- `app/pages/account_groups.py:589`, `app/pages/account_groups.py:826`,
  `app/pages/schedules.py:1263` — ВСЕ три вызова приложения, подающие фрагмент. ⚠️ ЧИТАТЬ КАЖДЫЙ И
  СМОТРЕТЬ, что подаётся вторым аргументом канала исхода: именно это и есть замер `WR-06`.
- `10-22-SUMMARY.md` — форма, которой четвёртая партия помечала утверждения опровергнутыми с
  воспроизводимым входом. Настоящая правка наследует её.
  </read_first>

  <behavior>
1. Утверждение модуля о единственном месте решения о форме ответа СУЖЕНО: оно называет число
   сегодняшних выходов в обход слоя и указывает, где они перечислены.
2. Утверждение о коде исхода на фрагментной ветке СУЖЕНО: оно говорит о СПОСОБНОСТИ, а не о
   поведении, называет число фрагментных вызовов и то, что ни один из них кода исхода не подаёт.
3. Слово о закрытой границе заменено на утверждение о заведённом впрок и покрытом суитой механизме.
4. Прежние формулировки помечены опровергнутыми с указанием круга и замера, а не стёрты.
5. Механизм не удаляется ни одной строкой.
  </behavior>

  <action>
**(1) Снять ОБА замера прогоном ДО правки текста.** Первый: сколько переведённых обработчиков несут
собственную сборку ответа (взять из сводки задачи 1). Второй: сколько вызовов приложения подают
фрагмент и сколько из них подаёт код исхода. ⚠️ ОРИЕНТИР ПЛАНИРОВЩИКА: три фрагментных вызова, ни
одного с кодом исхода. Исполнитель ставит СВОЙ замер; расхождение — находка.

**(2) Сузить утверждение модуля.** Новая редакция утверждает: слой есть единственное место, где
решение о форме ответа принимается ОСОЗНАННО И ОДНАЖДЫ; сегодня в страничном слое существует N
выходов, собирающих ответ помимо него; они перечислены ИМЕНОВАННЫМ РЕЕСТРОМ (назвать модуль суиты и
имя реестра), каждый со своим обоснованием и ждущим состоянием решения. ⚠️ ВЫВОД НЕ ОТМЕНЯЕТСЯ:
единственность остаётся ЦЕЛЬЮ слоя; сужается утверждение о её ДОСТИГНУТОСТИ.

**(3) Сузить утверждение о канале исхода.** Новая редакция утверждает: код исхода СПОСОБЕН доехать и
на ветке фрагмента внеполосным блоком, но сегодня этим не пользуется ни один обработчик — все N
фрагментных вызовов подают пустой код (назвать решение, по которому плашки на успех нет); механизм
заведён и покрыт суитой ЗАРАНЕЕ и станет ПОВЕДЕНИЕМ с первым фрагментом, несущим отказ.

**(4) Пометить обе прежние формулировки опровергнутыми, а не стереть.** По действующей идиоме: круг,
дата, замер, которым опровергнуто, и ВОСПРОИЗВОДИМЫЙ ВХОД — команда, которой замер повторяется.

**(5) Механизм не трогать.** Ни одной строки кода приклейки, сборки внеполосного блока и шаблона
блока. ⚠️ Удаление заведённого впрок механизма и сужение утверждения о нём — разные действия, и
второе не влечёт первого. Это прохибиция `must_haves` настоящего плана.

**(6) Чего задача НЕ делает.** Не правит вызовов приложения и не заводит первого фрагмента с кодом
исхода. Не трогает проверки статуса (задача 3). Не правит реестра (задача 1).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка докстрингов задела правила, читающие исходник слоя ответа</fails_when>
    <automated>uv run python -c "import ast,pathlib,sys; s=pathlib.Path('app/pages/htmx.py').read_text(encoding='utf-8'); t=ast.parse(s); names={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef))}; need={'_glue_notice','_notice_oob','location_response','respond'}; miss=sorted(need-names); sys.exit('механизм слоя ответа потерял функции: '+repr(miss) if miss else 0)"</automated>
    <fails_when>ненулевой код возврата — правка докстринга удалила функцию механизма, тогда как её предмет — только текст утверждений</fails_when>
    <automated>uv run pytest tests/ -q -m "not planning"</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка разошлась с продуктовой суитой</fails_when>
  </verify>

  <acceptance_criteria>
- Оба замера приведены в сводке ДО текста правки, с числами: выходов в обход слоя и фрагментных
  вызовов с кодом исхода. Ориентир планировщика (три фрагментных вызова, ни одного с кодом) назван
  рядом; расхождение, если оно есть, названо поимённо.
- Суженное утверждение модуля называет ЧИСЛО выходов и МЕСТО их перечня (модуль суиты и имя реестра):
  оба названы в сводке.
- Суженное утверждение о канале исхода называет число фрагментных вызовов и решение, по которому
  плашки на успех нет.
- Обе прежние формулировки помечены опровергнутыми с ВОСПРОИЗВОДИМЫМ ВХОДОМ: команда замера
  приведена в тексте пометки и продублирована в сводке; прогон этой команды в сводке показан.
- Ни одна функция механизма не удалена: вывод проверки состава функций приведён в сводке.
- Число удалённых строк диффа по файлу приведено в сводке, и каждая удалённая строка названа: она
  обязана принадлежать переписанному тексту утверждения, а не коду.
  </acceptance_criteria>

  <done>
Оба утверждения сужены до измеренного с указанием чисел и мест; прежние формулировки помечены
опровергнутыми с воспроизводимым входом; механизм цел.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: приклейка внеполосного блока перестаёт молчать на ответе, которому тело запрещено статусом (`WR-06`, вторая половина)</name>

  <files>
app/pages/htmx.py
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `app/pages/htmx.py:209-300` — приклейка внеполосного блока целиком: проверка типа содержимого,
  пересчёт длины, подмена тела. ⚠️ ЧИТАТЬ ВНИМАТЕЛЬНО: там уже стоят ДВЕ заботы — о типе содержимого
  и о длине, — и обе с обоснованиями. Статус есть ТРЕТИЙ инвариант того же правила, и его отсутствие
  видно ровно на фоне первых двух.
- `app/pages/htmx.py:133-150` — сборка ответа перехода со статусом, у которого тела нет по
  определению: образец того, какие статусы тела не имеют и почему это записано свойством статуса, а
  не обещанием кода.
- `tests/test_pages/test_htmx_response_layer.py` — действующие правила над слоем ответа: новое
  правило встаёт рядом с ними по форме, а не заводит своей.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `### WR-06`, оба вторичных
  дефекта.
  </read_first>

  <behavior>
1. Попытка приклеить внеполосный блок к ответу, чей статус запрещает тело, падает громко и называет
   статус в тексте отказа.
2. Приклейка к ответу с обычным успешным статусом и типом содержимого разметки работает как прежде.
3. Приклейка к ответу с чужим типом содержимого падает как прежде — прежняя забота не тронута.
4. Длина тела пересчитывается как прежде.
5. Текст отказа называет ПРИЧИНУ следствием: ответ ушёл бы с телом, которого его статус запрещает.
6. Граница безопасности подмены тела названа в докстринге: подмена безопасна ровно потому, что все
   сегодняшние сборщики фрагмента возвращают свежесобранный ответ.
  </behavior>

  <action>
**(1) RED ПЕРЕД GREEN.** Сначала правило: собрать ответ со статусом, у которого тела нет по
определению, подать в приклейку и потребовать громкого отказа. Прогнать, получить КРАСНЫЙ (сегодня
приклейка молчит и рождает нарушение протокола), привести дословный вывод в сводку. ⚠️ В сводке
назвать, ЧТО именно наблюдалось до правки: тело дописано и заголовок длины выставлен на ответе,
которому тело запрещено.

**(2) Добавить проверку статуса РЯДОМ с проверкой типа содержимого**, а не выше и не ниже наугад:
порядок назвать в комментарии. Диапазон запрещающих тело статусов перечислить явно и назвать
основание — те же статусы, о которых говорит сборка ответа перехода. Текст отказа называет статус
числом и следствие словами.

**(3) Назвать ГРАНИЦУ БЕЗОПАСНОСТИ ПОДМЕНЫ ТЕЛА** отдельным абзацем докстринга приклейки: подмена
тела у объекта ответа безопасна ровно потому, что все сегодняшние сборщики фрагмента возвращают
свежесобранный ответ, на котором вызывающий не успел ничего зарегистрировать. Назвать ЧИСЛО таких
сборщиков и указать, что появление сборщика, отдающего ответ с уже навешенными фоновыми задачами или
заголовками, эту границу снимет. ⚠️ Это НЕ правка кода: подмена не переписывается, называется её
условие. Ревизия просит именно этого — «keep the mechanism honest».

**(4) Три прежние заботы приклейки НЕ ТРОГАТЬ** и проверить прогоном, что они по-прежнему работают:
чужой тип содержимого, пересчёт длины, сборка блока окружением шаблонов.

**(5) Чего задача НЕ делает.** Не заводит первого фрагмента с кодом исхода. Не переписывает подмены
тела. Не трогает утверждений докстрингов (задача 2 сделала это раньше).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_response_layer.py -q</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — правила слоя ответа не собраны либо новая проверка задела прежние заботы приклейки</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_response_layer.py -q -k "status or no_body or glue"</automated>
    <fails_when>ненулевой код возврата, либо `no tests ran` в выводе — новое правило названо так, что его не найти по предмету</fails_when>
    <automated>uv run pytest tests/ -q -m "not planning"</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка приклейки разошлась с продуктовой суитой</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — исходник не собирается</fails_when>
  </verify>

  <acceptance_criteria>
- КРАСНЫЙ ДО ПРАВКИ НАБЛЮДЁН: дословный вывод приведён в сводке, и в нём названо, что до правки тело
  дописывалось к ответу, которому статус тело запрещает.
- Правило проверяет ОБЕ стороны: громкий отказ на запрещающем статусе И работу на обычном успешном
  статусе. Оба исхода приведены в сводке — без второго правило выполнялось бы приклейкой, падающей
  ВСЕГДА.
- Три прежние заботы приклейки зелены: их исходы приведены в сводке отдельными строками.
- Текст отказа называет статус ЧИСЛОМ и следствие словами: текст приведён в сводке.
- Абзац о границе безопасности подмены тела называет ЧИСЛО сегодняшних сборщиков фрагмента и условие,
  при котором граница снимается; число сличено с замером задачи 2.
- Подмена тела не переписана: в сводке приведено число изменённых строк внутри тела приклейки, и
  названо, какие из них относятся к новой проверке, а какие — к комментарию.
  </acceptance_criteria>

  <done>
Приклейка падает громко на ответе, которому тело запрещено статусом, и работает как прежде на
остальных; три прежние заботы целы; граница безопасности подмены тела названа числом и условием.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| слой ответа → рантайм разметки в браузере | форма ответа решает, увидит ли человек исход действия; ответ без тела и без заголовка перехода не свопается вовсе |
| сборщик фрагмента → приклейка внеполосного блока | приклейка подменяет тело чужого объекта ответа и пересчитывает его длину |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-31-01 | Repudiation | `app/pages/htmx.py` (утверждение о единственном месте) + гейт `G-2` | medium | mitigate | Утверждение о единственности стои́т рядом с выходами, его обходящими, и гейт, построенный ради его истинности, к ним слеп. Читатель верит утверждению и не ищет. Митигация — расширенный разбор плюс именованный реестр с объявленным числом (задача 1) и суженное утверждение (задача 2). ⚠️ МИТИГАЦИЯ ЕСТЬ ВИДИМОСТЬ, А НЕ СНЯТИЕ: снятие требует решения о форме ответа, запертого D-01 |
| T-10-31-02 | Denial of Service | отказ по происхождению на разметочном транспорте | medium | mitigate | Ответ без тела не свопается, обработчик завершения запроса видит признак неуспеха, панель остаётся открытой без сообщения — человек нажимает впустую. ⚠️ ФОРМА ОТВЕТА НЕ МЕНЯЕТСЯ ПЛАНОМ: цена названа следствием в обосновании каждой записи реестра, состояние решения ждущее, адресаты разведены (Фаза 11 снимает работой, решение о форме — владельца). Прецедент разведения — окно 51 |
| T-10-31-03 | Repudiation | докстринг сборки ответа (канал исхода на фрагментной ветке) | medium | mitigate | Утверждение о закрытой границе описывает СПОСОБНОСТЬ, а читается как ПОВЕДЕНИЕ: замер даёт ноль вызовов приложения, подающих фрагмент вместе с кодом исхода. Следующий автор обработчика полагает путь обкатанным, тогда как он не исполнялся ни разу. Митигация — сужение утверждения до измеренного с воспроизводимым входом (задача 2) |
| T-10-31-04 | Tampering | приклейка внеполосного блока к ответу без тела | medium | mitigate | Ответ со статусом, запрещающим тело, получил бы тело и заголовок длины — нарушение протокола вместо громкого отказа, разбираемое на стороне клиента. Статус есть ТРЕТИЙ инвариант того же правила, у которого первые два (тип содержимого и длина) уже стоят. Митигация — проверка статуса с текстом отказа, называющим статус числом (задача 3) |
| T-10-31-05 | Tampering | подмена тела чужого объекта ответа | low | mitigate | Подмена безопасна ровно потому, что все сегодняшние сборщики фрагмента возвращают свежесобранный ответ. Условие НЕ ЗАПИСАНО нигде, и первый сборщик, отдающий ответ с навешенными фоновыми задачами, снимет границу молча. Митигация — условие названо числом и записано там, где его прочтёт следующий сборщик (задача 3, пункт 3) |
| T-10-31-SC | Tampering | установки пакетов | low | accept | Установок нет: `pyproject.toml` не в `files_modified`, новых зависимостей не вводится |
</threat_model>

## Артефакты, которые создаёт этот план

- расширенный разбор собственной сборки ответа и константа перечня узнаваемых имён
- именованный реестр собственных выходов переведённых обработчиков, его объявленное число и летопись
- три правила над реестром: полноты, обоснований и авторства
- проверка статуса в приклейке внеполосного блока и текст её отказа
- абзац о границе безопасности подмены тела с числом сборщиков фрагмента

Полный перечень артефактов ПАРТИИ — `10-24-PLAN.md`, раздел `## Артефакты, которые создаёт эта партия`.

## Что этот план НЕ закрывает, и основание у каждого пункта своё

- **Исход (2) находки `WR-04` — перевод отказа по происхождению на слой ответа.** НЕ ОТЛОЖЕН, А НЕ
  ВЫБРАН, и основание названо ТРЕМЯ СЛЕДСТВИЯМИ, ни одно из которых не есть оценка труда: (а) это
  решение о ФОРМЕ ОТВЕТА, запертой решением владельца **D-01**, а чеканить изъятие из запертого
  решения плану запрещено — единственный прецедент изъятия (**D-08**) записан ВЛАДЕЛЬЦЕМ, и
  четвёртая партия уже разошлась с этим предметом окном 51, сделав расхождение видимым и решения не
  приняв; (б) перевод потребовал бы НОВОГО кода реестра уведомлений («источник запроса не
  подтверждён»), а новых кодов фаза не заводит по **D-03** с основанием, записанным вехой в Out of
  Scope; (в) он отменил бы записанное свойство «панель остаётся открытой на отказе», доказанное
  планом 10-07 на реальной форме события и стоящее пунктом ручного обхода с окном 22.
  **ФАЗА-ВЛАДЕЛЕЦ РЕШЕНИЯ О ФОРМЕ НЕ НАЗНАЧЕНА И НАЗНАЧЕНА БЫТЬ НЕ МОЖЕТ** — это решение владельца, а
  не фазы; фаза РАБОТЫ, если роадмап её называет, записывается отдельным адресатом.
- **Первый фрагментный ответ, несущий код исхода.** Не заводится: он был бы НОВЫМ ПРОДУКТОВЫМ
  ПОВЕДЕНИЕМ (плашка на фрагментном пути), а решение о том, на каких исходах плашка выдаётся, принято
  **D-03** и записано вехой. План сужает УТВЕРЖДЕНИЕ о механизме, а не вводит его первого потребителя.

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_response_layer.py -q` — зелено.
- `uv run pytest tests/ -q -m "not planning"` — зелено.
- `uv run python -m compileall -q app main.py tests` — код возврата ноль.
- Три наблюдённых красных приведены в сводке дословно: расширенный разбор без реестра, объявленное
  число реестра с заведомо неверным значением, приклейка к ответу без тела до правки.
- Два замера (выходов в обход слоя, фрагментных вызовов с кодом исхода) приведены в сводке числами.
- Отвергнутый исход и три его основания записаны и в шапке реестра, и в сводке.
</verification>

<success_criteria>
Гейт единственного решения о форме ответа видит собственную сборку ответа любого вида; каждый
найденный выход назван записью именованного реестра с пятью полями, обоснованием, называющим цену
следствием, и ждущим состоянием решения; два утверждения слоя ответа сужены до измеренного с
воспроизводимым входом и без удаления механизма; приклейка внеполосного блока падает громко на
ответе, которому тело запрещено статусом, и её граница безопасности названа числом и условием.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-31-SUMMARY.md` по завершении.
</output>
