---
phase: 10-rychag-components-modal-html
plan: 54
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, ПРИ `tdd_mode: true`, И ОСНОВАНИЕ НАЗЫВАЕТСЯ. Предмет плана —
# ТРИ ЗАМЕРА, КОТОРЫЕ СНИМАЕТ ЧЕЛОВЕК ГЛАЗОМ. Тест-первая последовательность здесь невыразима не
# потому, что трудно, а потому, что НЕЧЕМ: прямоугольник плашки есть произведение РАСКЛАДКИ на
# ПОЛОЖЕНИЕ ПРОКРУТКИ, а в суите нет ни движка раскладки, ни браузерного привода — греп по
# `pyproject.toml` и `tests/conftest.py` на `playwright|selenium|puppeteer|splinter` даёт НОЛЬ
# вхождений (замер 2026-09-11 на `d17c594`, переснят планировщиком 2026-09-12). Правило, написанное
# «тест-первым» на этот предмет, утверждало бы ОБЪЯВЛЕНИЕ вместо ПРЯМОУГОЛЬНИКА — ровно подмена,
# которой фаза уже оплатила запись 77 журнала окон.
wave: 2
# ⚠️ ВОЛНА 2 — ОДНА ЗАВИСИМОСТЬ ОТ ПЛАНА 10-52, И ОНА НЕ ФАЙЛОВАЯ. Перечни правимых файлов с планом
# 10-52 не пересекаются ни одним путём, и продуктового дерева настоящий план не касается вовсе —
# наблюдаемое поведение уже отгружено планами 10-49 и 10-51. Зависимость СМЫСЛОВАЯ и следует из
# порядка партии: партия ведётся ОДНИМ трассирующим срезом, проверенным ДО любого расширения, и
# человеческие замеры идут ПОСЛЕ него. Прикладная польза порядка тоже есть: человек, которому
# предъявляют процедуру, видит партию, у которой машинная половина уже зелена, — то есть его глаз
# тратится на то, что машине недоступно, а не на то, что она ещё не проверила.
depends_on: ["10-52"]
gap_closure: true
gap_ids: [G-10-7]
# ⚠️ ИДЕНТИФИКАТОР ЗДЕСЬ ИЗ РЕЕСТРА ОБХОДА, А НЕ ИЗ РАЗДЕЛА `gaps` ОДИННАДЦАТОГО КРУГА: настоящий
# план обслуживает пункт `missing[1]` записи `G-10-7` («Правило, стерегущее, что прямоугольник
# плашки отказа лежит в окне при рабочем положении прокрутки») — ЕДИНСТВЕННЫМ достижимым сегодня
# способом: замером ЧЕЛОВЕКА, а не правилом. ⚠️ И ОН ЭТОЙ ЗАПИСИ НЕ ЗАКРЫВАЕТ: запись закрывает
# ЗАМЕР, а не план, и до замера `G-10-7` остаётся `status: failed`.
files_modified:
  - .planning/phases/10-rychag-components-modal-html/10-UAT.md
autonomous: true
# ⚠️ `autonomous: true` ПРИ ТРЁХ ЧЕЛОВЕЧЕСКИХ ЗАМЕРАХ — ЭТО НЕ ПРОТИВОРЕЧИЕ, И ОНО ЗАМЕРЕНО:
# `config-get workflow.human_verify_mode` → `"end-of-phase"` (2026-09-12). В этом режиме задач
# `checkpoint:human-verify` планировщик НЕ ЭМИТИРУЕТ вовсе, а подробности проверки уезжают в блок
# `«verify» «human-check»` задачи, откуда их собирает верификатор в конце фазы и сводит в поток
# `human_needed` → `10-UAT.md`. Форма унаследована от планов 10-07, 10-10 и 10-16 этой же фазы —
# все три несут `«human-check»` и все три объявлены автономными. Значение `false` здесь означало бы
# `checkpoint:*`-задачу, которой в плане нет.
requirements: [FORM-06]

user_setup:
  - service: браузер с открытой консолью и вкладкой сетевых запросов плюс живой стенд приложения
    why: "Три открытых пункта перечня «Human Verification Required» одиннадцатого круга требуют ГЛАЗА, и машинного пути у проекта нет НИ ОДНОГО. Прямоугольник плашки есть произведение раскладки на положение прокрутки: `_css_rules_of` и соседи читают ОБЪЯВЛЕНИЯ таблицы стилей (движка раскладки за ними нет), `run_node_script` исполняет сценарий на СТАБАХ узлов (геометрии у стаба нет), браузерного привода в суите ноль вхождений. Настоящий обрыв транспорта сверх того есть свойство СРЕДЫ, а не дерева: воспроизвести его в суите значит подменить средство запроса — то есть вернуться ровно к тому, что сделало шаг 2.8 ненаблюдённым 2026-09-09."
    env_vars:
      - name: UAT10_EMAIL
        source: "поле `seed_env` шапки `10-UAT.md`; посев идёт под префиксом имён `UAT10-` (поле `stand_prefix`)"
    dashboard_config:
      - task: "Посеять стенд по разделу «Как привести стенд в нужное состояние» артефакта `10-UAT.md`: ОДНО объявление с НЕ МЕНЕЕ ЧЕМ ТРЕМЯ расписаниями под префиксом `UAT10-`. ⚠️ КОМАНДА ПОСЕВА ИСПОЛНИМА НА СЕГОДНЯШНЕЙ СХЕМЕ, И ЭТО ЗАМЕРЕНО: план 10-48 закрыл `G-10-A3` (посев падал на ограничении `ck_schedules_active_requires_next_run`), правило `tests/test_planning/test_the_walkthrough_stand_is_seedable.py` — 6 passed."
        location: "стенд наблюдения (решение о проде против localhost принимает человек — все три прежних обхода шли на ПРОДЕ по его прямому ответу на прямо заданный вопрос о риске)"
      - task: "Снять стенд по разделу «Как убрать стенд» СРАЗУ после наблюдения; три прежних обхода заканчивались замером `UAT10- rows: schedules=0 ads=0 accounts=0`, и он приводится в отчёте о наблюдении"
        location: "тот же стенд"

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (`config-get`, 2026-09-12): `security_asvs_level` → `1`,
# `security_block_on` → `"high"`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ. `estimate-calibration` на `9e6b859` (2026-09-12): `sample_count: 0`,
  # `factor: 1`, `applied: false`, `min_samples: 3`, `confidence: "low"`. `confidence` ВЫВЕДЕНА ИЗ
  # ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена себе. ⚠️ ПРОЕКЦИЯ СЧИТАЕТ РАБОТУ ИСПОЛНИТЕЛЯ (правка
  # одного артефакта двумя задачами), а НЕ время человека у браузера: последнее планом не
  # оценивается и оценке не подлежит.
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ЧЕЛОВЕК ПОЛУЧАЕТ ПРОЦЕДУРУ, А НЕ ПРОСЬБУ ПОСМОТРЕТЬ: у каждого из двух перезамеров названы стенд, последовательность действий, СПОСОБ вызвать настоящий отказ, ожидаемые величины ЧИСЛАМИ и признак, по которому замер считается НЕ снятым"
    - "ОСНОВАНИЕ ПЕРЕЗАМЕРА НАЗВАНО ДЕРЕВОМ, А НЕ РЕГЛАМЕНТОМ: между провалом шага 2.8 и настоящим планом лёг план 10-51 (подъём заготовок стал БЕЗУСЛОВНЫМ), и перезамер меряет именно эту правку — а не повторяет прежний замер ради формы"
    - "ЛОВУШКИ НАБЛЮДЕНИЯ НАЗВАНЫ ДО НАБЛЮДЕНИЯ, А НЕ ПОСЛЕ: успешный ФОНОВЫЙ обмен гасит плашку (окно 74), две одновременно показанные заготовки накрывают друг друга (окно 76), а окно обрыва follow-up GET есть величина ПЕРЕМЕННАЯ — человек, не знающий этих трёх, снял бы отрицательный признак с работающего дерева"
    - "ПОДПИСЬ НА ПРИЁМКЕ ОБЪЯВЛЕНА ЕДИНСТВЕННЫМ, ЧТО ЗАКРЫВАЕТ `FORM-06`, и назван вопрос, на который она отвечает: принять отметки шести проверок либо назвать поимённо те, что требуют перепрохождения"
    - statement: "САМОЗАВЕРЕНИЯ НЕ ПРОИСХОДИТ, И ЭТО ИЗМЕРЕНО, А НЕ ОБЕЩАНО: `status` шапки `10-UAT.md` остаётся `partial`, `checks_declared` остаётся `6`, ни одна клетка отметок не заполнена, ни один флажок `FORM-06` не поставлен — и всё это предъявлено диффом и прогоном планировочных гейтов"
      verification: test
    - statement: "ПРЕДИКАТ, ЧИТАЮЩИЙ ТРИ ЗАГОЛОВКА ПУНКТОВ, НЕ СЛОМАН: приписка настоящего плана НЕ несёт заголовка вида «### N.» — четвёртый такой заголовок сломал бы счёт `phase uat-passed`, и запись об этом стои́т в самом артефакте разметочным комментарием"
      verification: test
  artifacts:
    - "`.planning/phases/10-rychag-components-modal-html/10-UAT.md` — ОДНА приписка с заголовком НЕ вида «### N.», несущая процедуру двух перезамеров с ожидаемыми величинами, перечень трёх ловушек наблюдения и вопрос подписи на приёмке; отметок не заполнено ни одной"
  key_links:
    - "ожидаемые величины ЧИСЛАМИ → отличимость снятого признака от несnятого: единственное, чем «плашка видна» перестаёт быть суждением; ломается — человек возвращает слово вместо замера, и четвёртый обход даёт то же, что три прежних"
    - "перечень ловушек наблюдения → законность отрицательного признака: единственное, чем отказ, вызванный фоновым обменом или наложением двух заготовок, отличим от отказа дерева; ломается — человек сообщает дефект, которого нет, либо пропускает дефект, который есть"
    - "нетронутые `status` и `checks_declared` → незакрытие фазы машиной: единственное, чем подготовка процедуры отличима от объявления обхода пройденным; ломается — правило `test_the_walkthrough_cannot_self_certify` краснит прогон, и оно для этого и живёт"
  prohibitions:
    - statement: "ПОЛЕ `status` ШАПКИ `10-UAT.md` ТЕРМИНАЛЬНЫМ НЕ СТАНОВИТСЯ: значения `passed` и `complete` запрещены до подписи человека на приёмке, и ради запрета этого самозаверения живёт `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`. Настоящий план подписи не ставит и поставить не может"
      status: flagged-unverified
    - statement: "НИ ОДНА КЛЕТКА ШЕСТИ ТАБЛИЦ ОТМЕТОК НЕ ЗАПОЛНЯЕТСЯ, И НИ ОДНА ОТМЕТКА ПРОХОЖДЕНИЯ НЕ СТАВИТСЯ: артефакт под каждой таблицей говорит «заполняет человек на приёмке», и исполнитель, заполнивший её наблюдением агента, повторил бы ровно то, из-за чего `status` держится нетерминальным третий обход подряд"
      status: flagged-unverified
    - statement: "`checks_declared: 6` НЕ ДВИГАЕТСЯ, НОВЫХ РАЗДЕЛОВ `## Проверка N` НЕ ЗАВОДИТСЯ И НОВЫХ ТАБЛИЦ `### Отметка` НЕ ЗАВОДИТСЯ: три числа обхода сличаются правилом `test_the_number_of_walkthrough_checks_agrees_three_ways`, и приписка есть ПРОЗА, а не седьмая проверка"
      status: flagged-unverified
    - statement: "ЗАГОЛОВОК ПРИПИСКИ НЕ ИМЕЕТ ВИДА «### N.»: три заголовка пунктов раздела открытых человеческих вопросов читает предикат `phase uat-passed`, и четвёртый сломал бы его счёт. Запись об этом уже стои́т в артефакте разметочным комментарием приписки третьего обхода и настоящим планом ПОВТОРЯЕТСЯ, а не выводится заново"
      status: flagged-unverified
    - statement: "`FORM-06` НЕ ОТМЕЧАЕТСЯ ВЫПОЛНЕННЫМ: ни флажок `.planning/REQUIREMENTS.md:31`, ни клетка `:136` (`Gaps Found`) — требование закрывается ПОДПИСЬЮ ЧЕЛОВЕКА на приёмке обхода, и правило `test_no_requirement_is_marked_complete_before_its_phase_verification_passed` стережёт именно это"
      status: flagged-unverified
    - statement: "ЗАПИСЬ ГЭПА `G-10-7` НЕ ОБЪЯВЛЯЕТСЯ ЗАКРЫТОЙ НИ ПЛАНОМ, НИ ЕГО СВОДКОЙ: её закрывает ЗАМЕР ЧЕЛОВЕКА, а не подготовка процедуры для этого замера. До замера запись остаётся `status: failed`"
      status: flagged-unverified
    - statement: "`app/` НЕ ПРАВИТСЯ НИ НА СИМВОЛ: наблюдаемое поведение отгружено планами 10-49 (гашение по признаку успеха) и 10-51 (безусловный подъём), и правка дерева в ходе подготовки перезамера сделала бы предмет наблюдения ДРУГИМ, чем тот, ради которого перезамер назначен"
      status: flagged-unverified
    - statement: "ПРЕЖНИЕ ЗАПИСИ ШАПКИ И ПОДПИСЕЙ НЕ ВЫЧЁРКИВАЮТСЯ НИ ОДНА: блоки `walkthrough`, `walkthrough_2`, `walkthrough_3`, все приписки и все прежние подписи остаются ДОСЛОВНО, и рядом с ними встаёт то, что их переросло (идиома D-30/D-32). Прежняя запись `{top: 96}` перезамера 2026-09-11 ВЕРНА для своего замера и не правится: признак зависит от прокрутки, а не от дерева"
      status: flagged-unverified
    - statement: "СТЕНД НЕ СЕЕТСЯ ИСПОЛНИТЕЛЕМ АВТОМАТИЧЕСКИ: ключ `workflow.live_dom_uat` равен `false`, все три прежних обхода шли на ПРОДЕ по прямому ответу человека на прямо заданный вопрос о риске, и посев обязан жить РОВНО во время наблюдения — иначе в боевой базе остаются строки, за которые никто не отвечает. Команды посева и уборки приводятся в артефакте ДОСЛОВНО, исполняет их человек либо агент с его разрешением в сессии"
      status: flagged-unverified
  assumptions:
    - statement: "РЕБРО `idempotency` ТРЕБОВАНИЯ FORM-06 («что произойдёт, если это исполнить дважды на том же входе») НАСТОЯЩИМ ПЛАНОМ НЕ РАЗРЕШАЕТСЯ, И ПРЕДМЕТ ЕГО ЗДЕСЬ ИМЕННО ПРОДУКТОВЫЙ: «после N удалений в документе не остаётся N мёртвых диалогов» есть ВТОРАЯ ПОЛОВИНА ЦЕЛИ ФАЗЫ, и она стои́т `PRESENT_BEHAVIOR_UNVERIFIED` в одиннадцатом круге — контракт ответа есть и стережётся, ЭФФЕКТА не утверждает ни одно правило, и это говорит сама суита (`tests/test_pages/test_confirm_delete_transport.py:80`). Настоящий план готовит процедуру для ТРЁХ открытых пунктов и проверки 1 (счёт диалогов до и после каждого удаления) СРЕДИ НИХ НЕТ: её признак снят тремя обходами (4→3→2, оба узла `null`), и открыт у неё ровно тот же третий пункт — ПОДПИСЬ. ⚠️ Ребро закрывается ПОДПИСЬЮ на приёмке, а не планом, и объявлять его разрешённым по трём замерам агента запрещено"
      edge: idempotency
      status: unresolved
    - statement: "РЕБРО `concurrency` ТРЕБОВАНИЯ FORM-06 («что гарантировано при прерывании либо параллельном исполнении») НАСТОЯЩИМ ПЛАНОМ НЕ РАЗРЕШАЕТСЯ, И ОНО ЗДЕСЬ НЕ АБСТРАКТНО: корень гэпа `G-10-7` ЕСТЬ ГОНКА — панель уходит по завершению POST РАНЬШЕ, чем поднимается отказ провалившегося follow-up GET, и плашка застаёт документ уже без признака открытой панели. План 10-51 снял ЗАВИСИМОСТЬ подъёма от этого признака, то есть обезвредил ПОСЛЕДСТВИЕ гонки, а самой гонки не убрал — порядок событий остался прежним. Второе лицо того же ребра: успешный ФОНОВЫЙ обмен гасит непрочитанный настоящий отказ (окно 74 / находка ревизии `IN-02`), и это названо ЛОВУШКОЙ НАБЛЮДЕНИЯ прямо, потому что иначе человек снял бы отрицательный признак с работающего дерева. Ни одно из двух лиц планом не закрывается"
      edge: concurrency
      status: unresolved
---

<objective>
**Три пункта закрывает человек, и ни один из них не закрыт машиной ни разу за три обхода.** Так
говорит раздел «Human Verification Required» одиннадцатого круга; первые три его пункта — предмет
настоящего плана.

**ПУНКТ 1 — ШАГ 2.8, ПЕРЕЗАМЕР ПОСЛЕ БЕЗУСЛОВНОГО ПОДЪЁМА.** Оборвать связь на follow-up GET после
удаления расписания и снять прямоугольник плашки обрыва при РАБОЧЕМ положении прокрутки. Замер
2026-09-11 (`walkthrough_3`, стенд ad_id=59 / sched-165) дал прямоугольник `{top: −701, left: 294,
944×63}` при `scrollY: 797`, `position: static`, `z-index: auto`, `elementFromPoint` в её точке её не
возвращает — **глазной вердикт по снимку: объяснения на экране НЕТ.** Это пункт `missing[1]` записи
`G-10-7` и запись 77 журнала окон.

**ПУНКТ 2 — ШАГ 4.4, ПЕРЕЗАМЕР ПОСЛЕ ТОЙ ЖЕ ПРАВКИ.** Открыть панель подтверждения, вызвать отказ,
нажать «Удалить», посмотреть, что встало поверх.

**ПУНКТ 3 — ПОДПИСЬ ЧЕЛОВЕКА НА ПРИЁМКЕ.** Принять отметки шести проверок либо назвать требующие
перепрохождения. **ЭТОЙ ПОДПИСЬЮ ЗАКРЫВАЕТСЯ `FORM-06`** — и ничем иным: признаки снимал АГЕНТ при
`workflow.live_dom_uat: false`, `status` артефакта равен `partial`, и
`tests/test_planning/test_the_walkthrough_cannot_self_certify.py` запрещает самозаверение прямо.

Purpose: между провалом шага 2.8 и настоящим днём лёг план 10-51 — он снял с подъёма заготовок
условие открытой панели, то есть УБРАЛ КОРЕНЬ, названный записью `G-10-7`. Перезамер меряет ИМЕННО
эту правку. ⚠️ И граница доказанного названа прямо: правило 10-51 утверждает ОБЪЯВЛЕНИЕ таблицы
стилей, а пункт требует ПРЯМОУГОЛЬНИКА, и подменять одно другим запрещено — это разные предметы, и
запись 77 журнала окон заведена ровно об этой разнице.

Output: одна приписка к `10-UAT.md`, несущая процедуру двух перезамеров с ожидаемыми величинами
ЧИСЛАМИ, перечень трёх ловушек наблюдения и вопрос подписи на приёмке. Отметок не заполнено ни одной,
`status` остался `partial`.

⚠️ **ЧЕГО ЭТОТ ПЛАН НЕ ДЕЛАЕТ, И ЭТО НАЗЫВАЕТСЯ ПЕРВЫМ.**

1. **ОН НЕ СНИМАЕТ ПРИЗНАКИ САМ, И НЕ ПОТОМУ, ЧТО ЗАПРЕЩЕНО РЕГЛАМЕНТОМ.** Признаки, снятые агентом,
   у этой фазы УЖЕ ЕСТЬ — трижды, — и ровно поэтому `status` держится нетерминальным третий обход
   подряд. Четвёртый агентский замер не сложится с тремя прежними в одну подпись: они отвечают на
   вопрос «нарисовал ли браузер», а открытый пункт спрашивает «подписал ли человек».
2. **ОН НЕ СТАВИТ `status` ТЕРМИНАЛЬНЫМ И НЕ ОТМЕЧАЕТ `FORM-06`.** Соблазн здесь максимален и назван
   самим артефактом: число проваленных шагов упало до нуля, и «раз всё сошлось» выглядит как
   закрытие. Запрет писался ровно на этот случай.
3. **ОН НЕ ЗАКРЫВАЕТ `G-10-7`.** Запись закрывает ЗАМЕР, а не процедура для замера.
4. **ОН НЕ ЗАВОДИТ БРАУЗЕРНОГО ПРИВОДА В СУИТЕ.** Это РЕШЕНИЕ ВЛАДЕЛЬЦА, а не правка плана гэпа: оно
   стои́т дороже самого гэпа (новая зависимость, новый род прогона, новое время суиты поверх
   сегодняшних тридцати семи минут) и передаётся названным. Пока его нет, класс «правило на
   отрисовку» недостижим для ЛЮБОГО плана этой фазы.

⚠️ **ТРИ ЛОВУШКИ НАБЛЮДЕНИЯ НАЗВАНЫ ЗДЕСЬ, А НЕ ОСТАВЛЕНЫ ЧЕЛОВЕКУ НА ОТКРЫТИЕ.** Каждая способна
дать ОТРИЦАТЕЛЬНЫЙ признак на работающем дереве, и человек, о них не знающий, сообщил бы дефект,
которого нет.

- **Успешный ФОНОВЫЙ обмен гасит обе заготовки** (окно 74, `T-10-49-05`, disposition `accept`,
  находка ревизии `IN-02`). Третий обработчик, заведённый планом 10-49, различает УСПЕХ, а не
  завершение, — и гасит плашку на успехе ЛЮБОГО обмена, включая сентинель подгрузки, автосохранение
  и опрос. Плашка, ушедшая с экрана через секунду после появления, есть ЭТО, а не дефект подъёма.
- **Две заготовки, показанные одновременно, накрывают друг друга** (окно 76, `T-10-51-04`,
  disposition `accept`, владельца нет, находка ревизии `IN-03`). Величины блока подъёма у них общие,
  и вторая встаёт ровно в точку первой. До плана 10-51 это случалось при открытой панели, теперь —
  всегда, пока обе видимы.
- **Окно обрыва follow-up GET есть величина ПЕРЕМЕННАЯ.** Замер 2026-09-11: первая попытка обрыва
  НЕ ДАЛА его вовсе (GET → 200), потому что полёт GET оказался 249 мс против 2635 мс перезамера при
  том же ограничении канала. Отсутствие обрыва с первой попытки есть свойство окна, а не признак
  дерева.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-UAT.md
@.planning/phases/10-rychag-components-modal-html/10-51-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**Новые записи в `.planning/phases/10-rychag-components-modal-html/10-UAT.md`** (символов кода план
не заводит ни одного):

- ОДНА приписка с заголовком НЕ вида «### N.» — по форме приписки третьего обхода, заведённой
  2026-09-11;
- внутри неё: процедура перезамера шага 2.8 с ожидаемыми величинами числами; процедура перезамера
  шага 4.4 с ожидаемыми величинами числами; перечень ТРЁХ ловушек наблюдения; вопрос подписи на
  приёмке с шестью проверками, перечисленными поимённо; команды посева и уборки стенда ДОСЛОВНО;
- разметочный комментарий приписки, повторяющий запрет на заголовок вида «### N.» и на движение трёх
  чисел обхода.

**Ни одного нового раздела `## Проверка N`, ни одной новой таблицы `### Отметка`, ни одной
заполненной клетки отметок, ни одного изменённого поля шапки.**
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Задача 1: процедура двух перезамеров встаёт в артефакт обхода с ожидаемыми величинами числами и тремя названными ловушками</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1-30` — шапка ЦЕЛИКОМ: `status`,
  `checks_declared: 6`, `stand_prefix`, `seed_env` и все комментарии-записи прежних дней. ⚠️ ЧИТАТЬ
  ДОСЛОВНО И НЕ ПРАВИТЬ НИ ОДНОГО ПОЛЯ. Комментарии шапки суть записи по идиоме D-30/D-32, и они
  остаются.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1110-1210` — раздел трёх открытых
  человеческих пунктов и приписка третьего обхода ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ОСОБЕННО ВНИМАТЕЛЬНО
  разметочный комментарий приписки: он говорит, что заголовок НАМЕРЕННО не вида «### N.», потому что
  три заголовка пунктов читает предикат `phase uat-passed` и четвёртый сломал бы его счёт. ФОРМА
  ПРИПИСКИ НАСЛЕДУЕТСЯ ОТСЮДА ДОСЛОВНО.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:611-705` — раздел «Проверка 2» целиком,
  включая шаг 2.8 и его подшаги 2.3-2.7. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: процедура перезамера ССЫЛАЕТСЯ на
  номера шагов, и ссылка обязана быть верной, а не правдоподобной.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:739-785` — раздел «Проверка 4» целиком,
  включая ДВА способа вызвать отказ и разницу между ними. ⚠️ ЧИТАТЬ ДОСЛОВНО: способ (б) требовал
  правки боевого дерева и потому был неприменим 2026-09-09; процедура обязана назвать, какой способ
  применим сегодня.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:643` — строка таблицы третьего замера
  шага 2.8: величины `{top: −701, left: 294, 944×63}`, `scrollY: 797`, `position: static`,
  `z-index: auto`, способ обрыва (Offline армируется ВО ВРЕМЯ полёта POST) и запись о первой попытке,
  обрыва не давшей. ⚠️ ОТСЮДА БЕРУТСЯ И СПОСОБ, И ОЖИДАЕМЫЕ ВЕЛИЧИНЫ ДЛЯ СЛИЧЕНИЯ.
- `.planning/phases/10-rychag-components-modal-html/10-51-SUMMARY.md` — чем именно снято условие
  подъёма и какие величины блока НЕ СДВИНУТЫ. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: ожидаемые величины перезамера
  выводятся ИЗ ЭТОЙ ПРАВКИ, а не из прежнего замера.
- `app/static/css/app.css:1188-1198` — блок подъёма после плана 10-51: селектор без предков,
  `position: fixed`, отступ сверху, слой 70 против 60 у панели. ⚠️ ЧИТАТЬ И НЕ ПРАВИТЬ: ожидаемые
  числа процедуры берутся ИЗ ТАБЛИЦЫ, а не из памяти о ней, и вторая их копия в артефакте разошлась
  бы с первой молча — поэтому в процедуре они приводятся С УКАЗАНИЕМ ИСТОЧНИКА.
- `.planning/WINDOWS.md`, записи 74, 76 и 77 ЦЕЛИКОМ — три ловушки наблюдения и основание
  непокрытия пункта `missing[1]`.
- `tests/test_planning/test_the_walkthrough_cannot_self_certify.py:60-90` и `:344-390` — словарь
  терминальных состояний, правило согласия трёх чисел и правило пустых отметок. ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО: правка артефакта не имеет права уронить ни одно из них, и единственный способ это
  знать — прочесть, что они читают.
- `tests/test_planning/test_the_walkthrough_stand_is_seedable.py` — что именно правило проверяет в
  команде посева. ⚠️ ЧИТАТЬ: команды посева и уборки приводятся в приписке ДОСЛОВНО из артефакта, и
  расхождение с проверяемым правилом краснит прогон.
  </read_first>

  <action>
**(1) ПРИПИСКА ОДНА, И ЗАГОЛОВОК ЕЁ НЕ ВИДА «### N.».** Форма наследуется от приписки третьего
обхода: заголовок с пометкой, под ним разметочный комментарий, повторяющий запрет (ни одного раздела
«## Проверка N», ни одной таблицы «### Отметка», `checks_declared` не двигается, `status` не
двигается, заголовок намеренно не нумерован — предикат `phase uat-passed` читает ТРИ заголовка
пунктов, и четвёртый сломал бы его счёт). ⚠️ ЗАПРЕТ ПОВТОРЯЕТСЯ, А НЕ ВЫВОДИТСЯ ЗАНОВО: он уже
записан в артефакте, и вторая его формулировка разошлась бы с первой молча.

**(2) ПРОЦЕДУРА ШАГА 2.8 — ПЯТЬ ВЕЛИЧИН, И КАЖДАЯ НАЗВАНА ЧИСЛОМ ИЛИ СЛОВОМ, А НЕ СУЖДЕНИЕМ.**
Последовательность: посеять стенд; открыть экран правки объявления; удалить расписание; оборвать
связь на follow-up GET СПОСОБОМ, ЗАМЕРЕННЫМ 2026-09-11 (ограничение канала плюс Offline,
армируемый ВО ВРЕМЯ ПОЛЁТА POST; вызов нажатия и вызов обрыва пускаются ОДНИМ БЛОКОМ — привод не
рвёт уже летящие запросы, поэтому POST доходит, а выпущенный следом GET упирается в разрыв канала);
снять признаки ПРИ РАБОЧЕМ ПОЛОЖЕНИИ ПРОКРУТКИ, а не у верхнего края.
Снимаются и приводятся: (а) событие и статус провалившегося запроса; (б) прямоугольник плашки
целиком — четыре числа; (в) значение `scrollY` в момент снятия прямоугольника; (г) способ
позиционирования и слой; (д) что возвращает `elementFromPoint` в точке плашки; (е) ГЛАЗНОЙ ВЕРДИКТ
ПО СНИМКУ ЭКРАНА — видно ли объяснение человеку, не прокручивавшему страницу.
⚠️ ОЖИДАНИЕ ФОРМУЛИРУЕТСЯ ЧЕРЕЗ НЕЗАВИСИМОСТЬ ОТ ПРОКРУТКИ, А НЕ ЧЕРЕЗ ОДНО ЧИСЛО `top`: подъём
стал безусловным, значит прямоугольник обязан лежать в окне ПРИ ЛЮБОМ `scrollY`, и замер при
`scrollY`, близком к нулю, признака НЕ СНИМАЕТ — именно так один положительный замер 2026-09-11
(`{top: 96}`) и не закрыл признака. Процедура говорит это прямо и требует НЕНУЛЕВОГО `scrollY`,
приведённого числом.
⚠️ ЧИСЛА БЛОКА ПОДЪЁМА В ПРИПИСКУ НЕ ПЕРЕПИСЫВАЮТСЯ: приписка называет ИСТОЧНИК
(`app/static/css/app.css`, блок подъёма) и требует сличить наблюдённое с ним. Вторая копия числа
разошлась бы с таблицей молча — та же доктрина, по которой правила 10-51 читают оба слоя из таблицы.

**(3) ПРОЦЕДУРА ШАГА 4.4 — ЧЕТЫРЕ ВЕЛИЧИНЫ.** Последовательность: открыть панель подтверждения
удаления; вызвать отказ ПРИМЕНИМЫМ СПОСОБОМ (назвать, какой из двух способов раздела «Проверка 4»
применим сегодня и почему второй неприменим); нажать «Удалить»; посмотреть ГЛАЗОМ, что встало
поверх. Снимаются: (а) прямоугольник плашки и прямоугольник панели; (б) слои обеих; (в) что
возвращает `elementFromPoint` в точке плашки; (г) осталась ли панель открытой и повторяется ли
действие БЕЗ переоткрытия панели. Плюс глазной вердикт по снимку.
⚠️ ЛОВУШКА ОКНА 76 НАЗЫВАЕТСЯ ЗДЕСЬ ЖЕ: если видимы ОБЕ заготовки, они встают в одну точку и
накрывают друг друга — наблюдатель обязан назвать, СКОЛЬКО заготовок видимо, прежде чем судить о
наложении плашки и панели.

**(4) ТРИ ЛОВУШКИ НАБЛЮДЕНИЯ — ОТДЕЛЬНЫМ ПЕРЕЧНЕМ, ДО ПРОЦЕДУР, А НЕ ПОСЛЕ.** Успешный фоновый
обмен гасит обе заготовки (окно 74); две видимые заготовки накрывают друг друга (окно 76); окно
обрыва follow-up GET переменно и с первой попытки может не дать обрыва (замер 2026-09-11). У каждой
названо, КАКОЙ ОТРИЦАТЕЛЬНЫЙ ПРИЗНАК она способна дать на работающем дереве.

**(5) КОМАНДЫ ПОСЕВА И УБОРКИ СТЕНДА ПРИВОДЯТСЯ ДОСЛОВНО ИЗ АРТЕФАКТА, А НЕ ПЕРЕСКАЗЫВАЮТСЯ.**
⚠️ И РЯДОМ — ЗАМЕР ИХ ИСПОЛНИМОСТИ: команда посева упала 2026-09-11 на ограничении схемы
(`ck_schedules_active_requires_next_run`, гэп `G-10-A3`), план 10-48 это закрыл, и правило
`tests/test_planning/test_the_walkthrough_stand_is_seedable.py` держит соответствие схеме — 6
passed. Наблюдатель обязан знать, что прибор проверен, прежде чем идти к стенду.

**(6) ПРЕЖНИЕ ЗАПИСИ НЕ ВЫЧЁРКИВАЮТСЯ НИ ОДНА.** Запись `{top: 96}` перезамера 2026-09-11 ВЕРНА для
своего замера и остаётся; рядом встаёт то, что её переросло — признак зависит от прокрутки, и один
положительный замер его не закрывает. То же для всех трёх блоков `walkthrough*` и всех прежних
подписей.

**(7) НИ ОДНА КЛЕТКА ОТМЕТОК НЕ ЗАПОЛНЯЕТСЯ, И ПОЛЯ ШАПКИ НЕ ДВИГАЮТСЯ.** `status` остаётся
`partial`, `checks_declared` остаётся `6`. Приписка есть ПРОЗА и ПРОЦЕДУРА, а не прохождение.
  </action>

  <acceptance_criteria>
- Приписка в артефакте РОВНО ОДНА, и её заголовок НЕ имеет вида «### N.» — проверяется счётом
  заголовков этого вида в разделе: их по-прежнему ТРИ.
- Под заголовком приписки стои́т разметочный комментарий, повторяющий запрет на нумерованный
  заголовок, на новые разделы проверок, на новые таблицы отметок и на движение трёх чисел обхода.
- Процедура шага 2.8 называет ШЕСТЬ снимаемых величин (событие и статус; четыре числа
  прямоугольника; `scrollY`; способ позиционирования и слой; ответ `elementFromPoint`; глазной
  вердикт) и ТРЕБУЕТ НЕНУЛЕВОГО `scrollY`, приведённого числом.
- Процедура шага 2.8 называет способ обрыва ДОСЛОВНО тем, каким он замерен 2026-09-11 (Offline
  армируется во время полёта POST; нажатие и обрыв пускаются одним блоком), и прямо говорит, что
  подмена средства запроса замером НЕ СЧИТАЕТСЯ.
- Процедура шага 4.4 называет ЧЕТЫРЕ снимаемых величины плюс глазной вердикт и требует назвать
  ЧИСЛО видимых заготовок прежде суждения о наложении.
- Ожидаемые величины блока подъёма в приписку НЕ ПЕРЕПИСАНЫ числами: назван ИСТОЧНИК
  (`app/static/css/app.css`, блок подъёма) и требование сличить с ним.
- Перечень трёх ловушек наблюдения стои́т ДО процедур, и у каждой названо, какой отрицательный
  признак она способна дать на работающем дереве.
- Команды посева и уборки приведены ДОСЛОВНО, и рядом приведён замер исполнимости посева (правило
  10-48, 6 passed).
- Прежние записи и подписи присутствуют в файле дословно и не правлены — проверяется диффом: в
  диффе нет ни одной удалённой строки прежних блоков `walkthrough*` и прежних подписей.
- Поля шапки стоя́т теми же значениями — `status: partial`, `checks_declared: 6`,
  `requirement: FORM-06`, `stand_prefix: UAT10-`, `seed_env: UAT10_EMAIL` — утверждается
  СОДЕРЖИМЫМ файла, а не диффом (дифф без закреплённой ревизии по зафиксированной правке пуст и прошёл бы вакуумно); ни одна
  клетка шести таблиц отметок не
  заполнена.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — приписка уронила гейт самозаверения обхода, гейт согласия трёх чисел либо гейт исполнимости посева. ⚠️ ПЛАНИРОВОЧНЫЕ ПРАВИЛА ПРОГОНЯЮТСЯ ЯВНО, А НЕ В СОСТАВЕ ПОЛНОЙ СУИТЫ: исполнители их деселектят, и дефекты планировочных записей видит только явный прогон</fails_when>
    <automated>grep -c -e '^status: partial$' -e '^checks_declared: 6$' -e '^requirement: FORM-06$' -e '^stand_prefix: UAT10-$' -e '^seed_env: UAT10_EMAIL$' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод НЕ РАВЕН "5" — хотя бы одно поле шапки сдвинуто, то есть план подошёл к самозаверению вплотную. ⚠️ ЭТО УТВЕРЖДЕНИЕ О СОДЕРЖИМОМ ФАЙЛА, А НЕ О ДИФФЕ, И ЭТО РЕШЕНИЕ: проверка диффа без закреплённой ревизии по уже ЗАФИКСИРОВАННОЙ правке пуста, и такой гейт проходил бы ВАКУУМНО ровно после коммита задачи. Пять объявленных значений сняты планировщиком 2026-09-12 на `9e6b859` командой того же вида — вывод был "5"</fails_when>
    <automated>git diff --numstat 9e6b859 -- .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>ВТОРОЕ число вывода (столбец удалённых строк) не равно нулю — значит из артефакта УДАЛЕНЫ строки, а прежние записи не вычёркиваются ни одна: приписка только ДОБАВЛЯЕТ. Пустой вывод тоже есть отказ: приписка не внесена вовсе. ⚠️ РЕВИЗИЯ ЗАКРЕПЛЕНА (`9e6b859` — база партии) И БЕЗ `..HEAD` — сравнение с РАБОЧИМ ДЕРЕВОМ, поэтому проверка видит правку и до коммита, и после него; проверка без закреплённой ревизии дала бы пустоту по зафиксированной правке и объявила бы «ничего не удалено» о файле, которого не смотрела. ⚠️ И ЭТО ОДНА КОМАНДА БЕЗ КОНВЕЙЕРА: конвейер из двух команд отдаёт код возврата ПОСЛЕДНЕЙ, и сломанная первая читалась бы как чистый результат</fails_when>
    <automated>grep -c '^walkthrough' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод НЕ РАВЕН "3" — блоков прохождения в шапке по-прежнему должно быть три (`walkthrough`, `walkthrough_2`, `walkthrough_3`): меньше означает вычеркнутую прежнюю запись, больше — что план завёл блок четвёртого прохождения, которого он не проводил</fails_when>
    <automated>grep -c '^### [0-9]\.' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод НЕ РАВЕН "3" — четвёртый нумерованный заголовок сломал бы счёт предиката `phase uat-passed`, а меньшее число означает, что один из трёх заголовков потерян правкой</fails_when>
    <human-check>ЧЕЛОВЕК СНИМАЕТ ДВА ПЕРЕЗАМЕРА НА ЖИВОМ СТЕНДЕ И СООБЩАЕТ ВЕЛИЧИНЫ, А НЕ СУЖДЕНИЕ.
ШАГ 2.8: после удаления расписания оборвать связь на follow-up GET способом, названным в приписке
(ограничение канала плюс Offline, армируемый во время полёта POST), и при НЕНУЛЕВОМ `scrollY`
сообщить шесть величин — событие и статус провалившегося запроса, четыре числа прямоугольника
плашки, само значение `scrollY`, способ позиционирования и слой, что возвращает `elementFromPoint` в
точке плашки, и глазной вердикт по снимку экрана (видно ли объяснение человеку, не прокручивавшему
страницу). Ожидается: прямоугольник ЦЕЛИКОМ в окне при ненулевом `scrollY`, способ позиционирования
и слой сходятся с блоком подъёма `app/static/css/app.css`, `elementFromPoint` возвращает узел внутри
плашки, объяснение на экране ЕСТЬ. Замер при `scrollY`, близком к нулю, признака НЕ СНИМАЕТ.
ШАГ 4.4: открыть панель подтверждения, вызвать отказ, нажать «Удалить» и сообщить четыре величины —
прямоугольники плашки и панели, их слои, ответ `elementFromPoint` в точке плашки, осталась ли панель
открытой и повторяется ли действие без её переоткрытия, — плюс ЧИСЛО видимых заготовок и глазной
вердикт. Ожидается: плашка поверх панели, панель открыта, повтор без переоткрытия.
⚠️ ТРИ ЛОВУШКИ, ПРИ КОТОРЫХ ОТРИЦАТЕЛЬНЫЙ ПРИЗНАК НЕ ЕСТЬ ДЕФЕКТ ДЕРЕВА: успешный ФОНОВЫЙ обмен
гасит обе заготовки (окно 74); две видимые заготовки встают в одну точку и накрывают друг друга
(окно 76); окно обрыва follow-up GET переменно и с первой попытки может обрыва не дать (замер
2026-09-11 — GET со статусом 200 при том же ограничении канала).
⚠️ ЗАМЕР, СНЯТЫЙ АГЕНТОМ, ЭТОТ ПУНКТ НЕ ЗАКРЫВАЕТ: у фазы таких замеров уже три, и ровно поэтому
`status` артефакта держится нетерминальным.</human-check>
  </verify>

  <done>
Артефакт обхода несёт ОДНУ приписку с ненумерованным заголовком, в ней — процедуры двух перезамеров
с шестью и четырьмя снимаемыми величинами, требование ненулевого положения прокрутки, дословный
способ обрыва, перечень трёх ловушек наблюдения до процедур и дословные команды посева с замером их
исполнимости. Ни одна клетка отметок не заполнена, ни одно поле шапки не тронуто, ни одна прежняя
запись не вычеркнута.
  </done>
</task>

<task type="auto">
  <name>Задача 2: вопрос подписи на приёмке встаёт в артефакт, а незакрытие предъявляется диффом и прогоном</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:506-845` — все ШЕСТЬ разделов проверок
  и все шесть таблиц отметок. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: вопрос подписи перечисляет шесть проверок
  ПОИМЁННО их собственными заголовками, и перечень, собранный по памяти, назвал бы не то.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1170-1190` — пункт 3 раздела открытых
  человеческих вопросов («Человеческая подпись на приёмке — ОТСУТСТВУЕТ») ЦЕЛИКОМ, включая абзац
  «ПОЧЕМУ МАШИНА ЭТОГО НЕ МОЖЕТ ПО ОПРЕДЕЛЕНИЮ». ⚠️ ФОРМУЛИРОВКА ВОПРОСА НАСЛЕДУЕТСЯ ОТСЮДА.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1596-1640` — запись гэпа `G-10-7`
  целиком с обоими пунктами `missing`. ⚠️ ЧИТАТЬ И НЕ ПРАВИТЬ: `status: failed` остаётся.
- `.planning/REQUIREMENTS.md:31` и `:136` — флажок `FORM-06` и клетка `Gaps Found`. ⚠️ ЧИТАТЬ И НЕ
  ПРАВИТЬ НИ ОДИН СИМВОЛ: требование закрывается подписью человека.
- `tests/test_planning/test_requirement_completion_follows_verification.py:398-430` — правило
  преждевременной отметки требования и правило объявленного числа строк. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО:
  единственный способ знать, что реестр требований не тронут, — прочесть, что правило читает.
- `tests/test_planning/test_the_walkthrough_cannot_self_certify.py:344-390` — правило пустых отметок
  и правило согласия трёх чисел.
- Приписка, внесённая задачей 1 — в том виде, в каком задача 1 её оставила. ⚠️ ЧИТАТЬ ПЕРЕД
  ПРАВКОЙ: вопрос подписи встаёт В ТУ ЖЕ приписку, а не во вторую.
  </read_first>

  <action>
**(1) ВОПРОС ПОДПИСИ ВСТАЁТ В ТУ ЖЕ ПРИПИСКУ, А НЕ ВО ВТОРУЮ.** Второй приписки от одного плана не
заводится: два раздела одного плана в одном артефакте есть та же болезнь, против которой заведён
якорь файла отложенного, — только здесь её ещё ничто не стережёт.

**(2) ВОПРОС ФОРМУЛИРУЕТСЯ ДВУМЯ ИСХОДАМИ, И ОБА ВЫРАЗИМЫ.** Исход первый: человек ПРИНИМАЕТ
отметки шести проверок — тогда терминальное состояние артефакта ставит ОН, своей подписью, и
`FORM-06` закрывается ею же. Исход второй: человек НАЗЫВАЕТ ПОИМЁННО проверки, требующие
перепрохождения, — тогда `status` остаётся нетерминальным, и названные проверки идут в следующий
обход. ⚠️ ТРЕТЬЕГО ИСХОДА — «согласиться молча» — НЕТ: подпись есть ответ, а не отсутствие
возражений.

**(3) ШЕСТЬ ПРОВЕРОК ПЕРЕЧИСЛЯЮТСЯ ПОИМЁННО ИХ СОБСТВЕННЫМИ ЗАГОЛОВКАМИ, И У КАЖДОЙ НАЗВАНО
СЕГОДНЯШНЕЕ СОСТОЯНИЕ ПРИЗНАКА** — кем снят (АГЕНТОМ, а не человеком), сколькими обходами, и что
именно остаётся открытым. ⚠️ СОСТОЯНИЕ НАЗЫВАЕТСЯ БЕЗ СМЯГЧЕНИЯ И БЕЗ ПРЕУВЕЛИЧЕНИЯ: три обхода
сняли признаки, число проваленных шагов упало до нуля, и ровно поэтому подпись стала ЕДИНСТВЕННЫМ,
что держит артефакт нетерминальным. Соблазн объявить обход закрытым здесь максимален, и запрет
писался ровно на этот случай.

**(4) НАЗЫВАЕТСЯ ПРЯМО, ЧТО ЗАКРЫВАЕТ ПОДПИСЬ И ЧТО НЕ ЗАКРЫВАЕТ.** Закрывает: `FORM-06` (флажок
`.planning/REQUIREMENTS.md:31` и клетка `:136`) и терминальное состояние артефакта. НЕ закрывает:
запись `G-10-7` (её закрывает замер шага 2.8, а не подпись под обходом в целом); критерий 3
роадмапа (предмет отдельного решения владельца, план 10-53); окна 74, 75, 76, 78, 79 журнала; 221
запрет фазы (пункт 4 перечня «Human Verification Required», отдельное решение).

**(5) ПРЕДЪЯВИТЬ НЕЗАКРЫТИЕ ДИФФОМ И ПРОГОНОМ, А НЕ ОБЕЩАНИЕМ.** В сводку: дифф артефакта не
содержит ни строки полей шапки и ни одной удалённой строки; дифф реестра требований ПУСТ; шесть
таблиц отметок в диффе отсутствуют; поля шапки стоя́т прежними значениями по СОДЕРЖИМОМУ файла;
планировочные гейты зелены. ⚠️ ЭТО НЕ ФОРМАЛЬНОСТЬ: одиннадцатый
круг проверял именно это и именно так — чтением шапки на HEAD и строк 31 и 136 реестра.

**(6) ЗАПИСАТЬ В СВОДКУ ОСТАТОК ВОСЕМЬЮ ПУНКТАМИ, БЕЗ СМЯГЧЕНИЯ.** Перечень тот же, что у планов
10-52 и 10-53, и он не сокращается по причине «уже назван»: сводка читается отдельно от чужой.
Пункты: пункт `missing[0]` записи гэпа одиннадцатого круга (решение владельца по критерию 3 —
адресат план 10-53); запись 75 журнала (перечень прочих путей устаревшей сводки не снимался; 10-50
закрыл только фрагментную ветвь `G-10-6`); запись 76 (две заготовки накрывают друг друга всегда;
`T-10-51-04`, `accept`, владельца нет; тождественна `IN-03`); запись 78 (проза заготовок разошлась с
деревом двумя утверждениями; `app/templates/` партии запрещён); запись 79 (суита недетерминирована
по времени суток; предмет вне Фазы 10, отложено владельцем); запись 74 (успешный фоновый обмен
гасит непрочитанный отказ; `T-10-49-05`, `accept`; тождественна `IN-02`); пункт 4 «Human
Verification Required» (221 запрет, `prohibitions_fully_enforced: 0`, разрешение ограничено
`seventh-batch-only`, реестр `10-PROHIBITIONS-SUBJECT.md` не правлен); находка ревизии `IN-01`
(инвентарь внеполосных блоков поднят числом, отказ печатает два числа вместо разницы множеств).
И СВЕРХ НИХ — ПРЯМОЕ УТВЕРЖДЕНИЕ: `G-10-7` остаётся `status: failed`, `FORM-06` не отмечен
выполненным, `status` артефакта обхода равен `partial`, подписи человека на приёмке КАК НЕ БЫЛО, ТАК
И НЕТ.
  </action>

  <acceptance_criteria>
- Вопрос подписи стои́т В ТОЙ ЖЕ приписке, что процедуры задачи 1; второй приписки от настоящего
  плана в артефакте нет.
- Вопрос сформулирован ДВУМЯ исходами, оба выразимы, и прямо сказано, что третьего («согласиться
  молча») нет.
- Шесть проверок перечислены ПОИМЁННО их собственными заголовками; у каждой названо, кем снят
  признак и что остаётся открытым.
- Названо прямо, что подпись ЗАКРЫВАЕТ (`FORM-06` и терминальное состояние артефакта) и что НЕ
  закрывает (пять поимённых пунктов).
- Поля шапки артефакта стоя́т прежними значениями (утверждается СОДЕРЖИМЫМ, а не диффом); столбец
  удалённых строк в `git diff --numstat 9e6b859` по артефакту равен нулю; дифф
  `.planning/REQUIREMENTS.md` от базы партии ПУСТ; шесть таблиц отметок в диффе отсутствуют.
- Планировочные гейты зелены; итоговая строка приведена числом.
- Остаток назван ВОСЕМЬЮ пунктами со ссылками на записи журнала и находки ревизии, и сверх них —
  прямое утверждение о `G-10-7`, `FORM-06`, `status` артефакта и отсутствии подписи.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — вопрос подписи уронил гейт самозаверения, гейт согласия трёх чисел либо гейт преждевременной отметки требования</fails_when>
    <automated>git diff --numstat 9e6b859 -- .planning/REQUIREMENTS.md app .planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md</automated>
    <fails_when>вывод НЕПУСТ — тронут реестр требований, дерево приложения либо реестр запретов: все три вне предмета настоящего плана, а первый из них есть прямое самозаверение `FORM-06`. ⚠️ РЕВИЗИЯ ЗАКРЕПЛЕНА (`9e6b859` — база партии) И БЕЗ `..HEAD`: сравнение с рабочим деревом видит правку и до коммита, и после него, тогда как проверка без закреплённой ревизии была бы пуста для зафиксированной правки и прошла бы вакуумно. ⚠️ `10-VERIFICATION.md` В ЭТОМ ПЕРЕЧНЕ НАМЕРЕННО ОТСУТСТВУЕТ: отчёт верификации правит план 10-53 ТОЙ ЖЕ ВОЛНЫ, и проверка на дифф объявила бы законную правку соседа нарушением настоящего плана. Запрет утверждается СЛЕДУЮЩЕЙ командой — инвариантами содержимого, которые верны и после правки соседа ⚠️ `.planning/ROADMAP.md` ИЗ ЭТОГО ПЕРЕЧНЯ ИЗЪЯТ ПО ДРУГОМУ ОСНОВАНИЮ, И ОНО НАЗЫВАЕТСЯ: его ЗАКОННО правит шаг трекинга исполнения (`roadmap update-plan-progress` ставит отметку завершения плана), и гейт на диффе краснел бы на законной правке. Запрет утверждается ИНВАРИАНТОМ ФОРМУЛИРОВКИ КРИТЕРИЯ 3 ниже.</fails_when>
    <automated>grep -c 'новых строк JS фаза не добавляет (машинно: диффом по `app/static/js/` и шаблонам)' .planning/ROADMAP.md</automated>
    <fails_when>вывод НЕ РАВЕН "1" — формулировка КРИТЕРИЯ 3 в роадмапе тронута, а её амендмент отвергнут решением D-13 явно («документ вехи держится ФИКСИРОВАННЫМ, а прочтение есть предмет CONTEXT.md»), и ни одна ветвь решения владельца его не предлагает. ⚠️ ЭТО ИНВАРИАНТ СОДЕРЖИМОГО, А НЕ ПРОВЕРКА ДИФФА, И ЭТО НЕСУЩЕЕ РЕШЕНИЕ: `roadmap update-plan-progress` ЗАКОННО правит `.planning/ROADMAP.md` в ходе исполнения КАЖДОГО плана — он ставит отметку завершения и вынуждает привести прозу счёта, — поэтому гейт «дифф роадмапа пуст» краснел бы у каждого плана фазы на законной правке трекинга. Запрет касается ФОРМУЛИРОВКИ КРИТЕРИЯ, и утверждается именно она. Замер планировщика 2026-09-12: вхождение ОДНО, строка 276</fails_when>
    <automated>grep -c -e '^status: gaps_found$' -e '^score: 8/10 must-haves verified$' -e '^overrides_applied: 0$' .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md</automated>
    <fails_when>вывод НЕ РАВЕН "3" — поле вердикта либо счёта отчёта верификации сдвинуто, то есть партия вынесла вердикт вместо верификатора. ⚠️ ЭТО ИНВАРИАНТ СОДЕРЖИМОГО, А НЕ ДИФФА, И ПОТОМУ ОН ВОЛНОБЕЗОПАСЕН: план 10-53 вносит в этот файл ЗАПИСЬ и полей вердикта не трогает — запрет назван его собственным перечнем прохибиций. Значения сняты планировщиком 2026-09-12 на `9e6b859` — вывод был "3"</fails_when>
    <automated>grep -n '^status:' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод содержит "passed" либо "complete" — артефакт объявил себя пройденным без подписи человека, и это ровно то самозаверение, ради запрета которого живёт правило `test_the_walkthrough_cannot_self_certify.py`. Пустой вывод тоже есть отказ: поле `status` из шапки исчезло</fails_when>
    <automated>grep -n '^- \[ \] \*\*FORM-06\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>пустой вывод — строка требования либо переписана, либо флажок поставлен, то есть `FORM-06` отмечен выполненным без подписи человека на приёмке</fails_when>
    <human-check>ЧЕЛОВЕК ПОДПИСЫВАЕТ ПРИЁМКУ ОБХОДА ЛИБО НАЗЫВАЕТ ПОИМЁННО ПРОВЕРКИ, ТРЕБУЮЩИЕ
ПЕРЕПРОХОЖДЕНИЯ. Вопрос: принимаются ли отметки ШЕСТИ проверок `10-UAT.md` — признаки которых
трижды снимал АГЕНТ через средство браузера при `workflow.live_dom_uat: false`, на ПРОДЕ, — или
какие-то из шести обязаны быть пройдены заново?
При ПРИНЯТИИ: терминальное состояние артефакта и отметку `FORM-06` ставит ПОДПИСЬ, и ставит её
человек, называя своё имя и дату; ЭТОЙ ПОДПИСЬЮ И ТОЛЬКО ЕЙ закрывается `FORM-06`.
При НАЗВАНИИ ПРОВЕРОК: `status` остаётся нетерминальным, названные проверки идут в следующий обход.
Третьего исхода — согласиться молча — нет: подпись есть ответ, а не отсутствие возражений.
⚠️ ЧТО ПОДПИСЬ НЕ ЗАКРЫВАЕТ, И ЭТО НАЗЫВАЕТСЯ ЕМУ ВМЕСТЕ С ВОПРОСОМ: запись `G-10-7` (её закрывает
замер шага 2.8); критерий 3 роадмапа (отдельное решение владельца, план 10-53); окна 74, 75, 76, 78,
79 журнала; 221 запрет фазы при нуле принуждённых.</human-check>
  </verify>

  <done>
Вопрос подписи стои́т в той же приписке двумя выразимыми исходами; шесть проверок перечислены
поимённо с сегодняшним состоянием признака; названо, что подпись закрывает и что не закрывает;
незакрытие предъявлено диффом и прогоном, а не обещанием; остаток назван восемью пунктами плюс
прямое утверждение о четырёх незакрытых величинах.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| замер агента → приёмка человека | три положительных замера машины лежат в артефакте и выглядят как приёмка; отличает их только поле наблюдателя и запрет терминального состояния |
| артефакт обхода → отметка требования | `FORM-06` закрывается подписью под обходом, поэтому всякое движение шапки артефакта есть движение требования вехи |
| процедура наблюдения → величины наблюдателя | между «посмотреть, видно ли» и «привести четыре числа прямоугольника» лежит вся разница между суждением и замером |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-54-01 | Spoofing | самозаверение обхода: `status` артефакта поставлен терминальным без подписи человека | high | mitigate | пять полей шапки утверждаются ИНВАРИАНТАМИ СОДЕРЖИМОГО (волнобезопасно, в отличие от диффа); гейт `«verify»` грепает поле `status` на терминальные значения; планировочное правило `test_the_walkthrough_cannot_self_certify` прогоняется явно |
| T-10-54-02 | Spoofing | преждевременная отметка `FORM-06` в реестре требований | high | mitigate | проверка диффа реестра требований от базы партии требует ПУСТОГО вывода; отдельный греп требует присутствия НЕОТМЕЧЕННОЙ строки требования; правило `test_no_requirement_is_marked_complete_before_its_phase_verification_passed` прогоняется явно |
| T-10-54-03 | Repudiation | замер, снятый при положении прокрутки у верхнего края: признак попадает в окно САМ, и один положительный замер закрывает то, что от прокрутки зависит | high | mitigate | процедура ТРЕБУЕТ ненулевого `scrollY`, приведённого числом, и прямо говорит, что замер при нулевом положении признака не снимает — основание замерено 2026-09-11 обоими исходами одного дня |
| T-10-54-04 | Information Disclosure | наблюдатель, не знающий трёх ловушек, сообщает дефект, которого нет, либо пропускает существующий | high | mitigate | перечень трёх ловушек (окна 74, 76 и переменность окна обрыва) стои́т ДО процедур, и у каждой названо, какой отрицательный признак она способна дать на работающем дереве |
| T-10-54-05 | Denial of Service | четвёртый заголовок вида «### N.» в разделе открытых вопросов ломает счёт предиката `phase uat-passed` | medium | mitigate | заголовок приписки НЕ нумерован по форме приписки третьего обхода; гейт `«verify»` требует РОВНО ТРЁХ нумерованных заголовков |
| T-10-54-06 | Tampering | вычёркивание прежних записей ради «актуальности» артефакта | medium | mitigate | приписка только ДОБАВЛЯЕТ; гейт `«verify»` считает удалённые строки диффа и требует не больше строки-заголовка |
| T-10-54-07 | Tampering | стенд, посеянный в боевую базу и оставленный там после наблюдения | medium | mitigate | посев не делается исполнителем автоматически: `workflow.live_dom_uat` равен `false`, команды посева и уборки приведены дословно, уборка требуется СРАЗУ после наблюдения, и замер уборки приводится в отчёте о наблюдении |
| T-10-54-08 | Repudiation | вторая копия чисел блока подъёма, переписанная в артефакт: разошлась бы с таблицей стилей молча | low | mitigate | приписка называет ИСТОЧНИК величин и требует сличить наблюдённое с ним, а не приводит собственные числа |
| T-10-54-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q` — зелено: гейт самозаверения обхода, гейт согласия трёх
   чисел, гейт преждевременной отметки требования и гейт исполнимости посева не тронуты.
2. Поля шапки `10-UAT.md` стоя́т прежними значениями (утверждается СОДЕРЖИМЫМ файла, а не диффом);
   столбец удалённых строк в `git diff --numstat 9e6b859` по артефакту равен нулю.
3. `git diff --numstat 9e6b859` пуст по `.planning/REQUIREMENTS.md`, `app/` и
   `10-PROHIBITIONS-SUBJECT.md`. Формулировка критерия 3 в `.planning/ROADMAP.md` утверждается
   ИНВАРИАНТОМ СОДЕРЖИМОГО (вхождение ОДНО), а не диффом: роадмап законно правит шаг трекинга
   исполнения. Отчёт верификации `10-VERIFICATION.md` утверждается ИНВАРИАНТАМИ
   СОДЕРЖИМОГО (`status: gaps_found`, `score`, `overrides_applied: 0`), а не диффом: его правит план
   10-53 той же волны, и проверка на дифф объявила бы законную правку соседа нарушением.
4. Нумерованных заголовков в разделе открытых человеческих вопросов РОВНО ТРИ.
5. Поле `status` шапки артефакта равно `partial`; строка `FORM-06` реестра требований не отмечена.
6. Три человеческих замера переданы блоками `«human-check»` и собираются верификатором в конце фазы
   (`workflow.human_verify_mode: end-of-phase`) — ни один из них не подменён машинным утверждением.
</verification>

<success_criteria>
- Человек получает ПРОЦЕДУРУ с ожидаемыми величинами числами, а не просьбу посмотреть: у шага 2.8
  названы шесть снимаемых величин и требование ненулевого положения прокрутки, у шага 4.4 — четыре
  плюс число видимых заготовок.
- Способ вызвать НАСТОЯЩИЙ обрыв назван дословно тем, каким он замерен, и подмена средства запроса
  замером не считается.
- Три ловушки наблюдения названы ДО наблюдения, и у каждой назван отрицательный признак, который она
  способна дать на работающем дереве.
- Вопрос подписи сформулирован двумя выразимыми исходами; названо, что подпись закрывает (`FORM-06`)
  и что не закрывает (пять поимённых пунктов).
- Самозаверения не произошло, и это ИЗМЕРЕНО: `status` равен `partial`, `checks_declared` равно `6`,
  отметки не заполнены, `FORM-06` не отмечен, предикат трёх заголовков не сломан.
- Остаток назван восемью пунктами без смягчения; `G-10-7` остаётся `status: failed`, подписи человека
  на приёмке как не было, так и нет.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-54-SUMMARY.md` when done
</output>
