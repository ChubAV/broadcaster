---
phase: 10-rychag-components-modal-html
plan: 23
subsystem: planning
tags: [roadmap, state, windows-ledger, uat, gap-closure, record-keeping]

requires:
  - phase: 10-rychag-components-modal-html (планы 10-17…10-22)
    provides: закрытые гейты записи, тексты трёх окон, предметы строк перечня и границы доказанного
provides:
  - Перечень семи планов четвёртой партии в трёх волнах и летопись числа планов фазы «16 → 23»
  - Машинно читаемое поле `progress.total_plans` = 61, выведенное ПРОГОНОМ разборщика тем же действием
  - Четыре окна реестра (49, 50, 51, 52), дописанные ОДНИМ владельцем командой ведения журнала
  - Закрытое ЗАМЕРОМ окно 44
  - Раздел «Чего ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла» и восемь строк перечня закрытого машинно
affects: [повторная верификация Фазы 10, приёмка обхода 10-UAT.md, Фаза 11, Фаза 15 (GATE-09), владелец вехи v2.1]

actuals:
  tokens: 24587
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Летопись числа в форме «прежнее значение не было ошибкой — оно устарело» с источником расхождения поимённо"
    - "Красный между двумя правками НАБЛЮДАЕТСЯ, а не предполагается: правка источника и правка производного поля — разные шаги"
    - "Владелец записи в партии ОДИН: общий дописываемый файл стои́т в `files_modified` ровно одного плана"

key-files:
  created:
    - .planning/phases/10-rychag-components-modal-html/10-23-SUMMARY.md
  modified:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/WINDOWS.md
    - .planning/phases/10-rychag-components-modal-html/10-UAT.md

key-decisions:
  - "Перечень состава строки `**Plans**:` дописан ДВУМЯ партиями, а не одной: он называл 4+3+4 = 11 при объявленных 16 — третья партия сдвинула ЧИСЛА строки и не сдвинула её ПЕРЕЧЕНЬ"
  - "Окно 44 закрыто: его предмет — устаревшие числа строки состава — приведён замером задачей 1; расхождение самого окна с деревом (цитата на поколение отстала, `line: 287` мимо) названо отдельно"
  - "Перечень рёбер зонда покрытия четвёртой партии расходится с перечнем третьей — это ДВА РАЗНЫХ ЗОНДА, и сведение их в одну строку потеряло бы авторство обоих"
  - "Мутации `.planning/STATE.md` через verbs GSD (`state.advance-plan`, `state.update-progress`, `record-metric`, `record-session`) НЕ ИСПОЛНЯЛИСЬ: план правит ОДНО поле замером и прямо запрещает трогать прозаические разделы"
  - "`FORM-06` НЕ переведён в завершённое и флажок не заполнен: условие перевода — вердикт `passed` повторной верификации, которого партия не выносит"

patterns-established:
  - "Гейт на СОГЛАСИИ трёх мест реестра, а не на литеральном числе строк: согласие истинно при любом законном числе окон"
  - "Прибавка общего дописываемого файла АТРИБУТИРУЕТСЯ ПОСТРО́ЧНО плану-источнику; отказ — строка, не атрибутируемая ни одному плану партии"
  - "Раздел «чего партия НЕ закрыла» стои́т РЯДОМ с разделом предыдущей партии и не подменяет его: слово-номер в заголовке несущее"

requirements-completed: []

coverage:
  - id: D1
    description: "Машинно читаемое поле `progress.total_plans` равно выведенному из отметок роадмапа (54 → 61), и красный между двумя правками наблюдён"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_planning/test_state_progress_matches_roadmap.py#test_the_machine_readable_progress_is_derived_from_the_roadmap"
        status: pass
      - kind: other
        ref: "прогон правила ПОСЛЕ правки роадмапа и ДО правки поля: `1 failed, 4 passed`, отказ называет ОБА числа (54 записано / 61 выведено)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Летопись числа планов фазы «16 → 23» записана в форме трёх предыдущих со всеми ШЕСТЬЮ обязательными частями"
    verification: []
    human_judgment: true
    rationale: "Форму летописи не читает ни одно правило и читать не имеет права — граница D-33. Соответствие шести обязательным частям судится чтением; текст приведён в сводке целиком"
  - id: D3
    description: "Семь строк перечня раздела фазы разложены по трём волнам согласно полям `wave` и `depends_on` самих планов"
    verification:
      - kind: other
        ref: "`grep -c '^- \\[ \\] 10-' .planning/ROADMAP.md` → 7; сличение с frontmatter семи планов (`awk` по блоку frontmatter) — раскладка совпала посимвольно"
        status: pass
    human_judgment: false
  - id: D4
    description: "Четыре окна дописаны КОМАНДОЙ ведения журнала, тексты трёх перенесены из сводки плана 10-22 ДОСЛОВНО, три места реестра согласны"
    verification:
      - kind: other
        ref: "посимвольное сличение переноса с оригиналом: 1937/1781/3539 символов, совпадение полное (True×3)"
        status: pass
      - kind: other
        ref: "`test \"$(grep -cE '^\\| [0-9]+ \\| ' .planning/WINDOWS.md)\" = \"$(sed -n 's/^total_count: \\([0-9]*\\)$/\\1/p' .planning/WINDOWS.md)\"` → AGREE (52 = 52; 45+1+6 = 52)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Состояние окна 44 решено ЗАМЕРОМ и снято командой ведения журнала"
    verification:
      - kind: other
        ref: "`gsd-tools windows fixed 44` → status `fixed`, `fixed_count` 5 → 6"
        status: pass
    human_judgment: true
    rationale: "Сам ответ «предмет закрыт» есть суждение о том, покрывает ли приведённая замером строка состава то, что окно передало дальше; команда лишь исполняет принятое решение, а не выносит его"
  - id: D6
    description: "Артефакт обхода по-прежнему ничего не самозаверяет после правки: состояние не сдвинуто, шесть таблиц отметок пусты, три счёта проверок равны"
    verification:
      - kind: unit
        ref: "tests/test_planning/test_the_walkthrough_cannot_self_certify.py (8 passed)"
        status: pass
      - kind: other
        ref: "`walkthrough_counts` ДО и ПОСЛЕ правки: state='testing', declared=6, sections=6, mark_tables=6, filled=0 — обе пары одинаковы"
        status: pass
    human_judgment: false
  - id: D7
    description: "Раздел «Чего ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла» называет ВСЕ непокрытые пункты, и у каждого своё основание, не являющееся оценкой труда"
    verification:
      - kind: other
        ref: "`git diff -- 10-UAT.md | grep -ci -e 'сложн' -e 'трудоём' -e 'позже'` → 0"
        status: pass
    human_judgment: true
    rationale: "Полнота перечня и ДОСТАТОЧНОСТЬ каждого основания машине недоступны: отрицательный поиск ловит запрещённые слова, но не отвечает, названо ли всё и названо ли верно"
  - id: D8
    description: "Восемь строк перечня «Что закрыто МАШИННО» — по одной на предмет партии, каждая с ГРАНИЦЕЙ доказанного; имена правил сличены с деревом и со сводками"
    verification:
      - kind: other
        ref: "35 имён правил из дописанных строк: все найдены в `tests/` объявлением `def <имя>(`; 34 из 35 найдены в сводках партии (35-е — правило самого плана 10-23, чья сводка пишется этим прогоном)"
        status: pass
    human_judgment: true
    rationale: "Присутствие имени в дереве проверяемо машинно; ВЕРНОСТЬ границы доказанного — суждение о том, где именно кончается доказательство правила, и его выносит читатель"
  - id: D9
    description: "Требование `FORM-06` осталось незавершённым, отметки обхода не заполнены, `.planning/REQUIREMENTS.md` и `09-UAT.md` не тронуты"
    requirement: "FORM-06"
    verification:
      - kind: other
        ref: "`git diff --name-only a89b2c8..HEAD -- .planning/REQUIREMENTS.md .planning/phases/09-*/` → пусто; `grep -cE '^\\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\\|' 10-UAT.md` → 0"
        status: pass
    human_judgment: false

duration: 2h 14m
completed: 2026-09-07
status: complete
---

# Phase 10 Plan 23: Летопись чисел, реестр окон и прямое называние непокрытого — Summary

**Четвёртая партия закрыта тремя действиями, которые нельзя сделать раньше, чем известен её состав: число планов фазы сдвинуто с летописью «16 → 23» и машинно читаемое поле счёта приведено ТЕМ ЖЕ ДЕЙСТВИЕМ с наблюдённым красным между правками; четыре окна дописаны ОДНИМ владельцем командой ведения журнала, три из них перенесены посимвольно; и в самом артефакте обхода названо шестнадцатью пунктами, чего партия НЕ закрыла.**

## Performance

- **Duration:** 2h 14m (из них 34 мин 12 с — один полный прогон суиты)
- **Started:** 2026-09-07T17:20:44Z
- **Completed:** 2026-09-07T19:35:10Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- **Летопись «16 → 23» записана, и оба числа строки состава СНЯТЫ ЗАМЕРОМ**, а не переписаны: 23 строки перечня раздела и 16 отметок исполнения.
- **Красный между правками НАБЛЮДЁН, а не предположен.** Правка роадмапа и правка поля сделаны разными шагами; между ними правило покраснело и назвало ОБА числа.
- **Найдено и закрыто расхождение ВНУТРИ правимой строки:** перечень состава называл 4+3+4 = 11 при объявленных 16 — третья партия сдвинула ЧИСЛА строки и не сдвинула её ПЕРЕЧЕНЬ.
- **Четыре окна дописаны одним владельцем**, три текста перенесены ПОСИМВОЛЬНО (1937/1781/3539 символов, совпадение полное), и окно 51 передаёт владельцу решение об изъятии из запертого D-01 как ВОПРОС.
- **Окно 44 решено замером и закрыто командой**; при этом названо, что само окно на поколение отстало от дерева.
- **Шестнадцать пунктов непокрытого названы в артефакте обхода** с собственным основанием у каждого, и расхождение перечня рёбер с перечнем предыдущей партии объяснено ДВУМЯ зондами, а не сведено в одну строку.
- **Артефакт по-прежнему ничего не самозаверяет:** три счёта проверок равны до и после правки, отметок ноль, `FORM-06` незавершено.

## Task Commits

1. **Задача 1: летопись числа планов фазы и машинно читаемое поле счёта — одним действием** — `04bd2c6` (docs)
2. **Задача 2: все окна четвёртой партии дописаны ОДНИМ владельцем, состояние окна 44 решено замером** — `ce5a42b` (docs)
3. **Задача 3: назвать ПРЯМО, чего ЧЕТВЁРТАЯ партия НЕ закрыла — в самом артефакте обхода** — `631aa0c` (docs)

## Files Created/Modified

- `.planning/ROADMAP.md` — три подзаголовка волн четвёртой партии, семь строк перечня в отметке «не исполнено», строка состава по замеру, летопись «16 → 23»
- `.planning/STATE.md` — единственное поле `progress.total_plans`: 54 → 61
- `.planning/WINDOWS.md` — окна 49, 50, 51, 52; состояние окна 44 переведено в `fixed`
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md` — раздел «Чего ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла» (16 пунктов) и восемь строк перечня «Что закрыто МАШИННО»

---

## ЗАДАЧА 1 — приёмочные критерии с выводами

### Семь строк дописаны и разложены по трём волнам

```
$ grep -c '^- [ ] 10-' .planning/ROADMAP.md
7
```

Подзаголовки волн (три):

- `**Закрытие гейпов, ЧЕТВЁРТАЯ ПАРТИЯ — волна 1**` *(`gap_closure: true`, нумерация волн начинается заново; источник — четвёртый `10-VERIFICATION.md` от 2026-09-04, `status: gaps_found`, 6/9, `behavior_unverified: 2`, ОДИН провал — он же РЕГРЕССИЯ того же корня, заведённая третьей партией, — и двенадцать открытых находок ревизии `10-REVIEW.md`)*
- `**Закрытие гейпов, ЧЕТВЁРТАЯ ПАРТИЯ — волна 2**` *(10-19 blocked on 10-18; 10-20 и 10-21 blocked on 10-17; 10-22 blocked on 10-18)*
- `**Закрытие гейпов, ЧЕТВЁРТАЯ ПАРТИЯ — волна 3**` *(blocked on ВСЕХ ШЕСТИ планах волн 1-2)*

**Раскладка СЛИЧЕНА С FRONTMATTER семи планов ПЕРЕД записью**, а не взята из памяти:

```
$ for f in 10-17 … 10-23; do awk '/^---$/{n++; next} n==1 && (/^wave:/ || /^depends_on:/)' $f-PLAN.md; done
10-17: wave: 1  depends_on: []
10-18: wave: 1  depends_on: []
10-19: wave: 2  depends_on: ["10-18"]
10-20: wave: 2  depends_on: ["10-17"]
10-21: wave: 2  depends_on: ["10-17"]
10-22: wave: 2  depends_on: ["10-18"]
10-23: wave: 3  depends_on: ["10-17","10-18","10-19","10-20","10-21","10-22"]
```

Расхождений с записанным перечнем НЕТ. `10-19` действительно стои́т во второй волне, а не в первой.

### Оба числа строки состава СНЯТЫ ЗАМЕРОМ

```
$ awk '/^### Phase 10:/{f=1} /^### Phase 11:/{f=0} f' .planning/ROADMAP.md | grep -cE '^- \[[x ]\] 10-'
23
$ awk '/^### Phase 10:/{f=1} /^### Phase 11:/{f=0} f' .planning/ROADMAP.md | grep -cE '^- \[x\] 10-'
16
```

⚠️ **ОРИЕНТИР ПЛАНИРОВЩИКА ПОДТВЕРЖДЁН ТОЧНО.** Он снят ДО исполнения партии и назвал `16` строк перечня и `16` отметок; замер ДО настоящей правки давал ровно это. После семи неотмеченных строк — 23 всего, 16 исполнено. Расхождения с ориентиром нет.

### Летопись — её текст целиком

**Летопись числа планов фазы: 16 → 23** (Фаза 10, планирование ЧЕТВЁРТОЙ партии закрытия гейпов 2026-09-04…09-07, приведено планом 10-23). ⚠️ ПРЕЖНЕЕ ЗНАЧЕНИЕ НЕ БЫЛО ОШИБКОЙ — ОНО УСТАРЕЛО: `16` было верным для состава фазы ДО четвёртой партии, и правится не оно, а числа, которые оно пережило (та же форма, которой фаза записала летописи «16 → 18», «7/7 → 11/11» и «11 → 16»). ИСТОЧНИК РАСХОЖДЕНИЯ ПОИМЁННО: ЧЕТВЁРТЫЙ отчёт верификации от 2026-09-04 (`status: gaps_found`, счёт `6/9`, `behavior_unverified: 2`) назвал ОДИН провал — он же `regressions[0]`, РЕГРЕССИЯ того же корня, заведённая третьей партией, — с ЧЕТЫРЬМЯ пунктами перечня `missing`; на него и на ДВЕНАДЦАТЬ открытых находок ревизии `10-REVIEW.md` (`WR-01`…`WR-08`, `IN-01`…`IN-04`; тринадцатая, `CR-01` ревизии, и есть корень провала) заведены СЕМЬ планов: 10-17 (блокер целиком, все четыре пункта `missing`), 10-18 (`WR-01`, `WR-07`, `IN-03` — гард происхождения на необратимом удалении), 10-19 (`WR-05`, `WR-06`, `IN-01`, `IN-02` — цепь шаблонов и форма выражения адреса), 10-20 (машинная половина гэпа самозаверения обхода и реестр якорей наблюдения), 10-21 (`WR-08` — ключ индекса отчётов «веха, фаза»), 10-22 (`WR-03`, `WR-02`, `WR-04` — три записи, утверждавшие больше, чем исполняет код) и 10-23 (летопись чисел, реестр окон и перечень непокрытого). ⚠️ ВТОРОЕ ЧИСЛО ТОЙ ЖЕ СТРОКИ ТОЖЕ УСТАРЕЛО, И ОНО ТОЖЕ НЕ БЫЛО ОШИБКОЙ: строка состава говорила «из них 11 исполнено» — `11` было верным для момента своей записи и пережило исполнение планов `10-12`…`10-16` третьей партии. ЗАМЕРЕННОЕ значение — `16`, снятое счётом отметок `- [x] 10-` внутри раздела фазы (`16`) в момент записи настоящей летописи, а не сложением в уме. ⚠️ ЧИСЛО ИСПОЛНЕННЫХ ПРИБАВКИ ОТ СЕМИ НОВЫХ СТРОК НЕ ПОЛУЧАЕТ: планы 10-17…10-23 стоят в перечне в отметке «не исполнено», и их отметки ставит оркестратор ПО ЗАВЕРШЕНИИ — прибавка, поставленная здесь, разошлась бы с разделом через минуту после собственной записи. ⚠️ МАШИННО ЧИТАЕМОЕ ПОЛЕ `progress.total_plans` В `.planning/STATE.md` ПРИВЕДЕНО ТЕМ ЖЕ ДЕЙСТВИЕМ (54 → 61) — источником счёта объявлен ROADMAP, и поле следует за ним, а не наоборот; значение выведено ПРОГОНОМ разборщика правила (`roadmap_totals`), а не арифметикой, и красный между двумя правками НАБЛЮДЁН, а не предположен. Поле `progress.completed_plans` не тронуто. ⚠️ ЗАПИСЬ 44 `.planning/WINDOWS.md` ЭТОЙ ЖЕ ПРАВКОЙ ЗАКРЫВАЕТСЯ: её предмет — устаревшие числа строки состава — приведён здесь замером, и состояние окна снято КОМАНДОЙ ведения журнала, а не ручной правкой строки.

Шесть обязательных частей пункта (4) `<action>` на месте: (1) объявленное новое значение и прежнее — «16 → 23»; (2) «⚠️ ПРЕЖНЕЕ ЗНАЧЕНИЕ НЕ БЫЛО ОШИБКОЙ — ОНО УСТАРЕЛО» дословно; (3) источник расхождения поимённо — четвёртый отчёт, `gaps_found`, `6/9`, `behavior_unverified: 2`, один провал (он же `regressions[0]`), четыре пункта `missing`, двенадцать открытых находок ревизии и семь планов с предметами; (4) отдельная оговорка о ВТОРОМ числе строки; (5) отдельная оговорка о том, что число исполненных прибавки не получает; (6) отдельная оговорка о машинно читаемом поле, приведённом тем же действием.

### Строка состава после правки — целиком

**Plans**: 23 плана, из них 16 исполнено. ⚠️ ЛЕТОПИСЬ ЭТОЙ СТРОКИ — НИЖЕ, В ЗАПИСЯХ «11 → 16» И «16 → 23»; прежняя формулировка «11 планов, из них 7 исполнено» НЕ БЫЛА ОШИБКОЙ и помечена устаревшей окном 44 `.planning/WINDOWS.md`: «7» было верным для момента её записи и пережило партию 10-08…10-11, а «11» — для момента ДО третьей партии закрытия гейпов. ⚠️ ЧИСЛО ОДНО И НЕ СКЛАДЫВАЕТСЯ ИЗ ДВУХ: прежняя формулировка «7/7 + 3 плана закрытия гейпов» читалась и как семь, и как десять — ровно тот документный дрейф, который отчёт верификации поймал строкой прогресса вехи (пункт `ℹ️ Info`, закрывается планом 10-11). Состав: 4 базовых (10-01…10-04) + 3 закрытия гейпов первой партии (10-05…10-07, `gap_closure: true`, запланированы 2026-09-03 по `10-VERIFICATION.md` `status: gaps_found` 2/4) + 4 закрытия гейпов второй партии (10-08…10-11, `gap_closure: true`, запланированы 2026-09-03 по повторному `10-VERIFICATION.md` `status: gaps_found` 4/6, один `gaps[0].status: failed` — блокер `CR-01`) + 5 закрытия гейпов ТРЕТЬЕЙ партии (10-12…10-16, `gap_closure: true`, запланированы 2026-09-04 по третьему `10-VERIFICATION.md` `status: gaps_found` 4/7) + 7 закрытия гейпов ЧЕТВЁРТОЙ партии (10-17…10-23, `gap_closure: true`, запланированы 2026-09-04…09-07 по четвёртому `10-VERIFICATION.md` `status: gaps_found` 6/9, один `gaps[0].status: failed` — он же `regressions[0]`). ⚠️ ПЕРЕЧЕНЬ СОСТАВА ДОПИСАН ДВУМЯ ПАРТИЯМИ, А НЕ ОДНОЙ: до настоящей правки он называл 4+3+4 = 11 при объявленных 16 — третья партия сдвинула ЧИСЛА строки и не сдвинула её ПЕРЕЧЕНЬ, и это расхождение названо здесь, а не оставлено следующему кругу. Исполненные планы не правятся, не перенумеровываются и не отменяются; волны КАЖДОЙ партии закрытия гейпов начинаются заново с 1.

### Машинно читаемое поле выведено ПРОГОНОМ разборщика

```
$ uv run python -c "… from test_state_progress_matches_roadmap import roadmap_totals …"
roadmap_totals = (54, 61)
```

Записано в поле второе число пары — `61`. ⚠️ **ОРИЕНТИР ПЛАНИРОВЩИКА ПОДТВЕРЖДЁН ТОЧНО:** пара ДО партии — `(54, 54)` (перепроверена собственным прогоном в начале исполнения), после семи неотмеченных строк — `(54, 61)`. Расхождения с ориентиром нет. Арифметика в уме не применялась ни разу.

### КРАСНЫЙ МЕЖДУ ПРАВКАМИ НАБЛЮДЁН

Прогон ПОСЛЕ правки роадмапа и ДО правки поля:

```
E   AssertionError: поле `progress.total_plans` во frontmatter `.planning/STATE.md` записано
    как 54, а из отметок `.planning/ROADMAP.md` выводится 61 — привести надо ПОЛЕ, потому что
    источником счёта планов объявлен ROADMAP
E   assert not [Divergence(field='total_plans', derived=61, recorded=54)]
…
1 failed, 4 passed, 1 warning in 0.15s
```

Оба числа видны в отказе. Прогон ПОСЛЕ правки поля: `5 passed`.

### Поле числа завершённых планов не тронуто — И ЗДЕСЬ НАХОДКА

```
$ git diff -- .planning/STATE.md | grep -c 'completed_plans'
1          ← ОРИЕНТИР ПЛАНА ГОВОРИЛ `0`
```

⚠️ **РАСХОЖДЕНИЕ С ПРИЁМОЧНЫМ КРИТЕРИЕМ НАЗВАНО, А НЕ ЗАМОЛЧАНО, И ЭТО ДЕФЕКТ САМОГО КРИТЕРИЯ, А НЕ ПРАВКИ.** Поле `completed_plans` стои́т СТРОКОЙ НИЖЕ правимого `total_plans`, поэтому при умолчательных трёх строках контекста оно попадает в дифф как КОНТЕКСТ (префикс пробела) при любой правке соседа. Критерий в такой форме неисполним по построению: он краснел бы даже на правке, которая поля не касается вовсе, — то есть дал бы отказ с НЕВЕРНОЙ АТРИБУЦИЕЙ. Замер строгими формами, отвечающими на вопрос, который критерий ЗАДАЁТ:

```
$ git diff -U0 -- .planning/STATE.md | grep -c 'completed_plans'
0
$ git diff -- .planning/STATE.md | grep -c '^[-+].*completed_plans'
0
```

Дифф целиком — одна изменённая строка:

```
-  total_plans: 54
+  total_plans: 61
   completed_plans: 54
```

### Шестнадцать прежних строк перечня и их отметки не тронуты

```
$ git diff -- .planning/ROADMAP.md | grep -c '^-.*- \[x\]'
0
$ git diff -- .planning/ROADMAP.md | grep -c '^-[^-]'
1          ← ровно строка состава, и она названа предметом правки
$ git diff --name-only -- .planning/REQUIREMENTS.md
(пусто)
```

---

## ЗАДАЧА 2 — приёмочные критерии с выводами

### Прибавка реестра атрибутирована ПОСТРО́ЧНО

```
ДО:    grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md → 48   |  total_count: 48
ПОСЛЕ: grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md → 52   |  total_count: 52
```

⚠️ **ОРИЕНТИР ПЛАНИРОВЩИКА (замер 2026-09-05: было 48, ожидается 52) ПОДТВЕРЖДЁН ТОЧНО.** Ветви «это НАХОДКА» планов 10-18, 10-19 и 10-22 пятого окна не родили: их находки уложились в три текста, выписанных планом 10-22.

| Прибавившаяся строка диффа | План-источник | Основание атрибуции |
|---|---|---|
| `+\| 49 \|` | **10-22** | ОКНО 1 сводки `10-22-SUMMARY.md` (задача 2, `WR-02`) |
| `+\| 50 \|` | **10-22** | ОКНО 2 сводки `10-22-SUMMARY.md` (задача 3, `WR-04`) |
| `+\| 51 \|` | **10-22** | ОКНО 3 сводки `10-22-SUMMARY.md` (задача 1, `WR-03`) |
| `+\| 52 \|` | **10-23** | `IN-04` ревизии, задача 2 настоящего плана |
| `+\| 44 \|` | **10-23** | НЕ НОВОЕ ОКНО: строка ПЕРЕПИСАНА командой `windows fixed 44` (задача 2, пункт (3) `<action>`) — `status` `open` → `fixed`, проставлен `resolved_at` |

Строк, не атрибутируемых ни одному плану партии, — **НОЛЬ**.

```
$ git diff -U0 -- .planning/WINDOWS.md | grep -cE '^\+\| [0-9]+ \| '
5          ← гейт краснеет при < 4; пятая строка атрибутирована выше
```

### Удалённые строки — все семь названы

```
$ git diff -U0 -- .planning/WINDOWS.md | grep -c '^-[^-]'
7
```

| Удалённая строка | Чем объясняется |
|---|---|
| `-open_count: 42`, `-fixed_count: 5`, `-total_count: 48`, `-last_updated: …` | четыре счётчика шапки, переписанные КОМАНДОЙ ведения журнала — ровно то, ради чего запись велась командой |
| `-\| 44 \| … \| open \| …` | строка окна 44, переписанная `windows fixed 44` |
| `-    "status": "open",`, `-    "resolved_at": null` | те же два поля окна 44 в машинном блоке в хвосте файла |

Ни одна строка не тронута руками: все семь — след двух команд ведения журнала.

### Три места реестра согласны

```
$ test "$(grep -cE '^\| [0-9]+ \| ' .planning/WINDOWS.md)" = "$(sed -n 's/^total_count: \([0-9]*\)$/\1/p' .planning/WINDOWS.md)" && echo AGREE || echo DISAGREE
AGREE
```

Счётчики шапки: `open_count: 45`, `waived_count: 1`, `fixed_count: 6`, `total_count: 52`. Сумма 45 + 1 + 6 = **52** = число строк таблицы = `total_count`.

### Вызовы КОМАНД ведения журнала и их вывод

```
$ node .claude/gsd-core/bin/gsd-tools.cjs windows append --kind deviation   --phase 10 --file app/templates/ads/partials/sched_delete_response.html --line 73  --description "…"
  exit 0 → total_count 49
$ … windows append --kind unmet-truth --phase 10 --file app/templates/components/modal.html --line 665 --description "…"
  exit 0 → total_count 50
$ … windows append --kind deviation   --phase 10 --file app/pages/schedules.py --line 90 --description "…"
  exit 0 → total_count 51
$ … windows append --kind deviation   --phase 10 --file app/templates/components/modal.html --description "…"   (IN-04, без --line)
  exit 0 → total_count 52, id нового окна 52
$ … windows fixed 44
  exit 0 → 44 -> fixed | resolved_at 2026-09-07T18:53:59.891Z | fixed_count 5 → 6
```

Ручной правки строк реестра не производилось ни одной.

### ТРИ перенесённых текста СЛИЧЕНЫ с оригиналом

Сличение сделано ПРОГРАММНО (разбор блокцитат сводки 10-22 против описаний, прочитанных из машинного блока реестра), а не глазом:

```
ОКНО 49: совпадение=True | длина оригинала=1937 длина переноса=1937
ОКНО 50: совпадение=True | длина оригинала=1781 длина переноса=1781
ОКНО 51: совпадение=True | длина оригинала=3539 длина переноса=3539
```

⚠️ **ЕДИНСТВЕННОЕ ПРЕОБРАЗОВАНИЕ, ПРИМЕНЁННОЕ К ТЕКСТАМ, — МЕХАНИЧЕСКОЕ И НАЗВАНО ЗДЕСЬ:** снят префикс блокцитаты `> ` и переносы строк заменены пробелами, потому что клетка таблицы реестра однострочна по построению. Ни одного слова не переписано. Проверено отдельно: вертикальной черты, которая разошлась бы с таблицей, ни в одном из трёх текстов нет.

### Четыре строки реестра целиком

**Окно 49** — `kind: deviation`, `phase: 10`, `file: app/templates/ads/partials/sched_delete_response.html`, `line: 73`, `status: open`:

> ЦЕЛЬ ТРЕТЬЕГО ВНЕПОЛОСНОГО УЗЛА ОТВЕТА УДАЛЕНИЯ ОСТАЁТСЯ СТАТИЧЕСКОЙ, И ЭТО ОБЪЯВЛЕНО, А НЕ ПРОМОЛЧАНО (план 10-22, находка `WR-02` четвёртого круга ревизии; прежнее имя того же предмета — `WR-05`). ПРЕДМЕТ: узел линейки счётчика адресован СТАТИЧЕСКИМ селектором `innerHTML:#sched-count` (`app/templates/ads/partials/sched_delete_response.html:73`, обёртка — `app/templates/ads/form.html:219`), тогда как число считается по `ad_id`, снятому с НАЙДЕННОЙ строки расписания (`app/pages/schedules.py`, `_ad_schedule_count`). ВОСПРОИЗВОДИМЫЙ ВХОД: запрос, который называет АДРЕСОМ расписание объявления B, а ПОЛЕМ КОНТЕКСТА — экран объявления A. ЦЕНА НАЗВАНА ДВУМЯ СЛЕДСТВИЯМИ: (1) в документ, открытый на объявлении A, встаёт число расписаний ЧУЖОГО объявления B; (2) оно ПЕРЕЖИВЁТ запрос — до перезагрузки страницы, потому что следующего ответа, который бы его поправил, не будет. ДОСТИЖИМОСТЬ ИЗ ИНТЕРФЕЙСА НУЛЕВАЯ И ИЗМЕРЕНА ЧТЕНИЕМ: обе формы пути удаления шлют `ad_id` СВОЕГО экрана (`ads/includes/sched_card.html:264` и `:291`). ГРАНИЦ ПРИВИЛЕГИЙ СЛУЧАЙ НЕ ПЕРЕСЕКАЕТ, И ЭТО ТОЖЕ ИЗМЕРЕНО: выборка счётчика скоуплена владельцем объявления (`Ad.user_id == user_id`, `app/pages/schedules.py:493`), поэтому чужое объявление даёт НОЛЬ. ПОЧЕМУ НЕ ПОЧИНЕНО ЗДЕСЬ — ОСНОВАНИЕМ СЛУЖИТ ОТСУТСТВИЕ НАБЛЮДАЕМОЙ ОПОРЫ, А НЕ ОЦЕНКА ЗАТРАТ: правка меняет цель узла у ВСЕХ его потребителей, а наблюдается она ЛИНЕЙКОЙ СЧЁТЧИКА — шагом 1.8 обхода `10-UAT.md`, который не пройден НИ РАЗУ, то есть опоры, относительно которой правку можно было бы судить, сегодня не существует ни одной. ⚠️ ВТОРАЯ ЗАПИСЬ О ТОМ ЖЕ ПРЕДМЕТЕ — ЗЕЛЁНОЕ ПРАВИЛО СУИТЫ `test_the_counter_node_belongs_to_the_ad_named_by_the_request` (`tests/test_pages/test_confirm_delete_transport.py`), УТВЕРЖДАЮЩЕЕ сегодняшнее поведение НОРМОЙ; предмет им закреплён, а не закрыт. ФАЗА-ВЛАДЕЛЕЦ НЕ НАЗНАЧЕНА НАМЕРЕННО: назначение чужой фазе есть решение вехи, а не плана (форма окон 34 и 47).

**Окно 50** — `kind: unmet-truth`, `phase: 10`, `file: app/templates/components/modal.html`, `line: 665`, `status: open`:

> ПУНКТ «ПЛОЩАДКА ПРИЗЕМЛЕНИЯ ФОКУСА ВЫБРАНА» ИСПОЛНЕН НЕ БЕЗ ОСТАТКА, И ОСТАТОК НАЗВАН (план 10-22, находка `WR-04` четвёртого круга ревизии). ПРЕДМЕТ: выражение приземления `land(back)` (`app/templates/components/modal.html:665`) при отсутствии открывавшего узла целится в область уведомлений `#notice` (`app/templates/includes/notice_area.html:135`). ЧТО ИСПОЛНЕНО: площадка ВЫБРАНА, она ОДНА на все 18 мест подтверждения, параметром макроса не стала. ЧТО НЕ ИСПОЛНЕНО: обещание «фокус приезжает в ОСМЫСЛЕННОЕ МЕСТО» — на ФРАГМЕНТНОМ пути, том самом, ДЛЯ КОТОРОГО площадка и выбиралась, область ПУСТА ВСЕГДА (плашки на успех нет, D-03; ни один из двух фрагментных ответов фазы кода уведомления не несёт). ДВА ИЗМЕРЕННЫХ ФАКТА, ПЕРЕПРОВЕРЕННЫХ ЧТЕНИЕМ, А НЕ ПЕРЕПИСАННЫХ ИЗ ОТЧЁТА: (1) У ПЛОЩАДКИ НЕТ ДОСТУПНОГО ИМЕНИ — узел области несёт `id`, роль `status`, живость `polite` и `tabindex="-1"`, и ни одного атрибута имени: поиск `aria-label`, `aria-labelledby` и `title` по файлу даёт НОЛЬ вхождений; (2) ПРИЗЕМЛЕНИЕ ФОКУСА В ОБЛАСТЬ ЖИВОГО ОБЪЯВЛЕНИЯ ДАЁТ РАСХОДЯЩЕЕСЯ ПОВЕДЕНИЕ РАНТАЙМОВ ЧТЕНИЯ С ЭКРАНА — часть перечитывает область при получении фокуса, часть молчит, то есть наблюдаемый исход у пункта не один, а два. ПОЧЕМУ ПРАВКА НЕ ДЕЛАЕТСЯ ПАРТИЕЙ ЗАКРЫТИЯ ГЕЙПОВ: площадку выбрало РЕШЕНИЕ ВЛАДЕЛЬЦА **D-11**, цена («на успехе область пуста») названа там же, и партия решений владельца не пересматривает — она сузила ПРОЧТЕНИЕ, а не решение. ⚠️ ВЛАДЕЛЕЦ ОСТАТКА УЖЕ ЗАПИСАН И НЕ ИЗОБРЕТАЕТСЯ ЗАНОВО: сводный обход доступности **Фазы 15 (GATE-09)** — тот же адресат, что назван абзацем «ЧТО ЗДЕСЬ НЕ РЕШАЕТСЯ» в самой шапке рычага и пунктом `Deferred Ideas` записи решений фазы. Второго назначения не заводится: два независимых назначения одного остатка разошлись бы молча.

**Окно 51** — `kind: deviation`, `phase: 10`, `file: app/pages/schedules.py`, `line: 90`, `status: open`:

> СЕМЬ ВХОДОВ СТРАНИЧНОГО СЛОЯ РАСХОДЯТСЯ С ЗАПЕРТЫМ РЕШЕНИЕМ **D-01** — ФОРМУ ОТКАЗА ВЫБИРАЕТ НЕ КЛАСС ДЕЙСТВИЯ, А ФРЕЙМВОРК, — И ВОПРОС, ОБЪЯВИТЬ ЛИ ЭТО ИМЕНОВАННЫМ ИЗЪЯТИЕМ ИЗ D-01 ПО ПРЕЦЕДЕНТУ D-08 ЛИБО СНЯТЬ РАСХОЖДЕНИЕ ПЕРЕНОСОМ ГРАНИЦЫ, **ЖДЁТ ВЛАДЕЛЬЦА** (план 10-22, находка `WR-03` четвёртого круга ревизии). ПЯТЬ ВХОДОВ НАЗЫВАЛ ОТЧЁТ, А ЗАМЕР ДАЛ СЕМЬ, И ЗАПИСАН ЗАМЕР. Перечень снят разбором дерева `app/pages/*.py` (параметры POST-обработчиков с границей `Path()`/`Form()`, несущей `ge=`/`le=`): `POST /schedules/new` → форма `ad_id` (`:825`) и форма `account_id` (`:832`); `POST /schedules/{schedule_id}/edit` → адрес `schedule_id` (`:917`), форма `ad_id` (`:918`), форма `account_id` (`:921`); `POST /schedules/{schedule_id}/toggle` → адрес `schedule_id` (`:998`); `POST /schedules/{schedule_id}/delete` → адрес `schedule_id` (`:1049`). Отчёт и правило `test_no_schedule_route_answers_with_a_handler_failure_on_an_out_of_range_identifier` считали ИСХОДЫ ПЯТИ ЗАПРОСОВ по одному параметру на запрос; маршрут правки несёт ТРИ ограниченных параметра, и два входа — `edit/форма ad_id` и `edit/форма account_id` — не назывались НИГДЕ. ЦЕНА НАЗВАНА ДВУМЯ СЛЕДСТВИЯМИ: (1) тело отказа НЕ ЕСТЬ СТРАНИЦА — отправитель обычной формы получает `{"detail": [...]}`, ровно ту форму, которую проект уже назвал дефектом у `app/pages/accounts.py`; (2) на пути разметочного рантайма код отказа НЕ НЕСЁТ ЗАГОЛОВКА ПЕРЕХОДА, ветвь закрытия панели не срабатывает, и панель остаётся открытой без объяснения — кода реестра в ответе нет. ДОСТИЖИМОСТЬ ИЗ ИНТЕРФЕЙСА НУЛЕВАЯ У ВСЕХ СЕМИ И ИЗМЕРЕНА ЧТЕНИЕМ РАЗМЕТКИ, а не перенесена из ревизии: идентификаторы пути стоят в адресах форм (`ads/includes/sched_card.html:122`, `:156`, `:255`, `:276`; `schedules/includes/schedule_row.html:81`), поля объявления — скрытые (`ads/form.html:242`, `sched_card.html:157`), поле аккаунта шлётся радиокнопками (`sched_card.html:170`) и при снятом выборе не шлётся вовсе. ФОРМА, КОТОРОЙ РЕШЕНИЕ ОБЯЗАНО БЫТЬ ЗАПИСАНО, ЕСЛИ ВЛАДЕЛЕЦ ЕГО ПРИМЕТ, — ПРЕЦЕДЕНТ **D-08**: единственное изъятие из D-01 в этом проекте названо там «ИМЕНОВАННЫМ ИЗЪЯТИЕМ ИЗ D-01» и записано ВЛАДЕЛЬЦЕМ на этапе обсуждения, с измеренным основанием. Поэтому план изъятия НЕ ЧЕКАНИТ: он завёл реестр `VALIDATION_REFUSAL_DIVERGENCES` (`tests/test_pages/test_htmx_gates.py`) с состоянием решения «ЖДЁТ ВЛАДЕЛЬЦА» у каждой из семи записей, а правило `test_no_divergence_claims_a_decision_the_owner_did_not_make` краснит на записи, объявившей себя решённой без ссылки на решение владельца (`D-NN`) либо номер закрытого окна реестра. ⚠️ МАШИННЫЙ ГЕЙТ ОТ ОТСРОЧКИ РЕШЕНИЯ НЕ СЛАБЕЕТ: вход, разошедшийся с D-01 и в реестре не названный, краснит прогон с именем. Отложено РЕШЕНИЕ о легитимности расхождения, а не его ВИДИМОСТЬ, — ревизия назвала молчаливое умолчание ХУДШИМ из трёх исходов, «потому что оно читается как исполненный инвариант». УСЛОВИЕ СНЯТИЯ САМОГО РАСХОЖДЕНИЯ ВЗЯТО ИЗ РОАДМАПА, А НЕ НАЗНАЧЕНО: оно снимается вместе с переносом границы внутрь обработчиков, а перенос осмыслен только вместе с массовым переводом маршрутов на слой ответа — **Фаза 11**, критерий 3 («ошибка валидации возвращает 422, перерисовывает форму и возвращает введённое эхом»), порядок внутри фазы ставит `schedules` первым. ⚠️ **ФАЗА-СНИМАТЕЛЬ САМОГО РЕШЕНИЯ НЕ НАЗНАЧЕНА, И ОСНОВАНИЕ ЗАПИСАНО:** изъятие из ЗАПЕРТОГО решения владельца принадлежит ВЛАДЕЛЬЦУ, и сочинять назначение планом запрещено (прецедент окон 34 и 47). Фаза 11 снимает расхождение РАБОТОЙ; законно ли расхождение до тех пор — решает владелец.

**Окно 52** — `kind: deviation`, `phase: 10`, `file: app/templates/components/modal.html`, `line:` (пусто), `status: open`:

> ОБЪЁМ КОММЕНТАРИЯ В ПРАВЛЕНЫХ ФАЙЛАХ ФАЗЫ ПРЕВЫСИЛ ОБЪЁМ КОДА НА ПОРЯДОК, И В ОДНОМ МЕСТЕ ЖИВУТ ТРИ ПОКОЛЕНИЯ ФОРМУЛИРОВОК (находка `IN-04` ревизии `10-REVIEW.md:517-534`, четвёртый круг). ИЗМЕРЕННАЯ ВЕЛИЧИНА, СНЯТАЯ РЕВИЗИЕЙ ПОИМЁННО ПО ФАЙЛАМ: `app/templates/components/modal.html` — 690 строк, из них ~655 комментария на 34 строки разметки; `app/pages/schedules.py:272-366` — докстринг `_ad_id_from_form` на 95 строк при 6 строках тела, с пунктами (а)–(е), где (г) помечен «ОПРОВЕРГНУТО», а (д) его заменяет. ЧТО ИМЕННО ДЕФЕКТ, А ЧТО НЕТ: идиома «опровергнутое не вычёркивается» есть ЗАПИСАННОЕ РЕШЕНИЕ ПРОЕКТА (D-30/D-32) и дефектом НЕ ЯВЛЯЕТСЯ — окно заведено на СТОИМОСТЬ ЧТЕНИЯ, а не на идиому: определить ДЕЙСТВУЮЩУЮ формулировку в `modal.html` можно только полным чтением файла, где рядом стоят «ПРИЗЕМЛЕНИЕ ЖИВЁТ В ОБЕИХ ВЕТВЯХ… Это утверждение ВЕРНО» и следующим абзацем «АТРИБУЦИЯ ЭТИХ ДВУХ ВЕТВЕЙ… ОПРОВЕРГНУТА РАНТАЙМОМ». СТОИМОСТЬ ОШИБКИ ЧТЕНИЯ НАЗВАНА ФАКТОМ, А НЕ ОПАСЕНИЕМ: гэп 1 ПЕРВОГО круга верификации этой же фазы возник РОВНО из расхождения записи с рантаймом. ПРЕДЛОЖЕННАЯ ПРАВКА (её текст принадлежит ревизии, а не окну): машинно читаемая разметка слоёв утверждения — префикс [ДЕЙСТВУЕТ] / [ОПРОВЕРГНУТО <план>] на каждом абзаце-утверждении, с правилом, требующим у КАЖДОГО опровергнутого абзаца действующего преемника; летопись при этом сохраняется, а восстанавливать порядок поколений глазом больше не нужно. ⚠️ ФАЗА-ВЛАДЕЛЕЦ НЕ НАЗНАЧЕНА НАМЕРЕННО: соглашение о разметке слоёв действует на КАЖДЫЙ файл проекта, то есть есть решение уровня ВЕХИ, а не побочная правка партии закрытия гейпов; назначение принадлежит владельцу вехи, и сочинять его планом запрещено (прецедент окон 34 и 47).

### ТРЕТЬЕ ОКНО ПЕРЕНЕСЕНО КАК ВОПРОС, А НЕ КАК ОТВЕТ

Строка окна 51 приведена целиком выше. Замер по её тексту:

```
«изъятие принято» в тексте: False
«ЖДЁТ ВЛАДЕЛЬЦА»: 2 вхождения
«ФАЗА-СНИМАТЕЛЬ САМОГО РЕШЕНИЯ НЕ НАЗНАЧЕНА»: True
```

Фаза-сниматель РЕШЕНИЯ не назначена; формулировки «изъятие принято» в окне нет ни в каком виде. Два адресата разведены в самом тексте окна и здесь повторены отдельно: **Фаза 11** снимает расхождение РАБОТОЙ (перенос границы внутрь обработчиков), а фаза-владелец самого РЕШЕНИЯ об изъятии из запертого D-01 не назначена и назначена планом быть не может.

### Оба окна БЕЗ фазы-снимателя — цитаты оснований

- **Окно 51:** «⚠️ **ФАЗА-СНИМАТЕЛЬ САМОГО РЕШЕНИЯ НЕ НАЗНАЧЕНА, И ОСНОВАНИЕ ЗАПИСАНО:** изъятие из ЗАПЕРТОГО решения владельца принадлежит ВЛАДЕЛЬЦУ, и сочинять назначение планом запрещено (прецедент окон 34 и 47).»
- **Окно 52 (`IN-04`):** «⚠️ ФАЗА-ВЛАДЕЛЕЦ НЕ НАЗНАЧЕНА НАМЕРЕННО: соглашение о разметке слоёв действует на КАЖДЫЙ файл проекта, то есть есть решение уровня ВЕХИ, а не побочная правка партии закрытия гейпов; назначение принадлежит владельцу вехи, и сочинять его планом запрещено (прецедент окон 34 и 47).»

### Ни одно основание отсрочки не есть оценка труда

```
$ git diff -- .planning/WINDOWS.md | grep -ci -e 'сложн' -e 'трудоём' -e 'позже'
0
```

### СОСТОЯНИЕ ОКНА 44 РЕШЕНО ЗАМЕРОМ

**Текст окна 44 (до закрытия):** «Строка состава раздела Фазы 10 говорит «11 планов, из них 7 исполнено»: число «7» верно для момента записи и пережило партию 10-08…10-11 (замер счётом отметок даёт 11 из 11). План 10-11 правку раздела фазы запрещает прямо и требует брать число СЧЁТОМ отметок, а не из строки состава, поэтому строка названа устаревшей летописью расхождения, но не переписана. **Остаток передан дальше**»

**Строка состава после задачи 1:** «**Plans**: 23 плана, из них 16 исполнено…» — оба числа сняты замером (23 строки перечня, 16 отметок).

**ПРЯМОЙ ОТВЕТ: предмет ЗАКРЫТ.** Остаток, который окно передало дальше, — привести числа строки состава замером, — взят настоящим планом: строка больше не несёт значения, пережившего работу, и её летопись записана рядом. Окно закрыто КОМАНДОЙ:

```
$ node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 44
{'open_count': 45, 'waived_count': 1, 'fixed_count': 6, 'total_count': 52}
44 -> fixed | resolved_at 2026-09-07T18:53:59.891Z
```

⚠️ **ДВА РАСХОЖДЕНИЯ САМОГО ОКНА С ДЕРЕВОМ НАЗВАНЫ ЗДЕСЬ, А НЕ ИСПРАВЛЕНЫ ЗАДНИМ ЧИСЛОМ.** (1) Окно цитирует формулировку «11 планов, из них 7 исполнено», которой в дереве нет с третьей партии: та переписала строку в «16 планов, из них 11 исполнено», не тронув окна. (2) Поле `line: 287` мимо цели: строка состава стои́т на 283-й, а на 287-й — подзаголовок `Plans:`. Ни то, ни другое не правится: текст закрытого окна есть архив того, что было видно тогда, и правка превратила бы его в переписанную задним числом историю.

⚠️ **ЧЕГО ЗАКРЫТИЕ ОКНА 44 НЕ УТВЕРЖДАЕТ:** что число «16 исполнено» переживёт простановку отметок семи новым строкам. Не переживёт — и это НЕ дефект, а ровно тот механизм, ради которого у фазы четыре летописи: число, верное в момент своей записи, устаревает от работы, которую оно переживает. Отметки ставит оркестратор ПО ЗАВЕРШЕНИИ, и следующее приведение — предмет следующей записи, а не незакрытого окна.

---

## ЗАДАЧА 3 — приёмочные критерии с выводами

### Раздел стои́т РЯДОМ, а не вместо

Порядок разделов артефакта после правки:

1. `## Что закрыто МАШИННО — проверять заново не нужно` (пополнен восемью строками)
2. `## Что в этот обход НЕ входит` (не тронут)
3. `## Чего третья партия закрытия гейпов НЕ закрыла` (не тронут ни символом)
4. `## Чего ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла` ← **новый**
5. `## Куда артефакт уезжает дальше` (не тронут)

```
$ grep -c 'ЧЕТВЁРТАЯ партия закрытия гейпов НЕ закрыла' 10-UAT.md
1
$ grep -c 'Чего третья партия закрытия гейпов НЕ закрыла' 10-UAT.md
1          ← не снят и не задвоен
```

### Названы ВСЕ пункты — счёт и перечень

**Шестнадцать строк таблицы**, из них тринадцать — предметы, две — предупреждения о неслиянии записей, одна — объяснение расхождения перечней рёбер:

| № | Пункт | Откуда взят |
|---|---|---|
| 1 | Критерий 2 роадмапа остаётся ЧЕЛОВЕЧЕСКИМ (шаги 1.1-1.8) | `behavior_unverified_items[0]` отчёта |
| 2 | `CR-03`, БРАУЗЕРНАЯ ПОЛОВИНА (шаги 1.13-1.15) | `behavior_unverified_items[1]` отчёта |
| 3 | ⚠️ предупреждение: пункты 1-2 и 4-5 — одни предметы с ДВУХ сторон, сведению не подлежат | находка настоящего плана |
| 4 | Критерий 2 — счёт диалогов и ловушка фокуса (шаги 1.1-1.9) | `human_verification[0]`, перенесено ДОСЛОВНО из `<not_closed_by_this_plan>` плана 10-20 |
| 5 | Переходная ветка и живость клиентского слоя (шаги 1.10-1.15) | `human_verification[2]`, ДОСЛОВНО |
| 6 | Уборка панели при не доехавшем follow-up GET (проверка 2, окно 28) | `human_verification[3]`, ДОСЛОВНО |
| 7 | Панель ЗАНЯТА, а не сломана (проверка 3, окно 35) | `human_verification[4]`, ДОСЛОВНО |
| 8 | Панель остаётся открытой на отказе (проверка 4, окно 22) | `human_verification[5]`, ДОСЛОВНО |
| 9 | Приземление фокуса после ухода панели (проверка 5) | `human_verification[6]`, ДОСЛОВНО |
| 10 | Документная половина закрытия `DEF-09-02` (проверка 6, окно 36) | седьмая строка `<not_closed_by_this_plan>`, ДОСЛОВНО |
| 11 | Ребро зонда `idempotency` — неразрешено | явное допущение партии (frontmatter планов 10-17 и 10-23) |
| 12 | Ребро зонда `concurrency` — неразрешено | то же |
| 13 | ⚠️ объяснение: перечень рёбер расходится с перечнем третьей партии — ДВА ЗОНДА | находка настоящего плана |
| 14 | Цель третьего внеполосного узла (`WR-02`; окно 49) | отложенная половина находки ревизии |
| 15 | Остаток пункта площадки приземления фокуса (`WR-04`; окно 50) | отложенная половина находки ревизии |
| 16 | Машинно читаемая разметка слоёв (`IN-04`; окно 52) | отложенная половина находки ревизии |
| 17 | РЕШЕНИЕ о законности расхождения с D-01 не принято (`WR-03`; окно 51) | добавлено сверх перечня плана — см. отклонение 2 |
| 18 | Долг направления отказа планов 10-01…10-07 (окно 48) | долг, партией не правленный |

⚠️ Строк в таблице **восемнадцать**, а не шестнадцать: две из них — предупреждения (3 и 13), не являющиеся пунктами непокрытого. Предметов, таким образом, **шестнадцать**: 2 (`behavior_unverified_items`) + 7 (`human_verification`) + 2 (рёбра) + 3 (отложенные половины) + 1 (решение по D-01) + 1 (долг окна 48).

Отдельными абзацами ПОД таблицей, а не строками, — ещё два: **окно 47** (восьмое измерение Найквиста; три пункта раздела предыдущей партии остаются в силе и строкой не повторяются) и **`FORM-06` остаётся незавершённым** с неизменившимся условием перевода.

### Ни одно основание не есть оценка труда

```
$ git diff -- .planning/phases/10-rychag-components-modal-html/10-UAT.md | grep -ci -e 'сложн' -e 'трудоём' -e 'позже'
0
```

### Восемь новых строк перечня «Что закрыто МАШИННО» — все с ГРАНИЦЕЙ

| Утверждение (план) | ГРАНИЦА доказанного (третий столбец) |
|---|---|
| Гейты записи каталога `tests/test_planning/` не зависят от вердикта живого дерева (**10-17**) | Зелено на ВСЕХ ЧЕТЫРЁХ состояниях вердикта. **НЕ доказано ничего о модулях ВНЕ каталога**; сам гейт из обхода исключён с записанным основанием |
| Четыре маршрута необратимого удаления проверяют происхождение (**10-18**) | Гард СТОИТ и перечень не растёт молча. ⚠️ **НЕ доказано, что он ЗАЩИЩАЕТ:** граница пропускает запрос без обоих заголовков — а это КАЖДЫЙ запрос суиты (`human_judgment: true` сводки 10-18) |
| Гейт цели перехода читает ВСЮ цепь шаблонов (**10-19**) | Объявление в БАЗЕ находится, база называется поимённо, цепь не вырождается молча. **НЕ доказано, что БРАУЗЕР ведёт себя по цепи** — шаги 1.13-1.15 |
| Артефакт обхода не объявит себя пройденным при пустых отметках (**10-20**) | Запись не шире отметок при ДВУХ терминальных состояниях. ⚠️ **О САМОМ ПРОХОЖДЕНИИ НЕ ДОКАЗАНО НИЧЕГО** |
| Двадцать якорей наблюдения присутствуют в шаблонах (**10-20**) | ПРИСУТСТВИЕ узла. **НЕ доказано, что шаг наблюдает через него обещанное** |
| Индекс отчётов ключуется парой «веха, фаза» (**10-21**) | Столкновение падает громко, разные вехи сосуществуют. ⚠️ **МЕХАНИЗМ СТОИТ НА СЛУЧАЙ:** `.planning/REQUIREMENTS.md:119-120` утверждает обратное посылке плана — фазы v2.1 суть 7…15, нумерация ПРОДОЛЖАЕТ v2.0 |
| Расхождение формы отказа СЕМИ входов объявлено машинно (**10-22**) | Неназванный вход краснит прогон; «решено» без ссылки на владельца краснит. ⚠️ **ЗАКОННОСТЬ РАСХОЖДЕНИЯ МАШИНОЙ НЕ ДОКАЗУЕМА** — окно 51 |
| Два места счёта планов не расходятся молча (**10-23**) | Поле равно выведенному из отметок. **НЕ доказано, что отметки соответствуют исполненному:** правило читает ЗАПИСЬ, а не дерево |

### Имена правил СЛИЧЕНЫ, а не выписаны по памяти

Тридцать пять имён из дописанных строк проверены ДВАЖДЫ — против дерева (объявление `def <имя>(`) и против сводок партии:

```
имён РОВНО в дописанных строках: 35
найдено в дереве:                35 / 35
найдено в сводках партии:        34 / 35
```

Единственное имя, которого в сводках партии нет, — `test_the_machine_readable_progress_is_derived_from_the_roadmap`: оно принадлежит правилу плана **10-23**, чья сводка есть настоящий документ. В дереве оно найдено (`tests/test_planning/test_state_progress_matches_roadmap.py`).

Примеры пар «имя в артефакте / файл в дереве / сводка»:

| Имя | Дерево | Сводка |
|---|---|---|
| `test_no_planning_gate_asserts_a_live_verdict_literal` | `tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py` | `10-17-SUMMARY.md` |
| `test_every_destructive_route_checks_the_origin` | `tests/test_pages/test_origin_guard_on_destructive_routes.py` | `10-18-SUMMARY.md` |
| `test_no_transition_destination_declares_a_top_level_binding_inline` | `tests/test_pages/test_hx_location_destinations.py` | `10-19-SUMMARY.md` |
| `test_no_walkthrough_declares_itself_passed_with_empty_marks` | `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` | `10-20-SUMMARY.md` |
| `test_the_verification_index_is_built_by_walking_the_whole_record_tree` | `tests/test_planning/test_requirement_completion_follows_verification.py` | `10-21-SUMMARY.md` |
| `test_no_divergence_claims_a_decision_the_owner_did_not_make` | `tests/test_pages/test_htmx_gates.py` | `10-22-SUMMARY.md` |

### Три счёта проверок остались равными

```
ДО правки:    WalkthroughCounts(state='testing', declared_checks=6, check_sections=6, mark_tables=6, filled_marks=0)
ПОСЛЕ правки: WalkthroughCounts(state='testing', declared_checks=6, check_sections=6, mark_tables=6, filled_marks=0)
```

### Отметок не заполнено ни одной

```
$ grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' 10-UAT.md
0
```

### Шапка не тронута

```
$ git diff -U0 -- .planning/phases/10-rychag-components-modal-html/10-UAT.md | head -20
@@ -509,0 +510,8 @@   ← восемь строк перечня закрытого машинно
@@ -549,0 +558,46 @@  ← сорок шесть строк нового раздела
```

Дифф ЧИСТО ДОПИСЫВАЮЩИЙ: строк с префиксом `-` в нём нет ни одной, первая правка стои́т на 510-й строке — шапка (строки 1-15) не задета.

### `09-UAT.md` и `.planning/REQUIREMENTS.md` не тронуты

```
$ git diff --name-only -- .planning/phases/09-*/ .planning/REQUIREMENTS.md
(пусто)
```

### Граф проекта обновлён

```
$ graphify update .
  AST extraction: 13/13 uncached files (100%)
  [graphify watch] Rebuilt: 17235 nodes, 31080 edges, 974 communities
  graph.json, graph.html и GRAPH_REPORT.md updated in graphify-out
EXIT=0
```

Побочного эффекта на приёмочные критерии нет, и это перепроверено: `git check-ignore -v graphify-out/graph.json` → `.gitignore:56 **/graphify-out/`; `git status --short` после прогона показывает только правленый `10-UAT.md`.

---

## Плановая `<verification>` — все шесть пунктов

| № | Команда | Результат |
|---|---|---|
| 1 | `uv run pytest tests/test_planning/ -q -p no:randomly` | **31 passed**, exit 0 |
| 2 | `uv run pytest tests/ -q -p no:randomly` | **2860 passed**, exit 0, 34 мин 12 с — посимвольно наследованная база партии |
| 3 | `grep -c '^- \[ \] 10-' .planning/ROADMAP.md` | **7** |
| 4 | согласие трёх мест реестра / прибавка строк | **AGREE**; прибавившихся строк **5** (≥ 4), пятая атрибутирована |
| 5 | `grep -cE '^\| *[0-9]{4}-[0-9]{2}-[0-9]{2} *\|' 10-UAT.md` | **0** |
| 6 | `git diff --name-only a89b2c8..HEAD -- .planning/REQUIREMENTS.md .planning/phases/09-*/` | пусто |

### Пункт 6, вторая половина: ПОСТРО́ЧНАЯ АТРИБУЦИЯ следа партии в `app/` и `tests/`

Собственный след настоящего плана — `git diff --name-only a89b2c8..HEAD` — это ровно три файла `.planning/` (плюс `10-UAT.md` четвёртым коммитом): ни `app/`, ни `tests/` он не касается. След ПАРТИИ (`git diff --name-only 95c2fbf..HEAD -- app/ tests/`) — семнадцать путей, и каждый атрибутирован плану-источнику ПРОГОНОМ по его коммитам:

| План | Пути |
|---|---|
| **10-17** | `tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py`, `tests/test_planning/test_requirement_completion_follows_verification.py` |
| **10-18** | `app/pages/account_groups.py`, `app/pages/accounts.py`, `app/pages/ads.py`, `app/pages/common.py`, `app/pages/htmx.py`, `app/pages/schedules.py`, `tests/test_pages/test_htmx_response_layer.py`, `tests/test_pages/test_origin_guard_on_destructive_routes.py` |
| **10-19** | `tests/test_pages/test_confirm_delete_transport.py`, `tests/test_pages/test_hx_location_destinations.py`, `tests/test_templates/test_components.py` |
| **10-20** | `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` |
| **10-21** | `tests/test_planning/test_requirement_completion_follows_verification.py` |
| **10-22** | `app/pages/schedules.py`, `app/templates/ads/partials/sched_delete_response.html`, `app/templates/components/modal.html`, `tests/test_pages/test_htmx_gates.py` |

Путей, не атрибутируемых НИ ОДНОМУ плану партии, — **НОЛЬ**. Замер сошёлся с ожиданием, записанным пунктом 6 плана, дословно: 10-18 — шесть модулей `app/pages/` и два `tests/test_pages/`; 10-19 — три модуля суиты; 10-22 — свои четыре пути.

### Пункт 7 — человеческая половина, НЕ СНЯТАЯ И НЕ ОБЪЯВЛЕННАЯ СНЯТОЙ

Шесть проверок обхода `10-UAT.md` на локальном стенде в браузере НЕ ПРОВОДИЛИСЬ; шесть таблиц отметок остаются ПУСТЫМИ. Критерий 2 роадмапа и требование `FORM-06` остаются НЕЗАКРЫТЫМИ, и ни один машинный прогон партии этого не меняет.

---

## Decisions Made

- **Перечень состава строки `**Plans**:` дописан ДВУМЯ партиями, а не одной.** До правки он называл 4 + 3 + 4 = 11 при объявленных в той же строке 16: третья партия сдвинула ЧИСЛА строки и не сдвинула её ПЕРЕЧЕНЬ. Оставить перечень при себе значило бы записать 4 + 3 + 4 + 7 = 18 против объявленных 23 — то есть воспроизвести в собственной правке ровно тот дрейф, который партия закрывает в чужих местах.
- **Окно 44 закрыто, а не оставлено открытым.** Его остаток — привести числа строки состава ЗАМЕРОМ — взят задачей 1 целиком. Отдельно записано, чего закрытие НЕ утверждает: что «16 исполнено» переживёт простановку отметок семи новым строкам.
- **Расхождения самого окна 44 с деревом названы, но не исправлены.** Цитата окна отстала на поколение, `line: 287` мимо цели. Текст закрытого окна есть архив того, что было видно тогда.
- **Перечень рёбер зонда покрытия НЕ сведён с перечнем предыдущей партии.** Третья партия называет неразрешёнными `ordering` и `concurrency`; четвёртая — `idempotency` и `concurrency`. Это ДВА РАЗНЫХ ЗОНДА над `must_haves` двух разных партий, и объяснение записано отдельной строкой раздела: сведённые в одну, они потеряли бы авторство обоих.
- **Мутации `.planning/STATE.md` через verbs GSD не исполнялись.** `state.advance-plan`, `state.update-progress`, `state.record-metric`, `state.record-session` и `roadmap update-plan-progress` переписывают прозаические разделы, которые план правит ЗАПРЕЩАЕТ («правило их не читает и читать не имеет права, но и план их не правит»), а `roadmap update-plan-progress` тронул бы строку прогресса, объявленную неприкосновенной. Единственное поле, которое план приводит, приведено прямой правкой по ЗАМЕРУ — ровно как предписано владельцем записи.
- **`FORM-06` не переводится в завершённое и `requirements.mark-complete` не вызывался.** Условие перевода — вердикт `passed` повторной верификации, которого настоящий план не выносит; `requirements-completed` во frontmatter пуст осознанно, а не по забывчивости.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] Перечень состава правимой строки противоречил числу в той же строке**

- **Found during:** Задача 1, пункт (3) `<action>`
- **Issue:** Строка `**Plans**:` объявляла 16 планов, а её собственный перечень состава называл 4 + 3 + 4 = 11: третья партия закрытия гейпов не дописала себя в перечень.
- **Fix:** Перечень дописан ДВУМЯ партиями сразу — третьей (5 планов, 10-12…10-16) и четвёртой (7 планов, 10-17…10-23), — и расхождение названо в самой строке прямо, а не молча закрыто.
- **Files modified:** `.planning/ROADMAP.md`
- **Verification:** 4 + 3 + 4 + 5 + 7 = 23 = замер перечня раздела; `git diff | grep -c '^-[^-]'` → 1 (та же одна строка).
- **Committed in:** `04bd2c6`

**2. [Rule 2 — Missing Critical] Перечень непокрытого не называл РЕШЕНИЕ, ждущее владельца**

- **Found during:** Задача 3, пункт (3) `<action>`
- **Issue:** План перечисляет для раздела ТРИ отложенные половины находок ревизии и не называет среди них расхождение формы отказа валидации с запертым D-01 (`WR-03`, окно 51) — при том, что это ЕДИНСТВЕННЫЙ предмет партии, где не сделана не запись и не работа, а не принято РЕШЕНИЕ, и адресат его — владелец, а не фаза.
- **Fix:** Добавлена семнадцатая строка таблицы с собственным основанием: видимость обеспечена машинно, решение передано окном 51, два адресата (Фаза 11 работой / владелец решением) разведены и сведению не подлежат.
- **Files modified:** `.planning/phases/10-rychag-components-modal-html/10-UAT.md`
- **Verification:** отрицательный поиск на оценку труда → 0; гейт плана 10-20 → 8 passed; три счёта проверок не сдвинулись.
- **Committed in:** `631aa0c`

---

**Total deviations:** 2 (1 расхождение записи с самой собой, 1 недостающий пункт перечня).
**Impact on plan:** Оба — внутри предмета своих задач и без расширения охвата: ни одной правки `app/`, `tests/`, `pyproject.toml`, `.planning/REQUIREMENTS.md`, `09-UAT.md`, исполненных планов и их сводок. Ни одна прохибиция плана не нарушена: шестнадцать прежних строк перечня и их отметки не тронуты, отметки обхода не заполнены, клетка `FORM-06` не переведена, строки реестра руками не правились, фаза-сниматель не сочинена ни одному окну.

## Issues Encountered

**Приёмочный критерий «`git diff -- .planning/STATE.md | grep -c 'completed_plans'` → 0» неисполним по построению.**
Поле `completed_plans` стои́т строкой ниже правимого `total_plans` и попадает в дифф как КОНТЕКСТ при умолчательных трёх строках. Критерий в такой форме краснел бы даже на правке, поля не касающейся, — отказ с НЕВЕРНОЙ АТРИБУЦИЕЙ того же класса, который партия чинила в `<verification>` планов 10-22 и 10-23. Замер приведён обеими строгими формами (`-U0` → 0; фильтр по изменённым строкам → 0) и полным диффом в одну строку. ⚠️ **ЭТО НАХОДКА ДЛЯ СЛЕДУЮЩЕГО ПЛАНИРОВЩИКА, А НЕ ОТКАЗ ПРАВКИ:** предмет критерия исполнен, форма критерия — нет.

**Полный прогон суиты исполнен ОДИН раз на три задачи.** Замер цены: 34 мин 12 с. Три прогона стоили бы более полутора часов при том, что каждая задача несёт собственные адресные гейты, исполненные на своём месте (задача 1 — `5 passed` и `31 passed`; задача 2 — `31 passed`; задача 3 — `8 passed`). Итоговый прогон исполнен ПОСЛЕ всех трёх правок и покрывает их все: **2860 passed**, exit 0.

## User Setup Required

None — внешней настройки план не требует.

## Next Phase Readiness

- **Партия закрыта записью, но НЕ вердиктом.** Следующий вход — повторная (пятая) верификация Фазы 10: единственный провал четвёртого круга (`gaps[0]`, он же `regressions[0]`) закрыт планом 10-17 всеми четырьмя пунктами `missing`, и это надо подтвердить независимым прогоном, а не принять по сводке.
- **`FORM-06` остаётся НЕЗАВЕРШЁННЫМ.** Условие перевода не изменилось и настоящей партией не приближено ни на шаг в человеческой своей половине: прохождение шести проверок обхода ЧЕЛОВЕКОМ И вердикт `passed`.
- **Владельцу вехи ждут ТРИ решения, ни одно из которых не назначено планом:** изъятие из запертого D-01 либо перенос границы (окно 51), машинно читаемая разметка слоёв утверждения (окно 52), клиентский тайм-аут запроса (окно 34). Плюс восьмое измерение Найквиста (окно 47).
- **Для Фазы 11:** реестр `VALIDATION_REFUSAL_DIVERGENCES` называет семь входов, которые массовый перевод обязан взять вместе с границей; окно 49 называет цель третьего внеполосного узла, наблюдаемую только шагом 1.8 обхода.
- **Для Фазы 15 (GATE-09):** остаток пункта площадки приземления фокуса передан ССЫЛКОЙ на уже записанное назначение (окно 50), нового назначения не заведено.
- **Реестр окон: 45 открытых из 52.** С `workflow.windows_enforce` `/gsd-ship` остаётся заблокированным — и это верное состояние, а не помеха.

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-07*

---

## Self-Check

- Пути `key-files` существуют на диске: `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/WINDOWS.md`, `10-UAT.md`, `10-23-SUMMARY.md` — все ЕСТЬ.
- Коммиты трёх задач в истории: `04bd2c6`, `ce5a42b`, `631aa0c` — все три найдены `git log`.
- Все приёмочные критерии трёх задач перепрогнаны и приведены выше с выводами; два расхождения с формой критериев (`completed_plans` в контексте диффа; пятая прибавившаяся строка реестра) названы поимённо и объяснены.
- Плановая `<verification>`: (1) `31 passed`; (2) `2860 passed`, exit 0; (3) `7`; (4) `AGREE` и `5` (≥ 4, пятая атрибутирована); (5) `0`; (6) пусто плюс постро́чная атрибуция семнадцати путей `app/`/`tests/` — неатрибутируемых НОЛЬ; (7) человеческая половина НЕ снята и снятой не объявлена.
- Цитаты в сводке подставлены ПРОГРАММНО из самих файлов (летопись и строка состава — из `.planning/ROADMAP.md`, четыре текста окон — из машинного блока `.planning/WINDOWS.md`), а не перепечатаны: пересказ через круг завёл бы второе поколение формулировки там, где партия только что пометила первое.
- Собственный след плана равен объявленным `files_modified` и ничему сверх: `git diff --name-only a89b2c8..HEAD` даёт ровно `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/WINDOWS.md`, `10-UAT.md` (плюс настоящая сводка метаданным коммитом). Ни `app/`, ни `tests/`, ни `pyproject.toml`, ни `.planning/REQUIREMENTS.md`, ни `09-UAT.md`.

## Self-Check: PASSED
