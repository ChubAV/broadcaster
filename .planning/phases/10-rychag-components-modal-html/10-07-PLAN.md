---
phase: 10-rychag-components-modal-html
plan: 07
type: execute
wave: 2
depends_on: ["10-05"]
gap_closure: true
files_modified:
  - app/templates/components/modal.html
  - tests/test_templates/test_components.py
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ Это НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а НЕ замер. Калибровки у проекта не
  # существует (estimate-calibration: sample_count 0, factor 1, min_samples 3),
  # поэтому tokens и raw_tokens совпадают, а confidence: low ВЫВЕДЕН ИЗ ЧИСЛА
  # ЗАМЕРОВ (ноль), а не из самооценки планировщика.
  # Смарт-зона: 92000 при бюджете 100000 — в бюджете. (Проекция поднята с 88000
  # проходом ревизии: задача 2 прибавила пятое правило — стражу доступности
  # кнопки отказа — и помощник _modal_cancel_button_tag, BL-2.)
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Гейп «DEF-09-02 — «Отмена» панели закрывает панель, но летящий запрос не отменяет; назначенная фаза — 10» (`10-VERIFICATION.md`, `status: partial`) ЗАКРЫТ, а не перемаршрутизирован: панель не уходит с экрана, пока необратимый запрос летит, и человек больше не может считать отменённым действие, которое происходит"
    - "Закрыты ВСЕ ТРИ пути ухода панели по воле человека, а не один: клавиша выхода на окне, щелчок по оверлею и кнопка отказа. Число путей ОБЪЯВЛЕНО и утверждается правилом — четвёртый путь, заведённый без гейта, краснеет"
    - "Предмет, размноженный фазой с ОДНОГО места до 18, закрывается ОДНОЙ правкой того же рычага: `DEF-09-02` был находкой обхода Фазы 9 на единственном месте, и правка здесь расходится на все 18 мест подтверждения тем же механизмом, которым фаза раздала им признак отправки"
    - "Гейт НЕ ЛОВУШКА: как только обмен завершился — успехом, отказом сервера или несостоявшимся обменом, — признак отправки сбрасывается безусловно, и все три пути ухода снова работают. Это утверждается ПОЛОЖИТЕЛЬНЫМ КОНТРОЛЕМ на форме события отказа, а не выводится из выражения"
    - "Записанный контракт «кнопка отказа не блокируется ничем: отмена разрушительного действия не имеет права быть труднее его подтверждения» НЕ ОТМЕНЁН, а ПРОЧИТАН: кнопка не получает признака отключённости, не уходит из обхода по клавише табуляции и не меняет подписи; на время полёта запроса недействующей становится ветвь ухода, потому что уход, не отменяющий запроса, есть не более лёгкая отмена, а ЛОЖНАЯ. Прочтение УТВЕРЖДАЕТСЯ ПРАВИЛОМ, а не только записью шапки: test_the_cancel_button_stays_available_while_the_dismissal_is_gated достаёт тег ИМЕННО этой кнопки и доказывает асимметрию с кнопкой подтверждения (у отказа привязки отключённости нет, у подтверждения есть) — грепом по файлу это неразличимо"
    - "Отказ не молчалив, и это ИЗМЕРЕНО, а не обещано: на время полёта запроса виден узел индикатора, который макрос печатает БЕЗУСЛОВНО всем 18 местам (`hx-indicator`), — человек видит, что панель занята, а не что она сломалась"
    - "Ребро КОНКУРЕНТНОСТИ отката-фолбэка («If interrupted or run in parallel, what is guaranteed?», FORM-06) разрешено ЗДЕСЬ и не дублируется планом 10-06: прерывание летящего запроса волей человека невозможно по построению, а одновременность двух подтверждений уже связана серверной единицей записи — один commit одной строки под условием владения"
    - "Цена гейта НАЗВАНА, А НЕ ЗАМОЛЧАНА: клиентского тайм-аута у проекта нет (в блоке конфигурации ключа нет, умолчание рантайма равно нулю), поэтому обмен, который не завершается НИКАК, оставляет панель недействующей до того, как рантайм сам поднимет обработчик отказа транспорта. Запись заводится окном с названными кандидатами на закрытие; правки блока конфигурации Фазы 7 этот план НЕ делает"
    - "Окно 15 `.planning/WINDOWS.md` переведено в закрытое состояние тем же коммитом, который закрывает предмет, а не отдельным заходом: открытое окно с закрытым предметом есть ровно то расхождение записи и кода, за которое веха уже платила раундами"
    - "`DEF-09-02` НАЗВАН ПОИМЁННО в артефакте фазы: до этого плана строки `DEF-09-02` не содержал ни один артефакт Фазы 10 — ни план, ни сводка, ни CONTEXT, ни отчёт ревизии, — и именно это делало пункт осиротевшим, а не отложенным"
    - statement: "Наблюдение того, что человек ДЕЙСТВИТЕЛЬНО видит занятую панель, а не сломанную, на живом стенде глазом не снято этим планом: суита исполняет объект клиентского состояния в интерпретаторе со стаб-документом и ни рантайма разметки, ни таблицы стилей не поднимает. Пункт передаётся ручному обходу фазы отдельной строкой рядом с действующими четырьмя"
      verification: backstop
  artifacts:
    - "app/templates/components/modal.html — выражения трёх путей ухода панели, снабжённые гейтом по признаку отправки (строки 459, 462, 472)"
    - "app/templates/components/modal.html — абзац шапки «УХОД ПАНЕЛИ ПО ВОЛЕ ЧЕЛОВЕКА ВО ВРЕМЯ ЛЕТЯЩЕГО ЗАПРОСА» с поимённым `DEF-09-02` и прочтением записи о кнопке отказа"
    - "tests/test_templates/test_components.py — DISMISSAL_PATHS_DECLARED, _modal_dismissal_expressions, scenarioDismissal"
    - "tests/test_templates/test_components.py — имена правил: test_the_panel_refuses_dismissal_while_the_request_is_in_flight, test_the_panel_accepts_dismissal_once_the_request_has_ended, test_every_dismissal_path_of_the_panel_is_gated_on_the_in_flight_state, test_control_negative_an_ungated_dismissal_reddens_the_gate, test_the_cancel_button_stays_available_while_the_dismissal_is_gated"
    - "tests/test_templates/test_components.py — _modal_cancel_button_tag: тег кнопки отказа, доставаемый действующим разборщиком по маркеру (предмет правила доступности — ИМЕННО эта кнопка, а не файл целиком)"
    - ".planning/WINDOWS.md — окно 15 в закрытом состоянии + новое окно с ценой гейта (отсутствие клиентского тайм-аута)"
    - ".planning/REQUIREMENTS.md — запись закрытия DEF-09-02 рядом с действующей формулировкой пункта"
  key_links:
    - "гейт по признаку отправки → все 18 мест подтверждения: единственная правка, которой предмет DEF-09-02 закрывается сразу везде; ломается — человек снова считает отменённым действие, которое происходит, и на 18 местах, а не на одном"
    - "сброс признака отправки по завершении обмена → работоспособность трёх путей ухода: единственное, что не даёт гейту стать ловушкой; ломается — панель после отказа сервера остаётся недействующей, и D-12 («панель остаётся открытой на отказе, повторить можно, не открывая заново») превращается в свою противоположность"
    - "DISMISSAL_PATHS_DECLARED → разметка панели: единственное, чем четвёртый путь ухода не может появиться без гейта; ломается — гейт держится дисциплиной читателя, а не правилом"
    - "окно 15 → предмет DEF-09-02: запись и код обязаны закрыться одним коммитом; ломается — открытое окно переживает закрытый предмет и следующий круг чинит закрытое"
    - "правило доступности кнопки отказа → записанный контракт «отмена не имеет права быть труднее подтверждения»: единственное, чем ПРОЧТЕНИЕ контракта отличается от его тихой отмены; ломается — запись остаётся без стерегущего правила, а необеспеченная правилом запись в этом проекте уже один раз разошлась с рантаймом (гейп 1 этой же фазы)"
  prohibitions:
    - statement: "кнопка отказа НЕ получает признака отключённости и не выпадает из обхода по клавише табуляции: записанный контракт «отмена не имеет права быть труднее подтверждения» прочитывается, а не отменяется, и разница между «не действует, пока панель занята» и «недоступна» есть предмет этого запрета"
      status: flagged-unverified
      verification: none
    - statement: "клиентская отмена летящего запроса НЕ вводится вместо гейта: она была бы ложью другого рода — сервер к моменту обрыва мог уже выполнить необратимое действие, и человек снова считал бы отменённым то, что произошло"
      status: flagged-unverified
      verification: none
    - statement: "блок конфигурации рантайма разметки (`app/templates/includes/htmx_config.html`) не правится: введение клиентского тайм-аута есть решение уровня вехи, действующее на КАЖДЫЙ запрос приложения, а не побочная правка рычага панели"
      status: flagged-unverified
      verification: none
    - statement: "прежняя формулировка шапки о кнопке отказа не вычёркивается и не переписывается: она верно называет принцип, и правка обязана прочитать его, а не отменить (идиома D-30/D-32 `.planning/STATE.md`)"
      status: flagged-unverified
      verification: none
    - statement: "перемаршрутизация DEF-09-02 в следующую фазу этим планом НЕ выбирается: она была бы законным исходом только записанной с основанием и с обновлённым окном, а предмет закрывается одной правкой того же рычага, которым фаза уже расходится на 18 мест"
      status: flagged-unverified
      verification: none
    - statement: "предсуществующий красный `test_the_overview_error_number_matches_the_users_own_dashboard` (окно 27, `DEF-09-01`) не чинится и не втягивается; критерии приёмки от него не зависят"
      status: flagged-unverified
      verification: none
---

<objective>
`DEF-09-02` закрывается: панель подтверждения перестаёт уходить с экрана, пока необратимый запрос
летит. Три пути ухода по воле человека — клавиша выхода, оверлей и кнопка отказа — получают гейт
по признаку отправки, число путей объявляется и утверждается правилом, а записи вехи (окно 15 и
формулировка пункта в требованиях) закрываются ТЕМ ЖЕ коммитом, что и предмет.

Purpose: пункт назначен ЭТОЙ фазе поимённо — `.planning/REQUIREMENTS.md`, открытое окно 15
`.planning/WINDOWS.md`, источник `09-.../deferred-items.md` — с основанием «фаза, которой макрос
принадлежит». Ни один артефакт Фазы 10 строки `DEF-09-02` не содержит: он не закрыт, не отложен
записью и не перемаршрутизирован. При этом фаза РАСШИРИЛА его предмет с одного места до 18 —
признак отправки теперь стои́т на всех 18 панелях, а три пути ухода его не спрашивают. Человек,
нажавший «Отмена» на медленной сети, считает, что отменил удаление; удаление происходит.

Output: гейт на трёх путях ухода; ПЯТЬ правил, из которых одно — инвентарь числа путей, одно —
отрицательный контроль и одно — стража доступности кнопки отказа (без неё записанный контракт
«отмена не имеет права быть труднее подтверждения» остался бы без стерегущего правила, а угроза
T-10-41 — без реализации); прочтение записи о кнопке отказа в шапке компонента; закрытое окно 15;
запись закрытия в требованиях; новое окно с названной ценой гейта.

⚠️ ЭТОТ ПЛАН — ЗАКРЫТИЕ ГЕЙПА (`gap_closure: true`), волна 2. Он зависит от плана 10-05 по ДВУМ
основаниям: множества изменяемых файлов пересекаются (`modal.html`, `test_components.py`), и
положительный контроль этого плана исполняет форму события ОТКАЗА, заведённую планом 10-05.
Планы 10-01…10-04 исполнены и закоммичены; ни один из них не правится и не перенумеровывается.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов партии закрытия гейпов — в `10-05-PLAN.md`
§«Artifacts this phase produces». Этим планом создаются:

- `DISMISSAL_PATHS_DECLARED` — число путей ухода панели по воле человека,
  `tests/test_templates/test_components.py`
- `_modal_dismissal_expressions(html_source)` — разбор трёх выражений ухода из ОТРИСОВАННОЙ
  разметки панели, там же
- `scenarioDismissal` — сценарий гарнира, исполняющий выражение ухода при заданном значении
  признака отправки, и ключ `dismissal` в перечне сценариев, там же
- имена правил: `test_the_panel_refuses_dismissal_while_the_request_is_in_flight`,
  `test_the_panel_accepts_dismissal_once_the_request_has_ended`,
  `test_every_dismissal_path_of_the_panel_is_gated_on_the_in_flight_state`,
  `test_control_negative_an_ungated_dismissal_reddens_the_gate`,
  `test_the_cancel_button_stays_available_while_the_dismissal_is_gated`
- `_modal_cancel_button_tag(html_source)` — тег кнопки отказа, доставаемый ДЕЙСТВУЮЩИМ
  разборщиком тега кнопки по маркеру; предмет правила доступности — ИМЕННО эта кнопка, там же
- абзац шапки `app/templates/components/modal.html` «УХОД ПАНЕЛИ ПО ВОЛЕ ЧЕЛОВЕКА ВО ВРЕМЯ
  ЛЕТЯЩЕГО ЗАПРОСА» с поимённым `DEF-09-02`
- запись закрытия `DEF-09-02` в `.planning/REQUIREMENTS.md`
- окно 15 `.planning/WINDOWS.md` в закрытом состоянии и новое окно с ценой гейта
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: сквозной срез ОДНОГО пути ухода — клавиша выхода не уносит панель, пока запрос летит</name>

  <files>
tests/test_templates/test_components.py,
app/templates/components/modal.html
  </files>

  <reversibility rating="costly">
Гейт наследуется всеми 18 местами подтверждения одной правкой рычага и ПРОЧИТЫВАЕТ записанный
контракт о кнопке отказа; снятие гейта означало бы возврат к состоянию, где человек считает
отменённым происходящее действие, а его цена (отсутствие клиентского тайм-аута) требует
отдельной записи с назначенным владельцем.
  </reversibility>

  <read_first>
- `app/templates/components/modal.html:444-478` — макрос ЦЕЛИКОМ ПОСЛЕ плана 10-05. Три пути
  ухода панели по воле человека стоя́т на строках 459 (событие клавиатуры на окне у корневого
  узла), 462 (событие щелчка у узла оверлея) и 472 (событие щелчка у кнопки с ссылкой `cancel`).
  ⚠️ В ЭТОЙ ЗАДАЧЕ правится РОВНО первый; два остальных — задача 2.
- `app/templates/components/modal.html:466` — перехват отправки на форме панели: у панели УЖЕ
  ЕСТЬ один способ спросить «запрос летит», и гейт путей ухода обязан пользоваться тем же именем
  поля и той же формой условия. Второй способ спросить то же самое разошёлся бы с первым молча.
- `app/templates/components/modal.html:156-197` — блок «Защита от повторной отправки» целиком,
  включая предложение о том, что кнопка отказа не блокируется ничем. Читать ДО правки: задача 2
  это предложение ПРОЧИТЫВАЕТ, и заготовка строки, добавляемой здесь, обязана на него сослаться,
  а не опередить его.
- `10-VERIFICATION.md`, второй пункт `gaps` ЦЕЛИКОМ: `truth`, `reason`, единственная запись
  `artifacts` (`modal.html:459,462`) и `missing`. Это и есть формулировка предмета.
- `10-REVIEW.md` §WR-09 (строки 359-381) — независимый разбор с обоими выражениями выписанными и
  с названным последствием: запрос продолжается против компонента, чьё собственное состояние уже
  ложно, и на маршрутах без плашки на успех (D-03) человек не получает НИКАКОГО сигнала.
- `.planning/REQUIREMENTS.md` около строки 428 — запись `DEF-09-02` дословно, вместе с
  назначением фазы и основанием «фаза, которой макрос принадлежит».
- `tests/test_templates/test_components.py:1640-1770` — шапка гарнира жизненного цикла: `build`,
  `reset`, `locked`, `makeNode`, стаб-документ. Новый сценарий строится ТЕМ ЖЕ построителем.
- `tests/test_templates/test_components.py` — `EVENT_SHAPES`, `spyBranches`, `scenarioTransports`
  и ключ `transports`, заведённые планом 10-05: новый сценарий регистрируется РЯДОМ, тем же
  способом, и формы события у него общие с ними — своих не заводится.
- `tests/test_templates/test_components.py:2044-2082` — `_run_modal_lifecycle` и записанное
  основание расширения входов необязательными параметрами с умолчаниями; новый вход прибавляется
  ТОЙ ЖЕ формой, и неизменность вердиктов действующих правил утверждается прогоном ДО и ПОСЛЕ.
- `tests/test_templates/test_components.py:504-518` — `GUARD_ATTR_RE`, `_form_tag` и
  `_button_tag(out, marker)`: готовый разборщик тега кнопки по маркеру, которым достаётся кнопка
  отказа. Своего разборщика тегов не заводится.
- `tests/test_templates/test_components.py:432` — `_modal_block`: отрисовка панели, которую
  получает разборщик выражений ухода.
  </read_first>

  <precondition>
План 10-05 приземлён: `grep -cF 'detail.xhr' app/templates/components/modal.html` возвращает не
менее `1`, и `uv run pytest tests/test_templates/test_components.py -k "transport" -q -p no:randomly`
даёт `7 passed, 64 deselected`. Сегодняшнее состояние путей ухода:
`grep -cF 'x-on:keydown.escape.window="hide()"' app/templates/components/modal.html` возвращает `1`,
`grep -cF 'x-on:click="hide()"' app/templates/components/modal.html` возвращает `2`.
  </precondition>

  <behavior>
СРЕЗ ВЕРТИКАЛЬНЫЙ И ПРОИЗВОДСТВЕННЫЙ: разметка (выражение атрибута) → разбор отрисованной панели
→ гарнир (исполнение выражения с областью видимости объекта клиентского состояния) → наблюдаемое
состояние панели, — и делает это ровно для ОДНОГО пути: клавиши выхода на окне. Два остальных
пути и инвентарь числа — задача 2.

1. `test_the_panel_refuses_dismissal_while_the_request_is_in_flight` (в этой задаче — только путь
   клавиши выхода). Панель открыта, признак отправки истинен; исполняется выражение пути ухода.
   Вердикт: панель ОСТАЛАСЬ открытой, признак блокировки прокрутки НА ДОКУМЕНТЕ, фокус не
   двигался. Три утверждения, а не одно: панель, «закрытая» без снятия признака прокрутки, есть
   третье состояние, и различать его обязательно.

2. `test_control_negative_an_ungated_dismissal_reddens_the_gate` — ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ.
   Гарниру подаётся выражение ухода, ДОСЛОВНО РАВНОЕ сегодняшнему, подстановкой, а не чтением из
   шаблона (после правки его там не будет). Вердикт обязан быть: панель ЗАКРЫЛАСЬ при истинном
   признаке отправки. Правило, зелёное на этой подстановке, стережёт не то. Подстановка обязана
   доказать, что изменила что-то и ИМЕННО ТО: сличение подставленного выражения с прочитанным из
   шаблона обязано давать неравенство.

ПЕРЕХОД ЦВЕТА ОБЯЗАН БЫТЬ НАБЛЮДЁН И ЗАПИСАН SHA-АМИ: правила и разбор landing ПЕРВЫМ коммитом на
непрáвленом шаблоне (RED, код прогона ровно `1`), правка выражений — ВТОРЫМ (GREEN).
  </behavior>

  <action>
**1. Разбор выражений ухода (`_modal_dismissal_expressions`).**

Помощник принимает ОТРИСОВАННУЮ разметку панели и возвращает отображение «имя пути → выражение»
для трёх путей, названных ПОИМЁННО: путь клавиши выхода (атрибут события клавиатуры на окне у
корневого узла панели), путь оверлея (атрибут события щелчка у узла оверлея) и путь кнопки
отказа (атрибут события щелчка у кнопки, несущей ссылку `cancel`). Кнопка достаётся действующим
помощником разбора тега кнопки по маркеру — своего разборщика тегов не заводится.

Помощник утверждает, что нашёл РОВНО три выражения и что имена путей различны: разборщик,
молча вернувший два, сделал бы инвентарь задачи 2 вакуумно зелёным. Граница разборщика та же,
что у действующих разборщиков модуля, — ОДНА панель на поданную разметку, и это утверждается
числом найденных вхождений.

**2. Сценарий гарнира `scenarioDismissal`.**

Принимает выражение пути ухода и значение признака отправки. Собирает панель тем же построителем,
что и соседние сценарии, открывает её, выставляет признак отправки в заданное значение и
исполняет ВЫРАЖЕНИЕ телом функции с областью видимости объекта клиентского состояния — тем же
способом, каким рантайм клиентского состояния исполняет атрибуты, и тем же, каким это делает
сценарий транспортов плана 10-05. Вызов метода объекта напрямую проверял бы НЕ ТО: предмет здесь
ветвь, записанная в АТРИБУТЕ.

Вердикт несёт `open`, `locked`, `focused`, `attempted` и `repeat_matches`; сценарий, как и
соседние, прогоняется ДВАЖДЫ, и различающийся между прогонами исход есть отказ.

Сценарий регистрируется ключом `dismissal` в перечне сценариев. Входы функции запуска сценариев
расширяются НЕОБЯЗАТЕЛЬНЫМИ параметрами с умолчаниями, оставляющими действующие сценарии
байт-в-байт прежними, — той же формой, которой планы 10-02 и 10-05 прибавляли свои входы, и с тем
же обязательством: неизменность вердиктов действующих правил утверждается ПРОГОНОМ ДО И ПОСЛЕ.

**3. Правило и контроль пути клавиши выхода.** Пишутся по составу `<behavior>`. Отказ каждого
приводит вердикт целиком и называет, ЧТО именно не сошлось.

**4. Правка выражения пути клавиши выхода (`app/templates/components/modal.html`, строка 459).**

Выражение получает гейт по признаку отправки — тем же именем поля и в той же форме условия,
какой уже пользуется перехват отправки на форме панели (строка 466): у панели уже есть ОДИН
способ спросить «запрос летит», и второй способ спросить то же самое разошёлся бы с первым молча.

⚠️ **КНОПКА ОТКАЗА В ЭТОЙ ЗАДАЧЕ НЕ ТРОГАЕТСЯ** — она предмет задачи 2, и разведение сделано
затем, чтобы срез оставался ОДНИМ путём. ⚠️ **ПРИЗНАКА ОТКЛЮЧЁННОСТИ НИ ОДНА КНОПКА НЕ
ПОЛУЧАЕТ НИ В ОДНОЙ ЗАДАЧЕ ЭТОГО ПЛАНА**: предмет правки — ветвь ухода, а не доступность
элемента.

**5. Абзац шапки — ЗАГОТОВКА, полностью пишется задачей 2.** В этой задаче в шапку добавляется
одна строка рядом с абзацем о защите от повторной отправки: путь клавиши выхода спрашивает
признак отправки, предмет — `DEF-09-02`, и полная запись стои́т ниже. Разрывать шапку на два
несогласованных места запрещено — поэтому здесь именно строка со ссылкой, а не второй абзац.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py -k "dismissal" -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- **ПЕРЕХОД ЦВЕТА НАБЛЮДЁН, А НЕ ОБЕЩАН.** В сводке записаны два SHA: `RED_SHA` — коммит с
  правилами и разбором и БЕЗ правки шаблона, с выводом
  `uv run pytest tests/test_templates/test_components.py -k "dismissal" -q -p no:randomly` и кодом
  прогона РОВНО `1`; `GREEN_SHA` — следующий коммит с правкой выражения и зелёным прогоном того
  же фильтра.
- Отрицательный контроль умеет краснеть НА СЕГОДНЯШНЕМ ВЫРАЖЕНИИ: в сводке приведён его вердикт
  на подставленном литерале — `open: false` при истинном признаке отправки. Контроль, дающий
  `open: true` на этой подстановке, к слиянию не принимается.
- `grep -cF 'x-on:keydown.escape.window="hide()"' app/templates/components/modal.html` возвращает
  `0` (на `d6fc762` возвращал `1` — проверено).
- Разборщик находит РОВНО три пути: в сводке приведены три имени пути и три выражения дословно.
- Действующие правила приземления фокуса, блокировки прокрутки и транспортов не сдвинуты
  расширением гарнира:
  `uv run pytest tests/test_templates/test_components.py -k "scroll_lock or teardown or landing or lands_the_focus or transport" -q -p no:randomly`
  — зелено, и число прошедших равно тому же числу на состоянии после плана 10-05 (снять ДО правки
  и записать в сводке оба вывода).
- Числа критерия 3 не двинулись:
  `uv run pytest tests/test_templates/test_components.py -k "criterion_three_holds_by_the_numbers" -q -p no:randomly`
  → `1 passed`.
- `uv run pytest tests/test_templates/ -q -p no:randomly` — зелено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Путь клавиши выхода спрашивает признак отправки; правило исполняет ВЫРАЖЕНИЕ, а не метод, и
различает три состояния панели; отрицательный контроль доказан КРАСНЫМ на сегодняшнем выражении;
разборщик находит ровно три пути ухода и утверждает это числом. Переход цвета записан двумя SHA.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: два остальных пути ухода, инвентарь числа путей, положительный контроль и прочтение записи о кнопке отказа</name>

  <files>
tests/test_templates/test_components.py,
app/templates/components/modal.html
  </files>

  <read_first>
- `app/templates/components/modal.html:444-478` — макрос целиком ПОСЛЕ задачи 1: корневой узел с
  объектом клиентского состояния и атрибутами событий (`:456-461`), узел оверлея (`:462`), форма
  панели с перехватом отправки и атрибутом события завершения запроса (`:465-467`), кнопки
  (`:472-473`). Читать целиком: гейт ставится на ДВА оставшихся выражения, и оба надо видеть
  вместе с тем, что уже гейтировано.
- `app/templates/components/modal.html:156-197` — блок «Защита от повторной отправки», включая
  абзац о том, ЧТО именно останавливает вторую отправку на каждом из двух путей (WARN-2), абзац
  о единственном владельце блокировки кнопки и — ГЛАВНОЕ ДЛЯ ЭТОЙ ЗАДАЧИ — предложение «Кнопка
  ОТКАЗА не блокируется ничем: отмена разрушительного действия не имеет права быть труднее его
  подтверждения». Эта запись НЕ отменяется, а ПРОЧИТЫВАЕТСЯ; читать её дословно ДО правки.
- `app/templates/components/modal.html:100-115` и абзац, добавленный планом 10-05, — что уже
  записано про закрытие панели по завершении обмена; новая запись ставится РЯДОМ и на них
  ссылается, а не пересказывает их.
- `.planning/STATE.md` §Decisions, записи D-30 и D-32 — образец обращения с формулировкой,
  которую правка ПРОЧИТЫВАЕТ, а не отменяет.
- `10-REVIEW.md` §WR-09 (строки 359-381) — независимый разбор предмета с обоими выражениями
  выписанными и с названными последствиями: запрос продолжается против компонента, у которого
  собственное состояние уже ложно, и на маршрутах, не выдающих плашки на успех (D-03), человек не
  получает НИКАКОГО сигнала о том, что необратимое действие произошло.
- `10-VERIFICATION.md`, второй пункт `gaps` целиком — формулировка `truth`, `reason`, `artifacts`
  и `missing`. Запись закрытия обязана отвечать именно на них.
- `app/templates/includes/htmx_config.html:118-132` — блок конфигурации целиком. Убедиться
  ЛИЧНО, что ключа тайм-аута в нём НЕТ: это и есть названная цена гейта, и она записывается
  задачей 3, а блок НЕ правится.
- `tests/test_templates/test_components.py` — сценарий и разборщик, заведённые задачей 1, и
  формы события, заведённые планом 10-05: положительный контроль этой задачи исполняет форму
  события ОТКАЗА, а не выдумывает свою.
  </read_first>

  <precondition>
Задача 1 приземлена:
`grep -cF 'x-on:keydown.escape.window="hide()"' app/templates/components/modal.html` возвращает `0`,
и `uv run pytest tests/test_templates/test_components.py -k "dismissal" -q -p no:randomly` зелено.
  </precondition>

  <behavior>
1. `test_the_panel_refuses_dismissal_while_the_request_is_in_flight` РАСШИРЯЕТСЯ на все три пути:
   правило обходит отображение путей, полученное разборщиком, и для КАЖДОГО утверждает тот же
   вердикт из трёх полей. Отказ обязан называть ПУТЬ ПОИМЁННО — общий отказ «панель ушла» не
   сказал бы, какой из трёх.

2. `test_the_panel_accepts_dismissal_once_the_request_has_ended` — ПОЛОЖИТЕЛЬНЫЙ КОНТРОЛЬ И
   ЗАЩИТА ОТ ЛОВУШКИ, а не гигиена. Две половины:
   - панель, у которой признак отправки ложен, уходит по КАЖДОМУ из трёх путей: панель закрыта,
     признак блокировки прокрутки снят, фокус приземлился. Без этой половины несущее правило
     зелено и у панели, которая не закрывается НИКОГДА;
   - панель, пережившая ОТКАЗ СЕРВЕРА, снова уходит по каждому из трёх путей. Исполняется
     форма события отказа, заведённая планом 10-05: выражение завершения запроса сбрасывает
     признак отправки, панель остаётся открытой (D-12), и после этого пути ухода обязаны
     работать. Это и есть утверждение «гейт не ловушка», и оно НЕСУЩЕЕ: панель, которая после
     отказа не закрывается ничем, превращает обещание D-12 («повторить можно, не открывая её
     заново») в свою противоположность.

3. `test_every_dismissal_path_of_the_panel_is_gated_on_the_in_flight_state` — ИНВЕНТАРЬ. Число
   найденных разборщиком путей ухода равно объявленной константе, и КАЖДОЕ из выражений
   спрашивает признак отправки. Правило существует затем, чтобы четвёртый путь ухода, заведённый
   будущей фазой, не мог появиться без гейта молча. Отказ называет, СКОЛЬКО путей найдено,
   сколько объявлено и какие из них гейта не несут.

4. `test_control_negative_an_ungated_dismissal_reddens_the_gate` расширяется тем же обходом:
   подставленное негейтированное выражение обязано краснить правило на КАЖДОМ из трёх путей.

5. `test_the_cancel_button_stays_available_while_the_dismissal_is_gated` — СТРАЖА ЗАПИСАННОГО
   КОНТРАКТА О КНОПКЕ ОТКАЗА и ЕДИНСТВЕННАЯ реализация угрозы T-10-41. До этого прохода такого
   правила в плане не было вовсе: критерий приёмки требовал утверждения «правилом (не грепом)»,
   а объявленные имена правил его не содержали — задача была неисполнима, и угроза оставалась
   без митигации. Правило достаёт ДВА тега действующим разборщиком тега кнопки по маркеру —
   кнопку отказа (маркер — ссылка `cancel`) и кнопку подтверждения (маркер — тип отправки), — и
   утверждает ЧЕТЫРЕ вещи о ПЕРВОЙ и одну о ВТОРОЙ:
   - в теге кнопки отказа НЕТ ни одной привязки отключённости и занятости: ни привязки
     клиентского состояния к признаку отключённости, ни статического атрибута отключённости, ни
     привязки признака занятости, ни адресующего её селектора отключения рантайма;
   - тег кнопки отказа СОХРАНЯЕТ ссылку `cancel` — на неё ставится начальный фокус при открытии;
   - тег кнопки отказа НЕ несёт атрибута порядка обхода вовсе: обход по клавише табуляции у
     панели собирается перечислением фокусируемых узлов, и вынос кнопки из этого перечня был бы
     ровно тем, что контракт запрещает;
   - подпись кнопки отказа НЕПУСТА и равна тому, что макрос печатает параметром подписи: «стала
     недействующей ветвь ухода» не имеет права превратиться в «сменилась подпись».
   - **АСИММЕТРИЯ — И ЭТО ЗУБЫ ПРАВИЛА, а не украшение:** в теге кнопки ПОДТВЕРЖДЕНИЯ привязка
     отключённости ЕСТЬ, и правило это утверждает. Пара утверждений «у отказа нет / у
     подтверждения есть» — единственное, чего греп по файлу дать не может: в файле привязка
     отключённости присутствует ЗАКОННО (у кнопки подтверждения, в селекторе отключения на форме
     и в перечислителе фокусируемых узлов внутри объекта клиентского состояния), и счёт вхождений
     их не различает. Правило, зелёное при разборщике, вернувшем ОДИН И ТОТ ЖЕ тег на оба
     маркера, этой асимметрии дать не может — поэтому она же служит и антивакуумом: два
     сличаемых тега обязаны быть РАЗНЫМИ, и это утверждается до остальных проверок.
  </behavior>

  <action>
**1. Гейт на два оставшихся выражения (`app/templates/components/modal.html`, строки 462 и 472).**

Выражение узла оверлея и выражение кнопки отказа получают гейт по признаку отправки — ТОЙ ЖЕ
формой условия и тем же именем поля, что и путь клавиши выхода задачи 1. Три разные формы одного
условия разошлись бы молча; форма выбирается ОДНА и повторяется дословно.

⚠️ **КНОПКА ОТКАЗА НЕ ПОЛУЧАЕТ ПРИЗНАКА ОТКЛЮЧЁННОСТИ**, не теряет ссылки `cancel` (на неё
ставится начальный фокус при открытии), не выпадает из обхода по клавише табуляции и не меняет
подписи. Меняется РОВНО ветвь ухода. Различие между «не действует, пока панель занята» и
«недоступна» есть предмет прочтения записанного контракта, и стирать его нельзя.

**2. Пять правил** пишутся по составу `<behavior>`. Несущее и отрицательный контроль задачи 1
расширяются обходом по трём путям; правило инвентаря, положительный контроль и стража
доступности кнопки отказа заводятся здесь.

Стража доступности пользуется помощником `_modal_cancel_button_tag`, который есть ТОНКАЯ обёртка
над действующим разборщиком тега кнопки по маркеру, — своего разборщика тегов не заводится, и
второй копии логики извлечения тега не появляется. Обёртка нужна затем, чтобы предмет правила
был назван ИМЕНЕМ, а не выражением с маркером внутри тела правила: угроза T-10-41 говорит про
ИМЕННО ЭТУ кнопку, и в коде это обязано быть видно так же ясно, как в записи.

Константа `DISMISSAL_PATHS_DECLARED` объявляется со значением, ПОСТАВЛЕННЫМ ПРОГОНОМ
покрасневшего правила, а не сложением в уме: правило обязано сначала назвать в отказе «найдено N,
объявлено M» и перечислить пути поимённо. Над константой — абзац, называющий: что она считает
(пути ухода панели ПО ВОЛЕ ЧЕЛОВЕКА, а не пути ухода узла из документа — это ДРУГОЕ множество,
которого два и которое считает бухгалтерия приземления фокуса); и почему приводить эти два числа
к согласию нельзя.

**3. Абзац шапки «УХОД ПАНЕЛИ ПО ВОЛЕ ЧЕЛОВЕКА ВО ВРЕМЯ ЛЕТЯЩЕГО ЗАПРОСА».**

Ставится рядом с блоком о защите от повторной отправки. Обязан назвать ШЕСТЬ вещей и ни одной
меньше:

- предмет ПОИМЁННО: `DEF-09-02`, находка ручного обхода Фазы 9, назначенная Фазе 10 с основанием
  «фаза, которой макрос принадлежит», и то, что фаза РАСШИРИЛА его предмет с одного места до 18;
- что именно происходило: уход панели не отменял летящего запроса, запрос доходил до сервера, и
  на маршрутах, не выдающих плашки на успех, человек не получал НИКАКОГО сигнала о том, что
  необратимое действие произошло;
- три пути ухода поимённо и то, что гейт стои́т на всех трёх, а не на одном;
- **ПРОЧТЕНИЕ записи о кнопке отказа**: прежняя формулировка НЕ отменяется — кнопка по-прежнему
  не получает признака отключённости, не уходит из обхода по клавише табуляции и сохраняет
  начальный фокус; недействующей на время полёта становится ВЕТВЬ УХОДА, потому что уход, не
  отменяющий запроса, есть не более лёгкая отмена, а ЛОЖНАЯ. Прежний текст помечается
  ПРОЧИТАННЫМ, а не вычеркнутым;
- что отказ НЕ МОЛЧАЛИВ: на время полёта виден узел индикатора, который макрос печатает
  безусловно всем 18 местам, — человек видит занятую панель, а не сломанную;
- ЦЕНУ, названную прямо: клиентского тайм-аута у проекта нет, поэтому обмен, не завершающийся
  никак, оставляет панель недействующей до того, как рантайм сам поднимет обработчик отказа
  транспорта; запись цены заведена окном задачей 3, и номер окна называется здесь же.

Отдельной строкой — почему клиентская отмена летящего запроса ОТВЕРГНУТА как замена гейту: она
была бы ложью другого рода, потому что сервер к моменту обрыва мог уже выполнить необратимое
действие, и человек снова считал бы отменённым то, что произошло.

Отдельной строкой — имена правил, которые всё это стерегут: запись без стерегущего правила в
этом проекте уже один раз разошлась с рантаймом (гейп 1 этой же фазы).
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py -k "dismissal" -q -p no:randomly</automated>
    <human-check>На живом стенде: подтвердить удаление на медленной сети и, не дожидаясь ответа, нажать клавишу выхода, щёлкнуть по оверлею и нажать «Отмена». Ожидается: панель остаётся на месте, узел индикатора виден, кнопка «Отмена» остаётся фокусируемой и не выглядит отключённой. Затем вызвать отказ сервера (500) и повторить те же три действия — панель обязана закрыться каждым из них. Машинного эквивалента у этой проверки нет: суита исполняет объект клиентского состояния в интерпретаторе со стаб-документом и ни рантайма разметки, ни таблицы стилей не поднимает.</human-check>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_templates/test_components.py -k "dismissal" -q -p no:randomly` →
  `5 passed, 71 deselected`. Число `deselected` ВЫВЕДЕНО ИЗ ЗАМЕРА, а не сложено в уме: база
  `--collect-only -q -p no:randomly` на `d6fc762` даёт `64 tests collected`, план 10-05 прибавляет
  7 (итого 71), этот план — 5, итого **76**; из них 5 несут в имени `dismissal` — 76 − 5 = 71.
  Токена `dismissal` в базовых 64 именах НЕТ ни одного (измерено: `--collect-only -k "dismissal"`
  на `d6fc762` даёт `no tests collected (64 deselected)`), поэтому фильтр не подхватывает чужого,
  и пятое правило намеренно названо так, чтобы попасть под тот же фильтр — правило партии, не
  исполняемое `<verify>` партии, стерегло бы вхолостую. ⚠️ Расхождение фактического числа с этим
  есть НАХОДКА, а не повод подогнать фильтр: записать фактическое в сводке и назвать, что его
  сдвинуло (прецедент — запись 24 `.planning/WINDOWS.md`).
- Сегодняшних негейтированных выражений не осталось:
  `grep -cF 'x-on:click="hide()"' app/templates/components/modal.html` возвращает `0` (на
  `d6fc762` — `2`, ИЗМЕРЕНО в проходе ревизии командой
  `git show d6fc762:app/templates/components/modal.html | grep -cF 'x-on:click="hide()"'`);
  `grep -cF 'x-on:keydown.escape.window="hide()"'` возвращает `0` (на `d6fc762` — `1`, ИЗМЕРЕНО
  той же командой).
- Кнопка отказа не отключена и не потеряла ссылку:
  `grep -cF 'x-ref="cancel"' app/templates/components/modal.html` возвращает `1` (на `d6fc762` —
  `1`, ИЗМЕРЕНО в проходе ревизии), и в теге кнопки отказа НЕТ привязки признака отключённости —
  проверяется ПОИМЕНОВАННЫМ ПРАВИЛОМ `test_the_cancel_button_stays_available_while_the_dismissal_is_gated`
  (не грепом), потому что привязка отключённости у кнопки ПОДТВЕРЖДЕНИЯ присутствует законно и
  счёт вхождений их не различит.
  `uv run pytest tests/test_templates/test_components.py -k "cancel_button_stays_available" -q -p no:randomly`
  → `1 passed, 75 deselected` (76 − 1).
- Стража доступности доказала АСИММЕТРИЮ, а не только отсутствие: в сводке приведены ОБА тега
  дословно — кнопки отказа и кнопки подтверждения, — и названо, что первый привязки отключённости
  не несёт, а второй несёт. Правило, утверждающее только отсутствие у одной кнопки, к слиянию не
  принимается: оно зелено и у разборщика, вернувшего пустой тег.
- `DISMISSAL_PATHS_DECLARED` поставлено ПРОГОНОМ покрасневшего правила: в сводке приведён вывод
  отказа с «найдено N, объявлено M» и перечнем путей поимённо.
- Положительный контроль доказывает ОБЕ половины: в сводке приведён вердикт «панель уходит по
  каждому из трёх путей при ложном признаке отправки» И вердикт «панель уходит по каждому из трёх
  путей ПОСЛЕ отказа сервера». Правило без второй половины к слиянию не принимается: без неё гейт
  не отличим от ловушки.
- Отрицательный контроль краснит на КАЖДОМ из трёх путей: в сводке приведены три вердикта.
- `DEF-09-02` назван в коде:
  `grep -cF 'DEF-09-02' app/templates/components/modal.html` возвращает не менее `1` (на
  `d6fc762` — `0`, ИЗМЕРЕНО в проходе ревизии: строки `DEF-09-02` не содержал ни один артефакт
  фазы).
- Прежняя формулировка о кнопке отказа НЕ УДАЛЕНА:
  `grep -cF 'Кнопка ОТКАЗА не блокируется ничем' app/templates/components/modal.html` возвращает
  `1` — то же значение, что на `d6fc762` (ИЗМЕРЕНО в проходе ревизии: даёт `1`).
- Блок конфигурации не тронут: `git diff --stat d6fc762 -- app/templates/includes/htmx_config.html`
  пуст.
- Числа критерия 3 не двинулись:
  `uv run pytest tests/test_templates/test_components.py -k "criterion_three_holds_by_the_numbers" -q -p no:randomly`
  → `1 passed, 75 deselected` (76 − 1; фильтр выбирает РОВНО одно правило — измерено
  `--collect-only` на `d6fc762`).
- Долговых маркеров не прибавилось:
  `grep -nE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER" app/templates/components/modal.html tests/test_templates/test_components.py`
  — ноль вхождений.
- `uv run pytest tests/ -q -p no:randomly` — зелено за вычетом известного предсуществующего
  красного `test_the_overview_error_number_matches_the_users_own_dashboard` (окно 27,
  `DEF-09-01`). Критерии приёмки от него НЕ зависят.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Все три пути ухода панели по воле человека спрашивают признак отправки; их число объявлено и
утверждается правилом с отказом, называющим путь поимённо; положительный контроль доказывает, что
гейт не ловушка, — в том числе ПОСЛЕ отказа сервера; отрицательный контроль краснит на каждом из
трёх; доступность кнопки отказа стережёт ПОИМЕНОВАННОЕ правило, доказавшее асимметрию с кнопкой
подтверждения (у одной привязки отключённости нет, у другой есть), — угроза T-10-41 получила
реализацию, а не обещание; запись шапки называет `DEF-09-02` поимённо, прочитывает контракт о
кнопке отказа, не вычёркивая его, называет отказ немолчаливым и называет цену гейта с номером
окна.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 3: записи вехи — окно 15 закрывается, цена гейта заводится окном, `DEF-09-02` получает запись закрытия в требованиях</name>

  <files>
.planning/WINDOWS.md,
.planning/REQUIREMENTS.md
  </files>

  <read_first>
- `.planning/WINDOWS.md` — окно **15** целиком (строка таблицы и соответствующая запись в блоке
  JSON): `kind: todo`, `phase: 09`, файл-источник — `deferred-items.md` Фазы 9, описание
  «DEF-09-02 — «Отмена» панели подтверждения закрывает панель, но летящий запрос удаления не
  отменяет; назначенная Фаза 10», `status: open`. ⚠️ У файла ДВЕ ПОЛОВИНЫ — таблица и блок JSON,
  — и они обязаны говорить одно: правка одной половины оставила бы вторую противоречащей.
  Прочитать образец УЖЕ ЗАКРЫТЫХ окон (записи 12 и 13) и снять с них форму закрытия дословно,
  включая то, какие поля при этом заполняются.
- `.planning/REQUIREMENTS.md` — запись `DEF-09-02` (около строки 428, раздел о четырёх пунктах,
  «ЧТО ЗАКРЫТИЕМ НЕ СТАЛО») целиком, вместе с тремя соседними (`DEF-09-01`, `DEF-09-03`,
  `DEF-09-04`) и с итоговой строкой «все четыре МАРШРУТИЗИРОВАНЫ». Запись закрытия ставится
  РЯДОМ и НЕ переписывает прежнюю: прежняя верно описывает, чем пункт был и почему четвёртым
  кругом Фазы 9 не закрывался.
- `.planning/REQUIREMENTS.md:31` — текст FORM-06; `:136` — строка прослеживаемости
  `FORM-06 | Phase 10 | Pending`. НИ ОДНА из двух этим планом НЕ трогается: число мест закрыто
  планом 10-04, а статус двигает закрытие фазы, а не эта правка.
- `10-05-SUMMARY.md` и сводки задач 1-2 этого плана — имена правил и SHA переходов цвета:
  запись закрытия обязана называть, ЧЕМ предмет закрыт, а не только то, что он закрыт.
  </read_first>

  <precondition>
Задачи 1 и 2 приземлены:
`grep -cF 'x-on:click="hide()"' app/templates/components/modal.html` возвращает `0`, и
`uv run pytest tests/test_templates/test_components.py -k "dismissal" -q -p no:randomly` даёт
`5 passed, 71 deselected`.
  </precondition>

  <behavior>
Машинная половина этой задачи — на согласованности записей, а не на поведении, и она снимается
КОМАНДАМИ, а не чтением: состояние окна 15 в ОБЕИХ половинах файла окон (снятое по НОМЕРУ окна, а
не по строке предмета — счёт вхождений `DEF-09-02` в этом файле уже равен `2` и зубов не имеет) и
присутствие литерала начала записи закрытия в требованиях (`DEF-09-02 — ЗАКРЫТ`, которого на
`d6fc762` нет — измерено). Счёт вхождений `DEF-09-02` в требованиях сам по себе критерием НЕ
является: он равен `3` до правки.

Содержательная половина — что формулировки называют предмет ВЕРНО — закрывается ЧТЕНИЕМ при
приёмке фазы, по прецеденту разведения половин, записанному решением D-33 Фазы 5, и объявляется
этим честно, а не выдаётся за машинный результат.
  </behavior>

  <action>
**1. Окно 15 переводится в закрытое состояние** — ОБЕИМИ половинами файла сразу, по форме, снятой
с уже закрытых записей 12 и 13. Основание закрытия пишется одной строкой и называет: чем предмет
закрыт (гейт по признаку отправки на трёх путях ухода панели), где это живёт (рычаг
`components/modal.html`, раздаётся всем 18 местам подтверждения) и чем утверждается (имена ПЯТИ
правил, включая инвентарь числа путей, оба контроля и стражу доступности кнопки отказа).

⚠️ Обе половины файла и счётчики его шапки (`open_count`, `fixed_count`) двигаются ОДНОЙ
командой ведения журнала — `gsd-tools windows fixed 15`, — а не ручной правкой строки таблицы:
ручная правка одной половины оставила бы вторую или счётчики противоречащими, и это ровно тот
класс расхождения, который критерий приёмки ловит четырьмя командами. Основание закрытия
дописывается после того, как команда отработала.

⚠️ Окно закрывается ТЕМ ЖЕ коммитом, который закрывает предмет, — то есть после задач 1 и 2 и
вместе с записью требований. Открытое окно с закрытым предметом есть ровно то расхождение записи
и кода, за которое веха уже платила раундами.

**2. Заводится НОВОЕ окно с ценой гейта.** Вид — `todo`, фаза — `10`, файл —
`app/templates/includes/htmx_config.html`, состояние — открытое. Описание обязано назвать:
что клиентского тайм-аута у проекта нет (ключа в блоке конфигурации нет, умолчание рантайма равно
нулю); следствие — обмен, не завершающийся никак, оставляет панель недействующей, пока рантайм
сам не поднимет обработчик отказа транспорта; что на реальном обрыве сети обработчик поднимается
сразу, а неограниченным остаётся случай сервера, принявшего соединение и не ответившего; и что
введение тайм-аута действует на КАЖДЫЙ запрос приложения, то есть есть решение уровня вехи, а не
побочная правка рычага панели. КАНДИДАТЫ НА ЗАКРЫТИЕ НАЗЫВАЮТСЯ, А ФАЗА НЕ НАЗНАЧАЕТСЯ:
назначение принадлежит владельцу, и сочинять его здесь запрещено.

**3. Запись закрытия `DEF-09-02` в `.planning/REQUIREMENTS.md`.** Ставится РЯДОМ с действующей
формулировкой пункта, которая НЕ переписывается. Запись обязана НАЧИНАТЬСЯ литералом
`DEF-09-02 — ЗАКРЫТ` — символ в символ, с длинным тире, — и это не стиль: строка `DEF-09-02` в
файле уже встречается ТРИЖДЫ (строки 258, 428 и 429 на `d6fc762`, измерено), поэтому счёт
вхождений закрытия не доказывает, а этот литерал — единственное, чего в файле нет и что нельзя
получить иначе, чем НАПИСАВ запись. Он же и есть то, что читает критерий приёмки.

Дальше запись обязана назвать: чем закрыт (гейт на трёх путях ухода), чем утверждается (имена
ПЯТИ правил и команда воспроизведения с ожидаемой строкой `N passed, M deselected`), переход
цвета (`RED_SHA` → `GREEN_SHA` задачи 1) и то, что предмет был РАСШИРЕН фазой с одного места до
18 и закрыт сразу на всех 18 одной правкой рычага. Форма записи берётся у соседних закрытых
пунктов того же файла — своей формы не изобретается.

Итоговая строка «все четыре МАРШРУТИЗИРОВАНЫ, ни один не поглощён и ни один не забыт»
дополняется, а не переписывается: `DEF-09-02` из маршрутизированных переходит в ЗАКРЫТЫЕ, и это
называется прямо.

**4. Что НЕ трогается ни на символ:** текст FORM-06 и строка прослеживаемости
`FORM-06 | Phase 10`; записи `DEF-09-01`, `DEF-09-03` и `DEF-09-04` — их предметы живы;
`.planning/ROADMAP.md` вообще; каталог разведки; окна 21-27 — их предметы к `DEF-09-02` не
относятся.
  </action>

  <verify>
    <automated>w=.planning/WINDOWS.md; r=.planning/REQUIREMENTS.md; f=0; g() { printf '%s = %s (ожидалось %s)\n' "$1" "$3" "$2"; [ "$3" = "$2" ] || f=1; }; g 'таблица окно 15 open' 0 "$(grep -cE '^\| 15 \|.*\| open \|' "$w")"; g 'таблица окно 15 fixed' 1 "$(grep -cE '^\| 15 \|.*\| fixed \|' "$w")"; g 'JSON окно 15 open' 0 "$(grep -A9 '"id": 15,' "$w" | grep -c '"status": "open"')"; g 'JSON окно 15 fixed' 1 "$(grep -A9 '"id": 15,' "$w" | grep -c '"status": "fixed"')"; g 'DEF-09-02 закрытие' 1 "$(grep -cF 'DEF-09-02 — ЗАКРЫТ' "$r")"; uv run pytest tests/test_planning/ -q -p no:randomly -k "not machine_readable_progress_is_derived_from_the_roadmap" || f=1; [ "$f" = 0 ]</automated>
    <human-check>Приёмка документной половины: прочитать запись закрытия `DEF-09-02` в требованиях и обе половины окна 15 и убедиться, что они называют ОДИН И ТОТ ЖЕ предмет теми же словами, что прежние формулировки не переписаны, и что новое окно цены гейта называет кандидатов на закрытие, НЕ назначая фазу. Машинного эквивалента у этой проверки нет и не заводится — прецедент разведения половин записан решением D-33 Фазы 5.</human-check>
  </verify>

  <acceptance_criteria>
- Окно 15 закрыто ОБЕИМИ половинами файла, и это проверяется КОМАНДАМИ, а не чтением. ⚠️ Счёт
  вхождений `DEF-09-02` в файле окон зубов НЕ ИМЕЕТ и потому критерием не является: на `d6fc762`
  `grep -c 'DEF-09-02' .planning/WINDOWS.md` уже даёт `2` (ИЗМЕРЕНО в проходе ревизии — строка
  таблицы и запись JSON), то есть критерий на «не менее двух» был бы зелен до единого символа
  правки. Критерий — СОСТОЯНИЕ окна 15, снятое по номеру, а не по строке предмета:
  - таблица: `grep -cE '^\| 15 \|.*\| open \|' .planning/WINDOWS.md` → `0`
    (на `d6fc762` — `1`, ИЗМЕРЕНО) и `grep -cE '^\| 15 \|.*\| fixed \|' .planning/WINDOWS.md`
    → `1` (на `d6fc762` — `0`, ИЗМЕРЕНО);
  - блок JSON: `grep -A9 '"id": 15,' .planning/WINDOWS.md | grep -c '"status": "open"'` → `0`
    (на `d6fc762` — `1`, ИЗМЕРЕНО) и
    `grep -A9 '"id": 15,' .planning/WINDOWS.md | grep -c '"status": "fixed"'` → `1`
    (на `d6fc762` — `0`, ИЗМЕРЕНО).
  Обе половины и счётчики шапки файла двигаются ОДНОЙ командой ведения журнала
  (`gsd-tools windows fixed 15`) — ручная правка одной половины оставила бы вторую или счётчики
  противоречащими. Вывод всех четырёх команд приводится в сводке.
- Форма закрытия снята с уже закрытых окон, а не изобретена: в сводке приведены строки окон 12 и
  13 и новая строка окна 15 рядом, чтобы совпадение формы было видно.
- Новое окно цены гейта заведено и ОТКРЫТО; в сводке приведено его описание дословно, и в нём
  присутствуют все четыре названные вещи. Фаза-владелец в нём НЕ назначена, и это названо прямо.
- `DEF-09-02` получил запись закрытия в требованиях. ⚠️ **ПРЕЖНИЙ КРИТЕРИЙ БЫЛ ВАКУУМНО ЗЕЛЁН, И
  ЕГО АННОТАЦИЯ БЫЛА ЛОЖНА.** Он требовал `grep -c 'DEF-09-02' .planning/REQUIREMENTS.md` «не
  менее `2`» со ссылкой «на `d6fc762` возвращал `1` — проверено». Живой замер:
  `git show d6fc762:.planning/REQUIREMENTS.md | grep -c 'DEF-09-02'` даёт **`3`** (строки 258,
  428 и 429), то есть критерий выполнялся ДО единого символа правки и остался бы зелёным, если бы
  задача 3 не написала записи закрытия вовсе. Заменяется на пару, где зубы несёт ПЕРВОЕ:
  - `grep -cF 'DEF-09-02 — ЗАКРЫТ' .planning/REQUIREMENTS.md` возвращает `1` (на `d6fc762` — `0`,
    ИЗМЕРЕНО). Литерал `DEF-09-02 — ЗАКРЫТ` есть ОБЯЗАТЕЛЬНОЕ начало записи закрытия и
    единственное, чего на `d6fc762` в файле нет: пройти этот счёт можно только НОВОЙ записью;
  - `grep -c 'DEF-09-02' .planning/REQUIREMENTS.md` возвращает не менее `4` (на `d6fc762` — `3`,
    ИЗМЕРЕНО). «Не менее», а не «ровно»: итоговая строка о маршрутизации четырёх пунктов
    ДОПОЛНЯЕТСЯ, и упоминание в ней законно.
- Прежняя формулировка пункта НЕ переписана:
  `grep -cF 'Четвёртым кругом не закрыт и закрываться не должен был' .planning/REQUIREMENTS.md`
  возвращает `1` — то же значение, что на `d6fc762`.
- Текст FORM-06 и строка прослеживаемости не тронуты:
  `git diff d6fc762 -- .planning/REQUIREMENTS.md | grep -c '^[-+].*FORM-06'` возвращает `0`.
- Роадмап и каталог разведки не тронуты:
  `git diff --name-only d6fc762 -- .planning/ROADMAP.md .planning/research/` пусто.
- ⚠️ **ГЕЙТ `<verify>` ЭТОЙ ЗАДАЧИ РАЗЛИЧАЕТ ИСПОЛНЕННУЮ ЗАДАЧУ И НЕИСПОЛНЕННУЮ, А ПРЕЖНИЙ НЕ
  РАЗЛИЧАЛ.** Прежним `<automated>` стоял голый `uv run pytest tests/test_planning/ -q -p no:randomly`,
  и он КРАСЕН СЕГОДНЯ по причине, к предмету задачи не относящейся: ИЗМЕРЕНО в проходе ревизии на
  живом дереве `ba0fa87` — `1 failed, 4 passed`, и падает
  `tests/test_planning/test_state_progress_matches_roadmap.py::test_the_machine_readable_progress_is_derived_from_the_roadmap`,
  то есть РОВНО предсуществующее расхождение записи **9** `.planning/WINDOWS.md`, которое эта же
  задача называет в критериях и от которого объявляет приёмку независимой. Задача правит только
  `.planning/WINDOWS.md` и `.planning/REQUIREMENTS.md`, а `ROADMAP.md` не трогает вовсе (критерий
  «роадмап не тронут» выше), — значит правило осталось бы красным и ПОСЛЕ успешно исполненной
  задачи, а исполнитель либо жёг бы круги ревизии на удавшейся задаче, либо чинил бы запись 9 вне
  мандата. Зубы задачи живут в командах состояния окна 15 и в литерале закрытия — они и стали
  гейтом. Различение ИЗМЕРЕНО ОБЕИМИ СТОРОНАМИ в проходе ревизии:
  - **красное сегодня, и по нужной причине:** на `ba0fa87` команда даёт код `1` и печатает пять
    расхождений — `таблица окно 15 open = 1 (ожидалось 0)`, `таблица окно 15 fixed = 0 (ожидалось 1)`,
    `JSON окно 15 open = 1 (ожидалось 0)`, `JSON окно 15 fixed = 0 (ожидалось 1)`,
    `DEF-09-02 закрытие = 0 (ожидалось 1)`; pytest-половина при этом ЗЕЛЕНА —
    `4 passed, 1 deselected`;
  - **зелёное после правок задачи:** на копии дерева с приложенными правками задачи 3 (окно 15
    переведено в `fixed` ОБЕИМИ половинами файла, запись закрытия дописана) те же пять строк
    сходятся и grep-половина даёт код `0`.
  ⚠️ Ожидаемый вывод гейта после исполнения задачи: пять строк вида `… = N (ожидалось N)` со
  сходящимися числами, затем `4 passed, 1 deselected`, код прогона `0`.
- Исключение `-k "not machine_readable_progress_is_derived_from_the_roadmap"` названо ПОИМЁННО и
  привязано к записи **9** `.planning/WINDOWS.md` — тем же способом, каким планы этой партии
  исключают поимённо предсуществующий красный записи 27. Каталог `tests/test_planning/` из гейта
  НЕ ВЫБРАСЫВАЕТСЯ: остальные четыре правила каталога остаются в нём и обязаны быть зелены —
  молчаливое выбрасывание каталога сняло бы и их. Цвет правила записи 9 этим планом НЕ ЧИНИТСЯ и
  в мандат задачи не входит.
- `uv run pytest tests/ -q -p no:randomly` — зелено за вычетом ДВУХ известных предсуществующих
  красных, названных поимённо: `test_the_overview_error_number_matches_the_users_own_dashboard`
  (запись 27, `DEF-09-01`) и `test_the_machine_readable_progress_is_derived_from_the_roadmap`
  (запись 9). Критерии приёмки не зависят НИ ОТ ОДНОГО из них, и ни один из них этим планом не
  чинится. ⚠️ Второй красный ИЗМЕРЕН прогоном каталога `tests/test_planning/` (он входит в
  `tests/`, поэтому виден и здесь); полный прогон `tests/` в проходе ревизии не снимался — он не
  уложился в отведённое время, и выдавать его за замер запрещено.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Окно 15 закрыто обеими половинами файла тем же коммитом, что и предмет, по форме, снятой с уже
закрытых окон; цена гейта заведена ОТДЕЛЬНЫМ открытым окном с названными кандидатами на закрытие
и без сочинённого назначения фазы; `DEF-09-02` получил запись закрытия в требованиях рядом с
прежней формулировкой, которая не переписана; ни FORM-06, ни строка прослеживаемости, ни роадмап,
ни каталог разведки не тронуты.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| воля человека → летящий необратимый запрос | человек закрывает панель и считает действие отменённым; действие происходит |
| признак отправки → три пути ухода панели | единственная величина, отделяющая «панель занята» от «панель свободна»; не сброшенная, она превращает гейт в ловушку |
| записи вехи → следующий круг | открытое окно с закрытым предметом отправляет следующий круг чинить закрытое; закрытое окно с живым предметом — прятать живое |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-38 | Spoofing | уход панели, выдающий себя за отмену необратимого действия | high | mitigate | Гейт по признаку отправки на ВСЕХ ТРЁХ путях ухода по воле человека, а не на одном; число путей объявлено константой и утверждается правилом с отказом, называющим путь поимённо; отрицательный контроль подставляет негейтированное выражение и обязан краснить на каждом из трёх |
| T-10-39 | Denial of Service | гейт, ставший ловушкой: панель, которую нельзя закрыть ничем | high | mitigate | Положительный контроль из ДВУХ половин: панель уходит по каждому из трёх путей при ложном признаке отправки И после ОТКАЗА СЕРВЕРА, где признак сбрасывается безусловно. Правило без второй половины к слиянию не принимается. Остаточная неограниченность (обмен, не завершающийся никак, при отсутствующем клиентском тайм-ауте) НАЗВАНА окном, а не замолчана |
| T-10-40 | Repudiation | закрытие предмета без записи, или запись без закрытия предмета | high | mitigate | Окно 15 закрывается ТЕМ ЖЕ коммитом, что и предмет, обеими половинами файла и по форме, снятой с уже закрытых окон; `DEF-09-02` называется поимённо в КОДЕ и получает запись закрытия в требованиях рядом с прежней формулировкой, которая не переписывается |
| T-10-41 | Elevation of Privilege | доступность кнопки отказа, принесённая в жертву гейту | medium | mitigate | Кнопка не получает признака отключённости, сохраняет ссылку начального фокуса, не выпадает из обхода по клавише табуляции и не меняет подписи. Утверждается ПОИМЕНОВАННЫМ ПРАВИЛОМ `test_the_cancel_button_stays_available_while_the_dismissal_is_gated` (задача 2, `<behavior>` п. 5), которое достаёт тег ИМЕННО этой кнопки помощником `_modal_cancel_button_tag` и доказывает АСИММЕТРИЮ: у кнопки отказа привязки отключённости нет, у кнопки подтверждения — есть. Грепом по файлу это неразличимо: привязка отключённости присутствует законно у кнопки подтверждения, в селекторе отключения на форме и в перечислителе фокусируемых узлов объекта клиентского состояния |
| T-10-42 | Tampering | четвёртый путь ухода, заведённый будущей фазой без гейта | medium | mitigate | `DISMISSAL_PATHS_DECLARED` объявляет число путей, разборщик утверждает, что нашёл ровно столько, и правило требует гейта у КАЖДОГО найденного выражения; число поставлено прогоном покрасневшего правила, а не сложением в уме |
| T-10-43 | Tampering | правка блока конфигурации рантайма ради снятия остаточной неограниченности | medium | mitigate | Блок не правится этим планом; дифф по нему обязан быть пуст (критерий приёмки задачи 2). Введение тайм-аута действует на КАЖДЫЙ запрос приложения и объявлено решением уровня вехи с записью окном и НЕназначенной фазой |
| T-10-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не выполняет ни одной установки: ни новой зависимости, ни build-шага, ни нового вендоренного файла. Разведки у фазы нет по решению роадмапа — предмета аудита легитимности пакетов не существует |
</threat_model>

<verification>
- `uv run pytest tests/ -q -p no:randomly` — зелено за вычетом известного предсуществующего
  красного `test_the_overview_error_number_matches_the_users_own_dashboard` (окно 27).
- `uv run pytest tests/test_templates/test_components.py -k "dismissal" -q -p no:randomly` →
  `5 passed, 71 deselected` (76 правил файла после этого плана минус 5 выбранных; база `64`
  измерена `--collect-only` на `d6fc762`, план 10-05 прибавляет 7, этот — 5).
- `grep -cF 'x-on:click="hide()"' app/templates/components/modal.html` → `0`;
  `grep -cF 'x-on:keydown.escape.window="hide()"' app/templates/components/modal.html` → `0`;
  `grep -cF 'DEF-09-02' app/templates/components/modal.html` → не менее `1`.
- `grep -cF 'DEF-09-02 — ЗАКРЫТ' .planning/REQUIREMENTS.md` → `1` (на `d6fc762` — `0`, измерено);
  `grep -cE '^\| 15 \|.*\| open \|' .planning/WINDOWS.md` → `0` (на `d6fc762` — `1`, измерено).
- `git diff --stat d6fc762 -- app/templates/includes/htmx_config.html` — пуст.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- `graphify update .` — не выполняется из рабочего дерева исполнителя по тому же основанию, что
  у планов 10-01…10-04 (записи 23 и 25 `.planning/WINDOWS.md`); передать оркестратору.
- Документная половина (формулировки записи закрытия и окон) закрывается чтением при приёмке
  фазы и вынесена `<human-check>` задачи 3 — прецедент D-33 Фазы 5.
- Наблюдение поведения гейта на живом стенде вынесено `<human-check>` задачи 2 и
  `verification: backstop` в `must_haves`; машинного эквивалента ему не заводится.
</verification>

<success_criteria>
1. `DEF-09-02` ЗАКРЫТ: панель не уходит с экрана ни одним из трёх путей по воле человека, пока
   необратимый запрос летит, и это верно на всех 18 местах подтверждения одной правкой рычага.
2. Число путей ухода объявлено и утверждается правилом; четвёртый путь без гейта краснеет.
3. Гейт доказанно НЕ ЛОВУШКА: панель уходит по каждому из трёх путей при завершившемся обмене —
   в том числе ПОСЛЕ отказа сервера, где D-12 обещает возможность повтора без переоткрытия.
4. Отрицательный контроль краснит на дословно сегодняшнем негейтированном выражении на каждом из
   трёх путей; переход цвета задачи 1 записан двумя SHA.
5. Записанный контракт о кнопке отказа ПРОЧИТАН, а не отменён: признака отключённости кнопка не
   получает, прежний текст не вычеркнут, различие «не действует» / «недоступна» выписано — и всё
   это стережёт ПОИМЕНОВАННОЕ правило
   `test_the_cancel_button_stays_available_while_the_dismissal_is_gated`, доказавшее асимметрию с
   кнопкой подтверждения, а не одна лишь запись шапки (угроза T-10-41 реализована, а не обещана).
6. `DEF-09-02` назван ПОИМЁННО в коде и получил запись закрытия в требованиях; окно 15 закрыто
   обеими половинами файла ТЕМ ЖЕ коммитом, что и предмет.
7. Цена гейта — отсутствие клиентского тайм-аута — названа окном с кандидатами на закрытие и без
   сочинённого назначения фазы; блок конфигурации не тронут.
8. Ребро конкурентности отката-фолбэка разрешено здесь и не продублировано планом 10-06.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-07-SUMMARY.md` when done.

Сводка обязана:
- привести `RED_SHA` и `GREEN_SHA` перехода цвета задачи 1 вместе с кодом прогона на красном;
- привести вердикт отрицательного контроля на дословно сегодняшнем выражении для КАЖДОГО из трёх
  путей;
- привести ОБЕ половины вердикта положительного контроля — при ложном признаке отправки и ПОСЛЕ
  отказа сервера, — потому что именно вторая отличает гейт от ловушки;
- привести вывод отказа правила инвентаря («найдено N, объявлено M») с путями поимённо: число
  поставлено прогоном, а не сложением в уме;
- привести фактическую строку `N passed, M deselected` фильтра `-k "dismissal"` (ожидается
  `5 passed, 71 deselected`) и, если `M` разошлось с объявленным `71`, назвать, ЧТО его сдвинуло;
- привести ОБА тега — кнопки отказа и кнопки подтверждения — дословно и назвать асимметрию:
  у первой привязки отключённости нет, у второй есть. Это и есть реализация T-10-41, и без обоих
  тегов в сводке она не показана;
- привести дословно ПРОЧТЕНИЕ записи о кнопке отказа в том виде, в каком оно попало в шапку, и
  подтвердить, что прежний текст не вычеркнут;
- привести дословно строку закрытия окна 15 обеими половинами файла и строки уже закрытых окон
  12 и 13 рядом — чтобы совпадение формы было видно, — И вывод всех ЧЕТЫРЁХ команд состояния
  окна 15 (две по таблице, две по блоку JSON) вместе с их базовыми значениями на `d6fc762`;
- привести вывод `grep -cF 'DEF-09-02 — ЗАКРЫТ' .planning/REQUIREMENTS.md` (`1`) и назвать
  прямо, что счёт вхождений `DEF-09-02` критерием НЕ является: на `d6fc762` он равен `3`, и
  прежняя формулировка этого критерия («не менее `2`, на `d6fc762` — `1`») была ЛОЖНОЙ и
  вакуумно зелёной — исправлено проходом ревизии;
- привести дословно описание НОВОГО окна цены гейта и назвать прямо, что фаза-владелец в нём НЕ
  назначена;
- назвать, что `DEF-09-02` до этого плана не содержал НИ ОДИН артефакт фазы, и привести
  фактическое число вхождений строки в коде и в требованиях после правки;
- передать ручному обходу фазы новую строку наблюдения (занятая панель на медленной сети и её
  освобождение после отказа сервера) РЯДОМ с действующими четырьмя пунктами, не заменяя их;
- назвать НЕЗАКРЫТЫМИ и передать обходу: пункт 4 перечня ручного UAT и пункт 2 перечня
  `human_verification`, переданный планом 10-05;
- передать оркестратору невыполненный `graphify update .` с основанием записей 23 и 25
  `.planning/WINDOWS.md`.
</output>
