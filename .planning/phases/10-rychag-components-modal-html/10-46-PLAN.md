---
phase: 10-rychag-components-modal-html
plan: 46
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, И ПРИЧИНА СМЕШАННАЯ, ПОЭТОМУ НАЗЫВАЕТСЯ ЦЕЛИКОМ. Две задачи
# из трёх производят ПРАВИЛА, и они несут `tdd="true"` с блоком `<behavior>` — дисциплина RED→GREEN
# на них действует. Правки прозы в документах записи и в комментариях продуктовых файлов кода не
# производят, и для них «ожидаемый выход при заданном входе» невыразим. Форма `type: tdd` на уровне
# ПЛАНА не берётся по той же причине, что у планов 10-41…10-45: блок `<feature>` не несёт полей
# `read_first` и `acceptance_criteria`, требуемых потребителем выхода на КАЖДОЙ задаче.
wave: 3
# ⚠️ ВОЛНА 3 — ПРЕДМЕТНАЯ ЗАВИСИМОСТЬ. Отметки окон 68 и 69 ставятся не раньше, чем их истины
# достигнуты планами 10-43 и 10-42; окно 70 закрывается задачей 1 настоящего плана.
# ⚠️ НАСТОЯЩИЙ ПЛАН — ЕДИНСТВЕННЫЙ В ВОЛНЕ 3, И ЭТО РЕШЕНИЕ, А НЕ СОВПАДЕНИЕ. План 10-45 уведён в
# волну 4 объявленным ребром на настоящий план: пересечения по `files_modified` у пары нет ни
# одного, но все три его задачи утверждают ПУСТОЙ ДИФФ ПО `app`, а настоящий план ЗАКОННО правит
# `app/templates/components/modal.html` и `app/pages/schedules.py` (тела комментариев). Пересекается
# ОБЛАСТЬ КОМАНДЫ ПРОВЕРКИ, а не перечень файлов, и при одновременном исполнении проверка соседа
# отказала бы на работе настоящего плана неатрибутируемо. Основание выбора маршрута записано у
# `wave:` плана 10-45 целиком. Настоящему плану из этого следует ОДНО обязательство: правки двух
# продуктовых файлов обязаны быть ЗАФИКСИРОВАНЫ до возврата плана — незафиксированная правка
# осталась бы видна `git diff` соседа и покраснила бы его гейт уже в волне 4.
depends_on: ["10-42", "10-43"]
gap_closure: true
gap_ids: [V9-06, WARN-MODAL-721, WARN-SCHED-28, WARN-ROADMAP-PLANS]
files_modified:
  - .planning/phases/10-rychag-components-modal-html/deferred-items.md
  - .planning/phases/10-rychag-components-modal-html/10-UAT.md
  - .planning/WINDOWS.md
  - app/templates/components/modal.html
  - app/pages/schedules.py
  - tests/test_planning/test_deferred_items_are_idempotent.py
  - tests/test_planning/test_state_progress_matches_roadmap.py
  - tests/test_pages/test_prose_names_live_symbols.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ: `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3).
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ПОВТОРНОЕ ИСПОЛНЕНИЕ ПРАВКИ ОТЛОЖЕННОГО НЕОТЛИЧИМО ОТ ПЕРВОГО: у файла отложенного появляются якорь конца перечня и правило единственности раздела на план, и второй прогон второго раздела не заводит"
    - "ПОМЕТКА, НАЗЫВАЮЩАЯ ЧУЖИЕ СТРОКИ, ЗАМЕНЯЕТСЯ ПОМЕТКОЙ, НАЗЫВАЮЩЕЙ ЧУЖИЕ ИМЕНА: номера строк протухают молча, имена — громко, и разрешение имени в живой символ принуждается правилом"
    - "УТВЕРЖДЕНИЕ О НЕИСПОЛЬЗОВАНИИ ГРАНИЦЫ ИДЕНТИФИКАТОРА ПРИВОДИТСЯ К ЗАМЕРУ: имя ВВОЗИТСЯ И ЗОВЁТСЯ, и читатель, снявший ввоз при уборке транзитных имён, больше не получает от комментария разрешения на это"
    - "СТРОКА ПРОЗЫ О ЧИСЛЕ ПЛАНОВ ПЕРЕСТАЁТ РАСХОДИТЬСЯ С ОТМЕТКАМИ МОЛЧА: числа прозы сличаются с числом отметок раздела фазы отдельным правилом, потому что действующий гейт читает ОТМЕТКИ, а не прозу"
    - "ТРИ ОТКРЫТЫХ ОКНА ПОЛУЧАЮТ ВЕРДИКТ ПО ДОСТИЖЕНИЮ ИСТИНЫ, А НЕ ПО ЗАВЕРШЕНИЮ ПАРТИИ: отметка ставится КОМАНДОЙ ведения журнала и только при зелёном прогоне правила, закрывшего истину"
    - "НЕЗАКРЫТОЕ ЧЕЛОВЕКОМ ОСТАЁТСЯ НАЗВАННЫМ И НЕЗАКРЫТЫМ: шаг 4.4, ненаблюдённый шаг 2.8 и отсутствие человеческой подписи на приёмке приписываются к артефакту обхода прямо, и НИ ОДНО из них не объявляется пройденным"
    - "ОТЛОЖЕННОЕ, ЗАКРЫТОЕ ПАРТИЕЙ, ПОМЕЧАЕТСЯ ЗАКРЫТЫМ, А ОСТАВШЕЕСЯ — ОСТАВШИМСЯ: две записи отложенного, закрытые планами 10-42 и 10-45, получают пометку с именем закрывшего плана; две другие остаются с прежним владельцем"
  assumptions:
    - statement: "РЕБРО `idempotency` ТРЕБОВАНИЯ FORM-06 (что будет, если это исполнить дважды на том же входе) ОСТАЁТСЯ НЕРАЗРЕШЁННЫМ ДЛЯ ПРОДУКТА И РАЗРЕШАЕТСЯ ТОЛЬКО ДЛЯ ЗАПИСЕЙ: задача 1 закрывает повторяемость правки ДОКУМЕНТА ОТЛОЖЕННОГО (это и есть гэп V9-06), а повторное подтверждённое удаление на живом маршруте настоящей партией не трогается вовсе — ни одна задача продукта не касается"
      edge: idempotency
      status: unresolved
    - statement: "РЕБРО `concurrency` ТРЕБОВАНИЯ FORM-06 (что гарантировано при прерывании либо параллельном исполнении) ОСТАЁТСЯ НЕРАЗРЕШЁННЫМ И НАСТОЯЩЕЙ ПАРТИЕЙ НЕ ЗАКРЫВАЕТСЯ: продуктовых строк партия не трогает, а параллельность здесь есть только параллельность ВОЛН партии, и она разведена ДВУМЯ мерами разом — непересекающимися перечнями правимых файлов и объявленным ребром там, где пересекается ОБЛАСТЬ КОМАНДЫ ПРОВЕРКИ, а не перечень файлов (план 10-45 уведён в волну 4 ребром на настоящий план); это свойство планирования, а не гарантия маршрута удаления"
      edge: concurrency
      status: unresolved
  artifacts:
    - "deferred-items.md — якорь конца перечня, пометки закрытия двух записей и раздел настоящего плана, внесённый ПО ЯКОРЮ"
    - "tests/test_planning/test_deferred_items_are_idempotent.py — правило единственности раздела на план и единственности якоря, с отрицательным контролем на синтетике"
    - "app/templates/components/modal.html — пометка, называющая ИМЕНА символов вместо номеров строк"
    - "app/pages/schedules.py — приведённое к замеру утверждение о границе величины идентификатора"
    - "tests/test_pages/test_prose_names_live_symbols.py — правило разрешения имён, названных прозой, в живые объявления, с отрицательным контролем"
    - "tests/test_planning/test_state_progress_matches_roadmap.py — правило согласия прозы о числе планов с отметками раздела фазы"
    - "10-UAT.md — приписка о трёх незакрытых человеком пунктах, без единой отметки о прохождении"
    - ".planning/WINDOWS.md — вердикты окон 68, 69 и 70, поставленные командой ведения журнала"
  key_links:
    - "якорь конца перечня → повторяемость правки записи: единственное, чем второй прогон отличим от первого; ломается — раздел удваивается, и обнаруживает это только следующий круг"
    - "правило разрешения имени в живой символ → негниющая пометка: единственное, чем протухание пометки становится громким; ломается — пометка снова живёт три круга, называя чужие строки"
    - "правило согласия прозы с отметками → строка числа планов: единственное, чем расхождение прозы становится машинным; ломается — действующий гейт читает отметки и прозу не видит по построению"
  prohibitions:
    - statement: "`10-UAT.md` НЕ ПЕРЕВОДИТСЯ В `passed` ИЛИ `complete` НИ ОДНОЙ ЗАДАЧЕЙ, И НИ ОДНА ОТМЕТКА ПРОХОЖДЕНИЯ В НЁМ НЕ СТАВИТСЯ: ради запрета этого самозаверения живёт `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`"
      status: flagged-unverified
      verification: test
    - statement: "ЧИСЛО ОБЪЯВЛЕННЫХ ПРОВЕРОК ОБХОДА И ЧИСЛО ТАБЛИЦ ОТМЕТОК В `10-UAT.md` НЕ ДВИГАЮТСЯ: приписка есть ПРОЗА, а не новая проверка, и правило согласия трёх чисел обязано остаться зелёным"
      status: flagged-unverified
      verification: test
    - statement: "ВЕРДИКТ `10-VERIFICATION.md` НЕ ПРАВИТСЯ, ОТМЕТКА `FORM-06` В `.planning/REQUIREMENTS.md` НЕ ТРОГАЕТСЯ"
      status: flagged-unverified
      verification: test
    - statement: "ПОВЕДЕНИЕ ПРОДУКТА НЕ ПРАВИТСЯ: в `app/templates/components/modal.html` и `app/pages/schedules.py` правится ИСКЛЮЧИТЕЛЬНО тело комментария, и ни одна исполняемая строка не заводится, не снимается и не переставляется"
      status: flagged-unverified
      verification: test
    - statement: "ВЕРДИКТ ОКНА СТАВИТСЯ ТОЛЬКО ПРИ ЗЕЛЁНОМ ПРОГОНЕ ПРАВИЛА, ЗАКРЫВШЕГО ЕГО ИСТИНУ: окно, отмеченное по завершению партии, а не по достижению истины, есть та же запись шире дерева"
      status: flagged-unverified
      verification: none
    - statement: "ИСПОЛНЕННЫЕ ПЛАНЫ 10-01…10-40 И ИХ СВОДКИ НЕ ПРАВЯТСЯ НИ НА СИМВОЛ"
      status: flagged-unverified
      verification: test
---

<objective>
Записи фазы приводятся к тому, что на дереве: повторяемая правка отложенного, негниющие пометки в
двух продуктовых комментариях, машинное согласие прозы о числе планов с отметками и вердикты трёх
открытых окон.

**`V9-06` / окно 70 — повторное исполнение отличимо от первого.** Истина плана 10-39 «повторное
исполнение правок неотличимо от первого» достигнута ДВУМЯ правками из ТРЁХ. Задача 3 того плана
внесла раздел «## План 10-39» БЕЗ якоря, дописыванием в конец файла: второй прогон дал бы ВТОРОЙ
такой раздел, и счёт подразделов вырос бы с 11 до 16. Латентно — план исполняется один раз. У файла
отложенного нет ни якоря конца перечня, ни правила единственности раздела на план.

**Пометка `modal.html:721,723` — `WR-03` ПЯТОГО круга не закрыт и протух сильнее.** Пометка называет
объявление заголовка перехода «на :148» и сборку ответа «(:375)». Замер 2026-09-10:
`HX_LOCATION_HEADER` объявлен в `app/pages/htmx.py` на `:59` и читается на `:169`; `:357` занят
совсем другой функцией; `respond()` объявлен на `:394`. ⚠️ ОТЯГЧАЮЩЕЕ: план 10-35 правил ЭТОТ ЖЕ
файл двадцатью строками выше и протухшую пометку не тронул. Отсюда прямое следствие: номер строки в
пометке протухает МОЛЧА, и чинить надо не три числа, а ФОРМУ ССЫЛКИ.

**Пометка `schedules.py:28` — `WR-01` ПЯТОГО круга не закрыт.** Комментарий гласит, что имя границы
величины идентификатора ввозится и не зовётся в этом файле ни разу; замер: `:352` несёт сличение с
этим именем в ИСПОЛНЯЕМОМ коде. Читатель, снявший ввоз при уборке транзитных имён, уронит разбор
идентификатора на отсутствующем имени — на маршруте подтверждённого удаления расписания, В ПРОДЕ.

**Строка `**Plans**` роадмапа.** Строка читает «40 планов, из них 34 исполнено»; замер даёт 40 из 40
при сорока сводках. Собственная идиома фазы («прежнее значение не было ошибкой — оно устарело») САМА
устарела ровно на ту партию, которая её дописывала. Действующий гейт этого не ловит ПО ПОСТРОЕНИЮ: он
читает ОТМЕТКИ, а не прозу. ⚠️ ЧИСЛА СТРОКИ ПРИВЕДЕНЫ ПРИ ПЛАНИРОВАНИИ ВОСЬМОЙ ПАРТИИ вместе с
внесением шести новых строк перечня; настоящий план закрывает не ЧИСЛА, а ОТСУТСТВИЕ ПРИНУЖДЕНИЯ.

**Три открытых окна.** 68 (реестр якорей — закрывает план 10-43), 69 (описка внутри группы —
закрывает план 10-42), 70 (повторяемость правки записи — закрывает задача 1 настоящего плана).
Вердикт ставится КОМАНДОЙ ведения журнала и только при зелёном прогоне закрывшего правила.

**Три пункта, которые человек обязан закрыть сам и которые ни одна задача не закрывает.** Шаг 4.4
обхода (провален), шаг 2.8 (не наблюдён) и отсутствие человеческой подписи на приёмке 2026-09-09
(наблюдал агент). Они ПРИПИСЫВАЮТСЯ к артефакту обхода прямо, чтобы следующий круг не выводил их
заново, и НИ ОДИН не объявляется пройденным.

Purpose: пять из шести гэпов круга — о правилах; шестой и все четыре предупреждения — о записях. Фаза
получила круги 3, 4, 5, 6 и 8 за один и тот же класс «запись шире дерева», и закрывать его надо
формой ссылки и правилом, а не третьей правкой трёх чисел.

Output: повторяемая правка отложенного; две негниющие пометки; правило согласия прозы с отметками;
вердикты трёх окон; названное незакрытое.

⚠️ **ПОВЕДЕНИЕ ПРОДУКТА НЕ ТРОНУТО.** В двух продуктовых файлах правится ИСКЛЮЧИТЕЛЬНО тело
комментария.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-39-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**Новые файлы:**

- `tests/test_planning/test_deferred_items_are_idempotent.py` — правило единственности раздела на
  план и единственности якоря конца перечня во ВСЕХ файлах отложенного дерева планирования, с
  отрицательным контролем на синтетическом тексте;
- `tests/test_pages/test_prose_names_live_symbols.py` — правило разрешения имён, названных прозой
  продуктовых комментариев, в живые объявления, с отрицательным контролем.

**Новые символы:**

- `DEFERRED_END_ANCHOR` — якорь конца перечня отложенного (комментарий разметки, невидимый при
  отрисовке документа);
- `PROSE_SYMBOL_CITATIONS` — реестр «файл прозы → имя символа → файл объявления», выписанный
  литералами;
- `roadmap_plan_prose(text)` и `prose_divergence(roadmap_text)` в
  `tests/test_planning/test_state_progress_matches_roadmap.py`;
- `test_the_prose_plan_counts_agree_with_the_marks` и его отрицательный контроль на синтетическом
  роадмапе.

**Изменённые артефакты:** `deferred-items.md` (якорь, две пометки закрытия, раздел плана 10-46),
`10-UAT.md` (приписка о трёх незакрытых человеком пунктах), `.planning/WINDOWS.md` (вердикты окон 68,
69, 70), тело комментария `app/templates/components/modal.html:713-730`, тело комментария
`app/pages/schedules.py:26-33`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: у файла отложенного появляются якорь и правило единственности раздела — повторный прогон перестаёт удваивать запись</name>

  <files>
.planning/phases/10-rychag-components-modal-html/deferred-items.md
tests/test_planning/test_deferred_items_are_idempotent.py
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md:1-10` — шапка и правило файла.
  ⚠️ СЮДА ВСТАЁТ ОБЪЯВЛЕНИЕ ЯКОРЯ И ПРАВИЛА ЕДИНСТВЕННОСТИ.
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md:152-175` — раздел «## План
  10-39» целиком: как он внесён и почему второй прогон завёл бы второй такой раздел.
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md`, подразделы «`IN-02`» и
  «`IN-03`» раздела плана 10-39 — записи, чей владелец назван настоящей партией. ⚠️ ЧИТАТЬ
  ДОСЛОВНО: две из них закрываются планами 10-42 и 10-45, и это надо ПОМЕТИТЬ, а не переписать.
- `.planning/WINDOWS.md`, строка окна 70 — дословная формулировка недостигнутой истины, включая счёт
  подразделов 11 и 16.
- `tests/test_planning/test_state_progress_matches_roadmap.py:196-230` — `_synthetic_roadmap` и
  `_synthetic_state`. ⚠️ ФОРМА ОТРИЦАТЕЛЬНОГО КОНТРОЛЯ НА СИНТЕТИЧЕСКОМ ТЕКСТЕ НАСЛЕДУЕТСЯ ОТСЮДА:
  ожидание выписывается ЛИТЕРАЛОМ в синтетике, а не вычисляется из живого файла.
- `tests/test_planning/test_the_walkthrough_cannot_self_certify.py:100-120` — образец чтения поля
  состояния первого блока frontmatter. ⚠️ ФОРМА РАЗБОРА ДОКУМЕНТОВ ЗАПИСИ НАСЛЕДУЕТСЯ ОТСЮДА.
  </read_first>

  <behavior>
1. `deferred_items_findings(text)` на синтетическом тексте, где заголовок раздела одного плана
   встречается ДВАЖДЫ, даёт РОВНО ОДНУ находку, называющую этот план и оба вхождения.
2. На синтетическом тексте БЕЗ якоря конца перечня даёт ровно одну находку с ДРУГИМ основанием.
3. На синтетическом тексте, где якорь встречается дважды, даёт ровно одну находку с третьим
   основанием.
4. На боевом файле отложенного даёт ПУСТОТУ после правки; до правки — одну находку (якоря нет).
5. Правило перебирает ВСЕ файлы отложенного дерева планирования, а не один, и несёт антивакуумную
   половину первой: файлов найдено не ноль.
  </behavior>

  <action>
**(1) RED. НАПИСАТЬ ПРАВИЛО И ЕГО КОНТРОЛЬ ПЕРВЫМИ.** Контроль стои́т на ТРЁХ синтетических текстах,
выписанных литералами; правило — на боевых файлах. Сегодня правило красно у боевого файла отсутствием
якоря. Записать команду, код возврата, имя правила, ожидаемое и полученное в сводку как свидетельство
RED.

**(2) GREEN — ЯКОРЬ.** `DEFERRED_END_ANCHOR` — комментарий разметки, невидимый при отрисовке
документа, ставится в КОНЕЦ файла отложенного и объявляется в шапке файла вместе с правилом: «раздел
на план ОДИН, и вносится он ПЕРЕД якорем». ⚠️ ЯКОРЬ ЕСТЬ КОММЕНТАРИЙ РАЗМЕТКИ, А НЕ ЗАГОЛОВОК:
заголовок попал бы в оглавление и в счёт подразделов, то есть сам стал бы величиной, которую надо
сверять.

**(3) GREEN — ПРАВИЛО.** `deferred_items_findings(text)` принимает ТЕКСТ параметром (иначе контроль
зубов невыразим — та же форма и то же основание, что у `missing_anchors`) и возвращает расхождения
трёх родов, каждый со своим текстом: заголовок раздела плана встречается более одного раза; якоря
конца перечня нет; якорь встречается более одного раза. Правило перебирает все файлы отложенного
дерева планирования, найденные обходом, и несёт антивакуумную половину первой.

**(4) GREEN — РАЗДЕЛ НАСТОЯЩЕГО ПЛАНА ВНОСИТСЯ ПО ЯКОРЮ.** Раздел «## План 10-46» вносится ПЕРЕД
якорем, а не дописыванием в конец. Перед внесением считается число вхождений его заголовка, после —
тоже; оба числа называются в сводке, и второе равно единице. ⚠️ ЭТО И ЕСТЬ ДОСТИЖЕНИЕ ИСТИНЫ ОКНА 70,
показанное числом, а не заявленное.

**(5) GREEN — ДВЕ ЗАПИСИ ОТЛОЖЕННОГО ПОМЕЧАЮТСЯ ЗАКРЫТЫМИ, А НЕ УДАЛЯЮТСЯ.** Запись о рвущемся
образце знаков сочетания (вторая половина `IN-02` седьмого круга) закрыта планом 10-42; запись о
докстринге разборщика таблицы стилей (`IN-03` седьмого круга) закрыта планом 10-45 частично — в
части третьего элемента тройки. К каждой приписывается: ЧЕМ закрыто, КАКИМ планом, КОГДА и — у
второй — ЧТО ОСТАЛОСЬ. Прежний текст стои́т дословно. Идиома D-30/D-32. Две другие записи (`IN-04` о
прозе без гейта и `IN-05` об отступе безопасной зоны) остаются с прежним владельцем, и это
называется прямо, а не оставляется на догадку.

**(6) РАЗДЕЛ НАСТОЯЩЕГО ПЛАНА НАЗЫВАЕТ, ЧТО ВОСЬМАЯ ПАРТИЯ ОСТАВИЛА.** Поимённо: `IN-04` и `IN-05`
седьмого круга; шесть неограниченных входов и `WR-04-A` (долг без владельца, вне предмета цели фазы);
`IN-03` пятого круга о правиле, краснеющем на первом артефакте Фазы 11; шесть неограниченных входов
приложения каталога. У каждого — почему не правится здесь и кто владелец. ⚠️ ЭТО ЯВНОЕ ОТКЛОНЕНИЕ, А
НЕ МОЛЧАЛИВЫЙ ПРОПУСК: перечень назван в промпте планирования как «вне области, названо чтобы не было
подобрано молча», и запись об этом обязана существовать.
  </action>

  <acceptance_criteria>
- `DEFERRED_END_ANCHOR` встречается в файле отложенного РОВНО ОДИН раз и стои́т последним непустым
  содержательным элементом файла.
- Заголовок «## План 10-46» встречается РОВНО ОДИН раз; числа вхождений до и после названы в сводке.
- Правило на боевых файлах отложенного пусто; на трёх синтетических текстах даёт по одной находке с
  ТРЕМЯ РАЗНЫМИ основаниями.
- Синтетические тексты контроля выписаны литералами и не строятся из боевого файла — проверяется
  чтением диффа и называется в сводке.
- Записи об `IN-02` и `IN-03` седьмого круга помечены закрытыми с именем закрывшего плана; прежний
  текст стои́т дословно; `IN-04` и `IN-05` остаются с названным владельцем.
- Раздел плана 10-46 перечисляет поимённо всё, что партия оставила, с владельцем у каждого пункта.
- `uv run pytest tests/test_planning/ -q` завершается нулём.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/test_deferred_items_are_idempotent.py -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо отобрано меньше двух правил (несущее и контроль трёх оснований)</fails_when>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed", либо число зелёных меньше 28 плюс число новых правил</fails_when>
  </verify>

  <done>
У файла отложенного есть якорь конца перечня и объявленное правило единственности раздела на план;
раздел настоящего плана внесён по якорю, и второй прогон второго раздела не заводит; правило
принуждает три свойства и стои́т на литеральных синтетических текстах; две закрытые записи помечены,
две оставшиеся названы с владельцем; всё, оставленное партией, перечислено поимённо.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: две протухшие пометки в продуктовых файлах заменяются формой, которая протухает ГРОМКО</name>

  <files>
app/templates/components/modal.html
app/pages/schedules.py
tests/test_pages/test_prose_names_live_symbols.py
  </files>

  <read_first>
- `app/templates/components/modal.html:713-735` — абзац «⚠️ ВТОРОЙ ДИЗЪЮНКТ УСЛОВИЯ ЗАКРЫТИЯ ПАНЕЛИ»
  целиком, включая обе протухшие ссылки на номера строк. ⚠️ ЭТО ПРЕДМЕТ ПРАВКИ, И ПРАВИТСЯ ТОЛЬКО
  ТЕЛО КОММЕНТАРИЯ.
- `app/pages/htmx.py:55-62` — объявление имени заголовка перехода; `:150-175` — объявление и тело
  функции сборки ответа с переходом; `:355-360` — что на самом деле лежит там, куда указывает
  протухшая ссылка; `:390-400` — объявление функции ответа. ⚠️ ЧИТАТЬ ВСЕ ЧЕТЫРЕ ДИАПАЗОНА: имена
  берутся отсюда, а номера строк в новую пометку НЕ ПОПАДАЮТ ВОВСЕ.
- `app/main.py:218-230` — объявление обработчика отказа зависимости и вызов сборки ответа.
- `app/pages/schedules.py:26-35` — абзац о границе величины идентификатора вместе с ввозом.
  ⚠️ ПРЕДМЕТ ПРАВКИ.
- `app/pages/schedules.py:340-356` — функция разбора идентификатора из тела формы, где имя границы
  ЗОВЁТСЯ в исполняемом коде. ⚠️ ЧИТАТЬ ГЛАЗОМ НА ФАКТ: именно это опровергает пометку.
- `tests/test_pages/test_editor_schedules.py:30-40` и
  `tests/test_pages/test_confirm_delete_transport.py:112-122` — два модуля суиты, ввозящие имя
  границы ОТСЮДА. ⚠️ ВТОРАЯ ПОЛОВИНА ПОМЕТКИ ВЕРНА и остаётся: имя действительно сохранённый вход.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `Anti-Patterns Found` —
  дословные формулировки обоих предупреждений.
  </read_first>

  <behavior>
1. Правило на боевом дереве пусто: каждое имя реестра ссылок разрешается в объявление в названном
   файле, и каждый файл прозы это имя действительно называет.
2. Правило на реестре с именем, которого в названном файле НЕТ, даёт ровно одну находку с основанием
   «имя не разрешается в объявление» и называет и имя, и файл.
3. Правило на реестре, где файл прозы имени НЕ называет, даёт ровно одну находку с ДРУГИМ основанием:
   реестр, стерегущий несуществующую ссылку, стережёт пустоту.
4. В теле правимого комментария разметки не остаётся ни одного вхождения образца «двоеточие плюс
   цифры», указывающего в чужой файл.
  </behavior>

  <action>
**(1) RED. НАПИСАТЬ ПРАВИЛО И ДВА ЕГО КОНТРОЛЯ ПЕРВЫМИ.** Реестр ссылок собирается с ТРЕМЯ записями,
называющими имена, которые новая пометка будет употреблять; правило сегодня красно, потому что
пометка их ещё не называет. Записать команду, код возврата, имя правила, ожидаемое и полученное в
сводку как свидетельство RED.

**(2) GREEN — РЕЕСТР ССЫЛОК.** `PROSE_SYMBOL_CITATIONS` — кортеж записей «файл прозы, имя символа,
файл объявления», выписанный ЛИТЕРАЛАМИ. Три записи: имя заголовка перехода, функция сборки ответа с
переходом и функция ответа — все три объявлены в модуле слоя перехода, все три называются пометкой
рычага. Разрешение имени в объявление ищется образцом объявления языка (для имени верхнего уровня —
присваивание в начале строки; для функции — ключевое слово объявления перед именем), а не простым
вхождением: вхождение нашлось бы и в комментарии.

**(3) GREEN — ПРАВИЛО И ДВА КОНТРОЛЯ.** Правило перебирает реестр и требует пустоты находок; оба
контроля стоя́т на реестрах, выписанных ЛИТЕРАЛАМИ прямо в контроле, — по одному на основание.
⚠️ НЕЦИКЛИЧНОСТЬ ОБЪЯВЛЯЕТСЯ В ДОКСТРИНГЕ ПРЯМО: ожидание контроля не берётся из
`PROSE_SYMBOL_CITATIONS`; контроль, строящий негодный реестр из проверяемого, доказывал бы согласие
правила с собой.

**(4) GREEN — ПОМЕТКА РЫЧАГА ПЕРЕПИСЫВАЕТСЯ НА ИМЕНА.** Из тела комментария УБИРАЮТСЯ ВСЕ ссылки
вида «файл, двоеточие, номер строки» на чужие файлы; вместо них называются ИМЕНА: имя заголовка
перехода, имя функции сборки ответа, имя функции ответа, имя обработчика отказа зависимости — и файлы,
в которых они объявлены, БЕЗ номеров строк. Прежняя формулировка приводится дословно и помечается
УСТАРЕВШЕЙ (не ошибочной: она была верна для дерева, на котором писалась), с указанием замера
2026-09-10, назвавшего действительные места. Идиома D-30/D-32. Прямо называется, ПОЧЕМУ форма ссылки
сменилась: номер строки протухает молча, и это доказано трижды — пометка пережила три круга и правку
соседнего плана в том же файле.

⚠️ **ВНУТРИ ТЕЛА КОММЕНТАРИЯ НЕ ПОЯВЛЯЕТСЯ НИ ОДНОГО ОБЪЯВЛЕНИЯ РАЗМЕТКИ И НИ ОДНОЙ ИСПОЛНЯЕМОЙ
СТРОКИ.** Комментарий есть комментарий Jinja и вырезается при отрисовке; это его действующее свойство
и оно сохраняется.

**(5) GREEN — ПОМЕТКА РАЗБОРА ИДЕНТИФИКАТОРА ПРИВОДИТСЯ К ЗАМЕРУ.** Утверждение о том, что имя
границы величины идентификатора не зовётся в файле ни разу, ЗАМЕНЯЕТСЯ замером: имя зовётся РОВНО В
ОДНОМ месте — в функции разбора идентификатора из тела формы, — и СВЕРХ ТОГО ввоз есть сохранённый
вход для двух модулей суиты. Прежняя формулировка приводится дословно и помечается ОПРОВЕРГНУТОЙ (не
устаревшей: она была неверна и на дереве, где писалась) с указанием чем и когда. Прямо называется
следствие, которое пометка едва не оплатила: читатель, снявший ввоз при уборке транзитных имён,
уронил бы разбор идентификатора на отсутствующем имени — на маршруте подтверждённого удаления
расписания, в проде.

**(6) ПОДТВЕРДИТЬ, ЧТО НИ ОДНА ИСПОЛНЯЕМАЯ СТРОКА НЕ ТРОНУТА.** Дифф обоих продуктовых файлов
показывает изменения только внутри тел комментариев.
  </action>

  <acceptance_criteria>
- В теле правимого комментария разметки нет ни одного вхождения ссылки вида «имя файла, двоеточие,
  номер строки» на чужой файл — проверяется поиском по телу комментария и называется числом в сводке.
- Правило разрешения имён на боевом дереве пусто; оба контроля краснеют на своих литеральных
  реестрах с РАЗНЫМИ основаниями.
- Прежняя формулировка пометки рычага присутствует дословно и помечена устаревшей; прежняя
  формулировка пометки разбора идентификатора присутствует дословно и помечена опровергнутой.
- Новая формулировка пометки разбора идентификатора называет ЧИСЛО мест вызова (одно) и обе половины
  основания ввоза.
- `git diff app/templates/components/modal.html app/pages/schedules.py` показывает изменения только
  внутри тел комментариев: ни одна изменённая строка не несёт исполняемого кода.
- `uv run python -m compileall -q app main.py tests` завершается нулём.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_prose_names_live_symbols.py -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо отобрано меньше трёх правил (несущее и два контроля оснований)</fails_when>
    <automated>uv run pytest tests/test_templates/test_components.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_editor_schedules.py -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed" — правка комментариев задела правила, читающие исходники этих двух продуктовых файлов</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — продуктовый модуль перестал разбираться интерпретатором, чего правка комментария вызвать не может</fails_when>
  </verify>

  <done>
Пометка рычага называет ИМЕНА символов и файлы их объявления без единого номера строки, и разрешение
имён принуждается правилом с двумя контролями; пометка разбора идентификатора приведена к замеру и
называет число мест вызова; обе прежние формулировки стоя́т дословно с верной пометкой (устаревшая и
опровергнутая соответственно); ни одна исполняемая строка не тронута.
  </done>
</task>

<task type="auto">
  <name>Задача 3: проза о числе планов принуждается правилом, три окна получают вердикт, незакрытое человеком названо и оставлено незакрытым</name>

  <files>
tests/test_planning/test_state_progress_matches_roadmap.py
.planning/WINDOWS.md
.planning/phases/10-rychag-components-modal-html/10-UAT.md
  </files>

  <read_first>
- `tests/test_planning/test_state_progress_matches_roadmap.py:40-110` — константы путей и образцов,
  `roadmap_plan_counts`, `roadmap_totals`. ⚠️ ЧИТАТЬ ДОСЛОВНО: раздел фазы опознаётся по образцу
  заголовка, а отметки — по двум образцам строк перечня; новая половина ЧИТАЕТ ТЕ ЖЕ разделы.
- `tests/test_planning/test_state_progress_matches_roadmap.py:110-200` — `state_progress`,
  `Divergence`, `progress_divergence`, `_report` и несущее правило. ⚠️ ФОРМА ЗАПИСИ РАСХОЖДЕНИЯ
  НАСЛЕДУЕТСЯ ОТСЮДА.
- `tests/test_planning/test_state_progress_matches_roadmap.py:196-304` — `_synthetic_roadmap`,
  `_synthetic_state` и четыре контроля. ⚠️ ФОРМА ОТРИЦАТЕЛЬНОГО КОНТРОЛЯ НА СИНТЕТИКЕ НАСЛЕДУЕТСЯ
  ОТСЮДА ДОСЛОВНО.
- `.planning/ROADMAP.md`, строка `**Plans**` раздела Фазы 10 — образец прозы, которую разбирает новая
  половина. ⚠️ ЧИТАТЬ, НО НЕ ПРАВИТЬ: числа приведены при планировании восьмой партии, и предмет
  задачи — принуждение, а не они.
- `.planning/ROADMAP.md`, строки `**Plans**` разделов Фаз 07, 08, 09 и 11-15 — ЧЕТЫРЕ РАЗНЫЕ формы
  прозы. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: образец обязан опознавать ТОЛЬКО форму «N планов, из них M
  исполнено» и молча пропускать остальные, а пропуск — быть названным, а не молчаливым.
- `.planning/WINDOWS.md:1-20` — шапка журнала и объявленный способ смены вердикта: командой ведения
  журнала, а не правкой строки.
- `.planning/phases/10-rychag-components-modal-html/10-UAT.md:1-40` — шапка обхода целиком, включая
  запрет терминального состояния. ⚠️ ЧИТАТЬ ДОСЛОВНО И НЕ ПРАВИТЬ ПОЛЕ СОСТОЯНИЯ.
- `tests/test_planning/test_the_walkthrough_cannot_self_certify.py:340-400` — правило согласия трёх
  чисел обхода. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: приписка не имеет права сдвинуть ни число объявленных
  проверок, ни число таблиц отметок.
  </read_first>

  <behavior>
1. `prose_divergence` на синтетическом роадмапе, где проза говорит одно число, а отметок другое
   число, даёт РОВНО ОДНУ находку, называющую раздел, число прозы и число отметок.
2. На синтетическом роадмапе, где НИ ОДИН раздел не несёт прозы опознаваемой формы, правило РОНЯЕТСЯ
   антивакуумной половиной: разбор, не нашедший ни одной прозы, зеленел бы сам собой.
3. На боевом роадмапе правило пусто.
4. Правило перебирает ВСЕ разделы фаз, а не один, и разделы с прозой иной формы пропускает; число
   опознанных и число пропущенных называется в тексте антивакуумной половины.
5. Вердикт окна ставится только при зелёном прогоне правила, закрывшего его истину; окно, чьё правило
   красно либо не найдено, остаётся открытым, и это записывается.
6. `10-UAT.md` сохраняет прежнее поле состояния, прежнее число объявленных проверок и прежнее число
   таблиц отметок.
  </behavior>

  <action>
**(1) НАПИСАТЬ ПРАВИЛО СОГЛАСИЯ ПРОЗЫ С ОТМЕТКАМИ.** `roadmap_plan_prose(text)` возвращает карту
«раздел фазы → пара чисел прозы» по разделам, чья строка перечня планов несёт форму «N планов, из них
M исполнено»; `prose_divergence(roadmap_text)` сличает эту карту с картой отметок, которую уже строит
действующий разборщик. Расхождение записывается формой действующей записи расхождения — тем же типом,
теми же полями. ⚠️ ЧИСЛО ИСПОЛНЕННЫХ В ПРОЗЕ СЛИЧАЕТСЯ С ЧИСЛОМ ОТМЕТОК ИСПОЛНЕНИЯ, А НЕ СО ЧИСЛОМ
СВОДОК: сводка есть свидетельство завершения, а отметку ставит оркестратор, и сличение с иным
источником краснело бы на состоянии оркестратора, а не на расхождении записи.

**(2) АНТИВАКУУМНАЯ ПОЛОВИНА ИДЁТ ПЕРВОЙ И НАЗЫВАЕТ ЧИСЛА.** Разделов фаз найдено не ноль; разделов с
прозой опознаваемой формы найдено не ноль; число опознанных и число пропущенных названо в тексте
утверждения. ⚠️ БЕЗ ЭТОГО ПРАВИЛО ЗЕЛЕНЕЛО БЫ НА РОАДМАПЕ, ГДЕ ФОРМА ПРОЗЫ СМЕНИЛАСЬ ЦЕЛИКОМ, — то
есть ровно тогда, когда оно нужнее всего.

**(3) ДВА ОТРИЦАТЕЛЬНЫХ КОНТРОЛЯ НА СИНТЕТИКЕ.** Первый: проза расходится с отметками — ровно одна
находка с обоими числами. Второй: прозы опознаваемой формы нет вовсе — красна антивакуумная половина.
Оба текста синтетики выписаны ЛИТЕРАЛАМИ по форме соседних контролей модуля; ожидания в них тоже
литеральные. Свойство нецикличности объявляется в докстринге обоих.

**(4) ОТМЕТИТЬ ТРИ ОКНА — КОМАНДОЙ ВЕДЕНИЯ ЖУРНАЛА И ТОЛЬКО ПРИ ЗЕЛЁНОМ ПРОГОНЕ.** Перед каждой
отметкой прогоняется правило, закрывшее истину окна: для окна 68 — правило набираемости реестра
якорей (план 10-43); для окна 69 — правило согласия канона с перечнями гейта и контроль нецикличности
(план 10-42); для окна 70 — правило единственности раздела отложенного (задача 1 настоящего плана).
Вердикт ставится ТОЛЬКО при зелёном прогоне и ТОЛЬКО командой ведения журнала, а не правкой строки
файла. ⚠️ ЕСЛИ ХОТЯ БЫ ОДНО ПРАВИЛО КРАСНО ЛИБО НЕ НАЙДЕНО, соответствующее окно ОСТАЁТСЯ ОТКРЫТЫМ, и
это записывается в сводку отдельной строкой с названной причиной. Отметка по завершению партии, а не
по достижению истины, есть та же запись шире дерева, за которую фаза получила пять кругов.

**(5) ПРИПИСАТЬ К АРТЕФАКТУ ОБХОДА ТРИ ПУНКТА, КОТОРЫЕ ЧЕЛОВЕК ОБЯЗАН ЗАКРЫТЬ САМ.** Шаг 4.4
(провален; замер прода 2026-09-09 давал плашку в потоке, слой по умолчанию, прямоугольник целиком
выше окна; перезамер — человеком); шаг 2.8 (не наблюдён; предмет — плашка обрыва связи при не
доехавшем последующем запросе на НАСТОЯЩЕМ обрыве транспорта, а не подменой средства запроса);
человеческая подпись на приёмке 2026-09-09 (наблюдал агент через средство браузера, а признак живого
наблюдения выключен, и обход шёл на проде). У каждого пункта называется, ЧТО именно наблюдать и
ПОЧЕМУ машина этого не может. Приписка ставится ПРОЗОЙ и НЕ ЗАВОДИТ ни новой проверки, ни новой
таблицы отметок.
⚠️ **НИ ОДНО ИЗ ТРЁХ НЕ ОБЪЯВЛЯЕТСЯ ПРОЙДЕННЫМ, И ПОЛЕ СОСТОЯНИЯ АРТЕФАКТА НЕ ДВИГАЕТСЯ.** Сверх
того приписка называет, что правки настоящей партии предмета шага 4.4 НЕ СДВИНУЛИ: поведение продукта
партией не тронуто, и перезамер шага остаётся тем же перезамером.

**(6) ПОДТВЕРДИТЬ ПРОГОНОМ, ЧТО АРТЕФАКТ ОБХОДА НЕ САМОЗАВЕРИЛСЯ.** Каталог правил записи зелен,
поле состояния прежнее, число объявленных проверок прежнее, число таблиц отметок прежнее — все
четыре величины названы в сводке до и после.
  </action>

  <acceptance_criteria>
- `prose_divergence` на боевом роадмапе пусто; на синтетике с расхождением даёт ровно одну находку с
  обоими числами.
- Антивакуумная половина краснеет на синтетике без прозы опознаваемой формы и называет число
  опознанных и пропущенных разделов.
- Оба синтетических текста контроля выписаны литералами; свойство нецикличности объявлено в
  докстрингах — проверяется чтением диффа.
- Вердикт каждого из окон 68, 69, 70 поставлен командой ведения журнала И только после зелёного
  прогона закрывшего правила; для каждого окна имя правила и итог прогона названы в сводке. Окно с
  красным либо ненайденным правилом осталось открытым с записанной причиной.
- В `10-UAT.md` поле состояния, число объявленных проверок и число таблиц отметок совпадают с
  состоянием до правки (диффом и прогоном).
- Приписка называет все три пункта, у каждого — что наблюдать и почему машина не может.
- `uv run pytest tests/test_planning/ -q` завершается нулём.
- `git diff --numstat -- .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md`
  пуст — ОБЛАСТЬ КРИТЕРИЯ ТА ЖЕ, ЧТО У АВТОМАТИЧЕСКОЙ КОМАНДЫ ПРОВЕРКИ ЭТОГО БЛОКА, и шире её быть
  не может.
  ⚠️ ОСНОВАНИЕ ЗАПИСЫВАЕТСЯ, ЧТОБЫ ПРЕЖНЯЯ ФОРМУЛИРОВКА НЕ ВЕРНУЛАСЬ: задача 2 ТОГО ЖЕ ПЛАНА правит
  `app/templates/components/modal.html` и `app/pages/schedules.py` и заводит
  `tests/test_pages/test_prose_names_live_symbols.py`; критерий, называвший `app`, `tests/test_pages`
  и `tests/test_templates`, был бы выполним ТОЛЬКО при незаписанном допущении, что задача 2 успела
  зафиксироваться, — то есть противоречил бы работе предыдущей задачи собственного плана. Запреты
  правки продукта эта область не ослабляет: их держит критерий диффа двух продуктовых файлов у
  задачи 2 («ни одна изменённая строка не несёт исполняемого кода») и прогон правил, читающих оба
  файла.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed", либо число зелёных меньше 28 плюс число новых правил — приписка к артефакту обхода уронила правило самозаверения либо правило согласия трёх чисел</fails_when>
    <automated>git diff --numstat -- .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md</automated>
    <fails_when>вывод НЕПУСТ — тронуты отметка требования либо вердикт верификации, что запрещено прямо</fails_when>
  </verify>

  <done>
Согласие прозы о числе планов с отметками принуждается правилом с антивакуумной половиной и двумя
литеральными контролями; вердикты окон 68, 69 и 70 поставлены командой ведения журнала и только по
зелёному прогону закрывших правил, а неподтверждённые окна остались открытыми с причиной; три пункта,
принадлежащие человеку, приписаны к артефакту обхода прозой, ни один не объявлен пройденным, и поле
состояния артефакта не сдвинуто.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| задача записи → документ отложенного | правка, дописывающая в конец, на втором прогоне удваивает раздел, и следующий круг читает две несогласные записи |
| проза продуктового комментария → читатель, убирающий транзитные имена | комментарий, утверждающий, что имя не зовётся, есть разрешение снять ввоз — на маршруте необратимого удаления |
| исполнитель плана → артефакт ручного обхода | отметка прохождения, поставленная машиной, есть самозаверение второй половины цели фазы |
| исполнитель плана → журнал окон | вердикт окна, поставленный по завершению партии, а не по достижению истины, объявляет закрытым то, что не проверено |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-46-01 | Elevation of privilege | приписка к `10-UAT.md` | high | mitigate | Задача, объявившая шаг обхода пройденным, заверяет вторую половину цели фазы машиной вместо человека. Закрывается прямым запретом терминального состояния, требованием неизменности трёх величин артефакта и прогоном правила самозаверения |
| T-10-46-02 | Tampering | пометка о границе величины идентификатора | high | mitigate | Комментарий разрешает снять ввоз имени, которое зовётся в исполняемом коде на маршруте подтверждённого удаления расписания: следствие — отказ разбора идентификатора в проде. Закрывается приведением пометки к замеру с числом мест вызова |
| T-10-46-03 | Repudiation | пометка рычага с номерами чужих строк | medium | mitigate | Номер строки протухает молча; пометка пережила три круга и правку соседнего плана в том же файле. Закрывается сменой формы ссылки на имена и правилом разрешения имени в живое объявление |
| T-10-46-04 | Repudiation | вердикт окна, поставленный по завершению партии | high | mitigate | Окно, отмеченное закрытым без прогона закрывшего правила, объявляет достигнутой истину, которую никто не проверял. Закрывается требованием зелёного прогона перед каждой отметкой и записью причины у неподтверждённых |
| T-10-46-05 | Repudiation | раздел плана, внесённый дописыванием в конец | medium | mitigate | Второй прогон заводит второй раздел, и следующий круг читает две несогласные записи — дословный класс `V9-06`. Закрывается якорем конца перечня, правилом единственности и счётом вхождений до и после |
| T-10-46-06 | Spoofing | правило согласия прозы с отметками | medium | mitigate | Правило, не нашедшее ни одной прозы опознаваемой формы, зеленело бы само собой — то есть ровно тогда, когда форма прозы сменилась целиком. Закрывается антивакуумной половиной, называющей числа опознанных и пропущенных разделов |
| T-10-46-07 | Tampering | тела комментариев в двух продуктовых файлах | medium | mitigate | Правка комментария способна задеть исполняемую строку либо дословный кусок, которым доктóрят отрицательные контроли. Закрывается ограничением правки телом комментария, утверждением о диффе и прогоном правил, читающих оба файла |
| T-10-46-SC | Tampering | npm/pip/cargo installs | n/a | accept | ЗАМЕР: план не ставит ни одного пакета — правятся три документа записи, два тела комментариев и три модуля суиты. Гейт легитимности пакетов неприменим |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q` — зелено.
2. `uv run pytest tests/test_pages/test_prose_names_live_symbols.py -q` — зелено, отбор непуст.
3. `uv run pytest tests/test_templates/test_components.py tests/test_pages/test_confirm_delete_transport.py tests/test_pages/test_editor_schedules.py -q` — зелено.
4. `uv run python -m compileall -q app main.py tests` — код возврата ноль.
5. `git diff --numstat -- .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — пусто.
6. Поле состояния `10-UAT.md`, число объявленных проверок и число таблиц отметок совпадают с
   состоянием до правки; все три названы в сводке до и после.
7. Вердикты окон 68, 69, 70 и итог прогона закрывшего правила у каждого названы в сводке.
</verification>

<success_criteria>
- Повторная правка файла отложенного неотличима от первой, и это показано числом вхождений.
- Обе протухшие пометки заменены формой, которая протухает громко, и разрешение имён принуждается.
- Согласие прозы о числе планов с отметками стало машинным.
- Вердикты трёх окон поставлены по достижению истины, а не по завершению партии.
- Незакрытое человеком названо поимённо и осталось незакрытым.
- Поведение продукта не тронуто ни на строку.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-46-SUMMARY.md` when done
</output>
