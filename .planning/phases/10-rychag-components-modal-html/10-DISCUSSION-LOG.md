# Phase 10: Рычаг `components/modal.html` - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-02
**Phase:** 10-Рычаг `components/modal.html`
**Areas discussed:** Ответ сервера на 18 мест; Снятие панели и фокус; Судьба `hx_post` / `hx_include`; Инвентарь 16 против 18

---

## Ответ сервера на 18 мест

### Контракт ответа восьми непереведённых маршрутов

| Option | Description | Selected |
|--------|-------------|----------|
| По классу действия | Экран остаётся → фрагмент + OOB; иначе → `respond()` без фрагмента (204 + `HX-Location`) | ✓ |
| Минимальный единый | Все восемь → `HX-Location`; ноль новых шаблонов ответа, но критерий 2 остаётся без предмета | |
| Полный фрагментный | Все восемь → фрагмент + OOB; фактически FORM-03 внутри Фазы 10, ломает порядок вехи | |

**User's choice:** По классу действия
**Notes:** Решающий довод — риск «N мёртвых диалогов» иначе переехал бы в Фазу 11 сразу на пятнадцать мест, то есть Фаза 10 перестала бы отделять пилот от массовой работы.

### Как обработчик `/ads/{id}/delete` узнаёт контекст

| Option | Description | Selected |
|--------|-------------|----------|
| Скрытое поле в слоте | Блочный вызов панели; прецеденты `return_to=editor` и `search` живые | ✓ |
| Единый ответ на оба | Всегда `HX-Location` на `/ads`; список теряет позицию прокрутки | |
| Заголовок текущего адреса | Второй канал чтения htmx-состояния мимо слоя; без JS заголовка нет | |

**User's choice:** Скрытое поле в слоте
**Notes:** Макрос о контексте не узнаёт ничего — «одна правка» сохраняется.

### Плашка `?notice=`

| Option | Description | Selected |
|--------|-------------|----------|
| D-10 дословно | Фрагментный путь без плашки на успех; location-путь — коды, что есть сегодня | ✓ |
| Плашка на каждое подтверждённое | Реестр растёт на ~6 кодов; D-10 становится исключением | |
| Только отказы | Успех молчит везде, включая уже отгруженный `RETRY_QUEUED` | |

**User's choice:** D-10 дословно
**Notes:** Новых кодов реестра фаза не заводит.

### Ветка «список опустел»

| Option | Description | Selected |
|--------|-------------|----------|
| D-09 дословно | Ветвление по текущей выдаче; пустой список → `HX-Location` на тот же адрес | ✓ |
| Пустое состояние OOB-узлом | Вторая копия трёх различимых пустых состояний на каждый экран | |
| Подменять контейнер целиком | FORM-03 это прямо запрещает; теряется позиция прокрутки и курсор | |

**User's choice:** D-09 дословно

### Транспорт входа под пользователем

| Option | Description | Selected |
|--------|-------------|----------|
| `HX-Location`, как у всех | Прецедент `/impersonation/stop` из SIGN-03; cookie ставится на ответ 204 | ✓ |
| `HX-Redirect` | Третий выход `htmx.py` открывается раньше срока, отданного Фазе 11 | |
| Именованное исключение | Панель имперсонации остаётся на нативном POST | |

**User's choice:** `HX-Location`, как у всех
**Notes:** Ловушка переезда cookie на `Response(204)` названа и уходит в UAT.

### Перезапуск воркера и снятие задачи

| Option | Description | Selected |
|--------|-------------|----------|
| `HX-Location` на свой раздел | Гонки с тиком опроса нет по построению; ноль новых шаблонов | ✓ |
| Снятие задачи — фрагментом | Тик всё равно перерисует контейнер | |
| 204 без перехода | До 20 с тишины после подтверждённого необратимого действия | |

**User's choice:** `HX-Location` на свой раздел
**Notes:** Позже (в области «Инвентарь») решение пересматривалось и вернулось к этому же ответу — см. ниже.

### Шестой микро-контракт `?result=`

| Option | Description | Selected |
|--------|-------------|----------|
| Оставить, записать находку | `respond(redirect="/admin/queue?result=…")` работает без изменений; свод — Фаза 11 | ✓ |
| Свести в `?notice=` здесь же | Реестр, шаблон и чтение параметра правятся внутри фазы про разметку | |

**User's choice:** Оставить, записать находку

---

## Снятие панели и фокус

### Кто убирает панель

| Option | Description | Selected |
|--------|-------------|----------|
| Оба, но по путям | `hide()` там, где узел остаётся; OOB-снос там, где строка уезжает | ✓ |
| Только OOB-снос | На location-путях тела ответа нет (204) — узел не на чем принести | |
| Только `hide()` | Открыватель уезжает со строкой; критерий 2 остаётся без предмета | |

**User's choice:** Оба, но по путям

### Площадка приземления фокуса

| Option | Description | Selected |
|--------|-------------|----------|
| Область `#notice` | Единственный `id` на каждой странице обоих шеллов; на отказе — прямо на текст | ✓ |
| Параметр площадки у макроса | 18 вызовов получают по решению — форма, отвергнутая D-11 Фазы 9 | |
| Изъятие остаётся записанным | Шапка `modal.html` прямо назвала приземление предметом Фазы 10 | |

**User's choice:** Область `#notice`
**Notes:** Проверено: ориентира `<main>` в оболочке нет.

### Признак закрытия панели

| Option | Description | Selected |
|--------|-------------|----------|
| Только на успехе | `if ($event.detail.successful) hide()`; вперёд совместимо с 422 FORM-08 | ✓ |
| По завершению запроса | При 500 панель уходит, действие не выполнено — «оптимистичный UI» | |
| По коду ответа явно | Второе определение «успеха», расходящееся с `responseHandling` | |

**User's choice:** Только на успехе

### Прочтение критерия 3 роадмапа

| Option | Description | Selected |
|--------|-------------|----------|
| Прочтение по числам | Запрет на новые сущности, не на правку существующих; четыре числа под гейтом | ✓ |
| Буквально | Отменяет два предыдущих решения этой области | |
| Амендмент роадмапа | Переписывает формулировку критерия в ROADMAP.md | |

**User's choice:** Прочтение по числам
**Notes:** Записывается решением владельца по образцу D-04-A Фазы 9.

---

## Судьба `hx_post` / `hx_include`

### Параметр `hx_post`

| Option | Description | Selected |
|--------|-------------|----------|
| Снять параметр вовсе | Атрибуты безусловны; `CONDITIONAL_PLACES` 2 → 1 — слепая зона UAT-пункта 9 сокращается на рычаге | ✓ |
| Перевернуть умолчание на `true` | Условие Jinja внутри тега переживает фазу вместе со слепой зоной | |
| Оставить `false`, передавать в 18 | 11 правок в 10 файлах вместо одной; забытый вызов даёт молча неработающее место | |

**User's choice:** Снять параметр вовсе

### Курсор бесконечной прокрутки на `/accounts` и `/ads`

| Option | Description | Selected |
|--------|-------------|----------|
| Изъятие: эти два — location | Экран пересобирается GET-запросом, сервер считает курсор с нуля | ✓ |
| Перевести курсоры на keyset | Трогает `accounts`, поставленный роадмапом последним в Фазе 11 | |
| Дать `hx_include` потребителя | Возврат механизма, отвергнутого решением владельца `keyset` | |

**User's choice:** Изъятие: эти два — location
**Notes:** Найдено при разведке: оба списка листаются `?offset=`, фрагментное удаление воспроизводит CR-01 дословно.

### Параметр `hx_include`

| Option | Description | Selected |
|--------|-------------|----------|
| Снять вместе с `hx_post` | Ноль потребителей; основание «рычаг Фазы 10» перестало быть верным | ✓ |
| Оставить до Фазы 11 | Параметр без потребителя переживает вторую фазу подряд | |

**User's choice:** Снять вместе с `hx_post`

### Две копии свойств качества

| Option | Description | Selected |
|--------|-------------|----------|
| Копия с зубами | Гейт слицает набор свойств панели с тем, что печатает `form_wrapper` | ✓ |
| Панель рождается `form_wrapper` | Обёртка растёт параметрами ради одного потребителя; правка уходит в файл, раздающийся 46 формам | |
| Оставить как есть | Расхождение разойдётся молча и сразу на 18 местах | |

**User's choice:** Копия с зубами

---

## Инвентарь: 16 против 18

### Какое число фаза объявляет своим

| Option | Description | Selected |
|--------|-------------|----------|
| 18 измеренных, записи правятся | Числа гейта получены счётом по файлам; ROADMAP и REQUIREMENTS правятся с летописью | ✓ |
| 16 остаётся, 18 — второй счёт | Вызовов `modal()` в исходнике 11, а не 16 — числу 16 не соответствует ни одно множество | |
| Отложить в Фазу 15 | Верификатор будет сверять с критерием 1 и вернёт расхождение пробелом | |

**User's choice:** 18 измеренных, записи правятся

### Бухгалтерия переведённых обработчиков

| Option | Description | Selected |
|--------|-------------|----------|
| Два счёта разных множеств | `NOT_YET_CONVERTED_COUNT` 34 → 26 плюс новый счёт «отдаёт фрагмент» | ✓ |
| Один счёт, числа Фазы 11 правятся | Фаза 10 переписывает чужие критерии успеха до начала той фазы | |
| Снять только фрагментные | Обработчик на `respond()`, числящийся непереведённым, красит G-1/G-2 | |

**User's choice:** Два счёта разных множеств

### Ширина фрагментного пути

| Option (первый заход) | Description | Selected |
|--------|-------------|----------|
| Вернуть снятие задачи | У `/admin/queue` бесконечной прокрутки нет — курсорного дефекта не возникает | ✓ (отменено) |
| Принять одно место | Фрагментный путь — только карточка расписания в редакторе | |
| Плюс курсор `/ads` на keyset | Три новых фрагментных места | |

**Notes:** Выбор отменён немедленно новым измерением: строка очереди `id` не несёт, а ключ панели `queue-drop-{account}-{index}` собран из ПОЗИЦИИ строки, которая сдвигается при снятии соседней задачи. Перевести ключ на `task_id` запрещает отгруженное решение безопасности WR-04 (ключ становится именем атрибута). Присвоение `id` строкам `admin/includes/*` роадмап назначил Фазе 11. Попутно измерено: `/admin/queue` не опрашивается вовсе — опрос есть только у `admin/workers.html`.

| Option (после поправки) | Description | Selected |
|--------|-------------|----------|
| Принять одно место | Карточка расписания в редакторе: `id="sched-{id}"` и ключ `sched-del-{id}` — первичные ключи | ✓ |
| Перевести `/ads` на keyset | Фаза берёт правку курсора в разделе, отданном Фазе 11 | |
| Завести `id` строкам очереди | Три файла `admin/includes/*` назначены Фазе 11; ключ панели — решение безопасности | |

**User's choice:** Принять одно место

---

## Claude's Discretion

- Форма покрытия новой ветви `hide()` и приземления фокуса (исполняющие тесты `x-data` по образцу плана 09-17 против разметочного правила).
- Имя и точная форма гейта сличения свойств качества панели и `form_wrapper`.
- Имена скрытых полей контекста и их значения.
- Состав побочных внеполосных областей ответа удаления расписания (линейка `sched_count_label`).
- Как `#notice` получает `tabindex="-1"` — атрибутом в шаблоне или установкой из макроса.
- Имя второго счёта («отдаёт фрагмент») и файл, в котором он живёт.
- Нарезка фазы на планы и порядок волн; рычаг обязан идти первым.

## Deferred Ideas

- Перевод курсоров `/accounts/partial` и `/ads/partial` на ключ последней строки — Фаза 11.
- `id` строкам `admin/includes/*` и непозиционный способ пережить сдвиг у панелей очереди — Фаза 11.
- Свод `?result=` в `?notice=` — Фаза 11, раздел `admin`.
- Приземление фокуса на location-пути — Фаза 15 (GATE-09).
- Перевод формы панели на `form_wrapper` — кандидат к возврату в Фазе 11.
- `hx-push-url` по конвенции трёх случаев — QUAL-04, Фаза 15.
- Закрытие UAT-пункта 9 целиком — Фаза 15 (последнее место принадлежит Фазе 12).
- Возврат `hx_include` решением Фазы 11, если фрагментным ответам понадобится живое состояние документа.
