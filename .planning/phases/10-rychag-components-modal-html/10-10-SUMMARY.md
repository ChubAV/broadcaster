---
phase: 10-rychag-components-modal-html
plan: 10
subsystem: testing
tags: [uat, manual-walkthrough, htmx, alpine, oob-swap, modal, focus-trap]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "10-VERIFICATION.md с behavior_unverified: 1 и перечнем human_verification из шести пунктов"
  - phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
    provides: "09-UAT.md — форма обхода: стенд с префиксом, уборка последним шагом, таблицы «# / Шаг / Ожидаемый признак», ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ, пустые отметки"
provides:
  - "10-UAT.md — ручной обход фазы целиком: стенд, уборка, шесть проверок, шесть пустых таблиц отметок, перечень закрытого машинно, границы обхода и маршрутизация"
  - "Проверяемая по шагам процедура критерия 2 роадмапа: четыре числа, ловушка фокуса, линейка счётчика, переходная ветка D-04"
  - "Исполненные и проверенные на живой базе команды посева и уборки стенда с префиксом UAT10-"
affects: [11-alpine-posle-swapa, 15-gate-09-svodnyy-obhod, приёмка Фазы 10, повторная верификация Фазы 10]

actuals:
  tokens: 9297
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Обход как машинно удерживаемый артефакт: полнота держится гейтом присутствия несущих маркеров, а не вычиткой"
    - "Развилка посева с ровно одним маркером ветви, удерживаемая тем же гейтом"
    - "Граница доказанного против каждого машинного правила в разделе «Что закрыто МАШИННО»"

key-files:
  created:
    - .planning/phases/10-rychag-components-modal-html/10-UAT.md
  modified: []

key-decisions:
  - "Ожидаемое число диалогов на экране обхода названо ИЗМЕРЕННЫМ, а не выведенным из намерения: редактор рисует панель удаления самого объявления (ad-del-) сверх панелей расписаний, поэтому диалогов N+1, а несущий счёт — [id^=\"sched-del-\"]"
  - "Способ вызова отказа для проверки 4 дан ДВУМЯ путями с названной разницей; путь через негодную ВЕЛИЧИНУ ad_id запрещён явно — его закрыл план 10-08"
  - "Предупреждение о пустой области #notice поставлено ДО таблицы шагов проверки 5, а не после: иначе человек записал бы расхождение там, где его нет"
  - "Стенд посеян и убран ИСПОЛНЕНИЕМ на живой базе; ветвь развилки — ПОСЕВ ИСПОЛНЕН"
  - ".planning/WINDOWS.md планом не тронут: журнал в этой волне принадлежит плану 10-09, команда ведения перезаписывает файл целиком"

patterns-established:
  - "Граница доказанного: у каждого машинного правила в перечне названо не только имя, но и место, где доказательство кончается"
  - "Ожидаемый ответ, названный ЗАРАНЕЕ по каждой снимаемой величине — проверка без него закрывается любым наблюдением"

requirements-completed: []

coverage:
  - id: D1
    description: "Артефакт ручного обхода 10-UAT.md заведён и ПОЛОН: шесть проверок, шесть таблиц отметок, стенд, уборка, границы, маршрутизация"
    requirement: FORM-06
    verification:
      - kind: other
        ref: "uv run python -c ... (гейт полноты задачи 1: ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ, sched-del-, sched-count, UAT10-, FORM-06 + ровно один маркер ветви посева)"
        status: pass
      - kind: other
        ref: "uv run python -c ... (гейт полноты задачи 2: Slow 3G, is-modal-open, notice-alert, DEF-09-02, окна 22/28/35/36, «Что закрыто МАШИННО»)"
        status: pass
      - kind: other
        ref: "uv run python -c ... (гейт полноты задачи 3: четыре завершающих раздела, оговорка отметок, окно 34)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Стенд обхода приводится и убирается ИСПОЛНЕННЫМИ командами; посев не переживает план"
    verification:
      - kind: manual_procedural
        ref: "исполнено исполнителем: seeded: … ad_id=45 … → removed: schedules=3 ads=1 accounts=1 → UAT10- rows: schedules=0 ads=0 accounts=0"
        status: pass
    human_judgment: false
  - id: D3
    description: "Критерий 2 роадмапа — фактическое снятие осиротевшей панели рантаймом после N удалений"
    requirement: FORM-06
    verification: []
    human_judgment: true
    rationale: "Внеполосные узлы снятия применяет рантайм разметки, которого суита не поднимает; httpx свопов не делает. Артефакт делает пункт ПРОХОДИМЫМ, но не закрывает его — закрывает человек на приёмке (verification: backstop)"
  - id: D4
    description: "Пять остальных пунктов human_verification: follow-up GET, гейт ухода панели, панель на отказе, приземление фокуса, документная половина DEF-09-02"
    verification: []
    human_judgment: true
    rationale: "Требуют живого рантайма, объекта клиентского состояния, таблицы стилей и чтения глазами; ни один машинно не закрывается и закрыться не может"
  - id: D5
    description: "Обход проходим по шагам без чтения отчёта верификации и исходников (human-check задачи 3)"
    verification: []
    human_judgment: true
    rationale: "Читабельность процедуры — суждение читателя, а не вердикт правила; harvested в end-of-phase UAT по режиму workflow.human_verify_mode"

duration: 17 min
completed: 2026-09-03
status: complete
---

# Phase 10 Plan 10: Ручной обход фазы `10-UAT.md` Summary

**Вторая половина цели фазы получила ПРОХОДИМЫЙ артефакт: шесть проверок с приведённым стендом,
снимаемыми числами, названными заранее ожидаемыми ответами и пустыми таблицами отметок — вместо
строки отчёта «не проверено ничем».**

## Performance

- **Duration:** 17 мин
- **Started:** 2026-09-03T19:52:00Z (приблизительно; первый коммит — 20:03:36Z)
- **Completed:** 2026-09-03T20:09:15Z
- **Tasks:** 3
- **Files modified:** 1 создан (537 строк, 37 189 символов)

## Accomplishments

- Заведён `10-UAT.md` — обход фазы целиком: стенд, уборка, ШЕСТЬ проверок, шесть пустых таблиц
  отметок, перечень закрытого машинно, границы обхода и маршрутизация окон.
- Критерий 2 роадмапа переведён из «не проверено ничем» в «проверяемо по шагам»: проверка 1 несёт
  **двенадцать** шагов, снимает четыре числа, ловушку фокуса, линейку счётчика и переходную ветку.
- Стенд ПРИВЕДЁН, А НЕ ОПИСАН: команды посева и уборки исполнены на живой базе, посев не пережил
  план.
- Полнота артефакта удерживается **тремя машинными гейтами**, а не вычиткой; единственность ветви
  развилки посева — тем же гейтом.

## Task Commits

1. **Задача 1: сквозной срез обхода — стенд, уборка и проверка критерия 2** — `25854aa` (docs, tracer)
2. **Задача 2: пять остальных пунктов ручной верификации и граница доказанного** — `fc02636` (docs)
3. **Задача 3: границы обхода, маршрутизация окон и основание артефакта** — `a2a5098` (docs)

## Files Created/Modified

- `.planning/phases/10-rychag-components-modal-html/10-UAT.md` — ручной обход Фазы 10 (создан)

---

## 1. Проверка доступности живой базы — вывод ДОСЛОВНО и выбранная ветвь

Команда `<precondition>` задачи 1 (счёт пользователей) снята ПЕРВОЙ, до любой другой работы:

```
users: 4
```

Код возврата — `0`. **Выбрана ветвь `ПОСЕВ ИСПОЛНЕН`.** Ветвь выбрана ЭТИМ выводом, а не удобством
исполнителя: при ненулевом коде возврата план был бы обязан записать вторую ветвь с дословным текстом
отказа и передать факт оркестратору окном по форме записи 34. Второй ветви не потребовалось, окна из
развилки нет.

## 2. Команды посева и уборки — выводы ДОСЛОВНО

**Посев** (одна строка: почта владельца, идентификатор объявления, адрес экрана обхода):

```
seeded: user=chub.aleksandr.v@gmail.com ad_id=45 account_id=54 schedules=3 url=/ads/45/edit
```

**Уборка:**

```
removed: schedules=3 ads=1 accounts=1
```

**Проверочная команда уборки** (ноль строк с префиксом по каждой из трёх сущностей):

```
UAT10- rows: schedules=0 ads=0 accounts=0
```

⚠️ **Стенд после плана в базе НЕ ОСТАЛСЯ** — три нуля сняты ПОСЛЕ уборки. Человеку на приёмке сказано
в самом артефакте: первый прогон команды посева уже был, отлаживать её не придётся.

## 3. Перечень путей собственных коммитов плана — ОБА вывода

**С ограничением тремя документами планирования** (`git log -F --grep='(10-10)' … --name-only --
.planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/WINDOWS.md | sort -u`):

```
(пусто)
```

**АНТИВАКУУМ — тот же перечень БЕЗ ограничения путями:**

```
.planning/phases/10-rychag-components-modal-html/10-UAT.md
```

⚠️ **УТВЕРЖДЕНИЕ СДЕЛАНО ОБ АВТОРСТВЕ, А НЕ О СОСТОЯНИИ РАБОЧЕГО ДЕРЕВА.** План 10-09 идёт В ТОЙ ЖЕ
ВОЛНЕ и ЗАКОННО дописывает в `.planning/WINDOWS.md` свои окна; проверка по `git diff` краснела бы
ровно тогда, когда сиблинг сделал свою верную работу. Перечень снят ДО учётного коммита оркестратора
— после него в выводе законно появится `.planning/ROADMAP.md` (простановка отметки плана
исполненным, а не правка плана).

Дополнительно: `git diff --name-only -- app/ tests/` — пусто (кодовая часть дерева не тронута);
`git diff --name-only -- .planning/phases/09-…/` — пусто (прежний обход Фазы 9 читался, но не
правился).

## 4. Соответствие «проверка обхода → пункт `human_verification`» — ОДИН К ОДНОМУ

Пунктов в отчёте **шесть**, проверок в обходе **шесть**. Ни один пункт не растворён в соседнем и ни
один не потерян.

| Проверка обхода | Пункт `human_verification` `10-VERIFICATION.md` |
|---|---|
| 1 — Осиротевшая панель снята после N удалений | 1 — счёт `[role="dialog"]` и `[id^="sched-del-"]` после N удалений (критерий 2, ручной UAT пункт 4) |
| 2 — Уборка панели при не доехавшем follow-up GET | 2 — Slow 3G → 204 + `HX-Location` → Offline сразу после нажатия |
| 3 — Гейт ухода панели на живом стенде | 3 — Esc, оверлей и «Отмена» на медленной сети, не дожидаясь ответа (`DEF-09-02`) |
| 4 — Панель остаётся открытой на отказе | 4 — вызвать отказ на маршруте за панелью и наблюдать панель (половина D-12) |
| 5 — Приземление фокуса | 5 — `document.activeElement` после ухода панели на обоих путях |
| 6 — Документная половина закрытия `DEF-09-02` | 6 — прочитать глазами запись в требованиях, обе половины окна 15 и текст окна 34 |

## 5. Соответствие «проверка → окно»

| Проверка | Окно `.planning/WINDOWS.md` | Предмет окна |
|---|---|---|
| 2 | **28** | Панель поверх устаревшего экрана при не доехавшем follow-up GET — правкой 10-05 адресовано, машинно не закрыто |
| 3 | **35** | Наблюдение гейта ухода панели НА ЖИВОМ СТЕНДЕ (план 10-07) |
| 4 | **22** | Два собственных наблюдения плана 10-02: приземление фокуса и панель на отказе |
| 6 | **36** | Документная половина закрытия `DEF-09-02` — правила, стерегущего формулировки, нет |

⚠️ Проверки **1** и **5** своего окна не закрывают: их предмет уезжает в повторную верификацию фазы.
⚠️ **Ни одно окно этим планом НЕ ЗАКРЫТО и не тронуто:** окна закрываются ПОСЛЕ обхода, человеком, и
командой ведения журнала, а не ручной правкой строки.

## 6. Величины, снимаемые проверкой критерия 2, и ожидаемый ответ по каждой

| Величина | Как снимается | ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ |
|---|---|---|
| диалоги ДО удалений | `document.querySelectorAll('[role="dialog"]').length` | `4` — три панели расписаний + одна панель удаления объявления |
| панели расписаний ДО удалений | `document.querySelectorAll('[id^="sched-del-"]').length` | `3` |
| диалоги ПОСЛЕ N удалений | то же выражение | `4 − N` |
| панели расписаний ПОСЛЕ N удалений | то же выражение | `3 − N` |
| оба узла удалённой строки ПОИМЁННО | `getElementById('sched-{N}')` и `getElementById('sched-del-{N}')` | `null` и `null` — один `null` из двух закрытием не считается |
| ловушка фокуса | 15-20 нажатий Tab со слежением за `document.activeElement` | обход по документу свободный, в невидимый диалог не проваливается |
| линейка счётчика | число в `#sched-count` против числа видимых карточек | сошлось (`2`, затем `1`) |
| переходная ветка (D-04) | `window.__uat10` после удаления последнего расписания | `'жив'` — документ не перезагружался, переход прошёл ветвью `HX-Location`; на пришедшем экране `1` и `0` |

⚠️ **ЧИСЛО ДИАЛОГОВ ИЗМЕРЕНО, А НЕ ВЫВЕДЕНО ИЗ НАМЕРЕНИЯ.** Редактор рисует панель удаления САМОГО
объявления (`ads/form.html:297`, `modal(id='ad-del-' ~ ad.id …)` внутри `{%- if ad %}`) сверх панелей
расписаний. Обход, назвавший бы ожидаемым числом «три», обесценил бы себя первым же несовпадением —
ровно тот отказ, которого обход Фазы 9 избегал пересъёмом признаков по отгруженному состоянию.

## 7. Предупреждение о пустой области уведомлений — ДОСЛОВНО

Стои́т **ДО** таблицы шагов проверки 5:

> ⚠️ **ПРОЧИТАТЬ ДО ШАГОВ — ИНАЧЕ ЧЕЛОВЕК ЗАПИШЕТ РАСХОЖДЕНИЕ ТАМ, ГДЕ ЕГО НЕТ.** Площадка
> приземления `#notice` НА ФРАГМЕНТНОМ ПУТИ — том самом, ДЛЯ КОТОРОГО она и выбиралась, — **ПУСТА
> ВСЕГДА**: плашки на успех не выдаётся вовсе (D-03), и ни один из двух фрагментных ответов фазы кода
> уведомления не несёт. Поэтому ожидаемый ответ здесь звучит так: **фокус приезжает в осмысленное
> МЕСТО, но НЕ в осмысленный ТЕКСТ, и второго варианта у этого пути нет.** Пустая область — это НЕ
> расхождение. Записи варианта `error` едут в СОСЕДНЮЮ область `#notice-alert`, у которой признака
> программной фокусируемости нет намеренно, поэтому текстом отказа площадка не станет ни на одном
> сегодняшнем маршруте. Основание — `10-REVIEW.md` §WR-02, три измерения; действующая запись — шапка
> `app/templates/includes/notice_area.html` после правки плана **10-09** (прежнее обещание
> окупаемости на отказном пути помечено ОПРОВЕРГНУТЫМ, а не вычеркнуто).

## 8. Раздел «Что закрыто МАШИННО» — правила с границей доказанного

Названо **восемь** правил (требовалось не менее шести), у каждого — где доказательство кончается:

| Правило | ГРАНИЦА доказанного |
|---|---|
| `test_modal_site_inventory`, `test_modal_guard_is_inherited_by_every_consumer` (инвентарь мест и три счёта: 11 / 9 / 18) | Макрос печатает атрибуты безусловно, мест ровно 18. НЕ доказано, что рантайм их применил |
| `test_every_confirmed_delete_route_answers_both_transports`, `test_every_such_form_keeps_its_method_and_action`, `test_the_post_attribute_matches_the_action_character_for_character` | Доказан ОТВЕТ СЕРВЕРА обеим половинам пары транспортов. НЕ доказано, что браузер по нему сделал |
| `test_the_orphaned_panel_is_removed_by_its_own_oob_node` | Доказано ПРИСУТСТВИЕ обоих внеполосных узлов в теле ответа. Снятие рантаймом не доказано ничем — это проверка 1 |
| структурная предпосылка (`sched_card.html:112` против `:274`) | Панель стои́т СНАРУЖИ карточки, то есть узел снятия НУЖЕН. Что он срабатывает — предмет проверки 1 |
| ребро `idempotency`, план **10-06** (неотличимость повтора) | Доказано настоящими запросами и сличением множеств. **Обход этого НЕ ПЕРЕПРОВЕРЯЕТ** — глаз не прибавляет ничего |
| пять правил гейта ухода панели, план **10-07** (ребро `concurrency`) | Свойство доказано в интерпретаторе плюс мутацией. НЕ доказано, что человек видит панель ЗАНЯТОЙ, а не сломанной — это проверка 3 |
| `test_criterion_three_holds_by_the_numbers` плюс пустой `git diff -- app/static/js/` | Критерий 3 доказан полностью; глазом не проверяется и в обход не входит |
| `test_the_scroll_lock_never_survives_the_teardown_of_the_panel` и три соседних | Доказано ПОВЕДЕНЧЕСКИ, но в интерпретаторе со стаб-документом. Наблюдение в браузере — шаги 2.5-2.6 |

## 9. Выводы всех трёх гейтов полноты

| Гейт | Задача | Код возврата | Вывод |
|---|---|---|---|
| маркеры + единственность ветви развилки | 1 | `0` | пусто (все пять маркеров на месте; маркеров ветви посева — ровно **один**) |
| маркеры проверок 2-6 и раздела закрытого машинно | 2 | `0` | пусто (`Slow 3G`, `is-modal-open`, `notice-alert`, `DEF-09-02`, окна 22/28/35/36, «Что закрыто МАШИННО») |
| маркеры завершающих разделов | 3 | `0` | пусто (три раздела, оговорка отметок, `окно 34`) |

Все три сняты повторно на итоговом состоянии файла — все три зелены.

## 10. Таблицы отметок — ПУСТЫ, `FORM-06` не переводился, окна не закрывались

- Заголовков «Отметка о закрытии» — **6**; строк-заготовок вида `|  |  | …` — **6**; оговорок
  «заполняет человек на приёмке» — **6**. **Ни одна ячейка не заполнена.**
- `status: testing` во frontmatter, значение «пройдено» не ставилось, блока прохождения (дата,
  браузер, ОС, стенд, ответы) в файле НЕТ — его заводит человек, когда обход состоится.
- **`FORM-06` этим планом в `Complete` НЕ переведён**, отметка `Pending` в
  `.planning/REQUIREMENTS.md` не тронута. Основание: вторая половина требования закрывается ГЛАЗОМ, и
  исполнители прежних планов, оставившие отметку ожидания, поступили верно. Шаг
  `requirements.mark-complete` этим планом сознательно НЕ выполнялся.
- **Ни одно окно не закрыто и не тронуто:** `.planning/WINDOWS.md` планом не правился (см. §3).

## Decisions Made

1. **Ожидаемое число диалогов названо измеренным.** Обход, написанный по намерению, называет человеку
   то, чего он может не увидеть, и первым же несовпадением обесценивает себя целиком (записанный
   опыт Фазы 9). Поэтому у проверки 1 два счёта, и несущий — `[id^="sched-del-"]`.
2. **Способ вызова отказа дан двумя путями с названной разницей.** Обрыв транспорта (обязателен,
   унаследован у проверки 6.3 Фазы 9) и настоящий 500 временной правкой дерева с обязательным
   откатом. Оба поднимают ОДНУ ветвь предиката закрытия.
3. **Путь через негодную ВЕЛИЧИНУ `ad_id` запрещён явно.** Он давал пятисотку (`CR-01`), но её
   закрыл план **10-08** границей величины; обход, зовущий отказ этим способом, не дождался бы его.
4. **`.planning/WINDOWS.md` планом не правился.** Журнал в этой волне принадлежит плану 10-09,
   команда ведения перезаписывает файл целиком, и второй одновременный дописыватель потерял бы чужую
   строку. Тот же приём фаза уже применяет к невыполнимому `graphify update .` (записи 23 и 25).

## Deviations from Plan

None — план исполнен ровно как написан. Развилка посева разрешилась первой ветвью (живая база
доступна), поэтому вторая ветвь и передаваемое ею окно не потребовались.

## Issues Encountered

Ни одной. Обе команды стенда отработали с первого раза; все три гейта полноты зелены с первого
прогона.

## Известные незакрытые пункты — ПЕРЕДАЮТСЯ ПРИЁМКЕ ФАЗЫ

⚠️ **Этот план НЕ ЗАКРЫЛ и не мог закрыть ни одного поведенческого пункта.** Он сделал их
проходимыми. До прохождения обхода человеком открытыми остаются:

1. **Критерий 2 роадмапа** — фактическое снятие осиротевшей панели рантаймом (`behavior_unverified: 1`).
2. **Все ШЕСТЬ пунктов `human_verification`** `10-VERIFICATION.md` — по одному на каждую проверку
   обхода.
3. **Окна 22, 28, 35, 36** `.planning/WINDOWS.md` — закрываются прохождением обхода и командой ведения
   журнала.
4. **`FORM-06`** — переводится в завершённое только после прохождения обхода И повторной верификации.

Рантайм разметки, объект клиентского состояния и таблица стилей поднимаются только браузером;
машинной подмены не заведено ни в каком виде, и это решение, а не пропуск.

## Передаётся ОРКЕСТРАТОРУ

**1. Невыполненный `graphify update .`** — граф `graphify-out/` живёт только в основном рабочем
каталоге и в git не отслеживается (записи **23** и **25** `.planning/WINDOWS.md`), в
`.planning/config.json` стои́т `graphify.enabled: false`. Выполнить в основном каталоге после слияния
волны.

**2. Кандидат в журнал битых окон — план НЕ ЗАПИСЫВАЛ его сам** (журнал в этой волне принадлежит
плану 10-09; текст передаётся, как это фаза уже делала с записями 23 и 25):

> `kind: unrun-verify`, фаза 10, файл
> `.planning/phases/10-rychag-components-modal-html/10-UAT.md`. Ручной обход Фазы 10 создан, отметки
> НЕ заполнены: вторая половина цели фазы (критерий 2) и все шесть пунктов `human_verification`
> `10-VERIFICATION.md` закрываются глазами на приёмке фазы. Стенд посева `UAT10-` исполнен и убран
> исполнителем плана 10-10 (`UAT10- rows: schedules=0 ads=0 accounts=0`), команды проверены
> ИСПОЛНЕНИЕМ, а не выписаны по памяти. Запись закрывает человек, прошедший обход, — тот же класс,
> что запись 11 (обход Фазы 7).

## User Setup Required

None — внешней настройки план не требует. Живая локальная база у исполнителя была (`users: 4`).

## Next Phase Readiness

- Обход готов к прохождению: стенд приводится одной командой, проверенной исполнением; шесть проверок
  идут по шагам без чтения отчёта верификации и исходников.
- **Блокер фазы `CR-01` закрывает план 10-08, документный дрейф — план 10-11**; этот план ни того, ни
  другого не касается.
- Повторная верификация фазы обязана читать критерий 2 и шесть пунктов ручной верификации как
  ОТКРЫТЫЕ до заполнения таблиц отметок человеком.

---
*Фаза: 10-rychag-components-modal-html*
*Завершён: 2026-09-03*

## Self-Check: PASSED

- `10-UAT.md` — на диске.
- `10-10-SUMMARY.md` — на диске.
- Коммиты задач `25854aa`, `fc02636`, `a2a5098` — в истории.
- Коммит сводки — HEAD ветки на момент записи (хеш собственного коммита в его же теле
  не приводится: он не существует до момента создания).
