---
phase: 10-rychag-components-modal-html
plan: 25
subsystem: security
tags: [csrf, origin-guard, fastapi, starlette, ast-gate, docstring-retraction]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "гейт полноты гарда происхождения на маршрутах подтверждённого удаления (план 10-18) и его объявленные числа"
  - phase: 10-rychag-components-modal-html
    provides: "идиома «опровергнутое называется, а не стирается» (D-30/D-32) и форма объявленного числа с летописью"
provides:
  - "`is_same_origin` не признаёт совпадением источников пару ОТСУТСТВИЙ имени узла (`IN-01`)"
  - "обоснование гейта подтверждённого удаления стои́т на замеренном факте о собственном продукте, прежняя формулировка помечена опровергнутой (`WR-03`)"
  - "граница вселенных обоих гейтов полноты названа ТРЕМЯ замеренными числами и стережётся правилом с антивакуумом (`WR-05`)"
  - "одна датированная запись об одном опровергнутом перечне потребителей вместо трёх перекрывавшихся абзацев (`IN-03`)"
  - "именованная константа `ORIGIN_CHECK_BOUNDARY` — канон истинной границы сверки, на который сошлётся план 10-32 (`WR-02`)"
affects: [10-32, 11, security-review, verify-work]

actuals:
  tokens: 12766
  tasks: 3
  commits: 6

tech-stack:
  added: []
  patterns:
    - "каноническое утверждение, живущее ИМЕНОВАННОЙ константой модуля, а не абзацем докстринга: площадка вызова ссылается на имя вместо копии текста"
    - "правило границы, собирающее ЖАЛОБЫ списком вместо падения первым assert — так антивакуум выразим отдельным контролем"

key-files:
  created: []
  modified:
    - app/pages/common.py
    - tests/test_pages/test_origin_guard_on_destructive_routes.py

key-decisions:
  - "Объявлено ТРИ числа границы вселенных, а не два: 36 POST-обработчиков, 13 мест вызова гарда, 10 внутри вселенных обоих гейтов. Третье добавлено потому, что отчёт ревизии назвал «13 из 36» ПОКРЫТИЕМ вселенных, а замер даёт 10 — три маршрута несут гард, не будучи стережимы ничем"
  - "Три абзаца сведены в ДВА, а не в один: «РАМКИ» есть рамка (публичное имя, снятая приватная копия, два гейта), а не запись об опровергнутом; запись об опровергнутом перечне после сведения ровно ОДНА, как и требует предмет"
  - "Полный прогон `tests/test_pages/` выполнен БЕЗ снятия узла, которое предписывал план: сосед 10-24 завершён до старта, переходного состояния реестра расхождения не существовало"
  - "Ни одно из двух имён гейтов не получило второго вхождения в докстринге гарда — ограничение соблюдено, хотя стерегущей его проверки в дереве нет (заведено окно реестра)"

patterns-established:
  - "Отрицательный контроль к сужающей правке: правило «предикат отвергает X» ставится рядом с правилами «предикат пропускает Y, Z», иначе зелёное получил бы и предикат, отвергающий всё"
  - "Запись о числе несёт ЛЕТОПИСЬ: чем измерено, когда, каким планом и что означает молчаливый рост либо молчаливое похудение"

requirements-completed: [FORM-06]

coverage:
  - id: D1
    description: "`is_same_origin` не складывает два отсутствия имени узла в совпадение источников: `Origin: null` при безголовом запросе отвергается, три прежние ветви ведут себя как прежде"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_origin_guard_on_destructive_routes.py#test_a_hostless_origin_is_not_the_same_origin_as_a_hostless_request"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_origin_guard_on_destructive_routes.py#test_control_a_matching_origin_host_is_the_same_origin"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_origin_guard_on_destructive_routes.py#test_control_a_foreign_origin_host_is_refused"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_origin_guard_on_destructive_routes.py#test_control_a_request_with_neither_header_still_passes"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_origin_guard_on_destructive_routes.py#test_control_the_fetch_site_branch_outranks_the_origin_branch"
        status: pass
    human_judgment: false
  - id: D2
    description: "Граница вселенных обоих гейтов полноты названа тремя замеренными числами и стережётся правилом, падающим и на пустом измеренном множестве"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_origin_guard_on_destructive_routes.py#test_the_boundary_of_both_gate_universes_is_declared_by_number"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_origin_guard_on_destructive_routes.py#test_control_the_universe_boundary_rule_reddens_on_an_empty_measure"
        status: pass
      - kind: integration
        ref: "uv run pytest tests/test_pages/test_htmx_gates.py -q -k 'post_handlers or handlers' (независимая мера того же множества)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Обоснование гейта подтверждённого удаления стои́т на замеренном факте: признак `samesite=\"lax\"` выставлен продуктом явно в `_session_cookie_attrs`, довод гарда переформулирован через глубину, прежняя формулировка помечена опровергнутой"
    requirement: "FORM-06"
    verification:
      - kind: other
        ref: "grep -c '_session_cookie_attrs' tests/test_pages/test_origin_guard_on_destructive_routes.py -> 1 (критерий: не меньше 1)"
        status: pass
      - kind: other
        ref: "git diff -U0 -- tests/test_pages/test_origin_guard_on_destructive_routes.py: удалено ровно 7 строк, все семь — прежний абзац, восстановленный дословно в записи об опровергнутом"
        status: pass
    human_judgment: true
    rationale: "Истинность переписанного факта о собственном продукте машинно не утверждается: правило могло бы проверить наличие подстроки, но не то, что предложение перестало лгать. Замер, на котором факт стои́т, приведён ниже дословно; читателем должен быть человек"
  - id: D4
    description: "У одного опровергнутого перечня потребителей ОДНА датированная запись; предметы не потеряны; истинная граница сверки объявлена именем `ORIGIN_CHECK_BOUNDARY`"
    requirement: "FORM-06"
    verification:
      - kind: other
        ref: "uv run python -c \"import app.pages.common as m; names=[n for n in vars(m) if 'ORIGIN' in n.upper() and isinstance(getattr(m,n),str)]\" -> ['ORIGIN_CHECK_BOUNDARY']"
        status: pass
      - kind: other
        ref: "замер докстринга: 137 -> 134 строки, 14 -> 13 абзацев, 11 -> 11 названных предметов"
        status: pass
    human_judgment: true
    rationale: "Сохранность ПРЕДМЕТА записи при сведении проверена подстрочным замером одиннадцати маркеров — это доказывает присутствие текста, а не то, что смысл записи уцелел. Отличие сведения от стирания читает человек"

duration: 44 min
completed: 2026-09-08
status: complete
---

# Phase 10 Plan 25: предикат происхождения перестаёт утверждать больше, чем исполняет — Summary

**`is_same_origin` больше не признаёт совпадением пару ОТСУТСТВИЙ имени узла; обоснование гейта переписано с опровергнутого факта о `samesite` на замеренный; граница вселенных обоих гейтов полноты названа тремя измеренными числами (36 / 13 / 10) со стерегущим правилом; три перекрывавшихся абзаца об одном снятом перечне сведены в одну датированную запись, а истинная граница сверки заведена именованной константой `ORIGIN_CHECK_BOUNDARY`.**

## Performance

- **Duration:** 44 min (из них 31 — полный прогон `tests/test_pages/`)
- **Started:** 2026-09-08T07:52:00Z (замер по дереву до первой правки)
- **Completed:** 2026-09-08T08:36:00Z
- **Tasks:** 3
- **Files modified:** 2 боевых (`app/pages/common.py`, `tests/test_pages/test_origin_guard_on_destructive_routes.py`) + 2 планировочных (`deferred-items.md`, `WINDOWS.md`)

## Accomplishments

- **`IN-01` закрыт сужением ветви заголовка источника.** Разобранное имя узла источника сравнивается с именем узла запроса ТОЛЬКО после явной проверки наличия. Тело предиката осталось ВОСЕМЬЮ строками — правка есть уточнение сравнения, а не второй механизм.
- **`WR-03` закрыт заменой факта при сохранении вывода.** Несущее предложение гейта переписано: признак `samesite="lax"` ВЫСТАВЛЕН продуктом явно и в одном месте (`app/pages/auth.py`, `_session_cookie_attrs`), а довод гарда есть ГЛУБИНА — одна политика снимается сменой набора атрибутов в одной строке и не различает одноимённый источник иной схемы или иного порта.
- **`WR-05` закрыт ЧИСЛОМ вместо слова «полнота».** Заведено правило `test_the_boundary_of_both_gate_universes_is_declared_by_number` с антивакуумом и отрицательным контролем; классы 23 непокрытых маршрутов названы поимённо; записано прямо, что расширение вселенной принадлежит владельцу вехи и фаза-сниматель НЕ НАЗНАЧЕНА.
- **`IN-03` закрыт сведением, а не стиранием.** Три абзаца об одном снятом перечне сведены в рамку + ОДНУ датированную запись; все одиннадцать названных предметов сохранены (замер подстрокой), докстринг 137 → 134 строки.
- **Канон для `WR-02` заведён РАНЬШЕ площадок.** `ORIGIN_CHECK_BOUNDARY` — именованная константа уровня модуля, называющая функцию разбора cookie и ОБА её обращения к базе. План 10-32 переведёт четыре площадки на ссылку.

## Task Commits

1. **Задача 1 (RED): правило безголового сравнения и три отрицательных контроля** — `e2e34fa` (test)
2. **Задача 1 (GREEN): явная проверка наличия имени узла** — `62ecbfd` (fix)
3. **Задача 2: обоснование гейта на замере, граница вселенных числом** — `fc7f34f` (docs)
4. **Задача 3: одна датированная запись и канон границы сверки** — `f6885a8` (docs)
5. **Отложенное вне границы плана** — `db4f96e` (docs)
6. **Два окна реестра** — `77d3623` (docs)

_REFACTOR-коммита у задачи 1 нет: правка есть одно выражение, чистить было нечего._

## Files Created/Modified

- `app/pages/common.py` — исправлена ветвь заголовка источника; докстринг предиката получил абзац о явной проверке наличия, абзацы о границе вселенных с тремя числами и одну датированную запись об опровергнутом перечне вместо трёх; заведена константа `ORIGIN_CHECK_BOUNDARY`.
- `tests/test_pages/test_origin_guard_on_destructive_routes.py` — пять правил самого предиката, три замеренных числа с летописью, независимый разбор дерева, правило границы вселенных с антивакуумом и его контроль; переписано обоснование гейта, прежняя формулировка помечена опровергнутой.
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md` — две находки вне границы плана.
- `.planning/WINDOWS.md` — те же две находки окнами реестра.

## Замеры, приведённые как того требует план

### Задача 1: КРАСНЫЙ ДО ПРАВКИ, наблюдённый дословно

```
>       assert is_same_origin(request) is False, (
E       AssertionError: предикат признал совпадением источников пару ОТСУТСТВИЙ:
        заголовок `Origin: null` не несёт имени узла, и запрос его тоже не несёт.
        Запрос из песочницы проезжает ВЕТВЬЮ ЗАГОЛОВКА вместо объявленной ветви
        без заголовков — то есть граница, которую докстринг описывает точно, тихо
        расширена
E       assert True is False
E        +  where True = is_same_origin(<starlette.requests.Request object ...>)
1 failed, 9 passed, 1 warning in 0.49s
```

Предикат ответил СОВПАДЕНИЕМ — ровно то, что предъявил `IN-01`.

### Задача 1: собранные правила и длина тела

- `uv run pytest ... --collect-only` → **10 tests collected** в модуле, из них **ПЯТЬ новых** правил предиката (требовалось не менее четырёх).
- Тело предиката без докстринга: **8 строк** (порог — меньше десяти). Замер `ast` по `inspect.getsource`.
- Достижимость случая названа в докстринге ПРЯМО: «за действующим обратным прокси проекта имя узла у запроса есть всегда, поэтому чинится ТИХОЕ РАСШИРЕНИЕ границы… Это основание severity находки, а не повод не чинить».

### Задача 2: ОБА (точнее — ТРИ) числа, снятые СВОИМ разбором дерева

Замер `ast` по `app/pages/*.py` от 2026-09-08, выполненный ДО правки текста:

| величина | замер | ориентир планировщика |
|---|---|---|
| POST-обработчиков страничного слоя | **36** | 36 ✔ |
| из них ЗОВУТ гард происхождения | **13** (в ВОСЬМИ модулях) | 13 в 8 файлах ✔ |
| из них лежат во вселенной хотя бы одного гейта полноты | **10** | ориентира не было |

**РАСХОЖДЕНИЕ НАЗВАНО ПОИМЁННО, И ОНО НЕ С ОРИЕНТИРОМ ПЛАНИРОВЩИКА, А С ОТЧЁТОМ РЕВИЗИИ.** `WR-05` пишет: «Their union covers 13 of the 36 POST handlers». Замер даёт **10**. Три маршрута НЕСУТ гард, не лежа во вселенной ни одного гейта: `auth.py::stop_impersonation`, `billing.py::subscribe_to_plan`, `history.py::history_retry` — снятый с любого из них гард не покраснит ничего. Отчёт свёл «несёт гард» и «стережётся гейтом» в одно число; здесь они разведены и объявлены по отдельности.

### Задача 2: КРАСНЫЙ на МУТАЦИИ объявленного числа (дословно, оба раза)

Мутация `PAGE_POST_HANDLERS_MEASURED` 36 → 37:

```
E  AssertionError: ЧИСЛО POST-ОБРАБОТЧИКОВ РАЗОШЛОСЬ С ДЕРЕВОМ — замерено:
   POST-обработчиков 36 (объявлено 37), зовут гард 13 (объявлено 13), внутри
   вселенных обоих гейтов 10 (объявлено 10). Обработчик появился или исчез:
   ['account_groups.py::account_groups_delete', ... 36 ключей ...]
1 failed, 1 warning in 0.54s
```

Мутация `GATE_UNIVERSE_HANDLERS_MEASURED` 10 → 11:

```
E  AssertionError: ЧИСЛО МАРШРУТОВ ВНУТРИ ВСЕЛЕННЫХ ОБОИХ ГЕЙТОВ РАЗОШЛОСЬ С
   ДЕРЕВОМ — замерено: POST-обработчиков 36 (объявлено 36), зовут гард 13
   (объявлено 13), внутри вселенных обоих гейтов 10 (объявлено 11). Вселенная
   гейта расширена или сужена, и запись о границе в докстринге гарда стала
   ложной: ['account_groups.py::account_groups_delete', 'accounts.py::accounts_delete',
   'admin.py::admin_delete_user', 'admin.py::admin_drop_task',
   'admin.py::admin_impersonate', 'admin.py::admin_restart_worker',
   'admin.py::admin_toggle_block', 'admin.py::admin_toggle_free_access',
   'ads.py::ads_delete', 'schedules.py::schedules_delete']
1 failed, 1 warning in 0.68s
```

Оба текста отказа называют ВСЕ ТРИ числа. Числа возвращены, прогон зелёный.

### Задача 2: КРАСНЫЙ на ПУСТОМ множестве (антивакуум)

```
НА ПУСТОМ НАБОРЕ ИСХОДНИКОВ:
АНТИВАКУУМ: на разбор не подано НИ ОДНОГО модуля страничного слоя — правило
границы вселенных зеленело бы пустым множеством, а именно этим оно и опасно

НА ИСХОДНИКЕ БЕЗ МАРШРУТОВ:
АНТИВАКУУМ: в поданных модулях не найдено НИ ОДНОГО POST-маршрута — разбор
декораторов перестал их узнавать, и все три числа ниже сравнивались бы с пустотой
```

### Задача 2: сколько строк дифф удалил из докстринга гейта и почему каждая

`git diff -U0` даёт **семь удалённых строк** — ровно один абзац `⚠️ ПОЧЕМУ ЭТОГО НЕ ЗАМЕНЯЕТ samesite="lax"` целиком:

1-5. Пять строк несущего рассуждения об «одной политике браузера без единого рубежа» — **переписаны на месте**, содержание сохранено во второй половине нового абзаца («между сторонней страницей и необратимым удалением данных пользователя стояла ровно одна политика браузера»).

6-7. Две строки опровергнутого утверждения («…которое продукт не выставляет и не проверяет») — **восстановлены ДОСЛОВНО** в новом абзаце `⚠️ ПРЕЖНЯЯ ФОРМУЛИРОВКА ЭТОГО АБЗАЦА ОПРОВЕРГНУТА ЗАМЕРОМ И ОСТАВЛЕНА НАЗВАННОЙ, А НЕ СТЁРТОЙ`, вместе с указанием круга (`WR-03`, ревизия 2026-09-07) и замера (`app/pages/auth.py::_session_cookie_attrs`).

Ни одна из семи не потеряна: удаление есть перенос, и запись о дефекте не превратилась в её отсутствие.

### Задача 2: какая фаза упомянута в абзаце о границе вселенных и в какой роли

**НИКАКАЯ.** Машинный отбор упоминаний по новому абзацу `common.py` дал только слово «ВЕХИ» (дважды) — оба раза в роли ВЛАДЕЛЬЦА решения («продуктовый выбор ВЛАДЕЛЬЦА ВЕХИ», «ПРИНЯТОЕ СОСТОЯНИЕ ВЕХИ»). Ни одного номера фазы в роли снимателя абзац не несёт; вместо этого записано «ФАЗА-СНИМАТЕЛЬ НЕ НАЗНАЧЕНА НАМЕРЕННО» с основанием и прецедентом (окна 45, 47, 51).

### Задача 3: инвентарь абзацев ДО сведения

Докстринг предиката ДО: **137 строк, 14 абзацев**. Абзацы, чей предмет — снятый перечень потребителей (сводятся):

| № | первая строка | предмет |
|---|---|---|
| 6 | `⚠️ РАМКИ. Гард живёт ПУБЛИЧНЫМ именем здесь…` | рамка правила: один источник на проект, снятая приватная копия, два гейта поимённо, отказ вести перечень |
| 7 | `⚠️ ПРЕЖНЕЕ ПЕРЕЧИСЛЕНИЕ ЭТОГО АБЗАЦА ОПРОВЕРГНУТО ЗАМЕРОМ…` | вербатим перечня трёх потребителей + замер, которым опровергнут |
| 8 | `⚠️ ПЕРЕЧЕНЬ ПОТРЕБИТЕЛЕЙ ЗДЕСЬ БОЛЬШЕ НЕ ВЕДЁТСЯ, И ЭТО ОСОЗНАННО.` | то же самое другими словами + история фазы 6 + форма D-23 |

**АБЗАЦ, ЧЕЙ ПРЕДМЕТ ОТЛИЧАЕТСЯ, НАЗВАН ОТДЕЛЬНО И НЕ ТРОНУТ:** № 14, `Прежний остаток «удаления через POST гарда не получают» ЗАКРЫТ В АДМИНКЕ…`. Его предмет — ДРУГОЕ опровергнутое утверждение (об удалениях через POST), а не перечень потребителей. Строк этого абзаца в диффе НЕТ — проверено машинно (`git diff | grep` по четырём его маркерам дал пусто).

### Задача 3: замер результата сведения

| величина | ДО | ПОСЛЕ | способ замера |
|---|---|---|---|
| строк докстринга предиката | **137** | **134** | `len(m.is_same_origin.__doc__.splitlines())` |
| абзацев | **14** | **13** | `re.split(r"\n\s*\n", doc)` |
| НАЗВАННЫХ предметов сведённых абзацев | **11** | **11** | поиск одиннадцати маркеров-подстрок в докстринге |

Одиннадцать предметов, каждый проверен подстрокой и НАЙДЕН: (1) один источник правила на проект; (2) перечень потребителей не ведётся; (3a) имя гейта админки; (3b) имя гейта удалений; (4) приватная копия `history.py`, план 04-10 → снята 05-04; (5) вербатим перечня трёх потребителей; (6) замер 2026-09-05 (4 файла / 9 мест); (7) действующий преемник — два гейта, а не список; (8) фаза 6 добавила шесть маршрутов, половина взяла гард; (9) перечень, который надо не забыть исправить, ЗАБЫВАЮТ; (10) форма гейта запретов под чужой личностью (D-23).

**СВЕДЕНО В ДВА АБЗАЦА, А НЕ В ОДИН, И ЭТО НАЗВАНО РЕШЕНИЕМ.** «РАМКИ» есть РАМКА (публичное имя, снятая приватная копия, два гейта, форма D-23) — не запись об опровергнутом. Записей ОБ ОПРОВЕРГНУТОМ ПЕРЕЧНЕ после сведения ровно **ОДНА**, датированная, с указанием круга (`IN-03`, ревизия 2026-09-07) и плана (10-25), и с указанием, каким планом снят сам перечень (10-18 по `WR-07`). Именно это и требует предмет: «ОДНА действующая запись об одном опровергнутом перечне», а не «один абзац на три темы».

### Задача 3: канон доступен по ИМЕНИ

```
$ uv run python -c "import app.pages.common as m; names=[n for n in vars(m)
    if 'ORIGIN' in n.upper() and isinstance(getattr(m,n),str)]; print(names)"
['ORIGIN_CHECK_BOUNDARY']
```

Канон называет функцию разбора cookie и ОБА её обращения к базе: `db.get(User, payload['sub'])` (всегда) и `db.get(User, actor_id)` (при токене с признаком имперсонации). Формулировка «ДО ЛЮБОГО ОБРАЩЕНИЯ К БАЗЕ» в канон не попала — она и есть опровергнутое утверждение.

## Decisions Made

- **Третье число заведено сверх двух, названных планом.** План требовал «сколько накрывают обе вселенные и сколько всего». Замер показал, что «накрывают» и «несут гард» — разные множества (10 и 13), и объявление одного числа вместо двух воспроизвело бы ровно ту ошибку, которую `WR-05` предъявил слову «полнота».
- **Сведение дало два абзаца, а не один** (обоснование выше).
- **Полный прогон `tests/test_pages/` выполнен без снятия узла.** План предписывал `--deselect tests/test_pages/test_htmx_gates.py::test_every_framework_bounded_input_is_declared_as_a_divergence`, обосновывая это переходным состоянием соседа 10-24 в той же волне. Сосед завершён ДО старта настоящего плана (коммит `061060c`), переходного окна не существовало; снятие узла ослабило бы прогон без основания.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Объявлено ТРИ числа границы вселенных вместо двух**

- **Found during:** Задача 2 (замер дерева до правки текста)
- **Issue:** План и отчёт ревизии описывали одно число покрытия — «13 из 36». Замер `ast` показал, что 13 — это число маршрутов, ЗОВУЩИХ гард, а во вселенной хотя бы одного гейта полноты лежат только 10. Объявив 13 покрытием вселенных, докстринг утверждал бы, что три маршрута (`auth.py::stop_impersonation`, `billing.py::subscribe_to_plan`, `history.py::history_retry`) чем-то стережутся, тогда как снятый с них гард не покраснит ничего. Это тот же класс дефекта, ради которого план и заводится.
- **Fix:** заведены три константы с раздельной летописью (`PAGE_POST_HANDLERS_MEASURED`, `ORIGIN_GUARD_CALL_SITES_MEASURED`, `GATE_UNIVERSE_HANDLERS_MEASURED`); текст отказа правила называет все три; докстринг гарда несёт отдельный абзац «ТРИНАДЦАТЬ И ДЕСЯТЬ — РАЗНЫЕ ЧИСЛА» с поимённым перечнем трёх негестережимых маршрутов.
- **Files modified:** `app/pages/common.py`, `tests/test_pages/test_origin_guard_on_destructive_routes.py`
- **Verification:** мутация каждого из трёх чисел даёт красный, называющий все три (два красных приведены выше дословно); правило падает и на пустом множестве.
- **Committed in:** `fc7f34f`

**2. [Rule 1 - Bug] Шапка тестового модуля утверждала «ровно одно» о самой себе**

- **Found during:** Задача 1
- **Issue:** Шапка `test_origin_guard_on_destructive_routes.py` гласила: «Модуль утверждает ровно одно: гард СТОИТ на каждом маршруте подтверждённого удаления». После задач 1 и 2 модуль утверждает три вещи, и шапка стала ложной записью о собственном содержимом — тот же дефект, что `WR-03` предъявил гейту.
- **Fix:** «утверждает первое» + два абзаца о втором (поведение предиката по трём ветвям) и третьем (граница вселенных числом), с явной оговоркой, что «гард ЗАЩИЩАЕТ» модуль по-прежнему НЕ утверждает и границу «без обоих заголовков — пропуск» не пересматривает.
- **Files modified:** `tests/test_pages/test_origin_guard_on_destructive_routes.py`
- **Verification:** новые правила поставлены рядом с объявленной границей, и одно из них (`test_control_a_request_with_neither_header_still_passes`) стережёт её НЕсдвинутость.
- **Committed in:** `e2e34fa`

**3. [Rule 3 - Blocking] Снятие узла из полного прогона отменено — переходного состояния не существовало**

- **Found during:** Задачи 1 и 3 (обе несли одинаковый `--deselect` с обоснованием)
- **Issue:** План обосновывал снятие узла `test_every_framework_bounded_input_is_declared_as_a_divergence` тем, что сосед 10-24 в той же волне держит правило полноты реестра красным ПО ПОСТРОЕНИЮ. Замер: сосед завершён коммитом `061060c` ДО старта плана; прогон снятого узла в одиночку даёт `1 passed`. Снятие ослабило бы прогон без основания — ровно та вакуумная верификация, от которой предостерегает этот же круг ревизии.
- **Fix:** снятие отменено, полный прогон `tests/test_pages/` выполнен БЕЗ `--deselect`. Парный зонд реестра (`объявлено 8 | замерено 8 | жалоб 0`) выполнен дополнительно, а не вместо.
- **Files modified:** нет (правка команды верификации)
- **Verification:** `uv run pytest tests/test_pages/ -q` → **1533 passed, 0 failed, RC=0** за 30:37.
- **Committed in:** отступление в коде не выражено; зафиксировано этой сводкой.

**4. [Rule 2 - Missing Critical] Две находки вне границы плана записаны, а не починены и не замолчаны**

- **Found during:** Задачи 2 и 3
- **Issue:** (а) ЧЕТВЁРТАЯ копия опровергнутого утверждения о `samesite` живёт в докстринге `test_every_mutating_admin_route_checks_the_origin` (`tests/test_pages/test_admin_panel.py`) — файл вне `files_modified`, и `WR-03` адресовал находку одному файлу. (б) Докстринг гарда утверждает, что имя гейта «выписано ровно один раз» и это «принуждается машинной проверкой, считающей вхождения» — такой проверки в дереве НЕТ.
- **Fix:** обе записаны в `deferred-items.md` фазы и окнами реестра `.planning/WINDOWS.md` с основанием, замером и прямым «владелец не назначен». Ни одна не починена: (а) — расширение границы находки, (б) — выбор между «завести правило» и «снять утверждение».
- **Files modified:** `.planning/phases/10-rychag-components-modal-html/deferred-items.md`, `.planning/WINDOWS.md`
- **Verification:** ограничение «ровно один раз» планом СОБЛЮДЕНО — ни одно из двух имён гейтов не получило второго вхождения (проверено `grep -c`: по 1 в каждом файле).
- **Committed in:** `db4f96e`, `77d3623`

---

**Total deviations:** 4 auto-fixed (2 × Rule 2 missing-critical, 1 × Rule 1 bug, 1 × Rule 3 blocking).
**Impact on plan:** ни одно отступление не расширяет предмета. Два усиливают запись там, где план назвал бы дереву неверное число или неверное утверждение о самом себе; одно усиливает верификацию, отменяя необоснованное снятие узла; одно фиксирует найденное вне границы, не трогая его. Расширения области правки нет: боевых файлов по-прежнему два, ровно те, что во `files_modified`.

## Issues Encountered

- **Полный прогон `tests/test_pages/` идёт 30 минут при пределе одного вызова оболочки в 10.** Прогон выполнен фоном. По этой же причине команда `uv run pytest tests/test_pages/` выполнена ОДИН раз — после всех трёх задач, а не трижды: предмет команды у задач 1 и 3 дословно один, и три прогона измерили бы одно и то же трижды. Промежуточные состояния закрыты быстрыми целевыми прогонами (модуль гарда, `test_htmx_gates.py`, `compileall`), их результаты приведены выше.
- **Первый фоновый прогон отброшен намеренно:** он стартовал до правок задач 2 и 3 и читал бы дерево, меняющееся под ним; его результат ничего бы не доказывал. Зачтён только финальный прогон на зафиксированном дереве.

## Known Stubs

None — заглушек план не оставил. Обе константы, обе новые группы правил и канон несут реальные значения и реальные замеры.

## Threat Flags

None — новой сетевой поверхности, новых путей аутентификации, файлового доступа и изменений схемы на границе доверия план не вводит. Дисположиции `mitigate` из `<threat_model>` исполнены: `T-10-25-01` — задачей 1, `T-10-25-02` — задачей 2, `T-10-25-03` — тремя объявленными числами и поимённым перечнем непокрытого, `T-10-25-04` — задачей 3. `T-10-25-SC` (`accept`) подтверждён: `pyproject.toml` не тронут, зависимостей не добавлено.

## User Setup Required

None — внешней настройки план не требует.

## Next Phase Readiness

- **План 10-32 (волна 4) разблокирован:** канон `ORIGIN_CHECK_BOUNDARY` существует, доступен по имени и содержит ровно ту формулировку, на которую четыре площадки вызова должны сослаться вместо дословной копии. Порядок, названный планом, соблюдён: канон заведён РАНЬШЕ, чем на него сошлются.
- **Открыто и передано владельцу:** расширение вселенной серверного рубежа на 23 изменяющих маршрута (`WR-05` целиком) — фаза-владелец не назначена намеренно; четвёртая копия утверждения о `samesite` в `test_admin_panel.py`; отсутствие проверки, считающей вхождения имени гейта.
- **Блокеров исполнения нет.**

## Self-Check: PASSED

- Файлы на диске: `app/pages/common.py` ✔, `tests/test_pages/test_origin_guard_on_destructive_routes.py` ✔, `deferred-items.md` ✔.
- Коммиты в истории: `e2e34fa` ✔, `62ecbfd` ✔, `fc7f34f` ✔, `f6885a8` ✔, `db4f96e` ✔, `77d3623` ✔.
- Верификация плана: `uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py -q` → 12 passed (собрано 5 новых правил при пороге 4); `uv run pytest tests/test_pages/ -q` → **1533 passed, RC=0**; `uv run python -m compileall -q app main.py tests` → RC=0; зонд реестра расхождения → `объявлено 8 | замерено 8 | жалоб 0`, RC=0.
- Три наблюдённых красных приведены дословно: правило безголового сравнения ДО правки, правило границы вселенных на мутации числа (дважды), правило границы вселенных на пустом множестве.

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-08*
