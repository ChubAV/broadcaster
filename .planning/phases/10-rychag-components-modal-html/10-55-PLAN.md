---
phase: 10-rychag-components-modal-html
plan: 55
type: tdd
# ⚠️ `type: tdd`, И ЭТО СЛЕДУЕТ ИЗ ПРЕДМЕТА, А НЕ ИЗ ПРИВЫЧКИ. Предмет — деловое правило со
# ОБЪЯВЛЕННЫМИ ВХОДОМ И ВЫХОДОМ: шесть форм неисполнимой СОХРАНЁННОЙ строки на входе, один исход
# «отказ 400 с указанием на редактор» на выходе. `expect(fn(input)).toBe(output)` для такого
# правила выписывается ДО его реализации, значит эвристика TDD срабатывает. `workflow.tdd_mode`
# конфигурации равен `true` (замерено `config-get workflow.tdd_mode` 2026-09-12).
wave: 1
depends_on: []
gap_closure: true
gap_ids: [CR-01]
# ⚠️ ПРОИСХОЖДЕНИЕ ИДЕНТИФИКАТОРА НАЗЫВАЕТСЯ ЗДЕСЬ, ПОТОМУ ЧТО У ЭТОЙ ФАЗЫ УЖЕ БЫЛА
# ДВУСМЫСЛЕННОСТЬ ИМЕНИ ГЭПА (см. блок комментария `gap_ids` плана 10-52). `CR-01` — это
# ЕДИНСТВЕННАЯ запись раздела `gaps` отчёта `10-VERIFICATION.md` (двенадцатый круг, `verified:
# 2026-09-12T15:00:53Z`, `status: gaps_found`), и её `truth` дословно называет источник:
# «`CR-01` седьмого круга ревизии, `10-REVIEW.md:96-196`». То есть идентификатор ОДИН И ТОТ ЖЕ в
# обоих артефактах, и это совпадение проверено чтением, а не предположено:
#   • реестр `G-10-*` (`10-UAT.md`) записей об этом гэпе НЕ НЕСЁТ — там живут находки ОБХОДА;
#   • `CR-01` ШЕСТОГО круга ревизии — ДРУГАЯ находка (сеанс QR-входа Telegram), и она отложена в
#     Фазу 13 разделом `deferred` того же отчёта. Настоящий план её НЕ ТРОГАЕТ.
files_modified:
  - app/services/schedule_rules.py
  - app/pages/schedules.py
  - app/routes/schedules.py
  - tests/test_schedules_out_of_domain_resume.py
  - alembic/versions/0022_schedules_active_requires_next_run.py
  - .planning/phases/10-rychag-components-modal-html/deferred-items.md
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (`config-get`, 2026-09-12): `security_asvs_level` → `1`,
# `security_block_on` → `"high"`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ (ни `uv add`,
# ни `npm i`), поэтому протокол законности пакетов не требуется и чекпоинта не заводит. Двенадцать
# открытых угроз `10-SECURITY.md` живут под областью отказа `phase-10-only` и настоящим планом НЕ
# ПЕРЕСМАТРИВАЮТСЯ — ссылка на них стои́т в `<threat_model>`.

# ⚠️ `workflow.human_verify_mode` = `end-of-phase` (замерено 2026-09-12), поэтому
# `checkpoint:human-verify` в плане НЕТ НИ ОДНОГО; человеческая часть выражена блоком
# `<verify><human-check>` задачи 3.

# ⚠️ ВОСЕМНАДЦАТЬ ЗАПЕРТЫХ РЕШЕНИЙ `10-CONTEXT.md` (D-01…D-18) НАСТОЯЩИМ ПЛАНОМ НЕ РЕАЛИЗУЮТСЯ И НЕ
# ПЕРЕСМАТРИВАЮТСЯ, И ЭТО НАЗВАНО, А НЕ УМОЛЧАНО: все восемнадцать доставлены планами 10-01…10-54
# (замер двенадцатого круга верификации), а предмет настоящего плана — ДЕФЕКТ КОДА, отгруженного
# фазой (`app/routes/schedules.py` — +252/−20 к базе фазы, `app/pages/schedules.py` — +659/−39), а
# не решение о её составе. Ни один из четырёх критериев роадмапа планом не затрагивается: путь
# удаления расписания `compute_next_run_at` не вызывает вовсе. Из запертых решений план соблюдает
# **D-13** (прочтение критерия 3 ПО ЧИСЛАМ) тем, что шаблонов и каталога сценариев не трогает
# ВОВСЕ — исполняемых строк разметки в нём НОЛЬ, и ни одно из пяти чисел гейта не двигается.

# ⚠️ `$PLAN_BASE` — ИМЯ ДЕРЕВА ДО ПЕРВОГО КОММИТА ПЛАНА, И ОНО СНИМАЕТСЯ ПЕРВЫМ ЖЕ ДЕЙСТВИЕМ
# ИСПОЛНИТЕЛЯ: `PLAN_BASE=$(git rev-parse HEAD)` ДО правки чего-либо; снятое значение приводится в
# сводке. Все проверки вида «этот файл планом не тронут» набраны в форме `git diff
# "$PLAN_BASE"..HEAD -- <путь>`, И ЭТО НЕСУЩЕЕ СВОЙСТВО, А НЕ СЛОГ: голый `git diff` без диапазона
# на УЖЕ ЗАКОММИЧЕННОЙ правке печатает пустоту ВСЕГДА, то есть проверка проходила бы ВАКУУМНО —
# ровно тот класс ложной зелени, который эта фаза преследует у себя (лессон партии о RED по одному
# коду возврата). Диапазон пришпилен, и потому пустой вывод есть ЗАМЕР, а не тишина.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ. Замер 2026-09-12: `estimate-calibration` → `sample_count: 0`,
  # `factor: 1`, `applied: false`, `min_samples: 3`, `confidence: "low"`. `confidence` ВЫВЕДЕНА ИЗ
  # ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена себе, и `tokens` равны `raw_tokens` именно потому, что
  # множитель равен единице и не применён. Размер держится тем, что измеримо СЕЙЧАС: задач 3,
  # правимых файлов 6, из них продуктовых 3 и по одной точке правки в каждом.
  #
  # ⚠️ ПЕРЕБОР БЮДЖЕТА СМАРТ-ЗОНЫ НАЗВАН, А НЕ УМОЛЧАН, И ПРИНЯТ ОСОЗНАННО. Проекция 110000 против
  # бюджета 100000 — 1.10x. Проверщик плана 2026-09-12 отметил это предупреждением и порекомендовал
  # распил на два среза; рекомендация СОВЕЩАТЕЛЬНА (ADR-2629, решение 5), и планировщик выбрал
  # ПРИНЯТИЕ. Основания, и все три проверяемы:
  #   (а) ЦИФРА НЕ ОТКАЛИБРОВАНА. `sample_count: 0` при `min_samples: 3` означает, что 110000 есть
  #       ПРОЕКЦИЯ ПЛАНИРОВЩИКА, а не замер этого проекта. Пилить план по 10% перебора величины, у
  #       которой нет ни одного образца, значило бы принять решение по шуму.
  #   (б) НАБЛЮДАЕМЫЕ МЕТРИКИ В НОРМЕ И НАЗВАНЫ: задач 3 (порог 3), правимых файлов 6, продуктовых
  #       3, точек правки по одной в каждом, ни одна задача не трогает больше 5 файлов.
  #   (в) РАСПИЛ НЕ УДЕШЕВЛЯЕТ, А ПЕРЕКЛАДЫВАЕТ, И ПРИБАВЛЯЕТ ОДИН ХОЛОДНЫЙ СТАРТ. Вынос задачи 3
  #       (записи) отдельным планом заводит новый холодный старт исполнителя (CLAUDE.md, MEMORY.md,
  #       STATE.md, перечитывание плана — по замеру `references/checkpoints.md` «tens of thousands
  #       of tokens» за круг) ради снятия 10% деградации хвоста. Сверх того он поставил бы ТРЕТЬЕГО
  #       писателя на `deferred-items.md` и удлинил цепь волн до четырёх (10-55 → новый → 10-56 →
  #       10-57) — в партии, где ребро по этому файлу только что пришлось объявлять руками.
  # ⚠️ ЧТО ЭТО ПРИНЯТИЕ НЕ ГОВОРИТ: оно не говорит, что перебора нет, и не отменяет предупреждения.
  # Если исполнение покажет деградацию на задаче 3 (записи), это ОБРАЗЕЦ ДЛЯ КАЛИБРОВКИ, и следующая
  # партия будет пилить по замеру, а не по проекции. Факт перебора и выбранный маршрут называются в
  # сводке ЧИСЛОМ.
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "НЕИСПОЛНИМАЯ СОХРАНЁННАЯ СТРОКА ПОЛУЧАЕТ ОТКАЗ НА ВСЕХ ШЕСТИ ЗАМЕРЕННЫХ ФОРМАХ, А НЕ НА ОДНОЙ: человек, нажавший тумблер на строке с `times_of_day=[\"abc\"]`, `[\"25:00\"]`, `[\"9\"]`, `[9]` либо с `timezone=\"Mars/Phobos\"`, видит тот же 400 с указанием на редактор, что и на форме `None`, а не пятисотку без объяснения"
    - "ОТКАЗ РАСЧЁТА ОПОЗНАЁТСЯ ПО СОБЫТИЮ, А НЕ ПО ОДНОМУ ЕГО ЗНАЧЕНИЮ: три обработчика спрашивают ОДНОГО помощника, и `None`-исход с исключением для вызывающего неразличимы — разбор внутренностей вычислителя ему не отдан"
    - "ТЕКСТ ОТКАЗА И КОД ОТВЕТА НЕ СДВИНУЛИСЬ НИ НА СИМВОЛ: страничный тумблер по-прежнему уводит на редактор плашкой `notices.SCHEDULE_VALUES_OUT_OF_DOMAIN`, оба JSON-входа по-прежнему отвечают 400 с прежним `detail` — исход для человека тот же, изменился только СПОСОБ его опознавания"
    - "СТРОКА ПРИ ОТКАЗЕ НЕ ТРОНУТА: после отказа `is_active`, `next_run_at`, `days_of_week`, `times_of_day` и `timezone` перезагруженной из СУБД строки равны тем, что были до нажатия, — отказ оставляет строку ровно там, где нашёл"
    - "ГРАНИЦА, ОБЪЯВЛЕННАЯ МОДУЛЕМ ПРАВИЛ, СОВПАЛА С ЕГО ПРЕДМЕТОМ: `tests/test_schedules_out_of_domain_resume.py` сеет строку с испорченным ВРЕМЕНЕМ и с незнакомой ЗОНОЙ и требует отказа от обоих тумблеров и от частичного обновления — сегодня таких правил ноль"
    - "ЗЕЛЕНЬ НЕ ВАКУУМНА: посев ПЕРЕЧИТЫВАЕТ строку из СУБД и утверждает, что порча приземлилась, а положительный контроль показывает, что тот же путь на ЗАКОННЫХ значениях включает расписание — то есть 400 не есть огульный отказ"
    - "ШАПКА НАКАТА `0022` РАВНА ДЕРЕВУ: прежний текст стои́т дословно, а рядом с ним — что закрыто (`None`-исход) и что оставалось до настоящего плана (пять форм исключения), по идиоме D-30/D-32 этой же фазы"
    - "ПОВТОРНОЕ НАЖАТИЕ НА НЕИСПОЛНИМОЙ СТРОКЕ ДАЁТ ТОТ ЖЕ ИСХОД И ТУ ЖЕ СТРОКУ: второе обращение к тумблеру отвечает тем же кодом с тем же адресатом, и перезагруженная строка после двух нажатий равна себе до первого — ребро `idempotency` требования FORM-06 разрешено ЯВНЫМ критерием приёмки"
    - "ПРЕРВАННЫЙ ЛИБО ПАРАЛЛЕЛЬНЫЙ ПУТЬ НИЧЕГО НЕ ЗАПИСЫВАЕТ: отказ расчёта происходит ДО любой смены состояния, `commit()` на ветви отказа не достигается, и сессия откатывается зависимостью `get_db` — ребро `concurrency` требования FORM-06 разрешено ЯВНЫМ критерием приёмки, проверяемым равенством строки до и после"
  artifacts:
    - "`app/services/schedule_rules.py` — помощник `next_run_or_none`, отвечающий `None` на ЛЮБУЮ неисполнимость сохранённых значений, с перечнем ловимых классов исключений и докстрингом о том, почему исключение и пустой результат суть ОДИН исход для вызывающего"
    - "`app/pages/schedules.py` — страничный тумблер (`schedules_toggle`) спрашивает помощника вместо прямого вызова вычислителя"
    - "`app/routes/schedules.py` — `toggle_schedule` и `update_schedule` спрашивают того же помощника"
    - "`tests/test_schedules_out_of_domain_resume.py` — объявленный перечень форм неисполнимой строки, посев с параметром порчи, правило чистой формы и правила отказа для трёх входов, положительный контроль и правило повторного нажатия"
    - "`alembic/versions/0022_schedules_active_requires_next_run.py` — шапка, приведённая к факту по идиоме D-30/D-32 без вычёркивания прежнего текста"
    - "`.planning/phases/10-rychag-components-modal-html/deferred-items.md` — раздел `## План 10-55` с поимённым распоряжением по одиннадцати находкам седьмого круга ревизии (`WR-01`…`WR-06`, `IN-01`…`IN-05`)"
  key_links:
    - "помощник → три обработчика: единственное, чем три места лишены возможности опознавать отказ ПО-РАЗНОМУ; ломается — расхождение возвращается молча, ровно как оно уже вернулось в виде «закрыт один исход из шести» (перезамер двенадцатого круга)"
    - "перечень форм (`MALFORMED_STORED_FORMS`) → правила трёх входов: единственное, чем форма, добавленная в перечень, автоматически становится требованием ко ВСЕМ трём входам; ломается — новая форма закрывается на одном входе и остаётся открытой на двух, и это тот же дефект, который закрывает настоящий план"
    - "перечитывание посеянной строки из СУБД → неваккумность зелени: единственное, чем правило лишено возможности зеленеть на строке, которая не была испорчена; ломается — правила зелены, а предмет не измеряется вовсе (класс, названный самим модулем: «полная зелень означала „этот вход не измеряется“»)"
    - "шапка наката `0022` → следующий читатель ревизии: единственный канал, в котором записано, ЧТО именно ревизия оставила на бою; ломается — запись снова становится шире дерева, и это тот класс находки, который фаза полицейски преследует у себя"
  prohibitions:
    - statement: "ВЫЧИСЛИТЕЛЬ `compute_next_run_at` НЕ ПРАВИТСЯ НИ НА СИМВОЛ: защита ставится ПОМОЩНИКОМ рядом с ним, а не внутри него. Внесённый внутрь `try` превратил бы `None` из ответа «моментов нет» в ответ «что-то пошло не так», и ДВЕНАДЦАТЬ правил `tests/test_services/test_schedule_service.py`, утверждающих смысл `None`, потеряли бы предмет — а восьмой вызывающий (планировщик `app/worker/`) перестал бы отличать пустое расписание от испорченного"
      status: flagged-unverified
    - statement: "ТЕКСТ ОТКАЗА И КОД ОТВЕТА НЕ МЕНЯЮТСЯ НИ В ОДНОМ ИЗ ТРЁХ МЕСТ: предмет гэпа — СПОСОБ ОПОЗНАВАНИЯ, а не редакция отказа. Правка текста в этом же прогоне сделала бы переход цвета неизмеримым: нельзя отличить «отказ стал срабатывать» от «правило сверяет новую строку»"
      status: flagged-unverified
    - statement: "ВХОДЫ СОЗДАНИЯ И ОБНОВЛЕНИЯ НЕ ОСЛАБЛЯЮТСЯ: `_clean_times`, `_reject_malformed_times` и `validate_timezone` остаются на месте и продолжают отвечать 422. Помощник закрывает путь СОХРАНЁННЫХ значений, а не заменяет отсечку входов — сняв отсечку, план открыл бы РОДИТЬ такую строку через API, то есть заменил бы один дефект другим"
      status: flagged-unverified
    - statement: "`app/templates/` И `app/static/` НЕ ПРАВЯТСЯ НИ ОДНОЙ ЗАДАЧЕЙ: заготовки плашки и таблица стилей суть предмет планов 10-56 (волна 2) и 10-57 (волна 3), и правка их настоящим планом сдвинула бы ПРЕДМЕТ двух следующих планов до того, как они его замерят, — их RED-замеры стали бы вакуумными, а причина вакуума лежала бы в чужом плане. ⚠️ ПРЕЖНЕЕ ОСНОВАНИЕ ЗАПРЕТА («пересечение перечней правимых файлов в ОДНОЙ волне сделало бы параллельное исполнение небезопасным») БЫЛО ВЕРНО для дерева до ревизии плана 2026-09-12, когда 10-56 стоял в волне 1 рядом с настоящим планом; ревизия объявила ребро 10-56 → 10-55 по файлу `deferred-items.md` и развела волны, а запрет остался в силе — сменилось лишь его основание"
      status: flagged-unverified
    - statement: "ПЕРЕЧЕНЬ ЛОВИМЫХ КЛАССОВ ИСКЛЮЧЕНИЙ НЕ РАСШИРЯЕТСЯ ДО `except Exception`: пять названных классов суть ЗАМЕР шести форм, а огульный перехват проглотил бы отказ СУБД, отказ сети и ошибку программиста — и превратил бы пятисотку с объяснением в четырёхсотку без него"
      status: flagged-unverified
    - statement: "ПРЕЖНИЙ ТЕКСТ ШАПКИ НАКАТА `0022` НЕ ВЫЧЁРКИВАЕТСЯ И НЕ ПЕРЕПИСЫВАЕТСЯ: он был ВЕРЕН для своего дня на одной форме из шести, и правка на месте превратила бы архив в переписанную задним числом историю. Коррекция встаёт РЯДОМ (идиома D-30/D-32)"
      status: flagged-unverified
    - statement: "`10-UAT.md` НЕ ПРАВИТСЯ ВОВСЕ, ни одна отметка прохождения в нём не ставится, и поле `status` его шапки терминальным не становится: ради запрета этого самозаверения живёт `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`"
      status: flagged-unverified
    - statement: "`FORM-06` НЕ ОТМЕЧАЕТСЯ ВЫПОЛНЕННЫМ: ни флажок `.planning/REQUIREMENTS.md:31`, ни клетка `:136` (`Gaps Found`) не двигаются — требование закрывается ПОДПИСЬЮ ЧЕЛОВЕКА на приёмке обхода, и `test_requirement_completion_follows_verification.py` стережёт именно это"
      status: flagged-unverified
    - statement: "РЕЕСТР ЗАПРЕТОВ `10-PROHIBITIONS-SUBJECT.md` НЕ ПРАВИТСЯ НИ ОДНИМ СИМВОЛОМ: расхождение его шапки (200) с деревом (285 записей `- statement:`) есть СОВЕЩАТЕЛЬНАЯ пометка двенадцатого круга, а не предмет этой партии. Запреты настоящего плана несут `flagged-unverified` и принуждения НЕ ЗАЯВЛЯЮТ"
      status: flagged-unverified
    - statement: "ПОРЯДОК СНЯТИЯ `PRAGMA ignore_check_constraints` НЕ КОПИРУЕТСЯ С ОБРАЗЦА `IN-04`: если посеву вообще понадобится снятие проверок, `rollback()` встаёт ПЕРВОЙ строкой `finally`, иначе отказ посева маскирует ПЕРВИЧНУЮ причину `PendingRollbackError`-ом. Настоящая находка ревизии остаётся отложенной, но её ошибка в новый код не переносится"
      status: flagged-unverified
---

<objective>
**Помощник, который опознаёт неисполнимость СОХРАНЁННОЙ строки расписания по СОБЫТИЮ, а не по
одному его значению, — и три обработчика, спрашивающие его вместо вычислителя.**

Гэп предъявлен ПЕРЕЗАМЕРОМ ИСПОЛНЕНИЕМ, а не прочтением отчёта. Двенадцатый круг верификации
вызвал `app.services.schedule_service.compute_next_run_at` на шести формах испорченной сохранённой
строки на дереве `16df128` и получил ДОСЛОВНО:

| форма строки | ответ вычислителя |
|---|---|
| `days=[1]`, `times=['abc']`, UTC | RAISES `ValueError: invalid literal for int() with base 10: 'abc'` |
| `days=[1]`, `times=['25:00']`, UTC | RAISES `ValueError: hour must be in 0..23` |
| `days=[1]`, `times=['9']`, UTC | RAISES `IndexError: list index out of range` |
| `days=[1]`, `times=[9]`, UTC | RAISES `AttributeError: 'int' object has no attribute 'split'` |
| `days=[1]`, `times=['10:00']`, `Mars/Phobos` | RAISES `ZoneInfoNotFoundError` |
| `days=['1']`, `times=['10:00']`, UTC | `None` — ЕДИНСТВЕННЫЙ исход, закрытый прошлой партией |

Все три места правлены прошлой партией по образцу `if next_run is None: raise HTTPException(400, …)`
— прочитано планировщиком в дереве: `app/pages/schedules.py:1135-1148` (`schedules_toggle`),
`app/routes/schedules.py:494-514` (`toggle_schedule`), `app/routes/schedules.py:392-412`
(`update_schedule`). Ни одного `try`/`except` вокруг вызова нет ни в одном из трёх. **Исключение
проходит мимо `if next_run is None`, доезжает до общего обработчика `app/main.py:248` и даёт
человеку HTTP 500 без объяснения и без пути восстановления** — дословно тот исход, который седьмой
круг ревизии назвал блокером.

Purpose: закрыть не ЗНАЧЕНИЕ, а СОБЫТИЕ. Вызывающему отдан ровно один вопрос — «есть ли момент», —
и два разных способа ответить «нет» перестают быть его делом. Это и есть предложение ревизии
(`10-REVIEW.md:166-190`), принятое дословно, а не переизобретённое.

Output: помощник `next_run_or_none` в `app/services/schedule_rules.py`; три обработчика, спрашивающие
его; правила, СЕЮЩИЕ пять ранее непокрытых форм и требующие отказа от всех трёх входов; шапка наката
`0022`, приведённая к факту.

⚠️ **ЭТО ТРАССИРУЮЩИЙ СРЕЗ ПАРТИИ ПО СВОЕМУ ПРЕДМЕТУ, И ОН ИДЁТ ПЕРВЫМ НЕ ПО УДОБСТВУ.** Задача 1
проводит ОДИН путь сквозь ВСЕ слои предмета — посев строки в СУБД → чтение её обработчиком → расчёт
момента → опознавание отказа → ответ человеку — и доказывает архитектуру помощника на ОДНОЙ форме
(`times_of_day=["abc"]`) с настоящим сквозным замером. Только после зелёного среза задача 2
расширяет его на остальные две площадки и четыре формы. Срез производственного качества: заготовок
в нём нет, и он остаётся частью отгружаемого кода.

⚠️ **ЧЕГО ЭТОТ ПЛАН НЕ ЗАКРЫВАЕТ, И ЭТО НАЗВАНО ЗДЕСЬ, А НЕ ОСТАВЛЕНО ЧИТАТЕЛЮ.** Он не закрывает
`FORM-06` — требование закрывается ПОДПИСЬЮ ЧЕЛОВЕКА на приёмке обхода `10-UAT.md`, и ни один символ
плана этого флажка не двигает. Он не трогает ни одного из четырёх критериев роадмапа: путь удаления
расписания `compute_next_run_at` не вызывает вовсе (три вызова живут в `schedules_create`,
`schedules_update`, `schedules_toggle` — замерено разбором файла двенадцатым кругом). Он не трогает
плашек отказа и таблицы стилей — это предмет планов 10-56 и 10-57 той же партии.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@app/services/schedule_rules.py
@app/services/schedule_service.py
@tests/test_schedules_out_of_domain_resume.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: трассирующий срез — ОДНА форма испорченного времени сквозь все слои, RED с печатью полученного кода и GREEN на помощнике</name>

  <files>
tests/test_schedules_out_of_domain_resume.py
app/services/schedule_rules.py
app/pages/schedules.py
  </files>

  <read_first>
- `tests/test_schedules_out_of_domain_resume.py` ЦЕЛИКОМ (146 строк шапки и посева плюс шесть
  действующих правил). ⚠️ ЧИТАТЬ ДОСЛОВНО ГРАНИЦУ МОДУЛЯ (`:26-30`): «Предмет — ОТКАЗ ПО
  ОТСУТСТВИЮ МОМЕНТА, а не по пустоте списков». Настоящая задача эту границу РАСШИРЯЕТ, и прежний
  абзац при этом НЕ ВЫЧЁРКИВАЕТСЯ (идиома D-30/D-32): рядом с ним встаёт то, что его переросло.
- `tests/test_schedules_out_of_domain_resume.py:68-131` — `_seed_out_of_domain_schedule`. ⚠️ ФОРМА
  ПОСЕВА НАСЛЕДУЕТСЯ ОТСЮДА, А НЕ ИЗОБРЕТАЕТСЯ: объявление, `commit()`, порча ОТДЕЛЬНЫМ
  присваиванием после первого `commit()`, второй `commit()`. Именно этим порядком порча остаётся
  ЕДИНСТВЕННЫМ отличием от законной записи.
- `app/services/schedule_service.py` ЦЕЛИКОМ (46 строк). Точки без защиты: `ZoneInfo(tz_name)` и
  `time(int(parts[0]), int(parts[1]))`. ⚠️ ЧИТАТЬ И НЕ ПРАВИТЬ НИ НА СИМВОЛ — основание выписано
  запретом во frontmatter.
- `app/services/schedule_rules.py` ЦЕЛИКОМ (121 строка). ⚠️ ЧИТАТЬ ШАПКУ ДОСЛОВНО: модуль
  объявляет себя НЕЙТРАЛЬНЫМ («от него зависят оба слоя, а сам он не зависит ни от одного из них»),
  и помощник обязан этой доктрине подчиниться. Отсюда же берётся форма докстринга: ПОЧЕМУ правило
  существует, чем оно уже платило, и какова его граница.
- `app/pages/schedules.py:1100-1160` — `schedules_toggle` целиком, включая абзац «⚠️ ЭТО НЕ
  ГИПОТЕТИЧЕСКИЙ ВХОД» и ветвь `_editor_error_redirect(..., notices.SCHEDULE_VALUES_OUT_OF_DOMAIN)`.
  ⚠️ ТЕКСТ ОТКАЗА И ФОРМА ВОЗВРАТА НЕ МЕНЯЮТСЯ.
- `app/models/schedule.py:46-82` — `ACTIVE_REQUIRES_NEXT_RUN` и объявления колонок. ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО, И ВОТ ЗАЧЕМ: ЕДИНСТВЕННОЕ ограничение `CHECK` знает пару `is_active`/`next_run_at` и
  НЕ ЗНАЕТ области значений `times_of_day`/`timezone`. Замер планировщика 2026-09-12: порча ВРЕМЕНИ
  либо ЗОНЫ на ВЫКЛЮЧЕННОЙ строке ограничению не противоречит, поэтому `PRAGMA
  ignore_check_constraints` настоящему посеву НЕ НУЖЕН. Подсказка партии о `PRAGMA` есть указание на
  идиому, а не утверждение о необходимости; необходимость проверяется исполнением.
- `tests/test_application/test_send_analytics.py:1155-1175` — идиома `PRAGMA
  ignore_check_constraints` НА СЛУЧАЙ, если посев всё же потребует снятия проверок. ⚠️ ЧИТАТЬ
  ВМЕСТЕ С `10-REVIEW.md:512-531` (`IN-04`): снятие в этом образце стои́т ПОСЛЕ `commit()`, и при
  отказе посева `PendingRollbackError` маскирует первичную причину. В новый код эта очерёдность НЕ
  ПЕРЕНОСИТСЯ — `rollback()` первой строкой `finally`.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md:96-196` — запись `CR-01` целиком
  вместе с предложенной формой помощника (`:166-190`) и абзацем о достижимости.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:343-420` — запись гэпа
  целиком: перезамер шести форм, перечень артефактов и ТРИ пункта `missing`.
- `.planning/phases/10-rychag-components-modal-html/10-PATTERNS.md` — раздел о посеве и о форме
  правил с печатью полученного значения.
  </read_first>

  <behavior>
1. Чистый замер формы (без СУБД и без HTTP): на входе `days_of_week=[1]`, `times_of_day=["abc"]`,
   `tz_name="UTC"` вычислитель `compute_next_run_at` НЕ ВОЗВРАЩАЕТ момента — он поднимает
   исключение; помощник `next_run_or_none` на той же строке возвращает `None`.
2. Посев: выключенная строка, полная по `is_schedule_complete` (аккаунт, группы, дни и времена
   непусты) и неисполнимая по значениям (`times_of_day=["abc"]`). Перечитанная из СУБД строка несёт
   испорченное значение — иначе правило измеряет не то, ради чего заведено.
3. Страничный тумблер на этой строке отвечает ОТКАЗОМ с адресатом-редактором (тем же
   `notices.SCHEDULE_VALUES_OUT_OF_DOMAIN`), а не пятисоткой.
4. Перезагруженная из СУБД строка после отказа равна себе до нажатия по всем пяти полям:
   `is_active`, `next_run_at`, `days_of_week`, `times_of_day`, `timezone`.
5. RED замерен ПЕЧАТЬЮ ПОЛУЧЕННОГО: до появления помощника отказ правила называет ПОЛУЧЕННЫЙ код
   ответа (500) и ПОЛУЧЕННЫЙ класс исключения — а не только несогласие.
6. Положительный контроль: тот же посев с ЗАКОННЫМИ значениями времени включает расписание, то есть
   отказ не огульный.
  </behavior>

  <action>
**(1) ОБЪЯВЛЕНИЕ ФОРМ — `MALFORMED_STORED_FORMS`.** Заводится в
`tests/test_schedules_out_of_domain_resume.py` рядом с действующими `OUT_OF_DOMAIN_DAYS` и
`IN_DOMAIN_TIMES`, которые НЕ ТРОГАЮТСЯ. Перечень именованных форм неисполнимой СОХРАНЁННОЙ строки:
каждая запись несёт человекочитаемую метку, значения `days_of_week`, `times_of_day`, `timezone` и
ссылку на замер двенадцатого круга, из которого форма взята. Настоящая задача заводит перечень и
пользуется ОДНОЙ его записью — формой испорченного времени `["abc"]`; остальные пять подключает
задача 2. ⚠️ ПОЧЕМУ ПЕРЕЧЕНЬ, А НЕ ПЯТЬ ОТДЕЛЬНЫХ ПРАВИЛ: форма, добавленная в перечень, обязана
становиться требованием ко ВСЕМ трём входам сразу — иначе следующая форма закроется на одном входе
и останется открытой на двух, то есть повторится ровно тот дефект, который закрывает этот план.
Форма `days_of_week=['1']` (строка вместо числа, исход `None`) в перечень ВХОДИТ: она есть
ЕДИНСТВЕННЫЙ уже закрытый исход, и её присутствие делает перечень замером шести форм, а не пяти.

**(2) ПОСЕВ С ПАРАМЕТРОМ ПОРЧИ.** `_seed_out_of_domain_schedule` обобщается: порча передаётся
параметром (три значения формы), а умолчание остаётся ПОБАЙТОВО ПРЕЖНИМ — `days_of_week=[9]`,
`times_of_day=["10:00"]`, `timezone="UTC"`, — чтобы шесть действующих правил модуля остались
зелёными без правки ни одного из них. Порча по-прежнему наносится ОТДЕЛЬНЫМ присваиванием после
первого `commit()`. **ПОСЛЕ второго `commit()` посев ПЕРЕЧИТЫВАЕТ строку через `_reload` и
УТВЕРЖДАЕТ, что порча приземлилась** — это и есть антивакуумный зуб посева: без него правило зеленело
бы на строке, которая испорчена не была. Снятие проверок СУБД в посев НЕ ВНОСИТСЯ: замер
планировщика по `app/models/schedule.py:46-52` говорит, что единственное ограничение `CHECK` области
значений времени и зоны не знает. Если исполнение покажет иное, применяется идиома
`PRAGMA ignore_check_constraints` с `rollback()` ПЕРВОЙ строкой `finally` (основание — `IN-04`), и
факт применения называется в сводке.

**(3) ПРАВИЛО ЧИСТОЙ ФОРМЫ — `test_the_calculator_reports_unrunnability_two_ways_and_the_helper_one`.**
Без СУБД и без HTTP, по образцу действующего `test_the_seeded_shape_is_complete_and_yet_has_no_moment`.
Для формы `["abc"]`: вызов `compute_next_run_at` заворачивается так, чтобы ПОЛУЧЕННЫЙ исход
(класс исключения с его текстом либо возвращённое значение) попал в сообщение отказа ДОСЛОВНО;
утверждается, что момента вычислитель не вернул. Затем утверждается, что `next_run_or_none` на той
же строке отвечает `None`. ⚠️ КЛАСС ИСКЛЮЧЕНИЯ ПОИМЁННО НЕ ПРИШПИЛИВАЕТСЯ: пришпиленный класс
заморозил бы внутренности вычислителя, а предмет — что у неисполнимости РОВНО ОДИН исход для
вызывающего. Печатается полученное, утверждается свойство.

**(4) ПРАВИЛО СТРАНИЧНОГО ТУМБЛЕРА — `test_page_toggle_refuses_a_row_whose_times_are_malformed`.**
Форма наследуется у действующего `test_page_toggle_refuses_a_row_whose_days_are_out_of_domain`
(`:272`): тот же клиент, тот же посев, то же чтение адресата плашки. Утверждений три:
(а) код ответа ОТКАЗА, и в сообщении отказа стои́т ПОЛУЧЕННЫЙ код — на дереве до правки там
напечатается 500, и это и есть замер RED; (б) адресат возврата ведёт в редактор и несёт код плашки
`notices.SCHEDULE_VALUES_OUT_OF_DOMAIN`; (в) перезагруженная строка равна себе по всем пяти полям.

**(5) ПОЛОЖИТЕЛЬНЫЙ КОНТРОЛЬ — `test_control_a_legal_row_is_still_switched_on`.** Тот же посев с
ЗАКОННЫМИ днями и временами, то же нажатие: расписание включается, `next_run_at` непуст. ⚠️ ЗАЧЕМ
ОН ЗДЕСЬ: без него правило (4) зеленело бы и на обработчике, отказывающем ВСЕГДА, — а «починка»,
ломающая законный путь, есть худший исход, чем исходный дефект.

**(6) ПОМОЩНИК — `next_run_or_none` в `app/services/schedule_rules.py`.** Принимает СТРОКУ
расписания и отвечает моментом либо `None`. Читает `days_of_week`, `times_of_day`, `timezone`,
подставляя пустой список вместо `None` у первых двух. Вызывает `compute_next_run_at` и ловит ровно
пять классов, названных ревизией: `ValueError`, `TypeError`, `IndexError`, `AttributeError`,
`ZoneInfoNotFoundError` (последний ввозится из `zoneinfo`). Аннотация типа строки берётся ввозом под
`TYPE_CHECKING`, чтобы объявленная нейтральность модуля не превращалась в обязательный ввоз слоя
моделей на каждом импорте правил. Докстринг обязан назвать ТРИ вещи: (а) что исключение и пустой
результат суть ОДИН исход для вызывающего — «строка неисполнима по значениям», и разделять их значило
бы отдать ему разбор внутренностей вычислителя; (б) ПОЧЕМУ защита стои́т здесь, а не внутри
вычислителя (там `None` означает «моментов нет», и смысл этого ответа утверждают правила
`tests/test_services/test_schedule_service.py`, а восьмой вызывающий — планировщик — обязан
продолжать отличать пустое расписание от испорченного); (в) ГРАНИЦУ: перечень классов есть ЗАМЕР
шести форм, а не догадка, и огульный `except Exception` здесь запрещён, потому что проглотил бы
отказ СУБД и ошибку программиста.

**(7) ПЕРВАЯ ПЛОЩАДКА — `schedules_toggle` (`app/pages/schedules.py:1135-1148`).** Прямой вызов
`compute_next_run_at` заменяется вызовом `next_run_or_none(schedule)`. Условие `if next_run is None`,
ветвь отказа, текст плашки и форма возврата остаются ПОБАЙТОВО ПРЕЖНИМИ. Абзац комментария над
вызовом дополняется одним предложением о том, что отказ теперь опознаётся по СОБЫТИЮ; прежний текст
абзаца не вычёркивается. Ввоз `compute_next_run_at` в модуле остаётся ровно если у него есть другие
вызывающие в файле (замер: вызовов три — `schedules_create`, `schedules_update`, `schedules_toggle`),
то есть снимается ТОЛЬКО если стал неиспользуемым.

**(8) ЗАМЕР ПЕРЕХОДА ЦВЕТА.** RED снимается ДО действия (6): правила (3) и (4) прогоняются на дереве
без помощника, и в сводку переносится ДОСЛОВНАЯ строка отказа, называющая полученный код ответа и
полученный класс исключения. ⚠️ КОД ВОЗВРАТА ОДИН САМ ПО СЕБЕ НЕ ЕСТЬ ЗАМЕР RED: ошибка сбора,
описка в отборе `-k` и отказ ввоза дают тот же ненулевой код при неисполненном правиле. В сводке
обязана стоять ПЕЧАТЬ ПРАВИЛА, а не только его цвет. GREEN снимается после (6) и (7) тем же
командой.
  </action>

  <verify>
    <automated>uv run pytest tests/test_schedules_out_of_domain_resume.py -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed"; или в выводе есть "no tests ran"; или число `passed` меньше девяти (шесть действующих правил плюс три заведённых этой задачей) — недобор означает, что новые правила не собрались, а не что они зелены</fails_when>
    <automated>uv run pytest tests/test_services/test_schedule_service.py tests/test_routes/test_schedules_api_value_domain.py -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed" — смысл `None` у вычислителя и отсечка области значений на входах обязаны остаться нетронутыми</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -n "def next_run_or_none" app/services/schedule_rules.py` даёт РОВНО ОДНО вхождение.
- Перечень ловимых классов в теле помощника содержит все пять имён: `ValueError`, `TypeError`,
  `IndexError`, `AttributeError`, `ZoneInfoNotFoundError`; `except Exception` в помощнике
  отсутствует.
- `git diff "$PLAN_BASE"..HEAD -- app/services/schedule_service.py` ПУСТ: вычислитель не тронут ни на символ.
- `grep -c "compute_next_run_at" app/pages/schedules.py` даёт значение на единицу меньше, чем до
  правки (одна из трёх точек вызова переведена на помощника), и `grep -n "next_run_or_none"
  app/pages/schedules.py` даёт ровно одно вхождение вызова.
- Текст отказа страничного тумблера не сдвинут: `git diff "$PLAN_BASE"..HEAD -- app/pages/schedules.py` не содержит
  ни одной изменённой строки, в которой стои́т `SCHEDULE_VALUES_OUT_OF_DOMAIN`.
- В сводке приведена ДОСЛОВНАЯ строка отказа правила (4) на дереве ДО правки, и в ней напечатан
  полученный код ответа; и дословная строка отказа правила (3) с полученным классом исключения.
  Утверждение «правило покраснело» без этих двух строк акцептансом не считается.
- Посев перечитывает строку из СУБД и утверждает приземление порчи: в теле
  `_seed_out_of_domain_schedule` после второго `commit()` стои́т обращение к `_reload` и `assert` о
  значении испорченного поля.
- Шесть действующих правил модуля прошли без правки их тел: `git diff "$PLAN_BASE"..HEAD --
  tests/test_schedules_out_of_domain_resume.py` не содержит изменений внутри тел функций
  `test_api_toggle_refuses_a_row_whose_days_are_out_of_domain`,
  `test_api_toggle_still_pauses_a_row_whose_days_are_out_of_domain`,
  `test_api_update_refuses_a_patch_that_leaves_the_row_unrunnable`,
  `test_page_toggle_refuses_a_row_whose_days_are_out_of_domain`,
  `test_page_toggle_still_pauses_a_row_whose_days_are_out_of_domain`.
- Если `PRAGMA ignore_check_constraints` в посеве всё же применён, `rollback()` стои́т ПЕРВОЙ
  строкой блока `finally`, и факт применения назван в сводке вместе с причиной.
  </acceptance_criteria>

  <done>
Страничный тумблер на строке с `times_of_day=["abc"]` отвечает отказом с адресатом-редактором, а не
пятисоткой; помощник существует и отвечает `None`; строка при отказе не тронута; переход цвета
замерен печатью полученного значения на обоих правилах; положительный контроль зелен; срез
закоммичен.
  </done>

  <reversibility rating="reversible">
Помощник есть ДОБАВЛЕНИЕ рядом с вычислителем: откат сводится к возврату трёх строк вызова и снятию
функции. Ни схема, ни контракт ответа, ни текст отказа не сдвинуты, поэтому цена отката равна одному
обратному коммиту.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: расширение среза — две оставшиеся площадки и четыре оставшиеся формы, перечнем, а не пятью копиями</name>

  <files>
tests/test_schedules_out_of_domain_resume.py
app/routes/schedules.py
  </files>

  <read_first>
- `app/routes/schedules.py:370-420` — `update_schedule` целиком, включая ветвь `elif
  schedule.is_active:` и абзац «⚠️ ТОТ ЖЕ НЕПОКРЫТЫЙ ВИД, ЧТО У ТУМБЛЕРА (CR-01)». ⚠️ ЧИТАТЬ
  ДОСЛОВНО ТЕКСТ `detail`: он не меняется ни на символ.
- `app/routes/schedules.py:470-520` — `toggle_schedule` целиком, включая проверку полноты ВЫШЕ
  расчёта и абзац «ВАЛИДАТОРЫ ВХОДА СЮДА НЕ ПОМОГАЮТ ПО ПОСТРОЕНИЮ». ⚠️ ПОРЯДОК ПРОВЕРОК НЕ
  МЕНЯЕТСЯ: проверка полноты отвечает на вопрос «есть ли что отправлять», расчёт — «когда именно»,
  и слить их значило бы дать человеку совет, которого он не сможет выполнить.
- `tests/test_schedules_out_of_domain_resume.py` в состоянии ПОСЛЕ задачи 1: перечень
  `MALFORMED_STORED_FORMS`, обобщённый посев, правило чистой формы и правило страничного тумблера —
  формы правил наследуются отсюда.
- `tests/test_schedules_out_of_domain_resume.py:174-271` — два действующих правила JSON-входа
  (`test_api_toggle_refuses_a_row_whose_days_are_out_of_domain`,
  `test_api_update_refuses_a_patch_that_leaves_the_row_unrunnable`). ⚠️ ФОРМА ЗАПРОСА, ЗАГОЛОВКИ И
  СПОСОБ ЧТЕНИЯ `detail` БЕРУТСЯ ОТСЮДА.
- `app/services/schedule_rules.py` в состоянии после задачи 1 — сигнатура помощника и его докстринг.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:399-418` — пункты `missing`
  (а) и (б) дословно: «одним помощником на все три места» и «требующие от ОБОИХ тумблеров и от
  `update_schedule` ОТКАЗА, а не пятисотки».
  </read_first>

  <behavior>
1. Для КАЖДОЙ из шести форм перечня `MALFORMED_STORED_FORMS` помощник отвечает `None` — правило
   чистой формы становится параметризованным по перечню, а не по одной записи.
2. Тумблер JSON-API на строке каждой из пяти ранее непокрытых форм отвечает 400 с прежним `detail`,
   а не 500.
3. Частичное обновление JSON-API на ВКЛЮЧЁННОЙ строке каждой из тех же форм отвечает 400 с прежним
   `detail`, а не 500; строка остаётся включённой и её момент не сдвинут.
4. Пауза (выключение) на строке любой из форм по-прежнему проходит: путь выключения расчёта не
   ведёт, и «починка» его не ломает.
5. Строка при каждом отказе не тронута по всем пяти полям.
6. Повторное нажатие тумблера на той же неисполнимой строке даёт ТОТ ЖЕ код ответа и ТУ ЖЕ строку.
  </behavior>

  <action>
**(1) ВТОРАЯ И ТРЕТЬЯ ПЛОЩАДКИ.** В `toggle_schedule` (`:494-514`) и `update_schedule` (`:392-412`)
прямой вызов `compute_next_run_at` заменяется вызовом `next_run_or_none(schedule)`. Условия `if
next_run is None`, оба `raise HTTPException(status.HTTP_400_BAD_REQUEST, detail=…)` с их дословным
текстом, порядок проверок и присваивания `schedule.is_active` / `schedule.next_run_at` остаются
ПОБАЙТОВО ПРЕЖНИМИ. Абзацы комментариев над обоими вызовами дополняются одним предложением об
опознавании по событию; прежний текст не вычёркивается. Ввоз `compute_next_run_at` в модуле
снимается ТОЛЬКО если после правки у него не осталось вызывающих (проверяется грепом, а не
предположением).

**(2) ПАРАМЕТРИЗАЦИЯ ПО ПЕРЕЧНЮ.** Правило чистой формы и три правила входов параметризуются
`MALFORMED_STORED_FORMS` через `pytest.mark.parametrize` с человекочитаемым `ids` из меток перечня —
чтобы отказ называл ФОРМУ, а не индекс. ⚠️ ПОЧЕМУ НЕ ПЯТЬ КОПИЙ ПРАВИЛА НА ВХОД: пятнадцать копий
разъехались бы при первой же правке, и это ровно тот класс расхождения, ради устранения которого
существует сам модуль `schedule_rules` (его шапка: «правило, живущее на одном входе и отсутствующее
на другом, расходится молча»).

**(3) ПРАВИЛО ТУМБЛЕРА JSON-API — `test_api_toggle_refuses_every_malformed_stored_form`.** Форма
наследуется у действующего правила JSON-входа. Утверждений три: код ответа 400 при ПЕЧАТИ
ПОЛУЧЕННОГО кода в сообщении отказа; `detail` ответа посимвольно равен прежнему тексту (берётся из
ответа, а не из исходника обработчика — ожидание, добытое из предмета проверки, согласилось бы с
любой его правкой, поэтому текст выписывается в модуле правил ОДИН раз константой); перезагруженная
строка равна себе по пяти полям.

**(4) ПРАВИЛО ЧАСТИЧНОГО ОБНОВЛЕНИЯ — `test_api_update_refuses_a_patch_on_every_malformed_stored_form`.**
Посев ВКЛЮЧЁННОЙ строки с законным моментом и испорченными значениями (`is_active=True` — посев
задачи 1 это умеет параметром). Патч трогает ПОСТОРОННЕЕ поле, то есть воспроизводит ровно тот вход,
который назвала ревизия: «патч, НЕ ТРОГАЮЩИЙ `days_of_week`». Утверждается 400 с печатью полученного
кода, прежний `detail`, и что строка ОСТАЛАСЬ ВКЛЮЧЁННОЙ с НЕСДВИНУТЫМ моментом — отказ целиком, а
не тихое выключение чужого работающего расписания.

**(5) ПРАВИЛА ПАУЗЫ — по одному на вход, формой действующих
`…_still_pauses_a_row_whose_days_are_out_of_domain`.** Выключение строки любой из форм проходит:
ветвь выключения расчёта не ведёт. ⚠️ ЗАЧЕМ: без них «починка», отказывающая на ЛЮБОМ обращении к
неисполнимой строке, оставалась бы зелёной, а человек терял бы единственное действие, которым он
эту строку гасит.

**(6) ПРАВИЛО ПОВТОРНОГО НАЖАТИЯ — `test_a_second_press_on_an_unrunnable_row_refuses_identically`.**
Два последовательных обращения к одному входу на одной посеянной строке. Утверждается: коды обоих
ответов равны между собой и равны 400; перезагруженная строка после ВТОРОГО нажатия равна снимку,
снятому ДО первого, по всем пяти полям. ⚠️ ЭТО РАЗРЕШЕНИЕ РЕБРА `idempotency` ТРЕБОВАНИЯ FORM-06
ЯВНЫМ КРИТЕРИЕМ, а не отговоркой: ребро пришло из детерминированного зонда и закрывается замером.

**(7) ГРАНИЦА МОДУЛЯ — ПО ИДИОМЕ D-30/D-32.** Абзац `:26-30` («Предмет — ОТКАЗ ПО ОТСУТСТВИЮ
МОМЕНТА») остаётся ДОСЛОВНО. Рядом с ним встаёт новый абзац: он называет, что прежняя граница была
ВЕРНА для своего дерева (закрыт был один исход из шести, и модуль честно объявлял именно его), чем
она опровергнута (перезамер двенадцатого круга исполнением, шесть форм), и какова граница ТЕПЕРЬ
(шесть форм × три входа, перечнем). Вычёркивания нет ни в одном месте.
  </action>

  <verify>
    <automated>uv run pytest tests/test_schedules_out_of_domain_resume.py -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed"; или число `passed` меньше тридцати (шесть действующих правил плюс параметризованные по шести формам правила четырёх видов) — недобор означает, что параметризация не приземлилась</fails_when>
    <automated>uv run pytest tests/test_routes/test_schedules.py tests/test_routes/test_schedules_api_value_domain.py tests/test_routes/test_schedules_api_null.py tests/test_routes/test_schedules_toggle_detached.py tests/test_pages/test_schedules_poisoned_row.py tests/test_pages/test_editor_schedules.py -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed" — шесть модулей суиты, знающих оба входа расписаний, обязаны остаться зелёными: правка меняет СПОСОБ опознавания отказа, а не контракт ответа</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c "next_run_or_none" app/routes/schedules.py` даёт РОВНО 2 вхождения вызова (`:392-412` и
  `:494-514`), и `grep -c "compute_next_run_at" app/routes/schedules.py` даёт 0, если ввоз снят как
  неиспользуемый, — иначе греп называет остальных вызывающих, и в сводке приведены их имена.
- Суммарно по трём продуктовым файлам помощник вызывается ТРИ раза, и это подтверждено выводом
  `grep -rn "next_run_or_none" app/ | grep -v "def next_run_or_none"`.
- Текст обоих отказов JSON-входа не сдвинут: `git diff "$PLAN_BASE"..HEAD -- app/routes/schedules.py` не содержит
  изменённых строк внутри обоих литералов `detail`.
- Перечень `MALFORMED_STORED_FORMS` несёт ШЕСТЬ записей, и каждая метка приведена в сводке вместе с
  полученным от вычислителя исходом — то есть перечень есть ЗАМЕР, а не список из отчёта.
- Вывод `uv run pytest tests/test_schedules_out_of_domain_resume.py -v` содержит имя каждой из шести
  форм в идентификаторах параметризованных правил (проверяется грепом по метке каждой формы).
- В сводке приведены ДОСЛОВНЫЕ строки отказа обоих правил JSON-входа на дереве ДО правки этой
  задачи, и в каждой напечатан полученный код ответа.
- Правила паузы зелены на всех шести формах, и это названо отдельным числом в сводке.
  </acceptance_criteria>

  <done>
Все три входа отказывают ОБЪЯСНИМО на всех шести формах неисполнимой сохранённой строки; пауза
работает; повторное нажатие идемпотентно; шесть соседних модулей расписаний зелены; граница модуля
правил приведена к факту без вычёркивания прежнего текста.
  </done>
</task>

<task type="auto">
  <name>Задача 3: записи — шапка наката `0022` к факту и распоряжение по одиннадцати находкам ревизии, ни одна из которых не падает молча</name>

  <files>
alembic/versions/0022_schedules_active_requires_next_run.py
.planning/phases/10-rychag-components-modal-html/deferred-items.md
  </files>

  <read_first>
- `alembic/versions/0022_schedules_active_requires_next_run.py:1-80` — шапка наката ЦЕЛИКОМ, и
  особенно `:48-62`: абзац «⚠️ СЛЕДСТВИЕ ЭТОГО РЕШЕНИЯ НАЗЫВАЕТСЯ ЗДЕСЬ ЦЕЛИКОМ» и абзац «ДО ПРАВКИ
  CR-01 ЭТА ПОЛОВИНА БЫЛА СЛОМАНА», заканчивающийся утверждением «Теперь оба тумблера и частичное
  обновление JSON-API считают момент ДО смены состояния и отказывают ОБЪЯСНИМО». ⚠️ ЭТО И ЕСТЬ
  ЗАПИСЬ ШИРЕ ДЕРЕВА — она верна на одной форме из шести.
- `tests/test_migrations/test_0022_schedules_active_requires_next_run.py:361-383` —
  `test_the_backfill_runs_before_the_constraint_is_created`: правило читает СТРОКИ файла ревизии.
  ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: правка ШАПКИ сдвигает номера строк, и убедиться, что правило сличает
  ОТНОСИТЕЛЬНЫЙ ПОРЯДОК кода, а не абсолютные номера, обязан исполнитель — прогоном, а не чтением.
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md:1-24` — ФОРМА ФАЙЛА ЦЕЛИКОМ:
  якорь `<!-- deferred-items:end -->`, правило «РОВНО ОДИН РАЗДЕЛ НА ПЛАН», вставка ПЕРЕД якорем и
  основание (задача 3 плана 10-39 дописала раздел в конец, и второй прогон завёл бы второй раздел).
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md` — раздел `## План 10-54`
  (в хвосте файла) как ОБРАЗЕЦ формы записи: поимённая находка, «почему не здесь», НАЗВАННЫЙ
  ВЛАДЕЛЕЦ, следствие.
- `tests/test_planning/test_deferred_items_are_idempotent.py` ЦЕЛИКОМ — четыре принуждаемых свойства
  формы файла. ⚠️ ИСПОЛНИТЕЛИ ОТБИРАЮТ `tests/test_planning/` ИЗ ПРОГОНОВ; поэтому команда проверки
  настоящей задачи НАЗЫВАЕТ ЭТОТ КАТАЛОГ ЯВНО.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md:194-545` — все одиннадцать
  оставшихся находок седьмого круга (`WR-01`…`WR-06`, `IN-01`…`IN-05`) с их файлами и замерами.
  ⚠️ ЧИТАТЬ КАЖДУЮ: распоряжение обязано называть находку ПОИМЁННО и её файл, а не отсылать к
  диапазону строк отчёта.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:415-420` — пункт `missing`
  (в) дословно: «ШАПКА `0022:56-62` — ПРИВЕСТИ К ФАКТУ, не вычёркивая прежнего текста (идиома
  D-30/D-32 этой же фазы): назвать, ЧТО закрыто (`None`-исход) и ЧТО осталось (пять форм
  исключения)».
  </read_first>

  <action>
**(1) ШАПКА НАКАТА — КОРРЕКЦИЯ РЯДОМ, А НЕ ВМЕСТО.** Абзац `:56-62` остаётся ДОСЛОВНО, включая
предложение «Теперь оба тумблера и частичное обновление JSON-API считают момент ДО смены состояния и
отказывают ОБЪЯСНИМО». Непосредственно ЗА ним встаёт новый абзац по идиоме D-30/D-32 и по форме,
которой пользуются шапки `10-UAT.md` и `10-VERIFICATION.md`. Он обязан назвать ЧЕТЫРЕ вещи:
(а) что прежнее утверждение было ВЕРНО на ОДНОЙ форме из шести — на исходе `None`, который прошлая
партия и закрыла; (б) что на ПЯТИ формах вычислитель сообщал о неисполнимости ИСКЛЮЧЕНИЕМ, оно
проходило мимо сличения с пустым значением и давало пятисотку без объяснения; (в) чем это измерено и
когда — перезамером двенадцатого круга верификации ИСПОЛНЕНИЕМ, 2026-09-12, дерево `16df128`, шесть
форм; (г) чем закрыто — помощником, опознающим отказ по СОБЫТИЮ, и правилами, сеющими все шесть форм
для трёх входов (имя модуля правил называется). ⚠️ ПРЕЖНЯЯ ССЫЛКА «Правило, стерегущее это, —
`tests/test_schedules_out_of_domain_resume.py`» ОСТАЁТСЯ: она верна и стала вернее.

**(2) РАЗДЕЛ ОТЛОЖЕННОГО — ОДИННАДЦАТЬ НАХОДОК, НИ ОДНА НЕ ПАДАЕТ МОЛЧА.** В
`deferred-items.md` вносится РОВНО ОДИН новый раздел `## План 10-55`, вставленный **ПЕРЕД** якорем
`<!-- deferred-items:end -->`, а не дописанный в конец файла. Раздел несёт по одному подразделу на
каждую из одиннадцати находок седьмого круга ревизии: `WR-01` (отрицательный контроль гейта границ
дублирует его логику), `WR-02` (текст отказа выписан литералом дважды), `WR-03` (отказ страничного
тумблера теряет имя строки и разворот возврата), `WR-04` (`a_future_run_moment()` снял
воспроизводимость), `WR-05` (`FAILURE_BANNER_HANDLERS_MEASURED` обслуживает две разные величины),
`WR-06` (три изменяющих POST страничного модуля не сверяют происхождение), `IN-01` (мёртвые ввозы
`datetime`/`timezone` в восьми модулях), `IN-02` (`a_future_run_moment` подшит под чужой заголовок),
`IN-03` (`notices.has_code()` без потребителей), `IN-04` (снятие `PRAGMA` после `commit()`),
`IN-05` (два неохраняемых допущения разборщиков). Каждый подраздел несёт: ФАЙЛ находки, одну строку
о том, ЧТО найдено, **НАЗВАННОГО ВЛАДЕЛЬЦА** и одну строку причины отсрочки. ⚠️ ОСНОВАНИЕ ОТСРОЧКИ
ОДНО И НАЗЫВАЕТСЯ ПРЯМО: решение владельца от 2026-09-12 ограничило партию ТРЕМЯ находками
(`CR-01`, `UI-1`, `UI-2`), и находка, отсутствующая и в задаче, и в записи распоряжения, была бы
МОЛЧАЛИВЫМ ПРОПУСКОМ. Владельцем каждой из одиннадцати называется следующий круг закрытия гэпов
Фазы 10, кроме `WR-06` (сверка происхождения трёх POST — область решения о ГРАНИЦЕ гейта полноты, и
владелец ей человек) и `WR-05` (одно имя на две величины — область решения о том, ЧТО именно
стережётся, и владелец ей человек). Раздел заканчивается предложением о том, что находки `UI-3`…`UI-7`
UI-ревизии распоряжены ОТДЕЛЬНЫМ разделом плана 10-57 — чтобы читатель не вывел из молчания, что их
забыли.

**(3) ССЫЛКА В ОБРАТНУЮ СТОРОНУ.** В сводке плана приводится перечень одиннадцати находок с их
владельцами — тем же порядком, каким они стоя́т в разделе, — чтобы распоряжение читалось и без
открытия файла отложенного.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed"; или в выводе есть "no tests ran" — каталог назван ЯВНО потому, что исполнители его отбирают из прогонов, и дефект формы файла отложенного иначе доезжает до гейта оркестратора вместо задачи</fails_when>
    <automated>grep -c "^## План 10-55$" .planning/phases/10-rychag-components-modal-html/deferred-items.md</automated>
    <fails_when>вывод не равен 1 — ноль означает, что раздел не вставлен, два и более означают, что правка дописала второй раздел того же плана</fails_when>
    <automated>grep -n "deferred-items:end" .planning/phases/10-rychag-components-modal-html/deferred-items.md | tail -1</automated>
    <fails_when>вывод пуст (якорь потерян); или номер строки якоря МЕНЬШЕ номера строки заголовка раздела 10-55, полученного командой `grep -n "^## План 10-55$"` — раздел обязан стоя́ть ПЕРЕД якорем</fails_when>
    <automated>uv run pytest tests/test_migrations/test_0022_schedules_active_requires_next_run.py -q</automated>
    <fails_when>non-zero exit; или в итоговой строке стои́т "0 passed" — двадцать правил наката читают его файл, и правка шапки не имеет права сдвинуть ни одного из них</fails_when>
  </verify>

  <acceptance_criteria>
- Прежнее утверждение шапки стои́т в файле дословно: `grep -c "отказывают ОБЪЯСНИМО"
  alembic/versions/0022_schedules_active_requires_next_run.py` даёт не менее 1.
- Коррекция стои́т РЯДОМ и называет число: в шапке присутствует и слово о ПЯТИ формах исключения, и
  дата перезамера 2026-09-12, и имя дерева `16df128` — проверяется тремя положительными грепами,
  каждый из которых даёт не менее одного вхождения.
- Порядок абзацев проверен: номер строки прежнего утверждения МЕНЬШЕ номера строки коррекции
  (`grep -n` по обоим, сличение чисел в сводке).
- Все одиннадцать идентификаторов находок присутствуют в новом разделе: `grep -o "WR-0[1-6]\|IN-0[1-5]"`
  по участку файла от заголовка `## План 10-55` до якоря даёт одиннадцать РАЗЛИЧНЫХ значений.
- У каждого из одиннадцати подразделов назван владелец: число вхождений слова-маркера владельца в
  участке раздела не меньше одиннадцати.
- `git diff "$PLAN_BASE"..HEAD -- .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-UAT.md`
  ПУСТ: ни флажок `FORM-06`, ни клетка охвата, ни один символ обхода настоящей задачей не тронуты.
- `git diff "$PLAN_BASE"..HEAD -- .planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md` ПУСТ:
  расхождение реестра запретов есть совещательная пометка, а не предмет партии.
  </acceptance_criteria>

  <verify>
    <human-check>
⚠️ СЛЕДСТВИЕ, А НЕ ЗАДАЧА, И ЗАКРЫВАЕТ ЕГО ЧЕЛОВЕК. Настоящий план НЕ трогает плашек отказа и НЕ
сдвигает ни одного из четырёх критериев роадмапа, поэтому наблюдения шагов 2.8 и 4.4 обхода
`10-UAT.md` им НЕ обесцениваются (их обесценивают планы 10-56 и 10-57 — сказано в их собственных
записях). Человеку остаётся ровно одно, и машина этого не вправе: ПОДПИСЬ НА ПРИЁМКЕ обхода
`10-UAT.md`, и только она закрывает `FORM-06`. Закрытие `CR-01` подписи не заменяет и требования не
двигает.
    </human-check>
  </verify>

  <done>
Шапка наката равна дереву без вычёркивания прежнего текста; все одиннадцать находок седьмого круга
ревизии распоряжены поимённо с названным владельцем в разделе, вставленном перед якорем;
`tests/test_planning/` зелен; требование FORM-06 не тронуто.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| СУБД → обработчик | значения, прочитанные из СОХРАНЁННОЙ строки, приходят в обработчик БЕЗ прохождения валидаторов входа: их писал прямой psql, ручной UPDATE на бою либо накат ревизии — и ровно это допущение о чистоте сохранённого объявлено несостоятельным самой ревизией `0022:15-19` |
| вычислитель → вызывающий | вычислитель сообщает об одном факте ДВУМЯ способами (значение и исключение), и граница между «моментов нет» и «значения неисполнимы» не принуждается ничем, кроме помощника |
| обработчик → человек | общий обработчик исключений `app/main.py:248` превращает любой непойманный отказ в пятисотку без объяснения и без пути восстановления |
| план закрытия гэпа → запись наката | шапка ревизии читается следующим инженером как ФАКТ о бое, и утверждение шире дерева обходится дороже отсутствующего |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-55-01 | Denial of Service | `schedules_toggle`, `toggle_schedule`, `update_schedule` на неисполнимой сохранённой строке | high | mitigate | помощник `next_run_or_none` ловит пять названных классов; пять ранее непокрытых форм получают правила на всех трёх входах; человек вместо пятисотки получает 400 с адресатом-редактором |
| T-10-55-02 | Information Disclosure | пятисотка общего обработчика на форменном запросе | medium | mitigate | отказ перестаёт доходить до общего обработчика, поэтому трассировка и внутренние имена в ответ не попадают; текст отказа остаётся прежним и внутренностей вычислителя не называет |
| T-10-55-03 | Tampering | огульный `except Exception` в помощнике проглотил бы отказ СУБД и ошибку программиста | high | mitigate | перечень классов закрыт пятью именами замера; запрет выписан отдельной записью `prohibitions`; акцептанс требует отсутствия `except Exception` в теле помощника |
| T-10-55-04 | Tampering | правка вычислителя вместо заведения помощника: `None` перестал бы означать «моментов нет» | high | mitigate | `git diff "$PLAN_BASE"..HEAD -- app/services/schedule_service.py` обязан быть ПУСТ (акцептанс задачи 1); соседний модуль правил вычислителя прогоняется обеими задачами |
| T-10-55-05 | Repudiation | «починка», отказывающая на ЛЮБОМ обращении, осталась бы зелёной и отняла бы у человека паузу | high | mitigate | положительный контроль законной строки (задача 1) плюс правила паузы на всех шести формах (задача 2) |
| T-10-55-06 | Spoofing | зелень правил на строке, которая не была испорчена (посев не приземлился) | high | mitigate | посев ПЕРЕЧИТЫВАЕТ строку из СУБД и утверждает значение испорченного поля; без этого зуба «этот вход не измеряется» неотличимо от «этот вход исправен» |
| T-10-55-07 | Repudiation | RED, объявленный по ненулевому коду возврата при неисполненном правиле | high | mitigate | акцептанс требует ДОСЛОВНОЙ строки отказа с ПОЛУЧЕННЫМ кодом ответа и полученным классом исключения; кода возврата одного недостаточно, и это записано лессоном партии |
| T-10-55-08 | Tampering | раздел отложенного, дописанный в конец файла: второй прогон удвоил бы его | medium | mitigate | вставка ПЕРЕД якорем `<!-- deferred-items:end -->`, ровно один раздел на план, оба свойства принуждаются `tests/test_planning/test_deferred_items_are_idempotent.py`, и каталог назван в команде ЯВНО |
| T-10-55-09 | Repudiation | одна из одиннадцати находок ревизии, не попавшая ни в задачу, ни в запись | medium | mitigate | раздел отложенного несёт все одиннадцать идентификаторов, у каждого назван владелец, и акцептанс считает их число грепом |
| T-10-55-10 | Elevation of Privilege | машинная отметка `FORM-06` выполненным в обход подписи человека | high | mitigate | запрет выписан отдельной записью; акцептанс требует ПУСТОГО диффа `.planning/REQUIREMENTS.md` и `10-UAT.md`; принуждение — `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` и `test_requirement_completion_follows_verification.py` |
| T-10-55-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера; ввозятся только `zoneinfo` стандартной библиотеки и уже установленные пакеты проекта |

⚠️ ДВЕНАДЦАТЬ ОТКРЫТЫХ УГРОЗ `10-SECURITY.md` НАСТОЯЩИМ ПЛАНОМ НЕ ПЕРЕСМАТРИВАЮТСЯ: их область
отказа объявлена `phase-10-only`, и решение по ним принадлежит владельцу (пункт 4 перечня
«Human Verification Required» двенадцатого круга). Реестр угроз этой партии стои́т РЯДОМ с ними, а
не вместо них.
</threat_model>

## Artifacts this phase produces

**Новые символы продукта:**
- `next_run_or_none(schedule)` — функция в `app/services/schedule_rules.py`

**Новые символы правил (`tests/test_schedules_out_of_domain_resume.py`):**
- `MALFORMED_STORED_FORMS` — объявленный перечень шести форм неисполнимой сохранённой строки
- `test_the_calculator_reports_unrunnability_two_ways_and_the_helper_one`
- `test_page_toggle_refuses_a_row_whose_times_are_malformed`
- `test_api_toggle_refuses_every_malformed_stored_form`
- `test_api_update_refuses_a_patch_on_every_malformed_stored_form`
- `test_page_toggle_still_pauses_every_malformed_stored_form`
- `test_api_toggle_still_pauses_every_malformed_stored_form`
- `test_a_second_press_on_an_unrunnable_row_refuses_identically`
- `test_control_a_legal_row_is_still_switched_on`
- параметр порчи у существующего `_seed_out_of_domain_schedule` (умолчание побайтово прежнее)

**Правленые точки продукта (новых символов не заводят):**
- `app/pages/schedules.py` — одна точка вызова в `schedules_toggle`
- `app/routes/schedules.py` — две точки вызова: `toggle_schedule`, `update_schedule`

**Новые записи:**
- абзац-коррекция шапки `alembic/versions/0022_schedules_active_requires_next_run.py` (идиома D-30/D-32)
- раздел `## План 10-55` в `deferred-items.md` с одиннадцатью подразделами `WR-01`…`WR-06`, `IN-01`…`IN-05`

**Новых файлов, путей, классов CSS и идентификаторов шаблонов план не заводит НИ ОДНОГО.**

<verification>
1. `uv run pytest tests/test_schedules_out_of_domain_resume.py -q` — зелено, и число `passed` не
   меньше тридцати.
2. `uv run pytest tests/test_services/test_schedule_service.py tests/test_routes/test_schedules.py
   tests/test_routes/test_schedules_api_value_domain.py tests/test_routes/test_schedules_api_null.py
   tests/test_routes/test_schedules_toggle_detached.py tests/test_pages/test_schedules_poisoned_row.py
   tests/test_pages/test_editor_schedules.py tests/test_migrations/test_0022_schedules_active_requires_next_run.py -q`
   — зелено.
3. `uv run pytest tests/test_planning/ -q` — зелено (каталог назван явно).
4. `uv run python -m compileall -q app main.py tests` — без отказов.
5. `git diff "$PLAN_BASE"..HEAD -- app/services/schedule_service.py app/templates app/static .planning/REQUIREMENTS.md
   .planning/phases/10-rychag-components-modal-html/10-UAT.md
   .planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md` — ПУСТ.
6. Переходы цвета обоих RED-замеров приведены в сводке ДОСЛОВНЫМИ строками отказа с полученными
   значениями.
7. ПРИНЯТЫЙ ПЕРЕБОР БЮДЖЕТА НАЗВАН В СВОДКЕ ЧИСЛОМ: проекция 110000 против бюджета 100000 (1.10x),
   `sample_count: 0`, маршрут — ПРИНЯТИЕ, а не распил, с основанием из блока `estimate`. ⚠️ И ТАМ ЖЕ
   ПРИВОДИТСЯ ОБРАЗЕЦ ДЛЯ КАЛИБРОВКИ: фактический расход исполнения — он и есть первый образец,
   которого у проекции не было, и без него следующая партия будет решать тем же шумом.

⚠️ ПОЛНАЯ СУИТА (`just test` → `uv run pytest tests/ -v`, замер 2026-09-12: 3216 passed, ~39:27) в
петлю обратной связи задач НЕ ВХОДИТ по длительности и снимается один раз гейтом оркестратора. При
её прогоне помнить: `tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard`
НЕДЕТЕРМИНИРОВАН ПО ВРЕМЕНИ СУТОК (скользящее окно 24 ч против календарного дня) и отложен решением
владельца — его краснота настоящей партии НЕ ПРИНАДЛЕЖИТ, и объявлять её отказом плана нельзя;
называть её надлежит именем правила и причиной.
</verification>

<success_criteria>
- Помощник `next_run_or_none` существует в `app/services/schedule_rules.py` и ловит ровно пять
  названных классов исключений.
- Все три обработчика спрашивают помощника; текст отказа и код ответа не сдвинуты ни в одном из трёх.
- Шесть форм неисполнимой сохранённой строки × три входа покрыты правилами; пауза и законный путь
  подтверждены контролями.
- Строка при каждом отказе не тронута по пяти полям; повторное нажатие идемпотентно.
- Шапка наката `0022` равна дереву; прежний текст стои́т дословно.
- Одиннадцать находок седьмого круга ревизии распоряжены поимённо с названным владельцем.
- `FORM-06` не отмечен; `10-UAT.md` не тронут.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-55-SUMMARY.md` when done
</output>
