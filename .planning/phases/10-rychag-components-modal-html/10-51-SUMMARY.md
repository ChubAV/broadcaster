---
phase: 10-rychag-components-modal-html
plan: 51
subsystem: frontend
tags: [css, failure-visibility, htmx, pytest, tdd, gap-closure]
status: complete

requires:
  - phase: 10-rychag-components-modal-html
    provides: "условный подъём заготовок плашки отказа `app/static/css/app.css` (план 10-33, закрытие G-10-4), гашение обеих заготовок при ОТКРЫТИИ панели в рычаге (план 10-35, закрытие CR-01), гашение обеих заготовок по УСПЕШНОМУ обмену (план 10-49, закрытие G-10-5 — ВТОРОЙ и главный выход видимой плашки с экрана), запись гэпа G-10-7 (обход `walkthrough_3` 2026-09-11)"
provides:
  - "БЕЗУСЛОВНЫЙ подъём обеих заготовок в `app/static/css/app.css`: селектор адресует заготовку САМ, ни один признак состояния документа видимости записи отказа больше не решает"
  - "`test_the_failure_banner_lift_is_unconditional` — правило, краснеющее на возврате условности В ЛЮБОЙ ФОРМЕ: утверждение положительное, имени ни одного признака состояния правило не знает"
  - "`test_the_failure_banner_lift_declares_no_display_mode` — запрет, до сих пор державшийся прозой блока, получил зубы; его цена выросла вместе с безусловностью"
  - "`test_the_lever_raises_the_scroll_lock_flag` — гарантия прежней антивакуумной половины, переехавшая в собственное имя вместе со СВОИМ предметом (прокрутка списка за открытым окном)"
  - "`_selector_lifts_banner` — отбор блоков подъёма по признаку ПРЕДМЕТА, а не по признаку состояния: пять зависимых правил продолжают находить свой предмет после снятия условия"
  - "Два контроля с непересекающимися предметами: возврат условности ПОСТОРОННИМ признаком и дописанный способ отображения"
  - "Запись D-30/D-32 рядом с пунктом (в) таблицы стилей: опровергнутый вывод, верная посылка, положительный перезамер того же дня, снятое планом 10-49 основание и новая цена"
affects: [видимость записи отказа на всех экранах обоих шеллов, G-10-7 (корень — но НЕ запись), шаги 2.8 и 4.4 ручного обхода, план-читатель прозы `includes/htmx_error_banner.html`]

actuals:
  tokens: 11714
  tasks: 3
  commits: 3
plan_head_before: 39c5ab1473d6ce1b2e93b6fdd18e7b16fc674f7d

tech-stack:
  added: []
  patterns:
    - "Отбор предмета правила ключáется на признак ПРЕДМЕТА, а не на условие, которое план снимает: отбор, привязанный к снимаемому условию, превращает зависимые правила в зелёную пустоту РОВНО В МОМЕНТ правки — и это неотличимо от их прохождения"
    - "Утверждение о запрете формулируется ПОЛОЖИТЕЛЬНО («часть равна адресу предмета»), а не отрицанием знакомого имени: правило, знающее одно имя, пропускает дефект, вернувшийся под другим"
    - "Контроль возврата дефекта подаёт ПОСТОРОННИЙ экземпляр класса, а не тот, что стоял до правки: иначе доказано узнавание одного знакомого имени, а не класса"
    - "Гарантия, жившая ВНУТРИ снимаемого условия, но державшая СОБСТВЕННЫЙ предмет, переезжает в отдельное имя ДО правки и предъявляется зелёной на обоих деревьях — цвет, не изменившийся при переезде, и есть доказательство, что переехала она, а не завелась заново"
    - "Имя правила, пережившее свой предмет, переименовывается, а прежнее называется в докстринге; ссылки в сводках прежних планов не правятся — они описывают дерево своего времени"

key-files:
  created: []
  modified:
    - app/static/css/app.css
    - tests/test_pages/test_shell.py
    - .planning/WINDOWS.md
    - .planning/phases/10-rychag-components-modal-html/deferred-items.md

key-decisions:
  - "D-51-1: снято УСЛОВИЕ, а не заведён второй блок. Величины блока (положение вне потока, отступ сверху, слой 70, ширина, поля, скругление, подложка, тень) не сдвинуты НИ ОДНА — вторая копия величин разошлась бы с первой молча, и это записанное основание самого блока"
  - "D-51-2: отбор блоков подъёма переключён на имя ЗАГОТОВКИ (`_selector_lifts_banner`) ПЕРВЫМ шагом, до правки таблицы. Основание замерено: на отборе по имени признака состояния правка таблицы дала бы ПУСТОЕ множество, и пять зависимых правил (правило порядка слоёв и четыре контроля) зазеленели бы ВАКУУМОМ. Отбор не ключáется и ни на одно объявление блока — каждое из них есть ПРЕДМЕТ находки, и предмет, попавший в отбор, перестал бы быть находимым"
  - "D-51-3: утверждение безусловности ПОЛОЖИТЕЛЬНОЕ — «каждая часть селектора равна адресу заготовки». Имени `is-modal-open` правило не знает; имя приезжает в вывод ИЗ ПОЛУЧЕННОГО СЕЛЕКТОРА, который отказ печатает дословно. Правило, знающее одно имя, пропустило бы условность, вернувшуюся под другим"
  - "D-51-4: антивакуумная половина правила порядка слоёв ПЕРЕЕХАЛА в `test_the_lever_raises_the_scroll_lock_flag`, а не снята вместе с условием. Основание: она держала ДВА предмета, и второй условию подъёма не принадлежал никогда — признак, который рычаг перестал бы поднимать, оставил бы список ЕДУЩИМ за открытым окном (план 09-13, решение владельца `scroll-lock`)"
  - "D-51-5: запрет объявления способа отображения получил правило ИМЕННО СЕЙЧАС, и причина замерена, а не стилистическая: цена формы отказа выросла с «две пустые подложки на каждом открытии панели» до «две пустые подложки НА КАЖДОМ ЭКРАНЕ ПРОЕКТА, всегда»"
  - "D-51-6: `test_the_failure_banner_is_declared_above_the_panel_while_it_is_open` → `test_the_failure_banner_is_declared_above_the_panel`. Слова «пока она открыта» перестали называть хоть что-нибудь: порядок слоёв сличается теперь всегда. Прежнее имя названо в докстринге и в пункте (д) таблицы, а не стёрто"
  - "D-51-7: прежний заголовок блока и прежний текст пункта (в) оставлены ДОСЛОВНО; рядом встала запись D-30/D-32. Вывод пункта (в) опровергнут ЗАМЕРОМ, а посылка его верна — прокрутка после ухода панели действительно живая, и неверно лишь заключение, что этого довольно"

requirements-completed: []

coverage:
  - id: D1
    description: "Подъём обеих заготовок БЕЗУСЛОВЕН: селектор блока подъёма адресует заготовку сам, и ни один признак-предок в нём не стои́т"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_the_failure_banner_lift_is_unconditional"
        status: pass
  - id: D2
    description: "Возврат условности В ЛЮБОЙ ФОРМЕ краснит прогон — показано ПОСТОРОННИМ признаком-предком на доктóренной копии, а не знакомым именем"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_a_conditional_lift_reddens"
        status: pass
  - id: D3
    description: "Блок подъёма не объявляет способа отображения; форма отказа краснит прогон"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_the_failure_banner_lift_declares_no_display_mode"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_a_lift_declaring_a_display_mode_reddens"
        status: pass
  - id: D4
    description: "Рычаг поднимает признак блокировки прокрутки ГЛАГОЛОМ — гарантия не потеряна вместе со снятым условием подъёма"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_the_lever_raises_the_scroll_lock_flag"
        status: pass
  - id: D5
    description: "CR-01 не вернулся: гашение обеих заготовок при открытии панели в рычаге нетронуто"
    requirement: FORM-06
    verification:
      - kind: integration
        ref: "tests/test_templates/test_components.py#test_the_lever_clears_both_failure_banners_by_execution"
        status: pass
  - id: D6
    description: "G-10-4 не переоткрыт: слой подъёма выше слоя панели, положение вне потока на месте, цепь предков чиста"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_the_failure_banner_is_declared_above_the_panel"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_no_ancestor_of_the_failure_banner_traps_the_fixed_layer"
        status: pass
---

# Phase 10 Plan 51: Безусловный подъём заготовок плашки отказа Summary

Подъём обеих заготовок плашки отказа снят с условия открытой панели — запись отказа достижима глазу
и после того, как панель ушла, — и возврат условности в любой форме краснит прогон правилом,
которое не знает имени ни одного признака состояния.

## Что сделано

**Три задачи, три коммита, RED → GREEN измерен переходом цвета.**

| Задача | Коммит | Что отгружено |
|--------|--------|----------------|
| 1 (RED) | `c88cba0` `test(10-51)` | отбор блоков подъёма переключён на признак предмета; правило безусловности — КРАСНО; две гарантии — переехавшая и получившая зубы — зелены уже на нетронутой таблице |
| 2 (GREEN) | `34ac774` `feat(10-51)` | одна правка селектора, запись D-30/D-32 рядом с прежним основанием, пункт (д) дополнен, правило переименовано |
| 3 (зубы) | `95b5339` `test(10-51)` | два контроля на непересекающихся предметах; G-10-4 и CR-01 предъявлены незакрытыми заново; полная суита |

Последовательность RED → GREEN соблюдена: `test(10-51)` предшествует `feat(10-51)` в истории ветки.

## TDD Gate Compliance

| Гейт | Коммит | Состояние |
|------|--------|-----------|
| RED | `c88cba0` `test(10-51)` | ✅ правило `test_the_failure_banner_lift_is_unconditional` упало ПО ОБЪЯВЛЕННОЙ ПРИЧИНЕ |
| GREEN | `34ac774` `feat(10-51)` | ✅ то же правило зелено; модуль целиком `230 passed`, rc=0 |
| REFACTOR | `95b5339` `test(10-51)` | ✅ зубы (два контроля); полная суита `3182 passed`, rc=0 |

### RED-запись, проверенная прибором

`gsd-tools check tdd-red-evidence` → **`RED_EVIDENCE_OK`** (`reason: target_test_failed`,
`exit_code: 1`, `tests: 1`, `pass: 0`, `fail: 1`).

```json
{
  "command": "uv run pytest tests/test_pages/test_shell.py -q -k \"lift_is_unconditional\"",
  "exit_code": 1,
  "failing_test": "tests/test_pages/test_shell.py::test_the_failure_banner_lift_is_unconditional",
  "target_test": "tests/test_pages/test_shell.py::test_the_failure_banner_lift_is_unconditional",
  "expected": "['#htmx-failure-server', '#htmx-failure-network']",
  "actual": "['.is-modal-open #htmx-failure-server', '.is-modal-open #htmx-failure-network']",
  "verdict": "RED_EVIDENCE_OK",
  "reason": "target_test_failed"
}
```

⚠️ **Прибор читает TAP, суита говорит на pytest, и переходник назван, а не умолчан.** Вывод
переведён в TAP из `--junitxml` ТОГО ЖЕ прогона (файл в каталоге черновиков, не в дереве): переведена
СЕРИАЛИЗАЦИЯ замера, а не сочинена улика. Код возврата взят у настоящего `pytest`. Приём и его
основание записаны планами 10-36, 10-37 и 10-49 этой же фазы; форма идентификатора приведена к
путевой (точки `classname` → разделители) сразу, потому что расхождение этой формы уже стоило плану
10-49 одного ложного `INVALID_RED`.

## Переход цвета — оба состояния с командами и кодами возврата

| Состояние | Команда | Итоговая строка | rc |
|-----------|---------|-----------------|----|
| RED (до правки таблицы) | `uv run pytest tests/test_pages/test_shell.py -q -k "lift_is_unconditional"` | `1 failed, 229 deselected` | 1 |
| GREEN (после правки) | та же | `1 passed, 229 deselected` | 0 |

**Полученный селектор приведён ДОСЛОВНО, как того требует акцептанс:**

```
.is-modal-open #htmx-failure-server, .is-modal-open #htmx-failure-network
```

Отказ правила печатает его целиком, разбор на части и ожидание — и именно из напечатанного
селектора в вывод приезжает имя `is-modal-open`, которого гейт `<verify>` требует. Тело правила
этого имени не знает и знать не должно.

## Замеры, которых требовал акцептанс

- **Блоков подъёма в сегодняшней таблице — РОВНО ОДИН.** Замер после переключения отбора и до
  правки таблицы: `_banner_elevation_rules(_app_css_path())` → 1 блок, селектор приведён выше.
- **Собрано правил в модуле: 230** (`uv run pytest tests/test_pages/test_shell.py --collect-only -q`)
  при пороге 223 — порог есть замер 2026-09-11 на `d17c594` ДО партии; 227 дало дерево после волны 1
  (плюс четыре правила плана 10-49), 230 — после настоящего плана (плюс три правила задач 1 и 3;
  четвёртое новое имя — `_doctored_lift_block` — помощник, а не правило).
- **Четыре контроля, читающие отбор, зелены ДО правки таблицы:**
  `6 passed, 224 deselected`, rc=0 — переключение отбора их предмета не тронуло.
- **Переехавшая гарантия и получивший зубы запрет ЗЕЛЕНЫ уже до правки:**
  `2 passed, 228 deselected`, rc=0. Цвет, не изменившийся при переезде, и есть доказательство, что
  гарантия переехала, а не завелась заново.
- **`git diff --numstat -- app` пуст на задаче 1**; `git diff --numstat -- app/templates` пуст на
  всех трёх задачах.
- **Величины блока подъёма не сдвинуты ни одна** — проверено чтением диффа: снятыми оказались только
  две строки преамбулы и строки комментария, ни одной строки объявлений.

## G-10-4 и CR-01 предъявлены незакрытыми заново — ПРОГОНОМ, а не рассуждением

| Что предъявлено | Правила | Итог | rc |
|-----------------|---------|------|----|
| G-10-4 (порядок слоёв, чистота цепи предков, вселенная предков) | `test_the_failure_banner_is_declared_above_the_panel`, `test_no_ancestor_of_the_failure_banner_traps_the_fixed_layer`, `test_the_failure_banner_ancestor_universe_is_addressed_by_stylesheet_selectors` | `3 passed` | 0 |
| Цепь предков целиком | `-k "ancestor"` | `23 passed, 207 deselected` | 0 |
| CR-01 (гашение обеих заготовок при открытии панели) с двумя контролями | `test_the_lever_clears_both_failure_banners_by_execution`, `test_control_a_lever_whose_clearing_is_dropped_or_partial_reddens`, `test_control_a_lever_that_clears_only_in_a_line_comment_reddens` | `3 passed` | 0 |
| CR-01, подстрочный антивакуумный слой | `test_the_lever_clears_both_failure_banners_when_the_panel_opens` | `1 passed` | 0 |
| Рычаг не тронут | `git diff HEAD -- app/templates/components/modal.html` | пусто | — |

**ПОЧЕМУ БЕЗУСЛОВНЫЙ ПОДЪЁМ CR-01 НЕ ВОЗВРАЩАЕТ — РАССУЖДЕНИЕ ЗАПИСАНО ЗДЕСЬ, А ПРОГОН ЕГО НЕ
ЗАМЕНЯЕТ.** Подъём стал безусловным, но плашка, погашенная при открытии панели, НЕ ОТРИСОВЫВАЕТСЯ
ВОВСЕ — поднимать нечего. Цепь «гашение при открытии панели → CR-01 закрыт» проходит МИМО условия
подъёма и от его снятия не зависит ни в одном звене: гашение живёт в методе открытия рычага, а
подъём — в таблице стилей, и встречаются они только на видимой заготовке.

**Три положительных замера признака G-10-4** (2026-09-11, стенды ad_id=55 и ad_id=59, слой 70 против
60, прямоугольник целиком в окне, глазной вердикт) настоящей правкой обязаны остаться верными — и
остались: слой не сдвинут, положение вне потока не снято, цепь предков не тронута. ⚠️ ГРАНИЦА
НАЗЫВАЕТСЯ ПРЯМО: правила читают ОБЪЯВЛЕНИЯ таблицы, а не отрисовку.

## Контроли — два доктóривания, и второе не избыточно

| Контроль | Что доктóрится | Ожидание |
|----------|----------------|----------|
| `test_control_a_conditional_lift_reddens` | блоку подъёма возвращается предок-условие — **ПОСТОРОННИМ** признаком `is-some-unrelated-state`, а не `is-modal-open` | ровно одна находка; отказ печатает полученный селектор дословно и называет обе заготовки |
| `test_control_a_lift_declaring_a_display_mode_reddens` | блоку дописывается способ отображения | ровно одна находка, называющая объявленное значение |

**Предметы двух контролей показаны непересекающимися ИСПОЛНЕНИЕМ, а не заявлены:** на копии со
способом отображения правило безусловности остаётся ЗЕЛЁНЫМ, и это утверждается внутри второго
контроля.

**Контроль возврата условности сверх того утверждает, что краснота пришла не из пустоты отбора:** на
доктóренной копии отбор блоков подъёма по-прежнему даёт один блок. Без этого утверждения контроль
зеленел бы и на отборе, снова привязанном к признаку состояния, — то есть доказывал бы ровно тот
вакуум, против которого и заводился.

**Настоящая таблица контролями не правится:** каждый контроль сличает исходник боевой таблицы до и
после своего прогона; `git status --porcelain app/static/css/app.css app/templates` пуст.

## Проверка плана целиком

1. `uv run pytest tests/test_pages/test_shell.py -q` → **`230 passed`**, rc=0 (11:58).
2. `uv run pytest tests/ -q` → **`3182 passed`**, rc=0 (38:20).
3. Переход цвета правила безусловности измерен и приведён обоими состояниями (таблица выше).
4. `git diff --numstat -- app/templates` пуст на всех трёх задачах.
5. Правило закрытия CR-01 и правило порядка слоёв (предмет G-10-4) предъявлены зелёными отдельно.

## Deviations from Plan

**None — план исполнен ровно как написан.** Ни одного авто-исправления по правилам 1-3 не
потребовалось, архитектурных решений (правило 4) не возникало, шлюзов аутентификации не было.

Два уточнения формы, не меняющие ни одного утверждения плана:

1. **`_banner_elevation_findings` переключён на тот же отбор, что и `_banner_elevation_rules`.** План
   требовал сохранить его АРИФМЕТИКУ находок дословно (отсутствие блока — находка на КАЖДУЮ
   заготовку, всё прочее — находка на блок) и это сохранено; но внутренний счёт блоков ключáлся на
   то же снимаемое имя признака, и оставленный как есть он дал бы после правки таблицы «блоков
   подъёма 0» на каждую заготовку, то есть покрасил бы правило порядка слоёв НЕ НА ДЕФЕКТЕ. Оба
   места читают теперь `_selector_lifts_banner`.
2. **Помощник `_doctored_lift_block` вынесен отдельно** — двум контролям нужна одна и та же
   подстановка по БЛОКУ (а не по дословному куску, вбирающему прозу, `REVIEW-8/WR-05`), и вторая её
   копия разошлась бы с первой молча.

## Known Stubs

Заглушек план не внёс ни одной: правок продукта в нём ровно одна — селектор блока подъёма, — и она
исполнена целиком. Пропущенных правил (`skip`), неисполненных `<verify>` и незакрытых `TODO` в
диффе нет; проверено грепом по диффу партии.

## Остаток — ПЯТЬ пунктов, без смягчения

**(1) ДВЕ ЗАГОТОВКИ, ПОКАЗАННЫЕ ОДНОВРЕМЕННО, НАКРЫВАЮТ ДРУГ ДРУГА.** Величины блока подъёма у них
общие, и вторая встаёт ровно в точку первой. Окно было и прежде — при открытой панели, — настоящая
правка его РАСШИРЯЕТ до «всегда, пока обе видимы». Предмета гэпа не касается, ПОЧИНКОЙ ЗДЕСЬ НЕ
ЗАКРЫВАЕТСЯ, владельца не имеет. Реестр угроз: `T-10-51-04`, `accept`; запись 76 `.planning/WINDOWS.md`.

**(2) ОТКАЗ ФОНОВОГО ЗАПРОСА, ПРИШЕДШИЙ ПРИ ОТКРЫТОЙ ПАНЕЛИ**, по-прежнему встаёт поверх неё и
говорит НЕ об этом действии. Окно названо сводкой 10-35 (цена (ii)) и настоящим планом не
закрывается.

**(3) НАСТОЯЩИЙ ОТКАЗ, СТЁРТЫЙ ОТКРЫТИЕМ ПАНЕЛИ ЛИБО УСПЕШНЫМ ФОНОВЫМ ОБМЕНОМ, ОБРАТНО НЕ
ВОЗВРАЩАЕТСЯ.** Два окна одного класса, названные сводками 10-35 (цена (i)) и 10-49 (`T-10-49-05`,
запись 74). Настоящим планом не закрывается ни одно — и заметим прямо: именно ВТОРОЕ из них и сделало
безусловный подъём починкой, а не разменом.

**(4) ОТРИСОВКА ПОСЛЕ ПРАВКИ НЕ СНЯТА НИКЕМ.** Ни одно правило суиты её не утверждает и утверждать не
вправе: правила читают ОБЪЯВЛЕНИЯ таблицы стилей. Шаги **4.4** и **2.8** обхода после настоящей
партии обязаны быть ПЕРЕСНЯТЫ, и `status` артефакта `10-UAT.md` терминальным не становится ни от
одного символа настоящего плана — артефакт обхода этим планом не правится вовсе. Подписи человека на
приёмке как не было, так и нет.

**(5) ⚠️ ПУНКТ `missing[1]` ЗАПИСИ G-10-7 — НЕПОКРЫТ. НЕ «частично», НЕ «по существу закрыт» —
НЕПОКРЫТ.** Дословно:

> «Правило, стерегущее, что прямоугольник плашки отказа лежит в окне при рабочем положении
> прокрутки»

План отгрузил правило на **ОБЪЯВЛЕНИЯ ТАБЛИЦЫ СТИЛЕЙ** (`test_the_failure_banner_lift_is_unconditional`),
а пункт требует правила на **ПРЯМОУГОЛЬНИК**. Это разные предметы, и подмена одного другим была бы
ровно тем классом, которым фаза уже оплатила расхождение `Р-1` (прибор шире предмета) — только в
обратную сторону.

**ОСНОВАНИЕ НЕПОКРЫТИЯ — НЕ «ТРУДНО», А «НЕЧЕМ», И ОНО ЗАМЕРЕНО** (2026-09-11, `d17c594`).
Прямоугольник есть произведение РАСКЛАДКИ на ПОЛОЖЕНИЕ ПРОКРУТКИ; ни того, ни другого в суите нет ни
одного прибора:

- `_css_rules_of` и соседи читают ОБЪЯВЛЕНИЯ таблицы — движка раскладки за ними нет, и величины
  `top`/`scrollY` из объявлений не выводятся ВООБЩЕ;
- `run_node_script` исполняет сценарий на СТАБАХ узлов (`tests/conftest.py`), и геометрии у стаба
  нет: он несёт признак скрытости, который гарнир ему сам и выдал;
- браузерного привода в суите НЕТ НИ ОДНОГО: греп по `pyproject.toml` и `tests/conftest.py` на
  `playwright|selenium|puppeteer|splinter` не даёт НИ ОДНОГО вхождения. Сам гэп и замерен не суитой,
  а chrome-devtools MCP на ПРОДЕ.

**АДРЕСАТА ДВА, И ОБА НАЗВАНЫ ПОИМЁННО:**

1. **Прямоугольник снимает ЧЕЛОВЕК шагом 2.8 обхода** (`10-UAT.md`, проверка 2) ПОСЛЕ настоящей
   партии — тем же приёмом, которым он замерен сегодня.
2. **Заведение браузерного привода в суите есть РЕШЕНИЕ ВЛАДЕЛЬЦА**, а не правка плана гэпа: оно
   стои́т дороже самого гэпа (новая зависимость, новый род прогона, новое время суиты поверх
   сегодняшних 38 минут) и передаётся следующему кругу НАЗВАННЫМ. Пока его нет, класс «правило на
   отрисовку» недостижим для ЛЮБОГО плана этой фазы, а не только для настоящего.

**ЗАПИСЬ G-10-7 ОСТАЁТСЯ `status: failed`.** Сводка не объявляет её закрытой и не предлагает этого
сделать: покрыт `missing[0]` (достижимость записи отказа независимо от того, открыта панель или уже
ушла), не покрыт `missing[1]`. План закрывает **КОРЕНЬ** гэпа, а не его **ЗАПИСЬ**, и разница между
двумя этими вещами написана здесь словами. Запись 77 `.planning/WINDOWS.md`.

## Долг, найденный правкой и НЕ починенный по прямому запрету плана

`app/templates/includes/htmx_error_banner.html:138-146` несёт два утверждения, переставшие быть
верными в момент правки таблицы: (а) «Подъём УСЛОВЕН признаком открытой панели… вне открытой панели
поведение заготовок сегодняшнее»; (б) прежнее имя переименованного правила и число «два правила» при
пяти сегодняшних. **Правка `app/templates/` запрещена перечнем прохибиций настоящего плана** (правка
рычага открыла бы `CR-01`, правка заготовок пересеклась бы с планом 10-49), и запрет принуждается
пустым `git diff --numstat -- app/templates` на всех трёх задачах. Долг записан в
`deferred-items.md` (раздел «План 10-51») и записью 78 `.planning/WINDOWS.md`; владелец — следующий
круг закрытия гэпов Фазы 10.

Ссылки на прежнее имя правила в сводках 10-33 и 10-35 НЕ правятся: они описывают дерево своего
времени (та же идиома, что применил план 10-49 к сводкам 08-04 и 09-04).

## Требование FORM-06

**Выполненным НЕ отмечается.** Требование закрывается подписью человека на приёмке обхода, и
настоящим планом такая подпись поставлена быть не может: шаги 2.8 и 4.4 после этой правки не
переснимались, `status` артефакта обхода остаётся нетерминальным.

## Self-Check: PASSED

- `app/static/css/app.css` — FOUND; `tests/test_pages/test_shell.py` — FOUND;
  `.planning/WINDOWS.md` — FOUND; `deferred-items.md` — FOUND.
- Коммиты `c88cba0`, `34ac774`, `95b5339` — все три FOUND в `git log`.
- `git rev-list --count 39c5ab1..HEAD` = **3** — совпадает с `actuals.commits`.
- Акцептанс всех трёх задач перепрогнан; плановая `<verification>` перепрогнана целиком (числа
  выше).
