---
phase: 10-rychag-components-modal-html
plan: 52
subsystem: testing
tags: [pytest, jinja2, htmx, script-parsing, criterion-enforcement, tdd]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "план 10-49 — третий обработчик `htmx:afterRequest` в `includes/htmx_error_banner.html` и константа `FAILURE_BANNER_HANDLERS_MEASURED = 3` как её единственный источник"
  - phase: 10-rychag-components-modal-html
    provides: "план 10-13 — гейт `test_criterion_three_holds_by_the_numbers` с четырьмя утверждениями и прочтением D-13"
provides:
  - "пятое утверждение гейта критерия 3: регистрации обработчиков в телах блоков `<script>` всех шаблонов, объявленные ПЕРЕЧНЕМ ФАЙЛОВ С ЧИСЛАМИ"
  - "`_script_bodies` — разборщик тел блоков `<script>`; незакрытый блок есть ОТКАЗ, а не пустой результат"
  - "`_strip_js_comments` — строкознающее вырезание комментариев сценария (вторая половина к ввозимому `_strip_comments`)"
  - "`HANDLER_REGISTRATION_FORMS` — две распознаваемые формы регистрации с названной границей доказанного"
  - "два отрицательных контроля: лишняя регистрация у известного файла и НОВЫЙ файл, принёсший регистрацию"
  - "измерение перехода цвета: правило РАЗЛИЧАЕТ дерево до коммита `4e24d61` и дерево после него"
affects: ["10-53 (запись основания числа три — решение владельца)", "10-54 (человеческие замеры)", "фазы 11…15 (любая правка, вносящая сценарий в шаблон)"]

actuals:
  tokens: 10032
  tasks: 3
  commits: 3
plan_head_before: 59e179fc7a2755c96d15218c866129827c09bcc5

tech-stack:
  added: []
  patterns:
    - "разбор ТЕЛА блока `<script>`, а не файла: регистрация в прозе вокруг блока предметом критерия не является"
    - "строкознающее вырезание комментариев сценария — наивное `//.*$` снесло бы хвост строки с адресом"
    - "объявление числа ДВУМЯ слагаемыми: местный перечень плюс ввоз единственного источника"

key-files:
  created: []
  modified:
    - "tests/test_templates/test_components.py"

key-decisions:
  - "D-52-1: RED снят на ОБЪЯВЛЕНИИ, равном замеру базы фазы (`7c1041d`, заготовки — 2), а не на нетронутом дереве: покраснеть на настоящем дереве правило могло бы только ОТКАТОМ третьего обработчика, а откат запрещён — правка закрывает настоящий дефект `G-10-5`"
  - "D-52-2: значение заготовок в объявлении приходит ВВОЗОМ `FAILURE_BANNER_HANDLERS_MEASURED`; литерала в модуле нет, второй копии числа завести нечем"
  - "D-52-3: в перечень попадают ТОЛЬКО файлы с ненулевым числом — нули были бы величинами, которые надо сверять, и сторож завёл бы себе сторожа"
  - "D-52-4: имя второго контроля получило подстроку `handler_registration`, иначе отбор гейта партии собрал бы ТРИ правила вместо четырёх и цвет контроля не мерялся бы ничем (отступление, правило 3)"
  - "D-52-5: ОСНОВАНИЕ числа три (расширенное отступление владельца либо признанное нарушение критерия 3) настоящим планом НЕ записано и не предположено — его вносит план 10-53 после останова на владельце"

patterns-established:
  - "Летопись числа при объявлении по форме `TRANSITION_ASSEMBLY_PLACES`: чем и когда снято, что означает движение В КАЖДУЮ СТОРОНУ"
  - "Отказ печатает ФАЙЛ и ОБА числа при нём, а появившиеся и исчезнувшие файлы — РАЗНИЦЕЙ МНОЖЕСТВ, а не парой итогов (класс находки `IN-03` не повторён)"
  - "Зубы правила ИЗМЕРЯЮТСЯ мутацией разборщика, а не заявляются"

requirements-completed: []

coverage:
  - id: D1
    description: "Регистрация обработчика, добавленная в тело блока `<script>` любого шаблона `app/templates/**`, краснит ПРОГОН и называет файл с обоими числами"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_components.py::test_criterion_three_holds_by_the_numbers"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_components.py::test_control_negative_a_new_handler_registration_reddens_the_gate"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_components.py::test_control_negative_a_new_template_with_a_handler_registration_reddens_the_gate"
        status: pass
    human_judgment: false
  - id: D2
    description: "Правило РАЗЛИЧАЕТ дерево до коммита `4e24d61` (заготовки — 2) и дерево после него (3) — гейт, существовавший бы тогда, правку девятой партии увидел бы"
    verification:
      - kind: other
        ref: 'uv run pytest tests/test_templates/test_components.py -q -k "criterion_three" — RED `1 failed, 87 deselected` rc=1 на объявлении базы фазы (`9042c55`) → GREEN `1 passed, 87 deselected` rc=0 на ввозе единственного источника (`21637ee`); улика проверена `gsd-tools check tdd-red-evidence` → RED_EVIDENCE_OK'
        status: pass
    human_judgment: false
  - id: D3
    description: "Число регистраций заготовок не получило второй копии: значение приходит ввозом единственного источника"
    verification:
      - kind: other
        ref: "чтение диффа `21637ee`: литерал снят, стои́т `from tests.test_pages.test_shell import FAILURE_BANNER_HANDLERS_MEASURED`; `grep` по модулю даёт три вхождения имени и ни одного литерала в объявлении"
        status: pass
    human_judgment: false
  - id: D4
    description: "Зубы пятого утверждения не заявлены, а измерены: обезоруженный разборщик краснит гейт и ОБА контроля"
    verification:
      - kind: other
        ref: "мутация `_script_bodies` → пустой перечень: `3 failed, 1 passed, 86 deselected` (контроль каталога вендоренных сценариев остался зелен); дерево восстановлено"
        status: pass
    human_judgment: false
  - id: D5
    description: "ОСНОВАНИЕ числа три у заготовок — расширенное отступление владельца либо признанное нарушение критерия 3"
    requirement: "FORM-06"
    verification: []
    human_judgment: true
    rationale: "Пункт `missing[0]` записи гэпа одиннадцатого круга. Настоящий план объявляет ЧИСЛО дерева и ловит незаявленное; выбор одной из двух ветвей есть решение ВЛАДЕЛЬЦА, и машина не вправе предположить ни одну из них. Адресат — план 10-53 с остановом `checkpoint:decision`."

duration: 52min
completed: 2026-09-12
status: complete
---

# Phase 10 Plan 52: Пятое утверждение гейта критерия 3 Summary

**Критерий 3 получил принуждение там, где оно было вакуумным: регистрации обработчиков в телах блоков `<script>` всех семи шаблонов считаются ПЕРЕЧНЕМ ФАЙЛОВ С ЧИСЛАМИ, отказ печатает файл и оба числа, а переход цвета измерен исполнением правила на объявлении базы фазы против объявления на ввозе единственного источника.**

## Performance

- **Duration:** 52 min
- **Started:** 2026-09-12T07:47:01Z
- **Completed:** 2026-09-12T08:39:15Z
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments

- **Пятое утверждение заведено и КРАСНИТ ПРОГОН.** До него тело блока `<script>` внутри шаблона не измерялось НИ ОДНИМ из четырёх утверждений гейта — и ровно туда коммит `4e24d61` (план 10-49) внёс семь исполняемых строк JS. Теперь такая правка краснеет на КАЖДОМ прогоне, а не ждёт круга верификации.
- **Переход цвета ИЗМЕРЕН, а не заявлен.** Правило различает дерево до `4e24d61` (заготовки — 2) и дерево после него (3). Это ровно то измерение, которого у фазы не было ни одного дня.
- **Число заготовок не получило второй копии.** Значение приходит ввозом `FAILURE_BANNER_HANDLERS_MEASURED`; расхождение двух копий невозможно ПО ПОСТРОЕНИЮ.
- **Зубы измерены мутацией.** Обезоруженный `_script_bodies` краснит гейт и ОБА контроля; посторонний контроль каталога вендоренных сценариев остаётся зелен — то есть мутация попала в предмет, а не в суиту вообще.
- **Ни одна из двух ветвей решения владельца не предположена.** Докстринг говорит прямо: основание числа вносит план 10-53.

## Task Commits

1. **Задача 1 (RED): трассирующий срез — пятое утверждение сквозь все слои предмета** — `9042c55` (`test`)
2. **Задача 1 (GREEN): значение заготовок приходит ВВОЗОМ единственного источника** — `21637ee` (`feat`)
3. **Задача 2: два отрицательных контроля с границей** — `829e3c0` (`test`)
4. **Задача 3: предъявление среза целиком** — кода не правила ни на символ; её артефакт есть настоящая сводка, и её коммит — коммит метаданных плана.

**Plan metadata:** см. коммит `docs(10-52)`.

## Files Created/Modified

- `tests/test_templates/test_components.py` — +477/-4 строки: разборщик тел блоков `<script>`, вырезание комментариев сценария, перечень распознаваемых форм регистрации, измеренный перечень, объявление с летописью, пятое утверждение в теле гейта, два отрицательных контроля, знаменатель сообщений 4 → 5, запись D-30/D-32 в докстринге.

## Замер среза — ЧИСЛАМИ

### Что меряет разборщик на сегодняшнем дереве

**Шаблонов с блоком `<script>` — СЕМЬ**, и перечень приведён, а не только число:

1. `accounts/connect_tg_user.html`
2. `accounts/partials/connect_status.html`
3. `ads/form.html`
4. `auth_base.html`
5. `base.html`
6. `includes/htmx_config.html`
7. `includes/htmx_error_banner.html`

**Измеренный перечень регистраций — ДВЕ записи:** `{'ads/form.html': 8, 'includes/htmx_error_banner.html': 3}`. Пять из семи шаблонов несут блок сценария без единой регистрации и в перечень не попадают — ровно поэтому объявление есть ПЕРЕЧЕНЬ ФАЙЛОВ, а не один итог.

**Равенство со вырезанием комментариев сценария и БЕЗ него — ЗАМЕРЕНО, а не ожидалось:** оба перечня равны `{'ads/form.html': 8, 'includes/htmx_error_banner.html': 3}`; сличение дало `True`. То есть вырезание сегодня числа не меняет, а дыру закрывает: три десятка строк строчных комментариев внутри блока `ads/form.html` перестали быть тем, что может ЗАМЕНИТЬ собой ушедшую регистрацию.

**Вторая распознаваемая форма (`.onXxx =`) внутри тел блоков — НОЛЬ вхождений** (прогон по дереву). Форма включена не впрок: правило, знающее одну форму, пропустило бы регистрацию, вернувшуюся в другой, — молча, ровно как четыре утверждения пропустили третий обработчик.

**Литералов регулярных выражений внутри тел блоков — НОЛЬ** (прогон по дереву). Это названная граница `_strip_js_comments`: косая внутри такого литерала читается началом комментария, и день, когда первый появится, обязан начаться с правки функции, а не числа.

### Переход цвета — ОБА состояния, с полученным значением RED дословно

| Состояние | Команда | Итоговая строка | rc | Полученное значение |
|---|---|---|---|---|
| **RED** (объявление = замер базы фазы `7c1041d`, коммит `9042c55`) | `uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"` | `1 failed, 87 deselected, 1 warning in 1.02s` | **1** | `УТВЕРЖДЕНИЕ 5 ИЗ 5 … НЕ СОШЛОСЬ.` / `includes/htmx_error_banner.html: найдено 3, объявлено 2` / `появившиеся файлы: []` / `исчезнувшие файлы: []` |
| **GREEN** (объявление на ввозе единственного источника, коммит `21637ee`) | та же команда | `1 passed, 87 deselected, 1 warning in 0.79s` | **0** | перечень измеренного сошёлся с объявленным |

Правило СОБРАЛОСЬ и ИСПОЛНИЛОСЬ в обоих состояниях: итоговая строка RED начинается с `1 failed`, а не `1 error` и не `N deselected`. Отбор `criterion_three` собирает РОВНО ОДНО правило (87 deselected из 88 на момент задачи 1).

### RED-запись, проверенная прибором

`gsd-tools check tdd-red-evidence` → **`RED_EVIDENCE_OK`** (`reason: target_test_failed`, `exit_code: 1`, `tests: 1`, `pass: 0`, `fail: 1`).

```json
{
  "command": "uv run pytest tests/test_templates/test_components.py -q -k \"criterion_three\"",
  "exit_code": 1,
  "failing_test": "tests/test_templates/test_components.py::test_criterion_three_holds_by_the_numbers",
  "target_test": "tests/test_templates/test_components.py::test_criterion_three_holds_by_the_numbers",
  "expected": "{'ads/form.html': 8, 'includes/htmx_error_banner.html': 2}  (объявление, равное замеру базы фазы 7c1041d)",
  "actual": "{'ads/form.html': 8, 'includes/htmx_error_banner.html': 3}  (измерено на дереве 9e6b859)",
  "verdict": "RED_EVIDENCE_OK",
  "reason": "target_test_failed"
}
```

⚠️ **Прибор читает TAP, суита говорит на pytest, и переходник назван, а не умолчан.** Вывод переведён в TAP из `--junitxml` ТОГО ЖЕ прогона (файл в каталоге черновиков, не в дереве), идентификаторы приведены к путевой форме механически — приём и его основание записаны планами 10-36, 10-37 и 10-49 этой же фазы. Код возврата взят у настоящего `pytest`. Переведена СЕРИАЛИЗАЦИЯ замера, а не сочинена улика.

### Зубы — измерены мутацией, а не заявлены

Обезоруженный `_script_bodies` (возврат пустого перечня) при отборе `criterion_three or handler_registration or vendored_script`:

```
3 failed, 1 passed, 86 deselected
FAILED …::test_criterion_three_holds_by_the_numbers
FAILED …::test_control_negative_a_new_handler_registration_reddens_the_gate
FAILED …::test_control_negative_a_new_template_with_a_handler_registration_reddens_the_gate
```

Зелёным остался `test_control_negative_a_new_vendored_script_reddens_the_gate` — то есть мутация попала В ПРЕДМЕТ, а не в суиту вообще. Дерево восстановлено из копии до всякого коммита; в историю мутация не попала.

### Итоговые строки всех прогонов

| Прогон | Команда | Итоговая строка | rc |
|---|---|---|---|
| гейт после GREEN | `pytest tests/test_templates/test_components.py -q -k "criterion_three"` | `1 passed, 87 deselected` | 0 |
| отбор партии (≥4 правил) | `pytest … -k "criterion_three or handler_registration or vendored_script"` | `4 passed, 86 deselected` | 0 |
| модуль целиком | `pytest tests/test_templates/test_components.py -q` | `90 passed, 1 warning in 11.61s` | 0 |
| область партии | `pytest tests/test_templates/test_components.py tests/test_pages/test_shell.py tests/test_planning/ -q` | `366 passed, 117 warnings in 712.45s (0:11:52)` | 0 |
| полная суита с поимённым изъятием | `pytest tests/ -q --deselect tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard` | `3215 passed, 1 deselected, 974 warnings in 2391.45s (0:39:51)` | 0 |

Строк `ERROR: not found` в выводе полного прогона — **ноль**, то есть изъятое имя с деревом не разошлось и изъятие осталось ПОИМЁННЫМ.

### Область прогона названа ПРИЧИНОЙ, а не удобством

- `tests/test_templates/test_components.py` — правимый модуль;
- `tests/test_pages/test_shell.py` — **владелец ввозимой константы**: правило, читающее `FAILURE_BANNER_HANDLERS_MEASURED`, обязано остаться зелёным, иначе ввоз был бы сделан в модуль, который сам красен;
- `tests/test_planning/` ЦЕЛИКОМ — **не перестраховка**: исполнители деселектят планировочные правила, и дефекты планировочных записей видит только явный прогон.

### Изъятие правила записи 79 — ПОИМЁННОЕ, и причина названа

Изъято `tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard`. **Причина:** правило требует равенства двух чисел, равных лишь ЧАСТЬ СУТОК — плитка «Обзора» есть КАТЯЩЕЕСЯ окно 24 часа, дашборд считает КАЛЕНДАРНЫЙ день читателя; между 00:00 и ~05:00 UTC посевы падают за предыдущие сутки и правило краснеет БЕЗ ЕДИНОЙ ПРАВКИ (замер владельца: 02:03 UTC — red, 05:10 UTC — green). Гейт, опирающийся на его зелень, объявлял бы настоящий план неисполнимым ПО ЧАСАМ, а не по дереву.

Изъятие **поимённое, а не по слову**: изъятие по подстроке унесло бы вместе с ним соседние правила админки, и их краснота перестала бы читаться партией. **Предмет записи 79 ВНЕ Фазы 10 и отложен владельцем; настоящий план его НЕ ЧИНИТ и чинить не предлагает.**

### Границы предмета — проверены закреплённой ревизией

| Проверка | Результат |
|---|---|
| `git diff --numstat 9e6b859 -- app` | ПУСТО |
| `git diff --numstat 9e6b859 -- app/templates app/static` | ПУСТО |
| `git diff --numstat 9e6b859 -- app .planning/REQUIREMENTS.md …/10-UAT.md` | ПУСТО |

Ревизия закреплена базой партии и БЕЗ `..HEAD` — сравнение идёт РАБОЧЕГО ДЕРЕВА с базой, поэтому проверка видит правку и до коммита, и после него.

### Прочие замеры, которых требует акцептанс

- **Знаменатель ПЯТЬ** стои́т в **пяти** сообщениях отказа (счёт `grep -c "ИЗ 5 (" = 5`); знаменателя `ИЗ 4` в файле не осталось.
- **Имя `test_criterion_three_holds_by_the_numbers` не изменено**; отбор `-k criterion_three` продолжает его находить — предъявлено прогоном (`1 passed, 87 deselected`).
- **Прежний текст докстринга о четырёх числах ПРИСУТСТВУЕТ и не вычеркнут** (строка «Критерий 3 вехи — ЧЕТЫРЬМЯ ЧИСЛАМИ» и перечень четырёх вещей стоят дословно); рядом встала запись D-30/D-32 о том, что их переросло, с прямым указанием, что основание числа вносит план 10-53.
- **Незакрытый блок сценария даёт ОТКАЗ разборщика**, а не пустой результат (`raise ValueError` в `_script_bodies`) — проверяется чтением диффа `9042c55`.
- **Литерала числа заготовок в модуле НЕТ** после GREEN: три вхождения имени `FAILURE_BANNER_HANDLERS_MEASURED` (комментарий, ввоз, употребление) и ни одного литерала.

## Замер, опровергающий одно число входного контекста

Шапка `.planning/phases/10-rychag-components-modal-html/10-SECURITY.md` на HEAD прочитана ДОСЛОВНО:

```yaml
status: waived
threats_open: 12
asvs_level: 1
waiver_scope: phase-10-only
waiver_applies_to_milestone: false
waived_by: chubav
waived_on: 2026-09-10
waiver_basis: 10-SECURITY-SUBJECT.md
secure_phase_command_run: false
```

То есть `threats_open` равен **12**, а не нулю, и `status` равен `waived`, а не `passed`. **Настоящий план этого артефакта НЕ ПРАВИТ:** поле изъятия принадлежит владельцу и команде безопасности фазы, а не плану закрытия гэпов. Запись сделана потому, что молчаливое согласие с неверным числом есть та же болезнь, против которой заводится пятое утверждение.

## Decisions Made

- **D-52-1.** RED снят на ОБЪЯВЛЕНИИ, равном замеру базы фазы, а не на нетронутом дереве. Обычная форма RED здесь недостижима честно: покраснеть на настоящем дереве правило могло бы только ОТКАТОМ третьего обработчика, а откат запрещён — правка закрывает настоящий дефект `G-10-5` и стережётся двумя контролями плана 10-49.
- **D-52-2.** Значение заготовок приходит ВВОЗОМ единственного источника; ввоз ФУНКЦИОНАЛЬНО-МЕСТНЫЙ по уже применённой в этом модуле дважды форме (`:2869`, `:4276`) — модульный ввоз перенёс бы на все 90 правил файла отказ уровня модуля чужого файла (`REVIEW-8/IN-01`).
- **D-52-3.** В измеренный перечень попадают ТОЛЬКО файлы с ненулевым числом: нули были бы величинами, которые надо сверять, и сторож завёл бы себе сторожа.
- **D-52-4.** Имя второго контроля получило подстроку `handler_registration` — см. «Deviations».
- **D-52-5.** ОСНОВАНИЕ числа три НЕ записано и не предположено: его вносит план 10-53 после останова на владельце.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Имя второго отрицательного контроля выведено в отбор гейта партии**

- **Found during:** Задача 2 (два отрицательных контроля)
- **Issue:** Артефактный перечень плана называет второй контроль `test_control_negative_a_new_template_with_a_registration_reddens_the_gate`, а `<verify>` той же задачи требует, чтобы отбор `-k "criterion_three or handler_registration or vendored_script"` собрал **не менее ЧЕТЫРЁХ** правил, и его `fails_when` называет этот же отказ дословно: «имя одного из новых контролей не попало в отбор и его цвет не меряется ничем». Имя без подстроки `handler_registration` в отбор НЕ ПОПАДАЕТ: замер прогоном дал `3 passed, 87 deselected` — то есть гейт задачи был бы неисполним по плану как написан, а контроль был бы заведён правилом, чью красноту никто не читает.
- **Fix:** Контроль назван `test_control_negative_a_new_template_with_a_handler_registration_reddens_the_gate` (прибавлено одно слово). Основание записано ДОКСТРИНГОМ при самом контроле, а не только здесь: правило, чья краснота никем не читается, есть то же вакуумное принуждение, против которого заводится пятое утверждение.
- **Files modified:** `tests/test_templates/test_components.py`
- **Verification:** отбор собирает `4 passed, 86 deselected`, rc=0.
- **Committed in:** `829e3c0` (коммит задачи 2)

**2. [Rule 1 - Bug] Первое утверждение контроля лишней регистрации читало измеренный перечень по ключу**

- **Found during:** Задача 2
- **Issue:** `measured[FAILURE_BANNER_TEMPLATE]` на обезоруженном разборщике дало бы `KeyError` — то есть ОШИБКУ сбора вместо отказа утверждения с объяснением. Контроль, падающий `KeyError`, не говорит читателю, ЧТО именно сломалось.
- **Fix:** Чтение переведено на `.get(...)`; отказ теперь печатает измеренное против объявленного плюс подсаженное.
- **Files modified:** `tests/test_templates/test_components.py`
- **Verification:** мутация разборщика даёт `AssertionError` с текстом, а не `KeyError`.
- **Committed in:** `829e3c0` (коммит задачи 2)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Обе правки внутри предмета плана и внутри его файла. Ни одно из четырёх действующих утверждений не тронуто, имя гейта не изменено, `app/` не тронут ни на символ. Расширения объёма нет.

## Issues Encountered

Нет. Ни один прогон не потребовал повторной попытки; ни одно утверждение не пришлось ослаблять.

## ОСТАТОК — ВОСЕМЬ ПУНКТОВ, НИ ОДИН НЕ СМЯГЧЁН

1. **Пункт `missing[0]` записи гэпа одиннадцатого круга — РЕШЕНИЕ ВЛАДЕЛЬЦА по критерию 3 — настоящим планом НЕ ЗАКРЫТ и закрыт быть не мог.** Две ветви названы явно: (а) расширить область действующего отступления записью, называющей ТРЕТИЙ файл и ТРЕТИЙ обработчик поимённо, с ценой (окно 74) — тогда критерий 3 закрывается `PASSED (override)`; либо (б) признать критерий 3 нарушенным и назвать, что с этим делать. Закрывает план 10-53 остановом на владельце. Машина не вправе выбрать за него ни одну из двух.
2. **Три человеческих пункта раздела «Human Verification Required» `10-VERIFICATION.md` — не сдвинуты ни на волос:** шаг 2.8 обхода (прямоугольник плашки в окне — `missing[1]` записи `G-10-7`, окно 77); шаг 4.4 обхода (перезамер после снятия условия подъёма); **подпись человека на приёмке обхода**. Их адресат — план 10-54 и человек. **`FORM-06` закрывается ПОДПИСЬЮ, а не прогоном.**
3. **Запись 75 журнала окон (`open`)** — перечень ПРОЧИХ путей, оставляющих сводку объявления устаревшей, не снимался: план 10-50 закрыл только фрагментную ветвь удаления расписания (`G-10-6`), и отсутствие перечня за отсутствие путей не выдаётся.
4. **Запись 76 журнала окон (`open`, `T-10-51-04`, disposition `accept`, владельца нет)** — две заготовки, показанные одновременно, накрывают друг друга ТЕПЕРЬ ВСЕГДА; тождественна находке ревизии `IN-03` о совмещённом блоке величин в таблице стилей.
5. **Запись 78 журнала окон (`open`)** — проза `app/templates/includes/htmx_error_banner.html:130-149` разошлась с деревом ДВУМЯ утверждениями (условность подъёма; переименованное правило `…_above_the_panel_while_it_is_open` → `…_above_the_panel`); настоящий план `app/templates/` не правит, и потому расхождение остаётся.
6. **Запись 79 журнала окон (`open`)** — недетерминированность суиты по времени суток; предмет вне Фазы 10, отложен владельцем, изъят из полного прогона ПОИМЁННО. Настоящим планом не чинится.
7. **Запись 74 журнала окон (`open`, `T-10-49-05`, `accept`)** — четвёртое окно видимости отказа: успешный ФОНОВЫЙ обмен гасит непрочитанный настоящий отказ; тождественна находке ревизии `IN-02`, и она же есть предмет ребра `concurrency`, названного во frontmatter плана допущением (`status: unresolved`).
8. **Пункт 4 «Human Verification Required» — решение владельца по запретам фазы.** Шапка `10-PROHIBITIONS-SUBJECT.md` на HEAD: `prohibitions_unresolved_total: 200`, `prohibitions_fully_enforced: 0`, `permit_scope: seventh-batch-only`, `permit_applies_to_phase: false`, `permit_covers_prohibitions: 39`. Сверх них — запреты настоящей партии. ⚠️ Числа ОБЩЕГО счёта запретов в двух артефактах РАСХОДЯТСЯ и здесь не примиряются: `10-VERIFICATION.md` говорит «по каждому из 221 запрета», `10-PROHIBITIONS-SUBJECT.md` — «200 из 239 запретов фазы». Реестр настоящим планом **НЕ ПРАВИТСЯ**: правка его была бы вердиктом вместо владельца.

**Плюс, сверх восьми, — находка ревизии `IN-01`** (`tests/test_templates/test_htmx_markup_gates.py`, инвентарь внеполосных блоков поднят числом, отказ печатает два числа вместо разницы множеств): вне объёма партии. Настоящий план её КЛАСС в своём правиле не повторяет (отказ печатает разницу множеств), но самой находки **НЕ ЗАКРЫВАЕТ**.

### Сказано прямо, без смягчения

- **`G-10-7` остаётся `status: failed`.** Настоящим планом не тронут.
- **`FORM-06` НЕ ОТМЕЧЕН ВЫПОЛНЕННЫМ.** Ни флажок `.planning/REQUIREMENTS.md:31`, ни клетка `:136` (`Gaps Found`) не двигались; `requirements-completed` настоящей сводки ПУСТ намеренно. Требование закрывается подписью человека на приёмке обхода.
- **`10-UAT.md` настоящим планом НЕ ПРАВЛЕН**, поле `status` его шапки не двигалось, ни одна отметка прохождения в нём не ставилась. Проверено закреплённой ревизией (дифф пуст).
- **Третий обработчик в `app/templates/includes/htmx_error_banner.html` НЕ ОТКАЧЕН и не правлен ни на символ.**

## Журнал окон (`.planning/WINDOWS.md`)

Новых записей настоящий план не заводит: заглушек нет, пропущенных правил нет, непрогнанных `<verify>` нет. Оба отступления выше **устранены внутри плана** и открытыми дефектами не являются — открытых пунктов, требующих записи, партия не породила.

## User Setup Required

None — внешней конфигурации план не требует.

## Next Phase Readiness

- **Готово к плану 10-53** (запись основания числа три — `checkpoint:decision` на владельце ПЕРЕД задачей, которая его записывает). План 10-53 предъявляет владельцу то, что ИЗМЕРЕНО правилом, а не пересказано отчётом: перечень `{'ads/form.html': 8, 'includes/htmx_error_banner.html': 3}` и переход цвета обоими состояниями.
- **Готово к плану 10-54** (человеческие замеры) — он опирается на те же числа.
- **Блокер, остающийся открытым:** решение владельца по критерию 3. До него `10-VERIFICATION.md` держит критерий 3 в `status: failed`, и настоящий план этого не меняет — он лишь делает следующую такую правку ВИДИМОЙ прогону.

## Self-Check: PASSED

- `tests/test_templates/test_components.py` — FOUND на диске.
- Коммиты `9042c55`, `21637ee`, `829e3c0` — FOUND в `git log --oneline --all`.
- Гейт TDD: `test(10-52)` предшествует `feat(10-52)` в истории ветки; последовательность RED → GREEN соблюдена.
- `git rev-list --count 59e179fc..HEAD` = 3 — совпадает с `actuals.commits`.

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-12*
