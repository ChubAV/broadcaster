---
phase: 10-rychag-components-modal-html
plan: 12
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/pages/schedules.py
  - tests/test_pages/test_editor_schedules.py
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер. Состояние калибровки СНЯТО
  # ПЛАНИРОВЩИКОМ, а не взято из памяти: `estimate-calibration` → sample_count 0,
  # factor 1, applied false, min_samples 3. Поэтому tokens и raw_tokens совпадают,
  # а confidence: low ВЫВЕДЕН ИЗ ЧИСЛА ЗАМЕРОВ (их ноль), а не из самооценки.
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ГРАНИЦА ВЕЛИЧИНЫ ИДЕНТИФИКАТОРА СТОИТ НА ГРАНИЦЕ ПРИЛОЖЕНИЯ, А НЕ НА ОДНОМ ЗАМЕРЕННОМ ПОЛЕ: значение, лежащее вне диапазона колонки, отвергается ДО того, как обработчик получил управление, и потому не может доехать до драйвера БД НИ ОДНИМ из путей — ни телом формы, ни адресом запроса"
    - "ВСЕ ПЯТЬ ВХОДОВ ФАЙЛА, ДОЕЗЖАЮЩИХ ДО СРАВНЕНИЯ SQL, ОГРАНИЧЕНЫ ОДНОЙ ВЕЛИЧИНОЙ ИЗ ОДНОГО МЕСТА: идентификатор пути удаления, идентификатор пути правки, идентификатор пути тумблера, обязательное поле объявления на создании и необязательное поле аккаунта на создании и правке"
    - "ПРАВИЛО, СТЕРЕГУЩЕЕ ЭТО, ДОСТАЁТ ДО ДЕФЕКТА, И ЕГО СПОСОБНОСТЬ ДОСТАТЬ ПОКАЗАНА, А НЕ ЗАЯВЛЕНА: перечень пяти маршрутов прогоняется величиной ВНЕ int32 и на дереве ДО правки краснеет ВСЕМИ ПЯТЬЮ строками; вывод покрасневшего прогона приводится дословно"
    - "ПЯТЬ МАРШРУТОВ УТВЕРЖДАЮТСЯ ПЕРЕЧНЕМ, А НЕ ПО ОДНОМУ: покрасневшее правило показывает ВСЕ несогласные строки одним отображением, иначе шестой вход того же класса чинится по одному за прогон — та самая форма отказа, которой фаза провалена третьим кругом"
    - "СМЕЖНОСТЬ ГРАНИЦЫ УТВЕРЖДЕНА С ОБЕИХ СТОРОН: величина, равная верхней границе колонки, принимается; величина на единицу больше — отвергается. Граница, проверенная только изнутри, зеленела бы на помощнике, не отвергающем ничего"
    - "НЕОТЛИЧИМОСТЬ ПОВТОРА ПОДТВЕРЖДЁННОГО УДАЛЕНИЯ ПЕРЕЖИВАЕТ ГРАНИЦУ: правила повтора и совпадения ветвей, заведённые планами 10-06 и 10-11, зелены ПОСЛЕ правки тем же прогоном, что и ДО неё, и числа прошедших названы обоими прогонами"
    - "ГРАНИЦА НЕ ОТВЕРГАЕТ ГОДНОГО: живой идентификатор своей записи по-прежнему проходит все пять маршрутов и даёт прежнюю форму ответа — антивакуум показан, а не обещан"
    - "РАСПОРЯЖЕНИЕ НЕГОДНОЙ ВЕЛИЧИНОЙ РАЗЛИЧАЕТСЯ ПО РОЛИ ЗНАЧЕНИЯ, И ЭТО ЗАПИСАНО: обязательный вход маршрута отвечает отказом валидации, необязательное поле КОНТЕКСТА экрана по-прежнему ОТБРАСЫВАЕТСЯ без потери действия (решение плана 10-08 не отменяется). Две разные формы у одной величины — решение, а не расхождение, и основание каждой названо"
    - "ШЕСТОЙ ВХОД ТОГО ЖЕ КЛАССА НЕ УЕЗЖАЕТ НЕОГРАНИЧЕННЫМ МОЛЧА: статический гейт читает сигнатуры маршрутов файла и краснеет на идентификаторе, объявленном без общей границы; счёт ограниченных входов объявлен числом с летописью"
    - "ДОКСТРИНГ, УТВЕРЖДАВШИЙ ПОКРЫТИЕ МАРШРУТА, ПРИВЕДЁН К ИЗМЕРЕННОМУ: прежняя формулировка помечена ОПРОВЕРГНУТОЙ рядом с истинной, а не вычеркнута (идиома D-30/D-32) — она называла маршрут покрытым, и читатель, поверивший ей, был обманут дважды"
  artifacts:
    - "app/pages/schedules.py — общая граница величины идентификатора под именем, называющим её предмет (КОЛОНКУ, а не объявление), с летописью переименования и основанием, СНЯТЫМ С МОДЕЛЕЙ"
    - "app/pages/schedules.py — типы-псевдонимы ограниченного идентификатора для пути и для полей формы; применены во всех четырёх сигнатурах маршрутов"
    - "app/pages/schedules.py — суженный докстринг помощника поля контекста с помеченной опровергнутой прежней формулировкой"
    - "tests/test_pages/test_editor_schedules.py — перечень пяти маршрутов с их входами и правило «ни один не отвечает отказом обработчика» одним отображением"
    - "tests/test_pages/test_editor_schedules.py — правило смежности границы (величина границы принимается, величина на единицу больше отвергается)"
    - "tests/test_pages/test_editor_schedules.py — статический гейт сигнатур с объявленным числом ограниченных входов и его летописью"
  key_links:
    - "общая граница → четыре сигнатуры маршрутов: единственное, чем пятый и шестой входы того же класса не расходятся с первым; ломается — граница снова стоит там, куда смотрели, и молчит там, куда не смотрели"
    - "перечень пяти маршрутов → прогон: единственное, чем правило способно ДОСТАТЬ до дефекта; ломается — константа перечня уезжает внутрь диапазона, за которым дефект живёт, и правило зеленеет одинаково на починенном дереве и на сломанном"
    - "статический гейт сигнатур → перечень маршрутов файла: единственное, чем НОВЫЙ маршрут не приезжает с неограниченным идентификатором; ломается — правило поведения стережёт пять известных входов, а шестой заводится молча"
  prohibitions:
    - statement: "зелёная суита НЕ предъявляется как довод закрытия: правило, чья константа выбрана внутри диапазона, за которым живёт дефект, зеленеет одинаково на починенном дереве и на сломанном, и предъявление такого прогона доводом есть расхождение ЗАПИСИ с деревом"
      status: flagged-unverified
      verification: none
    - statement: "правка НЕ ставится только там, где предмет замерен: закрытие одного из двух источников одной величины оставляет маршрут ровно в том состоянии, в каком гэп был открыт, и это уже произошло дважды в этой фазе"
      status: flagged-unverified
      verification: none
    - statement: "прежняя формулировка докстринга НЕ вычёркивается: в части поля формы она верна и правка по ней исполнена корректно; ложна лишь её часть о покрытии маршрута, и помечается ОПРОВЕРГНУТОЙ рядом с истинной, а не стирается"
      status: flagged-unverified
      verification: none
    - statement: "решение плана 10-08 об ОТБРАСЫВАНИИ негодного поля КОНТЕКСТА не отменяется этой правкой и не подменяется отказом обработчика: испорченное поле контекста не повод потерять само действие, и третья форма ответа, различимая по величине поля, вернула бы карту занятых идентификаторов (T-10-07)"
      status: flagged-unverified
      verification: none
  # ⚠️ РЕБРА ЗОНДА ПОКРЫТИЯ (`edge-probe` по FORM-06, все пять пришли `unresolved`).
  # Из пяти ДВА разрешены здесь ЯВНЫМ критерием приёмки (они выписаны выше обычными
  # строками `truths`): `adjacency` — граница `2_147_483_647` и её сосед на единицу
  # больше; `idempotency` — неотличимость повтора переживает границу. Ребро `empty`
  # разрешено планом 10-13 (ветка удаления ПОСЛЕДНЕГО расписания). Рёбра `ordering` и
  # `concurrency` НЕ разрешены: защитимого машинного критерия у них в этой партии нет,
  # и они вынесены ЯВНЫМИ ДОПУЩЕНИЯМИ в плане 10-16, а не отброшены молча.
  # РАВЕНСТВО БЕЗ МОЛЧАЛИВЫХ ПОТЕРЬ: 5 предъявленных = 3 внесённых + 2 явных допущения.
---

<objective>
Закрываются ДВА блокера отчёта верификации третьего круга — `CR-01` (унаследованный незакрытым) и
`CR-02` (новый, того же корня): величина идентификатора, лежащая вне диапазона колонки, доезжает до
драйвера БД по ПЯТИ входам файла `app/pages/schedules.py` и роняет обработчик пятисоткой.

Purpose: у гэпа один корень и два симптома, и предыдущая партия закрыла ровно тот источник
величины, на который смотрел отчёт. Верификатор назвал форму отказа прямо: «правка закрыла предмет
там, где на него смотрели, и не закрыла там, куда не смотрели». Поэтому предмет здесь — не пять
правок, а ОДНА граница с ОДНИМ владельцем, применённая на границе приложения, плюс правило,
способное ДОСТАТЬ до дефекта, и статический гейт, не пускающий шестой вход того же класса.

Output: общая граница величины с основанием, снятым с моделей; типы-псевдонимы, применённые во всех
четырёх сигнатурах; перечень пяти маршрутов, покрасневший ДО правки всеми пятью строками и зелёный
после; правило смежности границы; статический гейт сигнатур с объявленным числом.

⚠️ ЭТОТ ПЛАН — ЗАКРЫТИЕ ГЕЙПОВ (`gap_closure: true`), ТРЕТЬЯ ПАРТИЯ, ВОЛНА 1. Планы 10-01…10-11
исполнены, закоммичены и НЕ ПРАВЯТСЯ ни на символ; их сводки — запись сделанного.

⚠️ `10-RESEARCH.md` И `10-VALIDATION.md` У ФАЗЫ НЕТ ПО ЯВНОМУ ВЫБОРУ ОПЕРАТОРА: режим `--gaps`
разведку не запускает, и оператор выбрал продолжить без неё. Восьмое измерение Найквиста этой
партией НЕ ЗАКРЫВАЕТСЯ, и записано это здесь, а не обнаруживается следующим кругом.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-08-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Создаётся ЭТИМ планом (проход source-grounding обязан считать новым и не требовать подтверждения
дрейфа):

- `ID_MAX` — общая верхняя граница величины идентификатора, `app/pages/schedules.py`
  (переименование действующего `_AD_ID_MAX`; летопись имени пишется здесь)
- `ScheduleIdPath` — тип-псевдоним ограниченного идентификатора ПУТИ, там же
- `AdIdForm` — тип-псевдоним ограниченного обязательного идентификатора объявления из ФОРМЫ, там же
- `AccountIdForm` — тип-псевдоним ограниченного необязательного идентификатора аккаунта из ФОРМЫ,
  там же
- `UNBOUNDED_ROUTE_CASES` — перечень пяти входов «маршрут × имя входа × способ передачи»,
  `tests/test_pages/test_editor_schedules.py`
- `OUT_OF_COLUMN_RANGE` — величина вне диапазона колонки, подаваемая перечню, там же
- `test_no_schedule_route_answers_with_a_handler_failure_on_an_out_of_range_identifier` — правило
  перечня одним отображением, там же
- `test_the_column_bound_admits_its_own_value_and_refuses_the_next_one` — правило смежности, там же
- `BOUNDED_ROUTE_INPUTS_DECLARED` — объявленное число ограниченных входов файла, там же
- `test_every_identifier_input_of_the_schedule_routes_carries_the_shared_bound` — статический гейт
  сигнатур, там же
- `_route_signature_sources` — разборщик сигнатур маршрутов, принимающий ИСХОДНИК параметром (для
  группы контроля), там же

Существует ДО плана и только ПРАВИТСЯ (новым не является): `_AD_ID_MAX`, `_ad_id_from_form`,
`schedules_create`, `schedules_update`, `schedules_toggle`, `schedules_delete`, `_owns_ad`,
`_ownership_verdict`, `authed_client`, `owner`, `_seed_ad`, `FORM_HEADERS`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: одна граница — один владелец — пять входов, сквозным срезом от константы до ответа маршрута</name>

  <files>
app/pages/schedules.py
tests/test_pages/test_editor_schedules.py
  </files>

  <read_first>
- `app/pages/schedules.py:1-10` — блок ввоза. Ни `Path`, ни `Annotated` сегодня не ввозятся; имя
  `Path` в модуле свободно (`pathlib` здесь не ввозится вовсе), поэтому столкновения имён нет.
- `app/pages/schedules.py:37-47` — комментарий-обоснование действующей границы `_AD_ID_MAX` ЦЕЛИКОМ.
  ⚠️ ЧИТАТЬ ДОСЛОВНО: основание уже сформулировано как «верхняя граница КОЛОНКИ идентификатора
  (знаковое тридцатидвухбитное целое)», то есть свойство КОЛОНКИ, а не объявления. Имя константы
  этому основанию противоречит, и расхождение имени с основанием — половина причины, по которой
  граница осталась на одном входе.
- `app/pages/schedules.py:270-307` — докстринг `_ad_id_from_form` ЦЕЛИКОМ и тело помощника. Пункт
  (в) утверждает, что граница «СТОИТ ДО ЗАПРОСА», пункт (г) называет маршрут подтверждённого
  удаления предметом закрытия. Оба утверждения читатель принимает как «маршрут покрыт»; замер
  верификатора показал обратное.
- `app/pages/schedules.py:726-740` — сигнатура `schedules_create`: обязательное поле объявления и
  необязательное поле аккаунта, оба без границы величины.
- `app/pages/schedules.py:757-762` — вызов `_ownership_verdict` ДО любой записи. Два из пяти отказов
  падают ИМЕННО здесь, то есть роняется вычисление вердикта о владении, а не выборка.
- `app/pages/schedules.py:819-829` — сигнатура `schedules_update`: идентификатор пути плюс те же два
  поля формы.
- `app/pages/schedules.py:833-840` — запрос правки; кадр `:834` из замера верификатора.
- `app/pages/schedules.py:900-920` — сигнатура и запрос `schedules_toggle`; кадр `:911`.
- `app/pages/schedules.py:951-970` — сигнатура и запрос `schedules_delete`; кадр `:967`. ⚠️ ЭТО
  ТОТ САМЫЙ МАРШРУТ, на котором блокер был открыт и на котором он воспроизведён ЗАНОВО.
- `app/pages/schedules.py:438-482` — `_owns_ad` и `_ownership_verdict`: оба принимают величину без
  проверки и уносят её в сравнение.
- `app/models/ad.py:13`, `app/models/schedule.py:12`, `app/models/messenger_account.py:12` — все три
  идентификатора объявлены одним и тем же отображаемым целым без указания расширенной разрядности.
  ⚠️ ОСНОВАНИЕ ГРАНИЦЫ СНИМАЕТСЯ ОТСЮДА, А НЕ КОПИРУЕТСЯ ЧИСЛОМ: величина вторична, первично то,
  что все три колонки одного типа и одной разрядности.
- `tests/test_pages/test_editor_schedules.py:1-40` — модульный докстринг ЦЕЛИКОМ. ⚠️ ОН И ЕСТЬ
  ВЛАДЕЛЕЦ ИНВАРИАНТА: «прямой POST мимо браузера обязан давать отказ валидации, а не 500
  (T-02-24, T-02-25)». Правило пяти маршрутов заводится ЗДЕСЬ именно поэтому — инвариант и его
  принуждение обязаны жить в одном месте.
- `tests/test_pages/test_editor_schedules.py:42-70` — `FORM_HEADERS`, фикстура `owner`, помощник
  `_seed_ad`. Расстановка нового правила собирается ИМИ, своих посевов не заводится.
- `tests/test_pages/test_editor_schedules.py:170-260` — образцы правил четырёх маршрутов: как
  берётся `authed_client`, как шлётся тело, как утверждается адрес приземления.
- `tests/test_pages/test_editor_schedules.py:685-740` — действующие правила T-02-24 и T-02-25:
  форма утверждения «отказ валидации, а не 500» на величинах, не приводящихся к типу. Новое
  правило — их продолжение на ось ВЕЛИЧИНЫ, и форма наследуется.
- `tests/conftest.py:125-137` — фикстура `authed_client`: cookie обычного пользователя, страничные
  маршруты читают именно её.
  </read_first>

  <precondition>
Дерево на HEAD после коммита `c899f04`. Базовые замеры планировщика, от которых считается прибавка:
`uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly` → `54 passed`;
`uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly` → `57 passed`.
Расхождение с этими числами ДО первой правки означает, что дерево не то, на котором план собран, —
это ОСТАНОВ с записью, а не повод пересчитать ожидание.
  </precondition>

  <behavior>
СРЕЗ ВЕРТИКАЛЬНЫЙ И СКВОЗНОЙ: константа → тип-псевдоним → четыре сигнатуры → настоящий POST по
пяти входам → форма ответа. Ни один слой не остаётся заглушкой, и правило проходит путь целиком.

**RED (первый ход, и он обязателен).**
1. Перечень пяти входов и правило перечня заводятся ПЕРВЫМИ и прогоняются на НЕПРАВЛЕНОМ дереве.
2. Ожидаемый цвет — КРАСНЫЙ, и краснеть обязаны ВСЕ ПЯТЬ строк одним отображением: маршрут
   удаления, маршрут правки, маршрут тумблера, поле объявления на создании, поле аккаунта на
   создании. Красный по одной строке за прогон означает, что отображение собрано неверно, и это
   правится ДО перехода к зелёному.
3. Вывод покрасневшего прогона снимается ДОСЛОВНО и уезжает в сводку. Это единственное
   доказательство того, что правило ДОСТАЁТ до дефекта; правило, не показанное красным, от
   правила, неспособного покраснеть, не отличается ничем.

**GREEN.**
4. Величина, лежащая вне диапазона колонки, отвергается на границе приложения — ДО того, как тело
   обработчика получило управление, и потому ДО любого обращения к базе, включая вычисление
   вердикта о владении.
5. Все пять строк перечня зеленеют ОДНИМ прогоном. Форма ответа на негодную величину — отказ
   валидации (код `422`), то есть ровно то, что модуль правил редактора себе и объявил.
6. АНТИВАКУУМ, и без него зелёное ничего не значит: живой идентификатор своей записи по-прежнему
   проходит каждый из пяти входов и даёт ПРЕЖНЮЮ форму ответа. Помощник, отвергающий всё, покраснел
   бы здесь.
7. Правила T-02-24 и T-02-25, стоящие в этом же файле, остаются зелёными: ось ВЕЛИЧИНЫ прибавлена к
   оси ТИПА, а не подменила её.
  </behavior>

  <action>
**1. Перечень пяти входов — заводится и краснеет ПЕРВЫМ.**

В `tests/test_pages/test_editor_schedules.py` завести константу `OUT_OF_COLUMN_RANGE` — величину
вне диапазона колонки, взятую ТЕМ ЖЕ способом, каким её берёт действующий обход транспортов:
строкой из двадцати пяти девяток, а не числом. Основание записать у самой константы: приведение к
числу на стороне теста спрятало бы предмет, потому что предмет — именно то, что до приведения на
стороне приложения значение доезжает.

Завести `UNBOUNDED_ROUTE_CASES` — перечень из ПЯТИ записей, по одной на каждый вход, названный
отчётом верификации. Каждая запись несёт: имя случая для отображения, способ передачи величины
(адрес запроса либо тело формы), шаблон адреса и тело. Пять записей поимённо:
идентификатор пути маршрута удаления; идентификатор пути маршрута правки; идентификатор пути
маршрута тумблера; обязательное поле объявления маршрута создания; необязательное поле аккаунта
маршрута создания. У маршрутов правки и создания тело обязано нести ГОДНЫЙ идентификатор
объявления, иначе случай проверял бы не тот вход, который назван его именем.

Завести правило `test_no_schedule_route_answers_with_a_handler_failure_on_an_out_of_range_identifier`.
Оно прогоняет ВЕСЬ перечень в цикле, копит исходы в отображение «имя случая → код ответа» и
сличает отображение ЦЕЛИКОМ с ожидаемым. ⚠️ ИМЕННО ОТОБРАЖЕНИЕМ, А НЕ УТВЕРЖДЕНИЕМ НА КАЖДОМ
ШАГЕ: правило с ранним отказом показывает одну несогласную строку из пяти, и следующий круг чинил
бы вход за входом, воспроизводя ровно ту форму отказа, которой фаза провалена. Текст отказа обязан
называть ВСЕ несогласные строки и напоминать, что предмет — инвариант T-02-24 / T-02-25.

Прогнать. Снять вывод ДОСЛОВНО. Ожидание: пять строк с кодом отказа обработчика против пяти
ожидаемых кодов отказа валидации.

**2. Одна граница — один владелец.**

В `app/pages/schedules.py` переименовать действующую границу в `ID_MAX`. Имя меняется потому, что
предмет у величины ШИРЕ прежнего имени: основание, уже записанное в комментарии, называет предметом
КОЛОНКУ идентификатора, а не объявление, и все три колонки, участвующие в маршрутах файла,
объявлены одним отображаемым целым одной разрядности. Это проверить ЧТЕНИЕМ трёх моделей и
записать как ИЗМЕРЕНИЕ, а не как перенос числа.

Летопись имени записать у самой константы по форме, которой фаза уже писала летописи чисел: прежнее
имя названо, причина его смены названа, и прямо сказано, что прежнее имя НЕ БЫЛО ОШИБКОЙ — оно
было верным для единственного входа, который тогда ограничивался, и пережило появление четырёх
остальных.

Завести три типа-псевдонима поверх общей границы: `ScheduleIdPath` для идентификатора, приходящего
адресом запроса; `AdIdForm` для обязательного идентификатора объявления, приходящего телом формы;
`AccountIdForm` для необязательного идентификатора аккаунта, приходящего телом формы. Нижняя
граница у всех трёх — единица (идентификаторы автоинкремента с единицы и начинаются; ни ноль, ни
отрицательные в домене не лежат — основание уже записано у действующего помощника и переносится
дословно). Верхняя — общая константа. У необязательного псевдонима пустое значение остаётся
законным состоянием: схема допускает отвязанное расписание, и граница величины к отсутствию
значения не применяется.

⚠️ ТРИ ПСЕВДОНИМА, А НЕ ОДИН, И ЭТО РЕШЕНИЕ: способ передачи величины — свойство МАРШРУТА, и один
псевдоним на все входы либо потребовал бы от вызывающих выбирать способ на месте (то есть вернул бы
решение туда, откуда его забирают), либо молча принял бы поле формы там, где ожидается адрес.
Общей у трёх остаётся ВЕЛИЧИНА — ровно то единственное место, которого требует отчёт.

**3. Четыре сигнатуры.**

Применить псевдонимы во всех четырёх обработчиках: маршрут создания получает ограниченные поля
объявления и аккаунта; маршрут правки — ограниченный идентификатор пути и те же два поля; маршрут
тумблера — ограниченный идентификатор пути; маршрут удаления — ограниченный идентификатор пути.
Тела обработчиков не трогаются НИ ОДНОЙ строкой: предмет правки — граница приложения, а не логика.

⚠️ ЕСЛИ ОГРАНИЧЕНИЕ ВЕЛИЧИНЫ НА ПОЛЕ ФОРМЫ НЕ СРАБОТАЕТ (перечень остаётся красным на двух
строках маршрута создания, а на трёх строках путей зеленеет) — это НЕ повод снять требование.
Запасной ход назван заранее и им же ограничен: та же общая константа применяется ЯВНОЙ проверкой
в теле маршрута создания и маршрута правки ДО вызова вычисления вердикта о владении, с записью у
места проверки о том, почему декларативный путь здесь не сработал. Величина при этом остаётся
ОДНА и берётся из ОДНОГО места — требование отчёта держится обоими ходами.

**4. Помощник поля контекста остаётся при своём распоряжении.**

`_ad_id_from_form` продолжает ОТБРАСЫВАТЬ негодную величину, а не отказывать обработчику. Решение
плана 10-08 не отменяется: испорченное поле КОНТЕКСТА экрана не повод потерять само действие, а
третья форма ответа, различимая по величине поля, вернула бы карту занятых идентификаторов
(T-10-07). Помощник обязан читать ТУ ЖЕ общую константу — второй экземпляр величины разошёлся бы с
первым при первой же правке.

Разницу распоряжений записать ЯВНО у обоих мест: обязательный вход маршрута отвечает отказом
валидации, необязательное поле контекста отбрасывается. Различие по РОЛИ значения, а не по типу
величины, и без записи оно читалось бы как расхождение.

**5. Докстринг помощника поля контекста — сузить до измеренного.**

Пункт (г) действующего докстринга называет предметом закрытия маршрут подтверждённого удаления
целиком; замер верификатора показал, что закрыт был только один из двух источников величины на
этом маршруте. Прежнюю формулировку ПОМЕТИТЬ ОПРОВЕРГНУТОЙ рядом с истинной — не вычёркивать
(идиома D-30/D-32 `STATE.md`). Истинная формулировка называет предметом ПОЛЕ КОНТЕКСТА и прямо
отсылает к общей границе как к месту, закрывающему остальные входы.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо число прошедших меньше 54 (базовый замер планировщика до правки), либо в выводе отсутствует имя правила перечня пяти маршрутов</fails_when>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо число прошедших отличается от 57 (базовый замер планировщика): граница величины не имеет права ни уронить, ни изменить ни одного действующего случая обхода транспортов</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>любой вывод на стандартный поток ошибок либо ненулевой код возврата</fails_when>
  </verify>

  <acceptance_criteria>
- **КРАСНЫЙ ПОКАЗАН, А НЕ ЗАЯВЛЕН.** В сводке приведён ДОСЛОВНЫЙ вывод правила перечня на дереве ДО
  правки, и в нём видны ВСЕ ПЯТЬ строк с кодом отказа обработчика. Прогон, начавшийся с зелёного,
  к слиянию не принимается: правило, не показанное красным, неотличимо от правила, неспособного
  покраснеть.
- **ПЯТЬ СТРОК ОДНИМ ОТОБРАЖЕНИЕМ.** Покрасневший вывод содержит все пять несогласных строк в
  ОДНОМ сообщении об отказе, а не одну строку и остановку.
- Все пять строк зелены после правки; в сводке приведён вывод зелёного прогона правила перечня.
- Основание границы СНЯТО С МОДЕЛЕЙ: в сводке названы три файла моделей и вид объявления
  идентификатора в каждом; сказано прямо, что все три одной разрядности. Перенос числа без этого
  чтения не принимается.
- Летопись имени константы записана у самой константы: прежнее имя названо, причина смены названа,
  и прямо сказано, что прежнее имя не было ошибкой.
- Общая величина существует в ОДНОМ экземпляре:
  `grep -vE '^\s*#' app/pages/schedules.py | grep -c '2_147_483_647'` → `1`.
- Все четыре сигнатуры несут псевдонимы:
  `grep -vE '^\s*#' app/pages/schedules.py | grep -cE 'ScheduleIdPath|AdIdForm|AccountIdForm'` → не
  менее `7` (три идентификатора пути, два поля объявления, два поля аккаунта).
- АНТИВАКУУМ ПОКАЗАН: в сводке приведён исход прогона на ГОДНОМ идентификаторе по каждому из пяти
  входов — форма ответа прежняя, отказа валидации нет.
- Тела обработчиков не тронуты:
  `git diff -U0 -- app/pages/schedules.py | grep -cE '^[-+]\s+(result|verdict|schedule) ='` → `0`.
  Если правка тел потребовалась (сработал запасной ход) — она названа в сводке ПОИМЁННО вместе с
  выводом, по которому декларативный путь признан не сработавшим.
- Прежняя формулировка докстринга ПОМЕЧЕНА ОПРОВЕРГНУТОЙ и стоит рядом с истинной; в сводке
  приведён её текст до и после.
- `uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly` — зелено, число
  прошедших названо и сопоставлено с базовым `54 passed`; прибавка объяснена числом заведённых
  случаев.
  </acceptance_criteria>

  <reversibility rating="costly">
Форма ответа на негодную величину меняется у ПЯТИ входов сразу — с отказа обработчика на отказ
валидации. Откат — правка одного объявления псевдонимов, но наблюдаемое поведение пяти публичных
маршрутов до отката успевает уехать к любому клиенту. Из интерфейса путь недостижим (каждый
идентификатор, который шлёт разметка, есть живой первичный ключ), и это ИЗМЕРЕНО чтением
`app/templates/ads/partials/sched_card.html`, а не предположено.
  </reversibility>

  <done>
Величина вне диапазона колонки не доезжает до драйвера БД ни одним из пяти входов; правило перечня
показано красным всеми пятью строками ДО правки и зелено ПОСЛЕ; общая граница существует в одном
экземпляре с летописью имени и основанием, снятым с моделей; обход транспортов и правила T-02-24 /
T-02-25 держатся числами прогонов.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: смежность границы с обеих сторон и неотличимость повтора, пережившая правку</name>

  <files>
tests/test_pages/test_editor_schedules.py
  </files>

  <read_first>
- `app/pages/schedules.py` — общая граница и три псевдонима ПОСЛЕ задачи 1. Правило смежности
  утверждает свойство ИМЕННО этой величины и обязано читать её из приложения, а не выписывать
  вторым литералом.
- `tests/test_pages/test_editor_schedules.py` — перечень пяти входов и правило перечня, заведённые
  задачей 1. Смежность прибавляется к ним и ИХ расстановку переиспользует.
- `tests/test_pages/test_confirm_delete_transport.py:2250-2300` — `UNUSABLE_AD_ID_VALUES` и
  `UNUSABLE_AD_ID_LANDINGS`: перечень негодных величин ПОЛЯ и их ожидаемые исходы. ⚠️ ЧИТАТЬ, ЧТОБЫ
  НЕ ЗАВЕСТИ ВТОРОЙ ИСТОЧНИК: величины поля живут ТАМ, величины входов маршрута — ЗДЕСЬ, и
  смешивать их нельзя. Здесь предмет — ровно смежность границы.
- `tests/test_pages/test_confirm_delete_transport.py:3050-3080` — правила неотличимости повтора и
  перечень изъятий (планы 10-06, 10-11). Их цвет ДО и ПОСЛЕ правки — предмет второго утверждения
  этой задачи.
- `tests/test_pages/test_editor_schedules.py:685-740` — форма правил T-02-24 / T-02-25. Новое
  правило наследует их форму отказа.
  </read_first>

  <behavior>
1. Величина, РАВНАЯ верхней границе колонки, принимается: маршрут отвечает прежней формой ответа, а
   не отказом валидации. Без этого утверждения граница зеленела бы на помощнике, отвергающем и
   годное тоже, — и «отказ валидации, а не 500» держалось бы ценой сломанного продукта.
2. Величина НА ЕДИНИЦУ БОЛЬШЕ верхней границы — отвергается. Это и есть замеряемая смежность:
   ребро `adjacency` зонда покрытия названо им прямо, и разрешено оно здесь.
3. Обе величины читаются ИЗ ПРИЛОЖЕНИЯ (ввозом общей константы), а не выписываются литералами:
   вторая копия числа разошлась бы с первой молча, и правило начало бы утверждать о числе, которого
   в приложении нет.
4. Неотличимость повтора подтверждённого удаления, заведённая планами 10-06 и 10-11, зелена ПОСЛЕ
   границы тем же прогоном, что и ДО неё. Ребро `idempotency` зонда покрытия разрешено этим
   утверждением: граница величины не имеет права сделать повтор различимым.
  </behavior>

  <action>
**1. Правило смежности.** Завести
`test_the_column_bound_admits_its_own_value_and_refuses_the_next_one` в
`tests/test_pages/test_editor_schedules.py`. Оно ввозит общую границу из `app/pages/schedules.py` и
шлёт по маршруту тумблера — самому короткому из трёх маршрутов с идентификатором пути, у которого
нет ни тела формы, ни ветвления по признаку возврата, — ДВЕ величины: саму границу и границу плюс
единицу.

Ожидание для границы: форма ответа ПРЕЖНЯЯ, то есть та же, что и на любом несуществующем
идентификаторе внутри диапазона. ⚠️ ОЖИДАНИЕ ФОРМУЛИРУЕТСЯ КАК СОВПАДЕНИЕ С ОТВЕТОМ НА
НЕСУЩЕСТВУЮЩИЙ ИДЕНТИФИКАТОР, А НЕ КАК КОНКРЕТНЫЙ КОД: величина границы есть законный
идентификатор, которого в базе нет, и правило, ожидающее код буквой, разъехалось бы с продуктом при
первой правке формы ответа на отсутствующую запись. Ожидание для границы плюс единицы: отказ
валидации.

Текст отказа обязан называть обе снятые формы ответа и прямо говорить, какая из двух сторон
границы разошлась с ожиданием.

**2. Антивакуум смежности.** Правило обязано утверждать, что две соседние величины дали РАЗНЫЕ
исходы. Помощник, отвечающий одинаково на обе, зеленел бы на любом из двух ожиданий по отдельности,
и это уже случалось в этой фазе.

**3. Неотличимость повтора.** Прогнать правила повтора и совпадения ветвей обхода транспортов ДО и
ПОСЛЕ задачи 1 одним и тем же фильтром имени, снять оба числа прошедших и записать их в сводку
рядом. Своих правил здесь не заводить: предмет — то, что действующие правила пережили границу, а не
новое свойство. Если число разошлось — это ОСТАНОВ с записью окном, а не повод подправить
ожидание.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly -k "column_bound or out_of_range or validation"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке (фильтр не выбрал ни одного правила — правило заведено под другим именем и не стережёт ничего)</fails_when>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "repeat or repeated or exempt or indistinguish"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо число прошедших отличается от снятого ДО задачи 1</fails_when>
  </verify>

  <acceptance_criteria>
- Обе величины ЧИТАЮТСЯ ИЗ ПРИЛОЖЕНИЯ: в файле правил нет второго вхождения числа границы —
  `grep -vE '^\s*#' tests/test_pages/test_editor_schedules.py | grep -c '2_147_483_647'` → `0`.
- В сводке приведены ОБА снятых исхода смежности (величина границы и величина на единицу больше) с
  кодами ответа и формой каждого.
- Антивакуум смежности показан: в сводке прямо сказано, что исходы двух соседних величин РАЗЛИЧНЫ,
  и приведены оба.
- В сводке приведены ДВА числа прошедших по фильтру повтора — снятое ДО задачи 1 и снятое после — и
  они совпадают. Расхождение объявлено остановом, а не объяснено.
- Обработчики не тронуты этой задачей: `git diff --name-only -- app/pages/` — пусто.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правила не меняют поведения продукта ни на символ; откат — снятие двух правил.
  </reversibility>

  <done>
Обе стороны границы утверждены и различны; величина читается из приложения в единственном
экземпляре; неотличимость повтора пережила правку и это показано двумя числами прогонов, а не
обещано.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 3: статический гейт сигнатур — шестой вход того же класса не уезжает неограниченным молча</name>

  <files>
tests/test_pages/test_editor_schedules.py
  </files>

  <read_first>
- `app/pages/schedules.py` ЦЕЛИКОМ после задачи 1 — все объявления маршрутов файла и их параметры.
  Гейт разбирает ИМЕННО этот исходник, и его форма определяет форму разборщика.
- `tests/test_templates/test_htmx_inventory.py:314-340` — `_strip_comments` и записанное основание
  вырезания комментариев. ⚠️ ОСНОВАНИЕ ПЕРЕНОСИТСЯ СЮДА ДОСЛОВНО И ЯВЛЯЕТСЯ НЕСУЩИМ: кодовая база
  проекта несёт объёмные комментарии-обоснования, и гейт, считающий прозу, краснеет на правку
  комментария и зеленеет на комментарий, повторяющий искомое. Здесь предмет — Python, и вырезать
  надо строчные комментарии Python и тела докстрингов.
- `tests/test_templates/test_htmx_markup_gates.py:319-340` — `_all_templates(directory)`: разборщик
  ПРИНИМАЕТ каталог параметром, и записанное основание — «группа контроля обязана подать обходу
  ИЗМЕНЁННУЮ копию дерева; разборщик, зашитый на единственный источник, сделал бы группу контроля
  невыразимой». Разборщик сигнатур наследует этот приём и по той же причине.
- `tests/test_pages/test_confirm_delete_transport.py:1187-1200` — форма правила объявленного числа:
  как пишется сообщение об отказе, называющее фактическое и объявленное.
- `.planning/WINDOWS.md`, записи 39-44 — форма записи окна. Гейт, чей предмет шире файла
  `app/pages/schedules.py`, передаётся окном, а не расширяется здесь.
  </read_first>

  <behavior>
1. Гейт читает исходник страничного модуля расписаний, находит объявления обработчиков маршрутов и
   для каждого параметра, чьё имя оканчивается признаком идентификатора, утверждает: параметр
   объявлен через общий тип-псевдоним, а не голым целым.
2. Комментарии и тела докстрингов ВЫРЕЗАЮТСЯ до разбора. Без вырезания гейт был бы
   самоотменяющимся: любой комментарий, называющий форму неограниченного параметра, читался бы как
   сам параметр.
3. Число ограниченных входов ОБЪЯВЛЕНО и стережётся: параметр, ушедший из перечня, краснит прогон
   так же, как параметр, пришедший в него без границы.
4. У гейта есть ГРУППА КОНТРОЛЯ: разборщику подаётся ИЗМЕНЁННАЯ копия исходника, в которой один
   параметр возвращён к голому целому, и гейт обязан на ней покраснеть. Зубы показаны, а не
   объявлены.
5. Гейт НЕ судит прозу и не читает `.planning/`: предмет — сигнатуры одного исходного файла.
  </behavior>

  <action>
**1. Разборщик.** Завести `_route_signature_sources`, принимающий ИСХОДНИК параметром (не путь и не
константу) и возвращающий пары «имя обработчика — текст его сигнатуры». Приём и его основание
взяты у разборщика шаблонов: группа контроля обязана уметь подать изменённую копию, иначе зубы
гейта придётся заявлять вместо того, чтобы их показывать.

Отбор обработчиков вести по признаку объявления маршрута, стоящему НАД функцией, а не по имени
функции: имя — соглашение, признак маршрута — факт.

Вырезание комментариев и докстрингов выполнять ДО разбора, с записанным у места основанием (форма
основания переносится из `_strip_comments`).

**2. Гейт.** Завести
`test_every_identifier_input_of_the_schedule_routes_carries_the_shared_bound`. Для каждой сигнатуры
собрать параметры, чьё имя оканчивается признаком идентификатора, и сличить ОТОБРАЖЕНИЕМ «имя
обработчика и имя параметра → несёт ли общую границу». Сличение отображением ЦЕЛИКОМ, а не
утверждением на каждом шаге, — по той же причине, по которой так собрано правило пяти маршрутов.

Из перечня ЯВНО исключается параметр владельца, приходящий не запросом, а зависимостью сессии:
его величина не подконтрольна отправителю. Исключение записывается С ОСНОВАНИЕМ прямо у гейта, а не
подразумевается фильтром, — перечень изъятий без обоснований превращается в список того, до чего не
дошли руки (форма перечня изъятий, принятая планом 10-06).

**3. Объявленное число.** Завести `BOUNDED_ROUTE_INPUTS_DECLARED` и утвердить равенство ему числа
найденных ограниченных входов. Число ПОСТАВИТЬ ПРОГОНОМ покрасневшего правила и привести его вывод
дословно; число, посчитанное в уме, не принимается. Летопись числа записать по форме летописей
фазы: откуда взялось, чем прибавлено, каким планом.

**4. Группа контроля.** Завести отрицательный контроль: разборщику подаётся копия исходника, в
которой ОДИН идентификатор пути возвращён к объявлению без границы, и утверждается, что гейт на ней
краснеет. Копия собирается в памяти, файл дерева не правится.

**5. Что гейт НЕ закрывает — назвать прямо.** Соседние страничные модули несут идентификаторы того
же класса и этим гейтом НЕ покрыты. Расширение предмета за пределы файла — не эта партия: у
остальных модулей нет ни воспроизведённого отказа, ни объявленного инварианта, и гейт, заведённый
на весь каталог без замера, краснел бы на месте, о котором никто ничего не измерил. Передать окном
`.planning/WINDOWS.md` с типом `todo`, назвав предметом «граница величины идентификатора на
страничных модулях за пределами расписаний» и кандидатом — Фазу 15.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly -k "identifier_input or shared_bound or control"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо в собранном перечне имён отсутствует правило отрицательного контроля</fails_when>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо число прошедших меньше числа, снятого после задачи 2</fails_when>
  </verify>

  <acceptance_criteria>
- **ЧИСЛО ПОСТАВЛЕНО ПРОГОНОМ.** В сводке приведён ДОСЛОВНЫЙ вывод покрасневшего правила числа
  («ограниченных входов … а объявлено …») и только затем записанное значение.
- **ЗУБЫ ПОКАЗАНЫ.** В сводке приведён вывод отрицательного контроля: гейт на изменённой копии
  исходника КРАСНЕЕТ, и названо, какой именно параметр был возвращён к необъявленной форме.
- **ГЕЙТ НЕ САМООТМЕНЯЕТСЯ.** В сводке показано, что вырезание комментариев работает: приведён
  результат разбора исходника, в который добавлена строка комментария, называющая форму
  неограниченного параметра, — вердикт гейта не изменился.
- Перечень изъятий несёт основание НА ЗАПИСЬ у каждого пункта; в сводке приведён текст основания.
- Файл дерева отрицательным контролем не правится: `git diff --name-only -- app/pages/schedules.py`
  показывает файл изменённым ЗАДАЧЕЙ 1, а не этой, и ни один параметр идентификатора не вернулся к
  необъявленной форме — счёт ведётся ПО ИСХОДНИКУ С ВЫРЕЗАННЫМИ КОММЕНТАРИЯМИ, иначе абзац
  обоснования, называющий эту форму, отменял бы проверку сам собой:
  `grep -vE '^\s*#' app/pages/schedules.py | grep -cE '^\s+[a-z_]*id: int[,)]'` → `0`.
- Окно о непокрытых соседних модулях записано; в сводке приведён его номер и текст.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Гейт не меняет поведения продукта; откат — снятие правила и константы.
  </reversibility>

  <done>
Идентификаторы всех маршрутов файла несут общую границу, это утверждено отображением целиком, число
объявлено и поставлено прогоном, зубы гейта показаны отрицательным контролем, а непокрытый остаток
передан окном, а не умолчан.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер/клиент → страничный маршрут расписаний | недоверенный ввод пересекает границу тремя путями: адрес запроса, тело формы, cookie сессии |
| страничный маршрут → драйвер БД | величина, прошедшая коэрцию типа, становится операндом сравнения SQL |
| страничный маршрут → вычисление вердикта о владении | величина участвует в решении об авторизации ДО любой записи |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-12-01 | Denial of Service | `schedules_delete`, `schedules_update`, `schedules_toggle`, `schedules_create` | high | mitigate | Общая граница величины на границе приложения (задача 1): значение вне диапазона колонки отвергается ДО получения управления телом обработчика. Перечень пяти входов краснеет на любом возврате отказа обработчика |
| T-10-12-02 | Information Disclosure | `_ownership_verdict`, `_owns_ad` | high | mitigate | Два из пяти отказов падают ВНУТРИ вычисления вердикта о владении: решение об авторизации принимается исключением. Граница стоит ДО вызова вердикта (задача 1), поэтому вердикт вычисляется на величине, уже признанной допустимой |
| T-10-12-03 | Information Disclosure | форма ответа на негодную величину | medium | accept | Отказ валидации на величине вне диапазона отличим от прежней формы ответа на несуществующий идентификатор внутри диапазона. Картой занятых идентификаторов это не является: допустимость величины вне диапазона колонки не зависит от содержимого базы ВОВСЕ. Правила неотличимости повтора (T-10-07) зелены после правки, и это утверждается числом прогона (задача 2) |
| T-10-12-04 | Tampering | статический гейт сигнатур | medium | mitigate | Гейт вырезает комментарии и докстринги до разбора и несёт отрицательный контроль на изменённой копии исходника (задача 3): правило, зеленеющее на комментарии, повторяющем искомое, было бы самоотменяющимся |
| T-10-12-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: ни одна задача плана не устанавливает пакетов ни одним пакетным менеджером; `pyproject.toml` планом не правится. Таблица легитимности пакетов не требуется, и её отсутствие не есть пропуск |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly` — зелено, число
   прошедших больше базового `54 passed`, прибавка объяснена числом заведённых случаев.
2. `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly` — ровно
   `57 passed`: граница не двинула ни одного действующего случая обхода транспортов.
3. `uv run pytest tests/ -q` — прогон целиком зелен; число прошедших названо и сопоставлено с
   числом до партии.
4. `uv run python -m compileall -q app main.py tests` — без ошибок.
5. В сводке приведены ДОСЛОВНО: покрасневший вывод правила пяти маршрутов ДО правки, зелёный вывод
   после, вывод покрасневшего правила объявленного числа, вывод отрицательного контроля гейта
   сигнатур.
</verification>

<success_criteria>
- Ни один из пяти входов файла не отвечает отказом обработчика на величину вне диапазона колонки, и
  это утверждено ОДНИМ отображением, а не пятью правилами.
- Правило, стерегущее свойство, показано КРАСНЫМ на неправленом дереве всеми пятью строками.
- Величина границы существует в одном экземпляре, её основание снято с моделей, имя несёт летопись.
- Смежность границы утверждена с обеих сторон и стороны различны.
- Неотличимость повтора пережила правку и это показано двумя числами прогонов.
- Статический гейт сигнатур зелен, его число поставлено прогоном, его зубы показаны отрицательным
  контролем, непокрытый остаток передан окном.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-12-SUMMARY.md` по завершении.
</output>
