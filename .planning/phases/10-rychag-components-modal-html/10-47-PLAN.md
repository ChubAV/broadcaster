---
phase: 10-rychag-components-modal-html
plan: 47
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`: предмет плана — ЗАМЕР по документам планирования и РЕШЕНИЕ
# ВЛАДЕЛЬЦА. Кода, для которого выразимо «ожидаемый выход при заданном входе», план не производит ни
# строки. Тот же случай и то же основание, что у плана 10-44.
wave: 2
# ⚠️ ВОЛНА 2 — ТОЛЬКО ТРАССИРУЮЩИЙ ПОРЯДОК. Пересечения по файлам с планами 10-42, 10-43 и 10-44 нет
# ни одного: настоящий план создаёт ОДИН новый артефакт каталога фазы и ни строки суиты или продукта.
depends_on: ["10-41"]
gap_closure: true
gap_ids: [HV-04-PROHIBITIONS]
files_modified:
  - .planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md
autonomous: false
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ: `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3).
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ТРИДЦАТЬ ДЕВЯТЬ ЗАПРЕТОВ СЕДЬМОЙ ПАРТИИ ПЕРЕСТАЮТ БЫТЬ ПЕРЕЧНЕМ БЕЗ ВЕРДИКТА: у каждого предъявлено ЛИБО имя правила, которое его принуждает, ЛИБО прямое «принуждения нет»"
    - "ДЕСКРИПТОР ПРОВЕРКИ ОТДЕЛЁН ОТ ФАКТА ПРОВЕРКИ, И ЭТО ГЛАВНОЕ РАЗЛИЧЕНИЕ ПЛАНА: 25 запретов несут дескриптор `test`, но дескриптор есть ОБЕЩАНИЕ, а не принуждение — предъявляется ИМЯ найденного правила либо его отсутствие"
    - "ЧЕТЫРНАДЦАТЬ ЗАПРЕТОВ БЕЗ ДЕСКРИПТОРА НАЗВАНЫ ПОИМЁННО: у них принуждения нет по построению, и им нужно ЯВНОЕ РАЗРЕШЕНИЕ владельца, а не молчание"
    - "ИСТИННЫЙ РАЗМЕР ПРЕДМЕТА ПРЕДЪЯВЛЕН, А НЕ СКРЫТ: сорок исполненных планов несут 206 запретов (177 без дескриптора, 29 с дескриптором `test`), и владелец видит, что тридцать девять — это доля, а не всё"
    - "СОБСТВЕННЫЙ ВКЛАД ВОСЬМОЙ ПАРТИИ ОБЪЯВЛЕН ЕЮ ЖЕ: партия прибавляет 33 запрета (19 без дескриптора, 14 с дескриптором `test`), и это названо числом в том же предмете решения, а не умолчано"
    - "РЕШЕНИЕ ПРИНИМАЕТ ВЛАДЕЛЕЦ, А НЕ ИСПОЛНИТЕЛЬ: выбор между требованием принуждения и явным разрешением есть решение о том, чем фаза закрывается, и внутри задачи он не делается ни одной ветвью"
    - "ВЕРДИКТ ВЕРИФИКАЦИИ И ОТМЕТКА ТРЕБОВАНИЯ НЕ ДВИГАЮТСЯ, ИСПОЛНЕННЫЕ ПЛАНЫ НЕ ПРАВЯТСЯ НИ НА СИМВОЛ"
  artifacts:
    - "10-PROHIBITIONS-SUBJECT.md — поимённая таблица 39 запретов седьмой партии: план, текст, дескриптор, найденное принуждение либо его отсутствие"
    - "10-PROHIBITIONS-SUBJECT.md — числа по всей фазе (206 запретов сорока исполненных планов, 177 без дескриптора, 29 с дескриптором) и по восьмой партии (33, из них 19 без дескриптора)"
    - "10-PROHIBITIONS-SUBJECT.md — запись ответа владельца: идентификатор ветви, имя, дата, основание и область"
  key_links:
    - "имя найденного правила против дескриптора → отличение принуждения от обещания: единственное, чем `verification: test` перестаёт быть словом; ломается — фаза закрывается перечнем обещаний"
    - "поимённый перечень запретов без дескриптора → предмет явного разрешения: единственное, чем разрешение отличимо от недосмотра; ломается — четырнадцать запретов остаются в вечном fail-closed"
    - "числа по всей фазе → честность масштаба: единственное, чем владелец видит, что тридцать девять есть доля от двухсот шести; ломается — решение принимается о выборке, объявленной целым"
  prohibitions:
    - statement: "РЕШЕНИЕ НЕ ПРИНИМАЕТСЯ ВНУТРИ ЗАДАЧИ НИ ОДНОЙ ВЕТВЬЮ: исполнитель предъявляет замер и обе ветви с их ценами и ОСТАНАВЛИВАЕТСЯ"
      status: flagged-unverified
      verification: none
    - statement: "ИСПОЛНЕННЫЕ ПЛАНЫ 10-01…10-40 НЕ ПРАВЯТСЯ НИ НА СИМВОЛ: дескриптор проверки, дописанный в исполненный план задним числом, есть переписанная история и превращает обещание в принуждение росчерком пера"
      status: flagged-unverified
      verification: test
    - statement: "ПРАВИЛА НЕ ЗАВОДЯТСЯ РАДИ ЗАКРЫТИЯ ЗАПРЕТА В ЭТОМ ПЛАНЕ: план ПРЕДЪЯВЛЯЕТ состояние, а заведение недостающих правил есть работа, объём которой и решает владелец"
      status: flagged-unverified
      verification: none
    - statement: "`10-VERIFICATION.md` НЕ ПРАВИТСЯ, ОТМЕТКА `FORM-06` В `.planning/REQUIREMENTS.md` НЕ ТРОГАЕТСЯ, ПРОДУКТ И СУИТА НЕ ПРАВЯТСЯ НИ НА СТРОКУ"
      status: flagged-unverified
      verification: test
    - statement: "ИМЯ ПРИНЯВШЕГО И ДАТА НЕ ВЫДУМЫВАЮТСЯ И НЕ ПОДСТАВЛЯЮТСЯ ЗАГЛУШКОЙ"
      status: flagged-unverified
      verification: none
---

<objective>
Тридцать девять запретов седьмой партии, все `flagged-unverified`, получают предъявленное состояние и
решение владельца — либо принуждение, либо явное разрешение.

**Что измерено, разбором frontmatter, а не чтением.** Планы 10-35…10-40 несут **39** записей блока
запретов: **25** с дескриптором `verification: test` и **14** с `verification: none`. Числа сходятся с
пунктом 4 перечня Human Verification девятого круга побайтово.

**Почему перечень с дескриптором — ещё не принуждение.** `verification: test` есть ОБЪЯВЛЕНИЕ автора о
том, ЧЕМ запрет предполагается стеречь, а не ссылка на найденное правило. Различение это и есть
предмет плана: у каждого из 25 предъявляется ИМЯ правила, которое его принуждает, — найденное в
дереве, — либо прямое «правила с таким предметом в дереве нет». Разница между «объявлено» и «найдено»
есть ровно та разница, за которую фаза получила круги 3, 4, 5, 6 и 8 под именем «запись шире дерева».

**Почему четырнадцать без дескриптора не закрываются молчанием.** По регламенту они fail-closed:
запрет без дескриптора принуждения не имеет по построению и требует ЯВНОГО разрешения владельца.
Молчание здесь неотличимо от недосмотра — то же, чем `V9-05` прожил девять кругов.

**Истинный размер предмета называется, а не скрывается.** Замер по всем сорока исполненным планам:
запретов **206**, из них **177** без дескриптора и **29** с дескриптором `test`. То есть 39 — доля, а
не всё, и решение, принятое как о целом, переоткроется на первом же взгляде в соседний план.

**Собственный вклад восьмой партии объявляется ею же.** Планы 10-41…10-47 прибавляют **33** запрета
(**19** без дескриптора, **14** с дескриптором `test`). Промпт планирования назвал это ограничением на
планировщика прямо, и честная запись обязана предъявить своё приращение в том же предмете решения.

Purpose: пункт 4 перечня Human Verification блокирует закрытие фазы по регламенту, а предмета решения
у него нет — есть только число. План даёт предмет и останавливается.

Output: поимённая таблица состояния 39 запретов, числа по фазе и по партии, ответ владельца.

⚠️ **НИ ОДНА ВЕТВЬ НЕ ПРАВИТ НИ ПРОДУКТА, НИ СУИТЫ, НИ ИСПОЛНЕННЫХ ПЛАНОВ.** Дескриптор, дописанный в
исполненный план задним числом, превратил бы обещание в принуждение росчерком пера.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

- `.planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md` — НОВЫЙ файл: предмет
  решения по пункту 4 перечня Human Verification. Несёт поимённую таблицу 39 запретов седьмой партии
  с их дескрипторами и найденным принуждением, числа по всей фазе и по восьмой партии, перечень
  запретов без дескриптора и, после чекпойнта, запись ответа владельца.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Задача 1: состояние 39 запретов предъявляется поимённо — имя найденного правила либо его отсутствие</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-35-PLAN.md`, блок `must_haves.prohibitions`
  целиком — семь записей, форма поля дескриптора. ⚠️ ЧИТАТЬ ДОСЛОВНО: это образец формы, по которой
  разбираются остальные пять планов партии.
- `.planning/phases/10-rychag-components-modal-html/10-38-PLAN.md`, блок `must_haves.prohibitions` —
  единственный план партии, где ВСЕ шесть записей несут дескриптор `test`.
- `.planning/phases/10-rychag-components-modal-html/10-39-PLAN.md`, блок `must_haves.prohibitions` —
  восемь записей, наибольшее число в партии.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md`, пункт 4 перечня Human
  Verification — дословная формулировка требования и его чисел.
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-SECURITY.md:1-20` — форма
  frontmatter артефакта каталога фазы. ⚠️ ФОРМА НАСЛЕДУЕТСЯ, а не изобретается.
- `.planning/STATE.md`, записи D-30/D-32 — опровергнутое и разрешённое помечается, а не стирается.
  </read_first>

  <action>
**(1) СНЯТЬ ЗАМЕР РАЗБОРОМ, А НЕ ЧТЕНИЕМ.** Разобрать frontmatter каждого плана фазы и собрать блок
запретов: число записей на план, распределение по значению поля дескриптора, поимённый перечень для
планов 10-35…10-40. Отдельно — те же числа по всем сорока исполненным планам и по планам восьмой
партии. ⚠️ ЗАПИСЬ БЕЗ ПОЛЯ ДЕСКРИПТОРА И ЗАПИСЬ-СТРОКА (без словаря вовсе) СЧИТАЮТСЯ ОТДЕЛЬНЫМИ
РОДАМИ и не сливаются с `none`: слитый счёт скрыл бы разницу между «дескриптор объявлен пустым» и
«поля нет».

**(2) СЛИЧИТЬ СНЯТОЕ С ЧИСЛАМИ, НАЗВАННЫМИ В `<objective>`.** Ожидание: 39 записей у планов
10-35…10-40 (25 с дескриптором `test`, 14 с `none`); 206 у всех сорока исполненных (177 `none`, 29
`test`); 33 у восьмой партии (19 `none`, 14 `test`). ⚠️ РАСХОЖДЕНИЕ НЕ СГЛАЖИВАЕТСЯ: в предмет
решения идут СНЯТЫЕ числа, а расхождение называется в сводке отдельной строкой с вероятной причиной.

**(3) ДЛЯ КАЖДОГО ИЗ 25 ЗАПРЕТОВ С ДЕСКРИПТОРОМ `test` — НАЙТИ ПРИНУЖДЕНИЕ ЛИБО ОБЪЯВИТЬ ЕГО
ОТСУТСТВИЕ.** Поиск идёт по дереву `tests/` по ПРЕДМЕТУ запрета: имена файлов, символов и путей,
названные в тексте запрета, ищутся в правилах, и предъявляется ИМЯ правила, которое падало бы при
нарушении запрета. Итог по каждой записи — одно из трёх: `принуждается` с именем правила;
`не принуждается` с прямым указанием, что правила с таким предметом в дереве не нашлось;
`принуждается частично` с именем правила и названной половиной, которая им не покрыта.
⚠️ «НАЙДЕНО» ЗНАЧИТ «НАЗВАНО ИМЕНЕМ», А НЕ «ВЕРОЯТНО СУЩЕСТВУЕТ»: запись без имени правила есть
запись `не принуждается`, и смягчать её формулировкой запрещено — смягчение и есть тот самый класс.

**(4) ДЛЯ КАЖДОГО ИЗ 14 ЗАПРЕТОВ БЕЗ ДЕСКРИПТОРА — ВЫПИСАТЬ ТЕКСТ И НАЗВАТЬ, ПОЧЕМУ ПРИНУЖДЕНИЯ У
НЕГО НЕТ ПО ПОСТРОЕНИЮ.** Часть из них выразима правилом, часть — нет (например, «решение не
принимается внутри задачи» есть свойство ПРОЦЕССА, а не дерева). Различение выписывается прямо: оно и
есть то, о чём владельцу предстоит решать.

**(5) НАПИСАТЬ `10-PROHIBITIONS-SUBJECT.md`.** Frontmatter формой соседнего артефакта фазы, с полем
состояния `subject` и БЕЗ поля вердикта. Тело: числа замера; таблица 39 записей с колонками «план,
текст запрета (сокращённо), дескриптор, итог, имя правила»; отдельный раздел четырнадцати без
дескриптора с полным текстом каждой; раздел чисел по всей фазе и по восьмой партии; абзац о том,
чего файл НЕ делает — он не выносит вердикта, не объявляет запреты соблюдёнными и не двигает отметку
требования.
  </action>

  <acceptance_criteria>
- `10-PROHIBITIONS-SUBJECT.md` существует; frontmatter несёт поле состояния `subject` и не несёт поля
  вердикта.
- Таблица содержит РОВНО столько строк, сколько дал замер по планам 10-35…10-40, и у каждой строки
  заполнены все пять колонок.
- Каждая строка с итогом `принуждается` несёт ИМЯ правила; ни одна не оставлена с описанием вместо
  имени.
- Раздел запретов без дескриптора несёт полный текст каждого и различение «выразимо правилом /
  свойство процесса».
- Числа по всей фазе и по восьмой партии присутствуют и сличены с числами `<objective>`; расхождения,
  если есть, названы в сводке.
- Присутствует абзац о том, чего файл не делает.
- `git status --porcelain` не показывает ни одного изменённого файла плана с номером не выше 40.
  </acceptance_criteria>

  <verify>
    <automated>uv run python -c "import pathlib,sys; p=pathlib.Path('.planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md'); s=p.read_text(encoding='utf-8') if p.is_file() else ''; need=['status: subject','10-35','10-40','verification','дескриптор']; miss=[n for n in need if n not in s]; sys.exit('нет в 10-PROHIBITIONS-SUBJECT.md: '+repr(miss) if miss else 0)"</automated>
    <fails_when>ненулевой код возврата — файла нет либо в нём отсутствует одна из названных строк; вывод перечисляет отсутствующие</fails_when>
    <automated>git status --porcelain -- .planning/phases/10-rychag-components-modal-html/</automated>
    <fails_when>в выводе есть строка, оканчивающаяся на "-PLAN.md" с номером плана не выше 40 — исполненный план тронут, что запрещено прямо</fails_when>
  </verify>

  <done>
Состояние всех 39 запретов седьмой партии предъявлено поимённо: у каждого — дескриптор, итог и имя
принуждающего правила либо прямое «не принуждается»; четырнадцать без дескриптора выписаны полным
текстом с различением выразимости; числа по фазе и по восьмой партии предъявлены; ни один исполненный
план не тронут.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Задача 2: РЕШЕНИЕ ВЛАДЕЛЬЦА — требовать принуждения у каждого запрета либо дать явное разрешение записью</name>

  <decision>Требуется ли принуждение (правило в дереве) у КАЖДОГО из 39 запретов седьмой партии до
закрытия фазы — или запреты, у которых принуждения нет, получают ЯВНОЕ РАЗРЕШЕНИЕ владельца записью с
основанием, и фаза закрывается с ним.</decision>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md` ЦЕЛИКОМ — предмет
  решения, собранный задачей 1. ⚠️ ЧИТАТЬ ДОСЛОВНО И ПЕРЕД ОТВЕТОМ.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md`, пункт 4 перечня Human
  Verification — дословная формулировка требования и слова «fail-closed по регламенту».
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md`, блок
  `overrides` — ЖИВОЙ ПРЕЦЕДЕНТ явного разрешения в этом проекте (решение `accept-override`,
  `chubav`, 2026-09-01). ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: форма записи в проекте уже есть.
- `.planning/STATE.md`, записи D-30/D-32 — форма записи решения.
  </read_first>

  <context>
**ЧТО ПРЕДЪЯВЛЯЕТСЯ ВЛАДЕЛЬЦУ ДО ОТВЕТА — ДОСЛОВНО ИЗ `10-PROHIBITIONS-SUBJECT.md`:** таблица 39
записей с итогом у каждой; полный текст четырнадцати запретов без дескриптора; числа по всей фазе
(206 запретов сорока исполненных планов, 177 без дескриптора) и по восьмой партии (33, из них 19 без
дескриптора).

**Ветвь (A) — `enforce-each`.** Принуждение требуется у каждого. Каждая запись с итогом
`не принуждается` либо `принуждается частично` становится работой: правило заводится, и фаза не
закрывается, пока оно не заведено.
**Цена ветви, названная прямо:** объём работы РАВЕН ЧИСЛУ ЗАПИСЕЙ БЕЗ ПРИНУЖДЕНИЯ, снятому задачей 1,
и он не оценивается здесь заранее — это была бы оценка вместо замера. Сверх того часть запретов
принуждению НЕ ПОДДАЁТСЯ по существу: «решение не принимается внутри задачи» есть свойство ПРОЦЕССА,
а не дерева, и правила для него не существует ни в какой форме. То есть ветвь (A) в чистом виде
недостижима, и её честное исполнение потребует ВТОРОГО решения — о том, что делать с невыразимыми.
Наконец, ветвь оставляет открытым вопрос о 167 запретах остальных планов фазы: решение о 39 при 206
есть решение о доле, объявленной целым.

**Ветвь (B) — `permit-with-record`.** Запреты, у которых принуждения нет, получают ЯВНОЕ РАЗРЕШЕНИЕ
владельца записью с основанием, именем и датой. Запреты с найденным принуждением остаются
принуждаемыми, и их имена правил записаны.
**Цена ветви, названная прямо:** фаза объявляет, что часть её собственных запретов держится
осторожностью читателя, а не машиной, и запись об этом переживает фазу. Всякая следующая фаза вехи
получит прецедент «запрет без дескриптора закрывается разрешением», и его придётся либо повторять,
либо явно отменять. Разрешение НЕ ДЕЛАЕТ запрет соблюдённым — оно делает его НЕ БЛОКИРУЮЩИМ, и
разница обязана стоять в записи дословно, иначе следующий круг прочтёт разрешение как проверку.

⚠️ **ТРЕТЬЕЙ ВЕТВИ — «ОСТАВИТЬ КАК ЕСТЬ» — В ПЕРЕЧНЕ НЕТ.** Она и есть сегодняшнее состояние, и по
регламенту она fail-closed: фаза не закроется никогда.
  </context>

  <options>
    <option id="enforce-each">
      <name>(A) Принуждение ТРЕБУЕТСЯ у каждого: записи без правила становятся работой</name>
      <pros>Ни один запрет фазы не держится словом: у каждого выразимого есть правило, которое падает при нарушении. Прецедента «запрет закрывается разрешением» не возникает, и следующие фазы вехи наследуют строгую форму. Найденные при поиске дыры (запрет, объявленный стерегомым, но не стерегомый ничем) закрываются там, где они есть, а не переносятся</pros>
      <cons>Объём работы равен числу записей без принуждения и заранее не оценивается — оценка вместо замера была бы ровно тем, чего фаза избегает. Часть запретов принуждению НЕ ПОДДАЁТСЯ по существу (свойства процесса, а не дерева), поэтому ветвь в чистом виде недостижима и потребует ВТОРОГО решения о невыразимых. Решение о 39 при 206 запретах фазы есть решение о доле, объявленной целым, и остальные 167 остаются открытыми</cons>
    </option>
    <option id="permit-with-record">
      <name>(B) Запреты без принуждения получают ЯВНОЕ РАЗРЕШЕНИЕ владельца записью с основанием</name>
      <pros>Пункт 4 перечня перестаёт блокировать закрытие фазы, и состояние становится машинно читаемым: разрешённое отличимо от незамеченного. Запреты с найденным принуждением остаются принуждаемыми и их имена правил записаны — то есть решение принимается о ТОЧНО НАЗВАННОМ остатке, а не о перечне целиком. Форма записи в проекте уже есть (решение `accept-override` Фазы 9), и вторая её форма не заводится. Ни продукта, ни суиты, ни исполненных планов не трогается</cons>
      <cons>Фаза объявляет, что часть её собственных запретов держится осторожностью читателя, а не машиной, и запись переживает фазу: следующая фаза вехи получает прецедент, который придётся либо повторять, либо явно отменять. Разрешение НЕ ДЕЛАЕТ запрет соблюдённым — оно делает его не блокирующим, и следующий круг рискует прочесть его как проверку, если разница не выписана дословно. Дыры, найденные задачей 1 (запрет, объявленный стерегомым и не стерегомый ничем), остаются дырами и получают только владельца</cons>
    </option>
  </options>

  <reversibility rating="one-way">
Обе ветви объявляют, ЧЕМ фаза считает свои запреты закрытыми, и переживают фазу: следующие круги и
следующие фазы читают запись как основание. Ветвь (B) сверх того публикует в проекте ПРЕЦЕДЕНТ
обращения с запретами без дескриптора, наследуемый всей вехой. Отмена любой из двух записей задним
числом есть переоткрытие уже принятого владельцем решения. Поэтому дверь гейтируется человеком до
единого символа записи.
  </reversibility>

  <resume-signal>Ответить РОВНО одним идентификатором: `enforce-each` либо `permit-with-record`, и
назвать ИМЯ для поля принявшего и ДАТУ (обе величины идут в запись дословно и заглушкой не
подставляются).
При `enforce-each` — назвать, что делать с запретами, принуждение которых невыразимо в дереве, и
входят ли в область решения 167 запретов остальных планов фазы.
При `permit-with-record` — подтвердить формулировку «разрешение делает запрет НЕ БЛОКИРУЮЩИМ, но НЕ
СОБЛЮДЁННЫМ» дословно и назвать область разрешения: только 39 запретов седьмой партии, вся Фаза 10
либо вся веха.</resume-signal>

  <acceptance_criteria>
- Владельцу предъявлены таблица 39 записей с итогом у каждой, полный текст четырнадцати без
  дескриптора и числа по фазе и по восьмой партии — в формулировках артефакта дословно.
- Обе ветви предъявлены с их ценами в формулировках раздела `<context>` дословно.
- Ответ записан ровно одним идентификатором из двух вместе с именем и датой; выбор внутри задачи не
  сделан ни при каких условиях.
- При `permit-with-record` названа область разрешения; при `enforce-each` названо, что делать с
  невыразимыми и входят ли в область 167 остальных запретов.
  </acceptance_criteria>

  <verify>
    <human-check>Владелец назвал ОДНУ из двух ветвей (`enforce-each` либо `permit-with-record`), назвал имя для записи и дату, а также ответил на дополнительный вопрос своей ветви. Замер предъявлен ему ДО ответа и приведён в сводку дословно.</human-check>
  </verify>

  <done>
Предмет предъявлен целиком; обе ветви и их цены названы дословно; выбор внутри задачи не сделан;
ответ владельца записан идентификатором ветви, именем, датой и ответом на дополнительный вопрос.
  </done>
</task>

<task type="auto">
  <name>Задача 3: ответ владельца записывается по якорю, и ни одна ветвь не объявляет запрет соблюдённым</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md` ЦЕЛИКОМ — куда встаёт
  запись ответа. ⚠️ ЯКОРЬ: раздел ответа встаёт ОДИН раз, ПО ИМЕНИ РАЗДЕЛА, а не дописыванием в
  конец файла (прямой урок `V9-06` и окна 70).
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md`, блок
  `overrides` — форма записи разрешения: поля решения, причины, имени и даты.
- `.planning/STATE.md`, записи D-30/D-32.
- `tests/test_planning/test_requirement_completion_follows_verification.py` — что правило читает в
  артефактах фазы. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: новый артефакт не имеет права его уронить.
  </read_first>

  <behavior>
1. В артефакте встаёт РОВНО ОДИН раздел ответа с идентификатором ветви, именем, датой, основанием и
   ответом на дополнительный вопрос ветви.
2. Повторное исполнение задачи не заводит второго такого раздела: запись идёт по имени раздела, и
   счёт его вхождений до и после равен единице.
3. Ветвь `permit-with-record`: запись несёт ДОСЛОВНУЮ формулировку «разрешение делает запрет НЕ
   БЛОКИРУЮЩИМ, но НЕ СОБЛЮДЁННЫМ» и названную область.
4. Ветвь `enforce-each`: запись несёт перечень записей, ставших работой, и прямое указание, что фаза
   не закрывается до их заведения; ни одно правило настоящим планом НЕ ЗАВОДИТСЯ.
5. Ни в одной ветви не правятся `10-VERIFICATION.md`, `.planning/REQUIREMENTS.md`, исполненные планы,
   продукт и суита.
  </behavior>

  <action>
**(1) ВЗЯТЬ ОТВЕТ ЧЕКПОЙНТА И ИСПОЛНИТЬ РОВНО ОДНУ ВЕТВЬ.** Заготовка отвергнутой ветви в файле не
оставляется: она прочиталась бы следующим кругом как второе, несогласное решение.

**(2) ЗАПИСЬ ИДЁТ ПО ЯКОРЮ.** Раздел ответа имеет собственный заголовок; перед записью считается
число его вхождений, после — тоже, и оба числа называются в сводке.

**(3) ОСНОВАНИЕ СОБИРАЕТСЯ ИЗ ЗАМЕРА ЗАДАЧИ 1, А НЕ ИЗ ПАМЯТИ.** Числа берутся из артефакта, а не
переписываются из настоящего плана: план писан до прогона.

**(4) РАЗЛИЧЕНИЕ «НЕ БЛОКИРУЕТ» И «СОБЛЮДЁН» ВЫПИСЫВАЕТСЯ ДОСЛОВНО В ВЕТВИ РАЗРЕШЕНИЯ.** ⚠️ БЕЗ
НЕГО СЛЕДУЮЩИЙ КРУГ ПРОЧТЁТ РАЗРЕШЕНИЕ КАК ПРОВЕРКУ — то есть запись снова станет шире дерева, и
партия воспроизведёт класс, против которого работает.

**(5) НИ ОДНОГО ПРАВИЛА НЕ ЗАВОДИТСЯ И НИ ОДИН ЗАПРЕТ НЕ ОБЪЯВЛЯЕТСЯ СОБЛЮДЁННЫМ.** Настоящий план
ПРЕДЪЯВЛЯЕТ и ЗАПИСЫВАЕТ; заведение недостающих правил есть работа, объём которой и решает владелец.

**(6) ПРОВЕРИТЬ, ЧТО КАТАЛОГ ПРАВИЛ ЗАПИСИ ЗЕЛЕН.** Новый файл в каталоге фазы не имеет права уронить
ни одно из правил каталога.
  </action>

  <acceptance_criteria>
- Раздел ответа встречается РОВНО ОДИН раз; число вхождений до и после названо в сводке.
- Раздел несёт идентификатор ветви, имя, дату, основание и ответ на дополнительный вопрос ветви; ни
  одно поле не заполнено заглушкой.
- В ветви `permit-with-record` формулировка «разрешение делает запрет НЕ БЛОКИРУЮЩИМ, но НЕ
  СОБЛЮДЁННЫМ» присутствует дословно, и названа область разрешения.
- В ветви `enforce-each` перечислены записи, ставшие работой, и сказано прямо, что фаза не
  закрывается до их заведения.
- Ни одного правила в `tests/` настоящим планом не заведено: `git diff --numstat -- tests app` пуст.
- `uv run pytest tests/test_planning/ -q` завершается нулём.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed", либо число зелёных меньше 28 — новый артефакт каталога фазы уронил правило записи</fails_when>
    <automated>git diff --numstat -- tests app .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md</automated>
    <fails_when>вывод НЕПУСТ — тронуты суита, продукт, отметка требования либо вердикт верификации, что запрещено прямо</fails_when>
  </verify>

  <done>
Ответ владельца записан ровно одним разделом по якорю, с идентификатором ветви, именем, датой,
основанием из замера и ответом на дополнительный вопрос; ветвь разрешения выписала различение «не
блокирует / не соблюдён» дословно и назвала область; ни одного правила не заведено и ни один запрет не
объявлен соблюдённым; вердикт, отметка требования, продукт и суита не тронуты.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| исполнитель плана → владелец фазы | решение о том, чем фаза считает свои запреты закрытыми, принадлежит владельцу |
| дескриптор `verification: test` → читатель перечня | дескриптор читается как «стережётся правилом», будучи объявлением автора, а не ссылкой на найденное правило |
| запись разрешения → следующий круг и следующая фаза | разрешение читается как прецедент и, если различение не выписано, как проверка |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-47-01 | Elevation of privilege | задача записи решения | high | mitigate | Исполнитель, разрешивший запреты сам, объявляет фазу закрытой вместо владельца. Закрывается блокирующим чекпойнтом перед задачей записи и прямым запретом выбора внутри задачи |
| T-10-47-02 | Spoofing | дескриптор `verification: test` без найденного правила | high | mitigate | Перечень обещаний выдаёт себя за перечень принуждений, и фаза закрывается на словах. Закрывается требованием ИМЕНИ найденного правила у каждой записи и итогом `не принуждается` при его отсутствии |
| T-10-47-03 | Repudiation | запись разрешения без различения «не блокирует / не соблюдён» | high | mitigate | Следующий круг прочтёт разрешение как проверку, и запись снова станет шире дерева. Закрывается дословной формулировкой различения и её приёмочным критерием |
| T-10-47-04 | Tampering | исполненные планы 10-35…10-40 | high | mitigate | Дескриптор, дописанный задним числом, превратил бы обещание в принуждение росчерком пера. Закрывается прямым запретом и утверждением о неизменности файлов планов не выше 40 |
| T-10-47-05 | Repudiation | решение о 39 при 206 запретах фазы | medium | mitigate | Решение о доле, объявленной целым, переоткроется на первом взгляде в соседний план. Закрывается предъявлением чисел по всей фазе и по восьмой партии в том же предмете решения |
| T-10-47-06 | Repudiation | раздел ответа, дописанный в конец файла | medium | mitigate | Повторное исполнение завело бы второй раздел — дословный класс `V9-06`. Закрывается записью по якорю и счётом вхождений до и после |
| T-10-47-SC | Tampering | npm/pip/cargo installs | n/a | accept | ЗАМЕР: план не ставит ни одного пакета — создаётся один документ записи. Гейт легитимности пакетов неприменим |
</threat_model>

<verification>
1. `10-PROHIBITIONS-SUBJECT.md` существует, несёт таблицу всех записей замера и числа по фазе и по
   партии.
2. Раздел ответа встречается ровно один раз; идентификатор ветви, имя и дата заполнены не заглушкой.
3. `uv run pytest tests/test_planning/ -q` — зелено (28 правил).
4. `git diff --numstat -- tests app .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — пусто.
5. Ни один файл `10-01`…`10-40` не изменён.
</verification>

<success_criteria>
- Тридцать девять запретов седьмой партии имеют предъявленное состояние, а не только число.
- Дескриптор проверки отделён от факта проверки именем найденного правила.
- Четырнадцать запретов без дескриптора названы полным текстом и разделены на выразимые и невыразимые.
- Истинный размер предмета (206 запретов фазы, 33 прибавки восьмой партии) предъявлен, а не скрыт.
- Решение принял владелец, а не исполнитель, и оно записано машинно читаемым местом.
- Ни один запрет не объявлен соблюдённым, и ни одно правило настоящим планом не заведено.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-47-SUMMARY.md` when done
</output>
