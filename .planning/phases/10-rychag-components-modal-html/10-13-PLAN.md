---
phase: 10-rychag-components-modal-html
plan: 13
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/templates/ads/form.html
  - tests/test_pages/test_hx_location_destinations.py
  - .planning/WINDOWS.md
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер. Состояние калибровки СНЯТО
  # ПЛАНИРОВЩИКОМ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. Отсюда tokens == raw_tokens и confidence: low, ВЫВЕДЕННЫЙ ИЗ
  # ЧИСЛА ЗАМЕРОВ (их ноль), а не из самооценки планировщика.
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ШТАТНОЕ УДАЛЕНИЕ ПОСЛЕДНЕГО РАСПИСАНИЯ БОЛЬШЕ НЕ УБИВАЕТ КЛИЕНТСКИЙ СЛОЙ РЕДАКТОРА: исходник инлайн-скрипта экрана редактора исполняется ДВАЖДЫ В ОДНОЙ ОБЛАСТИ ИМЁН и второй раз не роняет себя ранней ошибкой языка"
    - "СВОЙСТВО УТВЕРЖДЕНО ИСПОЛНЕНИЕМ, А НЕ ЧТЕНИЕМ ТЕКСТА: правило поднимает интерпретатор JS общим запуском проекта, подаёт ему НАСТОЯЩИЙ отрисованный исходник экрана и сличает вердикты двух прогонов — то есть достаёт ровно до того места, где дефект живёт"
    - "ПРАВИЛО ПОКАЗАНО КРАСНЫМ НА НЕПРАВЛЕНОМ ДЕРЕВЕ: второй прогон исходника ДО правки отказывает ранней ошибкой языка с именем первого же столкнувшегося объявления, и вывод приведён дословно"
    - "ПЕРВЫЙ ПРОГОН ОБЯЗАН ПРОХОДИТЬ (антивакуум): правило, у которого падают ОБА прогона, зеленело бы после правки, ничего о переисполнении не утверждая"
    - "НИ ОДИН ЭКРАН, СЛУЖАЩИЙ ЦЕЛЬЮ ЗАГОЛОВКА ПЕРЕХОДА, НЕ НЕСЁТ ОБЪЯВЛЕНИЙ ВЕРХНЕГО УРОВНЯ В ИНЛАЙН-СКРИПТЕ: перечень целей выведен из ВЫЗОВОВ слоя ответа, отдающих заголовок перехода, а не набран руками, и его число объявлено"
    - "ЧИСЛО ЦЕЛЕЙ ДВИГАЕТСЯ ЯВНО: новый вызов слоя ответа без фрагмента краснит прогон, и следующий экран-цель не приезжает в перечень молча"
    - "РЕБРО `empty` ЗОНДА ПОКРЫТИЯ РАЗРЕШЕНО ЯВНЫМ КРИТЕРИЕМ: удаление ПОСЛЕДНЕГО расписания — это и есть ветка перехода, и экран, на который она приземляет, переживает своё переисполнение"
    - "КРИТЕРИЙ 3 РОАДМАПА НЕ НАРУШЕН, И ЭТО ЗАМЕРЕНО, А НЕ ОБЕЩАНО: правка не заводит ни файла в каталоге сценариев, ни встроенного обработчика отправки, ни атрибута события разметки, ни узла клиентского состояния — четыре числа D-13 стоят на месте, и обход каталога сценариев диффом пуст"
    - "ЭКРАН, НЕСУЩИЙ ТУ ЖЕ ФОРМУ, НО НЕ ЯВЛЯЮЩИЙСЯ ЦЕЛЬЮ, НЕ ПРАВИТСЯ ВСЛЕПУЮ: у экрана подключения его функции достижимы ИЗ АТРИБУТОВ РАЗМЕТКИ, и обёртка сломала бы три кнопки — изъятие записано С ОСНОВАНИЕМ и передано окном, а не сделано молча и не проглочено"
  artifacts:
    - "app/templates/ads/form.html — инлайн-скрипт редактора, переживающий переисполнение: объявления верхнего уровня из области имён документа убраны"
    - "tests/test_pages/test_hx_location_destinations.py — гарнир двойного исполнения на общем запуске интерпретатора и правило, сличающее вердикты двух прогонов"
    - "tests/test_pages/test_hx_location_destinations.py — перечень экранов-целей заголовка перехода, выведенный из вызовов слоя ответа, с объявленным числом и летописью"
    - "tests/test_pages/test_hx_location_destinations.py — статическое правило «цель перехода не несёт объявлений верхнего уровня» с отрицательным контролем"
    - ".planning/WINDOWS.md — окно об экране подключения: та же форма, изъятие с основанием, кандидат назван"
  key_links:
    - "ветка удаления последнего расписания → экран редактора: единственное место, где транспорт фазы приземляет на экран с собственным клиентским слоем; ломается — штатное действие молча убивает загрузку изображений, счётчик символов и снятие вложений, и на сервере нет ни одного признака"
    - "перечень целей → вызовы слоя ответа: единственное, чем следующий экран-цель не приезжает без проверки; ломается — правило стережёт известные цели и слепо к новой, ровно как гейт величины был слеп за границей int32"
    - "гарнир двойного исполнения → общий запуск интерпретатора: единственное, чем правило ДОСТАЁТ до предмета; ломается — правило читает текст скрипта вместо того, чтобы его исполнить, и зеленеет на любой форме записи, которая ему нравится"
  prohibitions:
    - statement: "машинная подмена рантайма разметки НЕ заводится: интерпретатор здесь исполняет ИСХОДНИК СКРИПТА ЭКРАНА, а не изображает свопы htmx и не утверждает, что внеполосный узел применён. Правило, объявившее обратное, вернуло бы фазе ровно тот дефект, которым она провалена первым кругом, — расхождение ЗАПИСИ с рантаймом"
      status: flagged-unverified
      verification: none
    - statement: "штатное действие пользователя НЕ имеет права молча снести уже работающий клиентский слой: единственным признаком отказа остаётся одна строка в консоли, которой человек не видит, и продукт после этого выглядит рабочим, не будучи им"
      status: flagged-unverified
      verification: none
    - statement: "экран подключения НЕ оборачивается вслепую ради зелёного правила: его функции вызываются из атрибутов разметки, обёртка убрала бы их из области имён документа и сломала бы три кнопки — изъятие записывается с основанием, а не обходится расширением правила"
      status: flagged-unverified
      verification: none
    - statement: "критерий 3 роадмапа НЕ трактуется вольно: прочтение по числам принято решением владельца (D-13), и правка обязана оставить все четыре числа на месте — послабление здесь было бы починкой одного критерия ценой другого"
      status: flagged-unverified
      verification: none
  # ⚠️ РЕБРА ЗОНДА ПОКРЫТИЯ. Здесь разрешено ребро `empty` («каков результат для
  # пустого входа?») — оно ложится ровно на ветку удаления ПОСЛЕДНЕГО расписания и
  # выписано выше обычной строкой `truths`. Рёбра `adjacency` и `idempotency`
  # разрешены планом 10-12; `ordering` и `concurrency` вынесены явными допущениями
  # в план 10-16. Молчаливых потерь нет.
---

<objective>
Закрывается блокер `CR-03` отчёта верификации третьего круга — РЕГРЕССИЯ, ЗАВЕДЁННАЯ ЭТОЙ ФАЗОЙ:
переведя ветку удаления последнего расписания с обычного перенаправления на 204 с заголовком
перехода, фаза сделала переисполнение инлайн-скрипта редактора объявления неизбежным, а скрипт к
переисполнению не готов ни на строчку.

Purpose: цепь установлена по коду звено за звеном и каждое звено измерено — ответ 204 с заголовком
перехода, ajax-переход рантайма с целью по умолчанию и подменой содержимого тела, разрешённое
исполнение узлов сценария в подменённом теле, и восемнадцать объявлений верхнего уровня в обычном
инлайн-скрипте экрана-цели. Повторное объявление есть РАННЯЯ ошибка языка: скрипт падает целиком,
до исполнения первой строки. Умирают загрузка изображений, счётчик символов и снятие вложений —
молча, без единого признака на сервере. До фазы маршрут отвечал перенаправлением, браузер строил
документ заново, и скрипт исполнялся ровно один раз.

Output: скрипт редактора, переживающий переисполнение; правило, ИСПОЛНЯЮЩЕЕ его исходник дважды в
одной области имён и потому достающее до дефекта; перечень экранов-целей заголовка перехода,
выведенный из вызовов слоя ответа, с объявленным числом; изъятие экрана подключения с основанием и
окном.

⚠️ ГРАНИЦА ДОКАЗУЕМОГО НАЗЫВАЕТСЯ ЗДЕСЬ ПРЯМО, А НЕ ОБНАРУЖИВАЕТСЯ СЛЕДУЮЩИМ КРУГОМ. Правило,
заводимое этим планом, доказывает, что ИСХОДНИК СКРИПТА переживает второе исполнение в одной
области имён. Что рантайм браузера ведёт себя так, как установлено цепью, — наблюдается только
глазом; это шаг 1.10 обхода `10-UAT.md`, и он передан плану 10-16. Настоящий план закрывает
машинную половину и НЕ ОБЪЯВЛЯЕТ закрытой человеческую.

⚠️ ЭТОТ ПЛАН — ЗАКРЫТИЕ ГЕЙПОВ (`gap_closure: true`), ТРЕТЬЯ ПАРТИЯ, ВОЛНА 1. Планы 10-01…10-11
исполнены и НЕ ПРАВЯТСЯ.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Создаётся ЭТИМ планом (проход source-grounding обязан считать новым):

- `tests/test_pages/test_hx_location_destinations.py` — модуль целиком (новый файл)
- `DOUBLE_EXECUTION_HARNESS` — гарнир двойного исполнения исходника скрипта, там же
- `_run_twice` — сборка павлоада и вызов общего запуска интерпретатора, там же
- `_inline_script_bodies` — извлечение тел инлайн-скриптов из ОТРИСОВАННОГО документа, принимающее
  разметку параметром, там же
- `test_the_editor_script_survives_a_second_execution_in_the_same_realm` — несущее правило, там же
- `test_the_first_execution_of_the_editor_script_succeeds` — положительный контроль (антивакуум),
  там же
- `test_a_reinstated_top_level_binding_reddens_the_double_execution_rule` — отрицательный контроль
  (мутация), там же
- `HX_LOCATION_DESTINATION_CALLS_DECLARED` — объявленное число вызовов слоя ответа, отдающих
  заголовок перехода, там же
- `HX_LOCATION_DESTINATION_TEMPLATES` — перечень экранов-целей, там же
- `TOP_LEVEL_BINDING_EXEMPT_TEMPLATES` — перечень изъятий с основанием на запись, там же
- `test_the_number_of_transition_answering_calls_is_declared` — правило числа, там же
- `test_no_transition_destination_declares_a_top_level_binding_inline` — статическое правило, там же
- `_strip_js_comments` — вырезание комментариев JS до разбора, там же

Существует ДО плана и только ПРАВИТСЯ либо ВВОЗИТСЯ (новым не является): `run_node_script`,
`NODE_BIN`, `authed_client`, `db_session`, `templates`, `_strip_comments`, `_all_templates`,
`respond`, `location_response`, `app/templates/ads/form.html`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: исходник скрипта редактора исполняется дважды в одной области имён — правило, красный, и правка</name>

  <files>
tests/test_pages/test_hx_location_destinations.py
app/templates/ads/form.html
  </files>

  <read_first>
- `app/templates/ads/form.html:307-503` — ИНЛАЙН-СКРИПТ ЦЕЛИКОМ, от открывающего тега до
  закрывающего. ⚠️ ЧИТАТЬ ВЕСЬ, А НЕ ШАПКУ: правка обязана сохранить порядок объявлений, все
  подстановки шаблонизатора и хвост, вызывающий отрисовку вложений последней строкой.
- `app/templates/ads/form.html:311-339` — восемнадцать объявлений верхнего уровня. Это предмет: они
  переживают снос узла скрипта, и повторное объявление есть ранняя ошибка языка.
- `app/templates/ads/form.html:479-503` — хвост: два условных подключения обработчиков, четыре
  безусловных подключения на форму объявления и вызов отрисовки. ⚠️ ВСЕ ПОДКЛЮЧЕНИЯ ИДУТ НА УЗЛЫ,
  ВЗЯТЫЕ ИЗ ДОКУМЕНТА, И НИ ОДНОГО — НА ДОКУМЕНТ ИЛИ ОКНО. ЭТО ИЗМЕРЕНО: повторное исполнение
  подключит обработчики к СВЕЖИМ узлам подменённого тела, а не удвоит подписку на долгоживущем
  объекте. Основание переносится в комментарий правки.
- `app/templates/ads/form.html:1-24` — шапка шаблона: он наследует общий шелл и его содержимое
  живёт в блоке. Скрипт стоит ВНУТРИ блока.
- `app/templates/includes/htmx_error_banner.html:102-115` — ДЕЙСТВУЮЩИЙ ПРИЁМ ПРОЕКТА против
  повторного исполнения при подмене тела: признак на наборе данных долгоживущего узла. ⚠️ ЧИТАТЬ
  ОБЯЗАТЕЛЬНО: проект эту опасность уже знал и однажды уже защитился от неё; скрипт редактора той
  же защиты не получил, когда фаза сделала его целью подмены.
- `app/pages/htmx.py:132-150` — `location_response`: ответ 204 с заголовком перехода. Кадр, с
  которого начинается цепь.
- `app/pages/htmx.py:305-365` — `respond`: ⚠️ ВЕТВЛЕНИЕ ЗДЕСЬ И ЕСТЬ ОПРЕДЕЛЕНИЕ МНОЖЕСТВА ЦЕЛЕЙ.
  Заголовок перехода отдаётся ровно тогда, когда фрагмент НЕ ЗАДАН; вызов с фрагментом на пути
  разметки отдаёт фрагмент и целью перехода не является.
- `app/pages/schedules.py:1035-1042` — ветка удаления ПОСЛЕДНЕГО расписания: вызов слоя ответа без
  фрагмента, то есть приземление на экран редактора заголовком перехода.
- `tests/conftest.py:379-432` — `run_node_script`: ЕДИНСТВЕННЫЙ на проект запуск интерпретатора.
  ⚠️ ЧИТАТЬ ДОСЛОВНО ДВА ЕГО СВОЙСТВА: вердикт — ПОСЛЕДНЯЯ строка стандартного вывода, разобранная
  как JSON; ненулевой код подпроцесса РОНЯЕТ правило. Отсюда следует форма гарнира: он обязан
  ловить отказы сам и печатать вердикт, а не падать.
- `tests/test_templates/test_components.py:1921-1960` и `:2660-2725` — образец гарнира и его
  вызова: подстановка павлоада по образцу, утверждение единственности образца, форма вердикта.
  Новый гарнир наследует эту форму целиком.
- `tests/conftest.py:125-137` — `authed_client`: отрисованный документ берётся НАСТОЯЩИМ запросом
  по маршруту редактора, а не сборкой шаблона с выдуманным окружением.
- `tests/test_pages/test_editor_schedules.py:52-70` — `_seed_ad`: посев объявления, из которого
  собирается адрес редактора.
  </read_first>

  <precondition>
Интерпретатор JS доступен в PATH под именем, объявленным в `tests/conftest.py`. Проверяется
однократно перед началом: `node --version` печатает версию. Отсутствие интерпретатора — ОСТАНОВ:
общий запуск проекта в этом случае роняет правило намеренно (`pytest.skip` там запрещён), и
заводить правило, которое некому исполнить, бессмысленно.
  </precondition>

  <behavior>
СРЕЗ ВЕРТИКАЛЬНЫЙ: посев объявления → настоящий запрос по маршруту редактора → извлечение тела
инлайн-скрипта из отрисованного документа → исполнение исходника ДВАЖДЫ в одной области имён →
вердикт. Ни одного слоя-заглушки: исходник настоящий, подстановки шаблонизатора настоящие,
интерпретатор настоящий.

**RED (первый ход).**
1. Правило заводится ДО правки шаблона и прогоняется на неправленом дереве.
2. Ожидаемый цвет — КРАСНЫЙ, и ожидаемая причина названа заранее: второй прогон отказывает РАННЕЙ
   ОШИБКОЙ ЯЗЫКА, и её текст называет имя первого столкнувшегося объявления. Отказ иного рода
   означает, что гарнир меряет не то, — это правится ДО перехода к зелёному.
3. Вывод покрасневшего прогона снимается ДОСЛОВНО, включая имя и текст отказа, и уезжает в сводку.

**ПОЛОЖИТЕЛЬНЫЙ КОНТРОЛЬ (антивакуум, обязателен).**
4. ПЕРВЫЙ прогон исходника обязан ПРОХОДИТЬ. Правило, у которого падают оба прогона, после правки
   позеленело бы, не утверждая о переисполнении ничего: достаточно было бы, чтобы отказ первого
   прогона перестал быть ранней ошибкой языка.
5. Отсюда требование к заглушке окружения: она обязана быть достаточной, чтобы весь скрипт прошёл
   до конца, включая хвост с подключением обработчиков и вызовом отрисовки.

**GREEN.**
6. После правки шаблона ВТОРОЙ прогон проходит так же, как первый: вердикт обоих — успех.
7. Отрисованный документ по-прежнему несёт ровно один инлайн-скрипт экрана, и его содержимое
   по-прежнему подставляется шаблонизатором (значения из настроек и из базы приезжают
   сериализацией, а не текстом).

**ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ (мутация, обязателен).**
8. Тому же гарниру подаётся исходник, в который ВЕРНУЛИ одно объявление верхнего уровня, и правило
   обязано на нём покраснеть. Копия собирается в памяти; файл дерева не правится.
  </behavior>

  <action>
**1. Модуль и гарнир.** Завести `tests/test_pages/test_hx_location_destinations.py`. Ввезти общий
запуск интерпретатора из `tests/conftest`; собственного вызова подпроцесса НЕ ЗАВОДИТЬ — имя
интерпретатора есть свойство запуска, и второй его экземпляр разошёлся бы с первым молча (записанное
основание плана 09-19).

Гарнир `DOUBLE_EXECUTION_HARNESS` собирается по образцу действующего гарнира жизненного цикла
панели: павлоад приезжает ПОДСТАНОВКОЙ по образцу, встречающемуся ровно один раз, и единственность
образца утверждается перед подстановкой.

Внутри гарнира: создать ОДНУ область имён средствами стандартного модуля виртуальной машины
интерпретатора и исполнить в ней поданный исходник ДВАЖДЫ. Каждый прогон обёрнут в перехват; вердикт
— объект с двумя записями, у каждой признак успеха, имя отказа и его текст. Гарнир обязан завершаться
УСПЕШНО в любом случае и печатать вердикт последней строкой: ненулевой код подпроцесса роняет правило
и сделал бы красный неотличимым от поломки гарнира.

Окружение области имён — ЗАГЛУШКА ДОКУМЕНТА, снисходительная по построению: любое обращение к
свойству возвращает вызываемый объект того же рода, любой вызов возвращает его же. Основание
записать прямо: предмет правила — СТОЛКНОВЕНИЕ ОБЪЯВЛЕНИЙ, а не полнота модели документа, и
заглушка, спотыкающаяся на каком-нибудь узле, превратила бы правило в утверждение о заглушке.
Заглушка обязана покрывать всё, чем скрипт пользуется на верхнем уровне: выборку узлов по
идентификатору, выборку внутри узла, подключение обработчиков, создание узлов, набор данных,
список классов и сетевой вызов.

**2. Извлечение исходника.** Завести `_inline_script_bodies`, принимающий РАЗМЕТКУ параметром и
возвращающий тела инлайн-скриптов. Параметр, а не константа, — по тому же основанию, по которому
разборщик шаблонов проекта принимает каталог: без этого отрицательный контроль был бы невыразим.
Правило берёт разметку НАСТОЯЩИМ запросом по маршруту редактора под личностью обычного пользователя,
посеяв объявление действующим помощником.

⚠️ ОТБИРАЕТСЯ СКРИПТ ЭКРАНА, А НЕ ЛЮБОЙ. Документ несёт ещё и скрипты общего шелла; правило обязано
выбрать тело по признаку, принадлежащему именно экрану редактора, и утвердить, что выбрано РОВНО
ОДНО тело. Ноль выбранных тел означал бы правило, зеленеющее на пустоте.

**3. Несущее правило и два контроля.** Завести три правила: несущее (второй прогон не отказывает
ранней ошибкой языка), положительный контроль (первый прогон успешен), отрицательный контроль
(исходник с возвращённым объявлением верхнего уровня краснит несущее). Тексты отказов обязаны
приводить имя и сообщение отказа интерпретатора дословно — иначе следующий читатель будет гадать,
что именно столкнулось.

**4. Правка шаблона.** Убрать объявления верхнего уровня скрипта редактора из области имён
документа, сохранив ПОРЯДОК объявлений, все подстановки шаблонизатора и хвост дословно. Способ —
тот, что не требует переписывать тело: содержимое скрипта заключается в немедленно вызываемое
выражение-функцию, и всё, что было верхним уровнем, становится телом этого выражения.

Записать у места правки ЧЕТЫРЕ основания, каждое своей строкой:
(а) ЗАЧЕМ — экран стал целью заголовка перехода, а рантайм разметки исполняет узлы сценария
подменённого тела заново; до фазы маршрут отвечал перенаправлением, и переисполнения не было;
(б) ПОЧЕМУ ЭТОГО ДОСТАТОЧНО — все подключения обработчиков идут на узлы, ВЗЯТЫЕ ИЗ ДОКУМЕНТА, и ни
одно не идёт на документ или окно, поэтому повторное исполнение подключает обработчики к свежим
узлам подменённого тела, а не удваивает подписку на долгоживущем объекте; это ИЗМЕРЕНО чтением
хвоста скрипта, а не предположено;
(в) ПОЧЕМУ НЕ ПРИЗНАК НА ДОЛГОЖИВУЩЕМ УЗЛЕ — приём соседнего включаемого файла защищает от
ПОВТОРНОЙ ПОДПИСКИ на долгоживущий объект, и там он верен; здесь предмет другой — свежие узлы
подменённого тела обязаны получить обработчики ЗАНОВО, и признак, запрещающий второе исполнение,
оставил бы экран после перехода без клиентского слоя вовсе;
(г) ПОЧЕМУ НЕ ОТКАТ ВЕТКИ НА ПЕРЕНАПРАВЛЕНИЕ — форма ответа выбрана решением D-01/D-04 фазы, и
возврат ветки к перенаправлению чинил бы один экран, оставив мину заряженной для следующего
маршрута, приземляющего в редактор.

**5. Критерий 3 роадмапа — назвать напряжение и снять его замером.** Правка меняет ШАБЛОН, а не
каталог сценариев, и по прочтению D-13 критерий запрещает НОВЫЕ СУЩНОСТИ, а не правку
существующих. Не прибавляется ни файла в каталоге сценариев, ни встроенного обработчика отправки,
ни атрибута события разметки, ни узла клиентского состояния. Все четыре утверждения ЗАМЕРИТЬ
прогоном действующих гейтов и обходом каталога сценариев диффом, а не объявить. Замер и его вывод
уезжают в сводку.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо в выводе присутствует «интерпретатор JS» (общий запуск не нашёл интерпретатора и уронил правило намеренно)</fails_when>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо число прошедших отличается от 157 (базовый замер планировщика): четыре числа критерия 3 не имеют права двинуться от правки шаблона</fails_when>
    <automated>git diff 7c1041d..HEAD -- app/static/js/</automated>
    <fails_when>вывод НЕПУСТ — критерий 3 роадмапа требует, чтобы фаза не тронула каталог сценариев ни одной строкой</fails_when>
  </verify>

  <acceptance_criteria>
- **КРАСНЫЙ ПОКАЗАН, А НЕ ЗАЯВЛЕН.** В сводке приведён ДОСЛОВНЫЙ вердикт гарнира на дереве ДО
  правки: первый прогон успешен, второй отказал РАННЕЙ ОШИБКОЙ ЯЗЫКА, и приведено имя первого
  столкнувшегося объявления. Отказ иного рода объявлен остановом, а не принят.
- **АНТИВАКУУМ ПОКАЗАН.** В сводке приведён вердикт первого прогона ДО правки — успех. Правило, у
  которого падали бы оба прогона, к слиянию не принимается.
- **ЗУБЫ ПОКАЗАНЫ.** В сводке приведён вывод отрицательного контроля: исходник с возвращённым
  объявлением верхнего уровня краснит несущее правило, и названо, какое именно объявление
  возвращено.
- Отрисованный документ несёт РОВНО ОДНО тело скрипта экрана: в сводке названо число выбранных тел.
- Подстановки шаблонизатора живы: `grep -c 'tojson' app/templates/ads/form.html` даёт ТО ЖЕ число,
  что и до правки, и оба числа приведены в сводке.
- Порядок объявлений сохранён: в сводке приведён вывод
  `git diff -- app/templates/ads/form.html | grep -cE '^-\s*(const|let) '` и сказано, сколько строк
  объявлений сдвинулось отступом, а не исчезло.
- Хвост скрипта сохранён дословно: `git diff -- app/templates/ads/form.html` не содержит удалённой
  строки вызова отрисовки вложений; проверено и названо в сводке.
- Экран редактора по-прежнему отдаётся: в сводке приведён код ответа настоящего запроса по маршруту
  редактора (`200`) и длина документа.
- **ЧЕТЫРЕ ЧИСЛА КРИТЕРИЯ 3 ЗАМЕРЕНЫ.** В сводке приведены: пустой вывод обхода каталога сценариев
  диффом; число прошедших гейтов разметки и компонентов ДО и ПОСЛЕ правки (оба равны 157); и прямое
  утверждение, что ни встроенного обработчика отправки, ни атрибута события разметки, ни узла
  клиентского состояния правка не прибавила.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правка ограничена одним шаблоном и не меняет ни одной строки логики скрипта: откат — снятие
обёртки. Альтернатива (возврат ветки маршрута к перенаправлению) ОТВЕРГНУТА и основание записано:
она чинила бы один экран, оставив мину для следующего маршрута, приземляющего в редактор, и прямо
противоречила бы D-01/D-04.
  </reversibility>

  <done>
Исходник скрипта редактора исполняется дважды в одной области имён без ранней ошибки языка; правило
показано красным ДО правки и зелено после; антивакуум и мутация показаны выводами; четыре числа
критерия 3 замерены и стоят на месте.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: перечень экранов-целей заголовка перехода, его число и правило «цель не несёт объявлений верхнего уровня»</name>

  <files>
tests/test_pages/test_hx_location_destinations.py
.planning/WINDOWS.md
  </files>

  <read_first>
- `app/pages/htmx.py:305-365` — `respond` ЦЕЛИКОМ. ⚠️ ЗДЕСЬ ЖИВЁТ ОПРЕДЕЛЕНИЕ МНОЖЕСТВА: заголовок
  перехода отдаётся ровно на ветке, где фрагмент не задан. Вызов с фрагментом целью перехода не
  является, и правило, посчитавшее все вызовы подряд, утверждало бы о множестве, которого нет.
- `app/pages/ads.py`, `app/pages/account_groups.py`, `app/pages/accounts.py`, `app/pages/admin.py`,
  `app/pages/history.py`, `app/pages/schedules.py` — ВСЕ вызовы слоя ответа. Планировщик снял их
  обходом: их 31, из них часть подаёт фрагмент. ⚠️ ЧИСЛО ПЛАНИРОВЩИКА ЯВЛЯЕТСЯ ОРИЕНТИРОМ, А НЕ
  ОТВЕТОМ: исполнитель обязан поставить оба числа СВОИМ прогоном покрасневшего правила.
- `app/templates/accounts/connect_tg_user.html:32,44,59` — ТРИ кнопки, вызывающие функции скрипта
  ИЗ АТРИБУТА РАЗМЕТКИ. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО ПЕРЕД ЛЮБЫМ РЕШЕНИЕМ ОБ ЭТОМ ФАЙЛЕ: обёртка убрала бы
  функции из области имён документа и сломала бы все три кнопки. Это ИЗМЕРЕНО, а не предположено.
- `app/templates/accounts/connect_tg_user.html:72-73` — два объявления верхнего уровня той же формы,
  что и у редактора.
- `tests/test_templates/test_htmx_inventory.py:314-340` — `_strip_comments` и записанное основание
  вырезания. Здесь предмет — комментарии JS, и вырезать надо ИХ; основание переносится дословно.
- `tests/test_templates/test_htmx_markup_gates.py:319-340` — `_all_templates(directory)` и
  основание приёма «разборщик принимает источник параметром».
- `tests/test_pages/test_confirm_delete_transport.py:1187-1200` — форма правила объявленного числа
  и форма летописи числа.
- `.planning/WINDOWS.md` записи 39-44 — форма записи окна: тип, файл, строка, текст, состояние.
  </read_first>

  <behavior>
1. Множество целей заголовка перехода ВЫВОДИТСЯ из исходников страничных модулей: считаются вызовы
   слоя ответа, НЕ подающие фрагмент. Число объявляется и стережётся: новый такой вызов краснит
   прогон, и следующий экран-цель не приезжает в перечень молча.
2. Перечень экранов-целей ставится В СООТВЕТСТВИЕ этому числу: каждой цели сопоставлен шаблон,
   который её отрисовывает. Цель без сопоставленного шаблона краснит прогон — иначе перечень
   объявлял бы себя полным, не будучи им.
3. Для каждого шаблона перечня утверждается: его инлайн-скрипты не несут объявлений верхнего уровня.
   Разбор идёт по исходнику С ВЫРЕЗАННЫМИ КОММЕНТАРИЯМИ обоих родов — иначе абзац обоснования,
   называющий запрещённую форму, отменял бы правило сам собой.
4. У правила есть ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ: разборщику подаётся изменённая копия исходника с
   возвращённым объявлением, и правило обязано покраснеть.
5. Экран подключения в перечень целей НЕ ВХОДИТ — измерено: ни один вызов слоя ответа не приземляет
   на него. Он вносится в перечень изъятий С ОСНОВАНИЕМ НА ЗАПИСЬ и передаётся окном, а не
   правится вслепую и не умалчивается.
  </behavior>

  <action>
**1. Число вызовов, отдающих переход.** Завести `HX_LOCATION_DESTINATION_CALLS_DECLARED` и правило
`test_the_number_of_transition_answering_calls_is_declared`. Правило обходит страничные модули,
считает вызовы слоя ответа, у которых НЕ ЗАДАН фрагмент, и сличает счёт с объявленным числом. Счёт
вести по исходнику с вырезанными комментариями и докстрингами: абзацы обоснования в этом проекте
свободно называют и имя функции, и имя её аргумента.

Число ПОСТАВИТЬ ПРОГОНОМ покрасневшего правила; вывод привести дословно. Летопись числа записать по
форме летописей фазы: откуда взялось, чем измерено, каким планом.

**2. Перечень целей.** Завести `HX_LOCATION_DESTINATION_TEMPLATES` — соответствие «адрес
приземления → шаблон, его отрисовывающий». Утвердить, что перечень накрывает ВСЕ адреса, найденные
счётом: адрес без сопоставленного шаблона краснит прогон. Адреса, собираемые из значений
(идентификатор объявления, идентификатор пользователя), приводятся к форме шаблона адреса, и
приведение записывается у самого перечня.

**3. Статическое правило.** Завести `_strip_js_comments` и
`test_no_transition_destination_declares_a_top_level_binding_inline`. Для каждого шаблона перечня:
вырезать комментарии шаблонизатора и разметки действующим разборщиком, вырезать комментарии JS новым,
выделить тела инлайн-скриптов и утвердить отсутствие объявлений верхнего уровня. Объявлением верхнего
уровня считается объявление, стоящее на нулевом уровне вложенности фигурных скобок тела скрипта;
объявления внутри функций и внутри обёртки предметом не являются — уровень вложенности и есть
различающий признак, и его надо считать, а не угадывать отступом.

Сообщение об отказе обязано называть шаблон, номер строки и имя объявления — иначе следующий
читатель получит «где-то есть» вместо предмета.

**4. Отрицательный контроль.** Подать разборщику копию исходника экрана редактора с возвращённым
объявлением верхнего уровня и утвердить, что правило краснеет. Копия — в памяти; файл дерева не
правится.

**5. Изъятие экрана подключения — с основанием, а не молчанием.** Завести
`TOP_LEVEL_BINDING_EXEMPT_TEMPLATES` с одной записью и ОБЯЗАТЕЛЬНЫМ полем основания. Основание
записать полностью: экран несёт ту же форму (два объявления верхнего уровня), но целью заголовка
перехода НЕ ЯВЛЯЕТСЯ — ни один вызов слоя ответа на него не приземляет, и это измерено счётом из
пункта 1; а его функции достижимы ИЗ АТРИБУТОВ РАЗМЕТКИ тремя кнопками, поэтому обёртка убрала бы
их из области имён документа и сломала бы работающий экран ради зелёного правила. Утвердить
правилом, что у каждой записи перечня изъятий основание НЕПУСТО: перечень без обоснований
превращается в список того, до чего не дошли руки (форма, принятая планом 10-06).

Записать окно в `.planning/WINDOWS.md` типом `todo`: предмет — «экран подключения несёт объявления
верхнего уровня в инлайн-скрипте и станет уязвим, как только окажется целью заголовка перехода;
починка требует снятия трёх вызовов из атрибутов разметки», файл — шаблон экрана подключения,
кандидат — Фаза 11 (она переводит разделы письма и может сделать этот экран целью). Состояние —
`open`.

**6. Чего это правило НЕ утверждает — назвать прямо.** Оно утверждает форму ИСХОДНИКА целей. Что
рантайм браузера ведёт себя по установленной цепи и что человек видит живой клиентский слой после
перехода — не утверждается ничем машинным и передано обходу `10-UAT.md` (план 10-16). Записать это
в докстринге модуля, а не оставить читателю.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо в собранном перечне имён отсутствует правило отрицательного контроля статического разбора</fails_when>
    <automated>uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly --collect-only</automated>
    <fails_when>собрано менее шести правил (несущее двойного исполнения, положительный контроль, отрицательный контроль исполнения, число вызовов, статическое правило целей, отрицательный контроль статического разбора) — недостающее правило заведено не было</fails_when>
  </verify>

  <acceptance_criteria>
- **ОБА ЧИСЛА ПОСТАВЛЕНЫ ПРОГОНОМ.** В сводке приведён ДОСЛОВНЫЙ вывод покрасневшего правила числа
  вызовов и только затем записанное значение. Число, посчитанное в уме или взятое из этого плана,
  не принимается: ориентир планировщика (31 вызов слоя ответа всего) назван в сводке РЯДОМ со
  своим замером, и расхождение объяснено.
- Перечень целей ПОЛОН относительно счёта: в сводке приведён вывод правила полноты и названо, какие
  адреса приведены к форме шаблона адреса.
- **ЗУБЫ СТАТИЧЕСКОГО ПРАВИЛА ПОКАЗАНЫ.** В сводке приведён вывод отрицательного контроля: копия
  исходника с возвращённым объявлением краснит правило, и в сообщении об отказе видны шаблон,
  строка и имя объявления.
- **ПРАВИЛО НЕ САМООТМЕНЯЕТСЯ.** В сводке показано, что вырезание комментариев JS работает:
  разборщику подан исходник, в который добавлена строка комментария, называющая запрещённую форму,
  и вердикт правила не изменился.
- Изъятие несёт непустое основание, и это утверждено правилом; в сводке приведён текст основания и
  вывод правила непустоты.
- Экран подключения НЕ ПРАВЛЕН: `git diff --name-only -- app/templates/accounts/` — пусто.
- Окно записано; в сводке приведён его номер и текст.
- Докстринг модуля называет границу утверждаемого; в сводке приведён этот абзац.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правила и перечни не меняют поведения продукта; откат — снятие модуля. Решение НЕ править экран
подключения зафиксировано записью и окном и пересматривается фазой, которая сделает его целью.
  </reversibility>

  <done>
Множество целей заголовка перехода выведено из вызовов слоя ответа, объявлено числом и стережётся;
ни одна цель не несёт объявлений верхнего уровня в инлайн-скрипте; зубы правила показаны мутацией;
изъятие записано с основанием и передано окном; граница утверждаемого названа в самом модуле.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ сервера → дерево документа браузера | заголовок перехода приказывает рантайму разметки заменить содержимое тела и исполнить узлы сценария заменённого содержимого |
| шаблон экрана → область имён документа | объявления верхнего уровня инлайн-скрипта переживают снос своего узла |
| шаблон экрана → атрибуты разметки | функции скрипта достижимы из атрибутов, и обёртка убирает их из области имён |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13-01 | Denial of Service | `app/templates/ads/form.html:307-503`, ветка `app/pages/schedules.py:1042` | high | mitigate | Объявления верхнего уровня убираются из области имён документа (задача 1). Свойство утверждается ИСПОЛНЕНИЕМ исходника дважды в одной области имён, а не чтением его текста. Отказ сегодня СИЛЕН: штатное удаление последнего расписания убивает загрузку изображений, счётчик символов и снятие вложений у любого пользователя, и единственный признак — одна строка в консоли |
| T-10-13-02 | Tampering | будущие цели заголовка перехода | high | mitigate | Перечень целей выводится из вызовов слоя ответа и стережётся объявленным числом (задача 2): следующий экран-цель не приезжает молча. Без этого правило стерегло бы известные цели и было слепо к новой — ровно та форма отказа, которой фаза провалена третьим кругом |
| T-10-13-03 | Denial of Service | `app/templates/accounts/connect_tg_user.html` | medium | accept | Экран несёт ту же форму, но целью перехода не является (измерено счётом). Обёртка сломала бы три кнопки, вызывающие его функции из атрибутов разметки. Изъятие записано с основанием и передано окном Фазе 11 — той, что может сделать экран целью |
| T-10-13-04 | Tampering | статическое правило целей | medium | mitigate | Комментарии обоих родов вырезаются до разбора, и это показано прогоном на исходнике с добавленным комментарием (задача 2): правило, зеленеющее на прозе, называющей искомое, было бы самоотменяющимся |
| T-10-13-05 | Repudiation | граница утверждаемого | medium | mitigate | Модуль в собственном докстринге называет, чего он НЕ утверждает: поведение рантайма браузера и живость клиентского слоя глазом не машинны и переданы обходу. Без этой записи зелёное правило читалось бы как закрытие пункта, который оно не закрывает |
| T-10-13-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: ни одна задача не устанавливает пакетов; интерпретатор JS в проекте уже есть (на нём собран `wa_worker/`) и ввозится действующим общим запуском. Таблица легитимности пакетов не требуется |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly` — зелено,
   собрано не менее шести правил.
2. `uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py -q -p no:randomly`
   — ровно `157 passed`: четыре числа критерия 3 не двинулись.
3. `git diff 7c1041d..HEAD -- app/static/js/` — пусто.
4. `uv run pytest tests/ -q` — прогон целиком зелен; число прошедших названо.
5. `uv run python -m compileall -q app main.py tests` — без ошибок.
6. В сводке приведены ДОСЛОВНО: вердикт гарнира ДО правки (первый прогон успешен, второй отказал
   ранней ошибкой языка), вердикт после, вывод отрицательного контроля исполнения, вывод
   покрасневшего правила числа вызовов, вывод отрицательного контроля статического разбора.
</verification>

<success_criteria>
- Исходник скрипта экрана редактора переживает второе исполнение в одной области имён, и это
  доказано ИСПОЛНЕНИЕМ.
- Правило показано красным ДО правки с названной причиной; антивакуум и мутация показаны выводами.
- Множество целей заголовка перехода выведено из вызовов слоя ответа, объявлено числом с летописью и
  накрыто перечнем шаблонов.
- Ни одна цель не несёт объявлений верхнего уровня в инлайн-скрипте; изъятие ровно одно и оно с
  основанием.
- Четыре числа критерия 3 замерены и стоят на месте; каталог сценариев не тронут.
- Граница машинно утверждаемого названа в самом модуле, а человеческая половина передана плану
  10-16, а не объявлена закрытой.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-13-SUMMARY.md` по завершении.
</output>
