---
phase: 10-rychag-components-modal-html
plan: 26
subsystem: testing
tags: [pytest, markers, test-organization, walkthrough-anchors, wr-07]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "план 10-20 — реестр `WALKTHROUGH_ANCHORS`, объявленное число 20 с летописью и контроль зубов правила якорей"
  - phase: 10-rychag-components-modal-html
    provides: "планы 10-14 и 10-17 — единственное машинное принуждение регламента отметок требований, живущее в `tests/test_planning/`"
provides:
  - "модуль `tests/test_templates/test_walkthrough_anchors.py`: правило якорей наблюдения живёт там, где лежит его предмет — среди правил разметки"
  - "маркер `planning`, объявленный ОДНИМ местом на проект хуком `pytest_configure` в `tests/conftest.py` — без раздела настроек и без смещения корня прогона"
  - "пометка уровня модуля во всех четырёх модулях `tests/test_planning/`: `-m planning` и `-m \"not planning\"` разбирают суиту порознь, ничего не выключая"
  - "замер равенства сумм при переезде (224 = 224) и при разведении (28 + 2846 = 2874) — доказательство, что не потеряно ни одного правила"
affects: [10-27, 10-28, 10-29, 10-30, 10-31, 10-32, Фаза 11]

actuals:
  tokens: 11745
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "переезд правила К СВОЕМУ ПРЕДМЕТУ с доказательством сохранности: число собранных правил сличается ДО и ПОСЛЕ по обоим каталогам и по их сумме, а не заявляется"
    - "маркер каталога как инструмент РАЗБОРА, а не отключения: граница годности записана в двух местах (`tests/conftest.py` и `tests/test_planning/__init__.py`), и оба говорят одно"
    - "регистрация имени маркера ХУКОМ, а не разделом настроек, когда раздела у проекта нет вовсе: его появление сместило бы корень прогона всей суиты ради трёх строк"

key-files:
  created:
    - tests/test_templates/test_walkthrough_anchors.py
  modified:
    - tests/conftest.py
    - tests/test_planning/__init__.py
    - tests/test_planning/test_the_walkthrough_cannot_self_certify.py
    - tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py
    - tests/test_planning/test_requirement_completion_follows_verification.py
    - tests/test_planning/test_state_progress_matches_roadmap.py

key-decisions:
  - "Правило якорей переехало ЦЕЛИКОМ вместе с реестром, объявленным числом и его летописью — сирот в прежнем файле не осталось, включая константу `TEMPLATES_ROOT`, чей единственный потребитель уехал"
  - "Летопись объявленного числа 20 перенесена ДОСЛОВНО (посимвольное совпадение подтверждено сличением с `git show HEAD`): она есть запись о том, ЧЕМ число измерено, и переписанная своими словами стала бы утверждением автора переезда вместо утверждения автора замера"
  - "Форматировщик `_report` СКОПИРОВАН в новый модуль, а не импортирован из прежнего: импорт связал бы каталог правил разметки с каталогом правил записи — ровно ту связь, ради разрыва которой правило и переехало. Скопированы три строки склейки, не несущие ни одного утверждения о дереве"
  - "Корень разметки в новом модуле объявлен в форме, ДЕЙСТВУЮЩЕЙ В КАТАЛОГЕ (`TEMPLATES_DIR = Path(__file__).resolve().parents[2] / \"app\" / \"templates\"`), а не перенесён выражением с прежнего места"
  - "Маркер объявлен хуком `pytest_configure`, `pyproject.toml` не правился ни на символ: раздела `[tool.pytest.ini_options]` у проекта НЕТ вовсе, и его появление сделало бы файл якорем rootdir, изменив поведение всей суиты"
  - "Переехавшее правило собирается ТРЕМЯ, а не четырьмя правилами, как ожидал план: оба отрицательных контроля и положительный антивакуум живут ОДНОЙ функцией по несущему основанию (докстринг называет порядок половин: без (б) половина (а) не отличала бы работу правила от разборщика, находящего нарушение всегда). Функция НЕ РАЗБИТА ради совпадения с планом — это было бы подгонкой замера"

patterns-established:
  - "Доказательство сохранности при переезде: три числа ДО, три ПОСЛЕ, равенство суммы. Заявление «перенесено целиком» без этих шести чисел ничего не стоит"
  - "Граница годности маркера записана в ДВУХ местах, потому что читатель приходит с обеих сторон — от объявления имени и от помеченного каталога"

requirements-completed: [FORM-06]

coverage:
  - id: D1
    description: "Правило существования якоря наблюдения, его реестр, объявленное число с летописью и контроль зубов живут в каталоге правил разметки; переименование узла разметки больше не краснит правило каталога записи"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py#test_every_walkthrough_anchor_exists_in_its_source_template"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py#test_the_number_of_walkthrough_anchors_is_declared"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py#test_control_a_missing_anchor_is_named_by_the_rule"
        status: pass
    human_judgment: false
  - id: D2
    description: "При переезде не потеряно ни одного правила: сумма собранных двумя каталогами равна ДО и ПОСЛЕ (224 = 224)"
    requirement: FORM-06
    verification:
      - kind: other
        ref: "uv run pytest tests/test_templates/ tests/test_planning/ --collect-only -q → 224 ДО, 224 ПОСЛЕ"
        status: pass
    human_judgment: false
  - id: D3
    description: "Каталог правил записи можно прогнать и разобрать порознь: отбор по маркеру и отбор по его отрицанию оба непусты и в сумме равны прогону без отбора"
    requirement: FORM-06
    verification:
      - kind: other
        ref: "uv run pytest tests/ -m planning --collect-only -q → 28/2874; -m 'not planning' → 2846/2874; без отбора → 2874"
        status: pass
      - kind: other
        ref: "uv run pytest tests/ -m 'not planning' --collect-only -q | grep -c tests/test_planning/ → 0"
        status: pass
    human_judgment: false
  - id: D4
    description: "Маркер объявлен, а не введён молча: прогон не печатает предупреждения о незарегистрированном имени, объявление стои́т в одном месте на проект"
    requirement: FORM-06
    verification:
      - kind: other
        ref: "uv run pytest tests/ -W error::pytest.PytestUnknownMarkWarning --collect-only -q → rc=0, вхождений PytestUnknownMarkWarning: 0"
        status: pass
      - kind: other
        ref: "uv run pytest --markers | grep @pytest.mark.planning → объявление найдено"
        status: pass
    human_judgment: false
  - id: D5
    description: "Правка общего для всей суиты `tests/conftest.py` не задела ни одного правила проекта"
    verification:
      - kind: integration
        ref: "uv run pytest tests/ -q → 2874 passed in 2089.97s (0:34:49)"
        status: pass
    human_judgment: false

duration: 21 min
completed: 2026-09-08
status: complete
---

# Phase 10 Plan 26: Правила записи перестают ронять продуктовую суиту чужим предметом Summary

**Правило якорей наблюдения переехало из `tests/test_planning/` в `tests/test_templates/` со всем своим предметом (реестр, объявленное число, дословная летопись, оба основания ненайденности, контроль зубов), а каталог правил записи получил маркер `planning`, объявленный хуком в `tests/conftest.py` — обе причины неудобства сняты, ни одно правило не ослаблено, и это доказано равенством сумм 224 = 224 и 28 + 2846 = 2874.**

## Performance

- **Duration:** 21 min работы + 35 min полного прогона суиты
- **Started:** 2026-09-08T08:47:46Z (по коммиту-предшественнику)
- **Completed:** 2026-09-08T09:08:26Z (второй коммит задачи) / полный прогон завершён 2026-09-08T09:47Z
- **Tasks:** 2
- **Files modified:** 7 (1 создан, 6 изменены)

## Accomplishments

- **Правило якорей живёт там, где лежит его предмет.** `tests/test_templates/test_walkthrough_anchors.py` открывает `app/templates` из каталога правил РАЗМЕТКИ. Переименование узла разметки теперь краснит правило того каталога, который читатель менял, а не документный гейт.
- **Переезд доказан числами, а не заявлен.** Шесть замеров собранных правил (три ДО, три ПОСЛЕ) сходятся: сумма не изменилась, каталоги разошлись ровно на величину блока.
- **Каталог записи можно прогнать и диагностировать порознь.** Маркер `planning` объявлен ОДНИМ местом на проект; отбор по нему и отбор по его отрицанию оба непусты и в сумме равны прогону без отбора.
- **Ничего не выключено.** Прогон без отбора собирает те же 2874 правила, что и до плана; `pyproject.toml` не правился ни на символ; в игнорируемые ничего не добавлено.
- **Полный прогон суиты зелен.** 2874 passed — правка общего `tests/conftest.py` не задела ни одного правила проекта.

## Task Commits

1. **Задача 1: правило якорей переезжает к своему предмету** — `153f531` (refactor)
2. **Задача 2: каталог правил записи получает маркер** — `df82a88` (test)

## Files Created/Modified

- `tests/test_templates/test_walkthrough_anchors.py` — **НОВЫЙ.** Реестр `WALKTHROUGH_ANCHORS` (20 записей), `WALKTHROUGH_ANCHORS_DECLARED = 20` с дословной летописью, `NO_SOURCE_FILE`/`NO_ANCHOR_IN_FILE`, `Anchor`, `MissingAnchor`, `missing_anchors`, `_report` и три собираемых правила. Шапка несёт три утверждения основания переезда и источник (`WR-07`, 2026-09-07).
- `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` — блок якорей изъят ЦЕЛИКОМ (258 строк); на его месте — отсылка к новому владельцу с основанием; удалена осиротевшая `TEMPLATES_ROOT`; исправлена ставшая ложной строка шапки; добавлена пометка модуля.
- `tests/conftest.py` — хук `pytest_configure` с объявлением маркера `planning` и комментарием, называющим ОБА основания.
- `tests/test_planning/__init__.py` — докстринг пакета: зачем маркер, почему помечены все модули до единого, и граница годности («маркер НЕ ЕСТЬ разрешение не прогонять»).
- `tests/test_planning/test_planning_gates_are_independent_of_the_live_verdict.py`, `test_requirement_completion_follows_verification.py`, `test_state_progress_matches_roadmap.py` — пометка уровня модуля со ссылкой на основание.

## Замеры

### Задача 1 — шесть чисел переезда

Команда: `uv run pytest <цель> --collect-only -q` (одиночный `-q`, см. отступление 2).

| Каталог | ДО | ПОСЛЕ | Разность |
|---|---|---|---|
| `tests/test_planning/` (правила записи) | 31 | 28 | **−3** |
| `tests/test_templates/` (правила разметки) | 193 | 196 | **+3** |
| **оба вместе** | **224** | **224** | **0** |

Сумма сошлась: **224 = 224**. Разности равны −3 и +3, а не −4 и +4, как ожидал план — основание отступления 1 ниже.

### Задача 1 — летопись объявленного числа

Перенесена **ДОСЛОВНО**. Сличение с прежним файлом на `HEAD` до плана дало **посимвольное совпадение** (685 символов, `True`). Текст ИЗ НОВОГО ФАЙЛА:

```
# ЛЕТОПИСЬ ЧИСЛА: 20. ОТКУДА ВЗЯЛОСЬ — ВЫПИСКА ИЗ ШАГОВ ВСЕХ ШЕСТИ ПРОВЕРОК обхода
# Фазы 10: выписан каждый узел, который шаг ЧИТАЕТ либо НАЖИМАЕТ, вместе с файлом,
# который артефакт сам называет источником. ЧЕМ ИЗМЕРЕНО — чтением шести проверок и
# проверкой КАЖДОГО якоря ЧТЕНИЕМ файла-источника (не найденных — ноль), а не
# памятью о шаге: якорь, вписанный по памяти, превратил бы правило в проверку самого
# себя. КАКИМ ПЛАНОМ — 10-20 (2026-09-07, четвёртая партия закрытия гейпов Фазы 10).
# ⚠️ МОЛЧА ПОХУДЕВШИЙ РЕЕСТР означает, что наблюдение сняли, не тронув обхода; молча
# выросший — что якорь добавили, не назвав шага, который его читает.
WALKTHROUGH_ANCHORS_DECLARED = 20
```

### Задача 1 — зубы правила НА НОВОМ МЕСТЕ

Подмена одного якоря в копии реестра **в памяти** (ни реестр, ни шаблоны не правились). Вывод дословно:

```
(б) непрáвленый реестр — находок: 0
(а) `[data-заведомо-отсутствующий-якорь]` → components/modal.html (шаг 1.1): якоря нет в файле-источнике
(в) `[data-якорь-без-файла]` → заведомо/несуществующий.html (шаг 1.1): файла-источника нет в дереве
корень разметки: /source/broadcaster/app/templates
```

Подменённый якорь назван **по имени**, вместе с файлом-источником и шагом обхода. Два основания ненайденности по-прежнему **различены**: (а) даёт `якоря нет в файле-источнике`, (в) — `файла-источника нет в дереве`. Корень разметки вычислен от собственного пути нового файла и указывает на `app/templates` — прогоном, а не чтением.

### Задача 2 — три числа отбора

| Отбор | Собрано |
|---|---|
| `-m planning` | **28** / 2874 (2846 deselected) |
| `-m "not planning"` | **2846** / 2874 (28 deselected) |
| без отбора | **2874** |

**28 + 2846 = 2874** — сумма сходится, расхождения нет, непомеченных модулей нет.

Число **без отбора не изменилось**: 2874 до пометки и 2874 после — маркер сам по себе ничего не выключил. Два каталога задачи 1 после пометки собирают те же **224**.

Чистота отбора замерена отдельно: `-m "not planning" --collect-only` даёт **ноль** путей внутри `tests/test_planning/` (`grep -c` → `0`), а `-m planning` перечисляет только четыре модуля этого каталога (3 + 15 + 5 + 5 = 28).

### Задача 2 — предупреждение о незарегистрированном имени

```
uv run pytest tests/ -q -W error::pytest.PytestUnknownMarkWarning --collect-only -q
rc=0
вхождений `PytestUnknownMarkWarning` в выводе: 0
```

```
uv run pytest --markers | grep -A1 "@pytest.mark.planning"
@pytest.mark.planning: правила, чей предмет есть ЗАПИСЬ проекта (`.planning/`), а не его
продукт; заведён для РАЗДЕЛЬНОГО ПРОГОНА И ДИАГНОЗА, а не для отключения
```

### Пометки каталога

| Замер | Значение |
|---|---|
| модулей правил с `pytestmark = pytest.mark.planning` | **4** |
| модулей правил в каталоге (`test_*.py`) | **4** |
| всего `.py` в каталоге (с `__init__.py`) | **5** |

Помечены **все до единого**. Литеральная команда критерия приёмки даёт `5` — основание отступления 5 ниже.

### Полный прогон суиты

`tests/conftest.py` разделяется ВСЕЙ суитой, поэтому прогон снят целиком, а не по двум каталогам:

```
2874 passed, 964 warnings in 2089.97s (0:34:49)
```

Тридцать пять минут — норма проекта для полного прогона, а не зависание.

### Диффы задач

`git diff --name-only` по обеим задачам не содержит **ни `pyproject.toml`, ни единого пути внутри `app/templates/`**. Удалений отслеживаемых файлов нет.

## Decisions Made

Перечень — во frontmatter (`key-decisions`). Отдельно стои́т отметить два:

1. **`_report` скопирован, а не импортирован.** Импорт форматировщика из каталога правил записи связал бы каталог правил разметки с каталогом правил записи — ровно ту связь, ради разрыва которой правило и переехало. Продублированы три строки склейки строк, не несущие ни одного утверждения о дереве; основание записано рядом с копией.
2. **Функция контроля НЕ разбита на две ради совпадения с ожиданием плана.** Разбиение дало бы ожидавшиеся планом четыре собираемых правила, но сломало бы порядок половин, который докстринг называет несущим: без положительной половины (б) отрицательная (а) не отличала бы работу правила от разборщика, находящего нарушение ВСЕГДА. Подгонка замера под план — та же болезнь, что подгонка кода под тест.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Ошибка замера в плане] Переехало ТРИ собираемых правила, а не четыре**

- **Найдено при:** Задача 1, замер ПОСЛЕ переезда
- **Проблема:** План объявил, что переезжают «четыре правила» и что разности каталогов равны `+4` и `−4`. Блок якорей содержит **три** собираемых правила: `test_every_walkthrough_anchor_exists_in_its_source_template`, `test_the_number_of_walkthrough_anchors_is_declared`, `test_control_a_missing_anchor_is_named_by_the_rule`. План посчитал «оба контроля» двумя собираемыми правилами, но обе отрицательные половины (а — подменённый якорь, в — отсутствующий файл-источник) и положительная половина антивакуума (б) живут ОДНОЙ функцией.
- **Починка:** В сводку внесено ЗАМЕРЕННОЕ число (−3 / +3), а не объявленное. Функция контроля НЕ разбита на две ради совпадения: разбиение сломало бы названный докстрингом порядок половин. Несущее свойство плана — «ни одно правило не потеряно» — доказано РАВЕНСТВОМ СУММ (224 = 224), которое от числа функций в блоке не зависит.
- **Файлы:** замер, не правка
- **Проверка:** `uv run pytest tests/test_templates/test_walkthrough_anchors.py --collect-only -q` → 3 собрано; сумма двух каталогов 224 ДО и 224 ПОСЛЕ
- **Коммит:** `153f531`

**2. [Rule 3 - Вакуумная команда проверки] Третья команда `<verify>` задачи 1 не могла измерить того, что объявляла**

- **Найдено при:** Задача 1, замер ДО
- **Проблема:** План задал `uv run pytest tests/test_templates/ tests/test_planning/ -q --collect-only -q` с условием отказа «итоговое число собранных отличается от замера ДО». При ДВОЙНОМ `-q` pytest печатает счёт ПО ФАЙЛАМ и **не печатает итоговой строки вовсе** — названное условие отказа на этом выводе невыразимо, и команда зеленела бы при любом расхождении.
- **Починка:** Команда исправлена на одиночный `-q`, дающий строку `224 tests collected`. Оба замера — ДО и ПОСЛЕ — сняты исправленной командой.
- **Файлы:** команда замера, не правка дерева
- **Проверка:** исправленная команда напечатала `224 tests collected in 0.41s` в обоих замерах
- **Коммит:** замер вошёл в `153f531`

**3. [Rule 1 - Ложная ссылка после изъятия] Шапка прежнего модуля утверждала о правиле, которого в нём больше нет**

- **Найдено при:** Задача 1, изъятие блока
- **Проблема:** Докстринг `test_the_walkthrough_cannot_self_certify.py` содержал «Правило якорей **ниже** утверждает ПРИСУТСТВИЕ узла в шаблоне». После изъятия ниже нет никакого правила якорей — утверждение стало ложным, а читатель шапки пошёл бы искать несуществующее.
- **Починка:** Строка переписана: правило названо ПЕРЕЕХАВШИМ, назван новый адрес, план и источник `WR-07`.
- **Файлы:** `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`
- **Проверка:** `grep -c "test_walkthrough_anchors"` в прежнем файле → `2` (шапка и отсылка на месте блока)
- **Коммит:** `153f531`

**4. [Rule 3 - Осиротевшая константа] `TEMPLATES_ROOT` остался бы указывать из каталога записи в продуктовую разметку**

- **Найдено при:** Задача 1, изъятие блока
- **Проблема:** `TEMPLATES_ROOT = TREE_ROOT / "app" / "templates"` имел РОВНО ОДНОГО потребителя — `missing_anchors` внутри переезжающего блока. Оставленный на месте, он был бы мёртвым объявлением, продолжающим адресовать продуктовую разметку из каталога правил записи, то есть той самой связью, которую `WR-07` называет предметом жалобы.
- **Починка:** Константа удалена вместе с блоком; проверка `assert "TEMPLATES_ROOT" not in new` встроена в скрипт изъятия. В новом модуле объявление заведено заново в форме, действующей в каталоге разметки.
- **Файлы:** `tests/test_planning/test_the_walkthrough_cannot_self_certify.py`, `tests/test_templates/test_walkthrough_anchors.py`
- **Проверка:** `grep -n "TEMPLATES_ROOT"` в прежнем файле → пусто; 28 правил каталога зелены
- **Коммит:** `153f531`

**5. [Rule 1 - Критерий приёмки считает упоминание, а не пометку] `grep -l "pytestmark" tests/test_planning/*.py | wc -l` даёт `5`, а не `4`**

- **Найдено при:** Задача 2, проверка критериев приёмки
- **Проблема:** Критерий требовал значения `4`. Замер даёт `5`, потому что `__init__.py` **упоминает** токен `pytestmark` в прозе — и упоминает по прямому указанию самого плана (шаг 3: «либо один раз в `__init__.py` каталога со ссылкой из модулей»). Литеральная команда считает УПОМИНАНИЯ, а не пометки: два указания плана вступили в противоречие друг с другом.
- **Починка:** Прозу `__init__.py` **не переписывал** — вычёркивание слова ради совпадения счётчика было бы подгонкой замера. Вместо этого замер исправлен так, чтобы мерить предмет: `grep -lE "^pytestmark = pytest\.mark\.planning$" tests/test_planning/test_*.py | wc -l` → **4** при **4** модулях правил в каталоге. Независимое и более сильное доказательство того же свойства: `-m "not planning"` собирает **ноль** путей внутри `tests/test_planning/` — непомеченный модуль дал бы там ненулевое число.
- **Файлы:** замер, не правка дерева
- **Проверка:** исправленная команда → `4`; `grep -c "tests/test_planning/"` в выводе отрицательного отбора → `0`
- **Коммит:** `df82a88`

**6. [Rule 3 - Порядок импортов] `import pytest` дописан в три модуля каталога**

- **Найдено при:** Задача 2, пометка модулей
- **Проблема:** Три из четырёх модулей каталога (`test_planning_gates_are_independent_of_the_live_verdict.py`, `test_state_progress_matches_roadmap.py`, `test_the_walkthrough_cannot_self_certify.py`) не импортировали `pytest` вовсе — пометка уровня модуля без импорта не собралась бы. Первая вставка положила импорт перед блоком стандартной библиотеки, разойдясь с формой соседей.
- **Починка:** Импорт добавлен и поставлен в блок сторонних пакетов после стандартной библиотеки — в форме, действующей в `test_requirement_completion_follows_verification.py`.
- **Файлы:** три модуля `tests/test_planning/`
- **Проверка:** `uv run python -m compileall -q app main.py tests` → чисто; `uv run pytest tests/test_planning/ -q` → 28 passed
- **Коммит:** `df82a88`

---

**Total deviations:** 6 auto-fixed (3 × Rule 1 — ошибка замера/ложная ссылка/негодный критерий, 3 × Rule 3 — вакуумная команда/осиротевшая константа/недостающий импорт)
**Impact on plan:** Ни одно отступление не расширило предмета и не ослабило ни одного правила. Три из шести (№ 1, 2, 5) суть исправления ЗАМЕРА, а не дерева: во всех трёх план объявлял число или условие, которого его собственная команда измерить не могла. Ни в одном случае замер не подгонялся под план — ни разбиением функции, ни правкой прозы ради счётчика.

## Issues Encountered

**Противоречие внутри критериев приёмки задачи 2.** Шаг (3) плана прямо разрешает записать основание маркера один раз в `__init__.py`, а критерий приёмки считает вхождения токена `pytestmark` по всему каталогу, включая `__init__.py`. Разрешённое действие делает критерий недостижимым. Разрешено в пользу ПРЕДМЕТА (все модули правил помечены), а не буквы команды; исправленный замер и независимая проверка отрицательным отбором приведены выше.

**Полный прогон суиты занял 34 минуты 49 секунд.** Это норма проекта, а не зависание. Прогон снят целиком, а не по двум каталогам, потому что `tests/conftest.py` разделяется всей суитой и сужение прогона скрыло бы поломку общей фикстуры.

## Known Stubs

Нет. Заглушек, пропущенных правил и непрогнанных команд `<verify>` план не оставил: все четыре команды задачи 1 и все четыре задачи 2 прогнаны, две из них — в исправленном виде с записанным основанием исправления.

## Threat Flags

Нет. План не заводит ни одного сетевого входа, пути аутентификации, файлового доступа и изменения схемы. Все правки — внутри `tests/`.

## User Setup Required

None - внешней настройки не требуется.

## Next Phase Readiness

- **Готово к плану 10-27.** ⚠️ Граница годности маркера для соседей по волне названа планом прямо: план 10-26 стои́т в волне 1, порядок между планами одной волны ничем не гарантирован, и отбор по ОТРИЦАНИЮ ещё не объявленного имени НЕ ПАДАЕТ — он молча собирает другое множество и даёт зелёный, ничего не измерив. Поэтому планы ТОЙ ЖЕ волны 1 (в том числе 10-27) отбирают продуктовую суиту **путём** (`--ignore=tests/test_planning`), а не маркером. Планы ПОСЛЕДУЮЩИХ волн пользуются `-m "not planning"` свободно: объявление гарантировано волной.
- **Прохибиция, переходящая в чужие руки:** каталог `tests/test_planning/` НЕ добавляется в игнорируемые и не выключается. Маркер заведён затем, чтобы каталог можно было прогнать ОТДЕЛЬНО, а не чтобы не прогонять вовсе. Утверждение записано в двух местах дерева (`tests/conftest.py` и `tests/test_planning/__init__.py`) — обходить его придётся, стерев оба.
- **Блокеров нет.** Полная суита зелена (2874 passed), сборка чиста.

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-08*

## Self-Check: PASSED

- `tests/test_templates/test_walkthrough_anchors.py` — FOUND
- `.planning/phases/10-rychag-components-modal-html/10-26-SUMMARY.md` — FOUND
- коммиты `153f531`, `df82a88`, `83da589` — FOUND
