---
phase: 10-rychag-components-modal-html
plan: 21
type: execute
wave: 2
# ⚠️ ЗАВИСИМОСТЬ ОТ 10-17 — ПО ФАЙЛУ И ПО ПРЕДМЕТУ. Настоящий план правит ТОТ ЖЕ модуль
# `tests/test_planning/test_requirement_completion_follows_verification.py`, что и план 10-17,
# и правит ту же функцию сборки индекса, вокруг которой стоят его контроли. Две правки одного
# файла в одной волне столкнулись бы на файле, а не на предмете.
depends_on: ["10-17"]
gap_closure: true
files_modified:
  - tests/test_planning/test_requirement_completion_follows_verification.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ — форма унаследована
# от планов 10-12…10-20. ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `workflow.security_asvs_level` → `1`;
# `workflow.security_block_on` → `"high"`; `workflow.security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ДВА ОТЧЁТА ВЕРИФИКАЦИИ НА ОДИН КЛЮЧ НЕ ВЫТЕСНЯЮТ ДРУГ ДРУГА МОЛЧА: индекс отчётов ключуется ВЕХОЙ И ФАЗОЙ, и столкновение краснит прогон, называя ОБА пути"
    - "СОБЫТИЕ НАСТУПИТ ПО РАСПИСАНИЮ ПРОЕКТА, А НЕ ПО СЛУЧАЙНОСТИ, И ПРАВИЛО ЗАВОДИТСЯ ИМЕННО ПОЭТОМУ: нумерация фаз в каждой вехе начинается заново, поэтому после архивации v2.1 и старта v2.2 в дереве окажутся два отчёта одного номера"
    - "ВЕХА ОТЧЁТА ВЫВОДИТСЯ ИЗ ЕГО ПУТИ, А НЕ ИЗ ЛИТЕРАЛА: отчёт действующей вехи лежит в каталоге фаз, отчёт архивной — в каталоге вехи; вписанное имя вехи устарело бы в день закрытия вехи, то есть воспроизвело бы предмет блокера четвёртого круга в новом месте"
    - "ВЕХА ЗАПИСИ ТРЕБОВАНИЙ ВЫВОДИТСЯ ИЗ ЕЁ СОБСТВЕННОГО ЗАГОЛОВКА ПО ФОРМЕ, А НЕ ВПИСАНА КОНСТАНТОЙ: смена вехи двигает обе стороны сверки одним движением, и правило продолжает сверять то же самое"
    - "ВЫВОД ВЕХИ НЕ ВЫРОЖДАЕТСЯ В ПУСТОЙ: правило утверждает, что заголовок записи разобран и метка вехи непуста, — иначе несущее правило зеленело бы ВАКУУМОМ, сверяя завершённые строки не с чем"
    - "НЕСУЩЕЕ ПРАВИЛО ПРОДОЛЖАЕТ СТОЯТЬ НА ЖИВОМ ДЕРЕВЕ: правится СПОСОБ ПОИСКА отчёта, а не источник входа правила, и вся работа плана 10-17 по разведению живого и синтетического сохраняется дословно"
    - "ЗУБЫ ПОКАЗАНЫ СИНТЕТИЧЕСКИМ ДЕРЕВОМ ДВУХ ВЕХ: корень с двумя отчётами одного номера в разных вехах даёт ДВА разных ключа, а корень с двумя отчётами одного номера в ОДНОЙ вехе краснит громко"
  artifacts:
    - "tests/test_planning/test_requirement_completion_follows_verification.py — вывод вехи отчёта из его пути и ключ индекса «веха + фаза»"
    - "tests/test_planning/test_requirement_completion_follows_verification.py — громкий отказ на столкновении ключей с названием ОБОИХ путей"
    - "tests/test_planning/test_requirement_completion_follows_verification.py — вывод вехи записи требований из её собственного заголовка по форме, с утверждением непустоты"
    - "tests/test_planning/test_requirement_completion_follows_verification.py — два контроля зубов на синтетическом дереве двух вех"
  key_links:
    - "ключ «веха + фаза» → сверка завершённой строки с вердиктом: единственное, чем сверка не поедет на ЧУЖОЙ вердикт после старта следующей вехи; ломается — несущее правило продолжает зеленеть, ничего не проверяя, и это худший из отказов, потому что тихий"
    - "вывод вехи из пути → соглашение о расположении записи: единственное, чем правило не придётся править в день закрытия вехи; ломается — правило с вписанным именем вехи отключают ровно тогда, когда веха закрывается"
    - "утверждение непустоты вывода → несущее правило: единственное, чем сверка не зазеленеет вакуумом; ломается — завершённые строки сверяются не с чем, и отказа нет ни одного"
  prohibitions:
    - statement: "имя действующей вехи НЕ вписывается в модуль литералом: константа с именем вехи устарела бы в день её закрытия, и правило пришлось бы править ровно тем движением, которое партия объявила недопустимым, — подгонкой утверждения под сегодняшнее дерево"
      status: flagged-unverified
      verification: none
    - statement: "столкновение ключей НЕ разрешается выбором «последнего по сортировке» и не разрешается вовсе: молчаливое разрешение и есть предмет находки, а правило обязано ПАДАТЬ ГРОМКО, называя оба пути, и оставлять решение человеку"
      status: flagged-unverified
      verification: none
    - statement: "результат плана 10-17 НЕ ОТКАТЫВАЕТСЯ и не переписывается: оба отрицательных контроля остаются на синтетических входах, а несущие правила — на живом дереве; настоящий план меняет СПОСОБ ПОИСКА отчёта, а не источник входа"
      status: flagged-unverified
      verification: none
    - statement: "отчёты быстрых работ, чьё имя не несёт номера фазы, В ИНДЕКС НЕ ВНОСЯТСЯ и настоящим планом не вносятся: они принадлежат не фазе, а быстрой работе, и таблица состояний на них не ссылается — основание записано докстрингом самой сборки индекса"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ НАД FORM-06 — ЯВНЫЕ ДОПУЩЕНИЯ ПАРТИИ (полная запись — 10-17-PLAN.md):
  #   • `idempotency` — продуктовая половина закрыта планом 10-06; повтор в РАНТАЙМЕ браузера
  #     наблюдаем только обходом `10-UAT.md`, шаги 1.4-1.9, не пройденные ни разу. `unresolved`.
  #   • `concurrency` — адресован частично планом 10-07 и проверками 2 и 3 обхода; полного
  #     машинного критерия в суите без процесса выразить нечем. `unresolved`.
  # 2 предъявленных = 0 разрешённых критерием + 2 явных допущения.
---

<objective>
Закрывается `WR-08` ревизии кода: индекс отчётов верификации ключуется ТОЛЬКО номером фазы, поэтому
два отчёта одного номера из разных вех молча вытесняют друг друга, и с этого момента завершённая
строка требования старой вехи сверяется с вердиктом ЧУЖОЙ фазы — а несущее правило продолжает
зеленеть, ничего не проверяя.

Purpose: событие наступит ПО РАСПИСАНИЮ ПРОЕКТА, а не по случайности. Нумерация фаз в каждой вехе
начинается заново — это записано решением проекта («Roadmap v2.0: нумерация фаз перезапущена с 1»,
`.planning/STATE.md`), — а обход индекса намеренно рекурсивен и захватывает архив
(`.planning/milestones/…`), чтобы переезд фазы в архив не выключил правило молча. Две верные вещи
вместе дают столкновение ключей: после архивации действующей вехи и старта следующей в дереве
окажутся два отчёта одного номера. Сегодня дубликатов нет, и это ИЗМЕРЕНО (13 отчётов, все имена
уникальны) — предмет в том, что отказ будет ТИХИМ, а не в том, что он уже случился.

⚠️ **ПОЧЕМУ ЭТО ТОТ ЖЕ КЛАСС, ЧТО И БЛОКЕР ПАРТИИ, И ПОЧЕМУ ОБА ЗАКРЫВАЮТСЯ В ОДНОМ ФАЙЛЕ.** Блокер
четвёртого круга — правило, зелень которого держится на сегодняшнем состоянии дерева. `WR-08` —
правило, зелень которого назавтра будет держаться НИ НА ЧЁМ. Оба отказа тихие, оба живут в модуле,
заведённом ради принуждения регламента, и оба чинятся одним движением мысли: величина, которую
правило принимает за неизменную, обязана либо выводиться, либо падать громко.

Output: индекс отчётов ключуется вехой и фазой; веха выводится из ПУТИ отчёта, а не из литерала;
столкновение ключей краснит прогон с названием обоих путей; веха записи требований выводится из её
собственного заголовка по форме, и вывод не вырождается в пустой.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@tests/test_planning/test_requirement_completion_follows_verification.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: сквозной срез — индекс ключуется ВЕХОЙ И ФАЗОЙ, веха выводится из пути, столкновение падает громко</name>

  <files>
tests/test_planning/test_requirement_completion_follows_verification.py
  </files>

  <read_first>
- `tests/test_planning/test_requirement_completion_follows_verification.py:127-184` — `VerificationReport`
  и `_verification_status_by_phase(root)` ЦЕЛИКОМ, вместе с докстрингом. ⚠️ ЧИТАТЬ ДОСЛОВНО:
  докстринг объясняет, ПОЧЕМУ обход рекурсивен (переезд фазы в архив не имеет права выключить
  правило) и ПОЧЕМУ номер берётся из имени файла, а не из поля `phase` шапки (поле несёт слог, а у
  отчётов быстрых работ — вовсе не номер). Обе причины остаются в силе и настоящим планом не
  пересматриваются.
- `:209-235` — `premature_completions`: единственный потребитель индекса. Смена ключа обязана пройти
  через него, а не остаться в сборке.
- `:82-87` — `_normalised_phase`: `07` и `7` — одна фаза. Правило нормализации не трогается.
- Результат плана 10-17 в дереве: оба отрицательных контроля на синтетических входах,
  `_fake_planning_root`, `_synthetic_requirements`. ⚠️ СБОРКА СИНТЕТИЧЕСКОГО КОРНЯ БУДЕТ РАСШИРЕНА
  ЭТОЙ ЗАДАЧЕЙ, а не заменена: она уже раскладывает отчёты по подкаталогам, и вехе нужен ещё один
  уровень.
- Расположение отчётов В ДЕРЕВЕ — СНЯТЬ ЗАМЕРОМ, а не взять из памяти: где лежат отчёты действующей
  вехи, где лежат архивные, и как называется каталог вехи. Замер привести в сводку: соглашение о
  расположении и есть источник вывода вехи, и вписанное «примерно так» увело бы разбор мимо архива.
- `10-REVIEW.md:446-483` — `WR-08` целиком, включая предложенный образец ключа и утверждения об
  отсутствии коллизий.
- `.planning/STATE.md`, запись «Roadmap v2.0: нумерация фаз перезапущена с 1» — основание того, что
  событие наступит по расписанию проекта.
  </read_first>

  <precondition>
План 10-17 приземлён: оба отрицательных контроля питаются синтетическими входами, прогон модуля
зелен. Настоящая задача правит сборку индекса, вокруг которой стоят эти контроли; до 10-17 её правка
столкнулась бы с их живым корнем и предмет размылся бы на два.
  </precondition>

  <behavior>
1. Веха отчёта выводится ИЗ ЕГО ПУТИ по соглашению о расположении записи: отчёт, лежащий в каталоге
   фаз действующей вехи, принадлежит действующей вехе; отчёт, лежащий в каталоге архивной вехи,
   принадлежит ей.
2. Ключ индекса — пара «веха, фаза». Два отчёта одного номера из РАЗНЫХ вех дают два РАЗНЫХ ключа и
   в индексе сосуществуют.
3. Два отчёта одного номера в ОДНОЙ вехе краснят сборку ГРОМКО, называя ОБА пути. Молчаливого
   разрешения нет ни одного: ни «последний по сортировке», ни «первый».
4. Отчёт, чьё имя не несёт номера фазы, в индекс по-прежнему не попадает.
5. Единственный потребитель индекса ищет отчёт по паре, а не по номеру.
6. Зубы: синтетическое дерево с двумя отчётами номера `91` в разных вехах даёт два ключа;
   синтетическое дерево с двумя отчётами номера `91` в одной вехе краснит сборку.
  </behavior>

  <action>
**(1) Завести вывод вехи из пути.** Функция принимает путь отчёта и корень записи и возвращает метку
вехи. Соглашение о расположении СНЯТЬ ЗАМЕРОМ по дереву (пункт `read_first`) и записать его в
докстринг: где лежат отчёты действующей вехи, где архивные, как из пути берётся метка. ⚠️ МЕТКУ
ДЕЙСТВУЮЩЕЙ ВЕХИ НЕ ВПИСЫВАТЬ ЛИТЕРАЛОМ: у отчётов действующей вехи имени вехи в пути нет, поэтому
им даётся метка, ОЗНАЧАЮЩАЯ «действующая», а не имя. Литерал имени вехи устарел бы в день её
закрытия — то есть воспроизвёл бы предмет блокера четвёртого круга в новом месте (объявлено
прохибицией плана).

**(2) Сменить ключ индекса.** `_verification_status_by_phase` переименовать по предмету (индекс
теперь не «по фазе»), ключом сделать пару «веха, фаза», в `VerificationReport` добавить поле вехи.
Провести переименование через ВСЕХ потребителей — их немного, и они найдены поиском ДО правки; число
привести в сводку.

**(3) Столкновение — ГРОМКОЕ.** Перед записью в индекс проверять, что ключа там ещё нет. Есть —
падать с текстом, называющим ключ и ОБА пути, и говорящим, что произошло: индекс молча вытеснил бы
один отчёт другим, и сверка завершённой строки пошла бы с ЧУЖИМ вердиктом. ⚠️ ФОРМА ОТКАЗА —
ИСКЛЮЧЕНИЕ ИЛИ УТВЕРЖДЕНИЕ ВНУТРИ СБОРКИ, а не возврат признака: признак, который потребитель
может не прочитать, есть молчаливое разрешение под другим именем.

**(4) Привести потребителя.** `premature_completions` ищет отчёт по паре «веха строки, фаза строки».
⚠️ ВЕХА СТРОКИ — ПРЕДМЕТ ЗАДАЧИ 2; в настоящей задаче потребитель получает веху ОДНИМ ЯВНЫМ
ПАРАМЕТРОМ либо ищет по метке действующей вехи, и выбранный вариант НАЗЫВАЕТСЯ в докстринге. Второй
источник вехи в этой функции не заводить: два источника одного решения разошлись бы при первой
правке — основание, которым сам модуль объясняет параметризацию входов.

**(5) Расширить `_fake_planning_root`.** Сборка получает возможность класть отчёт в НАЗВАННУЮ веху:
вход дополняется меткой вехи у каждой записи, умолчание — действующая веха, чтобы контроли плана
10-17 не правились вовсе. ⚠️ ЕСЛИ УМОЛЧАНИЕ СОХРАНИТЬ НЕ ВЫХОДИТ — править контроли 10-17 МОЖНО,
но каждая правка называется в сводке поимённо вместе с причиной: партия уже один раз потеряла
свойство на «заодно поправленном» контроле.

**(6) Два контроля зубов.** `test_control_two_reports_of_one_number_in_different_milestones_coexist` —
синтетическое дерево с отчётами номера `91` в действующей и в архивной вехе: в индексе ДВА ключа, и
вердикты у них разные (задать их разными, иначе контроль не отличит два ключа от одного).
`test_control_two_reports_of_one_number_in_one_milestone_fail_loudly` — синтетическое дерево с
двумя отчётами номера `91` в ОДНОЙ вехе: сборка падает, и в тексте отказа видны ОБА пути.

**(7) Что НЕ трогается.** Рекурсивность обхода и её основание. Взятие номера из имени файла и его
основание. `_normalised_phase`. Оба отрицательных контроля плана 10-17 — кроме случая пункта (5),
и тогда с поимённым названием правки. Пять несущих правил, стоящих на живом дереве.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `13` — одиннадцать правил после плана 10-17 плюс два новых контроля не заведены либо не зелены</fails_when>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly -k "different_milestones or one_milestone_fail_loudly"</automated>
    <fails_when>вывод содержит `no tests ran` либо `failed` — два контроля зубов не заведены под названными именами либо не зелены</fails_when>
    <automated>uv run pytest tests/test_planning/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — правка сборки индекса задела соседний модуль каталога либо нарушила правило независимости плана 10-17</fails_when>
  </verify>

  <acceptance_criteria>
- Соглашение о расположении отчётов СНЯТО ЗАМЕРОМ: в сводке приведена команда обхода и её вывод —
  сколько отчётов найдено, сколько из них в архиве, как выглядит путь каждого вида.
- Метки действующей вехи ЛИТЕРАЛОМ в модуле нет: в сводке приведён вывод
  `grep -c 'v2\.1' tests/test_planning/test_requirement_completion_follows_verification.py` → `0`.
- Ключ индекса — пара: в сводке приведён вывод сборки на ЖИВОМ дереве — число ключей и пример
  одного архивного и одного действующего.
- Столкновение падает ГРОМКО: в сводке приведён текст отказа контроля, и в нём видны оба пути.
- Два отчёта одного номера в РАЗНЫХ вехах сосуществуют: в сводке приведены оба ключа и оба вердикта,
  и они различны.
- Число потребителей индекса, найденных ДО правки, приведено в сводке, и все они правлены.
- Контроли плана 10-17 не тронуты — либо каждая правка названа поимённо с причиной.
- Пять несущих правил не тронуты: в сводке приведён перечень имён функций, попавших в дифф.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится сборка индекса внутри ОДНОГО тестового модуля; поведения продукта задача не меняет. Откат
возвращает молчаливое вытеснение — то есть наблюдаем контролем, который на прежней сборке краснеет
по построению.
  </reversibility>

  <done>
Индекс отчётов ключуется вехой и фазой; веха выводится из пути по замеренному соглашению, а не из
литерала; столкновение ключей падает громко с названием обоих путей; два отчёта одного номера из
разных вех сосуществуют, и это показано контролем.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: веха записи требований выводится из её собственного заголовка по ФОРМЕ, и вывод не вырождается в пустой</name>

  <files>
tests/test_planning/test_requirement_completion_follows_verification.py
  </files>

  <read_first>
- `.planning/REQUIREMENTS.md:1` — заголовок документа целиком. ⚠️ ЧИТАТЬ ДОСЛОВНО: он называет веху,
  которой принадлежит запись, и это ЕДИНСТВЕННОЕ место документа, где веха названа для всей записи.
  Форма заголовка снимается ОТСЮДА, а не из памяти.
- `.planning/REQUIREMENTS.md:12`, `:79` — разделы `## v2.1 Requirements` и `## v2.2 Requirements`.
  ⚠️ ЭТО ВТОРОЙ КАНДИДАТ В ИСТОЧНИКИ ВЕХИ, и он ХУЖЕ первого: таблица состояний в документе ОДНА и
  лежит ВНЕ обоих разделов, поэтому вывести веху СТРОКИ из раздела нельзя — строка живёт не в них.
  Это решение записывается в докстринг, а не оставляется читателю: следующий автор придёт ровно к
  этой развилке.
- `.planning/REQUIREMENTS.md:117-180` — раздел прослеживаемости и таблица состояний: где именно
  лежат строки, которые судит несущее правило, и в каком разделе документа.
- `:134-138` — строки `FORM-04`…`FORM-08` с клетками фаз, и строки с клеткой `— (v2.2)`. ⚠️ СТРОКИ
  БЕЗ ФАЗЫ УЖЕ ВЫВЕДЕНЫ ИЗ ВСЕЛЕННОЙ несущего правила и стерегутся отдельным правилом
  (`test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself`) — их настоящая задача не
  трогает.
- Результат задачи 1 в дереве: вывод вехи из пути, ключ-пара, громкий отказ на столкновении и то,
  КАКИМ СПОСОБОМ потребитель индекса получает веху сегодня (пункт (4) задачи 1). Настоящая задача
  заменяет этот способ выводом из заголовка.
- `tests/test_planning/test_state_progress_matches_roadmap.py` — сосед, выводящий счёт из
  ОБЪЯВЛЕННОГО ИСТОЧНИКА и не держащий числовых констант вовсе. Тот же приём и то же основание:
  константа сделала бы правило хрупким ровно к событию, ради которого оно написано.
  </read_first>

  <precondition>
Задача 1 приземлена: индекс ключуется парой и падает громко на столкновении. Без неё выводить веху
записи некуда — потребителю нечем ею воспользоваться.
  </precondition>

  <behavior>
1. Веха записи требований выводится ИЗ ЗАГОЛОВКА документа по ФОРМЕ строки, а не по её точному
   тексту: правка формулировки заголовка не имеет права ронять правило.
2. Вывод не вырождается: заголовок, из которого метка не извлеклась, краснит прогон ГРОМКО, а не
   даёт пустую метку.
3. Несущее правило ищет отчёт по паре «веха записи, фаза строки» и продолжает стоять на ЖИВОМ
   дереве.
4. Отдельное правило утверждает, что метка вехи записи разобрана и непуста, — иначе несущее правило
   зеленело бы вакуумом, не найдя ни одного отчёта и потому не найдя ни одного нарушения.
5. Развилка «заголовок против разделов документа» разрешена и ЗАПИСАНА: таблица состояний лежит вне
   разделов вех, поэтому веху СТРОКИ из раздела вывести нельзя.
6. Зубы: синтетическая запись с заголовком другой вехи и синтетический корень, где отчёт лежит в
   ТОЙ вехе, дают ноль нарушений; тот же корень при заголовке первой вехи даёт нарушение.
  </behavior>

  <action>
**(1) Завести вывод вехи записи.** Функция принимает ИСХОДНИК записи требований ТЕКСТОМ (идиома
модуля) и возвращает метку вехи, разобранную из заголовка ПО ФОРМЕ. Форму снять чтением заголовка.
Метка, не извлёкшаяся из заголовка, — ГРОМКИЙ отказ с текстом заголовка в сообщении: пустая метка
означала бы, что индекс не найдёт ни одного отчёта и правило зазеленеет вакуумом.

**(2) Записать развилку в докстринг.** Назвать ОБА кандидата в источники вехи и сказать, почему
выбран заголовок: разделы `## vN.N Requirements` веху называют, но таблица состояний в документе
ОДНА и лежит ВНЕ обоих разделов, поэтому веху СТРОКИ из раздела вывести нельзя, а веху ДОКУМЕНТА —
можно. ⚠️ ЗАПИСАТЬ ЗДЕСЬ, А НЕ ОСТАВИТЬ ЧИТАТЕЛЮ: следующий автор придёт ровно к этой развилке, и
неразобранная она даст второй источник одного решения.

**(3) Привести несущее правило и `premature_completions`.** Потребитель индекса получает веху,
выведенную из записи, и ищет отчёт по паре. ⚠️ ЕСЛИ ВЕХА ЗАПИСИ НЕ СОВПАДАЕТ С МЕТКОЙ «ДЕЙСТВУЮЩАЯ»
ИЗ ЗАДАЧИ 1 — а она не совпадёт, потому что заголовок называет веху ИМЕНЕМ, а путь действующей вехи
имени не несёт, — завести ОДНО объявленное соответствие «имя вехи записи ↔ метка расположения» и
записать его основание: отчёты действующей вехи лежат в каталоге фаз без имени вехи в пути, и это
СОГЛАШЕНИЕ РАСПОЛОЖЕНИЯ, а не свойство вехи. Соответствие объявить ОДНИМ местом; второе место
разошлось бы с первым при первой правке.

**(4) Правило непустоты `test_the_milestone_of_the_record_is_derived_and_non_empty`.** Утверждает:
метка вехи записи разобрана, непуста, и в индексе ЖИВОГО дерева есть хотя бы один отчёт с этой
меткой. Третья часть несущая: разобранная, но НИ С ЧЕМ НЕ СОВПАВШАЯ метка даёт ровно ту зелень
вакуумом, ради которой правило и пишется.

**(5) Контроль зубов `test_control_a_record_of_another_milestone_looks_up_another_report`.** Подать
синтетическую запись, чей заголовок называет ДРУГУЮ веху, вместе с синтетическим корнем, где отчёт
той же фазы лежит в ДВУХ вехах с РАЗНЫМИ вердиктами. Утверждать: найден вердикт отчёта ИМЕННО той
вехи, которую называет заголовок. Вторая половина — тот же корень при заголовке первой вехи: найден
ДРУГОЙ вердикт. ⚠️ ДВЕ ПОЛОВИНЫ НЕСУЩИЕ ОБЕ: контроль, показавший одну, зеленел бы на поиске,
игнорирующем веху.

**(6) Что НЕ трогается.** Правило
`test_a_row_without_a_phase_is_declared_deferred_by_the_record_itself` и вселенная строк без фазы.
Правило числа строк таблицы и его летопись. Правило согласия флажка с клеткой и оба его контроля.
Результат задачи 1 — кроме способа получения вехи потребителем, который она сама объявила
предметом настоящей задачи. Результат плана 10-17: оба отрицательных контроля остаются на
синтетических входах.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке присутствует слово `failed`, либо число перед словом `passed` меньше `15` — тринадцать правил после задачи 1 плюс правило непустоты и контроль зубов не заведены либо не зелены</fails_when>
    <automated>grep -c "v2\.1" tests/test_planning/test_requirement_completion_follows_verification.py</automated>
    <fails_when>вывод отличен от `0` — имя действующей вехи вписано в модуль литералом, и правило устареет в день закрытия вехи</fails_when>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо в итоговой строке присутствует слово `failed` — правка сборки индекса или вывода вехи задела соседний модуль</fails_when>
  </verify>

  <acceptance_criteria>
- Вывод вехи из заголовка работает на ЖИВОЙ записи: в сводке приведена разобранная метка и строка
  заголовка, из которой она взята.
- Развилка «заголовок против разделов» записана в докстринге: в сводке приведена её цитата, и в ней
  назван оба кандидата и причина выбора.
- Соответствие «имя вехи записи ↔ метка расположения» объявлено ОДНИМ местом: в сводке приведено
  это место и его основание.
- Правило непустоты утверждает ТРИ вещи: метка разобрана, метка непуста, в индексе живого дерева
  есть отчёт с этой меткой. В сводке приведены все три и число найденных отчётов с меткой.
- Контроль зубов показывает ОБЕ половины: в сводке приведены оба найденных вердикта, и они разные.
- Литерала имени вехи в модуле нет: в сводке приведён вывод второй команды раздела `<verify>` (`0`).
- Правила, названные пунктом (6), не тронуты: в сводке приведён перечень имён функций, попавших в
  дифф, и ни одного из них в нём нет.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится вывод одной величины внутри ОДНОГО тестового модуля; поведения продукта задача не меняет.
Откат возвращает поиск по метке расположения вместо вехи записи — то есть наблюдаем контролем,
который на прежнем поиске краснеет по построению.
  </reversibility>

  <done>
Веха записи требований выводится из её собственного заголовка по форме и не вырождается в пустую;
несущее правило ищет отчёт по паре «веха, фаза» и продолжает стоять на живом дереве; развилка
источников вехи разрешена и записана; зубы показаны обеими половинами.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Новые символы `tests/test_planning/test_requirement_completion_follows_verification.py`:**

| Символ | Что это |
|---|---|
| вывод вехи отчёта из пути | метка «действующая» для отчётов каталога фаз, имя вехи для архивных; литерала имени действующей вехи НЕТ |
| поле вехи в `VerificationReport` | часть ключа индекса |
| вывод вехи записи требований из заголовка | форма снята чтением заголовка, отказ громкий с текстом заголовка |
| объявленное соответствие «имя вехи записи ↔ метка расположения» | ОДНО место, с записанным основанием |
| `test_control_two_reports_of_one_number_in_different_milestones_coexist` | зубы: два ключа, два разных вердикта |
| `test_control_two_reports_of_one_number_in_one_milestone_fail_loudly` | зубы: сборка падает, в отказе оба пути |
| `test_the_milestone_of_the_record_is_derived_and_non_empty` | правило непустоты тремя утверждениями |
| `test_control_a_record_of_another_milestone_looks_up_another_report` | зубы двумя половинами |

**Изменённые символы того же файла:** сборка индекса отчётов (ключ-пара «веха, фаза», громкий отказ
на столкновении, переименование по предмету); `premature_completions` (ищет по паре);
`_fake_planning_root` (умеет класть отчёт в названную веху, умолчание — действующая).

**Новых файлов, правок продукта, миграций и правок `.planning/` план НЕ производит.**
</artifacts_this_phase_produces>

<threat_model>
⚠️ Таблица собрана на **ASVS 1** (`workflow.security_asvs_level` → `1`) с порогом блокировки
**`high`** (`workflow.security_block_on` → `"high"`) при `workflow.security_enforcement` → `true`.
Все три величины сняты `config-get`. Строка `*-SC` несёт `accept`: пакетов план не устанавливает.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| номер фазы → отчёт верификации | номер фазы уникален ВНУТРИ вехи, а обход индекса рекурсивен и захватывает архив |
| заголовок записи требований → сверка завершённых строк | веха записи есть половина ключа, и невыведенная она даёт зелень вакуумом |
| соглашение о расположении записи → правило | вписанное имя вехи устареет в день её закрытия |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-21-01 | Spoofing | индекс отчётов верификации | high | mitigate | Ключ становится парой «веха, фаза» (задача 1): завершённая строка старой вехи перестаёт сверяться с вердиктом ЧУЖОЙ фазы. Отказ тихий по природе, поэтому severity высокая при нулевой сегодняшней достижимости |
| T-10-21-02 | Repudiation | столкновение ключей индекса | high | mitigate | Столкновение падает ГРОМКО с названием ОБОИХ путей; молчаливое разрешение («последний по сортировке») объявлено прохибицией плана |
| T-10-21-03 | Denial of Service | вывод вехи записи требований | high | mitigate | Правило непустоты утверждает три вещи, включая наличие в индексе живого дерева хотя бы одного отчёта с выведенной меткой: разобранная, но ни с чем не совпавшая метка даёт зелень вакуумом |
| T-10-21-04 | Tampering | метка действующей вехи | medium | mitigate | Метка выводится из ПУТИ, имени вехи литералом в модуле нет, и это проверяется командой раздела `<verify>`. Литерал устарел бы в день закрытия вехи — предмет блокера четвёртого круга в новом месте |
| T-10-21-05 | Tampering | результат плана 10-17 | medium | mitigate | Сборка синтетического корня РАСШИРЯЕТСЯ умолчанием, а не заменяется; если правка контролей 10-17 всё же понадобится, каждая называется в сводке поимённо с причиной |
| T-10-21-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и `pyproject.toml` не правит |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q -p no:randomly` — зелено.
2. `uv run pytest tests/ -q -p no:randomly` — прогон целиком зелен.
3. `grep -c "v2\.1" tests/test_planning/test_requirement_completion_follows_verification.py` → `0`.
4. **ЗАМЕР СОСУЩЕСТВОВАНИЯ ДВУХ ВЕХ:** исполнитель приводит в сводку вывод сборки индекса на
   синтетическом дереве, где отчёт номера `91` лежит в двух вехах, — два ключа и два разных
   вердикта.
5. **ЗАМЕР НЕЗАВИСИМОСТИ ОТ ВЕРДИКТА СВОЕЙ ФАЗЫ СОХРАНЯЕТСЯ:** повторить проверку 3 раздела
   `<verification>` плана 10-17 (подмена поля `status` отчёта `10-VERIFICATION.md` на `passed` и на
   `human_needed`) и убедиться, что прогон модуля по-прежнему ОДИНАКОВ. Состояние файла после
   замера проверяется `git diff --name-only -- .planning/` — пусто.
6. `git diff --name-only -- .planning/ app/` — пусто.
</verification>

<success_criteria>
- Индекс отчётов ключуется вехой и фазой; веха выводится из пути, а не из литерала.
- Столкновение ключей падает громко и называет оба пути; молчаливого разрешения нет.
- Веха записи требований выводится из её собственного заголовка по форме; вывод не вырождается в
  пустой, и это утверждается правилом непустоты тремя частями.
- Развилка источников вехи разрешена и ЗАПИСАНА, а не оставлена следующему автору.
- Оба предмета показали зубы на синтетическом дереве двух вех.
- Результат плана 10-17 сохранён: контроли остаются на синтетических входах, несущие правила — на
  живом дереве, и независимость прогона от вердикта своей фазы переснята.
- `.planning/` и `app/` не тронуты; требование `FORM-06` остаётся незавершённым.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-21-SUMMARY.md` по завершении.
</output>
