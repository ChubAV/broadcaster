---
phase: 10-rychag-components-modal-html
plan: 15
type: execute
wave: 2
depends_on: ["10-12"]
gap_closure: true
files_modified:
  - tests/test_pages/test_confirm_delete_transport.py
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер. Состояние калибровки СНЯТО
  # ПЛАНИРОВЩИКОМ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. Отсюда tokens == raw_tokens и confidence: low, ВЫВЕДЕННЫЙ ИЗ
  # ЧИСЛА ЗАМЕРОВ, а не из самооценки планировщика.
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "МАТРИЦА НЕГОДНЫХ ВЕЛИЧИН ПРОГОНЯЕТСЯ ПО ОБОИМ ИСТОЧНИКАМ ЗНАЧЕНИЯ, А НЕ ПО ОДНОМУ: те же пять величин едут и ТЕЛОМ формы, и АДРЕСОМ запроса, и обе оси утверждаются в одном модуле"
    - "СЛЕПАЯ ЗОНА НАЗВАНА И ЗАКРЫТА В ЕЁ СОБСТВЕННОМ ИСТОЧНИКЕ: константа адреса обхода лежит ВНУТРИ диапазона, за которым живёт дефект, и потому вся пятизначная матрица прогонялась по заведомо годному пути запроса. Прибавляется ВТОРАЯ константа — вне диапазона, — и её основание записано у неё самой"
    - "ЧИСЛО ОСЕЙ ОБЪЯВЛЕНО: ось, молча выпавшая из обхода, краснит прогон. Без числа следующая правка сняла бы одну из двух и не заметила бы этого — ровно то, что уже произошло с одной из осей"
    - "ИСХОДЫ СНИМАЮТСЯ ВСЕ И СЛИЧАЮТСЯ ОТОБРАЖЕНИЕМ ЦЕЛИКОМ: покрасневшее сличение показывает ВСЕ строки, а не первую, иначе следующая правка чинится по одной строке за прогон"
    - "НИ ОДНА ВЕЛИЧИНА НИ ПО ОДНОЙ ОСИ НЕ ДАЁТ ОТКАЗА ОБРАБОТЧИКА: инвариант T-02-24 / T-02-25 утверждается на обеих осях, а не на той, на которую смотрели"
    - "У КАЖДОЙ ЗАПИСИ ОЖИДАЕМОГО ИСХОДА ЕСТЬ ОСНОВАНИЕ НА ЗАПИСЬ: снятый исход, записанный ожидаемым без объяснения, есть отпечаток поведения, а не утверждение о нём, и зеленел бы на любом поведении, которое случилось первым"
    - "АНТИВАКУУМ ДВОЙНОЙ И ОБА НУЖНЫ: годная величина по оси АДРЕСА по-прежнему даёт фрагментную форму ответа, годная величина по оси ТЕЛА по-прежнему приземляет в свой редактор. Без первого правило зеленело бы на приложении, отвергающем всё; без второго — на приложении, у которого адрес перестал собираться вовсе"
    - "ПРЕЖНЯЯ ФОРМУЛИРОВКА У КОНСТАНТЫ ПОМЕЧЕНА ОПРОВЕРГНУТОЙ, А НЕ ВЫЧЕРКНУТА: в части ЗАПАСНОГО ИСТОЧНИКА КОНТЕКСТА она верна дословно и правка по ней исполнена корректно; ложным было лишь чтение «матрица накрывает маршрут»"
  artifacts:
    - "tests/test_pages/test_confirm_delete_transport.py — константа адреса с величиной ВНЕ диапазона колонки и её основание"
    - "tests/test_pages/test_confirm_delete_transport.py — прогон матрицы по оси АДРЕСА с отображением ожидаемых исходов и основанием у каждой записи"
    - "tests/test_pages/test_confirm_delete_transport.py — объявленное число осей обхода негодных величин с летописью"
    - "tests/test_pages/test_confirm_delete_transport.py — помеченная опровергнутой прежняя формулировка у действующей константы адреса"
  key_links:
    - "матрица величин → ось АДРЕСА: единственное, чем правило достаёт до второго источника той же величины; ломается — гейт стережёт ровно ту половину, которая починена, и слеп ровно к той, которая осталась"
    - "константа вне диапазона → прогон: единственное, чем правило перестаёт зеленеть одинаково на починенном дереве и на сломанном; ломается — зелёная суита предъявляется доводом закрытия и не является им"
    - "объявленное число осей → обход: единственное, чем ось не выпадает из обхода молча; ломается — следующая правка снимает одну из двух и не замечает"
  prohibitions:
    - statement: "снятый исход НЕ записывается ожидаемым без основания: отпечаток поведения зеленеет на любом поведении, которое случилось первым, и утверждением о продукте не является"
      status: flagged-unverified
      verification: none
    - statement: "действующая константа адреса НЕ переписывается и не вычёркивается: её предмет — ЗАПАСНОЙ источник контекста, которому нужна несуществующая строка ВНУТРИ диапазона, и в этом предмете она верна. Прибавляется вторая, а не правится первая"
      status: flagged-unverified
      verification: none
    - statement: "обработчики этим планом НЕ правятся ни на символ: граница величины — предмет плана 10-12, а здесь предмет — способность правила ДОСТАТЬ до неё"
      status: flagged-unverified
      verification: none
  # ⚠️ РЕБРА ЗОНДА ПОКРЫТИЯ. Ребро `adjacency` разрешено планом 10-12 на стороне
  # приложения; здесь оно ПРОДОЛЖЕНО на ось адреса запроса и потому упомянуто, но
  # своим предметом плана не является. Рёбра `empty` (план 10-13), `idempotency`
  # (план 10-12), `ordering` и `concurrency` (явные допущения плана 10-16) здесь не
  # заводятся. Молчаливых потерь нет.
---

<objective>
Закрывается ВТОРОЙ пункт `missing` гэпа `CR-01` отчёта верификации: «прогнать матрицу
`UNUSABLE_AD_ID_VALUES` ещё и по АДРЕСУ запроса, а не только по телу».

Purpose: третий круг верификации назвал главный вывод прямо — зелёная суита ничего не опровергает,
потому что константа стерегущего правила выбрана ВНУТРИ диапазона, за которым дефект и живёт. Вся
пятизначная матрица негодных величин прогонялась по заведомо годному пути запроса. Правило,
неспособное дойти до дефекта, зеленеет одинаково и на починенном дереве, и на сломанном; предъявлять
такой прогон доводом закрытия нельзя.

План 10-12 ставит границу на стороне приложения. Настоящий план делает так, чтобы стерегущее правило
эту границу ДОСТАВАЛО: те же пять величин едут второй осью — адресом запроса, — исходы снимаются все
и сличаются отображением целиком, число осей объявляется, а прежняя формулировка у константы
помечается опровергнутой в той части, в которой она оказалась ложной.

Output: вторая константа адреса с величиной вне диапазона и записанным основанием; прогон матрицы по
оси адреса с отображением ожидаемых исходов, у каждого своё основание; объявленное число осей с
летописью; двойной антивакуум.

⚠️ ЭТОТ ПЛАН — ЗАКРЫТИЕ ГЕЙПОВ (`gap_closure: true`), ТРЕТЬЯ ПАРТИЯ, ВОЛНА 2. Зависимость от плана
10-12 — СУЩЕСТВЕННАЯ, а не файловая: до появления границы величины ось адреса красная по
построению, и «зелёный» этого плана без 10-12 недостижим.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/WINDOWS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-08-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-12-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Создаётся ЭТИМ планом (проход source-grounding обязан считать новым):

- `OUT_OF_RANGE_SCHEDULE_URL` — адрес маршрута удаления с идентификатором ВНЕ диапазона колонки,
  `tests/test_pages/test_confirm_delete_transport.py`
- `_unusable_path_url` — сборка адреса маршрута из подаваемой величины, там же
- `_post_with_path_identifier` — прямой POST по оси АДРЕСА, там же
- `UNUSABLE_PATH_ID_OUTCOMES` — отображение «величина → ожидаемый исход» оси адреса с основанием у
  каждой записи, там же
- `test_an_unusable_path_identifier_never_reaches_the_database` — правило оси адреса, там же
- `test_a_usable_path_identifier_still_answers_with_a_fragment` — антивакуум оси адреса, там же
- `UNUSABLE_VALUE_AXES_DECLARED` — объявленное число осей обхода негодных величин, там же
- `test_the_number_of_unusable_value_axes_is_declared` — правило числа осей, там же

Существует ДО плана и только ПРАВИТСЯ либо ПЕРЕИСПОЛЬЗУЕТСЯ (новым не является):
`UNUSABLE_AD_ID_VALUES`, `UNUSABLE_AD_ID_LANDINGS`, `MISSING_SCHEDULE_URL`, `SUMMARY_SCREEN`,
`FORM_CONTENT_TYPE`, `RETURN_TO_EDITOR_VALUE`, `_unusable_ad_id_body`, `_post_with_context_field`,
`_the_fragment_route`, `_identify`, `_current_user`, `HTMX_HEADERS`, `DOCUMENT_MARK`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: вторая ось — те же пять величин едут АДРЕСОМ запроса, и правило достаёт до второго источника</name>

  <files>
tests/test_pages/test_confirm_delete_transport.py
  </files>

  <read_first>
- `tests/test_pages/test_confirm_delete_transport.py:2230-2270` — шапка раздела негодных величин
  ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ДОСЛОВНО АБЗАЦ «ПОЧЕМУ АДРЕС ЗАПРОСА — НЕСУЩЕСТВУЮЩАЯ СТРОКА»: он и есть
  запись, из-за которой обход не пошёл по второй оси. Довод в нём ВЕРЕН для своего предмета
  (запасной источник контекста вступает в дело только там, где строки нет) и ложен как чтение
  «маршрут накрыт целиком».
- `:2250-2270` — `UNUSABLE_AD_ID_VALUES`: пять величин и записанное у каждой основание. Вторая ось
  прогоняется ТЕМИ ЖЕ пятью; своих величин не заводится.
- `:2272` — `MISSING_SCHEDULE_URL`. ⚠️ ВЕЛИЧИНА ВНУТРИ ДИАПАЗОНА КОЛОНКИ, и это ровно та слепая
  зона, которую отчёт назвал предупреждением таблицы Anti-Patterns.
- `:2274-2300` — `SUMMARY_SCREEN`, `FORM_CONTENT_TYPE`, `RETURN_TO_EDITOR_VALUE`,
  `UNUSABLE_AD_ID_LANDINGS`. Форма отображения ожидаемых исходов наследуется целиком.
- `:2302-2320` — `_unusable_ad_id_body` и `_post_with_context_field`: как собирается тело, зачем
  прослеживаются перенаправления, какие заголовки обязательны. Ось адреса наследует эту форму.
- `:2318-2380` — `test_an_unusable_ad_id_never_reaches_the_database` ЦЕЛИКОМ. ⚠️ ЧИТАТЬ ТРИ ЕГО
  УТВЕРЖДЕНИЯ И ИХ ОСНОВАНИЯ: «не 500» — дословный предмет гэпа; сличение с ДВУМЯ объявленными
  формами ответа — против третьей формы, различимой по величине и возвращающей карту занятых
  идентификаторов (T-10-07); отсутствие целого документа — против прозрачно пройденного
  перенаправления. Правило оси адреса утверждает ТО ЖЕ ТРОЙНОЕ.
- `:2380-2440` — `test_the_landing_screen_is_never_assembled_from_a_rejected_context_field`: форма
  сличения отображением ЦЕЛИКОМ и оба антивакуума с их записанными основаниями.
- `:1575-1590` — `_the_fragment_route`: помощник выбора фрагментного маршрута и его отказ,
  утверждающий единственность.
- `app/pages/schedules.py` после плана 10-12 — общая граница и псевдоним идентификатора пути.
  ⚠️ ЧИТАТЬ, ЧТОБЫ ЗНАТЬ, КАКУЮ ФОРМУ ОТВЕТА ждать: она определена ТАМ, и ожидание здесь обязано
  быть выведено из неё, а не выдумано.
- `.planning/phases/10-rychag-components-modal-html/10-12-SUMMARY.md` — снятые исходы пяти входов.
  Ожидания оси адреса обязаны с ними согласоваться; расхождение — ОСТАНОВ, а не повод подправить
  ожидание.
  </read_first>

  <precondition>
План 10-12 приземлён: `uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly`
зелено, и общая граница величины стоит на идентификаторе пути маршрута удаления. Базовый замер
модуля обхода ДО этого плана: `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly`
→ `57 passed` (замер планировщика; план 10-12 обязан был оставить его без изменений). Расхождение —
ОСТАНОВ с записью.
  </precondition>

  <behavior>
СРЕЗ ВЕРТИКАЛЬНЫЙ: константа величины вне диапазона → сборка адреса → настоящий POST по оси адреса →
снятие исходов всех пяти величин → сличение отображением целиком → антивакуум.

**RED (первый ход, и он показателен даже при уже стоящей границе).**
1. Правило оси адреса заводится и прогоняется. Ожидаемый цвет на дереве С границей — зелёный; но
   его недостаточно.
2. Поэтому красный показывается МУТАЦИЕЙ: правило прогоняется на дереве, где граница идентификатора
   пути временно снята (изменение делается в рабочем дереве и ОТКАТЫВАЕТСЯ немедленно после снятия
   вывода, коммитом не закрепляется). Ожидание — отказ обработчика на величине из двадцати пяти
   девяток. Вывод снимается ДОСЛОВНО.
3. ⚠️ БЕЗ ЭТОГО ШАГА ПЛАН НЕ ИМЕЕТ СМЫСЛА ВОВСЕ: его предмет — не «правило зелено», а «правило
   способно покраснеть на том дереве, на котором дефект жив». Именно это свойство отсутствовало у
   действующего правила, и именно оно проверяется здесь.

**GREEN.**
4. Все пять величин по оси адреса дают исход, не являющийся отказом обработчика.
5. Каждый исход укладывается либо в одну из ДВУХ объявленных фазой форм ответа, либо в отказ
   валидации; третьей различимой формы не появляется.
6. Целого документа в ответе нет ни на одной величине.

**АНТИВАКУУМ (двойной, и оба нужны).**
7. Годный идентификатор ЖИВОЙ строки по оси адреса по-прежнему даёт фрагментную форму ответа.
8. Годная величина по оси ТЕЛА по-прежнему приземляет в свой редактор — действующее правило оси тела
   прогоняется и его число прошедших сличается с базовым.
  </behavior>

  <action>
**1. Константа вне диапазона.** Завести `OUT_OF_RANGE_SCHEDULE_URL` рядом с действующей константой
адреса. Значение собирается из ТОЙ ЖЕ величины из двадцати пяти девяток, что уже стоит в перечне
величин, — ввозом из перечня, а не вторым литералом: второй литерал разошёлся бы с первым молча.

У обеих констант записать разделение предметов:
— действующая нужна ЗАПАСНОМУ ИСТОЧНИКУ КОНТЕКСТА и обязана быть величиной ВНУТРИ диапазона, иначе
запрос отвергается раньше, чем поле контекста вообще читается, и правило теряет предмет;
— новая нужна оси АДРЕСА и обязана быть величиной ВНЕ диапазона.
⚠️ Записать прямо: две константы — не дублирование, а два разных предмета, и свести их в одну
нельзя ни в какую сторону.

**2. Пометить прежнюю формулировку опровергнутой, не вычёркивая.** Абзац «ПОЧЕМУ АДРЕС ЗАПРОСА —
НЕСУЩЕСТВУЮЩАЯ СТРОКА» оставить на месте и приписать рядом истинную формулировку: довод верен для
ЗАПАСНОГО ИСТОЧНИКА КОНТЕКСТА и не является утверждением о покрытии маршрута; чтение «матрица
накрывает маршрут» ОПРОВЕРГНУТО замером верификатора третьего круга, воспроизведённым на маршруте
подтверждённого удаления. Форма записи — идиома D-30/D-32 `STATE.md`: опровергнутое помечается рядом
с истинным, а не стирается.

**3. Сборка адреса и отправка.** Завести `_unusable_path_url`, собирающий адрес маршрута удаления из
подаваемой величины СТРОКОЙ, и `_post_with_path_identifier` по образцу действующей отправки: те же
заголовки разметки, тот же тип содержимого, то же прослеживание перенаправлений и то же основание
для него. Тело формы нести ГОДНЫМ: предмет оси адреса — идентификатор пути, и испорченное тело
проверяло бы обе оси разом, ничего не утверждая ни об одной.

**4. Отображение ожидаемых исходов.** Завести `UNUSABLE_PATH_ID_OUTCOMES` — «величина → ожидаемый
код и адрес приземления» по образцу действующего отображения оси тела.

⚠️ У КАЖДОЙ ЗАПИСИ — СВОЁ ОСНОВАНИЕ НА ЗАПИСЬ, И ЭТО НЕСУЩЕЕ ТРЕБОВАНИЕ, А НЕ УКРАШЕНИЕ. Снятый
исход, записанный ожидаемым без объяснения, есть ОТПЕЧАТОК ПОВЕДЕНИЯ: он позеленеет на любом
поведении, которое случилось первым, и утверждением о продукте не станет. Для каждой из пяти величин
записать, ЧТО именно её отвергает — граница величины на стороне приложения, коэрция типа, домен
идентификатора, — и почему исход именно такой. Величина, чей исход исполнитель объяснить не может,
— ОСТАНОВ с записью окном, а не строка отображения.

**5. Правило оси адреса.** Завести `test_an_unusable_path_identifier_never_reaches_the_database`.
Утверждения — ТЕ ЖЕ ТРИ, что у правила оси тела, и по тем же основаниям: не отказ обработчика; исход
укладывается в объявленные формы либо в отказ валидации; целого документа в ответе нет. Исходы
снимаются ВСЕ и сличаются отображением ЦЕЛИКОМ, а не по одному в цикле с ранним отказом.

**6. Красный мутацией.** Снять границу идентификатора пути в рабочем дереве, прогнать правило оси
адреса, снять вывод ДОСЛОВНО, вернуть границу. Изменение коммитом не закрепляется; факт мутации и её
вывод уезжают в сводку. ⚠️ ПОСЛЕ ВОЗВРАТА ГРАНИЦЫ ОБЯЗАТЕЛЕН ПОВТОРНЫЙ ПРОГОН: мутация, оставшаяся
в дереве, была бы худшим исходом плана, чем его отсутствие.

**7. Антивакуум.** Завести `test_a_usable_path_identifier_still_answers_with_a_fragment`: живой
идентификатор своей строки по оси адреса даёт фрагментную форму ответа. Расстановка берётся
действующим помощником фрагментного маршрута; своих посевов не заводится.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "path_identifier or unusable"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке (фильтр не выбрал ни одного правила — правила заведены под другими именами и не стерегут ничего)</fails_when>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо число прошедших меньше 57 (базовый замер планировщика): ни один действующий случай обхода не имеет права упасть от появления второй оси</fails_when>
    <automated>git diff --name-only -- app/</automated>
    <fails_when>вывод НЕПУСТ — мутация границы осталась в рабочем дереве, и это худший исход плана, чем его отсутствие</fails_when>
  </verify>

  <acceptance_criteria>
- **КРАСНЫЙ ПОКАЗАН МУТАЦИЕЙ.** В сводке приведён ДОСЛОВНЫЙ вывод правила оси адреса на дереве СО
  СНЯТОЙ границей: величина из двадцати пяти девяток даёт отказ обработчика. Прогон, показавший
  только зелёное, к слиянию не принимается — предмет плана есть способность правила покраснеть.
- **МУТАЦИЯ СНЯТА.** В сводке приведён вывод `git diff --name-only -- app/` — пусто — и вывод
  повторного зелёного прогона ПОСЛЕ возврата границы.
- **У КАЖДОЙ ЗАПИСИ ОТОБРАЖЕНИЯ ЕСТЬ ОСНОВАНИЕ.** В сводке приведены все пять записей вместе с их
  основаниями; величина без объяснения объявлена остановом, а не записана.
- **ИСХОДЫ СЛИЧАЮТСЯ ЦЕЛИКОМ.** В сводке показано, что покрасневшее сличение печатает ВСЕ
  несогласные строки: приведён вывод прогона на мутации, и в нём видно более одной строки.
- Две константы адреса разведены по предметам, и разделение записано; в сводке приведён текст обоих
  оснований.
- Прежняя формулировка ПОМЕЧЕНА ОПРОВЕРГНУТОЙ и стоит рядом с истинной; в сводке приведён текст до
  и после.
- Антивакуум показан: в сводке приведён исход годного идентификатора по оси адреса (фрагментная
  форма) и число прошедших действующего правила оси тела ДО и ПОСЛЕ.
- Обработчики этим планом не тронуты (кроме снятой и возвращённой мутации): `git diff --stat -- app/`
  — пусто.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правила и константы не меняют поведения продукта ни на символ; откат — снятие правил. Мутация
границы существует только внутри задачи и обязана быть снята до её завершения.
  </reversibility>

  <done>
Матрица негодных величин прогоняется по оси АДРЕСА запроса; правило показано КРАСНЫМ на дереве, где
дефект жив, и зелёным на починенном; у каждой записи ожидаемого исхода есть основание; антивакуум
двойной; прежняя формулировка помечена опровергнутой, а не вычеркнута; мутация снята и это показано.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: число осей обхода объявлено — ось не выпадает из обхода молча</name>

  <files>
tests/test_pages/test_confirm_delete_transport.py
  </files>

  <read_first>
- `tests/test_pages/test_confirm_delete_transport.py:14-30` — шапка модуля с таблицей осей и чисел.
  ⚠️ ЧИТАТЬ: модуль УЖЕ ведёт числа по осям и запрещает сличать их между собой. Новое число встаёт в
  ту же форму и наследует запрет.
- `:1099` и `:1187-1200` — `CONFIRMED_DELETE_ROUTES_DECLARED` и правило его равенства: образец формы
  объявленного числа, текста отказа и летописи.
- `:890-935` — второе объявленное число со своей летописью. ⚠️ ДВЕ ЛЕТОПИСИ С ОДИНАКОВОЙ
  ФОРМУЛИРОВКОЙ были бы приглашением свести числа в одно; новое число обязано нести СВОЮ.
- Правила и отображения оси адреса, заведённые задачей 1.
  </read_first>

  <behavior>
1. Число осей обхода негодных величин ОБЪЯВЛЕНО. Сегодня их две: тело формы и адрес запроса.
2. Правило собирает оси по признаку, принадлежащему им самим (по перечню отображений ожидаемых
   исходов, у каждой оси своё), и сличает счёт с объявленным.
3. Ось, молча выпавшая из обхода, краснит прогон. Ось, молча прибавленная, — тоже: число двигается
   ЯВНО и с летописью.
4. Число НЕ сличается ни с одним из двух действующих чисел модуля: они считают другие множества, и
   привести их к согласию правкой одного нельзя — запрет уже записан у самих констант.
  </behavior>

  <action>
**1. Число.** Завести `UNUSABLE_VALUE_AXES_DECLARED` и
`test_the_number_of_unusable_value_axes_is_declared`. Оси собирать по перечню отображений ожидаемых
исходов: у оси тела — действующее, у оси адреса — заведённое задачей 1. Сличение — счёт против
объявленного числа, с текстом отказа по образцу двух действующих правил числа.

**2. Летопись.** Записать СВОЮ формулировку летописи: было ноль объявленных осей (число не
велось вовсе), стало два; источник расхождения назван поимённо — отчёт верификации третьего круга,
пункт `gaps[0].missing`, требующий прогона матрицы по адресу запроса; и прямо сказано, что прежнее
отсутствие числа НЕ БЫЛО ОШИБКОЙ ЗАПИСИ — ось была одна, и считать было нечего; ошибкой было то, что
единственность оси нигде не утверждалась и потому её неполнота не могла покраснеть.

**3. Запрет сведения чисел.** Записать у новой константы, что она НЕ приводится к согласию ни с
числом маршрутов, ни с числом случаев обхода: три числа считают три разных множества, и сведение
любых двух лишило бы каждое своего предмета. Форма запрета берётся у действующих констант дословно.

**4. Отрицательный контроль.** Утвердить, что правило числа краснеет, если из перечня осей убрать
одну: перечень собирается разборщиком, принимающим источник параметром, и контроль подаёт ему
укороченный перечень.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly -k "axes or axis"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо в собранном перечне имён отсутствует отрицательный контроль числа осей</fails_when>
  </verify>

  <acceptance_criteria>
- **ЧИСЛО ПОСТАВЛЕНО ПРОГОНОМ.** В сводке приведён ДОСЛОВНЫЙ вывод покрасневшего правила («осей
  обхода … а объявлено …») и только затем записанное значение.
- Летопись числа записана СВОЕЙ формулировкой; в сводке приведён её текст, и видно, что она не
  повторяет формулировок двух действующих летописей.
- Запрет сведения чисел записан у константы; в сводке приведён его текст.
- **ЗУБЫ ПОКАЗАНЫ.** В сводке приведён вывод отрицательного контроля на укороченном перечне осей.
- `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly` — зелено,
  число прошедших названо и сопоставлено с числом после задачи 1.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Число и правило не меняют поведения продукта; откат — снятие константы и правила.
  </reversibility>

  <done>
Число осей обхода негодных величин объявлено, поставлено прогоном, несёт свою летопись и свой запрет
сведения; ось не выпадает из обхода молча, и зубы правила показаны укороченным перечнем.
  </done>
</task>

<task type="execute">
  <name>Задача 3: предупреждение таблицы Anti-Patterns закрыто в его собственном источнике — запись слепой зоны и её граница</name>

  <files>
tests/test_pages/test_confirm_delete_transport.py
  </files>

  <read_first>
- `10-VERIFICATION.md`, таблица Anti-Patterns, строка `tests/test_pages/test_confirm_delete_transport.py:2272`
  — дословный текст предупреждения: «Константа гейта выбрана внутри диапазона, за которым живёт
  дефект» и его следствие «правило зеленеет одинаково на починенном и на сломанном дереве в половине
  своего предмета».
- `10-VERIFICATION.md`, раздел «О ЗЕЛЁНОЙ СУИТЕ» — ⚠️ ЧИТАТЬ ЦЕЛИКОМ: он называет ИЗМЕРЕННУЮ ГРАНИЦУ
  того, что суита утверждает, и говорит прямо, что записывать эту границу надо здесь, а не
  обнаруживать следующим кругом.
- `tests/test_pages/test_confirm_delete_transport.py:1-55` — модульный докстринг: где в нём
  называются границы предмета модуля и в какой форме.
- Константы и правила, заведённые задачами 1 и 2.
  </read_first>

  <behavior>
1. Модуль в СВОЁМ докстринге называет, что он утверждает и чего НЕ утверждает по оси величин: обе
   оси названы; сказано, что до третьего круга ось адреса не обходилась вовсе и почему это не было
   видно.
2. Названа ИЗМЕРЕННАЯ ГРАНИЦА того, что модуль вообще способен утверждать: он идёт через транспорт
   без процесса на встроенной базе и не исполняет ни строчки разметочного рантайма; присутствие узла
   в теле ответа он доказывает, применение узла рантаймом — нет.
3. Запись не самозаверяется: она называет границу, а не объявляет её закрытой.
  </behavior>

  <action>
**1. Запись слепой зоны в докстринге модуля.** Дописать абзац, называющий поимённо: до третьего
круга верификации матрица негодных величин прогонялась ТОЛЬКО по оси тела формы, потому что адрес
запроса был зашит константой внутри диапазона колонки; половина предмета оставалась не обойденной, и
зелёный прогон этого не показывал. Назвать, чем это закрыто (вторая ось, вторая константа,
объявленное число осей) и чем оно НЕ закрыто.

**2. Граница утверждаемого — назвать прямо.** Дописать абзац: модуль идёт через транспорт без
процесса на встроенной базе и не исполняет разметочного рантайма. Он доказывает ДОСТАВКУ разметки и
заголовков и не доказывает ЭФФЕКТА: что снятие произошло, что осиротевшая панель уехала, что ловушки
фокуса в документе не осталось. Эта половина принадлежит обходу `10-UAT.md` и передана плану 10-16.
⚠️ Формулировку взять близкой к тексту отчёта верификации, но НЕ выдавать её за закрытие: запись
называет границу, а не снимает её.

**3. Что этим планом НЕ делается — назвать прямо.** Машинной подмены разметочного рантайма не
заводится ни в каком виде. Правило, объявившее, что внеполосный узел ПРИМЕНЁН, вернуло бы фазе
ровно тот дефект, которым она провалена первым кругом. Планы 10-05…10-07 и 10-10 от этого
отказались сознательно, и настоящий план продолжает тот же отказ.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо число прошедших меньше числа, снятого после задачи 2</fails_when>
    <automated>grep -c '10-UAT.md' tests/test_pages/test_confirm_delete_transport.py</automated>
    <fails_when>вывод `0` — граница утверждаемого не названа, и передача второй половины обходу в модуле не записана</fails_when>
  </verify>

  <acceptance_criteria>
- В сводке приведены ОБА дописанных абзаца целиком.
- Абзац слепой зоны называет: чем она была, почему её не было видно, чем закрыта и чем НЕ закрыта.
- Абзац границы называет то, что модуль доказывает, и то, чего он не доказывает, и передаёт вторую
  половину обходу поимённо.
- Правил этой задачей не заводится и не правится: `git diff -U0 -- tests/test_pages/test_confirm_delete_transport.py`
  за эту задачу не содержит добавленных строк, начинающихся с объявления функции правила; проверено
  и названо в сводке.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правится только проза докстринга; поведения не меняется.
  </reversibility>

  <done>
Слепая зона обхода названа в его собственном источнике вместе с тем, чем она закрыта и чем нет;
граница машинно утверждаемого записана и вторая половина передана обходу поимённо; машинной подмены
рантайма не заведено.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| клиент → адрес запроса маршрута | недоверенная величина приходит ПЕРВОЙ осью и до третьего круга не обходилась вовсе |
| клиент → тело формы маршрута | недоверенная величина приходит ВТОРОЙ осью; она обходилась и была закрыта планом 10-08 |
| правило обхода → предмет утверждения | константа правила, выбранная внутри диапазона, за которым живёт дефект, делает правило неспособным его достать |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15-01 | Denial of Service | `POST /schedules/{id}/delete`, ось адреса | high | mitigate | Матрица негодных величин прогоняется по оси адреса (задача 1), и её способность покраснеть ПОКАЗАНА мутацией границы, а не заявлена. Сама граница — предмет плана 10-12 |
| T-10-15-02 | Repudiation | зелёный прогон как довод закрытия | high | mitigate | Правило, чья константа лежит внутри диапазона, зеленеет одинаково на починенном дереве и на сломанном; предъявление такого прогона доводом есть расхождение записи с деревом. Закрывается второй константой и показанным красным (задача 1) плюс записью слепой зоны в самом модуле (задача 3) |
| T-10-15-03 | Information Disclosure | третья форма ответа, различимая по величине | medium | mitigate | Правило оси адреса сличает исход с ДВУМЯ объявленными формами и отказом валидации; третья различимая форма краснит. Свойство T-10-07 (неотличимость, не выдающая карты занятых идентификаторов) остаётся утверждаемым |
| T-10-15-04 | Tampering | мутация границы внутри задачи 1 | high | mitigate | Мутация снимается до завершения задачи, и её отсутствие УТВЕРЖДАЕТСЯ командой проверки (`git diff --name-only -- app/` пусто), а не памятью исполнителя. Мутация, оставшаяся в дереве, была бы худшим исходом плана, чем его отсутствие |
| T-10-15-05 | Repudiation | отпечаток поведения вместо утверждения | medium | mitigate | У каждой записи отображения ожидаемых исходов обязательно основание на запись; величина без объяснения объявлена остановом. Иначе отображение зеленело бы на любом поведении, которое случилось первым |
| T-10-15-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и не правит `pyproject.toml`. Таблица легитимности пакетов не требуется |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_confirm_delete_transport.py -q -p no:randomly` — зелено,
   число прошедших больше базового `57 passed`, прибавка объяснена числом заведённых случаев.
2. `uv run pytest tests/test_pages/test_editor_schedules.py -q -p no:randomly` — зелено: вторая ось
   не двинула ни одного правила соседнего модуля.
3. `git diff --name-only -- app/` — пусто: мутация снята, обработчики этим планом не правлены.
4. `uv run pytest tests/ -q` — прогон целиком зелен; число прошедших названо.
5. `uv run python -m compileall -q app main.py tests` — без ошибок.
6. В сводке приведены ДОСЛОВНО: вывод правила оси адреса на мутации (красный), вывод после возврата
   границы (зелёный), вывод покрасневшего правила числа осей, оба дописанных абзаца докстринга.
</verification>

<success_criteria>
- Матрица негодных величин прогоняется по ОБЕИМ осям, и обе объявлены числом.
- Способность правила покраснеть на дереве, где дефект жив, ПОКАЗАНА мутацией, а не заявлена.
- У каждой записи ожидаемого исхода есть основание на запись.
- Антивакуум двойной и показан обеими половинами.
- Прежняя формулировка помечена опровергнутой, а не вычеркнута; две константы разведены по
  предметам с записанным разделением.
- Слепая зона и граница машинно утверждаемого записаны в самом модуле, а вторая половина цели
  передана обходу поимённо.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-15-SUMMARY.md` по завершении.
</output>
