---
phase: 10-rychag-components-modal-html
plan: 49
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, ПО ТОЙ ЖЕ ПРИЧИНЕ, ЧТО У ПЛАНОВ 10-41…10-45: форма
# `type: tdd` описывается блоком `<feature>` без полей `read_first` и `acceptance_criteria`.
# Дисциплина RED→GREEN держится задачами.
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-10-5]
files_modified:
  - app/templates/includes/htmx_error_banner.html
  - tests/test_pages/test_shell.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ: `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, И ЗАМЕР ПЕРЕСНЯТ: `estimate-calibration` → sample_count 0, factor 1,
  # applied false, min_samples 3, confidence low. Переснято 2026-09-11 на `d17c594`; значения ТЕ ЖЕ.
  # `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена себе.
  # ⚠️ КАЛИБРОВКА ЗДЕСЬ НЕДОСТИЖИМА, И ЭТО НАЗВАНО, А НЕ УМОЛЧАНО: образцов 0 при пороге 3, и первые
  # три появятся лишь после исполнения планов настоящей партии. Проекция остаётся СОВЕТОМ, а не
  # гейтом. Размер держится тем, что измеримо СЕЙЧАС: задач 3, файлов на задачу не больше двух.
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ПЛАШКА ОТКАЗА НЕ ПЕРЕЖИВАЕТ УСПЕШНОГО ОБМЕНА: после успешного ответа обе заготовки снова несут признак скрытости, и это показано ИСПОЛНЕНИЕМ сценария в интерпретаторе JS, а не чтением исходника"
    - "РАЗЛИЧЕНИЕ ИДЁТ ПО ПРИЗНАКУ УСПЕХА, А НЕ ПО ФАКТУ ЗАВЕРШЕНИЯ: завершение обмена БЕЗ признака успеха заготовку НЕ ГАСИТ, и это показано отдельным доктóриванием — иначе плашка гасла бы собственным отказом"
    - "ЧИСЛО ЗАРЕГИСТРИРОВАННЫХ ОБРАБОТЧИКОВ ПЕРЕСНЯТО И СВЯЗАНО: правило однократности регистрации знает новое число и по-прежнему краснит удвоение на повторном исполнении"
    - "ДОПУЩЕНИЕ ФАЗЫ 8 «ТРЕТЬЕГО ОБРАБОТЧИКА НЕ ЗАВОДИТСЯ» НЕ ВЫЧЕРКНУТО, А НАЗВАНО ПЕРЕРОСШИМ: рядом с ним стои́т запись, называющая, чем именно оно переросло и какой ценой (идиома D-30/D-32)"
    - statement: "CR-01 СЕДЬМОГО КРУГА НЕ ВОЗВРАЩАЕТСЯ: гашение заготовок при открытии панели остаётся в рычаге нетронутым, и правило `test_the_lever_clears_both_failure_banners_when_the_panel_opens` зелено"
      verification: test
  artifacts:
    - "`app/templates/includes/htmx_error_banner.html` — третий обработчик, гасящий обе заготовки по признаку успешного завершения обмена, и запись D-30/D-32 рядом с прежним допущением"
    - "`tests/test_pages/test_shell.py` — поведенческое правило на гарнире интерпретатора JS (показ отказом → гашение успехом), переснятое число обработчиков и два отрицательных контроля"
  key_links:
    - "признак успеха в детали события → предмет правила: единственное, чем «обмен удался» отличимо от «обмен завершился»; ломается — плашка гаснет собственным отказом, и человек не видит объяснения ВООБЩЕ"
    - "гарнир, дающий узлам настоящий признак скрытости → сила правила: единственное, чем «гашение произошло» отличимо от «обработчик зарегистрирован»; ломается — правило зеленеет на обработчике с пустым телом"
    - "нетронутый рычаг → незакрытие CR-01 повторно: единственное, чем «новое окно закрыто» отличимо от «старое окно открыто заново»; ломается — отказ, к панели отношения не имеющий, снова встаёт над кнопкой необратимого удаления"
  prohibitions:
    - statement: "`app/templates/components/modal.html` НЕ ПРАВИТСЯ НИ НА СИМВОЛ: гашение при открытии панели есть закрытие CR-01, и перенос его сюда открыл бы CR-01 заново"
      status: flagged-unverified
      verification: test
    - statement: "СЦЕНАРИЙ НЕ ПОЛУЧАЕТ НИ ОДНОГО СТОКА РАЗМЕТКИ И НЕ НАЧИНАЕТ ЧИТАТЬ ИЗ ОТВЕТА НИЧЕГО, КРОМЕ ПРИЗНАКОВ СОСТОЯНИЯ: тело чужого ответа, попавшее в плашку, вынесло бы наружу внутреннее устройство (T-08-20)"
      status: flagged-unverified
      verification: test
    - statement: "ТЕКСТЫ ОБЕИХ ЗАГОТОВОК НЕ ПРАВЯТСЯ: вторая фраза плашки обрыва связи есть D-16 Фазы 9, и её смысл стерегут выписанные основы"
      status: flagged-unverified
      verification: test
    - statement: "ПРИЗНАК ОДНОКРАТНОСТИ РЕГИСТРАЦИИ ОСТАЁТСЯ НА ТОМ ЖЕ УЗЛЕ, К КОТОРОМУ ВЕШАЮТСЯ СЛУШАТЕЛИ (T-09-19-02): признак на документе пережил бы подмену узла и оставил бы новый узел без обработчиков вовсе"
      status: flagged-unverified
      verification: test
    - statement: "`app/static/css/app.css` НЕ ТРОГАЕТСЯ: подъём заготовок есть предмет плана 10-51, и одновременная правка двух файлов двумя руками сделала бы отказ неатрибутируемым"
      status: flagged-unverified
      verification: test
---

<objective>
**Плашка отказа переживает УСПЕШНЫЙ повтор того же действия и утверждает о нём неправду.**
Наблюдено дважды на проде, на РАЗНЫХ стендах (перезамер 2026-09-11, стенд ad_id=55 / sched-152;
обход `walkthrough_3`, стенд ad_id=59 / sched-162).

Замер второго наблюдения дословно: сеть возвращена, «Удалить» нажата ПОВТОРНО без переоткрытия
панели, удаление СОСТОЯЛОСЬ — оба узла `sched-162` и `sched-del-162` → `null`, диалоги 4→3, несущий
счёт 3→2, `is-modal-open` снят, `overflow: visible`, фокус вернулся на `DIV#notice`, на сервере
остались `[163, 164]`. **Плашка `htmx-failure-network` при этом ОСТАЛАСЬ НА ЭКРАНЕ:** признак
скрытости снят, `display: block`, прямоугольник `{top: 96, left: 294, 944×63}` целиком в окне,
`elementFromPoint` возвращает узел внутри неё. Глазной вердикт по снимку: красная полоса «Запрос не
дошёл до сервера… обновите страницу, чтобы увидеть настоящее состояние» стои́т над ВЕРНЫМ экраном
сразу после успешного НЕОБРАТИМОГО удаления.

**Корень сверен с деревом и подтверждён обходом.** В `app/templates/includes/htmx_error_banner.html`
заведены РОВНО ДВА обработчика — отказа ответа (строка 164) и отказа отправки (строка 169), — и оба
только СНИМАЮТ признак скрытости. Единственное место в дереве, где заготовки гасятся, — метод
открытия рычага `app/templates/components/modal.html` (план 10-35), то есть **гашение привязано к
ОТКРЫТИЮ ПАНЕЛИ, а не к УСПЕХУ ОБМЕНА.** Успешный повтор внутри уже открытой панели панель не
переоткрывает — значит не гасит ничего.

⚠️ **ЭТО ТРЕТЬЕ ОКНО, А НЕ ПОВТОР CR-01, И РАЗНИЦА НЕСУЩАЯ.** CR-01 седьмого круга был об отказе,
вынесенном над СЛЕДУЮЩЕЙ панелью; план 10-35 закрыл его гашением при открытии панели. Оба поля
`limit` сводки 10-35 называют ДРУГИЕ два окна — отказ фонового запроса при открытой панели и отказ,
стёртый открытием панели. Настоящее — ТРЕТЬЕ, и владельца у него до сих пор не было.

Purpose: человек не должен читать утверждение об отказе над экраном, который уже верен, — а тем
более сразу после необратимого действия, которое СОСТОЯЛОСЬ. Ложное утверждение перед необратимым
удалением проект уже однажды оплатил (CR-01); здесь оно стои́т ПОСЛЕ удаления и утверждает, что
удаления могло не быть.

Output: третий обработчик, гасящий обе заготовки по признаку УСПЕХА обмена, и поведенческое правило,
исполняющее сценарий в интерпретаторе JS и проверяющее ГАШЕНИЕ, а не регистрацию.

⚠️ **ДОПУЩЕНИЕ ФАЗЫ 8 «ТРЕТЬЕГО ОБРАБОТЧИКА НЕ ЗАВОДИТСЯ» ЭТИМ ПЛАНОМ ПЕРЕОТКРЫВАЕТСЯ, И ЭТО
НАЗЫВАЕТСЯ ПРЯМО, А НЕ ПРОВОДИТСЯ МОЛЧА.** Само допущение записало условие своего пересмотра
дословно: «решение записано допущением плана и возвращается к обсуждению, если накопление плашек
станет заметно». Накопление стало заметно ЗАМЕРОМ на проде, дважды. Прежний текст не вычёркивается
ни в одном месте — рядом с ним встаёт то, что его переросло.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-UAT.md
@.planning/phases/10-rychag-components-modal-html/10-35-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**`app/templates/includes/htmx_error_banner.html`:**

- третий обработчик — гашение обеих заготовок по признаку УСПЕШНОГО завершения обмена, внутри той же
  обёртки признака однократности, что и два действующих;
- запись D-30/D-32 рядом с абзацем «ЧЕГО СЦЕНАРИЙ НЕ ДЕЛАЕТ» и рядом с абзацем о сужении допущения.

**Новые символы в `tests/test_pages/test_shell.py`:**

- `FAILURE_BANNER_SUCCESS_EVENT` — имя события завершения обмена;
- `FAILURE_BANNER_SUCCESS_FLAG` — имя признака успеха в детали события;
- `FAILURE_BANNER_DISPATCH_HARNESS` — гарнир, дающий узлам НАСТОЯЩИЙ признак скрытости и умеющий
  рассылать события зарегистрированным слушателям;
- `_failure_banner_hidden_after(path, script)` — состояние признака скрытости обеих заготовок после
  поданной последовательности событий;
- `test_the_failure_banner_is_cleared_by_a_successful_exchange` — положительное поведенческое правило;
- `test_a_failed_exchange_does_not_clear_the_failure_banner` — граница между завершением и успехом;
- `test_control_a_banner_script_without_the_success_handler_reddens` — отрицательный контроль;
- `test_control_a_banner_script_that_clears_on_any_completion_reddens` — второй отрицательный контроль.

**Изменённые символы:** `FAILURE_BANNER_EVENTS`, `FAILURE_BANNER_HANDLERS_MEASURED` (2 → 3 по
ПЕРЕСНЯТОМУ замеру), `FAILURE_BANNER_REGISTRATION_HARNESS` (стаб узла получает недостающий метод),
`test_the_failure_banner_carries_both_handlers` (имя выводится исполнителем: число обработчиков
переросло слово «оба»).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: поведенческое правило гашения по успеху — RED на сегодняшнем сценарии</name>

  <files>
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `tests/test_pages/test_shell.py:2044-2080` — `FAILURE_BANNER_OWNER`, `FAILURE_BANNER_SHELLS`,
  `FAILURE_BANNER_IDS`, `FAILURE_BANNER_EVENTS`, `VALIDATION_STATUS`, `RESPONSE_BODY_READ`. ⚠️ ЧИТАТЬ
  ДОСЛОВНО АБЗАЦЫ ОСНОВАНИЙ: почему ожидание выписывается здесь, а не выбирается из проверяемого
  файла. Это основание действует и для новых констант настоящей задачи.
- `tests/test_pages/test_shell.py:2280-2320` — `FAILURE_BANNER_HANDLERS_MEASURED` вместе с абзацем
  «⚠️ ЧИСЛО ИЗМЕРЕНО ПО ДЕРЕВУ, А НЕ ВЗЯТО ИЗ ПАМЯТИ» и `FAILURE_BANNER_REGISTRATION_HARNESS`
  целиком. ⚠️ ЗАМЕТИТЬ: стаб узла по идентификатору умеет РОВНО снятие признака; обработчик,
  ставящий признак обратно, на этом стабе упал бы, если бы гарнир его вызывал.
- `tests/test_pages/test_shell.py:2320-2345` — `_failure_banner_script`, `_BANNER_SCRIPT_RE`,
  `_failure_banner_registration_count`.
- `tests/test_pages/test_shell.py:2389-2420` — `test_the_failure_banner_carries_both_handlers` и
  `test_the_failure_banner_registers_its_handlers_once_per_body`.
- `tests/conftest.py`, `run_node_script` — целиком, включая абзац о запрете пропуска правила при
  отсутствии интерпретатора. Пропуск здесь ЗАПРЕЩЁН: пропущенное правило неотличимо от зелёного.
- `app/static/js/htmx.min.js` — ⚠️ ЗАМЕР, А НЕ ПАМЯТЬ: найти место, где на объекте сведений об
  ответе выставляются признаки провала и успеха, и убедиться собственными глазами, что (а) признак
  успеха есть отрицание признака провала, (б) на пути обрыва транспорта объект этих признаков НЕ
  ПОЛУЧАЕТ ВОВСЕ, потому что разбор ответа там не исполняется. Обе половины переснять и привести в
  сводке дословной выдержкой — от них зависит выбор предиката.
- `app/templates/includes/htmx_error_banner.html:157-174` — заготовки и сценарий целиком.
  </read_first>

  <behavior>
1. Гарнир исполняет сценарий, рассылает событие отказа отправки — обе заготовки ВИДИМЫ (признак
   скрытости снят хотя бы с одной, и правило называет, с какой именно).
2. Следом гарнир рассылает событие завершения обмена С ПРИЗНАКОМ УСПЕХА — СЕГОДНЯ обе заготовки
   остаются видимыми. Это и есть измеренный RED.
3. Гарнир, которому не подавали ни одного события, оставляет обе заготовки скрытыми — антивакуум:
   без него «гашение произошло» было бы неотличимо от «показа не было».
  </behavior>

  <action>
**(1) RED ПЕРВЫМ И ЗАМЕРОМ.** Написать `test_the_failure_banner_is_cleared_by_a_successful_exchange`
и прогнать на НЕТРОНУТОМ дереве. В сводку попадают команда, код возврата, имя правила, ожидаемое и
полученное состояние признака скрытости ОБЕИХ заготовок.

**(2) ГАРНИР — НОВЫЙ, А НЕ ПРАВКА ДЕЙСТВУЮЩЕГО.** `FAILURE_BANNER_REGISTRATION_HARNESS` считает
регистрации и стабит всё остальное; его предмет — ЧИСЛО, и правка его под рассылку событий отняла бы
у правила однократности его собственный предмет. Заводится `FAILURE_BANNER_DISPATCH_HARNESS` рядом, и
основание соседства записывается там же. Гарнир обязан:
  - держать узлы заготовок с НАСТОЯЩИМ состоянием признака скрытости — снятие и постановка меняют
    состояние, а не проглатываются стабом (иначе правило зеленеет на обработчике с пустым телом);
  - хранить слушателей по имени события и уметь рассылать событие с поданной деталью;
  - печатать вердикт последней строкой — состояние признака скрытости каждой заготовки по имени.
⚠️ ИМЕНА ЗАГОТОВОК ГАРНИР БЕРЁТ ИЗ `FAILURE_BANNER_IDS`, а не выписывает второй копией.

**(3) ДВА НОВЫХ ОЖИДАНИЯ ВЫПИСЫВАЮТСЯ ЛИТЕРАЛАМИ.** `FAILURE_BANNER_SUCCESS_EVENT` — имя события
завершения обмена; `FAILURE_BANNER_SUCCESS_FLAG` — имя признака успеха в детали. Основание то же, что
у `VALIDATION_STATUS`: ожидание, добытое из предмета проверки, согласилось бы с любой его правкой.
⚠️ ОБА ИМЕНИ ПЕРЕСНЯТЫ С ВЕНДОРЕННОГО РАНТАЙМА на шаге чтения, а не взяты из памяти, и выдержка
приведена в сводке.

**(4) АНТИВАКУУМ ПЕРВЫМ, КАК У СОСЕДНИХ ПРАВИЛ.** До положительного утверждения правило проверяет,
что БЕЗ единого события обе заготовки скрыты, а ПОСЛЕ события отказа хотя бы одна видима. Без этих
двух половин «успех погасил» неотличимо от «показа не было вовсе».

**(5) ГРАНИЦА ПРАВИЛА НАЗЫВАЕТСЯ В ДОКСТРИНГЕ.** Правило исполняет сценарий в интерпретаторе JS на
СТАБАХ, а не в браузере: оно доказывает, что ТЕЛО обработчика гасит заготовки по признаку успеха, и
НЕ доказывает, что событие с этим признаком приходит в браузере именно тогда, когда обмен удался.
Второе снимается ручным обходом. Записать прямо, как это сделано у правила однократности регистрации.
  </action>

  <acceptance_criteria>
- `test_the_failure_banner_is_cleared_by_a_successful_exchange` на нетронутом дереве КРАСЕН; команда,
  код возврата и состояние обеих заготовок приведены в сводке.
- Гарнир держит НАСТОЯЩЕЕ состояние признака скрытости: доктóренный сценарий, снимающий и ставящий
  признак, меняет вердикт гарнира — показано отдельным прогоном.
- Антивакуумные половины (ни одного события → обе скрыты; после отказа → хотя бы одна видима) стоя́т
  ДО положительного утверждения.
- Имена события и признака успеха ПЕРЕСНЯТЫ с `app/static/js/htmx.min.js`, и выдержка приведена в
  сводке дословно.
- Действующий `FAILURE_BANNER_REGISTRATION_HARNESS` не правится — заведён отдельный гарнир, и
  основание соседства записано рядом.
- `git diff --numstat -- app` пуст.
  </acceptance_criteria>

  <verify>
    <automated>OUT=$(uv run pytest tests/test_pages/test_shell.py -q -k "cleared_by_a_successful_exchange" 2>&1); printf '%s\n' "$OUT" | tail -6; printf '%s\n' "$OUT" | grep -qE '^FAILED .*::test_the_failure_banner_is_cleared_by_a_successful_exchange$' && printf '%s\n' "$OUT" | tail -1 | grep -qE '^1 failed'</automated>
    <fails_when>ненулевой код возврата, а он ненулевой при ЛЮБОМ из двух — и оба суть ОДНО требование: RED засчитывается лишь при правиле ОТОБРАННОМ, ИСПОЛНЕННОМ и упавшем. (а) В выводе нет строки `FAILED …::test_the_failure_banner_is_cleared_by_a_successful_exchange` — правило не упало вовсе (зеленеет там, где обход дважды наблюдал дефект) либо упало ДРУГОЕ правило. (б) Итоговая строка не начинается с `1 failed` — правило не было отобрано (`N deselected`) либо прогон оборвался сборкой (`1 error`). ⚠️ ПРЕЖНЯЯ ИНВЕРСИЯ `test $? -ne 0` ЗЕЛЕНЕЛА НА ОБОИХ, И ЭТО ЗАМЕРЕНО НА ЭТОМ САМОМ МОДУЛЕ: пустой отбор даёт `223 deselected, 1 warning in 0.10s` при коде 0, отказ сборки — `1 error in 0.18s` при ненулевом коде, то есть неотличим от честного RED (замер 2026-09-11, `d17c594`). ⚠️ ОБЪЯВЛЕННАЯ ПРИЧИНА ОТКАЗА держится акцептансом задачи, а не грепом по тексту: вердикт гарнира печатается последней строкой и попадает в сводку, где ОБЕ заготовки названы по имени вместе с состоянием признака скрытости</fails_when>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "registers_its_handlers_once or carries"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — заведение нового гарнира задело предмет действующих правил</fails_when>
    <automated>git diff --numstat -- app</automated>
    <fails_when>вывод НЕПУСТ — продукт тронут, а задача правит только модуль суиты</fails_when>
  </verify>

  <done>
Поведенческое правило заведено, на нетронутом дереве красно ровно тем, что наблюдал обход; гарнир
держит настоящее состояние признака скрытости; антивакуум стои́т первым; имена события и признака
переснаты с вендоренного рантайма; граница правила названа.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: третий обработчик гасит обе заготовки по признаку успеха — GREEN, и допущение Фазы 8 названо переросшим</name>

  <files>
app/templates/includes/htmx_error_banner.html
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `app/templates/includes/htmx_error_banner.html` ЦЕЛИКОМ, и особенно четыре абзаца:
  «ПОЧЕМУ ОБРАБОТЧИКОВ ДВА, А НЕ ОДИН», «ЧЕГО СЦЕНАРИЙ НЕ ДЕЛАЕТ» (последняя фраза — само
  допущение), «⚠️ ЭТО ДОПУЩЕНИЕ ФАЗЫ 8 СУЖЕНО, А НЕ ОТМЕНЕНО И НЕ ВЫЧЕРКНУТО» и «⚠️ РЕГИСТРАЦИЯ
  ОБРАБОТЧИКОВ ОДНОКРАТНА». ⚠️ ЧИТАТЬ ДОСЛОВНО: допущение САМО назвало условие своего пересмотра, и
  настоящая правка исполняет это условие, а не обходит его.
- `app/templates/components/modal.html:695-710` — метод открытия рычага и гашение обеих заготовок в
  нём. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО И НЕ ПРАВИТЬ НИ НА СИМВОЛ: это закрытие CR-01, и перенос гашения отсюда
  открыл бы CR-01 заново.
- `tests/test_pages/test_shell.py:2484-2560` — контроль накопления слушателей и
  `test_the_failure_banner_touches_no_markup_sink`: сток разметки в сценарии запрещён, и третий
  обработчик обязан остаться без него.
- `tests/test_pages/test_shell.py:2561-2600` — `test_the_failure_banner_tells_a_validation_answer_
  from_a_crash`: раннее сравнение с кодом ответа валидации стои́т ПЕРВЫМ и не должно быть сдвинуто.
  </read_first>

  <behavior>
1. Сценарий вешает ТРИ слушателя на тот же узел и под тем же признаком однократности; повторное
   исполнение по-прежнему не удваивает их числа.
2. Событие завершения обмена С ПРИЗНАКОМ УСПЕХА ставит признак скрытости ОБЕИМ заготовкам.
3. Событие завершения обмена БЕЗ признака успеха НЕ ГАСИТ ни одной.
4. Сценарий по-прежнему не собирает разметки и не читает из ответа ничего, кроме признаков
   состояния; раннее сравнение с кодом ответа валидации стои́т первым в своём обработчике.
  </behavior>

  <action>
**(1) GREEN — ТРЕТИЙ ОБРАБОТЧИК ВНУТРИ ДЕЙСТВУЮЩЕЙ ОБЁРТКИ ПРИЗНАКА ОДНОКРАТНОСТИ.** Обработчик
вешается на ТОТ ЖЕ узел, что и два действующих, и ВНУТРИ той же обёртки: признак, поставленный на
другой узел, пережил бы подмену узла и оставил бы новый узел без обработчиков вовсе (T-09-19-02).
Тело обработчика: ранний выход, если признака успеха в детали события нет либо он не истинен; иначе
— постановка признака скрытости обеим заготовкам по их идентификаторам.
⚠️ **РАННИЙ ВЫХОД ПО ОТСУТСТВИЮ ПРИЗНАКА УСПЕХА — НЕ ПЕДАНТИЗМ, А ПРЕДМЕТ.** Событие завершения
обмена поднимается И НА ПУТИ ОБРЫВА ТРАНСПОРТА, ДО события отказа отправки (замер вендоренного
рантайма, задача 1). Обработчик без раннего выхода погасил бы заготовку СОБСТВЕННЫМ отказом за
мгновение до её показа, и человек не увидел бы объяснения ВООБЩЕ — отказ громче исходного.

**(2) ОТСТУП ДЕЙСТВУЮЩИХ ТЕЛ НЕ ПРАВИТСЯ.** Основание записано в самом файле абзацем «⚠️ ТЕЛА ОБОИХ
ОБРАБОТЧИКОВ ОСТАЛИСЬ ПОСИМВОЛЬНО ПРЕЖНИМИ»: переиндентация переписала бы каждую строку, и правка
перестала бы читаться как то, чем она является — добавлен третий обработчик, больше ничего.

**(3) ЗАПИСЬ D-30/D-32 ВСТАЁТ РЯДОМ С ДОПУЩЕНИЕМ, А НЕ ВМЕСТО НЕГО.** Прежний текст — «Снятую плашку
сценарий обратно не прячет: автоматическое сокрытие потребовало бы третьего обработчика, а рамка вехи
ограничивает число новых сценариев; решение записано допущением плана и возвращается к обсуждению,
если накопление плашек станет заметно» — остаётся ДОСЛОВНО. Рядом встаёт запись, называющая ЧЕТЫРЕ
вещи:
  - условие пересмотра, записанное самим допущением, НАСТУПИЛО, и наступление ЗАМЕРЕНО: дважды на
    проде, на разных стендах, с приведёнными прямоугольниками и глазным вердиктом;
  - что именно переросло: «плашку не прячет никто» было сужено планом 10-35 до «её прячет рычаг при
    открытии панели», и настоящая правка сужает дальше — её прячет ТАКЖЕ успешный обмен;
  - ЦЕНА, названная фактом, а не эпитетом: УСПЕШНЫЙ ФОНОВЫЙ запрос (сентинель прокрутки,
    автосохранение) гасит НАСТОЯЩИЙ отказ, который человек не успел прочитать. Окно то же по классу,
    что цена (i) плана 10-35, и оно НЕ НОЛЬ. Обмен принят сознательно: ложное утверждение об отказе
    над верным экраном сразу после необратимого удаления стои́т дороже потерянного уведомления о
    прошлом — тот же довод и в ту же сторону, что у 10-35;
  - ЧЕМ СТЕРЕЖЁТСЯ: именем поведенческого правила задачи 1 и именем правила границы (задача 3).
⚠️ **ИМЕНА СОБЫТИЙ В ПРОЗЕ НАБИРАЮТСЯ СЛОВАМИ, А НЕ ЛИТЕРАЛАМИ**, и путь к рычагу — тоже словами:
основание записано в самом файле дважды (гейт наследования панели считает импортёров грепом по
исходнику вместе с прозой; объяснение, набранное проверяемой строкой, роняет собственный гейт).

**(4) ЧИСЛО ОБРАБОТЧИКОВ ПЕРЕСНИМАЕТСЯ, А НЕ ПРИБАВЛЯЕТСЯ НА ГЛАЗОК.** `FAILURE_BANNER_HANDLERS_
MEASURED` получает новое значение ЗАМЕРОМ по дереву (счётом вызовов регистрации в сценарии), и абзац
основания рядом с константой дополняется припиской: прежнее значение 2 ОШИБКОЙ НЕ БЫЛО — оно
принадлежало дереву до настоящей правки. `FAILURE_BANNER_EVENTS` пополняется именем события
завершения обмена; правило, называвшее обработчиков «обоими», переименовывается — число переросло
слово, и имя правила обязано это сказать.

**(5) ГАРНИР РЕГИСТРАЦИЙ ПОЛУЧАЕТ НЕДОСТАЮЩИЙ МЕТОД СТАБА, ЕСЛИ ПРОГОН ЭТОГО ПОТРЕБУЕТ.** Стаб узла
по идентификатору умеет РОВНО снятие признака. Правка — минимальная и названная: стаб дополняется
методом постановки, чтобы отказ гарнира не выдавался за отказ предмета.
  </action>

  <acceptance_criteria>
- `test_the_failure_banner_is_cleared_by_a_successful_exchange` ЗЕЛЕНО; в сводке названы ОБА
  состояния — красное до правки и зелёное после, с командами и кодами возврата.
- `FAILURE_BANNER_HANDLERS_MEASURED` равно ПЕРЕСНЯТОМУ числу, и правило однократности регистрации
  зелено на обоих прогонах (одно исполнение и два).
- Прежний текст допущения присутствует ДОСЛОВНО; рядом стои́т запись, называющая все четыре вещи
  перечня действия (3), включая цену без смягчения.
- Имена событий и путь к рычагу набраны в прозе СЛОВАМИ: гейт единственности источника и гейт
  наследования панели зелены.
- `test_the_failure_banner_touches_no_markup_sink` и
  `test_the_failure_banner_tells_a_validation_answer_from_a_crash` зелены: сток разметки не заведён,
  раннее сравнение не сдвинуто.
- `git diff --numstat -- app/templates/components/modal.html app/static/css/app.css` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed"</fails_when>
    <automated>N=$(uv run pytest tests/test_pages/test_shell.py --collect-only -q 2>&1 | tail -1 | grep -oE '^[0-9]+'); echo "собрано: $N (порог 223)"; test "$N" -ge 223</automated>
    <fails_when>собрано МЕНЬШЕ 223 правил — то есть правила пропали из модуля, и зелёный прогон соседней команды зеленел бы ВАКУУМОМ. ⚠️ ПОРОГ ЕСТЬ ЗАМЕР, И КОМАНДА ЗАМЕРА СТОИ́Т РЯДОМ, ЧТОБЫ СЛЕДУЮЩИЙ КРУГ ПЕРЕСНЯЛ, А НЕ УНАСЛЕДОВАЛ: `uv run pytest tests/test_pages/test_shell.py --collect-only -q` → `223 tests collected`, снято 2026-09-11 на `d17c594` ДО партии. ⚠️ ПРЕЖНЕЕ ЧИСЛО 161 БЫЛО НЕВЕРНО НА 62 ПРАВИЛА и заменено переснятым; ошибкой оно было не в момент записи, а к моменту проверки — дерево ушло вперёд, и порог, взятый из памяти, пропустил бы пропажу шести десятков правил молча. После правки настоящего плана число обязано быть 223 ПЛЮС новые правила плана; порог держит НИЖНЮЮ границу и лишнего не запрещает</fails_when>
    <automated>git diff --numstat -- app/templates/components/modal.html app/static/css/app.css</automated>
    <fails_when>вывод НЕПУСТ — тронут рычаг (закрытие CR-01) либо таблица стилей (предмет плана 10-51), и отказ соседнего плана стал бы неатрибутируемым</fails_when>
  </verify>

  <done>
Третий обработчик гасит обе заготовки по признаку успеха и не гасит их завершением без успеха; число
обработчиков переснято и связано правилами; допущение Фазы 8 стои́т дословно, рядом с ним — запись о
том, чем оно переросло и какой ценой; рычаг и таблица стилей не тронуты.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: зубы — два отрицательных контроля и проверка, что CR-01 не вернулся</name>

  <files>
tests/test_pages/test_shell.py
  </files>

  <read_first>
- `tests/test_pages/test_shell.py:3858-3980` — `test_the_lever_clears_both_failure_banners_when_the_
  panel_opens` и его первый контроль. ⚠️ ЧИТАТЬ ЦЕЛИКОМ: это действующее правило закрытия CR-01, и
  настоящая задача обязана предъявить его ЗЕЛЁНЫМ после правки соседней задачи, а не предположить.
- `tests/test_pages/test_shell.py:2484-2528` — `test_control_an_unguarded_banner_accumulates_
  listeners`: образец контроля, готовящего доктóренную копию во временном каталоге.
- `.planning/phases/10-rychag-components-modal-html/10-35-SUMMARY.md` — оба поля `limit`: два окна,
  которых правка 10-35 не достаёт. Настоящий план закрывает ТРЕТЬЕ и НЕ закрывает те два; это
  записывается в сводку, а не выдаётся за общее решение.
  </read_first>

  <behavior>
1. Копия сценария БЕЗ третьего обработчика краснит поведенческое правило — то есть правило видит
   изъятие своего предмета.
2. Копия сценария, гасящая заготовки на ЛЮБОМ завершении обмена (ранний выход по признаку успеха
   изъят), краснит правило границы: после отказа отправки заготовка обязана остаться ВИДИМОЙ, а на
   такой копии она гаснет собственным отказом.
3. Правило закрытия CR-01 зелено, и рычаг не тронут ни на символ.
  </behavior>

  <action>
**(1) ДВА КОНТРОЛЯ, И ВТОРОЙ НЕ ИЗБЫТОЧЕН.** Первый (`…without_the_success_handler_reddens`)
доказывает, что правило видит ОТСУТСТВИЕ предмета. Второй
(`…that_clears_on_any_completion_reddens`) доказывает, что правило видит ПОДМЕНУ предмета: разница
между «гасить по успеху» и «гасить по завершению» есть разница между работающим каналом видимости
отказа и каналом, который гасит себя сам. Без второго контроля правило зеленело бы на сценарии,
делающем человека слепым ровно там, где отказ и случился.

**(2) КОПИИ ГОТОВЯТСЯ, А НАСТОЯЩИЙ ФАЙЛ НЕ ПРАВИТСЯ.** Форма — та же, что у действующего контроля
накопления слушателей: доктóренный текст кладётся во временный каталог, гарнир исполняется по
поданному пути. Ожидания выписываются ЛИТЕРАЛАМИ, а не вычисляются из проверяемого источника.

**(3) `test_a_failed_exchange_does_not_clear_the_failure_banner` — ОТДЕЛЬНОЕ ИМЕНОВАННОЕ ПРАВИЛО,
А НЕ ПОЛОВИНА СОСЕДА.** Его предмет — ГРАНИЦА, и он обязан краснеть сам, называя починку, а не
исчезать внутри чужого отказа.

**(4) CR-01 ПРЕДЪЯВЛЯЕТСЯ НЕВЕРНУВШИМСЯ ПРОГОНОМ, А НЕ РАССУЖДЕНИЕМ.** Прогнать
`test_the_lever_clears_both_failure_banners_when_the_panel_opens` вместе с его контролями и привести
в сводке имена и коды возврата. Рядом привести пустой вывод `git diff` по рычагу.

**(5) ПОЛНЫЙ ПРОГОН СУИТЫ И ЗАПИСЬ ОСТАТКА.** Прогнать `uv run pytest tests/` целиком. В сводку —
итоговая строка числом и ЧЕТЫРЕ окна поимённо: два, названные сводкой 10-35 (фоновый отказ при
открытой панели; отказ, стёртый открытием панели), ТРЕТЬЕ — закрытое настоящим планом, и НОВОЕ,
внесённое им же (успешный фоновый обмен гасит непрочитанный настоящий отказ). Четвёртое окно
наблюдаемо только рантаймом браузера и правилом не стережётся — назвать это прямо, как это сделано в
обоих местах, где живёт допущение Фазы 8.
  </action>

  <acceptance_criteria>
- Оба контроля краснят то, что объявлено; ожидаемые состояния названы в сводке для каждого отдельно.
- `test_a_failed_exchange_does_not_clear_the_failure_banner` существует ИМЕНОВАННЫМ правилом.
- Настоящий `app/templates/includes/htmx_error_banner.html` контролями не правится: копии готовятся
  во временном каталоге.
- `test_the_lever_clears_both_failure_banners_when_the_panel_opens` зелено; `git diff --numstat --
  app/templates/components/modal.html` пуст.
- `uv run pytest tests/` зелен; итоговая строка приведена числом.
- В сводке названы ЧЕТЫРЕ окна поимённо, из них новое — прямо, без смягчения.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q -k "failure_banner or lever_clears or clears_on_any_completion"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed"</fails_when>
    <automated>uv run pytest tests/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed" — партия обязана оставить суиту зелёной целиком</fails_when>
    <automated>git status --porcelain app/templates/components/modal.html</automated>
    <fails_when>вывод НЕПУСТ — рычаг тронут, и закрытие CR-01 больше не то, что было проверено</fails_when>
  </verify>

  <done>
У правила два отрицательных контроля с непересекающимися предметами, граница «успех против
завершения» вынесена именованным правилом, закрытие CR-01 предъявлено зелёным прогоном, полная суита
зелена, остаток из четырёх окон назван поимённо.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ сервера → сценарий видимости отказа | деталь события обмена пересекает границу и читается сценарием |
| общий канал отказа → обе оболочки и все формы вехи | один источник обслуживает шелл приложения и шелл авторизации |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-49-01 | Information Disclosure | третий обработчик `htmx_error_banner.html` | medium | mitigate | из детали события читаются ТОЛЬКО признаки состояния; тело ответа не читается — держит `test_the_failure_banner_touches_no_markup_sink` и запрет `RESPONSE_BODY_READ` |
| T-10-49-02 | Spoofing | видимость отказа как канал доверия | high | mitigate | ранний выход по отсутствию признака успеха: без него обработчик гасил бы заготовку СОБСТВЕННЫМ отказом, и человек не увидел бы объяснения вовсе — держит `test_a_failed_exchange_does_not_clear_the_failure_banner` и второй контроль |
| T-10-49-03 | Denial of Service | накопление слушателей на узле тела | medium | mitigate | третий обработчик вешается ВНУТРИ действующей обёртки признака однократности; число переснято и связано `test_the_failure_banner_registers_its_handlers_once_per_body` |
| T-10-49-04 | Tampering | признак однократности на чужом узле | high | mitigate | признак остаётся на ТОМ ЖЕ узле, к которому вешаются слушатели — держит `test_the_failure_banner_guard_lives_on_the_node_it_wires` (T-09-19-02) |
| T-10-49-05 | Repudiation | непрочитанный настоящий отказ, погашенный успешным фоновым обменом | medium | accept | цена названа фактом в двух местах прозы и в сводке; обмен принят сознательно по тому же доводу, что цена (i) плана 10-35; наблюдаемо только рантаймом браузера, владельца не имеет |
| T-10-49-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_shell.py -q` — зелено, и число собранных правил не меньше
   ЗАМЕРА **223** плюс новые правила плана. ⚠️ Команда замера порога — `uv run pytest
   tests/test_pages/test_shell.py --collect-only -q`; снято 2026-09-11 на `d17c594` до партии.
   Прежнее число 161 было неверно на 62 правила и заменено переснятым.
2. `uv run pytest tests/ -q` — зелено целиком.
3. `git diff --numstat -- app/templates/components/modal.html app/static/css/app.css` пуст на всех
   трёх задачах.
4. Переход цвета поведенческого правила измерен и приведён обоими состояниями.
5. Четыре окна названы поимённо в сводке, включая внесённое настоящим планом.
</verification>

<success_criteria>
- Плашка отказа не переживает успешного обмена, и это показано ИСПОЛНЕНИЕМ сценария, а не чтением.
- Завершение обмена без признака успеха заготовку не гасит, и это отдельное именованное правило с
  собственным контролем.
- Допущение Фазы 8 стои́т дословно, рядом с ним — запись о том, чем оно переросло и какой ценой.
- Закрытие CR-01 предъявлено зелёным и рычаг не тронут.
- Полная суита зелена.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-49-SUMMARY.md` when done
</output>
