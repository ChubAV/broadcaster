---
phase: 10-rychag-components-modal-html
plan: 50
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, ПО ТОЙ ЖЕ ПРИЧИНЕ, ЧТО У ПЛАНОВ 10-41…10-45.
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-10-6]
files_modified:
  - app/pages/schedules.py
  - app/templates/ads/partials/sched_delete_response.html
  - tests/test_pages/test_editor_schedules.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ: `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, И ЗАМЕР ПЕРЕСНЯТ: `estimate-calibration` → sample_count 0, factor 1,
  # applied false, min_samples 3, confidence low. Переснято 2026-09-11 на `d17c594`; значения ТЕ ЖЕ.
  # `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена себе.
  # ⚠️ КАЛИБРОВКА ЗДЕСЬ НЕДОСТИЖИМА, И ЭТО НАЗВАНО, А НЕ УМОЛЧАНО: образцов 0 при пороге 3, и первые
  # три появятся лишь после исполнения планов настоящей партии. Проекция остаётся СОВЕТОМ, а не
  # гейтом. Размер держится тем, что измеримо СЕЙЧАС: задач 3, файлов на задачу не больше трёх.
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ДВА ЧИСЛА ОДНОГО ЭКРАНА ПОСЛЕ ФРАГМЕНТНОГО УДАЛЕНИЯ НЕ ПРОТИВОРЕЧАТ ДРУГ ДРУГУ: линейка списка и сводка предпросмотра приезжают ОДНИМ ответом и ОДНИМ значением, и это показано разбором тела ответа"
    - "ЧИСЛО В ДВУХ ОБЛАСТЯХ ОДНОГО ОТВЕТА НЕ МОЖЕТ РАЗОЙТИСЬ ПО ПОСТРОЕНИЮ: обе области получают ОДНУ переменную ответа, а не два независимых чтения базы"
    - "СОДЕРЖИМОЕ СВОДКИ ИМЕЕТ РОВНО ОДИН ИСТОЧНИК РАЗМЕТКИ ПРИ ДВУХ МЕСТАХ ВКЛЮЧЕНИЯ — тем же правилом и по тому же основанию, что линейка счётчика"
    - "ПУТЬ ДЕГРАДАЦИИ И ВЕТКА ПЕРЕХОДА НЕ ТРОНУТЫ: без заголовка htmx ответ остаётся прежним перенаправлением, а опустевший редактор по-прежнему закрывается переходом и фрагмента не собирает вовсе (IN-03)"
    - statement: "БЛИЖАЙШИЙ ЗАПУСК В СВОДКЕ ПЕРЕСТАЁТ БЫТЬ УСТАРЕВШИМ ВМЕСТЕ С ЧИСЛОМ: удаление последнего расписания с ближайшим моментом двигает обе строки сводки, а не одну"
      verification: test
  artifacts:
    - "`app/pages/schedules.py` — отложенная сборка фрагмента, читающая объявление владельца и ближайший момент запуска, и ОДНО значение счёта, уезжающее в обе области ответа"
    - "`app/templates/ads/partials/sched_delete_response.html` — ЧЕТВЁРТЫЙ внеполосный узел верхнего уровня со сводкой объявления и приведённая проза о числе узлов (идиома D-30/D-32)"
    - "`tests/test_pages/test_editor_schedules.py` — поведенческое правило согласия трёх счётов (линейка, сводка, сервер) и правило единственности источника разметки сводки"
  key_links:
    - "одна переменная счёта на две области → предмет гэпа: единственное, чем «числа согласны» отличимо от «числа сегодня совпали»; ломается — два чтения базы расходятся при первой же правке одного из них"
    - "узел сводки ПРЯМЫМ ДЕТЯМ файла → применимость внеполосной подмены: блок конфигурации несёт запрет вложенных внеполосных узлов, и у вложенного рантайм молча снимает признак — ни ошибки, ни отличия в статусе"
    - "отложенность сборки → цена пути деградации: единственное, чем «запрос не выполняется без htmx» отличимо от «выполняется и выбрасывается»; ломается — человек без JavaScript платит тремя запросами за разметку, которая ему не уедет"
  prohibitions:
    - statement: "ВЕТВИ ПО ФАКТУ УДАЛЕНИЯ В ШАБЛОНЕ ОТВЕТА НЕ ЗАВОДИТСЯ НИ В КАКОМ ВИДЕ: условная сборка сделала бы НАЛИЧИЕ узла признаком того, что удаление состоялось, и карту чужих идентификаторов можно было бы составить перебором по адресу (T-10-01)"
      status: flagged-unverified
      verification: test
    - statement: "ОБЩЕЙ ОБЁРТКИ У УЗЛОВ ОТВЕТА НЕ ПОЯВЛЯЕТСЯ: блок конфигурации несёт запрет вложенных внеполосных узлов, и обёрнутый узел не свопается ВОВСЕ — молча"
      status: flagged-unverified
      verification: test
    - statement: "ВТОРОГО ЭКЗЕМПЛЯРА РАЗМЕТКИ СВОДКИ НЕ ЗАВОДИТСЯ: содержимое приходит включением единственного источника, как у линейки счётчика"
      status: flagged-unverified
      verification: test
    - statement: "СКОУП ВЛАДЕЛЬЦА У ВСЕХ НОВЫХ ЧТЕНИЙ ОБЯЗАТЕЛЕН: выборка объявления и выборка ближайшего момента идут со связью владельца, как действующий счёт расписаний"
      status: flagged-unverified
      verification: test
    - statement: "`app/templates/includes/htmx_error_banner.html`, `app/templates/components/modal.html` И `app/static/css/app.css` НЕ ТРОГАЮТСЯ: это предметы планов 10-49 и 10-51, и одновременная правка сделала бы отказ неатрибутируемым"
      status: flagged-unverified
      verification: test
---

<objective>
**После ФРАГМЕНТНОГО удаления два числа на одном экране противоречат друг другу.** Наблюдено
2026-09-11 обходом `walkthrough_3`, ДВАЖДЫ на разных стендах:

- замер 1 — линейка списка «2 расписания», сводка предпросмотра «РАСПИСАНИЯ 3», на сервере 2;
- замер 2 — линейка «1 расписание», сводка «РАСПИСАНИЯ 2», на сервере 1.

Настоящая перезагрузка каждый раз возвращает сводке верное число — то есть расхождение живёт РОВНО до
следующей полной загрузки и всё это время смотрит человеку в глаза.

**Корень сверен с деревом.** Область `#ad-summary` обновляется внеполосно РОВНО ОДНИМ ответом дерева —
`app/templates/ads/includes/autosave_response.html:20`. Фрагментный ответ удаления расписания этой
части не шлёт: свопается список расписаний и линейка, а сводка остаётся прежней. На переходной ветке
(204 + заголовок смены адреса) дефект не наблюдаем — там подменяется тело целиком.

⚠️ **ЭТО НЕ ТО ЖЕ, ЧТО РАСХОЖДЕНИЕ ШАГА 2.8, И РАЗНИЦА НЕСУЩАЯ.** Там экран расходится с сервером
ПОТОМУ ЧТО ОТВЕТ НЕ ДОЕХАЛ, и плашка об этом честно предупреждает. Здесь ответ доехал, своп
СОСТОЯЛСЯ и прошёл успешно — а два числа ОДНОГО экрана противоречат друг другу, и ни одна плашка об
этом не говорит, потому что говорить ей не о чем: отказа не было.

⚠️ **ВЛАДЕЛЕЦ ГЭПА НАЗНАЧЕН ЧЕЛОВЕКОМ, А НЕ ВЫВЕДЕН МАШИНОЙ.** Запись гэпа сама поставила вопрос
прямо: предмет Фазы 10 — рычаг панели, а не сводка объявления. Ответ дал ЧЕЛОВЕК прямой репликой на
прямо заданный вопрос — ФАЗА 10, в той же партии, что G-10-5 и G-10-7; основание решения: дефект
вылез на пути удаления расписания, который Фаза 10 и строила. ⚠️ ЗАПИСЬ СОМНЕНИЯ В ГЭПЕ НЕ
ВЫЧЁРКНУТА — вопрос был законным.

Purpose: экран, на котором два числа об одном предмете спорят, учит человека не верить ни одному из
них. Цена молчаливая: расхождение не поднимает ни события, ни строки в консоли, ни отличия в статусе
— его видит только глаз, и только если смотреть на обе области сразу.

Output: фрагментный ответ удаления шлёт сводку объявления ЧЕТВЁРТЫМ внеполосным узлом, и число в обеих
областях ответа приходит из ОДНОЙ переменной — разойтись им нечем.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-UAT.md
@.planning/phases/10-rychag-components-modal-html/10-01-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**`app/pages/schedules.py`:** `_ad_next_run_at(db, user_id, ad_id)` и `_ad_row(db, user_id, ad_id)` —
оба со связью владельца, оба вызываются ТОЛЬКО внутри отложенной сборки фрагмента; расширенный
контекст отрисовки ответа, где счёт вычисляется ОДИН раз и уезжает в обе области.

**`app/templates/ads/partials/sched_delete_response.html`:** четвёртый внеполосный узел верхнего
уровня — сводка объявления включением единственного источника; приведённая проза о числе узлов, где
прежняя формулировка «три узла» не вычёркивается, а называется устаревшей с названным источником
расхождения.

**Новые символы в `tests/test_pages/test_editor_schedules.py`:**

- `_oob_region(body, node_id)` — область внеполосного узла по идентификатору из тела ответа;
- `_first_integer(text)` — число, напечатанное в поданной области;
- `test_the_ruler_and_the_summary_agree_after_a_fragment_delete` — согласие ТРЁХ счётов;
- `test_the_summary_node_is_a_top_level_child_of_the_response` — применимость внеполосной подмены;
- `test_the_ad_summary_markup_has_exactly_one_source` — единственность источника разметки сводки;
- `test_the_summary_next_run_follows_the_delete` — вторая строка сводки двигается вместе с числом.

**Изменённые символы:** `test_editor_delete_returns_oob_nodes` (докстринг о числе узлов приводится;
счёт узлов снятия остаётся прежним и это утверждается прямо).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: правило согласия трёх счётов — RED на сегодняшнем ответе</name>

  <files>
tests/test_pages/test_editor_schedules.py
  </files>

  <read_first>
- `tests/test_pages/test_editor_schedules.py:1440-1545` — `_editor_delete_body`,
  `test_editor_delete_degrades_without_htmx`, `test_editor_delete_returns_oob_nodes` целиком, включая
  комментарий о фикстуре `htmx_client`. Форма нового правила берётся ОТСЮДА, а не изобретается.
- `tests/test_pages/test_editor_schedules.py:113-140` — `_seed_schedule` вместе с комментарием о
  моменте запуска: он ставится ТОЛЬКО включённой строке, и это важно для второй строки сводки.
- `app/templates/ads/includes/summary.html` ЦЕЛИКОМ — четыре строки «ключ — значение» и имена
  переменных, от которых они зависят: `ad`, `user`, `editor`. ⚠️ ЧИТАТЬ ДОСЛОВНО последнюю строку
  докстринга: контекст объявлен явно, и новое место отрисовки обязано подать РОВНО его.
- `app/templates/ads/includes/autosave_response.html` ЦЕЛИКОМ — единственное сегодня место, шлющее
  область сводки внеполосно, и абзац о том, почему у ответа нет основного места подмены.
- `app/templates/ads/partials/sched_delete_response.html` ЦЕЛИКОМ — три узла, запрет ветви по факту
  удаления, запрет общей обёртки и абзац о том, почему линейка приезжает СОДЕРЖИМЫМ.
- `app/pages/schedules.py:1159-1270` — `_fragment()` целиком с докстрингом и вызов `_ad_schedule_
  count`. ⚠️ ЧИТАТЬ ДОСЛОВНО абзац про `IN-03`: на пути без htmx разметка не собирается вовсе, и
  запрос подсчёта не выполняется. Новые чтения обязаны жить ВНУТРИ этой функции.
  </read_first>

  <behavior>
1. Объявление с ТРЕМЯ расписаниями; одно удаляется через htmx с признаком возврата в редактор.
2. Тело ответа несёт область линейки счётчика и область сводки объявления; числа, напечатанные в
   ОБЕИХ областях, равны между собой И равны счёту расписаний объявления на сервере.
3. СЕГОДНЯ области сводки в теле нет вовсе — это измеренный RED, и он называется отдельно от
   расхождения чисел: «узла нет» и «числа разошлись» суть разные отказы, и правило обязано их
   различать.
  </behavior>

  <action>
**(1) RED ПЕРВЫМ И ЗАМЕРОМ.** Написать `test_the_ruler_and_the_summary_agree_after_a_fragment_delete`
и прогнать на нетронутом дереве. В сводку — команда, код возврата, имя правила и ДОСЛОВНЫЙ текст
отказа: сегодня он обязан говорить об ОТСУТСТВИИ узла сводки, а не о разошедшихся числах.

**(2) РАЗБОР ТЕЛА — ПО ОБЛАСТИ УЗЛА, А НЕ ПО ВХОЖДЕНИЮ ЧИСЛА.** `_oob_region(body, node_id)`
возвращает кусок тела, принадлежащий внеполосному узлу с поданным идентификатором. Счёт числа по
всему телу был бы негодным прибором: в теле стоя́т идентификаторы вида `sched-{N}`, и первое же число
пришло бы оттуда. ⚠️ ЭТО ТОТ ЖЕ КЛАСС ДЕФЕКТА ПРИБОРА, ЧТО РАСХОЖДЕНИЕ Р-1 обхода (селектор шире
предмета), и он называется здесь, а не воспроизводится.

**(3) ТРИ СЧЁТА, А НЕ ДВА.** Сличаются: число линейки, число сводки и счёт расписаний объявления,
снятый прямым запросом к тестовой базе. ⚠️ ТРЕТИЙ СЧЁТ НЕ ИЗБЫТОЧЕН: два числа, приезжающие из одной
переменной, СОГЛАСНЫ ВСЕГДА — в том числе когда оба неверны. Без серверного счёта правило утверждало
бы согласие, а не верность.

**(4) АНТИВАКУУМ ПЕРЕД СЛИЧЕНИЕМ.** До сравнения утверждается, что ОБЕ области в теле найдены и что
в каждой напечатано число. Область, не найденная разбором, дала бы отсутствующее равным
отсутствующему.

**(5) ВТОРАЯ СТРОКА СВОДКИ — ОТДЕЛЬНОЕ ПРАВИЛО.**
`test_the_summary_next_run_follows_the_delete`: стенд из двух расписаний с РАЗНЫМИ моментами запуска;
удаляется то, чей момент БЛИЖЕ. Сводка в ответе обязана назвать момент ОСТАВШЕГОСЯ расписания.
⚠️ ЗАЧЕМ ОТДЕЛЬНО: гэп назван числом расписаний, но устаревает в сводке НЕ ТОЛЬКО оно — вторая строка
читает тот же контекст и разошлась бы так же молча. Правило заводится сейчас, потому что предмет
общий, а не потому, что дефект наблюдён обоими глазами.

**(6) ПРАВИЛО ПРИМЕНИМОСТИ ПОДМЕНЫ.**
`test_the_summary_node_is_a_top_level_child_of_the_response`: узел сводки обязан быть ПРЯМЫМ ДЕТЯМ
тела ответа. Блок конфигурации несёт запрет вложенных внеполосных узлов: у вложенного рантайм МОЛЧА
снимает признак и не свопает вовсе — ни ошибки, ни предупреждения, ни отличия в статусе. Правило
читает тело ответа, а не шаблон: предмет — то, что уехало, а не то, что набрано.
  </action>

  <acceptance_criteria>
- `test_the_ruler_and_the_summary_agree_after_a_fragment_delete` на нетронутом дереве КРАСЕН, и текст
  отказа говорит об ОТСУТСТВИИ узла сводки; приведён дословно в сводке.
- Разбор идёт по ОБЛАСТИ внеполосного узла; счёта числа по всему телу в правиле нет — проверяется
  чтением диффа.
- Сличаются ТРИ счёта, третий — прямым запросом к базе; основание третьего названо в докстринге.
- Антивакуумные половины (обе области найдены, в каждой есть число) стоя́т ДО сличения.
- Правило второй строки сводки и правило применимости подмены заведены отдельными именами.
- `git diff --numstat -- app` пуст.
  </acceptance_criteria>

  <verify>
    <automated>OUT=$(uv run pytest tests/test_pages/test_editor_schedules.py -q -k "ruler_and_the_summary_agree" 2>&1); printf '%s\n' "$OUT" | tail -6; printf '%s\n' "$OUT" | grep -qE '^FAILED .*::test_the_ruler_and_the_summary_agree_after_a_fragment_delete$' && printf '%s\n' "$OUT" | tail -1 | grep -qE '^1 failed'</automated>
    <fails_when>ненулевой код возврата, а он ненулевой при ЛЮБОМ из двух — и оба суть ОДНО требование: RED засчитывается лишь при правиле ОТОБРАННОМ, ИСПОЛНЕННОМ и упавшем. (а) В выводе нет строки `FAILED …::test_the_ruler_and_the_summary_agree_after_a_fragment_delete` — правило не упало вовсе (зеленеет там, где обход дважды наблюдал расхождение) либо упало ДРУГОЕ правило. (б) Итоговая строка не начинается с `1 failed` — правило не было отобрано (`N deselected`) либо прогон оборвался сборкой (`1 error`). ⚠️ ПРЕЖНЯЯ ИНВЕРСИЯ `test $? -ne 0` ЗЕЛЕНЕЛА НА ОБОИХ, И ЭТО ЗАМЕРЕНО: пустой отбор даёт `N deselected … in 0.10s` при коде 0, отказ сборки — `1 error in 0.18s` при ненулевом коде, то есть неотличим от честного RED (замер 2026-09-11, `d17c594`). ⚠️ ОБЪЯВЛЕННАЯ ПРИЧИНА ОТКАЗА («узла сводки в теле НЕТ», а не «числа разошлись») держится акцептансом задачи: текст отказа приводится в сводке дословно, и различение двух родов отказа есть предмет самого правила</fails_when>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py -q -k "returns_oob_nodes or degrades_without_htmx or orphaned_panel"</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — заведение новых правил задело действующие</fails_when>
    <automated>git diff --numstat -- app</automated>
    <fails_when>вывод НЕПУСТ — продукт тронут, а задача правит только модуль суиты</fails_when>
  </verify>

  <done>
Правило согласия трёх счётов заведено и на нетронутом дереве красно отсутствием узла сводки; разбор
идёт по области узла, а не по телу целиком; заведены правило второй строки сводки и правило
применимости внеполосной подмены; продукт не тронут.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: сводка приезжает четвёртым внеполосным узлом, и число у обеих областей одно</name>

  <files>
app/pages/schedules.py
app/templates/ads/partials/sched_delete_response.html
  </files>

  <read_first>
- `app/pages/schedules.py:451-495` — `_ad_has_a_schedule` и `_ad_schedule_count` вместе с их
  докстрингами. ⚠️ ЧИТАТЬ ДОСЛОВНО: скоуп владельца и основание отложенности. Новые чтения строятся
  ПО ЭТОМУ ОБРАЗЦУ, а не рядом с ним своим.
- `app/pages/schedules.py:1159-1270` — `_fragment()` целиком. ⚠️ Имя `_ad_schedule_count` НЕ
  ПЕРЕИМЕНОВЫВАТЬ: оно названо прозой в трёх местах этого же файла и в шаблоне ответа, а
  `tests/test_pages/test_prose_names_live_symbols.py` требует, чтобы имя, названное прозой,
  разрешалось в объявление. Переименование сделало бы правку шире предмета.
- `app/pages/ads.py:286-305` — как ближайший момент запуска считается в контексте редактора: одна
  выборка, минимум по непустым значениям. Новое чтение обязано дать ТО ЖЕ ЧИСЛО на тех же данных,
  иначе сводка после удаления и сводка после перезагрузки разошлись бы молча — ровно тот отказ,
  который линейка счётчика уже однажды оплатила (WARN-1 / WR-01 Фазы 9).
- `app/templates/ads/includes/sched_count_rule.html` — образец включения ЕДИНСТВЕННОГО ИСТОЧНИКА
  содержимого при двух местах отрисовки, вместе с абзацем «⚠️ ПРИЗНАКА ВНЕПОЛОСНОЙ ПОДМЕНЫ ЗДЕСЬ НЕТ
  И БЫТЬ НЕ ДОЛЖНО». Сводка подключается ТЕМ ЖЕ приёмом.
- `app/pages/common.py:259-300, 380-390` — регистрация глобалей окружения, от которых зависит сводка
  (слово состояния объявления и отображение времени пользователя). ⚠️ ПРОВЕРИТЬ ИСПОЛНЕНИЕМ, что они
  доступны при отрисовке через окружение, а не только через ответ-шаблон: сборка фрагмента идёт
  прямым получением шаблона из окружения.
  </read_first>

  <behavior>
1. Ответ фрагментного удаления несёт ЧЕТЫРЕ внеполосных узла верхнего уровня; узлов снятия
   по-прежнему ДВА.
2. Число расписаний вычисляется в сборке фрагмента ОДИН раз и уезжает И в линейку, И в сводку.
3. Ближайший момент запуска и объявление владельца читаются ТОЛЬКО внутри отложенной сборки; путь без
   htmx по-прежнему не выполняет ни одного из этих запросов.
4. Ответ по-прежнему ОДИНАКОВ для найденной и не найденной строки: ветви по факту удаления нет.
  </behavior>

  <action>
**(1) GREEN — ДВА НОВЫХ ЧТЕНИЯ СО СКОУПОМ ВЛАДЕЛЬЦА, ОБА ОТЛОЖЕННЫЕ.** Завести рядом с
`_ad_schedule_count` две функции того же вида и с тем же скоупом: одна отдаёт строку объявления
владельца, вторая — ближайший момент запуска среди расписаний этого объявления. Вызываются ОБЕ ТОЛЬКО
внутри `_fragment()`. Докстринг каждой называет скоуп и отложенность теми же словами, что у соседа, —
и НЕ ПЕРЕСКАЗЫВАЕТ их заново: ссылка на основание соседа короче и не расходится с ним.
⚠️ ЧИСЛО ЗАПРОСОВ ОТЛОЖЕННОЙ СБОРКИ РАСТЁТ С ОДНОГО ДО ТРЁХ, И ЭТО НАЗЫВАЕТСЯ ПРЯМО, А НЕ
УМАЛЧИВАЕТСЯ. Цена платится ТОЛЬКО на пути htmx с признаком возврата в редактор; путь деградации и
ветка перехода не платят ничего — это и есть предмет `IN-03`, и он проверяется правилом задачи 3.

**(2) СЧЁТ ВЫЧИСЛЯЕТСЯ ОДИН РАЗ И ПОДАЁТСЯ ОБЕИМ ОБЛАСТЯМ.** В `_fragment()` значение счёта кладётся
в локальное имя и уезжает в отрисовку ДВАЖДЫ: переменной линейки и полем словаря контекста
предпросмотра. ⚠️ ЭТО И ЕСТЬ ЗАКРЫТИЕ ГЭПА ПО ПОСТРОЕНИЮ, А НЕ ПО СОВПАДЕНИЮ: два независимых чтения
базы согласны сегодня и разойдутся при первой же правке одного из них, а одно значение разойтись ни с
чем не может.

**(3) КОНТЕКСТ ПРЕДПРОСМОТРА ПОДАЁТСЯ РОВНО ТОТ, ЧТО ОБЪЯВЛЕН ИСТОЧНИКОМ РАЗМЕТКИ.** Докстринг
`ads/includes/summary.html` называет свой контекст явно; сборка фрагмента подаёт его и ничего сверх.
Имена полей словаря берутся у контекста редактора (`app/pages/ads.py`), а не выдумываются: второй
набор имён разошёлся бы с первым молча.

**(4) ЧЕТВЁРТЫЙ УЗЕЛ — ПРЯМОЙ РЕБЁНОК ФАЙЛА, СОДЕРЖИМОЕ — ВКЛЮЧЕНИЕМ.** В шаблон ответа
дописывается узел области сводки с признаком внеполосной подмены, а его содержимое приходит
ВКЛЮЧЕНИЕМ единственного источника разметки сводки. Обёртки над узлами не заводится: у вложенного
внеполосного узла рантайм молча снимает признак и не свопает вовсе.
⚠️ ПОДМЕНЯЕТСЯ УЗЕЛ, А НЕ СОДЕРЖИМОЕ, — И ЭТО ОТЛИЧИЕ ОТ ЛИНЕЙКИ, КОТОРОЕ НАЗЫВАЕТСЯ, А НЕ
ПОВТОРЯЕТСЯ ПО ИНЕРЦИИ. У линейки подменяется содержимое, потому что её область обязана пережить
сколько угодно ответов (D-12 Фазы 9). Область сводки в редакторе объявлена с тем же
идентификатором, что приезжает, и второе место её отрисовки (ответ автосохранения) подменяет ИМЕННО
УЗЕЛ — форма берётся ОТТУДА, чтобы два ответа не свопали одну область по-разному.

**(5) ПРОЗА ОБОИХ ФАЙЛОВ ПРИВОДИТСЯ ПО ИДИОМЕ D-30/D-32.** Перечень «тремя узлами верхнего уровня» и
докстринг отложенной сборки «Три внеполосных узла» ОСТАЮТСЯ ДОСЛОВНО, и рядом с каждым встаёт
запись: число узлов стало четыре, источник расхождения — гэп G-10-6 обхода `walkthrough_3` от
2026-09-11 с двумя замерами; прежняя формулировка ошибкой не была — она верна для дерева, на котором
писалась. ⚠️ ТАМ ЖЕ НАЗЫВАЕТСЯ ЦЕНА НЕОТЛИЧИМОСТИ, УНАСЛЕДОВАННАЯ ЧЕТВЁРТЫМ УЗЛОМ: холостой путь
удаления (чужое, несуществующее, уже удалённое расписание) шлёт теперь узлы, часть которых целей в
документе не находит, и каждый такой узел стои́т строки в консоли. Остаток унаследован перечнем
`OOB_TARGET_EXCEPTIONS` с назначенной Фазой 15 и настоящим планом НЕ ЗАКРЫВАЕТСЯ — сказать это прямо.

**(6) ВЕТВИ ПО ФАКТУ УДАЛЕНИЯ НЕ ПОЯВЛЯЕТСЯ.** Объявление владельца может не найтись (чужое либо
несуществующее расписание, признак возврата собран руками). В этом случае узел сводки уезжает
ВСЁ РАВНО, собранный на пустом объявлении, — ровно как уезжают узлы снятия. Условная сборка сделала
бы НАЛИЧИЕ узла признаком того, что удаление состоялось (T-10-01).
  </action>

  <acceptance_criteria>
- `test_the_ruler_and_the_summary_agree_after_a_fragment_delete` ЗЕЛЕНО; в сводке названы ОБА
  состояния — красное до правки и зелёное после.
- Счёт расписаний вычисляется в `_fragment()` ОДИН раз и подаётся обеим областям — проверяется
  чтением диффа; двух чтений базы под одно число в файле нет.
- Имя `_ad_schedule_count` не переименовано; `tests/test_pages/test_prose_names_live_symbols.py`
  зелен.
- Новые чтения вызываются ТОЛЬКО внутри отложенной сборки — проверяется чтением диффа и правилом
  задачи 3.
- Узел сводки — прямой ребёнок файла ответа; общей обёртки нет.
- Прежние формулировки о числе узлов присутствуют дословно, рядом с каждой — запись с названным
  источником расхождения и с ценой неотличимости.
- `git diff --numstat -- app/templates/includes/htmx_error_banner.html app/templates/components/modal.html app/static/css/app.css` пуст.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py tests/test_pages/test_ads_editor.py tests/test_pages/test_confirm_delete_transport.py -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — правка задела редактор объявления либо контракт транспорта подтверждения</fails_when>
    <automated>uv run pytest tests/test_pages/test_prose_names_live_symbols.py tests/test_templates/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed" — имя, названное прозой, перестало разрешаться в объявление, либо инвентарь шаблонов разошёлся с деревом</fails_when>
    <automated>git diff --numstat -- app/templates/includes/htmx_error_banner.html app/templates/components/modal.html app/static/css/app.css</automated>
    <fails_when>вывод НЕПУСТ — тронуты предметы планов 10-49 и 10-51, и отказ соседа стал бы неатрибутируемым</fails_when>
  </verify>

  <done>
Фрагментный ответ несёт четвёртым узлом сводку объявления; число приходит в обе области из одного
значения; новые чтения отложены и скоуплены владельцем; ветви по факту удаления не появилось; проза
обоих файлов приведена по идиоме D-30/D-32 с названной ценой.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: зубы — единственность источника разметки сводки, цена пути деградации и неприкосновенность ветки перехода</name>

  <files>
tests/test_pages/test_editor_schedules.py
  </files>

  <read_first>
- `tests/test_pages/test_editor_schedules.py` — собственный модуль после задач 1 и 2.
- Правило единственности источника разметки линейки счётчика (`test_the_schedule_counter_markup_has_
  exactly_one_source`) — найти его в дереве и прочитать ЦЕЛИКОМ. Правило сводки строится ПО ЭТОМУ
  ОБРАЗЦУ: та же форма, тот же класс отказа, то же основание («вторая копия разошлась бы с первой
  молча»).
- `app/templates/ads/form.html:270-276` — объявление области сводки на странице редактора.
- `app/pages/schedules.py:1115-1160` — ветка перехода: условие ухода в неё и абзац о том, почему
  порядок «сначала признак, потом запрос к базе» несущий.
  </read_first>

  <behavior>
1. Разметка сводки имеет РОВНО ОДИН источник при ДВУХ местах включения; третья копия краснит правило.
2. Путь без htmx по-прежнему отвечает прежним перенаправлением и НЕ ВЫПОЛНЯЕТ запросов отложенной
   сборки.
3. Удаление ПОСЛЕДНЕГО расписания по-прежнему уходит в ветку перехода: фрагмент не собирается, узла
   сводки в ответе нет вовсе, и это не дефект, а объявленное поведение (D-04).
  </behavior>

  <action>
**(1) ЕДИНСТВЕННОСТЬ ИСТОЧНИКА РАЗМЕТКИ СВОДКИ.**
`test_the_ad_summary_markup_has_exactly_one_source` обходит разметку проекта и требует: файл,
несущий разметку сводки, ровно один; включающих его файлов — ДВА, и оба названы поимённо. ⚠️
АНТИВАКУУМ ПЕРВЫМ: число включающих обязано быть не ноль, иначе «источник один» зеленело бы на
источнике, которого никто не включает. Основание правила записывается его же словами: человек видел
бы РАЗНОЕ число в зависимости от того, перезагрузил он страницу или удалил расписание, — то есть
ровно гэп G-10-6, только внесённый заново разметкой вместо контекста.

**(2) ЦЕНА ПУТИ ДЕГРАДАЦИИ ПРЕДЪЯВЛЯЕТСЯ СЧЁТОМ ЗАПРОСОВ, А НЕ ОБЕЩАНИЕМ (IN-03).** Правило считает
запросы, выполненные обработчиком на пути БЕЗ заголовка htmx, и требует, чтобы ни один из трёх
запросов отложенной сборки там не выполнялся. Счёт снимается наблюдением за исполнением запросов
сессии — форму выбирает исполнитель, но ОБЕЩАНИЕМ она быть не может: `IN-03` — записанное свойство, и
третий запрос, тихо переехавший наружу отложенной сборки, обязан краснить прогон.
⚠️ ЕСЛИ ФОРМА НАБЛЮДЕНИЯ ЗАПРОСОВ В ПРОЕКТЕ ЕЩЁ НЕ ЗАВЕДЕНА — завести её ЗДЕСЬ, минимальной, и
назвать границу: правило считает запросы ОДНОГО обработчика на ОДНОМ пути и не утверждает ничего о
числе запросов проекта вообще.

**(3) ВЕТКА ПЕРЕХОДА ПРЕДЪЯВЛЯЕТСЯ НЕТРОНУТОЙ.** Правило: объявление с ОДНИМ расписанием, удаление
через htmx — ответ обязан остаться прежним (заголовок смены адреса, тела фрагмента нет). ⚠️ ЭТО НЕ
ИЗБЫТОЧНО ПРИ ЖИВЫХ ПРАВИЛАХ ВЕТВЛЕНИЯ: настоящий план добавил в отложенную сборку чтения, и
соблазн подать сводку «на всякий случай» и на переходной ветке — прямой. Второй экземпляр пустого
состояния запрещён решением D-04, и запрет обязан иметь зубы именно сейчас.

**(4) ПРАВИЛО ЧИСЛА УЗЛОВ ПРИВОДИТСЯ, А НЕ ПЕРЕПИСЫВАЕТСЯ.** В `test_editor_delete_returns_oob_nodes`
докстринг приводится к сегодняшнему числу узлов; утверждение о ДВУХ узлах снятия остаётся и
усиливается прямой фразой: узлов снятия по-прежнему два, и четвёртый узел их числа не сдвинул.

**(5) ПОЛНЫЙ ПРОГОН СУИТЫ.** Прогнать `uv run pytest tests/` целиком; итоговая строка — в сводку
числом. ⚠️ ОТДЕЛЬНО ПРЕДЪЯВИТЬ ЗЕЛЁНЫМ `test_the_counter_node_belongs_to_the_ad_named_by_the_request`
(`tests/test_pages/test_confirm_delete_transport.py`): это правило закрепляет СЕГОДНЯШНЕЕ поведение
третьего узла нормой, и четвёртый узел наследует ту же адресацию. Если оно покраснело — предмет
изменился, и это надо назвать, а не подкрутить.

**(6) ЗАПИСАТЬ В СВОДКУ ОСТАТОК.** Настоящий план закрывает расхождение на ФРАГМЕНТНОЙ ветке пути
удаления расписания. Он НЕ закрывает: (а) прочие пути, способные оставить сводку устаревшей, если
такие есть, — перечня их не снималось, и выдавать отсутствие перечня за отсутствие путей запрещено;
(б) остаток `OOB_TARGET_EXCEPTIONS` с назначенной Фазой 15. Оба пункта назвать прямо.
  </action>

  <acceptance_criteria>
- Правило единственности источника разметки сводки заведено, антивакуумная половина стои́т первой, оба
  включающих файла названы поимённо.
- Правило счёта запросов на пути деградации заведено и краснеет, если чтение переезжает наружу
  отложенной сборки; граница правила названа в докстринге.
- Правило неприкосновенности ветки перехода заведено и зелено.
- `test_editor_delete_returns_oob_nodes` зелено, докстринг приведён, утверждение о двух узлах снятия
  усилено прямой фразой.
- `test_the_counter_node_belongs_to_the_ad_named_by_the_request` зелено и названо в сводке отдельно.
- `uv run pytest tests/` зелен; итоговая строка приведена числом.
- В сводке назван остаток из двух пунктов.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed", либо отобрано меньше правил, чем до партии, плюс новые правила плана</fails_when>
    <automated>uv run pytest tests/ -q</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed" — партия обязана оставить суиту зелёной целиком</fails_when>
  </verify>

  <done>
Разметка сводки имеет один источник при двух включениях и это принуждено; цена пути деградации
предъявлена счётом запросов, а не обещанием; ветка перехода предъявлена нетронутой; полная суита
зелена; остаток назван.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| адрес запроса → выборка расписания | идентификатор пути подконтролен отправителю и пересекает границу владения |
| поле контекста формы → адресация внеполосных узлов | `ad_id` приходит телом запроса и участвует в выборе того, чьи числа уедут в документ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-50-01 | Information Disclosure | новые чтения объявления и ближайшего момента в `app/pages/schedules.py` | high | mitigate | обе выборки идут со связью владельца по образцу `_ad_schedule_count`; чужое объявление даёт пустой результат, а не чужие числа |
| T-10-50-02 | Information Disclosure | четвёртый узел как признак существования строки | high | mitigate | ветви по факту удаления в шаблоне нет: узел сводки уезжает и тогда, когда объявление не найдено — неотличимость найденной и не найденной строки сохранена (T-10-01) |
| T-10-50-03 | Spoofing | адресация узла сводки статическим селектором при чужом `ad_id` в теле запроса | medium | accept | тот же класс, что унаследованный `WR-02`/`WR-05` у третьего узла: из интерфейса случай недостижим (обе формы пути шлют `ad_id` своего экрана), скоуп владельца даёт пустое, а не чужое; остаток назван перечнем `OOB_TARGET_EXCEPTIONS` с назначенной Фазой 15 |
| T-10-50-04 | Denial of Service | три запроса вместо одного на путь удаления | low | mitigate | запросы живут ВНУТРИ отложенной сборки; путь деградации и ветка перехода не платят ничего — держит правило счёта запросов задачи 3 (IN-03) |
| T-10-50-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_editor_schedules.py -q` — зелено.
2. `uv run pytest tests/ -q` — зелено целиком.
3. Переход цвета правила согласия трёх счётов измерен и приведён обоими состояниями.
4. `git diff --numstat -- app/templates/includes/htmx_error_banner.html app/templates/components/modal.html app/static/css/app.css` пуст на всех трёх задачах.
5. `test_the_counter_node_belongs_to_the_ad_named_by_the_request` предъявлено зелёным отдельно.
</verification>

<success_criteria>
- Линейка списка и сводка предпросмотра приезжают одним ответом и одним значением, и согласие
  показано разбором тела, а не наблюдением глазом.
- Третий счёт (сервер) отличает согласие от верности.
- Разметка сводки имеет один источник при двух включениях, и это принуждено правилом.
- Путь деградации и ветка перехода не тронуты, и это показано, а не обещано.
- Полная суита зелена.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-50-SUMMARY.md` when done
</output>
