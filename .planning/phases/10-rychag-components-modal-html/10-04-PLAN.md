---
phase: 10-rychag-components-modal-html
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - tests/test_pages/test_htmx_gates.py
  - tests/test_templates/test_components.py
  - app/templates/components/modal.html
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FORM-06]

estimate:
  # ⚠️ Это НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а НЕ замер. Калибровки у проекта не
  # существует (estimate-calibration: sample_count 0, factor 1), поэтому
  # tokens и raw_tokens совпадают, а confidence: low ВЫВЕДЕН ИЗ ЧИСЛА
  # ЗАМЕРОВ (ноль), а не из самооценки планировщика.
  # Смарт-зона: 90000 при бюджете 100000 — в бюджете.
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Заведён ВТОРОЙ СЧЁТ «отдаёт фрагмент», и числа не совмещены в одно: первый счёт («стои́т на слое ответа») остаётся убывающим счётчиком прогресса вехи, второй — растущим счётом обработчиков, отдающих фрагмент, и по нему меряется FORM-03 Фазы 11 (D-18)"
    - "Второй счёт вырос с двух на единицу: удаление расписания из редактора добавлено к тумблеру и удалению группы аккаунта; число объявлено константой и утверждается разбором дерева, а не доверием к перечню"
    - "Два счёта связаны правилом СОГЛАСОВАННОСТИ, а не приведены к равенству: обработчик, отдающий фрагмент, обязан отсутствовать в перечне отставания — обработчик на слое ответа, числящийся непереведённым, есть ровно то расхождение двух множеств, от которого стоят гейты единственности решения"
    - "Критерий 3 роадмапа утверждается ЧЕТЫРЬМЯ ЧИСЛАМИ, а не заявлением: набор вендоренных файлов сценариев равен объявленному и не вырос; число шаблонов со встроенным обработчиком отправки не выросло; вхождений атрибутов событий htmx в дереве ноль; число узлов клиентского состояния не двинулось (D-13)"
    - "Каждое из четырёх чисел берётся из ЕДИНСТВЕННОГО источника: три существующих читаются импортом объявляющих их модулей, и только четвёртое (набор вендоренных файлов) объявляется здесь впервые — второй экземпляр любого из них разошёлся бы с первым молча"
    - "Прочтение критерия 3 (D-13) записано В КОДЕ, рядом с правкой, а не только в артефакте планирования: следующим читателем будет верификатор, а он читает код"
    - "Расхождение записей вехи закрыто: заголовок и первый критерий Фазы 10 в роадмапе и текст FORM-06 в требованиях говорят 18, и летопись называет ИСТОЧНИК расхождения — планы 06-05, 06-07 и 06-12 добавили места после прогноза разведки от 2026-08-26 (D-17)"
    - "Записи разведки НЕ правятся: они архив того, что было видно тогда, и правка превратила бы архив в задним числом переписанную историю"
    - "Прогноз разведки назван УСТАРЕВШИМ, а не ошибочным: формулировка летописи обязана говорить именно это, потому что «16» было верным на момент своей записи"
    - statement: "Соответствие записей вехи и числа гейта проверено ГЛАЗАМИ, а не машиной: документная половина утверждения «объявление против кода» здесь остаётся documentary по прецеденту D-33 Фазы 5 — кодовая половина несёт машинный гейт (число мест объявлено константой и сходится тремя счётами), документная закрывается чтением при приёмке фазы. ⚠️ ДЫРА НАЗВАНА, А НЕ ЗАКРЫТА: после закрытия фазы у документной половины НЕ ОСТАЁТСЯ стерегущего правила — если ROADMAP или REQUIREMENTS разъедутся с числом гейта снова, ничто не покраснеет. Кандидат на закрытие назван: сводный обход доступности и документов Фазы 15 (GATE-09) либо отдельная запись бэклога «правило сличения числа мест в записях вехи с константой гейта». Фаза 10 её не закрывает потому, что правило, читающее .planning/, есть новый КЛАСС гейта (гейт на документ, а не на код), и заводить его попутно рычагу значило бы смешать два изменения"
      verification: backstop
  artifacts:
    - "tests/test_pages/test_htmx_gates.py — FRAGMENT_RESPONSE_HANDLERS, FRAGMENT_RESPONSE_HANDLERS_DECLARED, test_every_fragment_response_handler_is_actually_on_the_response_layer, test_the_number_of_fragment_response_handlers_is_the_declared_one, test_no_fragment_response_handler_is_listed_as_not_yet_converted, test_control_negative_an_undeclared_fragment_handler_reddens_the_gate"
    - "tests/test_templates/test_components.py — VENDORED_JS_FILES, test_criterion_three_holds_by_the_numbers, test_control_negative_a_new_vendored_script_reddens_the_gate"
    - "app/templates/components/modal.html — абзац шапки с прочтением критерия 3 роадмапа (D-13)"
    - ".planning/ROADMAP.md — заголовок и критерий 1 Фазы 10 с числом 18 и летописью 16 → 18"
    - ".planning/REQUIREMENTS.md — текст FORM-06 с числом 18 и той же летописью"
  key_links:
    - "FRAGMENT_RESPONSE_HANDLERS → критерий 1 Фазы 11: единственное число, по которому следующая фаза измерит свой массовый перевод; ломается — Фаза 11 меряет прогресс убывающим счётчиком отставания, который отвечает за ДРУГОЕ свойство, и два разных вопроса получают один ответ"
    - "VENDORED_JS_FILES → критерий 3 роадмапа: единственный машинный признак того, что фаза не добавила нового файла сценариев; ломается — критерий 3 закрывается заявлением, а не измерением"
    - "летопись 16 → 18 → следующий читатель записей вехи: единственное место, где сказано, ПОЧЕМУ число выросло; ломается — расхождение выглядит опечаткой, и следующая фаза «чинит» его обратно"
  prohibitions:
    - statement: "два счёта не совмещаются в одно число: одно число прогресса не может отвечать за два разных свойства — идиома у проекта своя (два счёта РАЗНЫХ множеств, приводить их к согласию правкой одного нельзя)"
      status: flagged-unverified
      verification: none
    - statement: "критерии успеха Фазы 11 не правятся: Фаза 10 не переписывает чужие критерии до того, как та фаза началась (D-18)"
      status: flagged-unverified
      verification: none
    - statement: "формулировка критерия 3 в роадмапе не амендируется — документ вехи держится фиксированным, а прочтение есть предмет CONTEXT.md и кода (D-13)"
      status: flagged-unverified
      verification: none
    - statement: "записи разведки (.planning/research/*) не правятся ни на символ: они архив того, что было видно на 2026-08-26 (D-17)"
      status: flagged-unverified
      verification: none
    - statement: "из перечня отставания снимаются ТОЛЬКО восемь обработчиков фазы; снятие «заодно» соседних, ещё не переведённых, запрещено — счётчик прогресса вехи обязан остаться правдивым"
      status: flagged-unverified
      verification: none
    - statement: "второй экземпляр любого из четырёх чисел критерия 3 не заводится: три существующих читаются из объявляющих их модулей импортом"
      status: flagged-unverified
      verification: none
---

<objective>
Фаза закрывает свою бухгалтерию. Заводится ВТОРОЙ СЧЁТ — «обработчик отдаёт фрагмент», — по
которому Фаза 11 будет мерить свой массовый перевод, и он не смешивается с убывающим счётчиком
отставания. Критерий 3 роадмапа перестаёт быть заявлением и становится четырьмя числами.
Расхождение записей вехи («16 мест» против 18, считаемых по файлам) закрывается правкой
роадмапа и требований с летописью, называющей источник расхождения.

Purpose: без второго счёта Фаза 11 меряла бы прогресс числом, отвечающим за другое свойство;
без чисел критерия 3 «новых строк JS фаза не добавляет» проверялось бы чтением диффа глазами;
без летописи расхождение «16 против 18» выглядело бы опечаткой, и следующая фаза починила бы
его обратно.

Output: два новых перечня с числами и контролями, гейт критерия 3, абзац прочтения D-13 в
шапке компонента, исправленные записи вехи.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md
@.planning/phases/10-rychag-components-modal-html/10-PATTERNS.md
@.planning/phases/10-rychag-components-modal-html/10-02-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов фазы — в `10-01-PLAN.md` §«Artifacts this phase produces».
Этим планом создаются:

- `FRAGMENT_RESPONSE_HANDLERS`, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` —
  `tests/test_pages/test_htmx_gates.py`
- `VENDORED_JS_FILES` — `tests/test_templates/test_components.py`
- имена правил: `test_every_fragment_response_handler_is_actually_on_the_response_layer`,
  `test_the_number_of_fragment_response_handlers_is_the_declared_one`,
  `test_no_fragment_response_handler_is_listed_as_not_yet_converted`,
  `test_control_negative_an_undeclared_fragment_handler_reddens_the_gate`,
  `test_criterion_three_holds_by_the_numbers`,
  `test_control_negative_a_new_vendored_script_reddens_the_gate`
- абзац шапки `app/templates/components/modal.html` с прочтением критерия 3 роадмапа
</artifacts_this_phase_produces>

<tasks>

<task type="execute" tdd="true">
  <name>Задача 1: Второй счёт «отдаёт фрагмент» и критерий 3 в четырёх числах</name>

  <files>
tests/test_pages/test_htmx_gates.py,
tests/test_templates/test_components.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py:1-80` — шапка модуля: гейты единственности решения о
  форме ответа (G-1/G-2), их предметы и записанное объяснение, почему гейт на пустоту не
  является гейтом; `:199-283` — `NOT_YET_CONVERTED` и `NOT_YET_CONVERTED_COUNT` ПОСЛЕ плана
  10-03 (значение 26) с полной летописью; `:355-470` — `_Handler`, `_declares_post`,
  `_calls_response_layer` и `_builds_own_redirect`: готовый разбор по дереву, на котором стои́т
  и второй счёт; `:468+` — `_post_handlers` и то, как он собирает словарь обработчиков.
- `tests/test_templates/test_components.py:905-915` — `MODAL_IMPORTERS` / `MODAL_EVENT_NAMES` /
  `MODAL_PLACES`; `:1150-1200` — `MODAL_CONSUMERS` и комментарий «два счёта РАЗНЫХ множеств,
  приводить их к согласию правкой одного нельзя» — ПРЯМОЕ основание D-18, читать дословно.
- `tests/test_templates/test_components.py:725-735` — `KNOWN_SUBMIT_HANDLER_FILES`; `:1000-1020`
  — правило, утверждающее равенство найденного набора объявленному.
- `tests/test_templates/test_htmx_markup_security.py:60-80` — абзац о том, что атрибутов
  событий рантайма htmx в проекте ноль и ноль этот утверждается отдельно; `:172` —
  `INLINE_HANDLER_ATTR`, единственный источник выражения для этих атрибутов; `:320-335` —
  действующее правило, которое его применяет.
- `tests/test_templates/test_htmx_markup_gates.py:1869-1895` — `CLIENT_STATE_NODES = 24` и
  её летопись, включая записи о том, что планы Фазы 9 её не двигали.
- `tests/test_pages/test_htmx_response_layer.py:44-55` — образец импорта помощника из
  СОСЕДНЕГО тестового модуля с записанным основанием «единственный источник, а не вторая
  копия»: по этой же доктрине читаются три существующих числа критерия 3.
- `app/static/js/` — два файла: вендоренные рантаймы разметки и клиентского состояния.
- `tests/test_pages/test_shell.py:1240-1340` — что уже утверждается о вендоренном рантайме
  (ссылка в оболочке, отсутствие обращений к хранилищу браузера) и чего там НЕТ: правила о
  СОСТАВЕ каталога сценариев не существует.
- `10-CONTEXT.md` D-13 и D-18 целиком, включая перечень отвергнутых вариантов.
- `.planning/ROADMAP.md` §«Phase 11» — критерий 1: 15 обработчиков класса A и 5 класса B;
  читать обязательно, потому что второй счёт заводится ПОД НЕГО и не имеет права его
  переписать.
  </read_first>

  <precondition>
Планы 10-02 и 10-03 приземлены. Проверить:
`grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'NOT_YET_CONVERTED_COUNT = 26'`
возвращает `1`; `uv run pytest tests/test_templates/ tests/test_pages/ -q` — зелено за вычетом
известного красного `full-suite-ads-editor-order-pollution`.
  </precondition>

  <behavior>
1. `test_every_fragment_response_handler_is_actually_on_the_response_layer` — НЕСУЩЕЕ: каждый
   объявленный обработчик существует, идёт через слой ответа И передаёт ему фрагмент. Признак
   передачи фрагмента берётся РАЗБОРОМ ДЕРЕВА: у вызова слоя ответа есть именованный аргумент
   фрагмента. Отказ обязан называть обработчика поимённо и то, чего у него не нашлось.
2. `test_the_number_of_fragment_response_handlers_is_the_declared_one` — число найденных
   обработчиков с признаком передачи фрагмента равно объявленному. Число ставится ПРОГОНОМ
   покрасневшего правила: правило обязано сначала назвать в отказе «найдено N, объявлено M» и
   перечислить найденных поимённо.
3. `test_no_fragment_response_handler_is_listed_as_not_yet_converted` — СОГЛАСОВАННОСТЬ двух
   счётов: пересечение двух множеств пусто. Правило существует потому, что обработчик на слое
   ответа, числящийся непереведённым, есть ровно то расхождение, от которого стоят гейты
   единственности решения.
4. `test_control_negative_an_undeclared_fragment_handler_reddens_the_gate` — ОТРИЦАТЕЛЬНЫЙ
   КОНТРОЛЬ: подставленный в словарь исходников обработчик, передающий фрагмент и не
   объявленный записью, КРАСНИТ правило числа. Подмена не имеет права протечь в следующий тест
   через кэш чтения.
5. `test_criterion_three_holds_by_the_numbers` — критерий 3 роадмапа четырьмя числами:
   набор файлов каталога сценариев равен объявленному; число шаблонов со встроенным
   обработчиком отправки равно длине уже существующего перечня; вхождений атрибутов событий
   рантайма разметки в дереве ноль; число узлов клиентского состояния равно уже объявленной
   константе. Три последних числа читаются ИМПОРТОМ из объявляющих их модулей.
6. `test_control_negative_a_new_vendored_script_reddens_the_gate` — контроль: подставленный в
   набор лишний файл сценариев КРАСНИТ правило.
  </behavior>

  <action>
**1. Второй счёт (`tests/test_pages/test_htmx_gates.py`).**

Заводится перечень `FRAGMENT_RESPONSE_HANDLERS` — ключи той же формы, что и у перечня
отставания (`путь/файл.py::имя_функции`), и константа `FRAGMENT_RESPONSE_HANDLERS_DECLARED`
с числом записей. Сегодняшние три: тумблер группы аккаунта, удаление группы аккаунта и —
новая — удаление расписания из редактора объявления. Каждая запись сопровождается
комментарием по форме `MODAL_CONSUMERS`: чем плане она заведена и почему ЭТОТ обработчик
отдаёт фрагмент, а соседние — нет.

Над перечнем пишется абзац, объясняющий, ЗАЧЕМ счёт второй, а не тот же самый. Абзац обязан
сказать четыре вещи и ни одной меньше:
- перечень отставания есть УБЫВАЮЩЕЕ число и отвечает на вопрос «стои́т ли обработчик на слое
  ответа»; этот — РАСТУЩЕЕ и отвечает на вопрос «отдаёт ли он фрагмент»;
- одно число прогресса не может отвечать за два разных свойства, и идиома у проекта своя —
  назвать её поимённо (два счёта РАЗНЫХ множеств у панели подтверждения, приводить их к
  согласию правкой одного нельзя);
- по этому числу меряется первый критерий Фазы 11, и Фаза 10 его НЕ переписывает — она даёт
  Фазе 11 измерительный прибор, а не новую цель;
- снятие из перечня отставания ТОЛЬКО фрагментных обработчиков было отвергнуто: обработчик на
  слое ответа, числящийся непереведённым, — то самое расхождение двух множеств, ради которого
  гейты единственности решения и существуют.

Признак передачи фрагмента вычисляется помощником, работающим по ДЕРЕВУ РАЗБОРА, а не по
подстроке: у вызова слоя ответа ищется именованный аргумент фрагмента. Помощник объявляется
рядом с действующими `_calls_response_layer` и `_builds_own_redirect` и наследует их
записанную границу: псевдоним импорта не узнаётся, и ошибка здесь безопасна по направлению —
обработчик окажется НЕ объявленным фрагментным, а не «разрешённым по умолчанию».

Летопись числа заводится сразу, первой записью: 2 → 3, Фаза 10, план 10-04, источник движения
СЛОВАМИ («удаление расписания из редактора объявления стало третьим обработчиком, отдающим
фрагмент»).

**2. Критерий 3 в числах (`tests/test_templates/test_components.py`).**

Объявляется `VENDORED_JS_FILES` — множество ИМЁН файлов каталога сценариев с комментарием,
называющим оба файла по назначению и объясняющим, почему набор объявляется ИМЕНАМИ, а не
числом: файл, переименованный на месте, при счёте числом прошёл бы молча.

Правило `test_criterion_three_holds_by_the_numbers` сводит четыре утверждения в одно место и
называет в докстринге прочтение критерия 3 (D-13) целиком: критерий запрещает НОВЫЕ СУЩНОСТИ,
а не правку существующих; правка тел методов объекта клиентского состояния и выражения
существующего атрибута новой сущностью не является; отвергнутое буквальное прочтение
запретило бы починку теми же критериями, которые просят починки.

Три из четырёх чисел ЧИТАЮТСЯ ИМПОРТОМ из объявляющих их модулей — перечень шаблонов со
встроенным обработчиком отправки уже живёт в этом же файле, выражение для атрибутов событий
рантайма разметки и число узлов клиентского состояния импортируются из соседних тестовых
модулей по действующей доктрине единственного источника (образец импорта помощника между
тестовыми модулями — файл слоя ответа). Второй экземпляр любого из трёх не заводится: он
разошёлся бы с первым молча, и правило проверяло бы устаревшее значение при зелёном соседе.

Отказ правила обязан называть, КАКОЕ из четырёх утверждений не сошлось, и приводить найденное
и объявленное — общий отказ «критерий 3 нарушен» не сказал бы, где искать.

Отрицательный контроль подставляет в набор имён лишний файл и утверждает, что правило
КРАСНЕЕТ. Подстановка доказывает, что изменила что-то и ИМЕННО то.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_components.py -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_htmx_gates.py -k "fragment_response_handler" -x` — зелено, четыре правила включая контроль.
- `grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'FRAGMENT_RESPONSE_HANDLERS_DECLARED = 3'` возвращает `1`.
- Два счёта не пересекаются: `uv run pytest tests/test_pages/test_htmx_gates.py -k "no_fragment_response_handler_is_listed_as_not_yet_converted" -x` — зелено.
- Первый счёт не тронут этим планом: `grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'NOT_YET_CONVERTED_COUNT = 26'` возвращает `1`.
- `uv run pytest tests/test_templates/test_components.py -k "criterion_three_holds_by_the_numbers or control_negative_a_new_vendored_script" -x` — зелено.
- Набор вендоренных файлов объявлен ИМЕНАМИ: `grep -v '^#' tests/test_templates/test_components.py | grep -c 'VENDORED_JS_FILES'` возвращает не менее `2` (объявление и использование).
- Второго экземпляра существующих чисел не заведено: `grep -c 'CLIENT_STATE_NODES = ' tests/test_templates/test_components.py` возвращает `0`; `grep -c 'KNOWN_SUBMIT_HANDLER_FILES = ' tests/test_templates/test_components.py` возвращает `1` (единственное, действующее объявление).
- `uv run pytest tests/test_templates/ tests/test_pages/ -q` — зелено за вычетом известного красного `full-suite-ads-editor-order-pollution`.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Второй счёт заведён, объявлен числом, утверждён разбором дерева и снабжён отрицательным
контролем; два счёта связаны правилом согласованности, а не сведены в одно; критерий 3
роадмапа утверждается четырьмя числами, три из которых читаются из единственных источников
импортом, а четвёртое объявлено здесь именами файлов; прочтение критерия 3 записано в
докстринге правила.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: Прочтение критерия 3 в коде и приведение записей вехи «16 → 18»</name>

  <files>
app/templates/components/modal.html,
.planning/ROADMAP.md,
.planning/REQUIREMENTS.md
  </files>

  <read_first>
- `app/templates/components/modal.html` — шапка ПОСЛЕ планов 10-01 и 10-02: абзацы о снятых
  параметрах (помечены отменёнными) и об исполненном приземлении фокуса. Новый абзац
  становится рядом с ними и по той же плотности записи.
- `.planning/ROADMAP.md` §«Phase 10: Рычаг `components/modal.html`» — заголовок, цель и все
  четыре критерия успеха; критерий 1 несёт число, критерий 3 — формулировку, которую прочтение
  D-13 объясняет, но НЕ правит.
- `.planning/ROADMAP.md` §«Ручной UAT — девять именованных пунктов» — строка пункта 4 и её
  колонка фазы; строка пункта 9 и его назначенная фаза.
- `.planning/REQUIREMENTS.md:31` — текст FORM-06 с числом; `:135` — строка прослеживаемости
  FORM-06 → Phase 10 → Pending.
- `.planning/STATE.md` §Decisions, записи D-30 и D-32 — образец обращения с отменённой
  формулировкой: прежнее не вычёркивается, а называется устаревшим с указанием того, что и
  когда его отменило.
- `10-CONTEXT.md` D-17 и D-13 целиком; §specifics, вторая строка — прямое требование к
  формулировке летописи: «прогноз разведки не был ошибкой, он устарел, и запись обязана
  говорить именно это».
- `10-PATTERNS.md` §«Что измерено попутно» — измерение, закрывающее чтение «16 — это панели,
  18 — кнопки»: вызовов макроса в исходнике 14 строк в 11 файлах, и числу «16» не
  соответствует ни одно считаемое по файлам множество.
- `.planning/research/SUMMARY.md` (и соседние файлы каталога разведки) — прочитать ровно затем,
  чтобы УБЕДИТЬСЯ, где именно живёт прогноз «16», и НЕ ПРАВИТЬ его.
  </read_first>

  <precondition>
Задача 1 приземлена; `uv run pytest tests/ -q` — зелено за вычетом известного красного
`full-suite-ads-editor-order-pollution`. Число мест подтверждено гейтом:
`grep -v '^#' tests/test_templates/test_components.py | grep -c 'MODAL_PLACES = 18'`
возвращает `1`, и `uv run pytest tests/test_templates/test_components.py -k "modal_site_inventory" -q`
проходит.
  </precondition>

  <behavior>
Машинной половины у этой задачи ОДНА: абзац прочтения критерия 3 живёт в коде, и его наличие
проверяется грепом по шаблону компонента. Документная половина — правка двух записей вехи —
закрывается ЧТЕНИЕМ при приёмке фазы, и это названо прямо, а не выдано за машинный результат:
прецедент разведения половин по предмету записан решением D-33 Фазы 5 (кодовая половина несёт
настоящий гейт, документная переводится в судейскую честно).

Числу «18» машинный гейт уже есть и заведён не здесь: перечень мест подтверждения объявлен
константой и сходится тремя счётами. Задача приводит записи вехи В СООТВЕТСТВИЕ с этим гейтом,
а не заводит второй источник числа.
  </behavior>

  <action>
**1. Абзац прочтения критерия 3 в шапке `app/templates/components/modal.html`.**
Абзац обязан назвать: сам критерий дословно; ЧТО он запрещает в прочтении владельца (новые
СУЩНОСТИ, а не правку существующих); четыре вещи, которые фаза не прибавила, — файл в каталоге
сценариев, встроенный обработчик отправки, атрибут события рантайма разметки, узел клиентского
состояния; и то, что все четыре утверждаются гейтом, с указанием имени правила. Отдельной
строкой — что буквальное прочтение отвергнуто, потому что оно запретило бы починку теми же
критериями, которые просят починки, и что формулировка критерия в роадмапе НЕ амендирована:
документ вехи держится фиксированным, а прочтение есть предмет записи решений и кода.
Абзац ставится рядом с правкой, которую он объясняет, а не в конец файла: следующим читателем
будет верификатор, и он читает код.

**2. `.planning/ROADMAP.md` — строка цели и критерий 1 Фазы 10.**
Число мест приводится к 18 в ДВУХ местах раздела, и оба измерены грепом, а не найдены на
глаз: строка `**Goal**:` («во всех 16 местах одной правкой одного файла») и первый критерий
успеха («Все 16 мест подтверждения удаления…»). Рядом — летопись отдельной строкой, называющая:
прежнее значение и новое; источник расхождения ПОИМЁННО (планы 06-05, 06-07 и 06-12 добавили
места после прогноза разведки от 2026-08-26); и характер расхождения теми самыми словами —
прогноз не был ошибкой, он УСТАРЕЛ. Отдельно записывается измерение, закрывающее ошибочное
чтение «16 — это панели, а 18 — кнопки-триггеры»: вызовов макроса в исходнике 14 строк в 11
файлах, то есть числу 16 не соответствует ни одно множество, считаемое по файлам.
Формулировка критерия 3 НЕ трогается ни на символ. Прочие критерии, цель, зависимости и запись
о разведке не трогаются. Строка пункта 4 перечня ручного UAT не трогается.

**3. `.planning/REQUIREMENTS.md` — текст FORM-06.**
Число приводится к 18, и рядом ставится ТА ЖЕ летопись — не пересказ, а та же формулировка, с
тем же источником расхождения. Два места одного утверждения обязаны говорить одно: расхождение
между ними есть тот класс дефекта, за который веха уже платила раундами.
Строка прослеживаемости `FORM-06 | Phase 10` не трогается — статус двигает не эта правка, а
закрытие фазы.

**4. Записи разведки НЕ правятся.** Ни один файл каталога разведки не изменяется. Основание
записывается в летописи одной строкой: они архив того, что было видно тогда, и правка
превратила бы архив в переписанную задним числом историю.

**5. Сводная строка фазы.** В летопись роадмапа добавляется одна строка, называющая, ЧТО фаза
подвинула в числах: слепая зона гейтов разметки сокращена на рычаге всех 18 мест; счётчик
прогресса вехи опущен 34 → 26; заведён второй счёт, растущий с двух на единицу. Строка нужна
затем, чтобы читатель, поймавший красное число через фазу, увидел за один заход, что двигалось
и кем.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_components.py -k "modal_site_inventory or modal_guard_is_inherited or criterion_three" -q</automated>
    <human-check>Приёмка документной половины: прочитать §«Phase 10» роадмапа и текст FORM-06 требований и убедиться, что оба говорят 18, что летопись в обоих местах ОДНА И ТА ЖЕ формулировка, и что прогноз разведки назван устаревшим, а не ошибочным. Машинного эквивалента у этой проверки нет и не заводится — прецедент разведения половин записан решением D-33 Фазы 5.</human-check>
  </verify>

  <acceptance_criteria>
- Абзац прочтения критерия 3 живёт в коде: `grep -c 'критерий 3' app/templates/components/modal.html` возвращает не менее `1`, и в том же файле присутствует имя правила `test_criterion_three_holds_by_the_numbers`.
- Записи вехи говорят 18 в ОБОИХ местах роадмапа: `awk '/^### Phase 10:/,/^### Phase 11:/' .planning/ROADMAP.md | grep -cE '18 мест(ах)?'` возвращает не менее `2` (строка цели и критерий 1); `awk '/^### Phase 10:/,/^### Phase 11:/' .planning/ROADMAP.md | grep -cE '(всех|Все) 16 мест'` возвращает `0`.
- Требования говорят 18: `grep -n 'FORM-06' .planning/REQUIREMENTS.md | head -1` указывает на строку, в которой `grep -c '18 местах'` даёт `1`, а `grep -c '16 местах'` — `0`.
- Летопись присутствует в ОБОИХ файлах: `awk '/^### Phase 10:/,/^### Phase 11:/' .planning/ROADMAP.md | grep -c '06-05'` возвращает не менее `1`; `grep -c '06-05' .planning/REQUIREMENTS.md` возвращает не менее `1`.
- Формулировка критерия 3 не тронута: `awk '/^### Phase 10:/,/^### Phase 11:/' .planning/ROADMAP.md | grep -c 'новых строк JS фаза не добавляет'` возвращает `1` — то же значение, что и на пред-фазовом коммите `9f3c59e` (проверено: `git show 9f3c59e:.planning/ROADMAP.md | awk '/^### Phase 10:/,/^### Phase 11:/' | grep -c 'новых строк JS фаза не добавляет'` даёт `1`).
- Записи разведки не тронуты: `git diff --name-only 9f3c59e -- .planning/research/` пусто. База ПИННИТСЯ ЯВНЫМ SHA, а не `HEAD~1`: при покоммитном исполнении задач `HEAD~1` — это предыдущая ЗАДАЧА, а не пред-фазовый коммит, и дифф по каталогу разведки на нём был бы пуст даже после правки этого каталога первой задачей.
- Строка пункта 4 ручного UAT не тронута: `grep -c 'действительно снимает осиротевшую панель' .planning/ROADMAP.md` возвращает `1`.
- `uv run pytest tests/ -q` — зелено за вычетом известного красного `full-suite-ads-editor-order-pollution`.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Прочтение критерия 3 записано в коде рядом с правкой, которую оно объясняет, и называет имя
утверждающего его правила. Обе записи вехи говорят 18 и несут ОДНУ И ТУ ЖЕ летопись с
поимённым источником расхождения и словом «устарел», а не «ошибочен». Формулировка критерия 3,
строка пункта 4 ручного UAT и весь каталог разведки не тронуты. Сводная строка фазы называет
три подвинутых числа.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| исходники приложения → разборщик гейта | гейты читают дерево разбора и текст шаблонов; сломанный разборщик даёт пустое множество, а пустое множество вакуумно зелено |
| записи вехи → следующий читатель | число мест подтверждения, прочитанное из документа, может разойтись с числом, утверждаемым гейтом |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-22 | Tampering | второй счёт, зеленеющий на пустом множестве | medium | mitigate | Число объявлено ИМЕНОВАННОЙ КОНСТАНТОЙ и утверждается отдельным правилом; отрицательный контроль подставляет необъявленного фрагментного обработчика и обязан покраснеть — пустое множество перестаёт быть вакуумно зелёным |
| T-10-23 | Tampering | новый файл сценариев, добавленный будущей фазой молча | medium | mitigate | Набор объявлен ИМЕНАМИ, а не числом: файл, переименованный на месте, при счёте числом прошёл бы молча. Контроль подставляет лишний файл и краснит правило |
| T-10-24 | Tampering | атрибут события рантайма разметки, введённый в шаблон | high | mitigate | Ноль вхождений утверждается импортом ЕДИНСТВЕННОГО выражения из модуля правил безопасности разметки; второго экземпляра выражения не заводится, поэтому расхождение двух копий невозможно по построению |
| T-10-25 | Repudiation | расхождение записей вехи и числа гейта | medium | mitigate | Кодовая половина несёт машинный гейт (число мест сходится тремя счётами); документная приводится в соответствие правкой с летописью, называющей источник, и её проверка объявлена ЧТЕНИЕМ честно, а не выдана за машинную (прецедент D-33 Фазы 5) |
| T-10-26 | Information Disclosure | правка каталога разведки задним числом | low | mitigate | Записи разведки не правятся ни на символ; отсутствие изменений проверяется диффом по каталогу в критериях приёмки |
| T-10-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не выполняет ни одной установки: ни новой зависимости, ни build-шага, ни вендоренного файла. Разведки у фазы нет по решению роадмапа — предмета аудита легитимности не существует |
</threat_model>

<verification>
- `uv run pytest tests/ -q` — зелено на закрытии фазы, за вычетом известного красного
  `full-suite-ads-editor-order-pollution` (порядок исполнения сюиты, к htmx отношения не имеет).
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- `graphify update .` — граф приведён в соответствие после всех правок фазы.
- Документная половина приведения «16 → 18» закрывается чтением при приёмке фазы и вынесена
  `<human-check>` задачи 2 и `verification: backstop` в `must_haves`.
</verification>

<success_criteria>
1. Второй счёт заведён, объявлен числом, утверждён разбором дерева, снабжён отрицательным
   контролем и связан с первым правилом согласованности, а не сведён с ним в одно.
2. Летопись второго счёта называет источник движения словами; критерии Фазы 11 не тронуты.
3. Критерий 3 роадмапа утверждается четырьмя числами; три из них читаются из единственных
   источников импортом, четвёртое объявлено именами файлов.
4. Прочтение критерия 3 (D-13) записано в шапке компонента рядом с правкой и называет имя
   утверждающего правила.
5. Роадмап и требования говорят 18 и несут одну и ту же летопись с поимённым источником
   расхождения; прогноз разведки назван устаревшим, а не ошибочным.
6. Формулировка критерия 3, строка пункта 4 ручного UAT и каталог разведки не тронуты.
7. Проверка документной половины объявлена чтением честно, а не выдана за машинный результат;
   и остающаяся ОТКРЫТОЙ дыра документного дрейфа названа в сводке вместе с кандидатом на её
   закрытие (Фаза 15 / GATE-09 либо запись бэклога).
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-04-SUMMARY.md` when done.

Сводка обязана:
- привести вывод покрасневшего правила, которым поставлено число второго счёта;
- назвать оба счёта и их РАЗНЫЕ предметы одной фразой каждый — чтобы следующая фаза не свела их
  в одно;
- привести дословно летопись «16 → 18» в том виде, в каком она попала в оба файла, и
  подтвердить, что формулировка в обоих местах ОДНА;
- перечислить незакрытые машинно пункты фазы (пункт 4 ручного UAT, приземление фокуса,
  поведение при отказе, фактическая смена личности, документная половина приведения записей) и
  передать их артефакту ручного обхода фазы;
- назвать ОТКРЫТОЙ дыру документного дрейфа: после фазы записи вехи не стережёт ни одно
  правило, кандидат на закрытие — сводный обход Фазы 15 (GATE-09) либо отдельная запись
  бэклога; дыра адресована, а не просто разрешена прецедентом D-33 Фазы 5.
</output>
