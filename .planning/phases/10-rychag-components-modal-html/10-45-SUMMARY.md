---
phase: 10-rychag-components-modal-html
plan: 45
subsystem: testing
status: complete
tags: [pytest, jinja2, css-parsing, javascript, mutation-testing, gate-hardening, WR-01, WR-04, WR-05, WR-07, IN-01, IN-02]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "гейт единственности источника заготовок в его суженной форме и её основание (план 10-35, `CR-01` седьмого круга)"
  - phase: 10-rychag-components-modal-html
    provides: "форма параметра «читатель приходит входом, а не читается с диска» и её основание (`_template_reader_with`, `_lever_clearing_findings`, планы 10-19 и 10-41)"
  - phase: 10-rychag-components-modal-html
    provides: "`LEVER_PARSE_FAILURE_PREFIX`, `_JS_LINE_COMMENT_RE` и разбор тела метода открытия (план 10-41)"
  - phase: 10-rychag-components-modal-html
    provides: "посимвольный разбор селекторов и канон ловушек предков — тот же модуль, переписанный этой же волной (план 10-42)"
  - phase: 10-rychag-components-modal-html
    provides: "зафиксированные правки `app/templates/components/modal.html` и `app/pages/schedules.py`, без которых гейт пустого диффа по `app` краснел бы на работе соседа (план 10-46, объявленное ребро 10-45→10-46)"
provides:
  - "`_declares_banner_id` и `_names_banner_literally` — обе половины гейта единственности источника перестали зависеть от рода кавычек и от их наличия"
  - "`_banner_markup_owners` / `_banner_script_owners` / `_failure_banner_template_sources` — отбор владельцев принимает КАРТУ шаблонов, и контроль зубов стал выразим без правки дерева"
  - "`TEMPLATE_MACRO_IMPORT_AS_RE` — вторая форма подключения макроса стала видимым ребром графа достижимости"
  - "`_auth_shell_heirs(read=, names=)` и `_template_tree_names()` — состав наследников подменяем, и сценарий «восьмой экран входа» показан исполнением"
  - "`test_the_lever_path_lives_under_the_templates_root` — утверждение уровня модуля переехало в именованное правило с текстом починки; голых `assert` уровня модуля в файле не осталось"
  - "`_css_rule_block(raw)` и разделение «якорь по куску, объявление по блоку» — сличение перестало держаться одним символом точки с запятой"
  - "счёт вложенности тела метода открытия знает строковые литералы JS (три рода кавычек, экранирование)"
  - "четыре новых контроля: квотонезависимость, вторая форма ребра, новый наследник, фигурная скобка внутри литерала JS"
affects: [10-46, 10-47, 10-UAT, 10-VERIFICATION, следующий круг закрытия гэпов Фазы 10]

actuals:
  tokens: 10232
  tasks: 3
  commits: 6
  plan_head_before: 72feb108f7b24da0d766b842db50f448e8509d6d
  # ⚠️ ЧИСЛА ЗАМЕРЕНЫ, А НЕ ПЕРЕСКАЗАНЫ. `commits` — `git rev-list --count
  # 72feb108..HEAD` на момент записи сводки; замыкающие коммиты метаданных идут
  # ПОСЛЕ замера и в это число не попали (тем же инструментом `/gsd-verify-work`
  # насчитает 7 или 8). `tokens` — `git diff 72feb108..HEAD -- tests | wc -m`
  # (40929) делённое на 4, то есть та же шкала chars/4, на которой стои́т
  # `estimate.tokens: 90000`. Перелёт оценки в 8.8 раза НЕ ПОДРАВНИВАЕТСЯ:
  # проекция была `confidence: low` при нуле образцов калибровки.

tech-stack:
  added: []
  patterns:
    - "предикат собирается ВОКРУГ константы имени и знает ВСЕ законные формы записи: род кавычек есть часть НАБОРА, а не часть предмета"
    - "отбор выносится из тела правила в функцию, принимающую карту источников: контроль зубов подаёт СВОЮ карту вместо правки живого дерева"
    - "две формы одного рода ребра — ДВА образца, а не один с чередованием: один образец на обе был бы шире каждой и завёл бы в граф звенья, которых рантайм не зовёт"
    - "латентность находки предъявляется ЧИСЛОМ (вхождений второй формы — 0), а не обещанием «сегодня их нет»"
    - "утверждение уровня модуля есть НЕ-проверка в обоих исходах: роняет сбор безымянно либо исчезает под `python -O`. Место такого утверждения — именованное правило"
    - "«якорь по куску, объявление по блоку»: подстановке нужна однозначность в исходнике, утверждению — предмет, а не проза о нём"
    - "счёт вложенности со состоянием строкового литерала; граница шаблонной подстановки называется сразу, а не оставляется следующему кругу"

key-files:
  created: []
  modified:
    - "tests/test_pages/test_shell.py"

key-decisions:
  - "Отбор ОБЕИХ половин гейта единственности вынесен в `_banner_markup_owners`/`_banner_script_owners` В КОММИТЕ RED, с СЕГОДНЯШНЕЙ квотозависимой семантикой предикатов. Иначе контроль был бы невыразим: доктóрить пришлось бы либо живое дерево (второй обработчик в продукте ради зелени контроля), либо ничего. Разделение на два коммита сохраняет чистую красноту: RED падает УТВЕРЖДЕНИЕМ на числах, а не именем несуществующей функции"
  - "Разметочная половина спрашивает ОБЕ заготовки (`any` по `FAILURE_BANNER_IDS`), а прежняя — только первую. Сужение до первой было экономией без основания: вторая заготовка объявляется тем же файлом и той же формой, и второй источник, объявивший ТОЛЬКО её, проходил бы разметочную половину и после закрытия `WR-01`"
  - "Обратная кавычка внесена в `_JS_STRING_QUOTES` наравне с двумя другими: шаблонный литерал ЕСТЬ строковый литерал, и `getElementById(`htmx-…`)` вешает обработчик так же. Граница, которую это НЕ закрывает (подстановка `${…}` внутри шаблонного литерала), названа в докстринге в тот же час, а не оставлена следующему кругу"
  - "`TEMPLATE_MACRO_IMPORT_AS_RE` — ОТДЕЛЬНЫЙ образец, а не расширение действующего чередованием. Ревизия предлагала слить их в один с двумя группами захвата; слитая форма совпала бы с формами, которых Jinja не знает, и разбор двух групп на каждом совпадении сделал бы перечень образцов неоднородным (у трёх других — одна группа)"
  - "RED контроля нового наследника — `NameError`/`TypeError` (способности подать состав имён НЕТ), и это записано честно вместо подгонки под «красное утверждение». Поведенческая половина той же дыры снята ОТДЕЛЬНЫМ замером до правки: подмена ТОЛЬКО читателя даёт НОЛЬ находок на восьмом экране — ровно то, что `WR-07` предъявил прозой"
  - "`_css_rule_block` заведён в коммите RED тождественной функцией (кусок как есть) — это ДОСЛОВНО сегодняшняя семантика всех потребителей куска, а не заглушка. Та же идиома, что у предикатов задачи 1: сперва назвать сегодняшнее поведение именем, потом править названное"
  - "`_stylesheet_with_extra_declaration` НЕ переведён на блок и остаётся на куске: он ЯКОРЬ подстановки, а не счётчик объявлений, и разделение записано в докстринге `_css_rule_block` прямо. Перевод десяти его вызовов был бы правкой вне предмета находки"
  - "Требование FORM-06 НЕ отмечено выполненным: вердикт фазы `gaps_found`, и отметка была бы записью шире дерева"

patterns-established:
  - "RED достигается ПЕРЕИМЕНОВАНИЕМ сегодняшнего поведения, а не заглушкой: функция заводится с той семантикой, что есть, контроль краснеет на её числах, и правка идёт вторым коммитом. Даёт красноту-утверждение там, где иначе была бы краснота-`NameError`"
  - "Два рода RED различаются в записи: «красное утверждение» (числа не те) и «красное отсутствие способности» (подать нечего). Второй помечается и дополняется ОТДЕЛЬНЫМ поведенческим замером, чтобы не выдавать отсутствие сигнатуры за поведение"
  - "Пересъёмка унаследованного числа даёт ТРИ исхода, и все три называются: подтверждено (246/278/32), опровергнуто с новым значением (161 → 217 правил), не воспроизведено ни в одну сторону (277)"

requirements-completed: []
# ⚠️ ПУСТО НАРОЧНО. `requirements: [FORM-06]` стои́т во frontmatter плана, но
# вердикт фазы — `gaps_found`, и `10-VERIFICATION.md` настоящим планом не
# правится (запрет плана, `verification: test`). Та же форма, что у сводок
# 10-42 и 10-46.

coverage:
  - id: D1
    description: "Обе половины гейта единственности источника заготовок не зависят от рода кавычек и от их наличия; три формы второго источника краснят гейт"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_a_second_script_with_the_other_quote_reddens_the_single_source_gate"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_failure_banner_has_single_source"
        status: pass
    human_judgment: false
  - id: D2
    description: "Граф достижимости рычага знает обе формы подключения макроса; вторая форма показана на доктóренной копии наследника"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_an_auth_template_that_imports_the_lever_with_an_alias_reddens"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_no_auth_shell_template_reaches_the_modal_lever"
        status: pass
    human_judgment: false
  - id: D3
    description: "Состав наследников корня шелла авторизации подменяем параметром, и сценарий «восьмой экран входа, добавленный без правки суиты» показан исполнением"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_a_new_auth_heir_that_reaches_the_lever_reddens"
        status: pass
    human_judgment: false
  - id: D4
    description: "Утверждение уровня модуля о пути рычага переехало в именованное правило с текстом починки; сбор файла безымянно не падает"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_the_lever_path_lives_under_the_templates_root"
        status: pass
      - kind: integration
        ref: "uv run pytest tests/test_pages/test_shell.py -q (223 passed, сбор цел)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Третий элемент тройки разборщика таблицы назван тем, что он есть; счёт объявлений ведётся по блоку и не зависит от точки с запятой"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_the_parsed_block_of_the_elevation_rule_excludes_the_prose_before_it"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_a_banner_lift_block_without_a_fixed_position_reddens"
        status: pass
    human_judgment: false
  - id: D6
    description: "Счёт вложенности тела метода открытия знает строковые литералы JS; обе стороны утверждены — скобка в литерале глубины не двигает, действительно незакрытая поднимает отказ"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_shell.py#test_control_a_brace_inside_a_js_string_does_not_move_the_depth"
        status: pass
    human_judgment: false

duration: 36 min
completed: 2026-09-10
---

# Phase 10 Plan 45: Шесть находок восьмого круга в модуле шелла Summary

**Шесть правил модуля шелла, каждое из которых видело у́же собственного предмета, приведены к предмету: род кавычек перестал решать, кто владеет заготовками; вторая форма подключения макроса стала видимым ребром графа; состав наследников стал входом, и «восьмой экран входа» показан исполнением; утверждение уровня модуля переехало в именованное правило; счёт объявлений перестал держаться одним символом `;`, а счёт вложенности узнал строковые литералы JS.**

## Performance

- **Duration:** 36 min
- **Started:** 2026-09-10T18:33Z (первый прогон RED)
- **Completed:** 2026-09-10T19:09Z
- **Tasks:** 3 из 3
- **Files modified:** 1 (`tests/test_pages/test_shell.py`, +621 / −40)

## Accomplishments

- **`REVIEW-8/WR-01` закрыт.** Обе половины гейта единственности источника заготовок стали квотонезависимыми. Разметочная (`_declares_banner_id`) знает три законные формы признака — двойные кавычки, одинарные и БЕЗ кавычек; сценарная (`_names_banner_literally`) знает три рода строкового литерала JS, включая шаблонный. Конъюнкция сценарной половины с регистрацией обработчика НЕ ослаблена — это её предмет, и она есть единственное, чем читатель заготовок отличим от их источника.
- **`REVIEW-8/WR-04` закрыт.** `TEMPLATE_MACRO_IMPORT_AS_RE` внесён в перечень образцов рёбер ТРЕТЬИМ ребром подключения. Форма `{% import "…" as … %}` приводит рычаг в шелл ровно так же, как форма `{% from … import … %}`, и правилом больше не пропускается.
- **`REVIEW-8/WR-07` закрыт.** `_auth_shell_heirs` принимает читателя и состав имён дерева параметрами с сегодняшними умолчаниями; `_auth_shell_lever_findings` пробрасывает оба в ОБА сборщика. Сценарий, которым докстринг оправдывал чтение дерева, перестал быть обещанием.
- **`REVIEW-8/IN-01` закрыт.** Голое утверждение уровня модуля о пути рычага заменено на `test_the_lever_path_lives_under_the_templates_root` с текстом отказа, называющим обе величины и две починки. Голых `assert` уровня модуля в файле не осталось ни одного (проверено `grep -n "^assert "`).
- **`REVIEW-8/WR-05` закрыт.** `_css_rule_block(raw)` отсекает прозу, попавшую в кусок по построению разборщика; счёт объявлений ведётся ПО БЛОКУ и по разобранным объявлениям тела, подстановка — по блоку же. Точка с запятой перестала быть частью сличаемого.
- **`REVIEW-8/IN-02` (половина о фигурных скобках) закрыт.** Счёт вложенности тела метода открытия ведёт состояние строкового литерала. Обе стороны утверждены: скобка внутри литерала глубины не двигает, действительно незакрытая по-прежнему поднимает отказ «тело не замкнуто».
- **Ни одной правки продукта и разметки.** `git diff --numstat -- app` пуст на каждом из трёх шагов проверки и на плановой верификации.

## Task Commits

1. **Задача 1: единственность источника перестала зависеть от рода кавычек**
   - RED — `490491f` (`test`)
   - GREEN — `f06ae25` (`fix`)
2. **Задача 2: обе формы подключения, состав наследников входом, утверждение уровня модуля переехало**
   - RED — `381b632` (`test`)
   - GREEN — `1192e0d` (`fix`)
3. **Задача 3: два разборщика называются тем, что они есть**
   - RED — `de5255d` (`test`)
   - GREEN — `9b4937b` (`fix`)

## TDD Gate Compliance

| Задача | RED | GREEN | REFACTOR | Вердикт |
|--------|-----|-------|----------|---------|
| 1 | ✓ `490491f` | ✓ `f06ae25` | — | Pass (с оговоркой ниже) |
| 2 | ✓ `381b632` | ✓ `1192e0d` | — | Pass (с оговоркой ниже) |
| 3 | ✓ `de5255d` | ✓ `9b4937b` | — | Pass (с оговоркой ниже) |

⚠️ **ОГОВОРКА ОДНА НА ВСЕ ТРИ, И ОНА НАЗЫВАЕТСЯ ЗДЕСЬ, А НЕ ЗАМАЛЧИВАЕТСЯ.** Гейт
`gsd-core/references/tdd.md` ищет GREEN-коммит образцом `^feat\(10-45\):`, а все три
GREEN-коммита набраны типом `fix`. Прогон гейта:

```
git log --oneline -E --grep="^test\(10-45\):"      -> 3 коммита
git log --oneline -E --grep="^feat\(10-45\):"      -> НИ ОДНОГО
git log --oneline -E --grep="^fix\(10-45\):"       -> 3 коммита
git log --oneline -E --grep="^refactor\(10-45\):"  -> НИ ОДНОГО
```

Тип выбран по таблице типов коммитов протокола исполнения: план не вносит нового
поведения, а чинит шесть ПРАВИЛ, видевших у́же своего предмета, — то есть `fix`
(«Bug fix, error correction»). `feat` описал бы работу неверно. REFACTOR-коммитов нет,
и это законно: гейт объявляет его необязательным, а чистить после GREEN было нечего.

⚠️ **ВТОРАЯ ОГОВОРКА — О ФОРМЕ СВИДЕТЕЛЬСТВА RED.** `gsd_run check tdd-red-evidence`
разбирает TAP (node:test) и вывода pytest не читает: на любом, даже подлинно красном
прогоне он отдаёт `INVALID_RED (zero_tests_discovered)`. Это известное свойство среды,
записанное планами 10-41…10-46. Свидетельство RED снято в форме, которую предписывает
сам план (команда, код возврата, имя правила, ожидаемое и полученное) и приведено ниже
поимённо. Синтетический TAP не подделывался, пакеты не ставились.

## Свидетельства RED — поимённо

**Задача 1** — `uv run pytest tests/test_pages/test_shell.py -q -k "single_source or notice_area or failure_banner or second_script"`, код возврата **1**, `1 failed, 15 passed`:

```
FAILED test_control_a_second_script_with_the_other_quote_reddens_the_single_source_gate
AssertionError: СЦЕНАРНАЯ ПОЛОВИНА ГЕЙТА ПРОПУСТИЛА ВТОРОЙ ОБРАБОТЧИК, обратившийся
к заготовке #htmx-failure-network ДВОЙНЫМИ кавычками: ['includes/htmx_error_banner.html']
ожидалось: {'includes/htmx_error_banner.html', 'auth/second_script.html'}
```

Все ТРИ случая сняты отдельным прогоном ДО правки (второй источник невидим ни одной формой):

| Случай | До правки | После правки |
|--------|-----------|--------------|
| сценарий, двойные кавычки | `['includes/htmx_error_banner.html']` — второй источник ПРОПУЩЕН | обе записи |
| разметка, одинарные кавычки | `['includes/htmx_error_banner.html']` — ПРОПУЩЕН | обе записи |
| разметка, без кавычек | `['includes/htmx_error_banner.html']` — ПРОПУЩЕН | обе записи |

**Задача 2** — `uv run pytest … -k "auth_shell or reaches_the_modal_lever or imports_the_lever or new_auth_heir or lever_path_lives"`, код возврата **1**, `2 failed, 8 passed`:

```
FAILED test_control_an_auth_template_that_imports_the_lever_with_an_alias_reddens
AssertionError: ПРАВИЛО НЕ ЗАМЕТИЛО ПОДКЛЮЧЕНИЕ РЫЧАГА `components/modal.html` ФОРМОЙ
С ПЕРЕИМЕНОВАНИЕМ МОДУЛЯ В НАСЛЕДНИКЕ `auth/forgot_password.html`: находок 0 — ()
ожидалось: 1

FAILED test_control_a_new_auth_heir_that_reaches_the_lever_reddens
NameError: name '_template_tree_names' is not defined
```

Второй отказ есть КРАСНОТА ОТСУТСТВИЯ СПОСОБНОСТИ, а не красное утверждение, и потому
дополнен ПОВЕДЕНЧЕСКИМ замером той же дыры, снятым до правки:

```
ALIAS      (форма import-as, подменён читатель)          -> находок 0   # дыра WR-04
FROM       (форма from-import, подменён читатель)        -> находок 1   # контроль жив
NEW HEIR   (восьмой экран, подменён ТОЛЬКО читатель)     -> находок 0   # дыра WR-07
NEW HEIR   (подан состав имён)                           -> TypeError: _auth_shell_heirs()
                                                            got an unexpected keyword argument 'names'
```

**Задача 3** — `uv run pytest … -k "elevation or css_rule_block or brace_inside_a_js_string or excludes_the_prose or lever_clears or keeps_a_stale_banner"`, код возврата **1**, `2 failed, 3 passed`:

```
FAILED test_the_parsed_block_of_the_elevation_rule_excludes_the_prose_before_it
AssertionError: БЛОК НАЧИНАЕТСЯ КОММЕНТАРИЕМ, А НЕ ПРЕАМБУЛОЙ — проза, стоящая ПЕРЕД
блоком, вошла в него (длина куска 10465, длина блока 10465)

FAILED test_control_a_brace_inside_a_js_string_does_not_move_the_depth
AssertionError: разбор перешагнул закрывающую скобку тела и вобрал соседний метод:
" var brace = '{'; this.open = true; },\n  hide() { this.open = false; }\n"
```

⚠️ Второй отказ ПРЕВЗОШЁЛ ожидание плана и записывается как найденный, а не как
ожидавшийся: план ждал отказа «тело не замкнуто», а разбор вернул КУСОК С ЧУЖИМ
МЕТОДОМ ВНУТРИ — то есть кусок, границы которого никто не знает. Это ровно тот исход,
который докстринг `_lever_show_body` называл худшим из двух, и он воспроизведён.

## Замеры — все, что план требовал назвать

| Величина | Замер 2026-09-10 (HEAD) | Прежнее значение |
|----------|------------------------|------------------|
| Владельцев заготовок по разметочной половине | 1 — `includes/htmx_error_banner.html` | то же |
| Владельцев по сценарной половине | 1 — `includes/htmx_error_banner.html` | то же |
| Рычаг `components/modal.html` во множестве владельцев | **НЕТ** (замер прогоном, не вывод) | нет |
| Шаблонов в дереве `app/templates/` | 91 | 91 |
| Вхождений формы `{% import … as … %}` | **0** (сырых `{% import` — тоже 0) | «предположено, не измерено» (`WR-04`) |
| Вхождений формы `{% from … import … %}` | 246 (сырых 278, в прозе 32) | 246 / 278 / 32 — **ПОДТВЕРЖДЕНЫ** |
| Наследников корня `auth_base.html` | 7 | 7 |
| Звеньев графа корня шелла авторизации | 5 | 5 |
| Находок достижимости на нетронутом дереве | 0 | 0 |
| Длина дословного куска блока подъёма | 10465 | 7487 (план) — **устарело, не ошибка** |
| Длина БЛОКА после отсечения прозы | 330 | — |
| Счёт `position: fixed` по КУСКУ | 2 | 2 |
| Счёт `position: fixed;` по КУСКУ | 1 (держалось одним символом) | 1 |
| Счёт `position: fixed` по БЛОКУ | **1** — без точки с запятой | — |
| Длина тела метода `show()` | 397 символов | — |
| Обратных кавычек в теле метода | **0** | — |
| Двойных кавычек в теле метода | 0 | — |
| Одинарных кавычек в теле метода | 10 (ни одна не обнимает фигурной скобки) | — |
| Правил в `tests/test_pages/test_shell.py` | **223** (до плана — 217) | 161 (план), 168 (`10-VERIFICATION.md`) |
| Строк в модуле | 6304 (до плана — 5982) | 4793 (план) |

### Три исхода пересъёмки — все названы, ни один не подогнан

1. **ПОДТВЕРЖДЕНО.** Числа абзаца о подключениях (246 / 278 / 32) переснятые совпали
   с записанными. Пометка «устарело» к ним НЕ ставится: они верны на HEAD.
2. **ОПРОВЕРГНУТО С НОВЫМ ЗНАЧЕНИЕМ.** Число правил модуля: план называл **161**,
   `10-VERIFICATION.md` — **168** (счёт ДВУХ модулей вместе). На HEAD **до** настоящего
   плана — **217**, после — **223**. Число 161 было верно для дерева ДО плана 10-42,
   переписавшего этот модуль той же волной (+1054 / −136 строк); ошибкой оно не было и
   здесь не вычёркивается. То же с числом строк: 4793 → 5982 → 6304.
3. **НЕ ВОСПРОИЗВЕДЕНО НИ В ОДНУ СТОРОНУ.** Число **277**, названное `<objective>` плана
   как «вхождений первой формы», не совпало ни с одним из двух замеров: по
   де-комментированному исходнику 246, по сырому тексту 278. Команды замера приведены
   выше; чему принадлежит 277 — не установлено, и подгонять его под 278 «опиской в
   единицу» было бы ровно тем сочинением, против которого стои́т эта таблица.

## Files Created/Modified

- `tests/test_pages/test_shell.py` — единственный тронутый файл (+621 / −40). Шесть
  находок восьмого круга, все в модуле суиты.

## Decisions Made

Полный перечень — во frontmatter (`key-decisions`). Несущие:

- **Отбор владельцев вынесен в функции в коммите RED, с сегодняшней семантикой.**
  Иначе контроль зубов был бы невыразим, а RED пришёлся бы на `NameError` вместо
  утверждения на числах.
- **Разметочная половина расширена с ОДНОЙ заготовки до ОБЕИХ.** Прежнее сужение до
  `FAILURE_BANNER_IDS[0]` было экономией без основания: второй источник, объявивший
  только вторую заготовку, проходил бы её и после закрытия `WR-01`.
- **Два образца ребра, а не один с чередованием.** Починка, предложенная ревизией
  (один образец с двумя группами захвата), отвергнута с основанием: она была бы шире
  каждой из форм и сделала бы перечень образцов неоднородным.
- **`_stylesheet_with_extra_declaration` оставлен на куске.** Он ЯКОРЬ подстановки, а
  не счётчик объявлений; разделение записано в докстринге `_css_rule_block`.

## Deviations from Plan

**None — план исполнен ровно так, как написан.** Ни одна правка не вышла за
`files_modified: [tests/test_pages/test_shell.py]`, ни одного пакета не поставлено, ни
одного файла продукта или разметки не тронуто.

Две правки внутри задач, сделанные ДО коммита и потому не являющиеся отступлениями,
названы здесь для полноты: (1) сличение преамбулы блока с первым элементом тройки
потребовало ТОГО ЖЕ сжатия пробелов, каким её приводит разбор — в блоке она стои́т
дословно, с переводом строки; (2) первая редакция синтетики «действительно незакрытой
скобки» оказалась ЗАМКНУТОЙ (закрывающая скобка значения атрибута `x-data` балансировала
счёт), и синтетика переписана так, чтобы незакрытость была настоящей.

## Частичное закрытие чужой записи

⚠️ **Раздел «План 10-39» файла `deferred-items.md`, подраздел `IN-03` о разборщике
таблицы стилей, настоящим планом закрыт НЕ ПОЛНОСТЬЮ, и это надо назвать.** Закрыта
находка о ТРЕТЬЕМ ЭЛЕМЕНТЕ ТРОЙКИ (`REVIEW-8/WR-05`). Две другие границы того же
разборщика — уплощение группирующих правил и раскрытие всего, чья преамбула начинается
со знака правила (отчего `@font-face` исчезает, а кадры `@keyframes` встают в перечень
как правила) — **НЕ закрыты и остаются за своим владельцем**. Обе названы в докстринге
`_css_rules` прямым текстом, чтобы читатель не прочёл закрытым весь разборщик. Сам файл
`deferred-items.md` настоящим планом НЕ правится: он не входит в `files_modified`, и
владельцем ЗАПИСИ назначен план 10-46.

⚠️ **Пометка `IN-03` седьмого круга в том же файле есть НАЗНАЧЕНИЕ ВЛАДЕЛЬЦА (план
10-45), а не вердикт закрытия** — так её и записал план 10-46, потому что сводки
настоящего плана на дереве тогда не было. Настоящая сводка есть основание для
следующего круга перевести ту пометку в вердикт; сама она себе вердикта не ставит.

## Issues Encountered

**None.** Прогоны длинные (модуль целиком — ~11.5 мин), но зелёные; отказов среды не
было.

⚠️ Известное свойство среды, подтверждённое и настоящим планом: `gsd_run check
tdd-red-evidence` не читает вывода pytest и отдаёт `INVALID_RED
(zero_tests_discovered)` на подлинно красном прогоне. Свидетельства RED сняты в форме,
предписанной планом, и приведены выше поимённо.

## Шаг трекинга — два найденных свойства инструментов

⚠️ **`roadmap update-plan-progress` поставил отметку 47/47, и правило прозы плана 10-46
покраснело ТРЕТИЙ раз — впервые в ЧУЖОМ плане.** Отказ дословно: «проза строки
`**Plans**` говорит 46 (исполнено планов), а ОТМЕТОК перечня — 47; привести надо ПРОЗУ».
Проза приведена, прежнее «46» помечено устаревшим с названным источником расхождения и
не вычеркнуто (идиома D-30/D-32). Значение этого срабатывания в том, что правило
заведено ОДНИМ планом, а сработало у ДРУГОГО, то есть перестало быть свойством своего
автора.

⚠️ **`state advance-plan` ПРАВИТ СТРОКУ ВНУТРИ ЧУЖОЙ ДОСЛОВНОЙ ЦИТАТЫ — найдено здесь,
починено здесь, записано здесь.** Команда меняет поле `Status` раздела «Current
Position», но замену ведёт ПО ТЕКСТУ: в `.planning/STATE.md` есть абзац о записях
`deferred_items`, дословно цитирующий чужое поле `- **Status:** Ready to execute`, и
эта цитата была переписана на `- **Status:** Phase complete — ready for verification`,
то есть запись о ЧУЖОМ файле стала говорить неправду. Правка отката сделана в тот же
час и побайтово: цитата восстановлена дословно, настоящее поле `Status` осталось
приведённым. ⚠️ САМА КОМАНДА НЕ ПРАВИТСЯ настоящим планом — она вне `files_modified`, и
это работа не суиты, а инструмента; запись оставлена следующему кругу как найденное
свойство, а не как починка.

⚠️ Сверх того `advance-plan` не тронул строк «Completed Plans in Phase» и «Last
activity» и оставил в «Current Position» прозу «неисполненным остаётся ОДИН — 10-45»,
уже неверную к моменту её чтения. Обе строки приведены рукой.

## Verification Results

| # | Команда плановой верификации | Результат |
|---|------------------------------|-----------|
| 1 | `uv run pytest tests/test_pages/test_shell.py -q` | **223 passed** — зелено, сбор файла не падает |
| 2 | `uv run pytest tests/test_templates/test_components.py -q` | зелено (в общем прогоне двух модулей — **311 passed**) |
| 3 | `uv run pytest tests/test_planning/ -q` | **38 passed** |
| 4 | `uv run python -m compileall -q app main.py tests` | код возврата **0** |
| 5 | `git diff --numstat -- app` | **пусто** |
| 6 | Числа замеров названы в сводке | таблица «Замеры» выше — 18 строк |

Гейты задач, дословно:

- Задача 1: `-k "single_source or notice_area or failure_banner"` → **16 passed** (до
  правки 15; ровно плюс один новый контроль), `git diff --numstat -- app` пуст.
- Задача 2: `-k "auth_shell or reaches_the_modal_lever or imports_the_lever or
  new_auth_heir or lever_path_lives"` → **11 passed** (при пороге «не меньше пяти»);
  прогон модуля целиком — **221 passed** на момент задачи, строки `error … collect` в
  выводе нет.
- Задача 3: `-k "elevation or css_rule_block or brace_inside_a_js_string or lever_clears
  or keeps_a_stale_banner"` → **5 passed** (до правки 3; плюс два новых контроля);
  прогон двух модулей — **311 passed**.

## Known Stubs

**Ни одного.** Заготовок, `TODO`, `FIXME`, пропущенных правил (`skip`/`xfail`) и
непрогнанных команд `<verify>` план не оставил. Все шесть находок закрыты исполнением,
и у каждой есть контроль, краснеющий на форме, которую правило принимало до правки.

## Next Steps

Настоящий план — ПОСЛЕДНИЙ восьмой партии закрытия гэпов Фазы 10 и последний план фазы
по составу (47 из 47). Следующий вход — `/gsd-verify-work 10`: вердикт фазы остаётся
`gaps_found`, и требование `FORM-06` настоящим планом НЕ отмечено выполненным.

## Self-Check: PASSED

- `tests/test_pages/test_shell.py` — FOUND (единственный изменённый файл).
- Коммиты найдены в истории: `490491f`, `f06ae25`, `381b632`, `1192e0d`, `de5255d`,
  `9b4937b` — все шесть.
- Все `<acceptance_criteria>` трёх задач переспрошены прогоном (таблица выше).
- Все шесть команд `<verification>` плана исполнены; результаты приведены.
