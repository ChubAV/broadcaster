---
phase: 10-rychag-components-modal-html
plan: 28
type: execute
wave: 2
# ⚠️ ВОЛНА 2 — СЛЕДСТВИЕ ОБЩЕГО ФАЙЛА, А НЕ ОСТОРОЖНОСТИ. План 10-24 заводит нейтральный модуль
# границы и правит `app/pages/ads.py` и `tests/test_pages/test_htmx_gates.py`; настоящий план
# правит те же два файла. Волна: 1 + 1 = 2.
depends_on: ["10-24"]
gap_closure: true
files_modified:
  - app/pages/account_groups.py
  - app/pages/accounts.py
  - app/pages/ads.py
  - app/pages/history.py
  - tests/test_pages/test_identifier_bounds.py
  - tests/test_pages/test_htmx_gates.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (поля во frontmatter не заводятся, форма унаследована от
# планов 10-12…10-27): `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  # `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < min_samples 3), а не назначена автором.
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ВСЕ ЧЕТЫРЕ МАРШРУТА ПОДТВЕРЖДЁННОГО УДАЛЕНИЯ СЕМЬИ, СОБРАННОЙ ПЛАНОМ 10-18, ОТВЕЧАЮТ ОДИНАКОВО НА ВЕЛИЧИНУ ВНЕ ДИАПАЗОНА КОЛОНКИ: удаление аккаунта, объявления, группы аккаунта и расписания дают отказ валидации, а не отказ обработчика — семья перестала быть закрытой для одного своего члена из четырёх"
    - "ТУМБЛЕР ГРУППЫ АККАУНТА И ПРАВКА ОБЪЯВЛЕНИЯ, НАЗВАННЫЕ РЕВИЗИЕЙ ПОИМЁННО, ОТВЕЧАЮТ ТЕМ ЖЕ ОТКАЗОМ ВАЛИДАЦИИ: они не входят в семью удаления, но несут идентификатор ТОГО ЖЕ КЛАССА и падали тем же способом"
    - "ПОВТОР ОТПРАВКИ ИЗ ЖУРНАЛА ОТВЕЧАЕТ ОТКАЗОМ ВАЛИДАЦИИ НА НЕГОДНОЙ ВЕЛИЧИНЕ: шестой воспроизведённый ревизией маршрут закрыт вместе с остальными"
    - "ИДЕНТИФИКАТОРЫ, ПРИЕЗЖАЮЩИЕ НЕ АДРЕСОМ, А ПАРАМЕТРОМ ЗАПРОСА, ЗАКРЫТЫ ТОЙ ЖЕ ГРАНИЦЕЙ: курсор постраничного вывода групп и признак раскрытого расписания редактора уезжают операндом сравнения SQL ровно так же, как идентификатор пути, и разведение их по способу передачи было бы разведением по внешнему признаку"
    - "ГРАНИЦА СМЕЖНОСТИ ПОКАЗАНА НА КАЖДОМ ЗАКРЫТОМ ВХОДЕ, А НЕ НА ОДНОМ ОБРАЗЦЕ: отображение «вход → снятый код» сличается ЦЕЛИКОМ, и текст отказа называет ВСЕ несогласные строки"
    - "ЖИВЫЕ ВЕЛИЧИНЫ ПО-ПРЕЖНЕМУ РАБОТАЮТ НА КАЖДОМ ЗАКРЫТОМ ВХОДЕ: антивакуум прогнан по всему перечню, а не по одной строке — правило, отвергающее ВСЁ, дало бы тот же зелёный"
    - "РЕЕСТР РАСХОЖДЕНИЯ ВЫРОС РОВНО НА ЧИСЛО НОВЫХ ОГРАНИЧЕННЫХ ВХОДОВ POST-ОБРАБОТЧИКОВ, И РОСТ СНЯТ ПРОГОНОМ ЗАМЕРА, А НЕ СЛОЖЕН В УМЕ"
  artifacts:
    - "app/pages/account_groups.py — семь идентификаторов пути через общий ограниченный псевдоним и курсор постраничного вывода с верхней границей"
    - "app/pages/accounts.py — четыре идентификатора пути через общий ограниченный псевдоним"
    - "app/pages/ads.py — два идентификатора пути (правка и редактор) и признак раскрытого расписания с верхней границей"
    - "app/pages/history.py — два идентификатора пути через общий ограниченный псевдоним"
    - "tests/test_pages/test_identifier_bounds.py — матрица «вход → снятый код» по всем закрытым входам с антивакуумом на живых величинах"
    - "tests/test_pages/test_htmx_gates.py — записи реестра расхождения для новых ограниченных входов POST-обработчиков, объявленное число сдвинуто с летописью"
  key_links:
    - "матрица входов → каждый закрытый вход: единственное, чем «закрыто на образце» не прочитается как «закрыто на всех»; ломается — повторяется ровно `CR-02` третьего круга, где граница стояла там, куда смотрели, и молчала там, куда не смотрели"
    - "антивакуум на живых величинах → граница: единственное, чем правило не окажется отвергающим всё; ломается — зелёный прогон означает сломанный продукт, а не поставленную границу"
    - "реестр расхождения → правило полноты: единственное, чем девять новых ограниченных входов не станут молчаливым умолчанием; ломается — прогон краснеет с именами входов, и его чинят правкой числа вместо принятия решения"
  prohibitions:
    - statement: "тела обработчиков НЕ ПРАВЯТСЯ: предмет плана — сигнатуры, и правка тела ради зелени превратила бы границу приложения во внутреннюю проверку, то есть вернула бы решение туда, откуда его забирают"
      status: flagged-unverified
      verification: none
    - statement: "величины постраничного вывода (смещение и размер порции) НЕ ОГРАНИЧИВАЮТСЯ границей ИДЕНТИФИКАТОРА: они не есть идентификатор, отказ по ним ничего не говорит о владении строкой, и записанное основание соседнего гейта это уже называет"
      status: flagged-unverified
      verification: none
    - statement: "решение о законности расхождения формы отказа валидации с запертым D-01 НЕ ЧЕКАНИТСЯ планом: состояние всех новых записей реестра остаётся ждущим владельца, прецедент — окно 51"
      status: flagged-unverified
      verification: none
    - statement: "исполненные планы `10-01`…`10-23` и их сводки НЕ ПРАВЯТСЯ и не перенумеровываются"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ — ПОЛНАЯ ЛЕТОПИСЬ ПАРТИИ СТОИТ В `10-24-PLAN.md`. Настоящий план
  # расширяет рёбра `adjacency` и `empty` с ОДНОГО входа на семнадцать, не меняя их состояния:
  # оба остаются `resolved / explicit` с авторством ПЯТОЙ партии.
---

<objective>
Граница величины идентификатора, доказанная планом 10-24 на одном маршруте, встаёт на все
идентификаторы четырёх продуктовых страничных модулей.

Предмет — `CR-01` пятого круга ревизии в его полном объёме на продуктовой стороне: ревизор своим
клиентом получил отказ обработчика на удалении объявления, удалении аккаунта, удалении группы
аккаунта, тумблере группы, правке объявления и повторе отправки. Пять из шести живут в этих четырёх
модулях; шестой закрыт планом 10-24.

Purpose: план 10-18 собрал ЧЕТЫРЕ маршрута подтверждённого удаления в одну семью и поставил на них
общий гард. План 10-12 ограничил ОДНОГО члена этой семьи и оставил трёх. Ревизия показала, что
основание, по которому остальные ждали (у них нет воспроизведённого отказа), больше не действует:
отказ воспроизведён на каждом. Настоящий план закрывает продуктовую половину.

⚠️ **ЗАОДНО ЗАКРЫВАЮТСЯ ИДЕНТИФИКАТОРЫ, ПРИЕЗЖАЮЩИЕ ПАРАМЕТРОМ ЗАПРОСА, И ЭТО НЕ РАСШИРЕНИЕ ОБЪЁМА,
А ЕГО ЧЕСТНАЯ ГРАНИЦА.** Курсор постраничного вывода групп уезжает операндом сравнения SQL по колонке
идентификатора; признак раскрытого расписания редактора уезжает в сборку контекста и дальше в
выборку. Закрыть входы адреса и оставить эти два значило бы повторить ровно тот дефект, который
ревизия предъявила: семья закрыта для тех своих членов, на которые смотрели.

Output: пятнадцать идентификаторов пути и два параметра запроса под общей границей, матрица входов с
антивакуумом и выросший реестр расхождения.
</objective>

<reversibility>
  <decision rating="reversible">
Применение общего ограниченного псевдонима к пятнадцати параметрам пути. Откат — возврат одного слова
в каждой сигнатуре; данных не теряется, миграции нет.
  </decision>
  <decision rating="reversible">
Верхняя граница у двух параметров запроса. Откат — снятие одного именованного аргумента у каждого.
Нижняя граница у курсора уже стои́т и не трогается.
  </decision>
</reversibility>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-24-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-18-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: семья подтверждённого удаления перестаёт быть закрытой для одного члена из четырёх — три маршрута и тумблер группы</name>

  <files>
app/pages/account_groups.py
app/pages/accounts.py
tests/test_pages/test_identifier_bounds.py
  </files>

  <read_first>
- `app/pages/identifiers.py` — то, что завёл план 10-24: имена псевдонимов и их точная форма.
- `app/pages/account_groups.py:200-300` (страница групп и постраничный вывод), `:365-430`
  (состояние синхронизации и тумблер), `:585-700` (удаление группы). ⚠️ ЧИТАТЬ ВСЕ ТРИ ДИАПАЗОНА:
  предмет правки — сигнатуры, но порядок в телах читать обязательно, потому что именно он делает
  гард происхождения недостижимым на негодной величине (`app/pages/account_groups.py:691` — ревизия
  назвала это прямо).
- `app/pages/account_groups.py:340-350` — место, где курсор постраничного вывода уезжает операндом
  сравнения SQL по колонке идентификатора. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: это и есть основание, по которому
  курсор закрывается вместе с идентификаторами пути, а не оставляется.
- `app/pages/accounts.py:670-800` (состояние синхронизации, повтор синхронизации, синхронизация
  групп) и `:985-1040` (удаление аккаунта).
- `tests/test_pages/test_identifier_bounds.py` — модуль, заведённый планом 10-24: его форма
  наследуется, а не переизобретается (сырое тело строкой, сличение отображения целиком, антивакуум).
- `app/models/group.py:45`, `app/models/messenger_account.py:12` — объявления первичных ключей.
  ⚠️ ЧИТАТЬ: те же `Mapped[int]` без указания расширенной разрядности, то есть та же колонка и та же
  граница.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `### CR-01` — таблица
  воспроизведённых исходов и абзац о недостижимом гарде.
  </read_first>

  <behavior>
1. `POST /accounts/{account_id}/delete` с величиной выше границы колонки отвечает отказом валидации.
2. `POST /accounts/{account_id}/groups/{group_id}/delete` отвечает так же — и по каждому из ДВУХ
   своих идентификаторов порознь, а не только по одному.
3. `POST /accounts/{account_id}/groups/{group_id}/toggle` отвечает так же — по обоим идентификаторам.
4. `POST /accounts/{account_id}/retry-sync` и `POST /accounts/{account_id}/sync-groups` отвечают
   так же.
5. Читающие маршруты тех же модулей (страница групп, постраничный вывод, оба состояния
   синхронизации) отвечают так же — идентификатор пути там того же класса.
6. Курсор постраничного вывода групп с величиной выше границы колонки отвергается; с величиной
   внутри диапазона — работает как прежде.
7. На каждом закрытом входе живая величина работает: аккаунт синхронизируется, группа переключается,
   группа удаляется, аккаунт удаляется.
  </behavior>

  <action>
**(1) RED ПЕРЕД GREEN, И МАТРИЦЕЙ, А НЕ ОБРАЗЦОМ.** Сначала дописать в модуль суиты матрицу входов
этих двух модулей: запись входа несёт адрес с местозаполнителем величины, метод, имя параметра, вид
живой величины для антивакуума и ожидаемый код. Прогнать ДО правки сигнатур, получить КРАСНЫЙ с
перечнем ВСЕХ несогласных строк, привести дословный вывод в сводку. ⚠️ Правило обязано называть все
несогласные строки, а не первую: правило, останавливающееся на первой, чинилось бы по одному входу
за круг — записанное основание соседнего обхода.

**(2) Закрыть идентификаторы ПУТИ общим псевдонимом адреса.** В `app/pages/account_groups.py` —
семь параметров: идентификатор аккаунта на странице групп, на постраничном выводе, на состоянии
синхронизации, на тумблере и на удалении, плюс идентификатор группы на тумблере и на удалении. В
`app/pages/accounts.py` — четыре: идентификатор аккаунта на состоянии синхронизации, повторе
синхронизации, синхронизации групп и удалении. Псевдоним берётся из нейтрального модуля; локальных
копий не заводится.

**(3) Закрыть курсор постраничного вывода групп.** У него уже стои́т нижняя граница — добавляется
ВЕРХНЯЯ, той же величиной. Форма записи параметра НЕ МЕНЯЕТСЯ (значение по умолчанию с объявлением
параметра запроса): переписывание формы ради единообразия увеличило бы дифф, ничего не дав.
⚠️ Рядом дописать одну строку основания: курсор уезжает операндом сравнения SQL по колонке
идентификатора, поэтому его граница есть граница КОЛОНКИ, а не удобство.

**(4) Тела обработчиков НЕ ТРОГАТЬ.** Ни одной строки. В частности, не переносить проверку величины
внутрь тела: граница обязана стоять ДО тела, потому что именно этим гард происхождения перестаёт быть
недостижимым.

**(5) Антивакуум прогнать ПО ВСЕМУ ПЕРЕЧНЮ, а не по одной строке.** Каждый вход матрицы получает
живую посеянную величину, и её исход обязан не быть отказом валидации и не быть отказом обработчика.

**(6) Проверить смежность на каждом входе.** Величина, равная границе, отказом валидации не
отвергается; величина на единицу больше — отвергается. Проверка идёт по всем входам, а не по одному.

**(7) Чего задача НЕ делает.** Не трогает `app/pages/ads.py` и `app/pages/history.py` (задача 2). Не
трогает админского модуля (план 10-29). Не правит реестра расхождения (задача 3). Не заводит гейта
полноты по каталогу (план 10-30).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_identifier_bounds.py -q</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — матрица не собрана либо какой-то вход отвечает не отказом валидации, и текст отказа назовёт все несогласные строки</fails_when>
    <automated>uv run pytest tests/test_pages/test_account_groups.py tests/test_pages/test_accounts.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — ограничение сигнатур задело действующие правила двух модулей; в частности, правило, подающее ноль либо отрицательную величину и ожидающее иного кода</fails_when>
    <automated>uv run pytest tests/test_pages/ -q --deselect tests/test_pages/test_htmx_gates.py::test_every_framework_bounded_input_is_declared_as_a_divergence</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — ограничение задело правила страничного слоя за пределами двух модулей. ⚠️ СНЯТО РОВНО ОДНО ПРАВИЛО, И СНЯТО НЕ НАВСЕГДА: правило ПОЛНОТЫ реестра расхождения меряет ВЕСЬ страничный слой, настоящая задача ограничивает POST-входы двух продуктовых модулей, а записи о них заводит задача 3 ЭТОГО ЖЕ плана — то есть здесь оно красно ПО ПОСТРОЕНИЮ, а не по дефекту. Предмет снятого узла измеряется следующей командой ЯВНО и закрывается прогоном задачи 3</fails_when>
    <automated>uv run python -c "import sys; from tests.test_pages.test_htmx_gates import _validation_refusal_completeness_complaints as complaints, VALIDATION_REFUSAL_DIVERGENCES as declared; found=complaints(declared); print('жалоб правила полноты:', len(found)); print(*found, sep='\n'); own=[f for f in found if f.startswith('НАЙДЕН ЗАМЕРОМ, НО НЕ ОБЪЯВЛЕН') and ('app/pages/account_groups.py::' in f or 'app/pages/accounts.py::' in f)]; sys.exit(0 if found and len(own)==len(found) else 'долг реестра после задачи 1 не сводится к POST-входам ДВУХ модулей этой задачи: '+repr(found))"</automated>
    <fails_when>ненулевой код возврата — множество жалоб правила полноты ПУСТО (задача не ограничила ни одного POST-входа, то есть зелёный получен бездействием) либо содержит жалобу, не относящуюся к POST-входам `app/pages/account_groups.py` и `app/pages/accounts.py`: ограничение задело чужой модуль, реестр правился раньше задачи 3, либо жалоба иного вида («ОБЪЯВЛЕН, НО ЗАМЕРОМ НЕ НАЙДЕН», «ПСЕВДОНИМ РАЗОШЁЛСЯ С ЗАМЕРОМ») — то есть задета запись, заведённая планом 10-24</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — исходник не собирается</fails_when>
  </verify>

  <acceptance_criteria>
- КРАСНЫЙ ДО ПРАВКИ НАБЛЮДЁН МАТРИЦЕЙ: дословный вывод прогона матрицы на дереве ДО шага (2)
  приведён в сводке, и в нём перечислены ВСЕ несогласные строки со снятыми кодами.
- Число закрытых параметров пути в двух модулях равно одиннадцати, и оно снято ЗАМЕРОМ, а не
  сложено в уме: в сводке приведён вывод разбора сигнатур обоих модулей с перечнем параметров.
- Матрица покрывает ОБА идентификатора маршрутов тумблера и удаления группы порознь: в сводке
  названо, сколько строк матрицы приходится на маршрут с двумя идентификаторами, и это число равно
  двум на каждый такой маршрут.
- Антивакуум зелен по ВСЕМУ перечню: в сводке приведено число входов и число прогнанных живых
  величин, и они равны.
- Смежность показана на каждом входе: в сводке приведено, сколько входов проверено на границе и
  сколько — на границе плюс единица, и оба числа равны числу входов.
- ⚠️ ГРАНИЦА НАБЛЮДЕНИЯ НАЗВАНА ЧЕСТНО: все критерии выше наблюдают отказ НА ГРАНИЦЕ ПРИЛОЖЕНИЯ, до
  драйвера, и от драйвера не зависят. Поведения боевого PostgreSQL на НЕОГРАНИЧЕННОМ входе они не
  наблюдают — у суиты один драйвер, и это остаётся предметом окна 39.
- Тела обработчиков не правились: в сводке приведено число изменённых строк вне сигнатур, и оно
  равно нулю по каждому из двух файлов, кроме строки основания у курсора.
- ⚠️ ДОЛГ РЕЕСТРА ВНУТРИ ПЛАНА НАЗВАН ЧИСЛОМ И ПОИМЁННО: в сводке приведён дословный вывод прогона
  жалоб правила полноты после задачи 1 — сколько их и какие входы они называют. Каждая обязана
  относиться к POST-входу одного из двух модулей ЭТОЙ задачи; ни одной жалобы иного вида быть не
  может. Названо и то, что долг закрывает задача 3 ЭТОГО ЖЕ плана.
  </acceptance_criteria>

  <done>
Одиннадцать идентификаторов пути и курсор постраничного вывода закрыты; матрица зелена по отказам и
по живым величинам; смежность показана на каждом входе; красный до правки приведён.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: правка объявления, редактор и повтор отправки — три оставшихся продуктовых входа и признак раскрытого расписания</name>

  <files>
app/pages/ads.py
app/pages/history.py
tests/test_pages/test_identifier_bounds.py
  </files>

  <read_first>
- `app/pages/ads.py:645-740` — редактор объявления (чтение), правка объявления (запись) и удаление.
  ⚠️ УДАЛЕНИЕ УЖЕ ЗАКРЫТО ПЛАНОМ 10-24 — читать, чтобы не закрыть второй раз и не завести второй
  записи в реестре расхождения.
- `app/pages/ads.py`, место, где признак раскрытого расписания уезжает в сборку контекста редактора.
  ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО И ПРОСЛЕДИТЬ ДАЛЬШЕ: если значение доезжает до сравнения по колонке
  идентификатора, его граница есть граница колонки; если не доезжает — это НАХОДКА, и она называется
  в сводке, а параметр закрывается всё равно, потому что он объявлен идентификатором расписания.
- `app/pages/history.py:785-860` — карточка записи журнала и повтор отправки. ⚠️ ЧИТАТЬ ПОРЯДОК В
  ТЕЛЕ повтора: гард происхождения там стои́т после разбора cookie и до выборки записи, и на негодной
  величине до него не доходило.
- `app/models/send_log.py:12` — объявление первичного ключа записи журнала.
- `tests/test_pages/test_identifier_bounds.py` — матрица, дописанная задачей 1: новые входы встают в
  ту же матрицу, а не в отдельную.
  </read_first>

  <behavior>
1. `POST /ads/{ad_id}/edit` с величиной выше границы колонки отвечает отказом валидации.
2. `GET /ads/{ad_id}/edit` отвечает так же.
3. `GET /ads/{ad_id}/edit` с негодной величиной ПРИЗНАКА РАСКРЫТОГО РАСПИСАНИЯ отвечает так же, при
   годном идентификаторе объявления.
4. `POST /history/{log_id}/retry` отвечает так же.
5. `GET /history/{log_id}` отвечает так же.
6. На каждом закрытом входе живая величина работает: редактор открывается, правка сохраняется,
   карточка журнала показывается, повтор ставится в очередь.
7. Удаление объявления, закрытое планом 10-24, остаётся закрытым РОВНО ОДИН раз: второго псевдонима
   и второй записи реестра у него нет.
  </behavior>

  <action>
**(1) RED ПЕРЕД GREEN.** Дописать в ту же матрицу входы этих двух модулей, прогнать ДО правки,
привести КРАСНЫЙ с перечнем всех несогласных строк в сводку.

**(2) Закрыть идентификаторы ПУТИ.** В `app/pages/ads.py` — два: идентификатор объявления на
редакторе и на правке. В `app/pages/history.py` — два: идентификатор записи журнала на карточке и на
повторе. Псевдоним — общий из нейтрального модуля.

**(3) Закрыть признак раскрытого расписания.** Ему добавляются ОБЕ границы — нижняя и верхняя, —
потому что сегодня у него нет ни одной. Форма записи параметра не меняется. Рядом дописать одну
строку основания: параметр объявлен идентификатором расписания, а идентификаторы этого проекта лежат
в диапазоне колонки; пустое значение остаётся законным состоянием (расписание может быть не выбрано).

**(4) Проверить, что удаление объявления не закрывается второй раз.** Разбором сигнатур
`app/pages/ads.py` убедиться, что параметр удаления объявлен ровно одним псевдонимом. Вывод разбора
привести в сводку. Второй псевдоним на одном параметре был бы невидим при чтении и виден только
замером.

**(5) Тела обработчиков НЕ ТРОГАТЬ.**

**(6) Антивакуум и смежность — по всем новым входам**, той же формой, что и в задаче 1.

**(7) Назвать НАХОДКУ, если она есть.** Если признак раскрытого расписания до сравнения по колонке не
доезжает, это записывается в сводку прямо: параметр закрыт по ОБЪЯВЛЕННОМУ предмету (идентификатор
расписания), а не по прослеженному пути, и разница названа. Молчаливое закрытие «на всякий случай»
есть запись шире дерева.

**(8) Чего задача НЕ делает.** Не трогает админского модуля. Не правит реестра расхождения (задача 3).
Не трогает докстринга помощника чтения идентификатора из формы (план 10-32).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_identifier_bounds.py -q</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — матрица не собрана либо какой-то вход отвечает не отказом валидации</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads.py tests/test_pages/test_ads_editor.py tests/test_pages/test_history.py tests/test_pages/test_history_retry.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — ограничение задело действующие правила редактора, журнала или повтора отправки</fails_when>
    <automated>uv run pytest tests/test_pages/ -q --deselect tests/test_pages/test_htmx_gates.py::test_every_framework_bounded_input_is_declared_as_a_divergence</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — ограничение задело правила страничного слоя за пределами четырёх модулей. ⚠️ СНЯТО РОВНО ОДНО ПРАВИЛО, И СНЯТО НЕ НАВСЕГДА: правило ПОЛНОТЫ реестра расхождения несёт долг задач 1 и 2 вместе, а записи заводит задача 3 ЭТОГО ЖЕ плана — здесь оно красно ПО ПОСТРОЕНИЮ. Предмет снятого узла измеряется следующей командой ЯВНО и закрывается прогоном задачи 3</fails_when>
    <automated>uv run python -c "import sys; from tests.test_pages.test_htmx_gates import _validation_refusal_completeness_complaints as complaints, VALIDATION_REFUSAL_DIVERGENCES as declared; found=complaints(declared); print('жалоб правила полноты:', len(found)); print(*found, sep='\n'); mine=('app/pages/account_groups.py::','app/pages/accounts.py::','app/pages/ads.py::','app/pages/history.py::'); own=[f for f in found if f.startswith('НАЙДЕН ЗАМЕРОМ, НО НЕ ОБЪЯВЛЕН') and any(m in f for m in mine)]; from_ads=[f for f in own if 'app/pages/ads.py::' in f]; from_log=[f for f in own if 'app/pages/history.py::' in f]; sys.exit(0 if len(own)==len(found) and from_ads and from_log else 'долг реестра после задачи 2 не сводится к POST-входам ЧЕТЫРЁХ модулей плана либо ни один новый POST-вход не пришёл из модуля объявлений или модуля журнала: '+repr(found))"</automated>
    <fails_when>ненулевой код возврата — долг правила полноты содержит жалобу за пределами четырёх модулей плана, либо ни одной жалобы из `app/pages/ads.py` (правка объявления POST-входа не получила), либо ни одной из `app/pages/history.py` (повтор отправки не получил), либо жалоба иного вида — то есть задета запись, заведённая планом 10-24 или задачей 1</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — исходник не собирается</fails_when>
  </verify>

  <acceptance_criteria>
- КРАСНЫЙ ДО ПРАВКИ НАБЛЮДЁН: дословный вывод прогона матрицы с новыми входами на дереве ДО шага (2)
  приведён в сводке.
- Число закрытых параметров пути в двух модулях равно четырём, снято замером разбора сигнатур, и
  перечень приведён в сводке.
- Разбор сигнатур `app/pages/ads.py` показывает, что параметр удаления объявления несёт РОВНО ОДИН
  псевдоним; вывод разбора приведён в сводке.
- Признак раскрытого расписания несёт обе границы, и его пустое значение остаётся законным: в сводке
  приведён исход запроса редактора БЕЗ этого параметра — он не есть отказ валидации.
- Судьба прослеживания признака до сравнения по колонке НАЗВАНА В СВОДКЕ ПРЯМО: доезжает либо не
  доезжает, и на каком основании параметр закрыт.
- Антивакуум и смежность прогнаны по всем новым входам; три числа (входов, живых величин, проверок
  смежности) приведены в сводке и равны.
- ⚠️ ГРАНИЦА НАБЛЮДЕНИЯ ТА ЖЕ, ЧТО И В ЗАДАЧЕ 1, и повторяется в сводке: наблюдается отказ на границе
  приложения, не поведение боевого драйвера.
- ⚠️ ДОЛГ РЕЕСТРА ПОСЛЕ ЗАДАЧИ 2 НАЗВАН ЧИСЛОМ И СЛИЧЁН С ЗАДАЧЕЙ 1: в сводке приведены оба числа
  жалоб правила полноты — после задачи 1 и после задачи 2, — и разность между ними равна числу
  новых POST-входов, ограниченных задачей 2. Эта разность есть та же величина, которую задача 3
  обязана внести в реестр, и расхождение между ними есть НАХОДКА, а не помеха.
  </acceptance_criteria>

  <done>
Четыре идентификатора пути и признак раскрытого расписания закрыты; удаление объявления закрыто ровно
один раз; матрица зелена по отказам и по живым величинам; находка прослеживания названа.
  </done>
</task>

<task type="auto">
  <name>Задача 3: реестр расхождения растёт на девять записей — по числу новых ограниченных входов POST-обработчиков</name>

  <files>
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py:2340-2790` ЦЕЛИКОМ — раздел реестра расхождения после правки
  плана 10-24: объявление записи, константы ждущего состояния и условия снятия, общая константа цены,
  сам перечень, объявленное число с двумя строками летописи, замер `_framework_bounded_post_inputs`
  и три правила (полноты, обоснований, авторства).
  ⚠️ ЧИТАТЬ ВЕСЬ РАЗДЕЛ: правило обоснований роняет прогон, если любое из пяти полей пусто, если
  условие снятия не называет фазы-снимателя, либо если обоснование не начинается с общей константы
  цены.
- `10-24-SUMMARY.md` — как заведена восьмая запись и каким прогоном снят её ключ.
- `app/templates/account_groups/includes/group_row.html`, `app/templates/accounts/partial_cards.html`,
  `app/templates/ads/includes/ad_card.html`, `app/templates/history/includes/history_card.html` —
  разметка, из которой приезжают величины закрытых входов. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: обоснование каждой
  записи обязано называть ДОСТИЖИМОСТЬ ИЗ ИНТЕРФЕЙСА, ИЗМЕРЕННУЮ ЧТЕНИЕМ РАЗМЕТКИ, а не переписанную
  из ревизии; для каждого входа найти место, откуда величина приезжает, и назвать путь и строку.
  </read_first>

  <behavior>
1. Замер находит на девять входов больше, чем после плана 10-24.
2. Каждый новый вход несёт запись со всеми пятью непустыми полями.
3. Обоснование каждой записи начинается с общей константы цены и продолжается ИЗМЕРЕННОЙ достижимостью
   из интерфейса с путём и строкой разметки.
4. Условие снятия каждой записи называет фазу-снимателя, взятую из роадмапа.
5. Состояние решения каждой записи — ждущее владельца.
6. Объявленное число равно длине перечня и снято ПРОГОНОМ замера.
7. Правило полноты, правило обоснований и правило авторства зелены и не зеленеют на пустом множестве.
  </behavior>

  <action>
**(1) Снять ключи ПРОГОНОМ, а не набрать по памяти.** Прогнать замер на действующих исходниках,
получить полный перечень измеренных входов, вычесть объявленные — разность и есть девять новых
ключей. Их вписать ДОСЛОВНО. ⚠️ Ключ, набранный по памяти, разойдётся со сравнением множеств и
уронит правило полноты жалобой об объявленном, но не найденном.

**(2) Написать девять обоснований, и КАЖДОЕ со своей достижимостью.** Общая константа цены —
началом; дальше для каждого входа СВОЁ: откуда приезжает величина (сервер собрал её в атрибут
действия формы панели подтверждения по идентификатору строки; сервер собрал её в атрибут действия
формы тумблера; и так далее), путь файла разметки и строка. ⚠️ ОДНО ОБОСНОВАНИЕ НА ДЕВЯТЬ ЗАПИСЕЙ
ЗАПРЕЩЕНО: достижимость есть свойство ВХОДА, и общее обоснование сказало бы о девяти входах одно, не
измерив ни одного. Если достижимость какого-то входа ОТЛИЧНА ОТ НУЛЕВОЙ (величина редактируется через
интерфейс), это записывается ПРЯМО и является находкой, а не помехой.

**(3) Условие снятия и состояние решения** — действующие константы модуля. Новых не заводить: фаза,
снимающая расхождение, у всех девяти одна и та же, и она взята из роадмапа, а не назначена планом.

**(4) Сдвинуть объявленное число и дописать ТРЕТЬЮ строку летописи**: 8 → 17, партия 5, настоящий
план, задачи 1 и 2 закрыли девять входов POST-обработчиков в четырёх продуктовых модулях; и
предупреждение о том, что план 10-29 сдвинет число ещё раз.
⚠️ ЧИСЛО СТАВИТСЯ ПРОГОНОМ ПОКРАСНЕВШЕГО ПРАВИЛА, А НЕ ВЫЧИТАНИЕМ В УМЕ — прямое требование к способу,
записанное соседним объявленным числом фазы.

**(5) Показать зубы правила полноты на НОВЫХ входах.** Прогнать правило с укороченным перечнем (без
девяти новых), подав его параметром, и привести в сводку дословную жалобу с именами всех девяти
входов.

**(6) Прежние восемь записей и две прежние строки летописи НЕ ПРАВИТЬ.**

**(7) Чего задача НЕ делает.** Не принимает решения о законности расхождения. Не расширяет охвата
замера. Не трогает правил формы ответа (план 10-31).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -k "validation_refusal or divergence"</automated>
    <fails_when>ненулевой код возврата, либо слово `failed` в итоговой строке, либо `no tests ran` — фильтр не собрал ни одного правила реестра, и зелёный получен пустым множеством</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка реестра задела соседние гейты того же модуля</fails_when>
    <automated>uv run python -c "import re,pathlib,sys; s=pathlib.Path('tests/test_pages/test_htmx_gates.py').read_text(encoding='utf-8'); m=re.search(r'VALIDATION_REFUSAL_DIVERGENCES_DECLARED\s*=\s*(\d+)',s); sys.exit(0 if m and m.group(1)=='17' else 'объявленное число реестра расхождения не равно семнадцати: '+(m.group(1) if m else 'объявление не найдено'))"</automated>
    <fails_when>ненулевой код возврата — объявленное число не сдвинуто либо сдвинуто не на то значение, то есть часть новых ограниченных входов осталась молчаливым умолчанием</fails_when>
    <automated>uv run pytest tests/ -q -m "not planning"</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — партия разошлась с продуктовой суитой</fails_when>
  </verify>

  <acceptance_criteria>
- Девять ключей сняты ПРОГОНОМ замера: в сводке приведён вывод разности «измеренные минус
  объявленные» до правки перечня, и в нём ровно девять строк.
- Каждое из девяти обоснований называет СВОЙ путь файла разметки и СВОЮ строку; в сводке приведена
  таблица «вход → путь разметки → строка», девять строк, без повторов пути со строкой.
- Достижимость каждого входа названа ИЗМЕРЕННОЙ: в сводке сказано, для скольких входов она нулевая и
  для скольких отлична от нулевой; входы второй категории названы поимённо.
- Летопись числа несёт ТРЕТЬЮ строку и называет план и задачи, которыми число сдвинуто.
- Число поставлено прогоном покрасневшего правила: в сводке приведён красный вывод до сдвига числа.
- Зубы правила полноты показаны на девяти новых входах: дословная жалоба с девятью именами приведена
  в сводке.
- Прежние восемь записей не правились: в сводке приведено число удалённых строк диффа внутри блока
  прежних записей, и оно равно нулю.
  </acceptance_criteria>

  <done>
Реестр несёт семнадцать записей с девятью новыми обоснованиями, каждое со своей измеренной
достижимостью; объявленное число равно семнадцати и поставлено прогоном красного; зубы правила
полноты показаны на всех девяти.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой (адрес запроса) | пятнадцать целочисленных идентификаторов из недоверенного адреса доезжают операндом сравнения SQL |
| браузер → страничный слой (параметр запроса) | курсор постраничного вывода и признак раскрытого расписания уезжают тем же путём, что и идентификатор адреса |
| сторонний сайт → страничный слой | межсайтовый POST с приложенной cookie неотличим от своего; на негодной величине гард происхождения был недостижим |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-28-01 | Denial of Service | пять воспроизведённых ревизией маршрутов четырёх продуктовых модулей | high | mitigate | Величина вне диапазона колонки отвергается на границе приложения, а не доезжает до драйвера: воспроизведённый отказ обработчика на корректно сформированном запросе становится отказом валидации. Проверяется матрицей входов с антивакуумом (задачи 1 и 2) |
| T-10-28-02 | Spoofing | гард происхождения на удалении аккаунта, удалении группы и повторе отправки | high | mitigate | ⚠️ РЕВИЗИЯ НАЗВАЛА ЭТО ПРЯМО: гард стои́т в теле, приведение к целому удаётся, отказ случается позже в слое доступа к данным — значит на негодной величине гард НЕДОСТИЖИМ, и каллер с неподтверждённым источником доводит маршрут до отказа обработчика. С границей на сигнатуре запрос отвергается ДО тела. Остаточная граница гарда (запрос без обоих заголовков) не пересматривается |
| T-10-28-03 | Information Disclosure | текст отказа обработчика на шести маршрутах | medium | mitigate | Отказ валидации фреймворка не несёт ни следа стека, ни имени таблицы, ни признака существования строки. Ограничение снимает ветвь, на которой этот текст рождался |
| T-10-28-04 | Tampering | форма отказа валидации на девяти новых POST-входах | medium | mitigate | Расхождение с запертым D-01 НЕ СКРЫВАЕТСЯ: девять записей реестра с ИЗМЕРЕННОЙ достижимостью каждого входа, условием снятия и ждущим состоянием решения (задача 3). Митигация есть ВИДИМОСТЬ расхождения; снятие — работа Фазы 11 по роадмапу |
| T-10-28-05 | Denial of Service | курсор постраничного вывода групп | medium | mitigate | Курсор уезжает операндом сравнения SQL по колонке идентификатора и нижнюю границу уже несёт; верхняя ставится той же величиной. Без неё вход остался бы открытым ровно тем способом, который ревизия предъявила на адресах |
| T-10-28-SC | Tampering | установки пакетов | low | accept | Установок нет: `pyproject.toml` не в `files_modified`, новых зависимостей не вводится |
</threat_model>

## Артефакты, которые создаёт этот план

- матрица входов «вход → снятый код» с антивакуумом и проверкой смежности в
  `tests/test_pages/test_identifier_bounds.py`
- записи реестра расхождения с девятого по семнадцатую и значение объявленного числа `17`
- третья строка летописи объявленного числа реестра

Полный перечень артефактов ПАРТИИ — `10-24-PLAN.md`, раздел `## Артефакты, которые создаёт эта партия`.

## Что этот план НЕ закрывает, и основание у каждого пункта своё

- **Идентификаторы административного модуля.** Разведены в план 10-29 не по трудоёмкости, а по
  ВЛАДЕЛЬЦУ ГЕЙТА: полноту административных маршрутов держит СВОЙ гейт со своим объявленным числом
  (`test_every_mutating_admin_route_checks_the_origin`), и правка обоих множеств одним планом
  смешала бы два независимо объявленных числа в одном диффе. Разведение сохраняет свойство «число
  сдвинуто вместе со своим множеством».
- **Гейт полноты границы по всему каталогу страничного слоя.** План 10-30, а не этот: гейт, заведённый
  раньше, чем закрыт админский модуль, краснел бы по построению всю волну 2 и всю волну 3 и был бы
  выключен раньше, чем начал стеречь.
- **Величины постраничного вывода (смещение и размер порции).** НЕ ОТЛОЖЕНЫ, А НЕ ВХОДЯТ В ПРЕДМЕТ:
  они не есть идентификатор, отказ по ним ничего не говорит о владении строкой, и записанное
  основание соседнего гейта уже называет их изъятыми по этой же причине. Прохибиция в `must_haves`
  говорит то же.
- **Поведение боевого драйвера на неограниченном входе (окно 39).** Основанием служит ОТСУТСТВИЕ
  ВТОРОГО ДРАЙВЕРА У СУИТЫ. План делает вопрос менее важным (отказ ставится до драйвера), но не
  отвечает на него, и окно остаётся открытым.

<verification>
- `uv run pytest tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_gates.py -q` — зелено.
- `uv run pytest tests/ -q -m "not planning"` — зелено.
- `uv run python -m compileall -q app main.py tests` — код возврата ноль.
- Два наблюдённых красных матрицы (до задачи 1 и до задачи 2) приведены в сводке дословно.
- Красный правила полноты реестра на девяти новых входах приведён в сводке дословно.
- Таблица «вход → путь разметки → строка» на девять строк приведена в сводке.
- ⚠️ ВНУТРИПЛАНОВЫЙ ДОЛГ ПРАВИЛА ПОЛНОТЫ ЗАКРЫТ ВНУТРИ ЭТОГО ЖЕ ПЛАНА, И ЭТО ПРОВЕРЯЕТСЯ, А НЕ
  ОБЕЩАЕТСЯ. Задачи 1 и 2 ограничивают POST-входы, задача 3 заводит о них записи, и между ними
  правило ПОЛНОТЫ реестра красно ПО ПОСТРОЕНИЮ. Поэтому широкие прогоны задач 1 и 2 снимают РОВНО
  ЭТОТ узел (`test_every_framework_bounded_input_is_declared_as_a_divergence`), измеряя его предмет
  отдельной командой с проверкой ВЛАДЕНИЯ каждой жалобой, а прогон задачи 3 берёт
  `tests/test_pages/test_htmx_gates.py` ЦЕЛИКОМ — снятый узел исполняется там и обязан быть
  зелёным. Реестр остаётся закрытым перечнем полноты; ослаблен он не был ни на одной задаче.
- Два числа жалоб правила полноты (после задачи 1 и после задачи 2) приведены в сводке, и их
  разность равна числу POST-входов, ограниченных задачей 2.
</verification>

<success_criteria>
Все четыре маршрута семьи подтверждённого удаления, тумблер группы, правка объявления и повтор
отправки отвечают отказом валидации на величине вне диапазона колонки; идентификаторы, приезжающие
параметром запроса, закрыты той же границей; смежность и антивакуум показаны на КАЖДОМ входе, а не на
образце; реестр расхождения вырос на девять записей с измеренной достижимостью каждой и объявленным
числом, поставленным прогоном красного.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-28-SUMMARY.md` по завершении.
</output>
