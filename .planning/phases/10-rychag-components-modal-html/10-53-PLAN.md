---
phase: 10-rychag-components-modal-html
plan: 53
type: execute
# ⚠️ `type: execute`, А НЕ `type: tdd`, И ЭТО НЕ НЕДОСМОТР ПРИ `tdd_mode: true`. Предмет плана —
# РЕШЕНИЕ ВЛАДЕЛЬЦА и ЗАПИСЬ этого решения. Тест-первая последовательность здесь невыразима: у
# записи владельца нет поведения, которое можно было бы покраснить до её внесения, а правило,
# требующее присутствия чужого решения, краснело бы на отсутствие ОТВЕТА ЧЕЛОВЕКА — то есть меряло
# бы человека, а не дерево. Машинное принуждение самого числа заведено планом 10-52 и здесь не
# переоткрывается.
wave: 2
# ⚠️ ВОЛНА 2 — ДВЕ ЗАВИСИМОСТИ ОТ ПЛАНА 10-52, И СМЫСЛОВАЯ ЗДЕСЬ ПЕРВАЯ, А НЕ ФАЙЛОВАЯ.
# (1) СМЫСЛОВАЯ, НЕСУЩАЯ. Одиннадцатый круг отказал фазе не за правку, а за то, что ЦЕНА НЕ
#     ПРЕДЪЯВЛЕНА тому, кто один вправе её принять. Предъявлять цену ПЕРЕСКАЗОМ ОТЧЁТА значило бы
#     повторить ту же ошибку в мягкой форме: владелец получил бы вывод, а не замер. План 10-52
#     заводит прибор, который печатает ПОЛУЧЕННОЕ ЗНАЧЕНИЕ — перечень файлов с числами, — и
#     останов настоящего плана предъявляет владельцу именно его вывод.
# (2) ФАЙЛОВАЯ. `tests/test_templates/test_components.py` правит план 10-52 (волна 1); настоящий
#     план дописывает в докстринг того же гейта ОСНОВАНИЕ числа. Одновременное исполнение задело бы
#     один файл двумя руками.
depends_on: ["10-52"]
gap_closure: true
gap_ids: [GAP-1a]
# ⚠️ ИДЕНТИФИКАТОР НЕ ИЗ РЕЕСТРА `G-10-*` ОБХОДА: гэп заведён ОДИННАДЦАТЫМ КРУГОМ в разделе `gaps`
# артефакта `10-VERIFICATION.md`. Настоящий план закрывает пункт `missing[0]` — РЕШЕНИЕ ВЛАДЕЛЬЦА;
# пункт `missing[1]` (пятое утверждение гейта) закрыт планом 10-52.
files_modified:
  - .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
  - tests/test_templates/test_components.py
  - .planning/phases/10-rychag-components-modal-html/deferred-items.md
  - .planning/WINDOWS.md
# ⚠️ `.planning/WINDOWS.md` ПРАВИТСЯ ТОЛЬКО В ОДНОЙ ИЗ ДВУХ ВЕТВЕЙ, И ЭТО НАЗВАНО, А НЕ УМОЛЧАНО:
# при `criterion-stands-violated` долг заводится записью журнала, при `extend-override` цена уже
# записана окном 74 и второй записи не заводится. Перечень объявляет ВОЗМОЖНОЕ касание — план,
# скрывший файл от перечня ради красоты, столкнулся бы на нём с соседом по волне.
autonomous: false
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (`config-get`, 2026-09-12): `security_asvs_level` → `1`,
# `security_block_on` → `"high"`. Установок пакетных менеджеров план не несёт НИ ОДНОЙ.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ. `estimate-calibration` на `9e6b859` (2026-09-12): `sample_count: 0`,
  # `factor: 1`, `applied: false`, `min_samples: 3`, `confidence: "low"`. `confidence` ВЫВЕДЕНА ИЗ
  # ЧИСЛА ОБРАЗЦОВ (0 < 3), а не назначена себе.
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ЦЕНА ПРЕДЪЯВЛЕНА ТОМУ, КТО ОДИН ВПРАВЕ ЕЁ ПРИНЯТЬ: владелец получает ШЕСТЬ ЗАМЕРОВ, снятых КОМАНДАМИ на HEAD, обе ветви и цену каждой — и отвечает ОДНИМ идентификатором"
    - "ОТВЕТ ВЛАДЕЛЬЦА ПОЛУЧАЕТ МАШИННО ЧИТАЕМОЕ МЕСТО: следующий круг перестаёт отличать ПРИНЯТОЕ отступление от НЕЗАМЕЧЕННОГО чтением прозы — он читает поле"
    - "ЧИСЛО ГЕЙТА ПОЛУЧАЕТ ОСНОВАНИЕ: докстринг `test_criterion_three_holds_by_the_numbers` называет, ЧЕМ именно объявлено значение трёх регистраций в заготовках — расширенным отступлением с датой и именем либо признанным нарушением критерия с названным долгом"
    - "ВЫБОР ВНУТРИ ЗАДАЧИ НЕ СДЕЛАН НИ ПРИ КАКИХ УСЛОВИЯХ, включая случай, когда одна ветвь кажется очевидной: прочтение чужого критерия приёмки есть решение о том, что фаза считает сделанным"
    - statement: "ПОЛЯ ВЕРДИКТА ОТЧЁТА НЕ ТРОНУТЫ: `status`, `score`, `behavior_unverified`, `overrides_applied`, блок `re_verification`, раздел `gaps` и раздел `advisory` остаются в точности такими, какими их оставил одиннадцатый круг — они принадлежат верификатору, и план, поправивший их, вынес бы вердикт вместо него"
      verification: test
    - statement: "ПРЕЖНЯЯ ЗАПИСЬ ОТСТУПЛЕНИЯ 2026-09-09 НЕ ВЫЧЁРКИВАЕТСЯ И НЕ ПЕРЕПИСЫВАЕТСЯ: она верна для своей области (51 строка в двух поимённо названных файлах), и рядом с ней встаёт то, что её переросло (идиома D-30/D-32)"
      verification: test
  artifacts:
    - "`.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md` — ответ владельца машинно читаемым местом в блоке `overrides` шапки: при `extend-override` ВТОРАЯ запись, называющая третий файл и третью сущность поимённо; при `criterion-stands-violated` приписка к существующей записи, называющая ветвь, имя и дату и говорящая, что область НЕ РАСШИРЕНА"
    - "`tests/test_templates/test_components.py` — основание числа трёх регистраций в докстринге гейта критерия 3"
    - "`.planning/phases/10-rychag-components-modal-html/deferred-items.md` — раздел `## План 10-53`, внесённый ПЕРЕД якорем конца перечня, с последствиями ответа и перечнем непокрытого"
    - "`.planning/WINDOWS.md` — при ветви `criterion-stands-violated` запись долга, заведённая КОМАНДОЙ ведения журнала, а не руками"
  key_links:
    - "останов на владельце → законность записи: единственное, чем принятое отступление отличимо от отступления, которое машина назначила себе сама; ломается — фаза объявляет своим критерием то, что удобно исполнителю, и следующий круг поднимает вопрос заново четвёртым разом"
    - "поле шапки отчёта → память следующего круга: единственное машинно читаемое место, где решение переживает партию; ломается — решение живёт прозой, и каждый круг выводит его заново из диффа"
    - "основание в докстринге гейта → связь числа с решением: единственное, чем читатель правила узнаёт, ПОЧЕМУ объявлено три, а не два; ломается — число снова стои́т без причины, и следующий исполнитель сдвинет его как замер, а не как решение"
  prohibitions:
    - statement: "ВЫБОР ВЕТВИ ВНУТРИ ЗАДАЧИ 1 НЕ ДЕЛАЕТСЯ НИ ПРИ КАКИХ УСЛОВИЯХ, и режим автопродвижения на чекпоинт с `gate=\"blocking-human\"` не действует: одиннадцатый круг называет ограничение дословно — «Толковать её шире, чем она себя объявляет, верификатор не вправе», и то же ограничение действует на план"
      status: flagged-unverified
    - statement: "ТРЕТИЙ ОБРАБОТЧИК НЕ ОТКАТЫВАЕТСЯ НИ В ОДНОЙ ИЗ ДВУХ ВЕТВЕЙ: при `criterion-stands-violated` фаза остаётся с кодом, который её собственный критерий запрещает, И С ЗАПИСЬЮ ОБ ЭТОМ — откат вернул бы дефект `G-10-5` (плашка отказа переживала успешный обмен), и цена отката названа владельцу ПРЯМО как часть цены ветви"
      status: flagged-unverified
    - statement: "ПОЛЯ ВЕРДИКТА `10-VERIFICATION.md` НЕ ПРАВЯТСЯ: `status: gaps_found`, `score`, `behavior_unverified`, `overrides_applied: 0`, блоки `re_verification`, `gaps`, `deferred`, `advisory`, `behavior_unverified_items`, `coincidental_reliance_items` и тело отчёта ниже шапки остаются нетронутыми. ⚠️ `overrides_applied` НЕ ПРИВОДИТСЯ В СОГЛАСИЕ С ЧИСЛОМ ЗАПИСЕЙ, И ЭТО РЕШЕНИЕ, А НЕ ПРОПУСК: одиннадцатый круг поставил там нуль С НАЗВАННЫМ ОСНОВАНИЕМ («ОНА НЕ ЗАСЧИТАНА В СЧЁТ НАСТОЯЩЕГО КРУГА»), то есть поле есть счёт КРУГА, а не счёт записей; правка его планом переписала бы замер верификатора его же именем"
      status: flagged-unverified
    - statement: "`app/` НЕ ПРАВИТСЯ НИ НА СИМВОЛ: правка рычага `components/modal.html` открыла бы `CR-01`, правка заготовок пересеклась бы с предметом решения в том же прогоне, где решение принимается"
      status: flagged-unverified
    - statement: "`10-UAT.md` НЕ ПРАВИТСЯ ВОВСЕ, его `status` терминальным не становится, ни одна отметка прохождения не ставится, `checks_declared: 6` не двигается: артефакт обхода — предмет плана 10-54 и человека"
      status: flagged-unverified
    - statement: "`FORM-06` НЕ ОТМЕЧАЕТСЯ ВЫПОЛНЕННЫМ: ни флажок `.planning/REQUIREMENTS.md:31`, ни клетка `:136` — требование закрывается подписью человека на приёмке обхода, и ни одна ветвь решения по критерию 3 его не закрывает"
      status: flagged-unverified
    - statement: "РЕЕСТР `10-PROHIBITIONS-SUBJECT.md` НЕ ПРАВИТСЯ: его числа есть предмет ОТДЕЛЬНОГО решения владельца (пункт 4 перечня «Human Verification Required»), и правка их настоящим планом была бы вердиктом вместо владельца по чужому вопросу"
      status: flagged-unverified
    - statement: "ТРЕТЬЕЙ ВЕТВИ — «ОСТАВИТЬ КАК ЕСТЬ» — ВЛАДЕЛЬЦУ НЕ ПРЕДЛАГАЕТСЯ: она и есть сегодняшнее состояние, и именно она признана негодной одиннадцатым кругом, потому что у следующего круга нет способа отличить принятое отступление от незамеченного"
      status: flagged-unverified
    - statement: "АМЕНДМЕНТ ФОРМУЛИРОВКИ КРИТЕРИЯ 3 В `.planning/ROADMAP.md` НИ ОДНОЙ ВЕТВЬЮ НЕ ПРЕДЛАГАЕТСЯ И НИ ОДНОЙ ЗАДАЧЕЙ НЕ ДЕЛАЕТСЯ: он отвергнут решением D-13 явно («документ вехи держится фиксированным, а прочтение есть предмет CONTEXT.md»), и предлагать его здесь значило бы переоткрывать чужое решение боковым следствием"
      status: flagged-unverified
  assumptions:
    - statement: "РЕБРО `idempotency` ТРЕБОВАНИЯ FORM-06 («что произойдёт, если это исполнить дважды на том же входе») НАСТОЯЩИМ ПЛАНОМ НЕ РАЗРЕШАЕТСЯ, и у него здесь ДВА РАЗНЫХ ПРЕДМЕТА, разведённых прямо. (а) ПРОДУКТОВЫЙ — «после N удалений в документе не остаётся N мёртвых диалогов» и правило повторного удаления — планом не трогается вовсе: продуктовых строк в нём нет. (б) ЗАПИСНОЙ — повторное исполнение плана дописало бы ВТОРОЙ раздел в `deferred-items.md`, и это ЗАКРЫТО ФОРМОЙ ФАЙЛА: раздел вносится ПЕРЕД якорем `<!-- deferred-items:end -->`, разделов на план РОВНО ОДИН, и оба свойства принуждаются правилами `test_every_deferred_items_file_stays_append_safe` и `test_the_anchor_closes_every_live_deferred_file`. ⚠️ ЗАКРЫТА ЗДЕСЬ ПОЛОВИНА (б), а не ребро: подменять одно другим запрещено"
      edge: idempotency
      status: unresolved
    - statement: "РЕБРО `concurrency` ТРЕБОВАНИЯ FORM-06 («что гарантировано при прерывании либо параллельном исполнении») НАСТОЯЩИМ ПЛАНОМ НЕ РАЗРЕШАЕТСЯ. Предмет ребра у фазы — третий обработчик, гасящий обе заготовки по признаку успеха ЛЮБОГО обмена (окно 74 / находка ревизии `IN-02`), и гонка «панель уходит по завершению POST раньше, чем поднимается отказ follow-up GET» (корень `G-10-7`). Настоящий план ЗАПИСЫВАЕТ решение об этом обработчике и НЕ МЕНЯЕТ ни его поведения, ни порядка срабатывания. Параллельность здесь есть только параллельность ВОЛН партии: настоящий план и план 10-54 идут одной волной и разведены непересекающимися перечнями правимых файлов — `10-UAT.md` не входит в перечень настоящего плана ни одной задачей"
      edge: concurrency
      status: unresolved
---

<objective>
**Критерий 3 роадмапа стои́т опровергнутым МОЛЧА, и закрывается это НЕ правкой кода: правка нужна и
работает.** Так говорит одиннадцатый круг, и дальше называет ровно две законные ветви — расширить
область действующего отступления записью, называющей ТРЕТИЙ файл и ТРЕТИЙ обработчик поимённо, либо
признать критерий нарушенным и назвать, что с этим делать.

**ПОЧЕМУ ДЕЙСТВУЮЩЕЕ ОТСТУПЛЕНИЕ СЮДА НЕ ДОСТАЁТ — ПО ЕГО СОБСТВЕННОМУ ТЕКСТУ, А НЕ ПО ВЫВОДУ
КРУГА.** Запись `overrides[0]` шапки `10-VERIFICATION.md`, подписанная владельцем `chubav`
2026-09-09 по ветви `accept-reading`, несёт поле `covers`: «51 строка JS в
`app/templates/ads/form.html` и `app/templates/components/modal.html`. Граница отступления — правка
ТЕЛА метода уже существующего объекта клиентского состояния и правка выражения УЖЕ СУЩЕСТВУЮЩЕГО
атрибута события; заведение НОВОЙ сущности настоящей записью НЕ принимается.» Новая регистрация
слушателя в ТРЕТЬЕМ файле — ровно то, что запись исключает, и подписана она за ДВОЕ СУТОК до правки,
которую предлагалось бы ею накрыть. У той же записи есть поле
`not_reached_by_this_override`, и оно называет третий файл прямо.

Purpose: цена обязана быть предъявлена тому, кто один вправе её принять. Не потому, что так велит
регламент, а потому что альтернатива уже измерена: расхождение, живущее прозой, поднимается КАЖДЫМ
кругом заново — у круга нет способа отличить ПРИНЯТОЕ отступление от НЕЗАМЕЧЕННОГО.

Output: ответ владельца, записанный машинно читаемым местом, и основание числа трёх регистраций,
записанное рядом с гейтом, который это число объявляет.

⚠️ **ОБА ИСХОДА ЗАКОННЫ, И ОБА ВЫРАЗИМЫ ЗАДАЧАМИ. ПРЕДПОЛОЖИТЬ ЛЮБОЙ ИЗ ДВУХ ПЛАН НЕ ВПРАВЕ.**
Одиннадцатый круг формулирует ограничение дословно — «Толковать её шире, чем она себя объявляет,
верификатор не вправе», — и то же ограничение действует на планировщика и на исполнителя. Задача 2
несёт ДВЕ ветви записи, и ни одна из них не названа предпочтительной.

⚠️ **ИДЕНТИФИКАТОРЫ ВЕТВЕЙ ЗДЕСЬ НОВЫЕ, А НЕ `accept-reading`/`reject-reading`, И ЭТО РЕШЕНИЕ.**
Прежняя пара уже израсходована ответом 2026-09-09 и живёт в поле `accepted_branch` существующей
записи. Переиспользование той же пары сделало бы два разных ответа владельца неразличимыми по полю —
а ради различимости поле и заводилось.

⚠️ **ЧТО НЕ ЯВЛЯЕТСЯ ПРЕДМЕТОМ НИ ОДНОЙ ВЕТВИ.** Откат третьего обработчика: он закрывает настоящий
дефект `G-10-5` и стережётся правилами 10-49 с двумя отрицательными контролями. Амендмент
формулировки критерия в роадмапе: отвергнут решением D-13 явно. Правка полей вердикта отчёта: они
принадлежат верификатору, и одиннадцатый круг сам сказал, к чему вернётся его вердикт при расширении
отступления — к `human_needed`. Правка реестра запретов: это ОТДЕЛЬНЫЙ вопрос владельца (пункт 4
перечня «Human Verification Required»), и смешивать два решения в одном останове значило бы получить
один ответ на два вопроса.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-40-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-49-SUMMARY.md
</context>

<artifacts_produced>
## Артефакты, которые производит этот план

**Новые записи (не символы кода):**

- `overrides[1]` шапки `10-VERIFICATION.md` — ВЕТВЬ `extend-override`: вторая запись отступления с
  полями `must_have`, `reason`, `accepted_by`, `accepted_at`, `accepted_branch`, `covers`,
  `price`, `enforced_by`; либо приписка-комментарий к `overrides[0]` — ВЕТВЬ
  `criterion-stands-violated`;
- раздел `## План 10-53` в `deferred-items.md`, внесённый ПЕРЕД якорем `<!-- deferred-items:end -->`;
- запись журнала окон (только ветвь `criterion-stands-violated`), заведённая командой
  `windows append`;
- абзац основания числа в докстринге `test_criterion_three_holds_by_the_numbers`.

**Символов кода план не заводит ни одного:** правило, принуждающее число, отгружено планом 10-52, и
настоящий план его не переоткрывает.
</artifacts_produced>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Задача 1: РЕШЕНИЕ ВЛАДЕЛЬЦА — расширить область отступления на третий файл и третью сущность или признать критерий 3 нарушенным</name>

  <decision>Расширяется ли область действующего отступления записью, называющей ПОИМЁННО третий файл
(`app/templates/includes/htmx_error_banner.html`) и третью сущность (регистрацию обработчика
`htmx:afterRequest` на узле тела документа, `:227-233`, коммит `4e24d61`), — или критерий 3
роадмапа признаётся НАРУШЕННЫМ, и долг называется записью.</decision>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:191-222` — блок `overrides`
  ЦЕЛИКОМ: комментарий круга о том, почему запись НЕ ЗАСЧИТАНА, все поля существующей записи и
  особенно `covers` и `not_reached_by_this_override`. ⚠️ ЧИТАТЬ ДОСЛОВНО: границу отступления
  объявляет САМА ЗАПИСЬ, и предъявление владельцу обязано цитировать её, а не пересказывать.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:270-325` — запись гэпа
  целиком: причина, три артефакта с претензиями и ОБА пункта `missing` с их формулировками.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:489-509` — вердикт одной
  фразой и абзац «ЧЕГО ЭТОТ ОТКАЗ НЕ ЗНАЧИТ». ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: второй абзац есть половина
  предъявления, и предъявление без него превратилось бы в обвинение правки.
- `.planning/ROADMAP.md`, раздел `### Phase 10`, критерий 3 ДОСЛОВНО. ⚠️ В ПРЕДЪЯВЛЕНИЕ И В ЗАПИСЬ
  ФОРМУЛИРОВКА ПЕРЕНОСИТСЯ ПОБУКВЕННО: отступление от пересказанного критерия есть отступление не
  от того критерия.
- `.planning/phases/10-rychag-components-modal-html/10-40-PLAN.md:144-300` — ЖИВОЙ ПРЕЦЕДЕНТ
  останова этого класса в этой же фазе: форма разделов `<decision>`, `<options>`,
  `<reversibility>`, `<resume-signal>` и способ предъявления замеров ДО остановки. ⚠️ ФОРМА
  НАСЛЕДУЕТСЯ, а не изобретается; второй её формы в проекте не заводится.
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md`, блок
  `overrides` — ПЕРВЫЙ прецедент формы записи в проекте (ветвь `accept-override`, `chubav`,
  2026-09-01).
- `app/templates/includes/htmx_error_banner.html:216-235` — предмет решения ГЛАЗОМ НА ФАКТ:
  единственный блок `<script>`, обёртка признака однократности и ТРИ регистрации, из которых третья
  и есть предмет. ⚠️ ЧИТАТЬ И НЕ ПРАВИТЬ.
- `app/templates/includes/htmx_error_banner.html:130-149` — проза, утверждавшая до 2026-09-11
  «третьего обработчика не заведено» (окно 78). ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: владельцу предъявляется, что
  правка опровергла ЗАПИСАННОЕ РЕШЕНИЕ, а не только критерий.
- `.planning/WINDOWS.md`, запись 74 ЦЕЛИКОМ — цена, которую сама девятая партия назвала и приняла
  (успешный ФОНОВЫЙ обмен гасит непрочитанный настоящий отказ, `T-10-49-05`, disposition `accept`).
- `tests/test_templates/test_components.py`, раздел критерия 3 ПОСЛЕ плана 10-52 — пятое
  утверждение и его объявление. ⚠️ ЧИТАТЬ ПОСЛЕ ВОЛНЫ 1, а не по плану 10-52: владельцу
  предъявляется вывод ПРИБОРА, а не планового ожидания.
  </read_first>

  <context>
**ШЕСТЬ ЗАМЕРОВ, КОТОРЫЕ ИСПОЛНИТЕЛЬ ОБЯЗАН СНЯТЬ КОМАНДАМИ НА HEAD И ПРЕДЪЯВИТЬ ДО ОСТАНОВКИ.**
Замер, снятый чтением отчёта вместо дерева, обесценивает решение ровно тем же способом, каким
одиннадцатый круг отказал фазе: владелец получил бы вывод, а не измерение.

1. **Первая половина критерия держится.** `git diff --numstat 7c1041d..HEAD -- app/static/js/` —
   ожидается ПУСТО. Если не пусто — назвать поимённо.
2. **Вторая половина пала, и чем именно.**
   `git diff --numstat 7c1041d..HEAD -- app/templates/includes/htmx_error_banner.html` и
   `git diff 4e24d61^..4e24d61 -- app/templates/includes/htmx_error_banner.html` — семь исполняемых
   строк, целиком новая регистрация.
3. **Летопись файла.**
   `git log --follow --oneline -- app/templates/includes/htmx_error_banner.html` — два обработчика
   пришли Фазой 8 (`6d2eaf2`), третий Фазой 10 (`4e24d61`).
4. **Вывод прибора, заведённого волной 1.** Перечень регистраций, измеренный ПРАВИЛОМ, а не
   грепом: `uv run python -c` с ввозом помощников из
   `tests.test_templates.test_components` и печатью измеренного перечня; рядом — прогон гейта
   `uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"`. Ожидается
   перечень из двух записей (`ads/form.html` → 8, `includes/htmx_error_banner.html` → 3) и зелёный
   гейт.
5. **Партия не назвала критерий ни разу.** Грепы по `10-49-PLAN.md` и `10-49-SUMMARY.md` на
   «критерий 3» и «D-13». Замер планировщика 2026-09-12: НОЛЬ вхождений в каждом из четырёх
   сочетаний. Переснять и предъявить.
6. **Область действующего отступления — его собственным текстом.** Поля `covers` и
   `not_reached_by_this_override` записи `overrides[0]` ДОСЛОВНО.

**ВЕТВЬ (A) — `extend-override`.** В блок `overrides` шапки встаёт ВТОРАЯ запись по форме первой:
формулировка критерия ПОБУКВЕННО из роадмапа, причина, имя принявшего, дата, идентификатор ветви,
поле `covers`, называющее ТРЕТИЙ файл и ТРЕТЬЮ сущность ПОИМЁННО, поле цены со ссылкой на окно 74 и
поле принуждения с именем пятого утверждения гейта. Критерий 3 после этого закрывается
верификатором как `PASSED (override)` — САМ вердикт пишет круг, а не план.
**Цена ветви, названная прямо:** граница «новая сущность против правки существующей» РАСШИРЯЕТСЯ
вторым разом, и расширение переживает фазу. Первая запись объявила прочтение; вторая объявит, что
прочтение растягивается и на заведение НОВОЙ сущности, если та чинит настоящий дефект, — то есть
исключение, прямо записанное в границе первой записи, перестаёт быть исключением. Всякая следующая
фаза вехи будет опираться на это как на прецедент. Пятое утверждение гейта принуждает ЧИСЛО, а не
границу: оно поймает незаявленную регистрацию и НЕ поймает вопроса «а надо ли было её заводить».

**ВЕТВЬ (B) — `criterion-stands-violated`.** Расширение отклоняется. Критерий 3 признаётся
НАРУШЕННЫМ; запись `overrides[0]` получает приписку с идентификатором ветви, именем и датой,
говорящую, что область НЕ РАСШИРЕНА; долг заводится записью журнала окон и разделом отложенного.
**Цена ветви, названная прямо:** код НЕ ОТКАТЫВАЕТСЯ НИ СТРОКОЙ — откат вернул бы дефект `G-10-5`
(плашка отказа переживала успешный обмен, воспроизведён дословно двумя обходами). То есть фаза
остаётся с кодом, который её собственный критерий запрещает, И С ЗАПИСЬЮ ОБ ЭТОМ. Критерий 3
перестаёт считаться выполненным, и закрыть долг внутри Фазы 10 нечем: закрытие требовало бы либо
переписать чинящий код так, чтобы он не был исполняемым JS в шаблоне, либо амендировать критерий
отдельным решением — а амендмент отвергнут D-13 явно.

⚠️ **ТРЕТЬЕЙ ВЕТВИ — «ОСТАВИТЬ КАК ЕСТЬ» — В ПЕРЕЧНЕ НЕТ, И ЭТО НЕ УПУЩЕНИЕ.** Она и есть
сегодняшнее состояние, и именно она признана негодной: у следующего круга нет способа отличить
принятое отступление от незамеченного, и он поднимет вопрос снова — четвёртым разом в этой фазе.

⚠️ **ЧЕТВЁРТОЙ ВЕТВИ — «ОТКАТИТЬ ПРАВКУ» — В ПЕРЕЧНЕ НЕТ, И ОСНОВАНИЕ ЗАМЕРЕНО.** Правка закрывает
`G-10-5`, снабжена двумя отрицательными контролями и перепроверена прогоном верификатора (13
passed). Откат есть новый дефект, а не закрытие гэпа, и предлагать его владельцу под видом
соблюдения критерия значило бы предложить починку одного критерия ценой другого.
  </context>

  <options>
    <option id="extend-override">
      <name>(A) Область отступления РАСШИРЯЕТСЯ записью, называющей третий файл и третью сущность поимённо</name>
      <pros>Расхождение получает машинно читаемое место, и следующий круг перестаёт поднимать его заново: у него появляется способ отличить ПРИНЯТОЕ отступление от НЕЗАМЕЧЕННОГО — ровно то основание, по которому владелец подписал первую запись 2026-09-09. Правка защитима по существу: она закрывает настоящий дефект `G-10-5`, различает успех и завершение обмена собственным правилом с собственным контролем и перепроверена прогоном верификатора. Цена правки уже названа самой девятой партией и принята ею открыто (окно 74, `T-10-49-05`, disposition `accept`) — то есть владельцу предлагается принять то, что уже описано, а не то, что скрыто. Код не правится ни строкой, и вердикт фазы возвращается к `human_needed`, то есть к состоянию, где её закрывает человек, а не машина. Принуждение числа с этого дня есть: пятое утверждение гейта (план 10-52) краснит ПРОГОН на следующей незаявленной регистрации</pros>
      <cons>Исключение, прямо записанное в границе первой записи («заведение НОВОЙ сущности настоящей записью НЕ принимается»), перестаёт быть исключением — и перестаёт им быть НЕ по общему правилу, а вторым частным разрешением. Граница «новая сущность против правки существующей» после этого не означает почти ничего: следующая фаза вехи прочтёт две записи подряд как «критерий 3 уступает всякий раз, когда правка чинит настоящий дефект», а настоящий дефект найдётся почти всегда. Пятое утверждение принуждает ЧИСЛО, а не границу: оно поймает незаявленную регистрацию и не поймает вопроса, надо ли было её заводить. Документ вехи остаётся с буквальной формулировкой, и согласуют его с деревом теперь ДВЕ записи вместо одной — то есть читателю роадмапа для понимания критерия 3 нужно прочесть ещё и шапку чужого отчёта</cons>
    </option>
    <option id="criterion-stands-violated">
      <name>(B) Критерий 3 признаётся НАРУШЕННЫМ, долг называется записью</name>
      <pros>Граница отступления держится ровно такой, какой её подписал владелец, и второго растягивания не происходит: прецедента «критерий уступает всякий раз, когда правка чинит дефект» в проекте не возникает. Долг НАЗВАН, а не растворён в прочтении, — следующая фаза видит его записью журнала с файлом, строками и коммитом, а не выводом из двух отступлений. Честность фазы в остальном уже измерена и от этой ветви только выигрывает: девятая партия сама завела окна 74-78, сама оставила `G-10-7` проваленным, сама не отметила `FORM-06`, — и признание единственного пропуска ставит запись в тот же ряд. Принуждение числа не зависит от ветви: пятое утверждение гейта заведено планом 10-52 и работает при любом ответе</pros>
      <cons>⚠️ КОД НЕ ОТКАТЫВАЕТСЯ НИ СТРОКОЙ, и это замер, а не смягчение: откат вернул бы `G-10-5` — плашку отказа, переживающую успешный обмен и утверждающую о текущем действии неправду перед кнопкой необратимого удаления. То есть фаза остаётся с кодом, который её собственный критерий запрещает, и с записью об этом. Критерий 3 перестаёт считаться выполненным, и закрыть долг внутри Фазы 10 НЕЧЕМ: оба мыслимых закрытия (переписать починку так, чтобы она не была исполняемым JS в шаблоне; амендировать критерий) лежат вне неё, а второе отвергнуто D-13 явно. Вердикт фазы остаётся `gaps_found` по критерию, который никакая её работа больше не сдвинет, — то есть фаза закрывается с открытым долгом, и это придётся нести следующим фазам вехи</cons>
    </option>
  </options>

  <reversibility rating="one-way">
Запись о расширении области отступления либо запись о нарушении критерия приёмки объявляет, что фаза
считает сделанным, и переживает фазу: следующие круги, следующие фазы вехи и `/gsd-verify-phase`
читают её как ОСНОВАНИЕ. Ветвь (A) сверх того публикует в проекте ВТОРУЮ ступень растягивания
границы «новая сущность против правки существующей» — наследуемую всеми последующими фазами вехи и
уже не отличимую от общего правила. Отмена любой из двух записей задним числом есть переоткрытие
принятого владельцем решения, а не правка строки: первая запись этого класса (2026-09-09) уже
прожила два круга и цитируется обоими. Поэтому дверь гейтируется человеком до единого символа
записи, и режим автопродвижения на неё не действует.
  </reversibility>

  <resume-signal>Ответить РОВНО одним идентификатором: `extend-override` либо
`criterion-stands-violated`, и назвать ИМЯ для поля принявшего и ДАТУ (обе величины идут в запись
дословно и заглушкой не подставляются).
При `extend-override` — подтвердить, что запись охватывает ИМЕННО регистрацию третьего обработчика в
`app/templates/includes/htmx_error_banner.html`, и назвать, распространяется ли она на БУДУЩИЕ
регистрации в том же файле или только на эту (ответ идёт в поле `covers` дословно).
При `criterion-stands-violated` — подтвердить, что код НЕ ОТКАТЫВАЕТСЯ ни строкой, и назвать, куда
записывается долг: в `deferred-items.md` фазы, в реестр окон `.planning/WINDOWS.md` либо в оба.</resume-signal>

  <action>
**(1) СНЯТЬ ВСЕ ШЕСТЬ ЗАМЕРОВ РАЗДЕЛА `<context>` НА HEAD КОМАНДАМИ.** Вывод каждого попадает в
предъявление ДОСЛОВНО. ⚠️ ЗАМЕР 4 СНИМАЕТСЯ ПРИБОРОМ ВОЛНЫ 1, А НЕ ГРЕПОМ: греп даёт число, правило
даёт перечень с объявленным рядом, и разница между ними есть ровно то, чего фазе не хватало.

**(2) ПРЕДЪЯВИТЬ ВЛАДЕЛЬЦУ ШЕСТЬ ЗАМЕРОВ, ДВЕ ВЕТВИ И ЦЕНУ КАЖДОЙ.** Цены переносятся из раздела
`<context>` и из `<options>` ДОСЛОВНО, а не пересказываются. ⚠️ ВМЕСТЕ С ЗАМЕРАМИ ПРЕДЪЯВЛЯЕТСЯ
АБЗАЦ «ЧЕГО ЭТОТ ОТКАЗ НЕ ЗНАЧИТ» одиннадцатого круга: предъявление без него превращается в
обвинение правки, а круг прямо сказал, что правка не плоха.

**(3) ОСТАНОВИТЬСЯ.** ⚠️ ВЫБОР ВНУТРИ ЗАДАЧИ НЕ ДЕЛАЕТСЯ НИ ПРИ КАКИХ УСЛОВИЯХ, включая случай,
когда одна ветвь кажется очевидной, и включая режим автопродвижения: чекпоинт несёт
`gate="blocking-human"`, и он не автоодобряется ни в одном режиме. Ждать ответа владельца.

**(4) ЗАПИСАТЬ ОТВЕТ ДОСЛОВНО.** Идентификатор ветви, имя принявшего, дату и (по ветви) ответ на
уточняющий вопрос `<resume-signal>` — в сводку и в задачу 2. Имя и дата НЕ ВЫДУМЫВАЮТСЯ и заглушкой
не подставляются. ⚠️ ЕСЛИ ВЛАДЕЛЕЦ НАЗОВЁТ ДАТУ, ОТЛИЧНУЮ ОТ ДАТЫ ВНЕСЕНИЯ, расхождение НАЗЫВАЕТСЯ
полем, а не сглаживается: прецедент этой фазы — поле `accepted_by_provenance` записи 2026-09-09.
  </action>

  <acceptance_criteria>
- Все шесть замеров раздела `<context>` сняты КОМАНДАМИ на HEAD, и вывод каждого приведён в сводку
  дословно — не пересказом и не выпиской из отчёта верификации.
- Замер 4 снят ПРИБОРОМ волны 1 (измеренный перечень плюс прогон гейта), а не грепом по дереву.
- Поля `covers` и `not_reached_by_this_override` существующей записи приведены владельцу ДОСЛОВНО.
- Формулировка критерия 3 приведена владельцу ПОБУКВЕННО из `.planning/ROADMAP.md`.
- Владельцу предъявлены ОБЕ ветви с их ценами в формулировках разделов `<context>` и `<options>`
  дословно, вместе с абзацем «ЧЕГО ЭТОТ ОТКАЗ НЕ ЗНАЧИТ».
- Ни одна из двух ветвей не названа предпочтительной ни словом; третья и четвёртая ветви не
  предложены, и основание их отсутствия названо.
- Ответ владельца записан ровно одним идентификатором из двух, вместе с именем и датой; выбор внутри
  задачи не сделан ни при каких условиях.
- `git diff --numstat 9e6b859` по всему дереву пуст: задача 1 ничего не правит (ревизия закреплена
  базой партии и без `..HEAD` — сравнение с рабочим деревом).
  </acceptance_criteria>

  <verify>
    <human-check>Владелец назвал ОДНУ из двух ветвей (`extend-override` либо `criterion-stands-violated`), назвал имя для записи и дату, и ответил на уточняющий вопрос своей ветви. Шесть замеров предъявлены ему ДО ответа и приведены в сводку дословно, включая вывод прибора волны 1.</human-check>
  </verify>

  <done>
Шесть замеров сняты командами на HEAD и предъявлены владельцу вместе с обеими ветвями, их ценами и
абзацем «чего этот отказ не значит»; выбор внутри задачи не сделан; ответ владельца записан
идентификатором ветви, именем, датой и ответом на уточняющий вопрос.
  </done>
</task>

<task type="auto">
  <name>Задача 2: ответ владельца становится записью, число гейта получает основание, а прежняя проза не вычёркивается</name>

  <files>
.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
tests/test_templates/test_components.py
.planning/phases/10-rychag-components-modal-html/deferred-items.md
.planning/WINDOWS.md
  </files>

  <read_first>
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:191-222` — блок `overrides`
  ЦЕЛИКОМ. ⚠️ ЯКОРЬ ЗАПИСИ: новая запись встаёт СЮДА, и ничего другого в шапке не правится.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:1-10` и `:160-191` — поля
  вердикта и счётов (`status`, `score`, `covered_digest`, `behavior_unverified`,
  `overrides_applied`). ⚠️ ЧИТАТЬ И НЕ ПРАВИТЬ НИ ОДНО. Особенно `overrides_applied: 0`: круг
  поставил там нуль С НАЗВАННЫМ ОСНОВАНИЕМ, и это счёт КРУГА, а не счёт записей.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:223-270` — блок
  `re_verification` и разделы `gaps_closed` / `gaps_remaining` / `regressions`. ⚠️ ЧИТАТЬ И НЕ
  ПРАВИТЬ: они принадлежат верификатору.
- `tests/test_planning/test_requirement_completion_follows_verification.py` ЦЕЛИКОМ — что именно
  правило читает в этом отчёте и в реестре требований. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: правка шапки не имеет
  права его уронить, и единственный способ это знать — прочесть, что он читает.
- `tests/test_planning/test_deferred_items_are_idempotent.py:58-160` — `_anchor_lines`,
  `deferred_items_findings`, `anchor_is_required`. ⚠️ ЧИТАТЬ ДОСЛОВНО: форма файла отложенного
  ПРИНУЖДАЕТСЯ, и раздел, дописанный в конец вместо вставки перед якорем, краснит прогон.
- `.planning/phases/10-rychag-components-modal-html/deferred-items.md:1-30` и последние 20 строк —
  правило формы и якорь `<!-- deferred-items:end -->`. ⚠️ РАЗДЕЛ ВСТАВЛЯЕТСЯ ПЕРЕД ЯКОРЕМ, и раздел
  на план РОВНО ОДИН.
- `tests/test_templates/test_components.py`, докстринг `test_criterion_three_holds_by_the_numbers`
  ПОСЛЕ плана 10-52 — куда встаёт абзац основания. ⚠️ ЧИТАТЬ ПОСЛЕ ВОЛНЫ 1, а не по плану 10-52.
- `.planning/STATE.md`, записи D-30/D-32 — опровергнутое либо разрешённое ПОМЕЧАЕТСЯ, а не
  стирается.
- `.planning/WINDOWS.md:1-16` — шапка журнала со счётами и строка о том, что записи ведутся
  КОМАНДОЙ. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: правка журнала руками разошлась бы со счётами шапки молча.
  </read_first>

  <action>
**(1) ВЕТВЬ `extend-override` — ВТОРАЯ ЗАПИСЬ В БЛОКЕ `overrides`, И ПЕРВАЯ НЕ ТРОНУТА.** Поля по
форме первой записи плюс два, которых у неё не было: `price` — ссылка на запись 74 журнала окон с её
формулировкой цены; `enforced_by` — имя пятого утверждения гейта критерия 3 и путь его модуля, то
есть чем именно с этого дня принуждается объявленное число. Поле `must_have` несёт формулировку
критерия 3 ПОБУКВЕННО из роадмапа. Поле `covers` называет файл
`app/templates/includes/htmx_error_banner.html`, строки `227-233`, коммит `4e24d61`, форму правки
(регистрация слушателя на узле тела документа) и ОТВЕТ ВЛАДЕЛЬЦА на уточняющий вопрос — покрыты ли
будущие регистрации в том же файле или только эта. Поля `accepted_by`, `accepted_at`,
`accepted_branch` — дословно из ответа.
⚠️ КОММЕНТАРИЙ КРУГА НАД БЛОКОМ (о том, почему первая запись НЕ ЗАСЧИТАНА) НЕ ВЫЧЁРКИВАЕТСЯ И НЕ
ПРАВИТСЯ: он ВЕРЕН для своего дня и для своей записи, и рядом с ним встаёт новая запись, а не
переписанный старый комментарий.

**(2) ВЕТВЬ `criterion-stands-violated` — ПРИПИСКА, А НЕ ПЕРЕПИСЫВАНИЕ.** К существующей записи
`overrides[0]` добавляется приписка-комментарий, называющая идентификатор ветви, имя и дату ответа и
говорящая ПРЯМО: область НЕ РАСШИРЕНА, критерий 3 признан НАРУШЕННЫМ решением владельца, код не
откатывается. ⚠️ ВТОРОЙ ЗАПИСИ `overrides` В ЭТОЙ ВЕТВИ НЕ ЗАВОДИТСЯ: запись отступления для
ОТКЛОНЁННОГО расширения была бы записью о том, чего не произошло, и следующий круг прочёл бы её как
принятое отступление. Долг заводится записью журнала окон КОМАНДОЙ
`node .claude/gsd-core/bin/gsd-tools.cjs windows append --kind unmet-truth --phase 10 --file app/templates/includes/htmx_error_banner.html --description "<текст>"`,
и текст называет: критерий 3 признан нарушенным решением владельца (ветвь, имя, дата), семь
исполняемых строк `:227-233` коммита `4e24d61`, что закрытие внутри Фазы 10 недостижимо, и что откат
запрещён ценой возврата `G-10-5`. ⚠️ ЖУРНАЛ ПРАВИТСЯ КОМАНДОЙ, А НЕ РУКАМИ: счёта шапки ведёт она, и
ручная строка разошлась бы с ними молча.

**(3) ОБЕ ВЕТВИ — ОСНОВАНИЕ ЧИСЛА В ДОКСТРИНГЕ ГЕЙТА.** Абзац при пятом утверждении называет, ЧЕМ
объявлено значение трёх регистраций в заготовках: при `extend-override` — расширенным отступлением с
ветвью, именем, датой и указанием на поле шапки, где оно живёт; при `criterion-stands-violated` —
признанным нарушением критерия 3 с той же тройкой величин и указанием на запись журнала окон.
⚠️ В ОБЕИХ ВЕТВЯХ АБЗАЦ ГОВОРИТ ОДНО И ТО ЖЕ О ПРЕДМЕТЕ ПРАВИЛА: правило ловит НЕЗАЯВЛЕННОЕ, а не
заявленное, и объявленное число менять вправе только новое решение владельца, а не замер
исполнителя. ⚠️ ФРАЗА ПЛАНА 10-52 «основание вносит план 10-53» ЗАМЕНЯЕТСЯ ВНЕСЁННЫМ ОСНОВАНИЕМ, и
это единственное место, где текст плана 10-52 правится: ссылка на будущее, оставленная после того,
как будущее пришло, есть ровно окно 78 в миниатюре.

**(4) ОБЕ ВЕТВИ — РАЗДЕЛ `## План 10-53` В ФАЙЛЕ ОТЛОЖЕННОГО, ВСТАВЛЕННЫЙ ПЕРЕД ЯКОРЕМ.** Содержание:
(а) ответ владельца одной строкой с ветвью, именем и датой; (б) что этим НЕ закрыто — три
человеческих пункта и `FORM-06`, адресат которых план 10-54 и человек; (в) окно 78 (проза заготовок
разошлась с деревом ДВУМЯ утверждениями) — по-прежнему НЕ ПОЧИНЕНО, потому что `app/templates/`
настоящему плану запрещён, и адресат назван; (г) отставание реестра запретов числом
(`prohibitions_unresolved_total: 200` при 221 плюс запреты настоящей партии) с адресатом — пункт 4
перечня «Human Verification Required», отдельное решение владельца. ⚠️ РАЗДЕЛ НА ПЛАН РОВНО ОДИН, И
ВСТАВЛЯЕТСЯ ОН ПЕРЕД ЯКОРЕМ: дописывание в конец краснит правило формы, и основание этого правила —
замер, а не опасение (раздел 10-39 удвоился бы вторым прогоном).

**(5) ЧТО НЕ ПРАВИТСЯ НИ В ОДНОЙ ВЕТВИ — ПЕРЕЧИСЛЯЕТСЯ ПЕРЕД ПРАВКОЙ, А НЕ ПОСЛЕ.** Поля вердикта и
счётов шапки отчёта; блок `re_verification`; разделы `gaps`, `deferred`, `advisory`,
`behavior_unverified_items`, `coincidental_reliance_items`; тело отчёта ниже шапки; `10-UAT.md`
целиком; `.planning/REQUIREMENTS.md` целиком; `10-PROHIBITIONS-SUBJECT.md` целиком;
`.planning/ROADMAP.md` целиком; `app/` целиком.

**(6) ЗАПИСАТЬ В СВОДКУ ОСТАТОК ВОСЕМЬЮ ПУНКТАМИ, БЕЗ СМЯГЧЕНИЯ.** Перечень тот же, что у плана
10-52, и он не сокращается по причине «уже назван»: сводка читается отдельно от чужой сводки.
Пункты: три человеческих (шаг 2.8, шаг 4.4, подпись на приёмке — адресат план 10-54 и человек;
`FORM-06` закрывается ПОДПИСЬЮ); запись 75 журнала (перечень прочих путей устаревшей сводки не
снимался; 10-50 закрыл только фрагментную ветвь); запись 76 (две заготовки накрывают друг друга
всегда; `T-10-51-04`, `accept`, владельца нет; тождественна `IN-03`); запись 78 (проза разошлась с
деревом двумя утверждениями); запись 79 (суита недетерминирована по времени суток; предмет вне Фазы
10, отложено владельцем); запись 74 (успешный фоновый обмен гасит непрочитанный отказ;
`T-10-49-05`, `accept`; тождественна `IN-02`); пункт 4 «Human Verification Required» (221 запрет,
`prohibitions_fully_enforced: 0`, разрешение ограничено `seventh-batch-only`); находка ревизии
`IN-01` (инвентарь внеполосных блоков поднят числом, отказ печатает два числа вместо разницы
множеств) — вне объёма партии.
  </action>

  <acceptance_criteria>
- Внесена ровно ОДНА форма записи — та, что соответствует названной владельцем ветви; вторая форма
  в файле отсутствует.
- При `extend-override`: запись несёт все поля формы первой записи плюс `price` и `enforced_by`;
  `covers` называет файл, строки, коммит, форму правки и ответ владельца об охвате будущих
  регистраций; формулировка критерия сличена с `.planning/ROADMAP.md` ПОСИМВОЛЬНО программно, и
  способ сличения приведён в сводке.
- При `criterion-stands-violated`: второй записи `overrides` в файле НЕТ; приписка называет ветвь,
  имя, дату и прямо говорит, что область не расширена; запись журнала окон заведена КОМАНДОЙ, и её
  идентификатор приведён в сводке.
- Первая запись отступления и комментарий круга над блоком присутствуют в файле ДОСЛОВНО и не
  правлены — проверяется диффом.
- Поля вердикта и счётов стоя́т в файле теми же значениями, какими их оставил одиннадцатый круг:
  `status: gaps_found`, `score: 8/10 must-haves verified`, `behavior_unverified: 1`,
  `overrides_applied: 0` — утверждается СОДЕРЖИМЫМ файла, а не диффом (дифф без закреплённой ревизии по зафиксированной
  правке пуст и прошёл бы вакуумно); блок `re_verification` и разделы `gaps`, `deferred`, `advisory`
  в диффе НЕ ПРИСУТСТВУЮТ.
- Докстринг гейта критерия 3 называет основание числа тремя величинами (ветвь, имя, дата) и место,
  где решение живёт; фраза «основание вносит план 10-53» из текста ушла.
- Раздел `## План 10-53` в файле отложенного РОВНО ОДИН и стои́т ПЕРЕД якорем; якорь в файле один и
  последний.
- `.planning/REQUIREMENTS.md`, `10-PROHIBITIONS-SUBJECT.md` и `app/` в диффе от базы партии
  отсутствуют полностью; НЕПРАВЛЕННОСТЬ `.planning/ROADMAP.md` утверждается ИНВАРИАНТОМ
  ФОРМУЛИРОВКИ КРИТЕРИЯ 3 (вхождение ОДНО), а не диффом — его законно правит шаг трекинга
  исполнения, и гейт на диффе краснел бы на законной правке. ⚠️ `10-UAT.md` утверждается ИНВАРИАНТАМИ СОДЕРЖИМОГО
  (`status: partial`, `checks_declared: 6`), а не диффом: его правит план 10-54 ТОЙ ЖЕ ВОЛНЫ, и
  проверка на дифф объявила бы законную правку соседа нарушением настоящего плана.
- Остаток назван ВОСЕМЬЮ пунктами со ссылками на записи журнала и находки ревизии; ни один не назван
  закрытым или частично закрытым.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата, либо строка "no tests ran" в выводе, либо в итоговой строке есть слово "failed" — правка планировочной записи уронила гейт формы отложенного, гейт отметки требования либо гейт самозаверения обхода</fails_when>
    <automated>uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"</automated>
    <fails_when>ненулевой код возврата, либо итоговая строка не начинается с "1 passed" — правка докстринга задела тело правила либо его отбор</fails_when>
    <automated>grep -c -e '^status: gaps_found$' -e '^score: 8/10 must-haves verified$' -e '^behavior_unverified: 1$' -e '^overrides_applied: 0$' .planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md</automated>
    <fails_when>вывод НЕ РАВЕН "4" — хотя бы одно поле вердикта либо счёта сдвинуто с того значения, которое оставил одиннадцатый круг, то есть план вынес вердикт вместо верификатора. ⚠️ ЭТО УТВЕРЖДЕНИЕ О СОДЕРЖИМОМ ФАЙЛА, А НЕ О ДИФФЕ, И ЭТО РЕШЕНИЕ: проверка диффа без закреплённой ревизии по уже ЗАФИКСИРОВАННОЙ правке пуста, и такой гейт проходил бы ВАКУУМНО ровно после коммита задачи. Четыре объявленных значения сняты планировщиком 2026-09-12 на `9e6b859` командой того же вида — вывод был "4"</fails_when>
    <automated>git diff --numstat 9e6b859 -- app .planning/REQUIREMENTS.md .planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md</automated>
    <fails_when>вывод НЕПУСТ — тронут файл из перечня запретов: дерево приложения, реестр требований либо реестр запретов фазы. ⚠️ РЕВИЗИЯ ЗАКРЕПЛЕНА (`9e6b859` — база партии) И БЕЗ `..HEAD`: сравнение идёт РАБОЧЕГО ДЕРЕВА с базой, поэтому команда видит правку и до коммита, и после него. ⚠️ `10-UAT.md` В ЭТОМ ПЕРЕЧНЕ НАМЕРЕННО ОТСУТСТВУЕТ, И ЭТО НЕ ПОСЛАБЛЕНИЕ ЗАПРЕТА: артефакт обхода правит план 10-54 ТОЙ ЖЕ ВОЛНЫ, и проверка на дифф объявила бы законную правку соседа нарушением настоящего плана. Запрет утверждается СЛЕДУЮЩЕЙ командой — инвариантами содержимого, которые верны и после правки соседа ⚠️ `.planning/ROADMAP.md` В ЭТОМ ПЕРЕЧНЕ НАМЕРЕННО ОТСУТСТВУЕТ, И ЭТО НЕ ПОСЛАБЛЕНИЕ ЗАПРЕТА: его ЗАКОННО правит шаг трекинга исполнения (`roadmap update-plan-progress` ставит отметку завершения плана), и гейт на диффе краснел бы на законной правке. Запрет утверждается СЛЕДУЮЩЕЙ командой — инвариантом формулировки критерия 3.</fails_when>
    <automated>grep -c 'новых строк JS фаза не добавляет (машинно: диффом по `app/static/js/` и шаблонам)' .planning/ROADMAP.md</automated>
    <fails_when>вывод НЕ РАВЕН "1" — формулировка КРИТЕРИЯ 3 в роадмапе тронута, а её амендмент отвергнут решением D-13 явно («документ вехи держится ФИКСИРОВАННЫМ, а прочтение есть предмет CONTEXT.md»), и ни одна ветвь решения владельца его не предлагает. ⚠️ ЭТО ИНВАРИАНТ СОДЕРЖИМОГО, А НЕ ПРОВЕРКА ДИФФА, И ЭТО НЕСУЩЕЕ РЕШЕНИЕ: `roadmap update-plan-progress` ЗАКОННО правит `.planning/ROADMAP.md` в ходе исполнения КАЖДОГО плана — он ставит отметку завершения и вынуждает привести прозу счёта, — поэтому гейт «дифф роадмапа пуст» краснел бы у каждого плана фазы на законной правке трекинга. Запрет касается ФОРМУЛИРОВКИ КРИТЕРИЯ, и утверждается именно она. Замер планировщика 2026-09-12: вхождение ОДНО, строка 276</fails_when>
    <automated>grep -c -e '^status: partial$' -e '^checks_declared: 6$' .planning/phases/10-rychag-components-modal-html/10-UAT.md</automated>
    <fails_when>вывод НЕ РАВЕН "2" — артефакт обхода стал терминальным либо у него сдвинулось объявленное число проверок, то есть партия подошла к самозаверению. ⚠️ ЭТО ИНВАРИАНТ СОДЕРЖИМОГО, А НЕ ДИФФА, И ПОТОМУ ОН ВОЛНОБЕЗОПАСЕН: он верен и до правки плана 10-54, и после неё, потому что обоим планам двигать эти два поля ЗАПРЕЩЕНО. Значения сняты планировщиком 2026-09-12 на `9e6b859` — вывод был "2"</fails_when>
    <automated>uv run pytest tests/ -q --deselect tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard</automated>
    <fails_when>ненулевой код возврата, либо в итоговой строке есть слово "failed", либо в выводе есть строка "ERROR: not found" (изъятое имя разошлось с деревом). ⚠️ ИЗЪЯТИЕ ПОИМЁННОЕ И НАЗВАНО ПРИЧИНОЙ, А НЕ УДОБСТВОМ: правило записи 79 требует равенства двух чисел, равных лишь часть суток, и краснеет между 00:00 и ~05:00 UTC без единой правки (замер владельца: 02:03 UTC red, 05:10 UTC green). Гейт, опирающийся на его зелень, объявлял бы план неисполнимым по часам, а не по дереву</fails_when>
  </verify>

  <done>
Ответ владельца стои́т машинно читаемым местом в той форме, которая соответствует названной ветви, и
только в ней; первая запись отступления и комментарий круга не тронуты; поля вердикта не правлены;
число гейта получило основание тремя величинами; раздел отложенного внесён перед якорем ровно один;
остаток назван восемью пунктами.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| решение владельца → запись артефакта | между ответом человека и полем, которое читает следующий круг, стои́т исполнитель, и всякая его вольность становится решением, которого владелец не принимал |
| план закрытия гэпов → вердикт верификатора | шапка отчёта несёт и записи, вносимые планами, и поля вердикта, принадлежащие кругу; граница между ними не принуждается ничем, кроме перечня запретов плана |
| запись отступления → следующие фазы вехи | принятая запись читается как ПРАВИЛО ПРОЕКТА и переживает фазу, поэтому её область есть решение о вехе, а не о партии |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-53-01 | Elevation of Privilege | исполнитель, выбравший ветвь за владельца, потому что одна кажется очевидной | high | mitigate | `checkpoint:decision` с `gate="blocking-human"` ПЕРЕД задачей записи; автопродвижение на этот гейт не действует ни в одном режиме; запрет во frontmatter назван отдельной записью |
| T-10-53-02 | Spoofing | запись отступления, охват которой шире принятого владельцем | high | mitigate | `covers` заполняется ОТВЕТОМ на уточняющий вопрос `<resume-signal>` (покрыты ли будущие регистрации того же файла или только эта); формулировка критерия сличается с роадмапом посимвольно программно |
| T-10-53-03 | Tampering | правка полей вердикта отчёта планом: `status`, `score`, `overrides_applied` | high | mitigate | перечень непрáвимого выписан перед правкой; гейт `«verify»` грепает дифф отчёта на эти поля и требует ПУСТОГО вывода |
| T-10-53-04 | Repudiation | вторая запись `overrides` в ветви отклонения — следующий круг прочёл бы её как принятое отступление | high | mitigate | в ветви `criterion-stands-violated` второй записи не заводится вовсе; ответ становится припиской к существующей записи и записью журнала окон, заведённой КОМАНДОЙ |
| T-10-53-05 | Repudiation | ссылка «основание вносит план 10-53», оставленная в докстринге после того, как основание внесено | medium | mitigate | действие (3) правит ровно эту фразу и заменяет её внесённым основанием; акцептанс требует её отсутствия |
| T-10-53-06 | Denial of Service | раздел файла отложенного, дописанный в конец вместо вставки перед якорем: второй прогон удвоил бы его | medium | mitigate | раздел вставляется ПЕРЕД якорем, разделов на план ровно один, и оба свойства принуждаются правилами `test_every_deferred_items_file_stays_append_safe` и `test_the_anchor_closes_every_live_deferred_file` |
| T-10-53-07 | Information Disclosure | предъявление владельцу, собранное пересказом отчёта вместо замеров дерева | high | mitigate | шесть замеров снимаются КОМАНДАМИ на HEAD, четвёртый — прибором волны 1; акцептанс требует дословного вывода каждого |
| T-10-53-08 | Tampering | запись журнала окон, внесённая руками в обход команды ведения: счёта шапки разошлись бы молча | medium | mitigate | запись заводится `windows append` с явными `--kind`, `--phase`, `--file`, `--description`; идентификатор записи приводится в сводке |
| T-10-53-SC | Tampering | установки npm/pip/cargo | n/a | accept | план не несёт НИ ОДНОЙ установки пакетного менеджера |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning/ -q` — зелено: гейт формы отложенного, гейт отметки требования
   и гейт самозаверения обхода не тронуты правкой записей.
2. `uv run pytest tests/test_templates/test_components.py -q -k "criterion_three"` — РОВНО ОДНО
   собранное правило, зелено: правка докстринга не задела тела.
3. Поля вердикта и счётов `10-VERIFICATION.md` стоя́т теми же значениями, какими их оставил
   одиннадцатый круг — утверждается СОДЕРЖИМЫМ файла (четыре объявленных значения), а не диффом.
4. `git diff --numstat 9e6b859 -- …` пуст по `app/`, `.planning/REQUIREMENTS.md` и
   `10-PROHIBITIONS-SUBJECT.md`; ревизия закреплена базой партии и без `..HEAD`, поэтому проверка
   видит правку и до коммита, и после него. Формулировка критерия 3 в `.planning/ROADMAP.md` и шапка
   `10-UAT.md` утверждаются ИНВАРИАНТАМИ СОДЕРЖИМОГО: первую законно правит шаг трекинга, второй —
   план 10-54 той же волны.
5. `uv run pytest tests/ -q --deselect tests/test_pages/test_admin_panel.py::test_the_overview_error_number_matches_the_users_own_dashboard`
   — зелено; изъятие названо причиной записи 79.
6. Ответ владельца приведён в сводке идентификатором ветви, именем, датой и ответом на уточняющий
   вопрос — дословно, без заглушек.
</verification>

<success_criteria>
- Цена предъявлена владельцу шестью замерами, снятыми командами на HEAD, вместе с обеими ветвями и
  ценой каждой; выбор внутри задачи не сделан.
- Ответ владельца стои́т машинно читаемым местом, и следующий круг читает его полем, а не выводит из
  диффа.
- Число трёх регистраций в гейте критерия 3 имеет ОСНОВАНИЕ, названное тремя величинами, и правило
  по-прежнему ловит незаявленное, а не заявленное.
- Первая запись отступления, комментарий круга над блоком и поля вердикта не тронуты ни одним
  символом.
- Раздел отложенного внесён перед якорем ровно один; запись журнала (в своей ветви) заведена
  командой.
- `10-UAT.md` не правлен, `FORM-06` не отмечен выполненным, `G-10-7` остаётся `status: failed`,
  реестр запретов не тронут — и остаток назван восемью пунктами без смягчения.
</success_criteria>

<output>
Create `.planning/phases/10-rychag-components-modal-html/10-53-SUMMARY.md` when done
</output>
