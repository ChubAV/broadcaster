---
phase: 10-rychag-components-modal-html
plan: 32
type: execute
wave: 4
# ⚠️ ВОЛНА 4 — СЛЕДСТВИЕ ТРЁХ ЗАВИСИМОСТЕЙ, И ВСЕ ТРИ ПРЕДМЕТНЫЕ, А НЕ ТОЛЬКО ФАЙЛОВЫЕ.
# (1) Каноническое утверждение истинной границы сверки заводит план 10-25 (волна 1) — площадки
#     переводятся на ССЫЛКУ, и ссылаться раньше, чем канон существует, нельзя: правка родила бы
#     ПЯТУЮ копию утверждения вместо снятия четырёх.
# (2) Четыре продуктовых модуля правит план 10-28 (волна 2), `app/pages/schedules.py` — план 10-24
#     (волна 1).
# (3) ⚠️ ЗАВИСИМОСТЬ ОТ 10-29 БЫЛА ПРОПУЩЕНА, И ЭТО ИСПРАВЛЕНО ЗАМЕРОМ, А НЕ ПЕРЕСТРАХОВКОЙ:
#     задача 3 читает `10-29-SUMMARY.md` и обязана назвать СУММУ закрытых входов по сводкам
#     10-24 + 10-28 + 10-29. План 10-29 стои́т в волне 3, а объявленная волна была та же — то есть
#     артефакт, который задача 3 читает, не был гарантирован к моменту чтения. Волна:
#     max(1, 1, 2, 3) + 1 = 4.
#     Пересечения по файлам с соседями волны 4 нет: 10-30 правит два модуля суиты и реестр окон,
#     10-31 — `app/pages/htmx.py` и `tests/test_pages/test_htmx_gates.py`; настоящий план не
#     трогает ни одного из них.
depends_on: ["10-24", "10-25", "10-28", "10-29"]
gap_closure: true
files_modified:
  - app/pages/history.py
  - app/pages/accounts.py
  - app/pages/ads.py
  - app/pages/account_groups.py
  - app/pages/schedules.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ БЕЗОПАСНОСТИ (поля во frontmatter не заводятся, форма унаследована от
# планов 10-12…10-31): `security_asvs_level` → `1`, `security_block_on` → `"high"`,
# `security_enforcement` → `true`.

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` → sample_count 0, factor 1, applied false.
  # `confidence` ВЫВЕДЕНА ИЗ ЧИСЛА ОБРАЗЦОВ (0 < min_samples 3), а не назначена автором.
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ЧЕТВЁРТАЯ КОПИЯ СНЯТОГО ПЕРЕЧНЯ ПОТРЕБИТЕЛЕЙ ГАРДА УБРАНА ИЗ ФАЙЛА, ГДЕ ОНА ПЕРЕЖИЛА ОПРОВЕРЖЕНИЕ СВОЕГО БЛИЗНЕЦА: на её месте стои́т указатель на общего владельца и прямое утверждение, что чисел потребителей здесь не ведут"
    - "НИ ОДНА ПЛОЩАДКА ВЫЗОВА ГАРДА БОЛЬШЕ НЕ ВЕДЁТ СОБСТВЕННОГО СЧЁТА ПОТРЕБИТЕЛЕЙ: перечень, который надо не забыть исправить, забывают — и это доказано самой снятой строкой, разошедшейся вчетверо"
    - "ЗАГОЛОВКИ СВЕРКИ НА ПЛОЩАДКАХ ГОВОРЯТ ИСТИННУЮ ГРАНИЦУ, А НЕ ПОВТОРЯЮТ ОПРОВЕРГНУТУЮ ЧЕТЫРЕЖДЫ: сверка стои́т после прав и ДО ЧТЕНИЯ ЦЕЛЕВОЙ СТРОКИ, а чтение субъекта уже произошло — и каждая площадка СССЫЛАЕТСЯ на каноническое утверждение, а не хранит свою редакцию"
    - "ЗАПИСЬ О ЗАКРЫТОСТИ СЕМЬИ МАРШРУТОВ ПРИВЕДЕНА К ЗАМЕРУ: она называет, что закрыто сегодня, чем закрыто и каким правилом стережётся, — и это ДВА правила, а не одно, потому что предметов два"
    - "У ОДНОГО ПОМОЩНИКА — ОДНА ДЕЙСТВУЮЩАЯ ЗАПИСЬ ОБ ОПРОВЕРГНУТОМ, А НЕ ДВЕ, ССЫЛАЮЩИЕСЯ ДРУГ НА ДРУГА ЧЕРЕЗ ВЕСЬ ДОКСТРИНГ: сведение сохраняет ПРЕДМЕТ каждой, дату и круг"
    - "ЧИСЛО НАЗВАННЫХ ПРЕДМЕТОВ ПОСЛЕ СВЕДЕНИЯ НЕ УМЕНЬШИЛОСЬ, А ЧИСЛО СТРОК УМЕНЬШИЛОСЬ: это и есть отличие сведения от стирания, и оба числа сняты замером"
  artifacts:
    - "app/pages/history.py — заголовок площадки вызова гарда без собственного счёта потребителей, со ссылкой на общего владельца"
    - "app/pages/accounts.py, app/pages/ads.py, app/pages/account_groups.py, app/pages/schedules.py — четыре заголовка сверки, ссылающиеся на каноническое утверждение истинной границы вместо собственной редакции"
    - "app/pages/schedules.py — запись о закрытости входов приведена к замеру пятой партии и называет ОБА стерегущих правила; докстринг помощника чтения поля контекста несёт ОДНУ действующую запись об опровергнутом"
  key_links:
    - "ссылка на каноническое утверждение → четыре площадки: единственное, чем утверждение о порядке проверок не разъедется четырьмя редакциями; ломается — правится одна из четырёх, и через круг читатель держит два несогласных утверждения об одном порядке, ровно как это случилось со счётом потребителей"
    - "снятый счёт потребителей → гейты полноты: единственное, чем число не начнёт отставать от дерева снова; ломается — следующий потребитель гарда появляется, ни один комментарий не правится, и запись врёт молча"
    - "запись о закрытости → замер: единственное, чем «семья закрыта» станет утверждением, проверяемым чтением двух правил; ломается — читатель принимает закрытие ЧАСТИ за закрытие целого, то есть повторяет обман, названный самим этим докстрингом"
  prohibitions:
    - statement: "опровергнутые формулировки НЕ СТИРАЮТСЯ: сведение сохраняет предмет, дату и круг каждой записи, а число названных предметов сличается до и после — идиома есть записанное решение проекта (D-30/D-32)"
      status: flagged-unverified
      verification: none
    - statement: "тела обработчиков и порядок проверок в них НЕ ПРАВЯТСЯ: предмет плана — записи, и правка порядка под текст была бы подгонкой кода под комментарий"
      status: flagged-unverified
      verification: none
    - statement: "машинно читаемая разметка слоёв утверждения НЕ ВВОДИТСЯ: соглашение о ней действует на КАЖДЫЙ файл проекта, то есть принадлежит владельцу ВЕХИ, и окно 52 реестра остаётся открытым с этим основанием"
      status: flagged-unverified
      verification: none
    - statement: "граница гарда «запрос без обоих заголовков пропускается» НЕ ПЕРЕСМАТРИВАЕТСЯ: она есть записанное закрытие прежнего круга, намеренно не переоткрытое ревизией пятого круга"
      status: flagged-unverified
      verification: none
    - statement: "летопись имени границы величины и её основания НЕ ДУБЛИРУЮТСЯ: они переехали в нейтральный модуль планом 10-24, и вторая копия текста разошлась бы с первой молча"
      status: flagged-unverified
      verification: none

  # ⚠️ РЁБРА ЗОНДА ПОКРЫТИЯ — ПОЛНАЯ ЛЕТОПИСЬ ПАРТИИ СТОИТ В `10-24-PLAN.md`. Настоящий план
  # рёбер не сдвигает: его предмет — записи, а не поведение.
---

<objective>
Записи в четырёх продуктовых модулях перестают утверждать больше, чем исполняет код.

Три находки пятого круга, и все три — один класс:
`WR-01` — счёт потребителей гарда, названный четвёртой партией опровергнутым в общем модуле, УЦЕЛЕЛ
третьим файлом дальше, внутри функции, которую эта же фаза правила: он говорит о трёх при
замеренных тринадцати;
`WR-02` — четыре заголовка сверки, дословно одинаковые, утверждают, что сверка стои́т до ЛЮБОГО
обращения к базе, тогда как чтение субъекта уже произошло внутри разбора cookie;
`IN-04` — докстринг помощника чтения поля контекста несёт ДВЕ живые записи об опровергнутом, одна из
которых ссылается вперёд на другую через весь текст.

Purpose: ревизия называет `WR-01` «собственным объявленным классом дефекта этой фазы, оставленным на
месте ровно тем планом, который опроверг его близнеца». Партия чинит и копию, и накопление, которое
её спрятало, — и порядок между ними назван: план 10-25 волны 1 завёл КАНОНИЧЕСКОЕ место, на которое
площадки ссылаются; настоящий план переводит площадки на ссылку. Обратный порядок родил бы пятую
копию.

⚠️ **ЗАПИСЬ О ЗАКРЫТОСТИ ВХОДОВ ПРИВОДИТСЯ К ЗАМЕРУ, А НЕ СУЖАЕТСЯ, И ЭТО РАСХОЖДЕНИЕ С БУКВОЙ
ПРЕДПИСАНИЯ РЕВИЗИИ, НАЗВАННОЕ ПРЯМО.** Ревизия предписала «удалить следствие, что семья маршрутов
закрыта: она закрыта для одного из четырёх членов». Предписание писалось на дереве, где остальные три
были неограничены. К моменту исполнения настоящего плана планы 10-24, 10-28 и 10-29 закрыли ВСЕ
двадцать девять входов страничного слоя, и запись, суженная по букве предписания, стала бы У́ЖЕ
дерева — то есть тем же дефектом с обратным знаком. Запись приводится к ЗАМЕРУ: что закрыто, чем и
какими правилами стережётся.

⚠️ **ЧИСЛО ДВАДЦАТЬ ДЕВЯТЬ ПЕРЕСЧИТАНО ПО САМИМ ПЛАНАМ, А НЕ ПЕРЕПИСАНО.** Прежняя редакция говорила
«двадцать восемь»; сумма по планам её не подтвердила. Слагаемые: **10-24** — 1 (`ad_id` маршрута
удаления объявления); **10-28** — 17 (15 идентификаторов адреса: 7 в модуле групп аккаунта, 4 в
модуле аккаунтов, 2 в модуле объявлений, 2 в модуле журнала — плюс 2 параметра запроса: курсор
постраничного вывода групп и признак раскрытого расписания редактора); **10-29** — 11
идентификаторов адреса административного модуля. **1 + 17 + 11 = 29.** Это ОРИЕНТИР планировщика,
снятый чтением планов; истиной его делает замер исполнителя по сводкам трёх планов (задача 3), и
расхождение с ориентиром есть НАХОДКА, называемая поимённо, а не повод подогнать число под ожидание.
То же число стои́т в `<objective>` плана 10-30 и в его задаче 2 — три места обязаны говорить одно.

Output: одна копия одного утверждения на каждый предмет, ссылки вместо редакций и записи, равные
дереву.
</objective>

<reversibility>
  <decision rating="reversible">
Перевод четырёх заголовков сверки на ссылку и снятие счёта потребителей. Откат — правка текста;
поведение продукта не меняется ни на байт.
  </decision>
  <decision rating="reversible">
Сведение двух записей об опровергнутом в одну. Откат — правка текста. ⚠️ Цена названа: сведение
СОХРАНЯЕТ предмет и дату каждой, и проверяется это счётом названных предметов до и после — без него
откат восстанавливал бы текст, а не запись.
  </decision>
</reversibility>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-rychag-components-modal-html/10-REVIEW.md
@.planning/phases/10-rychag-components-modal-html/10-25-SUMMARY.md
@.planning/phases/10-rychag-components-modal-html/10-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: четвёртая копия снятого перечня потребителей убрана оттуда, где она пережила опровержение своего близнеца (`WR-01`)</name>

  <files>
app/pages/history.py
  </files>

  <read_first>
- `app/pages/history.py:905-925` — заголовок площадки вызова гарда внутри повтора отправки. ⚠️ ЭТО
  ПРЕДМЕТ ПРАВКИ: там записано число потребителей, верное для момента своей записи и пережившее
  появление остальных.
- `app/pages/common.py:695-760` — докстринг гарда ПОСЛЕ правок плана 10-25: одна действующая запись
  об опровергнутом перечне, каноническое утверждение границы сверки, названное именем, и абзац о
  границе вселенных с двумя замеренными числами. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: текст, на который
  переводится площадка, обязан быть взят ОТТУДА, а не написан заново.
- `10-18-SUMMARY.md` — как четвёртая партия опровергла близнеца этой записи и какими словами.
  ⚠️ ЧИТАТЬ: форма опровержения наследуется, а не изобретается.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `### WR-01` целиком,
  включая перечень из восьми файлов и тринадцати мест вызова.
  </read_first>

  <behavior>
1. Заголовок площадки вызова гарда в модуле журнала не содержит числа потребителей.
2. На его месте стои́т указатель на общего владельца гарда и прямое утверждение, что чисел
   потребителей здесь не ведут.
3. Утверждение называет ПРИЧИНУ: перечень, который надо не забыть исправить, забывают — и это
   доказано самой снятой строкой.
4. Утверждение называет, ЧТО держит полноту вместо перечня: гейты, названные докстрингом самого
   гарда.
5. Летопись площадки сохранена: откуда взялась приватная копия и каким планом она снята.
6. Ни одна строка тела обработчика не тронута.
  </behavior>

  <action>
**(1) Замерить ДО.** Прогоном по дереву: сколько файлов `app/` зовут гард и сколько в них мест
вызова. ⚠️ ОРИЕНТИР ПЛАНИРОВЩИКА: восемь файлов, тринадцать мест вызова (замер на дереве до
исполнения партии). Исполнитель ставит СВОЙ замер; расхождение — находка. Замер привести в сводку:
он и есть доказательство, что число в снимаемой строке разошлось с деревом, и во сколько раз.

**(2) Переписать заголовок в форму указателя.** Три обязательные части, и число потребителей ни в
одной из них: (а) гард — ОБЩИЙ на проект, с указанием модуля; (б) летопись площадки — здесь он жил
приватной копией с такого-то плана, копия снята таким-то; (в) чисел потребителей здесь НЕ ВЕДУТ,
потому что перечень, который надо не забыть исправить, забывают — доказано этой самой строкой,
разошедшейся во столько-то раз (число из замера пункта (1)); полноту держат гейты, названные
докстрингом самого гарда.

**(3) НЕ ОБЪЯВЛЯТЬ АВТОРА ПРЕЖНЕЙ ЗАПИСИ ОШИБШИМСЯ.** Прежнее число было верным для состава
потребителей на момент своей записи и пережило появление остальных — та же форма, которой фаза
записала свои летописи чисел.

**(4) Проверить, что четвёртой копии больше нет НИГДЕ.** Прогоном по дереву `app/`: у скольких мест
вызова гарда в предшествующем комментарии стои́т число потребителей. Ожидаемое значение — ноль; если
найдётся пятая копия, это НАХОДКА, она называется поимённо и снимается тем же движением.

**(5) Тело обработчика НЕ ТРОГАТЬ.**

**(6) Чего задача НЕ делает.** Не трогает заголовков сверки на четырёх площадках (задача 2). Не
трогает докстринга гарда — он приведён планом 10-25. Не заводит машинного правила против чисел в
комментариях: соглашение о разметке слоёв утверждения принадлежит владельцу вехи (окно 52), и правило
над всеми комментариями проекта было бы им.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_history_retry.py tests/test_pages/test_origin_guard_on_destructive_routes.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка комментария задела правила повтора отправки либо гейт гарда, читающий исходник</fails_when>
    <automated>grep -c "app/pages/common.py" app/pages/history.py</automated>
    <fails_when>вывод равен `0` — указателя на общего владельца гарда в модуле журнала нет, и читатель, пришедший за гардом, не найдёт его места</fails_when>
    <automated>uv run pytest tests/test_pages/ -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка задела правила страничного слоя. ⚠️ НЕДЕТЕРМИНИРОВАННЫЙ КРАСНЫЙ ЗДЕСЬ ПРИНЯТ, И ОСНОВАНИЕ У ПРИНЯТИЯ СВОЁ, ИЗМЕРЕННОЕ, — НЕ ОЦЕНКА ТРУДА И НЕ ПЕРЕЧИСЛЕНИЕ. ЭТО КАНОНИЧЕСКОЕ МЕСТО ОСНОВАНИЯ ДЛЯ ВСЕХ ТРЁХ ЗАДАЧ ПЛАНА: команда собирает `tests/test_pages/test_htmx_gates.py` — файл, стоящий в `files_modified` плана 10-31 ТОЙ ЖЕ ВОЛНЫ 4 при `parallelization: true` в `.planning/config.json` и не стоящий в `files_modified` настоящего плана. Задача 1 плана 10-31 расширяет разбор собственной сборки ответа ПЕРЕД тем, как завести реестр `OWN_RESPONSE_DIVERGENCES`, — между этими шагами правила того файла красны ПО ПОСТРОЕНИЮ у соседа. ⚠️ ПРИЁМ ПАРТИИ (снятие ОДНОГО узла плюс явный замер его предмета, как в 10-24 задаче 2, 10-28 задачах 1 и 2, 10-29 задаче 1) ЗДЕСЬ НЕПРИМЕНИМ ПО ЗАМЕРУ, А НЕ ПО ВКУСУ: узла с известным именем нет — план 10-31 имени не закрепляет, его собственная проверка отбирает новые правила фильтром `-k "own_response or builds_own or divergence"`, а имя оставлено исполнителю. ЗАМЕРЕНО НА ДЕРЕВЕ: `uv run pytest tests/test_pages/test_htmx_gates.py -q --deselect …::test_this_node_does_not_exist_at_all` даёт `35 passed`, код `0`, без единого предупреждения, тогда как снятие СУЩЕСТВУЮЩЕГО узла даёт `34/35 tests collected (1 deselected)`. Снятие УГАДАННОГО имени было бы МОЛЧА ХОЛОСТЫМ: шумный красный сменился бы тихим зелёным, а парный замер стерёг бы узел, которого никто не снимал. Обменять шум на молчание запрещено принципом самой партии. ⚠️ ЦЕЛИКОМ СНЯТЬ ЭТОТ ФАЙЛ ТОЖЕ НЕЛЬЗЯ, И ЭТО ТОЖЕ ЗАМЕР: его гейты разбирают исходники `app/pages/*.py` — ровно те пять модулей, которые правит настоящий план, — и снятие файла убрало бы покрытие СОБСТВЕННОЙ работы плана. ⚠️ РАЗЛИЧИТЕЛЬ ОБЪЯВЛЕН НАБЛЮДАЕМЫМ ПРИЗНАКОМ, А НЕ СУЖДЕНИЕМ ИСПОЛНИТЕЛЯ: красный, чей упавший узел лежит в `tests/test_pages/test_htmx_gates.py`, есть переходное состояние плана 10-31; красный в любом другом файле есть дефект настоящего плана и останавливает исполнение немедленно. ПОВТОР ОДИН И КОНЕЧЕН: команда прогоняется повторно один раз после завершения плана 10-31; тот же узел, красный и тогда, есть ДЕФЕКТ, а не гонка. Имя файла и число повторов приводятся в сводке</fails_when>
  </verify>

  <acceptance_criteria>
- Замер приведён в сводке: число файлов-потребителей гарда и число мест вызова, снятые прогоном.
  Ориентир планировщика (восемь и тринадцать) назван рядом; расхождение объяснено.
- В тексте нового заголовка названо, ВО СКОЛЬКО РАЗ разошлась снятая строка, и это число выведено из
  замера, а не из ревизии.
- Заголовок содержит все три обязательные части, и это проверено чтением; результат чтения назван в
  сводке по каждой части.
- Пятой копии не найдено либо она найдена, названа поимённо и снята: вывод прогона поиска приведён в
  сводке.
- Тело обработчика не тронуто: число изменённых строк вне блока комментария приведено в сводке и
  равно нулю.
  </acceptance_criteria>

  <done>
Заголовок площадки в модуле журнала не ведёт счёта потребителей, указывает на общего владельца и
называет причину; замер приведён; тело не тронуто.
  </done>
</task>

<task type="auto">
  <name>Задача 2: четыре заголовка сверки перестают повторять опровергнутое и ссылаются на каноническое утверждение (`WR-02` на местах)</name>

  <files>
app/pages/accounts.py
app/pages/ads.py
app/pages/account_groups.py
app/pages/schedules.py
  </files>

  <read_first>
- `app/pages/accounts.py:1020-1035`, `app/pages/ads.py:762-777`,
  `app/pages/account_groups.py:682-696`, `app/pages/schedules.py:1055-1070` — ЧЕТЫРЕ заголовка
  сверки. ⚠️ ЧИТАТЬ ВСЕ ЧЕТЫРЕ И СЛИЧИТЬ ИХ ТЕКСТЫ: они дословно одинаковы, и именно это делает их
  ЧЕТЫРЬМЯ КОПИЯМИ одного утверждения, а не четырьмя утверждениями.
- `app/pages/common.py` ПОСЛЕ правок плана 10-25 — каноническое утверждение истинной границы сверки,
  объявленное ИМЕНЕМ. ⚠️ ЧИТАТЬ ОБЯЗАТЕЛЬНО: площадки переводятся на ССЫЛКУ НА ИМЯ, и текст
  утверждения берётся оттуда, а не переписывается.
- `app/pages/common.py:565-585` — разбор cookie и оба его обращения к базе: это и есть то, что
  делает прежнюю формулировку ложной.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, раздел `### WR-02` целиком.
  </read_first>

  <behavior>
1. Ни один из четырёх заголовков не утверждает, что сверка стои́т до любого обращения к базе.
2. Каждый называет ИСТИННУЮ границу: после прав и до чтения ЦЕЛЕВОЙ строки.
3. Каждый ССЫЛАЕТСЯ на каноническое утверждение по имени, а не хранит свою редакцию его текста.
4. Прежняя формулировка помечена опровергнутой в ОДНОМ месте — там, где стои́т канон, — а не в
   четырёх.
5. Свойство «отказ по происхождению не имеет права стать признаком существования строки» сохранено
   на всех четырёх площадках.
6. Ни одна строка тела обработчиков не тронута, включая порядок проверок.
  </behavior>

  <action>
**(1) Сличить четыре текста ДО правки** и записать в сводку, совпадают ли они посимвольно. Если
какой-то из четырёх успел разойтись с остальными, это НАХОДКА: копии уже начали жить своей жизнью, и
это ровно то, ради чего они сводятся.

**(2) Перевести каждый на ССЫЛКУ.** Новый заголовок площадки состоит из: имени канонического
утверждения (чтобы читатель нашёл текст) плюс ОДНОЙ строки о том, что именно эта площадка делает
(отказывает без объяснения причины чужому источнику). ⚠️ ТЕКСТ КАНОНА НЕ КОПИРУЕТСЯ В ЗАГОЛОВОК: это
породило бы четыре копии заново, только с новым текстом.

**(3) Свойство «отказ не есть признак существования строки» ОСТАВИТЬ на площадках.** Оно есть
свойство ЭТОГО отказа, а не общей границы, и в канон не переезжает.

**(4) Прежнюю формулировку пометить опровергнутой ОДИН раз** — там, где стои́т канон, если план 10-25
этого ещё не сделал. Сличить с состоянием `app/pages/common.py` ПЕРЕД правкой: две пометки об одном
опровержении есть то же накопление, которое партия снимает в соседних местах.

**(5) Тела обработчиков НЕ ТРОГАТЬ.** В частности, НЕ переносить сверку выше разбора cookie ради
истинности прежней формулировки: это подгонка кода под комментарий, зеркальная подгонке теста под
код, и сверка без опознанного субъекта отказывала бы там, где сегодня отвечает переходом на вход.

**(6) Замерить результат.** Сколько символов занимали четыре заголовка ДО и ПОСЛЕ; сколько РАЗНЫХ
утверждений о границе сверки живёт в `app/pages/` ПОСЛЕ (ожидаемое — одно). Оба замера в сводку.

**(7) Чего задача НЕ делает.** Не трогает записей `app/pages/schedules.py` о закрытости входов и
докстринга помощника (задача 3). Не трогает модуля журнала (задача 1). Не пересматривает границы
«запрос без обоих заголовков пропускается».
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка заголовков задела гейт гарда, читающий исходник страничного слоя</fails_when>
    <automated>uv run python -c "import pathlib,sys,re; names=[]; import app.pages.common as m; names=[n for n in vars(m) if 'ORIGIN' in n.upper() and isinstance(getattr(m,n),str)]; hits=sum(1 for p in pathlib.Path('app/pages').glob('*.py') if p.name!='common.py' and any(n in p.read_text(encoding='utf-8') for n in names)); print('площадок, ссылающихся на канон:', hits); sys.exit(0 if hits>=4 else 'на каноническое утверждение ссылаются менее четырёх площадок')"</automated>
    <fails_when>ненулевой код возврата либо напечатанное число меньше `4` — какая-то из площадок осталась со своей редакцией вместо ссылки</fails_when>
    <automated>uv run pytest tests/test_pages/ -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка задела правила страничного слоя. ⚠️ НЕДЕТЕРМИНИРОВАННЫЙ КРАСНЫЙ ЗДЕСЬ ПРИНЯТ, И ОСНОВАНИЕ СТОИ́Т ОДИН РАЗ — в УСЛОВИИ ОТКАЗА ЭТОЙ ЖЕ КОМАНДЫ ЗАДАЧИ 1 настоящего плана: сосед 10-31 той же волны 4 держит `tests/test_pages/test_htmx_gates.py` в своём `files_modified`, приём партии со снятием одного узла неприменим (имени узла нет, а снятие угаданного имени ЗАМЕРЕНО холостым — `35 passed`, код `0`), снятие файла целиком убрало бы покрытие собственной работы плана. Вторая копия основания здесь не чеканится намеренно: это ровно тот дефект накопления, который настоящий план чинит задачами 1 и 3. ⚠️ ДЕЙСТВУЮЩЕЕ ЗДЕСЬ ПРАВИЛО ПРИВЕДЕНО ЦЕЛИКОМ, потому что исполнитель читает его у команды: красный, чей упавший узел лежит в `tests/test_pages/test_htmx_gates.py`, есть переходное состояние плана 10-31 и прогоняется повторно ОДИН раз после его завершения; красный в любом другом файле есть дефект настоящей задачи и останавливает исполнение немедленно; тот же узел, красный и после завершения 10-31, есть ДЕФЕКТ, а не гонка</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — исходник не собирается</fails_when>
  </verify>

  <acceptance_criteria>
- Сличение четырёх текстов ДО правки приведено в сводке: совпадают посимвольно либо расходятся, и
  расхождение названо поимённо.
- Все четыре площадки ссылаются на канон ПО ИМЕНИ: вывод прогона проверки приведён в сводке, и число
  в нём не меньше четырёх.
- Ни одна площадка не хранит копии текста канона: в сводке названо, сколько РАЗНЫХ утверждений о
  границе сверки живёт в каталоге страничного слоя после правки, и это число равно одному.
- Свойство «отказ не есть признак существования строки» присутствует на всех четырёх площадках:
  число площадок с ним приведено в сводке замером и равно четырём.
- Пометка об опровергнутой формулировке существует РОВНО ОДНА во всём `app/pages/`: её место названо
  в сводке.
- Тела обработчиков не тронуты: число изменённых строк вне блоков комментария приведено в сводке по
  каждому из четырёх файлов и равно нулю.
- Два замера объёма (символов ДО и ПОСЛЕ) приведены в сводке.
  </acceptance_criteria>

  <done>
Четыре заголовка говорят истинную границу ссылкой на один канон; копий текста канона нет; свойство
отказа сохранено на всех четырёх; тела не тронуты.
  </done>
</task>

<task type="auto">
  <name>Задача 3: запись о закрытости входов приводится к ЗАМЕРУ, и у помощника чтения поля контекста остаётся ОДНА действующая запись об опровергнутом (`CR-01` документная половина, `IN-04`)</name>

  <files>
app/pages/schedules.py
  </files>

  <read_first>
- `app/pages/schedules.py:277-390` — докстринг помощника чтения поля контекста ЦЕЛИКОМ, все его
  пункты и обе живые записи об опровергнутом. ⚠️ ЧИТАТЬ ЦЕЛИКОМ И ВЫПИСАТЬ: пункт `(г)` несёт
  опровержение, чью верную формулировку даёт пункт `(д)`; отдельная запись у начала докстринга
  ссылается ВПЕРЁД на «верную формулировку в конце». Предмет задачи — эти две записи и пункт `(д)`.
- `app/pages/schedules.py:50-100` ПОСЛЕ правок плана 10-24 — там осталась отсылка к нейтральному
  модулю, а летопись имени и основания переехали. ⚠️ ЧИТАТЬ: их дублировать запрещено.
- `10-24-SUMMARY.md`, `10-28-SUMMARY.md`, `10-29-SUMMARY.md` — числа закрытых входов по планам:
  запись пункта `(д)` приводится к ИХ сумме, а не к памяти.
- `10-30-PLAN.md`, раздел `<objective>` и задача 1 — правило полноты границы по каталогу. ⚠️ ЧИТАТЬ:
  запись обязана назвать ОБА стерегущих правила, и второе из них заводит план 10-30. ⚠️ ПЛАН 10-30
  СТОИ́Т В ТОЙ ЖЕ ВОЛНЕ 4, ЧТО И НАСТОЯЩИЙ, И ПОРЯДОК МЕЖДУ ПЛАНАМИ ОДНОЙ ВОЛНЫ НЕ ГАРАНТИРОВАН
  НИЧЕМ: их `files_modified` не пересекаются, и зависимости между ними нет. Поэтому состояние
  правила каталога снимается ЗАМЕРОМ дерева на момент правки, а не предполагается; если оно ещё не
  заведено, запись называет его как ЗАВОДИМОЕ планом 10-30, а не как действующее, — и это отличие
  называется прямо, в самом докстринге.
- `.planning/phases/10-rychag-components-modal-html/10-REVIEW.md`, разделы `### CR-01` (абзац о
  следствии закрытой семьи) и `### IN-04` целиком.
  </read_first>

  <behavior>
1. Пункт `(д)` докстринга называет ЗАМЕРЕННОЕ состояние: сколько входов страничного слоя закрыто, чем
   именно и какими правилами стережётся.
2. Названы ДВА стерегущих правила — правило прогона входов этого файла и правило полноты по каталогу,
   — и различие их предметов названо одной фразой.
3. Две записи об опровергнутом сведены в ОДНУ датированную, стоящую в одном месте.
4. Сведённая запись сохраняет предмет КАЖДОЙ из двух: что утверждала первая, что вторая, чем каждая
   опровергнута и каким кругом.
5. Ссылки вперёд через весь докстринг больше нет: читателю не нужно держать в голове порядок двух
   исправлений, чтобы узнать один факт.
6. Число строк докстринга уменьшилось, число названных предметов — нет.
7. Формулировка основания самой границы (предмет — колонка) НЕ ТРОГАЕТСЯ и НЕ ДУБЛИРУЕТСЯ: она
   переехала в нейтральный модуль планом 10-24.
  </behavior>

  <action>
**(1) Инвентаризовать перед сведением.** Выписать в сводку обе записи об опровергнутом: первую строку
каждой, ЧТО она утверждала, ЧЕМ опровергнута и КАКИМ кругом. Сводить вслепую нельзя: запись, чей
предмет отличается, потерялась бы.

**(2) Свести в ОДНУ датированную запись** и поставить её ТАМ, где стои́т исправляемое утверждение, а
не в начале докстринга. Сведённая запись сохраняет предметы обеих и НАЗЫВАЕТ ИХ РАЗДЕЛЬНО: одна
утверждала границу, которой в коде не было; вторая называла предметом закрытия маршрут целиком,
тогда как помощник читает поле контекста. Обе даты и оба круга — на месте.

**(3) Привести пункт `(д)` к ЗАМЕРУ.** Новая редакция утверждает: предмет ЭТОГО помощника — поле
контекста, и только оно (это не менялось); входы страничного слоя закрыты на границе приложения общей
величиной — назвать ЧИСЛО, снятое суммой сводок планов 10-24, 10-28 и 10-29 (⚠️ ОРИЕНТИР
ПЛАНИРОВЩИКА: **1 + 17 + 11 = 29**, слагаемые разобраны в `<objective>`; в докстринг попадает ЗАМЕР
исполнителя, и расхождение с ориентиром называется в сводке поимённо, а не сглаживается); стерегут
это ДВА правила, и их предметы различны — прогон настоящих запросов по входам ЭТОГО файла и
статический разбор ВСЕГО каталога.
⚠️ **РАСХОЖДЕНИЕ С БУКВОЙ ПРЕДПИСАНИЯ РЕВИЗИИ ЗАПИСЫВАЕТСЯ В САМОМ ДОКСТРИНГЕ ОДНОЙ СТРОКОЙ.**
Ревизия предписала УДАЛИТЬ следствие о закрытой семье, потому что на её дереве закрыт был один член
из четырёх. К моменту этой правки закрыты все, и запись, суженная по букве, стала бы У́ЖЕ дерева.
Строка называет: предписание было верным для дерева, на котором писалось; исполнено оно ПО ПРЕДМЕТУ
(запись приведена к замеру), а не по букве; круг и дата ревизии названы.

**(4) НЕ ДУБЛИРОВАТЬ основания границы.** Предмет границы (колонка), нижняя граница (единица) и место
границы (граница приложения) живут в нейтральном модуле с плана 10-24. Здесь — отсылка, если она
нужна пункту, и ни строкой больше.

**(5) Замерить результат.** Число строк докстринга ДО и ПОСЛЕ; число названных предметов ДО и ПОСЛЕ
(по инвентарю пункта (1)). Оба замера в сводку. Второе число уменьшиться не имеет права.

**(6) Проверить, что тело помощника не тронуто.** Шесть строк кода остаются шестью строками.

**(7) Чего задача НЕ делает.** Не вводит машинно читаемой разметки слоёв утверждения: соглашение о
ней действует на КАЖДЫЙ файл проекта, принадлежит владельцу вехи, и окно 52 остаётся открытым. Не
трогает заголовка сверки этого файла (задача 2 сделала это раньше). Не трогает `UNBOUNDED_ROUTE_CASES`
и правил суиты.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_editor_schedules.py tests/test_pages/test_confirm_delete_transport.py -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка докстринга задела правила модуля расписаний либо обход транспортов</fails_when>
    <automated>uv run python -c "import inspect,sys,app.pages.schedules as m; d=m._ad_id_from_form.__doc__ or ''; src=inspect.getsource(m._ad_id_from_form); body=[l for l in src.splitlines() if l.strip() and not l.strip().startswith('#')]; print('строк докстринга:', len(d.splitlines())); print('строк исходника помощника:', len(body)); sys.exit(0 if d.strip() else 'докстринг помощника пуст — сведение превратилось в стирание')"</automated>
    <fails_when>ненулевой код возврата либо напечатанное число строк докстринга равно нулю — записи стёрты, а не сведены</fails_when>
    <automated>uv run pytest tests/test_pages/ -q</automated>
    <fails_when>ненулевой код возврата либо слово `failed` в итоговой строке — правка задела правила страничного слоя. ⚠️ НЕДЕТЕРМИНИРОВАННЫЙ КРАСНЫЙ ЗДЕСЬ ПРИНЯТ, И ОСНОВАНИЕ СТОИ́Т ОДИН РАЗ — в УСЛОВИИ ОТКАЗА ЭТОЙ ЖЕ КОМАНДЫ ЗАДАЧИ 1 настоящего плана: сосед 10-31 той же волны 4 держит `tests/test_pages/test_htmx_gates.py` в своём `files_modified`, приём партии со снятием одного узла неприменим (имени узла нет, а снятие угаданного имени ЗАМЕРЕНО холостым — `35 passed`, код `0`), снятие файла целиком убрало бы покрытие собственной работы плана. Вторая копия основания здесь не чеканится намеренно — по той же идиоме, которой настоящая задача сводит две записи об опровергнутом в одну. ⚠️ ДЕЙСТВУЮЩЕЕ ЗДЕСЬ ПРАВИЛО ПРИВЕДЕНО ЦЕЛИКОМ, потому что исполнитель читает его у команды: красный, чей упавший узел лежит в `tests/test_pages/test_htmx_gates.py`, есть переходное состояние плана 10-31 и прогоняется повторно ОДИН раз после его завершения; красный в любом другом файле есть дефект настоящей задачи и останавливает исполнение немедленно; тот же узел, красный и после завершения 10-31, есть ДЕФЕКТ, а не гонка</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>ненулевой код возврата — исходник не собирается</fails_when>
  </verify>

  <acceptance_criteria>
- Инвентарь обеих записей об опровергнутом приведён в сводке: первая строка каждой, что утверждала,
  чем опровергнута и каким кругом.
- Число названных предметов ПОСЛЕ сведения не меньше, чем ДО: оба числа приведены в сводке.
- Число строк докстринга ПОСЛЕ меньше, чем ДО: оба числа приведены в сводке выводом прогона.
- Ссылки вперёд через докстринг не осталось: в сводке названо, сколько записей об опровергнутом
  живёт в докстринге после правки, и это число равно одному.
- Пункт `(д)` называет ЧИСЛО закрытых входов, и оно равно сумме по сводкам планов 10-24, 10-28 и
  10-29; слагаемые и сумма приведены в сводке. Ориентир планировщика (**1 + 17 + 11 = 29**) назван в
  сводке РЯДОМ с замером; если замер дал иное, названо, какое слагаемое разошлось и на каких входах,
  и в докстринг идёт ЗАМЕР, а не ориентир.
- ⚠️ ЧИСЛО ЖИВЁТ В ТРЁХ МЕСТАХ, И СОГЛАСИЕ ТРЁХ ПРОВЕРЕНО ЧТЕНИЕМ: `<objective>` настоящего плана,
  `<objective>` плана 10-30 и задача 2 плана 10-30. В сводке названо, совпало ли записанное во всех
  трёх с замером; несовпадение есть НАХОДКА и правится в том месте, которое разошлось с замером.
- Пункт `(д)` называет ДВА стерегущих правила и различие их предметов ОДНОЙ фразой; текст приведён в
  сводке. Если правило каталога на момент исполнения ещё не заведено, названо, что оно ЗАВОДИТСЯ
  планом 10-30, и это отличие видно в тексте.
- Расхождение с буквой предписания ревизии записано ОДНОЙ строкой В САМОМ ДОКСТРИНГЕ, с кругом и
  датой: текст приведён в сводке.
- Основания границы не продублированы: `grep -c "2_147_483_647\|2147483647" app/pages/schedules.py`
  даёт `0`, и это значение приведено в сводке.
- Тело помощника не тронуто: число строк исходника помощника приведено в сводке до и после и равно.
  </acceptance_criteria>

  <done>
Запись о закрытости входов равна замеру и называет два стерегущих правила; у помощника одна
действующая запись об опровергнутом с сохранёнными предметами обеих; ссылки вперёд нет; основания
границы не продублированы; тело не тронуто.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| запись в коде → следующий читатель | комментарий, утверждающий больше кода, читается как исполненный инвариант, и защиту от него не ищут |
| сторонний сайт → страничный слой | четыре площадки сверки источника, чей заголовок описывает не тот порядок, который исполняется |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-32-01 | Repudiation | `app/pages/history.py`, заголовок площадки вызова гарда | medium | mitigate | Число потребителей разошлось с деревом в четыре раза и уцелело в файле, который эта же фаза правила, ровно тем планом, который опроверг его близнеца. Читатель, поверивший ему, недооценивает охват гарда. Митигация — снятие числа, указатель на общего владельца и названная причина, по которой чисел здесь не ведут (задача 1) |
| T-10-32-02 | Repudiation | четыре заголовка сверки на площадках | medium | mitigate | Четыре дословные копии утверждения, ложного по замеру: сверка стои́т после чтения субъекта, а не до любого обращения к базе. Копия правится по одной, и через круг читатель держит два несогласных утверждения об одном порядке. Митигация — ссылка на единственный канон, заведённый планом 10-25 (задача 2). ⚠️ Порядок проверок в коде НЕ ПРАВИТСЯ: перенос сверки выше разбора cookie был бы подгонкой кода под комментарий, и сверка без опознанного субъекта отказывала бы там, где сегодня отвечает переходом на вход |
| T-10-32-03 | Repudiation | докстринг помощника чтения поля контекста | low | mitigate | Две живые записи об опровергнутом со ссылкой вперёд через весь текст: читателю надо удержать порядок двух исправлений, чтобы узнать один факт. Цена предъявлена самим кругом — читатель, держащий перекрытия, пропускает копию в соседнем файле. Митигация — сведение с сохранением предмета каждой и счётом названных предметов до и после (задача 3) |
| T-10-32-04 | Tampering | запись о закрытости входов | medium | mitigate | ⚠️ УГРОЗА ДВУСТОРОННЯЯ, И ОБЕ СТОРОНЫ НАЗВАНЫ. Запись шире дерева обманывает читателя закрытием; запись у́же дерева — тем же способом с обратным знаком, и именно её дала бы буква предписания ревизии, писавшаяся на дереве до партии. Митигация — приведение записи к ЗАМЕРУ (сумма по трём сводкам) с названием обоих стерегущих правил и с прямой строкой о расхождении с буквой предписания |
| T-10-32-SC | Tampering | установки пакетов | low | accept | Установок нет: `pyproject.toml` не в `files_modified`, новых зависимостей не вводится |
</threat_model>

## Артефакты, которые создаёт этот план

- заголовок площадки вызова гарда без счёта потребителей в `app/pages/history.py`
- четыре заголовка сверки, ссылающиеся на каноническое утверждение по имени
- приведённая к замеру запись о закрытости входов и одна сведённая запись об опровергнутом в
  `app/pages/schedules.py`

Полный перечень артефактов ПАРТИИ — `10-24-PLAN.md`, раздел `## Артефакты, которые создаёт эта партия`.

## Что этот план НЕ закрывает, и основание у каждого пункта своё

- **Машинно читаемая разметка слоёв утверждения (`IN-04` в его широком прочтении, окно 52).**
  Основанием служит ОБЛАСТЬ ДЕЙСТВИЯ СОГЛАШЕНИЯ, а не оценка труда: правило о том, как размечаются
  слои утверждения, действует на КАЖДЫЙ файл проекта, то есть принадлежит владельцу ВЕХИ, а не партии
  закрытия гейпов. ⚠️ Идиома «опровергнутое не вычёркивается» есть ЗАПИСАННОЕ РЕШЕНИЕ проекта
  (**D-30**/**D-32**) и дефектом не является — окно заведено на СТОИМОСТЬ ЧТЕНИЯ. Настоящий план
  снимает стоимость В ДВУХ НАЗВАННЫХ МЕСТАХ (докстринг гарда — планом 10-25, докстринг помощника —
  задачей 3), не отменяя идиомы и не вводя соглашения. **ФАЗА-ВЛАДЕЛЕЦ НЕ НАЗНАЧЕНА НАМЕРЕННО.**
- **Перенос сверки источника выше разбора cookie.** НЕ ОТЛОЖЕН, А ОТВЕРГНУТ, и основание названо
  следствием: сверка без опознанного субъекта отказывала бы там, где сегодня неопознанный посетитель
  отвечает переходом на вход, — то есть правка ради истинности комментария изменила бы поведение
  продукта. Ревизия предлагала ровно обратное: назвать истинную границу, а не двигать код под текст.
- **Граница гарда «запрос без обоих заголовков пропускается».** Записанное закрытие прежнего круга,
  намеренно не переоткрытое ревизией пятого круга.

<verification>
- `uv run pytest tests/test_pages/ -q` — зелено.
- `uv run pytest tests/ -q -m "not planning"` — зелено.
- `uv run python -m compileall -q app main.py tests` — код возврата ноль.
- Замер потребителей гарда (файлов и мест вызова) приведён в сводке.
- Сличение четырёх заголовков ДО правки приведено в сводке.
- Инвентарь двух записей об опровергнутом и четыре числа (строк и предметов, ДО и ПОСЛЕ) приведены
  в сводке.
- Сумма закрытых входов по трём сводкам приведена слагаемыми и итогом.
</verification>

<success_criteria>
Ни одна площадка вызова гарда не ведёт собственного счёта потребителей; четыре заголовка сверки
говорят истинную границу ссылкой на единственный канон и не хранят его копий; запись о закрытости
входов равна замеру, называет оба стерегущих правила и содержит прямую строку о расхождении с буквой
предписания ревизии; у помощника чтения поля контекста одна действующая запись об опровергнутом,
сохранившая предметы обеих, при уменьшившемся числе строк и неуменьшившемся числе названных
предметов.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-32-SUMMARY.md` по завершении.
</output>
