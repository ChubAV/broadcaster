---
phase: 10-rychag-components-modal-html
plan: 14
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - tests/test_planning/test_requirement_completion_follows_verification.py
autonomous: true
requirements: [FORM-06]

# ⚠️ ПОЛЕЙ `security_asvs_level` И `security_block_on` ВО FRONTMATTER НЕТ, И ЭТО РЕШЕНИЕ,
# А НЕ ПРОПУСК. Основание СНЯТО ЗАМЕРОМ ПЛАНИРОВЩИКОМ, а не взято из памяти:
#  — СХЕМА ПЛАНА GSD ЭТИХ ПОЛЕЙ НЕ ЗНАЕТ. Обязательный перечень — `phase`, `plan`, `type`,
#    `wave`, `depends_on`, `files_modified`, `autonomous`, `must_haves`
#    (`gsd-core/bin/lib/frontmatter.cjs:1215`, он же продублирован в `verify.cjs:851`);
#    ни одного из двух имён в нём нет.
#  — ОБЕ ВЕЛИЧИНЫ ЖИВУТ В КОНФИГЕ и разрешаются ОРКЕСТРАТОРОМ на диспетче планировщика
#    (`gsd-core/workflows/plan-phase.md:420` и `:787`), а планом не объявляются.
#  — ЗАМЕРЕННЫЕ ЗНАЧЕНИЯ: `config-get workflow.security_asvs_level` → `1`;
#    `config-get workflow.security_block_on` → `high`;
#    `config-get workflow.security_enforcement` → `true`.
#    Таблица STRIDE ниже собрана ИМЕННО НА ЭТОМ УРОВНЕ И С ЭТИМ ПОРОГОМ, и обе величины
#    названы в самой таблице, а не подразумеваются.
# ДОПИСАТЬ ОБА ПОЛЯ В ПЯТЬ НОВЫХ ПЛАНОВ — ОТВЕРГНУТО: их нет ни у одного из шестнадцати планов
# фазы и вообще нигде в `.planning/` (замер проверяющего), и заведённые здесь они дали бы
# конвенцию, расходящуюся с одиннадцатью предыдущими планами, вместо записи о том, где
# величины на самом деле живут. МОЛЧАНИЕ ОТВЕРГНУТО ТОЖЕ: оно неотличимо от пропуска — именно
# поэтому решение записано, а не оставлено умолчанием.

estimate:
  # ⚠️ НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ, а не замер. Состояние калибровки СНЯТО
  # ПЛАНИРОВЩИКОМ: `estimate-calibration` → sample_count 0, factor 1, applied false,
  # min_samples 3. Отсюда tokens == raw_tokens и confidence: low, ВЫВЕДЕННЫЙ ИЗ
  # ЧИСЛА ЗАМЕРОВ, а не из самооценки планировщика.
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ЗАПИСЬ ТРЕБОВАНИЯ НЕ МОЖЕТ БЫТЬ ШИРЕ ДЕРЕВА: требование, помеченное завершённым, обязано иметь отчёт верификации своей фазы в состоянии `passed` — и это утверждается ПРАВИЛОМ, а не дисциплиной автора записи"
    - "ПРАВИЛО ЖИВЁТ ТАМ, ГДЕ ЕГО ЗАПУСКАЮТ: оно входит в общий прогон проекта, и коммит, поставивший отметку раньше вердикта, краснит прогон — обходить становится нечего"
    - "ДВЕ ЗАПИСИ ОДНОГО ФАКТА СОГЛАСОВАНЫ: флажок требования в теле файла и клетка состояния в таблице говорят одно, и расхождение краснеет. Сегодня их по 41 и по 41, из них помеченных завершёнными 23 и 23 — числа СНЯТЫ ЗАМЕРОМ, а не памятью"
    - "ПРАВИЛО ПОКАЗАНО КРАСНЫМ НА ИЗМЕНЁННОЙ КОПИИ: копии записи, в которой требование фазы с отчётом `gaps_found` помечено завершённым, правило отказывает и называет требование, фазу и состояние отчёта поимённо"
    - "ФАЙЛ ЗАПИСИ НЕ ПРАВИТСЯ ЭТИМ ПЛАНОМ НИ НА СИМВОЛ: возврат `FORM-06` в незавершённое состояние УЖЕ ИСПОЛНЕН коммитом `c899f04` (`:31` читается незавершённым, `:136` — `Gaps Found`), и повторная правка была бы работой по уже сделанному"
    - "ПРАВИЛО НЕ СУДИТ ПРОЗУ И ЭТИМ НЕ ПЕРЕОТКРЫВАЕТ D-33: его предмет — ДВА машинно читаемых поля и ОДИН объявленный источник, между которыми обязано держаться соответствие; суждения предмет не требует, и документная половина прохибиций по-прежнему судится человеком"
    - "ОТЧЁТ ВЕРИФИКАЦИИ ИЩЕТСЯ ПО ВСЕМУ ДЕРЕВУ ЗАПИСИ, А НЕ ПО ОДНОМУ КАТАЛОГУ: переезд фазы в архив не имеет права молча выключить правило — путь выводится обходом, а не зашивается"
    - statement: "Собственная регламентная формулировка проекта («условие перевода — вердикт `passed` повторной верификации», `.planning/REQUIREMENTS.md`) этим правилом ПОЛУЧАЕТ ПРИНУЖДЕНИЕ, но текст регламента не правится и не переписывается: правило ссылается на него, а не подменяет собой"
      verification: backstop
  artifacts:
    - "tests/test_planning/test_requirement_completion_follows_verification.py — модуль целиком: разбор записи требований, индекс отчётов верификации, несущее правило, правило согласия двух записей, отрицательные контроли"
  key_links:
    - "клетка состояния требования → состояние отчёта верификации его фазы: единственное, чем запись не уезжает шире дерева; ломается — требование объявляется завершённым при живом блокере, и это уже произошло коммитом 0ea886d"
    - "флажок в теле файла → клетка таблицы: единственное, чем два места одного факта не расходятся; ломается — читатель получает два разных ответа на один вопрос, и оба выглядят авторитетными"
    - "индекс отчётов → обход дерева записи: единственное, чем переезд фазы в архив не выключает правило молча; ломается — правило зеленеет на пустом индексе, ничего не утверждая"
  prohibitions:
    - statement: "требование НЕ помечается завершённым раньше вердикта `passed` отчёта верификации своей фазы: запись в этом проекте есть несущий контракт, и запись, ШИРЕ поведения, — ровно тот класс расхождения, которым фаза провалена первым кругом"
      status: flagged-unverified
      verification: none
    - statement: "правило НЕ судит прозу операционных документов: решение D-33 отказало машинному гейту на прозе с названной причиной, и настоящий модуль этого решения не переоткрывает — предметом остаются машинно читаемые поля"
      status: flagged-unverified
      verification: none
    - statement: "уже исполненный возврат `FORM-06` в незавершённое состояние НЕ переделывается: коммит `c899f04` эту правку сделал, и вторая правка того же места превратила бы запись в переписанную задним числом историю"
      status: flagged-unverified
      verification: none
  # ⚠️ РЕБРА ЗОНДА ПОКРЫТИЯ. Все пять рёбер (`adjacency`, `empty`, `ordering`,
  # `idempotency`, `concurrency`) относятся к ПОВЕДЕНИЮ FORM-06 и разрешены либо
  # вынесены явными допущениями в планах 10-12, 10-13 и 10-16. Настоящий план их
  # предметом не имеет и своих не заводит — молчаливых потерь нет, есть названная
  # непричастность.
---

<objective>
Закрывается четвёртый гэп отчёта верификации третьего круга — документный: требование `FORM-06`
было помечено завершённым в `.planning/REQUIREMENTS.md` коммитом `0ea886d` (план 10-11) при живом
блокере `CR-01` и НИ РАЗУ не пройденном обходе `10-UAT.md`.

⚠️ ПЕРВАЯ ПОЛОВИНА ГЭПА УЖЕ ЗАКРЫТА НА ДЕРЕВЕ, И ЭТОТ ПЛАН ЕЁ НЕ ПЕРЕДЕЛЫВАЕТ. Коммит `c899f04`
(«revert premature Complete requirements after gaps found») лёг ПОСЛЕ написания отчёта: сегодня
`.planning/REQUIREMENTS.md:31` читается незавершённым, `:136` — `Gaps Found`. Работа по уже
сделанному здесь не планируется.

Purpose: открытой остаётся ВТОРАЯ половина, названная отчётом прямо, — «правило или пункт
регламента, запрещающий отметку требования раньше `status: passed` отчёта верификации». Регламент у
проекта УЖЕ ЕСТЬ и записан его собственными словами: «условие перевода — вердикт `passed` повторной
верификации». Не было ПРИНУЖДЕНИЯ — и ровно поэтому отметка встала раньше вердикта, а поймал это
следующий круг верификации, а не прогон.

Прецедент формы решения в проекте уже стоит: `tests/test_planning/test_state_progress_matches_roadmap.py`
заведён по той же причине («правило существовало; принуждения у него не было») и тем же способом —
модулем в `tests/`, который входит в команду, запускаемую всеми.

Output: правило соответствия «отметка завершённости ↔ вердикт отчёта верификации», правило согласия
двух записей одного факта, оба с отрицательными контролями, и явно названная граница, по которой
решение D-33 не переоткрывается.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md
@.planning/phases/10-rychag-components-modal-html/10-11-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Создаётся ЭТИМ планом (проход source-grounding обязан считать новым):

- `tests/test_planning/test_requirement_completion_follows_verification.py` — модуль целиком
  (новый файл)
- `_requirement_rows` — разбор клеток таблицы состояний, принимающий ИСХОДНИК параметром
- `_requirement_flags` — разбор флажков требований в теле файла, принимающий исходник параметром
- `_verification_status_by_phase` — индекс состояний отчётов верификации по номеру фазы, обходом
  всего дерева записи
- `COMPLETED_STATUS` — литерал состояния завершённости клетки таблицы
- `PASSED_VERDICT` — литерал вердикта отчёта верификации
- `REQUIREMENT_ROWS_DECLARED` — объявленное число строк таблицы состояний
- `test_no_requirement_is_marked_complete_before_its_phase_verification_passed` — несущее правило
- `test_the_flag_and_the_status_cell_of_every_requirement_agree` — правило согласия двух записей
- `test_the_number_of_requirement_rows_is_declared` — правило объявленного числа
- `test_control_negative_a_premature_completion_reddens_the_rule` — отрицательный контроль несущего
  правила
- `test_control_negative_a_disagreeing_flag_reddens_the_agreement_rule` — отрицательный контроль
  правила согласия

Существует ДО плана и НЕ ПРАВИТСЯ: `.planning/REQUIREMENTS.md`,
`tests/test_planning/test_state_progress_matches_roadmap.py`, `tests/test_planning/__init__.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="execute" tdd="true">
  <name>Задача 1: отметка завершённости требования обязана иметь вердикт отчёта верификации — правило и его зубы</name>

  <files>
tests/test_planning/test_requirement_completion_follows_verification.py
  </files>

  <read_first>
- `tests/test_planning/test_state_progress_matches_roadmap.py:1-60` — ПРЕЦЕДЕНТ ФОРМЫ ЦЕЛИКОМ.
  ⚠️ ЧИТАТЬ ДОСЛОВНО ТРИ ЕГО АБЗАЦА: «ЗАЧЕМ» (правило существовало, принуждения у него не было),
  «ЧЕГО ЭТОТ ФАЙЛ НЕ ДОКАЗЫВАЕТ» (границы предмета названы прямо) и «ПОЧЕМУ ЭТО НЕ ОТМЕНА РЕШЕНИЯ
  D-33» (различие между суждением о прозе и соответствием двух машинно читаемых полей). Новый модуль
  наследует все три абзаца по форме и обязан написать свои.
- `tests/test_planning/test_state_progress_matches_roadmap.py:44-58` — как берётся корень записи, как
  объявлены пути и литералы разбора. Наследуется дословно; второго способа найти корень не заводится.
- `.planning/REQUIREMENTS.md:118-160` — таблица состояний ЦЕЛИКОМ: шапка, разделитель и 41 строка.
  Форма клетки: имя требования, фаза, состояние.
- `.planning/REQUIREMENTS.md:20-60` — флажки требований в теле файла: форма
  `- [ ] **ИМЯ**: текст` / `- [x] **ИМЯ**: текст`. ⚠️ СТРОКА 31 — `FORM-06`, и она читается
  НЕЗАВЕРШЁННОЙ: правка коммитом `c899f04` уже исполнена.
- `.planning/REQUIREMENTS.md:425-440` — собственная регламентная формулировка проекта: «условие
  перевода — вердикт `passed` повторной верификации». ⚠️ ЭТО И ЕСТЬ ПРАВИЛО, КОТОРОЕ МОДУЛЬ
  ПРИНУЖДАЕТ. Текст регламента НЕ ПРАВИТСЯ: модуль на него ссылается.
- `.planning/phases/10-rychag-components-modal-html/10-VERIFICATION.md:1-10` — форма шапки отчёта:
  `phase`, `verified`, `status`. Поле `status` и есть читаемое.
- `.planning/phases/07-*/07-VERIFICATION.md`, `08-*`, `09-*` — шапки трёх прочих отчётов. Все три
  читаются `passed`; фазы 7, 8 и 9 несут все 23 завершённых требования, поэтому правило зелено на
  сегодняшнем дереве. ⚠️ ЭТО ПРОВЕРИТЬ СВОИМ ОБХОДОМ, а не принять с этой строки.
  </read_first>

  <precondition>
Состояние записи на дереве СНЯТО ПЕРЕД ПЕРВОЙ ПРАВКОЙ и совпадает с ожидаемым:
`sed -n '31p;136p' .planning/REQUIREMENTS.md` печатает строку `FORM-06` с НЕЗАПОЛНЕННЫМ флажком и
клетку состояния `Gaps Found`. Расхождение означает, что дерево не то, на котором план собран, —
это ОСТАНОВ с записью, а НЕ повод «заодно поправить»: возврат уже исполнен коммитом `c899f04`, и
вторая правка того же места переписала бы историю задним числом.
  </precondition>

  <behavior>
1. Разбор таблицы состояний даёт 41 строку «требование → фаза → состояние». Число объявлено и
   стережётся: строка, ушедшая из таблицы, краснит прогон так же, как пришедшая.
2. Индекс отчётов верификации собирается ОБХОДОМ всего дерева записи, а не чтением одного каталога:
   переезд фазы в архив не имеет права молча выключить правило. Ключ индекса — номер фазы,
   значение — вердикт из шапки отчёта.
3. Для каждой строки, помеченной завершённой, утверждается: отчёт верификации её фазы СУЩЕСТВУЕТ и
   его вердикт — `passed`. Отсутствие отчёта у завершённой строки краснит прогон: запись, за которой
   нет вердикта, и есть предмет гэпа, а «отчёта не нашлось» — не оправдание, а тот же случай.
4. Строки, НЕ помеченные завершёнными, правилом не судятся вовсе: их состояние — предмет роадмапа,
   а не этого модуля.
5. Отказ показывает ВСЕ несогласные строки одним отображением и называет по каждой: требование,
   фазу, состояние клетки и вердикт отчёта (либо его отсутствие).
6. У правила есть ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ: разборщику подаётся ИЗМЕНЁННАЯ КОПИЯ записи, в которой
   требование фазы с вердиктом `gaps_found` помечено завершённым, и правило обязано покраснеть.
   Копия собирается в памяти; файл дерева не правится.
  </behavior>

  <action>
**1. Модуль и его три абзаца.** Завести
`tests/test_planning/test_requirement_completion_follows_verification.py`. Докстринг модуля пишется
по форме прецедента и несёт ТРИ обязательных абзаца:

(а) ЗАЧЕМ — назвать событие поимённо: коммит `0ea886d` (план 10-11) поставил отметку завершённости
`FORM-06` ПОСЛЕ того, как блокер был открыт прежним отчётом, и ДО того, как он был закрыт; поймал
это следующий круг верификации, а не прогон. Регламент у проекта был и записан его собственными
словами; принуждения у регламента не было. Настоящий модуль и есть принуждение: он живёт в `tests/`
и потому входит в команду, которую в проекте запускают все.

(б) ЧЕГО ЭТОТ ФАЙЛ НЕ ДОКАЗЫВАЕТ — назвать границы прямо: он не судит прозу записи требований ни
одной строкой; не утверждает, что отчёт верификации ВЕРЕН (отчёт объявлен источником вердикта, и
модуль сверяется с ним, а не проверяет его); не знает, вовремя ли поставлена отметка, — он знает
лишь, что отметка и вердикт не противоречат друг другу.

(в) ПОЧЕМУ ЭТО НЕ ОТМЕНА РЕШЕНИЯ D-33 — воспроизвести довод прецедента своими словами применительно
к своему предмету: D-33 отказал машинному гейту на ПРОЗЕ операционного документа, потому что
отличить константу величины от законного числа того же документа машине нечем. Здесь предмет
ДРУГОЙ: два машинно читаемых поля (клетка состояния и вердикт шапки) и объявленное между ними
соответствие. Суждения предмет не требует, поэтому D-33 не переоткрывается и не отменяется;
документная половина прохибиций по-прежнему судится человеком.

**2. Разборщики.** Завести `_requirement_rows` и `_verification_status_by_phase`. Оба принимают
ИСХОДНИК (соответственно текст записи и корень обхода) ПАРАМЕТРОМ, а не берут из константы: без
этого отрицательный контроль был бы невыразим, и зубы правила пришлось бы заявлять вместо того,
чтобы их показывать (записанное основание разборщика шаблонов проекта).

Разбор клеток вести ПО ФОРМЕ СТРОКИ, а не по точному тексту: правка формулировки состояния не имеет
права ронять правило — хрупкое правило отключают, и вместе с ним отключается свойство (записанное
основание прецедента).

Индекс отчётов собирать обходом всего дерева записи по форме имени файла отчёта, а не чтением
одного каталога. Номер фазы брать из имени файла либо из шапки отчёта — способ выбрать ОДИН и
записать основание выбора; два независимых способа разошлись бы молча.

**3. Литералы.** `COMPLETED_STATUS` и `PASSED_VERDICT` выписать константами С ОСНОВАНИЕМ у каждой:
первая — состояние клетки, объявляющее требование завершённым; вторая — вердикт шапки, которым
регламент проекта обусловил перевод. Ни одну не выводить из другой.

**4. Несущее правило.** Завести
`test_no_requirement_is_marked_complete_before_its_phase_verification_passed`. Собрать несогласные
строки в отображение и сличить его с пустым. ⚠️ ОТОБРАЖЕНИЕМ, А НЕ УТВЕРЖДЕНИЕМ НА КАЖДОМ ШАГЕ:
покрасневшее правило обязано показать ВСЕ несогласные строки, иначе следующая правка чинится по
одной строке за прогон.

**5. Объявленное число строк.** Завести `REQUIREMENT_ROWS_DECLARED` и правило равенства. Число
ПОСТАВИТЬ ПРОГОНОМ покрасневшего правила и привести вывод дословно. Летопись числа записать по
форме летописей проекта: откуда взялось, чем измерено, каким планом.

**6. Отрицательный контроль.** Завести
`test_control_negative_a_premature_completion_reddens_the_rule`: подать разборщику копию записи, в
которой требование фазы с вердиктом `gaps_found` помечено завершённым, и утвердить, что несущее
правило на ней краснеет, а текст отказа называет требование, фазу и вердикт отчёта.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке, либо собрано менее трёх правил (несущее, число строк, отрицательный контроль)</fails_when>
    <automated>uv run pytest tests/test_planning -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо число прошедших меньше 5 (базовый замер планировщика до плана): действующее правило машинно читаемого прогресса не имеет права упасть от появления соседа</fails_when>
  </verify>

  <acceptance_criteria>
- **ЧИСЛО ПОСТАВЛЕНО ПРОГОНОМ.** В сводке приведён ДОСЛОВНЫЙ вывод покрасневшего правила числа
  строк («строк таблицы … а объявлено …») и только затем записанное значение. Число, взятое из
  этого плана (41), названо рядом со своим замером как ОРИЕНТИР, и совпадение либо расхождение
  объяснено.
- **ЗУБЫ ПОКАЗАНЫ.** В сводке приведён вывод отрицательного контроля: копия записи с преждевременной
  отметкой краснит несущее правило, и в тексте отказа видны требование, фаза и вердикт отчёта.
- **ОТКАЗ ПОКАЗЫВАЕТ ВСЕ СТРОКИ.** В сводке показано, что при ДВУХ преждевременных отметках в копии
  отказ называет ОБЕ, а не первую.
- Индекс отчётов собран ОБХОДОМ: в сводке приведено число найденных отчётов и их фазы (ожидаемо
  четыре: 7, 8, 9, 10) и сказано, каким обходом они найдены.
- Правило зелено на СЕГОДНЯШНЕМ дереве: в сводке названо число завершённых строк (ожидаемо 23) и
  подтверждено, что вердикты их фаз — `passed`. ⚠️ Числа СНЯТЬ СВОИМ ЗАМЕРОМ; ориентиры плана
  привести рядом.
- Файл записи требований НЕ ТРОНУТ: `git diff --name-only -- .planning/REQUIREMENTS.md` — пусто.
- Три обязательных абзаца докстринга на месте; в сводке приведён абзац о D-33 целиком.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Модуль не меняет ни поведения продукта, ни содержимого записи; откат — снятие файла. Решение
ПРИНУЖДАТЬ регламент правилом, а не дисциплиной, — прямое следствие того, что дисциплина уже
подвела один раз и была поймана верификацией, а не прогоном.
  </reversibility>

  <done>
Требование, помеченное завершённым без вердикта `passed` своей фазы, краснит общий прогон проекта;
правило показывает все несогласные строки одним отображением; его зубы показаны отрицательным
контролем на изменённой копии; число строк поставлено прогоном; файл записи не тронут.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: две записи одного факта обязаны говорить одно — флажок в теле файла и клетка таблицы</name>

  <files>
tests/test_planning/test_requirement_completion_follows_verification.py
  </files>

  <read_first>
- `.planning/REQUIREMENTS.md:20-60` — флажки требований в теле файла. Планировщик снял замером: их
  41, из них заполненных 23. ⚠️ ЧИСЛА ПЛАНИРОВЩИКА — ОРИЕНТИР, А НЕ ОТВЕТ: исполнитель ставит их
  своим прогоном.
- `.planning/REQUIREMENTS.md:118-160` — таблица состояний: 41 строка, из них 23 завершённых.
  Соответствие один к одному с флажками — предмет этой задачи, и его надо УТВЕРДИТЬ, а не принять.
- `tests/test_planning/test_requirement_completion_follows_verification.py` — разборщики и литералы
  задачи 1. Второго разбора не заводится: разборщик флажков прибавляется к ним и наследует приём
  «источник приходит параметром».
- `10-VERIFICATION.md`, раздел `gaps` четвёртого пункта — дословный разбор расхождения: строка 31
  несла заполненный флажок, строка 136 — состояние завершённости, и обе поставлены одним коммитом.
  ⚠️ ДВА МЕСТА ОДНОГО ФАКТА ПОЕХАЛИ ВМЕСТЕ И ПОТОМУ НЕ ПОЙМАЛИ ДРУГ ДРУГА — правило согласия ловит
  случай, когда поедет только одно.
  </read_first>

  <behavior>
1. Разбор флажков даёт отображение «требование → заполнен ли флажок». Разбор клеток (задача 1) даёт
   «требование → состояние». Множества ИМЁН обязаны совпадать: требование, потерявшее одну из двух
   записей, краснит прогон.
2. Для каждого требования утверждается: флажок заполнен тогда и только тогда, когда клетка несёт
   состояние завершённости. Расхождение краснит и называет требование, флажок и клетку.
3. У правила есть ОТРИЦАТЕЛЬНЫЙ КОНТРОЛЬ: копии записи, в которой у одного требования флажок и
   клетка разошлись, правило отказывает.
4. Отказ показывает ВСЕ расхождения одним отображением.
  </behavior>

  <action>
**1. Разборщик флажков.** Завести `_requirement_flags`, принимающий текст записи параметром.
Разбор — по форме строки флажка, а не по точному тексту требования: текст требований в этом проекте
объёмен и правится, и правило, привязанное к нему, отключат вместе со свойством.

**2. Правило согласия.** Завести `test_the_flag_and_the_status_cell_of_every_requirement_agree`.
Собрать три множества несогласий — требования только с флажком, требования только с клеткой,
требования с расходящимися значениями — и сличить отображение из трёх с пустым. Текст отказа обязан
называть все три множества раздельно: «нет клетки», «нет флажка» и «значения разошлись» — разные
события, и слитый отказ заставил бы следующего читателя разбирать, какое из трёх случилось.

**3. Отрицательный контроль.** Завести
`test_control_negative_a_disagreeing_flag_reddens_the_agreement_rule`: подать копию записи, в
которой у одного требования флажок заполнен, а клетка нет, и утвердить отказ с названным
требованием. Копия — в памяти; файл дерева не правится.

**4. Что правило НЕ утверждает — назвать прямо в его докстринге.** Оно не утверждает, что ЛЮБОЕ из
двух значений верно: два согласных места могут быть согласно неверными, и именно так пункт и был
заведён — коммит поставил обе отметки разом. Ловит правило другой случай: когда поедет ОДНО из двух.
Утверждение о верности значения принадлежит несущему правилу задачи 1, и разделение предметов
записать прямо, а не оставить читателю.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/test_requirement_completion_follows_verification.py -q -p no:randomly -k "agree or agreement or control"</automated>
    <fails_when>ненулевой код возврата, либо «0 passed» / «no tests ran» в итоговой строке (фильтр не выбрал ни одного правила — правило заведено под другим именем и не стережёт ничего)</fails_when>
    <automated>uv run pytest tests/test_planning -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата либо число прошедших меньше числа, снятого после задачи 1</fails_when>
  </verify>

  <acceptance_criteria>
- В сводке приведены СНЯТЫЕ замером числа: сколько флажков всего, сколько заполненных, сколько
  строк таблицы, сколько завершённых. Ориентиры плана (41 / 23 / 41 / 23) названы рядом, и
  совпадение либо расхождение объяснено.
- **ЗУБЫ ПОКАЗАНЫ.** В сводке приведён вывод отрицательного контроля с названным требованием.
- **ТРИ МНОЖЕСТВА НЕСОГЛАСИЙ РАЗДЕЛЬНЫ.** В сводке показан отказ на копии, где есть требование без
  клетки И требование с расходящимися значениями, и видно, что множества названы раздельно.
- Докстринг правила называет, чего оно НЕ утверждает; в сводке приведён этот абзац.
- Файл записи требований НЕ ТРОНУТ: `git diff --name-only -- .planning/REQUIREMENTS.md` — пусто.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <reversibility rating="reversible">
Правило не меняет ни поведения продукта, ни содержимого записи; откат — снятие правила.
  </reversibility>

  <done>
Флажок требования и клетка его состояния обязаны говорить одно, расхождение краснит общий прогон,
три множества несогласий названы раздельно, зубы показаны отрицательным контролем, и разделение
предметов двух правил записано, а не оставлено читателю.
  </done>
</task>

</tasks>

<threat_model>
⚠️ УРОВЕНЬ И ПОРОГ НАЗВАНЫ, А НЕ ПОДРАЗУМЕВАЮТСЯ. Таблица собрана на **ASVS 1**
(`workflow.security_asvs_level` → `1`) с порогом блокировки **`high`**
(`workflow.security_block_on` → `high`) при включённом принуждении
(`workflow.security_enforcement` → `true`). Все три величины СНЯТЫ `config-get`, а не объявлены
планом, и почему они не стоят во frontmatter — записано решением в самом frontmatter. Строка
`*-SC` несёт `high` как severity КЛАССА «цепочка поставки», а `accept` — потому что ПРЕДМЕТА НЕТ
(ни одна задача плана не устанавливает пакетов): открытой угрозой она не является и в
`threats_open` при пороге `high` не считается.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| автор коммита → запись требований | отметка завершённости ставится человеком либо агентом и до сих пор не имела принуждения |
| запись требований → читатель (человек, агент, следующая фаза) | запись есть несущий контракт: она читается как утверждение о состоянии дерева |
| отчёт верификации → запись требований | вердикт отчёта объявлен регламентом условием перевода |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14-01 | Repudiation | `.planning/REQUIREMENTS.md`, клетки состояний | high | mitigate | Правило соответствия «отметка ↔ вердикт отчёта» входит в общий прогон проекта (задача 1): коммит, поставивший отметку раньше вердикта, краснит прогон. Обходить становится нечего — прецедент формы и его основание уже стоят в соседнем модуле |
| T-10-14-02 | Tampering | две записи одного факта | medium | mitigate | Правило согласия флажка и клетки (задача 2) ловит случай, когда поедет одно из двух. Случай «поехали вместе» ловит правило задачи 1, и разделение предметов записано в докстрингах |
| T-10-14-03 | Denial of Service | индекс отчётов верификации | medium | mitigate | Индекс собирается обходом всего дерева записи, а не чтением одного каталога: переезд фазы в архив не выключает правило молча. Отсутствие отчёта у завершённой строки краснит прогон, а не пропускает её |
| T-10-14-04 | Tampering | разборщики записи | low | mitigate | Оба разборщика принимают источник параметром, поэтому отрицательные контроли выразимы и зубы правил ПОКАЗАНЫ прогоном на изменённой копии, а не заявлены |
| T-10-14-SC | Tampering | установки пакетов | high | accept | ПРЕДМЕТА НЕТ: план не устанавливает пакетов и не правит `pyproject.toml`. Таблица легитимности пакетов не требуется |
</threat_model>

<verification>
1. `uv run pytest tests/test_planning -q -p no:randomly` — зелено, число прошедших больше базового
   `5 passed`, прибавка объяснена числом заведённых правил.
2. `uv run pytest tests/ -q` — прогон целиком зелен.
3. `git diff --name-only -- .planning/REQUIREMENTS.md` — пусто: запись не правится этим планом.
4. `sed -n '31p;136p' .planning/REQUIREMENTS.md` — строка `FORM-06` с незаполненным флажком и клетка
   `Gaps Found`, то есть ровно то состояние, которое было ДО плана.
5. В сводке приведены ДОСЛОВНО: вывод покрасневшего правила числа строк, вывод обоих отрицательных
   контролей, абзац докстринга о D-33.
</verification>

<success_criteria>
- Требование, помеченное завершённым при вердикте отчёта, отличном от `passed`, краснит общий
  прогон проекта.
- Флажок и клетка состояния каждого требования согласованы, и расхождение краснит.
- Оба правила показывают ВСЕ несогласные строки одним отображением, а не первую.
- Зубы обоих правил показаны отрицательными контролями на изменённых копиях; файл записи не тронут.
- Границы предмета названы прямо: прозу модуль не судит, верность отчёта не проверяет, решение D-33
  не переоткрывает.
</success_criteria>

<output>
Создать `.planning/phases/10-rychag-components-modal-html/10-14-SUMMARY.md` по завершении.
</output>
