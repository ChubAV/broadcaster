---
phase: 10-rychag-components-modal-html
plan: 43
subsystem: testing
tags: [pytest, jinja2, html-parsing, walkthrough, selectors]

requires:
  - phase: 10-rychag-components-modal-html
    provides: "реестр якорей наблюдения `WALKTHROUGH_ANCHORS`, объявленное число 21 с летописью, `missing_anchors` и правило существования якоря (планы 10-20, 10-26, 10-34, 10-36)"
  - phase: 10-rychag-components-modal-html
    provides: "правило конъюнкции несущего счёта шага 1.2 и разборщик открывающего тега рычага с учётом кавычек (план 10-36)"
provides:
  - "поле выражения КАЖДОЙ из 21 записи реестра набирается в консоли — как есть либо после подстановки объявленного заполнителя, и это принуждается правилом"
  - "поля `note` и `placeholder` записи якоря с пустыми умолчаниями; `__str__` несёт И выражение, И пояснение"
  - "`SELECTOR_LEGAL_CHARS`, `SELECTOR_START_CHARS`, `PLACEHOLDER_SUBSTITUTE`, `_console_form`, `TypeabilityFinding`, `typeability_findings` — три РАЗНЫХ основания отказа с объявленным порядком проверок"
  - "`ID_FINGERPRINT` / `ROLE_FINGERPRINT` — литеральные отпечатки, различающие две ветви конъюнкции, и правило различимости двух текстов отказа"
  - "зеркальный контроль ветви идентификатора: ветвь, до сих пор не исполнявшаяся ни одним контролем, исполняется"
  - "разбор открывающего тега входит в значение признака ТОЛЬКО после знака присваивания — апостроф внутри незакавыченного значения разбор не обрывает"
affects: [10-44, 10-46, 10-47, 10-UAT, 10-VERIFICATION]

actuals:
  tokens: 12531
  tasks: 3
  commits: 8
  plan_head_before: b84901b73524bcb324ec5176bef1d26922a9bb3f
  # ⚠️ ЧИСЛО КОММИТОВ ЗАМЕРЕНО, А НЕ ПЕРЕСКАЗАНО: `git rev-list --count b84901b..HEAD`
  # даёт 8 — ПЯТЬ коммитов задач, замыкающий коммит метаданных (эта сводка, STATE,
  # ROADMAP, WINDOWS), седьмой, сводящий `progress.completed_plans` с отметками
  # ROADMAP (см. «Deviations»), и восьмой, приводящий ЭТО ЖЕ ЧИСЛО к замеру. Тем же
  # инструментом, каким его считает `/gsd-verify-work`.
  # ⚠️ ВОСЬМОЙ КОММИТ ВКЛЮЧЁН В ЧИСЛО, А НЕ ВЫНЕСЕН ЗА СКОБКУ, И ЭТО ОСОЗНАННО:
  # запись, считающая себя саму, сходится только если последняя правка идёт
  # `git commit --amend` — иначе счёт гонится за собственным хвостом. Так и сделано.
  # ⚠️ ЧИСЛО ТОКЕНОВ СНЯТО ТОЙ ЖЕ ШКАЛОЙ, ЧТО И ОЦЕНКА (знаки/4) ПО ФАКТИЧЕСКОМУ
  # ДИФФУ: `git diff b84901b..HEAD -- tests/ | wc -c` → 50 126 знаков. Оценка плана
  # была 70 000 при `confidence: low` и нуле образцов калибровки; промах в пять с
  # половиной раз НЕ ОКРУГЛЁН в сторону оценки — округлённое число испортило бы
  # каждую следующую проекцию.

tech-stack:
  added: []
  patterns:
    - "договор поля данных, ПРИНУЖДЁННЫЙ правилом, а не записанный прозой в докстринге: реестр больше не умеет молча вернуть прозу в поле, которое человек набирает"
    - "литеральный отпечаток ветви вместо среза константы: срез сличал бы текст отказа с самим собой и пережил бы правку, снова делающую ветви неразличимыми"
    - "правило различимости текстов отказа как отдельный слой над контролями ветвей"
    - "сужение ветви разбора (кавычка открывает значение только после знака присваивания) вместо её отмены, с границей, названной ЗАМЕРОМ"

key-files:
  created: []
  modified:
    - "tests/test_templates/test_walkthrough_anchors.py"

key-decisions:
  - "Проза ПЕРЕЕХАЛА в поле `note`, а не была вычеркнута: `__str__` выносит запись в текст отказа ОБОИХ правил модуля, и пояснение, потерянное при переезде, обеднило бы отказ ровно там, где его читает человек на приёмке"
  - "Основания отказа набираемости РАЗВЕДЕНЫ на три («проза в поле», «заполнитель не объявлен», «это не выражение селектора») с ОБЪЯВЛЕННЫМ порядком проверок: выражение с необъявленным заполнителем упало бы и на знаках тоже, и отказ назвал бы не ту починку"
  - "`SELECTOR_LEGAL_CHARS` объявлено КОНСЕРВАТИВНЫМ, и следствие названо прямо: выражение, законное по грамматике, но использующее знак вне множества, покраснеет — и чинить надо будет МНОЖЕСТВО, а не запись. Обратный порядок был бы подгонкой данных под правило"
  - "Кнопка подтверждения адресуется РОДОМ КНОПКИ ВНУТРИ ПАНЕЛИ, а не признаком привязки: имя признака привязки несёт двоеточие, а двоеточие в имени признака требует в консоли экранирования — то есть «набираемое» выражение перестало бы набираться без оговорки. Это единственная запись реестра, где две половины отвечают за РАЗНЫЕ свойства одного узла, и это названо комментарием записи"
  - "Отпечатки ветвей выписаны ЛИТЕРАЛАМИ, а не срезами констант: срез, вычисленный из текста отказа, сличал бы текст с самим собой и остался бы истинным при любой правке, включая такую, что снова сделает оба текста неразличимыми"
  - "Ветвь разбора для незакавыченного значения с УГЛОВОЙ СКОБКОЙ не заводится намеренно, и граница названа ЧИСЛОМ: вхождений незакавыченных значений в открывающем теге рычага сегодня НОЛЬ при десяти знаках присваивания. Заводить разбор под форму, которой в дереве нет, значило бы стеречь ПУСТОТУ"
  - "Записи контроля набираемости выписаны литералами и НЕ БЕРУТСЯ из `WALKTHROUGH_ANCHORS` ни одним выражением; нецикличность ОБЪЯВЛЕНА в докстринге контроля — ожидание, вычисленное из проверяемого реестра, доказывало бы согласие правила с собой (класс окна 69)"

patterns-established:
  - "Приписка к летописи вместо переписывания: абзац «полей два там, где могло бы быть одно» остался ВЕРНЫМ и не вычеркнут — дописан абзац о четырёх полях (D-30/D-32)"
  - "Уточнение числа окна ЗАМЕРОМ, а не объявлением его ошибочным: заполнителей четыре, а не три; прежнее значение было верным для формы, считавшей один род заполнителя"

requirements-completed: [FORM-06]
# ⚠️ ЗАПИСЬ НАЗЫВАЕТ ТРЕБОВАНИЕ ПЛАНА, А НЕ ОТМЕТКУ В `.planning/REQUIREMENTS.md`.
# `requirements ready-ids` даёт 0/1: правило
# `test_no_requirement_is_marked_complete_before_its_phase_verification_passed`
# требует прежде `passed` у верификации фазы, а её вердикт — `gaps_found`.
# Отметка НЕ ПОСТАВЛЕНА и поставлена быть не могла.

coverage:
  - id: D1
    description: "Поле выражения КАЖДОЙ из 21 записи реестра якорей набирается в консоли — как есть либо после подстановки объявленного заполнителя"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_every_walkthrough_anchor_is_typeable_in_the_console"
        status: pass
    human_judgment: false
  - id: D2
    description: "Три основания отказа набираемости различены и каждое доказано СВОЕЙ записью контроля, выписанной литералом"
    requirement: "FORM-06"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_an_untypeable_anchor_is_named_by_its_reason"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_a_declared_placeholder_absent_from_the_selector_is_named_too"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_the_console_form_substitutes_the_declared_placeholder"
        status: pass
    human_judgment: false
  - id: D3
    description: "Проза переехала в поле пояснения и по-прежнему попадает в текст отказа обоих правил модуля"
    verification:
      - kind: command
        ref: "uv run python -c \"import tests.test_templates.test_walkthrough_anchors as m; print(str(m.WALKTHROUGH_ANCHORS[6]))\" → `#sched-del-{N}` — внеполосное снятие панели → ads/partials/sched_delete_response.html (шаг 1.6)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Правка полей выражения и пояснения не задела правила существования якорей и объявленного числа"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_every_walkthrough_anchor_exists_in_its_source_template"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_the_number_of_walkthrough_anchors_is_declared"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_a_missing_anchor_is_named_by_the_rule"
        status: pass
    human_judgment: false
  - id: D5
    description: "Контроль конъюнкции различает сработавшую ветвь от соседней, и различимость двух текстов отказа утверждена отдельным правилом (REVIEW-8/WR-06)"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_the_two_conjunction_failures_are_distinguishable"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_the_dialog_role_moved_off_the_id_node_reddens"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_the_id_moved_off_the_tag_reddens_with_its_own_text"
        status: pass
    human_judgment: false
  - id: D6
    description: "Разбор открывающего тега не ломается на апострофе внутри незакавыченного значения, и свойство учёта кавычек при этом сохранено (REVIEW-8/IN-02, половина о кавычках)"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_an_apostrophe_in_an_unquoted_value_does_not_break_the_parse"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_the_quoted_value_still_shields_the_angle_bracket"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py::test_control_a_naive_first_bracket_scan_would_misread_this_markup"
        status: pass
    human_judgment: false
  - id: D7
    description: "Выражения реестра ДЕЙСТВИТЕЛЬНО находят узлы в живом документе браузера"
    verification: []
    human_judgment: true
    rationale: "Правило утверждает ровно одно — выражение НАБИРАЕТСЯ (состоит из законных знаков и начинается знаком, которым выражение селектора может начинаться). Что набранное выражение вернёт непустой счёт в живом дереве, машине недоступно и есть предмет самого ручного обхода: владелец — шаги 1.1…5.5 `10-UAT.md`. Это записано в докстринге правила прямо"

duration: 15 min
completed: 2026-09-10
status: complete
---

# Phase 10 Plan 43: Реестр якорей приведён к своему договору Summary

**Поле, которое человек НАБИРАЕТ В КОНСОЛИ, начало содержать выражение, а не прозу, — и это принуждается правилом: находок набираемости было тринадцать из двадцати одной записи, стало НОЛЬ, разметка не тронута ни на символ.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-10T13:03:00Z
- **Completed:** 2026-09-10T13:18:30Z
- **Tasks:** 3
- **Files touched:** 1 (`tests/test_templates/test_walkthrough_anchors.py`, +573 / −17)

## Accomplishments

### Задача 1 — тринадцать записей отдали поле выражения выражению

- Запись якоря получила два поля с ПУСТЫМИ умолчаниями: `placeholder` (что подставляется вместо
  заполнителя) и `note` (что запись поясняет). Умолчания несущие: `test_control_a_missing_anchor_is_named_by_the_rule`
  собирает записи именованными аргументами без этих полей, и обязательные поля уронили бы его НА СБОРКЕ.
- `__str__` несёт теперь И выражение, И пояснение: `` `#sched-del-{N}` — внеполосное снятие панели → ads/partials/sched_delete_response.html (шаг 1.6) ``.
  Пояснение, потерянное при переезде, обеднило бы отказ ОБОИХ правил модуля.
- Тринадцать записей приведены по таблице плана. Поля метки, источника и шага не тронуты НИ У ОДНОЙ.
  Два выражения сняты С РАЗМЕТКИ, а не выведены из прежнего поля, и граница каждого названа комментарием:
  `[x-ref="cancel"]` (обе половины записи отвечают за ОДНО и перестанут находиться одновременно — связанность
  нарочная) и `.modal__panel button[type="submit"]` (единственная запись реестра, где половины отвечают за
  РАЗНЫЕ свойства одного узла, потому что двоеточие в имени признака привязки требует в консоли экранирования).
- `SELECTOR_LEGAL_CHARS` и `SELECTOR_START_CHARS` выписаны литералами и объявлены консервативными;
  `PLACEHOLDER_SUBSTITUTE` — цифра, потому что все четыре заполнителя суть первичные ключи.
- `typeability_findings(anchors)` принимает реестр ПАРАМЕТРОМ (основание перенесено ссылкой на
  `missing_anchors`, а не второй копией) и даёт РОВНО ОДНУ находку на запись — по ПЕРВОМУ сработавшему
  из трёх оснований; порядок проверок объявлен несущим.
- Летопись числа дописана ПРИПИСКОЙ: правится ФОРМА полей, а не состав; число 21 на месте.

### Задача 2 — контроль конъюнкции различает сработавшую ветвь от соседней

- `ID_FINGERPRINT` и `ROLE_FINGERPRINT` — короткие фрагменты ЛИТЕРАЛАМИ, а не срезами констант.
- `test_the_two_conjunction_failures_are_distinguishable` утверждает четыре вещи и, отдельно, ЗАМЕР,
  ради которого правило заведено: фрагмент `1.2` несут ОБА текста отказа.
- Контроль ветви признака роли приведён: находка ТОЖДЕСТВЕННА `ROLE_LEFT_THE_TAG`, отпечатка чужой ветви
  в ней НЕТ. Прежнее сличение `assert "1.2" in findings[0], findings[0]` приведено в докстринге ДОСЛОВНО и
  помечено опровергнутым — чем (`REVIEW-8/WR-06`), когда (2026-09-10) и чем закрыто.
- `test_control_the_id_moved_off_the_tag_reddens_with_its_own_text` — зеркальный контроль с ТРЕМЯ
  предохранителями: дословный кусок единствен, исходник изменён, признак роли в доктóренном исходнике
  ОСТАЛСЯ (иначе контроль доказывал бы одновременное исчезновение обоих, а не уход одного).

### Задача 3 — разбор тега перестал ломаться на апострофе

- Кавычка открывает состояние «внутри значения признака» ТОЛЬКО если ближайший предшествующий непробельный
  знак есть знак присваивания. Выход — на парной кавычке того же рода, как и был; всё остальное поведение
  побайтово прежнее.
- Граница названа в докстринге ЗАМЕРОМ, а не закрыта молча: незакавыченное значение с УГЛОВОЙ СКОБКОЙ разбор
  по-прежнему оборвёт; вхождений незакавыченных значений в открывающем теге рычага сегодня НОЛЬ при ДЕСЯТИ
  знаках присваивания, и все десять ведут к закавыченным значениям.
- `test_control_the_quoted_value_still_shields_the_angle_bracket` — свойство, ради которого учёт кавычек
  заведён, сохранено, и незакрытая кавычка значения по-прежнему даёт «тег не замкнут».

## Замеры, снятые исполнением

| Что | До | После |
| --- | --- | --- |
| находки правила набираемости по боевому реестру | **13** (9 «проза в поле выражения» + 4 «заполнитель не объявлен») | **0** |
| записей реестра, набираемых КАК ЕСТЬ | 8 из 21 | 21 из 21 (17 как есть + 4 после подстановки) |
| записей с непустым полем пояснения | 0 | 13 |
| записей с объявленным заполнителем | 0 | 4 (`{N}`×3, `{id}`×1) |
| `WALKTHROUGH_ANCHORS_DECLARED` / фактическое число | 21 / 21 | 21 / 21 |
| `missing_anchors(WALKTHROUGH_ANCHORS)` | `[]` | `[]` |
| правил в `test_walkthrough_anchors.py` | 7 | 15 |
| правил, отбираемых `-k "dialog_role or conjunction_failures or id_moved_off_the_tag"` | 2 | 4 |
| наивный разбор открывающего тега рычага, знаков | 710 | 710 |
| разбор с учётом кавычек, знаков | 2008 | 2008 |
| `git diff --numstat -- app/templates` | пусто | пусто |

⚠️ **ОБА ЧИСЛА ДОКСТРИНГА `_modal_opening_tag` ОСТАЛИСЬ ВЕРНЫМИ** — прогон после правки даёт те же
710 и 2008. Правки чисел с пометкой прежних устаревшими не потребовалось.

⚠️ **ЧИСЛО ЗАПОЛНИТЕЛЕЙ В ОКНЕ 68 УТОЧНЕНО ЗАМЕРОМ (ТРИ → ЧЕТЫРЕ), А НЕ ОБЪЯВЛЕНО ОШИБОЧНЫМ.** Окно
называет три; перебор поля выражения по вхождению открывающей фигурной скобки даёт четыре — три записи с
заполнителем номера расписания и одна с заполнителем идентификатора объявления. Прежнее значение было
ВЕРНЫМ для формы, считавшей только первый род заполнителя. Идиома D-30/D-32.

## TDD Gate Compliance

Дисциплина RED→GREEN соблюдена на уровне задач (`tdd="true"` плюс блок `<behavior>`), как и объявляет
frontmatter плана. Гейтовые коммиты присутствуют: `test(10-43)` — b127bde, 518b60b, ec36932;
`feat(10-43)` — 1d92240; `fix(10-43)` — 21f11e1.

⚠️ **`gsd_run check tdd-red-evidence` НЕ ПРИМЕНЯЛСЯ, И ОСНОВАНИЕ — СРЕДА, А НЕ ВЫБОР.** Проверка разбирает
TAP (`node:test`) и вывод pytest прочитать не умеет: на исправном красном прогоне она даёт
`INVALID_RED (zero_tests_discovered)`. Факт записан планами 10-41 и 10-42 этой же партии. Синтезировать TAP
ради зелёного вердикта проверки значило бы подделать свидетельство, поэтому свидетельство RED снято в форме,
которую называет САМ план: команда, код возврата, имя правила, ожидаемое и полученное.

### Свидетельство RED по задачам

**Задача 1.** Команда: `uv run pytest tests/test_templates/test_walkthrough_anchors.py -q -k typeable`.
Код возврата: **1** (`1 failed, 7 passed`). Правило: `test_every_walkthrough_anchor_is_typeable_in_the_console`.
Ожидалось: находок **0**. Получено: находок **13** — девять по основанию «проза в поле выражения» и четыре
по основанию «заполнитель не объявлен», каждая с именем записи и её консольной формой в тексте отказа.
Красный — на утверждении `assert not findings`, то есть на предмете задачи, а не на сборке.

**Задача 2 — РЕД ОСОБОГО РОДА, И ЭТО НАЗЫВАЕТСЯ ПРЯМО.** Предмет задачи лежит в СЛОЕ КОНТРОЛЯ, а не в
проверяемой функции: `dialog_role_findings` обе ветви несла и несёт ВЕРНО, и потому ни один новый контроль
не мог упасть на её поломке. Правило fail-fast «unexpected GREEN» (`gsd-core/references/tdd.md`) велит в
таком случае РАССЛЕДОВАТЬ, а не форсировать красноту — расследование проведено, и красное снято двумя
замерами.

1. Команда: `uv run pytest tests/test_templates/test_walkthrough_anchors.py -q -k "id_moved_off_the_tag"`.
   Код возврата: **5**, `11 deselected` — зеркального контроля не было ВОВСЕ, ветвь идентификатора не
   исполнялась ни одним правилом. Отбор, объявленный проверкой самого плана
   (`-k "dialog_role or conjunction_failures or id_moved_off_the_tag"`), давал **2** правила при требуемых
   ЧЕТЫРЁХ — то есть проверка плана была красна по СВОЕМУ собственному критерию.
2. Воспроизведение `REVIEW-8/WR-06` ИСПОЛНЕНИЕМ: доктóривание, убирающее подстановку идентификатора, даёт
   `len(findings) == 1`, и сличение существующего контроля `"1.2" in findings[0]` на ней **ИСТИННО**;
   `"1.2" in ID_LEFT_THE_TAG` → `True`, `"1.2" in ROLE_LEFT_THE_TAG` → `True`. То есть контроль,
   объявленный доказательством ветви признака роли, зеленел и на ветви идентификатора.

После правки тот же отбор даёт **4** правила, все зелёные.

**Задача 3.** Команда: `uv run pytest tests/test_templates/test_walkthrough_anchors.py -q -k apostrophe`.
Код возврата: **1** (`1 failed, 13 deselected`). Правило:
`test_control_an_apostrophe_in_an_unquoted_value_does_not_break_the_parse`. Ожидалось: разбор вернёт тег
`<div class="modal" data-owner=it's role="dialog">`. Получено:
`Failed: разбор оборвался на апострофе внутри НЕЗАКАВЫЧЕННОГО значения … ModalTagNotParsed: открывающий тег
рычага не замкнут`. Красный — на предмете задачи, поднятый САМИМ разборщиком, а не сборкой модуля.

## Deviations from Plan

### [Rule 3 — Блокирующее] Граница коммитов RED/GREEN задачи 1 сдвинута на один шаг

- **Найдено при:** Задача 1, шаг (1) действия.
- **Расхождение:** план кладёт правило набираемости в коммит RED, а всю машинерию (`SELECTOR_LEGAL_CHARS`,
  `_console_form`, три основания, `typeability_findings`) и поля записи — в шаги GREEN. Литерально это
  неисполнимо: правило без машинерии не запускается вовсе и дало бы `NameError`/`AttributeError`, то есть
  ПОЛОМКУ СБОРКИ, а не красноту на предмете. Красный от нехватки имени неотличим от опечатки и по доктрине
  `tdd.md` есть `INVALID_RED`.
- **Как разведено:** машинерия и поле `placeholder` с пустым умолчанием уехали в коммит RED (b127bde) — они
  НИЧЕГО не чинят: тринадцать записей остаются прозой, и правило красно ровно на них. ПОЧИНКА (тринадцать
  приведённых записей, `note`, `__str__`, летопись, контроли) целиком лежит в коммите GREEN (1d92240).
- **Проверка:** прогон RED дал `1 failed, 7 passed` с тринадцатью находками, перечисленными поимённо.
- **Файлы:** `tests/test_templates/test_walkthrough_anchors.py`. **Коммиты:** b127bde, 1d92240.

### [Rule 2 — Недостающее принуждение] Дописан контроль подстановки заполнителя

- **Найдено при:** Задача 1, шаг (7) действия.
- **Расхождение:** план называет у контроля набираемости четыре литеральные записи (три негодные и одну
  годную), но `behavior` п. 3 требует ещё и зеркала второго основания — объявленного заполнителя, которого в
  выражении НЕТ. Впихивать пятую запись в контроль означало бы разойтись с критерием приёмки, требующим
  РОВНО четырёх.
- **Как разведено:** заведены два отдельных контроля —
  `test_control_a_declared_placeholder_absent_from_the_selector_is_named_too` (зеркало второго основания) и
  `test_control_the_console_form_substitutes_the_declared_placeholder` (подстановка не молчит). Контроль
  трёх оснований несёт РОВНО четыре записи, как и требует критерий.
- **Файлы:** `tests/test_templates/test_walkthrough_anchors.py`. **Коммит:** 1d92240.

### [Записано, а не сглажено] Оценка плана промахнулась в пять с половиной раз

- Оценка: 70 000 токенов при `confidence: low` и `sample_count: 0`. Замер той же шкалой (знаки/4 по
  фактическому диффу): **12 531**. Число не округлено в сторону оценки — округлённое испортило бы каждую
  следующую проекцию (ADR-2629).

### [Rule 1 — Дефект] Отметка плана в ROADMAP покраснила гейт согласия счёта планов

- **Найдено при:** замыкающем коммите метаданных.
- **Дефект:** `roadmap update-plan-progress` поставил отметку плана 10-43, из отметок `.planning/ROADMAP.md`
  стало выводиться **81** исполненных плана, а машинно читаемое поле `progress.completed_plans` во
  frontmatter `.planning/STATE.md` осталось на **80**. `tests/test_planning/test_state_progress_matches_roadmap.py::test_the_machine_readable_progress_is_derived_from_the_roadmap`
  покраснел — ровно то окно между коммитом плана и коммитом трекинга, которое правило и заведено делать
  видимым (план 05-31). `state update-progress` этого поля не двигает.
- **Починка:** приведено ПОЛЕ (80 → 81), потому что источником счёта планов объявлен ROADMAP, а не STATE.
- **Проверка:** `uv run pytest tests/test_planning/ -q` → **28 passed**.
- **Файлы:** `.planning/STATE.md`. **Коммит:** 3bc45ca.

**Всего расхождений:** 3 разведено автоматически (1 по правилу 1, 1 по правилу 3, 1 по правилу 2) плюс одна
запись замера.
**Влияние:** нулевое на предмет плана — все критерии приёмки трёх задач выполнены, ни одно требование плана
не ослаблено.

## Authentication Gates

None — план правит один модуль суиты и не обращается ни к одной внешней службе.

## Known Stubs

None. Ни одна из пятнадцати функций-правил модуля не помечена `skip`/`xfail`, ни одна проверка `<verify>`
плана не осталась непрогнанной.

## Threat Flags

Новой поверхности безопасности план не вносит: правится один модуль суиты, сетевых точек входа, путей
аутентификации, обращений к файловой системе за пределами чтения `app/templates/` и изменений схемы данных
не заведено. Все пять митигаций реестра угроз плана применены (`T-10-43-01`…`T-10-43-05`), запись
`T-10-43-SC` (`accept`) подтверждена замером: пакетов не ставилось ни одного.

## Verification

| # | Проверка | Результат |
| --- | --- | --- |
| 1 | `uv run pytest tests/test_templates/test_walkthrough_anchors.py -q` | **15 passed** |
| 2 | `uv run pytest tests/test_planning/ -q` | **28 passed** (правило самозаверения обхода не тронуто; прогнано ПОВТОРНО после сведения поля `progress.completed_plans`) |
| 3 | `uv run python -m compileall -q app main.py tests` | код возврата **0** |
| 4 | `git diff --numstat -- app/templates` | **пусто** |
| 5 | `git diff --numstat -- .planning` (на момент завершения задач) | **пусто** |
| 6 | Замеры «до/после» по набираемости | **13 → 0**, названы обоими числами |
| — | `uv run pytest tests/test_templates/ -q` (сверх плана, на collateral) | **216 passed** |

## Success Criteria

- [x] Каждая из 21 записи реестра набирается в консоли, и это принуждается ПРАВИЛОМ, а не осторожностью.
- [x] Проза переехала в своё поле и не потеряна ни в одном тексте отказа.
- [x] Контроль конъюнкции различает сработавшую ветвь от соседней, и различимость текстов доказана.
- [x] Разбор открывающего тега не ломается на апострофе внутри незакавыченного значения.
- [x] Разметка и документы записи не тронуты ни на символ.

## Ledger

**Окно 68 `.planning/WINDOWS.md` переведено в `fixed`.** Истина `must_haves.truths[0]` плана 10-36 — «все
21 запись реестра якорей набираются в консоли» — ДОСТИГНУТА и ПОКАЗАНА прогоном, а не заявлена: правило
`test_every_walkthrough_anchor_is_typeable_in_the_console` даёт ноль находок, и реестр больше не умеет молча
вернуть прозу в поле выражения. Новых окон план не заводит.

## Issues Encountered

None.

## Next Phase Readiness

Гэпы `V9-03`, `REVIEW-8/WR-06` и `REVIEW-8/IN-02` (половина о кавычках) закрыты. Остаток волны 2 восьмой
партии — планы 10-44 и 10-47. Требование `FORM-06` отметки завершённости не получило и получить не могло:
`requirements ready-ids` даёт 0/1, а верификация фазы стои́т в `gaps_found`.

## Self-Check: PASSED

- `tests/test_templates/test_walkthrough_anchors.py` — **FOUND** на диске.
- Коммиты **FOUND** в `git log`: b127bde, 1d92240, ec36932, 518b60b, 21f11e1, 76d7c70, 3bc45ca
  плюс замыкающая правка этой сводки (`--amend`, восьмой).
- Все `<acceptance_criteria>` трёх задач перепрогнаны после последнего коммита: набираемость 0 находок,
  `missing_anchors` пусто, объявленное число 21, отбор задачи 2 даёт 4 зелёных правила, замеры 710/2008 на
  месте, диффы `app/templates` и `.planning` пусты.
