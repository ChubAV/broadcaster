---
phase: 10
plan: 47
subsystem: planning-records
status: complete
tags: [prohibitions, owner-decision, permit, gap-closure, HV-04-PROHIBITIONS, FORM-06]

requires:
  - "10-41 — трассирующий срез восьмой партии, исполнен до настоящего плана"
  - "блоки `must_haves.prohibitions` 47 планов Фазы 10 как материал замера"
  - "пункт 4 перечня Human Verification девятого круга (`10-VERIFICATION.md:828-836`) как предмет"
provides:
  - "10-PROHIBITIONS-SUBJECT.md — поимённое состояние 39 запретов седьмой партии плюс ЗАПИСЬ РАЗРЕШЕНИЯ владельца"
  - "машинно читаемая ОБЛАСТЬ разрешения: `permit_scope: seventh-batch-only`, `permit_applies_to_phase: false`, `permit_applies_to_milestone: false`"
  - "машинно читаемое НЕПОКРЫТОЕ: `prohibitions_unresolved_outside_batch: 167`, `prohibitions_unresolved_eighth_batch: 33`, `prohibitions_unresolved_total: 200`"
  - "`prohibitions_fully_enforced: 0` — замер, который разрешением НЕ СДВИНУТ"
  - "решение D-36 в `.planning/STATE.md`"
affects:
  - "следующий круг верификации Фазы 10 — пункт 4 перечня Human Verification получает предъявляемую запись вместо молчания"
  - "167 запретов планов 10-01…10-34 и 33 запрета восьмой партии — ЯВНО НЕ ЗАТРОНУТЫ и названы нерешёнными"
  - "Фаза 11 и далее — ЯВНО НЕ ЗАТРОНУТЫ: область разрешения сужена до седьмой партии, прецедент не объявлен"

tech-stack:
  added: []
  patterns:
    - "запись по ЯКОРЮ вместо дописывания в конец файла (урок V9-06): счёт вхождений заголовка снимается до и после"
    - "область решения дублируется машинно читаемыми полями шапки, а не остаётся читаемой только прозой (идиома плана 10-44)"
    - "непокрытое решением называется ЧИСЛАМИ в той же шапке, а не оставляется на догадку"
    - "опровергнутое пересъёмкой число помечается на месте, а не вычёркивается (идиома D-30/D-32)"
    - "утверждение о дереве перепроверяется чтением и `git blame` ПЕРЕД записью, а не переносится из предыдущего замера"

key-files:
  created: []
  modified:
    - ".planning/phases/10-rychag-components-modal-html/10-PROHIBITIONS-SUBJECT.md"
    - ".planning/STATE.md"
    - ".planning/ROADMAP.md"

key-decisions:
  - "D-36 — ветвь `permit-with-record`, принял `chubav` 2026-09-10, область ТОЛЬКО 39 запретов седьмой партии (`10-35`…`10-40`)"
  - "«РАЗРЕШЕНИЕ ДЕЛАЕТ ЗАПРЕТ НЕ БЛОКИРУЮЩИМ, НО НЕ СОБЛЮДЁННЫМ» — формулировка владельца перенесена дословно и поставлена на видное место"
  - "167 запретов планов 10-01…10-34 и 33 запрета восьмой партии названы НЕРЕШЁННЫМИ прямо, числами и в шапке"
  - "два запрета, опровергнутые ДЕРЕВОМ (10-37, 10-38), названы ОТДЕЛЬНО от «не принуждается» — у них нарушенная буква, а не отсутствие правила"
  - "число 165 (планы 10-01…10-32) опровергнуто пересъёмкой — снято 154; помечено, не вычеркнуто"

requirements-completed: []

coverage:
  - deliverable: "Ответ владельца записан по якорю ровно одним разделом"
    verification:
      - kind: command
        ref: "grep -c '^## Ответ владельца' 10-PROHIBITIONS-SUBJECT.md → 0 до записи, 1 после"
        status: pass
    human_judgment: false
  - deliverable: "Формулировка «не блокирует / не соблюдён» присутствует дословно"
    verification:
      - kind: command
        ref: "grep -c 'РАЗРЕШЕНИЕ ДЕЛАЕТ ЗАПРЕТ НЕ БЛОКИРУЮЩИМ, НО НЕ СОБЛЮДЁННЫМ' → 1"
        status: pass
    human_judgment: false
  - deliverable: "Область разрешения записана машинно читаемо и не шире принятого"
    verification:
      - kind: command
        ref: "yaml.safe_load шапки → permit_scope=seventh-batch-only, permit_applies_to_phase=false, permit_applies_to_milestone=false"
        status: pass
    human_judgment: false
  - deliverable: "Каталог правил записи не уронен новым артефактом"
    verification:
      - kind: test
        ref: "tests/test_planning/ → 28 passed"
        status: pass
    human_judgment: false
  - deliverable: "Ни правила, ни продукт, ни вердикт, ни отметка требования не тронуты"
    verification:
      - kind: command
        ref: "git diff --numstat -- tests app .planning/REQUIREMENTS.md 10-VERIFICATION.md → пусто"
        status: pass
    human_judgment: false
  - deliverable: "Основание разрешения соответствует тому, что владелец принимал на останове"
    human_judgment: true
    rationale: "Соответствие записи принятому решению есть суждение принявшего; машина сверяет поля, а не смысл согласия"

metrics:
  duration: "~25m (продолжение после чекпойнта; задача 1 исполнена прежним агентом)"
  completed: 2026-09-10

actuals:
  # ЗАМЕР ТЕМ ЖЕ ИНСТРУМЕНТОМ, ЧТО И ОЦЕНКА: chars/4 по файлу, который план реально изменил
  # (`10-PROHIBITIONS-SUBJECT.md`, 35 345 символов, 467 строк). Оценка плана — 60 000 при
  # `confidence: low` и `sample_count: 0`; промах в 6,8 раза НЕ ПОДГОНЯЕТСЯ под замысел, потому
  # что подогнанное число корёжит калибровку следующих оценок сильнее, чем названное расхождение.
  tokens: 8836
  tasks: 3
  # ⚠️ ЗАМЕРЕНО, А НЕ ПЕРЕСКАЗАНО: `git rev-list --count 5dd22c5..HEAD` на момент написания
  # сводки дал **3** (4fe4d50 задача 1, 6ce646d задача 2, f4de20c задача 3). Записано **6** — с
  # тремя коммитами, которые вносит само закрытие плана и которые иначе остались бы несчитанными:
  # сводка, трекинг (STATE/ROADMAP) и самопроверка. Самоссылка признана, а не спрятана: тот же
  # класс и то же основание, что у поправки 6 → 7 в сводке плана 10-44.
  commits: 6

plan_head_before: 5dd22c5c49be16ea3e6e2b029dabc8f86bf98575
---

# Phase 10 Plan 47: разрешение по запретам седьмой партии, записанное владельцем — Summary

Тридцать девять запретов седьмой партии, все `flagged-unverified` и все без единого принуждения,
перестали быть перечнем без вердикта: состояние каждого предъявлено поимённо, и владелец закрыл
пункт 4 перечня Human Verification решением `permit-with-record` — с именем, датой, дословной
формулировкой «НЕ БЛОКИРУЮЩИМ, НО НЕ СОБЛЮДЁННЫМ» и областью, суженной до одной партии.

## Что сделано

### Задача 1 (исполнена прежним агентом, коммит `4fe4d50`) — предмет решения

Блоки `must_haves.prohibitions` всех 47 планов фазы разобраны YAML-разбором и сведены в
`10-PROHIBITIONS-SUBJECT.md`: таблица 39 записей седьмой партии с дескриптором, итогом и ИМЕНЕМ
принуждающего правила либо прямым «правила с таким предметом в дереве нет», полный текст 14
запретов без дескриптора с различением выразимости, числа по фазе и по восьмой партии.

**Настоящий агент задачу НЕ ПЕРЕИСПОЛНЯЛ.** Коммит проверен на дереве
(`git log --oneline -E --grep="^[a-z]+\((0*10)-(0*47)\):"`) ДО любого другого действия; оба его
`<automated>` контроля переснимались по ходу задач 2 и 3 и оставались зелёными.

### Задача 2 (коммит `6ce646d`) — ответ владельца записан по якорю

Раздел «Ответ владельца» встал в файл **вставкой ПЕРЕД разделом «Чего этот файл НЕ делает»**, а не
дописыванием в конец (прямой урок `V9-06` и окна 70).

**Счёт вхождений — снят прогоном, а не заявлен.** Заголовок `^## Ответ владельца`: **0** до записи,
**1** после. ⚠️ Счёт ПОДСТРОКИ «Ответ владельца» по всему файлу даёт **2**, и расхождение называется,
а не прячется: второе вхождение — прозаическая ссылка на раздел в переписанном абзаце «Он не выбирает
ветвь». Якорем идемпотентности объявлен ЗАГОЛОВОК, и он единственен.

Записаны без единой заглушки: ветвь `permit-with-record`, принявший `chubav`, дата `2026-09-10`,
отвергнутая ветвь `enforce-each`. Формулировка владельца перенесена ДОСЛОВНО и поставлена
отдельным подразделом, а не растворена в прозе.

Тем же коммитом переписан ставший неверным абзац: «Раздел ответа … ВСТАНЕТ в этот файл ПОСЛЕ ответа
владельца» стоял в будущем времени и в момент появления раздела стал бы ложью файла о самом себе.
Абзац переведён в прошедшее время, правка НАЗВАНА в самом файле (идиома D-30/D-32), а не сделана
молча.

### Задача 3 (коммит `f4de20c`) — разрешение записано с основанием, областью и опровержениями

**Область записана машинно читаемо** — прямое требование владельца, исполненное по форме плана
`10-44`: `permit_scope: seventh-batch-only`, `permit_applies_to_phase: false`,
`permit_applies_to_milestone: false`, `permit_covers_plans` шестью именами, `permit_covers_prohibitions: 39`.

**Непокрытое названо ЧИСЛАМИ в той же шапке, а не прозой:**
`prohibitions_unresolved_outside_batch: 167` (из них 163 без дескриптора),
`prohibitions_unresolved_eighth_batch: 33`, `prohibitions_unresolved_total: 200`.
Разрешение покрывает **39 из 239** запретов фазы — **16,3 %**.

**`prohibitions_fully_enforced: 0`** — замер, который разрешением НЕ СДВИНУТ: ни один из 25
запретов с дескриптором `test` не принуждается целиком.

Основание собрано из замера задачи 1 и **переснято прогоном заново** по 47 планам.

## Решение владельца — записано, не переоткрыто

| Поле | Значение |
|------|----------|
| Ветвь | **`permit-with-record`** |
| Принявший | **`chubav`** |
| Дата | **`2026-09-10`** |
| Отвергнуто | **`enforce-each`** — принуждение у каждого запрета НЕ потребовано |
| Область | **ТОЛЬКО 39 запретов седьмой партии `10-35`…`10-40`** |
| Не покрыто | 167 запретов `10-01`…`10-34` · 33 запрета восьмой партии · любая иная фаза вехи v2.1 |

**Формулировка владельца, перенесённая дословно:**

> **РАЗРЕШЕНИЕ ДЕЛАЕТ ЗАПРЕТ НЕ БЛОКИРУЮЩИМ, НО НЕ СОБЛЮДЁННЫМ.**

Она стои́т в файле на видном месте отдельным подразделом, потому что без неё следующий круг прочёл
бы разрешение как ПРОВЕРКУ — то есть запись снова стала бы шире дерева и партия воспроизвела бы
класс, против которого работает.

**Основание — замер, а не мнение.** 39 запретов = 25 с дескриптором `test` + 14 с `none`. Из 25:
**0** принуждается целиком, **8** частично, **17** не принуждается вовсе. Из 14: **2** выразимы
правилом, **6** выразимы наполовину, **6** НЕВЫРАЗИМЫ ПО СУЩЕСТВУ (свойство процесса, а не дерева).
Ноль в первой строке и есть основание: ветвь `enforce-each` в чистом виде недостижима, и это было
названо ДО ответа, а не после.

## Расхождения, пережившие решение — не сглажены

**1. Число 165 опровергнуто пересъёмкой.** Раздел «Истинный размер предмета» (запись задачи 1)
утверждает, что планы `10-01`…`10-32` несут **165** запретов. Пересъёмка задачи 3 даёт **154** (все
154 — без дескриптора). Ни одна граница числа 165 не воспроизводит: `10-01`…`10-32` → 154,
`10-01`…`10-33` → 159, `10-01`…`10-34` → 167. **Вероятная причина — арифметический вывод вместо
счёта** (`167 − 2`). **В запись пошло СНЯТОЕ число 154**; прежняя формулировка помечена
опровергнутой на месте, а не вычеркнута. Утверждение, которое число несло, пересъёмкой
ПОДТВЕРЖДЕНО: до плана `10-33` дескриптора нет ни у одного запрета фазы. На область решения не
влияет — числа `167` и `163` подтверждены точно.

**2. Строка `:5918` уточнена до `:5916`.** Запись задачи 1 назвала `tests/test_pages/test_shell.py:5918`
несущей сам оператор ввоза `_template_chain`; перепроверка даёт оператор на **`:5916`**, а `:5918` —
его ПРИМЕНЕНИЕ. Существо не меняется; расхождение названо.

**3. Найдено ВТОРОЕ место литерала слоя.** Сверх названного задачей 1 места `:5363` замер даёт
`tests/test_pages/test_shell.py:4750` — `AncestorTrapCase("z-index", LAYER_GROUP, "auto", "3", …)`,
значение слоя литералом в исполняемом коде правил. Его автор — `a4b3947` (`test(10-42)`), то есть
план ВОСЬМОЙ партии: под букву запрета плана `10-37` место не подпадает по владельцу, но
принадлежит тому же классу и заведено партией, которая проблему предъявляет. Проверены и ОТВЕРГНУТЫ
как места этого класса: `:3158` (подстановка читаемых величин) и `:4094` (литерал ИМЕНИ свойства).

## Два запрета, опровергнутые ДЕРЕВОМ — названы отдельно от прочих

По прямому требованию владельца эти две записи не растворены в множестве «не принуждается»: у них
не отсутствие правила, а **НАРУШЕННАЯ БУКВА**. Оба места перепроверены чтением файла и `git blame`
ПЕРЕД записью.

| Запрет | Место на дереве | Автор опровержения |
|---|---|---|
| `10-38`: «`_template_chain` НЕ ПЕРЕИСПОЛЬЗУЕТСЯ» | `test_shell.py:5916` (ввоз), `:5918` (применение) — внутри самого правила плана 10-38 | `2bfa658` — **`feat(10-38)`**, сам план |
| `10-37`: «ЛИТЕРАЛОВ СЛОЯ В КОДЕ ПРАВИЛ НЕ ПОЯВЛЯЕТСЯ» | утверждение в ДОКСТРИНГЕ; литерал `"z-index: 3"` на `test_shell.py:5363` | `15b9e99` — **`test(10-37)`**, сам план |

🚫 **НАХОДКА, КОТОРОЙ У ЗАДАЧИ 1 НЕ БЫЛО: ОБА ЗАПРЕТА НАРУШЕНЫ ТЕМ ЖЕ ПЛАНОМ, КОТОРЫЙ ИХ ОБЪЯВИЛ,
И ТЕМ ЖЕ КОММИТОМ, КОТОРЫМ ОН ИСПОЛНЯЛСЯ.** Установлено `git blame`, а не предположено.

Разрешение НЕ объявляет их соблюдёнными, НЕ чинит их и НЕ заводит правил: правка исполненных планов
`10-01`…`10-40` запрещена прямо, правка суиты запрещена настоящим планом.

## Прогоны — числами

| Проверка | Результат |
|---|---|
| `uv run pytest tests/test_planning/ -q` | **28 passed** (порог плана — не меньше 28) |
| `git diff --numstat -- tests app .planning/REQUIREMENTS.md 10-VERIFICATION.md` | **пусто** |
| `git status --porcelain -- .planning/phases/10-.../` | только `10-PROHIBITIONS-SUBJECT.md` |
| Контроль задачи 1 (`status: subject`, `10-35`, `10-40`, `verification`, `дескриптор`) | rc=0 |
| Заголовок `^## Ответ владельца` | 0 → **1** |
| Дословная формулировка владельца | **1** вхождение |
| Файлы планов `10-01`…`10-40` тронуты | **ни одного** |
| YAML шапки разбирается, поля вердикта нет | подтверждено разбором |

## Deviations from Plan

**Ни одного отступления по правилам 1–4.** План исполнен как написан.

Три УТОЧНЕНИЯ ЗАМЕРА (не отступления — они предписаны самим планом: «расхождение не сглаживается,
в запись идут СНЯТЫЕ числа») описаны выше разделом «Расхождения, пережившие решение»: 165 → 154,
`:5918` → `:5916`, найденное второе место литерала `:4750`.

**Пакетов не ставилось ни одного** — гейт легитимности пакетов неприменим (строка `T-10-47-SC`
реестра, распоряжение `accept`).

## Authentication Gates

Ни одного.

## Известные заготовки (stubs)

Ни одной. План не производит ни строки кода; заготовок в артефакте нет — все поля заполнены
снятыми величинами, имя и дата пришли от владельца.

## Что осталось нерешённым — названо, а не умолчано

**167 запретов планов `10-01`…`10-34` (163 без дескриптора) и 33 запрета восьмой партии
`10-41`…`10-47` (19 без дескриптора) НАСТОЯЩИМ РЕШЕНИЕМ НЕ ПОКРЫТЫ.** Решено 39 из 239 — 16,3 %.
Владелец потребовал сказать это прямо, и это сказано и прозой, и полями шапки.

**Требование `FORM-06` отметки НЕ ПОЛУЧИЛО** — вердикт фазы `gaps_found`, а правило
`test_requirement_completion_follows_verification` требует прежде вердикта `passed`.

## Next

Волна 2 восьмой партии: остаток планов фазы. Пункт 4 перечня Human Verification получил
предъявляемую запись; следующему кругу верификации предстоит прочесть её как разрешение, а не как
проверку — различение выписано в файле дословно ровно для этого.

## Self-Check: PASSED

Снято прогоном, а не заявлено, ПОСЛЕ коммита трекинга.

**Файлы — все четыре найдены на диске:** `10-47-SUMMARY.md`, `10-PROHIBITIONS-SUBJECT.md`,
`.planning/STATE.md`, `.planning/ROADMAP.md`.

**Коммиты — все пять найдены на дереве** (`git log --oneline --all`): `4fe4d50` (задача 1,
прежний агент), `6ce646d` (задача 2), `f4de20c` (задача 3), `e2c479c` (сводка), `1951a97`
(трекинг).

**Счёт коммитов — ЗАМЕРЕН:** `git rev-list --count 5dd22c5..HEAD` → **5** на момент самопроверки.
Записанное в `actuals.commits` число **6** включает коммит самой самопроверки, который вносит эту
строку и иначе остался бы несчитанным. Самоссылка признана, а не спрятана.

**Удалений нет ни одного:** `git diff --diff-filter=D --name-only 5dd22c5..HEAD` — пусто.

**Ни один план `10-01`…`10-40` не тронут:** отбор по диффу всего диапазона плана не дал ни одного
файла `*-PLAN.md`/`*-SUMMARY.md` с номером не выше 40.

**Изменённое планом множество — ровно четыре файла и ни одного сверх:** `ROADMAP.md`, `STATE.md`,
`10-47-SUMMARY.md`, `10-PROHIBITIONS-SUBJECT.md`. Ни `tests/`, ни `app/`, ни
`.planning/REQUIREMENTS.md`, ни `10-VERIFICATION.md`.
