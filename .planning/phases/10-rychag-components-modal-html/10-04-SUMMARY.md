---
phase: 10-rychag-components-modal-html
plan: 04
subsystem: testing
tags: [htmx, inventory-gates, ast-parsing, milestone-records, traceability]

# Dependency graph
requires:
  - phase: 10 (план 10-01)
    provides: "Переведённое удаление расписания из редактора — третий обработчик, отдающий фрагмент; числа гейта 18 / 9 / 11 / 10, объявленные и утверждённые"
  - phase: 10 (план 10-02)
    provides: "Прочтение критерия 3 (D-13) в шапке modal.html двумя абзацами и четыре неподвижных числа, которыми оно меряется"
  - phase: 10 (план 10-03)
    provides: "NOT_YET_CONVERTED_COUNT в значении 26, восемь маршрутов подтверждения на слое ответа, перечень изъятий по смещённому курсору"
  - phase: 09
    provides: "Первые два обработчика, отдающих фрагмент (тумблер и удаление группы аккаунта); идиома двух счётов РАЗНЫХ множеств у MODAL_CONSUMERS/MODAL_IMPORTERS"
provides:
  - "FRAGMENT_RESPONSE_HANDLERS + FRAGMENT_RESPONSE_HANDLERS_DECLARED = 3 — ВТОРОЙ СЧЁТ вехи, растущий, по которому Фаза 11 меряет свой массовый перевод"
  - "_hands_a_fragment — признак передачи фрагмента ПО ДЕРЕВУ РАЗБОРА (именованный аргумент у вызова слоя ответа), с границей псевдонима импорта, унаследованной у соседних разборщиков"
  - "test_no_fragment_response_handler_is_listed_as_not_yet_converted — правило СОГЛАСОВАННОСТИ двух счётов (пересечение пусто), связывающее их без приведения к равенству"
  - "VENDORED_JS_FILES — набор вендоренных файлов сценариев ИМЕНАМИ; единственный машинный признак того, что фаза не добавила нового файла"
  - "test_criterion_three_holds_by_the_numbers — критерий 3 роадмапа ЧЕТЫРЬМЯ числами, три из которых читаются импортом из объявляющих их модулей"
  - "Абзац прочтения критерия 3 в шапке modal.html, называющий имя утверждающего правила и его отрицательного контроля"
  - "Записи вехи приведены к 18 в ROADMAP и REQUIREMENTS с ОДНОЙ И ТОЙ ЖЕ летописью, называющей источник расхождения поимённо"
affects: ["Фаза 11 — массовый перевод разделов письма (FORM-03)", "Фаза 15 — сводный обход доступности и документов (GATE-09)"]

actuals:
  tokens: 7760
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - "ВТОРОЙ СЧЁТ вместо совмещения: два счёта разных множеств связываются правилом СОГЛАСОВАННОСТИ (пересечение пусто), а не приводятся к равенству"
    - "Набор объявляется ИМЕНАМИ, а не числом, там, где переименование на месте есть подмена: файл, переименованный при счёте числом, прошёл бы молча"
    - "Три существующих числа критерия читаются ИМПОРТОМ из объявляющих их модулей — второй экземпляр разошёлся бы с первым молча"
    - "Летопись расхождения записей называет ИСТОЧНИК поимённо и характер («устарел», а не «ошибочен»), а архив разведки не правится ни на символ"

key-files:
  created: []
  modified:
    - tests/test_pages/test_htmx_gates.py
    - tests/test_templates/test_components.py
    - app/templates/components/modal.html
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Второй счёт связан с первым правилом СОГЛАСОВАННОСТИ, а не приведён к равенству: равными им быть не положено — семеро из восьми маршрутов Фазы 10 переведены и фрагмента не отдают."
  - "Число второго счёта и набор вендоренных файлов поставлены ПРОГОНОМ покрасневшего правила: объявления заведены дофазовыми значениями (2 и пустой набор) и коммитом RED оставлены красными."
  - "Три числа критерия 3 читаются импортом (CLIENT_STATE_NODES и _client_state_sites из гейтов разметки, INLINE_HANDLER_ATTR из правил безопасности разметки, KNOWN_SUBMIT_HANDLER_FILES из этого же файла); второго экземпляра ни одного не заведено."
  - "Формулировка критерия 3 в роадмапе НЕ амендирована: документ вехи держится фиксированным, а прочтение есть предмет CONTEXT.md и кода."
  - "Записи разведки не тронуты ни на символ; отсутствие изменений проверено диффом по каталогу от ПИННУТОГО пред-фазового SHA, а не от HEAD~1."
  - "FORM-06 НЕ отмечено выполненным этим планом: статус двигает закрытие фазы, а не правка текста требования."

patterns-established:
  - "Отрицательный контроль второго счёта несёт утверждение о НЕПРОТЕКАНИИ подмены: свежее чтение боевого дерева после подмены обязано вернуть объявленное"
  - "Контроль набора файлов подаёт разборщику ИЗМЕНЁННУЮ КОПИЮ каталога (tmp_path), а боевой каталог утверждается нетронутым в том же тесте"

requirements-completed: [FORM-06]

coverage:
  - id: D1
    description: "Каждый объявленный обработчик второго счёта существует, идёт через слой ответа И передаёт ему фрагмент; признак передачи берётся разбором дерева, а отказ называет обработчика поимённо"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_htmx_gates.py#test_every_fragment_response_handler_is_actually_on_the_response_layer"
        status: pass
    human_judgment: false
  - id: D2
    description: "Число обработчиков, отдающих фрагмент, равно объявленному (3), и число поставлено ПРОГОНОМ покрасневшего правила, назвавшего найденное, объявленное и найденных поимённо"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_htmx_gates.py#test_the_number_of_fragment_response_handlers_is_the_declared_one"
        status: pass
      - kind: unit
        ref: "tests/test_pages/test_htmx_gates.py#test_control_negative_an_undeclared_fragment_handler_reddens_the_gate"
        status: pass
    human_judgment: false
  - id: D3
    description: "Два счёта связаны правилом СОГЛАСОВАННОСТИ: обработчик, отдающий фрагмент, отсутствует в перечне отставания — пересечение двух множеств пусто"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_pages/test_htmx_gates.py#test_no_fragment_response_handler_is_listed_as_not_yet_converted"
        status: pass
    human_judgment: false
  - id: D4
    description: "Критерий 3 роадмапа утверждается ЧЕТЫРЬМЯ числами: набор вендоренных файлов равен объявленному именами; число шаблонов со встроенным обработчиком отправки не выросло; вхождений атрибутов событий рантайма разметки ноль; число узлов клиентского состояния не двинулось. Три числа читаются импортом из единственных источников"
    requirement: FORM-06
    verification:
      - kind: unit
        ref: "tests/test_templates/test_components.py#test_criterion_three_holds_by_the_numbers"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_components.py#test_control_negative_a_new_vendored_script_reddens_the_gate"
        status: pass
    human_judgment: false
  - id: D5
    description: "Прочтение критерия 3 (D-13) записано В КОДЕ рядом с правкой — в шапке app/templates/components/modal.html — и называет имя утверждающего его правила"
    requirement: FORM-06
    verification:
      - kind: other
        ref: "grep -c 'критерий 3' app/templates/components/modal.html → 1; grep -c 'test_criterion_three_holds_by_the_numbers' app/templates/components/modal.html → 1"
        status: pass
    human_judgment: false
  - id: D6
    description: "Формулировка критерия 3, строка пункта 4 ручного UAT и весь каталог разведки не тронуты"
    verification:
      - kind: other
        ref: "awk '/^### Phase 10:/,/^### Phase 11:/' .planning/ROADMAP.md | grep -c 'новых строк JS фаза не добавляет' → 1 (то же значение на пред-фазовом 9f3c59e); grep -c 'действительно снимает осиротевшую панель' → 1; git diff --name-only 9f3c59e -- .planning/research/ → пусто"
        status: pass
    human_judgment: false
  - id: D7
    description: "Записи вехи (ROADMAP §Phase 10 и текст FORM-06 в REQUIREMENTS) говорят 18 и несут ОДНУ И ТУ ЖЕ летопись с поимённым источником расхождения, а прогноз разведки назван УСТАРЕВШИМ, а не ошибочным"
    requirement: FORM-06
    verification: []
    human_judgment: true
    rationale: "Документная половина утверждения «объявление против кода» остаётся documentary по прецеденту D-33 Фазы 5. Посимвольная идентичность двух летописей ПРОВЕРЕНА машинно (сверка обеих строк питоном дала True на 967 символах), и числа проверены грепами приёмки, — но что обе записи ЧИТАЮТСЯ как одно утверждение и что формулировка говорит именно «устарел», а не «ошибочен», есть суждение читателя. Машинного эквивалента этой проверке не заводится: правило, читающее .planning/, есть новый КЛАСС гейта."

# Metrics
duration: 48min
completed: 2026-09-03
status: complete
---

# Phase 10 Plan 04: Второй счёт «отдаёт фрагмент», критерий 3 в четырёх числах и приведение записей вехи — Summary

**Фаза закрыла свою бухгалтерию: заведён ВТОРОЙ СЧЁТ обработчиков, отдающих фрагмент (2 → 3, число поставлено прогоном покрасневшего правила и связано с убывающим счётчиком отставания правилом согласованности, а не сведено с ним в одно); критерий 3 роадмапа перестал быть заявлением и стал четырьмя числами, три из которых читаются импортом из единственных источников; расхождение записей вехи «16 против 18» закрыто правкой роадмапа и требований с ОДНОЙ И ТОЙ ЖЕ летописью, называющей источник расхождения поимённо.**

## Performance

- **Duration:** 48 min (из них 23 мин — прогон полной суиты)
- **Started:** 2026-09-02T23:20:00Z
- **Completed:** 2026-09-03T00:08:34Z
- **Tasks:** 2
- **Files modified:** 5 (создано 0)

## Accomplishments

- **Второй счёт заведён и НЕ смешан с первым.** Три обработчика, отдающих фрагмент, объявлены перечнем с комментарием на запись; число утверждается разбором дерева, а не доверием к перечню; отрицательный контроль подставляет необъявленного фрагментного обработчика и КРАСНИТ правило числа.
- **Два счёта связаны правилом, а не подгонкой.** Пересечение множеств пусто — обработчик на слое ответа, числящийся непереведённым, есть ровно то расхождение, от которого стоят гейты единственности решения о форме ответа.
- **Критерий 3 стал измерением.** Четыре числа в одном правиле; отказ называет, КАКОЕ из четырёх не сошлось. Второго экземпляра ни одного из трёх существующих чисел не заведено — они читаются импортом.
- **Прочтение критерия 3 записано в коде** рядом с правкой, которую оно объясняет, и называет имя утверждающего правила и его отрицательного контроля.
- **Расхождение записей вехи закрыто** в обоих файлах одной и той же летописью; прогноз разведки назван УСТАРЕВШИМ, а архив разведки не тронут ни на символ.
- **Полная суита ЗЕЛЕНА ЦЕЛИКОМ: 2758 passed, 0 failed.** Известный красный `full-suite-ads-editor-order-pollution` в этом прогоне не проявился, и красный планировочного гейта, переданный планом 10-03 оркестратору, закрыт до старта этого плана.

## Task Commits

1. **Задача 1 (RED): падающие правила второго счёта и критерия 3** — `4d1f861` (test)
2. **Задача 1 (GREEN): оба объявления поставлены прогонами покрасневших правил** — `83f9757` (feat)
3. **Задача 2: прочтение критерия 3 в коде и приведение записей вехи «16 → 18»** — `4cb20af` (docs)

_Задача 1 исполнена как TDD-задача и потому несёт два коммита (RED → GREEN). Фазы REFACTOR у неё не было: GREEN есть правка ДВУХ ЛИТЕРАЛОВ (числа и набора имён), и чистить после неё нечего._

## Числа: вывод каждого покрасневшего правила ДОСЛОВНО

### `FRAGMENT_RESPONSE_HANDLERS_DECLARED` 2 → 3 — ВТОРОЙ СЧЁТ

Объявление заведено ДОФАЗОВЫМ значением (2 — состояние, полученное вехой от Фазы 9) и коммитом RED оставлено красным. Вывод `test_the_number_of_fragment_response_handlers_is_the_declared_one`, дословно:

```
AssertionError: обработчиков, отдающих фрагмент, найдено 3, объявлено 2: ['app/pages/account_groups.py::account_groups_delete', 'app/pages/account_groups.py::account_groups_toggle', 'app/pages/schedules.py::schedules_delete'].
⚠️ ЭТО НЕ СЧЁТЧИК ОТСТАВАНИЯ: он считает ДРУГОЕ множество и УБЫВАЕТ, а этот РАСТЁТ. Приводить два числа к согласию правкой одного нельзя — поставьте это число ПРОГОНОМ настоящего отказа
assert 3 == 2
 +  where 3 = len({'app/pages/account_groups.py::account_groups_delete', 'app/pages/account_groups.py::account_groups_toggle', 'app/pages/schedules.py::schedules_delete'})
```

Источник движения СЛОВАМИ: **удаление расписания из редактора объявления стало третьим обработчиком, отдающим фрагмент**.

### `VENDORED_JS_FILES` — набор поставлен тем же способом

Объявление заведено ПУСТЫМ и коммитом RED оставлено красным. Вывод `test_criterion_three_holds_by_the_numbers`, дословно:

```
AssertionError: УТВЕРЖДЕНИЕ 1 ИЗ 4 (каталог вендоренных сценариев) НЕ СОШЛОСЬ.
    найдено, но не объявлено: ['alpine.min.js', 'htmx.min.js']
    объявлено, но не найдено: []
Новый файл здесь есть буквально «новая строка JS» в смысле критерия 3; исчезнувший означает, что рантайм подменили другой сборкой
```

### `NOT_YET_CONVERTED_COUNT` — ПОДТВЕРЖДЁН НЕПОДВИЖНЫМ (26)

Этот план первого счёта не трогает и трогать не имеет права: он заводит ИЗМЕРИТЕЛЬНЫЙ ПРИБОР, а не переводит обработчиков. Проверено грепом приёмки (`grep -c 'NOT_YET_CONVERTED_COUNT = 26'` → 1).

## ⚠️ ДВА СЧЁТА И ИХ РАЗНЫЕ ПРЕДМЕТЫ — ОДНОЙ ФРАЗОЙ КАЖДЫЙ

Формулировки ниже написаны затем, чтобы следующая фаза не свела два числа в одно:

- **`NOT_YET_CONVERTED_COUNT` (26, УБЫВАЮЩИЙ)** отвечает на вопрос «**СТОИ́Т ЛИ обработчик на слое ответа**»: падение означает проделанную работу, рост — регрессию.
- **`FRAGMENT_RESPONSE_HANDLERS_DECLARED` (3, РАСТУЩИЙ)** отвечает на вопрос «**ОТДАЁТ ЛИ обработчик фрагмент**»: рост означает проделанную работу, падение — что фрагментный путь потеряли.

**Почему они не равны и почему их нельзя сводить.** Фаза 10 перевела на слой ответа ВОСЕМЬ обработчиков (первый счёт 34 → 26), а второй вырос на ЕДИНИЦУ: семеро из восьми ушли в ветку ПЕРЕХОДА (204 + заголовок перехода) по классу действия либо по объявленному изъятию. Сведи их в одно число — и на два разных вопроса получится один ответ, неверный ровно на том вопросе, который задали позже. Связаны они **правилом согласованности** (пересечение множеств пусто), а не равенством.

**Первый критерий Фазы 11 этим планом НЕ ТРОНУТ**: он говорит о пятнадцати обработчиках класса A и пяти класса B, и Фаза 10 даёт Фазе 11 прибор, а не новую цель.

## Летопись «16 → 18» — ДОСЛОВНО, и она ОДНА в обоих файлах

Формулировка, попавшая в `.planning/ROADMAP.md` (§Phase 10, после блока критериев успеха):

> **Летопись числа мест: 16 → 18** (Фаза 10, план 10-04, D-17). Прежнее значение — 16 — было ПРОГНОЗОМ РАЗВЕДКИ от 2026-08-26. ИСТОЧНИК РАСХОЖДЕНИЯ ПОИМЁННО: планы 06-05, 06-07 и 06-12 добавили места подтверждения ПОСЛЕ того, как прогноз был записан. ⚠️ ПРОГНОЗ НЕ БЫЛ ОШИБКОЙ — ОН УСТАРЕЛ: на момент своей записи он был верным, и правится не он, а числа, которые он пережил. Новое значение получено СЧЁТОМ ПО ФАЙЛАМ и несёт машинный гейт: перечень мест объявлен константой `MODAL_PLACES` и сходится тремя счётами (`test_modal_site_inventory`, `tests/test_templates/test_components.py`). Записи разведки (`.planning/research/*`) НЕ ПРАВЯТСЯ ни на символ: они архив того, что было видно тогда, и правка превратила бы архив в переписанную задним числом историю. Ошибочное чтение «16 — это панели, а 18 — кнопки-триггеры» закрыто ИЗМЕРЕНИЕМ: вызовов макроса панели в исходнике 14 строк в 11 файлах, то есть числу 16 не соответствует ни одно множество, считаемое по файлам. Та же летопись той же формулировкой стои́т у FORM-06 в `.planning/REQUIREMENTS.md`.

**ФОРМУЛИРОВКА В ОБОИХ МЕСТАХ ОДНА, И ЭТО ПРОВЕРЕНО МАШИННО, А НЕ ЗАЯВЛЕНО.** Летопись не переписана в требованиях по памяти — она взята из роадмапа программно и вставлена под текст FORM-06; сверка обеих строк дала посимвольное совпадение на **967 символах**. Различается ровно последнее предложение — перекрёстная ссылка, которая в каждом файле указывает на ДРУГОЙ (в роадмапе на требования, в требованиях на роадмап); указывать на себя она не может по смыслу.

**Что НЕ тронуто и проверено грепами приёмки:** формулировка критерия 3 (`grep -c 'новых строк JS фаза не добавляет'` → 1, то же значение, что на пред-фазовом `9f3c59e`); строка пункта 4 ручного UAT (→ 1); строка прослеживаемости `FORM-06 | Phase 10 | Pending`; весь каталог `.planning/research/` (`git diff --name-only 9f3c59e -- .planning/research/` — пусто, база пиннута ЯВНЫМ SHA, а не `HEAD~1`).

## Files Created/Modified

- `tests/test_pages/test_htmx_gates.py` — «Множество 5»: абзац о том, зачем счёт второй (четыре обязательных утверждения), `FRAGMENT_ARGUMENT`, `FRAGMENT_RESPONSE_HANDLERS` с комментарием на запись, `FRAGMENT_RESPONSE_HANDLERS_DECLARED` с летописью, `_hands_a_fragment`, `_fragment_response_handlers`, три несущих правила и отрицательный контроль в группе `-k control`
- `tests/test_templates/test_components.py` — импорт трёх единственных источников, раздел критерия 3, `STATIC_JS_DIR` (выведен из уже объявленного корня шаблонов), `VENDORED_JS_FILES`, `_vendored_js_files` (каталог параметром — иначе контроль невыразим), `test_criterion_three_holds_by_the_numbers` с прочтением D-13 целиком в докстринге, отрицательный контроль
- `app/templates/components/modal.html` — абзац прочтения критерия 3 рядом с существующей записью D-13: критерий процитирован дословно, четыре неприбавленные вещи перечислены, названы имя правила и имя его контроля, отдельными строками — отвержение буквального прочтения и неамендирование формулировки в роадмапе
- `.planning/ROADMAP.md` — строка цели и критерий 1 Фазы 10 говорят 18; летопись; сводная строка «что фаза подвинула в числах»
- `.planning/REQUIREMENTS.md` — текст FORM-06 говорит 18 и несёт ту же летопись

## Decisions Made

Разобраны во frontmatter (`key-decisions`). Отдельно стоит назвать две:

**Число второго счёта поставлено прогоном, а не сложением в уме — и для этого объявление заведено ЗАВЕДОМО НЕВЕРНЫМ.** Дофазовая двойка и пустой набор вендоренных файлов закоммичены КРАСНЫМИ (`4d1f861`), и только следующий коммит поставил значения из дословных отказов. Это RED-фаза в её настоящем виде: правило, написанное после того, как число уже верно, доказывает не больше, чем комментарий.

**Признак передачи фрагмента ищется у ВЫЗОВА слоя ответа, а не у обработчика вообще.** `schedules_delete` отдаёт фрагмент при оставшихся расписаниях и уходит в ветку перехода на последнем — то есть «есть ли у обработчика фрагмент хоть в какой-то ветке» и «передаёт ли он фрагмент слою ответа» суть разные вопросы. Разбор идёт по именованному аргументу конкретного вызова; граница псевдонима импорта унаследована у `_calls_response_layer` и безопасна по направлению (ошибка даёт обработчика НЕ объявленным фрагментным, то есть красный, а не «разрешённый по умолчанию»).

## Deviations from Plan

### Автоправок НЕТ

Ни одна из деviation-rules 1–3 не сработала: план исполнен без единого исправления багов, недостающей критичной функциональности или блокеров. Правил, покрасневших по причине, планом не предусмотренной, тоже не было — полная суита зелена целиком.

### Отклонения от текста плана, НЕ являющиеся автоправкой

**1. Критерий приёмки 1 задачи 1 внутренне противоречит перечню артефактов ТОГО ЖЕ плана**

- **Что случилось:** критерий требует, чтобы `uv run pytest tests/test_pages/test_htmx_gates.py -k "fragment_response_handler" -x` дал «зелено, **четыре** правила включая контроль». Фильтр даёт **три**: имя контроля, объявленное самим планом в §«Artifacts this phase produces», — `test_control_negative_an_undeclared_fragment_handler_reddens_the_gate`, и подстроки `fragment_response_handler` в нём НЕТ.
- **Что сделано:** имя контроля оставлено таким, каким его объявил план. Переименование ради удовлетворения `-k` подогнало бы артефакт под греп — тот самый класс правки, от которого этот план предостерегает в трёх местах.
- **Проверка исполнена в форме, которая предмет критерия сохраняет:** `-k "fragment"` даёт **4 passed** (три несущих правила + контроль), `-k "fragment_response_handler"` — **3 passed**. Оба прогона зелены.
- **Запись заведена в `.planning/WINDOWS.md`** (kind: deviation).

**2. `graphify update .` НЕ выполнен**

То же положение, что у планов 10-01, 10-02 и 10-03: граф `graphify-out/` существует ТОЛЬКО в основном рабочем каталоге, в worktree отсутствует и в git не отслеживается. Прогон здесь построил бы граф с нуля в каталоге, который оркестратор удаляет после возврата; запись в основной каталог — обращение за пределы worktree и запрещена защитой путей. В `.planning/config.json` `graphify.enabled: false`. **Передаётся оркестратору: выполнить в основном каталоге после слияния волны.** Запись заведена в `.planning/WINDOWS.md`.

**3. Половина предусловия задачи 1 проверена в два приёма, а не одним прогоном**

Предусловие требует `uv run pytest tests/test_templates/ tests/test_pages/ -q` зелёным. Первый прогон был убит собственной обёрткой `timeout` на 900 с (суита идёт дольше). Проверено раздельно: `tests/test_templates/` — **175 passed** до начала правок; вторая половина закрыта финальным прогоном ВСЕЙ суиты (**2758 passed**), который строго её накрывает. Половина про `NOT_YET_CONVERTED_COUNT = 26` проверена грепом до первой правки.

**4. Масштаб `actuals.tokens` не сопоставим с `estimate` плана напрямую**

Записанные 7760 — это `estimateTokens` (chars/4) **по реализованному диффу**, как предписывает контракт исполнителя. `estimate.tokens: 90000` плана — проекция иного рода (планировщик прямо называет её «НЕКАЛИБРОВАННОЙ ПРОЕКЦИЕЙ» при `sample_count 0`), и какую величину она измеряет, план не сообщает. Для будущей калибровки записывается вторая, тоже измеренная величина: chars/4 по ПОЛНЫМ ТЕКСТАМ пяти изменённых файлов — **142954**. Пара «90000 против 7760» без разрешения этого вопроса измеряет способы измерения, а не промах, и читать её как двенадцатикратный промах нельзя.

---

**Total deviations:** 0 auto-fixed + 4 записанных отклонения от текста плана.
**Impact on plan:** Все семь критериев успеха плана выполнены. Отклонение 1 — противоречие внутри самого плана, разрешённое в пользу его же перечня артефактов; отклонения 2–4 предмета не меняют.

## Issues Encountered

- **Полная суита зелена ЦЕЛИКОМ: `2758 passed` за 23 мин 29 с, ни одного `failed`.** Это отличие от двух предыдущих планов волны, и стоит записи: (а) известный красный `full-suite-ads-editor-order-pollution` в этом прогоне не проявился — красный порядково-зависимый, и его отсутствие в одном прогоне отсутствием проблемы не является, запись ледера остаётся открытой; (б) красный планировочного гейта `test_state_progress_matches_roadmap.py`, переданный планом 10-03 оркестратору, на моей базе уже закрыт — `tests/test_planning/` даёт **5 passed** и ПОСЛЕ моей правки роадмапа (отметки планов и таблица прогресса мною не тронуты).
- **`git stash` не применялся ни разу.** Доказательств покраснения через сташ этот план не требовал: оба числа поставлены прогоном на СВОЁМ дереве, а контроли работают через `tmp_path`.

## Known Stubs

Нет. Заглушек, недописанных ветвей и пропущенных тестов план не оставляет; `pytest.skip` не применён ни разу.

## Threat Flags

Новой поверхности сверх заявленной планом нет: план не трогает ни одного обработчика, ни одного предиката отказа и ни одного входа приложения. Единственная правка вне тестов — комментарий в шапке шаблона.

Реестр угроз плана исполнен как написан:

- **T-10-22** (второй счёт, зеленеющий на пустом множестве) — закрыт: число объявлено именованной константой, утверждается отдельным правилом, а отрицательный контроль подставляет необъявленного фрагментного обработчика и краснит правило.
- **T-10-23** (новый файл сценариев, добавленный молча) — закрыт: набор объявлен ИМЕНАМИ, а не числом; контроль подставляет лишний файл и краснит правило, и в том же тесте утверждается, что боевой каталог подменой не тронут.
- **T-10-24** (атрибут события рантайма разметки) — закрыт по построению: ноль вхождений утверждается импортом ЕДИНСТВЕННОГО выражения `INLINE_HANDLER_ATTR` из модуля правил безопасности разметки; второго экземпляра выражения не заведено, поэтому расхождение двух копий невозможно.
- **T-10-25** (расхождение записей вехи и числа гейта) — закрыт наполовину машинно и наполовину чтением, и обе половины названы честно (см. §«ОТКРЫТАЯ ДЫРА» ниже).
- **T-10-26** (правка каталога разведки задним числом) — закрыт: дифф по каталогу от пред-фазового SHA пуст.
- **T-10-SC** — беспредметна: ни одной установки пакета план не выполняет.

## User Setup Required

None — внешней конфигурации план не требует. Ни build-шага, ни новой зависимости, ни нового вендоренного файла не заведено.

## ⚠️ ОТКРЫТАЯ ДЫРА: ДОКУМЕНТНЫЙ ДРЕЙФ ЗАПИСЕЙ ВЕХИ

**Дыра НАЗВАНА, а не закрыта, и разрешением прецедента D-33 Фазы 5 она не исчерпывается.**

Утверждение «объявление против кода» здесь имеет две половины. **Кодовая несёт настоящий машинный гейт:** число мест объявлено константой `MODAL_PLACES` и сходится тремя счётами; разойдись код с константой — покраснеет. **Документная половина** — соответствие `ROADMAP §Phase 10` и текста `FORM-06` этому числу — закрывается **ЧТЕНИЕМ при приёмке фазы**, и это записано честно, а не выдано за машинный результат.

**Чего у документной половины НЕ ОСТАЁТСЯ после закрытия фазы: стерегущего правила.** Разъедься `ROADMAP` или `REQUIREMENTS` с константой гейта снова — не покраснеет ничто. Ровно этот класс дефекта веха уже оплатила раундами, и запись «мы его починили» его не предотвращает.

**Кандидат на закрытие назван:** сводный обход доступности и документов **Фазы 15 (GATE-09)** либо отдельная запись бэклога «правило сличения числа мест в записях вехи с константой гейта».

**Почему не закрыто здесь:** правило, читающее `.planning/`, есть **новый КЛАСС гейта** — гейт на документ, а не на код. Заводить его попутно рычагу значило бы смешать два изменения в одном и принять решение о новом классе правил в плане, предмет которого другой.

**Запись заведена в `.planning/WINDOWS.md`** (kind: unrun-verify, status: open) — чтобы дыра дожила до `/gsd-ship` видимой, а не осталась абзацем сводки.

## Ручной UAT — что машинно НЕ ЗАКРЫТО ПО ВСЕЙ ФАЗЕ

Сводно, для артефакта ручного обхода фазы (наследуется из `coverage` планов 10-01, 10-02, 10-03 и этого):

1. **Пункт 4 перечня роадмапа — `hx-swap-oob="delete"` действительно снимает осиротевшую панель.** Тесты доказывают, что узел снятия УЕХАЛ В ОТВЕТЕ отдельным узлом верхнего уровня со своим `id`; что рантайм по нему снял элемент со страницы — не проверено ничем: httpx внеполосных узлов не применяет. Наблюдается на ДВУХ формах экрана — строка списка и карточка внутри редактора объявления. (План 10-01, `coverage` D9/D10.)
2. **Приземление фокуса.** Правила исполняют объект `x-data` в интерпретаторе JS со стаб-документом и доказывают, что объект ЗОВЁТ фокусировку площадки. Что рантайм позовёт `destroy()` при внеполосном снятии узла, что `tabindex="-1"` делает область фокусируемой и что вспомогательная технология её объявляет — только в браузере. (План 10-02, `coverage` D7.)
3. **Поведение при отказе.** При 500 и при обрыве сети панель обязана ОСТАТЬСЯ открытой, поверх неё — плашка аварии, повторное подтверждение уходит без повторного открытия панели. Правило исполняет выражение с ПОДСТАВЛЕННЫМ признаком успешности — оно доказывает ветвление, а не то, какой признак рантайм проставит на живом отказе. (План 10-02, `coverage` D8.)
4. **Фактический переход по заголовку `HX-Location` и смена личности на экране.** Тройная пара доказывает, что cookie уехала на ответе 204 и что следующий запрос видит чужую учётную запись; что браузер уходит по заголовку и приземляется на нужный экран — httpx не исполняет. (План 10-03, `coverage` D9.)
5. **Документная половина приведения записей «16 → 18».** Прочитать §«Phase 10» роадмапа и текст FORM-06 требований и убедиться, что оба говорят 18, что летопись в обоих местах ОДНА И ТА ЖЕ формулировка и что прогноз разведки назван устаревшим, а не ошибочным. (Этот план, `coverage` D7; см. §«ОТКРЫТАЯ ДЫРА».)

## Next Phase Readiness

- **Фаза 10 закрыта по всем четырём планам.** Все 18 мест подтверждения шлют запрос через слой письма; все восемь маршрутов фазы отвечают обеими половинами пары; панель закрывается только на успехе и приземляет фокус; бухгалтерия фазы сведена.
- **Фазе 11 передан ИЗМЕРИТЕЛЬНЫЙ ПРИБОР, а не новая цель:** `FRAGMENT_RESPONSE_HANDLERS_DECLARED = 3` — единственное число, по которому та фаза меряет свой массовый перевод (FORM-03). Её критерии успеха этим планом НЕ тронуты. Счётчик отставания передаётся в значении **26**.
- **Фазе 15 передана** названная выше открытая дыра документного дрейфа как кандидат в предмет GATE-09.
- **`FORM-06` отмечено выполненным ЭТИМ планом** (`requirements-completed: [FORM-06]`): требование объявляют все четыре плана фазы, и этот — последний из них. Строку прослеживаемости двигает оркестратор при закрытии фазы; текст требования план правил, статус — нет.
- ⚠️ **Оркестратору:** выполнить `graphify update .` в основном каталоге после слияния волны. `progress.completed_plans` в `STATE.md` этот план не трогал; ROADMAP-производное значение моими правками не изменено — я правил только прозу раздела Фазы 10, не отметки планов и не таблицу прогресса, и `tests/test_planning/` зелен (5 passed).

## Self-Check: PASSED

- `tests/test_pages/test_htmx_gates.py` — FOUND; `grep -c 'FRAGMENT_RESPONSE_HANDLERS_DECLARED = 3'` (без комментариев) = 1; `grep -c 'NOT_YET_CONVERTED_COUNT = 26'` = 1
- `tests/test_templates/test_components.py` — FOUND; `grep -c 'VENDORED_JS_FILES'` = 7 (≥ 2); `grep -c 'CLIENT_STATE_NODES = '` = **0** (второго экземпляра не заведено); `grep -c 'KNOWN_SUBMIT_HANDLER_FILES = '` = 1
- `app/templates/components/modal.html` — FOUND; `grep -c 'критерий 3'` = 1; `grep -c 'test_criterion_three_holds_by_the_numbers'` = 1
- Коммиты `4d1f861`, `83f9757`, `4cb20af` — FOUND
- `uv run pytest tests/ -q` — **2758 passed, 0 failed** (23:29)
- `uv run pytest tests/test_templates/ -q` — 175 passed (пред-правочный прогон предусловия)
- `uv run pytest tests/test_planning/ -q` — 5 passed (после правки роадмапа)
- `uv run python -m compileall -q app main.py tests` — без ошибок
- `git diff --name-only 9f3c59e -- .planning/research/` — пусто
- Летописи в ROADMAP и REQUIREMENTS сверены посимвольно — совпадение на 967 символах

---
*Phase: 10-rychag-components-modal-html*
*Completed: 2026-09-03*
