---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 03
type: execute
wave: 4
# ⚠️ ВОЛНА 4, А НЕ 3, И ЗАВИСИМОСТЬ ОТ 09-04 ОБЪЯВЛЕНА, ХОТЯ ФАЙЛЫ НЕ ПЕРЕСЕКАЮТСЯ.
# Задача 3 запирает фазу полным прогоном `uv run pytest tests/ -q` и допускает ровно один
# названный красный. План 09-04 — TDD: он сознательно кладёт КРАСНЫЙ
# test_the_network_banner_names_the_screen_server_divergence в tests/test_pages/test_shell.py
# ДО правки htmx_error_banner.html. Полный прогон, попавший в это окно, упал бы по чужой
# причине. Связь временнáя, а не файловая, и потому объявлена явно, а не оставлена на удачу.
depends_on: ["09-02", "09-04"]
files_modified:
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [FORM-02, FORM-09, QUAL-01]

estimate:
  # ⚠️ confidence: low при нулевом числе замеров — калибровки проекта не существует.
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Каждый hx-post в дереве рождён компонентным макросом либо стоит в явном перечне исключений с обоснованием на запись и отдельно утверждаемым числом — свойства качества раздаются построением, а не ревью 47 форм поотдельности (FORM-09, D-03)"
    - "Форма-триггер удаления в строке группы hx-post не несёт и макросом-обёрткой не оборачивается: при поднявшемся htmx и неподнявшемся Alpine hx-post на ней удалил бы группу без подтверждения и без единого видимого признака (D-03)"
    - "Внутри шаблона, объявляющего цель подмены, каждый x-data — голый, без выражения: узел, несущий состояние, обязан лежать снаружи любой цели подмены (D-08)"
    - "Множество «шаблоны, объявляющие цель подмены» берётся ВЫЗОВОМ _swap_declaring_templates (заведён планом 09-02, задача 2), а не пересобирается вторым описанием: оно содержит и components/form_wrapper.html (печатает hx-target), и account_groups/includes/group_row.html (объявленный callers вызывающий) — без второго слагаемого правило D-08 зелено по построению, потому что x-data в обёртке нет вовсе"
    - "Действующий GATE-06 (узел с x-data не бывает целью hx-target) сохраняется дословно: новое правило его дополняет, а не заменяет — GATE-06 не ловит случай «цель чиста, а состояние у её потомков»"
    - "Каждое правило гейта, заведённое фазой, имеет случай контроля, который на подставленном дереве КРАСНЕЕТ — без него правило зелено по построению и не доказывает ничего"
    - "Все ДЕВЯТЬ движущихся инвентарных чисел фазы сведены и подтверждены одним прогоном; четыре неподвижных числа подтверждены неподвижными"
  artifacts:
    - "tests/test_templates/test_htmx_markup_gates.py — MACRO_BORN_EXCEPTIONS, MACRO_BORN_EXCEPTIONS_DECLARED, test_every_macro_born_exception_is_actually_outside_components"
    - "tests/test_templates/test_htmx_markup_gates.py — правило D-08 и его контроль"
    - "tests/test_templates/test_htmx_markup_gates.py — сводная летопись девяти движущихся чисел фазы и записанный результат измерения G-11"
  key_links:
    - "MACRO_BORN_EXCEPTIONS → ads/form.html: единственное сегодняшнее исключение, помеченное как предмет Фазы 12; ломается — перечень превращается в список без основания, и следующая фаза читает его как «наверное, забыли»"
    - "правило D-08 → _swap_declaring_templates (план 09-02) → PARAMETRIC_SWAP_TARGETS.callers → group_row.html: ломается — множество сводится к обёртке без x-data, правило и его контроль зелены по построению, и узел с выражением x-data попадает внутрь свапаемого фрагмента, теряя состояние молча"
  prohibitions:
    - statement: "действующий test_no_client_state_node_is_a_swap_target (GATE-06) не переименовывается и не заменяется предметом — правило Фазы 15, критерий 4"
      status: flagged-unverified
      verification: none
    - statement: "полный запрет x-data внутри цели подмены не вводится: он потребовал бы перенести на сервер и снятие кнопки, и открытие панели в этой же фазе (D-08, отвергнутая альтернатива)"
      status: flagged-unverified
      verification: none
    - statement: "форма автосохранения редактора объявлений на общий макрос-обёртку в этой фазе не переводится — она входит в перечень исключений и является предметом Фазы 12"
      status: flagged-unverified
      verification: none
    - statement: "jinja2 в гейт разметки не ввозится ради разбора AST шаблона: файл обходится re + ast, и шаблонизатор был бы второй моделью мира (D-08c отвергнут)"
      status: flagged-unverified
      verification: none
---

<objective>
Границы правил, которые фаза обязана поставить прежде, чем механизм разойдётся по 47 формам:
**«каждый `hx-post` рождён макросом»** (D-03 — то, что делает FORM-09 свойством построения) и
**«внутри цели подмены допустим только голый `x-data`»** (D-08 — то, чего действующий GATE-06
не ловит). Плюс сводная сверка девяти инвентарных чисел фазы одним прогоном.

Purpose: без первого правила свойства качества раздаются не макросом, а дисциплиной, и Фаза 11
переведёт часть форм мимо него молча. Без второго — узел, несущий клиентское состояние, попадёт
внутрь свапаемого фрагмента, и после подмены состояние потеряется без единого признака.

Output: два перечня-правила по идиоме SP-1, два контроля по SP-2, сводная летопись чисел.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-RESEARCH.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-PATTERNS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VALIDATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-01-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-02-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов фазы — в `09-01-PLAN.md` §«Artifacts this phase produces».
Этим планом создаются:

- константы `MACRO_BORN_EXCEPTIONS`, `MACRO_BORN_EXCEPTIONS_DECLARED`
- тесты `test_every_macro_born_exception_carries_a_reason`,
  `test_the_number_of_macro_born_exceptions_is_the_declared_one`,
  `test_every_macro_born_exception_is_actually_outside_components`,
  `test_every_htmx_post_is_born_of_a_component_macro`,
  `test_no_client_state_expression_lives_inside_a_swap_declaring_template`
- контроли `test_control_negative_a_post_outside_a_macro_reddens_the_gate`,
  `test_control_negative_an_expression_x_data_in_a_swap_template_reddens_the_gate`
</artifacts_this_phase_produces>

<tasks>

<task type="execute" tdd="true">
  <name>Задача 1: Правило «каждый `hx-post` рождён компонентным макросом» (D-03)</name>

  <files>tests/test_templates/test_htmx_markup_gates.py</files>

  <read_first>
- `tests/test_templates/test_htmx_markup_gates.py` — `:100-148` (летопись `HX_POST_PLACES`,
  регулярки `HX_POST_ATTR` / `HX_POST_TAG`), `:171-198` (`_all_templates` — единица обхода
  ФАЙЛ), `:232-259` (`_post_sites`, структура `Site` с полем `template`), `:1140-1166` (G-7 —
  образец правила «на каждом месте отправки объявлено …»), `:1383-1434` (ГРУППА КОНТРОЛЯ,
  `Substitution`, `_tree_with` и его двойной предохранитель).
- `tests/test_pages/test_impersonation_gate.py:89-97, 182-215, 589-608, 687-730` — источник SP-1.
- `app/templates/ads/form.html` — единственное **сегодняшнее** место `hx-post` вне компонентных
  макросов: форма автосохранения редактора объявлений. Прочитать окружающие комментарии, чтобы
  обоснование записи не звучало как «список устарел».
- `app/templates/components/form_wrapper.html`, `app/templates/components/modal.html` — два
  места `hx-post`, которые правилу удовлетворяют по построению.
- `app/templates/account_groups/includes/group_row.html:78-88` — форма-триггер удаления, которая
  `hx-post` **не получает** и получить не должна (D-03).
- `09-CONTEXT.md` §Decisions D-03 — основание, снятое сценарием «htmx поднялся, Alpine нет».
- `09-PATTERNS.md` §SP-1, §SP-2, §No Analog Found (строка про это правило).
  </read_first>

  <precondition>
Планы 09-01 и 09-02 приземлены: `HX_POST_PLACES = 3`, места отправки —
`ads/form.html`, `components/form_wrapper.html`, `components/modal.html`. Проверить
`uv run pytest tests/test_templates/test_htmx_markup_gates.py -q` — зелено до первой правки.
  </precondition>

  <behavior>
1. `test_every_htmx_post_is_born_of_a_component_macro` — зелёный на итоговом дереве: два места
   лежат в `app/templates/components/` внутри `{% macro %}`, третье (`ads/form.html`) записано
   исключением.
2. `test_every_macro_born_exception_carries_a_reason` — красный при пустом обосновании.
3. `test_the_number_of_macro_born_exceptions_is_the_declared_one` — красный при расхождении.
4. `test_every_macro_born_exception_is_actually_outside_components` — третье, **несущее**
   утверждение SP-1, и оно обязано существовать ОТДЕЛЬНЫМ ТЕСТОМ: запись перечня, чьё место
   фактически **удовлетворяет** общему правилу (лежит в `components/` внутри `{% macro %}`),
   краснеет. ⚠️ `test_every_htmx_post_is_born_of_a_component_macro` эту роль исполнить **не
   может**: устаревшая запись им просто никогда не читается — место и без неё зелено, перечень
   не консультируется, и запись живёт вечно, молча выводя из-под правила будущее настоящее
   нарушение с тем же путём. Три из четырёх перечней фазы третий тест несут; этот обязан нести
   тоже.
5. `test_control_negative_a_post_outside_a_macro_reddens_the_gate` — подстановка добавляет
   `hx-post` в шаблон вне `components/` ⇒ правило краснеет.
  </behavior>

  <action>
Правило: место `hx-post` (`_post_sites`) обязано лежать в файле каталога
`app/templates/components/`, и в этом файле обязан присутствовать `{% macro `, — **либо** путь
файла записан в `MACRO_BORN_EXCEPTIONS`.

`MACRO_BORN_EXCEPTIONS: dict[str, str]` — одна запись: `"ads/form.html"` → обоснование,
называющее (а) что это форма автосохранения редактора объявлений, отгруженная до вехи и
идущая своим ответом-фрагментом; (б) что её перевод на общий макрос-обёртку — **предмет
Фазы 12** (FETCH-01), а не забытая работа. Формулировка обязана читаться как «место заявлено
исключением с назначенной фазой», а не как «список устарел».

`MACRO_BORN_EXCEPTIONS_DECLARED = 1` — отдельной константой с ⚠️-комментарием по форме
`test_impersonation_gate.py:89-97`: беззвучно выросшее число означает, что форма получила
`hx-post` мимо макроса, а решения о ней никто не принимал; беззвучно упавшее — что исключение
закрыто, и это обязано быть записано следующей записью летописи.

**Третий, несущий тест — `test_every_macro_born_exception_is_actually_outside_components`.**
Каждая запись обязана ФАКТИЧЕСКИ находиться в исключаемом положении: её путь **не** лежит в
`app/templates/components/` **либо** в файле нет `{% macro `. Запись, чьё место переехало в
компонентный макрос (то есть работа Фазы 12 сделана), обязана краснеть — иначе перечень
превращается в ложь, а число не двигается. ⚠️ Роль третьего утверждения общее правило
`test_every_htmx_post_is_born_of_a_component_macro` исполнить не может: устаревшая запись им
никогда не консультируется.

⚠️ Тот же перечень заводит и план 09-01, задача 3 — `DISABLED_ELT_EXCEPTIONS["ads/form.html"]`,
на то же место и с той же назначенной фазой. Это **не дубликат**: там исключение из правила
«объявлен `hx-disabled-elt`», здесь — из правила «рождён компонентным макросом». Обоснования
обязаны называть **одну и ту же** причину (Фаза 12, FETCH-01) и одну и ту же назначенную фазу,
чтобы Фаза 12 закрыла обе записи одним движением, а не нашла вторую через полгода.

Шапка перечня обязана выписать **разделение двух списков** (идиома D-14 Фазы 8): здесь —
«никогда не пойдёт по общему правилу либо назначено другой фазе»; «ещё не переведено» живёт
в `NOT_YET_CONVERTED` другого файла и с этим перечнем не смешивается.

Докстринг правила обязан назвать, ЧТО именно правило защищает: свойства качества
(`hx-disabled-elt`, `hx-indicator`, совпадение `hx-post` с `action`) раздаёт **один макрос**, и
без правила они держатся дисциплиной ревью 47 форм поотдельности — ровно то, от чего FORM-09
отказывается. И обязан назвать записанное следствие D-03: форма-триггер в строке группы
`hx-post` **не получает**, потому что при поднявшемся htmx и неподнявшемся Alpine он удалил бы
группу без подтверждения и без единого видимого признака.

Контроль по SP-2: `test_control_negative_a_post_outside_a_macro_reddens_the_gate` — подстановка
добавляет `hx-post="/x"` в шаблон вне `components/`, не входящий в перечень; образец подстановки
обязан встречаться в исходнике **ровно один раз** и результат обязан отличаться от исходника.
Текст отказа называет ПОСЛЕДСТВИЕ: форма получила отправку htmx, не получив ни блокировки
повторного нажатия, ни индикатора, ни проверки совпадения адреса.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "macro_born or born_of_a_macro or control" -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "born_of_a_component_macro" -x` — зелено.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "macro_born_exception" -q` —
  **три** теста зелены: обоснование, число и «запись фактически вне `components/`».
- `grep -v '^#' tests/test_templates/test_htmx_markup_gates.py | grep -c 'MACRO_BORN_EXCEPTIONS_DECLARED = 1'` возвращает `1`.
- Обоснование записи содержит подстроку `Фазы 12`, и проверка **привязана к значению записи**, а
  не к файлу целиком; обе записи об `ads/form.html` называют одну и ту же назначенную фазу:
  `uv run python -c "import sys; sys.path.insert(0,'tests/test_templates'); from test_htmx_markup_gates import MACRO_BORN_EXCEPTIONS as M, DISABLED_ELT_EXCEPTIONS as D; assert 'Фазы 12' in M['ads/form.html']; assert 'Фазы 12' in D['ads/form.html'].reason; assert len(M) == 1"`
- Форма-триггер не тронута: `hx-post` не появился в разметке строки группы. ⚠️ Считается по
  исходнику **без комментариев** — абзац WHY, объясняющий, почему `hx-post` на неё не ставится
  (D-03), обязан в файле быть, и грубый греп краснел бы на исполненном требовании:
  `uv run python -c "import re,pathlib; s=pathlib.Path('app/templates/account_groups/includes/group_row.html').read_text(); s=re.sub(r'\{#.*?#\}','',s,flags=re.S); s=re.sub(r'<!--.*?-->','',s,flags=re.S); assert 'hx-post' not in s"`
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "control" -q` — зелено; число
  собранных контролей выросло ровно на 1.
  </acceptance_criteria>

  <done>
FORM-09 стало свойством построения: любое новое место `hx-post`, не рождённое компонентным
макросом, роняет суиту, а исключение требует написать причину и подвинуть число — то есть
принять решение.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: Граница клиентского состояния внутри цели подмены (D-08)</name>

  <files>tests/test_templates/test_htmx_markup_gates.py</files>

  <read_first>
- `tests/test_templates/test_htmx_markup_gates.py` — докстринг файла, раздел «⚠️ ЧЕГО ГЕЙТ НЕ
  ВИДИТ», пункты **б** («обход не разворачивает наследование и включения») и **в** («признаком
  клиентского состояния считается ровно один атрибут, и вложенность не учитывается»);
  `:938-1006` (`CLIENT_STATE_NODES`, `CLIENT_STATE_ATTR` / `CLIENT_STATE_TAG`,
  `_client_state_sites`), `:1045-1062` (`_swap_target_ids`), `:1140-1166` (`_offenders_client_state_is_a_target`),
  `:1340-1380` (`test_the_number_of_client_state_nodes_is_the_declared_one` и
  `test_no_client_state_node_is_a_swap_target` — **GATE-06, сохраняется дословно**),
  `:1585-1614` (`test_control_negative_a_client_state_node_as_a_target_reddens_the_gate` —
  готовый образец контроля этого семейства), `:1383-1434` (ГРУППА КОНТРОЛЯ).
- `tests/test_templates/test_htmx_markup_gates.py` — **`_swap_declaring_templates` и
  `PARAMETRIC_SWAP_TARGETS`, заведённые планом 09-02, задача 2**, и `09-02-PLAN.md` §задача 2
  шаг 2 целиком: форма ключа и поле `callers` заданы там и здесь **не пересматриваются**.
- `09-01-PLAN.md` §«Форма ключа параметрической записи» в `<objective>` — три правила фазы
  (ключ = файл разборщика, γ1, `callers`); читать, чтобы не завести четвёртое.
- `app/templates/components/form_wrapper.html` — ⚠️ файл, который **и есть** место `hx-target`
  для разборщика, и в котором `x-data` НЕТ ВОВСЕ: одного его в множестве недостаточно.
- `app/templates/account_groups/includes/group_row.html` — после планов 09-01/09-02: голый
  `x-data` на `<span>` с `x-init` и голый `x-data` на форме-триггере удаления; панель
  подтверждения (единственный узел с `x-data`-**выражением**) стоит СНАРУЖИ строки.
  ⚠️ Сам `hx-target` он **не несёт** — в множество попадает только полем `callers`.
- `app/templates/components/modal.html:94-102` — `x-data` с выражением (`open`, `sending`,
  `opener`, ловушка фокуса) — узел, ради которого правило и пишется.
- `09-RESEARCH.md` §4.1 «Цена для D-08» — три реализации по возрастанию цены, рекомендация
  **D-08a**, и почему D-08b (счётчик глубины по исходнику) — свойство сегодняшнего
  форматирования, а не построения, а D-08c (AST Jinja) — вторая модель мира.
- `tests/test_pages/test_account_groups.py:57-71` (`_row_html`) — прецедент счётчика глубины **по
  отрендеренному HTML**; для выбранной реализации он не нужен, но должен быть прочитан, чтобы
  выбор был сделан со знанием альтернативы.
  </read_first>

  <reversibility rating="costly">
D-08 помечен `costly`: правило распространяется на все свапаемые фрагменты Фаз 10-15.
Реализация выбрана **шире буквы** решения (D-08a), и это обязано быть выписано докстрингом, а не
подразумеваться — иначе следующая фаза прочитает правило как точную формулировку D-08 и
удивится, почему оно краснеет на узле, лежащем вне цели.
  </reversibility>

  <behavior>
⚠️ **Множество «шаблоны, объявляющие цель подмены» на итоговом дереве состоит из ДВУХ файлов, и
ни один из них не подразумевается — оба перечислены.** Прежняя редакция этой задачи утверждала,
что такой шаблон один и это `account_groups/includes/group_row.html`; утверждение было **ложным**:
`group_row.html` сам `hx-target` не несёт вовсе, он лишь передаёт `target=` в макрос-обёртку.

Множество берётся **вызовом `_swap_declaring_templates(templates)`** (заведён планом 09-02,
задача 2, рядом с `PARAMETRIC_SWAP_TARGETS`) и здесь **не пересобирается**: два описания одного
множества разошлись бы молча, и правило говорило бы о дереве, которого нет. Его состав:
1. `components/form_wrapper.html` — из `_target_sites`, потому что `hx-target` написан здесь.
   `x-data` в этом файле **нет вовсе**, поэтому одного этого слагаемого правилу мало: на нём оно
   зелено по построению и контроль не краснеет ни на чём.
2. `account_groups/includes/group_row.html` — из `PARAMETRIC_SWAP_TARGETS[...].callers`,
   единственного объявленного пути к вызывающему. Здесь и живут оба голых `x-data`, и здесь
   подстановка контроля обязана сработать.

`test_no_client_state_expression_lives_inside_a_swap_declaring_template` — зелёный на итоговом
дереве: в обоих файлах множества каждый `x-data` голый (`x-data` без `=`). Контроль
`test_control_negative_an_expression_x_data_in_a_swap_template_reddens_the_gate` подставляет в
`group_row.html` `x-data="{ open: false }"` ⇒ правило краснеет — и это проверяемо, потому что
`group_row.html` в множество ВХОДИТ.

⚠️ **Предусловие задачи, проверяемое до первой строки кода:**
`account_groups/includes/group_row.html` обязан лежать в `_swap_declaring_templates(_all_templates())`.
Если его там нет, план 09-02, задача 2 не приземлилась в объявленной форме, и писать правило
бессмысленно — оно будет зелёным по построению.
  </behavior>

  <action>
Реализовать вариант **D-08a**: «в шаблоне, объявляющем цель подмены, каждый `x-data` обязан быть
голым — без `=` и без выражения». Считается уже имеющимися `_client_state_sites` плюс регуляркой
`x-data\s*=`; вложенность не считается и **не должна** считаться.

Множество шаблонов — **вызов `_swap_declaring_templates(templates)`**, и ничего больше. Правило
**не** пересобирает его из `_target_sites`, **не** читает `PARAMETRIC_SWAP_TARGETS` напрямую и
**не** заводит собственного сканера вызывающих: и ключ, и поле `callers`, и их согласие с
`_macro_callers` — предмет плана 09-02, задача 2, и здесь только потребляются. Второе описание
одного множества — это вторая модель мира, и разошлась бы она молча.

Докстринг обязан быть честным в **четырёх** пунктах, иначе правило прочтут как точную
формулировку D-08:
0. **Откуда берётся множество и почему в нём ДВА файла.** `components/form_wrapper.html` — потому
   что `hx-target` написан там; `account_groups/includes/group_row.html` — потому что он объявлен
   вызывающим в `PARAMETRIC_SWAP_TARGETS`. Назвать прямо: без второго слагаемого множество
   состоит из файла, в котором `x-data` нет вовсе, и правило зелено по построению — то есть не
   доказывает ничего. Это и есть цена, которую платит любой файловый разборщик, встретив
   макрос-обёртку.
1. **Правило шире буквы решения.** D-08 говорит «внутри цели свапа», правило говорит «в шаблоне,
   объявляющем цель свапа». Оно ловит и то, что лежит снаружи цели в том же файле.
2. **Почему точная форма не выбрана.** Счётчик глубины по исходнику шаблона (D-08b) спотыкается
   о `{% if %}` с открывающими и закрывающими тегами в разных ветках; на сегодняшнем
   `group_row.html` он сошёлся бы, но это свойство форматирования, а не построения. Разбор в
   AST Jinja (D-08c) потребовал бы ввезти шаблонизатор в файл, который сегодня обходится
   `re` + `ast`, — вторая модель мира, которую придётся держать верной все Фазы 10-15.
3. **Что правило НЕ заменяет.** Действующий `test_no_client_state_node_is_a_swap_target`
   (GATE-06) сохраняется **дословно** и остаётся отдельным утверждением: он ловит «узел с
   состоянием сам является целью», новое правило — «состояние живёт у потомков цели, а цель
   чиста». Переименование существующего теста с подменой предмета недопустимо (правило Фазы 15,
   критерий 4).

Текст отказа обязан называть ПОСЛЕДСТВИЕ: узел с клиентским состоянием, попавший внутрь
свапаемого фрагмента, после подмены теряет состояние — и не сообщает об этом ничем; панель
подтверждения с ловушкой фокуса в такой ситуации задваивается или умирает молча.

Контроль по SP-2 — форма из `:1585-1614`, `_tree_with(tmp_path, Substitution(...))`, образец
подстановки встречается ровно один раз, результат отличается от исходника.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "client_state or control" -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "client_state" -q` — все тесты
  семейства зелены, включая неизменённый `test_no_client_state_node_is_a_swap_target` и
  `test_the_number_of_client_state_nodes_is_the_declared_one` (`CLIENT_STATE_NODES = 24`).
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "client_state_expression" -x` — зелено.
- ⚠️ **Предусловие, проверяемое ДО правки** (иначе правило зелено по построению):
  `uv run python -c "import sys; sys.path.insert(0,'tests/test_templates'); from test_htmx_markup_gates import _all_templates, _swap_declaring_templates; s=_swap_declaring_templates(_all_templates()); assert 'account_groups/includes/group_row.html' in s and 'components/form_wrapper.html' in s, sorted(s)"`
- Правило потребляет помощник, а не пересобирает множество. Проверяется **положительно**:
  в теле `test_no_client_state_expression_lives_inside_a_swap_declaring_template` встречается
  вызов `_swap_declaring_templates(`. ⚠️ Отрицательный греп на `PARAMETRIC_SWAP_TARGETS` здесь
  недопустим: пункт 0 докстринга ОБЯЗАН назвать этот перечень словами, и проверка краснела бы
  ровно на исполненном требовании.
- Второе множество слагаемых действительно достигнуто, а не заявлено:
  `uv run python -c "import sys; sys.path.insert(0,'tests/test_templates'); import test_htmx_markup_gates as g; f=g._offenders_client_state_expression; import inspect; assert '_swap_declaring_templates' in inspect.getsource(f)"`
  (имя функции-сборщика нарушителей взять фактическое, если оно отличается).
- `grep -c 'def test_no_client_state_node_is_a_swap_target' tests/test_templates/test_htmx_markup_gates.py` возвращает `1` — тест не переименован.
- Докстринг нового правила содержит подстроки `шире` и `GATE-06` — граница названа словами:
  `uv run python -c "import pathlib; s=pathlib.Path('tests/test_templates/test_htmx_markup_gates.py').read_text(); assert 'шире' in s and 'GATE-06' in s"`.
- Временная проверка зубов, выполняемая и откатываемая исполнителем: заменить голый `x-data` на
  `x-data="{ open: false }"` в `group_row.html` ⇒
  `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k client_state_expression` краснеет; откатить.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "control" -q` — зелено; число
  собранных контролей выросло ровно на 1.
  </acceptance_criteria>

  <done>
Граница «состояние живёт снаружи подменяемого» стала машинным правилом с честно выписанной
границей и доказанными зубами: множество шаблонов взято вызовом `_swap_declaring_templates` и
содержит **оба** файла — и обёртку, печатающую `hx-target`, и объявленного вызывающего, в котором
`x-data` действительно живёт, — поэтому контроль краснеет на настоящем узле, а не «проходит» на
пустоте. Действующий GATE-06 сохранён дословно и не подменён предметом.
  </done>
</task>

<task type="execute">
  <name>Задача 3: Сводная летопись девяти чисел и полный круг контролей</name>

  <files>tests/test_templates/test_htmx_markup_gates.py</files>

  <precondition>
План 09-04 приземлён целиком: `tests/test_pages/test_shell.py` зелен, а
`app/templates/includes/htmx_error_banner.html` уже правлен. Проверить
`uv run pytest tests/test_pages/test_shell.py -q` — зелено **до** первой правки этой задачи.
⚠️ Основание — TDD-порядок плана 09-04: он сознательно кладёт красный
`test_the_network_banner_names_the_screen_server_divergence` ДО правки шаблона. Полный прогон
этой задачи, попавший в то окно, упал бы по чужой причине, и исполнитель потратил бы круг на
поиск регрессии, которой нет. Ради этого 09-03 объявлен волной 4 с зависимостью от 09-04, хотя
файлы планов не пересекаются: связь **временнáя**, и она объявлена, а не оставлена на удачу.
  </precondition>

  <read_first>
- `tests/test_templates/test_htmx_markup_gates.py` — `:100-110` (форма летописи: «уменьшение
  объявленного числа допустимо и означает СОЗНАТЕЛЬНОЕ снятие места, записанное следующей
  записью этой летописи»), `:925-943` (три комментария-летописи целей, OOB-блоков и узлов
  состояния), `:899-918` (литерал `len(paths)`), `:1643-1674`
  (`test_control_positive_the_untouched_tree_keeps_every_gate_green` — контроль, утверждающий,
  что на **нетронутом** дереве сходится каждое число).
- `tests/test_pages/test_htmx_gates.py:185-235` (`NOT_YET_CONVERTED`, `NOT_YET_CONVERTED_COUNT`
  и её ⚠️-комментарий), `:688-724` (два теста на одно число, тексты отказа разные).
- `09-RESEARCH.md` §3 — таблица «было (измерено) → станет» со ссылками на тесты-читатели.
- `09-01-SUMMARY.md` и `09-02-SUMMARY.md` — записанные движения чисел и измеренное значение
  литерала `len(paths)`.
  </read_first>

  <action>
Задача **не двигает ни одного числа** — она сводит летопись и доказывает, что круг замкнут.

1. **Свести ДЕВЯТЬ движущихся чисел фазы** в единый комментарий-летопись в
   `tests/test_templates/test_htmx_markup_gates.py`, рядом с объявлениями констант, по форме
   `:100-110`. Записать по каждому: было → стало и **из чего складывается** новое значение:
   `HX_POST_PLACES` 1 → 3 (`ads/form.html`, `components/form_wrapper.html`,
   `components/modal.html`); `HX_TARGETS` 0 → 1 (форма тумблера через макрос-обёртку;
   константа считает **места**, а не `#`-идентификаторы); `OOB_BLOCKS` 6 → 9 (счётчик, снятие
   строки, снятие панели — счётчик выписан **одним** источником, поэтому 9, а не 10);
   `NOT_YET_CONVERTED_COUNT` 36 → 34 (**движение счётчика прогресса вехи**, а не правка теста);
   `MACRO_DEFINITION_SITES_DECLARED` 0 → 2 (перечень **обязан опустеть в Фазе 10**);
   `DISABLED_ELT_EXCEPTIONS_DECLARED` 0 → **2** (`components/form_wrapper.html` — вызывающий
   передаёт свой селектор, D-06; `ads/form.html` — атрибута нет вовсе, назначено **Фазе 12**);
   `MACRO_BORN_EXCEPTIONS_DECLARED` 0 → 1; `PARAMETRIC_SWAP_TARGETS_DECLARED` 0 → 1 (девятое
   число, объявленное планом 09-02 **безусловно**);
   литерал `len(paths)` в `test_both_branches_of_the_editor_action_are_extracted` — значение,
   **измеренное** планами 09-01 и 09-02, с записью, из чего множество складывается и почему
   места определения макросов в него не входят.
   ⚠️ Десятое число — `ID_IN_TWO_ROLES_DECLARED` — существует **только**, если измерение
   плана 09-02, задача 2, шаг 1 дало непустое пересечение. Летопись обязана назвать результат
   измерения в любом случае: «пересечение пусто, второй перечень не заведён» — это запись, а не
   умолчание.

2. **Подтвердить неподвижными** четыре числа отдельным утверждением, а не умолчанием:
   `CLIENT_STATE_NODES = 24`, `FRAGMENT_ROUTES_DECLARED = 12`, `HX_HEADER_WRITES = 2`,
   `POST_HANDLERS = 36`. У каждого комментарий-летопись пополняется строкой «Фаза 9 это число
   не двигала, и вот почему»: `x-data` переехал с формы на `<span>` внутри той же строки;
   оба переведённых обработчика упоминают `respond` ⇒ `degrades=True` ⇒ фрагментными не
   считаются; заголовки пишет слой ответа, новых мест записи фаза не завела; маршрутов фаза не
   заводила.

3. **Проверить, что круг контролей замкнут.** Перечислить в комментарии раздела ГРУППЫ КОНТРОЛЯ
   каждое правило, заведённое фазой, и имя его контроля; утверждением
   `test_control_positive_the_untouched_tree_keeps_every_gate_green` дополнить проверку новых
   чисел (`MACRO_DEFINITION_SITES_DECLARED`, `DISABLED_ELT_EXCEPTIONS_DECLARED`,
   `MACRO_BORN_EXCEPTIONS_DECLARED`, `PARAMETRIC_SWAP_TARGETS_DECLARED` и, если он заведён,
   `ID_IN_TWO_ROLES_DECLARED`), чтобы положительный контроль не отставал от отрицательных.
   ⚠️ Перечень контролей фазы обязан включать **два, читающих не шаблоны, а стили** —
   `test_control_negative_the_runtime_indicator_class_reddens_the_gate` и
   `test_control_negative_a_threshold_on_the_resting_rule_reddens_the_gate` (план 09-01,
   задача 3): они подают изменённую копию `app.css` через `_css_with`, потому что `_tree_with`
   подменяет только шаблоны, и без них два правила индикатора были бы зелены по построению.
   Правило без контроля в перечне быть не должно; если такое обнаружилось — это находка, и её
   записывают в сводку, а не заглаживают.

4. **Сверить с разведкой и записать расхождения.** Прогнать
   `uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q`
   и сравнить фактические значения с таблицей `09-RESEARCH.md` §3. Каждое расхождение записать в
   сводку плана **с основанием**; «сошлось» без приведённых значений недопустимо — зелёный цвет
   сломанного разбора посимвольно совпадает с зелёным цветом соблюдённого правила.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q` — зелено целиком.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q` — зелено целиком.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "control_positive" -x` — зелено,
  и тест читает **все** новые константы фазы (проверяется грепом имён внутри его тела).
- Летопись перечисляет ровно **девять** движущихся и четыре неподвижных числа, плюс запись о
  результате измерения G-11 (заведён ли десятый); проверяется прочтением сводки плана, где они
  выписаны списком с фактическими значениями прогона.
- **Собственный круг фазы — сначала и отдельно:**
  `uv run pytest tests/test_templates/ tests/test_pages/ -q` — зелено. Это тот круг, за который
  задача отвечает; красный в нём — регрессия фазы.
- **Полный прогон — вторым шагом и с названным окном:** `uv run pytest tests/ -q`. Допустимых
  красных ровно два вида, и оба обязаны быть названы в сводке **по имени**:
  (1) известный долг `full-suite-ads-editor-order-pollution` (`.planning/todos/pending/`);
  (2) ⚠️ красный из `tests/test_pages/test_shell.py` — **признак того, что предусловие задачи не
  выполнено**: план 09-04 не приземлён целиком, и прогон попал в его TDD-окно. Такой красный
  регрессией фазы **не является** и заглаживаться правкой гейта **не должен**: задача
  останавливается, ждёт 09-04 и повторяет прогон. Любой другой красный — регрессия фазы.
  </acceptance_criteria>

  <done>
Все девять чисел фазы сведены в одну летопись с указанием, из чего складывается каждое; результат
измерения G-11 записан независимо от того, заведён ли десятый перечень; четыре неподвижных
подтверждены явно; положительный контроль читает все новые константы, отрицательные контроли
перечислены поимённо, включая два, подающих изменённую копию `app.css`. Полный прогон суиты
зелёный, кроме названного долга.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| автор будущей формы → гейт разметки | гейт — единственное, что удерживает свойства качества на 47 формах; ослабление его условия неотличимо от снятия |
| перечень исключений → правило | запись без обоснования и без утверждаемого числа выводит место из-под правила молча |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-09 | Information Disclosure | новое правило гейта как способ ослабить проверку | medium | mitigate | Каждый перечень — SP-1: обоснование на запись, отдельно утверждаемое ЧИСЛО, и утверждение, что запись фактически находится в исключаемом положении (устаревшая запись краснеет). Каждое правило — SP-2: контроль, который на подставленном дереве краснеет |
| T-9-12 | Elevation of Privilege | форма с `hx-post`, заведённая мимо макроса-обёртки | high | mitigate | `test_every_htmx_post_is_born_of_a_component_macro`: место вне `components/` и вне перечня роняет суиту. Форма без макроса не получила бы ни `hx-disabled-elt` (двойная отправка), ни посимвольного совпадения `hx-post` с `action` (адрес в никуда без JS) |
| T-9-13 | Tampering | узел с клиентским состоянием внутри свапаемого фрагмента | medium | mitigate | Правило D-08a: в шаблоне, объявляющем цель подмены, каждый `x-data` голый. Панель подтверждения с ловушкой фокуса обязана остаться снаружи — иначе после подмены их станет две с одним `id` (T-11-04) |
| T-9-14 | Repudiation | молчаливое движение инвентарного числа | medium | mitigate | Летопись: каждое число правится вместе с комментарием, перечисляющим, из чего оно складывается; `test_control_positive_the_untouched_tree_keeps_every_gate_green` читает все новые константы, поэтому пропущенное обновление положительного контроля само краснеет |
| T-9-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План правит один тестовый файл и не выполняет ни одной установки |
</threat_model>

<verification>
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q` — зелено после каждой задачи.
- `uv run pytest tests/test_templates/ tests/test_pages/ -q` — собственный круг фазы, зелёный.
- `uv run pytest tests/ -q` — полный прогон на гейте фазы. Допустимые красные названы поимённо в
  `<acceptance_criteria>` задачи 3: известный долг `full-suite-ads-editor-order-pollution` и —
  как признак невыполненного предусловия, а не регрессии — красный из `test_shell.py`, если
  план 09-04 ещё не приземлён целиком. Оба обязаны быть названы отдельно от результатов фазы.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
</verification>

<success_criteria>
1. Любое место `hx-post` вне компонентного макроса роняет суиту, либо записано исключением с
   обоснованием и утверждаемым числом.
2. Любой `x-data` с выражением в шаблоне, объявляющем цель подмены, роняет суиту; GATE-06
   сохранён дословно и не подменён предметом.
3. У каждого нового правила есть контроль, краснеющий на подставленном дереве.
4. Девять движущихся чисел сведены в летопись с указанием состава; четыре неподвижных
   подтверждены явно; результат измерения G-11 записан в любом случае.
5. Положительный контроль читает все новые константы фазы.
6. Множество «шаблоны, объявляющие цель подмены» взято **вызовом** `_swap_declaring_templates`
   и содержит объявленного вызывающего — правило D-08 не зелено по построению.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-03-SUMMARY.md` when done.

Сводка обязана выписать списком фактические значения всех тринадцати чисел (девять движущихся,
четыре неподвижных) с указанием прогона, из которого они получены; назвать результат измерения
G-11 и заведён ли `ID_IN_TWO_ROLES_DECLARED`; и отдельно назвать известный долг
`full-suite-ads-editor-order-pollution`, если он проявился.
</output>
