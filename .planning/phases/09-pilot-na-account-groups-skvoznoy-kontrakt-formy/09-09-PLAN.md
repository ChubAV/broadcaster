---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 09
type: execute
wave: 4
depends_on: ["09-06", "09-07", "09-08"]
gap_closure: true
files_modified:
  - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
  - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
  - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-04-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - app/templates/account_groups/includes/group_row.html
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: false
requirements: [QUAL-01, QUAL-02, QUAL-06]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Допущение A1 («браузер снимает фокус с элемента, которому выставлена блокировка») ИЗМЕРЕНО, а не предположено: в таблице отметок пункта 3 стоит фактический ответ «да» или «нет», снятый живым `document.activeElement` при нетронутой мыши"
    - "Конфликт D-06 ↔ QUAL-06 разрешён РЕШЕНИЕМ ВЛАДЕЛЬЦА, принятым ПОСЛЕ измерения, а не выбранным заранее: исполнитель ни одного из трёх записанных способов не применяет самовольно"
    - "Допущение A4 («отбрасывающая форма синхронизации под запрет гейта Фазы 8 не подпадает») ПРОЧИТАНО по исходнику гейта и записано дословно ДО того, как владелец выбирает вариант (б) — иначе выбор делался бы вслепую"
    - "Таблицы отметок пунктов 3, 5 и 6 в `09-UAT.md` заполнены НАБЛЮДЁННЫМ ПРИЗНАКОМ: отметка без наблюдённого признака закрытием не считается, и слово «ок» отметкой не является"
    - "Артефакт ручного обхода написан по ОТГРУЖЕННОМУ состоянию, а не по намерению: наблюдаемые признаки пересняты после планов 09-05…09-08, и новая поверхность (починка курсора прокрутки, пустое состояние под поиском) внесена в обход отдельными проверками"
    - "Бухгалтерия сошлась в ОДНУ сторону: `.planning/REQUIREMENTS.md` остаётся источником истины, а сводка плана 09-04 приводится к нему, а не наоборот (WARN-8)"
    - "Состояний требований этот план НЕ ПЕРЕВОДИТ вовсе: столбец `Status` и флажки в теле `.planning/REQUIREMENTS.md` остаются нетронутыми, а план оставляет вместо них раздел свидетельств — на каждое требование машинная проверка, наблюдённый признак и вердикт. Перевод состояний — работа повторной верификации, которая побежит после плана и увидит и суиту, и заполненные таблицы отметок"
    - "Запись исхода D-06 ↔ QUAL-06 живёт в ДОЛГОЖИВУЩЕМ месте (`.planning/REQUIREMENTS.md`, рядом со строкой QUAL-06), а копия в `09-VERIFICATION.md` помечена копией и несёт прямое указание повторной верификации сохранить её: артефакт верификации перегенерируется, и единственный экземпляр записи в нём исчез бы вместе с перегенерацией"
    - "Успех фазы не объявлен по зелёной суите: суита утверждает ДОСТАВКУ разметки и заголовков, а не ЭФФЕКТ подмены, приземления, фокуса и видимости"
  artifacts:
    - ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md — заполненные таблицы отметок пунктов 3, 5 и 6, разделы «Как привести стенд в нужное состояние» и «Как убрать стенд»"
    - ".planning/REQUIREMENTS.md — раздел «Фаза 9 — свидетельства к пересмотру состояний» и, при исходе `accept-deviation`, долгоживущая запись overrides рядом со строкой QUAL-06"
    - ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md — помеченная копия записи overrides либо запись закрытия A1 измерением"
  key_links:
    - "измерение 3.3 → решение владельца → отметки требований: рвётся — QUAL-01 и QUAL-06 наследуются Фазой 15 как факт, которого не было"
    - "`09-UAT.md` → требование GATE-09 Фазы 15: сводное закрытие девяти пунктов наследует заполненные отметки, а не задачу собрать перечень заново"
  prohibitions:
    - statement: "браузерный стенд не заводится: пункты 3, 5 и 6 закрываются глазами человека, `E2E-01` остаётся отложенным"
      status: flagged-unverified
      verification: none
    - statement: "вариант «блокировать обёртку метки вместо чекбокса» не воскрешается: он назван негодным ПО СУЩЕСТВУ (блокировка на этом элементе не действует), и назван ровно затем, чтобы его не пробовали как самый дешёвый"
      status: flagged-unverified
      verification: none
    - statement: "исполнитель обхода не выбирает способ снятия конфликта D-06 ↔ QUAL-06 сам и не подставляет ожидаемый ответ вместо наблюдённого"
      status: flagged-unverified
      verification: none
    - statement: "состояния требований этим планом не переводятся ВОВСЕ: столбец `Status` и флажки в теле `.planning/REQUIREMENTS.md` остаются нетронутыми, перевод делает повторная верификация; отметки, откаченные коммитом `183511c`, обратно не ставятся ни молча, ни по суждению"
      status: flagged-unverified
      verification: none
    - statement: "седьмой ключ блока конфигурации htmx не добавляется и третий встроенный обработчик событий не заводится — даже если выбранный вариант потребовал бы удобства"
      status: flagged-unverified
      verification: none
---

<objective>
Закрыть **GAP-3** (ручной обход не был проведён) и **GAP-2** (конфликт D-06 ↔ QUAL-06 разрешён
не был), а следом свести бухгалтерию требований в одну сторону — к
`.planning/REQUIREMENTS.md`.

Purpose: `09-UAT.md` существует как ПРОЦЕДУРА, написанная по отгруженному состоянию, и это
хорошо. Но её три таблицы отметок ПУСТЫ, статус `pending`, а допущения A1, A3 и A5 не измерены
ни разу. Записанное правило фазы говорит прямо: **«УСПЕХ ФАЗЫ 9 НЕ ОБЪЯВЛЯЕТСЯ ПО ЗЕЛЁНОЙ
СУИТЕ»** — суита утверждает доставку разметки и заголовков, а не то, что подмена произошла,
счётчик приземлился, фокус куда-то вернулся и индикатор был виден глазу.

Отдельно: конфликт D-06 ↔ QUAL-06 покоится на допущении A1, которое **не проверялось ни разу**.
Если фокус переживает блокировку — конфликта не существует вовсе, и это надо записать так же
явно, как записано ожидание «нет». Сначала измерение, потом решение; не наоборот.

Output: заполненные наблюдённым признаком таблицы отметок, записанное в долгоживущем месте
решение владельца, раздел свидетельств в `.planning/REQUIREMENTS.md` (машинная проверка +
наблюдённый признак + вердикт на каждое из пяти требований), согласованный с ним
`09-VERIFICATION.md`, исправленная сводка плана 09-04 и убранный посевной стенд.

⚠️ **Состояний требований этот план НЕ переводит.** Преждевременная отметка «выполнено» — та
самая ошибка, что породила третий пробел фазы и потребовала отката коммитом `183511c`; здесь на
её месте стоит не суждение исполнителя, а запрет с машинной проверкой диффом. Перевод состояний
делает повторная верификация, читая раздел свидетельств.

⚠️ **`autonomous: false`.** План содержит два останова, ни один из которых не имеет права быть
одобрен автоматически: измерение — потому что его подстановка обесценила бы всю фазу, решение —
потому что D-06 локировано и помечено дорогим, а выбор принадлежит владельцу.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

⚠️ **Матрица покрытия внешнего API не заводится, и это записанный пропуск, а не молчание.**
Детерминированный обход области фазы вернул «внешней интеграции не обнаружено»
(`check api-coverage-verify-pre` на каталоге фазы: `detected: false`, `block: false`), и по
правилу самой способности при таком исходе проверка пропускается целиком, а файла матрицы не
требуется. Предмет фазы — шаблоны, обработчики и правила гейтов внутри приложения.

Символы и правки, заводимые этим планом:

- В `09-UAT.md`: новые проверки **3.5** (курсор прокрутки переживает удаление) и **5.3**
  (пустое состояние под активным поиском) с собственными наблюдаемыми признаками; заполненные
  таблицы отметок пунктов 3, 5 и 6; разделы «Как привести стенд в нужное состояние» и **«Как
  убрать стенд»** (посев с префиксом `UAT9-` и уборка по нему же); раздел «Что измерено» с
  фактическими ответами по A1, A3, A5 и записью прочитанной формулировки гейта по A4.
- В `.planning/REQUIREMENTS.md`: новый раздел
  **`### Фаза 9 — свидетельства к пересмотру состояний (заполнено планом 09-09)`** — по строке
  на каждое из пяти требований фазы: машинная проверка, наблюдённый признак, вердикт. Столбец
  `Status` и флажки в теле файла план НЕ трогает: перевод состояний — работа повторной
  верификации.
- В `.planning/REQUIREMENTS.md` при исходе `accept-deviation`: **долгоживущая запись overrides**
  рядом со строкой QUAL-06 — тот экземпляр, который переживёт перегенерацию артефакта
  верификации.
- В `09-VERIFICATION.md`: либо ПОМЕЧЕННАЯ КОПИЯ записи `overrides:` с указанием, где лежит
  оригинал и что повторная верификация обязана её сохранить, либо запись закрытия допущения A1
  измерением — ровно одна из двух, в зависимости от исхода.
- В `09-04-SUMMARY.md`: поле `requirements-completed` приведено к
  `.planning/REQUIREMENTS.md` — с ПОМЕТКОЙ об исправлении, а не молча (WARN-8).
- **Только при исходе «фокус теряется» и выборе варианта (а) или (б):** правка
  `app/templates/account_groups/includes/group_row.html` и записи `DISABLED_ELT_EXCEPTIONS` /
  `DISABLED_ELT_EXCEPTIONS_DECLARED` в `tests/test_templates/test_htmx_markup_gates.py`.
</artifacts_this_phase_produces>

<checkpoint_type_note>
⚠️ **О типах останова.** Задание называет измерение «checkpoint:manual». Такого типа в
исполнителе нет: канонических три — `checkpoint:human-verify`, `checkpoint:decision`,
`checkpoint:human-action`. Измерение оформлено **`checkpoint:human-action` с
`gate="blocking-human"`**, и это не формальность, а два следствия:

1. `workflow.human_verify_mode` в этом проекте — `end-of-phase`, то есть `human-verify`
   переносится в конец фазы и собирается пачкой. Здесь это сломало бы порядок: решение
   владельца (следующая задача) ПИТАЕТСЯ результатом измерения, и отложенное измерение оставило
   бы решение без входа.
2. Предметом задачи является не post-hoc подтверждение уже сделанного, а САМА величина, которую
   не добыть ни одним доступным проекту средством: браузерного стенда нет и не заводится
   (`E2E-01` отложен вехой), httpx не исполняет ни строчки JS. Это ровно определение
   `human-action`: действие, у которого нет ни CLI, ни API.

`gate="blocking-human"` на обоих останова́х обязателен: он не обходится ни в каком режиме,
включая автоматический. Останов, который автоматика одобрила бы сама, здесь обесценил бы фазу
целиком.
</checkpoint_type_note>

<tasks>

<task type="auto">
  <name>Задача 1: обход приведён к отгруженному состоянию и подготовлен к исполнению</name>

  <files>.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md</files>

  <read_first>
- `09-UAT.md` **целиком** (348 строк): раздел «Почему этот артефакт существует» с записанным
  правилом фазы, таблица «Что закрыто МАШИННО», раздел «Наблюдаемые признаки — сняты с
  ОТГРУЖЕННОГО состояния», пункты 3 (четыре проверки), 6 (две проверки), 5 (две проверки),
  таблица допущений A1/A2/A3/A5 и абзац про A4, раздел «Что в этот обход НЕ входит».
- Сводки `09-05-SUMMARY.md`, `09-06-SUMMARY.md`, `09-07-SUMMARY.md`, `09-08-SUMMARY.md` —
  ЧТО именно изменилось на экране после закрытия пробелов: стабильный идентификатор сентинела и
  четвёртый внеполосный узел, ветвление пустого состояния по текущей выдаче, единый источник
  линейки, самодостаточный класс индикатора, согласованный владелец блокировки кнопки
  подтверждения. Наблюдаемые признаки обхода обязаны быть пересняты с ЭТОГО состояния.
- `tests/test_pages/test_money_perimeter_gate.py:472` (`SYNC_ATTRIBUTE`), `:595` (разбор
  значения признака синхронизации), `:745-770`
  (`test_no_perimeter_route_carries_a_queued_request_sync`) — **это и есть формулировка запрета
  Фазы 8, вокруг которой стоит допущение A4.** Прочитать дословно: на какие маршруты правило
  распространяется (перечень периметра) и какой именно режим синхронизации оно запрещает.
- `app/templates/account_groups/includes/group_row.html:109-115` — вызов макроса-обёртки формы
  тумблера с передаваемой целью блокировки; `app/templates/components/toggle.html` — разметка
  чекбокса и его стабильный идентификатор.
- `09-VERIFICATION.md` §`behavior_unverified_items` и §Human Verification Required — четыре
  пункта, ради которых обход и проводится.
  </read_first>

  <reversibility rating="reversible">Правки документов и один новый документ.</reversibility>

  <action>
**1. Пересъём наблюдаемых признаков.** Раздел «Наблюдаемые признаки — сняты с ОТГРУЖЕННОГО
состояния» перечитать против дерева ПОСЛЕ планов 09-05…09-08 и поправить каждое расхождение.
Обход, написанный по намерению, называет человеку то, чего он может не увидеть, и первым же
несовпадением обесценивает себя целиком — этот абзац в артефакте уже есть, и он обязывает.
Как минимум добавить в перечень признаков: узел сентинела с его стабильным идентификатором и
текстом загрузки в конце списка; поведение индикатора, теперь не зависящее от выкладки формы.

**2. Новая проверка 3.5 — курсор прокрутки переживает удаление.** Ради неё и брался пилот, и
без неё обход не закрывает то, что чинил план 09-05. Шаги: аккаунт с числом групп БОЛЬШЕ
тридцати пяти; открыть экран, убедиться, что виден ровно первый десяток…тридцаток строк и
внизу узел подгрузки; **запомнить название последней видимой строки**; удалить строку из
ПЕРВОЙ трети списка через панель подтверждения; долистать до низа так, чтобы узел подгрузки
сработал; наблюдаемый признак — **ни одно название группы не пропало и ни одно не появилось
дважды**; счётчик над списком и число фактически отрисованных строк сходятся.
Записать рядом отдельным абзацем, что признак отказа здесь МОЛЧАЛИВ: статус 200, консоль
чистая, счётчик честен — расхождение видно только сверкой списка с самим собой.

**3. Новая проверка 5.3 — пустое состояние под активным поиском.** Ввести в поиск строку,
находящую РОВНО ОДНУ группу; удалить её; наблюдаемый признак — экран показывает «Группы не
найдены» с кнопкой «Сбросить», а не пустую карточку; нажатие «Сбросить» возвращает полный
список.

**4. Таблицы отметок дополнить колонками** под новые проверки (3.5 в таблице пункта 3, 5.3 в
таблице пункта 5) — так, чтобы каждая проверка имела СВОЮ отметку и слить их было нельзя. Это
уже действующее правило артефакта («проверок здесь четыре, а не одна, и сливать их нельзя»), и
оно распространяется на новые.

**5. A4 — прочитать и ЗАПИСАТЬ ДО решения.** В разделе допущений заменить абзац про A4 на
ПРОЧИТАННУЮ формулировку запрета Фазы 8: назвать файл, имя правила и дословно — на какие
маршруты оно распространяется и какой режим синхронизации запрещает; затем одной фразой сделать
вывод для варианта (б): подпадает маршрут тумблера под запрет или нет.
⚠️ Это ЧТЕНИЕ, а не применение. Вариант (б) остаётся невыбранным; задача — снабдить владельца
фактом вместо допущения.

**6. Данные и стенд — С ПОМЕЧЕННЫМ ИМЕНЕМ И С УБОРКОЙ.** В начало обхода добавить раздел «Как
привести стенд в нужное состояние»: точная команда запуска приложения (`just run`), адрес
экрана, и КОМАНДА посева данных — одной строкой через `uv run python -c ...` по существующим
моделям, без заведения файла в `scripts/` (новых поверхностей фаза не создаёт). Посев обязан
дать: один аккаунт с числом групп больше тридцати пяти, из которых часть выключена, и хотя бы
одна группа с названием, находимым уникальной строкой поиска.

⚠️ **Всё посеянное несёт ОБЩИЙ ПРЕФИКС ИМЕНИ `UAT9-`** — и аккаунт, и каждая группа
(`UAT9-account`, `UAT9-g001`… и одна `UAT9-needle` под уникальный поиск). Префикс здесь не
украшение: посев идёт в ЖИВУЮ локальную базу разработчика, где рядом лежат его собственные
данные, и без пометки убрать посеянное можно только глазами. Уникальная строка поиска для
проверок берётся из этого же префикса.

⚠️ **Тем же разделом — «Как убрать стенд»**: команда уборки одной строкой через
`uv run python -c ...`, снимающая РОВНО строки с префиксом `UAT9-` (группы аккаунта, затем сам
аккаунт), и рядом — команда проверки, что после уборки таких строк не осталось (счёт по тому же
префиксу даёт ноль). Уборка выполняется человеком сразу после обхода; это записано отдельной
последней строкой инструкции останова задачи 2 и проверяется машинно задачей 4.

⚠️ Проверить ОБЕ команды — посева и уборки — ИСПОЛНЕНИЕМ на локальной базе (посеять, прочитать
счёт, убрать, прочитать счёт снова) и записать в артефакт те команды, которые отработали, а не
те, которые должны были бы. В сводку выписать оба счёта.

**7. Пропуск матрицы покрытия внешнего API — записать, а не умолчать.** Прогнать
`node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage-verify-pre <каталог фазы>` и
выписать в сводку его вывод дословно. При `detected: false` файла матрицы не заводить: правило
способности при таком исходе требует пропустить проверку целиком. Пропуск, зафиксированный
выводом обхода, отличается от пропуска, о котором никто не спросил, — и разница обязана быть
видна в сводке.
  </action>

  <verify>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage-verify-pre .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy</automated>
    <automated>uv run pytest tests/test_pages/test_money_perimeter_gate.py -q -k "sync"</automated>
  </verify>

  <acceptance_criteria>
- В `09-UAT.md` существуют разделы проверок **3.5** и **5.3** со своими таблицами шагов и наблюдаемых признаков; таблицы отметок пунктов 3 и 5 содержат колонки под них.
- Абзац про допущение A4 содержит имя файла гейта, имя правила и дословную выдержку о том, на какие маршруты оно распространяется, — выписать в сводку.
- Раздел «Как привести стенд в нужное состояние» содержит команду посева, ОТРАБОТАВШУЮ на локальной базе; в сводке приведён её вывод (число созданных групп). Все посеянные имена начинаются с `UAT9-`.
- Раздел «Как убрать стенд» содержит команду уборки и команду проверки, обе ОТРАБОТАВШИЕ: после уборки счёт строк с префиксом `UAT9-` равен нулю. Оба счёта (после посева и после уборки) выписаны в сводку.
- Вывод `check api-coverage-verify-pre` по каталогу фазы выписан в сводку дословно и содержит `"block": false`; файла матрицы покрытия в каталоге фазы нет, и это соответствует исходу обхода.
- Таблицы отметок пунктов 3, 5 и 6 по-прежнему ПУСТЫ (их заполняет человек на следующей задаче), а `status` артефакта остаётся `pending` — заполнение исполнителем запрещено.
- `uv run pytest tests/ -q -p no:randomly -x` — вся суита зелена перед остановом (обход обязан идти по дереву, которое не сломано).
  </acceptance_criteria>

  <done>
Обход исполним: наблюдаемые признаки сняты с сегодняшнего дерева, новая поверхность внесена
отдельными проверками, стенд поднимается одной командой, данные сеются одной командой, а
допущение A4 прочитано и записано фактом.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Задача 2: ИЗМЕРЕНИЕ — ручной обход пунктов 3, 5 и 6, включая допущение A1</name>

  <precondition>Приложение запущено и отвечает на `/accounts` (`just run`, порт 8000), в базе
есть аккаунт с числом групп больше тридцати пяти, посеянный командой из раздела «Как привести
стенд в нужное состояние» артефакта `09-UAT.md`, и вся суита зелена
(`uv run pytest tests/ -q -p no:randomly`).</precondition>

  <action>Провести ручной обход `09-UAT.md` — пункты 3 (пять проверок), 5 (три проверки) и 6
(две проверки) — и заполнить три таблицы отметок наблюдённым признаком.</action>

  <instructions>
Всё, что можно было автоматизировать, автоматизировано: приложение поднято, данные посеяны,
суита зелена, артефакт обхода приведён к сегодняшнему состоянию экрана. Остаётся ровно то, чего
не даёт ни один доступный проекту инструмент: **эффект в живом браузере**. Браузерного стенда
в проекте нет и не заводится — Playwright отклонён на уровне вехи и отложен требованием
`E2E-01`; ~1700 тестов идут на in-memory базе через транспорт ASGI, **не исполняя ни строчки
JS**. httpx отдаёт ТЕКСТ ответа: он не свопает фрагмент в дерево документа и не собирает
внеполосные узлы.

Открыть `/accounts/{id}/groups` посеянного аккаунта и пройти по артефакту
`.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md`:

**Пункт 3 — своп, приземление счётчика, фокус, курсор.**
- **3.1** прокрутить ниже сгиба, переключить тумблер СРЕДНЕЙ ВИДИМОЙ строки мышью: строка
  перерисовалась на месте, позиция прокрутки не потеряна, соседние строки не мигнули; в
  DevTools → Elements узел строки в документе ОДИН.
- **3.2** линейка над списком: число изменилось; содержимое области заменено, сама область
  осталась той же. ⚠️ Если счётчик не обновился при ответе 200 и чистой консоли — первый
  подозреваемый НЕ обработчик, а вложенный внеполосный узел; записать наблюдение как есть.
- **3.3 — ИЗМЕРЕНИЕ, РАДИ КОТОРОГО ЭТОТ ОСТАНОВ СУЩЕСТВУЕТ. МЫШЬЮ НЕ ДЕЛАЕТСЯ.**
  Клавишей **Tab** довести фокус до чекбокса строки (видна рамка фокуса), нажать **Space**, и,
  **не трогая мышь**, в DevTools → Console выполнить `document.activeElement`.
  Записать ФАКТИЧЕСКИЙ ответ — «да» (фокус остался на том же тумблере) или «нет» — и рядом то,
  чем на самом деле оказался активный элемент.
  ⚠️ **Записанное ожидание сегодня — «нет», и оно обязано быть подтверждено или опровергнуто, а
  не подставлено.** Допущение A1 («браузер снимает фокус с элемента, которому выставлена
  блокировка») не измерялось ни разу: код рантайма прочитан, поведение браузера — нет. Если
  ответ «да» — конфликта D-06 ↔ QUAL-06 НЕ СУЩЕСТВУЕТ, и это надо записать так же явно, как
  записано ожидание «нет».
- **3.4** удалить группу с клавиатуры и посмотреть `document.activeElement`: фокус на теле
  документа — **это ожидаемо и принято**, записать наблюдение.
- **3.5** проверка курсора прокрутки — по разделу артефакта: удалить строку из первой трети,
  долистать до низа, сверить, что ни одно название не пропало и ни одно не появилось дважды.

**Пункт 6 — порог индикатора и защита от двойного нажатия.**
- **6.1** DevTools → Network → Throttling → **Slow 3G**, переключить тумблер: точка появляется
  и держится, пока идёт запрос. Снять троттлинг, переключить 5-6 раз подряд: точка **не мигает
  ни разу**. ⚠️ Если мигает при выключенном троттлинге — подозреваемых два и они разные: имя
  класса (рантайм дописывает собственный стиль в конец головы документа и при равной
  специфичности выигрывает) и место задержки (она обязана висеть на состоянии «запрос идёт»).
  Записать наблюдение, а не увеличивать порог первым движением: допущение **A3** («ответ
  тумблера укладывается быстрее 300 мс») замерами не подтверждено.
- **6.2** на Slow 3G быстро нажать тумблер **дважды**: в Network **один** запрос, а не два.
  Отдельно: открыть подтверждение удаления, нажать «Удалить» и, пока запрос идёт, попробовать
  нажать **«Отмена»** — она обязана остаться нажимаемой. Дополнительно к прежнему: дождаться
  ОТКАЗА (например, остановив сервер на время запроса) и убедиться, что кнопка подтверждения
  после отказа не осталась включённой с признаком занятости.

**Пункт 5 — граница Alpine после подмены.**
- **5.1** удалить группу через панель: узла панели удалённой группы в документе не осталось,
  поверх пустого места не висит невидимый диалог, Tab по странице в него не проваливается.
- **5.2** переключить тумблер строки: узел её панели РОВНО ОДИН; открыть подтверждение у
  соседней строки (Esc закрывает, Tab не уходит в невидимую копию) и у подменённой (то же
  самое, **и имя группы в панели верное** — допущение **A5**).
- **5.3** пустое состояние под активным поиском — по разделу артефакта.

**После обхода — УБРАТЬ СТЕНД.** Выполнить команду уборки из раздела «Как убрать стенд»
артефакта `09-UAT.md` и следом команду проверки: строк с префиксом `UAT9-` в базе не осталось.
Посев шёл в живую локальную базу рядом с настоящими данными, и тридцать пять групп, оставленных
после обхода, — мусор, который переживёт фазу и будет попадаться на глаза ещё долго.

**Куда пишется ответ.** В три таблицы отметок артефакта `09-UAT.md`: дата, браузер и ОС, и по
каждой проверке — **наблюдённый признак**, а не слово «ок». Отметка без наблюдённого признака
закрытием не считается: подпись под непроверенным Фаза 15 унаследует как факт (требование
GATE-09 — сводное закрытие всех девяти пунктов ручного UAT). Поле `status` артефакта поменять
на `passed`, если разошедшихся проверок нет, либо на `diagnosed` с перечнем расхождений.

**Расхождение на обходе — что делать.** Для 3.3 — записать ответ и передать владельцу три
способа снятия конфликта из артефакта, а не выбирать самому. Для 3.2 — искать вложенный
внеполосный узел, а не ошибку обработчика. Для 6.1 — проверять имя класса и место задержки, а
не увеличивать порог.
  </instructions>

  <verification>После возобновления исполнитель проверяет машинно: три таблицы отметок
непусты; в колонке 3.3 стоит «да» либо «нет»; ни в одной ячейке нет слова «ок» в качестве
единственного содержимого; `status` артефакта больше не `pending`; строк с префиксом `UAT9-` в
базе не осталось.</verification>

  <resume-signal>Заполнить таблицы в `09-UAT.md`, убрать стенд командой из раздела «Как убрать
стенд» и написать «обход проведён», указав ответ по 3.3 («да» / «нет»), перечислив расхождения,
если они есть, и приведя вывод команды проверки уборки.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Задача 3: РЕШЕНИЕ ВЛАДЕЛЬЦА по конфликту D-06 ↔ QUAL-06 — на записанном ответе, а не на ожидании</name>

  <decision>Что делать с конфликтом D-06 ↔ QUAL-06 на пути формы тумблера — по ФАКТИЧЕСКОМУ
ответу измерения 3.3, а не по ожиданию.</decision>

  <context>
Форма тумблера передаёт собственную цель блокировки — сам чекбокс, а не кнопку отправки.
Отступление объявлено образцово: запись перечня исключений с обоснованием, отдельно утверждаемое
число и сверка вызывающих. Но его объявленная ЦЕНА — отмена QUAL-06 на том же пути, и
формулировка критерия 3 роадмапа выделяет «только кнопка отправки» жирным.

**Решение зависит от того, что показало измерение, и потому принимается ЗДЕСЬ, а не раньше.**

**Если ответ 3.3 — «да» (фокус остался):** конфликта нет вовсе, допущение A1 ложно, и выбирать
нечего — надо записать факт. Выберите `no-conflict`.

**Если ответ 3.3 — «нет» (фокус потерян):** три варианта ниже. Вариант **(в)** — блокировать
обёртку метки вместо чекбокса — **исключён по существу**: блокировка на этом элементе не
действует, защиты не даёт вовсе, а фокус никуда не денется просто потому, что не произошло
ничего. Он назван ровно затем, чтобы его не пробовали как самый дешёвый, и в списке ниже
отсутствует намеренно.

**Обратимость.** D-06 — локированное решение с пометкой **costly**: правило раздаётся макросом
на сорок семь форм вехи, и переигрывание после Фазы 11 задело бы каждую. Поэтому выбор
принадлежит владельцу, а не исполнителю обхода и не верификатору. Варианты `option-a` и
`option-b` меняют поведение отгруженной формы; вариант `accept-deviation` не меняет ничего в
коде, но фиксирует, что QUAL-06 на пути тумблера остаётся невыполненным, и эта запись
наследуется Фазой 15 как факт.
  </context>

  <options>
    <option id="no-conflict">
      <name>Конфликта нет — записать закрытие A1 измерением</name>
      <pros>Ничего не меняется в коде; QUAL-06 на пути тумблера оказывается выполненным, и это подтверждено измерением, а не рассуждением; допущение A1 закрывается навсегда и не всплывает в Фазах 10-15</pros>
      <cons>Применим ТОЛЬКО если ответ 3.3 — «да»; при ответе «нет» этот вариант выбирать нельзя</cons>
    </option>
    <option id="option-a">
      <name>(а) Не блокировать чекбокс вовсе</name>
      <pros>QUAL-06 становится выполнимым немедленно и без новых атрибутов; вред QUAL-01 к ИНВЕРСИИ не относится — обработчик инвертирует состояние, а не устанавливает его, поэтому два переключения дают два инвертирования, второй записи не создаётся, и конечное состояние сходится с показанным браузеру; серверная идемпотентность (одна строка под тройным ограничением) уже отгружена и заперта</pros>
      <cons>На медленной сети человек успевает нажать дважды и увидеть промежуточное состояние; запись исключения в перечне закрывается, и объявленное число исключений уменьшается — движение обязано быть записано летописью</cons>
    </option>
    <option id="option-b">
      <name>(б) Отбрасывающая форма синхронизации на форме тумблера вместо блокировки</name>
      <pros>Второй запрос отбрасывается клиентом, чекбокс фокуса не теряет — выполняются и QUAL-01, и QUAL-06 разом</pros>
      <cons>Живость варианта зависит от формулировки запрета Фазы 8 (допущение A4): она ПРОЧИТАНА и записана задачей 1 этого плана — прочтите вывод в `09-UAT.md` перед выбором; если запрет оказался шире, вариант мёртв. Кроме того, это новый атрибут на форме, раздаваемой макросом, — то есть решение, которое Фазы 10-15 унаследуют</cons>
    </option>
    <option id="accept-deviation">
      <name>Принять девиацию записью overrides</name>
      <pros>Код не трогается вовсе; отступление остаётся объявленным, обоснованным и посчитанным; решение можно отложить до Фазы 11, где тумблеры переводятся массово</pros>
      <cons>QUAL-06 на пути тумблера остаётся НЕВЫПОЛНЕННЫМ, и это наследуется Фазой 15 как факт; критерий 3 роадмапа остаётся неисполненным по букве, и запись об этом обязана стоять рядом с отметкой требования</cons>
    </option>
  </options>

  <resume-signal>Ответить одним из: `no-conflict`, `option-a`, `option-b`, `accept-deviation`.
Если выбран `option-b`, подтвердить, что вывод по допущению A4 прочитан.</resume-signal>
</task>

<task type="auto">
  <name>Задача 4: исход применён, бухгалтерия сведена в одну сторону</name>

  <files>.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md, .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-04-SUMMARY.md, .planning/REQUIREMENTS.md, app/templates/account_groups/includes/group_row.html, tests/test_templates/test_htmx_markup_gates.py</files>

  <read_first>
- `09-UAT.md` после обхода — заполненные таблицы отметок и фактический ответ 3.3.
- Ответ владельца на останов решения.
- `09-VERIFICATION.md` §frontmatter (`gaps`, `overrides_applied: 0`,
  `behavior_unverified_items`, `coincidental_reliance_items`, `human_verification`) — форма, в
  которую встаёт запись исхода; §Warnings **WARN-8** — «правым является REQUIREMENTS.md;
  сводку не следует сверять в обратную сторону».
- `.planning/REQUIREMENTS.md:27,34,39,40,44` (тексты требований) и `:131,138,140,141,145`
  (строки таблицы состояний) — сегодня FORM-02 / FORM-09 / QUAL-01 в состоянии `Gaps Found`,
  QUAL-02 / QUAL-06 — `Pending`. Преждевременные отметки уже откачены коммитом `183511c`.
  ⚠️ **Читать затем, чтобы знать состояния, а НЕ затем, чтобы их менять:** ни одна из этих
  строк и ни один флажок в теле файла этим планом не правятся (пункт 4 действия). План
  дописывает раздел свидетельств НИЖЕ таблицы и, при нужном исходе, долгоживущую запись
  overrides — и ничего больше.
- `git log -1 183511c --stat` — что именно откатывал этот коммит и почему запрет пункта 4
  сформулирован запретом, а не осторожностью.
- `09-04-SUMMARY.md` §frontmatter — поле `requirements-completed`, объявляющее QUAL-02 и
  QUAL-06 выполненными.
- `tests/test_templates/test_htmx_markup_gates.py:2004-2050` —
  `DISABLED_ELT_EXCEPTIONS` (запись про форму без кнопки отправки с названной ценой и тремя
  способами снятия) и `DISABLED_ELT_EXCEPTIONS_DECLARED = 2` с летописью.
- `app/templates/account_groups/includes/group_row.html:109-115` — вызов с передаваемой целью
  блокировки: точка правки для вариантов (а) и (б).
  </read_first>

  <reversibility rating="costly">
Отметки требований наследуются Фазой 15 как факт (требование GATE-09 — сводное закрытие), и
там их уже никто перепроверять не станет. Именно поэтому ни одна отметка не ставится сверх
того, что заработано измерением.
  </reversibility>

  <action>
**1. Применить исход останова решения — РОВНО ОДНУ ветку.**

- **`no-conflict`:** кода не касаться. В `09-UAT.md` рядом с проверкой 3.3 записать, что
  допущение A1 закрыто ИЗМЕРЕНИЕМ и оказалось ложным, с указанием браузера и ОС. Запись о
  закрытии второго пробела измерением положить по правилу пункта 2 (оригинал в
  `.planning/REQUIREMENTS.md`, помеченная копия в `09-VERIFICATION.md`); `overrides` при этом
  исходе НЕ заводится — девиации принимать нечего, конфликта не было. В записи
  `DISABLED_ELT_EXCEPTIONS` абзац «цена названа
  словами» переписать: цена измерена и не подтвердилась, отступление остаётся, а QUAL-06 на
  этом пути выполняется. Число объявленных исключений НЕ меняется.
- **`option-a`:** в вызове макроса-обёртки формы тумблера снять передачу собственной цели
  блокировки — форма переходит на умолчание макроса. Запись
  `DISABLED_ELT_EXCEPTIONS` для места определения макроса-обёртки закрывается, число
  объявленных исключений уменьшается на единицу, и это движение записывается СЛЕДУЮЩЕЙ строкой
  летописи с причиной («конфликт снят решением владельца по итогам измерения 3.3»), а не
  обнаруживается через фазу. В `group_row.html` записать одной фразой, почему блокировки больше
  нет и чем защищено двойное нажатие (инверсия, а не установка; одна строка под тройным
  ограничением). Прогнать всю суиту.
- **`option-b`:** добавить признак отбрасывающей синхронизации на форму тумблера **в вызове**,
  а не в макросе (иначе решение раздалось бы сорока семи формам, которых владелец не выбирал),
  и снять передачу собственной цели блокировки, если она перестала быть нужной. Обязательное
  условие: перед правкой сослаться в тексте на прочитанную формулировку запрета Фазы 8 и на
  вывод по A4. Записи и числа перечня привести в соответствие с летописью.
- **`accept-deviation`:** кода не касаться. Завести запись overrides — по правилу пункта 2.

**2. ЗАПИСЬ ИСХОДА ЛОЖИТСЯ В ДОЛГОЖИВУЩЕЕ МЕСТО, а не только туда, что перегенерируется.**

`09-VERIFICATION.md` — артефакт ВЕРИФИКАЦИИ: следующий прогон проверки перепишет его целиком.
Запись, несущая весь исход столкновения D-06 ↔ QUAL-06, положенная только туда, исчезнет вместе
с перегенерацией, и Фаза 15 унаследует пустоту вместо решения владельца. Поэтому:

- **Оригинал** — в `.planning/REQUIREMENTS.md`, непосредственно под строкой QUAL-06 раздела
  свидетельств (пункт 3): что именно принято (девиация от буквы критерия 3 роадмапа на пилотной
  форме), на каком основании, фактический ответ измерения 3.3 с браузером и ОС, и явно
  зафиксированная цена — QUAL-06 на пути тумблера остаётся невыполненным. При исходах
  `no-conflict` / `option-a` / `option-b` в том же месте лежит соответствующая запись: допущение
  A1 закрыто измерением (с ответом), либо конфликт снят выбранным вариантом.
- **Копия** — в `09-VERIFICATION.md` в форме файла (`overrides:` и счётчик `overrides_applied`,
  поднятый с нуля, при `accept-deviation`; запись закрытия A1 измерением — при остальных
  исходах). Копия обязана нести ОДНУ ЯВНУЮ ФРАЗУ: где лежит оригинал и что повторная верификация
  обязана эту запись сохранить или перенести из оригинала, а не потерять при перегенерации.
- Оригинал и копия обязаны совпадать по существу; расхождение между ними разрешается в пользу
  `.planning/REQUIREMENTS.md` — тем же правилом, которым разрешается WARN-8.

**3. `09-VERIFICATION.md` — привести к факту.** В `behavior_unverified_items` каждый пункт,
закрытый обходом, пометить закрытым с указанием, ЧЕМ он закрыт (дата обхода, браузер,
наблюдённый признак). Пункты, оставшиеся незакрытыми, оставить незакрытыми — обход, не давший
ответа, не превращается в ответ. `human_verification` привести в соответствие: закрытые пункты
снять, оставшиеся оставить с причиной.

**4. `.planning/REQUIREMENTS.md` — СВИДЕТЕЛЬСТВА, А НЕ ОТМЕТКИ. Столбец `Status` этот план не
трогает вовсе, и это ГЕЙТ, а не осторожность.**

Преждевременная отметка «выполнено» — ровно та ошибка, которая породила третий пробел этой фазы
и потребовала отката коммитом `183511c`. Суждение исполнителя («сомнение решается в пользу
незакрытия») её один раз уже не остановило, поэтому здесь стоит не суждение, а запрет: **ни одна
строка таблицы состояний и ни один флажок в теле файла этим планом не меняются.** Перевод
состояний — работа шага повторной верификации, который побежит ПОСЛЕ этого плана и увидит и
зелёную суиту, и заполненные таблицы отметок, и раздел свидетельств ниже.

Вместо отметок план дописывает в `.planning/REQUIREMENTS.md` раздел
`### Фаза 9 — свидетельства к пересмотру состояний (заполнено планом 09-09)` — по одному
СПИСОЧНОМУ пункту на каждое из пяти требований фазы, из четырёх полей:

⚠️ **Раздел набирается списком, а НЕ таблицей, и это машинное требование, а не вкус.** Проверка
неприкосновенности состояний (критерий приёмки ниже) сличает строки ТАБЛИЦЫ состояний до и после
правки; новые табличные строки с теми же идентификаторами требований попали бы в ту же выборку и
покрасили бы собственный гейт плана. Списочная форма делает это невыразимым.

Поля пункта:
- **требование** — FORM-02 / FORM-09 / QUAL-01 / QUAL-02 / QUAL-06;
- **машинная проверка** — ОДНА конкретная команда с ожидаемым исходом (имя теста или прогон
  файла гейтов), а не «суита зелена»;
- **наблюдённый признак** — номер проверки обхода и ЧТО именно было увидено, дословно из
  таблицы отметок; там, где наблюдения не требуется, — прочерк с одной фразой почему;
- **вердикт** — `заработано` либо `не заработано`. Вердикт `заработано` ставится ТОЛЬКО когда
  обе предыдущие клетки заполнены фактом; пустая или частично заполненная строка даёт
  `не заработано` автоматически, и выбора здесь у исполнителя нет.

Соответствия проверок требованиям — они и есть гейт, менять их нельзя:
- **FORM-02** — `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly`
  зелено, включая `test_the_scroll_cursor_survives_a_fragment_delete` и
  `test_a_no_op_delete_does_not_double_a_row`; наблюдение — проверка **3.5**.
- **FORM-09** — `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q` зелено,
  включая правило самодостаточности класса индикатора (план 09-08) и правило единого источника
  линейки (план 09-07); наблюдение — прочерк: единственность источника глазами не наблюдается.
- **QUAL-01** — `uv run pytest tests/test_pages/test_account_groups.py -q -k "repeated_delete"`
  зелено; наблюдение — проверка **6.2**; плюс условие: исход останова решения либо снял
  столкновение с буквой критерия 3, либо принял девиацию записью пункта 2.
- **QUAL-02** — `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "indicator"`
  зелено; наблюдение — проверка **6.1** И подтверждённое ею допущение A3.
- **QUAL-06** — наблюдение **3.3** с фактическим «да»/«нет» И исход останова решения
  `no-conflict`, `option-a` или `option-b`; при `accept-deviation` вердикт — `не заработано`, со
  ссылкой на долгоживущую запись overrides пункта 2.

**5. WARN-8 — сводка приводится к требованиям, а не наоборот.** В `09-04-SUMMARY.md` поле
`requirements-completed` привести в соответствие с `.planning/REQUIREMENTS.md` и рядом, в теле
сводки, добавить ПОМЕТКУ об исправлении: что было заявлено, что оказалось верным, и почему
правым признан файл требований (QUAL-06 на пути тумблера прямо не выполнялся). Молча
переписывать поле нельзя: сводка — исторический документ, и след правки в ней обязателен.

**6. `.planning/STATE.md` и `ROADMAP.md` этим планом НЕ правятся** — это делает оркестратор
после верификации. Состояния требований — тоже не здесь (пункт 4).

**7. Уборка стенда проверяется машинно.** Прогнать команду проверки из раздела «Как убрать
стенд» артефакта `09-UAT.md`: строк с префиксом `UAT9-` в базе не осталось. Вывод выписать в
сводку. Если остались — убрать и прогнать ещё раз; посев не имеет права пережить фазу.

**8. Прогон** всей суиты и проверка отсутствия долговых маркеров в тронутых файлах.
  </action>

  <verify>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
    <automated>bash -c "diff <(git show HEAD:.planning/REQUIREMENTS.md | grep -E '^\| [A-Z]+-[0-9]+ \| Phase ') <(grep -E '^\| [A-Z]+-[0-9]+ \| Phase ' .planning/REQUIREMENTS.md)"</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/ -q -p no:randomly` — зелено целиком.
- **Состояния требований не тронуты, и это проверяется сличением, а не доверием:** `diff <(git show HEAD:.planning/REQUIREMENTS.md | grep -E '^\| [A-Z]+-[0-9]+ \| Phase ') <(grep -E '^\| [A-Z]+-[0-9]+ \| Phase ' .planning/REQUIREMENTS.md)` → пусто, и `diff <(git show HEAD:.planning/REQUIREMENTS.md | grep -E '^- \[[ x]\] ') <(grep -E '^- \[[ x]\] ' .planning/REQUIREMENTS.md)` → пусто. Оба вывода выписать в сводку. Выборка привязана к ФОРМЕ строки таблицы состояний (второй столбец — фаза), поэтому дописанный ниже раздел свидетельств в неё не попадает — при условии, что он набран списком, а не таблицей.
- В `.planning/REQUIREMENTS.md` существует раздел `### Фаза 9 — свидетельства к пересмотру состояний (заполнено планом 09-09)`, набранный СПИСКОМ, и в нём РОВНО пять пунктов — по одному на FORM-02, FORM-09, QUAL-01, QUAL-02, QUAL-06; у каждого заполнены все четыре поля, и вердикт — `заработано` либо `не заработано`. Раздел выписать в сводку целиком.
- Запись исхода существует В ДВУХ местах: оригинал в `.planning/REQUIREMENTS.md` рядом со строкой QUAL-06 и помеченная копия в `09-VERIFICATION.md`, причём копия несёт фразу о том, где оригинал и что повторная верификация обязана запись сохранить. Обе выписать в сводку дословно и сверить их между собой.
- В `09-VERIFICATION.md` присутствует РОВНО ОДНА из двух форм: `overrides:` с непустым основанием и `overrides_applied`, поднятым с нуля (при `accept-deviation`), либо запись закрытия допущения A1 измерением (при остальных исходах).
- Строк с префиксом `UAT9-` в базе не осталось: вывод команды проверки уборки выписан в сводку и равен нулю.
- `09-04-SUMMARY.md` содержит пометку об исправлении поля `requirements-completed`, называющую прежнее и новое значение; `grep -c "requirements-completed" .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-04-SUMMARY.md` → 1.
- При исходах `option-a` / `option-b`: `DISABLED_ELT_EXCEPTIONS_DECLARED` изменено и несёт следующую строку летописи с подстрокой `план 09-09`; `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q` — зелено.
- При исходе `no-conflict` / `accept-deviation`: `git diff --stat app/ tests/` пуст — кода правка не касалась.
- `grep -rnE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER" .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md` → пусто.
  </acceptance_criteria>

  <done>
Исход измерения и решения применён ровно одной веткой и записан туда, где переживёт
перегенерацию; `09-VERIFICATION.md`, `.planning/REQUIREMENTS.md` и сводка плана 09-04 говорят
одно и то же, и это одно — `.planning/REQUIREMENTS.md`. Состояний требований план не переводил:
он оставил свидетельства и вердикты, а перевод отдал повторной верификации — тому шагу, который
единственный видит и суиту, и заполненные отметки.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| человек на приёмке → артефакт обхода | отметка без наблюдённого признака наследуется Фазой 15 как факт |
| решение владельца → разметка формы тумблера | выбор варианта (а)/(б) меняет поведение отгруженной формы и наследуется макросом |
| посевная команда → локальная база | обход требует данных, которых в базе разработчика может не быть |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-09-01 | Repudiation | отметка обхода без наблюдённого признака | high | mitigate | правило артефакта записано и повторено в инструкции останова; исполнитель после возобновления проверяет непустоту таблиц машинно и отсутствие слова «ок» в качестве единственного содержимого ячейки |
| T-09-09-02 | Repudiation | отметка требования сверх заработанного | high | mitigate | план не переводит состояний вовсе — вместо суждения стоит запрет, проверяемый диффом (`git diff -U0 .planning/REQUIREMENTS.md` не содержит изменённых строк таблицы состояний и переставленных флажков). Вместо отметок остаётся раздел свидетельств: на каждое требование ОДНА машинная проверка, наблюдённый признак из таблицы отметок и вердикт, где неполная строка автоматически даёт `не заработано`. Перевод делает повторная верификация — тот шаг, который видит и суиту, и заполненные отметки |
| T-09-09-03 | Tampering | вариант (б) применён без чтения запрета Фазы 8 | medium | mitigate | формулировка запрета читается и записывается ЗАДАЧЕЙ 1, то есть ДО останова решения; выбор варианта (б) требует подтверждения, что вывод по допущению A4 прочитан |
| T-09-09-04 | Elevation of privilege | вариант (а) снимает клиентскую блокировку | low | accept | блокировка объявлена клиентским удобством, а не серверной защитой; идемпотентность обеспечивается инверсией и одной строкой под тройным ограничением, и это отгружено и закреплено тестами |
| T-09-09-05 | Information disclosure | посевные данные на локальной базе | low | mitigate | команда посева выполняется разработчиком на своей базе; данные синтетические, реальных величин не содержат. Всё посеянное несёт префикс имени `UAT9-`, раздел «Как убрать стенд» даёт команду уборки ровно по этому префиксу, уборка — последний шаг инструкции останова, а её результат проверяется машинно задачей 4: строк с префиксом не осталось |
| T-09-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | установок пакетов план не производит; браузерный стенд не заводится — это прямая прохибиция |
</threat_model>

<verification>
1. `uv run pytest tests/ -q -p no:randomly` — зелено.
2. Три таблицы отметок `09-UAT.md` непусты, `status` артефакта не `pending`.
3. В колонке 3.3 стоит «да» или «нет» с указанием фактического активного элемента.
4. `09-VERIFICATION.md` несёт ровно одну запись исхода; `overrides_applied` соответствует ей;
   запись помечена копией и называет место оригинала.
5. `.planning/REQUIREMENTS.md` несёт раздел свидетельств из пяти строк, а столбец `Status` и
   флажки в теле файла не изменены ни в одной строке (проверяется диффом); `09-04-SUMMARY.md`
   приведён к `.planning/REQUIREMENTS.md`, а не наоборот.
7. Стенд убран: строк с префиксом `UAT9-` в базе не осталось.
6. `node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage-verify-pre <каталог фазы>` — `block: false`; пропуск матрицы записан в сводке.
</verification>

<success_criteria>
- Допущение A1 измерено, а не предположено; ответ записан фактом.
- Решение по D-06 ↔ QUAL-06 принято владельцем ПОСЛЕ измерения и записано.
- Таблицы отметок пунктов 3, 5 и 6 заполнены наблюдённым признаком.
- Ни одно состояние требования не переведено этим планом вовсе: план оставил свидетельства с
  машинными проверками и вердиктами, а перевод отдал повторной верификации.
- Запись исхода лежит в долгоживущем месте и переживёт перегенерацию артефакта верификации;
  бухгалтерия сведена к `.planning/REQUIREMENTS.md`.
- Посевной стенд убран по префиксу `UAT9-`; локальная база разработчика вернулась к своему
  состоянию.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-09-SUMMARY.md` when done
</output>
