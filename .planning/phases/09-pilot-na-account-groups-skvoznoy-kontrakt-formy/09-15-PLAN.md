---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 15
type: execute
wave: 3
depends_on: ["13", "14"]
gap_closure: true
files_modified:
  - app/pages/account_groups.py
  - app/templates/account_groups/includes/group_row.html
  - tests/test_pages/test_account_groups.py
  - tests/test_pages/test_htmx_gates.py
autonomous: true
requirements: [FORM-02, QUAL-01, QUAL-06]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Тумблер приводит группу в СОСТОЯНИЕ, ПОКАЗАННОЕ ЧЕЛОВЕКУ, а не инвертирует вслепую: присланное значение чекбокса читается и применяется. В мире «Alpine жив, htmx мёртв», заведённом планом 09-07, кнопка «Применить» выживает, автоотправки нет, и чётное число переключений с последующим нажатием давало СОСТОЯНИЕ, ОБРАТНОЕ показанному (WR-02)"
    - "Тихая инверсия — не косметика: по D-05 тумблер есть ЕДИНСТВЕННЫЙ способ исключить группу из рассылки без удаления, поэтому обратное состояние означает сообщения в чат, который человек считает выключенным"
    - "Правка ИДЕМПОТЕНТНА и QUAL-01 не ослабляет: присвоение присланного значения дважды даёт то же состояние, что и один раз, — защита от второго нажатия не зависит от инверсии"
    - "Путь деградации не двинулся: запрос без признака htmx по-прежнему отвечает 302 на экран групп с сохранённым фильтром, и обе пары `*_degrades_without_htmx` зелены"
    - "Обе формы пути удаления шлют ОДИН И ТОТ ЖЕ набор полей: форма-триггер несёт скрытое поле строки поиска так же, как его несёт форма панели подтверждения. Иначе путь без Alpine приземляется на НЕотфильтрованный список, а ветка «выдача опустела» решается по всему аккаунту вместо той выдачи, что человек видел (WR-03)"
    - "Равенство наборов полей утверждается ПРАВИЛОМ, а не совпадением разметки: разъехавшиеся формы краснеют"
    - "Маршрут порции не отвечает целым документом ни в одной ветке: истёкшая сессия и чужой аккаунт закрываются слоем ответа, а не собственным перенаправлением, — иначе XHR прозрачно следует за 302, и htmx вклеивает целую страницу логина в середину списка со вторым `<form action=\"/login\">` (WR-04)"
    - "Соседний маршрут уже отвергает ровно это прямым текстом (`account_groups_sync_status`); фрагментному маршруту достаётся то же правило, а не другое"
    - "Число непереведённых обработчиков `NOT_YET_CONVERTED_COUNT` двигается ЗАПИСЬЮ ЛЕТОПИСИ с номером плана, а не подгоняется молча; оба его правила зелены"
    - "Два запроса подсчёта не исполняются на ветке, которая их выбрасывает: обработчик удаления следует собственному правилу-близнецу, записанному у тумблера (IN-03)"
    - "QUAL-01, ребро `idempotency`: повторный запрос переключения не создаёт второй записи и приводит группу в то же состояние — присвоение присланного значения идемпотентно по построению, в отличие от инверсии"
    - statement: "Фокус после подмены строки тумблера остаётся на элементе с тем же `id`: разметочная половина QUAL-06 не двигается правкой обработчика, а поведенческая половина уже закрыта наблюдением 3.3-Б круга 2"
      verification: backstop
  artifacts:
    - "app/pages/account_groups.py — обработчик тумблера, читающий присланное состояние; маршрут порции через слой ответа; два запроса подсчёта внутри отложенной сборки фрагмента"
    - "app/templates/account_groups/includes/group_row.html — форма-триггер удаления со скрытым полем строки поиска"
    - "tests/test_pages/test_account_groups.py — правило равенства наборов полей двух форм удаления, поведенческие правила состояния тумблера и правило «маршрут порции не отвечает документом»"
    - "tests/test_pages/test_htmx_gates.py — сдвинутое `NOT_YET_CONVERTED_COUNT` с записью летописи"
  key_links:
    - "показанное состояние чекбокса → состояние группы в базе: рвётся — рассылка идёт в чат, который человек выключил, и признака отказа нет ни одного"
    - "форма-триггер (путь без Alpine) → адрес приземления и ветка «выдача опустела»: рвётся — человек остаётся перед пустой карточкой отфильтрованной выдачи, из которой нет выхода, а починка WR-06 на базовом пути не срабатывает"
    - "сентинел → маршрут порции при истёкшей сессии: рвётся — в середину списка приезжает целая страница логина со вторым набором идентификаторов, и уникальность `id` документа ломается тем же способом, что и в CR-01"
  prohibitions:
    - statement: "серверная идемпотентность и защита от двойного нажатия не ослабляются: `hx-sync='this:drop'` в вызове макроса-обёртки остаётся, тройной WHERE обоих обработчиков не трогается"
      status: flagged-unverified
      verification: none
    - statement: "форма-триггер не оборачивается макросом-обёрткой и признака htmx-отправки не получает: решение D-03 не пересматривается, правка добавляет ровно одно скрытое поле"
      status: flagged-unverified
      verification: none
    - statement: "маршрут порции не получает собственного RedirectResponse ни в одной ветке после перевода: два решения об одной форме ответа означают, что путь деградации снова перестаёт быть обязательным (гейт G-2)"
      status: flagged-unverified
      verification: none
    - statement: "число NOT_YET_CONVERTED_COUNT не подгоняется под новое дерево молча: движение получает строку летописи с номером плана и причиной"
      status: flagged-unverified
      verification: none
    - statement: "тест, чьё ожидание изменилось из-за смены семантики тумблера, не переписывается молча: причина называется в сводке, и число таких тестов записывается числом"
      status: flagged-unverified
      verification: none
    - statement: "DEF-09-02 («Отмена» не отменяет летящий запрос) не чинится: рычаг — components/modal.html, принадлежащий Фазе 10"
      status: flagged-unverified
      verification: none
    - statement: "DEF-09-01 (тест обзора краснеет от часов) не чинится: предсуществующий дефект вне фазы, доказанный измерением"
      status: flagged-unverified
      verification: none
    - statement: "планы 09-01 … 09-14, их сводки и 09-VERIFICATION.md не правятся, не перенумеровываются и не отменяются"
      status: flagged-unverified
      verification: none
    - statement: "состояния требований и флажки .planning/REQUIREMENTS.md этим планом не переводятся вовсе"
      status: flagged-unverified
      verification: none
    - statement: "долговых маркеров в артефактах не появляется"
      status: flagged-unverified
      verification: none
---

<objective>
Закрыть четыре ещё открытых замечания ревизии `09-REVIEW.md`, каждое из которых — настоящий изъян
пилотной поверхности, и ни одно из которых не должно достаться Фазам 10-11 молча.

Purpose: отчёт верификации назвал WR-02 … WR-05 и IN-01 … IN-03 «настоящими замечаниями, ни одно
из которых не опровергает объявленных критериев», и перечислил их именно затем, «чтобы Фазы 10-11
не унаследовали их молча». Унаследовать молча — это и значит оставить их без плана. Три из них
имеют названного пострадавшего:

- **WR-02** — обработчик тумблера игнорирует присланное значение и инвертирует вслепую. Пока
  Alpine снимал кнопку «Применить» безусловно, одно изменение равнялось одной инверсии. План 09-07
  сделал снятие условным (`x-init="if (window.htmx) $el.remove()"`) и перенёс автоотправку на
  `hx-trigger="change"` — и завёл мир «Alpine жив, htmx мёртв», в котором кнопка выживает, а
  автоотправки нет. Чётное число переключений плюс нажатие даёт состояние, ОБРАТНОЕ показанному. По
  D-05 тумблер — единственный способ исключить группу из рассылки без удаления.
- **WR-03** — форма-триггер удаления не несёт скрытого поля строки поиска, которое несёт форма
  панели. Путь без Alpine приземляется на НЕотфильтрованный список, и ветка «выдача опустела» —
  весь смысл починки WR-06 — на отфильтрованной выдаче базового пути сработать не может.
- **WR-04** — маршрут порции отвечает `302` на `/login` при истёкшей сессии. XHR следует за
  редиректом прозрачно, htmx получает `200` и ЦЕЛЫЙ документ логина и вклеивает его в середину
  списка. Соседний маршрут (`account_groups_sync_status`) отвергает ровно это прямым текстом;
  фрагментному того же не досталось.
- **IN-03** — два запроса подсчёта исполняются и выбрасываются на ветке перехода. Обработчик
  тумблера откладывает ту же работу внутрь `_fragment` ровно по этой причине и говорит об этом в
  докстринге; обработчик удаления собственному правилу-близнецу не следует.

Output: три правки приложения, каждая с машинным правилом, которое покраснело бы на её отсутствии;
сдвинутое с летописью `NOT_YET_CONVERTED_COUNT`.

⚠️ **Что этот план НЕ трогает и почему.** WR-01 закрыт планом 09-14, WR-05 и IN-01 закрыты планом
09-13, CR-01 закрыт планом 09-13 — двойного планирования нет. IN-02 закрыт правкой прозы в задаче 2
плана 09-14. **DEF-09-02** (Отмена не отменяет летящий запрос) отложен в Фазу 10 по принадлежности
рычага, **остаток DIV-09-01** — в Фазу 15 по принадлежности слепой зоны, **DEF-09-01** — вне фазы,
доказан измерением. Ни один из трёх здесь не переплановывается.

⚠️ **Волна 3, `depends_on: ["13","14"]`.** Все три правки лежат в файлах, которые двигают планы
09-13 и 09-14; исполнять их параллельно значило бы править дерево, которого нет.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-PATTERNS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-REVIEW.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-06-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-07-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-13-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-14-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Имена, которые этот план ЗАВОДИТ (в дереве их сегодня нет):

**`app/pages/account_groups.py`:**

- параметр обработчика тумблера `is_active: str | None = Form(None)` — присланное значение
  чекбокса;
- ветка отказа маршрута `account_groups_partial` через слой ответа (`respond`) вместо собственного
  `RedirectResponse` — и, как следствие, вхождение этого маршрута в множество ПЕРЕВЕДЁННЫХ.

**`tests/test_pages/test_account_groups.py`:**

- `test_the_toggle_honours_the_posted_state` — присланное состояние применяется;
- `test_two_toggles_and_a_submit_land_on_what_the_control_shows` — мир «Alpine жив, htmx мёртв»:
  чётное число переключений плюс отправка даёт ПОКАЗАННОЕ состояние;
- `test_the_toggle_is_idempotent_on_a_repeated_post` — повтор того же тела даёт то же состояние;
- `test_both_delete_forms_post_the_same_field_names` — равенство наборов имён полей двух форм;
- `DELETE_FORM_FIELD_NAMES_MEASURED` — антивакуумное число полей;
- `test_the_portion_route_never_answers_with_a_whole_document` — маршрут порции при истёкшей сессии
  и при чужом аккаунте.

**`tests/test_pages/test_htmx_gates.py`:** движение `NOT_YET_CONVERTED_COUNT` с новой строкой
летописи (значение ставится ИЗМЕРЕНИЕМ, а не арифметикой этого плана).

**`app/templates/account_groups/includes/group_row.html`:** скрытое поле строки поиска внутри
формы-триггера удаления.

⚠️ **Матрица покрытия внешнего API не заводится:** детерминированный обход области фазы вернул
`detected: false`. ⚠️ **Останов допущений архитектуры не сработал** (`detected: false`). ⚠️ **Гейт
схемы БД молчит:** ORM-моделей и ревизий Alembic план не трогает — `Group.is_active` уже
существует, тип и умолчание не меняются. ⚠️ **Пакетов план не устанавливает.**
</artifacts_this_phase_produces>

<flagged_assumptions>
## Допущения, оставленные ОТКРЫТЫМИ

Ребро **QUAL-01 / `idempotency`** закрывается здесь для пути ТУМБЛЕРА (`verification: explicit`):
присвоение присланного значения идемпотентно по построению, и это утверждается
`test_the_toggle_is_idempotent_on_a_repeated_post`.

Остаются **`unresolved`** и названы здесь, а не отброшены молча:

- **FORM-02 / `unclassified`** — обход категории не назначил. План двигает ОБА пути деградации
  (форма-триггер и маршрут порции), поэтому допущение «деградация не ухудшилась» проверяется
  прогоном обеих пар `*_degrades_without_htmx` и новым правилом маршрута порции, а не рассуждением.
- **QUAL-06 / `unclassified`** — фокус планом не трогается; допущение проверяется прогоном
  `test_toggle_fragment_keeps_the_toggle_id` и остаётся `verification: backstop` в части
  поведения.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: тумблер приводит группу в ПОКАЗАННОЕ состояние, а не в обратное (WR-02)</name>

  <files>app/pages/account_groups.py, tests/test_pages/test_account_groups.py</files>

  <read_first>
- `app/pages/account_groups.py`, обработчик `account_groups_toggle` целиком: сигнатура (около
  370-380), докстринг с абзацем о запрете собственного перенаправления, тройной `WHERE`, строка
  инверсии `group.is_active = not group.is_active` (около 432) и абзац комментария над ней —
  **особенно его вторая половина**: «⚠️ ОНА ЖЕ ДЕЛАЕТ ПОВТОРНОЕ НАЖАТИЕ БЕЗВРЕДНЫМ (QUAL-01)».
  Именно это утверждение правка обязана СОХРАНИТЬ, а не потерять.
- `app/templates/account_groups/includes/group_row.html` строки 130-200 — блочный вызов
  макроса-обёртки формы тумблера: `hx-trigger="change"`, `sync='this:drop'`, пустая цель
  блокировки, слот из макроса тумблера и `<span x-data x-init="if (window.htmx) $el.remove()">` с
  кнопкой внутри. **Это и есть мир «Alpine жив, htmx мёртв»**, названный ревизией.
- `app/templates/components/toggle.html` — убедиться ИСПОЛНЕНИЕМ, а не памятью, каким именно
  именем и значением чекбокс уходит в теле формы.
- `tests/test_pages/test_account_groups.py`:
  - `test_toggle_degrades_without_htmx` (около 473) — он шлёт запрос БЕЗ ТЕЛА и ожидает, что группа
    станет неактивной. **Это ожидание правкой не меняется**: пустое тело означает «чекбокс снят»,
    то есть неактивна, — но убедиться в этом обязан прогон, а не рассуждение;
  - `test_toggle_returns_the_row_fragment` (около 501) и все прочие тесты, зовущие маршрут
    переключения: найти их греп-обходом по адресу маршрута и прочитать КАЖДЫЙ, прежде чем менять
    семантику;
  - `test_toggle_does_not_edit_the_schedules` — D-05, состав расписаний маршрут не читает и не
    пишет;
  - `test_toggle_fragment_keeps_the_toggle_id` — разметочная половина QUAL-06.
- `09-REVIEW.md`, WR-02 целиком — механизм, названный по шагам, и предложенная правка.
- `09-CONTEXT.md`, решение **D-05** — тумблер как единственный способ исключить группу из рассылки
  без удаления; на нём стоит серьёзность находки.
  </read_first>

  <behavior>
Обработчик переключения БЕРЁТ состояние из тела формы, а не инвертирует хранимое.

- Тест 1 (`test_the_toggle_honours_the_posted_state`): тело с присланным значением чекбокса делает
  группу активной независимо от прежнего состояния; тело БЕЗ значения делает её неактивной
  независимо от прежнего. Оба направления, оба стартовых состояния — четыре случая.
- Тест 2 (`test_two_toggles_and_a_submit_land_on_what_the_control_shows`, НЕСУЩИЙ — это и есть
  дефект): исходно группа неактивна; человек в мире «Alpine жив, htmx мёртв» включает и выключает
  чекбокс (на экране — по-прежнему выключено) и нажимает «Применить», то есть шлёт ОДИН запрос с
  телом БЕЗ значения чекбокса. Группа обязана остаться НЕАКТИВНОЙ. **До правки этот тест красный:**
  слепая инверсия делает её активной.
- Тест 3 (`test_the_toggle_is_idempotent_on_a_repeated_post`): два одинаковых запроса подряд дают
  то же состояние, что и один. Утверждение прямо заменяет то, что раньше давала инверсия, и
  сохраняет QUAL-01.
- Тест 4 (деградация не двинулась): `test_toggle_degrades_without_htmx` остаётся зелёным БЕЗ
  правки — ответ 302 на экран групп, группа неактивна.
  </behavior>

  <action>
**1. Сигнатура обработчика.** Добавить параметр `is_active: str | None = Form(None)` — присланное
значение чекбокса. Тип строковый и необязательный намеренно: снятый чекбокс не шлёт НИЧЕГО, и
булев параметр с умолчанием заставил бы клиента объявлять отсутствие, чего разметка формы сделать
не может.

**2. Присвоение вместо инверсии.** Строку инверсии заменить на присвоение
`group.is_active = is_active is not None`. Тройной `WHERE`, `commit`, ветка «строка не найдена» и
абзац о неотличимости не трогаются ни на символ.

**3. Комментарий над строкой переписывается ЦЕЛИКОМ, а не правится по слову.** В нём обязаны
стоять четыре вещи: (а) состояние берётся ИЗ ФОРМЫ, потому что между отрисовкой и отправкой
чекбокс может смениться чётное число раз; (б) названный мир, в котором это достижимо — «Alpine жив,
htmx мёртв», заведённый планом 09-07 условным снятием кнопки; (в) повторный запрос по-прежнему
безвреден, потому что присвоение идемпотентно — то есть QUAL-01 не ослаблен, а держится теперь
другим свойством, и это НАЗВАНО; (г) прежнее обоснование инверсии не стирается, а называется
описывающим снятую форму, с номером этого плана (образец обращения с отменённой формулировкой —
записи D-30 и D-32 в `.planning/STATE.md`).

**4. Абзац D-08 про обратимость действия** («действие обратимо одним нажатием и потому обходится
без панели подтверждения») сохраняется: он про интерфейс, а не про способ вычисления состояния.

**5. Найти и обновить тесты, чьё ожидание меняется.** Греп-обходом по адресу маршрута
переключения собрать ВСЕ вызывающие его тесты и прочитать каждый. Тест, ожидание которого меняется
из-за смены семантики, правится с НАЗВАННОЙ причиной в его собственном докстринге; число таких
тестов записывается в сводку числом, а не словом «несколько».

**6. Порядок коммитов.** Сначала коммит с тестами (тест 2 красный), затем коммит с правкой
обработчика (тест 2 зелёный). Переход цвета обязан быть виден в истории.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle"</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "two_toggles_and_a_submit or toggle_honours_the_posted_state or toggle_is_idempotent"</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "degrades_without_htmx or does_not_edit_the_schedules or fragment_keeps_the_toggle_id"</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "def test_the_toggle_honours_the_posted_state" tests/test_pages/test_account_groups.py` → `1`.
- `grep -c "def test_two_toggles_and_a_submit_land_on_what_the_control_shows" tests/test_pages/test_account_groups.py` → `1`.
- `grep -c "def test_the_toggle_is_idempotent_on_a_repeated_post" tests/test_pages/test_account_groups.py` → `1`.
- Переход цвета доказан историей: `git log --oneline` показывает коммит с тестами и отдельный коммит с правкой обработчика, и `git stash`-независимый прогон теста 2 на первом коммите даёт ненулевой код.
- Присланное значение читается: `grep -c "is_active: str | None = Form(None)" app/pages/account_groups.py` → `1`.
- `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle"` → `0 failed`.
- Деградация не двинулась: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "degrades_without_htmx"` → `0 failed`, и `git diff tests/test_pages/test_account_groups.py` не содержит изменений в теле `test_toggle_degrades_without_htmx`.
- D-05 цел: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "does_not_edit_the_schedules"` → `1 passed`, и `git diff app/pages/account_groups.py` не содержит обращений к репозиторию расписаний внутри обработчика переключения.
- QUAL-06 разметочная половина цела: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "fragment_keeps_the_toggle_id"` → `1 passed`.
- Гейт G-2 зелен и не тронут: `uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly` → `0 failed`.
- Число тестов, чьё ожидание изменилось, записано в сводке ЧИСЛОМ, и у каждого такого теста в докстринге названа причина.
  </acceptance_criteria>

  <done>Тумблер приводит группу в состояние, показанное человеку. Мир «Alpine жив, htmx мёртв»
исполняется тестом, который краснел до правки. Идемпотентность, деградация, D-05 и разметочная
половина QUAL-06 целы.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: обе формы пути удаления шлют один набор полей, и это держит правило (WR-03)</name>

  <files>app/templates/account_groups/includes/group_row.html, tests/test_pages/test_account_groups.py</files>

  <read_first>
- `app/templates/account_groups/includes/group_row.html` строки 185-277 целиком: форма-триггер
  (около 190-193) с `x-data x-on:submit.prevent="$dispatch(...)"` и блочный вызов панели
  подтверждения со слотом из скрытых полей. **Абзац «⚠️ ФОРМА-ТРИГГЕР ВЫШЕ НЕ ТРОНУТА ВОВСЕ
  (D-03)» читать целиком:** он объясняет, чего форма-триггер НЕ получает, и правка обязана это
  сохранить — добавляется ровно одно скрытое поле, признака htmx-отправки и обёртки макросом не
  добавляется.
- `app/pages/account_groups.py`: `_clean_search` (:38-41), `_filter_params` (:63-65), `_screen_url`
  (:68-89) с абзацем «адрес после действия и адрес после перезагрузки обязаны приходить из одного
  источника», `_current_listing_has_a_row` (:92-113) с абзацем про WR-06, и место, где обработчик
  удаления читает строку поиска из тела формы (около 596-605).
- `09-REVIEW.md`, WR-03 целиком — и вторая половина находки: **ветка решения тоже смещается**,
  `_current_listing_has_a_row(..., search=None)` спрашивает про весь аккаунт вместо той выдачи,
  которую видел человек, поэтому починка WR-06 на базовом пути сработать не может.
- `09-06-SUMMARY.md` — что именно чинил WR-06 и каким утверждением это закреплено.
- `tests/test_pages/test_account_groups.py` — существующие правила пути удаления и помощники
  разбора разметки; `test_delete_degrades_without_htmx`.
  </read_first>

  <behavior>
Новое правило `test_both_delete_forms_post_the_same_field_names` утверждает: **множество имён полей,
которые шлёт форма-триггер удаления, совпадает с множеством имён полей, которые шлёт форма панели
подтверждения того же ряда.**

- Тест 1 (НЕСУЩИЙ, разметочный): оба множества, снятые с отрендеренной страницы, равны. **До
  правки красный** — у формы-триггера нет поля строки поиска.
- Тест 2 (антивакуум): число имён равно `DELETE_FORM_FIELD_NAMES_MEASURED` и строго больше нуля;
  без него правило зеленело бы от разборщика, потерявшего обе формы разом.
- Тест 3 (поведенческий): удаление ПОСЛЕДНЕЙ строки ОТФИЛЬТРОВАННОЙ выдачи по базовому пути (без
  признака htmx) приземляет на адрес С СОХРАНЁННЫМ фильтром — то есть ветка «выдача опустела»
  решается по той выдаче, которую человек видел. До правки красный: строка поиска до сервера не
  доезжает.
  </behavior>

  <action>
**1. Правка разметки — ровно одно скрытое поле.** В форму-триггер удаления добавить скрытое поле с
тем же именем и тем же значением, что уже стоит в слоте вызова панели подтверждения (значение —
`filter_search`). Больше в этой форме не меняется НИЧЕГО: ни признака htmx-отправки, ни обёртки
макросом, ни атрибутов Alpine — решение D-03 не пересматривается, и абзац о нём в шапке макроса
дополняется одной строкой с номером этого плана, а не переписывается.

**2. Разборщик двух форм.** В тестовом файле завести помощник, который по отрендеренной странице
находит для заданного идентификатора группы обе формы, ведущие на адрес удаления, и возвращает
множества имён их полей. Границу назвать честно абзацем докстринга по образцу соседних сканеров
файла: разборщик не поддерживает вложенных форм, в дереве их сегодня нет, и Фаза 10, доводящая
число мест подтверждения до шестнадцати, обязана эту границу пересмотреть.

**3. Правило и его антивакуум.** Порядок утверждений: сначала антивакуумное (обе формы найдены,
число имён равно `DELETE_FORM_FIELD_NAMES_MEASURED` и больше нуля), затем несущее (множества
равны). Сообщение об отказе называет разность множеств и следствие прямым текстом: путь без Alpine
приземляется на список, который человек не фильтровал, и ветка «выдача опустела» решается по
другому вопросу, чем тот, на который смотрит человек.

**4. Поведенческое утверждение.** Посеять аккаунт с несколькими группами, из которых ОДНА отвечает
строке поиска; удалить её базовым путём (клиент без признака htmx), передав тело формы-триггера
как его шлёт разметка; утвердить, что `Location` ответа несёт параметр фильтра. Это и есть та
половина находки, которую разметочное правило само по себе не ловит.

**5. IN-03 в том же обработчике.** Перенести два запроса подсчёта (`_group_counts`) ВНУТРЬ
отложенной сборки фрагмента обработчика удаления — туда же, куда их кладёт обработчик тумблера, и
по той же причине, названной в его докстринге. `_current_listing_has_a_row` исполняется первым в
любом случае, поэтому порядок ничего не теряет. В докстринге назвать, что обработчик следует
собственному правилу-близнецу, и сослаться на соседний абзац, а не пересказывать его.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "both_delete_forms_post_the_same_field_names"</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "degrades_without_htmx or listing or empt"</automated>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_inventory.py -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "def test_both_delete_forms_post_the_same_field_names" tests/test_pages/test_account_groups.py` → `1`.
- `grep -c "DELETE_FORM_FIELD_NAMES_MEASURED" tests/test_pages/test_account_groups.py` → не меньше `2`.
- Переход цвета доказан историей: коммит с правилом (красное) отделён от коммита с правкой разметки (зелёное), оба видны в `git log --oneline`.
- Правка разметки — ровно одно поле: `git diff app/templates/account_groups/includes/group_row.html` содержит ровно одну добавленную строку с тегом `input` внутри формы-триггера, и ни одной изменённой строки в её открывающем теге.
- Решение D-03 цело: в форме-триггере нет ни одного атрибута с префиксом `hx-` — проверяется прогоном существующего правила `test_every_htmx_post_is_born_of_a_component_macro` (`0 failed`) и `grep -c "hx-" ` по строкам этой формы, равным `0` в диффе.
- Поведенческая половина закрыта: удаление последней строки отфильтрованной выдачи базовым путём даёт `Location` с параметром фильтра — утверждается новым тестом, и он зелен.
- IN-03 закрыт: в обработчике удаления вызов `_group_counts` стоит ВНУТРИ тела вложенной сборки фрагмента (проверяется чтением диффа и записывается в сводке), и `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly` → `0 failed`.
- Инвентарные гейты разметки зелены: `uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_inventory.py -q -p no:randomly` → `0 failed`. Если добавленное поле сдвинуло инвентарное число — движение получило строку летописи с номером `09-15`.
  </acceptance_criteria>

  <done>Обе формы пути удаления шлют один набор полей, и это утверждается правилом с антивакуумом;
базовый путь приземляется на ту выдачу, которую человек видел, и ветка «выдача опустела» на нём
работает; два лишних запроса подсчёта на ветке перехода не исполняются.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: маршрут порции не отвечает целым документом ни в одной ветке (WR-04)</name>

  <files>app/pages/account_groups.py, tests/test_pages/test_account_groups.py, tests/test_pages/test_htmx_gates.py</files>

  <read_first>
- `app/pages/account_groups.py`:
  - `account_groups_partial` целиком (около 266-320) — обе ветки собственного перенаправления:
    истёкшая сессия (`/login`) и чужой аккаунт (`/accounts`);
  - `account_groups_sync_status` (около 335-350) — **соседний маршрут, который отвергает ровно это
    прямым текстом**; читать его формулировку и повторять решение, а не изобретать своё;
  - докстринг `account_groups_toggle` (около 378-398), абзац «⚠️ СОБСТВЕННОГО
    ОТВЕТА-ПЕРЕНАПРАВЛЕНИЯ ЗДЕСЬ НЕТ НИ В ОДНОЙ ВЕТКЕ, ВКЛЮЧАЯ „НЕТ СЕССИИ“» — там же записано,
    почему имена класса ответа и статуса в докстринге набраны СЛОВАМИ, а не литералами: гейт
    греплет тело обработчика, и докстринг, набравший их дословно, удовлетворил бы греп сам. Новый
    докстринг обязан соблюсти это же правило.
- `app/pages/htmx.py`: `respond()` (около 305-400) — сигнатура с обязательным ключевым `redirect=`,
  что происходит на пути без признака htmx (302) и на пути с ним (204 + заголовок перехода),
  `_local_path` и его отказ на внешнем адресе.
- `tests/test_pages/test_htmx_gates.py`:
  - шапка модуля, абзац про летопись `NOT_YET_CONVERTED_COUNT` (около 60-75);
  - `NOT_YET_CONVERTED_COUNT` — константа около строки 262 (действующее значение читать из
    файла, а не из этого плана) и комментарий над ней около строки 197 про то, что тестов на неё
    ДВА и у каждого движения свой текст;
  - `test_no_converted_handler_builds_its_own_redirect` (гейт G-2) и оба правила числа (около
    726 и 746) — прочитать сообщения об отказе: они прямо говорят, что упавшее число есть ПРОГРЕСС
    вехи и требует опустить константу.
- `09-REVIEW.md`, WR-04 целиком — механизм («XHR следует за редиректом прозрачно») и предложенная
  правка.
  </read_first>

  <behavior>
Новое правило `test_the_portion_route_never_answers_with_a_whole_document` утверждает: **маршрут
порции не отдаёт целого документа ни в одной ветке отказа.**

- Тест 1 (истёкшая сессия, с признаком htmx): ответ не содержит признака целого документа
  (`<!DOCTYPE`), и переход сообщается заголовком, а не телом. **До правки красный:** клиент следует
  за редиректом и получает страницу логина со статусом 200.
- Тест 2 (истёкшая сессия, БЕЗ признака htmx): ответ — 302 на адрес входа. Путь деградации не
  двинулся.
- Тест 3 (чужой аккаунт, с признаком htmx): то же самое для второй ветки отказа — она обязана
  идти тем же путём, а не остаться единственной с собственным перенаправлением.
- Тест 4 (счастливый путь не тронут): порция своего аккаунта по-прежнему отдаёт строки и узел
  курсора.
  </behavior>

  <action>
**1. Обе ветки отказа — через слой ответа.** Заменить оба собственных перенаправления маршрута
порции вызовами `respond(request, redirect=…)` с теми же адресами, что стоят сегодня: адрес входа
для отсутствующей сессии и адрес списка аккаунтов для чужого аккаунта. Собственной сборки
перенаправления в теле обработчика не остаётся ни одной — гейт G-2 считает нарушением ЛЮБУЮ
собственную сборку у обработчика, который вдобавок зовёт слой ответа.

**2. Докстринг маршрута.** Дописать абзац: почему фрагментному маршруту нельзя отвечать
перенаправлением — XHR следует за ним прозрачно, и в середину списка приезжает целый документ со
вторым набором идентификаторов, то есть уникальность `id` документа ломается тем же способом, что
и в CR-01. Сослаться на соседний маршрут, который отвергает это же, а не пересказывать его довод.
⚠️ Имена класса ответа и статуса набрать СЛОВАМИ, а не литералами — по правилу, уже записанному в
докстринге обработчика тумблера: иначе докстринг удовлетворит греп гейта сам.

**3. Число непереведённых обработчиков.** Маршрут переходит в множество ПЕРЕВЕДЁННЫХ, поэтому
`NOT_YET_CONVERTED_COUNT` опускается. Значение поставить ПО ВЫВОДУ покрасневшего правила, а не
вычитанием единицы в уме, и дописать строку летописи по форме уже существующих: «было → стало,
Фаза 9, план 09-15, причина». Причина называется предметно: маршрут порции переведён затем, чтобы
htmx не получал целого документа логина в середину списка.

**4. Импорт `RedirectResponse`.** Если после правки он остаётся нужен другим маршрутам модуля —
оставить; если перестаёт использоваться вовсе — снять, и прогоном убедиться, что гейт G-2 при этом
не потерял предмет проверки (правило считает вхождения в ТЕЛАХ обработчиков, а не импорты, но
убедиться обязан прогон).

**5. Прогон полной суиты.** Итог задачи — зелёная суита целиком, не меньше 2655 собранных.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "portion_route_never_answers"</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly</automated>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "def test_the_portion_route_never_answers_with_a_whole_document" tests/test_pages/test_account_groups.py` → `1`.
- Переход цвета доказан историей: коммит с правилом (красное) отделён от коммита с переводом маршрута (зелёное).
- Обе ветки отказа маршрута порции идут через слой ответа: в теле `account_groups_partial` есть два вызова `respond(`, и `uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly -k "no_converted_handler_builds_its_own_redirect"` → `1 passed`.
- `NOT_YET_CONVERTED_COUNT` опущено и снабжено летописью: `grep -c "09-15" tests/test_pages/test_htmx_gates.py` не меньше `1`, и оба правила числа зелены (`uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly` → `0 failed`).
- Путь деградации маршрута порции цел: запрос без признака htmx получает `302` — утверждается тестом 2 нового правила, и он зелен.
- Счастливый путь порции цел: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "partial or portion or scroll or sentinel"` → `0 failed`.
- Докстринг не удовлетворяет греп гейта сам: `uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly` → `0 failed` при непустом множестве проверяемых обработчиков (правило числа зелено, то есть множество не опустело).
- `uv run pytest tests/ -q -p no:randomly` → `0 failed`, собрано не меньше `2655`.
- Ни одна строка таблицы состояний и ни один флажок `.planning/REQUIREMENTS.md` не изменены: `git diff --stat .planning/REQUIREMENTS.md` пуст.
- Планы 09-01 … 09-14, их сводки и `09-VERIFICATION.md` не тронуты.
  </acceptance_criteria>

  <done>Маршрут порции не отвечает целым документом ни в одной ветке отказа; путь деградации на нём
прежний; число непереведённых обработчиков опущено записью летописи, и оба его правила зелены.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → FastAPI (`POST /accounts/{id}/groups/{gid}/toggle`) | недоверенный ввод: значение чекбокса в теле формы, заголовки htmx, кука сессии |
| браузер → FastAPI (`POST /accounts/{id}/groups/{gid}/delete`) | недоверенный ввод: строка поиска в теле формы, идентификаторы пути |
| браузер → FastAPI (`GET /accounts/{id}/groups/partial`) | недоверенный ввод: курсор, размер порции, строка поиска; ответ приезжает В СЕРЕДИНУ существующего документа |
| внешний мессенджер → шаблон строки группы | имя группы приложением не контролируется |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-15-01 | Tampering | значение чекбокса в теле запроса переключения | medium | mitigate | значение управляет ТОЛЬКО состоянием собственной строки и только после тройного `WHERE` (`Group.id`, `Group.user_id`, `Group.account_id`); чужой строки не достаёт. Присвоение идемпотентно, второй запрос новой записи не создаёт (QUAL-01) |
| T-09-15-02 | Tampering | строка поиска, приезжающая теперь ДВУМЯ формами | medium | mitigate | значение проходит тот же `_clean_search` и уезжает в запрос BIND-ПАРАМЕТРОМ сравнения, а не конкатенацией SQL (T-03-21); в адрес перехода оно попадает через `urlencode`, а `_local_path` слоя ответа не выпускает в заголовок ни символа вне ASCII и ни одного управляющего |
| T-09-15-03 | Elevation of privilege | перевод маршрута порции на слой ответа не должен ослабить проверку владения | high | mitigate | аутентификация и владение проверяются ЗДЕСЬ ЗАНОВО и до развилки транспорта; меняется только ФОРМА ответа на отказ. Закреплено тестами 1-3 нового правила и прогоном файла экрана целиком |
| T-09-15-04 | Information disclosure | целый документ логина, вклеенный в середину списка | high | mitigate | ветки отказа переведены на слой ответа: htmx получает переход заголовком, а не телом; второй `<form action="/login">` и второй набор идентификаторов в документ не попадают |
| T-09-15-05 | Repudiation | движение `NOT_YET_CONVERTED_COUNT` без следа | medium | mitigate | строка летописи с номером плана и предметной причиной; оба правила числа зелены |
| T-09-15-06 | Tampering | имя группы из внешнего мессенджера в атрибутах строки | medium | mitigate | план не вводит ни одного нового вывода имени; добавляемое скрытое поле несёт строку ПОИСКА, уже экранируемую тем же механизмом, что и в форме панели; прогон `tests/test_templates/test_components.py` обязателен |
| T-09-15-SC | Tampering | установка пакетов | low | accept | план не выполняет ни одной команды менеджера пакетов; гейт легитимности пакетов не срабатывает |

**Блокирующий порог — `high`, уровень ASVS — L1.** Обе угрозы этого уровня (T-09-15-03,
T-09-15-04) снабжены машинными проверками в задаче 3, и без них задача не считается выполненной.
</threat_model>

<verification>
1. `uv run pytest tests/ -q -p no:randomly` → зелено целиком, собрано не меньше 2655.
2. Каждое из трёх новых правил прошло путь RED → GREEN разными коммитами, и это видно в истории.
3. Обе пары `*_degrades_without_htmx` зелены; путь без htmx на всех трёх маршрутах прежний.
4. `hx-sync='this:drop'`, тройной `WHERE` обоих обработчиков и разметочная половина QUAL-06 целы.
5. `NOT_YET_CONVERTED_COUNT` опущено записью летописи; оба его правила и гейт G-2 зелены.
6. Число тестов, чьё ожидание изменилось из-за смены семантики тумблера, записано в сводке числом,
   и у каждого названа причина.
7. DEF-09-01, DEF-09-02 и остаток DIV-09-01 не тронуты и не переплановываются.
8. Ни одна строка таблицы состояний и ни один флажок `.planning/REQUIREMENTS.md` не изменены.
9. Планы 09-01 … 09-14, их сводки и `09-VERIFICATION.md` не тронуты.
</verification>

<success_criteria>
- Тумблер приводит группу в показанное человеку состояние во всех трёх достижимых мирах (htmx жив;
  Alpine жив, htmx мёртв; ни того ни другого), и это утверждается тестом, краснувшим до правки.
- Обе формы пути удаления шлют один набор полей; базовый путь приземляется на ту выдачу, которую
  человек видел.
- Маршрут порции не отвечает целым документом ни в одной ветке отказа.
- Каждое закрытое замечание ревизии несёт машинное правило, которое покраснело бы на его
  отсутствии; ни одно не закрыто одной правкой без гейта.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-15-SUMMARY.md` when done
</output>
