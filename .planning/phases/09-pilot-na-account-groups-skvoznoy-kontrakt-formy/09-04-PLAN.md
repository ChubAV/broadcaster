---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - app/templates/includes/htmx_error_banner.html
  - tests/test_pages/test_shell.py
  - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
autonomous: true
requirements: [QUAL-02, QUAL-06]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Текст плашки обрыва сети говорит о возможном РАСХОЖДЕНИИ экрана с сервером, а не повторяет «попробуйте ещё раз»: у тумблера браузер переключает чекбокс до ответа, и при неответившем сервере на экране остаётся оптимистичный результат (D-16)"
    - "Дополнение затрагивает ровно ОДНУ строку текста в отгруженном includes/htmx_error_banner.html — это дополнение формулировки, а не пересмотр QUAL-03"
    - "Третьего встроенного обработчика событий htmx в шаблонах не появляется: клиентский откат чекбокса отвергнут, инвентаризация встроенных обработчиков остаётся прежней (D-16)"
    - "Артефакт ручного обхода 09-UAT.md существует и называет пункты 3 и 6 перечня роадмапа поимённо, а пункт 5 — как частичную проверку границы"
    - "Пункт 3 обхода содержит ОТДЕЛЬНУЮ клавиатурную проверку возврата фокуса, у которой записан ожидаемый ответ «нет» и три способа снятия конфликта D-06 ↔ QUAL-06"
    - "Успех фазы не объявляется по зелёной суите: браузерного стенда в проекте нет и не заводится, пункты 3 и 6 закрываются глазами человека"
    - statement: "Глазами подтверждено, что своп произошёл и именно в #group-row-N, OOB-счётчик приземлился в #account-groups-count, позиция прокрутки не потеряна — UAT-3"
      verification: backstop
    - statement: "Глазами подтверждено (или опровергнуто с записью результата), что после переключения тумблера клавиатурой фокус остался на том же тумблере — UAT-3, ожидаемый ответ сегодня «нет»"
      verification: backstop
    - statement: "Глазами подтверждено, что индикатор виден на медленной сети и не мигает на быстрой, и что hx-disabled-elt реально мешает второму нажатию — UAT-6"
      verification: backstop
    - statement: "Глазами подтверждено, что осиротевшая панель подтверждения снята после удаления и что после подмены строки панель не задвоилась, а Alpine в ней жив — UAT-5 частично"
      verification: backstop
  artifacts:
    - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
    - "tests/test_pages/test_shell.py — test_the_network_banner_names_the_screen_server_divergence"
  key_links:
    - "htmx_error_banner.html:74 → пользователь при обрыве сети: единственное место, где расхождение экрана с сервером называется словами; ломается — пользователь думает, что группа выключена, а объявление туда уходит"
    - "09-UAT.md → /gsd-verify-work: пункты 3 и 6 закрываются глазами; без артефакта они неотличимы от «покрыто тестами»"
  prohibitions:
    - statement: "второй обработчик события в htmx_error_banner.html не добавляется и <script> :76-86 не правится — третий встроенный обработчик покрасил бы инвентаризацию (test_only_known_non_dialog_submit_handlers_remain)"
      status: flagged-unverified
      verification: none
    - statement: "клиентский откат чекбокса при отказе не заводится: строка приезжает с сервера настоящим состоянием, а не ожидаемым (D-16)"
      status: flagged-unverified
      verification: none
    - statement: "Playwright и браузерный стенд не заводятся — отклонены на уровне вехи и отложены как E2E-01"
      status: flagged-unverified
      verification: none
    - statement: "строка 73 (плашка отказа сервера) не трогается — предмет дополнения только обрыв связи"
      status: flagged-unverified
      verification: none
---

<objective>
Две вещи, которые фаза закрывает **не кодом обработчика**: одна строка текста, признающая
единственную оставшуюся оптимистичную поверхность вехи (D-16), и артефакт ручного обхода,
без которого пункты 3 и 6 перечня роадмапа неотличимы от «покрыто тестами».

Purpose: у тумблера есть изъян, которого нет у кнопок — браузер переключает чекбокс **до**
ответа. D-13 закрывает случай «сервер ответил»; оптимистичным остаётся «сервер не ответил
вовсе», и его закрывает плашка QUAL-03 — её текст обязан сказать о расхождении экрана с
сервером. А успех фазы не может быть объявлен по зелёной суите: httpx не свопает и не собирает
OOB.

Output: дополненная строка `includes/htmx_error_banner.html:74`, утверждение в `test_shell.py`,
`09-UAT.md` с пунктами 3, 6 и частично 5.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-RESEARCH.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VALIDATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-01-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов фазы — в `09-01-PLAN.md` §«Artifacts this phase produces».
Этим планом создаются:

- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md`
- тест `test_the_network_banner_names_the_screen_server_divergence` в `tests/test_pages/test_shell.py`
</artifacts_this_phase_produces>

<tasks>

<task type="execute" tdd="true">
  <name>Задача 1: Плашка обрыва связи называет расхождение экрана с сервером (D-16)</name>

  <files>app/templates/includes/htmx_error_banner.html, tests/test_pages/test_shell.py</files>

  <read_first>
- `app/templates/includes/htmx_error_banner.html` — **читать ЦЕЛИКОМ.** Докстринг `:1-71`
  объясняет, почему плашек ДВЕ (разные поводы и разные следующие действия человека), почему
  заготовки обёрнуты, а не выписаны классами, и что суита утверждает **доставку** строк, а не
  их действие. Точка правки — строка **74** (`id="htmx-failure-network"`). Строка 73
  (`htmx-failure-server`) и `<script>` `:76-86` **не трогаются**.
- `tests/test_pages/test_shell.py:2043-2065` — `FAILURE_BANNER_OWNER`, `FAILURE_BANNER_IDS`,
  `FAILURE_BANNER_EVENTS`, `VALIDATION_STATUS`, `RESPONSE_BODY_READ` — форма объявления
  ожидания **строкой в тесте**, а не выборкой из проверяемого файла: «ожидание, добытое из
  предмета проверки, согласилось бы с любой его правкой».
- `tests/test_pages/test_shell.py:2155-2240` — `test_main_shell_carries_both_failure_banners`,
  `test_auth_shell_carries_both_failure_banners`, `test_the_failure_banner_carries_both_handlers`,
  `test_the_failure_banner_touches_no_markup_sink`,
  `test_the_failure_banner_tells_a_validation_answer_from_a_crash` — рамка, в которую встаёт
  новое утверждение.
- `09-CONTEXT.md` §Decisions D-16 и §Specific Ideas (последний абзац про дополнение текста):
  дополнение — **одна строка**, обязано говорить о расхождении экрана с сервером и **не
  повторять** «попробуйте ещё раз»: пользователь уже прочитал это в первой половине.
- `app/templates/components/alert.html` — макрос, печатающий текст плашки; параметров
  идентификатора и скрытия он не принимает.
  </read_first>

  <reversibility rating="reversible">
Правка касается одной строки текста в отгруженном артефакте Фазы 8. Это дополнение
формулировки, а не пересмотр QUAL-03; откат — возврат одной строки.
  </reversibility>

  <behavior>
1. `test_the_network_banner_names_the_screen_server_divergence` — красный до правки: текст
   плашки `htmx-failure-network` не содержит слов о расхождении показанного на экране с
   состоянием на сервере.
2. Тот же тест утверждает, что дополнение **не повторяет** уже сказанное: подстрока
   «попробуйте ещё раз» встречается в строке `htmx-failure-network` ровно один раз (она там
   уже есть), а не дважды.
3. Существующие тесты рамки (`carries_both_failure_banners`, `carries_both_handlers`,
   `touches_no_markup_sink`, `tells_a_validation_answer_from_a_crash`) остаются зелёными:
   правится только текстовый аргумент `alert(...)`.
  </behavior>

  <action>
**1. `tests/test_pages/test_shell.py`** — рядом с `FAILURE_BANNER_IDS` объявить константу
ожидания **строкой в тесте**, по форме `VALIDATION_STATUS` (`:2059-2061`): набор слов, которые
обязаны присутствовать в тексте плашки обрыва связи, например
`NETWORK_BANNER_DIVERGENCE = ("экран", "серв")` — с комментарием, объясняющим, ПОЧЕМУ ожидание
выписано здесь, а не выбрано из проверяемого файла (ожидание, добытое из предмета проверки,
согласилось бы с любой его правкой), и ПОЧЕМУ оно вообще существует: у тумблера браузер
переключает чекбокс до ответа, поэтому при неответившем сервере на экране остаётся
оптимистичный результат, а веха оптимистичный UI прямо запрещает — «пользователь думает, что
группа выключена, а объявление туда уходит».

Новый тест `test_the_network_banner_names_the_screen_server_divergence` читает исходник
`includes/htmx_error_banner.html`, выделяет строку узла `id="htmx-failure-network"` и требует
наличия всех слов ожидания, а также ровно одного вхождения «попробуйте ещё раз» — дополнение
обязано **добавлять смысл, а не повторять** уже сказанное.

**2. `app/templates/includes/htmx_error_banner.html`, строка 74.** Текст аргумента `alert(...)`
дополняется **одним** предложением, которое:
- говорит, что показанное на экране могло разойтись с тем, что записано на сервере, и что
  обновление страницы покажет настоящее состояние;
- **не повторяет** «Проверьте соединение и попробуйте ещё раз» — эта половина уже есть;
- остаётся текстом-параметром макроса `alert`, то есть проходит обычное автоэкранирование:
  готовая разметка макросу не передаётся (GATE-07).
Строка 73, `<script>` `:76-86` и докстринг `:1-71` (кроме, при необходимости, одной поясняющей
строки о D-16) не трогаются: третий встроенный обработчик покрасил бы инвентаризацию встроенных
обработчиков, а клиентский откат чекбокса отвергнут решением.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_shell.py -k "network_banner_names" -x` — зелено.
- `uv run pytest tests/test_pages/test_shell.py -q` — зелено целиком, включая четыре теста рамки
  плашки.
- `git diff --stat app/templates/includes/htmx_error_banner.html` показывает изменения **только**
  в области строки 74 (плюс, возможно, одна поясняющая строка докстринга); строка 73 и блок
  `<script>` не затронуты.
- `grep -c 'addEventListener' app/templates/includes/htmx_error_banner.html` возвращает `2` —
  третьего встроенного обработчика не появилось.
- `grep -c 'safe\|Markup(' app/templates/includes/htmx_error_banner.html` возвращает `0`.
  </acceptance_criteria>

  <done>
Единственная оставшаяся оптимистичная поверхность вехи названа словами там, где пользователь её
встретит: при обрыве связи плашка говорит о возможном расхождении экрана с сервером, а не
повторяет «попробуйте ещё раз». Утверждение выписано ожиданием-строкой в тесте, а не выборкой
из проверяемого файла.
  </done>
</task>

<task type="execute">
  <name>Задача 2: Артефакт ручного обхода `09-UAT.md` — пункты 3 и 6, частично 5</name>

  <files>.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md</files>

  <read_first>
- `.planning/ROADMAP.md` §«Ручной UAT — девять именованных пунктов» — таблица девяти; фазе
  принадлежат **3** и **6**, пункт **5** проверяется здесь частично (граница), пункт **4** —
  Фазе 10 и в этот артефакт не входит.
- `.planning/ROADMAP.md` §«Phase 9», критерии 1, 3, 4 — какие половины машинны, а какие нет.
- `09-VALIDATION.md` §Manual-Only Verifications — пять поведений с готовыми инструкциями обхода;
  переносятся в артефакт, а не переписываются заново.
- `09-RESEARCH.md` §4.3 — конфликт D-06 ↔ QUAL-06 целиком: порядок вызовов htmx
  (`nn(r)` → своп → `rn(R,q)`), почему `document.activeElement` становится `<body>`, три
  записанных способа снятия конфликта и почему третий (блокировать `<label>`) негоден.
- `09-RESEARCH.md` §5.4 — почему на пути **удаления** восстанавливать фокус не на что по
  построению, и что это обязано быть названо, а не оставлено необъяснённым.
- `09-RESEARCH.md` §Assumptions Log — A1, A2, A3, A5: допущения, которые обход обязан проверить
  или назвать непроверенными.
- `.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md` — форма
  артефакта ручного обхода, отгруженная Фазой 7: структура пунктов, поля «что делать / что
  ожидается / результат».
  </read_first>

  <action>
Создать `09-UAT.md` по форме `07-UAT.md`. Артефакт — **процедура**, а не отчёт: поля результата
остаются пустыми до обхода.

Обязательная шапка: браузерного стенда в проекте нет и не заводится (Playwright отклонён на
уровне вехи, отложен как `E2E-01`), суита утверждает **доставку** разметки и заголовков, но не
**эффект**; поэтому **успех фазы не объявляется по зелёной суите**.

**Пункт 3 (свап и OOB) — четыре отдельные проверки, а не одна:**
1. *Своп в тот элемент.* Прокрутить список групп ниже сгиба, переключить тумблер **средней**
   строки → строка перерисовалась **на месте**, список не дёрнулся, позиция прокрутки не
   потеряна, соседние строки не перерисованы.
2. *Приземление OOB.* В той же проверке — число в линейке над списком («N активных из M»)
   изменилось. Ожидаемая цель — `#account-groups-count`. ⚠️ Если счётчик не обновился при
   ответе 200 и чистой консоли — это признак вложенного OOB-узла
   (`allowNestedOobSwaps: false` снимает признак **молча**), а не признак ошибки сервера.
3. *Возврат фокуса, КЛАВИАТУРОЙ.* Установить фокус на тумблер клавишей Tab, переключить
   **Space** → после подмены фокус остался на том же тумблере?
   ⚠️ **ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ — «НЕТ».** Основание записано и проверено чтением вендоренного
   htmx 2.0.10: `hx-disabled-elt` (D-06) снимает блокировку **после** свапа (`rn(R,q)` в
   `onload` после `M(r,T)`), отключённый чекбокс теряет фокус **до** свапа,
   `document.activeElement` становится `<body>`, и условие восстановления фокуса «активный
   элемент ушёл из документа» ложно. **Проверить, а не предположить**: допущение A1 (браузер
   снимает фокус с элемента, которому выставлен `disabled`) не измерялось. Если ответ «да» —
   конфликта нет, и это записать. Если «нет» — записать «нет» и передать владельцу три
   записанных способа снятия:
   (а) не блокировать чекбокс вовсе — вред QUAL-01 к **инверсии** не относится: два
   переключения дают два инвертирования, второй записи не создаётся, конечное состояние
   сходится с показанным;
   (б) `hx-sync="this:drop"` на форме тумблера вместо блокировки — ⚠️ перед применением
   проверить формулировку гейта Фазы 8 (запрещён `hx-sync` с `queue` на опасных маршрутах;
   `drop` под запрет не подпадает, но формулировку разведка не читала — допущение A4);
   (в) блокировать обёртку `<label class="toggle">` — **вариант негодный**, `disabled` на
   `label` не действует и защиты не даёт; назван, чтобы его не пробовали.
4. *Фокус после удаления — названное изъятие, а не пропуск.* После удаления восстанавливать
   фокус **не на что по построению**: ни строки, ни панели, ни кнопки, начавшей запрос, в
   документе не остаётся, а механизм htmx требует нового элемента с ТЕМ ЖЕ `id`. Регрессии
   нет — сегодня страница перезагружается и фокус теряется так же. Записать как изъятие
   словами; параметр `id` у `button()` этого не изменил бы и потому в Фазе 9 не заводится.

**Пункт 6 (`hx-indicator` / `hx-disabled-elt`) — две проверки:**
1. *Порог виден и не мигает.* DevTools → Network throttling «Slow 3G» → при переключении
   тумблера индикатор появляется. Без троттлинга — **не мигает** ни разу. ⚠️ Если мигает при
   выключенном троттлинге, первый подозреваемый — имя класса: собственный стиль htmx для
   `.htmx-indicator` приезжает в конец `<head>`, то есть после `app.css`, и при равной
   специфичности выигрывает, съедая задержку. Второй подозреваемый — задержка, повешенная на
   базовое правило вместо состояния «запрос идёт». Допущение A3 (ответ укладывается быстрее
   300 мс) замерами не подтверждено — при систематическом мигании порог обесценился.
2. *Двойное нажатие.* Быстро нажать на тумблер дважды → второй запрос не уходит (вкладка
   Network). ⚠️ Отдельно проверить, что кнопка **Отмены** в панели подтверждения удаления
   **не** блокируется никогда.

**Пункт 5 (частично, граница Alpine) — две проверки:**
1. Удалить группу → осиротевшая панель подтверждения снята со страницы (в DOM не осталось узла
   `id="group-del-N"`), поверх пустого места не висит `role="dialog"` с ловушкой фокуса.
2. Переключить тумблер строки → панель подтверждения **этой** строки не задвоилась (узлов
   `id="group-del-N"` в документе ровно один); открыть подтверждение у соседней строки → Alpine
   в ней жив, Esc закрывает, Tab не уходит в невидимую копию.

**Раздел «Что проверяется глазами, потому что иначе никак»** — перечислить пять поведений из
`09-VALIDATION.md` §Manual-Only Verifications с их требованиями и пометкой, что каждое
объявлено в планах как `verification: backstop`, то есть при верификации фазы без явного
свидетельства уходит в `human_needed`, а не проходит молча.

**Раздел «Допущения, которые обход обязан закрыть»** — A1 (фокус при `disabled`), A2 (отступы
`.count-rule` под новой обёрткой — предмет UI-ревью), A3 (порог 300 мс против реального времени
ответа), A5 (тело панели при переключении не устаревает).

**Раздел «Что в этот обход НЕ входит»** — пункт 4 перечня (снятие осиротевшей панели во всех
16 местах) принадлежит Фазе 10; пункты 1, 2, 8 закрыты Фазами 7 и 8; пункты 7 и 9 — Фаза 15.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib,sys; p=pathlib.Path('.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md'); s=p.read_text(encoding='utf-8'); need=['ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ','Slow 3G','group-del-','account-groups-count','E2E-01']; miss=[n for n in need if n not in s]; sys.exit('нет в 09-UAT.md: '+repr(miss) if miss else 0)"</automated>
  </verify>

  <acceptance_criteria>
- Файл `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md` существует.
- Команда `<automated>` выше проходит: артефакт содержит подстроки `ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ`,
  `Slow 3G`, `group-del-`, `account-groups-count`, `E2E-01`.
- Пункт 3 разбит **не менее чем на четыре** нумерованные проверки, среди которых отдельная
  клавиатурная (Space) и отдельная про фокус после удаления как названное изъятие.
- Пункт 3, проверка возврата фокуса, перечисляет **три** способа снятия конфликта D-06 ↔ QUAL-06,
  и третий из них помечен как негодный:
  `uv run python -c "import pathlib; s=pathlib.Path('.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md').read_text(); assert 'hx-sync' in s and 'label' in s and 'негодн' in s"`.
- Пункт 6 содержит обе проверки — порог видимости и защиту от двойного нажатия — и отдельную
  строку про кнопку Отмены.
- Раздел допущений называет A1, A2, A3, A5 поимённо.
- Раздел «Что в этот обход НЕ входит» называет пункт 4 как принадлежащий Фазе 10.
- Поля результата **пустые**: артефакт создан как процедура, а не как отчёт.
  </acceptance_criteria>

  <done>
Пункты 3 и 6 перечня ручного UAT (плюс частично 5) выписаны поимённо, с точной процедурой,
записанным ожиданием «нет» на клавиатурный фокус и тремя названными выходами из конфликта.
Успех фазы больше не может быть объявлен по зелёной суите: закрытие этих пунктов требует
человека, и это записано в артефакте, а не подразумевается.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ сервера → экран пользователя | при обрыве связи ответа нет вовсе, а чекбокс уже переключён браузером: экран показывает состояние, которого на сервере может не быть |
| текст плашки → пользователь | единственный канал, в котором расхождение экрана с сервером называется словами |
| «зелёная суита» → вывод о готовности | суита утверждает доставку разметки, но не эффект; вывод «работает» из зелёного прогона был бы ложным покрытием |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-15 | Repudiation | оптимистичный экран при неответившем сервере | high | mitigate | Текст плашки `htmx-failure-network` дополняется предложением о возможном расхождении экрана с сервером и о том, что обновление покажет настоящее состояние (D-16). Утверждается `test_the_network_banner_names_the_screen_server_divergence` с ожиданием, выписанным строкой в тесте, а не выбранным из проверяемого файла |
| T-9-16 | Tampering | текст плашки как канал разметки | medium | mitigate | Текст остаётся параметром макроса `alert(...)` и проходит обычное автоэкранирование; `\|safe` и `Markup(` в файле равны нулю (GATE-07); блок `<script>` не правится |
| T-9-17 | Repudiation | объявление успеха фазы по зелёной суите | high | mitigate | `09-UAT.md` называет пункты 3 и 6 поимённо, а четыре поведения объявлены в планах `verification: backstop` — при верификации без явного свидетельства они уходят в `human_needed`, а не проходят молча |
| T-9-18 | Information Disclosure | чтение тела чужого ответа в плашку | medium | accept | Уже закрыто Фазой 8: `test_the_failure_banner_touches_no_markup_sink` запрещает `responseText`; план `<script>` не трогает и это свойство сохраняет |
| T-9-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План правит один шаблон, один тестовый файл и один документ; установок нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_shell.py -q` — зелено.
- `uv run pytest tests/ -q` — полный прогон; единственный допустимый красный —
  `full-suite-ads-editor-order-pollution` (`.planning/todos/pending/`).
- `09-UAT.md` существует и проходит проверку `<automated>` задачи 2.
- ⚠️ **Обход не исполняется исполнителем плана.** Артефакт создаётся как процедура; закрытие
  пунктов 3, 6 и частично 5 — предмет `/gsd-verify-work` и человека (`human_verify_mode:
  end-of-phase`).
</verification>

<success_criteria>
1. Плашка обрыва связи говорит о расхождении экрана с сервером одной добавленной строкой и не
   повторяет «попробуйте ещё раз».
2. Третьего встроенного обработчика событий htmx в шаблонах не появилось.
3. `09-UAT.md` существует, называет пункты 3 и 6 поимённо и разбивает пункт 3 не менее чем на
   четыре проверки.
4. Клавиатурная проверка возврата фокуса записана с ожидаемым ответом «нет» и тремя названными
   выходами, третий из которых помечен негодным.
5. Раздел допущений называет A1, A2, A3, A5; раздел исключений называет пункт 4 как предмет
   Фазы 10.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-04-SUMMARY.md` when done.

Сводка обязана привести добавленное предложение плашки дословно и назвать, что обход `09-UAT.md`
исполнителем **не проводился** — артефакт создан как процедура.
</output>
