---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 20
type: execute
wave: 4
depends_on: ["17", "18", "19"]
gap_closure: true
files_modified:
  - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: false
requirements: [FORM-02, FORM-09, QUAL-01, QUAL-02, QUAL-06]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC4 (своп произошёл ИМЕННО в тот элемент, внеполосный счётчик приземлился куда задумано, фокус после подмены вернулся на элемент с тем же `id`) наблюдается ГЛАЗОМ на СЕГОДНЯШНЕМ дереве: роадмап называет прямо — сервером это не доказуемо в принципе, httpx не свопает и не собирает внеполосные узлы"
    - "Прежние отметки обхода ОБЕСЦЕНЕНЫ И ЭТО НАЗВАНО ПЕРВЫМ, а не спрятано: 3.5-Б второго круга снята на механизме АБСОЛЮТНОГО СМЕЩЕНИЯ, который план 09-13 удалил из дерева ЦЕЛИКОМ; механизм `keyset` глазами не наблюдался НИ РАЗУ, а добавка `scroll-lock` и её починка — новая видимая поверхность, не наблюдённая никем"
    - "Прокрутка ПОСЛЕ успешного удаления — отдельный шаг обхода, а не следствие соседнего: именно её отсутствие и было блокером CR-01, и признак отказа МОЛЧАЛИВЫЙ (200, чистая консоль, честная линейка счётчика)"
    - "Ветка `HX-Location` обходится ОТДЕЛЬНО, потому что она не перезагружает документ: состояние элемента `<html>` после перехода наблюдаемо только браузером, и ни один серверный прогон его не видит"
    - "Мир «Alpine мёртв, htmx жив» обходится ОТДЕЛЬНО: серверно измерена только половина (пустое скрытое поле в ответе тумблера), а приземление на отфильтрованный список и экран «Группы не найдены» с кнопкой «Сбросить» (D-09) требуют управляемой поломки Alpine в браузере. Сам мир заведён связкой D-05 и D-07, и обход проверяет его как достижимое положение, а не как экзотику"
    - "Ветка «чужая или несуществующая группа» наблюдается как НЕОТЛИЧИМАЯ (D-13, D-04-A): человек, повторно отправивший форму или нажавший «назад», обязан получить тот же экран, что и при успехе, — и это наблюдается глазом, а не выводится из статуса"
    - "Отступление QUAL-01 от буквы «на каждой форме письма» (цель блокировки D-06) повторно НЕ пересматривается: решение владельца ветвью `accept-override` принято 2026-09-01 и записано дважды — в `09-VERIFICATION.md` и в `.planning/REQUIREMENTS.md`; четвёртый круг его наследует, а не переигрывает"
    - "Число регистраций обработчиков плашки отказа после нескольких переходов `HX-Location` замеряется В БРАУЗЕРЕ — ревизия назвала эту неопределённость прямо (WR-05), и суита её не снимает: она исполняет сценарий со стаб-узлом и подмены самого узла не воспроизводит"
    - "Свидетельства четвёртого круга набраны СПИСКОМ, а не таблицей: проверка неприкосновенности состояний сличает строки ТАБЛИЦЫ, и табличные строки с теми же идентификаторами требований покрасили бы собственный гейт плана — та же причина, по которой списочными набраны первый, второй и третий круги"
    - "Состояния требований этим кругом НЕ ПЕРЕВОДЯТСЯ НИ ОДНИМ СИМВОЛОМ, и это НЕ осторожность, а вывод из собственной истории фазы: перевод в `Complete` внутри незакрытой фазы делался ДВАЖДЫ и ДВАЖДЫ откатывался (коммиты `183511c` и `4fe89da`). Условие перевода — вердикт `passed` повторной верификации, а не закрытие разрывов исполнителем"
    - "Текущее состояние таблицы ПРОВЕРЯЕТСЯ МАШИННО, а не предполагается: все пять клеток Фазы 9 обязаны читаться `Gaps Found` на момент задачи, и расхождение с ожиданием останавливает работу вместо тихой правки"
    - "Летопись битых окон приводится в соответствие с деревом ИНСТРУМЕНТОМ, а не рукой: записи 12 и 13, чей предмет снят планом 09-13, закрываются через `gsd-tools windows fixed`, потому что счётчики шапки файла ведёт он"
    - "Устаревший `outstanding` во frontmatter `09-UAT.md` называет запись, которой в дереве больше нет; исправляется он ЛЕТОПИСНОЙ строкой, а не удалением: закрыть дефект удалением записи о нём — ровно то, чего фаза себе запретила"
    - "QUAL-06, поведенческая половина: фокус после переключения КЛАВИАТУРОЙ остаётся на элементе с тем же `id` — наблюдается заново, потому что строка группы третьим и четвёртым кругами правилась"
    - statement: "Повторная верификация согласится с записанными свидетельствами, потому что каждое опирается на тот же машинный признак и то же наблюдение, которые прочтёт она"
      verification: backstop
  artifacts:
    - ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md — раздел «Четвёртый круг закрытия» с тремя проверками, выписанными ожидаемыми признаками и таблицей отметок; исправленный летописной строкой frontmatter"
    - ".planning/REQUIREMENTS.md — подраздел «Четвёртый круг закрытия (планы 09-17 … 09-19)» СПИСКОМ: на каждое из пяти требований машинная проверка, наблюдённый признак и вердикт БЕЗ перевода состояния"
    - ".planning/WINDOWS.md — записи 12 и 13, закрытые инструментом со ссылкой на снявший их план"
  key_links:
    - "наблюдение глазом → вердикт SC4: рвётся — единственная половина критерия 4, которую сервер не доказывает, остаётся неисполненной, и Фаза 15 наследует её как факт"
    - "закрытый разрыв → перевод состояния: рвётся — требование объявлено выполненным внутри незакрытой фазы, и откат состояний повторяется ТРЕТИЙ раз"
    - "запись свидетельств → повторная верификация: рвётся — она читает состояния, за которыми не стоит воспроизводимого признака, и вынуждена либо поверить, либо откатить"
  # ⚠️ ФОРМА ЗАПИСЕЙ НИЖЕ — ЗАФИКСИРОВАННЫЙ ОБРАЗЕЦ ПРОЕКТА, А НЕ ДРЕЙФ ЭТОГО КРУГА.
  # Дескрипторы `status: flagged-unverified` и `verification: none` набираются так с прошлых
  # кругов фазы, и ПРИЁМКА ЧИТАЕТ ИХ ИМЕННО В ЭТОЙ ФОРМЕ. Descriptor-less форма общей
  # спецификации здесь СОЗНАТЕЛЬНО не применяется: разнобой между кругами хуже любой из двух форм.
  prohibitions:
    - statement: "столбец Status пяти требований Фазы 9 этим планом не переводится ни одним символом: условие перевода — вердикт passed повторной верификации"
      status: flagged-unverified
      verification: none
    - statement: "флажки в теле файла требований не трогаются: их снимает не план, а закрытие фазы"
      status: flagged-unverified
      verification: none
    - statement: "отметки первого, второго и третьего кругов обхода не переписываются: отметка, снятая с прежнего механизма, называется обесцененной, а не исправляется задним числом"
      status: flagged-unverified
      verification: none
    - statement: "09-VERIFICATION.md этим планом не правится вовсе: отчёт перегенерируется прогоном верификации"
      status: flagged-unverified
      verification: none
    - statement: "ни одного файла app/ и tests/ план не трогает: git diff --stat по обоим каталогам пуст"
      status: flagged-unverified
      verification: none
    - statement: "расхождение, найденное на обходе, записывается и работа останавливается — на ходу оно не чинится: формулировки правок круга уже приняты, и правка под находку есть предмет следующего решения"
      status: flagged-unverified
      verification: none
    - statement: "перевод состояний не опирается на блок рекомендаций отчёта верификации: ссылка на него не набирается ни в одном виде"
      status: flagged-unverified
      verification: none
    - statement: "планы 09-01 … 09-19 и их сводки не правятся, не перенумеровываются и не отменяются"
      status: flagged-unverified
      verification: none
    - statement: "долговых маркеров в артефактах не появляется"
      status: flagged-unverified
      verification: none
---

<objective>
Закрыть единственную половину фазы, которой не даёт ни один доступный проекту инструмент, — **эффект
в живом браузере**, — и свести бухгалтерию четвёртого круга, НЕ переводя ни одного состояния.

Purpose: отчёт верификации оставил `behavior_unverified: 1` (SC4) и **три** пункта человеческой
проверки. Все три названы им поимённо и все три обходятся ЗАНОВО, потому что прежние отметки
описывают ДРУГОЕ ДЕРЕВО:

1. **`09-UAT.md` 3.1 / 3.2 / 3.3-Б и ЗАНОВО 3.5 на механизме `keyset`, плюс новый шаг — прокрутить
   страницу ПОСЛЕ успешного удаления и долистать до сентинела.** Отметка 3.5-Б второго круга снята
   на механизме АБСОЛЮТНОГО СМЕЩЕНИЯ, который план 09-13 удалил из дерева целиком; `keyset` глазами
   не наблюдался ни разу. Прокрутка после удаления — предмет CR-01 четвёртого круга и до плана 09-17
   не работала вовсе.
2. **Ветка `HX-Location`: удалить ПОСЛЕДНЮЮ строку отфильтрованной выдачи и попробовать прокрутить
   пришедший экран.** Ветка документ НЕ перезагружает (прочитано по вендоренному рантайму 2.0.10),
   поэтому состояние элемента `<html>` переживает переход и наблюдаемо только браузером. Тем же
   шагом замеряется остаточная неопределённость WR-05 — число регистраций обработчиков плашки
   после нескольких переходов.
3. **Мир «Alpine мёртв, htmx жив»: отключить Alpine, открыть `?search=<строка>`, переключить
   тумблер строки, затем удалить её.** Серверно измерена только половина (пустое скрытое поле в
   ответе тумблера); приземление на ОТФИЛЬТРОВАННЫЙ список и экран «Группы не найдены» с кнопкой
   «Сбросить» требуют управляемой поломки Alpine в браузере.

Output: раздел четвёртого круга артефакта обхода с выписанными ожидаемыми признаками и заполненной
таблицей отметок; четвёртый круг свидетельств СПИСКОМ; летопись битых окон, приведённая в
соответствие с деревом.

⚠️ **СОСТОЯНИЯ ТРЕБОВАНИЙ ЭТИМ КРУГОМ НЕ ПЕРЕВОДЯТСЯ, И ЭТО ВЫВОД ИЗ СОБСТВЕННОЙ ИСТОРИИ ФАЗЫ, А НЕ
ОСТОРОЖНОСТЬ.** Перевод пяти клеток в `Complete` внутри незакрытой фазы делался ДВАЖДЫ и ДВАЖДЫ
откатывался — коммитами `183511c` и `4fe89da`. Второй откат случился потому, что вердикт FORM-02
опирался поимённо на закрытие WR-03, которое дерево отменило двумя коммитами позже. Условие
перевода — вердикт `passed` ПОВТОРНОЙ ВЕРИФИКАЦИИ, а не суждение исполнителя о том, что разрывы
закрыты. Образец уже есть в этой же фазе: план 09-12 записал второй круг свидетельств и не тронул
состояний ни одним символом.

⚠️ **Волна 4, `depends_on: ["17", "18", "19"]`.** Наблюдение снимается с ОТГРУЖЕННОГО дерева:
обход, проведённый до починок, описывал бы не то дерево — ровно та ошибка, из-за которой
понадобился второй круг.

⚠️ **Что этот план НЕ трогает.** Ни одного файла `app/` и `tests/`. CR-01 — план 09-17, CR-02 и
WR-01, WR-02 — план 09-18, WR-04 и WR-05 — план 09-19. `09-VERIFICATION.md` не правится вовсе.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-REVIEW.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-12-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-17-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-18-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-19-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Имена и разделы, которые этот план ЗАВОДИТ (в дереве их сегодня нет):

**`.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md`:**

- раздел «Четвёртый круг закрытия» с тремя проверками — **4.1** (повторный обход 3.1 / 3.2 / 3.3-Б /
  3.5 на механизме `keyset` плюс прокрутка ПОСЛЕ удаления), **4.2** (ветка `HX-Location` плюс замер
  числа регистраций обработчиков плашки), **4.3** (мир «Alpine мёртв, htmx жив»);
- таблица отметок четвёртого круга (пустая после задачи 1, заполненная после задачи 2);
- ключ frontmatter `walkthrough_4` с датой, браузером, ОС, перечнем проверок и наблюдёнными
  величинами;
- летописная строка при ключе `outstanding`: названная им запись
  `INCLUDE_TARGET_EXCEPTIONS['group-list-sentinel']` планом 09-13 ЗАКРЫТА, а не отложена, и в дереве
  тестов её больше нет (проверено грепом верификатором).

**`.planning/REQUIREMENTS.md`:**

- подраздел «Четвёртый круг закрытия (планы 09-17 … 09-19)» внутри раздела «Фаза 9 — свидетельства
  к пересмотру состояний», набранный СПИСКОМ.

**`.planning/WINDOWS.md`:** записи 12 и 13 переводятся в `fixed` ИНСТРУМЕНТОМ
(`gsd-tools windows fixed`), счётчики шапки пересчитывает он же.

⚠️ **Матрица покрытия внешнего API не заводится:** детерминированный обход области фазы вернул
`detected: false`. ⚠️ **Останов допущений архитектуры не сработал** (`detected: false`). ⚠️ **Гейт
схемы БД молчит:** плана не касается ни одна ORM-модель. ⚠️ **Пакетов план не устанавливает.**
</artifacts_this_phase_produces>

<flagged_assumptions>
## Допущения, оставленные ОТКРЫТЫМИ (обход рёбер, спец-заготовки нет)

Все ШЕСТЬ пунктов детерминированного обхода рёбер разобраны планами круга поимённо: два ребра
QUAL-01 обращены в утверждения планами 09-17 и 09-18 (`idempotency` — `explicit`, `concurrency` —
`backstop`); три `unclassified` (FORM-02, FORM-09, QUAL-06) названы открытыми планом 09-17; шестое
(`QUAL-02 / unclassified`) названо открытым планом 09-19. Молчаливо не выброшен ни один, и настоящий
план новых рёбер не заводит: он не правит ни строки кода.

Этот план ЗАКРЫВАЕТ ПО-ЧЕЛОВЕЧЕСКИ ту половину, которую рёбра не покрывают вовсе, — **эффект в
браузере**. Она не является ребром обхода и не подменяет ни одно из них: наблюдение не делает
машинного признака, а машинный признак не делает наблюдения.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Задача 1: три проверки четвёртого круга ВЫПИСАНЫ с ожидаемым признаком у каждой, и обесцененные отметки названы, а не исправлены</name>

  <files>.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md</files>

  <read_first>
- `09-UAT.md` ЦЕЛИКОМ — frontmatter (`walkthrough`, `walkthrough_2`, `outstanding`), §«Как привести
  стенд в нужное состояние», §«Как убрать стенд», пункты 3.1, 3.2, 3.3-Б, 3.5, 5.3, 6.x, 7.x и
  таблицы отметок обоих кругов: **это образец, который раздел четвёртого круга повторяет, а не
  изобретает заново**, включая форму «шаг → ожидаемый признак» и предупреждение «ЧТО ИЗМЕНИЛОСЬ В
  КОДЕ ПОСЛЕ ЭТОГО ОБХОДА».
- `09-VERIFICATION.md` §«Требуется человеческая проверка» — три пункта дословно, с их полями
  `test`, `expected`, `why_human`; и `behavior_unverified_items` целиком.
- `09-17-SUMMARY.md` — какая ветвь механизма блокировки прокрутки применена: от этого зависит, ЧТО
  именно считать верным исходом шага «прокрутить после удаления».
- `09-18-SUMMARY.md` — что именно теперь приезжает в ответе тумблера и куда приземляется путь без
  Alpine.
- `09-19-SUMMARY.md` — имя признака однократной регистрации: без него шаг замера в 4.2 не
  поставить.
- `09-12-PLAN.md`, задача 1 — **образец того, как круг проверок выписывается ДО обхода**, чтобы на
  обходе не приходилось решать, что считать успехом.
  </read_first>

  <action>
**1. Раздел «Четвёртый круг закрытия».** Завести раздел с тремя проверками. У КАЖДОГО шага —
собственная строка таблицы «шаг → ожидаемый признак», по образцу пунктов 3.1-3.5. Ожидаемый признак
выписывается ДО обхода: решать на обходе, что считать успехом, значит подгонять отметку.

- **4.1 — повторный обход на механизме `keyset` плюс прокрутка ПОСЛЕ удаления.** Шаги 3.1, 3.2,
  3.3-Б и ЗАНОВО 3.5 (с обязательной ВЫПИСКОЙ названия последней видимой строки — именно её не
  хватило первому кругу), затем НОВЫЙ шаг: удалить группу и попробовать прокрутить страницу,
  долистав до сентинела. Ожидаемое: строка перерисовывается на месте, список не дёргается, счётчик
  обновляется, фокус после переключения клавиатурой остаётся на элементе с тем же `id`, курсор не
  теряет и не задваивает строк, **и страница ПОСЛЕ удаления прокручивается, а сентинел
  срабатывает**. Отдельной строкой — проверка в DevTools: на элементе `<html>` после удаления нет
  признака блокировки прокрутки (имя признака выписать из `app/static/css/app.css`).
- **4.2 — ветка `HX-Location` плюс замер регистраций.** Удалить ПОСЛЕДНЮЮ строку отфильтрованной
  выдачи и попробовать прокрутить пришедший экран; проверить, что адрес приземления НЕСЁТ фильтр.
  Отдельным шагом — замер остаточной неопределённости WR-05: сделать несколько переходов внутри
  сессии и посмотреть, сколько раз зарегистрированы обработчики плашки отказа (признак
  однократности из сводки 09-19 стои́т на узле тела; посмотреть его наличие и то, что второго
  комплекта слушателей не появилось). Ожидаемое: экран прокручивается; адрес несёт фильтр; признак
  однократности на узле есть, и число регистраций не растёт.
- **4.3 — мир «Alpine мёртв, htmx жив».** Отключить Alpine (или сломать его загрузку), открыть
  `?search=<строка>`, переключить тумблер строки, затем удалить её. Ожидаемое: приземление на
  ОТФИЛЬТРОВАННЫЙ список; при удалении единственной найденной строки — экран «Группы не найдены» с
  подсказкой и кнопкой «Сбросить», а не пустая карточка без выхода. Отдельной строкой — посмотреть
  в разметке ответа тумблера, что скрытое поле строки поиска несёт ту же строку, а не пустую.

**2. Пустая таблица отметок четвёртого круга** с колонками: проверка, наблюдённый признак,
расхождение. Ни одной заполненной ячейки — их ставит человек в задаче 2.

**3. Предупреждение, которое читается ПЕРВЫМ.** Над разделом — абзац: отметки второго круга
описывают дерево ДО третьего и четвёртого; 3.5-Б снята на механизме АБСОЛЮТНОГО СМЕЩЕНИЯ, снятом
планом 09-13, и обесценена ЦЕЛИКОМ; механизм `keyset` глазами не наблюдался ни разу; добавка
`scroll-lock` и её починка планом 09-17 — новая видимая поверхность. Отметки прежних кругов НЕ
переписываются: отметка, снятая с прежнего механизма, называется обесцененной, а не исправляется
задним числом.

**4. Frontmatter.** Завести ключ `walkthrough_4` с пустыми полями под дату, браузер, ОС, перечень
проверок и наблюдённые величины. Ключ `outstanding` НЕ удалять: дописать к нему летописную строку —
названная им запись `INCLUDE_TARGET_EXCEPTIONS['group-list-sentinel']` планом 09-13 ЗАКРЫТА записью
летописи, а не отложена, и в дереве тестов её больше нет (проверено грепом верификатором).
Удаление ключа было бы закрытием дефекта удалением записи о нём.

**5. Стенд.** Сослаться на существующие §«Как привести стенд в нужное состояние» и §«Как убрать
стенд», а не переписывать их. Назвать ДОПОЛНИТЕЛЬНОЕ требование к стенду четвёртого круга: аккаунт
с числом групп БОЛЬШЕ размера страницы (иначе шаг «долистать до сентинела» доказывает пустоту
пустого) и группа с уникально находимым названием для 4.3.
  </action>

  <verify>
    <automated>S=$(grep -c "^## Четвёртый круг закрытия\|^### 4\.[123]" .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md); echo "заголовков раздела и проверок: $S"; [ "$S" -ge 4 ]</automated>
    <automated>W=$(grep -c "^walkthrough_4:" .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md); echo "ключей четвёртого обхода: $W"; [ "$W" = "1" ]</automated>
    <automated>O=$(grep -c "outstanding" .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md); echo "вхождений ключа outstanding: $O"; [ "$O" -ge 1 ]</automated>
    <automated>C=$(git diff --stat app/ tests/ | wc -l); echo "изменённых файлов app/ и tests/: $C"; [ "$C" = "0" ]</automated>
    <automated>R=$(git diff -U0 .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md | grep -c "^-.*2026-08-3[01] | Chrome"); echo "удалённых строк отметок прежних кругов: $R"; [ "$R" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
- В `09-UAT.md` заведён раздел «Четвёртый круг закрытия» и три подраздела `4.1`, `4.2`, `4.3` — `grep -c "^## Четвёртый круг закрытия\|^### 4\.[123]" …/09-UAT.md` → значение не меньше `4`.
- У каждого шага каждой из трёх проверок выписан ОЖИДАЕМЫЙ ПРИЗНАК отдельной ячейкой таблицы; ни одного шага без ожидания.
- Проверка `4.1` несёт шаг с ВЫПИСКОЙ названия последней видимой строки и отдельный шаг «прокрутить страницу ПОСЛЕ удаления и долистать до сентинела».
- Проверка `4.2` несёт шаг проверки адреса приземления НА ФИЛЬТР и шаг замера числа регистраций обработчиков плашки.
- Проверка `4.3` несёт шаг «удалить единственную найденную строку» с ожиданием экрана «Группы не найдены» и кнопки «Сбросить».
- Таблица отметок четвёртого круга заведена и ПУСТА: ни одной заполненной ячейки.
- Frontmatter несёт `walkthrough_4:` — `grep -c "^walkthrough_4:" …/09-UAT.md` → `1`.
- Ключ `outstanding` не удалён и снабжён летописной строкой о том, что названная им запись закрыта планом 09-13.
- Отметки первого, второго и третьего кругов не изменены: `git diff -U0 …/09-UAT.md | grep -c "^-.*2026-08-3[01] | Chrome"` → `0`.
- `git diff --stat app/ tests/` пуст.
  </acceptance_criteria>

  <done>Три проверки четвёртого круга выписаны с ожидаемым признаком у каждого шага; обесцененность
прежних отметок названа, а не исправлена; таблица отметок готова к заполнению человеком.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Задача 2: НАБЛЮДЕНИЕ — три проверки четвёртого круга на отгруженном дереве</name>

  <precondition>Приложение поднято и отвечает на `/accounts` (`just run`, порт 8000); правки планов
09-17, 09-18 и 09-19 в рабочем дереве, и суита зелена целиком
(`uv run pytest tests/ -q -p no:randomly`); в базе посеян стенд с префиксом `UAT9-` командой из
§«Как привести стенд в нужное состояние» — аккаунт с числом групп БОЛЬШЕ размера страницы и группа с
уникально находимым названием.</precondition>

  <read_first>
- `09-UAT.md` раздел «Четвёртый круг закрытия» (заведён задачей 1) — три проверки с выписанными
  ожидаемыми признаками и пустая таблица отметок; §«Как привести стенд в нужное состояние» и §«Как
  убрать стенд».
- `09-UAT.md` предупреждение над разделом — почему отметки прежних кругов описывают другое дерево и
  переписыванию не подлежат.
- `09-17-SUMMARY.md` — какая ветвь механизма блокировки прокрутки применена и как именно она
  наблюдаема в DevTools; `09-19-SUMMARY.md` — имя признака однократной регистрации.
  </read_first>

  <action>Пройти три проверки раздела «Четвёртый круг закрытия» артефакта `09-UAT.md` и заполнить
таблицу отметок четвёртого круга наблюдённым признаком.</action>

  <instructions>
Всё, что можно было автоматизировать, автоматизировано: обе починки шипнуты, оба поведенческих
правила зелены исполненным переходом цвета, суита зелена целиком. Остаётся ровно то, чего не даёт ни
один доступный проекту инструмент, — **эффект в живом браузере**. Браузерного стенда в проекте нет и
не заводится: суита идёт транспортом ASGI и утверждает ДОСТАВКУ разметки и заголовков, а не то, что
документ прокручивается и что фокус куда-то вернулся.

**Почему круг четвёртый, а не повтор от занудства.** Три из четырёх сегодняшних отметок описывают
механизм, которого на экране больше нет: 3.5-Б снята на АБСОЛЮТНОМ СМЕЩЕНИИ, удалённом планом
09-13; `keyset` глазами не наблюдался ни разу; добавка `scroll-lock` и её починка не наблюдались
никем. Отметка, снятая с исчезнувшего механизма, — это не «почти закрыто», это описание другого
дерева.

Пройти по артефакту
`.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md`, раздел «Четвёртый
круг закрытия»:

- **4.1 — МЫШЬЮ 3.3-Б НЕ ДЕЛАЕТСЯ.** Tab до чекбокса (видна рамка фокуса), Space, и, **не трогая
  мышь**, в консоли выполнить `document.activeElement`. Записать ФАКТИЧЕСКИЙ ответ и чем оказался
  активный элемент. Затем 3.5 ЗАНОВО: **выписать название последней видимой строки ДОСЛОВНО прямо в
  отметку**, удалить строку из первой трети, долистать до низа, сверить список с самим собой.
  Затем НОВЫЙ шаг, ради которого круг и собран: удалить группу и **попробовать прокрутить
  страницу**, долистав до сентинела. Записать, прокручивается ли, и что показывает DevTools на
  элементе `<html>` — есть ли на нём признак блокировки прокрутки.
- **4.2 — ветка `HX-Location`.** Удалить ПОСЛЕДНЮЮ строку отфильтрованной выдачи. Записать: (а)
  прокручивается ли пришедший экран; (б) **адрес в строке браузера дословно** — несёт ли он фильтр.
  Затем несколько переходов внутри сессии и замер: стои́т ли на узле тела признак однократности и не
  появилось ли второго комплекта слушателей. Записать наблюдение, а не оценку.
- **4.3 — мир «Alpine мёртв, htmx жив».** Отключить Alpine (заблокировать его загрузку в DevTools →
  Network → Block request URL — или иным управляемым способом; способ ЗАПИСАТЬ). Открыть
  `?search=<строка>`, находящую РОВНО ОДНУ группу; переключить тумблер строки; затем удалить её.
  Записать: **адрес приземления дословно** и что показывает экран — «Группы не найдены» с кнопкой
  «Сбросить» или пустая карточка. Отдельно: в разметке ответа тумблера посмотреть значение скрытого
  поля строки поиска и записать его ДОСЛОВНО (ожидается та же строка, а не пустая).

**После обхода — УБРАТЬ СТЕНД** командой из §«Как убрать стенд» и привести вывод команды проверки
уборки.

**Куда пишется ответ.** В таблицу отметок четвёртого круга: дата, браузер и ОС, и по каждой
проверке — наблюдённый признак, а не слово «ок». Подпись под непроверенным Фаза 15 унаследует как
факт (требование GATE-09 — сводное закрытие всех девяти пунктов ручного UAT). Те же величины
продублировать в ключ `walkthrough_4` frontmatter.

**Расхождение на обходе — что делать.** Записать его и ОСТАНОВИТЬСЯ, а не чинить на ходу:
формулировки правок круга уже приняты, и правка под находку — предмет следующего решения, а не
этого останова. Если расхождение относится к прокрутке после удаления, это означает, что CR-01 не
закрыт, и работа круга не завершена, — записать это прямым текстом.

**Чего делать НЕ надо.** Не переписывать отметки прежних кругов, не менять состояния требований, не
трогать `09-VERIFICATION.md`, не править ни одного файла `app/` и `tests/`.
  </instructions>

  <verification>После возобновления исполнитель проверяет машинно: таблица отметок четвёртого круга
непуста; в ней есть дата, браузер и ОС; заполнены все ячейки трёх проверок; ячейка 4.1 несёт
выписанное название строки И ответ о прокрутке после удаления; ячейка 4.2 несёт дословный адрес
приземления; ячейка 4.3 несёт дословный адрес приземления, наблюдённый экран и дословное значение
скрытого поля; слова «ок» в качестве единственного содержимого ячейки нет; строк с префиксом
`UAT9-` в базе не осталось; `git diff --stat app/ tests/` пуст.</verification>

  <resume-signal>Заполнить таблицу отметок четвёртого круга в `09-UAT.md` и ключ `walkthrough_4`,
убрать стенд и написать «четвёртый круг проведён», указав: ответ 3.3-Б («да» / «нет» и чем оказался
активный элемент), выписанное название строки 3.5, **прокручивается ли страница после удаления и
есть ли на `<html>` признак блокировки**, адрес приземления ветки `HX-Location` дословно, наблюдение
о числе регистраций обработчиков плашки, адрес приземления и экран мира «Alpine мёртв, htmx жив» и
дословное значение скрытого поля строки поиска в ответе тумблера.</resume-signal>

  <acceptance_criteria>
- Таблица отметок четвёртого круга несёт строку данных с датой, браузером и ОС, и все ячейки трёх проверок заполнены.
- Ячейка 4.1 несёт: слово «да» либо «нет» по 3.3-Б И чем оказался активный элемент; ВЫПИСАННОЕ название строки; прямой ответ, прокручивается ли страница после удаления; наблюдение по элементу `<html>`.
- Ячейка 4.2 несёт ДОСЛОВНЫЙ адрес приземления и прямой ответ, прокручивается ли пришедший экран, плюс наблюдение о числе регистраций обработчиков плашки.
- Ячейка 4.3 несёт способ отключения Alpine, ДОСЛОВНЫЙ адрес приземления, наблюдённый экран и ДОСЛОВНОЕ значение скрытого поля строки поиска в ответе тумблера.
- Ни одна ячейка не состоит из слова «ок».
- Ключ `walkthrough_4` frontmatter заполнен теми же величинами.
- Отметки прежних кругов не изменены: `git diff -U0 …/09-UAT.md | grep -c "^-.*2026-08-3[01] | Chrome"` → `0`.
- Стенд убран: вывод команды проверки уборки показывает ноль строк с префиксом `UAT9-`.
- `git diff --stat app/ tests/` пуст.
- Расхождение, если оно найдено, записано в колонку «расхождение» и НЕ починено на ходу.
  </acceptance_criteria>

  <done>Три проверки четвёртого круга пройдены на отгруженном дереве, и таблица отметок несёт
наблюдённый признак по каждой — включая прямой ответ на вопрос, прокручивается ли страница после
удаления. Стенд убран.</done>
</task>

<task type="auto">
  <name>Задача 3: четвёртый круг свидетельств записан СПИСКОМ; состояния требований не тронуты ни одним символом; летопись битых окон приведена в соответствие с деревом</name>

  <files>.planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>

  <precondition>Задача 2 завершена: таблица отметок четвёртого круга заполнена, стенд убран.</precondition>

  <read_first>
- `.planning/REQUIREMENTS.md` §«Фаза 9 — свидетельства к пересмотру состояний» ЦЕЛИКОМ — первый,
  второй и третий круги: **это образец, который четвёртый повторяет**, включая форму «машинная
  проверка → наблюдённый признак → вердикт» и абзац о том, почему круги набраны СПИСКОМ, а не
  таблицей.
- `.planning/REQUIREMENTS.md` §«Traceability» — таблица состояний: пять строк Фазы 9 читаются
  СЕГОДНЯ, а не предполагаются по памяти.
- `09-17-SUMMARY.md`, `09-18-SUMMARY.md`, `09-19-SUMMARY.md` — какие правила заведены, какие
  переходы цвета исполнены и с какими кодами; **числа берутся из ПРОГОНОВ, снятых заново, а не
  переписываются из сводок**.
- `09-UAT.md` таблица отметок четвёртого круга — наблюдённые признаки.
- `.planning/WINDOWS.md` записи 12, 13, 14 — что именно в них записано и чем закрыт предмет каждой.
- `09-12-PLAN.md`, задача 3 — **образец круга свидетельств БЕЗ перевода состояний**.
  </read_first>

  <action>
**1. Текущее состояние таблицы ПРОВЕРЯЕТСЯ, а не предполагается.** Прочитать пять строк Фазы 9
столбца `Status` и убедиться, что все пять читаются `Gaps Found`. Расхождение с ожиданием
ОСТАНАВЛИВАЕТ работу и записывается прямым текстом: тихая правка таблицы под ожидание есть ровно тот
класс, за который фаза уже дважды платила откатом.

**2. Подраздел «Четвёртый круг закрытия (планы 09-17 … 09-19)» СПИСКОМ.** Набирается СПИСКОМ, а не
таблицей: проверка неприкосновенности состояний сличает строки ТАБЛИЦЫ, и табличные строки с теми же
идентификаторами требований покрасили бы собственный гейт плана. На каждое из пяти требований —
три позиции:

- **машинная проверка** — команда `uv run pytest …` с `-k`-выборкой правил круга и её вывод, снятые
  ЗАНОВО на сегодняшнем дереве вместе с HEAD, на котором сняты. Сводка описывает дерево на момент
  своего плана, свидетельство обязано описывать дерево на момент записи;
- **наблюдённый признак** — отметка четвёртого круга (или прочерк с названной причиной, если
  наблюдения у требования нет по существу, как у FORM-09);
- **вердикт** — что именно закрыто и чем; для FORM-02 назвать ОБА держателя (CR-01 и CR-02) и
  сказать, чем каждый закрыт и каким переходом цвета это воспроизводится.

**3. Отдельной позицией — два пункта `gaps` отчёта.** По образцу третьего круга: чем закрыт каждый,
какими правилами утверждается, какой командой воспроизводится и каким переходом цвета (с кодами и
SHA). Для CR-01 назвать прямо, что правило ПОВЕДЕНЧЕСКОЕ — оно исполняет объект `x-data`, — и что
разметочное на его месте зеленело бы вакуумом.

**4. Отдельной позицией — что закрытием НЕ стало.** Назвать поимённо: `DEF-09-03` (закрытие WR-05
третьего круга не держит ни одно правило; шесть литералов на соседних экранах), `DEF-09-04`
(вселенная гейта G-2 — только POST), `DEF-09-01`, `DEF-09-02` — все четыре маршрутизированы, ни один
не поглощён и ни один не забыт.

**5. ⚠️ СОСТОЯНИЯ НЕ ПЕРЕВОДЯТСЯ НИ ОДНИМ СИМВОЛОМ, И ПРИЧИНА ЗАПИСЫВАЕТСЯ ВИДИМОЙ СТРОКОЙ.**
Перевод в `Complete` внутри незакрытой фазы делался дважды и дважды откатывался (`183511c`,
`4fe89da`); во второй раз — потому что вердикт FORM-02 опирался поимённо на закрытие, которое дерево
отменило двумя коммитами позже. Условие перевода — вердикт `passed` ПОВТОРНОЙ ВЕРИФИКАЦИИ. Записать
это абзацем в шапке подраздела, чтобы следующий читатель не счёл непереведённые клетки забытой
работой. Флажки в теле файла не трогать.

⚠️ **Ссылка на блок рекомендаций отчёта верификации не набирается ни в одном виде.** Свидетельство
опирается на ЗАКРЫТЫЕ РАЗРЫВЫ и зелёные прогоны, а не на то, что отчёт посоветовал.

**6. Летопись битых окон — ИНСТРУМЕНТОМ, а не рукой.** Счётчики шапки `.planning/WINDOWS.md`
(`open_count`, `fixed_count`) ведёт инструмент, и ручная правка таблицы разошлась бы с ними молча.
Закрыть записи, чей предмет снят:

- запись **12** (строка консоли `hx-include` после правки плана 09-11) — предмет снят планом 09-13
  вместе с объявлением включаемых данных;
- запись **13** (`INCLUDE_TARGET_EXCEPTIONS['group-list-sentinel']`) — предмет снят тем же планом;
  верификатор проверил грепом, что записи в дереве тестов больше нет.

Команда: `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 12` и то же для `13`. Запись **14**
(DEF-09-01) НЕ трогается: её предмет жив и маршрутизирован «вне фазы». Вывод обеих команд и
получившийся `open_count` привести в сводке.

**7. Ни одного файла `app/` и `tests/` задача не трогает.** `git diff --stat app/ tests/` обязан
быть пуст — это её собственный машинный критерий.
  </action>

  <verify>
    <automated>G=$(grep -E "^\| (FORM-02|FORM-09|QUAL-01|QUAL-02|QUAL-06) \| Phase 9 \|" .planning/REQUIREMENTS.md | grep -c "Gaps Found"); echo "клеток Фазы 9 в состоянии Gaps Found: $G"; [ "$G" = "5" ]</automated>
    <automated>S=$(grep -c "Четвёртый круг закрытия" .planning/REQUIREMENTS.md); echo "подразделов четвёртого круга: $S"; [ "$S" -ge 1 ]</automated>
    <automated>T=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^[-+]\| (FORM|QUAL|FOUND|GATE|PAY|FETCH|SIGN|EDIT|UPLD|E2E)-[0-9]+ \|"); echo "тронутых строк ТАБЛИЦЫ состояний: $T"; [ "$T" = "0" ]</automated>
    <automated>B=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^[-+]- \[[ x]\] \*\*"); echo "тронутых флажков в теле файла: $B"; [ "$B" = "0" ]</automated>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs windows status | head -20</automated>
    <automated>C=$(git diff --stat app/ tests/ | wc -l); echo "изменённых файлов app/ и tests/: $C"; [ "$C" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
- Все пять клеток Фазы 9 читаются `Gaps Found` ДО и ПОСЛЕ задачи: `grep -E "^\| (FORM-02|FORM-09|QUAL-01|QUAL-02|QUAL-06) \| Phase 9 \|" .planning/REQUIREMENTS.md | grep -c "Gaps Found"` → `5`.
- Ни одна строка ТАБЛИЦЫ состояний не изменена: `git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^[-+]\| (FORM|QUAL|FOUND|GATE|PAY|FETCH|SIGN|EDIT|UPLD|E2E)-[0-9]+ \|"` → `0`.
- Ни один флажок в теле файла не изменён: `git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^[-+]- \[[ x]\] \*\*"` → `0`.
- Подраздел «Четвёртый круг закрытия (планы 09-17 … 09-19)» заведён и набран СПИСКОМ: строк вида `| ID | Phase 9 |` в нём нет.
- У каждого из пяти требований в подразделе есть три позиции: машинная проверка с КОМАНДОЙ и ВЫВОДОМ, наблюдённый признак (или прочерк с причиной), вердикт.
- Все приведённые прогоны сняты ЗАНОВО на сегодняшнем дереве, и HEAD, на котором они сняты, назван.
- Отдельными позициями записаны: два пункта `gaps` (чем закрыт каждый, каким переходом цвета, с кодами и SHA) и четыре маршрутизированных пункта (`DEF-09-01` … `DEF-09-04`).
- В шапке подраздела видимой строкой записано, почему состояния НЕ переведены, со ссылкой на оба отката (`183511c`, `4fe89da`).
- Записи 12 и 13 `.planning/WINDOWS.md` переведены в `fixed` ИНСТРУМЕНТОМ; вывод обеих команд и получившийся `open_count` приведены в сводке. Запись 14 не тронута.
- `git diff --stat app/ tests/` пуст.
- Долговых маркеров в изменённых файлах нет.
  </acceptance_criteria>

  <done>Четвёртый круг свидетельств записан списком, с прогонами, снятыми заново, и наблюдениями
четвёртого обхода; состояния требований не тронуты ни одним символом, и причина названа видимой
строкой; летопись битых окон приведена в соответствие с деревом инструментом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| человек → живая база разработчика | посев стенда идёт в базу, где рядом лежат собственные аккаунты и группы владельца |
| браузер → документ | наблюдаемое состояние элемента `<html>` и число слушателей на узле тела — единственный источник истины для трёх проверок |
| исполнитель → бухгалтерия требований | перевод состояния есть утверждение о выполненности, которое наследуют Фазы 10-15 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-20-01 | Repudiation | подпись под непроверенным наблюдением | high | mitigate | каждая ячейка отметки обязана нести НАБЛЮДЁННЫЙ ПРИЗНАК, а не слово «ок»; выписанное название строки, дословный адрес приземления и дословное значение скрытого поля проверяются машинно после возобновления. Требование GATE-09 наследует эти отметки как факт |
| T-09-20-02 | Tampering | тихая правка таблицы состояний под ожидание | high | mitigate | состояния не переводятся ни одним символом, и это утверждается ДВУМЯ машинными критериями (`git diff -U0` по строкам таблицы и по флажкам); текущее состояние ПРОВЕРЯЕТСЯ до записи, а расхождение останавливает работу |
| T-09-20-03 | Information disclosure | посев стенда в живую базу разработчика | medium | mitigate | всё посеянное несёт общий префикс `UAT9-`; §«Как убрать стенд» исполняется после обхода, и вывод команды проверки уборки — критерий приёмки. Механизм не заводится этим планом, а наследуется от круга 2 |
| T-09-20-04 | Tampering | ручная правка счётчиков летописи битых окон | medium | mitigate | записи закрываются ИНСТРУМЕНТОМ (`gsd-tools windows fixed`), который ведёт счётчики шапки; ручная правка таблицы разошлась бы с ними молча |
| T-09-20-05 | Repudiation | правка отметок прежних кругов задним числом | medium | mitigate | отметки прежних кругов не переписываются; число удалённых строк отметок утверждается критерием и равно нулю. Отметка, снятая с прежнего механизма, называется обесцененной, а не исправляется |
| T-09-20-06 | Elevation of privilege | наблюдение проводится с правами владельца в живой базе | low | accept | обход ведёт сам владелец в своём окружении; ни одного нового пути доступа план не открывает и ни одной строки кода не правит |
| T-09-20-SC | Tampering | npm/pip/cargo installs | low | accept | план не выполняет ни одной команды менеджера пакетов |

**Блокирующий порог — `high`, уровень ASVS — L1.** Обе угрозы этого уровня (T-09-20-01,
T-09-20-02) несут диспозицию `mitigate` с названным механизмом и проверяемым критерием; ни одна не
принята.
</threat_model>

<verification>
- Три проверки четвёртого круга выписаны с ожидаемым признаком у каждого шага ДО обхода.
- Таблица отметок четвёртого круга заполнена наблюдённым признаком по каждой проверке, включая
  прямой ответ на вопрос, прокручивается ли страница ПОСЛЕ удаления.
- Четвёртый круг свидетельств записан СПИСКОМ, с прогонами, снятыми заново, и названным HEAD.
- Ни одна клетка столбца `Status` и ни один флажок не изменены; причина названа видимой строкой.
- Записи 12 и 13 летописи битых окон закрыты инструментом; запись 14 не тронута.
- `git diff --stat app/ tests/` пуст.
</verification>

<success_criteria>
- SC4 и три пункта человеческой проверки наблюдены на СЕГОДНЯШНЕМ дереве, а не унаследованы с
  прежнего механизма.
- Бухгалтерия круга сведена без единого перевода состояния — и это записано как решение, а не
  выглядит как забытая работа.
- Летопись битых окон описывает дерево, которое есть, а не то, которое было.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-20-SUMMARY.md` when done
</output>
