---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 10
subsystem: ui
tags: [htmx, jinja2, gates, tdd, uat, templates]

# Dependency graph
requires:
  - phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
    provides: "макрос-обёртка form_wrapper, перечень DISABLED_ELT_EXCEPTIONS и идиома SP-1 (план 09-01); ветка option-b решения D-06, снявшая с вызова собственную цель блокировки (план 09-09)"
  - phase: 08-sloy-otveta-i-geyty-razmetki
    provides: "файл гейтов разметки, помощники подстановки дерева _tree_with / _all_templates и дисциплина отрицательных контролей"
provides:
  - "правило test_no_caller_declares_a_blocking_target_its_form_cannot_have — свойство «объявленная цель блокировки достижима в собственном слоте вызова» стало машинным"
  - "_form_wrapper_call_blocks — разборщик блочных вызовов макроса-обёртки: пары «аргументы, слот»"
  - "_offenders_unreachable_blocking_target — сканер вызывающих, объявивших недостижимую цель"
  - "UNREACHABLE_TARGET_CALL_BLOCKS_MEASURED — антивакуумная константа числа блоков вызова"
  - "условная печать цели блокировки в form_wrapper: атрибут при пустом значении не печатается вовсе"
  - "09-UAT.md пункт 7 «Строки консоли двух расхождений» с проверками 7.1 и 7.2 и ЗАПОЛНЕННОЙ таблицей отметок"
  - "наблюдённый факт: обе строки консоли (DIV-09-01 и DIV-09-02) существуют — Chrome / macOS, 2026-08-30"
affects: ["09-11 (DIV-09-01, тот же род расхождения)", "09-12 (QUAL-06 и QUAL-01, повторные измерения 3.3 и 6.2)", "Фаза 11 (раздача макроса сорока семи формам)", "Фаза 12 (FETCH-01)", "Фаза 15 (QUAL-04)"]

# Actuals (#2632) — та же шкала estimateTokens (chars/4), что и у сметы плана,
# по реализованному диффу ceeddb6~1..HEAD. Число НЕ округлено в сторону сметы.
actuals:
  tokens: 16179
  tasks: 4
  commits: 5

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "правило гейта доказывает СВОИ ЗУБЫ подстановкой в собственном теле, живое дерево при этом не правится (образец — test_the_indicator_class_is_self_sufficient)"
    - "антивакуумная константа рядом с каждым сканером: правило, потерявшее предмет разбора, обязано краснеть, а не зеленеть пустотой"
    - "«цели нет» передаётся ПУСТЫМ значением параметра, а не непередачей: непереданный параметр берёт умолчание, переданная пустота его отменяет"
    - "опора подстановки сдвигается ВМЕСТЕ с разметкой, которую она держит, и несёт ветвь шаблонизатора ЦЕЛИКОМ — иначе подстановка ломает баланс блоков"
    - "смена РОДА записи перечня SP-1 записывается строкой летописи наравне с изменением числа"

key-files:
  created: []
  modified:
    - tests/test_templates/test_htmx_markup_gates.py
    - app/templates/components/form_wrapper.html
    - app/templates/account_groups/includes/group_row.html
    - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md

key-decisions:
  - "Владелец выбрал ветвь `record-kind`: макрос перестаёт печатать цель блокировки при непереданном значении, вызов передаёт «цели нет», запись перечня меняет род. Основание владельца — тег макроса правится дешевле всего, пока его использует ОДНА форма"
  - "Вариант `real-target` отклонён владельцем: его измеренная цена — отмена QUAL-06 на пути тумблера (измерение 3.3, фактический ответ «нет») — не принимается"
  - "Вариант `defer` отклонён владельцем: строка остаётся в консоли на КАЖДОЕ переключение, то есть на самом частом действии экрана"
  - "Вариант `no-divergence` неприменим по факту: отметка 7.2 несёт наблюдённую строку, а не «строки не было»"
  - "Умолчание параметра `disabled_elt` НЕ ТРОНУТО — разметка остальных сорока шести форм вехи не двинулась ни на символ; правка ограничена одним вызывающим"
  - "Состояния требований и флажки .planning/REQUIREMENTS.md этим планом НЕ переводятся — прямой запрет плана; перевод состояний делает повторная верификация"

patterns-established:
  - "Наблюдение стоит ПЕРЕД правкой и не переставляется: правка снимает наблюдаемое явление, и отложенное наблюдение не состоялось бы уже никогда"
  - "Отметка обхода несёт СКОПИРОВАННУЮ строку консоли (с источником и стеком) либо прямое «строки не было»; слово «ок» отметкой не является"
  - "Подстановка отрицательного контроля ЗАМЕЩАЕТ переданный аргумент, а не дописывает второй: разборщик берёт ПЕРВОЕ вхождение, и дописанный второй позеленил бы контроль молча"

requirements-completed: [FORM-09, QUAL-01, QUAL-06]

coverage:
  - id: D1
    description: "Свойство «ни один вызывающий макроса-обёртки не объявляет цель блокировки, которой на его форме при живом рантайме нет» утверждается МАШИННО, правило краснеет на подставленном дереве и несёт антивакуумное утверждение"
    requirement: "FORM-09"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py#test_no_caller_declares_a_blocking_target_its_form_cannot_have"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py#test_control_negative_a_submit_target_behind_a_self_removing_wrapper_reddens_the_gate"
        status: pass
    human_judgment: false
  - id: D2
    description: "Форма тумблера больше не объявляет недостижимой цели блокировки: макрос печатает атрибут только при переданном непустом значении, вызов передаёт пустую цель"
    requirement: "FORM-09"
    verification:
      - kind: unit
        ref: "uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly (71 passed)"
        status: pass
      - kind: integration
        ref: "uv run pytest tests/ -q -p no:randomly (2650 passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Строка `The selector \"find button[type=submit]\" on hx-disabled-elt returned no matches!` УШЛА из консоли живого браузера после правки"
    requirement: "FORM-09"
    verification: []
    human_judgment: true
    rationale: "Строка есть свойство живого рантайма: 2650 тестов идут через транспорт ASGI и не исполняют ни строчки JS. ⚠️ ПОСЛЕ правки строка в браузере ПОВТОРНО НЕ НАБЛЮДАЛАСЬ — снятие расхождения утверждается машинным правилом, а не глазом. Повторный проход 7.2 обязателен на приёмке фазы: правило фазы «подпись под ненаблюдённым запрещена» действует и в эту сторону тоже"
  - id: D4
    description: "Обе строки консоли двух расхождений НАБЛЮДЕНЫ в живом браузере ДО правки, на отгруженном дереве, и записаны с датой, браузером и ОС"
    requirement: "FORM-09"
    verification:
      - kind: manual_procedural
        ref: ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md#Отметка о закрытии пункта 7"
        status: pass
    human_judgment: true
    rationale: "Наблюдение выполнено владельцем 2026-08-30 (Chrome / macOS) на HEAD ceeddb6 с нетронутым `app/`; обе строки скопированы дословно вместе с источником и стеком. Автоматизации этого шага в проекте нет и не заводится (E2E-01 отложено вехой), поэтому свидетельство остаётся человеческим по построению"
  - id: D5
    description: "Серверная идемпотентность обработчика тумблера правкой цели блокировки не тронута: инверсия `is_active` одной строкой под тройным ограничением на месте"
    requirement: "QUAL-01"
    verification:
      - kind: integration
        ref: "uv run pytest tests/test_pages/test_account_groups.py tests/test_templates/test_components.py -q -p no:randomly (162 passed)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Род записи перечня исключений цели блокировки сменён НЕ МОЛЧА: движение записано строкой летописи `2 → 2, Фаза 9, план 09-10` с причиной, объявленное число утверждается отдельной константой (идиома SP-1)"
    requirement: "QUAL-01"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py#test_every_disabled_elt_exception_is_actually_an_exception"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py#test_the_declared_number_of_disabled_elt_exceptions_is_the_real_one"
        status: pass
    human_judgment: false
  - id: D7
    description: "Решение о роде записи принято ВЛАДЕЛЬЦЕМ на наблюдённом факте и на измеренной цене варианта `real-target` (отмена QUAL-06 на этом пути), а не исполнителем"
    requirement: "QUAL-06"
    verification:
      - kind: manual_procedural
        ref: ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md#Исход решения владельца по 7.2"
        status: pass
    human_judgment: true
    rationale: "Выбор ветви — решение владельца по построению: правится поверхность, которую макрос раздаёт сорока семи формам вехи. Машиной такой выбор не проверяется; проверяется только то, что ответ процитирован дословно и применена ровно одна названная ветвь"
  - id: D8
    description: "QUAL-06 (возврат фокуса после подмены) на пути тумблера"
    requirement: "QUAL-06"
    verification: []
    human_judgment: true
    rationale: "Здесь закрыта только ПОЛОВИНА: измеренная цена варианта с настоящей блокировкой названа владельцу перед выбором, и выбранная ветвь QUAL-06 не задевает (блокировки не было и нет — фокусу теряться не на чем). ⚠️ Возврат фокуса на изменённой разметке в браузере НЕ ИЗМЕРЯЛСЯ ни разу — это предмет плана 09-12, проверка 3.3 ПОВТОРНО"

# Metrics
duration: 1h 33m
completed: 2026-08-30
status: complete
---

# Phase 09 Plan 10: Расхождение DIV-09-02 — от наблюдения глазом до машинного правила Summary

**Форма тумблера перестала объявлять цель блокировки, которой на ней при живом htmx нет: расхождение сначала НАБЛЮДЕНО в браузере, затем закрыто выбранной владельцем ветвью `record-kind`, а свойство «объявленная цель достижима в собственном слоте вызова» стало машинным правилом с доказанными зубами.**

## Performance

- **Duration:** 1h 33m
- **Started:** 2026-08-30T18:59:59Z (коммит задачи 1)
- **Completed:** 2026-08-30T20:32:45Z (коммит задачи 4)
- **Tasks:** 4 из 4 (две из них — остановы человека: наблюдение и решение)
- **Files modified:** 4

## Accomplishments

- **Расхождение стало ВИДИМЫМ обоими доступными проекту способами.** Машинное правило `test_no_caller_declares_a_blocking_target_its_form_cannot_have` покраснело на живом дереве и назвало нарушителя поимённо (`{'account_groups/includes/group_row.html': 'find button[type=submit]'}`); артефакт обхода получил пункт 7 с обеими выписанными ожидаемыми строками консоли.
- **Обе строки НАБЛЮДЕНЫ в живом браузере ДО единой правки** — Chrome / macOS, 2026-08-30, на отгруженном дереве (HEAD `ceeddb6`, `app/` не тронут). Чтение исходника при приёмке 09-09 подтвердилось глазом: РАСХ-2 существует, вариант `no-divergence` отпал по факту, а не по рассуждению.
- **Правка применена ровно та, которую назвал владелец,** и ни одного «заодно» к ней. Умолчание параметра не тронуто — разметка остальных сорока шести форм вехи не двинулась ни на символ.
- **Правило прошло RED → GREEN ДВУМЯ РАЗНЫМИ КОММИТАМИ** (`ceeddb6` → `12ff0de`), и переход цвета виден в истории, а не объявлен в прозе.
- **Запись перечня исключений сменила род не молча:** строка летописи `2 → 2, Фаза 9, план 09-10` с причиной, и устаревшее утверждение прежней записи («пустое поле callers») переписано, а не оставлено противоречить новому положению дел.

## Task Commits

1. **Задача 1: расхождение становится видимым машинным правилом и глазом (tracer, TDD RED)** — `ceeddb6` (test) — *исполнена предыдущим экземпляром исполнителя; коммит проверен на месте и по содержимому*
2. **Задача 2: НАБЛЮДЕНИЕ — обе строки консоли на отгруженном дереве** — `8f17f18` (docs)
3. **Задача 3: РЕШЕНИЕ ВЛАДЕЛЬЦА — род записи перечня** — `7b7617f` (docs)
4. **Задача 4: исход применён, правило зеленеет переходом цвета (TDD GREEN)** — `12ff0de` (feat)

**Plan metadata:** см. коммит `docs(09-10): complete ...`

_TDD-цикл плана: RED — `ceeddb6` (`test(09-10)`), GREEN — `12ff0de` (`feat(09-10)`). Шага REFACTOR не потребовалось: правка тега — одна ветвь шаблонизатора, чистить нечего._

## Files Created/Modified

- `tests/test_templates/test_htmx_markup_gates.py` — разборщик блоков вызова `_form_wrapper_call_blocks`, сканер `_offenders_unreachable_blocking_target`, антивакуумная константа `UNREACHABLE_TARGET_CALL_BLOCKS_MEASURED`, само правило и двадцать второй отрицательный контроль файла; запись `DISABLED_ELT_EXCEPTIONS['components/form_wrapper.html']` сменила род, летопись константы продолжена, опоры подстановок `WRAPPER_QUALITY_ATTRS` и `CALLER_TOGGLE_*` сдвинуты вместе с разметкой.
- `app/templates/components/form_wrapper.html` — цель блокировки печатается ВЕТВЬЮ, а не безусловно; внутри ветви нет символа `>`; абзац шапки называет атрибут описательно и литералом его не набирает.
- `app/templates/account_groups/includes/group_row.html` — вызов макроса-обёртки передаёт ПУСТУЮ цель блокировки; комментарий ПРОДОЛЖЕН следующим абзацем летописи с наблюдённой строкой консоли, а не переписан.
- `.planning/phases/.../09-UAT.md` — пункт 7 с проверками 7.1 и 7.2, ЗАПОЛНЕННАЯ таблица отметок, дословно процитированный ответ владельца и строка исхода.

## Decisions Made

**Ответ владельца на останов решения — процитирован дословно, а не выведен:**

> `record-kind`
>
> (Вариант I: макрос перестаёт печатать атрибут при непереданной цели; вызов передаёт «цели нет», запись перечня меняет род.)

**Основание, названное владельцем при выборе, дословно:**

> тег макроса-обёртки правится дешевле всего, пока его использует ОДНА форма; после Фазы 11, раздающей макрос сорока семи формам, та же правка задела бы каждую. Измеренная цена варианта `real-target` (отмена QUAL-06 на этом пути, измерение 3.3) не принимается; `defer` отклонён потому, что строка остаётся в консоли на каждое переключение — на самом частом действии экрана.
>
> Вариант `no-divergence` неприменим: отметка 7.2 несёт наблюдённую строку, а не «строки не было».

**Наблюдение задачи 2 — дословные строки консоли (Chrome / macOS, 2026-08-30, HEAD `ceeddb6`):**

- **7.2, тумблер — СТРОКА БЫЛА:** `The selector "find button[type=submit]" on hx-disabled-elt returned no matches!` (источник `htmx.min.js?v=7f0b468a11f0:1`, стек `H → ve → nn → he → (анонимная) → s`). Запрос уходит, группа включается/выключается как положено.
- **7.1, удаление — СТРОКА БЫЛА:** `The selector "#group-list-sentinel" on hx-include returned no matches!` (источник `htmx.min.js?v=7f0b468a11f0:1`, стек `H → ve → dn → he → (анонимная) → s`). Запрос уходит, статус 200, группа удаляется из базы.
- **Уборка стенда:** `UAT9- rows: groups=0 accounts=0`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Селектор `-k "unreachable_blocking_target"` из плана не выбирает предписанный тест**

- **Found during:** Задача 1 (предыдущий экземпляр исполнителя)
- **Issue:** И `<verify>`, и критерий приёмки задачи 1 предписывают отбор `-k "unreachable_blocking_target"`. Отбор по ключевому слову у pytest видит имена элементов, модулей и маркеров — но НЕ имена вспомогательных функций, а подстрока `unreachable_blocking_target` встречается только в имени сканера `_offenders_unreachable_blocking_target` и НИ РАЗУ в имени теста `test_no_caller_declares_a_blocking_target_its_form_cannot_have`. Предписанная команда отобрала бы ноль тестов и завершилась бы ненулевым кодом «no tests ran» — то есть выглядела бы ожидаемым RED, ничего не проверив.
- **Fix:** Рабочий отбор `-k "blocking_target or self_removing_wrapper"` и его дополнение `-k "not blocking_target and not self_removing_wrapper"`; оба проверены на столкновения по всему каталогу `tests/` и выбирают ровно два новых теста. **Имя теста оставлено ровно таким, каким его предписывает план,** а не подогнано под селектор: подгонка имени ради команды сделала бы имя хуже ради удобства запуска.
- **Files modified:** нет (правка касается команд запуска, а не дерева)
- **Verification:** `pytest -k "blocking_target or self_removing_wrapper"` → 1 failed, 1 passed (designed RED, назвавший нарушителя); дополнение → 69 passed
- **Committed in:** `ceeddb6` (записано в теле коммита задачи 1)

**2. [Rule 1 - Bug, документация] Летопись числа контролей файла гейтов расходилась с самим файлом**

- **Found during:** Задача 1 (предыдущий экземпляр исполнителя)
- **Issue:** Докстринг положительного контроля объявлял «одиннадцать отрицательных», тогда как файл нёс двадцать. Расхождение молчаливое: числа никто не сличал, и следующий читатель принял бы объявленное за измеренное.
- **Fix:** Число исправлено на «двадцать один отрицательный» в ходе подъёма 21 → 22 и заведена явная запись летописи `# --- ЧИСЛО КОНТРОЛЕЙ ФАЙЛА: 22 ---`, чтобы число было ЗАПИСАНО, а не выводилось; четыре ссылки «а не двадцать вторым контролем» сдвинуты на «двадцать третьим».
- **Files modified:** `tests/test_templates/test_htmx_markup_gates.py`
- **Verification:** `grep -c "^def test_control" tests/test_templates/test_htmx_markup_gates.py` сошлось с объявленным числом
- **Committed in:** `ceeddb6`

**3. [Rule 3 - Blocking] Посев стенда из артефакта обхода делает проверку 7.1 невоспроизводимой**

- **Found during:** Задача 2 (наблюдение владельца)
- **Issue:** Раздел «Как привести стенд в нужное состояние» сеет СОРОК групп — столько нужно проверке 3.5, которой сентинел бесконечной прокрутки обязан быть виден. Precondition задачи 2 требует ровно обратного: аккаунт с числом групп тридцать и меньше. При сорока группах сентинел на экране ЕСТЬ (`PAGE_SIZE = 30`, `app/pages/account_groups.py:35` — узел рисуется только при наличии следующей порции), и проверка 7.1 не воспроизвелась бы вовсе: она показала бы «расхождения нет» ПРИ НАЛИЧИИ расхождения. Это молчаливый ложноотрицательный ответ — ровно тот класс, ради которого пункт 7 и написан.
- **Fix:** Посев выполнен той же командой с `range(1, 12)` вместо `range(1, 40)` — одиннадцать групп плюс `UAT9-needle`, итого двенадцать; префикс `UAT9-` не менялся, уборка та же. Отклонение записано абзацем В САМОМ пункте 7 артефакта, а не только здесь: следующий проходящий обход обязан узнать об этом из артефакта, а не из сводки.
- **Files modified:** `.planning/phases/.../09-UAT.md`
- **Verification:** обе проверки воспроизвелись, обе строки консоли наблюдены; уборка `UAT9- rows: groups=0 accounts=0`
- **Committed in:** `8f17f18`

**4. [Rule 1 - Bug, документация] Обоснование записи перечня противоречило бы новому положению дел**

- **Found during:** Задача 4
- **Issue:** План предписывал «дописать в `reason` причину нового рода записи». Дописывания было МАЛО: прежний текст утверждал «переопределяющих вызывающих сегодня нет ни одного», «фактически действует умолчание самого макроса» и «пустое поле callers здесь ПРОВЕРЯЕМО» — после правки все три утверждения ложны, потому что вызывающий снова передаёт параметр. Запись, к которой дописали правду поверх неправды, читается как противоречащая себе, и следующий читатель выбирает половину наугад.
- **Fix:** Устаревшие предложения переписаны под новое положение дел (действующее значение определяет ВЫЗЫВАЮЩИЙ, при непереданном параметре — умолчание; поле callers краснеет и на начавшего передавать молча, и на переставшего передавать). Прежнее основание, ОСТАЮЩЕЕСЯ в силе (параметрическое значение на месте определения), сохранено и помечено как таковое.
- **Files modified:** `tests/test_templates/test_htmx_markup_gates.py`
- **Verification:** `test_every_disabled_elt_exception_is_actually_an_exception` и оба соседних утверждения SP-1 зелены
- **Committed in:** `12ff0de`

**5. [Rule 3 - Blocking] Опора отрицательного контроля дописывала второй аргумент вместо замещения первого**

- **Found during:** Задача 4
- **Issue:** План назвал поимённо только опору `WRAPPER_QUALITY_ATTRS`. Но опоры двадцать второго контроля (`CALLER_TOGGLE_CALL_TAIL` / `CALLER_TOGGLE_SUBMIT_TARGET_TAIL`, заведены задачей 1) стояли на хвосте вызова `sync='this:drop')` и ДОПИСЫВАЛИ цель блокировки. После правки вызов сам несёт `disabled_elt=''`, и подстановка дала строку аргументов с ДВУМЯ значениями одного параметра. Разборщик берёт ПЕРВОЕ вхождение — то есть пустую цель, — и контроль позеленел, притворившись доказательством зубов, не будучи им. Отказ наблюдён прогоном, а не выведен: `1 failed` с сообщением «правило этого не заметило — значит зубов у него нет».
- **Fix:** Обе опоры сдвинуты так, что подстановка ЗАМЕЩАЕТ переданную цель (`disabled_elt='', sync='this:drop')` → `disabled_elt='find button[type=submit]', sync='this:drop')`), и причина записана комментарием над константами вместе с тем, как отказ проявился.
- **Files modified:** `tests/test_templates/test_htmx_markup_gates.py`
- **Verification:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly` → 71 passed
- **Committed in:** `12ff0de`

---

**Total deviations:** 5 auto-fixed (2 × Rule 1 — документационные расхождения; 3 × Rule 3 — блокирующие: неработающий селектор запуска, невоспроизводимый стенд, самозеленеющая подстановка контроля).

**Impact on plan:** Ни одно отступление объёма не расширило и ни одной ветви решения не подменило. Три из пяти — свойства ПРОВЕРОК, а не проверяемого: команда, которая ничего не отбирала; стенд, на котором проверка не воспроизводилась; контроль, который зеленел на собственной подстановке. Каждое из трёх молчало бы, и молчание выглядело бы успехом — что и есть их цена.

## Deliberate departures from standard close-out

- **`requirements.mark-complete` НЕ ЗАПУСКАЛСЯ, и это прямой запрет плана,** а не пропуск: «состояния требований и флажки в `.planning/REQUIREMENTS.md` этим планом не переводятся вовсе — перевод состояний делает повторная верификация». Поле `requirements-completed` во frontmatter скопировано из плана дословно (требование формы сводки) и отметкой «выполнено» не является. `git diff .planning/REQUIREMENTS.md` пуст — это критерий приёмки задачи 4, и он проверен.

## Issues Encountered

- **Гейт трекинга `test_state_progress_matches_roadmap` красный, и он был красным УЖЕ НА HEAD до единой правки этого плана.** Поле `progress.completed_plans` в `.planning/STATE.md` записано как `27`, из отметок `.planning/ROADMAP.md` выводится `26`. Проверено ИСПОЛНЕНИЕМ на файлах коммита `ceeddb6`, а не рассуждением: `progress_divergence` на них даёт ту же единственную расходимость. Это объявленное окно самого механизма («в окне между коммитом плана и коммитом трекинга гейт КРАСНЫЙ — это и есть его предмет, окно обязано быть видимым»), и закрывается оно отметкой плана 09-10 в ROADMAP на шаге трекинга. Правкой шаблонов не вызвано и в область плана не входит.
- Полная суита: **2650 passed, 1 failed** (тот самый гейт трекинга), при пороге плана «не меньше 2649 пройденных».

## Known Stubs

Нет. Заглушек, долговых маркеров и пропущенных тестов правка не оставила: `grep` по обоим правленым шаблонам на `TODO` / `FIXME` / «placeholder» даёт ноль, перечень `UNREACHABLE_TARGET_EXCEPTIONS` ветви `defer` не заводился (ветвь не выбрана), и ни одного `skip` в тронутом файле гейтов не появилось.

## Threat Flags

Нет. Новой сетевой поверхности, путей аутентификации, файлового доступа и изменений схемы правка не вводит; `T-09-10-03` (обработчик тумблера не тронут), `T-09-10-04` (нового вывода имени группы не появилось) и `T-09-10-06` (внутри новой ветви нет символа `>`) закрыты прогонами, названными в реестре угроз плана.

## User Setup Required

Нет — внешних сервисов правка не затрагивает.

## Next Phase Readiness

- **Дорожка для плана 09-11 доказана.** DIV-09-01 (`hx-include` на сентинел, которого может не быть) — расхождение того же рода, и его строка консоли НАБЛЮДЕНА в том же сеансе: план 09-11 правит на подтверждённом дереве, а не на записи месячной давности.
- **Открытым остаётся ПОВТОРНОЕ наблюдение 7.2** — что строка из консоли действительно ушла. Здесь оно не проводилось, и это записано прямым текстом, а не сглажено.
- **QUAL-06 остаётся неизмеренным на изменённой разметке** — предмет плана 09-12 (проверка 3.3 повторно). Выбранная ветвь его не задевает, но и не подтверждает.
- **Фаза 11 наследует новую ветвь тега.** Способ выключить цель блокировки одним аргументом теперь существует; цена закрыта перечнем исключений и правилом достижимости, но при раздаче макроса сорока семи формам граница разборщика блочных вызовов (вложенные вызовы не поддерживаются) обязана быть пересмотрена — это записано в докстринге `_form_wrapper_call_blocks`, а не оставлено на догадку.

## Self-Check: PASSED

- Файлы на диске: `tests/test_templates/test_htmx_markup_gates.py` FOUND, `app/templates/components/form_wrapper.html` FOUND, `app/templates/account_groups/includes/group_row.html` FOUND, `09-UAT.md` FOUND.
- Коммиты в истории: `ceeddb6` FOUND, `8f17f18` FOUND, `7b7617f` FOUND, `12ff0de` FOUND.
- Критерии приёмки задачи 4 перепрогнаны поимённо: печатающих форм атрибута в макросе ровно `1`; `disabled_elt=` в вызове `1`; `09-10` в летописи файла гейтов — `10` вхождений; изменённых строк таблицы состояний и флажков `.planning/REQUIREMENTS.md` — `0` и `0`; диффов по `09-0*-PLAN.md`, `09-0*-SUMMARY.md` и `09-VERIFICATION.md` — пусто; строка исхода в `09-UAT.md` на месте.
- Проверка плана «RED → GREEN двумя разными коммитами» подтверждена историей: `ceeddb6` (`test`) → `12ff0de` (`feat`).
- Единственный красный тест дерева — гейт трекинга STATE/ROADMAP, красный до правок этого плана (проверено исполнением на файлах `ceeddb6`).

---
*Phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy*
*Completed: 2026-08-30*
