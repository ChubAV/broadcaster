---
context: phase
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 18
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-09-02
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **Карта `waves` из `phase-plan-index` для этой фазы неверна** — она схлопывает 09-17…09-20 в волну 1, тогда как frontmatter объявляет `wave: 1..4` и цепочку `depends_on` 17→18→19→20. — Mitigation: волны брать из frontmatter планов, а не из индекса; плюс обязательная проверка пересечения `files_modified` перед группировкой.
- [ ] CONSTRAINT: **Гейт `executor-isolation-dispatch` требует `isolation="worktree"` для `gsd-executor`.** Для планов 09-18…09-20 это ШТАТНЫЙ путь и ограничением не является — RED-коммита в чужом worktree у них нет. Отклонение волны 1 (субагент без гварда в существующем worktree) было разовым и на них НЕ распространяется.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Параллельный запуск gap-планов по индексу | `phase-plan-index` вернул все четыре плана в одной волне. Запуск их параллельно порвал бы объявленную цепочку `depends_on`. | blocking | Волны брать из frontmatter; при расхождении с индексом верить frontmatter. |
| Чтение состояния чужого worktree двухточечным `git diff` | `git diff --stat <base> <branch>` показывает мнимый снос тысяч строк — артефакт сравнения деревьев. | advisory | Читать через `git merge-base` + `git log --oneline <base>..<branch>` + `git show --stat`. |
| Отчёт о «незаполненных» SUMMARY по grep-у | `grep -l "To be filled\|placeholder\|TBD"` даёт ложные совпадения — это собственные проверочные команды сводок. | advisory | Каждое совпадение открывать и читать строку целиком. |

<current_state>
Четвёртый круг закрытия гейпов Фазы 09 (`/gsd-execute-phase 09 --gaps-only`).
Из 20 планов фазы **17 завершены**; три незавершённых (09-18, 09-19, 09-20) несут `gap_closure: true`.

**Волна 1 (план 09-17) ЗАКРЫТА И СЛИТА 2026-09-02.** Ветвь `destroy-guard` (решение владельца)
применена; переход цвета ИСПОЛНЕН в отдельном отсоединённом дереве на `RED_SHA 2f9875f` с кодом
ровно `1`; суита зелена целиком (2682 passed). Коммиты: `2f9875f` (RED) → `00b6879` (GREEN,
`modal.html` + `app.css`) → `0a6ce08` (сводка, `status: complete`) → `d97c526` (слияние worktree).
Worktree `agent-a217c9b7b59eb5230` слит и удалён.
</current_state>

<completed_work>
- 09-17 задача 1 — правило сноса заведено и красно (`2f9875f` = RED_SHA)
- 09-17 задача 2 — `checkpoint:decision`, владелец назвал ветвь `destroy-guard`
- 09-17 задача 3 — ветвь применена, переход цвета исполнен, сводка `status: complete`
- Волна 1 слита по манифесту `worktree.cleanup-wave` → `d97c526`
- Устаревший `.planning/milestone.lock` (pid 941133 мёртв) снят
- `HANDOFF.json` прошлой паузы потреблён и удалён
</completed_work>

<remaining_work>
- **Волна 2 — 09-18** (`autonomous: true`, 3 задачи `auto`) — CR-02/WR-01/WR-02: ответ тумблера отдаёт ПУСТОЕ скрытое поле `search`; оба выхода на `_screen_url`; ложная проза `group_row.html:151-154`
- **Волна 3 — 09-19** — WR-04/WR-05: граница окна теста панели повтора + отложенные пункты
- **Волна 4 — 09-20** (`checkpoint:human-action`, `blocking-human`) — пересдача UAT в живом браузере + бухгалтерия круга
- **Хвостовые гейты** — `aggregate_results` → `code_review_gate` → `regression_gate` → `verify_phase_goal` → `update_roadmap`

`section_manifest`: included = `regression-gate`; excluded = `partial-wave`, `gap-closure-artifacts`.
</remaining_work>

<blockers>
- **Человеческая проверка впереди**: план 09-20 несёт `checkpoint:human-action` с `gate="blocking-human"` — живой браузер, автоматизации не подлежит. Пересдать 09-UAT.md 3.1 / 3.2 / 3.3-Б и ЗАНОВО 3.5 на механизме `keyset`, плюс новый шаг: прокрутить страницу ПОСЛЕ успешного удаления и долистать до сентинела.
- **Чужой worktree `agent-a58648dd532fec1ad`** @ `faf37db` — из прошлой сессии, НЕ в манифесте, отставание 71 коммит. Единственный уникальный коммит правит только `09-UAT.md` (+358/−18) и почти наверняка перекрывается планом 09-20, который этот файл переписывает заново. Не сливать не разобравшись; чистить только адресно.
</blockers>

## Infrastructure State
- **Ветка**: `gsd/phase-09-pilot-na-account-groups-skvoznoy-kontrakt-formy`, HEAD `d97c526`, дерево чистое
- **Режимы**: auto-mode `false`; MVP `false`; TDD `true` (гейт `tdd.review-checkpoint` совещательный); капабилити `code-review` активна; `--no-verify` запрещён
- **Изоляция**: `harness-worktree`; для 09-18…09-20 — штатный `Agent(gsd-executor, isolation="worktree")`

<context>
Фаза 09 — пилот: доказать сквозной контракт формы на НАИМЕНЬШЕЙ поверхности ПРЕЖДЕ массовой работы
Фазы 10, которая раздаёт `hx_post` шестнадцати местам подтверждения ОДНОЙ правкой.
Оркестратор ведёт волны сам и держит контекст тонким: планы исполняют субагенты `gsd-executor`
в изолированных worktree; `STATE.md`/`ROADMAP.md` исполнителям трогать запрещено.
</context>

<next_action>
Дождаться post-merge гейта волны 1 (полная суита на слитом дереве), затем при `TEST_EXIT=0`:
`roadmap.update-plan-progress` → диспатч волны 2 (план 09-18) штатным
`Agent(subagent_type="gsd-executor", isolation="worktree")`.
</next_action>
