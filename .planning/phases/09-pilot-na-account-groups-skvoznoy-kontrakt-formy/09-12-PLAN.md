---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 12
type: execute
wave: 3
depends_on: ["10", "11"]
gap_closure: true
files_modified:
  - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [FORM-02, QUAL-01, QUAL-02, QUAL-06]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ни одна отметка не подписана под ненаблюдённым: каждая ячейка несёт наблюдённый признак — увиденный результат, скопированную строку или выписанное название, — и слово «ок» отметкой не считается"
    - "Наблюдения сняты с ТОГО дерева, которое отгружено: проверки идут ПОСЛЕ планов 09-10 и 09-11, а не до, потому что обе правки меняют ровно тот механизм, о котором проверки говорят"
    - "Состояний требований этот план НЕ ПЕРЕВОДИТ вовсе: столбец `Status` и флажки в теле `.planning/REQUIREMENTS.md` остаются нетронутыми, а план оставляет вместо них ВТОРОЙ круг раздела свидетельств. Перевод состояний делает повторная верификация, которая единственная видит и суиту, и заполненные отметки"
    - "Неприкосновенность состояний утверждается командой, СПОСОБНОЙ ПОКРАСНЕТЬ: число уходит в переменную, исход даёт сличение, и рядом стоит антивакуумное утверждение — дифф по `.planning/REQUIREMENTS.md` непуст именно работой плана. Печать числа конвейером `wc -l` зеленела при любом значении и гейтом не была"
    - "Раздел свидетельств набран СПИСКОМ, а не таблицей: проверка неприкосновенности состояний сличает строки ТАБЛИЦЫ, и табличные строки с теми же идентификаторами требований покрасили бы собственный гейт плана"
    - statement: "После переключения тумблера КЛАВИАТУРОЙ фокус остался на том же тумблере: `document.activeElement` при нетронутой мыши вернул элемент с тем же идентификатором. Сегодняшний ответ «нет» описывает механизм, которого на экране больше нет, и возврат фокуса после правки — рассуждение, а не измерение (QUAL-06, проверка 3.3 ПОВТОРНО)"
      verification: backstop
    - statement: "На медленной сети двойное нажатие даёт ОДИН запрос переключения: второй отбрасывает клиент отбрасывающей синхронизацией, а не блокировкой чекбокса. Отбрасывание — свойство рантайма; суита утверждает только наличие атрибута в разметке (QUAL-01, поведенческая половина, проверка 6.2 ПОВТОРНО)"
      verification: backstop
    - statement: "Отметка проверки курсора прокрутки несёт ВЫПИСАННОЕ название последней видимой строки и результат сверки списка с самим собой: правило проверки требует названия прямо, и домысливать за человека запрещено (FORM-02, наблюдательная половина, проверка 3.5 ПОВТОРНО)"
      verification: backstop
    - statement: "После ОТКАЗА запроса кнопка подтверждения не осталась включённой с признаком занятости, а кнопка отказа оставалась нажимаемой всё время запроса (QUAL-02, WARN-2 — шаг обхода, который не проводился ни разу)"
      verification: backstop
    - statement: "Консоль экрана групп после правок планов 09-10 и 09-11 не несёт ни одной строки о неразрешённом селекторе — либо несёт РОВНО ту, что осталась объявленной записью отложенного расхождения, и никакой другой. Опорный признак обхода «ответ 200 и чистая консоль» либо восстановлен, либо его нерабочая часть названа поимённо"
      verification: backstop
  artifacts:
    - ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md — раздел «Второй круг закрытия: проверки на изменённой разметке» с проверками 3.3-Б, 6.2-Б, 3.5-Б, 6.3 и 7.3, каждая со своим выписанным ожидаемым признаком, и таблица отметок второго круга"
    - ".planning/REQUIREMENTS.md — подраздел «Второй круг закрытия (планы 09-10 … 09-12)» внутри существующего раздела свидетельств: на каждое из четырёх требований машинная проверка, наблюдённый признак и вердикт, СПИСКОМ"
  key_links:
    - "наблюдение на изменённой разметке → раздел свидетельств → повторная верификация: рвётся — Фаза 15 (сводное закрытие девяти пунктов) наследует как факт отметку, снятую с механизма, которого на экране нет"
    - "выписанное название строки → сверка списка с самим собой: рвётся — признак отказа МОЛЧАЛИВ, и «список выглядел нормально» закрытием не является"
    - "отбрасывающая синхронизация в вызове → один запрос вместо двух: рвётся — второе нажатие уходит на сервер, и защита от двойного нажатия существует только в разметке"
  prohibitions:
    - statement: "состояния требований этим планом не переводятся ВОВСЕ: столбец Status и флажки в теле .planning/REQUIREMENTS.md остаются нетронутыми; отметки, откаченные коммитом 183511c, обратно не ставятся ни молча, ни по суждению"
      status: flagged-unverified
      verification: none
    - statement: "раздел свидетельств не переводится в табличную форму: сличение строк таблицы покрасило бы собственный гейт плана на его же записи"
      status: flagged-unverified
      verification: none
    - statement: "исполнитель не подставляет ожидаемый ответ вместо наблюдённого и не досочиняет за человека выписанное название строки"
      status: flagged-unverified
      verification: none
    - statement: "вердикты и таблицы 09-VERIFICATION.md этим планом не переписываются: артефакт верификации перегенерирует повторная верификация"
      status: flagged-unverified
      verification: none
    - statement: "планы 09-01 … 09-11 и их сводки не правятся, не перенумеровываются и не отменяются"
      status: flagged-unverified
      verification: none
    - statement: "браузерный стенд не заводится и Playwright не ввозится: проверки закрываются глазом человека, E2E-01 остаётся отложенным"
      status: flagged-unverified
      verification: none
    - statement: "посевные строки стенда не остаются в базе после обхода: посев идёт в живую локальную базу рядом с настоящими данными"
      status: flagged-unverified
      verification: none
    - statement: "долговых маркеров в артефактах не появляется"
      status: flagged-unverified
      verification: none
---

<objective>
Закрыть четыре наблюдения, которые сегодня либо сняты с механизма, которого на экране больше нет,
либо не снимались ни разу, — **на дереве, отгруженном после планов 09-10 и 09-11**.

Purpose: обход 2026-08-30 прошёл честно и дал ответы, но три из них устарели по причине,
названной прямо в самом артефакте: решение владельца по конфликту D-06 ↔ QUAL-06 изменило
механизм ПОСЛЕ обхода, а планы 09-10 и 09-11 изменили его ещё раз.

- **3.3** («фокус после переключения клавиатурой») дала «нет» — и это верное измерение
  БЛОКИРОВКИ ЧЕКБОКСА, которой на форме больше нет. Что фокус ВЕРНЁТСЯ — рассуждение, а не
  наблюдение.
- **6.2** («двойное нажатие даёт один запрос») снята с той же блокировки; отбрасывающая
  синхронизация глазами не наблюдалась ни разу.
- **3.5** («курсор прокрутки») пройдена по всем четырём шагам, но отметка **не несёт выписанного
  названия строки**, которого правило проверки требует прямо. Домысливать за человека запрещено,
  поэтому наблюдательная половина FORM-02 остаётся незакрытой.
- **Кнопка подтверждения после ОТКАЗА запроса** (WARN-2) — шаг, который не проводился и отметки
  не получил ни разу. У атрибута блокировки на этой кнопке было два владельца, и план 09-08
  свёл их к одному; наблюдения этому сведению не досталось.

Плюс один признак, который эти два круга закрытия и должны были вернуть: **чистая консоль на
экране групп**. Обход опирается на неё в трёх местах, и обе находки её сломали.

Output: раздел второго круга в артефакте обхода с выписанными ожидаемыми признаками; заполненная
наблюдением таблица отметок; ВТОРОЙ круг раздела свидетельств в `.planning/REQUIREMENTS.md` —
на каждое требование машинная проверка, наблюдённый признак и вердикт.

⚠️ **Состояний требований этот план НЕ переводит.** Преждевременная отметка «выполнено» — та
самая ошибка, что породила третий пробел фазы и потребовала отката коммитом `183511c`. Перевод
состояний делает повторная верификация, читающая раздел свидетельств.

⚠️ **`autonomous: false`** — один останов наблюдения, и подстановка ожидаемого вместо
наблюдённого обесценила бы оба круга закрытия разом.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-PATTERNS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-10-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-11-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-08-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Заводимых имён в коде этот план не несёт вовсе — он не трогает ни одного файла приложения и ни
одного теста. Заводится только в артефактах планирования:

- В `09-UAT.md`: раздел **«Второй круг закрытия: проверки на изменённой разметке»**, проверки
  **3.3-Б**, **6.2-Б**, **3.5-Б**, **6.3** и **7.3**, таблица отметок второго круга, и обновлённое
  поле `status` артефакта.
- В `.planning/REQUIREMENTS.md`: подраздел **«Второй круг закрытия (планы 09-10 … 09-12)»**
  внутри уже существующего раздела «Фаза 9 — свидетельства к пересмотру состояний», СПИСКОМ.

⚠️ **Матрица покрытия внешнего API не заводится:** детерминированный обход области фазы вернул
`detected: false`, проверка пропускается целиком. ⚠️ **Останов допущений архитектуры не сработал**
(`detected: false`). ⚠️ **Гейт схемы БД молчит:** проект на SQLAlchemy и Alembic, миграций план не
заводит и файлов приложения не трогает. ⚠️ **Пакетов план не устанавливает.**
</artifacts_this_phase_produces>

<edge_coverage_lift>
## Разрешение краевых предикатов (SPEC отсутствует, работает запасной путь)

Этот план собирает ВСЕ оставшиеся `backstop`-предикаты фазы, и каждый несёт структурный маркер —
при верификации без явного свидетельства он обязан уйти в `human_needed`, а не пройти молча.

| Требование | Категория | Исход | Куда легло |
|---|---|---|---|
| QUAL-01 | concurrency | `resolved`, verification: **backstop** | «на медленной сети двойное нажатие даёт ОДИН запрос» — проверка 6.2-Б. Серверная половина (идемпотентность) закрыта explicit-предикатом плана 09-10 и здесь не дублируется |
| QUAL-06 | unclassified | `resolved`, verification: **backstop** | «фокус остался на том же тумблере» — проверка 3.3-Б |
| FORM-02 | unclassified | `resolved`, verification: **backstop** | «отметка несёт ВЫПИСАННОЕ название строки» — проверка 3.5-Б. Машинная половина закрыта explicit-предикатами планов 09-05 и 09-11 |
| QUAL-02 | unclassified | `resolved`, verification: **backstop** | «после отказа запроса кнопка подтверждения не осталась включённой с признаком занятости» — проверка 6.3. Порог видимости индикатора (6.1) закрыт обходом 2026-08-30 и не переснимается |
| FORM-09 | unclassified | `resolved`, verification: **explicit** | закрыт машинно планом 09-10; здесь не дублируется, чтобы два плана не объявляли одно свидетельство |

**Нерешённых строк не осталось**, флагированных допущений планировщика этот план не несёт.
</edge_coverage_lift>

<tasks>

<task type="auto">
  <name>Задача 1: пять проверок второго круга — с выписанным ожидаемым признаком у каждой</name>

  <files>.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md</files>

  <read_first>
- `09-UAT.md` целиком, и особенно: пункт 3 (проверки 3.1-3.5 и их таблица отметок с оговоркой по
  3.5), пункт 6 (проверки 6.1 и 6.2, их таблица и предупреждение «6.2 СНЯТА С ПРЕЖНЕГО
  МЕХАНИЗМА»), пункт 5, §«Расхождения обхода» (РАСХ-1 и РАСХ-2 вместе со строками исхода,
  дописанными планами 09-10 и 09-11), §«Как привести стенд в нужное состояние» и §«Как убрать
  стенд», §«Что проверяется глазами, потому что иначе никак». **Новые проверки обязаны
  продолжать форму существующих, а не заводить вторую.**
- `09-10-SUMMARY.md` и `09-11-SUMMARY.md` — КАКАЯ ветвь применена в каждом расхождении. От этого
  зависит формулировка ожидаемого признака проверки 7.3: если владелец выбрал отложить, консоль
  обязана нести РОВНО объявленную строку и никакой другой, и это надо выписать дословно.
- `09-08-SUMMARY.md` §про WARN-2 / WR-02 — что именно шипнуло для кнопки подтверждения после
  отказа запроса: состояние отправки возвращается в исходное по ЗАВЕРШЕНИЮ запроса, а не по
  успеху. Проверка 6.3 наблюдает ровно это.
- `09-VERIFICATION.md` §`human_verification` — шесть пунктов, из которых два закрыли планы 09-10
  и 09-11, а четыре закрывает этот план. Формулировки ожиданий брать оттуда дословно, а не
  пересказывать.
- `app/templates/components/modal.html` строки 186-212 — состояние отправки, привязка признака
  занятости к нему и кнопка отказа с типом «кнопка». Читать затем, чтобы выписать ожидаемый
  признак проверки 6.3 точно, а не приблизительно.
  </read_first>

  <action>
Дописать в `09-UAT.md` раздел **«Второй круг закрытия: проверки на изменённой разметке»**, а
существующие таблицы отметок первого круга НЕ ТРОГАТЬ: они верны для дерева, на котором снимались,
и переписанная задним числом отметка — это подделка наблюдения, а не уточнение.

**Форма раздела повторяет форму существующих пунктов, и от неё зависит машинный критерий.**
Заголовок раздела — ВТОРОГО уровня (`## Второй круг закрытия: проверки на изменённой разметке`),
каждая из пяти проверок — своим заголовком ТРЕТЬЕГО уровня (`### 3.3-Б …`), и у каждой своя
таблица с той же шапкой из трёх колонок, что у 3.1-3.5, 6.1-6.2 и 5.1-5.3: номер шага, шаг,
ожидаемый наблюдаемый признак. Раздел ставится ПОСЛЕ существующих пунктов обхода: критерий
приёмки считает шапки признаков внутри его собственных границ — от заголовка раздела до
следующего заголовка второго уровня или до конца файла.

⚠️ **Основание критерия ИЗМЕРЕНО, а не предположено.** Сегодня в `09-UAT.md` строка-шапка колонки
ожидаемого признака встречается **10 раз** — по одной на каждую из десяти существующих проверок
(3.1-3.5, 6.1-6.2, 5.1-5.3); число снято с живого артефакта. План 09-10 добавит к нему свои
проверки 7.1 и 7.2, и итог ПО ВСЕМУ ФАЙЛУ зависит от того, набрал ли исполнитель того плана обе
одной таблицей или двумя, — то есть числом по всему файлу это свойство невыразимо. Поэтому
критерий утверждает АБСОЛЮТНОЕ число внутри СВОЕГО раздела (пять), а не прирост: утверждение о
приросте без записанного исходного числа проверить нечем, и зеленело бы оно при любом исходе.
⚠️ В ПРОЗЕ раздела строку-шапку целиком не набирать: она и есть считаемое, и объяснение о ней
удовлетворило бы счёт само.

В шапке раздела записать прямым текстом, ПОЧЕМУ круг второй: механизм менялся дважды после
обхода 2026-08-30 — решением владельца по D-06 ↔ QUAL-06 (план 09-09) и правками расхождений
(планы 09-10 и 09-11), — и отметки первого круга описывают дерево до этих правок.

**Пять проверок, каждая со своей строкой в таблице отметок. Сливать их нельзя** — правило
пункта 3 распространяется и сюда: разойтись они могут независимо.

**3.3-Б — фокус после переключения КЛАВИАТУРОЙ, на изменённой разметке.**
Шаги: Tab до чекбокса строки (видна рамка фокуса) → Space → **не трогая мышь**, в консоли
выполнить `document.activeElement`.
Ожидаемый признак: активным элементом остался чекбокс С ТЕМ ЖЕ идентификатором `group-toggle-N`,
что был до нажатия. Записать ФАКТИЧЕСКИЙ ответ и то, чем на самом деле оказался активный элемент.
⚠️ Записать в шапке проверки: ожидание сегодня — «да», и оно основано на РАССУЖДЕНИИ (чекбокс
уезжает из документа вместе с подменённой строкой, идентификатор стабилен в обеих отрисовках), а
не на измерении. Ответ «нет» означает, что рассуждение неверно, и это надо записать так же явно.

**6.2-Б — двойное нажатие на НОВОМ механизме.**
Шаги: DevTools → Network, фильтр по адресу переключения → Throttling → Slow 3G → быстро нажать на
тумблер ДВАЖДЫ.
Ожидаемый признак: в Network РОВНО ОДИН запрос переключения, а не два; чекбокс при этом
**остаётся доступным** (блокировки на нём больше нет — второй запрос отбрасывает клиент).
⚠️ Записать: если запросов два — отбрасывающая синхронизация не работает, и первый подозреваемый
не обработчик, а место, где признак объявлен: он передаётся ВЫЗОВОМ макроса-обёртки, а не самим
макросом.

**3.5-Б — курсор прокрутки, С ВЫПИСАННЫМ НАЗВАНИЕМ.**
Шаги: обновить страницу аккаунта с числом групп больше размера страницы → **выписать в отметку
название последней видимой строки первой порции ДОСЛОВНО** → удалить строку из первой трети →
долистать до низа → сверить список с самим собой.
Ожидаемый признак: выписанное название на месте и встречается РОВНО ОДИН раз; ни одно название не
пропало и ни одно не появилось дважды; счётчик сходится с числом отрисованных строк.
⚠️ Записать правило прямо: отметка «список выглядел нормально» закрытием этой проверки НЕ
считается — она обязана нести выписанное название и результат сверки. Именно этого не хватило
первому кругу.

**6.3 — кнопка подтверждения после ОТКАЗА запроса (WARN-2, шаг, который не проводился ни разу).**
Шаги: открыть панель подтверждения удаления → DevTools → Network → Offline → нажать «Удалить» →
дождаться отказа → посмотреть на кнопку подтверждения и на кнопку отказа.
Ожидаемый признак: после отказа кнопка подтверждения **нажимаема снова** и **не несёт признака
занятости**; кнопка отказа оставалась нажимаемой ВСЁ время запроса. Плашка обрыва связи при этом
называет расхождение экрана с сервером.
⚠️ Записать: включённая праздная кнопка с признаком занятости — это и есть отказ, ради которого
проверка заведена; у атрибута блокировки на этой кнопке было ДВА владельца, и план 09-08 свёл их
к одному, но наблюдения сведению не досталось.

**7.3 — консоль после обоих кругов закрытия.**
Шаги: открыть экран групп с открытой консолью, очистить её → переключить тумблер → удалить группу.
Ожидаемый признак: строк о неразрешённом селекторе НЕТ НИ ОДНОЙ — либо есть РОВНО та, что
осталась объявленной записью отложенного расхождения, и никакой другой. Выписать ожидаемое
дословно по сводкам планов 09-10 и 09-11, а не по памяти.
⚠️ Записать: это и есть восстановление опорного признака «ответ 200 И ЧИСТАЯ КОНСОЛЬ», на который
обход опирается в трёх местах.

**Таблица отметок второго круга.** Колонки: дата, браузер и ОС, 3.3-Б, 6.2-Б, 3.5-Б, 6.3, 7.3,
исход. Под таблицей — правило: отметка без наблюдённого признака закрытием не считается; слово
«ок» отметкой не является; ячейка 3.5-Б обязана нести выписанное название строки.

**Стенд.** Раздел «Как привести стенд в нужное состояние» переиспользовать как есть, добавив
одну строку: для проверки 7.3 нужен аккаунт с числом групп НЕ БОЛЬШЕ размера страницы, а для
3.5-Б — БОЛЬШЕ; это два разных аккаунта или один, приведённый к нужному числу дважды.
  </action>

  <verify>
    <automated>U=.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md; grep -qF "Второй круг закрытия" "$U" || exit 1; for C in "3.3-Б" "6.2-Б" "3.5-Б" "6.3" "7.3"; do grep -qF "$C" "$U" || exit 1; done</automated>
    <automated>U=.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md; N=$(awk '/^## /{f=0} /^## Второй круг закрытия/{f=1} f' "$U" | grep -c "Ожидаемый наблюдаемый признак" || true); [ "$N" = "5" ]</automated>
    <automated>N=$(git diff -U0 .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md | grep -c "^-.*2026-08-30 | Chrome" || true); [ "$N" = "0" ]</automated>
    <automated>D=$(git diff --stat -- .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-0*-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-10-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-11-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-0*-SUMMARY.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-10-SUMMARY.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-11-SUMMARY.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md app tests); [ -z "$D" ]</automated>
  </verify>

  <acceptance_criteria>
- `09-UAT.md` содержит подстроки `Второй круг закрытия`, `3.3-Б`, `6.2-Б`, `3.5-Б`, `6.3`, `7.3` и `document.activeElement`.
- У каждой из пяти проверок есть СВОЯ шапка колонки ожидаемого признака, и число утверждается АБСОЛЮТНОЕ и внутри границ своего раздела: `U=.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md; N=$(awk '/^## /{f=0} /^## Второй круг закрытия/{f=1} f' "$U" | grep -c "Ожидаемый наблюдаемый признак" || true); [ "$N" = "5" ]` → код `0`. Утверждение проверено на живом артефакте с подставленным разделом: при отсутствии раздела оно даёт `0` и краснеет, при пяти проверках — `5` и зеленеет. Прежняя формулировка утверждала ПРИРОСТ без записанного исходного числа и потому непроверяема; сегодняшнее число по всему файлу — **10** (шапки десяти существующих проверок), и оно записано в действии задачи как основание.
- Таблица отметок второго круга существует и ПУСТА: строк данных под шапкой нет — заполняет её человек на останове задачи 2.
- Существующие таблицы отметок первого круга не изменены: `git diff -U0 .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md | grep -c "^-.*2026-08-30 | Chrome"` → `0`.
- Формулировка ожидаемого признака проверки 7.3 выписана ПО СВОДКАМ планов 09-10 и 09-11: в тексте проверки назван выбранный владельцем вариант по каждому расхождению.
- Планы, сводки и отчёт верификации не тронуты: вывод `git diff --stat` по ним пуст.
- Файлов приложения и тестов эта задача не трогает: `git diff --stat app/ tests/` пуст.
  </acceptance_criteria>

  <done>Артефакт обхода несёт пять проверок второго круга, каждую с выписанным ожидаемым
признаком и с записанной причиной, почему круг второй. Таблица отметок готова и пуста — под неё
встаёт наблюдение человека, а не суждение исполнителя.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Задача 2: НАБЛЮДЕНИЕ — пять проверок второго круга на отгруженном дереве</name>

  <precondition>Приложение поднято и отвечает на `/accounts` (`just run`, порт 8000); правки
планов 09-10 и 09-11 в рабочем дереве и суита зелена целиком
(`uv run pytest tests/ -q -p no:randomly`); в базе посеян стенд с префиксом `UAT9-` командой из
раздела «Как привести стенд в нужное состояние» — аккаунт с числом групп больше размера страницы
для 3.5-Б и аккаунт с числом групп не больше размера страницы для 7.3.</precondition>

  <read_first>
- `09-UAT.md` раздел «Второй круг закрытия» (заведён задачей 1) — пять проверок с выписанными
  ожидаемыми признаками и пустая таблица отметок; §«Как привести стенд в нужное состояние» и
  §«Как убрать стенд».
- `09-UAT.md` предупреждение «ЧТО ИЗМЕНИЛОСЬ В КОДЕ ПОСЛЕ ЭТОГО ОБХОДА» под таблицей отметок
  пункта 3 — почему отметки первого круга описывают дерево до правок и переписыванию не подлежат.
- `09-10-SUMMARY.md` и `09-11-SUMMARY.md` — какая ветвь применена в каждом расхождении; от этого
  зависит, что считать верным исходом проверки 7.3.
  </read_first>

  <action>Пройти пять проверок раздела «Второй круг закрытия» артефакта `09-UAT.md` и заполнить
таблицу отметок второго круга наблюдённым признаком.</action>

  <instructions>
Всё, что можно было автоматизировать, автоматизировано: обе правки шипнуты, оба правила гейтов
зелены переходом цвета, суита зелена целиком, артефакт обхода приведён к сегодняшнему состоянию
экрана. Остаётся ровно то, чего не даёт ни один доступный проекту инструмент: **эффект в живом
браузере**. Браузерного стенда в проекте нет и не заводится — 2649 тестов идут через транспорт
ASGI и не исполняют ни строчки JS; они утверждают ДОСТАВКУ разметки и заголовков, а не то, что
фокус куда-то вернулся и что второй запрос не ушёл.

**Почему круг второй, а не повтор от занудства.** Три из четырёх сегодняшних отметок сняты с
механизма, которого на экране больше нет: до решения владельца второе нажатие удерживала
блокировка чекбокса, теперь — отбрасывающая синхронизация; и та же блокировка была причиной
потери фокуса. Отметка, снятая с исчезнувшего механизма, — это не «почти закрыто», это описание
другого дерева.

Пройти по артефакту
`.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md`, раздел «Второй
круг закрытия»:

- **3.3-Б — МЫШЬЮ НЕ ДЕЛАЕТСЯ.** Tab до чекбокса (видна рамка фокуса), Space, и, **не трогая
  мышь**, в консоли выполнить `document.activeElement`. Записать ФАКТИЧЕСКИЙ ответ и чем оказался
  активный элемент. Ожидание сегодня — «да», и оно основано на рассуждении: подтвердить или
  опровергнуть, но не подставить.
- **6.2-Б.** Network, фильтр по адресу переключения, Slow 3G, быстро нажать тумблер дважды.
  Записать ЧИСЛО ушедших запросов. Если их два — записать это как есть; правка не сработала, и
  молчаливая отметка тут хуже красного теста.
- **3.5-Б.** Обновить страницу крупного аккаунта, **выписать название последней видимой строки
  ДОСЛОВНО прямо в отметку**, удалить строку из первой трети, долистать до низа, сверить список с
  самим собой. Именно выписанного названия не хватило первому кругу, и именно из-за этого
  наблюдательная половина FORM-02 стоит незакрытой.
- **6.3.** Открыть панель подтверждения удаления, Network → Offline, нажать «Удалить», дождаться
  отказа. Посмотреть: кнопка подтверждения нажимаема снова и признака занятости не несёт; кнопка
  отказа оставалась нажимаемой всё время. Этот шаг не проводился НИ РАЗУ.
- **7.3.** Очистить консоль, переключить тумблер, удалить группу. Записать, сколько строк о
  неразрешённом селекторе появилось и какие именно, — дословно.

**После обхода — УБРАТЬ СТЕНД** командой из раздела «Как убрать стенд» и привести вывод команды
проверки уборки.

**Куда пишется ответ.** В таблицу отметок второго круга: дата, браузер и ОС, и по каждой проверке
— наблюдённый признак, а не слово «ок». Подпись под непроверенным Фаза 15 унаследует как факт
(требование GATE-09 — сводное закрытие всех девяти пунктов ручного UAT).

**Расхождение на обходе — что делать.** Записать его и остановиться, а не чинить на ходу:
формулировки правок этого круга уже приняты владельцем, и правка под находку — предмет следующего
решения, а не этого останова.

**Чего делать НЕ надо.** Не переписывать отметки первого круга, не менять состояния требований,
не трогать `09-VERIFICATION.md`.
  </instructions>

  <verification>После возобновления исполнитель проверяет машинно: таблица отметок второго круга
непуста; в ней есть дата, браузер и ОС; заполнены все пять ячеек; ячейка 3.5-Б несёт выписанное
название строки (а не только результат сверки); слова «ок» в качестве единственного содержимого
ячейки нет; строк с префиксом `UAT9-` в базе не осталось.</verification>

  <resume-signal>Заполнить таблицу отметок второго круга в `09-UAT.md`, убрать стенд и написать
«второй круг проведён», указав: ответ 3.3-Б («да» / «нет» и чем оказался активный элемент), число
запросов в 6.2-Б, выписанное название строки в 3.5-Б, исход 6.3 и перечень появившихся строк
консоли в 7.3.</resume-signal>

  <acceptance_criteria>
- Таблица отметок второго круга несёт строку данных с датой, браузером и ОС, и все ПЯТЬ ячеек заполнены.
- Ячейка 3.3-Б несёт слово «да» либо «нет» И то, чем оказался активный элемент.
- Ячейка 6.2-Б несёт ЧИСЛО ушедших запросов, а не оценку.
- Ячейка 3.5-Б несёт ВЫПИСАННОЕ название строки дословно — именно его не хватило первому кругу, и без него проверка не закрыта.
- Ячейка 6.3 несёт наблюдение по обеим кнопкам: подтверждения после отказа и отказа во время запроса.
- Ячейка 7.3 несёт число и дословный текст появившихся строк консоли либо прямое «строк не было».
- Ни одна ячейка не состоит из слова «ок»; отметки ПЕРВОГО круга не изменены — `git diff -U0 .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md | grep -c "^-.*2026-08-30 | Chrome"` → `0`.
- Стенд убран: вывод команды проверки уборки показывает ноль строк с префиксом `UAT9-`.
- `git diff --stat app/ tests/` пуст.
  </acceptance_criteria>

  <done>Пять проверок второго круга пройдены на отгруженном дереве, и таблица отметок несёт
наблюдённый признак по каждой — включая выписанное название строки, которого не хватило первому
кругу. Стенд убран.</done>
</task>

<task type="auto">
  <name>Задача 3: второй круг свидетельств записан, состояния требований не тронуты ни одним символом</name>

  <files>.planning/REQUIREMENTS.md, .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md</files>

  <read_first>
- Заполненная таблица отметок второго круга `09-UAT.md` — источник всех наблюдённых признаков.
- `.planning/REQUIREMENTS.md` §«Фаза 9 — свидетельства к пересмотру состояний» целиком: форма
  записи (машинная проверка → наблюдённый признак → вердикт), правило «частичная строка даёт
  «не заработано» без выбора у исполнителя», и абзац **«РАЗДЕЛ НАБРАН СПИСКОМ, А НЕ ТАБЛИЦЕЙ, И
  ЭТО МАШИННОЕ ТРЕБОВАНИЕ»** — сличение строк таблицы покрасило бы собственный гейт плана.
- `.planning/REQUIREMENTS.md` строки таблицы состояний (около 131-145) и флажки требований
  (около 27-44). ⚠️ **Читать затем, чтобы ЗНАТЬ состояния, а не менять их.** Сегодня FORM-02,
  FORM-09 и QUAL-01 стоят `Gaps Found`, QUAL-02 и QUAL-06 — `Pending`. Ни одна из этих строк и ни
  один флажок этим планом не правятся.
- `git log -1 183511c --stat` — что откатывал этот коммит и почему запрет сформулирован запретом,
  а не осторожностью.
- `09-10-SUMMARY.md` и `09-11-SUMMARY.md` — машинные проверки и их выводы, которые встают в
  колонку «машинная проверка» второго круга: команды прогонов и их результаты брать оттуда
  дословно, а не пересказывать.
  </read_first>

  <action>
**1. Второй круг раздела свидетельств.** Дописать в `.planning/REQUIREMENTS.md` ВНУТРЬ
существующего раздела «Фаза 9 — свидетельства к пересмотру состояний» подраздел **«Второй круг
закрытия (планы 09-10 … 09-12)»**. Форму записи сохранить дословно — **СПИСКОМ, не таблицей**, — и
на каждое из четырёх требований дать три поля.

⚠️ **Строка-заголовок каждой записи набирается РОВНО в той форме, что у соседних записей первого
круга: элемент списка, идентификатор требования в жирном и ничто между ним и звёздочками** —
`- **FORM-02**`, `- **QUAL-01**`, `- **QUAL-02**`, `- **QUAL-06**`. По этой форме сличает
антивакуумное утверждение блока проверки: оно требует РОВНО ОДНОЙ добавленной такой строки на
каждое требование. Приписка внутрь жирного («FORM-02 (второй круг)») покрасила бы его на верной по
существу работе; пояснение ставится ПОСЛЕ звёздочек, а не внутрь них. Сами записи:

- **FORM-02** — машинная проверка: прогон правила объявленных селекторов и правила выживания
  курсора прокрутки из сводки плана 09-11 с их фактическим выводом. Наблюдённый признак: отметка
  3.5-Б с **выписанным названием строки дословно**. Вердикт: `заработано`, если обе клетки
  заполнены фактом и название выписано; `не заработано` в любом ином случае — правило раздела не
  оставляет исполнителю выбора.
- **QUAL-01** — машинная проверка: прогон правила достижимости цели блокировки и тестов
  повторного удаления из сводки плана 09-10. Наблюдённый признак: отметка 6.2-Б с числом ушедших
  запросов. Вердикт по тому же правилу.
- **QUAL-02** — машинная проверка: прогон правил индикатора. Наблюдённый признак: отметка 6.3
  (кнопка подтверждения после отказа и кнопка отказа во время запроса). Вердикт по тому же
  правилу.
- **QUAL-06** — машинная проверка: прогон правила стабильности идентификатора тумблера во
  фрагменте. Наблюдённый признак: отметка 3.3-Б с фактическим ответом и с тем, чем оказался
  активный элемент. Вердикт по тому же правилу.

Отдельной строкой подраздела записать исход проверки **7.3**: восстановлен ли опорный признак
«ответ 200 и чистая консоль», и если нет — какая именно строка осталась и под какой объявленной
записью она живёт. Этот признак не принадлежит одному требованию и потому стоит отдельно.

**2. Цена, если она есть, называется явно.** Если какая-то проверка дала не то, что ожидалось,
записать это ПРЯМЫМ ТЕКСТОМ рядом с вердиктом, вместе с тем, что именно это означает для
требования. Сглаженная формулировка здесь дороже красного вердикта: раздел читает повторная
верификация, и он же наследуется Фазой 15.

**3. Статус артефакта обхода.** Поле `status` в шапке `09-UAT.md` перевести в `passed`, если
разошедшихся проверок нет, либо в `diagnosed` с перечнем расхождений. Дописать в шапку одну
строку: второй круг проведён такого-то числа, на таком-то браузере и ОС, после планов 09-10 и
09-11.

**4. ЧЕГО ЭТОТ ПЛАН НЕ ДЕЛАЕТ, И ЭТО ПРОВЕРЯЕТСЯ ДИФФОМ.** Ни одна строка таблицы состояний, ни
один флажок требования, ни один вердикт `09-VERIFICATION.md` не правятся. Перевод состояний —
работа повторной верификации, которая единственная видит и зелёную суиту, и заполненные отметки,
и оба круга свидетельств.

**5. Порядок прогона проверок: блок исполняется ПО РАБОЧЕМУ ДЕРЕВУ, ДО коммита задачи.** Три
утверждения блока читают `git diff` без базы, то есть сличают рабочее дерево с индексом; после
коммита дифф пуст, и антивакуумное утверждение покраснеет на верной работе. Если проверки
понадобилось повторить ПОСЛЕ коммита — базой берётся предыдущий коммит (`git diff -U0 HEAD~1 -- …`),
и это называется в сводке, а не молча подменяется. Четвёртое утверждение (подраздел свидетельств
несёт ровно одну строку-заголовок на каждое из четырёх требований) читает САМ ФАЙЛ и от состояния
индекса не зависит вовсе — оно и есть та часть антивакуума, которую нельзя удовлетворить
посторонней правкой.

⚠️ **Подраздел ставится заголовком ЧЕТВЁРТОГО уровня** (`#### Второй круг закрытия (планы 09-10 …
09-12)`) внутри существующего раздела третьего уровня — тем же уровнем, каким набрана соседняя
запись исхода D-06 ↔ QUAL-06. От этого зависит граница, по которой четвёртое утверждение вырезает
подраздел.
  </action>

  <verify>
    <automated>N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^[-+].*\| (FORM|FOUND|QUAL|FETCH)-[0-9]+ \|" || true); [ "$N" = "0" ]</automated>
    <automated>N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^[-+]- \[[ x]\] \*\*" || true); [ "$N" = "0" ]</automated>
    <automated>for R in FORM-02 QUAL-01 QUAL-02 QUAL-06; do N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^\+- \*\*$R\*\*" || true); [ "$N" = "1" ] || exit 1; done</automated>
    <automated>RQ=.planning/REQUIREMENTS.md; SEC=$(awk '/^### /{f=0} /^#### /{f=0} /^#### Второй круг закрытия/{f=1} f' "$RQ"); for Q in FORM-02 QUAL-01 QUAL-02 QUAL-06; do N=$(printf '%s\n' "$SEC" | grep -cE "^- \*\*$Q\*\*" || true); [ "$N" = "1" ] || exit 1; done</automated>
    <automated>N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^\+\|.*(FORM|FOUND|QUAL|FETCH)-[0-9]+" || true); [ "$N" = "0" ]</automated>
    <automated>D=$(git diff --stat -- .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-0*-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-10-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-11-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-0*-SUMMARY.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-10-SUMMARY.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-11-SUMMARY.md); [ -z "$D" ]</automated>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
⚠️ **Каждая команда блока проверки СПОСОБНА ЗАВЕРШИТЬСЯ НЕНУЛЕВЫМ КОДОМ, и это здесь не
формальность.**
Прежняя редакция трёх из четырёх команд печатала ЧИСЛО и всегда выходила с нулём: конвейер
`… | wc -l` и `git diff --stat` зеленеют при любом значении. Гейт, который не может покраснеть,
стоял ровно на том единственном месте, где этот план вообще способен навредить, — на запрете
преждевременного перевода состояния требования (та самая ошибка, что потребовала отката коммитом
`183511c`). Теперь число уходит в переменную, а исход даёт СЛИЧЕНИЕ.

⚠️ **`|| true` стоит на `grep`, а не на присваивании, питающем сличение.** `grep -c` при нуле
совпадений выходит с кодом 1 — это его нормальный ответ «ноль», а не отказ; проглатывается именно
он, внутри подстановки. Само `[ "$N" = "0" ]` не защищено ничем и краснеет как положено.

⚠️ **АНТИВАКУУМ — две команды, и без них проверки неприкосновенности прошли бы при полном
отсутствии работы.** «Ноль изменённых строк таблицы состояний» верно и для файла, которого никто
не открывал. Поэтому отдельно утверждается, что работа СДЕЛАНА и сделана именно та:
(а) по диффу — на каждое из четырёх требований добавлена ровно одна строка-заголовок записи вида
`- **FORM-02**`, по одной, не по две и не ни одной; (б) по САМОМУ ФАЙЛУ, независимо от состояния
индекса — вырезанный подраздел второго круга несёт ровно одну такую строку на каждое из четырёх.
Посторонняя правка файла ни одному из двух не удовлетворяет; четыре табличные строки с теми же
идентификаторами покрасили бы проверку неприкосновенности.

⚠️ **Отдельная команда держит ФОРМУ записи** (список, а не таблица) — машинное требование самого
раздела свидетельств, `.planning/REQUIREMENTS.md` около строки 193.

- Строки таблицы состояний не изменены: `N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^[-+].*\| (FORM|FOUND|QUAL|FETCH)-[0-9]+ \|" || true); [ "$N" = "0" ]` → код `0`.
- Флажки требований не изменены: `N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^[-+]- \[[ x]\] \*\*" || true); [ "$N" = "0" ]` → код `0`.
- **Антивакуум по диффу:** дифф по `.planning/REQUIREMENTS.md` непуст и непуст ИМЕННО работой этого плана — `for R in FORM-02 QUAL-01 QUAL-02 QUAL-06; do N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^\+- \*\*$R\*\*" || true); [ "$N" = "1" ] || exit 1; done` → код `0`. Без него два предыдущих утверждения зеленели бы при полном отсутствии работы. Прогон — по рабочему дереву, ДО коммита задачи (см. пункт 5 действия).
- **Антивакуум по файлу** (от состояния индекса не зависит): `RQ=.planning/REQUIREMENTS.md; SEC=$(awk '/^### /{f=0} /^#### /{f=0} /^#### Второй круг закрытия/{f=1} f' "$RQ"); for Q in FORM-02 QUAL-01 QUAL-02 QUAL-06; do N=$(printf '%s\n' "$SEC" | grep -cE "^- \*\*$Q\*\*" || true); [ "$N" = "1" ] || exit 1; done` → код `0`. Утверждение проверено на живом файле с подставленным подразделом: без подраздела вырезка пуста и оно краснеет, при четырёх записях — зеленеет. Посторонняя правка файла его не удовлетворяет: считается только то, что лежит ВНУТРИ границ подраздела второго круга.
- `.planning/REQUIREMENTS.md` содержит подстроку `Второй круг закрытия (планы 09-10` и по каждому из четырёх требований — три поля: `машинная проверка`, `наблюдённый признак`, `вердикт`.
- Подраздел набран списком, а не таблицей: `N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -cE "^\+\|.*(FORM|FOUND|QUAL|FETCH)-[0-9]+" || true); [ "$N" = "0" ]` → код `0`.
- Каждый вердикт — одно из двух слов, `заработано` либо `не заработано`, и ни одного третьего.
- Каждая колонка «наблюдённый признак» несёт наблюдение из таблицы отметок второго круга, а не пересказ ожидания; для FORM-02 в ней присутствует выписанное название строки.
- `09-UAT.md`: поле `status` больше не описывает первый круг — в шапке есть строка о втором круге с датой и браузером.
- Отчёт верификации, планы и сводки 09-01 … 09-11 не тронуты, и это УТВЕРЖДАЕТСЯ, а не печатается: `D=$(git diff --stat -- .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-0*-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-10-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-11-PLAN.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-0*-SUMMARY.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-10-SUMMARY.md .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-11-SUMMARY.md); [ -z "$D" ]` → код `0`. ⚠️ Перечень назван поимённо до 09-11 включительно: прежний образец охватывал только 09-0*, и правка планов второго круга прошла бы мимо гейта.
- `git diff --stat app/ tests/` пуст: файлов приложения и тестов этот план не трогает вовсе.
- `uv run pytest tests/ -q -p no:randomly` → `0` failed; число пройденных не меньше `2649`.
  </acceptance_criteria>

  <done>Оба круга свидетельств лежат в долгоживущем месте: на каждое требование машинная проверка,
наблюдённый признак и вердикт. Состояния требований не тронуты — их переводит повторная
верификация, читающая этот раздел. Артефакт обхода несёт честный статус, стенд убран.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| посевной стенд → живая локальная база | обход сеет строки рядом с настоящими данными разработчика |
| человек на приёмке → артефакт отметок | наблюдение попадает в долгоживущую запись, которую Фаза 15 унаследует как факт |
| артефакт свидетельств → повторная верификация | раздел читается машиной, и его форма (список, не таблица) есть машинное требование |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12-01 | Repudiation | отметка без наблюдённого признака | high | mitigate | правило раздела прямое: отметка без наблюдённого признака закрытием не считается; проверка возобновления сличает ячейки машинно и отдельно требует выписанного названия строки в 3.5-Б |
| T-09-12-02 | Tampering | посевные строки, оставшиеся в живой базе после обхода | medium | mitigate | посев ведётся строго под префиксом `UAT9-`, уборка выполняется командой раздела «Как убрать стенд», вывод проверки уборки приводится в сигнале возобновления |
| T-09-12-03 | Tampering | преждевременный перевод состояния требования | high | mitigate | запрет плана плюс машинная проверка диффом в УТВЕРЖДАЮЩЕЙ форме: число уходит в переменную, исход даёт сличение (`[ "$N" = "0" ]`), и команда способна выйти ненулевым кодом — печать числа через `wc -l` зеленела при любом значении и гейтом не была. Плюс антивакуумное утверждение: дифф непуст ИМЕННО работой плана (по одной добавленной строке-элементу списка на каждое из четырёх требований), иначе «ноль изменённых строк» прошло бы и при полном отсутствии работы. Это ровно та ошибка, что потребовала отката коммитом `183511c` |
| T-09-12-04 | Denial of service | проверка 6.3 требует отказа запроса, вызванного отключением сети | low | accept | отказ вызывается переключателем DevTools на локальном стенде; боевые данные не задействованы |
| T-09-12-05 | Information disclosure | отметка называет имена реальных групп разработчика | low | accept | стенд посеян под собственным префиксом, и выписывать положено название ПОСЕЯННОЙ строки, а не чужой |
| T-09-12-06 | Repudiation | подраздел свидетельств, набранный таблицей, покрасил бы собственный гейт | medium | mitigate | форма записи задана прямым требованием и проверяется отдельным критерием приёмки |
| T-09-12-SC | Tampering | установка пакетов | low | accept | план не выполняет ни одной команды менеджера пакетов и не трогает ни одного файла приложения |

**Блокирующий порог — `high`.** Обе угрозы этого уровня (T-09-12-01, T-09-12-03) снабжены
машинными проверками, и задача 3 без них не считается выполненной.
</threat_model>

<verification>
1. Таблица отметок второго круга заполнена наблюдённым признаком по всем пяти проверкам, и
   ячейка 3.5-Б несёт выписанное название строки.
2. Стенд `UAT9-` убран, вывод команды проверки уборки приведён.
3. Ни одна строка таблицы состояний и ни один флажок `.planning/REQUIREMENTS.md` не изменены —
   проверено диффом.
4. Подраздел свидетельств второго круга набран списком и несёт три поля на каждое из четырёх
   требований.
5. `09-VERIFICATION.md`, планы и сводки 09-01 … 09-11 не тронуты.
6. `uv run pytest tests/ -q -p no:randomly` → зелено целиком.
</verification>

<success_criteria>
- Четыре наблюдения, устаревшие или не проводившиеся ни разу, сняты с ОТГРУЖЕННОГО дерева и
  записаны с датой, браузером и ОС.
- Опорный признак обхода «ответ 200 и чистая консоль» либо восстановлен, либо его нерабочая часть
  названа поимённо вместе с объявленной записью, под которой она живёт.
- Оба круга свидетельств лежат в `.planning/REQUIREMENTS.md` в форме, которую читает повторная
  верификация, и ни одно состояние требования этим планом не переведено.
- Ни одна отметка не подписана под ненаблюдённым.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-12-SUMMARY.md` when done
</output>
