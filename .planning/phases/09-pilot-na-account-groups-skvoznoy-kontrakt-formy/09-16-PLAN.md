---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 16
type: execute
wave: 4
depends_on: ["13", "14", "15"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
autonomous: false
requirements: [FORM-02, FORM-09, QUAL-01, QUAL-02, QUAL-06]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Состояния требований переводятся ПО ЗАКРЫТЫМ РАЗРЫВАМ, а не по рекомендации отчёта: у каждой изменённой клетки столбца `Status` названы закрытый пункт `gaps` и ЗЕЛЁНАЯ машинная проверка, воспроизводимая командой"
    - "Перевод происходит ПОСЛЕ обоих закрытий, а не рядом с ними: план стоит в волне 4 и зависит от 09-13, 09-14 и 09-15. Отметка «выполнено» внутри незакрытой фазы — ровно та ошибка, что породила третий пробел фазы и потребовала отката коммитом `183511c`"
    - "Требование, у которого зелёного признака нет, НЕ переводится, и причина называется поимённо: молчаливое «оставили как было» неотличимо от забытой работы"
    - "Третий круг раздела свидетельств набран СПИСКОМ, а не таблицей: проверка неприкосновенности состояний сличает строки ТАБЛИЦЫ, и табличные строки с теми же идентификаторами требований покрасили бы собственный гейт плана — та же причина, по которой списочными набраны первый и второй круги"
    - "Отступление QUAL-01 от буквы «на каждой форме письма» закрывается РЕШЕНИЕМ ВЛАДЕЛЬЦА, а не суждением исполнителя: запись владельца о том, что overrides не требуется, писалась планом 09-09, когда форма ещё жила по общему умолчанию цели блокировки, — снятие атрибута произошло ПОСЛЕ неё и той записью не покрыто"
    - "Правка `09-VERIFICATION.md`, если владелец её выбрал, ТОЛЬКО ДОБАВЛЯЮЩАЯ: ни одна существующая строка вердиктов и таблиц не меняется и не удаляется, и это утверждается числом удалённых строк, равным нулю"
    - "Прогон свидетельств выполняется ЗАНОВО на сегодняшнем дереве, а не переписывается из сводок планов: команда и её вывод приводятся дословно вместе с HEAD, на котором сняты"
    - "Флажки в теле файла требований остаются нетронутыми: их снимает не план, а закрытие фазы"
    - "Каждая команда `<verify>` этого плана СПОСОБНА ПОКРАСНЕТЬ: там, где стояла печать счётчика с `|| echo`, стоит сличение с ожидаемым значением. Команда, завершающаяся нулём при любом значении счётчика, гейтом не является, а сличение, живущее только в `acceptance_criteria`, до исполнителя может не дойти, если `<verify>` отчиталась зелёным"
    - statement: "Повторная верификация согласится с переведёнными состояниями, потому что каждый перевод опирается на тот же машинный признак, который прочтёт она"
      verification: backstop
  artifacts:
    - ".planning/REQUIREMENTS.md — подраздел «Третий круг закрытия (планы 09-13 … 09-15)» СПИСКОМ: на каждое из пяти требований машинная проверка, признак и вердикт"
    - ".planning/REQUIREMENTS.md — столбец `Status` пяти строк Фазы 9, переведённый по названным зелёным признакам либо оставленный с названной причиной"
    - ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md — блок `overrides` во frontmatter, ДОБАВЛЕННЫЙ дословно по образцу самого отчёта, если владелец выбрал эту ветвь"
  key_links:
    - "закрытый пункт gaps → перевод состояния требования: рвётся — требование объявлено выполненным внутри незакрытой фазы, и откат состояний повторяется третий раз"
    - "раздел свидетельств → повторная верификация: рвётся — она читает состояния, за которыми не стоит воспроизводимого признака, и вынуждена либо поверить, либо откатить"
    - "решение владельца по QUAL-01 → перевод QUAL-01: рвётся — отступление от буквы требования закрывается суждением исполнителя, чего фаза себе запретила"
  # ⚠️ ФОРМА ЗАПИСЕЙ НИЖЕ — ЗАФИКСИРОВАННЫЙ ОБРАЗЕЦ ПРОЕКТА, А НЕ ДРЕЙФ ЭТОГО КРУГА.
  # Дескрипторы `status: flagged-unverified` и `verification: none` набираются так с прошлых
  # кругов фазы, и ПРИЁМКА ЧИТАЕТ ИХ ИМЕННО В ЭТОЙ ФОРМЕ: `09-VERIFICATION.md:328`,
  # §«Prohibitions (judgment-tier, `verification: none`)», сличает их по дескрипторам и выносит
  # вердикт по ним. Descriptor-less форма общей спецификации здесь СОЗНАТЕЛЬНО не применяется:
  # разнобой между кругами хуже любой из двух форм, и следующая сверка формы обязана считать это
  # образцом, а не дрейфом. Все четыре плана третьего круга (09-13, 09-14, 09-15, 09-16) набраны
  # ОДИНАКОВО.
  prohibitions:
    - statement: "требование не переводится в Complete по рекомендации отчёта верификации: перевод требует названного зелёного признака и названного закрытого пункта gaps"
      status: flagged-unverified
      verification: none
    - statement: "вердикты и таблицы 09-VERIFICATION.md не переписываются: допустимо ТОЛЬКО добавление блока overrides во frontmatter, и только по ответу владельца"
      status: flagged-unverified
      verification: none
    - statement: "флажки в теле .planning/REQUIREMENTS.md не проставляются этим планом"
      status: flagged-unverified
      verification: none
    - statement: "раздел свидетельств не переводится в табличную форму"
      status: flagged-unverified
      verification: none
    - statement: "исполнитель не выбирает ветвь QUAL-01 сам и не подставляет ожидаемый вывод команды вместо полученного"
      status: flagged-unverified
      verification: none
    - statement: "прогоны колонки «машинная проверка» не переписываются из сводок планов 09-13 … 09-15: команды исполняются заново на сегодняшнем дереве"
      status: flagged-unverified
      verification: none
    - statement: "планы 09-01 … 09-15 и их сводки не правятся, не перенумеровываются и не отменяются"
      status: flagged-unverified
      verification: none
    - statement: "код приложения и тесты этим планом не правятся вовсе"
      status: flagged-unverified
      verification: none
    - statement: "долговых маркеров в артефактах не появляется"
      status: flagged-unverified
      verification: none
---

<objective>
Перевести состояния требований Фазы 9 — **после того, как оба пункта `gaps` закрыты**, и **по
закрытым пунктам**, а не по рекомендации отчёта.

Purpose: `09-VERIFICATION.md` несёт `requirement_state_recommendation` (перевести FORM-09, QUAL-02,
QUAL-06 в `Complete`, держать FORM-02 и QUAL-01), и прямо говорит, что рекомендация НЕ ИСПОЛНЕНА
этим прогоном: «отмечать требования Complete внутри незакрытой фазы значило бы то же самое, что
фаза себе запретила». Получается тупик: верификация не переводит, потому что фаза не закрыта; планы
не переводят, потому что это дело верификации. Тупик разрывается здесь — переводом, который стоит
ПОСЛЕ закрытия обоих разрывов и опирается на воспроизводимый признак, а не на чужую рекомендацию.

Второй предмет плана — **отступление QUAL-01**. Исход требования достигнут и наблюдён (6.2-Б: один
запрос на два быстрых нажатия; Отмена нажимаема), цель блокировки «только `find button[type=submit]`»
исполнена буквально. Но оговорка «на каждой форме письма» на ПИЛОТНОЙ форме не выполнена — атрибута
там нет вовсе, защиту несёт `hx-sync="this:drop"`. Это осознанное решение владельца (`record-kind`,
план 09-10), объявленное записью `DISABLED_ELT_EXCEPTIONS` с летописью и проверяемым полем
`callers`; забытой работой оно не является. Но запись владельца «overrides этот исход не требует»
писалась планом 09-09, **когда форма ещё жила по общему умолчанию**, — снятие атрибута произошло
ПОСЛЕ неё и той записью не покрыто. Решение принадлежит владельцу.

Output: третий круг раздела свидетельств СПИСКОМ; ответ владельца по QUAL-01; переведённые
состояния, у каждого из которых назван закрытый пункт `gaps` и зелёная команда.

⚠️ **`autonomous: false`** — один останов решения. Закрыть расхождение с буквой требования
суждением исполнителя — то же самое, что закрыть дефект удалением записи о нём.

⚠️ **Волна 4.** План зависит от 09-13, 09-14 и 09-15 не по файлам, а по СУЩЕСТВУ: свидетельство,
снятое до закрытия разрывов, описывает не то дерево.

⚠️ **`graphify update .` ЖИВЁТ НЕ ЗДЕСЬ, И ЭТО НАЗВАНО, А НЕ УМОЛЧАНО.** `CLAUDE.md` §graphify
требует обновления графа после правки кода, и `09-RESEARCH.md:86` адресовало это планировщику
формулировкой «включить в последний план фазы». Последним планом третьего круга стал ЭТОТ, но он
кода не трогает и прямой прохибицией («код приложения и тесты этим планом не правятся вовсе»)
запрещает себе его трогать — поручение, буквально исполненное здесь, противоречило бы собственной
прохибиции плана по своему поводу и обновляло бы граф ДО последней правки, а не после. Поэтому
обязательство перенесено в **задачу 3 плана 09-15** — последнюю задачу последнего плана, который
правит `app/`, и исполняется там ПОСЛЕ зелёной суиты. Прохибиция этого плана остаётся в силе без
поправки: команда графа правит `graphify-out/`, а не `app/` и не `tests/`, и критерий
«`git diff --stat app/ tests/` пуст» ею не нарушается.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-12-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-13-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-14-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-15-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Имён в коде этот план не заводит вовсе — он не трогает ни одного файла приложения и ни одного
теста. Заводится только в артефактах планирования:

- В `.planning/REQUIREMENTS.md`: подраздел **«Третий круг закрытия (планы 09-13 … 09-15)»** внутри
  уже существующего раздела «Фаза 9 — свидетельства к пересмотру состояний», СПИСКОМ; изменённые
  значения столбца `Status` пяти строк Фазы 9.
- В `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md`
  (только при ветви `accept-override`): ключ frontmatter **`overrides`** с одной записью, набранной
  ДОСЛОВНО по образцу, приведённому в самом отчёте (§«Образец записи `overrides`»), с
  заполненными `accepted_by` и `accepted_at`.

⚠️ **Матрица покрытия внешнего API не заводится:** детерминированный обход области фазы вернул
`detected: false`. ⚠️ **Останов допущений архитектуры не сработал** (`detected: false`). ⚠️ **Гейт
схемы БД молчит:** файлов приложения план не трогает. ⚠️ **Пакетов план не устанавливает.**
</artifacts_this_phase_produces>

<flagged_assumptions>
## Допущения, оставленные ОТКРЫТЫМИ

Все пять `unclassified`-рёбер обхода (**FORM-02**, **FORM-09**, **QUAL-02**, **QUAL-06** и
неклассифицированная половина **QUAL-01**) остаются **`unresolved`** и названы здесь, а не
отброшены молча: обход категории им не назначил, и автоматически закрытыми они не помечаются. Этот
план их и не закрывает — он фиксирует СОСТОЯНИЕ требований по признакам, которые закрывали планы
09-13 … 09-15, и каждое такое ребро остаётся предметом повторной верификации.

Утверждение «повторная верификация согласится с переведёнными состояниями» остаётся
`verification: backstop`: подтвердить его может только сама верификация, и исполнитель за неё не
расписывается.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Задача 1: третий круг раздела свидетельств — прогоны снимаются ЗАНОВО, состояния НЕ трогаются</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
- `.planning/REQUIREMENTS.md` целиком, и особенно:
  - таблица `| Requirement | Phase | Status |` (около строк 120-150) — действующие значения пяти
    строк Фазы 9 читать ИЗ ФАЙЛА, а не из этого плана;
  - раздел «Фаза 9 — свидетельства к пересмотру состояний», подразделы первого и второго круга
    целиком — **форма записи, которую третий круг повторяет**: на каждое требование три позиции
    («машинная проверка», «наблюдённый признак», «вердикт»), и цена, если она есть, названа
    отдельным абзацем с пометкой;
  - шапки обоих подразделов — там дословно записано, ПОЧЕМУ подраздел набран списком, а не
    таблицей, и почему состояний он не переводит. Третий круг обязан нести такую же шапку с
    поправкой на то, что перевод делает СЛЕДУЮЩАЯ задача этого же плана;
  - §«ЗАПИСЬ ИСХОДА D-06 ↔ QUAL-06 — ОРИГИНАЛ (план 09-09, 2026-08-30)» — оригинал записи и
    названная в нём цена; этот раздел не правится.
- `09-13-SUMMARY.md`, `09-14-SUMMARY.md`, `09-15-SUMMARY.md` — какие правила заведены и как они
  называются. ⚠️ Читать ради ИМЁН правил, а не ради их выводов: выводы снимаются заново.
- `09-VERIFICATION.md`: блок `gaps` целиком (оба пункта и их `missing`), блок
  `requirement_state_recommendation`, и §«Requirements Coverage» — там на каждое требование
  записано, что именно держало его в текущем состоянии.
- `09-12-SUMMARY.md` — как второй круг доказал неприкосновенность состояний командой, СПОСОБНОЙ
  ПОКРАСНЕТЬ (число изменённых строк таблицы в переменную, сличение, плюс антивакуумное
  утверждение о непустом диффе). Задача 1 повторяет тот же приём.
  </read_first>

  <action>
**1. Прогоны — заново, на сегодняшнем дереве.** Для каждого из пяти требований исполнить команду
`uv run pytest` с точной выборкой правил, относящихся к этому требованию, и записать в подраздел
КОМАНДУ и её ВЫВОД дословно (число пройденных и отклонённых). Переписывать числа из сводок планов
09-13 … 09-15 запрещено: сводка описывает дерево на момент своего плана, а свидетельство обязано
описывать дерево на момент перевода. Записать HEAD, на котором сняты прогоны.

**2. Состав выборок — предметный, а не «весь файл».** Для FORM-02 — правила курсора и чередования,
обе пары деградации, правило равенства наборов полей форм удаления, правило маршрута порции. Для
FORM-09 — гейты разметки и перечни исключений с их числами. Для QUAL-01 — правила идемпотентности
и холостого удаления, правило состояния тумблера, правило достижимости цели блокировки. Для
QUAL-02 — правила индикатора. Для QUAL-06 — правило стабильности идентификатора тумблера во
фрагменте. Точные имена брать из файлов тестов, а не из памяти.

**3. Позиция «наблюдённый признак».** Наблюдений третий круг не заводит: браузерный стенд не
поднимается, и обход не проводится. В позиции ставить ссылку на уже снятые наблюдения второго круга
(3.3-Б, 6.2-Б, 3.5-Б, 6.3, 7.3) с прямым указанием, что они сняты ДО правок третьего круга, и
называть, какие из них правки третьего круга могли обесценить, — если такие есть. Молчать об этом
нельзя: наблюдение, снятое с изменённого механизма, есть ровно то, из-за чего понадобился второй
круг.

**4. Отдельная строка про опорный признак «200 и чистая консоль».** По образцу второго круга —
отдельной строкой, потому что одному требованию он не принадлежит. Записать, чем он теперь
держится: правилом прозы плана 09-14 и записью `OOB_TARGET_EXCEPTIONS` с назначенной фазой; и
назвать поимённо, какие остаточные строки консоли объявлены и на каком положении экрана они
достижимы.

**5. Отдельная строка про закрытые пункты `gaps`.** На каждый из двух пунктов отчёта — какой
правкой закрыт, каким правилом это утверждается и какой командой воспроизводится.

**6. Состояний задача НЕ трогает.** Неприкосновенность утверждается тем же приёмом, что во втором
круге: число изменённых строк ТАБЛИЦЫ уходит в переменную, исход даёт сличение с нулём, и рядом
стоит антивакуумное утверждение — дифф файла непуст ИМЕННО работой задачи. Печать числа конвейером
без сличения гейтом не является.

**7. Форма — СПИСОК.** Табличные строки с идентификаторами требований покрасили бы собственную
проверку неприкосновенности; шапка подраздела обязана называть эту причину, как её называют шапки
первого и второго кругов.
  </action>

  <verify>
    <automated>N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -c '^[+-]|.*| Phase 9 |'); echo "изменённых строк таблицы: $N"; [ "$N" = "0" ]</automated>
    <automated>A=$(git diff --numstat .planning/REQUIREMENTS.md | awk '{s+=$1} END{print s+0}'); echo "добавлено строк: $A"; [ "$A" -gt 0 ]</automated>
    <automated>grep -c "Третий круг закрытия (планы 09-13 … 09-15)" .planning/REQUIREMENTS.md</automated>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "Третий круг закрытия (планы 09-13 … 09-15)" .planning/REQUIREMENTS.md` → `1`.
- Подраздел набран списком: в его границах `grep -c "^| "` → `0` (ни одной табличной строки).
- На каждое из пяти требований в подразделе есть три позиции — строки, начинающиеся с `**машинная проверка:**`, `**наблюдённый признак:**` и `**вердикт:**`; их по пять каждой (`grep -c` в границах подраздела → `5`, `5`, `5`).
- Присутствуют две отдельные строки: про опорный признак «200 и чистая консоль» и про закрытые пункты `gaps`.
- Прогоны сняты заново: в подразделе записан HEAD, и он совпадает с `git rev-parse --short HEAD` на момент задачи.
- Состояния не тронуты: число изменённых строк таблицы, содержащих `| Phase 9 |`, равно `0` — и это получено СЛИЧЕНИЕМ, а не печатью.
- Антивакуум — СЛИЧЕНИЕМ, а не печатью: `A=$(git diff --numstat .planning/REQUIREMENTS.md | awk '{s+=$1} END{print s+0}'); [ "$A" -gt 0 ]` → нулевой код. Команда, которая печатает число и завершается нулём при любом его значении, гейтом не является: пустой дифф прошёл бы её зелёным.
- Флажки в теле файла не тронуты: `git diff .planning/REQUIREMENTS.md` не содержит строк, начинающихся с `+- [x]` или `-- [ ]`.
- Код приложения и тесты не тронуты: `git diff --stat app/ tests/` пуст.
- `uv run pytest tests/ -q -p no:randomly` → `0 failed`.
  </acceptance_criteria>

  <done>Третий круг свидетельств записан списком, прогоны сняты заново на сегодняшнем дереве с
названным HEAD, состояния и флажки не тронуты, и неприкосновенность доказана сличением, способным
покраснеть.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Задача 2: РЕШЕНИЕ ВЛАДЕЛЬЦА — чем закрывается расхождение QUAL-01 с буквой «на каждой форме письма»</name>

  <decision>Записывается ли принятое отступление блоком `overrides` в отчёте верификации, правится
ли текст самого требования — или QUAL-01 остаётся в `Gaps Found` до следующего прогона.</decision>

  <read_first>
- **ПЕРВЫМ — вывод задачи 1:** какие прогоны зелены и что записано в третьем круге свидетельств.
- `09-VERIFICATION.md` §«Requirements Coverage», строка **QUAL-01** целиком — там названо ровно
  то, что решается: исход требования достигнут и наблюдён, цель блокировки исполнена буквально, а
  оговорка «на каждой форме письма» на пилотной форме не выполнена, потому что атрибута там нет
  вовсе.
- `09-VERIFICATION.md` §«Образец записи `overrides` — если владелец принимает отступление QUAL-01»
  — **готовый YAML, который ветвь `accept-override` берёт ДОСЛОВНО**, дописав `accepted_by` и
  `accepted_at`.
- `.planning/REQUIREMENTS.md`, текст требования **QUAL-01** (около строки 39) и §«ЗАПИСЬ ИСХОДА
  D-06 ↔ QUAL-06 — ОРИГИНАЛ» — запись владельца, сделанная планом 09-09, и названная в ней цена.
- `tests/test_templates/test_htmx_markup_gates.py` — `DISABLED_ELT_EXCEPTIONS`, её летопись (в
  частности строка «2 → 2, план 09-09 — ЧИСЛО НЕ ДВИНУЛОСЬ, А ЗАПИСЬ СМЕНИЛА РОД») и проверяемое
  поле `callers`: отступление объявлено и ПРОВЕРЯЕТСЯ, а не заявлено.
- `09-10-SUMMARY.md` — ответ владельца `record-kind` дословно и его основание.
  </read_first>

  <context>
Отступление выглядит **осознанным, а не забытым**: решение принято владельцем (`record-kind`,
план 09-10), механизм замены (`hx-sync="this:drop"`) наблюдён работающим (6.2-Б: один запрос на два
быстрых нажатия), запись перечня несёт обоснование, летопись и проверяемое поле `callers`.

**Что делает решение необходимым.** Запись владельца «overrides этот исход не требует» писалась
планом 09-09, когда форма тумблера ещё жила по ОБЩЕМУ УМОЛЧАНИЮ цели блокировки. Снятие атрибута
произошло планом 09-10, то есть ПОСЛЕ этой записи, и той записью не покрыто. Закрыть образовавшийся
зазор суждением исполнителя нельзя: расхождение с буквой требования — предмет решения владельца, и
именно это фаза себе предписала.

**Что уже известно наверняка, без предположений:**

- исход требования («двойное нажатие не создаёт второй записи») ДОСТИГНУТ и НАБЛЮДЁН — 6.2-Б;
- кнопка Отмены не блокируется никогда — наблюдено 6.3, закреплено `test_cancel_is_never_disabled`;
- серверная идемпотентность цела и планом 09-15 УСИЛЕНА: присвоение присланного значения
  идемпотентно по построению, тогда как прежняя инверсия давала идемпотентность только в паре;
- цель блокировки «только `find button[type=submit]`» исполнена буквально: печатаемых вхождений в
  дереве два, оба именно этой цели;
- отступление объявлено `DISABLED_ELT_EXCEPTIONS` с летописью и полем `callers`, которое МЕРЯЕТСЯ
  по дереву — вызывающий, молча начавший или переставший передавать своё значение, краснеет.

**Обратимость.** Рейтинг `costly`: запись `overrides` попадает во frontmatter отчёта верификации,
который ПЕРЕГЕНЕРИРУЕТСЯ следующим прогоном, — значит она обязана быть воспроизведена этим прогоном
из ответа владельца, а не «жить сама». Правка текста требования дороже: `.planning/REQUIREMENTS.md`
читают все последующие фазы, и переформулированное требование меняет то, что Фаза 15 будет сверять
на 47 формах.
  </context>

  <options>
    <option id="accept-override">
      <name>(I) Отступление ПРИНИМАЕТСЯ записью `overrides` во frontmatter отчёта верификации</name>
      <pros>Текст требования не двигается, и Фаза 15 продолжает сверять 47 форм по прежней букве. Отступление названо, обосновано, привязано к наблюдению (7.2 — строка в консоли на каждое переключение; 6.2-Б — один запрос на два нажатия) и к проверяемой записи перечня. YAML уже написан самим отчётом — исполнителю остаётся дописать имя принявшего и время</pros>
      <cons>Запись живёт во frontmatter файла, который перегенерируется следующим прогоном верификации: если ответ владельца не сохранён ещё где-то, она исчезнет вместе с файлом. Поэтому ветвь обязывает продублировать ответ в `.planning/REQUIREMENTS.md`, в третьем круге свидетельств, — иначе решение владельца окажется записанным только в перегенерируемом артефакте</cons>
    </option>
    <option id="amend-requirement">
      <name>(II) Правится ТЕКСТ требования: буква приводится к тому, что требование на самом деле защищает</name>
      <pros>Расхождение исчезает у корня: требование перестаёт называть МЕХАНИЗМ («`hx-disabled-elt` на каждой форме письма») там, где защищает ИСХОД («двойное нажатие не создаёт второй записи»), и на форме без достижимой цели блокировки перестаёт быть невыполнимым по построению. Фаза 15 получает формулировку, которую можно сверить на всех 47 формах, включая те, у которых узла отправки нет</pros>
      <cons>Требование читают все последующие фазы, и его переформулировка меняет предмет сводного обхода Фазы 15 — то есть цена платится не здесь. Прежняя формулировка обязана быть НЕ СТЁРТА, а названа устаревшей с указанием того, что её обнаружило (образец — записи D-30 и D-32 в `.planning/STATE.md`). ⚠️ Ветвь названа планировщиком; отчёт верификации предлагал только запись `overrides`</cons>
    </option>
    <option id="hold">
      <name>(III) QUAL-01 остаётся в `Gaps Found`, и решение переносится на повторную верификацию</name>
      <pros>Решение не принимается второпях в конце фазы; повторная верификация видит и суиту, и заполненные отметки, и все три круга свидетельств разом</pros>
      <cons>Фаза не закрывается: одно требование из пяти остаётся в `Gaps Found`, и следующий прогон встанет ровно перед этим же вопросом, только с ещё одним кругом позади. Тупик «верификация не переводит, потому что фаза не закрыта; план не переводит, потому что это дело верификации» воспроизводится в четвёртый раз</cons>
    </option>
  </options>

  <reversibility rating="costly">
Ветвь (I) обратима дёшево — блок frontmatter снимается. Ветвь (II) обратима дорого: текст
требования читают Фазы 10-15, и его переформулировка меняет предмет сводного обхода 47 форм.
Ветвь (III) обратима, но переносит стоимость на следующий прогон целиком.
  </reversibility>

  <resume-signal>Ответить одним из: `accept-override`, `amend-requirement`, `hold`. При
`accept-override` — назвать `accepted_by` (имя принявшего) и подтвердить, что цена принята. При
`amend-requirement` — назвать новую формулировку требования дословно.</resume-signal>

  <acceptance_criteria>
- Ответ владельца — РОВНО ОДИН из трёх идентификаторов, и он записан в сводке плана дословно.
- Для `accept-override` названо имя принявшего; YAML взят из §«Образец записи `overrides`» отчёта ДОСЛОВНО, без переписывания формулировок.
- Для `amend-requirement` новая формулировка требования приведена дословно, и названо, что делать с прежней (не стирать, а назвать устаревшей).
- Для `hold` названо, что именно должно появиться к следующему прогону, чтобы вопрос закрылся.
- Исполнитель не выбирал ветвь сам: ответ владельца процитирован, а не выведен.
  </acceptance_criteria>

  <done>Владелец назвал ветвь и основание. Задача 3 применяет ровно её.</done>
</task>

<task type="auto">
  <name>Задача 3: состояния переведены ПО ЗАКРЫТЫМ РАЗРЫВАМ — каждая клетка со своим признаком, каждая непереведённая со своей причиной</name>

  <files>.planning/REQUIREMENTS.md, .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md</files>

  <read_first>
- Ответ владельца на останов задачи 2 и записанный задачей 1 третий круг свидетельств.
- `.planning/REQUIREMENTS.md`, таблица `| Requirement | Phase | Status |` — действующие значения
  пяти строк Фазы 9 читать ИЗ ФАЙЛА.
- `09-VERIFICATION.md`: `requirement_state_recommendation` (что рекомендовано и с какой
  оговоркой), блок `gaps` (оба пункта), §«Requirements Coverage» (что держало каждое требование).
- `09-VERIFICATION.md` §«Образец записи `overrides`» — при ветви `accept-override` YAML берётся
  оттуда дословно.
- `.planning/STATE.md`, записи D-30 и D-32 — **образец обращения с отменённой формулировкой**:
  прежняя не стирается, а называется ошибочной с указанием того, что её обнаружило.
  </read_first>

  <action>
**1. Правило перевода — одно и жёсткое.** Клетка `Status` меняется ТОЛЬКО если рядом, в третьем
круге свидетельств, названы (а) закрытый пункт `gaps` либо прямое указание, что требование ни
одним пунктом не держалось, и (б) команда `uv run pytest`, зелёная на сегодняшнем дереве, и (в) при
наличии отступления — решение владельца. Нет хотя бы одного — клетка НЕ меняется, а причина
пишется отдельной строкой того же подраздела. Рекомендация отчёта основанием НЕ является и в
качестве основания не записывается.

**2. Что ожидается по каждому требованию** (значения ставятся ПО ПРИЗНАКАМ, а не по этому списку —
он говорит, где искать, а не что написать):
- **FORM-09**, **QUAL-02**, **QUAL-06** — отчёт рекомендует `Complete`; проверить, что признаки
  третьего круга это подтверждают, и что правки планов 09-13 … 09-15 их не обесценили.
- **FORM-02** — держалось пунктом `gaps` про CR-01 и замечанием WR-03; оба закрыты планами 09-13
  и 09-15. Признак — зелёный тест чередования плюс правило равенства наборов полей форм.
- **QUAL-01** — держалось вторым пунктом (цена холостого пути) и зазором в записи владельца;
  первый закрыт планом 09-14, второй — ответом на останов задачи 2.

**3. Ветвь `accept-override`.** Дописать во frontmatter `09-VERIFICATION.md` ключ `overrides` с
одной записью, взятой из §«Образец записи `overrides`» ДОСЛОВНО, заполнив `accepted_by` именем из
ответа владельца и `accepted_at` временем в формате ISO. **Правка ТОЛЬКО ДОБАВЛЯЮЩАЯ:** ни одна
существующая строка файла не меняется и не удаляется. Ответ владельца ПРОДУБЛИРОВАТЬ в третьем
круге свидетельств `.planning/REQUIREMENTS.md` — отчёт перегенерируется, и решение, записанное
только в нём, исчезнет вместе с ним.

**4. Ветвь `amend-requirement`.** Переписать текст требования в `.planning/REQUIREMENTS.md`
дословно по ответу владельца; прежнюю формулировку НЕ СТИРАТЬ, а перенести в примечание с
пометкой, что она описывала механизм там, где требование защищает исход, и с указанием прогона,
это обнаружившего. `09-VERIFICATION.md` в этой ветви не трогается вовсе.

**5. Ветвь `hold`.** Клетка QUAL-01 не трогается; в третьем круге свидетельств пишется отдельная
строка с названным условием закрытия. Остальные четыре требования переводятся по своим признакам —
ветвь касается только QUAL-01.

**6. Флажки в теле файла не трогаются ни в одной ветви.** Их снимает закрытие фазы, а не этот план.

**7. Сводка плана.** Записать таблицей: требование → было → стало → закрытый пункт `gaps` →
команда признака. Для непереведённых — требование → осталось → причина.
  </action>

  <verify>
    <automated>git diff -U0 .planning/REQUIREMENTS.md | grep '^[+-]|.*| Phase 9 |' || echo "строки Фазы 9 не изменены"</automated>
    <automated>D=$(git diff --numstat .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md | awk '{print $2}'); echo "удалено строк отчёта: ${D:-0}"; [ "${D:-0}" = "0" ]</automated>
    <automated>F=$(git diff .planning/REQUIREMENTS.md | grep -c '^[+-]- \[' || true); echo "тронутых флажков: $F"; [ "$F" = "0" ]</automated>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- Каждая ИЗМЕНЁННАЯ клетка `Status` строки Фазы 9 имеет в третьем круге свидетельств названный закрытый пункт `gaps` и команду `uv run pytest`, зелёную на сегодняшнем дереве. Проверяется сличением: число изменённых строк таблицы Фазы 9 равно числу требований, у которых в подразделе есть строка `**вердикт:** заработано` с названным пунктом `gaps`.
- Каждая НЕИЗМЕНЁННАЯ клетка `Status` строки Фазы 9 имеет в подразделе отдельную строку с причиной; строк-причин ровно столько, сколько неизменённых клеток.
- Рекомендация отчёта не использована как основание: подстроки `requirement_state_recommendation` в добавленных строках `.planning/REQUIREMENTS.md` нет (`git diff .planning/REQUIREMENTS.md | grep -c '^+.*requirement_state_recommendation'` → `0`).
- При ветви `accept-override`: `git diff --numstat` по `09-VERIFICATION.md` показывает `0` удалённых строк; во frontmatter присутствует ключ `overrides` с непустыми `accepted_by` и `accepted_at`; тот же ответ владельца продублирован в `.planning/REQUIREMENTS.md` (`grep -c "accept-override" .planning/REQUIREMENTS.md` → не меньше `1`).
- При ветви `amend-requirement`: прежняя формулировка требования сохранена в примечании (`git diff .planning/REQUIREMENTS.md` содержит и добавленную новую формулировку, и добавленное примечание с прежней); `git diff --stat` по `09-VERIFICATION.md` пуст.
- При ветви `hold`: клетка QUAL-01 не изменена, и в подразделе есть строка с названным условием закрытия.
- Флажки не тронуты, и это ГЕЙТ, а не печать: `F=$(git diff .planning/REQUIREMENTS.md | grep -c '^[+-]- \[' || true); [ "$F" = "0" ]` → нулевой код. Прежняя форма (`grep -c … || echo`) печатала счётчик и завершалась нулём при ЛЮБОМ его значении: три тронутых флажка дали бы «3» и прошли бы зелёным, а сличение осталось бы только в критериях, до которых исполнитель мог не дойти.
- Код приложения и тесты не тронуты: `git diff --stat app/ tests/` пуст. ⚠️ Обновление `graphify-out/`, выполненное задачей 3 плана 09-15, этому критерию не мешает: команда графа `app/` и `tests/` не правит.
- Планы 09-01 … 09-15 и их сводки не тронуты: `git diff --stat .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/` показывает только `09-16-PLAN.md`, `09-16-SUMMARY.md` и (при ветви `accept-override`) `09-VERIFICATION.md`.
- `uv run pytest tests/ -q -p no:randomly` → `0 failed`, собрано не меньше `2655`.
- В сводке плана есть таблица переводов «требование → было → стало → закрытый пункт gaps → команда признака».
  </acceptance_criteria>

  <done>Состояния требований Фазы 9 переведены по закрытым разрывам и зелёным признакам; каждая
непереведённая клетка несёт названную причину; решение владельца по QUAL-01 применено дословно и
продублировано там, где переживёт перегенерацию отчёта; флажки и код не тронуты.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| артефакты планирования → следующие фазы | состояние требования читают Фазы 10-15 как ФАКТ; неверное состояние тише отсутствующего |
| отчёт верификации → следующий прогон | файл ПЕРЕГЕНЕРИРУЕТСЯ, поэтому решение, записанное только в нём, не переживает прогона |
| исполнитель → ответ владельца | подстановка ожидаемого ответа вместо полученного обесценивает все три круга закрытия |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-16-01 | Repudiation | перевод состояния без воспроизводимого основания | high | mitigate | каждая изменённая клетка обязана иметь названный закрытый пункт `gaps` и зелёную команду; правило проверяется сличением числа изменённых строк с числом строк-вердиктов подраздела |
| T-09-16-02 | Tampering | переписывание вердиктов и таблиц отчёта верификации | high | mitigate | правка отчёта допустима ТОЛЬКО добавляющая; число удалённых строк, равное нулю, утверждается командой в `<verify>` |
| T-09-16-03 | Repudiation | решение владельца, записанное только в перегенерируемом артефакте | medium | mitigate | ветвь `accept-override` обязывает продублировать ответ в `.planning/REQUIREMENTS.md`; проверяется грепом |
| T-09-16-04 | Tampering | прогоны, переписанные из сводок вместо исполнения | medium | mitigate | в подразделе записывается HEAD, и он сверяется с `git rev-parse --short HEAD` на момент задачи |
| T-09-16-05 | Elevation of privilege | закрытие расхождения с буквой требования суждением исполнителя | high | mitigate | останов `blocking-human`; ответ владельца цитируется дословно, и его отсутствие делает задачу 3 неисполнимой |
| T-09-16-06 | Information disclosure | — | — | n/a | план не трогает ни одного файла приложения; недоверенный ввод в его периметре отсутствует |
| T-09-16-SC | Tampering | установка пакетов | low | accept | план не выполняет ни одной команды менеджера пакетов; гейт легитимности пакетов не срабатывает |

**Блокирующий порог — `high`, уровень ASVS — L1.** Все три угрозы этого уровня (T-09-16-01,
T-09-16-02, T-09-16-05) снабжены машинными проверками либо останова́ми, и без них задача 3 не
считается выполненной.
</threat_model>

<verification>
1. `uv run pytest tests/ -q -p no:randomly` → зелено целиком, собрано не меньше 2655.
2. Третий круг раздела свидетельств записан СПИСКОМ, прогоны сняты заново, HEAD назван.
3. Задача 1 состояний не тронула, и это доказано сличением, способным покраснеть, с антивакуумным
   утверждением о непустом диффе.
4. Каждая изменённая клетка `Status` имеет названный закрытый пункт `gaps` и зелёную команду;
   каждая неизменённая — названную причину.
5. Рекомендация отчёта не использована как основание перевода ни в одной клетке.
6. Правка `09-VERIFICATION.md` (если была) — только добавляющая: удалённых строк ноль.
7. Флажки в теле `.planning/REQUIREMENTS.md` не тронуты.
8. Код приложения и тесты не тронуты вовсе.
9. Планы 09-01 … 09-15 и их сводки не тронуты.
</verification>

<success_criteria>
- Состояния требований Фазы 9 переведены ПОСЛЕ закрытия обоих пунктов `gaps` и ПО НИМ, а не по
  рекомендации отчёта.
- Требование, у которого зелёного признака нет, не переведено, и причина названа поимённо.
- Расхождение QUAL-01 с буквой «на каждой форме письма» закрыто решением владельца, а не суждением
  исполнителя, и записано там, где переживёт перегенерацию отчёта.
- Ни одна существующая строка вердиктов и таблиц `09-VERIFICATION.md` не изменена и не удалена.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-16-SUMMARY.md` when done
</output>
