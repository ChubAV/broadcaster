---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 06
type: execute
wave: 2
depends_on: ["09-05"]
gap_closure: true
files_modified:
  - app/pages/account_groups.py
  - tests/test_pages/test_account_groups.py
autonomous: true
requirements: [FORM-02, QUAL-01]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Удаление ПОСЛЕДНЕЙ НАЙДЕННОЙ строки при активном поиске больше не оставляет пустую карточку без пустого состояния: ветка «список опустел» решается по тому, что осталось бы в ТЕКУЩЕЙ ВЫДАЧЕ, а не по числу групп аккаунта (WR-06 / WARN-6)"
    - "Переход при опустевшей выдаче несёт строку поиска, поэтому человек попадает на экран с ветвью «Группы не найдены» и кнопкой «Сбросить», а не в тупик, из которого выход только правкой адреса"
    - "Повторное удаление через htmx неотличимо ПО ФРАГМЕНТУ, и фрагментная ветка при этом ДОСТИГНУТА: тест сеет не меньше двух групп и утверждает наличие строки в теле первого ответа прежде, чем сравнивать тела (WARN-4, устранение «зелено по построению»)"
    - "Равенство тел утверждается ВНУТРИ класса «строка не найдена» (чужая, несуществующая, уже удалённая), а не между успешным удалением и холостым: прежняя пара «первый / второй ответ при одном и том же теле» была ПРОКСИ, державшимся на совпадении, и после починки курсора (план 09-05) совпадать перестала — при искусственно неизменном `rendered_rows` смещение четвёртого узла законно расходится"
    - "Настоящий повтор — второе нажатие в ЖИВОМ документе — по-прежнему даёт побайтово тот же ответ: число отрисованных строк читается из уже подменённого сентинела, то есть приезжает уменьшенным, и смещение сходится. Это утверждается отдельно, а не подразумевается"
    - "Ветка ответа по-прежнему не различает чужую группу и несуществующую: у обеих текущая выдача не меняется, значит и ветка та же (D-04, T-9-10)"
    - "Путь деградации сохраняет фильтр: без htmx удаление перенаправляет на экран с той же строкой поиска, а не на неотфильтрованный список"
  artifacts:
    - "tests/test_pages/test_account_groups.py::test_deleting_the_last_matched_row_lands_on_the_empty_state"
    - "tests/test_pages/test_account_groups.py::test_repeated_delete_is_harmless_over_htmx — усиленный вариант"
  key_links:
    - "скрытое поле `search` формы подтверждения (заведено планом 09-05) → ветвление обработчика → адрес перехода: рвётся — человек остаётся перед пустой карточкой без выхода"
  prohibitions:
    - statement: "второго экземпляра пустого состояния не заводится: три различимых пустых состояния живут в `account_groups/list.html` в ОДНОМ экземпляре, и фрагментной их копии не появляется (D-09)"
      status: flagged-unverified
      verification: none
    - statement: "различимость «чужая» и «несуществующая» не вводится: ветка обязана считаться по величинам, одинаковым для обоих случаев"
      status: flagged-unverified
      verification: none
    - statement: "строка поиска не участвует в подсчёте линейки счётчика: линейка описывает аккаунт целиком, а не текущую выдачу, и это записанное решение фазы 3 не пересматривается"
      status: flagged-unverified
      verification: none
    - statement: "плашка `?notice=` на успешное удаление не выдаётся (D-10)"
      status: flagged-unverified
      verification: none
---

<objective>
Закрыть два именованных дефекта на отгруженной поверхности: **WR-06 / WARN-6** — тупик после
удаления последней найденной строки, и **WARN-4 / WR-04** — единственный тест фазы, доехавший
до отгрузки зелёным по построению.

Purpose: ветка «список опустел» решается по account-wide `total_groups`, а поиск в подсчёт не
входит. Значит, когда поиск сузил выдачу до одной строки и человек её удалил, `total_groups`
всё ещё больше нуля, берётся фрагментная ветка, строка снимается на месте — и человек остаётся
перед пустой карточкой. Три различимых пустых состояния, включая то, что предлагает
«Сбросить», не показываются ни одно, и вернуться к неотфильтрованному списку можно только
правкой адреса. Тот же по форме дефект — удаление последней строки текущей порции, когда
дальше есть ещё страницы.

Второй предмет: `test_repeated_delete_is_harmless_over_htmx` сеет ОДНУ группу, поэтому обе
отправки уходят в ветку 204 с пустым телом, и утверждаемое равенство ФРАГМЕНТОВ не исполняется
ни разу. Это третий случай «зелено по построению» за фазу и единственный, доехавший до
отгрузки: два предыдущих исполнители поймали сами и записали отклонениями.

Output: ветвление по текущей выдаче, переход, сохраняющий фильтр, и охраняющий тест, который
действительно исполняет охраняемое свойство.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-REVIEW.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `tests/test_pages/test_account_groups.py::test_deleting_the_last_matched_row_lands_on_the_empty_state`
- `tests/test_pages/test_account_groups.py::test_deleting_the_last_matched_row_degrades_with_the_filter`
- `tests/test_pages/test_account_groups.py::test_the_delete_branch_does_not_reveal_a_foreign_group_under_search`
- `tests/test_pages/test_account_groups.py::test_repeated_delete_is_harmless_over_htmx` — **существующий тест, усиленный** (не новый символ, но изменённый контракт: сеет не меньше двух групп и утверждает достижение фрагментной ветки)
- Вспомогательная функция ветвления в `app/pages/account_groups.py` — имя за исполнителем;
  требование к имени: оно обязано говорить о ТЕКУЩЕЙ ВЫДАЧЕ, а не о числе групп аккаунта,
  иначе следующий читатель повторит сегодняшнюю ошибку по имени.

Инвентарные числа этот план **не двигает**: новых мест отправки, целей подмены и внеполосных
блоков он не заводит. Это утверждается прогоном гейтов, а не молчанием.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: ветка «список опустел» решается по текущей выдаче, а не по числу групп аккаунта</name>

  <files>app/pages/account_groups.py, tests/test_pages/test_account_groups.py</files>

  <read_first>
- `app/pages/account_groups.py` **целиком, после правок плана 09-05**. Ключевые места:
  `_build_groups_query` (её докстринг называет класс отказа, к которому относится и этот
  дефект), `_clean_search`, `_filter_params`, `_group_counts` (докстринг прямо записывает
  «Поиск в подсчёт НЕ входит: линейка описывает аккаунт целиком» — это решение НЕ
  пересматривается), `account_groups_delete` и её ветка `total_groups == 0` с комментарием,
  который сегодня читается так, будто покрывает любой путь опустошения.
- `app/templates/account_groups/list.html` — три ветки пустого состояния и порядок их
  проверки: `filter_search` идёт ПЕРВОЙ именно потому, что поиск может обнулить выдачу при
  заполненном списке. Ветка `filter_search` печатает `empty_state` с действием «Сбросить»,
  ведущим на экран групп без параметров.
- `app/pages/htmx.py` — `respond(request, *, redirect, notice=None, fragment=None)`: ветка
  `fragment is None` даёт 302 без htmx и 204 с заголовком перехода — с ним; `_local_path`
  ограничивает адрес перехода локальным путём.
- `09-REVIEW.md` §WR-06 — трассировка дефекта и предложенная форма починки.
- `09-CONTEXT.md` §Decisions **D-09** (опустевший список закрывается переходом, а не второй
  отрисовкой пустого состояния) и **D-04** (ответ одинаков для найденной и не найденной
  группы) — обе локированы; план их исполняет, а не пересматривает.
  </read_first>

  <reversibility rating="reversible">
Правка ветвления одного обработчика и адреса перехода. Форма ответа (переход, а не вторая
отрисовка пустого состояния) не меняется — меняется только УСЛОВИЕ, по которому она выбирается.
  </reversibility>

  <behavior>
1. `test_deleting_the_last_matched_row_lands_on_the_empty_state`: посеять несколько групп,
   из которых строке поиска отвечает РОВНО ОДНА; удалить её через `htmx_client`, передав
   строку поиска телом формы. Ответ — статус 204 с заголовком перехода, и адрес перехода несёт
   ту же строку поиска. Тела фрагмента нет.
2. Тот же тест утверждает НЕВАКУУМНОСТЬ: в аккаунте после удаления остались другие группы
   (`total_groups` строго больше нуля) — иначе ветка совпала бы со старой и утверждение
   ничего не сказало бы о починке.
3. `test_deleting_the_last_matched_row_degrades_with_the_filter`: то же действие БЕЗ признака
   htmx отвечает 302, и адрес перенаправления несёт ту же строку поиска.
4. `test_the_delete_branch_does_not_reveal_a_foreign_group_under_search`: при одной и той же
   активной строке поиска чужой идентификатор и несуществующий идентификатор дают один статус,
   один заголовок перехода и одинаковое тело.
   ⚠️ Равенство ТЕЛ сравнивается ПОСЛЕ подстановки идентификатора — тем же приёмом и по тому же
   основанию, что в плане 09-05, задача 3, поведение 3: первый внеполосный узел печатает
   идентификатор, пришедший из ПУТИ, а двумя разными случаями один и тот же идентификатор не
   адресуется. Спрашивающий узнаёт из ответа ровно то, что сам назвал в адресе.
5. Прежнее поведение сохраняется: при пустой строке поиска и удалении последней группы
   аккаунта ветка та же, что и была (переход), а при удалении одной из многих без поиска —
   фрагмент. Обе половины закреплены существующими тестами и обязаны остаться зелёными.
  </behavior>

  <action>
**1. Ветвление считается по ТЕКУЩЕЙ ВЫДАЧЕ.** В `account_groups_delete`, после удаления и
после подсчёта чисел линейки, добавить отдельный вопрос: «осталась ли в выдаче с этой строкой
поиска хотя бы одна строка?» — одним запросом `_build_groups_query(user.id, account_id, term)`
с ограничением в одну строку, где `term` получен из пришедшего телом поля `search` через
`_clean_search`. Переход выбирается, когда выдача пуста; фрагмент — когда в ней что-то есть.

⚠️ **Числа линейки при этом считаются по-прежнему по аккаунту целиком** и строкой поиска не
сужаются: линейка описывает аккаунт, а не выдачу, и это записанное решение. Смешать два вопроса
в одном запросе нельзя — получилась бы линейка, врущая ровно там, где список перестал
помещаться на экран.

**2. Адрес перехода несёт фильтр.** Величина, передаваемая в `respond(redirect=...)`,
собирается из адреса экрана и параметров фильтра тем же помощником `_filter_params`, что и
адрес сентинела, — чтобы адрес после действия и адрес после перезагрузки собирались одним
источником. При пустой строке поиска адрес обязан остаться ПОСИМВОЛЬНО прежним: сегодняшние
тесты деградации сверяют его дословно, и лишний вопросительный знак покрасил бы их.

**3. Докстринг обработчика переписать в двух местах.**
Первое: абзац про ветвление «список опустел / не опустел» сегодня говорит, что оно считается
по числам аккаунта, — теперь он обязан говорить, что оно считается по текущей выдаче, и
называть ПРИЧИНУ смены словами: ветка по числу аккаунта оставляла человека перед пустой
карточкой, из которой нет выхода, потому что все три различимых пустых состояния живут на
странице и во фрагмент не приезжают (D-09).
Второе: абзац про неотличимость обязан объяснить, ПОЧЕМУ смена условия её не сломала: у чужой
и у несуществующей группы текущая выдача не меняется ни на строку, поэтому и ветка у них та же,
какой она была бы у соседнего успешного удаления в том же аккаунте.

**4. Изъятие, которое остаётся, назвать словами.** Удаление последней строки ТЕКУЩЕЙ ПОРЦИИ
при непустых следующих страницах этой правкой не закрывается и закрываться не должно: выдача не
пуста, фрагментная ветка верна, а курсор чинит четвёртый внеполосный узел плана 09-05. Записать
это в докстринг одной фразой, чтобы следующий читатель не принял её за незакрытую половину
WR-06.

**5. Тесты** из `<behavior>` дописать рядом с существующей парой деградации, в её форме.
Формулировки отказов обязаны называть следствие для человека («человек остался перед пустой
карточкой без кнопки сброса фильтра»), а не факт несовпадения статусов.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_account_groups.py -q -k "last_matched_row or does_not_reveal_a_foreign_group"` → 3 passed.
- `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly` — зелено целиком, включая прежние `test_delete_degrades_without_htmx`, `test_delete_returns_oob_nodes`, `test_delete_removes_the_group_and_redirects`.
- Адрес перенаправления при ПУСТОЙ строке поиска не изменился: `uv run pytest tests/test_pages/test_account_groups.py -q -k "delete_degrades_without_htmx or delete_removes_the_group_and_redirects"` → зелено без правки самих этих тестов (в диффе они не изменены — проверяется `git diff --stat` по файлу тестов и перечислением изменённых тестов в сводке).
- В докстринге `account_groups_delete` присутствует абзац, называющий текущую выдачу основанием ветвления, и отдельная фраза про изъятие из пункта 4 — обе выписать в сводку дословно.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q` → зелено: инвентарные числа планом не сдвинуты.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
  </acceptance_criteria>

  <done>
Удаление последней найденной строки приводит человека на экран с ветвью «Группы не найдены» и
кнопкой «Сбросить», а не в пустую карточку; неотличимость чужой и несуществующей группы
сохранена и утверждена отдельно.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: охраняющий тест повторного удаления перестаёт быть зелёным по построению</name>

  <files>tests/test_pages/test_account_groups.py</files>

  <read_first>
- `tests/test_pages/test_account_groups.py:1284-1330` — `test_repeated_delete_is_harmless_over_htmx`
  целиком: он сеет ОДНУ группу через `_seed_group`, поэтому обе отправки уходят в ветку с пустым
  телом, и все четыре утверждения удовлетворяются тривиально.
- `tests/test_pages/test_account_groups.py` — соседний `test_delete_returns_oob_nodes`: он зовёт
  `_seed_many(..., ["Первая", "Вторая"])` ИМЕННО ЗАТЕМ, чтобы достать фрагментную ветку. Форма
  посева взять оттуда.
- `tests/test_pages/test_account_groups.py` — парный `test_repeated_delete_is_harmless` (путь
  без htmx): вторая половина пары, которую усиливаемый тест обязан зеркалить.
- `09-VERIFICATION.md` §`coincidental_reliance_items` и §Anti-Patterns Found (строка про
  `test_pages/test_account_groups.py:1284-1330`) — формулировка того, ЧТО именно не исполняется.
- `09-REVIEW.md` §WR-04 — инструментированный прогон: `first=204 body=b'' second=204 body=b''`.
- `app/pages/account_groups.py` — `account_groups_delete` после задачи 1: тело фрагмента
  собирается из `group_id` ПУТИ, а не из найденной строки, — именно это свойство тест обязан
  исполнить, а не обойти. Плюс собранное планом 09-05 смещение четвёртого узла: `rendered_rows`
  минус число снятых с экрана строк.
- `09-05-PLAN.md` §`<cursor_repair_vs_d04>` **целиком** — разбор столкновения «починка курсора ↔
  D-04» и записанное следствие для ЭТОГО теста: почему пара «первый / второй ответ при одном и
  том же теле» перестаёт быть верной и на что равенство переносится. Без этого раздела правка
  теста читается как ослабление утверждения.
- `09-05-SUMMARY.md` — фактическое имя величины «сколько строк этот ответ снимает с экрана» и
  строка сборки смещения, выписанная исполнителем плана 09-05 дословно.
  </read_first>

  <reversibility rating="reversible">Изменение одного теста.</reversibility>

  <behavior>
1. Тест сеет НЕ МЕНЬШЕ ДВУХ групп и делает ТРИ отправки через `htmx_client`: (1) удаление
   группы `G` с `rendered_rows = R`; (2) повтор удаления `G` — с `rendered_rows = R − 1`, то
   есть с тем числом, которое живой документ пришлёт вторым нажатием, прочитав его из уже
   подменённого сентинела; (3) удаление `G` ещё раз с тем же телом, что и (2).
2. **Положительное утверждение достижения фрагментной ветки идёт ПЕРВЫМ**, до всякого
   сравнения тел: статус первого ответа 200 и в его теле присутствует узел снятия строки с
   идентификатором удаляемой группы. Формулировка отказа обязана прямо говорить, что без этого
   утверждение о равенстве тел вакуумно.
3. **Равенство утверждается на паре (2)/(3)** — обе отправки лежат в классе «строка не найдена»
   с одинаковым телом: тела побайтово равны, статусы равны, заголовки перехода равны, и в теле
   нет целого документа. Это и есть охраняемое свойство D-04, а не его прокси.
4. **Отдельным утверждением — настоящий повтор:** ответ (2), пришедший с `rendered_rows = R − 1`,
   побайтово равен ответу (1), пришедшему с `rendered_rows = R`. Именно так пара «нажал —
   нажал ещё раз» выглядит в живом документе, и именно в этом виде «повтор безвреден и
   неотличим» продолжает держаться после починки курсора.
5. **Прежняя форма утверждения — «(1) равно повтору с тем же самым `R`» — БОЛЬШЕ НЕ
   УТВЕРЖДАЕТСЯ, и это записанная смена контракта, а не смягчение.** При искусственно неизменном
   `rendered_rows` первая отправка снимает строку (смещение `R − 1`), а вторая не снимает ничего
   (смещение `R`), и расхождение ВЕРНО: одинаковое число отрисованных строк после разного числа
   снятых означает разное состояние экрана. Прежнее равенство держалось на совпадении, которого
   до четвёртого внеполосного узла просто не могло не быть. Разбор целиком — раздел
   `<cursor_repair_vs_d04>` плана 09-05.
6. Группа физически удалена из базы после первой отправки и остаётся удалённой после второй и
   третьей.
7. Прежняя половина пары — путь БЕЗ htmx — не трогается.
  </behavior>

  <action>
Переписать тело `test_repeated_delete_is_harmless_over_htmx` под `<behavior>`: посев через
`_seed_many` с двумя именами, цель — первая из посеянных.

Докстринг теста дополнить абзацем о том, ЧТО было исправлено и почему это важно записать, а не
просто починить: тест сеял одну группу, обе отправки попадали в ветку пустого ответа, и
сравнивались два пустых тела — то есть охраняемое свойство («тело собирается из `group_id`
ПУТИ, а не из найденной строки») не исполнялось ни разу. Абзац обязан назвать приём, которым
это теперь исключено: **достижение ветки утверждается положительно и раньше сравнения**.

⚠️ **Смену контракта записать в докстринг теста отдельным абзацем — это вторая правка, и она
важнее первой.** После плана 09-05 ответ несёт четвёртый внеполосный узел, и прежняя пара
«первый ответ равен второму при одном и том же теле» перестаёт быть верной: первая отправка
снимает строку, вторая не снимает ничего, значит число строк, которые документ продолжает
показывать, у них разное — и смещение курсора обязано разойтись. Абзац обязан назвать три вещи:
(1) равенство переносится на пару отправок, обе лежащие в классе «строка не найдена», — это и
есть охраняемое свойство D-04; (2) настоящий повтор в живом документе идёт с УМЕНЬШЕННЫМ
`rendered_rows` (число читается из уже подменённого сентинела), и на нём равенство держится —
это утверждается отдельно; (3) прежняя форма держалась на совпадении, которого до четвёртого
узла не могло не быть, и потому проксировала свойство, а не проверяла его.

⚠️ Пара отправок, между которыми сравниваются тела, обязана идти с ОДНИМ И ТЕМ ЖЕ телом:
различие ответов, полученное от разного тела, о неотличимости не сказало бы ничего. Это условие
относится к паре (2)/(3), а не к паре (1)/(2).

⚠️ Тест обязан остаться про НЕОТЛИЧИМОСТЬ, а не превратиться в тест про наличие узлов: узел
проверяется ровно затем, чтобы доказать, что сравнение вообще что-то сравнивает.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -k "repeated_delete"</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_account_groups.py -q -k "repeated_delete"` → 2 passed (обе половины пары).
- Тест доказан невакуумным исполнением: временно сломать сборку тела ответа так, чтобы фрагментная ветка не достигалась (например, вернуть ветку перехода безусловно), убедиться, что `test_repeated_delete_is_harmless_over_htmx` КРАСНЕЕТ на первом же утверждении, и вернуть дерево в исходное состояние. Текст отказа выписать в сводку; `git status --short` после возврата обязан показывать только ожидаемые файлы плана.
- В теле теста первое исполняемое утверждение относится к статусу 200 и наличию узла строки, а не к равенству тел — проверяется чтением и выписывается в сводку.
- Равенство тел утверждается на паре отправок, ОБЕ из которых лежат в классе «строка не найдена»; отдельным утверждением закреплён настоящий повтор (вторая отправка с уменьшенным `rendered_rows` даёт тело, побайтово равное первой). Обе строки утверждений выписать в сводку дословно.
- Абзац докстринга о смене контракта существует и называет все три вещи из `<action>`; выписать в сводку дословно. `uv run pytest tests/test_pages/test_account_groups.py -q -k "scroll_cursor or no_op_delete"` → зелено: правка теста не отменила ни одного утверждения плана 09-05.
- `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly` — зелено целиком.
  </acceptance_criteria>

  <done>
Тест повторного удаления исполняет охраняемое свойство: фрагментная ветка достигнута
положительным утверждением, равенство тел сравнивает два непустых фрагмента внутри класса
«строка не найдена», а настоящий повтор живого документа закреплён отдельным утверждением.
Смена контракта записана абзацем, а не обнаружена через фазу. Невакуумность доказана временной
правкой живого дерева, а не рассуждением.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → `POST /accounts/{A}/groups/{G}/delete` | поле `search` приходит из недоверенного документа и участвует в выборе ветки и в адресе перехода |
| адрес перехода → браузер | значение уходит в заголовок перехода htmx и в заголовок перенаправления |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06-01 | Information disclosure | ветвление по текущей выдаче | high | mitigate | выдача строится тем же `_build_groups_query` с тройным ограничением по владельцу и аккаунту; чужая и несуществующая группа выдачу не меняют, поэтому ветка у них одна — закреплено `test_the_delete_branch_does_not_reveal_a_foreign_group_under_search` |
| T-09-06-02 | Tampering | `search` → адрес перехода | high | mitigate | строка проходит `_clean_search`, адрес собирается тем же `_filter_params`, что и на странице, и уходит через `respond()`, где `_local_path` не выпускает внешний адрес в заголовок перехода |
| T-09-06-03 | Spoofing | подбор чужих идентификаторов перебором | medium | mitigate | ответ остаётся неотличимым по статусу, телу и заголовку; усиленный тест повторного удаления теперь действительно исполняет сравнение фрагментов, а не двух пустых тел |
| T-09-06-04 | Denial of service | дополнительный запрос выдачи на каждое удаление | low | accept | один запрос с ограничением в одну строку по уже существующему индексируемому условию; стоимость сопоставима с двумя запросами подсчёта, которые обработчик делает и сегодня |
| T-09-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | план установок пакетов не производит; новых зависимостей не появляется |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly` — зелено.
2. `uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q` — зелено, инвентарные числа не сдвинуты.
3. `uv run python -m compileall -q app main.py tests` — без ошибок.
4. `grep -rnE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER" app/pages/account_groups.py tests/test_pages/test_account_groups.py` → пусто.
5. Невакуумность усиленного теста доказана временной правкой живого дерева; дерево возвращено.
</verification>

<success_criteria>
- Тупика после удаления последней найденной строки нет; переход несёт фильтр.
- Неотличимость чужой и несуществующей группы сохранена под активным поиском.
- Единственный доехавший до отгрузки тест «зелено по построению» исполняет охраняемое свойство,
  и это доказано красным на подставленном дереве.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-06-SUMMARY.md` when done
</output>
