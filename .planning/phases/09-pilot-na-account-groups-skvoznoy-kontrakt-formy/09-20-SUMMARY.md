---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 20
subsystem: testing
tags: [uat, manual-walkthrough, htmx, alpine, scroll-lock, keyset, hx-location]

# Dependency graph
requires:
  - phase: 09 (план 09-13)
    provides: "механизм `keyset` вместо абсолютного смещения курсора; добавка `scroll-lock` — признак `is-modal-open` на элементе `<html>`; `_screen_url` / `_filter_params`"
  - phase: 09 (план 09-17)
    provides: "ветвь `destroy-guard` — метод `destroy()` под несущей проверкой `if (this.open)` снимает признак блокировки прокрутки на ветви СНОСА узла панели; от неё зависит, ЧТО считать верным исходом шага «прокрутить после удаления»"
  - phase: 09 (план 09-18)
    provides: "строка поиска доезжает до ТРЕТЬЕГО места отрисовки (ответ тумблера); оба выхода тумблера собирают адрес помощником `_screen_url`"
  - phase: 09 (план 09-19)
    provides: "признак однократной регистрации `data-htmx-failure-wired` / `document.body.dataset.htmxFailureWired` — без его имени шаг замера в 4.2 не поставить"
  - phase: 09 (план 09-12)
    provides: "образец того, как круг проверок выписывается ДО обхода — раздел «Второй круг закрытия» `09-UAT.md`"
provides:
  - "раздел `09-UAT.md` «Четвёртый круг закрытия» с тремя проверками (4.1, 4.2, 4.3) и выписанным ожидаемым признаком у КАЖДОГО из 33 шагов"
  - "пустая таблица отметок четвёртого круга с колонками «проверка / наблюдённый признак / расхождение» плюс дата и браузер/ОС, и заготовка трёх строк под заполнение"
  - "предупреждение, читаемое ПЕРВЫМ: три сегодняшние отметки обесценены НАЗВАНИЕМ, а не исправлением"
  - "ключ frontmatter `walkthrough_4` с пустыми полями под все наблюдаемые величины трёх проверок"
  - "летописная строка `outstanding_chronicle` при ключе `outstanding`: названная им запись закрыта планом 09-13, а ключ не удалён намеренно"
affects: [09-20 задача 3, "Phase 15 (GATE-09)"]

actuals:
  tokens: 8236
  tasks: 1
  commits: 2

tech-stack:
  added: []
  patterns:
    - "ожидаемый признак выписывается ДО обхода у КАЖДОГО шага: решать на обходе, что считать успехом, значит подгонять отметку"
    - "признак, наблюдаемый выражением консоли, предпочтён признаку, наблюдаемому глазом, там, где экран может оказаться короче окна (`classList.contains('is-modal-open')`, `getComputedStyle(...).overflow`)"
    - "метка живого документа (`window.__uat4`) как проверяемое отличие перехода `HX-Location` от обычной навигации"
    - "обесцененная отметка НАЗЫВАЕТСЯ летописью, а не переписывается: 226 добавленных строк, 0 удалённых"

key-files:
  created: []
  modified:
    - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md

key-decisions:
  - "Раздел поставлен ПОСЛЕ раздела второго круга и ПЕРЕД §«Что проверяется глазами, потому что иначе никак»: круги обхода лежат подряд, а разделы о природе ручной проверки остаются общими для всех кругов"
  - "Шаг прокрутки экрана приземления `HX-Location` (4.2.6) разложен на ДВА признака заранее: экран «Группы не найдены» может быть короче окна, и прокручивать на нём физически нечего. Ответом по существу объявлены `classList.contains('is-modal-open')` → `false` и `overflow` ≠ `hidden`; правило записано в артефакте, чтобы человек НЕ решал этого на обходе"
  - "Шаг 4.2.4 (`window.__uat4`) заведён затем, чтобы наблюдение о числе регистраций обработчиков плашки было ОТНОСИМО к предмету: если документ перезагрузился, замер говорит не о том пути"
  - "Имя признака блокировки прокрутки выписано ИЗ ТАБЛИЦЫ СТИЛЕЙ со ссылкой на строки (`app/static/css/app.css:961-964`), а не вспомнено"
  - "Шаг 4.1.8 требует выписать адрес сентинела с `after_id=` и убедиться в отсутствии `offset=`: это единственный ГЛАЗАМИ наблюдаемый признак того, что курсор стал ключевым"
  - "Ключ `outstanding` НЕ удалён и НЕ переписан: к нему добавлен сосед `outstanding_chronicle`. Удаление ключа было бы закрытием дефекта удалением записи о нём"
  - "Таблица отметок оставлена БЕЗ единой заполненной ячейки — даже без строк-меток `4.1` / `4.2` / `4.3`: шаблон трёх строк вынесен в блок кода под таблицей, чтобы форма была очевидна, а критерий пустоты не оспорим"

patterns-established:
  - "Круг обхода повторяет форму предыдущего круга целиком: заголовок второго уровня, проверки третьего, таблица «# / шаг / ожидаемый наблюдаемый признак», предупреждение об изменившемся дереве, правило заполнения, правило «расхождение — записать и остановиться»"
  - "Каждое требование к ячейке отметки записано ДВАЖДЫ: в ожидаемом признаке шага и в правиле заполнения под таблицей — чтобы пропуск одного места не обесценил проверку"

requirements-completed: []

coverage:
  - id: D1
    description: "Раздел «Четвёртый круг закрытия» заведён; три проверки 4.1 / 4.2 / 4.3 выписаны, у каждого шага своя ячейка ожидаемого признака"
    requirement: "FORM-02"
    verification:
      - kind: command
        ref: "grep -c \"^## Четвёртый круг закрытия\\|^### 4\\.[123]\" .planning/phases/09-.../09-UAT.md → 4"
        status: pass
    human_judgment: false
  - id: D2
    description: "Проверка 4.1 несёт шаг ВЫПИСКИ названия последней видимой строки (4.1.9) и отдельный шаг «прокрутить страницу ПОСЛЕ удаления и долистать до сентинела» (4.1.11, 4.1.13) плюс наблюдение по `<html>` (4.1.12)"
    requirement: "QUAL-02"
    verification:
      - kind: manual_procedural
        ref: "09-UAT.md §4.1, шаги 4.1.9 / 4.1.11 / 4.1.12 / 4.1.13"
        status: unknown
    human_judgment: true
  - id: D3
    description: "Проверка 4.2 несёт шаг проверки адреса приземления НА ФИЛЬТР (4.2.5) и шаг замера числа регистраций обработчиков плашки (4.2.7 — 4.2.9)"
    requirement: "QUAL-02"
    verification:
      - kind: manual_procedural
        ref: "09-UAT.md §4.2, шаги 4.2.5 / 4.2.7 / 4.2.8 / 4.2.9"
        status: unknown
    human_judgment: true
  - id: D4
    description: "Проверка 4.3 несёт шаг «удалить единственную найденную строку» с ожиданием экрана «Группы не найдены» и кнопки «Сбросить», а также дословную выписку скрытого поля строки поиска"
    requirement: "FORM-09"
    verification:
      - kind: manual_procedural
        ref: "09-UAT.md §4.3, шаги 4.3.4 / 4.3.5 / 4.3.7"
        status: unknown
    human_judgment: true
  - id: D5
    description: "Frontmatter несёт `walkthrough_4` с пустыми полями; ключ `outstanding` не удалён и снабжён летописной строкой"
    requirement: "QUAL-06"
    verification:
      - kind: command
        ref: "grep -c \"^walkthrough_4:\" → 1; grep -c \"outstanding\" → 4; yaml.safe_load frontmatter → ok"
        status: pass
    human_judgment: false

# Metrics
duration: ~35 min (задача 1)
completed: 2026-09-02
status: halted
---

# Phase 9 Plan 20: Четвёртый круг закрытия — обход выписан, обход НЕ ПРОВЕДЁН — Summary

**Задача 1 исполнена: три проверки четвёртого круга выписаны в `09-UAT.md` с ожидаемым признаком у
каждого из 33 шагов, обесцененность трёх сегодняшних отметок НАЗВАНА (0 удалённых строк на 226
добавленных), таблица отметок готова к заполнению. Задача 2 — `checkpoint:human-action` с
`gate="blocking-human"` — НЕ ИСПОЛНЕНА и исполнена быть не могла: это наблюдение эффекта в живом
браузере. Задача 3 НЕ НАЧАТА: её предусловие — заполненная таблица отметок.**

## ⚠️ ПЛАН ОСТАНОВЛЕН НА ЗАДАЧЕ 2 — ЭТО НЕ ОТКАЗ, А ЗАПЛАНИРОВАННЫЙ ОСТАНОВ

`autonomous: false`, задача 2 несёт `type="checkpoint:human-action"` и `gate="blocking-human"`.
Останов снимается **только владельцем**, прошедшим обход. Автоматического пути нет и не заводится:
браузерного стенда в проекте нет, суита идёт транспортом ASGI — `httpx` не применяет свопов, не
собирает внеполосных узлов, не исполняет `hx-trigger="revealed"` и не знает ни про `overflow`
документа, ни про то, куда после подмены уехал фокус.

**Предусловие задачи 2 исполнителем НЕ ПРОВЕРЯЛОСЬ И НЕ ГОТОВИЛОСЬ** (приложение на порту 8000,
зелёная суита, посеянный стенд `UAT9-`): всё это — часть работы владельца на обходе, и посев в
живую базу разработчика исполнителем из изолированного рабочего дерева был бы вмешательством в
чужое окружение.

## Что сделала задача 1

**1. Раздел «Четвёртый круг закрытия: эффект в живом браузере на дереве после третьего круга»**
поставлен после раздела второго круга и перед §«Что проверяется глазами, потому что иначе никак».
Форма повторяет второй круг целиком: заголовок второго уровня, три проверки заголовками третьего,
у каждой таблица «# / шаг / ожидаемый наблюдаемый признак».

**2. Предупреждение, которое читается ПЕРВЫМ** — по образцу «ЧТО ИЗМЕНИЛОСЬ В КОДЕ ПОСЛЕ ЭТОГО
ОБХОДА» пункта 3. Названы поимённо три обесцененные отметки:

* **3.5-Б обесценена ЦЕЛИКОМ** — снята на механизме **АБСОЛЮТНОГО СМЕЩЕНИЯ**, который план 09-13
  удалил из дерева вместе с полем и четвёртым внеполосным узлом;
* **механизм `keyset` глазами не наблюдался НИ РАЗУ**;
* **добавка `scroll-lock` и её починка планом 09-17 — новая видимая поверхность**, не наблюдённая
  никем; между планами 09-13 и 09-17 экран после первого же удаления прокручиваться переставал
  (блокер **CR-01**), и признак отказа был МОЛЧАЛИВЫМ.

Отметки прежних кругов **не переписаны ни одной строкой** — измерено: `git diff --numstat` даёт
`226 0`.

**3. Три проверки с выписанным ожидаемым признаком у каждого шага.**

* **4.1 (15 шагов)** — 3.1, 3.2, 3.3-Б и ЗАНОВО 3.5 на механизме `keyset`, плюс НОВЫЙ шаг: удалить
  группу и **попробовать прокрутить страницу**, долистав до сентинела. Отдельными шагами: выписка
  адреса сентинела (`after_id=` есть, `offset=` нет ни одного) — единственный глазами наблюдаемый
  признак ключевого курсора; выписка названия последней видимой строки; наблюдение по элементу
  `<html>` с именем признака, выписанным из `app/static/css/app.css:961-964`.
* **4.2 (9 шагов)** — ветка `HX-Location`: удалить ПОСЛЕДНЮЮ строку отфильтрованной выдачи,
  убедиться, что документ **не перезагружался** (метка `window.__uat4`), выписать адрес приземления
  дословно, прокрутить пришедший экран, и замерить WR-05: признак `data-htmx-failure-wired` на узле
  тела и **ЧИСЛО** слушателей `htmx:responseError` / `htmx:sendError` после **трёх** переходов.
* **4.3 (9 шагов)** — мир «Alpine мёртв, htmx жив»: блокировка `/static/js/alpine.min.js`,
  `?search=UAT9-needle`, переключение тумблера, дословная выписка скрытого поля `name="search"` в
  форме удаления переключённой строки, затем удаление единственной найденной строки с ожиданием
  экрана «Группы не найдены» + подсказка + кнопка «Сбросить».

**4. Пустая таблица отметок** — колонки «Дата / Браузер · ОС / Проверка / Наблюдённый признак /
Расхождение», ни одной заполненной ячейки; шаблон трёх строк вынесен в блок кода под таблицей.

**5. Frontmatter** — ключ `walkthrough_4` с 24 полями под все наблюдаемые величины, из них 22
пустых (заполнены только `after_plans` и `checks`); ключ
`outstanding` **не удалён** и снабжён соседом `outstanding_chronicle`.

**6. Стенд** — существующие §«Как привести стенд в нужное состояние» и §«Как убрать стенд»
переиспользованы ссылкой; названо дополнительное требование круга: аккаунт с числом групп БОЛЬШЕ
размера страницы (`PAGE_SIZE = 30`) и группа `UAT9-needle`, находимая РОВНО одна.

## Verify задачи 1 — коды возврата

| # | Команда | Ожидание | Факт | Итог |
|---|---|---|---|---|
| 1 | `grep -c "^## Четвёртый круг закрытия\|^### 4\.[123]" 09-UAT.md` | `≥ 4` | **`4`** | `0` |
| 2 | `grep -c "^walkthrough_4:" 09-UAT.md` | `= 1` | **`1`** | `0` |
| 3 | `grep -c "outstanding" 09-UAT.md` | `≥ 1` | **`4`** | `0` |
| 4 | `git diff --stat app/ tests/ \| wc -l` | `= 0` | **`0`** | `0` |
| 5 | `git diff -U0 09-UAT.md \| grep -c "^-.*2026-08-3[01] \| Chrome"` | `= 0` | **`0`** | `0` |

Сверх пяти: `git diff --numstat 09-UAT.md` → **`226  0`** (ни одной удалённой строки во всём
файле), `python3 yaml.safe_load` frontmatter → разбирается, `grep TODO\|FIXME\|HACK\|XXX` → ноль.

## Commits

1. **Задача 1** — `5b6df35` (`docs(09-20)`): раздел четвёртого круга, таблица отметок, frontmatter.
2. **Сводка останова** — коммит настоящего файла.

## Что НЕ сделано и почему

| Не сделано | Причина |
|---|---|
| **Задача 2 — обход** | `checkpoint:human-action`, `gate="blocking-human"`. Эффект в живом браузере; снимается владельцем |
| **Задача 3 — свидетельства, летопись битых окон** | `<precondition>`: «Задача 2 завершена: таблица отметок четвёртого круга заполнена, стенд убран». Не выполнено |
| `.planning/REQUIREMENTS.md` | Предмет задачи 3 |
| `.planning/WINDOWS.md` (записи 12 и 13) | Предмет задачи 3 |
| Новая запись летописи о непроведённом обходе | НЕ заведена НАМЕРЕННО, и решение названо здесь, а не пропущено. Останов запланирован планом и отслеживается оркестратором, а не оставлен позади; новая запись сдвинула бы `open_count` с `14`, и машинный критерий задачи 3 (`windows status`) пришлось бы читать против сдвинутого числа. Если обход НЕ состоится и план будет закрыт без него — запись обязана появиться, и это решение владельца, а не исполнителя |
| `.planning/STATE.md`, `.planning/ROADMAP.md` | Запрещено оркестратором: их пишет только он, и post-merge гейт падает на их рассинхроне (`tests/test_planning/test_state_progress_matches_roadmap.py`) |
| Состояния требований | Не переводятся этим планом ни одним символом — вывод из истории фазы: перевод в `Complete` внутри незакрытой фазы делался ДВАЖДЫ и ДВАЖДЫ откатывался (`183511c`, `4fe89da`) |
| `09-VERIFICATION.md` | Прохибиция плана: отчёт перегенерируется прогоном верификации |

## Записка исполнителю задачи 3 (после обхода)

**Команда летописи битых окон записана в плане относительным путём**
(`node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 12`), **а каталога `.claude/` в этом рабочем
дереве НЕТ** — оно изолированное. Рабочий путь отсюда:
`node /source/broadcaster/.claude/gsd-core/bin/gsd-tools.cjs windows fixed 12` (и то же для `13`).
Запись **14** не трогается. Текущая шапка `.planning/WINDOWS.md`: `open_count: 14`,
`fixed_count: 2` — после двух закрытий ожидается `12` и `4`.

**Состояние таблицы требований на момент останова проверено машинно** и соответствует ожиданию
задачи 3:
`grep -E "^\| (FORM-02|FORM-09|QUAL-01|QUAL-02|QUAL-06) \| Phase 9 \|" .planning/REQUIREMENTS.md | grep -c "Gaps Found"`
→ **`5`**. Все пять клеток Фазы 9 читаются `Gaps Found`; этим планом ни одна не тронута.

## Self-Check: PASSED

* `.planning/phases/09-.../09-UAT.md` — **FOUND**, изменён, 226 добавленных строк, 0 удалённых.
* Коммит `5b6df35` — **FOUND** в `git log`.
* `git status --short` после коммита задачи 1 — **пусто**.
* `git diff --diff-filter=D --name-only HEAD~1 HEAD` — **пусто** (ни одного удалённого файла).
* Ветка: `worktree-agent-ab25b9d9d65598012`, рабочее дерево
  `/source/broadcaster/.claude/worktrees/agent-ab25b9d9d65598012`.
