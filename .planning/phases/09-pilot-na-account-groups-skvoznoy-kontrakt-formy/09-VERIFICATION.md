---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
verified: 2026-08-29T23:38:51Z
status: gaps_found
score: 9/15 must-haves verified
behavior_unverified: 3
overrides_applied: 0
re_verification: null
walkthrough_executed:
  date: 2026-08-30
  browser: "Chrome / macOS"
  artifact: ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md"
  artifact_status: diagnosed
  stand: "UAT9- (account_id=50, 40 групп, 30 активных); стенд убран, проверка вернула `UAT9- rows: groups=0 accounts=0`"
a1_closed_by_measurement:
  assumption: "A1 — браузер снимает фокус с элемента, которому выставлен `disabled`"
  answer: "нет"
  observed: "document.activeElement при нетронутой мыши вернул `<body>` — фокус на тумблере не остался"
  measured: "2026-08-30, Chrome / macOS, проверка 3.3 обхода"
  verdict: "допущение A1 ИСТИННО; столкновение D-06 ↔ QUAL-06 настоящее, а не выведенное из чтения кода"
  owner_decision: option-b
  owner_basis: >-
    единственный вариант, закрывающий и QUAL-01, и QUAL-06 разом; допущение A4
    прочитано по исходнику гейта Фазы 8 ДО выбора (запрещено слово `queue`,
    отмена названа незадетой прямым текстом, правило применяется только к пяти
    маршрутам периметра, тумблера среди них нет)
  applied: >-
    app/templates/account_groups/includes/group_row.html — вызов макроса-обёртки
    перестал передавать собственную цель блокировки и передаёт sync='this:drop'
    (признак поставлен В ВЫЗОВЕ, а не в макросе); запись перечня
    DISABLED_ELT_EXCEPTIONS приведена к новому положению дел следующей строкой
    летописи с причиной «конфликт снят решением владельца по итогам измерения 3.3»
  overrides_needed: false
  price: >-
    QUAL-06 на пути тумблера решением D-06 больше не отменяется, но и не
    подтверждён наблюдением: возврат фокуса на ИЗМЕНЁННОЙ разметке в браузере не
    измерялся ни разу
  original_record: ".planning/REQUIREMENTS.md — раздел «Фаза 9 — свидетельства к пересмотру состояний», подраздел «ЗАПИСЬ ИСХОДА D-06 ↔ QUAL-06 — ОРИГИНАЛ»"
  copy_notice: >-
    ⚠️ ЭТО ПОМЕЧЕННАЯ КОПИЯ, А НЕ ОРИГИНАЛ. Оригинал живёт в
    .planning/REQUIREMENTS.md рядом со строкой QUAL-06 раздела свидетельств
    (поле original_record выше). Настоящий файл ПЕРЕГЕНЕРИРУЕТСЯ следующим
    прогоном проверки, поэтому ПОВТОРНАЯ ВЕРИФИКАЦИЯ ОБЯЗАНА сохранить эту
    запись либо перенести её из оригинала, а не потерять при перегенерации. При
    расхождении копии с оригиналом правым является .planning/REQUIREMENTS.md —
    тем же правилом, которым разрешается WARN-8.
walkthrough_divergences:
  - id: DIV-09-01
    status: open
    found_in: "проверка 3.4 обхода (удаление группы), 2026-08-30, Chrome / macOS"
    observed: 'консоль браузера: htmx.min.js:1 The selector "#group-list-sentinel" on hx-include returned no matches!'
    mechanism: >-
      group_row.html:207 передаёт hx_include='#group-list-sentinel' в макрос
      панели подтверждения БЕЗУСЛОВНО, а сентинел рисуется только под
      {% if has_next %} (list.html:202, partial_cards.html:19), где
      has_next = len(rows) > PAGE_SIZE и PAGE_SIZE = 30
      (account_groups.py:227, :35)
    consequences:
      - "аккаунт с 30 группами и меньше: сентинела нет НИКОГДА — ошибка валится при КАЖДОМ удалении; это основной путь в бою, а не край случая"
      - "аккаунт с >30 групп: после долистывания до конца последняя порция приходит с has_next=false, сентинел исчезает из документа насовсем — дальше та же ошибка на каждом удалении"
    data_harm: >-
      нет: rendered_rows приходит None, repaired_offset становится None
      (account_groups.py:629-631), четвёртый внеполосный узел не отправляется —
      что корректно, чинить нечего
    real_harm: >-
      ПОТЕРЯН ДИАГНОСТИЧЕСКИЙ ПРИЗНАК. Обход в трёх местах опирается на «ответ
      200 И ЧИСТАЯ КОНСОЛЬ» как на опорный признак (предупреждения проверок 3.2
      и 3.5); на этом экране консоль больше не чистая, и признак перестал
      работать
    why_open: >-
      правка под находку в объём плана 09-09 не входит и владельцем не
      выбиралась: hx_include заведён планом 09-05 как часть починки курсора
      прокрутки, и изменение его условия — правка ровно той поверхности, ради
      проверки которой обход и проводился. Вход ближайшей фазы, трогающей этот
      экран
    recorded_in: "09-UAT.md §«Расхождения обхода», РАСХ-1"
  - id: DIV-09-02
    status: open
    provenance: >-
      НЕ НАБЛЮДЕНИЕ ОБХОДА. Найдено ЧТЕНИЕМ ИСХОДНИКА при приёмке правки самого
      плана 09-09 (ветка option-b); в браузере ещё не наблюдалось. Смешивать с
      DIV-09-01, увиденной глазом, нельзя
    found_in: "приёмка правки плана 09-09 оркестратором, 2026-08-30"
    observed: 'ожидаемая строка консоли: The selector "find button[type=submit]" on hx-disabled-elt returned no matches!'
    mechanism: >-
      ветка option-b сняла с вызова передачу disabled_elt='find
      input[type=checkbox]', и форма тумблера перешла на умолчание
      макроса-обёртки 'find button[type=submit]'. Но form_wrapper.html печатает
      hx-disabled-elt БЕЗУСЛОВНО, а единственная кнопка отправки этой формы —
      «Применить» — обёрнута в x-init="if (window.htmx) $el.remove()" и при
      живом htmx с формы снимается. ve(t, n) в htmx.min.js — ОБЩИЙ разрешитель
      селекторов для hx-include, hx-indicator И hx-disabled-elt: при нуле
      совпадений пишет "returned no matches!" и возвращает [be], где
      be = document.createElement("output"). nn(e) зовёт его на КАЖДОМ запросе
    consequences:
      - "строка в консоль на КАЖДОЕ переключение тумблера — на самое частое действие экрана"
      - "до правки её не было: 'find input[type=checkbox]' совпадал; регрессия введена самой веткой option-b"
    data_harm: >-
      нет: блокировка ставится на детачнутый <output>, до документа не доходит
    real_harm: >-
      ТОТ ЖЕ, ЧТО У DIV-09-01, И НА ТОМ ЖЕ ЭКРАНЕ: ещё одна строка в консоли,
      добивающая признак «200 и чистая консоль». Суита этого не видит и увидеть
      не может — 2649 тестов идут через ASGI и не исполняют JS, а разметка формы
      сама по себе верна: неверно СООТНОШЕНИЕ объявленного селектора с деревом,
      которое создаст Alpine
    why_open: >-
      починка требует решения о роде записи в DISABLED_ELT_EXCEPTIONS — либо
      макрос учится не печатать атрибут при disabled_elt=None и форма тумблера
      заводит собственную запись (объявленное число 2 → 3), либо форма получает
      цель, переживающую Alpine. Оба — правка поверхности, раздаваемой макросом
      на сорок семь форм вехи, и выбор принадлежит владельцу, а не приёмке
    recorded_in: "09-UAT.md §«Расхождения обхода», РАСХ-2"
    same_class_as: DIV-09-01
gaps:
  - truth: "Включение, выключение и удаление группы на /accounts/{id}/groups происходят без перезагрузки страницы: ответ — фрагмент по id конкретной строки плюс побочные области через hx-swap-oob (роадмап, критерий 1)"
    status: partial
    reason: >-
      Тумблер закрыт целиком. У УДАЛЕНИЯ множество «побочных областей» НЕПОЛНО:
      сентинел бесконечной прокрутки несёт запечённое АБСОЛЮТНОЕ смещение
      (offset=30), ответ удаления его не трогает, и после удаления строки с
      первой страницы одна группа исчезает из списка молча. Воспроизведено
      исполнением на этом дереве (35 групп, удалена g5 → g31 не отрисовывается
      ни первой страницей, ни порцией сентинела; ошибки нет ни в статусе, ни в
      теле, ни в консоли). ДО фазы этого не было: удаление отвечало 302,
      страница перестраивалась целиком вместе с сентинелом. То есть это
      регрессия, внесённая переводом удаления на фрагментный ответ, а не
      предсуществующий дефект пагинации.
    artifacts:
      - path: "app/pages/account_groups.py:486-523"
        issue: "фрагментная ветка удаления не чинит курсор пагинации"
      - path: "app/templates/account_groups/partials/delete_response.html:44-46"
        issue: "три внеполосных узла; сентинела среди них нет"
      - path: "app/templates/account_groups/list.html:202"
        issue: "offset={{ next_offset }} запечён в hx-get сентинела один раз при отрисовке страницы"
      - path: "app/templates/account_groups/partial_cards.html:17"
        issue: "та же разметка сентинела во второй копии (обе утверждены идентичными)"
    missing:
      - "Починка курсора пагинации в ответе удаления: либо keyset-пагинация (after_id вместо offset), либо четвёртый OOB-узел, подменяющий сентинел по стабильному id с уменьшенным offset"
      - "Регрессионный тест: посеять PAGE_SIZE+5 групп, удалить одну с первой страницы через htmx, затем запросить то, что дёрнул бы сентинел, и утвердить, что в объединении двух страниц не потеряно ни одного id"
      - "Ветвление «список опустел» решается по account-wide total_groups (account_groups.py:488); при активном поиске удаление последней НАЙДЕННОЙ строки оставляет пустую карточку без пустого состояния и без кнопки «Сбросить» (WR-06)"
  - truth: "Свойства качества раздаёт ОДИН макрос-обёртка формы: hx-disabled-elt целью ТОЛЬКО find button[type=submit] (роадмап, критерий 3)"
    status: partial
    reason: >-
      Половина «ОДИН макрос» выполнена и огейчена: form_wrapper.html печатает и
      hx-disabled-elt, и hx-indicator; селектор индикатора не передаёт ни один
      вызывающий; правило «каждый hx-post рождён компонентным макросом» имеет
      зубы. Половина «целью ТОЛЬКО find button[type=submit]» на пилотной
      поверхности НЕ выполнена: единственная переведённая фазой форма тумблера
      передаёт свой селектор find input[type=checkbox] (D-06). Отступление
      объявлено записью DISABLED_ELT_EXCEPTIONS с обоснованием, числом и сверкой
      вызывающих — то есть это осознанная девиация, а не забытая работа, — но
      формулировка критерия выделяет «только» жирным, а само отступление и есть
      прямая причина того, что QUAL-06 на этом пути не выполняется. Половина про
      кнопку Отмены выполнена и огейчена (test_cancel_is_never_disabled, четыре
      проверяемых множества + контроль).
    artifacts:
      - path: "app/templates/account_groups/includes/group_row.html:109-111"
        issue: "disabled_elt='find input[type=checkbox]' вместо общего умолчания"
      - path: "tests/test_templates/test_htmx_markup_gates.py:2004-2050"
        issue: "DISABLED_ELT_EXCEPTIONS['components/form_wrapper.html'] — запись, выводящая пилотную форму из-под буквы критерия"
    missing:
      - "ЛИБО решение владельца по одному из трёх записанных способов снятия конфликта D-06 ↔ QUAL-06 (не блокировать чекбокс / hx-sync='this:drop' / вариант с label помечен негодным)"
      - "ЛИБО явная запись overrides: в этом файле, принимающая девиацию от формулировки критерия 3"
    resolution: >-
      ЗАКРЫТ планом 09-09 первой ветвью: владелец выбрал hx-sync='this:drop'
      (вариант «б») ПОСЛЕ измерения 3.3, форма тумблера перестала передавать
      собственную цель блокировки и живёт по общему умолчанию
      'find button[type=submit]'. Отступления от буквы критерия 3 на пилотной
      поверхности не осталось, записи overrides этот исход не требует
      (overrides_applied остаётся 0). Полная запись — a1_closed_by_measurement,
      оригинал — .planning/REQUIREMENTS.md
  - truth: "REQUIREMENTS.md: QUAL-01 отмечено Complete коммитом e0b59a3"
    status: failed
    reason: >-
      Отметка не заработана текстом требования. QUAL-01 гласит «hx-disabled-elt
      на каждой форме письма, целью ТОЛЬКО find button[type=submit]» — на
      пилотной форме цель другая (см. предыдущий пункт). Поведенческая половина
      («двойное нажатие не создаёт второй записи» как реальная защита браузера)
      объявлена planами verification: backstop и закрывается пунктом 6.2
      ручного обхода, который НЕ ЗАПОЛНЕН. Серверная идемпотентность (инверсия,
      одна строка под тройным WHERE) — выполнена и закреплена; она и есть та
      часть, которая заработана.
    artifacts:
      - path: ".planning/REQUIREMENTS.md:39,140"
        issue: "[x] QUAL-01 / | QUAL-01 | Phase 9 | Complete |"
    missing:
      - "Откатить QUAL-01 в Pending до закрытия UAT 6.2 и решения по D-06 ↔ QUAL-06, либо принять девиацию overrides-записью и записать это рядом с отметкой"
    resolution: >-
      Отметка откачена коммитом 183511c ещё до плана 09-09 (сегодня QUAL-01
      стоит Gaps Found). План 09-09 состояний требований не переводил ВОВСЕ —
      неприкосновенность столбца Status и флажков проверена сличением строк до и
      после правки, оба вывода пусты, — а вместо отметок оставил раздел
      свидетельств «Фаза 9 — свидетельства к пересмотру состояний» в
      .planning/REQUIREMENTS.md: на каждое из пяти требований машинная проверка,
      наблюдённый признак и вердикт. Перевод состояний делает повторная
      верификация, читающая этот раздел
deferred: []
behavior_unverified_items:
  - truth: "Своп действительно произошёл и ИМЕННО в #group-row-N; OOB-счётчик приземлился в #account-groups-count; позиция прокрутки списка не потеряна (роадмап, критерий 4 и поведенческая половина критерия 1)"
    test: "Открыть /accounts/{A}/groups с 7+ группами, прокрутить ниже сгиба, переключить тумблер средней видимой строки; DevTools → Elements"
    expected: "Строка перерисовалась на месте, узел id=group-row-N в документе один, позиция прокрутки не изменилась; содержимое #account-groups-count заменено, сама обёртка осталась той же"
    why_human: "httpx не свопает и не собирает внеполосные узлы — он видит только ТЕКСТ ответа. Браузерного стенда в проекте нет (E2E-01 отложен). Процедура — 09-UAT.md, проверки 3.1 и 3.2"
    status: closed
    closed_by: >-
      обход 2026-08-30, Chrome / macOS, проверки 3.1 и 3.2: «Строка
      перерисовалась на месте; список не прыгнул; соседние строки не мигнули»;
      «Счётчик над списком обновился корректно». Правкой плана 09-09 эта
      поверхность не затронута: атрибуты цели, подмены и внеполосных узлов не
      менялись
  - truth: "После переключения тумблера КЛАВИАТУРОЙ (Space) фокус остался на том же тумблере (QUAL-06)"
    test: "Tab до чекбокса #group-toggle-N, Space, затем document.activeElement в консоли, мышь не трогать"
    expected: "ЗАПИСАННОЕ ОЖИДАНИЕ СЕГОДНЯ — «НЕТ» (D-06 снимает блокировку ПОСЛЕ свапа). Измерить, а не предположить: допущение A1 («браузер снимает фокус с disabled-элемента») не проверялось ни разу. Если «да» — конфликта D-06 ↔ QUAL-06 не существует и это надо записать так же явно"
    why_human: "Требует живого document.activeElement и реального порядка блокировка→своп→разблокировка в браузере. Процедура — 09-UAT.md, проверка 3.3, с тремя записанными способами снятия конфликта"
    status: measured_then_changed
    measured: >-
      2026-08-30, Chrome / macOS: ответ «нет» — document.activeElement вернул
      <body>. Записанное ожидание подтвердилось, допущение A1 закрыто измерением
    still_unverified: >-
      ⚠️ ОСТАЁТСЯ НЕЗАКРЫТЫМ, И ЭТО НЕ ФОРМАЛЬНОСТЬ. Решением владельца (вариант
      «б») блокировка чекбокса снята, и сегодняшнее измерение описывает механизм,
      которого на экране больше нет. Что фокус ВЕРНЁТСЯ на тумблер после правки —
      рассуждение (чекбокс уезжает из документа вместе с подменённой строкой, id
      стабилен в обеих отрисовках), а не наблюдение. Проверка 3.3 обязана быть
      пройдена ЗАНОВО на изменённой разметке
  - truth: "hx-indicator виден на медленной сети и не мигает на быстрой; hx-disabled-elt реально мешает второму нажатию; кнопка Отмены остаётся нажимаемой во время запроса (роадмап, критерий 3, поведенческая половина)"
    test: "DevTools → Network → Slow 3G, переключить тумблер; снять троттлинг и переключить 5-6 раз; на Slow 3G нажать дважды быстро; открыть подтверждение удаления и нажать «Отмена» во время запроса"
    expected: "Точка .form-busy появляется на медленной и не мигает на быстрой; в Network один запрос toggle, а не два; «Отмена» нажимаема"
    why_human: "Порог 300 мс — свойство времени и глаза; машинно проверено только ОБЪЯВЛЕНИЕ порога в app.css. Процедура — 09-UAT.md, проверки 6.1 и 6.2"
    status: partially_closed
    closed_by: >-
      обход 2026-08-30, Chrome / macOS, проверка 6.1: «Точка на Slow 3G
      появляется и держится; при снятом троттлинге 5-6 переключений подряд — не
      мигнула ни разу» (допущение A3 подтверждено). Проверка 6.2: «двойное
      нажатие даёт ОДИН запрос toggle», «Отмена во время запроса остаётся
      нажимаемой»
    still_unverified: >-
      ⚠️ Наблюдение 6.2 снято с ПРЕЖНЕГО механизма (блокировка чекбокса). После
      решения владельца второе нажатие отбрасывает hx-sync="this:drop", и на
      новом механизме 6.2 глазами не наблюдалась. Отдельно не проводился шаг
      «дождаться ОТКАЗА, остановив сервер, и убедиться, что кнопка подтверждения
      не осталась включённой с признаком занятости» (WARN-2)
  - truth: "Осиротевшая панель подтверждения снята после удаления; после подмены строки панель не задвоилась, Alpine в ней жив (граница пункта 5)"
    test: "Удалить группу через панель; поиск по group-del- в Elements; затем переключить тумблер соседней строки и открыть подтверждение у подменённой и у соседней строки"
    expected: "Узла group-del-{N} удалённой группы не осталось, невидимого role=dialog поверх пустого места нет; после свапа узел ровно один, Esc закрывает, имя группы в панели верное"
    why_human: "Требует живого Alpine и живого DOM после свапа — httpx внеполосных узлов не собирает. Процедура — 09-UAT.md, проверки 5.1 и 5.2"
    status: closed
    closed_by: >-
      обход 2026-08-30, Chrome / macOS, проверки 5.1, 5.2 и 5.3:
      «Осиротевшей панели нет, Tab не проваливается»; «Панель не задвоилась, Esc
      закрывает, имя группы верное» (допущение A5 подтверждено); «После удаления
      единственной найденной строки экран показал „Группы не найдены“ с кнопкой
      „Сбросить“; „Сбросить“ вернула полный список»
  - truth: "Курсор прокрутки переживает фрагментное удаление: ни одна группа не пропала и ни одна не задвоилась (FORM-02, поведенческая половина; чинил план 09-05)"
    test: "09-UAT.md, проверка 3.5: выписать название последней видимой строки, удалить строку из первой трети, долистать до низа, сверить список с самим собой"
    expected: "Ни одно название не пропало и ни одно не появилось дважды; выписанное название на месте и ровно одно; счётчик сходится с числом отрисованных строк"
    why_human: "Признак отказа МОЛЧАЛИВ: 200, чистая консоль, честный счётчик — расхождение видно только сверкой списка с самим собой"
    status: partially_closed
    closed_by: >-
      обход 2026-08-30, Chrome / macOS: обход по четырём шагам проведён, дыр и
      повторов в списке не обнаружено
    still_unverified: >-
      ⚠️ Выписанного названия строки (шаг 3.5.2) отметка не несёт, а правило самой
      проверки требует его прямо. Домысливать за человека запрещено, поэтому
      наблюдательная половина FORM-02 остаётся незакрытой до повторного прохода
      3.5 с выписанным названием
coincidental_reliance_items:
  - truth: "Ответ удаления ОДИНАКОВ для найденной и не найденной группы (QUAL-01, идемпотентность, D-04)"
    reason: fixture-only
    harden: >-
      Свойство верно ПО КОДУ (тело собирается из group_id пути, числа считаются
      одинаково), но охраняющий его тест
      test_repeated_delete_is_harmless_over_htmx сеет ОДНУ группу и потому оба
      раза попадает в ветку total_groups == 0: сравниваются два пустых тела
      204-ответа, и утверждаемое равенство ФРАГМЕНТОВ не исполняется ни разу.
      Посеять минимум две группы и добавить положительное утверждение, что
      фрагментная ветка достигнута (status 200 и наличие id=group-row-N).
human_verification_closed:
  - test: "09-UAT.md, пункт 3 (3.1 своп, 3.2 приземление счётчика, 3.3 фокус клавиатурой, 3.4 фокус после удаления)"
    closed_by: "обход 2026-08-30, Chrome / macOS; таблица отметок пункта 3 заполнена наблюдённым признаком, для 3.3 записан фактический ответ «нет» и выбранный владельцем вариант «б»"
  - test: "09-UAT.md, пункт 6 (6.1 порог индикатора, 6.2 двойное нажатие + кнопка Отмены)"
    closed_by: "обход 2026-08-30; допущение A3 подтверждено наблюдением 6.1.3 (не мигнула ни разу при снятом троттлинге). ⚠️ Про 6.2 см. behavior_unverified_items — механизм с тех пор изменён"
  - test: "09-UAT.md, пункт 5 частично (5.1 сирота снята, 5.2 не задвоилась Alpine жив, 5.3 пустое состояние под поиском)"
    closed_by: "обход 2026-08-30; допущение A5 закрыто наблюдением 5.2.4 («имя группы верное»)"
  - test: "РЕШЕНИЕ ВЛАДЕЛЬЦА по конфликту D-06 ↔ QUAL-06"
    closed_by: "принято 2026-08-30 ПОСЛЕ измерения: вариант «б» (hx-sync='this:drop' вместо блокировки), применён планом 09-09; overrides не требуется — конфликт снят, а не принят. Полная запись — a1_closed_by_measurement выше и её оригинал в .planning/REQUIREMENTS.md"
  - test: "РЕШЕНИЕ ВЛАДЕЛЬЦА: судьба отметки QUAL-01"
    closed_by: "снято по существу: столкновение с буквой критерия 3 устранено вариантом «б», форма тумблера живёт по общему умолчанию цели блокировки. Перевод состояния требования — работа повторной верификации, читающей раздел свидетельств .planning/REQUIREMENTS.md; план 09-09 состояний не переводил вовсе"
human_verification:
  - test: "09-UAT.md, проверка 3.3 ПОВТОРНО — на изменённой разметке (после снятия блокировки чекбокса)"
    expected: "Tab до #group-toggle-N, Space, document.activeElement при нетронутой мыши — фокус ОСТАЛСЯ на тумблере с тем же id"
    why_human: "Сегодняшний ответ «нет» описывает механизм, которого на экране больше нет; возврат фокуса после правки — рассуждение, а не измерение"
  - test: "09-UAT.md, проверка 6.2 ПОВТОРНО — на новом механизме защиты от двойного нажатия"
    expected: "На Slow 3G двойное нажатие даёт ОДИН запрос toggle: второй отбрасывается клиентом (hx-sync='this:drop'), а не блокировкой чекбокса"
    why_human: "Отбрасывание запроса — свойство рантайма в браузере; суита утверждает только наличие атрибута в разметке"
  - test: "09-UAT.md, проверка 3.5 ПОВТОРНО — с выписанным названием строки"
    expected: "Отметка несёт выписанное на шаге 3.5.2 название и результат сверки списка с самим собой"
    why_human: "Признак отказа молчалив; правило проверки требует выписанного названия прямо, а сегодняшняя отметка его не несёт"
  - test: "РЕШЕНИЕ по расхождению DIV-09-01 (hx-include на несуществующий сентинел)"
    expected: "Владелец решает, чинится ли условие hx_include (и в какой фазе), — правка в объём плана 09-09 не входила"
    why_human: "hx_include заведён планом 09-05 как часть починки курсора прокрутки; изменение его условия — правка проверяемой обходом поверхности"
  - test: "НАБЛЮДЕНИЕ расхождения DIV-09-02 в браузере (hx-disabled-elt на снятую Alpine кнопку)"
    expected: "Одно переключение тумблера с открытой консолью: строка 'The selector \"find button[type=submit]\" on hx-disabled-elt returned no matches!' появляется — или не появляется, и тогда чтение исходника было неверным"
    why_human: "DIV-09-02 найдена ЧТЕНИЕМ, а не глазом; подпись под ненаблюдённым запрещена правилом фазы"
  - test: "РЕШЕНИЕ по расхождению DIV-09-02 (род записи в DISABLED_ELT_EXCEPTIONS)"
    expected: "Владелец выбирает: макрос перестаёт печатать атрибут при disabled_elt=None и форма тумблера заводит свою запись (2 → 3), либо форма получает цель, переживающую Alpine"
    why_human: "Правка поверхности, раздаваемой макросом на сорок семь форм вехи; выбор принадлежит владельцу, а не приёмке"
---

# Phase 9: Пилот на `account_groups` — сквозной контракт формы — Verification Report

**Phase Goal:** контракт «фрагмент + OOB + notice + деградация + свойства качества» доказан целиком на наименьшей поверхности, и гейты Фазы 8 получают зубы прежде, чем начнётся массовая работа
**Verified:** 2026-08-29T23:38:51Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

Механическая часть цели достигнута в значительной степени: контракт формы
существует ОДНИМ файлом, оба обработчика экрана идут через слой ответа, форма
ответа «фрагмент + внеполосный узел» и форма «без основной цели свопа + три
внеполосных узла» шипнуты, а гейты Фазы 8 действительно получили зубы —
21 контроль, каждый краснеет на подставленном дереве или подставленной копии
стилей, и два из них проверены временной правкой ЖИВОГО дерева.

Цель, однако, не достигнута ЦЕЛИКОМ, и по двум разным причинам:

1. **Контракт на пилотной поверхности не полон.** Множество «побочных областей»
   у удаления оказалось неполным: сентинел бесконечной прокрутки остался с
   устаревшим абсолютным смещением, и группа исчезает из списка молча.
   Воспроизведено исполнением. Это ровно тот класс отказа, ради поимки которого
   пилот и берётся на наименьшей поверхности — но он не пойман, а внесён.
2. **Одна из заявленных гарантий контракта на этой же поверхности отменена
   другой.** `hx-disabled-elt` (D-06) отменяет возврат фокуса (QUAL-06). Это
   записано ЗАРАНЕЕ, с тремя способами снятия и с выведением в ручной UAT, —
   что делает работу честной, но не делает контракт доказанным.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | **SC1.** Тумблер и удаление без перезагрузки: фрагмент по `id` строки + побочные области через `hx-swap-oob`; контейнер целиком не перерисовывается | ✗ FAILED | Тумблер — ✓ (`test_toggle_returns_the_row_fragment`: 200, нет `<!DOCTYPE`, есть `id="group-row-N"`, есть `hx-swap-oob="innerHTML:#account-groups-count"`). Удаление — ✓ по трём объявленным узлам (`test_delete_returns_oob_nodes`), но **множество побочных областей неполно**: сентинел с `offset=30` не обновляется. Воспроизведено: 35 групп, удалена g5 → **g31 потеряна** (`MISSING from union: [31]`) |
| 2 | **SC2.** Обе формы рабочие без JS: `method="post"`, непустой `action`, `hx-post` == `action` посимвольно, `action` ведёт на маршрут полного документа (GATE-04) | ✓ VERIFIED | G-3 и G-4 идут по `_post_sites` БЕЗ исключения мест определения макросов → покрывают и `form_wrapper.html`, и `modal.html` (85 гейтовых тестов зелены). Поведенчески: `test_toggle_degrades_without_htmx` и `test_delete_degrades_without_htmx` — 302 на `/accounts/{a}/groups`, действие состоялось. ⚠️ см. WARN-3 про G-5 |
| 3 | **SC3.** Свойства качества раздаёт ОДИН макрос; `hx-disabled-elt` целью **только** `find button[type=submit]`; `hx-indicator` одним классом с порогом через `transition-delay` | ✗ FAILED | «ОДИН макрос» — ✓ (`form_wrapper.html:80-89` печатает оба атрибута; селектор индикатора зашит; `test_every_htmx_post_is_born_of_a_component_macro` + `MACRO_BORN_EXCEPTIONS`). «Отмена не блокируется» — ✓ (`test_cancel_is_never_disabled`, 4 множества + контроль). **«только `find button[type=submit]`» — НЕТ**: `group_row.html:111` передаёт `find input[type=checkbox]` (объявленное исключение D-06). Индикатор — ✓ `.form-busy` + `transition-delay .3s` на `.htmx-request` (`app.css:1735-1746`), имя НЕ `htmx-indicator` |
| 4 | **SC4.** Своп произошёл ИМЕННО в тот элемент, OOB-счётчик приземлился, фокус вернулся — ручной UAT, пункт 3 | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Артефакт `09-UAT.md` существует (348 строк), называет пункты 3/6/5 поимённо, разбивает пункт 3 на четыре независимых исхода. `status: pending`, **все три таблицы отметок ПУСТЫ**. Критерий сам говорит «сервером не доказуемо в принципе» |
| 5 | **SC5.** Проверено и записано, несёт ли `#group-row-N` атрибут `x-data`; гейт границы Alpine держит это дальше | ✓ VERIFIED | `group_row.html:40` — `<div data-group-row id="group-row-{{ group.id }}"…>`, `x-data` нет. Записано в трёх местах (`09-RESEARCH.md:442-460` с построчной таблицей узлов, `09-CONTEXT.md:35`, `09-DISCUSSION-LOG.md:109`). Держит `test_no_client_state_expression_lives_inside_a_swap_declaring_template` (D-08a) с антивакуумным утверждением и контролем; GATE-06 сохранён дословно (`grep -c` → 1) |
| 6 | У обоих переведённых обработчиков нет собственного `RedirectResponse` ни в одной ветке, включая «нет сессии» — G-2 впервые получил предмет | ✓ VERIFIED | `account_groups.py:346,392` и `:466,486` — все ветки через `respond()`. `NOT_YET_CONVERTED_COUNT` 36 → 34, `test_no_converted_handler_builds_its_own_redirect` зелен на непустом множестве |
| 7 | Ответ удаления ОДИНАКОВ для найденной и не найденной группы (идемпотентность, D-04) | ✓ VERIFIED (coincidental-reliance) | Верно по коду: тело собирается из `group_id` ПУТИ (`account_groups.py:514-528`), ветвление по числам аккаунта из адреса. Но охраняющий тест исполняет только ветку 204 — см. `coincidental_reliance_items` и WARN-4 |
| 8 | Кнопка Отмены в панели подтверждения не блокируется никогда | ✓ VERIFIED | `modal.html:138` — `type="button"`, `hx-disabled-elt="find button[type=submit]"`. Правило проверяет ТРИ множества селекторов плюс сам тип кнопки; зубы доказаны и подстановкой, и временной правкой живого дерева (сводка 09-02) |
| 9 | Каждый `hx-post` рождён компонентным макросом либо стоит в перечне с обоснованием и назначенной фазой (FORM-09) | ✓ VERIFIED | `_offenders_macro_born` + `MACRO_BORN_EXCEPTIONS = {ads/form.html: «ПРЕДМЕТ Фазы 12»}`, `MACRO_BORN_EXCEPTIONS_DECLARED = 1`, три утверждения SP-1 включая несущее (`test_every_macro_born_exception_is_actually_outside_components`) + контроль. `hx-post` в `group_row.html` вне комментариев отсутствует (проверено исполнением) |
| 10 | Десять движущихся и четыре неподвижных инвентарных числа сведены и утверждены собственными обходами | ✓ VERIFIED | 85 зелёных на `test_htmx_markup_gates.py` + `test_htmx_gates.py`. Летопись каждого числа с составом; неподвижность утверждена записями, а не молчанием; `len(paths)` пересчитан исполнением и остался 2 |
| 11 | Плашка обрыва связи называет расхождение экрана с сервером; третьего встроенного обработчика не появилось | ✓ VERIFIED | `htmx_error_banner.html:76` — добавленное предложение на месте; `grep -c addEventListener` → **2**; `test_the_network_banner_names_the_screen_server_divergence` и 6 соседних утверждений рамки Фазы 8 зелены |
| 12 | Артефакт ручного обхода существует как ПРОЦЕДУРА, называет пункты 3 и 6 поимённо, пункт 5 — частично, с записанным ожиданием «нет» и тремя способами снятия конфликта | ✓ VERIFIED | `09-UAT.md` — все требуемые подстроки на месте (`ОЖИДАЕМЫЙ ОТВЕТ СЕГОДНЯ`, `Slow 3G`, `group-del-`, `account-groups-count`, `E2E-01`, `hx-sync`, `label`, «негодный»); раздел «что НЕ входит» относит пункт 4 к Фазе 10, пункт 5 целиком — к Фазе 11 |
| 13 | После переключения тумблера клавиатурой фокус остался на том же тумблере (QUAL-06) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Разметка обеспечена: `id="group-toggle-N"` есть и в первичной отрисовке, и во фрагменте (`test_toggle_fragment_keeps_the_toggle_id`, с несущей половиной «это не документ»). Фактический возврат фокуса — не измерен ни разу; записанное ожидание «НЕТ», допущение A1 не проверялось |
| 14 | Индикатор виден на медленной сети и не мигает на быстрой; блокировка реально мешает второму нажатию | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Машинно закреплено только ОБЪЯВЛЕНИЕ порога в `app.css`. UAT 6.1/6.2 не заполнены |
| 15 | Осиротевшая панель снята после удаления; после подмены строки не задвоилась; Alpine в ней жив | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Разметка есть: `delete_response.html:45` (`hx-swap-oob="delete"` по `#group-del-N`), `toggle_response.html:29` (`with_modal=false`, закреплено `test_toggle_fragment_carries_no_second_modal`). Фактическое снятие — httpx внеполосных узлов не собирает |

**Score:** 9/15 truths verified (4 present, behavior-unverified; 2 failed)

### Deferred Items

Проверено против целей и критериев Фаз 10-15: **ни один из найденных пробелов не
адресован позднейшей фазой.**

- Фаза 10 (FORM-06) закрывает 16 мест подтверждения и сводное закрытие пункта 4
  ручного UAT — про курсор пагинации там нет ничего.
- Фаза 11 говорит о фрагментах и `hx-swap-oob` в ДРУГИХ разделах и о
  «переинициализации Alpine» — про сентинел экрана групп ничего.
- Фаза 15 закрывает инвентари и девять пунктов UAT сводно, но не чинит дефекты.

Правило «при сомнении — не откладывать» применено: совпадений нет ни одного,
даже косвенного.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/templates/components/form_wrapper.html` | Макрос-обёртка, печатающий тег формы, `hx-post`, цель, блокировку и индикатор | ✓ VERIFIED | 89 строк, макрос `form_wrapper(action, target, swap, trigger, disabled_elt='find button[type=submit]')`; `method="post"` литералом; `action` и `hx-post` — одно выражение; оба селектора с префиксом `find `; узел индикатора внутри формы; совместим с блочным вызовом |
| `app/templates/account_groups/partials/toggle_response.html` | Плоское тело: строка + узел счётчика | ✓ VERIFIED | 30 строк; `group_row(..., with_modal=false)` + `{% include count_rule_oob %}`; общей обёртки нет |
| `app/templates/account_groups/partials/count_rule_oob.html` | Один узел верхнего уровня `hx-swap-oob="innerHTML:#account-groups-count"` | ✓ VERIFIED | 42 строки; узел прямой потомок файла; включается ОБОИМИ ответами (`OOB_BLOCKS` +2, а не +3) |
| `app/templates/account_groups/partials/delete_response.html` | Три внеполосных узла верхнего уровня | ✓ VERIFIED | 46 строк; два `hx-swap-oob="delete"` по собственному `id` + включение узла счётчика. **Четвёртого узла (сентинел) нет — см. gap 1** |
| `app/static/css/app.css` — раздел «Индикатор htmx-формы» | `.form-busy` с порогом 300 мс через `transition-delay` | ⚠️ ЕСТЬ, НО ХРУПКО | `:1735-1746`; задержка на состоянии `.htmx-request`, покой сразу; имя НЕ `htmx-indicator`. **`display` не задан** — узел `<span>` (inline), размеры действуют только потому, что обе сегодняшние формы оказались flex-контейнерами (WARN-5) |
| `app/pages/account_groups.py` — `_group_counts()` | Один источник чисел линейки | ✓ VERIFIED | `:110-149`; зовут страница и оба обработчика; поиск в подсчёт не входит (записано и является причиной WARN-6) |
| `app/templates/components/modal.html` — параметр `hx_post` | Опциональный, умолчание не двигает разметку ни на символ | ✓ VERIFIED | `:111` сигнатура, `:132-136` условные вставки. При `hx_post=false` `{%-` съедает перевод строки → вывод посимвольно прежний; исполнителем сверено с эталоном (1878 байт) |
| `tests/test_templates/test_htmx_markup_gates.py` — перечни и сканеры | `MACRO_DEFINITION_SITES`, `DISABLED_ELT_EXCEPTIONS`, `INDICATOR_CLASS`, `LONG_LIVED_REGION_IDS`, `PARAMETRIC_SWAP_TARGETS`, `MACRO_BORN_EXCEPTIONS`, `_macro_callers`, `_swap_declaring_templates`, `_app_css`/`_css_with` | ✓ VERIFIED | Все на месте; 64 теста файла зелены; **21 контроль** (`grep -c "^def test_control"` → 21) |
| `tests/test_pages/test_shell.py` — гейт смысла плашки | `test_the_network_banner_names_the_screen_server_divergence` | ✓ VERIFIED | Зелен; разбор сужен до ОДНОЙ строки узла (`_network_banner_line`), чтобы докстринг файла не зеленил гейт |
| `09-UAT.md` | Процедура ручного обхода | ✓ VERIFIED (как артефакт) / ⚠️ НЕ ИСПОЛНЕН | 348 строк, три таблицы отметок пусты, `status: pending` |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `group_row.html` | `form_wrapper.html` | `{% call form_wrapper(...) %}` | ✓ WIRED | `:109-115`; форма тумблера получает `hx-post`, цель, `hx-swap`, `hx-trigger`, блокировку и индикатор разом |
| `toggle_response.html` | `group_row(..., with_modal=false)` | импорт макроса | ✓ WIRED | Третий вызов того же макроса; `test_toggle_fragment_carries_no_second_modal` |
| `count_rule_oob.html` | `#account-groups-count` в `list.html` | `hx-swap-oob="innerHTML:#..."` | ✓ WIRED | Постоянная обёртка `list.html:178` существует всегда, даже пустая |
| `account_groups_toggle` / `_delete` | `respond(request, redirect=..., fragment=...)` | слой ответа Фазы 8 | ✓ WIRED | Все ветки обоих обработчиков; G-2 зелен на непустом множестве |
| `MACRO_DEFINITION_SITES` | `_action_sites()` | пропуск мест определения | ⚠️ WIRED, НО ГЛУШИТ | Правила адреса теперь применяются к ОДНОЙ форме дерева (`ads/form.html`); число скрытых за исключением ВЫЗЫВАЮЩИХ не считается (WARN-3) |
| `_macro_callers` → `DISABLED_ELT_EXCEPTIONS.callers` → `PARAMETRIC_SWAP_TARGETS.callers` → `_swap_declaring_templates` → правило D-08 | один сканер на фазу | ✓ WIRED | Множество измерено исполнением: `{account_groups/includes/group_row.html, components/form_wrapper.html}`; антивакуумное утверждение в теле правила краснеет при потере поля `callers` |
| `delete_response.html` | сентинел бесконечной прокрутки | — | ✗ NOT_WIRED | **Связи нет и в этом дефект.** `list.html:202` / `partial_cards.html:17` несут запечённый `offset`, ответ удаления его не трогает |
| `modal.html` (`hx_post=true`) | `account_groups_delete` | `hx-post="{{ action }}"` | ✓ WIRED | Единственное из 16 мест, получившее htmx; остальные 15 байт-в-байт прежние |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `toggle_response.html` | `group`, `schedules_count` | `db.execute(select(Group)…)` + `_schedule_counts()` | ✓ | ✓ FLOWING |
| `count_rule_oob.html` | `active_groups`, `total_groups` | `_group_counts()` — два выделенных `COUNT` | ✓ | ✓ FLOWING |
| `delete_response.html` | `group_id` | сегмент маршрута | ✓ | ✓ FLOWING (осознанно из ПУТИ, а не из строки — это и есть механизм неотличимости) |
| `list.html` `#account-groups-count` | `active_groups`, `total_groups` | `_group_counts()` в обработчике страницы | ✓ | ✓ FLOWING |
| `list.html` сентинел | `next_offset` | `PAGE_SIZE` при отрисовке страницы | ✓ на момент отрисовки | ⚠️ STATIC — значение запекается один раз и после фрагментного удаления становится ложным |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Оба пути экрана групп (тумблер, удаление, деградация, чужая группа) | `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly` | 102 passed | ✓ PASS |
| Все гейты разметки и гейты обработчиков | `uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q` | 85 passed | ✓ PASS |
| Гейт смысла плашки + рамка плашек Фазы 8 | `uv run pytest tests/test_pages/test_shell.py -k "network_banner or failure_banner"` | 7 passed | ✓ PASS |
| **Курсор пагинации переживает фрагментное удаление** | временный тест: 35 групп → `POST …/groups/5/delete` (htmx) → `GET …/groups/partial?offset=30&limit=30` | `MISSING from union: [31]` | **✗ FAIL** |
| Отсутствие долговых маркеров в 16 файлах фазы | `grep -E` по общепринятому перечню долговых слов (сам перечень здесь НЕ набран — см. пометку плана 09-09 ниже) | ни одного | ✓ PASS |
| Запреты планов (машинная часть) | `hx-push-url` → 0; `hx-confirm` → 0; новых файлов в `app/static/js/` → 0; `jinja2` в гейте → 0; GATE-06 → 1; запрещённый фильтр → 0; имя незаведённой константы → 0; `htmx_config.html` без диффа; `addEventListener` → 2; `hx-post` в `group_row.html` вне комментариев → нет | все соблюдены | ✓ PASS |

_Временный тест удалён; рабочее дерево чистое (`git status --short` → только предсуществующий `.planning/milestone.lock`)._

### Probe Execution

| Probe | Command | Result | Status |
|---|---|---|---|
| — | — | — | SKIPPED: `scripts/*/tests/probe-*.sh` в проекте нет; ни один PLAN/SUMMARY фазы проб не объявляет |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| **FORM-02** | 09-01, 09-02, 09-03 | Форма рабочая без JS: `method="post"`, непустой `action`, `hx-post` == `action` посимвольно, `action` ведёт на маршрут полного документа | ⚠️ SATISFIED С ОГОВОРКОЙ (отмечено Complete) | G-3/G-4 покрывают все три места отправки; деградация обоих маршрутов доказана поведенчески. Оговорка: G-5 («адрес ведёт на полный документ») больше не применяется НИ К ОДНОЙ форме, кроме `ads/form.html` — см. WARN-3 |
| **FORM-09** | 09-01, 09-03 | Единый макрос-обёртка раздаёт свойства качества вместо ревью 47 форм | ✓ SATISFIED (отмечено Complete) | `form_wrapper.html` + `test_every_htmx_post_is_born_of_a_component_macro` с зубами и объявленным перечнем исключений. Отметка заработана |
| **QUAL-01** | 09-01, 09-02, 09-03 | Двойное нажатие не создаёт второй записи; `hx-disabled-elt` целью **только** `find button[type=submit]`; Отмена не блокируется никогда | ✗ BLOCKED (ошибочно отмечено Complete) | Серверная идемпотентность ✓; Отмена ✓ машинно. «Только `find button[type=submit]`» — ✗ на пилотной форме; поведенческая защита от двойного нажатия — незакрытый backstop (UAT 6.2). См. gap 3 |
| **QUAL-02** | 09-01, 09-02, 09-04 | Индикатор через один класс в `app.css` с порогом через `transition-delay` | ? NEEDS HUMAN (Pending — верно) | Объявление порога ✓ машинно; видимость и отсутствие мигания — UAT 6.1, не заполнен. ⚠️ 09-04-SUMMARY объявляет `requirements-completed: [QUAL-02, QUAL-06]`, но REQUIREMENTS.md держит оба Pending. Прав файл, а не сводка |
| **QUAL-06** | 09-01, 09-04 | После подмены фрагмента фокус возвращается на элемент с тем же `id` | ✗ BLOCKED (Pending — верно) | Разметочная половина ✓ (стабильный `id` в обеих отрисовках). Механизм отменён D-06; записано заранее, три способа снятия конфликта не выбраны. Решение владельца требуется |

**Orphaned requirements:** нет. REQUIREMENTS.md отображает на Фазу 9 ровно пять
идентификаторов, и все пять заявлены планами.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `tests/test_pages/test_account_groups.py` | 1284-1330 | Тест зелен по построению: сеет ОДНУ группу → обе отправки уходят в ветку `total_groups == 0` (204, пустое тело); утверждаемое равенство ФРАГМЕНТОВ не исполняется ни разу | ⚠️ Warning | Третий случай «зелено по построению» за фазу — два предыдущих исполнители поймали сами и записали отклонениями. Этот доехал до отгрузки |
| `app/templates/account_groups/list.html` | 180-185 | Третья, неконтролируемая копия разметки линейки счётчика (байт-в-байт с `count_rule_oob.html:37-42`) | ⚠️ Warning | Заявленная гарантия единственного источника выполнена между ДВУМЯ ОТВЕТАМИ, но не между ответами и страницей. Правка формулировки в одном файле молча разведёт линейку после действия и линейку после F5; гейта нет (`OOB_BLOCKS` считает файлы с `hx-swap-oob`, а `list.html` его не несёт) |
| `app/static/css/app.css` | 1735-1740 | `.form-busy` без `display`; `<span>` — inline, `width/height/border-radius` не действуют | ⚠️ Warning | Работает только потому, что обе сегодняшние формы — flex-контейнеры. 3-й…47-й вызывающий получит невидимую точку 0×0, и отказ будет молчаливым: гейт проверяет только `transition`-задержки |
| `app/templates/components/modal.html` | 132-139 | Два владельца атрибута `disabled` на кнопке подтверждения: Alpine (`x-bind:disabled="sending"`) и htmx (`hx-disabled-elt`) | ⚠️ Warning | После отказа запроса htmx снимает `disabled`, а `sending` остаётся `true` → `aria-busy="true"` на включённой праздной кнопке; заявление шапки компонента про «вторую отправку отменяет `x-on:submit`» на htmx-пути неверно (`preventDefault` не останавливает собственный слушатель htmx) |
| `app/templates/account_groups/partials/delete_response.html` | 45 | Контракт возврата фокуса компонента панели обойдён: панель УДАЛЯЕТСЯ внеполосно, `hide()` не вызывается, `opener.focus()` не срабатывает | ℹ️ Info | Названо и принято в `09-UAT.md` §3.4 как изъятие, а не пропуск; регрессии нет — сегодня страница и так перезагружается |
| `tests/test_pages/test_htmx_gates.py` | 63-69, 791-795 | Два комментария в настоящем времени утверждают, что G-2 проходит вакуумно — после Фазы 9 у него два предмета | ℹ️ Info | Следующий читатель (автор Фазы 10) прочтёт «этот гейт не доказывает ничего» о гейте, который доказывает |
| `tests/test_pages/test_responsive_markup.py` | 1761-1768 | `test_billing_component_library_did_not_grow` пинует число, не относящееся к биллингу | ℹ️ Info | Отсюда двойной подъём 15 → 16; ловушка задокументирована, но не устранена |

**Отдельно (не anti-pattern, а положительное свойство):** долговых маркеров в
шестнадцати файлах фазы — **ни одного**. Отступления оформлены именованными
перечнями с обоснованием, назначенной фазой и отдельно утверждаемым числом, а не
комментариями.

⚠️ **ПЕРЕЧЕНЬ ДОЛГОВЫХ СЛОВ ЗДЕСЬ БОЛЬШЕ НЕ НАБРАН ДОСЛОВНО, И СНЯЛ ЕГО ПЛАН
09-09 (правка двух строк отчёта, выводов не меняющая).** Прежде отчёт называл
шесть слов буквально — и сам попадал в собственную выборку: проверка «долговых
маркеров в артефактах обхода и верификации не осталось» краснела на ПРОЗЕ ОБ
ИХ ОТСУТСТВИИ вместо самих маркеров. Это ровно та ловушка, которую проект уже
описал в гейтах разметки («имя запрещённой конструкции здесь не набрано: греп
удовлетворила бы документация о запрете»), и здесь она сработала во второй раз.
Утверждение отчёта не изменилось: маркеров не было и нет.

### Warnings (пронумерованы для ссылок)

- **WARN-1** — третья копия разметки линейки (`list.html` vs `count_rule_oob.html`).
- **WARN-2** — два владельца `disabled` на кнопке подтверждения панели.
- **WARN-3** — `MACRO_DEFINITION_SITES` выводит из-под ЧЕТЫРЁХ правил адреса оба
  места определения макросов, а число скрытых за исключением ВЫЗЫВАЮЩИХ не
  считается. Фазы 10-15 могут добавить 45 вызывающих с опечатками в адресе или
  с адресом на фрагментный маршрут, и `MACRO_DEFINITION_SITES_DECLARED` останется
  равным 2. То же по форме относится к `PARAMETRIC_SWAP_TARGETS`, снявшему
  G-9/G-11/G-12 с единственной настоящей цели подмены вехи.
- **WARN-4** — `test_repeated_delete_is_harmless_over_htmx` зелен по построению.
- **WARN-5** — `.form-busy` без `display`; «один класс на 47 форм» держится на
  совпадении.
- **WARN-6** — удаление последней НАЙДЕННОЙ строки при активном поиске оставляет
  пустую карточку без пустого состояния (ветка решается по account-wide
  `total_groups`).
- **WARN-7** — два устаревших комментария про вакуумность G-2.
- **WARN-8** — расхождение бухгалтерии: `09-04-SUMMARY.md` объявляет
  `requirements-completed: [QUAL-02, QUAL-06]`, а REQUIREMENTS.md держит оба
  Pending. Правым является REQUIREMENTS.md (QUAL-06 на пути тумблера прямо не
  выполняется); сводку не следует «сверять» в обратную сторону.
  **СНЯТ планом 09-09:** поле `requirements-completed` сводки 09-04 приведено к
  `.planning/REQUIREMENTS.md` — не молча, а с пометкой об исправлении в теле
  сводки (что было заявлено, что оказалось верным, почему правым признан файл
  требований). Бухгалтерия сведена в ОДНУ сторону, обратной сверки не
  производилось.

### Prohibitions (judgment-tier, NON-AUTHORITATIVE)

Все объявленные планами запреты несут `verification: none` / `status:
flagged-unverified`, то есть являются judgment-tier. Автономная верификация
записывает НЕАВТОРИТЕТНЫЙ вердикт судьи и флаг «unverified-prohibition — human
review recommended»; ни один из них не считается зелёным молча.

| Prohibition | Judge verdict (non-authoritative) | Evidence |
|---|---|---|
| `components/modal.html` не правится планом 09-01 | held | Правка пришла коммитом 09-02 (`e9faee4`), как и объявлено |
| `hx-push-url` не появляется | held | `grep -rn hx-push-url app/templates/` → 0 |
| Плашка `?notice=` не выдаётся на успешный тумблер | held | `respond()` зовётся без `notice` в обоих обработчиках |
| Новых зависимостей, build-шага и вендоренных файлов | held | Диффа по `app/static/js/`, `pyproject.toml`, `uv.lock` за фазу нет |
| Седьмой ключ конфигурации htmx не добавляется | held | `htmx_config.html` за фазу без диффа |
| Правило G-11 не снимается, фильтр `{{` в `_swap_target_ids` не заводится | held | Оба грепа → 0; `test_no_id_is_both_a_swap_target_and_an_oob_target` на месте |
| Третьего встроенного обработчика htmx не появляется | held | `grep -c addEventListener` → 2 |
| `hx-confirm` не вводится | held | `grep -rn hx-confirm app/templates/` → 0 |
| GATE-06 не переименован и не подменён предметом | held | `grep -c "def test_no_client_state_node_is_a_swap_target"` → 1, тест зелен |
| `jinja2` не ввозится в гейт разметки | held | 0 импортов; файл на `re` + `ast` |
| Форма-триггер удаления `hx-post` не получает | held | Разбор с вырезанными комментариями: `hx-post` в `group_row.html` отсутствует |
| Умолчание `modal()` не двигается ни на символ | held | `{%- if hx_post %}` съедает перевод строки; исполнителем сверено с эталоном 1878 байт; 33 утверждения `test_components.py` зелены |
| Playwright и браузерный стенд не заводятся | held | Диффа зависимостей нет; `E2E-01` назван в `09-UAT.md` |

⚠️ **unverified-prohibition — human review recommended** для всех тринадцати:
вердикты выше суждение верификатора, а не сработавшее правило.

### Human Verification Required

#### 1. `09-UAT.md`, пункт 3 — своп, приземление счётчика, фокус

**Test:** Открыть `/accounts/{A}/groups` у аккаунта с 7-8 группами, прокрутить
ниже сгиба, переключить тумблер средней видимой строки; затем повторить
клавиатурой (Tab → Space) и посмотреть `document.activeElement`.
**Expected:** Строка перерисовалась на месте, узел `#group-row-N` один, прокрутка
не потеряна; содержимое `#account-groups-count` заменено, обёртка та же; для 3.3
записан ФАКТИЧЕСКИЙ ответ — ожидание «нет» записано заранее и обязано быть
подтверждено или опровергнуто, а не подставлено.
**Why human:** Критерий 4 роадмапа: «сервером это не доказуемо в принципе». httpx
не свопает и не собирает внеполосные узлы.

#### 2. `09-UAT.md`, пункт 6 — порог индикатора и защита от двойного нажатия

**Test:** Slow 3G → переключить тумблер; No throttling → переключить 5-6 раз;
Slow 3G → нажать дважды быстро; открыть подтверждение удаления и во время
запроса нажать «Отмена».
**Expected:** Точка появляется на медленной и не мигает на быстрой; в Network один
запрос `toggle`; «Отмена» нажимаема.
**Why human:** Порог 300 мс — свойство времени и глаза; допущение A3 замерами не
подтверждено.

#### 3. `09-UAT.md`, пункт 5 (частично) — граница Alpine после подмены

**Test:** Удалить группу; поиск по `group-del-` в Elements; переключить тумблер
соседней строки; открыть подтверждение у подменённой и у соседней строки.
**Expected:** Сироты нет, задвоения нет, Esc закрывает, имя группы в панели верное
(допущение A5).
**Why human:** Требует живого Alpine и живого DOM после свапа.

#### 4. РЕШЕНИЕ ВЛАДЕЛЬЦА: конфликт D-06 ↔ QUAL-06

**Test:** Выбрать один из трёх записанных способов — (а) не блокировать чекбокс
вовсе, (б) `hx-sync="this:drop"` (сначала прочитать формулировку запрета Фазы 8 —
допущение A4), (в) блокировать `<label>` — **вариант помечен негодным**.
**Expected:** Решение записано; QUAL-06 либо становится выполнимым, либо
невыполнение принимается явной `overrides`-записью.
**Why human:** D-06 — локированное решение с пометкой «дорогое»; выбор делает
владелец, а не исполнитель обхода и не верификатор.

#### 5. РЕШЕНИЕ ВЛАДЕЛЬЦА: судьба отметки QUAL-01

**Test:** Решить, откатывается ли QUAL-01 в Pending до закрытия UAT 6.2 и выбора
способа по п.4, либо девиация от «только `find button[type=submit]`» принимается
`overrides`-записью.
**Expected:** REQUIREMENTS.md и этот файл согласованы.
**Why human:** Это вопрос контракта требований, а не факта в коде.

### Gaps Summary

**Три пробела, из них один — настоящий отказ на отгруженной поверхности.**

**Первый и решающий — CR-01, подтверждённый исполнением.** Перевод удаления на
фрагментный ответ убрал полную перестройку страницы, а вместе с ней —
единственное, что чинило курсор бесконечной прокрутки. Сентинел несёт
АБСОЛЮТНОЕ смещение, запечённое в его `hx-get` при отрисовке страницы; ответ
удаления снимает строку, снимает панель и обновляет линейку, но сентинела не
касается. На 35 группах удаление `g5` делает `g31` неотрисовываемой ни одной из
двух порций — молча: статус 200, консоль чистая, линейка честно говорит «34
групп», а на экране их 33. Это не косметика и не пограничный случай: он
достижим ровно на тех аккаунтах, ради которых на экране появился поиск. И это
именно та цена, которую пилот на наименьшей поверхности обязан был обнаружить
ДО того, как форма ответа разойдётся по десяти потребителям в Фазах 10-11 — то
есть дефект попадает в самое сердце формулировки цели фазы.

**Второй — расхождение с буквой критерия 3, которое не является случайностью.**
Единственная переведённая фазой форма блокирует не submit-кнопку, а чекбокс.
Отступление объявлено образцово: запись перечня с обоснованием, число, сверка
вызывающих, названная цена и три способа снятия. Но названная цена — это отмена
QUAL-06 на том же самом пути, и она реализовалась. Пилот честно показал, что
`hx-disabled-elt` и «фокус возвращается на элемент с тем же `id`» на форме без
submit-кнопки несовместимы. Это ценный результат пилота. Он не является
выполнением критерия.

**Третий — бухгалтерский, но не безобидный.** QUAL-01 отмечено Complete, хотя
жирное «только `find button[type=submit]`» на пилотной поверхности не
соблюдено, а поведенческая половина требования — незакрытый backstop. Отметка,
поставленная сейчас, будет унаследована Фазой 15 как факт (GATE-09 — сводное
закрытие), где перепроверять её уже никто не станет.

**Что при этом сделано хорошо и должно пережить закрытие пробелов.** Гейты
действительно получили зубы: 21 контроль, каждое правило фазы названо вместе с
именем своего контроля списком, два правила проверены временной правкой ЖИВОГО
дерева, антивакуумное утверждение внутри правила D-08 не даёт ему позеленеть от
потери чужого перечня, а положительный контроль читает все четыре новых перечня
и все четыре новых сборщика нарушителей. Приём «перечень исключений SP-1 с
обоснованием, назначенной фазой и отдельно утверждаемым числом» применён шесть
раз и ни разу не выродился в список без основания. Долговых маркеров нет ни
одного. Артефакт ручного обхода написан по ОТГРУЖЕННОМУ состоянию, а не по
намерению, и разбивает пункт 3 на четыре независимых исхода вместо одного «ок».
Всё это — рабочая часть цели «гейты Фазы 8 получают зубы прежде, чем начнётся
массовая работа», и она достигнута.

---

## Дополнение плана 09-09: обход исполнен, решение принято (2026-08-30)

Раздел дописан ПОСЛЕ отчёта верификации и его выводов не переписывает: он
называет, что из перечисленного выше закрыто ЖИВЫМ ОБХОДОМ, что решено
владельцем, и что осталось незакрытым — вместе с причиной.

### Обход проведён

`09-UAT.md` — `status: diagnosed` (не `passed`), 2026-08-30, **Chrome / macOS**,
стенд `UAT9-` (account_id=50, 40 групп, 30 активных). Три таблицы отметок
заполнены наблюдённым признаком; допущения **A1**, **A3** и **A5** закрыты
измерением; **A4** закрыто чтением исходника гейта Фазы 8 ещё задачей 1.
Посевной стенд убран, уборка проверена машинно: `UAT9- rows: groups=0
accounts=0`.

### Запись исхода D-06 ↔ QUAL-06 — ПОМЕЧЕННАЯ КОПИЯ

⚠️ **ЭТО КОПИЯ. ОРИГИНАЛ ЛЕЖИТ В `.planning/REQUIREMENTS.md`** — раздел «Фаза 9
— свидетельства к пересмотру состояний», подраздел «ЗАПИСЬ ИСХОДА D-06 ↔
QUAL-06 — ОРИГИНАЛ», непосредственно под строкой свидетельства QUAL-06.
Настоящий файл — артефакт ВЕРИФИКАЦИИ, и следующий прогон проверки перепишет
его целиком; запись, лежащая только здесь, исчезла бы вместе с перегенерацией, и
Фаза 15 унаследовала бы пустоту вместо решения владельца. **Повторная верификация
обязана эту запись сохранить либо перенести из оригинала.** При расхождении копии
с оригиналом правым является `.planning/REQUIREMENTS.md` — тем же правилом,
которым разрешается WARN-8.

Существо записи: измерение **3.3** дало **«нет»** (`document.activeElement`
вернул `<body>`, Chrome / macOS, 2026-08-30) — допущение **A1** истинно,
конфликт настоящий. Владелец выбрал **вариант «б»**: отбрасывающая форма
синхронизации `hx-sync="this:drop"` на форме тумблера ВМЕСТО блокировки;
основание — единственный вариант, закрывающий и QUAL-01, и QUAL-06 разом, при
прочитанном по исходнику запрете Фазы 8 (допущение **A4**: запрещено слово
`queue`, отмена названа незадетой прямым текстом, правило применяется только к
пяти маршрутам периметра, тумблера среди них нет). Применено в вызове
макроса-обёртки, а не в макросе; собственная цель блокировки снята; перечень
исключений приведён к новому положению дел следующей строкой летописи.
**Записи `overrides` этот исход не требует и не заводит** — девиацию принимать
нечего, отступление от буквы критерия 3 на пилотной поверхности снято;
`overrides_applied` остаётся 0. **Цена названа явно:** возврат фокуса на
изменённой разметке в браузере не измерялся ни разу.

### Что осталось незакрытым — с причиной, а не молчанием

| # | Пункт | Почему не закрыт |
|---|---|---|
| 1 | Возврат фокуса после подмены (QUAL-06, проверка **3.3**) | Измерение снято с механизма, снятого этим же планом; на изменённой разметке не наблюдалось |
| 2 | Отбрасывание второго запроса (QUAL-01, проверка **6.2**) | То же: наблюдение описывает блокировку чекбокса, а не пришедшую ей на смену отбрасывающую синхронизацию |
| 3 | Курсор прокрутки (FORM-02, проверка **3.5**) | Отметка не несёт выписанного названия строки, которого правило проверки требует прямо; домысливать за человека запрещено |
| 4 | Кнопка подтверждения после ОТКАЗА запроса (WARN-2) | Шаг обхода не проводился и отметки не получил |
| 5 | **DIV-09-01** — `hx-include` указывает на сентинел, которого на экране может не быть | Правка в объём плана 09-09 не входила и владельцем не выбиралась; находка записана `09-UAT.md` §«Расхождения обхода» и `walkthrough_divergences` выше |
| 6 | **DIV-09-02** — `hx-disabled-elt` формы тумблера целится в кнопку, снятую Alpine при живом htmx | Регрессия, введённая самой веткой `option-b`; найдена ЧТЕНИЕМ исходника при приёмке, глазом ещё не наблюдалась. Починка требует решения о роде записи в `DISABLED_ELT_EXCEPTIONS` — выбор владельца |

**DIV-09-01 коротко.** При удалении группы консоль выдаёт
`The selector "#group-list-sentinel" on hx-include returned no matches!`.
Механизм: `group_row.html:207` передаёт `hx_include='#group-list-sentinel'`
безусловно, а сентинел рисуется только под `{% if has_next %}`
(`list.html:202`, `partial_cards.html:19`) при `PAGE_SIZE = 30`. Следствий два:
у аккаунта с тридцатью группами и меньше сентинела нет никогда, и ошибка валится
при каждом удалении (основной путь в бою); у аккаунта крупнее — после
долистывания до конца сентинел исчезает насовсем, и дальше то же самое. **Вреда
данным нет** (`rendered_rows` приходит `None`, четвёртый внеполосный узел не
отправляется — чинить нечего). **Вред в другом: потерян диагностический
признак** — обход в трёх местах опирается на «200 и чистая консоль», а консоль
на этом экране больше не чистая.

**DIV-09-02 коротко — и с честным провенансом.** Ветка `option-b` сняла с вызова
передачу `disabled_elt='find input[type=checkbox]'`, и форма тумблера перешла на
умолчание макроса `find button[type=submit]`. Но `form_wrapper.html` печатает
`hx-disabled-elt` безусловно, а единственная кнопка отправки этой формы —
«Применить» — снимается Alpine при живом htmx (`x-init`). `ve(t, n)` в
`htmx.min.js` — ОБЩИЙ разрешитель для `hx-include`, `hx-indicator` И
`hx-disabled-elt`: при нуле совпадений пишет `returned no matches!` и возвращает
детачнутый `<output>`. Значит на КАЖДОЕ переключение тумблера — ещё одна строка
в консоль; до правки её не было. **Вреда действию нет**, вред тот же, что у
DIV-09-01, и на том же экране. ⚠️ **Найдено ЧТЕНИЕМ исходника при приёмке правки,
в браузере не наблюдалось** — наблюдение стоит одного переключения тумблера и
внесено отдельным пунктом `human_verification`. **Обе находки одного рода:
объявленный селектор против дерева, которого нет; закрывать их разумно одним
планом.**

---

_Verified: 2026-08-29T23:38:51Z_
_Verifier: Claude (gsd-verifier)_
_Дополнено планом 09-09: 2026-08-30 — обход исполнен, решение владельца по D-06 ↔ QUAL-06 принято и применено, бухгалтерия сведена к `.planning/REQUIREMENTS.md`_
