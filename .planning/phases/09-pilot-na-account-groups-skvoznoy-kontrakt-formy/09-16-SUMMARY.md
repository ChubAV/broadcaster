---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 16
subsystem: testing
tags: [requirements, traceability, verification, gap-closure, checkpoint, overrides]

requires:
  - phase: 09-13
    provides: закрытие пункта gaps про CR-01 сменой формы контракта курсора (keyset) с переходом цвета 5de9c56 → 0326694
  - phase: 09-14
    provides: закрытие пункта gaps про ложный инвариант тишины внеполосного узла и именованный остаток OOB_TARGET_EXCEPTIONS
  - phase: 09-15
    provides: закрытие WR-02, WR-03, WR-04, IN-03 и зелёную суиту 2677 на базе волны
provides:
  - Третий круг раздела свидетельств `.planning/REQUIREMENTS.md` — СПИСКОМ, с прогонами, снятыми ЗАНОВО на HEAD 1fabfef
  - Принятое решение владельца по отступлению QUAL-01 — блоком `overrides` в отчёте верификации И продублированное в REQUIREMENTS.md
  - Переведённые состояния пяти требований Фазы 9, у каждого назван закрытый пункт gaps и зелёная команда
  - Четыре открытых пункта, названных поимённо и НЕ закрытых переводом состояний
affects: ["Фаза 10", "Фаза 11", "Фаза 15", "повторная верификация Фазы 9"]

actuals:
  tokens: 9700
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - "Свидетельство описывает дерево на МОМЕНТ ПЕРЕВОДА, а не на момент своего плана: прогоны снимаются заново, HEAD записывается в сам подраздел"
    - "Позиция «наблюдённый признак» обязана называть, обесценила ли его правка круга: наблюдение, снятое с изменённого механизма, есть ровно то, из-за чего понадобился предыдущий круг"
    - "Решение владельца дублируется в НЕперегенерируемый артефакт: запись, живущая только в отчёте верификации, не переживает следующего прогона"
    - "Провенанс подписи записывается видимой строкой: имя, взятое из настройки репозитория, а не набранное владельцем, обязано быть ИСПРАВИМЫМ, а не унаследованным молча"
    - "Перевод состояния требования сличается ЧИСЛОМ: изменённых строк таблицы ровно столько, сколько вердиктов `заработано` в подразделе свидетельств"

key-files:
  created:
    - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-16-SUMMARY.md
  modified:
    - .planning/REQUIREMENTS.md
    - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md

key-decisions:
  - "Ответ владельца — `accept-override` (задача 2), процитирован, а не выведен; `amend-requirement` и `hold` не выбраны, текст требования QUAL-01 не двигается"
  - "`accepted_by: chubav` снят из `git config user.name`, и провенанс записан ВИДИМОЙ СТРОКОЙ в обоих артефактах: это личность отвечавшего, а не подпись, поставленная владельцем"
  - "`overrides_applied: 0` в отчёте НАМЕРЕННО не тронут: правка допустима только добавляющая, и расхождение названо внутри самой записи вместо молчаливой починки счётчика"
  - "Поля `must_have` и `reason` взяты из образца отчёта ДОСЛОВНО; потребованные уточнения добавлены ОТДЕЛЬНЫМИ ключами именно затем, чтобы образец остался дословным"
  - "Вердикт FORM-02 — `заработано` по машинному признаку, с ценой, названной тремя пунктами; перевод FORM-02 и QUAL-01 РАЗОШЁЛСЯ с рекомендацией отчёта, потому что оба державших их пункта закрыты ПОСЛЕ его прогона"
  - "Четыре открытых пункта записаны отдельным списком, чтобы перевод состояний их не проглотил"

patterns-established:
  - "Останов решения владельца закрывается сводкой `status: halted`, а по получении ответа сводка ПЕРЕПИСЫВАЕТСЯ в `complete` — промежуточное состояние остаётся в истории коммитом, а не подчищается"

requirements-completed: [FORM-02, FORM-09, QUAL-01, QUAL-02, QUAL-06]

coverage:
  - id: D1
    description: "Третий круг раздела свидетельств записан СПИСКОМ, на каждое из пяти требований три позиции, прогоны сняты ЗАНОВО на сегодняшнем дереве с названным HEAD"
    verification:
      - kind: other
        ref: "grep -c 'Третий круг закрытия (планы 09-13 … 09-15)' → 1; в границах подраздела '^| ' → 0, три метки позиций → 5/5/5; HEAD 1fabfef записан и совпадал с git rev-parse --short HEAD на момент задачи 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Задача 1 состояний и флажков НЕ тронула, и это утверждено сличением, способным покраснеть, с антивакуумом"
    verification:
      - kind: other
        ref: "N=0 сличением ([ \"$N\" = \"0\" ]); A=69 > 0 сличением; тронутых флажков 0; git diff --stat app/ tests/ пуст"
        status: pass
    human_judgment: false
  - id: D3
    description: "Суита на дереве задачи 1 зелена целиком"
    verification:
      - kind: integration
        ref: "uv run pytest tests/ -q -p no:randomly → 2677 passed, 0 failed (23:31)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Отступление QUAL-01 принято решением ВЛАДЕЛЬЦА, записано блоком `overrides` дословно по образцу отчёта и продублировано там, где переживёт перегенерацию"
    requirement: QUAL-01
    verification:
      - kind: other
        ref: "YAML frontmatter 09-VERIFICATION.md разбирается: overrides[0].accepted_branch=accept-override, accepted_by=chubav, accepted_at=2026-09-01; grep -c 'accept-override' .planning/REQUIREMENTS.md → 3"
        status: pass
    human_judgment: false
  - id: D5
    description: "Правка отчёта верификации ТОЛЬКО ДОБАВЛЯЮЩАЯ: ни одна существующая строка вердиктов и таблиц не изменена и не удалена"
    verification:
      - kind: other
        ref: "git diff --numstat 1fabfef -- 09-VERIFICATION.md → 65 0 (удалённых строк ноль)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Состояния пяти требований Фазы 9 переведены ПО ЗАКРЫТЫМ РАЗРЫВАМ, и число переводов сличено с числом заработанных вердиктов"
    verification:
      - kind: other
        ref: "изменённых строк таблицы Фазы 9 = 5; вердиктов `заработано` = 5; сличение сошлось. Единственные удалённые строки во всём плане — ровно эти 5 клеток"
        status: pass
    human_judgment: false
  - id: D7
    description: "Рекомендация отчёта не использована как основание перевода ни в одной клетке"
    verification:
      - kind: other
        ref: "git diff 1fabfef HEAD -- .planning/REQUIREMENTS.md | grep -c '^+.*requirement_state_recommendation' → 0"
        status: pass
    human_judgment: false
  - id: D8
    description: "Флажки в теле файла требований не тронуты; код приложения и тесты не тронуты вовсе"
    verification:
      - kind: other
        ref: "тронутых флажков 0 сличением; git diff --stat 1fabfef HEAD -- app/ tests/ пуст; uv run pytest tests/test_planning/ -q → 5 passed"
        status: pass
    human_judgment: false
  - id: D9
    description: "Атрибуция `accepted_by` верна — имя принадлежит тому, кто действительно принял отступление"
    verification: []
    human_judgment: true
    rationale: "ТРЕБУЕТ ГЛАЗА ЧЕЛОВЕКА. Владелец ответил идентификатором ветви и имени рядом не набрал; значение снято из `git config user.name` → `chubav`. Это личность отвечавшего на останов, а НЕ подпись, им поставленная. Провенанс записан видимой строкой в обоих артефактах именно затем, чтобы атрибуцию можно было ИСПРАВИТЬ. Подтвердить её может только человек."
  - id: D10
    description: "Поведенческие половины FORM-02 и QUAL-06 на СЕГОДНЯШНЕМ механизме"
    verification: []
    human_judgment: true
    rationale: "Наблюдений третий круг не заводил вовсе. Отметка 3.5-Б обесценена ЦЕЛИКОМ — снята с механизма абсолютного смещения, удалённого планом 09-13; `keyset` и добавка `scroll-lock` глазами не наблюдались ни разу. Закрывается обходом, а не суитой."
  - id: D11
    description: "Закрытие WR-05 и граница счётчика отставания держатся СТОЯЩИМ ПРАВИЛОМ"
    verification: []
    human_judgment: true
    rationale: "НЕ ДЕРЖАТСЯ, и это записано открытым пунктом. Отсутствия литерала `limit=30` не утверждает ни один тест, а `NOT_YET_CONVERTED_COUNT` слеп к GET-входам в обе стороны. Оба — предмет решения вехи и Фазы 10, а не этого плана."

duration: 45 min активной работы (+ останов решения владельца)
completed: 2026-09-01
status: complete
---

# Phase 09 Plan 16: Перевод состояний требований по закрытым разрывам Summary

**Состояния пяти требований Фазы 9 переведены в `Complete` ПО ЗАКРЫТЫМ ПУНКТАМ `gaps` и зелёным командам сегодняшнего дерева, а не по рекомендации отчёта — с которой перевод по FORM-02 и QUAL-01 сознательно РАЗОШЁЛСЯ; отступление QUAL-01 от буквы «на каждой форме письма» закрыто РЕШЕНИЕМ ВЛАДЕЛЬЦА (`accept-override`), записанным дословным блоком `overrides` и продублированным туда, где оно переживёт перегенерацию отчёта.**

## Performance

- **Duration:** 45 мин активной работы, разорванных остановом решения владельца
- **Started:** 2026-08-31T20:26:15Z
- **Halted (задача 2):** 2026-08-31T21:01:13Z
- **Resumed / Completed:** 2026-09-01T03:40:45Z
- **Tasks:** 3 из 3
- **Files modified:** 2 (`.planning/REQUIREMENTS.md`, `09-VERIFICATION.md`) + сводка

## Таблица переводов — требование → было → стало → закрытый пункт `gaps` → команда признака

| Требование | Было | Стало | Закрытый пункт `gaps` | Команда признака (HEAD `1fabfef`) |
|---|---|---|---|---|
| FORM-02 | `Gaps Found` | `Complete` | Пункт 1 (CR-01), закрыт планом 09-13; плюс WR-03, закрыт планом 09-15 | `-k "scroll_cursor or interleaved_portion_and_delete or control_positive_the_reverse_order or degrades_without_htmx or both_delete_forms_post_the_same_field_names or portion_route_never_answers or trigger_form_body_lands_on_the_filtered_listing"` → `13 passed, 124 deselected` |
| FORM-09 | `Gaps Found` | `Complete` | Ни одним пунктом не держалось | `tests/test_templates/test_htmx_markup_gates.py` целиком → `73 passed` |
| QUAL-01 | `Gaps Found` | `Complete` | Пункт 2 (цена холостого пути), закрыт планом 09-14; **третье условие — решение владельца `accept-override`** | `-k "blocking_target or self_removing_wrapper or cancel_is_never_disabled or disabled_elt"` → `8 passed, 65 deselected` |
| QUAL-02 | `Pending` | `Complete` | Ни одним пунктом не держалось | `-k "indicator"` → `4 passed, 69 deselected` |
| QUAL-06 | `Pending` | `Complete` | Ни одним пунктом не держалось | `-k "toggle_fragment_keeps_the_toggle_id"` → `1 passed, 136 deselected` |

**Сличение, а не заявление:** изменённых строк таблицы Фазы 9 — `5`; вердиктов
`**вердикт:** заработано` в подразделе свидетельств — `5`; числа сошлись.
**Единственные удалённые строки во всём плане — ровно эти пять клеток**
(`git diff 1fabfef HEAD -- .planning/REQUIREMENTS.md | grep '^-'`), то есть всё
остальное во всех трёх артефактах — чистое добавление.

## Accomplishments

- **Тупик фазы разорван, и разорван в правильном месте.** Отчёт верификации вынес
  рекомендацию и прямо отказался её исполнять («отмечать требования Complete
  внутри незакрытой фазы значило бы то же самое, что фаза себе запретила»).
  Перевод сделан ПОСЛЕ закрытия обоих пунктов `gaps` и ПО НИМ.
- **Перевод РАЗОШЁЛСЯ с рекомендацией там, где расходятся факты.** Отчёт
  рекомендовал держать `Gaps Found` по FORM-02 и QUAL-01. Обе клетки переведены —
  потому что оба державших их пункта закрыты ПОСЛЕ прогона отчёта (планами 09-13,
  09-14, 09-15), а по QUAL-01 получено ещё и решение владельца. Совпадение по
  остальным трём — следствие того, что машинный признак один и тот же, а не того,
  что рекомендация исполнена. Имя ключа рекомендации в файл не записано вовсе:
  собственный гейт плана сличает добавленные строки на его отсутствие (→ `0`).
- **Решение владельца записано ДВАЖДЫ, и это не избыточность.** Блок `overrides`
  живёт во frontmatter отчёта, который ПЕРЕГЕНЕРИРУЕТСЯ следующим прогоном;
  дубликат — в `.planning/REQUIREMENTS.md`, который не перегенерируется. Запись,
  положенная только в отчёт, исчезла бы вместе с ним.
- **Провенанс подписи назван, а не подделан.** Владелец ответил идентификатором
  ветви и имени рядом не набрал. `accepted_by: chubav` снят исполнением
  `git config user.name`; в обоих артефактах стоит видимая строка о том, что это
  личность ОТВЕЧАВШЕГО, а не подпись, им поставленная, — чтобы следующий читатель
  мог ИСПРАВИТЬ атрибуцию, а не унаследовать её молча.
- **Образец `overrides` остался дословным.** Поля `must_have` и `reason` взяты из
  §«Образец записи `overrides`» отчёта побайтно (скопированы `sed`, а не
  перенабраны). Уточнения, потребованные при принятии решения, — граница
  принимаемого, непокрытость записью эпохи 09-09, провенанс имени — добавлены
  ОТДЕЛЬНЫМИ КЛЮЧАМИ именно затем, чтобы не переформулировать образец.
- **Правка отчёта строго добавляющая, и счётчик не подкручен.**
  `git diff --numstat` → `65	0`. `overrides_applied: 0` намеренно НЕ тронут:
  его исправление было бы изменением существующей строки (угроза T-09-16-02).
  Расхождение названо внутри самой записи; пересчитает его следующий прогон.
- **Третий круг свидетельств называет обесцененное.** Отметка **3.5-Б** снята с
  механизма абсолютного смещения, удалённого планом 09-13 целиком, — обесценена
  ЦЕЛИКОМ. Отметка **7.3** обесценена частично: её ноль снят до прочтения
  вендоренного рантайма планом 09-14. Уцелевшие названы уцелевшими ПО ДЕРЕВУ
  (`group_row.html:178` по-прежнему `sync='this:drop'`; возврат `sending` в
  `modal.html` не изменён ни на символ; `.form-busy` не тронут).
- **Четыре открытых пункта не проглочены переводом**, а записаны отдельным
  списком с адресатом у каждого.

## Task Commits

1. **Задача 1: третий круг свидетельств, прогоны заново, состояния не тронуты** — `df5f876` (docs)
2. **Сводка промежуточного состояния `status: halted`** — `f82a565` (docs)
3. **Задача 2: отступление QUAL-01 принято владельцем, ветвь `accept-override`** — `ac0cdb3` (docs)
4. **Задача 3: состояния переведены по закрытым разрывам** — `4b9c4d9` (docs)

**Сводка плана:** коммитится этим же шагом (docs).

_Коммит 2 — промежуточный: план останавливался на решении владельца и был закрыт
сводкой `status: halted`. Она НЕ подчищена задним числом, а переписана здесь; след
останова остаётся в истории._

## Что не закрыто переводом — четыре открытых пункта

Записаны и в `.planning/REQUIREMENTS.md`, чтобы повторная верификация нашла их
открытыми, а не выводила заново:

| # | Пункт | Чего не хватает | Адресовано |
|---|---|---|---|
| а | Закрытие **WR-05** не держит ни одно правило | Отсутствия литерала `limit=30` не утверждает ни один тест; `test_page_shows_thirty_rows_and_a_sentinel` сверяет ОТРЕНДЕРЕННЫЙ адрес и зелен и при параметре, и при литерале. Регресс НЕВИДИМ. Шесть литералов живы на соседних экранах | Повторной верификации и Фазе 10 |
| б | Добавка **`scroll-lock`** | Не покрыта ни тестом, ни глазом (D10 сводки 09-13) | Обходу |
| в | **`NOT_YET_CONVERTED_COUNT`** 34 → 34 | Гейт G-2 слеп к GET-входам в ОБЕ стороны; граница названа, но не расширена — это решение вехи | Решению вехи |
| г | Отметка **3.5-Б** обесценена целиком | Снята с механизма, удалённого планом 09-13; `keyset` глазами не наблюдался ни разу | Обходу (повторный проход 3.5) |

**Почему это не помешало переводу.** Ни один из четырёх не является пунктом
`gaps` и ни один не опровергает буквы переведённых требований: (а) и (в) — это
отсутствие ПРАВИЛА там, где свойство сегодня верно, (б) и (г) — отсутствие
НАБЛЮДЕНИЯ. Но требование в `Complete` читается следующими фазами как ФАКТ, и
молчание о том, что часть признаков держится разовым измерением, было бы ровно
тем дрейфом, против которого заведены гейты.

## Decisions Made

**Ответ владельца (задача 2), процитированный, а не выведенный:**

> `accept-override`

Ветви `amend-requirement` и `hold` НЕ выбраны. Следствие названо прямо: текст
требования QUAL-01 не двигается ни на символ, и Фаза 15 продолжает сверять 47
форм по ПРЕЖНЕЙ букве.

**Что именно принято, с границей.** Исход QUAL-01 достигнут и наблюдён (6.2-Б —
один запрос на два быстрых нажатия; 6.3 — «Отмена» нажимаема и нажалась в
полёте). Цель блокировки «только `find button[type=submit]`» исполнена БУКВАЛЬНО:
печатаемых вхождений в дереве два, оба этой цели. Расхождение сведено к одному:
оговорка «`hx-disabled-elt` **на каждой форме письма**» на пилотной форме
тумблера не выполнена — атрибута там нет вовсе, защиту несёт
`hx-sync="this:drop"`.

**Чем это НЕ было покрыто раньше — вся причина, по которой решение
понадобилось.** Отступление объявлено записью
`DISABLED_ELT_EXCEPTIONS['components/form_wrapper.html']` с летописью из трёх
строк и ПРОВЕРЯЕМЫМ полем `callers`; забытой работой оно не является. Но строка
«записи `overrides` этот исход не требует и не заводит» писалась планом **09-09**,
когда форма ещё жила по ОБЩЕМУ УМОЛЧАНИЮ цели блокировки. Снятие атрибута
произошло планом **09-10**, то есть ПОСЛЕ неё, и той записью не покрыто.
Принятое решение покрывает ИМЕННО ПОЛОЖЕНИЕ ПОСЛЕ 09-10.

**Вердикт FORM-02 — `заработано` по правилу плана, а не по ощущению.** Правило
требует (а) закрытый пункт `gaps` либо прямое указание, что требование им не
держалось, (б) зелёную команду сегодняшнего дерева, (в) при наличии отступления —
решение владельца. Для FORM-02 выполнены все три (отступления нет). Цена названа
тремя пунктами, а не сглажена.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Собственный гейт плана покраснел на моей же формулировке**

- **Found during:** Задача 3
- **Issue:** в строку «чем основание НЕ является» я набрал имя ключа
  рекомендации отчёта ЛИТЕРАЛЬНО, поясняя, что основанием он не служил. Критерий
  приёмки сличает добавленные строки на отсутствие этой подстроки
  (`grep -c '^+.*requirement_state_recommendation'` → `0`), и гейт дал `1`.
  Смысл строки был противоположен нарушению, но проверка литеральная, а критерий
  плана однозначен.
- **Fix:** строка переформулирована без литерала — «блок рекомендации состояний
  из frontmatter отчёта» — с прямым указанием, что имя намеренно не набирается,
  потому что собственный гейт плана сличает его отсутствие. Содержание не
  потеряно ни на одно утверждение.
- **Verification:** гейт перепрогнан → `0`, все восемь гейтов задачи 3 зелены.
- **Committed in:** `4b9c4d9`

### Отступления, названные прямо

**2. Задача 2 произвела КОММИТ,** хотя по букве плана она — чистый останов
решения, а запись `overrides` отнесена к задаче 3. Разделение сделано по
указанию координатора при возобновлении и по существу лучше: принятие решения
владельца и перевод состояний — разные утверждения, и разнести их по коммитам
значит сделать каждое отдельно откатываемым. Ни одно правило плана этим не
ослаблено.

**3. К дословному образцу `overrides` добавлены СЕМЬ ключей сверх образца**
(`accepted_by_provenance`, `accepted_branch`, `accepted_branches_rejected`,
`covers`, `not_covered_by`, `duplicated_in`, `note_on_overrides_applied`,
`note_on_verbatim`). План требует брать YAML дословно; потребованная при принятии
точность в образец не помещалась. Разрешено добавлением ОТДЕЛЬНЫХ ключей:
`must_have` и `reason` остались побайтно теми же (скопированы `sed -n '398,408p'`,
а не перенабраны), и ни одна формулировка образца не переписана.

**4. Сводка `status: halted` (`f82a565`) переписана в `complete`,** а не оставлена
второй файлом. Промежуточное состояние сохранено историей коммита, а не
дублирующим артефактом.

---

**Total deviations:** 1 auto-fixed (1 bug) + 3 названных отступления от буквы плана.
**Impact on plan:** ни одно отступление не ослабляет ни одного правила. Автоправка — починка собственной формулировки, покрасившей собственный гейт; три остальных касаются формы записи, а не её содержания.

## Issues Encountered

**1. Главная трудность круга — что делать с наблюдениями, которые правки волны
обесценили.** Соблазн был записать 3.5-Б действующим свидетельством: отметка
честная, снята человеком, вердикт её верен. Но снята она с механизма, которого в
дереве больше нет, — а это ровно та причина, по которой понадобился второй круг.
Записана обесцененной, с названием того, что пришло взамен: регрессионный тест
чередования с переходом цвета `5de9c56` → `0326694`, то есть именно то, чего
требовал `missing` пункта `gaps` и чего у отметки не было вовсе.

**2. Счётчик `overrides_applied` пришлось оставить неверным.** Он говорит `0`
при одной применённой записи. Починить его значило бы изменить существующую
строку отчёта, что прохибиция плана запрещает прямо. Выбрано назвать расхождение
внутри записи, а не подкрутить число молча.

**3. Прочих проблем нет.** Суита на дереве задачи 1 зелена целиком с первого
прогона (2677 passed, 0 failed); чужой красноты нет ни одной.

## Deferred Issues

Сверх четырёх открытых пунктов выше — ничего. Пять `unclassified`-рёбер обхода
остаются предметом повторной верификации, как план и объявил. Утверждение
«повторная верификация согласится с переведёнными состояниями» остаётся
`backstop`: подтвердить его может только она сама, и исполнитель за неё не
расписывается.

## Known Stubs

Отсутствуют. Заглушек, пустых значений и долговых маркеров (`TODO`, `FIXME`,
`XXX`, `HACK`, `PLACEHOLDER`) план не вносил: он не трогает ни одного файла
приложения и ни одного теста.

## Threat Flags

Новой поверхности сверх объявленной в `<threat_model>` плана не заведено. Все три
угрозы уровня `high` снабжены машинными проверками и закрыты:

| Угроза | Проверка | Итог |
|---|---|---|
| T-09-16-01 (перевод состояния без воспроизводимого основания) | Сличение: изменённых строк таблицы `5` = вердиктов `заработано` `5`; у каждой клетки назван пункт `gaps` и команда | Закрыта |
| T-09-16-02 (переписывание вердиктов и таблиц отчёта) | `git diff --numstat` по `09-VERIFICATION.md` → `65	0` | Закрыта: удалённых строк ноль; счётчик `overrides_applied` намеренно не тронут |
| T-09-16-03 (решение владельца только в перегенерируемом артефакте) | `grep -c "accept-override" .planning/REQUIREMENTS.md` → `3` | Закрыта дублированием |
| T-09-16-04 (прогоны, переписанные из сводок) | HEAD `1fabfef` записан в подраздел и сверен на момент задачи 1 | Закрыта |
| T-09-16-05 (закрытие расхождения суждением исполнителя) | Останов `blocking-human` исполнен: план ОСТАНОВЛЕН, ответ получен от владельца и процитирован | Закрыта — ветвь исполнителем не выбиралась |

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Фаза 9 закрыта по состояниям требований:** все пять — `Complete`, у каждого
  назван закрытый пункт `gaps` и воспроизводимая зелёная команда.
- **Повторной верификации — четыре открытых пункта**, записанных в
  `REQUIREMENTS.md` списком с адресатами, и `overrides` с провенансом подписи,
  требующим подтверждения человеком (D9).
- **Фазе 10 и обходу — три наблюдательных долга:** повторный проход 3.5 на
  механизме `keyset`, проверка `scroll-lock` на списке длиннее экрана,
  поведенческая половина QUAL-06.
- **Вехе — одно решение:** расширять ли область гейта G-2 с POST-обработчиков на
  все входы. Граница названа, но не расширена; это решение вехи, а не плана.
- **Оркестратору:** `.planning/STATE.md` и `.planning/ROADMAP.md` этим планом не
  тронуты (режим worktree). `requirements mark-complete` по этому плану ТЕПЕРЬ
  корректен: `requirements-completed` заполнен пятью идентификаторами, и клетки
  `Status` уже переведены самим планом.
- ⚠️ **Счётчик `overrides_applied: 0`** в `09-VERIFICATION.md` остаётся неверным
  намеренно — пересчитать его обязан следующий прогон верификации, который файл
  и перегенерирует.

## Self-Check: PASSED

- `09-16-SUMMARY.md` записана на диск и коммитится этим же шагом.
- Все четыре коммита задач найдены в `git log 1fabfef..HEAD`: `df5f876`, `f82a565`, `ac0cdb3`, `4b9c4d9`.
- Восемь гейтов задачи 3 зелены: сличение `5 = 5`; имени ключа рекомендации в добавленных строках `0`; тронутых флажков `0`; удалённых строк отчёта `0`; `accept-override` в `REQUIREMENTS.md` — `3`; `accepted_by`/`accepted_at` непусты; `git diff --stat app/ tests/` пуст; табличных строк в подразделе `0`.
- Frontmatter `09-VERIFICATION.md` РАЗБИРАЕТСЯ как YAML (`yaml.safe_load`): `overrides` — одна запись, `accepted_branch=accept-override`, `accepted_by=chubav`, `accepted_at=2026-09-01`.
- Единственные удалённые строки во всём плане — ровно пять переведённых клеток `Status`; всё остальное чистое добавление.
- `git diff --stat 1fabfef HEAD -- .planning/` показывает ровно три файла: `REQUIREMENTS.md`, `09-16-SUMMARY.md`, `09-VERIFICATION.md`. Планы 09-01 … 09-15 и их сводки не тронуты; `STATE.md` и `ROADMAP.md` не тронуты.
- `uv run pytest tests/test_planning/ -q` → `5 passed`. Полная суита на дереве задачи 1 → `2677 passed, 0 failed`.
- Ветвь выбрана ВЛАДЕЛЬЦЕМ и процитирована дословно; исполнитель её не выводил.

---
*Phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy*
*Completed: 2026-09-01*
