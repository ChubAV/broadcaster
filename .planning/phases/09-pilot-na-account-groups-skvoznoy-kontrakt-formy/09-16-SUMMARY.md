---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 16
subsystem: testing
tags: [requirements, traceability, verification, gap-closure, checkpoint]

requires:
  - phase: 09-13
    provides: закрытие пункта gaps про CR-01 сменой формы контракта курсора (keyset) с переходом цвета 5de9c56 → 0326694
  - phase: 09-14
    provides: закрытие пункта gaps про ложный инвариант тишины внеполосного узла и именованный остаток OOB_TARGET_EXCEPTIONS
  - phase: 09-15
    provides: закрытие WR-02, WR-03, WR-04, IN-03 и зелёную суиту 2677 на базе волны
provides:
  - Третий круг раздела свидетельств `.planning/REQUIREMENTS.md` — СПИСКОМ, с прогонами, снятыми ЗАНОВО на HEAD 1fabfef
  - Названное обесценивание наблюдений второго круга правками третьего (3.5-Б — целиком, 7.3 — частично)
  - Поимённо названный остаток опорного признака «200 и чистая консоль» и оба пункта gaps с воспроизводимыми командами
  - Названные непокрытые места, унаследованные волной: WR-05 без стоящего правила, scroll-lock без проверки, слепота счётчика отставания к GET-входам
affects: ["Фаза 10", "Фаза 11", "Фаза 15", "повторная верификация Фазы 9"]

actuals:
  tokens: 6300
  tasks: 1
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Свидетельство описывает дерево на МОМЕНТ ПЕРЕВОДА, а не на момент своего плана: прогоны снимаются заново, HEAD записывается в сам подраздел"
    - "Позиция «наблюдённый признак» обязана называть, обесценила ли его правка круга: наблюдение, снятое с изменённого механизма, есть ровно то, из-за чего понадобился предыдущий круг"
    - "Строки подраздела, не принадлежащие одному требованию, набираются ДРУГИМИ метками — иначе счёт позиций собственного гейта расходится"

key-files:
  created:
    - .planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-16-SUMMARY.md
  modified:
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Вердикт FORM-02 — `заработано` ПО МАШИННОМУ ПРИЗНАКУ, с ценой, названной тремя пунктами: 3.5-Б описывает снятый механизм, scroll-lock не проверен ничем, закрытие WR-05 не держит ни одно правило"
  - "Вердикт QUAL-01 — `не заработано`, и причина НЕ в машинном признаке (он зелен), а в неполученном ответе владельца: третье условие перевода открыто"
  - "`requirements-completed` оставлен ПУСТЫМ намеренно: ни одна клетка `Status` этим планом не переведена, и заполнить поле значило бы отметить требования выполненными молчанием — ровно тем способом, который план себе запретил"
  - "Останов задачи 2 (`gate=\"blocking-human\"`) НЕ обойдён и НЕ решён исполнителем: ветвь QUAL-01 принадлежит владельцу"

patterns-established:
  - "План, остановленный проектным чекпойнтом, закрывается сводкой `status: halted`, а не сводкой `complete` с недоделанными задачами"

requirements-completed: []

coverage:
  - id: D1
    description: "Третий круг раздела свидетельств записан СПИСКОМ, на каждое из пяти требований три позиции, прогоны сняты ЗАНОВО на сегодняшнем дереве с названным HEAD"
    verification:
      - kind: other
        ref: "grep -c 'Третий круг закрытия (планы 09-13 … 09-15)' .planning/REQUIREMENTS.md → 1; в границах подраздела '^| ' → 0, три метки позиций → 5/5/5; HEAD 1fabfef записан и совпадает с git rev-parse --short HEAD"
        status: pass
    human_judgment: false
  - id: D2
    description: "Состояния требований и флажки задачей 1 НЕ тронуты, и это утверждено сличением, способным покраснеть, с антивакуумом"
    verification:
      - kind: other
        ref: "N=$(git diff -U0 .planning/REQUIREMENTS.md | grep -c '^[+-]|.*| Phase 9 |'); [ \"$N\" = \"0\" ] → 0; A=69 > 0; тронутых флажков 0; git diff --stat app/ tests/ пуст"
        status: pass
    human_judgment: false
  - id: D3
    description: "Суита на дереве задачи 1 зелена целиком"
    verification:
      - kind: integration
        ref: "uv run pytest tests/ -q -p no:randomly → 2677 passed, 0 failed (23:31)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Состояния требований Фазы 9 переведены по закрытым разрывам (задача 3 плана)"
    verification: []
    human_judgment: true
    rationale: "НЕ ИСПОЛНЕНО. Задача 3 неисполнима до ответа владельца на останов задачи 2: правило перевода требует третьим условием решение владельца по отступлению QUAL-01, а исполнитель ветвь выбирать не вправе. Ни одна клетка `Status` не изменена."
  - id: D5
    description: "Расхождение QUAL-01 с буквой «на каждой форме письма» закрыто решением владельца"
    verification: []
    human_judgment: true
    rationale: "ОТКРЫТО. Останов `checkpoint:decision` с `gate=\"blocking-human\"` возвращён оркестратору неотвеченным. Гейт никогда не автоодобряется — ни в auto-режиме, ни в yolo; в конфигурации проекта auto-режим к тому же выключен (`auto_advance: false`, `_auto_chain_active: false`)."
  - id: D6
    description: "Поведенческие половины FORM-02 и QUAL-06 на СЕГОДНЯШНЕМ механизме"
    verification: []
    human_judgment: true
    rationale: "Наблюдений третий круг не заводил вовсе. 3.5-Б снята с механизма абсолютного смещения, снятого планом 09-13 целиком; `keyset` и добавка `scroll-lock` глазами не наблюдались ни разу. Закрывается обходом, а не суитой."

duration: 35 min
completed: 2026-08-31
status: halted
---

# Phase 09 Plan 16: Перевод состояний требований по закрытым разрывам Summary

**Третий круг раздела свидетельств записан СПИСКОМ с прогонами, снятыми ЗАНОВО на HEAD `1fabfef` (суита 2677 passed, 0 failed), и с прямо названным обесцениванием наблюдений второго круга; ПЕРЕВОД СОСТОЯНИЙ НЕ СДЕЛАН — план остановлен на проектном останове решения владельца по отступлению QUAL-01, и ни одна клетка `Status` не изменена.**

## ⚠️ ПЛАН ОСТАНОВЛЕН, А НЕ ЗАВЕРШЁН — И ЭТО ЕГО ЗАМЫСЕЛ, А НЕ СБОЙ

`status: halted`. План объявлен `autonomous: false` и несёт ОДИН останов —
задачу 2, `checkpoint:decision` с `gate="blocking-human"`. Такой гейт не
автоодобряется ни в одном режиме; в конфигурации проекта auto-режим к тому же
выключен (`workflow.auto_advance: false`, `workflow._auto_chain_active: false`).

**Что осталось неисполненным:** задача 2 (ответ владельца) и задача 3 (перевод
состояний, применение ветви, возможная запись `overrides` в `09-VERIFICATION.md`).

**Почему исполнитель не решил вопрос сам.** Предмет останова — расхождение с
БУКВОЙ требования QUAL-01 («`hx-disabled-elt` **на каждой форме письма**»), а
фаза себе прямо предписала: закрыть такое расхождение суждением исполнителя —
то же самое, что закрыть дефект удалением записи о нём. Собственная прохибиция
плана говорит это же: «исполнитель не выбирает ветвь QUAL-01 сам».

## Performance

- **Duration:** 35 min
- **Started:** 2026-08-31T20:26:15Z
- **Completed (halt):** 2026-08-31T21:01:13Z
- **Tasks:** 1 из 3 (задача 2 — останов, задача 3 — не начата)
- **Files modified:** 1 (`.planning/REQUIREMENTS.md`)

## Accomplishments

- **Третий круг свидетельств записан, и прогоны в нём — СЕГОДНЯШНИЕ.** Шесть
  выборок исполнены заново на HEAD `1fabfef`; ни одно число не переписано из
  сводок 09-13 … 09-15. HEAD записан в самом подразделе и сверен с
  `git rev-parse --short HEAD`.
- **Обесценивание наблюдений названо, а не умолчано — и это главное содержание
  круга.** Отметка **3.5-Б** (FORM-02) снималась на механизме АБСОЛЮТНОГО
  смещения с четвёртым внеполосным узлом; план 09-13 снял из дерева и смещение,
  и узел, и скрытое поле `rendered_rows` целиком. Сегодняшний механизм
  (`after_id`) глазами не наблюдался ни разу. Отметка **7.3** обесценена
  частично: её ноль снят ДО прочтения вендоренного рантайма планом 09-14 и верен
  для боевого пути, но не для холостого.
- **Уцелевшие наблюдения названы уцелевшими ПО ДЕРЕВУ, а не по памяти.** 6.2-Б:
  `group_row.html:178` по-прежнему передаёт `sync='this:drop'` и пустую цель
  блокировки. 6.3: возврат признака занятости `x-on:htmx:after-request` в
  `components/modal.html` не изменён ни на символ (изменились только `show()` и
  `hide()` — по одному вызову `classList`). 3.3-Б: `group_row.html:179` печатает
  тот же `id='group-toggle-' ~ group.id`. 6.1: `.form-busy` и его порог `.3s` не
  тронуты, единственная правка `app.css` третьего круга — постороннее предмету
  правило `.is-modal-open`.
- **Оба пункта `gaps` расписаны с воспроизводимыми командами и переходами
  цвета:** `5de9c56` → `0326694` (CR-01) и `0134ef7` → `2898095`, `b5e3af3` →
  `571e45f` (ложный инвариант).
- **Непокрытое унаследованное названо тремя пунктами, а не переведено в
  «выполнено» молчанием:** закрытие **WR-05** не держит ни одно правило (находка
  плана 09-15: `test_page_shows_thirty_rows_and_a_sentinel` сверяет
  ОТРЕНДЕРЕННЫЙ адрес и зелен и при параметре, и при вернувшемся литерале; на
  шести местах соседних экранов литерал живёт до сих пор); добавка
  **`scroll-lock`** (D10) не покрыта ни тестом, ни глазом; **счётчик отставания**
  `NOT_YET_CONVERTED_COUNT` слеп к GET-входам в обе стороны и потому не двинулся
  (34 → 34).
- **Состояния и флажки не тронуты, и это ГЕЙТ, а не печать.** `N = 0` сличением,
  антивакуум `A = 69 > 0` сличением, тронутых флажков `0`,
  `git diff --stat app/ tests/` пуст.
- **Суита зелена целиком:** `2677 passed, 0 failed` за 23:31.

## Task Commits

1. **Задача 1: третий круг свидетельств, прогоны заново, состояния не тронуты** — `df5f876` (docs)

**Сводка плана:** коммитится этим же шагом (docs).

_Задача 2 — останов решения, коммита не производит. Задача 3 не начата._

## Files Created/Modified

- `.planning/REQUIREMENTS.md` — подраздел «Третий круг закрытия (планы 09-13 …
  09-15)», 69 добавленных строк, поставлен ПЕРЕД записью исхода D-06: граница
  машинной вырезки точная, и антивакуумное утверждение нельзя удовлетворить
  посторонней правкой ниже по тексту (образец — план 09-12).
- `.planning/phases/…/09-16-SUMMARY.md` — настоящая сводка, `status: halted`.
- `.planning/phases/…/09-VERIFICATION.md` — **НЕ ТРОНУТ.** Запись `overrides`
  заводится только по ветви `accept-override` и только по ответу владельца.

## Что показал третий круг — по требованиям

| Требование | Машинный признак | Вердикт круга | Состояние `Status` сейчас |
|---|---|---|---|
| FORM-02 | 13 passed, 124 deselected | `заработано` (цена названа тремя пунктами) | `Gaps Found` — НЕ ПЕРЕВЕДЕНО |
| FORM-09 | 73 passed; 9 passed, 64 deselected | `заработано` | `Gaps Found` — НЕ ПЕРЕВЕДЕНО |
| QUAL-01 | 10 passed, 127 deselected; 8 passed, 65 deselected | `не заработано` — открыт ответ владельца | `Gaps Found` — НЕ ПЕРЕВЕДЕНО |
| QUAL-02 | 4 passed, 69 deselected | `заработано` | `Pending` — НЕ ПЕРЕВЕДЕНО |
| QUAL-06 | 1 passed, 136 deselected | `заработано` | `Pending` — НЕ ПЕРЕВЕДЕНО |

⚠️ **Столбец «Состояние сейчас» и есть предмет неисполненной задачи 3.** Вердикт
круга — это ОСНОВАНИЕ для перевода, а не перевод. Таблицу перевода «требование →
было → стало → закрытый пункт gaps → команда признака», которую требует задача 3,
эта сводка привести не может: перевода не было.

## Decisions Made

**Вердикт FORM-02 — `заработано`, и решение это принято ПО ПРАВИЛУ ПЛАНА, а не
по ощущению.** Правило перевода требует (а) закрытый пункт `gaps` либо прямое
указание, что требование им не держалось, (б) зелёную команду сегодняшнего
дерева, (в) при наличии отступления — решение владельца. Для FORM-02: пункт
`gaps` про CR-01 закрыт планом 09-13, WR-03 закрыт планом 09-15, команда зелена,
отступления от буквы требования нет. Буква FORM-02 (`method="post"`, непустой
`action`, посимвольное равенство `hx-post` и `action`, `action` на маршрут
полного документа) машинная по существу. **Но цена названа, а не сглажена:**
поведенческую половину, привязанную к FORM-02 самой фазой, сегодня держит
правило, а не глаз.

**Вердикт QUAL-01 — `не заработано`, и причина названа поимённо.** Машинный
признак зелен, пункт `gaps` про цену холостого пути закрыт планом 09-14,
серверная идемпотентность планом 09-15 усилена. Открыто ровно третье условие:
оговорка «на каждой форме письма» на пилотной форме не выполнена, отступление
осознанное (решение владельца `record-kind`, план 09-10), но запись владельца
«`overrides` этот исход не требует» писалась планом 09-09, когда форма ещё жила
по общему умолчанию цели блокировки, — снятие атрибута произошло ПОСЛЕ неё и той
записью не покрыто.

**`requirements-completed` оставлен ПУСТЫМ.** Шаблон сводки требует скопировать
массив `requirements` плана дословно, и это отступление названо здесь прямо:
скопировать его значило бы отметить пять требований выполненными молчанием,
тогда как ни одна клетка `Status` не переведена. Именно эта ошибка — отметка
«выполнено» внутри незакрытой фазы — породила третий пробел Фазы 9 и потребовала
отката коммитом `183511c`. Поле заполняет задача 3 ПОСЛЕ ответа владельца.

## Deviations from Plan

### Отступления, названные прямо

**1. `requirements-completed` пуст вместо копии массива `requirements` плана.**
Причина выше. Отступление от шаблона сводки, а не от плана: сам план
прохибицией запрещает перевод без названного признака и решения владельца.

**2. Сводка набрана при ОДНОЙ исполненной задаче из трёх, и потому `status:
halted`, а не `complete`.** Шаблон прямо предусматривает это значение для
«designed stop… intentionally left tasks unfinished». Требование режима worktree
(«SUMMARY.md обязана быть закоммичена до возврата, иначе она умрёт вместе с
worktree») исполнено без ложного заявления о завершении.

**3. Формат позиций третьего круга: две строки, не принадлежащие одному
требованию, набраны ДРУГИМИ метками** (`**правило:** / **наблюдение:** /
**исход:**` и `**пункт 1 …** / **пункт 2 …**`), а не тремя метками требований.
Причина машинная: критерий приёмки требует РОВНО пяти строк каждой из трёх
меток в границах подраздела, и второй круг, набравший опорный признак теми же
метками, дал бы здесь шесть. Существо строк не изменилось.

### Auto-fixed Issues

Ничего не автоправилось: план кода и тестов не трогает вовсе, и правки
потребовал ровно один артефакт планирования.

---

**Total deviations:** 0 auto-fixed + 3 названных отступления от буквы шаблона и плана.
**Impact on plan:** ни одно отступление не ослабляет ни одного правила плана. Первое и второе — прямое следствие останова: заполнить их «как обычно» значило бы заявить работу, которой не было.

## Issues Encountered

**1. Единственная содержательная трудность круга — что делать с наблюдениями,
которые правки волны обесценили.** Соблазн был записать 3.5-Б как действующее
свидетельство: отметка честная, снята человеком, вердикт её верен. Но снята она
с механизма, которого в дереве больше нет, — а это ровно та причина, по которой
понадобился второй круг. Записано как обесцененное, с названием того, что пришло
взамен (регрессионный тест чередования с переходом цвета — то, чего у отметки не
было вовсе).

**2. Прочих проблем нет.** Суита зелена целиком с первого прогона; чужой красноты
нет ни одной. Предсуществующий красный тест, о котором писал план 09-14
(`test_the_machine_readable_progress_is_derived_from_the_roadmap`), на базе этой
волны зелен — как и сообщала сводка 09-15.

## Deferred Issues

Ничего не отложено СВЕРХ названного. Открытым остаётся то, что план объявил
открытым: пять `unclassified`-рёбер обхода (FORM-02, FORM-09, QUAL-01, QUAL-02 и
неклассифицированная половина QUAL-01) и утверждение-`backstop` «повторная
верификация согласится с переведёнными состояниями» — подтвердить его может
только сама верификация.

## Known Stubs

Отсутствуют. Заглушек, пустых значений и долговых маркеров (`TODO`, `FIXME`,
`XXX`, `HACK`, `PLACEHOLDER`) план не вносил: он не трогает ни одного файла
приложения и ни одного теста.

## Threat Flags

Новой поверхности сверх объявленной в `<threat_model>` плана не заведено. Все
три угрозы уровня `high` относятся к неисполненной задаче 3 либо к останову:

| Угроза | Состояние |
|---|---|
| T-09-16-01 (перевод состояния без воспроизводимого основания) | Не могла сработать: переводов не было. Основания для будущего перевода записаны и воспроизводимы командами |
| T-09-16-02 (переписывание вердиктов и таблиц отчёта верификации) | Не сработала: `09-VERIFICATION.md` не тронут ни на байт |
| T-09-16-05 (закрытие расхождения суждением исполнителя) | **Не сработала именно потому, что план остановлен.** Ветвь QUAL-01 не выбрана исполнителем и возвращена владельцу неотвеченной |

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Что нужно, чтобы план дошёл до конца:** один ответ владельца — ровно один из
  `accept-override`, `amend-requirement`, `hold`. При `accept-override` — имя
  принявшего (`accepted_by`) и подтверждение принятой цены. При
  `amend-requirement` — новая формулировка требования дословно.
- **Основания для перевода уже лежат на диске** и переживают перезапуск:
  подраздел «Третий круг закрытия» коммитом `df5f876`. Задаче 3 не придётся
  переснимать прогоны — они сняты на том же дереве.
- **Фазам 10-15 передано три непокрытых места, названных поимённо:** WR-05 без
  стоящего правила, `scroll-lock` без проверки, слепота гейта G-2 и счётчика
  отставания к GET-входам.
- **Оркестратору:** `.planning/STATE.md` и `.planning/ROADMAP.md` этим планом не
  тронуты (режим worktree — их ведёт оркестратор). `requirements mark-complete`
  по этому плану выполнять НЕЛЬЗЯ: `requirements-completed` пуст намеренно.

## Self-Check: PASSED

- `09-16-SUMMARY.md` записана на диск и коммитится этим же шагом.
- Коммит задачи 1 найден в `git log`: `df5f876`.
- `grep -c "Третий круг закрытия (планы 09-13 … 09-15)" .planning/REQUIREMENTS.md` → `1`.
- В границах подраздела: `^| ` → `0`; `**машинная проверка:**` → `5`; `**наблюдённый признак:**` → `5`; `**вердикт:**` → `5`.
- HEAD, записанный в подразделе (`1fabfef`), совпал с `git rev-parse --short HEAD` на момент задачи 1.
- Гейты неприкосновенности прошли СЛИЧЕНИЕМ: `N = 0`, `A = 69 > 0`, флажков `0`.
- `git diff --stat app/ tests/` пуст; `git diff --stat` по `09-VERIFICATION.md`, `.planning/STATE.md`, `.planning/ROADMAP.md` пуст.
- `uv run pytest tests/ -q -p no:randomly` → `2677 passed, 0 failed` (23:31), собрано `2677` (порог плана — не меньше `2655`).
- Останов задачи 2 возвращён оркестратору НЕОТВЕЧЕННЫМ; ветвь исполнителем не выбрана.

---
*Phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy*
*Halted at: 2026-08-31 (останов решения владельца, задача 2)*
