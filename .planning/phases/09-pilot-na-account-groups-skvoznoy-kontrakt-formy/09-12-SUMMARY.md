---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 12
subsystem: testing
tags: [uat, htmx, alpine, accessibility, requirements, evidence, manual-verification]

# Dependency graph
requires:
  - phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
    provides: "ветвь record-kind и правило достижимости цели блокировки (план 09-10); ветвь conditional-include, правило объявленных селекторов и запись INCLUDE_TARGET_EXCEPTIONS по идиоме SP-1 (план 09-11); сведение двух владельцев признака занятости к одному (план 09-08); решение владельца по D-06 ↔ QUAL-06 и раздел свидетельств .planning/REQUIREMENTS.md (план 09-09)"
provides:
  - "09-UAT.md — заполненная таблица отметок ВТОРОГО круга: пять проверок на изменённой разметке, каждая с наблюдённым признаком, включая выписанное название строки UAT9-g048"
  - "09-UAT.md — пять оговорок О-1 … О-5: отступление в порядке шагов 6.3, два отброшенных поля сырых выводов, расхождение по уборке стенда и наблюдение обхода сверх пяти проверок"
  - ".planning/REQUIREMENTS.md — подраздел «Второй круг закрытия (планы 09-10 … 09-12)» СПИСКОМ: на FORM-02, QUAL-01, QUAL-02, QUAL-06 по три поля плюс отдельная строка исхода проверки 7.3"
  - "deferred-items.md — DEF-09-02 с назначенной Фазой 10: «Отмена» закрывает панель, но летящий запрос не отменяет"
  - "опорный признак обхода «ответ 200 И ЧИСТАЯ КОНСОЛЬ» ВОССТАНОВЛЕН наблюдением — ноль строк о неразрешённом селекторе, чем закрыт и повторный проход 7.1 / 7.2"
affects: ["повторная верификация Фазы 9 (перевод состояний FORM-02, QUAL-01, QUAL-02, QUAL-06)", "Фаза 10 (рычаг components/modal.html — назначенная фаза DEF-09-02)", "Фаза 15 (GATE-09 — сводное закрытие девяти пунктов ручного UAT; остаток INCLUDE_TARGET_EXCEPTIONS)"]

# Actuals (#2632) — та же шкала estimateTokens (chars/4), что и у сметы плана,
# по реализованному диффу e7b23f5^..HEAD (задачи 1-3, без коммита сводки).
# Число НЕ округлено в сторону сметы: смета 64000, факт 14140 — промах в 4.5 раза,
# ровно того же рода, что записал план 09-11 (86000 против 14086).
actuals:
  tokens: 14140
  tasks: 3
  commits: 4

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Сообщённое владельцем число проверяется машинно ПЕРЕД тем, как встать в отметку: в долгоживущую запись идёт измеренный исход, а расхождение сообщённого и измеренного записывается оговоркой, а не сглаживается"
    - "Сырой вывод снимавшего инструмента разбирается по полям: поле, зависящее от захардкоженного эталона самого инструмента, в отметку не идёт, и закрытие держится на признаках, от эталона не зависящих (тождество числа, пустой список дублей)"
    - "Отступление в ПОРЯДКЕ шагов обхода (режим сети под первой половиной проверки) объявляется отдельной оговоркой с причиной, а не растворяется в ячейке отметки"
    - "Подраздел свидетельств ставится так, чтобы граница его машинной вырезки была ТОЧНОЙ — перед следующим заголовком того же уровня, а не в хвост файла: иначе антивакуумное утверждение можно удовлетворить посторонней правкой ниже по тексту"

key-files:
  created: []
  modified:
    - ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md"
    - ".planning/REQUIREMENTS.md"
    - ".planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/deferred-items.md"

key-decisions:
  - "Стенд убран ИСПОЛНИТЕЛЕМ после расхождения сообщённого и измеренного: сообщено `groups=0 accounts=0`, измерено `groups=32 accounts=2`; в отметку встал измеренный исход"
  - "Проверка 6.3 снята в ДВЕ части — «в полёте» под Slow 3G, «после отказа» под Offline; отступление записано оговоркой О-1"
  - "Поля `пропало`/`лишнее` (3.5-Б) и `индикаторЗанятости` (6.3) отброшены как артефакты снимавших однострочников — оговорки О-2 и О-3"
  - "DEF-09-02 отложен с назначенной Фазой 10: код не правится, вердикт 6.3 не переписывается"
  - "Подраздел свидетельств поставлен ПЕРЕД записью исхода D-06, чтобы граница машинной вырезки была точной"
  - "Поле `status` артефакта обхода переведено `diagnosed` → `passed`: ни одна из пяти проверок не разошлась, а наблюдение сверх них и остаток Фазы 15 названы в шапке поимённо"
  - "Состояния требований НЕ переведены ни одним символом — их переводит повторная верификация"

patterns-established:
  - "Второй круг закрытия: наблюдение, снятое с механизма, которого на экране больше нет, переснимается на отгруженном дереве, а отметка первого круга при этом не переписывается ни задним числом, ни «уточнением»"
  - "Антивакуум подраздела свидетельств утверждается ДВАЖДЫ — по диффу (ровно одна добавленная строка-заголовок на требование) и по самому файлу внутри точных границ подраздела"

requirements-completed: []  # ⚠️ ПУСТО НАМЕРЕННО. План 09-12 состояний требований НЕ ПЕРЕВОДИТ — это его прямой запрет и предмет двух его машинных гейтов. FORM-02, QUAL-01, QUAL-02 и QUAL-06 остаются в своих состояниях (Gaps Found / Pending) до повторной верификации, которая единственная видит и зелёную суиту, и оба круга свидетельств.

coverage:
  - id: D1
    description: "Пять проверок второго круга заведены в 09-UAT.md, каждая со своим выписанным ожидаемым признаком, таблица отметок готова и пуста"
    requirement: "QUAL-06"
    verification:
      - kind: other
        ref: "awk '/^## /{f=0} /^## Второй круг закрытия/{f=1} f' 09-UAT.md | grep -c 'Ожидаемый наблюдаемый признак' → 5"
        status: pass
      - kind: other
        ref: "git diff -U0 09-UAT.md | grep -c '^-.*2026-08-30 | Chrome' → 0 (отметки первого круга не тронуты)"
        status: pass
    human_judgment: false
  - id: D2
    description: "3.3-Б — после переключения тумблера КЛАВИАТУРОЙ фокус остался на том же тумблере (`document.activeElement` вернул `group-toggle-2231`)"
    requirement: "QUAL-06"
    verification:
      - kind: unit
        ref: "tests/test_pages/test_account_groups.py -k toggle_fragment_keeps_the_toggle_id (1 passed, 116 deselected)"
        status: pass
    human_judgment: true
    rationale: "Возврат фокуса есть свойство ЖИВОГО РАНТАЙМА браузера: 2655 тестов идут через транспорт ASGI и не исполняют ни строчки JS. Суита утверждает только стабильность идентификатора в разметке — что фокус вернулся, видит лишь глаз, и он это увидел 2026-08-31"
  - id: D3
    description: "6.2-Б — на медленной сети двойное нажатие даёт ОДИН запрос переключения (отбрасывающая синхронизация, а не блокировка чекбокса)"
    requirement: "QUAL-01"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py -k 'blocking_target or self_removing_wrapper' (2 passed, 69 deselected)"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_account_groups.py -k repeated_delete (2 passed, 115 deselected)"
        status: pass
    human_judgment: true
    rationale: "Отбрасывание второго запроса — свойство рантайма htmx; суита утверждает только НАЛИЧИЕ атрибута в разметке и число запросов увидеть не может. Наблюдено глазом на Slow 3G: ушёл один запрос"
  - id: D4
    description: "3.5-Б — курсор бесконечной прокрутки переживает удаление: выписанное название `UAT9-g048` на месте ровно один раз, дублей нет, тождество числа сходится"
    requirement: "FORM-02"
    verification:
      - kind: integration
        ref: "tests/test_pages/test_account_groups.py -k 'no_declared_include_selector or scroll_cursor_survives or include_target_exception' (5 passed, 112 deselected)"
        status: pass
    human_judgment: true
    rationale: "Признак отказа МОЛЧАЛИВ — сеть отвечает 200, консоль чистая, счётчик честен. Расхождение видно только сверкой списка с самим собой после подгрузки, и правило проверки требует ВЫПИСАННОГО названия строки прямо: домысливать за человека запрещено"
  - id: D5
    description: "6.3 — после ОТКАЗА запроса кнопка подтверждения нажимаема снова и признака занятости не несёт; кнопка отказа оставалась нажимаемой всё время запроса (WARN-2 / WR-02 не воспроизвёлся)"
    requirement: "QUAL-02"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py -k indicator (4 passed, 67 deselected)"
        status: pass
    human_judgment: true
    rationale: "Отказ запроса вызывается переключателем DevTools и наблюдается на живом экране; сведение двух владельцев признака занятости (план 09-08) утверждается разметочными правилами, а возврат `sending` по ЗАВЕРШЕНИЮ запроса виден только глазом. Шаг не проводился ни разу до этого круга"
  - id: D6
    description: "7.3 — опорный признак «ответ 200 И ЧИСТАЯ КОНСОЛЬ» восстановлен: ноль строк о неразрешённом селекторе после переключения тумблера и после удаления группы"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py#test_no_caller_declares_a_blocking_target_its_form_cannot_have + tests/test_pages/test_account_groups.py#test_no_declared_include_selector_names_an_id_the_document_lacks"
        status: pass
    human_judgment: true
    rationale: "Строка консоли есть свойство живого рантайма htmx; сводки планов 09-10 и 09-11 прямо назвали повторное наблюдение 7.1 / 7.2 недостающим. Это наблюдение его и закрывает"
  - id: D7
    description: "Второй круг свидетельств записан в .planning/REQUIREMENTS.md СПИСКОМ, состояния требований не тронуты ни одним символом"
    verification:
      - kind: other
        ref: "git diff -U0 HEAD~1 -- .planning/REQUIREMENTS.md | grep -cE '^[-+].*\\| (FORM|FOUND|QUAL|FETCH)-[0-9]+ \\|' → 0 (строки таблицы состояний)"
        status: pass
      - kind: other
        ref: "git diff -U0 HEAD~1 -- .planning/REQUIREMENTS.md | grep -cE '^[-+]- \\[[ x]\\] \\*\\*' → 0 (флажки требований)"
        status: pass
      - kind: other
        ref: "антивакуум по диффу: ровно одна добавленная строка `- **R**` на каждое из FORM-02, QUAL-01, QUAL-02, QUAL-06 → 1/1/1/1"
        status: pass
      - kind: other
        ref: "антивакуум по файлу внутри границ подраздела: 1/1/1/1"
        status: pass
      - kind: other
        ref: "форма списком, не таблицей: grep -cE '^\\+\\|.*(FORM|FOUND|QUAL|FETCH)-[0-9]+' → 0"
        status: pass
    human_judgment: false
  - id: D8
    description: "Стенд UAT9- убран из живой базы разработчика и уборка проверена машинно"
    verification:
      - kind: other
        ref: "SELECT count(*) WHERE name LIKE 'UAT9-%' → `UAT9- rows: groups=0 accounts=0`"
        status: pass
    human_judgment: false
  - id: D9
    description: "Полная суита зелена целиком после обеих задач плана"
    verification:
      - kind: unit
        ref: "uv run pytest tests/ -q -p no:randomly → 2655 passed, 0 failed (0:34:22), порог плана 2649"
        status: pass
    human_judgment: false

# Metrics
duration: 2h 31m
completed: 2026-08-31
status: complete
---

# Phase 9 Plan 12: Второй круг закрытия — четыре наблюдения сняты с отгруженного дерева Summary

**Пять проверок на изменённой разметке пройдены и отмечены наблюдённым признаком: фокус после переключения клавиатурой ВЕРНУЛСЯ (`group-toggle-2231`), двойное нажатие дало ОДИН запрос, курсор прокрутки пережил удаление с выписанным `UAT9-g048`, кнопка подтверждения после отказа запроса не осталась занятой, а консоль экрана групп чиста — при этом ни одно состояние требования не переведено, а два расхождения обхода и одна находка сверх проверок записаны, а не сглажены.**

## Performance

- **Duration:** 2h 31m (задача 1 — предыдущий экземпляр исполнителя; задачи 2-3 — этот)
- **Started:** 2026-08-31T05:11:23Z (коммит задачи 1)
- **Completed:** 2026-08-31T07:42:06Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- **Четыре наблюдения, которые были либо устаревшими, либо не снимавшимися ни разу, сняты с ОТГРУЖЕННОГО дерева** (после планов 09-09, 09-10 и 09-11) и записаны с датой, браузером и ОС. Три из них описывали механизм, которого на экране больше нет.
- **`3.3-Б` ОПРОВЕРГЛА ответ первого круга — но на другом механизме, и это сказано прямо.** Первый круг дал «нет» (`<body>`), сегодня `document.activeElement` при нетронутой мыши вернул тот же тумблер `id="group-toggle-2231"`. Рассуждение, записанное задачей 1 В ОЖИДАНИИ измерения (блокировки чекбокса больше нет, терять фокус не на чем; идентификатор стабилен в обеих отрисовках), подтвердилось измерением, а не подставилось вместо него.
- **`3.5-Б` закрыла наблюдательную половину FORM-02 тем самым, чего не хватило первому кругу:** в отметке лежит ВЫПИСАННОЕ дословно `UAT9-g048`, и рядом — сверка списка с самим собой (`дубли: []`) и тождество числа (было 32, удалена одна, отрисовано 31 при счётчике 31).
- **`6.3` проведена ВПЕРВЫЕ за фазу** — шаг, который не получал отметки ни разу. WARN-2 / WR-02 **не воспроизвёлся**: после отказа запроса кнопка подтверждения нажимаема снова и признака занятости не несёт, то есть сведение двух владельцев признака планом 09-08 работает, а `sending` возвращается в исходное по ЗАВЕРШЕНИЮ запроса, а не по успеху.
- **Опорный признак обхода «ответ 200 И ЧИСТАЯ КОНСОЛЬ» ВОССТАНОВЛЕН** — ноль строк о неразрешённом селекторе. Это же наблюдение закрывает повторный проход 7.1 и 7.2, который сводки планов 09-10 и 09-11 прямо назвали недостающим («после правки строка в браузере повторно не наблюдалась»).
- **Второй круг свидетельств лёг в долгоживущее место** — `.planning/REQUIREMENTS.md`, СПИСКОМ, на каждое из четырёх требований машинная проверка (прогнанная заново на сегодняшнем дереве, а не переписанная из сводок), наблюдённый признак и вердикт, плюс отдельная строка исхода 7.3 с поимённо названным остатком Фазы 15.
- **Ни одно состояние требования не переведено ни одним символом** — и это утверждено двумя гейтами, СПОСОБНЫМИ ПОКРАСНЕТЬ, плюс двумя антивакуумными утверждениями.

## Task Commits

Каждая задача закоммичена атомарно:

1. **Задача 1: пять проверок второго круга — с выписанным ожидаемым признаком у каждой** — `e7b23f5` (docs) — *исполнена предыдущим экземпляром исполнителя; коммит проверен на месте: 191 строка в 09-UAT.md, `git diff --stat e7b23f5^..e7b23f5 -- app/ tests/` пуст*
2. **Задача 2: НАБЛЮДЕНИЕ — пять проверок второго круга на отгруженном дереве** — `b8d76e5` (docs)
3. **Задача 3: второй круг свидетельств записан, состояния требований не тронуты ни одним символом** — `02ea968` (docs)

**Plan metadata:** `docs(09-12): complete второй круг закрытия plan`

## Files Created/Modified

- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md` — раздел «Второй круг закрытия» с пятью проверками (задача 1), заполненная таблица отметок второго круга и пять оговорок О-1 … О-5 (задача 2), шапка `walkthrough_2` и `status: passed` (задача 3). +256 строк за три задачи.
- `.planning/REQUIREMENTS.md` — подраздел «Второй круг закрытия (планы 09-10 … 09-12)» внутри существующего раздела свидетельств Фазы 9: четыре записи по три поля и отдельная строка исхода 7.3. +46 строк, **ноль тронутых строк таблицы состояний и ноль тронутых флажков**.
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/deferred-items.md` — **DEF-09-02** с назначенной Фазой 10, механизмом, прочитанным по исходнику, и тремя неравноценными развилками решения.

## Decisions Made

- **Стенд убран ИСПОЛНИТЕЛЕМ, и расхождение записано.** Владелец сообщил `UAT9- rows: groups=0 accounts=0`; машинная проверка по той же команде и той же базе вернула `groups=32 accounts=2`. В отметку встал ИЗМЕРЕННЫЙ исход. Основание: подставить сюда сообщённое число вместо измеренного означало бы ровно ту подпись под ненаблюдённым, ради запрета которой заведён весь второй круг.
- **Проверка 6.3 снята в две части** (в полёте — Slow 3G, после отказа — Offline), потому что на Offline запрос падает практически мгновенно и шаг 6.3.3 наблюдать не на чем. Ни один шаг не пропущен; изменён только режим сети первой половины. Оговорка **О-1**.
- **Два поля сырых выводов отброшены как артефакты снимавших однострочников, а не как поведение экрана** — `пропало`/`лишнее` в 3.5-Б (эталон имён инструмента разошёлся со стендом) и `индикаторЗанятости` в 6.3 (проверялось лишь НАЛИЧИЕ узла `.form-busy`, который есть всегда, когда форма ходит через htmx). Оговорки **О-2** и **О-3**. Закрытие обеих проверок держится на признаках, от этих полей не зависящих.
- **DEF-09-02 отложен с назначенной Фазой 10, код не правится, вердикт 6.3 не переписывается.** 6.3 спрашивает только о нажимаемости кнопки отказа, и она нажимаема — наблюдение это подтверждает, а не опровергает.
- **Подраздел свидетельств поставлен ПЕРЕД записью исхода D-06.** Плана этот выбор не касался (он задал только уровень заголовка), но от него зависит ТОЧНОСТЬ границы машинной вырезки: при постановке в хвост раздела вырезка тянулась бы до конца файла, и антивакуумное утверждение по файлу можно было бы удовлетворить посторонней правкой ниже по тексту — то есть гейт потерял бы ровно то свойство, ради которого заведён.
- **`status` артефакта обхода переведён `diagnosed` → `passed`.** Правило действия задачи 3 привязывает `diagnosed` к перечню РАЗОШЕДШИХСЯ проверок; ни одна из пяти не разошлась. Наблюдение сверх пяти проверок (DEF-09-02) и незакрытый остаток `INCLUDE_TARGET_EXCEPTIONS['group-list-sentinel']` (Фаза 15) названы в шапке `walkthrough_2` поимённо, поэтому за словом `passed` ничего не спрятано.
- **Состояния требований не переведены.** `requirements-completed` этой сводки ПУСТ намеренно.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Стенд `UAT9-` не был убран: сообщённое разошлось с измеренным**

- **Found during:** Задача 2 (машинная проверка при возобновлении)
- **Issue:** Сигнал возобновления нёс вывод уборки `UAT9- rows: groups=0 accounts=0`. Проверка задачи 2 требует машинно убедиться, что «строк с префиксом `UAT9-` в базе не осталось». Прогон команды раздела «Как убрать стенд» на живой базе (`192.168.0.9:5432/broadcaster`) вернул **`UAT9- rows: groups=32 accounts=2`**: два аккаунта `UAT9-account` (id 51 и 52) и 32 группы, среди них выписанная `UAT9-g048` и `UAT9-needle`. Записать сообщённое число как факт значило бы подписаться под ненаблюдённым — ровно то, что этот план запрещает.
- **Fix:** Выполнена команда уборки из раздела «Как убрать стенд» (прямая мера угрозы T-09-12-02, префикс-ограниченная, ранее исполненная планом 09-09) — `removed: groups=32 accounts=2`; повторная проверка дала `UAT9- rows: groups=0 accounts=0`. Оба вывода записаны оговоркой **О-4** в артефакте обхода, вместе с расхождением.
- **Files modified:** `09-UAT.md` (оговорка О-4); живая база — удалены 32 строки и 2 аккаунта строго под префиксом `UAT9-`
- **Verification:** `UAT9- rows: groups=0 accounts=0`
- **Committed in:** `b8d76e5`

**2. [Rule 3 - Blocking] Проверка 6.3 неисполнима одним режимом сети — разбита на две части**

- **Found during:** Задача 2 (разбор наблюдения владельца)
- **Issue:** Шаг 6.3.3 требует нажать «Отмена», **пока запрос идёт**, а шаг 6.3.4 — дождаться отказа под `Offline`. На `Offline` запрос падает практически мгновенно, и наблюдать «пока запрос идёт» не на чем: два шага одной проверки требуют взаимоисключающих условий.
- **Fix:** Наблюдение в полёте снято под **Slow 3G**, наблюдение после отказа — под **Offline**, как предписано. Отступление от порядка шагов записано отдельной оговоркой **О-1**, а не растворено в ячейке отметки. Обе кнопки наблюдены, ни один шаг не пропущен.
- **Files modified:** `09-UAT.md`
- **Verification:** ячейка 6.3 несёт наблюдение по обеим кнопкам в обеих частях; `disabled` и `aria-busy` сняты отдельно и оба праздны после отказа
- **Committed in:** `b8d76e5`

**3. [Rule 1 - Bug] Эталон снимавшего однострочника 3.5-Б разошёлся со стендом — поля вывода отброшены, а не приняты**

- **Found during:** Задача 2 (разбор наблюдения владельца)
- **Issue:** Однострочник сверки нёс СВОЙ захардкоженный эталон имён (`UAT9-g001…g039` плюс `UAT9-needle`), а стенд оказался другим: 32 группы с непоследовательными именами вплоть до `UAT9-g049` (следы повторных посевов). Поля `пропало` и `лишнее` его вывода выглядели тревожно, но описывали НЕВЕРНЫЙ эталон, а не поведение экрана. Принять их означало бы записать ложное расхождение; молча выбросить — сгладить.
- **Fix:** Поля названы артефактом инструмента оговоркой **О-2**, и закрытие 3.5-Б переставлено на два признака, от эталона не зависящих: тождество числа (было 32, удалена одна, отрисовано 31 при счётчике 31 — молчаливая потеря строки дала бы `отрисовано: 30`) и `дубли: []` (задвоение дало бы непустой список и `отрисовано: 32`). Тем же рассуждением отброшено поле `индикаторЗанятости` вывода 6.3 (оговорка **О-3**): однострочник проверял НАЛИЧИЕ узла `.form-busy`, который присутствует всегда, когда форма ходит через htmx.
- **Files modified:** `09-UAT.md`
- **Verification:** ячейки 3.5-Б и 6.3 несут признаки, названные в оговорках; ни один отброшенный признак не удалён из записи молча
- **Committed in:** `b8d76e5`

**4. [Rule 2 - Missing critical] Точная граница машинной вырезки подраздела свидетельств**

- **Found during:** Задача 3
- **Issue:** План задал подразделу только УРОВЕНЬ заголовка, не место. При постановке его в хвост раздела свидетельств (после записи исхода D-06) вырезка `awk` тянулась бы до конца файла, потому что следующего заголовка того же или высшего уровня там нет. Антивакуумное утверждение по файлу — то самое, которое план называет «частью, которую нельзя удовлетворить посторонней правкой», — стало бы удовлетворимо строкой `- **FORM-02**`, дописанной где угодно ниже по тексту.
- **Fix:** Подраздел поставлен ПЕРЕД `#### ЗАПИСЬ ИСХОДА D-06 ↔ QUAL-06`, и граница вырезки кончается этим заголовком. Проверено исполнением: вырезка даёт 44 строки, ровно по одной строке-заголовку на каждое из четырёх требований.
- **Files modified:** `.planning/REQUIREMENTS.md`
- **Verification:** `SEC=$(awk '/^### /{f=0} /^#### /{f=0} /^#### Второй круг закрытия/{f=1} f' .planning/REQUIREMENTS.md)` → 44 строки; `grep -cE "^- \*\*$Q\*\*"` → `1` для каждого из FORM-02, QUAL-01, QUAL-02, QUAL-06
- **Committed in:** `02ea968`

**5. [Rule 2 - Missing critical] Строка исхода 7.3 переименована с «вердикта» на «исход»**

- **Found during:** Задача 3 (прогон собственного критерия приёмки)
- **Issue:** Критерий приёмки требует: «каждый вердикт — одно из двух слов, `заработано` либо `не заработано`, и ни одного третьего». Строка исхода проверки 7.3 не принадлежит ни одному требованию (план ставит её отдельной строкой) и несёт не вердикт требования, а состояние опорного признака. Названная «вердиктом», она давала пятое значение и красила собственный критерий плана на верной по существу работе.
- **Fix:** Поле этой строки названо `исход` — тем самым словом, которым его называет действие задачи 3 («записать ИСХОД проверки 7.3»). Четыре вердикта требований остались ровно двухсловными.
- **Files modified:** `.planning/REQUIREMENTS.md`
- **Verification:** `grep -c '\*\*вердикт:\*\*'` внутри подраздела → `4`; строк с вердиктом вне двух разрешённых слов → `0`
- **Committed in:** `02ea968`

---

**Total deviations:** 5 auto-fixed (2 blocking, 1 bug, 2 missing critical)
**Impact on plan:** Ни одно отступление не расширило объём: файлов приложения и тестов план не тронул вовсе (`git diff --stat app/ tests/` от базы плана пуст). Три отступления — прямые следствия правила «подпись под ненаблюдённым запрещена»: они не сгладили расхождения, а назвали их. Два — починка собственных гейтов плана, сделанная так, чтобы гейт остался способен покраснеть.

## Deferred Issues

- **DEF-09-02 — «Отмена» закрывает панель подтверждения, но летящий запрос НЕ отменяет.** Наблюдено сверх пяти проверок круга, на части «в полёте» проверки 6.3: кнопка нажалась, панель закрылась, **группа всё равно удалилась**. Механизм прочитан по исходнику `app/templates/components/modal.html`: `x-on:click="hide()"`, а `hide()` делает только `open = false` и возврат фокуса — ни `htmx.trigger(form, 'htmx:abort')`, ни `AbortController` в обработчике нет. **Назначенная фаза — Фаза 10** («Рычаг `components/modal.html`», FORM-06). Код не правится, вердикт 6.3 не переписывается: 6.3 спрашивает о НАЖИМАЕМОСТИ кнопки отказа, и кнопка нажимаема. Три развилки решения (отменять запрос / не давать закрыть панель / назвать поведение словами) записаны в `deferred-items.md` вместе с ценой каждой — они неравноценны, и выбор принадлежит владельцу.
- **DEF-09-01 — `test_the_overview_error_number_matches_the_users_own_dashboard` краснеет от часов, а не от кода.** В этот прогон **не сработал**: полная суита дала `2655 passed, 0 failed` (прогон 06:0x → 06:4x UTC, вне красного окна ~00:00-05:00 по времени пользователя). Находка остаётся открытой — она свойство часов прогона, а не кода, и её отсутствие сегодня не есть её починка.
- **Остаток `INCLUDE_TARGET_EXCEPTIONS['group-list-sentinel']`, назначенная Фаза 15** (второе следствие DIV-09-01, план 09-11). На стенде проверки 7.3 его положение экрана недостижимо, поэтому здесь ожидался и наблюдён ноль строк; запись остаётся открытой и этим кругом не закрывается. Названа поимённо в строке исхода 7.3 раздела свидетельств и в шапке `walkthrough_2` артефакта обхода.

## Known Stubs

Заглушек нет. Ни один файл приложения и ни один тест этим планом не тронуты — `git diff --stat app/ tests/` от базы плана пуст, и это его собственный машинный критерий, прогнанный в обеих задачах.

## Threat Flags

Новой поверхности безопасности план не заводит: сетевых маршрутов, путей авторизации, обращений к файлам и изменений схемы в нём нет вовсе. Из угроз реестра сработала одна, и она была предусмотрена:

| Угроза | Что случилось | Как закрыто |
|---|---|---|
| T-09-12-02 (Tampering — посевные строки, оставшиеся в живой базе) | Сработала: стенд второго круга остался в живой базе разработчика — 32 группы и 2 аккаунта, при сообщённой уборке | Предписанная мера применена буквально: уборка префикс-ограниченной командой раздела «Как убрать стенд», вывод проверки приведён в отметке и в оговорке О-4 |
| T-09-12-01 (Repudiation — отметка без наблюдённого признака) | Не сработала | Все пять ячеек несут признак; 3.5-Б несёт выписанное название; ни одна ячейка не состоит из «ок» — проверено машинно |
| T-09-12-03 (Tampering — преждевременный перевод состояния требования) | Не сработала | Четыре гейта зелены переходом, включая два антивакуумных: 0 изменённых строк таблицы, 0 изменённых флажков, ровно по одной добавленной строке-заголовку на требование по диффу и по файлу |
| T-09-12-06 (Repudiation — подраздел таблицей) | Не сработала | `grep -cE "^\+\|.*(FORM\|FOUND\|QUAL\|FETCH)-[0-9]+"` → `0` |

## Issues Encountered

- **Расхождение сообщённого и измеренного по уборке стенда** — разобрано выше (отступление 1). Разрешено прогоном команды уборки и записью обоих выводов; сообщённое число в отметку не пошло.
- **Полная суита идёт 34 минуты** (`2062.54s`), и это не отказ, а свойство суиты: 2655 тестов. Прогон выполнен целиком, до коммита задачи 3, с результатом `0 failed` при пороге плана «не меньше 2649 пройденных».
- **Порядок прогона гейтов задачи 3 соблюдён буквально:** три утверждения, читающие `git diff` без базы, прогнаны ПО РАБОЧЕМУ ДЕРЕВУ до коммита. Повторный прогон после коммита выполнен с базой `HEAD~1`, как предписывает пункт 5 действия задачи, и это названо здесь, а не подменено молча — исходы совпали.

## User Setup Required

None — внешних служб план не настраивает.

## Next Phase Readiness

**Готово к повторной верификации Фазы 9.** Оба круга свидетельств лежат в `.planning/REQUIREMENTS.md` в форме, которую читает повторная верификация: на FORM-02, QUAL-01, QUAL-02 и QUAL-06 — машинная проверка, наблюдённый признак и вердикт `заработано`, с явно названной ценой у каждого. **Перевод состояний остаётся за верификацией**, которая единственная видит и зелёную суиту (`2655 passed`), и заполненные таблицы отметок обоих кругов, и оба круга свидетельств.

Что наследуют дальше:

- **Фаза 10** — `DEF-09-02` как назначенную ей находку, вместе с тремя развилками и их ценой. Правка кнопки отказа задевает то самое свойство, которое защищают проверка 6.3 и требование QUAL-02, — решать её вместе с ними, а не мимо.
- **Фаза 15 (GATE-09)** — отметки обоих кругов как факт, и незакрытый остаток `INCLUDE_TARGET_EXCEPTIONS['group-list-sentinel']`, названный поимённо.
- **Фаза 11** — сегодняшнее наблюдение сделано на ОДНОЙ форме; при раздаче макроса-обёртки сорока семи формам все пять признаков придётся переснять на сводном обходе, потому что признак передаётся ВЫЗОВОМ макроса, а не самим макросом.

Блокирующих концов у плана нет: расхождений среди пяти проверок ноль, стенд из живой базы убран и уборка проверена, файлов приложения и тестов план не тронул.

---
*Phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy*
*Completed: 2026-08-31*

## Self-Check: PASSED

Прогнано по дереву после коммита задачи 3, база `HEAD~2` / `HEAD~4` там, где утверждение читает дифф (пункт 5 действия задачи 3 — названо, а не подменено молча).

| Что утверждается | Прогон | Исход |
|---|---|---|
| Файлы на диске | `09-12-SUMMARY.md`, `09-UAT.md`, `.planning/REQUIREMENTS.md`, `deferred-items.md` | все FOUND |
| Коммиты в истории | `e7b23f5`, `b8d76e5`, `02ea968`, `d15f09f` | все FOUND |
| Таблица отметок второго круга непуста, 3.5-Б несёт `UAT9-g048` | вырезка строки данных из таблицы | PASS |
| Стенд `UAT9-` убран | `UAT9- rows: groups=0 accounts=0` | PASS |
| Состояния требований и флажки не тронуты | `0` изменённых строк таблицы, `0` изменённых флажков | PASS |
| Подраздел свидетельств — список, по одной записи на каждое из четырёх требований | вырезка подраздела, `grep -cE "^- \*\*R\*\*"` → `1/1/1/1` | PASS |
| `09-VERIFICATION.md`, планы и сводки 09-01 … 09-11 не тронуты | `git diff --stat` пуст | PASS |
| Файлов приложения и тестов план не тронул | `git diff --stat app/ tests/` пуст | PASS |
| Полная суита | `uv run pytest tests/ -q -p no:randomly` → `2655 passed, 0 failed` (0:34:22), порог `2649` | PASS |
