---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/templates/components/modal.html
  - app/templates/account_groups/includes/group_row.html
  - app/templates/account_groups/partials/delete_response.html
  - app/pages/account_groups.py
  - tests/test_pages/test_account_groups.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [FORM-02, QUAL-01, QUAL-02]

estimate:
  # ⚠️ confidence: low при нулевом числе замеров — калибровки проекта не существует.
  tokens: 124000
  raw_tokens: 124000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Удаление группы идёт через htmx без перезагрузки страницы: ответ hx-swap=\"none\", всё едет внеполосно — снятие строки, снятие панели подтверждения, счётчик (FORM-02, D-11)"
    - "Панель подтверждения снимается вторым OOB-узлом hx-swap-oob=\"delete\" уже здесь, а не откладывается до Фазы 10: без снятия после N удалений в документе копятся N панелей role=\"dialog\" с живыми ловушками фокуса (D-02)"
    - "Ответ удаления ОДИНАКОВ для найденной и не найденной группы: по несуществующему id внеполосное снятие не делает ничего, повторный запрос безвреден и не сообщает, какие идентификаторы заняты чужими группами (QUAL-01 идемпотентность, D-04)"
    - "Опустевший список закрывается HX-Location на тот же адрес, а не второй отрисовкой пустого состояния: три различимых пустых состояния остаются в list.html в одном экземпляре (D-09)"
    - "hx-post попадает на форму панели подтверждения ОПЦИОНАЛЬНЫМИ параметрами modal(): при умолчании hx_post=false разметка не меняется ни на символ, и остальные 15 мест подтверждения не трогаются (D-01)"
    - "Кнопка Отмены в панели подтверждения не блокируется никогда: правило проверяет ТРИ множества селекторов — литеральные значения на местах hx-post, параметрические (обязаны быть записью DISABLED_ELT_EXCEPTIONS) и значения, ПЕРЕДАВАЕМЫЕ вызывающими; последнее существует потому, что макрос-обёртка прячет вызывающего от файлового разборщика (QUAL-01)"
    - "Удаление остаётся рабочим без JS: форма панели сохраняет method=\"post\" литералом и непустой action; без HX-Request маршрут отвечает прежним 302 (FORM-02)"
    - "У переведённого account_groups_delete нет собственного RedirectResponse ни в одной ветке, включая «нет сессии» (G-2)"
    - "Инвентарь сдвинут: HX_POST_PLACES 2→3, OOB_BLOCKS 7→9, MACRO_DEFINITION_SITES_DECLARED 1→2, NOT_YET_CONVERTED_COUNT 35→34, PARAMETRIC_SWAP_TARGETS_DECLARED 0→1; литерал len(paths) пересчитан исполнением повторно"
    - "Параметрическая цель подмены ОБЪЯВЛЕНА БЕЗУСЛОВНО: PARAMETRIC_SWAP_TARGETS ключом по файлу разборщика, полем callers (§«Форма ключа параметрической записи» плана 09-01) и несущим правилом «каждое МЕСТО цели либо литеральное, либо относительное, либо объявленное параметрическое» — арифметика считает места с обеих сторон, а не места против идентификаторов"
    - "Помощник _swap_declaring_templates объявлен ЗДЕСЬ, рядом с перечнем, и является единственным читателем поля callers для правила D-08 плана 09-03: два плана согласованы одной функцией, а не двумя описаниями"
    - "Вопрос G-11 закрыт ИЗМЕРЕНИЕМ, а не допущением: пересечение _swap_target_ids & _oob_target_ids вычислено на итоговом дереве и приведено в сводке дословно; ID_IN_TWO_ROLES_BY_DESIGN заводится ТОЛЬКО при непустом пересечении, и пустой результат тоже записывается с основанием"
    - statement: "Осиротевшая панель подтверждения после удаления действительно снята со страницы, а после подмены строки тумблером панель не задвоилась; Alpine в оставшихся панелях жив — ручной UAT, пункт 5 (частично) и пункт 3"
      verification: backstop
  artifacts:
    - app/templates/account_groups/partials/delete_response.html
    - "app/templates/components/modal.html — параметр hx_post"
    - "tests/test_templates/test_htmx_markup_gates.py — ParametricTarget, PARAMETRIC_SWAP_TARGETS, PARAMETRIC_SWAP_TARGETS_DECLARED, _swap_declaring_templates (БЕЗУСЛОВНО)"
    - "tests/test_templates/test_htmx_markup_gates.py — ID_IN_TWO_ROLES_BY_DESIGN, ID_IN_TWO_ROLES_DECLARED (УСЛОВНО: только если измеренное пересечение непусто)"
  key_links:
    - "modal(hx_post=true) в group_row.html → account_groups_delete: единственное место подтверждения, получающее htmx в этой фазе; ломается — удаление уходит полной перезагрузкой либо, наоборот, все 16 мест меняются разом и Фаза 10 теряет предмет"
    - "delete_response.html → #group-del-N и #group-row-N: два OOB-узла со снятием по собственному id; ломается — на экране остаётся диалог с живой ловушкой фокуса поверх пустого места"
    - "count_rule_oob.html → оба шаблона ответа: один источник узла счётчика; ломается — OOB_BLOCKS становится 10 вместо 9 и два экземпляра линейки расходятся"
    - "PARAMETRIC_SWAP_TARGETS.callers → _swap_declaring_templates → правило D-08 плана 09-03: ЕДИНСТВЕННЫЙ путь, которым правило границы клиентского состояния доходит до account_groups/includes/group_row.html; ломается — множество сводится к components/form_wrapper.html, в котором x-data нет вовсе, правило зеленеет по построению и его контроль не краснеет ни на чём"
  prohibitions:
    - statement: "умолчание modal() не меняется ни на символ: 15 остальных мест подтверждения обязаны рендериться байт-в-байт как до плана — правка на все 16 мест это предмет Фазы 10 (FORM-06)"
      status: flagged-unverified
      verification: none
    - statement: "hx-confirm не вводится — он вызывает браузерный confirm(), выброшенный в v2.0 вместе с 14 местами"
      status: flagged-unverified
      verification: none
    - statement: "панель подтверждения не переезжает внутрь строки: она обязана остаться СНАРУЖИ любой цели подмены (записанное решение v2.0, T-11-04)"
      status: flagged-unverified
      verification: none
    - statement: "второй экземпляр пустого состояния во фрагменте не заводится — опустевший список закрывается HX-Location (D-09)"
      status: flagged-unverified
      verification: none
    - statement: "требование к вызывающему modal() не ослабляется: id обязан оставаться величиной, выбранной сервером, потому что подставляется в ИМЯ атрибута x-on:modal-open-{{ id }}.window"
      status: flagged-unverified
      verification: none
---

<objective>
Второй путь экрана — **удаление** — переводится на тот же контракт: ответ без основной цели
свапа, три внеполосных узла, `HX-Location` на опустевший список. `hx-post` приезжает на форму
панели подтверждения **опциональными параметрами `modal()`** с умолчанием, при котором разметка
не меняется ни на символ.

Purpose: доказать вторую ветку GATE-05 («на каждом `hx-post` есть `hx-target` **либо**
`hx-swap="none"`») и завести механизм снятия осиротевшей панели, который Фаза 10 унаследует
готовым на 16 мест. Граница проведена по критериям Фазы 10: рычаг здесь **не дёргается**.

Output: `account_groups/partials/delete_response.html`, параметр `hx_post` у `modal()`,
переведённый `account_groups_delete`, безусловно объявленный `PARAMETRIC_SWAP_TARGETS` вместе с
`_swap_declaring_templates`, закрытый измерением вопрос G-11, пять сдвинутых чисел.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-RESEARCH.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-PATTERNS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VALIDATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов, заводимых фазой, — в `09-01-PLAN.md` §«Artifacts this phase produces».
Этим планом создаются:

- `app/templates/account_groups/partials/delete_response.html`
- параметр `hx_post` макроса `modal()` в `app/templates/components/modal.html`
- ветвление `account_groups_delete` на `respond()` в `app/pages/account_groups.py`
- **БЕЗУСЛОВНО** (задача 2, шаг 2): `ParametricTarget`, `PARAMETRIC_SWAP_TARGETS`,
  `PARAMETRIC_SWAP_TARGETS_DECLARED`, `_swap_declaring_templates`
- **УСЛОВНО** (задача 2, шаг 3 — только если измеренное пересечение непусто):
  `ID_IN_TWO_ROLES_BY_DESIGN`, `ID_IN_TWO_ROLES_DECLARED`
- имена тестов: `test_delete_degrades_without_htmx`, `test_delete_returns_oob_nodes`,
  `test_last_group_goes_to_location`,
  `test_delete_does_not_trust_the_account_id_from_the_url_over_htmx`,
  `test_cancel_is_never_disabled`, `test_control_negative_a_disabled_cancel_reddens_the_gate`,
  и безусловно `test_every_parametric_swap_target_carries_a_reason`,
  `test_the_number_of_parametric_swap_targets_is_the_declared_one`,
  `test_every_declared_parametric_target_is_actually_parametric`,
  `test_every_declared_parametric_caller_actually_calls_the_macro`,
  `test_every_swap_target_is_either_literal_or_declared_parametric`,
  `test_control_negative_an_undeclared_parametric_target_reddens_the_gate`;
  условно — `test_every_two_role_id_carries_a_reason`,
  `test_the_number_of_two_role_ids_is_the_declared_one`,
  `test_every_declared_two_role_id_is_actually_in_conflict`
</artifacts_this_phase_produces>

<tasks>

<task type="execute" tdd="true">
  <name>Задача 1: Удаление на `respond()` — ответ целиком внеполосный, `modal()` параметрами</name>

  <files>
app/templates/components/modal.html,
app/templates/account_groups/includes/group_row.html,
app/templates/account_groups/partials/delete_response.html (NEW),
app/pages/account_groups.py,
tests/test_pages/test_account_groups.py,
tests/test_pages/test_htmx_gates.py,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/components/modal.html` — **читать ЦЕЛИКОМ.** Докстринг `:1-79` — 79 строк на
  30 строк разметки, плотность SP-4; `:54-64` снимает половину работы (новой CSS для видимости
  блокировки не нужно — `.btn[disabled]` отгружено); `:81-91` — почему `id` обязан быть
  величиной, выбранной сервером (подставляется в **ИМЯ** атрибута
  `x-on:modal-open-{{ id }}.window`, а имена атрибутов не экранируются ничем); `:92` —
  сигнатура и приём «умолчание-литерал в двойных кавычках»; `:111-112` — точка правки, форма
  панели; `:113-114` — идиома опционального параметра `{%- if body %}` и совместимость с
  `caller`; `:117` — кнопка подтверждения `type="submit"`, кнопка отказа выше — `type="button"`.
- `app/templates/account_groups/includes/group_row.html:89-109` — вызов `modal()` и комментарий,
  почему панель стоит СНАРУЖИ строки; адрес удаления записан здесь `~`-конкатенацией.
- `app/templates/account_groups/partials/count_rule_oob.html` (создан планом 09-01) — узел
  счётчика, включаемый **обоими** шаблонами ответа.
- `app/templates/ads/includes/autosave_response.html` — единственный шипнутый ответ без основной
  цели свапа; его шапка объясняет, ПОЧЕМУ цели нет. Тело плоское, узлы — прямые дети файла.
- `app/templates/account_groups/partials/sync_result.html:18-34` — четыре инварианта свапаемого
  блока; п. 3 дословно предупреждает о задвоении панели (T-11-04).
- `app/pages/account_groups.py:335-377` — `account_groups_delete`: тройной `WHERE` (`:344-349`),
  `ScheduleRepository(db).remove_group_ids` (`:366-371`), комментарий «Ответ ОДИНАКОВ…»
  (`:374-376`) — основание D-04, переезжает **дословно**; `:347` — `RedirectResponse(url="/login")`.
- `app/pages/account_groups.py::_group_counts` (создан планом 09-01).
- `app/pages/htmx.py:305-367` — `respond` и ветка `fragment is None` → `location_response`
  (204 + `HX-Location`): готовый механизм D-09, писать нечего.
- `tests/test_pages/test_account_groups.py:991` (`test_delete_does_not_trust_the_account_id_from_the_url`),
  `:1031` (`test_repeated_delete_is_harmless` — переезжает на новый транспорт дословно).
- `tests/test_templates/test_htmx_markup_gates.py` — `:139-148` (`HX_POST_TAG`, границы тега
  обрываются на первом `>`), `:400-411` (`_offenders_method_and_action` — требует **литерального**
  `post`), `:440-470` (G-3, G-4), `:759-771` (`_action_sites`, точка исключения
  `MACRO_DEFINITION_SITES`, заведена планом 09-01), `:1025-1043` (`_oob_target` — форма
  `hx-swap-oob="delete"` берёт цель из собственного `id` тега), `:1208-1252`
  (число OOB-блоков и ветка «условных блоков ровно один»).
- `09-RESEARCH.md` §2.2 (четыре вердикта разборщиков на будущем исходнике `modal.html`),
  §2.5 γ1 (умолчание макроса для метода), §Code Examples «Шаблон ответа удаления»,
  Pitfall 6, Pitfall 8, Pitfall 11.
  </read_first>

  <precondition>
План 09-01 приземлён: существуют `app/pages/account_groups.py::_group_counts`,
`app/templates/account_groups/partials/count_rule_oob.html`, перечень `MACRO_DEFINITION_SITES`
с точкой исключения внутри `_action_sites`, и значения `HX_POST_PLACES = 2`, `OOB_BLOCKS = 7`,
`NOT_YET_CONVERTED_COUNT = 35`, `MACRO_DEFINITION_SITES_DECLARED = 1`. Проверить
`uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q`
— зелено до первой правки.
  </precondition>

  <reversibility rating="costly">
Задача реализует D-11 в его наследуемой половине: форма ответа панели подтверждения
(`hx-swap="none"`, всё внеполосно) **наследуется 16 местами Фазы 10**. Решающий довод записан:
при `hx-target` макрос `modal()` обязан был бы знать цель каждого из 16 мест, и «одна правка»
Фазы 10 превратилась бы в 16 разных вызовов. `one-way` не заслужено — форма меняется правкой
одного макроса, но после Фазы 10 цена вырастет шестнадцатикратно.
  </reversibility>

  <behavior>
RED — до правки разметки:
1. `NOT_YET_CONVERTED_COUNT` 35 → 34 и снятие
   `"app/pages/account_groups.py::account_groups_delete"` из `NOT_YET_CONVERTED`.
2. `HX_POST_PLACES` 2 → 3, `OOB_BLOCKS` 7 → 9, `MACRO_DEFINITION_SITES_DECLARED` 1 → 2.
3. Новые тесты `test_delete_degrades_without_htmx`, `test_delete_returns_oob_nodes`,
   `test_last_group_goes_to_location`,
   `test_delete_does_not_trust_the_account_id_from_the_url_over_htmx`.

GREEN — `modal()`, шаблон ответа, обработчик, γ1 для метода.

Ожидаемые случаи:
- POST `/accounts/{a}/groups/{g}/delete` **без** `HX-Request`, `follow_redirects=False`
  → `302`, `location == f"/accounts/{a}/groups"`.
- Тот же POST **с** `HX-Request`, когда в аккаунте остаются другие группы → `200`;
  `"<!DOCTYPE" not in response.text`;
  `f'id="group-row-{g}"' in response.text` **и** рядом `hx-swap-oob="delete"`;
  `f'id="group-del-{g}"' in response.text` **и** рядом `hx-swap-oob="delete"`;
  `'hx-swap-oob="innerHTML:#account-groups-count"' in response.text`.
- Тот же POST с `HX-Request` по **несуществующему** `group_id` и по **чужой** группе → ответ
  **побайтово тот же**, что у найденной (D-04): тело собирается из `group_id` пути, снятие по
  несуществующему `id` не делает ничего.
- Удаление **последней** группы аккаунта с `HX-Request` → `204`,
  `response.headers["HX-Location"] == f"/accounts/{a}/groups"`, тела нет (D-09).
- Чужой `account_id` в пути при своей группе → строка в БД **не удалена**, ответ неотличим от
  успешного (пара к `test_delete_does_not_trust_the_account_id_from_the_url`).
- `test_repeated_delete_is_harmless` — сохраняется, и получает вторую половину под `htmx_client`.
  </behavior>

  <action>
**1. `app/templates/components/modal.html` — опциональный параметр (D-01).**
Сигнатура `:92` пополняется **одним** флагом: `hx_post=false`, записанным по идиоме соседей
(умолчание-литерал). Форма панели `:111-112` получает условную вставку внутри открывающего тега:
`{%- if hx_post %} hx-post="{{ action }}" hx-swap="none" hx-disabled-elt="find button[type=submit]"
hx-indicator="find .form-busy"{% endif %}` и, **внутри тела формы**, условный узел индикатора
`{%- if hx_post %}<span class="form-busy" aria-hidden="true"></span>{% endif %}`.

Правила, каждое — следствие проверенного факта:
- `hx-post` печатает **ту же переменную `{{ action }}`**, что и `action` — G-4 сравнивает сырые
  строки шаблона; два разных выражения с одинаковым результатом считаются нарушением осознанно.
- `hx-swap="none"` — у формы панели цели свопа нет вовсе (D-11); при `hx-target` макрос обязан
  был бы знать цель каждого из 16 мест.
- внутри открывающего тега **не должно появиться ни одного символа `>`** — `HX_POST_TAG`
  обрывается на первом.
- признак `hx-swap-oob` в этом файле не появляется вовсе, поэтому ветка «условных блоков ровно
  один» (`test_the_number_of_oob_blocks_is_the_declared_one`) не затрагивается.
- **умолчание `hx_post=false` обязано оставлять разметку без изменений ни на символ** —
  проверяется рендером макроса с прежними аргументами и посимвольным сравнением с эталоном,
  снятым до правки.
- Требование к вызывающему из `:81-91` **не ослабляется**: `id` остаётся величиной, выбранной
  сервером; докстринг не сокращается.
Докстринг пополняется абзацем, называющим, почему параметр опционален и почему умолчание не
двигается: правка, расходящаяся на 10 потребителей, ушла бы ДО того, как контракт подтверждён
руками, и FORM-06 вместе с Фазой 10 потеряли бы предмет.

**2. G-3 и метод — резолюция γ1 (RESEARCH §2.5).** `_offenders_method_and_action` (`:400-411`)
требует литерального `post`, а `modal.html` печатает `method="{{ method }}"`. Гейт учится
разбирать сигнатуру объемлющего `{% macro %}` и подставлять **литеральное умолчание параметра**:
для `modal.html:92` это `method="post"`, записанное двойными кавычками **специально для грепа**
(комментарий `:81-83` это прямо объясняет). Разборщик завести **именованной функцией**
`_macro_default_value(source, parameter) -> str | None`: она возвращает умолчание, только если оно
литерал; иначе место считается нарушением как прежде. Перечня исключений эта резолюция **не
заводит** — она обобщается и не требует ручного сопровождения. В докстринге правила записать
проверенный факт: ни один из вызовов `modal()` в дереве не передаёт метода, отличного от `post`
(семь передают `method="post"` явно, остальные берут умолчание).

⚠️ **γ1 — правило 2 §«Форма ключа параметрической записи» (план 09-01), и второго экземпляра
механизма не заводится.** Область его применения ограничена **намеренно**: γ1 годится там, где
переопределения вызывающим не бывает (`method` в `modal()` никем не переопределяется), и НЕ
годится там, где оно бывает — на `hx-disabled-elt` формы-обёртки, где значение действительно
приходит от вызывающего. Там действует правило 3 (поле `callers` + сканер `_macro_callers`,
заведён планом 09-01, задача 3). Основание выбора выписано в §«Форма ключа…» и здесь **не
пересматривается**: правило, зеленеющее по умолчанию макроса, молча одобрило бы вызывающего,
передавшего селектор, накрывающий кнопку Отмены.

**3. `app/templates/account_groups/includes/group_row.html`.** Вызов `modal(...)` (`:104-109`)
получает `hx_post=true`. Больше в файле не меняется **ничего**: форма-триггер удаления
`:78-88` остаётся нетронутой (D-03), панель остаётся снаружи строки, комментарии `:90-103`
сохраняются.

**4. `app/templates/account_groups/partials/delete_response.html` (NEW).** Плоское тело, три
узла верхнего уровня, признак `hx-swap-oob` объявлен **безусловно**:
- `<div id="group-row-{{ group_id }}" hx-swap-oob="delete"></div>` — снятие строки;
- `<div id="group-del-{{ group_id }}" hx-swap-oob="delete"></div>` — снятие осиротевшей панели (D-02);
- `{% include "account_groups/partials/count_rule_oob.html" %}` — счётчик, **тот же** источник,
  что у ответа тумблера (один источник, а не вторая копия: от этого зависит `OOB_BLOCKS = 9`, а
  не 10).
Общей обёртки нет: `allowNestedOobSwaps: false` молча снимает признак с вложенного узла.
Шапка переносит по смыслу комментарий `account_groups.py:374-376`: ответ **ОДИНАКОВ** для
найденной и не найденной группы, по несуществующему `id` внеполосное снятие не делает ничего,
и потому повторный запрос остаётся безвредным и не сообщает, какие идентификаторы заняты чужими
группами. Шапка обязана назвать и вторую цену — без узла снятия панели после N удалений в
документе копятся N панелей `role="dialog"` с живыми ловушками фокуса, потому что панель
сознательно стоит СНАРУЖИ удаляемой строки.

**5. `app/pages/account_groups.py::account_groups_delete`.**
- Тройной `WHERE` и его комментарий (`:344-349`), `ScheduleRepository(db).remove_group_ids`
  и её комментарий (`:360-371`), комментарий «Ответ ОДИНАКОВ…» (`:374-376`) сохраняются
  **дословно**.
- `:347` `RedirectResponse(url="/login", status_code=302)` → `return await respond(request,
  redirect="/login")` (G-2 смотрит на **всё** тело функции).
- Финальный `RedirectResponse` `:377` заменяется ветвлением по **остатку строк**:
  - после `commit` посчитать `active_groups, total_groups = await _group_counts(db, user.id, account_id)`;
  - `total_groups == 0` → `return await respond(request, redirect=f"/accounts/{account_id}/groups")`
    — фрагмента нет, слой отвечает 302 без htmx и 204 + `HX-Location` с htmx (D-09). Второй
    отрисовки пустого состояния не заводится: три различимых ветки живут в `list.html` в одном
    экземпляре;
  - иначе → нульарная асинхронная `_fragment`, рендерящая
    `"account_groups/partials/delete_response.html"` с `group_id`, `active_groups`,
    `total_groups`, и `return await respond(request, redirect=f"/accounts/{account_id}/groups",
    fragment=_fragment)`;
  - `notice` **не передаётся** (D-10).
  ⚠️ Ветвление считается по `group_id` **пути**, а не по найденной строке: тело ответа обязано
  быть одинаковым для найденной и не найденной группы (D-04). Для не найденной группы
  `total_groups` считается тем же `_group_counts` и ветка «пусто/не пусто» получается той же —
  различимого признака не появляется.

**6. Инвентарные числа и их летописи** (каждое — вместе с правкой комментария):
`HX_POST_PLACES` 2 → **3** (`ads/form.html`, `components/form_wrapper.html`,
`components/modal.html`); `OOB_BLOCKS` 7 → **9** (+снятие строки, +снятие панели);
`MACRO_DEFINITION_SITES_DECLARED` 1 → **2** с записью `"components/modal.html"` → обоснование,
называющее, что адрес подтверждения — параметр макроса, что G-4 здесь по-прежнему применяется и
зелен, и что перечень **обязан опустеть в Фазе 10**, когда проверка адресов переедет на
вызывающих (FORM-06); `NOT_YET_CONVERTED_COUNT` 35 → **34** со снятием
`account_groups_delete` — **движение счётчика прогресса вехи**; `HX_TARGETS` = 1,
`CLIENT_STATE_NODES` = 24, `FRAGMENT_ROUTES_DECLARED` = 12, `HX_HEADER_WRITES` = 2,
`POST_HANDLERS` = 36 — **не двигать** и подтвердить прогоном.
Литерал `len(paths)` в `test_both_branches_of_the_editor_action_are_extracted` (`:909`)
**пересчитать исполнением повторно** — теперь мест определения макросов два, и множество
извлечённых путей обязано быть измерено, а не предположено.

**7. `tests/test_pages/test_account_groups.py`** — пары и случаи из `<behavior>` по SP-3;
`test_repeated_delete_is_harmless` **сохраняется дословно** и получает вторую половину под
`htmx_client`, утверждающую побайтовое равенство ответов на первое и повторное удаление.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_pages/test_account_groups.py -k "delete_degrades_without_htmx or delete_returns_oob_nodes or last_group_goes_to_location or repeated_delete_is_harmless" -x` — зелено.
- `uv run pytest tests/test_pages/test_account_groups.py -k "delete_does_not_trust" -x` — зелено (обе половины пары).
- `uv run pytest tests/test_pages/test_htmx_gates.py -k "own_redirect or backlog" -x` — зелено;
  `grep -c 'RedirectResponse' app/pages/account_groups.py` не содержит вхождений внутри тел
  `account_groups_toggle` и `account_groups_delete`.
- `grep -v '^#' tests/test_pages/test_htmx_gates.py | grep -c 'NOT_YET_CONVERTED_COUNT = 34'` возвращает `1`;
  `grep -c 'account_groups_delete' tests/test_pages/test_htmx_gates.py` возвращает `0`.
- `grep -v '^#' tests/test_templates/test_htmx_markup_gates.py | grep -c 'HX_POST_PLACES = 3'` возвращает `1`;
  то же для `OOB_BLOCKS = 9` и `MACRO_DEFINITION_SITES_DECLARED = 2`.
- Умолчание `modal()` не сдвинулось: рендер `modal(**MODAL_ARGS)` без `hx_post` даёт разметку,
  посимвольно равную эталону, снятому до правки (эталон сохранить временным файлом и сверить
  `diff`, файл не коммитить); в отрендеренной разметке отсутствуют подстроки `hx-post`,
  `hx-swap`, `hx-disabled-elt`, `hx-indicator`, `form-busy`.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q` — зелено целиком, включая
  G-3, G-4, оба правила адреса и всю ГРУППУ КОНТРОЛЯ.
- `grep -c 'hx-swap-oob="delete"' app/templates/account_groups/partials/delete_response.html` возвращает `2`.
- `grep -c '<div' app/templates/account_groups/partials/delete_response.html` возвращает `2` —
  обёртки над OOB-узлами нет.
  </acceptance_criteria>

  <done>
Удаление группы идёт через htmx: `hx-swap="none"`, ответ несёт снятие строки, снятие панели и
счётчик тремя внеполосными узлами верхнего уровня; удаление последней группы отвечает 204 +
`HX-Location`; без `HX-Request` — прежний 302; ответ найденной и не найденной группы совпадает
побайтово. Умолчание `modal()` не сдвинулось ни на символ. Четыре числа сдвинуты, пять
подтверждены неподвижными.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 2: Вопрос G-11 закрывается ИЗМЕРЕНИЕМ, а не допущением</name>

  <files>tests/test_templates/test_htmx_markup_gates.py</files>

  <read_first>
- `tests/test_templates/test_htmx_markup_gates.py` — `:1009-1023` (`_id_occurrences` —
  шаблонизированные `id` из счёта исключены), `:1025-1043` (`_oob_target` — **фильтра `{{`
  здесь НЕТ**, шаблонизированный `id` тега в множество целей попадает), `:1045-1062`
  (`_swap_target_ids` — берёт только значения, начинающиеся с `#`, фильтра `{{` тоже нет),
  `:1104-1133` (`_offenders_target_does_not_exist` — фильтр `if "{{" in value or "{%" in value:
  continue` есть, и `_offenders_id_in_two_roles`), `:1190-1206`
  (`test_every_swap_target_points_at_an_existing_id`), `:1262-1280`
  (`test_no_id_is_both_a_swap_target_and_an_oob_target` и его докстринг: намерение — «две
  подмены **того же ответа**»), `:1362-1380` (G-12), `:1553-1566` (готовый контроль двух ролей —
  **не правится**).
- `app/templates/components/form_wrapper.html` — печатает `hx-target="{{ target }}"`.
- `app/templates/account_groups/partials/delete_response.html` — узел с
  `id="group-row-{{ group_id }}"` и `hx-swap-oob="delete"`.
- `09-RESEARCH.md` §1 целиком — почему форма (a) «в пределах одного ответа» не вычислима и
  почему рекомендована (b); §1.3 — три обязательных утверждения, третье несущее.
- `09-CONTEXT.md` §Landmines, первый пункт — «разрешать ОБЯЗАТЕЛЬНО осознанно: снятие правила
  целиком вернуло бы дыру, ради которой оно писалось».
- **`09-01-PLAN.md` §«Форма ключа параметрической записи» в `<objective>` — читать ДО первой
  строки кода.** Ключ, γ1 и поле `callers` заданы там; здесь они только применяются.
- `tests/test_templates/test_htmx_markup_gates.py` — `_macro_callers` (заведён планом 09-01,
  задача 3): единственный на фазу сканер вызывающих, и второго заводить нельзя; `:1010-1023`
  и `RELATIVE_TARGET_PREFIXES` (`:1000-1006`) — три вида значения цели, по которым правило ниже
  разбивает МЕСТА на разряды.
- `app/templates/account_groups/includes/group_row.html` — вызывающий, передающий
  `target='#group-row-' ~ group.id`; ⚠️ сам он `hx-target` **не несёт** и в `_target_sites` не
  входит, поэтому дойти до него можно только объявленным полем.
- `09-PATTERNS.md` §SP-1, §SP-2.
  </read_first>

  <action>
⚠️ **Задача начинается с измерения, а не с правки.** Разведка (§1.1) предсказывала, что строка
`group-row-{{ group.id }}` попадёт в **оба** множества и G-11 покраснеет. Это предсказание
сделано при допущении, что `hx-target` записан литерально в шаблоне. В выбранной форме фазы
`hx-target` печатает **макрос-обёртка** значением `{{ target }}`, а `_swap_target_ids`
(`:1045-1062`) кладёт в множество только остаток значения, начинающегося с `#`. Поэтому
пересечение обязано быть **вычислено на итоговом дереве**, а результат — решить, нужен ли ВТОРОЙ
перечень (шаг 3). Первый перечень от измерения не зависит и объявляется безусловно (шаг 2).

Шаг 1. Исполнить разборщики самого гейта на настоящем дереве и записать вывод в сводку плана.
Команда — однострочный `uv run python -c`, добавляющий `tests/test_templates` в `sys.path`,
импортирующий из модуля `test_htmx_markup_gates` функции `_all_templates`, `_swap_target_ids` и
`_oob_target_ids`, и печатающий три отсортированных множества: цели подмены, внеполосные цели и
их **пересечение**. Готовая форма команды — в `<acceptance_criteria>` ниже.

⚠️ **Ветвления «либо один перечень, либо другой» больше нет, и это исправление.** Прежняя форма
задачи делала существование `PARAMETRIC_SWAP_TARGETS` условным, а от него зависит правило D-08
плана 09-03 — то есть план волны 3 опирался бы на результат измерения волны 2. Разведены две
разные вещи:
- **факт «цель печатается параметром макроса, а фактическую цель передаёт вызывающий»** — верен
  независимо от любого пересечения и объявляется **БЕЗУСЛОВНО** (шаг 2);
- **факт «один `id` оказался в двух ролях»** — вычисляется измерением и решает только,
  нужен ли ВТОРОЙ перечень (шаг 3).

Шаг 2 (**безусловно**). Первая цель подмены вехи для гейта **невидима**: `_swap_target_ids`
(`:1045-1062`) кладёт в множество только остаток значения, начинающегося с `#`, а обёртка печатает
`hx-target="{{ target }}"`. Значит цель не проверяют ни G-9 (существование), ни G-11 (две роли),
ни G-12 (граница клиентского состояния). Невидимость **объявляется и считается**, по идиоме SP-1
и по §«Форма ключа параметрической записи» плана 09-01.

Форма записи — `NamedTuple` по имени `ParametricTarget` с тремя полями: `macro` типа `str` (имя
макроса, печатающего цель параметром), `callers` типа `tuple[str, ...]` (шаблоны, зовущие этот
макрос с параметром цели) и `reason` типа `str`. Голый `dict[str, str]` не годится: вызывающие —
проверяемый факт и предмет правила D-08 плана 09-03, а не проза.

- `PARAMETRIC_SWAP_TARGETS: dict[str, ParametricTarget]` — **ключ — файл, который возвращает
  разборщик**, то есть `"components/form_wrapper.html"`, а не место вызова. Запись:
  `macro="form_wrapper"`, `callers=("account_groups/includes/group_row.html",)`, и обоснование:
  цель приходит **параметром** макроса, поэтому в множество литеральных целей не попадает;
  проверка конкретной цели переезжает на ВЫЗЫВАЮЩИХ (Фаза 10, FORM-06); фактическая цель формы
  тумблера — `#group-row-{{ group.id }}`, а внеполосное снятие той же строки живёт в ответе
  удаления, и это РАЗНЫЕ ответы (D-11).
- `PARAMETRIC_SWAP_TARGETS_DECLARED = 1` — отдельной константой с ⚠️-комментарием.
- `test_every_parametric_swap_target_carries_a_reason` — обоснование непусто.
- `test_the_number_of_parametric_swap_targets_is_the_declared_one` — число утверждается.
- **`test_every_declared_parametric_target_is_actually_parametric`** — у каждой записи значение
  `hx-target` в этом файле действительно содержит `{{`; запись, ставшая литеральной, обязана
  краснеть (аналог третьего, несущего утверждения §1.3).
- **`test_every_declared_parametric_caller_actually_calls_the_macro`** — объявленное множество
  `callers` обязано **совпасть** с `_macro_callers(templates, entry.macro, "target")` (сканер
  заведён планом 09-01, задача 3; второго не заводить). Забытый вызывающий краснеет, лишний
  краснеет. Без этого теста поле `callers` было бы прозой, а правило D-08 плана 09-03 опиралось
  бы на непроверенный список.
- **`test_every_swap_target_is_either_literal_or_declared_parametric`** — несущее правило,
  закрывающее дыру. ⚠️ **Арифметика считает МЕСТА с обеих сторон.** Прежняя форма
  (`len(_target_sites) == len(_swap_target_ids) + len(PARAMETRIC_SWAP_TARGETS)`) складывала
  разные единицы: слева места (список), справа идентификаторы (множество) и файлы (ключи
  словаря). Две цели на один и тот же литеральный `id` или два параметрических места в одном
  файле дали бы ложный красный; на этой фазе равенство сходится (1 == 0 + 1) случайно, а писано
  правило для Фаз 10-15. Верная форма: каждое место из `_target_sites` разложить по значению
  ровно в **один** разряд — литеральное (значение начинается с `#`), относительное (значение
  начинается с одного из `RELATIVE_TARGET_PREFIXES`), параметрическое (значение содержит `{{`
  или `{%`); утверждать, что сумма трёх разрядов равна числу мест (незаявленная форма значения
  обязана быть замечена, а не отнесена к остатку), и что множество файлов параметрического
  разряда **вложено** в ключи `PARAMETRIC_SWAP_TARGETS`.
  Границу назвать докстрингом честно: ключ — файл, поэтому два параметрических места в одном
  файле покрываются одной записью; Фаза 10, если заведёт второе, обязана эту границу пересмотреть.
- **`_swap_declaring_templates(templates) -> set[str]`** — помощник объявляется **здесь, рядом с
  перечнем**, а не в плане 09-03: множество шаблонов, объявляющих цель подмены, есть
  `{site.template for site in _target_sites(templates)}` **плюс** все `callers` всех записей
  `PARAMETRIC_SWAP_TARGETS`. Единственный потребитель — правило D-08 плана 09-03, и оно вызывает
  эту функцию, а не пересобирает множество по собственному описанию: два описания одного множества
  разошлись бы молча. Докстринг обязан назвать, ЧТО без второго слагаемого множество сводится к
  `components/form_wrapper.html` — файлу, в котором `x-data` нет вовсе, — и правило D-08 стало бы
  зелёным по построению.
- `test_no_id_is_both_a_swap_target_and_an_oob_target` **не трогается вовсе**: правило остаётся
  действующим для всех литеральных идентификаторов, и его контроль `:1553` продолжает краснеть
  на `#ad-preview`.
**Запрещено:** добавлять в `_swap_target_ids` фильтр `if "{{" in value: continue` — это сняло бы
правило со ВСЕХ построчных целей вехи разом, то есть с того самого класса, которым Фазы 10-15
наполнят проект.

Шаг 3 (**условно, по результату шага 1**).
- **Пересечение НЕ пусто** — реализовать рекомендацию (b) RESEARCH §1.3 в точности:
  `ID_IN_TWO_ROLES_BY_DESIGN: dict[str, str]` с **измеренными** ключами и обоснованием на запись
  (цель подмены у формы тумблера и внеполосная цель СНЯТИЯ в ответе удаления — это РАЗНЫЕ ответы,
  а не две подмены одного (D-11); двойной подмены за один ответ здесь нет и быть не может, потому
  что ответ удаления идёт `hx-swap="none"` и основного свопа не имеет вовсе);
  `ID_IN_TWO_ROLES_DECLARED` = измеренное число, отдельной константой с ⚠️-комментарием; три
  теста: `test_every_two_role_id_carries_a_reason`,
  `test_the_number_of_two_role_ids_is_the_declared_one` и несущий
  **`test_every_declared_two_role_id_is_actually_in_conflict`** — каждая запись обязана
  ФАКТИЧЕСКИ лежать в пересечении, иначе устаревшая запись живёт вечно и молча гасит будущее
  настоящее столкновение с тем же `id`; `_offenders_id_in_two_roles` вычитает перечень.
- **Пересечение ПУСТО** — второго перечня **не заводить**: его третье утверждение было бы ложью,
  запись не лежала бы в пересечении. Пустой результат — тоже результат: записать его в сводку с
  основанием («цель формы тумблера параметрическая и в `_swap_target_ids` не попадает, поэтому
  столкновения с внеполосной целью снятия строки не возникает»), а не умолчать.

Шаг 4. Контроль по SP-2 — **безусловно**:
`test_control_negative_an_undeclared_parametric_target_reddens_the_gate`: подстановка добавляет
второй параметрический `hx-target` в шаблон, не входящий в `PARAMETRIC_SWAP_TARGETS` ⇒ несущее
правило краснеет. Текст отказа называет ПОСЛЕДСТВИЕ: цель подмены вошла в проект, и ни одно
правило её не проверяет. Для ветви шага 3 отдельного контроля не требуется — существующий
`:1553` покрывает правило двух ролей.

Шаг 5. В сводке плана записать **измеренные множества целиком** и назвать, заведён ли второй
перечень, с основанием. Формулировка «G-11 не покраснел» без приведённых множеств недопустима:
она неотличима от «обход целей не находит».
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py -q</automated>
  </verify>

  <acceptance_criteria>
- Команда измерения из шага 1 выполнена, её вывод приведён в `09-02-SUMMARY.md` дословно.
  Готовая форма (одна строка):
  `uv run python -c "import sys; sys.path.insert(0,'tests/test_templates'); from test_htmx_markup_gates import _all_templates, _swap_target_ids, _oob_target_ids; t=_all_templates(); print(sorted(_swap_target_ids(t))); print(sorted(_oob_target_ids(t))); print(sorted(_swap_target_ids(t) & _oob_target_ids(t)))"`
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "parametric" -q` — собрано
  **не менее пяти** тестов, все зелёные.
- `grep -v '^#' tests/test_templates/test_htmx_markup_gates.py | grep -c 'PARAMETRIC_SWAP_TARGETS_DECLARED = 1'` возвращает `1`.
- Ключ, поле `callers` и его согласие с измерением — привязано к значению записи, а не к прозе
  где-то в модуле:
  `uv run python -c "import sys; sys.path.insert(0,'tests/test_templates'); from test_htmx_markup_gates import PARAMETRIC_SWAP_TARGETS as P, _all_templates, _macro_callers, _swap_declaring_templates; e=P['components/form_wrapper.html']; assert e.macro=='form_wrapper' and e.callers==('account_groups/includes/group_row.html',), e; t=_all_templates(); assert set(e.callers)==set(_macro_callers(t,e.macro,'target')); assert 'account_groups/includes/group_row.html' in _swap_declaring_templates(t)"`
- ⚠️ Последняя строка предыдущей проверки — **предусловие правила D-08 плана 09-03**: если
  `group_row.html` не попал в `_swap_declaring_templates`, правило D-08 зеленеет по построению, и
  его контроль не покраснеет ни на чём. Проверять здесь, а не там.
- Несущее правило считает МЕСТА, а не идентификаторы, и это проверяется **положительно**
  (отрицательный греп по прежней форме краснел бы на докстринге, который сам же и требуется):
  в теле `test_every_swap_target_is_either_literal_or_declared_parametric` встречаются
  `_target_sites` и `RELATIVE_TARGET_PREFIXES` — то есть места разложены по трём разрядам, а не
  сложены с множеством идентификаторов.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "id_is_both_a_swap_target" -x` — зелено.
- `grep -c 'if "{{" in value: continue' tests/test_templates/test_htmx_markup_gates.py` внутри тела
  `_swap_target_ids` возвращает `0` — правило не снято фильтром.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "control" -q` — зелено; число
  собранных контролей выросло ровно на 1, и `test_control_negative_one_id_in_two_roles_reddens_the_gate`
  среди них не правился.
- Второй перечень заведён **тогда и только тогда**, когда измерение шага 1 дало непустое
  пересечение; в сводке приведены и множества, и решение. При пустом пересечении
  `grep -c 'ID_IN_TWO_ROLES_DECLARED' tests/test_templates/test_htmx_markup_gates.py` возвращает `0`.
  </acceptance_criteria>

  <done>
Параметрическая цель объявлена безусловно, ключом по файлу разборщика и полем `callers`, которое
утверждено против измерения `_macro_callers`; `_swap_declaring_templates` объявлен рядом с
перечнем и является единственным описанием множества «шаблоны, объявляющие цель подмены» —
план 09-03 его **вызывает**, а не пересобирает. Несущее правило считает места, а не
идентификаторы, и потому переживёт Фазы 10-15. Столкновение G-11 закрыто измеренным фактом:
множества приведены в сводке, второй перечень заведён либо не заведён по результату, и в обоих
случаях решение записано с основанием. Правило G-11 для литеральных идентификаторов не ослаблено
ничем.
  </done>
</task>

<task type="execute" tdd="true">
  <name>Задача 3: Гейт «кнопка Отмены не блокируется никогда»</name>

  <files>tests/test_templates/test_htmx_markup_gates.py</files>

  <read_first>
- `app/templates/components/modal.html:54-64` — «Кнопка ОТКАЗА не блокируется ничем: отмена
  разрушительного действия не имеет права быть труднее его подтверждения»; `:117` — кнопка
  подтверждения `type="submit"` с `x-bind:disabled="sending"`, кнопка отказа выше —
  `type="button"` с `x-ref="cancel"`.
- `tests/test_templates/test_htmx_markup_gates.py` — `:221-252` (`_attr_value`), `:1140-1166`
  (G-7 как образец правила «на каждом месте отправки объявлено …»), `:1383-1434` (ГРУППА
  КОНТРОЛЯ), `DISABLED_ELT_EXCEPTIONS` (заведён планом 09-01, задача 3).
- `.planning/REQUIREMENTS.md` §QUAL-01 — «кнопка Отмены в панели подтверждения не блокируется
  никогда» — записанное решение v2.0.
  </read_first>

  <action>
Правило `test_cancel_is_never_disabled`: ни один селектор блокировки, действующий на дереве, не
имеет права попасть в кнопку отказа. Проверяемая форма конкретна и не требует разбора CSS —
проверяются **три** множества селекторов, и третье существует именно потому, что макрос-обёртка
прячет вызывающего:
1. **Литеральные значения на местах `hx-post`** — обязаны быть `find button[type=submit]` (кнопка
   отказа имеет `type="button"` и под него не подпадает).
2. **Параметрические значения** — место обязано быть записью `DISABLED_ELT_EXCEPTIONS` (заведён
   планом 09-01, задача 3; форма `DisabledEltException`, поля `callers` и `reason`).
3. ⚠️ **Значения, ПЕРЕДАВАЕМЫЕ вызывающими** — единственное место фазы, где значение реально
   отличается от общего правила, и файл-носитель `hx-post` о нём не знает. Для каждой записи
   перечня и каждого её `callers` выбрать из исходника вызывающего значение, переданное
   параметром `disabled_elt`, и утверждать **два** факта: (а) это значение **дословно** названо в
   `reason` записи (иначе селектор действует, а решения о нём никто не записывал); (б) селектор
   не может накрыть кнопку отказа — он либо не упоминает `button` вовсе (случай
   `find input[type=checkbox]`), либо ограничен `[type=submit]`. Сегодняшний вызывающий один
   (`account_groups/includes/group_row.html`), и его множество берётся из объявленного поля
   `callers`, уже сверенного с `_macro_callers` планом 09-02, задача 2 — второго сканера не
   заводить.

Дополнительно и независимо: в `components/modal.html` кнопка с `x-ref="cancel"` обязана нести
`type="button"` — снятие этого атрибута делает её submit-кнопкой и, значит, целью блокировки;
правило утверждает наличие подстроки в файле.

Докстринг называет цену: заблокированная кнопка отказа делает отмену разрушительного действия
труднее его подтверждения, а при отказе сети — вовсе невозможной, потому что снятие блокировки
приезжает только с ответом.

Контроль по SP-2 — `test_control_negative_a_disabled_cancel_reddens_the_gate`: подстановка
меняет `type="button"` у кнопки с `x-ref="cancel"` на `type="submit"` ⇒ правило краснеет.
Текст отказа называет последствие, а не факт.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "cancel_is_never_disabled or control" -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "cancel_is_never_disabled" -x` — зелено.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k "control" -q` — зелено; число
  собранных контролей выросло ровно на 1.
- Временная проверка зубов, выполняемая и откатываемая исполнителем: заменить `type="button"` на
  `type="submit"` у кнопки `x-ref="cancel"` в `components/modal.html` ⇒
  `uv run pytest tests/test_templates/test_htmx_markup_gates.py -k cancel` краснеет; откатить.
- `uv run pytest tests/test_templates/ tests/test_pages/ -q` — зелено (слияние волны).
  </acceptance_criteria>

  <done>
Буква QUAL-01 «кнопка Отмены не блокируется никогда» стала машинным правилом с доказанными
зубами, а не комментарием в докстринге макроса.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | `account_id` и `group_id` приходят сегментами URL от недоверенного клиента |
| вызывающий `modal()` → имя атрибута Alpine | `id` панели подставляется в **ИМЯ** атрибута `x-on:modal-open-{{ id }}.window`, а имена атрибутов не экранируются ничем |
| ответ удаления → документ пользователя | внеполосные узлы снимают элементы по `id`, собранному из сегмента пути |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-07 | Information Disclosure | `account_groups_delete` — повторное или чужое удаление через фрагментный транспорт | high | mitigate | Ответ собирается из `group_id` **пути**, а не из найденной строки, поэтому найденная и не найденная группа дают побайтово одинаковый ответ (D-04). `test_repeated_delete_is_harmless` переезжает дословно и получает вторую половину под `htmx_client`, утверждающую равенство ответов |
| T-9-08 | Tampering | `id` панели подтверждения в имени атрибута Alpine | high | mitigate | Параметр `hx_post` не касается `id` и **не ослабляет** требования к вызывающему; докстринг `modal.html:81-91` сохраняется целиком. Фаза передаёт туда `group.id` — первичный ключ. Гейт компонентов и запрет `|safe`/`Markup(` остаются в силе |
| T-9-10 | Information Disclosure | ветвление «список опустел / не опустел» как побочный канал | medium | mitigate | Ветвление считается по `_group_counts` **аккаунта владельца**, а не по факту нахождения строки: для чужой и несуществующей группы ветка получается той же, что и для последнего успешного удаления в том же аккаунте, и различимого признака не появляется. Утверждается тестом сравнения ответов |
| T-9-11 | Tampering | внеполосное снятие узла по `id` из пути | medium | accept | `hx-swap-oob="delete"` снимает узел только в документе того же пользователя; сервер по несуществующему `id` не делает ничего. Пользователь может снять элемент **собственной** страницы — это не пересечение границы доверия |
| T-9-03 | Tampering | `HX-Location` на опустевший список | high | mitigate | Адрес строит `respond(redirect=…)`; `_local_path` отвергает схему, `//`, `\`, управляющие символы и не-ASCII. Новых мест записи `HX-*` не заводится: `HX_HEADER_WRITES` остаётся 2 |
| T-9-05 | Spoofing | подделка `HX-Request` | low | accept | Вердикт доступа вычисляется ДО развилки транспорта (T-08-09) |
| T-9-SC | Tampering | установки пакетов npm / pip / cargo | low | accept | План не выполняет ни одной установки; `09-RESEARCH.md` §Package Legitimacy Audit — «неприменимо» |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_account_groups.py tests/test_templates/test_htmx_markup_gates.py tests/test_pages/test_htmx_gates.py -q` — зелено после каждой задачи.
- `uv run pytest tests/test_templates/ tests/test_pages/ -q` — зелено на слиянии волны.
- `uv run python -m compileall -q app main.py tests` — без ошибок.
- Известный красный `full-suite-ads-editor-order-pollution` в полном прогоне планом не втягивается.
</verification>

<success_criteria>
1. Удаление идёт через htmx: `hx-swap="none"`, три внеполосных узла верхнего уровня, снятие
   осиротевшей панели включено.
2. Опустевший список отвечает 204 + `HX-Location`; второй отрисовки пустого состояния нет.
3. Ответ найденной и не найденной группы совпадает побайтово.
4. Умолчание `modal()` не изменилось ни на символ; 15 остальных мест подтверждения не тронуты.
5. `HX_POST_PLACES` 3, `OOB_BLOCKS` 9, `MACRO_DEFINITION_SITES_DECLARED` 2,
   `NOT_YET_CONVERTED_COUNT` 34, `PARAMETRIC_SWAP_TARGETS_DECLARED` 1; пять чисел подтверждены
   неподвижными.
6. `PARAMETRIC_SWAP_TARGETS` объявлен безусловно, его `callers` сверены с `_macro_callers`, и
   `_swap_declaring_templates` содержит `account_groups/includes/group_row.html` — предусловие
   правила D-08 плана 09-03 проверено ЗДЕСЬ.
7. Вопрос G-11 закрыт приведёнными в сводке множествами; второй перечень заведён либо не заведён
   по результату, и решение записано с основанием.
8. «Кнопка Отмены не блокируется никогда» — машинное правило с контролем, действующее и на
   значения, передаваемые ВЫЗЫВАЮЩИМИ.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-02-SUMMARY.md` when done.

Сводка обязана привести дословный вывод команды измерения G-11 и назвать
`NOT_YET_CONVERTED_COUNT` 35 → 34 движением счётчика прогресса вехи.
</output>
