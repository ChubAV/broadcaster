---
phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
plan: 18
type: execute
wave: 2
depends_on: ["17"]
gap_closure: true
files_modified:
  - app/pages/account_groups.py
  - app/templates/account_groups/includes/group_row.html
  - app/templates/account_groups/partials/toggle_response.html
  - tests/test_pages/test_account_groups.py
autonomous: true
requirements: [FORM-02, QUAL-01, QUAL-06]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Строка поиска доезжает до ВСЕХ ТРЁХ мест отрисовки формы удаления, а не до двух: страница (`list.html`), порция (`partial_cards.html`) и ОТВЕТ ТУМБЛЕРА (`toggle_response.html`). Сегодня третье место печатает пустое значение — измерено верификатором прогоном: страница с `?search=Альфа` отдаёт два поля со значением `Альфа`, htmx-ответ тумблера той же строки отдаёт одно поле с `value=\"\"` (CR-02)"
    - "Обработчик тумблера ПРИНИМАЕТ строку поиска телом формы и проводит её через тот же `_clean_search`, что и обработчик удаления: сегодня `account_groups_toggle` поля `search` не принимает ВОВСЕ, то есть довести значение до ответа было нечем в принципе"
    - "Форма тумблера несёт скрытое поле строки поиска так же, как его несут обе формы пути удаления: без него величине неоткуда приехать на пути «Alpine мёртв, htmx жив», который фаза назвала поимённо (`account_groups.py:474-480`). Мир этот заведён связкой D-05 (`x-on:change` снят, отправку забрал слой письма) и D-07 (кнопка «Применить» снимается ТОЛЬКО при живом htmx, а не при живом Alpine)"
    - "ОБА выхода обработчика тумблера собирают адрес приземления помощником `_screen_url(account_id, term)`, а не f-строкой: докстринг помощника (`account_groups.py:68-74`) требует единого источника прямым текстом — «адрес после действия и адрес после перезагрузки обязаны приходить из одного источника, иначе они разъедутся молча». Измерено: POST без признака htmx со страницы `?search=Альфа` даёт `302 → Location: /accounts/1/groups` (WR-01)"
    - "Правило равенства наборов полей перестаёт утверждать симметрию ШИРЕ своего окна: сегодня `test_both_delete_forms_post_the_same_field_names` читает РАЗМЕТКУ СТРАНИЦЫ, а мест отрисовки той же формы ТРИ — и правило зелено на сломанном дереве. Это тот же образец «зелено по построению», цену которого фаза оплатила однажды (WARN-4 первого круга)"
    - "Число мест отрисовки формы удаления ОБЪЯВЛЕНО и утверждается отдельным правилом: четвёртое место, заведённое молча, краснеет — иначе окно правила снова уедет уже́ предмета, и следующий круг узнает об этом ревизией после отгрузки"
    - "Починка WR-06 («выдача опустела» решается по ТЕКУЩЕЙ выдаче) начинает работать на переключённой строке: `_current_listing_has_a_row` получает ту строку поиска, которую человек видел, а не `None`; удалив единственную найденную строку, человек попадает на экран «Группы не найдены» с кнопкой «Сбросить», а не на пустую карточку без выхода. Ветка эта — записанное решение D-09 (опустевший список закрывается переходом на тот же адрес, а не вторым экземпляром пустого состояния), и правка её не пересматривает, а доводит до неё недостающую величину"
    - "Неотличимость чужой и несуществующей группы (D-13) НЕ ослабляется переводом выхода на `_screen_url`: обе идут одной веткой и получают ОДИН И ТОТ ЖЕ адрес приземления — тот же, что получила бы найденная"
    - "Плашка `?notice=` по-прежнему не выдаётся ни на успешный тумблер, ни на успешное удаление (D-10): правка величины `redirect=` не превращает ни один выход в уведомление, и исход по-прежнему виден в самой строке и в линейке счётчика, приезжающей постоянной обёрткой с подменой СОДЕРЖИМОГО (D-12)"
    - "Проза строки группы перестаёт описывать механизм, которого у кода нет: абзац `group_row.html:151-154` — ЗАПИСАННОЕ ОБОСНОВАНИЕ снятия клиентской блокировки, и читатель Фазы 10, решающий тот же вопрос для сорока шести оставшихся форм, прочтёт здесь свойство, которого нет. Форма, о которой абзац говорит, снята планом 09-15 (`account_groups.py:510` присваивает показанное состояние), а сам абзац в круге закрытия не менялся ни на символ (WR-02)"
    - "Ложность прозы утверждается ПРАВИЛОМ с антивакуумом и отрицательным контролем, а не единичной правкой: правка без правила чинит одно место и оставляет класс, а именно класс наследуют Фазы 10-15"
    - "Переход цвета КАЖДОГО нового правила ИСПОЛНЕН, а не заявлен: прогон нового правила на ДО-правочном состоянии производственного файла в ОТДЕЛЬНОМ отсоединённом рабочем дереве даёт код РОВНО `1`, и в сводку попадает ВЫВОД команды. Порог именно `1`: код `5` означает, что правило не собралось вовсе, `4` — ошибку в наборе команды, и оба прошли бы порог «ненулевой» вакуумом"
    - "Путь деградации не двинулся ни на одно свойство: `test_toggle_degrades_without_htmx` и `test_delete_degrades_without_htmx` зелены, собственного `RedirectResponse` у переведённых обработчиков не появляется (гейт G-2), `NOT_YET_CONVERTED_COUNT` не трогается"
    - "QUAL-01, ребро `idempotency`: присвоение присланного состояния остаётся идемпотентным по построению — параметр `search` состояния не касается, и повторный тот же запрос по-прежнему не создаёт второй записи и не двигает состояния"
    - statement: "QUAL-01, ребро `concurrency`: два переключения одной строки под тройным `WHERE` не создают второй записи, и последний записавший выигрывает; параллельного исполнения суита не поднимает, и утверждать шире измеренного нельзя"
      verification: backstop
    - statement: "QUAL-06: фокус после подмены строки остаётся на элементе с тем же `id` — скрытое поле в форме тумблера идентификатора тумблера не трогает; разметочная половина закрыта `test_toggle_fragment_keeps_the_toggle_id`, поведенческая — глазом человека в плане 09-20"
      verification: backstop
  artifacts:
    - "app/pages/account_groups.py — `account_groups_toggle` со `search: str | None = Form(None)`, проведённым через `_clean_search`, и ОБА его выхода на `_screen_url(account_id, term)`"
    - "app/templates/account_groups/partials/toggle_response.html — вызов `group_row(...)` с `filter_search`, и `filter_search` в перечне ожидаемых величин шапки"
    - "app/templates/account_groups/includes/group_row.html — скрытое поле строки поиска в форме тумблера и правдивый абзац обоснования снятия клиентской блокировки"
    - "tests/test_pages/test_account_groups.py — правило третьего места отрисовки, поведенческое правило приземления с тела ответа тумблера, правило адреса приземления тумблера, объявленное число мест отрисовки и правило прозы о механизме тумблера с отрицательным контролем"
  key_links:
    - "форма тумблера → обработчик тумблера → ответ тумблера → форма удаления в ответе: рвётся — переключённая строка теряет строку поиска, и путь без Alpine уезжает на НЕотфильтрованный список"
    - "строка поиска → `_current_listing_has_a_row`: рвётся — вопрос задаётся про ВЕСЬ аккаунт вместо той выдачи, которую человек видел, и починка WR-06 на этом пути не срабатывает в принципе"
    - "`_screen_url` → оба выхода тумблера: рвётся — адрес после действия и адрес после перезагрузки расходятся молча, ровно как запрещает докстринг помощника"
    - "проза `group_row.html` → читатель Фазы 10: рвётся — записанное обоснование снятия клиентской блокировки уезжает на сорок шесть форм, описывая механизм, которого нет"
  # ⚠️ ФОРМА ЗАПИСЕЙ НИЖЕ — ЗАФИКСИРОВАННЫЙ ОБРАЗЕЦ ПРОЕКТА, А НЕ ДРЕЙФ ЭТОГО КРУГА.
  # Дескрипторы `status: flagged-unverified` и `verification: none` набираются так с прошлых
  # кругов фазы, и ПРИЁМКА ЧИТАЕТ ИХ ИМЕННО В ЭТОЙ ФОРМЕ. Descriptor-less форма общей
  # спецификации здесь СОЗНАТЕЛЬНО не применяется: разнобой между кругами хуже любой из двух
  # форм. Все четыре плана третьего круга (09-13 … 09-16) набраны ОДИНАКОВО, и четвёртый круг
  # (09-17 … 09-20) продолжает ту же форму.
  prohibitions:
    - statement: "серверная идемпотентность и защита от двойного нажатия не ослабляются: `hx-sync='this:drop'` в вызове макроса-обёртки остаётся, пустая цель блокировки остаётся, тройной WHERE обоих обработчиков не трогается"
      status: flagged-unverified
      verification: none
    - statement: "форма-триггер удаления и форма тумблера не оборачиваются макросом-обёрткой сверх того, чем обёрнуты сегодня, и признака htmx-отправки форма-триггер не получает: решение D-03 не пересматривается"
      status: flagged-unverified
      verification: none
    - statement: "собственного RedirectResponse ни у одного переведённого обработчика не появляется ни в одной ветке: гейт G-2 не ослабляется, NOT_YET_CONVERTED_COUNT не трогается"
      status: flagged-unverified
      verification: none
    - statement: "неотличимость чужой и несуществующей группы (D-13, D-04-A) не ослабляется: обе ветки получают один и тот же адрес приземления и один и тот же состав ответа"
      status: flagged-unverified
      verification: none
    - statement: "контракт курсора keyset плана 09-13 не трогается: after_id, page_size, сигнатура макроса сентинела и тройной WHERE маршрута порции остаются прежними"
      status: flagged-unverified
      verification: none
    - statement: "запись OOB_TARGET_EXCEPTIONS плана 09-14 и её объявленное число не двигаются"
      status: flagged-unverified
      verification: none
    - statement: "отступление DISABLED_ELT_EXCEPTIONS, принятое владельцем 2026-09-01 ветвью accept-override, не пересматривается и не расширяется"
      status: flagged-unverified
      verification: none
    - statement: "имя test_both_delete_forms_post_the_same_field_names не меняется: команда свидетельств .planning/REQUIREMENTS.md выбирает его подстрокой, и переименование молча вывело бы правило из выборки"
      status: flagged-unverified
      verification: none
    - statement: "DEF-09-02 и DEF-09-01 не чинятся: первый — рычаг Фазы 10, второй — предсуществующий дефект вне фазы"
      status: flagged-unverified
      verification: none
    - statement: "планы 09-01 … 09-17, их сводки и 09-VERIFICATION.md не правятся, не перенумеровываются и не отменяются"
      status: flagged-unverified
      verification: none
    - statement: "состояния требований и флажки .planning/REQUIREMENTS.md этим планом не переводятся вовсе"
      status: flagged-unverified
      verification: none
    - statement: "долговых маркеров в артефактах не появляется"
      status: flagged-unverified
      verification: none
---

<objective>
Закрыть **CR-02** — блокер, отменивший починку WR-03 плана 09-15 двумя коммитами позже, — и два
следствия того же корня: **WR-01** (адрес приземления тумблера собран литералом и теряет фильтр) и
**WR-02** (проза строки утверждает механизм, которого у обработчика больше нет).

Purpose: **ИЗМЕРЕНО ПРОГОНОМ, А НЕ ВЫВЕДЕНО ЧТЕНИЕМ.** Посев: аккаунт, две группы, страница
запрошена с `?search=Альфа`. Страница отдаёт два скрытых поля `search` со значением `Альфа` — то
есть починка WR-03 на первичной отрисовке настоящая. Затем htmx-переключение той же строки
(`POST /accounts/1/groups/3/toggle`, `HX-Request: true`) отдаёт 200, и в ответе скрытое поле ровно
одно и ПУСТОЕ: `value=""`.

**Причина в исходнике на трёх строках.** `toggle_response.html:29` зовёт `group_row(...)` без
`filter_search`; умолчание параметра — пустая строка (`group_row.html:53`), и она печатается в
скрытое поле формы-триггера (`group_row.html:203`); обработчик `account_groups_toggle`
(`account_groups.py:416-423`) поля `search` не принимает ВОВСЕ, то есть передать его и не мог бы.
Для сравнения `account_groups_delete` (`:551`) его принимает.

**Мир достижим и назван САМОЙ ФАЗОЙ поимённо** — «Alpine мёртв, htmx жив» (зеркало обоснования
WR-02, `account_groups.py:474-480`): при мёртвом Alpine `x-on:submit.prevent` не навешивается,
форма-триггер уходит обычным POST-ом с `search=""`, и дальше `term = None` даёт (1) `_screen_url`
(`:696`) без фильтра — человек приземляется на НЕотфильтрованный список; (2)
`_current_listing_has_a_row` (`:698`) спрашивает про ВЕСЬ аккаунт вместо той выдачи, которую человек
видел, — то есть починка WR-06 на этом пути сработать не может в принципе, и, удалив единственную
найденную строку, человек остаётся перед пустой карточкой без выхода.

**ПРАВИЛО ЭТОГО НЕ ЛОВИТ, И ЭТО ПРОВЕРЯЕМО.**
`test_both_delete_forms_post_the_same_field_names` и
`test_the_trigger_form_body_lands_on_the_filtered_listing`
(`tests/test_pages/test_account_groups.py:5428-5476`) читают РАЗМЕТКУ СТРАНИЦЫ; ТРЕТЬЕГО места
отрисовки той же формы — ответа тумблера — в их предмет не входит. Верификатор перепроверил, что
оба РЕАЛЬНО краснели на своём RED-коммите `c38b3cd` (2 failed) и зелены сейчас: правило настоящее,
но его окно уже́ предмета. То есть на дереве живёт зелёное правило, утверждающее симметрию, которой
нет, — тот самый образец «зелено по построению», цену которого фаза оплатила однажды.

Output: строка поиска, доезжающая до всех трёх мест отрисовки; оба выхода тумблера на едином
источнике адреса; объявленное число мест отрисовки с правилом, у которого есть зубы; правдивая
проза с правилом и отрицательным контролем.

⚠️ **ПОЧЕМУ ИМЯ СУЩЕСТВУЮЩЕГО ПРАВИЛА НЕ МЕНЯЕТСЯ, ХОТЯ ЕГО ПРЕДМЕТ РАСШИРЯЕТСЯ.** Список `missing`
отчёта просит «расширить `test_both_delete_forms_post_the_same_field_names` на ТРЕТЬЕ место
отрисовки». Расширение исполняется ПАРОЙ: существующее правило остаётся правилом СТРАНИЦЫ (его имя
выбирается подстрокой командой свидетельств в `.planning/REQUIREMENTS.md`, и переименование молча
вывело бы правило из выборки — то есть закрыло бы дефект удалением записи о нём), а третье место
получает собственное правило, объявленное число мест отрисовки и перекрёстную ссылку в докстрингах
обоих. Предмет накрыт целиком, окно правила равно предмету, а записанная команда свидетельств
остаётся честной.

⚠️ **Что этот план НЕ трогает.** CR-01 и добавка `scroll-lock` — план 09-17 (волна 1). WR-04
(окно теста панели повтора) и WR-05 (регистрация обработчиков плашки) — план 09-19. Наблюдение
глазами и перевод состояний требований — план 09-20. Двойного планирования нет.

⚠️ **Волна 2, `depends_on: ["17"]`.** Пересечения файлов с планом 09-17 нет; зависимость — порядок
трассирующего среза: контракт сноса доказывается на самой узкой поверхности ПРЕЖДЕ расширения.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-PATTERNS.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-REVIEW.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-VERIFICATION.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-06-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-15-SUMMARY.md
@.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-17-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Имена, которые этот план ЗАВОДИТ (в дереве их сегодня нет):

**`app/pages/account_groups.py`:**

- параметр обработчика тумблера `search: str | None = Form(None)`;
- локальные величины `term` и `screen_url` в теле `account_groups_toggle` — по образцу
  `account_groups_delete` (`:680-696`).

**`app/templates/account_groups/partials/toggle_response.html`:**

- ожидаемая величина `filter_search` и её передача в вызов `group_row(...)`.

**`app/templates/account_groups/includes/group_row.html`:**

- скрытое поле строки поиска внутри блока вызова `form_wrapper` формы тумблера.

**`tests/test_pages/test_account_groups.py`:**

- `DELETE_FORM_RENDER_SITES` — объявленный перечень мест отрисовки формы удаления (файл шаблона +
  обоснование), и `DELETE_FORM_RENDER_SITES_DECLARED = 3` — отдельно утверждаемое число;
- `_delete_trigger_form(html, account_id, group_id)` — разборщик ОДНОЙ формы-триггера, пригодный
  для фрагмента, где панели подтверждения нет по построению (`with_modal=false`);
- `test_the_number_of_delete_form_render_sites_is_the_declared_one` — зубы у объявленного числа;
- `test_the_toggle_response_renders_the_delete_form_with_the_same_field_names` — НЕСУЩЕЕ правило
  третьего места отрисовки;
- `test_the_toggle_response_trigger_form_body_lands_on_the_filtered_listing` — ПОВЕДЕНЧЕСКАЯ
  половина: тело формы из ОТВЕТА ТУМБЛЕРА уходит базовым путём и приземляется на отфильтрованную
  выдачу;
- `test_the_toggle_landing_address_carries_the_search_filter` — WR-01, на найденной И на
  ненайденной группе, с сохранением неотличимости;
- `TOGGLE_CLAIM_SITES_MEASURED` — антивакуумное число мест прозы, рассуждающих о механизме
  идемпотентности тумблера;
- `test_every_claim_about_the_toggle_names_the_mechanism_the_handler_has` — правило прозы (WR-02);
- `test_control_negative_a_claim_site_naming_the_removed_form_reddens` — его отрицательный контроль.

⚠️ **Матрица покрытия внешнего API не заводится:** детерминированный обход области фазы вернул
`detected: false`. ⚠️ **Останов допущений архитектуры не сработал** (`detected: false`). ⚠️ **Гейт
схемы БД молчит:** ни одной ORM-модели, ни одной ревизии Alembic план не трогает — `Group.is_active`
и `Group.name` существуют с прежних вех. ⚠️ **Пакетов план не устанавливает**, аудит легитимности
пакетов не требуется.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Допущения, оставленные ОТКРЫТЫМИ (обход рёбер, спец-заготовки нет)

У фазы нет `SPEC.md`, поэтому рёбра пришли детерминированным обходом требований. По требованиям
ЭТОГО плана (FORM-02, QUAL-01, QUAL-06) обход дал четыре пункта; равенство «ни одного молчаливого
пропуска» удерживается так: **2 обращены в утверждения `must_haves.truths`** (`idempotency` —
`explicit`; `concurrency` — `backstop`) + **2 названы здесь открытыми** = 4.

ЗАКРЫВАЮТСЯ этим планом:

- **QUAL-01 / `idempotency`** — присвоение присланного состояния идемпотентно по построению, и
  параметр `search` состояния не касается; утверждается сохранением зелёного
  `test_the_toggle_is_idempotent_on_a_repeated_post` (`verification: explicit`).
- **QUAL-01 / `concurrency`** — тройной `WHERE` и единица записи в один `commit` не трогаются;
  параллельного исполнения суита не поднимает (`verification: backstop`).

ОСТАЮТСЯ **`unresolved`** и названы здесь, а не отброшены молча:

- **FORM-02 / `unclassified`** — обход не отнёс требование ни к одной категории. Предмет требования
  (`method="post"`, непустой `action`, посимвольное равенство `hx-post` и `action`, `action` на
  маршрут полного документа) этим планом двигается ровно в одну сторону: форма тумблера получает
  ОДНО скрытое поле и ни одного атрибута. Допущение «деградация не задета» проверяется прогоном
  `test_toggle_degrades_without_htmx`, `test_delete_degrades_without_htmx` и
  `tests/test_pages/test_htmx_gates.py` целиком в задачах 2 и 3, а не рассуждением.
- **QUAL-06 / `unclassified`** — план возврат фокуса не трогает: `id='group-toggle-' ~ group.id`
  остаётся посимвольно прежним. Допущение проверяется прогоном
  `test_toggle_fragment_keeps_the_toggle_id` в задаче 2.

Ни одно из двух не помечается закрытым автоматически и в `must_haves.truths` не попадает.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: три правила, краснеющие на сегодняшнем дереве — третье место отрисовки, приземление с его тела и адрес выхода тумблера</name>

  <files>tests/test_pages/test_account_groups.py</files>

  <read_first>
- `tests/test_pages/test_account_groups.py` около 5290-5480 ЦЕЛИКОМ — `DELETE_FORM_FIELD_NAMES_MEASURED`,
  `FORM_BLOCK_RE`, `FORM_FIELD_NAME_RE`, `FORM_INPUT_PAIR_RE`, `_delete_forms`, `_form_field_names`,
  `_form_body`, `test_both_delete_forms_post_the_same_field_names`,
  `test_the_trigger_form_body_lands_on_the_filtered_listing`: **это образец, который новые правила
  повторяют, а не изобретают заново**, включая порядок «сперва антивакуум, потом несущее» и форму
  сообщения об отказе, называющую следствие.
- `tests/test_pages/test_account_groups.py` около 4890-4990 — `OobTargetException`,
  `OOB_TARGET_EXCEPTIONS`, `OOB_TARGET_EXCEPTIONS_DECLARED` и три утверждения вокруг них: **второй
  образец**, по которому набирается объявленный перечень мест отрисовки с отдельно утверждаемым
  числом.
- Помощники посева и разбора того же файла: `_seed_account` (`:88`), `_seed_group` (`:107`),
  `_seed_many` (`:776`), `_row_ids` (`:57`), `_row_html`, `_search_param` (`:3048`); фикстуры
  `authed_client`, `htmx_client`, `db_session` и абзац о том, почему их нельзя мешать в одном тесте.
- `app/templates/account_groups/partials/toggle_response.html` ЦЕЛИКОМ — какие величины файл
  ожидает сегодня и почему строка зовётся с `with_modal=false`.
- `app/templates/account_groups/includes/group_row.html` — сигнатура макроса (`:53`), форма тумблера
  (вызов `form_wrapper`, около `:176-186`) и обе формы пути удаления (около `:191-210`).
- `app/pages/account_groups.py:416-423` (сигнатура тумблера), `:466` и `:548` (оба выхода на
  f-строке), `:68-90` (`_screen_url` и `_filter_params`), `:38` (`_clean_search`), `:680-700`
  (как то же делает обработчик удаления).
- `09-VERIFICATION.md`, гейп 2, список `missing:` дословно — все четыре пункта.
- `09-REVIEW.md`, CR-02 и WR-01 — те же выводы, полученные ревизией независимо.
- `09-PATTERNS.md` §«SP-1» — идиома измеренного числа и трёх утверждений.
  </read_first>

  <behavior>
- **Объявленное число мест отрисовки, `test_the_number_of_delete_form_render_sites_is_the_declared_one`:**
  сканер обходит `app/templates/` и считает места вызова макроса строки группы вне файла его
  определения; число равно `DELETE_FORM_RENDER_SITES_DECLARED` и строго больше нуля, а каждый
  найденный путь присутствует в объявленном перечне `DELETE_FORM_RENDER_SITES` с непустым
  обоснованием. Четвёртое место, заведённое молча, краснеет; исчезнувшее — тоже. Сегодня —
  ЗЕЛЁНОЕ: мест ровно три (`list.html`, `partial_cards.html`, `toggle_response.html`), и правило
  сторожит их число, а не чинит дефект.
- **Несущее, `test_the_toggle_response_renders_the_delete_form_with_the_same_field_names`:** посев
  с одной отвечающей поиску группой; страница запрашивается с фильтром, снимаются имена полей формы
  ПАНЕЛИ (эталон); затем той же строке шлётся htmx-переключение, и из ОТВЕТА снимаются имена полей
  формы-триггера. Порядок утверждений: сперва АНТИВАКУУМНОЕ (обе формы найдены, эталон несёт ровно
  `DELETE_FORM_FIELD_NAMES_MEASURED` имён, число строго больше нуля), затем НЕСУЩЕЕ (множества
  равны) и ЗНАЧЕНИЕ (скрытое поле ответа несёт ту же строку поиска, а не пустую). Сегодня — КРАСНОЕ:
  верификатор измерил `value=""` при `Альфа` на странице.
- **Поведенческое, `test_the_toggle_response_trigger_form_body_lands_on_the_filtered_listing`:** тело
  формы-триггера берётся ИЗ ОТВЕТА ТУМБЛЕРА так, как его шлёт разметка, и уходит базовым путём
  (клиент без признака htmx) на адрес удаления; `Location` ответа несёт параметр фильтра, а
  удаление единственной найденной строки приводит на выдачу, где строк нет. Сегодня — КРАСНОЕ.
- **WR-01, `test_the_toggle_landing_address_carries_the_search_filter`:** POST без признака htmx на
  адрес переключения телом формы тумблера со страницы `?search=<строка>` даёт `302`, и `Location`
  несёт фильтр. Второй половиной — тот же запрос на идентификатор ЧУЖОЙ и на идентификатор
  НЕСУЩЕСТВУЮЩЕЙ группы: адреса приземления совпадают между собой и с адресом найденной, то есть
  неотличимость (D-13) переводом на единый источник не ослаблена. Сегодня — КРАСНОЕ: измерено
  верификатором `302 → /accounts/1/groups`.
  </behavior>

  <action>
**1. Разборщик одной формы.** Завести `_delete_trigger_form(html, account_id, group_id)` — вариант
существующего `_delete_forms` для разметки, в которой панели подтверждения НЕТ ПО ПОСТРОЕНИЮ
(`with_modal=false`). Он утверждает, что форм на адрес удаления найдена РОВНО ОДНА, и возвращает её.
Границу назвать абзацем докстринга по образцу `_delete_forms`: разборщик не поддерживает вложенных
форм, в дереве их сегодня нет, и Фаза 10 обязана эту границу пересмотреть. Существующий
`_delete_forms` НЕ трогать: он утверждает ровно две формы и на фрагменте обязан краснеть.

**2. Объявленный перечень мест отрисовки.** `DELETE_FORM_RENDER_SITES` — отображение
относительного пути шаблона в обоснование («страница», «порция бесконечной прокрутки», «ответ
тумблера»), `DELETE_FORM_RENDER_SITES_DECLARED = 3` отдельной константой. Комментарий шапки — по
образцу `OOB_TARGET_EXCEPTIONS`: перечень ОБЪЯВЛЕН, а не выведен из дерева, потому что правило,
берущее число из самого дерева, приняло бы каждое следующее новое место молча — ровно тем способом,
каким третье место и осталось непокрытым.

**3. Три правила по образцу файла.** Порядок утверждений в каждом: антивакуумное → несущее →
следствие. Сообщение об отказе называет СЛЕДСТВИЕ прямым текстом, а не разность: путь без Alpine
приземляется на список, который человек не фильтровал, и ветка «выдача опустела» решается по всему
аккаунту вместо той выдачи, что человек видел.

**4. Перекрёстные ссылки в докстрингах.** В докстринге существующего
`test_both_delete_forms_post_the_same_field_names` дописать абзац: правило — половина ПАРЫ, его
предмет — два места отрисовки СТРАНИЦЫ, третье место накрывает
`test_the_toggle_response_renders_the_delete_form_with_the_same_field_names`, а число мест
сторожит `test_the_number_of_delete_form_render_sites_is_the_declared_one`. Имя правила НЕ менять:
команда свидетельств в `.planning/REQUIREMENTS.md` выбирает его подстрокой. В докстринге нового
правила — обратная ссылка и причина, по которой пара, а не переименование.

**5. Фикстуры не мешать.** `htmx_client` возвращает ТОТ ЖЕ объект клиента и выставил бы признак htmx
всему тесту — абзац об этом уже стоит у соседних правил файла; повторить его границу там, где нужен
базовый путь, и брать одну фикстуру.

**6. Файлов приложения задача не трогает.** `git diff --stat app/` обязан быть пуст: правила
заводятся ДО правки и краснеют на неправленом дереве. Коммит этой задачи — `RED_SHA` задачи 2.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "number_of_delete_form_render_sites"</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_response_renders_the_delete_form or toggle_response_trigger_form_body_lands or toggle_landing_address_carries_the_search_filter"; RC=$?; echo "код трёх новых правил на неправленом дереве: $RC"; [ "$RC" -eq 1 ]</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "not toggle_response_renders_the_delete_form and not toggle_response_trigger_form_body_lands and not toggle_landing_address_carries_the_search_filter"</automated>
    <automated>C=$(git diff --stat app/ | wc -l); echo "изменённых файлов app/: $C"; [ "$C" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
- В `tests/test_pages/test_account_groups.py` объявлены `DELETE_FORM_RENDER_SITES`, `DELETE_FORM_RENDER_SITES_DECLARED` и `_delete_trigger_form` — `grep -c "^DELETE_FORM_RENDER_SITES\b\|^DELETE_FORM_RENDER_SITES_DECLARED\|^def _delete_trigger_form" tests/test_pages/test_account_groups.py` → `3`.
- `DELETE_FORM_RENDER_SITES_DECLARED` равно `3`, и каждое из трёх мест перечня — существующий файл: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "number_of_delete_form_render_sites"` → `0`.
- Заведены три правила: `test_the_toggle_response_renders_the_delete_form_with_the_same_field_names`, `test_the_toggle_response_trigger_form_body_lands_on_the_filtered_listing`, `test_the_toggle_landing_address_carries_the_search_filter`.
- Прогон подмножества этих трёх даёт код РОВНО `1` — правила СОБРАЛИСЬ и ПОКРАСНЕЛИ. Код `0` означает правило, зелёное по построению; код `5` — что правила не собрались вовсе; оба недопустимы.
- Прогон ОСТАЛЬНЫХ правил файла даёт `0`: заведение новых правил не уронило ни одного существующего.
- Имя `test_both_delete_forms_post_the_same_field_names` не изменено: `grep -c "async def test_both_delete_forms_post_the_same_field_names" tests/test_pages/test_account_groups.py` → `1`.
- Докстринг существующего правила несёт перекрёстную ссылку на новое: `grep -c "test_the_toggle_response_renders_the_delete_form_with_the_same_field_names" tests/test_pages/test_account_groups.py` → значение не меньше `2` (объявление плюс ссылка).
- `git diff --stat app/` пуст.
- SHA коммита этой задачи записан в сводку как `RED_SHA`.
  </acceptance_criteria>

  <done>Три правила существуют и КРАСНЕЮТ на сегодняшнем дереве кодом ровно `1`; объявленное число
мест отрисовки заведено и зелено; существующее правило страницы не переименовано и несёт
перекрёстную ссылку. Файлы приложения не тронуты.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: строка поиска доезжает до третьего места отрисовки, и оба выхода тумблера приходят из единого источника</name>

  <files>app/pages/account_groups.py, app/templates/account_groups/partials/toggle_response.html, app/templates/account_groups/includes/group_row.html</files>

  <precondition>Задача 1 исполнена и закоммичена: три правила краснеют кодом ровно `1`, `RED_SHA`
известен.</precondition>

  <read_first>
- `app/pages/account_groups.py:410-560` ЦЕЛИКОМ — сигнатура `account_groups_toggle`, докстринг про
  отсутствие собственного перенаправления, тройной `WHERE`, ветка ненайденной группы (`:466`),
  присвоение состояния (`:510`), отложенная сборка `_fragment` и финальный выход (`:548`).
- `app/pages/account_groups.py:660-700` — **образец, который тумблер повторяет, а не изобретает**:
  как обработчик удаления принимает `search`, проводит его через `_clean_search`, собирает
  `screen_url` ОДИН РАЗ на обе ветки и что об этом сказано комментарием.
- `app/pages/account_groups.py:38` (`_clean_search`), `:63-90` (`_filter_params`, `_screen_url` с
  докстрингом о едином источнике и о посимвольной прежности адреса при пустой строке).
- `app/templates/account_groups/includes/group_row.html:176-186` — вызов `form_wrapper` формы
  тумблера блоком, и `:191-210` — обе формы пути удаления со скрытым полем, **это образец
  размещения поля**.
- `app/templates/account_groups/partials/toggle_response.html` ЦЕЛИКОМ — перечень ожидаемых величин
  в шапке и вызов макроса строки.
- `app/templates/account_groups/list.html:219` и `app/templates/account_groups/partial_cards.html:18`
  — как `filter_search` доезжает до макроса в двух уже работающих местах.
  </read_first>

  <behavior>
После правки три правила задачи 1 зеленеют:
- htmx-ответ тумблера несёт форму-триггер с тем же набором имён полей, что и форма панели на
  странице, и с тем же ЗНАЧЕНИЕМ строки поиска;
- тело формы-триггера из ОТВЕТА ТУМБЛЕРА, посланное базовым путём, приземляется на отфильтрованную
  выдачу;
- POST без признака htmx на адрес переключения приземляется на адрес, несущий фильтр, — и для
  найденной, и для чужой, и для несуществующей группы одинаково.

Не двигается: присвоение показанного состояния, тройной `WHERE`, отсутствие собственного
перенаправления, `hx-sync='this:drop'`, пустая цель блокировки, `id='group-toggle-' ~ group.id`.
  </behavior>

  <action>
**1. Обработчик принимает строку поиска.** Дописать `account_groups_toggle` параметр
`search: str | None = Form(None)` — тем же типом и тем же умолчанием, что у `account_groups_delete`
(`:551`). Тип строковый и необязательный намеренно, по той же причине, что уже записана у
`is_active`: разметка отсутствие поля объявить не может.

**2. Единый источник адреса.** В теле обработчика, ДО развилки, взять `term = _clean_search(search)`
и `screen_url = _screen_url(account_id, term)` — ровно как это делает обработчик удаления
(`:690-696`), — и перевести на `screen_url` ОБА выхода: ветку ненайденной группы (`:466`) и
финальный (`:548`). Комментарий над сборкой адреса написать по образцу соседа: адрес после действия
и адрес после перезагрузки обязаны приходить из одного источника, иначе они разъедутся молча.

⚠️ **Неотличимость (D-13) переводом НЕ ослабляется, и это называется абзацем.** Чужая и
несуществующая группа идут ОДНОЙ веткой и получают ОДИН И ТОТ ЖЕ адрес — тот же, что получила бы
найденная. Различимым ответ стал бы только в том случае, если бы ветки собирали адрес по-разному;
перевод на общий источник делает ровно обратное.

**3. Значение доезжает до шаблона.** В вызов отрисовки `toggle_response.html` внутри `_fragment()`
добавить `filter_search=term or ""` — тем же выражением, каким это делает страница
(`list.html:219` берёт `term or ""` из `_clean_search`). В шапке `toggle_response.html` дописать
`filter_search` в перечень ожидаемых величин и абзац с номером ЭТОГО плана: третье место отрисовки
формы удаления печатает строку поиска, и без неё путь без Alpine теряет фильтр (CR-02). Вызов
`group_row(...)` на `:29` получает `filter_search=filter_search`.

**4. Форме тумблера — своё скрытое поле.** Внутрь блока вызова `form_wrapper` формы тумблера
(`group_row.html:176-186`) добавить скрытое поле с именем `search` и значением `filter_search` —
тем же именем и тем же значением, что уже стоят в обеих формах пути удаления. Больше в этой форме
не меняется НИЧЕГО: ни `disabled_elt=''`, ни `sync='this:drop'`, ни `trigger='change'`, ни
`target`, ни `swap`, ни идентификатор тумблера. Над полем — абзац: без него величине неоткуда
приехать на пути «Alpine мёртв, htmx жив», и адрес приземления теряет фильтр (WR-01).

**5. ⚠️ ПЕРЕХОД ЦВЕТА ИСПОЛНЯЕТСЯ, А НЕ УТВЕРЖДАЕТСЯ.** Порядок коммитов: СНАЧАЛА коммит правил
(красный, задача 1), ЗАТЕМ коммит правки (зелёный). Фраза «коммиты различны» доказательством не
является. Поэтому ПОСЛЕ правки исполнить несущее правило на ДО-правочном состоянии производственных
файлов в ОТДЕЛЬНОМ отсоединённом рабочем дереве и записать в сводку **вывод команды**:
`ROOT=$(git rev-parse --show-toplevel); [ -x "$ROOT/.venv/bin/python" ] || { echo "нет $ROOT/.venv — сперва uv sync; проверка НЕ ИСПОЛНЕНА"; exit 1; }; WT=$(mktemp -d); git worktree add --detach -q "$WT" "$RED_SHA" || { echo "дерево на $RED_SHA не создано — проверка НЕ ИСПОЛНЕНА"; exit 1; }; ( cd "$WT" && "$ROOT/.venv/bin/python" -m pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_response_renders_the_delete_form" ); RC=$?; git worktree remove --force "$WT"; echo "код правила на ДО-правочном дереве $RED_SHA: $RC"; [ "$RC" -eq 1 ]`
Код обязан быть РОВНО `1`. Порог именно `1`: `5` означает, что правило не собралось вовсе, `4` —
ошибку в наборе команды, и оба прошли бы порог «ненулевой» вакуумом. Проверка идёт в ОТДЕЛЬНОМ
дереве, а не `git checkout` по файлу: проверка, которая ПИШЕТ в проверяемый файл, способна пройти
зелёной ровно там, где производственная правка уничтожена, — тот же класс отказа, который круг и
ловит. Внутри рабочего дерева `uv run` НЕ звать: он заводит там второй `.venv`.

**6. Соседние свойства прогоняются, а не предполагаются целыми.** Прогнать: обе пары деградации,
гейты `tests/test_pages/test_htmx_gates.py` целиком (отсутствие собственного перенаправления,
`NOT_YET_CONVERTED_COUNT`), `test_toggle_fragment_keeps_the_toggle_id`,
`test_toggle_fragment_carries_no_second_modal`, `test_the_toggle_is_idempotent_on_a_repeated_post`,
`test_the_toggle_honours_the_posted_state`, `test_the_delete_response_is_indistinguishable_…`,
правила курсора `keyset` и весь файл целиком.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_response_renders_the_delete_form or toggle_response_trigger_form_body_lands or toggle_landing_address_carries_the_search_filter"</automated>
    <automated>ROOT=$(git rev-parse --show-toplevel); [ -x "$ROOT/.venv/bin/python" ] || { echo "нет $ROOT/.venv — сперва uv sync; проверка НЕ ИСПОЛНЕНА"; exit 1; }; WT=$(mktemp -d); git worktree add --detach -q "$WT" "$RED_SHA" || { echo "дерево на $RED_SHA не создано — проверка НЕ ИСПОЛНЕНА"; exit 1; }; ( cd "$WT" &amp;&amp; "$ROOT/.venv/bin/python" -m pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_response_renders_the_delete_form" ); RC=$?; git worktree remove --force "$WT"; echo "код правила на ДО-правочном дереве $RED_SHA: $RC"; [ "$RC" -eq 1 ]</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly</automated>
    <automated>F=$(grep -c 'redirect=f"/accounts/{account_id}/groups"' app/pages/account_groups.py); T=$(grep -c 'f"/accounts/{account_id}/groups"' app/pages/account_groups.py); echo "выходов, собранных f-строкой: $F; всего вхождений (единственное законное — тело _screen_url): $T"; [ "$F" = "0" ] &amp;&amp; [ "$T" = "1" ]</automated>
    <automated>S=$(grep -c '<input type="hidden" name="search"' app/templates/account_groups/includes/group_row.html); echo "скрытых полей строки поиска в строке группы: $S"; [ "$S" = "3" ]</automated>
  </verify>

  <acceptance_criteria>
- `app/pages/account_groups.py` объявляет параметр тумблера: `sed -n '/^async def account_groups_toggle/,/^):/p' app/pages/account_groups.py | grep -c "search: str | None = Form(None)"` → `1`.
- Оба выхода тумблера идут через помощника, а не через литерал: `grep -c 'redirect=f"/accounts/{account_id}/groups"' app/pages/account_groups.py` → `0`.
- ⚠️ **Вхождение f-строки в файле остаётся РОВНО ОДНО, и это ТЕЛО `_screen_url` (строка 88), а не недоделка:** `grep -c 'f"/accounts/{account_id}/groups"' app/pages/account_groups.py` → `1`. ИЗМЕРЕНО планировщиком на `HEAD 4fe89da`: вхождений сегодня ТРИ — тело помощника плюс два выхода тумблера. Критерий «ноль» уничтожил бы сам единый источник, ради которого правка и делается.
- `app/templates/account_groups/partials/toggle_response.html` передаёт величину макросу: `grep -c "filter_search=filter_search" app/templates/account_groups/partials/toggle_response.html` → `1`.
- Строка группы несёт ТРИ скрытых поля строки поиска (форма тумблера, форма-триггер, форма панели): `grep -c '<input type="hidden" name="search"' app/templates/account_groups/includes/group_row.html` → `3`.
- ПОВЕДЕНЧЕСКИ: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_response_renders_the_delete_form or toggle_response_trigger_form_body_lands or toggle_landing_address_carries_the_search_filter"` → `0`.
- Переход цвета ИСПОЛНЕН: вывод команды прогона на `$RED_SHA` в отдельном дереве записан в сводку ДОСЛОВНО, и код равен РОВНО `1`.
- Деградация не двинулась: `uv run pytest tests/test_pages/test_account_groups.py tests/test_pages/test_htmx_gates.py -q -p no:randomly -k "degrades_without_htmx or no_converted_handler_builds_its_own_redirect or not_yet_converted"` → `0`.
- Свойства тумблера целы: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_fragment_keeps_the_toggle_id or toggle_fragment_carries_no_second_modal or toggle_is_idempotent_on_a_repeated_post or toggle_honours_the_posted_state or two_toggles_and_a_submit"` → `0`.
- Неотличимость цела: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "indistinguishable_for_a_foreign or repeated_delete_is_harmless"` → `0`.
- Контракт курсора не ослаблен: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "interleaved_portion_and_delete or control_positive_the_reverse_order or scroll_cursor"` → `0`.
- `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly` → `0`.
- Тело правил между RED- и GREEN-коммитом не изменено; если изменено — причина названа в сводке прямым текстом.
  </acceptance_criteria>

  <done>Строка поиска доезжает до всех трёх мест отрисовки формы удаления; оба выхода тумблера
приходят из единого источника адреса; три правила зеленеют исполненным переходом цвета;
неотличимость, деградация, идемпотентность и контракт курсора не тронуты.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: проза перестаёт описывать снятый механизм, и это держит ПРАВИЛО, а не единичная правка (WR-02)</name>

  <files>app/templates/account_groups/includes/group_row.html, tests/test_pages/test_account_groups.py</files>

  <read_first>
- `app/templates/account_groups/includes/group_row.html:120-180` ЦЕЛИКОМ — абзац, стоящий над
  вызовом `form_wrapper` с `disabled_elt=''`, и вся летопись вокруг него: это ЗАПИСАННОЕ
  ОБОСНОВАНИЕ снятия клиентской блокировки, а не стилистика.
- `app/pages/account_groups.py:491-512` — **образец летописи, к которому абзац приводится**:
  прежнее обоснование не вычёркивается, а называется описывающим снятую форму; там же — почему
  присвоение даёт идемпотентность в ПОЛНОМ смысле, тогда как инверсия давала её только в паре.
- `tests/test_pages/test_account_groups.py` около 4940-5010 —
  `test_every_claim_about_a_missing_oob_target_names_the_runtime_event` и его отрицательный
  контроль: **образец правила прозы**, который это правило повторяет, а не изобретает заново,
  включая антивакуумное число мест и форму подстановки.
- `tests/test_templates/test_htmx_markup_gates.py:1383-1434` — `_tree_with` и двойной
  предохранитель подстановки: образец встречается ровно один раз, результат отличается от исходника.
- `09-REVIEW.md`, WR-02 целиком — включая предложенную форму абзаца и довод о том, что проза
  занижает реальную гарантию и называет её не тем механизмом.
- `.planning/STATE.md` записи D-30 и D-32 — образец летописи проекта.
  </read_first>

  <behavior>
- **Несущее, `test_every_claim_about_the_toggle_names_the_mechanism_the_handler_has`:** сканер
  обходит ОБЛАСТЬ ПРИЛОЖЕНИЯ — `app/templates/account_groups/` и `app/pages/account_groups.py` — и
  находит места прозы, рассуждающие о том, чем обработчик тумблера обеспечивает безвредность
  повторного нажатия. Каждое найденное место обязано называть ДЕЙСТВУЮЩУЮ форму (обработчик
  ПРИСВАИВАЕТ присланное значение) и не обязано молчать о снятой — упоминание снятой формы
  допустимо ТОЛЬКО в летописной рамке, то есть рядом с указанием номера плана, которым она снята.
  Порядок утверждений: сперва АНТИВАКУУМНОЕ (мест найдено ровно `TOGGLE_CLAIM_SITES_MEASURED`, число
  строго больше нуля), затем НЕСУЩЕЕ. Сегодня — КРАСНОЕ: абзац `group_row.html:151-154` называет
  снятую форму без летописной рамки.
- **Отрицательный контроль, `test_control_negative_a_claim_site_naming_the_removed_form_reddens`:**
  тот же сканер, поданный ПОДСТАВЛЕННОМУ дереву, где в место прозы возвращена формулировка о снятой
  форме без летописной рамки, обязан покраснеть. Двойной предохранитель подстановки обязателен:
  образец встречается ровно один раз, результат отличается от исходника.
  </behavior>

  <action>
**1. Абзац приводится к форме летописи, а не переписывается начисто.** В
`app/templates/account_groups/includes/group_row.html:151-154` сказать действующее: обработчик
ПРИСВАИВАЕТ `is_active` присланное значение одной строкой под тройным ограничением, поэтому и
дошедшее второе то же тело не создаёт записи И не двигает состояния. Следующей строкой — летопись:
до плана 09-15 здесь стояло утверждение о слепой инверсии; оно было верно ДЛЯ СВОЕЙ ФОРМЫ и снято
вместе с ней (WR-02, план 09-18). Утверждение «серверной защитой это не является (PAY-02)»
остаётся дословно: правка касается механизма, а не статуса.

⚠️ **Довод усиливается, а не только исправляется.** Присвоение даёт идемпотентность в ПОЛНОМ
смысле, тогда как инверсия давала её только в паре: проза сегодня занижает реальную гарантию и
одновременно называет её не тем механизмом. Абзац обязан сказать обе половины.

**2. Правило прозы.** Завести `TOGGLE_CLAIM_SITES_MEASURED` — число мест прозы, рассуждающих о
механизме безвредности повторного нажатия тумблера, ИЗМЕРЕННОЕ по дереву (идиома SP-1), с
комментарием: без него правило зеленело бы вакуумом на сканере, потерявшем все места разом.
Область сканирования — ТОЛЬКО `app/`: константы самого правила живут в `tests/` и попали бы в
собственную выборку, если бы область была шире. Границу назвать абзацем докстринга.

**3. Отрицательный контроль обязателен.** Правило без контроля доказывает ровно ничего: красным оно
не бывало никогда, и отличить его зубы от его слепоты нечем — стандарт, который тот же круг
применяет к собственным правилам (`tests/test_pages/test_account_groups.py:4155-4158`).

**4. Порядок коммитов и переход цвета.** СНАЧАЛА коммит правила (красное), ЗАТЕМ коммит правки
абзаца (зелёное). ПОСЛЕ правки исполнить правило на ДО-правочном состоянии в ОТДЕЛЬНОМ
отсоединённом рабочем дереве на SHA коммита правила и записать в сводку вывод команды; код обязан
быть РОВНО `1`.

**5. Что этой задачей НЕ трогается.** Ни одного атрибута формы тумблера, ни одного значения,
передаваемого макросу-обёртке, ни одной строки обработчика. Задача правит ПРОЗУ и заводит правило.
`git diff --stat app/pages/` обязан быть пуст.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "claim_about_the_toggle_names_the_mechanism or claim_site_naming_the_removed_form"</automated>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly</automated>
    <automated>P=$(grep -c "ПРИСВАИВАЕТ" app/templates/account_groups/includes/group_row.html); echo "мест, называющих действующую форму: $P"; [ "$P" -ge 1 ]</automated>
    <automated>W=$(grep -c "WR-02" app/templates/account_groups/includes/group_row.html); echo "летописных ссылок на находку: $W"; [ "$W" -ge 1 ]</automated>
    <automated>S=$(grep -c "PAY-02" app/templates/account_groups/includes/group_row.html); echo "мест, называющих статус защиты: $S"; [ "$S" -ge 1 ]</automated>
    <automated>C=$(git diff --stat app/pages/ | wc -l); echo "изменённых файлов app/pages/: $C"; [ "$C" = "0" ]</automated>
    <automated>uv run pytest tests/ -q -p no:randomly</automated>
  </verify>

  <acceptance_criteria>
- Абзац называет ДЕЙСТВУЮЩУЮ форму: `grep -c "ПРИСВАИВАЕТ" app/templates/account_groups/includes/group_row.html` → значение не меньше `1` (диапазон строк в критерии НЕ фиксируется: правка сдвигает нумерацию, и критерий, привязанный к номерам, обесценился бы сам собой).
- Абзац несёт летописную рамку с идентификатором находки: `grep -c "WR-02" app/templates/account_groups/includes/group_row.html` → значение не меньше `1`.
- Утверждение о статусе не потеряно: `grep -c "PAY-02" app/templates/account_groups/includes/group_row.html` → значение не меньше `1`.
- Место, в котором стои́т абзац, названо в сводке номером строки, снятым ПОСЛЕ правки, — чтобы следующий читатель нашёл его без поиска.
- В `tests/test_pages/test_account_groups.py` объявлено `TOGGLE_CLAIM_SITES_MEASURED` и оно строго больше нуля — утверждается самим правилом; `grep -c "^TOGGLE_CLAIM_SITES_MEASURED" tests/test_pages/test_account_groups.py` → `1`.
- Заведены два правила: `test_every_claim_about_the_toggle_names_the_mechanism_the_handler_has` и `test_control_negative_a_claim_site_naming_the_removed_form_reddens`; оба зелены на HEAD — `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "claim_about_the_toggle_names_the_mechanism or claim_site_naming_the_removed_form"` → `0`.
- Переход цвета ИСПОЛНЕН: вывод прогона правила на SHA коммита правила в отдельном дереве записан в сводку ДОСЛОВНО, код равен РОВНО `1`.
- `git diff --stat app/pages/` пуст — задача правит только прозу шаблона и тесты.
- `uv run pytest tests/ -q -p no:randomly` → `0`.
- Долговых маркеров в изменённых файлах нет: `grep -rn "TODO\|FIXME\|XXX\|HACK\|PLACEHOLDER\|TBD" app/templates/account_groups/includes/group_row.html app/templates/account_groups/partials/toggle_response.html app/pages/account_groups.py` → пусто.
  </acceptance_criteria>

  <done>Записанное обоснование снятия клиентской блокировки описывает действующий механизм и несёт
летопись снятого; ложность прозы этого класса держит правило с антивакуумом и отрицательным
контролем, а не единичная правка. Суита зелена целиком.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → обработчик | строка поиска приходит ТЕЛОМ ФОРМЫ из недоверенного документа и попадает в адрес приземления и в заголовок перехода |
| обработчик → база | идентификаторы аккаунта и группы приходят путём запроса; владение проверяется тройным `WHERE` |
| обработчик → документ | значение строки поиска печатается в скрытое поле трёх форм отрисованной строки |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-18-01 | Tampering | строка поиска в теле формы тумблера | medium | mitigate | величина проходит тот же `_clean_search`, что и на странице и в обработчике удаления, и попадает в адрес ТОЛЬКО через `_screen_url`, кодирующий её `urlencode`; `_local_path` слоя ответа не выпускает в заголовок ни символа вне ASCII и ни одного управляющего. Величина сужает ВЫДАЧУ и не касается ни одного условия владения |
| T-09-18-02 | Elevation of privilege | параметр `search` не должен становиться вторым способом адресовать чужую строку | high | mitigate | тройной `WHERE` (`Group.id`, `Group.user_id`, `Group.account_id`) не трогается ни на символ; строка поиска в него не входит и входить не может. Закреплено прогоном `test_the_delete_response_is_indistinguishable_…` и `tests/test_pages/test_account_groups.py` целиком в задачах 2 и 3 |
| T-09-18-03 | Information disclosure | адрес приземления ненайденной группы становится различимым | high | mitigate | обе ветки (чужая и несуществующая) получают ОДИН И ТОТ ЖЕ адрес — тот же, что найденная; утверждается второй половиной `test_the_toggle_landing_address_carries_the_search_filter` и существующим правилом неотличимости. Перевод на единый источник делает ветки МЕНЕЕ различимыми, а не более |
| T-09-18-04 | Information disclosure | имя группы из внешнего мессенджера в скрытом поле и в атрибутах строки | medium | mitigate | план не вводит ни одного нового вывода имени; печатается только строка ПОИСКА, введённая самим человеком, и автоэкранирование значений атрибутов проекта (GATE-07) не ослабляется. Прогон `tests/test_templates/test_components.py` и `test_account_groups.py` целиком |
| T-09-18-05 | Repudiation | правка прозы обоснования без следа | medium | mitigate | абзац приводится к форме летописи с номером плана и идентификатором находки; прежний текст называется описывающим снятую форму, а не вычёркивается; правило прозы с отрицательным контролем сторожит класс |
| T-09-18-06 | Denial of service | параметр размера порции и число запросов подсчёта | low | accept | план маршрута порции не трогает; ограничение `Query(PAGE_SIZE, ge=1, le=100)` не двигается, отложенная сборка `_fragment` обоих обработчиков остаётся на месте |
| T-09-18-07 | Spoofing | подделанный признак запроса htmx выбирает форму ответа | low | accept | принято Фазой 8 (T-08-09): вердикт доступа вычисляется ДО развилки транспорта и от заголовка не зависит |
| T-09-18-SC | Tampering | npm/pip/cargo installs | low | accept | план не выполняет ни одной команды менеджера пакетов; гейт легитимности пакетов не срабатывает, таблицы аудита от разведки не требуется |

**Блокирующий порог — `high`, уровень ASVS — L1.** Обе угрозы этого уровня (T-09-18-02,
T-09-18-03) несут диспозицию `mitigate` с названным механизмом и проверяемым прогоном; ни одна не
принята.
</threat_model>

<verification>
- Строка поиска доезжает до всех ТРЁХ мест отрисовки формы удаления, и это утверждается правилом
  третьего места плюс объявленным числом мест с зубами.
- Тело формы-триггера из ОТВЕТА ТУМБЛЕРА, посланное базовым путём, приземляется на отфильтрованную
  выдачу.
- Оба выхода обработчика тумблера собраны `_screen_url(account_id, term)`; вхождений f-строки
  адреса в файле ноль.
- Неотличимость чужой и несуществующей группы сохранена и утверждена.
- Проза строки описывает действующий механизм и несёт летопись снятого; класс сторожит правило с
  отрицательным контролем.
- Переход цвета исполнен для каждого нового правила: код на ДО-правочном дереве равен РОВНО `1`.
- `uv run pytest tests/ -q -p no:randomly` → `0`.
</verification>

<success_criteria>
- CR-02 закрыт: третье место отрисовки формы удаления получает данные, которые правило считает
  обязательными, и правило накрывает все три места, а не два.
- WR-01 закрыт: адрес приземления тумблера приходит из того же источника, что и адрес после
  перезагрузки.
- WR-02 закрыт: записанное обоснование снятия клиентской блокировки описывает механизм, который у
  кода есть, — и класс сторожится правилом.
- Ни одно закрытие прошлых кругов не ослаблено: `keyset`, `OOB_TARGET_EXCEPTIONS`,
  `DISABLED_ELT_EXCEPTIONS`, гейт G-2, неотличимость и деградация целы.
</success_criteria>

<output>
Create `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-18-SUMMARY.md` when done
</output>
