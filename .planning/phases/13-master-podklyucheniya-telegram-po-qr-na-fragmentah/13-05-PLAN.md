---
phase: 13-master-podklyucheniya-telegram-po-qr-na-fragmentah
plan: 05
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - tests/test_routes/test_tg_user_auth.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_templates/test_htmx_markup_security.py
  - tests/test_templates/test_components.py
autonomous: true
requirements: [FETCH-02]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: sample_count 0, factor 1; confidence выведен, а не самооценён.
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Гейт критерия 2 ЗАМКНУТ: каждая ветка шага в `tg_connect_step.html` (`start`, `waiting`, `qr_expired`, `password`, `connected`, `error`) достигается хотя бы одной записью `POLLING_CASES`, и каждая запись подтверждает свою ветку структурной меткой ответа — новая ветка шаблона без записи краснит правило (критерий 2, D-05)"
    - "Для каждой записи с опросом (старт, обновление кода) в реестре есть хотя бы одна запись маршрута опроса, отвечающая 200 БЕЗ `hx-trigger` — у каждого фрагмента с `hx-trigger=\"every \"` есть парный без него, обобщение `test_sync_polling_stops` (критерий 2)"
    - "Правила гейта исполняются на ПАРАМЕТРАХ (текст ответа, реестр, текст шаблона), и у каждого есть отрицательный контроль на синтетике: терминальный фрагмент с триггером, реестр без терминальных записей опроса, идентификатор якоря внутри фрагмента, ветка шаблона без записи — каждый краснит своё правило (D-05, RESEARCH Pitfall 1)"
    - "Изъятие верхнеуровневых связей мастера (`TOP_LEVEL_BINDING_EXEMPT_TEMPLATES`) снято вместе с предметом и переписано в ИМЕНОВАННЫЙ НОЛЬ; правило непустоты и правило «экран подключения не цель перехода» переписаны так, что не зеленеют вакуумом на пустом перечне (D-12, RESEARCH Pitfall 4)"
    - "Проза о трёх атрибутах-обработчиках мастера (`test_htmx_markup_security.py`) и докстринг строкознающего разборщика (`test_components.py`) получили летописи Фазы 13 с замером дерева, а не переписаны молча (D-12, R-08-02)"
  artifacts:
    - "tests/test_routes/test_tg_user_auth.py — замыкание POLLING_CASES по веткам шаблона, правило пары, отрицательные контроли"
    - "tests/test_pages/test_hx_location_destinations.py — TOP_LEVEL_BINDING_EXEMPT_TEMPLATES как именованный ноль"
  key_links:
    - "реестр `POLLING_CASES` ↔ ветки `step` шаблона `tg_connect_step.html`: ломается — новая ветка с триггером уходит в поставку без пары и становится вечным опросом"
  prohibitions:
    - statement: "правило гейта не удаляется ради зелени: покрасневшее по построению правило переписывается в форму именованного нуля или на синтетику (RESEARCH Pitfall 4)"
      status: flagged-unverified
      verification: none
---

<objective>
Замыкание критерия 2 и уборка устаревших записей о снятом мастере. Реестр `POLLING_CASES`, который
планы 13-01…13-04 наращивали по состояниям, замыкается по веткам шаблона шага и получает
отрицательные контроли; перечни и докстринги, которые снятие скрипта сделало устаревшими, но не
красными, приводятся к дереву летописью.

Purpose: гейт опросов разметки слеп к опросчику, рождённому макросом (RESEARCH §Инвентарь, строка
19; измерено планом 13-01), поэтому критерий 2 держится только правилом на отрисованных ответах — и
это правило обязано ловить появление новой ветки, а не только проверять известные.

Output: замкнутый гейт критерия 2 с контролями; именованный ноль изъятия верхнеуровневых связей;
летописи в двух докстрингах гейтов.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-CONTEXT.md
@.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Шаблон (новый, 13-01):** `app/templates/accounts/includes/tg_connect_step.html`; переменные
`step` (`start`, `waiting`, `qr_expired`, `password`, `connected`, `error`), `session_id`, `qr_code`,
`error`, `password_error`; якорь `tg-connect-step`.

**`app/pages/accounts.py`:** `TG_CONNECT_STEP_TEMPLATE`, `_tg_step_markup`, `_save_tg_account`,
`TG_API_NOT_CONFIGURED_MESSAGE`, `TG_AUTH_FAILED_MESSAGE`, `TG_SESSION_EXPIRED_MESSAGE`,
`TG_SESSION_NOT_FOUND_MESSAGE`, `TG_REFRESH_FAILED_MESSAGE`, `TG_EMPTY_PASSWORD_MESSAGE`.

**`app/messengers/telegram_user.py`:** `QRAuthState.user_id`, `_owned`, статусы `qr_expired` и `gone`,
сигнатуры с `user_id`.

**Тесты:** `POLLING_CASES`, `test_polling_stops_by_a_response_without_trigger`; в этом плане —
`STEP_MARKS` (ветка шага → структурная метка ответа), помощники правил над параметрами, правило
замыкания по веткам шаблона, правило пары и отрицательные контроли; фикстура гонок (13-02);
ключи пар `ACCOUNTS_CONNECT_TG_USER_*` (13-01…13-03).

**Снимается (этот план):** запись `accounts/connect_tg_user.html` в `TOP_LEVEL_BINDING_EXEMPT_TEMPLATES`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Гейт критерия 2 замкнут по веткам шаблона и доказан отрицательными контролями</name>

  <files>
tests/test_routes/test_tg_user_auth.py
  </files>

  <read_first>
- `tests/test_routes/test_tg_user_auth.py` целиком — `POLLING_CASES` и
  `test_polling_stops_by_a_response_without_trigger` в том виде, в каком их оставил 13-04.
- `app/templates/accounts/includes/tg_connect_step.html` целиком — условия ветвления по `step`.
- `tests/test_pages/test_htmx_preserved.py:288-318` — пара «останов / продолжение» и её докстринг
  (почему тест присутствия и тест отсутствия нужны оба).
- `tests/test_pages/test_htmx_post_pairs.py:1672-1735` — форма правила замыкания с реестром
  ПАРАМЕТРОМ и отрицательный контроль, проверяющий ТУ ЖЕ функцию, а не её копию.
- `13-RESEARCH.md` строки 619-630 (§Phase Requirements → Test Map, строка критерия 2).
  </read_first>

  <behavior>
1. `test_every_wizard_step_is_reached_by_a_polling_case` — множество веток, прочитанное из текста
   шаблона (литералы сравнения `step` в условиях `if`/`elif`, включая перечисление в одном
   условии), РАВНО множеству ключей `STEP_MARKS` и покрыто шагами записей `POLLING_CASES`; отказ
   называет недостающие ветки поимённо.
2. Каждая запись `POLLING_CASES` объявляет ожидаемую ветку; правило записи утверждает метку этой
   ветки в ответе (`STEP_MARKS`: `start` — «Начать подключение», `error` — «Начать заново»,
   `waiting` — `hx-trigger="every 3s"`, `qr_expired` — `hx-post="/accounts/connect/tg_user/refresh-qr"`,
   `password` — `name="password"`, `connected` — «Подключено»). Шаг `start` достигается записью
   страницы мастера (GET).
3. `test_every_polling_fragment_has_a_terminal_pair` — для каждой записи с опросом в реестре есть
   запись маршрута `qr-status` с ответом 200 без `hx-trigger`.
4. Отрицательные контроли, каждый зовёт ТУ ЖЕ функцию правила с синтетическим входом:
   `test_control_a_terminal_fragment_with_a_trigger_reddens_the_rule`;
   `test_control_a_registry_without_terminal_polls_reddens_the_pair_rule`;
   `test_control_the_anchor_inside_a_fragment_reddens_the_guard`;
   `test_control_a_template_branch_without_a_case_is_named` (синтетический шаблон с лишней веткой —
   отказ называет её имя).
Правила этого плана зелены на прибытии: поведение уже в дереве (13-01…13-04). Непустоту каждого
правила доказывает его контроль — это и есть RED плана (память проекта
`red-gate-inversion-false-greens`: инверсия кода возврата уликой RED не является).
  </behavior>

  <action>
Проверки текущего правила `test_polling_stops_by_a_response_without_trigger` выносятся в чистые
помощники над параметрами — `_polling_violations(text, *, polls, step)`, `_pairing_violations(cases)`,
`_anchor_in_fragment(text)`, `_unreached_steps(template_text, cases)` — по форме
`_closure_complaints` в `test_htmx_post_pairs.py` (реестр и текст приходят параметром, чтобы контроль
проверял ТУ ЖЕ проверку). Разбор веток шаблона — регулярным выражением по литералам сравнения
`step`, с антивакуумом: найдено веток не меньше шести, иначе разбор ослеп, и правило краснеет
словами «разбор веток не нашёл ни одной». Ветка `start`/`error` объявлена одним условием с
перечислением — разбор обязан её прочесть как две ветки. Записи `POLLING_CASES` получают поле
ожидаемой ветки; запись «страница мастера» добавляется, если её нет. Шапка блока гейта в модуле
называет: обобщение `test_sync_polling_stops` (критерий 2), причину, по которой гейт живёт здесь, а
не в `test_htmx_inventory.py` (слепота гейта опросов к опросчику из макроса, измеренная 13-01), и
поправку D-05 (опросчик внутри фрагмента ожидания).
  </action>

  <verify>
    <automated>uv run pytest tests/test_routes/test_tg_user_auth.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_routes/test_tg_user_auth.py -q -p no:randomly -k "control"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or fewer than "4 passed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'def test_every_wizard_step_is_reached_by_a_polling_case' tests/test_routes/test_tg_user_auth.py` возвращает `1`; `grep -c 'def test_every_polling_fragment_has_a_terminal_pair' tests/test_routes/test_tg_user_auth.py` возвращает `1`.
- `grep -c 'def test_control_' tests/test_routes/test_tg_user_auth.py` не меньше `4`.
- `grep -c 'STEP_MARKS' tests/test_routes/test_tg_user_auth.py` не меньше `2`.
- `uv run pytest tests/test_routes/test_tg_user_auth.py -q -p no:randomly -k "polling"` — зелено, отбор не пуст.
  </acceptance_criteria>

  <done>
Критерий 2 держится замкнутым гейтом на отрисованных ответах: каждая ветка шага достигается,
каждый опрос имеет терминальную пару, у каждого правила есть отрицательный контроль.
</done>
</task>

<task type="auto">
  <name>Задача 2: Записи о снятом мастере в перечнях гейтов — именованный ноль и летописи с замером</name>

  <files>
tests/test_pages/test_hx_location_destinations.py,
tests/test_templates/test_htmx_markup_security.py,
tests/test_templates/test_components.py
  </files>

  <read_first>
- `tests/test_pages/test_hx_location_destinations.py:490-520` (`TOP_LEVEL_BINDING_EXEMPT_TEMPLATES` и
  его основание), `:760-775` (где изъятие читается обходом цепочки), `:1182-1215`
  (`test_every_exempt_template_carries_a_non_empty_rationale`,
  `test_the_connect_screen_is_not_a_transition_destination_today`), а также правила статического
  обнаружения верхнеуровневых связей (`:933-1010`) — чем проверить, что на экране подключения
  связей больше нет.
- `tests/test_templates/test_htmx_markup_security.py:40-75` — докстринг о границе инвентаря
  атрибутов событий и дополнение плана 11-18.
- `tests/test_templates/test_components.py:4980-5005` — докстринг `_strip_js_comments` («разбор
  строкознающий, и это измерено предметом») и `:5064-5090` (`_script_handler_registrations`).
- `.planning/PROJECT.md:261` — долг R-08-02.
- `13-RESEARCH.md` строки 455 и 463 (§Инвентарь, строки 8 и 16), 487-488 (§Pitfall 4).
  </read_first>

  <action>
**1. `tests/test_pages/test_hx_location_destinations.py` (D-12, Pitfall 4).**
`TOP_LEVEL_BINDING_EXEMPT_TEMPLATES` становится пустым словарём с объявленным числом
`TOP_LEVEL_BINDING_EXEMPTIONS_DECLARED = 0` — именованный ноль с летописью «Фаза 13, план 13-05»:
основание единственной записи (три функции сценария, достижимые из атрибутов разметки) снято вместе
со сценарием планом 13-01, и изъятие потеряло предмет. `test_every_exempt_template_carries_a_non_empty_rationale`
переписывается: утверждение непустоты перечня заменяется утверждением «длина перечня равна
объявленному числу» (ноль заявлен явно, и появление первой записи краснеет), проверка основания
каждой записи остаётся. `test_the_connect_screen_is_not_a_transition_destination_today` на пустом
перечне вакуумен и переписывается в правило о ПРЕДМЕТЕ: разбор верхнеуровневых связей того же
модуля над цепочкой шаблонов экрана подключения (`accounts/connect_tg_user.html`) находит НОЛЬ
связей — основание изъятия исчезло, и возврат сценария на экран краснеет здесь. Удаление правил
вместо переписывания запрещено.

**2. `tests/test_templates/test_htmx_markup_security.py` (R-08-02).** Докстринг о границе
инвентаря получает «ДОПОЛНЕНИЕ ФАЗЫ 13 (план 13-01)»: три обработчика экрана подключения Telegram
сняты вместе со сценарием мастера, предмет R-08-02 исчерпан. Замер дерева на дату плана вписывается
дословно командой и выводом: обычных атрибутов событий `onclick`/`onsubmit` в `app/templates/` —
ноль; вне предмета R-08-02 в дереве есть атрибут события `components/thumb.html` (запасной адрес
миниатюры) — включение семейства обычных атрибутов событий в инвентарь с объявленным числом есть
решение фазы упрочнения (Фаза 15), а не этой, и граница названа, а не растворена.

**3. `tests/test_templates/test_components.py`.** Докстринг `_strip_js_comments` получает летопись:
адрес мастера Telegram, приведённый как предмет измерения, снят из дерева Фазой 13. Исполнитель
ЗАМЕРЯЕТ, остались ли в телах блоков сценария шаблонов строки, содержащие двойную косую (команда и
вывод — в летопись): если остались — пример заменяется найденным; если нет — докстринг говорит, что
строкознающий разбор остаётся защитой, а не отражением замера. Поведение функции не меняется.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_hx_location_destinations.py tests/test_templates/test_htmx_markup_security.py tests/test_templates/test_components.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_inventory.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_markup_security.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_impersonation_gate.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_preserved.py tests/test_pages/test_htmx_post_pairs.py tests/test_routes/test_tg_user_auth.py tests/test_messengers/test_telegram_user.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- Изъятие — именованный ноль: `uv run python -c "import sys; from tests.test_pages.test_hx_location_destinations import TOP_LEVEL_BINDING_EXEMPT_TEMPLATES as s, TOP_LEVEL_BINDING_EXEMPTIONS_DECLARED as n; sys.exit(0 if s == {} and n == len(s) else 1)"` — код 0.
- `grep -c 'Фаза 13' tests/test_templates/test_htmx_markup_security.py` не меньше `1`; `grep -c 'Фаза 13' tests/test_templates/test_components.py` не меньше `1`.
- `uv run pytest tests/test_pages/test_hx_location_destinations.py -q -p no:randomly -k "exempt or connect_screen"` — зелено, отбор не пуст.
  </acceptance_criteria>

  <done>
Перечни гейтов не хранят записей о снятом сценарии мастера: изъятие — именованный ноль с правилом о
предмете, проза и докстринги приведены к замеру дерева летописью.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| шаблон шага → браузер | наличие или отсутствие формы-опросчика в каждом ответе определяет, остановится ли опрос |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Denial of Service | новая ветка шаблона шага с опросчиком без терминальной пары — вечный опрос каждые 3 с на каждой вкладке | medium | mitigate | Замыкание `POLLING_CASES` по веткам шаблона, правило пары, контроль «якорь внутри фрагмента»; каждое правило с отрицательным контролем на синтетике |
| T-13-16 | Tampering | возврат инлайн-сценария на экран подключения без решения | low | mitigate | Правило «на экране подключения ноль верхнеуровневых связей» вместо вакуумного правила изъятия |
</threat_model>

<verification>
- После каждой задачи — её `<verify>`; последняя команда задачи 2 — прогон гейтов волны.
- `uv run python -m compileall -q app main.py tests`; `graphify update .`.
</verification>

<success_criteria>
1. Гейт критерия 2 замкнут по веткам шаблона и имеет четыре отрицательных контроля.
2. Изъятие верхнеуровневых связей мастера — именованный ноль; правила не зеленеют вакуумом.
3. Проза и докстринги о снятом мастере приведены к замеру дерева.
</success_criteria>

<output>
Create `.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-05-SUMMARY.md` when done.
</output>
