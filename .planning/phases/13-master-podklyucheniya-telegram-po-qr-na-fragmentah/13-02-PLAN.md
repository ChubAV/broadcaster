---
phase: 13-master-podklyucheniya-telegram-po-qr-na-fragmentah
plan: 02
type: tdd
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/pages/accounts.py
  - app/templates/accounts/includes/tg_connect_step.html
  - tests/test_routes/test_tg_user_auth.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
autonomous: true
requirements: [FETCH-02]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: sample_count 0, factor 1; confidence выведен, а не самооценён.
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Опрос в статусе `needs_2fa` отвечает 200 и шагом пароля без `hx-trigger`: форма `hx-post=\"/accounts/connect/tg_user/verify-2fa\"` со скрытым `session_id`, поле `password` с `required` и БЕЗ `value` — опрос остановлен ответом (критерий 2)"
    - "Неверный пароль 2FA отвечает 422 и шагом пароля с ошибкой «Неверный пароль 2FA.» у поля; присланного пароля в теле ответа нет, поле пустое (D-08, T-06-02)"
    - "Пустой или отсутствующий пароль отвечает 422 и шагом пароля с ошибкой «Введите пароль» у поля — серверная проверка тем же текстом, что клиентская сегодня (D-08)"
    - "Верный пароль сохраняет ровно один `MessengerAccount` через `_save_tg_account` из РЕЗУЛЬТАТА `complete_auth`, а не из строки `submit_2fa`, и отвечает «Подключено» без `hx-trigger` (D-01, D-07, RESEARCH Pitfall 3)"
    - "Два конкурентных `verify-2fa` с верным паролем дают ровно один `MessengerAccount`; два конкурентных опроса после `success` дают ровно один `MessengerAccount` — `complete_auth` не подменяется (D-01)"
    - "Иная ошибка Telethon при входе с паролем отвечает шагом `error` с «Ошибка авторизации» и формой «Начать заново», а не 500 и не баннером отказа (D-09, RESEARCH Pitfall 5)"
    - "`verify-2fa` без признака htmx: исход 302 на `/accounts/connect/tg_user`, ошибка поля — страница мастера с кодом 422; без сессии входа — `/login` на обоих транспортах (D-10, D-11)"
    - statement: "В браузере аккаунт с 2FA проходит шаг пароля: неверный пароль оставляет человека на шаге с ошибкой у поля, верный приводит к «Подключено» — ручной UAT фазы (критерий 4)"
      verification: backstop
  artifacts:
    - "app/pages/accounts.py — accounts_connect_tg_user_verify_2fa на respond()/respond_field_error(); TG_EMPTY_PASSWORD_MESSAGE; ветка needs_2fa опроса"
    - "app/templates/accounts/includes/tg_connect_step.html — ветка password"
    - "tests/test_routes/test_tg_user_auth.py — тесты шага 2FA, гонки verify-2fa и опросов, записи POLLING_CASES"
  key_links:
    - "опрос `needs_2fa` → шаг пароля (`hx-post=verify-2fa`, `hx-target=#tg-connect-step`) → `accounts_connect_tg_user_verify_2fa` → `submit_2fa` → `complete_auth` → `_save_tg_account`: ломается — человек с 2FA не подключается"
    - "`respond_field_error` → правило `422: swap true` блока конфигурации → шаг пароля с ошибкой у поля: ломается — неверный пароль молча не перерисовывает форму"
  prohibitions:
    - statement: "пароль 2FA не возвращается в ответ, не переносится в атрибуты вне поля, в хранилища браузера и в журнал (D-06, D-08, T-06-02)"
      status: flagged-unverified
      verification: none
---

<objective>
Путь человека с двухфакторной защитой — на фрагментах: опрос, увидевший `needs_2fa`, отдаёт шаг
пароля; `verify-2fa` переведён на слой ответа; неверный и пустой пароль отвечают 422 с ошибкой у
поля без эха; верный пароль сохраняет аккаунт тем же помощником, что и опрос. И — машинное
доказательство D-01: ровно один аккаунт на одно сканирование на ОБОИХ путях сохранения, под гонкой.

Purpose: сегодня два конкурентных `verify-2fa` создают два аккаунта (RESEARCH §Pitfall 3:
аккаунт берётся из строки `submit_2fa`, результат `complete_auth` отбрасывается). Перевод
обработчика закрывает гонку тем, что строка сессии берётся только из результата `complete_auth`,
снимающего сессию `pop` до первого `await`.

Output: ветка `password` шаблона шага, переведённый `verify-2fa`, тесты шага 2FA и двух гонок,
сдвинутые перечни.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-CONTEXT.md
@.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Шаблон (новый, 13-01):** `app/templates/accounts/includes/tg_connect_step.html`; переменные
`step` (`start`, `waiting`, `qr_expired`, `password`, `connected`, `error`), `session_id`, `qr_code`,
`error`, `password_error`; якорь `tg-connect-step` на странице.

**`app/pages/accounts.py`:** `TG_CONNECT_STEP_TEMPLATE`, `_tg_step_markup`, `_save_tg_account`,
`TG_API_NOT_CONFIGURED_MESSAGE`, `TG_AUTH_FAILED_MESSAGE`, `TG_SESSION_EXPIRED_MESSAGE`,
`TG_SESSION_NOT_FOUND_MESSAGE` (13-04), `TG_REFRESH_FAILED_MESSAGE` (13-03),
`TG_EMPTY_PASSWORD_MESSAGE` (этот план); `accounts_connect_tg_user_qr_status` под POST.

**`app/messengers/telegram_user.py`:** `QRAuthState.user_id`, `_owned`, статусы `qr_expired` и
`gone`, сигнатуры с `user_id` (13-03, 13-04).

**Тесты:** `POLLING_CASES`, `test_polling_stops_by_a_response_without_trigger`; фикстура отдельных
сессий базы на запрос (этот план); `ACCOUNTS_CONNECT_TG_USER_START_QR`,
`ACCOUNTS_CONNECT_TG_USER_QR_STATUS`, `ACCOUNTS_CONNECT_TG_USER_VERIFY_2FA` (этот план),
`ACCOUNTS_CONNECT_TG_USER_REFRESH_QR` (13-03).

**Снимается (этот план):** JSON-контракт `verify-2fa` и его JSON-тесты.
</artifacts_this_phase_produces>

<tdd_notes>
- Коммиты: RED — `test(13-02): …`, GREEN — `feat(13-02): …`.
- RED-улика: pytest с `--junit-xml`, перегон в TAP скриптом в scratchpad (память проекта
  `red-evidence-verb-wants-node-tap`); RED — это названное упавшее правило, а не ненулевой код.
- Целевое правило RED-улики — `test_polling_needs_2fa_answers_the_password_step`: на дереве 13-01
  опрос в `needs_2fa` отвечает шагом ошибки, и правило падает на утверждении о поле пароля, то есть
  на планируемом поведении. Правила гонки на сегодняшнем JSON-обработчике могут упасть исключением
  разбора тела, а не утверждением, — целью RED-улики они поэтому не назначаются. Их непустоту
  доказывает замер мутанта, записанный в SUMMARY командой и выводом: обработчик, берущий строку
  сессии из `submit_2fa`, даёт ДВА аккаунта под той же гонкой.
</tdd_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Шаг пароля 2FA на фрагментах и ровно один аккаунт на сканирование под гонкой</name>

  <files>
app/pages/accounts.py,
app/templates/accounts/includes/tg_connect_step.html,
tests/test_routes/test_tg_user_auth.py
  </files>

  <read_first>
- `app/pages/accounts.py` — блок мастера TG после 13-01 (`TG_*`, `_tg_step_markup`, `_save_tg_account`,
  `accounts_connect_tg_user_qr_status`) и сегодняшний `accounts_connect_tg_user_verify_2fa`;
  `:570-640` (`accounts_connect_max_start`: ветка 422 — сборщики `_page` и `_fragment`,
  `respond_field_error`).
- `app/pages/htmx.py:847-900` — `respond_field_error(request, *, page, fragment)`.
- `app/messengers/telegram_user.py:137-171` — `submit_2fa` (ловит только `PasswordHashInvalidError`,
  `ValueError("Неверный пароль 2FA.")`, `RuntimeError` на отсутствующей сессии) и `complete_auth`.
- `app/templates/accounts/includes/tg_connect_step.html` (после 13-01) и
  `app/templates/components/field.html:7-28` — `field(..., required, error, autocomplete)`.
- Тексты шага пароля сняты со страницы мастера до 13-01 и приведены в действии дословно:
  «У вашего аккаунта включена двухфакторная аутентификация. Введите пароль.», метка
  «Пароль 2FA», заглушка «Пароль двухфакторной аутентификации», кнопка «Подтвердить»,
  `autocomplete='off'`. Сверка при сомнении: `git show master:app/templates/accounts/connect_tg_user.html`.
- `tests/test_routes/test_tg_user_auth.py` (после 13-01) — фикстуры модуля, `POLLING_CASES`,
  JSON-тесты `verify-2fa`, которые этот план переписывает.
- `13-RESEARCH.md` строки 302-322 (§Pattern 2–3), 484-492 (§Pitfall 3, 5).
  </read_first>

  <behavior>
RED:
1. `test_polling_needs_2fa_answers_the_password_step` — состояние `needs_2fa`: 200, форма
   `hx-post="/accounts/connect/tg_user/verify-2fa"`, скрытое `session_id`, поле `name="password"` с
   `required`, строки `value=` у поля пароля нет, `hx-trigger` нет.
2. `test_a_wrong_password_answers_422_at_the_field_without_echo` — `submit_2fa` поднимает
   `ValueError("Неверный пароль 2FA.")` (подмена клиента, у которого `sign_in` поднимает
   `PasswordHashInvalidError`): 422, «Неверный пароль 2FA.» во фрагменте, присланного пароля в теле
   нет, `hx-trigger` нет; без признака htmx — 422 и страница мастера с тем же текстом.
3. `test_an_empty_password_answers_422_with_the_client_text` — пустое и отсутствующее поле: 422,
   «Введите пароль»; `submit_2fa` не вызывался.
4. `test_a_right_password_saves_one_account_from_complete_auth` — настоящее состояние `needs_2fa` с
   клиентом, у которого `sign_in` успешен, а `session.save()` (синхронный `MagicMock`) отдаёт
   `"session-2fa"`: 200, «Подключено», `hx-trigger` нет; ровно один аккаунт с
   `credentials="session-2fa"`; сессии нет в `_qr_sessions`.
5. `test_a_telethon_failure_on_the_password_step_is_a_fragment` — `sign_in` поднимает иное
   исключение: 200, шаг `error` с «Ошибка авторизации», форма «Начать заново», не 500.
6. `test_two_concurrent_password_submits_save_one_account` — фикстура ОТДЕЛЬНОЙ сессии базы на
   запрос (см. действие, п. 3); `sign_in` — корутина с `await asyncio.sleep(0)`; два POST
   `verify-2fa` через `asyncio.gather`; `select count(MessengerAccount)` = 1; один ответ «Подключено».
7. `test_two_concurrent_polls_after_success_save_one_account` — та же фикстура; настоящее
   `QRAuthState(status="success", session_string=…)`, `client.disconnect` — корутина с
   `await asyncio.sleep(0)`; два POST `qr-status` через `asyncio.gather`; count = 1; `complete_auth`
   НЕ подменяется.
8. `test_a_second_poll_after_success_does_not_save_again` — последовательно: первый опрос →
   «Подключено», второй тем же `session_id` → шаг `error`; count = 1.
9. `test_verify_2fa_degrades_and_requires_a_session` — без htmx успех → 302 на
   `/accounts/connect/tg_user` (адрес литералом в `.post(…)`); без cookie → `/login` на обоих
   транспортах; неизвестная сессия → шаг `error` с «Сессия авторизации истекла. Начните заново.».
10. Записи `POLLING_CASES`: опрос `needs_2fa` (без опроса), `verify-2fa` успех, неверный пароль (422),
    пустой пароль (422), ошибка Telethon — все без `hx-trigger`.
GREEN — ветка шаблона и обработчик.
  </behavior>

  <action>
**1. Ветка `password` в `tg_connect_step.html` (D-08, T-06-02).** Абзац «У вашего аккаунта
включена двухфакторная аутентификация. Введите пароль.»; блочный вызов
`form_wrapper(action='/accounts/connect/tg_user/verify-2fa', target='#tg-connect-step',
swap='innerHTML')` (умолчание цели блокировки — кнопка отправки: защита от двойного нажатия на
клиенте); внутри — скрытое поле `session_id`, `field(name="password", label='Пароль 2FA',
type='password', id='password', required=true, placeholder='Пароль двухфакторной аутентификации',
autocomplete='off', error=password_error)` БЕЗ `value` (D-08: пароль не эхается, поле приходит
пустым), `button('Подтвердить')`. Постоянный `id='password'` — QUAL-06: после подмены 422 фокус
возвращается на поле. Комментарий над формой — перенос комментария T-06-02 о пароле из снятого
шаблона и строка «ошибка поля — у формы (D-12 Фазы 8), 422 подменяет эту же цель правилом блока
конфигурации».

**2. Обработчик `accounts_connect_tg_user_verify_2fa` (D-01, D-08, D-09, D-10, D-11).**
`@router.post(..., response_class=HTMLResponse)`; порядок тела: пользователь → `respond(request,
redirect="/login")`; поля читаются из `await request.form()` в теле (`session_id`, `password`),
не `Form(...)` (Pitfall 8). Пароль пустой после отсечения пробелов → `respond_field_error(request,
page=_page, fragment=_fragment)` с `TG_EMPTY_PASSWORD_MESSAGE = "Введите пароль"`: `_page` — страница
мастера (`accounts/connect_tg_user.html`) с `step="password"`, `session_id`, `password_error`;
`_fragment` — `_tg_step_markup(step="password", session_id=…, password_error=…)`; пароль в контекст
НЕ передаётся ни в одном из двух. Статус сессии `get_qr_status(session_id)["status"]`: `expired` →
шаг `error` с `TG_SESSION_EXPIRED_MESSAGE`; не `needs_2fa` → шаг `error` с `TG_AUTH_FAILED_MESSAGE`.
Затем `await submit_2fa(session_id, password)`: `ValueError` → тот же выход 422 с `str(e)` у поля
(«Неверный пароль 2FA.» дословно, D-09); `RuntimeError` → шаг `error` с `str(e)`; любое иное
исключение → `logger`-запись без пароля и шаг `error` с `TG_AUTH_FAILED_MESSAGE` (Pitfall 5: 500 в
этом месте поднимал бы общий баннер отказа). Возврат `submit_2fa` НЕ используется для записи:
`session_string = await complete_auth(session_id)`; непустая → `await _save_tg_account(db, user,
session_string)` и шаг `connected`; пустая (проигравший гонку) → шаг `error` с
`TG_SESSION_EXPIRED_MESSAGE` (13-04 сменит его на «не найдена»). Все исходы кроме 422 —
`respond(request, redirect="/accounts/connect/tg_user", fragment=<сборщик ИМЕНЕМ>)`. Докстринг
называет: D-08, Pitfall 3 (почему строка из `complete_auth`), Pitfall 5, путь деградации D-11.

**3. Опрос: ветка `needs_2fa`.** В `accounts_connect_tg_user_qr_status` статус `needs_2fa` отвечает
шагом `password` с `session_id`; запасная ветка «иной статус → `TG_AUTH_FAILED_MESSAGE`» остаётся
для статусов, которых машина не знает.

**4. Тесты (D-01).** Фикстура гонок заводит движок `sqlite+aiosqlite` на ВРЕМЕННОМ ФАЙЛЕ
(`tmp_path`), фабрику сессий и переопределение `get_db`, отдающее НОВУЮ сессию на каждый запрос,
плюс вход пользователя; основание в докстринге: общая сессия фикстуры `db_session` означала бы, что
два конкурентных запроса делят один `AsyncSession`, чего SQLAlchemy не допускает, и тест краснел
бы по чужой причине либо сериализовался. JSON-тесты `verify-2fa` из модуля снимаются — их предмет
переписан тестами из `<behavior>`. Клиент Telethon в посевах: `AsyncMock` с `session =
MagicMock()`, иначе `session.save()` вернёт корутину вместо строки.
  </action>

  <verify>
    <automated>uv run pytest tests/test_routes/test_tg_user_auth.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_routes/test_tg_user_auth.py -q -p no:randomly -k "concurrent or second_poll"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'step == "password"' app/templates/accounts/includes/tg_connect_step.html` возвращает `1`; `grep -c "action='/accounts/connect/tg_user/verify-2fa'" app/templates/accounts/includes/tg_connect_step.html` возвращает `1`.
- `grep -c 'TG_EMPTY_PASSWORD_MESSAGE = "Введите пароль"' app/pages/accounts.py` возвращает `1`.
- `awk '/^async def accounts_connect_tg_user_verify_2fa/,/^@router/' app/pages/accounts.py | grep -c 'respond_field_error'` не меньше `1`; `awk '/^async def accounts_connect_tg_user_verify_2fa/,/^@router/' app/pages/accounts.py | grep -c 'await complete_auth('` возвращает `1`; `awk '/^async def accounts_connect_tg_user_verify_2fa/,/^@router/' app/pages/accounts.py | grep -c '_save_tg_account('` возвращает `1`.
- Запись аккаунта живёт в одном месте: `awk '/^async def _save_tg_account/{f=1;print;next} f&&/^(async )?def |^@router|^[A-Z_]+ = /{f=0} f' app/pages/accounts.py | grep -c 'MessengerAccount('` возвращает `1`; `awk '/^async def accounts_connect_tg_user_verify_2fa/,/^@router/' app/pages/accounts.py | grep -c 'MessengerAccount('` возвращает `0`; `awk '/^async def accounts_connect_tg_user_qr_status/,/^@router/' app/pages/accounts.py | grep -c 'MessengerAccount('` возвращает `0`.
- `uv run pytest tests/test_routes/test_tg_user_auth.py -q -p no:randomly -k "concurrent"` — два правила гонки исполнены и зелены.
  </acceptance_criteria>

  <done>
Человек с 2FA проходит шаг пароля на фрагментах; неверный и пустой пароль — 422 у поля без эха;
аккаунт сохраняет одно место записи из результата `complete_auth`; обе гонки дают ровно один аккаунт.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Перечни слоя ответа для `verify-2fa`</name>

  <files>
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py` — `NOT_YET_CONVERTED`, летопись `NOT_YET_CONVERTED_COUNT`,
  `FRAGMENT_RESPONSE_HANDLERS` и его число (строки 13-01 в летописях — образец формы).
- `tests/test_pages/test_htmx_post_pairs.py` — раздел посева мастера TG из 13-01, случаи TG,
  `POST_PAIR_CASES_DECLARED`, `PAIRED_302_ASSERTIONS_DECLARED` и их летописи.
- `tests/test_pages/test_hx_location_destinations.py` — летопись и значение
  `HX_LOCATION_DESTINATION_CALLS_DECLARED`.
  </read_first>

  <action>
Числа — ПРОГОНОМ покрасневшего правила, летопись «Фаза 13, план 13-02» с дословным выводом отказа.
**1. `test_htmx_gates.py`.** Ключ `accounts_connect_tg_user_verify_2fa` снимается из
`NOT_YET_CONVERTED` ПЕРЕВОДОМ (ожидание 12 → 11) и вносится в `FRAGMENT_RESPONSE_HANDLERS` с
обоснованием (ожидание 15 → 16).
**2. `test_htmx_post_pairs.py`.** Константа `ACCOUNTS_CONNECT_TG_USER_VERIFY_2FA`; два случая:
неизвестная сессия с непустым паролем — `FRAGMENT` с меткой
`hx-post="/accounts/connect/tg_user/start-qr"` и `landing="/accounts/connect/tg_user"`; без сессии —
`LOCATION` на `/login`. Ветка 422 в реестр не входит — по границе обхода, записанной у случаев
профиля и MAX. `POST_PAIR_CASES_DECLARED` и `PAIRED_302_ASSERTIONS_DECLARED` — прогоном.
**3. `test_hx_location_destinations.py`.** `HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном
(ожидание 75 → 76: один `respond(request, redirect="/login")` без фрагмента).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_inventory.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_markup_security.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_impersonation_gate.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_preserved.py tests/test_pages/test_htmx_post_pairs.py tests/test_routes/test_tg_user_auth.py tests/test_messengers/test_telegram_user.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'ACCOUNTS_CONNECT_TG_USER_VERIFY_2FA' tests/test_pages/test_htmx_post_pairs.py` не меньше `2`.
- `grep -c 'Фаза 13, план 13-02' tests/test_pages/test_htmx_gates.py` не меньше `2`; не меньше `1` в `tests/test_pages/test_htmx_post_pairs.py` и в `tests/test_pages/test_hx_location_destinations.py`.
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q -p no:randomly -k "every_converted_handler_has_a_pair"` — зелено.
  </acceptance_criteria>

  <done>
Перевод `verify-2fa` отражён в отставании, фрагментных обработчиках, парах и назначениях переходов;
числа поставлены прогоном с летописью.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | поля формы `session_id` и `password` |
| страничный слой → Telethon | `sign_in(password=…)` на клиенте живой сессии |
| слой сессий → база | запись `MessengerAccount` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Tampering | `accounts_connect_tg_user_verify_2fa` и опрос — двойное сохранение под гонкой (Pitfall 3) | medium | mitigate | Строка сессии берётся ТОЛЬКО из результата `complete_auth` (`pop` до первого `await`); одно место записи `_save_tg_account`; правила `test_two_concurrent_password_submits_save_one_account` и `test_two_concurrent_polls_after_success_save_one_account` |
| T-13-05 | Information Disclosure | ответ 422 шага пароля и журнал | high | mitigate | Пароль в контекст шаблона не передаётся ни на странице, ни во фрагменте; поле без `value`; тест утверждает отсутствие присланной строки в теле; запись журнала на ветке ошибки Telethon не содержит пароля |
| T-13-08 | Denial of Service | необработанное исключение Telethon → 500 → баннер отказа (Pitfall 5) | low | mitigate | Иные исключения `submit_2fa` превращаются во фрагмент `error` с формой «Начать заново» |
</threat_model>

<verification>
- После каждой задачи — её `<verify>`; последняя команда задачи 2 — прогон гейтов волны.
- `uv run python -m compileall -q app main.py tests`; `graphify update .`.
</verification>

<success_criteria>
1. Шаг пароля 2FA работает на фрагментах; 422 у поля без эха пароля на обоих транспортах.
2. Ровно один аккаунт на сканирование на обоих путях сохранения — доказано конкурентными тестами без
   подмены `complete_auth`.
3. Перечни слоя ответа сдвинуты прогоном.
</success_criteria>

<output>
Create `.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-02-SUMMARY.md` when done.
</output>
