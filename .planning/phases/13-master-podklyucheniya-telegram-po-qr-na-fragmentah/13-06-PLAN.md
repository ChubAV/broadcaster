---
phase: 13-master-podklyucheniya-telegram-po-qr-na-fragmentah
plan: 06
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FETCH-02]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: sample_count 0, factor 1; confidence выведен, а не самооценён.
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "У критерия 1 Фазы 13 в `.planning/ROADMAP.md` стоит летопись «пять маршрутов → четыре на фрагментах, пятый (`complete`) снят по D-01, опрос сменил метод GET → POST»; текст критерия не переписан (D-01)"
    - "У критерия 2 стоит летопись: опросчик — форма внутри фрагмента ожидания (поправка посылки D-05), гейт опросов разметки слеп к опросчику из макроса, критерий держится правилом на отрисованных ответах (D-05)"
    - "У критерия 3 и у FETCH-02 в `.planning/REQUIREMENTS.md` стоит летопись «проверка владения не сохранена, а заведена, потому что её не было»; тексты критерия и требования не переписаны (D-04)"
    - "Критерий 5 записан прямо: деградации без JS у мастера нет и сегодня — сегодняшняя страница не несла ни одной формы, и кнопка старта без сценария не делала ничего; веха на этом участке регрессировать не может (D-11). Запись стоит летописью у критерия 5 и разделом в SUMMARY плана, с командой и выводом замера"
    - "FETCH-02 НЕ отмечен выполненным: флажок `[ ]` и ячейка `Pending` не тронуты — отметка следует за верификацией фазы"
    - "Записи разведки `.planning/research/*` не правятся (D-01, D-04)"
  artifacts:
    - ".planning/ROADMAP.md — летописи критериев 1, 2, 3 и 5 раздела Фазы 13"
    - ".planning/REQUIREMENTS.md — летопись FETCH-02"
  key_links:
    - "летопись критерия 3 ↔ летопись FETCH-02: ломается — верификатор читает «сохранена» буквально и ищет проверку, которой до фазы не было"
  prohibitions:
    - statement: "текст критерия ROADMAP и текст требования не переписываются — рядом ставится летопись (идиома D-30/D-32 проекта)"
      status: flagged-unverified
      verification: none
---

<objective>
Записи фазы приводятся к сделанному: летописи у критериев 1, 2, 3 и 5 Фазы 13 в ROADMAP и у
FETCH-02 в REQUIREMENTS — без переписывания самих текстов, по идиоме «прежнее значение не было
ошибкой — оно устарело».

Purpose: три посылки записей устарели решениями фазы и одна была ложной с самого начала —
«проверка владения СОХРАНЕНА» (D-04: её не было). Верификатор, читающий критерий без летописи,
искал бы пять маршрутов, опросчик на якоре и сохранённую проверку.

Output: четыре абзаца летописи в ROADMAP, один в REQUIREMENTS, документная запись критерия 5.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-CONTEXT.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Шаблон (новый, 13-01):** `app/templates/accounts/includes/tg_connect_step.html`; якорь
`tg-connect-step`; ветки `start`, `waiting`, `qr_expired`, `password`, `connected`, `error`.

**`app/pages/accounts.py`:** `TG_CONNECT_STEP_TEMPLATE`, `_tg_step_markup`, `_save_tg_account`,
`TG_API_NOT_CONFIGURED_MESSAGE`, `TG_AUTH_FAILED_MESSAGE`, `TG_SESSION_EXPIRED_MESSAGE`,
`TG_SESSION_NOT_FOUND_MESSAGE`, `TG_REFRESH_FAILED_MESSAGE`, `TG_EMPTY_PASSWORD_MESSAGE`.

**`app/messengers/telegram_user.py`:** `QRAuthState.user_id`, `_owned`, статусы `qr_expired` и
`gone`, сигнатуры с `user_id`.

**Тесты:** `POLLING_CASES`, `STEP_MARKS`, `test_polling_stops_by_a_response_without_trigger`, правила
замыкания и контроли (13-05); фикстура гонок (13-02); ключи пар `ACCOUNTS_CONNECT_TG_USER_*`.

**Записи (этот план):** летописи критериев 1, 2, 3, 5 Фазы 13 в `.planning/ROADMAP.md`, летопись
FETCH-02 в `.planning/REQUIREMENTS.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Задача 1: Летописи критериев 1, 2, 3 и 5 Фазы 13 в ROADMAP</name>

  <files>
.planning/ROADMAP.md
  </files>

  <read_first>
- `.planning/ROADMAP.md` — раздел `### Phase 13:` целиком (критерии 1–5, строка `**Plans**`, перечень
  планов) и раздел `### Phase 11:` (строки `**Летопись критерия N: …** (Фаза 11, план 11-20, D-…)`
  после перечня критериев — ОБРАЗЕЦ формы: полужирный заголовок, скобка «фаза, план, решения»,
  абзац, «⚠️ ПРОГНОЗ НЕ БЫЛ ОШИБКОЙ — ОН УСТАРЕЛ», «Записи разведки НЕ ПРАВЯТСЯ»).
- `13-CONTEXT.md` D-01, D-04, D-05, D-11, D-12.
- `13-01-SUMMARY.md` … `13-05-SUMMARY.md` — фактические числа и формулировки, поставленные
  прогонами (`NOT_YET_CONVERTED_COUNT`, `MANUAL_FETCH_*`, слепота гейта опросов, тексты отказа).
- `tests/test_planning/test_state_progress_matches_roadmap.py:103-135` — какую прозу строки
  `**Plans**` правило читает (строка `**Plans**` этим планом НЕ правится).
  </read_first>

  <action>
Абзацы ставятся в раздел Фазы 13 ПОСЛЕ перечня критериев и ДО строки `**Plans**`, в форме летописей
Фазы 11; тексты критериев не трогаются.

**Летопись критерия 1: «пять маршрутов отдают фрагменты» → четыре маршрута на фрагментах, пятый
снят** (Фаза 13, план 13-06, D-01, D-12). Содержание: маршрут завершения снят решением владельца
D-01 — аккаунт сохраняет запрос опроса, первым увидевший успех, поэтому опрос пишет в базу и сменил
метод GET → POST при прежнем адресе и прежнем имени обработчика; четыре маршрута (старт, опрос,
обновление кода, вход с паролем 2FA) отвечают фрагментами шага; остаточное окно ~3 с между
сканированием и опросом принято владельцем; из шаблона сняты сценарий мастера, три
атрибута-обработчика (долг R-08-02 закрыт), четыре скрытые секции и блок ошибки; ручная сборка
запросов в шаблонах — именованный ноль (число из SUMMARY 13-01), утверждение «ноль во всём
`app/templates/`» — FETCH-03, Фаза 15. «⚠️ ПОСЫЛКА НЕ БЫЛА ОШИБКОЙ — ОНА УСТАРЕЛА решением D-01».
«Записи разведки (`.planning/research/*`) НЕ ПРАВЯТСЯ».

**Летопись критерия 2: доказан на отрисованных ответах** (Фаза 13, план 13-06, D-05). Содержание:
опросчик — форма внутри фрагмента ожидания, целящаяся в постоянный якорь `#tg-connect-step`;
посылка D-05 «идентификатор якоря и запрос опроса на одном элементе» давала вечный опрос (RESEARCH
§Pitfall 1) и поправлена при сохранении замысла; гейт опросов разметки
`tests/test_templates/test_htmx_inventory.py` слеп к опросчику, рождённому макросом (замер 13-01);
критерий держится правилом `test_polling_stops_by_a_response_without_trigger` в
`tests/test_routes/test_tg_user_auth.py`, замкнутым по веткам шаблона шага (13-05); опрос в ожидании
отвечает 204. «⚠️ Верификатору читать критерий 2 вместе с этой летописью».

**Летопись критерия 3: проверка владения не «сохранена», а ЗАВЕДЕНА** (Фаза 13, план 13-06, D-04).
Содержание: до фазы состояние QR-сессии не хранило пользователя, старт его не принимал, обработчики
проверяли только факт входа — любой вошедший с чужим `session_id` сохранял чужой Telegram себе;
посылка критерия и `.planning/research/FEATURES.md:241` («по-прежнему проверяется») была ложной;
фаза привязала сессию к пользователю при старте (под имперсонацией — к субъекту), проверка стоит в
слое сессий до любой мутации, чужой `session_id` получает ответ, побайтно равный ответу на
неизвестный, и не трогает сессию владельца. Та же летопись — у FETCH-02 в `.planning/REQUIREMENTS.md`.

**Летопись критерия 5: деградации без JS здесь нет и сегодня** (Фаза 13, план 13-06, D-11).
Содержание: замер базы фазы командой
`f=$(mktemp) && git show "$(git merge-base HEAD master)":app/templates/accounts/connect_tg_user.html > "$f" && grep -c '<form' "$f"`
(вывод — дословно; ожидание `0`) показывает, что сегодняшняя страница не несла ни одной формы, а кнопка старта
была кнопкой без отправки, работавшей только сценарием; мастер был JS-only на 100 %, и веха на этом
участке регрессировать не может; после фазы каждый POST мастера без JavaScript приземляет 302 на
страницу мастера (путь деградации `respond()`, FOUND-04), но мастера без JavaScript фаза не строит
— возможностей, которых не было, она не заводит. Аудит не вправе записать здесь провал.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning -q</automated>
    <fails_when>non-zero exit with a failing rule other than `test_the_machine_readable_progress_is_derived_from_the_roadmap` (that rule's subject is the orchestrator's tracking fields in STATE.md — if it alone fails, its output is recorded verbatim in the SUMMARY and handed to the orchestrator), or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c '^\*\*Летопись критерия 1: .*(Фаза 13, план 13-06' .planning/ROADMAP.md` возвращает `1`; то же для критериев 2, 3 и 5 — по `1` каждый.
- Тексты критериев не переписаны: `d=$(mktemp) && git diff "$(git merge-base HEAD master)" -- .planning/ROADMAP.md > "$d" && grep -c '^-  [1-5]\. ' "$d"` печатает `0` (ни одна строка перечня критериев не удалена и не заменена; пустой вывод — отказ git, а не согласие).
- Летопись критерия 1 несёт оговорку о разведке: `awk '/^\*\*Летопись критерия 1: .*Фаза 13/' .planning/ROADMAP.md | grep -c 'НЕ ПРАВЯТСЯ'` возвращает `1`.
- `uv run pytest tests/test_planning -q -k "prose_plan_counts"` — зелено: строка `**Plans**` планом не тронута.
  </acceptance_criteria>

  <done>
Раздел Фазы 13 несёт летописи у критериев 1, 2, 3 и 5; тексты критериев прежние.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Летопись FETCH-02 в REQUIREMENTS и запись критерия 5 в артефакте фазы</name>

  <files>
.planning/REQUIREMENTS.md
  </files>

  <read_first>
- `.planning/REQUIREMENTS.md:25-55` — требование FETCH-02 (:51) и образец летописи под требованием
  (`**Летопись FORM-03 и FORM-04: …**` под FORM-03, :30 — отступ, форма, «Та же летопись стоит у …»);
  `:140-155` — таблица трассировки (строка `| FETCH-02 | Phase 13 | Pending |`).
- `tests/test_planning/test_requirement_completion_follows_verification.py:398-420, 933-960` — правила
  «выполнено только после верификации» и «флажок и ячейка согласны».
- `13-CONTEXT.md` D-04.
  </read_first>

  <action>
**1. `.planning/REQUIREMENTS.md` (D-04).** Под строкой FETCH-02, с отступом в два пробела по образцу
летописи FORM-03/FORM-04: **Летопись FETCH-02: «при сохранённой серверной проверке владения» →
проверка ЗАВЕДЕНА** (Фаза 13, план 13-06, D-04). Содержание — то же, что у летописи критерия 3
ROADMAP, сжато: проверки до фазы не было, посылка была ложной, а не устаревшей; фаза завела
привязку сессии к пользователю при старте и проверку в слое сессий до любой мутации; чужой
`session_id` отвечает как неизвестный. «Та же летопись стоит у критерия 3 Фазы 13 в
`.planning/ROADMAP.md`». Флажок `[ ]` строки FETCH-02 и ячейка `Pending` таблицы трассировки НЕ
трогаются — отметку ставит закрытие фазы после верификации.

**2. Запись критерия 5 в SUMMARY плана (D-11).** SUMMARY этого плана несёт раздел
`## Критерий 5: деградации без JS нет и сегодня` — команда замера базы (та же, что в летописи
критерия 5), её вывод дословно, и ссылка на летопись критерия 5 в ROADMAP. Это та «документная
запись в артефактах фазы», которую требует критерий 5.

**3. Если `tests/test_planning` краснеет по чужой причине.** Правило
`test_the_machine_readable_progress_is_derived_from_the_roadmap` сверяет поля трекинга
`.planning/STATE.md`, которые ведёт оркестратор (память проекта «ROADMAP mark desyncs STATE
progress»): его отказ, не вызванный этим планом, записывается в SUMMARY дословно и передаётся
оркестратору; `.planning/STATE.md` этот план не правит.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning -q</automated>
    <fails_when>non-zero exit with a failing rule other than `test_the_machine_readable_progress_is_derived_from_the_roadmap` (tracking-owned, recorded verbatim in the SUMMARY if it alone fails), or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_planning -q -k "requirement or flag_and_the_status"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c '^  \*\*Летопись FETCH-02: .*(Фаза 13, план 13-06, D-04)' .planning/REQUIREMENTS.md` возвращает `1`.
- `grep -c '^- \[ \] \*\*FETCH-02\*\*' .planning/REQUIREMENTS.md` возвращает `1`; `grep -c '| FETCH-02 | Phase 13 | Pending |' .planning/REQUIREMENTS.md` возвращает `1`.
- `git diff --quiet "$(git merge-base HEAD master)" -- .planning/research/` — код 0: записи разведки не тронуты.
- `.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-06-SUMMARY.md` содержит строку `## Критерий 5: деградации без JS нет и сегодня` (проверяется после записи SUMMARY: `grep -c '^## Критерий 5: деградации без JS нет и сегодня' …/13-06-SUMMARY.md` возвращает `1`).
  </acceptance_criteria>

  <done>
FETCH-02 несёт летопись заведённой проверки владения, не отмечен выполненным; критерий 5 записан
документно в артефакте фазы с замером базы.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| записи планирования → верификатор и аудит вехи | критерии и требования читаются как контракт приёмки |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-17 | Repudiation | критерий 3 и FETCH-02 утверждают «сохранённую» проверку, которой не было — ложное основание приёмки | low | mitigate | Летописи у критерия 3 и FETCH-02 называют проверку заведённой; текст не переписывается, чтобы история посылки осталась читаемой |
| T-13-18 | Tampering | преждевременная отметка FETCH-02 выполненным | low | mitigate | План флажок и ячейку не трогает; `test_requirement_completion_follows_verification.py` краснеет на отметке раньше верификации |
</threat_model>

<verification>
- `uv run pytest tests/test_planning -q` — после каждой задачи (исполнители обычно отбирают суиту
  без `tests/test_planning/`, и только этот прогон видит дефекты записей).
</verification>

<success_criteria>
1. Летописи у критериев 1, 2, 3, 5 Фазы 13 и у FETCH-02 стоят; тексты не переписаны.
2. FETCH-02 не отмечен выполненным; записи разведки не тронуты.
3. Критерий 5 записан документно с замером базы.
</success_criteria>

<output>
Create `.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-06-SUMMARY.md` when done.
</output>
