---
phase: 13-master-podklyucheniya-telegram-po-qr-na-fragmentah
plan: 03
type: tdd
wave: 3
depends_on: ["13-02"]
files_modified:
  - app/messengers/telegram_user.py
  - app/pages/accounts.py
  - app/templates/accounts/includes/tg_connect_step.html
  - tests/test_messengers/test_telegram_user.py
  - tests/test_routes/test_tg_user_auth.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
autonomous: true
requirements: [FETCH-02]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: sample_count 0, factor 1; confidence выведен, а не самооценён.
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Таймаут `QRLogin.wait()` (истёк токен QR, ~30 с) переводит сессию в статус `qr_expired` без текста ошибки и без записи `logger.error` с трассировкой; сессия остаётся жива (D-03)"
    - "Опрос в статусе `qr_expired` отвечает 200 и шагом «QR-код истёк. Обновите его, чтобы продолжить.» с формой `hx-post=\"/accounts/connect/tg_user/refresh-qr\"`, скрытым `session_id` и кнопкой «Обновить QR-код», без `hx-trigger` — опрос остановлен; автоматического обновления нет (D-02)"
    - "`refresh-qr` для сессии в `qr_expired` в пределах срока отвечает 200 и шагом ожидания с НОВЫМ QR и формой-опросчиком `hx-trigger=\"every 3s\"` — опрос возобновлён (D-02)"
    - "`refresh-qr` для сессии старше `QR_SESSION_TTL` отвечает шагом `error` «Сессия авторизации истекла. Начните заново.» с формой старта; `recreate` не вызывается, устаревшая сессия не оживает (D-03, RESEARCH Pitfall 2)"
    - "`refresh_qr` пускает пересоздание ТОЛЬКО из статуса `qr_expired`: из `waiting`, `needs_2fa`, `success` возвращает None, не зовёт `recreate` и не трогает статус; обработчик отвечает «Не удалось обновить QR. Начните заново.» (D-09)"
    - "Слой сессий изменён ровно на классификацию таймаута и две проверки `refresh_qr`: `QR_SESSION_TTL = 300`, `_cleanup_expired_sessions` и класс `TelegramUserMessenger` не тронуты (D-13)"
    - "`refresh-qr` без признака htmx — 302 на `/accounts/connect/tg_user`; без сессии входа — `/login` на обоих транспортах; JSON не отдаётся (D-10, D-11)"
    - statement: "В браузере через ~30 с после выдачи кода экран сам показывает «QR-код истёк» и кнопку; нажатие даёт новый QR и опрос возобновляется; простой на экране «код истёк» дольше ~270 с даёт «Сессия авторизации истекла» — ручной UAT фазы (критерий 4)"
      verification: backstop
  artifacts:
    - "app/messengers/telegram_user.py — ветка asyncio.TimeoutError в _wait_for_qr; проверки статуса и срока в refresh_qr"
    - "app/pages/accounts.py — accounts_connect_tg_user_refresh_qr на respond(); TG_REFRESH_FAILED_MESSAGE; ветка qr_expired опроса"
    - "app/templates/accounts/includes/tg_connect_step.html — ветка qr_expired"
  key_links:
    - "`QRLogin.wait()` → `asyncio.TimeoutError` → `_wait_for_qr` → `state.status = \"qr_expired\"` → опрос → шаг «код истёк»: ломается — через ~30 с человек видит «Ошибку авторизации» без выхода (сегодняшнее поведение)"
    - "кнопка «Обновить QR-код» → `accounts_connect_tg_user_refresh_qr` → `refresh_qr` (`recreate` + новая задача ожидания) → шаг ожидания с опросчиком: ломается — новый код показан, но опрос не возобновлён, и сканирование не завершается"
  prohibitions:
    - statement: "истёкший код не обновляется сам: забытая вкладка не продолжает обращаться к Telegram (D-02, решение владельца)"
      status: flagged-unverified
      verification: none
---

<objective>
Ветка «код истёк» оживает: таймаут токена QR становится статусом, а не ошибкой (D-03 —
единственное место, где фаза меняет поведение, а не транспорт), опрос показывает «QR-код истёк» с
кнопкой, `refresh-qr` переведён на слой ответа и возобновляет опрос новым кодом (D-02); истёкшая
целиком сессия не оживает.

Purpose: без D-03 критерий 4 («обновление истёкшего кода» на живой сессии) не проходится — сегодня
через ~30 с человек видит «Ошибку авторизации», и выхода из неё нет.

Output: правка `_wait_for_qr` и `refresh_qr`, ветка `qr_expired` шаблона, переведённый
`refresh-qr`, тесты на настоящем `QRLogin`, сдвинутые перечни.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-CONTEXT.md
@.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Шаблон (новый, 13-01):** `app/templates/accounts/includes/tg_connect_step.html`; переменные
`step` (`start`, `waiting`, `qr_expired`, `password`, `connected`, `error`), `session_id`, `qr_code`,
`error`, `password_error`; якорь `tg-connect-step`.

**`app/pages/accounts.py`:** `TG_CONNECT_STEP_TEMPLATE`, `_tg_step_markup`, `_save_tg_account`,
`TG_API_NOT_CONFIGURED_MESSAGE`, `TG_AUTH_FAILED_MESSAGE`, `TG_SESSION_EXPIRED_MESSAGE`,
`TG_EMPTY_PASSWORD_MESSAGE`, `TG_REFRESH_FAILED_MESSAGE` (этот план),
`TG_SESSION_NOT_FOUND_MESSAGE` (13-04).

**`app/messengers/telegram_user.py`:** статус `qr_expired` (этот план); `QRAuthState.user_id`,
`_owned`, статус `gone`, сигнатуры с `user_id` (13-04).

**Тесты:** `POLLING_CASES`, `test_polling_stops_by_a_response_without_trigger`, фикстура гонок;
`ACCOUNTS_CONNECT_TG_USER_REFRESH_QR` (этот план) рядом с ключами 13-01 и 13-02.

**Снимается (этот план):** JSON-контракт `refresh-qr` и его JSON-тесты; последний JSON-обработчик
мастера.
</artifacts_this_phase_produces>

<tdd_notes>
- Коммиты: RED — `test(13-03): …`, GREEN — `feat(13-03): …`.
- RED-улика: pytest с `--junit-xml`, перегон в TAP (память `red-evidence-verb-wants-node-tap`).
- ⚠️ ТЕСТ D-03 ОБЯЗАН ВОСПРОИЗВОДИТЬ `asyncio.TimeoutError`, а не ставить `status="qr_expired"`
  напрямую — иначе он зелен вакуумом (CONTEXT §Landmines). RED задачи 1 измеряется на сегодняшнем
  коде дословно: зонд RESEARCH §Pattern 4 дал `status='error' error=''` — целевое правило
  `test_an_expired_qr_token_is_a_status_not_an_error`. Целевое правило RED задачи 2 —
  `test_refresh_qr_does_not_revive_an_outdated_session`: сегодняшний `refresh_qr` срок не
  проверяет и пересоздаёт код, и правило падает на утверждении «`recreate` не ожидался». Правила
  маршрута на JSON-обработчике могут упасть исключением разбора тела и целью не назначаются.
- Предусловие UAT (не машины): часы стенда синхронизированы NTP — `wait()` считает таймаут по
  локальным часам против серверного `expires` (RESEARCH §Pitfall 6).
</tdd_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: «QR-код истёк» — статус, а не ошибка, и шаг с кнопкой вместо тупика</name>

  <files>
app/messengers/telegram_user.py,
tests/test_messengers/test_telegram_user.py,
app/pages/accounts.py,
app/templates/accounts/includes/tg_connect_step.html,
tests/test_routes/test_tg_user_auth.py
  </files>

  <read_first>
- `app/messengers/telegram_user.py:24-112` — `QR_SESSION_TTL`, `QRAuthState` (комментарий статусов
  :35), `_cleanup_expired_sessions`, `_wait_for_qr` (:77-97, общая ветка `except Exception` —
  корень D-03), `get_qr_status`.
- Исходник Telethon `telethon/tl/custom/qrlogin.py` в `.venv` (путь:
  `uv run python -c "import telethon.tl.custom.qrlogin as m; print(m.__file__)"`), строки 16-105 —
  `QRLogin.__init__`, `recreate`, `url`, `expires`, `wait` (таймаут = `expires − now`, событие через
  `add_event_handler`/`remove_event_handler`, `asyncio.wait_for`).
- `tests/test_messengers/test_telegram_user.py:1-30` (импорты) и `:328-392` (тесты QR-входа).
- `app/pages/accounts.py` — `accounts_connect_tg_user_qr_status` после 13-02.
- `app/templates/accounts/includes/tg_connect_step.html` после 13-02.
- `tests/test_routes/test_tg_user_auth.py` — `POLLING_CASES`, автоматическая фикстура модуля.
- `13-RESEARCH.md` строки 324-357 (§Pattern 4), 542-566 (§Code Examples: классификация и тест D-03),
  494-495 (§Pitfall 6).
  </read_first>

  <behavior>
RED:
1. `tests/test_messengers/test_telegram_user.py::test_an_expired_qr_token_is_a_status_not_an_error` —
   НАСТОЯЩИЙ `QRLogin(client, [])` с `_resp = SimpleNamespace(expires=now + 0.05 с, token=b"x")`,
   клиент — заглушка с синхронными `add_event_handler`/`remove_event_handler`;
   `QRAuthState(client=client, qr_login=q)` в `_qr_sessions`; `await _wait_for_qr(sid)`;
   `state.status == "qr_expired"`, `not state.error`; подменённый `logger` модуля — `error` не вызван.
2. `tests/test_routes/test_tg_user_auth.py::test_polling_an_expired_code_offers_a_refresh` — состояние
   `qr_expired`: 200, «QR-код истёк. Обновите его, чтобы продолжить.», форма
   `hx-post="/accounts/connect/tg_user/refresh-qr"` со скрытым `session_id`, кнопка «Обновить
   QR-код», `hx-trigger` нет.
3. Запись `POLLING_CASES`: опрос `qr_expired` — без опроса.
GREEN — ветка `except asyncio.TimeoutError`, ветка шаблона, ветка опроса.
  </behavior>

  <action>
**1. `_wait_for_qr` (D-03, D-13).** Между `except asyncio.CancelledError: pass` и общим
`except Exception as e:` — ветка `except asyncio.TimeoutError:` со строкой
`state.status = "qr_expired"`; ни записи журнала уровня ошибки, ни трассировки в ней нет
(нормальное истечение не логируется как ошибка с трассировкой; запись уровня `info` без
`session_id` допустима, но не обязательна). Комментарий ветки: токен QR живёт ~30 с (`expires` Telegram), а сессия — 300 с; это
разные сроки, и истёкший токен — статус «код истёк», а не ошибка (D-03). На Python 3.12
`asyncio.TimeoutError is TimeoutError` — одна ветка ловит оба имени. `SessionPasswordNeededError`
приходит ПОСЛЕ события, а не из таймаута, и остаётся в своей ветке. Комментарий статусов у
`QRAuthState.status` дополняется значением `qr_expired`. `QR_SESSION_TTL`,
`_cleanup_expired_sessions`, `get_qr_status` и класс `TelegramUserMessenger` не трогаются (D-13).

**2. Ветка `qr_expired` шаблона шага (D-02).** Абзац «QR-код истёк. Обновите его, чтобы
продолжить.» (текст D-02/D-03 — решение планировщика по рекомендации RESEARCH §Open Questions 3,
живёт в шаблоне, единственным местом); блочный вызов
`form_wrapper(action='/accounts/connect/tg_user/refresh-qr', target='#tg-connect-step',
swap='innerHTML')` со скрытым полем `session_id` и `button('Обновить QR-код')`; ссылка «Отмена».
Истёкший QR на шаге НЕ показывается: ответ опроса адреса кода не несёт, а мёртвый код приглашал бы
к бесполезному сканированию — комментарий ветки называет это.

**3. Опрос.** `accounts_connect_tg_user_qr_status`: статус `qr_expired` → шаг `qr_expired` с
`session_id`. До задачи 2 кнопка шага шлёт в JSON-обработчик — окно внутри плана.

**4. Тесты.** Тест D-03 строится на настоящем `QRLogin` по RESEARCH §Code Examples; подмена
`wait()` значением `status` запрещена (CONTEXT §Landmines).
  </action>

  <verify>
    <automated>uv run pytest tests/test_messengers/test_telegram_user.py -q -p no:randomly -k "expired"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
    <automated>uv run pytest tests/test_routes/test_tg_user_auth.py tests/test_messengers/test_telegram_user.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'except asyncio.TimeoutError' app/messengers/telegram_user.py` возвращает `1`; `grep -c 'state.status = "qr_expired"' app/messengers/telegram_user.py` возвращает `1`.
- `awk '/except asyncio.TimeoutError/,/except Exception/' app/messengers/telegram_user.py | grep -c 'logger.error'` возвращает `0`.
- `grep -cE 'QR_SESSION_TTL =[[:space:]]*300\b' app/messengers/telegram_user.py` возвращает `1` — срок сессии не тронут (D-13).
- `grep -c 'step == "qr_expired"' app/templates/accounts/includes/tg_connect_step.html` возвращает `1`; `grep -c 'Обновить QR-код' app/templates/accounts/includes/tg_connect_step.html` не меньше `1`.
- `grep -c 'QRLogin(' tests/test_messengers/test_telegram_user.py` не меньше `1` — тест D-03 на настоящем объекте Telethon.
  </acceptance_criteria>

  <done>
Истёкший токен QR — статус `qr_expired` без ошибки и без трассировки в журнале; опрос отвечает шагом
«QR-код истёк» с кнопкой и останавливается.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: «Обновить QR-код» — новый код и возобновлённый опрос; устаревшая сессия не оживает</name>

  <files>
app/messengers/telegram_user.py,
tests/test_messengers/test_telegram_user.py,
app/pages/accounts.py,
tests/test_routes/test_tg_user_auth.py
  </files>

  <read_first>
- `app/messengers/telegram_user.py:115-134` — `refresh_qr` (срок не проверяет, статус не проверяет).
- `app/pages/accounts.py` — сегодняшний JSON `accounts_connect_tg_user_refresh_qr` и блок мастера TG
  после задачи 1.
- `tests/test_routes/test_tg_user_auth.py` — JSON-тесты `refresh-qr` (снимаются), `POLLING_CASES`.
- `13-RESEARCH.md` строки 355-357 (§Pattern 4, механика `refresh_qr`), 480-482 (§Pitfall 2).
  </read_first>

  <behavior>
RED:
1. `test_refresh_qr_recreates_only_an_expired_code` (слой сессий) — из `qr_expired` в сроке:
   `recreate` ожидан один раз, статус `waiting`, `created_at` обновлён, новая задача ожидания,
   возврат — адрес кода. Из `waiting`, `needs_2fa`, `success`: None, `recreate` не ожидался, статус
   прежний.
2. `test_refresh_qr_does_not_revive_an_outdated_session` (слой сессий) — `created_at` старше
   `QR_SESSION_TTL`: None, `recreate` не ожидался.
3. `test_refreshing_an_expired_code_resumes_polling` (маршрут, СКВОЗНОЙ срез слайса) — настоящий
   `QRLogin` с коротким `expires` и настоящий `_wait_for_qr` задачей; клиент — заглушка, чей вызов
   отдаёт новый ответ токена (`token=b"new"`, `expires` через 30 с); опрос после истечения → шаг
   «код истёк»; POST `refresh-qr` → 200, новый `data:image/png;base64,`, ровно одно
   `hx-trigger="every 3s"`, то же `session_id` скрытым полем; следующий опрос → 204.
4. `test_refreshing_an_outdated_session_says_it_expired` — сессия старше срока в `qr_expired`: шаг
   `error` «Сессия авторизации истекла. Начните заново.», форма старта, `hx-trigger` нет.
5. `test_refreshing_a_non_expired_code_is_refused` — сессия в `waiting`: шаг `error` «Не удалось
   обновить QR. Начните заново.», `recreate` не вызван.
6. `test_refresh_degrades_and_requires_a_session` — без htmx → 302 на `/accounts/connect/tg_user`
   (адрес литералом в `.post(…)`); без cookie → `/login` на обоих транспортах; неизвестная сессия →
   шаг `error` «Сессия авторизации истекла. Начните заново.».
7. Записи `POLLING_CASES`: `refresh-qr` успех (С опросом), сессия старше срока, не истёкший код,
   неизвестная сессия (без опроса).
GREEN — проверки `refresh_qr` и обработчик.
  </behavior>

  <action>
**1. `refresh_qr` (D-03, D-13).** В начале, ДО `recreate`: состояния нет или нет `qr_login` →
None (как сегодня); `state.status != "qr_expired"` → None (пересоздание пускается только из «код
истёк»: подделанный запрос из `success`/`needs_2fa` иначе вернул бы сессию в `waiting` и стёр бы
готовый вход — RESEARCH §Pattern 4); `time.time() - state.created_at > QR_SESSION_TTL` → None
(Pitfall 2: чистка по сроку зовётся только из `start_qr_auth`, и без этой проверки устаревшая
сессия оживала бы). Механика пересоздания (`recreate` → `waiting` → `created_at` → отмена старой
задачи → новая `_wait_for_qr`) не меняется. Комментарий называет: 300 с отсчитываются от ПОСЛЕДНЕГО
выпуска кода, потому что `created_at` сбрасывается здесь.

**2. Обработчик `accounts_connect_tg_user_refresh_qr` (D-02, D-03, D-09, D-10, D-11).**
`@router.post(..., response_class=HTMLResponse)`; пользователь → `respond(request,
redirect="/login")`; `session_id` из `await request.form()` в теле. Классификация
`get_qr_status(session_id)["status"]`: `expired` → шаг `error` с `TG_SESSION_EXPIRED_MESSAGE` (D-03:
«сессия истекла целиком» — существующий текст дословно, D-09); не `qr_expired` → шаг `error` с
`TG_REFRESH_FAILED_MESSAGE = "Не удалось обновить QR. Начните заново."` (текст сегодняшнего
обработчика дословно). Затем `new_url = await refresh_qr(session_id)`: None →
`TG_REFRESH_FAILED_MESSAGE`; адрес → шаг `waiting` с `session_id` и
`qr_code=_generate_qr_base64(new_url)` — опрос возобновляется тем же опросчиком (D-02).
Каждый исход — `respond(request, redirect="/accounts/connect/tg_user", fragment=<сборщик ИМЕНЕМ>)`.
Докстринг называет D-02 (кнопка, не автообновление), D-03, путь деградации D-11. JSON-тесты
`refresh-qr` снимаются — их предмет переписан тестами из `<behavior>`.

**3. Тестовые двойники.** Для правил слоя сессий — объект кода с `recreate` (`AsyncMock`) и
атрибутом `url`; для сквозного теста — настоящий `QRLogin`, клиент которого — `AsyncMock`
(вызов отдаёт `SimpleNamespace(token=…, expires=…)`) с синхронными `MagicMock`
`add_event_handler`/`remove_event_handler`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_messengers/test_telegram_user.py tests/test_routes/test_tg_user_auth.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_routes/test_tg_user_auth.py -q -p no:randomly -k "refresh"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def refresh_qr/,/^async def submit_2fa/' app/messengers/telegram_user.py | grep -c 'qr_expired'` не меньше `1`; `awk '/^async def refresh_qr/,/^async def submit_2fa/' app/messengers/telegram_user.py | grep -c 'QR_SESSION_TTL'` не меньше `1`.
- `grep -c 'TG_REFRESH_FAILED_MESSAGE = "Не удалось обновить QR. Начните заново."' app/pages/accounts.py` возвращает `1`.
- `grep -c '@router.post("/accounts/connect/tg_user/refresh-qr", response_class=HTMLResponse)' app/pages/accounts.py` возвращает `1`.
- Класс адаптера не тронут (D-13): `b=$(mktemp) && git show "$(git merge-base HEAD master)":app/messengers/telegram_user.py > "$b" && diff <(sed -n '/^# Messenger adapter/,$p' "$b") <(sed -n '/^# Messenger adapter/,$p' app/messengers/telegram_user.py)` — пустой вывод, код 0.
- Чистка по сроку не тронута (D-13): `b=$(mktemp) && git show "$(git merge-base HEAD master)":app/messengers/telegram_user.py > "$b" && diff <(awk '/^def _cleanup_expired_sessions/{f=1} /^async def start_qr_auth/{f=0} f' "$b") <(awk '/^def _cleanup_expired_sessions/{f=1} /^async def start_qr_auth/{f=0} f' app/messengers/telegram_user.py)` — пустой вывод, код 0.
  </acceptance_criteria>

  <done>
«Обновить QR-код» даёт новый код и возобновляет опрос; сессия старше срока и не истёкший код
отказываются, не трогая сессию; слой сессий изменён ровно в пределах D-03.
  </done>
</task>

<task type="auto">
  <name>Задача 3: Перечни слоя ответа для `refresh-qr` — отставание мастера пусто</name>

  <files>
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py` — `NOT_YET_CONVERTED` и летопись числа (строки 13-01 и
  13-02), `FRAGMENT_RESPONSE_HANDLERS` и его число.
- `tests/test_pages/test_htmx_post_pairs.py` — раздел посева мастера TG, случаи TG, числа и летописи.
- `tests/test_pages/test_hx_location_destinations.py` — летопись `HX_LOCATION_DESTINATION_CALLS_DECLARED`.
  </read_first>

  <action>
Числа — ПРОГОНОМ покрасневшего правила, летопись «Фаза 13, план 13-03» с дословным выводом.
**1. `test_htmx_gates.py`.** Ключ `accounts_connect_tg_user_refresh_qr` снимается из
`NOT_YET_CONVERTED` ПЕРЕВОДОМ (ожидание 11 → 10) и вносится в `FRAGMENT_RESPONSE_HANDLERS`
(ожидание 16 → 17). Летопись `NOT_YET_CONVERTED_COUNT` получает СВОДНУЮ строку Фазы 13 по форме
сводной летописи Фазы 11: 14 → 10, четыре обработчика мастера словами — старт (перевод, 13-01),
завершение (СНЯТИЕ, D-01, 13-01), вход с паролем 2FA (перевод, 13-02), обновление кода (перевод,
13-03); остаток 10 — ровно десять авторизационных обработчиков Фазы 14.
**2. `test_htmx_post_pairs.py`.** Константа `ACCOUNTS_CONNECT_TG_USER_REFRESH_QR`; случаи:
неизвестная сессия — `FRAGMENT` с меткой `hx-post="/accounts/connect/tg_user/start-qr"`,
`landing="/accounts/connect/tg_user"`; без сессии — `LOCATION` на `/login`. Числа реестра и
утверждений 302 — прогоном.
**3. `test_hx_location_destinations.py`.** `HX_LOCATION_DESTINATION_CALLS_DECLARED` — прогоном
(ожидание 76 → 77).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_inventory.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_markup_security.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_impersonation_gate.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_preserved.py tests/test_pages/test_htmx_post_pairs.py tests/test_routes/test_tg_user_auth.py tests/test_messengers/test_telegram_user.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- В отставании нет ни одного ключа мастера: `uv run python -c "import sys; from tests.test_pages.test_htmx_gates import NOT_YET_CONVERTED as s; sys.exit(1 if any('accounts_connect_tg_user_' in k for k in s) else 0)"` — код 0.
- Фрагментными числятся ровно четыре обработчика мастера: `uv run python -c "import sys; from tests.test_pages.test_htmx_gates import FRAGMENT_RESPONSE_HANDLERS as s; sys.exit(0 if sum('accounts_connect_tg_user_' in k for k in s) == 4 else 1)"` — код 0.
- `grep -c 'ACCOUNTS_CONNECT_TG_USER_REFRESH_QR' tests/test_pages/test_htmx_post_pairs.py` не меньше `2`.
- `grep -c 'Фаза 13, план 13-03' tests/test_pages/test_htmx_gates.py` не меньше `2`.
  </acceptance_criteria>

  <done>
В отставании не осталось ни одного обработчика мастера; четыре переведённых обработчика числятся
фрагментными и имеют пары; числа поставлены прогоном, сводная летопись Фазы 13 записана.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | поле `session_id` формы обновления |
| слой сессий → Telegram | `recreate()` — новый `ExportLoginToken` |
| часы сервера ↔ `expires` Telegram | таймаут `wait()` считается по локальным часам |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12 | Tampering / DoS | `refresh_qr` — оживление устаревшей сессии и сброс готового входа подделанным запросом (Pitfall 2) | medium | mitigate | Пересоздание только из `qr_expired` и только в сроке `QR_SESSION_TTL`; иначе None без `recreate` и без смены статуса; правила слоя сессий утверждают отсутствие вызова |
| T-13-13 | Denial of Service | расхождение часов стенда с `expires` → каждый код мгновенно «истёк» (Pitfall 6) | low | accept | Предусловие UAT: NTP на стенде; поведение до фазы при том же расхождении — мгновенная «Ошибка авторизации», то есть фаза отказ не ухудшает |
| T-13-15 | Information Disclosure | запись `qr_auth_error` с `session_id` и трассировкой на каждом нормальном истечении | low | mitigate | Ветка таймаута не пишет `logger.error` (D-03); `session_id` уходит в журнал реже |
| T-13-07 | Denial of Service | автообновление кода забытой вкладкой | low | mitigate | Обновление только кнопкой; шаг «код истёк» без триггера (D-02) |
</threat_model>

<verification>
- После каждой задачи — её `<verify>`; последняя команда задачи 3 — прогон гейтов волны.
- `uv run python -m compileall -q app main.py tests`; `graphify update .`.
</verification>

<success_criteria>
1. Таймаут токена — статус `qr_expired`, доказано на настоящем `QRLogin`.
2. «Код истёк» → «Обновить QR-код» → новый код и опрос — сквозной тест маршрутов.
3. Устаревшая сессия не оживает; слой сессий изменён в пределах D-03/D-13.
4. Отставание мастера пусто, перечни сдвинуты прогоном.
</success_criteria>

<output>
Create `.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-03-SUMMARY.md` when done.
</output>
