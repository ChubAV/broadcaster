---
phase: 07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/test_templates/test_htmx_inventory.py
  - .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md
autonomous: true
# FOUND-01 закрывается ручной половиной критерия 5 (22 места работают на 2.0.10);
# QUAL-05 — ручной половиной критерия 4 (снимков после выхода не остаётся).
# Машинные половины обоих требований закрыты планом 07-01.
requirements: [FOUND-01, QUAL-05]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Инвентарный гейт утверждает три числа механизмов — 12 мест `hx-trigger=\"revealed\"`, 8 безусловных мест `hx-trigger=\"every Ns\"`, 2 места с атрибутами внутри Jinja-условия — И их сумму 22; появление или исчезновение любого места роняет тест (D-14, D-18)."
    - "Гейт считает МЕСТА, несущие `hx-get`, а не вхождения строки `revealed`: тринадцатое вхождение — прозаический комментарий `app/templates/base.html:241` — в счёт не попадает, и гейт отдельно фиксирует расхождение 13 вхождений против 12 мест, чтобы зелёный по совпадению был невозможен."
    - "Обход шаблонов рекурсивный: 4 из 22 мест живут в подкаталогах `partials/`, и плоский обход каталогов их не увидел бы."
    - "Перечень мест выписан в тесте константами и не выведен из проверяемых шаблонов; утверждений два — перечень против суммы и перечень против исходников, — потому что первое ловит расхождение внутри теста, а второе расхождение теста с кодом."
    - "Комментарий-летопись над константами ПЕРВОЙ записью фиксирует поправку разбивки 10/10/2 → 12/8/2: числа CONTEXT.md не совпали с кодом, и поправка записана как поправка, а не выписана тихо верной."
    - "`07-UAT.md` существует в каталоге фазы, группирует обход по трём механизмам, под каждым несёт адреса экранов, ожидаемое поведение и место отметки закрытия; пункты 1 и 2 перечня из девяти названы в нём поимённо и связаны с критериями 4 и 5 роадмапа (D-15)."
    - "Ни один `hx-*` атрибут этим планом не добавлен, не снят и не отредактирован: обход фиксирует состояние, а не правит его (рамка фазы)."
    - statement: "После выхода из системы ключ снимков истории в localStorage браузера пуст — проверено глазами в DevTools на профиле, в котором снимки БЫЛИ, а не на свежем (критерий 4 роадмапа, пункт 1 перечня из девяти)."
      verification: backstop
    - statement: "Все 22 места `hx-get` работают на рантайме 2.0.10, и каскад infinite scroll на `hx-trigger=\"revealed\"` не воспроизводится — проверено обходом по трём механизмам на десяти экранах (критерий 5 роадмапа, пункт 2 перечня из девяти)."
      verification: backstop
  artifacts:
    - path: "tests/test_templates/test_htmx_inventory.py"
      provides: "Единственный машинный тест, который получает фаза сверх гейтов шелла: инвентарь 22 мест числом по трём механизмам"
      min_lines: 120
    - path: ".planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md"
      provides: "Артефакт ручного обхода: три механизма, адреса экранов, ожидаемое поведение, отметки закрытия; вход в сводное закрытие GATE-09 Фазы 15"
      contains: "Механизм"
  key_links:
    - from: "tests/test_templates/test_htmx_inventory.py"
      to: "app/templates"
      via: "рекурсивный обход всех шаблонов проекта парами «относительный путь — исходник»"
      pattern: "rglob"
    - from: ".planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md"
      to: "tests/test_templates/test_htmx_inventory.py"
      via: "перечень механизмов в артефакте и константы в гейте — один и тот же перечень; расхождение обязано ронять гейт"
      pattern: "22"
  prohibitions:
    - statement: "MUST NOT отмечать пункт обхода закрытым без записи о том, ЧТО наблюдалось: отметка «ок» без наблюдаемого признака превращает единственный ручной инструмент фазы в подпись под непроверенным, а Фаза 15 наследует её как факт — при том, что зелёная суита здесь не значит ничего и роадмап говорит это дословно"
      category: transparency
    - statement: "MUST NOT выводить ожидаемое число мест из проверяемых шаблонов: перечень, посчитанный по коду в момент прогона, согласится с любой правкой и молча переживёт исчезновение места — ровно тот отказ, ради которого инвентарные гейты в проекте и заведены"
      category: transparency
    - statement: "MUST NOT объявлять обход закрытым по одному экрану механизма: пары «список + карточки списка» несут одну разметку, но условная подгрузка (механизм 3) видна только в состоянии синхронизации, и экран, не приведённый в это состояние, обхода не проходил — он показал ту же страницу без атрибутов"
      category: transparency
---

<objective>
Единственная фаза вехи без машинного теста получает свой машинный тест, а её ручная
половина — артефакт, который переживает сброс контекста.

Purpose: критерии 4 и 5 роадмапа закрываются ГЛАЗАМИ по построению — суита не исполняет
ни строчки JS, браузерного стенда в проекте нет и не заводится. Роадмап говорит это
дословно: «зелёная суита здесь не значит ничего». Значит закрытие обязано быть записанным,
а не подразумеваемым: иначе отметка «обошли 22 места» тихо устаревает в тот момент, когда
Фаза 11 добавит двадцать третье, и Фаза 15 собирает перечень заново, уже не зная, что
именно наблюдалось в Фазе 7. Гейт держит ЧИСЛО, артефакт держит НАБЛЮДЕНИЕ, и ни один
из двух не притворяется вторым.

Output: `tests/test_templates/test_htmx_inventory.py` — инвентарь 22 мест числом по трём
механизмам с летописью поправки; `07-UAT.md` — обход по механизмам с адресами десяти
экранов, ожидаемым поведением и отметками закрытия.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-CONTEXT.md
@.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-PATTERNS.md
@.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-01-SUMMARY.md
</context>

## Artifacts this phase produces (план 07-03)

| Символ | Место | Вид |
|--------|-------|-----|
| `tests/test_templates/test_htmx_inventory.py` | `tests/` | новый файл тестов |
| `HxGetSite` | `tests/test_templates/test_htmx_inventory.py` | новый `NamedTuple` (шаблон, механизм, число мест) |
| `REVEALED_SITES` | `tests/test_templates/test_htmx_inventory.py` | новая константа-перечень (механизм 1) |
| `POLL_SITES` | `tests/test_templates/test_htmx_inventory.py` | новая константа-перечень (механизм 2) |
| `CONDITIONAL_SITES` | `tests/test_templates/test_htmx_inventory.py` | новая константа-перечень (механизм 3) |
| `REVEALED_PLACES` = 12 | `tests/test_templates/test_htmx_inventory.py` | новая константа-число |
| `POLL_PLACES` = 8 | `tests/test_templates/test_htmx_inventory.py` | новая константа-число |
| `CONDITIONAL_PLACES` = 2 | `tests/test_templates/test_htmx_inventory.py` | новая константа-число |
| `HX_GET_PLACES` = 22 | `tests/test_templates/test_htmx_inventory.py` | новая константа-сумма |
| `REVEALED_LITERAL_OCCURRENCES` = 13 | `tests/test_templates/test_htmx_inventory.py` | новая константа-ловушка счёта |
| `_all_templates()` | `tests/test_templates/test_htmx_inventory.py` | новый помощник (рекурсивный обход) |
| `_hx_get_lines()` | `tests/test_templates/test_htmx_inventory.py` | новый помощник: строки, несущие `hx-get` |
| `test_hx_get_inventory_matches_declared_mechanisms` | `tests/test_templates/test_htmx_inventory.py` | новый тест (D-18) |
| `test_revealed_literal_count_exceeds_site_count` | `tests/test_templates/test_htmx_inventory.py` | новый тест (ловушка счёта) |
| `07-UAT.md` | `.planning/phases/07-.../` | новый артефакт ручного обхода (D-15) |

Ничего не удаляется. Ни один шаблон, ни один `hx-*` атрибут, ни один обработчик
этим планом не правится.

## Зависимость от плана 07-01 — по существу, а не по порядку

`files_modified` этого плана не пересекается ни с планом 07-01, ни с планом 07-02:
он заводит два новых файла и не трогает ни одного существующего. Зависимость от 07-01
всё же настоящая и в том, что артефакт `07-UAT.md` выписывает наблюдаемые в DevTools
признаки — разобранный атрибут блока конфигурации и строку очистки ключа снимков истории.
Признаки берутся из ОТГРУЖЕННОГО состояния, а не из плана: обход, написанный по
намерению, а не по факту, называет пользователю то, чего он может не увидеть, и первым
же несовпадением обесценивает себя целиком. Инвентарный гейт от 07-01 не зависит и
позеленел бы и до него — но резать план надвое ради одной волны значило бы развести
перечень механизмов и обход по механизмам в два места, а это ОДИН перечень.

<tasks>

<task type="auto">
  <name>Задача 1: Инвентарный гейт 22 мест — числом по трём механизмам, с летописью поправки</name>

  <reversibility rating="reversible">
    Новый файл тестов без потребителей; удаление файла возвращает проект в исходное
    состояние. Числа меняются осознанно и с записью в летописи — в этом и смысл гейта (D-18).
  </reversibility>

  <files>
    tests/test_templates/test_htmx_inventory.py
  </files>

  <read_first>
    .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-PATTERNS.md — § Inventory Corrections ЦЕЛИКОМ: таблица поправки, полный перечень «файл:строка» по трём механизмам, пригодный к дословному переносу, и разбор ловушки счёта,
    tests/test_templates/test_components.py:1-20 — шапка файла и `TEMPLATES_DIR`: база пути к шаблонам уже существует, новая не заводится,
    tests/test_templates/test_components.py:740-760 — форма перечня: именованный кортеж мест с ожидаемым числом на каждое, плюс константа-сумма,
    tests/test_templates/test_components.py:757-800 — форма комментария-летописи над константой: каждое изменение числа сопровождается записью «какой план, что внёс и почему», включая признание не сбывшегося прогноза,
    tests/test_templates/test_components.py:856-875 — `_all_templates()` (рекурсивный обход парами «относительный путь — исходник») и `_macro_calls()` (помощник счёта, СОЗНАТЕЛЬНО исключающий объявление из счёта вызовов) — прямая модель отсечения комментария,
    tests/test_templates/test_components.py:1005-1015 — ДВОЙНОЕ утверждение: сначала перечень против суммы, потом перечень против исходников,
    tests/test_templates/test_components.py:1205-1215 — форма сообщения об отказе, копируемая буквально,
    tests/test_pages/test_access_gate.py:41-52 — комментарий «Перечень выписан ЗДЕСЬ, а не выведен из исходника»,
    tests/test_pages/test_impersonation_gate.py:55-70 — записанная исправленная граница: плоский обход каталогов не увидел бы места в подкаталоге,
    app/templates/base.html:241 — тринадцатое вхождение строки `revealed`: прозаический комментарий, ради которого гейт обязан считать места с `hx-get`, а не вхождения строки
  </read_first>

  <action>
    Завести `tests/test_templates/test_htmx_inventory.py` — инвентарный гейт на 22 места
    `hx-get`, числом по трём механизмам. Файл живёт в `tests/test_templates/`, а не в
    `tests/test_pages/`: это гейт РАЗМЕТКИ, читающий исходники шаблонов, и его канонический
    образец — `test_components.py`, где физически живут `ROW_DELETE_PLACES` и `MODAL_PLACES`.
    Гейты в `tests/test_pages/test_access_gate.py` и `test_impersonation_gate.py` — это гейты
    множеств по синтаксическому дереву Python, форма другая, и копировать нужно не их.

    **Числа берутся с диска, а не из CONTEXT.md.** CONTEXT.md записал разбивку
    `revealed` = 10 / `every Ns` = 10 / условных = 2. По коду разбивка иная: `revealed` = 12,
    безусловных `every Ns` = 8, условных = 2. Сумма 22 в обоих случаях верна, разбивка — нет.
    Гейт, написанный по CONTEXT.md, был бы красным в момент написания. Полный перечень
    «файл:строка» по трём механизмам лежит в `07-PATTERNS.md` § Inventory Corrections и
    переносится оттуда дословно.

    **Ловушка счёта, которую гейт обязан пережить.** Наивный поиск строки `revealed` по
    шаблонам даёт 13 попаданий, а не 12: тринадцатое — прозаический комментарий
    `app/templates/base.html:241`, объясняющий, почему в проекте выключены переходы вида.
    Гейт обязан считать МЕСТА, несущие `hx-get`, а не вхождения строки, иначе он зелен на
    13 по совпадению. Прецедент того же класса решения — помощник счёта вызовов макроса в
    `test_components.py:868-875`, где объявление сознательно исключено из счёта. Завести
    ОТДЕЛЬНЫЙ тест, утверждающий обе величины сразу: вхождений строки 13, мест 12, — тогда
    исчезновение комментария или появление четырнадцатого вхождения тоже станет видимым,
    а не растворится в первом числе.

    **Обход рекурсивный.** 4 из 22 мест живут в подкаталогах `partials/`, и плоский обход
    каталогов их не увидел бы — эта граница в проекте уже записана как исправленная
    (`test_impersonation_gate.py`). Помощник обхода копируется дословно с
    `test_components.py:856-865`: пары «путь относительно каталога шаблонов — исходник»,
    отсортированные.

    **Перечень выписывается константами.** Три кортежа именованных мест — по механизмам —
    где каждый элемент несёт относительный путь шаблона и ожидаемое число мест в нём.
    Три файла несут по ДВА места каждый (`accounts/list.html`, `accounts/partial_cards.html`
    — одно `revealed` и одно `every`; `accounts/connect_wa.html` и `accounts/connect_max.html`
    — по два `every` в одном файле), поэтому перечень обязан быть «путь + число», а не
    множеством путей. Плюс четыре константы-числа: 12, 8, 2 и сумма 22.

    **Утверждений два, как в образце.** Первое — сумма чисел перечня равна объявленной
    константе механизма, и сумма трёх констант равна 22: ловит расхождение ВНУТРИ самого
    теста. Второе — перечень против исходников: для каждого места счёт по шаблону, накопление
    расхождений в отображение «шаблон → нашли N, ожидалось M» и одно утверждение по всему
    отображению. Ловит расхождение теста с кодом. Только вместе они не дают перечню
    механизмов и числу 22 разъехаться молча.

    **Разделение механизмов.** Место относится к механизму 1, если строка с `hx-get` несёт
    `hx-trigger="revealed"`. К механизму 3 — если строка с `hx-get` находится внутри
    Jinja-условия по признаку синхронизации (два таких места, оба со срабатыванием раз в
    5 секунд; это та самая слепая зона гейтов разметки, названная пунктом 9 перечня из
    девяти и принадлежащая Фазе 15). К механизму 2 — все остальные места со срабатыванием
    по расписанию. Отнесение проверяется по СТРОКЕ с `hx-get`, а условность — по наличию
    открывающего Jinja-условия в той же строке.

    **Комментарий-летопись над константами обязателен** и открывается ПЕРВОЙ записью о
    поправке: «планирование Фазы 7 записало разбивку 10/10/2; счёт по файлам дал 12/8/2 при
    той же сумме 22; принята величина, полученная счётом, а не перенесённая из ожидания».
    Форма — с `test_components.py:757-800`, где прецедент признания не сбывшегося прогноза
    уже записан. Тихо выписать верные числа запрещено: запись о поправке и есть то, ради
    чего летопись ведётся.

    Форма сообщения об отказе копируется буквально с `test_components.py:1211-1214`:
    «мест … N, ожидалось M — место молча исчезло или появилось незаявленное», с перечислением
    расхождения по шаблонам.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_inventory.py -q -x</automated>
    <automated>uv run pytest tests/test_templates -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_templates/test_htmx_inventory.py -q` завершается кодом 0 и собирает не менее двух тестов.
    - `grep -c 'HX_GET_PLACES = 22' tests/test_templates/test_htmx_inventory.py` равно 1.
    - `grep -c 'REVEALED_PLACES = 12' tests/test_templates/test_htmx_inventory.py` равно 1.
    - `grep -c 'POLL_PLACES = 8' tests/test_templates/test_htmx_inventory.py` равно 1.
    - `grep -c 'CONDITIONAL_PLACES = 2' tests/test_templates/test_htmx_inventory.py` равно 1.
    - `grep -c 'REVEALED_LITERAL_OCCURRENCES = 13' tests/test_templates/test_htmx_inventory.py` равно 1 — ловушка счёта зафиксирована числом, а не обойдена.
    - `grep -c 'rglob' tests/test_templates/test_htmx_inventory.py` не меньше 1 — обход рекурсивный.
    - `grep -c '10/10/2' tests/test_templates/test_htmx_inventory.py` равно 1 — летопись открыта записью о поправке, а не тихо верными числами.
    - `grep -rn 'hx-get' app/templates/ | wc -l` выводит `22`, `grep -rn 'hx-get' app/templates/ | grep -c 'revealed'` выводит `12`, а `grep -rn 'hx-trigger="revealed"' app/templates/ | wc -l` выводит `13` — три величины, которые гейт держит.
    - Временное добавление места `hx-get` с `hx-trigger="revealed"` в любой шаблон роняет гейт с сообщением, называющим этот шаблон; после удаления гейт снова зелёный.
    - `uv run pytest tests/test_templates -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Отметка «обошли 22 места» перестала быть договорённостью: она есть три числа в тесте,
    выписанные, а не выведенные, и появление или исчезновение места роняет его с сообщением,
    называющим шаблон. Комментарий, дающий тринадцатое вхождение строки, зафиксирован
    отдельным числом, поэтому гейт не может позеленеть по совпадению.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Артефакт ручного обхода `07-UAT.md` — три механизма, десять экранов, отметки закрытия</name>

  <files>
    .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md
  </files>

  <read_first>
    .planning/ROADMAP.md — § «Phase 7», пять критериев успеха (критерии 4 и 5 закрываются ЗДЕСЬ), и § «Ручной UAT — девять именованных пунктов», строки 1 и 2 таблицы плюс замыкающая строка про сводное закрытие GATE-09 в Фазе 15,
    .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-PATTERNS.md — § Inventory Corrections ЦЕЛИКОМ: перечень «файл:строка» по трём механизмам и замечание про пары «список + карточки списка», из-за которого обход механизма 1 идёт по 6 экранам, а не по 12 строкам,
    .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-CONTEXT.md — решения D-11, D-12, D-14, D-15, D-16, D-17 целиком: почему обход идёт по механизмам, что именно ожидается от смены умолчания прокрутки и какой запасной план принят заранее,
    .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-01-SUMMARY.md — отгруженное состояние: какой блок конфигурации реально приехал и как выглядит строка очистки ключа снимков истории; наблюдаемые признаки берутся отсюда, а не из плана,
    .planning/research/PITFALLS.md:313-350 — § Pitfall 9: имя ключа, хранилище `localStorage`, умолчание размера кеша 10, «переживает выход из системы и закрытие вкладки»,
    app/templates/base.html:241 — записанный прецедент: проект УЖЕ ловил мерцание и рывки прокрутки на infinite scroll и вылечил их выключением переходов вида, а не конфигурацией; если каскад воспроизведётся, это уже случавшееся, а не новость
  </read_first>

  <action>
    Создать `07-UAT.md` в каталоге фазы — артефакт ручного обхода, который переживает
    сброс контекста и становится входом в сводное закрытие GATE-09 Фазы 15, вместо того
    чтобы Фаза 15 собирала перечень заново.

    **Шапка.** Почему артефакт вообще существует: браузерного стенда в проекте нет и не
    заводится (стенд отклонён на уровне вехи и отложен отдельным требованием), суита идёт
    без процесса и портов и не исполняет ни строчки JS, поэтому критерии 4 и 5 закрываются
    глазами по построению — и роадмап говорит про них дословно, что зелёная суита здесь не
    значит ничего. Назвать, что именно ЗАКРЫТО машинно планом 07-01 (наличие и порядок шести
    ключей, позиция блока выше рантайма, наличие и единственность строки очистки, подлинность
    артефакта хешем) и что машинно закрыть НЕЛЬЗЯ (само поведение). Записать версию рантайма,
    на которой ведётся обход.

    **Раздел «Пункт 1 перечня — замена рантайма» (критерий 4, QUAL-05).** Обход на профиле,
    в котором снимки БЫЛИ, а не на свежем: формулировка критерия читается буквально —
    «после выхода из системы снимков не остаётся». Шаги: войти, создать объявление (сегодня
    это единственное место проекта, порождающее снимок истории), убедиться в DevTools, что
    ключ снимков в локальном хранилище непуст; затем выйти по ссылке выхода — она обычная,
    с полной навигацией на экран входа, поэтому строка очистки исполняется гарантированно;
    затем убедиться, что ключа больше нет. Отдельным шагом: убедиться, что после обновления
    страницы ключ не наполняется заново — нулевой размер кеша истории закрывает будущее.
    Записать наблюдаемый признак каждого шага, а не только отметку.

    **Раздел «Пункт 2 перечня — 22 места на 2.0.10» (критерий 5, FOUND-01).** Три подраздела
    по механизмам (D-14), под каждым — адреса экранов, ожидаемое поведение и место отметки.

    Механизм 1, бесконечная подгрузка (12 мест, 6 экранов — пары «список + карточки списка»
    несут одну разметку): `/ads`, `/schedules`, `/history`, `/accounts`,
    `/accounts/{account_id}/groups`, `/admin/users/{user_id}/history`. Ожидаемое поведение:
    прокрутка до конца списка подгружает следующую страницу один раз, без каскада и без
    рывков прокрутки. Записать здесь же поправку D-16: умолчание прокрутки читается только
    модификаторами, а ни одно из 12 мест их не несёт — значит смена умолчания этих мест,
    скорее всего, не касается, и каскад, если он воспроизведётся, будет следствием
    изменившейся семантики самого срабатывания. Записать и прецедент: проект уже ловил
    мерцание и рывки на этом механизме и вылечил их выключением переходов вида
    (`app/templates/base.html:241`). Записать запасной план D-17 как ПРИНЯТЫЙ ЗАРАНЕЕ:
    при каскаде правится условие срабатывания в 12 местах через общий макрос; конфигурация
    не трогается, седьмой ключ не заводится, требование FOUND-02 править не придётся.

    Механизм 2, поллинг (8 безусловных мест, 5 экранов): `/dashboard` (лента, период из
    настроек), `/admin/workers` (период из настроек), `/accounts` (раз в 5 секунд, два места
    одной разметки), `/accounts/connect/wa` (раз в 3 секунды, ДВА места в одном шаблоне),
    `/accounts/connect/max` (раз в 3 секунды, ДВА места в одном шаблоне). Ожидаемое
    поведение: обновление продолжает приходить с прежним периодом, подмена идёт в
    объявленную цель, накопления параллельных запросов нет. Отметить отдельно, что два
    экрана подключения несут по два места каждый и обход обязан увидеть оба.

    Механизм 3, атрибуты внутри Jinja-условия (2 места, 2 экрана В СОСТОЯНИИ СИНХРОНИЗАЦИИ):
    `/accounts/{account_id}/groups` и `/accounts`. Ожидаемое поведение: пока синхронизация
    идёт, карточка сама опрашивает состояние; по завершении атрибуты исчезают вместе с
    условием и опрос прекращается. Записать явно: экран, не приведённый в состояние
    синхронизации, обхода НЕ проходил — он показал ту же страницу без атрибутов. Это та
    самая слепая зона гейтов разметки, названная пунктом 9 перечня из девяти и
    принадлежащая Фазе 15.

    **Форма отметки.** Под каждым пунктом — строка с датой, наблюдённым признаком и
    исходом. Отметка без наблюдённого признака не считается закрытием: артефакт есть
    единственный ручной инструмент фазы, и подпись под непроверенным Фаза 15 унаследует
    как факт. Оставить пустые отметки незаполненными — заполняет их человек на приёмке,
    а не исполнитель плана.

    **Замыкающий раздел.** Куда артефакт уезжает дальше: сводное закрытие девяти пунктов —
    требование GATE-09 Фазы 15, и этот файл есть его вход. Перечислить, какие пункты
    перечня закрыты здесь (1 и 2) и какие в этой фазе не рассматривались вовсе.
  </action>

  <verify>
    <automated>test -f .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md</automated>
    <automated>uv run pytest tests/test_templates/test_htmx_inventory.py -q</automated>
    <automated>just test</automated>
    <human-check>Пройти обход по `07-UAT.md`: пункт 1 — на профиле, в котором снимки истории УЖЕ есть (создать объявление, увидеть непустой ключ в DevTools, выйти, увидеть пустой); пункт 2 — десять экранов по трём механизмам, отдельно проверяя, что бесконечная подгрузка не идёт каскадом и не рвёт прокрутку, а два экрана подключения опрашиваются оба. Заполнить отметки наблюдёнными признаками, а не словом «ок». Расхождение — вход в запасной план D-17, а не в правку конфигурации.</human-check>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md` завершается кодом 0.
    - `grep -c '^## ' .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md` не меньше 4 — шапка, два пункта перечня и замыкающий раздел.
    - `grep -c 'Механизм' .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md` не меньше 3 — три механизма названы.
    - Все десять адресов экранов присутствуют в файле: `/dashboard`, `/ads`, `/schedules`, `/history`, `/accounts`, `/accounts/{account_id}/groups`, `/accounts/connect/wa`, `/accounts/connect/max`, `/admin/workers`, `/admin/users/{user_id}/history`.
    - `grep -c '22' .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md` не меньше 1, и числа 12, 8, 2 присутствуют — разбивка по механизмам совпадает с константами гейта задачи 1.
    - `grep -c 'D-17' .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md` не меньше 1 — запасной план на каскад записан в артефакте, а не только в контексте фазы.
    - `grep -c '2.0.10' .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md` не меньше 1 — версия рантайма, на которой ведётся обход, названа.
    - Файл содержит незаполненные места отметок под каждым пунктом (заполняет человек на приёмке, не исполнитель).
    - `just test` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Ручной обход фазы существует как файл, а не как намерение: три механизма, десять
    адресов, ожидаемое поведение под каждым, запасной план на каскад и места отметок.
    Фаза 15 получает вход в сводное закрытие GATE-09, а не задачу собрать перечень заново.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| перечень в тесте → 22 места разметки | единственная связь между заявленным инвентарём и фактическим состоянием кода |
| ручное наблюдение → запись о закрытии | всё, что отделяет проверенное от объявленного проверенным: машинного свидетеля у критериев 4 и 5 нет |
| Фаза 7 → Фаза 15 (GATE-09) | артефакт обхода наследуется как факт следующей вехой работ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Spoofing | гейт, зелёный по совпадению на прозаическом комментарии | high | mitigate | Счёт ведётся по местам, несущим `hx-get`, а не по вхождениям строки; отдельный тест фиксирует обе величины сразу — 13 вхождений и 12 мест, — поэтому совпадение чисел перестаёт быть возможным исходом |
| T-07-12 | Repudiation | отметка о закрытии пункта обхода без наблюдения | medium | mitigate | Артефакт требует под каждым пунктом наблюдённого признака и даты, а не одной отметки; отметки оставляются незаполненными исполнителем и заполняются на приёмке; файл коммитится и становится входом GATE-09 |
| T-07-13 | Tampering | место `hx-get`, добавленное будущей фазой мимо перечня | medium | mitigate | Двойное утверждение — перечень против суммы и перечень против исходников — плюс рекурсивный обход, видящий 4 места в подкаталогах; сообщение об отказе называет шаблон-нарушитель |
| T-07-14 | Information Disclosure | адреса административных экранов в артефакте планирования | low | accept | Артефакт живёт в репозитории проекта рядом с исходниками тех же маршрутов; ни одного значения доступа, идентификатора пользователя или данных сессии в нём нет |
| T-07-SC | Tampering | установка пакетов (npm/pip/cargo) | — | N/A | Ни один менеджер пакетов планом не запускается; заводятся файл тестов на стандартной библиотеке и документ. Аудит легитимности пакетов беспредметен, чекпойнт не заводится за отсутствием предмета |
</threat_model>

<verification>
- `uv run pytest tests/test_templates/test_htmx_inventory.py -q` — зелёный, не менее двух тестов.
- `uv run pytest tests/test_templates -q` — зелёный.
- `just test` — зелёный.
- Временное добавление места `hx-get` роняет инвентарный гейт с сообщением, называющим шаблон.
- `07-UAT.md` существует, несёт три механизма, десять адресов и незаполненные отметки.
- Ручной обход по артефакту пройден человеком, отметки заполнены наблюдёнными признаками (критерии 4 и 5 роадмапа — приёмка в конце фазы).
</verification>

<success_criteria>
- Инвентарь 22 мест утверждён тремя числами и суммой; ловушка счёта на комментарии зафиксирована отдельным числом.
- Летопись над константами открыта записью о поправке разбивки, а не тихо верными числами.
- `07-UAT.md` существует и группирует обход по трём механизмам с адресами десяти экранов.
- Запасной план на каскад записан в артефакте до того, как каскад понадобился.
- Ни один `hx-*` атрибут фазой не тронут.
- Полная суита зелёная.
</success_criteria>

<output>
Create `.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-03-SUMMARY.md` when done
</output>
