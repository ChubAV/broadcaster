---
phase: 07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii
plan: 03
subsystem: testing
tags: [htmx, jinja2, templates, pytest, inventory-gate, manual-uat]

# Dependency graph
requires:
  - phase: 07-01 (эта же фаза, волна 1)
    provides: "отгруженный рантайм 2.0.10, includes/htmx_config.html с шестью ключами, строка очистки ключа снимков истории в auth-шелле — наблюдаемые признаки обхода сняты отсюда, а не из плана"
provides:
  - "tests/test_templates/test_htmx_inventory.py — единственный машинный тест, который фаза получает сверх гейтов шелла: инвентарь 22 мест hx-get числом по трём механизмам (12 / 8 / 2)"
  - "Ловушка счёта зафиксирована числом: 13 вхождений строки revealed против 12 мест разметки, разница ровно на комментарий base.html:241"
  - "07-UAT.md — артефакт ручного обхода: три механизма, десять адресов экранов, ожидаемое поведение, запасной план D-17, незаполненные отметки"
  - "Вход в сводное закрытие GATE-09 Фазы 15 — перечень не придётся собирать заново"
affects: ["Фаза 11 (добавит места hx-get — гейт покраснеет)", "Фаза 15 (GATE-09, сводное закрытие девяти пунктов)"]

# Actuals (#2632)
actuals:
  tokens: 10467
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Инвентарь по МЕХАНИЗМАМ, а не по одному числу: три перечня-константы плюс сумма, отнесение места разбирается по строке с hx-get"
    - "Ловушка счёта утверждается ОТДЕЛЬНЫМ тестом, фиксирующим обе величины сразу (вхождения строки и места разметки) плюс их разницу"
    - "Место с неопознанной формой срабатывания роняет гейт, а не относится к остаточной категории"
    - "Артефакт ручного обхода как файл в каталоге фазы: наблюдаемые признаки сняты с отгруженного состояния, отметки оставлены незаполненными для приёмки"

key-files:
  created:
    - tests/test_templates/test_htmx_inventory.py
    - .planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-UAT.md
  modified:
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Разбивка 12/8/2 подтверждена счётом по диску ДО написания гейта; поправка 10/10/2 → 12/8/2 записана первой записью летописи как поправка, а не выписана тихо верной"
  - "Отнесение места к механизму разбирается по СТРОКЕ с hx-get; место, не подошедшее ни под revealed, ни под Jinja-условие, ни под every Ns, роняет гейт по имени шаблона вместо тихого зачисления в поллинг"
  - "Отметки в 07-UAT.md оставлены НЕЗАПОЛНЕННЫМИ: заполняет человек на приёмке наблюдённым признаком — исполнитель плана подписать за него не может"
  - "Наблюдаемые признаки артефакта сняты с отгруженного состояния волны 1 (реальная строка очистки, реальный блок из шести ключей), а не из текста плана"

patterns-established:
  - "Отрицательная проверка гейта исполнением: место hx-get временно добавлено в шаблон, зафиксировано падение с именем шаблона, шаблон восстановлен — гейт доказан рабочим, а не объявлен таковым"
  - "Артефакт ручного UAT в каталоге фазы, группированный по механизмам, с явным разделом «что закрыто машинно» и «что машинно закрыть нельзя»"

requirements-completed: [FOUND-01, QUAL-05]

# Coverage metadata (#1602)
coverage:
  - id: D1
    description: "Мест hx-get в шаблонах ровно 22, разбивка по трём механизмам 12 / 8 / 2, и перечень-константа сходится и с суммой, и с исходниками; появление или исчезновение места роняет гейт с сообщением, называющим шаблон"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_inventory.py#test_hx_get_inventory_matches_declared_mechanisms"
        status: pass
      - kind: other
        ref: "отрицательная проверка исполнением: место hx-get с hx-trigger=\"revealed\" временно добавлено в app/templates/dashboard.html — гейт упал сообщением 'мест hx-get по механизмам [revealed:dashboard.html] … мест 1, ожидалось 0'; после восстановления снова зелёный"
        status: pass
    human_judgment: false
  - id: D2
    description: "Ловушка счёта пережита: вхождений строки hx-trigger=\"revealed\" 13, мест разметки 12, разница ровно 1 — прозаический комментарий base.html:241 в счёт мест не попадает"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_inventory.py#test_revealed_literal_count_exceeds_site_count"
        status: pass
      - kind: other
        ref: "grep -rn 'hx-get' app/templates/ | wc -l = 22; | grep -c revealed = 12; grep -rn 'hx-trigger=\"revealed\"' app/templates/ | wc -l = 13"
        status: pass
    human_judgment: false
  - id: D3
    description: "Обход шаблонов рекурсивный (rglob): 4 из 22 мест живут в подкаталогах partials/ и admin/, плоский обход каталогов их не увидел бы"
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_inventory.py#test_hx_get_inventory_matches_declared_mechanisms (места account_groups/partials/sync_result.html и accounts/partials/sync_status_card.html входят в счёт)"
        status: pass
    human_judgment: false
  - id: D4
    description: "07-UAT.md существует, группирует обход по трём механизмам, несёт десять адресов экранов, ожидаемое поведение под каждым, запасной план D-17 и незаполненные места отметок"
    verification:
      - kind: other
        ref: "grep -c '^## ' = 4; grep -c 'Механизм' = 4; все десять адресов присутствуют; grep -c 'D-17' = 3; grep -c '2.0.10' = 4"
        status: pass
    human_judgment: false
  - id: D5
    description: "Ни один hx-* атрибут планом не добавлен, не снят и не отредактирован — обход фиксирует состояние, а не правит его"
    verification:
      - kind: other
        ref: "git diff --name-only ca90ed99..HEAD — ни одного файла из app/templates/ среди изменённых"
        status: pass
    human_judgment: false
  - id: D6
    description: "ПОВЕДЕНЧЕСКАЯ половина QUAL-05: после выхода из системы ключ снимков истории в localStorage пуст — на профиле, в котором снимки БЫЛИ, а не на свежем (критерий 4 роадмапа, пункт 1 перечня из девяти)"
    verification: []
    human_judgment: true
    rationale: "Суита не исполняет ни строчки JS: httpx отдаёт текст ответа, а не браузер с хранилищем. Наличие и единственность строки очистки закрыты машинно планом 07-01; её ДЕЙСТВИЕ проверяется глазами в DevTools. Процедура выписана в 07-UAT.md разделом «Пункт 1»; отметка НЕ заполнена — заполняет человек на приёмке фазы."
  - id: D7
    description: "ПОВЕДЕНЧЕСКАЯ половина FOUND-01: все 22 места hx-get работают на рантайме 2.0.10, и каскад infinite scroll на hx-trigger=\"revealed\" не воспроизводится (критерий 5 роадмапа, пункт 2 перечня из девяти)"
    verification: []
    human_judgment: true
    rationale: "Роадмап говорит про критерий 5 дословно: «зелёная суита здесь не значит ничего». Гейт этого плана утверждает СОСТАВ (22 места числом по механизмам), а не поведение — для поведения нужен браузер, исполняющий JS, а браузерного стенда в проекте нет и не заводится (E2E-01 отложен на уровне вехи). Процедура на десять экранов выписана в 07-UAT.md разделом «Пункт 2»; отметки НЕ заполнены — заполняет человек на приёмке фазы."

# Metrics
duration: 35 min
completed: 2026-08-27
status: complete
---

# Phase 07 Plan 03: Инвентарный гейт 22 мест и артефакт ручного обхода Summary

**Инвентарь 22 мест `hx-get` числом по трём механизмам (12 / 8 / 2) с ловушкой счёта, зафиксированной отдельным числом — 13 вхождений строки против 12 мест разметки, — плюс `07-UAT.md`: обход по трём механизмам на десяти экранах с запасным планом D-17, записанным до того, как каскад понадобился.**

## Performance

- **Duration:** 35 min (из них 24 мин 37 с — один прогон полной суиты)
- **Started:** 2026-08-27T10:13:00Z
- **Completed:** 2026-08-27T10:48:20Z
- **Tasks:** 2
- **Files modified:** 3 (2 создано, 1 правлен — `REQUIREMENTS.md`)

## Accomplishments

- **Единственная фаза вехи без машинного теста получила свой машинный тест.** `test_htmx_inventory.py` держит три числа и сумму: `REVEALED_PLACES = 12`, `POLL_PLACES = 8`, `CONDITIONAL_PLACES = 2`, `HX_GET_PLACES = 22`. Перечень выписан константами, а не выведен из проверяемых шаблонов — тест, считающий ожидание по коду в момент прогона, согласился бы с любой правкой.
- **Разбивка проверена счётом по диску ДО написания гейта.** Три величины сняты командой: мест `hx-get` — 22, из них с `revealed` — 12, вхождений строки `hx-trigger="revealed"` — 13. Совпало с §Inventory Corrections; расхождения сверх уже записанной поправки не обнаружилось, и о его отсутствии сказано прямо, а не умолчанием.
- **Ловушка счёта не обойдена, а зафиксирована числом.** Отдельный тест утверждает обе величины сразу — 13 вхождений и 12 мест — И их разницу, равную единице. Исчезновение прозаического комментария `base.html:241` или появление четырнадцатого вхождения теперь тоже видимы, а не растворяются в первом числе.
- **Гейт доказан рабочим исполнением, а не объявлен таковым.** Место `hx-get` с `hx-trigger="revealed"` временно добавлено в `dashboard.html`: упали ОБА теста, сообщение назвало шаблон-нарушитель (`revealed:dashboard.html`, «мест 1, ожидалось 0»). Шаблон восстановлен, дерево чистое.
- **Место с неопознанной формой срабатывания роняет гейт.** Сверх плана: место, не подошедшее ни под `revealed`, ни под Jinja-условие, ни под `every Ns`, не зачисляется в поллинг по остаточному признаку, а называется отдельным утверждением. Иначе незаявленная форма срабатывания сохраняла бы сумму 22 и проходила молча.
- **`07-UAT.md` называет разрыв покрытия вслух.** Отдельные разделы «что закрыто машинно» (семь строк с именами тестов) и «что машинно закрыть НЕЛЬЗЯ» — само поведение. Наблюдаемые признаки сняты с отгруженного состояния волны 1: реальная строка `localStorage.removeItem('htmx-history-cache')` и реальный блок из шести ключей, а не их описание из плана.
- **Запасной план D-17 записан в артефакте до того, как понадобился**, вместе с поправкой D-16 (`scrollBehavior` читается только модификаторами `show:`/`scroll:`, которых ни одно из 12 мест не несёт — причину каскада искать не там) и записанным прецедентом `base.html:241`.

## Task Commits

1. **Задача 1: Инвентарный гейт 22 мест по трём механизмам** — `64d6840` (test)
2. **Задача 2: Артефакт ручного обхода `07-UAT.md`** — `94c71ff` (docs)

## Files Created/Modified

- `tests/test_templates/test_htmx_inventory.py` (создан, 312 строк — требование `min_lines: 120` выполнено с запасом). Три перечня-константы `HxGetSite`, четыре числа, константа-ловушка, рекурсивный обход `rglob`, разборщик механизма по строке и два теста.
- `.planning/phases/07-.../07-UAT.md` (создан, 241 строка). Шапка с разрывом покрытия, пункт 1 (шесть шагов на профиле, где снимки БЫЛИ), пункт 2 (три механизма, десять адресов), замыкающий раздел с картой девяти пунктов по фазам.
- `.planning/REQUIREMENTS.md` — отмечены FOUND-01 и QUAL-05 (обе поверхности: чекбокс и таблица прослеживаемости).

## Decisions Made

- **Разбивка взята с диска, а не из CONTEXT.md.** 07-CONTEXT.md (D-14, D-18) записал 10/10/2; счёт по файлам дал 12/8/2 при той же сумме 22. Гейт, написанный по CONTEXT.md, был бы красным в момент написания. Поправка открывает летопись ПЕРВОЙ записью — тихо выписать верные числа значило бы стереть свидетельство о том, что ожидание разошлось с кодом.
- **Утверждений два, как в образце.** Перечень против суммы ловит расхождение ВНУТРИ теста (константу поправили, перечень нет); перечень против исходников ловит расхождение теста с кодом. По отдельности каждое можно удовлетворить, оставив второе неверным.
- **Файл живёт в `tests/test_templates/`, а не в `tests/test_pages/`.** Это гейт РАЗМЕТКИ, читающий исходники шаблонов; его канонический образец — `test_components.py` (`ROW_DELETE_PLACES`, `MODAL_PLACES`). Гейты в `test_access_gate.py` / `test_impersonation_gate.py` — гейты множеств по синтаксическому дереву Python, форма другая.
- **Перечень «путь + число», а не множество путей.** Три файла несут по два места: `accounts/connect_wa.html` и `accounts/connect_max.html` — по два места `every 3s` каждый (ветки состояния подключения), `accounts/list.html` и `accounts/partial_cards.html` — по одному месту в ДВУХ разных механизмах. Порог «хотя бы одно» растворил бы потерю одной ветки подключения.
- **Отметки в артефакте оставлены незаполненными.** Заполняет человек на приёмке наблюдённым признаком. Исполнитель плана подписать за него не может — прохибиция плана (T-07-12) читается буквально.

## Deviations from Plan

### 1. [Уточнение состава] Утверждение о неопознанной форме срабатывания сверх перечня артефактов

- **Найдено при:** задаче 1, при разборе отнесения места к механизму.
- **Что добавлено:** категория `unknown` в разборщике и отдельное утверждение `assert not found["unknown"]`.
- **Почему:** план задаёт отнесение как «механизм 1 — если `revealed`; механизм 3 — если Jinja-условие; механизм 2 — ВСЕ ОСТАЛЬНЫЕ». Прочитанное буквально, «все остальные» зачисляет в поллинг место с любой незаявленной формой срабатывания, и сумма 22 при этом сохраняется — то есть новая форма срабатывания прошла бы молча ровно так же, как молча проходило бы исчезновение места. Категория `unknown` закрывает этот путь, не меняя ни одного объявленного числа.
- **Файлы:** `tests/test_templates/test_htmx_inventory.py`. **Коммит:** `64d6840`.

### 2. [Формулировка] Второе вхождение строки `10/10/2` переписано ради буквального критерия приёмки

- **Найдено при:** прогоне критериев приёмки задачи 1.
- **Ситуация:** критерий требует `grep -c '10/10/2'` **равно 1**; первая редакция летописи упоминала прежнюю разбивку дважды (в самой записи о поправке и в объяснении, зачем запись ведётся), давая 2.
- **Решение:** второе упоминание переформулировано на «другая разбивка в 07-CONTEXT.md» без повтора числового литерала. Смысл записи сохранён целиком, критерий выполняется буквально.
- **Файлы:** `tests/test_templates/test_htmx_inventory.py`. **Коммит:** `64d6840` (до коммита).

---

**Total deviations:** 2 (1 уточнение состава, 1 формулировка). Ни одна не потребовала правила 4 (архитектурное решение).
**Impact on plan:** рамки плана не расширены, ни одно объявленное число не изменено. **Ни один `hx-*` атрибут не добавлен, не снят и не отредактирован** — подтверждено `git diff --name-only ca90ed99..HEAD`: среди изменённых файлов нет ни одного из `app/templates/`.

## Issues Encountered

### 🔴 Полная суита красная ОДНИМ тестом — причина вне этого плана, правка запрещена рамкой

`just test` — **1 failed, 2309 passed** (24 мин 37 с). Упавший тест:
`tests/test_planning/test_state_progress_matches_roadmap.py::test_the_machine_readable_progress_is_derived_from_the_roadmap`

```
поле `progress.completed_plans` во frontmatter `.planning/STATE.md` записано как 0,
а из отметок `.planning/ROADMAP.md` выводится 2 — привести надо ПОЛЕ, потому что
источником счёта планов объявлен ROADMAP
```

**Причина установлена свидетельством, а не предположением.** Последний коммит, трогавший
`STATE.md` и `ROADMAP.md`, — `ca90ed9` «docs(phase-07): update tracking after wave 1», и это
БАЗОВЫЙ коммит данного worktree. Он проставил `[x]` планам 07-01 и 07-02 в роадмапе, не сдвинув
`progress.completed_plans` в STATE.md. Расхождение существовало ДО обоих коммитов этого плана;
тест падает одинаково и в полном прогоне, и в одиночку.

**Почему не исправлено:**
1. **Вне рамки плана.** `files_modified` — два файла; ни `STATE.md`, ни `ROADMAP.md` в них нет.
2. **Прямо запрещено заданием исполнителя:** STATE.md и ROADMAP.md принадлежат оркестратору,
   который сводит их централизованно после возврата всех агентов волны. Правка из worktree —
   ровно тот конфликт, ради предотвращения которого запрет и введён.
3. Расхождение **самоустраняется** ближайшим шагом оркестратора (`state.update-progress`):
   волна 2 завершена, планов с SUMMARY станет 3 из 3.

⚠️ **Для оркестратора:** пока `progress.completed_plans` не приведён к числу, выводимому из
роадмапа, пост-слияние `just test` останется красным этим одним тестом. Прогон гейта фазы имеет
смысл ПОСЛЕ шага обновления STATE.md, а не до него.

**Вклад этого плана в суиту — только положительный:** два новых теста, оба зелёные;
`tests/test_templates` — 55 passed; `tests/test_templates/test_htmx_inventory.py` — 2 passed.

## TDD Gate Compliance

План объявлен `type: execute`, ни одна задача не несёт `tdd="true"` — последовательность
RED/GREEN/REFACTOR к нему не применяется, и её отсутствие нарушением не является. Задача 1
по существу и есть тест; отрицательная проверка исполнением (временно добавленное место роняет
гейт, удалённое — возвращает зелёный) выполняет здесь роль фазы RED: гейт доказан способным
краснеть, а не принят зелёным на веру.

## Known Stubs

Заглушек нет: обе величины гейта сняты с реальных исходников шаблонов, обход рекурсивный,
сообщения об отказе называют шаблон.

**Не заглушка, но названный разрыв покрытия — ДВА (D6 и D7 в блоке `coverage`).** Поведенческие
половины QUAL-05 и FOUND-01 машинно не закрыты и объявлены таковыми явно: суита не исполняет JS.
Процедуры выписаны в `07-UAT.md`; **отметки НАМЕРЕННО оставлены незаполненными** — их заполняет
человек на приёмке фазы наблюдённым признаком. Заполнить их исполнителем плана значило бы
подписаться под непроверенным, и Фаза 15 унаследовала бы подпись как факт (прохибиция T-07-12).

## Threat Flags

Новой поверхности сверх реестра угроз плана не появилось. План не вводит ни одного сетевого
обработчика, пути аутентификации, изменения схемы данных или разметки: заведены файл тестов на
стандартной библиотеке (`re`, `pathlib`, `typing`) и документ планирования. Ни один менеджер
пакетов не запускался — T-07-SC беспредметен.

Отработанные митигации реестра: **T-07-11** (счёт по местам с `hx-get`, а не по вхождениям строки,
плюс отдельный тест на обе величины и их разницу); **T-07-13** (двойное утверждение + рекурсивный
обход, сообщение об отказе называет шаблон-нарушитель — доказано исполнением); **T-07-12**
(артефакт требует наблюдённого признака и даты, отметки оставлены незаполненными, файл
закоммичен и становится входом GATE-09). **T-07-14** принят как риск диспозицией плана.

## Requirements

| Требование | Состояние | Основание |
|---|---|---|
| FOUND-01 | **отмечено выполненным** | Гейт общего ID снят: 07-01 (второй объявивший) уже несёт SUMMARY, `requirements.ready-ids` вернул оба ID готовыми. ⚠️ Отмечена РАБОТА обоих планов; поведенческая половина (22 места на 2.0.10 глазами) остаётся за отметками `07-UAT.md`, D7 в `coverage`. |
| QUAL-05 | **отмечено выполненным** | Тот же гейт общего ID. Машинная половина закрыта планом 07-01, инструмент ручной половины создан здесь; сама половина — за отметками `07-UAT.md`, D6 в `coverage`. |

## User Setup Required

Нет — блок `user_setup` во frontmatter плана отсутствует, внешних сервисов план не вводит.

## Verification Results

| Проверка | Результат |
|---|---|
| `uv run pytest tests/test_templates/test_htmx_inventory.py -q -x` | **2 passed** |
| `uv run pytest tests/test_templates -q` | **55 passed** |
| `just test` | 1 failed, **2309 passed** — упавший тест разобран в §Issues Encountered, причина вне плана |
| Отрицательная проверка: место `hx-get` добавлено в `dashboard.html` | оба теста упали, сообщение назвало `revealed:dashboard.html`; после восстановления — зелёные |
| `grep -rn 'hx-get' app/templates/ \| wc -l` | `22` |
| `grep -rn 'hx-get' app/templates/ \| grep -c 'revealed'` | `12` |
| `grep -rn 'hx-trigger="revealed"' app/templates/ \| wc -l` | `13` |
| Константы `HX_GET_PLACES = 22`, `REVEALED_PLACES = 12`, `POLL_PLACES = 8`, `CONDITIONAL_PLACES = 2`, `REVEALED_LITERAL_OCCURRENCES = 13` | по 1 вхождению каждая |
| `grep -c 'rglob'` / `grep -c '10/10/2'` | `2` / `1` |
| `07-UAT.md`: `^## ` / `Механизм` / `D-17` / `2.0.10` | `4` / `4` / `3` / `4` |
| Десять адресов экранов в `07-UAT.md` | все десять присутствуют, множество совпадает точно |
| `graphify update .` | 12930 узлов, 24412 рёбер, код 0 |
| `git diff --name-only ca90ed99..HEAD` | ни одного файла из `app/templates/` |

## Next Phase Readiness

- **Фаза 7 готова к приёмке с одной оговоркой:** машинная часть закрыта целиком, ручная — создана
  как инструмент, но НЕ пройдена. Критерии 4 и 5 роадмапа закрываются заполнением отметок
  `07-UAT.md` человеком; до этого момента они не закрыты, и зелёная суита этого не меняет.
- **Готово для Фазы 11:** гейт покраснеет в момент, когда фаза добавит двадцать третье место, и
  сообщение назовёт шаблон. Летопись над константами задаёт форму записи для изменения числа.
- **Готово для Фазы 15 (GATE-09):** `07-UAT.md` — вход в сводное закрытие девяти пунктов. Пункты 1
  и 2 закреплены за этой фазой, пункт 9 (слепая зона Jinja-условий) назван и явно передан в 15.
- **Внимание оркестратору при слиянии:** `STATE.md` и `ROADMAP.md` этим планом НЕ трогались.
  `REQUIREMENTS.md` правлен — конфликт возможен, если его правил сосед по волне (в волне 2 план
  один, поэтому конфликт маловероятен). Красный `test_state_progress_matches_roadmap` — см.
  §Issues Encountered: он снимается шагом обновления STATE.md, а не правкой кода.

## Self-Check: PASSED

- `tests/test_templates/test_htmx_inventory.py` — FOUND на диске, 312 строк (`min_lines: 120` выполнено).
- `.planning/phases/07-.../07-UAT.md` — FOUND на диске, 241 строка, `contains: "Механизм"` выполнено (4 вхождения).
- Коммиты `64d6840` и `94c71ff` — оба найдены в `git log --oneline --all`.
- Удалённых файлов ни в одном коммите нет (`git diff --diff-filter=D` пуст дважды).
- Временный пробный шаблон восстановлен: `git status` чист, `git diff app/templates/dashboard.html` пуст.
- Ни `STATE.md`, ни `ROADMAP.md` не изменены — подтверждено `git diff --name-only` от базы.

---
*Phase: 07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii*
*Completed: 2026-08-27*
