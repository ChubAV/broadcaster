---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 10
type: execute
wave: 5
depends_on: ["12-09"]
gap_closure: true
autonomous: true
requirements: [FETCH-01]
files_modified:
  - app/pages/ads.py
  - app/services/image_upload.py
  - tests/test_pages/test_origin_guard_on_destructive_routes.py
  - tests/test_pages/test_ads_image_upload.py
  - tests/test_nginx_body_limit.py
  - tests/test_services/test_images.py

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Маршрут загрузки ЗОВЁТ гарду источника: межсайтовая форма больше не пишет объекты в префикс жертвы (WR-03, решение владельца D-15)."
    - "Замеряющее правило, записавшее отсутствие гарды, ИНВЕРТИРОВАНО ВВЕРХ вместе с предметом: число мест вызова сдвинуто с тринадцати на четырнадцать, летопись переписана, отрицательный контроль сохранён — правило не удалено и не ослаблено."
    - "Сверка источника стои́т ПОСЛЕ прав и ДО разбора составного запроса: межсайтовый запрос не заставляет воркер сбуферить тело до потолка обратного прокси."
    - "Два комментария-инварианта говорят правду о транспорте: единственный предел ДО чтения — потолок тела на обратном прокси; предел размера картинки ограничивает то, что ДЕКОДИРУЕТСЯ и СОХРАНЯЕТСЯ (WR-01)."
    - "Ни одна строка суиты не ссылается на снятый планом 12-05 тестовый модуль (IN-01)."
  artifacts:
    - "app/pages/ads.py — вызов `is_same_origin` в `ads_images_upload` и правдивые комментарии о границе чтения"
    - "app/services/image_upload.py — правдивый комментарий над порционным чтением"
    - "tests/test_pages/test_origin_guard_on_destructive_routes.py — `ORIGIN_GUARD_CALL_SITES_MEASURED` = 14 с переписанной летописью"
    - "tests/test_pages/test_ads_image_upload.py — `test_a_cross_site_form_writes_nothing_to_storage`"
  key_links:
    - "`is_same_origin` (`app/pages/common.py:704`) → `ads_images_upload`: маршрут страничного слоя, пишущий в хранилище, перестаёт быть единственным без этой сверки"
    - "вызов гарды → `ORIGIN_GUARD_CALL_SITES_MEASURED`: замер и предмет двигаются ОДНИМ коммитом, поэтому расхождение записи с деревом остаётся видимым"

security:
  asvs_level: 1
  block_on: high
---

<objective>
Исполнить решение владельца **D-15** (гарда источника на маршруте загрузки, находка ревью `WR-03`),
сделать правдивыми два комментария-инварианта, которых транспорт не держит (`WR-01`), и
перенацелить две оставшиеся ссылки на снятый тестовый модуль (`IN-01`).

Purpose: `ads_images_upload` — единственный пишущий в хранилище маршрут страничного слоя без
`is_same_origin`, и отсутствие это УНАСЛЕДОВАНО от снятого JSON-маршрута, а не принято заново на
поверхности, где идиома гарды уже есть. Аутентификация идёт кукой, составной запрос есть «простой»
запрос перекрёстного происхождения, поэтому чужая страница пишет объекты в префикс жертвы до
потолка тела за запрос, повторяемо. Отдельно: комментарий, утверждающий защиту, которой транспорт
не даёт, хуже отсутствия комментария — следующий читатель обопрётся на несуществующий рубеж.

Output: вызов гарды, инвертированный вверх замер, две правдивые записи о границе чтения и три
перенацеленные ссылки (третью закрыл план 12-06).
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Гарда источника зовётся на маршруте загрузки, замер инвертируется вверх (D-15)</name>

  <reversibility rating="reversible">Откат — снятие одной строки вызова и возврат числа замера с четырнадцати на тринадцать; внешних потребителей и миграций у правки нет.</reversibility>

  <read_first>
    - `app/pages/common.py:686-760` — `ORIGIN_CHECK_BOUNDARY` (:690) и `is_same_origin` (:704): где именно стои́т сверка и почему именно там
    - `app/pages/ads.py:1110-1125` — ДЕЙСТВУЮЩИЙ образец вызова в этом же модуле: сверка и форма отказа
    - `app/pages/ads.py:20-32` — ввоз `is_same_origin` в модуль УЖЕ есть (:26), второго ввоза не заводить
    - `app/pages/ads.py:764-820` — начало `ads_images_upload`: чтение пользователя, затем разбор составного запроса
    - `tests/test_pages/test_origin_guard_on_destructive_routes.py:655-726` — группа замеров, летопись числа POST-обработчиков (:683-703), летопись числа мест вызова (:705-710), число внутри вселенных (:712-724)
    - `tests/test_pages/test_origin_guard_on_destructive_routes.py:840-870` — правило, сравнивающее три объявленных числа с тремя измеренными
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md:225-233` — решение владельца D-15 дословно
  </read_first>

  <files>app/pages/ads.py, tests/test_pages/test_origin_guard_on_destructive_routes.py, tests/test_pages/test_ads_image_upload.py</files>

  <behavior>
    - Запрос `POST /ads/images` с заголовком источника, чей хост не совпадает с хостом запроса, отвечает кодом 403 и не пишет в хранилище ни одной записи.
    - Тот же запрос с заголовком `Sec-Fetch-Site` со значением, отличным от `same-origin`, отвечает так же.
    - Запрос БЕЗ обоих заголовков проходит как прежде — названная граница защиты сохранена, и вся действующая суита загрузки остаётся зелёной без правки.
    - Отказ по источнику происходит ДО разбора составного запроса: тело запроса воркером не буферизуется.
    - Измеренное число мест вызова гарды становится четырнадцать, число POST-обработчиков страничного слоя остаётся тридцать семь, число внутри вселенных обоих гейтов остаётся десять.
  </behavior>

  <action>
    В `app/pages/ads.py`, обработчик `ads_images_upload`: после `get_user_from_cookie` и ДО вызова
    `request.form` поставить сверку источника ровно той формы, что уже применена в этом же модуле, —
    проверка `is_same_origin(request)` и возврат ответа с кодом 403. Ввоз имени в модуле уже есть,
    второго не заводить. Порядок обязателен по двум причинам сразу, и обе записать комментарием:
    канон границы (`ORIGIN_CHECK_BOUNDARY`) ставит сверку ПОСЛЕ прав и ДО чтения предмета, а
    предметом здесь является ТЕЛО запроса — разбор составного запроса буферизует каждую часть, и
    сверка ниже него оплачивала бы межсайтовому отправителю всю работу буферизации до потолка тела
    на обратном прокси. Причина отказа чужому источнику не сообщается: код без тела, как и у соседа
    в том же модуле.

    В докстринг обработчика дописать абзац о том, что гарда теперь есть, и назвать, чем её
    отсутствие было прежде: не забывчивостью, а решением, УНАСЛЕДОВАННЫМ от снятого JSON-маршрута и
    не принятым заново на поверхности, где идиома гарды уже существует. Названное воздействие,
    которое это закрывает, — запись объектов в префикс жертвы чужой страницей, повторяемо, до
    потолка тела за запрос.

    В `tests/test_pages/test_origin_guard_on_destructive_routes.py` сдвинуть
    `ORIGIN_GUARD_CALL_SITES_MEASURED` с тринадцати на четырнадцать и дописать строку летописи в
    действующей форме: откуда, куда, какой фазой, каким планом, ИСТОЧНИК ДВИЖЕНИЯ одной фразой
    («загрузка вложения объявления получила сверку источника решением владельца D-15»), и отметка
    «ПОСТАВЛЕНО ПРОГОНОМ покрасневшего правила» с дословным текстом покраснения. Число прогоняется,
    а не вычитается в уме: сперва добавить вызов, прогнать
    `uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py -q`, прочитать
    измеренное число в сообщении и записать ЕГО.

    ⚠️ АБЗАЦ :695-700 ПЕРЕПИСЫВАЕТСЯ ВВЕРХ, А НЕ СТИРАЕТСЯ. Он сегодня утверждает, что «два числа
    ниже не двинулись», и называет это замером, а не недосмотром. Одно из двух теперь двинулось.
    Новая редакция сохраняет действующее утверждение о числе внутри вселенных обоих гейтов (десять —
    действие по-прежнему не является подтверждённым удалением), называет прежнюю редакцию снятой и
    указывает причину снятия: отсутствие гарды было ЗАМЕРЕНО верно, но замер фиксировал
    унаследованное решение, и владелец принял его заново с обратным исходом (D-15). Летопись числа
    мест вызова (:705-710) дополняется восьмым модулем, у которого стало два вызова вместо одного.

    В `tests/test_pages/test_ads_image_upload.py` завести
    `test_a_cross_site_form_writes_nothing_to_storage`: запрос с одной годной картинкой PNG и
    заголовком источника чужого хоста; утверждения — код ответа 403 и `mock_s3.call_count == 0`.
    Второе утверждение несущее: код отказа без нулевого числа записей не доказал бы, что работа не
    сделана ДО отказа.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_origin_guard_on_destructive_routes.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога — объявленные числа разошлись с измеренными</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py::test_a_cross_site_form_writes_nothing_to_storage -q</automated>
    <fails_when>ненулевой код возврата, либо "0 passed" в итоге, либо "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_pages/test_htmx_gates.py tests/test_pages/test_impersonation_gate.py tests/test_pages/test_access_gate.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
  </verify>

  <acceptance_criteria>
    - В срезе обработчика `ads_images_upload` (`awk '/^async def ads_images_upload/,/^@router/' app/pages/ads.py`) присутствует вызов `is_same_origin`, и он стои́т ВЫШЕ строки `request.form`.
    - `grep -n "is_same_origin" app/pages/ads.py` печатает не меньше трёх строк: ввоз и два места вызова.
    - `grep -n "_MEASURED = " tests/test_pages/test_origin_guard_on_destructive_routes.py` печатает три строки, и объявленные в них значения читаются как 37 для числа POST-обработчиков, 14 для числа мест вызова гарды и 10 для числа внутри вселенных обоих гейтов.
    - Два числа из трёх — 37 и 10 — не тронуты этой задачей: их значения совпадают со значениями до правки.
    - Рядом с изменённым числом стои́т строка летописи с отметкой о постановке прогоном и названием решения владельца `D-15`.
  </acceptance_criteria>

  <done>Межсайтовая форма получает 403 и не пишет в хранилище ни байта; замеряющее правило утверждает наличие гарды, а не её отсутствие.</done>
</task>

<task type="auto">
  <name>Задача 2: Два комментария-инварианта начинают говорить правду о транспорте (WR-01)</name>

  <read_first>
    - `app/services/image_upload.py:296-326` — комментарий над порционным чтением: утверждение о том, что предел ограничивает ПРИНИМАЕМОЕ, и ссылка на `WR-02`
    - `app/pages/ads.py:796-803` — комментарий про пределы числа частей (G-3) и сам вызов разбора
    - `app/pages/ads.py:858-875` — комментарий про обработку частей по очереди и фразу об отказе по потолку без чтения тел
    - `nginx/nginx.conf.template`, `nginx/nginx-http.conf.template` — объявление потолка тела запроса и формула прозой: единственный предел ДО чтения
    - `tests/test_nginx_body_limit.py` — правило, сверяющее потолок обратного прокси с настройками
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md:172-193` — находка WR-01 с замером по исходникам слоя письма
  </read_first>

  <files>app/services/image_upload.py, app/pages/ads.py</files>

  <action>
    Обе записи правятся по идиоме проекта «прежняя редакция НАЗЫВАЕТСЯ, а не стирается»: снимаемое
    утверждение приводится, помечается снятым, и называется, ЧЕМ оно опровергнуто.

    В `app/services/image_upload.py` над порционным чтением: сохранить действующее и верное —
    порции не склеиваются после превышения, остаток тела этой функцией не читается, распознавание
    типа стои́т ниже предела размера вынужденно. Снять утверждение о том, что предел ограничивает
    ПРИНИМАЕМОЕ, и назвать причину снятия: на этом транспорте вызывающий получает часть уже
    разобранной — слой письма разбирает составной запрос целиком и складывает каждую файловую часть
    во временный файл с буфером в памяти ДО первой строки логики обработчика. Назвать действующую
    границу поимённо: единственный предел ДО чтения — потолок тела запроса на обратном прокси
    (`nginx/*.conf.template`, сверяется правилом `tests/test_nginx_body_limit.py`); предел размера
    картинки ограничивает то, что ДЕКОДИРУЕТСЯ и СОХРАНЯЕТСЯ. Ссылку на `WR-02` сохранить как
    исторический источник рассуждения, отметив, что на прежнем транспорте — одна файловая часть,
    читаемая самим обработчиком, — оно было верно.

    В `app/pages/ads.py`, абзац про обработку частей по очереди: снять фразу о том, что оставшиеся
    части получают отказ по потолку БЕЗ чтения тел, и заменить точным описанием того, чего отказ
    ДЕЙСТВИТЕЛЬНО избегает — декодирования, пережатия и двух записей в хранилище. Чтения он не
    избегает: тело уже разобрано. Свойство «работа, результат которой заведомо некуда деть, не
    делается» при этом остаётся верным и остаётся основанием — меняется только перечень того, что
    сэкономлено.

    В `app/pages/ads.py`, абзац про пределы числа частей (G-3): сохранить действующее основание
    (у файловых частей своего предела у слоя письма нет; форма запроса перестала быть «ровно одна
    часть») и дописать, ЧТО этот предел бережёт на самом деле — не память воркера от первой части,
    а число частей, которое разборщик согласится разобрать; практическая граница на один запрос
    называется произведением предела числа частей на размер буфера временного файла, и верхним
    рубежом над ней стои́т потолок тела на обратном прокси.

    Ни одна проверка, ни один предел и ни одно значение этой задачей не двигаются: правятся только
    комментарии. Разойдись правка с этим — суита обязана покраснеть, и именно это меряет прогон
    ниже.
  </action>

  <verify>
    <automated>uv run pytest tests/test_services/test_image_upload.py tests/test_pages/test_ads_image_upload.py tests/test_nginx_body_limit.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога — правка комментария затронула поведение</fails_when>
    <automated>git diff --numstat -- app/services/image_upload.py app/pages/ads.py</automated>
    <fails_when>вывод пуст — ни один из двух файлов не правлен, то есть задача не исполнена</fails_when>
  </verify>

  <acceptance_criteria>
    - Комментарий над порционным чтением в `app/services/image_upload.py` называет потолок тела запроса на обратном прокси единственным пределом до чтения и называет прежнюю редакцию снятой.
    - Абзац в `app/pages/ads.py`, описывающий отказ по потолку, перечисляет декодирование, пережатие и записи в хранилище как то, чего отказ избегает, и не утверждает, что тела не читаются.
    - Абзац про пределы числа частей называет практическую границу на один запрос произведением числа частей на размер буфера временного файла.
    - `uv run pytest tests/test_services/test_image_upload.py -q` зелено при том же числе собранных правил, что до задачи.
  </acceptance_criteria>

  <done>Два комментария описывают рубеж, который транспорт действительно держит, и называют тот, который он не держал никогда.</done>
</task>

<task type="auto">
  <name>Задача 3: Две оставшиеся ссылки на снятый тестовый модуль перенацелены (IN-01)</name>

  <read_first>
    - `tests/test_nginx_body_limit.py:24-36` — докстринг модуля, ссылка на приём из снятого модуля
    - `tests/test_services/test_images.py:1-12` — докстринг модуля, ссылка на сквозной путь эндпоинта в снятом модуле
    - `tests/test_services/test_image_upload.py:1-40` — модуль, в который переехали утверждения о сервисе; действующий адрес для перенацеливания
    - `tests/test_pages/test_ads_image_upload.py:125-133` — образец уже перенацеленной ссылки (её закрыл план 12-06): дисциплина «путь заменяется описанием словами»
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-05-SUMMARY.md` — какой именно модуль снят и чем закрыты его утверждения
  </read_first>

  <files>tests/test_nginx_body_limit.py, tests/test_services/test_images.py</files>

  <action>
    Обе ссылки указывают на `tests/test_routes/test_uploads.py`, снятый планом 12-05 целиком. Фаза
    применила дисциплину «путь заменяется описанием словами» в пяти местах и пропустила три; третье
    закрыл план 12-06 в своём файле.

    В `tests/test_nginx_body_limit.py` ссылку на приём «ожидание берётся из настроек, а не из
    литерала» перенацелить на действующий адрес — правило того же смысла в
    `tests/test_services/test_image_upload.py` — либо описать приём СЛОВАМИ без пути, если правила
    ровно того же имени там нет. Выбор между двумя формами делать чтением действующего модуля, а не
    предположением: ссылка на несуществующее имя правила хуже описания словами.

    В `tests/test_services/test_images.py` ссылку «сквозной путь эндпоинта закрыт в …» перенацелить
    на два действующих места, потому что сквозной путь после переезда разделён надвое: сервисная
    половина — `tests/test_services/test_image_upload.py`, страничная — `tests/test_pages/test_ads_image_upload.py`.
    Утверждение о том, что объектов в хранилище становится два, живёт теперь в сервисной половине.

    ⚠️ ЛЕТОПИСИ СО СНЯТЫМ ПУТЁМ ДОСЛОВНО ЗДЕСЬ НЕ ПИШЕТСЯ, И ЭТО ИСКЛЮЧЕНИЕ ИЗ ИДИОМЫ «прежняя
    редакция называется, а не стирается» — исключение названное, а не молчаливое. Прежний адрес
    описывается СЛОВАМИ («модуль тестов прежнего JSON-маршрута загрузки, снятый планом 12-05»),
    потому что дословно набранный путь удовлетворил бы приёмочный обход сам и сделал бы правило
    самоопровергающимся. Та же оговорка действует для файла, перенацеленного планом 12-06.

    Ни одного утверждения ни в одном из двух файлов не трогать: правятся только докстринги модулей.
  </action>

  <verify>
    <automated>uv run pytest tests/test_nginx_body_limit.py tests/test_services/test_images.py tests/test_services/test_image_upload.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
    <automated>grep -rln "test_routes/test_uploads" tests/ || echo NO_STALE_CITATIONS</automated>
    <fails_when>вывод содержит хотя бы один путь файла вместо строки NO_STALE_CITATIONS</fails_when>
  </verify>

  <acceptance_criteria>
    - Прогон `grep -rln "test_routes/test_uploads" tests/` не находит ни одного файла (вывод пуст, команда печатает подстановку `NO_STALE_CITATIONS`).
    - Докстринг `tests/test_services/test_images.py` называет оба действующих места — сервисное и страничное.
    - Число собранных правил в `tests/test_nginx_body_limit.py` и `tests/test_services/test_images.py` не изменилось.
  </acceptance_criteria>

  <done>Ни одна строка суиты не указывает на модуль, снятый этой же фазой.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| чужая страница → `POST /ads/images` | аутентификация идёт кукой, составной запрос есть «простой» запрос перекрёстного происхождения, и браузер прикладывает куку сам |
| обратный прокси → воркер приложения | единственный предел размера тела ДО того, как приложение что-либо прочитает |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-10-01 | Spoofing | `ads_images_upload`, межсайтовая отправка формы | high | mitigate | вызов `is_same_origin` ПОСЛЕ прав и ДО разбора тела; отказ кодом 403 без тела, причина чужому источнику не сообщается (ASVS L1 V4.2.2, решение владельца D-15) |
| T-12-10-02 | Denial of Service | буферизация тела межсайтового запроса | high | mitigate | сверка стои́т выше разбора составного запроса, поэтому межсайтовый отправитель не оплачивает воркеру буферизацию до потолка тела |
| T-12-10-03 | Repudiation | комментарий, утверждающий несуществующую защиту | medium | mitigate | оба комментария приведены к действительной границе; прежние редакции названы снятыми с указанием, чем опровергнуты |
| T-12-10-04 | Denial of Service | потолок тела, поднятый на все адреса (`WR-02`) | medium | accept | вне рамки партии решением владельца; остаётся открытой записью ревью с названным воздействием |
| T-12-10-05 | Elevation of privilege | запрос без обоих заголовков источника | low | accept | названная граница защиты самой гарды, выписанная в её докстринге и действующая для всех четырнадцати мест вызова одинаково |
</threat_model>

## Артефакты, которые этот план производит

- `app/pages/ads.py`: второе место вызова `is_same_origin` в модуле
- `tests/test_pages/test_ads_image_upload.py`: правило `test_a_cross_site_form_writes_nothing_to_storage`
- `tests/test_pages/test_origin_guard_on_destructive_routes.py`: значение `ORIGIN_GUARD_CALL_SITES_MEASURED = 14` и новая строка летописи

Новых функций, файлов, шаблонов, маршрутов и текстов для человека план не заводит. Запрет
«замеряющее правило инвертируется вверх» авторизован планом 12-08 и здесь исполняется, а не
перечеканивается.

<verification>
- Межсайтовая форма получает 403 и не пишет в хранилище.
- Три объявленных числа группы замеров сходятся с измеренными: 37 / 14 / 10.
- Два комментария-инварианта описывают действительную границу транспорта.
- Ссылок на снятый тестовый модуль в суите не осталось.
</verification>

<success_criteria>
- Решение владельца D-15 исполнено, и замеряющее правило утверждает строго больше, чем утверждало.
- Находки `WR-01` и `IN-01` видны в задачах, действиях и приёмочных признаках.
- `WR-02` названа и оставлена открытой записью ревью, а не свёрнута молча.
</success_criteria>

<output>
Создать `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-10-SUMMARY.md` по завершении.
</output>
