---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 07
type: execute
wave: 2
depends_on: ["12-06"]
gap_closure: true
autonomous: true
requirements: [FETCH-01]
files_modified:
  - app/pages/ads.py
  - app/templates/ads/includes/autosave_response.html
  - app/templates/ads/includes/media_upload_form.html
  - tests/test_pages/test_ads_image_upload.py
  - tests/test_pages/test_ads_editor.py
  - tests/test_templates/test_htmx_markup_gates.py
  - .planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Строка отказа переживает автосохранение, которое ЭТОТ ЖЕ ответ и заказывает: ответ автосохранения, не менявший состав вложений, не приносит узла `#media-tray` вовсе (гап 2, CR-01, D-16)."
    - "Убранное кнопкой «×» вложение по-прежнему исчезает с экрана: ответ автосохранения, в котором ключ БЫЛ убран, приносит `#media-tray` с признаком внеполосной подмены — покупка плана 12-03 не потеряна."
    - "Форма загрузки объявляет ту же стратегию наложения, что форма объявления (`hx-sync=\"this:queue last\"`), поэтому второй выбор файлов сериализуется за первым и включает уже приехавшие ключи (гап 3, WR-04, D-16)."
    - "Свойство измеряется правилом, которое рендерит ДВА фрагмента друг против друга: до этой партии таких правил в суите не было ни одного, и именно поэтому все три дефекта прошли зелёными."
  artifacts:
    - "app/pages/ads.py — `_autosave_response` принимает признак перерисовки полосы; `_save_from_editor` ставит его ровно тогда, когда состав вложений изменился"
    - "app/templates/ads/includes/autosave_response.html — включение полосы под условием, порядок включений не тронут"
    - "app/templates/ads/includes/media_upload_form.html — вызов `form_wrapper` с `sync='this:queue last'`"
    - "tests/test_pages/test_ads_image_upload.py — `test_a_refusal_row_survives_the_autosave_the_same_response_triggers`"
    - "tests/test_pages/test_ads_editor.py — `test_autosave_response_carries_the_media_tray` перенацелено на путь убирания; сосед `test_a_text_only_autosave_leaves_the_tray_alone`"
    - "tests/test_templates/test_htmx_markup_gates.py — правило равенства стратегии наложения двух форм редактора"
    - ".planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md — записанное допущение о конкурентности (D-17)"
  key_links:
    - "`HX-Trigger-After-Swap: ads-image-attached` ответа загрузки → `hx-trigger=\"… ads-image-attached from:body\"` формы объявления → ответ автосохранения: круг замкнут, и он больше не стирает то, ради чего D-04 заплатил лживым кодом 200"
    - "`hx-sync` формы загрузки ↔ `hx-sync` формы объявления: разные элементы, одна стратегия; равенство держится правилом, а не совпадением"
  assumptions:
    - statement: "Оборванная партия оставляет записанные объекты в хранилище без ссылки, а две вкладки одного объявления считают свободные места каждая из своего снимка скрытых полей. Это ПРИНЯТОЕ допущение, а не работа."
      source: edge-probe
      requirement_id: FETCH-01
      category: concurrency
      status: unresolved
      decision: D-17
    - statement: "При стратегии `this:queue last` третий быстрый выбор файлов ВЫТЕСНЯЕТ второй из очереди, и файлы второго выбора не уходят вовсе. Остаток назван, а не закрыт: стратегия выбрана владельцем решением D-16."
      source: planner
      requirement_id: FETCH-01
      category: concurrency
      status: unresolved
      decision: D-16
  prohibitions:
    - statement: "Файл, который человек выбрал, не исчезает МОЛЧА: всё, что не прикрепилось, получает названную строку, а всё, что прикрепилось, остаётся видимым."
      status: unverified
      verification: none

security:
  asvs_level: 1
  block_on: high
---

<objective>
Закрыть гапы 2 и 3 верификации и находки ревью `CR-01` и `WR-04` (половина наложения в полёте) по
решению владельца **D-16**, и ЗАПИСАТЬ допущение о конкурентности по решению **D-17**.

Purpose: корень у обоих гапов один и назван верификатором — «полоса стала единственным состоянием,
и любой, кто перерисует её из неполной истины, стирает работу человека». Здесь снимаются два таких
писателя: ответ автосохранения, рисующий полосу из базы, отстающей от документа на незаписанный
ключ, и второй летящий запрос загрузки со своим снимком ключей.

Output: ответ автосохранения перестаёт трогать полосу, когда состав вложений не менялся; форма
загрузки получает очередь; и в суите появляется правило, рендерящее ДВА фрагмента друг против
друга — то, чего в ней не было и из-за чего дефекты прошли зелёными.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-03-SUMMARY.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Ответ автосохранения перестаёт перерисовывать полосу, когда состав вложений не менялся</name>

  <read_first>
    - `app/pages/ads.py:422-448` — `_autosave_response`, его контекст шаблона и три прочих места вызова (:488, :627, :735)
    - `app/pages/ads.py:499-640` — `_save_from_editor`: разбор `remove_image` (около :570) и сборщик `_fragment` (:611-630)
    - `app/templates/ads/includes/autosave_response.html` целиком — особенно :36-43, порядок включений и наследование признака `oob`
    - `app/templates/ads/includes/media_strip.html:33-53` — узел `#media-tray` и условие признака внеполосной подмены
    - `tests/test_pages/test_ads_editor.py:614-662` — `test_autosave_response_carries_the_media_tray`: правило, которое ПЕРЕНАЦЕЛИВАЕТСЯ вместе с предметом
    - `tests/test_pages/test_ads_editor.py:40-160` — `form_body`, `HX_HEADERS`, `_seed_ad`, `_user`, `image_key`
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md` — гап 2 целиком
  </read_first>

  <files>app/pages/ads.py, app/templates/ads/includes/autosave_response.html, tests/test_pages/test_ads_editor.py, tests/test_pages/test_ads_image_upload.py</files>

  <behavior>
    - Автосохранение, в теле которого нет `remove_image` (или значение которого в списке ключей не найдено), отвечает без узла `id="media-tray"`: предпросмотр, сводка, индикатор и скрытое поле записи на месте, полосы нет.
    - Автосохранение, убравшее ключ, отвечает узлом `id="media-tray"` с признаком `hx-swap-oob` и без убранного ключа — все утверждения существующего правила держатся на этом пути.
    - Смешанная партия на `/ads/images` даёт фрагмент со строками отказа; последующее автосохранение с теми же скрытыми ключами и без `remove_image` строк отказа с экрана не уносит, потому что узла полосы в его ответе нет.
    - Ответ недоступного объявления (`INACCESSIBLE_AD_MESSAGE`, :735) и ответ отказа по вложениям (`_attachment_refusal`, :488) полосу не приносят — умолчание признака ложно.
  </behavior>

  <action>
    В `app/pages/ads.py` расширить `_autosave_response` необязательным именованным параметром `repaint_media: bool = False` и положить его в контекст шаблона ключом `repaint_media`. Умолчание ЛОЖЬ — и это не осторожность, а суть правки: перерисовка полосы становится исключением, которое надо заслужить, вместо умолчания, которое стирает.

    В `_save_from_editor` завести признак «состав вложений изменился этим запросом»: он истинен ровно тогда, когда `remove_image` пришёл И значение действительно убрано из `image_list`. Сборщик `_fragment` передаёт признак в `_autosave_response`. Два прочих вызывающих (`_attachment_refusal`, ветвь недоступного объявления) не правятся — им умолчание подходит по существу: ни один из них состава вложений не менял.

    В `app/templates/ads/includes/autosave_response.html` обернуть включение полосы условием `repaint_media`. Три ограничения на форму правки, каждое несущее:
    (а) ПОРЯДОК ВКЛЮЧЕНИЙ НЕ МЕНЯЕТСЯ и признак `oob`, поставленный строкой 36 для включения `autosave.html`, продолжает доставаться полосе наследованием области видимости. Находка `IN-02` о хрупкости этого наследования решением владельца оставлена ОТКРЫТОЙ ЗАПИСЬЮ РЕВЬЮ и в эту партию не входит; условие ставится ВНУТРИ существующего порядка, а не вместо него.
    (б) Литерал `hx-swap-oob` в `media_strip.html` не трогается, поэтому инвентарь внеполосных блоков (`OOB_BLOCKS = 20`) не двигается: условие стоит у ВКЛЮЧЕНИЯ, а не у признака.
    (в) Комментарий шапки файла (:1-33), перечисляющий пять внеполосных идентификаторов, дополняется абзацем по идиоме «прежняя редакция называется, а не стирается»: полоса приезжает теперь НЕ каждым автосохранением, а только тем, которое состав вложений изменило, и причина названа — ответ, перерисовывающий полосу из базы, отстающей от документа на незаписанный ключ, уносит с экрана и свежую плитку, и строки отказа.

    В `tests/test_pages/test_ads_editor.py` правило `test_autosave_response_carries_the_media_tray` ПЕРЕНАЦЕЛИВАЕТСЯ на путь убирания, а не ослабляется: запрос получает `remove_image=<ключ>`, все три существующих утверждения (узел, признак подмены, скрытое поле оставшегося ключа) сохраняются дословно, а в докстринг дописывается летопись перенацеливания с причиной. Рядом заводится `test_a_text_only_autosave_leaves_the_tray_alone`: тот же объект, запрос без `remove_image`, утверждение — узла `id="media-tray"` в ответе нет, а `id="ad-preview"` и `id="autosave-indicator"` есть.

    В `tests/test_pages/test_ads_image_upload.py` завести правило на ВЗАИМОДЕЙСТВИЕ двух фрагментов — `test_a_refusal_row_survives_the_autosave_the_same_response_triggers`: сперва `POST /ads/images` со смешанной партией (одна годная картинка PNG и одна часть `.webp`), из ответа берутся `refusal_rows` и `HIDDEN_KEY_FIELD.findall`; затем `POST /ads/{ad_id}/edit` ровно с этими ключами и без `remove_image` — то есть точно тот запрос, который поднимает слушатель `ads-image-attached`. Утверждения: в первом ответе строк отказа одна, во втором ответе подстроки `id="media-tray"` нет вовсе, и ключ принятого файла во втором ответе сохранён в `Ad.images`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_editor.py tests/test_pages/test_ads_image_upload.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py::test_a_refusal_row_survives_the_autosave_the_same_response_triggers tests/test_pages/test_ads_editor.py::test_a_text_only_autosave_leaves_the_tray_alone tests/test_pages/test_ads_editor.py::test_autosave_response_carries_the_media_tray -q</automated>
    <fails_when>ненулевой код возврата, либо в итоге не "3 passed"</fails_when>
    <automated>uv run pytest tests/test_templates/test_htmx_inventory.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в итоге — инвентарь внеполосных блоков сдвинулся, хотя признак не трогали</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "repaint_media" app/pages/ads.py` не меньше 3 (параметр, контекст, передача из сборщика).
    - `grep -c "repaint_media" app/templates/ads/includes/autosave_response.html` не меньше 1.
    - В `app/templates/ads/includes/autosave_response.html` строка `{% set oob = true %}` по-прежнему стои́т ВЫШЕ включения `ads/includes/media_strip.html`.
    - `test_autosave_response_carries_the_media_tray` содержит `remove_image` в теле запроса и сохраняет утверждения про `id="media-tray"`, `hx-swap-oob` и `name="images"`.
    - `uv run pytest tests/test_templates/test_htmx_inventory.py -q` зелено при неизменном объявленном числе внеполосных блоков.
  </acceptance_criteria>

  <done>Строка отказа, показанная смешанной партией, переживает автосохранение, которое эта же партия и заказывает; убранная кнопкой «×» плитка по-прежнему исчезает с экрана.</done>
</task>

<task type="auto">
  <name>Задача 2: Форма загрузки получает ту же очередь, что форма объявления</name>

  <read_first>
    - `app/templates/ads/includes/media_upload_form.html:32-55` — вызов макроса `form_wrapper`
    - `app/templates/components/form_wrapper.html:187-199` — сигнатура макроса; параметр `sync` уже существует (:187, печать :192)
    - `app/templates/ads/form.html:149-156` — форма объявления: `hx-sync="this:queue last"`, действующий образец
    - `tests/test_templates/test_htmx_markup_gates.py:3558-3660` — `DISABLED_ELT_EXCEPTIONS`, запись формы загрузки и абзац про запрет Фазы 8 на слово `queue` (:3611): перед правкой ПРОЧИТАТЬ, что запрет адресован пяти маршрутам периметра
    - `tests/test_templates/test_htmx_markup_gates.py:1440-1460` и `:2575-2595` — прочие места, где форма загрузки уже перечислена
  </read_first>

  <files>app/templates/ads/includes/media_upload_form.html, tests/test_templates/test_htmx_markup_gates.py</files>

  <action>
    Сперва ПРОВЕРИТЬ по исходнику, а не предположить: запрет Фазы 8 на слово `queue` (назван в `tests/test_templates/test_htmx_markup_gates.py:3611`) адресован пяти маршрутам периметра, и форма объявления `ads/form.html` уже несёт `hx-sync="this:queue last"` зелёной суитой — то есть поверхность редактора объявлений в периметр не входит. Если чтение исходника даст иное, ОСТАНОВИТЬСЯ и записать расхождение в сводку, а не обходить запрет.

    В `app/templates/ads/includes/media_upload_form.html` дописать в вызов `form_wrapper` параметр `sync='this:queue last'` — ровно ту строку, которую называет решение владельца D-16, и ровно то значение, которое несёт форма объявления. Параметр у макроса уже есть (`components/form_wrapper.html:187`), нового кода в макросе не пишется.

    Комментарий шапки файла дополнить четвёртым названным свойством: форма загрузки и форма объявления — РАЗНЫЕ элементы, поэтому очередь одной на другую не распространяется, и второй быстрый выбор файлов давал два летящих запроса, каждый со своим снимком ключей; второй ответ подменой `innerHTML` уносил ключи первого, объекты оставались в хранилище сиротами, а человек видел меньше плиток, чем выбрал файлов, БЕЗ сообщения. Назвать и остаток: при вытесняющей очереди третий быстрый выбор вытесняет второй, и файлы второго выбора не уходят вовсе — стратегия взята решением владельца D-16, остаток записан допущением (задача 3).

    В `tests/test_templates/test_htmx_markup_gates.py` завести правило равенства стратегии наложения двух форм редактора: обе формы читаются из исходников шаблонов, у формы объявления берётся литерал `hx-sync`, у формы загрузки — значение аргумента `sync` вызова макроса, и утверждается их равенство. Форму правила и способ чтения исходника взять у соседей того же модуля (`_strip_comments`, чтение дерева от корня по собственному пути). Сообщение утверждения объясняет, ЧТО увидит человек при расхождении: плитки, пропавшие без сообщения.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "this:queue last" app/templates/ads/includes/media_upload_form.html` не меньше 1.
    - Отрендеренная форма загрузки несёт атрибут `hx-sync` со значением, равным значению `hx-sync` формы `#ad-form` в `app/templates/ads/form.html`, и равенство утверждено новым правилом суиты.
    - Параметр `disabled_elt=''` вызова сохранён — именованное изъятие D-09 не тронуто, и запись в `DISABLED_ELT_EXCEPTIONS` остаётся верной.
    - Прогон `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q` зелен, число объявленных изъятий цели блокировки не двигалось.
  </acceptance_criteria>

  <done>Два быстрых выбора файлов дают два запроса, идущих по очереди: второй сериализуется после первого и включает уже приехавшие ключи.</done>
</task>

<task type="auto">
  <name>Задача 3: Допущение о конкурентности записано явно (D-17)</name>

  <read_first>
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md` — действующая форма записи (поля `status`, `What`, `Found during`, `Why not fixed here`, `Impact`)
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md:242-248` — решение владельца D-17 дословно
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md:369-381` — находка `IN-05`, из которой растёт половина допущения
    - `app/pages/ads.py` — строка расчёта свободных мест (после правок плана 12-06)
  </read_first>

  <files>.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md, app/pages/ads.py</files>

  <action>
    Решение владельца D-17 требует, чтобы допущение было ЗАПИСАНО, а не закрыто и не забыто. Дописать в `deferred-items.md` две записи в действующей форме файла, со статусом `accepted-assumption`, а не `open`:

    Первая — «Оборванная партия оставляет объекты-сироты, а две вкладки считают свободные места из своих снимков». Содержание: партия, прерванная закрытием вкладки между записью объекта и приходом фрагмента, оставляет в хранилище объект, на который не ссылается ни одно объявление; две вкладки одного объявления считают число свободных мест каждая из своего набора скрытых полей, поэтому суммарно могут принять больше файлов, чем показывает потолок. Указать, что настоящая квота на объявление стоит НЕ здесь, а на сохранении (`own_image_keys`), и значение на загрузке есть цифра согласованности интерфейса, а не ресурсный предел (`IN-05` ревью). Причина невыполнения — решение владельца D-17: взять в эту партию значило бы вырастить объём, а завести отдельной фазой — записать в ROADMAP предмет, которого нет. Это ровно та неразрешённая кромка, которую пробник кромок выдал на FETCH-01 категорией `concurrency`.

    Вторая — «Вытесняющая очередь теряет средний выбор файлов». Содержание: при `hx-sync="this:queue last"` третий быстрый выбор файлов вытесняет из очереди второй, и файлы второго выбора не уходят на сервер вовсе; человек видит плитки первого и третьего выбора. Стратегия названа решением владельца D-16 поимённо, поэтому остаток фиксируется записью, а не правкой. Назвать и альтернативу, и цену её отклонения: очередь «все подряд» на файловом поле переотправила бы ТЕКУЩЕЕ содержимое поля, то есть продублировала бы последний выбор вместо восстановления пропавшего.

    В `app/pages/ads.py` рядом со строкой расчёта свободных мест дописать одну ссылку прозой на то, что цифра есть согласованность интерфейса, а не квота, и что настоящий предел живёт на сохранении. Ссылку дать словами на запись в `deferred-items.md`, без правки самого расчёта: находка `IN-05` решением владельца оставлена ОТКРЫТОЙ ЗАПИСЬЮ РЕВЬЮ, и эта задача её не закрывает — она делает принятое допущение читаемым там, где следующий читатель на него наткнётся.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_pages/test_ads_editor.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога — правка комментария затронула поведение</fails_when>
  </verify>

  <acceptance_criteria>
    - В `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md` появились две записи, и обе несут `status: accepted-assumption`.
    - Обе записи называют решение владельца по идентификатору: первая — `D-17`, вторая — `D-16`.
    - Первая запись называет `own_image_keys` как место настоящей квоты и `IN-05` как источник наблюдения.
    - В `app/pages/ads.py` рядом с расчётом свободных мест есть прозаическая отсылка к записи допущения; сам расчёт не изменён этой задачей.
  </acceptance_criteria>

  <done>Допущение о конкурентности существует записью, названной по решению владельца, а не молчанием; вторая, вытекающая из выбранной стратегии очереди, названа рядом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ автосохранения → документ | внеполосная подмена заменяет узел ЦЕЛИКОМ и способна унести состояние, которого сама не производила |
| два летящих запроса от одного человека → одна цель подмены | каждый несёт собственный снимок состояния вложений |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07-01 | Tampering | `autosave_response.html`, внеполосный `#media-tray` | high | mitigate | полоса приезжает только тем ответом, который состав вложений изменил; ответ, ничего не менявший, узла не несёт |
| T-12-07-02 | Denial of Service | наложение запросов загрузки | medium | mitigate | `hx-sync="this:queue last"` сериализует форму загрузки; два летящих составных запроса на одного человека больше не возникают |
| T-12-07-03 | Repudiation | строка отказа, живущая один круг к серверу | medium | mitigate | причина отказа остаётся на экране; человек может её прочитать, а не догадаться о ней |
| T-12-07-04 | Denial of Service | объекты-сироты при обрыве партии | low | accept | принято решением владельца D-17, записано в `deferred-items.md` с названным воздействием (расход хранилища, ограниченный потолком запроса) |
</threat_model>

## Артефакты, которые этот план производит

- `app/pages/ads.py`: именованный параметр и ключ контекста `repaint_media`
- `app/templates/ads/includes/autosave_response.html`: условие `repaint_media` у включения полосы
- `app/templates/ads/includes/media_upload_form.html`: аргумент `sync='this:queue last'`
- `tests/test_pages/test_ads_image_upload.py`: правило `test_a_refusal_row_survives_the_autosave_the_same_response_triggers`
- `tests/test_pages/test_ads_editor.py`: правило `test_a_text_only_autosave_leaves_the_tray_alone`
- `tests/test_templates/test_htmx_markup_gates.py`: правило равенства стратегии наложения двух форм редактора
- `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md`: две записи со статусом `accepted-assumption`

Новых маршрутов, идентификаторов разметки, заголовков ответа и текстов для человека план не заводит.

## Кромка, выданная пробником, и что с ней сделано

Пробник кромок выдал на FETCH-01 ровно одну строку: категория `concurrency`, статус `unresolved`,
вопрос «что гарантировано при обрыве или параллельном исполнении». Она НЕ разрешена автоматически и
НЕ отброшена: половина её (наложение в полёте) закрывается задачами 1 и 2 по решению владельца D-16,
вторая половина (сироты при обрыве, межвкладочный счёт свободных мест) остаётся названным
допущением по решению владельца D-17 и записана задачей 3. Равенство «одна выданная кромка — один
авторизованный или помеченный пункт» соблюдено блоком `must_haves.assumptions` выше.

<verification>
- Смешанная партия и последующее автосохранение сведены одним правилом; строки отказа переживают круг.
- Убирание кнопкой «×» по-прежнему уносит плитку с экрана — правило перенацелено, а не удалено.
- Стратегии наложения двух форм редактора равны, и равенство держит правило.
- Допущение о конкурентности существует записью с идентификаторами решений владельца.
</verification>

<success_criteria>
- Гапы 2 и 3 верификации закрыты кодом, и каждый закрыт правилом, рендерящим два фрагмента друг против друга.
- Находки вне рамки партии (`WR-02`, `IN-02`…`IN-06`) не тронуты; `IN-02` и `IN-05` названы в плане как оставленные открытыми записями ревью.
- D-16 и D-17 процитированы по идентификатору.
</success_criteria>

<output>
Создать `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-07-SUMMARY.md` по завершении.
</output>
