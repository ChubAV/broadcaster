---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 05
subsystem: testing
tags: [htmx, fastapi, image-upload, gates, inventory, test-migration]

# Dependency graph
requires:
  - phase: 12-zagruzka-izobrazheniy-bez-fetch
    provides: "план 12-01 — `app/services/image_upload.py` как единственный дом не-транспортной логики, страничный маршрут `POST /ads/images` внутри `ads_router`, запись гейта подстановки личности на новый обработчик, окно сосуществования двух входов"
  - phase: 12-zagruzka-izobrazheniy-bez-fetch
    provides: "план 12-04 — окончательная форма страничного обработчика: разбор партии целиком, `own_image_keys` до любой работы, один код ответа на три исхода, заголовок события после подмены"
  - phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
    provides: "перечни исключений разметочных гейтов `DISABLED_ELT_EXCEPTIONS` и `MACRO_BORN_EXCEPTIONS` и записи 6–7 их сводной летописи, адресовавшие два изъятия Фазе 12"
  - phase: 11-massovyy-perevod-razdelov-pisma
    provides: "D-07 — `app/routes/` объявлен «другим транспортом»; конвенция, по которой принято решение о поверхности маршрута"
provides:
  - "`app/services/image_upload.py` стал ЕДИНСТВЕННЫМ домом логики загрузки: второй копии в дереве нет"
  - "`tests/test_services/test_image_upload.py` — 38 правил о сервисе приёма (53 собранных случая), переехавших из суиты снятого JSON-входа"
  - "Решение о поверхности маршрута загрузки отражено во ВСЕХ ТРЁХ перечнях гейтов, а не в двух"
  - "Два изъятия Фазы 9 для `ads/form.html` переадресованы Фазе 15 (FORM-01) записью с названной причиной"
  - "`README.md` без исчезнувшего адреса слоя JSON-API и с названным переездом"
affects: [13-fetch-02, 15-form-01, 15-fetch-03]

# Actuals (#2632) — chars/4 по РЕАЛИЗОВАННОМУ диффу, тот же прибор, что у сводки 12-04.
# ⚠️ ШКАЛЫ `estimate` И `actuals` ЗДЕСЬ РАСХОДЯТСЯ, И ЭТО НАЗВАНО, А НЕ СГЛАЖЕНО.
# Проекция плана (125 000 — самая крупная в фазе) выведена из НЕДЕЛИМОЙ ПРЕАМБУЛЫ:
# @-включённых `12-CONTEXT.md`, `12-RESEARCH.md`, двух сводок и рабочего процесса
# исполнения, — а замер ниже покрывает только дифф тринадцати файлов. Сводить их
# вычитанием нельзя: они меряют разное, и числом, годным для калибровки проекции
# преамбулы, этот замер НЕ является. Форма записи та же, что у сводок 12-01, 12-03
# и 12-04; названа она затем, чтобы повторный замер не прочёлся как расхождение.
# ⚠️ `commits` ИЗМЕРЕН `git rev-list --count <plan_head_before>..HEAD` В МОМЕНТ
# ЗАПИСИ ЭТОЙ СВОДКИ и потому считает ДВА коммита задач, но НЕ следующий за ними
# коммит метаданных плана — тот появляется позже и на HEAD даёт три.
actuals:
  tokens: 33043
  tasks: 2
  commits: 2
  plan_head_before: 7127978463ce4861e7f9e7cb072887961ae00573

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Утверждение о сервисе проверяется вызовом сервиса, а не HTTP-запросом: файловая часть собирается прямо, и класс её параметрический — измерение объёма чтения идёт по ОБОИМ классам, что приезжают в сервис"
    - "Инвентарное число ставится ПРОГОНОМ покрасневшего правила, и вывод прогона переносится в летопись дословно — включая случай, когда второе утверждение теста видно лишь после правки первого"
    - "Чужое изъятие ПЕРЕАДРЕСУЕТСЯ записью: число не двигается, и именно поэтому смена адресата выписывается словами — молчащее число прочиталось бы как «фаза забыла»"
    - "Проза комментария не НАБИРАЕТ пути и имени, которые сличает критерий: гейт считает вхождения в ТЕКСТЕ файла и объяснение от кода не отличает"

key-files:
  created:
    - tests/test_services/test_image_upload.py
  modified:
    - app/main.py
    - app/services/image_keys.py
    - app/services/images.py
    - README.md
    - tests/test_pages/test_ads_image_upload.py
    - tests/test_pages/test_access_gate.py
    - tests/test_pages/test_impersonation_gate.py
    - tests/test_templates/test_htmx_markup_gates.py
    - tests/test_pages/test_htmx_gates.py
    - tests/test_pages/test_ads_image_ownership.py
    - tests/test_routes/test_ads.py
  removed:
    - app/routes/uploads.py
    - tests/test_routes/test_uploads.py

key-decisions:
  - "Критерий «прежнего адреса модуля в суите не осталось» НЕЛЬЗЯ было закрыть задачей 1 целиком, и это названо, а не обойдено: две последние его строки держит запись гейта подстановки личности, а снять её раньше модуля значило бы оставить живой маршрут ВНЕ всех трёх множеств и уронить гейт по построению. Критерий доведён до нуля задачей 2 и перепроверен там"
  - "Измерение объёма чтения ПАРАМЕТРИЗОВАНО двумя классами файловой части (базовым Starlette и подклассом FastAPI). У HTTP-входа класс приносил разбор запроса и выбирать его тест не мог; у прямого вызова сервиса класс выбирает сам тест, и утверждение «читал с ограничением размера» стало бы тавтологией на одном классе"
  - "Утверждение о коде 502 РАЗДВОЕНО, а не перенесено: сервис поднимает исключение (отказ инфраструктуры, а не отказ файлу), маршрут отвечает 502 и НЕ прячет аварию во фрагмент с кодом 200 — это граница правила «всегда 200» (D-04), и без утверждения о ней граница держалась бы прозой"
  - "Причина записи гейта подстановки личности сохранена ДОСЛОВНО при перенацеливании: решение о загрузке под чужой личностью от транспорта не зависит, и переписанная заодно с переездом причина выглядела бы новым решением владельца, которого не было"
  - "Основания правок ДВУХ перечней гейта доступа написаны раздельно, своими словами для каждого вопроса, без ссылки друг на друга: файл объявляет множества независимыми и выведение одного из другого запрещает прямо — правка «заодно» и была бы таким выведением, сделанным руками"

patterns-established:
  - "Баланс переезда суиты записывается числами по всем четырём исходам — уехало, добавилось, снято вместе с предметом, разделилось на два — и сверяется с собранным числом суиты: без этого «переехало» неотличимо от «потерялось»"
  - "Ссылка комментария на снятый модуль переадресуется ОПИСАНИЕМ, а не новым путём там, где путь сличает критерий"

requirements-completed: [FETCH-01]

coverage:
  - id: D1
    description: "`app/routes/uploads.py` снят ЦЕЛИКОМ: модулей маршрутов слоя JSON-API стало на один меньше, адрес `/api/uploads/image` больше не существует, приложение собирается без него"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "test -e app/routes/uploads.py → 1; grep -c 'uploads' app/main.py → 0; ls app/routes/*.py | wc -l → 7 (было 8)"
        status: pass
      - kind: other
        ref: "uv run python -m compileall -q app main.py tests → молчит, rc=0 (приложение без снятого модуля собирается)"
        status: pass
      - kind: integration
        ref: "полный прогон suite после задачи 2: 3427 passed из 3428 собранных — единственный красный есть пред-существующий дефект окна 93"
        status: pass
    human_judgment: false
  - id: D2
    description: "1099 строк утверждений ПЕРЕЕХАЛИ, а не исчезли: утверждения о сервисе живут в `tests/test_services/test_image_upload.py`, утверждения о маршруте — в `tests/test_pages/test_ads_image_upload.py`, и баланс переезда сведён числами"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "uv run pytest tests/test_services/test_image_upload.py -q -p no:randomly → 53 passed (при требовании плана «не меньше десяти»)"
        status: pass
      - kind: unit
        ref: "uv run pytest tests/test_pages/test_ads_image_upload.py -q -p no:randomly → 14 passed (было 12)"
        status: pass
      - kind: other
        ref: "собрано суитой 3427 → 3428; баланс 40 правил = 38 в сервис + 2 снято с предметом, плюс 2 производных правила маршрута — арифметика сошлась до единицы"
        status: pass
    human_judgment: false
  - id: D3
    description: "Ни одной подмены и ни одной ссылки по прежнему адресу модуля в суите не осталось; цель подмены — имя нового сервисного модуля"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "grep -rc 'app.routes.uploads' tests/ | grep -v ':0$' → пустой вывод"
        status: pass
      - kind: unit
        ref: "grep -c 'app.services.image_upload.upload_file_to_s3' tests/test_services/test_image_upload.py → 23"
        status: pass
    human_judgment: false
  - id: D4
    description: "Решение о поверхности маршрута отражено во ВСЕХ ТРЁХ перечнях гейтов: оба перечня гейта доступа 5 → 4, запись гейта подстановки личности перенацелена на страничный обработчик с сохранённой причиной"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "tests/test_pages/test_access_gate.py + tests/test_pages/test_impersonation_gate.py → 35 passed (тот же базовый счёт, что до работ)"
        status: pass
      - kind: unit
        ref: "grep -c 'uploads_router' tests/test_pages/test_access_gate.py → 0; оба множества на месте (по 1 совпадению каждого объявления)"
        status: pass
      - kind: unit
        ref: "grep -c 'app/pages/ads.py::' tests/test_pages/test_impersonation_gate.py → 5 (при требовании плана «не меньше 4»)"
        status: pass
      - kind: unit
        ref: "MUTATING_ROUTE_COUNT 50 → 49 и MUTATING_MODULE_COUNT 15 → 14, оба ПРОГОНОМ покрасневшего правила"
        status: pass
    human_judgment: false
  - id: D5
    description: "Два изъятия, адресованных Фазе 12 сводной летописью Фазы 9, ПЕРЕАДРЕСОВАНЫ Фазе 15 записью с названной причиной — не сняты молча и не оставлены указывать на закрывающуюся фазу"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "grep -c 'НАЗНАЧЕНО ФАЗЕ 12' tests/test_templates/test_htmx_markup_gates.py → 0; grep -ci 'фазе 15' → 2 (по одной на запись)"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py в связке с двумя гейтами доступа → 114 passed; числа перечней не двинулись (2 → 2 и 1 → 1), и это записано летописью"
        status: pass
      - kind: other
        ref: "историческая летопись Фазы 9 (строки 6 и 7) ДОПОЛНЕНА пометкой; прежний текст на месте — grep -in 'назначено фазе 12' находит его"
        status: pass
    human_judgment: false
  - id: D6
    description: "`README.md` не называет исчезнувшего адреса и объясняет переезд: строка таблицы JSON-API снята, под таблицей назван новый адрес и форма ответа (фрагмент разметки, а не JSON); снятый модуль убран из дерева проекта"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "grep -c '/api/uploads/image' README.md → 0; grep -n 'uploads' README.md → пусто"
        status: pass
    human_judgment: false
  - id: D7
    description: "В браузере снятие старого входа ничего не сломало: редактор объявления по-прежнему прикрепляет картинку, полоса рисуется, признак «идёт запрос» виден"
    requirement: FETCH-01
    verification: []
    human_judgment: true
    rationale: "Машинно проверено, что старого входа нет и что суита целиком зелена; что ЖИВОЙ экран редактора после снятия модуля ведёт себя как до него — свойство браузера, которого суита не исполняет (браузерного привода в дереве нет ни одного). Пункт обхода уже стоит в очереди `12-UAT.md` требованием плана 12-04 (coverage D9); этот план предмета обхода не добавляет, но и не закрывает его."

# Metrics
duration: 1h 30m
completed: 2026-09-19
status: complete
---

# Phase 12 Plan 05: Снятие старой поверхности загрузки Summary

**Старый вход загрузки снят одним движением с тем, что его держало: 1099 строк утверждений переехали по двум адресам (38 правил о сервисе в новый модуль `tests/test_services/`, два производных правила о маршруте — в модуль страницы), `app/routes/uploads.py` исчез вместе с подключением и адресом, а решение о поверхности маршрута отражено во всех ТРЁХ перечнях гейтов — включая тот, которого критерий 3 не называет.**

## Performance

- **Duration:** 1h 30m (из них 71 минута — два полных прогона суиты по 35 минут)
- **Started:** 2026-09-19T00:35:09Z
- **Completed:** 2026-09-19T02:05:44Z
- **Tasks:** 2 из 2
- **Files modified:** 13 (1 создан, 10 правлены, 2 сняты)

## Accomplishments

- **`app/services/image_upload.py` стал ЕДИНСТВЕННЫМ домом логики загрузки.** Второй копии в дереве нет: снятый модуль объявлял `sniff_image`, `safe_filename`, `retarget_extension`, тексты отказа, таблицу сигнатур и предел размера порции — всё это переехало планом 12-01 дословно, и до этого плана в дереве жили ДВЕ копии. Модулей маршрутов в слое JSON-API стало 7 → 6.
- **Переезд суиты сведён БАЛАНСОМ, а не объявлен.** Старый модуль давал 40 правил / 54 собранных случая. 38 правил уехали в сервис (53 случая), 2 сняты вместе с предметом, и ещё 2 правила о маршруте выведены из тех, чья форма ответа сменилась. Собранное число суиты 3427 → 3428 — сходится до единицы, то есть ни одно правило не потеряно молча.
- **Ни одной подмены по прежнему адресу модуля в суите не осталось** — и это проверено грепом, у которого точка не экранирована, то есть он ловит и путь со слешами. Пять прозаических ссылок (три в суите, две в `app/services/`) переадресованы ОПИСАНИЕМ, а не новым путём: критерий считает вхождения в ТЕКСТЕ файла и комментарий от кода не отличает — ту же цену уже заплатил план 12-03.
- **Все три перечня гейтов отражают решение, и ни один не подогнан ради зелени.** `GATED_API_ROUTERS` 5 → 4 и `BLOCK_CHECKED_API_ROUTERS` 5 → 4 — двумя ОТДЕЛЬНЫМИ правками с раздельными основаниями, потому что файл объявляет множества независимыми. Третий перечень — гейт подстановки личности (находка G-7 разведки, критерий 3 его не называет): запись перенацелена на страничный обработчик с сохранённой дословно причиной, и рядом записано, что перечней три, чтобы следующий читатель не искал третий заново.
- **Требование каждого множества выполняется ДРУГИМ механизмом, а не отменяется, и это записано у каждого своими словами.** Обработчик переехал внутрь `ads_router`, где уже висит `require_access` (`app/pages/__init__.py`), а её провал едет отказом, который человек ВИДИТ — 204 с заголовком перехода, а не JSON, которого страница не покажет.
- **Два чужих изъятия ПЕРЕАДРЕСОВАНЫ, а не сняты и не оставлены указывать на закрывающуюся фазу.** `DISABLED_ELT_EXCEPTIONS['ads/form.html']` и `MACRO_BORN_EXCEPTIONS['ads/form.html']` называют теперь Фазу 15 (FORM-01, сводный обход сорока семи форм) — решением владельца, одним движением, как требует связывающий их абзац. Числа перечней при этом НЕ двинулись (2 → 2 и 1 → 1), и именно поэтому смена адресата выписана словами: молчащее число прочиталось бы при сверке как «Фаза 12 забыла».
- **`README.md` не оставляет исчезнувший адрес без объяснения.** Строка таблицы JSON-API снята; под таблицей назван новый адрес, названа форма ответа (фрагмент разметки, а не JSON) и названа фаза переезда. Снятый модуль убран и из дерева проекта.

## Task Commits

1. **Задача 1: переезд 1099 строк по двум адресам** — `c2d7bf1` (test)
2. **Задача 2: модуль снят, три перечня отражают решение, изъятия переадресованы** — `8cdeb03` (refactor)

**Plan metadata:** см. коммит `docs(12-05)` следом за этой сводкой.

⚠️ **RED-коммита у задачи 1 НЕТ, и это не нарушение дисциплины, а исход измерения предикатом.** Подробности — в разделе «TDD Gate Compliance» ниже.

## Files Created/Modified

- `tests/test_services/test_image_upload.py` — **создан**: 38 правил о сервисе приёма (53 собранных случая) с дословно перенесёнными комментариями-обоснованиями; построители настоящих изображений, бомбы разжатия, GIF, WebP и SVG; помощник сборки файловой части с параметрическим классом
- `tests/test_routes/test_uploads.py` — **снят целиком** (1099 строк) после того, как оба адресата приняли свои утверждения
- `app/routes/uploads.py` — **снят целиком** (319 строк, D-03)
- `app/main.py` — снят импорт роутера загрузки и блок его подключения с двумя зависимостями; на месте блока абзац о том, куда переехал обработчик и чем он закрывается теперь
- `tests/test_pages/test_ads_image_upload.py` — два правила о входе: SVG под именем PNG отвергается строкой отказа при коде 200 (переехало ПЕРЕВЁРНУТЫМ), авария хранилища отвечает 502 (переехало как есть); построитель байтов SVG
- `tests/test_pages/test_access_gate.py` — оба перечня по четыре роутера, у каждого своё основание
- `tests/test_pages/test_impersonation_gate.py` — запись нового обработчика с прежней причиной, `MUTATING_ROUTE_COUNT` 50 → 49, `MUTATING_MODULE_COUNT` 15 → 14, абзац о том, что перечней три
- `tests/test_templates/test_htmx_markup_gates.py` — обе записи `ads/form.html` переадресованы Фазе 15; две локальные летописи и строки 6–7 сводной летописи Фазы 9 дополнены
- `tests/test_pages/test_htmx_gates.py`, `tests/test_routes/test_ads.py`, `tests/test_pages/test_ads_image_ownership.py` — прозаические ссылки на снятый модуль описаны словами
- `app/services/image_keys.py`, `app/services/images.py` — две ссылки, устаревшие снятием модуля, переадресованы на модуль сервиса; текст правил не тронут
- `README.md` — строка снятого адреса убрана из таблицы, переезд назван под таблицей, `uploads.py` убран из дерева проекта

## Баланс переезда суиты

Требование плана: «без баланса „переехало“ неотличимо от „потерялось“».

| Исход | Правил | Собранных случаев | Что именно |
|---|---|---|---|
| Уехало в `tests/test_services/test_image_upload.py` | 38 | 52 старых + 1 новый случай параметризации = 53 | всё, кроме трёх строк ниже |
| Производные правила о маршруте в `tests/test_pages/test_ads_image_upload.py` | 2 | 2 | SVG под именем PNG (перевёрнуто), авария хранилища (как есть) |
| Снято вместе с предметом | 2 | 2 | `test_upload_unauthenticated`, `test_upload_image_with_cookie_auth` |
| **Итого старый модуль** | **40** | **54** | снят целиком |

**Два правила, разделившихся на два каждое** (учтены выше в первых двух строках, а не дважды):

- отказ SVG → в сервисе «`Rejected` и в хранилище ничего», на маршруте «200 и своя строка отказа, а `<script>` до документа не доехал»;
- провал записи объекта → в сервисе «поднимается исключение 502, миниатюра не пишется», на маршруте «ответ 502, а не фрагмент с кодом 200».

**Два снятых правила — почему у них не осталось предмета:**

1. `test_upload_unauthenticated` утверждал код 401 на JSON-входе. Аутентификацию на новой поверхности спрашивает не обработчик, а пер-роутерная зависимость страничной сборки, и отказ её едет 204 с заголовком перехода. Это свойство уже утверждено правилом `test_upload_is_closed_by_the_router_access_gate` (план 12-01); переписать старое правило значило бы завести ВТОРУЮ проверку того же с другим ожидаемым кодом.
2. `test_upload_image_with_cookie_auth` утверждал, что вход работает не только с токеном в заголовке, но и с cookie. Двойственности больше нет: страничные маршруты читают ТОЛЬКО cookie, и этим путём ходит каждое правило нового модуля через общего клиента. Предмет правила — именно вторая половина двойственности — исчез вместе с первой.

**Сверка собранным числом:** 3427 → 3428. Прибавка ровно один: −54 старого модуля, +53 сервиса, +2 маршрута. Единственный новый собранный случай сверх переехавших — вторая параметризация измерения объёма чтения (см. «Decisions»).

## Инвентарные числа (D-13)

Каждое движение поставлено ПРОГОНОМ покрасневшего правила, и вывод прогона перенесён в летопись дословно.

| Число | Было → стало | Как поставлено |
|---|---|---|
| `GATED_API_ROUTERS` | 5 → 4 | снятие записи; правило зеленеет вместе с отсутствием роутера в сборке |
| `BLOCK_CHECKED_API_ROUTERS` | 5 → 4 | то же, ОТДЕЛЬНОЙ правкой с отдельным основанием |
| `MUTATING_ROUTE_COUNT` | 50 → 49 | прогон: `изменяющих маршрутов стало 49, а выписано 50` / `assert 49 == 50` |
| `MUTATING_MODULE_COUNT` | 15 → 14 | прогон: `модулей с изменяющими маршрутами стало 14, а выписано 15` / `assert 14 == 15` |
| `DISABLED_ELT_EXCEPTIONS_DECLARED` | 2 → 2 | ДВИЖЕНИЯ НЕТ; переадресован адресат записи, и записано это словами |
| `MACRO_BORN_EXCEPTIONS_DECLARED` | 1 → 1 | ДВИЖЕНИЯ НЕТ; то же |
| Модулей маршрутов в `app/routes/` | 7 → 6 | `ls app/routes/*.py \| wc -l`: 8 → 7 файлов включая `__init__.py` |

⚠️ **`MUTATING_MODULE_COUNT` удалось увидеть только ВТОРЫМ прогоном, и это записано в самой летописи.** Оба числа проверяются одним тестом, и утверждение о числе маршрутов стоит первым — до утверждения о числе модулей исполнение не доходило вовсе. Порядок пришлось пройти дважды: сперва поставлено 49, затем перепрогон показал `14 == 15`, и только тогда поставлено 14. Поставить 14 «заодно» значило бы объявить число, покраснения которого никто не видел.

## Decisions Made

Пять решений вынесены во frontmatter (`key-decisions`). Ни одно из четырнадцати запертых решений `12-CONTEXT.md` не пересмотрено; D-03 (снять модуль целиком), D-01 (поверхность маршрута), D-13 (инвентарь одним коммитом с работой) и D-14 (чужой долг адресуется записью) соблюдены дословно. Решение владельца по вопросу 3 разведки («переадресовать ЗАПИСЬЮ», Фаза 15) исполнено буквально, включая отвергнутый владельцем вариант «снять их» — он не рассматривался заново.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Пять прозаических ссылок на снятый модуль переписаны ОПИСАНИЕМ**

- **Found during:** Задача 1, сверка `<acceptance_criteria>` (`grep -rc 'app.routes.uploads' tests/` вернул три файла при нуле кода)
- **Issue:** критерий плана записан образцом, в котором точка НЕ экранирована, поэтому он совпадает и с путём через слеши. Три комментария суиты (`tests/test_routes/test_ads.py`, `tests/test_pages/test_ads_image_ownership.py`, `tests/test_pages/test_htmx_gates.py`) называли снятый модуль ДОСЛОВНЫМ путём как источник формы ключа и как прежнюю поверхность обработчика. Плюс две ссылки в `app/services/image_keys.py` и `app/services/images.py` после снятия модуля стали указывать на файл, которого нет.
- **Fix:** во всех пяти местах путь заменён ОПИСАНИЕМ («сервис приёма загрузки в `app/services`», «снятый планом 12-05 модуль маршрута загрузки слоя JSON-API»), и при каждой правке записано, ПОЧЕМУ описание, а не новый путь. Текст самих правил и обоснований не тронут ни в одном месте.
- **Files modified:** `tests/test_routes/test_ads.py`, `tests/test_pages/test_ads_image_ownership.py`, `tests/test_pages/test_htmx_gates.py`, `app/services/image_keys.py`, `app/services/images.py`
- **Verification:** `grep -rc 'app.routes.uploads' tests/ | grep -v ':0$'` → пустой вывод; полный прогон суиты зелен (кроме пред-существующего красного окна 93)
- **Committed in:** `c2d7bf1` (три файла суиты), `8cdeb03` (два файла `app/services`)

**2. [Rule 2 — Missing Critical] Измерение объёма чтения параметризовано двумя классами файловой части**

- **Found during:** Задача 1, перенос `test_oversized_upload_is_not_buffered_whole`
- **Issue:** у HTTP-входа класс файловой части приносил разбор запроса, и старое правило УТВЕРЖДАЛО, какой класс пришёл (`{cls} == {StarletteUploadFile}`) — именно затем, что подкласс FastAPI передаёт размер в базовый метод ЯВНО и «аргумента не было» на нём различить нельзя. У прямого вызова сервиса класс выбирает сам тест, поэтому то же утверждение стало бы тавтологией, а измерение перестало бы покрывать класс, который в сервис реально приезжает.
- **Fix:** помощник сборки части получил параметр класса, а правило — параметризацию по обоим классам; вместе с ней добавлено утверждение, что запрошенные размеры порций равны ОБЪЯВЛЕННОМУ в сервисе. Правило `test_read_measurement_targets_the_class_the_handler_receives` сохранено — оно и объясняет, почему обёртка кладётся на базовый класс.
- **Files modified:** `tests/test_services/test_image_upload.py`
- **Verification:** оба случая зелены; это и есть единственный новый собранный случай сверх переехавших (3427 → 3428)
- **Committed in:** `c2d7bf1`

---

**Total deviations:** 2 auto-fixed (1 × Rule 3 — блокирующее, 1 × Rule 2 — недостающее критичное).
**Impact on plan:** ни одна правка не ослабила правило; вторая правило усилила. Расширения объёма нет: обе вынуждены сменой входа, через который подаются переехавшие утверждения.

## Отклонения ОТ ОЖИДАНИЙ ПЛАНА (не дефекты, но записаны)

- **Критерий «прежнего адреса модуля в суите не осталось ни одного» НЕ МОГ позеленеть на задаче 1, и это свойство плана, а не исполнения.** Две последние его строки держит запись гейта подстановки личности, которую снимает задача 2. Снять её раньше модуля нельзя ПО ПОСТРОЕНИЮ: обход читает оба каталога маршрутов, и живой маршрут, не попавший ни в одно из трёх множеств, роняет гейт — то самое заявленное свойство формы, ради которого запись и существует. На задаче 1 критерий доведён до одного файла (двух строк) и перепроверен на задаче 2, где вывод стал пустым. Записано, а не пропущено: молча засчитанный критерий, который не мог пройти, есть проверка, не способная пойти ни в одну сторону.
- **`MUTATING_MODULE_COUNT` двинулось, хотя план его не называл.** План перечислил три перечня и два числа гейта доступа; второе число гейта подстановки личности выяснилось прогоном. Это ровно тот класс находок, о котором предупреждал G-10 и опыт волн 1–2: план, СНИМАЮЩИЙ маршрут, двигает инвентарные числа так же верно, как план, его добавляющий.
- **Полный прогон нашёл РОВНО ОДИН красный, и он не наш.** В отличие от волн 1–2 (по пять инвентарных находок каждая) полный прогон новых красных правил не дал: гейты были приведены в порядок целевыми прогонами до него. Единственный красный — пред-существующий дефект, замеренный тремя способами (см. «Issues Encountered»). ⚠️ Отсутствие находок НЕ повод пропускать полный прогон: именно он превратил «похоже, других движений нет» в измеренный факт, и стоил он 71 минуту из 90 минут плана.

## TDD Gate Compliance

`workflow.tdd_mode` включён, и задача 1 несёт `tdd="true"`. Гейт `execute-mvp-tdd` проверен ПЕРЕД шагом реализации штатным предикатом, а не на глаз:

```
gsd-tools query task.is-behavior-adding 12-05-PLAN.md
→ { "is_behavior_adding": false,
    "checks": { "tdd_true": true, "has_behavior_block": true, "has_source_files": false },
    "reason": "Not behavior-adding: <files> has no non-test source file" }
```

| Gate | Коммит | Статус |
|------|--------|--------|
| RED | — | **не требуется**: предикат вернул `is_behavior_adding: false` — все три файла задачи 1 суть файлы суиты, поведения задача не добавляет |
| GREEN | — | **не требуется** по тому же основанию |
| REFACTOR | — | не заводился |

⚠️ **ПОЧЕМУ RED-ФАЗЫ ЗДЕСЬ НЕ БЫЛО, И ЭТО НЕ ОБХОД ЦИКЛА.** Задача 1 — ПЕРЕЕЗД утверждений, а не заведение поведения: сервис, о котором они говорят, написан планами 12-01 и 12-04 и уже зелен. Переехавшие правила поэтому зелены С ПЕРВОГО ПРОГОНА, и правило fail-fast «unexpected GREEN in RED phase» здесь именно применено, а не проигнорировано: причина зелени установлена — «поведение уже существует», — и она есть ровно тот случай, который это правило велит ИССЛЕДОВАТЬ, а не тот, который велит остановиться. Написать сначала красное значило бы сломать сервис ради формы цикла. Задача 2 атрибута `tdd` не несёт вовсе.

## Threat Flags

Новой поверхности не появилось — этот план поверхность УБИРАЕТ. Меры по трём `mitigate`-угрозам плана применены и наблюдаемы:

| Угроза | Мера | Чем наблюдаема |
|---|---|---|
| T-12-17 (маршрут выпал из периметра гейта доступа вместе с переездом) | закрытие обеспечивает пер-роутерная зависимость `ads_router` (`app/pages/__init__.py`), а не снятая запись множества; основание записано у КАЖДОГО из двух множеств своими словами | `test_upload_is_closed_by_the_router_access_gate` (204 + заголовок перехода); оба перечня гейта доступа зелены — 35 passed вместе с гейтом подстановки личности |
| T-12-18 (загрузка под ЧУЖОЙ личностью разрешена без принятого решения) | запись перенацелена на страничный обработчик с СОХРАНЁННОЙ дословно причиной; обработчик, не попавший ни в одно из трёх множеств, роняет гейт по построению | `test_every_mutating_route_is_classified` зелен; три контроля гейта (`-k control`) зелены, то есть зубы гейта доказаны, а не заявлены |
| T-12-19 (снятое изъятие Фазы 9 исчезает молча, решение теряется) | обе записи переадресованы ЗАПИСЬЮ с названной причиной и новой назначенной фазой; сводная летопись ДОПОЛНЕНА, а не переписана | `grep -c 'НАЗНАЧЕНО ФАЗЕ 12'` → 0 и `grep -ci 'фазе 15'` → 2; прежний текст строк 6–7 находится грепом на месте |

`T-12-SC` (установки пакетов) — `accept`: план не установил ни одного внешнего пакета и ни одного не запрашивал.

⚠️ **Оба запрета плана остаются `flagged-unverified`, и это объявлено планом заранее, а не забыто.** «Перечни гейтов не подгоняются ради зелени» и «чужое изъятие не снимается молча» — свойства, у которых машинного отрицания в дереве нет: первое держится тем, что основание каждой правки ВЫПИСАНО и ссылается на конвенцию проекта (D-07 Фазы 11), второе — тем, что переадресация есть текст, который читает человек. Ни то, ни другое не есть ПРАВИЛО, краснеющее при нарушении.

## Known Stubs

Нет. Заглушка, оставленная планом 12-01 (`Старый JSON-вход загрузки и его суита в 1099 строк остаются на месте`, запись 91 `.planning/WINDOWS.md`), ЗАКРЫТА этим планом и переведена в `fixed` (`open_count` 78 → 77, `fixed_count` 13 → 14).

**Что этим планом НЕ закрыто и закрыто быть не могло:**

- **Запись 92** (боевой предел тела запроса nginx, план 12-02) — ждёт ПЕРЕРАЗВЁРТЫВАНИЯ боевого стенда. Правка шаблона в гите боевой предел не двигает, nginx в среде разработки отсутствует, машинной проверки нет. Оставлена `open` намеренно.
- **Запись 93** (заведена этим планом) — пред-существующий дефект суиты, требующий решения владельца; см. ниже.

## Issues Encountered

- **Полный прогон суиты дал РОВНО ОДИН красный, и он пред-существующий — шестое срабатывание того же дефекта (окна 14, 27, 79, 85, теперь 93).** `test_the_overview_error_number_matches_the_users_own_dashboard` краснеет после полуночи UTC: плитка «Обзора» берёт СКОЛЬЗЯЩИЕ 24 часа, дашборд — КАЛЕНДАРНЫЕ сутки читателя, и посевы на `now-3h`/`now-5h` после полуночи лежат во вчерашних сутках. **Замерено трижды и тремя РАЗНЫМИ способами:** (1) одиночный прогон правила краснеет за 1,2 с, то есть порядковой зависимостью не является; (2) дифф плана — тринадцать файлов загрузки изображений, гейтов и документации, ни одного файла админки, дашборда или аналитики; (3) собранное число сошлось до единицы в обоих полных прогонах (3428 собрано = 3427 passed + 1 failed), то есть правил не потеряно и не добавлено сверх названных. ⚠️ **К утру по UTC правило позеленеет само, и это будет ЛОЖНЫЙ зелёный** (окно 79). Закрытие требует решения владельца, какое из двух окон уступает, и правки `app/`, которую Фаза 12 своим предметом не объявляла. Заведено записью 93 реестра, а не починено: пред-существующий дефект вне предмета задачи — out of scope по правилу границы объёма.
- **Полный прогон стоит 35 минут, и он был нужен дважды** (35:37 после задачи 1, 35:25 после задачи 2). Быстрые прогоны затронутых модулей инвентарных правил не видят по построению (G-10), и именно полный прогон превратил «других движений нет» в измеренный факт.
- **Нестабильный `test_image_base_url_comes_from_app_settings`** прошёл в обоих полных прогонах. Как и в прошлых планах фазы — наблюдение прогонов, а не вывод «нестабильность исчезла».
- **Ни один инвентарный номер не поставлен вычитанием в уме.** Второе число гейта подстановки личности потребовало ДВУХ прогонов подряд, потому что первое утверждение того же теста до него не допускало; порядок описан в летописи самого файла.

## User Setup Required

None — внешних сервисов план не настраивает и ни одного пакета не устанавливает.

## Next Phase Readiness

- **Критерий 3 фазы закрыт, и закрыт он ТРЕМЯ перечнями, а не двумя.** Пятый и последний план фазы исполнен: старой поверхности загрузки в дереве нет ни одним модулем, ни одним адресом, ни одной строкой README.
- **FETCH-01 объявлен всеми пятью планами фазы**, и с появлением этой сводки последний из них закрыт — требование готово к отметке.
- ⚠️ **Фазе 15 (FORM-01) адресованы два изъятия, которых её собственный ROADMAP не называл.** Записи `DISABLED_ELT_EXCEPTIONS['ads/form.html']` и `MACRO_BORN_EXCEPTIONS['ads/form.html']` ждут перевода формы объявления на макрос-обёртку; оба числа (`DISABLED_ELT_EXCEPTIONS_DECLARED` 2, `MACRO_BORN_EXCEPTIONS_DECLARED` 1) обязаны опуститься там. Планировщику Фазы 15: перевод задевает перечень условий отправки формы, стратегию наложения запросов (`hx-sync="this:queue last"`) и способ подмены «ничего» (`hx-swap="none"`) — именно поэтому владелец отверг его внутри Фазы 12.
- ⚠️ **Два пункта ждут человека, и ни один из них не закрывается этим планом:** боевой предел тела запроса nginx после переразвёртывания (запись 92) и три строки обхода редактора, поставленные планом 12-04 (coverage D9). Приёмка фазы обязана увидеть оба.
- ⚠️ **Пред-существующий красный суиты требует решения владельца** (запись 93). Прогон фазы после ~05:00 UTC покажет зелёное дерево БЕЗ единой правки — не принимать это за починку.

---
*Phase: 12-zagruzka-izobrazheniy-bez-fetch*
*Completed: 2026-09-19*

## Self-Check: PASSED

- `key-files.created` — `tests/test_services/test_image_upload.py` найден на диске; сама эта сводка найдена по объявленному пути.
- `key-files.removed` — `app/routes/uploads.py` и `tests/test_routes/test_uploads.py` отсутствуют оба, проверено `[ -e ]`.
- Коммиты `c2d7bf1` и `8cdeb03` найдены в `git log --oneline --all`; ИЗМЕРЕННЫЙ счёт `git rev-list --count 7127978..HEAD` — 2, и он же записан во frontmatter вместе с `plan_head_before`.
- Все `<acceptance_criteria>` обеих задач перепрогнаны ПОСЛЕ последней правки: 5 из 5 у задачи 1 (включая полный прогон и сверку собранного числа с балансом переезда) и 7 из 7 у задачи 2. Единственный критерий, не проходивший на своей задаче, — «прежнего адреса модуля в суите не осталось»; он доведён до пустого вывода задачей 2, и причина сдвига записана разделом «Отклонения ОТ ОЖИДАНИЙ ПЛАНА».
- `<verification>` плана исполнена целиком: полный прогон `3427 passed` из `3428` собранных (единственный красный — пред-существующее окно 93), `uv run python -m compileall -q app main.py tests` молчит, rc=0; затронутые модули перепроверены ПООДИНОЧКЕ (G-10); `graphify update .` выполнен — 21452 узла, 37305 связей.
- Реестр окон приведён в соответствие: запись 91 переведена в `fixed`, запись 93 заведена; записи 92 не касался.
- `requirements.ready-ids` для `FETCH-01` вернул `1/1 requirement(s) ready to mark complete` — пятый и последний план фазы, объявлявший это требование, закрыт.
