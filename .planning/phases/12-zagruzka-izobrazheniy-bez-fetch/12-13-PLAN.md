---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 13
type: execute
wave: 3
depends_on: ["12-12"]
gap_closure: true
autonomous: true
requirements: [FETCH-01]
files_modified:
  - app/pages/ads.py
  - tests/test_pages/test_ads_image_upload.py
  - .planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Партия, отвергнутая по чужому ключу, больше не исчезает без единого слова о ФАЙЛАХ: каждый выбранный человеком файл получает СВОЮ строку с именем (WR-08, D-20)."
    - "Свойство защиты от подделки не ослаблено: на этой ветви в хранилище по-прежнему не уходит ни одной записи, файловые части не читаются и не сохраняются."
    - "«Это значение не ваше» и «поэтому остальное в запросе не считается» разделены: отказ относится к значениям, но выбор человека назван поимённо."
    - "Ни одно правило, закреплявшее прежнее молчание, не ослаблено: каждое инвертировано ВВЕРХ вместе с предметом, с сохранённой летописью."
    - "Остаток «б» (ключ старого образца в `Ad.images`) назван записью: он не починен этой партией, но перестал быть МОЛЧАЛИВЫМ."
  artifacts:
    - "app/pages/ads.py — ветвь отказа по чужому ключу называет каждый файл партии своей строкой; `attached` и отсутствие обращений к хранилищу не тронуты"
    - "tests/test_pages/test_ads_image_upload.py — расширенное `test_an_own_key_survives_a_foreign_key_in_the_same_batch` и инвертированные вверх соседи, закреплявшие безымянный отказ"
    - ".planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md — запись остатка «б» со статусом `accepted-assumption`"
  key_links:
    - "`partition_own_image_keys` → ветвь отказа: предикат принадлежности остаётся ЕДИНСТВЕННЫМ, ветвление по нему не меняется — меняется только то, что человек читает"
    - "`safe_filename` → имя отвергнутого файла: имя отправителя проходит ту же нормализацию, что и на прочих ветвях отказа (D-05)"
    - "отсутствие обращений к хранилищу на ветви → запрет плана 12-06: работа ради запроса, признанного подделанным, не делается"
  assumptions:
    - statement: "Остаток «б» не починен: ключ старого образца, лежащий в `Ad.images` и не проходящий сегодняшнего образца, по-прежнему отцепляется и по-прежнему уводит партию на ветвь отказа. Партия меняет только ГРОМКОСТЬ исхода — человек узнаёт, что его файлы не прикрепились, — а не достижимость самого состояния."
      source: planner
      requirement_id: FETCH-01
      category: error-handling
      status: unresolved
      decision: D-20
  prohibitions:
    - statement: "Файл, который человек выбрал, не исчезает МОЛЧА: всё, что не прикрепилось, получает названную строку."
      status: unverified
      verification: none
    - statement: "Работа ради запроса, уже признанного подделанным, НЕ делается: громкость отказа не покупается чтением и записью файловых частей."
      status: unverified
      verification: none
    - statement: "Рамка партии, назначенная владельцем, не расширяется молча: IN-07…IN-10 остаются открытыми записями ревью и попутно не чинятся."
      status: unverified
      verification: none

security:
  asvs_level: 1
  block_on: high
---

<objective>
Закрыть находку ревью **WR-08** решением владельца **D-20**, которым рамка партии расширена ЯВНО.

Purpose: когда любое значение скрытого поля вложений не проходит сверку принадлежности, обработчик
дописывает ОДНУ безымянную строку и уходит в ветвь `if` — цикл по файлам пропускается целиком
(`app/pages/ads.py:1016-1045` против пропущенного `else` :1046-1186). Файлы, которые человек только
что выбрал, не читаются, не сохраняются и НЕ УПОМИНАЮТСЯ: единственная строка говорит о КЛЮЧЕ.
`attached` остаётся ложью, поэтому нет ни автосохранения, ни события, ни второго сигнала.

Ветвь достижима БЕЗ нападающего, и это не догадка: ключ старого образца, лежащий в `Ad.images`,
приезжает скрытым полем и сегодняшнего образца не проходит (остаток «б», выписанный комментарием
:1065-1079). С этого момента КАЖДАЯ попытка загрузки — молчаливый no-op до перезагрузки страницы.
Суита сегодня это закрепляет (`:715-762`, `:767-820`: `mock_s3.call_count == 0` при годном PNG в
партии), поэтому ничего и не замечает.

Output: значениям отказано по-прежнему, но каждый ФАЙЛ партии назван своей строкой; ни одной записи
в хранилище на этой ветви не прибавилось; правила, закреплявшие молчание, инвертированы вверх.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-06-SUMMARY.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-12-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Каждый файл отвергнутой по ключу партии назван своей строкой</name>

  <reversibility rating="reversible">Откат снимает один цикл в одной ветви и утверждения новых
  правил; ни контракта ответа, ни формы отказа, ни рубежа владения правка не двигает.</reversibility>

  <read_first>
    - `app/pages/ads.py:982-1045` — ветвь отказа сверки целиком: разбор `images`, признак `attached`, вызов `partition_own_image_keys`, безымянная строка отказа и все обоснования над ней
    - `app/pages/ads.py:1046-1186` — пропускаемый `else`: расчёт свободных мест, цикл по файловым частям, форма пропуска строковой части, `safe_filename` в именах отвергнутых файлов
    - `app/pages/ads.py:1188-1239` — сборщик фрагмента и условие заголовка события (`attached`)
    - `app/services/image_upload.py` — `UPLOAD_FILE_FIELD`, `safe_filename`, тексты отказа как закрытый набор констант
    - `app/services/image_keys.py:35-58` — `INACCESSIBLE_IMAGE_MESSAGE` дословно
    - `app/templates/ads/includes/media_refusals.html` — разметка строки отказа и правило «имя приходит уже нормализованным»; строка без имени печатается БЕЗ разделителя намеренно
    - `tests/test_pages/test_ads_image_upload.py:715-762` — `test_a_foreign_key_is_not_echoed_back`
    - `tests/test_pages/test_ads_image_upload.py:767-820` — `test_an_own_key_survives_a_foreign_key_in_the_same_batch`
    - `tests/test_pages/test_ads_image_upload.py:825-886` — `test_the_keys_that_survive_a_refusal_are_the_ones_the_next_save_persists`
    - `tests/test_pages/test_ads_image_upload.py:676-713` — `test_every_outcome_answers_two_hundred`; `:1379-1400` — `test_a_fully_refused_upload_asks_for_nothing`: обе проверить на принадлежность к этой ветви ЧТЕНИЕМ, а не предположением
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md:210-250` — находка **WR-08** целиком, включая предложенную форму починки
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md` — решение владельца **D-20** дословно, включая перечень остающегося ВНЕ рамки
  </read_first>

  <files>app/pages/ads.py, tests/test_pages/test_ads_image_upload.py</files>

  <behavior>
    - Партия из чужого ключа и годного PNG: ответ 200, во фрагменте нет скрытого поля чужого ключа, строк отказа ДВЕ — безымянная про недоступное вложение и строка с именем `cat.png`; обращений к хранилищу ноль.
    - Партия из своего ключа, чужого ключа и годного PNG: свой ключ во фрагменте остаётся, строк отказа две, вторая названа по имени файла; обращений к хранилищу ноль.
    - Партия из одного чужого ключа БЕЗ файловых частей: строка отказа по-прежнему ОДНА, безымянная, — приписывать имя нечему.
    - Строковая часть, пришедшая под именем файлового поля, пропускается той же формой, что и в действующем цикле: находка `IN-08` остаётся ОТКРЫТОЙ ЗАПИСЬЮ и этой задачей не чинится.
    - Имя отвергнутого файла проходит нормализацию `safe_filename` — то же правило, что на прочих ветвях отказа (D-05).
    - Признак «принят хотя бы один файл» остаётся ложью: автосохранения и заголовка события на этой ветви как не было, так и нет.
  </behavior>

  <action>
    ПРАВИЛА ЗАВОДЯТСЯ ПЕРВЫМИ и прогоняются красными; вывод снимается с
    `--junit-xml=/tmp/gsd-red-12-13-t1.xml`, дословный текст покраснения приводится в сводке.

    В `app/pages/ads.py`, в ветви отказа сверки (`:1016-1045`), после существующей безымянной строки
    отказа завести цикл по файловым частям запроса: перебрать `form_data.getlist(UPLOAD_FILE_FIELD)`,
    пропустить строковые части той же формой, какой это делает действующий цикл ниже, и на каждую
    файловую часть дописать отказ с именем, прошедшим `safe_filename`, и причиной
    `INACCESSIBLE_IMAGE_MESSAGE`. Нового текста для человека не заводить: тексты отказа — закрытый
    набор констант, и вторая формулировка той же причины разошлась бы с первой молча.

    ЧЕГО ЦИКЛ НЕ ДЕЛАЕТ, И ЭТО ГЛАВНОЕ ЕГО СВОЙСТВО: он не читает содержимого частей и не зовёт
    сервис записи. Части к этой строке уже разобраны рантаймом запроса во временные файлы, а работа
    ради запроса, признанного подделанным, по-прежнему не делается — запрет плана 12-06 в силе
    дословно, и число обращений к хранилищу на ветви остаётся нулём.

    Комментарий над строкой отказа правится по идиоме «прежняя редакция называется, а не стирается»
    (D-30/D-32). Здесь стои́т: «Строка отказа ОДНА и без имени файла: отказ относится к ключу, а
    файла за ним нет». Первая половина — про отнесение отказа к КЛЮЧУ — верна и сохраняется; снимается
    вторая, и причина снятия выписывается прямо: файлы за запросом ЕСТЬ, человек их только что
    выбрал, и молчание о них превращало отказ по одному значению в бесследное исчезновение всей
    партии. Назвать достижимость БЕЗ нападающего (остаток «б») и назвать решение владельца **D-20**
    по идентификатору, вместе с тем, что рамка партии расширена ИМ, а не планом.

    В `tests/test_pages/test_ads_image_upload.py`:
    (а) `test_an_own_key_survives_a_foreign_key_in_the_same_batch` РАСШИРЯЕТСЯ, как того требует
    D-20: прежние утверждения (свой ключ остался, чужой не переиздан, ноль обращений к хранилищу)
    сохраняются дословно, и к ним добавляется — каждый файл партии назван: среди строк отказа есть
    строка с именем выбранного файла, и общее число строк равно числу выбранных файлов плюс одна
    безымянная;
    (б) `test_a_foreign_key_is_not_echoed_back` ИНВЕРТИРУЕТСЯ ВВЕРХ, а не ослабляется: утверждение
    «строк отказа одна» было верно для дерева, на котором писалось, и его СМЫСЛ — «подделанный ключ
    не переиздаётся, и работы ради него не делается» — не отменён, а усилен. Докстринг получает
    летопись: почему число строк выросло и что именно оно теперь доказывает. Утверждение про ноль
    обращений к хранилищу остаётся НЕТРОНУТЫМ — оно и есть свойство, которое нельзя обменять на
    громкость;
    (в) `test_every_outcome_answers_two_hundred`, `test_a_fully_refused_upload_asks_for_nothing` и
    `test_the_keys_that_survive_a_refusal_are_the_ones_the_next_save_persists` ПРОВЕРИТЬ прогоном.
    Каждое, чьё число строк сдвинулось, править ТОЛЬКО инверсией вверх с летописью; ни одно не
    ослаблять и ни одно не удалять. Если сдвига нет — не трогать вовсе;
    (г) завести `test_a_refused_key_does_not_swallow_the_batch_silently`: партия из чужого ключа и
    ДВУХ годных файлов; утверждения — строк отказа три, имена обоих файлов присутствуют, скрытых
    полей ключей нет, обращений к хранилищу ноль. Докстринг называет достижимость без нападающего и
    объясняет, почему это потеря РАБОТЫ, а не косметика: человек выбрал файлы в диалоге, диалог
    закрылся, на экране не изменилось ничего.

    ВНЕ РАМКИ И НЕ ТРОГАТЬ ПОПУТНО (решение владельца **D-20** перечисляет поимённо): `IN-07` (дубли
    строк отказа по числу частей), `IN-08` (строковая часть под файловым полем пропускается молча),
    `IN-09` (журнал аварии хранилища пишет константу), `IN-10` (гейт гарды источника — счёт, а не
    имя), а также `WR-02` и `IN-02`…`IN-06`. Они остаются открытыми записями ревью. Запрет плана
    12-09 «рамка партии не расширяется молча» держится тем, что расширение объявлено владельцем и
    процитировано здесь по идентификатору.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, ЛИБО в перечне `FAILED` стои́т хотя бы одно имя. Сигнал назван именами, а не словом "failed" в итоге, потому что итог не отличает «задача сломала правило» от «в модуле красно что-то чужое»: от порядка правила этого модуля не зависят, опора партии на нём зелена (§Validation Sign-Off карты `12-VALIDATION.md`), поэтому каждое имя принадлежит ЭТОЙ задаче, и имя РАЗЛИЧАЕТ причину. Ожидаемые и правимые ИНВЕРСИЕЙ ВВЕРХ: правила, считающие число строк отказа на этой ветви (`test_a_foreign_key_is_not_echoed_back`, `test_every_outcome_answers_two_hundred`, `test_a_fully_refused_upload_asks_for_nothing`, `test_the_keys_that_survive_a_refusal_are_the_ones_the_next_save_persists`). НЕДОПУСТИМЫЕ ни при каком условии: любое правило, утверждающее ноль обращений к хранилищу, — его покраснение означает, что громкость куплена записью, и правится оно СНЯТИЕМ правки, а не утверждением.</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py::test_a_refused_key_does_not_swallow_the_batch_silently tests/test_pages/test_ads_image_upload.py::test_an_own_key_survives_a_foreign_key_in_the_same_batch tests/test_pages/test_ads_image_upload.py::test_a_foreign_key_is_not_echoed_back -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в строке итога не "3 passed"</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_ownership.py tests/test_services/test_image_upload.py tests/test_services/test_image_keys.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, ЛИБО в перечне `FAILED` стои́т хотя бы одно имя. Ни одного правила в этих трёх модулях задача не правит вовсе, поэтому любое имя означает, что правка ветви задела то, во что не целилась, и имя называет ЧТО: `test_ads_image_ownership.py` — рубеж владения; `test_image_upload.py` — сервис записи (то есть цикл всё-таки читает или пишет части); `test_image_keys.py` — предикат принадлежности. Ни одно из трёх не лечится правкой утверждения.</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -v '^\s*#' app/pages/ads.py | grep -c 'getlist(UPLOAD_FILE_FIELD)'` равен 2 — файловые части ПЕРЕБИРАЮТСЯ в двух местах: в ветви отказа и в действующем цикле. Гейт стои́т над числом мест ПЕРЕБОРА, а не над числом вхождений имени, и это замер, а не вкус: вхождений имени на сегодняшнем дереве уже ДВА (ввоз `app/pages/ads.py:64` плюс единственный цикл `:1127`), поэтому второй цикл дал бы три, и гейт «равно 2» над именем краснел бы на ПРАВИЛЬНОЙ работе. Мест перебора сегодня одно (замерено тем же образцом), после правки — два.
    - В ветви отказа сверки нет вызова сервиса записи: `git diff 3b9b8cfe..HEAD -- app/pages/ads.py` показывает добавленный цикл без обращения к хранилищу, и утверждения о нуле записей в трёх правилах зелены. Диапазон пришпилен к `3b9b8cfe` — коммиту ре-верификации, основанию партии: голый `git diff` на уже закоммиченной правке пуст и прошёл бы вхолостую.
    - `grep -c 'def test_a_refused_key_does_not_swallow_the_batch_silently' tests/test_pages/test_ads_image_upload.py` равен 1.
    - Утверждение о нуле обращений к хранилищу в `test_a_foreign_key_is_not_echoed_back` и `test_an_own_key_survives_a_foreign_key_in_the_same_batch` не изменено ни на символ.
    - В `app/pages/ads.py` комментарий над строкой отказа называет **D-20** и остаток «б» как достижимость без нападающего.
    - Ни одно правило суиты не удалено: `git diff 3b9b8cfe..HEAD -- tests/test_pages/test_ads_image_upload.py` не содержит удалённых определений правил.
    - RED измерен: новое правило прогонялось до правки обработчика, покраснело, вывод снят с `--junit-xml=/tmp/gsd-red-12-13-t1.xml`, дословный текст приведён в сводке.
  </acceptance_criteria>

  <done>Человек, чья партия отвергнута по одному значению скрытого поля, читает на экране имя КАЖДОГО выбранного им файла, а в хранилище по-прежнему не уходит ни одной записи.</done>
</task>

<task type="auto">
  <name>Задача 2: Остаток «б» назван записью, а комментарии приведены в соответствие</name>

  <read_first>
    - `app/pages/ads.py:1065-1079` — абзац об остатке «б»: достижимость, отвергнутые альтернативы, сравнение с состоянием ДО фазы
    - `app/pages/ads.py` после задачи 1 — правленный комментарий над строкой отказа
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md` — действующая форма записи и записи планов 12-07 и 12-12
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md:210-250` — **WR-08** и связь с остатком R-1/«б»
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md` — **D-20** дословно
  </read_first>

  <files>app/pages/ads.py, .planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md</files>

  <action>
    Абзац об остатке «б» (`app/pages/ads.py:1065-1079`) описывает исход прежней редакцией: «плитка
    исчезает, строка отказа приходит без имени файла, следующее сохранение записывает объявление без
    такого ключа». После задачи 1 вторая треть этого предложения НЕПРАВДА: строка без имени приходит
    по-прежнему, но рядом с ней стои́т строка на КАЖДЫЙ выбранный файл. Правка делается по идиоме
    «прежняя редакция называется, а не стирается»: прежний текст назван, сказано, что изменилось и
    почему, и сказано, что САМ остаток не починен — ключ старого образца по-прежнему отцепляется, и
    три причины не чинить его (расширение образца сняло бы единственное доказательство формы ключа;
    спросить у базы не у кого, потому что маршрут привязан к ПОЛЬЗОВАТЕЛЮ, а не к черновику, D-02;
    перенос данных вне рамки) остаются в силе дословно.

    Дописать в `deferred-items.md` одну запись со статусом `accepted-assumption` в действующей форме
    файла: «Ключ старого образца в `Ad.images` по-прежнему отцепляется». Содержание: достижимость без
    нападающего; что партия изменила (исход перестал быть МОЛЧАЛИВЫМ — человек читает имя каждого
    своего файла и понимает, что прикрепления не было) и чего не изменила (состояние по-прежнему
    достижимо, выход из него по-прежнему один — обновить страницу и добавить изображение заново, и
    эта фраза в тексте отказа работает дословно); почему не починено здесь — три причины из
    комментария, названные ссылкой, а не переписанные второй раз; воздействие — ограничено
    объявлениями, чьи ключи заведены до нормализации имён.

    Запись называет **D-20** и находку **WR-08** по идентификаторам. Записи планов 12-07 и 12-12 не
    трогаются.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning -q</automated>
    <fails_when>ненулевой код возврата, ЛИБО в перечне `FAILED` стои́т хотя бы одно имя. Каталог читает ЗАПИСИ планирования и от кода не зависит, поэтому имя принадлежит ЭТОЙ задаче и называет предмет: правило формы записей `deferred-items.md` — дописанная запись не держит действующей формы файла; правило соответствия карты и планов — карта проверки не перечисляет задач партии, и чинится это правкой `12-VALIDATION.md`, а не смягчением записи.</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_pages/test_ads_editor.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, ЛИБО в перечне `FAILED` стои́т хотя бы одно имя, КРОМЕ `test_image_base_url_comes_from_app_settings` (известная нестабильность порядка; одиночное покраснение — загрязнение, проверяется повторным одиночным прогоном ЕЁ ОДНОЙ). Задача правит ТОЛЬКО комментарии и запись планирования, поэтому любое другое имя означает, что правка комментария задела ПОВЕДЕНИЕ — то есть тронута строка кода, а не проза, — и лечится снятием этой строки, а не утверждением.</fails_when>
  </verify>

  <acceptance_criteria>
    - В `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md` появилась запись со `status: accepted-assumption`, называющая `D-20` и `WR-08`.
    - Запись называет ОБЕ половины: что партия изменила и чего не изменила.
    - В `app/pages/ads.py` абзац об остатке «б» сохраняет прежнюю редакцию названной и говорит, что сам остаток не починен.
    - Записи планов 12-07 и 12-12 в `deferred-items.md` не изменены: `git diff 3b9b8cfe..HEAD -- .planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md` показывает только дописанные строки.
    - Поведение не тронуто: дифф РАБОЧЕГО ДЕРЕВА по `app/pages/ads.py` ДО коммита этой задачи (`git diff -- app/pages/ads.py`, проверка намеренно о незакоммиченном дереве) содержит только строки комментариев.
  </acceptance_criteria>

  <done>Остаток, ради громкости которого партия и расширена, существует записью с названным воздействием, а комментарий ветви не обещает того, чего больше нет.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| значение скрытого поля вложений → предикат принадлежности | клиент вправе подделать документ; ветвь отказа и есть рубеж |
| имя файла, присланное отправителем → строка отказа на экране | сырой ввод попадает в разметку |
| файловые части запроса на ветви отказа | части уже разобраны рантаймом, но НЕ должны быть прочитаны и записаны |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-13-01 | Spoofing | подделанный ключ в скрытом поле | high | mitigate | ветвление по `partition_own_image_keys` не меняется: виновное значение во фрагмент не попадает, подтверждённое подмножество остаётся |
| T-12-13-02 | Denial of Service | объекты-сироты от работы ради подделанного запроса | high | mitigate | цикл называет файлы ПО ИМЕНИ, но не читает их содержимого и не зовёт запись: число обращений к хранилищу на ветви остаётся нулём, и это утверждается тремя правилами |
| T-12-13-03 | Injection | имя файла отправителя в строке отказа | medium | mitigate | имя проходит `safe_filename` до шаблона — та же нормализация, что на прочих ветвях (D-05); одного автоэкранирования шаблонизатора мало, и это правило переносится дословно |
| T-12-13-04 | Repudiation | молчаливое исчезновение выбранной партии | high | mitigate | каждый файл получает свою строку; «это значение не ваше» отделено от «остальное не считается» |
| T-12-13-05 | Denial of Service | достижимость ветви ключом старого образца (остаток «б») | medium | accept | не чинится этой партией по трём названным причинам; записывается допущением задачей 2, исход перестаёт быть молчаливым |
</threat_model>

## Артефакты, которые этот план производит

- `app/pages/ads.py`: цикл по файловым частям в ветви отказа сверки; правленые комментарии ветви и остатка «б»
- `tests/test_pages/test_ads_image_upload.py`: правило `test_a_refused_key_does_not_swallow_the_batch_silently`; расширенное `test_an_own_key_survives_a_foreign_key_in_the_same_batch`; инвертированное вверх `test_a_foreign_key_is_not_echoed_back`
- `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md`: запись `accepted-assumption` об остатке «б»

Новых маршрутов, новых текстов для человека, новых заголовков ответа и новых констант план не заводит.

<verification>
- Партия, отвергнутая по ключу, называет каждый свой файл; безымянная строка про ключ остаётся на месте.
- Ноль обращений к хранилищу на ветви держится тремя правилами, ни одно из которых не ослаблено.
- Правила, закреплявшие молчание, инвертированы вверх с летописью, а не переписаны.
- Остаток «б» существует записью.
</verification>

<success_criteria>
- **WR-08** закрыт кодом; **D-20** процитирован по идентификатору в исходнике, в правиле и в записи.
- `IN-07`…`IN-10`, `WR-02`, `IN-02`…`IN-06` не тронуты и остаются открытыми записями ревью.
- Запрет плана 12-06 не ослаблен: громкость отказа не куплена ни одной записью в хранилище.
</success_criteria>

<output>
Создать `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-13-SUMMARY.md` по завершении.
</output>
