---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 11
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [FETCH-01]
files_modified:
  - app/services/image_keys.py
  - app/pages/common.py
  - app/templates/ads/includes/media_strip.html
  - app/templates/ads/includes/media_add_tile.html
  - app/templates/ads/includes/autosave_response.html
  - app/pages/ads.py
  - tests/test_pages/test_ads_editor.py
  - tests/test_pages/test_ads_image_upload.py
  - tests/test_templates/test_htmx_markup_gates.py

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ПОРЯДОК II зонда WR-07 держится: ключ, принятый загрузкой и УЖЕ записанный в хранилище, не выносится из документа ответом убирания — ответ убирания не приносит узла полосы вовсе и адресует РОВНО тот ключ, который этот запрос видел (гап 3, D-19)."
    - "Убранное кнопкой «×» вложение по-прежнему исчезает с экрана: покупка плана 12-03 не потеряна, сменился только способ — адресное снятие одной плитки вместо подмены узла целиком."
    - "Идентификатор плитки в документе и идентификатор, который называет блок снятия, производит ОДНА функция: разойтись двум написаниям нечем (Hyrum)."
    - "Текстовое автосохранение по-прежнему не приносит ни узла полосы, ни единого блока снятия — свойство гапа 2 (план 12-07) не ослаблено."
    - "Плитка «+ ФАЙЛ» не врёт после убирания на потолке: она приезжает заново тем же ответом и печатается из ЕДИНСТВЕННОГО источника разметки."
    - "Строки отказа переживают убирание: ответ убирания больше не подменяет узел, внутри которого они живут."
    - "Убранный ключ не возвращается в документ как ПРИКРЕПЛЯЕМОЕ состояние: поля `name=\"images\"` ответ убирания не несёт ни одного, и убранного ключа нет среди значений таких полей. Присутствие того же ключа в ответе как ВЫЧИТАЮЩЕЙ метки (план 12-12) этому не противоречит и замеряется правилом того плана, а не оставляется рассуждением."
  artifacts:
    - "app/services/image_keys.py — `media_dom_id(key)`: единственное место, где ключ превращается в идентификатор узла документа"
    - "app/pages/common.py — регистрация `media_dom_id` глобалом окружения шаблонов"
    - "app/templates/ads/includes/media_strip.html — плитка несёт идентификатор, скрытое поле ключа переехало ВНУТРЬ плитки, условный признак подмены снят вместе со своим единственным истинным вызывающим"
    - "app/templates/ads/includes/media_add_tile.html — единственный источник разметки плитки добавления (второе печатающее место)"
    - "app/templates/ads/includes/autosave_response.html — блоки адресного снятия вместо включения полосы целиком"
    - "app/pages/ads.py — `_autosave_response` принимает перечень ключей, обязанных покинуть документ, вместо разрешения перерисовать полосу"
    - "tests/test_pages/test_ads_editor.py — `test_a_removal_deletes_only_the_removed_tile` (перенацеленное вверх правило) и правило плитки добавления после убирания на потолке"
    - "tests/test_pages/test_ads_image_upload.py — `test_order_ii_a_stored_key_is_not_carried_out_by_the_removal`: два обработчика друг против друга"
  key_links:
    - "`media_dom_id` → плитка полосы и блок снятия ответа убирания: одна функция, два печатающих места, равенство держится вызовом, а не совпадением"
    - "ответ убирания → документ: адресуется ОДИН ключ, который запрос видел; узел, где живут чужие ключи и строки отказа, не трогается"
    - "правило равенства перечня снятых идентификаторов → способ подмены `delete`: перечень читается ТОЛЬКО с блоков снятия, потому что тем же ответом законно приезжают и другие внеполосные блоки — плитка добавления (задача 2) и метка убирания (план 12-12). Привязка к `delete` есть условие, при котором работа соседней задачи не краснит чужой гейт"
    - "вендоренный рантайм разметки (`app/static/js/htmx.min.js`, 2.0.10) → способ снятия `delete`: ветка присутствует в исходнике рантайма, а цель собирается решёткой плюс `CSS.escape` от значения идентификатора (замерено по исходнику) — то есть рантайм набора символов НЕ ограничивает, и хеш в `media_dom_id` стои́т ради устойчивости и единственности правила производства на ДВУХ форматах ключа, а не ради допустимости символов в селекторе"
  assumptions:
    - statement: "Кромка `concurrency` требования FETCH-01 остаётся `unresolved` в двух своих гранях: оборванная партия оставляет объекты-сироты и две вкладки считают свободные места каждая из своего снимка (записаны допущением D-17), а вытесняющая очередь теряет средний выбор файлов (записана допущением D-16). Этот план закрывает ТРЕТЬЮ грань — наложение убирания и загрузки — и только её."
      source: edge-probe
      requirement_id: FETCH-01
      category: concurrency
      status: unresolved
      decision: D-17
    - statement: "Воскресшая плитка ПОРЯДКА I этим планом ещё не снимается: ответ загрузки, пришедший последним, переиздаёт снимок целиком. Грань закрывается планом 12-12 (метка убирания и вычитание на сохранении), и до его исполнения ПОРЯДОК I остаётся открытым."
      source: planner
      requirement_id: FETCH-01
      category: concurrency
      status: unresolved
      decision: D-19
  prohibitions:
    - statement: "Ни один ключ, который человек ВИДИТ прикреплённым, не исчезает без названной строки — ни из документа, ни из базы, ни при наложении двух запросов."
      status: unverified
      verification: none
    - statement: "Ответ не заявляет власти над состоянием, которого его запрос не видел: узел, содержащий чужие ключи, не подменяется целиком ради снятия одного."
      status: unverified
      verification: none
    - statement: "Рамка партии, назначенная владельцем, не расширяется молча: находки вне рамки не чинятся попутно, а исчезновение их предмета называется вслух."
      status: unverified
      verification: none

security:
  asvs_level: 1
  block_on: high
---

<objective>
Закрыть ПОРЯДОК II зонда WR-07 решением владельца **D-19**: перерисовка полосы на пути убирания «×»
перестаёт заявлять власть над ключами, которых ЭТОТ запрос не видел.

Purpose: сегодня ответ убирания подменяет узел `#media-tray` ЦЕЛИКОМ, собрав его из `ad.images` после
коммита (`app/pages/ads.py:618-621,680-682`; `autosave_response.html:60-65`). Ключ, который летящая
загрузка уже записала в хранилище, в этой базе отсутствует — и подмена выносит его скрытое поле из
документа. Замер верификатора: `C present in removal tray: False` при `s3 calls: 2`. Человек видел
плитку, она исчезла БЕЗ сообщения, объект остался сиротой.

Объединение базы с присланным снимком эту грань НЕ закрывает, и это проверено по исходнику, а не
принято на слово: снимок запроса убирания — `[A, B]`, база после коммита — `[B]`, ключа `C` нет ни в
одном из двух множеств. Адресовать запрос вправе РОВНО ОДИН ключ — тот, который он действительно
видел и убрал.

Output: ответ убирания несёт не узел полосы, а по одному блоку адресного снятия на убранный ключ;
плитка получает устойчивый идентификатор, производимый одной функцией; плитка «+ ФАЙЛ», потерявшая
вместе с перерисовкой своего обновителя, приезжает отдельным блоком из единственного источника
разметки.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-03-SUMMARY.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: Сквозной срез — убранная плитка уходит ПО ИМЕНИ, а не подменой узла</name>

  <reversibility rating="reversible">Откат снимает одну функцию, один глобал окружения, два атрибута
  разметки и один параметр помощника; ни миграции, ни внешнего состояния, ни адреса маршрута правка
  не заводит.</reversibility>

  <read_first>
    - `app/pages/ads.py:444-488` — `_autosave_response`, докстринг признака перерисовки и контекст шаблона
    - `app/pages/ads.py:608-695` — `_save_from_editor`: снятие признака изменения состава (:613-621) и сборщик `_fragment` (:660-694)
    - `app/pages/ads.py:491-538` — `_attachment_refusal`, второй вызывающий помощника (умолчание ему подходит по существу)
    - `app/templates/ads/includes/media_strip.html` целиком — шапка, узел, цикл плиток (:73-91), метка плитки добавления (:92)
    - `app/templates/ads/includes/autosave_response.html` целиком — перечень внеполосных идентификаторов в шапке и включение полосы (:56-65)
    - `app/templates/ads/form.html:190-222` — постоянная обёртка полосы и соседство формы загрузки; `:466-482` — счётчик, читающий число плиток из разметки
    - `app/services/image_keys.py:59-118` — `thumb_key`, `partition_own_image_keys`; соседство для новой функции
    - `app/pages/common.py:280-300` — где регистрируются глобалы окружения шаблонов (`THUMB_KEY_PREFIX`, `upload_field`)
    - `tests/test_pages/test_ads_editor.py:614-700` — правило, которое ПЕРЕНАЦЕЛИВАЕТСЯ вместе с предметом, и его сосед
    - `tests/test_pages/test_ads_image_upload.py:40-100` — `HIDDEN_KEY_FIELD`, `refusal_rows`, `image_key`, `UPLOAD_FIELD`, фикстуры
    - `tests/test_pages/test_ads_image_upload.py:886-975` — идиома правила, сводящего ДВА обработчика
    - `tests/test_templates/test_htmx_markup_gates.py:2280-2320` — объявленное число внеполосных блоков
    - `tests/test_templates/test_htmx_markup_gates.py:2470-2530` — разбор цели внеполосного блока
    - `tests/test_templates/test_htmx_markup_gates.py:5130-5200` — правило «внеполосный узел — верхнеуровневый узел своего ответа»
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md` — блок `gaps` целиком (ПОРЯДОК I и ПОРЯДОК II зонда WR-07)
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md` — решение владельца **D-19** дословно
  </read_first>

  <files>app/services/image_keys.py, app/pages/common.py, app/templates/ads/includes/media_strip.html, app/templates/ads/includes/autosave_response.html, app/pages/ads.py, tests/test_pages/test_ads_editor.py, tests/test_pages/test_ads_image_upload.py, tests/test_templates/test_htmx_markup_gates.py</files>

  <behavior>
    - Ответ на запрос с `remove_image` несёт РОВНО ОДИН верхнеуровневый блок снятия, идентификатор которого равен значению `media_dom_id` убранного ключа, и не несёт подстроки `id="media-tray"` вовсе.
    - Тот же ответ не переиздаёт ни одного скрытого поля `name="images"`: оставшиеся ключи он не видел своей властью и в документе их не трогает.
    - Убранного ключа нет среди ЗНАЧЕНИЙ полей `name="images"` ответа. Утверждение стои́т над значениями этих полей, а НЕ над присутствием ключа как произвольной подстроки ответа: план 12-12 этой же партии заводит вычитающую метку, несущую ровно этот ключ ровно ради того, чтобы он не вернулся, и бессрочный запрет на ключ-подстроку краснел бы на правильной работе.
    - Запрос без `remove_image` (текстовое автосохранение) не несёт ни блока снятия, ни узла полосы — соседнее правило `test_a_text_only_autosave_leaves_the_tray_alone` держится дословно.
    - `remove_image` со значением, которого в списке ключей нет, блока снятия не порождает: состав не изменён, снимать нечего.
    - ПОРЯДОК II, два обработчика друг против друга: `POST /ads/images` со снимком `[A, B]` и годным файлом даёт фрагмент со скрытыми полями `[A, B, C]`; следующий `POST /ads/{id}/edit` со СТАРЫМ снимком `[A, B]` и `remove_image=A` не несёт ни узла полосы, ни ключа `C`, ни блока СНЯТИЯ с чужим идентификатором — среди блоков со способом подмены `delete` единственный названный идентификатор принадлежит ключу `A`.
    - Перечень идентификаторов, названных к СНЯТИЮ, читается образцом, привязанным к способу подмены `delete`, и прочие внеполосные блоки того же ответа в него не входят ПО ПОСТРОЕНИЮ ОБРАЗЦА, а не по счастливому совпадению их отсутствия: в этом же ответе законно живут плитка добавления (задача 2) и метка убирания (план 12-12, задача 1).
    - Страница редактора после правки рендерит плитку с идентификатором, а скрытое поле ключа лежит ВНУТРИ плитки; включение формы загрузки (`#media-strip input[name='images']`) по-прежнему собирает все ключи, счётчик по-прежнему считает плитки.
  </behavior>

  <action>
    ПОРЯДОК РАБОТЫ ОБРАТЕН ПОРЯДКУ ЧТЕНИЯ: правила заводятся ПЕРВЫМИ и прогоняются красными, иначе не
    измерено, что они меряют предмет, а не собственную формулировку. Красный прогон снимать с
    `--junit-xml=/tmp/gsd-red-12-11-t1.xml` и дословный текст покраснения привести в сводке (идиома
    «ПОСТАВЛЕНО ПРОГОНОМ», D-13; разбор пробника улик требует именно этой формы вывода).

    В `app/services/image_keys.py` завести `media_dom_id(key: str) -> str`, возвращающую
    `"media-item-"` плюс первые 16 символов шестнадцатеричного представления `hashlib.sha256` от
    ключа в кодировке UTF-8. Соседство выбрано не по вкусу: здесь уже живут `thumb_key` и
    `partition_own_image_keys`, то есть всё, что знает форму ключа. Докстринг обязан назвать ПРИЧИНУ
    хеша, а не оставить его вкусом, и причина эта — УСТОЙЧИВОСТЬ И ЕДИНСТВЕННОСТЬ ПРАВИЛА
    ПРОИЗВОДСТВА НА ДВУХ ФОРМАТАХ КЛЮЧА. Ключ сегодняшнего образца имеет форму
    `{user_id}/{uuid4hex}_{имя}`, а ключ старого образца (остаток «б», `app/pages/ads.py:1065-1079`)
    формы не держит вовсе: нарезка по разделителям дала бы для него пустой или неуникальный
    идентификатор, а для сегодняшнего — идентификатор, меняющийся вместе с именем файла, то есть
    идентификатор узла зависел бы от того, как человек назвал файл. Хеш даёт ОДНО правило
    производства на оба формата, значение фиксированной длины и устойчивость при любом имени.

    ОТДЕЛЬНЫМ ПРЕДЛОЖЕНИЕМ ДОКСТРИНГ ОБЯЗАН НАЗВАТЬ СОБЛАЗНИТЕЛЬНОЕ ОСНОВАНИЕ ЗАМЕРЕННЫМ ЛОЖНЫМ,
    иначе следующий читатель выведет его заново и станет защищать несуществующее ограничение. Ложное
    основание звучит так: «косая черта и произвольное имя файла недопустимы в селекторе внеполосной
    цели». Это НЕПРАВДА, и неправда замерена по вендоренному рантайму
    (`app/static/js/htmx.min.js`, версия 2.0.10): функция разбора внеполосного признака собирает цель
    как решётку плюс результат `CSS.escape` от значения идентификатора, поэтому и косая черта, и
    произвольное имя были бы в селекторе безопасны. Ветка разбора `стиль:селектор` в рантайме
    существует и правую половину берёт сырой, но способ `delete` двоеточия не несёт и до этой ветки
    не доходит. Хеш здесь НЕ средство защиты и НЕ обход ограничения рантайма — он средство
    устойчивости, и это записать прямо, чтобы следующий читатель не принял его за рубеж.

    В `app/pages/common.py` зарегистрировать `media_dom_id` глобалом окружения шаблонов рядом с
    `THUMB_KEY_PREFIX` и `upload_field` (:285-297) — тем же способом и без нового механизма.

    В `app/templates/ads/includes/media_strip.html`:
    (а) плитка `span.media-tile` цикла (:74) получает `id="{{ media_dom_id(key) }}"`;
    (б) скрытое поле ключа (:90) переезжает ВНУТРЬ этого же `span`, потому что адресное снятие
    уносит УЗЕЛ, а плитка без своего скрытого поля оставила бы в документе состояние без картинки —
    следующая отправка формы прикрепила бы ключ, которого человек уже не видит. Атрибут
    `form="ad-form"` у поля сохраняется дословно: полоса по-прежнему стои́т ВНЕ формы объявления;
    (в) условный признак внеполосной подмены на узле полосы (:65, условие по переменной `oob`)
    снимается ВМЕСТЕ со своим единственным истинным вызывающим — идиома D-09 Фазы 8 «мёртвое
    снимается сразу и вместе с тем, что его держало». Прежняя редакция НАЗЫВАЕТСЯ абзацем шапки, а не
    стирается (идиома D-30/D-32): узел приезжал внеполосно ответу автосохранения, ответ этот собирал
    его из базы, отстающей от документа на незаписанный ключ, и ПОРЯДОК II зонда WR-07 показал цену —
    ключ, уже лежащий в хранилище, уносился из документа без единой строки;
    (г) шапка дополняется абзацем о том, что печатающих мест у ПЛИТКИ теперь два — эта полоса и блок
    снятия ответа убирания, — и связывает их одна функция, а не два написания.

    В `app/templates/ads/includes/autosave_response.html` включение полосы под условием (:60-65)
    заменяется циклом по новому значению контекста `media_gone`: на каждый ключ — один
    верхнеуровневый пустой `span` с `id="{{ media_dom_id(key) }}"` и атрибутом внеполосной подмены со
    значением `delete`. Три присваивания, обслуживавшие снятое включение (`image_keys`, `refusals`,
    `max_images`), снимаются вместе с ним: других читателей у них в этом файле нет. Признак `oob`,
    поставленный строкой 55 для включения `ads/includes/autosave.html`, НЕ трогается. Шапка файла
    правится по идиоме «прежняя редакция называется»: перечень внеполосных идентификаторов теряет
    узел полосы и приобретает параметрический идентификатор плитки; причина названа замером зонда
    WR-07, а не общими словами. Отдельным предложением записать, что находка `IN-02` (наследование
    признака `oob` соседним включением) этой партией НЕ чинится и остаётся ОТКРЫТОЙ ЗАПИСЬЮ ревью —
    просто предмет её на пути полосы перестал существовать вместе со снятым включением; рамка партии
    расширена владельцем ЯВНО и только на WR-08 (**D-20**), и молча она не расширяется (запрет плана
    12-09).

    В `app/pages/ads.py` `_autosave_response` принимает `media_gone: list[str] | None = None` вместо
    булева разрешения перерисовать полосу и кладёт список в контекст шаблона ключом `media_gone`
    (пустой список при `None`). Докстринг переписывается по идиоме D-30/D-32: прежняя редакция про
    «разрешение перерисовать полосу» НАЗЫВАЕТСЯ и объясняется, почему разрешения мало — перерисовка
    узла целиком заявляет власть надо ВСЕМ его содержимым, включая ключ, о котором этот запрос не
    знал, и гап 3 верификации закрыт только тогда, когда ответ адресует ровно то, что видел.
    Привести в докстринге оба замера зонда WR-07 дословно (`deleted key A resurrected in db: True`;
    `C present in removal tray: False` при `s3 calls: 2`) и назвать решение владельца **D-19** по
    идентификатору. `_save_from_editor` сохраняет снятие признака изменения состава как есть и
    передаёт в сборщике `media_gone` из одного элемента — убранного ключа — когда состав
    действительно изменён, и пустой список иначе. Двух прочих вызывающих правка не касается.

    ПРЯМОЙ ЗАПРЕТ НА СОСЕДНЮЮ ПРАВКУ: ответ маршрута загрузки (`_media_strip_fragment`, :1188-1235) в
    этой задаче НЕ трогается ни строкой. ПОРЯДОК I закрывается планом 12-12 другим средством, и
    смешивать два механизма в одной задаче значило бы лишить каждый собственного замера.

    В `tests/test_pages/test_ads_editor.py` правило `test_autosave_response_carries_the_media_tray`
    ИНВЕРТИРУЕТСЯ ВВЕРХ вместе с предметом и переименовывается в
    `test_a_removal_deletes_only_the_removed_tile` (идиома плана 12-08: замеряющее правило,
    закрепляющее снятое поведение, инвертируется, а не переписывается). Докстринг сохраняет прежнюю
    летопись перенацеливания плана 12-07 и дописывает новую: прежде правило требовало УЗЕЛ полосы с
    признаком подмены, и требование было верно для дерева, на котором писалось; предмет — «убранное
    исчезает с экрана» — не отменён, а УСИЛЕН, потому что узел целиком уносил и чужие ключи.
    Отдельной строкой летопись обязана назвать и судьбу прежнего утверждения о скрытом поле
    ОСТАВШЕГОСЯ ключа (`tests/test_pages/test_ads_editor.py:679-682`): прежде правило требовало, чтобы
    поле оставшегося ключа приехало ВНУТРИ внеполосной полосы, — теперь ответ не несёт полей
    `name="images"` вовсе, и оставшийся ключ живёт в документе крепче, чем прежде, потому что ответ
    его УЗЛА НЕ ТРОГАЕТ: он не переиздаётся, а значит и не может быть переиздан из отстающей базы.
    Утверждения: ответ несёт признак подмены со значением `delete`; среди блоков СНЯТИЯ (то есть
    блоков, у которых значение признака внеполосной подмены равно `delete`) названный идентификатор
    равен `media_dom_id(dropped)`, и значение берётся ВЫЗОВОМ функции из `app/services/image_keys.py`,
    а не повторной сборкой в тесте — иначе правило меряло бы собственную формулировку; подстроки
    `id="media-tray"` в ответе нет; полей `name="images"` ответ не несёт ни одного; убранного ключа нет
    среди ЗНАЧЕНИЙ полей `name="images"` этого ответа.

    ПОСЛЕДНЕЕ УТВЕРЖДЕНИЕ ЗАМЕНЯЕТ ДЕЙСТВУЮЩЕЕ, И ЗАМЕНА ОБЯЗАНА БЫТЬ НАЗВАНА ЛЕТОПИСЬЮ, А НЕ СДЕЛАНА
    МОЛЧА. Сегодня правило утверждает отсутствие убранного ключа как СТРОКИ где угодно в ответе
    (`tests/test_pages/test_ads_editor.py:683-686`). Предмет, который эта строка охраняет, — «убранный
    ключ не возвращается в документ как ПРИКРЕПЛЯЕМОЕ состояние», то есть как значение поля, которое
    `_save_from_editor` прочитает в `ad.images`. Ровно это и утверждается теперь, и утверждается
    СИЛЬНЕЕ: полей `name="images"` ответ не несёт ни одного, а не только не несёт одного названного
    значения. Снимается у утверждения ровно одна грань — власть над ключом как над произвольной
    подстрокой ответа; причина снятия называется прямо: план 12-12 ЭТОЙ ЖЕ ПАРТИИ заводит ВЫЧИТАЮЩУЮ
    метку `removed_images`, которая несёт ровно этот ключ ровно ради того, чтобы он НЕ вернулся, и
    бессрочный запрет на ключ-подстроку краснел бы на правильной работе, меряя ИМЯ ПОЛЯ вместо судьбы
    ключа. Докстринг обязан назвать оба правила-пары, заводимые планом 12-12 задачей 1:
    `test_a_marked_key_does_not_come_back_through_a_stale_snapshot` (ключ не воскресает в базе) и
    `test_the_removal_mark_is_the_only_place_the_removed_key_comes_back` (единственное место возврата
    ключа в ответе — вычитающая метка, и это ЗАМЕРЕНО, а не подразумевается). Сказать прямо, что на
    дереве после ЭТОГО плана второго правила ещё нет, и назвать его как обязательство партии.

    ДВА УТВЕРЖДЕНИЯ О ПОЛЯХ `name="images"` — НУЛЕВОЙ СЧЁТ И ОТСУТСТВИЕ КЛЮЧА СРЕДИ ЗНАЧЕНИЙ —
    ЧИТАЮТСЯ ДВУМЯ РАЗНЫМИ ИНСТРУМЕНТАМИ, И ПУТАТЬ ИХ НЕЛЬЗЯ: ИМЕННО НА ЭТОМ СТОИ́Т ВЕСЬ ДОВОД О ТОМ,
    ЧТО ПРАВИЛО СТАЛО СИЛЬНЕЕ, А НЕ СЛАБЕЕ.
    (1) НУЛЕВОЙ СЧЁТ утверждается над ИМЕНЕМ ПОЛЯ — над вхождением `name="images"` в ответе — и ни над
    чем более узким. Ни порядок атрибутов, ни наличие `type="hidden"`, `value=…` или `form="ad-form"`
    в это утверждение не входят: заявлено, что поля вложений в ответе НЕТ НИ ОДНОГО ни в каком
    написании.
    (2) Утверждение о ЗНАЧЕНИЯХ (убранного ключа среди них нет) читается образцом того же вида, каким
    читает значения `HIDDEN_KEY_FIELD` в `tests/test_pages/test_ads_image_upload.py:78-80` — полным
    набором атрибутов, потому что образцу нужно ДОСТАТЬ значение, а не пересчитать поля (свой образец
    в этом модуле, а не ввоз чужого: перекрёстных ввозов помощников между модулями тестов здесь нет).

    ПОЧЕМУ РАЗДЕЛЕНИЕ НЕСУЩЕЕ, А НЕ ПЕДАНТИЧНОЕ: `HIDDEN_KEY_FIELD` пришпилен к полному набору
    атрибутов В ОДНОМ порядке. Нулевой счёт, снятый ЭТИМ образцом, был бы зелен от любого поля
    `name="images"`, написанного в другом порядке атрибутов или без одного из них, — то есть заявлял
    бы «полей нет», не умея их увидеть. Такое утверждение оказалось бы СЛАБЕЕ того, которое оно
    заменяет (`tests/test_pages/test_ads_editor.py:683-686` утверждает отсутствие НАЗВАННОГО значения
    безусловно), и объявленное усиление обернулось бы РЕГРЕССОМ — ровно тем, чего вся эта летопись и
    не допускает. Счёт по ИМЕНИ поля этой дыры не имеет: ни одно написание поля вложений мимо него не
    пройдёт, и потому «полей нет ни одного» действительно сильнее, чем «нет одного названного
    значения». Эта же развязка инструментов держится и там, где та же пара утверждений заявляется ещё
    раз в партии, — в правиле `test_the_removal_mark_is_the_only_place_the_removed_key_comes_back`
    плана 12-12 (задача 1), чей первый пункт есть тот же нулевой счёт над ИМЕНЕМ поля.

    В `tests/test_pages/test_ads_image_upload.py` завести правило ПОРЯДКА II —
    `test_order_ii_a_stored_key_is_not_carried_out_by_the_removal`, повторяющее зонд WR-07
    верификатора на уровне HTTP по идиоме соседа `test_a_refusal_row_survives_the_autosave_the_same_response_triggers`:
    (1) посеять объявление с двумя своими ключами `A` и `B`; (2) `POST /ads/images` со снимком
    `[A, B]` и годным PNG — из ответа взять `HIDDEN_KEY_FIELD.findall` и убедиться, что появился
    третий ключ `C`; (3) `POST /ads/{id}/edit` со СТАРЫМ снимком `[A, B]` и `remove_image=A` — то
    есть ровно тот запрос, который человек отправляет кнопкой «×», пока загрузка ещё летит.
    Утверждения: в ответе нет `id="media-tray"`; значения `C` в ответе нет; перечень названных к
    СНЯТИЮ идентификаторов равен ровно `[media_dom_id(A)]`; в базе после запроса `[B]`. Докстринг
    называет замер верификатора и объясняет, почему порознь оба обработчика зелены: правил на
    ВЗАИМОДЕЙСТВИЕ убирания и загрузки в суите не было ни одного, и ровно поэтому исход не был виден.

    РАВЕНСТВО ПЕРЕЧНЯ ОБЯЗАНО БЫТЬ ПРИШПИЛЕНО К СПОСОБУ ПОДМЕНЫ `delete`, И ЭТО НЕСУЩЕЕ ТРЕБОВАНИЕ, А
    НЕ АККУРАТНОСТЬ. Образец, снимающий идентификаторы у ЛЮБОГО внеполосного блока ответа, краснел бы
    на ЗАКОННОЙ работе соседних задач партии: задача 2 ЭТОГО плана приносит тем же ответом плитку
    добавления с идентификатором `media-add-tile`, а план 12-12 задачей 1 — метку убирания с
    идентификатором `media-tombstones`. Оба блока приезжают ровно тогда же, когда блоки снятия (состав
    изменён этим запросом), то есть попали бы в перечень и сделали бы равенство ложным, не тронув
    предмета правила ни на символ. Поэтому образец берёт ТОЛЬКО блоки, у которых значение признака
    внеполосной подмены равно `delete`, — а значит перечень измеряет «кого этот ответ УНОСИТ», а не
    «сколько блоков он вообще несёт». Ровно это и есть предмет: власть ответа над чужим состоянием.
    Записать основание в докстринге правила, чтобы следующий читатель не «починил» образец, расширив
    его до всех блоков.

    Если правило «внеполосный узел — верхнеуровневый узел своего ответа» или разбор цели
    внеполосного блока (`tests/test_templates/test_htmx_markup_gates.py`) требуют объявить
    ПАРАМЕТРИЧЕСКИЙ идентификатор в действующем перечне — объявить его там вместе с числом записей,
    ОДНИМ коммитом с работой (идиома D-13), и назвать причину записью, а не подгонкой. Объявленное
    число внеполосных блоков двигается ЗАМЕРОМ: сперва прогон, затем правка объявленного числа под
    полученное, и в сводке приводится, сколько блоков ушло и сколько пришло.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_editor.py tests/test_pages/test_ads_image_upload.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, ЛИБО в перечне `FAILED` стои́т хотя бы одно имя, КРОМЕ `test_image_base_url_comes_from_app_settings`. Сигнал назван так, а не словом "failed" в итоге, чтобы отличать СВОЁ покраснение от чужого: названное имя — известная нестабильность порядка (`.planning/todos/pending/full-suite-ads-editor-order-pollution.md`), её одиночное покраснение означает загрязнение и проверяется повторным одиночным прогоном ЕЁ ОДНОЙ; опора партии на этих двух модулях зелена (снимается перед первой волной, §Validation Sign-Off карты `12-VALIDATION.md`), поэтому любое ДРУГОЕ имя в перечне принадлежит ЭТОЙ задаче. Ожидаемые её адреса: правила полосы и убирания в `test_ads_editor.py`, правила фрагмента загрузки в `test_ads_image_upload.py`.</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py::test_order_ii_a_stored_key_is_not_carried_out_by_the_removal tests/test_pages/test_ads_editor.py::test_a_removal_deletes_only_the_removed_tile tests/test_pages/test_ads_editor.py::test_a_text_only_autosave_leaves_the_tray_alone -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в строке итога не "3 passed"</fails_when>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_inventory.py tests/test_templates/test_components.py tests/test_templates/test_ads_form_security.py -q</automated>
    <fails_when>ненулевой код возврата, ЛИБО в перечне `FAILED` стои́т хотя бы одно имя. От порядка правила этих четырёх модулей не зависят и опора партии на них зелена, поэтому каждое имя принадлежит ЭТОЙ задаче, и имя РАЗЛИЧАЕТ причину: правило объявленного числа внеполосных блоков или правило разбора цели внеполосного блока в `test_htmx_markup_gates.py` — инвентарь разошёлся с деревом и двигается замером; любое имя из `test_htmx_inventory.py`, `test_components.py` или `test_ads_form_security.py` — правка задела не то, во что целилась, и сдвигом объявленного числа НЕ лечится.</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -v '^\s*#' app/services/image_keys.py | grep -c 'def media_dom_id'` равен 1.
    - `grep -c 'media_dom_id' app/pages/common.py` не меньше 1 (регистрация глобала).
    - `grep -c 'media_dom_id' app/templates/ads/includes/media_strip.html` равен 1 и `grep -c 'media_dom_id' app/templates/ads/includes/autosave_response.html` равен 1 — идентификатор производит одна функция в обоих печатающих местах.
    - `grep -c 'oob is defined' app/templates/ads/includes/media_strip.html` равен 0.
    - В `app/templates/ads/includes/autosave_response.html` не осталось ИСПОЛНЯЕМОГО включения файла полосы — проверяется чтением тела шаблона. Счётным гейтом это не меряется намеренно: имя файла остаётся в комментарии шапки, где прежняя редакция называется по идиоме, и счёт вхождений был бы зелен от собственной прозы.
    - Исполняемых включений файла полосы в дереве осталось два — страница редактора (`app/templates/ads/form.html`) и ответ маршрута загрузки (`app/pages/ads.py`): проверяется чтением обоих мест.
    - В `app/templates/ads/includes/media_strip.html` скрытое поле `name="images"` стои́т ВНУТРИ тега `span` плитки, а не соседом: проверяется чтением цикла плиток.
    - Сигнатура `_autosave_response` в `app/pages/ads.py` больше не объявляет булева параметра разрешения перерисовки — проверяется чтением сигнатуры, а не счётом вхождений: прежнее имя намеренно остаётся в докстринге по идиоме «прежняя редакция называется», и счётный гейт был бы зелен от собственной прозы.
    - `grep -v '^\s*#' app/pages/ads.py | grep -c 'media_gone'` не меньше 3 (параметр, контекст шаблона, передача из сборщика).
    - `uv run pytest tests/test_pages/test_ads_image_upload.py::test_order_ii_a_stored_key_is_not_carried_out_by_the_removal -q -p no:randomly` зелено, и это правило поднимает ДВА обработчика в одном прогоне.
    - Утверждение об убранном ключе в `test_a_removal_deletes_only_the_removed_tile` сформулировано над ЗНАЧЕНИЯМИ полей `name="images"`, а не над присутствием ключа как произвольной подстроки ответа: проверяется чтением тела правила. Замена действующего утверждения названа в докстринге летописью, и летопись называет вычитающую метку плана 12-12 причиной сужения. Счётным гейтом это не меряется намеренно: прежняя форма утверждения остаётся в докстринге по идиоме «прежняя редакция называется», и счёт вхождений был бы зелен от собственной прозы.
    - НУЛЕВОЙ СЧЁТ полей вложений в том же правиле снят над ИМЕНЕМ поля (вхождение `name="images"`), а НЕ через образец полного набора атрибутов вида `HIDDEN_KEY_FIELD`: проверяется чтением тела правила. Это и есть условие, при котором замена действующего `value="{dropped}" not in html` остаётся УСИЛЕНИЕМ: счёт через полный набор атрибутов был бы зелен от поля вложений, написанного в другом порядке атрибутов, то есть слабее заменяемого. Двум утверждениям — два инструмента, и читаются они в теле правила раздельно.
    - Перечень идентификаторов, названных к снятию, в `test_order_ii_a_stored_key_is_not_carried_out_by_the_removal` снимается ТОЛЬКО с блоков, у которых значение признака внеполосной подмены равно `delete`: проверяется чтением образца в теле правила. Без этой привязки равенство перечня краснело бы от законных блоков соседних задач партии (плитка `media-add-tile` задачи 2, метка `media-tombstones` плана 12-12), не тронув предмета правила.
    - RED измерен, а не заявлен: оба новых правила прогонялись ДО правки исходников, покраснели, вывод снят с `--junit-xml=/tmp/gsd-red-12-11-t1.xml`, и дословный текст покраснения приведён в сводке плана.
    - Ответ маршрута загрузки не тронут: `git diff 3b9b8cfe..HEAD -- app/pages/ads.py` не содержит изменений в границах `_media_strip_fragment`. Основание диапазона названо, а не подразумевается: `3b9b8cfe` — коммит ре-верификации, с которого начинается эта партия; голый `git diff` на закоммиченной правке пуст и прошёл бы вхолостую.
  </acceptance_criteria>

  <done>Ключ, принятый загрузкой и записанный в хранилище, переживает нажатие «×» по соседней плитке: ответ убирания снимает ровно ту плитку, которую этот запрос видел, и ничего больше.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Плитка «+ ФАЙЛ» перестаёт врать после убирания на потолке</name>

  <read_first>
    - `app/templates/ads/includes/media_strip.html` после задачи 1 — метка плитки добавления в конце узла и её условие скрытости
    - `app/templates/ads/includes/media_refusals.html` целиком — образец шапки вынесенного единственного источника разметки и причина выноса
    - `app/templates/ads/includes/autosave_response.html` после задачи 1 — где встают верхнеуровневые блоки
    - `app/pages/ads.py:420-441` — `_editor_context`: откуда в шаблоне берётся потолок (`editor.max_images`)
    - `tests/test_pages/test_ads_editor.py:1400-1450` — действующие правила плитки добавления на странице (скрыта, а не удалена)
    - `tests/test_pages/test_ads_image_upload.py:650-700` — правило, режущее разметку плитки добавления в ответе загрузки
    - `app/static/css/app.css:2060-2080` — правила оформления плитки добавления и её скрытого состояния
  </read_first>

  <files>app/templates/ads/includes/media_add_tile.html, app/templates/ads/includes/media_strip.html, app/templates/ads/includes/autosave_response.html, tests/test_pages/test_ads_editor.py, tests/test_templates/test_htmx_markup_gates.py</files>

  <behavior>
    - Объявление на потолке (`max_images_per_ad` вложений): страница печатает плитку добавления скрытой — действующее правило держится дословно.
    - Убирание одного вложения у такого объявления приносит плитку добавления ЗАНОВО, уже без признака скрытости, и человек выбирает следующий файл, не перезагружая страницу.
    - Убирание у объявления НЕ на потолке приносит её же — в том же виде, в каком она уже стои́т; повторная подмена ничего не ломает.
    - Текстовое автосохранение плитки добавления не приносит вовсе.
    - Разметка плитки печатается из ОДНОГО файла: в полосе и в ответе убирания она одна и та же.
  </behavior>

  <action>
    Метка «+ ФАЙЛ» (`media_strip.html`, конец узла полосы) выносится в новый файл
    `app/templates/ads/includes/media_add_tile.html`. Причина выноса — та же, по которой ранее вынесена
    строка отказа (`media_refusals.html`): печатающих мест стало ДВА, и вторая копия разошлась бы с
    первой МОЛЧА. Шапка нового файла пишется по образцу `media_refusals.html`: зачем файл существует,
    какие переменные ожидает, и какое ограничение переносится дословно.

    Ожидаемые переменные нового файла — `attached_count` (число уже прикреплённых вложений),
    `max_images` и необязательный `oob`. Именно СЧЁТ, а не список ключей: у ответа убирания списка
    нет и быть не должно — весь смысл задачи 1 в том, что этот ответ списками не распоряжается.
    Метка получает `id="media-add-tile"` и печатает признак внеполосной подмены только при истинном
    `oob`.

    `media_strip.html` включает новый файл, передавая `attached_count` длиной своего списка ключей, и
    НЕ передаёт `oob` вовсе. Передача — через `{% with %}`, а не через присваивание в общей области
    видимости: именно наследование через область видимости и есть хрупкость, названная находкой
    `IN-02`; заводить её второй раз в НОВОМ коде не следует. Это не починка `IN-02` (та остаётся
    открытой записью ревью, рамка партии не расширяется) — это отказ повторять её в том, что пишется
    сейчас, и так и записать в шапке.

    `autosave_response.html` печатает плитку добавления верхнеуровневым блоком РОВНО ТОГДА, когда
    перечень ключей на снятие непуст, — то есть ровно тем же условием, что и блоки снятия: состав
    вложений изменён, значит число прикреплённых изменилось, значит доступность выбора файла могла
    измениться. Значения передаются `{% with %}`: `attached_count` — длина списка вложений
    перечитанной записи, `max_images` — потолок из контекста редактора, `oob` — истина.

    Назвать в шапке `autosave_response.html` цену, которую эта плитка платит, чтобы следующий
    читатель не принял её за противоречие задаче 1: число прикреплённых берётся ИЗ БАЗЫ и на момент
    убирания может отставать на летящую загрузку, поэтому «+ ФАЙЛ» способна оказаться видимой там,
    где мест уже нет. Исход при этом ГРОМКИЙ, а не молчаливый: сервер считает свободные места сам и
    отвечает строкой отказа по потолку С ИМЕНЕМ файла (D-06). Ключа эта плитка не несёт и потерять
    его не может — она управляющий элемент, а не состояние.

    В `tests/test_pages/test_ads_editor.py` завести правило
    `test_a_removal_at_the_ceiling_brings_the_add_tile_back`: посеять объявление ровно с
    `max_images_per_ad` своими ключами, убедиться, что страница печатает плитку добавления скрытой,
    затем отправить убирание одного ключа и утверждать, что ответ несёт `id="media-add-tile"` и что
    признака скрытости у этого тега нет. Второе утверждение поднимается срезом ТЕГА, а не поиском по
    всему ответу: признак скрытости встречается в документе и у других элементов.

    Объявленное число внеполосных блоков в `tests/test_templates/test_htmx_markup_gates.py` двигается
    ЗАМЕРОМ и одним коммитом с работой (идиома D-13).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_editor.py tests/test_pages/test_ads_image_upload.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, ЛИБО в перечне `FAILED` стои́т хотя бы одно имя, КРОМЕ `test_image_base_url_comes_from_app_settings` (известная нестабильность порядка; её одиночное покраснение — загрязнение, проверяется повторным одиночным прогоном ЕЁ ОДНОЙ). Ожидаемый адрес покраснения ЭТОЙ задачи — действующие правила плитки добавления (`tests/test_pages/test_ads_editor.py:1400-1450`); их покраснение означает, что вынос разметки в отдельный файл не бесследен, и правится ВЫНОСОМ, а не правкой их утверждений. НЕ ОЖИДАЕМЫЙ АДРЕС, И ЭТО ЧАСТЬ СИГНАЛА: правила задачи 1, меряющие перечень снятых идентификаторов (`test_order_ii_a_stored_key_is_not_carried_out_by_the_removal`, `test_a_removal_deletes_only_the_removed_tile`). Плитка добавления приезжает тем же ответом, но их образцы читают ТОЛЬКО блоки со способом подмены `delete`, поэтому краснеть от неё не могут; если покраснели — привязка образца к `delete` в задаче 1 не выполнена, и правится она ТАМ, у образца, а не здесь и не сдвигом объявленного числа.</fails_when>
    <automated>uv run pytest tests/test_templates/ -q</automated>
    <fails_when>ненулевой код возврата, ЛИБО в перечне `FAILED` стои́т хотя бы одно имя. Опора партии на каталоге зелена, от порядка эти правила не зависят, поэтому имя принадлежит ЭТОЙ задаче и различает причину: правило объявленного числа внеполосных блоков — инвентарь двигается замером; правило единственности источника разметки или инвентаря шаблонов — вынос плитки задел не то, во что целился.</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_editor.py::test_a_removal_at_the_ceiling_brings_the_add_tile_back -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо в строке итога не "1 passed"</fails_when>
  </verify>

  <acceptance_criteria>
    - Файл `app/templates/ads/includes/media_add_tile.html` существует и `grep -c 'media-tile--add' app/templates/ads/includes/media_strip.html` равен 0 — разметка плитки печатается из одного места.
    - `grep -c 'media_add_tile.html' app/templates/ads/includes/media_strip.html` равен 1 и `grep -c 'media_add_tile.html' app/templates/ads/includes/autosave_response.html` равен 1.
    - `grep -c 'media-add-tile' app/templates/ads/includes/media_add_tile.html` не меньше 1.
    - `grep -c '{% with' app/templates/ads/includes/autosave_response.html` не меньше 1 — признак передан явно, а не унаследован областью видимости.
    - Действующие правила страницы (`tests/test_pages/test_ads_editor.py:1400-1450`) зелены без единой правки их утверждений.
    - `uv run pytest tests/test_templates/ -q` зелено при объявленном числе внеполосных блоков, сходящемся с деревом.
  </acceptance_criteria>

  <done>После убирания вложения у объявления на потолке человек может выбрать следующий файл, не перезагружая страницу, и разметка этой плитки живёт в одном файле.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ убирания → документ | внеполосный блок правит документ; до этого плана он заменял узел ЦЕЛИКОМ и уносил состояние, которого сам не производил |
| присланный клиентом ключ → идентификатор узла документа | значение, подконтрольное отправителю, попадает в атрибут разметки |
| два летящих запроса одного человека → одна область документа | у каждого свой снимок состояния вложений |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11-01 | Tampering | `autosave_response.html`, внеполосная правка документа | high | mitigate | ответ адресует РОВНО те ключи, которые его запрос видел и убрал; узел, содержащий чужие ключи и строки отказа, не подменяется |
| T-12-11-02 | Injection | `media_dom_id(key)` → атрибут `id` разметки | medium | mitigate | значение идентификатора есть шестнадцатеричный хеш фиксированной длины с постоянным префиксом; сырой ключ в атрибут не попадает, набор символов закрыт по построению |
| T-12-11-03 | Spoofing | ключ, присланный документом, на пути убирания | medium | mitigate | рубеж владения не сдвинут: `own_image_keys` по-прежнему единственный, кто решает «мой ли ключ», и блок снятия печатается только для ключа, прошедшего сверку и действительно убранного из списка |
| T-12-11-04 | Denial of Service | плитка добавления, показанная на потолке из-за отстающей базы | low | accept | ключ не теряется; исход — строка отказа по потолку С ИМЕНЕМ файла, то есть громкий отказ вместо молчания; записывается остатком планом 12-12 |
| T-12-11-05 | Tampering | ПОРЯДОК I зонда WR-07 (ответ загрузки приходит последним) | high | transfer | этим планом НЕ закрыт и передан плану 12-12 (метка убирания и вычитание на сохранении); открытость названа в `must_haves.assumptions`, а не умолчана |
</threat_model>

## Артефакты, которые этот план производит

- `app/services/image_keys.py`: функция `media_dom_id`
- `app/pages/common.py`: глобал окружения шаблонов `media_dom_id`
- `app/templates/ads/includes/media_strip.html`: атрибут `id` у плитки, скрытое поле ключа внутри плитки
- `app/templates/ads/includes/media_add_tile.html`: новый файл — единственный источник разметки плитки добавления; идентификатор `media-add-tile`
- `app/templates/ads/includes/autosave_response.html`: блоки адресного снятия по параметрическому идентификатору `media-item-…`; блок плитки добавления
- `app/pages/ads.py`: параметр и ключ контекста `media_gone`
- `tests/test_pages/test_ads_editor.py`: правила `test_a_removal_deletes_only_the_removed_tile`, `test_a_removal_at_the_ceiling_brings_the_add_tile_back`
- `tests/test_pages/test_ads_image_upload.py`: правило `test_order_ii_a_stored_key_is_not_carried_out_by_the_removal`
- `tests/test_templates/test_htmx_markup_gates.py`: сдвинутое замером объявленное число внеполосных блоков (и запись параметрического идентификатора, если действующий разбор её потребует)

Новых маршрутов, новых заголовков ответа и новых текстов для человека план не заводит.

## Кромка, выданная пробником, и что с ней сделано

Пробник кромок выдал на FETCH-01 одну строку: `category: concurrency`, `status: unresolved`. У неё
три грани. Грань «убирание против загрузки, ПОРЯДОК II» закрывается ЭТИМ планом и становится
проверяемой истиной. Грань «убирание против загрузки, ПОРЯДОК I» остаётся открытой до плана 12-12 и
записана флагом в `must_haves.assumptions`. Грани «сироты при обрыве партии, межвкладочный счёт» и
«вытесняющая очередь теряет средний выбор» остаются ПРИНЯТЫМИ допущениями решений владельца D-17 и
D-16 и уже записаны в `deferred-items.md`. Ни одна не отброшена молча.

<verification>
- Ответ убирания не несёт узла полосы и называет ровно один идентификатор — тот, который видел.
- Ключ, записанный летящей загрузкой, переживает убирание соседней плитки (ПОРЯДОК II, два обработчика в одном правиле).
- Убранное с экрана исчезает; строки отказа и плитка добавления не страдают.
- Идентификатор плитки производит одна функция, и правило берёт его вызовом, а не повтором.
</verification>

<success_criteria>
- ПОРЯДОК II зонда WR-07 держится и держится ПРАВИЛОМ, а не рассуждением.
- Решение владельца **D-19** процитировано по идентификатору в исходнике и в правиле.
- Находки вне рамки (`WR-02`, `IN-02`…`IN-06`, `IN-07`…`IN-10`) не тронуты; исчезновение предмета `IN-02` на пути полосы названо вслух (**D-20**).
- Ни одно правило суиты не ослаблено: перенацеленное правило инвертировано ВВЕРХ с сохранённой летописью. Сужение утверждения об убранном ключе до ЗНАЧЕНИЙ полей `name="images"` ослаблением не является и не считается таковым по построению: ответ не несёт таких полей ВОВСЕ, то есть на оси «ключ как прикрепляемое состояние» правило стало сильнее, а снятая ось (ключ как произвольная подстрока) отдана вычитающей метке плана 12-12 и замеряется там правилом `test_the_removal_mark_is_the_only_place_the_removed_key_comes_back` — не рассуждением.
</success_criteria>

<output>
Создать `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-11-SUMMARY.md` по завершении.
</output>
