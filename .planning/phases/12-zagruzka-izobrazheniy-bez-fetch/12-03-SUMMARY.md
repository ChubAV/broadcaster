---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 03
subsystem: ui
tags: [htmx, jinja2, oob-swap, image-upload, editor, security-gate]

# Dependency graph
requires:
  - phase: 12-zagruzka-izobrazheniy-bez-fetch
    provides: "план 12-01 — единственный источник разметки полосы `ads/includes/media_strip.html`, форма загрузки `ads/includes/media_upload_form.html`, узел `#media-tray`, маршрут `/ads/images`"
  - phase: 09-pilot-na-account-groups-skvoznoy-kontrakt-formy
    provides: "идиома «постоянная обёртка + подмена содержимого» (D-12) и ловушка G-11 «идентификатор не бывает целью подмены и внеполосной целью разом»"
  - phase: 10-rychag-components-modal-html
    provides: "обёртка скрипта редактора немедленно вызываемым выражением (план 10-13) — тело правится, обёртка остаётся"
provides:
  - "`ads/form.html` без клиентской сборки разметки и без ручной сборки запроса: `createElement`, `replaceChildren` и `fetch` равны НУЛЮ"
  - "Полоса вложений на странице редактора — постоянная обёртка `#media-strip` с включением единственного источника разметки; форма загрузки её соседом"
  - "Форма объявления закрывается сразу после полей: форма загрузки и полоса стали её СОСЕДЯМИ, скрытые поля ключей связаны атрибутом `form=\"ad-form\"`"
  - "Порог счётчика символов читается из РАЗМЕТКИ — числа плиток полосы, которую рисует сервер (D-11)"
  - "Условный признак внеполосной подмены `oob` на узле `#media-tray`: ответ автосохранения приносит полосу внеполосным блоком"
  - "Шесть правил, переведённых ВМЕСТЕ с предметом: инвертированы, перенацелены или сняты с записью"
affects: [12-04, 12-05, 13-fetch-02, 15-fetch-03]

# Actuals (#2632) — chars/4 по РЕАЛИЗОВАННОМУ диффу, не счётчик харнесса.
actuals:
  tokens: 25090
  tasks: 2
  commits: 4
  plan_head_before: cb601376cd56286fb046c6e9567b3d0cdc1bb9e6

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Условный параметр `oob` у включаемого файла-содержимого: один признак на три места включения, и двум прежним разметка остаётся байт-в-байт прежней"
    - "Утверждение гейта ИНВЕРТИРУЕТСЯ вместе с исчезновением предмета («≥ 3» → «== 0»), а не ослабляется и не снимается"
    - "Проза комментария не набирает имён, которые сличает правило: гейт считает вхождения в ТЕКСТЕ файла и объяснение от кода не отличает"
    - "Инвентарное число, пошедшее ВНИЗ, записывается перечислением того, что ушло, — иначе движение неотличимо от потери разборщиком"

key-files:
  created: []
  modified:
    - app/templates/ads/form.html
    - app/templates/ads/includes/media_strip.html
    - app/templates/ads/includes/autosave_response.html
    - tests/test_templates/test_ads_form_security.py
    - tests/test_templates/test_htmx_inventory.py
    - tests/test_templates/test_walkthrough_anchors.py
    - tests/test_templates/test_htmx_markup_gates.py
    - tests/test_templates/test_components.py
    - tests/test_pages/test_ads_editor.py
    - tests/test_pages/test_hx_location_destinations.py

key-decisions:
  - "Признак `data-media` переставлен ПЕРВЫМ атрибутом узла полосы: правило плитки режет разметку литералом `<div data-media`, и после переезда полосы литералу негде находиться, кроме как в включаемом файле. Альтернатива — навесить признак ещё и на обёртку — завела бы вторую область раскладки (CSS `[data-media]` — это flex) и две копии признака"
  - "Прежние редакции комментариев называются ОПИСАНИЕМ, а не цитатой: три места шаблона и одно место теста цитировали бы ровно те имена, которые сличают гейты, и правило покраснело бы на объяснении того, что оно охраняет"
  - "Утверждение об условных внеполосных блоках УСИЛЕНО с литерального числа до ПЕРЕЧНЯ ИМЁН: второй условный блок ВЗАМЕН первого дал бы то же число при полностью сменившемся предмете"
  - "Хвостовой вызов отрисовки из скрипта снят без замены: первичное состояние счётчика приезжает готовым из разметки, и пересчёт на старте пересчитывал бы уже посчитанное"
  - "Инвентарь регистраций обработчиков опущен 8 → 5 БЕЗ решения владельца, и граница названа: решение требуется на РОСТ (новая сущность в смысле D-13), а здесь сущностей стало меньше"

patterns-established:
  - "Два идентификатора на одну разметку, потому что роли разные: обёртка — цель подмены, внутренний узел — внеполосная цель (Pattern 5 разведки, ловушка G-11)"
  - "Правило, потерявшее ПОЛОВИНУ предмета, разбирается на половины: слабая снимается вместе с предметом, сильная сохраняется отдельным утверждением"

requirements-completed: [FETCH-01]

coverage:
  - id: D1
    description: "Клиентской сборки разметки в редакторе нет ВОВСЕ: `createElement` и `replaceChildren` в `ads/form.html` равны нулю, и утверждение гейта ИНВЕРТИРОВАНО, а не ослаблено"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "tests/test_templates/test_ads_form_security.py#test_ads_form_uses_property_assignment"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_ads_form_security.py#test_ads_form_builds_dom_not_markup"
        status: pass
      - kind: other
        ref: "grep -c 'createElement' app/templates/ads/form.html → 0; grep -c 'replaceChildren' → 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Ручной сборки запроса в разделе объявлений не осталось: мест в `app/templates/` стало 5, остаток — пять вхождений экрана подключения Telegram (предмет Фазы 13)"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "tests/test_templates/test_htmx_inventory.py#test_manual_request_assembly_matches_the_declared_count"
        status: pass
      - kind: other
        ref: "grep -c 'fetch' app/templates/ads/form.html → 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "Полоса вложений рисуется СЕРВЕРОМ из единственного источника и включается страницей редактора; `#image-inputs` и `#upload-error` исчезли вместе с функцией, которая их держала"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_editor.py#test_editor_tile_asks_for_the_thumbnail_and_declares_a_fallback"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_editor.py#test_editor_shows_only_add_tile_without_attachments"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_editor.py#test_attachment_remove_is_a_named_submit_inside_the_form"
        status: pass
      - kind: other
        ref: "grep -c 'id=\"image-inputs\"' app/templates/ads/form.html → 0; grep -c 'id=\"upload-error\"' → 0"
        status: pass
    human_judgment: false
  - id: D4
    description: "Форма загрузки и полоса стали СОСЕДЯМИ формы объявления, а не её содержимым; секция расписаний осталась соседом, вложенных форм нет"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_editor_markup_has_no_nested_forms"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_editor_schedules.py#test_editor_schedule_section_is_a_sibling_of_the_ad_form"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_walkthrough_anchors.py#test_every_walkthrough_anchor_exists_in_its_source_template"
        status: pass
    human_judgment: false
  - id: D5
    description: "Ответ автосохранения приносит полосу внеполосным блоком по ВТОРОМУ идентификатору: цель подмены (`#media-strip`) и внеполосная цель (`#media-tray`) не совпадают"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_editor.py#test_autosave_response_carries_the_media_tray"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py#test_no_id_is_both_a_swap_target_and_an_oob_target"
        status: pass
      - kind: unit
        ref: "tests/test_templates/test_htmx_markup_gates.py#test_the_number_of_oob_blocks_is_the_declared_one"
        status: pass
    human_judgment: false
  - id: D6
    description: "Ответ автосохранения по-прежнему не содержит НИ ОДНОЙ формы: форма загрузки лежит вне полосы, и правило не ослаблено ни на символ"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_editor.py#test_autosave_response_carries_no_form"
        status: pass
    human_judgment: false
  - id: D7
    description: "Порог счётчика символов читается из числа плиток полосы, а не из клиентского массива; ни нового поля, ни внеполосной подмены счётчика не заведено"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_editor.py#test_counter_threshold_follows_the_presence_of_attachments"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_editor.py#test_editor_counter_is_server_rendered"
        status: pass
    human_judgment: true
    rationale: "Оба правила проверяют СЕРВЕРНУЮ половину порога — класс счётчика в отрендеренной разметке. Клиентская половина (переключение порога на ближайшем нажатии клавиши по числу плиток в документе) машинной проверки не имеет: суита не исполняет JS редактора, а гарнир двойного исполнения (`test_hx_location_destinations.py`) меряет столкновение объявлений, а не поведение счётчика. Наблюдаемо только в браузере."
  - id: D8
    description: "В браузере убранное кнопкой «×» вложение исчезает с экрана после круга к серверу, набранный текст не теряется, страница не перерисовывается целиком"
    requirement: FETCH-01
    verification: []
    human_judgment: true
    rationale: "Машинно проверен КОНТРАКТ ОТВЕТА (D5): полоса приезжает внеполосным блоком с признаком подмены и скрытыми полями ключей. Что рантайм разметки действительно применит этот блок к живому документу, что каретка и набранный текст переживут круг и что перерисовки всей страницы не случится — свойства браузера, которых суита не исполняет. Пункт ручного обхода фазы, обязан попасть в `12-UAT.md` отдельной строкой (`<human-check>` задачи 2)."

# Metrics
duration: 1h 45m
completed: 2026-09-18
status: complete
---

# Phase 12 Plan 03: Полоса вложений в редакторе Summary

**Редактор объявления перешёл на серверную полосу вложений: `imagePaths` и все семь функций, что на нём держались, сняты вместе с гейтами, потерявшими предмет, а убранная кнопкой «×» плитка исчезает с экрана внеполосным блоком ответа автосохранения по ВТОРОМУ идентификатору.**

## Performance

- **Duration:** 1h 45m (из них 74 минуты — два полных прогона суиты по 37 минут)
- **Started:** 2026-09-18T19:46:21Z
- **Completed:** 2026-09-18T21:32:05Z
- **Tasks:** 2 из 2
- **Files modified:** 10 (создано 0, правлено 10)

## Accomplishments

- **Мена состоялась: DOM стал состоянием.** Из скрипта редактора ушли `imagePaths`, `renderImages()`, `uploadFile()`, `handleFiles()`, `refusalTextOf()`, `removeImage()`, `fileNameOf()`, `showUploadError()`, `clearUploadError()`, `requestSave()`, слушатель файлового поля и семь констант. Осталось ровно то, что фаза не трогает: счётчик символов и состояние ошибки автосохранения. `createElement`, `replaceChildren` и `fetch` в файле равны НУЛЮ.
- **Гейт безопасности ИНВЕРТИРОВАН, а не снят.** `test_ads_form_uses_property_assignment` требовал узловой сборки (`≥ 3` и `≥ 2`) и был фактически неверен: докстринг говорил про предпросмотр, а мерило считало клиентскую сборку плиток. Теперь требование — ровно ноль обоих имён, то есть СИЛЬНЕЕ прежнего; соседнее «разметка не собирается строкой» не тронуто и осталось строгим.
- **Форма объявления закрылась раньше карточки.** Тег формы переехал внутрь `data-form` и закрывается сразу после скрытого поля идентификатора — иначе форма загрузки оказалась бы ВЛОЖЕННОЙ, а вложенную форму браузер молча отбрасывает. Порядок разметки не изменился ни на узел; скрытые поля ключей едут с формой по атрибуту `form="ad-form"`.
- **Убранное вложение доезжает до экрана.** Узел `#media-tray` получил признак внеполосной подмены ПОД УСЛОВИЕМ, и ответ автосохранения включает полосу тем же единственным источником разметки. Обёртка `#media-strip` осталась целью подмены формы загрузки: совпади идентификаторы — упало бы правило двух ролей.
- **Шесть правил переведены ВМЕСТЕ с предметом, ни одно не ослаблено:** одно инвертировано, два перенацелены (контракт скрытых полей → на файл полосы, две записи реестра якорей → на включаемые файлы), три сняты с записью прежней редакции (перехват «×», приставка миниатюры в скрипте, чтение причины из тела ответа), и одна сильная половина снятого правила сохранена отдельным утверждением.
- **Пять инвентарных чисел и опор, которых быстрые прогоны не видят по построению**, найдены ПОЛНЫМ прогоном (G-10) и поставлены прогоном покрасневшего правила — см. «Deviations».

## Task Commits

1. **Задача 1, RED: правила, у которых предмет уезжает с клиента на сервер** — `f451455` (test)
2. **Задача 1, GREEN: полоса с сервера, клиентское состояние снято** — `b12c835` (feat)
3. **Задача 2, RED: полоса внеполосным блоком ответа автосохранения** — `a4e0872` (test)
4. **Задача 2, GREEN: условный признак и внеполосный блок** — `4995b52` (feat)

REFACTOR-коммитов нет ни у одной задачи, и это записано, а не умолчано: обе задачи СНИМАЮТ код и разметку, а не добавляют слой, который можно было бы затем причесать. После GREEN улучшать было нечего.

## Files Created/Modified

- `app/templates/ads/form.html` — форма закрывается после полей; блок вложений заменён обёрткой `#media-strip` с включением единственного источника и соседней формой загрузки; скрипт без состояния вложений; импорт макроса миниатюры снят; шапка файла и шапка обёртки скрипта переписаны с называнием прежних редакций
- `app/templates/ads/includes/media_strip.html` — признак `data-media` первым атрибутом, условный признак внеполосной подмены `oob`, шапка переписана (отступление от образца объяснено, а не сделано молча)
- `app/templates/ads/includes/autosave_response.html` — внеполосный блок полосы, пятый идентификатор в перечне шапки, правлен абзац про индикатор ВНУТРИ формы
- `tests/test_templates/test_ads_form_security.py` — инверсия гейта, перенацеливание контракта скрытых полей, снятие правила слушателя с сохранением его сильной половины
- `tests/test_templates/test_htmx_inventory.py` — `MANUAL_FETCH_PLACES` и потолок Фазы 8 6 → 5, запись раздела объявлений снята, летопись дописана
- `tests/test_templates/test_walkthrough_anchors.py` — две записи реестра перенацелены; неподвижность объявленного числа записана
- `tests/test_templates/test_htmx_markup_gates.py` — `OOB_BLOCKS` 19 → 20 с летописью; `CONDITIONAL_OOB_TEMPLATES` взамен литерального числа; `FORM_ATTR_INDENT` — одна копия отступа для опор подстановок
- `tests/test_templates/test_components.py` — инвентарь регистраций обработчиков `ads/form.html` 8 → 5 с перечислением трёх ушедших
- `tests/test_pages/test_ads_editor.py` — новое правило внеполосной полосы; сняты два правила и половина третьего; импорт текста отказа снят вместе с единственным читателем
- `tests/test_pages/test_hx_location_destinations.py` — признак экрана редактора `IMAGE_BASE_URL` → `CAPTION_LIMIT`, проза контроля правлена тем же именем

## Decisions Made

Пять решений вынесены во frontmatter (`key-decisions`). Ни одно из четырнадцати запертых решений `12-CONTEXT.md` не пересмотрено. D-13 соблюдён: каждое движение инвентарного числа записано строкой летописи с пометкой «Фаза 12, план 12-03», включая запись «ДВИЖЕНИЯ НЕТ» у реестра якорей.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Признак `data-media` переставлен первым атрибутом узла полосы**

- **Found during:** Задача 1, шаг 2 (замена блока вложений)
- **Issue:** правило `test_editor_tile_asks_for_the_thumbnail_and_declares_a_fallback` режет разметку ЛИТЕРАЛОМ `<div data-media`. До переезда литерал находился в `ads/form.html`; после переезда полосы единственный кандидат — `ads/includes/media_strip.html`, где план 12-01 написал `<div id="media-tray" data-media>`. Литерал не совпал бы, и правило плитки покраснело бы, ничего по существу не изменившись.
- **Fix:** порядок атрибутов узла изменён на `<div data-media id="media-tray">`, причина записана комментарием в файле. Файл в `<files>` задачи 1 не значился — правка сделана как блокирующая.
- **Why not the alternative:** навесить `data-media` ещё и на обёртку значило бы завести ВТОРУЮ область раскладки — в `app.css` `[data-media]` есть `display: flex`, и обёртка стала бы flex-контейнером вокруг flex-строки.
- **Verification:** `test_editor_tile_asks_for_the_thumbnail_and_declares_a_fallback` и `test_responsive_markup.py` зелены; вхождение `data-media` в документе одно.
- **Committed in:** `b12c835`

**2. [Rule 3 — Blocking] Проза комментариев не набирает имён, которые сличают гейты**

- **Found during:** Задача 1, сверка `<acceptance_criteria>` (`grep -c 'createElement'` вернул 2 при нуле кода)
- **Issue:** абзацы, называющие ПРЕЖНЮЮ редакцию дословной цитатой, набирали ровно те имена, которые правила считают вхождениями в ТЕКСТЕ файла. Гейт комментарий от кода не отличает, и объяснение того, что правило охраняет, его же и красило. Тот же класс — критерий `grep -c 'app.routes.uploads'`: прозаический путь с косыми чертами совпадает с образцом, где точка не экранирована.
- **Fix:** четыре места переписаны ОПИСАНИЕМ вместо цитаты («создание элемента», «подмена детей узла», «снимаемый модуль маршрутов загрузки»), и в двух местах записано, ПОЧЕМУ имя не набрано. Заодно правлен предсуществующий абзац про источник правды формы ключа: после переезда плана 12-01 правду держит `app/services/image_upload.py`, и абзац теперь говорит верное.
- **Verification:** все восемь грепов критериев задачи 1 дают требуемые значения; правила безопасности зелены.
- **Committed in:** `b12c835`

**3. [Rule 3 — Blocking] Инвентарь регистраций обработчиков `ads/form.html` 8 → 5**

- **Found during:** ПОЛНЫЙ прогон после задачи 1 (`uv run pytest tests/ -q`) — правило `test_criterion_three_holds_by_the_numbers` в `test_components.py` и два его контроля
- **Issue:** утверждение 5 из 5 («регистрации обработчиков в телах блоков `<script>`») объявляло восемь; со снятием клиентского слоя их стало пять. Правило само называет два прочтения движения вниз — «регистрация ушла из дерева ЛИБО её потерял разборщик», — и второе опаснее первого.
- **Fix:** прочтение ДОКАЗАНО перечислением, а не объявлено: ушли ровно три (слушатель `change` файлового поля, слушатель `error` миниатюры в клиентской плитке, перехват `click` кнопки «×»), остались ровно пять (ввод в поле текста и четыре подписки формы). Число поставлено прогоном покрасневшего правила, вывод которого выписан в летопись дословно. Граница названа: решение владельца требуется на РОСТ, а не на убыль.
- **Verification:** `test_components.py` — 92 passed; два контроля, читающих каталог объявлений, зелены.
- **Committed in:** `b12c835`

**4. [Rule 3 — Blocking] Опоры подстановок контролей формы объявления разъехались с отступом**

- **Found during:** тот же ПОЛНЫЙ прогон — два контроля `test_htmx_markup_gates.py`
- **Issue:** `FORM_OPEN` и вторая опора — не регулярные выражения, а ТОЧНЫЕ строки с десятью пробелами отступа. Переезд тега формы внутрь `data-form` увеличил отступ до шестнадцати, и контроли назвали это дословно: «образец подстановки встречается в ads/form.html 0 раз(а), а не один».
- **Fix:** отступ объявлен ОДНОЙ константой `FORM_ATTR_INDENT`, из которой строятся обе опоры: две копии числа пробелов разъехались бы молча при следующем таком переезде.
- **Verification:** `test_htmx_markup_gates.py` — 79 passed, оба контроля зелены.
- **Committed in:** `b12c835`

**5. [Rule 2 — Missing Critical] Утверждение об условных внеполосных блоках усилено перечнем имён**

- **Found during:** Задача 2, `<verify>` после правки шаблонов
- **Issue:** правило считало условные блоки литеральным `len(conditional) == 1`. Полоса стала вторым таким блоком, и число пришлось бы двигать. Но число, поднятое до двух, не заметило бы ПОДМЕНЫ: условный блок, появившийся ВЗАМЕН прежнего, дал бы ту же двойку при полностью сменившемся предмете — а смысл утверждения именно в том, что разбор видит блок, собранный самым хитрым способом.
- **Fix:** заведён `CONDITIONAL_OOB_TEMPLATES` — перечень ИМЁН с записью, зачем условен каждый; утверждение сличает множества.
- **Verification:** правило зелено; подмена любого из двух имён краснит его.
- **Committed in:** `4995b52`

**6. [Rule 3 — Blocking] `OOB_BLOCKS` 19 → 20 и снятые импорты**

- **Found during:** Задача 2, `<verify>`; Задача 1, снятие правил
- **Issue:** новый условный признак двинул инвентарь внеполосных блоков; снятие правила про приставку миниатюры и половины правила про текст отказа оставило два импорта без единого читателя, а снятие полосы из страницы — импорт макроса миниатюры в `ads/form.html`.
- **Fix:** `OOB_BLOCKS` поставлен ПРОГОНОМ покрасневшего правила («внеполосных блоков найдено 20, объявлено 19») с летописью, объясняющей, почему прибавка ровно один. Три импорта сняты вместе со своими читателями, каждый — с записью основания.
- **Verification:** полный прогон `3418 passed`, rc=0.
- **Committed in:** `b12c835`, `4995b52`

---

**Total deviations:** 6 auto-fixed (5 × Rule 3 — блокирующие, 1 × Rule 2 — недостающее критичное).
**Impact on plan:** ни одна правка не ослабила правило; две (№ 3 и № 5) сделали утверждения сильнее прежних. Расширения объёма нет: четыре из шести — инвентари и опоры, требуемые D-13 самим планом, две — форма записи прежних редакций.

## Отклонения ОТ ОЖИДАНИЙ ПЛАНА (не деффекты, но записаны)

- **Две записи реестра якорей и контракт скрытых полей позеленели СРАЗУ, а не покраснели.** План предсказал это для контракта скрытых полей дословно («предмет переехал, а не исчез»), но для реестра якорей ожидал красного. Красным он быть не мог: план 12-01 уже завёл оба включаемых файла с нужными признаками, поэтому перенацеливание нашло предмет с первого прогона. Записано, а не замаскировано.
- **`createElement` в `ads/form.html` было ШЕСТЬ, а не пять.** Разведка (G-2) намерила пять — 4 в `renderImages()` и 1 в `showUploadError()`. Прогон покрасневшего правила назвал шесть (`assert 6 == 0`). Разница не имеет последствий: все вхождения ушли, и число взято ИЗ ПРОГОНА, а не из разведки.

## TDD Gate Compliance

| Gate | Коммит | Статус |
|------|--------|--------|
| RED (задача 1) | `f451455` `test(12-03)` | ✓ улика проверена прибором: `RED_EVIDENCE_OK` / `target_test_failed`, `6 failed / 33 passed`, целевой тест `test_manual_request_assembly_matches_the_declared_count` покраснел утверждением `assert 6 == 5` |
| GREEN (задача 1) | `b12c835` `feat(12-03)` | ✓ RED предшествует GREEN; 172 passed в затронутых модулях |
| RED (задача 2) | `a4e0872` `test(12-03)` | ✓ улика проверена прибором: `RED_EVIDENCE_OK` / `target_test_failed`, `1 failed / 2 passed`, покраснение утверждением `assert 'id="media-tray"' in ...` |
| GREEN (задача 2) | `4995b52` `feat(12-03)` | ✓ RED предшествует GREEN; 3 passed по `-k autosave_response`, включая неправленое `test_autosave_response_carries_no_form` |
| REFACTOR | — | не заводился ни у одной задачи: обе СНИМАЮТ код, причёсывать после GREEN было нечего |

Гейт `execute-mvp-tdd` (TDD_MODE=true) проверялся перед шагом реализации каждой задачи: `test(12-03)`-коммит существует и трогает `tests/`, улика RED признана намеренной прибором, `feat(12-03)`-коммита раньше него нет. Гейт НЕ сработал ни разу.

⚠️ **Целевой тест улики задачи 1 выбран, а не взят первым попавшимся.** Красных правил было шесть, но три из них — КОНТРОЛИ, краснеющие от самого движения константы, а не от состояния дерева; четвёртое (`never_grows`) говорит о находке, а не о прогрессе. Целевым назван тот, чей отказ ЕСТЬ задача плана: «число упало до 5 — опустите константу и потолок». Он покраснел утверждением о предмете, а не о форме записи.

## Known Stubs

Нет. Заглушка, оставленная планом 12-01 (`полоса и форма загрузки не подключены к странице редактора`, запись 90 `.planning/WINDOWS.md`), ЗАКРЫТА этим планом и переведена в `fixed`. Две оставшиеся заглушки фазы принадлежат планам 12-04 (сверка принадлежности ключей при загрузке) и 12-05 (снятие `app/routes/uploads.py` и переезд его суиты) и этим планом не двигались.

## Threat Flags

Новой поверхности, не названной `<threat_model>` плана, не появилось. Три `mitigate`-меры применены и наблюдаемы:

| Угроза | Мера | Чем наблюдаема |
|---|---|---|
| T-12-10 (сток разметки в редакторе, CR-01) | утверждение гейта ИНВЕРТИРОВАНО на более сильное — клиентской сборки разметки в файле нет вовсе; соседнее «нет стока, разбирающего строку» не тронуто | `test_ads_form_uses_property_assignment` (`== 0` обоих имён) и `test_ads_form_builds_dom_not_markup` |
| T-12-11 (имя файла подконтрольно отправителю) | на плитке рендерится только нормализованное имя из СОХРАНЁННОГО ключа, и живёт оно в подсказке; правило переехало вместе с разметкой в единственный источник | `test_attachment_remove_is_a_named_submit_inside_the_form`, разметка `media_strip.html` |
| T-12-09 (подделанный ключ в скрытом поле едет в `Ad.images`) | последний рубеж НЕ двинут: `own_image_keys` при сохранении объявления сверяет форму ключа и префикс пользователя; план его не ослаблял и не обходил | `tests/test_pages/test_ads_image_ownership.py` зелён в полном прогоне |

`T-12-SC` (установки пакетов) — `accept`: план не установил ни одного внешнего пакета.

## Issues Encountered

- **Полный прогон суиты снова стоит 37 минут, и он снова был не формальностью.** Первый прогон (после задачи 1) нашёл ПЯТЬ красных правил в двух файлах, которых быстрые прогоны затронутых модулей не видят по построению: инвентарь регистраций обработчиков и две опоры подстановок, завязанные на точный отступ тега формы. Второй прогон подтвердил зелёное дерево: `3418 passed`, rc=0, 36:42.
- **Счёт тестов изменился на −1, и разница сошлась ТОЧНО.** База 3419 (после плана 12-02) минус три снятых правила (`test_ads_form_remove_handler_is_listener`, `test_editor_javascript_carries_the_thumbnail_prefix`, `test_editor_upload_handler_reads_the_refusal_from_the_response_body`) плюс два новых (`test_ads_form_carries_no_inline_handler_attribute`, `test_autosave_response_carries_the_media_tray`) = 3418. Совпадение до единицы означает, что снятое снято, а не потеряно молча.
- **Нестабильный `test_image_base_url_comes_from_app_settings` прошёл в обоих полных прогонах.** Как и прежде, это наблюдение прогонов, а не вывод «нестабильность исчезла». ⚠️ Предмет этой нестабильности при этом СУЗИЛСЯ: константа `IMAGE_BASE_URL` из шаблона ушла, и тест теперь меряет только шаблонный глобал `s3_public_url()`. Целью это не ставилось (§Deferred `12-CONTEXT.md`) и попутным результатом объявляется осторожно.

## User Setup Required

None — внешних сервисов план не настраивает и ни одного пакета не устанавливает.

## Next Phase Readiness

- **Готово для 12-04** (сверка принадлежности присланных ключей и расчёт свободных мест при ЗАГРУЗКЕ): полоса живёт на экране, ключи едут скрытыми полями, и плану остаётся закрыть серверную половину. ⚠️ Он будет двигать инвентарные числа снова — закладывать полный прогон.
- **Готово для 12-05** (снятие `app/routes/uploads.py` и переезд его суиты в 1099 строк): последний читатель модуля со стороны редактора снят этим планом — импорта `UNSUPPORTED_IMAGE_MESSAGE` в `test_ads_editor.py` больше нет.
- ⚠️ **Пункт ручного обхода фазы обязан попасть в `12-UAT.md`:** в редакторе объявления с двумя вложениями нажать «×» на первом — плитка исчезает после круга к серверу, набранный текст не теряется, страница не перезагружается; предпросмотр и сводка обновляются тем же ответом. Машинно проверен контракт ОТВЕТА, но не его применение браузером (coverage D8).
- ⚠️ **Вторая строка того же обхода — порог счётчика:** у объявления с вложением набрать текст длиной между порогом подписи и общим порогом и убедиться, что счётчик желтеет на ближайшем нажатии клавиши (coverage D7).

---
*Phase: 12-zagruzka-izobrazheniy-bez-fetch*
*Completed: 2026-09-18*

## Self-Check: PASSED

- `key-files.created` пуст — проверять на диске нечего; все десять файлов `key-files.modified` существуют и присутствуют в диффе `cb60137..HEAD`.
- Коммиты `f451455`, `b12c835`, `a4e0872`, `4995b52` найдены в `git log --oneline --all`; измеренный счёт `git rev-list --count cb60137..HEAD` — 4, и он же записан во frontmatter вместе с `plan_head_before`.
- Все `<acceptance_criteria>` обеих задач перепрогнаны после последней правки: 8 из 8 у задачи 1 (включая оба грепа на ноль и полный прогон), 6 из 6 у задачи 2.
- `<verification>` плана исполнена целиком: полный прогон `3418 passed`, rc=0, 36:42; `uv run python -m compileall -q app main.py tests` молчит, rc=0; `graphify update .` выполнен.
- Запись 90 `.planning/WINDOWS.md` переведена в `fixed` — заглушка, которую этот план обязан был закрыть, закрыта.
