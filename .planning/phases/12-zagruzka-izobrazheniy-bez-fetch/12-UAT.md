---
status: partial
phase: 12-zagruzka-izobrazheniy-bez-fetch
requirement: FETCH-01
source: [12-VERIFICATION.md, 12-REVIEW.md, ROADMAP.md]
created: 2026-09-21
created_by: orchestrator (/gsd-execute-phase 12 --gaps-only, после третьей верификации)
started: 2026-09-21T00:10:00Z
updated: 2026-09-21T08:44:00Z
checks_declared: 8
walkthrough:
  date: 2026-09-21
  browser: "Chrome 152"
  os: "macOS 15"
  observer: "Александр"
  stand: "боевой https://broadcaster.all-torgi.ru"
  checks_marked: [1, 2, 3]
  checks_answered_without_sign: [4, 5, 6, 7, 8]
  divergences: 0
  marks_deferred_to_end: >-
    РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-09-21 (спрошено в обходе): признаки по пунктам, отвечённым голым
    «pass», называются ОДНОЙ РЕПЛИКОЙ В КОНЦЕ обхода, и тогда же заполняются их таблицы.
    До этого момента такие пункты числятся в `checks_answered_without_sign`, а не в `checks_marked`.
  closed_partial: >-
    ВТОРОЕ РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-09-21, спрошенное В КОНЦЕ обхода: признаки по пунктам 4–8
    названы НЕ БЫЛИ (на повторный запрос пришло то же «pass», то есть вердикт, а не наблюдение),
    и обход закрыт как `partial` с ПЯТЬЮ ПУСТЫМИ таблицами. Заполнять их машинной уликой
    ЗАПРЕЩЕНО и не сделано: улика лежит своим разделом, отметки ждут человека. Вернуться к
    пяти пунктам можно в любой момент — `/gsd-verify-work 12`.
  unrecorded: >-
    (а) версия nginx на стенде наблюдателем не названа — см. отметку проверки 2;
    (б) проверки 4, 5, 6, 7 и 8 отвечены «pass» без наблюдённого признака — их таблицы отметок
    НАМЕРЕННО оставлены пустыми и в `checks_marked` не входят.
# ⚠️ ФОРМА ТЕКУЩЕЙ ЭПОХИ, А НЕ ТОЛЬКО ШАБЛОН GSD. Разделы `Current Test`/`Tests`/`Summary`/`Gaps`
# ведёт `/gsd-verify-work`; разделы «Проверка N» с таблицами отметок ставят артефакт ПОД
# `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` (как обходы фаз 9, 10 и 11),
# а не в число изъятых. Терминальное `status` при пустой отметке краснит прогон.
# ⚠️ БЛОК ПРОХОЖДЕНИЯ `walkthrough` ЗАВЕДЁН 2026-09-21, КОГДА ОБХОД НАЧАЛСЯ, — по форме
# ключей `walkthrough`/`walkthrough_2` обходов Фаз 9–11. Ключ `checks_marked` растёт по мере
# приёмки и ОБЯЗАН сойтись с числом заполненных таблиц отметок. Значение `status: passed`
# либо `complete` до заполнения ВСЕХ ВОСЬМИ таблиц ставить ЗАПРЕЩЕНО.
# ⚠️ ВЕРДИКТ ВЕРИФИКАЦИИ — `human_needed`, 41/44, НУЛЬ ПРОВАЛЕННЫХ. Гап G3 закрыт КОДОМ и
# измерен зондом верификатора на обоих порядках, с отрицательным контролем. Эти восемь
# пунктов — то, чего суита не исполняет (рантайм htmx, CSS, боевой прокси), а не остатки гапа.
# ⚠️ 2026-09-21 ПО ВОСЬМИ ПУНКТАМ СНЯТА МАШИННАЯ УЛИКА НА БОЕВОМ СТЕНДЕ (раздел «## Машинная
# улика обхода»). Она ПРИЁМКОЙ НЕ ЯВЛЯЕТСЯ и отметок не заполняет: правило
# `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` требует ЧЕЛОВЕКА с
# наблюдённым признаком, и решение владельца от 2026-09-21 — записать замер уликой, а
# отметки оставить приёмке. Улика нужна, чтобы приёмка опиралась на замер, а не на нуль.
# ⚠️ `result` МЕНЯЕТ ТОЛЬКО ПРИЁМКА, НЕ УЛИКА. Пункты, у которых `result: pass`, закрыты
# наблюдением ЧЕЛОВЕКА (ключ `observed_by` и заполненная таблица отметки), а не машинным
# замером; у остальных `result` остаётся `[pending]` при живой улике. `status` — нетерминальный
# до заполнения ВСЕХ ВОСЬМИ таблиц.
---

## Current Test

[testing paused — 5 items outstanding]

Отвечены все восемь пунктов, закрыты отметкой три (1, 2, 3). Незакрытыми остаются наблюдённые
признаки проверок 4, 5, 6, 7 и 8 — в том числе ДВЕ ОБЯЗАТЕЛЬНЫЕ КЛЕТКИ: состав вложений после
обновления страницы (проверка 7, доказательство закрытия гапа G3) и число меток в
`#media-tombstones` после двух убираний (проверка 8).

## Tests

### 1. Признак «идёт запрос» при выборе файла (критерий 4 ROADMAP)
expected: Индикатор виден в течение запроса, гаснет после подмены полосы.
result: pass
reported: "точка загорелась и погасла"
observed_by: "Александр, Chrome 152 / macOS 15, 2026-09-21"
probe: наблюдён машинно 2026-09-21 (CDP, боевой стенд) — улика 1. НЕ ПРИЁМКА.

### 2. Боевой предел прокси: десять снимков по 4–5 МБ разом
expected: Все десять прикрепляются; 413 в `just prod-logs nginx` нет.
result: pass
reported: "413 в логах нет, все десять прикрепились"
observed_by: "Александр, боевой стенд, 2026-09-21"
probe: наблюдён машинно 2026-09-21 (CDP + логи контейнера прокси) — улика 2. НЕ ПРИЁМКА.

### 3. Кнопка «×» на первом из двух вложений
expected: Плитка исчезает после круга к серверу, набранный текст не теряется, страница не перезагружается, СОСЕДНЯЯ плитка остаётся на месте.
result: pass
reported: "соседняя плитка осталась на месте"
observed_by: "Александр, Chrome 152 / macOS 15, 2026-09-21"
probe: наблюдён машинно 2026-09-21 (CDP, настоящее нажатие) — улика 3. НЕ ПРИЁМКА.

### 4. Порог счётчика символов у объявления с вложением
expected: Счётчик желтеет на ближайшем нажатии клавиши.
result: pass
reported: "pass"
observed_by: "Александр, 2026-09-21 — ПРИЗНАК НЕ НАЗВАН"
mark_outstanding: >-
  Ответ «pass» дан БЕЗ наблюдённого признака и без фактической длины текста, поэтому таблица
  отметки проверки 4 ОСТАВЛЕНА ПУСТОЙ, а не заполнена датой ради зелени правила. Это ровно тот
  случай, что уже был в обходе Фазы 11 (отметки проверок 1, 2, 3 — «ответы „pass“ без
  наблюдённого признака») и потребовал переоткрытия. Пункт ждёт строки наблюдателя.
probe: наблюдён машинно 2026-09-21 (CDP, настоящее нажатие клавиши) — улика 4. НЕ ПРИЁМКА.

### 5. Черновик от первой картинки без единого символа текста
expected: После обновления страницы картинка на месте, черновик создан.
result: pass
reported: "pass"
observed_by: "Александр, 2026-09-21 — ПРИЗНАК НЕ НАЗВАН"
mark_outstanding: >-
  Ответ «pass» без наблюдённого признака (адрес после обновления, судьба картинки не названы).
  Таблица отметки проверки 5 ОСТАВЛЕНА ПУСТОЙ — см. тот же разбор у проверки 4.
probe: наблюдён машинно 2026-09-21 (CDP, обновление страницы) — улика 5. НЕ ПРИЁМКА.

### 6. Пять файлов на два свободных места — время жизни строк отказа
expected: Две плитки появились, три строки отказа названы поимённо И ОСТАЛИСЬ ЧИТАЕМЫ дольше одного круга автосохранения.
result: pass
reported: "pass"
observed_by: "Александр, 2026-09-21 — ПРИЗНАК НЕ НАЗВАН (отложен в конец обхода)"
mark_outstanding: >-
  Ответ «pass» без наблюдённого признака: время жизни строк на экране наблюдателем не названо.
  Таблица отметки проверки 6 ОСТАВЛЕНА ПУСТОЙ до сбора признаков в конце обхода.
probe: наблюдён машинно 2026-09-21 (CDP, замер по времени) — улика 6. НЕ ПРИЁМКА.

### 7. ПОДТВЕРЖДЕНИЕ ЗАКРЫТИЯ ГАПА: убирание поверх летящей загрузки
expected: Ни одного из двух прежних исходов. Убранная плитка НЕ возвращается насовсем; новая плитка НЕ исчезает без сообщения. После обновления — ровно оставшееся вложение плюс загруженное.
result: pass
reported: "pass"
observed_by: "Александр, 2026-09-21 — ПРИЗНАК НЕ НАЗВАН (отложен в конец обхода)"
mark_outstanding: >-
  Ответ «pass» без наблюдённого признака, и здесь это весомее прочего: ОБЯЗАТЕЛЬНАЯ КЛЕТКА
  отметки — состав вложений ПОСЛЕ обновления страницы, то есть ровно то доказательство,
  которым закрывается гап G3. Таблица отметки проверки 7 ОСТАВЛЕНА ПУСТОЙ до сбора признаков
  в конце обхода.
probe: наблюдён машинно 2026-09-21 (CDP, нажатие в полёте загрузки) — улика 7. НЕ ПРИЁМКА.

### 8. Два способа подмены, впервые заведённые в проекте
expected: (а) уходит одна плитка; (б) в узле меток две метки, прежняя НЕ стёрта второй; (в) «+ ФАЙЛ» снова видна и позволяет выбрать файл без перезагрузки.
result: pass
reported: "pass"
observed_by: "Александр, 2026-09-21 — ПРИЗНАК НЕ НАЗВАН (отложен в конец обхода)"
mark_outstanding: >-
  Ответ «pass» без наблюдённого признака. ОБЯЗАТЕЛЬНАЯ КЛЕТКА — число меток в узле
  `#media-tombstones` после ДВУХ убираний; ни она, ни исходы (а) и (в) наблюдателем не названы.
  Таблица отметки проверки 8 ОСТАВЛЕНА ПУСТОЙ до сбора признаков в конце обхода.
probe: наблюдён машинно 2026-09-21 (CDP, настоящие нажатия) — улика 8. НЕ ПРИЁМКА.

---

## Проверка 1 — признак «идёт запрос» (тест 1)

**Что делать.** В редакторе объявления выбрать файл и смотреть на экран до появления плитки.

**Что должно быть видно.** Признак `.form-busy` виден в течение запроса и гаснет после подмены полосы.

**Почему человек.** Видимость — свойство браузера и CSS. Разметочная половина закрыта машинно:
отрендеренная форма несёт `hx-indicator="find .form-busy"`.

### Отметка о закрытии проверки 1

*(заполняет человек на приёмке; отметка без наблюдённого признака закрытием не считается)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| 2026-09-21 | Chrome 152 / macOS 15 | Александр | «точка загорелась и погасла» — оба перехода наблюдены ГЛАЗОМ, чем и закрыта граница 2 машинной улики (там «виден» измерено вычисленным стилем) |

---

## Проверка 2 — боевой предел обратного прокси (тест 2)

**Что делать.** ПОСЛЕ переразвёртывания nginx бросить десять снимков по 4–5 МБ разом; смотреть
`just prod-logs nginx`.

**Что должно быть видно.** Все десять прикрепляются, 413 в логах прокси нет.

**Почему человек.** nginx в среде разработки отсутствует; правка шаблона в гите боевой предел не
двигает. Правило суиты доказывает согласованность ИСХОДНИКОВ шаблонов с `Settings`, а не состояние
работающего прокси. Запись 92 `.planning/WINDOWS.md`, статус `open`.

### Отметка о закрытии проверки 2

*(заполняет человек на приёмке; обязательная клетка — отсутствие 413 в логах прокси)*

| Дата | Стенд / версия nginx | Кто наблюдал | Наблюдённый признак (логи прокси) |
|---|---|---|---|
| 2026-09-21 | боевой `https://broadcaster.all-torgi.ru`; версия nginx наблюдателем НЕ НАЗВАНА | Александр | «413 в логах нет, все десять прикрепились» — обязательная клетка (отсутствие 413) закрыта наблюдением человека |

---

## Проверка 3 — кнопка «×» на первом из двух вложений (тест 3)

**Что делать.** В редакторе объявления с двумя вложениями нажать «×» на первом.

**Что должно быть видно.** Плитка исчезает после круга к серверу; набранный текст не теряется;
страница не перезагружается; **СОСЕДНЯЯ плитка остаётся на месте.**

**Почему человек.** Машинно проверен КОНТРАКТ ответа — блок `hx-swap-oob="delete"` с верным
идентификатором и ноль полей `name="images"`. ПРИМЕНЕНИЕ блока живым документом суита не исполняет:
рантайм htmx в ней не запускается.

### Отметка о закрытии проверки 3

*(заполняет человек на приёмке; обязательная клетка — судьба СОСЕДНЕЙ плитки)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| 2026-09-21 | Chrome 152 / macOS 15 | Александр | «соседняя плитка осталась на месте» — обязательная клетка закрыта дословно; остальные три признака пункта (круг к серверу, целость текста, отсутствие перезагрузки) покрыты вердиктом `pass`, но ОТДЕЛЬНО наблюдателем не названы |

---

## Проверка 4 — порог счётчика символов (тест 4)

**Что делать.** У объявления с вложением набрать текст длиной между порогом подписи и общим порогом.

**Что должно быть видно.** Счётчик желтеет на ближайшем нажатии клавиши.

**Почему человек.** Серверная половина закрыта правилами; переключение порога — переход состояния в
исполняемом JS, которого суита не исполняет.

### Отметка о закрытии проверки 4

*(заполняет человек на приёмке)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| | | | |

---

## Проверка 5 — черновик от первой картинки (тест 5)

**Что делать.** На `/ads/new` прикрепить одну картинку, НЕ вводя ни символа, затем обновить страницу.

**Что должно быть видно.** Картинка на месте, черновик создан.

**Почему человек.** Прямая проверка допущения A2 разведки — всплытие события соседа до тела
документа. Машинной проверки у всплытия нет; машинно закрыт только контракт заголовка
(`HX-Trigger-After-Swap: ads-image-attached`).

### Отметка о закрытии проверки 5

*(заполняет человек на приёмке)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| | | | |

---

## Проверка 6 — время жизни строк отказа (тест 6)

**Что делать.** На `/ads/new` бросить пять файлов на два свободных места и СМОТРЕТЬ НА ЭКРАН дольше
одного круга автосохранения.

**Что должно быть видно.** Две плитки появились; три строки отказа названы поимённо И ОСТАЛИСЬ
ЧИТАЕМЫ.

**Почему человек.** Машинно закрыто, что ответ автосохранения строку не уносит — включения полосы в
нём нет вовсе. Подтвердить ВРЕМЯ ЖИЗНИ строки на экране обязан человек.

### Отметка о закрытии проверки 6

*(заполняет человек на приёмке; обязательная клетка — строки пережили круг автосохранения)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак |
|---|---|---|---|
| | | | |

---

## Проверка 7 — убирание поверх летящей загрузки (тест 7)

**Что делать.** В редакторе с двумя вложениями выбрать крупный файл и, ПОКА идёт загрузка, нажать
«×» на первой плитке. Затем обновить страницу.

**Что должно быть видно.** Ни одного из двух прежних вредоносных исходов: убранная плитка НЕ
возвращается насовсем (мелькнуть на один круг она вправе — это записанный остаток), новая плитка НЕ
исчезает без сообщения. После обновления в объявлении ровно оставшееся вложение плюс загруженное.

**Почему человек.** Пункт ПЕРЕФОРМУЛИРОВАН: прошлая верификация ждала здесь ВРЕДА, эта ждёт его
ОТСУТСТВИЯ. Оба порядка измерены верификатором на уровне HTTP и подтверждены **отрицательным
контролем** (без метки вычитания ключ снова воскресает), но ЧЕРЕДОВАНИЕ двух запросов и применение
внеполосных блоков — свойства браузера.

### Отметка о закрытии проверки 7

*(заполняет человек на приёмке; обязательная клетка — состав вложений ПОСЛЕ обновления страницы)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак (состав после обновления) |
|---|---|---|---|
| | | | |

---

## Проверка 8 — два способа подмены, впервые заведённые в проекте (тест 8)

**Что делать.** Три взгляда:
- **(а)** Нажать «×» — уходит ли РОВНО ОДНА плитка (способ `delete`).
- **(б)** Нажать «×» ДВА раза по разным плиткам и в инспекторе посмотреть узел `#media-tombstones`
  внутри `#ad-form`: накопились ли ДВЕ метки `removed_images` (способ `beforeend` — ДОПИСКА, а не
  подмена).
- **(в)** У объявления НА ПОТОЛКЕ убрать вложение — вернулась ли плитка «+ ФАЙЛ» видимой.

**Что должно быть видно.** (а) уходит одна плитка; (б) в узле меток две метки, прежняя НЕ стёрта
второй; (в) «+ ФАЙЛ» снова видна и позволяет выбрать файл без перезагрузки.

**Почему человек.** Семантика обоих способов сверена верификатором ПО ИСХОДНИКУ вендоренного
рантайма (`app/static/js/htmx.min.js` 2.0.10: `He` собирает цель как `"#" + CSS.escape(id)`, `delete`
уходит в `Ue(n)`, при не-`outerHTML` стиле в цель вставляется СОДЕРЖИМОЕ блока), но **ни один из двух
способов в этом проекте прежде не применялся**, и суита рантайма не исполняет. Замер зубов правила
`beforeend` снят исполнителем ПОДСТАНОВКОЙ, а не браузером, — и это названо в сводке плана 12-12, а
не сглажено.

### Отметка о закрытии проверки 8

*(заполняет человек на приёмке; обязательная клетка — число меток в узле `#media-tombstones` после
ДВУХ убираний)*

| Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак (а / б / в) |
|---|---|---|---|
| | | | |

---

## Машинная улика обхода (CDP, боевой стенд, 2026-09-21)

⚠️ **ЭТО НЕ ОТМЕТКИ И НЕ ПРИЁМКА.** Раздел намеренно не несёт ни одного подзаголовка вида
«### Отметка» и ни одного раздела вида «## Проверка N»: таблицы отметок заполняет ЧЕЛОВЕК, а
объявленное шапкой число проверок — 8, и три счёта правила
`test_the_number_of_walkthrough_checks_agrees_three_ways` обязаны остаться сошедшимися. Здесь
записано ровно то, что наблюдено МАШИНОЙ, с инструментом и его границами, — чтобы приёмка
человека опиралась на замер, а не начиналась с нуля.

**Чем наблюдено.** Живой Chrome (`Macintosh; Intel Mac OS X 10_15_7`, Chrome 152) на БОЕВОМ
стенде `https://broadcaster.all-torgi.ru`, вождение по CDP (`chrome-devtools` MCP). Рантайм
разметки в документе — вендоренный htmx 2.0.10. Обратный прокси — контейнер
`nginx-broadcaster`, поднят за 16 минут до замера (то есть ПОСЛЕ переразвёртывания, чего
требовала предпосылка пункта 2); его логи читаны напрямую. Стенды замера — черновики 68, 69, 70
пользователя «Александр», заведённые замером и по решению владельца ОСТАВЛЕННЫЕ на месте.

⚠️ **ГРАНИЦЫ ИНСТРУМЕНТА, НАЗВАННЫЕ ДО ВЫВОДОВ — их три, и ни одна не сглажена:**

1. **Диалог ОС не участвовал.** Байты файлов собраны В САМОЙ СТРАНИЦЕ (`canvas` →
   `toBlob('image/jpeg')`) и подставлены в `#file-input` через `DataTransfer` + событие
   `change`. Причина названа, а не умолчана: браузер живёт на ДРУГОЙ машине, файловой системы
   которой у наблюдателя нет вовсе — попытка штатной загрузки дала `net::ERR_FILE_NOT_FOUND`.
   Всё, что НИЖЕ выбора файла, при этом настоящее: событие `change`, сбор формы рантаймом,
   составной запрос, обратный прокси, сервер, S3, ответ и подмена узлов.
2. **«Виден» измерено вычисленным стилем.** В улике 1 это `visibility: visible` и
   `opacity: 1` живого узла, а НЕ увиденная глазом точка. Разница существенна ровно для этого
   пункта, и человек на приёмке смотрит именно её.
3. **Нажатия «×» — настоящие нажатия браузера** (CDP click по узлу), а не вызовы `.click()` из
   скрипта. Отсюда следствие, которое стои́т записать: замер способа `beforeend` (улика 8б)
   снят БРАУЗЕРОМ, тогда как замер, названный в сводке плана 12-12, был снят ПОДСТАНОВКОЙ.
   Настоящая улика строже прежней, и прежнюю она не отменяет, а покрывает.

### Улика 1 — признак «идёт запрос»

Стенд: черновик, файл 3.98 МБ (полёт 19 с — намеренно дольше задержки 0.3 s, заложенной CSS).
Писец: `MutationObserver` по классу плюс опрос вычисленного стиля каждые 25 мс.

| мс | класс `.form-busy` | `opacity` | `visibility` |
|---|---|---|---|
| 0 | `form-busy` | 0 | hidden |
| 158 | `form-busy htmx-request` | 0 | hidden |
| 477 | `form-busy htmx-request` | 0.069 | **visible** |
| 602 | `form-busy htmx-request` | **1** | visible |
| 19134 | `form-busy` (класс снят после подмены) | 1 | visible |
| 19276 | `form-busy` | **0** | **hidden** |

Наблюдённый признак: признак ЗАГОРЕЛСЯ в течение запроса и ПОГАС после подмены полосы.
Отдельно измерено и названо: CSS несёт `transition: opacity .12s linear .3s` на
`.form-busy.htmx-request` — то есть на запросах короче ~0.3 s признак не обязан появиться
вовсе, и это свойство разметки, а не отказ. Полоса подменилась, ключ вложения появился.

### Улика 2 — боевой предел обратного прокси

Действующий конфиг РАБОТАЮЩЕГО прокси: `/etc/nginx/conf.d/default.conf:82` —
`client_max_body_size 64M`. Брошено десять снимков **по 4.72 МБ, тело 47.2 МБ, одним
запросом**.

```
2026/09/21 05:32:52 [warn] a client request body is buffered to a temporary file
    /var/cache/nginx/client_temp/0000000010, request: "POST /ads/images HTTP/1.1"
109.196.195.54 - - [21/Sep/2026:05:36:52] "POST /ads/images HTTP/1.1" 200 7350
```

Наблюдённый признак: прикрепились **все десять** (`prod-1.jpg` … `prod-10.jpg`, десять полей
`images`, ни одной видимой строки отказа), ответ 200, полёт 240 с. **413 во всём логе прокси —
НОЛЬ** (`grep -cE "413|too large body"` дал `0`). Пункт объявлен записью 92
`.planning/WINDOWS.md` со статусом `open` — настоящая улика её закрывает по существу.

### Улика 3 — «×» на первом из двух вложений

| признак | до | после |
|---|---|---|
| плитки | `uat-big-1.jpg`, `uat-small-2.jpg` | **`uat-small-2.jpg`** — соседняя на месте |
| набранный текст | 1100 символов | **1100** — не потерян |
| метка живучести окна | `alive-…412946` | **та же** → перезагрузки не было |
| записей `navigation` | 1 | **1** |

Круг к серверу состоялся один: `POST /ads/68/edit` → 200. Внеполосный блок применился адресно
(см. улику 8а). Наблюдённый признак: плитка ушла после круга, соседняя осталась, текст цел,
страница не перезагрузилась.

### Улика 4 — порог счётчика символов

Порог по исходнику: `warnAt = attachedCount() ? CAPTION_LIMIT : TEXT_WARN_AT`, где
`TEXT_WARN_AT = 0.9 × 4096 = 3686`. Вложений на стенде — 2.

- 1099 символов положены в поле БЕЗ события `input` → счётчик `counter`, текст «0 / 4096»,
  жёлтого нет. Это и есть та половина, что доказывает: порог не переключается сам.
- **Одно настоящее нажатие клавиши** → «1100 / 4096», класс `counter counter--warn`,
  вычисленный цвет `oklch(0.82 0.16 85)` (переменная `--warn`).

Наблюдённый признак: счётчик пожелтел НА БЛИЖАЙШЕМ нажатии, и порог сработал на 1100 — то
есть по подписи (1024), а не по общему (3686), и только из-за наличия вложений.

### Улика 5 — черновик от первой картинки без единого символа текста

На `/ads/new` прикреплена одна картинка, в поля не введено НИ ОДНОГО символа. Документ сам
перешёл на `/ads/68/edit`. После обновления страницы: `title=""`, `body=""`, плитка на месте,
миниатюра и полный объект отдаются с S3
(`…/thumbs/1/d3d0789b…_uat-big-1.jpg`). Наблюдённый признак: черновик создан, картинка на
месте. Это прямая улика допущения A2 разведки — всплытие события соседа до тела документа.

### Улика 6 — время жизни строк отказа

8 плиток на стенде, брошено 5 файлов на 2 свободных места.

| мс | плиток | строки отказа |
|---|---|---|
| 0 | 8 | — |
| 2125 | **10** | три, поимённо: `over-3.jpg`, `over-4.jpg`, `over-5.jpg` — «Можно прикрепить не больше 10 вложений. Удалите лишние и попробуйте снова.» |
| 2127 | 10 | **начат круг автосохранения** (`POST /ads/68/edit`) |
| 2263 | 10 | круг осел — **строки на месте** |
| 26064 | 10 | **строки на месте** |

Наблюдённый признак: две плитки появились, три строки названы ПОИМЁННО и ПЕРЕЖИЛИ круг
автосохранения, оставшись читаемыми на t+26 с. Побочно: на потолке «+ ФАЙЛ» получила
`display: none`.

### Улика 7 — убирание поверх летящей загрузки (подтверждение закрытия гапа G3)

Стенд: объявление 69 с двумя вложениями; выбран файл 3.98 МБ; «×» по первой плитке нажат
ПОКА шла загрузка.

| мс | событие | плитки |
|---|---|---|
| 4213 | `POST /ads/images` — загрузка полетела | `t7-first`, `t7-second` |
| 8986 | нажат «×» → `POST /ads/new` | |
| 9091 | адресное удаление + метка дописана | **`t7-second`** |
| **22868** | пришёл ответ загрузки, подмена полосы | `t7-first`, `t7-second`, `t7-inflight` ← мелькнула |
| **22969** | автосохранение по метке | **`t7-second`, `t7-inflight`** |

Убранная плитка вернулась ровно на **101 мс — один круг**, то есть в точности тот записанный
остаток, который пункт разрешает («мелькнуть на один круг она вправе»), и НЕ насовсем. Новая
плитка НЕ исчезла и сообщения об отказе не потребовалось.

**Серверная истина после обновления страницы:** ровно `t7-second.jpg` + `t7-inflight.jpg`, два
поля `images`, меток вычитания — нуль. Наблюдённый признак: НИ ОДНОГО из двух прежних
вредоносных исходов; состав вложений — оставшееся плюс загруженное.

### Улика 8 — два способа подмены, впервые заведённые в проекте

**(а) `delete`.** В журнале событий рантайма: `htmx:oobBeforeSwap` с целью
`media-item-df53cdd09b304591` — адресное удаление РОВНО ОДНОЙ плитки по её идентификатору,
2 → 1 при неподвижной соседке.

**(б) `beforeend` — ДОПИСКА, а не подмена.** После второго нажатия «×» узел
`#media-tombstones` нёс:

```html
<span id="media-tombstones" hidden="">
  <input type="hidden" name="removed_images" value="1/d3d0789b…_uat-big-1.jpg">
  <input type="hidden" name="removed_images" value="1/1b7ba333…_uat-small-2.jpg">
</span>
```

Две метки; **прежняя НЕ стёрта второй**. Третье убирание дало три. Замер снят настоящими
нажатиями браузера — см. границу 3 выше.

**(в) Возврат «+ ФАЙЛ» с потолка.** На потолке (10 вложений) — `display: none`. После
убирания одного: 9 плиток, `display: flex`, `visibility: visible`, атрибут `for="file-input"`
на месте. Через неё ПРИКРЕПЛЁН новый файл: 9 → 10, отказов нет, записей `navigation` по-прежнему
**1** и метка живучести окна та же — перезагрузки не было ни в одном из шагов улики 8.

### Чего настоящая улика НЕ касается

Открытое решение владельца **WR-10** (несущая единственность ключа в `ad.images`) этим замером
не тронуто и тронуто быть не могло: путь к нему — рукотворный запрос с повторённым полем
`images`, а интерфейс такого не печатает. Развилка остаётся за владельцем в том же виде, в
каком записана ниже.

---

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

⚠️ СЧЁТ `passed` И ЧИСЛО ЗАПОЛНЕННЫХ ОТМЕТОК ЗДЕСЬ РАСХОДЯТСЯ, И ЭТО ЗАПИСЬ, А НЕ ОПИСКА:
пройденных ответом — 8, закрытых отметкой — 3. Пункты 4, 5, 6, 7 и 8 отвечены голым «pass» без
наблюдённого признака, их таблицы оставлены ПУСТЫМИ (ключ `checks_answered_without_sign` шапки);
по решению владельца признаки называются одной репликой В КОНЦЕ обхода. Терминальное `status`
при этом расхождении краснит правило самозаверения — и обязано краснить.

Обход НАЧАТ 2026-09-21: пункты 1, 2 и 3 пройдены человеком (Александр, Chrome 152 / macOS 15),
таблицы их отметок заполнены наблюдёнными признаками. Пункт 2 закрывает по существу запись 92
`.planning/WINDOWS.md` (боевой предел прокси) — саму запись надлежит перевести отдельным действием,
и до тех пор она остаётся `open`. `pending: 0` означает, что ОТВЕЧЕНЫ все восемь, и НЕ означает,
что все восемь ЗАКРЫТЫ: закрытых отметкой — три, и эта разница здесь стои́т правдой, а не
забывчивостью.

По всем восьми 2026-09-21 снята МАШИННАЯ УЛИКА на боевом стенде
(раздел «## Машинная улика обхода»): признак наблюдён у восьми из восьми, противоречий ожиданиям
не найдено ни одного, гапов улика не открыла. Приёмкой это не является по решению владельца от
2026-09-21 и по правилу `test_the_walkthrough_cannot_self_certify.py`. Приёмка человеком 2026-09-21
дала `pass` по всем восьми, но наблюдённый признак назван лишь по трём, поэтому `status` остаётся
НЕТЕРМИНАЛЬНЫМ, а пять таблиц отметок — пустыми и ждущими одной реплики наблюдателя.

Вердикт верификации — `human_needed` при 41/44 и **нуле проваленных
истин**: гап G3 закрыт кодом на обоих порядках зонда WR-07 и подтверждён отрицательным контролем.
Восемь пунктов ниже — не остатки гапа, а то, чего суита не исполняет: рантайм htmx, CSS и боевой
обратный прокси.

Пункты 1, 2, 4 — перенесённые `behavior_unverified` истины прошлых верификаций, в рамку этой партии
не входившие. Пункты 3, 5, 6, 7 — перенесённые пункты обхода, из них пункт 7 ПЕРЕФОРМУЛИРОВАН (ждёт
отсутствия вреда, а не вреда). Пункт 8 — НОВЫЙ, заведён этой партией.

`FETCH-01` остаётся `Gaps Found` и движется только `passed`-вердиктом: правило
`tests/test_planning/test_requirement_completion_follows_verification.py` этого требует, и план 12-13
проверил его на себе — поставил отметку, увидел покраснение, откатил отметку, а не правило.

## Gaps

Гапов нет: проваленных истин нуль.

**Открытым решением владельца остаётся WR-10** — не гап, а развилка, и она вне этого обхода.
Адресное снятие сделало единственность ключа в `ad.images` несущей, а обеспечивать её нечем.
Измерено верификатором по всей цепочке: дубль записывается ОДНИМ запросом (200, `dup stored: True`),
редактор печатает ДВА узла с одним идентификатором, `querySelectorAll` вендоренного рантайма
возвращает оба, `delete` применяется к каждому, сервер снимает одно вхождение — и следующее
автосохранение пишет `ad.images = []`. Одно нажатие «×», два вложения исчезли, без сообщения.

Достижимо только рукотворным запросом с повторённым полем `images` (интерфейс такого не печатает:
ключи — `uuid4().hex`, одно скрытое поле на плитку), поэтому и верификатор, и независимое ревью
ставят ⚠️ Warning, а не блокер. Но в отличие от прочих задетых запретов у этого пути **нет ни
починки, ни записи**, а эта фаза такого не допускала ни разу. Выбор двоичный и принадлежит владельцу:
сделать различность свойством единственного предиката принадлежности — либо завести
`accepted-assumption` в форме D-16/D-17.
