---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 06
type: tracer
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [FETCH-01]
files_modified:
  - app/services/image_keys.py
  - app/pages/ads.py
  - tests/test_pages/test_ads_image_upload.py
  - tests/test_services/test_image_keys.py

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Партия «свой ключ + чужой ключ» отвечает фрагментом, в котором СВОЙ ключ присутствует скрытым полем `name=\"images\"`, а чужой отсутствует (гап 1, CR-02)."
    - "Скрытые поля, пережившие отказ, есть ровно те ключи, которые следующее сохранение объявления записывает в `Ad.images` — свойство измеряется ДВУМЯ обработчиками, сведёнными в одном правиле."
    - "Одиннадцать СВОИХ законных ключей при `max_images_per_ad`=10 не стираются: полоса сохраняет все одиннадцать плиток, а входящие файлы получают строку про потолок (достижимость БЕЗ нападающего, зонд Б верификатора)."
    - "Свойство «работа ради подделанного запроса не делается» сохранено: при любом негодном значении в списке ключей файловые части не читаются и в хранилище не пишутся."
    - "Вторая половина текста FETCH-01 — «успешно загруженные файлы остаются прикреплёнными, даже если часть отвалилась» — держится на пути отказа СВЕРКИ КЛЮЧЕЙ, а не только на пути отказа ФАЙЛУ."
  artifacts:
    - "app/services/image_keys.py — `partition_own_image_keys`, единственный предикат принадлежности ключа, через который выражен и `own_image_keys`"
    - "app/pages/ads.py — ветвь отказа сверки в `ads_images_upload` строит полосу из ПОДТВЕРЖДЁННОГО подмножества, а не из пустого списка"
    - "tests/test_pages/test_ads_image_upload.py — `test_an_own_key_survives_a_foreign_key_in_the_same_batch`, `test_the_keys_that_survive_a_refusal_are_the_ones_the_next_save_persists`, `test_own_keys_over_the_ceiling_are_not_erased_by_a_new_batch`"
    - "tests/test_services/test_image_keys.py — `test_partition_keeps_order_and_separates_the_foreign`"
  key_links:
    - "`partition_own_image_keys` → `own_image_keys`: один предикат, два вызывающих; второго места, решающего «мой ли это ключ», в дереве нет (D-07, Hyrum)"
    - "скрытое поле `name=\"images\" form=\"ad-form\"` фрагмента загрузки → `_save_from_editor` через `form_data.getlist(\"images\")`: связь замыкается ОДНИМ правилом суиты, а не двумя зелёными по отдельности"
  assumptions:
    - statement: "Ключ, УЖЕ лежащий в `Ad.images` и не проходящий сегодняшний образец `_IMAGE_KEY_PATTERN`, этой партией НЕ спасается: он уходит в `offending`, теряет плитку, и следующее сохранение записывает объявление без него. Это пункт (б) второго `missing` первого гапа, и он назван ОСТАТКОМ, а не закрыт. Обоснование отказа от каждой из трёх альтернатив — в `<objective>` и в комментарии, который заводит задача 2."
      source: planner
      requirement_id: FETCH-01
      category: data-migration
      status: unresolved
      decision: D-07
  prohibitions:
    - statement: "Отказ не отвечает авторитетно выглядящей ПУСТОЙ полосой: ответ, который выглядит как полное состояние вложений, но собран из неполной истины, уничтожает работу человека молча."
      status: unverified
      verification: none

security:
  asvs_level: 1
  block_on: high
---

<objective>
Закрыть гап 1 верификации (`status: partial`, критерий 2 ROADMAP) и находку ревью `CR-02`: ветвь
отказа сверки ключей перестаёт отвечать пустой полосой и начинает отвечать полосой из
ПОДТВЕРЖДЁННОГО подмножества.

Purpose: сегодня попытка загрузки с одним негодным значением в списке ключей ТИХО отцепляет
законные вложения — фрагмент приходит с `image_keys=[]`, подмена `innerHTML` выносит из документа
все скрытые поля, и следующее автосохранение записывает `ad.images = []`. До фазы негодный ключ
ГРОМКО блокировал сохранение. Это и есть невыполненная вторая половина FETCH-01.

Output: единственный предикат принадлежности в `app/services/image_keys.py`, переписанная ветвь
отказа в `ads_images_upload`, и правило суиты, которое СВОДИТ ответ загрузки с последующим
сохранением — то, чего в суите не было ни одного и из-за чего три дефекта прошли зелёными.

⚠️ ОСТАТОК, НАЗВАННЫЙ, А НЕ ЗАКРЫТЫЙ — пункт (б) второго `missing` первого гапа. Второй `missing`
называет ДВЕ достижимости без нападающего. Первая — (а), потолок, опущенный ниже числа уже
прикреплённых, — закрывается задачей 2. Вторая — (б), ключ, уже лежащий в `Ad.images` и образца
`_IMAGE_KEY_PATTERN` не проходящий, потому что запись вложений не перезаписывается никогда
(`app/services/image_upload.py:371-374`), — выбранной формой исправления НЕ спасается: такой ключ
приезжает скрытым полем наравне с подделанным, уходит в `offending`, теряет плитку, и следующее
сохранение записывает объявление без него. Сдвиг назван верификатором дословно: прежде такой ключ
ГРОМКО ронял сохранение, теперь попытка загрузки его отцепляет — не молча (строка отказа с
`INACCESSIBLE_IMAGE_MESSAGE` приходит и после плана 12-07 переживает автосохранение), но и не
адресно: `display_name` у неё пуст, и КАКОЕ именно вложение отцеплено, она не называет.

Почему все три альтернативы отвергнуты на существе, а не по трудоёмкости:
- **расширить образец** под прежнюю форму имени — снять ровно тот рубеж, который держит
  `T-12-06-01`: образец и есть единственное доказательство формы ключа, и значение вида
  `"7/<32 hex>_a.png\n"` проходило бы снова, а оттуда попадало в адрес изображения и в адаптеры
  мессенджеров (основание выписано в `app/services/image_keys.py:28-32`);
- **спросить у базы**, лежит ли присланный ключ в `Ad.images` — маршрут загрузки привязан к
  ПОЛЬЗОВАТЕЛЮ, а не к черновику (D-02; докстринг `ads_images_upload`: «адреса конкретного
  редактора маршрут не знает вовсе»), поэтому спрашивать не у кого; а второй авторитет по
  принадлежности разошёлся бы с первым (D-07, Hyrum) — тот самый Hyrum, ради которого вся эта
  задача сводит предикат в ОДНО место;
- **переписать `Ad.images`** переносом данных — вне рамки партии, назначенной владельцем
  (§«Рамка партии» `12-CONTEXT.md`), и против действующей записи «Уже сохранённые ключи не
  переименовываются» (`app/services/image_upload.py:372-374`).

И то, что делает остаток переносимым: СЕГОДНЯ, на дереве с уже сданными планами 12-01…12-05, такой
ключ делает объявление НЕРЕДАКТИРУЕМЫМ — `own_image_keys` на сохранении (`app/pages/ads.py:555`)
роняет каждое автосохранение кодом 400. Остаток меняет форму уже сломанного состояния и даёт из
него выход («добавьте изображение заново» — совет, который в этом случае работает дословно), а не
ломает исправное.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: Сквозной срез — свой ключ переживает чужой в одной партии и доезжает до базы</name>

  <read_first>
    - `app/services/image_keys.py` целиком — `own_image_keys` (:86), `_IMAGE_KEY_PATTERN`, `INACCESSIBLE_IMAGE_MESSAGE`
    - `app/pages/ads.py:792-941` — обработчик `ads_images_upload`, ветвь `except HTTPException` (:838-851), сборщик `_media_strip_fragment` (:890-937)
    - `app/pages/ads.py:499-600` — `_save_from_editor`: потребитель скрытых полей, `own_image_keys` на сохранении (:543)
    - `tests/test_pages/test_ads_image_upload.py:1-145` — шапка модуля, `HIDDEN_KEY_FIELD`, `refusal_rows`, `image_key`, фикстура `owner`
    - `tests/test_pages/test_ads_image_upload.py:472-522` — `test_a_foreign_key_is_not_echoed_back`: правило, которое ДОПОЛНЯЕТСЯ соседом, а не переписывается
    - `tests/test_services/test_image_keys.py` — форма модуля без клиентских фикстур
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md` — гап 1: зонды А и Б верификатора
  </read_first>

  <files>app/services/image_keys.py, app/pages/ads.py, tests/test_pages/test_ads_image_upload.py, tests/test_services/test_image_keys.py</files>

  <behavior>
    - `partition_own_image_keys(values, user_id)` возвращает пару списков `(own, offending)`: `own` — значения, прошедшие `_IMAGE_KEY_PATTERN.fullmatch` И сравнение первой группы со строкой `user_id`, в ИСХОДНОМ порядке; `offending` — все остальные, тоже в исходном порядке.
    - `own_image_keys(values, user_id, max_images)` поведения НЕ меняет: тот же отказ 400 по длине, тот же отказ 400 `INACCESSIBLE_IMAGE_MESSAGE` при первом негодном значении, тот же возвращаемый список. Меняется только то, что предикат принадлежности берётся из `partition_own_image_keys`, а не написан вторым разом.
    - Запрос `POST /ads/images` с `images=[свой_ключ, чужой_ключ]` и одной годной файловой частью отвечает 200; во фрагменте `свой_ключ` присутствует скрытым полем `name="images" … form="ad-form"`, `чужой_ключ` отсутствует в тексте ответа вовсе; строк отказа ровно одна, её причина содержит «недоступно»; `mock_s3.call_count == 0`.
    - Скрытые поля из этого фрагмента, отправленные следующим запросом в `POST /ads/{id}/edit`, дают `Ad.images == [свой_ключ]`, а не `[]`.
    - Запрос только с чужим ключом (существующее правило) поведения не меняет: скрытых полей ноль, потому что подтверждённое подмножество пусто.
  </behavior>

  <action>
    В `app/services/image_keys.py` завести `partition_own_image_keys(values: list[str], user_id: int) -> tuple[list[str], list[str]]` и выразить через неё цикл проверки внутри `own_image_keys`: предикат «этот ключ мой» обязан существовать в дереве РОВНО ОДИН раз. Отказ по длине в `own_image_keys` остаётся на своём месте и первым — контракт сохранения не двигается ни на символ (это последний рубеж владения, и ослаблять его нечем). Докстринг новой функции называет, ЗАЧЕМ нужна разделяющая форма: вызывающему на загрузке нужно не «всё или ничего», а «что подтверждено, то остаётся на экране».

    В `app/pages/ads.py`, обработчик `ads_images_upload`: снять вызов `own_image_keys` с ветви `try` и заменить его разделением. Новая форма ветвления — `image_keys, offending = partition_own_image_keys(...)`; при непустом `offending` (либо при нестроковой части под именем `images`) в `refusals` уходит ОДНА строка с `INACCESSIBLE_IMAGE_MESSAGE` и `display_name=""`, а файловые части не обрабатываются ВОВСЕ. Свойство «работа ради подделанного запроса не делается» несущее: его держит `mock_s3.call_count == 0` соседнего правила, и терять его нельзя. Значение `image_keys` при этом равно подтверждённому подмножеству, а не пустому списку.

    Комментарий-обоснование ветви (`app/pages/ads.py:838-851`) переписать по идиоме проекта «прежняя редакция НАЗЫВАЕТСЯ, а не стирается»: сохранить действующее рассуждение про код 400 и отсутствие подмены на четырёхсотом, а абзац «Список ключей уходит ПУСТЫМ» пометить как снятую редакцию с причиной снятия — полоса после плана 12-03 есть ЕДИНСТВЕННЫЙ источник вложений в документе, поэтому пустой список есть не осторожность, а стирание. Новая формулировка называет, что на экране остаётся подтверждённое, а отвергается только виновное.

    В `tests/test_services/test_image_keys.py` добавить `test_partition_keeps_order_and_separates_the_foreign`: смешанный вход из своего, чужого и синтаксически негодного значения; проверяются оба списка и сохранение порядка.

    В `tests/test_pages/test_ads_image_upload.py` добавить два правила РЯДОМ с `test_a_foreign_key_is_not_echoed_back`, не трогая его тела: `test_an_own_key_survives_a_foreign_key_in_the_same_batch` (смешанная партия, утверждения из блока behavior) и `test_the_keys_that_survive_a_refusal_are_the_ones_the_next_save_persists` — правило на ДВА обработчика: ответ загрузки разбирается `HIDDEN_KEY_FIELD.findall`, найденные значения отправляются в `POST /ads/{ad_id}/edit` заголовками htmx, и `Ad.images` перечитывается из `db_session`. Форму запроса к редактору взять у `tests/test_pages/test_ads_editor.py` (`form_body`, `HX_HEADERS`, `_seed_ad`) — ввозить помощники из соседнего тестового модуля нельзя, повторить их форму по месту, как это уже сделано в шапке файла.

    Заодно (тот же файл, одна строка): ссылка в комментарии на :127, указывающая типизированным путём с номерами строк на модуль тестов прежнего JSON-маршрута загрузки, заменяется описанием источника СЛОВАМИ — модуль снят планом 12-05 целиком. Это первая из трёх ссылок находки `IN-01`; остальные две закрывает план 12-10. ⚠️ Снятый путь не набирается в новой редакции комментария дословно, даже летописью: приёмочный обход находки ищет его по дереву `tests/`, и дословное упоминание удовлетворило бы обход само.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_services/test_image_keys.py tests/test_pages/test_ads_editor.py -q -p no:randomly</automated>
    <fails_when>ненулевой код возврата, либо строка итога содержит "failed", либо в итоге меньше трёх новых имён — прогон с `-k "survives or persists or partition"` даёт "0 passed"</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py::test_a_foreign_key_is_not_echoed_back -q</automated>
    <fails_when>ненулевой код возврата или "0 passed" в итоге — существующее правило было переписано вместо дополнения</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "def partition_own_image_keys" app/services/image_keys.py` не меньше 1.
    - В теле `own_image_keys` вызывается `partition_own_image_keys`; второго написания сравнения `match.group(1) != str(user_id)` в `app/services/image_keys.py` нет.
    - В срезе обработчика `ads_images_upload` (`awk '/^async def ads_images_upload/,/^@router/' app/pages/ads.py`) присутствует `partition_own_image_keys` и отсутствует присваивание `image_keys: list[str] = []` как источник ответа — инициализация допустима, но ветвь отказа присваивает подтверждённое подмножество.
    - `test_a_foreign_key_is_not_echoed_back` в `tests/test_pages/test_ads_image_upload.py` сохраняет все пять своих утверждений дословно, включая `mock_s3.call_count == 0`.
    - `uv run pytest tests/test_pages/test_ads_image_upload.py::test_the_keys_that_survive_a_refusal_are_the_ones_the_next_save_persists -q` зелено, и утверждение сравнивает `Ad.images` с непустым списком.
  </acceptance_criteria>

  <done>Смешанная партия «свой + чужой» отдаёт фрагмент со своим ключом, чужой отвергнут строкой, в хранилище не ушло ни одной записи, и следующее сохранение объявления записывает свой ключ, а не пустой список.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Потолок не стирает уже прикреплённое — путь БЕЗ нападающего</name>

  <read_first>
    - `app/pages/ads.py:852-888` — расчёт `free` и цикл по частям (после правки задачи 1)
    - `app/services/image_keys.py` — отказ по длине в `own_image_keys` (остаётся нетронутым)
    - `app/services/image_upload.py:162-180` — `upload_limit_message`
    - `tests/test_pages/test_ads_image_upload.py:373-435` — `test_ceiling_takes_the_free_slots_and_refuses_the_rest`: образец формы правила про потолок
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md` — гап 1, ВТОРОЙ `missing` целиком: он называет ДВЕ достижимости без нападающего — (а) потолок ниже числа прикреплённых (закрывается здесь) и (б) ключ прежней формы в `Ad.images` (записывается остатком, см. `<objective>`)
    - `app/services/image_upload.py:365-375` — запись «Уже сохранённые ключи не переименовываются»: причина, по которой (б) существует и переносом данных здесь не снимается
  </read_first>

  <files>app/pages/ads.py, tests/test_pages/test_ads_image_upload.py</files>

  <behavior>
    - При `max_images_per_ad = 10` и одиннадцати СВОИХ синтаксически годных ключах в запросе фрагмент несёт одиннадцать скрытых полей `name="images"` — ни один ключ не потерян.
    - Свободных мест при этом ноль: каждая присланная файловая часть получает строку `upload_limit_message(10)`, `mock_s3.call_count == 0`.
    - Плитка «+ ФАЙЛ» скрыта (условие `image_keys | length >= max_images` уже в шаблоне).
    - Заголовок `HX-Trigger-After-Swap` не ставится: не принят ни один файл.
    - Прежнее правило `test_ceiling_takes_the_free_slots_and_refuses_the_rest` остаётся зелёным без правки.
  </behavior>

  <action>
    В `app/pages/ads.py` расчёт свободных мест привести к форме `free = max(0, settings.max_images_per_ad - len(image_keys))`. Обоснование записать комментарием рядом и записать его ПРЕДМЕТНО: потолок управляет тем, сколько НОВЫХ файлов принимается, и не имеет права управлять судьбой УЖЕ прикреплённых. Отрицательное значение прежде означало «прикреплено больше потолка», и на этой ветви сверка поднимала отказ по длине, который уносил с экрана все одиннадцать законных ключей. Достижимость названа поимённо: `max_images_per_ad`, опущенный ниже числа уже прикреплённых у живого объявления.

    Отказ по длине в `own_image_keys` НЕ трогается: на сохранении он и есть настоящая квота, и ослабление его было бы ослаблением рубежа владения. Разница между двумя вызывающими называется комментарием в обработчике: сохранение решает, что записать в базу, и обязано отказать; загрузка решает, что показать на экране, и обязана сохранить показанное.

    Второго инструмента расчёта потолка не заводится (D-07, Hyrum): значение по-прежнему одна строка из результата разделения, и `own_image_keys` остаётся единственным местом, где потолок ОТКАЗЫВАЕТ.

    ⚠️ ВТОРАЯ ДОСТИЖИМОСТЬ БЕЗ НАПАДАЮЩЕГО ЗАПИСЫВАЕТСЯ ОСТАТКОМ ТАМ ЖЕ, ГДЕ ЗАКРЫВАЕТСЯ ПЕРВАЯ. Тем же комментарием, рядом с расчётом свободных мест, дописать абзац про пункт (б) того же `missing`: ключ, уже лежащий в `Ad.images` и образца ключа не проходящий, попадает в `offending` наравне с подделанным — различить их на ЭТОМ входе нечем, оба приезжают скрытым полем документа. Исход назвать прямо: плитка исчезает, строка отказа приходит без имени файла, следующее сохранение записывает объявление без такого ключа. Назвать и то, чем это было ДО партии: сверка на сохранении роняла каждое автосохранение кодом 400, то есть объявление не редактировалось вовсе, — остаток меняет форму уже сломанного состояния и даёт из него выход, а не ломает исправное. Назвать, ПОЧЕМУ здесь не чинится: расширение образца сняло бы рубеж владения, спросить у базы не у кого (маршрут привязан к пользователю, а не к черновику, D-02), перенос данных вне рамки партии. Три довода — в трёх строках, без пересказа `<objective>`.

    В `tests/test_pages/test_ads_image_upload.py` добавить `test_own_keys_over_the_ceiling_are_not_erased_by_a_new_batch`: фикстура `test_settings` опускает `max_images_per_ad` до 10, в запрос уходят одиннадцать ключей, построенных `image_key(owner.id, …)`, и одна годная файловая часть. Утверждения — из блока behavior, с адресными сообщениями в форме соседних правил модуля (что увидит человек, а не повтор выражения).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py -q -k "ceiling or over_the_ceiling"</automated>
    <fails_when>ненулевой код возврата, либо "0 passed" в итоге, либо в итоге меньше двух собранных правил</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_services/test_image_keys.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
  </verify>

  <acceptance_criteria>
    - В срезе обработчика `ads_images_upload` расчёт свободных мест записан через `max(0, …)`.
    - `own_image_keys` в `app/services/image_keys.py` сохраняет отказ по длине дословно — `len(values) > max_images` поднимает `HTTPException` с кодом 400.
    - `uv run pytest tests/test_pages/test_ads_image_upload.py::test_own_keys_over_the_ceiling_are_not_erased_by_a_new_batch -q` зелено, и правило утверждает ровно одиннадцать совпадений `HIDDEN_KEY_FIELD`.
    - `test_ceiling_takes_the_free_slots_and_refuses_the_rest` не правлен и зелен.
    - Остаток (б) записан в срезе обработчика: `awk '/^async def ads_images_upload/,/^@router/' app/pages/ads.py | grep -cF 'Ad.images'` не меньше 1. До этой задачи в срезе не было ни одного вхождения (замерено), поэтому признак меряет новую запись, а не старую.
  </acceptance_criteria>

  <done>Одиннадцать законных ключей при потолке десять остаются на экране и в документе; входящие файлы получают строку про потолок; в хранилище не ушло ничего.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → `POST /ads/images` | список текущих ключей приезжает скрытыми полями документа и подконтролен отправителю |
| ответ-фрагмент → документ | подмена `innerHTML` цели `#media-strip` переписывает состояние вложений формы объявления целиком |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06-01 | Tampering | `ads_images_upload`, список `images` | high | mitigate | `partition_own_image_keys` сверяет КАЖДОЕ значение с образцом ключа и префиксом пользователя до того, как оно попадёт во фрагмент; чужое значение во фрагмент не переиздаётся |
| T-12-06-02 | Denial of Service | `ads_images_upload`, файловые части при подделанном ключе | medium | mitigate | при непустом `offending` файловые части не читаются и в хранилище не пишутся — работа ради подделанного запроса не делается, объектов-сирот не возникает |
| T-12-06-03 | Tampering | ответ-фрагмент как источник состояния формы | high | mitigate | отказ отвечает подтверждённым подмножеством; пустая полоса как «авторитетное» состояние снята — именно она приводила к `ad.images = []` следующим автосохранением |
| T-12-06-04 | Information disclosure | строка отказа | low | accept | причина берётся из закрытого набора констант (`INACCESSIBLE_IMAGE_MESSAGE`), значение клиента в текст не подставляется |
</threat_model>

## Артефакты, которые этот план производит

Новые символы, имена и тексты, впервые появляющиеся здесь (перечень нужен проходу сверки
источников, чтобы новые имена не были приняты за расхождение):

- `app/services/image_keys.py`: функция `partition_own_image_keys`
- `tests/test_services/test_image_keys.py`: правило `test_partition_keeps_order_and_separates_the_foreign`
- `tests/test_pages/test_ads_image_upload.py`: правила `test_an_own_key_survives_a_foreign_key_in_the_same_batch`, `test_the_keys_that_survive_a_refusal_are_the_ones_the_next_save_persists`, `test_own_keys_over_the_ceiling_are_not_erased_by_a_new_batch`

Новых файлов, маршрутов, шаблонов, заголовков ответа и текстов для человека план не заводит.

## Запись прохода по запретам (spec-less fallback, §B)

Отбор шёл по требованию FETCH-01. Сырых кандидатов — 13; после ступени точности оставлено 4, и
каждый авторизован РОВНО В ОДНОМ плане партии: здесь — «отказ не отвечает пустой полосой»; в 12-07 —
«выбранный человеком файл не пропадает молча»; в 12-08 — «замеряющее правило инвертируется вверх»;
в 12-09 — «рамка партии, назначенная владельцем, не расширяется молча».

Снято по канон-отсылке (хлебные крошки, запреты не чеканятся): межсайтовая подделка изменяющего
запроса — OWASP / ASVS V4.2.2, и она взята РАБОТОЙ по D-15 в плане 12-10; обход каталога в имени
присланного файла — OWASP, закрыт `safe_filename` и правилом `test_a_rejected_name_is_normalised_before_it_is_shown`.

Снято как рядовая инженерия: гигиена текста исключения в ответе, выбор «чинить источник, а не
приёмник», отсутствие нового клиентского кода, дословность переносимых комментариев. Восемь
запретов планов 12-01…12-05 со `verification: none` НЕ перечеканиваются — решение владельца D-18
принимает их в том виде, в каком их прочёл верификатор.

<verification>
- Смешанная партия «свой + чужой» отдаёт фрагмент со своим ключом (зонд А верификатора инвертирован).
- Одиннадцать своих ключей при потолке десять остаются на экране (зонд Б верификатора инвертирован).
- Вторая достижимость того же `missing` — ключ прежней формы в `Ad.images` — названа ОСТАТКОМ в трёх местах плана (frontmatter `assumptions`, `<objective>`, комментарий задачи 2) и не выдана за закрытую.
- Ответ загрузки и последующее сохранение объявления сведены ОДНИМ правилом суиты — раньше таких правил не было ни одного.
- Прогон `uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_services/test_image_keys.py tests/test_pages/test_ads_editor.py -q` зелен.
</verification>

<success_criteria>
- Вторая половина текста FETCH-01 держится на пути отказа сверки ключей, а не только на пути отказа файлу.
- Ни одно существующее правило суиты не ослаблено и не удалено; `test_a_foreign_key_is_not_echoed_back` дополнено соседом.
- Предикат принадлежности ключа существует в дереве один раз.
</success_criteria>

<output>
Создать `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-06-SUMMARY.md` по завершении.
</output>
