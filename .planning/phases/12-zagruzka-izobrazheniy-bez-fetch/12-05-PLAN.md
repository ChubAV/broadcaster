---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 05
type: execute
wave: 4
depends_on: ["12-04"]
files_modified:
  - tests/test_services/test_image_upload.py
  - tests/test_pages/test_ads_image_upload.py
  - tests/test_routes/test_uploads.py
  - app/routes/uploads.py
  - app/main.py
  - README.md
  - tests/test_pages/test_access_gate.py
  - tests/test_pages/test_impersonation_gate.py
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [FETCH-01]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` вернул sample_count 0, factor 1;
  # confidence выведен из нуля замеров, а не самооценкой.
  # Превышение бюджета 100k ПРИНЯТО осознанно — основания в §scope_acceptance ниже.
  # Это самый тяжёлый план фазы: задача 1 читает 1099 строк целиком и неделимо.
  tokens: 125000
  raw_tokens: 125000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`app/routes/uploads.py` снят ЦЕЛИКОМ: в `app/routes/` стало на один модуль меньше, адреса `/api/uploads/image` больше не существует, и в `README.md` строки о нём нет (D-03, D-13)"
    - "1099 строк утверждений суиты ПЕРЕЕХАЛИ, а не исчезли: утверждения о сервисе (распознавание, нормализация имени, приведение расширения, порядок отказов, порционное чтение) живут в `tests/test_services/test_image_upload.py`, утверждения о маршруте — в `tests/test_pages/test_ads_image_upload.py` (G-8)"
    - "Решение о поверхности маршрута отражено во ВСЕХ ТРЁХ перечнях гейтов, а не в двух: `GATED_API_ROUTERS` 5 → 4, `BLOCK_CHECKED_API_ROUTERS` 5 → 4 и запись гейта подстановки личности перенацелена на новый обработчик (критерий 3, D-01, G-7)"
    - "Два изъятия, адресованных Фазе 12 сводной летописью Фазы 9, ПЕРЕАДРЕСОВАНЫ ЗАПИСЬЮ с названной причиной, а не сняты молча и не оставлены указывать на эту фазу (OQ-3)"
    - "Ни одного места, подменяющего запись объекта по прежнему адресу модуля, в суите не осталось: цель подмены — имя нового сервисного модуля, и промах даёт осмысленный отказ, а не ошибку атрибута (G-8)"
  artifacts:
    - "tests/test_services/test_image_upload.py — переехавшие утверждения о сервисе"
    - "tests/test_pages/test_access_gate.py — оба перечня по четыре роутера"
    - "tests/test_pages/test_impersonation_gate.py — запись нового обработчика загрузки с прежней причиной"
    - "tests/test_templates/test_htmx_markup_gates.py — переадресованные записи двух перечней исключений"
  key_links:
    - "`app/main.py` (подключение роутера загрузки) → снятый модуль: ломается — приложение не собирается вовсе"
    - "решение о поверхности маршрута → три перечня гейтов: ломается — маршрут, не попавший ни в одно множество, роняет гейт по построению (заявленное свойство формы)"
  prohibitions:
    - statement: "перечни гейтов не подгоняются ради зелени: решение о том, КУДА переезжает маршрут, принято по конвенции проекта, и все три перечня ОТРАЖАЮТ его — критерий 3 ROADMAP говорит это дословно"
      status: flagged-unverified
      verification: none
    - statement: "чужое изъятие не снимается молча и не остаётся указывать на закрывшуюся фазу — диспозиция ЗАПИСЫВАЕТСЯ, иначе при сверке молчание прочитается как «фаза забыла» (OQ-3, идиома D-14)"
      status: flagged-unverified
      verification: none
---

<objective>
Старая поверхность снимается целиком и одним движением с тем, что её держало: 1099 строк утверждений
переезжают по двум адресам, модуль исчезает, и решение о поверхности маршрута отражается во всех трёх
перечнях гейтов, а не в двух.

Purpose: это закрытие критерия 3 и записи D-13. Снять модуль раньше значило бы покраснить суиту,
которой нечем заменить предмет; снять его позже — оставить в дереве две копии одной логики.

Output: `app/services/image_upload.py` как единственный дом логики загрузки, три перечня гейтов,
переадресованные изъятия Фазы 9 и `README.md` без исчезнувшего адреса.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-RESEARCH.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-01-SUMMARY.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Полный перечень символов фазы — `12-01-PLAN.md`. Этим планом: модуль
`tests/test_services/test_image_upload.py`. Снимаются: `app/routes/uploads.py` целиком,
`tests/test_routes/test_uploads.py` целиком, имя `uploads_router`.
</artifacts_this_phase_produces>

<scope_acceptance>
## Бюджет плана: перерасход ПРИНЯТ осознанно, а не не замечен

`estimate-check --calibrated` называет план превышающим бюджет 100k (проекция 125k — самая
крупная в фазе) и отмечает девять файлов в `files_modified` при цели 5–8. Разделение рассмотрено и
ОТКЛОНЕНО; основания проверяемы.

1. **Свидетельство некалиброванное — это сказано им самим.** `estimate-calibration` вернул
   `sample_count: 0`, `factor: 1.0`, `confidence: low`: проекция ни разу не сверена с замером.
2. **Проекцию определяет НЕДЕЛИМАЯ преамбула, а не задачи.** `<context>` @-включает
   `12-CONTEXT.md` (25 137 знаков), `12-RESEARCH.md` (73 780) и две сводки, плюс рабочий процесс
   исполнения (41 025 знаков) — порядка 50–55 тысяч токенов ФИКСИРОВАННОЙ стоимости, которую
   каждая копия плана платит ЦЕЛИКОМ. Разделение снимает с пика 20–30 тысяч работы и добавляет
   50–55 тысяч преамбулы к общей стоимости.
3. **Самое тяжёлое чтение фазы НЕДЕЛИМО по существу задачи.** Задача 1 обязана прочесть
   `tests/test_routes/test_uploads.py` ЦЕЛИКОМ (1099 строк): род каждого утверждения решает адрес
   его переезда, и решить это по фрагменту нельзя — именно поэтому разведка вынесла переезд
   отдельной задачей (G-8). Разделение плана этого чтения не уменьшает ни на строку; оно
   перекладывает соседнюю, ЛЁГКУЮ задачу в отдельный план вместе с полной преамбулой.
4. **Разведение по волнам оставило бы в дереве ДВЕ копии одной логики.** Задача 1 переселяет
   утверждения, задача 2 снимает модуль, который эти утверждения держали. Между ними в отдельных
   планах легла бы целая волна, в которой `app/routes/uploads.py` жив и уже не покрыт своей
   суитой. §objective этого плана называет цену прямо: снять модуль раньше — покраснить суиту,
   которой нечем заменить предмет; позже — держать две копии. Окно измеряется задачами, а не
   волнами, намеренно.
5. **Девять файлов — не девять предметов.** Предмет ОДИН: поверхность маршрута. Это удаляемый
   модуль, две строки в `app/main.py`, строка таблицы `README.md`, два новых/пополняемых тестовых
   модуля и три перечня гейтов по одной-две строки каждый. И вынести перечни нельзя:
   **D-13 (`12-CONTEXT.md`) — запертое решение — требует, чтобы инвентарные числа и утверждающие
   их гейты правились ОДНИМ коммитом с работой.** Отдельный план для гейтов есть прямое нарушение
   D-13, а не оптимизация раскладки; критерий 3 ROADMAP требует того же дословно.

**Чем перерасход закрывается вместо разделения — действие, а не надежда.** Если исполнитель видит,
что бюджет кончается ДО начала второй задачи, он **коммитит завершённую задачу и возвращает
оставшуюся оркестратору**, а не дописывает её вырожденно. ⚠️ Возврат на этом шве оставляет дерево
с ДВУМЯ копиями логики загрузки и со снятой суитой старой копии — оркестратор обязан прочесть это
как незакрытый шов, а не как готовую фазу: до исполнения задачи 2 критерий 3 не закрыт.
</scope_acceptance>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Переезд 1099 строк — утверждения о сервисе и утверждения о маршруте по своим адресам</name>

  <files>
tests/test_services/test_image_upload.py,
tests/test_pages/test_ads_image_upload.py,
tests/test_routes/test_uploads.py
  </files>

  <read_first>
- `tests/test_routes/test_uploads.py` ЦЕЛИКОМ (1099 строк) — предмет переезда: импорты из снимаемого
  модуля (:18-23), генераторы тел (`make_png_bytes` :71, `make_real_png_bytes` :142,
  `make_declared_huge_png_bytes` :154), 26 подмен записи объекта, ~25 запросов по прежнему адресу.
  ⚠️ Прочитать ЦЕЛИКОМ прежде, чем править: утверждения делятся на два рода, и род каждого решает
  адрес переезда.
- `app/services/image_upload.py` — готовый после планов 12-01 и 12-04 сервис: имена, формы отказа,
  порядок проверок.
- `tests/test_pages/test_ads_image_upload.py` — готовый после планов 12-01 и 12-04 модуль маршрута:
  форма пары клиентов, генераторы тел, адресные сообщения.
- `tests/test_services/test_image_keys.py` — форма модуля в этом каталоге: чистые утверждения, без
  клиентских фикстур, ожидания на импортированных именах, а не на литералах.
- `12-RESEARCH.md` G-8 целиком; `12-PATTERNS.md` §`tests/test_pages/test_ads_image_upload.py` —
  оба образца-аналога и предупреждение о цели подмены.
  </read_first>

  <behavior>
Перенос утверждений — не переписывание: каждое переехавшее утверждение сохраняет СВОЙ предмет, своё
имя и своё адресное сообщение; меняется только то, через какой вход оно подано.

`tests/test_services/test_image_upload.py` (новый модуль) принимает утверждения О СЕРВИСЕ:
распознавание типа по первым байтам для JPEG и PNG и отказ всему прочему, включая SVG и WebP;
нормализация клиентского имени — отбрасывание сегментов пути, сужение набора символов, предел длины,
непустое значение по умолчанию; безусловное приведение расширения, включая совпадение регистра и
случай «формат не менялся»; порядок отказов — предел размера ВЫШЕ распознавания типа; порционное
чтение с прерыванием на первом превышении; форма ключа; миниатюра ВТОРАЯ и её провал запрос не
проваливает; текст отказа по числу точек отличается от текста про форматы.

`tests/test_pages/test_ads_image_upload.py` принимает утверждения О МАРШРУТЕ, и они переезжают
ПЕРЕВЁРНУТЫМИ ровно там, где форма ответа сменилась решением фазы: где прежний модуль ждал кода
отказа и тела с полем причины, новый ждёт кода 200 и строки отказа во фрагменте (D-04); где прежний
ждал поля ключа в теле — новый ждёт скрытого поля во фрагменте. Утверждения о доступе и о
принадлежности ключа переезжают без переворота.

⚠️ Утверждение, потерявшее предмет вместе с формой ответа (например, о форме тела JSON), СНИМАЕТСЯ с
записью в сводке плана — какое и почему, — а не переписывается в утверждение о другом предмете.
  </behavior>

  <action>
**1. Модуль сервиса.** Заводится `tests/test_services/test_image_upload.py` по форме соседей каталога.
Ожидания идут на ИМПОРТИРОВАННЫЕ имена сервиса, а не на литералы: тест на литерале сравнивал бы модуль
сам с собой. Генераторы тел копируются из снимаемого модуля вместе с их комментариями. Подмена записи
объекта ведётся по адресу `app.services.image_upload.upload_file_to_s3` — ⚠️ цель подмены привязана к
ИМЕНИ МОДУЛЯ, и промах даёт ошибку атрибута вместо осмысленного отказа (G-8); прежний адрес в дереве
не остаётся ни одним вхождением.

**2. Модуль маршрута** пополняется переехавшими утверждениями о входе; форма адресных сообщений
сохраняется — сообщение объясняет, ЧТО увидит человек при отказе, а не повторяет выражение.

**3. Снятие модуля суиты.** `tests/test_routes/test_uploads.py` удаляется ЦЕЛИКОМ — после того, как
оба адресата приняли свои утверждения, и ни минутой раньше. В сводке плана записывается баланс: сколько
утверждений уехало в сервис, сколько в маршрут, сколько снято вместе с предметом и какие именно.
Баланс — не украшение отчёта: без него «переехало» неотличимо от «потерялось».
  </action>

  <verify>
    <automated>uv run pytest tests/test_services/test_image_upload.py tests/test_pages/test_ads_image_upload.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/ -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `test -e tests/test_routes/test_uploads.py; echo $?` печатает `1` — модуль снят.
- `grep -rc 'app.routes.uploads' tests/ | grep -v ':0$'` печатает пустой вывод — прежнего адреса подмены и прежнего импорта в суите не осталось ни одного.
- `grep -c 'app.services.image_upload.upload_file_to_s3' tests/test_services/test_image_upload.py` возвращает не меньше `1`.
- `uv run pytest tests/test_services/test_image_upload.py -q -p no:randomly` — зелено, не меньше десяти тестов.
- Число собранных тестов суиты не упало молча: вывод `uv run pytest tests/ -q --collect-only 2>/dev/null | tail -1` записан в сводку и сопоставлен с балансом переезда.
  </acceptance_criteria>

  <!-- planner-discipline-allow: app.routes.uploads -->

  <done>
Утверждения о сервисе и утверждения о маршруте живут по своим адресам, прежний модуль суиты снят, ни
одной подмены по прежнему адресу модуля не осталось, и баланс переезда записан в сводке.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Модуль снят, три перечня гейтов отражают решение, чужие изъятия переадресованы</name>

  <reversibility rating="costly">Переезд обработчика загрузки с поверхности JSON-API на страничную (D-01) — откат трогает `app/main.py`, оба перечня гейта доступа, запись гейта подстановки личности, адрес маршрута и каждый его тест. Не односторонний: внешних потребителей у `/api/uploads/image` нет, миграции не требуется.</reversibility>

  <files>
app/routes/uploads.py,
app/main.py,
README.md,
tests/test_pages/test_access_gate.py,
tests/test_pages/test_impersonation_gate.py,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/main.py:30-40` — импорт роутера загрузки; `:115-135` — его подключение с двумя зависимостями и
  соседняя форма подключения роутера объявлений.
- `tests/test_pages/test_access_gate.py:60-100` — `OPEN_ROUTERS` и `GATED_API_ROUTERS` с их
  обоснованиями; `:130-150` — `BLOCK_CHECKED_API_ROUTERS`. ⚠️ В файле объявлено, что множества
  НЕЗАВИСИМЫ и выведение одного из другого запрещено: правки делаются по отдельности, а не «заодно».
- `tests/test_pages/test_impersonation_gate.py:78-84` — периметр обхода (`ROUTE_DIRECTORIES`) и
  заявленное свойство «маршрут, не попавший ни в одно множество, роняет гейт»; `:182-235` —
  `ALLOWED_ROUTES` и форма записи «путь::имя функции»: причина; `:218` — запись снимаемого обработчика.
- `tests/test_templates/test_htmx_markup_gates.py:184-191` — записи 6 и 7 сводной летописи Фазы 9,
  адресующие два изъятия ЭТОЙ фазе; `:3437-3545` — ОБЕ записи `DISABLED_ELT_EXCEPTIONS` целиком
  (`components/form_wrapper.html` и `ads/form.html`) и объявленное число; `:3610-3656` —
  `MACRO_BORN_EXCEPTIONS` с той же назначенной фазой и абзацем о том, почему обе записи обязаны
  двигаться ОДНИМ движением.
- `README.md:305-330` — таблица адресов JSON-API и строка снимаемого адреса.
- `.planning/ROADMAP.md` §Phase 15 — раздел, которому переадресуются изъятия; `12-CONTEXT.md` D-01,
  D-13, D-14 (идиома «чужой долг адресуется записью»).
  </read_first>

  <action>
**1. Снятие модуля (D-03, D-13).** `app/routes/uploads.py` удаляется целиком. В `app/main.py`
снимаются импорт роутера загрузки и блок его подключения вместе с двумя зависимостями; соседние
подключения не трогаются ни на символ. Модулей маршрутов в `app/routes/` становится на один меньше —
это движение записывается в сводку плана числом.

**2. Оба перечня гейта доступа (критерий 3, D-01).** Имя `uploads_router` снимается из
`GATED_API_ROUTERS` и, ОТДЕЛЬНОЙ правкой, из `BLOCK_CHECKED_API_ROUTERS` — 5 → 4 в каждом. Причина
записывается рядом с каждым множеством своими словами для этого множества, а не одной ссылкой на
другое: обработчик загрузки переехал на страничную поверхность и закрывается теперь пер-роутерной
зависимостью `ads_router` в `app/pages/__init__.py`, то есть требование множества выполняется другим
механизмом, а не отменяется. ⚠️ Решение о поверхности принято по конвенции проекта (D-07 Фазы 11
назвал `app/routes/` другим транспортом, а слой ответа и его отказ живут в `app/pages/`), а НЕ
подогнано ради зелени — критерий 3 ROADMAP требует этого дословно, и запись говорит об этом прямо.

**3. Третий перечень (G-7).** В `tests/test_pages/test_impersonation_gate.py` ключ снятого
обработчика заменяется на `app/pages/ads.py::<имя нового обработчика загрузки>` с СОХРАНЁННОЙ
причиной. Запись обязательна: обход читает оба каталога маршрутов, и изменяющее объявление, не
попавшее ни в одно множество, роняет гейт — это заявленное свойство формы, а не недосмотр.
Рядом записывается, что перечней ТРИ, а не два, и что критерий 3 называет только два — чтобы
следующий читатель не искал третий заново.

**4. Переадресация двух изъятий Фазы 9 (OQ-3, идиома D-14).** Записи `ads/form.html` в
`DISABLED_ELT_EXCEPTIONS` и в `MACRO_BORN_EXCEPTIONS` НЕ снимаются и НЕ остаются указывать на эту
фазу. В обеих основание переписывается: назначенной фазой становится Фаза 15 (сводный обход сорока
семи форм, FORM-01), и называется причина переадресации — перевод самой формы объявления на
макрос-обёртку задевает перечень условий отправки, стратегию наложения запросов и способ подмены
«ничего», то есть свойства, ради которых форма написана руками, и работа эта лежит вне границы
Фазы 12, объявленной её контекстом. Обе записи правятся ОДНИМ движением, как того требует абзац,
связывающий их между собой. Строки 6 и 7 сводной летописи Фазы 9 дополняются пометкой о
переадресации со ссылкой на решение владельца этой фазы; прежний текст не стирается.
⚠️ Переадресация — ЗАПИСЬ, а не снятие: молчание при сверке прочиталось бы как «Фаза 12 забыла»,
ровно тот класс, против которого написан D-14.

**5. `README.md`.** Строка таблицы JSON-API со снятым адресом удаляется. Вместо неё в раздел,
описывающий страничные маршруты, добавляется строка о новом адресе загрузки с пометкой, что ответ —
фрагмент разметки, а не JSON; если такого раздела в документе нет, под таблицей ставится одна строка,
называющая переезд и фазу, — исчезнувший адрес не оставляется без объяснения.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_access_gate.py tests/test_pages/test_impersonation_gate.py tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>non-zero exit, or any line of output (compileall is silent on success under -q)</fails_when>
    <automated>uv run pytest tests/ -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `test -e app/routes/uploads.py; echo $?` печатает `1`; `grep -c 'uploads' app/main.py` возвращает `0`.
- `grep -c 'uploads_router' tests/test_pages/test_access_gate.py` возвращает `0`; в файле остаются оба множества — `grep -cE '^GATED_API_ROUTERS = \{$' tests/test_pages/test_access_gate.py` возвращает `1` и `grep -cE '^BLOCK_CHECKED_API_ROUTERS = \{$' tests/test_pages/test_access_gate.py` возвращает `1`.
- `grep -c 'app/pages/ads.py::' tests/test_pages/test_impersonation_gate.py` возвращает не меньше `4` — запись загрузки добавилась к трём прежним записям раздела объявлений.
- `grep -c 'НАЗНАЧЕНО ФАЗЕ 12' tests/test_templates/test_htmx_markup_gates.py` возвращает `0` — ни одна ЗАПИСЬ перечня не называет закрывающуюся фазу назначенной; историческая летопись Фазы 9 при этом не стирается и её упоминания остаются.
- `grep -ci 'фазе 15' tests/test_templates/test_htmx_markup_gates.py` возвращает не меньше `2` — обе записи переадресованы.
- `grep -c '/api/uploads/image' README.md` возвращает `0`.
- `uv run pytest tests/ -q` — зелено в полном прогоне; затронутые модули повторно проверены в одиночном прогоне (G-10).
  </acceptance_criteria>

  <!-- planner-discipline-allow: uploads_router -->
  <!-- planner-discipline-allow: /api/uploads/image -->
  <!-- planner-discipline-allow: НАЗНАЧЕНО ФАЗЕ 12 -->

  <done>
Модуль загрузки снят вместе с его подключением, все три перечня гейтов отражают принятое решение о
поверхности, два чужих изъятия переадресованы Фазе 15 записью с названной причиной, и в `README.md`
исчезнувшего адреса нет.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| внешний клиент → приложение | снятый адрес перестаёт существовать; внешних потребителей у него не было — единственным вызывающим была разметка редактора |
| перечни гейтов → объект приложения | гейты читают ИСХОДНИК, а не собранное приложение: роутер без зависимости в объекте приложения выглядит совершенно обычно |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-17 | Elevation of Privilege | маршрут загрузки выпал из периметра гейта доступа вместе с переездом | high | mitigate | Закрытие обеспечивает пер-роутерная зависимость `ads_router` (`app/pages/__init__.py:143`), а не снятая запись множества; утверждение проверяется прогоном обоих перечней и правилом «маршрут вне всех множеств роняет гейт» |
| T-12-18 | Elevation of Privilege | загрузка под ЧУЖОЙ личностью остаётся разрешённой без принятого решения | high | mitigate | Запись гейта подстановки личности перенацелена на новый обработчик с СОХРАНЁННОЙ причиной; обработчик, не попавший ни в одно множество, роняет гейт по построению |
| T-12-19 | Repudiation | снятое изъятие Фазы 9 исчезает молча, и решение о нём теряется | medium | mitigate | Обе записи переадресованы ЗАПИСЬЮ с названной причиной и новой назначенной фазой; сводная летопись дополняется, а не переписывается |
| T-12-SC | Tampering | установки пакетов npm/pip/cargo | low | accept | Фаза не устанавливает ни одного внешнего пакета |
</threat_model>

<verification>
- После задачи 1: `uv run pytest tests/test_services/test_image_upload.py tests/test_pages/test_ads_image_upload.py -q -p no:randomly`.
- После задачи 2: `uv run pytest tests/test_pages/test_access_gate.py tests/test_pages/test_impersonation_gate.py tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly`.
- Перед приёмкой фазы: `uv run pytest tests/ -q` зелёная; `uv run python -m compileall -q app main.py tests`; повторный прогон затронутых модулей ПООДИНОЧКЕ (G-10); `graphify update .`.
</verification>

<success_criteria>
1. `app/routes/uploads.py` и `tests/test_routes/test_uploads.py` сняты; утверждения переехали по двум
   адресам, и баланс переезда записан.
2. Все три перечня гейтов отражают решение о поверхности маршрута; ни один не подогнан ради зелени.
3. Два изъятия Фазы 9 переадресованы Фазе 15 записью с причиной; `README.md` не называет исчезнувшего
   адреса.
</success_criteria>

<output>
Create `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-05-SUMMARY.md` when done.
</output>
