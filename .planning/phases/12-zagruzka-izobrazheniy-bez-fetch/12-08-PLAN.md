---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 08
type: execute
wave: 3
depends_on: ["12-07"]
gap_closure: true
autonomous: true
requirements: [FETCH-01]
files_modified:
  - app/services/image_upload.py
  - app/pages/ads.py
  - tests/test_pages/test_ads_image_upload.py

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Авария хранилища на части *n* партии становится отказом ПО ФАЙЛУ: ключи частей 1..n-1 доезжают до документа скрытыми полями, а неудавшаяся часть получает свою строку (гап 1, третий пункт `missing`; WR-05; D-16)."
    - "Послабление, данное МИНИАТЮРЕ (её провал загрузку не проваливает, P-7), на сжатую версию не расползлось: после провала записи доставляемого объекта второй записи по этой части не делается."
    - "Сервисный контракт `store_upload` не двинут: авария хранилища по-прежнему поднимает `HTTPException(502)`, и правило сервиса, это утверждающее, остаётся зелёным без правки."
    - "Текст отказа приходит из ЗАКРЫТОГО набора констант сервиса; подробность исключения хранилища человеку не пересказывается."
  artifacts:
    - "app/services/image_upload.py — `STORAGE_UNAVAILABLE_MESSAGE` рядом с прочими текстами закрытого набора"
    - "app/pages/ads.py — цикл по частям ловит отказ инфраструктуры пофайлово и продолжает партию"
    - "tests/test_pages/test_ads_image_upload.py — `test_a_storage_failure_still_answers_bad_gateway` инвертировано вверх в `test_a_storage_failure_becomes_a_row_and_keeps_the_accepted_keys`"
  key_links:
    - "`store_upload` → цикл `ads_images_upload`: исключение инфраструктуры больше не выходит мимо сборщика фрагмента, поэтому подмена происходит и принятые ключи доезжают"
    - "`STORAGE_UNAVAILABLE_MESSAGE` → строка отказа `media_strip.html`: тот же путь, которым уже идут отказы по типу, по размеру и по потолку"
  prohibitions:
    - statement: "Замеряющее правило, закрепляющее нежелательное поведение, ИНВЕРТИРУЕТСЯ на более сильное утверждение вместе с предметом — оно не удаляется, не ослабляется и не подгоняется ради зелени."
      status: unverified
      verification: none

security:
  asvs_level: 1
  block_on: high
---

<objective>
Закрыть находку ревью `WR-05` и третий пункт `missing` первого гапа верификации: авария хранилища
посреди партии перестаёт быть ответом 5xx и становится отказом ПО ФАЙЛУ. Прямое исполнение решения
владельца **D-16** во второй его половине.

Purpose: сегодня `store_upload`, поднявший `HTTPException(502)` на части *n*, уносит исключение мимо
сборщика фрагмента. Части `1..n-1` уже записаны в хранилище, их ключи уже лежат в `image_keys` — но
на пятисотом рантайм разметки подмены не делает вовсе, документ этих ключей не узнаёт, и объекты
остаются сиротами. Ровно то рассуждение, которое обработчик применил к отказу владения
(«исключению нельзя дать доехать до браузера»), к аварии хранилища применено не было.

Output: закрытый текст отказа в сервисе, пофайловый перехват в цикле обработчика и инвертированное
вверх правило, которое сегодняшнее поведение закрепляет.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-01-SUMMARY.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Отказ хранилища получает свою строку и не уносит принятые ключи</name>

  <read_first>
    - `app/services/image_upload.py:130-180` — `UNSUPPORTED_IMAGE_MESSAGE`, `OVERSIZED_IMAGE_MESSAGE`, `upload_limit_message`: форма и место закрытого набора текстов
    - `app/services/image_upload.py:279-425` — `store_upload` целиком: докстринг про «единственный случай, уходящий исключением», и подъём `HTTPException(502)` на :396-400
    - `app/services/image_upload.py:182-206` — `Accepted`, `Rejected`
    - `app/pages/ads.py:852-890` — цикл по частям после правок планов 12-06 и 12-07
    - `app/pages/ads.py:838-851` — комментарий-обоснование ветви отказа: образец рассуждения «исключению нельзя дать доехать до браузера», которое здесь распространяется
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md:269-293` — находка WR-05 с наброском правки
  </read_first>

  <files>app/services/image_upload.py, app/pages/ads.py</files>

  <behavior>
    - Партия из двух годных картинок, в которой запись ПЕРВОЙ проходит, а запись доставляемого объекта ВТОРОЙ падает, отвечает 200 фрагментом полосы.
    - Во фрагменте одно скрытое поле `name="images"` — ключ первой части; вторая получает строку отказа с нормализованным именем файла и текстом `STORAGE_UNAVAILABLE_MESSAGE`.
    - Заголовок `HX-Trigger-After-Swap: ads-image-attached` поставлен: хотя бы один файл принят.
    - Обращений к хранилищу ровно три: доставляемый объект и миниатюра первой части, доставляемый объект второй. Четвёртого нет — послабление P-7 на сжатую версию не расползлось.
    - Партия из ОДНОЙ части, запись которой падает, отвечает 200 фрагментом с одной строкой отказа и без скрытых полей: ключей, которые надо было бы сохранить, у неё нет.
    - `store_upload` по-прежнему поднимает `HTTPException(502)`: правило сервиса `test_delivery_failure_raises_bad_gateway` (`tests/test_services/test_image_upload.py:1045`) остаётся зелёным без единой правки.
  </behavior>

  <action>
    В `app/services/image_upload.py` рядом с `UNSUPPORTED_IMAGE_MESSAGE` и `OVERSIZED_IMAGE_MESSAGE` завести константу `STORAGE_UNAVAILABLE_MESSAGE` со значением `"файл не сохранился — хранилище сейчас недоступно, попробуйте ещё раз через минуту"`. Обоснование записать комментарием рядом: тексты отказа в этом модуле образуют ЗАКРЫТЫЙ НАБОР, а не собираются по месту, и подробность исключения хранилища в этот набор не входит — она адресована журналу, а не человеку, и пересказ её на экран отдал бы отправителю сведения об устройстве хранилища. Текст говорит о СУДЬБЕ файла, а не о его пригодности: сказать «ваш файл не подошёл» там, где не подошло хранилище, значило бы соврать.

    `store_upload` НЕ ПРАВИТСЯ: он по-прежнему поднимает `HTTPException(502)`, и его докстринг остаётся верен в своей букве — исключением наружу уходит ровно один случай. Дописывается один абзац: ВЫЗЫВАЮЩИЙ на партии теперь этот случай ловит и превращает в отказ по файлу, потому что у партии есть, что терять, — уже принятые ключи; отдельного вызывающего на одиночном файле у модуля нет, поэтому форма исключения остаётся единственно верной для сервиса.

    В `app/pages/ads.py` обернуть вызов `store_upload` внутри цикла по частям в перехват `HTTPException`: в `refusals` уходит `Rejected` с `display_name=safe_filename(part.filename)` и `reason=STORAGE_UNAVAILABLE_MESSAGE`, свободное место при этом НЕ тратится, цикл продолжается следующей частью. Перед перехватом оставить запись в журнал уровня предупреждения с подробностью исключения — единственное место, где эта подробность живёт. Значение `attached` от неудавшейся части не меняется: заголовок события ставится наличием хотя бы одного ПРИНЯТОГО файла, и неудача записи принятием не является.

    Комментарий-обоснование записать рядом с перехватом и записать ПРЕДМЕТНО: исключение, выпущенное наружу из цикла, оставляет партию без ответа-фрагмента, а на пятисотом подмены нет вовсе — документ не узнаёт ключей частей, УЖЕ записанных в хранилище, и объекты остаются сиротами при потерянной работе человека. Это то же рассуждение, что уже выписано над ветвью отказа сверки; здесь оно распространяется на отказ инфраструктуры, а не заводится заново.

    Импорт `STORAGE_UNAVAILABLE_MESSAGE` добавить к существующему списку имён, ввозимых из `app.services.image_upload` в `app/pages/ads.py`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_services/test_image_upload.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога — сервисный контракт `store_upload` был двинут, хотя правиться он не должен</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "STORAGE_UNAVAILABLE_MESSAGE" app/services/image_upload.py` не меньше 1, и `grep -c "STORAGE_UNAVAILABLE_MESSAGE" app/pages/ads.py` не меньше 2 (ввоз и применение).
    - В срезе обработчика `ads_images_upload` (`awk '/^async def ads_images_upload/,/^@router/' app/pages/ads.py`) вызов `store_upload` стои́т внутри блока `try`, а соответствующий `except HTTPException` добавляет `Rejected` и продолжает цикл.
    - Подъём `HTTPException` с кодом 502 в `app/services/image_upload.py` сохранён: `grep -c "HTTP_502_BAD_GATEWAY" app/services/image_upload.py` не меньше 1.
    - `uv run pytest tests/test_services/test_image_upload.py::test_delivery_failure_raises_bad_gateway -q` зелено при неизменённом теле правила.
  </acceptance_criteria>

  <done>Авария записи на второй части партии оставляет первую прикреплённой, даёт второй свою строку и отвечает фрагментом, а не пятисотым.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Замеряющее правило инвертируется вверх вместе с предметом</name>

  <read_first>
    - `tests/test_pages/test_ads_image_upload.py:664-702` — `test_a_storage_failure_still_answers_bad_gateway`: правило, закрепляющее сегодняшнее поведение, и его докстринг про «границу „всегда 200“»
    - `tests/test_pages/test_ads_image_upload.py:1-145` — `HIDDEN_KEY_FIELD`, `refusal_rows`, `EVENT_HEADER`, `UPLOAD_EVENT`, построители настоящих картинок
    - `tests/test_pages/test_ads_image_upload.py:323-372` — `test_partial_batch_keeps_the_accepted_file`: образец правила на партию из двух частей
    - `app/services/image_upload.py` — `STORAGE_UNAVAILABLE_MESSAGE` после задачи 1
  </read_first>

  <files>tests/test_pages/test_ads_image_upload.py</files>

  <behavior>
    - Правило подаёт партию из ДВУХ годных картинок и раскладывает `mock_s3.side_effect` последовательностью так, что первая часть записывается целиком (доставляемый объект и миниатюра), а запись доставляемого объекта второй части поднимает `RuntimeError`.
    - Утверждается: код ответа 200; ровно одно совпадение `HIDDEN_KEY_FIELD` и его значение проходит образец `IMAGE_KEY`; ровно одна строка отказа, её имя непусто и нормализовано, её причина равна тексту `STORAGE_UNAVAILABLE_MESSAGE`; заголовок `EVENT_HEADER` в ответе присутствует со значением `UPLOAD_EVENT`.
    - Утверждается парное свойство сервиса: `mock_s3.call_count == 3` — четвёртого обращения нет, значит послабление P-7 на сжатую версию не расползлось.
    - Прежнее утверждение кода 502 не удаляется молча: оно переезжает в докстринг летописью с названной причиной снятия и остаётся проверяемым на СЕРВИСНОМ слое правилом `test_delivery_failure_raises_bad_gateway`.
  </behavior>

  <action>
    ⚠️ ЗАЗЕМЛЕНИЕ АДРЕСА ПЕРЕД ПРАВКОЙ. Промпт и отчёт ревью называют это правило живущим в
    `tests/test_services/test_image_upload.py`; замер дерева ставит его в
    `tests/test_pages/test_ads_image_upload.py:667`. Правится СТРАНИЧНЫЙ модуль. Сервисное правило,
    носящее похожее имя, — `test_delivery_failure_raises_bad_gateway`
    (`tests/test_services/test_image_upload.py:1045`), и оно не трогается вовсе.

    Переименовать `test_a_storage_failure_still_answers_bad_gateway` в
    `test_a_storage_failure_becomes_a_row_and_keeps_the_accepted_keys` и переписать тело под
    поведение из блока behavior. Правило ИНВЕРТИРУЕТСЯ ВВЕРХ, а не ослабляется: прежнее утверждало
    один факт об ответе (код 502), новое утверждает четыре факта о судьбе работы человека —
    сохранённый ключ, названную причину, поставленный заголовок события и несостоявшуюся четвёртую
    запись в хранилище. Утверждение `mock_s3.call_count` сохраняется как несущее и лишь сдвигается
    с единицы на тройку вместе с формой партии.

    Докстринг переписать по идиоме проекта «прежняя редакция называется, а не стирается». Сохранить
    действующее рассуждение о том, что провал записи есть отказ ИНФРАСТРУКТУРЫ, а не отказ файлу, и
    что показать плитку значило бы соврать дважды. Назвать, ЧТО изменилось: у партии, в отличие от
    одиночного файла, есть, что терять, — ключи частей, уже записанных в хранилище; на пятисотом
    подмены нет, поэтому прежняя граница «всегда 200» покупала честность кода ценой потерянной
    работы. Плитки для неудавшейся части при этом не появляется — соврать дважды новое правило не
    даёт ровно так же, как прежнее.

    Сообщения утверждений писать в форме соседей модуля: адресно объяснять, ЧТО увидит человек при
    покраснении, а не пересказывать выражение.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py::test_a_storage_failure_becomes_a_row_and_keeps_the_accepted_keys -q</automated>
    <fails_when>ненулевой код возврата, либо "0 passed" в итоге, либо "no tests ran" — правило не собралось под новым именем</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_services/test_image_upload.py tests/test_pages/test_ads_editor.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_a_storage_failure_becomes_a_row_and_keeps_the_accepted_keys" tests/test_pages/test_ads_image_upload.py` не меньше 1.
    - Тело нового правила содержит утверждение `mock_s3.call_count == 3` и утверждение о ровно одном совпадении `HIDDEN_KEY_FIELD`.
    - Докстринг нового правила несёт летопись снятия прежнего утверждения с названной причиной.
    - `tests/test_services/test_image_upload.py` в диффе задачи отсутствует — сервисное правило не правлено.
    - Совокупное число собранных правил в `tests/test_pages/test_ads_image_upload.py` не уменьшилось относительно прогона до правки.
  </acceptance_criteria>

  <done>Правило, закреплявшее потерю принятых ключей, утверждает теперь их сохранение, и утверждает строго больше, чем утверждало прежде.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| приложение → объектное хранилище | внешняя зависимость, отказывающая независимо от ввода пользователя |
| подробность исключения → экран человека | текст, порождённый инфраструктурой, не предназначен отправителю |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08-01 | Information disclosure | строка отказа при аварии хранилища | medium | mitigate | человеку показывается константа закрытого набора `STORAGE_UNAVAILABLE_MESSAGE`; подробность исключения уходит только в журнал уровня предупреждения |
| T-12-08-02 | Denial of Service | объекты-сироты при аварии посреди партии | medium | mitigate | ключи уже записанных частей доезжают до документа, поэтому следующее сохранение объявления их присваивает и объекты перестают быть беспризорными |
| T-12-08-03 | Spoofing | плитка объекта, которого в хранилище нет | high | mitigate | неудавшаяся часть получает строку отказа и НЕ получает плитки — фрагмент не утверждает существования того, чего не записал |
| T-12-08-04 | Tampering | повторные попытки записи в обход предела | low | accept | поведение повтора не меняется: человек повторяет выбор файла, и путь проходит те же проверки типа, размера и потолка |
</threat_model>

## Артефакты, которые этот план производит

- `app/services/image_upload.py`: константа `STORAGE_UNAVAILABLE_MESSAGE` и её текст «файл не сохранился — хранилище сейчас недоступно, попробуйте ещё раз через минуту»
- `tests/test_pages/test_ads_image_upload.py`: правило `test_a_storage_failure_becomes_a_row_and_keeps_the_accepted_keys` (переименование `test_a_storage_failure_still_answers_bad_gateway`)

Новых функций, файлов, маршрутов, шаблонов и заголовков ответа план не заводит.

<verification>
- Авария записи посреди партии отвечает фрагментом, а не пятисотым; принятые ключи доезжают до документа.
- Сервисный контракт `store_upload` не двинут, и правило сервиса зелено без правки.
- Замеряющее правило страничного слоя инвертировано вверх: утверждений стало больше, а не меньше.
</verification>

<success_criteria>
- Третий пункт `missing` первого гапа верификации закрыт кодом.
- Находка ревью `WR-05` видна в задаче, действии и правиле суиты.
- Вторая половина решения владельца D-16 исполнена.
</success_criteria>

<output>
Создать `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-08-SUMMARY.md` по завершении.
</output>
