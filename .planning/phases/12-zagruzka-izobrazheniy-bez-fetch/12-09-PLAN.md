---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 09
type: execute
wave: 4
depends_on: ["12-08"]
gap_closure: true
autonomous: true
requirements: [FETCH-01]
files_modified:
  - app/templates/ads/includes/media_refusals.html
  - app/templates/ads/includes/media_strip.html
  - app/services/image_upload.py
  - app/pages/ads.py
  - tests/test_pages/test_ads_image_upload.py
  - tests/test_pages/test_htmx_gates.py

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Отказ по числу частей составного запроса читается человеком: он приезжает строкой отказа той же формы, что отказ по типу, по размеру и по потолку, а не сырым JSON-ответом с кодом 400 (WR-06, против D-04)."
    - "Эта строка НЕ стирает полосу: разбор запроса не состоялся, значит текущие ключи серверу неизвестны, и ответ дописывает строку в конец `#media-strip`, а не подменяет его содержимое — урок CR-02 применён к новой ветви, а не повторён в ней."
    - "Разметка строки отказа существует в дереве ОДНИМ источником: и полоса, и ответ по числу частей печатают её одним включением."
    - "Применение точечного заголовка подмены объявлено ПОИМЁННО в перечне, а число перечня сдвинуто прогоном покрасневшего правила — оно не выведено вычитанием в уме (идиома D-13, D-16 Фазы 11)."
  artifacts:
    - "app/templates/ads/includes/media_refusals.html — единственный источник разметки строк отказа"
    - "app/services/image_upload.py — `upload_parts_message`"
    - "app/pages/ads.py — перехват отказа разбора составного запроса и сборщик ответа из одних строк отказа"
    - "tests/test_pages/test_ads_image_upload.py — `test_too_many_parts_is_a_readable_row_not_json`"
    - "tests/test_pages/test_htmx_gates.py — первая запись `RETARGET_RESWAP_USES` и сдвинутое `RETARGET_RESWAP_USES_DECLARED`"
  key_links:
    - "`MAX_UPLOAD_PARTS` → `request.form(max_files=…, max_fields=…)` → перехват → `upload_parts_message`: единственная НОВАЯ защита фазы получает читаемый человеком отказ"
    - "`media_refusals.html` → `media_strip.html` и → сборщик ответа по числу частей: два печатающих места, один источник разметки"
  prohibitions:
    - statement: "Рамка партии, назначенная владельцем, не расширяется молча: находка вне рамки не чинится попутно и не сворачивается в соседнюю задачу — она либо названа в плане как несущая, либо остаётся открытой записью ревью."
      status: unverified
      verification: none

security:
  asvs_level: 1
  block_on: high
---

<objective>
Закрыть находку ревью `WR-06`: предел числа частей составного запроса — единственная защита, которую
эта фаза завела НОВОЙ, — перестаёт уходить человеку сырым JSON-ответом с кодом 400 и становится
читаемой строкой отказа.

Purpose: `request.form(max_files=MAX_UPLOAD_PARTS, max_fields=MAX_UPLOAD_PARTS)` не обёрнут; слой
письма превращает отказ разборщика в `HTTPException(400)`, каркас рисует его JSON-ом на
HTML-поверхности, а на четырёхсотом рантайм разметки подмены не делает вовсе. Человек, бросивший в
редактор папку с сотней снимков, получает общую плашку «Действие не выполнено» — то есть ровно тот
исход, ради устранения которого D-04 заплатил лживым кодом 200.

Output: закрытый текст отказа, перехват отказа разбора, ответ, который ДОПИСЫВАЕТ строку и не
подменяет полосу, и поимённая запись применённого точечного заголовка в перечне.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-VERIFICATION.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Разметка строки отказа переезжает в единственный источник</name>

  <read_first>
    - `app/templates/ads/includes/media_strip.html` целиком — особенно :54-71: цикл строк отказа и два комментария-ограничения (строка отказа обязана быть НЕ блоком; имя печатается уже нормализованным)
    - `app/templates/ads/includes/media_strip.html:46-53` — контракт порядка атрибутов узла `#media-tray`
    - `tests/test_pages/test_ads_editor.py` — правило `test_editor_tile_asks_for_the_thumbnail_and_declares_a_fallback`: оно режет разметку литералом и первым закрытием блока, поэтому вывод обязан остаться байт-в-байт прежним
    - `app/templates/ads/includes/autosave_response.html` — третье включающее место полосы
  </read_first>

  <files>app/templates/ads/includes/media_refusals.html, app/templates/ads/includes/media_strip.html</files>

  <action>
    Завести `app/templates/ads/includes/media_refusals.html` — единственный источник разметки строк
    отказа. Содержимое: цикл по `refusals`, печатающий на каждый элемент абзац класса
    `alert alert--error` с ролью `alert`, с именем через разделитель только при непустом
    `display_name`. Оба действующих комментария-ограничения переезжают в новый файл ДОСЛОВНО:
    (а) строка обязана быть не блоком, иначе срез правила плитки обрубится; (б) имя печатается уже
    нормализованным сервисом, и второй нормализации здесь нет намеренно, потому что одного
    автоэкранирования шаблонизатора против имени в четыре тысячи символов мало.

    В `app/templates/ads/includes/media_strip.html` заменить цикл включением нового файла. Вывод
    обязан остаться БАЙТ-В-БАЙТ прежним: то же место внутри узла, те же пробелы и переносы, те же
    управляющие дефисы `{%-` / `-%}`. Способ убедиться назван прямо — прогон правил редактора и
    правил загрузки до и после правки; расхождение вывода покраснит правило плитки, которое режет
    разметку литералом.

    Шапку `media_strip.html` дополнить абзацем: печатающих строку отказа мест стало ДВА — полоса и
    ответ по числу частей составного запроса, у которого своей полосы нет и быть не может, потому
    что разбор запроса не состоялся и текущие ключи серверу неизвестны. Вторая копия разметки
    разошлась бы с первой молча, и прецедент этому уже записан в том же файле абзацем про
    единственный источник полосы.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_editor.py tests/test_pages/test_ads_image_upload.py tests/test_templates/test_components.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога — вывод полосы изменился, хотя правка объявлена бесследной</fails_when>
    <automated>uv run pytest tests/test_templates/test_htmx_inventory.py tests/test_templates/test_htmx_markup_gates.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
  </verify>

  <acceptance_criteria>
    - Файл `app/templates/ads/includes/media_refusals.html` существует и содержит ровно один цикл по `refusals`.
    - `grep -c "media_refusals.html" app/templates/ads/includes/media_strip.html` не меньше 1.
    - `grep -rln "for refusal in refusals" app/templates/` печатает РОВНО ОДИН путь, и это `app/templates/ads/includes/media_refusals.html` — разметка строки живёт в одном месте.
    - Правило `test_editor_tile_asks_for_the_thumbnail_and_declares_a_fallback` зелено без правки.
    - Прогон правил редактора и правил загрузки даёт то же число собранных правил, что до задачи.
  </acceptance_criteria>

  <done>Разметка строки отказа существует в дереве одним источником, и вывод полосы не изменился ни на символ.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Отказ по числу частей читается человеком и не стирает полосу</name>

  <read_first>
    - `app/pages/ads.py:792-820` — начало обработчика, комментарий про пределы числа частей (G-3) и вызов `request.form`
    - `app/pages/ads.py:890-941` — сборщик `_media_strip_fragment` и возврат через `respond`; образец того, как сборщик ставит заголовок на возвращаемый объект
    - `app/services/image_upload.py:54-70` — `UPLOAD_FILE_FIELD`, `MAX_UPLOAD_PARTS`
    - `app/services/image_upload.py:162-180` — `upload_limit_message`: образец функции-текста с подстановкой числа
    - `app/pages/htmx.py:712-900` — `respond`, и абзац :875 о том, что точечные заголовки применяются ПОИМЁННО
    - `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-REVIEW.md:295-317` — находка WR-06 с наброском правки
  </read_first>

  <files>app/services/image_upload.py, app/pages/ads.py, tests/test_pages/test_ads_image_upload.py</files>

  <behavior>
    - Запрос с числом файловых частей больше `MAX_UPLOAD_PARTS` отвечает кодом 200, а не 400.
    - Тело ответа — одна строка отказа класса `alert alert--error` с текстом `upload_parts_message(MAX_UPLOAD_PARTS)`; в теле нет ни узла `id="media-tray"`, ни плитки «+ ФАЙЛ», ни единого скрытого поля `name="images"`.
    - Заголовок ответа объявляет способ подмены `beforeend`: строка ДОПИСЫВАЕТСЯ в конец цели, а не заменяет её содержимое. Цель подмены не переназначается — она по-прежнему `#media-strip` из разметки формы.
    - Заголовок `HX-Trigger-After-Swap` не ставится: не принят ни один файл.
    - Тело ответа не содержит подстроки `detail` — каркасного JSON-представления отказа на этой ветви больше нет.
    - Обычная партия в пределах предела поведения не меняет вовсе: подмена по-прежнему `innerHTML`, и заголовка способа подмены в её ответе нет.
  </behavior>

  <action>
    В `app/services/image_upload.py` рядом с `upload_limit_message` завести
    `upload_parts_message(max_parts: int) -> str`, возвращающую
    `f"за один раз можно отправить не больше {max_parts} файлов — выберите меньше и попробуйте снова"`.
    Текст принадлежит тому же ЗАКРЫТОМУ набору, что и прочие причины отказа, и живёт в сервисе по той
    же причине: второго написания в дереве не появляется.

    В `app/pages/ads.py` обернуть вызов `request.form(max_files=…, max_fields=…)` перехватом
    `HTTPException` и отвечать с этой ветви сборщиком, который печатает ОДНИ строки отказа
    включением `ads/includes/media_refusals.html` с единственным элементом
    `Rejected(display_name="", reason=upload_parts_message(MAX_UPLOAD_PARTS))`. На возвращаемый объект
    сборщик ставит заголовок способа подмены со значением `beforeend`. Возврат идёт через тот же
    `respond` с тем же адресом деградации `/ads/new`, что и на остальных ветвях.

    ⚠️ ПОЧЕМУ ИМЕННО ДОПИСЫВАНИЕ, А НЕ ПОДМЕНА — записать комментарием и записать предметно. Разбор
    составного запроса не состоялся, поэтому текущих ключей вложений сервер НЕ ЗНАЕТ: ни одного
    поля `images` он не прочитал. Ответить полосой значило бы ответить полосой ПУСТОЙ, то есть
    повторить ровно тот дефект, который закрывает план 12-06, — подмена `innerHTML` вынесла бы из
    документа все скрытые поля, и следующее автосохранение записало бы объявление без вложений.
    Дописывание оставляет и полосу, и её скрытые поля нетронутыми, а строка ложится СОСЕДОМ узла
    `#media-tray`, поэтому её не уносит и внеполосная перерисовка полосы. Снимается строка сама —
    следующая удачная загрузка подменяет содержимое цели целиком.

    Действующий комментарий про пределы числа частей (G-3) не стирается: к нему дописывается абзац о
    том, что у предела появился ЧИТАЕМЫЙ отказ, и называется, чем он был прежде — JSON-ответом
    каркаса на HTML-поверхности, на котором подмены не происходит вовсе.

    В `tests/test_pages/test_ads_image_upload.py` завести `test_too_many_parts_is_a_readable_row_not_json`:
    запрос строится с числом файловых частей на единицу больше `MAX_UPLOAD_PARTS` (значение ввозится
    из сервиса — здесь это НЕ второе объявление, а обращение к пределу, который правило и меряет),
    части — мелкие годные PNG. Утверждения — из блока behavior, сообщения в форме соседей модуля.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py::test_too_many_parts_is_a_readable_row_not_json -q</automated>
    <fails_when>ненулевой код возврата, либо "0 passed" в итоге, либо "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_services/test_image_upload.py tests/test_pages/test_ads_editor.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога — реестр пар транспортов разошёлся с новой ветвью</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "def upload_parts_message" app/services/image_upload.py` не меньше 1.
    - В срезе обработчика `ads_images_upload` вызов `request.form` стои́т внутри блока `try`, и соответствующий `except HTTPException` возвращает ответ через `respond`.
    - Ответ на запрос с превышенным числом частей имеет код 200 и содержит подстроку `alert--error`.
    - В теле того же ответа нет подстроки `id="media-tray"` и нет подстроки `name="images"`.
    - Правило `test_every_outcome_answers_two_hundred` зелено и по-прежнему меряет отсутствие кодов 400 и 422 в срезе обработчика.
  </acceptance_criteria>

  <done>Сотня снимков, брошенная в редактор, даёт читаемую строку про предел вместо общей плашки, и ни одно уже прикреплённое вложение при этом с экрана не уходит.</done>
</task>

<task type="auto">
  <name>Задача 3: Применение точечного заголовка объявляется поимённо, число ставится прогоном</name>

  <read_first>
    - `tests/test_pages/test_htmx_gates.py:4955-5010` — шапка группы точечных заголовков, `RETARGET_HEADER`, `RESWAP_HEADER`, `POINTED_HEADERS`
    - `tests/test_pages/test_htmx_gates.py:4993-5010` — `RETARGET_RESWAP_USES` (пуст по замеру) и `RETARGET_RESWAP_USES_DECLARED` (ноль), включая абзац про то, почему ноль объявлен именованной константой
    - `tests/test_pages/test_htmx_gates.py:5012-5075` — `_retarget_reswap_uses`: обход по дереву разбора, ключ вида `app/pages/<файл>.py::<функция>`, признак — строковая константа, равная имени заголовка
    - `tests/test_pages/test_htmx_gates.py` — правило `test_every_retarget_or_reswap_use_is_declared` и его отрицательный контроль синтетическим модулем
    - `app/pages/ads.py` — место записи заголовка способа подмены после задачи 2
  </read_first>

  <files>tests/test_pages/test_htmx_gates.py</files>

  <action>
    После задачи 2 перечень применений точечных заголовков перестал быть пустым, и правило полноты
    обязано покраснеть. ⚠️ ЧИСЛО И КЛЮЧ СТАВЯТСЯ ПРОГОНОМ ПОКРАСНЕВШЕГО ПРАВИЛА, А НЕ ВЫЧИТАНИЕМ В
    УМЕ (идиома D-13): сперва прогнать `uv run pytest tests/test_pages/test_htmx_gates.py -q -k retarget`,
    прочитать в сообщении покраснения ТОЧНЫЙ ключ, который выдал обход (обход ключует по функции,
    объемлющей строковую константу, и для заголовка, записанного внутри вложенного сборщика, ключ
    будет именем этого сборщика, а не именем маршрута), и записать в перечень ИМЕННО его.

    Внести в `RETARGET_RESWAP_USES` первую запись: ключ — выданный прогоном, значение — обоснование
    применения в форме соседних перечней модуля. Обоснование обязано назвать предмет, а не механизм:
    разбор составного запроса не состоялся, текущих ключей вложений сервер не знает, и подмена
    содержимого цели ответила бы полосой ПУСТОЙ, то есть отцепила бы законные вложения — тот самый
    дефект, который закрывает план 12-06. Дописывание строки в конец цели оставляет и полосу, и её
    скрытые поля нетронутыми. Переназначения цели при этом нет: цель остаётся объявленной разметкой
    формы, меняется только СПОСОБ подмены.

    Сдвинуть `RETARGET_RESWAP_USES_DECLARED` с нуля на единицу и дописать строку летописи в
    действующей форме соседних летописей модуля: откуда, куда, какой фазой, каким планом, ИСТОЧНИК
    ДВИЖЕНИЯ одной фразой, и отметка «ПОСТАВЛЕНО ПРОГОНОМ покрасневшего правила» с дословным текстом
    покраснения.

    Абзац шапки группы, объявляющий перечень пустым по замеру и разбирающий двух несостоявшихся
    кандидатов D-16 Фазы 11, НЕ стирается: к нему дописывается третий кандидат — ТОТ, ЧТО
    СОСТОЯЛСЯ, — с причиной, по которой он разрешился заголовком, а не иначе. Отрицательный контроль
    перечня синтетическим модулем не трогается: он доказывает зубы обхода и при непустом перечне.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_inventory.py tests/test_pages/test_ads_image_upload.py -q</automated>
    <fails_when>ненулевой код возврата или "failed" в строке итога</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c "RETARGET_RESWAP_USES_DECLARED = 1" tests/test_pages/test_htmx_gates.py` не меньше 1.
    - Словарь `RETARGET_RESWAP_USES` содержит ровно одну запись, её ключ начинается с `app/pages/ads.py::`, а значение — непустая строка обоснования.
    - Рядом с константой числа стои́т строка летописи, называющая фазу, план и источник движения, и содержащая отметку о постановке прогоном.
    - Правило `test_every_retarget_or_reswap_use_is_declared` зелено, и его отрицательный контроль не правлен.
  </acceptance_criteria>

  <done>Единственное применение точечного заголовка в проекте объявлено поимённо с обоснованием, число перечня сдвинуто прогоном покрасневшего правила, и зубы перечня сохранены.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → разборщик составного запроса | число и размер частей задаёт отправитель; разбор идёт ДО первой строки логики обработчика |
| отказ разборщика → поверхность ответа | отказ, отрисованный каркасом, приходит в форме, которой поверхность не ждёт |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09-01 | Denial of Service | число частей составного запроса | high | mitigate | предел `MAX_UPLOAD_PARTS` сохраняется без единого ослабления; меняется только ФОРМА отказа — предел остаётся вторым рубежом после потолка тела на обратном прокси |
| T-12-09-02 | Tampering | ответ ветви отказа разбора | high | mitigate | ответ ДОПИСЫВАЕТ строку и не подменяет содержимое цели, поэтому скрытые поля ключей остаются в документе; урок CR-02 применён к новой ветви |
| T-12-09-03 | Information disclosure | тело отказа разборщика | medium | mitigate | человеку показывается константа закрытого набора `upload_parts_message`; представление отказа каркасом на этой ветви больше не достигается |
| T-12-09-04 | Repudiation | невидимое применение точечного заголовка | medium | mitigate | применение объявлено поимённо в перечне, число сдвинуто прогоном покрасневшего правила, отрицательный контроль перечня сохранён |
</threat_model>

## Артефакты, которые этот план производит

- `app/templates/ads/includes/media_refusals.html` — НОВЫЙ файл, единственный источник разметки строк отказа
- `app/services/image_upload.py`: функция `upload_parts_message` и её текст «за один раз можно отправить не больше N файлов — выберите меньше и попробуйте снова»
- `app/pages/ads.py`: сборщик ответа ветви отказа разбора и запись заголовка способа подмены со значением `beforeend`
- `tests/test_pages/test_ads_image_upload.py`: правило `test_too_many_parts_is_a_readable_row_not_json`
- `tests/test_pages/test_htmx_gates.py`: первая запись словаря `RETARGET_RESWAP_USES` и значение `RETARGET_RESWAP_USES_DECLARED = 1`

## Находки вне рамки партии, названные здесь

`WR-02` (сужение потолка тела запроса до адреса загрузки) и `IN-02`…`IN-06` решением владельца
остаются ОТКРЫТЫМИ ЗАПИСЯМИ РЕВЬЮ и в эту партию не входят. Ни одна из них не является несущей для
задач этого плана: разметка строки отказа переезжает без изменения вывода, поэтому наследование
признака внеполосной подмены (`IN-02`) не затрагивается, а расчёт свободных мест (`IN-05`) на ветви
отказа разбора не исполняется вовсе.

<verification>
- Превышение числа частей даёт читаемую строку и код 200.
- Ни одно уже прикреплённое вложение этой ветвью с экрана не уходит.
- Разметка строки отказа существует одним источником.
- Применение точечного заголовка объявлено, число поставлено прогоном.
</verification>

<success_criteria>
- Единственная НОВАЯ защита фазы перестала быть единственным отказом, которого человек не может прочитать.
- Урок первого гапа применён к новой ветви, а не повторён в ней.
- Рамка партии не расширена: находки вне неё названы и оставлены открытыми.
</success_criteria>

<output>
Создать `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-09-SUMMARY.md` по завершении.
</output>
