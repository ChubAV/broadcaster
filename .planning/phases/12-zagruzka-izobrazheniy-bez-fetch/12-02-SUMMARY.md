---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 02
subsystem: infra
tags: [nginx, reverse-proxy, config, pytest, upload-limits]

# Dependency graph
requires:
  - phase: 12-zagruzka-izobrazheniy-bez-fetch
    provides: "план 12-01 перевёл партию вложений на ОДИН составной запрос — именно он и делает прежний потолок прокси непригодным"
provides:
  - "`client_max_body_size 64M` в ОБОИХ шаблонах nginx, с формулой `max_images_per_ad` × `max_image_size_mb`, записанной прозой НАД значением"
  - "Директива предела тела запроса в `nginx/nginx-http.conf.template` — заведена ВПЕРВЫЕ; умолчание больше не подразумевается (A1, T-12-08)"
  - "`tests/test_nginx_body_limit.py` — единственный тест суиты, читающий `nginx/`: четыре правила плюс контроль зубов"
affects: [12-03, 12-04, 12-05, 13-fetch-02, 15-fetch-03]

# Actuals (#2632)
actuals:
  tokens: 6400
  tasks: 2
  commits: 2
  plan_head_before: 8f4ecff46f3d9041246bdc0049d80bc2d2dbd5c8

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Живая настройка рантайма сверяется с настройками приложения МАШИННО: тест читает исходник шаблона конфига и берёт ожидание из `Settings`, а не из литерала"
    - "Граница правила, читающего конфиг, объявляется в докстринге вместе с тем, чего правило НЕ доказывает (что боевой nginx перезапущен)"
    - "Контроль зубов подделывает исходник В ПАМЯТИ и сперва ДОКАЗЫВАЕТ, что подстановка сработала, — иначе правило зеленело бы вакуумом"

key-files:
  created:
    - tests/test_nginx_body_limit.py
  modified:
    - nginx/nginx.conf.template
    - nginx/nginx-http.conf.template

key-decisions:
  - "Задача 1 закоммичена как `chore(12-02)`, а не `feat`: правка шаблонов конфига — `chore` по таблице типов, И это же единственный тип, при котором рантайм-гейт TDD не срабатывает проверкой «feat раньше test» (`execute-mvp-tdd.md`, check 3). Тип выбран по таблице, совпадение с требованием гейта — следствие, а не подгонка"
  - "RED задачи 2 замерен на дереве ДО задачи 1, во временном рабочем каталоге (идиома плана 11-14): в готовом дереве модуль зеленеет сразу, и объявить это RED-фазой значило бы объявить измеренным то, что не исполнялось"
  - "Ожидание берётся из `Settings(_env_file=None, …)`, а не из голого `Settings()`: боевой `.env` разработчика в тест протекать не должен (защита `tests/conftest.py`), иначе правило меряло бы чужую машину и падало бы там, где `.env` нет вовсе"
  - "Регулярное выражение применяется к исходнику БЕЗ строк-комментариев: закомментированная директива предела не задаёт, но совпадению неотличима от действующей — без отбрасывания правило зеленело бы на шаблоне со снятым живым значением"
  - "Значение `64M` не подгонялось под «покрасивее»: 50 МБ полезных байтов (10 × 5) плюс ~14 МБ на границы частей составного запроса и текстовые поля списка ключей"

patterns-established:
  - "Перечень файлов конфига в периметре правила — словарь `путь → файл`, где ключ есть то, что человек прочитает в тексте отказа: на две копии правила одна формулировка означала бы поиск руками"
  - "Суффиксы величин nginx (k/m/g) разбираются, а не предполагаются: значение в килобайтах без разбора суффикса сравнивалось бы как мегабайты и зеленело бы на пределе в тысячу раз меньше нужного"

requirements-completed: [FETCH-01]

coverage:
  - id: D1
    description: "Оба шаблона прокси объявляют `client_max_body_size 64M` ровно один раз; в HTTPS-шаблоне значение `20M` заменено, в HTTP-шаблоне директива заведена впервые"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "tests/test_nginx_body_limit.py#test_both_nginx_templates_declare_a_body_ceiling"
        status: pass
      - kind: other
        ref: "grep -c 'client_max_body_size 64M' nginx/nginx.conf.template && grep -c 'client_max_body_size 64M' nginx/nginx-http.conf.template → 1 и 1, rc=0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Потолок прокси не ниже произведения `max_images_per_ad` × `max_image_size_mb`; расхождение краснит суиту, а ожидание берётся из `Settings`, а не из литерала"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "tests/test_nginx_body_limit.py#test_the_proxy_ceiling_covers_the_application_ceiling"
        status: pass
      - kind: unit
        ref: "tests/test_nginx_body_limit.py#test_a_lowered_ceiling_reddens_the_rule"
        status: pass
    human_judgment: false
  - id: D3
    description: "Два шаблона объявляют ОДНО значение: по HTTP и по HTTPS продукт не ведёт себя по-разному"
    requirement: FETCH-01
    verification:
      - kind: unit
        ref: "tests/test_nginx_body_limit.py#test_both_templates_declare_the_same_ceiling"
        status: pass
    human_judgment: false
  - id: D4
    description: "Связь значения с настройками приложения записана ПРОЗОЙ рядом со значением в обоих файлах — читатель, поднявший потолок вложений, видит, что делать с прокси"
    requirement: FETCH-01
    verification:
      - kind: other
        ref: "grep -c 'max_images_per_ad' nginx/nginx.conf.template → 2; grep -c 'max_images_per_ad' nginx/nginx-http.conf.template → 2"
        status: pass
    human_judgment: false
  - id: D5
    description: "Потолок приложения НЕ сужен: `max_images_per_ad` и `max_image_size_mb` не тронуты, отказ по числу вложений остаётся решением сервера"
    requirement: FETCH-01
    verification:
      - kind: other
        ref: "git diff --name-only 8f4ecff..HEAD -- app/config.py → пустой вывод"
        status: pass
    human_judgment: false
  - id: D6
    description: "На боевом стенде партия из десяти снимков по ~4–5 МБ доезжает до приложения, а не получает 413 от прокси"
    requirement: FETCH-01
    verification: []
    human_judgment: true
    rationale: "nginx в среде разработки отсутствует (§Environment Availability разведки), и правка шаблона в гите боевой предел НЕ двигает — нужно переразвёртывание. Машинной проверки у этого свойства нет ни одной; правило суиты доказывает согласованность ИСХОДНИКОВ, а не состояние работающего прокси. Пункт заведён в `.planning/WINDOWS.md` записью 92 (`unrun-verify`, open) и обязан попасть в `12-UAT.md` отдельной строкой."

# Metrics
duration: 45 min
completed: 2026-09-18
status: complete
---

# Phase 12 Plan 02: Потолок тела запроса обратного прокси Summary

**Прокси перестал резать партию до приложения: `client_max_body_size 64M` в обоих шаблонах nginx с выведенной из `max_images_per_ad` × `max_image_size_mb` формулой в прозе, и правило суиты, которое краснеет, если эти величины разъедутся.**

## Performance

- **Duration:** 45 min (из них 37 минут — полный прогон суиты)
- **Started:** 2026-09-18T18:17:43Z
- **Completed:** 2026-09-18T19:03:03Z
- **Tasks:** 2 из 2
- **Files modified:** 3 (1 создан, 2 правлены)

## Accomplishments

- **Единственная рантайм-система, всё ещё настроенная на прежнее поведение «один файл за раз», приведена в step с приложением.** Разведка назвала её прямо (§Runtime State Inventory): после правки каждого файла репозитория оставался nginx с 20-мегабайтным потолком против приложенческих 50 МБ. Теперь оба шаблона объявляют `64M`.
- **HTTP-шаблон получил директиву ВПЕРВЫЕ, и это закрывает не только размер.** Прежде предела здесь не было ВОВСЕ, и какое умолчание действовало — не измерено (запись A1). Значение теперь НАЗЫВАЕТСЯ, а не выясняется: вывод из отсутствия директивы был бы догадкой, а не фактом. Это же и есть мера по T-12-08.
- **Связь двух величин перестала держаться дисциплиной читателя.** `tests/test_nginx_body_limit.py` читает исходник обоих шаблонов, берёт ожидание из `Settings` и краснит суиту при расхождении — четыре правила: объявление ровно один раз в каждом шаблоне, покрытие потолка приложения, совпадение значений двух шаблонов и контроль зубов на понижённом значении.
- **Правило — единственное в суите, читающее `nginx/`.** До него расхождение конфига прокси с приложением не видел ни один из 3415 тестов, потому что смотреть туда было некому. Прибавка ровно четыре: `3415 → 3419 passed`.
- **Потолок приложения не сдвинут ни на символ.** Сужение размера партии владелец отверг (OQ-2) как вторую смену поведения сверх тех, что фаза уже несёт; `app/config.py` в диффе плана отсутствует.

## Task Commits

1. **Задача 1: потолок тела запроса в обоих шаблонах с записанной формулой** — `777d9fb` (chore)
2. **Задача 2: машинная сверка потолка прокси с настройками приложения** — `00efe9b` (test)

_План даёт ДВА коммита, а не три: см. «TDD Gate Compliance» — «реализация», зеленящая правило задачи 2, есть правка конфига задачи 1, и заводить под неё отдельный `feat(12-02)` значило бы закоммитить одно изменение дважды._

## Files Created/Modified

- `tests/test_nginx_body_limit.py` — четыре правила сверки потолка прокси с `Settings`, разбор суффиксов nginx, отбрасывание строк-комментариев, контроль зубов в памяти; докстринг называет границу правила и то, чего оно НЕ доказывает
- `nginx/nginx.conf.template` — `20M` → `64M`, двадцать две строки прозы над значением: формула, требование поднимать вместе с настройками, имя держащего связь теста, разбор про 413 как 4xx
- `nginx/nginx-http.conf.template` — директива заведена впервые внутри блока `server` до блоков `location`; та же проза плюс причина ЯВНОГО задания (умолчание не измерено, A1) и отдельная оговорка, что абзац про заголовок строгой транспортной безопасности не тронут и не обобщён

## Decisions Made

Пять решений вынесены во frontmatter (`key-decisions`). Ни одно из четырнадцати запертых решений `12-CONTEXT.md` не пересмотрено; ответ владельца по OQ-2 исполнен буквально: значение `64M`, оба шаблона, формула прозой, потолок приложения не тронут.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] RED-фаза задачи 2 замерена на дереве ДО задачи 1, а не в готовом дереве**

- **Found during:** Задача 2, RED-фаза
- **Issue:** план ставит задачу 2 ПОСЛЕ задачи 1 и прямо пишет «GREEN — правила зеленеют на значениях задачи 1». В готовом дереве новый модуль зеленеет с первого прогона, а это `unexpected_green` — правило fail-fast `tdd.md` требует остановиться, а не объявить такое RED-фазой. Предмета для отката при этом нет: порядок задач в плане верен, менять его значило бы переписывать план.
- **Fix:** взята идиома плана 11-14 (записана в STATE): временный рабочий каталог в каталоге сессии, куда выложены `nginx/*.template` из коммита `8f4ecff` (состояние ДО задачи 1) и копия нового модуля; прогон оттуда. Дерево проекта не трогалось.
- **Verification:** `3 failed, 1 passed`, rc=1; целевой тест `test_the_proxy_ceiling_covers_the_application_ceiling` покраснел настоящим утверждением — `assert 20971520 >= 52428800`, и отказ напечатал «потолок прокси 20 МБ меньше потолка приложения 50 МБ». Улика проверена прибором: `RED_EVIDENCE_OK` / `target_test_failed`.
- **Committed in:** `00efe9b`

**2. [Rule 3 — Blocking] Улика RED переведена в форму прибора механическим переводчиком**

- **Found during:** Задача 2, шаг персистирования улики
- **Issue:** `check tdd-red-evidence` разбирает TAP (`# tests`, `# pass`, `# fail`, `not ok N - имя`), которого pytest не печатает ни при каком наборе ключей, а плагина TAP в дереве нет. Запись, набранная руками, УТВЕРЖДАЛА бы прогон вместо того, чтобы его ПЕРЕНОСИТЬ.
- **Fix:** переводчик `red_record.py` в рабочем каталоге сессии (в дерево НЕ вносится — тот же приём, что в планах 10-56 и 12-01): он ИСПОЛНЯЕТ команду и собирает запись ИЗ ЕЁ ВЫВОДА — имена из строк `FAILED …::имя`, числа из итоговой строки. Ошибки СБОРА (`ERROR`) считаются отдельно и в поддельные падения не превращаются.
- **Verification:** первый прогон переводчика поймал настоящий `fixture_or_load_failure` — `ModuleNotFoundError: No module named 'app'` из-за корня pytest во временном каталоге; исправлено `PYTHONPATH`, и только после этого запись стала уликой. То есть переводчик доказал, что различает сбой загрузки и падение утверждения.
- **Committed in:** `00efe9b` (сам переводчик — прибор сессии, не код продукта)

**3. [Rule 2 — Missing Critical] Правило отбрасывает строки-комментарии перед разбором**

- **Found during:** Задача 2, написание разбора
- **Issue:** плана этого не требует, но без отбрасывания закомментированная директива (`# client_max_body_size 64M;`) совпадала бы с регулярным выражением наравне с действующей. Шаблон, у которого живое значение снято, а след остался строкой пояснения, зеленел бы — то есть правило утверждало бы наличие предела там, где его нет.
- **Fix:** `_without_comments()` перед разбором, с объяснением причины в докстринге.
- **Verification:** четыре теста зелены; на прогоне RED HTTP-шаблон (директивы нет вовсе) дал `0 объявлений` и покраснел — значит отбрасывание не съело действующие строки.
- **Committed in:** `00efe9b`

**4. [Rule 2 — Missing Critical] Контроль зубов сперва ДОКАЗЫВАЕТ подстановку, потом её использует**

- **Found during:** Задача 2, тест `test_a_lowered_ceiling_reddens_the_rule`
- **Issue:** план описывает контроль как «в копии исходника в памяти значение заменяется на `1M`». Замена, которая ничего не заменила (сменился формат записи, регулярное выражение разошлось), оставила бы исходник целым, правило — зелёным, и тест зеленел бы ВАКУУМОМ, объявляя проверенным то, что не исполнялось ни разу.
- **Fix:** `subn()` вместо `sub()`, утверждение `replacements == 1` ДО применения правила, плюс утверждение, что файл дерева после теста совпадает с исходным.
- **Verification:** тест зелен; на прогоне RED он был единственным прошедшим — то есть он не зависит от состояния дерева, чего от контроля зубов и требуется.
- **Committed in:** `00efe9b`

**5. [Rule 1 — Bug] Ожидание берётся из `Settings(_env_file=None, …)`, а не из голого `Settings()`**

- **Found during:** Задача 2, `<action>` шаг 2
- **Issue:** план пишет «потолок приложения берётся из `Settings()`». Голый `Settings()` читает `.env` РАЗРАБОТЧИКА (`model_config = {"env_file": ".env"}`) — `tests/conftest.py` защищается от этого явно, с комментарием «.env разработчика не должен протекать в тесты». Хуже того, `database_url` и `secret_key` обязательны и умолчаний не имеют: там, где `.env` нет, голый вызов уронил бы сбор модуля.
- **Fix:** `Settings(_env_file=None, database_url=…, secret_key=…)` — ровно те два поля и ровно та защита, что в `conftest.py`. Сверяются УМОЛЧАНИЯ, отгруженные вместе с шаблонами, то есть та самая пара величин, из которой выведена записанная в шаблонах формула.
- **Verification:** `grep -c 'Settings'` → 4; правило зелено и в одиночном прогоне, и в полном; выведенное ожидание — 50 МБ, что совпадает с `10 × 5` умолчаний `app/config.py:43-44`.
- **Committed in:** `00efe9b`

---

**Total deviations:** 5 auto-fixed (3 × Rule 3 — блокирующие, 2 × Rule 2 — недостающее критичное, 1 из них перекрывается с Rule 1).
**Impact on plan:** ни одна правка не ослабила правило и не расширила объём. Три из пяти — форма улики RED, которой план не мог предугадать; две — зубы самого правила, без которых оно зеленело бы по неверной причине. Ни одна строка продукта сверх двух шаблонов не тронута.

## TDD Gate Compliance

| Gate | Коммит | Статус |
|------|--------|--------|
| RED | `00efe9b` `test(12-02)` | ✓ улика проверена прибором: `RED_EVIDENCE_OK` / `target_test_failed`, `3 failed, 1 passed`, целевой тест покраснел утверждением `assert 20971520 >= 52428800` |
| GREEN | `777d9fb` `chore(12-02)` | ✓ по существу, ✗ по букве типа коммита — см. оговорку ниже |
| REFACTOR | — | не заводился: после GREEN улучшать было нечего |

⚠️ **GREEN-коммит этого плана носит тип `chore`, а не `feat`, и это решение, а не небрежность.** Правило задачи 2 зеленеет правкой ШАБЛОНОВ КОНФИГА — то есть задачей 1. Таблица типов коммитов отдаёт конфигу `chore`, и заводить сверх него пустой `feat(12-02)` значило бы закоммитить одно изменение дважды ради формы. Второе следствие того же выбора проверено замером, а не положено: рантайм-гейт `execute-mvp-tdd.md` требует, чтобы `feat({phase}-{plan})` НЕ существовал раньше `test({phase}-{plan})` (check 3), — `git log --grep="^feat\(0*12-0*2\):"` пуст, гейт не сработал.

⚠️ **RED замерен ВНЕ дерева проекта, и это названо, а не умолчано.** Порядок задач плана (конфиг → правило) делает покраснение в готовом дереве невозможным: модуль зеленеет с первого прогона. Объявить такое RED-фазой значило бы объявить измеренным то, что не исполнялось. Замер сделан на дереве коммита `8f4ecff` во временном каталоге сессии — идиома плана 11-14, записанная в STATE. Проверка `is-behavior-adding` при этом вернула `true` (`tdd_true`, `has_behavior_block`, `has_source_files`), то есть гейт считался активным и был пройден по всем трём проверкам, а не обойдён как неприменимый.

## Known Stubs

Нет. План не оставил ни одной заглушки: оба шаблона несут действующие значения, правило проверяет их целиком.

## Threat Flags

Новой поверхности, не названной `<threat_model>` плана, не появилось. Обе `mitigate`-угрозы применены и наблюдаемы:

| Угроза | Мера | Чем наблюдаема |
|---|---|---|
| T-12-07 (подъём потолка расширяет пропускаемое тело) | значение ВЫВЕДЕНО из потолка приложения, а не взято «с запасом»: 64M против 50 МБ полезных байтов; второй рубеж — `MAX_UPLOAD_PARTS` и порционное чтение плана 12-01 | `test_the_proxy_ceiling_covers_the_application_ceiling` не даёт значению разойтись с настройками |
| T-12-08 (HTTP-шаблон без объявленного предела) | предел задан ЯВНО; вывод из отсутствия директивы не делается, причина записана прозой в файле | `test_both_nginx_templates_declare_a_body_ceiling` краснеет на нуле объявлений в любом из двух шаблонов |

`T-12-SC` (установки пакетов) — `accept`: план не установил ни одного внешнего пакета.

## Issues Encountered

- **Полный прогон суиты снова стоит 37 минут** (2233 с против 2246 с у плана 12-01). Он был нужен: только он отвечает на вопрос, не двинул ли новый модуль инвентарных чисел, которых быстрые прогоны не видят по построению (G-10). Ответ — не двинул: `3419 passed` при базовом `3415`, прибавка ровно четыре, ни одного правила не покраснело.
- **Первая попытка замера RED дала `ModuleNotFoundError: No module named 'app'`** — корень pytest во временном каталоге не видит пакета проекта. Это НЕ было RED: прибор такое классифицирует как `fixture_or_load_failure`, и переводчик показал бы `errors=1` при нуле падений. Исправлено `PYTHONPATH`, замер повторён.
- **Нестабильный `test_image_base_url_comes_from_app_settings`** прошёл в полном прогоне. Как и прежде, это наблюдение одного прогона, а не вывод «нестабильность исчезла».

## User Setup Required

None — внешних сервисов план не настраивает и ни одного пакета не устанавливает. ⚠️ Но правка шаблона в гите боевой предел НЕ двигает: чтобы `64M` подействовал, нужно переразвёртывание/перезапуск nginx. Это записано в `.planning/WINDOWS.md` (запись 92) и обязано попасть в `12-UAT.md`.

## Next Phase Readiness

- Готово для 12-03, 12-04 и 12-05: план не тронул ни одного файла, который они правят, и ни одного инвентарного числа, которое им предстоит двигать.
- ⚠️ **Боевое свойство остаётся незакрытым и должно быть закрыто ОБХОДОМ, а не суитой.** Пункт: после переразвёртывания nginx выбрать в редакторе десять снимков по 4–5 МБ разом — все десять прикрепляются, в `just prod-logs nginx` нет 413, общая плашка «Действие не выполнено» не поднимается; записать также, что переразвёртывание выполнено. Заведено записью 92 `.planning/WINDOWS.md` со статусом `open`, что держит `/gsd-ship` закрытым, пока пункт не пройден.
- Правило сверки живёт независимо от фазы: любой будущий подъём `max_images_per_ad` или `max_image_size_mb` без правки шаблонов покраснит суиту немедленно.

---
*Phase: 12-zagruzka-izobrazheniy-bez-fetch*
*Completed: 2026-09-18*

## Self-Check: PASSED

- Файл `key-files.created` существует на диске: `tests/test_nginx_body_limit.py` — один из одного.
- Коммиты `777d9fb` и `00efe9b` найдены в `git log --oneline --all`; измеренный счёт `git rev-list --count 8f4ecff..HEAD` — 2, и он же записан во frontmatter вместе с `plan_head_before`.
- Все `<acceptance_criteria>` обеих задач перепрогнаны после последней правки: 5 из 5 у задачи 1 (включая `git diff -- app/config.py` пустым), 4 из 4 у задачи 2.
- `<verification>` плана исполнена целиком: `grep -c 'client_max_body_size 64M'` по обоим шаблонам → `1` и `1` при rc=0; `uv run pytest tests/test_nginx_body_limit.py -q -p no:randomly` → `4 passed`; полный прогон `uv run pytest tests/ -q` → `3419 passed`, rc=0, 37:13; `uv run python -m compileall -q app main.py tests` молчит; `graphify update .` выполнен (21352 узла, 37128 рёбер).
- Пункт боевой проверки (успех-критерий 3) записан: `.planning/WINDOWS.md`, запись 92, `kind: unrun-verify`, `status: open`.
