---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 13
subsystem: api
tags: [fastapi, jinja2, htmx, upload, error-handling, tdd]

# Dependency graph
requires:
  - phase: 12-zagruzka-izobrazheniy-bez-fetch
    provides: "рубеж владения ключом и разделяющая форма вызова `partition_own_image_keys` (12-01, 12-06); единственный источник разметки строки отказа (12-09); нормализация имени отвергнутого файла `safe_filename` (D-05); адресное снятие плитки (12-11) и вычитающая метка убирания (12-12)"
provides:
  - "ветвь отказа сверки владения называет КАЖДЫЙ выбранный человеком файл своей строкой — «это значение не ваше» отделено от «поэтому остальное в запросе не считается»"
  - "правило `test_a_refused_key_does_not_swallow_the_batch_silently` — партия из чужого ключа и двух годных файлов"
  - "остаток «б» (ключ старого образца в `Ad.images`) существует записью `accepted-assumption` с названным воздействием"
affects: []

actuals:
  tokens: 7876
  tasks: 2
  commits: 4
  plan_head_before: 132c896614d42b9ac9322fba140de8bba4ad7bdb

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Отказ по ЗНАЧЕНИЮ не отменяет отчёта о ВЫБОРЕ человека: отвергнутая партия называет каждый свой файл, не делая ради него ни единицы работы"
    - "Громкость отказа не покупается работой: цикл перебирает ИМЕНА уже разобранных частей и не трогает ни их содержимого, ни сервиса записи"

key-files:
  created: []
  modified:
    - app/pages/ads.py
    - tests/test_pages/test_ads_image_upload.py
    - .planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md

key-decisions:
  - "WR-08 закрыт РАЗДЕЛЕНИЕМ двух утверждений, а не ослаблением сверки: ветвление по предикату принадлежности не тронуто ни на символ — изменилось только то, что человек читает (D-20)"
  - "Громкость отказа не куплена ни одной записью в хранилище: новый цикл читает `part.filename` и больше ничего; запрет плана 12-06 держится тремя правилами"
  - "Нового текста для человека не заведено: причина отказа по файлу — та же константа `INACCESSIBLE_IMAGE_MESSAGE`, что и у безымянной строки про ключ (вторая формулировка той же причины разошлась бы с первой молча)"
  - "Правила, закреплявшие безымянный отказ, инвертированы ВВЕРХ с летописью: прежняя единица была верна для своего дерева и зелена НА ДЕФЕКТЕ"
  - "Остаток «б» записан допущением, а не починен: партия изменила ГРОМКОСТЬ исхода, а не достижимость состояния"

patterns-established:
  - "Приёмочный гейт ставится над числом МЕСТ ПЕРЕБОРА, а не над числом вхождений имени: вхождений уже было два, и гейт «равно 2» над именем краснел бы на правильной работе"
  - "Отказ, относящийся к ЗНАЧЕНИЮ, и отказ, относящийся к ФАЙЛУ, живут в одном ответе и различаются наличием имени — разбор строк отказа опирается на контракт нормализации имени"

# ⚠️ ПУСТО НАМЕРЕННО, И ЭТО НЕ НЕДОСМОТР. План объявляет `requirements: [FETCH-01]`, и сдвоенный
# гейт `requirements.ready-ids` сообщил `1/1 ready` — 12-13 есть ПОСЛЕДНИЙ из трёх объявивших планов.
# Но отметку отверг СОБСТВЕННЫЙ гейт проекта: `tests/test_planning/test_requirement_completion_follows_verification.py`
# держит правило «отметка завершённости не бывает шире вердикта отчёта», а у фазы 12 вердикт
# `gaps_found`, а не `passed`. Правило проекта строже гейта GSD и знает то, чего тот не знает;
# ослаблять его нельзя. FETCH-01 останется `Gaps Found` до вердикта `passed` повторной верификации.
# Подробности — в разделе «Deviations from Plan».
requirements-completed: []

coverage:
  - id: D1
    description: "Партия, отвергнутая по одному значению скрытого поля вложений, больше не исчезает без единого слова о ФАЙЛАХ: каждый выбранный человеком файл получает свою строку с именем (WR-08, D-20)"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_a_refused_key_does_not_swallow_the_batch_silently"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_an_own_key_survives_a_foreign_key_in_the_same_batch"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_a_foreign_key_is_not_echoed_back"
        status: pass
    human_judgment: false
  - id: D2
    description: "Свойство защиты от подделки не ослаблено: на этой ветви в хранилище по-прежнему не уходит ни одной записи, файловые части не читаются и не сохраняются (запрет плана 12-06)"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_a_foreign_key_is_not_echoed_back (mock_s3.call_count == 0, строка 849)"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_an_own_key_survives_a_foreign_key_in_the_same_batch (mock_s3.call_count == 0, строка 928)"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_a_refused_key_does_not_swallow_the_batch_silently (mock_s3.call_count == 0, строка 1005)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Виновное значение по-прежнему не переиздаётся во фрагмент, и законный ключ смешанной партии по-прежнему переживает отказ: рубеж владения не сдвинут"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_an_own_key_survives_a_foreign_key_in_the_same_batch (keys == [mine], foreign not in response)"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_the_keys_that_survive_a_refusal_are_the_ones_the_next_save_persists"
        status: pass
      - kind: integration
        ref: "uv run pytest tests/test_pages/test_ads_image_ownership.py tests/test_services/test_image_upload.py tests/test_services/test_image_keys.py -q -p no:randomly (84 passed)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Имя отвергнутого файла проходит `safe_filename` — та же нормализация, что на прочих ветвях отказа (D-05, T-12-13-03)"
    verification:
      - kind: unit
        ref: "grep app/pages/ads.py — display_name=safe_filename(part.filename) в добавленном цикле"
        status: pass
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_a_rejected_name_is_normalised_before_it_is_shown (сосед, не тронут)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Признак «принят хотя бы один файл» остаётся ложью: автосохранения и заголовка события на этой ветви как не было, так и нет"
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_a_fully_refused_upload_asks_for_nothing"
        status: pass
    human_judgment: false
  - id: D6
    description: "Человек, чья партия отвергнута по одному значению скрытого поля, ЧИТАЕТ на экране имя каждого выбранного им файла"
    requirement: FETCH-01
    verification:
      - kind: integration
        ref: "tests/test_pages/test_ads_image_upload.py#test_a_refused_key_does_not_swallow_the_batch_silently (строки отказа разобраны из ответа)"
        status: pass
    human_judgment: true
    rationale: "Машинно проверен КОНТРАКТ ответа — состав и имена строк отказа во фрагменте. ПРИМЕНЕНИЕ фрагмента живым документом суита не исполняет: рантайм htmx не запускается, и подмена содержимого полосы не происходит. Тот же разрыв назван пунктом 3 `human_verification` действующей верификации фазы."
  - id: D7
    description: "Остаток «б» (ключ старого образца в `Ad.images`) существует записью `accepted-assumption` с названным воздействием и обеими половинами: что партия изменила и чего не изменила"
    verification:
      - kind: unit
        ref: "uv run pytest tests/test_planning -q (44 passed) + grep -c 'status: accepted-assumption' == 6"
        status: pass
    human_judgment: false

# Metrics
duration: 22 min
completed: 2026-09-20
status: complete
---

# Phase 12 Plan 13: Отказ по чужому ключу перестал съедать партию молча Summary

**Ветвь отказа сверки владения в `ads_images_upload` перебирает файловые части запроса и дописывает отказ с именем, прошедшим `safe_filename`, — человек читает имя КАЖДОГО выбранного им файла, а в хранилище по-прежнему не уходит ни одной записи (находка ревью WR-08, решение владельца D-20).**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-20T22:14:00Z
- **Completed:** 2026-09-20T22:36:05Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- **WR-08 закрыт КОДОМ, и закрыт разделением двух утверждений.** Прежде любое значение скрытого поля `images`, не прошедшее сверку принадлежности, уводило обработчик в ветвь `if`, и цикл по файловым частям пропускался ЦЕЛИКОМ: единственная строка ответа говорила о КЛЮЧЕ, а о файлах, которые человек только что выбрал в системном диалоге, не говорилось ничего. Теперь рядом с безымянной строкой про ключ стои́т строка на каждый выбранный файл. «Это значение не ваше» отделено от «поэтому остальное в запросе не считается».
- **Анти-подделочное свойство не ослаблено ни на символ, и это замерено тремя правилами.** Новый цикл читает `part.filename` — и больше ничего: ни содержимого частей, ни сервиса записи. Утверждения `mock_s3.call_count == 0` в трёх правилах (строки 849, 928, 1005) сохранены ДОСЛОВНО и зелены. Громкость отказа не куплена ни одной записью в хранилище (запрет плана 12-06).
- **Достижимость БЕЗ нападающего названа в трёх местах и нигде не выдана за починку.** Ключ старого образца в `Ad.images` уводит партию на ту же ветвь — и до этой партии каждая попытка загрузки такого человека была молчаливым no-op до перезагрузки страницы. Остаток «б» записан `accepted-assumption` с обеими половинами: что партия изменила (исход перестал быть МОЛЧАЛИВЫМ) и чего не изменила (состояние по-прежнему достижимо, выход из него по-прежнему один).
- **Два правила инвертированы ВВЕРХ с летописью, ни одно не ослаблено и ни одно не удалено.** «Строк отказа одна» было верно для дерева, на котором писалось, — и было зелено НА ДЕФЕКТЕ: оно мерило молчание и объявляло его правильным. Число стало «число выбранных файлов плюс одна безымянная», и рост числа есть рост того, что правило доказывает.
- **Ловушка приёмочного гейта, найденная ревью плана, исполнена в исправленной редакции и перемерена.** Гейт считает МЕСТА ПЕРЕБОРА (`getlist(UPLOAD_FILE_FIELD)` вне комментариев): один сегодня, два после правки. Гейт над числом вхождений ИМЕНИ был бы зелен на неправильной работе — вхождений уже было два (ввоз плюс единственный цикл), и правильная правка дала бы три.

## Task Commits

1. **Задача 1 (auto, TDD):**
   - `9659723a` (test) — **RED**: три правила прогнаны красными, `--junit-xml=/tmp/gsd-red-12-13-t1.xml`
   - `28c08e77` (feat) — **GREEN**: цикл по файловым частям в ветви отказа, правленый комментарий ветви
2. **Задача 2 (auto):**
   - `8d9c45c7` (docs) — абзац об остатке «б» и запись `accepted-assumption`

**Plan metadata:** коммит этой сводки.

_REFACTOR-коммита нет намеренно: цикл минимален и повторяет форму действующего цикла ниже по файлу — чистить в нём нечего, и коммит «refactor» без изменений был бы неправдой._

## TDD Gate Compliance

План `type: execute`, поэтому `tdd.review-checkpoint` сообщит `tddPlans: 0` и последовательность RED→GREEN НЕ ЗАМЕРИТ. Улика поэтому живёт здесь, в форме, заведённой планом 12-03 и повторённой планами 12-11 и 12-12.

| Задача | RED-коммит | Дословный текст покраснения | GREEN-коммит |
|---|---|---|---|
| 1 — `test_a_refused_key_does_not_swallow_the_batch_silently` | `9659723a` | `AssertionError: строк отказа 1 вместо 3 — по одной на каждый выбранный файл плюс безымянная про КЛЮЧ: [('', 'Одно из вложений недоступно. Обновите страницу и добавьте изображение заново.')]` | `28c08e77` |
| 1 — `test_an_own_key_survives_a_foreign_key_in_the_same_batch` | `9659723a` | `AssertionError: строк отказа 1 вместо 2 — по одной на каждый выбранный файл плюс безымянная про КЛЮЧ: [('', 'Одно из вложений недоступно. Обновите страницу и добавьте изображение заново.')]` | `28c08e77` |
| 1 — `test_a_foreign_key_is_not_echoed_back` | `9659723a` | `AssertionError: строк отказа 1 вместо двух — безымянной про КЛЮЧ и одной на выбранный человеком ФАЙЛ: [('', 'Одно из вложений недоступно. Обновите страницу и добавьте изображение заново.')]` | `28c08e77` |

**Прогон снят с `--junit-xml`** (`/tmp/gsd-red-12-13-t1.xml`), идиома «ПОСТАВЛЕНО ПРОГОНОМ» (D-13). Каждое из трёх покраснений — НАЗВАННАЯ причина: целевое правило исполнилось и упало на утверждении о заявленном поведении, а не на ошибке ввоза, не на падении помощника и не на чужом утверждении. Итог прогона: `3 failed, 20 deselected`, код возврата 1.

⚠️ **ВЕРБ `check tdd-red-evidence` ВЫДАЛ `INVALID_RED`, И ЭТО МНИМОЕ ПОКРАСНЕНИЕ, КОТОРОЕ ЗАПИСЫВАЕТСЯ, А НЕ ЗАМАЛЧИВАЕТСЯ.** Верб прогнан дважды. Первый раз запись была отвергнута по ФОРМЕ (`invalid_record`) — он ждёт ключи `command`/`exitCode`/`output`/`targetTest`, а не змеиный регистр. Со второй, верной записью вердикт стал `INVALID_RED (zero_tests_discovered)` при `exit_code: 1` и `tests: 0`. Причина установлена ЧТЕНИЕМ реализации (`gsd-core/bin/lib/tdd-red-evidence.cjs`), а не догадкой: разбор вывода идёт `parseNodeTestSummary` / `tapFailedTestNames`, то есть верб читает **TAP-вывод `node:test`**. Вывод `pytest` — не TAP, поэтому верб видит ноль прогнанных правил в прогоне, где их прогнано три и три покраснело. Это структурная неприменимость инструмента к проекту на Python, а не отказ улики. Синтезировать TAP из junit-XML ради зелёного вердикта я НЕ стал: это было бы измерением инструмента измерения, а не предмета. Первичная улика — junit-XML прогона и таблица выше; формой улики партия следует своим соседям 12-11 и 12-12.

## Files Created/Modified

- `app/pages/ads.py` — в ветви отказа сверки владения (`if offending or len(string_images) != len(raw_images)`) после безымянной строки отказа заведён цикл по файловым частям: строковые части пропускаются той же формой, что и в действующем цикле ниже, каждая файловая получает `Rejected(display_name=safe_filename(part.filename), reason=INACCESSIBLE_IMAGE_MESSAGE)`. Комментарий над строкой отказа правлен идиомой D-30/D-32; абзац об остатке «б» правлен ею же.
- `tests/test_pages/test_ads_image_upload.py` — новое правило `test_a_refused_key_does_not_swallow_the_batch_silently`; расширенное `test_an_own_key_survives_a_foreign_key_in_the_same_batch`; инвертированное вверх `test_a_foreign_key_is_not_echoed_back`.
- `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/deferred-items.md` — запись `accepted-assumption` «Ключ старого образца в `Ad.images` по-прежнему отцепляется» под собственным заголовком; записи планов 12-07 и 12-12 не тронуты (в диффе файла ноль удалённых строк).

## Decisions Made

- **Рамка расширена ВЛАДЕЛЬЦЕМ, а не планом, и это процитировано по идентификатору в трёх местах** — в исходнике, в докстринге нового правила и в записи остатка. `D-20` назвал WR-08 поимённо и тем же решением перечислил остающееся ВНЕ партии (`IN-07`…`IN-10`). Запрет плана 12-09 «рамка партии не расширяется молча» держится именно этой цитатой.
- **Нового текста для человека не заведено.** Причина отказа по файлу — та же константа `INACCESSIBLE_IMAGE_MESSAGE`, что у безымянной строки про ключ. Вторая формулировка той же причины разошлась бы с первой молча, и человек читал бы РАЗНОЕ об одном и том же.
- **Цикл поставлен ПОСЛЕ безымянной строки, а не вместо неё.** Отказ относится к КЛЮЧУ, и эта половина прежнего комментария верна: безымянная строка остаётся. Снята вторая половина — «а файла за ним нет», — и причина снятия выписана прямо: файлы ЕСТЬ, человек их только что выбрал.
- **Правило разбирает строки отказа по контракту нормализации, а не по индексу.** Утверждения ищут безымянную строку и словарь именованных, поэтому не зависят от порядка добавления — правило переживёт перестановку, если она когда-нибудь случится, и не зазеленеет от неё случайно.
- **REFACTOR-фазы нет.** Минимальный цикл GREEN уже есть окончательная форма: он буквально повторяет форму действующего цикла ниже. Коммит `refactor` без изменений был бы записью работы, которой не было.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Гейт GSD объявил `FETCH-01` готовым к отметке, а собственный гейт проекта отметку ОТВЕРГ**

- **Найдено:** шаг обновления записей, после обеих задач
- **Проблема:** протокол исполнителя велит отметить требования, которые `requirements.ready-ids` называет готовыми. Верб сообщил `1/1 ready`: `FETCH-01` объявляют три плана партии, и 12-13 — последний из них. Отметка была поставлена вербом `requirements.mark-complete` и немедленно покрасила `tests/test_planning/test_requirement_completion_follows_verification.py`: «требование `FETCH-01` (Phase 12) помечено `Complete`, а у его фазы вердикт `gaps_found` — условие перевода записано самим проектом: вердикт `passed` повторной верификации». ⚠️ **ДВА ГЕЙТА МЕРЯЮТ РАЗНОЕ, И ОДИН ИЗ НИХ СТРОЖЕ.** `ready-ids` знает ровно одно — что у всех объявивших планов есть сводки. О ВЕРДИКТЕ отчёта верификации он не знает ничего, а партия эта — ГАП-ЗАКРЫВАЮЩАЯ: вердикт фазы `gaps_found`, и повторная верификация ещё не проходила.
- **Правка:** отметка СНЯТА (`git checkout -- .planning/REQUIREMENTS.md`), `FETCH-01` остаётся `Gaps Found`. Правило проекта не ослаблено ни на символ — оно право, а не помеха: отметить требование завершённым до вердикта `passed` значило бы объявить закрытым то, чего никто не перепроверял. `requirements-completed` сводки поэтому пуст, и причина выписана прямо во frontmatter, а не оставлена читателю.
- **Файлы:** `.planning/REQUIREMENTS.md` (возвращён к исходному состоянию — в диффе партии его нет)
- **Проверка:** `uv run pytest tests/test_planning -q` — 44 passed
- **Коммит:** правка отменена, коммита не породила; запись существует ЗДЕСЬ, потому что молча отменённая правка есть ровно то, что эта фаза учится не делать

**2. [Rule 1 - Bug] `progress.completed_plans` разошёлся с отметками ROADMAP на единицу**

- **Найдено:** шаг обновления записей, прогон `tests/test_planning`
- **Проблема:** `roadmap.update-plan-progress` поставил отметку плану 12-13, и из отметок ROADMAP стало выводиться 129 исполненных планов, а поле `progress.completed_plans` во frontmatter `.planning/STATE.md` осталось на 128. Верб `state.update-progress` СООБЩИЛ верное число (`completed: 129`), но поле не тронул. Тот же дефект того же верба закрывал коммит `132c8966` после плана 12-12 — то есть он повторяется на каждом плане.
- **Правка:** поле приведено к 129 вручную, как и предписывает текст покраснения («привести надо ПОЛЕ, потому что источником счёта планов объявлен ROADMAP»).
- **Файлы:** `.planning/STATE.md`
- **Проверка:** `uv run pytest tests/test_planning -q` — 44 passed
- **Коммит:** коммит этой сводки

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** ни одна не тронула предмета плана и не расширила рамку. Первая — коллизия двух гейтов записей, разрешённая в пользу СТРОЖАЙШЕГО; вторая — повторяющийся дефект верба трекинга, найденный прогоном, который исполнители обычно не делают.

Предмет самого плана исполнен ровно как написан.

Ловушка приёмочного гейта, названная в разборе партии (`grep -c 'UPLOAD_FILE_FIELD'` равно 2 вместо счёта мест перебора), была исправлена ещё на ревью плана; я исполнил критерий в его действующей редакции и ПЕРЕМЕРИЛ обе величины на дереве до правки: вхождений имени — 2 (ввоз `app/pages/ads.py:64` плюс единственный цикл), мест перебора вне комментариев — 1. После правки мест перебора — 2. Это подтверждает и диагноз ревью, и верность исправленного критерия. Других гейтов, зелёных от собственной прозы, в этом плане не найдено: остальные четыре критерия задачи 1 и пять критериев задачи 2 меряют дифф, счёт определений и состав изменённых строк, то есть предмет, а не написание.

## Issues Encountered

- **`check tdd-red-evidence` неприменим к прогонам pytest** — верб разбирает TAP-вывод `node:test` и потому сообщает мнимый `INVALID_RED (zero_tests_discovered)` на прогоне, где три правила исполнились и три покраснели. Записано разделом TDD Gate Compliance с названной причиной, установленной чтением реализации. Улика не подгонялась под разборщик.
- **`state.add-decision` отвергает пути вне корня проекта** (`Path escapes allowed directory`) при коде выхода 0 — файлы с текстом решений пришлось класть внутрь дерева. Предсуществующее поведение верба, правками этой партии не порождено; здесь названо, а не выписано в `deferred-items.md`, потому что предмет принадлежит инструменту GSD, а не этому проекту.

## Known Stubs

Нет. Заглушек, подделанных значений и незаведённых источников данных правка не оставила. Единственная новая запись `deferred-items.md` — ПРИНЯТЫЙ остаток с названным воздействием и тремя причинами не чинить его, а не незаведённый источник данных.

## Threat Flags

Нет. Новых сетевых адресов, путей аутентификации, обращений к файлам и изменений схемы на рубежах доверия правка не заводит.

- **T-12-13-01 (Spoofing)** — ветвление по `partition_own_image_keys` не тронуто ни на символ: виновное значение во фрагмент не попадает, подтверждённое подмножество остаётся. Замерено `test_an_own_key_survives_a_foreign_key_in_the_same_batch` и зелёным прогоном `tests/test_pages/test_ads_image_ownership.py`.
- **T-12-13-02 (DoS / объекты-сироты)** — цикл называет файлы ПО ИМЕНИ и не читает их содержимого: число обращений к хранилищу на ветви остаётся нулём, утверждается тремя правилами.
- **T-12-13-03 (Injection)** — имя проходит `safe_filename` до шаблона, та же нормализация, что на прочих ветвях (D-05); второй нормализации в шаблоне нет намеренно, и это записано в самом шаблоне.
- **T-12-13-04 (Repudiation)** — предмет правки: молчаливое исчезновение партии устранено.
- **T-12-13-05 (DoS, disposition `accept`)** — достижимость ветви ключом старого образца записана допущением задачей 2.

## Осталось открытым — названо, а не умолчано

- **Остаток «б» НЕ ПОЧИНЕН.** Ключ старого образца в `Ad.images` по-прежнему отцепляется, состояние по-прежнему достижимо, выход из него по-прежнему один — обновить страницу и добавить изображение заново. Партия изменила ГРОМКОСТЬ исхода. Три причины не чинить (расширение образца сняло бы единственное доказательство формы ключа; маршрут привязан к ПОЛЬЗОВАТЕЛЮ, а не к черновику, D-02; перенос данных вне рамки) остаются в силе дословно.
- **`IN-07` (дубли строк отказа по числу частей), `IN-08` (строковая часть под файловым полем пропускается молча), `IN-09` (журнал аварии хранилища пишет константу), `IN-10` (гейт гарды источника — счёт, а не имя) остаются ОТКРЫТЫМИ ЗАПИСЯМИ РЕВЬЮ** и попутно не чинились. `IN-08` примыкает к правке вплотную — новый цикл пропускает строковую часть ТОЙ ЖЕ формой, что и действующий, — и это выписано комментарием прямо на месте, чтобы следующий читатель не принял повторение за одобрение.
- **Вне рамки и не тронуто:** `WR-02`, `IN-02`…`IN-06`.
- **Три запрета `must_haves.prohibitions` этого плана** остаются `verification: none` по форме записи плана: отдельного правила, краснеющего при их нарушении, план заводить не обязывал. Фактически первые два держатся правилами суиты (имена файлов в строках отказа; три утверждения о нуле обращений к хранилищу), третий — ревизией диффа.
- **Пункты `human_needed` верификации фазы кодом не закрывались** ни одним из двух коммитов работы.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Все 13 планов фазы 12 исполнены.** Обе развязки гапа G3 закрыты (ПОРЯДОК II — планом 12-11, ПОРЯДОК I — планом 12-12), находка ревью WR-08 закрыта этим планом.
- **`FETCH-01` НЕ отмечено завершённым, и это правильное состояние, а не пропущенный шаг.** Сдвоенный гейт `requirements.ready-ids` сообщил `1/1 ready` (12-13 — последний из трёх объявивших планов), но СОБСТВЕННЫЙ гейт проекта отметку отверг: вердикт фазы 12 — `gaps_found`, а условие перевода записано проектом как вердикт `passed` ПОВТОРНОЙ верификации. Требование остаётся `Gaps Found` и станет `Complete` ровно тогда, когда `/gsd-verify-work 12` даст `passed`. Подробности — девиация 1.
- **Инвентарь внеполосных блоков стои́т на 22 и этой партией НЕ ДВИНУТ:** ни одного нового блока и ни одной правки шаблонов план не заводит. `tests/test_templates/` зелено без изменения объявленного числа.
- **Опора зелена:** `tests/test_pages/test_ads_image_upload.py` — 23 правила; `test_ads_image_ownership.py` + `tests/test_services/test_image_upload.py` + `test_image_keys.py` — 84; `test_ads_image_upload.py` + `test_ads_editor.py` — 73; `tests/test_templates/` + `tests/test_routes/test_ads.py` — 244; `tests/test_planning` — 44; `uv run python -m compileall` — чисто.
- **Следующий шаг — `/gsd-verify-work 12`**, не новый план.

## Self-Check

- `app/pages/ads.py`, `tests/test_pages/test_ads_image_upload.py`, `deferred-items.md` — все FOUND на диске
- `9659723a`, `28c08e77`, `8d9c45c7` — все FOUND в `git log`
- Приёмочные критерии ОБЕИХ задач прогнаны повторно — все PASS (7 критериев задачи 1, 5 критериев задачи 2)
- `<verify>` обеих задач прогнаны повторно — зелено, ни одного имени в перечне `FAILED`
- `commits` измерен `git rev-list --count 132c8966..HEAD`, база записана в `plan_head_before`

## Self-Check: PASSED

---
*Phase: 12-zagruzka-izobrazheniy-bez-fetch*
*Completed: 2026-09-20*
