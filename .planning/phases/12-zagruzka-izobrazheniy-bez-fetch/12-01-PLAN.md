---
phase: 12-zagruzka-izobrazheniy-bez-fetch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/image_upload.py
  - app/pages/ads.py
  - app/templates/ads/includes/media_strip.html
  - app/templates/ads/includes/media_upload_form.html
  - app/templates/components/form_wrapper.html
  - tests/test_pages/test_ads_image_upload.py
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [FETCH-01]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: `estimate-calibration` вернул sample_count 0, factor 1;
  # confidence выведен из нуля замеров, а не самооценкой.
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Один годный JPEG или PNG, посланный на `/ads/images` с признаком htmx, даёт 200 и фрагмент полосы: плитка с миниатюрой плюс скрытое поле `name=\"images\"` с атрибутом `form=\"ad-form\"` — ключ уезжает со СЛЕДУЮЩЕЙ отправкой формы объявления (критерий 1, D-07)"
    - "Тот же адрес БЕЗ признака htmx отвечает 302 на редактор: путь деградации у маршрута есть и заполнен честно (FOUND-04), а достижим он только прямым POST, потому что у формы загрузки нет кнопки отправки (D-09)"
    - "Маршрут заведён внутри `ads_router`, на котором уже висит `Depends(require_access)`; ни строки гейта в теле обработчика не написано, и отказ доступа едет 204 + `HX-Location`, а не молчанием (критерий 3, D-01)"
    - "Распознавание типа по СОДЕРЖИМОМУ, нормализация имени и приведение расширения живут в `app/services/image_upload.py` вместе с комментариями-обоснованиями ДОСЛОВНО; порядок «отказ по размеру ВЫШЕ распознавания типа» сохранён и наблюдаем (D-03, CR-02)"
    - "Число частей составного запроса ограничено явно (`request.form(max_files=…, max_fields=…)`): у приложения появился второй рубеж, которого у него не было вовсе (G-3, ASVS V12)"
    - "Форма загрузки рождена компонентным макросом `form_wrapper`, `hx-indicator=\"find .form-busy\"` приезжает от макроса (критерий 4), а отступление цели блокировки записано поимённо в перечне исключений, а не сделано молча (D-09)"
    - statement: "В браузере выбор файла поднимает признак «идёт запрос» и плитка появляется без перезагрузки страницы — ручной UAT фазы"
      verification: backstop
  artifacts:
    - "app/services/image_upload.py — sniff_image, safe_filename, retarget_extension, store_upload, Accepted, Rejected, UPLOAD_FILE_FIELD, MAX_UPLOAD_PARTS, тексты отказа"
    - "app/pages/ads.py — ads_images_upload на ads_router по адресу /ads/images и сборщик фрагмента полосы"
    - "app/templates/ads/includes/media_strip.html — ЕДИНСТВЕННЫЙ источник разметки полосы вложений"
    - "app/templates/ads/includes/media_upload_form.html — форма загрузки через form_wrapper, соседом полосы"
    - "tests/test_pages/test_ads_image_upload.py — пара htmx/без-htmx и фрагмент полосы"
  key_links:
    - "форма загрузки (`hx-post=/ads/images`, `hx-target=#media-strip`, `hx-encoding`) → `ads_images_upload` → `store_upload` → `upload_file_to_s3`: ломается — файл уходит в никуда, а человек видит общую плашку вместо плитки"
    - "скрытое поле `name=\"images\"` с `form=\"ad-form\"` → `_save_from_editor` (`form_data.getlist(\"images\")`): ломается — загруженная картинка не доезжает до объявления, и человек теряет работу молча"
    - "`ads_router` (`app/pages/__init__.py:143`, `Depends(require_access)`) → новый маршрут: ломается — загрузка остаётся открытой при истёкшем доступе"
  prohibitions:
    - statement: "комментарии-обоснования переносятся ДОСЛОВНО; переписанный своими словами комментарий есть потеря выстраданного знания, а не редактура (D-03)"
      status: flagged-unverified
      verification: none
    - statement: "инвентарное число разметки ставится ПРОГОНОМ покрасневшего правила, а не вычитанием в уме (D-13, дисциплина летописи test_htmx_markup_gates.py)"
      status: flagged-unverified
      verification: none
---

<objective>
Сквозной срез фазы: одна годная картинка проходит путь целиком — поле файла в разметке, составной
запрос htmx, страничный маршрут внутри `ads_router`, перенесённый сервис, хранилище, фрагмент полосы —
и возвращается плиткой со скрытым полем ключа.

Purpose: архитектура фазы (страничная поверхность вместо JSON-API, фрагмент вместо JSON, DOM вместо
`imagePaths`) доказывается на ОДНОМ пути прежде, чем на неё лягут партия, потолок и отказы. Тупик
архитектуры стои́т одного коммита здесь и десяти — потом.

Output: `app/services/image_upload.py`, маршрут `POST /ads/images` в `app/pages/ads.py`, шаблон
`ads/includes/media_strip.html`, форма загрузки `ads/includes/media_upload_form.html`, два новых
параметра макроса-обёртки, новый тестовый модуль маршрута.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-CONTEXT.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-RESEARCH.md
@.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Перечень ВСЕЙ фазы; планы 12-02…12-05 ссылаются сюда.

**Модуль `app/services/image_upload.py` (новый):** `sniff_image`, `safe_filename`,
`retarget_extension`, `store_upload`, `Accepted`, `Rejected`, `upload_limit_message`,
`UNSUPPORTED_IMAGE_MESSAGE`, `OVERSIZED_IMAGE_MESSAGE`, `UPLOAD_CHUNK_SIZE`, `MAX_FILENAME_LENGTH`,
`FALLBACK_FILENAME`, `UPLOAD_FILE_FIELD`, `MAX_UPLOAD_PARTS`, `_IMAGE_SIGNATURES`,
`_KNOWN_IMAGE_EXTENSIONS`, `_PATH_SEPARATORS`, `_UNSAFE_FILENAME_CHARS`.

**`app/pages/ads.py`:** обработчик `ads_images_upload` (адрес `/ads/images`), сборщик
`_media_strip_fragment`.

**Шаблоны (новые):** `app/templates/ads/includes/media_strip.html`,
`app/templates/ads/includes/media_upload_form.html`. **Идентификаторы:** `#media-strip` (постоянная
обёртка, цель подмены), `#media-tray` (узел содержимого, внеполосная цель). **Имя события:**
`ads-image-attached`.

**Макрос `form_wrapper`:** новые параметры `encoding`, `include`.

**Тесты (новые):** `tests/test_pages/test_ads_image_upload.py`,
`tests/test_services/test_image_upload.py`.

**Снимается:** модуль `app/routes/uploads.py` целиком, модуль `tests/test_routes/test_uploads.py`
целиком, идентификаторы `#image-inputs` и `#upload-error`, тест
`test_editor_javascript_carries_the_thumbnail_prefix`.
</artifacts_this_phase_produces>

<scope_note>
## Форма среза и что он СОЗНАТЕЛЬНО не делает

Срез несёт архитектуру целиком и ровно один путь по ней. Вне среза до волны 3 остаются: сверка
принадлежности присланных ключей (`own_image_keys`), расчёт свободных мест и отказ по потолку (D-06),
перехват отказа `own_image_keys` ради «всегда 200» (D-04). До волны 3 маршрут принимает присланные
ключи как есть и рисует их обратно — это и есть НЕДОСТАЮЩАЯ половина, закрываемая планом 12-04, а не
принятое решение. Страница редактора подключает полосу планом 12-03; до него новый шаблон рисует
только ответ маршрута, и старый обработчик `app/routes/uploads.py` остаётся на месте вместе со своей
суитой — снимает его план 12-05 вместе с переездом 1099 строк утверждений (G-8). Окно сосуществования
двух копий логики названо здесь, а не оставлено на догадку: снять модуль раньше значило бы покраснить
1099 строк, которым ещё нечем заменить предмет.
</scope_note>

<tasks>

<task type="tracer" tdd="true">
  <name>Задача 1: Сквозной срез — одна картинка от составного запроса до плитки полосы</name>

  <reversibility rating="costly">Перенос не-транспортной логики в `app/services/` (D-03) — откат трогает каждый импорт перенесённых имён; внешних потребителей у имён нет, миграции нет.</reversibility>

  <files>
app/services/image_upload.py,
app/pages/ads.py,
app/templates/ads/includes/media_strip.html,
tests/test_pages/test_ads_image_upload.py
  </files>

  <read_first>
- `app/routes/uploads.py` ЦЕЛИКОМ (319 строк) — предмет переноса: шапка про нормализацию имени (:18-24),
  порционное чтение (:26-29), абзац про WebP/GIF/MAX и вектор CR-02 над `_IMAGE_SIGNATURES` (:32-82),
  оба текста отказа с их обоснованиями (:84-111), `_KNOWN_IMAGE_EXTENSIONS` (:113-117), `sniff_image`
  (:120), `safe_filename` (:137), `retarget_extension` (:157), тело `upload_image` (:191-319) с
  комментариями про WR-02, CR-02, issue #40, P-7.
- `app/pages/ads.py:409-433` — `_autosave_response`: форма нульарного сборщика фрагмента.
- `app/pages/ads.py:486-560` — `_save_from_editor`: `await request.form()`, `form_data.getlist("images")`,
  отказ файловой части под именем `images` (WR-03, :523-541), `own_image_keys` (:542).
- `app/pages/ads.py:652-736` — `ads_create`: скелет страничного обработчика, порядок «пользователь →
  отказ → работа → `respond`», отсутствие гейта доступа в теле.
- `app/pages/htmx.py:712-846` — сигнатура и тело `respond`: `redirect` обязателен, ветка фрагмента
  возвращает ответ сборщика как есть.
- `app/services/image_keys.py` целиком — форма ключа, `thumb_key`, `own_image_keys` (нужен волне 3).
- `app/templates/ads/form.html:102-128` — сегодняшний блок вложений: полоса, плитки, кнопка «×»,
  скрытые поля; :141-162 — комментарий про `accept` и файловое поле.
- `app/templates/ads/includes/sched_count_rule.html` целиком — обязательная форма шапки включаемого
  файла «единственный источник разметки» и правило «признака внеполосной подмены здесь нет».
- `app/templates/components/thumb.html` — макрос `thumb()` с запасным полноразмерным адресом.
- `tests/test_pages/test_account_groups.py:516-550` — образец теста htmx-фрагмента: пара клиентов,
  утверждение об отсутствии целого документа, адресное сообщение в каждом `assert`.
- `tests/test_routes/test_uploads.py:60-160,264-320` — генераторы тел (`make_png_bytes`,
  `make_real_png_bytes`, `make_declared_huge_png_bytes`) и форма отправки файла.
- `12-CONTEXT.md` D-01, D-02, D-03, D-04, D-07; `12-RESEARCH.md` §Pattern 1, §Pattern 2, §Pattern 4,
  G-3, §Don't Hand-Roll.
  </read_first>

  <behavior>
RED (`tests/test_pages/test_ads_image_upload.py`, новый модуль):
1. `test_one_image_over_htmx_returns_the_strip_fragment`: POST `/ads/images` с признаком htmx и одной
   файловой частью под именем `files` (настоящий PNG) → 200; в теле нет `<!DOCTYPE`; есть `<img` с
   адресом миниатюры ключа; есть `<input type="hidden" name="images"` со значением ключа И с
   `form="ad-form"`; ключ имеет форму `{user_id}/{32 hex}_{имя}.png`.
2. `test_upload_without_htmx_redirects_to_the_editor`: тот же запрос БЕЗ признака htmx → 302,
   заголовок `location` равен `/ads/new`.
3. `test_upload_is_closed_by_the_router_access_gate`: запрос пользователя с истёкшим доступом с
   признаком htmx → 204 и заголовок `HX-Location` (зависимость роутера, не строка в теле).
GREEN — перенос сервиса, маршрут, шаблон фрагмента.
  </behavior>

  <action>
**1. Модуль `app/services/image_upload.py` (D-03).** Содержимое `app/routes/uploads.py` переносится
ДОСЛОВНО — каждый комментарий-обоснование целиком, без пересказа: абзацы про нормализацию клиентского
имени, про порционное чтение, про WebP в Telethon и в Baileys, про GIF, про `Photo(path=...)` в MAX,
про вектор CR-02, про снятый запрет декодирования и issue #40, про WR-02, оба абзаца над текстами
отказа, абзац над списком известных расширений. Переезжают: `_PATH_SEPARATORS`,
`_UNSAFE_FILENAME_CHARS`, `MAX_FILENAME_LENGTH`, `FALLBACK_FILENAME`, `UPLOAD_CHUNK_SIZE`,
`_IMAGE_SIGNATURES`, `UNSUPPORTED_IMAGE_MESSAGE`, `OVERSIZED_IMAGE_MESSAGE`,
`_KNOWN_IMAGE_EXTENSIONS`, `sniff_image`, `safe_filename`, `retarget_extension`. Строки текстов
отказа не меняются ни на символ.

⚠️ Две ссылки внутри переносимых комментариев устарели переездом и правятся ВМЕСТЕ с переносом, иначе
комментарий станет неправдой: абзац над `UNSUPPORTED_IMAGE_MESSAGE` ссылается на копию в
`app/templates/ads/form.html` и на render-тест, держащий копии в step, — копия снимается планом
12-03, и абзац переписывается на «текст живёт в ОДНОМ месте; гарантия «человек читает точную
причину» держится тестом фрагмента»; абзац над `OVERSIZED_IMAGE_MESSAGE` ссылается на `uploadFile()`
— ссылка заменяется на фрагмент полосы. Прочие комментарии не трогаются.

**2. Новое в модуле.** `UPLOAD_FILE_FIELD = "files"` — ЕДИНСТВЕННОЕ объявление имени файлового поля
(WR-03: имя `images` запрещено, потому что обработчик сохранения отвергает файловые части под ним
осознанно; комментарий называет эту причину). `MAX_UPLOAD_PARTS = 64` — предел ЧИСЛА частей
составного запроса; комментарий называет основание: Starlette ограничивает только НЕфайловые части, у
файловых предела нет вовсе, а `max_part_size` умолчанием к файлам не применяется; значение выбрано
заведомо выше осмысленной партии (потолок вложений 10) и заведомо ниже тысячи, допускаемой умолчанием.
Два замороженных датакласса: `Accepted` с полем `key` и `Rejected` с полями `display_name` и `reason`
— форма отказа в сервисе перестаёт быть `HTTPException`, потому что страничный слой обязан ответить
200 (D-04), а не кодом. `async def store_upload(file, *, user_id, settings) -> Accepted | Rejected`:
порционное чтение с прерыванием на первом превышении → `Rejected` с текстом про размер; `sniff_image`
по первым байтам → `Rejected` с `UNSUPPORTED_IMAGE_MESSAGE`; `prepare_upload` в потоке с обеими
ветвями исключений; `retarget_extension(safe_filename(file.filename), prepared.extension)`; ключ
`{user_id}/{uuid4().hex}_{имя}`; запись объекта, затем миниатюра ВТОРОЙ, чей провал запрос не
проваливает. ⚠️ ПОРЯДОК ПРОВЕРОК СОХРАНЯЕТСЯ ДОСЛОВНО: предел размера стои́т ВЫШЕ распознавания типа,
и следствие наблюдаемо — тело, которое и превышает предел, и не является изображением, получает отказ
по размеру. `display_name` отвергнутого файла — ВСЕГДА `safe_filename(file.filename)`: сырое имя
подконтрольно отправителю и до показа человеку нормализацию не проходило (D-05). Провал записи
объекта в хранилище остаётся исключением и наверх не глушится.
`upload_limit_message(max_images)` объявляется здесь же — текст потолка переезжает на сервер;
потребитель у него появится в плане 12-04.

**3. Маршрут `ads_images_upload` в `app/pages/ads.py` (D-01, D-02, D-04).** `@router.post("/ads/images",
response_class=HTMLResponse)` на том же `router`, что `ads_create`. Гейта доступа в теле НЕТ и заводить
его не нужно — `Depends(require_access)` висит на роутере (`app/pages/__init__.py:143`), и это закрытие
критерия 3. Порядок тела: `user = await get_user_from_cookie(...)`, при отсутствии —
`await respond(request, redirect="/login")`; `form_data = await request.form(max_files=MAX_UPLOAD_PARTS,
max_fields=MAX_UPLOAD_PARTS)`; текущие ключи — `form_data.getlist("images")`, отбираются только
строковые значения; файловые части — `form_data.getlist(UPLOAD_FILE_FIELD)`, каждая по очереди через
`store_upload`, результаты копятся В ПОРЯДКЕ ПРИСЛАННЫХ ЧАСТЕЙ; итоговые ключи — текущие плюс ключи
принятых; отказы — список `Rejected`. Тела файлов не удерживаются все разом: часть обрабатывается,
её байты отпускаются, берётся следующая (G-9). Возврат — `await respond(request, redirect="/ads/new",
fragment=_media_strip_fragment(...))`, где сборщик нульарный и асинхронный, по форме
`_autosave_response`. Адрес деградации `/ads/new` подаётся настоящим и объясняется ИМЕНОВАННЫМ
комментарием при обработчике: маршрут привязан к пользователю, а не к черновику (D-02), поэтому адреса
конкретного редактора он не знает; путь без JavaScript недостижим из интерфейса, потому что у формы
загрузки нет кнопки отправки (D-09), и заполнен он честно, а не заглушкой.

**4. Сборщик `_media_strip_fragment`.** Нульарный async, отдаёт
`templates.TemplateResponse(request, "ads/includes/media_strip.html", …)` с ключами `image_keys`
(итоговый список), `refusals` (список `Rejected`), `max_images` (`settings.max_images_per_ad`).

**5. Шаблон `app/templates/ads/includes/media_strip.html` (D-07, D-08, §Pattern 4).** Шапка файла
воспроизводит форму `sched_count_rule.html` тремя абзацами: (а) это ЕДИНСТВЕННЫЙ источник разметки
полосы, включают его страница редактора и ответ загрузки (ответ автосохранения добавит план 12-03), и
вторая копия разошлась бы с первой молча; (б) признака внеполосной подмены здесь СЕГОДНЯ нет — его
вводит план 12-03 условным параметром, и до тех пор файл в инвентаре внеполосных блоков не числится;
(в) ожидаемые переменные `image_keys`, `refusals`, `max_images`.
Разметка: узел `<div id="media-tray" data-media>` — внутри сперва строки отказа, затем плитки, затем
плитка добавления. Строка отказа — `<p class="alert alert--error" role="alert">` с именем и причиной;
⚠️ строка отказа обязана быть НЕ `<div>`: `test_editor_tile_asks_for_the_thumbnail_and_declares_a_fallback`
режет разметку по ПЕРВОМУ закрытию блока после признака `data-media`, и блочная строка отказа обрубила
бы срез до плитки. Плитка — `<span class="media-tile">` с вызовом макроса `thumb(key)` и именованной
кнопкой отправки: класс `media-tile__remove`, `type="submit"`, `name="remove_image"`, `value` ключа,
`aria-label="Убрать вложение"`, `title` из хвоста ключа после первого подчёркивания. ⚠️ У кнопки и у
скрытого поля появляется `form="ad-form"`, которого сегодня нет: полоса выезжает из формы объявления
(её подключает план 12-03), а связь элемента с формой по атрибуту — тот самый механизм, на котором уже
держится кнопка «Сохранить» (`form.html:319`). Скрытые поля — `<input type="hidden" name="images"
value="{{ key }}" form="ad-form">` на каждый ключ. Плитка добавления — `<label class="media-tile
media-tile--add"{% if image_keys | length >= max_images %} hidden{% endif %} for="file-input">+ ФАЙЛ</label>`;
порядок написания атрибутов сохраняется дословно, потому что `test_editor_hides_add_tile_at_the_limit`
сличает подстроку. Имя файла на плитке НЕ рендерится текстом и живёт в подсказке — правило переносится
комментарием вместе с разметкой.

**6. Тестовый модуль `tests/test_pages/test_ads_image_upload.py`.** Пара клиентов `authed_client` /
`htmx_client` по образцу `test_account_groups.py:516-550`; в каждом `assert` — адресное сообщение о
том, что увидит человек. Генераторы тел копируются из `tests/test_routes/test_uploads.py`. Запись
объекта подменяется по адресу `app.services.image_upload.upload_file_to_s3` — цель патча привязана к
имени модуля, и промах даст `AttributeError` вместо осмысленного отказа (G-8).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_pages/test_access_gate.py tests/test_pages/test_impersonation_gate.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run python -m compileall -q app main.py tests</automated>
    <fails_when>non-zero exit, or any line of output (compileall is silent on success under -q)</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'def store_upload' app/services/image_upload.py` возвращает `1`; `grep -c 'UPLOAD_FILE_FIELD = "files"' app/services/image_upload.py` возвращает `1`.
- `grep -c 'MAX_UPLOAD_PARTS' app/pages/ads.py` возвращает не меньше `1` — пределы частей переданы в разбор формы.
- `grep -c 'ads/images' app/pages/ads.py` возвращает не меньше `1`; `awk '/^async def ads_images_upload/,/^@router/' app/pages/ads.py | grep -c 'require_access'` возвращает `0` — гейт не переписан в теле.
- Порядок проверок сохранён: в `app/services/image_upload.py` номер строки первого вхождения `max_bytes` МЕНЬШЕ номера строки первого вхождения `sniff_image(content)` — `grep -n 'max_bytes' app/services/image_upload.py | head -1` и `grep -n 'sniff_image(content)' app/services/image_upload.py | head -1`.
- Комментарии-обоснования перенесены: `grep -c 'WebP' app/services/image_upload.py` не меньше `1`, `grep -c 'CR-02' app/services/image_upload.py` не меньше `1`, `grep -c 'WR-02' app/services/image_upload.py` не меньше `1`, `grep -c 'issue #40' app/services/image_upload.py` не меньше `1`.
- `grep -c 'form="ad-form"' app/templates/ads/includes/media_strip.html` возвращает не меньше `2` — скрытое поле и кнопка убирания.
- `uv run pytest tests/test_pages/test_ads_image_upload.py -q -p no:randomly` — зелено, не меньше трёх тестов.
  </acceptance_criteria>

  <done>
Одна годная картинка, посланная на `/ads/images` с признаком htmx, возвращает 200 и фрагмент полосы с
плиткой и скрытым полем ключа, несущим `form="ad-form"`; без признака htmx тот же адрес отвечает 302
на редактор; истёкший доступ закрывает зависимость роутера. Не-транспортная логика живёт в
`app/services/image_upload.py` вместе с перенесёнными дословно обоснованиями.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Форма загрузки на макросе-обёртке — составной запрос без единой строки JS</name>

  <files>
app/templates/components/form_wrapper.html,
app/templates/ads/includes/media_upload_form.html,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `app/templates/components/form_wrapper.html` целиком (173 строки) — ЧЕТЫРЕ жёстких ограничения шапки
  (:24-83): `method="post"` литералом в двойных кавычках; `action` и `hx-post` печатают одно и то же
  выражение посимвольно; внутри тега нет ни одного символа закрытия тега; атрибут индикатора стои́т
  ПОСЛЕДНИМ, сразу за прочими правилами; ветвь пустой цели блокировки (:98-121).
- `app/templates/ads/includes/sched_card.html:160-175` — действующий образец вызова с `trigger='change'`
  и пустой целью блокировки.
- `tests/test_templates/test_htmx_markup_gates.py:97-141` — `HX_POST_PLACES` и объяснение «место ОДНО
  независимо от числа вызывающих»; `:143-205` — сводная летопись чисел Фазы 9, записи 6 и 7;
  `:3437-3470` — `DISABLED_ELT_EXCEPTIONS`, поле `callers` записи `components/form_wrapper.html`;
  `:3540-3560` — `DISABLED_ELT_EXCEPTIONS_DECLARED`; `:3571-3600` — сканер вызывающих `_macro_callers`;
  `:4586-4600` — правило «рождён компонентным макросом».
- `app/templates/ads/form.html:141-162` — комментарий про `accept` как подсказку диалогу, а не проверку;
  он переезжает вместе с полем.
- `12-RESEARCH.md` §Pattern 1 (условие составного запроса в вендоренном рантайме), §Pattern 3.
  </read_first>

  <action>
**1. Макрос `form_wrapper` получает два параметра (§Pattern 3).** В сигнатуру добавляются `encoding=false`
и `include=None`; в теге печатаются условными ветвями, как соседние `trigger` и `sync`: при истинном
`encoding` — `hx-encoding="multipart/form-data"` И `enctype="multipart/form-data"` (вендоренный рантайм
принимает любое из двух, но без `enctype` форма без скрипта послала бы файл кодированием, которого
сервер не разберёт); при непустом `include` — `hx-include="{{ include }}"`. Обе ветви ставятся ДО
ветви цели блокировки, то есть атрибут индикатора остаётся последним в теге. Ни один символ закрытия
тега внутри ветвей не появляется. Умолчания подобраны так, что разметка остальных сорока шести форм
вехи не двигается ни на символ. Шапка файла получает абзац о новых параметрах в её собственной форме:
почему кодирование — параметр, а не умолчание (составной запрос нужен ровно одной форме вехи, а
остальным он удорожил бы каждый запрос), и почему набор включаемых полей задаёт вызывающий.

**2. Форма загрузки `app/templates/ads/includes/media_upload_form.html` (новый файл).** Шапка
объясняет ТРИ свойства, каждое решением: (а) форма стои́т СОСЕДОМ полосы и соседом формы объявления, а
не внутри них — вложенную форму браузер молча отбрасывает, и та же причина держит секцию расписаний
снаружи (`form.html:169-178`); (б) кнопки отправки у формы НЕТ, и это ИМЕНОВАННОЕ изъятие: путь без
JavaScript остаётся недостижимым из интерфейса намеренно, потому что без автосохранения отправка формы
загрузки навигацией потеряла бы набранный текст (D-09) — вывести это правило из безымянности поля
больше нельзя, имя у поля есть, иначе рантайм не послал бы файл; (в) форма лежит ВНЕ цели подмены,
потому что подмена содержимого полосы стёрла бы её вместе с полем файла, а событие заголовка ответа
поднимается на узле, пославшем запрос (нужно плану 12-04).
Тело — блочный вызов `form_wrapper` с адресом `/ads/images`, целью `#media-strip`, способом подмены
`innerHTML`, отправкой по изменению, включением `#media-strip input[name='images']`, включённым
кодированием и ПУСТОЙ целью блокировки. Внутри вызова — единственное поле: `<input class="field__input"
type="file" id="file-input" name="{{ … }}" accept="image/jpeg,image/png" multiple hidden>`, где имя
поля печатается значением, пришедшим из контекста шаблона (ключ `upload_field`), а не литералом:
единственное объявление имени живёт в `app/services/image_upload.py`. Комментарий про `accept` как
подсказку файловому диалогу, а не проверку, переносится сюда дословно, включая довод про авторитет
серверного распознавания по содержимому.

⚠️ Шаблонный глобал `upload_field` объявляется рядом с прочими глобалами изображений в
`app/pages/common.py` — тем же способом, каким объявлен `THUMB_KEY_PREFIX` (:284). Второго литерала
имени в дереве не появляется.

**3. Перечень исключений цели блокировки.** Вызывающий `ads/includes/media_upload_form.html`
дописывается в поле `callers` записи `components/form_wrapper.html` в `DISABLED_ELT_EXCEPTIONS`;
основание пополняется строкой летописи: у формы загрузки нет достижимой цели блокировки, потому что
кнопки отправки у неё нет вовсе (D-09 Фазы 12), а объявленная и недостижимая цель пишет строку в
консоль на КАЖДЫЙ запрос и ломает опорный признак обхода «ответ 200 и чистая консоль». Число
`DISABLED_ELT_EXCEPTIONS_DECLARED` считает ЗАПИСИ, а не вызывающих, и двигается только если краснеет
правило.

**4. Инвентарные числа разметки.** `HX_POST_PLACES`, `HX_TARGETS`, перечни параметрических целей
подмены и их вызывающих — ставятся ПРОГОНОМ покрасневшего правила, а не сложением в уме; каждое
движение дописывается пронумерованной строкой летописи с пометкой «Фаза 12, план 12-01». Ожидание
разведки — что `HX_POST_PLACES` не двигается (разборщик считает ФАЙЛЫ, печатающие атрибут, а новая
форма рождена макросом), — записывается в летопись даже при неподвижности числа, как это уже сделано
записями «ДВИЖЕНИЯ НЕТ» Фазы 10.
  </action>

  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_components.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_pages/test_ads_editor.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c 'hx-encoding' app/templates/components/form_wrapper.html` возвращает `1`; `grep -c 'hx-include' app/templates/components/form_wrapper.html` возвращает `1`.
- `grep -c 'form_wrapper' app/templates/ads/includes/media_upload_form.html` возвращает не меньше `1`; `grep -c 'type="submit"' app/templates/ads/includes/media_upload_form.html` возвращает `0` — кнопки отправки у формы нет (D-09).
- `grep -c 'ads/includes/media_upload_form.html' tests/test_templates/test_htmx_markup_gates.py` возвращает не меньше `1` — вызывающий объявлен поимённо.
- `grep -c 'id="file-input"' app/templates/ads/includes/media_upload_form.html` возвращает `1`; `grep -c 'name="images"' app/templates/ads/includes/media_upload_form.html` возвращает `0` — файловая часть под именем `images` запрещена (WR-03).
- `grep -c 'upload_field' app/pages/common.py` возвращает не меньше `1`.
- `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "disabled_elt or born_of_a_component or hx_post"` — зелено.
  </acceptance_criteria>

  <done>
Форма загрузки рождена макросом-обёрткой, шлёт составной запрос по изменению файлового поля, включает
текущие ключи, целится в `#media-strip` подменой содержимого и не объявляет недостижимой цели
блокировки; отступление записано поимённо с основанием D-09.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | составной запрос: содержимое файлов, клиентские имена файлов, объявленные типы частей, число частей, список текущих ключей |
| страничный слой → хранилище S3 | ключ объекта и `Content-Type` сохраняемых байтов |
| хранилище → браузер | объект отдаётся с origin хранилища и с записанным при загрузке типом |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering / Elevation | `store_upload` — SVG или иной активный документ под видом PNG (CR-02) | high | mitigate | Тип определяется по первым байтам `sniff_image`, список закрыт на JPEG и PNG; присланный заголовок типа не используется НИГДЕ, включая запись в хранилище; `content_type` объекта берётся из ПРОИЗВЕДЁННЫХ приложением байтов (`prepared.content_type`) |
| T-12-02 | Tampering | `safe_filename` — сегменты пути в клиентском имени выводят ключ за префикс пользователя | high | mitigate | Сегменты пути отбрасываются целиком, набор символов сужен до `[A-Za-z0-9._-]`, длина обрезана; перенос дословный, поведение не смягчается |
| T-12-03 | Denial of Service | `store_upload` — тело произвольного размера в памяти (WR-02) | medium | mitigate | Порционное чтение с прерыванием на ПЕРВОМ превышении: остаток тела не читается, собранные порции не склеиваются |
| T-12-04 | Denial of Service | разбор составного запроса — партия из тысячи частей (G-3, НОВОЕ В ЭТОЙ ФАЗЕ) | high | mitigate | `await request.form(max_files=MAX_UPLOAD_PARTS, max_fields=MAX_UPLOAD_PARTS)`; у файловых частей Starlette своего предела не имеет, и до этой фазы второго рубежа у приложения не было |
| T-12-05 | Denial of Service | `prepare_upload` — decompression bomb | medium | accept | Предел числа точек `MAX_DECODED_PIXELS` проверяется ДО распаковки пикселей и уже действует (`app/services/images.py`); фаза его не трогает и новой поверхности не открывает |
| T-12-06 | Elevation of Privilege | загрузка при истёкшем доступе | high | mitigate | Маршрут заведён внутри `ads_router`, на котором висит `Depends(require_access)`; правило утверждается перечнями `tests/test_pages/test_access_gate.py` (правятся планом 12-05) |
| T-12-SC | Tampering | установки пакетов npm/pip/cargo | low | accept | Фаза не устанавливает ни одного внешнего пакета; секция §Package Legitimacy Audit разведки применима вакуумно |
</threat_model>

<verification>
- После каждой задачи: `uv run pytest tests/test_pages/test_ads_image_upload.py tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly`.
- На слиянии волны: `uv run pytest tests/ -q`; `uv run python -m compileall -q app main.py tests`; `graphify update .`.
- ⚠️ Затронутые модули проверяются И в одиночном прогоне, И в полном: нестабильность порядка исполнения
  известна и заведена в `.planning/todos/pending/full-suite-ads-editor-order-pollution.md` (G-10), и
  своё покраснение иначе неотличимо от чужого.
</verification>

<success_criteria>
1. Годный файл, посланный на `/ads/images` с признаком htmx, возвращает 200 и фрагмент полосы; без
   признака htmx — 302 на редактор; истёкший доступ закрыт зависимостью роутера.
2. Не-транспортная логика живёт в `app/services/image_upload.py`; порядок «размер выше типа» и
   комментарии-обоснования перенесены дословно.
3. Форма загрузки рождена макросом-обёрткой, отступление цели блокировки записано поимённо.
</success_criteria>

<output>
Create `.planning/phases/12-zagruzka-izobrazheniy-bez-fetch/12-01-SUMMARY.md` when done.
</output>
