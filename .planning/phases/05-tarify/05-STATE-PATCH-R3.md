# 05-STATE-PATCH-R3 — готовая к применению замена секций `.planning/STATE.md`

**План:** 05-14 (гэп 2 отчёта `05-VERIFICATION.md`, раунд 3, `status: partial`)
**Дата подготовки:** 2026-08-17
**Вход:** `.planning/STATE.md`, **125 строк**, в главном рабочем каталоге на коммите
`e6c14a3`. Номера строк ниже относятся к этому состоянию файла.

Этот файл — **отдельный артефакт**. Патч плана 05-12 (`05-STATE-PATCH.md`, коммит
`c177c44`) описывает уже применённые правки и НЕ перезаписывается: превратить запись об
исполненной работе в запись о планируемой — тот самый класс подмены, который эта фаза
ловит третий раунд подряд.

Файл лежит в каталоге фазы намеренно: файлы каталога фазы коммитятся планом в любом
режиме исполнения и переживают снос worktree. Даже если применение к рабочему дереву не
удастся, готовый текст остаётся под рукой, а не в теле отчёта.

Правок ровно **СЕМЬ**, по разделу на каждую:

| # | Раздел `STATE.md` | Что делается |
|---|-------------------|--------------|
| 1 | §Blockers/Concerns, 🟡 сводная запись (строка 91) | вводная часть заменяется реестром раундов по коммитам |
| 2 | §Blockers/Concerns | новая 🔴 запись: сокет Docker у веб-слоя, `WR-07 (раунд 3)` |
| 3 | §Blockers/Concerns | новая 🟡 запись: распоряжения по тринадцати находкам раунда 3 |
| 4 | §Deferred Items, строка 119 | правка записи о `CR-01 (раунд 2)`: закрытие названо половинным |
| 5 | §Deferred Items | новая строка: `CR-01 (раунд 3)` закрыт планом `05-13` |
| 6 | §Decisions, строка 80 | дополнение записи о семантике смены тарифа исходом на истёкшем сроке |
| 7 | «Current Position» + frontmatter `progress` | приведение в соответствие с ROADMAP |

Ни один раздел `STATE.md`, не названный выше, не трогается; файл не перегенерируется
целиком. Порядок применения — сверху вниз, от раздела 1 к разделу 7.

---

## ⚠️ ЛОВУШКА НУМЕРАЦИИ — прочитать до применения любой правки

`05-REVIEW.md` перезаписывался **трижды**. Номера находок между раундами
ПЕРЕНАЗНАЧЕНЫ, и совпадение номера не означает совпадения находки:

| Номер | Раунд 1 (`git show 4989f7a:…`) | Раунд 2 (`git show 1d29360:…`) | Раунд 3 (`git show 164d210:…`) |
|-------|-------------------------------|-------------------------------|-------------------------------|
| CR-01 | гард источника вебхука читает адрес пира | молчаливое понижение тарифа в `_apply_extension` | оплаченное понижение на истёкшем сроке не выдаётся |
| CR-02 | двойное зачисление, check-then-act | синхронный вызов SDK без таймаута | — (раунд 3 несёт один critical) |
| WR-06 | необработанное уведомление отвечает 200 | `∞` в сайдбаре при нулевом балансе | план вне `PLAN_LIMITS` рисует четыре оси как «без ограничений» |
| **WR-07** | — | негодный ФОРМАТ `PLAN_LIMITS` роняет `json.loads` | **сокет Docker у веб-слоя, разбирающего JSON вебхука** |
| WR-10 | негодный ТИП лимита роняет `axis_percent` | — (раунд 2 знает только WR-01…WR-09) | — |

Отсюда правило записи: **каждый номер раунда 3 пишется в форме `<номер> (раунд 3)`**.
Голый номер WR-07 в записи приписал бы сокет Docker находке о конфигурации тарифов.

**Сокращать счёт вхождений удалением записи ЗАПРЕЩЕНО.** Требование к номеру —
квалификация раундом, а не уменьшение числа его вхождений. Вхождений `WR-07` после
применения станет **четыре**: заголовок 🔴 записи (раздел 2), перечень 🟡 записи
(раздел 3), перекрёстная ссылка распоряжения `WR-06 (раунд 3)` на отложенное
распоряжение `WR-07 (раунд 2)` (раздел 3) и существующая запись строки 91 (не трогается).
Проверка ведётся РАВЕНСТВОМ числа вхождений `WR-07` числу вхождений
`WR-07 (раунд <любой номер>)`, а не константой величины: константа устарела бы от первой
перекрёстной ссылки и ещё раз от раунда 4 — то есть отказала бы ровно тем способом,
который этот патч чинит в указателе на раунды.

**Форма замещающего текста.** Каждая запись `STATE.md` внутри блоков ниже стоит ОДНОЙ
физической строкой, начинающейся ровно с `- 🔴 ` либо `- 🟡 `. Мягких переносов внутри
записи быть не должно: гейты патча и гейты самого `STATE.md` считают номера ВНУТРИ строки
записи, и разваленная на несколько строк запись заставила бы их считать РАЗНОЕ.
Пояснительная проза патча живёт ВНЕ блоков замены — голый номер она называть вправе, и
гейты её не читают именно поэтому.

---

## 1. §Blockers/Concerns — вводная часть 🟡 сводной записи (строка 91)

**Удаляется** — фрагмент строки 91 **от начала строки до (НЕ включая) слов
`**Закрыты планами:**`**, дословно:

```
- 🟡 [Phase 5, код-ревью — сводное распоряжение по обоим раундам] Ревью фазы ПЕРЕЗАПИСЫВАЛОСЬ, и номер без раунда указывает не на ту находку: на диске лежит раунд 3 (`05-REVIEW.md`, итог `critical: 1, warning: 7, info: 5`, коммит `164d210`), раунд 2 читается только из git (`git show 1d29360:.planning/phases/05-tarify/05-REVIEW.md`, итог `critical: 2, warning: 9, info: 7`), раунд 1 — тоже только из git (`git show 4989f7a:.planning/phases/05-tarify/05-REVIEW.md`).
```

**Встаёт вместо этого** (одной физической строкой):

```
- 🟡 [Phase 5, код-ревью — сводное распоряжение по раундам 1 и 2] Имя `05-REVIEW.md` принадлежит ТРЁМ разным документам: файл перезаписывался каждым раундом, номера находок между раундами переназначены, и голый номер без раунда указывает не на ту находку. Каждый раунд адресуется своим неизменяемым коммитом: раунд 1 — `git show 4989f7a:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 2, warning: 10, info: 6`), раунд 2 — `git show 1d29360:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 2, warning: 9, info: 7`), раунд 3 — `git show 164d210:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 1, warning: 7, info: 5`). Полный перечень раундов, включая ещё не написанные, отдаёт команда `git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md` — новейший сверху. Какой из раундов занимает файл сегодня, эта запись НЕ утверждает намеренно: ровно такое утверждение устаревало трижды подряд, и следующая перезапись отберёт имя снова. Распоряжения раундов 1 и 2 — ниже в этой же записи, распоряжения раунда 3 — отдельной записью следом.
```

**Остаток строки 91, начиная со слов `**Закрыты планами:**` и до конца строки,
переносится БЕЗ ЕДИНОГО ИЗМЕНЕНИЯ.** Не перенабирать: правка делается на месте, удалением
головы строки и вставкой новой головы перед `**Закрыты планами:**`. Перечень распоряжений
раундов 1 и 2 — включая пару WR-10 (раунд 1) / WR-07 (раунд 2), записанную ОДНОЙ строкой с
ОДНИМ распоряжением, — не трогается ни на символ и не разъединяется.

**Что изменилось и почему.**

- **Заголовок:** «по обоим раундам» → «по раундам 1 и 2». Раундов теперь три, и «оба» —
  то же самое устаревающее утверждение, только в заголовке. Распоряжения раунда 3 живут
  отдельной записью (раздел 3), поэтому заголовок называет свой предмет точно.
- **Реестр по коммитам:** новейший раунд назван наравне с прежними. Именно этого не
  делали три предыдущие редакции строки — они называли новейший раунд словами «на диске»,
  и каждая перезапись `05-REVIEW.md` делала запись ложной.
- **Самообновляющийся указатель:** перечень раундов отдаёт команда, а не автор записи.
  Раунд 4 не потребует правки этой строки вовсе.
- **Чего в записи НЕТ:** ни одного утверждения о том, что лежит в `05-REVIEW.md`
  сегодня. Формулировка «на диске» из файла уходит целиком — это и есть предмет гэпа.

## 2. §Blockers/Concerns — новая 🔴 запись: сокет Docker у веб-слоя

**Встаёт сразу ПОСЛЕ строки 90** (🔴 запись о блокирующем вызове SDK) и ПЕРЕД строкой 91.
Существующая 🔴 запись о `YooPayment.create()` НЕ трогается и НЕ снимается: она
по-прежнему верна и по-прежнему открыта.

```
- 🔴 [Phase 5, ревью раунда 3 — WR-07 (раунд 3)] `docker-compose.prod.yml:91-92` монтирует `/var/run/docker.sock` в контейнер `web` — это root-equivalent доступ к хосту у процесса, который терминирует `POST /api/billing/webhook`. Аутентификация этого маршрута — IP из заголовка, проставляемого прокси (`app/routes/billing.py:60-103`), и она отключается одной переменной окружения (`YOOKASSA_WEBHOOK_VERIFY_IP=false` — объявленный аварийный выход). Любая ошибка класса RCE на этом пути становится компрометацией ХОСТА, а не контейнера. Находка ПРЕДШЕСТВУЕТ фазе 05: монтирование обслуживает `app/services/wa_container_manager.py`. Фаза её не вносила, но УВЕЛИЧИЛА объём того, что доходит до этого процесса, — самая дорогая покупка продукта теперь идёт через него. Сегодняшний режим находки ревью назвало прямо: **принято никем конкретно**. **Распоряжение:** вынести управление контейнерами из `web` в отдельный воркер, владеющий сокетом, с обращением к нему через существующую очередь Redis; либо поставить перед сокетом фильтрующий прокси (например `tecnativa/docker-socket-proxy`) с разрешёнными только теми командами, которые `wa_container_manager` действительно использует, и отдать `web` адрес прокси вместо сырого сокета. Если ни то ни другое не входит в бюджет — принятый риск с НАЗВАННЫМ ВЛАДЕЛЬЦЕМ, по образцу записи R-03-09 Фазы 3 (§Deferred Items). Владелец: ⟨не назначен — назначает владелец проекта⟩. Кодом план 05-14 не трогает ни `docker-compose.prod.yml`, ни `wa_container_manager`: ревью само называет вынос работой своего размера и предлагает записью назначить владельца.
```

**Почему отдельной 🔴 записью, а не строкой в перечне.** Это единственная находка раунда
3 с тяжестью `high`, и она о доступе к хосту. Заголовок называет РАУНД намеренно: `WR-07`
раунда 2 — негодный формат `PLAN_LIMITS`, совсем другая находка, и её запись уже лежит в
файле строкой 91.

**Место для владельца оставлено видимым пробелом.** Заполняет его человек при применении
патча. Пустой владелец в записи честнее выдуманного: «принято никем конкретно» — это
диагноз ревью, и подделать его закрытие именем наугад значило бы повторить дефект под
видом его исправления.

## 3. §Blockers/Concerns — новая 🟡 запись: распоряжения по находкам раунда 3

**Встаёт сразу ПОСЛЕ строки 91** (сводная запись по раундам 1 и 2, правленная разделом 1)
и ПЕРЕД строкой `- Brownfield-риск: …`.

Одной физической строкой, как соседняя сводная запись: бюджет файла — 145 строк, и строка
на находку обменяла бы один дефект на другой.

**Заголовок обязан быть дословно `🟡 [Phase 5, код-ревью раунда 3 — распоряжения]`:**
проверки задачи 2 ищут эту строку по заголовку и считают номера ВНУТРИ неё.

```
- 🟡 [Phase 5, код-ревью раунда 3 — распоряжения] Раунд 3 читается командой `git show 164d210:.planning/phases/05-tarify/05-REVIEW.md` (итог `critical: 1, warning: 7, info: 5`); каждый номер ниже квалифицирован раундом, потому что между раундами номера переназначены. **Закрыто планом:** `CR-01 (раунд 3)` — оплаченное понижение на истёкшем сроке списывалось, но не выдавалось, а понижение становилось недостижимым навсегда; закрыт планом `05-13`, отдельной строкой §Deferred Items. **Отложены с распоряжением, работа своего размера:** `WR-01 (раунд 3)` — отказ ПОВЫШЕНИЯ незнакомому плану сообщается пользователю словом «понижение»: `switch_is_refused` возвращает `True` с обеих сторон (намеренный fail-closed), а вызывающий отображает любой `True` в один код причины `downgrade`, и человеку говорят неправду плюс советуют дождаться конца срока, что не поможет; распоряжение — разделить код причины, а это требует различить два исхода отказа в самом правиле, владелец не назначен; `WR-07 (раунд 3)` — сокет Docker у веб-слоя: отдельной 🔴 записью выше, распоряжение и владелец там же; `WR-06 (раунд 3)` — план, выпавший из `PLAN_LIMITS`, рисует все четыре оси как «без ограничений», то есть безлимитный тариф, которого у пользователя нет: та же причина, что у отложенного распоряжения WR-10 (раунд 1) / `WR-07 (раунд 2)`, и распоряжение то же — типизированная запись плана, проверяемая при построении `Settings`; ВТОРОГО распоряжения не заводится, действует существующее. **Записаны с распоряжением, кода этим планом не требуют:** `WR-02 (раунд 3)` — умолчание `yookassa_return_url` строит адрес возврата из ОТОБРАЖАЕМОГО ИМЕНИ приложения (`settings.app_name`), и при незаданной переменной платёж создаётся с `return_url = "Broadcaster/billing"`; распоряжение — сделать настройку обязательной для создания платежа либо ввести явный `app_base_url`, и закрепить переменную в `docker-compose.prod.yml` так же, как план 05-07 закрепил `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER`; `WR-03 (раунд 3)` — константа `STATUS_PENDING` мертва (ноль ссылок), а путь записи пишет голый литерал `"pending"`: собственное правило модуля «константами, никогда строковыми литералами» нарушено ровно в том месте, которое оно защищает; распоряжение — либо `status=STATUS_PENDING`, либо удалить константу, но не держать обе формы одного значения; `WR-04 (раунд 3)` — `plan_card.html` зашивает `'free'`, хотя обработчик уже передаёт `free_plan_id` и `balance.html` им пользуется: распоряжение — передать идентификатор в карточку и сравнивать с ним; `WR-05 (раунд 3)` — журнал операций молча обрезается на 20 строках на том же экране, где журнал платежей свой потолок объявляет: распоряжение — объявить потолок или дать постраничный проход, инвариант применяется к обоим журналам или ни к одному; `IN-01 (раунд 3)` — неиспользуемый импорт `text` и ни разу не вызванный `logger` в `billing_service.py`, модуле, выполняющем КАЖДОЕ изменение баланса в продукте: распоряжение — убрать импорт и либо удалить `logger`, либо начать писать им `add_messages`/`reset_free_monthly`; `IN-02 (раунд 3)` — `app/application/billing/__init__.py` пуст, притом что каждый модуль пакета объявляет свои границы отдельно: распоряжение — объявить общее правило границ один раз на уровне пакета; `IN-03 (раунд 3)` — утверждение о границах в `subscription_period.py` верно для того, что модуль ИСПОЛЬЗУЕТ, и неверно для того, что он ВТЯГИВАЕТ транзитивно; у `plan_switch.py` то же утверждение держится AST-тестом `test_the_only_import_of_the_rule_is_the_declared_plan_order`, у этого модуля равноценного теста нет, и утверждение может протухнуть молча: распоряжение — либо равноценный AST-тест, либо ослабить формулировку до фактически гарантируемого; `IN-04 (раунд 3)` — `datetime.now(timezone.utc)` берётся трижды за один рендер (`app/pages/billing.py:214, 223, 331`), и два решения об одной подписке читают два разных момента: распоряжение — снимать момент один раз на входе обработчика и передавать его; `IN-05 (раунд 3)` — фраза отказа существует в двух формулировках, а комментарий рядом утверждает, что соседство удерживает их от расхождения: соседство спасает от ЗАБЫВЧИВОСТИ, но не от расхождения смысла; распоряжение — вывести одну из другой либо признать в комментарии два намеренных регистра. Находки прежних раундов, повторно НЕ открываемые, перечислены таблицей «Previously Dispositioned» ревью раунда 3; их распоряжения лежат в сводной записи по раундам 1 и 2 выше и в §Deferred Items — вторых распоряжений им не заводится.
```

**Тринадцать номеров, все квалифицированы раундом:** `CR-01 (раунд 3)`,
`WR-01 (раунд 3)`, `WR-02 (раунд 3)`, `WR-03 (раунд 3)`, `WR-04 (раунд 3)`,
`WR-05 (раунд 3)`, `WR-06 (раунд 3)`, `WR-07 (раунд 3)`, `IN-01 (раунд 3)`,
`IN-02 (раунд 3)`, `IN-03 (раунд 3)`, `IN-04 (раунд 3)`, `IN-05 (раунд 3)`.

Ни одна находка раунда 3 не осталась без распоряжения. Ни одна не берётся в работу этим
планом: список `missing:` гэпа 2 требует ЗАПИСИ с распоряжением, а не исполнения.

## 4. §Deferred Items — правка строки 119 (`CR-01` раунда 2)

**Удаляется** строка 119 целиком:

```
| Phase 5 | CR-01 ревью раунда 2 — молчаливое понижение тарифа подтверждённым платежом в `_apply_extension` | ✓ Закрыто планом 05-11 (2026-08-16): правило `switch_is_refused` объявлено один раз (`app/application/billing/plan_switch.py`) и читается обеими стадиями, сохранение старшего тарифа пишет `subscription_plan_preserved` уровня `warning`, регрессия стадии применения — `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage`. ⚠️ Вердикт `05-VERIFICATION.md` раунда 2 — `gaps_found` — действует до повторной верификации: запись фиксирует исполненную работу, а не пройденную проверку | 2026-08-16 |
```

**Встаёт вместо этого:**

```
| Phase 5 | CR-01 ревью раунда 2 — молчаливое понижение тарифа подтверждённым платежом в `_apply_extension` | ⚠️ Закрыто планом 05-11 (2026-08-16) НАПОЛОВИНУ, завершено планом 05-13 (2026-08-17). Названное направление закрыто и подтверждено раундом 3 — шесть из семи must_haves плана 05-11 зелёные: правило `switch_is_refused` объявлено один раз (`app/application/billing/plan_switch.py`) и читается обеими стадиями, сохранение старшего тарифа пишет `subscription_plan_preserved` уровня `warning`, регрессия стадии применения — `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage`. Но объединено было ОБЪЯВЛЕНИЕ правила, а не его ВХОДНЫЕ ДАННЫЕ, и седьмой must_have (совпадение докстринга `_extend_subscription` с исполняемым кодом) раунд 3 признал фальсифицированным: стадии разошлись по члену о сроке, чем и была внесена регрессия — оплаченное понижение на ИСТЁКШЕМ сроке не выдавалось. Соседнее направление закрыто планом 05-13 отдельной строкой ниже. ⚠️ Вердикт `05-VERIFICATION.md` — `gaps_found` — действует до повторной верификации: запись фиксирует исполненную работу, а не пройденную проверку | 2026-08-16 |
```

**Почему правится.** Сегодня строка читается как ПОЛНОЕ закрытие. Раунд 3 доказал, что
закрытие было половинным и само внесло регрессию. Оговорка о вердикте СОХРАНЕНА дословно
по смыслу: она оказалась пророческой и остаётся верной. Дата в последней колонке
(`2026-08-16`) — дата отложения, не дата закрытия, и не меняется.

## 5. §Deferred Items — новая строка: `CR-01` раунда 3

**Встаёт в КОНЕЦ таблицы**, сразу после правленной строки 119.

```
| Phase 5 | CR-01 ревью раунда 3 — оплаченное понижение на ИСТЁКШЕМ сроке списывалось, но не применялось; понижение становилось недостижимым навсегда | ✓ Закрыто планом 05-13 (2026-08-17): признак живости оплаченного срока объявлен ОДИН раз — `subscription_is_live` в `app/application/billing/subscription_period.py` (не рядом с правилом, как предлагало ревью: `plan_switch.py` держит AST-проверяемый инвариант единственного импорта, а признаку нужен `normalize_utc`) — и стал ОБЯЗАТЕЛЬНЫМ keyword-only входом `period_is_live` правила `switch_is_refused` БЕЗ значения по умолчанию, поэтому спросить правило, не подав признак, невозможно (`TypeError`, закреплено `test_the_rule_cannot_be_asked_without_the_liveness_term`). Признак снимается ДО сдвига срока; порядок закреплён тестом по синтаксическому дереву `test_the_liveness_is_sampled_before_the_date_moves` (AST-гейт `live=0 < move=1`). Регрессия стадии применения на истёкшем сроке существует и ПАДАЛА до правки: `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage` давал `assert 'pro' == 'basic'`; вместе с ней `test_the_expired_period_writes_no_preserved_plan_warning`. Оба операнда признака проходят через `normalize_utc` (отклонение Rule 2 плана 05-13: naive-`now` на денежном пути поднял бы `TypeError`), закреплено `test_a_naive_now_does_not_break_the_liveness_term`. Суита 1665 → 1681. ⚠️ Вердикт `05-VERIFICATION.md` раунда 3 — `gaps_found` — действует до повторной верификации: запись фиксирует исполненную работу, а не пройденную проверку | 2026-08-17 |
```

**Закрытие названо ИМЕНАМИ — функцией, входом правила, четырьмя тестами и фактическим
значением RED, — а не словом «исправлено»:** имя проверяется грепом, «исправлено» не
проверяется ничем. Имена сверены по `05-13-SUMMARY.md`, а не по тексту плана: плановые
имена могли измениться при исполнении, и одно отклонение (`normalize_utc` обоих операндов)
действительно добавило к закрытию именованный тест, которого план не предусматривал.

## 6. §Decisions — дополнение записи о семантике смены тарифа (строка 80)

**Удаляется** строка 80 целиком. **Встаёт вместо этого** (существующий текст сохранён
дословно, дописан последний блок):

```
- Phase 5: семантика смены тарифа — `upgrade-only` (чекпойнт плана 05-10): повышение разрешено и оплаченный остаток не сжигает, понижение при действующей подписке не предлагается и не принимается. Правило объявлено ОДИН раз (`app/application/billing/plan_switch.py`) и читается ОБЕИМИ стадиями — гардом формы `POST /billing/subscribe` и `_apply_extension` (план 05-11). Названная цена варианта: повышение отдаёт лимиты старшего тарифа на оставшиеся дни младшего бесплатно, и это принято сознательно — применения тарифных лимитов в системе нет (D-08). **Исход на ИСТЁКШЕМ сроке — решение владельца, принятое чекпойнтом плана 05-13: вариант A, идентификатор `apply-after-expiry`.** Истёкший срок снимает отказ на ОБЕИХ стадиях, и оплаченное понижение ПРИМЕНЯЕТСЯ; ветка `refuse-always` (отказывать в продаже всегда) НЕ взята, шесть мест отзыва обещания не тронуты. Следствие для пользователя одной фразой: дождавшись окончания оплаченного срока, он может уйти на младший тариф — и получит именно тот тариф, за который заплатил, так что обещание «оплаченные дни не сгорают» исполняется, а не отзывается. Признак живости срока объявлен один раз (`subscription_is_live`, `app/application/billing/subscription_period.py`) и подаётся в правило обязательным keyword-only входом `period_is_live` (план 05-13).
```

**Почему дописывается, а не заводится новой записью.** У одного решения — одно место.
Сегодняшняя запись описывает ТОЛЬКО случай действующей подписки и об истёкшем сроке
молчит; из этого молчания гэп и вырос. Ответ чекпойнта записан ДОСЛОВНО, вместе с
идентификатором варианта (`apply-after-expiry`), чтобы читатель мог сверить его с
`05-13-SUMMARY.md` без пересказа.

## 7. «Current Position» и frontmatter `progress`

### 7.1. «Current Position» — удаляются строки 31-36

```
Phase: 05 (tarify) — EXECUTING
Plan: 1 of 14
Status: Executing Phase 05
Last activity: 2026-08-17 — Phase 05 execution started

Progress: [████████████████████] 64/64 планов (100%) — Фазы 1–4 из 6 завершены (67% фаз milestone v2.0); Фаза 5 — 12/12, фаза ждёт повторной верификации
```

**Встаёт вместо этого:**

```
Phase: 05 (tarify) — EXECUTING (закрытие гэпов раунда 3)
Plan: 13 of 14
Status: Исполнены планы 05-01…05-13; вердикт `05-VERIFICATION.md` раунда 3 — `gaps_found`. Гэп 1 (оплаченное понижение на истёкшем сроке) закрыт планом 05-13, гэп 2 (реестр раундов ревью и распоряжения раунда 3) закрывается планом 05-14. Отметку `Complete` требованиям возвращает повторная верификация, а не эти планы.
Last activity: 2026-08-17 — план 05-14: реестр раундов ревью и распоряжения раунда 3 записаны в STATE.md

Progress: [███████████████████░] 65/66 планов (98%) — Фазы 1–4 из 6 завершены (67% фаз milestone v2.0); Фаза 5 — 13/14, план 05-14 исполняется
```

### 7.2. frontmatter — удаляются строки 8-17

```yaml
stopped_at: План 05-12 исполнен — волна 9 завершена, фаза ждёт повторной верификации
last_updated: "2026-08-17T03:39:10.234Z"
last_activity: 2026-08-17
last_activity_desc: Phase 05 execution started
progress:
  total_phases: 6
  completed_phases: 4
  total_plans: 66
  completed_plans: 64
  percent: 67
```

**Встаёт вместо этого:**

```yaml
stopped_at: План 05-14 — реестр раундов ревью и распоряжения раунда 3 записаны в STATE.md
last_updated: "2026-08-17T00:00:00.000Z"
last_activity: 2026-08-17
last_activity_desc: "План 05-14: реестр раундов ревью и распоряжения раунда 3 записаны в STATE.md"
progress:
  total_phases: 6
  completed_phases: 4
  total_plans: 66
  completed_plans: 65
  percent: 67
```

Значение `last_updated` заменить фактическим моментом применения (ISO-8601, UTC).

### 7.3. Откуда взяты цифры — и почему они названы источником, а не константой

Источник — `ROADMAP.md`, а не этот патч. На момент подготовки ROADMAP говорит:

- блок Фазы 5, строка `**Plans**`: **`13/14 plans executed`**;
- таблица `## Progress`, строка `| 5. Тарифы |`: **`13/14`**.

Отсюда планов milestone заведено 13 + 15 + 12 + 12 + 14 = **66**, исполнено
13 + 15 + 12 + 12 + 13 = **65**. Frontmatter и текст обязаны говорить ОДНО И ТО ЖЕ: сегодня
frontmatter даёт `66/64`, а строка прогресса — `64/64 (100%)` и «Фаза 5 — 12/12», и обе
цифры не могут быть верны одновременно. Расхождение того же вида уже возникало и
исправлялось планом 05-12 — поэтому проверять надо ОБЕ цифры, а не одну.

**Если на момент применения ROADMAP уже называет `14/14`** (оркестратор засчитал план
05-14 исполненным шагом 5.7 `execute-phase.md`) — поставить `Plan: 14 of 14`,
`66/66 планов (100%)`, `Фаза 5 — 14/14, фаза ждёт повторной верификации`,
`completed_plans: 66` и полосу `[████████████████████]`. Цифра берётся из ROADMAP в момент
применения, а не из этого патча: то же правило, по которому раздел 1 заменил утверждение
автора командой `git log`.

---

## Что этот патч НЕ делает

- **Требований не перемечает.** `.planning/REQUIREMENTS.md` не открывается на запись
  вовсе: `BILL-05`/`BILL-06`/`BILL-07` сняты с `Complete` 2026-08-16 после вердикта
  `gaps_found`, и однажды преждевременная отметка уже была поставлена и отозвана (коммит
  `2124d1e`). Отметку возвращает верификация после подтверждения закрытия гэпов, а не
  план, который эти гэпы закрывает.
- **Кода не касается.** Гэп 2 — организационный: `app/`, `tests/`, `alembic/`,
  `pyproject.toml`, `uv.lock` пусты в диффе плана 05-14. Ни одна находка раунда 3 не
  берётся в работу — все получают ЗАПИСЬ с распоряжением, ровно то, чего требует список
  `missing:`.
- **Сокет Docker не переносит и не проксирует.** Ревью само называет вынос управления
  контейнерами работой своего размера и предлагает записью назначить владельца — патч
  исполняет предложенное, а не подменяет его.
- **Существующую 🔴 запись о блокирующем вызове SDK не снимает.** Дефект открыт, запись
  остаётся видимой. Счёт строк `^- 🔴` растёт с 1 до 2, а не падает: закрытие дефекта не
  подменяется удалением записи о нём.
- **Ручных проверок задачами не делает.** Восемь пунктов `human_verification` и один
  `behavior_unverified_items` из `05-VERIFICATION.md` остаются человеческими: мобильная
  ширина 375px (разметка планом не тронута ни на строку), настоящий платёж в тестовом
  магазине ЮKassa (D-26), первое настоящее уведомление через боевой nginx после выката,
  статус «отклонён» у отменённого платежа (D-27), читаемость формулировок и три
  прохибиции `BILL-05`/`BILL-06`, остающиеся `unresolved`/`judgment`.

## Бюджет файла

Вход — **125 строк**. Добавляются три физические строки: 🔴 запись о сокете Docker
(раздел 2), 🟡 запись распоряжений раунда 3 (раздел 3), строка §Deferred Items о
`CR-01 (раунд 3)` (раздел 5). Правки разделов 1, 4, 6 и 7 идут заменой строк на строки и
числа строк не меняют. Итог — **128 строк** при цели плана 145 и потолке рабочего
процесса 150.

Если бюджет всё же оказался превышен — сокращаются ФОРМУЛИРОВКИ распоряжений, а не
отбрасывается находка: перечень из тринадцати номеров обязан остаться полным.

## Проверки, которые обязаны сойтись на `.planning/STATE.md` после применения

Десять проверок; входные значения измерены на главном дереве в коммите `e6c14a3`.

| # | Проверка | На входе | Обязано стать |
|---|----------|----------|---------------|
| 1 | `grep -Eo 'git show (4989f7a\|1d29360\|164d210):' .planning/STATE.md \| wc -l` | 2 | **3** |
| 2 | `grep -c 'git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md' .planning/STATE.md` | 0 | **>= 1** |
| 3 | `grep -c 'на диске' .planning/STATE.md` | 1 | **0** |
| 4 | `grep '🟡 \[Phase 5, код-ревью раунда 3' .planning/STATE.md \| grep -Eo 'CR-01 \(раунд 3\)\|WR-0[1-7] \(раунд 3\)\|IN-0[1-5] \(раунд 3\)' \| sort -u \| wc -l` | 0 | **13** |
| 5 | `test "$(grep -Eo 'WR-07' .planning/STATE.md \| wc -l)" = "$(grep -Eo 'WR-07 \(раунд [0-9]+\)' .planning/STATE.md \| wc -l)"` | истинно (1 = 1) | **истинно** (ожидаемо 4 = 4) |
| 6 | `grep -c 'WR-07 (раунд 2)' .planning/STATE.md` И `grep -c 'WR-07 (раунд 3)' .planning/STATE.md` | 1 и **0** | **>= 1** и **>= 1** |
| 7 | `grep -Ec 'subscription_is_live\|05-13' .planning/STATE.md` | 0 | **>= 2** |
| 8 | `grep -c '^- 🔴' .planning/STATE.md` | 1 | **>= 2** |
| 9 | `wc -l .planning/STATE.md` | 125 | **<= 145** |
| 10 | `git diff --name-only -- .planning/REQUIREMENTS.md` | пусто | **пусто** |

Семь из десяти на входе КРАСНЫЕ (1, 2, 3, 4, вторая половина 6, 7, 8) — краснотой они и
доказывают, что проверяют работу, а не её отсутствие. Три (5, 9, 10) защитные: они зелёные
на входе и обязаны остаться зелёными, потому что ловят не невыполнение работы, а её
выполнение НЕ ТЕМ способом — сокращением счёта удалением записи, раздуванием файла и
преждевременной отметкой требований.

**Если проверка 5 не сходится, ответ — дописать раунд к голому номеру, а НЕ удалить
вхождение.**
