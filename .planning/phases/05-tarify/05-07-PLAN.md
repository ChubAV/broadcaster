---
phase: 05-tarify
plan: 07
type: execute
wave: 6
depends_on: ["05-06"]
gap_closure: true
files_modified:
  - app/config.py
  - app/routes/billing.py
  - docker-compose.prod.yml
  - tests/test_routes/test_billing_webhook_proxy_headers.py
  - tests/test_routes/test_billing_webhook_source.py
  - .planning/phases/05-tarify/COVERAGE.md
autonomous: true
requirements: [BILL-05, BILL-07]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Подделанный `X-Forwarded-For` не даёт доверия: запрос, прошедший через ProxyHeadersMiddleware с доверием всем пирам (боевой режим `--forwarded-allow-ips=*`), получает 403 и до handle_webhook не доходит (гэп 1)"
    - "Умолчание `yookassa_webhook_client_ip_header` в `app/config.py` — непустое имя заголовка `X-Real-IP`, который nginx проекта ПЕРЕЗАПИСЫВАЕТ на каждом location, а не дописывает"
    - "Пустое значение настройки заголовка означает ОТКАЗ каждому уведомлению, а не доверие адресу пира: отказ по умолчанию закрывает гард, а не открывает его"
    - "Отказ по причине ненастроенного заголовка виден в журнале уровнем error, а не тишиной — молча остановленный приём денег неотличим от отсутствия платежей"
    - "`docker-compose.prod.yml` объявляет `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER` явной записью: безопасное значение стало свойством деплойного артефакта, а не организационной записью в STATE.md"
    - "Настоящее уведомление ЮKassa с доверенным адресом в настроенном заголовке по-прежнему доходит до handle_webhook и возвращает 200 — гард закрыт, а не сломан"
    - "Аварийный выключатель `yookassa_webhook_verify_ip=false` по-прежнему пропускает любой источник и покрыт тестом"
    - "Ни один тест суиты не восстанавливает зелёный цвет отключением гарда: тесты, ходившие в маршрут вебхука на пустой настройке, получают явный доверенный заголовок"
    - statement: "Первое настоящее уведомление ЮKassa после выката проходит гард — боевой nginx действительно проставляет настроенный заголовок именно на маршруте вебхука"
      verification: backstop
  artifacts:
    - "tests/test_routes/test_billing_webhook_proxy_headers.py — регрессия, поднимающая приложение ЧЕРЕЗ ProxyHeadersMiddleware"
    - "app/config.py — непустое умолчание yookassa_webhook_client_ip_header"
    - "docker-compose.prod.yml — явная запись YOOKASSA_WEBHOOK_CLIENT_IP_HEADER в environment"
    - ".planning/phases/05-tarify/COVERAGE.md — сверка строки SecurityHelper.is_ip_trusted после закрытия гэпа"
  key_links:
    - "ProxyHeadersMiddleware(доверие всем пирам) → перезапись адреса пира из левого элемента X-Forwarded-For → _webhook_client_ip → _is_trusted_source → 403"
    - "app/config.py умолчание X-Real-IP → _webhook_client_ip → заголовок, который перезаписывает nginx"
    - "docker-compose.prod.yml environment → Settings → гард на боевом стенде"
  prohibitions:
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT настраивать гард именем заголовка, который прокси лишь ДОПИСЫВАЕТ, а не перезаписывает: декоративная проверка хуже отсутствующей, потому что о ней отчитываются как о защите"
      status: resolved
      verification: test
    - requirement_id: BILL-05
      category: safety
      statement: "MUST NOT останавливать приём настоящих платежей молча — отказ гарда по каждому уведомлению обязан быть виден в журнале, а не выглядеть как отсутствие платежей"
      status: resolved
      verification: test
---

<objective>
Гэп 1 из `05-VERIFICATION.md`: гард источника вебхука написан верно и **инертен в
отгруженной прод-конфигурации**. Пустое умолчание в `app/config.py:94` уводит в ветку
адреса пира, `docker-compose.prod.yml:78` запускает uvicorn с `--forwarded-allow-ips=*`,
и в этой комбинации адрес пира — это ЛЕВЫЙ элемент `X-Forwarded-For`, то есть значение,
которое присылает сам вызывающий. Правильное чтение справа (`app/routes/billing.py:81`)
в проде не исполняется ни разу.

Дыра живая **независимо от D-26**: вебхук пакетов сообщений обслуживается текущей
прод-ревизией `0012`, поэтому подделанное `payment.succeeded` начислит сообщения
сегодня.

Purpose: фаза сняла единственную прежнюю преграду (утечку `yookassa_payment_id` в
браузер) и заменила её ВИДИМОСТЬЮ защиты. Пока отказ по умолчанию открывает гард,
следующий выкат без ручного шага её не соблюдёт.
Output: отказ по умолчанию закрывает гард; регрессия, поднимающая приложение через
настоящий `ProxyHeadersMiddleware`; безопасное значение записано в деплойный артефакт.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@.planning/phases/05-tarify/COVERAGE.md
</context>

<!-- planner-discipline-allow: request.client -->
<!-- planner-discipline-allow: test_without_a_configured_header_the_peer_address_is_used -->

<flagged_assumptions>
**Спек-фазы у `05` по-прежнему нет**, поэтому edge-probe отработал по тексту требований
и вернул восемь применимых строк, все `unresolved`. Строка **BILL-05 / `unclassified`**
не разрешается автоматически ни при каких условиях: классификатор англоязычный, а
BILL-05 написан по-русски, и `unclassified` означает «классификатор не смог прочитать
текст», а не «краёв нет». Она поднимается флагированным допущением в плане `05-08`,
где её края (конкурентная доставка) выведены вручную. Остальные семь строк разнесены по
`must_haves` планов `05-08` и `05-09`; полная сверка с равенством счёта — в `05-09`.

**Допущение этого плана, названное явно:** `--forwarded-allow-ips=*` в
`docker-compose.prod.yml:78` **остаётся как есть**. Флаг обязателен по причине,
выписанной в комментарии над ним: без него `X-Forwarded-Proto: https` молча
отбрасывается и `url_for('static', …)` выдаёт http-ссылки на HTTPS-странице. Сужение
списка доверенных пиров до CIDR docker-сети отклонено: подсеть bridge-сети назначается
динамически, зашитый диапазон однажды разойдётся с реальностью и молча вернёт тот же
баг. Гард закрывается тем, что перестаёт зависеть от адреса пира вовсе, а не тем, что
чинит его происхождение.

**Замечания код-ревью, не подтверждённые верификацией, — распоряжение явное:**

| # | Findings | Распоряжение |
|---|----------|--------------|
| WR-09 | Вебхук не перезапрашивает платёж у ЮKassa (`payments.find_one`) | **Отложено с обоснованием.** `COVERAGE.md` уже приняла по этой возможности решение `OPT-OUT` со словами «tracked as a second anti-spoofing layer for a follow-up phase». Второй слой имеет смысл ПОСЛЕ того, как первый перестал быть декоративным; вводить его в одном плане с закрытием первого значило бы менять две переменные разом на денежном пути. Задача 2 сверяет строку матрицы и оставляет решение в силе с обновлённой причиной. |
| WR-06 | Необработанное уведомление отвечает 200, ЮKassa не повторяет доставку | **Отложено с обоснованием.** Изменение кода ответа вебхука меняет протокол повторных доставок ЮKassa, то есть трогает тот же вход в ту же неделю, что и гард. Отдельная работа со своим наблюдением за счётчиком повторов. **Запись — не здесь:** см. «Владение `.planning/STATE.md`» ниже. |

**Владение `.planning/STATE.md`: этот план его НЕ ПРАВИТ.** Планы `05-07`, `05-08` и
`05-09` идут одной волной 6, и запись CR-01 (`.planning/STATE.md:89`) соседствует со
строкой CR-02 (`:90`), которую в той же секции переписывает задача 3 плана `05-08`.
Две параллельные правки одной секции одного файла — коллизия, а не гипотеза, поэтому
**вся бухгалтерия STATE.md по гэпу 1 перенесена в задачу 3 плана `05-10`** (волна 7),
которая уже собирает сводную запись отложенных находок фазы. Там же — закрытие CR-01 и
запись отложенного WR-06. Здесь `.planning/STATE.md` не значится в `files_modified` и не
правится ни одной задачей; читается он только как контекст.

**Расхождение с записанной строкой D-26, названное прямо (не умолчание).**
`05-CONTEXT.md:80` внутри блока D-26 говорит: «Правок nginx или docker-compose не
требуется». Задача 2 добавляет `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER` в
`docker-compose.prod.yml` — то есть делает ровно то, чего эта строка не требовала.
Противоречия здесь нет: строка называет, что **необходимо**, а не что **запрещено**, и
контракт гэпа 1 предлагает запись в compose как принятое средство наравне с безопасным
умолчанием в `app/config.py` («ЛИБО»). План берёт **оба**: умолчание закрывает гард в
коде, запись в compose закрепляет его в артефакте выката. Но это перекрывает письменную
строку заблокированного решения D-26, и молчание об этом читалось бы недосмотром —
поэтому названо здесь, а не выведено читателем из диффа.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Сквозная линия — подделанный заголовок получает 403 через настоящий ProxyHeadersMiddleware</name>
  <reversibility rating="costly">Непустое умолчание заголовка меняет поведение приёма денег на боевом стенде. Откат — одна правка окружения (`YOOKASSA_WEBHOOK_VERIFY_IP=false`), без миграции и без нарушенного внешнего контракта, поэтому дверь дорогая, а не односторонняя.</reversibility>
  <files>tests/test_routes/test_billing_webhook_proxy_headers.py, app/routes/billing.py, app/config.py</files>
  <read_first>
    - app/routes/billing.py (строки 60-105 — `_webhook_client_ip` и `_is_trusted_source` целиком; строки 106-176 — обработчик вебхука с докстрингом «НАЗВАННАЯ ГРАНИЦА ЗАЩИТЫ», который после правки обязан описывать новое поведение)
    - app/config.py (строки 80-94 — секция `# YooKassa` целиком, включая комментарии над обеими настройками)
    - docker-compose.prod.yml (строки 66-81 — сервис `web`, комментарий над `command` объясняет, ЗАЧЕМ стоит `--forwarded-allow-ips`; строки 1-19 — блок `x-app-base` с перечнем `environment` и строкой `env_file`)
    - .venv/lib/python3.12/site-packages/uvicorn/middleware/proxy_headers.py (весь файл — как именно конструируется middleware при доверии всем пирам и какой элемент списка попадает в адрес пира; конструктор вызывать ровно так же, как это делает uvicorn по флагу)
    - tests/conftest.py (фикстуры `client`, `test_settings`, `db_session` — новый файл строит свой транспорт, но по образцу этой фикстуры и с теми же подменами зависимостей)
    - tests/test_routes/test_billing_webhook_source.py (весь файл — 8 существующих тестов; какие из них останутся верны после правки, а какой фиксирует небезопасную ветку)
    - nginx/nginx.conf.template (какие заголовки прокси ПЕРЕЗАПИСЫВАЕТ, а какие дописывает — от этого зависит, какое имя заголовка безопасно ставить умолчанием)
  </read_first>
  <behavior>
    - RED: приложение, поднятое через ProxyHeadersMiddleware с доверием всем пирам, на `POST /api/billing/webhook` с заголовком `X-Forwarded-For: 77.75.156.11` при пустой настройке заголовка отдаёт 403 и не доходит до `handle_webhook`. Сегодня отдаёт 200 — тест обязан упасть до правки.
    - RED: `Settings(_env_file=None).yookassa_webhook_client_ip_header` равно `X-Real-IP`. Сегодня равно пустой строке — тест обязан упасть до правки.
    - Подделка со списком: `X-Forwarded-For: 77.75.156.11, 203.0.113.7` через тот же middleware — 403.
    - Настроенный заголовок с доверенным адресом (`X-Real-IP: 77.75.156.11`) через тот же middleware — 200, `handle_webhook` вызван ровно один раз.
    - Настроенный заголовок с недоверенным адресом (`X-Real-IP: 203.0.113.7`) при подделанном `X-Forwarded-For: 77.75.156.11` — 403: доверие даёт только настроенный заголовок, никогда адрес пира.
    - Пустая настройка заголовка при `yookassa_webhook_verify_ip=True` — 403 на ЛЮБОЙ запрос, включая запрос с доверенного пира.
    - `yookassa_webhook_verify_ip=False` — 200 при любом наборе заголовков (аварийный выключатель жив).
  </behavior>
  <action>
Одна линия через каждый слой, который держит гард: конфиг → функция определения адреса
→ обработчик маршрута → транспорт теста, поднимающий приложение так же, как это делает
боевой uvicorn. Ценность именно в непрерывности: существующая регрессия зелена сегодня
ровно потому, что её транспорт до middleware не доходит.

**1. Сначала тест (RED).** Новый файл `tests/test_routes/test_billing_webhook_proxy_headers.py`.
Он строит приложение фабрикой `create_app(settings=test_settings)`, ставит те же подмены
зависимостей, что `tests/conftest.py:52-58` (`get_db`, `get_settings`), и **оборачивает
готовое ASGI-приложение в `ProxyHeadersMiddleware` из `uvicorn.middleware.proxy_headers`
с доверием всем пирам** — ровно тем конструктором, который uvicorn вызывает по флагу
`--forwarded-allow-ips=*` (точное имя и форму параметра снять из прочитанного исходника
middleware, а не угадывать по памяти: параметр менял имя между версиями). Транспорт —
`ASGITransport` поверх обёртки, а не поверх приложения. Клиент — `AsyncClient` с тем же
`base_url`, что в conftest.

Докстринг модуля называет, ЗАЧЕМ файл существует отдельно от
`test_billing_webhook_source.py`: тот проверяет ЛОГИКУ гарда на голом приложении, этот —
что логика вообще исполняется в той сборке, которая едет на бой. Оба нужны: суита была
зелёной при открытом проде именно потому, что второго не было.

Адреса взять теми же константами, что в существующем файле: доверенный `77.75.156.11`
(боевая сеть ЮKassa), недоверенный `203.0.113.7` (TEST-NET-3 по RFC 5737 — не станет
боевым адресом ни при каком обновлении SDK). Все семь пунктов `<behavior>` — отдельными
тестами с говорящими именами; два первых обязаны быть КРАСНЫМИ до шага 2. Прогнать и
убедиться, что они красные, ДО правки кода: тест, который не падал на дефекте, дефект и
не ловит.

Отдельным тестом — умолчание настройки: `Settings(_env_file=None)` (и фикстура
`test_settings`, которая его наследует) отдаёт `X-Real-IP`.

**2. `app/config.py`.** Умолчание `yookassa_webhook_client_ip_header` меняется с пустой
строки на `"X-Real-IP"`. Комментарий над настройкой переписывается: пусто больше не
означает «доверять адресу пира» — пусто означает ОТКАЗ, а `X-Real-IP` выбран потому, что
nginx проекта ставит его `$remote_addr`-ом на каждом location, то есть затирает всё,
что прислал клиент. Заголовок, который прокси лишь ДОПИСЫВАЕТ (`X-Forwarded-For` через
`$proxy_add_x_forwarded_for`), сюда ставить нельзя ни при каких условиях, и это
записывается в комментарий как запрет, а не как рекомендация. Настройка
`yookassa_webhook_verify_ip` не трогается.

**3. `app/routes/billing.py`, `_webhook_client_ip`.** Ветка адреса пира удаляется
целиком. Ненастроенное имя заголовка возвращает `None` (то есть «источник не
подтверждён») и пишет `logger.error` с ключом, называющим причину — например
`webhook_ip_header_not_configured`. Уровень именно `error`, а не `warning`: этот отказ
останавливает приём денег полностью, и в потоке предупреждений он потеряется. Логировать
один факт ненастроенности, без тела уведомления (T-05-07).

Чтение настроенного заголовка **не меняется**: правый элемент списка, потому что список
растёт слева направо и свой прокси дописывает реальный адрес справа. Эта логика верна
сегодня и остаётся дословно, вместе со своим докстрингом.

Докстринг `_webhook_client_ip` и раздел «НАЗВАННАЯ ГРАНИЦА ЗАЩИТЫ» в докстринге
обработчика переписываются под новое поведение: назвать, что за `--forwarded-allow-ips=*`
адрес пира — это подконтрольный вызывающему левый элемент `X-Forwarded-For`, и что
поэтому источником адреса он не служит ни при какой настройке. Формулировку про пустую
настройку («так работает локальный запуск без прокси и вся тестовая суита») удалить —
она перестала быть правдой.

**4. Прогнать всю суиту.** Тесты, которые ходили в маршрут вебхука на пустой настройке
и были зелёными за счёт отказа, обязаны получить **явный доверенный заголовок**, а не
отключение гарда: `yookassa_webhook_verify_ip=False` в чужом тесте прячет ровно ту
поломку, которую этот план чинит. Если такой тест найдётся — привести его к явному
заголовку и назвать это в SUMMARY поимённо.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes/test_billing_webhook_proxy_headers.py tests/test_routes/test_billing_webhook_source.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_routes/test_billing_webhook_proxy_headers.py -q` завершается с кодом 0
    - `uv run pytest tests/ -q` завершается с кодом 0
    - `grep -c 'ProxyHeadersMiddleware' tests/test_routes/test_billing_webhook_proxy_headers.py` >= 1
    - `grep -c 'X-Real-IP' app/config.py` >= 1
    - `grep -vc '^\s*#' app/routes/billing.py` — файл не содержит `request.client` ни в одной исполняемой строке: `grep -n 'request\.client' app/routes/billing.py` не даёт вывода
    - `grep -c 'webhook_ip_header_not_configured' app/routes/billing.py` >= 1
    - `grep -c 'logger.error' app/routes/billing.py` >= 1
    - Поведение: приложение в обёртке ProxyHeadersMiddleware с доверием всем пирам, пустая настройка заголовка, запрос с `X-Forwarded-For: 77.75.156.11` → 403, `handle_webhook` не вызван
    - Поведение: та же обёртка, настройка `X-Real-IP`, запрос с `X-Real-IP: 77.75.156.11` → 200, `handle_webhook` вызван ровно один раз
    - Поведение: та же обёртка, настройка `X-Real-IP`, запрос с `X-Real-IP: 203.0.113.7` и `X-Forwarded-For: 77.75.156.11` → 403
    - Поведение: та же обёртка, настройка `X-Real-IP`, список `X-Real-IP: 77.75.156.11, 203.0.113.7` → 403 (читается правый элемент)
    - Поведение: `yookassa_webhook_verify_ip=False` → 200 при любом наборе заголовков
    - Поведение: `Settings(_env_file=None).yookassa_webhook_client_ip_header` равно строке `X-Real-IP`
    - Ни один тест суиты не выставляет `yookassa_webhook_verify_ip=False` иначе как в тесте самого аварийного выключателя: `grep -rn 'yookassa_webhook_verify_ip = False' tests/` даёт ровно одно вхождение
  </acceptance_criteria>
  <done>Подделанный `X-Forwarded-For` больше не даёт доверия ни в одной сборке приложения, включая боевую; отказ по умолчанию закрывает гард; регрессия падает на старом коде и проходит на новом.</done>
</task>

<task type="auto">
  <name>Task 2: Деплойный артефакт и документы догоняют код</name>
  <files>docker-compose.prod.yml, tests/test_routes/test_billing_webhook_source.py, tests/test_routes/test_billing_webhook_proxy_headers.py, .planning/phases/05-tarify/COVERAGE.md</files>
  <precondition>Задача 1 завершена: умолчание `X-Real-IP` уже в `app/config.py`, иначе запись в compose закрепит значение, которого код не знает.</precondition>
  <read_first>
    - docker-compose.prod.yml (строки 1-19 — якорь `x-app-base`: перечень `environment` и строка `env_file`; новая переменная встаёт в тот же перечень тем же стилем)
    - tests/test_routes/test_billing_webhook_source.py (строки 153-169 — тест `test_without_a_configured_header_the_peer_address_is_used`, который закрепляет небезопасную ветку как корректную)
    - .planning/phases/05-tarify/COVERAGE.md (строки 18-41 — матрица; строки 44-93 — сверка с кодом; строка про `SecurityHelper.is_ip_trusted` и третья оговорка внизу)
    - .planning/STATE.md (§Blockers/Concerns — запись CR-01: **читать только**, чтобы понять, какую организационную меру эта работа снимает. Правится она планом `05-10`, задача 3; здесь `.planning/STATE.md` не открывается на запись)
  </read_first>
  <action>
**1. `docker-compose.prod.yml`.** В блок `environment` якоря `x-app-base` (строки 6-18)
добавить строку `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER: ${YOOKASSA_WEBHOOK_CLIENT_IP_HEADER:-X-Real-IP}`
тем же стилем, что соседние ключи. Смысл формы с `:-`: значение из `.env` побеждает,
но при его отсутствии артефакт даёт безопасное умолчание сам — сегодня перечень
переменную не упоминает вовсе, а `env_file: .env` (строка 19) даёт только то, что в
файле уже есть.

Над строкой — короткий комментарий на две-три строки в стиле уже стоящего над `command`:
переменная обязательна ИМЕННО потому, что рядом стоит `--forwarded-allow-ips=*`, из-за
которого адрес пира приходит подконтрольным вызывающему; имя заголовка обязано быть тем,
который nginx ПЕРЕЗАПИСЫВАЕТ. `command` и сам флаг **не трогать** — причина, по которой
он стоит, выписана в существующем комментарии и остаётся в силе.

**2. `tests/test_routes/test_billing_webhook_source.py`.** Тест
`test_without_a_configured_header_the_peer_address_is_used` переписывается: имя, докстринг
и утверждение обязаны говорить, что ненастроенный заголовок — это ОТКАЗ каждому запросу,
а не чтение адреса пира. Новое имя в духе `test_an_unconfigured_header_refuses_every_request`.
Утверждение усиливается: 403 приходит и тогда, когда транспорт пришёл бы с доверенного
адреса, — то есть проверяется отказ, а не совпадение с недоверенным пиром. Прочие семь
тестов файла остаются как есть: они проверяют логику разбора настроенного заголовка и
после правки верны дословно.

**3. Тест на деплойный артефакт.** В `tests/test_routes/test_billing_webhook_proxy_headers.py`
добавить тест, читающий `docker-compose.prod.yml` КАК ТЕКСТ и утверждающий, что имя
переменной `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER` в файле встречается. Докстринг называет,
зачем тест читает деплойный артефакт из суиты: дефект гэпа 1 жил ровно в расхождении
между кодом и артефактом выката, и грепом по коду он был непоймаем. Прецедент чтения
неисполняемого файла как текста в проекте есть — тесты раздела читают `app.css`.

**4. `.planning/phases/05-tarify/COVERAGE.md`.** Матрицу **не перегенерировать**.
Дописать в конец файла раздел «Сверка после закрытия гэпа 1 (план 05-07)» на несколько
строк:
- строка `SecurityHelper.is_ip_trusted` — решение `INTEGRATE` в силе, реализация та же,
  но источник адреса больше не может быть адресом пира; третья оговорка внизу файла
  перестала быть организационной мерой и стала свойством `app/config.py` и
  `docker-compose.prod.yml` — так и записать, старый текст оговорки не удалять, а
  пометить закрытым с указанием плана;
- строка `payments.find_one` — решение `OPT-OUT` **остаётся в силе**, причина
  уточняется: второй слой защиты имеет смысл после того, как первый перестал быть
  декоративным, и вводить его в одном плане с закрытием первого значило бы менять две
  переменные разом на денежном пути (WR-09, отложено с обоснованием);
- ни одна другая строка матрицы этой работой не затронута — сказать это прямо, чтобы
  молчание не читалось как недосмотр.

**`.planning/STATE.md` этой задачей НЕ ПРАВИТСЯ.** Закрытие записи `🔴 [Phase 5, CR-01]`
и запись отложенного WR-06 переехали в задачу 3 плана `05-10` (волна 7): три плана волны
6 иначе писали бы в одну секцию одного файла параллельно. Всё, что нужно `05-10` для этой
записи, обязано попасть в SUMMARY этого плана — назвать поимённо: что умолчание стало
свойством `app/config.py`, что значение закреплено в `docker-compose.prod.yml`, и что
человеческим остался ровно один пункт — проверка, что боевой nginx ставит заголовок
именно на маршруте вебхука (backstop-пункт `must_haves`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes/test_billing_webhook_source.py tests/test_routes/test_billing_webhook_proxy_headers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_routes/test_billing_webhook_source.py tests/test_routes/test_billing_webhook_proxy_headers.py -q` завершается с кодом 0
    - `grep -c 'YOOKASSA_WEBHOOK_CLIENT_IP_HEADER' docker-compose.prod.yml` >= 1
    - `grep -c 'forwarded-allow-ips' docker-compose.prod.yml` == 1 — флаг не удалён и не продублирован
    - `grep -c 'test_without_a_configured_header_the_peer_address_is_used' tests/test_routes/test_billing_webhook_source.py` == 0
    - `grep -c 'docker-compose.prod.yml' tests/test_routes/test_billing_webhook_proxy_headers.py` >= 1
    - `grep -c '05-07' .planning/phases/05-tarify/COVERAGE.md` >= 1
    - `grep -c 'find_one' .planning/phases/05-tarify/COVERAGE.md` == 3 — сегодня ровно 2 (измерено: строка матрицы + строка оговорки), раздел сверки добавляет третье вхождение; `>= 2` было бы выполнено ДО правки и изменение не поймало бы
    - `grep -c 'INTEGRATE' .planning/phases/05-tarify/COVERAGE.md` == 10 — сегодня ровно 9 (измерено: `:20`–`:25`, `:46`, `:51`, `:72`), плюс одно вхождение, которое добавляет шаг 4 этой же задачи строкой «`SecurityHelper.is_ip_trusted` — решение `INTEGRATE` в силе». Число точное, а не нижняя граница: критерий сторожевой («ни одна строка матрицы не потеряна перезаписью») И одновременно ловит правку — та же конструкция, что у `find_one` == 3 выше
    - `.planning/STATE.md` этой задачей не изменён: `git diff --name-only -- .planning/STATE.md` пуст (запись CR-01 закрывается планом `05-10`)
    - Поведение: тест, читающий `docker-compose.prod.yml`, падает, если строку с переменной из файла удалить (проверить руками, вернув строку на место)
  </acceptance_criteria>
  <done>Безопасное значение заголовка задано и кодом, и артефактом выката; ни один тест больше не закрепляет небезопасную ветку как корректную; матрица покрытия говорит о состоянии кода правду, а запись в STATE.md передана плану `05-10` вместе со всем нужным для неё текстом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| кто угодно → `POST /api/billing/webhook` | Неаутентифицированный вход, на котором принимается решение выдать платный ресурс |
| nginx → приложение | Адрес пира теряется на прокси; при `--forwarded-allow-ips=*` он становится значением, подконтрольным вызывающему |
| `.env` / compose → `Settings` | Конфигурация решает, закрыт гард или открыт; отсутствующее значение обязано закрывать |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-30 | Spoofing | `_webhook_client_ip` (`app/routes/billing.py:60-83`) + `docker-compose.prod.yml:78` | critical | mitigate | Задача 1: ветка адреса пира удалена целиком, ненастроенный заголовок возвращает «источник не подтверждён». Недостижимость, а не маловероятность: доверие может дать ТОЛЬКО заголовок, который перезаписывает свой прокси, поэтому подделка `X-Forwarded-For` не имеет пути к `is_ip_trusted` ни в одной сборке. Доказано регрессией через настоящий `ProxyHeadersMiddleware` |
| T-05-31 | Denial of Service | Гард после смены умолчания отвергает все настоящие уведомления | high | mitigate | Задача 1: имя умолчания — `X-Real-IP`, который nginx проекта ставит `$remote_addr`-ом на каждом location (проверено `nginx/nginx.conf.template`); задача 2 закрепляет его в compose. Аварийный выключатель `yookassa_webhook_verify_ip` жив и покрыт тестом. Остаточный риск снят backstop-пунктом ручной проверки первого настоящего уведомления |
| T-05-32 | Repudiation | Отказ гарда из-за ненастроенного заголовка не отличим от отсутствия платежей | medium | mitigate | Задача 1: `logger.error` с отдельным ключом причины. Уровень `error`, а не `warning`, назван в задаче явно — в потоке предупреждений полная остановка приёма денег потерялась бы |
| T-05-33 | Information Disclosure | Тело уведомления с `payment_method.card` в журнале при отказе | medium | mitigate | Задача 1: логируется факт ненастроенности и адрес, тело — никогда; правило унаследовано от T-05-07 и не ослабляется |
| T-05-34 | Tampering | Правка compose ломает передачу схемы https и возвращает Mixed Content | medium | mitigate | Задача 2: `command` и флаг `--forwarded-allow-ips` не трогаются; критерий приёмки требует ровно одно вхождение флага |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает ни одного пакета: `uvicorn` и `yookassa` уже в `uv.lock`, новый тест импортирует установленный `uvicorn`. Аудит легитимности — `05-RESEARCH.md` §Package Legitimacy Audit; задач вида «установить X» в плане нет |
</threat_model>

## Artifacts this phase produces

Символы, файлы и ключи, которых до планов `05-07`…`05-10` в проекте не существовало.
Перечень общий на весь набор гэп-планов и повторяется в каждом из них.

**Файлы (новые):**
- `tests/test_routes/test_billing_webhook_proxy_headers.py` (план 05-07)
- `tests/test_services/test_payment_concurrency.py` (план 05-08)
- `alembic/versions/0018_subscriptions_unique_user.py` (план 05-08, **условно** — по решению чекпойнта)
- `tests/test_migrations/test_0018_subscriptions_unique_user.py` (план 05-08, условно)
- `tests/test_pages/test_billing_payment_errors.py` (план 05-10)

**Alembic revision id (условный):** `0018` (`down_revision = "0017"`) — только при решении
`constraint` чекпойнта плана 05-08

**Функции и исключения (новые):**
- `_claim_payment(db, yookassa_id, new_status, now) -> bool` — `app/services/payment_service.py` (план 05-08)
- `PaymentCreationError` — `app/services/payment_service.py` (план 05-10)
- `_payment_error_message(code)` — `app/pages/billing.py` (план 05-10)

**Ключи журнала (новые):** `webhook_ip_header_not_configured` (05-07),
`webhook_claim_lost` (05-08), `payment_create_failed` (05-10)

**Ключи конфига:** новых нет; меняется УМОЛЧАНИЕ `Settings.yookassa_webhook_client_ip_header`
(пустая строка → `X-Real-IP`)

**Переменные окружения деплоя (новые в артефакте):** `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER`
в `docker-compose.prod.yml`

**Контракт маршрута (новый):** параметр запроса `GET /billing?error=<code>` со значениями
`payment` | `disabled` | `plan` | `package` (план 05-10)

**CSS-селектор (новый):** `[data-plan-cta] .btn` (план 05-09)

**Ограничение схемы (условное):** уникальность активной подписки по `subscriptions.user_id`
(план 05-08, по решению чекпойнта)

<verification>
- `uv run pytest tests/ -q` зелёный целиком
- Ни одного нового пакета в `pyproject.toml`
- `grep -rn 'request\.client' app/routes/billing.py` не даёт вывода
- Флаг `--forwarded-allow-ips` в `docker-compose.prod.yml` остался ровно один
</verification>

<success_criteria>
- Подделанный `X-Forwarded-For` получает 403 в сборке, идентичной боевой
- Отказ по умолчанию закрывает гард: ненастроенный заголовок отвергает каждое уведомление
- Безопасное значение задано и в коде, и в деплойном артефакте
- Аварийный выключатель жив и покрыт тестом
- Ни один тест не закрепляет небезопасную ветку как корректную
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-07-SUMMARY.md` when done
</output>
