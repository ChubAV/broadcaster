---
phase: 05-tarify
plan: 24
type: tdd
wave: 19
depends_on: ["05-22"]
gap_closure: true
files_modified:
  - app/services/payment_service.py
  - app/application/billing/plan_switch.py
  - tests/test_pages/test_billing_payment_errors.py
autonomous: false
requirements: [BILL-05]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Записанный ответ гарда подчиняется тому же правилу истечения, что и ветка правила: при ИСТЁКШЕМ оплаченном сроке записанный отказ снимается, план платежа применяется и следа `subscription_plan_preserved` на этом пути нет — ровно то, что докстринг `_extend_subscription:712-720` утверждает безусловно и чего код `:920-921` не исполняет (`WR-02` раунда 6)"
    - "Ни одно из двух объявлений — `payment_service.py:688` и `plan_switch.py:70` — не утверждает инварианта «план только повышается», если код его не исполняет: либо инвариант принуждается на стадии применения, либо предложение снято из ОБОИХ объявлений и исход понижения назван в докстринге. Что именно — решает владелец, потому что вторая ветвь переоткрывает записанное решение D-28"
    - "Исход, меняющий действующий тариф ВНИЗ, перестаёт происходить БЕЗ СЛЕДА: сегодня ветка отказа не берётся, поэтому `subscription_plan_preserved` не пишется, и понижение не оставляет в журнале ничего"
    - "Позиция ветки ПЕРВОЙ ВСТАВКИ (`_extend_subscription:743-754`) по отношению к записанному отказу названа ЯВНО, а не умолчанием: один и тот же записанный отказ соблюдается при наличии строки подписки и игнорируется при её отсутствии, и причина этой асимметрии сказана на месте"
    - "Решение D-28 («стадия применения читает записанный ФАКТ, а не пересчитывает предикат») не переоткрывается МОЛЧА: конфликт между ним и объявленным правилом `upgrade-only` вынесен владельцу чекпойнтом, а не разрешён планировщиком в пользу одной из сторон"
    - "Существующий зелёный тест, закрепивший D-28 на понижении (`test_a_recorded_answer_survives_a_change_of_the_subscription`, `tests/test_pages/test_billing_payment_errors.py:1755-1800`), либо остаётся зелёным без правок, либо переписан с названной причиной и с указанием, какое решение владельца его отменило. Молча он не меняется ни при каком ответе чекпойнта"
  artifacts:
    - "`app/services/payment_service.py` — блок решения `:918-926` подчиняет записанный ответ правилу истечения; ветка первой вставки называет свою позицию явно; докстринг `_extend_subscription` называет исход понижения"
    - "`app/application/billing/plan_switch.py` — объявление о том, что план только растёт, приведено к тому, что исполняется"
    - "`tests/test_pages/test_billing_payment_errors.py` — две регрессии, ПАДАЮЩИЕ на коде до правки: истёкший срок против записанного отказа и исход понижения через окно срока давности"
  key_links:
    - "`_apply_extension:920-921` (чтение записанного ответа) → `period_is_live`, снятый ПЕРВЫМ действием тела. Сегодня второй не участвует в первом вовсе, и именно поэтому записанный `False` переживает собственный период"
    - "чекпойнт задачи 1 → задача 3 и существующий тест D-28 (`:1755`). Односторонняя цепочка: обе ветви ответа меняют либо код, либо объявление, и одна из них отменяет зелёный тест — выбирать между записанным решением владельца и объявленным правилом планировщик не вправе"
    - "`app/models/payment.py:40-46` и `alembic/versions/0019_payment_switch_authorized.py:33-36` → значение `False` колонки. Оба ЯВНО требуют, чтобы колонка умела его выразить, то есть проект сам объявляет это значение живым контрактом; недостижимость через форму сегодня — свойство гарда (`billing.py:348-349`), а не колонки"
    - "окно СРОКА ДАВНОСТИ (`_open_subscription_intents`, `PENDING_INTENT_TTL_HOURS`) → достижимость понижения. Окно открыто, названо открытым в трёх местах и подтверждено собственным зелёным тестом `test_a_stale_intent_does_not_block_a_new_one`; закрывать его этот план не вменяется, но его цена растёт вторым следствием"
  prohibitions:
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT менять действующий тариф человека ВНИЗ, не оставив в журнале ни одной записи об этом: исход, по которому к нам придёт человек, обязан быть разбираем по журналу, а не восстанавливаем по догадке"
      status: unresolved
      verification: test
---

<objective>
**Гэп 2 отчёта `05-VERIFICATION.md` раунда 6, пункты `missing:` №2, №3 и №4 — два инварианта
денежного пути, опровергнутые прогоном.**

**(1) «ПЛАН ТОЛЬКО ПОВЫШАЕТСЯ».** Два объявления называют это инвариантом:
`payment_service.py:688` («Но ПЛАН при этом только повышается») и `plan_switch.py:70` («срок
двигается всегда, план только растёт»). Верификация опровергла оба прогоном: подписка `pro` с
живым сроком, приходит подтверждение платежа `basic` с записанным разрешением `True`
(достижимо через окно СРОКА ДАВНОСТИ, которое сам проект называет детерминированным и держит
зелёным тестом) — итог `plan == 'basic'`. Пользователь ПОНИЖЕН, и следа нет: ветка отказа не
бралась, поэтому `subscription_plan_preserved` не пишется. Деньги примерно сохраняются
конверсией (30 дней Pro → 98 дней Basic), поэтому это дефект корректности и наблюдаемости, а
не утечка, — но объявленное правило не исполняется, а объявления утверждают, что исполняется.

**(2) «ИСТЁКШИЙ СРОК СНИМАЕТ ОТКАЗ НА ОБЕИХ СТАДИЯХ».** Докстринг `_extend_subscription:712-720`
утверждает это БЕЗУСЛОВНО и добавляет, что на этом пути «следа `subscription_plan_preserved`
НЕТ». Верификация опровергла прогоном: платёж с записанным `switch_authorized = False` на
ИСТЁКШЕМ сроке оставляет план прежним и ПИШЕТ `subscription_plan_preserved` — потому что
`:920-921` читает записанный ответ, ни разу не спросив `period_is_live`. Значение `False`
через форму сегодня недостижимо, но `app/models/payment.py:40-46` и `0019:33-36` оба ЯВНО
требуют, чтобы колонка умела его выразить.

**(3) ВЕТКА ПЕРВОЙ ВСТАВКИ** (`:743-754`) применяет план платежа безусловно и не читает
`switch_authorized` вовсе: один и тот же записанный отказ соблюдается при наличии строки
подписки и игнорируется при её отсутствии.

**Найдено при планировании и вынесено владельцу отдельно.** Первый пункт нельзя закрыть
принуждением инварианта, не переоткрыв решение D-28: существующий ЗЕЛЁНЫЙ тест
(`tests/test_pages/test_billing_payment_errors.py:1755-1800`) утверждает ровно
противоположное — «сделка продана под обещание понижения и обязана быть выдана», и это и есть
содержание D-28. Выбирать между записанным решением владельца и объявленным правилом
планировщик не вправе, поэтому задача 1 — чекпойнт.

Purpose: объявление, которое можно проверить прогоном, обязано совпадать с прогоном; а
конфликт двух записанных решений обязан разрешаться владельцем, а не тем, кто первым правит
файл.
Output: записанный ответ подчинён правилу истечения; конфликт `upgrade-only` × D-28 разрешён
владельцем и исполнен; понижение перестало происходить без следа; позиция ветки первой вставки
названа.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@app/services/payment_service.py
@app/application/billing/plan_switch.py
</context>

<flagged_assumptions>

**Зонд краевых случаев: три требования фазы остаются `unclassified` / `unresolved`.**
Допущения планировщика E-01 (BILL-05), E-02 (BILL-06), E-03 (BILL-07) записаны планом `05-15`
и повторены планом `05-22`; здесь не дублируются построчно и не снимаются. Равенство: **3
поднятых == 0 внесённых в `must_haves` + 3 явных допущения.**

**Прохибиций сминчено ОДНА**, записана в `must_haves.prohibitions`, вынесено допущениями
ноль. Равенство: **1 == 1 + 0.** Прохибиции о подлинности уведомления и о PII в журнале —
КАНОН, покрыты `<threat_model>` и `/gsd-secure-phase`, здесь не минтятся.

**Строки контракта `05-UI-SPEC.md` не переоткрываются.** Разметка не трогается. Строка C2
(«Понижение плана при продлении», resolved/explicit) названа ЗАТРОНУТОЙ по существу: этот
план решает, исполняется ли объявленная ею семантика `upgrade-only` на стадии применения. Она
лифтится планом `05-22` простой строкой в `must_haves.truths` и вторым экземпляром здесь не
заводится — две формулировки одной истины и есть предмет гэпа 2.

**Окно СРОКА ДАВНОСТИ этим планом НЕ закрывается.** Оно числится записанным долгом фазы, и
`Gaps Summary` раунда 6 называет его прямо. План делает достижимое через него ПОНИЖЕНИЕ
видимым и/или невозможным — но самого окна не трогает: закрытие окна означает снятие намерения
с оплаты при истечении TTL, то есть работу своего размера с денежными последствиями. Названо
явно, а не пропущено молча.

**Остальной записанный долг фазы не вменяется и не закрывается**: частичный уникальный индекс
на незакрытое намерение, невыкаченная `0018`, D-26, D-08/BILL-02, D-27, отсутствие
RED-коммитов у `05-15` и `05-17`, мобильная проверка на 375px.

**Ревизий Alembic план не заводит, пакеты не устанавливаются.** Колонка `switch_authorized`
уже существует (ревизия `0019`); её тип и нулевость не меняются, поэтому гейт `05-20`
(`tests/test_migrations/test_model_matches_head.py`) обязан остаться зелёным и проверяется
критерием приёмки.

</flagged_assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Конфликт «план только растёт» × записанное решение D-28 — решение владельца</name>
  <reversibility rating="one-way">Одна из двух ветвей отменяет записанное решение владельца (D-28) и переписывает зелёный тест, который его закрепляет; вторая снимает объявленное правило продукта из двух мест. Обе меняют то, что получает покупатель, чей платёж подтверждается после смены состояния подписки. Откат после выката означает второе изменение исхода у людей, уже оплативших.</reversibility>
  <decision>Что происходит, когда подтверждается платёж МЛАДШЕГО тарифа, несущий записанное разрешение `True`, а подписка к этому моменту стала СТАРШЕЙ и живой</decision>
  <context>
Два записанных решения владельца требуют в этой точке разного, и до раунда 6 конфликт не был
назван.

**D-28 (чекпойнт плана `05-15`)** говорит: стадия применения читает записанный ФАКТ — что
было ПРОДАНО этому платежу, — а не пересчитывает предикат по изменившейся строке подписки.
Причина выбора записана прямо: пока ответ нигде не сохранялся, стадия принимала решение
ЗАНОВО и выдавала не то, что было продано. Это закреплено ЗЕЛЁНЫМ тестом
`tests/test_pages/test_billing_payment_errors.py:1755-1800`, чьё сообщение об ошибке гласит:
«сделка продана под обещание „понижение будет применено“ и НЕ выдана».

**Объявление `upgrade-only` (чекпойнт плана `05-10`)** говорит: план только растёт. Оно
записано в двух местах кода как ИНВАРИАНТ.

Верификация раунда 6 показала прогоном, что при живом старшем сроке исполняется ПЕРВОЕ, а
объявлено ВТОРОЕ. Достижимость: намерение `basic` заводится, когда подписки нет; через
`PENDING_INTENT_TTL_HOURS` оно перестаёт считаться потолком; заводится и подтверждается
намерение `pro`; затем подтверждается первое. Окно детерминированное, названо открытым в трёх
местах и подтверждено собственным зелёным тестом проекта.

Деньги при обоих ответах примерно сохраняются: понижение конвертирует остаток по отношению
цен (30 дней Pro → 98 дней Basic), отказ выдаёт долю месяца по цене действующего плана. Спор
идёт не о деньгах, а о том, КАКОЕ из двух записанных обещаний исполняется.
  </context>
  <options>
    <option id="record-wins">
      <name>(а) `record-wins` — D-28 остаётся в силе: записанное разрешение исполняется как продано, а объявление «план только растёт» СНИМАЕТСЯ из обоих мест и заменяется описанием настоящего исхода</name>
      <pros>Ни одно записанное решение владельца не отменяется, и ни один зелёный тест не переписывается. Обещание, под которое продана сделка, исполняется — человек получает то, за что заплатил, а не отказ по состоянию, возникшему после оплаты. Правка чисто описательная: поведение денежного пути не меняется ни на день. Понижение перестаёт быть бесследным — оно получает собственную запись журнала уровня `warning`, по которой разбирающий обращение может назвать человеку исход.</pros>
      <cons>Объявленное правило продукта `upgrade-only` перестаёт быть инвариантом и становится правилом ОДНОЙ стадии — стадии намерения. Карточка младшего плана обещает «переход после окончания оплаченного срока», а через окно срока давности переход происходит РАНЬШЕ, при живом старшем сроке; интерфейсного ответа на это планы фазы не заводили. Прохибиция BILL-05 (autonomy) в этой части остаётся `unresolved` не по недосмотру, а по решению.</cons>
    </option>
    <option id="upgrade-only-wins">
      <name>(б) `upgrade-only-wins` — инвариант принуждается на стадии применения: к записанному разрешению добавляется член `switch_is_refused(subscription.plan, db_payment.plan, period_is_live=True)`</name>
      <pros>Объявленное правило продукта становится истинным везде, где объявлено, и оба объявления остаются дословно. Понижение через окно срока давности становится НЕВОЗМОЖНЫМ, а не просто заметным: цена окна перестаёт расти вторым следствием. Исход попадает в существующую ветку отказа, то есть уже несёт `subscription_plan_preserved` с числом выданных дней и ценой-основанием — наблюдаемость достаётся даром.</pros>
      <cons>ОТМЕНЯЕТ записанное решение D-28 в его основном сценарии и переписывает зелёный тест, который его закрепляет (`:1755-1800`): сделка, проданная под обещание «понижение будет применено», не выдаётся, если период за время оплаты ожил. Человек платит за `basic`, получает долю месяца по цене `pro` (1490 / 4900 ≈ 9 дней вместо 98) и остаётся на `pro`, которого не просил. Это ощутимо хуже для покупателя в деньгах — при том что D-28 принимался ровно ради того, чтобы такого не происходило. Требует записать, что D-28 сужен, и назвать чем.</cons>
    </option>
  </options>
  <resume-signal>Выберите: `record-wins` или `upgrade-only-wins`</resume-signal>
  <acceptance_criteria>
    - Ответ владельца записан дословно в SUMMARY плана с идентификатором выбранной ветви
    - **Правило продолжения:** обе ветви исполняются в этом плане — они отличаются составом правки задачи 3, а не её объёмом. Остановки этот чекпойнт не предусматривает
    - При ответе `upgrade-only-wins` в SUMMARY названо прямо, что решение D-28 СУЖЕНО, в чём именно, и что зелёный тест `:1755-1800` переписан с этой причиной, а не удалён
    - Ни одна строка `app/` не изменена до получения ответа: `git diff --name-only -- app/` пуст на момент чекпойнта
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED — две регрессии, воспроизводящие прогоны верификации, падают на коде до правки</name>
  <reversibility rating="reversible">Тесты добавляются; существующие не трогаются в этой задаче.</reversibility>
  <precondition>Задача 1 получила ответ. Форма второй регрессии зависит от ответа, поэтому писать её до чекпойнта нельзя.</precondition>
  <files>tests/test_pages/test_billing_payment_errors.py</files>
  <read_first>
    - tests/test_pages/test_billing_payment_errors.py (строки 1740-1810 — существующий зелёный тест D-28 о понижении, проданном на истёкшем сроке: его посев, порядок подтверждений и сообщение об ошибке. Новая регрессия обязана отличаться от него ПОСЕВОМ, а не переписывать его)
    - tests/test_pages/test_billing_payment_errors.py (строки 1270-1300 — `_seed_expired_subscription`, зеркало `_seed_live_subscription` с прошедшим сроком; строки 655-700 — `_seed_live_subscription`, `_subscription_rows`, `_aware`)
    - tests/test_pages/test_billing_payment_errors.py (строки 1131-1175 — `_seed_subscription_payment` с параметром `switch_authorized`; сегодня значение `False` не подаёт ни один тест суиты)
    - tests/test_pages/test_billing_payment_errors.py (строки 2030-2065 — образец утверждения о журнале через `patch("app.services.payment_service.logger")` и разбор `spy.warning.call_args_list` по имени ключа и полям)
    - app/services/payment_service.py (строки 900-1000 — снятие `period_is_live`, блок решения по записанному ответу, ветка отказа с `subscription_plan_preserved` и её поля `decided_by`, `granted_days`, `price_basis`)
    - app/services/payment_service.py (строки 736-760 — ветка первой вставки `_extend_subscription`; строки 705-735 — докстринг о том, что истёкший срок снимает отказ на обеих стадиях)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 49, 56, 61-63 — формулировка находки, артефакты и пункты `missing:` №2, №3, №4; строки 200-205 — вывод прогонов WR-03 и WR-02, которые регрессии обязаны воспроизвести)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 241-330 — `WR-02` и `WR-03` целиком, включая предложенные правки)
  </read_first>
  <behavior>
    - Регрессия A («истёкший срок снимает записанный отказ»): подписка `pro` с ИСТЁКШИМ сроком плюс подтверждённый платёж `basic`, несущий `switch_authorized=False` → план становится `basic`, срок считается от СЕГОДНЯ (D-04), и записи `subscription_plan_preserved` в журнале НЕТ. На коде до правки план остаётся `pro` и запись ЕСТЬ — оба утверждения падают.
    - Регрессия B, ветвь `record-wins`: подписка `pro` с ЖИВЫМ сроком плюс подтверждённый платёж `basic` с `switch_authorized=True` → план становится `basic` (обещание сделки исполнено), И в журнале есть запись СВОЕГО ключа об изменении действующего тарифа вниз, несущая прежний план, план платежа и источник решения. На коде до правки план становится `basic` (проходит), а записи нет — тест падает на утверждении о журнале.
    - Регрессия B, ветвь `upgrade-only-wins`: тот же посев → план остаётся `pro`, и в журнале есть `subscription_plan_preserved` с `decided_by`, называющим записанный ответ. На коде до правки план становится `basic` — тест падает на утверждении о плане.
    - Ни одна регрессия не называет символа, которого ещё нет: обе утверждают наблюдаемое состояние строки `subscriptions`, перечитанной из БД, и содержимое журнала. Правило раздела «ГЭП 1 РАУНДА 5» соблюдается.
  </behavior>
  <action>
Обе регрессии пишутся в собственный раздел файла с комментарием-заголовком, называющим: предмет
(два инварианта, опровергнутых прогоном верификации раунда 6), ответ чекпойнта задачи 1 и его
идентификатор, и правило утверждений раздела. Раздел ставится ПОСЛЕ существующего раздела о
записанном разрешении, а не внутри него: тот описывает D-28 как он есть, этот — его границы.

**Регрессия A.** Посев: `_seed_expired_subscription(db_session, "pro")`, затем
`_seed_subscription_payment(db_session, "basic", "yoo_basic", switch_authorized=False,
amount="1490.00")`, затем `_confirm`. Утверждения: ровно одна строка `subscriptions`; её
`plan` равен `basic`; её `expires_at` посчитан от сегодня, а не от прошедшего срока — то есть
СТРОГО больше возвращённого посевом истёкшего значения и отличается от него больше чем на
календарный месяц (образец различения — докстринг `_seed_expired_subscription`). Отдельным
утверждением: среди `spy.warning` НЕТ вызова `subscription_plan_preserved`. Докстринг теста
называет, откуда взято значение `False`: `app/models/payment.py` и ревизия `0019` оба явно
требуют, чтобы колонка умела его выразить, — то есть проект сам объявляет его живым
контрактом, и недостижимость через форму сегодня есть свойство гарда, а не колонки.

**Регрессия B.** Посев: `_seed_live_subscription(db_session, "pro", days=25)`, затем
`_seed_subscription_payment(db_session, "basic", "yoo_basic", switch_authorized=True,
amount="1490.00")`, затем `_confirm`. Тело — по ветви ответа чекпойнта, как описано в
`<behavior>`. Докстринг теста называет ПУТЬ достижимости словами отчёта: намерение младшего
тарифа заводится, когда подписки нет; через срок давности оно перестаёт считаться потолком;
соседнее намерение старшего тарифа подтверждается первым; затем подтверждается первое. Посев
воспроизводит КОНЕЧНОЕ состояние этого пути напрямую, а не проигрывает его по шагам, — и в
докстринге сказано, что путь к состоянию закреплён отдельным зелёным тестом проекта
(`test_a_stale_intent_does_not_block_a_new_one`), поэтому воспроизводить его здесь второй раз
не нужно.

**Существующий зелёный тест D-28 (`:1755-1800`) в этой задаче НЕ ТРОГАЕТСЯ ни строкой.** Если
ответ чекпойнта — `upgrade-only-wins`, его переписывает задача 3 вместе с правкой кода, с
названной причиной и с указанием отменяющего решения. Переписать его здесь значило бы
покрасить его до того, как появилось основание.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_payment_errors.py -q -k "expired_lifts or recorded_refusal or demotion or downgrade_trace"</automated>
  </verify>
  <acceptance_criteria>
    - **RED ДОКАЗАН.** До правки `app/` прогон `<verify>` завершается НЕНУЛЕВЫМ кодом; имена упавших тестов и строки `assert` приведены в SUMMARY дословно
    - Красный прогон зафиксирован отдельным коммитом `test(05-24): …`; `git log --oneline -1` до задачи 3 показывает именно его
    - Регрессия A утверждает ОБА следствия — и применённый план, и ОТСУТСТВИЕ записи `subscription_plan_preserved`. Тест, утверждающий только план, не проверяет второй половины утверждения докстринга
    - Значение `False` появилось в суите: `grep -c "switch_authorized=False" tests/test_pages/test_billing_payment_errors.py` возвращает не менее `1` — до плана значение было `0`
    - Существующий тест D-28 не тронут: `git diff -U0 -- tests/test_pages/test_billing_payment_errors.py | grep -c "^-.*продана под обещание"` возвращает `0`
    - Ни одна строка `app/` не изменена этой задачей: `git diff --name-only -- app/` пуст
  </acceptance_criteria>
  <done>Обе регрессии существуют, воспроизводят прогоны верификации раунда 6 и показаны КРАСНЫМИ на коде до правки; красный прогон зафиксирован коммитом.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: GREEN — записанный ответ подчинён истечению, ответ чекпойнта исполнен, позиция ветки первой вставки названа</name>
  <reversibility rating="costly">Правка блока решения меняет исход подтверждённого платежа на двух путях. Схема не трогается, ревизий нет, откат — обратная правка одного условия; но исход у людей, чьи платежи подтвердились между выкатом и откатом, вторым изменением не отменяется.</reversibility>
  <precondition>Задача 2 исполнена и её красный прогон зафиксирован коммитом. Ответ чекпойнта задачи 1 известен и записан.</precondition>
  <files>app/services/payment_service.py, app/application/billing/plan_switch.py, tests/test_pages/test_billing_payment_errors.py</files>
  <read_first>
    - app/services/payment_service.py (строки 900-1000 целиком — снятие `period_is_live`, выход по пустому плану, блок решения по записанному ответу и правилу, ветка отказа с её тремя полями журнала)
    - app/services/payment_service.py (строки 680-735 — докстринг `_extend_subscription`: абзац «ЧТО ИМЕННО ДЕЛАЕТ ПОДТВЕРЖДЁННЫЙ ПЛАТЁЖ» с утверждением о повышении, абзац «ЧТО РЕШАЕТ, ПРИМЕНЯТЬ ЛИ ПЛАН (D-28)» и абзац «ЧТО ПРОИСХОДИТ, КОГДА ОПЛАЧЕННЫЙ СРОК УЖЕ ИСТЁК»)
    - app/services/payment_service.py (строки 736-760 — ветка первой вставки: комментарий о границе переноса уже стоит, комментария о записанном отказе нет)
    - app/application/billing/plan_switch.py (докстринг `switch_is_refused` целиком; особое внимание абзацу о цене отказа на двух стадиях, где стоит утверждение об инварианте)
    - tests/test_pages/test_billing_payment_errors.py (строки 1740-1810 — зелёный тест D-28; правится ТОЛЬКО при ответе `upgrade-only-wins`)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 241-330 — предложенные правки `WR-02` и `WR-03` с готовыми формулировками комментариев)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 61-64 — пункты `missing:` №2, №3 и №4 дословно)
  </read_first>
  <behavior>
    - Условие отказа, вычисляемое из ЗАПИСАННОГО ответа, содержит член `period_is_live`: при истёкшем сроке отказа не возникает независимо от записанного значения.
    - При ответе `upgrade-only-wins` то же условие содержит второй член — вызов правила смены тарифа с явным `period_is_live=True`.
    - Оба объявления инварианта («план только повышается» / «план только растёт») либо остаются дословно и становятся истинными, либо сняты и заменены описанием настоящего исхода — по ветви ответа. Промежуточного состояния (одно снято, второе осталось) не существует.
    - Ветка первой вставки называет свою позицию по отношению к записанному отказу явно.
    - Все существующие зелёные тесты денежного пути остаются зелёными, кроме одного, который отменяется ответом `upgrade-only-wins`, — и он переписывается, а не удаляется.
  </behavior>
  <action>
**Общая правка обеих ветвей — подчинение записанного ответа правилу истечения (`WR-02`).** В
блоке решения условие, выводимое из записанного значения колонки, дополняется членом
`period_is_live`. Рядом — комментарий, называющий: истёкший срок снимает отказ и у записанного
ответа тоже, потому что защищать нечего независимо от того, КТО отказ вынес; без этого члена
записанный `False` пережил бы собственный период, а докстринг `_extend_subscription`
утверждает обратное безусловно; значение сегодня недостижимо через форму, но модель и ревизия
`0019` обе явно требуют, чтобы колонка умела его выразить.

**Правка по ветви `record-wins`.** Из ОБОИХ объявлений снимается предложение об инварианте
роста плана, и на его место встаёт описание настоящего исхода: подтверждённый платёж
исполняется КАК ПРОДАН — записанное разрешение (D-28) исполняется, даже если к моменту
подтверждения подписка стала старшей; правило `upgrade-only` есть правило стадии НАМЕРЕНИЯ, а
не инвариант обеих стадий; путь, на котором происходит понижение, назван (окно срока давности
намерения) и назван открытым записанным долгом. В докстринге `_extend_subscription`
дополнительно называется, ЧТО получает человек в этом исходе: остаток старшего тарифа
конвертируется по отношению цен в дни младшего, то есть деньги примерно сохраняются, а тариф
меняется вниз.
<!-- planner-discipline-allow: план только растёт -->
Понижение перестаёт быть бесследным: перед присваиванием плана добавляется журнальная запись
уровня `warning` со СВОИМ ключом, срабатывающая, когда план платежа НИЖЕ действующего по
объявленному порядку тарифов и срок жив. Поля: прежний план, план платежа, источник решения
(то же значение, что кладётся в `decided_by`) и признак живости срока. Второй ключ здесь
оправдан ровно тем, чем не оправдан в ветке отката (`IN-04`): исход ДРУГОЙ — там тариф
сохранён, здесь изменён вниз, — и складывать их в один ключ значило бы завести третью пару
несравнимых событий.

**Правка по ветви `upgrade-only-wins`.** Условие отказа дополняется вторым членом — вызовом
правила смены тарифа с текущим планом подписки, планом платежа и явным `period_is_live=True`.
Рядом — комментарий, называющий: записанное разрешение не отменяет объявленного правила, оно
снято ДО того, как появился действующий старший тариф, и понижать по нему значило бы исполнить
сделку, которой на момент подтверждения уже не существует. Оба объявления инварианта остаются
дословно — они становятся истинными. Существующий тест D-28 (`:1755-1800`) переписывается: его
посев остаётся, ожидание меняется на сохранение старшего тарифа, а докстринг ДОПОЛНЯЕТСЯ
абзацем о том, какое решение владельца сузило D-28 и почему; имя теста приводится к тому, что
держит тело. Тест НЕ удаляется — он фиксирует, что было решено тогда.

**Позиция ветки первой вставки (общая для обеих ветвей, `missing:` №4).** В ветку
`_extend_subscription`, заводящую строку подписки, добавляется комментарий, называющий: почему
записанный отказ здесь не консультируется — отказывать нечему, прежнего плана не существует,
и правило смены тарифа сравнивает ранги, а не выдаёт их из воздуха; и что это свойство
СОСТОЯНИЯ, а не упущение, ровно как соседний комментарий о границе переноса. Комментарий
ставится рядом с существующим, а не вместо него.

**Ничего больше не правится.** Ветка отказа, её три поля журнала, ветка конверсии, граница
плана `05-22` и порядок снятия признака живости этой задачей не трогаются.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_payment_errors.py tests/test_services/test_payment_service.py tests/test_services/test_payment_concurrency.py tests/test_application/test_plan_switch.py tests/test_application/test_subscription_period.py -q</automated>
  </verify>
  <acceptance_criteria>
    - **GREEN ДОКАЗАН.** Прогон `<verify>` завершается кодом `0`; те же имена тестов, что падали в задаче 2, теперь проходят — перечислены поимённо в SUMMARY
    - Записанный ответ подчинён истечению: в блоке решения присутствует `period_is_live` в одном выражении с чтением колонки — цитируется в SUMMARY с номером строки. `grep -c "period_is_live" app/services/payment_service.py` увеличился относительно `git show HEAD~1:app/services/payment_service.py | grep -c "period_is_live"`
    - Ветвь ответа исполнена целиком, без промежуточного состояния: при `record-wins` число объявлений инварианта роста плана в `app/services/payment_service.py` и `app/application/billing/plan_switch.py` суммарно равно `0`; при `upgrade-only-wins` оно равно `2` и оба объявления не тронуты диффом. Значение приводится в SUMMARY командой и выводом
    - При `record-wins` понижение оставляет след: новый журнальный ключ встречается в `app/services/payment_service.py` ровно `1` раз и утверждается регрессией B — `grep -c "<ключ>" tests/test_pages/test_billing_payment_errors.py` не менее `1`
    - При `upgrade-only-wins` зелёный тест D-28 переписан, а не удалён: `grep -c "def test_" tests/test_pages/test_billing_payment_errors.py` не уменьшился, и в SUMMARY названы прежнее и новое имя теста с причиной переименования
    - Позиция ветки первой вставки названа: `git diff -- app/services/payment_service.py` показывает добавленный комментарий в диапазоне ветки вставки; цитируется в SUMMARY с номером строки
    - Гейт сверки модели с головой и порядка выката зелёный: `uv run pytest tests/test_migrations/ -q` — код `0`
    - AST-ловушка порядка снятия признака живости зелёная: `uv run pytest tests/test_pages/test_billing_payment_errors.py -q -k "liveness"` — код `0`
    - Ревизий Alembic не заведено: `git status --porcelain alembic/versions/` пуст
    - Полная суита зелёная: `uv run pytest tests/ -q` — код `0`; число пройденных тестов приведено в SUMMARY
  </acceptance_criteria>
  <done>Записанный ответ подчинён тому же правилу истечения, что и ветка правила; ответ владельца по конфликту `upgrade-only` × D-28 исполнен целиком в обоих объявлениях и в коде; понижение перестало быть бесследным либо перестало происходить; позиция ветки первой вставки названа явно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| записанное разрешение на строке платежа → стадия применения | Значение снято в момент продажи и переживает всю сессию оплаты; состояние подписки за это время меняется самим пользователем, соседним платежом или временем |
| окно срока давности намерения → состав одновременных намерений | Детерминированное окно, названное открытым в трёх местах; через него достижимо состояние, которого правило потолка не допускает |
| колонка `payments.switch_authorized` → исход платежа | Значение `False` объявлено живым контрактом моделью и ревизией, недостижимо через форму сегодня и находится в одной правке от достижимости |
| два объявления одного правила в разных модулях → следующий читатель | Инвариант, объявленный дважды и не исполняемый, чинится «в соответствии с комментарием», а не с поведением |
| ветка первой вставки против ветки продления | Один и тот же записанный отказ соблюдается при наличии строки подписки и игнорируется при её отсутствии |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-151 | Elevation of Privilege | Записанный отказ (`switch_authorized = False`) переживает истечение оплаченного срока: план платежа не применяется там, где защищать уже нечего | high | mitigate | Задача 3: условие отказа получает член `period_is_live`; задача 2, регрессия A закрепляет исход и отсутствие записи `subscription_plan_preserved` |
| T-05-152 | Repudiation | Действующий тариф меняется ВНИЗ, не оставляя в журнале ни одной записи: разбирающий обращение не может назвать человеку исход | high | mitigate | Задача 3, ветвь `record-wins`: собственный ключ журнала уровня `warning` перед присваиванием плана; ветвь `upgrade-only-wins`: исход попадает в существующую ветку отказа с её тремя полями |
| T-05-153 | Tampering | Планировщик разрешает конфликт двух записанных решений владельца в пользу одной стороны и переписывает зелёный тест, закрепляющий вторую | high | mitigate | Задача 1: чекпойнт с обеими ветвями, ценой каждой в числах и явным указанием, какая ветвь сужает D-28; задача 2 запрещает трогать существующий тест до ответа |
| T-05-154 | Repudiation | Объявление инварианта роста плана остаётся в одном из двух мест: правка «за компанию» оставляет второе, и следующий раунд находит третью формулировку | high | mitigate | Задача 3: критерий приёмки считает объявления в ОБОИХ файлах суммарно и требует `0` либо `2`, промежуточное состояние проваливает задачу |
| T-05-155 | Elevation of Privilege | Правка условия отказа задевает ветку правила и меняет исход платежей, у которых записанного ответа нет (`NULL`, пакетные и строки старше ревизии `0019`) | high | mitigate | Задача 3: правится только выражение, выводимое из записанного значения; `<verify>` включает `test_payment_service.py` и `test_payment_concurrency.py`; критерий требует зелёной полной суиты |
| T-05-156 | Denial of Service | Новая журнальная запись или новый член условия поднимает исключение на пути уведомления: 5xx запускает цикл повторов ЮKassa | high | mitigate | Задача 3: правило смены тарифа — чистая функция без БД и исключений; журнальная запись повторяет форму существующих; полная суита включает concurrency-тесты |
| T-05-157 | Spoofing | Новый журнальный ключ смешивается с существующими и оператор фильтрует не тот исход | medium | mitigate | Задача 3: ключ новый и единственный, поля названы, причина его отдельности от `subscription_prorating_skipped` записана рядом (исход ДРУГОЙ, а не то же событие) |
| T-05-158 | Information Disclosure | Новая журнальная запись печатает больше полей о пользователе, чем существующие записи модуля | low | accept | Состав полей повторяет `subscription_plan_preserved`: идентификатор пользователя, идентификатор платежа у ЮKassa, два идентификатора плана. Принимается тем же порядком, что для существующих записей |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов: `git diff --name-only -- pyproject.toml uv.lock` пуст. Аудит легитимности — `05-RESEARCH.md`, `## Package Legitimacy Audit` |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-24`.

**Файлы (новые):** нет

**Ключи журнала (новые), `app/services/payment_service.py`** — ровно ОДИН и только при ответе
`record-wins`: ключ уровня `warning`, называющий изменение действующего тарифа ВНИЗ по
записанному разрешению. Поля: прежний план, план платежа, источник решения, признак живости
срока. При ответе `upgrade-only-wins` новых ключей нет вовсе

**Имена тестов (новые), `tests/test_pages/test_billing_payment_errors.py`:**
- регрессия «истёкший срок снимает записанный отказ и не оставляет следа сохранения тарифа»
- регрессия «понижение через окно срока давности» — форма и имя по ветви ответа чекпойнта

**Имена тестов (переименованные)** — только при ответе `upgrade-only-wins`: существующий тест
D-28 о понижении, проданном на истёкшем сроке, приводится к тому, что держит его новое тело;
прежнее и новое имя называются в SUMMARY

**Решения (новые):** ответ чекпойнта задачи 1 — записывается номером D-32 планом `05-26`, а не
этим планом

**Функции, константы, колонки БД, Alembic revision id, маршруты, шаблоны, CSS-селекторы,
переменные окружения:** новых нет. `switch_is_refused`, `subscription_is_live`,
`_seed_expired_subscription`, `PENDING_INTENT_TTL_HOURS` — СУЩЕСТВУЮЩИЕ символы

<verification>
- Записанный ответ подчинён правилу истечения; регрессия A зелёная и была красной
- Конфликт `upgrade-only` × D-28 разрешён владельцем, и ответ исполнен ЦЕЛИКОМ — в коде и в
  обоих объявлениях
- Понижение перестало быть бесследным либо перестало происходить, по ветви ответа
- Позиция ветки первой вставки по отношению к записанному отказу названа явно
- Ни один существующий тест не удалён; переписанный — переписан с названной причиной
- Полная суита зелёная; ревизий Alembic не заведено, пакеты не установлены

**Перенесённые человеческие проверки — задачами НЕ становятся:**
- Мобильная ширина 375px — план не трогает разметки
- Настоящий платёж в тестовом магазине ЮKassa
- **Окно срока давности намерения остаётся ОТКРЫТЫМ** — записанный долг фазы; план делает его
  второе следствие видимым либо невозможным, самого окна не трогает
- Три judgment-tier прохибиции фазы остаются `unresolved`; при ответе `record-wins`
  прохибиция BILL-05 (autonomy) остаётся `unresolved` ПО РЕШЕНИЮ, а не по недосмотру, и это
  записывается планом `05-26`
</verification>

<success_criteria>
- Ни одно объявление денежного пути не утверждает инварианта, который опровергается прогоном
- Исход, меняющий тариф человека вниз, разбираем по журналу или невозможен
- Записанное решение D-28 и объявленное правило `upgrade-only` больше не спорят молча: спор
  назван, вынесен владельцу и разрешён его ответом
- Асимметрия двух веток `_extend_subscription` по отношению к записанному отказу названа на
  месте, а не оставлена умолчанием
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-24-SUMMARY.md` when done
</output>
