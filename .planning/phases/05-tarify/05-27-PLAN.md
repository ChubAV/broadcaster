---
phase: 05-tarify
plan: 27
type: execute
wave: 22
depends_on: ["05-26"]
gap_closure: true
files_modified:
  - .planning/phases/05-tarify/05-CONTEXT.md
  - .planning/phases/05-tarify/05-VERIFICATION.md
  - .planning/phases/05-tarify/05-UI-SPEC.md
autonomous: false
requirements: [BILL-05, BILL-06, BILL-07]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Расхождение записей о прохибиции плана `05-01` (гэп 2 раунда 7) закрыто ОТВЕТОМ ВЛАДЕЛЬЦА, а не решением исполнителя: чекпойнт предъявил ровно две диспозиции, названные отчётом верификации, и записанный ответ несёт идентификатор выбранной ветви дословно"
    - "Прочтение прохибиции `05-01` объявлено В ОДНОМ МЕСТЕ — решением D-34 в `05-CONTEXT.md` — и это место названо КАНОНИЧЕСКИМ прямым текстом, чтобы следующий потребитель цитировал его, а не формулировал своё. Второй формулировки того же прочтения этот план не заводит (второй пункт `missing:` гэпа 1 раунда 7)"
    - "При ветви `override-accepted` расхождение записей закрыто МАШИННО ЧИТАЕМОЙ записью: блок `overrides:` во frontmatter `05-VERIFICATION.md` несёт `must_have`, `reason`, `accepted_by` и `accepted_at`, и текст `must_have` совпадает с формулировкой гэпа 2 дословно"
    - "Строка контракта C2 таблицы «Перенесено из фазы» (`05-UI-SPEC.md:377`) перестала утверждать то, что снято решениями D-30 и D-31: «поведение `next_expiry` не изменено ни на строку», «прохибиция 05-01 соблюдена, а не переопределена» и «повышение отдаёт лимиты старшего тарифа на остаток младшего бесплатно». Прежняя редакция НАЗВАНА, а не стёрта"
    - "Ни один исполненный артефакт фазы (`05-01-PLAN.md`…`05-26-PLAN.md` и их SUMMARY) не тронут: расхождение с must_have №8 плана `05-22` закрывается записью D-34 и блоком `overrides:`, а исполненный план объявлен ИСТОРИЧЕСКИМ документом — тем же приёмом, каким это уже сделано для `05-18-SUMMARY.md`"
    - "Цена принятого исключения названа ЧИСЛОМ и в записи D-34: остаток свыше одного календарного месяца в ветке нечитаемой цены не переносится, и предоплативший год теряет около одиннадцати месяцев оплаченного времени. Эпитета без числа недостаточно — за эпитет вместо числа фаза получила раунды 5 и 6"
    - statement: "Человек, прочитавший D-34 и строку C2 после этой волны, называет статус прохибиции `05-01` одним словом и тем же словом, каким его называет код в `capped_carryover`. Совпадение прочтений судится человеком, а не грепом: греп считает вхождения токена, но не понимает, об одном ли предмете два абзаца"
      verification: backstop
  artifacts:
    - "`.planning/phases/05-tarify/05-CONTEXT.md` — решение D-34 своим номером по образцу D-28…D-33: идентификатор выбранной ветви, отвергнутая ветвь с причиной отказа, причина выбора прямым текстом, рейтинг обратимости, названный числом принятый долг и явное объявление записи КАНОНИЧЕСКИМ прочтением прохибиции `05-01`"
    - "`.planning/phases/05-tarify/05-VERIFICATION.md` — блок `overrides:` во frontmatter (ветвь `override-accepted`) либо запись о том, почему ветвь `records-amended` его не заводит"
    - "`.planning/phases/05-tarify/05-UI-SPEC.md` — строка C2 таблицы «Перенесено из фазы» (`:377`), приведённая к D-30/D-31 с названной прежней редакцией"
  key_links:
    - "ответ чекпойнта задачи 1 → запись D-34 → блок `overrides:` → строка C2. Цепочка ОДНОСТОРОННЯЯ и в этом порядке: запись, сделанная до ответа, описывала бы решение, которого владелец не принимал"
    - "запись D-34 (`05-CONTEXT.md`) → три абзаца `payment_service.py`, переписываемые планом `05-30`. План `05-30` ЦИТИРУЕТ прочтение D-34, а не формулирует своё: две формулировки одного прочтения и есть предмет гэпа 1"
    - "`capped_carryover:359-372` (`app/application/billing/subscription_period.py`) → формулировка D-34. Код уже написан по ответу владельца; запись приводится к КОДУ, а не наоборот — правым в споре двух объявлений отчёт признал то, которое совпадает с прогоном"
  prohibitions:
    - requirement_id: BILL-05
      category: safety
      statement: "MUST NOT решать за владельца вопрос, отмеченный отчётом верификации как ⚠️ LOOKS INTENTIONAL: диспозиция расхождения записей принадлежит владельцу, и молчаливый выбор исполнителя есть та же потеря записи, что стёртая запись"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT закрывать дефект удалением записи о нём: прежняя редакция строки C2 и прежнее прочтение прохибиции называются в тексте, а не исчезают из него"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-05
      category: integrity
      statement: "MUST NOT править исполненные артефакты фазы (`05-01`…`05-26` PLAN/SUMMARY): исполненный документ фиксирует, что было решено ТОГДА, и правка стирает причину, по которой мера понадобилась"
      status: unresolved
      verification: judgment
---

<objective>
**Гэп 2 раунда 7: расхождение записей о прохибиции `05-01` закрывается ОТВЕТОМ ВЛАДЕЛЬЦА и
одним каноническим прочтением, разосланным по потребителям записей.**

Отчёт верификации раунда 7 формулирует предмет прямо и снабжает его редким предупреждением:

> ⚠️ ЭТО ВЫГЛЯДИТ НАМЕРЕННЫМ. … ЭТО НЕ ДЕФЕКТ КОДА И НЕ ПОВОД ЧИНИТЬ ПОВЕДЕНИЕ: решение
> принято сознательно, цена названа числом, альтернатива `no-carry` отвергнута с причиной.
> Это НЕЗАКРЫТОЕ РАСХОЖДЕНИЕ ЗАПИСЕЙ.

Расхождение таково. Плану `05-22` его собственный `must_haves` записал строку №8 —
«прохибиция `05-01` остаётся СОБЛЮДЁННОЙ и на ветке отката». Владелец на чекпойнт задачи 1
ТОГО ЖЕ плана ответил `cap-one-month` — форму, которая делает прохибицию ИСКЛЮЧЕНИЕМ и
сжигает у предоплатившего год около одиннадцати месяцев (D-31 записывает это дословно и с
ценой). Исполнен ответ владельца. Свести текст плана с ответом никто не свёл, и сегодня
**три источника называют три разных статуса одной прохибиции**:

| Источник | Статус, который он называет |
|---|---|
| `app/application/billing/subscription_period.py:363-366` (`capped_carryover`) | «ИСКЛЮЧЕНИЕ из прохибиции плана `05-01` … а не её соблюдение» |
| `app/services/payment_service.py:693-695` (`_extend_subscription`) | «тем самым СОБЛЮДЕНА, а не переопределена» |
| `05-UI-SPEC.md:377` (строка C2) и `must_haves` №8 плана `05-22` | «соблюдена, а не переопределена» / «остаётся соблюдённой и на ветке отката» |

Пока их три, следующая правка возьмёт любой из них — и это ровно тот механизм, которым
раунд 7 объяснил появление ЧЕТВЁРТОГО экземпляра класса «объявление против кода».

Этот план закрывает ДОКУМЕНТНУЮ половину: он получает ответ владельца, записывает его
решением D-34 и объявляет эту запись **каноническим прочтением** — единственным местом, где
статус прохибиции `05-01` формулируется. Кодовую половину (три абзаца `payment_service.py`)
закрывает план `05-30`, ЦИТИРУЯ D-34.

Purpose: расхождение, отмеченное как намеренное, закрывается записью и подписью, а не кодом;
и закрывается ОДНИМ прочтением, потому что второе прочтение того же предмета и есть дефект.
Output: ответ владельца, решение D-34 своим номером, блок `overrides:` (либо запись о том,
почему его нет) и приведённая строка C2 контракта интерфейса.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-UI-SPEC.md
@app/application/billing/subscription_period.py
</context>

<flagged_assumptions>

**Зонд краевых случаев: три требования фазы остаются `unclassified` / `unresolved`.**
Классификатор зонда работает по английским формулировкам, а BILL-05, BILL-06 и BILL-07
записаны по-русски; строки вернулись `unclassified`, и это НЕ даёт права снять их
`backstop`-ом и НЕ даёт права уронить их молча. Они поднимаются здесь явными допущениями
планировщика и остаются `unresolved`:

- **E-01 (BILL-05, `unresolved`)** — «продлить текущую подписку»: краевые состояния (нулевой
  остаток, истёкший срок, повтор своего тарифа) зондом не классифицированы. Допущение: их
  семантика записана решениями D-04, D-06, D-30 и D-31 и этим планом не меняется.
- **E-02 (BILL-06, `unresolved`)** — «потребление и остаток по четырём осям»: краевые
  состояния (безлимит, план вне `PLAN_LIMITS`) зондом не классифицированы. Допущение:
  запись `IN-01` раунда 7 их называет, и распоряжение по ней принимает план `05-31`.
- **E-03 (BILL-07, `unresolved`)** — «история платежей»: краевые состояния (усечение журнала
  на `PAYMENT_LIST_CAP`) зондом не классифицированы. Допущение: `IN-05` раунда 7 их
  называет, распоряжение принимает план `05-31`.

Равенство держится дословно: **3 поднятых == 3 записанных допущения == 0 внесённых в
`must_haves.truths`**. Ни одно из трёх не превращается здесь в истину и ни одно не снято.

**Лифт `05-UI-SPEC.md ## UI Considerations`.** Строка C2 таблицы «Перенесено из фазы»
(`:377`) НЕ лифтится как живая истина: раунд 7 назвал её ОТКАЗАВШИМ артефактом. Она
приводится этим планом и лифтится только после — планом `05-31` в составе сверки. Прочие
строки того же раздела (`C1` ✅, `C3` 🧪 backstop) этим планом не задеваются.
</flagged_assumptions>

<artifacts_this_phase_produces>

Новые символы и артефакты, вводимые ЭТИМ планом (нужны для source-grounding: они не «дрейф»,
а вновь созданное):

| Артефакт | Где | Что это |
|---|---|---|
| `D-34` | `.planning/phases/05-tarify/05-CONTEXT.md` | Решение владельца о диспозиции расхождения записей; объявляется КАНОНИЧЕСКИМ прочтением статуса прохибиции `05-01` |
| `override-accepted` | идентификатор ветви чекпойнта | Ветвь: must_have №8 плана `05-22` снят собственным ответом владельца; закрытие — записью |
| `records-amended` | идентификатор ветви чекпойнта | Ветвь: прочтение отвергнуто; закрытие — правкой записей либо поведения, и тогда план останавливается |
| `overrides:` | frontmatter `.planning/phases/05-tarify/05-VERIFICATION.md` | Машинно читаемый блок закрытия must_have с подписью владельца |

Символы кода этот план не вводит и `app/` не трогает.
</artifacts_this_phase_produces>

<reversibility_and_checkpoints>

Решение задачи 1 имеет рейтинг **one-way**: оно снимает объявленный `must_have` исполненного
плана и записывает подпись владельца под принятой ценой в деньгах и днях. Вернуть его позже
означает объявить подпись недействительной. Поэтому задача 1 — `checkpoint:decision`, и
ответ владельца записывается ДОСЛОВНО, идентификатором ветви, а не пересказом.
</reversibility_and_checkpoints>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Владелец называет диспозицию расхождения записей о прохибиции 05-01</name>
  <reversibility rating="one-way">Ответ снимает объявленный `must_have` исполненного плана и ставит подпись под принятой ценой (около одиннадцати месяцев оплаченного времени у предоплатившего год). Записанная подпись становится основанием следующих решений фазы; отозвать её означает объявить недействительными решения, принятые по ней.</reversibility>
  <precondition>Отчёт `.planning/phases/05-tarify/05-VERIFICATION.md` раунда 7 существует, его frontmatter несёт `status: gaps_found` и второй элемент блока `gaps:` с текстом про must_have №8 плана `05-22`. Без отчёта чекпойнт предъявлял бы вопрос, которого никто не задавал.</precondition>
  <read_first>
    - .planning/phases/05-tarify/05-VERIFICATION.md (frontmatter, ВТОРОЙ элемент `gaps:` целиком — `truth`, `reason`, `artifacts`, `missing`; и раздел `### Gaps Summary`, абзац «**Предложение `override` — гэп 2 выглядит намеренным**» с готовым YAML. Формулировка вопроса берётся отсюда дословно, а не пересказывается)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-31 целиком — ответ `cap-one-month`, названная числом цена, четыре отвергнутых варианта. Это и есть ответ владельца, с которым спорит must_have №8)
    - app/application/billing/subscription_period.py (докстринг `capped_carryover`, строки 333-393 — абзац `359-372` называет статус прохибиции словом «ИСКЛЮЧЕНИЕ» и приводит обе половины цены числами)
    - .planning/phases/05-tarify/05-22-PLAN.md (ТОЛЬКО ЧТЕНИЕ, правке не подлежит — строка №8 блока `must_haves.truths`. Нужен её ДОСЛОВНЫЙ текст для формулировки чекпойнта и для поля `must_have` блока `overrides:`)
  </read_first>
  <decision>
Гэп 2 раунда 7: `must_haves.truths` №8 плана `05-22` («прохибиция плана `05-01` остаётся
соблюдённой и на ветке отката: разрешённое повышение остатка не сжигает ни на одной ветке
денежного пути») прямо противоположен ответу владельца `cap-one-month` на чекпойнт задачи 1
ТОГО ЖЕ плана. Исполнен ответ владельца. Чем закрывается расхождение?
  </decision>
  <context>
Измерено верификацией, а не выведено: посев «живой горизонт 365 дней + платёж `pro`» в
ветке отката отдаёт 59 дней от 2026-01-10 и 61 день от сегодняшнего дня — то есть около 336
и около 334 оплаченных дней НЕ ПЕРЕНОСЯТСЯ. Код это признаёт сам
(`subscription_period.py:363-366`: «ИСКЛЮЧЕНИЕ из прохибиции плана `05-01` … а не её
соблюдение»), и D-31 записывает ту же цену словами «теряет около одиннадцати месяцев
оплаченного времени».

Отчёт верификации настаивает: **это не дефект кода и не повод чинить поведение.** Решение
принято сознательно, цена названа числом, альтернатива `no-carry` отвергнута с причиной.
Открытым остаётся ЗАПИСЬ, а не поведение.

⚠️ Ответ на этот вопрос НЕ закрывает гэп 1: три абзаца `payment_service.py` и строка C2
`05-UI-SPEC.md` будут приведены к принятому прочтению в ЛЮБОЙ ветви — отчёт говорит это
прямо («иначе следующий раунд найдёт пятый экземпляр там же, где нашёл четыре»).
  </context>
  <options>
    <option id="override-accepted">
      <name>override-accepted — must_have №8 снят собственным ответом владельца; закрытие записью</name>
      <pros>Соответствует тому, что фактически произошло: владелец выбрал форму, зная её цену, и цена записана числом. Закрытие машинно читаемо (блок `overrides:` с подписью и временем), поведение денежного пути не трогается, верхняя граница предоплаченного горизонта — результат, доставшийся фазе шестью раундами, — остаётся на месте. Исполненные артефакты не правятся.</pros>
      <cons>Прохибиция `05-01` навсегда получает объявленное исключение, и всякий следующий читатель обязан читать её вместе с исключением. Интерфейсного ответа на укоротившийся горизонт фаза по-прежнему не заводит: человек, потерявший одиннадцать месяцев, узнаёт об этом из срока, а не из экрана.</cons>
    </option>
    <option id="records-amended">
      <name>records-amended — прочтение отвергается; расхождение закрывается правкой записей либо поведения</name>
      <pros>Прохибиция `05-01` остаётся без исключений, и «MUST NOT сжигать неистраченный остаток» читается буквально.</pros>
      <cons>Снятие зажима возвращает гэп 1 раунда 6 — величину, которой управляет покупатель, без верхней границы на ветке отката (396 дней Pro за 22 780 ₽ при прейскуранте 63 700 ₽), — то есть отменяет результат, ради которого фаза прошла шесть раундов. Отвергнутая форма `no-carry` отнимала бы ВСЕ оплаченные дни. Изменение поведения этим планом не исполняется: оно требует своего плана, и работа останавливается здесь.</cons>
    </option>
  </options>
  <verify>
    <human-check>
⚠️ **Машинного свидетеля у этой задачи НЕТ, и это сказано прямо, а не обойдено проверкой,
которая всегда зелена.** Прежняя редакция плана ставила здесь
`grep -q 'override' .planning/phases/05-tarify/05-VERIFICATION.md` — а слово `override`
стоит в отчёте УЖЕ СЕГОДНЯ, до всякого ответа владельца: им озаглавлен абзац «Предложение
`override` — гэп 2 выглядит намеренным» и им же открывается готовый YAML в `### Gaps
Summary`. Такая проверка не могла покраснеть ни при каком исходе и не свидетельствовала ни
об ответе, ни о заведённом блоке. Машинное свидетельство появляется в ЗАДАЧЕ 2 вместе с
блоком `overrides:` и проверяется её собственным `<verify>`; ставить его критерием ЗДЕСЬ
значило бы утверждать то, чего эта задача не производит, — ровно тот класс, за который фаза
получила четыре раунда.

Свидетель этой задачи — записанный ДОСЛОВНО идентификатор ветви (`override-accepted` либо
`records-amended`) и, при первой ветви, два полученных значения (`accepted_by`,
`accepted_at`). Он переносится в задачу 2 без пересказа и приводится в SUMMARY.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Ответ владельца получен и записан ДОСЛОВНО одним из двух идентификаторов: `override-accepted` либо `records-amended`
    - При ответе `override-accepted` от владельца получены ДВА дополнительных значения: имя для поля `accepted_by` и момент времени ISO-8601 для поля `accepted_at`. Заполнителя `⟨имя владельца⟩` в дереве не остаётся
    - При ответе `records-amended` задачи 2 и 3 НЕ исполняются: план останавливается и возвращает `## BLOCKED` с названием причины — изменение поведения денежного пути требует своего плана и своего чекпойнта
  </acceptance_criteria>
  <resume-signal>Ответьте `override-accepted` (и назовите имя для `accepted_by` и момент для `accepted_at`) либо `records-amended`</resume-signal>
  <done>Идентификатор ветви получен от владельца и зафиксирован; при `override-accepted` получены также `accepted_by` и `accepted_at`</done>
</task>

<task type="auto">
  <name>Task 2: Ответ записывается решением D-34 и объявляется КАНОНИЧЕСКИМ прочтением; блок overrides: закрывает must_have машинно</name>
  <reversibility rating="costly">Запись становится основанием, которое цитирует план `05-30` и всякий следующий читатель денежного пути. Ошибка здесь не роняет прогон, но переживает его: четыре раунда подряд гэп приходил из расхождения между записанным прочтением и исполняемым кодом.</reversibility>
  <precondition>Задача 1 исполнена, идентификатор ветви равен `override-accepted`. При `records-amended` эта задача не исполняется вовсе.</precondition>
  <files>.planning/phases/05-tarify/05-CONTEXT.md, .planning/phases/05-tarify/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-tarify/05-CONTEXT.md (записи D-28, D-29, D-30, D-31, D-32, D-33 — ОБРАЗЕЦ дословный: состав записи, порядок частей, форма «(гэп N раунда M, форма **(а)**, идентификатор `xxx`, ответ владельца дословно — `xxx`)», форма рейтинга обратимости `— **Reversibility:** …`, форма записи принятого долга «⚠️ Принятый долг назван явно», форма «Форсирующий признак возврата к вопросу»)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-31 целиком, строка `99` — источник числовой цены исключения и перечня отвергнутых форм; D-34 её НЕ переписывает и НЕ дублирует, а ссылается на неё)
    - .planning/phases/05-tarify/05-VERIFICATION.md (frontmatter — место, куда встаёт блок `overrides:`; и `### Gaps Summary`, готовый YAML предложения `override`. ⚠️ ГОТОВЫЙ YAML СЛУЖИТ ОБРАЗЦОМ ФОРМЫ — состава ключей и их порядка, — А НЕ ИСТОЧНИКОМ ТЕКСТА `must_have`: его строка `must_have` («Прохибиция плана 05-01 остаётся соблюдённой и на ветке отката: разрешённое повышение остатка не сжигает ни на одной ветке денежного пути») КОРОЧЕ и сформулирована ИНАЧЕ, чем закрываемый пункт, и копирование её провалило бы сверку. Из готового YAML берётся `reason`; `accepted_by` и `accepted_at` — значения, полученные от владельца в задаче 1)
    - .planning/phases/05-tarify/05-VERIFICATION.md (ВТОРОЙ элемент блока `gaps:` — поле `truth` целиком. ⚠️ ЭТО ЕДИНСТВЕННЫЙ ИСТОЧНИК ТЕКСТА `must_have`, и он длиннее образца из `### Gaps Summary`: он начинается со скобки-цитаты самой прохибиции и называет строку C2 контракта `05-UI-SPEC.md`. Текст поля `must_have` блока `overrides:` обязан совпасть с `gaps[1].truth` дословно, иначе машинное закрытие не свяжется с закрываемым пунктом)
    - app/application/billing/subscription_period.py (абзац `capped_carryover:359-372` — ИСТОЧНИК СЛОВ. Формулировка статуса в D-34 берётся теми же словами, какими её уже сказал код: «ИСКЛЮЧЕНИЕ из прохибиции плана `05-01` … а не её соблюдение»)
  </read_first>
  <action>
Задача пишет ДВЕ записи в двух файлах. Обе — добавление; ни одна существующая строка не
стирается.

**(1) Решение D-34 в `.planning/phases/05-tarify/05-CONTEXT.md`.** Встаёт следующим номером
после D-33, в том же подразделе решений владельца, и повторяет состав D-31 по частям:

- Заголовок с квалификацией: `(гэп 2 раунда 7, диспозиция расхождения записей, идентификатор
  `override-accepted`, ответ владельца дословно — `override-accepted`)`.
- **Что решено:** `must_haves.truths` №8 плана `05-22` СНЯТ ответом владельца `cap-one-month`
  на чекпойнт задачи 1 того же плана. Прохибиция плана `05-01` («MUST NOT сжигать
  неистраченный остаток уже оплаченного периода») имеет РОВНО ОДНО объявленное исключение —
  перенос остатка свыше одного календарного месяца в ветке, где цену любого из двух планов
  прочитать нельзя.
- **⚠️ ЭТА ЗАПИСЬ ОБЪЯВЛЯЕТСЯ КАНОНИЧЕСКИМ ПРОЧТЕНИЕМ.** Отдельным абзацем и прямым текстом:
  статус прохибиции `05-01` формулируется ЗДЕСЬ и нигде больше; всякий потребитель —
  докстринги `app/services/payment_service.py`, строка C2 `05-UI-SPEC.md`, будущие планы —
  ЦИТИРУЕТ это прочтение, а не формулирует своё. Причина названа механикой, а не этикетом:
  вторая формулировка одного предмета есть второй источник истины, и именно из него пришли
  четыре экземпляра класса «объявление против кода» в раунде 7. Названо и то, где прочтение
  уже сказано КОДОМ — `app/application/billing/subscription_period.py:359-372`
  (`capped_carryover`), — и что запись приведена к КОДУ, а не наоборот: спор двух объявлений
  одного денежного пути отчёт закрыл в сторону того, которое совпадает с прогоном.
- **Цена названа числом, а не эпитетом.** Остаток КОРОЧЕ календарного месяца переносится
  целиком и не теряет ни дня. Остаток ДЛИННЕЕ месяца не переносится в части свыше месяца:
  посев «живой горизонт 365 дней + платёж `pro`» отдаёт 59 дней от 2026-01-10 и 61 день от
  сегодняшнего дня, то есть около 336 и около 334 оплаченных дня соответственно НЕ
  ПЕРЕНОСЯТСЯ; D-31 называет ту же цену словами «около одиннадцати месяцев оплаченного
  времени». ⚠️ Ни одно из этих чисел не записывается КОНСТАНТОЙ величины без названной даты
  отсчёта: у каждого числа рядом стоит дата, от которой оно снято, — тем же приёмом, каким
  D-30 записал 396 и 140.
- **Отвергнутая ветвь:** `records-amended` — с причиной отказа одной фразой: снятие зажима
  вернуло бы величину, которой управляет покупатель, без верхней границы на ветке отката, то
  есть отменило бы результат, ради которого фаза прошла шесть раундов.
- **Причина выбора прямым текстом:** расхождение назначено ЗАПИСЯМ, а не поведению; отчёт
  верификации назвал его «⚠️ ЭТО ВЫГЛЯДИТ НАМЕРЕННЫМ» и прямо отказался вменять его коду.
- `— **Reversibility:** one-way` с одной фразой обоснования.
- **⚠️ Принятый долг назван явно:** интерфейсного ответа на укоротившийся горизонт фаза не
  заводила и этот план не заводит; человек узнаёт о потере из срока, а не из экрана. Плюс
  форсирующий признак возврата к вопросу — первое обращение человека, чей горизонт после
  повышения оказался короче ожидаемого.
- **Исполненные артефакты не правятся, и это сказано в записи.** `05-22-PLAN.md` — документ
  ИСТОРИЧЕСКИЙ: он фиксирует, что было объявлено ТОГДА, и правка его `must_haves` стёрла бы
  причину, по которой понадобилась эта запись. Приём тот же, каким фаза уже обошлась с
  `05-18-SUMMARY.md`. Машинное закрытие пункта берёт на себя блок `overrides:`.
- **Оговорка о нумерации — одной фразой, и она обязательна.** `D-34` есть следующий свободный
  номер решений ФАЗЫ 5 после `D-33`, и это верно. Но в этом же файле уже встречаются
  идентификаторы `D-35`, `D-36` и `D-37` (строки `140` и `160`, раздел о перенесённом из
  предшествующих фаз): они **принадлежат Фазе 4** и процитированы здесь как контекст, со своей
  нумерацией. Запись D-34 говорит это теми же словами, чтобы следующий планировщик не прочёл номера
  как занятые и не начал нумерацию Фазы 5 с `D-38`: у Фазы 5 следующий свободный номер после
  D-34 — `D-35` в НУМЕРАЦИИ ФАЗЫ 5, и совпадение с процитированным решением Фазы 4 есть
  следствие того, что нумераций две, а не коллизия.

**(2) Блок `overrides:` во frontmatter `.planning/phases/05-tarify/05-VERIFICATION.md`.**
Встаёт рядом с `deferred:` (верхний уровень frontmatter), формой из `### Gaps Summary`:
ключи `must_have`, `reason`, `accepted_by`, `accepted_at`.

⚠️ **У поля `must_have` РОВНО ОДИН источник, и это НЕ готовый YAML отчёта.** Текст
копируется ДОСЛОВНО из поля `truth` ВТОРОГО элемента блока `gaps:` того же файла. Готовый
YAML в `### Gaps Summary` даёт ФОРМУ (какие ключи и в каком порядке) и текст `reason`, но его
собственная строка `must_have` — пересказ, короче и иначе сформулированный, чем закрываемый
пункт: он опускает и цитату прохибиции, и ссылку на строку C2 `05-UI-SPEC.md`. Взять текст
оттуда значит завести блок, который выглядит закрывающим и не связывается с закрываемым
пунктом, — то есть воспроизвести предмет угрозы T-05-181 в самой мере против неё. Сверка
делается программно (критерий приёмки), а не глазами. `reason` — из готового YAML отчёта с
добавлением ссылки на D-34; `accepted_by` и `accepted_at` — значения, полученные от владельца
в задаче 1. Заполнителей `⟨…⟩` в файле не остаётся.

⚠️ Поле `overrides_applied: 0` во frontmatter отчёта — ЗАПИСЬ РАУНДА 7, снятая верификатором
в момент прогона, и она НЕ правится: правка чужого вердикта задним числом есть та же потеря
записи, что стёртая запись. Блок `overrides:` добавляется РЯДОМ, а расхождение с полем
`overrides_applied` оговаривается одной фразой в самом блоке — счёт применённых
переопределений возвращает следующая верификация, а не этот план.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.planning/phases/05-tarify/05-VERIFICATION.md',encoding='utf-8').split('---')[1]); o=d.get('overrides') or []; assert len(o)==1, o; e=o[0]; assert all(e.get(k) for k in ('must_have','reason','accepted_by','accepted_at')), e; assert '⟨' not in str(e), e; print('overrides ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'D-34' .planning/phases/05-tarify/05-CONTEXT.md` даёт не менее `1`, и запись стоит в подразделе решений владельца следующим номером после D-33
    - Запись D-34 содержит все шесть частей образца D-31: идентификатор ветви, отвергнутую ветвь с причиной, причину выбора, `— **Reversibility:**`, «⚠️ Принятый долг назван явно», «Форсирующий признак возврата к вопросу». Проверяется грепом по каждой из шести подстрок
    - Запись D-34 содержит слово `КАНОНИЧЕСК` (объявление единственного места формулировки) и ссылку `subscription_period.py:359-372` — источник слов
    - Запись D-34 содержит числа `59` и `61` вместе с датами, от которых они сняты (`2026-01-10` и «сегодняшний день»), и не содержит числа выдачи без даты отсчёта
    - Блок `overrides:` разбирается как YAML: команда `<verify>` завершается кодом `0` и печатает `overrides ok`
    - Поле `must_have` блока `overrides:` совпадает с полем `truth` второго элемента `gaps:` ДОСЛОВНО: `python3 -c "import yaml; d=yaml.safe_load(open('.planning/phases/05-tarify/05-VERIFICATION.md',encoding='utf-8').split('---')[1]); assert d['overrides'][0]['must_have']==d['gaps'][1]['truth']; print('match')"` печатает `match`. ⚠️ Этот критерий и есть машинное различение двух источников: текст, взятый из готового YAML `### Gaps Summary`, его НЕ проходит
    - Запись D-34 несёт оговорку о нумерации: `grep -c 'принадлежат Фазе 4' .planning/phases/05-tarify/05-CONTEXT.md` даёт не менее `1`, и фраза стоит внутри записи D-34
    - `git diff --name-only -- .planning/phases/05-tarify/05-22-PLAN.md .planning/phases/05-tarify/05-22-SUMMARY.md` пуст: исполненные артефакты не тронуты
    - `git diff -- .planning/phases/05-tarify/05-VERIFICATION.md | grep -c '^-[^-]'` даёт `0`: во вердикт раунда 7 только добавлено, ничего не удалено
  </acceptance_criteria>
  <done>Решение D-34 записано своим номером и объявлено каноническим прочтением; блок `overrides:` разбирается YAML-ом, несёт подпись владельца и дословно совпадающий текст `must_have`; исполненные артефакты фазы не тронуты</done>
</task>

<task type="auto">
  <name>Task 3: Строка C2 контракта интерфейса перестаёт утверждать то, что снято решениями D-30 и D-31</name>
  <reversibility rating="reversible">Правка одной строки документа контракта; возврат означает восстановление прежней редакции, которая тут же и названа.</reversibility>
  <precondition>Задача 2 исполнена: запись D-34 существует и объявлена каноническим прочтением. Строка C2 обязана ЦИТИРОВАТЬ её, а не формулировать четвёртый статус той же прохибиции.</precondition>
  <files>.planning/phases/05-tarify/05-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/05-tarify/05-UI-SPEC.md (строки 369-379 — раздел «### Перенесено из фазы — зондом не поднято, но не потеряно» и таблица целиком. ⚠️ ПРАВИТСЯ СТРОКА `C2` ЭТОЙ ТАБЛИЦЫ (`:377`, «Понижение плана при продлении»), а НЕ подраздел `### C2 — Метр оси` (`:214`): в документе два разных C2, и раунд 7 назвал отказавшим первый)
    - .planning/phases/05-tarify/05-VERIFICATION.md (первый элемент `gaps:` → `artifacts` → последняя запись про `05-UI-SPEC.md`: там названы ТРИ утверждения строки, подлежащие снятию, дословно. И второй элемент `gaps:` → `missing`, пункт 2 — та же тройка)
    - .planning/phases/05-tarify/05-CONTEXT.md (записи D-30, D-31 и свежая D-34 — источник того, ЧЕМ заменяются снятые утверждения; строка C2 их цитирует и не пересказывает)
  </read_first>
  <action>
Правится ОДНА строка таблицы — `C2` раздела «Перенесено из фазы» (`05-UI-SPEC.md:377`).
Сегодня её колонка «Чем закрывается» утверждает ТРИ вещи, каждая из которых снята:

1. «Повышение разрешено и оплаченный остаток НЕ сжигает» — снято D-31: остаток свыше одного
   календарного месяца в ветке нечитаемой цены не переносится.
2. «прохибиция `05-01` соблюдена, а не переопределена» — снято D-34: прохибиция имеет ровно
   одно объявленное исключение.
3. Формулировки, предполагающие, что поведение отсчёта срока не менялось, и что повышение
   отдаёт лимиты старшего тарифа на остаток младшего бесплатно, — снято D-30: остаток
   конвертируется ПО ОТНОШЕНИЮ ЦЕН (25 дней Basic по 1490 ₽/мес дают 7 дней Pro по
   4900 ₽/мес), то есть бесплатной выдачи больше нет, а точка отсчёта при повышении идёт от
   конвертированной точки, а не от `expires_at`.

Новая редакция строки обязана:

- **назвать прежнюю редакцию, а не стереть её.** Формой «⚠️ ПРЕЖНЯЯ РЕДАКЦИЯ ЭТОЙ СТРОКИ
  УТВЕРЖДАЛА …, И ЭТО СНЯТО РЕШЕНИЯМИ D-30, D-31 И D-34» — тем же приёмом, каким сняты
  прежние редакции абзацев `payment_service.py` планами `05-22` и `05-24`;
- **сослаться на каноническое прочтение**, а не сформулировать четвёртое: статус прохибиции
  `05-01` в строке называется ровно теми словами, которыми его называет D-34, со ссылкой на
  запись;
- **назвать цену числами с датой отсчёта**: 25 дней Basic → 7 дней Pro (конверсия, D-30) и
  365 дней остатка → 59 дней от 2026-01-10 / 61 день от сегодняшнего дня (зажим, D-31);
- **сохранить верную часть строки дословно**: четвёртое состояние CTA карточки младшего
  плана (подпись вместо кнопки), гард `POST /billing/subscribe` с кодом `downgrade` и
  ссылки на файлы разметки правке не подлежат — они верны и проверены;
- **назвать встречный долг, записанный D-32**: карточка младшего плана обещает «переход
  после окончания оплаченного срока», а через окно срока давности он происходит РАНЬШЕ.
  Долг не закрывается здесь, но и не исчезает из строки контракта.

Ни одна другая строка таблицы (`C1`, `C3`) и ни один другой раздел документа не правятся.
  </action>
  <verify>
    <automated>grep -n '^| C2 | \*\*Понижение плана при продлении' .planning/phases/05-tarify/05-UI-SPEC.md | grep -q 'D-34' &amp;&amp; grep -n '^| C2 | \*\*Понижение плана при продлении' .planning/phases/05-tarify/05-UI-SPEC.md | grep -q 'ПРЕЖНЯЯ РЕДАКЦИЯ'</automated>
  </verify>
  <acceptance_criteria>
    - В таблице раздела «Перенесено из фазы» ровно ОДНА строка, начинающаяся с `| C2 |` и содержащая «Понижение плана при продлении»; правится она, и только она
    - Строка C2 содержит подстроки `D-30`, `D-31` и `D-34`: статус прохибиции она ЦИТИРУЕТ, а не формулирует заново
    - Строка C2 содержит `ПРЕЖНЯЯ РЕДАКЦИЯ`: снятые утверждения названы, а не стёрты
    - Строка C2 больше не содержит утверждения «не сжигает» без соседнего указания на границу: `grep -n '| C2 |' .planning/phases/05-tarify/05-UI-SPEC.md` показывает строку, где рядом со всяким обещанием о переносе стоит названная граница в один календарный месяц
    - Строка C2 содержит числа `7` (дней Pro из 25 дней Basic) и `59` вместе с датой отсчёта `2026-01-10`
    - Подраздел `### C2 — Метр оси` (`:214`) не тронут: `git diff -- .planning/phases/05-tarify/05-UI-SPEC.md` не показывает изменений в строках 214-224
    - Число строк таблицы «Перенесено из фазы» не изменилось: `grep -c '^| C[0-9] |' .planning/phases/05-tarify/05-UI-SPEC.md` даёт то же значение, что до правки (`3`)
  </acceptance_criteria>
  <done>Строка C2 таблицы «Перенесено из фазы» приведена к D-30/D-31/D-34, прежняя редакция названа, цена названа числами с датой отсчёта, прочие строки и разделы документа не тронуты</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ответ владельца → запись решения | Записанное не тем идентификатором решение исполняется как другое решение; отменить подпись задним числом нельзя |
| `05-CONTEXT.md` (D-34) → докстринги `payment_service.py` (план `05-30`) | Каноническое прочтение переходит в код цитатой; вторая формулировка того же предмета воспроизводит гэп 1 |
| `05-VERIFICATION.md` → следующая верификация | Блок `overrides:` читается машиной; несовпадение текста `must_have` оставляет пункт незакрытым при видимости закрытия |
| `05-UI-SPEC.md` (C2) → следующий интерфейсный план | Контракт интерфейса, описывающий семантику двух решений назад, заводит требование к экрану по несуществующему правилу |
| исполненные PLAN/SUMMARY → сегодняшнее состояние | Правка исторического документа стирает причину, по которой мера понадобилась |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-180 | Repudiation | Диспозиция расхождения записей выбирается ИСПОЛНИТЕЛЕМ, а не владельцем, при том что отчёт пометил пункт «⚠️ ЭТО ВЫГЛЯДИТ НАМЕРЕННЫМ» | high | mitigate | Задача 1 — `checkpoint:decision` с двумя ветвями отчёта дословно; ветвь `records-amended` ОСТАНАВЛИВАЕТ план, а не исполняется молча |
| T-05-181 | Repudiation | Блок `overrides:` заводится с текстом `must_have`, не совпадающим с закрываемым пунктом: пункт выглядит закрытым и остаётся открытым | high | mitigate | Задача 2: критерий приёмки сверяет `overrides[0].must_have` с `gaps[1].truth` программно, а не глазами |
| T-05-182 | Spoofing | Подпись владельца остаётся заполнителем `⟨имя владельца⟩`, и запись выглядит подписанной, не будучи подписанной | high | mitigate | Задача 1 требует обоих значений от владельца; задача 2 — критерий «символа `⟨` в блоке нет» |
| T-05-183 | Tampering | Правка исполненного `05-22-PLAN.md` стирает объявление, ради расхождения с которым и заведена запись | high | mitigate | Задачи 2 и 3 не трогают исполненные артефакты; критерий требует пустого `git diff --name-only` по ним |
| T-05-184 | Repudiation | D-34 формулирует ЧЕТВЁРТЫЙ статус той же прохибиции вместо того, чтобы стать единственным | high | mitigate | Задача 2: формулировка берётся словами `capped_carryover:359-372`; задача 3 цитирует D-34 со ссылкой; критерии требуют присутствия `КАНОНИЧЕСК` и ссылки на строки кода |
| T-05-185 | Repudiation | Цена принятого исключения записана эпитетом («остаток может сгореть»), и следующий читатель принимает решение по несуществующей величине | high | mitigate | Задачи 2 и 3: критерии требуют чисел `59`/`61`/`7` рядом с датой отсчёта |
| T-05-186 | Tampering | Вердикт раунда 7 правится задним числом (поле `overrides_applied`, тексты `gaps:`), и отчёт перестаёт быть свидетельством | medium | mitigate | Задача 2: критерий требует нуля удалённых строк в диффе `05-VERIFICATION.md`; расхождение с `overrides_applied` оговаривается фразой, а не правкой |
| T-05-187 | Information Disclosure | В `accepted_by` попадают персональные данные сверх имени (адрес почты, телефон) | low | accept | Поле заполняется значением, которое назвал сам владелец; состав значения — его решение. Принимается тем же порядком, что для существующих подписей документа |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов и не трогает манифесты зависимостей: `git diff --name-only` не содержит `pyproject.toml` / `uv.lock` / `package.json`. Гейт легитимности пакетов неприменим — устанавливаемых пакетов ноль |
</threat_model>

<verification>

1. `python3 -c "import yaml; d=yaml.safe_load(open('.planning/phases/05-tarify/05-VERIFICATION.md',encoding='utf-8').split('---')[1]); print(d['overrides'])"` печатает один элемент со всеми четырьмя заполненными полями.
2. `grep -c 'КАНОНИЧЕСК' .planning/phases/05-tarify/05-CONTEXT.md` даёт не менее `1`.
3. `grep -c '^| C[0-9] |' .planning/phases/05-tarify/05-UI-SPEC.md` даёт `3` — строк таблицы столько же, сколько было.
4. `git diff --name-only` содержит РОВНО три файла: `05-CONTEXT.md`, `05-VERIFICATION.md`, `05-UI-SPEC.md`. Ни одного файла `app/`, ни одного файла `tests/`, ни одного исполненного PLAN/SUMMARY.
5. `uv run pytest tests/ -q` завершается тем же кодом, что до плана: код этот план не трогает, и падения тестов означали бы правку не того файла.

</verification>

<success_criteria>

- Ответ владельца получен, записан идентификатором ветви дословно и исполнен без интерпретации.
- Статус прохибиции `05-01` имеет РОВНО ОДНО место формулировки в записях фазы, и это место объявлено каноническим прямым текстом.
- Пункт гэпа 2 закрыт машинно читаемой записью с подписью и временем; текст закрываемого `must_have` совпадает с текстом гэпа дословно.
- Строка C2 контракта интерфейса описывает семантику СЕГОДНЯШНЮЮ, а прежнюю называет.
- Ни один исполненный артефакт фазы не тронут.

</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-27-SUMMARY.md` when done.

В SUMMARY назвать отдельными строками: (1) идентификатор ветви, полученный от владельца,
дословно; (2) номер записанного решения и подтверждение, что оно объявлено каноническим;
(3) результат сверки `overrides[0].must_have == gaps[1].truth`; (4) три утверждения, снятых
со строки C2, и то, чем каждое заменено; (5) подтверждение, что исполненные PLAN/SUMMARY
фазы не тронуты, с выводом `git diff --name-only`.
</output>
