---
phase: 05-tarify
plan: 12
type: execute
wave: 9
depends_on: ["05-11"]
gap_closure: true
files_modified:
  - .planning/phases/05-tarify/05-STATE-PATCH.md
  - .planning/STATE.md
autonomous: false
requirements: [BILL-05, BILL-07]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- Гэп 2, 05-VERIFICATION.md ---
    - "Ни одна 🔴 запись `.planning/STATE.md` больше не описывает ЗАКРЫТЫЙ дефект как открытый: обе записи ревью раунда 1 сняты со ссылкой на закрывшие их планы (05-07, 05-08) и на доказавшие закрытие регрессии"
    - "Блокер `_apply_extension` — молчаливое понижение оплаченного тарифа — записан в STATE.md со ссылкой на план 05-11 и на регрессию, его доказавшую: до этого он существовал только в `05-REVIEW.md` и в отчёте верификации"
    - "Отложенные с обоснованием находки код-ревью собраны ОДНОЙ записью, как объявил план 05-10, и каждая несёт распоряжение — закрыто, принято в работу или отложено с названной причиной"
    - "Идентификаторы находок в записи однозначны: ревью фазы перезаписывалось, и `CR`/`WR`/`IN`-номера РАЗНЫХ раундов означают РАЗНЫЕ находки; каждая запись называет раунд, а не голый номер"
    - "Единственный операционный документ проекта перестал вводить читателя в заблуждение на денежном пути: очередь невыкаченных ревизий названа честно (шесть, включая `0018`), а не пятью"
    - "Раздел «Current Position» упоминает исполненные планы 05-07…05-12, а не остаётся на «Plan 1 of 10»"
    - "Семантика смены тарифа записана решением фазы в §Decisions — сегодня она живёт только в докстрингах и планах"
    - "Артефакт записан В ФАЙЛ, а не обещан во frontmatter: два предыдущих плана объявили `.planning/STATE.md` в `files_modified` и не тронули его ни разу"
    # --- Backstop-пункт ---
    - statement: "Читатель STATE.md, пришедший в проект впервые, понимает из §Blockers/Concerns действительное состояние денежного пути за одну минуту, а не собирает его из четырёх документов фазы — понятность записи судится человеком, а не грепом"
      verification: backstop
  artifacts:
    - ".planning/phases/05-tarify/05-STATE-PATCH.md — готовая к применению замена секций STATE.md, коммитится вместе с планом и переживает снос worktree"
    - ".planning/STATE.md — §Blockers/Concerns без устаревших 🔴, §Deferred Items с закрытыми записями, §Decisions с семантикой смены тарифа, «Current Position» на исполненных планах"
  key_links:
    - "`05-07-SUMMARY.md` / `05-08-SUMMARY.md` / `05-10-SUMMARY.md` (раздел «Бухгалтерия STATE.md — ТАБУЛИРОВАНА, НЕ ЗАПИСАНА») → `05-STATE-PATCH.md` → `.planning/STATE.md`"
    - "`05-REVIEW.md` раунда 2 (на диске) и раунда 1 (только в git, коммит `4989f7a`) → сводная запись отложенных находок"
    - "`05-11-SUMMARY.md` → запись о закрытии блокера `_apply_extension`"
  prohibitions:
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT оставлять единственный операционный документ проекта утверждающим, что закрытый дефект денежного пути открыт — устаревшая 🔴 запись читается как действующее предупреждение и стоит дороже, чем её отсутствие"
      status: resolved
      verification: test
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT объявлять требование Complete раньше, чем верификация подтвердит закрытие гэпов — отметка, поставленная вперёд вердикта, уже была снята однажды и не ставится этим планом"
      status: resolved
      verification: test
---

<objective>
Гэп 2 отчёта `05-VERIFICATION.md`. `.planning/STATE.md` объявлен в `files_modified`
ОБОИХ планов `05-08` и `05-10` и не изменён ни одним: mtime файла — 16.08 11:09, то
есть раньше исполнения `05-08` (12:30) и `05-10` (14:20).

Тяжесть не бухгалтерская. Строки 89 и 90 несут 🔴 записи, содержимое которых теперь
ЛОЖНО: первая утверждает, что `app/config.py:94` даёт пустое значение (сейчас
`X-Real-IP`, строка 108) и что правки docker-compose не нужны (запись
`YOOKASSA_WEBHOOK_CLIENT_IP_HEADER` добавлена, `:27`); вторая — что `select(Payment)`
идёт без блокировки строки и что уникального индекса по `user_id` нет (обе половины
закрыты: `payment_service.py:291`, ревизия `0018`). Единственный операционный документ
проекта СЕЙЧАС говорит читателю, что дыра гарда вебхука и двойное зачисление открыты,
ссылаясь на строки кода, которых в этом виде больше нет, — и не упоминает нового
блокера денежного пути вовсе.

Оба SUMMARY признают пропуск честно («в режиме worktree STATE.md правит оркестратор» —
`05-08-SUMMARY:319`, `05-10-SUMMARY:178-184`), и это не отговорка: механизм назван в
`execute-plan.md` прямо. Значит правильный ответ — не объявить файл третий раз, а
изменить МАРШРУТ записи.

Purpose: артефакт, объявленный дважды и не написанный ни разу, обязан быть либо
написан, либо перестать объявляться. Этот план выбирает первое и ставит на пути
блокирующий гейт, чтобы третьего молчаливого пропуска не случилось.
Output: готовая к применению замена секций, коммитящаяся вместе с планом, и
применённый `.planning/STATE.md`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
</context>

<!-- planner-discipline-allow: 🔴 [Phase 5, CR-01] -->
<!-- planner-discipline-allow: 🔴 [Phase 5, CR-02] -->

<flagged_assumptions>

**Почему это не «ещё раз объявить STATE.md в `files_modified`».** Маршрут записи закрыт
не небрежностью исполнителя, а конфигурацией: `workflow.use_worktrees` = `true`
(`.planning/config.json`). Следствия выписаны в самих рабочих процессах:

- `execute-plan.md:406-410` — исполнитель определяет режим по `[ -f .git ]` и при
  worktree ПРОПУСКАЕТ шаг обновления STATE.md;
- `execute-plan.md:502-509` — при worktree коммит плана ИСКЛЮЧАЕТ `.planning/STATE.md`,
  то есть правка в worktree не коммитится и исчезает вместе с ним;
- `execute-phase.md:721` — в задание исполнителя вписано `IMPORTANT: Do NOT modify
  STATE.md or ROADMAP.md`;
- `execute-phase.md:5.7` — оркестратор после слияния волны действительно коммитит
  `.planning/STATE.md`, но САМ пишет туда только прогресс планов через
  `roadmap.update-plan-progress`; произвольный текст §Blockers/Concerns из SUMMARY он не
  переносит ничем.

Итог: текст, «переданный оркестратору через SUMMARY», не имеет получателя. Именно это
случилось дважды. Поэтому план устроен так:

1. **Твёрдый артефакт.** Задача 1 пишет `05-STATE-PATCH.md` в каталог фазы — файлы
   каталога фазы коммитятся планом ВСЕГДА, в любом режиме. Даже если применение не
   удастся, готовый текст переживёт снос worktree и будет лежать под рукой, а не в теле
   отчёта.
2. **Применение по фактическому дереву.** Задача 1 определяет настоящий корень
   репозитория через `git rev-parse --git-common-dir` и применяет замену к `STATE.md`
   ИМЕННО ТАМ. Из worktree это главный рабочий каталог, и оставленная там правка — ровно
   тот дифф, который оркестратор коммитит шагом 5.7. Попытка объявлена
   **best-effort**: отказ доступа не роняет задачу и не подделывает успех, он
   записывается.
3. **Блокирующий гейт.** Задача 2 — чекпойнт. Он существует не ради церемонии: он
   превращает тихий пропуск в громкую остановку. Два предыдущих раза пропуск был
   незаметен, потому что ничто не стояло на выходе.

**Тип чекпойнта — `checkpoint:human-action`, и это осознанное исключение.** Обычное
правило («не заводить человеческий чекпойнт там, где есть CLI или Write») здесь не
применимо: у исполнителя нет ни одного пути, эффект которого ГАРАНТИРОВАННО переживает
снос worktree, и это доказано двумя прогонами, а не предположением. Чекпойнт снимается
проверкой из четырёх грепов, а не мнением.

**Этот план в волне ОДИН.** Волна 9 состоит из него одного намеренно: исключение
STATE.md из коммита существует, чтобы параллельные исполнители не перезаписывали файл
друг за другом. Единственный писатель этой проблемы не имеет.

**⚠️ ЛОВУШКА НУМЕРАЦИИ, БЕЗ КОТОРОЙ ЗАПИСЬ ПОЛУЧИТСЯ ЛОЖНОЙ.** `05-REVIEW.md`
ПЕРЕЗАПИСЫВАЛСЯ: `git log -- .planning/phases/05-tarify/05-REVIEW.md` даёт два коммита —
`4989f7a` (раунд 1) и `1d29360` (раунд 2, файл на диске). Номера находок между раундами
ПЕРЕНАЗНАЧЕНЫ, и совпадение номеров не означает совпадения находок:

| Номер | Раунд 1 (коммит `4989f7a`, ссылается план 05-10) | Раунд 2 (на диске) |
|-------|--------------------------------------------------|--------------------|
| CR-01 | гард источника вебхука читает адрес пира | молчаливое понижение тарифа в `_apply_extension` |
| CR-02 | двойное зачисление, check-then-act без блокировки | синхронный вызов SDK без таймаута вешает событийный цикл |
| WR-05 | вызов SDK без `try/except` и блокирующий вызов | `GET /api/billing/transactions` без проверки `limit`/`offset` |
| WR-06 | необработанное уведомление отвечает 200 | виджет сайдбара печатает `∞` при нулевом балансе |
| IN-06 | кнопка «Продлить» у истёкшего `free` | инвариант цикла `live` внутри выражения множества |

Записи STATE.md обязаны называть РАУНД и СУТЬ, а не голый номер. Без этого сводная
запись объявила бы закрытым не то, что закрыто. Текст раунда 1 читается командой
`git show 4989f7a:.planning/phases/05-tarify/05-REVIEW.md` — другого места у него нет.

**Требования BILL-05/BILL-06/BILL-07 этим планом НЕ перемечаются в `Complete`.** Они
сняты с `Complete` 2026-08-16 именно потому, что фаза получила `gaps_found`
(`REQUIREMENTS.md:235-237`, `:256`), и однажды преждевременная отметка уже была
поставлена и отозвана (коммит `2124d1e`). Отметку возвращает верификация после
подтверждения закрытия обоих гэпов, а не план, который эти гэпы закрывает.
`.planning/REQUIREMENTS.md` в `files_modified` не объявлен и не трогается.

**Что этот план НЕ делает по границе объёма.** Ни одной правки кода: гэп 2 —
организационный. Найденные ревью раунда 2 дефекты (`IN-07` — платёж с пустым планом
записывается как `free`; `CR-02` раунда 2 — блокирующий вызов SDK; `WR-07` — негодный
`PLAN_LIMITS` роняет раздел) в работу НЕ берутся: они не входят ни в один из двух
`status: failed` гэпов. Они получают ЗАПИСЬ с распоряжением — ровно то, чего требует
список `missing:`.

**Ручные пункты `behavior_unverified_items` и `human_verification` задачами не
становятся** — см. `<verification>`.

**Каноничные позиции безопасности прохибициями не минтятся:** утечка секретов в
трекинговый документ покрыта `<threat_model>` (T-05-58) и `/gsd-secure-phase`.

</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Готовая замена секций STATE.md — записанная в коммитящийся артефакт и применённая к настоящему дереву</name>
  <precondition>`05-11-SUMMARY.md` существует в каталоге фазы: запись о закрытии блокера `_apply_extension` обязана ссылаться на ИСПОЛНЕННУЮ работу, а не на планируемую. При отсутствии — остановка: план волны 9 идёт после волны 8 именно затем, чтобы бухгалтерия описывала свершившееся.</precondition>
  <files>.planning/phases/05-tarify/05-STATE-PATCH.md, .planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (весь файл, 121 строка: frontmatter с `current_phase`/`progress`; «Current Position» строки 29-36 — там стоит `Plan: 1 of 10`; §Decisions строки 61-79 — стиль записей решений фазы; §Blockers/Concerns строки 85-92 целиком — включая строку 87 об очереди невыкаченных ревизий, где перечислены ПЯТЬ ревизий `0013`…`0017`, и строки 89-90 с двумя 🔴; §Deferred Items строки 104-115 — таблица с ТОЧНЫМ образцом записи закрытого дефекта: `| Phase 2 | CR-01 — … | ✓ Закрыто планом 02-02 (2026-08-11) | 2026-08-10 |`)
    - .planning/phases/05-tarify/05-VERIFICATION.md (frontmatter целиком: оба `gaps` со списками `missing:`; блок `behavior_unverified_items` и `human_verification` — семь пунктов, которые обязаны остаться человеческими; таблица Requirements Coverage — что именно засчитано и с какими оговорками)
    - .planning/phases/05-tarify/05-10-SUMMARY.md (раздел «Бухгалтерия `.planning/STATE.md` — ТАБУЛИРОВАНА, НЕ ЗАПИСАНА», строки 178 и далее: там лежит ДОСЛОВНО подготовленный текст закрытия CR-01 раунда 1, полученный из SUMMARY плана 05-07. Переписывать его заново не нужно — нужно сократить до размера записи таблицы)
    - .planning/phases/05-tarify/05-08-SUMMARY.md (строки 310-325: какие критерии по STATE.md исполнитель не мог проверить и что именно передавалось оркестратору по CR-02 раунда 1)
    - .planning/phases/05-tarify/05-07-SUMMARY.md (что исполнил план 05-07 и какой ЕДИНСТВЕННЫЙ пункт остался человеческим)
    - .planning/phases/05-tarify/05-11-SUMMARY.md (результат волны 8: имя функции правила, ключ журнала, имена регрессий — запись о закрытии блокера обязана называть их, а не «исправлено»)
    - .planning/phases/05-tarify/05-REVIEW.md (раунд 2, на диске: заголовки находок `CR-01`, `CR-02`, `WR-01`…`WR-09`, `IN-01`…`IN-07` и `## Summary` — итог `critical: 2, warning: 9, info: 7, total: 18`)
    - .planning/ROADMAP.md (блок фазы 5: список планов и формулировка `**Plans:** 10/10 plans executed` — «Current Position» STATE.md обязана согласоваться с ним, а не спорить)
  </read_first>
  <action>
**0. Прочитать ревью РАУНДА 1 из git.** Файла на диске больше нет — он перезаписан:
`git show 4989f7a:.planning/phases/05-tarify/05-REVIEW.md`. Без него сводная запись
назовёт номерами раунда 2 находки, о которых говорил план 05-10, и получится ложь. См.
таблицу переназначения номеров в `<flagged_assumptions>`.

**1. Написать `.planning/phases/05-tarify/05-STATE-PATCH.md`** — готовую к применению
замену, а не пересказ. Файл каталога фазы коммитится планом в любом режиме исполнения и
переживает снос worktree; он и есть твёрдый артефакт этой задачи.

Структура: по одному разделу на каждую правку STATE.md, в каждом — что удаляется
(с номером строки на входе) и что встаёт вместо этого, дословно, готовым к копированию.
Разделов ровно пять — перечислены ниже пунктами 2-6.

**2. §Blockers/Concerns — снять обе устаревшие 🔴 записи (строки 89 и 90).**
Обе удаляются из этой секции ЦЕЛИКОМ и переезжают в таблицу §Deferred Items закрытыми
строками (пункт 4). Причина такого переноса, а не переписывания на месте: в таблице уже
лежит точный образец — три закрытые записи Фазы 2 (`CR-01`, `CR-02`, `WR-01`), каждая в
одну строку; повторять формат вместо изобретения нового — правило раздела. Побочная
выгода — секция §Blockers/Concerns перестаёт расти.

**3. §Blockers/Concerns — что остаётся и что добавляется.**
- Строка 87 (очередь невыкаченных ревизий) **правится**: там перечислены ПЯТЬ ревизий
  `0013`…`0017`, а ревизия `0018` отгружена планом 05-08. Утверждение «долг вырос до
  пяти» стало неверным ровно в тот же день и тем же способом, что и обе снятые 🔴 —
  тот же дефект, та же секция, та же цена. Перечень и число исправляются на шесть;
  остальное содержание строки (D-26, необратимость `0013`, guard по
  `hostname`/`port`/`dbname`) не трогается.
- Строка 88 (D-27, подписка на `payment.canceled`) остаётся как есть — она по-прежнему
  верна и по-прежнему открыта.
- **Добавляется одна новая 🔴 запись** — блокирующий вызов SDK. Заголовок обязан
  называть раунд: находка ревью раунда 2, `CR-02`. Содержание: `YooPayment.create()` —
  синхронный HTTP-вызов без таймаута внутри `async def`
  (`app/services/payment_service.py:128-141`), а `docker-compose.prod.yml:87` не задаёт
  `--workers`, то есть воркер один и зависшее соединение с ЮKassa вешает всё приложение,
  включая `/health`. Распоряжение — отложено с обоснованием планом 05-10: перенос в
  поток меняет модель исполнения платёжного вызова и требует своего наблюдения за
  временем ответа, а таймаута SDK всё равно не предоставляет.
- **Добавляется ОДНА сводная запись отложенных находок код-ревью** — та, которую
  объявил план 05-10 и не написал никто. Одной записью, а не восемнадцатью строками:
  бюджет файла — 150 строк, и рассыпать по строке на находку значило бы обменять один
  дефект на другой. Каждая находка называется как `<номер> (раунд N) — суть →
  распоряжение`, через точку с запятой. Обязаны присутствовать поимённо `WR-07` и
  `WR-10` (их назвал список `missing:` верификатора), а также остальные отложенные
  раунда 2 — `WR-01`…`WR-09` кроме взятых в работу, `IN-01`…`IN-07` — и те номера
  раунда 1, которые план 05-10 перечислил в своей таблице распоряжений. Ни одна находка
  не остаётся без распоряжения: закрыто планом, принято в работу или отложено с
  названной причиной.

**4. §Deferred Items — три новые строки таблицы**, форматом соседних записей Фазы 2:
- `CR-01` **ревью раунда 1** (гард источника вебхука читал адрес пира при пустом
  заголовке) → закрыт планом `05-07`: безопасное умолчание `X-Real-IP` стало свойством
  `app/config.py`, значение закреплено записью в `docker-compose.prod.yml`, ветка адреса
  пира удалена целиком; доказано регрессией через настоящий `ProxyHeadersMiddleware`
  (`tests/test_routes/test_billing_webhook_proxy_headers.py`, 10 тестов). Организационной
  мерой запись быть перестала. Человеческим остаётся РОВНО ОДИН пункт — проверка, что
  боевой nginx ставит настроенный заголовок именно на маршруте вебхука.
- `CR-02` **ревью раунда 1** (двойное зачисление, check-then-act без блокировки) →
  закрыт планом `05-08`: `_claim_payment` — настоящий compare-and-swap с
  `rowcount == 1`, `with_for_update()` на выборке, приращение баланса на стороне СУБД,
  вторая активная строка подписки закрыта частичным уникальным индексом ревизии `0018`;
  доказано `test_payment_concurrency.py` и `test_0018_subscriptions_unique_user.py`
  (22 теста).
- `CR-01` **ревью раунда 2** (молчаливое понижение тарифа подтверждённым платежом в
  `_apply_extension`) → закрыт планом `05-11`. Назвать ИМЕНАМИ, а не словом
  «исправлено»: общее объявление правила `switch_is_refused`
  (`app/application/billing/plan_switch.py`), ключ журнала
  `subscription_plan_preserved`, регрессия стадии применения
  `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage`.
  Точные имена сверить по `05-11-SUMMARY.md`, а не по этому плану. Обязательная
  оговорка: вердикт `05-VERIFICATION.md` раунда 2 — `gaps_found` — действует до
  повторной верификации; запись фиксирует исполненную работу, а не пройденную проверку.

**5. §Decisions — одна новая запись.** Семантика смены тарифа как решение фазы:
`upgrade-only` (чекпойнт плана 05-10) — повышение разрешено и оплаченный остаток не
сжигает, понижение при действующей подписке не предлагается и не принимается; правило
объявлено ОДИН раз (`app/application/billing/plan_switch.py`) и читается ОБЕИМИ
стадиями — гардом формы `POST /billing/subscribe` и `_apply_extension` (план 05-11).
Названная цена варианта: повышение отдаёт лимиты старшего тарифа на оставшиеся дни
младшего бесплатно, и это принято сознательно (применения лимитов в системе нет, D-08).
Формат — строка того же вида, что соседние `Phase 3: …` записи.

**6. «Current Position» и frontmatter.** Сегодня там `Plan: 1 of 10` — утверждение,
которое ложно с 16.08. Привести в соответствие с ROADMAP: фаза 05, исполнены планы
05-01…05-12, фаза в статусе закрытия гэпов после вердикта `gaps_found`. `last_activity`
и `last_activity_desc` — на дату исполнения. Счётчики `progress` пересчитать так, чтобы
frontmatter и текст не спорили друг с другом: сегодня frontmatter говорит
`total_plans: 62 / completed_plans: 58`, а строка прогресса — `52/52 (100%)`, и обе
цифры не могут быть верны одновременно. Расхождение устраняется, число планов фазы
берётся из ROADMAP.

**7. Применить замену к настоящему дереву (best-effort, отказ не подделывается).**
Настоящий корень репозитория берётся из `git rev-parse --git-common-dir`: из worktree
это указывает на главный рабочий каталог, из главного — на него же. Применить правки к
`STATE.md` ИМЕННО там. Оставленный в главном дереве дифф — ровно то, что оркестратор
коммитит шагом 5.7 `execute-phase.md`; собственный коммит плана `.planning/STATE.md` в
режиме worktree всё равно исключает.

Если запись по этому пути недоступна — **не изображать успех и не тратить попытки**:
записать факт в SUMMARY отдельной строкой, назвать разрешённый путь и код отказа. Файл
`05-STATE-PATCH.md` остаётся твёрдым результатом задачи, а применение снимает задача 2.

**8. Проверить бюджет файла.** После правок `wc -l .planning/STATE.md` обязан остаться
в пределах 145 строк (рабочий процесс держит потолок 150). Снятие двух длинных 🔴
абзацев освобождает место под три строки таблицы и две новые записи; если бюджет всё
равно превышен — сокращается СВОДНАЯ запись, а не отбрасывается находка.

**9. Требования не перемечать.** `.planning/REQUIREMENTS.md` не открывается на запись
вовсе: `BILL-05`/`BILL-06`/`BILL-07` возвращает в `Complete` верификация после
подтверждения закрытия гэпов, и однажды преждевременная отметка уже была отозвана.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-tarify/05-STATE-PATCH.md && grep -c 'раунд' .planning/phases/05-tarify/05-STATE-PATCH.md</automated>
  </verify>
  <acceptance_criteria>
    - Файл `.planning/phases/05-tarify/05-STATE-PATCH.md` существует и закоммичен вместе с планом
    - `grep -c 'WR-07' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 1
    - `grep -c 'WR-10' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 1
    - `grep -c '05-07' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 1 и `grep -c '05-08' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 1 и `grep -c '05-11' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 1
    - `grep -Ec 'раунд(а)? 1' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 2 и `grep -Ec 'раунд(а)? 2' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 2 — номера находок разведены по раундам, а не сложены в одну кучу
    - `grep -c 'subscription_plan_preserved' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 1 — закрытие блокера названо именами, а не словом «исправлено»
    - Патч содержит распоряжение по каждой находке ревью: `grep -Eco 'WR-0[1-9]|WR-10|IN-0[1-7]|CR-0[12]' .planning/phases/05-tarify/05-STATE-PATCH.md` >= 15
    - Патч называет пять правок STATE.md поимённо: §Blockers/Concerns, §Deferred Items, §Decisions, «Current Position», frontmatter `progress`
    - SUMMARY плана содержит отдельную строку об исходе применения к настоящему дереву: применено либо отказано с названным путём и кодом
    - `git diff --name-only -- .planning/REQUIREMENTS.md` пуст — ни одно требование не перемечено
    - `git diff --name-only -- app/ tests/ alembic/` пуст — план не трогает код
  </acceptance_criteria>
  <done>Замена секций STATE.md написана дословно и закоммичена в каталог фазы; номера находок разведены по раундам ревью; попытка применения к настоящему дереву выполнена, а её исход записан честно.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Применить замену к STATE.md в главном дереве и подтвердить проверками</name>
  <read_first>
    - .planning/phases/05-tarify/05-STATE-PATCH.md (результат задачи 1 — пять разделов замены; порядок применения задан там)
    - .planning/STATE.md в ГЛАВНОМ рабочем каталоге (текущее состояние: применялись ли правки задачей 1 или файл ещё на входном виде — строки 89 и 90 с двумя 🔴)
    - .planning/phases/05-tarify/05-12-SUMMARY.md, если уже написан (строка об исходе применения: «применено» или отказ с названным путём и кодом)
  </read_first>
  <action>
Применить `.planning/phases/05-tarify/05-STATE-PATCH.md` к `.planning/STATE.md` в
ГЛАВНОМ рабочем каталоге репозитория и подтвердить результат проверками ниже.

**Почему это человеческий шаг, а не задача исполнителя.** У исполнителя нет пути,
эффект которого гарантированно переживает снос worktree: `execute-plan.md:502-509`
исключает `.planning/STATE.md` из коммита плана в режиме worktree, а
`execute-phase.md:721` прямо запрещает исполнителю трогать этот файл. Это не
предположение — это дважды воспроизведённый исход: планы `05-08` и `05-10` объявили
файл в `files_modified` и не изменили его ни разу, из-за чего и существует гэп 2. Гейт
стоит здесь затем, чтобы третьего молчаливого пропуска не случилось: остановка громкая,
а не тихая.

**Если задача 1 уже применила правки** (её SUMMARY говорит «применено») — работы нет,
остаётся выполнить проверки и подтвердить.
  </action>
  <instructions>
1. Убедиться, что находитесь в главном рабочем каталоге: `git rev-parse --git-dir`
   отдаёт `.git` (в worktree это был бы путь внутрь `.git/worktrees/...`).
2. Если правки ещё не применены — применить `05-STATE-PATCH.md` к `.planning/STATE.md`
   разделами, в том порядке, в котором они выписаны в патче. Ни один раздел STATE.md,
   не названный патчем, не трогается; файл не перегенерируется целиком.
3. Выполнить проверки из `<verification>` — все шесть обязаны сойтись.
4. Закоммитить `.planning/STATE.md` — либо шагом 5.7 `execute-phase.md`, либо вручную
   сообщением `docs(05): record phase accounting in STATE.md`.
  </instructions>
  <verification>
- `grep -c '🔴 \[Phase 5, CR-01\]' .planning/STATE.md` → **0** (на входе 1, строка 89)
- `grep -c '🔴 \[Phase 5, CR-02\]' .planning/STATE.md` → **0** (на входе 1, строка 90)
- `grep -Ec '05-07|05-08|05-11' .planning/STATE.md` → **>= 3** (на входе 0 по всем трём)
- `grep -Ec 'WR-07|WR-10' .planning/STATE.md` → **>= 2** (на входе 0)
- `wc -l .planning/STATE.md` → **<= 145**
- След не стёрт заодно: `grep -c '🔴' .planning/STATE.md` → **>= 1**. Открытая запись о
  блокирующем вызове SDK (ревью раунда 2, `CR-02`) обязана уцелеть; счёт 🔴 по файлу,
  гонимый к нулю, был бы подменой закрытия дефекта его удалением
- Требования не тронуты: `git diff --name-only -- .planning/REQUIREMENTS.md` → пусто
  </verification>
  <acceptance_criteria>
    - Все шесть проверок `<verification>` сошлись на `.planning/STATE.md` ГЛАВНОГО рабочего каталога, а не worktree
    - `.planning/STATE.md` закоммичен: `git log --oneline -1 -- .planning/STATE.md` называет коммит этой волны, а не `docs(phase-05): create phase plan`
    - `git diff --quiet .planning/STATE.md` — незакоммиченных остатков правки не осталось
    - Сигнал возобновления записан в SUMMARY плана дословно, вместе с фактическими значениями всех шести проверок
    - При несошедшейся проверке план НЕ закрывается: записывается фактическое значение и причина, и волна остаётся незавершённой
  </acceptance_criteria>
  <resume-signal>Введите «применено», если все шесть проверок сошлись, либо назовите проверку, которая не сошлась, и её фактическое значение</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree исполнителя → главное рабочее дерево | Правка `.planning/STATE.md`, сделанная в worktree, не коммитится и исчезает вместе с ним |
| `05-REVIEW.md` раунда 1 (только в git) → сводная запись | Номера находок между раундами переназначены; голый номер без раунда — ложное указание |
| `.planning/STATE.md` → читатель проекта | Единственный операционный документ; его утверждения о денежном пути принимаются на веру |
| SUMMARY плана → оркестратор | Текст «передан оркестратору» получателя не имеет: шаг 5.7 переносит только прогресс планов |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-57 | Repudiation | Операционный документ утверждает, что закрытые дефекты денежного пути открыты, и не упоминает действующего блокера | high | mitigate | Задачи 1-2: обе устаревшие 🔴 сняты со ссылкой на закрывшие планы и доказавшие регрессии; блокер `_apply_extension` записан; блокирующий чекпойнт не даёт пропустить запись третий раз |
| T-05-58 | Information Disclosure | В трекинговый документ, лежащий в репозитории, попадают значения секретов вместе с описанием гарда вебхука | medium | mitigate | Записи называют ИМЕНА переменных окружения и путь к значению, но не значения: `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER` — имя, `X-Real-IP` — имя заголовка, не секрет. Ни ключа магазина, ни идентификатора, ни строки подключения запись не несёт |
| T-05-59 | Tampering | Сводная запись называет находку голым номером, и распоряжение уходит НЕ ТОЙ находке | high | mitigate | Задача 1 шаг 0: раунд 1 читается из git-коммита `4989f7a`; каждая запись обязана называть раунд и суть. Критерий приёмки считает вхождения обоих раундов |
| T-05-60 | Tampering | «Закрытие» дефекта достигается удалением записи, а не закрытием дефекта | medium | mitigate | Чекпойнт проверяет, что счёт 🔴 по файлу НЕ ноль: открытая запись о блокирующем вызове SDK обязана уцелеть. Критерий, гонящий счёт к нулю, объявлен запрещённым |
| T-05-61 | Repudiation | Требование помечено `Complete` раньше вердикта верификации | high | mitigate | `.planning/REQUIREMENTS.md` не открывается на запись; критерий приёмки проверяет пустой дифф. Прецедент отзыва — коммит `2124d1e` |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает ни одного пакета и не трогает код вовсе — критерий приёмки проверяет пустой дифф по `app/`, `tests/`, `alembic/`. Аудит легитимности — `05-RESEARCH.md:144` `## Package Legitimacy Audit` |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-12`. Общий перечень фазы — в `05-10-PLAN.md`, перечень
кодовой работы гэп-закрытия — в `05-11-PLAN.md`.

**Файлы (новые):**
- `.planning/phases/05-tarify/05-STATE-PATCH.md` — готовая к применению замена секций
  `.planning/STATE.md`; коммитится планом и переживает снос worktree

**Файлы (изменённые):**
- `.planning/STATE.md` — §Blockers/Concerns, §Deferred Items, §Decisions,
  «Current Position», frontmatter `progress`

**Записи STATE.md (новые):**
- три строки таблицы §Deferred Items: `CR-01` раунда 1 → план 05-07, `CR-02` раунда 1 →
  план 05-08, `CR-01` раунда 2 → план 05-11
- одна 🔴 запись §Blockers/Concerns: `CR-02` раунда 2 — блокирующий вызов SDK без
  таймаута, отложен с обоснованием
- одна сводная запись отложенных находок код-ревью обоих раундов, включая `WR-07` и
  `WR-10` поимённо
- одна запись §Decisions: семантика смены тарифа `upgrade-only`, объявленная один раз и
  читаемая обеими стадиями

**Функции, ключи журнала, миграции, маршруты, CSS, строки копирайта:** новых нет — план
не трогает код

<verification>
- `grep -c '🔴 \[Phase 5, CR-01\]' .planning/STATE.md` == 0 и `grep -c '🔴 \[Phase 5, CR-02\]' .planning/STATE.md` == 0 — обе устаревшие записи сняты
- `grep -c '🔴' .planning/STATE.md` >= 1 — **обнулять этот счёт запрещено**: открытая запись о блокирующем вызове SDK обязана уцелеть, иначе закрытие дефекта подменяется удалением записи
- `grep -Ec '05-07|05-08|05-11' .planning/STATE.md` >= 3 — закрытия названы планами, которые их исполнили
- `grep -Ec 'WR-07|WR-10' .planning/STATE.md` >= 2 — сводная запись отложенных находок написана, а не растворена
- `grep -Ec 'раунд(а)? 1|раунд(а)? 2' .planning/STATE.md` >= 2 — номера находок разведены по раундам ревью
- `wc -l .planning/STATE.md` <= 145 — бюджет рабочего процесса соблюдён
- `git diff --name-only -- .planning/REQUIREMENTS.md` пуст — отметки требований возвращает верификация, а не этот план
- `git diff --name-only -- app/ tests/ alembic/ pyproject.toml` пуст — план организационный
- `.planning/phases/05-tarify/05-STATE-PATCH.md` закоммичен: даже при отказе применения текст не теряется

**Перенесённые человеческие проверки — задачами НЕ становятся и НЕ автоматизируются**
(семь пунктов `human_verification` и один `behavior_unverified_items` из
`05-VERIFICATION.md`): мобильная ширина 375px, настоящий платёж в тестовом магазине
ЮKassa (D-26), статус «отклонён» у отменённого платежа (D-27), читаемость формулировок
«в обработке» и отказа оплаты, проход первого настоящего уведомления через гард
источника после выката, и прохибиция `BILL-05`/fairness, остающаяся `unresolved` до
решения владельца. Браузерного и e2e-харнесса в проекте нет, боевого доступа к API
ЮKassa нет, состав событий задаётся вне репозитория.
</verification>

<success_criteria>
- Ни одна запись STATE.md не описывает закрытый дефект денежного пути как открытый
- Блокер `_apply_extension` зафиксирован в операционном документе, а не только в отчётах фазы
- Отложенные находки код-ревью собраны одной записью, и каждая несёт распоряжение с указанием раунда
- Артефакт написан в файл, а не обещан во frontmatter, и его отсутствие остановило бы план громко
- Требования не перемечены раньше вердикта верификации
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-12-SUMMARY.md` when done
</output>
