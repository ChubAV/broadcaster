---
phase: 05-tarify
plan: 09
type: execute
wave: 6
depends_on: ["05-06"]
gap_closure: true
files_modified:
  - app/static/css/app.css
  - app/pages/common.py
  - app/templates/billing/balance.html
  - tests/test_pages/test_responsive_markup.py
  - tests/test_pages/test_billing_section.py
  - .planning/phases/05-tarify/05-UI-SPEC.md
autonomous: true
requirements: [BILL-06, BILL-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Дефект мобильной пригодности (M3 / C1) ---
    - "C1/M3: правило `[data-plan-cta] .btn { min-height: 44px }` объявлено в app.css, и все три формы оплаты раздела достигают собственного порога проекта 44px (33px проходили порог WCAG 2.5.8 AA 24px и не проходили только порог проекта — формулировка не смягчается и не ужесточается)"
    - "Правило адресовано `[data-plan-cta]`, а не `.btn`: высота кнопок остальных 26 страниц проекта не меняется"
    # --- Устойчивость денежной подписи (WR-01) ---
    - "BILL-07 precision: `format_amount('NaN')` и `format_amount('Infinity')` возвращают исходную строку, а не поднимают InvalidOperation/ValueError — одна непригодная денежная строка в конфиге или в payments.amount_value больше не роняет весь раздел"
    - "Непригодное значение по-прежнему возвращается КАК ЕСТЬ, а не заменяется нулём: выдуманный ноль в денежной подписи — правдоподобная ложь"
    # --- Пустое состояние списка пакетов (U4 / U5) ---
    - "U4/BILL-07 empty: конфиг с нулём пакетов при включённых платежах рисует пустое состояние со словами, а не пустой блок под балансом"
    - "U5: состояния «ноль», «один» и «много» пакетов различимы — ноль даёт empty_state, один и много дают ту же сетку `[data-metrics]`"
    - "Пустое состояние при выключенных платежах (`Пополнение баланса доступно через администратора`) остаётся прежним и не подменяется новым: это разные причины пустоты"
    # --- Края, которые эта работа не меняет и обязана не сломать ---
    - "BILL-06 boundary: на границе лимита (used == limit) метр встаёт на 100 и называет настоящие числа; на шаг за границей (used == limit + 1) он не ошибка, не тревожный цвет и не отрицательный процент — 22 теста tests/test_application/test_plan_usage.py зелёные без правки"
    - "BILL-07 boundary: список ровно в потолок (200 платежей) не помечается обрезанным, на 201 помечается — оба существующих теста потолка зелёные без правки"
    # --- Лифт `## UI Considerations` 05-UI-SPEC.md, поверхности E2, E4, E5, E6 ---
    - "R5 (E2): деление на лимит не выполняется ни разу — ни в шаблоне, ни в модуле; процент приезжает клампованным 0..100, собственного отказа у метра нет"
    - "R6 (E2): ось с лимитом рисует progress(percent, label='{Ось} — {used} / {limit}') вариантом accent"
    - "R7 (E2): ось с limit is none шкалы не рисует вовсе — только '{Ось} — {used} / ∞' и 'без ограничений'"
    - "R8 (E2): превышение ставит шкалу на 100, подпись называет настоящие числа, тревожного варианта нет"
    - "R9 (E2): .progress__label не объявляет ни nowrap, ни усечения — подпись «Отправок в месяц» переносится"
    - "R13 (E4): безлимитный баланс даёт badge('Безлимит','warning') и 'безлимитная отправка сообщений'; ограниченный — счётчик, шкалу и строку про десять бесплатных сообщений"
    - "R14 (E4): отсутствие free_balance_reset_at убирает ТОЛЬКО свою строку, остальной блок стоит"
    - "R15 (E4): карточки пакетов лежат в [data-metrics] с repeat(auto-fit, minmax(210px,1fr)) — на 375px складываются в одну колонку"
    - "R16 (E5): два журнала несут ДВЕ РАЗНЫЕ пары строк пустого состояния — общего пустого состояния у них нет намеренно"
    - "R17 (E5): неизвестный статус платежа печатается моношрифтом как есть, а не прячется"
    - "R18 (E5): rowhead(PAY_COLUMNS, PAY_COLS) плюс payment_row на запись; «Назначение» собирается по карте first_payment_at без второго запроса"
    - "R19 (E5): отсутствие created_at даёт пустую ячейку даты, отсутствие description — пустую растущую ячейку; строка рисуется целиком"
    - "R20 (E5): потолок называет себя строкой 'Показаны последние {cap} платежей из {total}' вариантом warn; наборы PAY_COLS/TX_COLS держатся инертными правилом [data-row] на ≤860px"
    - "R21 (E5): ноль — empty_state; один и много — один и тот же примитив строки"
    - "R22 (E6): quota читается через .get на каждом поле — отсутствие данных даёт нули, а не отказ"
    - "R23 (E6): [data-quota] скрыт при ≤860px — мобильной поверхности у виджета нет вовсе"
    - "R24 (E6): значение виджета — {used} / {limit} либо ∞, подпись — постоянная строка 'Баланс сообщений'"
    # --- Backstop-пункты (лифт плоским скаляром, ADR-550) ---
    - statement: "B1 (E3): подпись CTA 'Перейти на {name}' не рвёт кнопку и не выезжает за карточку при длинном имени плана из конфига — правила у .btn__label в app.css нет ни одного, длину имени ограничивает только конфиг"
      verification: backstop
    - statement: "B2 (E4): длинное имя пакета переносится, а не расширяет карточку сетки [data-metrics] шире её минимума 210px — правила усечения у моношрифтовой подписи нет"
      verification: backstop
    - statement: "B3 (E5): длинный текст в ячейках «Описание» и «Назначение» переносится внутри строки, а не даёт строке горизонтальную прокрутку — обе объявлены grow и правила усечения не имеют"
      verification: backstop
    - statement: "BILL-06 precision: непригодный ПО ТИПУ лимит в PLAN_LIMITS (строка вместо числа) не роняет /billing 500-й — сравнение строки с числом в axis_percent сегодня поднимает TypeError (WR-10, отложено с обоснованием)"
      verification: backstop
    - statement: "BILL-07 adjacency: два платежа одного плана с совпадающим created_at не получают оба подпись «первый платёж» — карта first_payment_at сравнивает моменты, а не строки (WR-07, отложено с обоснованием)"
      verification: backstop
    - statement: "BILL-07 ordering: при сработавшем потолке списка самая ранняя ПОКАЗАННАЯ строка плана не является первым платежом и не должна так подписываться — журнал строится по убыванию даты и обрезается сверху (WR-07, отложено с обоснованием)"
      verification: backstop
  artifacts:
    - "app/static/css/app.css — правило [data-plan-cta] .btn с min-height 44px"
    - "app/pages/common.py — format_amount отвергает нечисловые значения внутри защищённой области"
    - "app/templates/billing/balance.html — пустое состояние списка пакетов"
    - "tests/test_pages/test_responsive_markup.py — греп-регрессия на высоту кнопки оплаты"
    - ".planning/phases/05-tarify/05-UI-SPEC.md — M3 и C1 закрыты, U4/U5 закрыты, новые строки в Copywriting Contract"
  key_links:
    - "[data-plan-cta] на трёх формах (plan_card.html:72, balance.html:96, balance.html:155) → правило min-height в app.css"
    - "format_amount → карточка плана, плитка пакета, строка платежа — один глобал Jinja на три поверхности"
    - "packages из конфига → цикл [data-metrics] → empty_state при пустом списке"
  prohibitions:
    - requirement_id: BILL-07
      category: safety
      statement: "MUST NOT ронять весь раздел тарифов из-за одной непригодной денежной строки — пользователь теряет доступ к балансу, планам и истории из-за чужой опечатки в конфиге"
      status: resolved
      verification: test
    - requirement_id: BILL-06
      category: transparency
      statement: "MUST NOT рисовать пустой блок там, где список пуст: пустота без слов читается как поломка интерфейса, а не как «показывать нечего»"
      status: resolved
      verification: test
---

<objective>
Три предупреждения и один интерфейсный дефект, поднятые `05-VERIFICATION.md` и
`05-UI-SPEC.md` и не входящие в два блокера.

1. **M3 / C1 — высота кнопки оплаты.** `.btn` вычисляется в 33px (`app.css:679-684`:
   13px `--fs-md` при `line-height:1` + 9px×2 padding + 1px×2 border). `[data-plan-cta]`
   — атрибут, стоящий на всех трёх формах оплаты раздела, — не имеет в `app.css` ни
   одного правила (грепом ноль). **Уточнение чекера, которое не смягчается и не
   ужесточается: 33px ПРОХОДИТ порог WCAG 2.5.8 AA (24px) и не проходит только
   собственный порог проекта 44px.**
2. **WR-01 — `format_amount` падает на `NaN` / `Infinity`.** Проверено исполнением:
   `Decimal("NaN")` разбирается успешно и уходит из-под `try`. Функция — глобал Jinja,
   зовётся на каждой карточке плана, каждой плитке пакета и каждой строке платежа, то
   есть одна плохая строка в конфиге цен или в `payments.amount_value` роняет
   `/billing` пятисоткой.
3. **U4 / U5 — список пакетов без пустого состояния.** `balance.html:143-161` крутит
   `packages` в `[data-metrics]` без запасного `empty_state`; тот, что стоит ниже,
   покрывает только выключенные платежи.

Purpose: раздел обязан переживать порчу своей конфигурации показом, а не отказом, и
собственные пороги проекта обязаны быть свойством CSS, а не строчкой в контракте.
Output: одно правило CSS с греп-регрессией, устойчивый `format_amount`, пустое состояние
со словами.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-UI-SPEC.md
@.planning/phases/05-tarify/05-VERIFICATION.md
</context>

<flagged_assumptions>
## Сверка краёв edge-probe — равенство счёта, без тихих потерь

Спек-фазы у `05` нет, поэтому `## Edge Coverage` не поставлялся, и края выведены
fallback-отчётом: **8 применимых, 8 `unresolved` на входе**. Ни одна строка не снята
автоматически.

| # | Requirement | Категория | Распоряжение | Где |
|---|-------------|-----------|--------------|-----|
| 1 | BILL-05 | `unclassified` | ⚠ остаётся `unresolved` — флагированное допущение, края выведены вручную | `05-08-PLAN.md` |
| 2 | BILL-06 | `boundary` | ✅ explicit — граница лимита и шаг за неё | truths этого плана |
| 3 | BILL-06 | `precision` | 🧪 backstop — нечисловой лимит в конфиге (WR-10) | truths этого плана |
| 4 | BILL-07 | `boundary` | ✅ explicit — ровно потолок и потолок + 1 | truths этого плана |
| 5 | BILL-07 | `adjacency` | 🧪 backstop — совпадающий `created_at` (WR-07) | truths этого плана |
| 6 | BILL-07 | `empty` | ✅ explicit — пустой список пакетов, задача 3 | truths этого плана |
| 7 | BILL-07 | `ordering` | 🧪 backstop — «первый платёж» под сработавшим потолком (WR-07) | truths этого плана |
| 8 | BILL-07 | `precision` | ✅ explicit — `format_amount` на нечисловом, задача 2 | truths этого плана |

**8 строк входа = 1 флагированное допущение + 4 explicit + 3 backstop + 0 отброшено.**

## Сверка `## UI Considerations` 05-UI-SPEC.md — 42 + 3, без умолчаний

| Класс | Счёт в контракте | Распоряжение |
|-------|------------------|--------------|
| ✅ resolved (explicit) R1–R24 | 24 | R5–R9, R13–R24 (17) — `must_haves.truths` этого плана; R1–R4, R10–R12 (7) — `must_haves.truths` плана `05-10` |
| 🧪 backstop B1–B3 | 3 | Плоским скаляром `{ statement, verification: backstop }` в `must_haves.truths` этого плана |
| ⛔ dismissed D1–D10 | 10 | Сняты в контракте с причиной (шесть строк `loading` — в разделе нет ни одного тега скрипта; четыре — фиксированная кардинальность четырёх осей и трёх планов). Основания этой работой не затронуты, в `must_haves` не лифтятся — это и есть распоряжение, а не умолчание |
| ⚠ unresolved U1–U5 | 5 | U4, U5 закрываются задачей 3 этого плана; U1–U3 закрываются планом `05-10`. Оба плана называют их вошедшими как `unresolved` |
| Перенесённое C1–C3 | 3 | C1 закрывается задачей 1 этого плана; C2 — чекпойнт плана `05-10`; C3 — backstop плана `05-10`, перенос в UAT |

## Замечания код-ревью, не подтверждённые верификацией, — распоряжение явное

| # | Finding | Распоряжение |
|---|---------|--------------|
| WR-07 | «первый платёж» подписан неверно при сработавшем потолке; два платежа одного плана с совпадающим `created_at` подписаны первыми оба | **Отложено с обоснованием.** Потолок — 200 платежей, а у пользователя их «единицы в год» (D-17): условие требует двадцати лет подписки без единого пропуска. Совпадение `created_at` до микросекунды на PostgreSQL недостижимо практически. Оба края записаны backstop-пунктами выше, то есть на проверке уходят в `insufficient_spec → human_needed`, а не в молчаливый проход. Правка требует либо серверного запроса `min(created_at) GROUP BY plan`, либо смены контракта макроса `payment_row` — это работа своего размера, а не строка. |
| WR-08 | `/api/billing/transactions` принимает непроверенные `limit`/`offset` | **Отложено с обоснованием.** Предсуществующий дефект JSON-поверхности, к разделу тарифов отношения не имеющий; вход аутентифицирован, ущерб — пятисотка себе самому. Записывается долгом в SUMMARY. |
| WR-10 | Опечатка типа в `PLAN_LIMITS` даёт 500 вместо ненарисованной шкалы | **Отложено с обоснованием**, backstop-пункт выше. Правильный ответ — типизированная модель записи плана на `Settings`, чтобы негодный конфиг падал на старте, а не на клике покупателя. Это отдельная работа: она меняет контракт `parsed_plan_limits`, который читают обработчик, шаблон и модуль осей. |
| IN-03 | `/api/billing/packages` отдаёт каталог при выключенных платежах | **Отложено с обоснованием.** Расхождение двух поверхностей о покупаемости пакетов; страница ведёт себя верно. Долг в SUMMARY. |
| IN-04 | `plan_axes` получает всю запись плана вместо отображения лимитов; неизвестный план даёт четыре ∞ | **Отложено с обоснованием.** Вторая половина (пользователь, чей план убрали из конфига, видит «лимитов нет») реальна, но её ответ — та же типизация записи плана из WR-10. Отдельная работа, один долг. |
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Порог нажимаемости 44px для трёх форм оплаты раздела (M3 / C1)</name>
  <files>app/static/css/app.css, tests/test_pages/test_responsive_markup.py</files>
  <read_first>
    - app/static/css/app.css (строки 675-705 — блок `.btn` целиком: откуда берутся 33px; строки 1155-1180 — блок раздела тарифов `[data-plans]`, `.card--current`, `[data-plan-limits]`, куда встаёт новое правило)
    - app/templates/billing/includes/plan_card.html (строки 60-80 — форма с атрибутом `data-plan-cta` и кнопка внутри неё)
    - app/templates/billing/balance.html (строки 95-100 и 150-160 — две остальные формы с тем же атрибутом)
    - .planning/phases/05-tarify/05-UI-SPEC.md (`## Mobile Contract` строки M1-M5 и `## Spacing Scale` — токен `touch` = 44px; `## UI Considerations` строка C1 с уточнением чекера про два порога)
    - tests/test_pages/test_responsive_markup.py (существующие тесты, читающие app.css КАК ТЕКСТ — форма утверждения и стиль докстринга, где прямо сказано, что проверяется объявление правила, а не отрисовка)
  </read_first>
  <action>
**1. `app/static/css/app.css`.** В блок раздела тарифов (рядом с `[data-plans]`,
`.card--current` и `[data-plan-limits]`, строки ~1155-1180) добавить правило
`[data-plan-cta] .btn { min-height: 44px }`.

Адресуется **`[data-plan-cta] .btn`, а не `.btn`**: общая кнопка проекта стоит на 26
страницах, и поднятие её высоты глобально — перебазирование ритма, работа milestone, а
не раздела. Атрибут `[data-plan-cta]` уже стоит на всех трёх формах оплаты
(`plan_card.html:72`, `balance.html:96`, `balance.html:155`) и до сих пор не имел в
`app.css` ни одного правила — то есть он был объявлен как точка расширения и остался
неиспользованным.

Свойство именно `min-height`: у `.btn` объявлен `display: inline-flex` с
`align-items: center`, поэтому подпись центрируется по вертикали сама, без правки
padding и без расхождения с унаследованным ритмом 9px из `## Spacing Scale`.

Над правилом — комментарий на две-три строки в стиле соседних: 44px — **собственный
порог проекта** (токен `touch` в `## Spacing Scale`), а не порог WCAG; вычисленные 33px
порог WCAG 2.5.8 AA (24px) **проходят** и не проходят только порог проекта. Формулировка
обязана быть именно такой: заявить здесь нарушение WCAG значило бы обосновать правильную
правку неправдой.

**2. `tests/test_pages/test_responsive_markup.py`.** Добавить тест, читающий `app.css`
как текст и утверждающий, что правило для `[data-plan-cta] .btn` объявлено и несёт
`min-height` со значением не меньше 44px. Докстринг — по образцу соседей: проверяется
ОБЪЯВЛЕНИЕ правила, а не отрисовка (браузера в суите нет); арифметика `.btn` (13px +
9×2 + 1×2 = 33px) выписывается в докстринге, чтобы будущая правка `--fs-md` или padding
не прошла молча.

Вторым утверждением того же или соседнего теста — что атрибут `data-plan-cta` стоит на
всех трёх формах оплаты: правило без атрибута так же бесполезно, как атрибут без
правила, и до этой работы в разделе была ровно вторая половина.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_responsive_markup.py -q` завершается с кодом 0
    - `grep -c 'data-plan-cta' app/static/css/app.css` >= 1
    - `grep -c 'min-height' app/static/css/app.css` >= 1
    - `grep -c 'data-plan-cta' app/templates/billing/includes/plan_card.html` == 1
    - `grep -c 'data-plan-cta' app/templates/billing/balance.html` == 2
    - Правило адресовано `[data-plan-cta] .btn`, а НЕ голому `.btn`: блок `.btn {` в `app/static/css/app.css` не содержит `min-height`
    - Поведение: удаление правила из `app.css` роняет новый тест (проверить руками, вернув правило на место)
    - `uv run pytest tests/ -q` завершается с кодом 0
  </acceptance_criteria>
  <done>Единственный найденный конкретный дефект мобильной пригодности раздела закрыт одним правилом и держится греп-регрессией; высота кнопок остальных страниц не тронута.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: format_amount переживает непригодное значение показом, а не пятисоткой (WR-01)</name>
  <files>app/pages/common.py, tests/test_pages/test_billing_section.py</files>
  <read_first>
    - app/pages/common.py (строки 244-292 — `format_amount` целиком с докстрингом и регистрация глобала Jinja; важно, где именно проходит граница `try` и что делает `int(whole)` после форматирования)
    - app/templates/billing/includes/plan_card.html (строка 49 — вызов на карточке плана)
    - app/templates/billing/balance.html (строка 148 — вызов на плитке пакета)
    - app/templates/billing/includes/payment_row.html (строка ~57 — вызов на строке платежа)
    - tests/test_pages/test_billing_section.py (существующие тесты `format_amount`, если есть, — новые встают рядом; иначе форма теста чистой функции без БД в этом файле)
  </read_first>
  <behavior>
    - RED: `format_amount('NaN')` возвращает строку `'NaN'`. Сегодня поднимает `decimal.InvalidOperation` — тест обязан упасть до правки.
    - RED: `format_amount('Infinity')` возвращает строку `'Infinity'`. Сегодня поднимает `ValueError` из `int(...)`.
    - `format_amount('-Infinity')` и `format_amount('sNaN')` возвращают исходную строку, а не поднимают исключение.
    - `format_amount('1490.00')` по-прежнему возвращает `'1 490 ₽'` с неразрывными пробелами.
    - `format_amount('4900.50')` по-прежнему возвращает `'4 900,50 ₽'`.
    - `format_amount(None)` и `format_amount('')` по-прежнему возвращают пустую строку.
    - `format_amount('не число')` по-прежнему возвращает `'не число'`.
    - Страница `/billing` с непригодной ценой в конфиге планов отдаёт 200 и печатает эту цену как есть, а не 500.
  </behavior>
  <action>
**1. Сначала тесты (RED).** Пункты `<behavior>` — отдельными тестами рядом с уже
существующими проверками `format_amount` (найти их грепом по имени функции в
`tests/`; если своего дома у функции нет — завести блок в
`tests/test_pages/test_billing_section.py`, где живёт остальное покрытие раздела).
Убедиться, что два первых КРАСНЫЕ до шага 2: они воспроизводят исполненный прогон из
отчёта верификации.

Последний пункт — интеграционный: `/billing` с подменённым `plan_limits`, где у одного
плана цена непригодна, обязан отдать 200. Он и есть настоящий предмет предупреждения:
падает не функция, а весь раздел вместе с балансом, планами и историей.

**2. `app/pages/common.py`.** Отказ ставится ВНУТРЬ защищённой области, а не рядом с
ней. Сегодня `Decimal(str(value))` разбирает `NaN` и `Infinity` УСПЕШНО — то есть
`except` не срабатывает, — и падение происходит ниже, на форматировании и на `int(...)`.
Правка: сразу после успешного разбора проверить конечность полученного числа и при
неконечном вернуть `str(value)` — тем же способом, каким функция уже возвращает
непригодное значение как есть.

Контракт функции при этом НЕ меняется: непригодное возвращается как есть, а не
заменяется нулём. Это правило уже выписано в докстринге и остаётся в силе — меняется
только то, что теперь оно исполняется для всего множества непригодных значений, а не для
подмножества, которое `Decimal` отвергает разбором.

Докстринг дополнить одной строкой о том, ПОЧЕМУ проверка стоит именно после разбора:
`NaN` и `Infinity` — валидные значения `Decimal`, и `except` их не видит; это ровно то
место, где предыдущая версия обещала устойчивость и не давала её.

Обратной функции по-прежнему нет и не заводится: подпись никогда не едет обратно в
платёж (A3).
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_section.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_billing_section.py -q` завершается с кодом 0
    - `uv run pytest tests/ -q` завершается с кодом 0
    - `grep -c 'is_finite' app/pages/common.py` >= 1
    - Поведение: `uv run python -c "from app.pages.common import format_amount; print(format_amount('NaN'), format_amount('Infinity'))"` печатает `NaN Infinity` и завершается с кодом 0
    - Поведение: `uv run python -c "from app.pages.common import format_amount; assert format_amount('1490.00') == '1 490 ₽'"` завершается с кодом 0
    - Поведение: `GET /billing` при непригодной цене одного плана в `plan_limits` отдаёт 200 и печатает эту цену как есть
    - Проверка отказа возвращает ИСХОДНОЕ значение, а не ноль: `grep -c 'return "0"' app/pages/common.py` == 0
  </acceptance_criteria>
  <done>Одна плохая денежная строка в конфиге или в журнале платежей больше не отнимает у пользователя весь раздел; контракт «непригодное — как есть» исполняется целиком, а не наполовину.</done>
</task>

<task type="auto">
  <name>Task 3: Пустое состояние списка пакетов (U4 / U5)</name>
  <files>app/templates/billing/balance.html, tests/test_pages/test_billing_section.py, .planning/phases/05-tarify/05-UI-SPEC.md</files>
  <read_first>
    - app/templates/billing/balance.html (строки 121-165 — блок 4 целиком: баланс, цикл пакетов в `[data-metrics]`, ветка выключенных платежей с `empty_state` на строках 162-163)
    - app/templates/components/empty_state.html (весь файл — сигнатура макроса: title, hint, action_label, action_href)
    - .planning/phases/05-tarify/05-UI-SPEC.md (строки 288-296 — заголовок `### ⚠ unresolved` и таблица U1-U5 ЦЕЛИКОМ, включая шапку и строку-разделитель: у таблицы сегодня ЧЕТЫРЕ колонки и ни одного маркера состояния в строках, и правка шага 3 добавляет пятую; строка 270 — итоговый счёт; строки 358-362 — таблица C1-C3, у которой колонка статуса УЖЕ есть, форма значения берётся оттуда; строка 15 и строка 383 — два места, где слова `⚠ unresolved` стоят как повествование и правке НЕ подлежат; `## Copywriting Contract` — таблица строк, куда встают новые; `## Mobile Contract` M1 — арифметика сетки, которая не меняется)
    - tests/test_pages/test_billing_section.py (существующие тесты пустых состояний двух журналов — форма проверки разметки и стиль имён)
  </read_first>
  <action>
**1. `app/templates/billing/balance.html`.** Ветка `{% if payments_enabled and not
balance_info.is_unlimited %}` (строка 143) сегодня крутит `packages` в `[data-metrics]`
безусловно. Добавить внутрь неё разбор пустого списка: при пустом `packages` вместо
сетки рисуется `empty_state` с заголовком `Пакеты сообщений временно недоступны` и
подсказкой `Загляните позже или обратитесь к администратору`.

**Существующее `empty_state` на строках 162-163 не трогать и не переиспользовать.**
Оно отвечает на другой вопрос — «платежи выключены администратором», и его текст
(`Пополнение баланса доступно через администратора`) в случае «платежи включены, но
каталог пуст» был бы неправдой: администратор тут ни при чём, покупать просто нечего.
Две причины пустоты — две пары слов, ровно тем же правилом, каким два журнала раздела
несут две разные пары строк (R16).

Сетка `[data-metrics]` и её арифметика (`repeat(auto-fit, minmax(210px,1fr))`, M1/R15)
не трогаются: пустое состояние встаёт ВМЕСТО сетки, а не внутрь неё.

**2. `tests/test_pages/test_billing_section.py`.** Три теста:
- пустой список пакетов при включённых платежах печатает новый заголовок и не печатает
  ни одной формы с `action="/billing/purchase"`;
- непустой список пакетов новый заголовок НЕ печатает (иначе пустое состояние
  рисовалось бы поверх наполненного);
- выключенные платежи по-прежнему печатают старую строку про администратора, а не
  новую (U4 не подменяет собой прежнее состояние).

Третий тест — не формальность: подмена одного пустого состояния другим и есть самый
вероятный способ закрыть U4 неправильно.

**3. `.planning/phases/05-tarify/05-UI-SPEC.md`.** Контракт обязан догнать разметку,
иначе следующая работа будет сверяться с устаревшим документом. Правки **точечные и
по строкам**, документ не перегенерируется:

- в `## Copywriting Contract` добавить две строки нового пустого состояния рядом с
  существующими `Empty state`-строками;

- **таблица раздела `### ⚠ unresolved` получает пятую колонку `Статус`.** Сегодня её
  шапка — `| # | Категория | Поверхность | Чего в разделе нет |`, и строки U1–U5
  (`05-UI-SPEC.md:290-296`) **не несут маркера состояния вовсе**: состояние выражено
  заголовком раздела. Поэтому и «убавить счёт `⚠ unresolved`» по этим строкам нечего —
  распоряжение обязано появиться там, где его можно прочитать построчно. Колонка
  добавляется в шапку, в строку-разделитель и в каждую из пяти строк:
  - U4 и U5 → `✅ resolved (explicit) — план 05-09`
  - U1, U2, U3 → `⚠ unresolved` (их закрывает план `05-10`)

- **заголовок `### ⚠ unresolved` остаётся дословно и из области правок ИСКЛЮЧЁН.** Он
  исторический: называет, что подняла зонд-проверка, а не сегодняшнее состояние. Под ним
  добавить одну строку курсивом: состояние каждой строки читается из колонки «Статус»,
  заголовок фиксирует происхождение. Так же ИСКЛЮЧЕНЫ из правок ретроспективная сноска
  `05-UI-SPEC.md:15` и строка происхождения `05-VERIFICATION.md` в `## Traceability`
  (`:383`) — обе содержат слова `⚠ unresolved` как часть повествования о том, как
  документ писался, и их удаление стёрло бы след, ради сохранения которого этот набор
  планов и существует;

- в `## Mobile Contract` строку M3 и в `## UI Considerations` строку C1 (`:360`)
  пометить выполненными с указанием плана `05-09` (задача 1). У C1 колонка `Статус` уже
  есть — её значение `⚠ unresolved` меняется на `✅ закрыто планом 05-09`. Строки C2 и C3
  **не трогаются**: C2 — предмет чекпойнта плана `05-10`, C3 — backstop, ушедший в UAT.
  **Уточнение чекера про два порога оставить дословно** — оно верно и после правки;

- итоговый счёт в шапке (`05-UI-SPEC.md:270`) пересчитать с сохранением суммы 42:
  `**Итог: 26 explicit · 3 backstop · 10 dismissed · 3 unresolved** — 42 из 42, без
  умолчаний.` Две строки переехали из `unresolved` в `explicit`, ни одна не исчезла.
  C1–C3 в эти 42 не входят и на счёт не влияют (они — «Перенесено из фазы», +3).
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_section.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_billing_section.py -q` завершается с кодом 0
    - `uv run pytest tests/ -q` завершается с кодом 0
    - `grep -c 'Пакеты сообщений временно недоступны' app/templates/billing/balance.html` == 1
    - `grep -c 'Пополнение баланса доступно через администратора' app/templates/billing/balance.html` == 1 — прежнее пустое состояние на месте и не продублировано
    - `grep -c 'empty_state' app/templates/billing/balance.html` == 5 — сегодня ровно 4 (измерено: импорт на `:5` плюс три вызова на `:163`, `:190`, `:223`), новый вызов даёт пятое; `>= 3` было бы выполнено ДО правки и изменение не поймало бы
    - `grep -c 'Пакеты сообщений временно недоступны' .planning/phases/05-tarify/05-UI-SPEC.md` >= 1
    - `grep -cE '^\| U[45] \|.*✅ resolved' .planning/phases/05-tarify/05-UI-SPEC.md` == 2 — сегодня 0 (у строк U1-U5 маркера состояния нет вовсе), после правки распоряжение читается построчно
    - `grep -c '^| U' .planning/phases/05-tarify/05-UI-SPEC.md` == 5 — сегодня 5; ни одна строка U не удалена ради «уменьшения счёта», распоряжение выражено колонкой, а не исчезновением строки
    - `grep -cE '^\| C1 \|.*⚠ unresolved' .planning/phases/05-tarify/05-UI-SPEC.md` == 0 — сегодня 1 (`05-UI-SPEC.md:360`); C1 закрыт задачей 1
    - `grep -cE '^\| C[123] \|' .planning/phases/05-tarify/05-UI-SPEC.md` == 3 — сегодня 3; строки C2 и C3 на месте и не задеты
    - `grep -c '26 explicit · 3 backstop · 10 dismissed · 3 unresolved' .planning/phases/05-tarify/05-UI-SPEC.md` == 1 — сегодня 0 (стоит `24 · 3 · 10 · 5`); сумма остаётся 42
    - `grep -c '^### ⚠ unresolved' .planning/phases/05-tarify/05-UI-SPEC.md` == 1 — заголовок раздела (`:288`) НЕ удалён: он исторический, а не описание сегодняшнего состояния
    - `grep -c '⚠ unresolved' .planning/phases/05-tarify/05-UI-SPEC.md` >= 4 — три вхождения вне области правок (сноска `:15`, заголовок `:288`, строка происхождения `:383`) плюс U1-U3, ждущие плана `05-10`; след аудита не стёрт
    - Поведение: `GET /billing` при `MESSAGE_PACKAGES = "[]"` и включённых платежах печатает новый заголовок и ни одной формы покупки пакета
    - Поведение: `GET /billing` при непустом каталоге пакетов новый заголовок не печатает
    - Поведение: `GET /billing` при `yookassa_enabled = false` печатает прежнюю строку про администратора и не печатает новую
  </acceptance_criteria>
  <done>Пустой каталог пакетов говорит словами, а не выглядит поломкой; две причины пустоты названы двумя разными парами слов; контракт интерфейса догнал разметку.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| конфигурация окружения → шаблон | `PLAN_LIMITS` и `MESSAGE_PACKAGES` приходят строкой окружения и попадают на экран через `format_amount`; порча конфига — недоверенный ввод от администратора, а не от пользователя |
| журнал платежей → строка истории | `payments.amount_value` пишется платёжным контуром и печатается тем же форматтером |
| CSS → нажимаемая поверхность | Правило раздела не должно менять поведение общих компонентов 26 страниц |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-42 | Denial of Service | `format_amount` на неконечном значении (`app/pages/common.py:270-281`) | medium | mitigate | Задача 2: отказ перенесён ВНУТРЬ защищённой области, проверка конечности после успешного разбора. Недостижимость: множество непригодных значений покрыто целиком, а не подмножеством, которое отвергает разбор |
| T-05-43 | Denial of Service | Порча `PLAN_LIMITS` типом значения уводит `/billing` в 500 (WR-10) | low | accept | Отложено с обоснованием: правильный ответ — типизированная модель записи плана, меняющая контракт `parsed_plan_limits` у трёх потребителей. Записано backstop-пунктом в `must_haves`, то есть на проверке уходит в `human_needed`, а не в молчаливый проход |
| T-05-44 | Tampering | Правило CSS раздела меняет высоту кнопок всего проекта | low | mitigate | Задача 1: селектор `[data-plan-cta] .btn`; критерий приёмки требует отсутствия `min-height` в блоке голого `.btn` |
| T-05-45 | Information Disclosure | Непригодное значение печатается как есть и может нести чужой текст | low | accept | Контракт функции («непригодное — КАК ЕСТЬ») принят фазой сознательно: выдуманный ноль в денежной подписи — правдоподобная ложь. Источник значения — конфиг администратора и собственный журнал платежей, пользовательского ввода в нём нет |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает ни одного пакета и не трогает `pyproject.toml`; правки — CSS, один шаблон, одна функция и тесты. Аудит легитимности — `05-RESEARCH.md` §Package Legitimacy Audit |
</threat_model>

## Artifacts this phase produces

Перечень общий на набор гэп-планов `05-07`…`05-10`, повторяется в каждом.

**Файлы (новые):**
- `tests/test_routes/test_billing_webhook_proxy_headers.py` (план 05-07)
- `tests/test_services/test_payment_concurrency.py` (план 05-08)
- `alembic/versions/0018_subscriptions_unique_user.py` (план 05-08, **условно**)
- `tests/test_migrations/test_0018_subscriptions_unique_user.py` (план 05-08, условно)
- `tests/test_pages/test_billing_payment_errors.py` (план 05-10)

**Alembic revision id (условный):** `0018` (`down_revision = "0017"`)

**Функции и исключения (новые):**
- `_claim_payment(db, yookassa_id, new_status, now) -> bool` — `app/services/payment_service.py` (05-08)
- `PaymentCreationError` — `app/services/payment_service.py` (05-10)
- `_payment_error_message(code)` — `app/pages/billing.py` (05-10)

**Ключи журнала (новые):** `webhook_ip_header_not_configured` (05-07),
`webhook_claim_lost` (05-08), `payment_create_failed` (05-10)

**Ключи конфига:** новых нет; меняется УМОЛЧАНИЕ `Settings.yookassa_webhook_client_ip_header`

**Переменные окружения деплоя (новые в артефакте):** `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER`
в `docker-compose.prod.yml` (05-07)

**Контракт маршрута (новый):** `GET /billing?error=<code>` со значениями
`payment` | `disabled` | `plan` | `package` (05-10)

**CSS-селектор (новый):** `[data-plan-cta] .btn` с `min-height: 44px` — **план 05-09**

**Строки копирайта (новые):** `Пакеты сообщений временно недоступны` /
`Загляните позже или обратитесь к администратору` (05-09); строки состояний отказа
оплаты (05-10)

**Ограничение схемы (условное):** частичный уникальный индекс по `subscriptions.user_id`
при `is_active` (05-08)

<verification>
- `uv run pytest tests/ -q` зелёный целиком
- Ни одного нового пакета в `pyproject.toml`
- `grep -c 'data-plan-cta' app/static/css/app.css` больше не ноль — ровно то, чего не хватало `## Mobile Contract`
- Строки U4, U5 и C1 в `05-UI-SPEC.md` несут распоряжение построчно: `grep -cE '^\| U[45] \|.*✅ resolved'` == 2 и `grep -cE '^\| C1 \|.*⚠ unresolved'` == 0
- Заголовок `### ⚠ unresolved` (`:288`), ретроспективная сноска (`:15`) и строка происхождения в `## Traceability` (`:383`) НЕ тронуты — это повествование о том, как писался документ, а не статусы строк
</verification>

<success_criteria>
- Три формы оплаты раздела достигают собственного порога проекта 44px, остальные 26 страниц не тронуты
- Непригодная денежная строка печатается, а не роняет раздел
- Пустой каталог пакетов говорит словами, отличными от слов про выключенные платежи
- Все 8 краёв edge-probe и все 42 + 3 строки `## UI Considerations` распорядены, ни одна не потеряна
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-09-SUMMARY.md` when done
</output>
