---
phase: 05-tarify
plan: 32
type: execute
wave: 25
depends_on: ["05-27", "05-28", "05-29", "05-30", "05-31"]
gap_closure: true
files_modified:
  - .planning/phases/05-tarify/05-CONTEXT.md
  - .planning/phases/05-tarify/COVERAGE.md
autonomous: true
requirements: [BILL-05, BILL-06, BILL-07]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Константа величины «60 дней» в записях D-30 и D-31 заменена тем, что документ МОЖЕТ держать истинным: инвариантом «два календарных месяца» с названным диапазоном в днях, либо числом с явно названной датой отсчёта — тем же приёмом, каким та же запись уже поступила с числами 396 и 140"
    - "Диапазон и распределение сняты СОБСТВЕННЫМ прогоном исполнителя над настоящим кодом, а не переписаны из отчёта: команда прогона названа в SUMMARY, и её вывод приведён целиком"
    - "Собственное объявление записи D-30 («ОБЕ ВЕЛИЧИНЫ ЭТОГО АБЗАЦА СВЕДЕНЫ С ИЗМЕРЕНИЕМ») приведено к тому, что в абзаце фактически сведено: либо оно распространено на ТРЕТЬЮ величину, либо прямо сказано, что третья величина сведена этим планом и как"
    - "`05-22-SUMMARY.md:134` не правится и не остаётся молчащим противоречием: его статус ИСТОРИЧЕСКОГО документа объявлен явно — тем же приёмом, каким это уже сделано для `05-18-SUMMARY.md`, — с указанием, где живёт действующая запись величины"
    - "Матрица `COVERAGE.md` проверена на действительность после раунда 7 и НЕ перегенерирована: затронутые строки названы поимённо, незатронутые названы незатронутыми прямо, чтобы молчание не читалось недосмотром"
    - "Ни одна прежняя запись `05-CONTEXT.md` и ни одна строка `COVERAGE.md` не стёрта: устаревшие ПЕРЕСКАЗАНЫ с названной причиной"
    - statement: "Человек, читающий D-30 и D-31 после этой волны, называет выдачу ветки отката так, что сказанное остаётся верным и завтра, и на другой дате отсчёта. Неустареваемость формулировки судится человеком: отличить константу, которая устареет, от законного числа документа машине нечем (решение D-33)"
      verification: backstop
  artifacts:
    - "`.planning/phases/05-tarify/05-CONTEXT.md` — записи D-30 и D-31 без константы величины на месте выдачи ветки отката; оговорка об историческом статусе `05-22-SUMMARY.md`; приведённое объявление о сведении величин"
    - "`.planning/phases/05-tarify/COVERAGE.md` — раздел сверки после раунда 7: затронутые строки названы, незатронутые названы незатронутыми"
  key_links:
    - "`capped_carryover` (`app/application/billing/subscription_period.py:333`) → число в записи. Величина ВЫЧИСЛЯЕТСЯ от даты отсчёта, поэтому запись, называющая её одним числом без даты, не может быть истинной больше чем на части дат: измерение показало 10 попаданий из 730"
    - "D-30 → D-31 → `05-22-SUMMARY.md:134`. Одна величина в трёх местах; правка одного места без двух других заводит расхождение вместо того, чтобы его закрыть"
    - "решение D-33 → форма правки. D-33 назвал класс «константа величины, которую документ не может держать истинной» и отказал ему в машинном гейте с причиной; закрытие поэтому идёт формулировкой, а не проверкой"
  prohibitions:
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT называть в операционном документе константу величины, которую документ не может держать истинной: верное значение становится неверным в момент коммита, и следующий читатель принимает решение по неверному факту"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-05
      category: integrity
      statement: "MUST NOT править исполненный SUMMARY ради согласования чисел: исторический документ фиксирует, что было измерено ТОГДА, и правка стирает причину, по которой мера понадобилась"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-06
      category: transparency
      statement: "MUST NOT перегенерировать матрицу возможностей API: перегенерация теряет прежние решения вместе с их причинами молча"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-05
      category: safety
      statement: "MUST NOT предполагать ветвь чекпойнта плана `05-27` молча: при ответе `records-amended` записи D-34 не существует, поведение подлежит пересмотру, и план ОСТАНАВЛИВАЕТСЯ (`## BLOCKED`) вместо того, чтобы исполнить одну из двух ветвей по умолчанию. Молчаливый выбор ветви вниз по волнам есть та же потеря записи, что стёртая запись"
      status: unresolved
      verification: judgment
---

<objective>
**Гэп 3 раунда 7: абзац, объявивший свои величины сведёнными с измерением, заводит рядом
третью, которую измерение опровергает.**

Отчёт формулирует это без смягчения:

> ПОЛОВИНА ЗАКРЫТА И ПРОВЕРЕНА, ПОЛОВИНА ЗАВЕДЕНА ЗАНОВО. … D-30 и D-31 записывают: «после
> правки отдаёт 60 дней (два календарных месяца)»; та же величина стоит таблицей в
> `05-22-SUMMARY.md:134`. Прогон настоящего кода на том же посеве даёт 59 дней от даты
> отсчёта, которой пользуется сам этот абзац (2026-01-10), и 61 день от сегодняшнего дня.

И снимает распределение по 730 датам начала: `{56: 2, 57: 2, 58: 2, 59: 113, 60: 10, 61: 484,
62: 117}` — записанное «60» есть САМОЕ РЕДКОЕ значение из семи возможных, верное на 1,4 %
дат и неверное на дате, названной в том же абзаце.

Скобка «(два календарных месяца)» называет инвариант ПРАВИЛЬНО. Неверна константа рядом с
ней — то есть ровно тот класс, о котором решение D-33 принимает решение абзацем ниже, и
заведён он в том же абзаце, который объявляет: «ОБЕ ВЕЛИЧИНЫ ЭТОГО АБЗАЦА СВЕДЕНЫ С
ИЗМЕРЕНИЕМ, И ПРИЧИНА СКАЗАНА СЛОВАМИ, А НЕ ЗНАКОМ ПРИБЛИЖЕНИЯ». К третьей величине это
объявление не относится, и это не оговорено.

Плюс `05-22-SUMMARY.md:134` — строка таблицы «395 дней → 60 дней (два календарных месяца)»,
где левое число уже исправлено в D-30 на 396 и оставлено здесь, а правое не измерялось.
Отчёт называет ДВА допустимых исхода, и второй из них — объявить SUMMARY историческим
документом, не подлежащим правке, тем же приёмом, каким это уже сделано для `05-18-SUMMARY.md`.

Purpose: документ, по которому принимаются решения, обязан быть верным в момент ЧТЕНИЯ, а не
в момент записи; и объявление о сведении величин обязано покрывать те величины, которые в
абзаце есть.
Output: инвариант вместо константы в двух записях, объявленный исторический статус третьего
места и сверенная после раунда 7 матрица возможностей API.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/COVERAGE.md
@app/application/billing/subscription_period.py
</context>

<flagged_assumptions>

**Зонд краевых случаев: три требования фазы остаются `unclassified` / `unresolved`.**
Классификатор зонда работает по английским формулировкам, а BILL-05, BILL-06 и BILL-07
записаны по-русски. Поднимаются явными допущениями и остаются `unresolved`:

- **E-01 (BILL-05, `unresolved`)** — краевые состояния продления. Допущение: план кода не
  трогает; он приводит ЗАПИСЬ о величине, которую код уже отдаёт.
- **E-02 (BILL-06, `unresolved`)** — краевые состояния показа осей. Допущение: не задевается.
- **E-03 (BILL-07, `unresolved`)** — краевые состояния журнала платежей. Допущение: не
  задевается.

Равенство: **3 поднятых == 3 записанных допущения == 0 внесённых в `must_haves.truths`**.

**Допущение о распределении.** Числа `{56: 2, 57: 2, 58: 2, 59: 113, 60: 10, 61: 484, 62: 117}`
сняты верификацией раунда 7. Исполнитель обязан ПЕРЕСНЯТЬ их собственным прогоном над
настоящим `capped_carryover` и записать СВОИ; числа отчёта служат ожиданием и ранним сигналом
расхождения, а не источником истины. Расхождение, если оно есть, называется словами.
</flagged_assumptions>

<artifacts_this_phase_produces>

| Артефакт | Где | Что это |
|---|---|---|
| формулировка-инвариант выдачи ветки отката | `05-CONTEXT.md`, записи D-30 и D-31 | «Два календарных месяца — от N до M дней в зависимости от даты отсчёта», с числами, снятыми прогоном |
| оговорка об историческом статусе `05-22-SUMMARY.md` | `05-CONTEXT.md`, запись D-30 | Объявление, что исполненный SUMMARY правке не подлежит, и указание, где живёт действующая запись |
| раздел «Сверка после закрытия гэпов раунда 7» | `COVERAGE.md` | Затронутые строки поимённо, незатронутые названы незатронутыми |

Символов кода план не вводит и файлов `app/` и `tests/` не трогает.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Величина выдачи ветки отката снимается прогоном и записывается инвариантом, а не константой</name>
  <reversibility rating="costly">Запись становится основанием следующих решений о деньгах. Ошибка здесь не роняет прогон, но переживает его: за расхождение записанной цены с исполняемым кодом фаза получила раунды 5, 6 и 7.</reversibility>
  <precondition>План `05-27` исполнен ПО ВЕТВИ `override-accepted`: запись D-34 стоит в `05-CONTEXT.md`, и правка D-30/D-31 не спорит с каноническим прочтением. ⚠️ ЕСЛИ ВЛАДЕЛЕЦ ВЫБРАЛ НА ЧЕКПОЙНТЕ ПЛАНА `05-27` ВЕТВЬ `records-amended`, ЭТА ЗАДАЧА НЕ ИСПОЛНЯЕТСЯ И ПЛАН ОСТАНАВЛИВАЕТСЯ, возвращая `## BLOCKED` с названной причиной — тем же стоп-путём, каким это сделано в задаче 2 плана `05-30`. Основание: при `records-amended` задачи 2 и 3 плана `05-27` не исполняются, записи D-34 не существует вовсе, а сама величина выдачи ветки отката подлежит пересмотру вместе с ПОВЕДЕНИЕМ — и это работа своего плана со своим чекпойнтом, а не правка формулировки. Молча предположить одну ветвь означало бы решить за владельца вопрос, вынесенный ему чекпойнтом. Планы `05-28`, `05-29`, `05-30` и `05-31` исполнены и слиты, поэтому прогон идёт над деревом, в котором докстринги уже приведены, гейт зелен и код не менялся.</precondition>
  <files>.planning/phases/05-tarify/05-CONTEXT.md</files>
  <read_first>
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-30 целиком, строка `93` — правится ОДИН её фрагмент: «после правки отдаёт 60 дней (два календарных месяца) при прейскуранте 9 800 ₽». Остальное сохраняется дословно, включая уже сведённые числа 396 и 140 и запись об ошибке журнального ключа)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-31 целиком, строка `99` — тот же фрагмент во второй записи: «после правки отдаёт 60 дней (два календарных месяца)». Правятся ОБЕ записи: правка одной завела бы расхождение внутри одного документа)
    - app/application/billing/subscription_period.py (`capped_carryover`, `add_one_month`, `countdown_base`, `normalize_utc` — функции, из которых снимается измерение. Величина есть `capped_carryover(current, now) - now` в днях при `current`, отстоящем на 365 дней)
    - .planning/phases/05-tarify/05-VERIFICATION.md (третий элемент `gaps:` — `reason` с распределением по 730 датам и оба пункта `missing:` дословно. Первый пункт называет ДВА допустимых исхода правки, и выбрать надо один, а не смешать)
    - .planning/phases/05-tarify/05-CONTEXT.md (решение D-33 — класс «константа величины, которую документ не может держать истинной», и причина, по которой на него нет машинного гейта. Правка есть исполнение D-33, а не обход)
  </read_first>
  <action>
**Снять измерение собственным прогоном.** Над настоящим `capped_carryover` посчитать выдачу
ветки отката в днях для посева «живой горизонт 365 дней + платёж старшего плана» при КАЖДОЙ
дате отсчёта в окне не меньше двух лет (730 дат), и получить: (а) множество возможных
значений с их частотами; (б) значение на дате `2026-01-10`, которой пользуется сам абзац;
(в) значение на дате исполнения. Команда прогона называется в SUMMARY дословно, вместе с
полным выводом, — иначе число в записи снова окажется принятым, а не измеренным.

**Заменить константу.** В ОБЕИХ записях (D-30 и D-31) фрагмент о выдаче после правки
переписывается ОДНИМ из двух допустимых исходов, названных отчётом, и выбранный исход
называется в SUMMARY:

- либо инвариантом с диапазоном: «два календарных месяца — от N до M дней в зависимости от
  даты отсчёта», где N и M сняты прогоном, а рядом сказано, ПОЧЕМУ величина не константа
  (длина календарного месяца зависит от даты, и `add_one_month` считает месяцы, а не дни);
- либо числом с ЯВНО названной датой отсчёта — «X дней от 2026-01-10» — тем же приёмом,
  каким тот же абзац уже поступил с числами 396 и 140.

Смешивать исходы нельзя: «60 дней (два календарных месяца)» и есть смесь, и ровно она
подвела.

**Прейскурант рядом с величиной сохраняется**, но пересчитывается под выбранную форму: цена
за месяц не меняется от даты, а число дней меняется, и запись обязана не создавать
впечатления, что меняются обе.

**Прежняя запись НАЗЫВАЕТСЯ, а не стирается.** Одной фразой: прежняя редакция называла
выдачу числом «60 дней»; прогон показал, что это самое редкое из возможных значений и что оно
неверно на дате отсчёта, названной в том же абзаце; величина заменена формой, которую
документ может держать истинной. Ссылка на находку — гэп 3 раунда 7.

⚠️ **НАЗЫВАЯ ПРЕЖНЮЮ РЕДАКЦИЮ, НЕ ВОСПРОИЗВОДИТЬ ЕЁ СОЧЕТАНИЕ ЦЕЛИКОМ.** Гейт задачи —
негативный: он требует, чтобы сочетание числа со скобкой-инвариантом («60 дней» и «(два
календарных месяца)» подряд, в одну строку) в документе не встречалось НИ РАЗУ. Фраза о
прежней редакции, процитировавшая его дословно, оставила бы гейт красным навсегда и была бы
неотличима от невыполненной работы. Поэтому прежняя величина называется ОДНИМ ЧИСЛОМ — «60»
или «60 дней» — а инвариант в той же фразе не приписывается: он и был верен, неверна была
константа рядом с ним, и это ровно то, что фраза обязана сказать.

⚠️ Ни одно из уже сведённых чисел абзаца (396 — ветка отката до правки; 140 — ветка
конверсии) не трогается: они сведены планом `05-26` и подтверждены прогоном верификации
раунда 7 «до дня».
  </action>
  <verify>
    <automated>! grep -q '60 дней (два календарных месяца)' .planning/phases/05-tarify/05-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - Константы в документе не осталось: `! grep -q '60 дней (два календарных месяца)' .planning/phases/05-tarify/05-CONTEXT.md` завершается кодом `0`. ⚠️ Прежняя редакция плана ставила здесь `grep -c …` и требовала вывода `0` — а `grep -c` при нуле совпадений печатает `0` и завершается кодом `1`: успешно исполненная задача давала КРАСНЫЙ `<verify>`, а зелёный означал, что константа на месте. Проверка была ИНВЕРТИРОВАНА, и это тот же класс «объявление против того, что оно делает», ради которого писана вся волна
    - Обе записи правлены: `grep -c 'два календарных месяца' .planning/phases/05-tarify/05-CONTEXT.md` даёт не менее `2`, и в обоих вхождениях рядом стоит либо диапазон «от N до M дней», либо число с датой отсчёта
    - Уже сведённые числа на месте: `grep -c '396' .planning/phases/05-tarify/05-CONTEXT.md` и `grep -c '140 дней Pro' .planning/phases/05-tarify/05-CONTEXT.md` не уменьшились относительно значений до задачи (оба значения приводятся в SUMMARY)
    - Прежняя редакция названа: обе записи содержат фразу о том, что прежде выдача называлась числом «60» и почему это заменено
    - Измерение приведено: в SUMMARY есть команда прогона и её полный вывод — множество значений с частотами, значение на `2026-01-10` и значение на дате исполнения
    - Расхождение с ожиданием отчёта, если оно есть, названо СЛОВАМИ, а не выровнено подгонкой
    - `git diff -- .planning/phases/05-tarify/05-CONTEXT.md | grep -c '^-[^-]'` показывает удалённые строки только в пределах двух правимых записей
  </acceptance_criteria>
  <done>Константа «60 дней» отсутствует в документе; обе записи называют выдачу формой, которую документ может держать истинной; измерение снято собственным прогоном и приведено целиком; прежняя редакция названа</done>
</task>

<task type="auto">
  <name>Task 2: Третье место величины получает объявленный статус, а объявление о сведении — свой объём</name>
  <reversibility rating="reversible">Добавление двух оговорок в существующую запись; откат — их удаление.</reversibility>
  <precondition>Задача 1 исполнена: действующая запись величины уже имеет форму, на которую можно сослаться. Оговорка, отсылающая к записи со старой константой, отправила бы читателя к тому же дефекту. ⚠️ ЕСЛИ ВЛАДЕЛЕЦ ВЫБРАЛ НА ЧЕКПОЙНТЕ ПЛАНА `05-27` ВЕТВЬ `records-amended`, ЭТА ЗАДАЧА НЕ ИСПОЛНЯЕТСЯ: план остановлен ещё задачей 1 (`## BLOCKED`), и оговорка об историческом статусе `05-22-SUMMARY.md` ссылалась бы на действующую запись, которой в этой ветви не существует.</precondition>
  <files>.planning/phases/05-tarify/05-CONTEXT.md</files>
  <read_first>
    - .planning/phases/05-tarify/05-22-SUMMARY.md (ТОЛЬКО ЧТЕНИЕ, правке не подлежит — строка `134`, строка таблицы «395 дней → 60 дней (два календарных месяца)». Нужен её дословный текст и номер строки для оговорки)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-30 — фрагмент «ОБЕ ВЕЛИЧИНЫ ЭТОГО АБЗАЦА СВЕДЕНЫ С ИЗМЕРЕНИЕМ, И ПРИЧИНА СКАЗАНА СЛОВАМИ, А НЕ ЗНАКОМ ПРИБЛИЖЕНИЯ». Это объявление о собственном объёме, и оно правится)
    - .planning/phases/05-tarify/05-26-PLAN.md и .planning/phases/05-tarify/05-26-SUMMARY.md (ТОЛЬКО ЧТЕНИЕ — приём, которым фаза уже объявила `05-18-SUMMARY.md` историческим документом: точная формулировка и место, где она стоит. Приём ПРОДОЛЖАЕТСЯ, а не изобретается заново)
    - .planning/phases/05-tarify/05-VERIFICATION.md (третий элемент `gaps:`, второй пункт `missing:` — дословная формулировка двух допустимых исходов для `05-22-SUMMARY.md:134`)
  </read_first>
  <action>
**Оговорка об историческом статусе.** В запись D-30 добавляется абзац, объявляющий: строка
`134` файла `05-22-SUMMARY.md` несёт величину в прежней форме («395 дней → 60 дней»), и она
НЕ ПРАВИТСЯ — SUMMARY есть исторический документ, фиксирующий, что было измерено и записано в
момент исполнения плана. Названо: (а) что левое число там прежнее (`395`), а действующее —
`396`, сведённое планом `05-26`; (б) что правое число там прежнее (`60`), а действующая
форма — та, которую записала задача 1; (в) ГДЕ живёт действующая запись, чтобы читатель
SUMMARY не принял её за текущую; (г) что приём тот же, каким фаза уже поступила с
`05-18-SUMMARY.md`, — то есть прецедент продолжается, а не заводится.

**Объявление о сведении получает свой объём.** Фрагмент «ОБЕ ВЕЛИЧИНЫ ЭТОГО АБЗАЦА СВЕДЕНЫ С
ИЗМЕРЕНИЕМ» переписывается так, чтобы называть то, что в абзаце фактически сведено, и КЕМ:
две величины сведены планом `05-26` и подтверждены прогоном верификации раунда 7 до дня;
третья величина — выдача ветки отката ПОСЛЕ правки — сведена этой волной, прогоном, и
записана формой, а не константой. Прежняя формулировка называется как БЫВШАЯ У́ЖЕ АБЗАЦА:
она говорила «обе» там, где величин было три, и это и есть находка гэпа 3.

⚠️ Ни одна прежняя запись не стирается. Обе оговорки — добавления к существующему абзацу.

⚠️ Ни один исполненный артефакт фазы не правится: `05-22-SUMMARY.md` открывается только на
чтение, и это подтверждается пустым диффом по нему.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only -- .planning/phases/05-tarify/05-22-SUMMARY.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only -- .planning/phases/05-tarify/05-22-SUMMARY.md .planning/phases/05-tarify/05-18-SUMMARY.md` пуст
    - Оговорка существует и называет строку: `grep -c '05-22-SUMMARY.md' .planning/phases/05-tarify/05-CONTEXT.md` даёт не менее `1`, и вхождение сопровождается номером строки `134`
    - Оговорка ссылается на прецедент: `grep -c '05-18-SUMMARY.md' .planning/phases/05-tarify/05-CONTEXT.md` даёт не менее `1`
    - Объявление о сведении больше не говорит «обе» о трёх величинах: `grep -c 'ОБЕ ВЕЛИЧИНЫ ЭТОГО АБЗАЦА СВЕДЕНЫ С ИЗМЕРЕНИЕМ' .planning/phases/05-tarify/05-CONTEXT.md` даёт `0`, а новая формулировка называет ТРИ величины и кем каждая сведена
    - Прежняя формулировка названа, а не стёрта: в записи есть фраза о том, что прежнее объявление говорило «обе» при трёх величинах
    - `git diff -- .planning/phases/05-tarify/05-CONTEXT.md | grep -c '^+' ` больше нуля, а число удалённых строк не превышает числа строк двух правимых записей
  </acceptance_criteria>
  <done>Строка `05-22-SUMMARY.md:134` объявлена исторической с указанием действующей записи и прецедента; объявление о сведении величин называет три величины и автора каждого сведения; исполненные SUMMARY не тронуты</done>
</task>

<task type="auto">
  <name>Task 3: Матрица возможностей API сверяется после раунда 7 — без перегенерации</name>
  <reversibility rating="reversible">Добавление раздела к существующему документу; откат — его удаление.</reversibility>
  <precondition>Планы `05-28`, `05-29`, `05-30` и `05-31` исполнены и их SUMMARY существуют: сверка описывает СОСТОЯВШИЕСЯ правки, а не намеченные. ⚠️ Ветвь `records-amended` до этой задачи не доходит: план остановлен задачей 1. Сверка, описывающая правки волны, половина которой не исполнялась, была бы записью о несостоявшемся.</precondition>
  <files>.planning/phases/05-tarify/COVERAGE.md</files>
  <read_first>
    - .planning/phases/05-tarify/COVERAGE.md (таблица решений в начале документа, строки 18-40 — двадцать одна строка возможностей ЮKassa с решениями `INTEGRATE` / `OPT-OUT` и причинами. ⚠️ ЭТА ТАБЛИЦА НЕ ПЕРЕГЕНЕРИРУЕТСЯ И НЕ ПРАВИТСЯ: перегенерация теряет прежние решения вместе с причинами молча)
    - .planning/phases/05-tarify/COVERAGE.md (разделы «## Сверка после закрытия гэпа 1 (план `05-07`…)», «## Сверка после закрытия гэпов раунда 5 …», «## Сверка после закрытия гэпов раунда 6 …» — ОБРАЗЕЦ ДОСЛОВНЫЙ: состав раздела, форма таблицы, как называются незатронутые строки)
    - .planning/phases/05-tarify/05-28-SUMMARY.md, .planning/phases/05-tarify/05-30-SUMMARY.md (что именно изменилось на денежном пути этой волной — источник строки «затронуто» и её обоснования)
    - .planning/phases/05-tarify/05-VERIFICATION.md (`### Requirements Coverage` — состояние трёх требований по вердикту раунда 7; сверка обязана не спорить с ним)
  </read_first>
  <action>
Добавить в конец `COVERAGE.md` раздел «## Сверка после закрытия гэпов раунда 7 (планы
`05-27`…`05-32`, 2026-08-18)», построенный по образцу трёх предыдущих сверок.

Раздел обязан назвать ПОИМЁННО:

- **Затронутые строки матрицы.** Волна правила защиту чтения перечня тарифов
  (`_plan_price`, план `05-28`) на пути, который обслуживает уведомление
  `payment.succeeded`, и три объявления того же пути (план `05-30`). Названо, изменилось ли
  РЕШЕНИЕ по строке (`INTEGRATE` / `OPT-OUT`) — и если не изменилось, сказано прямо, что
  изменилась РЕАЛИЗАЦИЯ, а не решение.
- **Незатронутые строки — прямо, а не молчанием.** Все `OPT-OUT` (возвраты, чеки,
  автоплатежи, выплаты, сделки, счета, СБП, `settings`, `payments.list`, `payments.capture`,
  `payments.cancel`, `payments.find_one`, `webhooks.*`, `personal_data.*`) остаются в прежнем
  решении и с прежней причиной; раунд 7 к ним не прикасался. Молчание о них читалось бы
  недосмотром, а причина повторного решения не изменилась.
- **Что раунд 7 к матрице НЕ добавил.** Ни одна находка раунда 7 не требует новой
  возможности API: `CR-01` есть чтение конфига, `WR-01`…`WR-06` и `IN-01`…`IN-06` — либо
  объявления, либо интерфейс, либо инфраструктура. Сказано прямо, чтобы отсутствие новых
  строк не читалось пропуском.
- **Признак повторной сверки.** Одной фразой: следующая сверка нужна при появлении
  требования, вводящего новую возможность (возвраты, чеки, автопродление) — то есть при
  переводе любой строки `OPT-OUT` в `INTEGRATE`, а не по календарю.

⚠️ Ни одна существующая строка не удаляется и не переписывается: раздел ДОБАВЛЯЕТСЯ. Число
строк таблицы решений после правки обязано совпасть с числом до неё.
  </action>
  <verify>
    <automated>grep -c 'Сверка после закрытия гэпов раунда 7' .planning/phases/05-tarify/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Сверка после закрытия гэпов раунда 7' .planning/phases/05-tarify/COVERAGE.md` даёт `1`
    - Число строк таблицы решений не изменилось: `grep -cE '^\| `?(payments|refunds|receipts|webhook|saved|payouts|deals|personal_data|invoices|sbp_banks|settings|SecurityHelper)' .planning/phases/05-tarify/COVERAGE.md` даёт то же значение, что до задачи (оба значения приводятся в SUMMARY)
    - Ни одна строка не удалена: `git diff -- .planning/phases/05-tarify/COVERAGE.md | grep -c '^-[^-]'` даёт `0`
    - Раздел называет незатронутые строки ПРЯМО: он содержит перечень `OPT-OUT`-возможностей с фразой о том, что их решения и причины не изменились
    - Раздел содержит утверждение, что раунд 7 не потребовал ни одной новой возможности API, с перечислением классов находок
    - Раздел называет признак, по которому понадобится следующая сверка
    - Раздел не спорит с `### Requirements Coverage` отчёта раунда 7: BILL-05, BILL-06 и BILL-07 названы удовлетворёнными по вердикту, а отметка `Complete` в `REQUIREMENTS.md` этим планом НЕ возвращается — её возвращает верификация
  </acceptance_criteria>
  <done>Раздел сверки после раунда 7 добавлен; затронутые и незатронутые строки названы поимённо; нуль удалённых строк; отметка `Complete` требованиям не возвращается</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| записанная величина → следующее решение о деньгах | Константа величины устаревает от даты чтения; решение принимается по неверному факту |
| `05-CONTEXT.md` → `05-22-SUMMARY.md` | Одна величина в трёх местах; правка одного места без объявления статуса двух других заводит расхождение |
| исполненный SUMMARY → сегодняшнее состояние | Правка исторического документа стирает причину, по которой мера понадобилась |
| матрица возможностей → следующая интеграция | Перегенерация теряет прежние решения вместе с их причинами молча |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-220 | Repudiation | Константа величины «60 дней» остаётся в записях, по которым принимаются решения, будучи верной на 1,4 % дат | high | mitigate | Задача 1: константа заменена формой, которую документ может держать истинной; критерий требует нуля вхождений прежнего сочетания |
| T-05-221 | Repudiation | Новое число берётся из отчёта, а не из прогона, и повторяет тот же способ отказа на новом значении | high | mitigate | Задача 1: измерение снимается собственным прогоном, команда и полный вывод приводятся в SUMMARY; расхождение с ожиданием отчёта называется словами |
| T-05-222 | Repudiation | Правится одна запись из двух, и внутри одного документа заводится расхождение | high | mitigate | Задача 1: правятся ОБЕ (D-30 и D-31); критерий считает вхождения формы в документе |
| T-05-223 | Tampering | Исполненный SUMMARY правится ради согласования числа, и теряется свидетельство о том, что было измерено тогда | high | mitigate | Задача 2: SUMMARY открывается только на чтение; статус объявляется оговоркой; критерий требует пустого диффа по двум SUMMARY |
| T-05-224 | Repudiation | Оговорка об историческом статусе не называет, ГДЕ живёт действующая запись, и читатель SUMMARY принимает старую величину за текущую | medium | mitigate | Задача 2: оговорка обязана назвать место действующей записи; критерий требует номера строки и ссылки на прецедент |
| T-05-225 | Tampering | Матрица возможностей перегенерируется, и прежние решения с причинами теряются | medium | mitigate | Задача 3: раздел добавляется; критерий требует нуля удалённых строк и совпадения числа строк таблицы |
| T-05-226 | Repudiation | Молчание о незатронутых строках читается недосмотром, и следующая интеграция начинает не с полного покрытия | medium | mitigate | Задача 3: незатронутые названы прямо перечнем; названо и то, что раунд 7 не потребовал новой возможности |
| T-05-227 | Repudiation | Отметка `Complete` возвращается требованиям планом, а не верификацией | medium | mitigate | Задача 3: критерий прямо запрещает возврат отметки; `REQUIREMENTS.md` не входит в `files_modified` |
| T-05-228 | Information Disclosure | В записи о величинах попадают боевые идентификаторы магазина либо значения ключей ЮKassa | low | accept | Записи называют дни, цены прейскуранта и имена символов; значений секретов ни одна не содержит. Принимается тем же порядком, что для существующих записей документа |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов и не трогает манифесты: `git diff --name-only` содержит только два файла `.planning/`. Устанавливаемых пакетов ноль |
</threat_model>

<verification>

1. `! grep -q '60 дней (два календарных месяца)' .planning/phases/05-tarify/05-CONTEXT.md` завершается кодом `0` (форма важна: `grep -c` при нуле совпадений завершается кодом `1` и инвертировал бы проверку).
2. `git diff --name-only -- .planning/phases/05-tarify/05-22-SUMMARY.md .planning/phases/05-tarify/05-18-SUMMARY.md` пуст.
3. `git diff -- .planning/phases/05-tarify/COVERAGE.md | grep -c '^-[^-]'` даёт `0`.
4. `git diff --name-only` содержит ровно два файла: `05-CONTEXT.md` и `COVERAGE.md`. Ни одного файла `app/`, ни одного файла `tests/`, ни одного исполненного PLAN/SUMMARY.
5. `uv run pytest tests/ -q` завершается тем же кодом, что до плана: кода план не трогает.

</verification>

<success_criteria>

- Ни одна из двух записей больше не называет выдачу ветки отката константой величины.
- Числа сняты собственным прогоном, команда и вывод приведены.
- Третье место величины имеет объявленный статус исторического документа с указанием действующей записи.
- Объявление о сведении величин покрывает те величины, которые в абзаце есть.
- Матрица возможностей сверена без перегенерации; затронутые и незатронутые строки названы.

</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-32-SUMMARY.md` when done.

В SUMMARY привести: (1) команду прогона и её ПОЛНЫЙ вывод — множество значений с частотами,
значение на `2026-01-10`, значение на дате исполнения; (2) выбранный из двух допустимых
исходов и новую формулировку обеих записей целиком; (3) расхождение с распределением отчёта
раунда 7, если оно есть, названное словами; (4) счёт вхождений `396` и `140 дней Pro` до и
после — как доказательство, что уже сведённые числа не тронуты; (5) число строк таблицы
решений `COVERAGE.md` до и после и вывод `git diff --name-only`.
</output>
