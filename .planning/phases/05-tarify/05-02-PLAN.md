---
phase: 05-tarify
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/services/payment_service.py
  - tests/test_services/test_payment_service.py
  - tests/test_migrations/test_0017_payment_kind_and_plan.py
autonomous: true
requirements: [BILL-05, BILL-07]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Вебхук payment.canceled переводит платёж в статус canceled, проставляет момент решения и НЕ начисляет ни сообщений, ни дней подписки (D-16)"
    - "Отменённый платёж перестаёт вечно висеть в статусе pending — на экране истории у него есть терминальный статус (D-16, критерий 3)"
    - "Повторный вебхук по уже отменённому платежу возвращает True и второй раз ничего не меняет: терминальный статус обрабатывается один раз, как и succeeded"
    - "Событие, которого обработчик не знает (refund.succeeded, payment.waiting_for_capture), по-прежнему возвращает False"
    - "Имена событий берутся константами WebhookNotificationEventType из установленного SDK, а не строковыми литералами: литерал с опечаткой не поднимет ошибку, событие просто молча не обработается"
    - "Продление действующей подписки не сжигает оплаченный остаток, продление истёкшей считается от сегодня — обе половины D-04 закреплены отдельными тестами"
    - "Ревизия 0017 применяется на схеме уровня 0016 и откатывается обратно без потери строк; строка платежа, существовавшая до ревизии, после upgrade имеет kind='package'"
    - "После upgrade колонка messages_count принимает NULL, а до upgrade не принимала"
  artifacts:
    - "tests/test_migrations/test_0017_payment_kind_and_plan.py — round-trip ревизии по образцу test_0016_send_logs_user_sent_at.py"
    - "app/services/payment_service.py — ветка события payment.canceled в handle_webhook"
  key_links:
    - "handle_webhook: событие → наличие id → поиск строки → терминальный статус → ветвление по kind"
    - "Payment.status ('pending' | 'succeeded' | 'canceled') → подпись бейджа в истории платежей (план 05-05)"
  prohibitions:
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT показывать платёж в терминальном состоянии как «в обработке» — история статусов обязана отражать то, что на самом деле произошло с деньгами"
      status: resolved
      verification: test
---

<objective>
Достроить обработчик вебхука до честного набора статусов: платёж, который ЮKassa
отменила, обязан перестать выглядеть как «в обработке». Плюс round-trip ревизии `0017`
и починка теста, который эта фаза ломает намеренно.

Purpose: критерий 3 фазы требует «статус каждой операции». Сегодня `handle_webhook`
возвращает `False` на всё, кроме `payment.succeeded`, поэтому отменённый платёж
остаётся `pending` навсегда — история показывала бы неправду, а не отсутствие данных.
Output: ветка `payment.canceled`, round-trip-тест ревизии, переписанный
`test_handle_webhook_wrong_event`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-RESEARCH.md
@.planning/phases/05-tarify/05-PATTERNS.md
@.planning/phases/05-tarify/05-01-SUMMARY.md
</context>

<!-- planner-discipline-allow: test_handle_webhook_wrong_event -->
<!-- planner-discipline-allow: :memory: -->

<known_test_breakage>
`tests/test_services/test_payment_service.py::test_handle_webhook_wrong_event` ломается
**намеренно** и правится задачей 2 этого плана. Тест сегодня утверждает, что
`payment.canceled` — «неверное событие»; D-16 делает это событие верным, и имя теста
начинает лгать. Красный прогон здесь — ожидаемый шаг, а не сюрприз; чинить его
откатом D-16 запрещено.
</known_test_breakage>

<flagged_assumptions>
Строка edge-probe для **BILL-07** вернулась `unclassified` / `unresolved` (классификатор
англоязычный, требование написано по-русски). Поднимается явно как флагированное
допущение и не отбрасывается; края выведены вручную из критерия 3 ROADMAP и D-14…D-17
и лежат в `must_haves.truths` этого плана и плана `05-04`.

**Канон-отсылка:** «просмотр чужих платежей» — канон контроля доступа (OWASP), покрыт
`<threat_model>` плана `05-04` и `/gsd-secure-phase`; отдельной прохибиции не заводится.
</flagged_assumptions>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Ветка payment.canceled в обработчике вебхука</name>
  <files>app/services/payment_service.py, tests/test_services/test_payment_service.py</files>
  <read_first>
    - app/services/payment_service.py (весь файл после правок плана 05-01 — форма ветвления по событию и по kind)
    - .venv/lib/python3.12/site-packages/yookassa/domain/notification/webhook_notification_types.py (строки 20-40 — все семь объявленных констант событий)
    - app/models/payment.py (после правок плана 05-01 — состав колонок, `confirmed_at`)
    - tests/test_services/test_payment_service.py (весь файл — образец мока `YooPayment.create` и мока `invalidate_balance_cache`)
  </read_first>
  <behavior>
    - `handle_webhook(event=payment.canceled)` по существующему `pending`-платежу с `kind='package'` → `True`, `Payment.status == 'canceled'`, `add_messages` не вызывался, баланс не изменился
    - `handle_webhook(event=payment.canceled)` по существующему `pending`-платежу с `kind='subscription'` → `True`, `Payment.status == 'canceled'`, строка `Subscription` не создана и `expires_at` существующей не сдвинут
    - `handle_webhook(event=payment.canceled)` по платежу, уже находящемуся в `canceled` → `True`, повторной записи нет
    - `handle_webhook(event=payment.canceled)` по платежу в статусе `succeeded` → `True`, статус НЕ откатывается в `canceled`
    - `handle_webhook(event=payment.canceled)` по несуществующему `yookassa_payment_id` → `False`
    - `handle_webhook(event='refund.succeeded')` → `False`
    - `handle_webhook(event='payment.waiting_for_capture')` → `False`
    - `handle_webhook(event=payment.succeeded)` по `pending`-платежу — прежнее поведение не изменилось (регрессия)
  </behavior>
  <action>
RED → GREEN → REFACTOR. Сначала тесты из `<behavior>`, затем реализация.

Расширить проверку события в `handle_webhook` до двух известных: константы
`WebhookNotificationEventType.PAYMENT_SUCCEEDED` и
`WebhookNotificationEventType.PAYMENT_CANCELED` из `yookassa.domain.notification`.
Всё остальное по-прежнему возвращает `False` — в SDK объявлено семь событий, и
`refund.succeeded`, `payout.*`, `deal.closed` этой фазе не принадлежат (см.
`.planning/phases/05-tarify/COVERAGE.md`). Строковые литералы имён событий в коде не
появляются: опечатка в литерале не поднимет ошибку, событие просто молча не
обработается.

**Порядок проверок не меняется.** Событие → наличие `id` в теле → поиск строки по
`yookassa_payment_id` → **терминальный статус** → ветвление по предмету покупки. Сегодня
защита от повторной обработки написана как `status == "succeeded"`; она обобщается до
«строка уже в терминальном статусе» — то есть `succeeded` или `canceled`. Это ровно то
же свойство, распространённое на второй терминал, а не второй экземпляр защиты: копия в
каждой ветке рано или поздно разойдётся с оригиналом. Существующая строка лога
`webhook_payment_already_processed` сохраняется.

Ветка отмены минимальна и намеренно ничего не начисляет: `status` становится
`canceled`, момент решения записывается в существующую колонку времени подтверждения
(колонка одна, и «когда платёж перешёл в терминальное состояние» — это её смысл;
заводить вторую колонку под отмену D-15 не велит, а расширять решение владельца нельзя),
`add_messages` не вызывается, `Subscription` не трогается, `invalidate_balance_cache`
не зовётся — баланс не изменился, инвалидировать нечего. Лог по образцу
`payment_succeeded`: `user_id`, `yookassa_id`, `kind`; тело уведомления целиком не
логируется.

Причина отмены (`cancellation_details` в теле уведомления) в БД не пишется и на экран
не выводится: ни требование, ни макет её не называют, а разбор чужой структуры ради
неиспользуемого поля — лишний контракт с внешним форматом.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_payment_service.py -k "canceled or succeeded" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_services/test_payment_service.py -k "canceled or succeeded" -q` завершается с кодом 0
    - `app/services/payment_service.py` содержит `PAYMENT_CANCELED`
    - `grep -Ec '"payment\.(succeeded|canceled)"' app/services/payment_service.py` == 0 — имена событий не захардкожены строками
    - Поведение: вебхук отмены по `pending`-платежу с `kind='package'` даёт `Payment.status == 'canceled'` и оставляет `MessageBalance` без изменений
    - Поведение: вебхук отмены по `pending`-платежу с `kind='subscription'` не создаёт строк в `subscriptions`
    - Поведение: вебхук отмены по платежу в статусе `succeeded` возвращает `True` и оставляет статус `succeeded`
    - Поведение: вебхук отмены по неизвестному `yookassa_payment_id` возвращает `False`
    - Поведение: `handle_webhook` с событием `refund.succeeded` возвращает `False`
  </acceptance_criteria>
  <done>Отменённый ЮKassa платёж получает терминальный статус в журнале и никогда ничего не начисляет; неизвестные события по-прежнему игнорируются.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Починка намеренно сломанного теста и покрытие обеих половин D-04</name>
  <files>tests/test_services/test_payment_service.py</files>
  <read_first>
    - tests/test_services/test_payment_service.py (строки 60-95 — `test_handle_webhook_wrong_event` и `test_handle_webhook_idempotent`, которые правятся)
    - app/application/billing/subscription_period.py (после плана 05-01 — `next_expiry`, чью семантику закрепляют новые тесты)
    - app/services/payment_service.py (после задачи 1)
    - tests/conftest.py (фикстуры `db_session`, `client`, `test_settings`)
  </read_first>
  <behavior>
    - Тест «неизвестное событие» проверяет действительно неизвестное событие (`refund.succeeded`), а не `payment.canceled`
    - Новый тест: настоящая отмена настоящего платежа даёт `canceled`
    - Продление подписки, действующей ещё 10 дней, даёт срок = старый срок + 1 месяц (остаток не сгорел)
    - Продление подписки, истёкшей 40 дней назад, даёт срок = сегодня + 1 месяц (прошлый месяц не воскрешён)
    - Двойной вебхук по одному платежу подписки двигает срок ровно один раз
    - Первая покупка с Free создаёт строку `Subscription` с `plan` из платежа, а не с умолчанием модели
  </behavior>
  <action>
Переписать `test_handle_webhook_wrong_event`, а не «починить, чтобы прошёл». После D-16
`payment.canceled` — **знакомое** событие, поэтому имя и намерение теста лгут: он
утверждает «событие неверное», а событие теперь верное. Тест переводится на
действительно неизвестное обработчику событие; в SDK для этого есть `refund.succeeded`
и `payment.waiting_for_capture`. Имя теста приводится в соответствие с тем, что он
проверяет. В SUMMARY зафиксировать, что тест изменён по D-16, а не подогнан под
результат.

`test_handle_webhook_idempotent` существует для баланса; рядом заводится его
близнец для подписки — повторный вебхук по одному платежу подписки обязан двигать срок
ровно один раз. Именно эта регрессия ловит будущую перестановку проверки
идемпотентности за ветвление по предмету покупки.

Добавить четыре теста продления, закрывающие обе половины D-04 и D-02/D-03:
действующая подписка (остаток не сгорает), истёкшая подписка (считается от сегодня),
первая покупка с Free (строка создаётся, `plan` берётся из `Payment.plan`, а не из
умолчания модели `"free"`), двойной вебхук. Посевы ставят `expires_at` **явно** —
у колонки нет `server_default`, но неявное «сейчас» сделало бы тест «действующей
подписки» неотличимым от теста «истёкшей» в момент прогона на границе.

Мок ЮKassa — установленный образец из этого же файла (строки 18-37): `MagicMock` с
`id` и `confirmation.confirmation_url`, `patch` на `get_settings` и на
`YooPayment.create`. Мок `invalidate_balance_cache` через `AsyncMock` — образец там же.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_payment_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_services/test_payment_service.py -q` завершается с кодом 0
    - `grep -c 'test_handle_webhook_wrong_event' tests/test_services/test_payment_service.py` == 0 — тест переименован под то, что он проверяет
    - `tests/test_services/test_payment_service.py` содержит тест продления действующей подписки и тест продления истёкшей — оба именуют своё условие в имени
    - Поведение: подписка с `expires_at = now + 10 дней` после успешного вебхука продления имеет `expires_at = (now + 10 дней) + 1 месяц`
    - Поведение: подписка с `expires_at = now - 40 дней` после успешного вебхука продления имеет `expires_at = now + 1 месяц`
    - Поведение: два одинаковых вебхука по одному платежу подписки дают тот же `expires_at`, что и один
    - Поведение: пользователь без строки `Subscription` после успешного вебхука `kind='subscription'`, `plan='basic'` имеет ровно одну строку с `plan == 'basic'`
  </acceptance_criteria>
  <done>Ни один тест не утверждает того, что фаза отменила; обе половины правила нового срока и идемпотентность подписки закреплены именованными регрессиями.</done>
</task>

<task type="auto">
  <name>Task 3: Round-trip ревизии 0017</name>
  <files>tests/test_migrations/test_0017_payment_kind_and_plan.py</files>
  <read_first>
    - tests/test_migrations/test_0016_send_logs_user_sent_at.py (весь файл — докстринг с четырьмя оговорками, файловая БД вместо `:memory:`, синхронный тест, явные стартовая и целевая ревизии, хелпер чтения схемы)
    - tests/test_migrations/test_0014_sync_result_columns.py (проверка появившихся колонок через `PRAGMA table_info`)
    - alembic/versions/0017_payment_kind_and_plan.py (после плана 05-01)
    - app/models/payment.py (состав колонок таблицы `payments` — из него собирается DDL уровня 0016)
    - alembic.ini, alembic/env.py (как тест конфигурирует Alembic)
  </read_first>
  <action>
Ревизия не исполняется **ни в одном обычном тесте**: суита строит схему через
`Base.metadata.create_all` (`tests/conftest.py`) и о существовании Alembic не знает.
Поэтому текст ревизии `0017` проверяется только этим файлом.

Каркас копируется с `test_0016_send_logs_user_sent_at.py` вместе с четырьмя его
оговорками, которые обязаны попасть в докстринг нового файла: **файловая БД**, потому
что Alembic открывает собственное соединение и `:memory:` ему не виден; **тест
синхронный**, потому что `alembic/env.py` в online-режиме сам вызывает `asyncio.run`;
**стартовая и целевая ревизии названы явно** (`0016` → `0017`), имя `head` целью не
берётся — оно перестанет означать эту ревизию при следующем пополнении истории;
**зачем файл вообще существует**.

Содержание теста: создать таблицу `payments` в состоянии уровня `0016` сырым DDL
(состав колонок берётся из `app/models/payment.py`, `messages_count INTEGER NOT NULL`),
засеять одну строку старого платежа за пакет, проштамповать базу ревизией `0016`,
выполнить `upgrade("0017")` и проверить: колонки `kind` и `plan` существуют;
`messages_count` больше не `NOT NULL` (проверяется и по `PRAGMA table_info`, и
настоящей вставкой строки с `NULL`); засеянная до ревизии строка получила
`kind = 'package'` — тот самый backfill через `server_default`, ради которого отдельный
UPDATE не писался. Затем `downgrade("0016")` и проверка: колонок `kind` и `plan` нет,
`messages_count` снова `NOT NULL`, засеянная строка на месте — откат не теряет данных.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migrations/test_0017_payment_kind_and_plan.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_migrations/test_0017_payment_kind_and_plan.py -q` завершается с кодом 0
    - `tests/test_migrations/test_0017_payment_kind_and_plan.py` содержит `"0016"` и `"0017"` как явные аргументы штампа и цели
    - `grep -c '"head"' tests/test_migrations/test_0017_payment_kind_and_plan.py` == 0
    - `grep -c ':memory:' tests/test_migrations/test_0017_payment_kind_and_plan.py` == 0
    - Поведение: после `upgrade("0017")` вставка строки `payments` с `messages_count = NULL` проходит
    - Поведение: после `upgrade("0017")` строка, засеянная до ревизии, имеет `kind = 'package'`
    - Поведение: после `downgrade("0016")` засеянная строка по-прежнему присутствует
    - `uv run pytest tests/ -q` завершается с кодом 0
  </acceptance_criteria>
  <done>Текст ревизии 0017 доказанно применяется и откатывается на схеме уровня 0016 без потери строк.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ЮKassa → `POST /api/billing/webhook` → `handle_webhook` | Тело уведомления — недоверенный ввод; решение о статусе платежа принимается здесь |
| Alembic → боевая схема `payments` | Изменение схемы, применяемое к живым данным |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | Ветка `payment.canceled` откатывает уже проведённый платёж | high | mitigate | Задача 1: проверка терминального статуса стоит ДО ветки отмены; платёж в `succeeded` не переводится в `canceled`; закреплено именованным тестом |
| T-05-11 | Tampering | Повторный вебхук отмены как канал повторной записи | medium | mitigate | Задача 1: терминальный статус обрабатывается один раз, симметрично `succeeded`; покрыто тестом |
| T-05-12 | Spoofing | Подделка события именем-литералом с опечаткой (событие молча не обрабатывается) | medium | mitigate | Задача 1: имена событий — константы SDK; отсутствие строковых литералов проверяется грепом в acceptance criteria |
| T-05-13 | Information Disclosure | Разбор и логирование `cancellation_details` вместе с телом уведомления | medium | mitigate | Задача 1: причина отмены не пишется и не логируется; логируются только `user_id`, `yookassa_id`, `kind` |
| T-05-14 | Denial of Service | Ревизия `0017` необратима и роняет боевую схему | high | mitigate | Задача 3: round-trip доказан тестом (upgrade + downgrade без потери строк). Гейт выката — план `05-06` |
| T-05-15 | Repudiation | Отменённый платёж вечно висит `pending` — журнал не отражает, что произошло с деньгами | high | mitigate | Задачи 1 и 2: терминальный статус проставляется и покрыт тестом; прохибиция «не показывать терминальный платёж как в обработке» в `must_haves.prohibitions` |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План новых пакетов не устанавливает; аудит легитимности в `05-RESEARCH.md` §Package Legitimacy Audit, `[SUS]`-вердикты разобраны там же и относятся к уже установленной зависимости |
</threat_model>

## Artifacts this phase produces

Символы и файлы, которых до этой фазы не существовало (полный перечень по фазе; этот
план создаёт `tests/test_migrations/test_0017_payment_kind_and_plan.py` и ветку события
отмены):

**Файлы (новые):** `alembic/versions/0017_payment_kind_and_plan.py`,
`app/application/billing/__init__.py`, `app/application/billing/subscription_period.py`,
`app/application/billing/plan_usage.py`, `app/templates/billing/includes/plan_card.html`,
`app/templates/billing/includes/usage_meters.html`,
`app/templates/billing/includes/payment_row.html`,
`tests/test_application/test_subscription_period.py`,
`tests/test_application/test_plan_usage.py`,
`tests/test_pages/test_billing_subscription.py`, `tests/test_pages/test_billing_section.py`,
`tests/test_migrations/test_0017_payment_kind_and_plan.py`,
`tests/test_routes/test_billing_webhook_source.py`,
`.planning/phases/05-tarify/COVERAGE.md`

**Alembic revision id:** `0017` (`down_revision = "0016"`)

**Функции и макросы (новые):** `add_one_month`, `next_expiry`, `is_same_origin`,
`current_month_bounds_utc`, `sends_in_current_month`, `plan_axes`, Jinja-макросы
`plan_card`, `usage_meter`, `payment_row`

**Поля моделей (новые):** `Payment.kind`, `Payment.plan`; `Payment.messages_count` становится nullable

**Ключи конфига (новые):** `Settings.plan_limits`, `Settings.parsed_plan_limits`,
`Settings.yookassa_webhook_verify_ip`, `Settings.yookassa_webhook_client_ip_header`

**Маршруты (новые):** `POST /billing/subscribe`, `POST /billing/purchase`

**CSS-атрибут (новый):** `[data-plans]`

<verification>
- `uv run pytest tests/ -v` зелёный целиком
- Ни один тест проекта не утверждает, что `payment.canceled` — неизвестное событие
- Имена событий вебхука в `app/services/payment_service.py` — только константы SDK
</verification>

<success_criteria>
- Платёж, отменённый ЮKassa, получает терминальный статус и ничего не начисляет
- Обе половины правила нового срока закреплены отдельными именованными тестами
- Ревизия `0017` доказанно обратима
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-02-SUMMARY.md` when done
</output>
