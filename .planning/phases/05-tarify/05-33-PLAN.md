---
phase: 05-tarify
plan: 33
type: execute
wave: 23
depends_on: ["05-28"]
gap_closure: true
files_modified:
  - app/services/payment_service.py
  - .planning/phases/05-tarify/05-CONTEXT.md
  - .planning/phases/05-tarify/05-VERIFICATION.md
  - .planning/phases/05-tarify/05-UI-SPEC.md
  - .planning/phases/05-tarify/COVERAGE.md
autonomous: true
requirements: [BILL-05, BILL-06, BILL-07]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Статус прохибиции плана `05-01` формулируется в ОДНОМ месте — решении D-34 `05-CONTEXT.md`, объявленном КАНОНИЧЕСКИМ прочтением, — и все прочие потребители его цитируют. Диспозиция расхождения записей дана владельцем и записана: `override-accepted`"
    - "Гэп 2 раунда 7 (истина №11) закрыт МАШИННО ЧИТАЕМОЙ записью: блок `overrides:` во frontmatter `05-VERIFICATION.md` разбирается YAML-ом, несёт заполненные `accepted_by` / `accepted_at` и текст `must_have`, ДОСЛОВНО равный `gaps[1].truth` того же файла — равенство проверяется программой, а не глазами"
    - "Три абзаца `payment_service.py` о ветке нечитаемой цены (`:685-698`, `:724-732`, `:1007-1012`) описывают то, что код делает С ПЛАНА 05-22: база там `capped_carryover`, обещание о сохранении остатка держится только в объёме верхней границы, а статус прохибиции назван теми же словами, какими его уже назвал `subscription_period.py:359-372`. Спор двух объявлений одного денежного пути закрыт в сторону того, которое совпадает с прогоном"
    - "Строка C2 таблицы «Перенесено из фазы» (`05-UI-SPEC.md:377`) перестала утверждать три вещи, снятые решениями D-30, D-31 и D-34, и цитирует каноническое прочтение вместо того, чтобы формулировать четвёртый статус той же прохибиции"
    - "Записи D-30 и D-31 больше не называют выдачу ветки отката константой величины: на её месте стоит форма, которую документ может держать истинной на всякой дате чтения. Числа сняты СОБСТВЕННЫМ прогоном исполнителя, а не переписаны из отчёта"
    - "Два пункта записаны ПРИНЯТЫМ ДОЛГОМ с названными причинами, а не заведены работой: (1) гейт объявлений не проверяет, что свидетель — О ТОМ ЖЕ утверждении, и это принято решением владельца; (2) исполненные артефакты `05-22-PLAN.md` (`must_haves` №8) и `05-22-SUMMARY.md:134` сохраняют свой текст и объявлены ИСТОРИЧЕСКИМИ"
    - "Матрица возможностей API `COVERAGE.md` сверена после раунда 7 и НЕ перегенерирована: затронутые строки названы поимённо, незатронутые названы незатронутыми прямо"
    - "Ни один машинный гейт этим планом не заведён и ни один существующий не расширен: `tests/` не входит в `files_modified`, число собранных тестов суиты не изменилось, реестр долга гейта объявлений не вырос"
    - statement: "Следующий читатель денежного пути — докстринга, контракта интерфейса или записи решений — находит ОДИН ответ на вопрос «сжигается ли остаток и в каком объёме», и этот ответ совпадает с прогоном. Единственность и понятность прочтения судятся человеком: отличить «второй источник истины» от «полезного повторения» машине нечем (решение D-33)"
      verification: backstop
  artifacts:
    - "`.planning/phases/05-tarify/05-CONTEXT.md` — решение D-34 (диспозиция `override-accepted`, каноническое прочтение прохибиции `05-01`, два принятых долга, оговорка о нумерации); записи D-30 и D-31 без константы величины; объявленный исторический статус `05-22-SUMMARY.md:134`"
    - "`.planning/phases/05-tarify/05-VERIFICATION.md` — блок `overrides:` во frontmatter, закрывающий истину №11 / гэп 2 раунда 7"
    - "`app/services/payment_service.py` — три приведённых абзаца о ветке нечитаемой цены, каждый со ссылкой на каноническое прочтение и с названной прежней редакцией"
    - "`.planning/phases/05-tarify/05-UI-SPEC.md` — строка C2 таблицы «Перенесено из фазы», приведённая к D-30/D-31/D-34"
    - "`.planning/phases/05-tarify/COVERAGE.md` — раздел сверки после раунда 7"
  key_links:
    - "`gaps[1].truth` (`05-VERIFICATION.md:53`) → поле `must_have` блока `overrides:`. Связь ДОСЛОВНАЯ и машинная: короткий пересказ из `### Gaps Summary` есть образец ФОРМЫ, а не источник текста, и блок, взявший текст оттуда, выглядел бы закрывающим, не связываясь с закрываемым пунктом"
    - "D-34 (`05-CONTEXT.md`) → три абзаца `payment_service.py` → строка C2 `05-UI-SPEC.md`. Цепочка ОДНОСТОРОННЯЯ: статус прохибиции формулируется в вершине и цитируется вниз. Четыре экземпляра класса «объявление против кода» в раунде 7 пришли ровно из того, что источников было три"
    - "`capped_carryover` (`app/application/billing/subscription_period.py:333`) → величина в записях D-30/D-31. Величина ВЫЧИСЛЯЕТСЯ от даты отсчёта: `add_one_month` считает месяцы, а не дни, поэтому запись, называющая её одним числом без даты, истинна лишь на части дат"
  prohibitions:
    - requirement_id: BILL-05
      category: integrity
      statement: "MUST NOT править содержимое исполненных `05-01`…`05-26` PLAN.md и SUMMARY.md: закрытие идёт ПРИСТРОЕННОЙ записью, а исторический документ фиксирует, что было объявлено и измерено тогда"
      status: unresolved
      verification: test
    - requirement_id: BILL-06
      category: integrity
      statement: "MUST NOT расширять `tests/test_application/test_declared_invariants.py` и MUST NOT заводить новый машинный гейт: каждый новый гейт этого класса производит собственную поверхность проверки, и именно этим путём раунд 7 нашёл свой гэп в щели средства, построенного тем же раундом"
      status: unresolved
      verification: test
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT закрывать расхождение трёх абзацев переопределением вместо исправления: `override` на них записал бы «мы принимаем, что три абзаца лгут» — тот самый класс, на котором фаза отказала четырежды"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT называть в операционном документе константу величины, которую документ не может держать истинной: верное значение становится неверным в момент коммита, и следующий читатель принимает решение по неверному факту"
      status: unresolved
      verification: test
    - requirement_id: BILL-05
      category: safety
      statement: "MUST NOT менять ни одной ИСПОЛНЯЕМОЙ строки `app/`: поведение денежного пути принято владельцем и записано D-31, а его изменение требует своего чекпойнта и своего плана"
      status: unresolved
      verification: test
---

<!-- planner-discipline-allow: откат к прежнему поведению -->
<!-- planner-discipline-allow: держится в обоих случаях -->
<!-- planner-discipline-allow: СОБЛЮДЕНА, а не переопределена -->
<!-- planner-discipline-allow: не изменено ни на строку -->
<!-- planner-discipline-allow: на остаток младшего бесплатно -->
<!-- planner-discipline-allow: 60 дн -->
<!-- planner-discipline-allow: ОБЕ ВЕЛИЧИНЫ ЭТОГО АБЗАЦА СВЕДЕНЫ С ИЗМЕРЕНИЕМ -->

<objective>
**Сведение записей и одна запись решения. Ничего больше — и это главное свойство плана,
а не оговорка о его скромности.**

Арифметика фазы, из-за которой объём закрытия раунда 7 сужен владельцем:

| | раунд 6 | раунд 7 |
|---|---|---|
| счёт | 17/20 | 19/24 |
| незакрытых | 3 | **5** |

Две истины закрыты, а знаменатель вырос на четыре — и вырос он **сам от себя**. План `05-22`
завёл свой `must_have` №8, и он стал гэпом 2 раунда 7. План `05-25` построил гейт объявлений,
и из него родились истины №18-20 **и та объявленная щель**, в которой раунд 7 нашёл свой гэп 1.
То есть гэп 1 раунда 7 найден в тексте, который правили планы ЭТОГО ЖЕ раунда, и в щели
средства, построенного планом ЭТОГО ЖЕ раунда.

При этом BILL-05, BILL-06 и BILL-07 — все три `✓ SATISFIED`; критерии успеха 1-3 —
`✓ VERIFIED`; критерий 4 держится `PRESENT_BEHAVIOR_UNVERIFIED` только потому, что
браузерного харнесса в проекте нет.

**Вывод владельца: цель фазы достигнута, а каждый следующий раунд закрытия производит
собственный следующий раунд. Чинится то, что действительно сломано; исправляется то, что
действительно ложно; записывается то, что действительно решение; фаза закрывается.**

Отсюда — прямой запрет, объявленный этим планом на себя:

- он **НЕ вносит новых `must_haves.truths`** сверх того, что доказывает сам;
- он **НЕ расширяет** `tests/test_application/test_declared_invariants.py`;
- он **НЕ заводит нового машинного гейта**;
- каталог `tests/` не входит в его `files_modified` вовсе.

Эта сдержанность и есть предмет сужения. Средство, закрывающее щель средства, — следующая
щель; там, где выбор стоит между «построить проверку» и «записать принятый долг с названной
причиной», этот план записывает долг.

**Что он всё-таки делает — четыре исправления и одна запись:**

1. **Три абзаца `payment_service.py` лгут с плана `05-22`** (`:724-732`, `:1007-1012`,
   `:685-698`). Они утверждают, что срок «по-прежнему считается от `expires_at`», что там
   «откат к прежнему поведению», что обещание «остаток не сгорает» «держится в обоих
   случаях», и что прохибиция `05-01` «СОБЛЮДЕНА, а не переопределена». Всё это неверно:
   база там `capped_carryover`, а остаток 365 дней превращается в 59 дней от 2026-01-10 и
   61 день от сегодняшнего дня. **Это ИСПРАВЛЕНИЕ, а не переопределение.** `override` на них
   записал бы «мы принимаем, что три абзаца лгут» — ровно тот класс, за который фаза получила
   четыре раунда подряд, и владелец его отверг прямо.
2. **Строка C2 `05-UI-SPEC.md:377`** утверждает три вещи, снятые решениями D-30 и D-31.
3. **Константа «60 дней (два календарных месяца)»** в записях D-30 и D-31. Измерение:
   59 на дате отсчёта самого абзаца, 61 сегодня, распределение по 730 датам
   `{56: 2, 57: 2, 58: 2, 59: 113, 60: 10, 61: 484, 62: 117}` — записанное значение самое
   редкое из семи, верное на 1,4 % дат.
4. **Запись:** владелец ОТВЕТИЛ на чекпойнт, который собирался поднять снятый план `05-27`.
   Диспозиция — **`override-accepted`**. Она записывается решением **D-34**, объявляется
   КАНОНИЧЕСКИМ прочтением прохибиции `05-01`, и исправления 1 и 2 делаются согласованными
   с ней.
5. **Два принятых долга** — записями, а не работой: щель гейта объявлений (свидетель может
   быть «не о том»); исторический статус `05-22-PLAN.md` (`must_haves` №8) и
   `05-22-SUMMARY.md:134`.
6. **Сверка `COVERAGE.md`** после раунда 7 — как после раундов 5 и 6.

Purpose: там, где один предмет описан тремя источниками, следующая правка возьмёт любой из
трёх; единственность прочтения дешевле всякой проверки над тремя.
Output: одно каноническое прочтение, четыре приведённых к нему текста, машинно читаемое
закрытие истины №11 и сверенная матрица возможностей.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-UI-SPEC.md
@.planning/phases/05-tarify/COVERAGE.md
@app/services/payment_service.py
@app/application/billing/subscription_period.py
</context>

<flagged_assumptions>

**Зонд краевых случаев: три требования фазы остаются `unclassified` / `unresolved`.**
Классификатор зонда работает по английским формулировкам, а BILL-05, BILL-06 и BILL-07
записаны по-русски, поэтому все три строки вернулись `unclassified`. Это не даёт права ни
снять их, ни объявить покрытыми — они поднимаются явными допущениями и остаются `unresolved`:

- **E-01 (BILL-05, `unresolved`)** — краевые состояния продления. Допущение: план не трогает
  ни одной исполняемой строки денежного пути; он приводит ОБЪЯВЛЕНИЯ о поведении, которое
  код уже исполняет, и ЗАПИСЬ о величине, которую код уже отдаёт.
- **E-02 (BILL-06, `unresolved`)** — краевые состояния показа осей. Допущение: не задевается;
  разметка и страничный слой в `files_modified` не входят.
- **E-03 (BILL-07, `unresolved`)** — краевые состояния журнала платежей. Допущение: не
  задевается; правки касаются докстрингов и документов планирования.

Равенство: **3 поднятых == 3 записанных допущения == 0 внесённых в `must_haves.truths`**.

**Допущение о распределении величины.** Числа `{56: 2, 57: 2, 58: 2, 59: 113, 60: 10,
61: 484, 62: 117}` сняты верификацией раунда 7. Исполнитель обязан ПЕРЕСНЯТЬ их собственным
прогоном над настоящим `capped_carryover` и записать СВОИ; числа отчёта служат ожиданием и
ранним сигналом расхождения, а не источником истины. Расхождение, если оно есть, называется
словами, а не выравнивается подгонкой.

**Допущение о подписи владельца.** Диспозиция `override-accepted` дана владельцем в сессии
планирования 2026-08-18. Значение `accepted_by` берётся из идентичности репозитория
(`git config user.name`), `accepted_at` — момент исполнения задачи в ISO-8601 UTC.
Заполнителей вида `⟨…⟩` в файле не остаётся, и это проверяется машинно.
</flagged_assumptions>

<artifacts_this_phase_produces>

| Артефакт | Где | Что это |
|---|---|---|
| Решение `D-34` | `05-CONTEXT.md`, подраздел решений владельца | Диспозиция `override-accepted`; КАНОНИЧЕСКОЕ прочтение статуса прохибиции `05-01`; два принятых долга; оговорка о нумерации |
| Блок `overrides:` | frontmatter `05-VERIFICATION.md` | Машинно читаемое закрытие истины №11 / гэпа 2 раунда 7 |
| Формулировка-инвариант выдачи ветки отката | `05-CONTEXT.md`, записи D-30 и D-31 | Диапазон либо число с явно названной датой отсчёта — вместо константы |
| Оговорка об историческом статусе `05-22-SUMMARY.md` | `05-CONTEXT.md`, запись D-30 | Объявление, что исполненный SUMMARY правке не подлежит, и указание, где живёт действующая запись |
| Раздел «Сверка после закрытия гэпов раунда 7» | `COVERAGE.md` | Затронутые строки поимённо, незатронутые названы незатронутыми |

**Символов кода план не вводит.** Ни одной новой функции, ни одного нового теста, ни одного
нового рецепта `justfile`. Единственный файл `app/`, который он открывает на запись, —
`app/services/payment_service.py`, и в нём меняются ТОЛЬКО строки докстрингов и комментариев.
Каталог `tests/` не входит в `files_modified` намеренно и на этом основании.

**Почему ни одна задача не помечена `tdd="true"`** при включённом `workflow.tdd_mode`: ни одна
из трёх не производит кода. Задачи 1 и 3 правят документы планирования; задача 2 правит
докстринги и одну строку таблицы контракта. Это ровно исключение «configuration-only /
documentation», записанное в правилах декомпозиции, а не обход режима: тест, написанный
раньше правки абзаца, был бы новым машинным гейтом — тем самым, который план себе запретил.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Ответ владельца записывается решением D-34 и объявляется КАНОНИЧЕСКИМ прочтением; блок overrides: закрывает истину №11 машинно; два долга приняты записями</name>
  <reversibility rating="costly">Запись ставит подпись владельца под принятой ценой (около одиннадцати месяцев оплаченного времени у предоплатившего год) и снимает объявленный `must_have` исполненного плана. Она становится основанием, которое цитируют задача 2 этого плана и всякий следующий читатель денежного пути; отозвать её означает объявить недействительными решения, принятые по ней. Чекпойнта здесь нет намеренно: развилка уже разрешена владельцем, и повторный вопрос был бы новой поверхностью, а не проверкой.</reversibility>
  <precondition>Отчёт `.planning/phases/05-tarify/05-VERIFICATION.md` раунда 7 существует, его frontmatter несёт `status: gaps_found` и разбирается как YAML, а список `gaps:` содержит не менее двух элементов, второй из которых говорит о `must_have` №8 плана `05-22`. Проверяется до начала работы: `python3 -c "import re,yaml; t=open('.planning/phases/05-tarify/05-VERIFICATION.md',encoding='utf-8').read(); d=yaml.safe_load(re.match(r'^---\n(.*?)\n---\n',t,re.S).group(1)); assert d['status']=='gaps_found' and len(d['gaps'])&gt;=2; print('ok')"`. Без отчёта запись закрывала бы пункт, которого никто не выносил.</precondition>
  <files>.planning/phases/05-tarify/05-CONTEXT.md, .planning/phases/05-tarify/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-tarify/05-VERIFICATION.md (ВТОРОЙ элемент блока `gaps:` целиком — `truth`, `reason`, `artifacts`, `missing`. ⚠️ Поле `truth` этого элемента есть ЕДИНСТВЕННЫЙ источник текста для поля `must_have` блока `overrides:`)
    - .planning/phases/05-tarify/05-VERIFICATION.md (раздел `### Gaps Summary`, абзац «**Предложение `override` — гэп 2 выглядит намеренным**» с готовым YAML. ⚠️ ГОТОВЫЙ YAML СЛУЖИТ ОБРАЗЦОМ ФОРМЫ — состава ключей и их порядка — И ИСТОЧНИКОМ ТЕКСТА `reason`, НО НЕ ИСТОЧНИКОМ `must_have`: его строка `must_have` короче и сформулирована иначе, чем закрываемый пункт, и копирование её провалило бы машинную сверку)
    - .planning/phases/05-tarify/05-CONTEXT.md (записи D-28…D-33 — ОБРАЗЕЦ ДОСЛОВНЫЙ: состав записи, порядок частей, форма квалификации «(гэп N раунда M, форма **(а)**, идентификатор `xxx`, ответ владельца дословно — `xxx`)», форма `— **Reversibility:** …`, форма «⚠️ Принятый долг назван явно», форма «Форсирующий признак возврата к вопросу»)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-31 целиком — источник числовой цены исключения и перечня отвергнутых форм. D-34 её НЕ переписывает и НЕ дублирует, а ссылается)
    - .planning/phases/05-tarify/05-CONTEXT.md (строки в районе `140` и `160`, раздел «Решения прошлых фаз, действующие здесь» — там процитированы `D-35`, `D-36`, `D-37` ФАЗЫ 4. Нужны их точные места для оговорки о нумерации)
    - app/application/billing/subscription_period.py (докстринг `capped_carryover`, абзац `359-372` — ИСТОЧНИК СЛОВ. Статус прохибиции в D-34 называется теми же словами, какими его уже сказал код)
    - .planning/phases/05-tarify/05-22-PLAN.md (ТОЛЬКО ЧТЕНИЕ, правке не подлежит — строка №8 блока `must_haves.truths`. Нужна для оговорки об историческом статусе)
    - tests/test_application/test_declared_invariants.py (ТОЛЬКО ЧТЕНИЕ — докстринг `:23-38`, где средство САМО объявляет свою границу: абзац, называющий существующий, но не тот тест, гейт пропустит зелёным. Формулировка принятого долга берётся оттуда, а не сочиняется)
  </read_first>
  <action>
Задача пишет ДВЕ записи в двух файлах. Обе — ДОБАВЛЕНИЕ; ни одна существующая строка не
стирается.

**(1) Решение D-34 в `.planning/phases/05-tarify/05-CONTEXT.md`.** Встаёт следующим номером
после D-33, в том же подразделе решений владельца, и повторяет состав D-31 по частям.

- **Заголовок с квалификацией:** «(гэп 2 раунда 7, диспозиция расхождения записей,
  идентификатор `override-accepted`, ответ владельца дословно — `override-accepted`)».
- **Что решено:** `must_haves.truths` №8 плана `05-22` СНЯТ ответом владельца `cap-one-month`
  на чекпойнт задачи 1 того же плана. Прохибиция плана `05-01` («MUST NOT сжигать
  неистраченный остаток уже оплаченного периода») имеет РОВНО ОДНО объявленное исключение —
  остаток свыше одного календарного месяца не переносится в ветке, где цену любого из двух
  планов прочитать нельзя.
- **⚠️ ЭТА ЗАПИСЬ ОБЪЯВЛЯЕТСЯ КАНОНИЧЕСКИМ ПРОЧТЕНИЕМ.** Отдельным абзацем, прямым текстом:
  статус прохибиции `05-01` формулируется ЗДЕСЬ и нигде больше; всякий потребитель —
  докстринги `app/services/payment_service.py`, строка C2 `05-UI-SPEC.md`, будущие планы —
  ЦИТИРУЕТ это прочтение, а не формулирует своё. Причина названа механикой, а не этикетом:
  вторая формулировка одного предмета есть второй источник истины, и именно из него пришли
  четыре экземпляра класса «объявление против кода» в раунде 7. Названо и то, где прочтение
  уже сказано КОДОМ (`app/application/billing/subscription_period.py:359-372`,
  `capped_carryover`), и что запись приведена К КОДУ, а не наоборот.
- **Цена названа числами, и у каждого числа стоит дата отсчёта.** Остаток КОРОЧЕ месяца
  переносится целиком и не теряет ни дня. Остаток ДЛИННЕЕ месяца не переносится в части
  свыше месяца: посев «живой горизонт 365 дней + платёж `pro`» даёт 59 дней от 2026-01-10 и
  61 день от сегодняшнего дня, то есть около 336 и около 334 оплаченных дня соответственно
  НЕ переносятся; D-31 называет ту же цену словами «около одиннадцати месяцев оплаченного
  времени». ⚠️ Ни одно число не пишется без даты, от которой оно снято, — приёмом, каким
  D-30 записал 396 и 140.
- **Почему исправление, а не переопределение — сказано прямо.** `override` закрывает
  расхождение ЗАПИСЕЙ (истина №11): текст исполненного плана против ответа владельца на
  чекпойнт того же плана. Он НЕ распространяется на три абзаца `payment_service.py` и на
  строку C2 `05-UI-SPEC.md`: там утверждения о ПОВЕДЕНИИ, и они просто неверны. Записать их
  переопределением значило бы записать «мы принимаем, что три абзаца лгут» — тот самый класс,
  на котором фаза отказала четырежды. Поэтому они ИСПРАВЛЯЮТСЯ (задача 2), и это названо
  в самой записи.
- **Отвергнутая ветвь:** `records-amended` — с причиной одной фразой: пересмотр прочтения
  вернул бы величину, которой управляет покупатель, без верхней границы на ветке отката, то
  есть отменил бы результат, ради которого фаза прошла шесть раундов.
- **⚠️ ПРИНЯТЫЙ ДОЛГ НАЗВАН ЯВНО — ДВА ПУНКТА, И ОБА С ПРИЧИНАМИ:**
  - **(а) Гейт объявлений не проверяет, что свидетель — О ТОМ ЖЕ утверждении.** Средство
    объявляет эту границу само (`tests/test_application/test_declared_invariants.py`,
    докстринг `:23-38`), и формулировка берётся оттуда. Долг принят решением владельца, и
    причина названа механикой, а не ленью: всякое машинное закрытие этой щели вводит НОВУЮ
    поверхность проверки, а раунд 7 нашёл свой гэп именно в щели средства, построенного тем
    же раундом. Форсирующий признак возврата к вопросу назван: находка класса «свидетель не о
    том» в раунде, который сам этих абзацев не правил.
  - **(б) Исполненные артефакты объявлены ИСТОРИЧЕСКИМИ.** `05-22-PLAN.md` (`must_haves`
    №8) сохраняет свой текст: он фиксирует, что было объявлено ТОГДА, и правка стёрла бы
    причину, по которой понадобилась эта запись. Машинное закрытие пункта берёт на себя блок
    `overrides:`. Форма оговорки — та же, какой фаза уже обошлась с `05-18-SUMMARY.md`
    (см. запись D-30), то есть прецедент ПРОДОЛЖАЕТСЯ, а не заводится.
- **Сужение объёма закрытия раунда 7 — записано здесь же, потому что это решение владельца,
  а не свойство планирования.** Названо: спланированные планы `05-27`, `05-29`, `05-30` и
  `05-32` СНЯТЫ до исполнения; в работе остались `05-28` (единственный настоящий дефект кода),
  `05-31` (механизм трекинга) и `05-33` (эта запись и приведение текстов). Причина названа
  арифметикой: раунд 6 — 17/20, раунд 7 — 19/24; знаменатель вырос на четыре, и вырос от
  артефактов, произведённых предыдущим раундом закрытия. Названы и снятые сущности:
  расширение множества модулей гейта и различение свидетеля по имени в работу НЕ входят и
  учтены пунктом (а) принятого долга.
- `— **Reversibility:** one-way` с одной фразой обоснования.
- **Оговорка о нумерации — одной фразой, и она обязательна.** `D-34` есть следующий свободный
  номер решений ФАЗЫ 5 после `D-33`, и это верно. Но в этом же файле уже встречаются
  идентификаторы `D-35`, `D-36` и `D-37` (раздел «Решения прошлых фаз, действующие здесь»,
  места около строк `140` и `160`): они **принадлежат Фазе 4** и процитированы здесь как
  контекст, со своей нумерацией. Запись говорит это теми же словами, чтобы следующий
  планировщик не прочёл номера как занятые и не начал нумерацию Фазы 5 с `D-38`.

**(2) Блок `overrides:` во frontmatter `.planning/phases/05-tarify/05-VERIFICATION.md`.**
Встаёт на верхнем уровне frontmatter рядом с `deferred:`, формой из `### Gaps Summary`:
ключи `must_have`, `reason`, `accepted_by`, `accepted_at`.

⚠️ **У поля `must_have` РОВНО ОДИН источник, и это НЕ готовый YAML отчёта.** Текст копируется
ДОСЛОВНО из поля `truth` ВТОРОГО элемента блока `gaps:` того же файла. Готовый YAML в
`### Gaps Summary` даёт ФОРМУ и текст `reason`, но его собственная строка `must_have` —
пересказ: она опускает и цитату прохибиции, и ссылку на строку C2 `05-UI-SPEC.md`. Взять текст
оттуда значит завести блок, который ВЫГЛЯДИТ закрывающим и не связывается с закрываемым
пунктом. Сверка делается программно (см. `<verify>`), а не глазами.

`reason` — из готового YAML отчёта, с добавлением ссылки на D-34: снят ответом владельца
`cap-one-month` на чекпойнт задачи 1 плана `05-22`; перенос свыше одного календарного месяца
есть объявленное ИСКЛЮЧЕНИЕ из прохибиции `05-01`; D-31 записывает цену числом; альтернатива
`no-carry` отвергнута с причиной. `accepted_by` — идентичность репозитория
(`git config user.name`); `accepted_at` — момент исполнения в ISO-8601 UTC. Заполнителей вида
`⟨…⟩` в файле не остаётся.

⚠️ Поле `overrides_applied: 0` во frontmatter отчёта — ЗАПИСЬ РАУНДА 7, снятая верификатором
в момент прогона, и она НЕ правится: правка чужого вердикта задним числом есть та же потеря
записи, что стёртая запись. Блок `overrides:` добавляется РЯДОМ, а расхождение с полем
`overrides_applied` оговаривается одной фразой в самом блоке — счёт применённых
переопределений возвращает следующая верификация, а не этот план.

⚠️ Никакого нового теста, никакой новой проверки, никакой правки `tests/` эта задача не
производит. Сверка `must_have` == `gaps[1].truth` есть одноразовая команда критерия
приёмки, а не заводимый гейт: гейт над этим полем был бы ровно той новой поверхностью,
ради отказа от которой объём и сужен.
  </action>
  <verify>
    <automated>python3 -c "import re,yaml; t=open('.planning/phases/05-tarify/05-VERIFICATION.md',encoding='utf-8').read(); d=yaml.safe_load(re.match(r'^---\n(.*?)\n---\n',t,re.S).group(1)); o=d.get('overrides') or []; assert len(o)==1, o; e=o[0]; assert all(e.get(k) for k in ('must_have','reason','accepted_by','accepted_at')), e; assert '⟨' not in str(e), e; assert e['must_have']==d['gaps'][1]['truth'], 'must_have != gaps[1].truth'; print('overrides ok')" &amp;&amp; grep -q 'D-34' .planning/phases/05-tarify/05-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - Команда `<verify>` завершается кодом `0` и печатает `overrides ok`: блок `overrides:` разбирается YAML-ом, все четыре ключа заполнены, заполнителей `⟨…⟩` нет, и текст `must_have` ДОСЛОВНО равен `gaps[1].truth`. ⚠️ Этот критерий и есть машинное различение двух источников: текст, взятый из готового YAML `### Gaps Summary`, его НЕ проходит
    - Запись D-34 существует и стоит следующим номером после D-33 в подразделе решений владельца: `grep -q 'D-34' .planning/phases/05-tarify/05-CONTEXT.md` завершается кодом `0`, и в SUMMARY приведена строка записи целиком
    - Запись D-34 несёт все части образца D-31 — проверяется грепом по каждой подстроке отдельно: `override-accepted`, `records-amended`, `Reversibility:`, `Принятый долг назван явно`, `Форсирующий признак возврата к вопросу`
    - Запись D-34 объявляет себя каноническим прочтением и называет источник слов: `grep -q 'КАНОНИЧЕСК' .planning/phases/05-tarify/05-CONTEXT.md` и `grep -q 'subscription_period.py:359-372' .planning/phases/05-tarify/05-CONTEXT.md` — обе команды завершаются кодом `0`
    - Запись D-34 содержит числа `59` и `61` и рядом с каждым — дату отсчёта (`2026-01-10` и «сегодняшний день»); ни одно число выдачи не стоит без даты. Приводится в SUMMARY цитатой фрагмента
    - Оба принятых долга записаны и оба с причинами: запись содержит ссылку на `test_declared_invariants.py` (щель свидетеля) и ссылку на `05-22-PLAN.md` с объявлением исторического статуса. Обе проверяются грепом
    - Оговорка о нумерации на месте: `grep -q 'принадлежат Фазе 4' .planning/phases/05-tarify/05-CONTEXT.md` завершается кодом `0`, и фраза стоит внутри записи D-34
    - Сужение объёма записано: запись называет снятыми планы `05-27`, `05-29`, `05-30`, `05-32` и оставшимися `05-28`, `05-31`, `05-33`, с причиной, названной арифметикой (`17/20` и `19/24`)
    - Исполненные артефакты не тронуты: `git diff --name-only -- .planning/phases/05-tarify/05-22-PLAN.md .planning/phases/05-tarify/05-22-SUMMARY.md` пуст
    - Во вердикт раунда 7 только добавлено, ничего не удалено: `git diff -- .planning/phases/05-tarify/05-VERIFICATION.md | grep -c '^-[^-]'` даёт `0`
    - Каталог `tests/` не тронут: `git diff --name-only -- tests/` пуст
  </acceptance_criteria>
  <done>Решение D-34 записано своим номером, объявлено каноническим прочтением и несёт оба принятых долга с причинами и оговорку о нумерации; блок `overrides:` разбирается YAML-ом, несёт подпись и дословно совпадающий с `gaps[1].truth` текст; исполненные артефакты и `tests/` не тронуты</done>
</task>

<task type="auto">
  <name>Task 2: Три абзаца payment_service.py и строка C2 контракта описывают то, что код делает с плана 05-22, и цитируют каноническое прочтение</name>
  <reversibility rating="costly">Правка объявлений денежного пути и строки контракта интерфейса. Ошибка здесь не роняет прогон, но переживает его: именно из расхождения объявления с поведением фаза получила раунды 4, 5, 6 и 7, и следующий читатель «чинит» код в сторону комментария.</reversibility>
  <precondition>План `05-28` исполнен и слит: `app/services/payment_service.py` уже несёт защиту `_plan_price` и её переписанное объявление, а `uv run pytest tests/test_application/test_declared_invariants.py -q` на дереве ДО этой задачи завершается кодом `0`. Гейт объявлений держит реестр долга на потолке (записей ровно столько, сколько потолок), поэтому запаса под абзац БЕЗ названного свидетеля нет по построению — каждый переписанный абзац обязан назвать СУЩЕСТВУЮЩИЙ тест. Цвет гейта до задачи снимается прогоном и приводится в SUMMARY как вход.</precondition>
  <files>app/services/payment_service.py, .planning/phases/05-tarify/05-UI-SPEC.md</files>
  <read_first>
    - app/application/billing/subscription_period.py (докстринг `capped_carryover` целиком, строки 333-393; особенно абзац `359-372` — ИСТОЧНИК СЛОВ. Формулировки берутся оттуда дословно, а не сочиняются заново: отчёт раунда 7 закрыл спор двух объявлений в сторону этого)
    - .planning/phases/05-tarify/05-CONTEXT.md (свежая запись D-34 — КАНОНИЧЕСКОЕ прочтение. Все три абзаца и строка C2 её ЦИТИРУЮТ, а не формулируют пятый статус той же прохибиции)
    - app/services/payment_service.py (абзац, начинающийся «⚠️ ОТСЧЁТ ПРИ ПОВЫШЕНИИ ИДЁТ НЕ ОТ `expires_at`» — экземпляр (1), район строк 724-732. Соседние абзацы того же докстринга не правятся)
    - app/services/payment_service.py (абзац докстринга `_apply_extension` в районе строк 1000-1014 — экземпляр (2). ⚠️ В нём же лежит ВЕРНАЯ часть про `converted_remainder`, отношение цен и нижнюю границу в один день: она сохраняется дословно)
    - app/services/payment_service.py (абзац докстринга `_extend_subscription` в районе строк 685-698, содержащий утверждение о безусловном соблюдении прохибиции `05-01` — экземпляр (3). ⚠️ В нём же лежат ВЕРНЫЕ части правки плана `05-24`: «⚠️ ПЕРЕЧЕНЬ КОНЧАЕТСЯ ЗДЕСЬ», разбор стадии применения и свидетель `test_neither_declaration_claims_the_plan_only_grows_at_the_apply_stage`. Они сохраняются дословно)
    - app/services/payment_service.py (тело `_apply_extension`, вызов `capped_carryover(subscription.expires_at, now)` и окружающие комментарии — ФАКТ, к которому приводятся абзацы. Поведение не меняется ни на строку)
    - tests/test_application/test_declared_invariants.py (ТОЛЬКО ЧТЕНИЕ — правила отбора абзацев и требование называть свидетеля по имени. Знание правил экономит цикл; правке файл не подлежит)
    - tests/test_pages/test_billing_payment_errors.py и tests/test_application/test_subscription_period.py (ТОЛЬКО ЧТЕНИЕ — там живут свидетели `test_a_remainder_shorter_than_a_month_is_carried_whole`, `test_a_remainder_longer_than_a_month_is_capped_at_one_month`, `test_an_upgrade_from_free_does_not_carry_the_whole_horizon`, `test_an_unreadable_paid_plan_price_does_not_carry_the_whole_horizon`. Имена берутся отсюда, а не по памяти)
    - .planning/phases/05-tarify/05-UI-SPEC.md (строки 369-379 — раздел «Перенесено из фазы» и таблица целиком. ⚠️ ПРАВИТСЯ СТРОКА `C2` ЭТОЙ ТАБЛИЦЫ (`:377`, «Понижение плана при продлении»), а НЕ подраздел `### C2 — Метр оси` (`:214`): в документе два разных `C2`, и раунд 7 назвал отказавшим первый)
    - .planning/phases/05-tarify/05-VERIFICATION.md (первый элемент `gaps:` → `artifacts` и `missing`; и второй элемент → `missing`, пункт 2. Там названы дословно и три ложных предложения абзацев, и три снятых утверждения строки C2)
  </read_first>
  <action>
Правятся ТРИ абзаца одного файла и ОДНА строка таблицы другого. Каждая правка точечная;
соседние абзацы, верные части внутри правимых абзацев и всё тело функций сохраняются
дословно.

**Экземпляр (1) — абзац «⚠️ ОТСЧЁТ ПРИ ПОВЫШЕНИИ ИДЁТ НЕ ОТ `expires_at`»** (район
`:724-732`). Сегодня он утверждает три вещи, неверные с плана `05-22`: что срок там, где цену
прочитать нельзя, считается от `expires_at`; что там происходит возврат к поведению,
действовавшему ДО плана `05-22`; что обещание о сохранении остатка держится на обеих ветках
без оговорки. Новая редакция говорит:

- отсчёт при повышении идёт от КОНВЕРТИРОВАННОЙ точки — верно, сохраняется;
- от существующего `expires_at` срок считается только там, где план НЕ меняется (повтор
  своего тарифа);
- там, где цену любого из двух планов прочитать нельзя (Free, план, выпавший из конфига),
  база — `capped_carryover`, и поведения, действовавшего до плана `05-22`, там больше нет:
  оно и БЫЛО гэпом 1 раунда 6;
- обещание о сохранении остатка держится ТОЛЬКО В ОБЪЁМЕ ВЕРХНЕЙ ГРАНИЦЫ: остаток короче
  одного календарного месяца переносится целиком и не теряет ни дня; остаток длиннее месяца
  не переносится в части свыше месяца — 365 дней остатка дают 59 дней от 2026-01-10 и 61 день
  от сегодняшнего дня;
- ⚠️ ПРЕЖНЯЯ РЕДАКЦИЯ НАЗВАНА, а не стёрта: сказано, ЧТО она утверждала (возврат к прежнему
  порядку отсчёта и безоговорочное сохранение остатка), что это было верно ДО плана `05-22` и
  перестало быть верным С НЕГО. ⚠️ Названо СВОИМИ СЛОВАМИ: дословная цитата снятого
  утверждения оставила бы негативную проверку задачи красной навсегда и была бы неотличима от
  невыполненной работы;
- свидетели названы так, чтобы имя не отрицало утверждения: рядом с половиной «переносится
  целиком» — `test_a_remainder_shorter_than_a_month_is_carried_whole`, рядом с половиной
  «не переносится свыше месяца» — `test_a_remainder_longer_than_a_month_is_capped_at_one_month`.

**Экземпляр (2) — абзац докстринга `_apply_extension`** (район `:1007-1012`). Сохраняется
дословно всё про `converted_remainder`, отношение цен, единственное объявление правила и
нижнюю границу в один день. Заменяются два последних предложения — о безоговорочной верности
обещания и о возврате к прежнему порядку с записью в журнал. Новая редакция называет
`capped_carryover` по имени, говорит, что тело этой же функции его ЧИТАЕТ (не считает
выражением у себя), и повторяет объём обещания теми же словами, что экземпляр (1). Верное
остаётся: исключений здесь нет и быть не может, потому что 5xx на уведомлении запустил бы
цикл повторов (T-05-104). Прежняя редакция названа своими словами.

**Экземпляр (3) — абзац о прохибиции `05-01` в докстринге `_extend_subscription`** (район
`:685-698`). Сохраняются дословно все части правки плана `05-24`: что перечень `upgrade-only`
кончается на стадии намерения, что платёж с записанным разрешением исполняется как продан, и
оба названных свидетеля. Заменяется утверждение о безусловном соблюдении прохибиции. Новая
редакция:

- называет статус прохибиции ТЕМИ ЖЕ СЛОВАМИ, что `capped_carryover:363-366`: прохибиция
  имеет ОБЪЯВЛЕННОЕ ИСКЛЮЧЕНИЕ, а не соблюдена безусловно;
- ССЫЛАЕТСЯ на каноническое прочтение — решение `D-34` в
  `.planning/phases/05-tarify/05-CONTEXT.md` — и на место, где правило объявлено кодом
  (`app/application/billing/subscription_period.py`, `capped_carryover`), вместо того чтобы
  формулировать пятый статус;
- называет объём исключения числом с датой: остаток свыше одного календарного месяца в ветке
  нечитаемой цены не переносится, и предоплативший год теряет около одиннадцати месяцев;
- ⚠️ называет прежнюю редакцию своими словами: сказано, что она объявляла прохибицию
  соблюдённой безусловно, что соседний модуль о той же ветке говорил обратное, и что спор
  закрыт в сторону объявления, совпавшего с прогоном;
- называет свидетелем `test_a_remainder_longer_than_a_month_is_capped_at_one_month`, чьё имя
  утверждению не противоречит.

**Строка C2 таблицы «Перенесено из фазы» (`05-UI-SPEC.md:377`).** Её колонка «Чем
закрывается» утверждает ТРИ вещи, каждая из которых снята:

1. что повышение оплаченный остаток не сжигает — снято D-31: остаток свыше одного
   календарного месяца в ветке нечитаемой цены не переносится;
2. что прохибиция `05-01` соблюдена и не переопределена — снято D-34: прохибиция имеет ровно
   одно объявленное исключение;
3. что поведение `next_expiry` не менялось и что повышение отдаёт лимиты старшего тарифа на
   остаток младшего даром — снято D-30: остаток конвертируется ПО ОТНОШЕНИЮ ЦЕН (25 дней
   Basic по 1490 ₽/мес дают 7 дней Pro по 4900 ₽/мес), то есть безвозмездной выдачи больше
   нет, а точка отсчёта при повышении идёт от конвертированной точки.

Новая редакция строки обязана: назвать прежнюю редакцию формой «⚠️ ПРЕЖНЯЯ РЕДАКЦИЯ ЭТОЙ
СТРОКИ УТВЕРЖДАЛА …, И ЭТО СНЯТО РЕШЕНИЯМИ D-30, D-31 И D-34» — своими словами, без цитаты
снятых формулировок; сослаться на каноническое прочтение вместо пятой формулировки; назвать
цену числами с датой отсчёта (25 дней Basic → 7 дней Pro; 365 дней остатка → 59 дней от
2026-01-10 / 61 день от сегодняшнего дня); СОХРАНИТЬ ДОСЛОВНО верные части — четвёртое
состояние CTA карточки младшего плана, гард `POST /billing/subscribe` с кодом `downgrade`,
`PLAN_ORDER` и ссылки на файлы разметки; назвать встречный долг, записанный D-32 (карточка
обещает переход «после окончания оплаченного срока», а через окно срока давности он
происходит раньше).

Ни одна другая строка таблицы (`C1`, `C3`), ни подраздел `### C2 — Метр оси` и ни один другой
раздел документа не правятся.

⚠️ **Ни одна ИСПОЛНЯЕМАЯ строка не меняется.** Предмет задачи — объявления. Всякая правка,
меняющая исход прогона, есть выход за границы плана: поведение денежного пути принято
владельцем и записано D-31/D-34, а его изменение требует своего чекпойнта и своего плана.

⚠️ **Реестр долга гейта объявлений НЕ пополняется и потолок НЕ поднимается.** Запаса нет по
построению; каждый переписанный абзац обязан назвать существующий и зелёный тест. Если
переписанный абзац оказался бы без свидетеля — это признак того, что правка вышла за предмет
задачи, и её надо сузить, а не поднимать потолок.

⚠️ **Гейт объявлений НЕ правится и НЕ расширяется.** Если абзац честен, а гейт помечает его —
план ОСТАНАВЛИВАЕТСЯ и возвращает `## BLOCKED` с названным абзацем и причиной. Правка
средства запрещена этим планом прямо: она была бы новой поверхностью проверки, а объём сужен
решением владельца именно ради отказа от неё.

Коммит: `docs(05-33): три абзаца ветки нечитаемой цены и строка C2 приведены к D-34`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_declared_invariants.py -q &amp;&amp; ! grep -q 'откат к прежнему поведению' app/services/payment_service.py &amp;&amp; ! grep -q 'держится в обоих случаях' app/services/payment_service.py &amp;&amp; ! grep -q 'СОБЛЮДЕНА, а не переопределена' app/services/payment_service.py &amp;&amp; grep -q 'ИСКЛЮЧЕНИЕ из прохибиции' app/services/payment_service.py &amp;&amp; grep -q 'D-34' app/services/payment_service.py</automated>
  </verify>
  <acceptance_criteria>
    - Команда `<verify>` завершается кодом `0` целиком. ⚠️ Форма проверок выбрана по КОДУ ВОЗВРАТА: `! grep -q` даёт `0` при отсутствии подстроки, тогда как `grep -c … = 0` печатает ноль и завершается кодом `1`, то есть краснел бы на успешно исполненной задаче
    - `uv run pytest tests/ -q` завершается кодом `0`: ни одно поведение не изменено, ни один тест не переписан
    - Гейт объявлений зелен, и реестр долга НЕ вырос: `git diff --name-only -- tests/` пуст (в том числе `tests/test_application/declared_invariants_without_witness.txt` и сам `test_declared_invariants.py`)
    - Ни одна исполняемая строка не изменена: `git diff -U0 -- app/services/payment_service.py` не содержит изменённых строк вне докстрингов и комментариев. Дифф приводится в SUMMARY целиком вместе с `git diff --stat`; критерий судится человеком по приведённому диффу
    - Каждый из трёх переписанных абзацев называет прежнюю редакцию: `test "$(grep -c 'ПРЕЖНЯЯ РЕДАКЦИЯ' app/services/payment_service.py)" -ge 4` завершается кодом `0`. Базовое значение до задачи равно `1` — снято прогоном при планировании и перепроверяется исполнителем на входе, оба числа приводятся в SUMMARY
    - Статус прохибиции в `payment_service.py` совпадает со статусом в `subscription_period.py` и цитирует каноническое прочтение: обе позитивные проверки команды `<verify>` (`ИСКЛЮЧЕНИЕ из прохибиции`, `D-34`) зелены. Базовые значения до задачи — `0` и `0`, снятые прогоном при планировании
    - Числа названы с датой: `grep -q '2026-01-10' app/services/payment_service.py` завершается кодом `0`, и рядом с датой стоит число выдачи
    - Верные части не потеряны: `grep -q 'test_neither_declaration_claims_the_plan_only_grows_at_the_apply_stage' app/services/payment_service.py` завершается кодом `0`; счёт вхождений `converted_remainder` не уменьшился (оба значения в SUMMARY)
    - Строка C2 приведена: `grep -E '^\| C2 \| \*\*Понижение плана при продлении' .planning/phases/05-tarify/05-UI-SPEC.md | grep -q 'D-34'` и та же выборка с `grep -q 'ПРЕЖНЯЯ РЕДАКЦИЯ'` — обе завершаются кодом `0`
    - Строка C2 больше не несёт снятых утверждений: `! grep -E '^\| C2 \| \*\*Понижение плана при продлении' .planning/phases/05-tarify/05-UI-SPEC.md | grep -q 'не изменено ни на строку'` и та же форма с `'на остаток младшего бесплатно'` — обе завершаются кодом `0`
    - Строка C2 несёт числа `7` и `59` вместе с датой отсчёта `2026-01-10`; приводится в SUMMARY целиком
    - Число строк таблицы «Перенесено из фазы» не изменилось: `test "$(grep -c '^| C[0-9] |' .planning/phases/05-tarify/05-UI-SPEC.md)" -eq 3` завершается кодом `0` (значение `3` снято прогоном при планировании)
    - Подраздел `### C2 — Метр оси` не тронут: в `git diff -- .planning/phases/05-tarify/05-UI-SPEC.md` нет изменений в районе строк 214-224; дифф приводится в SUMMARY
    - Исполненные артефакты не тронуты: `git diff --name-only -- .planning/phases/05-tarify/05-2[0-6]-*.md` пуст
  </acceptance_criteria>
  <done>Три абзаца описывают поведение с плана `05-22`, статус прохибиции один на всё дерево и цитирует D-34, прежние редакции названы своими словами, верные части сохранены, строка C2 приведена к D-30/D-31/D-34; гейт объявлений и вся суита зелены, реестр долга не вырос, исполняемых строк и файлов `tests/` не тронуто</done>
</task>

<task type="auto">
  <name>Task 3: Константа величины уходит из D-30 и D-31, третье её место объявляется историческим, матрица возможностей сверяется после раунда 7</name>
  <reversibility rating="costly">Запись величины становится основанием следующих решений о деньгах. Ошибка здесь не роняет прогон, но переживает его: за расхождение записанной цены с исполняемым кодом фаза получила раунды 5, 6 и 7.</reversibility>
  <precondition>Чистая функция `capped_carryover(current, now)` существует в `app/application/billing/subscription_period.py` (заведена планом `05-22`) и импортируется без побочных эффектов вместе с `add_one_month`, `countdown_base` и `normalize_utc`. Проверяется до начала работы: `uv run python -c "from app.application.billing.subscription_period import capped_carryover, add_one_month, countdown_base, normalize_utc; print('ok')"` завершается кодом `0`. Без работающего импорта измерение снималось бы не с настоящего кода.</precondition>
  <files>.planning/phases/05-tarify/05-CONTEXT.md, .planning/phases/05-tarify/COVERAGE.md</files>
  <read_first>
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-30 целиком — правятся ДВА её фрагмента: величина выдачи ветки отката после правки и объявление о том, что величины абзаца сведены с измерением. Остальное сохраняется дословно, включая уже сведённые числа 396 и 140 и запись об ошибке журнального ключа)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-31 целиком — тот же фрагмент величины во второй записи. Правятся ОБЕ: правка одной завела бы расхождение внутри одного документа)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-30, абзац об `05-18-SUMMARY.md` — ОБРАЗЕЦ ДОСЛОВНЫЙ формы «исполненный SUMMARY есть исторический документ и не правится». Приём ПРОДОЛЖАЕТСЯ, а не изобретается заново)
    - .planning/phases/05-tarify/05-CONTEXT.md (решение D-33 — класс «константа величины, которую документ не может держать истинной», и причина, по которой на него нет машинного гейта. Правка есть исполнение D-33, а не его обход)
    - app/application/billing/subscription_period.py (`capped_carryover`, `add_one_month`, `countdown_base`, `normalize_utc` — функции, из которых снимается измерение)
    - .planning/phases/05-tarify/05-VERIFICATION.md (ТРЕТИЙ элемент `gaps:` — `reason` с распределением по 730 датам и оба пункта `missing:` дословно. Первый пункт называет ДВА допустимых исхода правки, и выбрать надо ОДИН, а не смешать)
    - .planning/phases/05-tarify/05-22-SUMMARY.md (ТОЛЬКО ЧТЕНИЕ, правке не подлежит — строка `134`. Нужен её дословный текст и номер строки для оговорки)
    - .planning/phases/05-tarify/COVERAGE.md (таблица решений в начале документа, строки 18-40 — двадцать одна строка возможностей ЮKassa. ⚠️ ЭТА ТАБЛИЦА НЕ ПЕРЕГЕНЕРИРУЕТСЯ И НЕ ПРАВИТСЯ)
    - .planning/phases/05-tarify/COVERAGE.md (разделы «## Сверка после закрытия гэпа 1 …», «## Сверка после закрытия гэпов раунда 5 …», «## Сверка после закрытия гэпов раунда 6 …» — ОБРАЗЕЦ ДОСЛОВНЫЙ: состав раздела, форма таблицы, как называются незатронутые строки)
    - .planning/phases/05-tarify/05-28-SUMMARY.md (что изменилось на денежном пути планом `05-28` — источник строки «затронуто» и её обоснования)
    - .planning/phases/05-tarify/05-VERIFICATION.md (`### Requirements Coverage` — состояние трёх требований по вердикту раунда 7; сверка обязана не спорить с ним)
  </read_first>
  <action>
**(1) Снять измерение собственным прогоном.** Над настоящим `capped_carryover` посчитать
выдачу ветки отката в днях для посева «живой горизонт 365 дней + платёж старшего плана» при
КАЖДОЙ дате отсчёта в окне не меньше двух лет (730 дат), и получить: (а) множество возможных
значений с их частотами; (б) значение на дате `2026-01-10`, которой пользуется сам абзац;
(в) значение на дате исполнения. Команда прогона и её ПОЛНЫЙ вывод приводятся в SUMMARY —
иначе число в записи снова окажется принятым, а не измеренным. Расхождение с ожиданием отчёта
раунда 7, если оно есть, называется СЛОВАМИ, а не выравнивается подгонкой.

**(2) Заменить константу в ОБЕИХ записях.** Фрагмент о выдаче после правки переписывается
ОДНИМ из двух допустимых исходов, названных отчётом, и выбранный исход называется в SUMMARY:

- либо инвариантом с диапазоном: «два календарных месяца — от N до M дней в зависимости от
  даты отсчёта», где N и M сняты прогоном, а рядом сказано, ПОЧЕМУ величина не константа
  (`add_one_month` считает месяцы, а не дни, и длина календарного месяца зависит от даты);
- либо числом с ЯВНО названной датой отсчёта — «X дней от 2026-01-10» — тем же приёмом, каким
  тот же абзац уже поступил с числами 396 и 140.

Смешивать исходы нельзя: смесь числа со скобкой-инвариантом и есть прежняя редакция, и ровно
она подвела. Прейскурант рядом с величиной сохраняется, но приводится к выбранной форме: цена
за месяц от даты не зависит, а число дней зависит, и запись не имеет права создавать
впечатление, будто меняются обе.

⚠️ **НАЗЫВАЯ ПРЕЖНЮЮ РЕДАКЦИЮ, НЕ ВОСПРОИЗВОДИТЬ ЕЁ ФОРМУ.** Негативная проверка задачи
требует, чтобы в области записей D-30 и D-31 не осталось НИ ОДНОГО сочетания прежнего числа с
единицей «дней». Фраза, процитировавшая снятую формулировку дословно, оставила бы проверку
красной навсегда и была бы неотличима от невыполненной работы. Поэтому прежняя редакция
называется СВОЙСТВОМ, а не воспроизведением: «прежняя редакция называла выдачу одним числом,
не назвав даты отсчёта; прогон показал, что это значение верно на десяти датах из семисот
тридцати и неверно на дате, названной в том же абзаце». Ссылка на находку — гэп 3 раунда 7.
Распределение частот целиком живёт в SUMMARY, а не в записи.

⚠️ Ни одно из уже сведённых чисел абзаца (396 — ветка отката до правки; 140 — ветка конверсии)
не трогается: они сведены планом `05-26` и подтверждены прогоном верификации раунда 7 до дня.

**(3) Объявление о сведении получает свой объём.** Фрагмент D-30, утверждающий, что величины
абзаца сведены с измерением, переписывается так, чтобы называть то, что в абзаце ФАКТИЧЕСКИ
сведено, и КЕМ: две величины сведены планом `05-26` и подтверждены прогоном верификации
раунда 7 до дня; третья — выдача ветки отката после правки — сведена этой задачей, прогоном, и
записана формой, а не константой. Прежняя формулировка называется как БЫВШАЯ У́ЖЕ АБЗАЦА:
она говорила о двух величинах там, где их три, и это и есть находка гэпа 3.

**(4) Оговорка об историческом статусе третьего места величины.** В запись D-30 добавляется
абзац, объявляющий: строка `134` файла `05-22-SUMMARY.md` несёт величину в прежней форме и НЕ
ПРАВИТСЯ — SUMMARY есть исторический документ, фиксирующий, что было измерено и записано в
момент исполнения плана. Названо: (а) что левое число там прежнее (`395`), а действующее —
`396`, сведённое планом `05-26`; (б) что правое число там прежнее, а действующая форма — та,
которую записал пункт (2); (в) ГДЕ живёт действующая запись, чтобы читатель SUMMARY не принял
старую величину за текущую; (г) что приём тот же, каким фаза уже поступила с
`05-18-SUMMARY.md`, — то есть прецедент продолжается, а не заводится. Форма оговорки берётся
из записи D-30 дословно.

**(5) Сверка `COVERAGE.md` после раунда 7.** Добавить В КОНЕЦ файла раздел «## Сверка после
закрытия гэпов раунда 7 (планы `05-28`, `05-31`, `05-33`, 2026-08-18)», построенный по
образцу трёх предыдущих сверок. Раздел обязан назвать ПОИМЁННО:

- **Затронутые строки матрицы.** Волна правила защиту чтения перечня тарифов (`_plan_price`,
  план `05-28`) на пути, который обслуживает уведомление `payment.succeeded`, и три
  объявления того же пути (задача 2 этого плана). Названо, изменилось ли РЕШЕНИЕ по строке
  (`INTEGRATE` / `OPT-OUT`); если не изменилось — сказано прямо, что изменилась РЕАЛИЗАЦИЯ, а
  не решение.
- **Незатронутые строки — прямо, а не молчанием.** Все `OPT-OUT` (возвраты, чеки,
  автоплатежи, выплаты, сделки, счета, СБП, `settings`, `payments.list`, `payments.capture`,
  `payments.cancel`, `payments.find_one`, `webhooks.*`, `personal_data.*`) остаются в прежнем
  решении и с прежней причиной; раунд 7 к ним не прикасался. Молчание о них читалось бы
  недосмотром.
- **Что раунд 7 к матрице НЕ добавил.** Ни одна находка раунда 7 не требует новой возможности
  API: `CR-01` есть чтение конфига, прочие — либо объявления, либо интерфейс, либо
  инфраструктура. Сказано прямо, чтобы отсутствие новых строк не читалось пропуском.
- **Сужение объёма названо, а не скрыто.** Одной фразой: планы `05-27`, `05-29`, `05-30` и
  `05-32` сняты до исполнения решением владельца (запись D-34), поэтому сверка описывает
  правки трёх исполненных планов, а не шести спланированных.
- **Признак повторной сверки.** Одной фразой: следующая сверка нужна при переводе любой строки
  `OPT-OUT` в `INTEGRATE` (возвраты, чеки, автопродление), а не по календарю.

⚠️ Ни одна существующая строка `COVERAGE.md` не удаляется и не переписывается: раздел
ДОБАВЛЯЕТСЯ. Отметка `Complete` требованиям этой задачей не возвращается — её возвращает
верификация; `REQUIREMENTS.md` в `files_modified` не входит.

⚠️ Ни одна прежняя запись `05-CONTEXT.md` не стирается: устаревшие ПЕРЕСКАЗЫВАЮТСЯ с названной
причиной.

⚠️ Ни одного нового теста и ни одной новой проверки задача не заводит. Негативная проверка
величины есть одноразовая команда критерия приёмки, а не гейт в `tests/`: решение D-33 уже
отказало машинному гейту на этом классе с названной причиной, и заведение его здесь было бы
отменой решения молча.

Коммит: `docs(05-33): величина ветки отката записана формой, третье её место объявлено историческим, COVERAGE сверен после раунда 7`.
  </action>
  <verify>
    <automated>! sed -n '/^- \*\*D-30:\*\*/,/^- \*\*D-32:\*\*/p' .planning/phases/05-tarify/05-CONTEXT.md | grep -q '60 дн' &amp;&amp; grep -q 'Сверка после закрытия гэпов раунда 7' .planning/phases/05-tarify/COVERAGE.md &amp;&amp; test -z "$(git diff --name-only -- .planning/phases/05-tarify/05-22-SUMMARY.md .planning/phases/05-tarify/05-18-SUMMARY.md)"</automated>
  </verify>
  <acceptance_criteria>
    - Команда `<verify>` завершается кодом `0` целиком. ⚠️ Негативная половина написана формой `! … | grep -q …`, дающей `0` при отсутствии совпадения; форма `grep -c … = 0` завершилась бы кодом `1` на успешно исполненной задаче, то есть проверка была бы ИНВЕРТИРОВАНА — прежняя редакция плана этой волны сделала ровно эту ошибку
    - Проверка величины ОБЛАСТНАЯ, а не файловая, и это намеренно: `sed`-выборка ограничена записями D-30 и D-31, потому что за их пределами то же двузначное число законно (номера строк, суммы, ссылки на `05-REVIEW.md`), и файловый запрет был бы неисполним
    - Обе записи правлены и несут форму, а не константу: `test "$(grep -c 'два календарных месяца' .planning/phases/05-tarify/05-CONTEXT.md)" -ge 2` завершается кодом `0`, и в каждом вхождении рядом стоит либо диапазон «от N до M дней», либо число с датой отсчёта — приводится в SUMMARY цитатой обоих фрагментов
    - Уже сведённые числа на месте: счёт вхождений `396` и `140 дней Pro` в `05-CONTEXT.md` не уменьшился (оба значения до и после приводятся в SUMMARY)
    - Измерение приведено: в SUMMARY есть команда прогона и её полный вывод — множество значений с частотами, значение на `2026-01-10` и значение на дате исполнения. Распределение частот в записи D-30/D-31 НЕ дублируется
    - Прежняя редакция названа свойством, а не воспроизведением: обе записи содержат фразу о том, что прежде выдача называлась одним числом без даты отсчёта, и почему это заменено
    - Объявление о сведении получило свой объём: `! grep -q 'ОБЕ ВЕЛИЧИНЫ ЭТОГО АБЗАЦА СВЕДЕНЫ С ИЗМЕРЕНИЕМ' .planning/phases/05-tarify/05-CONTEXT.md` завершается кодом `0`, а новая формулировка называет ТРИ величины и кем каждая сведена
    - Оговорка об историческом статусе существует и называет строку: `grep -q '05-22-SUMMARY.md' .planning/phases/05-tarify/05-CONTEXT.md` завершается кодом `0`, и вхождение сопровождается номером строки `134`
    - Оговорка ссылается на прецедент: `grep -q '05-18-SUMMARY.md' .planning/phases/05-tarify/05-CONTEXT.md` завершается кодом `0`
    - Исполненные SUMMARY не тронуты — третья половина команды `<verify>`
    - Раздел сверки существует в одном экземпляре: `test "$(grep -c 'Сверка после закрытия гэпов раунда 7' .planning/phases/05-tarify/COVERAGE.md)" -eq 1` завершается кодом `0`
    - Ни одна строка `COVERAGE.md` не удалена: `test "$(git diff -- .planning/phases/05-tarify/COVERAGE.md | grep -c '^-[^-]')" -eq 0` завершается кодом `0`
    - Раздел сверки называет незатронутые `OPT-OUT`-строки перечнем, утверждает, что раунд 7 не потребовал ни одной новой возможности API, называет сужение объёма со ссылкой на D-34 и признак повторной сверки. Проверяется чтением раздела и приводится в SUMMARY целиком
    - Раздел не спорит с `### Requirements Coverage` отчёта: BILL-05, BILL-06 и BILL-07 названы удовлетворёнными ПО ВЕРДИКТУ, а отметка `Complete` в `REQUIREMENTS.md` не возвращается — `git diff --name-only -- .planning/REQUIREMENTS.md` пуст
    - Каталог `tests/` и каталог `app/` этой задачей не тронуты: `git diff --name-only -- tests/ app/` пуст
  </acceptance_criteria>
  <done>Обе записи называют выдачу ветки отката формой, которую документ может держать истинной; числа сняты собственным прогоном и приведены целиком; объявление о сведении покрывает три величины; `05-22-SUMMARY.md:134` объявлен историческим с указанием действующей записи и прецедента; матрица сверена без перегенерации и с названным сужением объёма</done>
</task>

</tasks>

<threat_model>

Уровень — OWASP ASVS L1. Диспозиция `mitigate` обязательна для всякой угрозы тяжести `high`;
`accept` допускается только с названной причиной.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| объявление в коде → следующий читатель | Абзац, утверждающий не то, что делает код рядом с ним, приводит к правке кода «в сторону комментария» на денежном пути |
| записанное решение → все его потребители | Один предмет, описанный тремя источниками, даёт следующей правке право взять любой из трёх |
| `gaps[1].truth` → поле `must_have` блока `overrides:` | Блок, чей текст не совпал с закрываемым пунктом, ВЫГЛЯДИТ закрывающим и не связывается с ним |
| записанная величина → следующее решение о деньгах | Константа величины устаревает от даты чтения; решение принимается по неверному факту |
| исполненный артефакт → сегодняшнее состояние | Правка исторического документа стирает причину, по которой мера понадобилась |
| матрица возможностей → следующая интеграция | Перегенерация теряет прежние решения вместе с их причинами молча |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-240 | Repudiation | Текст `must_have` блока `overrides:` берётся из короткого пересказа `### Gaps Summary`, и закрытие не связывается с закрываемым пунктом | high | mitigate | Задача 1: единственный источник — `gaps[1].truth`; равенство проверяется программой в `<verify>`, а не глазами |
| T-05-241 | Repudiation | Блок `overrides:` остаётся с заполнителями `⟨…⟩` и записывает подпись, которой никто не ставил | high | mitigate | Задача 1: `assert '⟨' not in str(e)` в `<verify>`; `accepted_by` берётся из идентичности репозитория, `accepted_at` — момент исполнения |
| T-05-242 | Tampering | Три ложных абзаца закрываются `override`, то есть записью «мы принимаем, что они лгут», вместо исправления | high | mitigate | Задача 1 записывает границу `override` прямо: он закрывает расхождение ЗАПИСЕЙ и не распространяется на утверждения о поведении; задача 2 их ИСПРАВЛЯЕТ |
| T-05-243 | Tampering | Правка докстрингов задевает исполняемую строку денежного пути и меняет поведение, принятое владельцем | high | mitigate | Задача 2: `uv run pytest tests/ -q` зелен; `git diff -U0` по файлу приводится в SUMMARY целиком и судится человеком |
| T-05-244 | Repudiation | Переписанный абзац теряет имя свидетеля, реестр долга гейта растёт, потолок поднимается «чтобы прошло» | high | mitigate | Задача 2: `git diff --name-only -- tests/` пуст; каждый абзац называет существующий и зелёный тест; выход при конфликте — `## BLOCKED`, а не правка средства |
| T-05-245 | Repudiation | Прежняя редакция цитируется дословно, негативная проверка краснеет навсегда, и исполненная работа неотличима от невыполненной | high | mitigate | Задачи 2 и 3: прежняя редакция называется СВОЙСТВОМ, а не воспроизведением; форма явно предписана действием |
| T-05-246 | Repudiation | Константа величины остаётся в записях, по которым принимаются решения, будучи верной на 1,4 % дат | high | mitigate | Задача 3: константа заменена формой; областная негативная проверка ограничена записями D-30 и D-31 |
| T-05-247 | Repudiation | Новое число берётся из отчёта, а не из прогона, и повторяет тот же способ отказа на новом значении | high | mitigate | Задача 3: измерение снимается собственным прогоном; команда и полный вывод в SUMMARY; расхождение с отчётом называется словами |
| T-05-248 | Tampering | Исполненный PLAN или SUMMARY правится ради согласования текста, и теряется свидетельство о том, что было объявлено тогда | high | mitigate | Задачи 1-3: пустой дифф по `05-2[0-6]-*.md` и по двум SUMMARY; статус объявляется оговоркой, а не правкой |
| T-05-249 | Denial of Service | План закрывает щель гейта машинно и производит новую поверхность проверки, из которой родится гэп раунда 8 | high | mitigate | Прохибиция плана и запись D-34: щель принята ДОЛГОМ с названной причиной; `tests/` не входит в `files_modified`; критерии требуют пустого диффа по `tests/` в каждой задаче |
| T-05-250 | Repudiation | Строка C2 формулирует пятый статус той же прохибиции вместо цитаты канонического прочтения | medium | mitigate | Задача 2: позитивная проверка на `D-34` внутри строки C2; негативные — на два снятых утверждения |
| T-05-251 | Tampering | Правится не тот `C2`: в документе два раздела с этим идентификатором | medium | mitigate | Задача 2: выборка гвоздём `^\| C2 \| \*\*Понижение плана при продлении`; число строк таблицы неизменно; дифф района 214-224 пуст |
| T-05-252 | Tampering | Матрица возможностей перегенерируется, и прежние решения с причинами теряются | medium | mitigate | Задача 3: раздел добавляется; критерий требует нуля удалённых строк |
| T-05-253 | Repudiation | Молчание о незатронутых строках читается недосмотром, и следующая интеграция начинает не с полного покрытия | medium | mitigate | Задача 3: незатронутые названы прямо перечнем; названо и то, что раунд 7 не потребовал новой возможности |
| T-05-254 | Repudiation | Отметка `Complete` возвращается требованиям планом, а не верификацией | medium | mitigate | Задача 3: пустой дифф по `.planning/REQUIREMENTS.md`; файл не входит в `files_modified` |
| T-05-255 | Repudiation | Снятие четырёх спланированных планов остаётся дырой в нумерации без записанной причины | medium | mitigate | Задача 1: сужение объёма записано в D-34 с арифметикой; ROADMAP и STATE несут ту же причину (правятся планировщиком при написании этой волны) |
| T-05-256 | Information Disclosure | В записи о величинах или в докстринги попадают боевые идентификаторы магазина либо значения ключей ЮKassa | low | accept | Записи называют дни, цены прейскуранта и имена символов; значений секретов ни одна не содержит. Принимается тем же порядком, что для существующих записей документа |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов и не трогает манифесты: `pyproject.toml`, `uv.lock`, `package.json` в `files_modified` не входят, и `git diff --name-only` их не показывает. Устанавливаемых пакетов ноль, поэтому таблица легитимности пакетов `05-RESEARCH.md` этой волной не запрашивается |

</threat_model>

<verification>

1. `python3 -c "…"` задачи 1 печатает `overrides ok`: блок `overrides:` разбирается YAML-ом,
   все четыре ключа заполнены, заполнителей нет, `must_have` дословно равен `gaps[1].truth`.
2. `uv run pytest tests/ -q` завершается кодом `0` — тем же, что до плана: кода план не
   меняет.
3. `! grep -q 'откат к прежнему поведению' app/services/payment_service.py` завершается кодом
   `0`; то же для `'держится в обоих случаях'` и `'СОБЛЮДЕНА, а не переопределена'`.
   Форма важна: `grep -c` при нуле совпадений завершается кодом `1` и инвертировал бы
   проверку.
4. `! sed -n '/^- \*\*D-30:\*\*/,/^- \*\*D-32:\*\*/p' .planning/phases/05-tarify/05-CONTEXT.md | grep -q '60 дн'`
   завершается кодом `0`.
5. `git diff --name-only -- tests/` пуст: ни одного нового теста, ни одной правки гейта
   объявлений, ни одной записи в реестре долга.
6. `git diff --name-only -- .planning/phases/05-tarify/05-2[0-6]-*.md .planning/REQUIREMENTS.md`
   пуст: исполненные артефакты и реестр требований не тронуты.
7. `git diff --name-only` содержит ровно пять файлов: `app/services/payment_service.py`,
   `05-CONTEXT.md`, `05-VERIFICATION.md`, `05-UI-SPEC.md`, `COVERAGE.md`.
8. `git diff -- .planning/phases/05-tarify/05-VERIFICATION.md | grep -c '^-[^-]'` даёт `0` и
   `git diff -- .planning/phases/05-tarify/COVERAGE.md | grep -c '^-[^-]'` даёт `0`: в чужой
   вердикт и в матрицу только добавлено.

</verification>

<success_criteria>

- Статус прохибиции `05-01` формулируется ровно в одном месте, и все четыре потребителя его
  цитируют.
- Труth №11 закрыта машинно читаемой записью, чей текст связывается с закрываемым пунктом
  программной сверкой.
- Три абзаца `payment_service.py` и строка C2 `05-UI-SPEC.md` описывают то, что код делает с
  плана `05-22`; прежние редакции названы, а не стёрты.
- Ни одна запись фазы не называет выдачу ветки отката константой величины.
- Два пункта записаны принятым долгом с названными причинами — как записи, а не как работа.
- Матрица возможностей сверена без перегенерации, с названным сужением объёма.
- Ни одного нового машинного гейта, ни одной правки `tests/`, ни одной изменённой исполняемой
  строки `app/`.

</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-33-SUMMARY.md` when done.

В SUMMARY привести: (1) запись D-34 целиком и блок `overrides:` целиком, вместе с выводом
программной сверки `must_have` == `gaps[1].truth`; (2) три переписанных абзаца
`payment_service.py` целиком и `git diff -U0` по файлу — как доказательство, что исполняемых
строк не тронуто; (3) счёт вхождений `ПРЕЖНЯЯ РЕДАКЦИЯ`, `ИСКЛЮЧЕНИЕ из прохибиции` и `D-34`
в `payment_service.py` ДО и ПОСЛЕ; (4) строку C2 целиком до и после; (5) команду прогона
величины и её ПОЛНЫЙ вывод — множество значений с частотами, значение на `2026-01-10`,
значение на дате исполнения — и выбранный из двух допустимых исходов; (6) расхождение с
распределением отчёта раунда 7, если оно есть, названное словами; (7) счёт вхождений `396` и
`140 дней Pro` до и после; (8) раздел сверки `COVERAGE.md` целиком; (9) вывод
`git diff --name-only` — доказательство, что тронуто ровно пять файлов и ни одного в `tests/`.
</output>
