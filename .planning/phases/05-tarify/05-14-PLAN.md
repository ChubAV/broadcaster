---
phase: 05-tarify
plan: 14
type: execute
wave: 11
depends_on: ["05-13"]
gap_closure: true
files_modified:
  - .planning/phases/05-tarify/05-STATE-PATCH-R3.md
  - .planning/STATE.md
autonomous: false
requirements: [BILL-05, BILL-06, BILL-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- Гэп 2 (status: partial), 05-VERIFICATION.md ---
    - "Указатель на раунды ревью перестал зависеть от того, какой раунд занимает файл сегодня: КАЖДЫЙ раунд адресуется собственным неизменяемым коммитом (`4989f7a`, `1d29360`, `164d210`), включая новейший, и перечень всех раундов — в том числе будущих — отдаёт названная в записи команда `git log`, а не утверждение автора записи"
    - "Запись пережила бы четвёртую перезапись `05-REVIEW.md` без единой правки: ни одно её утверждение не перестанет быть верным от появления раунда 4 — сегодняшняя формулировка устаревала трижды подряд, и предмет гэпа именно в этом"
    - "Находки раунда 3 записаны в `.planning/STATE.md`: CR-01 и семь предупреждений получили распоряжение — сегодня их нет в файле вовсе, а CR-01 раунда 3 не зафиксирован НИГДЕ, кроме `05-REVIEW.md` и отчёта верификации"
    - "Каждый номер находки раунда 3 назван вместе со своим раундом: номера между раундами ПЕРЕНАЗНАЧЕНЫ, и `WR-07` раунда 3 (сокет Docker у веб-слоя) — другая находка, чем `WR-07` раунда 2 (негодный формат `PLAN_LIMITS`), уже записанный в файле"
    - "Требование квалификации проверяется ИНВАРИАНТОМ, а не константой счёта: число вхождений `WR-07` равно числу вхождений `WR-07 (раунд <любой номер>)`, подпёртое присутствием обеих записей поимённо. Константа величины устарела бы от первой перекрёстной ссылки и ещё раз от раунда 4 — то есть отказала бы ровно тем способом, который этот план чинит в указателе на раунды; проверка, чинящая мутабельное утверждение, сама на мутабельном утверждении стоять не может"
    - "Запись о закрытии CR-01 раунда 2 планом 05-11 больше не читается как полное закрытие: раунд 3 нашёл регрессию, ВНЕСЁННУЮ этим закрытием, и строка называет и половинность закрытия, и план, который его завершил"
    - "Блокер денежного пути раунда 3 — оплаченное и невыданное понижение на истёкшем сроке — записан со ссылкой на закрывший план и на доказавшие закрытие регрессии, а не словом «исправлено»"
    - "Находка `WR-07` раунда 3 (root-equivalent доступ к хосту у процесса, разбирающего JSON вебхука) получила ВЛАДЕЛЬЦА и распоряжение: ревью прямо назвало `.planning/STATE.md` местом записи и назвало её сегодняшний режим — «принято никем конкретно»"
    - "Решение владельца, принятое чекпойнтом плана 05-13, записано решением фазы в §Decisions: сегодняшняя запись о семантике `upgrade-only` описывает только случай действующей подписки и об истёкшем сроке молчит"
    - "Артефакт записан В ФАЙЛ, а не обещан во frontmatter: маршрут записи `.planning/STATE.md` из worktree закрыт конфигурацией, и три плана подряд об этот маршрут уже спотыкались"
    - "«Current Position» и frontmatter `progress` не спорят ни друг с другом, ни с ROADMAP после появления планов 05-13 и 05-14"
    # --- Backstop-пункт ---
    - statement: "Читатель `.planning/STATE.md`, пришедший в проект впервые, понимает из §Blockers/Concerns действительное состояние денежного пути за одну минуту и находит нужный раунд ревью с первой попытки — понятность записи судится человеком, а не грепом"
      verification: backstop
  artifacts:
    - ".planning/phases/05-tarify/05-STATE-PATCH-R3.md — готовая к применению замена секций STATE.md, коммитится вместе с планом и переживает снос worktree; отдельный файл, `05-STATE-PATCH.md` плана 05-12 не перезаписывается"
    - ".planning/STATE.md — реестр раундов ревью по коммитам, распоряжения раунда 3, две правки §Deferred Items, новая 🔴 запись о сокете Docker, запись решения об истёкшем сроке в §Decisions, согласованная «Current Position»"
  key_links:
    - "`git log -- .planning/phases/05-tarify/05-REVIEW.md` → реестр раундов → номер находки → нужный раунд ревью. Связь, которая рвалась трижды, потому что её держало утверждение автора, а не команда"
    - "`05-13-SUMMARY.md` (ответ чекпойнта и имена регрессий) → §Deferred Items и §Decisions"
    - "`05-REVIEW.md` раунда 3 (`git show 164d210:…`) → распоряжения по CR-01 и семи предупреждениям"
    - "`05-VERIFICATION.md` раунда 3, `gaps[1].missing` → два пункта, оба обязаны быть закрыты этим планом"
  prohibitions:
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT оставлять единственный операционный документ проекта утверждающим то, что перестаёт быть верным при следующей перезаписи документа, на который он ссылается — указатель, требующий ручной правки после каждого раунда, уже отказал трижды подряд"
      status: resolved
      verification: test
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT называть находку голым номером там, где нумерация переназначается между раундами — распоряжение уйдёт не той находке"
      status: resolved
      verification: test
    - requirement_id: BILL-06
      category: transparency
      statement: "MUST NOT достигать «закрытия» дефекта удалением записи о нём вместо закрытия самого дефекта"
      status: resolved
      verification: test
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT объявлять требование Complete раньше, чем верификация подтвердит закрытие гэпов — отметка, поставленная вперёд вердикта, уже была снята однажды и не ставится этим планом"
      status: resolved
      verification: test
---

<!-- planner-discipline-allow: на диске -->
<!-- planner-discipline-allow: WR-07 (раунд 2) -->
<!-- planner-discipline-allow: WR-07 (раунд 3) -->
<!-- Обе квалифицированные формы обязаны стоять в `<action>`: предмет работы — записать
     номер ИМЕННО в этом виде, и распоряжение без образца исполнить нельзя. Грепы по обеим
     формам ПОЛОЖИТЕЛЬНЫЕ (`>= 1`, проверка присутствия, а не отсутствия) и идут по
     `.planning/STATE.md` и по файлу патча, а НЕ по этому плану — самоаннулирования нет ни
     в одну сторону. Отрицательного гейта на эти литералы в плане нет вовсе: единственный
     отрицательный гейт плана — «на диске» == 0, и он о другой строке. -->

<objective>
Гэп 2 отчёта `05-VERIFICATION.md` раунда 3, `status: partial`. Тяжесть низкая — ложного
утверждения о безопасности здесь нет, — но предмет записи ровно в том, чтобы номер
находки указывал на нужный раунд, и **третья перезапись `05-REVIEW.md` сделала указатель
неверным в третий раз подряд**.

Первый пункт `missing:` — правка указателя — на момент планирования УЖЕ ПРИМЕНЁН
коммитом `c440090` («point the review-round entry at round 3»). Это не делает гэп
закрытым: правка воспроизводит ту же конструкцию, которая ломалась дважды до неё —
утверждение автора о том, какой раунд занимает файл. Раунд 4 сломает её в четвёртый раз,
и следующий раунд бухгалтерии будет чинить ту же строку тем же способом. Поэтому план
чинит не значение, а **форму**: каждый раунд адресуется собственным неизменяемым
коммитом, а вопрос «какой из них новейший» отвечает команда `git log`, а не предложение
в тексте.

Второй пункт `missing:` не сделан вовсе: находок раунда 3 в `.planning/STATE.md` нет ни
одной. **CR-01 раунда 3 — блокер денежного пути — не зафиксирован НИГДЕ, кроме
`05-REVIEW.md` и отчёта верификации.** Ровно та же ситуация, из-за которой существовал
гэп 2 раунда 2: дефект живёт в отчётах фазы и отсутствует в единственном операционном
документе проекта.

Отдельно раунд 3 принёс `WR-07` — веб-процесс, разбирающий JSON вебхука, держит
`/var/run/docker.sock`, то есть root-equivalent доступ к хосту. Ревью само назвало
`.planning/STATE.md` местом записи и назвало сегодняшний режим этой находки: «принято
никем конкретно».

Purpose: указатель, который надо править вручную после каждого раунда, — это не запись, а
обязательство. Обязательство отказало трижды. План заменяет его конструкцией, у которой
нечему устаревать.
Output: готовая к применению замена секций, коммитящаяся вместе с планом, и применённый
`.planning/STATE.md`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
</context>

<flagged_assumptions>

**Почему это не «ещё раз объявить STATE.md в `files_modified`».** Маршрут записи закрыт
конфигурацией, а не небрежностью: `workflow.use_worktrees` = `true`. Следствия выписаны в
самих рабочих процессах и подтверждены тремя прогонами — планы `05-08` и `05-10`
объявили файл и не тронули его, план `05-12` дошёл до цели только через отдельный артефакт
и блокирующий гейт:

- `execute-plan.md:406-410` — исполнитель определяет режим по `[ -f .git ]` и при worktree
  ПРОПУСКАЕТ шаг обновления STATE.md;
- `execute-plan.md:502-509` — при worktree коммит плана ИСКЛЮЧАЕТ `.planning/STATE.md`;
- `execute-phase.md:721` — в задание исполнителя вписано `IMPORTANT: Do NOT modify
  STATE.md or ROADMAP.md`;
- `execute-phase.md:5.7` — оркестратор коммитит `.planning/STATE.md`, но пишет туда только
  прогресс планов; произвольный текст §Blockers/Concerns из SUMMARY он не переносит ничем.

Устройство плана поэтому дословно повторяет план 05-12, который через этот маршрут
прошёл: твёрдый артефакт в каталоге фазы → применение по фактическому дереву
(best-effort) → блокирующий чекпойнт на выходе.

**Новый файл, а не правка `05-STATE-PATCH.md`.** Артефакт плана 05-12 закоммичен
(`c177c44`) и описывает уже применённые правки. Перезапись его превратила бы запись об
исполненной работе в запись о планируемой — тот самый класс подмены, который эта фаза
ловит третий раунд подряд. Патч этого плана — отдельный файл `05-STATE-PATCH-R3.md`.

**⚠️ ЛОВУШКА НУМЕРАЦИИ, ставшая ХУЖЕ с появлением раунда 3.** `05-REVIEW.md`
перезаписывался трижды: `4989f7a` (раунд 1), `1d29360` (раунд 2), `164d210` (раунд 3).
Номера между раундами переназначены, и теперь коллизии есть в ОБЕ стороны:

| Номер | Раунд 1 (`git show 4989f7a:…`) | Раунд 2 (`git show 1d29360:…`) | Раунд 3 (`git show 164d210:…`) |
|-------|-------------------------------|-------------------------------|-------------------------------|
| CR-01 | гард источника вебхука читает адрес пира | молчаливое понижение тарифа в `_apply_extension` | оплаченное понижение на истёкшем сроке не выдаётся, понижение недостижимо навсегда |
| CR-02 | двойное зачисление, check-then-act | синхронный вызов SDK без таймаута | — (раунд 3 несёт один critical) |
| WR-06 | необработанное уведомление отвечает 200 | `∞` в сайдбаре при нулевом балансе | план вне `PLAN_LIMITS` рисует четыре оси как «без ограничений» |
| **WR-07** | — | негодный ФОРМАТ `PLAN_LIMITS` роняет `json.loads` | **сокет Docker у веб-слоя, разбирающего JSON вебхука** |
| WR-10 | негодный ТИП лимита роняет `axis_percent` | — (раунд 2 знает только WR-01…WR-09) | — |

`WR-07` уже стоит в `.planning/STATE.md` — в паре с `WR-10` раунда 1, с распоряжением о
типизированной записи плана. Дописать распоряжение раунда 3 голым номером `WR-07`
значило бы приписать сокет Docker к находке о конфигурации тарифов. Отсюда правило
записи для нового блока: **каждый номер раунда 3 стоит в форме `<номер> (раунд 3)`**, и
критерий приёмки считает их по этой форме внутри своей записи.

**Существующая пара `WR-10` (раунд 1) / `WR-07` (раунд 2) не трогается и не
разъединяется.** План 05-12 записал её ОДНОЙ строкой с ОДНИМ распоряжением, и обоснование
там верное: одна причина (негодный `PLAN_LIMITS`), два места отказа, один ответ.

**Форма проверки квалификации — сверено по БАЙТАМ файла, а не по его пересказу.** На
входе `.planning/STATE.md:91` несёт последовательность `WR-10 (раунд 1) и WR-07 (раунд 2)`
БЕЗ обратных кавычек вокруг номеров: обратные кавычки, которыми номера набраны в тексте
ЭТОГО плана, — оформление плана, а не байты STATE.md. Фактические значения на входе:
`grep -Eo 'WR-07' .planning/STATE.md | wc -l` → **1**, `grep -Eo 'WR-07 \(раунд [0-9]+\)'`
→ **1**. Существующая запись уже удовлетворяет требованию квалификации, и правки не
требует НИ ОДНОЙ — правило неизменности перечня раундов 1 и 2 (задача 1, пункт 2) остаётся
в силе целиком, разделов патча остаётся СЕМЬ.

**Проверка ведётся РАВЕНСТВОМ, а не величиной — и это то же самое решение, ради которого
существует гэп 2.** После этого плана `WR-07` встретится в STATE.md БОЛЬШЕ двух раз:
номер раунда 3 несут и новая 🔴 запись (заголовок), и новая 🟡 запись (перечень находок,
пункт 8 её перечня), а распоряжение по `WR-06 (раунд 3)` ссылается на отложенное
распоряжение `WR-07 (раунд 2)` третьим вхождением. Любая константа величины («ровно 2»)
устареет от первой же перекрёстной ссылки и ещё раз — от раунда 4, то есть отказывает
ровно тем способом, который этот план чинит в указателе на раунды. Поэтому критерий
формулируется как ИНВАРИАНТ: **число вхождений `WR-07` равно числу вхождений
`WR-07 (раунд <N>)`**, где `<N>` — любое число (`[0-9]+`, а не перечень `[23]`: раунд 4
не должен требовать правки критерия). Инвариант «каждое вхождение квалифицировано»
остаётся верным при любом числе вхождений и при любом числе раундов.

**Равенство подпирается проверками присутствия, иначе его удовлетворяет пустота.** `0 = 0`
истинно, поэтому к равенству добавлены два отдельных условия: `WR-07 (раунд 2)`
присутствует (запись прежнего раунда не удалена) и `WR-07 (раунд 3)` присутствует (запись
нового раунда добавлена — на входе её нет, и это единственная из трёх проверок, которая на
входе КРАСНАЯ). Исполнителю ЗАПРЕЩЕНО «чинить» файл сокращением счёта удалением одной из
записей — это была бы подмена закрытия дефекта удалением его следа, и именно поэтому
проверок три, а не одна.

**Гейт по файлу патча ограничен строками записей.** В `.planning/STATE.md` голому номеру
взяться неоткуда, поэтому там равенство проверяется по ВСЕМУ файлу. В
`05-STATE-PATCH-R3.md` пояснительная проза обязана называть голый номер, объясняя само
правило, — поэтому там равенство считается только по строкам, начинающимся с `- 🔴` или
`- 🟡`, то есть по тексту, который и уедет в STATE.md. Разница между двумя гейтами
намеренная и проверена на образце: по всему файлу образец даёт 5 против 4, по строкам
записей — 4 против 4.

**Первый пункт `missing:` уже применён — и всё равно исполняется.** Коммит `c440090`
поправил значение указателя. План правит конструкцию: значение снова устареет, конструкция
— нет. В SUMMARY обязана быть строка о том, что пункт закрыт не повторной правкой
значения, а сменой формы, иначе следующая верификация прочитает работу как «уже сделано
до вас».

**Требования BILL-05/BILL-06/BILL-07 этим планом НЕ перемечаются в `Complete`.** Они
сняты с `Complete` 2026-08-16 после вердикта `gaps_found`, и однажды преждевременная
отметка уже была поставлена и отозвана (`2124d1e`). Отметку возвращает верификация после
подтверждения закрытия гэпов, а не план, который эти гэпы закрывает.
`.planning/REQUIREMENTS.md` в `files_modified` не объявлен и не открывается на запись.

**Что этот план НЕ делает по границе объёма.** Ни одной правки кода: гэп 2 —
организационный. Ни одной правки `app/`, `tests/`, `alembic/`, `pyproject.toml`. Находки
раунда 3, не входящие в `status: failed` гэп, в работу НЕ берутся — они получают ЗАПИСЬ с
распоряжением, ровно то, чего требует список `missing:`. Сокет Docker (`WR-07` раунда 3)
не переносится и не проксируется этим планом: ревью само называет это работой своего
размера и предлагает записью назначить владельца — план исполняет предложенное, а не
подменяет его.

**Ручные пункты `behavior_unverified_items` и `human_verification` задачами не
становятся** — см. `<verification>`. Мобильная пригодность на 375px остаётся открытой:
план не трогает ни строки разметки.

**Зонд краевых случаев вернул все три требования фазы `unclassified`** — все три несут
явные допущения планировщика в `05-13-PLAN.md` (E-01, E-02, E-03) и здесь не
дублируются: минтить вторые копии тех же трёх строк значило бы завести два ответа на один
вопрос. Их состояние — `unresolved`, и оно не изменилось.

**Каноничные позиции безопасности прохибициями не минтятся:** утечка секретов в
трекинговый документ покрыта `<threat_model>` (T-05-70) и `/gsd-secure-phase`.

</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Реестр раундов ревью, которому нечему устареть, и распоряжения раунда 3 — записанные в коммитящийся артефакт и применённые к настоящему дереву</name>
  <precondition>`05-13-SUMMARY.md` существует в каталоге фазы и содержит ответ чекпойнта одним из двух идентификаторов (`apply-after-expiry` либо `refuse-always`): и запись §Decisions о семантике истёкшего срока, и запись §Deferred Items о закрытии CR-01 раунда 3 обязаны описывать ИСПОЛНЕННУЮ работу с настоящими именами регрессий. При отсутствии — остановка: план волны 11 идёт после волны 10 именно затем.</precondition>
  <files>.planning/phases/05-tarify/05-STATE-PATCH-R3.md, .planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (весь файл, 125 строк: frontmatter `progress` — `total_plans: 64 / completed_plans: 64`; «Current Position» строки 29-36 — счётчик планов и строка прогресса; §Decisions строка 80 — запись о семантике `upgrade-only`, описывающая только случай ДЕЙСТВУЮЩЕЙ подписки; §Blockers/Concerns строки 86-93 целиком — 🔴 запись о блокирующем вызове SDK, 🟡 сводное распоряжение строки 91 и его ледовая часть о раундах; §Deferred Items строки 105-119 — таблица, где строка 119 объявляет CR-01 раунда 2 закрытым планом 05-11, и три строки Фазы 2 как образец формата)
    - .planning/phases/05-tarify/05-VERIFICATION.md (frontmatter: `gaps[1]` со списком `missing:` — два пункта; `regressions` — формулировка о блокере, внесённом планом 05-11; `behavior_unverified_items` и `human_verification` — восемь пунктов, которые обязаны остаться человеческими; таблица Requirements Coverage — что именно засчитано и с какими оговорками)
    - .planning/phases/05-tarify/05-REVIEW.md (раунд 3: строки 64-84 — раздел о нестабильности номеров с таблицей трёх раундов и их результатами; строки 116-224 — CR-01; заголовки `### WR-01`…`### WR-07` и `### IN-01`…`### IN-05` с первой строкой каждого — предмет распоряжения берётся оттуда, а не из этого плана; строки 569-588 — таблица «Previously Dispositioned», которая называет, какие находки уже несут распоряжения и повторной записи НЕ требуют; строки 445-480 — WR-07 целиком, включая прямое указание записать его в STATE.md с владельцем)
    - .planning/phases/05-tarify/05-13-SUMMARY.md (результат волны 10: ответ чекпойнта, имена новых функций и регрессий, факт красноты RED-теста до правки — запись о закрытии обязана называть их, а не слово «исправлено»)
    - .planning/phases/05-tarify/05-STATE-PATCH.md (артефакт плана 05-12: структура разделов и формат «что удаляется / что встаёт вместо» — новый патч пишется по тому же образцу, чтобы читатель не разбирал два формата)
    - .planning/ROADMAP.md (блок Фазы 5: формулировка строки `**Plans:**` и список планов — «Current Position» STATE.md обязана согласоваться с ним, а не спорить)
  </read_first>
  <action>
**0. Прочитать раздел о нестабильности номеров в ревью раунда 3** (`05-REVIEW.md:64-84`).
Там уже лежит таблица трёх раундов с коммитами и результатами — она и есть источник
реестра, который встанет в STATE.md. Переписывать её своими словами не нужно; нужно
перенести суть в формат записи.

**1. Написать `.planning/phases/05-tarify/05-STATE-PATCH-R3.md`** — готовую к применению
замену, а не пересказ. Файл каталога фазы коммитится планом в любом режиме исполнения и
переживает снос worktree; он и есть твёрдый результат задачи. Структура — по одному
разделу на каждую правку STATE.md, в каждом что удаляется (с номером строки на входе) и
что встаёт вместо этого, дословно, готовым к копированию. Разделов ровно СЕМЬ — пункты
2-8 ниже.

**Форма замещающего текста обязательна, потому что по ней считают гейты обоих концов.**
Каждый блок замены оформляется огороженным блоком кода, и каждая запись STATE.md внутри
него стоит ОДНОЙ физической строкой с тем же началом, с каким она уедет в файл: `- 🔴 `
либо `- 🟡 `. Мягких переносов внутри записи быть не должно. Причина прикладная: критерии
приёмки этой задачи считают номера находок ВНУТРИ строки записи, а проверки задачи 2
считают их точно так же в самом `.planning/STATE.md`. Если запись развалится на несколько
физических строк, гейт патча и гейт файла посчитают РАЗНОЕ, и сойтись они смогут только
случайно — то есть проверка перестанет проверять. Пояснительная проза патча живёт ВНЕ
блоков замены; голый номер она называть вправе, и гейты её не читают именно поэтому.

**2. §Blockers/Concerns, 🟡 сводная запись (строка 91) — заменить ЛЕДОВУЮ ЧАСТЬ записи
конструкцией, которой нечему устареть.** Правится только вводное предложение записи, до
слов «**Закрыты планами:**»; перечень распоряжений раундов 1 и 2 за ним не трогается ни
на символ.

Новая конструкция обязана нести три вещи и не нести четвёртой:
- **Реестр по коммитам.** Каждый раунд адресуется СВОИМ неизменяемым коммитом, включая
  новейший: раунд 1 — `git show 4989f7a:.planning/phases/05-tarify/05-REVIEW.md` (итог
  `critical: 2, warning: 10, info: 6`); раунд 2 — `git show 1d29360:…` (`critical: 2,
  warning: 9, info: 7`); раунд 3 — `git show 164d210:…` (`critical: 1, warning: 7,
  info: 5`). Коммит новейшего раунда называется наравне с остальными — именно этого не
  делали три предыдущие редакции, и именно поэтому каждая устаревала.
- **Самообновляющийся указатель.** Полный перечень раундов, включая ещё не написанные,
  отдаёт команда `git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md` —
  новейший сверху. Команда записывается дословно, чтобы читатель мог её выполнить, а не
  реконструировать.
- **Причина конструкции, одной фразой.** Запись НЕ утверждает, какой из раундов занимает
  файл сегодня, потому что ровно это утверждение устаревало трижды: имя `05-REVIEW.md`
  принадлежит трём разным документам, и четвёртый отберёт его снова.
- Чего запись НЕ содержит: ни одного утверждения о текущем содержимом файла
  `05-REVIEW.md`. Проверяется критерием приёмки.

**3. §Blockers/Concerns — НОВАЯ 🔴 запись: `WR-07 (раунд 3)`, сокет Docker у веб-слоя.**
Заголовок обязан нести раунд. Содержание: `docker-compose.prod.yml:91-92` монтирует
`/var/run/docker.sock` в контейнер `web`, то есть даёт root-equivalent доступ к хосту
процессу, который терминирует `POST /api/billing/webhook`; аутентификация этого маршрута —
IP из заголовка, проставляемого прокси, и она отключается одной переменной окружения
(`YOOKASSA_WEBHOOK_VERIFY_IP=false`, объявленный аварийный выход). Любая ошибка класса RCE
на этом пути становится компрометацией хоста, а не контейнера.

Запись обязана назвать три вещи, которые ревью назвало явно: находка **предшествует фазе
05** (монтирование обслуживает `app/services/wa_container_manager.py`), фаза **увеличила
объём того, что доходит до этого процесса**, и сегодняшний режим находки — **принято
никем конкретно**. Распоряжение: вынос управления контейнерами в отдельный воркер поверх
существующей очереди Redis либо фильтрующий прокси сокета; до того — принятый риск с
названным владельцем, по образцу записи `R-03-09` Фазы 3 (§Deferred Items, строка 116).
Кодом этот план ничего из перечисленного не делает.

**4. §Blockers/Concerns — НОВАЯ 🟡 запись: распоряжения по находкам раунда 3.** Одной
физической строкой, как соседняя сводная запись: бюджет файла 145 строк, и строка на
находку обменяла бы один дефект на другой. Заголовок записи —
`🟡 [Phase 5, код-ревью раунда 3 — распоряжения]`, и он обязан быть дословно таким:
критерий приёмки считает номера ВНУТРИ этой строки, и найти её он может только по
заголовку.

**Все ТРИНАДЦАТЬ находок раунда 3 обязаны присутствовать поимённо, каждая в форме
`<номер> (раунд 3)` с сутью и распоряжением:**
- `CR-01 (раунд 3)` — закрыт планом 05-13, отдельной строкой §Deferred Items (пункт 6);
- `WR-01 (раунд 3)` — отказ ПОВЫШЕНИЯ незнакомому плану сообщается словом «понижение»:
  `switch_is_refused` возвращает `True` с обеих сторон, а вызывающий отображает любой
  `True` в код причины `downgrade`, и пользователю говорят неправду. Распоряжение — за
  владельцем, работа своего размера: разделение кода причины требует различить два
  исхода отказа в самом правиле;
- `WR-02 (раунд 3)` — умолчание `yookassa_return_url` строит адрес возврата из
  отображаемого имени приложения;
- `WR-03 (раунд 3)` — константа `STATUS_PENDING` мертва, а путь записи пишет голый
  литерал: собственное правило модуля нарушено ровно в том месте, которое оно защищает;
- `WR-04 (раунд 3)` — `plan_card.html` зашивает `'free'`, хотя обработчик уже передаёт
  `free_plan_id`;
- `WR-05 (раунд 3)` — журнал операций молча обрезается на 20 строках на том же экране,
  где журнал платежей свой потолок объявляет;
- `WR-06 (раунд 3)` — план, выпавший из `PLAN_LIMITS`, рисует все четыре оси как «без
  ограничений», то есть безлимитный тариф, которого у пользователя нет. Та же семья, что
  отложенное распоряжение `WR-10` (раунд 1) / `WR-07` (раунд 2), и распоряжение у него то
  же — типизированная запись плана; сослаться на существующее распоряжение, НЕ заводя
  второго;
- `WR-07 (раунд 3)` — сокет Docker: отдельной 🔴 записью выше (пункт 3);
- `IN-01 (раунд 3)` — неиспользуемый импорт `text` и неиспользуемый `logger` в модуле,
  который выполняет каждое изменение баланса в продукте;
- `IN-02 (раунд 3)` — `app/application/billing/__init__.py` пуст, притом что каждый
  модуль пакета объявляет свои границы отдельно;
- `IN-03 (раунд 3)` — утверждение о границах в `subscription_period.py` верно для того,
  что модуль ИСПОЛЬЗУЕТ, и неверно для того, что он ВТЯГИВАЕТ; у `plan_switch.py` то же
  утверждение держится AST-тестом, у этого модуля равноценного теста нет;
- `IN-04 (раунд 3)` — `datetime.now(timezone.utc)` берётся трижды за один рендер, и два
  решения об одной подписке читают два разных момента;
- `IN-05 (раунд 3)` — фраза отказа существует в двух формулировках, а комментарий рядом
  утверждает, что соседство их удерживает от расхождения.

Записи вида «уже несёт распоряжение» брать из таблицы «Previously Dispositioned»
(`05-REVIEW.md:569-588`) и НЕ заводить им вторых распоряжений: ревью явно перечислило
там находки прежних раундов, повторно не открываемые.

**5. §Deferred Items — правка существующей строки о CR-01 раунда 2 (строка 119).**
Сегодня строка объявляет закрытие полным. Раунд 3 доказал, что закрытие было половинным
и внесло регрессию: объединено ОБЪЯВЛЕНИЕ правила, а не его ВХОДНЫЕ ДАННЫЕ. Строка
обязана нести оба факта — что план 05-11 закрыл названное направление (и это подтверждено
раундом 3, шесть из семи его must_haves зелёные), и что он открыл соседнее, закрытое
планом 05-13. Оговорку о вердикте, действующем до повторной верификации, СОХРАНИТЬ: она
оказалась пророческой и остаётся верной.

**6. §Deferred Items — НОВАЯ строка: `CR-01 (раунд 3)`.** Формат соседних записей.
Содержание — именами, а не словом «исправлено»: закрыт планом `05-13`; признак живости
оплаченного срока объявлен один раз (`subscription_is_live`,
`app/application/billing/subscription_period.py`) и стал ОБЯЗАТЕЛЬНЫМ keyword-only входом
правила `switch_is_refused`, поэтому спросить правило, не подав признак, невозможно;
признак снимается ДО сдвига срока, и порядок закреплён тестом по синтаксическому дереву;
регрессия стадии применения на истёкшем сроке существует и падала до правки. **Точные
имена функций и тестов сверить по `05-13-SUMMARY.md`, а не по этому плану** — плановые
имена могли измениться при исполнении. Обязательная оговорка: вердикт раунда 3 —
`gaps_found` — действует до повторной верификации; запись фиксирует исполненную работу, а
не пройденную проверку.

**7. §Decisions — дополнить запись о семантике смены тарифа (строка 80).** Сегодня она
описывает только случай ДЕЙСТВУЮЩЕЙ подписки и об истёкшем сроке молчит — из этого
молчания и вырос гэп. Дописать ответ чекпойнта плана 05-13 дословно, с идентификатором
варианта, и назвать следствие для пользователя одной фразой. Строка того же вида, что
соседние `Phase 3: …` записи; новой записи не заводить — у одного решения одно место.

**8. «Current Position» и frontmatter `progress`.** Счётчик планов, строка прогресса,
`stopped_at`, `last_activity`, `last_activity_desc` и `total_plans`/`completed_plans`
приводятся в соответствие с ROADMAP после появления планов 05-13 и 05-14: планов фазы
стало 14, планов milestone — 66. Frontmatter и текст не должны спорить друг с другом —
это уже случалось и было исправлено планом 05-12; проверить обе цифры, а не одну.

**9. Применить замену к настоящему дереву (best-effort, отказ не подделывается).**
Настоящий корень репозитория берётся из `git rev-parse --git-common-dir`: из worktree это
указывает на главный рабочий каталог, из главного — на него же. Применить правки к
`STATE.md` ИМЕННО там. Оставленный в главном дереве дифф — ровно то, что оркестратор
коммитит шагом 5.7 `execute-phase.md`; собственный коммит плана `.planning/STATE.md` в
режиме worktree всё равно исключает.

Если запись по этому пути недоступна — не изображать успех и не тратить попытки:
записать факт в SUMMARY отдельной строкой, назвать разрешённый путь и код отказа. Файл
`05-STATE-PATCH-R3.md` остаётся твёрдым результатом задачи, а применение снимает задача 2.

**10. Проверить бюджет файла.** После правок `wc -l .planning/STATE.md` обязан остаться в
пределах 145 строк (рабочий процесс держит потолок 150). Новых физических строк
добавляется четыре — две записи §Blockers/Concerns и одна строка §Deferred Items, плюс
запас. Если бюджет всё равно превышен — сокращаются формулировки распоряжений, а не
отбрасывается находка.

**11. Требования не перемечать.** `.planning/REQUIREMENTS.md` не открывается на запись
вовсе: `BILL-05`/`BILL-06`/`BILL-07` возвращает в `Complete` верификация после
подтверждения закрытия гэпов, и однажды преждевременная отметка уже была отозвана
(`2124d1e`).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-tarify/05-STATE-PATCH-R3.md && test "$(grep -Eo 'git show (4989f7a|1d29360|164d210):' .planning/phases/05-tarify/05-STATE-PATCH-R3.md | sort -u | wc -l)" = "3" && test "$(grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R3.md | grep -Eo 'WR-07' | wc -l)" = "$(grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R3.md | grep -Eo 'WR-07 \(раунд [0-9]+\)' | wc -l)" && test "$(grep '🟡 \[Phase 5, код-ревью раунда 3' .planning/phases/05-tarify/05-STATE-PATCH-R3.md | grep -Eo 'CR-01 \(раунд 3\)|WR-0[1-7] \(раунд 3\)|IN-0[1-5] \(раунд 3\)' | sort -u | wc -l)" = "13"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `.planning/phases/05-tarify/05-STATE-PATCH-R3.md` существует и закоммичен вместе с планом; `git diff --name-only -- .planning/phases/05-tarify/05-STATE-PATCH.md` пуст — артефакт плана 05-12 не перезаписан
    - Реестр адресует все три раунда неизменяемыми коммитами: `test "$(grep -Eo 'git show (4989f7a|1d29360|164d210):' .planning/phases/05-tarify/05-STATE-PATCH-R3.md | sort -u | wc -l)" = "3"` истинно — новейший раунд назван наравне с прежними. Считаются РАЗЛИЧНЫЕ коммиты (`sort -u`), а не вхождения: повтор одного коммита не должен зеленить проверку за счёт неназванного
    - Самообновляющийся указатель записан дословно и выполним: `grep -c 'git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md' .planning/phases/05-tarify/05-STATE-PATCH-R3.md` >= 1
    - Все тринадцать находок раунда 3 несут распоряжение: `grep '🟡 \[Phase 5, код-ревью раунда 3' .planning/phases/05-tarify/05-STATE-PATCH-R3.md | grep -Eo 'CR-01 \(раунд 3\)|WR-0[1-7] \(раунд 3\)|IN-0[1-5] \(раунд 3\)' | sort -u | wc -l` == 13. Счёт ведётся ВНУТРИ строки заголовка `код-ревью раунда 3`, а не по файлу: номера раунда 3 совпадают с номерами прежних раундов, и счёт по файлу считал бы чужие находки
    - Каждое вхождение `WR-07` в СТРОКАХ ЗАПИСЕЙ патча квалифицировано раундом: `test "$(grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R3.md | grep -Eo 'WR-07' | wc -l)" = "$(grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R3.md | grep -Eo 'WR-07 \(раунд [0-9]+\)' | wc -l)"` истинно. Проверяется РАВЕНСТВО, а не величина, и квалификатор принимает ЛЮБОЙ номер раунда (`[0-9]+`): константа величины устарела бы от первой перекрёстной ссылки и ещё раз от раунда 4 — тем же способом, каким устаревал указатель, который этот план чинит. Счёт ведётся по строкам записей (`- 🔴`/`- 🟡`), потому что пояснительная проза патча обязана называть голый номер, объясняя само правило
    - Ни одна из двух записей не принесена в жертву счёту: `grep -c 'WR-07 (раунд 2)' .planning/phases/05-tarify/05-STATE-PATCH-R3.md` >= 1 И `grep -c 'WR-07 (раунд 3)' .planning/phases/05-tarify/05-STATE-PATCH-R3.md` >= 1. Без этой пары равенство удовлетворялось бы пустотой (`0 = 0`); вторая из двух проверок — единственная в тройке, которая на входе КРАСНАЯ
    - Патч называет закрытие CR-01 раунда 3 именами: `grep -c 'subscription_is_live' .planning/phases/05-tarify/05-STATE-PATCH-R3.md` >= 1 и `grep -c '05-13' .planning/phases/05-tarify/05-STATE-PATCH-R3.md` >= 2
    - Патч называет ответ чекпойнта дословно: `grep -Ec 'apply-after-expiry|refuse-always' .planning/phases/05-tarify/05-STATE-PATCH-R3.md` >= 1
    - Патч называет семь правок STATE.md поимённо: 🟡 реестр раундов, 🔴 сокет Docker, 🟡 распоряжения раунда 3, правка строки о CR-01 раунда 2, новая строка §Deferred Items, §Decisions, «Current Position» + frontmatter `progress`
    - SUMMARY плана содержит отдельную строку о том, что первый пункт `missing:` закрыт СМЕНОЙ ФОРМЫ записи, а не повторной правкой значения (значение уже поправлено коммитом `c440090`)
    - SUMMARY плана содержит отдельную строку об исходе применения к настоящему дереву: применено либо отказано с названным путём и кодом
    - `git diff --name-only -- .planning/REQUIREMENTS.md` пуст — ни одно требование не перемечено
    - `git diff --name-only -- app/ tests/ alembic/ pyproject.toml uv.lock` пуст — план не трогает код
  </acceptance_criteria>
  <done>Реестр раундов ревью написан в форме, которой нечему устареть при четвёртой перезаписи `05-REVIEW.md`; все тринадцать находок раунда 3 получили распоряжение с квалифицированным номером; закрытие блокера названо именами; попытка применения к настоящему дереву выполнена, а её исход записан честно.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Применить замену к STATE.md в главном дереве и подтвердить проверками</name>
  <read_first>
    - .planning/phases/05-tarify/05-STATE-PATCH-R3.md (результат задачи 1 — семь разделов замены; порядок применения задан там)
    - .planning/STATE.md в ГЛАВНОМ рабочем каталоге (текущее состояние: применены ли правки задачей 1, или файл ещё на входном виде — 🟡 запись строки 91 без реестра по коммитам и без распоряжений раунда 3)
    - .planning/phases/05-tarify/05-14-SUMMARY.md, если уже написан (строка об исходе применения: «применено» либо отказ с названным путём и кодом)
  </read_first>
  <action>
Применить `.planning/phases/05-tarify/05-STATE-PATCH-R3.md` к `.planning/STATE.md` в
ГЛАВНОМ рабочем каталоге репозитория и подтвердить результат проверками ниже.

**Почему это человеческий шаг, а не задача исполнителя.** У исполнителя нет пути, эффект
которого гарантированно переживает снос worktree: `execute-plan.md:502-509` исключает
`.planning/STATE.md` из коммита плана в режиме worktree, а `execute-phase.md:721` прямо
запрещает исполнителю трогать этот файл. Это не предположение, а трижды воспроизведённый
исход: планы `05-08` и `05-10` объявили файл в `files_modified` и не изменили его ни
разу, а план `05-12` дошёл до цели только через этот же гейт. Остановка здесь громкая, а
не тихая — в этом весь её смысл.

**Если задача 1 уже применила правки** (её SUMMARY говорит «применено») — работы нет,
остаётся выполнить проверки и подтвердить.

**Чего делать НЕЛЬЗЯ ни при какой проверке.** Приводить счёт вхождений `WR-07` к прежнему
значению удалением записи. Записей под этим номером теперь ДВЕ намеренно:
`WR-07 (раунд 2)` — негодный формат `PLAN_LIMITS`, `WR-07 (раунд 3)` — сокет Docker у
веб-слоя. Это разные находки под одним номером, и требование к ним — квалификация раундом,
а не сокращение счёта.

**Число ВХОЖДЕНИЙ номера при этом не фиксировано и фиксироваться не должно.** Их больше
двух: заголовок 🔴 записи, перечень 🟡 записи и перекрёстная ссылка распоряжения
`WR-06 (раунд 3)` на отложенное распоряжение `WR-07 (раунд 2)`. Проверка требует
РАВЕНСТВА числа вхождений числу квалифицированных вхождений — свойства, которое остаётся
верным при любом их числе и при любом числе будущих раундов. Если проверка равенства не
сходится, ответом является дописать раунд к голому номеру, а НЕ удалить вхождение.
  </action>
  <instructions>
1. Убедиться, что находитесь в главном рабочем каталоге: `git rev-parse --git-dir` отдаёт
   `.git` (в worktree это был бы путь внутрь `.git/worktrees/...`).
2. Если правки ещё не применены — применить `05-STATE-PATCH-R3.md` к `.planning/STATE.md`
   разделами, в том порядке, в котором они выписаны в патче. Ни один раздел STATE.md, не
   названный патчем, не трогается; файл не перегенерируется целиком.
3. Выполнить проверки из `<verification>` — все ДЕСЯТЬ обязаны сойтись. Входные значения
   выписаны в каждой проверке, и семь из них на входе КРАСНЫЕ (реестр коммитов 2 → 3;
   `git log` 0 → `>= 1`; «на диске» 1 → 0; находки раунда 3 0 → 13; `WR-07 (раунд 3)`
   0 → `>= 1`; имена закрытия 0 → `>= 2`; строки `^- 🔴` 1 → `>= 2`) — краснотой на входе
   они и доказывают, что проверяют работу, а не её отсутствие. Три остальные (равенство
   квалификации, на входе `1 = 1`; бюджет файла, на входе 125 `<= 145`; пустой дифф
   требований) — защитные: они зелёные на входе и обязаны остаться зелёными, потому что
   ловят не невыполнение работы, а её выполнение НЕ ТЕМ способом. Проверка присутствия
   обеих записей `WR-07` смешанная: половина про раунд 2 зелена на входе (её дело —
   не дать удалить), половина про раунд 3 красна (её дело — потребовать записать).
4. Закоммитить `.planning/STATE.md` — либо шагом 5.7 `execute-phase.md`, либо вручную
   сообщением `docs(05): make the review-round ledger outlive the next rewrite`.
  </instructions>
  <verification>
- Реестр по коммитам: `grep -Eo 'git show (4989f7a|1d29360|164d210):' .planning/STATE.md | wc -l` → **3** (на входе 2 — коммит новейшего раунда назван не был)
- Самообновляющийся указатель: `grep -c 'git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md' .planning/STATE.md` → **>= 1** (на входе 0)
- Утверждения о текущем содержимом файла ревью не осталось: `grep -c 'на диске' .planning/STATE.md` → **0** (на входе 1). Именно это утверждение устаревало трижды; его отсутствие и есть предмет правки
- Распоряжения раунда 3 записаны все: `grep '🟡 \[Phase 5, код-ревью раунда 3' .planning/STATE.md | grep -Eo 'CR-01 \(раунд 3\)|WR-0[1-7] \(раунд 3\)|IN-0[1-5] \(раунд 3\)' | sort -u | wc -l` → **13** (на входе 0)
- Номер, за которым стоят две разные находки, квалифицирован ВЕЗДЕ:
  `test "$(grep -Eo 'WR-07' .planning/STATE.md | wc -l)" = "$(grep -Eo 'WR-07 \(раунд [0-9]+\)' .planning/STATE.md | wc -l)"` → **истинно**. Проверяется РАВЕНСТВО, а не величина, и квалификатор принимает любой номер раунда. Величины здесь нет намеренно: после этого плана вхождений станет больше двух (заголовок 🔴 записи, перечень 🟡 записи, перекрёстная ссылка распоряжения `WR-06 (раунд 3)`), а раунд 4 добавит ещё — константа отказала бы тем же способом, каким отказывал указатель, который этот план чинит. На входе равенство уже истинно (**1 = 1**), поэтому одна эта проверка ничего не доказывает и идёт в паре со следующей
- Обе записи на месте, счёт не сокращён удалением: `grep -c 'WR-07 (раунд 2)' .planning/STATE.md` → **>= 1** (на входе 1 — запись прежнего раунда не тронута) И `grep -c 'WR-07 (раунд 3)' .planning/STATE.md` → **>= 1** (на входе **0** — это и есть работа). Без этой пары равенство выше удовлетворялось бы пустотой: `0 = 0` истинно
- Закрытие блокера названо именами: `grep -Ec 'subscription_is_live|05-13' .planning/STATE.md` → **>= 2** (на входе 0)
- След не стёрт заодно: `grep -c '^- 🔴' .planning/STATE.md` → **>= 2** (на входе **1**). Считаются строки САМИХ записей, а не любое упоминание значка: голый `grep -c '🔴'` на входе уже даёт 2 (запись строки 90 плюс ссылка на неё в прозе строки 91), то есть зеленел бы ДО работы и не проверял бы ничего. Открытая запись о блокирующем вызове SDK (`CR-02` раунда 2) обязана уцелеть, и к ней добавляется запись о сокете Docker (`WR-07` раунда 3); счёт 🔴, гонимый к нулю, был бы подменой закрытия дефекта его удалением
- Бюджет файла: `wc -l .planning/STATE.md` → **<= 145**
- Требования не тронуты: `git diff --name-only -- .planning/REQUIREMENTS.md` → пусто
  </verification>
  <acceptance_criteria>
    - Все ДЕСЯТЬ проверок `<verification>` сошлись на `.planning/STATE.md` ГЛАВНОГО рабочего каталога, а не worktree
    - `.planning/STATE.md` закоммичен: `git log --oneline -1 -- .planning/STATE.md` называет коммит этой волны, а не `docs(phase-05): create phase plan`
    - `git diff --quiet .planning/STATE.md` — незакоммиченных остатков правки не осталось
    - Сигнал возобновления записан в SUMMARY плана дословно, вместе с фактическими значениями всех десяти проверок
    - При несошедшейся проверке план НЕ закрывается: записывается фактическое значение и причина, и волна остаётся незавершённой
  </acceptance_criteria>
  <resume-signal>Введите «применено», если все десять проверок сошлись, либо назовите проверку, которая не сошлась, и её фактическое значение</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree исполнителя → главное рабочее дерево | Правка `.planning/STATE.md`, сделанная в worktree, не коммитится и исчезает вместе с ним |
| `05-REVIEW.md` прежних раундов (только в git) → запись распоряжения | Номера находок между раундами переназначены; голый номер без раунда — ложное указание |
| `.planning/STATE.md` → читатель проекта | Единственный операционный документ; его утверждения о денежном пути принимаются на веру |
| SUMMARY плана → оркестратор | Текст «передан оркестратору» получателя не имеет: шаг 5.7 переносит только прогресс планов |
| `docker.sock` → контейнер `web`, терминирующий вебхук | Граница, которую находка `WR-07` раунда 3 называет несуществующей: root-equivalent доступ к хосту у процесса, разбирающего JSON из сети |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-69 | Repudiation | Операционный документ не содержит блокера денежного пути, найденного раундом 3: он существует только в `05-REVIEW.md` и отчёте верификации, а оба перезаписываются | high | mitigate | Задачи 1-2: `CR-01 (раунд 3)` получает строку §Deferred Items с именами регрессий и закрывшим планом; блокирующий чекпойнт не даёт пропустить запись четвёртый раз |
| T-05-70 | Information Disclosure | В трекинговый документ, лежащий в репозитории, попадают значения секретов вместе с описанием гарда вебхука и аварийного выключателя | medium | mitigate | Записи называют ИМЕНА переменных окружения и путь к значению, но не значения: `YOOKASSA_WEBHOOK_VERIFY_IP` — имя, `/var/run/docker.sock` — путь монтирования, не секрет. Ни ключа магазина, ни идентификатора, ни строки подключения запись не несёт |
| T-05-71 | Tampering | Распоряжение уходит НЕ ТОЙ находке: `WR-07` и `WR-06` означают в раундах 2 и 3 разные вещи, и одна из двух записей уже лежит в файле | high | mitigate | Каждый номер раунда 3 пишется в форме `<номер> (раунд 3)`; критерий приёмки требует РАВЕНСТВА числа вхождений `WR-07` числу вхождений `WR-07 (раунд <любой>)`, то есть отсутствия голого номера — при любом числе вхождений и при любом числе будущих раундов. Форма сверена по байтам входного файла: существующая запись уже квалифицирована и правки не требует. Реестр раундов по коммитам делает проверку выполнимой читателем |
| T-05-72 | Repudiation | «Закрытие» находки достигается удалением записи о ней, а не закрытием находки | medium | mitigate | Чекпойнт проверяет ПРИСУТСТВИЕ каждой записи поимённо (`WR-07 (раунд 2)` и `WR-07 (раунд 3)` — обе `>= 1`) и счёт строк `^- 🔴` `>= 2`, а не величину общего счёта: величина сокращаема удалением, присутствие — нет. Одного равенства для этого мало (`0 = 0` истинно), поэтому проверок три. Критерий, гонящий любой из этих счётов вниз, объявлен запрещённым прямо в тексте задачи |
| T-05-73 | Elevation of Privilege | Root-equivalent доступ к хосту у процесса, терминирующего вебхук с аутентификацией по заголовку прокси и с объявленным аварийным выключателем этой аутентификации (`WR-07` раунда 3) | high | transfer | Записью назначается ВЛАДЕЛЕЦ и распоряжение; кодом план не трогает ни `docker-compose.prod.yml`, ни `wa_container_manager`. Ревью само называет вынос управления контейнерами в отдельный воркер работой своего размера и предлагает запись как минимально честный исход. Сегодняшний режим — «принято никем конкретно» — этой записью прекращается |
| T-05-74 | Repudiation | Требование помечено `Complete` раньше вердикта верификации | high | mitigate | `.planning/REQUIREMENTS.md` не открывается на запись; критерий приёмки проверяет пустой дифф. Прецедент отзыва — коммит `2124d1e` |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает ни одного пакета и не трогает код вовсе — критерий приёмки проверяет пустой дифф по `app/`, `tests/`, `alembic/`, `pyproject.toml`, `uv.lock`. Аудит легитимности — `05-RESEARCH.md:144` `## Package Legitimacy Audit` |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-14`. Общий перечень фазы — в `05-10-PLAN.md`, перечень
кодовой работы второго гэп-раунда — в `05-13-PLAN.md`.

**Файлы (новые):**
- `.planning/phases/05-tarify/05-STATE-PATCH-R3.md` — готовая к применению замена секций
  `.planning/STATE.md`; коммитится планом и переживает снос worktree. Файл плана 05-12
  `05-STATE-PATCH.md` НЕ перезаписывается

**Файлы (изменённые):**
- `.planning/STATE.md` — §Blockers/Concerns (реестр раундов, две новые записи),
  §Deferred Items (одна правка, одна новая строка), §Decisions, «Current Position»,
  frontmatter `progress`

**Записи STATE.md (новые):**
- реестр раундов ревью, адресующий каждый раунд собственным коммитом (`4989f7a`,
  `1d29360`, `164d210`) плюс команда `git log --oneline` для перечня будущих раундов
- 🔴 запись `WR-07 (раунд 3)` — сокет Docker у веб-слоя, распоряжение и владелец
- 🟡 запись `[Phase 5, код-ревью раунда 3 — распоряжения]` — тринадцать находок раунда 3,
  каждая с квалифицированным номером
- строка §Deferred Items `CR-01 (раунд 3)` → закрыт планом 05-13
- дополнение записи §Decisions о семантике смены тарифа: исход на ИСТЁКШЕМ сроке по
  ответу чекпойнта плана 05-13

**Записи STATE.md (изменённые):**
- строка §Deferred Items о `CR-01` раунда 2: закрытие названо половинным, назван план,
  завершивший его
- вводная часть 🟡 сводной записи по раундам 1 и 2: заменена реестром по коммитам;
  перечень распоряжений раундов 1 и 2 за ней не тронут

**Функции, ключи журнала, миграции, маршруты, CSS, строки копирайта:** новых нет — план не
трогает код

<verification>
- `grep -Eo 'git show (4989f7a|1d29360|164d210):' .planning/STATE.md | wc -l` == 3 — каждый раунд адресуется неизменяемым коммитом, включая новейший
- `grep -c 'git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md' .planning/STATE.md` >= 1 — перечень раундов отдаёт команда, а не автор записи
- `grep -c 'на диске' .planning/STATE.md` == 0 — утверждение, устаревавшее трижды, из файла ушло
- `grep '🟡 \[Phase 5, код-ревью раунда 3' .planning/STATE.md | grep -Eo 'CR-01 \(раунд 3\)|WR-0[1-7] \(раунд 3\)|IN-0[1-5] \(раунд 3\)' | sort -u | wc -l` == 13 — все находки раунда 3 несут распоряжение
- Число вхождений `WR-07` РАВНО числу вхождений `WR-07 (раунд [0-9]+)` — инвариант, а не величина: константа счёта устарела бы от первой перекрёстной ссылки и ещё раз от раунда 4, то есть отказала бы способом, который этот план и чинит
- `grep -c 'WR-07 (раунд 2)'` >= 1 и `grep -c 'WR-07 (раунд 3)'` >= 1 — **сокращать счёт удалением записи запрещено**: за номером стоят две разные находки, и равенство выше без этой пары удовлетворялось бы пустотой
- `grep -c '^- 🔴' .planning/STATE.md` >= 2 (на входе 1) — **обнулять этот счёт запрещено**: открытые записи о блокирующем вызове SDK и о сокете Docker обязаны уцелеть. Считаются строки записей, а не упоминания значка: по всему файлу счёт на входе уже равен 2 и проверял бы ноль работы
- `grep -Ec 'subscription_is_live|05-13' .planning/STATE.md` >= 2 — закрытие блокера названо именами, а не словом «исправлено»
- `wc -l .planning/STATE.md` <= 145 — бюджет рабочего процесса соблюдён
- `git diff --name-only -- .planning/REQUIREMENTS.md` пуст — отметки требований возвращает верификация, а не этот план
- `git diff --name-only -- app/ tests/ alembic/ pyproject.toml uv.lock` пуст — план организационный
- `.planning/phases/05-tarify/05-STATE-PATCH-R3.md` закоммичен: даже при отказе применения текст не теряется

**Перенесённые человеческие проверки — задачами НЕ становятся и НЕ автоматизируются**
(восемь пунктов `human_verification` и один `behavior_unverified_items` из
`05-VERIFICATION.md`):
- **Мобильная ширина 375px** (критерий 4 ROADMAP, `behavior_unverified: 1`): браузерного и
  e2e-харнесса в проекте нет, разметка и стили не менялись с раунда 2 и этим раундом тоже
  не менялись. Пункт переносится без изменений и остаётся открытым
- Настоящий платёж в тестовом магазине ЮKassa (D-26); первое настоящее уведомление через
  боевой nginx после выката; статус «отклонён» у отменённого платежа (D-27); читаемость
  формулировок «в обработке» и отказа оплаты
- **Прохибиция `BILL-05`/fairness — `unresolved`/`judgment`:** оба денежных направления
  закрыты (действующий срок — планом 05-11, истёкший — планом 05-13), половина «слово
  пользователю ДО нажатия кнопки» судится человеком
- **Прохибиция `BILL-06`/transparency — `unresolved`/`judgment`:** после закрытия гэпа 1
  возвращается к ОДНОМУ пути достижения вместо двух; интерфейсной работы ни один план
  раунда не ведёт, пункт записан, а не закрыт
- **Прохибиция `BILL-05`/autonomy — `unresolved`/`judgment`:** достаточность сказанного
  пользователю о том, может ли он уйти вниз, оценивает владелец
</verification>

<success_criteria>
- Запись о раундах ревью переживёт четвёртую перезапись `05-REVIEW.md` без правки: каждый раунд адресован своим коммитом, перечень отдаёт команда
- Все тринадцать находок раунда 3 записаны с квалифицированным номером и несут распоряжение
- Блокер денежного пути раунда 3 зафиксирован в операционном документе именами, а не словом «исправлено», и запись о половинном закрытии предыдущего раунда исправлена
- Находка о root-equivalent доступе у веб-слоя получила владельца и перестала быть принятой никем конкретно
- Артефакт написан в файл, а не обещан во frontmatter, и его отсутствие остановило бы план громко
- Требования не перемечены раньше вердикта верификации
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-14-SUMMARY.md` when done
</output>
