---
phase: 05-tarify
plan: 30
type: tdd
wave: 23
depends_on: ["05-27", "05-28", "05-29"]
gap_closure: true
files_modified:
  - tests/test_application/test_declared_invariants.py
  - app/services/payment_service.py
autonomous: true
requirements: [BILL-05, BILL-06, BILL-07]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Щель гейта закрыта В ТОМ ОБЪЁМЕ, КОТОРЫЙ ОНА СЕГОДНЯ ЗАНИМАЕТ: абзац, называющий свидетеля, чьё ИМЯ содержит отрицание утверждаемого абзацем (`does_not_carry` при абзаце «остаток не сгорает»), краснит гейт. Полная сверка СМЫСЛА в объём средства не входит, объявлена не входящей и не вводится"
    - "Одна прохибиция получает один объявленный статус на всё дерево: машина утверждает, что множество статусов прохибиции `05-01`, объявленных проверяемыми модулями, состоит РОВНО ИЗ ОДНОГО элемента. До этого плана их было два — «СОБЛЮДЕНА» (`payment_service.py:693-695`) и «ИСКЛЮЧЕНИЕ … а не её соблюдение» (`subscription_period.py:363-366`)"
    - "Обе новые проверки ПАДАЛИ на дереве до правки докстрингов, и это доказано RED-коммитом, а не утверждением. Кроме живого дерева, они доказаны на ТРЁХ ЗАМОРОЖЕННЫХ текстах — дословных копиях трёх абзацев, названных раундом 7, — так что «средство поймало бы находки раунда 7» остаётся исполняемым утверждением и после того, как дерево починено"
    - "Три абзаца `payment_service.py` о ветке ОТКАТА (`:724-732`, `:1007-1012`, `:685-698`) описывают то, что код делает С ПЛАНА 05-22: база там `capped_carryover`, «прежнего поведения» там нет, а обещание «остаток не сгорает» держится ТОЛЬКО В ОБЪЁМЕ ГРАНИЦЫ в один календарный месяц"
    - "Спор двух объявлений одного денежного пути закрыт в сторону того, которое совпадает с ПРОГОНОМ: прохибиция `05-01` названа имеющей объявленное исключение — теми же словами, какими это уже сказано в `capped_carryover:359-372`, и со ссылкой на КАНОНИЧЕСКОЕ прочтение D-34, а не собственной формулировкой"
    - "Прежние редакции трёх абзацев НАЗВАНЫ, а не стёрты: каждый несёт фразу о том, что именно утверждала прежняя редакция и с какого плана это перестало быть верным"
    - "Верная часть каждого из трёх абзацев сохранена дословно: правки планов `05-11`, `05-13`, `05-18` и `05-24` (обе стадии читают одно объявление; перечень `upgrade-only` кончается на стадии намерения; подтверждённый платёж всегда покупает дни; конверсия остатка по отношению цен) не потеряны"
    - statement: "Человек, прочитавший три абзаца подряд после этой волны, называет судьбу оплаченного остатка на ветке отката одной фразой и не встречает ни одного предложения, которое пришлось бы сверять с кодом, чтобы понять, верно ли оно. Совпадение прочтения с поведением судится человеком: машина сверяет имена и статусы, но не смысл"
      verification: backstop
  artifacts:
    - "`tests/test_application/test_declared_invariants.py` — проверка «имя свидетеля отрицает утверждение абзаца», проверка «одна прохибиция — один статус», три замороженных текста находок раунда 7 и контроли к обеим проверкам"
    - "`app/services/payment_service.py` — три переписанных абзаца о ветке отката, каждый со ссылкой на каноническое прочтение и с названной прежней редакцией"
  key_links:
    - "D-34 (`05-CONTEXT.md`, план `05-27`) → три абзаца `payment_service.py`. Абзацы ЦИТИРУЮТ каноническое прочтение; вторая формулировка того же прочтения воспроизвела бы гэп, а не закрыла его"
    - "`capped_carryover:359-372` → те же три абзаца. Слова берутся оттуда, потому что там они уже сверены с прогоном; отчёт закрыл спор в сторону этого объявления"
    - "проверка «одна прохибиция — один статус» → все проверяемые модули. Она СВЯЗЫВАЕТ разнесённые объявления: до неё каждое было верным поодиночке и ложным в паре, а гейт судил их по одному"
    - "план `05-29` (расширенное множество модулей) → обе новые проверки. Проверка, видящая половину денежного пути, объявляла бы полноту, которой у неё нет"
  prohibitions:
    - requirement_id: BILL-05
      category: integrity
      statement: "MUST NOT оставлять в коде объявление, утверждающее то, чего код рядом с ним не делает: за этот класс фаза получила раунды 4, 5, 6 и 7, и каждый следующий читатель «чинит» код в сторону комментария"
      status: unresolved
      verification: test
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT закрывать расхождение двух объявлений одного пути правкой того из них, которое совпадает с прогоном: правым признаётся объявление, подтверждённое запуском, а не то, которое удобнее переписать"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-05
      category: integrity
      statement: "MUST NOT расширять объявленную границу средства до «сверки смысла»: обещание проверки, которой нет, есть тот же класс отказа, против которого средство построено"
      status: unresolved
      verification: judgment
---

<objective>
**Гэп 1 раунда 7, пункты 1, 2 и 4 `missing:`: щель гейта закрывается в занятом объёме, а три
абзаца о ветке отката перестают утверждать отменённое.**

Отчёт раунда 7 назвал способ отказа целиком, а не только его экземпляры:

> Планы 05-22 и 05-24 переписали докстринги `payment_service.py`; план 05-25 прошёл по тем же
> абзацам и приписал каждому имя свидетеля; и три абзаца о ветке отката пережили обе правки,
> продолжая утверждать «откат к прежнему поведению» и «остаток не сгорает» — про ветку,
> которую план 05-22 как раз перестал откатывать и в которой остаток как раз сгорает. Гейт
> над ними зелен, потому что абзац называет существующий тест, а тест называется
> `..._does_not_carry_the_whole_horizon` — то есть носит в имени отрицание того, что абзац
> утверждает.

Три экземпляра, все воспроизведены прогоном верификатора:

| Место | Что утверждает | Что делает код с плана `05-22` |
|---|---|---|
| `payment_service.py:724-732` | «срок ПО-ПРЕЖНЕМУ считается от `expires_at`», «там откат к ПРЕЖНЕМУ поведению», «обещание «остаток не сгорает» держится в ОБОИХ случаях» | база — `capped_carryover`; прежнего поведения нет; 365 дней остатка превращаются в 59 |
| `payment_service.py:1007-1012` | та же пара утверждений | тело этой же функции зовёт `capped_carryover` на 242 строки ниже (`:1254`) |
| `payment_service.py:685-698` | «прохибиция `05-01` … тем самым СОБЛЮДЕНА … ни один разрешённый переход остатка не сжигает» | `subscription_period.py:363-366` о ТОЙ ЖЕ ветке: «ИСКЛЮЧЕНИЕ из прохибиции плана `05-01` … а не её соблюдение» |

**Гейт над всеми тремя зелен, и он про это честен.** Его шапка говорит: «Абзац, называющий
существующий, но НЕ ТОТ тест, гейт пропустит зелёным». Отчёт не требует снять эту границу —
он требует закрыть щель РОВНО В ТОМ ОБЪЁМЕ, КОТОРЫЙ ОНА СЕГОДНЯ ЗАНИМАЕТ:

> свидетель, чьё ИМЯ содержит отрицание того, что утверждает абзац (`does_not_carry`,
> `does_not_burn` при абзаце «не сгорает»), — машинно различимый случай, и его хватило бы на
> все три экземпляра выше. Полная сверка смысла в объём средства не входит и входить не
> обязана.

Экземпляр `685-698` — другого подвида: он не спорит с ИМЕНЕМ своего свидетеля, он спорит с
СОСЕДНИМ ОБЪЯВЛЕНИЕМ о том же предмете. Этот подвид тоже машинно различим и тоже закрывается
здесь: **одна прохибиция обязана иметь один объявленный статус на всё дерево.**

Purpose: перевести последний открытый экземпляр класса из чтения глазами в прогон — и не
пообещать при этом больше, чем средство делает.
Output: две машинные проверки с доказанными зубами, три абзаца, описывающие сегодняшнее
поведение, и объявленная граница средства, оставшаяся честной.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@app/services/payment_service.py
@app/application/billing/subscription_period.py
@tests/test_application/test_declared_invariants.py
</context>

<flagged_assumptions>

**Зонд краевых случаев: три требования фазы остаются `unclassified` / `unresolved`.**
Классификатор зонда работает по английским формулировкам, а BILL-05, BILL-06 и BILL-07
записаны по-русски. Поднимаются явными допущениями и остаются `unresolved`:

- **E-01 (BILL-05, `unresolved`)** — краевые состояния продления. Допущение: этот план
  ПОВЕДЕНИЯ денежного пути не меняет ни на строку — он меняет только объявления о нём и
  машинную проверку этих объявлений. Всякая правка, меняющая исход прогона, есть выход за
  границы плана и подлежит остановке.
- **E-02 (BILL-06, `unresolved`)** — краевые состояния показа осей. Допущение: не задевается.
- **E-03 (BILL-07, `unresolved`)** — краевые состояния журнала платежей. Допущение: не
  задевается; журнальные ключи ветки отката этот план не трогает.

Равенство: **3 поднятых == 3 записанных допущения == 0 внесённых в `must_haves.truths`**.

**Допущение о номерах строк.** Диапазоны `:724-732`, `:1007-1012`, `:685-698` сняты
верификацией раунда 7 на дереве до волны 22. План `05-28` правит докстринг `_plan_price`
(`:905-936`) и потому СДВИГАЕТ номера ниже него. Исполнитель ищет абзацы по ТЕКСТУ (якорные
фразы названы в `<read_first>` дословно), а не по номеру строки, и приводит в SUMMARY
фактические номера после сдвига.
</flagged_assumptions>

<artifacts_this_phase_produces>

| Артефакт | Где | Что это |
|---|---|---|
| `ClaimDenial` | `tests/test_application/test_declared_invariants.py` | Замороженная тройка «что утверждает абзац / какое имя свидетеля это отрицает / какая оговорка делает абзац честным» |
| `CLAIM_DENIALS` | там же | Кортеж пар для денежного пути (перенос остатка) |
| `declarations_whose_witness_name_denies_the_claim()` | там же | Помощник: отобранные абзацы, назвавшие свидетеля с отрицанием в имени и не оговорившие вторую половину |
| `PROHIBITION_REFERENCE`, `PROHIBITION_STATUSES` | там же | Ссылка на прохибицию и закрытый словарь её объявляемых статусов |
| `declared_prohibition_statuses()` | там же | Помощник: множество статусов, объявленных дереву о прохибиции `05-01` |
| `ROUND_SEVEN_FINDINGS` | там же | Три ЗАМОРОЖЕННЫХ текста абзацев раунда 7 — исполняемое доказательство «средство поймало бы» |
| `test_no_declaration_names_a_witness_whose_name_denies_it` | там же | Проверка A |
| `test_one_prohibition_has_one_declared_status_across_the_tree` | там же | Проверка B |
| `test_the_helpers_catch_the_three_round_seven_findings` | там же | Контроль над замороженными текстами |
| `test_a_paragraph_naming_both_halves_is_not_flagged` | там же | Положительный контроль над живым `capped_carryover` |
| комментарий о постоянстве `decided_by` | `app/services/payment_service.py` | Закрытие `IN-02` раунда 7: поле, постоянное по построению, названо таковым рядом с соседом, который уже назван |

Новых символов в `app/` план не вводит: он правит только текст трёх докстрингов и не трогает
ни одной исполняемой строки.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — две машинные проверки и их зубы; граница средства объявлена честно</name>
  <reversibility rating="reversible">Добавление проверок и контролей в файл тестов; откат — удаление добавленного.</reversibility>
  <precondition>Планы `05-27`, `05-28` и `05-29` исполнены и слиты: каноническое прочтение D-34 записано, множество проверяемых модулей расширено до обеих стадий, реестр долга приведён. Проверка, поставленная над половиной денежного пути, объявила бы полноту, которой у неё нет.</precondition>
  <files>tests/test_application/test_declared_invariants.py</files>
  <read_first>
    - tests/test_application/test_declared_invariants.py (шапка модуля, строки 1-45 — объявленные ГРАНИЦЫ средства, особенно пункт «Он НЕ ЧИТАЕТ ТЕЛА ФУНКЦИИ и не сравнивает смысл абзаца с поведением». Этот пункт СУЖАЕТСЯ, а не отменяется, и новая редакция обязана сказать, что именно теперь проверяется машинно, а что по-прежнему нет)
    - tests/test_application/test_declared_invariants.py (строки 114-200 — `Declaration`, `paragraph_words`, `word_is_capital`, `has_capital_run`, `paragraph_is_selected`, `paragraph_fingerprint`. Отбор абзацев НЕ меняется: обе новые проверки работают над тем же отобранным множеством)
    - tests/test_application/test_declared_invariants.py (строки 324-350 — `declared_test_names()` и `named_tests()`. Имена свидетелей берутся отсюда; проверка A применяется ТОЛЬКО к именам, которые резолвятся в существующие тесты, иначе она дублировала бы существующий гейт)
    - tests/test_application/test_declared_invariants.py (строки 428-533 — `_synthetic`, `_SYNTHETIC_WITHOUT_TEST`, `_SYNTHETIC_UNKNOWN_TEST`, `_SYNTHETIC_EXISTING_TEST`, `_SYNTHETIC_UNMARKED` и контроли к ним, и `ROUND_SIX_FINDINGS` со своим контролем. ОБРАЗЕЦ ДОСЛОВНЫЙ: как замораживаются находки прошлого раунда и как доказываются зубы — на синтетических исходниках, а не правкой настоящих файлов)
    - app/services/payment_service.py (три абзаца, отыскиваемые ПО ТЕКСТУ: (а) абзац, начинающийся «⚠️ ОТСЧЁТ ПРИ ПОВЫШЕНИИ ИДЁТ НЕ ОТ `expires_at`»; (б) абзац докстринга `_apply_extension`, начинающийся «ЧЕТВЁРТЫМ действием» и содержащий «откат к прежнему поведению с записью в журнал»; (в) абзац докстринга `_extend_subscription`, содержащий «тем самым СОБЛЮДЕНА, а не переопределена». ⚠️ ИХ ТЕКСТЫ ЗАМОРАЖИВАЮТСЯ В ЭТОЙ ЗАДАЧЕ ДОСЛОВНО, ДО правки в задаче 2)
    - app/application/billing/subscription_period.py (абзац `capped_carryover:359-372` — ПОЛОЖИТЕЛЬНЫЙ КОНТРОЛЬ: он называет ОБЕ половины цены и обоих свидетелей `test_a_remainder_shorter_than_a_month_is_carried_whole` и `test_a_remainder_longer_than_a_month_is_capped_at_one_month`, поэтому проверка A обязана его ПРОПУСТИТЬ. Абзац, помечающий его, был бы проверкой с ложными срабатываниями на честном тексте)
    - .planning/phases/05-tarify/05-VERIFICATION.md (первый элемент `gaps:`, четвёртый пункт `missing:` — объём, в котором щель обязана быть закрыта, и прямое указание, что полная сверка смысла в объём не входит)
  </read_first>
  <behavior>
    - **Проверка A, отказ:** абзац, утверждающий «остаток не сгорает» без оговорки о границе и называющий существующий тест с `does_not_carry` в имени, попадает в вывод помощника
    - **Проверка A, пропуск (честный абзац):** абзац, называющий ОБЕ половины («короче месяца переносится целиком … длиннее месяца сгорает в части свыше месяца») и называющий обоих свидетелей, в вывод не попадает
    - **Проверка A, пропуск (нерезолвящееся имя):** абзац, называющий несуществующий тест с `does_not_carry` в имени, проверкой A не помечается — им занимается существующий гейт свидетеля
    - **Проверка A, зеркальная пара:** абзац, утверждающий сгорание и называющий свидетеля `..._is_carried_whole` без оговорки о короткой половине, тоже попадает в вывод
    - **Проверка B, отказ:** дерево, где один модуль объявляет прохибицию `05-01` СОБЛЮДЁННОЙ, а другой — ИСКЛЮЧЕНИЕМ, даёт множество из двух статусов, и проверка краснеет с перечислением обоих мест
    - **Проверка B, различение отрицания:** фраза «а не переопределена» НЕ читается как объявление статуса «переопределена»; статус берётся только из утвердительной формы
    - **Проверка B, различение предмета:** идентификатор угрозы `T-05-01` (встречается в `app/templates/`, `app/routes/billing.py`) НЕ читается как ссылка на прохибицию `05-01`
    - **Проверка B, разрыв строки:** синтетический абзац, где слово `Прохибиция` стоит в конце одной строки, а номер `05-01` — в начале следующей с отступом (форма живого `payment_service.py:692-693`), НАХОДИТСЯ проверкой: поиск идёт по склеенному тексту абзаца
    - **Проверка A, разрыв строки:** синтетический абзац, где утверждение «остаток не сгорает» разорвано переводом строки между словами, помечается так же, как неразорванный
    - **Замороженные находки:** каждый из трёх текстов раунда 7 помечается соответствующим помощником — первые два проверкой A, третий проверкой B
    - **Живое дерево до задачи 2:** обе проверки КРАСНЫЕ
  </behavior>
  <action>
**Заморозить находки раунда 7 ПЕРВЫМ действием.** До всякой правки снять из живого дерева
ДОСЛОВНЫЕ тексты трёх абзацев (якоря названы в `<read_first>`) и положить их модульной
константой `ROUND_SEVEN_FINDINGS` — тем же приёмом, каким заморожены `ROUND_SIX_FINDINGS`, но
как ТЕКСТЫ, а не как тройки-ключи: ключ по фрагменту перестанет находиться, как только
задача 2 перепишет абзац, а утверждение «средство поймало бы находки раунда 7» обязано
остаться исполняемым и после починки дерева.

**Проверка A — имя свидетеля отрицает утверждение абзаца.** Ввести замороженную структуру
`ClaimDenial` с тремя полями: `claim` (что абзац утверждает), `denial_in_name` (какое имя
свидетеля это отрицает) и `acknowledgement` (оговорка, при которой абзац честен, потому что
называет обе половины сам). Пар в `CLAIM_DENIALS` минимум ДВЕ, и обе о переносе остатка:

- *перенос против сгорания*: утверждение — «остаток не сгорает» / «не сжигает … остаток» /
  «переносится целиком» без оговорки; отрицание в имени — `does_not_carry`; оговорка —
  упоминание границы («сгорает», «зажат», «верхняя граница», «в объёме … месяц», «исключение
  из прохибиции»);
- *сгорание против переноса* (зеркальная): утверждение — «сгорает» / «зажат» / «верхняя
  граница»; отрицание в имени — `is_carried_whole` либо `does_not_burn`; оговорка —
  упоминание короткой половины («не сгорает ни на день», «переносится целиком»).

Помощник `declarations_whose_witness_name_denies_the_claim()` возвращает отобранные абзацы,
у которых нашлась пара: `claim` совпал, среди названных абзацем имён есть РЕЗОЛВЯЩЕЕСЯ в
существующий тест и совпавшее с `denial_in_name`, а `acknowledgement` НЕ совпал. Сообщение об
отказе называет модуль, владельца, имя пары и имя свидетеля — иначе отказ нечитаем.

**Проверка B — одна прохибиция, один статус.** Ввести `PROHIBITION_REFERENCE` — ссылку на
прохибицию по форме «слово `прохибиц…` рядом с номером `05-01`» (именно рядом, чтобы
идентификатор угрозы `T-05-01` не попал под правило), и `PROHIBITION_STATUSES` — ЗАКРЫТЫЙ
словарь статусов: `соблюдена`, `исключение`, `переопределена`.

⚠️ **«РЯДОМ» СЧИТАЕТСЯ ПО СКЛЕЕННОМУ ТЕКСТУ АБЗАЦА, А НЕ ПО СЫРЫМ СТРОКАМ ФАЙЛА, И ЭТО НЕ
ДЕТАЛЬ РЕАЛИЗАЦИИ.** В живом дереве объявление разорвано ровно между двумя половинами
искомой пары: `payment_service.py:692-693` несёт «…Прохибиция плана» в конце одной строки и
«`05-01` — «MUST NOT сжигать…» в начале следующей, с четырьмя пробелами отступа. Наивный
поиск по сырым строкам не найдёт ни одного места — и тогда обязательный красный прогон
задачи («в выводе названы оба статуса — `соблюдена` и `исключение` — с модулями») не
состоится, а проверка B окажется зелёной от собственной слепоты, что хуже её отсутствия.
Поэтому перед поиском текст абзаца СКЛЕИВАЕТСЯ: переводы строк и ведущие отступы схлопываются
в один пробел — тем же приёмом, каким модуль уже пользуется при отборе абзацев. То же
относится и к поиску `claim`/`acknowledgement` проверки A: живые абзацы переносятся по
ширине, и половина утверждения регулярно оказывается на следующей строке.

Помощник
`declared_prohibition_statuses()` собирает по отобранным абзацам всех проверяемых модулей
множество объявленных статусов; статус засчитывается только в утвердительной форме — фраза
«а не переопределена» статусом `переопределена` не является. Тест утверждает, что мощность
множества не превышает единицы, и в сообщении об отказе перечисляет модуль, владельца и
статус по каждому месту.

**Границу средства объявить честно — в шапке модуля, а не только в плане.** Существующий
пункт «Он НЕ ЧИТАЕТ ТЕЛА ФУНКЦИИ и не сравнивает смысл абзаца с поведением» СУЖАЕТСЯ, а не
снимается. Новая редакция говорит тремя фразами: (1) теперь машинно различаются ДВА случая —
имя названного свидетеля отрицает утверждение абзаца, и один предмет получает два разных
объявленных статуса; (2) всё остальное несовпадение свидетеля с предметом по-прежнему судится
человеком и это объявленная граница, а не дефект; (3) обе новые проверки работают ПО ИМЕНАМ
и ПО ЗАКРЫТОМУ СЛОВАРЮ, то есть отказ на переименовании теста возможен, и это цена, принятая
сознательно, — переименование свидетеля обязано быть замечено.

⚠️ Зубы доказываются, а не объявляются, и доказываются на СИНТЕТИЧЕСКИХ исходниках плюс на
замороженных текстах — настоящие файлы `app/` для этого не правятся. Отдельным контролем —
ПОЛОЖИТЕЛЬНЫМ — живой абзац `capped_carryover:359-372` не помечается ни одной из проверок:
он называет обе половины и обоих свидетелей.

RED-коммит делается ДО правки `app/`: `test(05-30): гейт различает свидетеля с отрицанием в имени и спор двух статусов одной прохибиции`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_declared_invariants.py -q --deselect tests/test_application/test_declared_invariants.py::test_no_declaration_names_a_witness_whose_name_denies_it --deselect tests/test_application/test_declared_invariants.py::test_one_prohibition_has_one_declared_status_across_the_tree &amp;&amp; ! uv run pytest tests/test_application/test_declared_invariants.py::test_no_declaration_names_a_witness_whose_name_denies_it tests/test_application/test_declared_invariants.py::test_one_prohibition_has_one_declared_status_across_the_tree -q</automated>
  </verify>
  <verify_rationale>
⚠️ **Команда состоит из двух половин, и вторая ИНВЕРТИРОВАНА НАМЕРЕННО.** Прежняя редакция
плана ставила здесь один прогон по фильтру
`-k "round_seven or both_halves or denies or one_declared_status"` — а этот фильтр захватывает
ОБЕ живые проверки (`…_denies_it`, `…_one_declared_status_…`), которые на этой задаче обязаны
быть КРАСНЫМИ: их краснота и есть её предмет, записанный первыми двумя критериями приёмки и
`<done>`. Успешно исполненная задача давала бы красный `<verify>`, а зелёный означал бы, что
проверки слепы. Поэтому: первая половина исключает оба живых узла и требует ЗЕЛЁНОГО от всего
остального модуля — существующих контролей, замороженных текстов раунда 7, положительного
контроля на живом `capped_carryover`, контролей на разрыв строки и на `T-05-01`; вторая
половина требует КРАСНОГО ровно от двух живых узлов. Зелёными их делает задача 2, и её
`<verify>` — прямой.
  </verify_rationale>
  <acceptance_criteria>
    - На дереве ДО задачи 2: `uv run pytest tests/test_application/test_declared_invariants.py::test_no_declaration_names_a_witness_whose_name_denies_it -q` завершается НЕНУЛЕВЫМ кодом, и в выводе названы ДВА места из `app/services/payment_service.py` с именами их владельцев
    - На дереве ДО задачи 2: `uv run pytest tests/test_application/test_declared_invariants.py::test_one_prohibition_has_one_declared_status_across_the_tree -q` завершается НЕНУЛЕВЫМ кодом, и в выводе названы оба статуса — `соблюдена` и `исключение` — с модулями
    - `uv run pytest tests/test_application/test_declared_invariants.py::test_the_helpers_catch_the_three_round_seven_findings -q` завершается кодом `0` — все три замороженных текста помечены
    - `uv run pytest tests/test_application/test_declared_invariants.py::test_a_paragraph_naming_both_halves_is_not_flagged -q` завершается кодом `0` — живой `capped_carryover` не помечен
    - Контроль на нерезолвящееся имя зелен: абзац, называющий несуществующий тест с `does_not_carry` в имени, проверкой A не помечается
    - Контроль на `T-05-01` зелен: идентификатор угрозы не читается ссылкой на прохибицию — доказывается синтетическим исходником, содержащим `T-05-01` и ни одного слова `прохибиц`
    - `grep -c 'ROUND_SEVEN_FINDINGS' tests/test_application/test_declared_invariants.py` даёт не менее `2`
    - Шапка модуля переписана: `sed -n '1,60p' tests/test_application/test_declared_invariants.py` содержит фразу о том, что машинно различаются два названных случая, и фразу о том, что остальное по-прежнему судится человеком
    - Отбор абзацев не тронут: `uv run pytest tests/test_application/test_declared_invariants.py -q -k "selection or checked_set or ledger"` завершается кодом `0`
    - Разрыв строки не ослепляет ни одну из двух проверок: контроли «Проверка A, разрыв строки» и «Проверка B, разрыв строки» зелены. ⚠️ Это НЕ декоративный контроль: в живом дереве объявление прохибиции разорвано именно так (`payment_service.py:692-693`), и без склейки красный прогон предыдущего критерия не состоялся бы вовсе
    - RED-коммит СВОЕЙ задачи существует: `git log --oneline -1` показывает `test(05-30)`, а `git diff --name-only HEAD~1 HEAD` содержит ровно один файл — `tests/test_application/test_declared_invariants.py`
    - ⚠️ Порядок RED→GREEN этой задачей НЕ проверяется: GREEN-коммита на этот момент не существует ПО ПОСТРОЕНИЮ — его делает задача 2, и критерий порядка стоит у неё
  </acceptance_criteria>
  <done>Обе проверки заведены, обе КРАСНЫЕ на живом дереве, зубы доказаны на синтетике и на трёх замороженных текстах, положительный контроль зелен, граница средства объявлена в шапке; RED-коммит сделан</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — три абзаца о ветке отката описывают то, что код делает с плана 05-22</name>
  <reversibility rating="costly">Правка объявлений денежного пути. Ошибка здесь не роняет прогон, но переживает его: именно из расхождения объявления с поведением фаза получила раунды 4, 5, 6 и 7, и следующий читатель «чинит» код в сторону комментария.</reversibility>
  <precondition>Задача 1 исполнена, RED-коммит существует, обе проверки красные. И решение D-34 записано планом `05-27`: без канонического прочтения абзацы получили бы четвёртую формулировку вместо ссылки на единственную.</precondition>
  <files>app/services/payment_service.py</files>
  <read_first>
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-34 — КАНОНИЧЕСКОЕ прочтение статуса прохибиции `05-01`. ⚠️ ЕСЛИ ВЛАДЕЛЕЦ ВЫБРАЛ ВЕТВЬ `records-amended`, ЭТА ЗАДАЧА НЕ ИСПОЛНЯЕТСЯ: план останавливается и возвращает `## BLOCKED`, потому что тогда меняется ПОВЕДЕНИЕ, а это работа своего плана со своим чекпойнтом)
    - app/application/billing/subscription_period.py (докстринг `capped_carryover` целиком, строки 333-393; особенно абзац `359-372` — ИСТОЧНИК СЛОВ. Формулировки берутся оттуда дословно, а не сочиняются заново: отчёт закрыл спор двух объявлений в сторону этого)
    - app/services/payment_service.py (абзац, начинающийся «⚠️ ОТСЧЁТ ПРИ ПОВЫШЕНИИ ИДЁТ НЕ ОТ `expires_at`» — экземпляр (1). Соседние абзацы того же докстринга не правятся)
    - app/services/payment_service.py (абзац докстринга `_apply_extension`, начинающийся «ЧЕТВЁРТЫМ действием» — экземпляр (2). ⚠️ В нём же лежит ВЕРНАЯ часть про `converted_remainder`, отношение цен и нижнюю границу в один день: она сохраняется дословно)
    - app/services/payment_service.py (абзац докстринга `_extend_subscription`, содержащий «тем самым СОБЛЮДЕНА, а не переопределена» — экземпляр (3). ⚠️ В нём же лежат ВЕРНЫЕ части правки плана `05-24`: «перечень кончается здесь», разбор стадии применения, имена свидетелей `test_neither_declaration_claims_the_plan_only_grows_at_the_apply_stage`. Они сохраняются дословно)
    - app/services/payment_service.py (тело `_apply_extension`, вызов `base = capped_carryover(subscription.expires_at, now)` и окружающие комментарии — ФАКТ, к которому приводятся абзацы. Поведение не меняется ни на строку)
    - tests/test_application/test_declared_invariants.py (после задачи 1 — состав `CLAIM_DENIALS` и словарь `PROHIBITION_STATUSES`: переписанный абзац обязан пройти обе проверки, и знание их правил экономит цикл)
  </read_first>
  <behavior>
    - `uv run pytest tests/test_application/test_declared_invariants.py -q` завершается кодом `0`: обе новые проверки зелены
    - `declared_prohibition_statuses()` над живым деревом возвращает множество из ОДНОГО элемента — `исключение`
    - `declarations_whose_witness_name_denies_the_claim()` над живым деревом возвращает пустой список
    - `uv run pytest tests/ -q` завершается кодом `0`: ни одно поведение не изменено, ни один тест не переписан
    - `git diff app/services/payment_service.py` не содержит ни одной изменённой ИСПОЛНЯЕМОЙ строки — только строки докстрингов и комментариев
  </behavior>
  <action>
Правятся ТРИ абзаца. Каждая правка точечная; соседние абзацы, верные части внутри правимых
абзацев и всё тело функций сохраняются дословно.

**Экземпляр (1) — абзац «⚠️ ОТСЧЁТ ПРИ ПОВЫШЕНИИ ИДЁТ НЕ ОТ `expires_at`».** Сегодня он
утверждает три вещи, ложные с плана `05-22`: срок «по-прежнему считается от `expires_at`» там,
где цену прочитать нельзя; там «откат к прежнему поведению»; обещание «остаток не сгорает»
держится «в обоих случаях». Новая редакция говорит:

- отсчёт при повышении идёт от КОНВЕРТИРОВАННОЙ точки — это остаётся верным и сохраняется;
- от существующего `expires_at` срок считается только там, где план НЕ меняется (повтор
  своего тарифа);
- там, где цену любого из двух планов прочитать нельзя (Free, план, выпавший из конфига),
  база — `capped_carryover`, и **прежнего поведения там нет: оно и было гэпом**;
- обещание «остаток не сгорает» держится **только в объёме границы**: остаток короче одного
  календарного месяца переносится целиком и не теряет ни дня; остаток длиннее месяца не
  переносится в части свыше месяца — 365 дней остатка дают 59 дней от 2026-01-10 и 61 день от
  сегодняшнего дня;
- ⚠️ прежняя редакция НАЗВАНА: сказано, что она утверждала откат к прежнему поведению и
  безусловное сохранение остатка, что это было верно ДО плана `05-22` и перестало быть верным
  С НЕГО;
- свидетели названы так, чтобы имя не отрицало утверждения: рядом с половиной «переносится
  целиком» стоит `test_a_remainder_shorter_than_a_month_is_carried_whole`, рядом с половиной
  «сгорает свыше месяца» — `test_an_upgrade_from_free_does_not_carry_the_whole_horizon` и
  `test_a_remainder_longer_than_a_month_is_capped_at_one_month`.

**Экземпляр (2) — абзац «ЧЕТВЁРТЫМ действием» в докстринге `_apply_extension`.** Сохраняется
дословно всё про `convert-remainder`, отношение цен, единственное объявление правила в
`converted_remainder` и нижнюю границу в один день. Заменяются два последних предложения —
про «остаток не сгорает остаётся верным» и про «откат к прежнему поведению с записью в
журнал»: новая редакция называет `capped_carryover` по имени, говорит, что тело этой же
функции его ЧИТАЕТ, и повторяет объём обещания («в объёме границы») теми же словами, что и
экземпляр (1). Верное остаётся: исключений здесь нет и быть не может, потому что 5xx на
уведомлении запустил бы цикл повторов (T-05-104).

**Экземпляр (3) — абзац о прохибиции `05-01` в докстринге `_extend_subscription`.**
Сохраняются дословно все части правки плана `05-24`: что перечень `upgrade-only` кончается на
стадии намерения, что платёж с записанным разрешением исполняется как продан, и оба названных
свидетеля. Заменяется утверждение «тем самым СОБЛЮДЕНА, а не переопределена: ни один
разрешённый переход остатка не сжигает». Новая редакция:

- называет статус прохибиции ТЕМИ ЖЕ СЛОВАМИ, что `capped_carryover:363-366` — прохибиция
  имеет ОБЪЯВЛЕННОЕ ИСКЛЮЧЕНИЕ, а не соблюдена безусловно;
- ССЫЛАЕТСЯ на каноническое прочтение (решение D-34, `.planning/phases/05-tarify/05-CONTEXT.md`)
  и на место, где правило объявлено кодом (`app/application/billing/subscription_period.py`,
  `capped_carryover`), вместо того чтобы формулировать четвёртый статус;
- называет объём исключения числом: остаток свыше одного календарного месяца в ветке
  нечитаемой цены не переносится, и предоплативший год теряет около одиннадцати месяцев;
- ⚠️ называет прежнюю редакцию: сказано, что она объявляла прохибицию соблюдённой безусловно,
  что соседний модуль о той же ветке говорил обратное, и что спор закрыт в сторону
  объявления, совпавшего с прогоном;
- называет свидетелем `test_a_remainder_longer_than_a_month_is_capped_at_one_month`, чьё имя
  утверждению не противоречит.

**Экземпляр (4) — `IN-02` раунда 7, тот же класс и тот же файл.** В испускании
`subscription_plan_downgraded` поле `decided_by` ПОСТОЯННО ПО ПОСТРОЕНИЮ: ветка достижима
только при `refused is False`, а в случае `decided_by == "rule"` предикат считан тем же
`switch_is_refused` с теми же аргументами, поэтому гард выше сработать не мог. Соседнее поле
`period_was_live` получило явную пометку «ПОЛЕ ПОСТОЯННО ПО ПОСТРОЕНИЮ И ОСТАВЛЕНО
НАМЕРЕННО»; `decided_by` не получило, и следующий читатель примет его за различающее.
Добавляется ОДНА фраза рядом с `decided_by=decided_by`: поле здесь постоянно по построению и
оставлено ради одинаковой формы с `subscription_plan_preserved`. Поле не удаляется, состав
журнальной записи не меняется — меняется только комментарий рядом.

⚠️ **Ни одна исполняемая строка не меняется.** Предмет плана — объявления. Всякая правка,
меняющая исход прогона, есть выход за границы плана: поведение денежного пути принято
владельцем и записано D-31/D-34, а его изменение требует своего чекпойнта.

⚠️ **Никакая правка не делается ради того, чтобы «пройти греп».** Если абзац честен, а
проверка помечает его — правится ПРОВЕРКА (пара `CLAIM_DENIALS` или словарь статусов), и
причина правки называется в SUMMARY. Подгонка текста под регулярное выражение вместо
приведения его к поведению воспроизвела бы гэп в новой форме.

⚠️ **ЕСЛИ ПОМЕЧЕННЫЙ АБЗАЦ ОКАЗАЛСЯ ВНЕ `app/services/payment_service.py` — ПЛАН
ОСТАНАВЛИВАЕТСЯ (`## BLOCKED`), А ПРОВЕРКА НЕ СМЯГЧАЕТСЯ.** После плана `05-29` обе новые
проверки работают над ПЯТЬЮ модулями (`app/pages/billing.py`, `app/routes/billing.py`,
`app/services/billing_service.py`, `app/pages/common.py` плюс `payment_service.py`), а
`files_modified` этого плана из `app/` держит ОДИН — и это ограничение намеренное: предмет
плана суть три названных раундом 7 абзаца, а не сплошная ревизия объявлений денежного пути.
Живое дерево проверено при планировании: заявлений о статусе прохибиции `05-01` вне
`payment_service.py` (`:692-695`) и `subscription_period.py:363` нет, поэтому для проверки B
случай маловероятен; для проверки A он не измерялся. Но если он случится, разрешённых
выходов ДВА, и «смягчить проверку» среди них нет: (1) абзац ЛОЖЕН — тогда его правка лежит
вне объявленного `files_modified`, и план обязан остановиться с `## BLOCKED`, назвав модуль,
владельца абзаца и причину, чтобы правка получила свой план; (2) абзац ЧЕСТЕН и помечен
ошибочно — тогда правится проверка, и причина называется в SUMMARY отдельным пунктом.
Расширение `files_modified` на ходу запрещено: файл, не объявленный планом, в его волне может
принадлежать другому плану.

GREEN-коммит: `docs(05-30): три абзаца ветки отката приведены к поведению с плана 05-22`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_declared_invariants.py tests/test_services/test_payment_service.py tests/test_pages/test_billing_payment_errors.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_application/test_declared_invariants.py -q` завершается кодом `0`: обе новые проверки зелены
    - `uv run pytest tests/ -q` завершается кодом `0`
    - Ни одна исполняемая строка не изменена: `git diff -U0 -- app/services/payment_service.py` не содержит ни одной изменённой строки вне докстрингов и комментариев — проверяется чтением диффа целиком и подтверждается в SUMMARY выводом `git diff --stat`
    - В `app/services/payment_service.py` не осталось утверждения об откате к прежнему поведению на ветке нечитаемой цены: `grep -c 'откат к прежнему поведению' app/services/payment_service.py` даёт `0`
    - Статус прохибиции в `payment_service.py` совпадает со статусом в `subscription_period.py`: `grep -c 'ИСКЛЮЧЕНИЕ из прохибиции' app/services/payment_service.py` даёт не менее `1`, а `grep -c 'СОБЛЮДЕНА, а не переопределена' app/services/payment_service.py` даёт `0`
    - Каждый из трёх переписанных абзацев содержит фразу о ПРЕЖНЕЙ редакции: `grep -c 'ПРЕЖНЯЯ РЕДАКЦИЯ' app/services/payment_service.py` вырос ровно на `3` относительно значения до задачи (оба значения приводятся в SUMMARY)
    - Экземпляр (3) ссылается на каноническое прочтение: `grep -c 'D-34' app/services/payment_service.py` даёт не менее `1`
    - Числа названы: `grep -c '59' app/services/payment_service.py` даёт не менее `1`, и число стоит рядом с датой отсчёта `2026-01-10`
    - Верные части не потеряны: `grep -c 'test_neither_declaration_claims_the_plan_only_grows_at_the_apply_stage' app/services/payment_service.py` даёт не менее `1`; `grep -c 'converted_remainder' app/services/payment_service.py` не уменьшился
    - `IN-02` раунда 7 закрыт: в диапазоне испускания `subscription_plan_downgraded` рядом с `decided_by=decided_by` стоит комментарий со словами «постоянно по построению»; `grep -c 'decided_by=decided_by' app/services/payment_service.py` не изменился (поле не удалено)
    - Реестр долга гейта не вырос: `git diff --name-only -- tests/test_application/declared_invariants_without_witness.txt` пуст
    - Ни один файл `app/` вне `app/services/payment_service.py` не тронут: `git diff --name-only -- app/ | grep -vc 'app/services/payment_service.py'` даёт `0`. ⚠️ Если какая-либо из двух проверок пометила абзац ВНЕ `payment_service.py`, план остановлен с `## BLOCKED` и модуль назван — смягчение проверки ради зелёного прогона критерия НЕ удовлетворяет, потому что средство строилось против ровно того, что оно бы при этом пропустило
    - GREEN-коммит следует за RED-коммитом задачи 1: `git log --oneline -3` показывает `test(05-30)` раньше `docs(05-30)`
  </acceptance_criteria>
  <done>Три абзаца описывают поведение с плана `05-22`, статус прохибиции один на всё дерево и совпадает с каноническим прочтением, прежние редакции названы, верные части сохранены, обе новые проверки и вся суита зелены, исполняемых строк не тронуто</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| объявление в коде → следующий читатель | Объявление, расходящееся с поведением, заставляет «чинить» код в сторону комментария; правка выглядит приведением кода в соответствие |
| два объявления одного пути → одно решение | Пока статусов два, следующая правка возьмёт любой; выбор делается не по истине, а по тому, какой абзац ближе |
| имя свидетеля → утверждение абзаца | Имя, содержащее отрицание утверждения, есть машинно различимая ложь; всё остальное несовпадение — граница средства |
| каноническое прочтение (D-34) → докстринг | Вторая формулировка одного прочтения заводит второй источник истины — тот же дефект в новой форме |
| проверка по регулярному выражению → текст объявления | Подгонка текста под греп даёт зелёный гейт при неверном объявлении |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-202 | Repudiation | Три абзаца о ветке отката утверждают отменённое поведение; следующий читатель восстанавливает гэп правкой, выглядящей приведением кода к комментарию | critical | mitigate | Задача 2: все три приведены к поведению словами `capped_carryover:359-372`; задача 1: проверка A краснеет на таком абзаце и доказана на трёх замороженных текстах |
| T-05-203 | Repudiation | Два объявления одного денежного пути называют разные статусы одной прохибиции, и следующая правка берёт любой | high | mitigate | Задача 1: проверка B утверждает мощность множества статусов; задача 2: статус берётся из канонического прочтения D-34 |
| T-05-204 | Tampering | Правка докстрингов заодно меняет поведение денежного пути, принятое владельцем | critical | mitigate | Задача 2: критерий требует нуля изменённых исполняемых строк; полная суита обязана быть зелёной без переписывания ни одного теста |
| T-05-205 | Repudiation | Верная часть абзаца теряется вместе с ложной, и с ней теряются правки планов `05-11`, `05-13`, `05-18`, `05-24` | high | mitigate | Задача 2: критерии считают сохранённые имена свидетелей и упоминания `converted_remainder`; `<read_first>` называет верные части поимённо |
| T-05-206 | Spoofing | Текст подгоняется под регулярное выражение проверки вместо приведения к поведению: гейт зелен, объявление ложно | high | mitigate | Задача 2 прямо запрещает подгонку и требует правки ПРОВЕРКИ с названной в SUMMARY причиной, если помечен честный абзац; положительный контроль на живом `capped_carryover` держит проверку от ложных срабатываний |
| T-05-207 | Repudiation | Средство обещает больше, чем делает: «сверка смысла» объявляется закрытой | high | mitigate | Задача 1: шапка модуля СУЖАЕТ, а не снимает объявленную границу, и называет обе новые проверки поимённо; критерий требует наличия обеих фраз |
| T-05-208 | Denial of Service | Проверка по именам краснеет на переименовании теста и делает переименование дорогим | low | accept | Цена принята сознательно и объявлена в шапке: переименование свидетеля обязано быть замечено, потому что имя свидетеля есть часть утверждения абзаца |
| T-05-209 | Spoofing | Идентификатор угрозы `T-05-01` читается ссылкой на прохибицию `05-01`, и проверка B краснеет на шаблонах | medium | mitigate | Задача 1: ссылка требует слова `прохибиц…` рядом с номером; контроль на синтетическом исходнике с `T-05-01` доказывает различение |
| T-05-210 | Information Disclosure | Замороженные тексты находок выносят в файл тестов фрагмент, содержащий секрет | low | accept | Замораживаются докстринги денежного пути; значений секретов ни один не содержит. Принимается тем же порядком, что для `ROUND_SIX_FINDINGS` |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов: `git diff --name-only` не содержит `pyproject.toml` / `uv.lock`. Устанавливаемых пакетов ноль |
</threat_model>

<verification>

1. `uv run pytest tests/ -q` завершается кодом `0`.
2. `uv run pytest tests/test_application/test_declared_invariants.py -q` зелен; обе новые проверки в собранном множестве.
3. `git log --oneline -3` показывает RED-коммит `test(05-30)` раньше GREEN-коммита `docs(05-30)`.
4. `grep -c 'откат к прежнему поведению' app/services/payment_service.py` даёт `0`; `grep -c 'СОБЛЮДЕНА, а не переопределена' app/services/payment_service.py` даёт `0`.
5. `git diff --stat` показывает ровно два файла; в `app/services/payment_service.py` изменены только строки докстрингов.
6. `git diff --name-only -- tests/test_application/declared_invariants_without_witness.txt` пуст.

</verification>

<success_criteria>

- Абзац, называющий свидетеля с отрицанием в имени, краснит гейт — доказано на живом дереве и на трёх замороженных текстах раунда 7.
- Одна прохибиция имеет один объявленный статус на всё дерево, и это утверждает машина.
- Три абзаца о ветке отката описывают поведение с плана `05-22`, называют объём обещания и ссылаются на каноническое прочтение, а не формулируют четвёртое.
- Прежние редакции названы; верные части сохранены дословно.
- Объявленная граница средства сужена честно и по-прежнему называет, чего средство не делает.
- Поведение денежного пути не изменено ни на строку.

</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-30-SUMMARY.md` when done.

В SUMMARY привести: (1) фактические номера строк трёх абзацев ПОСЛЕ сдвига планом `05-28`;
(2) вывод красного прогона обеих проверок ДО задачи 2 с перечисленными местами и статусами;
(3) вывод зелёного прогона после; (4) хеши RED- и GREEN-коммитов в порядке; (5) `git diff --stat`
как доказательство нуля изменённых исполняемых строк; (6) счёт `ПРЕЖНЯЯ РЕДАКЦИЯ` до и после;
(7) если какая-либо пара `CLAIM_DENIALS` или словарь статусов правились после написания —
причину правки словами, отдельным пунктом; (8) перечень ВСЕХ помеченных обеими проверками
абзацев с их модулями — включая пустой перечень по модулям вне `payment_service.py`, если он
пуст: молчание здесь неотличимо от непроверенного.
</output>
