---
phase: 05-tarify
plan: 21
type: execute
wave: 17
depends_on: ["05-18", "05-19", "05-20"]
gap_closure: true
files_modified:
  - .planning/STATE.md
  - .planning/phases/05-tarify/05-CONTEXT.md
  - .planning/phases/05-tarify/COVERAGE.md
autonomous: true
requirements: [BILL-05, BILL-06, BILL-07]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Гэп 2 раунда 5, операционная половина ---
    - "Ни одна запись `.planning/STATE.md` не принимает риск ссылкой на механизм, который на названном пути не исполняется: оговорка о нейтральности остаточного окна гонки заменена настоящим остаточным исходом — обе строки несут записанное разрешение, доля месяца не исполняется, исход решает порядок подтверждения"
    - "Последствие решения D-26 в `.planning/STATE.md` названо фактическое: отказывают ВСЕ чтения таблицы платежей, включая приём пакетных платежей, работавший до этой фазы, и выкат кода с выкатом очереди миграций разделить нельзя. Само решение D-26 при этом НЕ переоткрывается — правится факт, по которому оно принимается"
    - "Записанная цена варианта `upgrade-only` и то, что код отдаёт после плана `05-18`, больше не отличаются на два порядка: запись называет выбранную форму границы её идентификатором и говорит, что именно теперь получает повышение"
    - "Решение владельца, снятое чекпойнтом плана `05-18`, записано в `05-CONTEXT.md` собственным номером — там же, где записаны D-28 и D-29, и по тому же образцу: выбранный вариант, отвергнутые варианты и причина выбора"
    # --- Гэп 3 раунда 5 ---
    - "§Current Position и frontmatter `progress` не спорят ни друг с другом, ни с ROADMAP, и — главное — перестают зависеть от числа, которое устаревает при появлении следующего плана: формулировка, называющая счёт планов прозой, заменена на не зависящую от него, ровно тем же приёмом, каким план `05-16` вычистил счёт из реестра раундов"
    - "Ни одна прозаическая строка `.planning/STATE.md` не называет константу величины, которую документ не может держать истинной: счёт остаётся ровно там, где у него есть единственный источник (ROADMAP и машинный frontmatter), и запись прямо называет этот источник"
    # --- Полнота распоряжений ---
    - "Ни одна находка код-ревью раунда 5 не осталась без распоряжения: девять номеров раунда квалифицированы раундом и каждый несёт своё распоряжение — закрыт планом, отложен с обоснованием либо записан без требования кода. Прохибиция «закрытие дефекта удалением записи о нём» соблюдена: ни одна прежняя запись не стёрта, устаревшие — переписаны с названной причиной"
    - "Матрица `COVERAGE.md` проверена на действительность после раунда 5 и не перегенерирована: изменившиеся строки названы поимённо, неизменившиеся названы неизменившимися прямо, чтобы молчание не читалось недосмотром"
    # --- Backstop-пункт ---
    - statement: "Человек, читающий §Current Position и §Blockers/Concerns после этого плана, понимает состояние фазы без обращения к отчёту верификации и не встречает ни одного утверждения, которое устареет от появления следующего плана. Понятность и неустареваемость формулировки судятся человеком, а не грепом"
      verification: backstop
  artifacts:
    - ".planning/STATE.md — исправленные записи о нейтральности окна и о последствии D-26, приведённая к коду цена варианта `upgrade-only`, §Current Position без констант величины и запись распоряжений раунда 5"
    - ".planning/phases/05-tarify/05-CONTEXT.md — решение владельца по гэпу 1 раунда 5 собственным номером, по образцу D-28 и D-29"
    - ".planning/phases/05-tarify/COVERAGE.md — сверка после раунда 5: затронутые строки названы, незатронутые названы незатронутыми"
  key_links:
    - "докстринг `create_payment` после плана `05-19` → запись `.planning/STATE.md` о нейтральности окна. Два места одного утверждения; правка одного без другого оставила бы документ, по которому принимаются решения, спорящим с кодом"
    - "докстринг ревизии `0019` после плана `05-20` → запись `.planning/STATE.md` о последствии D-26. Та же пара, тот же порядок: сначала код, потом документ"
    - "ответ чекпойнта задачи 1 плана `05-18` → D-30 в `05-CONTEXT.md` → запись о цене варианта в `.planning/STATE.md`. Односторонняя цепочка: без исполненного ответа документ описывал бы несуществующее"
    - "план `05-16` вычистил константы величины из реестра раундов и оставил их в соседней секции того же файла → гэп 3 раунда 5. Приём тот же, место другое: формулировка обязана перестать называть счёт, а не назвать его верно"
  prohibitions:
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT называть в операционном документе константу величины, которую документ не может держать истинной: она устаревает в момент следующего коммита и разводит документ с источником счёта"
      status: unresolved
      verification: test
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT принимать риск в документе, по которому принимаются решения, ссылкой на механизм, не исполняющийся на названном пути"
      status: unresolved
      verification: test
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT закрывать дефект удалением записи о нём: устаревшая запись переписывается с названной причиной, а не стирается"
      status: unresolved
      verification: test
    - requirement_id: BILL-06
      category: transparency
      statement: "MUST NOT оставлять находку код-ревью без распоряжения и MUST NOT называть находку голым номером без раунда: между раундами номера переназначены, и голый номер указывает не на ту находку"
      status: unresolved
      verification: test
---

<objective>
**Операционная половина гэпа 2 и гэп 3 отчёта `05-VERIFICATION.md` раунда 5.**

Второй раунд подряд блокирующая находка приходит из утверждения, которого код не исполняет, и
два из четырёх неверных утверждений раунда 5 лежат не в коде, а в `.planning/STATE.md` — в
документе, ПО КОТОРОМУ ПРИНИМАЮТСЯ РЕШЕНИЯ:

- **строка 124** принимает неатомарность потолка одновременных намерений оговоркой о том, что
  остаточный риск покрыт решением D-29. Для платежа, заведённого формой, ветка D-29
  недостижима по построению — цепочка из трёх звеньев разобрана планом `05-19`, — и риск
  принят по опоре, которой нет;
- **строка 88** описывает последствие решения D-26 как «подписка на проде не записывается,
  кнопка оплаты упирается в ошибку записи». Фактически отказывают ВСЕ чтения таблицы платежей,
  включая приём пакетных платежей, работавший до этой фазы. Гейт выката читается как «новая
  функция не доедет», а он «старая функция сломается».

Плюс **строка 80**: записанная цена варианта `upgrade-only` говорит об «оставшихся днях» —
величине, ограниченной одним периодом, — тогда как код не ограничивал её ничем. Пятый пункт
`missing:` гэпа 1 требует свести эти две величины, и после плана `05-18` свести их наконец
есть к чему.

**Гэп 3, низкой тяжести и с точной диагностикой.** Константу величины, которую план `05-16`
вычистил из реестра раундов, тот же файл сохранил в §Current Position — и она уже разошлась с
ROADMAP на один план. Правка, приводящая счёт к верному значению, чинит следствие: следующий
план сделает его неверным снова. Требуется тот же приём, который `05-16` применил к реестру, —
формулировка, не называющая счёта вовсе.

Purpose: документ, по которому принимаются решения, обязан быть верным в момент чтения, а не в
момент записи. Всё, что не может остаться верным, обязано перестать утверждаться.
Output: три исправленные записи, решение владельца собственным номером, §Current Position без
устаревающих констант и распоряжения по всем находкам раунда 5.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@.planning/phases/05-tarify/COVERAGE.md
</context>

<flagged_assumptions>

**Зонд краевых случаев вернул все три требования фазы `unclassified`, состояние не
изменилось.** BILL-05, BILL-06 и BILL-07 остаются `unresolved`; допущения планировщика по ним
записаны в `05-15-PLAN.md` (E-01, E-02, E-03) и здесь не дублируются. Равенство «без
молчаливых потерь» держится: 3 поднятых == 0 внесённых в `must_haves` + 3 явных допущения.

**Порядок относительно планов 05-18, 05-19 и 05-20 обязателен.** Документ пишется ПОСЛЕ кода,
а не до и не одновременно: три из четырёх правок этого плана описывают то, что делает код
после названных планов. Обратный порядок дал бы четвёртое утверждение, проверенное чужими
глазами, — ровно тот способ отказа, за который фаза получила раунды 4 и 5.

**Ни одной строки кода и ни одного теста.** `git diff --name-only -- app/ tests/ alembic/
entrypoint.sh` у этого плана обязан быть пуст. Всё, что можно было закрепить машиной, закрыто
планами `05-19` и `05-20`; здесь правятся документы.

**Решения D-26, D-27, D-28 и D-29 НЕ переоткрываются.** Правятся ФАКТЫ, по которым они
приняты и записаны, и добавляется новое решение по гэпу 1 раунда 5. Отчёт верификации прямо
исключает переоткрытие D-26 и D-27 из вменяемого фазе, и это правило здесь соблюдается.

**SUMMARY прошлых планов НЕ правятся ни строкой.** Пятый пункт `missing:` гэпа 1 называет три
места записанной цены варианта: `.planning/STATE.md:80`, чекпойнт плана `05-10` и докстринг
`payment_service.py`. Третье закрыто планом `05-19`, первое закрывается здесь; второе живёт в
`05-10-SUMMARY.md` — историческом документе, фиксирующем, что было решено ТОГДА. Переписывать
его значило бы стирать причину, по которой мера понадобилась. Вместо этого запись строки 80
прямо называет, что цена, названная на чекпойнте `05-10`, читалась у́же того, что исполнял код,
и что расхождение закрыто планом `05-18` в названном направлении.

**Распоряжения по находкам раунда 5 записываются, а КОД по отложенным находкам не пишется — и
это разные вещи.** Отчёт верификации выносит в гэпы четыре находки раунда 5 (`CR-01`, `CR-02`,
`WR-01`, `WR-04`, `WR-05`); ещё три (`WR-02`, `IN-01`, `IN-02`) в гэпы не вынесены. Оставить их
без распоряжения нельзя: `.planning/STATE.md` несёт прохибицию «ни одна находка не осталась без
распоряжения», и раунд 6 нашёл бы её нарушенной. Поэтому они ЗАПИСЫВАЮТСЯ с распоряжением и
названным владельцем-решением, но кода не требуют и задачами не становятся — тем же порядком,
каким план `05-14` обошёлся с находками раунда 3, а план `05-16` — с находками раунда 4.

**Счёт планов после этой волны меняется, и запись обязана это пережить.** К семнадцати
исполненным планам фазы добавляются четыре — `05-18`, `05-19`, `05-20` и этот. Любая
прозаическая формулировка, называющая счёт, станет неверной в момент, когда добавится пятый;
именно поэтому задача 2 требует формулировки, не зависящей от счёта, а не верного счёта.
Машинный `frontmatter.progress` при этом приводится к истинному значению: у него есть
единственный источник (ROADMAP), он читается инструментами, и прозаическим утверждением он не
является.

**Три judgment-tier прохибиции фазы остаются `unresolved`** и этим планом не перемечаются.
Право объявить любую из них закрытой принадлежит верификации раунда 6; документ фиксирует
исполненную работу, а не пройденную проверку, и обязан сказать это прямо — как это уже сделано
в пяти существующих записях §Deferred Items.

**Каноничные позиции безопасности прохибициями не минтятся** — покрыты `<threat_model>` ниже.
**Пакеты не устанавливаются.**

</flagged_assumptions>

<assumption_delta_decision>

**Сигнал сработал: семейство `chosen` — величина, прежде производная, стала выбираемой.**
Счёт планов фазы был производным от ROADMAP: сколько пунктов отмечено, столько и исполнено.
Прозаические строки §Current Position превратили его в ВЫБИРАЕМОЕ значение — автор записи
называет число сам, и с момента коммита оно живёт отдельно от источника. Ровно поэтому оно
разошлось с ROADMAP на один план, и ровно поэтому план `05-16`, вычистивший то же самое из
соседней секции, не закрыл вопрос.

**Существительное, о котором идёт речь:** **счёт исполненных планов фазы** — сегодня он
существует в трёх экземплярах (ROADMAP, машинный frontmatter, проза §Current Position), и ни
один не выводится из остальных.

**Решение: `promote` — ROADMAP объявляется единственным источником счёта, проза лишается
права его называть.** Третий экземпляр (проза) не приводится к верному значению, а
УПРАЗДНЯЕТСЯ: формулировка перестаёт называть счёт и называет ИНВАРИАНТ — в каком состоянии
находится фаза и чего она ждёт. Машинный frontmatter остаётся вторым представлением того же
числа осознанно: он читается инструментами, обновляется ими же и утверждением в прозе не
является; запись прямо называет ROADMAP его источником.

**Форсирующий признак дальнейшей промоции** назван на случай, если и этого окажется мало:
третий раунд подряд с находкой класса «константа величины в операционном документе». При его
наступлении промоция означает машинный гейт на `.planning/STATE.md` — ровно ту прохибицию,
которую план `05-16` объявил `verification: test` и оставил без принуждения (отчёт раунда 5
фиксирует: тестов, проверяющих `.planning/STATE.md`, в суите ноль).

</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Три записи `.planning/STATE.md` перестают утверждать то, чего код не делает, и решение владельца получает свой номер</name>
  <precondition>Планы `05-18`, `05-19` и `05-20` исполнены: их SUMMARY существуют в каталоге фазы, а `05-18-SUMMARY.md` называет идентификатор выбранной формы границы. Без них три из четырёх правок этой задачи описывали бы код, которого нет.</precondition>
  <files>.planning/STATE.md, .planning/phases/05-tarify/05-CONTEXT.md</files>
  <read_first>
    - .planning/STATE.md (строка 80 целиком — запись §Decisions о семантике смены тарифа: вариант `upgrade-only`, названная цена варианта, решения D-28 и D-29, ответ чекпойнта о форме потолка. Правится ОДИН фрагмент — о цене варианта; остальное сохраняется дословно)
    - .planning/STATE.md (строка 88 целиком — запись §Blockers/Concerns о невыкаченной очереди ревизий и решении D-26: перечень ревизий, последствия, необратимость `0013`, отработанные механики. Правится ОДИН фрагмент — о последствии; перечень и необратимость сохраняются)
    - .planning/STATE.md (строка 124 целиком — запись §Deferred Items о `CR-01` раунда 4, закрытом двумя планами: оговорки об атомарности, о ревизии `0019` и о сроке давности. Правится ОДНА оговорка — о нейтральности остаточного окна)
    - .planning/phases/05-tarify/05-CONTEXT.md (строки 82-88 — раздел «Решения владельца по гэпу 1 раунда 4»: D-28 и D-29 с выбранным вариантом, отвергнутыми вариантами, причиной выбора, рейтингом обратимости и предупреждением об очереди ревизий. ОБРАЗЕЦ, по которому пишется новое решение)
    - .planning/phases/05-tarify/05-18-SUMMARY.md (идентификатор выбранной формы, её параметр, перечень отвергнутых форм и запись `<assumption_delta_decision>` — материал нового решения берётся отсюда дословно, а не сочиняется)
    - app/services/payment_service.py (абзацы, переписанные планом `05-19`: оговорка о неатомарности потолка и абзац о цене варианта `upgrade-only`. Запись документа обязана СОВПАДАТЬ с ними, а не пересказывать своими словами — расхождение двух формулировок одного утверждения и есть предмет этого гэпа)
    - alembic/versions/0019_payment_switch_authorized.py (абзац о последствии D-26, переписанный планом `05-20`: то же требование совпадения)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 41-67 — гэп 2 целиком; строка 40 — пятый пункт `missing:` гэпа 1 о записанной цене варианта)
  </read_first>
  <action>
Правятся ТРИ фрагмента `.planning/STATE.md` и добавляется ОДНО решение в `05-CONTEXT.md`.
Каждая правка — точечная замена фрагмента внутри длинной записи; окружающий текст записи
сохраняется дословно, потому что он верен и потому что его потеря была бы закрытием дефекта
удалением записи о нём.

**Фрагмент об оговорке к неатомарности потолка (запись §Deferred Items о `CR-01` раунда 4).**
Снять опору на решение D-29 и назвать настоящий остаточный исход, СЛОВО В СЛОВО с новой
редакцией докстринга `create_payment`, которую записал план `05-19`: у платежа, заведённого
формой, записанное разрешение всегда положительно, потому что при отказе гард возвращает 302
до создания платежа; поэтому отказа не возникает ни у одной из двух строк, доля месяца не
исполняется вовсе, и исход решает ПОРЯДОК подтверждения. Отдельным предложением — механизм,
который дал бы нейтральность окна СВОЙСТВОМ (частичный уникальный индекс на незакрытое
подписочное намерение по образцу `uq_subscriptions_active_user` ревизии `0018`), с прямым
указанием, что он НЕ построен и является работой своего размера. Оговорки об атомарности как
таковой, о месте ревизии `0019` в невыкаченной очереди и о сроке давности из-за D-27 остаются
дословно: они верны.

**Фрагмент о последствии решения D-26 (запись §Blockers/Concerns).** Заменить описание
последствия на фактическое, совпадающее с новой редакцией докстринга ревизии `0019`, которую
записал план `05-20`: ORM-выборка сущности выписывает полный перечень отображённых колонок,
поэтому отказывают ВСЕ чтения таблицы платежей — раздел «Тарифы» отдаёт 500 любому вошедшему,
обработчик уведомления падает ДО ветвления по предмету покупки и потому ломает приём ПАКЕТНЫХ
платежей, работавший до этой фазы, а ЮKassa после конечного числа повторов сдаётся: деньги
списаны, товар не выдан. Назвать прямо, что выкат кода и выкат очереди миграций разделить
НЕЛЬЗЯ, и что гейт D-26 читается не как «новая функция не доедет». Сказать так же прямо, что
САМО решение D-26 этой правкой не переоткрывается — правится факт, по которому оно
принимается. Перечень невыкаченных ревизий, необратимость `0013` и отработанные планом `02-12`
механики сохраняются дословно; перечень ревизий обязан включать `0019` (и `0020`, если план
`05-18` её завёл) либо перестать называть их счётом — второе предпочтительно по причине,
изложенной в задаче 2.

**Фрагмент о цене варианта `upgrade-only` (запись §Decisions).** Заменить формулировку цены на
ту, что описывает код ПОСЛЕ плана `05-18`: назвать выбранную форму границы её идентификатором,
сказать, что именно теперь получает повышение, и назвать величину явно, а не эпитетом,
допускающим два прочтения с разницей в два порядка. Отдельным предложением — что цена,
названная на чекпойнте плана `05-10`, читалась у́же того, что исполнял код (остаток одного
периода против неограниченного накопленного горизонта), что расхождение было воспроизведено
верификацией раунда 5 числами и что оно закрыто планом `05-18` в названном направлении. Запись
о чекпойнте `05-10` НЕ стирается: она фиксирует, что было решено тогда.

**Новое решение владельца в `05-CONTEXT.md`.** Новый подраздел «Решения владельца по гэпу 1
раунда 5 (перед планированием раунда 6)» непосредственно после существующего подраздела о
гэпе 1 раунда 4, с решением под следующим свободным номером после D-29. Состав — по образцу
D-28 и D-29 дословно: какой вариант выбран и его идентификатор; какие отвергнуты и ОДНОЙ
фразой почему (чтобы следующий читатель не восстановил отвергнутый как «упрощение»); причина
выбора прямым текстом; рейтинг обратимости; и — если выбранная форма завела ревизию — место
этой ревизии в невыкаченной очереди D-26. Материал берётся из `05-18-SUMMARY.md`, а не
сочиняется.
  </action>
  <verify>
    <automated>grep -c "денежно нейтральным\|практического последствия у этого сегодня нет\|оставшиеся дни младшего бесплатно" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Команда `<verify>` возвращает `0`: ни одна из трёх прежних формулировок не осталась в файле
    - Настоящий остаточный исход окна назван: `grep -c "ПОРЯДОК подтверждения\|порядок подтверждения" .planning/STATE.md` возвращает не менее `1`
    - Механизм, дающий свойство, назван и назван несделанным: `grep -c "uq_subscriptions_active_user" .planning/STATE.md` возвращает не менее `1`
    - Фактическое последствие названо со всеми путями: в записи о D-26 присутствуют `get_payment_history` и `handle_webhook` (`grep -c` не менее `1` для каждого), и присутствует утверждение о пакетных платежах
    - Неразделимость выката сказана прямо: `grep -c "разделить\|отделить" .planning/STATE.md` находит фразу в записи о D-26 (проверяется чтением и цитируется в SUMMARY)
    - Формулировки документа СОВПАДАЮТ с кодом, а не пересказывают его: в SUMMARY приведены три пары «фрагмент документа / фрагмент докстринга» с указанием файла и диапазона строк каждого
    - Новое решение владельца записано: `grep -cE "^- \*\*D-3[0-9]:" .planning/phases/05-tarify/05-CONTEXT.md` возвращает не менее `1`, и запись содержит идентификатор выбранной формы, отвергнутые варианты и рейтинг обратимости
    - Запись о чекпойнте плана `05-10` не стёрта: `grep -c "05-10" .planning/STATE.md` не меньше, чем было до правки (`git show HEAD:.planning/STATE.md | grep -c "05-10"`)
    - Ни одной строки кода: `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` пуст
  </acceptance_criteria>
  <done>Три записи утверждают то, что делает код, слово в слово с его докстрингами; решение владельца по гэпу 1 раунда 5 записано собственным номером; ни одна прежняя верная запись не стёрта.</done>
</task>

<task type="auto">
  <name>Task 2: §Current Position перестаёт называть счёт, а находки раунда 5 получают распоряжения</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (строки 1-18 — frontmatter целиком, блок `progress` с `total_plans`, `completed_plans` и `percent`; поля `stopped_at`, `last_activity_desc`, `status`)
    - .planning/STATE.md (строки 29-36 — §Current Position целиком: строка `Plan: N of M`, строка `Status`, строка `Last activity`, строка `Progress` с полосой и счётом. ИМЕННО ЗДЕСЬ живёт константа величины, которую план `05-16` вычистил из соседней секции)
    - .planning/STATE.md (строка 92 — приём, которым план `05-16` снял константу величины из реестра раундов: «принадлежит нескольким разным документам», «их число эта запись не называет намеренно», «состав и число гэпов отдаёт сам отчёт». ОБРАЗЕЦ формулировки, не зависящей от счёта)
    - .planning/STATE.md (строки 92-94 — три записи распоряжений: сводная по раундам 1-2, по раунду 3 и по раунду 4. ОБРАЗЕЦ, по которому пишется запись раунда 5: одна запись на раунд, каждый номер квалифицирован раундом, команда `git show` для чтения неизменяемой копии, разделение «закрыто планом» / «отложено с распоряжением» / «записано, кода не требует»)
    - .planning/ROADMAP.md (§«Phase 5: Тарифы» — строка `**Plans:**` и перечень волн с отметками. ЕДИНСТВЕННЫЙ источник счёта после этого плана; frontmatter `progress` приводится по нему)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 50-56 — итог раунда 5 числами; строки 66-81 — таблица раундов с командами `git show`; заголовки находок `CR-01`, `CR-02`, `WR-01`…`WR-05`, `IN-01`, `IN-02` — по одному распоряжению на каждую)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 68-78 — гэп 3 целиком: диагноз и два пункта `missing:`, второй из которых требует формулировки, не зависящей от номера текущего плана)
  </read_first>
  <action>
**Часть 1 — §Current Position теряет право называть счёт.**

Строка вида «Plan: N of M» УПРАЗДНЯЕТСЯ, а не приводится к верному значению: верное значение
станет неверным в момент появления следующего плана, и это ровно тот способ отказа, который
план `05-16` чинил в строке 92 того же файла. На её месте — формулировка, называющая
ИНВАРИАНТ: в каком состоянии фаза (все запланированные планы исполнены / исполняется закрытие
гэпов такого-то раунда) и чего она ждёт (верификации следующего раунда). Прямо сказать, что
счёт эта секция не называет НАМЕРЕННО и что его единственный источник — `.planning/ROADMAP.md`
§«Phase 5», а полный перечень планов отдаёт сам ROADMAP, а не автор записи.

Строка `Progress` с полосой и счётом планов приводится к тому же правилу: полоса и проценты по
ФАЗАМ milestone допустимы (их число задано ROADMAP и меняется решением о milestone, а не
появлением плана), а счёт ПЛАНОВ прозой не называется. Если полоса без счёта планов теряет
смысл — она заменяется строкой о состоянии фазы.

Строки `stopped_at` и `last_activity_desc` frontmatter приводятся к настоящему состоянию и
пишутся так, чтобы не называть числа планов.

Блок `progress` frontmatter приводится к истинному значению по ROADMAP: `total_plans` и
`completed_plans` — машинные поля с единственным источником, и здесь они остаются осознанно.
Рядом, в §Current Position, одной фразой называется, что источник этих полей — ROADMAP, чтобы
следующий читатель знал, где сверять, и не вычислял счёт заново.

**Часть 2 — распоряжения по находкам код-ревью раунда 5.**

Новая запись §Blockers/Concerns, ОДНА на раунд, по образцу трёх существующих. Обязательный
состав:

- команда `git show` с коммитом раунда 5 для чтения неизменяемой копии отчёта, и его итог
  числами — ровно так, как это сделано для раундов 1-4;
- прямое предупреждение, что номера между раундами ПЕРЕНАЗНАЧЕНЫ и каждый номер ниже
  квалифицирован раундом;
- распоряжение по КАЖДОЙ из девяти находок раунда 5, разнесённое по трём группам, как в
  записях раундов 3 и 4:
  - **закрыто планами** — `CR-01` (блокер денежного пути; назвать план и выбранную форму
    границы её идентификатором, плюс регрессии ИМЕНАМИ), `CR-02` (назвать оба плана: кодовую
    половину и эту запись), `WR-01`, `WR-03`, `WR-04`, `WR-05` (назвать план и что именно
    закрыто в каждой);
  - **отложено с распоряжением, владелец не назначен** — `WR-02`: записанный отказ чтится
    ровно на одной из двух ветвей `_extend_subscription`, вторая пишет план безусловно;
    сегодня латентно, потому что отрицательный ответ через форму не приходит, и латентность
    названа ПРИЧИНОЙ отсрочки, а не отсутствием дефекта. Назвать признак, который снимет
    латентность: момент, когда отказ перестанет возвращаться немедленно и на платёж приедет
    записанный ОТКАЗ. Распоряжение — свести обе ветви к одному решению либо закрепить
    инвариант тестом, который упадёт, когда отрицательный ответ станет достижим;
  - **записано, кода не требует сегодня** — `IN-01` (подписочный платёж без плана не
    проверяется; сегодня недостижимо, потому что единственный вызывающий берёт идентификатор
    из выбранного плана; распоряжение — обязательная проверка рядом с потолком, тем же
    порядком, каким сделаны обязательными `kind` и `switch_authorized`) и `IN-02` (срок
    давности сравнивает часы приложения с часами СУБД; на боевом артефакте это два разных
    контейнера; распоряжение — считать порог тем же источником времени либо проставлять время
    рождения приложением, как это уже делает время подтверждения);
- прямое указание, что ни одна из перечисленных отложенных находок не входит в гэпы отчёта
  `05-VERIFICATION.md` раунда 5, а состав и число гэпов отдаёт сам отчёт — формулировка
  плана `05-16`, повторяемая намеренно;
- прямое указание, что запись фиксирует ИСПОЛНЕННУЮ работу, а не пройденную проверку: вердикт
  верификации даёт следующий раунд.

Ни одна прежняя запись не стирается. Записи о раундах 1-4 сохраняются дословно; устаревшие
фрагменты внутри них правятся задачей 1 точечно и с названной причиной.
  </action>
  <verify>
    <automated>grep -cE "Plan: [0-9]+ of [0-9]+" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Команда `<verify>` возвращает `0`: формулировка, называющая счёт планов, из §Current Position упразднена
    - Счёт планов не назван прозой нигде в секции: `sed -n '/^## Current Position/,/^## Performance Metrics/p' .planning/STATE.md | grep -cE "[0-9]+ ?/ ?[0-9]+ планов|[0-9]+ of [0-9]+"` возвращает `0`
    - Источник счёта назван: `sed -n '/^## Current Position/,/^## Performance Metrics/p' .planning/STATE.md | grep -c "ROADMAP"` возвращает не менее `1`
    - Намеренность умолчания сказана прямо: в §Current Position присутствует утверждение о том, что счёт эта секция не называет намеренно (проверяется чтением и цитируется в SUMMARY)
    - frontmatter приведён к ROADMAP: значения `total_plans` и `completed_plans` совпадают с числом пунктов перечня планов Фазы 5 в ROADMAP плюс планами прежних фаз; расчёт показан в SUMMARY
    - §Current Position и ROADMAP не спорят: ни одно утверждение секции не противоречит строке `**Plans:**` ROADMAP — проверяется чтением обеих и записывается в SUMMARY
    - Запись распоряжений раунда 5 существует: `grep -c "код-ревью раунда 5" .planning/STATE.md` возвращает не менее `1`
    - Все девять находок раунда 5 названы: `grep -c "CR-01 (раунд 5)\|CR-02 (раунд 5)\|WR-01 (раунд 5)\|WR-02 (раунд 5)\|WR-03 (раунд 5)\|WR-04 (раунд 5)\|WR-05 (раунд 5)\|IN-01 (раунд 5)\|IN-02 (раунд 5)" .planning/STATE.md` возвращает не менее `9`
    - Ни одного голого номера без раунда в новой записи: каждое вхождение шаблона `CR-`, `WR-` или `IN-` в новой записи сопровождается квалификацией раундом — проверяется чтением, число вхождений и число квалификаций приводятся в SUMMARY
    - Команда чтения неизменяемой копии отчёта раунда 5 присутствует: `grep -c "git show" .planning/STATE.md` возросло относительно `git show HEAD:.planning/STATE.md | grep -c "git show"`
    - Ни одна прежняя запись не стёрта: число записей §Blockers/Concerns и §Deferred Items не уменьшилось относительно `git show HEAD:.planning/STATE.md`
    - Ни одной строки кода: `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` пуст
  </acceptance_criteria>
  <done>§Current Position называет инвариант, а не счёт; источник счёта назван; все девять находок раунда 5 несут квалифицированные раундом распоряжения; ни одна прежняя запись не стёрта.</done>
</task>

<task type="auto">
  <name>Task 3: `COVERAGE.md` сверяется после раунда 5, а не перегенерируется</name>
  <files>.planning/phases/05-tarify/COVERAGE.md</files>
  <read_first>
    - .planning/phases/05-tarify/COVERAGE.md (весь файл: матрица из двадцати одной строки с решениями `INTEGRATE` и `OPT-OUT`, построчная сверка с кодом плана `05-06`, три оговорки, сверка после плана `05-07` и запись о несуществующей возможности SDK. ОБРАЗЕЦ сверки — раздел «Сверка после закрытия гэпа 1», где матрица НЕ перегенерирована, а названы затронутые строки и прямо сказано, что остальные не затронуты)
    - .planning/phases/05-tarify/05-18-SUMMARY.md (какая форма границы выбрана и что именно она делает: единственное, что могло бы затронуть поверхность API ЮKassa в этом раунде)
    - .planning/phases/05-tarify/05-19-SUMMARY.md, .planning/phases/05-tarify/05-20-SUMMARY.md (обе работы — текст и тесты; поверхности API касаться не должны, и это утверждение обязано быть проверено, а не предположено)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 560-575 — вторая половина `WR-05`: снятие намерения с оплаты названо механизмом, который дал бы недостижимость свойством. Он опирается на возможность SDK, помеченную в матрице `OPT-OUT` — эта строка обязана быть пересмотрена явно, даже если решение по ней не меняется)
    - app/services/payment_service.py (перечень обращений к SDK ЮKassa после раунда 5: сверка обязана опираться на грep по коду, а не на память)
  </read_first>
  <action>
Матрица НЕ перегенерируется. Добавляется новый раздел «Сверка после закрытия гэпов раунда 5»
по образцу существующего раздела о плане `05-07`, и в нём делаются ровно три вещи.

**Первое — установить факт грепом, а не памятью.** Перечислить все обращения к SDK ЮKassa в
`app/` после раунда 5 и сравнить с тем, что заявляет матрица. Если единственным вызовом
остался тот же, что назван в построчной сверке плана `05-06`, — сказать это прямо с указанием
файла и строки. Если выбранная форма границы завела новое обращение либо новое событие вебхука
— назвать затронутую строку матрицы и изменить её решение с причиной.

**Второе — пересмотреть строку возможности «пере-запрос статуса платежа» и строку
«отмена платежа» ЯВНО**, даже если решение по ним не меняется. Обе поднимались раундом 5 как
механизмы, способные закрыть окно срока давности свойством, а не формулировкой (`WR-05`), и
молчание о них после этого читалось бы недосмотром. Решение может остаться прежним; причина
обязана быть уточнена по состоянию после раунда 5, а не скопирована.

**Третье — сказать прямо, какие строки НЕ затронуты.** Формулировка образца: «ни одна другая
строка матрицы этой работой не затронута — сказано прямо, чтобы молчание не читалось
недосмотром». Обе прежние оговорки (неподтверждённая подписка на событие отмены в кабинете —
D-27; невыкаченная очередь ревизий — D-26) остаются в силе дословно, и вторая из них ДОПОЛНЯЕТСЯ
ссылкой на исправленное последствие: её нынешняя формулировка описывает радиус поражения у́же
фактического, тем же способом, каким это делала запись `.planning/STATE.md`.

Раздел заканчивается прямым утверждением о том, изменилось ли решение хоть по одной строке, и
если нет — что матрица остаётся действительной без правок.
  </action>
  <verify>
    <automated>grep -c "раунда 5" .planning/phases/05-tarify/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - Команда `<verify>` возвращает не менее `1`: новый раздел сверки существует
    - Факт установлен грепом: в разделе приведён перечень обращений к SDK с указанием файла и номера строки, и он совпадает с фактическим выводом `grep -rn "YooPayment\." app/`
    - Обе поднятые раундом 5 возможности пересмотрены явно: `grep -c "find_one" .planning/phases/05-tarify/COVERAGE.md` и `grep -c "cancel" .planning/phases/05-tarify/COVERAGE.md` возвращают большее значение, чем в `git show HEAD:.planning/phases/05-tarify/COVERAGE.md`
    - Незатронутые строки названы незатронутыми: в разделе присутствует прямое утверждение об этом (цитируется в SUMMARY)
    - Прежние оговорки не стёрты: `git diff -- .planning/phases/05-tarify/COVERAGE.md` не содержит удалённых строк из разделов, написанных планами `05-06` и `05-07`, кроме дополнения оговорки о D-26
    - Матрица не перегенерирована: число строк таблицы решений не изменилось, либо каждое изменение названо поимённо с причиной
    - Ни одной строки кода: `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` пуст
  </acceptance_criteria>
  <done>Матрица проверена на действительность после раунда 5, затронутые строки названы поимённо, незатронутые названы незатронутыми, и ни одна прежняя запись не стёрта.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/STATE.md` → решение о выкате | Документ, по которому принимаются решения; заниженное последствие делает решение принятым по неверному факту |
| запись документа → докстринг кода | Два места одного утверждения; расхождение между ними и есть класс, за который фаза получила два раунда подряд |
| проза, называющая счёт → ROADMAP | Третий экземпляр величины, у которой есть единственный источник; расходится в момент следующего коммита |
| номер находки без раунда → находка | Между раундами номера переназначены; голый номер указывает не на ту находку |
| SUMMARY прошлых планов → сегодняшнее состояние | Исторические документы фиксируют, что было решено тогда; правка стирает причину, по которой мера понадобилась |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-127 | Repudiation | Решение о выкате принимается по заниженному последствию, записанному в операционном документе: «новая функция не доедет» вместо «старая функция сломается» | high | mitigate | Задача 1: фрагмент заменён на фактический, слово в слово с докстрингом ревизии; критерии приёмки требуют присутствия обоих путей и утверждения о неразделимости выката |
| T-05-128 | Repudiation | Риск неатомарности потолка принят в документе ссылкой на механизм, недостижимый на названном пути | high | mitigate | Задача 1: оговорка заменена настоящим остаточным исходом; механизм, дающий свойство, назван и назван несделанным; формулировка совпадает с кодом |
| T-05-129 | Tampering | Правка документа расходится с правкой кода: два автора, два текста, одно утверждение — и следующий раунд находит третью формулировку | high | mitigate | Задача 1 исполняется ПОСЛЕ планов 05-19 и 05-20 и требует СОВПАДЕНИЯ формулировок; критерий приёмки требует привести в SUMMARY три пары «фрагмент документа / фрагмент докстринга» с диапазонами строк |
| T-05-130 | Repudiation | Устаревшая запись стирается вместо переписывания: дефект закрывается удалением записи о нём | high | mitigate | Задачи 1-3: критерии приёмки сравнивают число записей и вхождений с предыдущей ревизией файла; каждая правка точечная, окружающий текст сохраняется дословно |
| T-05-131 | Repudiation | Константа величины приводится к верному значению вместо упразднения и становится неверной от следующего плана — третий раунд подряд | medium | mitigate | Задача 2: формулировка УПРАЗДНЯЕТСЯ, источник счёта назван, намеренность умолчания сказана прямо; критерий приёмки грепом требует нуля вхождений шаблона счёта в секции |
| T-05-132 | Repudiation | Находка раунда 5 остаётся без распоряжения, и раунд 6 находит нарушенной собственную прохибицию документа | medium | mitigate | Задача 2: девять номеров, каждый квалифицирован раундом, разнесены по трём группам с распоряжением у каждого; критерий приёмки грепом считает вхождения |
| T-05-133 | Spoofing | Голый номер находки без раунда указывает на другой дефект: под тем же номером в прежних раундах лежит другое | medium | mitigate | Задача 2: каждое вхождение квалифицировано раундом, число вхождений и число квалификаций сверяются и приводятся в SUMMARY |
| T-05-134 | Repudiation | Матрица возможностей API перегенерируется, и прежние решения с их причинами теряются молча | medium | mitigate | Задача 3: матрица не перегенерируется, добавляется раздел сверки, незатронутые строки названы незатронутыми, дифф проверяется на отсутствие удалений |
| T-05-135 | Information Disclosure | В операционный документ попадают значения секретов или боевые адреса при описании последствий выката | low | accept | Записи описывают ИМЕНА символов, ревизий и путей; ни одна не содержит значений секретов. Принимается тем же порядком, что для существующих записей документа |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов и не трогает код вовсе: `git diff --name-only -- app/ tests/ alembic/ pyproject.toml uv.lock` пуст. Аудит легитимности — `05-RESEARCH.md`, `## Package Legitimacy Audit` |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-21`. План не производит ни одного символа кода — он производит
записи, и они перечисляются здесь по той же причине, по которой перечисляются символы: чтобы
сверка на дрейф не приняла их за неизвестные.

**Файлы (новые):** нет

**Записи (новые), `.planning/STATE.md`:**
- запись §Blockers/Concerns с распоряжениями по девяти находкам код-ревью раунда 5 — одна
  запись на раунд, по образцу записей раундов 3 и 4

**Записи (переписанные), `.planning/STATE.md`:**
- фрагмент §Deferred Items об оговорке к неатомарности потолка одновременных намерений
- фрагмент §Blockers/Concerns о последствии решения D-26
- фрагмент §Decisions о цене варианта `upgrade-only`
- §Current Position целиком — формулировка, не называющая счёта планов
- поля frontmatter `stopped_at`, `last_activity_desc`, `last_activity`, `last_updated` и блок
  `progress`

**Решения (новые), `.planning/phases/05-tarify/05-CONTEXT.md`:**
- решение владельца по гэпу 1 раунда 5 под следующим свободным номером после D-29, в новом
  подразделе «Решения владельца по гэпу 1 раунда 5 (перед планированием раунда 6)»

**Разделы (новые), `.planning/phases/05-tarify/COVERAGE.md`:**
- «Сверка после закрытия гэпов раунда 5»

**Функции, константы, исключения, колонки БД, Alembic revision id, ключи журнала, имена
тестов, CSS-селекторы, маршруты, шаблоны, переменные окружения:** новых нет — план не трогает
код

<verification>
- Ни одна из трёх прежних неверных формулировок не осталась в `.planning/STATE.md`
- Формулировки документа совпадают с докстрингами кода, и три пары приведены в SUMMARY с
  диапазонами строк
- §Current Position не называет счёта планов прозой, называет его источник и не спорит с
  ROADMAP; frontmatter `progress` приведён к ROADMAP, расчёт показан
- Все девять находок раунда 5 несут квалифицированные раундом распоряжения; ни одна прежняя
  запись не стёрта
- Решение владельца по гэпу 1 раунда 5 записано собственным номером в `05-CONTEXT.md`
- `COVERAGE.md` сверена, а не перегенерирована; затронутые строки названы, незатронутые
  названы незатронутыми
- `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh pyproject.toml uv.lock` пуст

**Перенесённые человеческие проверки — задачами НЕ становятся:**
- **Читаемость и неустареваемость новой формулировки §Current Position** — backstop-пункт
  `must_haves`: человек обязан понять состояние фазы без обращения к отчёту и не встретить
  утверждения, которое устареет от появления следующего плана
- Мобильная ширина 375px — план не трогает ни строки разметки
- Настоящий платёж в тестовом магазине ЮKassa (D-26), первое настоящее уведомление после
  выката (backstop плана 05-07), статус «отклонён» у отменённого платежа (D-27)
- **Три judgment-tier прохибиции фазы остаются `unresolved`**: право объявить их закрытыми
  принадлежит верификации раунда 6, а не этому плану
- Владелец риска `WR-07 (раунд 3)` (сокет Docker у веб-слоя) по-прежнему не назначен — пятый
  раунд без имени
- **`WR-02`, `IN-01` и `IN-02` раунда 5 отложены с распоряжением, владелец не назначен**:
  записаны, кода не требуют, задачами не становятся
</verification>

<success_criteria>
- Ни одна запись документа, по которому принимаются решения, не утверждает того, чего код не
  делает
- Гейт выката читается по фактическому последствию, а решение D-26 при этом не переоткрыто
- Записанная цена варианта `upgrade-only` и то, что отдаёт код, сведены; решение владельца
  записано собственным номером
- §Current Position называет инвариант, а не счёт, и не может устареть от появления
  следующего плана
- Ни одна находка раунда 5 не осталась без распоряжения, и ни один номер не назван голым
- Матрица возможностей API проверена на действительность и не перегенерирована
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-21-SUMMARY.md` when done
</output>
