---
phase: 05-tarify
round: 10
verified: '2026-08-19T22:40:00Z'
status: human_needed
score: 22/23 must-haves verified
behavior_unverified: 1
overrides_applied: 1
overrides:
- must_have: 'Прохибиция плана 05-01 («MUST NOT сжигать неистраченный остаток уже оплаченного периода»), объявленная соблюдённой строкой C2 контракта `05-UI-SPEC.md`, остаётся соблюдённой и на ветке отката: разрешённое повышение остатка не сжигает ни на одной ветке денежного пути (must_have №8 плана 05-22)'
  reason: 'Снят ответом владельца `cap-one-month` на чекпойнт задачи 1 плана 05-22 (D-31): перенос свыше одного календарного месяца — объявленное ИСКЛЮЧЕНИЕ из прохибиции 05-01, цена записана числом. Альтернатива `no-carry` отвергнута с причиной. Диспозиция дана владельцем 2026-08-18, идентификатор `override-accepted`, записана решением D-34 (`05-CONTEXT.md`) как КАНОНИЧЕСКОЕ прочтение статуса прохибиции 05-01. ПЕРЕНЕСЕНО РАУНДОМ 10 БЕЗ ИЗМЕНЕНИЙ: волна исправлений раунда 9 предмета переопределения не касалась — `capped_carryover` не тронут ни одной строкой (`git diff 323ed3b..HEAD` по `subscription_period.py` даёт только два удаления: тела `prorated_expiry` и `converted_remainder`, заменённые чтением `_shifted_by_days`).'
  accepted_by: chubav
  accepted_at: '2026-08-19T10:02:50Z'
retired_must_haves:
- must_have: 'Ни одно объявление в коде — докстринг тронутой функции или объявленный инвариант модуля — не утверждает того, чего код рядом с ним не делает (пункт №24 рамки раундов 7-9)'
  retired_by: 'D-36 (`05-CONTEXT.md:119`, коммит 239b82d, идентификатор ответа владельца `ratchet-owns-the-class`)'
  retired_at: '2026-08-19'
  disposition: 'СНЯТ КАК КРИТЕРИЙ ПРИЁМКИ ФАЗЫ 05, НО НЕ ОТМЕНЁН КАК ИНВАРИАНТ. Принуждение переехало на стоячий храповик `tests/test_application/test_declared_invariants.py` (`WITHOUT_WITNESS_CEILING = 37`), входящий в `just test`. Раунд 10 проверил РАБОТОСПОСОБНОСТЬ механизма, на который решение опирается, а не сам класс: гейт зелен (15 passed), реестр `declared_invariants_without_witness.txt` — 37 записей при потолке 37, потолок НЕ поднят, оба файла волной не тронуты (последний коммит по обоим — `04160ce`, план 05-25). Кодовую базу на новые экземпляры класса раунд 10 НЕ ОБХОДИЛ — это работа храповика, и её повторение здесь есть тот самый цикл, который D-36 закрывает.'
re_verification:
  previous_round: 9
  previous_status: gaps_found
  previous_score: 22/24
  previous_verified: '2026-08-19T18:05:00Z'
  gaps_closed:
  - 'ЭКЗЕМПЛЯР (а) раунда 9 — НЕПРИГОДНАЯ УПЛАЧЕННАЯ СУММА — ЗАКРЫТ, проверено МОИМ прогоном через настоящий маршрут `POST /api/billing/webhook`, а не по заявке. Все четыре формы, дававшие раунду 9 HTTP 500 (`NaN`, `Infinity`, `sNaN`, `-Infinity`), дают HTTP 200, платёж `succeeded`, отвергнутый младший тариф НЕ применён, срок сдвинут откатом к полному месяцу, журнал несёт `unreadable="amount"` + `stage="prorate_refused"`. Сверх набора проекта я прогнал строчные написания `nan` и `infinity` — те же 200 и та же классификация.'
  - 'ЭКЗЕМПЛЯР (б) раунда 9 — НЕВЫРАЗИМЫЙ СРОК — ЗАКРЫТ НА ОБЕИХ ВЕТКАХ. Цена `"0.01"`: ветка отказа → 200, `unreadable="span"`, `stage="prorate_refused"`; ветка конверсии → 200, `unreadable="span"`, `stage="convert_remainder"`. Раунд 9 имел здесь HTTP 500 (`OverflowError: date value out of range`). Обе ветки читают ОДИН помощник `_shifted_by_days` (`subscription_period.py:167`), а не пишут два `try` рядом.'
  - 'РАЗДЕЛЕНИЕ ДВУХ ИСХОДОВ ПРОВЕРЕНО, А НЕ ПРИНЯТО НА СЛОВО. `"1e400"` — КОНЕЧНОЕ значение `Decimal`, и оно обязано ловиться гардом СРОКА, а не гардом конечности: мой прогон даёт `unreadable="span"` (не `"amount"`) при HTTP 200. Закрытость перечня подтверждена прямым вызовом чистых функций: `prorated_expiry(paid=NaN)` поднимает `ValueError`, `paid=Infinity` — `OverflowError`, `paid=sNaN` — `InvalidOperation`; то есть `except OverflowError` в `_shifted_by_days` НЕ глушит нефинитность и не выдаёт её за невыразимый срок. Два исхода различимы и в журнале, и в типе отказа.'
  - 'ДЕЛОВОГО ПОТОЛКА НЕ ЗАВЕДЕНО (D-29 не нарушен) — проверено с обеих сторон. Цена `"1.00"` → HTTP 200, `expires_at = 2149-01-31`, откатов НЕТ; цена `"0.05"` → HTTP 200, `expires_at = 4474-05-22`, откатов НЕТ. То же на ветке конверсии (`2128-09-13` и `4066-06-15`). Пропорциональный срок возвращается моментом, а не уходит в откат: граница проведена по ВЫРАЗИМОСТИ момента, а не по правдоподобию величины.'
  - 'RED ПРЕДШЕСТВУЕТ GREEN — ПРОВЕРЕНО ПРОГОНОМ НА ЧУЖИХ КОММИТАХ, А НЕ ПО ПОРЯДКУ ЗАГОЛОВКОВ. Worktree на `4bbdc95` (тест без правки): 2 failed — оба с настоящим HTTP 500 и трассой `OverflowError: date value out of range` из `converted_remainder`. Worktree на `140b640` (тест без правки): 4 failed — ровно четыре нефинитные формы суммы; форма `1e400` там уже зелена, потому что правка невыразимого срока (`f38b5e2`) легла раньше, и это согласуется, а не противоречит.'
  - 'РЕГРЕССИИ НЕТ: полная суита МОИМ прогоном на HEAD — 1796 passed, exit 0 (26:14). Волна тронула ШЕСТЬ файлов (`git diff --stat 323ed3b..HEAD`), из них кода — два; разметка, стили и шаблоны не тронуты ни одной строкой.'
  gaps_remaining: []
  gaps_retired:
  - 'Пункт №24 (КЛАСС «объявление против кода») снят с приёмки фазы решением владельца D-36 `ratchet-owns-the-class`. НЕ «закрыт» и НЕ «пройден» — СНЯТ, с переносом принуждения на храповик. Формулировка выбрана намеренно: закрытием класса это не является и выдавать его за закрытие раунд 10 не станет.'
  regressions: []
deferred:
- truth: 'Цена плана в `PLAN_LIMITS`, положительная и выразимая календарём, но на порядки меньше настоящей, превращает штатный платёж в подписку на века — HTTP 200, молча, без записи в журнале (замер: `"0.05"` → `expires_at = 4474-05-22`)'
  addressed_in: '.planning/todos/pending/plan-price-credibility-bound.md (коммит 2310c23), severity medium, status pending'
  evidence: 'ПРИНЯТЫЙ ОСТАТОК, А НЕ ОТКРЫТЫЙ ГЭП, и причина названа записью, а не впечатлением: любой порог, ловящий `"0.05"` и щадящий `"1.00"`, есть business cap по правдоподобию, а D-29 запрещает ровно это («обрезание было бы взятием денег без выдачи»). Патология не в формуле, а в том, что `price` неверна; починка требует РЕШЕНИЯ О ЦЕНЕ и лежит вне денежного пути. Раунд 10 предъявляет величину прогоном и НЕ держит ею фазу.'
behavior_unverified_items:
- truth: Раздел тарифов пригоден к использованию на мобильных ширинах (критерий 4 ROADMAP)
  test: 'Открыть /billing в браузере на ширине 375px с реальными данными: три карточки планов, четыре метра, история платежей длиннее экрана, блок пакетов сообщений'
  expected: 'Карточки планов складываются в одну колонку; подписи колонок истории (`[data-cell-label]`) едут вместе со значениями; ни один блок не требует горизонтальной прокрутки; кнопки оплаты нажимаемы пальцем (порог проекта 44px)'
  why_human: 'Браузерного/e2e-харнесса в проекте нет — проверено мной заново: `playwright`, `selenium`, `puppeteer` не встречаются ни в `pyproject.toml`, ни в `uv.lock`. Все относящиеся тесты читают CSS и HTML как ТЕКСТ и утверждают ОБЪЯВЛЕНИЕ правила, а не отрисовку. Разметка и стили волной исправлений не тронуты вовсе: изменённых файлов шесть, шаблонов и стилей среди них нет. ДЕСЯТЫЙ раунд переноса без изменений — и это правильная диспозиция, а не гэп.'
human_verification:
- test: Открыть /billing на 375px и пройти раздел глазами (см. `behavior_unverified_items`)
  expected: Одна колонка, никакой горизонтальной прокрутки, кнопки нажимаемы
  why_human: Браузерного харнесса в проекте нет. Десятый раунд подряд; объявление правила держит машина, отрисовку — нет
---

# Фаза 5: Тарифы — отчёт верификации, РАУНД 10

**Цель фазы:** Пользователь понимает, сколько ресурса тарифа он израсходовал, за что платил и может продлить подписку не выходя из раздела.
**Проверено:** 2026-08-19T22:40:00Z
**Статус:** `human_needed`
**Повторная верификация:** Да — после волны исправлений раунда 9 (без плана, коммиты `4bbdc95`, `f38b5e2`, `140b640`, `65adfe9`, `239b82d`, слиты `cff5b40`) и решения владельца D-36

## Ответ прямо, без предисловия

**Оба блокера раунда 9 закрыты по существу, и я это ВОСПРОИЗВЁЛ, а не вычитал.** Волна не оставила ни одной формы, дающей 500 на маршруте уведомления при списанных деньгах; два исхода — нечитаемая сумма и невыразимый срок — остались РАЗЛИЧИМЫМИ, а не слились; делового потолка на пропорциональный срок никто не завёл, и цена `"1.00"` по-прежнему покупает 2149 год, а не календарный месяц.

**Пункт №24 снят с приёмки решением владельца D-36 — и это не то же самое, что «закрыт».** Я не искал новых экземпляров класса и не имел на это права по решению; я проверил механизм, на который решение опирается. Храповик зелен, реестр не вырос, потолок не поднят.

**Единственное, что не даёт поставить `passed`, — критерий 4 (мобильные ширины).** Он десятый раунд подряд остаётся вне досягаемости машины, потому что браузерного харнесса в проекте нет. Это `human_needed`, а не гэп: код и разметка на месте и не тронуты, не хватает ГЛАЗ.

## Что этот раунд должен был решить

| Вопрос раунда | Ответ | Чем снят |
| --- | --- | --- |
| Экземпляр (а) — нефинитная `amount_value` — закрыт? | **Да**, включая формы сверх названных | Мой прогон через `POST /api/billing/webhook`, 7 форм |
| `"1e400"` ловится гардом СРОКА, а не конечности? | **Да**, `unreadable="span"` при 200 | Тот же прогон, отдельная строка таблицы |
| Экземпляр (б) — невыразимый срок — закрыт на ОБЕИХ ветках? | **Да**, отказ и конверсия | Мой прогон, оба посева |
| Перечень `except` закрыт — нефинитность НЕ выдаётся за невыразимый срок? | **Да**, типы отказа разные | Прямой вызов чистых функций |
| Деловой потолок не заведён (D-29)? | **Не заведён**, `1.00` и `0.05` пропорциональны | Мой прогон, откатов ноль |
| Храповик держит? | **Держит**, 37/37, потолок не поднят | 15 passed + счёт записей кодом |
| Основание отзыва `Complete` у BILL-05/06/07 ещё стоит? | **Больше не стоит** | Разбор ниже |

## Наблюдаемые истины

Рамка раундов 7-9 состояла из 24 пунктов. Пункт №24 **СНЯТ** решением владельца D-36 (`ratchet-owns-the-class`) — рамка раунда 10 составляет **23 пункта**. Новых пунктов раунд не заводит: волна исправлений плана не имела, новых предметов не объявляла, шаблонов не касалась.

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| **Критерии успеха ROADMAP** | | | |
| 1 | Пользователь может продлить текущую подписку и увидеть обновлённый срок | ✓ VERIFIED | Форма продления → `POST /billing/subscribe`: `plan_card.html:94` и `balance.html:120` → обработчик `app/pages/billing.py:275`. Срок двигает вебхук, и он проверен прогоном на 15 сценариях (таблица ниже). Разметка волной не тронута; полная суита 1796 passed |
| 2 | Пользователь видит потребление и остаток по всем четырём осям | ✓ VERIFIED | `AXIS_ORDER` (`plan_usage.py:71`, четыре оси) → `plan_axes` (`:200`) → страница (`billing.py:9`) → цикл метров `balance.html:132`. Данные из БД, не литерал |
| 3 | Пользователь видит историю платежей с датой, суммой и статусом | ✓ VERIFIED | `count_payments`/`get_payment_history` (`billing_service.py:239`, `:208`) вызываются страницей (`billing.py:200-201`) → блок «История платежей» (`balance.html:206`) → `payment_row.html`: дата (`:50`), сумма (`:57`), статус (`:63`). Все три поля читаются из строки модели |
| 4 | Раздел тарифов пригоден к использованию на мобильных ширинах | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Объявление правила держит машина; отрисовки не видит ни один тест, харнесса в проекте нет (проверено заново по `pyproject.toml` и `uv.lock`). **ДЕСЯТЫЙ раунд переноса** |
| **Гэп 1 раунда 6 (граница предоплаченного горизонта)** | | | |
| 5 | Величина предоплаченного горизонта ограничена на ОБЕИХ ветках повышения | ✓ VERIFIED | `capped_carryover` волной не тронут (диффа по функции нет); мой прогон ветки конверсии на невыразимой цене даёт откат к границе, а не перенос горизонта |
| 6 | Правило границы объявлено ОДИН раз чистой функцией и денежным путём ЧИТАЕТСЯ | ✓ VERIFIED | `capped_carryover` (`subscription_period.py:407`), единственное чтение — `payment_service.py:1462` |
| 7 | Регрессия «нечитаемая цена ПЛАНА ПЛАТЕЖА не переносит весь горизонт» | ✓ VERIFIED | Зелёный в моём прогоне 12 целевых случаев |
| 8 | Регрессия «повышение с подписки `free` не переносит весь горизонт» | ✓ VERIFIED | Тот же прогон |
| 9 | Два испускания `subscription_prorating_skipped` различимы разбирающим обращение | ✓ VERIFIED | **УСИЛЕНО ВОЛНОЙ И ПРОВЕРЕНО МНОЙ:** поле `stage` даёт `prorate_refused` / `convert_remainder`, а поле `unreadable` получило ТРЕТЬЕ значение `"span"` в обеих ветках. Мой прогон предъявляет все три значения: `amount`, `span`, и (существующими тестами) `price` |
| 10 | Ни один существующий зелёный тест фазы не покраснел | ✓ VERIFIED | **МОЙ прогон полной суиты на HEAD: 1796 passed, exit 0** (было 1783 до волны — прирост 13 = новые случаи волны) |
| 11 | Прохибиция 05-01 остаётся СОБЛЮДЁННОЙ и на ветке отката (must_have №8 плана 05-22) | **PASSED (override)** | Override перенесён без изменений — предмет волной не тронут. Принято `chubav` 2026-08-19T10:02:50Z, каноническое прочтение D-34 |
| **Гэп 2 раунда 6 (объявления против кода — экземпляры)** | | | |
| 12 | Объявление `converted_remainder` приведено к поведению (длина месяца) | ✓ VERIFIED | Волной затронуто только возвращаемое значение (`datetime` → `datetime \| None`); утверждение о длине месяца не менялось, гейт объявлений зелен |
| 13 | Прямая таблица решений `converted_remainder` | ✓ VERIFIED | Не тронуто |
| 14 | Ветка `base == now_utc` объявлена несущей | ✓ VERIFIED | Не тронуто |
| 15 | Ни одно объявление не утверждает роста плана на стадии применения | ✓ VERIFIED | `test_neither_declaration_claims_the_plan_only_grows_at_the_apply_stage` зелен в полной суите |
| 16 | Понижение по записанному ответу оставляет след в журнале | ✓ VERIFIED | Не тронуто |
| 17 | Записанный ответ подчинён правилу истечения | ✓ VERIFIED | Не тронуто |
| 18 | Машинный гейт класса «объявление против кода»; храповик и негативные контроли | ✓ VERIFIED | **ПРЕДМЕТ ОСОБОЙ ПРОВЕРКИ ЭТОГО РАУНДА (D-36 стоит на нём).** Мой прогон: 15 passed. Реестр — **37 записей при `WITHOUT_WITNESS_CEILING = 37`**, сосчитано кодом (`ledger_entries()`), а не глазами; отобранных абзацев 153. Потолок НЕ поднят, оба файла волной не тронуты (последний коммит — `04160ce`, план 05-25) |
| 19 | Гейт честно называет, чего он НЕ доказывает | ✓ VERIFIED | Докстринг средства на месте; D-36 цитирует границу и записывает её в принятый долг прямым текстом |
| 20 | Тир прохибиций класса приведён к существующему принуждению (D-33) | ✓ VERIFIED | D-33 не переоткрыт; D-36 явно объявляет D-29/D-30/D-31/D-33/D-34/D-35 непереоткрытыми |
| **Операционная половина** | | | |
| 21 | D-31…D-36 записаны своими номерами; находки раунда получают распоряжения | ✓ VERIFIED | **D-36 на месте** (`05-CONTEXT.md:119`, коммит `239b82d`): идентификатор `ratchet-owns-the-class`, отвергнутая ветвь `keep-auditing` названа вместе с ценой, доказательства даны числами, проверяемыми по дереву. Две из них я пересчитал сам: планов фазы **30**, из них с `gap_closure: true` — **24**. Оговорка о чужой нумерации D-35/D-36/D-37 Фазы 4 на месте |
| 22 | Записанная цена D-30 сведена с тем, что код отдаёт в ветке отката | ✓ VERIFIED | Закрыт раундом 8; волной не тронуто |
| 23 | §Current Position и frontmatter `progress` не спорят с ROADMAP | ✓ VERIFIED | `just tracking-check` → 5 passed; `STATE.md` `progress` 82/82, ROADMAP «5. Тарифы \| 30/30» |
| ~~24~~ | ~~Ни одно объявление в коде не утверждает того, чего код рядом с ним не делает~~ | **RETIRED (D-36)** | Снят как критерий приёмки фазы 05 решением владельца `ratchet-owns-the-class`. Инвариант НЕ отменён — переехал на храповик (пункт №18, проверен). Раунд 10 кодовую базу на новые экземпляры НЕ ОБХОДИЛ, и это исполнение решения, а не пропуск |

**Счёт:** 22/23 must-have подтверждено (включая 1 `PASSED (override)`), 1 ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (критерий 4), 0 ✗ FAILED, 1 пункт снят с приёмки решением владельца.

## Мой прогон денежного пути (не заявка волны, а измерение)

Пробник заведён мной, прогнан через **настоящий маршрут** `POST /api/billing/webhook` (прямой вызов `handle_webhook` мимо `app/routes/billing.py:198-202` не проходит и 500 не ловит), после прогона удалён — рабочее дерево чисто.

**Посев «отказ ПО ПРАВИЛУ»:** живая подписка `pro` + платёж `basic` со `switch_authorized=None`. **Посев «разрешённое повышение»:** живая `basic` + платёж `pro` со `switch_authorized=True`.

### Формы уплаченной суммы (прейскурант проекта, без подмены цен) — экземпляр (а)

| `amount_value` | HTTP | Платёж | План | `expires_at` | `unreadable` | `stage` | Раунд 9 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `"NaN"` | **200** | succeeded | pro | 2026-10-13 | `amount` | `prorate_refused` | было **500** |
| `"Infinity"` | **200** | succeeded | pro | 2026-10-13 | `amount` | `prorate_refused` | было **500** |
| `"-Infinity"` | **200** | succeeded | pro | 2026-10-13 | `amount` | `prorate_refused` | — |
| `"sNaN"` | **200** | succeeded | pro | 2026-10-13 | `amount` | `prorate_refused` | было **500** |
| `"1e400"` | **200** | succeeded | pro | 2026-10-13 | **`span`** | `prorate_refused` | было **500** |
| `"nan"` (сверх набора) | **200** | succeeded | pro | 2026-10-13 | `amount` | `prorate_refused` | — |
| `"infinity"` (сверх набора) | **200** | succeeded | pro | 2026-10-13 | `amount` | `prorate_refused` | — |

Строка `"1e400"` — главная в таблице. Значение **КОНЕЧНО**, классификацией конечности не отсеивается и обязано попасть в соседний исход. Оно туда и попадает: `span`, а не `amount`. Два гарда не слиты в один, и журнал говорит разбирающему обращение правду о том, что именно сломано.

### Формы цены плана — экземпляр (б) и граница D-29

| Цена `pro` | Посев | HTTP | `expires_at` | Откатов | `unreadable` / `stage` |
| --- | --- | --- | --- | --- | --- |
| `"0.01"` | отказ | **200** | 2026-10-13 | 1 | `span` / `prorate_refused` |
| `"0.01"` | конверсия | **200** | 2026-10-13 | 1 | `span` / `convert_remainder` |
| `"0.05"` | отказ | **200** | **4474-05-22** | **0** | — |
| `"0.05"` | конверсия | **200** | **4066-06-15** | **0** | — |
| `"1.00"` | отказ | **200** | **2149-01-31** | **0** | — |
| `"1.00"` | конверсия | **200** | **2128-09-13** | **0** | — |
| `"4900.00"` (прейскурант) | отказ | 200 | 2026-09-22 | 0 | — |
| `"4900.00"` (прейскурант) | конверсия | 200 | 2026-09-26 | 0 | — |

Раунд 9 на цене `"0.01"` получал **HTTP 500 `OverflowError: date value out of range`** — теперь 200 на обеих ветках, с собственным словом в журнале. Строки `"0.05"` и `"1.00"` — **контроль в обратную сторону**: если бы волна «починила» дефект деловым потолком, они ушли бы в откат. Они не ушли: пропорциональный срок отдаётся целиком, D-29 не нарушен.

### Закрытость перечня `except` (прямой вызов чистых функций)

| Вызов | Ответ | Читается как |
| --- | --- | --- |
| `prorated_expiry(paid=Decimal("NaN"), price=4900)` | поднимает `ValueError` | нефинитность НЕ выдаётся за невыразимый срок |
| `prorated_expiry(paid=Decimal("Infinity"), …)` | поднимает `OverflowError` | тот же вывод; гасит его ВЫЗЫВАЮЩИЙ, классифицируя конечность |
| `prorated_expiry(paid=Decimal("sNaN"), …)` | поднимает `InvalidOperation` | тот же вывод |
| `prorated_expiry(paid=1490, price=Decimal("0.01"))` | **`None`** | момента нет — это часть контракта, а не отказ |
| `converted_remainder(new_price=Decimal("0.01"))` | **`None`** | ответ ОДИНАКОВ у обеих функций сдвига |

`_shifted_by_days` (`subscription_period.py:167-194`) ловит **только** `OverflowError`. Это проверено поведением, а не чтением: нефинитная сумма падает РАНЬШЕ, внутри `int()` в `prorated_days`, и до помощника не доходит — ровно как объявляет его докстринг. Единственные два вызывающих (`payment_service.py:1283`, `:1397`) оба стоят под гардом; третьего пути в эти функции в `app/` нет.

## RED предшествует GREEN — проверено на чужих коммитах

| Worktree | Что там лежит | Прогон целевых тестов |
| --- | --- | --- |
| `4bbdc95` | тест невыразимого срока БЕЗ правки | **2 failed**, обе — настоящий HTTP 500, трасса `OverflowError: date value out of range` из `converted_remainder` |
| `140b640` | тест непригодной суммы БЕЗ правки | **4 failed** — ровно четыре нефинитные формы; `1e400` зелен, потому что его исход починен раньше коммитом `f38b5e2` |

Порядок коммитов волны — тест, затем правка, дважды — не заявлен, а предъявлен прогоном. Оба worktree удалены.

## Правка кода: объём

`git diff --stat 323ed3b..HEAD` — шесть файлов, из них кода два. Содержательный дифф (без комментариев и докстрингов):

- `payment_service.py`: `paid = Decimal(...)` → `candidate = Decimal(...)`; `paid = candidate if candidate.is_finite() else None`; развилка `if price is None or paid is None` заменена вычислением `new_expiry` ДО развилки и проверкой `if new_expiry is None`; поле `unreadable` получило третье значение `"span"`. То же в ветке конверсии.
- `subscription_period.py`: два тела (`return base + timedelta(...)`) заменены чтением общего `_shifted_by_days`; сигнатуры обеих функций сдвига — `datetime | None`.

Ни `capped_carryover`, ни `prorated_days`, ни один шаблон, ни один стиль не тронуты.

## Требования

| Требование | Описание | Статус | Свидетельство |
| --- | --- | --- | --- |
| BILL-05 | Пользователь может продлить текущую подписку | ✓ SATISFIED | Форма → `plan_card.html:94`, `balance.html:120` → `POST /billing/subscribe` (`app/pages/billing.py:275`); срок двигает вебхук, проверенный мной на 15 сценариях |
| BILL-06 | Потребление и остаток по четырём осям | ✓ SATISFIED | `AXIS_ORDER` (`plan_usage.py:71`) → `plan_axes` (`:200`) → `balance.html:132` |
| BILL-07 | История своих платежей | ✓ SATISFIED | `get_payment_history` (`billing_service.py:208`) + `count_payments` (`:239`) → `balance.html:206` → `payment_row.html:50/57/63` (дата, сумма, статус) |

**Об отзыве `Complete` у трёх требований — прямо.** `REQUIREMENTS.md:235-237` снимает `Complete` у BILL-05/06/07 2026-08-16 с причиной **дословно** «фаза в статусе `gaps_found`». Это единственное названное основание, и **оно больше не стоит**: раунд 10 не находит ни одного гэпа, статус фазы — `human_needed`. Реализация, названная в `REQUIREMENTS.md:115-117` для всех трёх, мной прослежена до строки БД и до отрисовки. Формальное основание отзыва исчерпано; остаётся единственное незакрытое условие фазы — критерий 4, и он не относится ни к одному из трёх требований по предмету (продление, оси лимитов, история платежей — каждое проверено на существующих ширинах).

⚠️ **Наблюдение, не гэп:** номера строк в `REQUIREMENTS.md:115-117` устарели на десятки строк (`plan_card.html:72` → фактически `:94`; `balance.html:96/108/166` → `:120/:132/:206`; `billing.py:151` → `:275`; `billing_service.py:179/210` → `:208/:239`). Механизм на месте и проверен; расходятся только якоря. Стоит поправить одной правкой в тот же заход, каким будет возвращён `Complete`.

## Проверка храповика (механизм, на который опирается D-36)

| Проверка | Результат |
| --- | --- |
| `pytest tests/test_application/test_declared_invariants.py tests/test_planning/test_state_progress_matches_roadmap.py` | **15 passed** |
| Записей в реестре (сосчитано `ledger_entries()`, не глазами) | **37** |
| `WITHOUT_WITNESS_CEILING` | **37** — не поднят |
| Отобранных абзацев, которые гейт разбирает | 153 |
| Последний коммит по `declared_invariants_without_witness.txt` | `04160ce` (план 05-25) — волной НЕ тронут |
| Последний коммит по `test_declared_invariants.py` | `04160ce` — потолок волной НЕ трогали |
| Входит ли в `just test` | Да (`tests/` целиком; в моём прогоне 1796 passed) |

**Суждение прямо:** механизм, на который D-36 переносит принуждение, РАБОТАЕТ и запаса не имеет — реестр стоит РОВНО НА ПОТОЛКЕ (37 из 37). Это ровно то, чего от храповика хотят: следующий абзац без свидетеля краснит прогон немедленно, а не через раунд. Но это же значит, что механизм не переживёт ни одной небрежной правки объявлений без немедленной красноты — что и есть форсирующий признак, названный самим D-36.

## Поведенческие спот-чеки

| Поведение | Команда | Результат | Статус |
| --- | --- | --- | --- |
| Полная суита на HEAD | `uv run pytest tests/ -q` | **1796 passed**, exit 0, 26:14 | ✓ PASS |
| Целевая регрессия денежного пути | `pytest test_billing_payment_errors.py -k "unusable_amount or beyond_the_calendar or span_that_the_calendar or refused_branch_names or unreadable_paid_plan_price or non_finite_price"` | 12 passed | ✓ PASS |
| Храповик объявлений + гейт трекинга | `pytest test_declared_invariants.py test_state_progress_matches_roadmap.py` | 15 passed | ✓ PASS |
| Гейт трекинга планов | `just tracking-check` | 5 passed | ✓ PASS |
| Мой независимый пробник маршрута | 15 сквозных случаев + 5 прямых вызовов | все 200 / ожидаемые ответы | ✓ PASS |
| RED-контроль на `4bbdc95` | `pytest -k "beyond_the_calendar or …"` | 2 failed (настоящие 500) | ✓ PASS (краснота предъявлена) |
| RED-контроль на `140b640` | `pytest -k "unusable_amount"` | 4 failed | ✓ PASS (краснота предъявлена) |
| Отрисовка на 375px | — | харнесса нет | ? SKIP → человек |

## Найденные анти-паттерны

| Файл | Строка | Паттерн | Серьёзность | Влияние |
| --- | --- | --- | --- | --- |
| — | — | `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` в четырёх изменённых файлах | — | **не найдено ни одного** |

## Принятый остаток (не гэп)

Цена `"0.05"` даёт HTTP 200 и молча записанный `expires_at = 4474-05-22`. Записано и оценено: `.planning/todos/pending/plan-price-credibility-bound.md` (коммит `2310c23`, severity medium). Ловить это значит вводить порог по правдоподобию цены — то есть business cap, прямо запрещённый D-29, — и решение об этом есть решение О ЦЕНЕ, а не багфикс; лежит оно вне денежного пути. **Раунд 10 предъявляет величину замером и не держит ею фазу.**

⚠️ Мелкое расхождение, которое я называю, а не прячу: докстринг `prorated_days` и файл остатка называют для цены `"0.05"` число **923 800** дней (4555 год), а мой замер даёт **894 000** дней (4474 год). Обе величины верны — они отличаются длиной месяца отсчёта (31 против 30), ровно тем же способом, каким D-31 уже потребовал записывать величину ФОРМОЙ, а не константой. Класс «величина названа числом без даты отсчёта» снят с приёмки фазы решением D-36 и здесь только отмечен; в коде ничего не ломает.

## Сводка

**Гэпов нет.** Оба блокера раунда 9 закрыты по существу и проверены прогоном через настоящий маршрут, а не по заявке: ни одна из семи форм непригодной суммы и ни одна из двух ветвей невыразимого срока больше не даёт 500 на уведомлении при списанных деньгах. Различимость исходов сохранена (`amount` / `span` / `price` + `stage`), перечень перехватываемых типов закрыт и это подтверждено разными типами отказа у чистых функций, деловой потолок не заведён — контрольные цены `"1.00"` и `"0.05"` остаются пропорциональными. Порядок RED→GREEN предъявлен прогоном на двух чужих коммитах. Полная суита 1796 passed. Реестр объявлений не вырос, потолок не поднят, храповик зелен и входит в `just test`.

**Пункт №24 снят с приёмки, а не пройден,** и раунд 10 не выдаёт одно за другое: класс «объявление против кода» продолжает существовать в форме, которую машина не ловит, — так объявляет сам D-36 в своём принятом долге. Раунд по решению владельца кодовую базу на новые экземпляры не обходил.

**Единственное незакрытое условие — критерий 4, мобильные ширины,** и он не гэп, а работа для человека: браузерного харнесса в проекте нет, разметка и стили этой волной не тронуты, объявление правил держит машина (185 относящихся тестов зелены в составе суиты), отрисовку не видит никто. Десятый раунд подряд, и диспозиция та же — верная.

Вердикт: **`human_needed`**. Не `passed` — потому что критерий 4 остаётся непроверенным глазами. Не `gaps_found` — потому что ни снятый решением критерий, ни записанный и оценённый остаток гэпом не являются.

---

_Проверено: 2026-08-19T22:40:00Z_
_Верификатор: Claude (gsd-verifier), раунд 10_
