---
phase: 05-tarify
verified: 2026-08-17T06:09:30Z
status: gaps_found
score: 19/26 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 11/15
  previous_verified: 2026-08-16T18:50:00Z
  gaps_closed:
    - "Гэп 1 раунда 3 (регрессия плана 05-11): понижение, купленное ПОСЛЕ окончания оплаченного срока, теперь ПРИМЕНЯЕТСЯ. Проверено независимым прогоном, а не по SUMMARY: `subscription_is_live` объявлен один раз (`app/application/billing/subscription_period.py:53`), `switch_is_refused` принимает ОБЯЗАТЕЛЬНЫЙ keyword-only `period_is_live` без умолчания (`plan_switch.py:29`, `TypeError` закреплён `test_the_rule_cannot_be_asked_without_the_liveness_term`), признак снимается ДО сдвига срока (`payment_service.py:577` перед `:579`, порядок закреплён AST-тестом `test_the_liveness_is_sampled_before_the_date_moves`). Регрессия `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage` существует и зелёная. Прогон трёх относящихся файлов: 92 passed."
    - "Гэп 2 раунда 3 (`status: partial`): оба пункта `missing` закрыты. Указатель на раунды больше не утверждает, какой раунд лежит на диске — каждый раунд адресуется своим коммитом (`4989f7a`, `1d29360`, `164d210` — все три проверены `git show`, отдают заявленные документы), полный перечень отдаёт `git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md`. Находки раунда 3 записаны отдельной 🟡 записью (STATE.md:95), каждый номер квалифицирован раундом; инвариант квалификации держится: вхождений `WR-07` — 4, из них квалифицированных — 4 (по два на раунды 2 и 3)."
  gaps_remaining: []
  regressions:
    - "Новый блокер, ВНЕСЁННЫЙ закрытием гэпа 1 (CR-01 ревью раунда 4, подтверждён независимым воспроизведением этой верификацией). План 05-13 объединил ФОРМУ входных данных правила и не объединил МОМЕНТ их снятия. Решение ПРОДАВАТЬ снимается в момент нажатия кнопки, решение ВЫДАТЬ — в момент прихода уведомления, между ними лежит вся сессия оплаты на стороне ЮKassa, и разрешение сделки на платеже НЕ ЗАПИСЫВАЕТСЯ. Отказ на стадии применения при этом не отменяет начисление дней: срок сдвигается БЕЗУСЛОВНО строкой выше решения о плане. Итог воспроизведения: 3×1490 ₽ (`basic`) + 1×4900 ₽ (`pro`) = 9370 ₽ покупают 121 день тарифа Pro стоимостью 19 600 ₽."
gaps:
  - truth: "«Продали, но не выдали» недостижимо ни при каком состоянии подписки: не существует такого `expires_at`, при котором гард формы ПРОПУСКАЕТ платёж за переход, а стадия применения этот переход НЕ ВЫДАЁТ (must_have №1 плана 05-13); и выбранный чекпойнтом вариант исполнен ЦЕЛИКОМ — смешанного состояния «продажа разрешена — применение отказано» не осталось ни в одной ветке кода (must_have №7 плана 05-13)"
    status: failed
    reason: "Оба must_have фальсифицированы ОДНИМ корнем, и корень — не тот, что закрывал план 05-13. План 05-13 сделал признак живости ОБЯЗАТЕЛЬНЫМ входом правила; это верно и проверено. Но правило по-прежнему спрашивают ДВАЖДЫ, в РАЗНЫЕ моменты, по РАЗНОМУ состоянию БД: стадия НАМЕРЕНИЯ (`app/pages/billing.py:319-325`) считает `subscription_is_live` от снимка шелла в момент нажатия кнопки, стадия ПРИМЕНЕНИЯ (`app/services/payment_service.py:577`) — от перечитанной строки в момент прихода уведомления. Между этими моментами лежит вся сессия оплаты у ЮKassa, и решение, которым платёж БЫЛ РАЗРЕШЁН, на платеже не сохраняется вовсе — стадия применения его не восстанавливает, а принимает заново. Раунд 3 сформулировал дефект как «объединено ОБЪЯВЛЕНИЕ, а не ВХОДНЫЕ ДАННЫЕ»; раунд 4 — «объединена ФОРМА входных данных, а не МОМЕНТ их снятия». Отказ на стадии применения при этом НЕ отменяет начисление дней: `subscription.expires_at = next_expiry(...)` (`payment_service.py:579`) стоит БЕЗУСЛОВНО и ВЫШЕ решения о плане, поэтому отвергнутый платёж всё равно покупает месяц ДЕЙСТВУЮЩЕГО (старшего) тарифа. ВОСПРОИЗВЕДЕНО НЕЗАВИСИМО, без единой правки `app/`, через НАСТОЯЩИЙ обработчик формы и настоящий `handle_webhook`: пользователь БЕЗ подписки заводит три платежа `basic` и один `pro` (гард пропускает все четыре — защищать нечего, подписки нет; потолка на число `pending`-платежей в `create_payment` нет ни одного), подтверждает `pro` первым, затем три `basic`. Итог прогона: `план='pro', срок через 121 дней, уплачено 9370 ₽` при цене четырёх месяцев Pro 19 600 ₽; журнал трижды пишет `subscription_plan_preserved`. Гонки не требуется — достаточно открыть форму оплаты в нескольких вкладках. Второй путь того же корня (WR-01 раунда 4), где вред достаётся ПОЛЬЗОВАТЕЛЮ, а не кассе: истёкшая подписка `pro` → заводится платёж `basic` (гард пропускает по `apply-after-expiry`) → пользователь сначала оплачивает `pro` (срок снова живой) → затем оплачивает `basic` → деньги взяты, `basic` не выдан. Ровно тот исход, который раунд 3 назвал блокером, и ровно то обещание, которое даёт пользователю `DOWNGRADE_CARD_CAPTION` («Переход на младший тариф — после окончания оплаченного срока»). Тем самым фальсифицирована и прохибиция плана 05-13, помеченная `status: resolved, verification: test`: «MUST NOT брать деньги за тариф, который не будет выдан: решение ПРОДАВАТЬ и решение ВЫДАТЬ обязаны приниматься по одним и тем же входным данным» — входные данные демонстрируемо разные. Почему суита остаётся зелёной (1681 passed) и останется: КАЖДЫЙ тест обеих стадий держит состояние подписки НЕИЗМЕННЫМ между продажей и подтверждением; ни один тест не меняет состояние МЕЖДУ двумя стадиями, а именно там дефект и живёт."
    artifacts:
      - path: "app/services/payment_service.py"
        issue: "Строка 579: `subscription.expires_at = next_expiry(...)` исполняется БЕЗУСЛОВНО и ВЫШЕ решения о плане (`:584`). Отвергнутый платёж младшего тарифа покупает полный месяц действующего старшего. Докстринг `:554-557` называет безусловность решением («деньги списаны, „не выдать ничего“ — худший из исходов»), но НЕ называет её цену: месяц Pro за цену Basic, повторяемый без потолка"
      - path: "app/services/payment_service.py"
        issue: "Строка 577: `period_is_live` считается от строки БД в момент ПРИХОДА УВЕДОМЛЕНИЯ. Порядок относительно `next_expiry` верен и закреплён AST-тестом — но ловушка второго порядка (относительно момента ПРОДАЖИ) не адресована ничем: разрешение сделки на `Payment` не записывается"
      - path: "app/pages/billing.py"
        issue: "Строки 319-325: гард считает `subscription_is_live` от снимка шелла в момент нажатия. Ответ гарда нигде не сохраняется — `create_payment` (`payment_service.py:167-180`) полей о разрешённом переходе не имеет"
      - path: "app/services/payment_service.py"
        issue: "`create_payment` не ограничивает число одновременных `pending`-платежей одного пользователя: N нажатий = N платежей, каждый из которых на стадии применения добавит месяц"
      - path: "app/application/billing/plan_switch.py"
        issue: "Строка 48 докстринга: «У ПРАВИЛА ДВЕ СТАДИИ, И ОБЕ ЧИТАЮТ ЭТУ ФУНКЦИЮ С ОДНИМ НАБОРОМ ВЕЛИЧИН». Совпадает ФОРМА аргументов, а не значения. Утверждение, которого код не исполняет, — тот же класс, что план 05-13 брался устранить прохибицией «MUST NOT объявлять докстрингом правило, которого исполняемый код не исполняет»"
      - path: "tests/test_pages/test_billing_payment_errors.py"
        issue: "Ни одна из регрессий обеих стадий не МЕНЯЕТ состояние подписки между продажей и подтверждением. `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage` (`:899`) закрепляет `expires_at > now + 45 дней` — то есть ЗАКРЕПЛЯЕТ покупку второго месяца старшего тарифа платежом младшего как желаемое поведение"
    missing:
      - "Решение владельца по существу: чем кончается платёж, разрешённый при одном состоянии подписки и пришедший при другом. Три названных варианта, любой допустим, недопустим текущий (взять деньги и выдать не то, что продано): (а) записать ответ гарда НА ПЛАТЁЖ (колонка `payments.switch_authorized`) и читать факт, а не пересчитывать предикат по изменившейся БД; (б) считать оба решения по признаку, снятому в момент ПРОДАЖИ; (в) отвергнутому платежу выдавать дни УПЛАЧЕННОГО тарифа, а не действующего"
      - "Отдельное решение по безусловному сдвигу срока: отвергнутый платёж младшего тарифа сегодня покупает месяц старшего, и это не названо ни ценой варианта `upgrade-only` (там названа только бесплатная отдача лимитов при повышении), ни ценой варианта `apply-after-expiry`. При ценах 1490/4900 ₽ каждый такой платёж стоит кассе 3410 ₽, число повторов не ограничено ничем"
      - "Регрессия, МЕНЯЮЩАЯ состояние подписки МЕЖДУ продажей и подтверждением — единственный класс, которого нет ни в одном из 1681 теста. Обязана падать на текущем коде. Минимум два случая: (1) без подписки заводятся платежи `basic`+`pro`, `pro` подтверждается первым — итоговый срок обязан соответствовать УПЛАЧЕННОМУ, а не давать месяц Pro за 1490 ₽; (2) истёкший `pro` → продан `basic` → оплачен `pro` → оплачен `basic` — `basic` обязан быть выдан либо не продан"
      - "Потолок на число одновременных `pending`-подписочных платежей одного пользователя, либо явно записанное решение, почему его нет"
      - "Приведение докстринга `plan_switch.py:48` к исполняемому коду: «ОДИН НАБОР ВЕЛИЧИН» обязан быть либо достигнут, либо снят и заменён названием окна между стадиями"
  - truth: "Запись реестра раундов ревью в `.planning/STATE.md` пережила бы четвёртую перезапись `05-REVIEW.md` без единой правки: ни одно её утверждение не перестанет быть верным от появления раунда 4 (must_have №2 плана 05-14)"
    status: partial
    reason: "Механизм адресации починен и держится — это главное, и это проверено: раунды адресуются коммитами, `git log` отдаёт полный перечень, запись НАМЕРЕННО не утверждает, какой раунд лежит на диске. Но в той же строке остался счётчик ВЕЛИЧИНЫ: «Имя `05-REVIEW.md` принадлежит ТРЁМ разным документам». Раунд 4 закоммичен (`5da8e82`, `round: 4`, итог `critical: 1, warning: 4, info: 2`) — документов теперь ЧЕТЫРЕ, и утверждение недосчитывает. План 05-14 назвал этот способ отказа сам, своим же must_have №5: «Константа величины устарела бы от первой перекрёстной ссылки и ещё раз от раунда 4 — то есть отказала бы ровно тем способом, который этот план чинит в указателе». Инвариант квалификации номеров план так и построил (проверено: 4 = 4), а числительное в соседнем предложении оставил константой. Тяжесть НИЗКАЯ и качественно ниже, чем у гэпа 2 раунда 3: там указатель отправлял читателя в НЕ ТОТ документ, здесь маршрутизация верна, устарел только счёт. Отдельно: находки раунда 4 в STATE.md отсутствуют — это НЕ вменяется плану 05-14 (ревью раунда 4 прошло в 06:10, через 35 минут после его завершения), а переходит следующему раунду бухгалтерии."
    artifacts:
      - path: ".planning/STATE.md"
        issue: "Строка 92: «Имя `05-REVIEW.md` принадлежит ТРЁМ разным документам» — документов четыре (`4989f7a`, `1d29360`, `164d210`, `5da8e82`)"
      - path: ".planning/STATE.md"
        issue: "Строка 92, хвост: «ни одна из отложенных не входит в два `status: failed` гэпа `05-VERIFICATION.md`» — у раунда 3 `status: failed` нёс ОДИН гэп, второй был `partial`. Ещё одна константа величины в той же строке"
    missing:
      - "Замена числительного на формулировку, не зависящую от числа раундов (например «нескольким разным документам — перечень отдаёт `git log`»), в строке 92 STATE.md; та же правка у оборота «два `status: failed` гэпа»"
      - "Распоряжения по находкам ревью раунда 4 (CR-01, WR-01…WR-04, IN-01, IN-02) — работа СЛЕДУЮЩЕГО раунда бухгалтерии, не долг плана 05-14"
deferred: []
behavior_unverified_items:
  - truth: "Раздел тарифов пригоден к использованию на мобильных ширинах (критерий 4 ROADMAP)"
    test: "Открыть /billing в браузере на ширине 375px с реальными данными: три карточки планов, четыре метра, история платежей длиннее экрана, блок пакетов"
    expected: "Карточки планов складываются в одну колонку; подписи колонок истории ([data-cell-label]) едут вместе со значениями; ни один блок не требует горизонтальной прокрутки; кнопки оплаты нажимаемы пальцем (порог проекта 44px)"
    why_human: "Браузерного/e2e-харнесса в проекте нет (`playwright`/`selenium`/`puppeteer` в `pyproject.toml` отсутствуют). Все относящиеся тесты читают CSS и HTML как ТЕКСТ и проверяют ОБЪЯВЛЕНИЕ правила, а не отрисовку. Файлы разметки и стилей с раунда 2 не менялись ни на строку (git: планы 05-13/05-14 тронули `plan_switch.py`, `subscription_period.py`, `pages/billing.py`, `payment_service.py`, три файла тестов и `.planning`; `app/templates/` и `app/static/` — ни одного файла), поэтому пункт переносится без изменений четвёртый раунд подряд."
human_verification:
  - test: "Открыть /billing на 375px и пройти раздел глазами (см. behavior_unverified_items выше)"
    expected: "Одна колонка, никакой горизонтальной прокрутки, кнопки нажимаемы"
    why_human: "Браузерного харнесса в проекте нет"
  - test: "Настоящий платёж в тестовом магазине ЮKassa: форма → confirmation_url → возврат → приход уведомления → сдвинутый срок на /billing"
    expected: "Срок НЕ двигается до прихода уведомления и двигается после него"
    why_human: "Боевого доступа к API ЮKassa у исполнителя нет, всё покрытие на моках. Backstop-пункт must_haves планов 05-01, 05-06, 05-11 и 05-13. При решении D-26 недостижим на проде (колонок `payments.kind`/`plan` там нет)."
  - test: "Первое настоящее уведомление ЮKassa после выката проходит гард источника"
    expected: "Боевой nginx действительно проставляет X-Real-IP именно на маршруте вебхука, и настоящее уведомление получает 200, а не 403"
    why_human: "Backstop-пункт must_haves плана 05-07. Поведение боевого nginx на боевом маршруте кодом репозитория не проверяется."
  - test: "Отменённый платёж получает статус «отклонён» на экране истории"
    expected: "Строка платежа показывает бейдж «отклонён», а не «в обработке»"
    why_human: "Заблокирован: владелец не подтвердил подписку на событие `payment.canceled` в кабинете ЮKassa (D-27). Состав событий задаётся вне репозитория."
  - test: "Пользователь, вернувшийся с ЮKassa до прихода вебхука, понимает, что платёж ещё в обработке"
    expected: "Человек читает строку истории со статусом «в обработке» как «деньги в обработке», а не как «оплата не прошла»"
    why_human: "Backstop-пункт must_haves планов 05-04 и 05-10: понятность формулировки — суждение человека."
  - test: "Сообщение об отказе оплаты («ЮKassa не создала платёж») прочитывается как «попробуйте ещё раз»"
    expected: "Не читается как «с вас списали и не зачли»"
    why_human: "Backstop-пункт must_haves плана 05-10: понятность формулировки судится человеком."
  - test: "Человек с истёкшим оплаченным сроком читает экран раздела как согласованный с тем, что произойдёт после оплаты"
    expected: "Сочетание `DOWNGRADE_CARD_CAPTION` («переход на младший тариф — после окончания оплаченного срока»), текста отказа `downgrade` и фактического исхода читается как одно обещание"
    why_human: "Backstop-пункт must_haves плана 05-13. ⚠️ Читается ПОСЛЕ закрытия гэпа 1 настоящего отчёта: сегодня обещание карточки нарушается на пути WR-01 раунда 4 (продано на истёкшем сроке, применено на ожившем)."
  - test: "Владелец риска `WR-07 (раунд 3)` — монтирование `/var/run/docker.sock` в контейнер `web`, терминирующий вебхук ЮKassa — назван поимённо"
    expected: "В `.planning/STATE.md:93` вместо «⟨не назначен — назначает владелец проекта⟩» стоит имя, либо принято одно из двух распоряжений (вынос управления контейнерами в отдельный воркер / фильтрующий прокси перед сокетом)"
    why_human: "must_have №8 плана 05-14 требовал ВЛАДЕЛЬЦА и распоряжение. Распоряжение записано и проверено, владелец — решение человека, кодом и планом не назначается. Приоритет: root-equivalent доступ к хосту у процесса, разбирающего JSON вебхука, при аутентификации по заголовку прокси, отключаемой одной переменной окружения."
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, `status: unresolved` в планах 05-11 и 05-13): «MUST NOT превращать оплаченный остаток старшего тарифа в дни младшего без объявленного правила и без слова пользователю до нажатия кнопки» (BILL-05, fairness)"
    expected: "Владелец решает, считать ли прохибицию соблюдённой после планов 05-11 и 05-13"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): в НАЗВАННОМ прохибицией направлении соблюдена — действующий старший тариф младшим платежом не снимается (`test_a_live_period_still_keeps_the_higher_plan`, зелёный), и путь истёкшего срока, открывшийся в раунде 3, закрыт планом 05-13. Но обратное направление, вскрытое в этом раунде, прохибицией не покрыто и остаётся открытым: оплаченный младший тариф превращается в дни СТАРШЕГО, без слова пользователю и без названной цены (гэп 1)."
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, `status: unresolved` в планах 05-11 и 05-13): «MUST NOT показывать человеку строку истории с одним тарифом рядом с бейджем другого действующего тарифа без единого слова о том, что оплата продлила срок, а не сменила тариф» (BILL-06, transparency)"
    expected: "Владелец решает, нужен ли интерфейсный ответ на расхождение уплаченного и действующего тарифа — и в каком объёме"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): НАРУШЕНА. Путь истёкшего срока (раунд 3) закрыт, но воспроизведение этого раунда даёт ту же картину напрямую: в истории три строки «Basic · подписка», в бейдже — «Pro», ни одна строка раздела этого не объясняет. Единственный след — `subscription_plan_preserved` в журнале, видимый только нам. Планы 05-11 и 05-13 сознательно вынесли интерфейсную работу за границу объёма."
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, `status: unresolved` в плане 05-13): «MUST NOT запирать пользователя в тарифе на всё время жизни аккаунта — а если запирать, то сказать об этом теми же словами, которыми сегодня обещано обратное» (BILL-05, autonomy)"
    expected: "Владелец подтверждает, что вариант `apply-after-expiry` снял запирание в объёме, который он считает достаточным"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): в основном пути СНЯТА — истёкший срок открывает переход в любую сторону, и обещание `DOWNGRADE_CARD_CAPTION` исполняется. Остаются два узких пути запирания, оба — предупреждения раунда 4: (1) путь WR-01 (сделка разрешена на истёкшем сроке, применена на ожившем); (2) путь WR-03 — план без ранга, записанный в подписку на истёкшем сроке, после оживления периода отвергает ЛЮБОЙ переход."
---

# Phase 5: Тарифы — Verification Report (раунд 4)

**Phase Goal:** Пользователь понимает, сколько ресурса тарифа он израсходовал, за что платил и может продлить подписку не выходя из раздела.
**Verified:** 2026-08-17T06:09:30Z
**Status:** gaps_found
**Re-verification:** Да — четвёртый раунд, после планов 05-13 (гэп 1) и 05-14 (гэп 2)

## Прямой ответ на поставленный вопрос

Задание требует четырёх суждений. Они даются здесь, до таблиц.

### 1. Закрыт ли гэп 1 раунда 3 планом 05-13? — ДА, ЗАКРЫТ ПОЛНОСТЬЮ.

Проверено по коду и прогоном, а не по SUMMARY. Все четыре обещанных механизма
на месте и работают:

* `subscription_is_live` — единственное объявление, в
  `app/application/billing/subscription_period.py:53`. Приватной функции
  `_subscription_is_live` в `app/pages/billing.py` больше нет (грепом — ноль
  вхождений).
* `switch_is_refused` принимает `period_is_live` **обязательным keyword-only**
  без умолчания (`plan_switch.py:29`). Спросить правило, не подав признак,
  физически нельзя — `test_the_rule_cannot_be_asked_without_the_liveness_term`
  ловит `TypeError`.
* Признак снимается **до** сдвига срока: `payment_service.py:577` стоит перед
  `:579`, и порядок держится AST-тестом
  `test_the_liveness_is_sampled_before_the_date_moves`, который сравнивает
  индексы двух присваиваний в разобранном теле функции, а не ищет подстроку.
* Регрессия стадии применения на истёкшем сроке существует и зелёная:
  `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage`
  проверяет **и** `plan == 'basic'`, **и** обе границы срока (27 < дней < 45),
  то есть отличает «выдан от сегодня» от «не выдан вовсе» и от «посчитан от
  прошедшей даты».

Прогон трёх относящихся файлов в этой верификации: **92 passed**. Сценарий
раунда 3, воспроизведённый тогда вручную (оплачен `basic`, выдан `pro`), сегодня
даёт `basic`. Гэп закрыт.

### 2. Закрыт ли гэп 2 раунда 3 планом 05-14? — ДА ПО СУЩЕСТВУ, с одним оставшимся числительным.

Оба пункта `missing` раунда 3 закрыты, и закрыты лучше, чем требовалось: вместо
правки указателя на «раунд 3 — на диске» реестр перестроен так, что **вопрос
«какой раунд на диске» в нём не задаётся вовсе**. Каждый раунд адресуется своим
неизменяемым коммитом (проверил все три через `git show` — отдают заявленные
документы с заявленными итогами), полный перечень отдаёт названная в записи
команда `git log`. Находки раунда 3 записаны, каждый номер квалифицирован
раундом, и требование квалификации проверяется инвариантом, а не константой:
вхождений `WR-07` — 4, квалифицированных — 4.

Оставшееся — одно слово. «Имя `05-REVIEW.md` принадлежит **ТРЁМ** разным
документам»: раунд 4 закоммичен (`5da8e82`), документов четыре. План 05-14 сам
назвал этот способ отказа своим must_have №5 — «константа величины… отказала бы
ровно тем способом, который этот план чинит» — и оставил константу величины в
соседнем предложении. Это `status: partial`, не блокер: маршрутизация верна,
устарел только счёт.

### 3. Является ли CR-01 ревью раунда 4 новым гэпом? — ДА. Это блокер, и я называю это прямо.

Задание просило взвесить самостоятельно и сказать явно. Взвешено и подтверждено
**независимым воспроизведением**, без единой правки `app/`, через настоящий
обработчик формы и настоящий `handle_webhook`:

```
РЕЗУЛЬТАТ: план='pro', срок через 121 дней, уплачено 3×1490 + 1×4900 = 9370 ₽
```

Четыре месяца Pro стоят 19 600 ₽. Заплачено 9370 ₽. Повторов не ограничивает
ничто: потолка на число одновременных `pending`-платежей в `create_payment` нет,
а гард формы пропускает любой план, пока строки подписки не существует. Гонки не
требуется — достаточно открыть форму оплаты в нескольких вкладках.

**Почему это гэп, а не принятая цена варианта.** Три довода, каждый проверяемый:

1. **Фальсифицирован явный must_have плана 05-13** №1: «не существует такого
   `expires_at`, при котором гард формы ПРОПУСКАЕТ платёж за переход, а стадия
   применения этот переход НЕ ВЫДАЁТ». Такой случай существует и воспроизведён.
   План проверял это свойство, держа состояние подписки **неизменным** между
   двумя стадиями; дефект живёт ровно там, где состояние меняется.
2. **Фальсифицирована прохибиция, помеченная `resolved` / `verification: test`**:
   «решение ПРОДАВАТЬ и решение ВЫДАТЬ обязаны приниматься по одним и тем же
   входным данным». Данные демонстрируемо разные: намерение читает снимок шелла
   в момент нажатия, применение — перечитанную строку в момент уведомления, и
   ответ гарда на платеже не сохраняется вовсе.
3. **Ни один чекпойнт этого владельцу не предъявлял.** `upgrade-only` (05-10)
   назвал свою цену — бесплатные лимиты старшего тарифа на оставшиеся дни
   младшего. `apply-after-expiry` (05-13) назвал свою — переход открывается
   после окончания срока. Ни один не сказал: «отвергнутый платёж младшего тарифа
   покупает полный месяц старшего, и это повторяемо без потолка».

**Обратная сторона того же корня бьёт по пользователю, а не по кассе** (WR-01
раунда 4, тот же механизм, проверен на коде): истёкшая подписка `pro` →
пользователь заводит платёж `basic` (гард пропускает — так обещает и карточка:
«Переход на младший тариф — после окончания оплаченного срока») → сначала
оплачивает `pro`, срок оживает → затем оплачивает `basic` → деньги взяты,
`basic` не выдан. Это дословно тот исход, который раунд 3 назвал блокером
фазы. Симметрия исключает толкование «это решение, а не дефект»: одно и то же
окно между стадиями отдаёт деньги то кассе, то пользователю, в зависимости от
порядка оплат.

**Формулировка корня одной фразой, для следующего плана.** Раунд 3: план 05-11
объединил ОБЪЯВЛЕНИЕ правила, а не его ВХОДНЫЕ ДАННЫЕ. Раунд 4: план 05-13
объединил ФОРМУ входных данных, а не МОМЕНТ их снятия. Третья итерация той же
ошибки будет, если правку снова сделать на уровне сигнатуры: лечится не подпись,
а то, что решение о сделке нигде не записано.

### 4. Можно ли считать критерии и требования BILL выполненными? — КРИТЕРИИ ДА (три из четырёх, четвёртый — к человеку), ТРЕБОВАНИЯ НЕТ.

Все четыре критерия ROADMAP держатся, и новый дефект ни одного из них не
называет: срок двигается (SC1), четыре оси рисуются (SC2), история показывает
дату, сумму и статус (SC3), мобильная пригодность объявлена, но ничем
проверяемым не отрисована (SC4). Как в раундах 2 и 3, дефект лежит **вне четырёх
критериев** и, как в раундах 2 и 3, остаётся **блокером фазы**: по дереву решений
(правило 1 — FAILED-истина) фаза получает `gaps_found` независимо от состояния
критериев.

Отметку `Complete` требованиям возвращать **нельзя**: BILL-05 несёт путь гэпа 1 и
три неразрешённые прохибиции; BILL-06 рисует четыре оси по значению
`subscription.plan`, расходящемуся с уплаченным на этом пути; BILL-07 упирается в
D-27. В `.planning/REQUIREMENTS.md` все три стоят `In Progress` — проверено, и это
верно. Преждевременная отметка уже ставилась и отзывалась однажды (`2124d1e`).

## Goal Achievement

### Observable Truths — критерии ROADMAP

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| SC1 | Пользователь может продлить текущую подписку из раздела и сразу увидеть обновлённый срок | ✓ VERIFIED (тестовый стенд) | Регрессионная проверка: цепочка не менялась с раунда 3 (git — планы 05-13/05-14 тронули `plan_switch.py`, `subscription_period.py`, `pages/billing.py`, `payment_service.py`, три файла тестов и `.planning`; `app/templates/` — ни одного файла). Формы `plan_card.html:94` и `balance.html:120` → `POST /billing/subscribe` → `create_payment(kind='subscription')` → 302 → вебхук → `_extend_subscription` → `Subscription.expires_at`. Продление СВОЕГО тарифа новым признаком срока не задето: `target == current` → отказа нет; доказано `test_renewing_the_own_plan_still_moves_the_date_at_the_apply_stage` и `test_an_expired_period_still_renews_the_own_plan` (оба зелёные). ⚠️ В проде не выполнен (D-26, база на ревизии 0012). |
| SC2 | Пользователь видит потребление и остаток по всем ЧЕТЫРЁМ осям | ✓ VERIFIED (механика) | `AXIS_ORDER = (ads, groups, sends, accounts)` (`plan_usage.py:64`) → `plan_axes` (`pages/billing.py:9,168`) → контекст `usage` (`:229`) → цикл `usage_meter` (`balance.html:132`). Витрина цела. ⚠️ ЗНАЧЕНИЕ, которое её кормит, расходится с уплаченным на пути гэпа 1: воспроизведение показало три оплаченных `basic` при бейдже и лимитах `pro`. Как в раундах 2 и 3, дефект отнесён к пути ЗАПИСИ, а не к витрине. ⚠️ WR-06 раунда 3 (план вне `PLAN_LIMITS` рисует все оси «без ограничений») по-прежнему открыт — предупреждение. |
| SC3 | Пользователь видит историю платежей с датой, суммой и статусом | ✓ VERIFIED (тестовый стенд) | `count_payments` + `get_payment_history` (`pages/billing.py:182-183`; `billing_service.py:208`, `:239`) → `payment_row` (`balance.html:224`). Не менялось с раунда 2. ⚠️ Статус «отклонён» в проде не появится (D-27). |
| SC4 | Раздел пригоден к использованию на мобильных ширинах | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `[data-plan-cta] .btn { min-height: 44px }` (`app.css:1190`), атрибут стоит на формах оплаты (`balance.html:120`, `:189`, `plan_card.html:94`); `[data-cell-label]` в истории присутствует. Складывание объявлено. Браузера в суите нет — отрисовка, переполнение и попадание пальцем непроверяемы грепом. Разметка и стили с раунда 2 не изменились ни на строку, пункт переносится без изменений четвёртый раунд. |

**Score критериев:** 3/4 выполнены (1 присутствует, поведенчески не проверен)

### Observable Truths — гэпы раунда 3

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| Г1 (р3) | Понижение после окончания оплаченного срока принимается И ПРИМЕНЯЕТСЯ | ✓ VERIFIED — **гэп закрыт** | Все четыре механизма проверены по коду (см. «Прямой ответ», п. 1). Сценарий раунда 3 даёт `basic`, а не `pro`. 92 passed на трёх относящихся файлах. Ветка истёкшего срока на стадии применения теперь покрыта четырьмя тестами (`:1145`, `:1176`, `:1202`, `:1225`), включая ПАРНЫЙ тест живого периода — то есть снятие отказа «везде» тоже поймается. |
| Г2 (р3) | `.planning/STATE.md` не содержит ложных утверждений о том, где искать находки ревью | ⚠️ ЗАКРЫТ ПО СУЩЕСТВУ | Оба пункта `missing` закрыты, механизм адресации перестроен на коммиты + `git log` и проверен `git show` по всем трём хешам. Осталась одна константа величины («ТРЁМ»), устаревшая от раунда 4 — гэп 2 настоящего отчёта, `status: partial`. |

### Observable Truths — must_haves плана 05-13

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| 1 | «Продали, но не выдали» недостижимо ни при каком состоянии подписки | ✗ FAILED — **блокер** | Воспроизведено независимо через настоящий обработчик формы: гард пропустил платёж `basic` (подписки не было), стадия применения его не выдала (подписка уже `pro`). Свойство проверялось при НЕИЗМЕННОМ состоянии подписки; дефект живёт там, где состояние меняется между стадиями. Подробности — в `gaps[0]` |
| 2 | Признак живости — ОБЯЗАТЕЛЬНЫЙ keyword-only вход правила, невозможность закреплена тестом на `TypeError` | ✓ VERIFIED | `plan_switch.py:29` — `*, period_is_live: bool` без умолчания; `test_the_rule_cannot_be_asked_without_the_liveness_term` ловит `TypeError` на `switch_is_refused("pro","basic")`. Зелёный |
| 3 | Живость считается ОТ `expires_at` ДО его сдвига, порядок закреплён AST-тестом | ✓ VERIFIED | `payment_service.py:577` перед `:579`. `test_the_liveness_is_sampled_before_the_date_moves` разбирает тело в AST, вычёркивает докстринг из перечня операторов и сравнивает индексы двух присваиваний по ИМЕНИ ВЫЗЫВАЕМОЙ функции, а не подстрокой — перестановка операторов его ломает, упоминание имён в комментарии — нет. Зелёный |
| 4 | Признак объявлен ОДИН раз; приватной копии в `app/pages/billing.py` больше нет | ⚠️ ЧАСТИЧНО | Названный объект достигнут: приватной функции `_subscription_is_live` нет (греп — ноль), оба вызова читают объявление из `subscription_period.py`. НО рукописный эквивалент остался в том же обработчике: `expired` (`pages/billing.py:195-198`) — ручное отрицание того же предиката по той же величине, со своим снимком часов, в 12 строках от `live = subscription_is_live(...)` (`:210`). Комментарий `:100-107` при этом утверждает «Своей копии ни того, ни другого этот файл не держит». Расхождение сегодня — один граничный момент `expires_at == now` (обе величины `False`); цена — комментарий, который следующий читатель примет за гарантию. WR-02 раунда 4, предупреждение |
| 5 | Регрессия стадии ПРИМЕНЕНИЯ на ИСТЁКШЕМ сроке существует и на коде ДО правки падала | ✓ VERIFIED | `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage` существует, содержателен (проверяет план И обе границы срока) и зелёный. Fail-first подтверждён порядком коммитов: `b1b8de7 test(05-13): add failing regressions…` предшествует `94ca261 feat(05-13): make the liveness term a mandatory input…` — тесты закоммичены отдельным коммитом ДО правки `app/` |
| 6 | ТРИ докстринга совпадают с исполняемым кодом | ⚠️ ЧАСТИЧНО | Все три названных докстринга приведены к коду и закреплены проверками: `_extend_subscription` (`payment_service.py:435-486`, закреплён `test_the_switch_semantics_are_named_in_the_place_that_moves_the_date` — теперь требует и `истёк`, и `subscription_is_live`), абзац стадии применения в `plan_switch.py`, докстринг `_apply_extension`. НО в том же обновлённом докстринге появилось НОВОЕ утверждение, которого код не исполняет: `plan_switch.py:48` — «ОБЕ ЧИТАЮТ ЭТУ ФУНКЦИЮ С ОДНИМ НАБОРОМ ВЕЛИЧИН». Совпадает форма аргументов, а не значения (WR-01 раунда 4). Тот же класс, что план брался устранить |
| 7 | Выбранный чекпойнтом вариант исполнен ЦЕЛИКОМ; смешанного состояния «продажа разрешена — применение отказано» не осталось ни в одной ветке | ✗ FAILED — **блокер** | Смешанное состояние осталось — не внутри одного состояния подписки, а МЕЖДУ моментами: продажа разрешена при одном `expires_at`, применение отказано при другом. Дословно тот исход, который must_have называет недопустимым. Подробности — в `gaps[0]` |
| 8 | Всё, что закрыл план 05-11, остаётся закрытым | ✓ VERIFIED | 92 passed на трёх файлах, включая `test_a_live_period_still_keeps_the_higher_plan` (новый парный), `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage`, `test_a_confirmed_higher_plan_is_applied_at_the_apply_stage`, `test_an_upgrade_does_not_burn_the_paid_remainder`, `test_renewing_the_own_plan_still_moves_the_date_at_the_apply_stage`, `test_the_first_subscription_still_takes_its_plan_from_the_payment`, `test_an_unranked_plan_is_refused_from_either_side` |
| 9 | C2: интерфейсный ответ согласован с выбранным вариантом — обещание перехода после окончания срока исполнено, а не отозвано | ✓ VERIFIED | `DOWNGRADE_CARD_CAPTION` (`pages/billing.py:83`) обещает «Переход на младший тариф — после окончания оплаченного срока: оплаченные дни не сгорают»; вариант `apply-after-expiry` это обещание исполняет — доказано `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage`. Шесть мест отзыва обещания не тронуты, что верно. ⚠️ На пути WR-01 раунда 4 обещание нарушается — учтено гэпом 1, здесь не дублируется |

### Observable Truths — must_haves плана 05-14

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| 1 | Каждый раунд адресуется собственным неизменяемым коммитом; перечень отдаёт `git log`, а не утверждение автора | ✓ VERIFIED | STATE.md:92. Проверено не чтением, а исполнением: `git show 4989f7a:…` → `critical: 2, warning: 10, info: 6`; `1d29360` → `critical: 2, warning: 9, info: 7`; `164d210` → `round: 3, critical: 1, warning: 7, info: 5`. Все три отдают заявленные документы. Команда `git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md` названа в записи и работает |
| 2 | Запись пережила бы четвёртую перезапись без единой правки | ✗ FAILED (низкая тяжесть) | «Имя `05-REVIEW.md` принадлежит ТРЁМ разным документам» — с коммитом `5da8e82` (`round: 4`) их четыре. Второй оборот той же формы: «два `status: failed` гэпа» — у раунда 3 `failed` был один. Гэп 2 настоящего отчёта, `status: partial` |
| 3 | Находки раунда 3 записаны в `.planning/STATE.md` | ✓ VERIFIED | STATE.md:95 — отдельная 🟡 запись: CR-01 (закрыт планом 05-13), WR-01, WR-06, WR-07, WR-02…WR-05, IN-01…IN-05. Каждая с распоряжением. Плюс STATE.md:93 — отдельная 🔴 запись по WR-07 |
| 4 | Каждый номер раунда 3 назван вместе со своим раундом | ✓ VERIFIED | Все номера в записи :95 несут скобку `(раунд 3)`; в сводной записи :92 номера раундов 1 и 2 несут `раунда 1` / `(раунд 2)` |
| 5 | Требование квалификации проверяется ИНВАРИАНТОМ, а не константой счёта | ✓ VERIFIED | Исполнено: вхождений `WR-07` — 4, вхождений `WR-07 (раунд N)` — 4 (по два на раунды 2 и 3). Обе записи присутствуют поимённо. Инвариант переживает раунд 4 — в отличие от числительного в соседнем предложении (истина 2) |
| 6 | Запись о CR-01 раунда 2 больше не читается как полное закрытие | ✓ VERIFIED | STATE.md:123 — маркер сменён с ✓ на ⚠️, текст: «Закрыто планом 05-11 НАПОЛОВИНУ, завершено планом 05-13»; названы и половинность, и фальсифицированный седьмой must_have, и внесённая регрессия |
| 7 | Блокер денежного пути раунда 3 записан со ссылкой на закрывший план и доказавшие регрессии | ✓ VERIFIED | STATE.md:124 — план 05-13 назван, названы `test_the_rule_cannot_be_asked_without_the_liveness_term`, `test_the_liveness_is_sampled_before_the_date_moves`, `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage`, `test_the_expired_period_writes_no_preserved_plan_warning`, `test_a_naive_now_does_not_break_the_liveness_term`; названо и отклонение предложения ревью (объявление признака не в `plan_switch.py`) с причиной. Слова «исправлено» в записи нет |
| 8 | `WR-07 (раунд 3)` получила ВЛАДЕЛЬЦА и распоряжение | ⚠️ ЧАСТИЧНО | STATE.md:93 — 🔴 запись, распоряжение записано в двух вариантах (вынос управления контейнерами в отдельный воркер / фильтрующий прокси перед сокетом) плюс третий выход (принятый риск по образцу R-03-09). Названо, что сегодняшний режим — «принято никем конкретно». Владелец: «⟨не назначен — назначает владелец проекта⟩». Распоряжение есть, владельца нет — назначение человека, планом недостижимо. Вынесено в human_verification |
| 9 | Решение чекпойнта 05-13 записано решением фазы в §Decisions | ✓ VERIFIED | STATE.md:82 — вариант A `apply-after-expiry` назван идентификатором, названа невзятая ветка `refuse-always`, следствие для пользователя записано одной фразой, назван обязательный keyword-only вход `period_is_live` и единственное объявление `subscription_is_live` |
| 10 | Артефакт записан В ФАЙЛ, а не обещан во frontmatter | ✓ VERIFIED | `.planning/phases/05-tarify/05-STATE-PATCH-R3.md` — 46 431 байт на диске, закоммичен `0861e8d` вместе с применённым `.planning/STATE.md`. Файл 05-12 (`05-STATE-PATCH.md`, 19 595 байт) не перезаписан — оба существуют |
| 11 | «Current Position» и frontmatter `progress` не спорят ни друг с другом, ни с ROADMAP | ✓ VERIFIED | `progress: 65/66, 67%` ↔ «Progress: 65/66 планов (98%) — Фазы 1–4 из 6 завершены (67% фаз)» ↔ ROADMAP «14/14 plans executed», фаза 5 не отмечена `[x]`. Арифметика сходится: 13+15+12+12 фаз 1–4 = 52, +13 исполненных планов фазы 5 = 65 из 66 |

### Observable Truths — новое (ревью раунда 4)

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| — | Учтено истинами 1 и 7 плана 05-13 (гэп 1) и истиной 4 (WR-02) — отдельных строк не заводится, чтобы не считать один корень дважды | — | CR-01 и WR-01 раунда 4 — один корень (окно между стадиями), сведён в `gaps[0]`. WR-02 и WR-03 раунда 4 — предупреждения, см. таблицу анти-паттернов |

**Score:** 19/26 истин выполнены (1 присутствует поведенчески не проверенной; 3 провалены; 3 частично)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `app/application/billing/subscription_period.py` | `subscription_is_live` — единственное объявление признака живости, рядом с `next_expiry` | ✓ VERIFIED | Функция существует (`:53`), содержательна, оба операнда через `normalize_utc`, сравнение строгое. Докстринг называет ловушку порядка и тест, который её держит. Причина размещения именно здесь (инвариант единственного импорта у `plan_switch.py`) записана и проверяема |
| `app/application/billing/plan_switch.py` | `switch_is_refused` с ОБЯЗАТЕЛЬНЫМ keyword-only `period_is_live` | ⚠️ ЧАСТИЧНО | Сигнатура и короткое замыкание по сроку на месте и работают. Докстринг содержит утверждение, которого код не исполняет (`:48`, «ОДИН НАБОР ВЕЛИЧИН» — WR-01 раунда 4). Отдельно: ветка `if not period_is_live` короткозамыкает и fail-closed для плана без ранга — проверено исполнением: `switch_is_refused('pro','platinum',period_is_live=False)` → `False` (WR-03 раунда 4) |
| `app/services/payment_service.py` | `_apply_extension` снимает признак ДО сдвига срока; два докстринга, совпадающих с кодом | ⚠️ ЧАСТИЧНО | Порядок верен и закреплён AST-тестом; докстринги приведены к коду и закреплены проверками. НО сдвиг срока безусловен и стоит ВЫШЕ решения о плане — отвергнутый платёж покупает месяц действующего тарифа (гэп 1); решение гарда о сделке нигде не сохраняется |
| `app/pages/billing.py` | Своей копии признака живости не держит, оба вызова подают его аргументом | ⚠️ ЧАСТИЧНО | Приватной функции нет, оба вызова (`:210`, `:322`) подают признак аргументом. Рукописный эквивалент `expired` (`:195-198`) остался, комментарий `:100-107` его существование отрицает (WR-02 раунда 4) |
| `tests/test_application/test_subscription_period.py` | Таблица решений признака живости, включая границу «ровно сейчас» и оба диалекта времени | ✓ VERIFIED | 5 новых тестов признака (`:102`–`:154`): отсутствие подписки, параметризованная таблица дат, naive/aware одного момента, граница `expires_at == now`, naive-`now`. Все зелёные |
| `tests/test_application/test_plan_switch.py` | Обязательность входа: правило нельзя спросить, не сказав, есть ли что защищать | ✓ VERIFIED | 9 тестов, все зелёные, включая `test_the_rule_cannot_be_asked_without_the_liveness_term` (`TypeError`) и `test_an_expired_period_refuses_nothing` |
| `tests/test_pages/test_billing_payment_errors.py` | Регрессии стадии ПРИМЕНЕНИЯ на ИСТЁКШЕМ сроке и тест порядка двух действий | ⚠️ ЧАСТИЧНО | Четыре новые регрессии истёкшего срока + AST-тест порядка существуют и зелёные, включая ПАРНЫЙ тест живого периода. НО ни одна регрессия файла не МЕНЯЕТ состояние подписки между продажей и подтверждением — класс, в котором живёт гэп 1, не покрыт ни одним из 1681 теста |
| `.planning/STATE.md` | Реестр раундов по коммитам, распоряжения раунда 3, правки §Deferred Items, 🔴 запись о сокете Docker, запись решения в §Decisions | ⚠️ ЧАСТИЧНО | Всё перечисленное записано и проверено поимённо. Осталась одна константа величины («ТРЁМ»), устаревшая от раунда 4 |
| `.planning/phases/05-tarify/05-STATE-PATCH-R3.md` | Артефакт плана 05-14, отдельный от 05-STATE-PATCH.md | ✓ VERIFIED | 46 431 байт, закоммичен `0861e8d`; файл плана 05-12 не перезаписан |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `expires_at` | `_apply_extension` | `subscription_is_live` → `switch_is_refused(period_is_live=…)` | ✓ WIRED | Связь, помеченная `✗ NOT_WIRED` в раунде 3, существует: `subscription_period.py:53` → `payment_service.py:15,577,584`. Греп `subscription_is_live` по `app/services/` даёт две строки вместо нуля |
| `expires_at` | `subscribe_to_plan` и `refused_plan_ids` | `subscription_is_live` → `switch_is_refused` | ✓ WIRED | Уцелела при переносе объявления: `pages/billing.py:10,210,322`. Приватной копии не осталось |
| порядок внутри `_apply_extension` | снятие признака → `next_expiry` → решение о плане | AST-тест | ✓ WIRED | `:577` → `:579` → `:584`; `test_the_liveness_is_sampled_before_the_date_moves` держит порядок машиной |
| ответ гарда `subscribe_to_plan` | `_apply_extension` | — | ✗ NOT_WIRED | **Корень гэпа 1.** Решение, которым платёж БЫЛ РАЗРЕШЁН, на `Payment` не записывается: у модели нет поля о разрешённом переходе, `create_payment` (`:167-180`) его не принимает. Стадия применения решение не восстанавливает, а принимает заново по изменившемуся состоянию |
| `handle_webhook` | `Subscription.plan` → бейдж и четыре метра | `_extend_subscription` → `_apply_extension` → контекст шелла | ⚠️ WIRED, ДАННЫЕ РАСХОДЯТСЯ | Связь цела; на пути гэпа 1 записанное значение не соответствует уплаченному — воспроизведено (три платежа `basic`, бейдж `pro`) |
| `git log -- 05-REVIEW.md` | реестр раундов → номер находки → нужный раунд | STATE.md:92 | ✓ WIRED | Проверено исполнением всех трёх `git show`; связь больше не держится утверждением автора |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `balance.html` | `usage` (4 оси) | `plan_axes` → счётчики БД + `PLAN_LIMITS` | Да | ✓ FLOWING |
| `balance.html` | `payments` | `get_payment_history` → `select(Payment)` | Да | ✓ FLOWING |
| `balance.html` | `subscription.plan`, `subscription.expires_at` | `load_shell_context` → `Subscription` | Да, но на пути гэпа 1 значение расходится с уплаченным | ⚠️ FLOWING, ЗНАЧЕНИЕ РАСХОДИТСЯ |
| `balance.html` / `plan_card.html` | `refused_plan_ids` | `switch_is_refused(period_is_live=subscription_is_live(...))` | Да | ✓ FLOWING |
| `_apply_extension` | `period_is_live` | `subscription.expires_at` в момент УВЕДОМЛЕНИЯ | Да, но не тот момент, по которому продано | ⚠️ FLOWING ИЗ ДРУГОГО ИСТОЧНИКА |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Гэп 1 раунда 3 закрыт; правило и признак ведут себя как объявлено | `uv run pytest tests/test_application/test_plan_switch.py tests/test_application/test_subscription_period.py tests/test_pages/test_billing_payment_errors.py -q` | `92 passed in 60.01s` | ✓ PASS |
| Правило нельзя спросить без признака срока | `test_the_rule_cannot_be_asked_without_the_liveness_term` (в составе прогона выше) | зелёный, ловит `TypeError` | ✓ PASS |
| Признак снимается ДО сдвига срока (AST) | `test_the_liveness_is_sampled_before_the_date_moves` (в составе прогона выше) | зелёный | ✓ PASS |
| Истёкший срок ВЫДАЁТ уплаченный план | `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage` (в составе прогона выше) | зелёный, `plan == 'basic'`, 27 < дней < 45 | ✓ PASS |
| **CR-01 раунда 4 — независимое воспроизведение** (временный файл, удалён после прогона) | 3×`POST /billing/subscribe` plan=basic + 1×plan=pro без подписки → `handle_webhook` pro, затем три basic | `план='pro', срок через 121 дней, уплачено 9370 ₽`; гард не отказал ни одному из четырёх; журнал трижды `subscription_plan_preserved` | ✗ FAIL — дефект подтверждён |
| WR-03 раунда 4 — fail-closed снимается признаком срока | `switch_is_refused('pro','platinum',period_is_live=False)` | `False` (при `period_is_live=True` → `True`) | ✗ FAIL — предупреждение подтверждено |
| Реестр раундов ревью адресуется коммитами | `git show {4989f7a,1d29360,164d210}:.planning/phases/05-tarify/05-REVIEW.md` | все три отдают заявленные документы с заявленными итогами | ✓ PASS |
| Инвариант квалификации номеров находок | подсчёт `WR-07` против `WR-07 (раунд N)` в STATE.md | 4 = 4, обе записи поимённо | ✓ PASS |
| Полная суита и гейты | установлено оркестратором ДО верификации, не перезапускалось | 1681 passed (exit 0); регрессия фаз 1–4 — 1193 passed (exit 0); `compileall` — exit 0; `schema-drift` block=false; `ui.safety-gate` block=false (`hasUiFiles: false`) | ✓ PASS (принято как установленное) |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| — | `find scripts -path '*/tests/probe-*.sh'` | ничего не найдено; ни PLAN, ни SUMMARY фазы probe-скриптов не объявляют | ? SKIP (в проекте probe-скриптов нет) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| BILL-05 | 05-13, 05-14 (и 05-01…05-12) | Пользователь может продлить текущую подписку | ✗ BLOCKED | Механика продления работает (SC1). Но требование несёт путь гэпа 1 (продано одно, выдано другое) и три неразрешённые прохибиции (`fairness`, `transparency`, `autonomy`). `REQUIREMENTS.md:235` — `In Progress`, отметка `Complete` верно не возвращена |
| BILL-06 | 05-13, 05-14 (и 05-03, 05-05) | Потребление и остаток по четырём осям | ✗ BLOCKED | Витрина цела и проверена (SC2), но рисуется по `subscription.plan`, расходящемуся с уплаченным на пути гэпа 1. Прохибиция `transparency` (строка истории с одним тарифом рядом с бейджем другого) остаётся `unresolved`. `REQUIREMENTS.md:236` — `In Progress` |
| BILL-07 | 05-14 (и 05-04, 05-05) | Пользователь видит историю своих платежей | ? NEEDS HUMAN | Журнал работает и проверен (SC3). Упирается в D-27: пока подписка на `payment.canceled` в кабинете ЮKassa не подтверждена, статус «отклонён» на проде не появится. `REQUIREMENTS.md:237` — `In Progress` |

**Orphaned requirements:** нет. `REQUIREMENTS.md` относит к Phase 5 идентификаторы BILL-01…BILL-07; BILL-05/06/07 — объём фазы и объявлены во frontmatter планов; BILL-01/03/04 — `Complete (baseline)`; BILL-02 — `Partial` решением D-13, фаза его сознательно не закрывает (D-08). Все семь учтены.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `app/services/payment_service.py` | 579 | Безусловный сдвиг срока ВЫШЕ решения о плане | 🛑 Блокер | Отвергнутый платёж младшего тарифа покупает полный месяц действующего старшего; повторяемо без потолка. Гэп 1 |
| `app/services/payment_service.py` / `app/pages/billing.py` | 577 / 319-325 | Один предикат считается дважды, в разные моменты, по изменяемому состоянию; решение не сохраняется | 🛑 Блокер | Корень гэпа 1. TOCTOU между продажей и применением на денежном пути |
| `app/application/billing/plan_switch.py` | 48 | Докстринг утверждает «ОДИН НАБОР ВЕЛИЧИН» — совпадает форма, не значения | ⚠️ Предупреждение | Ложная уверенность того же класса, что дала блокер раунда 3, теперь записанная утвердительно (WR-01 раунда 4) |
| `app/pages/billing.py` | 195-198 против 100-107 | Рукописная копия предиката рядом с импортированным объявлением + комментарий, отрицающий её существование | ⚠️ Предупреждение | Сегодня расходятся только на границе `expires_at == now`; любое изменение смысла живости разведёт экран и гард молча (WR-02 раунда 4) |
| `app/application/billing/plan_switch.py` | 69-70 против 63-67 | `if not period_is_live: return False` короткозамыкает fail-closed для плана без ранга; пересечение двух правил не названо | ⚠️ Предупреждение | Истёкший срок + подтверждённый платёж плана вне `PLAN_ORDER` пишет незнакомое значение в `subscription.plan`; после оживления периода пользователь заперт. Проверено исполнением. Пара «истёкший срок + незнакомый план» не покрыта ни одним тестом (WR-03 раунда 4) |
| `docker-compose.prod.yml` | — | У `web`, `celery-beat` и обоих celery-воркеров нет политики перезапуска, у `nginx`/`certbot`/`flower` — есть | ⚠️ Предупреждение | WR-04 раунда 4. Вне объёма планов 05-13/05-14 |
| `app/constants.py` | 61-65 | Комментарий отправляет читателя за правилом не в тот модуль | ℹ️ Инфо | IN-01 раунда 4 |
| `tests/test_pages/test_billing_payment_errors.py` | 1249 | AST-тест держит ПОРЯДОК двух операторов, но не ВЕЛИЧИНУ, по которой снимается признак | ℹ️ Инфо | IN-02 раунда 4. Подмена аргумента `subscription_is_live` теста не ломает |
| модифицированные файлы фазы | — | Маркеры долга `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` | — | Ноль вхождений в семи файлах, тронутых планами 05-13/05-14. Гейт маркеров долга пройден |

### Human Verification Required

Десять пунктов, из них восемь перенесены из раунда 3 без изменений, два — новые.
Полные формулировки — во frontmatter (`human_verification`). Кратко:

1. **Мобильные ширины 375px** — браузерного харнесса в проекте нет; разметка и
   стили с раунда 2 не изменились. Критерий 4 ROADMAP не имеет автоматического
   доказательства в этом проекте вовсе.
2. **Настоящий платёж в тестовом магазине ЮKassa** — всё покрытие на моках.
3. **Первое настоящее уведомление проходит гард источника** — поведение боевого
   nginx кодом репозитория не проверяется.
4. **Отменённый платёж получает «отклонён»** — заблокирован D-27.
5. **«В обработке» читается как «деньги в обработке»** — суждение человека.
6. **Отказ оплаты читается как «попробуйте ещё раз»** — суждение человека.
7. **Экран истёкшего срока согласован с исходом оплаты** (backstop плана 05-13) —
   ⚠️ читать после закрытия гэпа 1: сегодня обещание карточки нарушается на пути
   WR-01 раунда 4.
8. **НОВОЕ: владелец риска `WR-07 (раунд 3)`** (сокет Docker у веб-слоя) не
   назначен — `.planning/STATE.md:93` держит пустой слот «⟨не назначен⟩».
   Распоряжение записано и проверено; имя ставит человек.
9. **Прохибиция BILL-05 `fairness`** (остаток старшего тарифа в дни младшего) —
   `unresolved`, вердикт LLM-судьи НЕ АВТОРИТЕТЕН: соблюдена в названном
   направлении, обратное направление вскрыто этим раундом.
10. **Прохибиция BILL-06 `transparency`** (строка истории с одним тарифом рядом с
    бейджем другого) — `unresolved`, вердикт LLM-судьи НЕ АВТОРИТЕТЕН: нарушена,
    воспроизведено напрямую. Плюс **прохибиция BILL-05 `autonomy`** (запирание в
    тарифе) — в основном пути снята вариантом `apply-after-expiry`, остаются два
    узких пути (WR-01 и WR-03 раунда 4).

**Прохибиции по тирам (ADR-550 D4).** Планы 05-13 и 05-14 объявляют 9 прохибиций.
Шесть — `verification: test`, `status: resolved`; пять из шести подтверждены
исполняемыми проверками. Шестая — «MUST NOT брать деньги за тариф, который не
будет выдан: решение ПРОДАВАТЬ и решение ВЫДАТЬ обязаны приниматься по одним и
тем же входным данным» — помечена `resolved`, но **фальсифицирована
воспроизведением** и учтена гэпом 1; зелёной она не считается. Три —
`verification: judgment`, `status: unresolved`; по каждой дан НЕ АВТОРИТЕТНЫЙ
вердикт LLM-судьи и выставлен флаг `unverified-prohibition — human review
recommended` (пункты 9 и 10 выше). Ни одна прохибиция молча в `passed` не
всасывается.

### Gaps Summary

**Гэп 1 (блокер).** Раунд 3 нашёл, что стадии правила разошлись по ВХОДНЫМ
ДАННЫМ. План 05-13 сделал входные данные обязательной частью сигнатуры — верно,
проверено, гэп раунда 3 закрыт. Но остался третий уровень той же ошибки: правило
спрашивают **дважды, в разные моменты, по изменяемому состоянию**, и ответ
первого спроса нигде не сохраняется. Между продажей и применением лежит вся
сессия оплаты у ЮKassa; состояние подписки за это время меняется — иногда самим
же пользователем, иногда параллельным платежом. Поверх этого сдвиг срока
безусловен и стоит ВЫШЕ решения о плане, поэтому отвергнутый платёж всё равно
покупает месяц действующего тарифа.

Оба следствия воспроизведены на живом коде: касса теряет 3410 ₽ на каждом
платеже `basic`, купленном при живом `pro` (повторов не ограничивает ничто), а
пользователь на зеркальном пути платит за `basic`, который ему не выдают, —
дословно тот исход, который раунд 3 назвал блокером. Ни один чекпойнт этого
владельцу не предъявлял: `upgrade-only` назвал одну цену, `apply-after-expiry` —
другую, эту не назвал никто.

Лечится **не сигнатурой** — сигнатуру уже чинили и получили этот раунд. Лечится
записью решения о сделке на сам платёж (или явным решением владельца, что
отвергнутому платежу выдаются дни УПЛАЧЕННОГО тарифа). Класс тестов, которого
нет ни в одном из 1681: регрессия, МЕНЯЮЩАЯ состояние подписки МЕЖДУ продажей и
подтверждением.

**Гэп 2 (низкая тяжесть, `partial`).** Реестр раундов ревью перестроен правильно
и переживёт следующие перезаписи — механизм адресации проверен исполнением
`git show` по всем трём хешам. Осталось одно числительное («ТРЁМ» при четырёх
документах) и один оборот того же вида. План 05-14 сам назвал этот способ отказа
и сам же построил рядом правильный инвариант — правка на одно предложение.

**Что НЕ является гэпом и названо явно, чтобы следующий раунд не гадал.**
Отсутствие браузерного харнесса (критерий 4) — не гэп, а свойство проекта;
пункт переносится в human_verification четвёртый раунд подряд. Находки раунда 4
в `.planning/STATE.md` — не долг плана 05-14 (ревью прошло через 35 минут после
его завершения), а работа следующего раунда бухгалтерии. Предупреждения WR-02,
WR-03, WR-04 и IN-01, IN-02 раунда 4 — в таблице анти-паттернов, блокерами не
считаются и закрытия этой фазой не требуют.

---

_Verified: 2026-08-17T06:09:30Z_
_Verifier: Claude (gsd-verifier), раунд 4_
