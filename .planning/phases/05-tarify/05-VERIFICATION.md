---
phase: 05-tarify
round: 5
verified: 2026-08-17T13:05:00Z
status: gaps_found
score: 25/30 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_round: 4
  previous_status: gaps_found
  previous_score: 19/26
  previous_verified: 2026-08-17T06:09:30Z
  gaps_closed:
    - "Гэп 1 раунда 4 (блокер денежного пути) закрыт В НАЗВАННОМ НАПРАВЛЕНИИ и это проверено прогоном настоящего кода, а не по SUMMARY. Ответ гарда живёт на строке платежа (`payments.switch_authorized`, ревизия `0019`, 13 тестов ревизии зелёные), читается ПЕРВЫМ (`payment_service.py:813-820`), правило при `NOT NULL` не спрашивается вовсе. Отвергнутый платёж покупает долю месяца по цене ДЕЙСТВУЮЩЕГО плана с нижней границей в один день (`prorated_expiry`/`prorated_days`, целиком в `Decimal`). Потолок одновременных подписочных намерений формы `cap-different-plan` поднимается ПЕРВЫМ оператором `create_payment`, до `_configure_yookassa()` и до `YooPayment.create`; 7 относящихся регрессий зелёные; 65 тестов прикладного слоя биллинга зелёные. Направление «младший платёж покупает месяц старшего» через форму более не воспроизводится."
    - "Гэп 2 раунда 4 (`status: partial`) закрыт полностью. В строке 92 STATE.md не осталось ни одной константы величины: «принадлежит нескольким разным документам», «их число эта запись не называет намеренно», «состав и число гэпов отдаёт сам отчёт». Раунд 4 адресуется собственным коммитом `5da8e82` наравне с раундами 1-3. Распоряжения по ВСЕМ СЕМИ находкам раунда 4 записаны (проверено грепом: `CR-01`, `WR-01`, `WR-02`, `WR-03`, `WR-04`, `IN-01`, `IN-02` — каждая присутствует и квалифицирована раундом). Строка §Deferred Items о блокере называет ОБА закрывших плана и регрессии именами. Запись о `CR-01` раунда 3 больше не читается как полное закрытие. D-28/D-29 и ответ чекпойнта о форме потолка записаны в §Decisions."
  gaps_remaining: []
  regressions:
    - "Утверждение о величине снято из реестра раундов (гэп 2 раунда 4), но константа величины ПЕРЕЕХАЛА в §Current Position: STATE.md объявляет «Фаза 5 — 16/17, план 05-16 исполняется» и `completed_plans: 68`, тогда как ROADMAP объявляет `17/17 plans executed` и все семнадцать пунктов отмечены `[x]`, а `05-16-SUMMARY.md` лежит на диске. Тот же способ отказа, который план 05-16 чинил в соседней строке."
gaps:
  - truth: "Операция продления и повышения, которую эта фаза добавляет, не позволяет получить тариф существенно дешевле прейскуранта: «оставшиеся дни», которые повышение отдаёт бесплатно по записанной цене варианта `upgrade-only`, ограничены сверху хоть чем-нибудь"
    status: failed
    reason: "ВОСПРОИЗВЕДЕНО НЕЗАВИСИМО, поверх настоящего `_apply_extension`, без единой правки `app/`: 12 подтверждённых платежей `basic` дают срок 2027-01-10 при плане `basic`, затем ОДИН платёж `pro` даёт срок 2027-02-10 при плане `pro`. Уплачено 22 780 рублей; прейскурант тринадцати месяцев Pro — 63 700 рублей. Утечка 40 920 рублей на одном пользователе за год, и она растёт ЛИНЕЙНО с горизонтом предоплаты, которого не ограничивает ничто. Произведение трёх решений, каждое верное по отдельности и ни разу не перемноженных: (1) `switch_is_refused(X, X, period_is_live=True)` возвращает `False` (`plan_switch.py:87-88`) — повтор своего тарифа правилом не задевается, и потолок 05-17 пропускает его НАМЕРЕННО (`payment_service.py:229-230`, фильтр `intent.plan != plan`); (2) `next_expiry` считает от ПОЗДНЕЙШЕЙ из двух точек (`subscription_period.py:100`, `countdown_base`), верхней границы у `expires_at` нет ни в коде, ни в схеме, поэтому каждый подтверждённый платёж добавляет календарный месяц К УЖЕ НАКОПЛЕННОМУ; (3) разрешённая ветка `_apply_extension` пишет `subscription.plan = db_payment.plan` (`payment_service.py:887`) ОДНИМ оператором на ВЕСЬ накопленный срок, потому что `subscription.plan` — скаляр без истории. Каждое из тринадцати нажатий проходит гард формы и потолок ЗАКОННО: двенадцать — продление своего тарифа, тринадцатое — разрешённое повышение. Ни гонок, ни истёкших намерений, ни устаревшей страницы. ПОЧЕМУ ЭТО НЕ ПОКРЫТО ЗАПИСАННЫМ РАСПОРЯЖЕНИЕМ: `.planning/STATE.md:80` называет цену варианта дословно — «повышение отдаёт лимиты старшего тарифа на ОСТАВШИЕСЯ ДНИ младшего бесплатно, и это принято сознательно»; тот же текст лежит в `payment_service.py` и в чекпойнте плана 05-10. «Оставшиеся дни» читается как остаток текущего периода — величина, ограниченная сверху одним месяцем. Код не ограничивает её ничем, и управляет ею ПОКУПАТЕЛЬ. Принятая цена и фактическая цена отличаются на два порядка, а решение принималось по первой. Смягчение назовём честно: применения тарифных лимитов в системе нет вовсе (D-08), поэтому `pro` сегодня не даёт доступа ни к чему сверх показа — это ОТКЛАДЫВАЕТ денежную цену до BILL-02, но не устраняет её: продаётся именно тариф, он печатается в карточке и в журнале платежей, и первая же выкатка принуждения превратит отложенную цену в списанную. Совпадает с `CR-01 (раунд 5)` `05-REVIEW.md`; механизм подтверждён мной независимо, а не принят по отчёту."
    artifacts:
      - path: "app/services/payment_service.py"
        issue: "Строки 886-887: `subscription.expires_at = next_expiry(...)` и следом `subscription.plan = db_payment.plan` — безусловно в разрешённой ветке. Один оператор переводит ВЕСЬ накопленный горизонт на старший тариф за цену одного месяца"
      - path: "app/application/billing/plan_switch.py"
        issue: "Строки 87-88: `if target_plan == current_plan: return False` — повтор своего тарифа правилом не задевается; строка 95 — повышение разрешено всегда. Оба верны по отдельности; их произведение с неограниченным горизонтом не считал никто"
      - path: "app/services/payment_service.py"
        issue: "Строка 229-230: `clashing = [intent for intent in open_intents if intent.plan != plan]` — потолок 05-17 НАМЕРЕННО пропускает повтор того же тарифа. Решение верное для своей задачи и оставляющее накопление горизонта неограниченным"
      - path: "app/application/billing/subscription_period.py"
        issue: "Строки 103-121 (`countdown_base`) и 85-100 (`next_expiry`): отсчёт от позднейшей из двух точек, верхней границы горизонта нет. Докстринг `prorated_days:135-138` объявляет отсутствие верхнего потолка ОСОЗНАННЫМ решением для доли месяца — для НАКОПЛЕНИЯ горизонта такого решения не принимал никто"
      - path: "tests/test_pages/test_billing_payment_errors.py"
        issue: "Строка 724, `test_an_upgrade_does_not_burn_the_paid_remainder`: закрепляет «остаток младшего переживает повышение» на остатке в 25 дней — то есть ЗАКРЕПЛЯЕТ механизм утечки как желаемое поведение на масштабе, где он безобиден. Теста на N повторов своего тарифа с последующим повышением в суите нет ни одного; все существующие прогоны заводят не более двух платежей"
    missing:
      - "Продуктовое решение владельца: что покупает повышение поверх ПРЕДОПЛАЧЕННОГО горизонта. Три пригодные формы, любая допустима, недопустима текущая. (а) Повышение считает доплату: остаток конвертируется по отношению цен тем же аппаратом `Decimal`, который уже написан для D-29, только в другую сторону. (б) Потолок горизонта предоплаты на стадии НАМЕРЕНИЯ, рядом с потолком одновременных намерений и в том же модуле: платёж, который увёл бы `expires_at` дальше N месяцев от сегодня, не продаётся, и человеку говорится словом (новый код причины по образцу `pending`) — тогда «оставшиеся дни» становятся ограниченной величиной, какой их и описывает принятое решение. (в) Повышение вступает в силу со следующего периода."
      - "Регрессия «N подтверждённых платежей ОДНОГО тарифа → срок растёт, и растёт не дальше объявленного потолка горизонта» — обязана падать на текущем коде"
      - "Регрессия «N×basic + 1×pro → выданный срок pro СООТВЕТСТВУЕТ уплаченному, а не равен N+1 месяцам» — обязана падать на текущем коде"
      - "Регрессия «повышение при ОДНОМ оплаченном месяце по-прежнему не сжигает остаток» — защитная, существующая семантика не должна сломаться правкой"
      - "Приведение записанной цены варианта `upgrade-only` (STATE.md:80, чекпойнт 05-10, докстринг `payment_service.py`) к тому, что код фактически отдаёт, — либо ограничение кода до записанной цены"
  - truth: "Обоснования безопасности денежного пути, записанные в коде и в `.planning/STATE.md`, ссылаются только на механизмы, которые на этих путях действительно исполняются: ни один докстринг тронутых функций и ни одна запись операционного документа не утверждает того, чего код не делает (must_have №6 плана 05-15; тот же класс, за который фаза уже получила раунды 3 и 4)"
    status: failed
    reason: "Четыре утверждения проверены и каждое опровергнуто исполняемым кодом. (1) НЕЙТРАЛЬНОСТЬ ОКНА ГОНКИ. Докстринг `create_payment` (`payment_service.py:209-218`) и `.planning/STATE.md:124` принимают неатомарность потолка оговоркой «остаточный риск покрыт решением D-29 — платёж, чей план не применён, покупает долю месяца по уплаченной сумме, поэтому окно остаётся денежно нейтральным». Проверено по значениям: гард формы при отказе возвращает 302 ДО `create_payment` (`app/pages/billing.py:348-349`), поэтому в `create_payment` приезжает только `switch_authorized = not switch_refused = True` (`:367`); на стадии применения `db_payment.switch_authorized is not None` даёт `refused = not True = False` (`payment_service.py:813-814`). Ветка `if refused:` со всей арифметикой доли месяца для платежа, заведённого ФОРМОЙ, недостижима НИ РАЗУ. D-29 остаётся живой только для строк со `switch_authorized IS NULL`, то есть заведённых до ревизии `0019`, и после её выката это множество перестаёт пополняться навсегда. Опора неверна по построению; сам проект признаёт недостижимость тремя строками (`app/models/payment.py:42-44`, `0019:33-36`, докстринг теста `test_billing_payment_errors.py:1899-1903`) — и рядом принимает риск ссылкой на неё. (2) ЦЕНА, ПО КОТОРОЙ СЧИТАЕТСЯ ДОЛЯ. `payment_service.py:762-764` и `:833-835` дважды утверждают «ДНИ СЧИТАЮТСЯ ПО УПЛАЧЕННОМУ ТАРИФУ, А НЕ ПО ДЕЙСТВУЮЩЕМУ», причём второй раз — НЕПОСРЕДСТВЕННО над строкой `price = _plan_price(subscription.plan)`, читающей цену ДЕЙСТВУЮЩЕГО. Верную формулировку несёт только `subscription_period.py:151-153`. Расхождение не косметическое: «по уплаченному» дало бы 1490/1490 = полный месяц, «по действующему» даёт 1490/4900 ≈ 9 дней — это разница между блокером раунда 4 и его исправлением, записанная как синонимы. Следующий человек «починит» код по комментарию и вернёт блокер правкой, выглядящей приведением в соответствие. (3) НЕДОСТИЖИМОСТЬ СОСТОЯНИЯ ДВУХ НАМЕРЕНИЙ. Докстринг `create_payment:195-207` объявляет недостижимым состояние «у одного пользователя ОДНОВРЕМЕННО висят два незакрытых намерения РАЗНЫХ тарифов» и называет ровно одно остаточное окно — гонку. Второе окно, детерминированное и куда более широкое, не названо вовсе: срок давности. Собственный ЗЕЛЁНЫЙ тест проекта `test_a_stale_intent_does_not_block_a_new_one` (`tests/test_services/test_payment_service.py:703-726`) доказывает обратное — намерение `pro` возрастом TTL+1 остаётся `pending` и оплачиваемым, рядом заводится `basic`, `_payments_count == 2`. Пользователь, подождавший сутки, получает два оплачиваемых намерения разных тарифов ГАРАНТИРОВАННО. Тем самым прохибиция плана 05-17 «MUST NOT позволять двум разрешённым намерениям разных тарифов висеть одновременно» фальсифицирована собственным тестом фазы. (4) ПОСЛЕДСТВИЕ РАСХОЖДЕНИЯ МОДЕЛИ И БОЕВОЙ СХЕМЫ. `.planning/STATE.md:88` и докстринг `0019:44-52` описывают последствие D-26 как «подписка на проде не записывается … кнопка оплаты упирается в ошибку записи» и «практического последствия у этого сегодня нет». Проверено: `Payment.__table__.columns` отдаёт `kind`, `plan`, `switch_authorized` — все три отсутствуют в боевой схеме `0012`. ORM-выборка сущности выписывает ПОЛНЫЙ перечень отображённых колонок, поэтому `UndefinedColumn` поднимут ВСЕ читающие пути: `get_payment_history` (`billing_service.py:231`) — весь раздел «Тарифы» отдаёт 500 любому вошедшему; `_open_subscription_intents` (`payment_service.py:138`); и `handle_webhook` (`payment_service.py:442-445`), где `select(Payment).with_for_update()` стоит ДО ветвления по `kind` (`:495`, `:517`) — то есть падает и для платежа за ПАКЕТ СООБЩЕНИЙ, работавшего до этой фазы. Маршрут ответит 500, ЮKassa повторит доставку ограниченное число раз и сдастся: деньги списаны, сообщения не начислены, платёж навсегда `pending`. Гейт выката D-26 сегодня описан как «новая функция не доедет»; фактически он «старая функция сломается», и решение о выкате принимается по первой формулировке. Само решение D-26 этот гэп НЕ переоткрывает — переоткрывает только ФАКТ, по которому оно принимается. Совпадает с `WR-01`, `WR-04`, `WR-05` и `CR-02` раунда 5 `05-REVIEW.md`; каждый механизм подтверждён мной независимо."
    artifacts:
      - path: "app/services/payment_service.py"
        issue: "Строки 209-218: оговорка «окно остаётся денежно нейтральным благодаря D-29» — ветка D-29 для платежа, заведённого формой, недостижима, оба платежа несут `True`, исход решает ПОРЯДОК подтверждения"
      - path: ".planning/STATE.md"
        issue: "Строка 124: та же неверная оговорка — но уже в документе, ПО КОТОРОМУ ПРИНИМАЮТСЯ РЕШЕНИЯ"
      - path: "app/services/payment_service.py"
        issue: "Строки 762-764 и 833-835: «дни считаются по УПЛАЧЕННОМУ тарифу» — код на строке 836 читает `_plan_price(subscription.plan)`, цену ДЕЙСТВУЮЩЕГО. Верная формулировка есть только в `subscription_period.py:151-153`"
      - path: "app/services/payment_service.py"
        issue: "Строки 195-207: объявлено недостижимым состояние, достижимость которого доказывает зелёный тест проекта в 200 строках от докстринга. Названо одно остаточное окно из двух — срок давности не назван"
      - path: ".planning/STATE.md"
        issue: "Строка 88: последствие D-26 названо уже фактического — отказывают ВСЕ чтения `payments`, включая пакетный вебхук, работавший до фазы"
      - path: "alembic/versions/0019_payment_switch_authorized.py"
        issue: "Строки 44-52: «практического последствия у этого сегодня нет ровно потому, что подписка на проде не записывается вовсе» — последствие есть и оно шире подписочной ветки"
      - path: "tests/test_pages/test_billing_payment_errors.py"
        issue: "Строки 1385-1450, AST-тест порядка: ходит по `function.body`, то есть по операторам ВЕРХНЕГО УРОВНЯ. Из четырёх операторов, двигающих срок в `_apply_extension`, видит ОДИН (`:886`); сдвиги в ветвях (`:801`, `:858`) невидимы, `prorated_expiry` (`:860`) не упомянут в тесте ни разу. Докстринг `:1402-1410` при этом утверждает, что тест несёт возросшую нагрузку плана 05-15 — «сдвиг срока теперь стоит В ДВУХ ВЕТКАХ». Проверка ВЕЛИЧИНЫ (`sampled.attr == 'expires_at'`, закрытие IN-02 раунда 4) сделана верно и относится к тому же единственному оператору"
      - path: "tests/test_services/test_payment_service.py"
        issue: "Строка 703, `test_a_stale_intent_does_not_block_a_new_one` — зелёный тест, доказывающий достижимость состояния, объявленного недостижимым; и имя `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form` (`test_billing_payment_errors.py:1889`) обещает больше, чем тело держит: два намерения заводятся ПОДРЯД, о сроке давности тело не знает"
    missing:
      - "Снять неверную опору в `payment_service.py:209-218` и `.planning/STATE.md:124` и назвать настоящую: в остаточном окне обе строки несут `True`, доля месяца не исполняется, исход решает порядок подтверждения. Если нейтральность окна нужна как СВОЙСТВО, её обязан дать другой механизм — частичный уникальный индекс «не более одного незакрытого подписочного намерения пользователя», по образцу `uq_subscriptions_active_user` ревизии `0018`"
      - "Привести оба абзаца `payment_service.py:762-764` и `:833-835` к формулировке `subscription_period.py:151-153` и назвать величину явно: `paid / price(subscription.plan)`"
      - "Назвать ОБА окна потолка в докстринге `create_payment:195-207` (гонка И срок давности) и переименовать `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form` по тому, что тест держит"
      - "Исправить формулировку последствия D-26 в `.planning/STATE.md:88` и в докстринге `0019:44-52`: при расхождении модели и схемы отказывают ВСЕ чтения `payments`, включая пакетный вебхук; выкат кода и выкат очереди миграций отделить друг от друга НЕЛЬЗЯ"
      - "Машинная проверка соответствия модели и головной ревизии (`{c.name for c in Payment.__table__.columns} <= inspect(engine).get_columns('payments')`) — дешёвая и ловит весь класс; плюс отказ `entrypoint.sh` поднимать `web` при `alembic current != head`"
      - "AST-тест порядка обязан ходить по `ast.walk(function)` и требовать снятия признака раньше КАЖДОГО сдвига, включая `prorated_expiry`; либо докстринг обязан перестать утверждать покрытие двух ветвей"
  - truth: "«Current Position» и frontmatter `progress` в `.planning/STATE.md` не спорят ни друг с другом, ни с ROADMAP после появления планов 05-15, 05-16 и 05-17 (must_have №9 плана 05-16)"
    status: partial
    reason: "Тяжесть НИЗКАЯ, маршрутизация верна, но константа величины, снятая из реестра раундов, уцелела в соседней секции того же файла. `.planning/STATE.md:32` — «Plan: 16 of 17»; `:33` — «Исполнены планы 05-01…05-15 и 05-17 … гэп 2 … ЗАКРЫВАЕТСЯ планом 05-16» (будущее время); `:36` — «Фаза 5 — 16/17, план 05-16 ИСПОЛНЯЕТСЯ»; frontmatter `completed_plans: 68` из 69. ROADMAP при этом объявляет `17/17 plans executed`, все семнадцать пунктов отмечены `[x]`, а `05-16-SUMMARY.md` лежит на диске (30 КБ, коммит `b0773f8`). Утверждение было верно в момент записи и перестало быть верным в момент коммита — ровно тот способ отказа, который план 05-16 чинил в строке 92 того же файла. Отдельно и справедливо: строка 8 (`stopped_at`) и строка 11 (`last_activity_desc`) говорят о плане 05-16 в прошедшем времени, то есть файл спорит сам с собой."
    artifacts:
      - path: ".planning/STATE.md"
        issue: "Строки 32, 33, 36 и frontmatter `completed_plans: 68` объявляют фазу 16/17 с планом 05-16 в работе; ROADMAP:306 и таблица Progress объявляют 17/17 executed"
      - path: ".planning/STATE.md"
        issue: "Строка 33 говорит «закрывается планом 05-16» в будущем времени, строки 8 и 11 — о том же плане в прошедшем"
    missing:
      - "Привести §Current Position и frontmatter `progress` к состоянию после волны 14: 17/17, `completed_plans: 69`, план 05-16 исполнен"
      - "Формулировка, не зависящая от номера текущего плана, вместо «Plan: N of M» — та же правка, которую план 05-16 уже сделал в строке 92"
deferred: []
behavior_unverified_items:
  - truth: "Раздел тарифов пригоден к использованию на мобильных ширинах (критерий 4 ROADMAP)"
    test: "Открыть /billing в браузере на ширине 375px с реальными данными: три карточки планов, четыре метра, история платежей длиннее экрана, блок пакетов сообщений"
    expected: "Карточки планов складываются в одну колонку; подписи колонок истории ([data-cell-label]) едут вместе со значениями; ни один блок не требует горизонтальной прокрутки; кнопки оплаты нажимаемы пальцем (порог проекта 44px)"
    why_human: "Браузерного/e2e-харнесса в проекте нет (playwright/selenium/puppeteer отсутствуют в pyproject.toml). Все относящиеся тесты читают CSS и HTML как ТЕКСТ и проверяют ОБЪЯВЛЕНИЕ правила, а не отрисовку — они зелёные (175 passed в test_responsive_markup.py + test_billing_section.py, включая test_billing_plans_grid_is_declared_folding и test_billing_payment_labels_ride_with_the_values). Разметка и стили не менялись с 2026-08-16 (последний коммит app/templates/ и app/static/css/app.css — 17b9bc9, план 05-10), поэтому пункт переносится без изменений пятый раунд подряд."
human_verification:
  - test: "Открыть /billing на 375px и пройти раздел глазами (см. behavior_unverified_items выше)"
    expected: "Одна колонка, никакой горизонтальной прокрутки, кнопки нажимаемы"
    why_human: "Браузерного харнесса в проекте нет. Пятый раунд подряд; объявление правила проверено машиной, отрисовка — нет"
  - test: "Настоящий платёж в тестовом магазине ЮKassa: форма → confirmation_url → возврат → приход уведомления → сдвинутый срок на /billing"
    expected: "Срок НЕ двигается до прихода уведомления и двигается после него"
    why_human: "Боевого доступа к API ЮKassa у исполнителя нет, всё покрытие на моках. Backstop-пункт must_haves планов 05-01, 05-06, 05-11, 05-13 и 05-15. При решении D-26 недостижим на проде"
  - test: "Первое настоящее уведомление ЮKassa после выката проходит гард источника"
    expected: "Боевой nginx действительно проставляет X-Real-IP именно на маршруте вебхука, и настоящее уведомление получает 200, а не 403"
    why_human: "Backstop-пункт must_haves плана 05-07. Поведение боевого nginx на боевом маршруте кодом репозитория не проверяется"
  - test: "Отменённый платёж получает статус «отклонён» на экране истории"
    expected: "Строка платежа показывает бейдж «отклонён», а не «в обработке»"
    why_human: "Заблокирован: владелец не подтвердил подписку на событие payment.canceled в кабинете ЮKassa (D-27). Состав событий задаётся вне репозитория"
  - test: "Строка отказа по потолку одновременных намерений читается как «предыдущая оплата ещё не завершена, подождите результата»"
    expected: "Не читается ни как «с вас списали и не зачли», ни как «кнопка сломана»"
    why_human: "Backstop-пункт must_haves плана 05-17: понятность формулировки судится человеком. Механика проверена — код причины pending входит в закрытое множество PAYMENT_ERROR_MESSAGES (app/pages/billing.py:66-115, редирект :380)"
  - test: "Пользователь, вернувшийся с ЮKassa до прихода вебхука, понимает, что платёж ещё в обработке"
    expected: "Человек читает строку истории со статусом «в обработке» как «деньги в обработке», а не как «оплата не прошла»"
    why_human: "Backstop-пункт must_haves планов 05-04 и 05-10"
  - test: "Сообщение об отказе оплаты («ЮKassa не создала платёж») прочитывается как «попробуйте ещё раз»"
    expected: "Не читается как «с вас списали и не зачли»"
    why_human: "Backstop-пункт must_haves плана 05-10"
  - test: "Человек с истёкшим оплаченным сроком читает экран раздела как согласованный с тем, что произойдёт после оплаты"
    expected: "Сочетание DOWNGRADE_CARD_CAPTION, текста отказа downgrade и фактического исхода читается как одно обещание"
    why_human: "Backstop-пункт must_haves планов 05-13 и 05-15. Путь WR-01 раунда 4 закрыт записанным разрешением — проверено; остаётся суждение о понятности"
  - test: "Владелец риска WR-07 (раунд 3) — монтирование /var/run/docker.sock в контейнер web, терминирующий вебхук ЮKassa — назван поимённо"
    expected: "В .planning/STATE.md:91 вместо «⟨не назначен — назначает владелец проекта⟩» стоит имя, либо принято одно из двух распоряжений (вынос управления контейнерами в отдельный воркер / фильтрующий прокси перед сокетом)"
    why_human: "must_have №8 плана 05-14 требовал ВЛАДЕЛЬЦА и распоряжение. Распоряжение записано и проверено, владелец — решение человека. Пятый раунд без имени"
  - test: "ПРОХИБИЦИЯ (test-tier, БЕЗ ПОДКЛЮЧЁННОГО ПРИНУЖДЕНИЯ, плана 05-17): «MUST NOT позволять двум разрешённым намерениям разных тарифов висеть одновременно»"
    expected: "Владелец решает, считать ли прохибицию соблюдённой при существующем сроке давности — или закрывать окно снятием намерения с оплаты при истечении TTL"
    why_human: "unverified-prohibition — human review recommended. Объявлена verification: test; принуждение подключено ЧАСТИЧНО и опровергается собственным зелёным тестом проекта test_a_stale_intent_does_not_block_a_new_one: через 24 часа состояние достижимо детерминированно. В окне гонки — тоже (атомарности потолок не даёт, признано докстрингом). Денежного следствия ОТДЕЛЬНОГО от гэпа 1 у этого окна нет"
  - test: "ПРОХИБИЦИИ (test-tier, БЕЗ ПОДКЛЮЧЁННОГО ПРИНУЖДЕНИЯ, плана 05-16, пять штук): о константах величины, о голых номерах находок, о закрытии дефекта удалением записи, о слове «исправлено» без имён механизма, о преждевременном Complete"
    expected: "Владелец решает, нужен ли машинный гейт на операционные документы — или прохибиции этого класса переводятся в judgment-tier честно"
    why_human: "unverified-prohibition — human review recommended. Все пять объявлены verification: test, но ни одного теста, проверяющего .planning/STATE.md, в суите не существует (грепом по tests/ — ноль). Прохибиция без принуждения зелёной не считается. Фактически: четыре из пяти соблюдены при чтении глазами, пятая (о константах величины) НАРУШЕНА в §Current Position того же файла — см. гэп 3"
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, status: unresolved в планах 05-11 и 05-13): «MUST NOT превращать оплаченный остаток старшего тарифа в дни младшего без объявленного правила и без слова пользователю до нажатия кнопки» (BILL-05, fairness)"
    expected: "Владелец решает, считать ли прохибицию соблюдённой"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): в НАЗВАННОМ направлении соблюдена и с этого раунда — крепче: отвергнутый платёж больше не покупает месяц действующего тарифа, а получает долю по уплаченной сумме (D-29). Но ОБРАТНОЕ направление, вскрытое этим раундом, прохибицией не покрыто и открыто шире прежнего: оплаченный младший тариф превращается в дни СТАРШЕГО в объёме ВСЕГО накопленного горизонта, без слова пользователю и без названной цены (гэп 1)"
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, status: unresolved в планах 05-11 и 05-13): «MUST NOT показывать человеку строку истории с одним тарифом рядом с бейджем другого действующего тарифа без единого слова о том, что оплата продлила срок, а не сменила тариф» (BILL-06, transparency)"
    expected: "Владелец решает, нужен ли интерфейсный ответ на расхождение уплаченного и действующего тарифа — и в каком объёме"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): НАРУШЕНА, и после гэпа 1 картина стала выразительнее: в истории двенадцать строк «Basic · подписка» и одна «Pro», в бейдже — «Pro» на тринадцать месяцев, ни одна строка раздела этого не объясняет. Единственный след — subscription_plan_preserved в журнале, видимый только нам, и он на этом пути даже не пишется (отказа не возникает). Планы 05-11 и 05-13 сознательно вынесли интерфейсную работу за границу объёма"
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, status: unresolved в плане 05-13): «MUST NOT запирать пользователя в тарифе на всё время жизни аккаунта — а если запирать, то сказать об этом теми же словами, которыми сегодня обещано обратное» (BILL-05, autonomy)"
    expected: "Владелец подтверждает, что вариант apply-after-expiry снял запирание в объёме, который он считает достаточным"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): в основном пути СНЯТА, и путь WR-01 раунда 4 закрыт записанным разрешением (D-28) — проверено. Остаётся узкий путь WR-03 раунда 4 (план без ранга, записанный в подписку на истёкшем сроке, после оживления периода отвергает ЛЮБОЙ переход) — отложен с распоряжением, владелец не назначен"
tdd_gate:
  status: advisory_disclosed
  note: "Планы 05-15 и 05-17 не имеют RED-коммита. Оба исполнителя раскрыли это сами: красные значения снимались одноразовыми пробами, удалёнными до коммита, потому что утверждения маскировались ImportError/TypeError на ещё не существующих символах. Записано; раскрытое ограничение не приравнивается к скрытому. MVP mode выключен, гейт неблокирующий"
---

# Phase 5: Тарифы — Verification Report (раунд 5)

**Phase Goal:** Пользователь понимает, сколько ресурса тарифа он израсходовал, за что платил и может продлить подписку не выходя из раздела.
**Verified:** 2026-08-17T13:05:00Z
**Status:** gaps_found
**Re-verification:** Да — после закрытия гэпов раунда 4 (планы 05-15, 05-17, 05-16)
**Round:** 5 — раунд 1 `4989f7a`, раунд 2 `1d29360`, раунд 3 `164d210`, раунд 4 `5da8e82`. Номера находок и гэпов между раундами ПЕРЕНАЗНАЧЕНЫ; каждая ссылка ниже квалифицирована раундом.

## Одной фразой

Обе работы раунда 4 сделаны и проверены прогоном, а не по SUMMARY: записанное разрешение сделки живёт на платеже и читается первым, отвергнутый платёж покупает долю месяца, потолок одновременных намерений поднимается до единого обращения к SDK, реестр раундов очищен от констант величины и несёт распоряжения по всем семи находкам. **И денежный дефект снова обнаружился в новой формулировке — пятый раунд подряд, на этот раз в направлении, которое четыре раунда считалось подарком.** Три верных по отдельности решения — «повтор своего тарифа правилом не задевается», «потолок намеренно пропускает тот же тариф», «повышение разрешено всегда» — вместе с неограниченным горизонтом предоплаты дают тринадцать месяцев Pro за 22 780 рублей при прейскуранте 63 700. Воспроизведено мной поверх настоящего кода.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| **Критерии ROADMAP** | | | |
| 1 | Пользователь может продлить текущую подписку из раздела тарифов и сразу увидеть обновлённый срок | ✓ VERIFIED | Форма `POST /billing/subscribe` в карточке текущего плана (`plan_card.html:94`, подпись «Продлить» — `:97`) и у истёкшего срока (`balance.html:96`); срок печатается `balance.html:100` («действует до …») и `:111` для истёкшего. Единственный писатель `expires_at` — вебхук (D-05). ⚠️ «Сразу» ограничено приходом уведомления — человеческий пункт ниже |
| 2 | Пользователь видит потребление и остаток по всем четырём осям лимитов | ✓ VERIFIED | `AXIS_ORDER = (AXIS_ADS, AXIS_GROUPS, AXIS_SENDS, AXIS_ACCOUNTS)` — `plan_usage.py:64`, цикл `:183`; метры рисуются `balance.html:108` через `usage_meters.html:29`; безлимитная ось даёт «без ограничений», а не ноль (`:30-33`). 65 тестов прикладного слоя зелёные |
| 3 | Пользователь видит историю платежей с датой, суммой и статусом | ✓ VERIFIED | `get_payment_history` / `count_payments` (`billing_service.py:208`, `:247`) → блок «История платежей» `balance.html:206`, строка `payment_row.html`. Данные текут из БД, не из литерала |
| 4 | Раздел тарифов пригоден к использованию на мобильных ширинах | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Правило ОБЪЯВЛЕНО и объявление держится машиной: 175 тестов `test_responsive_markup.py` + `test_billing_section.py` зелёные, в том числе `test_billing_plans_grid_is_declared_folding`, `test_billing_payment_labels_ride_with_the_values`, порог 44px. Отрисовки не видит ни один тест — браузерного харнесса в проекте нет. Пятый раунд переносится без изменений; разметка и стили не менялись с `17b9bc9` |
| **Гэп 1 раунда 4 — план 05-15** | | | |
| 5 | Ответ гарда записан НА ПЛАТЕЖЕ и стадия применения читает ФАКТ, а не пересчитывает предикат (D-28) | ✓ VERIFIED | `payment_service.py:813-820`: `if db_payment.switch_authorized is not None: refused = not …; decided_by = "recorded_answer"`, иначе правило. Колонка в модели (`payment.py`), ревизия `0019` — 13 тестов зелёные |
| 6 | Платёж, чей план не применён, даёт долю месяца по уплаченной сумме, а не календарный месяц действующего тарифа (D-29) | ✓ VERIFIED | Ветка `if refused:` — `prorated_expiry(…, paid=…, price=…)` (`payment_service.py:858-861`), арифметика в `Decimal`, `float` не проходит нигде; при нечитаемой цене — откат к полному месяцу с журналом, а не 5xx |
| 7 | Исход «взяли деньги и не выдали ничего» не введён ни на одной ветке | ✓ VERIFIED | `prorated_days:143` — `max(int(…), 1)`; нижняя граница не декоративна: целая часть доли от рубля при цене 4900 равна нулю |
| 8 | Регрессия, МЕНЯЮЩАЯ состояние подписки между продажей и подтверждением, существует | ✓ VERIFIED | Раздел регрессий в `test_billing_payment_errors.py` (`:1682`, `:1740`, `:1889`) — роли утверждений названы честно; там, где красного прогона не существовало, это записано прямо, а не приписано (сохранённая оговорка `05-15-SUMMARY.md` о том, что у второй регрессии красным было утверждение о СРОКЕ, а не о плане) |
| 9 | Потолок одновременных подписочных намерений обязателен; чекпойнт выбрал ФОРМУ | ✓ VERIFIED | Форма `cap-different-plan`, TTL 24 ч, записана в `.planning/STATE.md:80` вместе с тем, что ответ «потолка нет» был СНЯТ разбором |
| 10 | Ни один докстринг тронутых функций не переживает правку своего тела; обоснования ссылаются на исполняемые механизмы | ✗ FAILED | Четыре утверждения опровергнуты кодом: нейтральность окна через D-29 (ветка недостижима), «дни по УПЛАЧЕННОМУ тарифу» (код читает цену действующего), «состояние недостижимо» (опровергнуто своим же зелёным тестом), «практического последствия нет» у `0019`. См. гэп 2 |
| 11 | Ловушка порядка держится машиной И ПО ВЕЛИЧИНЕ | ✓ VERIFIED | `test_billing_payment_errors.py:1445-1450` — `sampled.attr == "expires_at"`. Утверждение must_have выполнено буквально; отдельная претензия к ПОКРЫТИЮ теста и его докстрингу отнесена к гэпу 2, а не сюда |
| 12 | Всё, что закрыли планы 05-11 и 05-13, остаётся закрытым | ✓ VERIFIED | 65 тестов `test_plan_switch.py` / `test_subscription_period.py` / `test_plan_usage.py` зелёные; `switch_is_refused` по-прежнему требует обязательный keyword-only `period_is_live`; признак снимается до сдвига |
| 13 | Ревизия `0019` аддитивна, обратима, и её место в невыкаченной очереди названо в самой ревизии | ✗ FAILED | Место названо верно («седьмой невыкаченной», D-26), `downgrade` называет потерю данных, `server_default` и `batch_alter_table` обоснованно отсутствуют. Но ПОСЛЕДСТВИЕ, названное там же (`:44-52`), неверно и уже фактического: отказывают все чтения `payments`, а не только подписочная ветка. См. гэп 2 |
| 14 | C2: обещание `DOWNGRADE_CARD_CAPTION` исполняется и на пути WR-01 раунда 4 | ✓ VERIFIED | Записанное разрешение переживает изменение строки подписки между продажей и подтверждением; регрессия `test_a_deal_sold_on_an_expired_period_is_delivered_after_the_period_revives` |
| **Гэп 2 раунда 4 — план 05-16** | | | |
| 15 | В указателе на раунды не осталось ни одной константы величины | ✓ VERIFIED | `STATE.md:92`: «принадлежит нескольким разным документам», «их число эта запись не называет намеренно», «состав и число гэпов отдаёт сам отчёт» |
| 16 | Раунд 4 адресуется собственным коммитом наравне с раундами 1-3 | ✓ VERIFIED | `5da8e82` присутствует дважды (реестр + запись распоряжений) |
| 17 | Распоряжения по всем СЕМИ находкам раунда 4 записаны | ✓ VERIFIED | Проверено грепом: `CR-01`, `WR-01`, `WR-02`, `WR-03`, `WR-04`, `IN-01`, `IN-02` — каждая присутствует со своим распоряжением (`STATE.md:94`) |
| 18 | Каждый номер квалифицирован раундом; квалификация держится инвариантом, а не константой счёта | ✓ VERIFIED | Все вхождения номеров раунда 4 несут «(раунд 4)» / «раунда 4»; 13 квалифицированных упоминаний |
| 19 | Блокер денежного пути записан со ссылкой на ОБА плана и на регрессии ИМЕНАМИ | ✓ VERIFIED | `STATE.md:124` — «Закрыто ДВУМЯ планами — ни один из них не закрывает находку в одиночку», регрессии перечислены именами, оговорки об атомарности/`0019`/D-27 не сглажены |
| 20 | Запись о `CR-01` раунда 3 больше не читается как полное закрытие | ✓ VERIFIED | `STATE.md:123` — «Но полным это закрытие не является: раунд 4 нашёл дефект ТОГО ЖЕ корня» |
| 21 | D-28, D-29 и ответ чекпойнта о форме потолка записаны в §Decisions | ✓ VERIFIED | `STATE.md:80` — все три, вместе с отвергнутыми вариантами и причиной выбора |
| 22 | Артефакт записан В ФАЙЛ, а не обещан во frontmatter | ✓ VERIFIED | Коммит `b0773f8` меняет `.planning/STATE.md`; патч-файл `05-STATE-PATCH-R4.md` на диске |
| 23 | «Current Position» и `progress` не спорят ни друг с другом, ни с ROADMAP | ✗ FAILED (partial) | `STATE.md:32,33,36` и `completed_plans: 68` объявляют 16/17 с планом 05-16 в работе; ROADMAP:306 — `17/17 plans executed`, все `[x]`, SUMMARY на диске. См. гэп 3 |
| **Гэп 1 раунда 4, четвёртый пункт — план 05-17** | | | |
| 24 | Число одновременных незакрытых подписочных намерений ограничено формой `cap-different-plan` | ✓ VERIFIED | `payment_service.py:225-244`; 7 регрессий потолка зелёные |
| 25 | Последний открытый путь случая (1) через форму более не достижим | ✓ VERIFIED | Два намерения разных тарифов ПОДРЯД через форму отвергаются; ⚠️ окно срока давности — см. гэп 2, пункт (3) |
| 26 | Случай (2) закрыт ОБЕИМИ ветвями дизъюнкции | ✓ VERIFIED | Проданный на истёкшем сроке `basic` выдаётся записанным разрешением (05-15); встречный `pro` через форму не продаётся (05-17) |
| 27 | Отказ происходит ДО вызова SDK ЮKassa | ✓ VERIFIED | Проверка — первый оператор `create_payment` (`:225`), до `_configure_yookassa()` (`:246`) и до `YooPayment.create`; держится счётчиком `create_mock.call_count == 0`, а не разбором исходника |
| 28 | Потолок не превращается в запирание | ✓ VERIFIED | `PENDING_INTENT_TTL_HOURS = 24`, терминальные статусы не мешают никогда, пакетная покупка не задевается; `_open_subscription_intents` нормализует `created_at` в Python, отсутствующее время рождения считается СВЕЖИМ (отказ по отсутствию данных закрывает, а не открывает) |
| 29 | Человек, попавший под потолок, получает СЛОВА, а не молчаливый редирект | ✓ VERIFIED | `PendingIntentCapError` → своя ветка `subscribe_to_plan` → `?error=pending` (`billing.py:380`) → закрытое множество `PAYMENT_ERROR_MESSAGES` (`:66-115`). Текст исключения на экран не уходит |
| **Производная от цели фазы** | | | |
| 30 | Операция продления и повышения не позволяет получить тариф существенно дешевле прейскуранта | ✗ FAILED | Воспроизведено поверх настоящего `_apply_extension`: 12×basic + 1×pro → план `pro`, срок 2027-02-10, уплачено 22 780 ₽ при прейскуранте 63 700 ₽. См. гэп 1 |

**Score:** 25/30 truths verified (1 present, behavior-unverified; 4 failed)

### Воспроизведение гэпа 1 — команда и вывод

Прогон поверх настоящего кода, без единой правки `app/`, без БД:

```
after 12x basic: basic 2027-01-10 12:00:00+00:00 paid 17880
rule refuses basic->pro on live period? False
after 1x pro   : pro   2027-02-10 12:00:00+00:00
paid total     : 22780
list price for 13 months of pro: 63700
```

Цены сняты из конфига тем же `_plan_price`, что читает денежный путь: `basic 1490.00`, `pro 4900.00`, `free` — цены нет.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/models/payment.py` | Колонка `switch_authorized`, nullable | ✓ VERIFIED | `Payment.__table__.columns` отдаёт 13 колонок, включая `kind`, `plan`, `switch_authorized` |
| `alembic/versions/0019_payment_switch_authorized.py` | Аддитивная обратимая ревизия, `down_revision = "0018"` | ⚠️ ЧАСТИЧНО | Ревизия верна; докстринг о последствии D-26 неверен (гэп 2) |
| `tests/test_migrations/test_0019_payment_switch_authorized.py` | Round-trip ревизии | ✓ VERIFIED | 13 passed |
| `app/services/payment_service.py` | Записанный факт + доля месяца + потолок | ⚠️ ЧАСТИЧНО | Механика верна; безусловная перезапись плана (гэп 1) и три неверных докстринга (гэп 2) |
| `app/application/billing/subscription_period.py` | `prorated_expiry` рядом с `next_expiry` | ✓ VERIFIED | Совпадает с `next_expiry` ТОЧНО при `paid == price`: `month_days` берётся у `add_one_month` от ТОЙ ЖЕ базы |
| `app/pages/billing.py` | Гард считает ответ ОДИН раз и передаёт отрицанием | ✓ VERIFIED | `switch_authorized=not switch_refused` (`:367`), литерала `True` нет |
| `app/application/billing/plan_switch.py` | Докстринг без утверждения, которого код не исполняет | ✓ VERIFIED | Утверждение «ОДНИМ НАБОРОМ ВЕЛИЧИН» снято |
| `.planning/STATE.md` | Реестр без констант величины, распоряжения раунда 4 | ⚠️ ЧАСТИЧНО | Реестр чист; §Current Position спорит с ROADMAP (гэп 3); две записи о денежном пути неверны (гэп 2) |
| `app/templates/billing/*` | Разметка раздела по макету | ✓ VERIFIED | Не менялась с `17b9bc9`; 175 тестов разметки зелёные |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `plan_card.html` / `balance.html` | `POST /billing/subscribe` | `<form method="post" action="/billing/subscribe">` | ✓ WIRED | `plan_card.html:94`, `balance.html:96` |
| `subscribe_to_plan` | `create_payment` | `switch_authorized=not switch_refused` | ✓ WIRED | `billing.py:341-370` |
| `create_payment` | `_open_subscription_intents` | Первый оператор, до SDK | ✓ WIRED | `payment_service.py:225-244`, счётчик вызовов SDK == 0 |
| `PendingIntentCapError` | Плашка раздела | `?error=pending` → `PAYMENT_ERROR_MESSAGES` | ✓ WIRED | `billing.py:380` → `:92` → `:115` |
| `payments.switch_authorized` | `_apply_extension` | Чтение записанного факта первым | ✓ WIRED | `payment_service.py:813-820` |
| `_apply_extension` | `prorated_expiry` | Ветка `if refused:` | ⚠️ WIRED, НО НЕДОСТИЖИМА ДЛЯ ПЛАТЕЖЕЙ ФОРМЫ | Гард возвращает 302 до создания платежа, поэтому `switch_authorized` всегда `True`; ветка живёт только для строк `NULL` (гэп 2) |
| `get_payment_history` | `payments` | `select(Payment)` | ✓ WIRED (репозиторий) / ✗ СЛОМАН НА ПРОДЕ | На схеме `0012` ORM-выборка выписывает несуществующие колонки (гэп 2) |
| `handle_webhook` | `payments` | `select(Payment).with_for_update()` до ветвления по `kind` | ✗ СЛОМАН НА ПРОДЕ ДЛЯ ОБОИХ ПРЕДМЕТОВ ПОКУПКИ | `payment_service.py:442-445` стоит выше `:495` и `:517` (гэп 2) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `balance.html` метры | `axes` | `plan_axes()` → одна выборка (`test_plan_axes_takes_exactly_one_query`) | Да | ✓ FLOWING |
| `balance.html` история | `payments` | `get_payment_history` → `select(Payment)` c пагинацией | Да | ✓ FLOWING |
| `balance.html` срок | `subscription.expires_at` | Строка `subscriptions`, писатель — вебхук | Да | ✓ FLOWING |
| `plan_card.html` цена | `plan.price` | `settings.parsed_plan_limits` | Да (`basic 1490.00`, `pro 4900.00`) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Прикладной слой биллинга | `pytest tests/test_application/test_subscription_period.py test_plan_switch.py test_plan_usage.py -q` | 65 passed | ✓ PASS |
| Разметка раздела и адаптивность (объявление) | `pytest tests/test_pages/test_responsive_markup.py tests/test_pages/test_billing_section.py -q` | 175 passed в 144 с | ✓ PASS |
| Потолок одновременных намерений | `pytest tests/test_services/test_payment_service.py -q -k "cap or intent or refusal or stale or same_plan"` | 7 passed | ✓ PASS |
| Round-trip ревизии `0019` | `pytest tests/test_migrations/test_0019_payment_switch_authorized.py -q` | 13 passed | ✓ PASS |
| Цены планов из конфига | `_plan_price` по каждому плану | `free None`, `basic 1490.00`, `pro 4900.00` | ✓ PASS |
| Накопление горизонта и перевод его на старший тариф | Прогон `_apply_extension` 12×basic + 1×pro | 13 месяцев Pro за 22 780 ₽ | ✗ FAIL (гэп 1) |
| Отображённые колонки `Payment` против боевой схемы `0012` | `Payment.__table__.columns` | 13 колонок, три отсутствуют на проде | ✗ FAIL (гэп 2) |
| Достижимость состояния двух намерений разных тарифов | `test_a_stale_intent_does_not_block_a_new_one` | зелёный — состояние достижимо через 24 ч | ✗ FAIL утверждения о недостижимости (гэп 2) |

Полная суита не запускалась намеренно: она зелёная на этом дереве (1722 passed, exit 0) и занимает ~27 минут; прогоны выше — точечные и относящиеся.

### Probe Execution

Конвенциональных проб (`scripts/*/tests/probe-*.sh`) в проекте нет; ни один PLAN и ни один SUMMARY фазы проб не объявляет. Пропущено с причиной.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| BILL-05 | 05-01…05-17 | Пользователь может продлить текущую подписку | ⚠️ BLOCKED (реализовано, не сдаётся) | Механика продления работает и проверена (труды 1, 5-9, 24-29). Но операция, которую требование называет, отдаёт тариф за долю прейскуранта (гэп 1), и две прохибиции BILL-05 (fairness, autonomy) остаются неразрешёнными. Отметку `Complete` возвращает верификация, а не план |
| BILL-06 | 05-03, 05-05 | Пользователь видит потребление и остаток по четырём осям | ✓ SATISFIED | `AXIS_ORDER` → четыре метра; безлимитная ось не показывается нулём. ⚠️ Прохибиция BILL-06 (transparency) остаётся неразрешённой и после гэпа 1 нарушается выразительнее |
| BILL-07 | 05-04, 05-05 | Пользователь видит историю своих платежей | ✓ SATISFIED (в репозитории) | `get_payment_history` → блок «История платежей» с датой, назначением, суммой и статусом. ⚠️ На боевой схеме `0012` весь раздел отдаёт 500 (гэп 2); это последствие D-26, а не отсутствие реализации |
| BILL-02 | — | Применение тарифных лимитов | ⚠️ ORPHANED-BY-DESIGN | Не входит в объём фазы решением D-08 и записан долгом. Не вменяется фазе, назван здесь ради полноты: именно он превращает отложенную цену гэпа 1 в списанную |

Осиротевших требований нет: `REQUIREMENTS.md:235-237` относит к Фазе 5 ровно BILL-05, BILL-06, BILL-07, и все три заявлены планами.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` в файлах, изменённых с раунда 4 (11 файлов) | — | Не найдено ни одного. Гейт долговых маркеров пройден |
| `app/services/payment_service.py` | 762-764, 833-835 | Комментарий утверждает противоположное соседней строке кода | 🛑 Blocker | Следующая «починка» по комментарию вернёт блокер раунда 4 |
| `app/services/payment_service.py` | 195-207, 209-218 | Обоснование безопасности ссылается на недостижимую ветку и на недостижимое состояние | 🛑 Blocker | Риск принят по неверной опоре |
| `tests/test_pages/test_billing_payment_errors.py` | 1385-1450 | Тест держит 1 из 4 операторов, докстринг утверждает покрытие двух ветвей | ⚠️ Warning | Половина инварианта под возросшей нагрузкой |
| `tests/…test_payment_service.py` / `…payment_errors.py` | 703, 1889 | Имя теста обещает больше, чем тело держит | ⚠️ Warning | Ложная уверенность при поиске покрытия по имени |
| `.planning/STATE.md` | 32, 33, 36 | Константа величины в §Current Position | ⚠️ Warning | Гэп 3 |

### Что выдержало враждебное чтение

Записано, чтобы раунд 6 не перепроверял это заново — проверено мной, а не принято по отчёту ревью:

- `prorated_expiry` совпадает с `next_expiry` ТОЧНО при `paid == price`: длина месяца берётся у `add_one_month` от ТОЙ ЖЕ базы, а не константой 30.
- Нижняя граница в один день не декоративна: целая часть доли от рубля при цене 4900 равна нулю.
- Арифметика денег целиком в `Decimal`; `paid` разбирается в `try` и при отказе разбора даёт откат к полному месяцу, а не 5xx на уведомлении.
- Потолок поднимается ДО `_configure_yookassa()` и до `YooPayment.create`, и это держится счётчиком вызовов, а не разбором исходника.
- `PendingIntentCapError` — свой тип, своя ветка, свой код причины; текст исключения на экран не уходит.
- `_open_subscription_intents` нормализует `created_at` в Python, а не в SQL; отсутствующее время рождения считается СВЕЖИМ.
- Ревизия `0019` без `server_default` и без `batch_alter_table` — оба решения обоснованы верно; `downgrade` называет потерю данных.
- Реестр раундов в `STATE.md` очищен от констант величины и несёт распоряжения по всем семи находкам раунда 4.

### Gaps Summary

Пять раундов подряд денежный дефект находится в НОВОЙ ФОРМУЛИРОВКЕ, а не в новом месте, и раунд 5 добавляет к этому наблюдению два вывода, которые сами по себе важнее любого отдельного гэпа.

**Первый.** Каждый раунд закрывает названное направление честно и полно — это подтверждено прогонами и здесь, и в раунде 4, — а следующий раунд находит соседнее направление того же корня. Корень один: `subscription.plan` — скаляр без истории, `subscription.expires_at` — величина без верхней границы, а решений, каждое из которых верно в одиночку, вокруг них уже пять. Гэп 1 этого раунда — не новый дефект, а первое ПРОИЗВЕДЕНИЕ трёх старых, и ни одно из них по отдельности исправлять не нужно. Пока верхней границы нет ни у горизонта, ни у конверсии, шестой раунд найдёт шестую формулировку.

**Второй, и он тревожнее.** Второй раунд подряд блокирующая находка приходит из УТВЕРЖДЕНИЯ, которого код не исполняет. В раунде 4 это был докстринг («ОДНИМ НАБОРОМ ВЕЛИЧИН»); в раунде 5 неверных утверждений четыре, и два из них лежат в `.planning/STATE.md` — документе, ПО КОТОРОМУ ПРИНИМАЮТСЯ РЕШЕНИЯ. Риск неатомарности потолка принят ссылкой на механизм, который на этом пути не исполняется ни разу. Гейт выката принимается по последствию, которое уже фактического: на боевой схеме отказывает не «новая функция», а КАЖДОЕ чтение таблицы платежей, включая приём пакетных платежей, работавший до этой фазы. Фаза не вносила решение D-26 и не обязана его отменять — но она обязана назвать его настоящую цену, иначе выкат состоится по неверному факту.

Гэп 3 — низкой тяжести и приведён ради полноты: константа величины, вычищенная планом 05-16 из реестра раундов, уцелела в §Current Position того же файла и уже разошлась с ROADMAP на один план.

Что НЕ вменяется фазе и здесь не повторяется: отсутствие применения тарифных лимитов (D-08, долг BILL-02); неатомарность потолка как ТАКОВАЯ (названа ограничением честно — неверна только оговорка о её нейтральности); место ревизии `0019` в невыкаченной очереди (D-26); неподтверждённая подписка на `payment.canceled` (D-27); отложенные с распоряжениями `WR-02`/`WR-03`/`WR-04` раунда 4; отсутствие RED-коммитов у планов 05-15 и 05-17 (раскрыто исполнителями самостоятельно, гейт неблокирующий, записано во frontmatter).

---

_Verified: 2026-08-17T13:05:00Z_
_Verifier: Claude (gsd-verifier)_
_Round: 5 — раунд 1 `4989f7a`, раунд 2 `1d29360`, раунд 3 `164d210`, раунд 4 `5da8e82`_
