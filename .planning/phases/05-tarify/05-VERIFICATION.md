---
phase: 05-tarify
round: 6
verified: 2026-08-17T19:40:00Z
status: gaps_found
score: 17/20 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_round: 5
  previous_status: gaps_found
  previous_score: 25/30
  previous_verified: 2026-08-17T13:05:00Z
  gaps_closed:
    - "Гэп 3 раунда 5 (константа величины в §Current Position) ЗАКРЫТ ПОЛНОСТЬЮ и проверен не по SUMMARY. `.planning/STATE.md` §Current Position не называет ни одной константы величины: строка `Plan: N of M` упразднена, `Progress:` говорит о фазах milestone, а не о планах фазы, и записан ЯВНЫЙ запрет с причиной («всякая константа величины в этой секции устаревает от появления следующего плана») плюс имя единственного источника счёта. Спора с ROADMAP не осталось: frontmatter `total_plans: 73 / completed_plans: 73` сходится и с ROADMAP §Phase 5 (`21/21 plans executed`), и с суммой по фазам (13+15+12+12+21=73). Записано и то, ПОЧЕМУ секция расходится с шаблоном `state.md`, с указанием коммита-регрессии инструмента."
    - "Гэп 2 раунда 5 закрыт ПО ВСЕМ ЧЕТЫРЁМ НАЗВАННЫМ УТВЕРЖДЕНИЯМ — проверено чтением кода, а не по отчётам исполнителей. (1) Оговорка о нейтральности окна гонки (`payment_service.py:243-258`) больше не ссылается на недостижимую ветку D-29: назван настоящий исход («обе строки несут `True`, доля месяца не исполняется, исход решает ПОРЯДОК подтверждения») и назван НЕСДЕЛАННЫМ механизм, который дал бы нейтральность свойством (частичный уникальный индекс). Та же формулировка в `.planning/STATE.md:145`. (2) Оба абзаца о цене доли месяца (`:846-848`, `:940-950`) называют `paid / price(subscription.plan)` — цену ДЕЙСТВУЮЩЕГО плана, ровно то, что читает `_plan_price(subscription.plan)` строкой ниже; прежнее «по УПЛАЧЕННОМУ тарифу» не сохранилось нигде. (3) Докстринг `create_payment:210-232` называет ОБА окна потолка, включая срок давности, и ссылается на опровергающий зелёный тест ПО ИМЕНИ; сам тест получил докстринг о том, что он доказывает, а `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form` ПЕРЕИМЕНОВАН в `test_a_second_intent_of_another_plan_is_refused_while_the_first_is_fresh` — по тому, что держит тело. (4) Последствие D-26 названо фактическим и в `0019` (`:51-95`), и в `.planning/STATE.md:98`: отказывают ВСЕ чтения `payments`, включая пакетный вебхук; выкат кода и выкат очереди разделить нельзя. Номера строк механизма перепроверены планом 05-20 и исправлены (`443-448`/`:496`/`:518`)."
    - "Машинная проверка соответствия модели и головной ревизии СУЩЕСТВУЕТ и имеет зубы: `tests/test_migrations/test_model_matches_head.py` + `test_deploy_applies_migrations_before_serving.py` — 6 passed, включая негативный контроль `missing_columns(mapped, mapped - {switch_authorized}) == ['switch_authorized']`. Схема строится НАСТОЯЩИМ Alembic до `head`; докстринг прямо называет, чего проверка НЕ доказывает (о боевой схеме она не знает ничего)."
    - "AST-ловушка порядка переписана на `ast.walk` и держит КАЖДЫЙ оператор, двигающий срок, а не один из четырёх; множество имён ВЫВОДИТСЯ из импорта модуля отсчёта, поэтому оператор, добавленный планом 05-18 (`payment_service.py:1064`), попал под инвариант сам. Рядом два негативных контроля над синтетическими исходниками (`test_the_order_helper_reports_a_move_placed_above_the_sample`, `..._hidden_inside_a_branch`). Прогон зелёный."
    - "Денежный дефект гэпа 1 раунда 5 ЗАКРЫТ В НАЗВАННОМ НАПРАВЛЕНИИ, и это воспроизведено МНОЙ поверх настоящего `_apply_extension` без единой правки `app/`: 12×`basic` + 1×`pro` при читаемых ценах даёт теперь 140 дней Pro (журнал: `remainder_days=365 → converted_days=110`), а не 396 дней, как в раунде 5. Защитная семантика не сломана: повышение при остатке в 25 дней даёт 7 дней Pro плюс месяц (38 дней от сегодня), то есть остаток НЕ сгорает. Решение владельца записано номером D-30 в `05-CONTEXT.md` по образцу D-28/D-29, с отвергнутыми вариантами и причиной выбора."
    - "Распоряжения по ВСЕМ ДЕВЯТИ находкам код-ревью раунда 5 записаны и КВАЛИФИЦИРОВАНЫ РАУНДОМ (проверено грепом: `CR-01`, `CR-02`, `WR-01`…`WR-05`, `IN-01`, `IN-02` — каждая со строкой «(раунд 5)»). Ни одна прежняя запись не стёрта: устаревшие ПЕРЕСКАЗАНЫ с названной причиной. `COVERAGE.md` сверена после раунда 5, а не перегенерирована."
  gaps_remaining:
    - "Гэп 1 раунда 5 закрыт НЕ ЦЕЛИКОМ: утечка выжила в ветке отката при нечитаемой цене, где верхней границы у горизонта по-прежнему нет. Воспроизведено мной численно — см. гэп 1 раунда 6."
    - "Гэп 2 раунда 5 закрыт по четырём НАЗВАННЫМ утверждениям, но КЛАСС воспроизведён заново в коде, написанном планом 05-18, и в двух прежних объявлениях, тронутых этим раундом — см. гэп 2 раунда 6."
  regressions: []
gaps:
  - truth: "Величина предоплаченного горизонта, которой управляет ПОКУПАТЕЛЬ, получает верхнюю границу НА ДЕНЕЖНОМ ПУТИ — то есть на всех его ветках, а не только там, где обе цены читаются (must_have №1 плана 05-18; прохибиция того же плана verification: test — «MUST NOT отдавать старший тариф на весь накопленный предоплаченный горизонт за цену ОДНОГО месяца»)"
    status: failed
    reason: "ВОСПРОИЗВЕДЕНО НЕЗАВИСИМО, поверх настоящего `_apply_extension`, без единой правки `app/`. В ветке отката `payment_service.py:1021-1036` при `price_from is None or price_to is None` переменная `base` остаётся связанной с `subscription.expires_at` (`:1014`), после чего `:1064` пишет `next_expiry(base, now)`, а `:1065` — `subscription.plan = db_payment.plan`. Это ДОСЛОВНО поведение до плана 05-18, то есть гэп 1 раунда 5 целиком. Числа совпадают с раундом 5 до дня: 12 подтверждённых `basic` плюс один `pro` дают срок 2027-02-10 при плане `pro` — 396 дней Pro за 22 780 ₽. Для сравнения, на той же машине и в том же прогоне ветка КОНВЕРСИИ даёт 140 дней. Разница между веткой и веткой — 256 дней старшего тарифа, и растёт она линейно с горизонтом, которым управляет покупатель. ДВА ВХОДА В ВЕТКУ, И ОДИН ИЗ НИХ НЕ ТРЕБУЕТ НИ ГОНКИ, НИ ПРАВКИ КОДА. (а) `price_to is None` — план платежа выпал из `PLAN_LIMITS` между продажей и подтверждением; проверено прогоном: при `PLAN_LIMITS` без `pro` тот же сценарий отдаёт 396 дней и пишет `unreadable=paid_plan_price`. (б) `price_from is None` — действующий план подписки не читается; сюда попадает `free` ПО ПОСТРОЕНИЮ, потому что `_plan_price` возвращает `None` при цене «не больше нуля» (`:814`), а `free` объявлен `\"0.00\"` (`config.py:75`); проверено прогоном: подписка на `free` с накопленным живым горизонтом плюс один платёж `pro` отдаёт те же 396 дней и пишет `unreadable=price`. Перечень тарифов правится ОКРУЖЕНИЕМ — это утверждает сам код (`:794-796`), — поэтому вход (а) достижим одной правкой `.env` без единой строки кода, и после неё ветку берёт КАЖДОЕ повышение с плана, чей идентификатор переименован или снят. ЧЕСТНО О ДОСТИЖИМОСТИ, ЧТОБЫ ТЯЖЕСТЬ НЕ БЫЛА ЗАВЫШЕНА: через интерфейс СЕГОДНЯ ветка не берётся — форма сверяет план с конфигом и отвергает `free` (`billing.py:318`), а строка подписки на `free` с накопленным горизонтом требует подписочных платежей с `plan IS NULL`, которых `create_payment` не заводит. То есть это не открытая дыра в проде, а СНЯТАЯ ГРАНИЦА на денежном пути, восстанавливающаяся от операционного действия. ВЕТКА НЕ ПОКРЫТА НИ ОДНИМ ТЕСТОМ: `subscription_prorating_skipped` встречается в суите ровно один раз (`test_billing_payment_errors.py:2096`) и относится к ветке ОТКАЗА (`test_a_price_that_cannot_be_read_falls_back_to_the_whole_month`); значения `unreadable=\"paid_plan_price\"` и ключа `subscription_remainder_converted` не утверждает ни один тест; `converted_remainder` не импортирован ни одним тестовым файлом вовсе (грепом по `tests/` — ноль). ЧТО ЗАПИСАНО И ЧЕГО НЕ ЗАПИСАНО: откат НАЗВАН и в D-30 (`05-CONTEXT.md:93`), и в `05-18-SUMMARY.md:273-275` — но назван словами «откат к прежнему поведению» и «Придумывать конверсию бесплатного остатка этот план не вправе», то есть как БЕЗОБИДНАЯ мера против 5xx. ЦЕНА отката не названа нигде, а она равна цене гэпа, который этот же план закрывал, и посчитана в том же абзаце D-30 (395 дней за 22 780 ₽). Это ровно тот способ отказа, за который фаза получила раунды 4 и 5: принятая цена решения и фактическая цена расходятся, а решение принимается по первой. Отдельно: D-30 называет журнальный ключ отката `subscription_remainder_converted`, тогда как код пишет там `subscription_prorating_skipped` (`:1030`) — `subscription_remainder_converted` пишется в ПРОТИВОПОЛОЖНОЙ ветке (`:1050`). Совпадает с `CR-01 (раунд 6)` `05-REVIEW.md`; механизм и числа подтверждены мной независимо прогоном, а не приняты по отчёту."
    artifacts:
      - path: "app/services/payment_service.py"
        issue: "Строки 1021-1036: при нечитаемой цене `base` остаётся `subscription.expires_at`, и строки 1064-1065 переводят ВЕСЬ накопленный горизонт на план платежа. Верхней границы в этой ветке нет ни арифметической, ни входной, ни временно́й"
      - path: "app/services/payment_service.py"
        issue: "Строка 814 (`return price if price > 0 else None`) × `config.py:75` (`free` ценой `\"0.00\"`) — цена плана `free` нечитаема ПО ПОСТРОЕНИЮ, а не по случайности. Любая строка подписки на `free` с живым сроком берёт ветку отката на первом же платном повышении"
      - path: "app/services/payment_service.py"
        issue: "Строки 743-754 (`_extend_subscription`, первая вставка): `plan=db_payment.plan or \"free\"` при `plan IS NULL` кладёт человека на `free` с оплаченным месяцем, а `:905-909` продлевает такую строку дальше, не трогая план. Это единственный сегодня производитель входа (б); симметричного пакетному (`:548-555`) гарда у подписочной ветки нет (`WR-05 (раунд 6)`)"
      - path: "tests/test_pages/test_billing_payment_errors.py"
        issue: "Ветка конверсии покрыта тремя регрессиями, ветка ОТКАТА в ней — ни одной. `test_a_price_that_cannot_be_read_falls_back_to_the_whole_month` (`:2065`) относится к ветке отказа и подменяет `unreadable=\"price\"` там; значение `\"paid_plan_price\"`, которое может испустить ТОЛЬКО эта ветка, не утверждается нигде"
      - path: ".planning/phases/05-tarify/05-CONTEXT.md"
        issue: "D-30 (строка 93) называет откат при нечитаемой цене, но не называет его ЦЕНУ — при том, что цена посчитана числами в том же абзаце; и называет журнальным ключом отката `subscription_remainder_converted`, которого код в этой ветке не пишет"
    missing:
      - "Верхняя граница в ветке отката. Дешевле всего — переносить не более одного календарного месяца остатка (`base = min(normalize_utc(subscription.expires_at), add_one_month(normalize_utc(now)))`): обещание «оплаченный остаток не сгорает» держится, а величина, которой управляет покупатель, перестаёт быть неограниченной. Исключение здесь по-прежнему недопустимо — 5xx на уведомлении запускает цикл повторов ЮKassa (T-05-104)"
      - "Регрессия «повышение при нечитаемой цене ПЛАНА ПЛАТЕЖА (плана нет в `PLAN_LIMITS`) не переносит весь горизонт» — обязана падать на текущем коде, и обязана утверждать `unreadable=\"paid_plan_price\"`, значение, которое сегодня не проверяет ничто"
      - "Регрессия «повышение с подписки на `free` с живым горизонтом не переносит весь горизонт» — обязана падать на текущем коде"
      - "Прямые модульные тесты `converted_remainder` в `tests/test_application/test_subscription_period.py` по образцу таблиц, которые этот файл уже держит для `prorated_expiry`: истёкший и отсутствующий `current` дают сегодня; живой остаток никогда не конвертируется в ноль дней; naive и aware дают один ответ; `old_price == new_price` возвращает остаток. Сегодня функция не импортирована этим файлом вовсе (`IN-01 (раунд 6)`)"
      - "Приведение записанной цены D-30 и `05-18-SUMMARY.md` к тому, что код фактически отдаёт в ветке отката, — либо ограничение кода до записанной цены. Расхождение не должно остаться ни в одну сторону, и это дословно тот же пункт, который гэп 1 раунда 5 требовал для варианта `upgrade-only`"
      - "Исправление журнального ключа в D-30 (`subscription_prorating_skipped`, а не `subscription_remainder_converted`) либо различающее поле `stage=` у двух испусканий одного ключа (`IN-04 (раунд 6)`)"
  - truth: "Ни одно объявление в коде — докстринг тронутой функции или объявленный инвариант модуля — не утверждает того, чего код рядом с ним не делает (труд №10 раунда 5, must_have №6 плана 05-15, весь заряд плана 05-19; тот же класс, за который фаза получила раунды 4 и 5)"
    status: failed
    reason: "Четыре НАЗВАННЫХ утверждения раунда 5 действительно исправлены — проверено мной по коду и записано выше в `gaps_closed`. Но класс воспроизведён ЗАНОВО, причём в коде, написанном планом 05-18, и в двух объявлениях, тронутых этим же раундом. (1) ДЛИНА МЕСЯЦА В `converted_remainder`. Докстринг `subscription_period.py:231-236` утверждает: «ДЛИНА МЕСЯЦА БЕРЁТСЯ У `add_one_month` ОТ ТОЙ ЖЕ БАЗЫ, А НЕ КОНСТАНТОЙ 30 — … константа разошлась бы с `next_expiry` в феврале и в декабре». Для ЭТОЙ функции утверждение ложно: `month_days` стоит знаменателем стоимости остатка и множителем внутри `prorated_days`, и два вхождения сокращаются. Проверено прогоном настоящей функции — ответ побайтово один при `month_days` 28, 30 и 31: остаток 365 дней даёт 110 дней при базах 2026-01-31, 2026-02-01, 2026-03-01 и 2026-04-01, и ровно столько же даёт `int(остаток_в_днях · 1490 / 4900)`, где длины месяца нет вовсе. Собственная регрессия фазы (`test_billing_payment_errors.py:2310`) уже записана в СОКРАЩЁННОЙ форме, без члена длины месяца, то есть код и его тест согласны между собой и оба спорят с абзацем. Живой риск, а не косметика: `add_one_month(now_utc)` и обратный ход через `timedelta` (`:249-254`) — мёртвое вычисление, которое следующий читатель «починит» в неверную сторону, поверив, что календарь здесь значит. Совпадает с `WR-01 (раунд 6)`. (2) «ПЛАН ТОЛЬКО ПОВЫШАЕТСЯ». Два объявления называют это инвариантом: `payment_service.py:688` («Но ПЛАН при этом только повышается») и `plan_switch.py:70` («срок двигается всегда, план только растёт»). ВОСПРОИЗВЕДЕНО ПРОГОНОМ: подписка `pro` с живым сроком, приходит подтверждение платежа `basic` с записанным разрешением `True` (достижимо через окно СРОКА ДАВНОСТИ, которое сам проект называет детерминированным и держит зелёным тестом) — итог `plan == 'basic'`. Пользователь ПОНИЖЕН, и следа нет: ветка отказа не бралась, поэтому `subscription_plan_preserved` не пишется. Деньги при этом примерно сохраняются конверсией (30 дней Pro → 98 дней Basic), поэтому это дефект корректности и наблюдаемости, а не утечка, — но объявленное правило `upgrade-only` не исполняется, а объявления утверждают, что исполняется. Совпадает с `WR-03 (раунд 6)`. (3) «ИСТЁКШИЙ СРОК СНИМАЕТ ОТКАЗ НА ОБЕИХ СТАДИЯХ». Докстринг `_extend_subscription:712-720` утверждает это БЕЗУСЛОВНО и добавляет, что на этом пути «следа `subscription_plan_preserved` НЕТ». ВОСПРОИЗВЕДЕНО ПРОГОНОМ: платёж с записанным `switch_authorized = False` на ИСТЁКШЕМ сроке оставляет план прежним (`pro` вместо уплаченного `basic`) и пишет `subscription_plan_preserved` — потому что `:920-921` читает записанный ответ, ни разу не спросив `period_is_live`. Значение `False` через форму сегодня недостижимо (гард возвращает 302 раньше) — но `app/models/payment.py:40-46` и `0019:33-36` оба ЯВНО требуют, чтобы колонка умела его выразить, то есть проект сам объявляет это значение живым контрактом. Совпадает с `WR-02 (раунд 6)`. Все три подтверждены мной прогоном либо численно, а не приняты по отчёту ревью."
    artifacts:
      - path: "app/application/billing/subscription_period.py"
        issue: "Строки 231-236: абзац о длине месяца утверждает свойство, которого у функции нет — величина сокращается; проверено на 28, 30 и 31 дне. Строки 249-254 — соответствующее мёртвое вычисление"
      - path: "app/services/payment_service.py"
        issue: "Строка 688 и `plan_switch.py:70`: инвариант «план только растёт» опровергнут прогоном через окно срока давности; понижение происходит БЕЗ записи в журнал"
      - path: "app/services/payment_service.py"
        issue: "Строки 712-720 против 920-921: «истёкший срок снимает отказ на ОБЕИХ стадиях» неверно для записанного `False` — записанный ответ читается без `period_is_live`. Строки 743-754 (первая вставка) не читают `switch_authorized` вовсе, то есть один и тот же записанный отказ соблюдается при наличии строки подписки и игнорируется при её отсутствии"
      - path: "app/application/billing/subscription_period.py"
        issue: "Строки 245-247 (`if base <= now_utc: return now_utc`) — недостижимая ветка: `countdown_base` (`:117-121`) уже возвращает `now_utc` в этом случае. Читается как второе независимое правило истечения рядом с настоящим — ровно та дублирующая конструкция, ради устранения которой модуль заведён (`IN-03 (раунд 6)`)"
    missing:
      - "Привести абзац `subscription_period.py:231-236` к тому, что делает функция: длина месяца в ответ НЕ входит и это свойство, а не упущение (сокращается); единица месяца остаётся здесь затем, чтобы деление денег на цену жило одним объявлением. Либо — если задумывалась зависимость от календаря — это правка ПОВЕДЕНИЯ с двумя разными длинами месяца, а не правка комментария"
      - "Либо принудить `upgrade-only` на стадии применения (добавить член `switch_is_refused(subscription.plan, db_payment.plan, period_is_live=True)` к записанному разрешению), либо снять предложение «план только растёт» из ОБОИХ объявлений и назвать исход понижения в докстринге `_extend_subscription`"
      - "Подчинить записанный ответ тому же правилу истечения, что и ветку правила (`refused = period_is_live and not db_payment.switch_authorized`), либо переписать абзац `:712-720`, назвав, что безусловность относится только к ветке правила"
      - "Назвать позицию ветки первой вставки по отношению к записанному отказу явно (отказывать нечему, прежнего плана нет) — или консультировать его там"
      - "Снять недостижимую ветку `subscription_period.py:245-247` либо заменить её комментарием о том, что зажим — работа `countdown_base`"
deferred: []
behavior_unverified_items:
  - truth: "Раздел тарифов пригоден к использованию на мобильных ширинах (критерий 4 ROADMAP)"
    test: "Открыть /billing в браузере на ширине 375px с реальными данными: три карточки планов, четыре метра, история платежей длиннее экрана, блок пакетов сообщений"
    expected: "Карточки планов складываются в одну колонку; подписи колонок истории ([data-cell-label]) едут вместе со значениями; ни один блок не требует горизонтальной прокрутки; кнопки оплаты нажимаемы пальцем (порог проекта 44px)"
    why_human: "Браузерного/e2e-харнесса в проекте нет (playwright/selenium/puppeteer отсутствуют в pyproject.toml). Все относящиеся тесты читают CSS и HTML как ТЕКСТ и проверяют ОБЪЯВЛЕНИЕ правила, а не отрисовку. Разметка и стили не менялись с `17b9bc9` (план 05-10) — проверено `git log -- app/templates/ app/static/css/app.css`, — поэтому пункт переносится без изменений ШЕСТОЙ раунд подряд"
human_verification:
  - test: "Открыть /billing на 375px и пройти раздел глазами (см. behavior_unverified_items выше)"
    expected: "Одна колонка, никакой горизонтальной прокрутки, кнопки нажимаемы"
    why_human: "Браузерного харнесса в проекте нет. Шестой раунд подряд; объявление правила проверено машиной, отрисовка — нет"
  - test: "Настоящий платёж в тестовом магазине ЮKassa: форма → confirmation_url → возврат → приход уведомления → сдвинутый срок на /billing"
    expected: "Срок НЕ двигается до прихода уведомления и двигается после него — на величину, которую называет D-30 (конверсия остатка), а не на календарный месяц"
    why_human: "Боевого доступа к API ЮKassa у исполнителя нет, всё покрытие на моках. Backstop-пункт must_haves планов 05-01, 05-06, 05-11, 05-13, 05-15 и 05-18. При решении D-26 недостижим на проде"
  - test: "Первое настоящее уведомление ЮKassa после выката проходит гард источника"
    expected: "Боевой nginx действительно проставляет X-Real-IP именно на маршруте вебхука, и настоящее уведомление получает 200, а не 403"
    why_human: "Backstop-пункт must_haves плана 05-07. Поведение боевого nginx кодом репозитория не проверяется"
  - test: "Первый настоящий выкат после прохождения очереди ревизий поднимает `web` только после успешной миграции, и раздел «Тарифы» на проде отвечает 200"
    expected: "`alembic upgrade head` проходит до `exec`, и `get_payment_history` не падает `UndefinedColumn`"
    why_human: "Backstop-пункт must_haves плана 05-20. Суита строит схему в памяти и о проде не знает; это решение владельца о выкате (D-26), а не свойство репозитория"
  - test: "Отменённый платёж получает статус «отклонён» на экране истории"
    expected: "Строка платежа показывает бейдж «отклонён», а не «в обработке»"
    why_human: "Заблокирован: владелец не подтвердил подписку на событие payment.canceled в кабинете ЮKassa (D-27). Состав событий задаётся вне репозитория"
  - test: "Человек, чей срок после ПОВЫШЕНИЯ стал короче ожидаемого, читает исход как объявленное правило, а не как ошибку"
    expected: "«25 дней Basic дали 7 дней Pro» читается как доплата, а не как отнятые дни. Сегодня на экране об этом не сказано НИЧЕГО: единственный след конверсии — журнальный ключ `subscription_remainder_converted`, видимый только нам"
    why_human: "Backstop-пункт must_haves плана 05-18. D-30 меняет наблюдаемую пользователем величину, а интерфейсного ответа на это изменение планы фазы сознательно не заводили"
  - test: "Строка отказа по потолку одновременных намерений читается как «предыдущая оплата ещё не завершена, подождите результата»"
    expected: "Не читается ни как «с вас списали и не зачли», ни как «кнопка сломана»"
    why_human: "Backstop-пункт must_haves плана 05-17. Механика проверена — код причины `pending` входит в закрытое множество PAYMENT_ERROR_MESSAGES (`billing.py:66-115`, редирект `:380`)"
  - test: "Пользователь, вернувшийся с ЮKassa до прихода вебхука, понимает, что платёж ещё в обработке"
    expected: "Человек читает строку истории со статусом «в обработке» как «деньги в обработке», а не как «оплата не прошла»"
    why_human: "Backstop-пункт must_haves планов 05-04 и 05-10"
  - test: "Сообщение об отказе оплаты («ЮKassa не создала платёж») прочитывается как «попробуйте ещё раз»"
    expected: "Не читается как «с вас списали и не зачли»"
    why_human: "Backstop-пункт must_haves плана 05-10"
  - test: "Владелец риска WR-07 (раунд 3) — монтирование /var/run/docker.sock в контейнер web, терминирующий вебхук ЮKassa — назван поимённо"
    expected: "В .planning/STATE.md вместо «⟨не назначен — назначает владелец проекта⟩» стоит имя, либо принято одно из двух распоряжений (вынос управления контейнерами в отдельный воркер / фильтрующий прокси перед сокетом)"
    why_human: "must_have №8 плана 05-14 требовал ВЛАДЕЛЬЦА и распоряжение. Распоряжение записано и проверено, владелец — решение человека. Шестой раунд без имени"
  - test: "ПРОХИБИЦИЯ (test-tier, ПРИНУЖДЕНИЕ ПОДКЛЮЧЕНО ЧАСТИЧНО, плана 05-18): «MUST NOT отдавать старший тариф на весь накопленный предоплаченный горизонт за цену ОДНОГО месяца: величина, которой управляет покупатель, не имеет права быть неограниченной на денежном пути»"
    expected: "Владелец решает: ограничить перенос в ветке отката или записать эту ветку принятым риском, назвав её цену числом"
    why_human: "unverified-prohibition — human review recommended. Объявлена verification: test; принуждение есть в ветке КОНВЕРСИИ (три зелёные регрессии) и отсутствует в ветке ОТКАТА, где прохибиция воспроизводимо нарушается (гэп 1). Прохибиция без принуждения на ветке зелёной не считается"
  - test: "ПРОХИБИЦИЯ (test-tier, БЕЗ ПОДКЛЮЧЁННОГО ПРИНУЖДЕНИЯ, плана 05-17): «MUST NOT позволять двум разрешённым намерениям разных тарифов висеть одновременно»"
    expected: "Владелец решает, считать ли прохибицию соблюдённой при существующем сроке давности — или закрывать окно снятием намерения с оплаты при истечении TTL"
    why_human: "unverified-prohibition — human review recommended. Окно СРОКА ДАВНОСТИ остаётся открытым и опровергается собственным зелёным тестом проекта `test_a_stale_intent_does_not_block_a_new_one`; план 05-19 сделал факт видимым в трёх местах, не изменив сам факт. Раунд 6 добавляет к цене этого окна ВТОРОЕ следствие: через него достижимо молчаливое ПОНИЖЕНИЕ плана (гэп 2, пункт 2)"
  - test: "ПРОХИБИЦИИ (test-tier, БЕЗ ПОДКЛЮЧЁННОГО ПРИНУЖДЕНИЯ, планов 05-16, 05-19, 05-20 и 05-21 — об операционных документах и об объявлениях в коде)"
    expected: "Владелец решает, нужен ли машинный гейт на `.planning/STATE.md` и на класс «объявление против кода» — или прохибиции этого класса переводятся в judgment-tier честно"
    why_human: "unverified-prohibition — human review recommended. Все объявлены verification: test, но ни одного теста, проверяющего `.planning/STATE.md` или сверяющего докстринг с поведением, в суите не существует (грепом по `tests/` — ноль). Фактически при чтении глазами: документные прохибиции соблюдены (см. gaps_closed), а прохибиции класса «объявление против кода» НАРУШЕНЫ заново — см. гэп 2"
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, status: unresolved в планах 05-11, 05-13, 05-18): «MUST NOT превращать оплаченный остаток старшего тарифа в дни младшего без объявленного правила и без слова пользователю до нажатия кнопки» (BILL-05, fairness)"
    expected: "Владелец решает, считать ли прохибицию соблюдённой после D-30"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): правило теперь ОБЪЯВЛЕНО (D-30) и симметрично — остаток конвертируется по отношению цен в обе стороны, — но СЛОВА пользователю до нажатия кнопки по-прежнему нет: ни карточка плана, ни экран раздела не называют, во что превратится остаток. Обратное направление, вскрытое раундом 5, закрыто в ветке конверсии и открыто в ветке отката"
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, status: unresolved в планах 05-11 и 05-13): «MUST NOT показывать человеку строку истории с одним тарифом рядом с бейджем другого действующего тарифа без единого слова о том, что оплата продлила срок, а не сменила тариф» (BILL-06, transparency)"
    expected: "Владелец решает, нужен ли интерфейсный ответ на расхождение уплаченного и действующего тарифа — и в каком объёме"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): НАРУШЕНА. Планы 05-11 и 05-13 сознательно вынесли интерфейсную работу за границу объёма, и D-30 добавил к этому расхождению вторую величину — сконвертированный остаток"
  - test: "ПРОХИБИЦИЯ (judgment-tier, НЕ ЗЕЛЁНАЯ, status: unresolved в плане 05-13): «MUST NOT запирать пользователя в тарифе на всё время жизни аккаунта — а если запирать, то сказать об этом теми же словами, которыми сегодня обещано обратное» (BILL-05, autonomy)"
    expected: "Владелец подтверждает, что вариант apply-after-expiry снял запирание в объёме, который он считает достаточным"
    why_human: "unverified-prohibition — human review recommended. Автономный вердикт LLM-судьи (НЕ АВТОРИТЕТНЫЙ): в основном пути СНЯТА. Остаётся узкий путь WR-03 раунда 4 (план без ранга, записанный в подписку) — отложен с распоряжением, владелец не назначен"
tdd_gate:
  status: advisory_disclosed
  note: "Планы 05-15 и 05-17 не имеют RED-коммита (раскрыто их исполнителями и записано раундом 5). Все четыре плана ЭТОГО прогона (05-18…05-21) держали RED→GREEN там, где вводили код. Гейт неблокирующий, MVP mode выключен"
---

# Phase 5: Тарифы — Verification Report (раунд 6)

**Phase Goal:** Пользователь понимает, сколько ресурса тарифа он израсходовал, за что платил и может продлить подписку не выходя из раздела.
**Verified:** 2026-08-17T19:40:00Z
**Status:** gaps_found
**Re-verification:** Да — после закрытия гэпов раунда 5 (планы 05-18, 05-19, 05-20, 05-21)
**Round:** 6 — раунд 1 `4989f7a`, раунд 2 `1d29360`, раунд 3 `164d210`, раунд 4 `5da8e82`, раунд 5 `ec9e34a`…`87d8fa8`. Номера находок между раундами ПЕРЕНАЗНАЧЕНЫ; каждая ссылка ниже квалифицирована раундом.

## Одной фразой

Три гэпа раунда 5 закрыты не на словах: гэп 3 закрыт полностью, гэп 2 закрыт по всем четырём названным утверждениям, гэп 1 закрыт в главном пути — и это подтверждено моим собственным прогоном настоящего кода, где 12×`basic` + 1×`pro` теперь даёт 140 дней Pro вместо 396. **И тот же прогон показывает, что в ветке отката при нечитаемой цене выживают ровно прежние 396 дней** — то есть граница поставлена на одной ветке из двух, а вторая не покрыта ни одним тестом. Отдельно: класс «объявление утверждает то, чего код не делает», ради которого писался план 05-19, воспроизведён ЗАНОВО в коде, написанном планом 05-18 (длина месяца в `converted_remainder` сокращается — проверено на 28, 30 и 31 дне), и в двух прежних инвариантах («план только растёт» опровергнут прогоном: пользователь понижается молча).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| **Критерии ROADMAP** | | | |
| 1 | Пользователь может продлить текущую подписку и увидеть обновлённый срок | ✓ VERIFIED | Форма `POST /billing/subscribe` в карточке текущего плана (`plan_card.html:97`, подпись «Продлить») и у истёкшего срока (`balance.html:122`); срок печатается `balance.html:100`. Регрессионная проверка: разметка не менялась с `17b9bc9`. ⚠️ «Сразу» ограничено приходом уведомления — человеческий пункт ниже |
| 2 | Пользователь видит потребление и остаток по всем четырём осям | ✓ VERIFIED | `AXIS_ORDER` — `plan_usage.py:64`, цикл `:183`; метры через `usage_meters.html`. 96 тестов прикладного и сервисного слоя биллинга зелёные |
| 3 | Пользователь видит историю платежей с датой, суммой и статусом | ✓ VERIFIED | `get_payment_history` / `count_payments` (`billing_service.py:208`, `:239`) → блок «История платежей» `balance.html:206`, строка `payment_row` `:224`. Данные текут из БД |
| 4 | Раздел тарифов пригоден к использованию на мобильных ширинах | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Правило ОБЪЯВЛЕНО и объявление держится машиной; отрисовки не видит ни один тест — браузерного харнесса в проекте нет. Шестой раунд переносится без изменений: `git log -- app/templates/ app/static/css/app.css` → последний коммит `17b9bc9` (план 05-10) |
| **Гэп 1 раунда 5 — план 05-18 (D-30, `convert-remainder`)** | | | |
| 5 | «12×`basic` + 1×`pro`» больше НЕ даёт тринадцати месяцев Pro | ✓ VERIFIED | Мой прогон поверх настоящего `_apply_extension`, без правок `app/`: 140 дней Pro (журнал `remainder_days=365 → converted_days=110`), было 396. Регрессия `test_a_prepaid_horizon_is_not_converted_to_the_senior_plan_for_one_month` зелёная |
| 6 | Повтор оплаты СВОЕГО тарифа остаётся честным: N платежей дают N месяцев по прейскуранту | ✓ VERIFIED | Мой прогон: 12×`basic` → 2027-01-10 при плане `basic`, ровно 12 месяцев. `switch_is_refused(X, X, …) → False` не тронут (`plan_switch.py:87-88`); `test_repeated_payments_of_one_plan_stay_priced_at_the_list` зелёная |
| 7 | Повышение при ОДНОМ оплаченном месяце по-прежнему не сжигает остаток | ✓ VERIFIED | Мой прогон: остаток 25 дней Basic → 7 дней Pro плюс оплаченный месяц = 38 дней от сегодня. Остаток пережил переход; `test_an_upgrade_with_one_paid_month_still_does_not_burn_the_remainder` зелёная |
| 8 | Исход «взяли деньги и не дали ничего» не введён ни на одной ветке | ✓ VERIFIED | Нижняя граница в один день достаётся `converted_remainder` от `prorated_days:143` (`max(int(…), 1)`); живой остаток не конвертируется в ноль. Арифметика целиком в `Decimal` — `grep "float("` по обоим модулям пуст |
| 9 | Величина предоплаченного горизонта ограничена НА ВСЕХ ветках денежного пути | ✗ FAILED | Ветка отката `payment_service.py:1021-1036` оставляет `base = subscription.expires_at`; мой прогон отдаёт там 2027-02-10 при плане `pro` — 396 дней, дословно доплановое поведение. Два входа, один достижим правкой `.env`. См. гэп 1 |
| 10 | Решение владельца о форме границы записано номером в `05-CONTEXT.md` по образцу D-28/D-29 | ✓ VERIFIED | `05-CONTEXT.md:93` — D-30, выбранный вариант `convert-remainder`, отвергнутые `cap-horizon` и `apply-after-period`, причина выбора, reversibility, названный долг. ⚠️ Журнальный ключ отката назван неверно — отнесено к гэпу 1 |
| **Гэп 2 раунда 5 — планы 05-19, 05-20, 05-21** | | | |
| 11 | Оговорка о нейтральности окна гонки ссылается на исполняемый механизм | ✓ VERIFIED | `payment_service.py:243-258`: назван настоящий исход («обе строки несут `True` … исход решает ПОРЯДОК подтверждения»), механизм, дающий нейтральность свойством, назван поимённо и НЕСДЕЛАННЫМ. Зеркало в `.planning/STATE.md:145` |
| 12 | Оба абзаца о цене доли месяца называют `paid / price(subscription.plan)` | ✓ VERIFIED | `:846-848` и `:940-950` — «цене ДЕЙСТВУЮЩЕГО плана», с явным предупреждением о том, чем опасно альтернативное прочтение. Соседняя строка `:951` читает ровно эту величину |
| 13 | Докстринг `create_payment` называет ОБА окна потолка | ✓ VERIFIED | `:210-232`: гонка И срок давности, второе названо детерминированным и более широким, со ссылкой на опровергающий тест ПО ИМЕНИ; сам тест получил докстринг о том, что он доказывает |
| 14 | Ни одно имя теста не обещает больше, чем держит тело | ✓ VERIFIED | `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form` переименован в `test_a_second_intent_of_another_plan_is_refused_while_the_first_is_fresh` (`:2143`) — не удалён, а приведён к телу |
| 15 | Последствие D-26 названо ФАКТИЧЕСКОЕ и в ревизии, и в операционном документе | ✓ VERIFIED | `0019:51-95` — «ВЫКАТИТЬ КОД БЕЗ ЭТОЙ ОЧЕРЕДИ НЕЛЬЗЯ … СТАРАЯ СЛОМАЕТСЯ», перечень падающих путей включая пакетный вебхук; `.planning/STATE.md:98` — то же плюс «ВЫКАТ КОДА И ВЫКАТ ОЧЕРЕДИ ОТДЕЛИТЬ НЕЛЬЗЯ». Номера строк перепроверены и исправлены |
| 16 | Существует машинная сверка модели с головной ревизией, и у неё есть зубы | ✓ VERIFIED | `tests/test_migrations/test_model_matches_head.py` + `test_deploy_applies_migrations_before_serving.py` — 6 passed, схема строится настоящим Alembic до `head`, негативный контроль краснеет при снятой колонке, докстринг честно называет, чего проверка не доказывает |
| 17 | AST-ловушка порядка держит КАЖДЫЙ сдвиг срока | ✓ VERIFIED | `ast.walk` вместо `function.body`, множество имён выводится из импорта; два негативных контроля над синтетическими исходниками (`:1576`, `:1593`); прогон зелёный. Оператор, добавленный планом 05-18 (`:1064`), попал под инвариант сам |
| 18 | Ни одно объявление в коде не утверждает того, чего код рядом не делает | ✗ FAILED | Класс воспроизведён заново: длина месяца в `converted_remainder` (проверено численно — сокращается), «план только растёт» (опровергнуто прогоном — молчаливое понижение), «истёкший срок снимает отказ на ОБЕИХ стадиях» (опровергнуто прогоном для записанного `False`). См. гэп 2 |
| **Гэп 3 раунда 5 — план 05-21** | | | |
| 19 | §Current Position и `progress` не спорят ни друг с другом, ни с ROADMAP и не зависят от счёта | ✓ VERIFIED | Строки `Plan: N of M` и полоса `Progress:` со счётом планов упразднены с записанной причиной и названным единственным источником счёта; frontmatter `73/73` сходится и с ROADMAP §Phase 5 (`21/21`), и с суммой по фазам (13+15+12+12+21) |
| 20 | Ни одна находка код-ревью раунда 5 не осталась без распоряжения, и ни один номер не назван голым | ✓ VERIFIED | Грепом: `CR-01`, `CR-02`, `WR-01`…`WR-05`, `IN-01`, `IN-02` — девять номеров, каждый со строкой «(раунд 5)» и своим распоряжением. Прежние записи ПЕРЕСКАЗАНЫ, а не стёрты. `COVERAGE.md` сверена (§«Сверка после закрытия гэпов раунда 5»), а не перегенерирована |

**Score:** 17/20 truths verified (1 present, behavior-unverified; 2 failed)

### Воспроизведение — команда и вывод

Прогон поверх настоящего `_apply_extension`, без единой правки `app/`, без БД (сценарии A/D — читаемые цены, B — план платежа выпал из `PLAN_LIMITS`, C — подписка на `free`):

```
prices: {'free': None, 'basic': Decimal('1490.00'), 'pro': Decimal('4900.00')}
A after 12x basic: basic 2027-01-10 paid 17880.00
  [warning] subscription_remainder_converted remainder_days=365 converted_days=110 price_from=1490.00 price_to=4900.00
A after 1x pro   : pro 2026-05-30 paid 22780.00 -> days of pro from now: 140     ← ГРАНИЦА РАБОТАЕТ

B after 12x basic: basic 2027-01-10 paid 17880.00
   prices now: {'free': None, 'basic': Decimal('1490.00'), 'pro': None}
  [warning] subscription_prorating_skipped unreadable=paid_plan_price
B after 1x pro   : pro 2027-02-10 paid 22780.00 -> days of pro from now: 396     ← ГРАНИЦЫ НЕТ

C after 12x plan-NULL: free 2027-01-10 paid 17880.00
  [warning] subscription_prorating_skipped unreadable=price
C after 1x pro   : pro 2027-02-10 paid 22780.00 -> days of pro from now: 396     ← ГРАНИЦЫ НЕТ

D one-month remainder upgrade: pro 2026-02-17 -> days from now: 38 (остаток 25 дней Basic → 7 дней Pro + месяц)
```

Проверка утверждения о длине месяца в `converted_remainder` (остаток 365 дней, `basic`→`pro`):

```
2026-01-31 month_days 28 rem 365 converted 110  int(rem*1490/4900) = 110
2026-03-01 month_days 31 rem 365 converted 110  int(rem*1490/4900) = 110
2026-04-01 month_days 30 rem 365 converted 110  int(rem*1490/4900) = 110
```

Проверка двух опровергнутых инвариантов:

```
WR-03: подписка pro (живой срок) + подтверждение basic со switch_authorized=True → plan == 'basic'  (понижение, без записи в журнал)
WR-02: подписка pro (ИСТЁКШИЙ срок) + платёж basic со switch_authorized=False   → plan == 'pro'     (отказ пережил истечение срока)
```

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/application/billing/subscription_period.py` | `converted_remainder` рядом с `next_expiry` / `prorated_expiry`, целиком в `Decimal` | ⚠️ ЧАСТИЧНО | Функция есть, объявлена один раз, `float` не заводится; арифметика верна и проверена численно. Но абзац о длине месяца утверждает свойство, которого у неё нет (гэп 2), и `:245-247` — недостижимая ветка |
| `app/services/payment_service.py` | Денежный путь читает новую границу; свой ключ журнала | ⚠️ ЧАСТИЧНО | Ветка конверсии верна и проверена прогоном; ветка ОТКАТА границы не ставит (гэп 1); два инварианта в докстрингах опровергнуты прогоном (гэп 2) |
| `tests/test_pages/test_billing_payment_errors.py` | Три именованные регрессии гэпа 1 плюс распоряжение по `test_an_upgrade_does_not_burn_the_paid_remainder` | ⚠️ ЧАСТИЧНО | Все четыре на месте и зелёные, старый тест переписан с сохранением имени (не удалён). Ветка отката в конверсии не покрыта ни одним тестом |
| `tests/test_application/test_subscription_period.py` | Модульные таблицы для арифметики денег | ✗ ПРОБЕЛ | `converted_remainder` не импортирован этим файлом вовсе — грепом по `tests/` символ не встречается ни разу. Единственная новая арифметика денежного пути покрыта только интеграционно, с допуском ±2 дня |
| `tests/test_migrations/test_model_matches_head.py` | Сверка отображённых колонок с головой + негативный контроль | ✓ VERIFIED | 6 passed вместе с `test_deploy_applies_migrations_before_serving.py`. ⚠️ Сверка идёт по ИМЕНАМ; расхождение типа/нулевости она пропустит, и в разделе «чего не доказывает» это не названо (`WR-07 (раунд 6)`) |
| `alembic/versions/0019_payment_switch_authorized.py` | Докстринг о последствии D-26 по фактическому радиусу | ✓ VERIFIED | `:51-95` переписан: механизм, перечень падающих путей, уцелевший `count_payments`, цена в виде остановленного приёма пакетных платежей |
| `.planning/STATE.md` | Записи о нейтральности окна и о D-26 приведены к коду; §Current Position без констант | ✓ VERIFIED | Все три правки на месте и проверены чтением; распоряжения по девяти находкам раунда 5 записаны |
| `.planning/phases/05-tarify/05-CONTEXT.md` | D-30 по образцу D-28/D-29 | ✓ VERIFIED | Строка 93. ⚠️ Ключ журнала отката назван неверно — отнесено к гэпу 1 |
| `.planning/phases/05-tarify/COVERAGE.md` | Сверка после раунда 5, а не перегенерация | ✓ VERIFIED | §«Сверка после закрытия гэпов раунда 5»: затронутые строки названы, незатронутые названы незатронутыми |
| `app/templates/billing/*` | Разметка раздела по макету | ✓ VERIFIED (регрессия) | Не менялась с `17b9bc9`; якоря критериев 1-3 на месте |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `plan_card.html` / `balance.html` | `POST /billing/subscribe` | `<form method="post">` | ✓ WIRED | `plan_card.html:97`, `balance.html:122` |
| `subscribe_to_plan` | `create_payment` | `switch_authorized=not switch_refused` | ✓ WIRED | `billing.py:341-370`, литерала `True` нет |
| `create_payment` | `_open_subscription_intents` | Первый оператор, до SDK | ✓ WIRED | `payment_service.py:278-297`, до `_configure_yookassa()` (`:299`) |
| `_apply_extension` | `converted_remainder` | Ветка «живой срок И разные планы» | ✓ WIRED (одна ветка из двух) | `:1038-1043` исполняется и проверена прогоном; соседняя ветка `:1021-1036` границы не ставит (гэп 1) |
| `converted_remainder` | `prorated_days` | Тот же аппарат `Decimal` в другую сторону | ✓ WIRED | Второй копии деления денег на цену нет; нижняя граница в один день достаётся оттуда же |
| `payments.switch_authorized` | `_apply_extension` | Чтение записанного факта первым | ⚠️ WIRED, НО БЕЗ ЧЛЕНА ИСТЕЧЕНИЯ | `:920-921` читает факт, не спрашивая `period_is_live`; ветка первой вставки (`:743-754`) не читает его вовсе (гэп 2) |
| `Payment.__table__.columns` | схема головной ревизии | `command.upgrade(cfg, 'head')` + `PRAGMA table_info` | ✓ WIRED | `test_model_matches_head.py`; сверка по именам, не по форме |
| `entrypoint.sh` | процесс приложения | `set -e` → `alembic upgrade head` → `exec "$@"` | ✓ WIRED | Закреплено `test_deploy_applies_migrations_before_serving.py` |
| `get_payment_history` | `payments` | `select(Payment)` | ✓ WIRED (репозиторий) / ✗ СЛОМАН НА ПРОДЕ | Последствие D-26, названо фактическим в обоих документах — не гэп фазы |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `balance.html` метры | `axes` | `plan_axes()` → одна выборка | Да | ✓ FLOWING |
| `balance.html` история | `payments` | `get_payment_history` → `select(Payment)` c пагинацией | Да | ✓ FLOWING |
| `balance.html` срок | `subscription.expires_at` | Строка `subscriptions`, писатель — вебхук | Да | ✓ FLOWING |
| `plan_card.html` цена | `plan.price` | `settings.parsed_plan_limits` | Да (`basic 1490.00`, `pro 4900.00`, `free 0.00`) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Прикладной и сервисный слой биллинга | `pytest tests/test_services/test_payment_service.py tests/test_application/test_{subscription_period,plan_switch,plan_usage}.py tests/test_services/test_payment_concurrency.py -q` | 96 passed | ✓ PASS |
| Сверка модели с головой + порядок выката | `pytest tests/test_migrations/test_model_matches_head.py tests/test_migrations/test_deploy_applies_migrations_before_serving.py -q` | 6 passed | ✓ PASS |
| Ловушка порядка и регрессии горизонта | `pytest tests/test_pages/test_billing_payment_errors.py -q -k "liveness or horizon or burn or converted or remainder"` | 4 passed | ✓ PASS |
| Граница на ГЛАВНОМ пути (12×basic + 1×pro) | Прогон настоящего `_apply_extension` | 140 дней Pro (было 396) | ✓ PASS |
| Граница в ветке ОТКАТА (`price_to` нечитаема) | Тот же прогон с `PLAN_LIMITS` без `pro` | 396 дней Pro за 22 780 ₽ | ✗ FAIL (гэп 1) |
| Граница в ветке ОТКАТА (подписка на `free`) | Тот же прогон, `subscription.plan = "free"` | 396 дней Pro за 22 780 ₽ | ✗ FAIL (гэп 1) |
| Защитная семантика (остаток не сгорает) | Прогон, остаток 25 дней | 7 дней Pro + месяц = 38 дней | ✓ PASS |
| Зависимость `converted_remainder` от длины месяца | Прогон при `month_days` 28 / 30 / 31 | 110 / 110 / 110 — величина сокращается | ✗ FAIL утверждения докстринга (гэп 2) |
| Инвариант «план только растёт» | Прогон: `pro` живой + подтверждение `basic` (`True`) | `plan == 'basic'`, журнала нет | ✗ FAIL утверждения (гэп 2) |
| Инвариант «истёкший срок снимает отказ на обеих стадиях» | Прогон: истёкший `pro` + платёж `basic` (`False`) | `plan == 'pro'`, `subscription_plan_preserved` записан | ✗ FAIL утверждения (гэп 2) |

Полная суита не запускалась намеренно: она зелёная на этом дереве (1733 passed) и занимает ~25 минут; прогоны выше — точечные и относящиеся.

### Probe Execution

Конвенциональных проб (`scripts/*/tests/probe-*.sh`) в проекте нет; ни один PLAN и ни один SUMMARY фазы проб не объявляет. Пропущено с причиной.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| BILL-05 | 05-01…05-18, 05-21 | Пользователь может продлить текущую подписку | ⚠️ BLOCKED (реализовано, не сдаётся) | Механика продления работает и проверена (труды 1, 5-8, 10-14). Но операция, которую требование называет, на одной из двух веток денежного пути по-прежнему отдаёт горизонт за цену месяца (гэп 1), и две прохибиции BILL-05 (fairness, autonomy) остаются неразрешёнными |
| BILL-06 | 05-03, 05-05, 05-18, 05-21 | Пользователь видит потребление и остаток по четырём осям | ✓ SATISFIED | `AXIS_ORDER` → четыре метра; безлимитная ось не показывается нулём. ⚠️ Прохибиция BILL-06 (transparency) остаётся неразрешённой |
| BILL-07 | 05-04, 05-05, 05-21 | Пользователь видит историю своих платежей | ✓ SATISFIED (в репозитории) | `get_payment_history` → блок «История платежей» с датой, назначением, суммой и статусом. ⚠️ На боевой схеме `0012` весь раздел отдаёт 500 — последствие D-26, названное фактическим обоими документами, а не отсутствие реализации |
| BILL-02 | — | Применение тарифных лимитов | ⚠️ ORPHANED-BY-DESIGN | Не входит в объём фазы решением D-08 и записан долгом. Не вменяется фазе; назван ради полноты: именно он превращает отложенную цену гэпа 1 в списанную |

Осиротевших требований нет: `REQUIREMENTS.md:235-237` относит к Фазе 5 ровно BILL-05, BILL-06, BILL-07, и все три заявлены планами.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` в восьми файлах, изменённых с раунда 5 | — | Не найдено ни одного. Гейт долговых маркеров пройден |
| `app/services/payment_service.py` | 1021-1036 | Ветка отката восстанавливает закрытый блокер, и её не видит ни один тест | 🛑 Blocker | Гэп 1 |
| `app/application/billing/subscription_period.py` | 231-236 | Абзац утверждает свойство, которого у функции нет (величина сокращается) | 🛑 Blocker | Гэп 2; строки 249-254 — соответствующее мёртвое вычисление, которое «починят» в неверную сторону |
| `app/services/payment_service.py` / `plan_switch.py` | 688 / 70 | Объявленный инвариант «план только растёт» опровергается прогоном | ⚠️ Warning | Молчаливое понижение без записи в журнал |
| `app/services/payment_service.py` | 712-720 против 920-921 | «Отказ снимается на ОБЕИХ стадиях» неверно для записанного `False` | ⚠️ Warning | Значение сегодня недостижимо через форму, но объявлено живым контрактом моделью и ревизией |
| `app/services/payment_service.py` | 372-385 | `await db.commit()` после УСПЕШНОГО `YooPayment.create` без обработки и без журнала | ⚠️ Warning | Отказ записи оставляет платёж у ЮKassa сиротой, чей идентификатор не попадает ни в один журнал (`payment_created` пишется ПОСЛЕ коммита); `webhook_payment_not_found` отвечает 200 и повторов не вызывает |
| `app/services/payment_service.py` | 548-555 против 743-754 | Пакетная ветка отказывается заявлять платёж без `messages_count`; подписочная не проверяет `plan` | ⚠️ Warning | `plan IS NULL` кладёт человека на `free` с оплаченным месяцем и создаёт единственный сегодня вход (б) гэпа 1 |
| `app/routes/billing.py` | 30-38 | `limit` / `offset` без границ уходят в `.limit()` / `.offset()` | ⚠️ Warning | `?limit=-1` — 500 на PostgreSQL (SQLite трактует как «без предела», поэтому суита этого не видит); у всех остальных перечней раздела потолок объявлен |
| `tests/test_migrations/test_model_matches_head.py` | 89-102 | Сверка по именам колонок; расхождение типа/нулевости проходит зелёным, и в разделе «чего не доказывает» это не названо | ⚠️ Warning | Ревизия, заведшая `switch_authorized` как `NOT NULL`, прошла бы гейт, а пакетная покупка (подаёт `None` явно) начала бы падать |
| `app/application/billing/subscription_period.py` | 245-247 | Недостижимая ветка: `countdown_base` уже зажимает `base` к `now_utc` | ℹ️ Info | Читается как второе правило истечения рядом с настоящим |
| `app/services/payment_service.py` | 964-971 против 1029-1036 | Один ключ журнала в двух ветках с разными словарями `unreadable` и разными наборами полей | ℹ️ Info | Оператор, фильтрующий по ключу, получает два разных события без поля, различающего ветку |

### Что выдержало враждебное чтение

Записано, чтобы раунд 7 не перепроверял это заново — проверено мной прогоном или чтением кода, а не принято по SUMMARY и не принято по отчёту ревью:

- Граница `convert-remainder` на ГЛАВНОМ пути работает и работает симметрично: 12 месяцев `basic` конвертируются в 110 дней Pro, 25 дней — в 7. Обещание «остаток не сгорает» держится обоими прогонами.
- `converted_remainder` не заводит второй арифметики денег: делением денег на цену остаётся `prorated_days`, откуда достаётся и нижняя граница в один день. `grep "float("` по обоим модулям пуст.
- Ветка ПЕРВОЙ ВСТАВКИ границу не обходит, и это свойство состояния, а не упущение: активной строки нет, значит нет ни прежнего плана, ни остатка. Проверено чтением `:737-754`.
- Ветка конверсии берётся только при ЖИВОМ сроке и РАЗЛИЧАЮЩИХСЯ планах, поэтому решение `apply-after-expiry` ею не задето, а повтор своего тарифа не задет вовсе.
- Признак живости по-прежнему снимается ПЕРВЫМ действием, и теперь это держится по всему телу: `ast.walk` с выводимым из импорта множеством имён и двумя негативными контролями.
- Все четыре названных раундом 5 неверных утверждения действительно исправлены — каждое проверено чтением кода, а не по SUMMARY.
- Реестр раундов и §Current Position в `.planning/STATE.md` свободны от констант величины, и запрет записан с причиной и с названным единственным источником счёта.
- Сверка модели с головой и порядок выката закреплены тестами, у которых есть негативные контроли; фикстуры монкейпатчат и `DATABASE_URL`, и `set_main_option`, поэтому на настоящую базу их не направить.

### Gaps Summary

**Первое, и это главное отличие раунда 6 от пяти предыдущих.** Работа раунда 5 сделана и сделана честно — три гэпа закрыты, четыре неверных утверждения исправлены, машинные проверки заведены с зубами, операционные документы приведены к коду. Впервые за шесть раундов гэп денежного пути не «переехал в новую формулировку», а был закрыт в главном направлении так, что я воспроизвёл закрытие числами: 396 дней стали 140.

**Второе.** Утечка не переехала — она ОСТАЛАСЬ в той единственной ветке, куда границу не поставили. `if price_from is None or price_to is None` — это не редкий отказ, а объявленное самим кодом нормальное состояние («перечень тарифов правится окружением … разойтись они могут в любую сторону и без нашего участия»), и в этой ветке живут ровно прежние 396 дней. Через интерфейс сегодня она не берётся, и это честно снижает тяжесть: нужна правка `.env` или строка подписки, которой `create_payment` не заводит. Но граница, снятая одной правкой окружения на денежном пути, — это не остаточный риск, а неполное закрытие, тем более что ветку не проверяет ни один тест, а её цена не названа ни в D-30, ни в SUMMARY, где откат описан как безобидная мера против 5xx.

**Третье, и оно про метод.** План 05-19 был написан против класса «объявление утверждает то, чего код не делает». Класс закрыт по четырём НАЗВАННЫМ экземплярам и воспроизведён в трёх новых, причём один из них написан планом 05-18 в том же прогоне: абзац о длине месяца в `converted_remainder` объясняет, почему константа 30 была бы неверна, — а величина в ответ не входит вовсе, что проверяется тремя строчками арифметики. Пока проверка этого класса остаётся чтением глазами (машинного гейта на «докстринг против поведения» в проекте нет, и все прохибиции этого класса объявлены `verification: test` без единого теста), седьмой раунд найдёт четвёртый экземпляр.

Что НЕ вменяется фазе и здесь не повторяется, а числится записанным долгом: второе окно потолка (срок давности) — открыто и названо открытым в трёх местах; частичный уникальный индекс «не более одного незакрытого намерения» — не построен и назван несделанным; `uq_subscriptions_active_user` ревизии `0018` — не выкачен; решение D-26 — деплой-гейт владельца, чей радиус поражения теперь назван фактическим; отсутствие применения тарифных лимитов (D-08, BILL-02); неподтверждённая подписка на `payment.canceled` (D-27); отсутствие RED-коммитов у планов 05-15 и 05-17 (раскрыто исполнителями, гейт неблокирующий).

---

_Verified: 2026-08-17T19:40:00Z_
_Verifier: Claude (gsd-verifier)_
_Round: 6 — раунд 1 `4989f7a`, раунд 2 `1d29360`, раунд 3 `164d210`, раунд 4 `5da8e82`, раунд 5 `ec9e34a`…`87d8fa8`_
