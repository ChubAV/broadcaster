---
phase: 05-tarify
plan: 26
type: execute
wave: 21
depends_on: ["05-22", "05-23", "05-24", "05-25"]
gap_closure: true
files_modified:
  - .planning/phases/05-tarify/05-CONTEXT.md
  - .planning/STATE.md
  - .planning/phases/05-tarify/COVERAGE.md
autonomous: true
requirements: [BILL-05, BILL-06, BILL-07]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Оба решения владельца, снятые чекпойнтами этой волны, записаны в `05-CONTEXT.md` СВОИМИ номерами по образцу D-28, D-29 и D-30: выбранный вариант с идентификатором, отвергнутые варианты с причиной отказа одной фразой, причина выбора прямым текстом и рейтинг обратимости"
    - "Записанная цена решения D-30 и то, что код отдаёт в ветке ОТКАТА, больше не расходятся: цена ветки названа ЧИСЛОМ, а не эпитетом «откат к прежнему поведению», и названа новая величина после плана `05-22`. Пятый пункт `missing:` гэпа 1 требовал свести эти две величины дословно"
    - "Правка записи D-30 не заводит второго расхождения на месте закрытого первого: прежнее `395` (`05-CONTEXT.md:93`) и `396` отчёта раунда 6 (`05-VERIFICATION.md:28`) выходят из плана СВЕДЁННЫМИ — либо одним числом, либо обоими с названной словами причиной различия (дата отсчёта прогона), и выбранное состояние названо в SUMMARY"
    - "Журнальный ключ отката в записи D-30 назван верно: код в этой ветке пишет `subscription_prorating_skipped`, а не `subscription_remainder_converted` — последний пишется в ПРОТИВОПОЛОЖНОЙ ветке (`IN-04` раунда 6). Прежняя запись ПЕРЕПИСАНА с названной причиной, а не стёрта"
    - "Ни одна из двенадцати находок код-ревью раунда 6 не осталась без распоряжения, и ни один номер не назван голым: `CR-01`, `WR-01`…`WR-07`, `IN-01`…`IN-04` — каждый со строкой «(раунд 6)» и со своим распоряжением (закрыт планом / отложен с обоснованием / записан без требования кода)"
    - "Тир прохибиций класса «операционные документы» приведён к существующему принуждению честно: четыре плана подряд объявляли его `verification: test` при нуле тестов, и вместо пятого необеспеченного объявления записано решение о переводе в judgment-tier с названной причиной. Кодовая половина того же класса, напротив, получила МАШИННЫЙ гейт планом `05-25` — различие предметов названо, а не размыто"
    - "Ни одна прежняя запись `.planning/STATE.md` и `05-CONTEXT.md` не стёрта: устаревшие ПЕРЕСКАЗАНЫ с названной причиной. Прохибиция «закрытие дефекта удалением записи о нём» соблюдена"
    - "Матрица `COVERAGE.md` проверена на действительность после раунда 6 и НЕ перегенерирована: затронутые строки названы поимённо, незатронутые названы незатронутыми прямо, чтобы молчание не читалось недосмотром"
    - "§Current Position и frontmatter `progress` не спорят ни друг с другом, ни с ROADMAP и по-прежнему не называют счёта планов прозой: запрет, записанный планом `05-21` с причиной, соблюдается и этой волной"
    - statement: "Человек, читающий `05-CONTEXT.md` и §Blockers/Concerns после этой волны, понимает, что именно решено о деньгах и почему, не открывая ни одного отчёта верификации, и не встречает утверждения, которое устареет от появления следующего плана. Понятность и неустареваемость судятся человеком, а не грепом"
      verification: backstop
  artifacts:
    - "`.planning/phases/05-tarify/05-CONTEXT.md` — два новых решения владельца своими номерами; запись D-30 переписана в двух местах (журнальный ключ и названная числом цена ветки отката)"
    - "`.planning/STATE.md` — распоряжения по двенадцати находкам раунда 6, решение о тире прохибиций документного класса, приведённые к коду записи о денежном пути и обновлённая §Current Position без констант величины"
    - "`.planning/phases/05-tarify/COVERAGE.md` — раздел сверки после раунда 6: затронутые строки названы, незатронутые названы незатронутыми"
  key_links:
    - "ответ чекпойнта задачи 1 плана `05-22` → решение своим номером в `05-CONTEXT.md` → запись о цене ветки отката в `.planning/STATE.md`. Односторонняя цепочка: без исполненного ответа документ описывал бы несуществующее"
    - "ответ чекпойнта задачи 1 плана `05-24` → второе решение своим номером. Оно СУЖАЕТ либо ПОДТВЕРЖДАЕТ записанное решение D-28, и в обоих случаях обязано сказать это прямо: решение, отменённое молча, есть та же потеря записи, что стёртая запись"
    - "машинный гейт плана `05-25` (кодовая половина класса) → решение о тире документных прохибиций (документная половина). Одно решение о методе, разнесённое по предмету; записывается одной записью, чтобы раунд 7 не читал две половины как спор"
    - "докстринги, переписанные планами `05-22`, `05-23`, `05-24` → записи `.planning/STATE.md`. Документ пишется ПОСЛЕ кода: обратный порядок дал бы утверждение, проверенное чужими глазами, — ровно тот способ отказа, за который фаза получила раунды 4, 5 и 6"
  prohibitions:
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT объявлять прохибицию `verification: test` при отсутствующем принуждении: тир обязан соответствовать тому, что существует в дереве, а не тому, что задумано — иначе документ обещает проверку, которой нет, и обещание переживает четыре раунда"
      status: unresolved
      verification: judgment
---

<objective>
**Операционная половина раунда 6: решения владельца получают номера, записанная цена сводится
с кодом, находки получают распоряжения, а тир прохибиций приводится к существующему
принуждению.**

Четыре обязательства приходят сюда из отчёта `05-VERIFICATION.md` раунда 6 и из `05-REVIEW.md`:

1. **Пятый пункт `missing:` гэпа 1** — свести записанную цену решения D-30 с тем, что код
   фактически отдаёт в ветке отката. Отчёт формулирует это дословно: «Расхождение не должно
   остаться ни в одну сторону, и это дословно тот же пункт, который гэп 1 раунда 5 требовал
   для варианта `upgrade-only`». Сегодня откат назван в D-30 и в `05-18-SUMMARY.md` словами
   «откат к прежнему поведению» и «Придумывать конверсию бесплатного остатка этот план не
   вправе» — то есть как БЕЗОБИДНАЯ мера против 5xx. ЦЕНА отката не названа нигде, а она
   равна цене гэпа, который тот же план закрывал, и посчитана числами в том же абзаце.
2. **Шестой пункт `missing:` гэпа 1** — журнальный ключ отката в D-30 назван неверно.
3. **Два решения владельца**, снятые чекпойнтами планов `05-22` и `05-24`, обязаны вернуться в
   `05-CONTEXT.md` своими номерами — по образцу D-28, D-29 и D-30 и по прямому требованию
   `<reversibility_and_checkpoints>` этой волны.
4. **Двенадцать находок код-ревью раунда 6** (`CR-01`, `WR-01`…`WR-07`, `IN-01`…`IN-04`)
   обязаны получить распоряжения, квалифицированные раундом: `.planning/STATE.md` несёт
   собственную прохибицию «ни одна находка не осталась без распоряжения», и раунд 7 нашёл бы
   её нарушенной.

Плюс **документная половина решения о методе**: прохибиции об операционных документах,
объявленные `verification: test` планами `05-16`, `05-19`, `05-20` и `05-21`, не имеют в суите
ни одного теста — проверено раундом 6 грепом. Кодовую половину класса закрыл машинный гейт
плана `05-25`; здесь закрывается документная — не пятым необеспеченным объявлением, а честным
тиром.

Purpose: документ, по которому принимаются решения, обязан быть верным в момент чтения, а не в
момент записи; и тир прохибиции обязан описывать то, что есть, а не то, что задумано.
Output: два решения своими номерами, исправленная запись D-30, распоряжения по двенадцати
находкам, записанное решение о тире и сверенная матрица возможностей API.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@.planning/phases/05-tarify/COVERAGE.md
</context>

<flagged_assumptions>

**Зонд краевых случаев: три требования фазы остаются `unclassified` / `unresolved`.**
Допущения планировщика E-01 (BILL-05), E-02 (BILL-06), E-03 (BILL-07) записаны планом `05-15`
и повторены планом `05-22`; здесь не дублируются построчно и не снимаются. Равенство: **3
поднятых == 0 внесённых в `must_haves` + 3 явных допущения.**

**Прохибиций сминчено ОДНА**, записана в `must_haves.prohibitions` с тиром `judgment`,
вынесено допущениями ноль. Равенство: **1 == 1 + 0.** Тир выбран `judgment` НАМЕРЕННО и это
предмет самого плана: свойство «тир соответствует существующему принуждению» проверяется
чтением дерева человеком, а механического гейта на планировочные документы этот план не
заводит — по причине, записанной задачей 2.

**Ни одной строки кода и ни одного теста.** `git diff --name-only -- app/ tests/ alembic/
entrypoint.sh pyproject.toml uv.lock` у этого плана обязан быть пуст. Всё, что можно было
закрепить машиной, закрыто планами `05-22`…`05-25`; здесь правятся документы.

**Решения D-26, D-27, D-28, D-29 и D-30 НЕ ПЕРЕОТКРЫВАЮТСЯ.** Правятся ФАКТЫ, по которым они
приняты и записаны, и добавляются новые решения. Единственное исключение названо прямо и
только оно: ответ чекпойнта плана `05-24` в ветви `upgrade-only-wins` СУЖАЕТ D-28, и это
записывается как сужение с указанием, что именно перестало быть верным, — а не переписыванием
самой записи D-28.

**SUMMARY прошлых планов НЕ ПРАВЯТСЯ ни строкой.** Пятый пункт `missing:` гэпа 1 называет два
места записанной цены: D-30 в `05-CONTEXT.md` и `05-18-SUMMARY.md`. Первое правится здесь;
второе — исторический документ, фиксирующий, что было решено и сделано ТОГДА, и переписывание
его стёрло бы причину, по которой мера понадобилась. Вместо этого исправленная запись D-30
прямо называет: описание отката в `05-18-SUMMARY.md` было у́же того, что исполнял код, цена
отката там не названа, и расхождение закрыто планом `05-22` в направлении, выбранном
владельцем. Тот же порядок применил план `05-21` к `05-10-SUMMARY.md`, и раунд 6 принял его.

**Строки контракта `05-UI-SPEC.md` не затрагиваются**: план не трогает ни кода, ни разметки.
Лифтить нечего.

**Записанный долг фазы не закрывается, но и не теряется**: второе окно потолка намерений,
частичный уникальный индекс, невыкаченная `0018`, D-26, D-08/BILL-02, D-27, отсутствие
RED-коммитов у `05-15` и `05-17`, мобильная проверка на 375px. Каждый пункт обязан быть
НАЗВАН в §Deferred Items с распоряжением; молчание о нём было бы его потерей.

**Если какой-либо из планов `05-22`…`05-25` несёт статус ОСТАНОВКИ, документ пишется по
остановленному состоянию, а не по задуманному.** План `05-22` останавливается после чекпойнта
при ответах `refuse-at-intent` или `accept-risk`. В этом случае решение владельца всё равно
записывается своим номером — оно ПРИНЯТО, — а записи о денежном пути описывают то, что есть на
момент записи, с прямым указанием, что граница в ветке отката не построена и блокер денежного
пути открыт. Записывать закрытие, которого нет, запрещено ровно тем же правилом, каким
запрещено записывать несуществующую семантику.

</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Два решения владельца получают номера; запись D-30 сводится с кодом в двух местах</name>
  <reversibility rating="costly">Запись решения владельца становится основанием, по которому будут приниматься следующие решения фазы и следующих фаз. Ошибка здесь не ломает прогон, но переживает его: пять раундов подряд гэп приходил из расхождения между записанной ценой решения и исполняемым кодом.</reversibility>
  <precondition>Планы `05-22` и `05-24` исполнены, их SUMMARY существуют в каталоге фазы и называют идентификаторы выбранных ветвей. Без них обе записи описывали бы решение, которого владелец не принимал.</precondition>
  <files>.planning/phases/05-tarify/05-CONTEXT.md</files>
  <read_first>
    - .planning/phases/05-tarify/05-CONTEXT.md (строки 82-93 — подразделы «Решения владельца по гэпу 1 раунда 4» (D-28, D-29) и «Решения владельца по гэпу 1 раунда 5» (D-30). ОБРАЗЕЦ дословный: состав записи, порядок частей, форма рейтинга обратимости, форма записи принятого долга)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-30 целиком — правятся ДВА её фрагмента: название журнального ключа отката и описание отката, где цена не названа. Остальное сохраняется дословно)
    - .planning/phases/05-tarify/05-22-SUMMARY.md (ответ чекпойнта задачи 1: идентификатор выбранной формы границы, отвергнутые формы и их цены. Материал записи берётся отсюда, а не сочиняется)
    - .planning/phases/05-tarify/05-24-SUMMARY.md (ответ чекпойнта задачи 1: идентификатор выбранной ветви конфликта `upgrade-only` × D-28; при ветви `upgrade-only-wins` — какой зелёный тест переписан и с какой причиной)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 45-46 — пятый и шестой пункты `missing:` гэпа 1 дословно; строка 28 — абзац о том, что цена отката не названа нигде, и числа этой цены)
    - app/services/payment_service.py (ветка отката после плана `05-22` — запись обязана СОВПАДАТЬ с тем, что делает код, а не пересказывать его своими словами)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 576-604 — `IN-04` о двух испусканиях одного ключа: источник исправления названия ключа)
  </read_first>
  <action>
Правятся ДВА фрагмента существующей записи D-30 и добавляются ДВА новых решения. Каждая правка
точечная; окружающий текст сохраняется дословно, потому что он верен и потому что его потеря
была бы закрытием дефекта удалением записи о нём.

**Фрагмент D-30 о журнальном ключе.** Заменить название ключа на тот, который код в ветке
отката ПИШЕТ, и добавить одну фразу о том, что ключ с прежним названием пишется в
ПРОТИВОПОЛОЖНОЙ ветке — той, где конверсия исполняется, — и что различает две ветки теперь
поле, добавленное планом `05-22`, а не ключ. Прежнее название не стирается, а называется
ошибкой записи с указанием, что она обнаружена раундом 6 и совпадает с находкой `IN-04`.

**Фрагмент D-30 о цене отката.** Заменить описание отката на такое, которое называет его цену
ЧИСЛОМ. Состав фразы: до плана `05-22` откат означал перенос ВСЕГО накопленного горизонта —
те же 396 дней Pro за 22 780 ₽ при прейскуранте 63 700 ₽, что и гэп, который сам план `05-18`
закрывал; ветка достижима двумя входами, ни один из которых не требует гонки; ни один тест её
не проверял; и с планом `05-22` она получила границу в форме, которую выбрал владелец, — с
названием формы и новой величиной в днях.

**ДВЕ ВЕЛИЧИНЫ ОДНОГО ЧИСЛА СВОДЯТСЯ ЗДЕСЬ ЖЕ, ИНАЧЕ ПРАВКА ЗАВЕДЁТ РАСХОЖДЕНИЕ ВМЕСТО ТОГО,
ЧТОБЫ ЕГО ЗАКРЫТЬ.** Сегодняшняя запись D-30 (`05-CONTEXT.md:93`) называет прежнюю выдачу
числом `395`, а отчёт раунда 6 воспроизводит ту же выдачу числом `396`
(`05-VERIFICATION.md:28`, `:183`, `:253`). Прохибиция «прежняя запись не стирается» толкает
сохранить оба, и тогда в одном абзаце окажутся два числа одной величины — та самая фигура
расхождения, против которой пишется весь план. Поэтому запись обязана выйти в одном из двух
состояний, и третьего нет: ЛИБО одно число на всю запись (второе снято как измерение той же
величины другого дня), ЛИБО оба числа с явным указанием, ЧЕМ они различаются — а различаются
они датой отсчёта: прогон раунда 5 мерил от своего сегодня, прогон раунда 6 — от своего, и день
разницы даёт календарная длина месяцев между двумя датами, а не два разных дефекта. Что именно
различает величины, обязано быть сказано словами в самой записи; «≈» перед числом этого не
говорит и достаточным не является. Отдельным предложением — что описание отката в
`05-18-SUMMARY.md` было у́же исполняемого («безобидная мера против 5xx»), что SUMMARY как
исторический документ не правится, и что расхождение закрыто кодом, а не переписыванием
истории.

**Новое решение — форма верхней границы в ветке отката.** Новый подраздел «Решения владельца
по гэпам раунда 6» после существующего подраздела о раунде 5, с решением под следующим
свободным номером после D-30. Состав по образцу D-28/D-29/D-30 дословно: выбранный вариант и
его идентификатор; отвергнутые варианты, каждый ОДНОЙ фразой о том, почему отвергнут (чтобы
следующий читатель не восстановил отвергнутый как «упрощение»); причина выбора прямым текстом;
величина, названная числами в обе стороны — что получает покупатель при остатке короче месяца
и что при остатке длиннее; рейтинг обратимости; и принятый долг, названный явно (при форме
`cap-one-month` — что остаток длиннее месяца ЧАСТИЧНО СГОРАЕТ, и это допущенное исключение из
прохибиции плана `05-01`, а не её соблюдение).

**Новое решение — конфликт `upgrade-only` × D-28.** Следующий свободный номер. Состав тот же.
Обязательные части, специфичные для этого решения: какое из двух записанных обещаний
исполняется и какое перестаёт быть инвариантом; что происходит с покупателем в числах (при
`record-wins` — остаток старшего тарифа конвертируется в дни младшего, деньги примерно
сохраняются, тариф меняется вниз и это теперь видно в журнале; при `upgrade-only-wins` — 1490 ₽
дают около девяти дней вместо девяноста восьми, и тариф остаётся прежним); и — при
`upgrade-only-wins` — ПРЯМОЕ указание, что решение D-28 СУЖЕНО, в чём именно и какой зелёный
тест переписан с этой причиной. Запись D-28 при этом не переписывается: сужение записывается
новым решением, а не правкой старого.

**Третье новое решение — тир прохибиций класса «объявление против кода».** Следующий свободный
номер. Оно фиксирует ответ на развилку, которую `Gaps Summary` раунда 6 поставил прямо:
кодовая половина класса получает МАШИННЫЙ гейт (план `05-25`, `verification: test` с
существующим тестом), документная половина — прохибиции о `.planning/STATE.md` планов `05-16`,
`05-19`, `05-20` и `05-21` — переводится в judgment-tier. Причина перевода называется прямо и
не как удобство: свойство «константа величины, которую документ не может держать истинной»
механически неотделимо от законных чисел документа (даты, цены, номера ревизий, номера строк),
и гейт на него давал бы либо ложные срабатывания, либо проверку, зелёную всегда; четыре плана
подряд объявляли тир `test` и не завели ни одного теста, поэтому пятое необеспеченное
объявление было бы повторением ошибки, а не её исправлением. Рейтинг обратимости — `costly`:
понижение тира снимает обещание проверки, и вернуть его позже означает построить гейт, от
которого здесь отказались с причиной. Форсирующий признак возврата к вопросу назван: третий
раунд подряд с находкой класса «константа величины в операционном документе».
  </action>
  <verify>
    <automated>test "$(grep -cE '^- \*\*D-3[1-9]:' .planning/phases/05-tarify/05-CONTEXT.md)" -ge 3</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: 395 -->
    <!-- Число названо в `<action>` как ПРЕДМЕТ сведения, а негативный греп идёт по ДРУГОМУ файлу (`05-CONTEXT.md`), а не по телу плана: самоаннулирования нет по построению -->
    - Команда `<verify>` завершается кодом `0`: три новых решения записаны своими номерами в форме образца. Обёртка `test "$(…)" -ge 3` обязательна, а не украшение: голый `grep -c` завершается кодом `1`, когда счёт равен нулю, но кодом `0` при любом ненулевом — то есть сам по себе счёта не проверяет
    - Каждое новое решение несёт все части образца: `grep -c "Reversibility" .planning/phases/05-tarify/05-CONTEXT.md` увеличился ровно на `3` относительно `git show HEAD:.planning/phases/05-tarify/05-CONTEXT.md | grep -c "Reversibility"`
    - Журнальный ключ отката назван верно: в записи D-30 присутствует `subscription_prorating_skipped` (`grep -c` не менее `1`), и рядом сказано, в какой ветке пишется второй ключ — проверяется чтением и цитируется в SUMMARY
    - Цена отката названа числом: в записи D-30 присутствуют числа `396` и `22 780` (либо `22780`) — `test "$(grep -c '396' .planning/phases/05-tarify/05-CONTEXT.md)" -ge 1`. Эпитета без числа недостаточно, и это ровно тот пункт, который отчёт требует дословно
    - **`395` и `396` СВЕДЕНЫ, а не поставлены рядом.** Проверяется в одном из двух состояний, и исполнитель называет в SUMMARY, какое выбрано: либо `test "$(grep -c '395' .planning/phases/05-tarify/05-CONTEXT.md)" -eq 0` — прежнее число снято как измерение той же величины другого дня, и запись говорит об этом словами; либо оба числа сохранены, и тогда в записи D-30 присутствует фраза, называющая ЧЕМ они различаются (дата отсчёта прогона раунда 5 против даты прогона раунда 6) — фраза цитируется в SUMMARY дословно. Состояние «два числа одной величины в одном абзаце без объяснения» проваливает задачу: это та же фигура расхождения, которую задача закрывает
    - Идентификатор выбранной формы границы совпадает с записанным в `05-22-SUMMARY.md`: оба значения приведены в SUMMARY рядом
    - Идентификатор выбранной ветви конфликта совпадает с записанным в `05-24-SUMMARY.md`; при ветви `upgrade-only-wins` в записи присутствует прямое утверждение о сужении D-28
    - Ни одна прежняя запись не стёрта: `grep -cE '^- \*\*D-[0-9]+:' .planning/phases/05-tarify/05-CONTEXT.md` не меньше прежнего значения плюс три
    - `05-18-SUMMARY.md` не тронут: `git diff --name-only -- .planning/phases/05-tarify/05-18-SUMMARY.md` пуст
    - Ни одной строки кода: `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` пуст
  </acceptance_criteria>
  <done>Три решения владельца записаны своими номерами по образцу D-28/D-29/D-30; запись D-30 называет верный журнальный ключ и цену ветки отката числом, причём `395` и `396` сведены — одним числом либо обоими с названной причиной различия; ни одна прежняя запись не стёрта и ни один SUMMARY не тронут.</done>
</task>

<task type="auto">
  <name>Task 2: `.planning/STATE.md` — распоряжения по двенадцати находкам раунда 6, записанное решение о тире и записи, приведённые к коду</name>
  <reversibility rating="reversible">Правятся записи операционного документа; код не трогается. Откат — обратная правка текста.</reversibility>
  <precondition>Задача 1 исполнена: решения существуют своими номерами, и записи документа могут на них ссылаться, а не пересказывать.</precondition>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (§Decisions целиком — образец формы записи решения и место, куда встают ссылки на новые номера; запись о семантике смены тарифа, переписанная планом `05-21`)
    - .planning/STATE.md (§Blockers/Concerns целиком — записи с распоряжениями по находкам раундов 3, 4 и 5: ОБРАЗЕЦ формы «одна запись на раунд, каждый номер квалифицирован раундом, у каждого своё распоряжение»)
    - .planning/STATE.md (§Deferred Items целиком — записи о записанном долге фазы и о прохибициях, оставленных `unresolved`; сюда встают решение о тире и перечень долга, который раунд 6 назвал не вменяемым фазе)
    - .planning/STATE.md (§Current Position и frontmatter `progress` — формулировка, которую план `05-21` лишил констант величины с записанной причиной. Правится СОДЕРЖАНИЕ состояния, но не возвращается счёт планов прозой)
    - .planning/phases/05-tarify/05-REVIEW.md (все двенадцать находок раунда 6: `CR-01` строки 106-186, `WR-01`…`WR-07` строки 190-519, `IN-01`…`IN-04` строки 522-604. Распоряжение пишется по СОДЕРЖАНИЮ находки, а не по её номеру)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 306-314 — `Gaps Summary` раунда 6 целиком, включая перечень записанного долга, который фазе НЕ вменяется; строки 99-119 — человеческие пункты, включая четыре пункта о прохибициях без принуждения)
    - .planning/phases/05-tarify/05-22-SUMMARY.md, 05-23-SUMMARY.md, 05-24-SUMMARY.md, 05-25-SUMMARY.md (что фактически сделано этой волной — записи документа описывают исполненное, а не запланированное)
  </read_first>
  <action>
**Распоряжения по двенадцати находкам раунда 6.** Одна новая запись §Blockers/Concerns на
раунд, по образцу записей раундов 3, 4 и 5. Каждый номер квалифицирован строкой «(раунд 6)» —
между раундами номера ПЕРЕНАЗНАЧЕНЫ, и голый номер указывает не на ту находку. Находки
разносятся на три группы, и группа называется у каждой:

- **закрыты планами этой волны** — с указанием, каким именно планом и чем именно (для `CR-01`,
  `WR-01`, `WR-02`, `WR-03`, `IN-01`, `IN-03`, `IN-04` — по факту исполненного, а не по
  замыслу; если план остановился на чекпойнте, находка попадает в третью группу с этой
  причиной);
- **отложены с обоснованием и названным владельцем-решением** — сюда идут находки, требующие
  кода вне объёма этой волны (`WR-04` о необработанном коммите после успешного создания
  платежа у ЮKassa; `WR-05` об отсутствии гарда на пустой план в подписочной ветке; `WR-06` о
  границах `limit`/`offset`; `WR-07` о сверке колонок по именам). У каждой — почему отложена и
  что она стоит, названное следствием, а не эпитетом;
- **записаны без требования кода** — `IN-02` об усечении неполного дня, закрытое фразой в
  докстринге планом `05-23`.

Ни одна прежняя запись не стирается. Устаревшие ПЕРЕСКАЗЫВАЮТСЯ с названной причиной — тем же
порядком, каким план `05-21` обошёлся с записями раунда 5.

**Записи о денежном пути приводятся к коду ПОСЛЕ этой волны.** Запись §Decisions о семантике
смены тарифа дополняется: какая форма границы стоит теперь в ветке отката и какое из двух
записанных обещаний исполняется при конфликте `upgrade-only` × D-28 — со ссылками на новые
номера решений, а не пересказом. Формулировка обязана СОВПАДАТЬ с докстрингами, переписанными
планами `05-22` и `05-24`, а не пересказывать их своими словами: расхождение двух формулировок
одного утверждения и есть тот класс, за который фаза получила три раунда подряд.

**Решение о тире прохибиций.** Новая запись §Deferred Items, ссылающаяся на номер решения из
задачи 1 и называющая ЯВНО: какие прохибиции переведены в judgment-tier (документные, планов
`05-16`, `05-19`, `05-20`, `05-21`), какие остались `test` и получили настоящий тест (кодовые,
план `05-25`), и по какому признаку проведена граница. Отдельным предложением — что перевод
тира НЕ означает снятия прохибиции: она остаётся `unresolved` и продолжает судиться человеком,
меняется только честность обещания о способе проверки.

**Записанный долг фазы перечисляется поимённо.** `Gaps Summary` раунда 6 называет восемь
пунктов, не вменяемых фазе. Каждый обязан быть НАЗВАН в §Deferred Items с распоряжением;
пункт, о котором документ молчит, потерян. Для окна срока давности намерения распоряжение
обязано назвать ВТОРОЕ следствие, вскрытое раундом 6, — через него достижимо изменение тарифа
вниз, — и сказать, чем оно закрыто планом `05-24` (видимостью или невозможностью, по ветви
ответа), а чем не закрыто (само окно остаётся открытым).

**§Current Position и frontmatter.** Состояние фазы приводится к исполненному: волна закрытия
гэпов раунда 6 исполнена, фаза ждёт верификации раунда 7. Запрет, записанный планом `05-21`,
соблюдается дословно — ни одна прозаическая строка секции не называет константы величины,
которую документ не может держать истинной, и единственный источник счёта по-прежнему назван.
Машинный `frontmatter.progress` приводится к ROADMAP, и расчёт показывается в SUMMARY.

**Три judgment-tier прохибиции фазы остаются `unresolved`** и этим планом не перемечаются:
право объявить любую из них закрытой принадлежит верификации раунда 7. Если ответ чекпойнта
плана `05-24` — `record-wins`, прохибиция BILL-05 (autonomy) остаётся `unresolved` ПО РЕШЕНИЮ,
и запись обязана сказать это прямо, а не оставить её в общем перечне.
  </action>
  <verify>
    <automated>test "$(grep -c 'раунд 6' .planning/STATE.md)" -ge 12</automated>
  </verify>
  <acceptance_criteria>
    - Команда `<verify>` завершается кодом `0`: каждая из двенадцати находок квалифицирована раундом. Обёртка `test "$(…)" -ge 12` обязательна по той же причине, что в задаче 1
    - Все двенадцать номеров присутствуют: для каждого из `CR-01`, `WR-01`…`WR-07`, `IN-01`…`IN-04` команда `grep -c "<номер>" .planning/STATE.md` возвращает не менее `1`; перечень команд и их вывод приведены в SUMMARY
    - Ни один номер не назван голым: число вхождений номеров раунда 6 равно числу квалификаций «(раунд 6)» рядом с ними — сверяется и приводится в SUMMARY числом
    - Каждая находка отнесена ровно к одной из трёх групп, и группы названы; сумма трёх групп равна двенадцати — приводится числом
    - Восемь пунктов записанного долга названы поимённо: для каждого приводится строка документа с распоряжением. Отсутствие любого из восьми проваливает задачу
    - Решение о тире записано и ссылается на номер из задачи 1: `grep -c "judgment" .planning/STATE.md` не менее `1`, и рядом названы обе половины класса
    - Формулировки о денежном пути СОВПАДАЮТ с кодом: в SUMMARY приведены две пары «фрагмент документа / фрагмент докстринга» с указанием файла и диапазона строк каждого
    - §Current Position по-прежнему без констант величины: `grep -cE 'Plan: [0-9]+ of [0-9]+|[0-9]+/[0-9]+ планов' .planning/STATE.md` возвращает `0`
    - Прежние записи не стёрты: `grep -c "раунд 5" .planning/STATE.md` не меньше, чем в `git show HEAD:.planning/STATE.md`
    - Ни одной строки кода: `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` пуст
  </acceptance_criteria>
  <done>Все двенадцать находок раунда 6 несут квалифицированные раундом распоряжения; решение о тире прохибиций записано с границей между половинами класса; записи о денежном пути совпадают с кодом; восемь пунктов записанного долга названы поимённо; §Current Position свободна от констант величины.</done>
</task>

<task type="auto">
  <name>Task 3: `COVERAGE.md` — сверка матрицы возможностей ЮKassa после раунда 6</name>
  <reversibility rating="reversible">Добавляется раздел сверки; матрица не перегенерируется. Откат — удаление раздела.</reversibility>
  <precondition>Планы `05-22`…`05-25` исполнены: сверка описывает состояние кода после волны, а не до неё.</precondition>
  <files>.planning/phases/05-tarify/COVERAGE.md</files>
  <read_first>
    - .planning/phases/05-tarify/COVERAGE.md (файл целиком: таблица решений по возможностям, построчная сверка плана `05-06`, раздел сверки после гэпа 1 плана `05-07` и раздел «Сверка после закрытия гэпов раунда 5» плана `05-21`. Последний — ОБРАЗЕЦ формы этого раздела)
    - app/services/payment_service.py (все обращения к SDK ЮKassa после этой волны: создание платежа, конфигурация, разбор уведомления. Факт устанавливается грепом, а не памятью)
    - app/routes/billing.py (гард источника уведомления и обращение к `SecurityHelper`)
    - .planning/phases/05-tarify/05-VERIFICATION.md (раздел `Gaps Summary` раунда 6 — какие возможности упоминались раундом как способные закрыть открытые окна)
  </read_first>
  <action>
Новый раздел «Сверка после закрытия гэпов раунда 6» в конец файла. Матрица НЕ
перегенерируется: раздел добавляется, прежние разделы остаются дословно.

**Первое — установить факт грепом, а не памятью.** Перечислить все обращения к SDK ЮKassa в
`app/` после волны и сравнить с тем, что заявляет матрица и построчная сверка плана `05-06`.
Если единственным вызовом остался прежний, сказать это прямо с указанием файла и номера
строки. Планы `05-22`…`05-25` меняли стадию ПРИМЕНЕНИЯ подтверждённого платежа и объявления,
а не обращения к API, — и если это так, утверждение записывается прямо, а не выводится
читателем из молчания.

**Второе — пересмотреть явно две строки, поднятые раундами 5 и 6 как способные закрыть
открытые окна свойством:** пере-запрос статуса платежа и отмену платежа. Решение по ним может
остаться прежним; ПРИЧИНА обязана быть уточнена по состоянию после раунда 6, а не скопирована.
Раунд 6 добавил к цене окна срока давности второе следствие (достижимое изменение тарифа
вниз), и это обязано быть названо в причине, даже если решение не меняется.

**Третье — сказать прямо, какие строки НЕ затронуты.** Формулировка образца плана `05-21`:
«ни одна другая строка матрицы этой работой не затронута — сказано прямо, чтобы молчание не
читалось недосмотром». Обе прежние оговорки (неподтверждённая подписка на событие отмены —
D-27; невыкаченная очередь ревизий — D-26) остаются в силе дословно.

Раздел заканчивается прямым утверждением о том, изменилось ли решение хоть по одной строке, и
если нет — что матрица остаётся действительной без правок.
  </action>
  <verify>
    <automated>test "$(grep -c 'раунда 6' .planning/phases/05-tarify/COVERAGE.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Команда `<verify>` завершается кодом `0`: новый раздел сверки существует
    - Факт установлен грепом: в разделе приведён перечень обращений к SDK с указанием файла и номера строки, и он совпадает с фактическим выводом `grep -rn "YooPayment\.\|SecurityHelper" app/`, приведённым в SUMMARY
    - Две названные возможности пересмотрены явно: `grep -c "find_one" .planning/phases/05-tarify/COVERAGE.md` и `grep -c "cancel" .planning/phases/05-tarify/COVERAGE.md` возвращают большее значение, чем в `git show HEAD:.planning/phases/05-tarify/COVERAGE.md`
    - Второе следствие окна срока давности названо в причине хотя бы одной из двух пересмотренных строк — цитируется в SUMMARY
    - Незатронутые строки названы незатронутыми: в разделе присутствует прямое утверждение об этом
    - Матрица не перегенерирована: число строк таблицы решений не изменилось — `grep -c '^| \`' .planning/phases/05-tarify/COVERAGE.md` совпадает с `git show HEAD:… | grep -c '^| \`'`, либо каждое изменение названо поимённо с причиной
    - Прежние разделы не стёрты: `git diff -- .planning/phases/05-tarify/COVERAGE.md` не содержит удалённых строк
    - Ни одной строки кода: `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` пуст
  </acceptance_criteria>
  <done>Матрица проверена на действительность после раунда 6, затронутые строки названы поимённо, незатронутые названы незатронутыми, и ни одна прежняя запись не стёрта.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `05-CONTEXT.md` → следующее решение владельца | Записанная цена решения есть основание, по которому принимаются следующие; расхождение с кодом воспроизводит гэп пятый раунд подряд |
| `.planning/STATE.md` → решение о выкате | Документ, по которому принимаются решения; заниженное последствие делает решение принятым по неверному факту |
| номер находки без раунда → находка | Между раундами номера переназначены; голый номер указывает не на ту находку |
| SUMMARY прошлых планов → сегодняшнее состояние | Исторические документы фиксируют, что было решено тогда; правка стирает причину, по которой мера понадобилась |
| тир прохибиции → существующее принуждение | Тир `test` без теста обещает проверку, которой нет; обещание пережило четыре плана |
| матрица возможностей API → следующая интеграция | Перегенерация теряет прежние решения вместе с их причинами молча |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-167 | Repudiation | Записанная цена решения D-30 и то, что отдаёт код в ветке отката, остаются расходящимися — тот же пункт, который гэп 1 раунда 5 требовал для варианта `upgrade-only` | high | mitigate | Задача 1: цена ветки названа ЧИСЛОМ, критерий приёмки требует присутствия чисел `396` и `22 780`; эпитета без числа недостаточно |
| T-05-177 | Repudiation | Правка записи D-30 ставит рядом два числа одной величины (`395` раунда 5 и `396` раунда 6), и читатель получает новое расхождение на месте закрытого — та же фигура, против которой пишется план | medium | mitigate | Задача 1: отдельный критерий приёмки требует одного из двух состояний — одно число либо оба с названной СЛОВАМИ причиной различия (дата отсчёта прогона); состояние «два числа без объяснения» проваливает задачу, и выбранное состояние называется в SUMMARY |
| T-05-168 | Repudiation | Решение владельца, снятое чекпойнтом, не возвращается в `05-CONTEXT.md` своим номером и живёт только в SUMMARY плана | high | mitigate | Задача 1: три решения своими номерами по образцу D-28/D-29/D-30; критерий приёмки считает номера и блоки рейтинга обратимости |
| T-05-169 | Tampering | Решение D-28 отменяется МОЛЧА правкой своей же записи вместо записи нового решения о сужении | high | mitigate | Задача 1: запись D-28 не переписывается; сужение записывается НОВЫМ решением с прямым указанием, что именно перестало быть верным |
| T-05-170 | Repudiation | Находка раунда 6 остаётся без распоряжения, и раунд 7 находит нарушенной собственную прохибицию документа | high | mitigate | Задача 2: двенадцать номеров разнесены на три названные группы, каждый квалифицирован раундом; критерии считают вхождения и требуют суммы, равной двенадцати |
| T-05-171 | Spoofing | Голый номер находки без раунда указывает на другой дефект: под тем же номером в прежних раундах лежит другое | medium | mitigate | Задача 2: каждое вхождение квалифицировано раундом; число вхождений и число квалификаций сверяются и приводятся в SUMMARY |
| T-05-172 | Repudiation | Пункт записанного долга не назван в документе и теряется: следующий раунд его не находит, а фаза сдаётся с ним | high | mitigate | Задача 2: восемь пунктов `Gaps Summary` перечисляются поимённо с распоряжением; отсутствие любого проваливает задачу |
| T-05-173 | Repudiation | Перевод тира читается как СНЯТИЕ прохибиции, и следующий раунд считает её закрытой | medium | mitigate | Задача 1 и 2: записано прямо, что перевод тира не снимает прохибицию — она остаётся `unresolved` и продолжает судиться человеком; меняется честность обещания о способе проверки |
| T-05-174 | Tampering | Матрица возможностей API перегенерируется, и прежние решения с их причинами теряются молча | medium | mitigate | Задача 3: раздел добавляется, критерий требует нуля удалённых строк и совпадения числа строк таблицы решений |
| T-05-175 | Repudiation | Запись документа расходится с докстрингом кода: два автора, два текста, одно утверждение — и раунд 7 находит третью формулировку | high | mitigate | Задача 2 исполняется ПОСЛЕ планов `05-22`…`05-25` и требует СОВПАДЕНИЯ формулировок; критерий требует привести в SUMMARY две пары «фрагмент документа / фрагмент докстринга» с диапазонами строк |
| T-05-176 | Information Disclosure | В операционный документ попадают значения секретов или боевые адреса при описании последствий выката | low | accept | Записи описывают ИМЕНА символов, ревизий и путей; значений секретов ни одна не содержит. Принимается тем же порядком, что для существующих записей документа |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов и не трогает кода вовсе: `git diff --name-only -- app/ tests/ alembic/ pyproject.toml uv.lock` пуст — критерий приёмки всех трёх задач. Аудит легитимности — `05-RESEARCH.md`, `## Package Legitimacy Audit` |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-26`. План не производит ни одного символа кода — он производит
записи, и они перечисляются здесь по той же причине, по которой перечисляются символы: чтобы
сверка на дрейф не приняла их за неизвестные.

**Файлы (новые):** нет

**Решения (новые), `.planning/phases/05-tarify/05-CONTEXT.md`** — три, в новом подразделе
«Решения владельца по гэпам раунда 6», под следующими свободными номерами после D-30:
- форма верхней границы переноса в ветке отката (ответ чекпойнта задачи 1 плана `05-22`)
- разрешение конфликта `upgrade-only` × D-28 (ответ чекпойнта задачи 1 плана `05-24`)
- тир прохибиций класса «объявление против кода»: машинный гейт для кодовой половины, честный
  judgment-tier для документной

**Записи (переписанные), `.planning/phases/05-tarify/05-CONTEXT.md`:**
- фрагмент D-30 о названии журнального ключа отката
- фрагмент D-30 о цене ветки отката, названной числом

**Записи (новые), `.planning/STATE.md`:**
- запись §Blockers/Concerns с распоряжениями по двенадцати находкам код-ревью раунда 6 — одна
  запись на раунд, по образцу записей раундов 3, 4 и 5
- запись §Deferred Items о тире прохибиций и границе между половинами класса

**Записи (переписанные), `.planning/STATE.md`:**
- фрагмент §Decisions о семантике смены тарифа — дополнен формой границы в ветке отката и
  разрешением конфликта
- §Current Position и поля frontmatter `stopped_at`, `last_activity`, `last_activity_desc`,
  `last_updated`, блок `progress`

**Разделы (новые), `.planning/phases/05-tarify/COVERAGE.md`:**
- «Сверка после закрытия гэпов раунда 6»

**Функции, константы, ключи журнала, имена тестов, колонки БД, Alembic revision id, маршруты,
шаблоны, CSS-селекторы, переменные окружения:** новых нет — план не трогает кода

<verification>
- Три решения владельца записаны своими номерами по образцу D-28/D-29/D-30
- Записанная цена D-30 и то, что отдаёт код в ветке отката, сведены; журнальный ключ назван
  верно; `05-18-SUMMARY.md` не тронут
- Все двенадцать находок раунда 6 несут квалифицированные раундом распоряжения, разнесённые
  на три названные группы
- Решение о тире прохибиций записано, и граница между кодовой и документной половинами класса
  названа
- Восемь пунктов записанного долга названы поимённо с распоряжением
- Формулировки документа совпадают с докстрингами кода; пары приведены в SUMMARY
- §Current Position свободна от констант величины; frontmatter `progress` сходится с ROADMAP
- `COVERAGE.md` сверена, а не перегенерирована
- `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh pyproject.toml uv.lock` пуст

**Перенесённые человеческие проверки — задачами НЕ становятся:**
- **Backstop-пункт `must_haves`:** понятность и неустареваемость новых записей — судится
  человеком, а не грепом
- Мобильная ширина 375px — план не трогает разметки
- Настоящий платёж в тестовом магазине ЮKassa; первое настоящее уведомление после выката;
  первый настоящий выкат после прохождения очереди ревизий; статус «отклонён» у отменённого
  платежа (D-27)
- Владелец риска `WR-07 (раунд 3)` (сокет Docker у веб-слоя) по-прежнему не назначен — шестой
  раунд без имени; запись обязана назвать это фактом, а не пропустить
- **Три judgment-tier прохибиции фазы остаются `unresolved`**: право объявить их закрытыми
  принадлежит верификации раунда 7, а не этому плану
</verification>

<success_criteria>
- Ни одна запись документа, по которому принимаются решения, не утверждает того, чего код не
  делает
- Записанная цена каждого решения о деньгах названа числом, а не эпитетом, допускающим два
  прочтения
- Ни одна находка раунда 6 и ни один пункт записанного долга не потеряны молчанием
- Тир каждой прохибиции описывает то, что существует в дереве, а не то, что задумано
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-26-SUMMARY.md` when done
</output>
