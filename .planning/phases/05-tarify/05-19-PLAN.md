---
phase: 05-tarify
plan: 19
type: tdd
wave: 16
depends_on: ["05-18"]
gap_closure: true
files_modified:
  - app/services/payment_service.py
  - tests/test_pages/test_billing_payment_errors.py
  - tests/test_services/test_payment_service.py
autonomous: true
requirements: [BILL-05]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Гэп 2 раунда 5, три из четырёх опровергнутых утверждений (четвёртое — план 05-20) ---
    - "Оговорка, которой докстринг `create_payment` принимает неатомарность потолка одновременных намерений, ссылается на механизм, ИСПОЛНЯЕМЫЙ на этом пути. Для платежа, заведённого формой, ветка доли месяца (D-29) недостижима по построению — гард возвращает 302 до создания платежа, — и остаточный исход окна гонки назван настоящим: обе строки несут записанное разрешение, отказа не возникает ни у одной, исход решает ПОРЯДОК подтверждения"
    - "Механизм, который дал бы нейтральность окна как СВОЙСТВО, а не как формулировку, назван в докстринге поимённо — частичный уникальный индекс «не более одного незакрытого подписочного намерения пользователя» по образцу `uq_subscriptions_active_user` ревизии `0018` — и назван НЕ СДЕЛАННЫМ, работой своего размера, а не обещанным"
    - "Оба абзаца о цене, по которой считается доля месяца, называют ту величину, которую код действительно делит: `paid / price(subscription.plan)` — сумму платежа, отнесённую к цене ДЕЙСТВУЮЩЕГО плана. Формулировка совпадает с `subscription_period.py:151-153`, а не противоречит ей; следующий человек, «чинящий» код по комментарию, больше не вернёт блокер раунда 4 правкой, выглядящей приведением в соответствие"
    - "Докстринг `create_payment` называет ОБА остаточных окна потолка, а не одно: гонку двух одновременных попыток И срок давности. Второе окно детерминировано, шире первого и доказано собственным ЗЕЛЁНЫМ тестом проекта; объявлять состояние недостижимым, имея такой тест в двухстах строках ниже, — тот же класс, за который фаза получила раунды 4 и 5"
    - "Абзац, называющий цену варианта `upgrade-only`, описывает семантику, которая существует в коде ПОСЛЕ плана 05-18, а не ту, которой больше нет. Величина названа явно, а не эпитетом «оставшиеся дни», допускающим два прочтения, отличающиеся на два порядка"
    - "AST-ловушка порядка держит КАЖДЫЙ оператор, двигающий срок, а не один из четырёх: проверка идёт по `ast.walk`, а не по `function.body`, и множество проверяемых имён ВЫВОДИТСЯ из фактического импорта модуля отсчёта, а не перечисляется константой — поэтому оператор, добавленный планом 05-18 или любым будущим, попадает под инвариант сам"
    - "Ловушка порядка доказана падающей на неверном порядке МАШИНОЙ, а не рассуждением: рядом стоит негативный контроль над синтетическим исходником с переставленными операторами, и он краснеет от СОДЕРЖАНИЯ, а не от отсутствия символа"
    - "Ни одно имя теста в затронутых файлах не обещает больше, чем держит тело: тест, заводящий два намерения ПОДРЯД, назван по тому, что он проверяет, а тест, доказывающий достижимость состояния через срок давности, несёт это в докстринге прямым текстом"
  artifacts:
    - "app/services/payment_service.py — четыре переписанных абзаца: оговорка о неатомарности потолка, два абзаца о цене доли месяца, абзац о том, что потолок делает недостижимым; плюс абзац о цене варианта `upgrade-only` в `_extend_subscription`"
    - "tests/test_pages/test_billing_payment_errors.py — `test_the_liveness_is_sampled_before_the_date_moves` на `ast.walk` с выводимым множеством имён, негативный контроль над синтетическим исходником и переименованный тест недостижимости случая (1)"
    - "tests/test_services/test_payment_service.py — докстринг `test_a_stale_intent_does_not_block_a_new_one`, называющий, что тест ДОКАЗЫВАЕТ второе окно потолка, а не только удобство пользователя"
  key_links:
    - "`app/pages/billing.py:348-349` (гард возвращает 302 ДО `create_payment`) → `:367` (`switch_authorized=not switch_refused` всегда `True` у платежа формы) → `payment_service.py:813-814` (`refused = not True = False`) → ветка `if refused:` недостижима. Цепочка из трёх звеньев, каждое верное, и произведение делает оговорку о нейтральности неверной по построению"
    - "`payment_service.py:836` (`price = _plan_price(subscription.plan)`) → величина, которую делит код. Абзацы `:762-764` и `:833-835` утверждают противоположное, причём второй — непосредственно НАД этой строкой"
    - "`_open_subscription_intents:149-157` (срок давности исключает намерение из подсчёта, но не снимает его с оплаты) → `test_a_stale_intent_does_not_block_a_new_one` (зелёный) → опровержение утверждения о недостижимости в докстринге `create_payment:195-207`"
    - "`ast.walk(function)` вместо `function.body` → видимость операторов ВНУТРИ ветвей (`:801`, `:858`, `:860`, `:883`) → инвариант «признак снимается раньше КАЖДОГО сдвига» вместо «раньше одного из четырёх»"
  prohibitions:
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT принимать риск на денежном пути ссылкой на механизм, который на этом пути не исполняется: обоснование, опирающееся на недостижимую ветку, читается следующим человеком как гарантия"
      status: unresolved
      verification: test
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT держать рядом два описания одной денежной величины, утверждающие противоположное: следующая «починка» пойдёт по комментарию и вернёт закрытый блокер правкой, выглядящей приведением в соответствие"
      status: unresolved
      verification: test
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT называть состояние недостижимым, имея в собственной суите зелёный тест, который его достигает"
      status: unresolved
      verification: test
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT давать тесту имя, обещающее инвариант шире того, что держит его тело: следующий читатель ищет покрытие по имени и получает ложную уверенность"
      status: unresolved
      verification: test
---

<objective>
**Гэп 2 отчёта `05-VERIFICATION.md` раунда 5, кодовая половина — блокирующий класс: записанные
обоснования, которых код не исполняет.**

Второй раунд подряд блокирующая находка приходит не из неверного кода, а из ВЕРНОГО кода
рядом с НЕВЕРНЫМ утверждением о нём. В раунде 4 это был один докстринг; в раунде 5 неверных
утверждений четыре, и три из них лежат в `app/services/payment_service.py`:

1. **Нейтральность окна гонки.** Докстринг `create_payment:209-218` принимает неатомарность
   потолка оговоркой о том, что остаточный риск покрыт решением D-29. Проверено по значениям:
   гард формы при отказе возвращает 302 ДО `create_payment` (`billing.py:348-349`), поэтому в
   `create_payment` приезжает только `switch_authorized = True` (`:367`), и на стадии
   применения `refused = not True = False` (`payment_service.py:813-814`). Ветка доли месяца
   для платежа, заведённого ФОРМОЙ, недостижима НИ РАЗУ. D-29 остаётся живой только для строк
   со `switch_authorized IS NULL` — заведённых до ревизии `0019`, — и это множество перестаёт
   пополняться навсегда после её выката. Риск принят по опоре, которой нет.

2. **Цена, по которой считается доля месяца.** Абзацы `:762-764` и `:833-835` дважды
   утверждают одно, а строка `:836` читает другое — причём второй абзац стоит
   НЕПОСРЕДСТВЕННО над ней. Верную формулировку несёт только
   `subscription_period.py:151-153`. Расхождение не косметическое: одно прочтение даёт полный
   месяц, другое — около девяти дней, то есть разницу между блокером раунда 4 и его
   исправлением, записанную как синонимы.

3. **Недостижимость состояния двух намерений.** Докстринг `:195-207` называет ровно одно
   остаточное окно — гонку. Второе, детерминированное и куда более широкое, не названо вовсе:
   срок давности. Собственный ЗЕЛЁНЫЙ тест проекта
   `test_a_stale_intent_does_not_block_a_new_one` доказывает обратное, стоя в двухстах строках
   от докстринга.

Плюс две находки класса ⚠️ из той же таблицы: AST-тест порядка держит ОДИН оператор из
четырёх, а его докстринг утверждает покрытие двух ветвей; и два имени тестов обещают больше,
чем держат их тела.

Purpose: гейт принимается по факту, и факт обязан быть верным. Правится не оформление —
правится УТВЕРЖДЕНИЕ, и каждая новая формулировка обязана быть проверяемой на коде, который
рядом.
Output: три абзаца, говорящих то, что код делает; ловушка порядка, держащая каждый сдвиг; и
имена тестов, равные их телам.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@.planning/phases/05-tarify/05-18-PLAN.md
</context>

<flagged_assumptions>

**Зонд краевых случаев вернул все три требования фазы `unclassified`, состояние не
изменилось.** BILL-05, BILL-06 и BILL-07 остаются `unresolved`; допущения планировщика по ним
записаны в `05-15-PLAN.md` (E-01, E-02, E-03) и здесь не дублируются. Равенство «без
молчаливых потерь» держится: 3 поднятых == 0 внесённых в `must_haves` + 3 явных допущения.

**Четвёртое опровергнутое утверждение гэпа 2 этому плану НЕ принадлежит.** Последствие
расхождения модели и боевой схемы (`.planning/STATE.md:88` и докстринг ревизии `0019`) —
предмет плана `05-20` (кодовая часть и машинная проверка) и плана `05-21` (операционный
документ). Разделение по файлам намеренное: два автора у одного абзаца — ровно та
конструкция, из-за которой фаза переписывается пятый раунд.

**`.planning/STATE.md` этим планом НЕ правится ни строкой.** Строка 124 несёт ту же неверную
оговорку, что и докстринг `:209-218`, но она принадлежит плану `05-21`, который пишет
операционный документ ПОСЛЕ того, как код перестал противоречить сам себе. `git diff
--name-only -- .planning/` у этого плана обязан быть пуст.

**Порядок относительно плана 05-18 обязателен, и вот почему.** Формулировки, которые этот
план записывает, зависят от ответа чекпойнта задачи 1 плана `05-18`: абзац о цене варианта
`upgrade-only` описывает семантику, которой после `05-18` может не существовать, а множество
операторов, двигающих срок в `_apply_extension`, могло вырасти. Написать эти абзацы раньше
значило бы записать третье неверное утверждение подряд.

**Механизм, дающий нейтральность окна как СВОЙСТВО, этим планом НЕ строится.** Частичный
уникальный индекс на незакрытое подписочное намерение — работа своего размера: своя ревизия
Alembic (восьмая либо девятая в невыкаченной очереди D-26), свой round-trip-тест, своё решение
о том, что делать с уже существующими строками, и своя обработка отказа ограничения в
`create_payment`. Он НАЗЫВАЕТСЯ в докстринге как несделанное, и это ровно то, чего требует
пункт `missing:` гэпа 2: «если нейтральность окна нужна как свойство, её обязан дать другой
механизм». Назвать механизм и не построить его — честная запись; выдать текущее состояние за
нейтральное — то, за что фаза получила этот раунд.

**Неатомарность потолка КАК ТАКОВАЯ не переоткрывается.** Отчёт называет её честно записанным
ограничением и прямо исключает из вменяемого фазе; неверна ТОЛЬКО оговорка о её
нейтральности. План правит оговорку и не трогает механизм.

**Ни одной строки поведения не меняется.** Задачи 1 и 3 — текст докстрингов и имена тестов;
задача 2 — тест. `git diff` по `app/` обязан состоять ИСКЛЮЧИТЕЛЬНО из строк внутри
докстрингов и комментариев: критерий приёмки задачи 1 проверяет это машиной, потому что
«правка только комментария» — ровно то место, где незаметно уезжает поведение.

**Три judgment-tier прохибиции фазы остаются `unresolved`** и этим планом не перемечаются.
Каноничные позиции безопасности прохибициями не минтятся — покрыты `<threat_model>` ниже.

**Пакеты не устанавливаются**; `pyproject.toml` и `uv.lock` не трогаются.

</flagged_assumptions>

<assumption_delta_decision>

**Детектор не срабатывает содержательно, и это записывается, а не умалчивается.** План не
вводит второй экземпляр ничего, не делает обязательное поле необязательным и не превращает
производную величину в выбираемую: он приводит ТЕКСТ в соответствие с уже существующим кодом.

**Существительное, о котором идёт речь:** **утверждение о механизме** — абзац докстринга,
на который опираются при принятии риска.

**Решение: `no-change`.** Сущности не заводится, модель не меняется. Но одно наблюдение
записывается, потому что оно объясняет два раунда подряд: утверждение о механизме сегодня
живёт в трёх местах — в докстринге кода, в `.planning/STATE.md` и в тексте плана, — и ни одно
из трёх не выводится из остальных. Это фактическая множественность без первичного экземпляра.
Форсирующий признак промоции назван прямо: ТРЕТИЙ раунд подряд с блокирующей находкой класса
«записанное обоснование, которого код не исполняет». При его наступлении промоция означает
машинный гейт на операционные документы — ровно ту прохибицию, которую план 05-16 объявил
`verification: test` и оставил без принуждения (отчёт раунда 5 фиксирует: тестов, проверяющих
`.planning/STATE.md`, в суите ноль).

</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Четыре абзаца `payment_service.py` начинают утверждать то, что делает код рядом</name>
  <precondition>План `05-18` исполнен: `05-18-SUMMARY.md` существует и называет идентификатор выбранной формы. Без него абзац о цене варианта `upgrade-only` не может быть написан — он описывал бы семантику, которой может не существовать.</precondition>
  <files>app/services/payment_service.py</files>
  <read_first>
    - app/services/payment_service.py (строки 161-245 — `create_payment` целиком: докстринг с абзацем о том, что потолок делает недостижимым (`:195-207`), и абзацем о том, чего потолок не даёт (`:209-218`); тело потолка `:225-244`. ⚠️ ЧИТАТЬ ОБА АБЗАЦА ДОСЛОВНО В ФАЙЛЕ — переписываются они, а не пересказываются)
    - app/services/payment_service.py (строки 736-888 — `_apply_extension` в состоянии ПОСЛЕ плана 05-18: абзац `:760-771` о том, где двигается срок и по какой цене; комментарий `:833-835` непосредственно над `price = _plan_price(subscription.plan)` — строка, читающая цену ДЕЙСТВУЮЩЕГО плана; ветка отката `:844-858`)
    - app/services/payment_service.py (строки 550-688 — `_extend_subscription`: абзац `:596-599`, называющий цену варианта `upgrade-only`, и абзац `:619-626` о числе дней у платежа с отвергнутым переходом)
    - app/application/billing/subscription_period.py (строки 146-183 — докстринг `prorated_expiry`: ЕДИНСТВЕННАЯ верная сегодня формулировка величины, к которой приводятся оба абзаца `payment_service.py`; строки 124-143 — `prorated_days` и записанная причина отсутствия верхнего потолка у ДОЛИ месяца)
    - app/pages/billing.py (строки 330-386 — `subscribe_to_plan`: гард считает ответ один раз, возвращает 302 ДО `create_payment` при отказе и передаёт отрицанием. Три звена цепочки, делающей ветку D-29 недостижимой для платежа формы, читаются здесь)
    - app/models/payment.py (строки 38-50 — комментарий у колонки `switch_authorized`: проект уже признаёт недостижимость значения `False` через форму, и новая редакция абзаца `:209-218` обязана быть согласована с этим признанием, а не спорить с ним)
    - alembic/versions/0018_subscriptions_unique_user.py (весь файл — образец частичного уникального индекса `uq_subscriptions_active_user`: механизм, который назван в новой редакции абзаца `:209-218` как способный дать нейтральность окна СВОЙСТВОМ, и которого этот план НЕ строит)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 41-67 — гэп 2 целиком: четыре опровергнутых утверждения и пять пунктов `missing:`; пункты 1, 2 и 3 — предмет этой задачи)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 321-375 — `WR-01`; строки 483-523 — `WR-04`; строки 524-575 — `WR-05`: три находки раунда 5, совпадающие с этой задачей поимённо)
    - .planning/phases/05-tarify/05-18-SUMMARY.md (идентификатор выбранной формы и её параметр — абзац о цене варианта `upgrade-only` пишется по нему)
  </read_first>
  <action>
Правятся ЧЕТЫРЕ абзаца, каждый — на своём месте, и каждый обязан пройти одну и ту же проверку:
утверждение можно подтвердить чтением кода в двадцати строках от него.

**Абзац об оговорке к неатомарности потолка (`create_payment`, около строки 209).** Снять
опору на решение D-29 и назвать настоящий остаточный исход окна гонки. Новая редакция обязана
сказать три вещи: (1) у платежа, заведённого ФОРМОЙ, записанное разрешение всегда
положительно, потому что при отказе гард возвращает 302 ДО создания платежа
(`app/pages/billing.py:348-349`, `:367`); (2) поэтому на стадии применения отказа не
возникает ни у одной из двух строк, ветка доли месяца не исполняется вовсе, и исход решает
ПОРЯДОК подтверждения — то есть тот же исход, что у разрешённого повышения; (3) решение D-29
остаётся живым только для строк со `switch_authorized IS NULL`, и после выката ревизии `0019`
это множество перестаёт пополняться навсегда. Отдельным предложением назвать механизм, который
дал бы нейтральность окна СВОЙСТВОМ: частичный уникальный индекс «не более одного незакрытого
подписочного намерения пользователя» по образцу `uq_subscriptions_active_user` ревизии `0018`
— и сказать прямо, что он НЕ построен и является работой своего размера, а не обещанием.

**Два абзаца о цене доли месяца (`_apply_extension`, около строк 762 и 833).** Привести оба к
формулировке `subscription_period.py:151-153` и назвать величину ЯВНО, выражением
`paid / price(subscription.plan)`: уплаченная сумма, отнесённая к цене ДЕЙСТВУЮЩЕГО плана.
Второй абзац стоит непосредственно над строкой, читающей эту цену, и обязан описывать именно
её. Добавить одно предложение о том, почему альтернативное прочтение опасно: полный месяц
действующего тарифа здесь и был дырой раунда 4, и следующая «починка» по прежнему комментарию
вернула бы её правкой, выглядящей приведением кода в соответствие с текстом.

**Абзац о том, что потолок делает недостижимым (`create_payment`, около строки 195).** Назвать
ОБА остаточных окна вместо одного. Первое — гонка двух одновременных попыток (уже названо).
Второе — СРОК ДАВНОСТИ: намерение старше `PENDING_INTENT_TTL_HOURS` перестаёт считаться
(`_open_subscription_intents:149-157`), но оплачиваемым быть не перестаёт — своей строки не
теряет и терминальным не становится, — поэтому подождавший сутки заводит второе намерение
другого тарифа гарантированно. Сказать, что второе окно ДЕТЕРМИНИРОВАНО и ШИРЕ первого, и что
оно есть цена неподтверждённой подписки на `payment.canceled` (D-27): закрыть его можно только
вместе с ней либо снятием намерения с оплаты при истечении срока давности, а не молчаливым
исключением строки из подсчёта. Сослаться на тест, который это доказывает, по имени.

**Абзац о цене варианта `upgrade-only` (`_extend_subscription`, около строки 596).** Описать
семантику, существующую ПОСЛЕ плана `05-18`, и назвать величину явно, а не эпитетом,
допускающим два прочтения с разницей в два порядка. Прежняя редакция говорила об остатке —
величине, ограниченной сверху одним периодом; код не ограничивал её ничем, и решение
принималось по первой формулировке. Новая редакция обязана назвать выбранную форму границы её
идентификатором из `05-18-SUMMARY.md` и сказать, ЧТО именно теперь получает повышение.

Ни одной строки исполняемого кода. Абзацы переписываются целиком, а не «смягчаются»:
косметическая переформулировка того же утверждения — ровно то, что отчёт называет
недостаточным.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_payment_service.py tests/test_pages/test_billing_payment_errors.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Поведение не изменилось: `git diff -U0 -- app/services/payment_service.py | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -vE '^[+-]\s*(#|"""|\s)' | grep -vE '^[+-]\s*[А-Яа-яA-Za-z⚠️`(*]' ` не находит ни одной строки, содержащей исполняемый оператор Python (проверяется глазами исполнителя И записывается в SUMMARY списком затронутых диапазонов строк)
    - Прежняя оговорка о нейтральности окна снята: `sed -n '160,250p' app/services/payment_service.py | grep -c "денежно нейтральным"` возвращает `0`, и в тех же строках `grep -c "ПОРЯДОК подтверждения"` возвращает не менее `1`
    - Механизм, дающий свойство, назван и назван несделанным: `sed -n '160,250p' app/services/payment_service.py | grep -c "uq_subscriptions_active_user"` возвращает не менее `1`
    - Оба абзаца о цене доли месяца называют делимое явно: `grep -c "paid / price(subscription.plan)" app/services/payment_service.py` возвращает не менее `2`
    - Прежняя формулировка о цене снята с обоих мест: `grep -c "ПО УПЛАЧЕННОМУ ТАРИФУ, А НЕ ПО ДЕЙСТВУЮЩЕМУ" app/services/payment_service.py` возвращает `0` и `grep -c "по УПЛАЧЕННОМУ тарифу, а не по действующему" app/services/payment_service.py` возвращает `0`
    - Второе окно потолка названо: `sed -n '160,250p' app/services/payment_service.py | grep -c "PENDING_INTENT_TTL_HOURS"` возвращает не менее `1`, и в тех же строках упомянут `D-27` (`grep -c "D-27"` не менее `1`)
    - Тест, доказывающий второе окно, назван в докстринге по имени: `sed -n '160,250p' app/services/payment_service.py | grep -c "test_a_stale_intent_does_not_block_a_new_one"` возвращает не менее `1`
    - Абзац о цене варианта называет идентификатор формы из `05-18-SUMMARY.md`: `sed -n '550,660p' app/services/payment_service.py | grep -cE "convert-remainder|cap-horizon|apply-after-period"` возвращает не менее `1`
    - Прежний эпитет о величине снят: `sed -n '550,660p' app/services/payment_service.py | grep -c "оставшиеся дни"` возвращает `0`
    - Суита не покраснела: команда `<verify>` завершается кодом 0
    - `git diff --name-only -- .planning/` пуст
  </acceptance_criteria>
  <done>Четыре абзаца утверждают то, что делает код рядом; каждое новое утверждение подтверждается чтением кода в двадцати строках от него; ни одной строки поведения не изменено.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Ловушка порядка держит КАЖДЫЙ сдвиг срока, и её краснота доказана машиной</name>
  <files>tests/test_pages/test_billing_payment_errors.py</files>
  <read_first>
    - tests/test_pages/test_billing_payment_errors.py (строки 1385-1450 — `test_the_liveness_is_sampled_before_the_date_moves` целиком: перебор `function.body`, помощник `_assignment_from`, проверка ВЕЛИЧИНЫ через `sampled.attr == "expires_at"` (закрытие `IN-02` раунда 4 — сделано верно и обязано сохраниться), и докстринг `:1402-1410`, утверждающий покрытие двух ветвей)
    - app/services/payment_service.py (строки 736-888 в состоянии ПОСЛЕ плана 05-18 — `_apply_extension`: операторы верхнего уровня против операторов ВНУТРИ ветвей. Сегодня из четырёх операторов, двигающих срок, на верхнем уровне стоит один; план 05-18 мог добавить ещё)
    - app/services/payment_service.py (строки 13-26 — блок импортов: `from app.application.billing.subscription_period import (...)`. Множество имён, двигающих срок, ВЫВОДИТСЯ отсюда, а не перечисляется константой в тесте — только так оператор, добавленный будущим планом, попадёт под инвариант сам)
    - app/application/billing/subscription_period.py (строки 81-184 — какие функции модуля возвращают новую точку срока: `next_expiry`, `prorated_expiry` и всё, что добавил план 05-18)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 428-482 — `WR-03` раунда 5: разбор настоящего исходника с перечнем типов операторов верхнего уровня против всех присваиваний-из-вызова внутри функции, и предложенная форма исправления)
  </read_first>
  <behavior>
    - RED (негативный контроль): помощник проверки порядка, применённый к СИНТЕТИЧЕСКОМУ исходнику функции, в котором признак живости снимается ПОСЛЕ сдвига срока, обязан сообщить о нарушении. Тест пишется первым и обязан упасть на УТВЕРЖДЕНИИ, а не на отсутствии символа: помощник создаётся заглушкой, сообщающей «нарушений нет», и только потом получает настоящую реализацию.
    - RED (негативный контроль, второй): помощник, применённый к синтетическому исходнику, где сдвиг стоит ВНУТРИ ветви, а признак снимается после него, тоже обязан сообщить о нарушении. Именно этот случай сегодняшний перебор `function.body` не видит.
    - GREEN: тот же помощник, применённый к настоящему `_apply_extension`, нарушений не находит.
    - Инвариант не перечисляет имена константой: множество имён, двигающих срок, выводится из фактического импорта модуля отсчёта в `payment_service.py`, поэтому имя, добавленное планом 05-18 или любым будущим, попадает под проверку без правки теста.
    - Инвариант держится и по ЦЕЛИ присваивания, а не только по имени вызываемого: каждое присваивание в `subscription.expires_at` стоит ПОСЛЕ снятия признака, сколько бы их ни было, и их не меньше трёх.
    - Проверка ВЕЛИЧИНЫ, от которой снимается признак (закрытие `IN-02` раунда 4), сохраняется дословно.
  </behavior>
  <action>
**Сначала негативные контроли (RED).** Помощник проверки порядка выносится из тела теста в
функцию модуля тестов, принимающую ИСХОДНИК функции строкой и возвращающую перечень нарушений.
Выносится он затем, чтобы его можно было применить к синтетическому исходнику: сегодняшний
тест проверяем только собой, и доказать его красноту можно лишь мутацией настоящего модуля —
ровно тот одноразовый приём, который планы 05-15 и 05-17 раскрыли как отсутствие RED-коммита.

Помощник создаётся ЗАГЛУШКОЙ, возвращающей пустой перечень нарушений, и негативные контроли
пишутся против неё. Оба обязаны упасть на утверждении «перечень нарушений не пуст», а не на
`ImportError` или `NameError`: тест, красневший от отсутствия символа, красноты инварианта не
доказывает, и отчёт раунда 5 фиксирует эту фигуру во frontmatter как раскрытое ограничение
двух планов подряд.

Два синтетических исходника задаются строками прямо в тесте: в первом снятие признака стоит
ниже сдвига на верхнем уровне, во втором сдвиг спрятан ВНУТРИ ветви `if`, а снятие стоит после
неё. Второй существеннее первого: сегодняшний перебор операторов верхнего уровня его не видит
вовсе, и именно это описывает находка `WR-03` раунда 5.

**Затем настоящая реализация помощника (GREEN).** Обход — `ast.walk` по всему телу функции,
а не `function.body`. Множество имён, двигающих срок, ВЫВОДИТСЯ: тест разбирает импорты модуля
`app.services.payment_service`, находит оператор импорта из
`app.application.billing.subscription_period` и берёт оттуда имена, а не перечисляет их
литералом. Литеральный перечень протух бы молча в тот момент, когда план 05-18 добавил
четвёртый сдвиг, — то есть повторил бы находку `WR-03` под новым номером.

Второй инвариант, независимый от имён: каждое присваивание, ЦЕЛЬЮ которого является
`subscription.expires_at`, стоит после снятия признака, и число таких присваиваний не меньше
трёх. Он ловит то, чего не ловит первый: сдвиг, сделанный не вызовом функции модуля отсчёта, а
любым другим выражением.

Проверка величины, от которой снимается признак (`sampled.attr` равен имени колонки срока),
сохраняется дословно: она закрывает `IN-02` раунда 4 и к находке `WR-03` отношения не имеет.

Докстринг теста переписывается по тому, что тест теперь ДЕРЖИТ, и обязан назвать, чего он не
держит. Прежняя редакция утверждала покрытие двух ветвей, имея одну, — то же утверждение о
механизме, которого нет, только в тесте.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_payment_errors.py -q -k "liveness_is_sampled or order_helper"</automated>
  </verify>
  <acceptance_criteria>
    - RED-коммит существует: `git log --oneline -- tests/test_pages/test_billing_payment_errors.py | head -3` содержит коммит с префиксом `test(05-19):`, и вывод упавшего прогона (строки `FAILED` с именами обоих негативных контролей) вставлен в `05-19-SUMMARY.md` дословно
    - Падение было на утверждении, а не на отсутствии символа: вывод в SUMMARY содержит `AssertionError`, и не содержит `ImportError` либо `NameError` в строках обоих негативных контролей
    - Обход идёт по всему телу: `grep -c "ast.walk" tests/test_pages/test_billing_payment_errors.py` возвращает не менее `1`
    - Перебор операторов верхнего уровня в этом тесте больше не используется: `sed -n '/def test_the_liveness_is_sampled_before_the_date_moves/,/^def /p' tests/test_pages/test_billing_payment_errors.py | grep -c "function.body"` возвращает `0`
    - Множество имён выводится, а не перечисляется: `sed -n '/def test_the_liveness_is_sampled_before_the_date_moves/,/^def /p' tests/test_pages/test_billing_payment_errors.py | grep -c "subscription_period"` возвращает не менее `1`
    - Инвариант по цели присваивания присутствует и требует не менее трёх сдвигов: тест содержит утверждение о числе присваиваний в `expires_at`, и `uv run pytest tests/test_pages/test_billing_payment_errors.py -q -k liveness_is_sampled` зелёный
    - Проверка величины сохранена: `sed -n '/def test_the_liveness_is_sampled_before_the_date_moves/,/^def /p' tests/test_pages/test_billing_payment_errors.py | grep -c "expires_at"` возвращает не менее `2`
    - Оба негативных контроля зелёные на настоящей реализации помощника, и настоящий `_apply_extension` нарушений не даёт
    - `git diff --name-only -- app/` пуст на коммитах этой задачи
  </acceptance_criteria>
  <done>Ловушка порядка ходит по всему телу функции, выводит множество имён из импорта, держит инвариант и по цели присваивания, и её краснота доказана двумя негативными контролями над синтетическим исходником.</done>
</task>

<task type="auto">
  <name>Task 3: Имена тестов становятся равны их телам</name>
  <files>tests/test_pages/test_billing_payment_errors.py, tests/test_services/test_payment_service.py</files>
  <read_first>
    - tests/test_pages/test_billing_payment_errors.py (строки 1885-1930 — `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form` целиком: тело заводит два намерения ПОДРЯД, о сроке давности не знает, и его докстринг подробно объясняет, чем тест отличается от соседнего — этот абзац обязан пережить переименование дословно, иначе следующий читатель удалит один из двух как дубликат)
    - tests/test_services/test_payment_service.py (строки 695-730 — `test_a_stale_intent_does_not_block_a_new_one`: тело и докстринг. Имя описывает тело верно; неверно молчание докстринга о том, что этот же тест ДОКАЗЫВАЕТ второе остаточное окно потолка)
    - app/services/payment_service.py (строки 195-218 в состоянии ПОСЛЕ задачи 1 — новая редакция абзаца об обоих окнах: докстринги тестов обязаны быть согласованы с ней, а не спорить)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 549-575 — вторая половина `WR-05` раунда 5: чем имя обещает больше тела и какое имя предлагается взамен)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 57-60 — таблица артефактов гэпа 2 с обоими именами; строка 64 — пункт `missing:` о переименовании)
  </read_first>
  <action>
**Переименование.** `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form`
получает имя, равное тому, что держит тело: тест заводит два намерения ПОДРЯД и проверяет, что
второе с ДРУГИМ тарифом отвергается, пока первое СВЕЖЕЕ. Имя, предложенное ревью раунда 5, —
`test_a_second_intent_of_another_plan_is_refused_while_the_first_is_fresh`. Тело не трогается
ни строкой: предмет правки — имя, а не покрытие.

Абзац докстринга, объясняющий, чем этот тест отличается от
`test_a_pro_deal_sold_before_any_subscription_is_not_erased_by_a_later_basic`, переносится
дословно: он существует затем, чтобы следующий читатель не удалил один из двух как дубликат,
и его потеря была бы закрытием дефекта удалением записи о нём.

Добавляется одно предложение о том, чего тест НЕ держит: он ничего не знает о сроке давности,
и состояние, которое он объявляет недостижимым, достижимо через сутки — с отсылкой к
`test_a_stale_intent_does_not_block_a_new_one` по имени. Без этого предложения новое имя
честнее старого, но читатель по-прежнему не узнает, где искать второе окно.

Все места, ссылающиеся на прежнее имя, обновляются вместе с ним — включая перечни имён тестов
в артефактах планов фазы, если они ссылаются на него как на существующий символ.

**Докстринг `test_a_stale_intent_does_not_block_a_new_one`.** Имя остаётся: оно описывает тело
верно. Правится молчание. Докстринг обязан сказать прямым текстом, что этот зелёный тест
ДОКАЗЫВАЕТ существование второго остаточного окна потолка: намерение старше срока давности
перестаёт считаться, но оплачиваемым быть не перестаёт, поэтому подождавший сутки заводит
второе намерение другого тарифа гарантированно. Сослаться на новую редакцию абзаца
`create_payment`, которую записала задача 1, и назвать, что закрытие окна требует снятия
намерения с оплаты при истечении срока давности либо подтверждённой подписки на
`payment.canceled` (D-27) — то есть работы своего размера, этим планом не выполняемой.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_payment_errors.py tests/test_services/test_payment_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Прежнее имя не осталось нигде в дереве: `grep -rn "test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form" tests/ app/ | wc -l` возвращает `0`
    - Новое имя существует ровно один раз как определение: `grep -c "def test_a_second_intent_of_another_plan_is_refused_while_the_first_is_fresh" tests/test_pages/test_billing_payment_errors.py` возвращает `1`
    - Тело переименованного теста не изменилось: `git diff -U0 -- tests/test_pages/test_billing_payment_errors.py` показывает изменения только в строке `def` и внутри докстринга — ни одного изменённого `assert`, ни одного изменённого вызова помощника
    - Абзац-различитель сохранён: в докстринге переименованного теста присутствует имя `test_a_pro_deal_sold_before_any_subscription_is_not_erased_by_a_later_basic` (`grep -c` не менее `1` в диапазоне теста)
    - Переименованный тест называет, чего не держит: в его докстринге присутствует имя `test_a_stale_intent_does_not_block_a_new_one`
    - Докстринг `test_a_stale_intent_does_not_block_a_new_one` называет второе окно: в диапазоне теста `grep -c "D-27"` не менее `1`, и присутствует слово о том, что намерение остаётся оплачиваемым
    - Оба файла зелёные: команда `<verify>` завершается кодом 0
    - `git diff --name-only -- app/ .planning/` пуст на коммитах этой задачи
  </acceptance_criteria>
  <done>Ни одно имя теста в затронутых файлах не обещает инварианта шире того, что держит тело; тела не тронуты; абзац-различитель сохранён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| докстринг → следующий человек, правящий код | Утверждение читается как гарантия и служит основанием правки; неверное утверждение направляет правку в обратную сторону |
| комментарий над строкой → сама строка | Расстояние в один оператор; расхождение между ними — самое дорогое, потому что выглядит опечаткой, а не дефектом |
| имя теста → поиск покрытия по имени | Читатель, ищущий, покрыт ли инвариант, останавливается на имени и не читает тело |
| AST-тест → множество операторов, двигающих срок | Тест видит ровно то подмножество, которое перечислил его автор; выросшее множество остаётся вне инварианта молча |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-110 | Repudiation | Риск неатомарности потолка принят ссылкой на ветку, недостижимую для платежа, заведённого формой: обоснование выглядит принятым решением, а опоры под ним нет | high | mitigate | Задача 1: абзац переписан, настоящий остаточный исход назван (порядок подтверждения), механизм, дающий свойство, назван поимённо и назван НЕ построенным; критерии приёмки держат обе стороны грепом |
| T-05-111 | Tampering | Следующая «починка» идёт по комментарию: `_plan_price(subscription.plan)` меняется на цену плана ПЛАТЕЖА, и блокер раунда 4 возвращается правкой, выглядящей приведением кода в соответствие с текстом | high | mitigate | Задача 1: оба абзаца называют делимое выражением `paid / price(subscription.plan)`; критерий приёмки требует не менее двух вхождений и нуля вхождений прежней формулировки |
| T-05-112 | Repudiation | Состояние объявлено недостижимым при наличии зелёного теста, который его достигает: аргументация плана 05-17 стоит на утверждении, фальсифицированном собственной суитой | high | mitigate | Задачи 1 и 3: докстринг называет ОБА окна, тест, доказывающий второе, назван в нём по имени, а докстринг самого теста перестаёт молчать о том, что он доказывает |
| T-05-113 | Tampering | Правка «только комментария» незаметно уносит строку поведения: диффы докстрингов не читают внимательно | high | mitigate | Задача 1: критерий приёмки требует машинного разбора диффа на отсутствие исполняемых операторов и записи затронутых диапазонов строк в SUMMARY; суита прогоняется до и после |
| T-05-114 | Elevation of Privilege | Инвариант порядка держит подмножество операторов: перестановка внутри ветви возвращает блокер истёкшего срока молча | high | mitigate | Задача 2: обход `ast.walk`, множество имён выводится из импорта, второй инвариант — по ЦЕЛИ присваивания с нижней границей числа сдвигов |
| T-05-115 | Repudiation | Краснота инварианта доказана одноразовой мутацией настоящего модуля, удалённой до коммита: доказательства не остаётся, и следующий раунд не может его перепроверить | medium | mitigate | Задача 2: два негативных контроля над СИНТЕТИЧЕСКИМ исходником живут в суите постоянно; критерий приёмки требует падения на `AssertionError`, а не на `ImportError` |
| T-05-116 | Repudiation | Переименование теста уносит абзац-различитель, и следующий читатель удаляет один из двух тестов как дубликат | medium | mitigate | Задача 3: тело не трогается, абзац переносится дословно, критерий приёмки грепом требует его присутствия и отсутствия изменённых `assert` в диффе |
| T-05-117 | Information Disclosure | Новая редакция докстринга печатает величины чужих платежей либо секреты конфигурации | low | accept | Абзацы описывают ПРАВИЛА и имена символов; ни один не содержит значений, приходящих из данных пользователя. Принимается тем же порядком, что для соседних абзацев модуля |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов: `git diff --name-only -- pyproject.toml uv.lock` пуст. Аудит легитимности — `05-RESEARCH.md`, `## Package Legitimacy Audit`; задач вида «установить X» в плане нет |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-19`.

**Файлы (новые):** нет

**Функции (новые):**
- помощник проверки порядка над ИСХОДНИКОМ функции, возвращающий перечень нарушений —
  `tests/test_pages/test_billing_payment_errors.py` (выносится из тела существующего теста,
  чтобы его можно было применить к синтетическому исходнику)

**Имена тестов (новые):**
- `test_a_second_intent_of_another_plan_is_refused_while_the_first_is_fresh` — переименование
  `test_the_two_authorized_intents_of_case_one_are_unreachable_through_the_form`, тело не
  меняется
- два негативных контроля помощника порядка над синтетическими исходниками (переставленный
  верхний уровень и сдвиг внутри ветви)

**Изменённые тела существующих тестов:**
- `test_the_liveness_is_sampled_before_the_date_moves` — обход, выведение множества имён,
  второй инвариант по цели присваивания; проверка величины сохраняется дословно

**Изменённые докстринги (существующие):**
- `test_a_stale_intent_does_not_block_a_new_one` — называет, что тест ДОКАЗЫВАЕТ второе окно
  потолка
- `test_the_liveness_is_sampled_before_the_date_moves` — перестаёт утверждать покрытие,
  которого не было

**Якоря докстрингов (переписанные), `app/services/payment_service.py`:** абзац об оговорке к
неатомарности потолка; два абзаца о цене доли месяца; абзац о том, что потолок делает
недостижимым; абзац о цене варианта `upgrade-only` в `_extend_subscription`

**Константы, исключения, колонки БД, Alembic revision id, ключи журнала, CSS-селекторы,
маршруты, шаблоны, переменные окружения:** новых нет

<verification>
- `uv run pytest tests/ -q` зелёный целиком
- Ни одной строки поведения: дифф `app/services/payment_service.py` состоит из строк
  докстрингов и комментариев, затронутые диапазоны перечислены в SUMMARY
- Каждое новое утверждение подтверждается чтением кода в двадцати строках от него, и в SUMMARY
  для каждого из четырёх абзацев названо, ЧЕМ именно оно подтверждается
- RED-коммит задачи 2 существует, оба негативных контроля падали на `AssertionError`, и вывод
  вставлен в SUMMARY дословно
- Прежнее имя переименованного теста не встречается в дереве ни разу
- `git diff --name-only -- .planning/ app/templates/ app/static/ alembic/ app/models/` пуст

**Перенесённые человеческие проверки — задачами НЕ становятся:**
- Мобильная ширина 375px — план не трогает ни строки разметки
- Настоящий платёж в тестовом магазине ЮKassa и первое настоящее уведомление после выката
- Три judgment-tier прохибиции фазы остаются `unresolved`
- Владелец риска `WR-07 (раунд 3)` по-прежнему не назначен
- Частичный уникальный индекс на незакрытое подписочное намерение НАЗВАН и НЕ построен — это
  работа своего размера, и её отсутствие записано в докстринге, а не умолчано
</verification>

<success_criteria>
- Ни один докстринг затронутых функций не утверждает того, чего код рядом не делает
- Оговорка о неатомарности потолка опирается на исполняемый механизм, а механизм, дающий
  свойство, назван и назван несделанным
- Обе формулировки о цене доли месяца называют одну и ту же величину, и она равна той, что
  делит код
- Оба остаточных окна потолка названы, и тест, доказывающий второе, назван по имени
- Ловушка порядка держит каждый сдвиг срока, а её краснота доказана постоянно живущими
  негативными контролями
- Имена тестов равны их телам; абзац-различитель сохранён
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-19-SUMMARY.md` when done
</output>
