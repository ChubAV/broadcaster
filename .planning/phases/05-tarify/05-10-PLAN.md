---
phase: 05-tarify
plan: 10
type: execute
wave: 7
depends_on: ["05-08", "05-09"]
gap_closure: true
files_modified:
  - app/services/payment_service.py
  - app/pages/billing.py
  - app/templates/billing/balance.html
  - app/templates/billing/includes/plan_card.html
  - tests/test_pages/test_billing_payment_errors.py
  - .planning/phases/05-tarify/05-UI-SPEC.md
  - .planning/STATE.md
autonomous: false
requirements: [BILL-05, BILL-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Состояния отказа обеих форм оплаты (U1, U2, U3) ---
    - "U1/U2: отказ API ЮKassa при создании платежа возвращает человека на /billing с названной причиной, а не необработанной пятисоткой — обе формы подписки (блок 1 и три карточки планов) ведут себя одинаково"
    - "U3: отказ API ЮKassa на форме покупки пакета ведёт себя тем же способом — один порядок проверок на обоих входах означает один ответ на отказ"
    - "U3: каждая ветка отказа-гарда несёт причину: выключенные платежи, ушедший из конфига план и негодный индекс пакета различимы на экране, а не сливаются в возврат на неизменившуюся страницу"
    - "Текст стороннего исключения ЮKassa НЕ печатается на экране ни в одной ветке: множество сообщений закрыто, код причины в разметку не подставляется"
    - "Неизвестный код причины в адресе не печатает ничего: подстановка произвольного текста через параметр запроса невозможна"
    - "Отказ создания платежа не оставляет строку payments: платёж не заводится в журнале, если ЮKassa его не создала"
    - "Отказ виден в журнале ключом payment_create_failed с идентификатором пользователя и предметом покупки"
    # --- Семантика смены плана (C2 / WR-02) ---
    - "C2: судьба оплаченного остатка при смене тарифа объявлена решением владельца, а не выведена из кода — до этого плана она не была названа ни в must_haves, ни в D-решениях"
    - "Понижение тарифа не превращает оплаченный остаток старшего плана в дни младшего молча: выбранная семантика закреплена тестами на basic→pro и pro→basic с неистёкшим остатком"
    - "Прохибиция 05-01 «не сжигать неистраченный остаток» после правки либо соблюдена, либо явно переопределена решением владельца с записью — молчаливого нарушения не остаётся"
    # --- Лифт `## UI Considerations` 05-UI-SPEC.md, поверхности E1 и E3 ---
    - "R1 (E1): при отсутствии expires_at блок печатает «бессрочный стартовый тариф», а не пустую карточку; истёкший срок даёт badge('срок истёк','warning') и предупреждающую строку"
    - "R2 (E1): план, которого нет в перечне конфига, не получает кнопку продления — current_in_config гасит форму вместо обещания оплаты, которой некуда идти"
    - "R3 (E1): ни одно правило раздела не объявляет блоку 1 nowrap и фиксированную ширину — длинная предупреждающая строка переносится потоком"
    - "R4 (E1): обе строки блока приходят из закрытых множеств — идентификатор плана из конфига и дата через format_datetime_for_user"
    - "R10 (E3): ровно три карточки; текущая помечена card--current, тег «ВАШ ПЛАН» важнее «БЕЗ ЛИМИТОВ», одновременно не рисуются"
    - "R11 (E3): ось со значением null печатается ∞ — ни нулём, ни большим числом; free формы не получает вовсе; payments_enabled=false гасит кнопку, а не витрину"
    - "R12 (E3): [data-plans] объявлен repeat(auto-fit, minmax(260px,1fr)) с gap 13px — на полотне 343px колонка ровно одна"
    # --- Backstop-пункты ---
    - statement: "C3: человек, вернувшийся с ЮKassa до прихода уведомления, читает строку истории «в обработке» как «деньги в обработке», а не как «оплата не прошла» — суждение о формулировке, не свойство разметки; перенесено в UAT планом 05-04 и остаётся там"
      verification: backstop
    - statement: "Сообщение об отказе оплаты прочитывается человеком как «попробуйте ещё раз», а не как «с вас списали и не зачли» — понятность формулировки судится человеком"
      verification: backstop
  artifacts:
    - "app/services/payment_service.py — PaymentCreationError и обёртка вызова SDK"
    - "app/pages/billing.py — _payment_error_message, закрытое множество причин отказа"
    - "app/templates/billing/balance.html — плашка отказа поверх раздела"
    - "tests/test_pages/test_billing_payment_errors.py — покрытие всех веток отказа обеих форм"
    - ".planning/phases/05-tarify/05-UI-SPEC.md — U1-U3 закрыты, C2 разрешён решением, новые строки в Copywriting Contract"
  key_links:
    - "create_payment → PaymentCreationError → обработчик формы → 302 на /billing?error=payment"
    - "GET /billing?error=<code> → _payment_error_message → контекст error_message → alert в balance.html"
    - "решение о смене плана → _extend_subscription и/или гард subscribe_to_plan → карточка плана"
  prohibitions:
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT возвращать человека на неизменившуюся страницу после нажатия кнопки оплаты — молчаливый редирект читается как «кнопка сломана», и следующим действием становится повторное нажатие"
      status: resolved
      verification: test
    - requirement_id: BILL-05
      category: fairness
      statement: "MUST NOT превращать оплаченный остаток старшего тарифа в дни младшего без объявленного правила и без слова пользователю до нажатия кнопки"
      status: resolved
      verification: judgment
---

<objective>
Пять неразрешённых соображений `05-UI-SPEC.md` и одно предупреждение
`05-VERIFICATION.md`, оставшиеся после закрытия обоих блокеров.

**U1-U3 — у раздела нет ответа на неудавшуюся оплату.** `create_payment` зовётся без
обёртки (`app/pages/billing.py:193` и `:256`), поэтому отказ API ЮKassa выходит
необработанной пятисоткой, а не состоянием раздела. Все отказы-гарды при этом
возвращают голый редирект на `/billing` без единого слова (`:185`, `:191`, `:245`,
`:251`, `:253`). Из отрисованной разметки эти ветки недостижимы — но **устаревшая
страница достижима**: администратор выключает платежи или план уходит из конфига между
отрисовкой и нажатием, и человек жмёт «Продлить», возвращаясь на ту же страницу без
объяснений.

**C2 / WR-02 — семантика смены плана не объявлена нигде.** `_extend_subscription`
(`payment_service.py:277-281`) перезаписывает план безусловно, а срок двигает от
СУЩЕСТВУЮЩЕГО: подписчик Pro с 25 неистёкшими днями, купивший Basic, немедленно
понижается и сохраняет эти дни — то есть 4 900 ₽, уже уплаченные за них, уничтожаются.
Симметрично, Basic, купивший Pro, получает лимиты Pro на остаток Basic бесплатно. D-03
переход между платными тарифами из фазы ИСКЛЮЧИЛ, а карточка чужого платного плана при
этом показывает «Перейти на {план}» и о судьбе остатка молчит.

Purpose: раздел, который берёт деньги, обязан отвечать на отказ словами, а правило
обращения с уже уплаченным обязано быть решением человека, а не следствием порядка
двух строк.
Output: закрытое множество состояний отказа на обеих формах; объявленная семантика
смены плана.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-UI-SPEC.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
</context>

<!-- planner-discipline-allow: ⚠ unresolved -->

<flagged_assumptions>
**U1-U5 вошли в планирование как `⚠ unresolved` строки `## UI Considerations`.** U4 и
U5 закрыты планом `05-09` (пустое состояние списка пакетов). U1, U2 и U3 закрываются
здесь задачей 1 и переходят в `✅ resolved (explicit)`. Ни одна из пяти не снята
умолчанием; полная сверка счёта 42 + 3 — в `05-09-PLAN.md`.

**C2 вошёл как `⚠ unresolved` с формулировкой «интерфейсного ответа сегодня нет».**
Он не разрешается планировщиком: семантика смены плана — продуктовое решение, которого
владелец не принимал, и вывести его из кода значило бы задним числом объявить
правильным то, что верификация назвала дефектом. Задача 2 — чекпойнт.

**Замечания код-ревью, не подтверждённые верификацией:**

| # | Finding | Распоряжение |
|---|---------|--------------|
| WR-05 (половина про обработку ошибок) | Вызов SDK без `try/except` даёт пользователю голую пятисотку | **Принято в работу**, задача 1 — это и есть U1-U3. |
| WR-05 (половина про блокирующий вызов) | Синхронный вызов SDK в событийном цикле без таймаута | **Отложено с обоснованием.** Перенос в поток меняет модель исполнения платёжного вызова и требует своего наблюдения за временем ответа; таймаута SDK всё равно не предоставляет, поэтому один `to_thread` проблему зависшего TLS-соединения не решает, а перекладывает. Отдельная работа. Записывается долгом задачей 3. |
| IN-06 | Кнопка «Продлить» рисуется для плана `free` с прошедшим сроком и упирается в молчаливый отказ | **Принято в работу**, задача 1: это ровно тот случай «управляющий элемент, который ничего не делает», который U3 называет по имени. После правки ветка либо не рисуется, либо её отказ говорит словами. |
| IN-01 | Параметр `type` в `add_messages` затеняет встроенное имя | **Отложено с обоснованием** (план `05-08`): переименование меняет ключевое слово у всех вызывающих и к гэпам отношения не имеет. |
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Закрытое множество состояний отказа на обеих формах оплаты (U1, U2, U3, IN-06)</name>
  <files>app/services/payment_service.py, app/pages/billing.py, app/templates/billing/balance.html, tests/test_pages/test_billing_payment_errors.py</files>
  <read_first>
    - app/pages/billing.py (весь файл — `billing_page` со строки 39, `subscribe_to_plan` со строки 151, `purchase_package` со строки 205; важны ВСЕ пять веток голого редиректа: 185, 191, 245, 251, 253)
    - app/services/payment_service.py (строки 55-125 — `create_payment` целиком: где стоит вызов SDK, где `db.add`, где `commit`; порядок решает, останется ли строка платежа после отказа)
    - app/templates/billing/balance.html (строки 1-45 — шапка шаблона, импорты макросов и место, куда встаёт плашка; строки 74-102 — блок 1 с формой продления при истёкшем сроке)
    - app/templates/components/alert.html (весь файл — сигнатура макроса и правило «макрос НЕ читает request из контекста; значение вычисляется в вызывающем шаблоне и приходит параметром»)
    - app/pages/history.py (как соседний раздел сообщает об отказе действия — образец формулировки и способа передачи в контекст, если он там есть)
    - .planning/phases/05-tarify/05-UI-SPEC.md (`## Copywriting Contract` — существующие Error state строки; `## UI Considerations` U1-U3 и D1-D6 с основанием «в разделе нет ни одного тега скрипта»)
  </read_first>
  <behavior>
    - RED: отказ `YooPayment.create` на `POST /billing/subscribe` даёт 302 на `/billing?error=payment`, а не пятисотку. Сегодня исключение выходит наружу — тест обязан упасть до правки.
    - RED: тот же отказ на `POST /billing/purchase` даёт 302 на `/billing?error=payment`.
    - Отказ создания платежа не оставляет ни одной строки в `payments`.
    - `POST /billing/subscribe` при выключенных платежах даёт 302 на `/billing?error=disabled`.
    - `POST /billing/subscribe` с идентификатором плана, которого нет в конфиге, даёт 302 на `/billing?error=plan`.
    - `POST /billing/subscribe` с `plan=free` даёт 302 на `/billing?error=plan`.
    - `POST /billing/purchase` с нечисловым и с внедиапазонным индексом даёт 302 на `/billing?error=package`.
    - `GET /billing?error=payment` печатает сообщение об отказе оплаты.
    - `GET /billing?error=<неизвестный код>` не печатает ни плашки, ни самого кода.
    - `GET /billing` без параметра не печатает плашки вовсе.
    - Текст стороннего исключения ЮKassa не встречается в разметке ни в одной ветке.
    - Кросс-доменный POST по-прежнему даёт 403, а не редирект с причиной: чужому источнику причина отказа не сообщается.
  </behavior>
  <action>
**1. Сначала тесты (RED).** Новый файл `tests/test_pages/test_billing_payment_errors.py`,
все пункты `<behavior>` отдельными тестами. Отказ SDK моделируется тем же способом,
каким `tests/test_services/test_payment_service.py` подменяет успешный вызов, — только
подменённый объект поднимает исключение. Два первых пункта обязаны быть КРАСНЫМИ до
шага 2.

**2. `app/services/payment_service.py`.** Объявить `PaymentCreationError` — своё
исключение модуля, наследник `Exception`. В `create_payment` обернуть вызов SDK в
`try/except Exception`: записать `logger.error("payment_create_failed", ...)` с
идентификатором пользователя и предметом покупки и поднять `PaymentCreationError from exc`.

Текст стороннего исключения уходит В ЖУРНАЛ и НИКОГДА на экран — прецедент R-03-09
Фазы 3 (раскрытие текста стороннего исключения в плашке принято владельцем риском
severity medium; повторять его на денежном пути не следует). Обёртка ставится так,
чтобы строка `payments` при отказе НЕ появлялась: вызов SDK предшествует записи в БД, и
этот порядок обязан сохраниться.

**3. `app/pages/billing.py` — закрытое множество причин.** Ввести
`_payment_error_message(code)` — отображение кода причины в готовую строку, ровно
четыре ключа:
- `payment` → `Не удалось начать оплату — попробуйте ещё раз через минуту`
- `disabled` → `Оплата сейчас недоступна — обратитесь к администратору`
- `plan` → `Этот тариф больше не предлагается — обновите страницу и выберите другой`
- `package` → `Этот пакет больше не предлагается — обновите страницу и выберите другой`

Неизвестный код даёт пустой результат. **Множество закрыто намеренно:** в разметку
уходит строка ИЗ ЭТОГО отображения, а не значение параметра запроса, поэтому подставить
через адрес произвольный текст невозможно (T-05-46). Код причины в разметке не печатается
ни значением, ни атрибутом.

Пять веток голого редиректа получают свой код:
- `settings.yookassa_enabled` ложь (строки 185 и 245) → `disabled`;
- план не найден или равен `free` (строка 191) → `plan`;
- индекс пакета нечисловой или вне диапазона (строки 251 и 253) → `package`;
- `PaymentCreationError` из обоих вызовов → `payment`.

Проверка источника (`is_same_origin`, строки 181 и 241) **остаётся 403 без причины**:
чужому источнику причина отказа не сообщается, и порядок проверок не меняется — сначала
кто пришёл, потом откуда, потом что просит.

`billing_page` читает `request.query_params.get("error")`, прогоняет через
`_payment_error_message` и кладёт результат в контекст отдельным ключом. Обработчик
по-прежнему НЕ ПИШЕТ В БД ни при каких условиях (D-05) — параметр запроса ничего не
меняет, только показывает.

**4. `app/templates/billing/balance.html`.** Плашка `alert(..., 'error')` рисуется
первой в разделе — до блока текущего тарифа, чтобы человек прочитал причину раньше, чем
увидит неизменившиеся числа. Только при непустом значении из контекста. Макрос
`alert` берёт значение параметром, а не читает `request` (правило выписано в его
докстринге). Ни одного тега скрипта не добавляется: раздел не несёт их вовсе (основание
снятия D1-D6 в контракте), и плашка приезжает отрисованной серверным обходом.

**5. IN-06.** Условие формы продления при истёкшем сроке (`balance.html:95`) сегодня
пропускает план `free`, потому что `free` есть в конфиге. Дополнить условие проверкой,
что план не бесплатный, — зеркалом `FREE_PLAN_ID` на стороне обработчика. Кнопка,
упирающаяся в отказ, читается как сломанный платёжный путь, и после шага 3 она стала бы
ещё и печатать причину там, где её вообще не следовало предлагать.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_payment_errors.py tests/test_pages/test_billing_section.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_billing_payment_errors.py -q` завершается с кодом 0
    - `uv run pytest tests/ -q` завершается с кодом 0
    - `grep -c 'PaymentCreationError' app/services/payment_service.py` >= 2 — объявление и подъём
    - `grep -c 'PaymentCreationError' app/pages/billing.py` >= 1
    - `grep -c 'payment_create_failed' app/services/payment_service.py` >= 1
    - `grep -c '_payment_error_message' app/pages/billing.py` >= 2 — объявление и вызов
    - `grep -c 'error=payment' app/pages/billing.py` >= 2 — обе формы
    - `grep -c 'error=disabled' app/pages/billing.py` >= 2
    - `grep -c 'url="/billing"' app/pages/billing.py` == 0 — голых редиректов без причины не осталось
    - `grep -c 'alert(' app/templates/billing/balance.html` >= 1
    - `grep -c '<script' app/templates/billing/balance.html` == 0
    - Поведение: отказ `YooPayment.create` на обеих формах даёт 302 с `Location`, равным `/billing?error=payment`, и ноль строк в таблице `payments`
    - Поведение: `GET /billing?error=payment` печатает строку `Не удалось начать оплату — попробуйте ещё раз через минуту`
    - Поведение: `GET /billing?error=%3Cscript%3E` не печатает ни плашки, ни подставленного значения параметра
    - Поведение: кросс-доменный `POST /billing/subscribe` отдаёт 403 без параметра причины
    - Поведение: истёкшая подписка с планом `free` не рисует форму продления
  </acceptance_criteria>
  <done>Обе формы оплаты отвечают на отказ названной причиной; произвольный текст через адрес подставить нельзя; управляющих элементов, упирающихся в молчаливый отказ, в разделе не осталось.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Что происходит с оплаченным остатком, когда пользователь покупает ДРУГОЙ платный тариф — семантика, не объявленная сегодня нигде (C2 / WR-02)</decision>
  <context>
**Что делает код сегодня.** `_extend_subscription` перезаписывает `plan` безусловно, а
`expires_at` двигает от существующего срока. Следствия обе стороны:
- **Понижение.** Подписчик Pro (4 900 ₽/мес) с 25 неистёкшими днями покупает Basic
  (1 490 ₽). Немедленно становится Basic и сохраняет эти 25 дней — то есть 4 900 ₽,
  уплаченные за них, превращаются в дни младшего тарифа.
- **Повышение.** Подписчик Basic, купивший Pro, получает лимиты Pro на остаток Basic
  бесплатно.

**Почему это решает владелец, а не планировщик.** D-03 переход между платными тарифами
из фазы ИСКЛЮЧИЛ со словами «там отдельный класс правил о неистраченном остатке
оплаченного срока». Семантика не названа ни в одном D-решении, ни в одном `must_haves`.
При этом путь **достижим**: карточка чужого платного плана рисует «Перейти на {план}», а
обработчик такой платёж принимает.

**Ограничение, которое нельзя обойти молча.** Прохибиция плана `05-01` — «MUST NOT
сжигать неистраченный остаток уже оплаченного периода: пользователь заплатил за эти дни»
— действует. Вариант, сжигающий остаток, обязан быть переопределением этой прохибиции
решением владельца, записанным явно, а не её тихим нарушением. Предложение код-ревью
(«смена плана начинает свежий период от сегодня») — именно такой вариант, и в этом виде
он назван ниже честно.

**Что общего у всех вариантов.** Ни один не требует миграции, ни один не меняет внешний
контракт — дверь обратимая. Различаются они тем, кому достаётся уже уплаченное.
  </context>
  <options>
    <option id="block-switch">
      <name>Смена платного тарифа не предлагается и не принимается, пока подписка действует</name>
      <pros>Исполняет D-03 буквой: перехода между платными тарифами в продукте нет, поэтому и уничтожать нечего. Прохибиция 05-01 соблюдена без оговорок. Защита на обоих концах: карточка чужого платного плана вместо CTA несёт подпись о том, что смена возможна после окончания срока, а обработчик отвергает такой платёж с причиной (`error=plan` из задачи 1) — то есть устаревшая страница тоже безопасна. Первая покупка с Free и продление своего тарифа работают как работали.</pros>
      <cons>Подписчик Basic не может перейти на Pro вовсе, пока не истечёт срок — для растущего клиента это отказ продать. Возврат возможности потребует той самой отдельной работы, которую D-03 отложил.</cons>
    </option>
    <option id="fresh-period">
      <name>Смена тарифа начинает свежий месяц от сегодня; остаток прежнего сгорает, и об этом сказано ДО нажатия</name>
      <pros>Путь остаётся открытым: перейти на Pro можно в любой момент. Правило простое и объяснимое одной строкой. Реализация — три строки в `_extend_subscription` плюс подпись на карточке.</pros>
      <cons>**Прямо переопределяет прохибицию 05-01** — остаток сгорает, и это обязано быть записано решением, а не пройти молча. Понижение тарифа становится способом потерять деньги, а предупреждение на карточке — единственной защитой пользователя. Пропорционального пересчёта нет, поэтому справедливость держится только на том, что человек прочитал подпись.</cons>
    </option>
    <option id="upgrade-only">
      <name>Повышение разрешено и не сжигает остаток; понижение не предлагается и не принимается</name>
      <pros>Закрывает денежно опасную половину (уничтожение уплаченного за старший тариф) и оставляет открытой безопасную. Прохибиция 05-01 соблюдена: остаток не сгорает ни в одном разрешённом переходе. Порядок тарифов уже задан конфигом, отдельного справочника не нужно.</pros>
      <cons>Вводит в продукт понятие «старше/младше», которого в конфиге сегодня нет явно — порядок придётся объявить и где-то держать. Повышение по-прежнему даёт лимиты Pro на остаток Basic бесплатно; это подарок, а не потеря, но правилом он тоже обязан быть названным. Асимметрия требует объяснения на экране.</cons>
    </option>
  </options>
  <resume-signal>Выберите: block-switch, fresh-period или upgrade-only</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Исполнение решения о смене плана и запись оставшихся долгов</name>
  <precondition>Решение задачи 2 (`block-switch` / `fresh-period` / `upgrade-only`) зафиксировано — состав задачи ветвится по нему.</precondition>
  <files>app/services/payment_service.py, app/pages/billing.py, app/templates/billing/includes/plan_card.html, tests/test_pages/test_billing_payment_errors.py, .planning/phases/05-tarify/05-UI-SPEC.md, .planning/STATE.md</files>
  <read_first>
    - app/services/payment_service.py (строки 248-291 `_extend_subscription` целиком с докстрингом — что именно там сказано про «срок двигается только здесь»)
    - app/application/billing/subscription_period.py (весь файл — докстринг `next_expiry` прямо говорит, что остаток не должен сгорать; любое решение обязано либо это подтвердить, либо переопределить явно)
    - app/pages/billing.py (строки 151-202 `subscribe_to_plan` — порядок проверок, куда встаёт гард смены плана)
    - app/templates/billing/includes/plan_card.html (весь файл — где рисуется CTA и три его состояния; сюда встаёт четвёртое, если решение его требует)
    - .planning/phases/05-tarify/05-01-PLAN.md (`must_haves.prohibitions` — точная формулировка прохибиции про неистраченный остаток)
    - .planning/phases/05-tarify/05-UI-SPEC.md (`## Copywriting Contract` — CTA-строки; `## Component Contracts` C1 — анатомия карточки и её четыре состояния)
  </read_first>
  <behavior>
    - Продление СВОЕГО действующего платного тарифа работает как работало: срок сдвигается на месяц от существующего, остаток не сгорает (существующие тесты зелёные без правки).
    - Первая покупка платного тарифа с Free работает как работала: строка подписки создаётся, план берётся из платежа.
    - Смена тарифа при действующей подписке (basic→pro и pro→basic с неистёкшим остатком) ведёт себя ровно так, как решено на чекпойнте, и покрыта отдельным тестом на каждое направление.
    - При решении, запрещающем переход, устаревшая страница безопасна: POST с чужим платным планом отвергается с причиной, а не исполняется.
    - При решении, запрещающем переход, карточка чужого платного плана не показывает CTA, обещающего оплату, которая будет отвергнута.
  </behavior>
  <action>
**Общее для всех трёх веток.** Сначала тесты на оба направления (`basic→pro` и
`pro→basic` с неистёкшим остатком) — сегодня их нет ни одного, и именно поэтому дефект
отгружен зелёным. Затем правка. Докстринг `_extend_subscription` дополняется абзацем,
называющим выбранную семантику и решение, которым она принята: сегодня он объясняет,
почему запрос активной подписки повторяет читателя дословно, и молчит о том, что
делается с планом.

**Ветка `block-switch`.** В `subscribe_to_plan`, после проверки, что план есть в
конфиге и не бесплатен, добавить проверку: если у пользователя есть ДЕЙСТВУЮЩАЯ (не
истёкшая) подписка на платный план и запрошенный план от неё отличается — вернуть 302 с
кодом причины `plan` (множество кодов из задачи 1 расширять не нужно) либо новым кодом
со своей строкой в `_payment_error_message`, если формулировка про «смена тарифа
возможна после окончания срока» точнее. Действующая подписка берётся из уже посчитанного
контекста шелла, вторым запросом не считается — правило раздела.

В `plan_card.html` появляется четвёртое состояние CTA: у чужого платного плана при
действующей платной подписке вместо кнопки — подпись о том, что смена тарифа возможна
после окончания текущего срока. Формулировка добавляется в `## Copywriting Contract`.

`_extend_subscription` при этом **не трогается**: путь, который его сюда приводил,
закрыт на входе.

**Ветка `fresh-period`.** В `_extend_subscription` смена плана определяется сравнением
`db_payment.plan` с планом найденной подписки; при расхождении новый срок считается от
СЕГОДНЯ, а не от существующего `expires_at`. Прохибиция `05-01` про неистраченный
остаток **переопределяется решением владельца** — записать это явно: в докстринге
функции, в SUMMARY плана и в `.planning/STATE.md`. Тихое нарушение прохибиции здесь
недопустимо; переопределение с записью — допустимо.

В `plan_card.html` у чужого платного плана под CTA появляется предупреждающая подпись о
том, что оплаченный остаток текущего тарифа не переносится. Она обязана стоять ДО
нажатия, а не появляться после: единственная защита пользователя в этом варианте — то,
что он прочитал.

**Ветка `upgrade-only`.** Порядок тарифов объявляется явно — списком идентификаторов от
младшего к старшему рядом с остальными значениями раздела (`app/constants.py`, где
живут значения, обязанные называться одинаково везде), а не выводится из цены: цена
настраивается окружением, и вывод порядка из неё сделал бы порядок тарифов
конфигурируемым побочно. Повышение проходит и остаток не сгорает (поведение
`next_expiry` не меняется); понижение отвергается на входе так же, как в
`block-switch`, и карточка младшего плана несёт соответствующую подпись.

**Все ветки — записи в документы:**
- `.planning/phases/05-tarify/05-UI-SPEC.md`: C2 переводится из `⚠ unresolved` в
  разрешённое с указанием решения и плана; U1-U3 — в `✅ resolved (explicit)`; новые
  строки CTA и состояний отказа добавляются в `## Copywriting Contract`; счёт в шапке
  `## UI Considerations` пересчитывается (после `05-09` и `05-10` строк `unresolved`
  не остаётся).
- `.planning/STATE.md` §Blockers/Concerns: записать выбранную семантику смены плана
  решением фазы; добавить долг WR-05 (блокирующий вызов SDK без таймаута) с
  обоснованием из `<flagged_assumptions>`.
- `.planning/STATE.md` §Deferred Items или §Blockers/Concerns: собрать одной записью
  оставшиеся отложенные с обоснованием замечания код-ревью по фазе — WR-03, WR-06,
  WR-07, WR-08, WR-09, WR-10, IN-01, IN-02, IN-03, IN-04, IN-05 — с указанием плана, в
  котором каждое распоряжено. Ни одно из восемнадцати замечаний ревью не остаётся без
  распоряжения: два блокера закрыты планами `05-07` и `05-08`, WR-01/WR-04/WR-05
  (обработка ошибок)/IN-06 — планами `05-09` и `05-10`, остальные отложены с названной
  причиной.

**Три пункта ручной проверки из `05-VERIFICATION.md` переносятся в очередь UAT фазы и
НЕ автоматизируются** — мобильная ширина 375px, настоящий платёж в тестовом магазине
ЮKassa и отменённый платёж со статусом «отклонён». Первый после задачи 1 плана `05-09`
проверяется по числовому порогу 44px из `## Mobile Contract`; второй и третий
заблокированы решениями D-26 и D-27 и остаются человеческими. Назвать это в SUMMARY
прямым списком, а не растворить в тексте.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_payment_errors.py tests/test_services/test_payment_service.py tests/test_pages/test_billing_subscription.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` завершается с кодом 0
    - Тест на направление `basic→pro` с неистёкшим остатком существует и проходит: `grep -rc 'basic' tests/test_pages/test_billing_payment_errors.py tests/test_services/test_payment_service.py` даёт не ноль, и в суите есть именованный тест смены плана
    - Тест на направление `pro→basic` с неистёкшим остатком существует и проходит
    - Поведение: продление СВОЕГО действующего платного тарифа по-прежнему сдвигает срок от существующего `expires_at`, а не от сегодня — `test_webhook_extends_an_active_subscription_without_burning_the_remainder` зелёный без правки
    - Поведение: первая покупка платного тарифа с Free по-прежнему создаёт строку подписки — `test_webhook_creates_the_first_subscription` зелёный без правки
    - При решении `block-switch` или `upgrade-only`, поведение: `POST /billing/subscribe` с чужим платным планом при действующей подписке отдаёт 302 с кодом причины и не создаёт строку `payments`
    - При решении `block-switch` или `upgrade-only`: `app/templates/billing/includes/plan_card.html` содержит четвёртое состояние CTA (подпись вместо кнопки), и оно покрыто тестом разметки
    - При решении `fresh-period`: докстринг `_extend_subscription` содержит слова о переопределении прохибиции 05-01, и то же переопределение записано в `.planning/STATE.md`
    - При решении `upgrade-only`: `grep -c 'PLAN_ORDER' app/constants.py` >= 1 — порядок тарифов объявлен явно, а не выведен из цены
    - `.planning/phases/05-tarify/05-UI-SPEC.md` не содержит строк в состоянии `⚠ unresolved`: `grep -c '⚠ unresolved' .planning/phases/05-tarify/05-UI-SPEC.md` == 0
    - `grep -c 'WR-05' .planning/STATE.md` >= 1
    - `.planning/STATE.md` называет распоряжение по каждому из отложенных замечаний ревью: `grep -Ec 'WR-0[36789]|WR-10|IN-0[12345]' .planning/STATE.md` >= 5
  </acceptance_criteria>
  <done>Судьба оплаченного остатка при смене тарифа объявлена решением владельца и закреплена тестами на оба направления; ни одно из восемнадцати замечаний код-ревью не осталось без распоряжения; три пункта ручной проверки перенесены в UAT, а не автоматизированы задним числом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → `GET /billing?error=<code>` | Параметр адреса — недоверенный ввод, попадающий на путь отрисовки раздела |
| ЮKassa API → `create_payment` | Внешний отказ приходит исключением стороннего дерева; его текст недоверен для показа |
| устаревшая страница → форма оплаты | Между отрисовкой и нажатием состояние сервера могло измениться; разметка гарантий не даёт |
| платёж → срок подписки | Решение о судьбе уже уплаченного принимается кодом, который об этом решении молчит |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-46 | Tampering | Подстановка произвольного текста в раздел через параметр `error` | high | mitigate | Задача 1: в разметку уходит строка из ЗАКРЫТОГО отображения `_payment_error_message`, значение параметра не печатается ни значением, ни атрибутом. Недостижимость, а не экранирование: подставить нечего, потому что вход в разметку не связан со входом из адреса |
| T-05-47 | Information Disclosure | Текст стороннего исключения ЮKassa на экране пользователя | medium | mitigate | Задача 1: исключение уходит в журнал, на экран — фиксированная строка. Прецедент R-03-09 Фазы 3 назван прямо и не повторяется на денежном пути |
| T-05-48 | Repudiation | Отказ создания платежа не оставляет следа | medium | mitigate | Задача 1: `payment_create_failed` с идентификатором пользователя и предметом покупки |
| T-05-49 | Integrity | Строка `payments` остаётся после неудавшегося создания платежа у ЮKassa | medium | mitigate | Задача 1: порядок «сначала SDK, потом запись в БД» сохраняется; критерий приёмки требует ноль строк после отказа |
| T-05-50 | Tampering | Смена тарифа уничтожает оплаченный остаток без объявленного правила | high | mitigate | Задачи 2-3: решение владельца, закреплённое тестами на оба направления. Вариант, сжигающий остаток, обязан переопределить прохибицию `05-01` ЗАПИСЬЮ, а не молчанием |
| T-05-51 | Elevation of Privilege | Повышение тарифа даёт лимиты старшего плана на остаток младшего бесплатно | low | accept | Подарок, а не потеря; применения лимитов в системе нет вовсе (D-08), поэтому «лимиты Pro» сегодня не дают доступа ни к чему сверх показа. Названо в опции `upgrade-only` явно |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает ни одного пакета и не трогает `pyproject.toml`. Аудит легитимности — `05-RESEARCH.md` §Package Legitimacy Audit; задач вида «установить X» в плане нет |
</threat_model>

## Artifacts this phase produces

Перечень общий на набор гэп-планов `05-07`…`05-10`, повторяется в каждом.

**Файлы (новые):**
- `tests/test_routes/test_billing_webhook_proxy_headers.py` (план 05-07)
- `tests/test_services/test_payment_concurrency.py` (план 05-08)
- `alembic/versions/0018_subscriptions_unique_user.py` (план 05-08, **условно**)
- `tests/test_migrations/test_0018_subscriptions_unique_user.py` (план 05-08, условно)
- `tests/test_pages/test_billing_payment_errors.py` — **план 05-10**

**Alembic revision id (условный):** `0018` (`down_revision = "0017"`)

**Функции и исключения (новые):**
- `_claim_payment(db, yookassa_id, new_status, now) -> bool` — `app/services/payment_service.py` (05-08)
- `PaymentCreationError` — `app/services/payment_service.py` — **план 05-10**
- `_payment_error_message(code)` — `app/pages/billing.py` — **план 05-10**
- `PLAN_ORDER` — `app/constants.py` — **план 05-10, условно** (только при решении `upgrade-only`)

**Ключи журнала (новые):** `webhook_ip_header_not_configured` (05-07),
`webhook_claim_lost` (05-08), `payment_create_failed` (05-10)

**Ключи конфига:** новых нет; меняется УМОЛЧАНИЕ `Settings.yookassa_webhook_client_ip_header`

**Переменные окружения деплоя (новые в артефакте):** `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER`
в `docker-compose.prod.yml` (05-07)

**Контракт маршрута (новый):** `GET /billing?error=<code>` со значениями
`payment` | `disabled` | `plan` | `package` — **план 05-10**

**CSS-селектор (новый):** `[data-plan-cta] .btn` с `min-height: 44px` (05-09)

**Строки копирайта (новые):** пустое состояние каталога пакетов (05-09); четыре строки
состояний отказа оплаты и подпись четвёртого состояния CTA карточки плана (05-10)

**Ограничение схемы (условное):** частичный уникальный индекс по `subscriptions.user_id`
при `is_active` (05-08)

<verification>
- `uv run pytest tests/ -q` зелёный целиком
- Ни одного нового пакета в `pyproject.toml`
- `05-UI-SPEC.md` не содержит строк `⚠ unresolved`
- Ни одно из восемнадцати замечаний `05-REVIEW.md` не осталось без распоряжения: закрыто, принято в работу или отложено с названной причиной и записью
- Три пункта ручной проверки перенесены в очередь UAT фазы и НЕ автоматизированы
</verification>

<success_criteria>
- Обе формы оплаты отвечают на отказ названной причиной, а не пятисоткой и не молчанием
- Подставить в раздел произвольный текст через параметр адреса невозможно
- Судьба оплаченного остатка при смене тарифа объявлена решением владельца и покрыта тестами на оба направления
- Прохибиция «не сжигать неистраченный остаток» либо соблюдена, либо переопределена явной записью
- Все строки `## UI Considerations` разрешены; три пункта UAT перенесены, а не автоматизированы
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-10-SUMMARY.md` when done
</output>
