---
phase: 05-tarify
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - graphify-out/graph.json
autonomous: false
requirements: [BILL-05, BILL-06, BILL-07]
user_setup:
  - service: yookassa
    why: "Событие payment.canceled по умолчанию НЕ рассылается — без подписки на него ветка D-16 зелёная в тестах и мёртвая в проде"
    dashboard_config:
      - task: "Включить рассылку уведомлений по событию payment.canceled"
        location: "Личный кабинет ЮKassa → Интеграция → HTTP-уведомления → выбор событий"
      - task: "Проверить, что notification URL указывает на POST /api/billing/webhook боевого домена"
        location: "Личный кабинет ЮKassa → Интеграция → HTTP-уведомления"
    env_vars:
      - name: YOOKASSA_WEBHOOK_CLIENT_IP_HEADER
        source: "Задать в .env прода одновременно с настройкой сквозного адреса клиента в nginx — по решению чекпойнта плана 05-01. Пустое значение оставляет поведение прежним"

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "BILL-02 в REQUIREMENTS.md перестаёт выглядеть закрытым: применения тарифных лимитов в коде нет, эта фаза его сознательно не закрывает, и долг обязан быть виден, а не раствориться молча (D-13)"
    - "BILL-05, BILL-06 и BILL-07 помечены выполненными в обеих таблицах REQUIREMENTS.md — и в списке требований, и в матрице трассировки"
    - "Заметка ROADMAP «сервис лимитов уже существует» исправлена: такого сервиса в проекте нет"
    - "Полный прогон набора тестов зелёный до передачи фазы на проверку"
    - "Граф кодовой базы обновлён после правок — протокол проекта требует этого после изменения кода"
    - statement: "Владелец подтвердил, что подписка на событие отмены платежа включена в кабинете ЮKassa: без неё критерий 3 закрыт формально и не закрыт фактически"
      verification: backstop
    - statement: "Владелец принял решение о выкате ревизии 0017, стоящей пятой в очереди невыкаченных, включая необратимую 0013"
      verification: backstop
    - statement: "Настоящий платёж в тестовом магазине ЮKassa проходит весь путь и срок подписки сдвигается — автоматизировать эту проверку нечем"
      verification: backstop
  artifacts:
    - ".planning/REQUIREMENTS.md — перепомеченный BILL-02 и закрытые BILL-05..07"
    - "graphify-out/graph.json — обновлённый граф кодовой базы"
  key_links:
    - "REQUIREMENTS.md BILL-02 → долг, который забирает будущая работа по гейтам создания"
    - "Ревизия 0017 → очередь невыкаченных 0013..0016 → решение владельца о выкате"
  prohibitions:
    - requirement_id: BILL-06
      category: transparency
      statement: "MUST NOT оставлять требование помеченным выполненным, когда код его не выполняет — документ, который врёт о состоянии системы, хуже отсутствующего документа"
      status: resolved
      verification: judgment
---

<objective>
Закрыть долг документов, который фаза обязана закрыть честно, и провести фазу через
три проверки, которые автоматизировать нечем.

Purpose: решение D-13 прямо требует перепометить BILL-02 — сейчас он помечен
выполненным, а применения тарифных лимитов в коде нет вовсе. Фаза сознательно строит
витрину, а не механизм принуждения, значит требование обязано перестать выглядеть
закрытым, иначе долг растворится молча. Плюс три факта, которые живут вне репозитория:
подписка на событие отмены в кабинете ЮKassa, решение о выкате очереди миграций и
настоящий платёж.
Output: правленый `REQUIREMENTS.md`, обновлённый граф, зафиксированное решение о выкате.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-RESEARCH.md
@.planning/phases/05-tarify/05-05-SUMMARY.md
</context>

<flagged_assumptions>
Три строки edge-probe (**BILL-05**, **BILL-06**, **BILL-07**) вернулись `unclassified` /
`unresolved` — классификатор англоязычный, требования написаны по-русски. Все три
подняты явно как флагированные допущения (полная таблица — план `05-01`) и не
отброшены.

**Допущения `05-RESEARCH.md`, доезжающие до этого плана:**
- **A1 (адрес источника вебхука за прокси)** — решено чекпойнтом плана `05-01`;
  здесь остаётся только выкат выбранного варианта, названный в `user_setup`.
- **Подписка на событие отмены платежа (§Pitfall 7)** — действие вне кода, у которого
  **нет резервного пути**. Без него отменённые платежи в проде так и останутся
  `pending`, и критерий 3 будет закрыт формально.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Честная перепометка требований</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (строки 45-52 — блок BILL-01..04 baseline; строки 110-116 — BILL-05..07; строки 180-190 и 228-234 — обе таблицы трассировки)
    - .planning/ROADMAP.md (раздел «Phase 5: Тарифы» — строка Notes с заметкой про сервис лимитов)
    - app/services/billing_cache.py (строки 25-40 — единственный лимит проекта: он возвращает «разрешено» на любое действие, кроме отправки)
    - tests/test_routes/test_limits.py (весь файл — тест, документирующий отсутствие лимитов прямым текстом)
    - .planning/phases/05-tarify/05-CONTEXT.md (D-08 и D-13 — почему перепометка обязательна)
  </read_first>
  <action>
Правка документов, не кода.

**BILL-02 перепомечается честно.** Требование «Система применяет тарифные лимиты к
объявлениям, группам и использованию платформы» сейчас помечено выполненным в обеих
таблицах. По коду это неправда: констант лимитов тарифа в проекте нет, единственный
лимит `check_balance_cached` возвращает «разрешено» на любое действие, кроме отправки, и
это задокументировано отдельным тестом. Фаза 5 решением D-08 сознательно не вводит
гейтов — она строит витрину. Значит требование обязано перестать выглядеть закрытым.
Снять отметку выполнения в списке требований и изменить статус в матрице трассировки;
рядом коротко назвать, что именно не выполнено (применение лимитов) и что выполнено
(показ потребления — это BILL-06). Формулировку держать фактической: документ фиксирует
состояние кода, а не оправдывается.

**BILL-05, BILL-06, BILL-07 помечаются выполненными** в обеих таблицах — списке
требований и матрице трассировки. Проверить, что правки внесены **в оба места**: они
расходятся молча, потому что читают их разные люди в разное время.

**Заметка ROADMAP исправляется.** Раздел фазы содержит утверждение, что сервис лимитов
уже существует. Такого сервиса в проекте нет; заметка вводит в заблуждение следующего
читателя ровно так же, как вводила планировщика. Исправить формулировку по факту:
модели подписки, платежа и баланса существуют, сервиса применения лимитов не существует,
и фаза его не заводит. Правка точечная, остальные разделы `ROADMAP.md` не трогать.
  </action>
  <verify>
    <automated>grep -c 'BILL-02' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] \*\*BILL-02\*\*' .planning/REQUIREMENTS.md` == 0 — требование больше не помечено выполненным в списке
    - `grep -E '^\| BILL-02 ' .planning/REQUIREMENTS.md` не содержит слова `Complete`
    - `grep -Ec '^\| BILL-0[567] .*(Complete|Done|Выполнено)' .planning/REQUIREMENTS.md` == 3 — все три требования фазы закрыты в матрице трассировки
    - `grep -Ec '\[x\] \*\*BILL-0[567]\*\*' .planning/REQUIREMENTS.md` == 3 — и в списке требований тоже
    - `grep -c 'сервис лимитов уже существу' .planning/ROADMAP.md` == 0
    - `.planning/REQUIREMENTS.md` содержит короткое пояснение, что именно осталось невыполненным по BILL-02
  </acceptance_criteria>
  <done>Ни одно требование не помечено выполненным вопреки коду; долг по применению лимитов виден в документе и забирается будущей работой.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Выкатывать ли ревизию `0017` на боевую базу и что делать с очередью из пяти невыкаченных ревизий</decision>
  <context>
Боевая база остаётся на ревизии `0012` — это действующий блокер `STATE.md`, поднятый
ещё Фазой 2. В репозитории head уже `0016`, а эта фаза добавляет `0017`. Применение
`0017` на боевой базе означает применение **всех пяти разом**: `0013`, `0014`, `0015`,
`0016`, `0017`.

Опасна из них `0013`: её `downgrade` **необратимо снимает колонку `ads.is_active`**, и
это выписано комментарием в самой ревизии. То есть очередь проходится в одну сторону.

Что зависит от выката: без `0017` в боевой схеме нет колонок `kind` и `plan`, и
`messages_count` остаётся `NOT NULL` — то есть **подписка в проде не работает вовсе**,
платёж за неё не запишется. Критерий 1 фазы на боевом стенде не выполняется до выката.

Что известно точно: guard по имени хоста, порту и имени базы плюс дамп таблицы
объявлений уже отработаны в плане `02-12` — механика безопасного выката существует и
проверена. Round-trip самой `0017` доказан тестом плана `05-02`.

Это решение владельца, а не исполнителя: цена ошибки — боевые данные.
  </context>
  <options>
    <option id="deploy-all-now">
      <name>Выкатить всю очередь `0013`…`0017` в один заход</name>
      <pros>Репозиторий и боевая схема сходятся; подписка начинает работать в проде сразу; долг, накопленный с Фазы 2, закрывается разом, а не растёт дальше. Механика выката с guard'ом и дампом уже отработана.</pros>
      <cons>Пять ревизий за один шаг: если что-то пойдёт не так, откат упирается в необратимую `0013`. Требует окна обслуживания и свежего дампа.</cons>
    </option>
    <option id="deploy-staged">
      <name>Выкатить очередь по шагам, с проверкой между ревизиями</name>
      <pros>Каждая ревизия проверяется отдельно; проблема локализуется до конкретного шага, а не до всей пачки. `0013` проходится осознанно, а не «заодно».</pros>
      <cons>Дольше и требует нескольких окон; всё равно упирается в ту же необратимую `0013`, только позже. Промежуточные состояния схемы придётся держать совместимыми с выкаченным кодом.</cons>
    </option>
    <option id="defer-deploy">
      <name>Отложить выкат: фаза принимается на тестовом стенде, боевая база остаётся на `0012`</name>
      <pros>Ноль риска для боевых данных сейчас; фаза принимается по тестовому стенду, где схема актуальна.</pros>
      <cons>Критерий 1 фазы на боевом стенде **не выполняется**: подписку нельзя ни купить, ни продлить, потому что колонок для неё нет. Долг невыкаченных ревизий растёт с пяти до шести и дальше. Раздел на проде показывает планы и оси, но кнопка оплаты приводит к ошибке записи.</cons>
    </option>
  </options>
  <resume-signal>Выберите: deploy-all-now, deploy-staged или defer-deploy</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Сквозная проверка фазы и обновление графа</name>
  <precondition>Подписка на событие отмены платежа включена в личном кабинете ЮKassa (user_setup) — иначе ветка отмены зелёная в тестах и мёртвая в проде.</precondition>
  <files>graphify-out/graph.json, .planning/STATE.md</files>
  <read_first>
    - CLAUDE.md (раздел graphify — правило «после правки кода запустить обновление графа»; раздел Commands — состав рецептов just)
    - .planning/phases/05-tarify/05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md, 05-04-SUMMARY.md, 05-05-SUMMARY.md (что фактически сделано каждым планом — из них собирается перечень для ручной проверки)
    - .planning/phases/05-tarify/COVERAGE.md (матрица покрытия ЮKassa — проверить, что ни одна строка INTEGRATE не осталась неисполненной)
    - .planning/STATE.md (раздел Blockers/Concerns — запись про невыкаченные ревизии обновляется по итогу чекпойнта задачи 2)
  </read_first>
  <action>
Прогнать полный набор тестов рецептом проекта и убедиться, что он зелёный целиком, а не
по выборочным файлам. Волновые прогоны предыдущих планов проверяли свои файлы; здесь
проверяется, что фаза не сломала ничего за своими границами — в первую очередь раздел
истории (у него уехал гард источника), шелл (у него изменилась подпись виджета) и
инвентаризацию шаблонов.

Обновить граф кодовой базы рецептом проекта: правило репозитория требует этого после
изменения кода, а фаза добавила новый пакет приложения, новые паршалы и новый маршрут.
Без обновления следующая фаза будет ориентироваться по устаревшему графу.

Сверить `COVERAGE.md` с фактом: каждая строка матрицы со значением INTEGRATE обязана
иметь исполнение в коде — создание платежа, тип подтверждения «редирект», метаданные,
оба обрабатываемых события уведомления и проверка адреса источника. Строки со значением
OPT-OUT проверить на то, что они действительно не реализованы «заодно»: реализованная
без решения возможность — такой же незакрытый вопрос, как пропущенная.

Обновить запись блокера в `STATE.md` по итогу решения задачи 2: назвать выбранный
вариант и текущее состояние очереди невыкаченных ревизий.

**Ручная проверка** — три пункта, которых автотестами не закрыть, и это названо прямо, а
не спрятано за зелёным прогоном:
1. Настоящий платёж в тестовом магазине ЮKassa: форма → страница оплаты → возврат →
   приход уведомления → сдвинутый срок на экране. Боевого доступа к API в тестах нет,
   всё покрытие идёт на моках.
2. Раздел на узкой ширине: карточки планов складываются в одну колонку, подписи колонок
   истории едут вместе со значениями, ни один блок не требует горизонтальной прокрутки.
   Браузерных и e2e-тестов в проекте нет — это ограничение, а не упущение.
3. Отменённый платёж приходит уведомлением и получает статус «отклонён» на экране —
   проверяемо только после включения подписки на это событие в кабинете.
  </action>
  <verify>
    <automated>just test</automated>
    <human-check>
      1. Совершить настоящий платёж за подписку в тестовом магазине ЮKassa: нажать «Продлить» / «Перейти на Basic» в разделе тарифов, оплатить на странице ЮKassa, вернуться. Ожидается: платёж виден в истории, после прихода уведомления его статус «проведён», а срок подписки сдвинут на месяц. Ключевая проверка: срок НЕ должен сдвинуться до прихода уведомления.
      2. Открыть /billing на ширине 375px. Ожидается: три карточки планов идут одной колонкой, четыре метра читаемы, у строк истории платежей подписи колонок стоят рядом со значениями, горизонтальной прокрутки нет ни у одного блока.
      3. Отменить платёж на стороне ЮKassa (или дождаться истечения). Ожидается: строка в истории меняет статус на «отклонён», ничего не начислено, срок подписки не сдвинут.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `just test` завершается с кодом 0
    - `uv run pytest tests/ -q` не сообщает ни одного `failed` и ни одного `error`
    - `graphify-out/graph.json` изменён относительно состояния до фазы (`git status --porcelain graphify-out/` непусто либо файл переиндексирован)
    - Граф содержит узел для `app/application/billing/plan_usage.py`
    - Каждая строка `COVERAGE.md` со значением INTEGRATE имеет соответствующее исполнение в `app/` — сверка проведена и зафиксирована в SUMMARY
    - `.planning/STATE.md` содержит обновлённую запись о состоянии очереди невыкаченных ревизий с названным решением
    - Все три пункта ручной проверки пройдены и их результат зафиксирован в SUMMARY поимённо, а не общей формулировкой
  </acceptance_criteria>
  <done>Полный набор тестов зелёный, граф актуален, матрица покрытия сверена с кодом, и три непроверяемых автоматикой пункта проверены человеком и записаны.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| репозиторий → боевая база | Применение цепочки ревизий к живым данным; `0013` необратима |
| личный кабинет ЮKassa → приложение | Состав рассылаемых событий задаётся вне репозитория и кодом не проверяется |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-32 | Denial of Service | Выкат очереди из пяти ревизий, включая необратимую `0013`, на боевую базу | high | mitigate | Задача 2 (`checkpoint:decision`): решение принимает владелец; механика guard'а по хосту/порту/имени базы и дампа отработана планом `02-12`; round-trip `0017` доказан тестом плана `05-02` |
| T-05-33 | Repudiation | Ветка обработки отмены платежа зелёная в тестах и мёртвая в проде — события просто не приходят | high | mitigate | `user_setup` + `<precondition>` задачи 3 + пункт 3 ручной проверки; резервного пути у этого действия нет, поэтому оно названо явно, а не подразумевается |
| T-05-34 | Repudiation | Требование помечено выполненным вопреки коду — долг растворяется молча | medium | mitigate | Задача 1: BILL-02 перепомечается в обеих таблицах с указанием, что именно не выполнено; прохибиция в `must_haves.prohibitions` |
| T-05-35 | Information Disclosure | Переменная окружения со сквозным заголовком адреса заполнена, а прокси его не перезаписывает — проверка источника становится декоративной | high | mitigate | `user_setup`: настройка `.env` и правка nginx выполняются **одним** выкатом; вариант поведения выбран чекпойнтом плана `05-01` |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Фаза не установила ни одного нового пакета; `just sync` покрывает всё. Аудит легитимности в `05-RESEARCH.md` §Package Legitimacy Audit |
</threat_model>

## Artifacts this phase produces

**Файлы (новые):** `alembic/versions/0017_payment_kind_and_plan.py`,
`app/application/billing/__init__.py`, `app/application/billing/subscription_period.py`,
`app/application/billing/plan_usage.py`, `app/templates/billing/includes/plan_card.html`,
`app/templates/billing/includes/usage_meters.html`,
`app/templates/billing/includes/payment_row.html`,
`tests/test_application/test_subscription_period.py`,
`tests/test_application/test_plan_usage.py`,
`tests/test_pages/test_billing_subscription.py`, `tests/test_pages/test_billing_section.py`,
`tests/test_migrations/test_0017_payment_kind_and_plan.py`,
`tests/test_routes/test_billing_webhook_source.py`,
`.planning/phases/05-tarify/COVERAGE.md`

**Файл, удаляемый фазой:** `app/templates/billing/plans.html`

**Alembic revision id:** `0017` (`down_revision = "0016"`)

**Функции, макросы и глобалы (новые):** `add_one_month`, `next_expiry`, `is_same_origin`,
`current_month_bounds_utc`, `sends_in_current_month`, `plan_axes`, `get_payment_history`,
Jinja-глобал форматирования денежной суммы, Jinja-макросы `plan_card`, `usage_meter`,
`payment_row`

**Поля моделей (новые):** `Payment.kind`, `Payment.plan`; `Payment.messages_count` становится nullable

**Ключи конфига (новые):** `Settings.plan_limits`, `Settings.parsed_plan_limits`,
`Settings.yookassa_webhook_verify_ip`, `Settings.yookassa_webhook_client_ip_header`

**Константы (новые):** потолок списка платежей в `app/constants.py`; идентификаторы и
подписи четырёх осей в `app/application/billing/plan_usage.py`

**Маршруты (новые):** `POST /billing/subscribe`, `POST /billing/purchase`
**Маршрут, удаляемый фазой:** JSON-покупка пакета в `app/routes/billing.py`

**CSS-атрибут (новый):** `[data-plans]` с сеткой `repeat(auto-fit, minmax(260px, 1fr))`

<verification>
- `just test` зелёный целиком
- Ни одно требование `REQUIREMENTS.md` не помечено выполненным вопреки коду
- Граф кодовой базы переиндексирован
- Матрица `COVERAGE.md` сверена с реализацией построчно
</verification>

<success_criteria>
- Долг BILL-02 виден в документе и не выглядит закрытым
- Три требования фазы закрыты в обеих таблицах
- Решение о выкате очереди ревизий принято владельцем и зафиксировано
- Три пункта ручной проверки пройдены и записаны поимённо
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-06-SUMMARY.md` when done
</output>
