---
phase: 05-tarify
plan: 16
type: execute
wave: 14
depends_on: ["05-15", "05-17"]
gap_closure: true
files_modified:
  - .planning/phases/05-tarify/05-STATE-PATCH-R4.md
  - .planning/STATE.md
autonomous: false
requirements: [BILL-05, BILL-06, BILL-07]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- Гэп 2 раунда 4 (status: partial), 05-VERIFICATION.md ---
    - "В указателе на раунды ревью не осталось ни одной КОНСТАНТЫ ВЕЛИЧИНЫ: число документов, которым принадлежало имя `05-REVIEW.md`, и число `status: failed` гэпов отчёта верификации больше не названы числительными — обе величины заменены формулировками, не зависящими от числа раундов, и обе устаревали бы от раунда 5 ровно так же, как от раунда 4"
    - "Раунд 4 адресуется собственным неизменяемым коммитом (`5da8e82`) наравне с раундами 1, 2 и 3: механизм адресации по коммитам, построенный планом 05-14 и проверенный верификацией, ПРИМЕНЁН к новому раунду, а не описан заново"
    - "Распоряжения по всем СЕМИ находкам ревью раунда 4 записаны в `.planning/STATE.md`: `CR-01`, `WR-01`, `WR-02`, `WR-03`, `WR-04`, `IN-01`, `IN-02` — каждая со своим распоряжением; сегодня в файле нет ни одной, и отчёт верификации прямо относит эту работу к настоящему раунду бухгалтерии, а не к плану 05-14"
    - "Каждый номер находки раунда 4 назван вместе со своим раундом: номера между раундами ПЕРЕНАЗНАЧЕНЫ, и `CR-01` раунда 4 — четвёртый разный дефект под этим номером; требование квалификации внутри новой записи проверяется ИНВАРИАНТОМ (число вхождений номера равно числу квалифицированных), а не константой счёта"
    - "Блокер денежного пути раунда 4 записан отдельной строкой §Deferred Items со ссылкой на ОБА закрывших плана (`05-15` — деньги и записанное разрешение, `05-17` — недопущение состояния) и на доказавшие закрытие регрессии — именами, а не словом «исправлено»: ни один из двух планов не закрывает находку в одиночку, и запись, называющая один, повторила бы половинность, за которую фаза уже получила раунд"
    - "Запись о закрытии `CR-01 (раунд 3)` планом 05-13 больше не читается как полное закрытие: раунд 4 нашёл дефект того же корня, оставшийся после него, — та же правка, которую план 05-14 сделал для записи о `CR-01` раунда 2, применяется к записи о `CR-01` раунда 3"
    - "Решения владельца D-28 и D-29 и ответ чекпойнта плана 05-15 о ФОРМЕ потолка одновременных подписочных намерений записаны решениями фазы в §Decisions — вместе с тем, что ответ «потолка нет» был СНЯТ разбором, а не забыт: четвёртый пункт `missing:` гэпа 1 закрывается кодом плана 05-17 И этой записью, и без записи следующий раунд не найдёт, почему потолок именно такой"
    - "Артефакт записан В ФАЙЛ, а не обещан во frontmatter: маршрут записи `.planning/STATE.md` из worktree закрыт конфигурацией, и четыре плана подряд об этот маршрут уже спотыкались"
    - "«Current Position» и frontmatter `progress` не спорят ни друг с другом, ни с ROADMAP после появления планов 05-15 и 05-16"
    # --- Backstop-пункт ---
    - statement: "Читатель `.planning/STATE.md`, пришедший в проект впервые, понимает из §Blockers/Concerns и §Decisions действительное состояние денежного пути за одну минуту и находит нужный раунд ревью с первой попытки — понятность записи судится человеком, а не грепом"
      verification: backstop
  artifacts:
    - ".planning/phases/05-tarify/05-STATE-PATCH-R4.md — готовая к применению замена секций STATE.md, коммитится вместе с планом и переживает снос worktree; отдельный файл, `05-STATE-PATCH.md` плана 05-12 и `05-STATE-PATCH-R3.md` плана 05-14 не перезаписываются"
    - ".planning/STATE.md — реестр раундов без констант величины и с коммитом раунда 4, распоряжения раунда 4, правка строки §Deferred Items о CR-01 раунда 3, новая строка §Deferred Items о блокере раунда 4, записи D-28/D-29 и ответа чекпойнта в §Decisions, согласованная «Current Position»"
  key_links:
    - "`git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md` → реестр раундов → номер находки → нужный раунд. Механизм построен планом 05-14 и проверен верификацией; настоящий план его ПРИМЕНЯЕТ к четвёртому раунду, а не переизобретает"
    - "`05-15-SUMMARY.md` (ответ чекпойнта о потолке, подтверждение D-28/D-29, имена регрессий и колонки) → §Decisions и §Deferred Items"
    - "`05-REVIEW.md` раунда 4 (`git show 5da8e82:…`) → распоряжения по семи находкам"
    - "`05-VERIFICATION.md` раунда 4, `gaps[1].missing` → два пункта, оба обязаны быть закрыты этим планом"
  prohibitions:
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT оставлять в операционном документе КОНСТАНТУ ВЕЛИЧИНЫ там, где величина растёт с каждым раундом — числительное устаревает ровно тем способом, который соседний инвариант в том же файле уже чинит"
      status: unresolved
      verification: test
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT называть находку голым номером там, где нумерация переназначается между раундами — распоряжение уйдёт не той находке"
      status: unresolved
      verification: test
    - requirement_id: BILL-06
      category: transparency
      statement: "MUST NOT достигать «закрытия» дефекта удалением записи о нём вместо закрытия самого дефекта"
      status: unresolved
      verification: test
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT записывать закрытие блокера словом «исправлено» без имён механизма и доказавших его регрессий — запись без имён неотличима от обещания, и однажды она уже скрыла половинность закрытия"
      status: unresolved
      verification: test
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT объявлять требование Complete раньше, чем верификация подтвердит закрытие гэпов — отметка, поставленная вперёд вердикта, уже была снята однажды и не ставится этим планом"
      status: unresolved
      verification: test
---

<!-- planner-discipline-allow: ТРЁМ разным документам -->
<!-- planner-discipline-allow: входит в два -->
<!-- planner-discipline-allow: ЧЕТЫРЁМ -->
<!-- planner-discipline-allow: CR-01 (раунд 4) -->
<!-- planner-discipline-allow: WR-07 (раунд 3) -->
<!-- Первые два литерала обязаны стоять в `<action>`: предмет работы — снять ИМЕННО эти
     обороты, и распоряжение «убрать константу величины» без цитаты исполнить нельзя.
     Отрицательные грепы обоих идут по `.planning/STATE.md` и по файлу патча, а НЕ по этому
     плану — самоаннулирования нет. Третий литерал обязан стоять в `<action>` по той же
     причине: запрет «не заменять число другим числом» без образца неисполним, а его
     отрицательный греп тоже идёт по `.planning/STATE.md`. Два последних литерала стоят под
     ПОЛОЖИТЕЛЬНЫМИ грепами (`>= 1`, проверка присутствия), и отрицательного гейта на них в
     плане нет вовсе. -->

<objective>
Гэп 2 отчёта `05-VERIFICATION.md` раунда 4, `status: partial`, тяжесть НИЗКАЯ. Механизм
адресации раундов, построенный планом 05-14, верификация проверила ИСПОЛНЕНИЕМ и признала
работающим: раунды адресуются коммитами, `git show` по всем трём хешам отдаёт заявленные
документы, перечень отдаёт `git log`, и запись НАМЕРЕННО не утверждает, какой раунд лежит на
диске. Маршрутизация верна — устарел только СЧЁТ.

Осталось два числительных в одном предложении, и оба — константы величины:

* `.planning/STATE.md:92` — «Имя `05-REVIEW.md` принадлежит ТРЁМ разным документам». С
  коммитом `5da8e82` (`round: 4`) их четыре.
* та же строка, хвост — «ни одна из отложенных не входит в два `status: failed` гэпа
  `05-VERIFICATION.md`». У раунда 3 `status: failed` нёс ОДИН гэп, второй был `partial`.

Ирония названа самим планом 05-14 его же must_have №5: «константа величины устарела бы от
первой перекрёстной ссылки и ещё раз от раунда 4 — то есть отказала бы ровно тем способом,
который этот план чинит в указателе». Инвариант квалификации номеров план построил
правильно (проверено: 4 = 4) и оставил числительные в соседнем предложении.

Второй пункт `missing:` — распоряжения по находкам ревью раунда 4 (`CR-01`, `WR-01`…`WR-04`,
`IN-01`, `IN-02`). Отчёт прямо снимает их с плана 05-14: ревью раунда 4 прошло в 06:10,
через 35 минут после его завершения. Это работа НАСТОЯЩЕГО раунда бухгалтерии, и сегодня в
`.planning/STATE.md` нет ни одной из семи находок.

Purpose: план 05-14 заменил обязательство конструкцией в ОДНОМ месте записи и оставил
обязательство в соседнем предложении. Настоящий план доводит замену до конца и применяет уже
построенный механизм к четвёртому раунду — а не строит пятый механизм.
Output: готовая к применению замена секций, коммитящаяся вместе с планом, и применённый
`.planning/STATE.md`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@.planning/phases/05-tarify/05-CONTEXT.md
</context>

<flagged_assumptions>

**Почему это не «ещё раз объявить STATE.md в `files_modified`».** Маршрут записи закрыт
конфигурацией, а не небрежностью: `workflow.use_worktrees` = `true`. Следствия выписаны в
самих рабочих процессах и подтверждены четырьмя прогонами — планы `05-08` и `05-10`
объявили файл и не тронули его, планы `05-12` и `05-14` дошли до цели только через отдельный
артефакт и блокирующий гейт:

- `execute-plan.md:406-410` — исполнитель определяет режим по `[ -f .git ]` и при worktree
  ПРОПУСКАЕТ шаг обновления STATE.md;
- `execute-plan.md:502-509` — при worktree коммит плана ИСКЛЮЧАЕТ `.planning/STATE.md`;
- `execute-phase.md:721` — в задание исполнителя вписано `IMPORTANT: Do NOT modify
  STATE.md or ROADMAP.md`;
- `execute-phase.md:5.7` — оркестратор коммитит `.planning/STATE.md`, но пишет туда только
  прогресс планов; произвольный текст §Blockers/Concerns из SUMMARY он не переносит ничем.

Устройство плана поэтому дословно повторяет планы 05-12 и 05-14, прошедшие через этот
маршрут: твёрдый артефакт в каталоге фазы → применение по фактическому дереву (best-effort)
→ блокирующий чекпойнт на выходе.

**Новый файл, а не правка `05-STATE-PATCH-R3.md`.** Артефакт плана 05-14 закоммичен
(`0861e8d`), проверен верификацией по размеру и содержимому и описывает УЖЕ ПРИМЕНЁННЫЕ
правки. Перезапись превратила бы запись об исполненной работе в запись о планируемой — тот
самый класс подмены, который эта фаза ловит четвёртый раунд подряд. Патч этого плана —
отдельный файл `05-STATE-PATCH-R4.md`.

**⚠️ ЛОВУШКА НУМЕРАЦИИ, ставшая ЧЕТЫРЁХСЛОЙНОЙ.** `05-REVIEW.md` перезаписывался четырежды:
`4989f7a` (раунд 1), `1d29360` (раунд 2), `164d210` (раунд 3), `5da8e82` (раунд 4). Номера
между раундами переназначены, и коллизии теперь есть у КАЖДОГО номера раунда 4:

| Номер | Раунд 1 (`4989f7a`) | Раунд 2 (`1d29360`) | Раунд 3 (`164d210`) | Раунд 4 (`5da8e82`) |
|-------|---------------------|---------------------|---------------------|---------------------|
| CR-01 | гард источника читает адрес пира | молчаливое понижение в `_apply_extension` | понижение на истёкшем сроке не выдаётся | **платёж младшего тарифа покупает месяц старшего** |
| WR-01 | `format_amount` на `NaN`/`Infinity` | lost update в `reset_free_monthly` | отказ повышения назван «понижением» | **две стадии снимают признак в разные моменты** |
| WR-02 | — | гард переоткрывается непроверенной переменной | `yookassa_return_url` из `app_name` | **рукописная копия признака живости** |
| WR-03 | SQLite-каскад ревизии `0017` | аварийный выключатель не оставляет следа | мёртвая константа `STATUS_PENDING` | **fail-closed снимается признаком срока** |
| WR-04 | константы предмета покупки | `"object": null` → 500 и цикл повторов | `plan_card.html` зашивает `'free'` | **нет политики перезапуска у четырёх сервисов** |
| IN-01 | — | — | неиспользуемые импорт и `logger` | **`constants.py` шлёт за правилом не в тот модуль** |
| IN-02 | — | — | пустой `app/application/billing/__init__.py` | **AST-тест держит порядок, но не величину** |

Отсюда правило записи для нового блока: **каждый номер раунда 4 стоит в форме
`<номер> (раунд 4)`**, и критерий приёмки считает их по этой форме ВНУТРИ своей записи.

**Глобального инварианта квалификации по всему файлу НЕ вводится, и причина измерена, а не
предположена.** На входе `grep -Eo '(CR|WR|IN)-[0-9]{2}' .planning/STATE.md | wc -l` даёт
**55**, а форма с квалификатором (`(раунд N)` либо `раунда N`) — **28**. Разница НЕ означает
55 − 28 неквалифицированных находок: в записях раундов 1 и 2 номера перечислены списками, у
которых квалификатор стоит один раз на весь список («WR-03, WR-06, WR-08, WR-09 раунда 1»,
«IN-01…IN-05 раунда 1»), и это законная форма — весь список принадлежит одному раунду.
Требовать глобального равенства значило бы переписать записи прежних раундов целиком ради
формы, а не ради смысла, — то есть завести работу, которой ни один `missing:` не просил, на
файле, где каждая правка стоит риска. Инвариант остаётся ДВУХСЛОЙНЫМ, как его построил план
05-14: он проверяется (1) внутри новой записи раунда 4, где номера стоят поимённо, и (2) на
номере `WR-07`, за которым стоят две РАЗНЫЕ находки и который поэтому квалифицирован везде
(на входе 4 = 4, проверка защитная).

**Существующие записи раундов 1, 2 и 3 не трогаются и не разъединяются**, кроме одной
правки, названной прямо: строка §Deferred Items о `CR-01 (раунд 3)`. Она объявляет закрытие
планом 05-13 полным; раунд 4 доказал, что после него остался дефект ТОГО ЖЕ корня. Это
дословно та правка, которую план 05-14 сделал для записи о `CR-01` раунда 2 (must_have №6,
проверен верификацией), и пропустить её значило бы оставить в файле ровно то утверждение,
ради которого 05-14 правил соседнее.

**Требования BILL-05/BILL-06/BILL-07 этим планом НЕ перемечаются в `Complete`.** Они сняты с
`Complete` 2026-08-16 после вердикта `gaps_found`, и однажды преждевременная отметка уже была
поставлена и отозвана (`2124d1e`). Отметку возвращает верификация после подтверждения
закрытия гэпов, а не план, который эти гэпы закрывает. `.planning/REQUIREMENTS.md` в
`files_modified` не объявлен и не открывается на запись.

**Что этот план НЕ делает по границе объёма.** Ни одной правки кода: гэп 2 —
организационный. Ни одной правки `app/`, `tests/`, `alembic/`, `pyproject.toml`. Находки
раунда 4, не входящие в `status: failed` гэп, в работу НЕ берутся — они получают ЗАПИСЬ с
распоряжением, ровно то, чего требует список `missing:` и что отчёт называет явно: «WR-02,
WR-03, WR-04 и IN-01, IN-02 раунда 4 — в таблице анти-паттернов, блокерами не считаются и
закрытия этой фазой не требуют». Две из них (`IN-01` и `IN-02`) всё же закрыты кодом планом
05-15 с названным там обоснованием, и запись обязана отразить это ФАКТОМ, а не переоткрывать
вопрос.

**Ручные пункты `behavior_unverified_items` и `human_verification` задачами не становятся** —
см. `<verification>`. Мобильная пригодность на 375px остаётся открытой пятый раунд подряд:
план не трогает ни строки разметки, и отчёт прямо называет отсутствие браузерного харнесса
свойством проекта, а не гэпом.

**Зонд краевых случаев вернул все три требования фазы `unclassified`** — все три несут явные
допущения планировщика в `05-15-PLAN.md` (E-01, E-02, E-03) и здесь не дублируются: минтить
вторые копии тех же трёх строк значило бы завести два ответа на один вопрос. Их состояние —
`unresolved`, и оно не изменилось.

**`COVERAGE.md` не перегенерируется и не правится:** план не трогает код и поверхность API
ЮKassa не меняет ни на строку.

**Каноничные позиции безопасности прохибициями не минтятся:** утечка секретов в трекинговый
документ покрыта `<threat_model>` (T-05-86) и командой `/gsd-secure-phase`.

</flagged_assumptions>

<assumption_delta_decision>

**Сигнал проверен и НЕ сработал: план не вводит ни одной новой сущности и ни одного нового
представления — он записывает уже принятые решения и уже исполненную работу.** Раздел
пишется всё равно, потому что ответ `no-change` обязан быть НАЗВАН, а не выведен читателем из
молчания: молчание в этой фазе трижды оказывалось не «нечего сказать», а «не заметили».

**Существительное, о котором мог бы идти разговор:** **раунд ревью** — единственный предмет
этого плана, у которого множественность растёт (документов под именем `05-REVIEW.md` было
одно, стало четыре, будет больше).

**Решение: `no-change`.** Обобщение уже произошло и уже промотировано — планом 05-14, и
верификация это подтвердила: первичным представлением раунда стал ЕГО КОММИТ, а не позиция в
файле, и перечень раундов отдаёт `git log`, а не утверждение автора. Настоящий план
ПРИМЕНЯЕТ существующее обобщение к четвёртому экземпляру и снимает два числительных,
переживших повышение по недосмотру. Заводить под это новую сущность — таблицу раундов,
отдельный файл реестра, поле во frontmatter — значило бы ввести второе представление того же
предмета рядом с уже работающим первым, то есть в точности `add-alongside`, который
протокол называет молчаливым противоречием обобщённому намерению.

**Что заставило бы вернуться к вопросу:** если число раундов вырастет настолько, что запись
перестанет читаться одной строкой, первичным представлением станет отдельный документ реестра
с одной строкой на раунд, а запись `.planning/STATE.md` демотируется до ссылки на него.
Сегодняшний бюджет (128 строк из 145) до этого порога не доводит, и предугадывать его правкой
формы значило бы чинить то, что ещё не сломалось.

</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Реестр без констант величины, распоряжения раунда 4 и решения о деньгах — записанные в коммитящийся артефакт и применённые к настоящему дереву</name>
  <precondition>ОБА отчёта существуют в каталоге фазы: `05-15-SUMMARY.md` — с подтверждением решений D-28 и D-29, ответом чекпойнта о ФОРМЕ потолка (`cap-different-plan` либо `cap-any-pending`), именем ревизии Alembic и именами регрессий; `05-17-SUMMARY.md` — с исполненной формой потолка и именем нового кода причины отказа. Без обоих §Decisions и §Deferred Items описывали бы планируемую работу вместо исполненной — ошибка, которую эта фаза ловит четвёртый раунд подряд. При отсутствии любого — остановка: план волны 14 идёт после волн 12 и 13 именно затем.</precondition>
  <files>.planning/phases/05-tarify/05-STATE-PATCH-R4.md, .planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (весь файл, 128 строк: frontmatter `progress`; «Current Position» — счётчик планов и строка прогресса; §Decisions — запись о семантике `upgrade-only`, дополненная планом 05-14 исходом на истёкшем сроке, и куда встают D-28/D-29; §Blockers/Concerns строки 88-95 целиком — две 🔴 записи, 🟡 сводная запись строки 92 с ДВУМЯ константами величины и 🟡 запись распоряжений раунда 3 строки 93 как ОБРАЗЕЦ формата новой записи; §Deferred Items — таблица, где лежат строки о CR-01 раундов 2 и 3)
    - .planning/phases/05-tarify/05-VERIFICATION.md (frontmatter: `gaps[1]` со списком `missing:` — два пункта; `gaps[1].artifacts` — обе строки с точными цитатами устаревших оборотов; `human_verification` — десять пунктов, которые обязаны остаться человеческими; таблица Requirements Coverage — что засчитано и с какими оговорками)
    - .planning/phases/05-tarify/05-REVIEW.md (раунд 4: строки 64-79 — таблица четырёх раундов с коммитами и итогами, источник новой строки реестра; строки 142-258 — CR-01; строки 263-317 — WR-01; строки 319-365 — WR-02; строки 366-418 — WR-03; строки 419-468 — WR-04; строки 473-519 — IN-01 и IN-02; строки 523-556 — таблица «Previously Dispositioned», называющая находки прежних раундов, повторно НЕ открываемые)
    - .planning/phases/05-tarify/05-15-SUMMARY.md (результат волны 12: ответ чекпойнта о ФОРМЕ потолка, подтверждение D-28/D-29, имя ревизии, имена колонки и функций, имена регрессий и факт их красноты до правки — записи обязаны называть их, а не слово «исправлено». Там же — строка о том, какой путь случая (1) волной 12 НЕ закрыт)
    - .planning/phases/05-tarify/05-17-SUMMARY.md (результат волны 13: исполненная форма потолка, срок давности числом часов, имя нового кода причины отказа и имя теста, доказавшего недостижимость случая (1) через форму)
    - .planning/phases/05-tarify/05-STATE-PATCH-R3.md (артефакт плана 05-14: структура разделов и формат «что удаляется / что встаёт вместо» — новый патч пишется по тому же образцу, чтобы читатель не разбирал два формата)
    - .planning/phases/05-tarify/05-CONTEXT.md (строки 82-87 — D-28 и D-29 дословно: в §Decisions уезжает их суть с идентификаторами, а не пересказ)
    - .planning/ROADMAP.md (блок Фазы 5: строка `**Plans:**` и список планов после появления 05-15 и 05-16 — «Current Position» STATE.md обязана согласоваться с ним, а не спорить)
  </read_first>
  <action>
**0. Прочитать таблицу четырёх раундов в ревью раунда 4** (`05-REVIEW.md:71-79`). Там уже
лежит реестр с коммитами и итогами, включая строку раунда 4 (`5da8e82`, итог
`critical: 1, warning: 4, info: 2`). Он и есть источник новой строки записи; переписывать
своими словами не нужно.

**1. Написать `.planning/phases/05-tarify/05-STATE-PATCH-R4.md`** — готовую к применению
замену, а не пересказ. Файл каталога фазы коммитится планом в любом режиме исполнения и
переживает снос worktree; он и есть твёрдый результат задачи. Структура — по одному разделу
на каждую правку STATE.md, в каждом что удаляется (с номером строки на входе) и что встаёт
вместо этого, дословно, готовым к копированию. Разделов ровно ШЕСТЬ — пункты 2-7 ниже.

**Форма замещающего текста обязательна, потому что по ней считают гейты обоих концов.**
Каждый блок замены оформляется огороженным блоком кода, и каждая запись STATE.md внутри него
стоит ОДНОЙ физической строкой с тем же началом, с каким она уедет в файл: `- 🔴 ` либо
`- 🟡 `. Мягких переносов внутри записи быть не должно. Причина прикладная: критерии приёмки
этой задачи считают номера находок ВНУТРИ строки записи, а проверки задачи 2 считают их
точно так же в самом `.planning/STATE.md`. Если запись развалится на несколько физических
строк, гейт патча и гейт файла посчитают РАЗНОЕ, и сойтись они смогут только случайно.
Пояснительная проза патча живёт ВНЕ блоков замены; голый номер она называть вправе, и гейты
её не читают именно поэтому.

**2. §Blockers/Concerns, 🟡 сводная запись (строка 92) — снять ОБЕ константы величины и
дописать коммит раунда 4.** Правится только вводное предложение записи, до слов
«**Закрыты планами:**», и её последнее предложение; перечень распоряжений раундов 1 и 2 между
ними не трогается ни на символ.

- **Первая константа.** Оборот «Имя `05-REVIEW.md` принадлежит ТРЁМ разным документам»
  заменяется формулировкой, не называющей числа вовсе: имя принадлежит нескольким разным
  документам, и их перечень отдаёт названная тут же команда `git log`. Числительное не
  заменяется на «ЧЕТЫРЁМ»: это была бы та же конструкция с другим значением, устаревающая от
  раунда 5 — ровно тот отказ, который эта запись и чинит.
- **Вторая константа.** Оборот «ни одна из отложенных не входит в два `status: failed` гэпа
  `05-VERIFICATION.md`» заменяется утверждением без числа: ни одна из перечисленных здесь
  отложенных находок не входит в гэпы `status: failed` отчёта `05-VERIFICATION.md`, а состав
  и число гэпов отдаёт сам отчёт. Утверждение обязано остаться ПРОВЕРЯЕМЫМ и верным: гэп 1
  раунда 4 порождён находками раунда 4, а не отложенными находками раундов 1 и 2, и запись
  говорит именно о вторых.
- **Реестр дополняется раундом 4** наравне с прежними: `git show 5da8e82:.planning/phases/05-tarify/05-REVIEW.md`
  (итог `critical: 1, warning: 4, info: 2`). Коммит новейшего раунда называется наравне с
  остальными — именно этого не делали три редакции подряд.
- Чего запись НЕ содержит: ни одного утверждения о том, какой раунд занимает файл сегодня, и
  ни одного числительного о числе раундов, документов или гэпов. Проверяется критериями
  приёмки.

**3. §Blockers/Concerns — НОВАЯ 🟡 запись: распоряжения по находкам раунда 4.** Одной
физической строкой, как соседние сводные записи: бюджет файла 145 строк, и строка на находку
обменяла бы один дефект на другой. Заголовок записи — `🟡 [Phase 5, код-ревью раунда 4 — распоряжения]`,
и он обязан быть дословно таким: критерий приёмки считает номера ВНУТРИ этой строки, и найти
её он может только по заголовку. Раунд 4 читается командой
`git show 5da8e82:.planning/phases/05-tarify/05-REVIEW.md`, и эта команда называется в записи.

**Все СЕМЬ находок раунда 4 обязаны присутствовать поимённо, каждая в форме
`<номер> (раунд 4)` с сутью и распоряжением:**
- `CR-01 (раунд 4)` — подтверждённый платёж МЛАДШЕГО тарифа двигал срок ДЕЙСТВУЮЩЕГО
  СТАРШЕГО на полный календарный месяц: 9370 ₽ покупали 121 день Pro стоимостью 19 600 ₽,
  повторов не ограничивало ничто. **Закрыт планами `05-15` и `05-17`** — деньги решением
  D-29, состояние «два разрешённых намерения разных тарифов» потолком, — отдельной строкой
  §Deferred Items (пункт 5). Оба плана названы, потому что ни один из двух не закрывает
  находку в одиночку;
- `WR-01 (раунд 4)` — две стадии правила снимали признак живости в РАЗНЫЕ моменты из РАЗНЫХ
  источников, ответ гарда на платеже не сохранялся, а докстринг `plan_switch.py:48` утверждал
  обратное. **Тот же корень, что `CR-01 (раунд 4)`; закрыт планом `05-15`** записью
  разрешения на платёж и снятием утверждения из докстринга;
- `WR-02 (раунд 4)` — `app/pages/billing.py:195-198` держит рукописное отрицание признака
  живости в десяти строках от импортированного объявления, а комментарий `:100-107`
  существование копии отрицает; расходятся сегодня только на границе `expires_at == now`.
  **Отложено с распоряжением:** выразить показ через то же объявление и снять второй снимок
  часов — это же закрывает `IN-04 (раунд 3)` в части `billing_page`; работа своего размера,
  владелец не назначен;
- `WR-03 (раунд 4)` — ветка `if not period_is_live: return False` короткозамыкает fail-closed
  у плана без ранга, и пересечение двух правил не названо нигде. **Отложено с распоряжением:**
  требует продуктового решения о том, какое из двух правил сильнее, плюс зеркальной регрессии
  на стадии применения. ⚠️ После плана `05-15` путь СОХРАНЯЕТСЯ и обязан быть назван прямо:
  незнакомый план, проданный на истёкшем сроке, получает записанное разрешение и применяется;
- `WR-04 (раунд 4)` — у `web`, `celery-beat` и обоих celery-воркеров боевого артефакта нет
  политики перезапуска, тогда как у `nginx`, `certbot` и `flower` она есть; упавший `web` не
  вернётся сам, а `nginx` вернётся и будет отдавать 502 на каждое уведомление ЮKassa.
  Находка ПРЕДШЕСТВУЕТ фазе 05, но фаза материально увеличила цену отказа `web`. **Отложено с
  распоряжением:** `restart: unless-stopped` в `x-app-base` плюс проверка идемпотентности
  `entrypoint.sh` относительно повторного запуска; вне объёма кода фазы, владелец не назначен;
- `IN-01 (раунд 4)` — `app/constants.py:65` отправляет читателя `PLAN_ORDER` за правилом в
  один модуль из двух. **Закрыто планом `05-15`**, задача 3, с названным там обоснованием:
  после плана потребителей правила стало больше, и строка стала бы неверной ещё раз;
- `IN-02 (раунд 4)` — AST-тест порядка держит ПОРЯДОК двух операторов и не держит ВЕЛИЧИНУ, по
  которой снимается признак. **Закрыто планом `05-15`**, задача 2, с названным там
  обоснованием: план увеличил нагрузку на этот тест, и половина инварианта под возросшей
  нагрузкой — долг следующего раунда.

Записи вида «уже несёт распоряжение» брать из таблицы «Previously Dispositioned»
(`05-REVIEW.md:523-556`) и НЕ заводить им вторых распоряжений: ревью явно перечислило там
находки прежних раундов, повторно не открываемые.

**4. §Deferred Items — правка существующей строки о `CR-01 (раунд 3)`.** Сегодня строка
объявляет закрытие планом 05-13 состоявшимся. Раунд 4 доказал, что после него остался дефект
ТОГО ЖЕ корня: объединена была ФОРМА входных данных, а не МОМЕНТ их снятия. Строка обязана
нести оба факта — что план 05-13 закрыл НАЗВАННОЕ направление (подтверждено верификацией:
все четыре обещанных механизма на месте, 92 теста зелёные), и что он оставил соседнее,
закрытое планом `05-15`. Это дословно та же правка, которую план 05-14 сделал для строки о
`CR-01` раунда 2, и её пропуск оставил бы в файле утверждение того же вида, ради которого
05-14 правил соседнее. Оговорку о вердикте, действующем до повторной верификации,
СОХРАНИТЬ.

**5. §Deferred Items — НОВАЯ строка: `CR-01 (раунд 4)`.** Формат соседних записей.
Содержание — именами, а не словом «исправлено», и ОБА закрывших плана названы, потому что ни
один не закрывает находку в одиночку:
- планом `05-15`: ответ гарда о разрешённом переходе записывается на сам платёж колонкой
  `payments.switch_authorized` (решение D-28), заведённой ревизией Alembic `0019`; стадия
  применения читает записанный ФАКТ, а не пересчитывает предикат по изменившейся строке
  подписки; платёж, чей план не применён, покупает дни по УПЛАЧЕННОМУ тарифу — долю месяца по
  отношению суммы к цене действующего плана (решение D-29), а не календарный месяц старшего;
  регрессии, МЕНЯЮЩИЕ состояние подписки МЕЖДУ продажей и подтверждением, существуют и падали
  до правки — оба названных отчётом случая;
- планом `05-17`: потолок одновременных подписочных намерений закрывает последний путь
  случая (1), где ОБА платежа заведены формой и оба несут разрешение `True` — там отказа не
  возникает ни у одного, доля месяца не исполняется, и последний подтверждённый младший
  переписывал уже выданный старший. Названы форма потолка и срок давности.

**Точные имена колонки, функций, ревизии, кода причины и тестов сверить по
`05-15-SUMMARY.md` и `05-17-SUMMARY.md`, а не по этому плану** — плановые имена могли
измениться при исполнении. Обязательные оговорки: вердикт раунда 4 — `gaps_found` —
действует до повторной верификации; запись фиксирует исполненную работу, а не пройденную
проверку; ревизия `0019` встаёт седьмой в невыкаченную очередь (D-26), и на боевой базе
колонки нет.

**6. §Decisions — записать D-28, D-29 и ответ чекпойнта о потолке.** Существующая запись о
семантике смены тарифа дополняется, новой записи о семантике не заводится — у одного решения
одно место. Дописать:
- **D-28** идентификатором и сутью: ответ гарда о разрешённом переходе записывается на сам
  платёж (`payments.switch_authorized`, ревизия `0019`), стадия применения читает записанный
  факт; названы отвергнутые варианты (б) и (в) и причина выбора — решение о сделке нигде не
  сохранялось, а варианты (б) и (в) чинят следствие;
- **D-29** идентификатором и сутью: отвергнутый платёж продолжает давать дни, но по
  уплаченному тарифу, а не по действующему; исход «взяли деньги и не дали ничего» не введён;
  названы отвергнутые варианты — сохранение безусловного сдвига как принятого размена и
  полный отказ в сдвиге;
- **ответ чекпойнта задачи 1 плана `05-15`** о ФОРМЕ потолка одновременных подписочных
  намерений: идентификатор варианта дословно (`cap-different-plan` либо `cap-any-pending`),
  срок давности числом часов и следствие для пользователя одной фразой. Отдельной фразой —
  что ответ «потолка нет» из перечня был СНЯТ разбором, а не забыт: без потолка случай (2)
  третьего пункта `missing:` не закрывается ни ветвью «выдан», ни ветвью «не продан».
  Четвёртый пункт `missing:` гэпа 1 закрывается кодом плана `05-17` И этой записью: без
  записи следующий раунд не найдёт, почему потолок именно такой.

**7. «Current Position» и frontmatter `progress`.** Счётчик планов, строка прогресса,
`stopped_at`, `last_activity`, `last_activity_desc` и `total_plans`/`completed_plans`
приводятся в соответствие с ROADMAP после появления планов 05-15, 05-16 и 05-17. Числа берутся из
`.planning/ROADMAP.md`, а НЕ из этого плана: он может разойтись с фактическим деревом, а
ROADMAP — источник, с которым запись обязана не спорить. Frontmatter и текст не должны
спорить друг с другом — это уже случалось и было исправлено планом 05-12; проверить обе
цифры, а не одну.

**8. Применить замену к настоящему дереву (best-effort, отказ не подделывается).**
Настоящий корень репозитория берётся из `git rev-parse --git-common-dir`: из worktree это
указывает на главный рабочий каталог, из главного — на него же. Применить правки к
`STATE.md` ИМЕННО там. Оставленный в главном дереве дифф — ровно то, что оркестратор
коммитит шагом 5.7 `execute-phase.md`; собственный коммит плана `.planning/STATE.md` в режиме
worktree всё равно исключает.

Если запись по этому пути недоступна — не изображать успех и не тратить попытки: записать
факт в SUMMARY отдельной строкой, назвать разрешённый путь и код отказа. Файл
`05-STATE-PATCH-R4.md` остаётся твёрдым результатом задачи, а применение снимает задача 2.

**9. Проверить бюджет файла.** После правок `wc -l .planning/STATE.md` обязан остаться в
пределах 145 строк (рабочий процесс держит потолок 150). Новых физических строк добавляется
две — 🟡 запись распоряжений раунда 4 и строка §Deferred Items, — на входе файл 128 строк.
Если бюджет всё равно превышен — сокращаются формулировки распоряжений, а не отбрасывается
находка.

**10. Требования не перемечать.** `.planning/REQUIREMENTS.md` не открывается на запись вовсе:
`BILL-05`/`BILL-06`/`BILL-07` возвращает в `Complete` верификация после подтверждения
закрытия гэпов, и однажды преждевременная отметка уже была отозвана (`2124d1e`).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-tarify/05-STATE-PATCH-R4.md && test "$(grep -Eo 'git show (4989f7a|1d29360|164d210|5da8e82):' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | sort -u | wc -l)" = "4" && test "$(grep '🟡 \[Phase 5, код-ревью раунда 4' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | grep -Eo 'CR-01 \(раунд 4\)|WR-0[1-4] \(раунд 4\)|IN-0[1-2] \(раунд 4\)' | sort -u | wc -l)" = "7" && test "$(grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | grep -Eo '(CR|WR|IN)-[0-9]{2}' | wc -l)" = "$(grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | grep -Eo '(CR|WR|IN)-[0-9]{2} \(раунд [0-9]+\)' | wc -l)"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `.planning/phases/05-tarify/05-STATE-PATCH-R4.md` существует и закоммичен вместе с планом; `git diff --name-only -- .planning/phases/05-tarify/05-STATE-PATCH.md .planning/phases/05-tarify/05-STATE-PATCH-R3.md` пуст — артефакты планов 05-12 и 05-14 не перезаписаны
    - Реестр адресует ВСЕ ЧЕТЫРЕ раунда неизменяемыми коммитами: `test "$(grep -Eo 'git show (4989f7a|1d29360|164d210|5da8e82):' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | sort -u | wc -l)" = "4"` истинно. Считаются РАЗЛИЧНЫЕ коммиты (`sort -u`), а не вхождения: повтор одного коммита не должен зеленить проверку за счёт неназванного
    - Обе константы величины из замещающего текста ушли: `grep -c 'ТРЁМ разным документам' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` считается только по строкам замены (`grep -E '^- (🔴|🟡)'`) и равен 0, и то же для оборота `входит в два`. Пояснительная проза патча цитировать их ВПРАВЕ — она объясняет, что именно снимается, и в STATE.md не уезжает
    - Ни одно числительное не заменено другим числительным: строки замены не содержат оборота `ЧЕТЫРЁМ разным документам` — `grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | grep -c 'ЧЕТЫРЁМ'` == 0. Замена числа на число воспроизвела бы ту же конструкцию с другим значением
    - Самообновляющийся указатель сохранён дословно: `grep -c 'git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` >= 1
    - Все семь находок раунда 4 несут распоряжение: `grep '🟡 \[Phase 5, код-ревью раунда 4' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | grep -Eo 'CR-01 \(раунд 4\)|WR-0[1-4] \(раунд 4\)|IN-0[1-2] \(раунд 4\)' | sort -u | wc -l` == 7. Счёт ведётся ВНУТРИ строки заголовка `код-ревью раунда 4`, а не по файлу: номера раунда 4 совпадают с номерами трёх прежних раундов, и счёт по файлу считал бы чужие находки
    - Каждый номер находки в СТРОКАХ ЗАПИСЕЙ патча квалифицирован раундом: `test "$(grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | grep -Eo '(CR|WR|IN)-[0-9]{2}' | wc -l)" = "$(grep -E '^- (🔴|🟡)' .planning/phases/05-tarify/05-STATE-PATCH-R4.md | grep -Eo '(CR|WR|IN)-[0-9]{2} \(раунд [0-9]+\)' | wc -l)"` истинно. Проверяется РАВЕНСТВО, а не величина, и квалификатор принимает ЛЮБОЙ номер раунда (`[0-9]+`, а не перечень): раунд 5 не должен требовать правки критерия
    - Патч называет закрытие блокера раунда 4 именами и ОБА закрывших плана: `grep -c 'switch_authorized' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` >= 1, `grep -c '0019' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` >= 1, `grep -c '05-15' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` >= 2 и `grep -c '05-17' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` >= 1
    - Патч называет оба решения владельца и ответ чекпойнта: `grep -Ec 'D-28' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` >= 1, `grep -Ec 'D-29' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` >= 1 и `grep -Ec 'cap-different-plan|cap-any-pending' .planning/phases/05-tarify/05-STATE-PATCH-R4.md` >= 1
    - Патч называет шесть правок STATE.md поимённо: 🟡 реестр без констант величины и с коммитом раунда 4, 🟡 распоряжения раунда 4, правка строки §Deferred Items о CR-01 раунда 3, новая строка §Deferred Items о блокере раунда 4, §Decisions (D-28, D-29, ответ чекпойнта), «Current Position» + frontmatter `progress`
    - SUMMARY плана содержит отдельную строку об исходе применения к настоящему дереву: применено либо отказано с названным путём и кодом
    - `git diff --name-only -- .planning/REQUIREMENTS.md` пуст — ни одно требование не перемечено
    - `git diff --name-only -- app/ tests/ alembic/ pyproject.toml uv.lock` пуст — план не трогает код
  </acceptance_criteria>
  <done>Реестр раундов дополнен коммитом раунда 4 и не содержит ни одной константы величины; все семь находок раунда 4 получили распоряжение с квалифицированным номером; закрытие блокера названо именами колонки, ревизии и регрессий; оба решения владельца и ответ чекпойнта записаны решениями фазы; попытка применения к настоящему дереву выполнена, а её исход записан честно.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Применить замену к STATE.md в главном дереве и подтвердить проверками</name>
  <read_first>
    - .planning/phases/05-tarify/05-STATE-PATCH-R4.md (результат задачи 1 — шесть разделов замены; порядок применения задан там)
    - .planning/STATE.md в ГЛАВНОМ рабочем каталоге (текущее состояние: применены ли правки задачей 1, или файл ещё на входном виде — 🟡 запись строки 92 с двумя константами величины, ни одной находки раунда 4, ни одного упоминания `05-15`)
    - .planning/phases/05-tarify/05-16-SUMMARY.md, если уже написан (строка об исходе применения: «применено» либо отказ с названным путём и кодом)
  </read_first>
  <action>
Применить `.planning/phases/05-tarify/05-STATE-PATCH-R4.md` к `.planning/STATE.md` в ГЛАВНОМ
рабочем каталоге репозитория и подтвердить результат проверками ниже.

**Почему это человеческий шаг, а не задача исполнителя.** У исполнителя нет пути, эффект
которого гарантированно переживает снос worktree: `execute-plan.md:502-509` исключает
`.planning/STATE.md` из коммита плана в режиме worktree, а `execute-phase.md:721` прямо
запрещает исполнителю трогать этот файл. Это не предположение, а четырежды воспроизведённый
исход: планы `05-08` и `05-10` объявили файл в `files_modified` и не изменили его ни разу, а
планы `05-12` и `05-14` дошли до цели только через этот же гейт. Остановка здесь громкая, а
не тихая — в этом весь её смысл.

**Если задача 1 уже применила правки** (её SUMMARY говорит «применено») — работы нет,
остаётся выполнить проверки и подтвердить.

**Чего делать НЕЛЬЗЯ ни при какой проверке.** Сокращать счёт вхождений номера удалением
записи. Записей под номерами `CR-01`, `WR-01`…`WR-04`, `IN-01`, `IN-02` в файле теперь по
нескольку намеренно: они принадлежат РАЗНЫМ раундам и разным дефектам. Требование к ним —
квалификация раундом, а не сокращение счёта. Точно так же запрещено гнать к нулю счёт строк
`^- 🔴`: открытые записи о блокирующем вызове SDK (`CR-02` раунда 2) и о сокете Docker
(`WR-07 (раунд 3)`) обязаны уцелеть — их закрытие достигается закрытием дефектов, а не
удалением следа.

**Чего НЕЛЬЗЯ делать с константами величины.** Заменять число другим числом. Оборот про
«ТРЁМ разным документам» подлежит замене формулировкой БЕЗ числа; «ЧЕТЫРЁМ» — та же
конструкция с другим значением, устаревающая от раунда 5, и проверка это ловит отдельным
критерием.
  </action>
  <instructions>
1. Убедиться, что находитесь в главном рабочем каталоге: `git rev-parse --git-dir` отдаёт
   `.git` (в worktree это был бы путь внутрь `.git/worktrees/...`).
2. Если правки ещё не применены — применить `05-STATE-PATCH-R4.md` к `.planning/STATE.md`
   разделами, в том порядке, в котором они выписаны в патче. Ни один раздел STATE.md, не
   названный патчем, не трогается; файл не перегенерируется целиком.
3. Выполнить проверки из `<verification>` — все ПЯТНАДЦАТЬ обязаны сойтись. Входные
   значения выписаны в каждой проверке и измерены на входном файле, а не предположены: восемь
   из них на входе КРАСНЫЕ (реестр коммитов 3 → 4; `ТРЁМ разным документам` 1 → 0;
   `входит в два` 1 → 0; находки раунда 4 0 → 7; `05-15` 0 → `>= 2`; `05-17` 0 → `>= 1`;
   `switch_authorized` 0 → `>= 1`; `D-28`/`D-29` 0 → `>= 2`) — краснотой на входе они и
   доказывают, что проверяют работу, а не её отсутствие. Семь остальных защитные: они зелёные на входе и
   обязаны остаться зелёными, потому что ловят не невыполнение работы, а её выполнение НЕ ТЕМ
   способом (`на диске` 0 → 0; равенство квалификации `WR-07` 4 = 4; присутствие
   `WR-07 (раунд 3)` 2 → `>= 1`; строки `^- 🔴` 2 → `>= 2`; отсутствие оборота с «ЧЕТЫРЁМ»;
   бюджет файла 128 `<= 145`; пустой дифф требований).
4. Закоммитить `.planning/STATE.md` — либо шагом 5.7 `execute-phase.md`, либо вручную
   сообщением `docs(05): record round-4 dispositions and drop the magnitude constants`.
  </instructions>
  <verification>
- Реестр по коммитам, включая раунд 4: `grep -Eo 'git show (4989f7a|1d29360|164d210|5da8e82):' .planning/STATE.md | sort -u | wc -l` → **4** (на входе **3** — коммит раунда 4 назван не был)
- Первая константа величины снята: `grep -c 'ТРЁМ разным документам' .planning/STATE.md` → **0** (на входе **1**)
- Вторая константа величины снята: `grep -c 'входит в два' .planning/STATE.md` → **0** (на входе **1**)
- Число не заменено числом: `grep -c 'ЧЕТЫРЁМ разным документам' .planning/STATE.md` → **0** (на входе 0, защитная). Замена «ТРЁМ» на «ЧЕТЫРЁМ» воспроизвела бы конструкцию, устаревающую от раунда 5, — то есть починила бы значение и не починила бы форму
- Утверждения о текущем содержимом файла ревью по-прежнему нет: `grep -c 'на диске' .planning/STATE.md` → **0** (на входе **0**, защитная — работу плана 05-14 нельзя откатить заодно)
- Самообновляющийся указатель на месте: `grep -c 'git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md' .planning/STATE.md` → **>= 1** (на входе 1, защитная)
- Распоряжения раунда 4 записаны все: `grep '🟡 \[Phase 5, код-ревью раунда 4' .planning/STATE.md | grep -Eo 'CR-01 \(раунд 4\)|WR-0[1-4] \(раунд 4\)|IN-0[1-2] \(раунд 4\)' | sort -u | wc -l` → **7** (на входе **0**)
- Каждый номер в НОВОЙ записи квалифицирован: `test "$(grep '🟡 \[Phase 5, код-ревью раунда 4' .planning/STATE.md | grep -Eo '(CR|WR|IN)-[0-9]{2}' | wc -l)" = "$(grep '🟡 \[Phase 5, код-ревью раунда 4' .planning/STATE.md | grep -Eo '(CR|WR|IN)-[0-9]{2} \(раунд [0-9]+\)' | wc -l)"` → **истинно**. Проверяется РАВЕНСТВО, а не величина; квалификатор принимает любой номер раунда. Проверка подпёрта предыдущей: без неё равенство удовлетворялось бы пустотой (`0 = 0` истинно)
- Инвариант плана 05-14 не сломан: `test "$(grep -Eo 'WR-07' .planning/STATE.md | wc -l)" = "$(grep -Eo 'WR-07 \(раунд [0-9]+\)' .planning/STATE.md | wc -l)"` → **истинно** (на входе **4 = 4**, защитная) И `grep -c 'WR-07 (раунд 3)' .planning/STATE.md` → **>= 1** (на входе **2**, защитная — запись о сокете Docker обязана уцелеть)
- Закрытие блокера названо именами и ОБОИМИ планами: `grep -c '05-15' .planning/STATE.md` → **>= 2** (на входе **0**), `grep -c '05-17' .planning/STATE.md` → **>= 1** (на входе **0**) И `grep -c 'switch_authorized' .planning/STATE.md` → **>= 1** (на входе **0**)
- Решения владельца записаны: `grep -Ec 'D-28|D-29' .planning/STATE.md` → **>= 2** (на входе **0**)
- След не стёрт заодно: `grep -c '^- 🔴' .planning/STATE.md` → **>= 2** (на входе **2**, защитная). Считаются строки САМИХ записей, а не любое упоминание значка: открытые записи о блокирующем вызове SDK и о сокете Docker обязаны уцелеть, и счёт 🔴, гонимый к нулю, был бы подменой закрытия дефекта его удалением
- Бюджет файла: `wc -l .planning/STATE.md` → **<= 145** (на входе **128**)
- Требования не тронуты: `git diff --name-only -- .planning/REQUIREMENTS.md` → пусто
  </verification>
  <acceptance_criteria>
    - Все ПЯТНАДЦАТЬ проверок `<verification>` сошлись на `.planning/STATE.md` ГЛАВНОГО рабочего каталога, а не worktree
    - `.planning/STATE.md` закоммичен: `git log --oneline -1 -- .planning/STATE.md` называет коммит этой волны, а не `docs(phase-05): create phase plan`
    - `git diff --quiet .planning/STATE.md` — незакоммиченных остатков правки не осталось
    - Сигнал возобновления записан в SUMMARY плана дословно, вместе с фактическими значениями всех пятнадцати проверок
    - При несошедшейся проверке план НЕ закрывается: записывается фактическое значение и причина, и волна остаётся незавершённой
  </acceptance_criteria>
  <resume-signal>Введите «применено», если все пятнадцать проверок сошлись, либо назовите проверку, которая не сошлась, и её фактическое значение</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree исполнителя → главное рабочее дерево | Правка `.planning/STATE.md`, сделанная в worktree, не коммитится и исчезает вместе с ним |
| `05-REVIEW.md` прежних раундов (только в git) → запись распоряжения | Номера находок между раундами переназначены четырежды; голый номер без раунда — ложное указание |
| `.planning/STATE.md` → читатель проекта | Единственный операционный документ; его утверждения о денежном пути принимаются на веру |
| `05-15-SUMMARY.md` → §Decisions и §Deferred Items | Имена колонки, ревизии и регрессий берутся из отчёта об ИСПОЛНЕННОЙ работе, а не из плана: плановые имена могли измениться при исполнении |
| SUMMARY плана → оркестратор | Текст «передан оркестратору» получателя не имеет: шаг 5.7 переносит только прогресс планов |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-85 | Repudiation | Операционный документ не содержит блокера денежного пути, найденного раундом 4, и решений владельца D-28/D-29: они существуют только в `05-REVIEW.md`, `05-VERIFICATION.md` и `05-CONTEXT.md`, а первые два перезаписываются каждым раундом | high | mitigate | Задачи 1-2: `CR-01 (раунд 4)` получает строку §Deferred Items с именами колонки, ревизии и регрессий; D-28, D-29 и ответ чекпойнта уезжают в §Decisions; блокирующий чекпойнт не даёт пропустить запись пятый раз |
| T-05-86 | Information Disclosure | В трекинговый документ, лежащий в репозитории, попадают значения секретов вместе с описанием денежного пути и потолка платежей | medium | mitigate | Записи называют ИМЕНА колонок, ревизий, переменных окружения и путь к значению, но не значения. Ни ключа магазина ЮKassa, ни идентификатора, ни строки подключения запись не несёт |
| T-05-87 | Tampering | Распоряжение уходит НЕ ТОЙ находке: каждый из семи номеров раунда 4 занят в одном-трёх прежних раундах другим дефектом | high | mitigate | Каждый номер раунда 4 пишется в форме `<номер> (раунд 4)`; критерий приёмки требует РАВЕНСТВА числа вхождений числу квалифицированных ВНУТРИ новой записи, подпёртого проверкой присутствия всех семи. Реестр раундов по коммитам, включая `5da8e82`, делает проверку выполнимой читателем |
| T-05-88 | Repudiation | «Закрытие» находки достигается удалением записи о ней, а не закрытием находки; либо константа величины «чинится» заменой числа на число | medium | mitigate | Чекпойнт проверяет ПРИСУТСТВИЕ записей поимённо (`WR-07 (раунд 3)` `>= 1`, строки `^- 🔴` `>= 2`, семь находок раунда 4 == 7), а не величину общего счёта, и отдельным критерием ловит замену числительного другим числительным. Запрет на оба способа объявлен прямо в тексте задачи |
| T-05-89 | Repudiation | Запись о закрытии `CR-01 (раунд 3)` продолжает читаться как полное, хотя раунд 4 нашёл дефект того же корня — тот же отказ, который план 05-14 чинил для записи о `CR-01` раунда 2 | medium | mitigate | Задача 1, пункт 4: строка §Deferred Items о `CR-01 (раунд 3)` называет и закрытое направление, и оставшееся, и план, закрывший второе. Оговорка о вердикте до повторной верификации сохраняется |
| T-05-90 | Repudiation | Требование помечено `Complete` раньше вердикта верификации | high | mitigate | `.planning/REQUIREMENTS.md` не открывается на запись; критерий приёмки проверяет пустой дифф. Прецедент отзыва — коммит `2124d1e` |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает ни одного пакета и не трогает код вовсе — критерий приёмки проверяет пустой дифф по `app/`, `tests/`, `alembic/`, `pyproject.toml`, `uv.lock`. Аудит легитимности — `05-RESEARCH.md:144` `## Package Legitimacy Audit` |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-16`. Общий перечень фазы — в `05-10-PLAN.md`, перечень
кодовой работы четвёртого гэп-раунда — в `05-15-PLAN.md`.

**Файлы (новые):**
- `.planning/phases/05-tarify/05-STATE-PATCH-R4.md` — готовая к применению замена секций
  `.planning/STATE.md`; коммитится планом и переживает снос worktree. Файлы
  `05-STATE-PATCH.md` (план 05-12) и `05-STATE-PATCH-R3.md` (план 05-14) НЕ перезаписываются

**Файлы (изменённые):**
- `.planning/STATE.md` — §Blockers/Concerns (реестр без констант величины и с коммитом
  раунда 4, новая 🟡 запись), §Deferred Items (одна правка, одна новая строка), §Decisions,
  «Current Position», frontmatter `progress`

**Записи STATE.md (новые):**
- 🟡 запись `[Phase 5, код-ревью раунда 4 — распоряжения]` — семь находок раунда 4, каждая с
  квалифицированным номером
- строка §Deferred Items `CR-01 (раунд 4)` → закрыт планом `05-15`, названы колонка
  `payments.switch_authorized`, ревизия `0019` и имена регрессий
- дополнение записи §Decisions: D-28, D-29 и ответ чекпойнта задачи 1 плана `05-15` о потолке
  одновременных `pending`-подписочных платежей

**Записи STATE.md (изменённые):**
- вводное и заключительное предложения 🟡 сводной записи: обе константы величины сняты,
  коммит раунда 4 (`5da8e82`) добавлен в реестр; перечень распоряжений раундов 1 и 2 между
  ними не тронут
- строка §Deferred Items о `CR-01 (раунд 3)`: закрытие названо верным в названном направлении
  и оставившим соседнее, закрытое планом `05-15`

**Функции, колонки, ключи журнала, миграции, маршруты, CSS, строки копирайта:** новых нет —
план не трогает код

<verification>
- `grep -Eo 'git show (4989f7a|1d29360|164d210|5da8e82):' .planning/STATE.md | sort -u | wc -l` == 4 — каждый раунд адресуется неизменяемым коммитом, включая новейший
- `grep -c 'ТРЁМ разным документам' .planning/STATE.md` == 0 и `grep -c 'входит в два' .planning/STATE.md` == 0 — обе константы величины из файла ушли
- `grep -c 'ЧЕТЫРЁМ разным документам' .planning/STATE.md` == 0 — число не заменено числом
- `grep -c 'на диске' .planning/STATE.md` == 0 — работа плана 05-14 не откачена заодно
- `grep '🟡 \[Phase 5, код-ревью раунда 4' .planning/STATE.md | grep -Eo 'CR-01 \(раунд 4\)|WR-0[1-4] \(раунд 4\)|IN-0[1-2] \(раунд 4\)' | sort -u | wc -l` == 7 — все находки раунда 4 несут распоряжение
- Внутри новой записи число вхождений номеров РАВНО числу квалифицированных — инвариант, а не величина
- Инвариант `WR-07` плана 05-14 остаётся истинным, и обе записи под этим номером на месте
- `grep -c '05-15' .planning/STATE.md` >= 2, `grep -c '05-17' .planning/STATE.md` >= 1, `grep -c 'switch_authorized' .planning/STATE.md` >= 1, `grep -Ec 'D-28|D-29' .planning/STATE.md` >= 2 — закрытие блокера обоими планами и решения владельца названы именами
- `grep -c '^- 🔴' .planning/STATE.md` >= 2 — открытые блокеры не стёрты заодно
- `wc -l .planning/STATE.md` <= 145; `git diff --name-only -- .planning/REQUIREMENTS.md` пуст; `git diff --name-only -- app/ tests/ alembic/` пуст

**Перенесённые человеческие проверки — задачами НЕ становятся и НЕ автоматизируются:**
- **Мобильная ширина 375px** (`behavior_unverified: 1`, критерий 4 ROADMAP): браузерного и
  e2e-харнесса в проекте нет, план не трогает ни строки разметки. Отчёт прямо называет это
  свойством проекта, а не гэпом; пункт переносится дословно пятый раунд подряд
- Настоящий платёж в тестовом магазине ЮKassa (D-26) и первое настоящее уведомление после
  выката (backstop плана 05-07)
- Статус «отклонён» у отменённого платежа (D-27): состав событий задаётся вне репозитория
- Читаемость формулировок «в обработке», отказа оплаты и новой строки отказа по потолку —
  суждение человека
- **Владелец риска `WR-07 (раунд 3)`** (сокет Docker у веб-слоя) по-прежнему не назначен:
  распоряжение записано и проверено, имя ставит человек. Запись не трогается этим планом,
  кроме сохранения её на месте
- **Владельцы находок `WR-02 (раунд 4)`, `WR-03 (раунд 4)` и `WR-04 (раунд 4)` не
  назначаются планом:** он записывает распоряжение по каждой, а назначение имени — решение
  человека. `WR-03 (раунд 4)` дополнительно требует ПРОДУКТОВОГО решения о том, какое из двух
  правил сильнее, — оно не принимается ни этим планом, ни планом 05-15
- **Три judgment-tier прохибиции фазы остаются `unresolved`,** и две прохибиции плана 05-13,
  переоткрытые планом 05-15, зелёными не считаются: право объявить их закрытыми принадлежит
  верификации раунда 5
- Читаемость самой записи `.planning/STATE.md` человеком, впервые пришедшим в проект, —
  backstop-пункт `must_haves`
</verification>

<success_criteria>
- В указателе на раунды ревью не осталось ни одной константы величины, и ни одно числительное
  не заменено другим числительным
- Раунд 4 адресуется собственным неизменяемым коммитом наравне с прежними тремя
- Все семь находок раунда 4 получили распоряжение с номером, квалифицированным раундом
- Блокер денежного пути раунда 4 записан именами колонки, ревизии и регрессий, а не словом
  «исправлено»
- Запись о закрытии `CR-01 (раунд 3)` больше не читается как полное закрытие
- Решения владельца D-28 и D-29 и ответ чекпойнта о потолке записаны решениями фазы —
  четвёртый пункт `missing:` гэпа 1 закрыт записью, а не только кодом
- «Current Position» и frontmatter `progress` согласованы с ROADMAP
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-16-SUMMARY.md` when done
</output>
