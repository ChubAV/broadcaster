---
phase: 05-tarify
plan: 11
type: tdd
wave: 8
depends_on: ["05-10"]
gap_closure: true
files_modified:
  - app/application/billing/plan_switch.py
  - app/pages/billing.py
  - app/services/payment_service.py
  - tests/test_application/test_plan_switch.py
  - tests/test_pages/test_billing_payment_errors.py
autonomous: true
requirements: [BILL-05, BILL-06]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- Гэп 1 (BLOCKER), 05-VERIFICATION.md ---
    - "Подтверждённый платёж младшего тарифа при действующем старшем НЕ понижает план: два подтверждённых платежа `pro`, затем `basic`, оставляют `subscription.plan == 'pro'`, а `expires_at` сдвинут ДВАЖДЫ"
    - "Правило `upgrade-only` читается обеими стадиями из ОДНОГО объявления: и гард формы `POST /billing/subscribe`, и `_apply_extension` зовут одну и ту же функцию; второй реализации сравнения рангов в `app/` не существует"
    - "Сохранение старшего тарифа оставляет след: журнал получает ключ `subscription_plan_preserved` с уплаченным и сохранённым планом — сегодня понижение не оставляет следа вообще"
    - "Повышение на стадии ПРИМЕНЕНИЯ по-прежнему применяется: действующий `basic` плюс подтверждённый платёж `pro` дают `plan == 'pro'`, и оплаченный остаток не сгорает"
    - "Продление СВОЕГО тарифа и первая покупка платного тарифа с Free не изменились ни на строку: `next_expiry` не тронут, вставка первой подписки берёт план из платежа как раньше"
    - "Незнакомый план (перечень окружения разошёлся с `PLAN_ORDER`) ранга на стадии применения НЕ получает: действующий план сохраняется, срок двигается, отказ записан журналом — та же догадко-боязнь, что у гарда формы"
    - "Докстринг `_extend_subscription` больше не утверждает, что понижение «не принимается», умалчивая о поведении подтверждённого платежа: правило, записанное там, где двигается срок, совпадает с исполняемым кодом"
    # --- Лифт `## UI Considerations` 05-UI-SPEC.md — только строки в объёме этого гэпа ---
    - "C2 («Перенесено из фазы», 05-UI-SPEC.md:377): объявленная чекпойнтом плана 05-10 семантика `upgrade-only` исполняется на ОБОИХ концах — и на входе, и там, где приходят деньги. Интерфейсный ответ (четвёртое состояние CTA карточки, `DOWNGRADE_CARD_CAPTION`) остаётся ровно тем же и не переписывается"
    # --- Backstop-пункты ---
    - statement: "Человек, оплативший младший тариф и оставшийся на старшем, видит строку истории «Basic · продление» рядом с бейджем действующего тарифа «Pro»; понятность этого сочетания — суждение человека, а не свойство разметки. Интерфейсной работы этот план НЕ ведёт (граница объёма), пункт записан, а не закрыт"
      verification: backstop
    - statement: "Настоящее уведомление ЮKassa о подтверждённом платеже проходит новую сверку ранга без отказа: боевого доступа к API у исполнителя нет, всё покрытие на моках (перенесено из human_verification 05-VERIFICATION.md, D-26)"
      verification: backstop
  artifacts:
    - "app/application/billing/plan_switch.py — `switch_is_refused`, ЕДИНСТВЕННОЕ объявление правила перехода между тарифами"
    - "app/services/payment_service.py — `_apply_extension` со сверкой ранга и записью в журнал; докстринг `_extend_subscription`, совпадающий с кодом"
    - "app/pages/billing.py — гард формы читает то же объявление, своей копии сравнения не держит"
    - "tests/test_application/test_plan_switch.py — таблица решений правила и границы модуля"
    - "tests/test_pages/test_billing_payment_errors.py — регрессии стадии ПРИМЕНЕНИЯ, которых в суите не было ни одной"
  key_links:
    - "`PLAN_ORDER` → `switch_is_refused` → `_apply_extension` — связь, помеченная `✗ NOT_WIRED` в 05-VERIFICATION.md (Key Link Verification)"
    - "`PLAN_ORDER` → `switch_is_refused` → `subscribe_to_plan` — связь СУЩЕСТВУЕТ и обязана уцелеть при переносе"
    - "`handle_webhook` → `_extend_subscription` → `_apply_extension` → `Subscription.plan` → контекст шелла → бейдж тарифа и четыре метра (BILL-06 рисуется по лимитам ЭТОГО значения)"
  prohibitions:
    - requirement_id: BILL-05
      category: fairness
      statement: "MUST NOT превращать оплаченный остаток старшего тарифа в дни младшего без объявленного правила и без слова пользователю до нажатия кнопки"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-05
      category: transparency
      statement: "MUST NOT записывать исход, в котором уплаченный тариф и действующий тариф разошлись, как обычный успех — расхождение обязано оставлять собственный след в журнале, а не прятаться за `subscription_payment_succeeded`"
      status: resolved
      verification: test
    - requirement_id: BILL-06
      category: transparency
      statement: "MUST NOT показывать человеку строку истории с одним тарифом рядом с бейджем другого действующего тарифа без единого слова о том, что оплата продлила срок, а не сменила тариф"
      status: unresolved
      verification: judgment
---

<objective>
Гэп 1 отчёта `05-VERIFICATION.md` — **блокер на денежном пути**. Правило владельца
`upgrade-only` (чекпойнт плана 05-10) существует РОВНО НА ВХОДЕ и не существует там,
где приходят деньги.

`PLAN_ORDER` (`app/constants.py:70`) читается ровно одним файлом —
`app/pages/billing.py:133`, стадия НАМЕРЕНИЯ. В `app/services/payment_service.py`
вхождений ноль, и `_apply_extension` (`:504-509`) перезаписывает
`subscription.plan` безусловно. Верификатор воспроизвёл это независимо на боевом
пути кода: пользователь без подписки, два `pending` платежа — `pro`, затем `basic`;
после первого `plan=pro, expires_at=2026-09-16`, после второго
`plan=basic, expires_at=2026-10-16`. Оплаченный МЕСЯЦ старшего тарифа стал днями
младшего, и следа в журнале нет — `subscription_payment_succeeded` печатает
`plan=basic` как обычный успех. Гард формы в этом сценарии не срабатывает вовсе:
на момент обоих нажатий действующей подписки не было.

Покрытие выполнено половиной: `test_an_upgrade_does_not_burn_the_paid_remainder`
доходит до `handle_webhook`, а `test_a_downgrade_is_refused_with_a_named_reason`
(`tests/test_pages/test_billing_payment_errors.py:696-711`) останавливается на 302
гарда формы и стадию применения не трогает.

Purpose: правило, которое стоит денег, обязано жить в ОДНОМ объявлении и читаться
обеими стадиями. Две независимые копии сравнения рангов воспроизвели бы ровно тот
класс расхождения, которым этот гэп и является.
Output: одно объявление правила, обе стадии на нём, регрессия на стадию применения
и запись в журнал о сохранённом тарифе.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@.planning/phases/05-tarify/05-UI-SPEC.md
</context>

<flagged_assumptions>

**Зонд краевых случаев вернул все три требования фазы `unclassified` — ни одна строка
не снята умолчанием.** `SPEC.md` у фазы нет вовсе, поэтому разделов `## Edge Coverage`
и `## Prohibitions` не существует и применяется запасной путь. Детерминированный зонд
дал `{"applicable":3,"resolved":0,"unresolved":3}` — все три строки `unclassified`,
`verification: null`, `resolution: null`. По протоколу `unclassified` НИКОГДА не
закрывается `backstop`-ом автоматически и не роняется молча. Все три вносятся сюда
явными допущениями планировщика:

| # | Требование | Состояние зонда | Допущение планировщика |
|---|-----------|-----------------|------------------------|
| E-01 | **BILL-05** — пользователь может продлить текущую подписку | `unclassified` / `unresolved` | Краевые случаи продления сняты НЕ зондом, а этим планом построчно: продление своего тарифа, первая покупка с Free, повышение, понижение, пустой план платежа, план без ранга. Классификатор зонда не отработал — строка требования написана по-русски, а лексикон классификатора английский. Это наблюдение об инструменте, не основание снять строку |
| E-02 | **BILL-06** — потребление и остаток по четырём осям | `unclassified` / `unresolved` | Краевые случаи осей закрыты планом 05-03 (22 теста `test_plan_usage.py`, зелёные в верификации). Этот план трогает BILL-06 РОВНО в одном месте: значение `subscription.plan`, по лимитам которого рисуются четыре метра. Механика показа не трогается — испорчен был источник, а не витрина |
| E-03 | **BILL-07** — история платежей | `unclassified` / `unresolved` | Краевые случаи журнала закрыты планами 05-02, 05-04, 05-08, 05-09 (пустое состояние, потолок 200, статусы, устойчивость `format_amount`). Этот план в журнал не пишет и его разметку не трогает; единственное касание — backstop о сочетании строки истории с бейджем тарифа, записанный, а не закрытый |

Та же лексиконовая слепота уже задокументирована в `COVERAGE.md` для детектора API.
Строки остаются `unresolved` и передаются дальше — их закрытие требует либо
англоязычной формулировки требований, либо ручной классификации владельцем.

**Прохибиция плана 05-10 (`BILL-05`/fairness) НЕ перемечается `resolved`.** План 05-10
пометил её `resolved`, и верификатор это опроверг воспроизведением. Этот план закрывает
ДЕНЕЖНУЮ половину — оплаченный остаток старшего тарифа перестаёт превращаться в дни
младшего, и это доказуемо тестом. Вторая половина — «слово пользователю ДО нажатия
кнопки» — в воспроизведённом сценарии НЕ выполнима вовсе: действующей подписки на
момент нажатия нет, поэтому ни подпись карточки (`DOWNGRADE_CARD_CAPTION`), ни гард
формы не участвуют. Прохибиция унесена вперёд со статусом `unresolved` и тиром
`judgment`; решение — за владельцем, как и записано в `human_verification`
`05-VERIFICATION.md`. Пометить её зелёной на основании этого плана значило бы повторить
ровно ту ошибку, которую верификатор поймал.

**Лифт `## UI Considerations` 05-UI-SPEC.md — точечный, а не оптовый.** В объёме этих
двух гэпов лежит РОВНО ОДНА строка — C2 из таблицы «Перенесено из фазы» (таблица
начинается `05-UI-SPEC.md:369`, строка C2 — `:377`), и она
лифтится явной истиной в `must_haves.truths`. Строки U1-U5 закрыты планами 05-09 и
05-10, R1-R24 сняты с отгруженного кода планами 05-05 и 05-09, B1-B3 — backstop о
длинном тексте, к денежному пути отношения не имеющие, D1-D10 сняты с причиной.
Повторный лифт всех 42 строк был бы переписыванием закрытой работы; ни одна из них этим
планом не затрагивается и ни одна не роняется.

**Детектор `assumption-delta` прогнан и вернул `detected: false`** (`signals: []`).
Раздел `<assumption_delta_decision>` поэтому не пишется — по протоколу при `detected:
false` вопрос не поднимается вовсе.

**`COVERAGE.md` не перегенерируется и не правится.** Набор интегрируемых возможностей
API ЮKassa этим планом не меняется: ни одного нового вызова SDK, ни одного нового
события вебхука. Матрица остаётся как есть.

**Схема БД не меняется, ревизии Alembic не заводится.** Сверка ранга — сравнение двух
строк в памяти; ни колонки, ни индекса она не требует. Очередь `0013`…`0018` этим планом
не трогается (D-26 в силе, боевая база на `0012`).

**Ручные пункты `behavior_unverified_items` и `human_verification` задачами НЕ становятся**
(мобильная ширина 375px, настоящий платёж в тестовом магазине ЮKassa, событие
`payment.canceled`, читаемость формулировок). Браузерного и e2e-харнесса в проекте нет —
grep по `pyproject.toml` на playwright/selenium/puppeteer пуст; боевого доступа к API
ЮKassa нет; состав событий D-27 задаётся вне репозитория. Они перечислены в
`<verification>` как перенесённые человеческие проверки.

**Каноничные позиции безопасности прохибициями НЕ минтятся.** Подделка и повтор
уведомления с чужим значением плана, инъекция через тело уведомления — канон; они
покрыты `<threat_model>` ниже (T-05-52, T-05-53) и командой `/gsd-secure-phase`,
отдельными прохибициями не объявляются.

</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Правило `upgrade-only` доходит туда, где приходят деньги — сквозная регрессия стадии применения</name>
  <precondition>`tests/test_pages/test_billing_payment_errors.py` содержит помощники `_seed_live_subscription`, `_subscription_rows`, `_aware`, `_current_user`, `_healthy_sdk`, `_post`, `_subscribe` (отгружены планом 05-10). Проверяется одним `grep -c` по именам до начала работы; при отсутствии — остановка, потому что весь состав задачи опирается на них.</precondition>
  <reversibility rating="costly">Меняется то, что подтверждённый платёж делает с действующей подпиской. Отката схемы не требует — ни миграции, ни внешнего контракта, ни колонки; правится код и тесты. Но исход касается уже уплаченных денег живых пользователей, поэтому «дёшево откатить» здесь неправда: направление выбрано НЕ этим планом, а чекпойнтом плана 05-10 (`upgrade-only`) и списком `missing:` верификатора («план ТОЛЬКО повышается, срок двигается всегда»); из двух возможных исходов выбран НЕразрушающий. `one-way` рейтинг был бы преувеличением — чекпойнт не заводится.</reversibility>
  <files>tests/test_pages/test_billing_payment_errors.py, app/application/billing/plan_switch.py, app/pages/billing.py, app/services/payment_service.py</files>
  <read_first>
    - app/services/payment_service.py (строки 396-509 целиком: докстринг `_extend_subscription`, ветка первой вставки в savepoint, `_active_subscription`, и `_apply_extension` — три строки, в которых лежит дефект; плюс строки 350-393 — как `handle_webhook` доходит до подписочной ветки и что печатает `subscription_payment_succeeded`; плюс строки 1-20 — блок импортов, куда встанет новый)
    - app/constants.py (строки 48-70 — объявление `PLAN_ORDER` целиком с докстрингом: там уже записано, ПОЧЕМУ порядок не выводится из цены и почему план без ранга получает отказ, а не догадку. Эти два правила обязаны уцелеть дословно при переносе)
    - app/pages/billing.py (строки 1-30 — импорты; строки 98-138 — `_subscription_is_live` и `_switch_is_refused` целиком с докстрингами; строки 231-244 — вычисление `refused_plan_ids` для карточек; строки 331-347 — гард в `subscribe_to_plan`. Это ДВА места вызова, оба обязаны продолжить работать)
    - app/application/billing/subscription_period.py (весь файл — образец границ модуля этого слоя: «ничего не пишет и ничего не читает из БД, не знает ни про Jinja, ни про Request, ни про сессию», и почему такая арифметика обязана проверяться без поднятия БД. Новый модуль пишется по этому образцу)
    - tests/test_pages/test_billing_payment_errors.py (строки 90-190 — помощники `_yoo_payment`, `_healthy_sdk`, `_post`, `_subscribe`, `_payments_count`, `_current_user`: у `_healthy_sdk` есть параметр `payment_id`, а `_post` его НЕ прокидывает, из-за чего два платежа получили бы один идентификатор; строки 589-625 — `_seed_live_subscription`, `_subscription_rows`, `_aware`; строки 653-770 — существующие тесты повышения, понижения, продления и первой покупки, рядом с которыми встают новые)
    - tests/test_services/test_payment_service.py (как соседний файл вызывает `handle_webhook` напрямую и что подменяет — образец формы вызова)
  </read_first>
  <behavior>
    - RED, воспроизведение верификатора: у пользователя нет подписки; создаются два платежа — `pro`, затем `basic` — с РАЗНЫМИ идентификаторами ЮKassa; `handle_webhook` зовётся дважды подряд событием `payment.succeeded`. Ожидание: строка подписки одна, `plan == "pro"`, `expires_at` сдвинут ДВАЖДЫ. Против сегодняшнего кода тест обязан УПАСТЬ на `plan == "pro"`, получив `"basic"`.
    - RED: тот же прогон записывает в журнал ключ `subscription_plan_preserved` с уплаченным и сохранённым планом. Сегодня такого ключа нет — тест обязан упасть.
    - Повышение на стадии применения проходит: действующая подписка `basic`, подтверждённый платёж `pro` → `plan == "pro"`.
    - Продление своего тарифа не изменилось: действующая `pro`, подтверждённый платёж `pro` → `plan == "pro"`, срок сдвинут от существующего.
    - Все 32 существующих теста файла остаются зелёными без правки их тел.
  </behavior>
  <action>
**1. Сначала тесты (RED).** Дописать в `tests/test_pages/test_billing_payment_errors.py`
новый раздел регрессий стадии ПРИМЕНЕНИЯ — рядом с существующими тестами гарда, потому
что помощники общие.

Идентификаторы платежей обязаны различаться. `_healthy_sdk` уже принимает `payment_id`,
но `_post` его не прокидывает и всегда отдаёт `"yoo_1"`; два платежа с одним
идентификатором дали бы в `handle_webhook` не воспроизведение дефекта, а
`MultipleResultsFound` на выборке. Прокинуть необязательный параметр
`payment_id` через `_post` и `_subscribe` со значением по умолчанию `"yoo_1"` — тогда
тридцать два существующих теста не меняются ни строкой.

Имена новых тестов (они же — предмет проверки, а не украшение):
- `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage` —
  воспроизведение верификатора целиком: два платежа `pro` → `basic`, два вызова
  `handle_webhook`, итог `plan == "pro"` и одна строка подписки. Разделение сроков
  числовое: один месяц — не больше 31 дня, два — не меньше 59, поэтому порог сравнения
  ставится на 45 дней от «сейчас», и промах в любую сторону однозначен.
- `test_the_preserved_plan_is_visible_in_the_log` — тот же прогон с подменой
  `app.services.payment_service.logger`; проверяется вызов с ключом
  `subscription_plan_preserved` и полями уплаченного и сохранённого плана.
- `test_a_confirmed_higher_plan_is_applied_at_the_apply_stage` — действующая `basic`,
  подтверждённый `pro` → `plan == "pro"`.
- `test_renewing_the_own_plan_still_moves_the_date_at_the_apply_stage` — действующая
  `pro`, подтверждённый `pro`: план тот же, срок сдвинут от существующего.

Первые два обязаны быть КРАСНЫМИ до шага 2 — иначе дефект не воспроизведён и правка
доказывает не то.

**2. `app/application/billing/plan_switch.py` — новый модуль, ЕДИНСТВЕННОЕ объявление
правила.** Публичная функция `switch_is_refused(current_plan, target_plan) -> bool`.
Тело и докстринг переносятся из `app/pages/billing.py:114-138` — логика не
переписывается, потому что она верна и покрыта; переезжает МЕСТО объявления. Ранг
берётся из `PLAN_ORDER` (`app.constants`), равные планы дают `False` (продление своего
тарифа гардом не задевается), незнакомый план с любой стороны даёт `True` — отказ, а не
догадка.

Модуль кладётся в `app/application/billing/` рядом с `subscription_period.py` и по его
образцу: он НИЧЕГО не пишет и ничего не читает из БД, не знает ни про Jinja, ни про
`Request`, ни про сессию SQLAlchemy, и его единственный импорт — `app.constants`.
Причина не в чистоте: `app/pages/` импортирует `app/services/payment_service.py`, и
обратный импорт замкнул бы цикл. Слой `app/application/` — единственное место, которое
видно ОБОИМ потребителям.

Докстринг дополняется абзацем о том, что у правила ДВЕ стадии: намерение
(`POST /billing/subscribe` решает, продавать ли) и применение (`_apply_extension`
решает, что делать с уже уплаченным). Обе читают эту функцию; расхождение между ними и
есть тот дефект, ради которого модуль заведён.

**3. `app/pages/billing.py` — потребитель, а не владелец правила.** Удалить приватное
объявление `_switch_is_refused` целиком, импортировать `switch_is_refused` из нового
модуля и заменить оба вызова: в вычислении `refused_plan_ids` (строка 243) и в гарде
`subscribe_to_plan` (строка 346). `PLAN_ORDER` из импорта `app.constants` убирается —
`PAYMENT_LIST_CAP` остаётся. `_subscription_is_live` НЕ переезжает: она читает
`expires_at` и `normalize_utc`, то есть про срок, а не про порядок тарифов, и её место
рядом с обработчиком. `DOWNGRADE_CARD_CAPTION`, `PAYMENT_ERROR_MESSAGES` и код причины
`downgrade` не трогаются вовсе.

**4. `app/services/payment_service.py` — сверка ранга там, где приходят деньги.**
Импортировать `switch_is_refused` рядом с уже стоящим импортом `next_expiry`.

В `_apply_extension` порядок обязан быть таким:
- срок двигается ВСЕГДА и первым — `next_expiry` вызывается как сегодня, поведение
  этой строки не меняется ни на йоту (прохибиция плана 05-01 про неистраченный остаток
  соблюдается ровно тем, что здесь ничего не меняется);
- пустой `db_payment.plan` — как сегодня, план не трогается;
- иначе решает `switch_is_refused(subscription.plan, db_payment.plan)`: `True` —
  действующий план СОХРАНЯЕТСЯ и пишется `logger.warning("subscription_plan_preserved", ...)`
  с `user_id`, `yookassa_id` (из `db_payment.yookassa_payment_id`), сохранённым и
  уплаченным планом; `False` — план записывается, как раньше.

Уровень `warning`, а не `info`: платёж принят и оплаченные дни выданы, но уплаченный
тариф применён НЕ был — это не рядовое событие вроде проигранной заявки, и оно обязано
быть видно тому, кто разбирает обращение пользователя.

Ключ `subscription_payment_succeeded` (строка 366-372) НЕ переименовывается и НЕ теряет
поле `plan`: оно описывает ПЛАТЁЖ, а не итоговую подписку, и это верно. Строка о
сохранённом тарифе выходит РАНЬШЕ него по потоку, поэтому пара читается однозначно.

Функция остаётся синхронной и по-прежнему не пишет в БД сама: `commit` делает
`handle_webhook` (`:365`), и этот порядок не трогается — заявка платежа и начисление
обязаны остаться в одной транзакции (решение плана 05-08).

Ветка ПЕРВОЙ вставки подписки (`:460-471`) не меняется: подписки нет, защищать нечего,
план берётся из платежа. Сверка ранга там сравнивала бы значение с пустотой.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_payment_errors.py tests/test_services/test_payment_service.py tests/test_pages/test_billing_subscription.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_billing_payment_errors.py -q` завершается с кодом 0
    - `uv run pytest tests/ -q` завершается с кодом 0
    - Файл `app/application/billing/plan_switch.py` существует
    - `grep -c 'def switch_is_refused' app/application/billing/plan_switch.py` == 1
    - `grep -c 'switch_is_refused' app/services/payment_service.py` >= 2 — импорт и вызов
    - `grep -c 'switch_is_refused' app/pages/billing.py` >= 3 — импорт и два вызова
    - `grep -c 'subscription_plan_preserved' app/services/payment_service.py` >= 1
    - `grep -c '^from app.constants import PAYMENT_LIST_CAP$' app/pages/billing.py` == 1 — импорт порядка тарифов ушёл вместе с правилом, потолок списка остался
    - Единственность объявления правила (проверка по объектам модулей, а не по тексту, поэтому не ломается комментарием): `uv run python -c "import app.pages.billing as p, app.services.payment_service as s; from app.application.billing.plan_switch import switch_is_refused as f; assert p.switch_is_refused is f; assert s.switch_is_refused is f; assert not hasattr(p, 'PLAN_ORDER'); assert not hasattr(s, 'PLAN_ORDER')"` завершается с кодом 0
    - Границы нового модуля: `uv run python -c "import app.application.billing.plan_switch as m; assert not hasattr(m, 'select'); assert not hasattr(m, 'AsyncSession'); assert not hasattr(m, 'Request')"` завершается с кодом 0
    - Поведение (воспроизведение верификатора): два подтверждённых платежа `pro`, затем `basic`, у пользователя без подписки оставляют РОВНО ОДНУ строку `subscriptions` с `plan == "pro"` и `expires_at` больше «сейчас плюс 45 дней»
    - Поведение: тот же прогон вызывает `logger.warning` с первым позиционным аргументом `subscription_plan_preserved`
    - Поведение: действующая подписка `basic` плюс подтверждённый платёж `pro` дают `plan == "pro"`
    - Поведение: действующая подписка `pro` плюс подтверждённый платёж `pro` оставляют `plan == "pro"` и двигают срок от существующего `expires_at`
    - Поведение (гард формы не задет переносом): `POST /billing/subscribe` с планом `basic` при действующей `pro` по-прежнему отдаёт 302 на `/billing?error=downgrade` и не создаёт строку `payments` — `test_a_downgrade_is_refused_with_a_named_reason` зелёный БЕЗ правки тела
    - Поведение (карточка не задета переносом): `test_the_lower_plan_card_explains_itself_instead_of_offering_a_button` и `test_the_higher_plan_card_still_offers_the_upgrade` зелёные без правки
    - `git diff --stat -- pyproject.toml uv.lock` пуст — ни одного нового пакета
    - `git diff --name-only -- alembic/` пуст — ревизии не заводится
  </acceptance_criteria>
  <done>Подтверждённый платёж младшего тарифа больше не понижает действующий старший: план только повышается, срок двигается всегда, а сохранение старшего тарифа оставляет собственную строку журнала. Правило объявлено один раз и читается обеими стадиями.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Соседние случаи стадии применения, границы модуля и докстринг, совпадающий с кодом</name>
  <files>tests/test_application/test_plan_switch.py, tests/test_pages/test_billing_payment_errors.py, app/services/payment_service.py</files>
  <read_first>
    - app/application/billing/plan_switch.py (результат задачи 1 — точная сигнатура, докстринг и поведение на незнакомом плане)
    - app/services/payment_service.py (строки 396-455 — докстринг `_extend_subscription` ЦЕЛИКОМ после задачи 1; особое внимание последнему абзацу «⚠️ ГАРД СТОИТ НА ВХОДЕ, А НЕ ЗДЕСЬ, И ЭТО НАМЕРЕННО» — именно он назван верификатором вводящим в заблуждение, и именно его обоснование опровергнуто)
    - tests/test_application/test_subscription_period.py (образец файла тестов этого слоя: как проверяется чистая функция без поднятия БД, как оформлен проход по краевым значениям)
    - tests/test_pages/test_billing_payment_errors.py (строки 624-651 — `test_the_order_of_plans_is_declared_and_not_derived_from_price` и `test_every_shipped_plan_of_the_config_has_a_rank`: они уже держат инвариант объявления порядка и обязаны остаться зелёными; строки 770-800 — `test_a_plan_without_a_rank_fails_closed`, образец параметризации незнакомого плана; строка 848 — `test_the_switch_semantics_are_named_in_the_place_that_moves_the_date`, который читает докстринг и требует в нём подстроки `upgrade-only` и `05-01`)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 27-32 — артефакт про докстринг и список `missing:` гэпа 1: три пункта, каждый обязан быть закрыт задачей 1 или этой)
  </read_first>
  <behavior>
    - `switch_is_refused` на равных планах даёт `False` (продление своего тарифа не задевается).
    - На повышении (`basic` → `pro`) даёт `False`.
    - На понижении (`pro` → `basic`, `basic` → `free`, `pro` → `free`) даёт `True`.
    - Незнакомый план С ЛЮБОЙ стороны даёт `True` — отказ, а не догадка; проверяется в обе стороны.
    - Модуль не тянет ни сессию БД, ни `Request`, ни Jinja: его импорт не поднимает ничего из `app.pages` и `app.services`.
    - Стадия применения: подтверждённый платёж с ПУСТЫМ планом двигает срок и не трогает действующий план.
    - Стадия применения: подтверждённый платёж с планом, которого нет в `PLAN_ORDER`, двигает срок, СОХРАНЯЕТ действующий план и пишет строку журнала.
    - Стадия применения: первая покупка платного тарифа с Free по-прежнему заводит строку подписки с планом из платежа.
    - Докстринг `_extend_subscription` называет поведение подтверждённого платежа младшего тарифа и по-прежнему содержит `upgrade-only` и `05-01`.
  </behavior>
  <action>
**1. `tests/test_application/test_plan_switch.py` — новый файл тестов правила.**
Таблица решений из `<behavior>` параметризацией: равные, повышение, три понижения,
незнакомый план с каждой стороны. Плюс тест границ модуля по образцу докстринга
`subscription_period.py`: импорт модуля не приносит ни `AsyncSession`, ни `select`, ни
`Request`; единственная внешняя зависимость — `app.constants`.

Плюс тест, ради которого этот план вообще существует: правило объявлено ОДИН раз.
Проверяется по объектам модулей, а не по тексту исходника — `app.pages.billing` и
`app.services.payment_service` ссылаются на ТУ ЖЕ функцию, что экспортирует
`app.application.billing.plan_switch`, и ни один из двух не держит собственного имени
`PLAN_ORDER`. Текстовая проверка здесь была бы хуже: она сломалась бы от упоминания
имени в комментарии и не поймала бы вторую реализацию под другим именем.

**2. Соседние случаи стадии применения** — дописать в
`tests/test_pages/test_billing_payment_errors.py`:
- `test_a_payment_without_a_plan_moves_the_date_and_leaves_the_plan_alone` — платёж
  подписочного вида с пустым `plan`. Ветка существует в коде сегодня и регрессии не
  имеет; после правки она обязана вести себя как раньше.
- `test_a_paid_plan_without_a_rank_keeps_the_live_plan_at_the_apply_stage` — платёж на
  план, которого нет в `PLAN_ORDER` (перечень окружения разошёлся с кодом). Действующий
  план сохраняется, срок двигается, строка журнала пишется. Это зеркало
  `test_a_plan_without_a_rank_fails_closed`, который держит тот же инвариант на стадии
  намерения: догадка стоит денег в обе стороны.
- `test_the_first_subscription_still_takes_its_plan_from_the_payment` — первая покупка с
  Free заводит строку с планом из платежа. Ветка вставки задачей 1 не менялась, и этот
  тест держит её от «заодно» правки в будущем.

**3. Докстринг `_extend_subscription` — правда вместо умолчания.** Верификатор назвал
проблему точно: докстринг утверждает, что понижение «не предлагается карточкой и не
принимается гардом», умалчивая, что подтверждённый платёж его ПРИМЕНЯЕТ, а следующий
абзац объявляет это намеренным. Обоснование там приведено верное («отказать оплаченному
платежу хуже»), но оно оправдывает НЕ-ОТКАЗ, а не понижение: сдвинуть срок и сохранить
старший тариф удовлетворяет обоим ограничениям сразу — ровно это теперь и делает код.

Переписать последний абзац так, чтобы он описывал исполняемое поведение: подтверждённый
платёж исполняется здесь ЛЮБОЙ и деньги всегда превращаются в дни, но ПЛАН при этом
только повышается; расхождение уплаченного и действующего тарифа записывается ключом
`subscription_plan_preserved`. Назвать обе стадии и место, где живёт общее правило
(`app/application/billing/plan_switch.py`).

Подстроки `upgrade-only` и `05-01` обязаны УЦЕЛЕТЬ — на них стоит
`test_the_switch_semantics_are_named_in_the_place_that_moves_the_date`, и он проверяет
ровно то, ради чего абзац существует.

Дописать в этот же тест положительное утверждение о том, что докстринг называет ключ
журнала расхождения, — чтобы «правило записано там, где двигается срок» перестало быть
проверкой на два слова и стало проверкой на исполняемое поведение.

**4. После правок кода** — `graphify update .` (правило проекта: граф обновляется после
изменения кода; операция AST-only, без обращений к API).
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_plan_switch.py tests/test_pages/test_billing_payment_errors.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` завершается с кодом 0
    - Файл `tests/test_application/test_plan_switch.py` существует, и `uv run pytest tests/test_application/test_plan_switch.py --collect-only -q` сообщает **не менее 7 собранных тестов**. Считаются СОБРАННЫЕ случаи, а не строки `def test_`: параметризованные понижения и незнакомый план с обеих сторон идут каждый за себя, поэтому число в критерии и число в выводе команды — одно и то же число. Таблица решений `<behavior>` даёт семь случаев (равные, повышение, три понижения, незнакомый план с каждой стороны), тесты границ модуля и единственности объявления — сверх них
    - `uv run pytest tests/test_application/test_plan_switch.py -q` завершается с кодом 0
    - Поведение: `switch_is_refused("pro", "pro")` даёт `False`; `switch_is_refused("basic", "pro")` даёт `False`; `switch_is_refused("pro", "basic")` даёт `True`; `switch_is_refused("platinum", "basic")` и `switch_is_refused("basic", "platinum")` дают `True`
    - Поведение: подтверждённый подписочный платёж с пустым планом двигает `expires_at` и оставляет `subscription.plan` прежним
    - Поведение: подтверждённый платёж на план вне `PLAN_ORDER` при действующей подписке оставляет `subscription.plan` прежним, двигает `expires_at` и вызывает `logger.warning` с ключом `subscription_plan_preserved`
    - Поведение: первая покупка платного тарифа при отсутствии подписки создаёт строку с `plan`, равным плану платежа
    - Докстринг `_extend_subscription` содержит подстроки `upgrade-only`, `05-01` и `subscription_plan_preserved`: `uv run python -c "import app.services.payment_service as m; d = m._extend_subscription.__doc__; assert 'upgrade-only' in d and '05-01' in d and 'subscription_plan_preserved' in d"` завершается с кодом 0
    - `test_the_switch_semantics_are_named_in_the_place_that_moves_the_date` зелёный и содержит новое положительное утверждение о ключе журнала
    - Существующие инварианты порядка тарифов не задеты: `test_the_order_of_plans_is_declared_and_not_derived_from_price` и `test_every_shipped_plan_of_the_config_has_a_rank` зелёные без правки тел
    - `graphify update .` выполнен и завершился успешно
    - `git diff --stat -- pyproject.toml uv.lock` пуст
  </acceptance_criteria>
  <done>Все три пункта `missing:` гэпа 1 закрыты: сверка ранга стоит в `_apply_extension` и читает то же объявление, что гард формы; сохранение старшего тарифа оставляет строку журнала; регрессия на стадию применения существует и покрывает повышение, понижение, продление, пустой план и план без ранга. Докстринг в месте, которое двигает срок, совпадает с исполняемым кодом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ЮKassa → `POST /api/billing/webhook` | Тело уведомления приезжает из сети; источником истины о предмете покупки быть не может |
| `payments` (своя БД) → `_apply_extension` | Значение `plan` строки платежа — единственный доверенный источник; оно решает, что произойдёт с действующей подпиской |
| `PLAN_LIMITS` (переменная окружения) → `PLAN_ORDER` (код) | Перечень тарифов правится окружением, порядок — кодом; разойтись они могут в любую сторону |
| `Subscription.plan` → показ тарифа и четыре метра | Испорченное на записи значение рисует витрину по лимитам чужого плана (BILL-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-52 | Elevation of Privilege | `_apply_extension` — подтверждённый (в т.ч. повторно доставленный или пришедший не по порядку) платёж младшего тарифа снимает с пользователя оплаченный старший | high | mitigate | Задача 1: сверка ранга через `switch_is_refused` на стадии применения. План только повышается; понижение сохраняет действующий тариф и пишет `subscription_plan_preserved`. Гард источника (план 05-07) и заявка `_claim_payment` (план 05-08) не трогаются: новая проверка стоит ПОСЛЕ выигранной заявки и внутри той же транзакции, единственный `commit` остаётся единственным |
| T-05-53 | Tampering | Выбор тарифа по значению из ТЕЛА уведомления вместо колонки `payments.plan` | high | mitigate | Сверка ранга читает `db_payment.plan` и `subscription.plan` — обе величины из СВОЕЙ базы. `payment_data` в `_apply_extension` не передаётся вовсе и передаваться не начнёт: сигнатура функции не расширяется. Существующее правило `handle_webhook` («предмет покупки решает колонка `kind` из БД, никогда `metadata`») этим планом не ослабляется |
| T-05-54 | Repudiation | Расхождение уплаченного и действующего тарифа не оставляет следа — разбор обращения пользователя опереть не на что | medium | mitigate | Задача 1: `logger.warning("subscription_plan_preserved", …)` с `user_id`, `yookassa_id`, сохранённым и уплаченным планом. Уровень `warning`, а не `info`: событие не рядовое |
| T-05-55 | Elevation of Privilege | План, которого нет в `PLAN_ORDER` (окружение разошлось с кодом), получает УГАДАННЫЙ ранг на стадии применения | high | mitigate | Отказ по умолчанию переносится вместе с функцией: незнакомый план с любой стороны даёт `True`, действующий тариф сохраняется, срок двигается, отказ записан. Задача 2 закрепляет регрессией в обе стороны |
| T-05-56 | Denial of Service | Циклический импорт между `app/pages` и `app/services` при попытке переиспользовать гард формы напрямую | medium | mitigate | Правило объявляется в `app/application/billing/`, который не импортирует ни один из двух. Критерий приёмки проверяет импорт обоих модулей и границы нового |
| T-05-51 | Elevation of Privilege | Повышение отдаёт лимиты старшего тарифа на оставшиеся дни младшего бесплатно | low | accept | Перенесено из плана 05-10 без изменений. Подарок, а не потеря; применения лимитов в системе нет вовсе (D-08), поэтому «лимиты Pro» сегодня не дают доступа ни к чему сверх показа |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает ни одного пакета и не трогает `pyproject.toml`/`uv.lock` — критерий приёмки проверяет это диффом. Аудит легитимности — `05-RESEARCH.md:144` `## Package Legitimacy Audit`; задач вида «установить X» в плане нет |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-11`. Общий перечень фазы — в `05-10-PLAN.md`.

**Файлы (новые):**
- `app/application/billing/plan_switch.py`
- `tests/test_application/test_plan_switch.py`

**Функции (новые):**
- `switch_is_refused(current_plan: str, target_plan: str) -> bool` — `app/application/billing/plan_switch.py`

**Функции (перенесённые, объявление удаляется из прежнего места):**
- `_switch_is_refused` — удаляется из `app/pages/billing.py`; оба вызова переходят на `switch_is_refused`

**Ключи журнала (новые):**
- `subscription_plan_preserved` — `app/services/payment_service.py`, уровень `warning`, поля `user_id`, `yookassa_id`, сохранённый и уплаченный план

**Имена тестов (новые):**
- `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage`
- `test_the_preserved_plan_is_visible_in_the_log`
- `test_a_confirmed_higher_plan_is_applied_at_the_apply_stage`
- `test_renewing_the_own_plan_still_moves_the_date_at_the_apply_stage`
- `test_a_payment_without_a_plan_moves_the_date_and_leaves_the_plan_alone`
- `test_a_paid_plan_without_a_rank_keeps_the_live_plan_at_the_apply_stage`
- `test_the_first_subscription_still_takes_its_plan_from_the_payment`
- полный набор `tests/test_application/test_plan_switch.py` (таблица решений, границы модуля, единственность объявления правила)

**Изменённая сигнатура помощника тестов:** `_post` и `_subscribe` в
`tests/test_pages/test_billing_payment_errors.py` получают необязательный `payment_id`
со значением по умолчанию `"yoo_1"`

**Alembic revision id:** новых нет — сверка ранга схемы не требует

**Ключи конфига и переменные окружения:** новых нет

**CSS-селекторы, строки копирайта, контракты маршрутов:** новых нет — интерфейс этим
планом не трогается

<verification>
- `uv run pytest tests/ -q` зелёный целиком
- Ни одного нового пакета в `pyproject.toml` / `uv.lock`; ни одной новой ревизии Alembic
- Правило перехода между тарифами объявлено ровно один раз и читается обеими стадиями — проверено по объектам модулей, а не по тексту исходника
- Воспроизведение верификатора (`pro`, затем `basic`, два вызова `handle_webhook`) даёт `plan == "pro"` и срок, сдвинутый дважды
- Все три пункта `missing:` гэпа 1 `05-VERIFICATION.md` закрыты задачами 1 и 2; ни один не отложен
- `graphify update .` выполнен после правок кода

**Перенесённые человеческие проверки — задачами НЕ становятся и НЕ автоматизируются:**
- Мобильная ширина 375px (`behavior_unverified_items`): браузерного харнесса в проекте нет; порог 44px закрыт планом 05-09 на уровне объявления
- Настоящий платёж в тестовом магазине ЮKassa (D-26): боевого доступа к API нет, всё покрытие на моках
- Статус «отклонён» у отменённого платежа (D-27): состав событий задаётся вне репозитория
- Читаемость формулировок «в обработке» и отказа оплаты: суждение человека
- Первое настоящее уведомление после выката проходит гард источника (backstop плана 05-07)
- **Прохибиция `BILL-05`/fairness остаётся `unresolved`/`judgment`:** денежная половина закрыта тестом, половина «слово пользователю до нажатия» в воспроизведённом сценарии недостижима. Решение — за владельцем, флаг `unverified-prohibition — human review recommended` сохраняется
</verification>

<success_criteria>
- Подтверждённый платёж младшего тарифа не понижает действующий старший ни при каком порядке доставки
- Правило `upgrade-only` объявлено один раз; вторая реализация сравнения рангов в `app/` невозможна по построению
- Расхождение уплаченного и действующего тарифа записывается собственным ключом журнала
- Повышение, продление своего тарифа и первая покупка с Free ведут себя ровно как раньше
- План без ранга получает отказ, а не догадку, на ОБЕИХ стадиях
- Докстринг в месте, которое двигает срок, совпадает с исполняемым кодом
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-11-SUMMARY.md` when done
</output>
