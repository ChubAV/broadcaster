---
phase: 05-tarify
plan: 23
type: tdd
wave: 19
depends_on: ["05-22"]
gap_closure: true
files_modified:
  - app/application/billing/subscription_period.py
  - tests/test_application/test_subscription_period.py
autonomous: true
requirements: [BILL-05]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Единственная новая арифметика денежного пути — `converted_remainder` — покрыта ПРЯМЫМИ модульными тестами в том файле, который владеет арифметикой, по образцу таблиц, которые он уже держит для `prorated_expiry`: сегодня символ не встречается в `tests/` ни разу (`IN-01` раунда 6, `✗ ПРОБЕЛ` в разделе Required Artifacts отчёта)"
    - "Абзац `subscription_period.py:231-236` о длине месяца перестаёт утверждать свойство, которого у функции нет: длина месяца в ответ НЕ входит — она стоит знаменателем стоимости остатка и множителем внутри `prorated_days`, и два вхождения сокращаются (проверено верификацией численно на 28, 30 и 31 дне: 110 / 110 / 110)"
    - "Утверждение о независимости ответа от длины месяца перестаёт быть прозой и становится ТЕСТОМ: три базы с длинами месяца 28, 31 и 30 дают один ответ в днях, и этот ответ равен `int(остаток_в_днях · old / new)` — форме, в которой длины месяца нет вовсе"
    - "Недостижимая ветка `subscription_period.py:245-247` (`if base <= now_utc: return now_utc`) не читается больше как второе независимое правило истечения рядом с настоящим: зажим к `now` есть работа `countdown_base` (`:117-121`), и это сказано на месте ветки (`IN-03` раунда 6)"
    - "Усечение неполного дня переноса названо на месте, а не подразумевается: `prorated_days` берёт целую часть, поэтому при равных ценах ответ равен `now + floor(остаток_в_днях)`, и до 23 ч 59 м уже оплаченного времени теряется. Обещание «остаток не сгорает» остаётся верным в днях и слегка неверно в последнем дне — сказано числом, а не эпитетом (`IN-02` раунда 6)"
    - "Ни одно поведение `converted_remainder` не изменено этим планом: правится ОБЪЯВЛЕНИЕ и мёртвая ветка, а не арифметика. Числа, воспроизведённые верификацией раунда 6 (365 дней → 110 дней; 25 дней → 7 дней), обязаны совпасть до дня после правки"
  artifacts:
    - "`tests/test_application/test_subscription_period.py` — таблица решений `converted_remainder` той же формы, что существующая таблица `prorated_expiry`: истёкший и отсутствующий `current`, нижняя граница в один день, naive против aware, равные цены, независимость от длины месяца"
    - "`app/application/billing/subscription_period.py` — абзац о длине месяца приведён к тому, что делает функция; недостижимая ветка снята либо заменена утверждением о зажиме; усечение неполного дня названо одной фразой"
  key_links:
    - "докстринг `converted_remainder` → тест независимости от длины месяца. После этого плана утверждение абзаца проверяется машиной: правка, вводящая настоящую зависимость от календаря, красит тест, а не проходит молча"
    - "`countdown_base` (`:117-121`) → ветка `:245-247`. Один зажим к `now` объявлен в первом; второй, стоящий во втором, недостижим и читается как независимое правило — ровно та дублирующая конструкция, ради устранения которой модуль заведён"
    - "`prorated_days` (`:143`, `max(int(…), 1)`) → нижняя граница `converted_remainder` и её же усечение. Одно объявление даёт и обещание «ноль дней не выдаётся никогда», и потерю неполного дня; тест обязан утверждать оба"
    - "числа отчёта раунда 6 (110 дней при 28, 30 и 31 дне; 7 дней от 25 дней остатка) → критерии приёмки задачи 2. Правка объявления, изменившая хоть одно из них, есть правка ПОВЕДЕНИЯ, а не комментария"
---

<objective>
**Гэп 1, пункт `missing:` №4, и гэп 2, пункты `missing:` №1 и №5 отчёта `05-VERIFICATION.md`
раунда 6.**

Класс «объявление утверждает то, чего код рядом с ним не делает» воспроизведён ЗАНОВО — и
первый из трёх новых экземпляров написан планом `05-18` в том же прогоне, в котором план
`05-19` закрывал четыре прежних экземпляра.

Докстринг `converted_remainder` (`subscription_period.py:231-236`) утверждает: «ДЛИНА МЕСЯЦА
БЕРЁТСЯ У `add_one_month` ОТ ТОЙ ЖЕ БАЗЫ, А НЕ КОНСТАНТОЙ 30 — … константа разошлась бы с
`next_expiry` в феврале и в декабре». Для ЭТОЙ функции утверждение ложно: величина стоит
знаменателем стоимости остатка и множителем внутри `prorated_days`, и два вхождения
сокращаются. Верификация проверила это прогоном настоящей функции — ответ побайтово один при
длине месяца 28, 30 и 31: остаток 365 дней даёт 110 дней при базах 2026-01-31, 2026-03-01 и
2026-04-01, и ровно столько же даёт форма, в которой длины месяца нет вовсе. Собственная
регрессия фазы (`test_billing_payment_errors.py:2310`) уже записана в СОКРАЩЁННОЙ форме — то
есть код и его тест согласны между собой и оба спорят с абзацем.

Живой риск, а не косметика: обратный ход через `timedelta` (`:249-254`) — мёртвое вычисление,
которое следующий читатель «починит» в неверную сторону, поверив, что календарь здесь значит.

Рядом — второй экземпляр того же класса меньшей тяжести: `:245-247` (`if base <= now_utc:
return now_utc`) недостижима, потому что `countdown_base` уже вернул `now_utc` в этом случае,
и читается как второе независимое правило истечения.

И — пустое место, откуда оба экземпляра выросли: единственная новая арифметика денежного пути
не покрыта ПРЯМЫМИ модульными тестами вовсе. Символ `converted_remainder` не встречается в
`tests/` ни разу; всё её покрытие — интеграционные утверждения с допуском ±2 дня, которые не
видят ни усечения дня, ни расхождения в феврале, ни разницы naive/aware.

Purpose: утверждение, которое можно проверить машиной, обязано проверяться машиной — иначе
седьмой раунд найдёт четвёртый экземпляр того же класса. Абзац исправляется НЕ вместо теста, а
вместе с ним.
Output: таблица решений `converted_remainder` в файле, который владеет арифметикой; абзац,
совпадающий с поведением; недостижимая ветка снята; усечение неполного дня названо.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@app/application/billing/subscription_period.py
</context>

<flagged_assumptions>

**Зонд краевых случаев: три требования фазы остаются `unclassified` / `unresolved`.**
Допущения планировщика E-01 (BILL-05), E-02 (BILL-06) и E-03 (BILL-07) впервые записаны
планом `05-15` и повторены планом `05-22`; здесь они не дублируются построчно, но и не
снимаются. Равенство «без молчаливых потерь» по секции рёбер: **3 поднятых == 0 внесённых в
`must_haves` + 3 явных допущения.**

**Прохибиций этот план не минтит НИ ОДНОЙ, и это сказано прямо, а не умолчанием.** Предмет
плана — объявления модуля арифметики; прохибиция этого класса («MUST NOT объявлять в
докстринге инвариант, не назвав теста, который его закрепляет») минтится планом `05-25`,
который строит на неё машинный гейт. Заводить её здесь вторым экземпляром значило бы получить
две формулировки одного запрета — ровно тот класс расхождения, против которого пишется вся
эта волна. Равенство по секции прохибиций: **0 поднятых == 0 записанных + 0 допущений.**

**Строки контракта `05-UI-SPEC.md` не затрагиваются.** План не трогает ни разметки, ни
стилей, ни величины, которую видит покупатель: правится ОБЪЯВЛЕНИЕ и мёртвый код. Ни одна
строка `## UI Considerations` не меняет состояния, и лифтить нечего.

**ПОВЕДЕНИЕ `converted_remainder` НЕ МЕНЯЕТСЯ.** Это граница плана, а не пожелание. Отчёт
раунда 6 прямо называет альтернативу: «если задумывалась зависимость от календаря — это
правка ПОВЕДЕНИЯ с двумя разными длинами месяца, а не правка комментария». Такая правка
меняет величину, которую наблюдает покупатель, и потребовала бы своего чекпойнта. Здесь
выбран второй путь того же пункта `missing:` — привести абзац к тому, что делает функция.
Выбор назван явно и обоснован: величина, которую выбрал владелец решением D-30, посчитана
именно этой арифметикой, и менять её вслед за комментарием было бы решением планировщика о
деньгах.

**Ревизий Alembic план не заводит, пакеты не устанавливаются.** `pyproject.toml`, `uv.lock`,
`alembic/` не трогаются.

**Записанный долг фазы не вменяется и не закрывается**: второе окно потолка намерений,
частичный уникальный индекс, невыкаченная `0018`, D-26, D-08/BILL-02, D-27, отсутствие
RED-коммитов у `05-15` и `05-17`, мобильная проверка на 375px.

</flagged_assumptions>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Таблица решений `converted_remainder` в файле, который владеет арифметикой</name>
  <reversibility rating="reversible">Тесты добавляются; существующие не трогаются. Откат — удаление добавленного раздела.</reversibility>
  <files>tests/test_application/test_subscription_period.py</files>
  <read_first>
    - tests/test_application/test_subscription_period.py (строки 168-281 — раздел «Доля месяца по уплаченному тарифу: таблица решений» целиком: комментарий-заголовок раздела, константы `PRICE_PRO` и `PRICE_BASIC`, семь тестов и параметризация naive/aware. Это ОБРАЗЕЦ формы, которой обязана следовать новая таблица — вплоть до правила «там, где длина месяца участвует множителем, проверяется диапазон, а не точное число»)
    - tests/test_application/test_subscription_period.py (строки 1-20 — докстринг файла и блок импортов из `app.application.billing.subscription_period`; строки 32-45 — параметризация прохода по каждому дню года как образец)
    - app/application/billing/subscription_period.py (`converted_remainder` целиком, включая тело: `countdown_base`, вычисление `month_days`, целочисленное деление `timedelta // timedelta`, вызов `prorated_days`. Тесты обязаны утверждать то, что функция ДЕЛАЕТ, а не то, что о ней написано)
    - app/application/billing/subscription_period.py (`prorated_days`, `:124-145` — источник нижней границы в один день и источник усечения целой части; `countdown_base`, `:103-122` — правило выбора точки отсчёта)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строка 44 — четвёртый пункт `missing:` гэпа 1, называющий состав таблицы дословно; строки 195-198 — числа прогона верификации, которые таблица обязана воспроизвести)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 522-556 — `IN-01` с предложенным составом таблицы и `IN-02` об усечении неполного дня)
  </read_first>
  <behavior>
    - Отсутствующий `current` (`None`) даёт ровно `now`, приведённый к UTC: конвертировать нечего.
    - Истёкший `current` (в прошлом) даёт ровно `now`: воскрешать прошедший срок функция не имеет права, и это зеркало `test_next_expiry_of_an_expired_subscription_counts_from_today`.
    - Живой остаток НИКОГДА не конвертируется в ноль дней: самый дешёвый живой остаток (один день `basic` при цене `pro`) даёт ровно один день, а не ноль — нижняя граница достаётся от `prorated_days` и это её главное свойство здесь.
    - naive и aware `current` одного момента дают ОДИН ответ — зеркало `test_a_naive_and_an_aware_date_give_the_same_fraction`; SQLite отдаёт колонку naive, PostgreSQL aware.
    - Равные цены (`old_price == new_price`) возвращают остаток самому себе В ЦЕЛЫХ ДНЯХ: ответ равен `now + timedelta(days=(base - now).days)`, а не `base`. Утверждение о точном равенстве `base` было бы ложным — целая часть отбрасывает неполный день (`IN-02`), и тест обязан закрепить настоящее поведение, а не желаемое.
    - Ответ НЕ ЗАВИСИТ от длины календарного месяца: три базы, у которых `add_one_month` даёт 28, 31 и 30 дней, при одинаковом остатке дают одинаковое число дней, и оно равно `int(остаток_в_днях · old / new)` — форме без длины месяца вовсе.
    - Повышение уменьшает остаток, понижение увеличивает: аппарат симметричен, и это утверждается обеими сторонами, а не одной.
  </behavior>
  <action>
Новый раздел таблицы решений пишется в конец `tests/test_application/test_subscription_period.py`,
по образцу существующего раздела `prorated_expiry` и с собственным комментарием-заголовком,
который называет: предмет (решение владельца D-30, форма `convert-remainder`), почему таблица
понадобилась именно теперь (единственная новая арифметика денежного пути не имела ПРЯМЫХ
тестов вовсе — `IN-01` раунда 6) и правило утверждений раздела.

`converted_remainder` добавляется в существующий блок импорта файла; второго места импорта не
заводится. Константы `PRICE_PRO` и `PRICE_BASIC` переиспользуются — третьей копии прейскуранта
в файле не появляется.

**Тест независимости от длины месяца — центральный тест раздела, и пишется он числами
отчёта.** Три момента `now`, у которых `add_one_month` даёт разную длину месяца: 2026-01-31
(28 дней), 2026-03-01 (31 день), 2026-04-01 (30 дней). Для каждого — `current = now +
timedelta(days=365)`, цены `PRICE_BASIC` → `PRICE_PRO`. Утверждается, что число выданных дней
одинаково у всех трёх и равно `int(Decimal(365) * PRICE_BASIC / PRICE_PRO)`, то есть 110.
Докстринг теста называет прямо: длина месяца в ответ не входит, это СВОЙСТВО функции, а не
упущение, и абзац докстринга, утверждавший обратное, был опровергнут верификацией раунда 6
этим самым вычислением.

**Тест равных цен пишется с усечением, а не против него.** Утверждение — `converted_remainder(current,
now, old_price=P, new_price=P) == now + timedelta(days=(current - now).days)` при живом
`current`. Докстринг называет, что неполный день переноса теряется, и что это прямое следствие
целой части в `prorated_days`, дающей заодно нижнюю границу в один день; обещание «остаток не
сгорает» верно в днях и слегка неверно в последнем дне.

**Тест нижней границы.** Живой остаток в один день при переходе `basic` → `pro` даёт ровно
один день, а не ноль: целая часть доли здесь равна нулю, и именно поэтому граница не
декоративна. Зеркало `test_a_sum_smaller_than_a_day_still_buys_one_day`.

**Тест симметрии.** Понижение (`pro` → `basic`) при живом остатке даёт БОЛЬШЕ дней, чем было:
аппарат D-29 запущен в другую сторону и работает в обе. Утверждение — сравнение с исходным
остатком в днях, без точного числа.

Все тесты снимают `now` явным литералом с `timezone.utc` по образцу соседей и не зовут
`datetime.now()`: раздел проверяет арифметику, а не сегодняшний день.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_subscription_period.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Прогон `<verify>` завершается кодом `0`; число пройденных тестов приведено в SUMMARY и больше прежнего не менее чем на семь
    - Символ появился в файле, который владеет арифметикой: `grep -c "converted_remainder" tests/test_application/test_subscription_period.py` возвращает не менее `8` (импорт плюс семь вызовов) — до плана значение было `0`
    - Тест независимости от длины месяца утверждает ТРИ базы и ОДНО число: в теле присутствуют литералы `2026-01-31`, `2026-03-01`, `2026-04-01` (в форме `datetime(2026, 1, 31, …)` и т. д.) и вычисленное ожидание без члена длины месяца. Проверяется чтением и цитируется в SUMMARY
    - Число, которое воспроизводит тест, совпадает с числом отчёта раунда 6: `110` дней при остатке 365 дней и переходе `basic` → `pro`. Расхождение означает, что арифметика изменилась, а этот план менять её не вправе
    - Тест равных цен утверждает усечение, а не точное равенство `base`: в теле присутствует `.days`, и утверждения `== current` нет. Проверяется чтением
    - Второй копии прейскуранта не заведено: `grep -c "1490" tests/test_application/test_subscription_period.py` не увеличился относительно `git show HEAD:tests/test_application/test_subscription_period.py | grep -c "1490"`
    - Ни одна строка `app/` не изменена этой задачей: `git diff --name-only -- app/` пуст
    - Ни один существующий тест файла не переписан и не удалён: `git diff -U0 -- tests/test_application/test_subscription_period.py | grep -c "^-def test_"` возвращает `0`
  </acceptance_criteria>
  <done>Таблица решений `converted_remainder` существует в файле, который владеет арифметикой, воспроизводит числа отчёта раунда 6 и закрепляет тестом то самое свойство, которое докстринг утверждал ложно.</done>
</task>

<task type="auto">
  <name>Task 2: Абзац о длине месяца приведён к поведению, недостижимая ветка снята, усечение названо</name>
  <reversibility rating="reversible">Правится объявление и мёртвый код; арифметика не трогается. Откат — обратная правка текста.</reversibility>
  <precondition>Задача 1 исполнена и её прогон зелёный: утверждение о независимости ответа от длины месяца закреплено тестом ДО того, как абзац переписан. Обратный порядок дал бы четвёртое утверждение, проверенное чужими глазами, — ровно тот способ отказа, за который фаза получила раунды 4, 5 и 6.</precondition>
  <files>app/application/billing/subscription_period.py</files>
  <read_first>
    - app/application/billing/subscription_period.py (`converted_remainder` целиком — докстринг и тело; особое внимание абзацу о длине месяца и строкам вычисления `month_days`, `second`, `paid`)
    - app/application/billing/subscription_period.py (`countdown_base`, `:103-122` — докстринг и тело: именно здесь объявлен зажим к `now`, и именно поэтому ветка в `converted_remainder` недостижима)
    - app/application/billing/subscription_period.py (`prorated_expiry`, `:146-185` — её абзац о длине месяца ВЕРЕН и остаётся дословно: там величина действительно входит в ответ. Читается затем, чтобы правка не задела соседа)
    - app/application/billing/subscription_period.py (`prorated_days`, `:124-145` — источник и нижней границы, и усечения)
    - tests/test_application/test_subscription_period.py (новый раздел задачи 1 — абзац обязан ссылаться на тест независимости ПО ИМЕНИ, ровно как `subscription_is_live` ссылается на `test_the_liveness_is_sampled_before_the_date_moves`)
    - .planning/phases/05-tarify/05-VERIFICATION.md (строки 60 и 64 — первый и пятый пункты `missing:` гэпа 2 дословно; строки 49 и 52 — формулировка находки и её артефакты)
    - .planning/phases/05-tarify/05-REVIEW.md (строки 190-240 — `WR-01` с таблицей численной проверки и предложенной редакцией; строки 556-575 — `IN-03` о недостижимой ветке; строки 540-555 — `IN-02` об усечении)
  </read_first>
  <action>
Правятся ТРИ места одной функции. Арифметика не трогается ни на символ: числа 110 и 7,
воспроизведённые верификацией, обязаны остаться теми же, и это проверяется тестами задачи 1
и существующими регрессиями.

**Абзац о длине месяца.** Заменить утверждение о том, что длина месяца берётся у
`add_one_month` вместо константы, на то, что делает функция: длина месяца в ОТВЕТ не входит,
и это СВОЙСТВО, а не упущение — она стоит знаменателем стоимости остатка и множителем внутри
`prorated_days`, и два вхождения сокращаются; ответ равен целой части от произведения остатка
в днях на отношение цен при любой длине месяца, и это проверено на 28, 30 и 31 дне. Отдельным
предложением — зачем единица месяца тогда остаётся в теле: затем, чтобы деление денег на цену
жило ОДНИМ объявлением (`prorated_days`), а не второй формулой рядом. Отдельным предложением —
ссылка на тест задачи 1 ПО ИМЕНИ: утверждение теперь закреплено машиной, и правка, вводящая
настоящую зависимость от календаря, красит тест, а не проходит молча.

⚠️ Соседний абзац `prorated_expiry` о длине месяца НЕ ТРОГАЕТСЯ: там величина действительно
входит в ответ (`base + timedelta(days=prorated_days(paid, price, month_days))`), и его
утверждение верно. Правка «за компанию» была бы введением четвёртого неверного утверждения.

**Недостижимая ветка.** Ветку `if base <= now_utc: return now_utc` снять и на её месте
оставить утверждение о том, что зажим к сегодняшнему дню есть работа `countdown_age` —
`countdown_base`, — и что второе правило истечения рядом с настоящим было бы ровно той
дублирующей конструкцией, ради устранения которой модуль заведён. Ответ функции при
отсутствующем и истёкшем `current` при этом не меняется — он приходит из `countdown_base`, и
это закреплено двумя тестами задачи 1. Если снятие ветки меняет ответ хоть в одном тесте,
ветка НЕ была недостижимой, и тогда правится не она, а `countdown_base`: об этом сказать в
SUMMARY прямо, а не подгонять тест.

**Усечение неполного дня.** Одной фразой в абзаце о нижней границе назвать: `prorated_days`
берёт ЦЕЛУЮ часть, поэтому при равных ценах ответ равен сегодняшнему дню плюс целое число
дней остатка, и до суток уже оплаченного времени теряется; обещание «оплаченный остаток не
сгорает» верно в днях и слегка неверно в последнем дне — это цена того, что срок хранится
днями, а не решение о деньгах. Фраза ставится ровно там, где обещание объявлено, а не в конце
докстринга: следующий читатель читает обещание, а не сноску.

**Ничего больше в файле не правится.** Ни `next_expiry`, ни `subscription_is_live`, ни
функция границы, заведённая планом `05-22`, этой задачей не трогаются.
  </action>
  <verify>
    <automated>uv run pytest tests/test_application/test_subscription_period.py tests/test_pages/test_billing_payment_errors.py tests/test_services/test_payment_concurrency.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Прогон `<verify>` завершается кодом `0`; ни одно число не изменилось — регрессия горизонта (`test_a_prepaid_horizon_is_not_converted_to_the_senior_plan_for_one_month`) и регрессия остатка в 25 дней зелёные с прежними ожиданиями
    - Утверждение о константе 30 осталось РОВНО ОДНО в файле, и оно принадлежит `prorated_expiry`, где оно верно: `grep -c "КОНСТАНТОЙ 30" app/application/billing/subscription_period.py` возвращает `1`. До правки значение было `2`
    - Абзац называет закрепляющий тест по имени: `grep -c "test_the_conversion_does_not_depend\|test_.*does_not_depend_on_the_length" app/application/billing/subscription_period.py` возвращает не менее `1`, и это имя существует в суите — `uv run pytest tests/test_application/test_subscription_period.py -q -k "<имя>"` собирает ровно один тест
    - Недостижимая ветка снята: число вхождений `return now_utc` в теле `converted_remainder` равно `0` — проверяется `git diff -- app/application/billing/subscription_period.py` и цитируется в SUMMARY
    - Поведение не изменилось на границах: три теста задачи 1 об отсутствующем, истёкшем и живом `current` зелёные без единой правки их ожиданий (`git diff -- tests/test_application/test_subscription_period.py` пуст в этой задаче)
    - Усечение названо на месте обещания: в SUMMARY приведена цитата абзаца с усечением и указан номер строки
    - Арифметика не тронута: `git diff -U0 -- app/application/billing/subscription_period.py | grep -c "^[+-].*prorated_days(\|^[+-].*Decimal("` возвращает `0`
    - `float` не заведён: `grep -c "float(" app/application/billing/subscription_period.py` даёт `0`
    - Полная суита зелёная: `uv run pytest tests/ -q` — код `0`
  </acceptance_criteria>
  <done>Абзац о длине месяца утверждает то, что функция делает, и утверждение закреплено тестом по имени; недостижимая ветка не читается больше вторым правилом истечения; усечение неполного дня названо там, где объявлено обещание. Ни одно число не изменилось.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| докстринг функции денежного пути → следующий читатель | Объявление, спорящее с телом, чинится «в неверную сторону»: читатель верит абзацу и правит арифметику денег |
| мёртвое вычисление рядом с живым → правка | `add_one_month(now_utc)` и обратный ход через `timedelta` выглядят несущими и приглашают их «использовать» |
| недостижимая ветка → правило истечения | Второе правило рядом с настоящим: правка одного из двух расходится молча |
| интеграционный тест с допуском ±2 дня → арифметика денег | Допуск скрывает усечение дня, расхождение февраля и разницу naive/aware — то есть ровно те дефекты, ради которых модуль вынесен из БД |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-145 | Tampering | Следующий читатель «приводит в соответствие» арифметику `converted_remainder` с ложным абзацем о длине месяца и меняет величину, которую наблюдает покупатель | high | mitigate | Задача 1: свойство закреплено тестом на трёх длинах месяца ДО правки абзаца; задача 2: абзац называет тест по имени, поэтому правка красит его, а не проходит молча |
| T-05-146 | Repudiation | Единственная новая арифметика денежного пути остаётся без прямых тестов, и следующая правка проверяется только допуском ±2 дня | high | mitigate | Задача 1: таблица решений той же формы, что у `prorated_expiry`, включая нижнюю границу, naive/aware и равные цены |
| T-05-147 | Tampering | Правка объявления незаметно меняет ПОВЕДЕНИЕ: снятие ветки или перестановка вычислений сдвигает выданные дни | high | mitigate | Задача 2: критерии требуют неизменности чисел 110 и 7 и нуля изменённых ожиданий в тестах; правка арифметики запрещена критерием на `git diff` |
| T-05-148 | Repudiation | Обещание «остаток не сгорает» остаётся объявленным шире, чем исполняется: неполный день теряется молча | medium | mitigate | Задача 2: усечение названо числом на месте обещания; задача 1 закрепляет его тестом равных цен |
| T-05-149 | Tampering | Правка задевает соседний абзац `prorated_expiry`, где утверждение о длине месяца ВЕРНО, и вводит четвёртое неверное утверждение | medium | mitigate | Задача 2: соседний абзац назван неприкосновенным явно; критерий приёмки требует ровно одного оставшегося вхождения и называет, кому оно принадлежит |
| T-05-150 | Information Disclosure | Тестовые данные вносят настоящие суммы или идентификаторы пользователей в файл модульных тестов | low | accept | Раздел оперирует только прейскурантом умолчаний конфига и датами; персональных данных в модуле нет по построению — он не знает ни про БД, ни про пользователя |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов: `git diff --name-only -- pyproject.toml uv.lock` пуст. Аудит легитимности — `05-RESEARCH.md`, `## Package Legitimacy Audit` |
</threat_model>

## Artifacts this phase produces

Перечень относится к плану `05-23`.

**Файлы (новые):** нет

**Имена тестов (новые), `tests/test_application/test_subscription_period.py`** — семь тестов
раздела таблицы решений `converted_remainder`:
- отсутствующий `current` даёт сегодня
- истёкший `current` даёт сегодня
- живой остаток никогда не конвертируется в ноль дней
- naive и aware дают один ответ
- равные цены возвращают остаток в целых днях
- ответ не зависит от длины календарного месяца (28 / 31 / 30)
- понижение увеличивает остаток — симметрия аппарата

**Функции, константы, ключи журнала, колонки БД, Alembic revision id, маршруты, шаблоны,
CSS-селекторы, переменные окружения:** новых нет. `converted_remainder`, `countdown_base`,
`prorated_days`, `PRICE_PRO` и `PRICE_BASIC` — СУЩЕСТВУЮЩИЕ символы, план их только читает и
переиспользует

**Правки объявлений (не новые символы):** абзац о длине месяца в `converted_remainder`; фраза
об усечении неполного дня; снятие недостижимой ветки

<verification>
- Единственная новая арифметика денежного пути покрыта прямыми модульными тестами
- Абзац о длине месяца совпадает с поведением, и совпадение проверяется машиной
- Недостижимая ветка не читается больше вторым правилом истечения
- Усечение неполного дня названо там, где объявлено обещание
- Ни одно число не изменилось: 110 дней при остатке 365, 7 дней при остатке 25
- Полная суита зелёная; ревизий Alembic не заведено, пакеты не установлены

**Перенесённые человеческие проверки — задачами НЕ становятся:**
- Мобильная ширина 375px — план не трогает разметки
- Настоящий платёж в тестовом магазине ЮKassa
- Три judgment-tier прохибиции фазы остаются `unresolved`
</verification>

<success_criteria>
- Объявление функции переноса и её поведение больше не спорят, и спор невозможно восстановить
  молча — его ловит тест
- Мёртвое вычисление и недостижимая ветка перестали приглашать к правке в неверную сторону
- Арифметика денег, которой владеет модуль, проверяется без поднятия БД, на краевых днях
  календаря, как и вся остальная арифметика этого модуля
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-23-SUMMARY.md` when done
</output>
