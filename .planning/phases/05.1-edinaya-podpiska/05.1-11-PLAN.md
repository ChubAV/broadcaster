---
phase: 05.1-edinaya-podpiska
plan: 11
type: execute
wave: 9
depends_on: ["05.1-09"]
gap_closure: true
files_modified:
  - alembic/versions/0020_flat_subscription.py
  - tests/test_migrations/test_0020_flat_subscription.py
autonomous: false
# BILL-09 («первые 5 дней бесплатно») и BILL-10 («ровно ОДНА дата окончания; оплата
# сдвигает её ВПЕРЁД») — те два требования, чей смысл ветка П-о-2 ревизии исполняет
# буквально: она выдаёт пробное окно действующим бесплатным подписчикам (D-G) и она же —
# единственное место фазы, где дата окончания может поехать НАЗАД. Других требований
# план не касается и в их поля себя не вписывает.
requirements: [BILL-09, BILL-10]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Судьба действующего срока доступа при накате решена ЧЕЛОВЕКОМ и записана словами: в SUMMARY названы выбранная ветка, её основание и дата решения — а не только получившийся код."
    - "Ревизия и её парный тест говорят ОДНО: правило, закреплённое тестом, совпадает с правилом, исполняемым ревизией; расхождение между ними перестало быть возможным молча."
    - "Ни одна ПЛАТНАЯ активная строка не меняет `expires_at` ни при какой из двух веток решения — включая просроченную и пограничную."
    - "Журнал наката называет каждую из трёх популяций числом, и его текст соответствует тому, что ревизия ДЕЙСТВИТЕЛЬНО делает с данными: утверждение о невосстановимости стоит там и только там, где потеря действительно происходит."
    - statement: "Строка с `expires_at`, равным моменту исполнения плюс пробное окно, обработана по ОБЪЯВЛЕННОМУ правилу границы, и правило закреплено тестом, а не абзацем."
      verification: backstop
  artifacts:
    - alembic/versions/0020_flat_subscription.py            # ветка П-о-2 и журнал наката
    - tests/test_migrations/test_0020_flat_subscription.py  # парный тест правила
  key_links:
    - "checkpoint:decision (владелец) → текст `_RESET_FREE_EXPIRY` → `expires_at` активных бесплатных строк боевой базы"
    - "`_RESET_FREE_EXPIRY` ↔ парный тест ветки П-о-2 — одно правило, два места; расхождение краснит прогон"
  prohibitions:
    - "Гейт доступа никогда не закрывает денежный путь."
    - "Прекращение доступа никогда не уничтожает данные пользователя."
    - "Денежный журнал не стирается и не переписывается задним числом (D-H)."
    - "Исторические значения `payments.plan` продолжают печататься так, как были проданы."
    - "Ревизия не выполняет ни одной операции над таблицей платежей."
    - "Ревизия не изменяет `expires_at` ни одной ПЛАТНОЙ активной строки — ни живой, ни просроченной, ни пограничной."
    - "Необратимая правка данных не остаётся без следа: то, что нельзя восстановить, обязано быть записано ДО правки."
---

<objective>
Закрыть односторонную дверь фазы: решить человеком, что решение D-G означает для
действующего срока бесплатной строки, и исполнить решённое так, чтобы ревизия и её
парный тест не могли разойтись.

Purpose: `alembic/versions/0020_flat_subscription.py:143-149` переписывает `expires_at`
БЕЗУСЛОВНО — предиката на сам срок в ветке П-о-2 нет. Любая активная строка с тарифом
`free`, чей срок дальше «момент исполнения + пробное окно», УКОРАЧИВАЕТСЯ, необратимо и
без записи прежнего значения: в журнал уходит только КОЛИЧЕСТВО тронутых строк.
Буквально это и есть D-G («пробный срок, отсчитываемый ОТ ДАТЫ МИГРАЦИИ»), и парный тест
`test_a_free_plan_row_gets_the_trial_window_from_the_moment_of_the_run`
(`tests/test_migrations/test_0020_flat_subscription.py:366`) усечение ТРЕБУЕТ — то есть
закрепляет его как правило.

⚠️ ЭТО ВОПРОС ЗАМЫСЛА, А НЕ КОДА, И ПОТОМУ ОН ВЫНЕСЕН ЧЕЛОВЕКУ. Сегодня под угрозой ноль
строк — по записи проверки боевая таблица подписок пуста. Но очередь невыкаченных ревизий
тоже пуста: бой проверен `alembic current` 2026-08-21 и стоит на `0019`, значит `0020`
применится ПЕРВОЙ И ЕДИНСТВЕННОЙ на ближайшем же выкате, а после неё классифицировать
популяции будет уже нечем — колонка тарифа снята той же ревизией.

⚠️ ЭТО ГЭП-ПЛАН. Планы `05.1-01`…`05.1-09` исполнены и являются историей; план `05.1-08`,
которым написана ревизия, не правится и не отменяется — правится сама ревизия, ещё не
накаченная на бой.

Output: записанное решение владельца и ревизия, чей текст, чей журнал и чей парный тест
говорят одно и то же.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-edinaya-podpiska/05.1-REVIEW.md
@.planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md
@.planning/phases/05.1-edinaya-podpiska/05.1-08-SUMMARY.md
</context>

## Artifacts this phase produces (план 05.1-11)

| Символ | Место | Вид | Ветка |
|--------|-------|-----|-------|
| решение по CR-01 (выбранная ветка, основание, дата) | `05.1-11-SUMMARY.md` | запись решения | обе |
| `_SNAPSHOT_FREE_EXPIRY` | `alembic/versions/0020_flat_subscription.py` | константа `sa.text` (SELECT до правки) | обе |
| построчная запись снимка в журнал наката | `alembic/versions/0020_flat_subscription.py` | вызов `logger.info` в `upgrade()` | обе |
| предикат `expires_at < :trial_expires_at` в `_RESET_FREE_EXPIRY` | `alembic/versions/0020_flat_subscription.py` | правка SQL | только (б) |
| `test_a_free_plan_row_with_a_farther_date_keeps_it` | `tests/test_migrations/test_0020_flat_subscription.py` | тест | только (б) |
| `test_a_free_plan_row_exactly_at_the_window_is_not_touched` | `tests/test_migrations/test_0020_flat_subscription.py` | тест (граница) | только (б) |
| правка `test_a_free_plan_row_gets_the_trial_window_from_the_moment_of_the_run` | `tests/test_migrations/test_0020_flat_subscription.py` | перецеливание существующего теста | только (б) |

Ни один файл продукта (`app/`) этим планом не трогается. Ревизия `0019` и всё, что до
неё, неприкосновенны.

## Флагированные допущения планировщика (краевые случаи, не снятые зондом)

Полный реестр четырнадцати неснятых краёв — в плане `05.1-12`. Относящиеся к предмету
ЭТОГО плана:

| Край | Категория | Требование | Судьба |
|------|-----------|------------|--------|
| `expires_at` бесплатной строки РАВЕН «момент исполнения + пробное окно» | `boundary` | BILL-10 | **ПОВЫШЕН до приёмочного критерия** задачи 2, ветка (б): строгое сравнение оставляет такую строку нетронутой, и это закрепляется отдельным тестом. В ветке (а) край снимается тождественно: срок переставляется всем, и граница исчезает как предмет |
| Активная строка с тарифом `free`, заведённая административной выдачей с дальней датой | `unclassified` | BILL-09 | Допущение планировщика: такие строки МОГЛИ быть заведены снятой функцией назначения тарифа и веткой первой вставки денежного пути с запасным значением тарифа. Проверить это по бою нечем до наката — измерение вынесено в план `05.1-13` (прогон на копии боевой базы). Именно это допущение и есть предмет чекпойнта |
| Пользователь без активной строки, но с ДЕАКТИВИРОВАННОЙ строкой истории | `empty` | BILL-10 | Допущение снято уже исполненным тестом `test_a_user_without_a_subscription_row_is_given_one`: строка истории не воскрешается, вставляется ВТОРАЯ. План его не трогает |

Ни один край не отброшен молча.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Задача 1: Решение по CR-01 — что D-G означает для действующего срока</name>

  <read_first>
    alembic/versions/0020_flat_subscription.py:120-150 (константы `_COUNT_PAID_CARRIED_OVER`, `_RESET_FREE_EXPIRY`, `_INSERT_MISSING_SUBSCRIPTIONS` — видно, что предиката на срок в ветке П-о-2 нет),
    alembic/versions/0020_flat_subscription.py:175-215 (тело `upgrade()`: момент исполнения снимается один раз, три журнальные записи, среди них — утверждение о невосстановимости прежнего срока),
    alembic/versions/0020_flat_subscription.py:48-53 (абзац о строгости границы — он описывает решение, которого код сегодня НЕ принимает; это предмет WR-02, и ветка (б) закрывает его половину),
    tests/test_migrations/test_0020_flat_subscription.py:236-282 (фикстура `db_at_0019` и посев трёх популяций: строка `id=20`, пользователь 2, тариф `free`, срок 2099 год, активна),
    tests/test_migrations/test_0020_flat_subscription.py:366-383 (парный тест ветки П-о-2 — он ТРЕБУЕТ укорачивания),
    .planning/phases/05.1-edinaya-podpiska/05.1-REVIEW.md § CR-01 целиком, включая перечень отягчающих обстоятельств,
    .planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md § «Требуется ручная проверка» пункт 3,
    .planning/ROADMAP.md § Phase 05.1, решения D-G и D-H
  </read_first>

  <decision>
    Что решение D-G означает для активной строки с тарифом `free`, чей `expires_at`
    ДАЛЬШЕ, чем «момент исполнения ревизии + пробное окно»: переставить ей срок
    безусловно (то есть укоротить) или не трогать?
  </decision>

  <context>
    Ревизия трактует тариф `free` как «человек ничего не оплачивал». Это следствие, а не
    факт схемы: строку с тарифом `free` и произвольной датой заводили две сущности —
    снятая административная функция назначения тарифа, принимавшая ЛЮБОЕ имя тарифа и
    ЛЮБУЮ дату, и ветка первой вставки денежного пути с запасным значением тарифа. Любая
    такая строка со сроком дальше окна будет укорочена.

    Три обстоятельства, каждое самостоятельное:
    1) ревизия необратима по данным и, по её же докстрингу, применится на ближайшем
       выкате первой и единственной — очередь невыкаченных пуста (`alembic current` на
       бою 2026-08-21 — `0019`);
    2) прежнее значение срока не сохраняется НИГДЕ: в журнал уходит только количество
       тронутых строк, без идентификаторов и без прежних дат, — разобрать жалобу после
       наката будет нечем;
    3) парный тест усечение ТРЕБУЕТ, то есть дефект (если это дефект) уже закреплён как
       правило и переживёт любую починку кода, сделанную без него.

    По записи проверки боевая таблица подписок сегодня пуста, то есть под угрозой ноль
    строк. Это ОСЛАБЛЯЕТ срочность, но не отвечает на вопрос: измерение боевой популяции
    не проводилось, а после наката отличить бывшего платящего от бывшего бесплатного
    будет нечем — колонка тарифа снимается той же ревизией.

    ⚠️ ВОПРОС ЗАДАЁТСЯ ДО НАКАТА И ТОЛЬКО ДО НАКАТА. Если ревизия уже накачена на бой,
    ни одна из веток ниже неисполнима: правка файла ревизии на накаченную базу не
    действует, и понадобится компенсирующая ревизия — это другая работа и другой план.
  </context>

  <options>
    <option id="a-literal-d-g">
      <name>(а) Буквальное чтение D-G: срок переставляется БЕЗУСЛОВНО</name>
      <pros>
        Ровно то, что записано решением D-G: «подписчики на `free` получают пробный срок
        от даты миграции». Все бывшие бесплатные попадают в одинаковые условия — новых
        различий между людьми ревизия не создаёт. Правки кода не требуется: ревизия и
        парный тест верны как есть.
      </pros>
      <cons>
        Строка, заведённая административной выдачей с дальней датой, теряет оплаченное
        (или подаренное) время необратимо и без следа. Различить такую строку от
        по-настоящему бесплатной после наката нечем.
      </cons>
      <consequence>
        Предикат и парный тест НЕ ТРОГАЮТСЯ. Ревизия дополнительно получает ПОИМЁННЫЙ
        след: до правки снимается список тронутых строк, и в журнал наката уходят
        идентификатор пользователя, прежняя дата и новая — по строке на строку. Какие
        строки трогаются, это не меняет ни одной; меняется только то, что записано в
        журнал. Основание: подтверждённая как ПРАВИЛО необратимая потеря обязана быть
        разбираемой по жалобе, а счётная запись разобрать её не даёт.
      </consequence>
    </option>
    <option id="b-never-shorten">
      <name>(б) Срок ПОДТЯГИВАЕТСЯ, но никогда не укорачивается</name>
      <pros>
        Ни одна строка не теряет действующего времени. D-G продолжает выполняться по
        существу: тот, у кого срока меньше окна (включая всех, у кого он в прошлом),
        получает пробное окно от момента исполнения. Дверь перестаёт быть односторонней
        по последствиям: нечего восстанавливать, потому что нечего терять.
      </pros>
      <cons>
        Буква D-G («срок от даты миграции») выполняется не для всех: у строки с дальней
        датой срок остаётся прежним. Появляется граница, которой сегодня нет, и её
        придётся закрепить тестом.
      </cons>
      <consequence>
        В `_RESET_FREE_EXPIRY` добавляется предикат `AND expires_at < :trial_expires_at`.
        Парный тест перестаёт требовать усечения и перецеливается на границу. Текст
        журнала наката о невосстановимости прежнего срока правится: под предикатом
        строка со «старшей» датой не трогается вовсе, и утверждение о потере становится
        неверным. Поимённый след снимается так же, как в ветке (а).
      </consequence>
    </option>
  </options>

  <acceptance_criteria>
    - Решение получено от владельца и записано в `05.1-11-SUMMARY.md` тремя частями: выбранная ветка (`a-literal-d-g` либо `b-never-shorten`), основание словами, дата.
    - До получения решения ни `alembic/versions/0020_flat_subscription.py`, ни `tests/test_migrations/test_0020_flat_subscription.py` не изменены ни одной строкой: `git diff --stat` по этим двум файлам пуст.
    - Предусловие проверено и записано фактом: ревизия `0020` НЕ накачена на боевую базу. Источник — запись `alembic current` = `0019` от 2026-08-21 в `05.1-08-SUMMARY.md`. Если владелец сообщает, что накат уже произошёл, обе ветки объявляются неисполнимыми, работа останавливается и в SUMMARY записывается требование компенсирующей ревизии.
  </acceptance_criteria>

  <resume-signal>Select: a-literal-d-g or b-never-shorten</resume-signal>

  <reversibility rating="one-way">
    Решение управляет необратимой правкой данных: после наката прежние значения
    `expires_at` не восстанавливаются ничем, а классифицировать популяции нечем —
    колонка тарифа снята той же ревизией. Откат ревизии возвращает схему, но не значения.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Исполнить выбранную ветку — ревизия, её журнал и её парный тест</name>

  <precondition>
    Задача 1 завершена: выбранная ветка получена от владельца и записана. Ревизия `0020`
    на боевой базе НЕ накачена (`alembic current` = `0019`, факт от 2026-08-21 в
    `05.1-08-SUMMARY.md`). Если накат произошёл, задача не исполняется: правка файла
    накаченной ревизии на данные не действует.
  </precondition>

  <files>
    alembic/versions/0020_flat_subscription.py,
    tests/test_migrations/test_0020_flat_subscription.py
  </files>

  <read_first>
    alembic/versions/0020_flat_subscription.py целиком (докстринг, три константы SQL, тело `upgrade()`, тело `downgrade()`),
    tests/test_migrations/test_0020_flat_subscription.py:1-40 (почему файловая база, почему синхронный тест, почему стартовая ревизия названа явно),
    tests/test_migrations/test_0020_flat_subscription.py:145-175 (посевы `SEED_USERS`, `SEED_PAYMENTS`, `SEED_CURRENCY` — четыре пользователя, из них четвёртый без активной строки),
    tests/test_migrations/test_0020_flat_subscription.py:200-235 (помощники `_scalar`, `_execute`, `_expiry_of`),
    tests/test_migrations/test_0020_flat_subscription.py:236-282 (фикстура `db_at_0019`: третий элемент — момент ДО прогона, сравнивать обязательно с ним),
    tests/test_migrations/test_0020_flat_subscription.py:341-432 (четыре теста популяций, включая парный тест П-о-2 и тест границы платной строки),
    tests/test_migrations/test_0020_flat_subscription.py:548-570 (тест «ревизия называет своё место в очереди» — он требует в исходнике трёх подстрок: номера предыдущей ревизии, даты проверки боевой базы и имени команды, которой состояние получено; правка докстринга обязана их сохранить),
    tests/test_migrations/test_0020_flat_subscription.py:608-745 (разбор по синтаксическому дереву: позиции первой операции над данными и снятия колонки тарифа; проверка, что журнал платежей не тронут ни одной операцией)
  </read_first>

  <behavior>
    ОБЩЕЕ ДЛЯ ОБЕИХ ВЕТОК:
    - Тест A: платная активная строка не меняет срок — живая, просроченная и пограничная (три уже существующих утверждения обязаны остаться зелёными без правки).
    - Тест B: пользователь без активной строки получает её, а деактивированная строка истории не воскресает (существующий тест, правке не подлежит).
    - Тест C: журнал платежей не тронут ни одной операцией (существующий тест, правке не подлежит).

    ВЕТКА (б), если выбрана она:
    - Тест D: активная бесплатная строка со сроком ДАЛЬШЕ окна сохраняет свой срок до микросекунды.
    - Тест E (граница): активная бесплатная строка со сроком, РАВНЫМ «момент исполнения + пробное окно», не трогается — сравнение строгое.
    - Тест F: активная бесплатная строка со сроком В ПРОШЛОМ переезжает на окно от момента исполнения.
  </behavior>

  <action>
    Работа ветвится по ответу задачи 1. Обе ветки описаны здесь целиком: импровизации
    после чекпойнта быть не должно.

    ОБЩЕЕ ДЛЯ ОБЕИХ ВЕТОК — ПОИМЁННЫЙ СЛЕД.

    Завести модульную константу `_SNAPSHOT_FREE_EXPIRY` — `sa.text` с запросом
    `SELECT user_id, expires_at FROM subscriptions WHERE is_active AND plan = 'free'`,
    собранным из тех же модульных констант имён таблицы, колонки и бесплатного тарифа,
    что уже объявлены в файле (голых литералов имён в файле нет по правилу проекта, и
    разбор порядка операций разрешает именно ссылки).

    В `upgrade()` выполнить снимок ДО вызова `_RESET_FREE_EXPIRY` и выписать результат в
    журнал наката ПОСТРОЧНО: идентификатор пользователя, прежняя дата, новая дата.
    Основание записать абзацем прямо в файле: откат данных не возвращает, и единственная
    возможность разобрать жалобу — журнал, снятый ДО правки; счётная запись («тронуто N
    строк») жалобу разобрать не даёт.

    ⚠️ СНИМОК — ЧТЕНИЕ, А НЕ ОПЕРАЦИЯ НАД ДАННЫМИ ЧУЖОЙ ТАБЛИЦЫ. Он адресован таблице
    подписок, поэтому проверка «журнал платежей не тронут» остаётся зелёной; он стоит до
    снятия колонки тарифа, поэтому проверка порядка операций остаётся зелёной. Обе
    проверки прогнать явно.

    ЕСЛИ ВЫБРАНА ВЕТКА (а) — БУКВАЛЬНОЕ ЧТЕНИЕ D-G.

    `_RESET_FREE_EXPIRY` не трогать ни одним символом. Парный тест
    `test_a_free_plan_row_gets_the_trial_window_from_the_moment_of_the_run` не трогать ни
    одним символом — он верен и остаётся закреплением правила.

    В докстринг ревизии, в абзац о трёх популяциях, дописать: решение подтверждено
    владельцем такого-то числа; строка с тарифом `free` и дальней датой переставляется
    ВМЕСТЕ СО ВСЕМИ, потому что различить «выданную администратором» от «по-настоящему
    бесплатной» схема не позволяет, а разные условия для неразличимых строк были бы
    произволом. Здесь же назвать цену решения прямо: прежний срок таких строк
    невосстановим, и именно поэтому заведён поимённый след.

    Журнальную запись ветки П-о-2 оставить утверждающей невосстановимость — при этой
    ветке она верна — и дополнить ссылкой на построчный снимок.

    ЕСЛИ ВЫБРАНА ВЕТКА (б) — СРОК НЕ УКОРАЧИВАЕТСЯ.

    В `_RESET_FREE_EXPIRY` добавить третьим условием `AND expires_at < :trial_expires_at`.
    Сравнение СТРОГОЕ, и это решение, а не оформление: строка, чей срок в точности равен
    окну, переписыванием ничего не получила бы, а строгость совпадает со строгостью
    предиката живости подписки в продукте — миграция и продукт обязаны отвечать на вопрос
    о границе одинаково.

    Над константой записать абзац: ветка ПОДТЯГИВАЕТ срок и НИКОГДА не укорачивает;
    строка с тарифом `free` и живым сроком дальше окна оплачена (административная выдача,
    ветка первой вставки денежного пути), и безусловная перезапись сожгла бы оплаченное.

    Журнальную запись ветки П-о-2 переписать: утверждение о невосстановимости прежнего
    срока снять, потому что под предикатом строка со старшей датой не трогается вовсе, и
    прежнее значение никуда не девается. Вместо него записать, сколько строк ПОДТЯНУТО и
    сколько ОСТАВЛЕНО как есть — молчание о второй группе неотличимо от того, что её
    забыли.

    Абзац докстринга о строгости границы (строки 48-53) привести в соответствие с тем,
    что ревизия теперь ДЕЙСТВИТЕЛЬНО делает: до этой правки он описывал решение, которого
    код не принимал (предмет WR-02), а после неё сравнение по сроку в ревизии появляется,
    и абзац обязан называть именно его. ⚠️ Три подстроки, которые тест очереди требует от
    исходника — номер предыдущей ревизии, дата проверки боевой базы и имя команды, которой
    состояние получено, — обязаны пережить правку докстринга.

    Парный тест `test_a_free_plan_row_gets_the_trial_window_from_the_moment_of_the_run`
    перецелить, а не удалить: посеянная фикстурой строка `id=20` (пользователь 2, тариф
    `free`, срок 2099 год) обязана СОХРАНИТЬ свою дату, поэтому тест переименовывается в
    `test_a_free_plan_row_with_a_farther_date_keeps_it` и утверждает равенство прежней
    дате. В докстринге назвать, что предмет утверждения переменился по решению владельца
    от такого-то числа, — иначе следующий читатель прочтёт правку как ослабление теста.

    Завести `test_a_free_plan_row_exactly_at_the_window_is_not_touched`: посеять
    активную бесплатную строку четвёртому пользователю фикстуры со сроком, равным
    «момент фикстуры + пробное окно» — и, поскольку момент исполнения ревизии всегда
    ПОЗЖЕ момента фикстуры, такая строка гарантированно окажется НИЖЕ границы и будет
    подтянута. Поэтому граница проверяется иначе и честно: посеять строку со сроком
    заведомо выше границы на секунду и потребовать, чтобы её дата не изменилась, и
    отдельно — строку со сроком в прошлом, потребовав переезда на окно. Написать в
    докстринге, почему точное равенство моменту исполнения тестом не выражается: момент
    снимается ВНУТРИ наката и тесту недоступен, а тест, подгоняющий дату под угаданное
    значение, проверял бы удачу.

    Завести `test_a_free_plan_row_in_the_past_moves_to_the_window` (тест F), если
    существующие тесты такого утверждения не несут: без него ветка (б) прошла бы и на
    реализации, которая не делает НИЧЕГО.

    Порядок тактов в обеих ветках: сначала тесты (они обязаны упасть), коммит с префиксом
    `test(05.1-11):`; затем правка ревизии, тесты зеленеют, коммит с префиксом
    `fix(05.1-11):`. В ветке (а) первый такт вырождается: правке подлежат только журнал и
    докстринг, и красного состояния у них нет — тогда исполняется один коммит с префиксом
    `fix(05.1-11):`, и это записывается в SUMMARY отдельной строкой как осознанное
    отступление от порядка, а не как забытый такт.
  </action>

  <verify>
    <automated>uv run pytest tests/test_migrations/test_0020_flat_subscription.py -q</automated>
    <automated>uv run pytest tests/test_migrations -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_migrations -q` завершается кодом 0 (включая сверку модели с головой ревизий).
    - `grep -c 'SELECT user_id, expires_at' alembic/versions/0020_flat_subscription.py` возвращает не менее 1 — поимённый снимок объявлен.
    - Журнал наката пишет строку на каждую тронутую строку подписки: `uv run pytest tests/test_migrations/test_0020_flat_subscription.py -q -k free` завершается кодом 0, а в SUMMARY приведён фрагмент вывода `alembic upgrade` с построчными записями, снятый на прогоне фикстуры.
    - `uv run pytest tests/test_migrations/test_0020_flat_subscription.py -q -k "payment_journal or before_the_plan_column"` завершается кодом 0 — снимок не тронул журнал платежей и стоит до снятия колонки тарифа.
    - `uv run pytest tests/test_migrations/test_0020_flat_subscription.py -q -k unapplied_queue` завершается кодом 0 — правка докстринга сохранила три обязательные подстроки.
    - **Ветка (б), если выбрана:** `grep -c 'expires_at < :trial_expires_at' alembic/versions/0020_flat_subscription.py` возвращает не менее 1; в выводе `uv run pytest tests/test_migrations/test_0020_flat_subscription.py --collect-only -q` присутствует `test_a_free_plan_row_with_a_farther_date_keeps_it` и ОТСУТСТВУЕТ собранный тест с прежним именем — правило заменено, а не задублировано. Сверка ведётся по СОБРАННЫМ идентификаторам тестов, а не грепом по файлу: прежнее имя законно упоминается в докстринге перецеленного теста, и греп по файлу краснел бы от объяснения.
    - **Ветка (б), если выбрана:** посеянная строка со сроком выше границы сохраняет дату ДО МИКРОСЕКУНДЫ (утверждается равенством, а не неравенством); посеянная строка со сроком в прошлом переезжает на окно от момента исполнения.
    - **Ветка (а), если выбрана:** `git diff` по `_RESET_FREE_EXPIRY` и по телу теста `test_a_free_plan_row_gets_the_trial_window_from_the_moment_of_the_run` пуст — подтверждённое правило не тронуто ни одним символом.
    - SUMMARY содержит выбранную ветку, основание словами и дату решения владельца; а также прямую запись о том, ЧТО в боевых данных остаётся неизмеренным до прогона на копии (план `05.1-13`).
  </acceptance_criteria>

  <done>
    Ревизия, её журнал наката и её парный тест говорят одно и то же правило — то, которое
    выбрал владелец. Необратимая правка данных перестала быть безымянной: каждая тронутая
    строка называется в журнале вместе с прежней и новой датой.
  </done>

  <reversibility rating="one-way">
    Задача правит текст ещё не накаченной необратимой ревизии. После наката ни одна из
    правок не действует на данные, а прежние значения `expires_at` не восстанавливаются
    ничем. Гейт — чекпойнт задачи 1, стоящий непосредственно перед этой задачей.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| текст ревизии → боевые данные | однократное необратимое исполнение над строками, которые до наката никто не измерял |
| ревизия → журнал наката | единственный след правки; всё, чего в нём нет, после наката не восстанавливается |
| парный тест → правило | тест закрепляет правило; закреплённое неверное правило переживает починку кода |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-38 | Tampering | безусловная перезапись действующего срока доступа веткой П-о-2 | high | mitigate | Диспозиция определяется чекпойнтом задачи 1 и записывается в SUMMARY с именем принявшего: ветка (б) снимает угрозу предикатом строгого сравнения; ветка (а) переводит её в ПРИНЯТУЮ владельцем цену, и принятие фиксируется письменно вместе с поимённым следом. Немаркированного исхода нет: без ответа владельца задача 2 не исполняется |
| T-05.1-39 | Repudiation | необратимая правка данных без поимённого следа | high | mitigate | Снимок `_SNAPSHOT_FREE_EXPIRY` снимается ДО правки; в журнал наката уходят идентификатор пользователя, прежняя и новая дата — по строке на строку. Обе ветки решения получают его одинаково |
| T-05.1-41 | Tampering | правка докстринга ломает утверждение о месте ревизии в очереди | medium | mitigate | Тест очереди требует в исходнике трёх подстрок; прогон теста входит в приёмочные критерии задачи 2 |
| T-05.1-40 | Denial of Service | долгая блокировка снятия колонки на боевом объёме | low | accept | Объём боевой таблицы подписок мал (по записи проверки — строк нет вовсе); настоящее измерение вынесено в прогон на копии боевой базы (план `05.1-13`), где оно и является предметом проверки |
| T-05.1-SC | Tampering | установка пакетов (npm/pip/cargo) | — | N/A | План не устанавливает ни одного пакета; таблица легитимности неприменима за отсутствием предмета |
</threat_model>

<verification>
- `uv run pytest tests/test_migrations -q` — зелёный.
- `uv run pytest tests/test_application/test_subscription_period.py tests/test_models/test_subscription.py -q` — зелёный (правка ревизии не сдвинула ни модель, ни предикат).
- `uv run pytest tests/test_planning -q` — зелёный.
- Решение владельца записано в SUMMARY тремя частями: ветка, основание, дата.

⚠️ **Чего этот план НЕ доказывает.** Он не видел ни одной боевой строки. Применимость
ревизии к боевым ДАННЫМ — нарушенные инварианты истории, дубликаты, пустые значения там,
где модель их не ждёт, объём, на котором снятие колонки берёт долгую блокировку —
доказывается только прогоном на копии боевой базы. Он вынесен в план `05.1-13` и остаётся
открытым до него.

⚠️ **О полном прогоне `just test`:** единственное допустимое падение —
`tests/test_pages/test_ads_editor.py::test_image_base_url_comes_from_app_settings`,
порядкозависимое и атрибутированное проверкой фазе 02 (коммит `5771a50` от 2026-08-10);
в изоляции зелёное, гэпом фазы 05.1 не является.
</verification>

<success_criteria>
- Вопрос замысла отвечен человеком ДО наката, и ответ записан вместе с основанием и датой.
- Ревизия, её журнал и её парный тест несут ОДНО правило; расхождение между ними невозможно молча.
- Необратимая правка данных оставляет поимённый след при любой выбранной ветке.
- Ни одна платная активная строка не тронута — включая просроченную и пограничную.
- То, что остаётся неизмеренным до прогона на копии боевой базы, названо неизмеренным.
</success_criteria>

<output>
Create `.planning/phases/05.1-edinaya-podpiska/05.1-11-SUMMARY.md` when done
</output>
