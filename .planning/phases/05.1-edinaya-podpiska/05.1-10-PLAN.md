---
phase: 05.1-edinaya-podpiska
plan: 10
type: execute
wave: 9
depends_on: ["05.1-09"]
gap_closure: true
files_modified:
  - app/constants.py
  - app/services/payment_service.py
  - tests/test_services/test_payment_service.py
  - tests/test_pages/test_billing_section.py
autonomous: true
# BILL-05 (продление подписки) и BILL-07 (история платежей) — две поверхности ОДНОГО
# значения «за что заплачено». Проверка нашла их разошедшимися: журнал печатает
# «Доступ к системе», а касса — литерал пустого значения. План закрывает расхождение и
# ничего сверх него; отметку `Complete` этим требованиям он НЕ ставит (условие отметки —
# ручная приёмка на 375px, план 05.1-13).
requirements: [BILL-05, BILL-07]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Строка назначения, уезжающая в `YooPayment.create` для подписочного платежа, равна «Доступ к системе» — той же формулировке, которую печатает строка истории платежей при пустом поле тарифа (истина 9 проверки, CR-02)."
    - "У назначения подписочного платежа нет ни одного места вставки: строка не меняется ни при каком значении аргумента тарифа, поэтому снятое поле (D-F) в кассу и в фискальный чек не попадает ни при каком входе."
    - "Свидетель существует и читает ТЕЛО вызова SDK, а не исходник: тест снимает аргумент `YooPayment.create` и утверждает строку назначения дословно. До этого плана греп по `tests/` не находил ни одного утверждения о назначении."
    - "Две поверхности назначения — касса и журнал — связаны исполняемым свидетелем: расхождение формулировок краснит прогон, а не обнаруживается на боевом платеже."
    - "Историческая строка журнала с НЕПУСТЫМ полем тарифа продолжает печатать имя своей эпохи: план не переименовывает ни одного проданного платежа (D-H)."
  artifacts:
    - app/constants.py                            # константа SUBSCRIPTION_DESCRIPTION
    - app/services/payment_service.py             # ветка подписки читает константу
    - tests/test_services/test_payment_service.py # свидетель тела вызова SDK
    - tests/test_pages/test_billing_section.py    # свидетель связи двух поверхностей
  key_links:
    - "app.constants.SUBSCRIPTION_DESCRIPTION → create_payment (ветка KIND_SUBSCRIPTION) → YooPayment.create['description'] → страница оплаты ЮKassa, фискальный чек, кабинет мерчанта"
    - "app.constants.SUBSCRIPTION_DESCRIPTION ↔ литерал ветки пустого тарифа в payment_row.html — связаны ТЕСТОМ, а не соглашением"
  prohibitions:
    - "Гейт доступа никогда не закрывает денежный путь."
    - "Прекращение доступа никогда не уничтожает данные пользователя."
    - "Денежный журнал не стирается и не переписывается задним числом (D-H)."
    - "Исторические значения `payments.plan` продолжают печататься так, как были проданы."
    - "Ни одно значение, пришедшее от пользователя, не попадает в текст назначения платежа, видимый в кассе и в фискальном чеке."
    - "Формулировка назначения не заводится третьей: в проекте её ровно две записи — константа и литерал шаблона, и они связаны тестом."
---

<objective>
Закрыть единственный блокер фазы: подписочный платёж обязан называть свой предмет
словами на ОБЕИХ поверхностях, где его видит человек, — и в журнале платежей, и в кассе
ЮKassa.

Purpose: фаза сняла тариф из модели (D-F), но описание платежа продолжает его читать.
Единственный вызывающий передаёт пустой тариф НАМЕРЕННО (`app/pages/billing.py:352`), а
`app/services/payment_service.py:349` подставляет его в шаблон строки без ветки —
значит дефект не вероятностный, а тождественный: он повторяется на КАЖДОМ платеже новой
модели и печатается покупателю на странице оплаты, в фискальном чеке и в кабинете
мерчанта. Тяжесть не в опечатке: фаза УЖЕ объявила верный ответ на этот самый вопрос и
применила его к соседней поверхности — `payment_row.html:51` при пустом тарифе печатает
«Доступ к системе». Две поверхности одного значения разошлись там, где человек
расстаётся с деньгами, и свидетеля у кассовой поверхности нет вовсе (греп `tests/` — ноль
утверждений о назначении), поэтому регрессия дефект тоже не поймала бы.

⚠️ ЭТО ГЭП-ПЛАН, А НЕ ПРОДОЛЖЕНИЕ ФАЗЫ. Планы `05.1-01`…`05.1-09` исполнены и являются
историей; этот план их не правит, не перенумеровывает и не отменяет. Он закрывает
названный проверкой провал истины 9 и ничего сверх него.

Output: одна формулировка назначения на две поверхности, связанные исполняемым
свидетелем, и тест, читающий тело вызова SDK.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md
@.planning/phases/05.1-edinaya-podpiska/05.1-REVIEW.md
</context>

## Artifacts this phase produces (план 05.1-10)

| Символ | Место | Вид |
|--------|-------|-----|
| `SUBSCRIPTION_DESCRIPTION` | `app/constants.py` | константа модуля (строка `Доступ к системе`) |
| импорт `SUBSCRIPTION_DESCRIPTION` в ветке `KIND_SUBSCRIPTION` | `app/services/payment_service.py` | чтение константы вместо подстановки |
| `SUBSCRIPTION_DESCRIPTION_TEXT` | `tests/test_services/test_payment_service.py` | модульная константа теста (литерал, выписанный дословно) |
| `test_the_subscription_payment_names_its_purpose_in_words` | `tests/test_services/test_payment_service.py` | тест |
| `test_the_purpose_of_a_subscription_payment_is_not_a_substitution` | `tests/test_services/test_payment_service.py` | тест |
| `test_the_subscription_purpose_is_a_name_and_not_an_fstring` | `tests/test_services/test_payment_service.py` | тест (разбор AST) |
| `test_the_two_surfaces_of_the_payment_purpose_speak_one_string` | `tests/test_pages/test_billing_section.py` | тест |
| `PAYMENT_ROW_HTML` | `tests/test_pages/test_billing_section.py` | константа пути к шаблону (по образцу `BALANCE_HTML`, `:55`) |

Ничего не удаляется. Сигнатура `create_payment` не сокращается ни на один параметр:
аргумент тарифа по-прежнему уезжает в ЖУРНАЛЬНУЮ колонку `payments.plan` (D-H), а
снятие пакетной ветки — предмет WR-05, отложенного этим прогоном явно.

## Флагированные допущения планировщика (краевые случаи, не снятые зондом)

Зонд краевых случаев вернул по требованиям фазы **14 применимых краёв, 0 снятых, 14
неснятых**. Полный реестр всех четырнадцати — в плане `05.1-12`, § «Реестр неснятых
краёв». Здесь названы те, что относятся к предмету ЭТОГО плана, и один из них ПОВЫШЕН до
приёмочного критерия, а не оставлен допущением:

| Край | Категория | Требование | Судьба |
|------|-----------|------------|--------|
| Назначение подписочного платежа при ПУСТОМ поле тарифа | `empty` | BILL-05, BILL-07 | **ПОВЫШЕН до приёмочного критерия** задачи 1 (тесты 1 и 2). Это и есть предмет CR-02 |
| Историческая строка журнала с НЕПУСТЫМ полем тарифа рядом с новой | `adjacency` | BILL-07 | Допущение: обе печатаются каждая по-своему, и это закреплено ЖИВЫМ тестом `test_the_purpose_of_a_payment_has_three_sources`, который план не трогает |
| Строка журнала вида «пакет» с пустым именем пакета | `empty` | BILL-07 | Допущение: родовая подпись ветки пакета живёт в шаблоне и планом не трогается; пакетная ветка сервиса отложена как WR-05 |

Ни один край не отброшен молча.

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Назначение подписочного платежа — постоянная строка и её свидетель</name>

  <files>
    app/constants.py,
    app/services/payment_service.py,
    tests/test_services/test_payment_service.py
  </files>

  <read_first>
    app/services/payment_service.py:340-400 (ветка `kind == KIND_SUBSCRIPTION`: сборка назначения и `metadata`, затем тело вызова `YooPayment.create` — видно, каким ключом назначение уезжает наружу),
    app/services/payment_service.py:163-175 (сигнатура `create_payment`: `plan` — keyword-only с умолчанием пустого значения; параметр остаётся, он пишется в журнальную колонку),
    app/pages/billing.py:342-360 (ЕДИНСТВЕННЫЙ вызывающий: пустой тариф передан намеренно, и обоснование записано прямо в вызове),
    app/templates/billing/includes/payment_row.html:44-54 (ЗДЕСЬ уже объявлена верная формулировка — ветка пустого тарифа строки журнала; читать обязательно, отсюда берётся значение константы),
    app/constants.py:18-46 (образец объявления значения, обязанного называться одинаково в нескольких местах: состояния объявления и подписи каналов — там же записано, почему копия литерала разъезжается молча),
    tests/test_services/test_payment_service.py:1-45 (импорты и ПРАВИЛО ПРОЕКТА: литерал в тесте выписывается дословно, а не импортируется из кода — иначе тест утверждает «значение равно самому себе»),
    tests/test_services/test_payment_service.py:795-825 (`_yoo_settings` и контекстный менеджер `_sdk`, отдающий мок наружу),
    tests/test_services/test_payment_service.py:869-885 (`_subscribe` — намерение оплатить доступ ровно тем входом, который производит продукт),
    tests/test_services/test_payment_service.py:905-925 (образец чтения тела вызова: `create_mock.call_args.args[0]`),
    tests/test_application/test_declared_invariants.py:60-96 (гейт объявлений денежного пути: множество проверяемых модулей, порог заглавного прогона, шаблон имени свидетеля, потолок реестра долга),
    .planning/phases/05.1-edinaya-podpiska/05.1-REVIEW.md § CR-02 целиком,
    .planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md § истина 9 и § «Трассировка данных» (строка описания платежа)
  </read_first>

  <behavior>
    - Тест 1 (пишется ПЕРВЫМ и обязан упасть): подписочное намерение уезжает в SDK со строкой назначения «Доступ к системе»; утверждение читает ТЕЛО вызова, а не исходник.
    - Тест 2: у назначения нет места вставки — вызов с НЕПУСТЫМ аргументом тарифа даёт ТУ ЖЕ строку. Это наблюдаемое следствие, а не перефразировка теста 1: он падает на любой реализации вида «если тариф пуст — одно, иначе — другое».
    - Тест 3 (разбор AST): в ветке подписки значение, присваиваемое назначению, — имя, а не собранная строка.
    - Тест 4: в строке назначения не встречается текстовое представление пустого значения — утверждение от ДЕФЕКТА, переживающее любую будущую правку формулировки.
  </behavior>

  <action>
    Работа идёт двумя тактами: сначала свидетель (он обязан упасть), потом реализация.

    ТАКТ RED. В `tests/test_services/test_payment_service.py` завести модульную константу
    `SUBSCRIPTION_DESCRIPTION_TEXT` со значением `Доступ к системе`, выписанным ДОСЛОВНО,
    а не импортированным из `app.constants`. Причина — правило, уже записанное в этом же
    файле рядом с `ACCESS_PRICE` и `PACKAGE_NOT_CREDITED_KEY`: тест, берущий значение из
    того же источника, что и код, утверждает «значение равно самому себе» и переживает
    любую подмену формулировки молча, а назначение платежа есть контракт с покупателем и
    меняться обязано ЗАМЕТНО.

    Рядом с `test_the_intent_records_that_no_rule_was_asked` завести три теста:

    `test_the_subscription_payment_names_its_purpose_in_words` — через контекстный
    менеджер `_sdk` и помощник `_subscribe` создать подписочное намерение, снять
    `create_mock.call_args.args[0]` и потребовать равенства ключа `description` модульной
    константе теста. Вторым утверждением — что в значении этого ключа не встречается
    текстовое представление пустого значения (тест 4 объединяется сюда: он утверждает о
    том же ключе того же вызова).

    `test_the_purpose_of_a_subscription_payment_is_not_a_substitution` — вызвать
    `create_payment` напрямую с теми же аргументами, что подаёт `_subscribe`, но с
    аргументом тарифа, равным строке `basic`, и потребовать ТУ ЖЕ строку назначения.
    В докстринге назвать, что тест закрывает: реализацию с запасным значением, при
    которой имя снятого тарифа всё ещё имеет живой путь в фискальный чек.

    `test_the_subscription_purpose_is_a_name_and_not_an_fstring` — прочитать
    `app/services/payment_service.py` модулем `ast`, найти функцию `create_payment`,
    внутри неё — ветку сравнения предмета покупки с `KIND_SUBSCRIPTION`, в ней —
    присваивание переменной `description`, и потребовать, чтобы узел значения был
    `ast.Name`. Образец разбора по синтаксическому дереву в проекте есть:
    `tests/test_migrations/test_0020_flat_subscription.py` и
    `tests/test_application/test_declared_invariants.py`.

    Прогнать `uv run pytest tests/test_services/test_payment_service.py -q -k purpose`
    и убедиться, что новые тесты КРАСНЫЕ. Зафиксировать коммитом с префиксом
    `test(05.1-10):`.

    ТАКТ GREEN. В `app/constants.py` объявить `SUBSCRIPTION_DESCRIPTION: str` со
    значением `Доступ к системе` — ровно тем, что уже стоит в ветке пустого тарифа
    `payment_row.html:51`. Третьей формулировки не заводить: ревизия кода предлагала
    `Доступ к системе Broadcaster`, и это была бы ТРЕТЬЯ запись одного значения — ровно
    тот класс расхождения, ради закрытия которого план существует. Место выбрано не по
    удобству: `app/constants.py` объявлен домом значений, которые обязаны называться
    одинаково в нескольких местах (его собственные абзацы о состояниях объявления и
    подписях каналов говорят это прямо), а назначение платежа именно таково — его
    произносят сервис и шаблон журнала.

    В `app/services/payment_service.py` импортировать константу из `app.constants` и в
    ветке подписки присвоить `description` ИМЕНЕМ константы. Подстановку убрать целиком,
    а не заменить выражением с запасным значением: аргумент тарифа тождественно пуст по
    построению (D-F снял поле из модели, D-A оставил один предмет продажи), и ветка «если
    тариф непуст — печатать его» была бы живым путём к печати имени тарифа, которого в
    модели больше нет.

    ⚠️ ВЕТКА РЕШЕНА В СЕРВИСЕ, А НЕ У ВЫЗЫВАЮЩЕГО, И ЭТО РАЗБОР, А НЕ ВКУС. Вызывающий
    один, и он уже поступает верно — передаёт пустой тариф намеренно и записывает
    почему. Собери он строку назначения сам, `create_payment` сохранил бы прежнюю
    подстановку для любого будущего вызывающего, и дефект пережил бы починку. Назначение
    подписочного платежа есть свойство ПРЕДМЕТА ПРОДАЖИ (предмет один), а не свойство
    места вызова.

    ⚠️ КОММЕНТАРИЙ, КОТОРЫЙ ВСТАНЕТ В `app/services/payment_service.py`, ОБЯЗАН НАЗВАТЬ
    СВИДЕТЕЛЯ ПОИМЁННО. Модуль входит в множество `EXTRA_CHECKED_MODULES` гейта
    объявлений денежного пути: абзац с двумя и более заглавными прогонами, не содержащий
    имени вида `test_...`, резолвящегося в живую функцию суиты, краснит
    `tests/test_application/test_declared_invariants.py` и требует записи в реестр долга,
    потолок которого может только снижаться. Назвать в абзаце
    `test_the_subscription_payment_names_its_purpose_in_words`.

    Пакетную ветку сборки назначения НЕ ТРОГАТЬ ни одной строкой: её снятие — предмет
    WR-05, отложенного этим прогоном явно (перечень отложенного — план `05.1-12`).
    Сигнатуру `create_payment` не сокращать: аргумент тарифа по-прежнему записывается в
    журнальную колонку строки платежа, и её содержимое защищено решением D-H.

    Прогнать те же тесты — они обязаны позеленеть. Зафиксировать коммитом с префиксом
    `feat(05.1-10):`.

    ПОСЛЕДНИМ ШАГОМ — ОБНОВИТЬ ГРАФ ЗНАНИЙ: `graphify update .`. Это прямое предписание
    инструкций проекта (`CLAUDE.md`, раздел graphify): «After modifying code, run
    `graphify update .` to keep the graph current». Обновление разборное, без обращений к
    платному API. Пропуск оставил бы `graphify-out/graph.json` помеченным устаревшим для
    денежного модуля — то есть для файла, о котором следующий вопрос к графу вероятнее
    всего и будет задан.
  </action>

  <verify>
    <automated>uv run pytest tests/test_services/test_payment_service.py -q</automated>
    <automated>uv run pytest tests/test_application/test_declared_invariants.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_services/test_payment_service.py -q` завершается кодом 0.
    - `uv run pytest tests/test_application/test_declared_invariants.py -q` завершается кодом 0 — новый абзац денежного пути назвал исполняемого свидетеля и не потребовал записи в реестр долга.
    - Тело вызова `YooPayment.create` для подписочного намерения несёт ключ `description`, равный строке `Доступ к системе`; утверждение снято с `create_mock.call_args.args[0]`, а не с исходника.
    - Вызов `create_payment` с аргументом тарифа `basic` даёт ТУ ЖЕ строку назначения — места вставки не осталось ни одного (краевой случай `empty` зонда закрыт наблюдаемым следствием, а не объявлением).
    - AST-тест утверждает, что узел значения, присваиваемого `description` в ветке подписки, — `ast.Name`; тест падает, если значение снова станет собранной строкой.
    - `app/constants.py` содержит объявление `SUBSCRIPTION_DESCRIPTION`, и `uv run python -c "from app.constants import SUBSCRIPTION_DESCRIPTION; print(SUBSCRIPTION_DESCRIPTION)"` печатает `Доступ к системе`.
    - `git log --oneline -5` содержит два коммита этого плана: один с префиксом `test(05.1-10)`, один с префиксом `feat(05.1-10)` — свидетель зафиксирован ДО реализации.
    - Сигнатура `create_payment` не потеряла ни одного параметра: `uv run python -c "import inspect, app.services.payment_service as m; print(sorted(inspect.signature(m.create_payment).parameters))"` печатает список, содержащий `plan`, `package_name`, `messages_count`.
    - `graphify update .` выполнен последним шагом и завершился успешно; вывод команды (число обновлённых узлов либо строка о неизменности) приведён в SUMMARY.
  </acceptance_criteria>

  <done>
    Покупателю в кассе, в фискальном чеке и в кабинете мерчанта печатается предмет
    покупки словами. Строка утверждена тестом, читающим тело вызова SDK; места вставки в
    неё не осталось; исторические строки журнала не тронуты.
  </done>

  <reversibility rating="reversible">
    Константа и присваивание снимаются обратным коммитом; данных правка не касается ни
    одной строки, схемы не трогает, наружу уходит только текст следующего платежа.
  </reversibility>
</task>

<task type="auto">
  <name>Задача 2: Связать две поверхности назначения исполняемым свидетелем</name>

  <files>
    tests/test_pages/test_billing_section.py
  </files>

  <read_first>
    app/templates/billing/includes/payment_row.html:1-54 (докстринг макроса — там прямо сказано, что ошибка в имени параметра проявляется не исключением, а ПУСТОЙ строкой при статусе 200; и ветка выбора назначения на строках 44-54),
    tests/test_pages/test_billing_section.py:50-60 (образец объявления константы пути к файлу шаблона — `BALANCE_HTML`),
    tests/test_pages/test_billing_section.py:110-135 (помощник `_payment_row` и посев строк журнала),
    tests/test_pages/test_billing_section.py:1200-1232 (ЖИВОЙ тест трёх источников назначения — его правило и его границы; трогать его запрещено),
    tests/test_pages/test_billing_section.py:485-520 (образец теста, читающего шаблон КАК ТЕКСТ),
    app/constants.py (константа, объявленная задачей 1)
  </read_first>

  <action>
    Завести константу пути `PAYMENT_ROW_HTML`, указывающую на
    `app/templates/billing/includes/payment_row.html`, по образцу соседней `BALANCE_HTML`.

    Завести тест `test_the_two_surfaces_of_the_payment_purpose_speak_one_string` с двумя
    утверждениями, ни одно из которых не заменяет другое:

    Первое — ИСХОДНИК. Импортировать `SUBSCRIPTION_DESCRIPTION` из `app.constants`,
    прочитать шаблон строки журнала как ТЕКСТ и потребовать, чтобы значение константы
    встречалось в нём. В докстринге объяснить, почему это НЕ «значение равно самому
    себе»: шаблон константы не читает — он несёт собственный литерал, и тест связывает
    ДВА независимых объявления одного значения. Разойдись они — прогон краснеет, а не
    боевой платёж.

    Второе — ОТРИСОВКА. Посеять подписочную строку журнала с пустым полем тарифа
    (`_payment_row`, затем обнулить поле тарифа), запросить `/billing` через
    `authed_client`, взять тело через `_body` и потребовать, чтобы значение той же
    константы встречалось в отрисованной странице. Исходник доказывает, что литерал
    объявлен; отрисовка — что он доезжает до экрана через макрос.

    ⚠️ ЖИВОЙ ТЕСТ `test_the_purpose_of_a_payment_has_three_sources` НЕ ТРОГАТЬ НИ ОДНОЙ
    БУКВОЙ. Он держит границу D-H: исторический платёж с непустым полем тарифа обязан
    печатать имя своей эпохи, а не быть переименованным в новую формулировку. Новый тест
    добавляется РЯДОМ и утверждает о другом.

    В докстринге теста записать отвергнутую альтернативу и её цену: единственный источник
    можно было бы получить, отдав константу в глобалы Jinja и напечатав её в шаблоне
    подстановкой. Отвергнуто по доводу, записанному в самом макросе: ошибка в имени
    параметра проявляется не исключением, а ПУСТОЙ строкой при статусе 200 — то есть
    опечатка в имени глобала напечатала бы в денежном журнале пустое назначение молча.
    Два объявления, связанные тестом, дешевле одного объявления с тихим отказом.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_section.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_billing_section.py -q` завершается кодом 0.
    - `uv run pytest tests/test_pages/test_billing_section.py -q -k purpose` собирает НЕ МЕНЕЕ двух тестов и завершается кодом 0 — живой тест трёх источников назначения пережил правку и стоит рядом с новым.
    - Новый тест падает, если значение `SUBSCRIPTION_DESCRIPTION` изменить, не тронув шаблон: проверяется однократной ручной подменой значения константы перед прогоном и возвратом обратно (результат подмены записывается в SUMMARY одной строкой).
    - `git diff --stat` по задаче показывает изменения ровно в одном файле — `tests/test_pages/test_billing_section.py`.
    - В отрисованной странице `/billing` при посеянной подписочной строке с пустым полем тарифа встречается значение константы; утверждение снято с тела ответа, а не с шаблона.
  </acceptance_criteria>

  <done>
    Расхождение двух поверхностей одной формулировки перестало быть возможным молча:
    оно краснит прогон. Историческая половина журнала не тронута.
  </done>

  <reversibility rating="reversible">
    Правка целиком в тестовом файле; снимается обратным коммитом, продукта не касается.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| наш сервер → ЮKassa (`YooPayment.create`) | текст назначения уезжает ТРЕТЬЕЙ СТОРОНЕ и печатается покупателю на странице оплаты, в фискальном чеке и в кабинете мерчанта |
| строка журнала `payments` → экран истории платежей | значение, записанное в прошлом, печатается человеку сегодня; переименование задним числом было бы ложью в денежном журнале (D-H) |
| `app.constants` → две независимые поверхности одного значения | расхождение двух записей одной формулировки обнаруживается на боевом платеже, если его не ловит прогон |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-35 | Repudiation | фискальный чек, не называющий предмет покупки | high | mitigate | Чек печатает предмет словами; строка закреплена тестом, читающим ТЕЛО вызова SDK (`create_mock.call_args.args[0]`), а не исходник. До этого плана назначение не утверждал ни один тест суиты |
| T-05.1-34 | Information Disclosure | текст назначения, видимый третьей стороне | medium | mitigate | Назначение — постоянная строка без единого места вставки; AST-тест требует имени, а не собранной строки, поэтому ни одно значение — ни из формы, ни из БД, ни из настроек — в чек попасть не может по построению. Соседний ключ `metadata` уже защищён приведением пустого значения к пустой строке и планом не трогается |
| T-05.1-36 | Tampering | расхождение константы и литерала шаблона | medium | mitigate | Тест связывает два независимых объявления и краснеет при расхождении; отвергнутая альтернатива (глобал Jinja) названа вместе с её ценой — тихая пустая строка в денежном журнале |
| T-05.1-37 | Tampering | переименование исторической строки журнала | high | mitigate | План не трогает ни ветку непустого тарифа шаблона, ни колонки таблицы платежей, ни живой тест трёх источников назначения; граница D-H остаётся закреплённой существующей регрессией |
| T-05.1-SC | Tampering | установка пакетов (npm/pip/cargo) | — | N/A | План не устанавливает ни одного пакета; таблица легитимности неприменима за отсутствием предмета, чекпойнт не заводится |
</threat_model>

<verification>
- `uv run pytest tests/test_services/test_payment_service.py tests/test_pages/test_billing_section.py -q` — зелёный.
- `uv run pytest tests/test_application/test_declared_invariants.py tests/test_planning -q` — зелёный.
- `uv run pytest tests/test_routes/test_billing.py tests/test_pages/test_billing_subscription.py tests/test_pages/test_billing_payment_errors.py -q` — зелёный (денежный путь не сдвинут ничем, кроме назначения).

⚠️ **О полном прогоне `just test`.** Проверка фазы установила ОДНО падение полной суиты и
атрибутировала его независимо: `tests/test_pages/test_ads_editor.py::test_image_base_url_comes_from_app_settings`,
порядкозависимое, введено коммитом фазы 02 (`git log -L 28,45:app/pages/common.py` даёт
`5771a50` от 2026-08-10), в изоляции зелёное. Гэпом фазы 05.1 оно не является и этим
планом не чинится. Если исполнитель запускает полную суиту, единственное допустимое
падение — это; любое второе принадлежит правке и обязано быть закрыто до коммита.
</verification>

<success_criteria>
- Подписочный платёж называет свой предмет словами в кассе, в чеке и в кабинете мерчанта.
- Строка назначения утверждена тестом, читающим тело вызова SDK; до плана её не утверждало ничто.
- Места вставки в назначении не осталось: строка не зависит ни от одного аргумента.
- Две поверхности одной формулировки связаны тестом; третьей записи значения в проекте нет.
- Историческая половина журнала печатается как продана: живой тест трёх источников назначения не тронут.
</success_criteria>

<output>
Create `.planning/phases/05.1-edinaya-podpiska/05.1-10-SUMMARY.md` when done
</output>
