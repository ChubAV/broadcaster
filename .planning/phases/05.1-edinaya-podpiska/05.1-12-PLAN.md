---
phase: 05.1-edinaya-podpiska
plan: 12
type: execute
wave: 10
depends_on: ["05.1-10", "05.1-11"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/05.1-edinaya-podpiska/05.1-12-SUMMARY.md
autonomous: true
# Три требования, чья реализация проверена по коду на всех уровнях (истины 1-4 отчёта
# проверки), но чья отметка в реестре отстала. BILL-05 и BILL-07 сюда НЕ входят
# сознательно: условием их отметки названа ручная приёмка на 375px, и ставит её план
# 05.1-13, а не этот.
requirements: [BILL-08, BILL-09, BILL-10]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Отметка требования стоит по ПРОВЕРЕННОМУ КОДУ, а не по исполненному плану: BILL-08, BILL-09 и BILL-10 отмечены `Complete` в ОБЕИХ таблицах, и основанием названо свидетельство проверки, а не факт исполнения плана."
    - "BILL-05 и BILL-07 остаются `In Progress`: условием их отметки названа ручная приёмка на 375px, и она не проведена. Реестр не опережает свидетельство ни на одну строку."
    - "Счётчики покрытия v2.0 выведены арифметикой и сходятся: сумма по четырём состояниям равна объявленному итогу, и внутреннее расхождение строки суммы со строками состояний закрыто заодно."
    - "Восемь предупреждений ревизии кода записаны отложенными ПОИМЁННО, с причиной у каждого: следующая фаза наследует решённый список, а не невидимую дыру."
    - "Четырнадцать неснятых краёв зонда записаны все: два повышены до приёмочных критериев, два вынесены человеку, остальные названы допущениями с указанием того, чем каждое сегодня держится. Ни один край не отброшен молча."
  artifacts:
    - .planning/REQUIREMENTS.md   # дерево требований и таблица прослеживаемости v2.0
  key_links:
    - "истины 1-4 отчёта проверки → строки BILL-08/09/10 дерева требований → строки BILL-08/09/10 таблицы прослеживаемости → счётчики покрытия v2.0"
    - "`.planning/ROADMAP.md` §Coverage (итог 41) ↔ строка суммы §Coverage (v2.0) `.planning/REQUIREMENTS.md` — итог не меняется, меняется распределение по состояниям"
  prohibitions:
    - "Гейт доступа никогда не закрывает денежный путь."
    - "Прекращение доступа никогда не уничтожает данные пользователя."
    - "Денежный журнал не стирается и не переписывается задним числом (D-H)."
    - "Исторические значения `payments.plan` продолжают печататься так, как были проданы."
    - "Ни одна строка требования не удаляется: отменённое и снятое обязано оставаться отличимым от потерянного."
    - "Отметка `Complete` не ставится требованию, условием которого названа непроведённая проверка."
---

<objective>
Привести реестр требований в соответствие с проверенным кодом и записать решённым всё,
что этот прогон закрывать не будет.

Purpose: проверка фазы доказала по коду на всех уровнях реализацию BILL-08 (цена одним
числом, лимитов нет), BILL-09 (пробные 5 дней и прекращение со словами) и BILL-10 (ровно
одна дата окончания, оплата сдвигает её вперёд) — истины 1, 2, 3 и 4 её отчёта. В реестре
все три по-прежнему стоят `Pending`: в дереве требований (`:130`, `:131`, `:132`) и в
таблице прослеживаемости v2.0 (`:259`, `:260`, `:261`). Правило проекта записано планом
`05-06` и подтверждено планом `05.1-09` — «отметка по проверенному коду, а не по
исполненному плану», — и сегодня оно выполнимо.

Второй предмет плана — не оставить невидимых дыр. Восемь предупреждений ревизии кода
(WR-01…WR-08) владелец решил в этот прогон не закрывать; решение записывается ПОИМЁННО с
причиной у каждого, чтобы следующая фаза унаследовала решённый список, а не молчание.
Туда же — четырнадцать неснятых краёв зонда: два из них уже повышены до приёмочных
критериев планов `05.1-10` и `05.1-11`, два вынесены человеку планом `05.1-13`, остальные
названы допущениями.

⚠️ ЭТОТ ПЛАН НЕ ТРОГАЕТ НИ ОДНОЙ СТРОКИ КОДА. Его предмет — документы `.planning/`.

Output: реестр, не отстающий от кода и не опережающий свидетельство; названный список
отложенного.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md
@.planning/phases/05.1-edinaya-podpiska/05.1-REVIEW.md
</context>

## Artifacts this phase produces (план 05.1-12)

| Символ | Место | Вид |
|--------|-------|-----|
| строки `- [x] **BILL-08**`, `- [x] **BILL-09**`, `- [x] **BILL-10**` | `.planning/REQUIREMENTS.md` (дерево требований) | отметка состояния |
| строки прослеживаемости BILL-08/09/10 со статусом `Complete` и основанием | `.planning/REQUIREMENTS.md` (таблица v2.0) | отметка состояния |
| пересчитанные счётчики `Complete` / `In Progress` / `Pending` / `Сумма` | `.planning/REQUIREMENTS.md` §Coverage (v2.0) | арифметика |
| строка `Last updated: … by plan 05.1-12` | `.planning/REQUIREMENTS.md` (подвал) | запись основания |
| § «Отложено сознательно: WR-01…WR-08» | `05.1-12-SUMMARY.md` | реестр отложенного |
| § «Реестр неснятых краёв (14)» | `05.1-12-SUMMARY.md` | реестр допущений |

Ни одна строка требования не удаляется. Итог v2.0 (41) не меняется: меняется только
распределение по состояниям.

<tasks>

<task type="auto">
  <name>Задача 1: Отметить BILL-08, BILL-09 и BILL-10 по проверенному коду и пересчитать счётчики</name>

  <files>
    .planning/REQUIREMENTS.md
  </files>

  <read_first>
    .planning/REQUIREMENTS.md:120-135 (дерево требований, блок «Тарифы»: строки BILL-05, BILL-07 и объявления BILL-08…BILL-11 — видно форму отметки и форму основания у уже отмеченного BILL-11),
    .planning/REQUIREMENTS.md:255-262 (таблица прослеживаемости v2.0: строки BILL-05, BILL-06, BILL-07, BILL-08, BILL-09, BILL-10, BILL-11 — видно, как записано основание у BILL-11 и почему BILL-05/BILL-07 стоят `In Progress`),
    .planning/REQUIREMENTS.md:275-295 (§Coverage (v2.0): перечни по состояниям, строка «Сумма», строки сверки с деревом и подвал `Last updated` — образец того, как записывалось основание прежних пересчётов),
    .planning/ROADMAP.md:612-625 (§Coverage: итог 41 объявлен здесь; он НЕ меняется этим планом),
    .planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md § «Наблюдаемые истины» строки 1-4 и § «Покрытие требований» строки BILL-05, BILL-07, BILL-08, BILL-09, BILL-10 (текст свидетельства берётся отсюда, а не пересказывается),
    tests/test_planning/test_state_progress_matches_roadmap.py:1-40 и :60-125 (что именно принуждается трекингом: счёт ПЛАНОВ выводится из ROADMAP и сверяется с frontmatter STATE.md; счётчики требований этим тестом НЕ читаются — важно знать границу, чтобы не искать в нём защиты, которой у него нет)
  </read_first>

  <action>
    Перевести BILL-08, BILL-09 и BILL-10 из `Pending` в `Complete` в ОБЕИХ таблицах —
    и в дереве требований, и в таблице прослеживаемости v2.0. Одной таблицы недостаточно:
    расхождение двух представлений одного состояния — ровно тот дефект, за который реестр
    уже правился планом `05-06`.

    В дереве требований поменять маркер строки с незакрытого на закрытый у трёх строк
    блока «Тарифы»; текст требования не трогать ни одной буквой — он взят из критериев
    приёмки фазы дословно.

    В таблице прослеживаемости заменить слово `Pending` на `Complete` с датой и
    ОСНОВАНИЕМ. Основание берётся из отчёта проверки, а не сочиняется: у BILL-08 — истина
    1 (цена — единственный литерал настройки, читают его три серверных потребителя,
    арифметики над ценой нет ни одной, тринадцать отрицательных грепов по коду дают ноль
    каждый, отказа по количеству нет ни на одном маршруте); у BILL-09 — истина 2 (длина
    пробного срока объявлена одной константой, заведение пробной строки объявлено ОДИН
    раз и вызывается обоими путями регистрации, прекращение и слова закреплены сквозным
    тестом); у BILL-10 — истины 3 и 4 (частичный уникальный индекс объявлен В МОДЕЛИ, а не
    только в ревизии; отсчёт срока идёт от позднейшей из двух точек; панель имеет ровно
    четыре состояния, и вычисляет их обработчик, а не разметка). В каждой строке назвать
    и то, ЧЕМ это проверено, — командой прогона или именем теста, по образцу уже
    стоящей строки BILL-11.

    ⚠️ BILL-05 И BILL-07 НЕ ТРОГАТЬ. Они остаются `In Progress`, и это не недосмотр:
    условием их отметки в той же таблице НАЗВАНА ручная приёмка вёрстки на 375px —
    перенесённый долг открытого пункта UAT фазы 5, — а её никто не проводил. Ставит её
    план `05.1-13`. Отметка, поставленная здесь, опередила бы свидетельство ровно так, как
    это уже случалось в проекте и было исправлено планом `05-06`.

    Пересчитать счётчики §Coverage (v2.0) АРИФМЕТИКОЙ, а не поправкой на глаз. Сегодня
    там объявлено: `Complete: 24`, `In Progress: 2`, `Pending: 15`, итог 41. Три
    требования переезжают из третьего состояния в первое, значит становится
    `Complete: 27`, `In Progress: 2`, `Pending: 12`, итог остаётся 41. Перечни в скобках
    у обоих изменившихся состояний привести в соответствие: три идентификатора добавить в
    перечень закрытых и убрать из перечня ожидающих.

    ⚠️ СТРОКА «СУММА» СЕГОДНЯ РАСХОДИТСЯ СО СТРОКАМИ СОСТОЯНИЙ, И ЭТО ЧИНИТСЯ ЗАОДНО.
    Она называет 23 закрытых и 16 ожидающих, тогда как строки состояний называют 24 и 15;
    оба набора дают в сумме 41, поэтому расхождение и пережило прежний пересчёт. После
    правки строка суммы обязана называть те же числа, что строки состояний. В подвале
    записать это отдельным предложением: иначе следующий читатель прочтёт новую сумму как
    опечатку.

    Дописать в подвал строку `Last updated: <дата> by plan 05.1-12` с основанием:
    отметка поставлена по отчёту проверки фазы 05.1 от 2026-08-21 (истины 1-4), а не по
    факту исполненных планов; ни одно требование не добавлено и не удалено; итог v2.0
    остался 41; BILL-05 и BILL-07 оставлены `In Progress` намеренно с названным условием
    отметки.

    Строки сверки с деревом («в разделе v2.0 Requirements столько-то строк…») проверить
    и, если они перестали быть верными, привести к факту тем же пересчётом. Итог
    `.planning/ROADMAP.md` §Coverage не трогать: он объявляет ЧИСЛО требований, а не их
    состояния, и это число не менялось.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning -q</automated>
    <automated>just tracking-check</automated>
  </verify>

  <acceptance_criteria>
    - `just tracking-check` завершается кодом 0.
    - `grep -c '^- \[x\] \*\*BILL-0[89]\*\*\|^- \[x\] \*\*BILL-10\*\*' .planning/REQUIREMENTS.md` возвращает 3 — все три отметки стоят в дереве требований.
    - `grep -c '^| BILL-0[89] | Phase 05.1 | Complete\|^| BILL-10 | Phase 05.1 | Complete' .planning/REQUIREMENTS.md` возвращает 3 — все три отметки стоят в таблице прослеживаемости.
    - `grep -n '^| BILL-05 \|^| BILL-07 ' .planning/REQUIREMENTS.md` показывает обе строки со статусом `In Progress` — они не тронуты.
    - `grep -n 'Complete: 27' .planning/REQUIREMENTS.md`, `grep -n 'In Progress: 2' .planning/REQUIREMENTS.md`, `grep -n 'Pending: 12' .planning/REQUIREMENTS.md` — каждая находит ровно одну строку.
    - Строка «Сумма» называет те же четыре числа, что строки состояний, и её арифметика даёт 41; проверяется чтением строки и сложением.
    - `grep -c 'by plan 05.1-12' .planning/REQUIREMENTS.md` возвращает не менее 1 — основание отметки записано в подвале.
    - Ни одна строка требования не удалена: в `git diff .planning/REQUIREMENTS.md` у каждой удалённой строки есть парная добавленная с ТЕМ ЖЕ идентификатором требования; проверяется чтением диффа и записывается в SUMMARY числом пар.
    - `git diff --stat` ПО КОММИТУ ЗАДАЧИ 1 показывает ровно один файл — `.planning/REQUIREMENTS.md`. Критерий ограничен коммитом задачи 1 намеренно: задача 2 того же плана законно пишет `05.1-12-SUMMARY.md`, и проверка, снятая после обеих задач, покраснела бы от собственной работы плана.
  </acceptance_criteria>

  <done>
    Реестр не отстаёт от кода на три строки и не опережает свидетельство ни на одну.
    Счётчики выведены арифметикой, их внутреннее расхождение закрыто, основание отметки
    записано в подвале с датой и источником.
  </done>

  <reversibility rating="reversible">
    Правка документа; снимается обратным коммитом. Ни кода, ни данных, ни схемы не
    касается.
  </reversibility>
</task>

<task type="auto">
  <name>Задача 2: Записать решённым отложенное — восемь предупреждений и четырнадцать краёв</name>

  <files>
    .planning/phases/05.1-edinaya-podpiska/05.1-12-SUMMARY.md
  </files>

  <read_first>
    .planning/phases/05.1-edinaya-podpiska/05.1-REVIEW.md § Warnings целиком (WR-01…WR-08 — формулировки и файлы берутся оттуда, а не пересказываются) и § Info (IN-01…IN-04),
    .planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md § «Найденные анти-паттерны» (та же восьмёрка, увиденная вторым инструментом — расхождения между двумя списками, если они есть, обязаны быть названы),
    § «Отложено сознательно» и § «Реестр неснятых краёв» ЭТОГО плана (ниже) — они и есть источник, который переносится в SUMMARY
  </read_first>

  <action>
    Перенести в `05.1-12-SUMMARY.md` ДОСЛОВНО два раздела, приведённые ниже в этом плане:
    «Отложено сознательно: WR-01…WR-08» и «Реестр неснятых краёв (14)». Перенос дословный,
    а не пересказ: пересказанная причина через фазу читается как мнение исполнителя, а
    записанная — как решение владельца.

    К разделу отложенного добавить одну строку от себя: сверить восьмёрку предупреждений
    ревизии кода с перечнем анти-паттернов отчёта проверки и назвать расхождения, если они
    есть. Два инструмента смотрели на один код независимо; пункт, который увидел только
    один из них, обязан быть назван, а не усреднён.

    ⚠️ ДВЕ СТРОКИ РЕЕСТРОВ ЗАВИСЯТ ОТ ВЕТКИ, ВЫБРАННОЙ НА ЧЕКПОЙНТЕ ПЛАНА `05.1-11`.
    Прочитать `05.1-11-SUMMARY.md`, взять оттуда выбранную ветку и записать в SUMMARY
    ОДНУ редакцию каждой из них — ту, что соответствует факту. Обе редакции выписаны в
    таблицах ниже; выбирать, а не пересказывать.

    Строка WR-02. При ветке (б) — половина закрыта планом `05.1-11`, вторая половина
    (вакуумность теста границы платной строки) отложена. При ветке (а) — WR-02 отложен
    ЦЕЛИКОМ, и причина записывается словами: сравнения по сроку в ревизии не появилось,
    абзац о строгости остаётся описывающим правило ПРОДУКТА, а не миграции.

    Край 13 реестра краёв. При ветке (б) — повышен ЧАСТИЧНО: строгость закреплена
    соседней точкой и утверждением об операторе, точное равенство остаётся допущением с
    названной причиной. При ветке (а) — край снят ТОЖДЕСТВЕННО ВМЕСТЕ С ПРЕДМЕТОМ: срок
    переставляется всем, границы не существует. Записать это именно как снятие предмета,
    а не как закрытие проверкой — разница в том, что во втором случае кто-то что-то
    проверил, а в первом проверять стало нечего.

    К разделу краёв добавить строку о том, где живёт второй повышенный край: назначение
    платежа при пустом поле тарифа — план `05.1-10`, задача 1. Он от ветки не зависит.

    Информационные пункты IN-01…IN-04 ревизии кода назвать одной строкой как не тронутые
    этим прогоном и оставшиеся записанными в отчёте ревизии. Молчание о них сделало бы их
    неотличимыми от закрытых.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'WR-0' .planning/phases/05.1-edinaya-podpiska/05.1-12-SUMMARY.md` возвращает не менее 8 — все восемь предупреждений названы поимённо.
    - У каждого из восьми в SUMMARY стоит причина отложения и файл, к которому пункт относится; проверяется чтением раздела (восемь строк, в каждой — идентификатор, файл, причина).
    - WR-01 и WR-04 помечены в SUMMARY как сильнейшие кандидаты следующего прохода с названной причиной у каждого.
    - Раздел «Реестр неснятых краёв» содержит ровно 14 строк с колонками «край / категория / требование / судьба»; сумма судеб — 2 повышено (одно из них ЧАСТИЧНО либо снято вместе с предметом — по выбранной ветке), 2 вынесено человеку, 10 допущений.
    - Строка WR-02 и строка края 13 записаны в SUMMARY в ОДНОЙ редакции каждая — той, что соответствует ветке, выбранной чекпойнтом плана `05.1-11`; в SUMMARY названо, какая ветка прочитана из `05.1-11-SUMMARY.md`.
    - `grep -c 'IN-0' .planning/phases/05.1-edinaya-podpiska/05.1-12-SUMMARY.md` возвращает не менее 1 — информационные пункты названы, а не опущены.
    - `uv run pytest tests/test_planning -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Следующая фаза получает решённый список, а не молчание: восемь предупреждений с
    причинами, четырнадцать краёв с судьбой у каждого, четыре информационных пункта
    названы не тронутыми.
  </done>

  <reversibility rating="reversible">
    Запись в SUMMARY; ничего не исполняет и ничего не меняет в продукте.
  </reversibility>
</task>

</tasks>

## Отложено сознательно: WR-01…WR-08

Решение владельца по этому прогону: закрыть блокер, одностороннюю дверь и реестр
требований; восемь предупреждений ревизии кода оставить следующему проходу. Список
записан ПОИМЁННО с причиной у каждого — не потому, что причины оправдывают отложение, а
потому, что молча пропущенное предупреждение через фазу неотличимо от закрытого.

| # | Файл | Суть | Причина отложения |
|---|------|------|-------------------|
| **WR-01** ⭐ | `app/metrics.py:27-30`, `:49-52` | Справка метрики числа активных пользователей обещает «людей с ОТКРЫТЫМ доступом», а запрос спрашивает только про признак активности строки: ни срок, ни признак льготы в условие не входят | **Сильнейший кандидат следующего прохода.** После наката ревизии активная строка есть у КАЖДОГО (популяцию без строки населяет сама ревизия), поэтому метрика становится тождественна «всего пользователей» и на закрытие доступа не реагирует вовсе. Дежурный, читающий панель, получает неверный ответ на вопрос «скольких продукт сейчас пускает». Отложено потому, что правка требует решения — привести запрос к справке или переименовать метрику вместе со справкой, — а решение это относится к наблюдаемости, а не к предмету фазы |
| **WR-02** ⚠️ | `alembic/versions/0020_flat_subscription.py:48-53`; `tests/test_migrations/test_0020_flat_subscription.py:400-427` | Ревизия объявляет правило строгости границы срока, которого в её теле нет; парный тест этого не ловит, потому что проверяет два тождественно истинных факта | **РЕДАКЦИЯ ЗАВИСИТ ОТ ВЕТКИ, ВЫБРАННОЙ НА ЧЕКПОЙНТЕ ПЛАНА `05.1-11`** — исполнитель выбирает одну из двух и записывает выбранную (см. задачу 2). **При ветке (б):** половину закрывает план `05.1-11` — сравнение по сроку в ревизии появляется, абзац приводится к нему, а строгость получает два теста; вторая половина (вакуумность теста границы ПЛАТНОЙ строки) отложена. **При ветке (а):** отложен ЦЕЛИКОМ — сравнения по сроку в ревизии не появилось, абзац остаётся описывающим правило продукта, а не миграции, и парный тест границы остаётся вакуумным |
| **WR-03** | `app/services/payment_service.py:679-688` | Событие «деньги взяты, товар не выдан» пишется уровнем `info` и потому не попадает ни в один алерт, хотя это самый тяжёлый класс события на денежном пути | Отложено: событие относится к платежам, заведённым ДО выката (пакетная ветка вебхука), и правка требует решения об операторском действии — ручном возврате в кабинете ЮKassa, — которого в продукте нет. Поднять уровень без названного действия значит завести алерт, на который нечем ответить |
| **WR-04** ⭐ | `app/pages/admin.py:498-585` | Привилегированный POST выдачи бесплатного доступа не делает сверки источника, тогда как два соседних POST того же проекта её делают; и он же отвечает молчаливым редиректом при отсутствии строки подписки | **Второй сильнейший кандидат.** Это самое привилегированное действие админки после удаления, и сегодня межсайтовую отправку удерживает только атрибут куки — то есть свойство КУКИ, а не свойство маршрута: смена атрибута или добавление второго способа аутентификации снимет защиту молча. Готовый помощник в проекте уже есть. Отложено только по решению о составе этого прогона |
| **WR-05** | `app/services/payment_service.py:26`, `:169-170`, `:358-365` | Пакетная ветка создания платежа недостижима по построению: единственный вызывающий всегда передаёт подписочный предмет покупки | Отложено: снятие меняет сигнатуру функции денежного пути и перецеливает два теста, а предмет — чистота кода, а не дефект поведения. ⚠️ Ветка ВЕБХУКА к этому пункту не относится и снятию не подлежит: по ней приходят уведомления о платежах, заведённых до выката |
| **WR-06** | `app/pages/admin.py:498`; `app/templates/admin/user_detail.html:145` | Адрес маршрута тумблера остался от снятого признака безлимита СООБЩЕНИЙ, тогда как обработчик, колонка и кнопка уже говорят о доступе | Отложено: переименование трогает шаблон и тест, поведения не меняет. ⚠️ Отмечено, что греп-гейт снятых имён формально не покраснел лишь потому, что ищет более длинную подстроку, чем стоит в адресе |
| **WR-07** | `tests/test_application/test_no_metering_remains.py:209-242` | Инвариант «признак льготы читается ровно в одном месте» проверяется обходом только одного пакета, тогда как колонку читают ещё в двух модулях показа одним и тем же выражением | Отложено: закрывается либо расширением области обхода свидетеля с явным белым списком читателей показа, либо объявлением производной величины рядом с предикатом — обе правки своего размера. ⚠️ Сегодня гейт зелен и создаёт впечатление закрытого вопроса |
| **WR-08** | `app/pages/admin.py:72-82`, `:552-559` | Админский читатель активной подписки выбирает ДРУГУЮ строку, чем четыре остальных читателя проекта; вторая его форма вовсе без ограничения выборки | Отложено: обе формы недостижимы, пока стоит частичный уникальный индекс активной подписки. ⚠️ Но они единственные в проекте, кто опирается на индекс МОЛЧА, и снятие индекса будущей фазой обнаружится на админском экране, а не в прогоне |

Информационные пункты ревизии кода IN-01…IN-04 этим прогоном не тронуты и остаются
записанными в `05.1-REVIEW.md`.

## Реестр неснятых краёв (14)

Детерминированный зонд краевых случаев прогнан по идентификаторам требований фазы и
вернул **14 применимых краёв, 0 снятых, 14 неснятых** (категории `unclassified`,
`boundary`, `adjacency`, `empty` и смежные). Спецификации у фазы нет, поэтому ни
`## Edge Coverage`, ни `## Prohibitions` зонду поданы не были, и ни один край не имеет
права быть отброшенным молча. Отнесение каждого края к требованию ниже — реконструкция
планировщика по категориям зонда; судьба названа у каждого.

| # | Край | Категория | Требование | Судьба |
|---|------|-----------|------------|--------|
| 1 | Продление у пользователя БЕЗ активной строки подписки | `empty` | BILL-05 | Допущение: ревизия `0020` заводит строку каждому; закреплено `test_a_user_without_a_subscription_row_is_given_one` |
| 2 | Оплата в момент, когда срок РАВЕН текущему | `boundary` | BILL-05 | Допущение: отсчёт идёт от позднейшей из двух точек, живость сравнивается строго; закреплено `tests/test_application/test_subscription_period.py` |
| 3 | Две доставки вебхука одного платежа внахлёст | `adjacency` | BILL-05 | Допущение: закреплено `test_overlapping_deliveries_extend_subscription_by_one_month` |
| 4 | Назначение подписочного платежа при ПУСТОМ поле тарифа | `empty` | BILL-05, BILL-07 | **ПОВЫШЕН до приёмочного критерия** — план `05.1-10`, задача 1 |
| 5 | Пустая история платежей | `empty` | BILL-07 | Допущение: пустое состояние печатается словами; закреплено тестами раздела подписки |
| 6 | Длинное историческое имя пакета в строке журнала на узком экране | `long-text` | BILL-07 | **ВЫНЕСЕН ЧЕЛОВЕКУ** — пункт 1 ручной приёмки, план `05.1-13` (порог «история не даёт горизонтальной прокрутки») |
| 7 | Историческая строка с НЕПУСТЫМ тарифом рядом с новой | `adjacency` | BILL-07 | Допущение: закреплено `test_the_purpose_of_a_payment_has_three_sources`; план `05.1-10` его не трогает |
| 8 | Цена, изменённая настройкой до пятизначной, в строке цены на 375px | `boundary` | BILL-08 | **ВЫНЕСЕН ЧЕЛОВЕКУ** — пункт 1 ручной приёмки, план `05.1-13`: расчётный запас строки цены ≈9px, и пятизначная цена его съедает |
| 9 | Историческая строка журнала с суммой, отличной от текущей цены | `unclassified` | BILL-08 | Допущение: журнал печатает сумму как продана (D-H); ревизия таблицу платежей не трогает |
| 10 | Момент истечения РОВНО в секунду сравнения | `boundary` | BILL-09 | Допущение: сравнение строгое в предикате живости подписки; закреплено тестами срока |
| 11 | Пользователь без строки подписки на входе гейта | `empty` | BILL-09 | Допущение: предикат отвечает «закрыто» при отсутствии строки; закреплено `tests/test_pages/test_access_gate.py` |
| 12 | Оплата ВНУТРИ пробного срока не сжигает остаток пробных дней | `adjacency` | BILL-09 | Допущение: отсчёт от позднейшей из двух точек; закреплено тестами срока |
| 13 | Срок бесплатной строки РАВЕН «момент исполнения + пробное окно» | `boundary` | BILL-10 | **ПОВЫШЕН ЧАСТИЧНО** — план `05.1-11`, задача 2, ветка (б): СТРОГОСТЬ сравнения закреплена двумя тестами (соседняя точка на секунду выше границы + утверждение об операторе внутри извлечённого текста SQL), а точное равенство остаётся ДОПУЩЕНИЕМ с названной причиной: момент исполнения снимается внутри наката и тесту недоступен, и причина записана в докстринге теста. В ветке (а) край снимается тождественно вместе с предметом: срок переставляется всем, и границы не существует |
| 14 | Выдача льготы пользователю БЕЗ строки подписки | `empty` | BILL-11 | Допущение (backstop UI-контракта E5): операция не отдаёт 500; закреплено планом `05.1-09` |

Итого судеб: **2 повышено до приёмочных критериев · 2 вынесено человеку · 10 допущений**.
Ни один край не отброшен.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| отчёт проверки → реестр требований | отметка состояния переносится из свидетельства в документ; перенос без свидетельства — это отметка, опережающая код |
| реестр требований → следующая фаза | следующая фаза читает состояния как факт и планирует поверх них |
| отчёт ревизии кода → список отложенного | пункт, не перенесённый в список, через фазу неотличим от закрытого |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-42 | Repudiation | отметка `Complete`, поставленная вперёд свидетельства | medium | mitigate | Каждая из трёх отметок несёт основание, названное истиной отчёта проверки и командой прогона; BILL-05 и BILL-07 сознательно не отмечаются, и причина записана в той же таблице |
| T-05.1-43 | Tampering | молча пропущенное предупреждение ревизии кода | medium | mitigate | Восемь предупреждений перенесены поимённо с причиной; два названы сильнейшими кандидатами следующего прохода; четыре информационных пункта названы не тронутыми |
| T-05.1-44 | Information Disclosure | реестр требований в репозитории | low | accept | Документ не содержит ни ключей, ни персональных данных, ни платёжных реквизитов — только формулировки требований и имена тестов |
| T-05.1-SC | Tampering | установка пакетов (npm/pip/cargo) | — | N/A | План не устанавливает ни одного пакета; таблица легитимности неприменима за отсутствием предмета |
</threat_model>

<verification>
- `just tracking-check` — зелёный (счёт планов сходится с ROADMAP).
- `uv run pytest tests/test_planning -q` — зелёный.
- Три требования отмечены в ОБЕИХ таблицах; BILL-05 и BILL-07 не тронуты.
- Счётчики §Coverage (v2.0) сходятся арифметически, и строка суммы называет те же числа, что строки состояний.
- Восемь предупреждений и четырнадцать краёв записаны в SUMMARY.

⚠️ **Граница трекинг-гейта названа прямо.** `tests/test_planning/` принуждает равенство
СЧЁТА ПЛАНОВ между ROADMAP и frontmatter STATE.md и не читает счётчиков требований
вовсе. Значит зелёный `just tracking-check` НЕ доказывает, что счётчики покрытия верны, —
их верность держится арифметикой, выписанной в приёмочных критериях, и подвальной
записью основания. Искать в трекинге защиты, которой у него нет, — тот же класс ошибки,
ради которого в проекте заведён гейт объявлений без свидетеля.
</verification>

<success_criteria>
- BILL-08, BILL-09, BILL-10 стоят `Complete` в обеих таблицах, у каждой отметки — основание из отчёта проверки и команда прогона.
- BILL-05 и BILL-07 остались `In Progress` с названным условием отметки.
- Счётчики покрытия v2.0 выведены арифметикой, сходятся, и их прежнее внутреннее расхождение закрыто.
- Основание отметки записано в подвале строкой `Last updated … by plan 05.1-12`.
- Восемь предупреждений ревизии кода записаны отложенными поимённо с причиной; WR-01 и WR-04 названы сильнейшими кандидатами следующего прохода.
- Четырнадцать неснятых краёв записаны с судьбой у каждого; ни один не отброшен.
</success_criteria>

<output>
Create `.planning/phases/05.1-edinaya-podpiska/05.1-12-SUMMARY.md` when done
</output>
