---
phase: 05.1-edinaya-podpiska
verified: 2026-08-21T09:11:34Z
status: gaps_found
score: 9/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "Назначение подписочного платежа читается словами на КАЖДОЙ поверхности, где его видит человек — снятое поле тарифа нигде не печатается как значение"
    status: failed
    reason: >-
      Фаза сняла тариф из модели, но одна из двух поверхностей назначения платежа
      продолжает его читать. Строка истории платежей правило соблюдает
      (`payment_row.html:51` — пустой тариф даёт «Доступ к системе»), а описание,
      уезжающее в ЮKassa, подставляет тот же пустой тариф в шаблон строки без
      ветки. Единственный вызывающий передаёт `plan=None` НАМЕРЕННО
      (`app/pages/billing.py:352`), поэтому дефект не вероятностный, а
      тождественный: покупателю в кассе, в фискальном чеке и в личном кабинете
      мерчанта печатается «Подписка «None»» на КАЖДОМ платеже фазы. Ни один тест
      описание не утверждает.
    artifacts:
      - path: "app/services/payment_service.py"
        issue: "Строка 349: `description = f\"Подписка «{plan}»\"` — `plan` тождественно `None`"
      - path: "app/pages/billing.py"
        issue: "Строка 352: единственный вызывающий передаёт `plan=None` по решению фазы"
    missing:
      - "Ветка назначения подписочного платежа при пустом тарифе — та же формулировка, что уже объявлена в `payment_row.html:51` («Доступ к системе»)"
      - "Свидетель на описание: тест, утверждающий строку, уезжающую в `YooPayment.create`, — сегодня её не проверяет ничто"
deferred: []
human_verification:
  - test: "Открыть `/billing` при ширине 375px в четырёх состояниях панели: `trial`, `paid`, `expired`, `comped`"
    expected: >-
      Строка цены не переносится и не обрезается; бейдж состояния стоит отдельной
      строкой; кнопка оплаты сохраняет нажимаемую высоту 44px; история платежей
      не даёт горизонтальной прокрутки
    why_human: >-
      Именованный гейт отметки BILL-05 и BILL-07 в таблице прослеживаемости
      (перенесённый долг открытого пункта UAT фазы 5). Браузерной оснастки в
      проекте нет — `playwright`/`selenium`/`puppeteer` отсутствуют в
      `pyproject.toml`; тесты читают CSS и HTML КАК ТЕКСТ и проверяют объявление
      правила, а не его отрисовку. Визуальная плотность ассертом не выражается.
  - test: "Прочитать глазами текст на `/billing?expired=1` в состоянии `expired` и в состоянии `trial`"
    expected: >-
      Человеку сказано, ЧТО произошло и ЧТО оплатить; текст читается как факт с
      продолжением, а не как потеря данных и не как упрёк
    why_human: >-
      Manual-Only пункт `05.1-VALIDATION.md`. Тест проверяет наличие строки и
      суммы; понятность формулировки — нет. Одновременно закрывает
      judgment-прохибицию плана 05.1-05 (см. ниже).
  - test: "Решение по CR-01 ДО наката ревизии 0020 на бой"
    expected: >-
      Владелец подтверждает, что ветка П-о-2 обязана переставлять `expires_at`
      БЕЗУСЛОВНО (буквальное чтение D-G), либо ветка получает предикат
      `AND expires_at < :trial_expires_at`, а парный тест перестаёт требовать
      усечения
    why_human: >-
      Односторонняя дверь. Ревизия 0020 не накачена, очередь пуста, и на бою
      сегодня 0 строк подписок — усечь нечего. Но накат происходит на следующем
      же деплое, и вопрос «что означает D-G» — вопрос замысла, а не кода.
  - test: "Прогон ревизии 0020 на КОПИИ боевой базы"
    expected: "Три популяции получают верные `expires_at`; `alembic current` после наката — `0020`"
    why_human: >-
      План 05.1-09 этот прогон объявил и ЗАБЛОКИРОВАЛ: дампа, достижимого без
      обращения к бою, не нашлось. Вместо него исполнен синтетический прогон на
      PostgreSQL — он прошёл, но популяции в нём посеяны, а не сняты с боя.
  - test: "Судьба judgment-прохибиции плана 05.1-05 — «текст о закрытии доступа не читается как потеря данных или как упрёк»"
    expected: "Человек подтверждает или отклоняет формулировку"
    why_human: >-
      ⚠️ unverified-prohibition — human review recommended. Прохибиция
      judgment-уровня; вердикт LLM ниже НЕАВТОРИТЕТЕН и зелёным не считается.
---

# Фаза 05.1: Единая подписка — отчёт о проверке

**Цель фазы:** Пользователь платит одно число за доступ к системе — 3000 ₽/мес — и не считает ничего: ни аккаунты, ни группы, ни сообщения.
**Проверено:** 2026-08-21T09:11:34Z
**Статус:** gaps_found
**Повторная проверка:** Нет — первичная проверка

---

## Достижение цели

Проверка велась от цели назад: сначала «что обязано быть ИСТИННЫМ», затем «что обязано СУЩЕСТВОВАТЬ», затем «что обязано быть СВЯЗАНО». Утверждения отчётов исполнителя доказательством не считались ни в одном пункте — каждая строка ниже снята с кода, а не с SUMMARY.

### Наблюдаемые истины

| # | Истина | Статус | Свидетельство |
|---|--------|--------|---------------|
| 1 | **Критерий 1.** Цена выражается одним числом — 3000 ₽/мес; ни аккаунты, ни группы, ни сообщения на сумму не влияют и ничем не ограничены | ✓ VERIFIED | `settings.subscription_price = "3000.00"` — единственный литерал (`app/config.py:77`); читают его ровно три места, и все три — серверные (`billing.py:282` в контекст, `billing.py:356` в платёж, `balance.html:71` через `format_amount`). Арифметики над ценой нет ни одной. Форма оплаты не несёт ни одного поля. Отрицательный греп по `app/`: `plan_limits`, `message_packages`, `free_monthly_messages`, `MessageBalance`, `BalanceTransaction`, `deduct_message`, `plan_usage`, `PlanAxis`, `plan_axes`, `AXIS_ORDER`, `is_unlimited`, `prorated_expiry`, `upgrade_only` — **0 вхождений каждое**. `tests/test_routes/test_limits.py` зелёный: отказа по количеству нет ни на одном маршруте |
| 2 | **Критерий 2.** Первые 5 дней бесплатно и в полном объёме; по истечении доступ прекращается, и человеку сказано словами | ✓ VERIFIED | `TRIAL_DAYS = 5` (`app/constants.py:89`); `start_trial` — ОДНО объявление (`subscription_service.py:43`), два вызывающих: страничный `pages/auth.py:336` и JSON `routes/auth.py:73`. Поведенчески закреплено: `test_the_page_registration_path_starts_the_trial`, `test_the_json_registration_path_starts_the_trial`, `test_the_two_paths_agree_on_the_length_of_the_trial`. Прекращение и слова — `test_the_trial_expires_and_payment_reopens_access` (сквозной трассер), `test_pages_of_value_are_closed_when_access_expired`. Прогон 34 passed |
| 3 | **Критерий 3.** У пользователя ровно ОДНА дата окончания доступа; оплата сдвигает её вперёд | ✓ VERIFIED | Частичный уникальный индекс `uq_subscriptions_active_user` объявлен В МОДЕЛИ, а не только в ревизии (`app/models/subscription.py:22-30`) — иначе суита, строящая схему через `create_all`, его не проверяла бы. `next_expiry` отсчитывает от позднейшей из двух точек, поэтому остаток пробных дней не сгорает. Прогон `test_payment_concurrency.py` + `test_subscription_period.py` + `test_models/test_subscription.py` зелёный. Повторная доставка вебхука закреплена `_claim_payment` и `test_overlapping_deliveries_extend_subscription_by_one_month` |
| 4 | **Критерий 4.** Пользователь видит дату окончания доступа и историю платежей — и больше ничего считать не должен | ✓ VERIFIED | Панель имеет ровно четыре состояния, и вычисляет их ОБРАБОТЧИК (`_access_state`, `billing.py:143`), а не разметка: `comped`/`expired`/`trial`/`paid` (`balance.html:43-49`). Каждое отличается СЛОВАМИ, не только цветом. Считать нечего: `usage_meters.html` и `plan_card.html` удалены, CSS-правил `.meter`/`.plan-card`/`.axis`/`quota` — 0. Пустая история печатает «Платежей пока нет» / «Здесь появятся оплаты доступа» (`balance.html:117`). Идентификатор ЮKassa на экран не выходит ни в одной ветке. Прогон `test_billing_section.py` + `test_billing_subscription.py` — 113 passed. ⚠️ Вёрстка на 375px — пункт ручной приёмки |
| 5 | **Критерий 5.** Администратор выдаёт бесплатный доступ, и это освобождает от оплаты ДОСТУПА, а не сообщений | ✓ VERIFIED | `subscriptions.has_free_access` читается в предикате `access_is_open` ПЕРВЫМ — раньше живости срока (`subscription_period.py:146`), и порядок закреплён разбором тела по AST. Двусторонний греп-гейт держит границу: признака нет ни в `app/worker/`, ни в `app/application/scheduling/`, а в прикладной логике он читается ровно в одном месте (`test_no_metering_remains.py`). Льгота НЕ воскрешает деактивированную строку — граница ветки закреплена отдельно. Прогон `test_free_access.py` (5 тестов, в т.ч. страницы и JSON-вход при МЁРТВОЙ дате) + `test_admin.py -k free_access` — зелёный |
| 6 | Гейт доступа закрывает все поверхности создания ценности и НЕ закрывает путь к оплате | ✓ VERIFIED | Страницы: шесть роутеров с `Depends(require_access)` (`pages/__init__.py:125-131`). JSON: пять роутеров с `Depends(get_current_user_id_with_access)` (`main.py:94-107`). Путь отправки: `check_access_cached` в планировщике (`tasks.py:194` → `use_cases.py:189`), вторая линия в `retry_send` (`tasks.py:445`), третья — повтор из истории (`history.py:983`). Денежный роутер не гейтится НИКОГДА и решение записано в исходнике. Оба гейта держит машинная сверка перечня по ИСХОДНИКУ, а не по собранному приложению (`test_access_gate.py`, 12 тестов) |
| 7 | Счётная модель снята из кода целиком, а не спрятана за отказом | ✓ VERIFIED | Маршрута покупки пакета не существует (в `app/pages/billing.py` ровно два: `GET /billing`, `POST /billing/subscribe`); маршрута пополнения администратором не существует. Ежемесячного начисления нет в `beat_schedule` (`celery_app.py:28-49` — пять задач, ни одной начисляющей). Кэш переименован ВМЕСТЕ со смыслом: ключ `access:{user_id}`, не `balance:` (`billing_cache.py:68`). Пакетная ветка вебхука стала журнальной, а не удалённой: помечает платёж проведённым и не начисляет |
| 8 | Ревизия 0020 приводит схему и три популяции к плоской модели, не переписывая денежный журнал | ✓ VERIFIED (с предупреждением) | `down_revision = "0019"`; снимает `subscriptions.plan`, роняет `message_balances` и `balance_transactions`, заводит `has_free_access` с умолчанием ЛОЖЬ у обеих сторон. Правка данных стоит ДО снятия колонки тарифа. Таблица `payments` не фигурирует ни в одной DDL-операции — единственное её упоминание в файле — текст журнальной записи (строка 328). Прогон `test_0020_flat_subscription.py` + `test_model_matches_head.py` — 21 passed. ⚠️ CR-01 — см. предупреждения |
| 9 | Назначение подписочного платежа читается словами на КАЖДОЙ поверхности, где его видит человек | ✗ FAILED | Две поверхности одного значения разошлись. `payment_row.html:51` правило соблюдает: `payment.plan` пуст → «Доступ к системе». `payment_service.py:349` того же пустого тарифа не спрашивает: `f"Подписка «{plan}»"` при `plan=None`, переданном единственным вызывающим НАМЕРЕННО (`billing.py:352`), даёт «Подписка «None»» — в кассе, в фискальном чеке и в кабинете мерчанта, на КАЖДОМ платеже фазы. Свидетеля у описания нет: греп `'Подписка «'` по `tests/` — 0 вхождений |
| 10 | Блок требований BILL-* отревизован: вердикты названы, новые требования объявлены, счётчики согласованы | ✓ VERIFIED (с предупреждением) | Пять вердиктов `VOIDED-BY-MODEL-CHANGE` с основанием стоят РЯДОМ с текстом требования, ни одна строка не удалена (`REQUIREMENTS.md:49-59`, `:123`, `:202-205`, `:257`). BILL-08…BILL-11 объявлены и присутствуют в ОБЕИХ таблицах. `just tracking-check` (`tests/test_planning/`) зелёный. ⚠️ Отметки BILL-08/09/10 отстали от кода — см. покрытие требований |

**Счёт:** 9/10 истин проверено (0 present, behavior-unverified)

---

### Требуемые артефакты

| Артефакт | Ожидалось | Статус | Детали |
|----------|-----------|--------|--------|
| `app/constants.py` | `TRIAL_DAYS`, `ACCESS_SOON_DAYS` | ✓ VERIFIED | `TRIAL_DAYS = 5` (:89), `ACCESS_SOON_DAYS = 7` (:100); литерала 7 в копирайте нет — виджет берёт порог глобалом |
| `app/config.py` | `Settings.subscription_price` | ✓ VERIFIED | Строкой `"3000.00"`, не `float` — обоснование записано при поле (:62) |
| `app/application/billing/subscription_period.py` | 4 функции живут, 5 сняты | ✓ VERIFIED | Живут: `add_one_month`, `subscription_is_live`, `access_is_open`, `days_left`, `next_expiry`, `countdown_base`. `prorated_expiry` снята — 0 вхождений по `app/` |
| `app/application/billing/plan_usage.py` | удалён | ✓ VERIFIED | Файла нет |
| `app/application/billing/plan_switch.py` | удалён | ✓ VERIFIED | Файла нет; `upgrade_only` — 0 вхождений |
| `app/models/message_balance.py`, `balance_transaction.py` | удалены | ✓ VERIFIED | Файлов нет; имена — 0 вхождений по `app/` |
| `app/models/subscription.py` | `-plan`, `+has_free_access` | ✓ VERIFIED | Колонки `plan` нет; `has_free_access` с `default=False, server_default="0"` — умолчание совпадает у Python и СУБД |
| `app/pages/__init__.py` | `require_access` | ✓ VERIFIED, WIRED | Отказ через `HTTPException(302)`, а не `RedirectResponse` — обоснование верное: значение зависимости FastAPI отбрасывает, и `return` не прервал бы обработчик НИ РАЗУ |
| `app/dependencies.py` | `get_current_user_id_with_access` | ✓ VERIFIED, WIRED | `get_current_user_id` остался нетронутым — отказ живёт в соседней зависимости |
| `app/services/billing_cache.py` | только функции вердикта доступа | ✓ VERIFIED | `check_access_cached`, `invalidate_access_cache`, `_get_redis` — и больше ничего |
| `app/templates/billing/balance.html` | четыре состояния | ✓ VERIFIED | `PANELS` — закрытое множество из четырёх, умолчание `expired` |
| `app/templates/billing/includes/plan_card.html`, `usage_meters.html` | удалены | ✓ VERIFIED | В каталоге `includes/` остался ровно один файл — `payment_row.html` |
| `app/templates/base.html` | виджет `[data-access]` | ✓ VERIFIED, WIRED | Ветка льготы первая, «последний день» РАНЬШЕ счёта дней, порог глобалом. При отсутствии ключа `access` виджет не рисуется — умолчание «закрыт» запрещено явно |
| `app/static/css/app.css` | `margin-top:auto` переехал на `[data-access]` | ✓ VERIFIED | `[data-access] { margin-top: auto; ... }` (:576); мобильное скрытие `[data-access], [data-user]` сохранено (:479). `data-quota` — 0 вхождений |
| `alembic/versions/0020_flat_subscription.py` | ревизия плоской модели | ✓ VERIFIED | См. истину 8 |
| `tests/test_pages/test_access_gate.py` | гейт страниц и JSON | ✓ VERIFIED | 12 тестов, читают ИСХОДНИК — роутер без зависимости не проскочит |
| `tests/test_application/test_no_metering_remains.py` | отрицательный греп-гейт | ✓ VERIFIED | Множество файлов ВЫВОДИТСЯ обходом каталога — модуль, добавленный следующим планом, попадает под гейт сам |
| `tests/test_migrations/test_0020_flat_subscription.py` | 18 тестов | ✓ VERIFIED | Прогон зелёный |
| `tests/conftest.py` | фикстуры `expired_client`, `comped_client` | ✓ VERIFIED, WIRED | Обе используются — без них критерии 2 и 5 непроверяемы ни на одном слое |

---

### Проверка ключевых связей

| Откуда | Куда | Через | Статус | Детали |
|--------|------|-------|--------|--------|
| `POST /register/complete`, `POST /api/auth/register` | `start_trial(db, user_id)` | одно объявление, два вызывающих | ✓ WIRED | `pages/auth.py:336`, `routes/auth.py:73` → `subscription_service.py:43` |
| `require_access` | `access_is_open` → `subscription_is_live` | один предикат | ✓ WIRED | Второго сравнения дат в гейте нет |
| `POST /billing/subscribe` | `YooPayment.create` | `price=settings.subscription_price` | ✓ WIRED | Цена читается СЕРВЕРОМ; форма не несёт ни одного поля |
| 6 страничных роутеров | `Depends(require_access)` | `include_router(...)` | ✓ WIRED | ads, accounts, account_groups, groups, schedules, history |
| 5 JSON-роутеров | `Depends(get_current_user_id_with_access)` | `app.include_router(...)` | ✓ WIRED | ads, uploads, accounts, schedules, history |
| `collect_due_schedules` | `check_access_cached` | параметр `check_limit` | ✓ WIRED | `tasks.py:194` → `use_cases.py:189`; отказ СДВИГАЕТ `next_run_at`, не удаляет |
| `handle_webhook` подписочная ветка | `invalidate_access_cache(user_id)` | после коммита | ✓ WIRED | `payment_service.py:647` — ПОСЛЕ фиксации срока, иначе гонка вернула бы старый вердикт на целый TTL |
| `POST /admin/users/{id}/unlimited` | `subscriptions.has_free_access` → `invalidate_access_cache` | тумблер | ✓ WIRED | `admin.py:573` → `:583` |
| `Subscription.has_free_access` | `access_is_open` (первая проверка) | предикат | ✓ WIRED | Оттуда — `require_access`, `check_access`, `get_shell_context` |
| `get_shell_context` | `base.html [data-access]` | `request.state.shell['access']` | ✓ WIRED | Форма `{open, expires_at, is_comped, days_left}` |
| `alembic 0020` | `down_revision = "0019"` | цепочка ревизий | ✓ WIRED | Head после наката — `0020` |

---

### Трассировка данных (уровень 4)

| Артефакт | Величина | Источник | Реальные данные | Статус |
|----------|----------|----------|-----------------|--------|
| `base.html [data-access]` | `access.days_left`, `access.expires_at`, `access.is_comped` | `get_shell_context` → `select(Subscription)` ОДНИМ запросом (`common.py:519-527`) | Да | ✓ FLOWING |
| `balance.html` панель | `access.state` | `_access_state(access, ever_paid)` — готовый ответ обработчика, не пересчёт в Jinja | Да | ✓ FLOWING |
| `balance.html` цена | `subscription_price` | `settings.subscription_price` → контекст (`billing.py:282`) | Да | ✓ FLOWING |
| `balance.html` история | `payments` | `get_payment_history` → `order_by(created_at.desc(), id.desc())` — вторичный ключ объявлен | Да | ✓ FLOWING |
| `balance.html` плашка закрытия | `access_notice` | `ACCESS_CLOSED_NOTICES[ever_paid]` — закрытое множество с сервера; параметр `?expired=1` обработчиком НЕ читается (грепом подтверждено) | Да | ✓ FLOWING |
| `admin/users.html` колонка доступа | `is_comped`, срок | `admin.py:104` → строка подписки | Да | ✓ FLOWING |
| Описание платежа ЮKassa | `plan` | `None` от единственного вызывающего | **Нет — литерал `None` попадает в текст** | ✗ HOLLOW_PROP |

---

### Поведенческие проверки

Прогонялись ТОЛЬКО именованные файлы, относящиеся к предмету фазы; полная суита не перезапускалась (её результат установлен оркестратором).

| Поведение | Команда | Результат | Статус |
|-----------|---------|-----------|--------|
| Пробный срок, гейт, снятие счётной модели | `pytest test_access_lifecycle.py test_trial.py test_access_gate.py test_no_metering_remains.py test_access_gate_scheduling.py -q` | 34 passed | ✓ PASS |
| Ревизия 0020 и сверка схемы с моделью | `pytest test_0020_flat_subscription.py test_model_matches_head.py -q` | 21 passed | ✓ PASS |
| Бесплатный доступ, срок, раздел подписки | `pytest test_free_access.py test_subscription_period.py test_models/test_subscription.py test_billing_subscription.py test_billing_section.py -q` | 113 passed | ✓ PASS |
| Отсутствие лимитов, одна активная строка, реестр инвариантов, трекинг | `pytest test_limits.py test_payment_concurrency.py test_declared_invariants.py test_planning/ -q` | 25 passed | ✓ PASS |
| Тумблер льготы и вторая линия гейта | `pytest test_admin.py -k "free_access or comped"` · `pytest test_worker/test_tasks.py -k access` | 5 passed · 7 passed | ✓ PASS |
| Атрибуция единственного падения полной суиты | `pytest test_ads_editor.py::test_image_base_url_comes_from_app_settings -q` | 1 passed (в изоляции) | ✓ PASS |

**Итого прогнано в рамках проверки: 205 тестов, 0 падений.**

**Атрибуция падения полной суиты проверена независимо, а не принята на слово.** `git log -L 28,45:app/pages/common.py` даёт единственный коммит `5771a50` от 2026-08-10 — `feat(02-01)`, фаза 02. Модульный синглтон `templates` и привязка глобалов изображений заведены там; фаза 05.1 этот участок не трогала. Падение порядкозависимое, в изоляции тест зелёный. **Гэпом фазы 05.1 не является.**

### Прогон проб (probes)

| Проба | Команда | Результат | Статус |
|-------|---------|-----------|--------|
| — | — | — | ? SKIP |

Конвенциональных проб `scripts/*/tests/probe-*.sh` в проекте нет; ни один PLAN и ни один SUMMARY фазы проб не объявляет. Роль исполняемого гейта здесь несут греп-тесты по исходникам (`test_no_metering_remains.py`, `test_access_gate.py`), и они прогнаны выше.

---

### Покрытие требований

Сверены ВСЕ идентификаторы из frontmatter девяти планов против `.planning/REQUIREMENTS.md`. Объявлено суммарно одиннадцать: BILL-01…BILL-11. Ни одного неучтённого, ни одного осиротевшего.

| Требование | План(ы) | Описание | Статус | Свидетельство |
|------------|---------|----------|--------|---------------|
| BILL-01 | 02, 07 | Просмотр тарифов Free/Basic/Pro | ✓ УЧТЕНО (VOID) | `VOIDED-BY-MODEL-CHANGE` с основанием D-A + D-F; строка прослеживаемости сохранена. Планы 02 и 07 исполняли СНЯТИЕ предмета — идентификатор в их frontmatter законен. **Гэпом не считается** |
| BILL-02 | 02, 07 | Применение тарифных лимитов | ✓ УЧТЕНО (VOID) | Долг закрыт СНЯТИЕМ ПРЕДМЕТА, записано словами (D-D). Проверено кодом: `test_limits.py` зелёный, отказа по количеству нет |
| BILL-03 | 02, 06 | Пополнение баланса сообщений | ✓ УЧТЕНО (VOID) | D-D. Маршрута покупки пакета в коде нет вовсе |
| BILL-04 | 02, 06 | Учёт баланса и списаний | ✓ УЧТЕНО (VOID) | D-D. `deduct_message` — 0 вхождений |
| BILL-05 | 01, 02, 05 | Продление текущей подписки | ? NEEDS HUMAN | Код есть и работает: `POST /billing/subscribe` → `create_payment` → `_extend_subscription`; кнопка «Продлить доступ» в состоянии `paid`. В таблице стоит `In Progress`, и условием отметки НАЗВАНА ручная приёмка 375px. Оставлено открытым СОЗНАТЕЛЬНО планом 05.1-09 — отметку ставит тот, кто проведёт приёмку |
| BILL-06 | 02, 07 | Потребление по четырём осям | ✓ УЧТЕНО (VOID) | Критерий 4 требует обратного. `AXIS_ORDER`, `plan_axes` — 0 вхождений, `usage_meters.html` удалён |
| BILL-07 | 02, 05 | История платежей | ? NEEDS HUMAN | Код есть и работает: `get_payment_history` → блок истории, пустое состояние, потолок списка, идентификатор ЮKassa не печатается. `In Progress` по той же причине, что BILL-05 |
| BILL-08 | 01, 02, 05, 06, 07 | Доступ стоит одно число — 3000 ₽/мес, без лимитов | ✓ SATISFIED (реестр отстал) | Истина 1 проверена по коду. ⚠️ В `REQUIREMENTS.md:130` и в таблице (`:259`) стоит `Pending` |
| BILL-09 | 01, 02, 03, 04 | Пробные 5 дней, затем прекращение со словами | ✓ SATISFIED (реестр отстал) | Истина 2 проверена по коду. ⚠️ Стоит `Pending` (`:131`, `:260`) |
| BILL-10 | 01, 02, 03, 04, 05, 08 | Ровно ОДНА дата окончания; оплата сдвигает вперёд | ✓ SATISFIED (реестр отстал) | Истины 3 и 4 проверены по коду. ⚠️ Стоит `Pending` (`:132`, `:261`) |
| BILL-11 | 02, 08, 09 | Администратор выдаёт бесплатный ДОСТУП | ✓ SATISFIED | Истина 5 проверена по коду. Отметка `Complete` стоит и **подтверждается** — она не опережает свидетельство |

**Осиротевших требований нет.** Обратная сверка: `REQUIREMENTS.md` относит к Phase 05.1 ровно BILL-05, BILL-07, BILL-08, BILL-09, BILL-10, BILL-11 — все шесть заявлены планами.

---

### Проверка прохибиций

| Прохибиция | План | Уровень | Статус | Свидетельство |
|------------|------|---------|--------|---------------|
| Гейт доступа не закрывает путь к оплате | 01 | test | ✓ VERIFIED | `test_the_money_path_stays_open_when_access_expired`, `test_pages_of_recovery_stay_open_when_access_expired`, `test_the_api_of_login_stays_open_when_access_expired` — все зелёные. В исходнике денежный роутер объявлен без гейта, и решение записано словами |
| Прекращение доступа не уничтожает данные пользователя | 03 | test | ✓ VERIFIED | `use_cases.py:192-198` — при отказе планировщик СДВИГАЕТ `next_run_at` и делает `continue`; ни `delete`, ни изменения владения на этом пути нет. Закреплено `test_access_gate_scheduling.py` |
| Денежный журнал не стирается и не переписывается задним числом | 08 | test | ✓ VERIFIED | Таблица `payments` не фигурирует ни в одной DDL-операции ревизии 0020 — единственное упоминание на строке 328 является текстом журнальной записи. `payments.plan`, `payments.switch_authorized`, `payments.package_name` на месте (D-H). Исторические строки не переименовываются: `payment_row.html:51` печатает прошлый тариф как есть |
| Текст о закрытии доступа не читается как потеря данных или как упрёк | 05 | judgment | ⚠️ FLAGGED — human review recommended | **Вердикт LLM НЕАВТОРИТЕТЕН.** Формулировка `balance.html:45` — «Доступ закрыт. Оплатите подписку — работа продолжится с того же места.» — на вид отвечает требованию: факт с продолжением, без упрёка. Но judgment-прохибиция зелёной по машинному прочтению не становится: пункт вынесен человеку (см. `human_verification`) |

---

### Найденные анти-паттерны

Просканированы все 37 файлов `app/` и `alembic/`, изменённые фазой (диапазон `8c4e420~1..HEAD`).

| Файл | Строка | Паттерн | Серьёзность | Влияние |
|------|--------|---------|-------------|---------|
| — | — | `TBD` / `FIXME` / `XXX` | — | **0 вхождений.** Гейт долговых маркеров пройден |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` | — | **0 вхождений** |
| `app/services/payment_service.py` | 349 | Снятое поле печатается в пользовательский текст | 🛑 BLOCKER | «Подписка «None»» в кассе и в чеке — см. истину 9 |
| `alembic/versions/0020_flat_subscription.py` | 143-149 | Безусловный `UPDATE` в необратимом шаге | ⚠️ WARNING | `_RESET_FREE_EXPIRY` не несёт предиката `expires_at <`, поэтому у строки `plan='free'` с более дальней датой срок УКОРАЧИВАЕТСЯ. **Буквально это и есть D-G** («пробный срок ОТ ДАТЫ МИГРАЦИИ»), и парный тест усечение ТРЕБУЕТ, закрепляя как правило. На бою сегодня 0 строк подписок — популяция П-о-2 пуста, под угрозой ноль строк. Но дверь односторонняя, а очередь ревизий пуста: 0020 накатится следующим же деплоем. Вынесено человеку |
| `app/pages/admin.py` | 498-585 | Привилегированный POST без `is_same_origin` | ⚠️ WARNING | Два соседних POST того же проекта сверку делают (`billing.py:335`, `history.py:921`); тумблер, раздающий ПЛАТНОЕ благо, — нет. Сегодня удерживает только `samesite="lax"`, то есть свойство КУКИ, а не свойство маршрута |
| `app/pages/admin.py` | 561-571 | Молчаливый отказ | ⚠️ WARNING | Нет строки подписки → `logger.warning` + голый редирект. Администратор видит перерисованную страницу без единого слова — неотличимо от сломанной кнопки |
| `app/metrics.py` | 27-30, 49-52 | Справка описывает предикат, которого нет в запросе | ⚠️ WARNING | `broadcaster_active_users` обещает «users with an open access period», а считает `is_active` без `expires_at` и без `has_free_access`. После наката 0020 активная строка есть у КАЖДОГО (популяцию П-о-1 населяет ревизия) — метрика станет тождественна «всего пользователей» и на закрытие доступа не отреагирует |
| `app/pages/admin.py` | 72-82, 552-559 | Читатель подписки расходится с четырьмя остальными | ⚠️ WARNING | Словарное присваивание по УБЫВАНИЮ срока оставляет САМУЮ РАННЮЮ строку — противоположный выбор; `admin_toggle_free_access` вовсе без `order_by`/`limit(1)` даст `MultipleResultsFound`. Обе формы недостижимы, пока стоит `uq_subscriptions_active_user`, но они единственные в проекте, опирающиеся на индекс МОЛЧА |
| `app/services/payment_service.py` | пакетная ветка `create_payment` | Недостижимый код | ℹ️ INFO | Вызывающий один, и он передаёт `KIND_SUBSCRIPTION` |
| `app/templates/billing/balance.html` | — | `access.days_left` атрибутом при `.get()` в соседних ветках | ℹ️ INFO | Стилистическое расхождение |
| `app/routes/billing.py` | — | Заголовок источника вебхука берётся из настройки без проверки пригодности | ℹ️ INFO | — |

---

### Требуется ручная проверка

#### 1. Вёрстка `/billing` на 375px в четырёх состояниях

**Что сделать:** открыть `/billing` при ширине 375px последовательно в состояниях `trial` (пробный срок идёт), `paid` (доступ оплачен), `expired` (срок истёк), `comped` (доступ выдан администратором).
**Ожидается:** строка цены не переносится и не обрезается; бейдж состояния стоит ОТДЕЛЬНОЙ строкой от строки цены (запаса в девять пикселей у неё нет); кнопка оплаты сохраняет нажимаемую высоту 44px; история платежей не даёт горизонтальной прокрутки, в том числе на длинном историческом имени пакета.
**Почему человек:** это НАЗВАННОЕ условие отметки BILL-05 и BILL-07 в таблице прослеживаемости — перенесённый долг открытого пункта UAT фазы 5. Браузерной оснастки в проекте нет: `playwright`, `selenium`, `puppeteer` отсутствуют и в `pyproject.toml`, и в `uv.lock`. Тесты читают CSS и HTML КАК ТЕКСТ и проверяют, что правило ОБЪЯВЛЕНО, — отрисовать его они не могут по устройству.

#### 2. Читаемость текста об истечении

**Что сделать:** прочитать глазами текст на `/billing?expired=1` и текст состояния `trial`.
**Ожидается:** человеку сказано, ЧТО произошло и ЧТО оплатить; текст читается как факт с продолжением, а не как потеря данных и не как упрёк.
**Почему человек:** Manual-Only пункт `05.1-VALIDATION.md`. Тест проверяет наличие строки и суммы; понятность формулировки ассертом не выражается. Этот же пункт закрывает judgment-прохибицию плана 05.1-05.

#### 3. Решение по CR-01 ДО наката ревизии 0020 на бой

**Что сделать:** решить, что означает D-G для строки `plan='free'`, чей `expires_at` дальше, чем «дата миграции + 5 дней».
**Ожидается:** либо подтверждение, что ветка П-о-2 обязана переставлять срок БЕЗУСЛОВНО (буквальное чтение D-G — и тогда код и парный тест верны как есть), либо предикат `AND expires_at < :trial_expires_at` в `_RESET_FREE_EXPIRY` и снятие требования усечения из теста.
**Почему человек:** это вопрос ЗАМЫСЛА, а не кода. Дверь односторонняя. Сегодня под угрозой ноль строк (на бою `subscriptions` пуста), но очередь ревизий тоже пуста — 0020 применится следующим же деплоем, и после него классифицировать популяции будет уже нечем.

#### 4. Прогон ревизии 0020 на КОПИИ боевой базы

**Что сделать:** снять дамп боевой базы, накатить `0020`, проверить три популяции и `alembic current`.
**Ожидается:** `alembic current` = `0020`; у платящих `expires_at` перенесён БЕЗ ПЕРЕСЧЁТА; у `free` — окно от момента исполнения; пользователи без активной строки её получили.
**Почему человек:** план 05.1-09 этот прогон ОБЪЯВИЛ и ЗАБЛОКИРОВАЛ — дампа, достижимого без обращения к бою, не нашлось. Исполненный вместо него синтетический прогон на PostgreSQL прошёл все четыре условия, но популяции в нём ПОСЕЯНЫ, а не сняты с боя.

#### 5. Judgment-прохибиция плана 05.1-05

**Что сделать:** вынести вердикт по формулировке закрытия доступа.
**Ожидается:** подтверждение или отклонение.
**Почему человек:** ⚠️ `unverified-prohibition — human review recommended`. Прохибиция judgment-уровня; машинный вердикт по ней НЕАВТОРИТЕТЕН и в зелёный счёт не входит.

---

### Сводка по гэпам

**Цель фазы достигнута конструктивно и провалена на одной поверхности.**

Счётная модель снята из продукта не спрятыванием, а удалением — это проверено не чтением отчётов, а тринадцатью отрицательными грепами по `app/` (каждый даёт ноль), отсутствием пяти файлов и отсутствием маршрутов покупки пакета и пополнения баланса. Цена действительно одно число и действительно в одном месте: `settings.subscription_price` читают ровно три потребителя, все серверные, арифметики над ней нет ни одной. Гейт доступа стоит на всех трёх поверхностях — страницах, JSON-входах и пути отправки, — и его перечень держит машинная сверка по ИСХОДНИКУ, а не по собранному приложению: роутер, добавленный будущим планом без зависимости, не проскочит молча. Льгота администратора читается ровно в одном предикате и доказанно не касается сообщений. Ревизия денежный журнал не трогает. Долговых маркеров в 37 изменённых файлах — ноль.

**Единственный блокер — CR-02, и он не косметический.** Фаза сняла тариф из модели, но описание платежа продолжает его читать: `f"Подписка «{plan}»"` при `plan=None`, который единственный вызывающий передаёт НАМЕРЕННО. Результат — «Подписка «None»» в кассе ЮKassa, в фискальном чеке и в кабинете мерчанта, на КАЖДОМ платеже новой модели. Тяжесть здесь не в опечатке, а в том, что фаза УЖЕ объявила верный ответ на этот самый вопрос и применила его к соседней поверхности: `payment_row.html:51` при пустом тарифе печатает «Доступ к системе». Две поверхности одного значения разошлись, и разошлись они там, где человек расстаётся с деньгами. Свидетеля у описания нет вовсе — греп `'Подписка «'` по `tests/` даёт ноль, поэтому дефект не был бы пойман и регрессией.

**Что гэпом НЕ является и почему это сказано отдельно.** Пять требований блока BILL несут вердикт `VOIDED-BY-MODEL-CHANGE` с названным основанием, и их идентификаторы в frontmatter планов 02, 06 и 07 законны: эти планы исполняли СНЯТИЕ предмета. Открытым долгом они не считаются. Единственное падение полной суиты атрибутировано независимо — `git log -L` по участку `app/pages/common.py` даёт коммит фазы 02 от 2026-08-10; в изоляции тест зелёный. Отсутствие рендер-тестов гэпом не объявляется: браузерной оснастки в проекте нет по устройству, и всё, что действительно требует глаз, вынесено в ручную проверку.

**Реестр требований отстал от кода на три строки.** BILL-08, BILL-09 и BILL-10 стоят `Pending`, хотя их реализация проверена здесь по коду на всех уровнях. Отметку им ставит приёмка фазы, и правило проекта — «отметка по проверенному коду, а не по исполненному плану» — теперь выполнимо. BILL-05 и BILL-07 оставлены `In Progress` СОЗНАТЕЛЬНО и правильно: условием их отметки названа ручная приёмка 375px, а её никто не проводил.

**Что закрыть перед следующей фазой:** один блокер (CR-02). **Что решить человеку перед деплоем:** CR-01 — до наката 0020, потому что дверь односторонняя.

---

_Проверено: 2026-08-21T09:11:34Z_
_Проверяющий: Claude (gsd-verifier)_
