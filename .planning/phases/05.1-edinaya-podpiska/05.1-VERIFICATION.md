---
phase: 05.1-edinaya-podpiska
verified: 2026-08-21T17:15:36Z
status: human_needed
score: 10/10 must-haves verified
behavior_unverified: 0
overrides_applied: 1
overrides:
  - must_have: "Вёрстка раздела подписки на 375px проверена человеком во всех четырёх состояниях панели, и результат по каждому из семи числовых порогов записан отдельно — «пройдено» без разбивки по порогам приёмкой не считается (must-have плана 05.1-13)"
    reason: >-
      Форма приёмки оказалась слабее объявленной: таблицы 7 × 4 нет, пороги 1, 3,
      4 и 6 числами не сняты, состояние `paid` в сессии не заводилось. Владелец,
      проведя приёмку в настоящем браузере на настоящей отдаваемой сервером
      странице, ОБЪЯВИЛ УСЛОВИЕ ОТМЕТКИ НАСТУПИВШИМ — решение записано словами
      вместе с ценой в `05.1-13-SUMMARY.md` §«Следствие для отметки», в обеих
      строках прослеживаемости `REQUIREMENTS.md` и в подвале реестра. Порог 7 —
      единственный настоящий детектор выноса бейджа — после замечания дозамерен
      ЧИСЛОМ (`getBoundingClientRect`) в трёх состояниях из четырёх. Override
      заведён проверяющим, чтобы принятое отступление было машинно видимым, а не
      растворилось в прозе сводки; источник решения назван, а не придуман.
    accepted_by: "владелец (owner), решение зафиксировано планом 05.1-13"
    accepted_at: "2026-08-21T16:27:00Z"
re_verification:
  previous_status: gaps_found
  previous_score: 9/10
  previous_verified: 2026-08-21T09:11:34Z
  gaps_closed:
    - "Назначение подписочного платежа читается словами на КАЖДОЙ поверхности, где его видит человек — CR-02 закрыт планом 05.1-10"
  gaps_remaining: []
  regressions: []
  human_items_closed:
    - "Вёрстка `/billing` на 375px — приёмка проведена владельцем 2026-08-21 (частично: см. override и остаточные пункты)"
    - "Читаемость текста об истечении — 12 «да» из 12 по четырём редакциям"
    - "Решение по CR-01 до наката 0020 — ветка `b-never-shorten`, исполнена планом 05.1-11"
    - "Прогон ревизии 0020 на КОПИИ боевой базы — проведён 2026-08-21T15:37Z, пять чисел сняты ДО наката"
    - "Judgment-прохибиция плана 05.1-05 — ПОДТВЕРЖДЕНА человеком"
deferred:
  - truth: "Блокировка пользователя не действует на страничном входе (CR-01 ревизии кода)"
    addressed_in: "Phase 6"
    evidence: "Success Criterion 2 фазы 6: «Администратор может найти пользователя поиском и фильтрами, ЗАБЛОКИРОВАТЬ и разблокировать его»; заведено todo `blocked-user-can-still-log-in`"
  - truth: "Метрика `broadcaster_active_users` считает всех зарегистрированных, а обещает открытый доступ (WR-01)"
    addressed_in: "Phase 6"
    evidence: "Success Criterion 1 фазы 6: «„Обзор“ показывает ключевые показатели сервиса» — предмет наблюдаемости админ-панели; записано отложенным поимённо в `05.1-12-SUMMARY.md`"
human_verification:
  - test: >-
      Открыть `/billing` на 375px в состоянии `paid` (доступ оплачен) — единственное
      состояние панели, которое в приёмочной сессии не заводилось
    expected: >-
      Строка цены не переносится и не обрезается, бейдж состояния стоит отдельной
      строкой, кнопка «Продлить доступ» сохраняет нажимаемую высоту 44px
    why_human: >-
      Приёмка 2026-08-21 прошла на `trial`, `expired` и `comped`; `paid` в сессии не
      заводилось, и `05.1-13-SUMMARY.md` §«Что по-прежнему НЕ закрыто» называет это
      прямо. Браузерной оснастки в проекте нет (`playwright`, `selenium`, `puppeteer`
      отсутствуют и в `pyproject.toml`, и в `uv.lock`), отрисовать разметку тестами
      невозможно по устройству
  - test: >-
      Открыть `/billing` на 375px с историей платежей из пяти строк, среди которых
      строка с ДЛИННЫМ историческим именем пакета (`payments.package_name` эпохи
      счётной модели)
    expected: "История не даёт горизонтальной прокрутки; подписи `Назначение` / `Сумма` / `Статус` читаются"
    why_human: >-
      Край 6 реестра неснятых краёв (`05.1-12-SUMMARY.md`) объявлен ВЫНЕСЕННЫМ
      ЧЕЛОВЕКУ именно в этот пункт приёмки, но история на пяти строках с длинным
      именем в сессии НЕ СЕЯЛАСЬ — это записано и в сводке плана 05.1-13 (порог 3
      «отдельно не подтверждён»), и в оговорке BILL-07 реестра требований
  - test: >-
      Открыть `/billing` на 375px, выставив `subscription_price` пятизначным
      значением
    expected: "Строка цены не переносится и не обрезается"
    why_human: >-
      Край 8 того же реестра, тоже объявленный ВЫНЕСЕННЫМ ЧЕЛОВЕКУ: расчётный
      горизонтальный запас строки цены ≈9px, и пятизначная цена его съедает.
      Приёмка шла на цене 3000 ₽, то есть край не был затронут
---

# Фаза 05.1: Единая подписка — отчёт о проверке (раунд 2)

**Цель фазы:** Пользователь платит одно число за доступ к системе — 3000 ₽/мес — и не считает ничего: ни аккаунты, ни группы, ни сообщения.
**Проверено:** 2026-08-21T17:15:36Z
**Статус:** human_needed
**Повторная проверка:** Да — после закрытия гэпов (раунд 1 — `gaps_found`, 9/10, отчёт сохранён в истории git, коммит `b8a8990`)

---

## Режим и метод

Проверка повторная. Набор обязательного взят из frontmatter раунда 1: десять наблюдаемых истин, перечень артефактов, ключевых связей и прохибиций. Провалившийся пункт (истина 9, CR-02) проверен ПОЛНОСТЬЮ — все четыре уровня плюс поведенческий свидетель; девять прошедших — регрессионно.

Утверждения SUMMARY доказательством не считались ни в одном пункте. Каждая строка ниже снята с кода, с прогона тестов или с даты и подписи человеческого вердикта в коммитнутом артефакте.

---

## Достижение цели

### Наблюдаемые истины

| # | Истина | Статус | Свидетельство |
|---|--------|--------|---------------|
| 1 | **Критерий 1.** Цена выражается одним числом — 3000 ₽/мес; ни аккаунты, ни группы, ни сообщения на сумму не влияют и ничем не ограничены | ✓ VERIFIED (регрессия) | `subscription_price: str = "3000.00"` — единственный литерал (`app/config.py:77`); читатели те же три и все серверные (`billing.py:282`, `billing.py:356`, `balance.html:71`). Тринадцать отрицательных грепов по `app/` перепроверены на этом HEAD и дают **0 каждый**: `plan_limits`, `message_packages`, `free_monthly_messages`, `MessageBalance`, `BalanceTransaction`, `deduct_message`, `plan_usage`, `PlanAxis`, `plan_axes`, `AXIS_ORDER`, `is_unlimited`, `prorated_expiry`, `upgrade_only`. `test_limits.py` зелёный |
| 2 | **Критерий 2.** Первые 5 дней бесплатно и в полном объёме; по истечении доступ прекращается, и человеку сказано словами | ✓ VERIFIED | `TRIAL_DAYS: int = 5` (`app/constants.py:89`). Сквозной трассер `test_the_trial_expires_and_payment_reopens_access` (`test_access_lifecycle.py:133`) и `test_pages_of_value_are_closed_when_access_expired` (`test_access_gate.py:181`) зелёные. **Половина «сказано словами» закрыта ЧЕЛОВЕКОМ:** владелец прочитал все четыре редакции на живой странице и подтвердил все три критерия по каждой — 12 «да» из 12 (`05.1-14-SUMMARY.md`, задача 1). Тексты сверены проверяющим с кодом дословно: `ACCESS_CLOSED_NOTICES` (`app/pages/billing.py:51-62`), `PANELS['expired']` и `PANELS['trial']` (`balance.html:43-46`) |
| 3 | **Критерий 3.** У пользователя ровно ОДНА дата окончания доступа; оплата сдвигает её вперёд | ✓ VERIFIED (регрессия) | Частичный уникальный индекс объявлен В МОДЕЛИ (`app/models/subscription.py`), `next_expiry` отсчитывает от позднейшей из двух точек. `test_payment_concurrency.py` (в т.ч. `test_overlapping_deliveries_extend_subscription_by_one_month`) и `test_subscription_period.py` зелёные в прогоне ниже |
| 4 | **Критерий 4.** Пользователь видит дату окончания доступа и историю платежей — и больше ничего считать не должен | ✓ VERIFIED | Четыре состояния вычисляет обработчик (`_access_state`), разметка только читает по ключу; `usage_meters.html` и `plan_card.html` отсутствуют — в `billing/includes/` остался ровно один файл `payment_row.html`. 113 тестов раздела зелёные. **Вёрстка на 375px принята человеком** 2026-08-21 в настоящем браузере через SSH-туннель к локальному стенду на ревизии `0020`; порог 7 дозамерен числом в трёх состояниях (`trial` 412.8/412.8, `expired` 485.5/485.5, `comped` 424.3/424.3 — зазор 0.0, условие `>=` выполняется равенством). ⚠️ Остаток формы приёмки — см. override и §«Требуется ручная проверка» |
| 5 | **Критерий 5.** Администратор выдаёт бесплатный доступ, и это освобождает от оплаты ДОСТУПА, а не сообщений | ✓ VERIFIED (регрессия) | Порядок в предикате перепроверен построчно: `access_is_open` сперва отсекает неактивную строку, затем `if subscription.has_free_access: return True` и только потом живость срока (`subscription_period.py:145-148`). `test_free_access.py`, `test_no_metering_remains.py`, `test_subscription_period.py` зелёные |
| 6 | Гейт доступа закрывает все поверхности создания ценности и НЕ закрывает путь к оплате | ✓ VERIFIED (регрессия) | Шесть страничных роутеров с `Depends(require_access)` (`app/pages/__init__.py:125-131`), пять JSON-роутеров с `Depends(get_current_user_id_with_access)` (`app/main.py:94-107`) — перечни на месте. `test_access_gate.py` (включая `test_the_money_path_stays_open_when_access_expired:394`) и `test_access_gate_scheduling.py` зелёные |
| 7 | Счётная модель снята из кода целиком, а не спрятана за отказом | ✓ VERIFIED (регрессия) | `app/application/billing/` содержит ровно `__init__.py` и `subscription_period.py`; моделей `message_balance.py` и `balance_transaction.py` нет. Отрицательные грепы — 0 (см. истину 1) |
| 8 | Ревизия 0020 приводит схему и три популяции к плоской модели, не переписывая денежный журнал | ✓ VERIFIED | 22 теста ревизии зелёные. **Сверх раунда 1: ревизия ИСПОЛНЕНА НА КОПИИ БОЕВОЙ БАЗЫ** 2026-08-21T15:37:06Z (дамп снят владельцем в 15:35Z, разрыв 2 мин 06 с). Предусловие `0019` прочитано командой `SELECT version_num FROM alembic_version`, а не взято из записи. Пять чисел сняты ДО наката (дальние `free` — 0, платные активные — 0, пользователи без активной строки — 4, всего пользователей — 4, всего строк подписок — 0); после наката `alembic current` = `0020`, активных строк = пользователей = 4, `payments` не тронута (колонки `kind`, `plan`, `messages_count`, `package_name`, `amount_value` на месте). Дамп и копия уничтожены, в репозиторий не попал ни один файл данных |
| 9 | Назначение подписочного платежа читается словами на КАЖДОЙ поверхности, где его видит человек — снятое поле тарифа нигде не печатается как значение | ✓ VERIFIED — **гэп раунда 1 ЗАКРЫТ** | `SUBSCRIPTION_DESCRIPTION: str = "Доступ к системе"` (`app/constants.py:126`) присваивается ГОЛЫМ ИМЕНЕМ в ветке `KIND_SUBSCRIPTION` (`payment_service.py:372`) и уезжает в тело `YooPayment.create` ключом `description`. Мест вставки нет. Свидетелей три, и они разного рода: `test_the_subscription_payment_names_its_purpose_in_words` снимает утверждение с ТЕЛА ВЫЗОВА SDK (`call_args.args[0]`), `test_the_purpose_of_a_subscription_payment_is_not_a_substitution` подаёт `plan="basic"` и требует ТУ ЖЕ строку, `test_the_subscription_purpose_is_a_name_and_not_an_fstring` разбирает AST ветки. Две поверхности связаны тестом `test_the_two_surfaces_of_the_payment_purpose_speak_one_string` (`test_billing_section.py:1276`, `:1289` — исходник шаблона И отрисовка). Граница D-H цела: `payment_row.html:51` при НЕПУСТОМ тарифе по-прежнему печатает имя эпохи, и это держит живой `test_the_purpose_of_a_payment_has_three_sources` |
| 10 | Блок требований BILL-* отревизован: вердикты названы, новые требования объявлены, счётчики согласованы | ✓ VERIFIED — **предупреждение раунда 1 снято** | Пять вердиктов `VOIDED-BY-MODEL-CHANGE` с основанием стоят рядом с текстом требования, ни одна строка не удалена. BILL-08, BILL-09 и BILL-10 переведены в `Complete` по ПРОВЕРЕННОМУ КОДУ (основанием в реестре названы истины отчёта проверки, а не факт исполненного плана); BILL-05 и BILL-07 — по проведённой приёмке. Счётчики сходятся арифметикой: 29 + 0 + 12 + 0 = 41; `In Progress: 0`. `tests/test_planning` — 5 passed (окно трекинга DEF-01 закрыто оркестратором) |

**Счёт:** 10/10 истин проверено (0 present, behavior-unverified). Гэпов не осталось; регрессий не обнаружено.

---

### Обязательное гэп-планов 05.1-10…05.1-14

Проверено отдельно от десяти истин: планы гэп-закрытия объявили собственные `must_haves`, и их провал был бы невидим в таблице выше.

| План | Обязательное | Статус | Свидетельство |
|------|--------------|--------|---------------|
| 05.1-10 | Назначение равно строке журнала; мест вставки нет; свидетель читает тело вызова; две поверхности связаны тестом; исторический платёж печатает имя своей эпохи | ✓ 5/5 | См. истину 9. Диапазон правки — ровно четыре объявленных файла, ни одного лишнего |
| 05.1-11 | Решение человека записано словами с датой; ревизия и парный тест говорят ОДНО; платная строка не меняет срок ни при какой ветке; журнал называет три популяции числами; **backstop** — строгость закреплена дважды | ✓ 5/5 | Ветка `b-never-shorten`, основание владельца дословно («Необратимость дороже буквы»), дата 2026-08-21 (`05.1-11-SUMMARY.md:66-75`). Предикат `AND expires_at < :trial_expires_at` стоит в `_RESET_FREE_EXPIRY` (`0020:215`). Парный тест ПЕРЕИМЕНОВАН и требует обратного прежнему: `test_a_free_plan_row_with_a_farther_date_keeps_it`. Обе половины правила закреплены (`..._in_the_past_moves_to_the_window`). Платная строка — `test_a_paid_row_keeps_its_date_even_when_it_is_already_in_the_past`. Backstop подтверждён ЯВНЫМ свидетельством, а не принят: строгость держат два разных теста — соседняя точка на секунду выше границы и `test_the_free_expiry_predicate_compares_strictly`, снимающий утверждение с ИЗВЛЕЧЁННОГО текста SQL и отдельно запрещающий `<=`. Поимённый след — снимок до и после правки, строка журнала на каждую строку (`0020:260-300`) |
| 05.1-12 | Отметка по проверенному коду; BILL-05/07 остаются открытыми до приёмки; счётчики выведены арифметикой; восемь предупреждений записаны поимённо; четырнадцать краёв записаны все | ✓ 5/5 | Реестр и счётчики проверены грепом и арифметикой. WR-01…WR-08 и IN-01…IN-04 записаны поимённо с причиной у каждого; 14 краёв записаны с судьбой у каждого (2 повышены, 2 вынесены человеку, 10 допущений). ⚠️ Записаны они в `05.1-12-SUMMARY.md`, а не в `deferred-items.md` — в коммитнутом артефакте фазы, поэтому обязательное выполнено; расположение отмечено как замечание, а не как провал |
| 05.1-13 | Приёмка по семи порогам в четырёх состояниях; отметка ТОЛЬКО после приёмки; долг UAT фазы 5 закрыт проверкой, а не вторым переносом; **backstop** — порог 7 отдельно от порога 2 | ⚠️ 3/4 + override | Приёмка проведена человеком, отметка поставлена после неё, долг закрыт проверкой (не переносом). Backstop порога 7 закрыт ЧИСЛОМ дозамером оркестратора. **Первое обязательное — разбивка по семи порогам — НЕ ВЫПОЛНЕНО, и сводка называет это прямо трижды.** Принято решением владельца → override во frontmatter |
| 05.1-14 | Тексты прочитаны человеком во всех четырёх редакциях; judgment-прохибиция получила вердикт человека; ревизия исполнена на копии боевой базы ДО выката; классификация трёх популяций снята ДО наката; **backstop** — число дальних `free` измерено на настоящих данных | ✓ 5/5 | 12 «да» из 12; прохибиция ПОДТВЕРЖДЕНА человеком; прогон 2026-08-21T15:37:06Z над ОКОНЧАТЕЛЬНЫМ текстом ревизии (редакция плана 05.1-11) и до выката; классификация снята в 15:36:49Z, то есть ДО наката. Backstop подтверждён явным числом: дальних `free` — **0**, значит обе ветки CR-01 дают на боевых данных тождественный результат, и пересмотр решения не требуется. Условие 4 («у плативших дата не изменилась») записано как **«выполняется ПУСТО»**, а не как проведённая поимённая сверка — честность формы отмечена и засчитана |

---

### Требуемые артефакты

Проверены заново; ниже — только изменённое гэп-закрытием и то, что раунд 1 держал под предупреждением. Остальные семнадцать строк раунда 1 перепроверены существованием и грепом, расхождений нет.

| Артефакт | Ожидалось | Статус | Детали |
|----------|-----------|--------|--------|
| `app/constants.py` | `SUBSCRIPTION_DESCRIPTION` | ✓ VERIFIED, WIRED | `:126`, строка постоянна, мест вставки нет; при объявлении записан размен «два объявления, связанные тестом, против одного глобала с тихим отказом» |
| `app/services/payment_service.py` | ветка подписки читает константу | ✓ VERIFIED, WIRED | `:18` импорт, `:372` голое присваивание, `:409` — ключ `description` тела вызова SDK |
| `tests/test_services/test_payment_service.py` | свидетель тела вызова SDK | ✓ VERIFIED | Три теста, добавлено 160 строк; до этого плана греп `'Подписка «'` по `tests/` давал 0 |
| `tests/test_pages/test_billing_section.py` | свидетель связи двух поверхностей | ✓ VERIFIED | Утверждений два: исходник шаблона и отрисовка через макрос — ни одно не заменяет другое |
| `alembic/versions/0020_flat_subscription.py` | ветка П-о-2 и журнал наката | ✓ VERIFIED | Предикат строгий; снимок до/после; поимённая строка журнала на каждую бесплатную активную строку; абзац докстринга приведён к исполняемому правилу, а прежнее расхождение НАЗВАНО, а не затёрто (`:57-61`) |
| `tests/test_migrations/test_0020_flat_subscription.py` | парный тест правила | ✓ VERIFIED | 22 теста; переименование теста границы объяснено в докстринге вместе с датой решения владельца |
| `.planning/REQUIREMENTS.md` | отметки и счётчики | ✓ VERIFIED | Шесть требований фазы в `Complete` в ОБЕИХ таблицах; подвал несёт три записи об основаниях (планы 05.1-09, 05.1-12, 05.1-13) |

---

### Проверка ключевых связей

| Откуда | Куда | Через | Статус | Детали |
|--------|------|-------|--------|--------|
| `app.constants.SUBSCRIPTION_DESCRIPTION` | `YooPayment.create['description']` | ветка `KIND_SUBSCRIPTION` `create_payment` | ✓ WIRED | Импорт `:18` → присваивание `:372` → тело вызова `:409`. Утверждено с тела вызова, а не с исходника |
| `SUBSCRIPTION_DESCRIPTION` | литерал ветки пустого тарифа `payment_row.html:51` | ТЕСТ, а не соглашение | ✓ WIRED | `test_the_two_surfaces_of_the_payment_purpose_speak_one_string` — расхождение краснит прогон |
| checkpoint:decision (владелец) | `_RESET_FREE_EXPIRY` | ветка `b-never-shorten` | ✓ WIRED | Решение записано словами с датой; исполнено предикатом; закреплено двумя тестами |
| `_RESET_FREE_EXPIRY` | парный тест ветки П-о-2 | одно правило, два места | ✓ WIRED | Тест ТРЕБУЕТ сохранения дальней даты — то же, что делает предикат |
| копия боевой базы | классификация трёх популяций → решение CR-01 | прогон 2026-08-21 | ✓ WIRED | Числа журнала наката совпали с классификацией шага 3 по всем трём популяциям |
| ручная приёмка на 375px | строки BILL-05 и BILL-07 → счётчики §Coverage | вердикт владельца | ✓ WIRED (с оговоркой) | Отметка стоит ПОСЛЕ приёмки; цена формы записана рядом с отметкой в трёх местах |
| Одиннадцать связей раунда 1 (гейты, вебхук, шелл, льгота, цепочка ревизий) | — | — | ✓ WIRED | Перепроверены грепом и прогоном; расхождений нет |

---

### Трассировка данных (уровень 4)

| Артефакт | Величина | Источник | Реальные данные | Статус |
|----------|----------|----------|-----------------|--------|
| Описание платежа ЮKassa | `description` | `app.constants.SUBSCRIPTION_DESCRIPTION` — постоянная строка без мест вставки | Да — снято с тела вызова SDK тестом | ✓ FLOWING (**было `✗ HOLLOW_PROP` в раунде 1**) |
| Строка журнала платежей | `purpose` | `payment.plan` при непустом, литерал при пустом | Да | ✓ FLOWING |
| `base.html [data-access]`, панель, цена, история, плашка закрытия, админская колонка | — | — | Да | ✓ FLOWING (регрессия раунда 1) |

---

### Поведенческие проверки

Прогонялись ТОЛЬКО именованные файлы предмета фазы. Полная суита не перезапускалась: её результат на этом дереве установлен оркестратором (`1 failed, 1759 passed`), и единственное падение — `test_ads_editor.py::test_image_base_url_comes_from_app_settings` — воспроизводится на доэтотной базе `54a121a` (`1 failed, 1751 passed`), зелено в изоляции и целым файлом, заведено todo `full-suite-ads-editor-order-pollution`. Гэпом фазы 05.1 не является.

| Поведение | Команда | Результат | Статус |
|-----------|---------|-----------|--------|
| Назначение платежа, ревизия 0020, раздел подписки | `pytest test_0020_flat_subscription.py test_payment_service.py test_billing_section.py -q` | **113 passed** | ✓ PASS |
| Пробный срок, гейты, снятие счётной модели, срок, льгота, лимиты, гонки вебхука, сверка модели со схемой, трекинг | `pytest test_access_lifecycle.py test_access_gate.py test_free_access.py test_no_metering_remains.py test_subscription_period.py test_access_gate_scheduling.py test_limits.py test_payment_concurrency.py test_model_matches_head.py test_planning -q` | **92 passed** | ✓ PASS |

**Итого прогнано в рамках этой проверки: 205 тестов, 0 падений.**

### Прогон проб (probes)

| Проба | Команда | Результат | Статус |
|-------|---------|-----------|--------|
| — | — | — | ? SKIP |

Конвенциональных проб `scripts/*/tests/probe-*.sh` в проекте нет; ни один из четырнадцати PLAN и ни один SUMMARY проб не объявляет. Роль исполняемого гейта несут греп- и AST-тесты по исходникам, и они прогнаны выше.

---

### Покрытие требований

Сверены ВСЕ идентификаторы из frontmatter четырнадцати планов против `.planning/REQUIREMENTS.md`. Объявлено суммарно одиннадцать: BILL-01…BILL-11. Неучтённых нет, осиротевших нет.

| Требование | План(ы) | Статус | Свидетельство |
|------------|---------|--------|---------------|
| BILL-01 | 02, 07 | ✓ УЧТЕНО (VOID) | `VOIDED-BY-MODEL-CHANGE` (D-A + D-F), строка прослеживаемости сохранена |
| BILL-02 | 02, 07 | ✓ УЧТЕНО (VOID) | D-D; проверено кодом — отказа по количеству нет ни на одном маршруте |
| BILL-03 | 02, 06 | ✓ УЧТЕНО (VOID) | D-D; маршрута покупки пакета в коде нет |
| BILL-04 | 02, 06 | ✓ УЧТЕНО (VOID) | D-D; `deduct_message` — 0 вхождений |
| BILL-05 | 01, 02, 05, 10, 13 | ✓ SATISFIED | `Complete` (2026-08-21, план 05.1-13) в дереве и в таблице. Основание — ПРОВЕДЁННАЯ приёмка на 375px. ⚠️ Форма вердикта слабее объявленной — см. override |
| BILL-06 | 02, 07 | ✓ УЧТЕНО (VOID) | Критерий 4 требует обратного; `usage_meters.html` удалён |
| BILL-07 | 02, 05, 10, 13 | ✓ SATISFIED | `Complete` по тому же вердикту. ⚠️ Оговорка при самом требовании названа честно: порог 3 отдельным пунктом не подтверждён, история на пяти строках с длинным именем пакета не сеялась |
| BILL-08 | 01, 02, 05, 06, 07, 12 | ✓ SATISFIED | `Complete` (план 05.1-12) по истине 1, основанием названо свидетельство проверки, а не факт исполненного плана |
| BILL-09 | 01…04, 11, 12, 14 | ✓ SATISFIED | `Complete` по истине 2; половина «сказано словами» подтверждена ЧЕЛОВЕКОМ; возврат в `In Progress` не потребовался — отклонённых текстов нет |
| BILL-10 | 01…05, 08, 11, 12, 14 | ✓ SATISFIED | `Complete` по истинам 3 и 4 |
| BILL-11 | 02, 08, 09 | ✓ SATISFIED | `Complete` (план 05.1-09) по истине 5 |

**Осиротевших требований нет.** Обратная сверка: `REQUIREMENTS.md` относит к Phase 05.1 ровно BILL-05, BILL-07, BILL-08, BILL-09, BILL-10, BILL-11 — все шесть заявлены планами и все шесть закрыты.

**Счётчики §Coverage (v2.0) сведены:** 29 (Complete) + 0 (In Progress) + 12 (Pending) + 0 (Partial) = 41 = итог; совпадает с §Coverage `ROADMAP.md`. Внутреннее расхождение строки суммы со строками состояний, тянувшееся с 2026-08-15, закрыто планом 05.1-12 и в отчёте раунда 1 значилось предупреждением — снято.

⚠️ **Одно расхождение реестра НЕ закрыто и лежит вне предмета фазы:** GRP-04, GRP-05, GRP-06 стоят `- [x]` в дереве требований и `Pending` в таблице прослеживаемости (счётчики выведены по таблице, поэтому арифметика сходится). Расхождение ПРЕДШЕСТВУЕТ фазе, записано в `deferred-items.md` и требует сверки прослеживаемости Фазы 3 по коду.

---

### Проверка прохибиций

| Прохибиция | План | Уровень | Статус | Свидетельство |
|------------|------|---------|--------|---------------|
| Гейт доступа не закрывает путь к оплате | 01, 10-14 | test | ✓ VERIFIED | `test_the_money_path_stays_open_when_access_expired` и два соседних — зелёные |
| Прекращение доступа не уничтожает данные пользователя | 03, 10-14 | test | ✓ VERIFIED | Планировщик СДВИГАЕТ `next_run_at` и делает `continue`; `test_access_gate_scheduling.py` зелёный |
| Денежный журнал не стирается и не переписывается задним числом (D-H) | 08, 10-14 | test | ✓ VERIFIED | `test_the_revision_performs_no_operation_on_the_payment_journal` + `test_the_payment_journal_check_reads_sql_hidden_in_an_f_string`; **подтверждено на копии боевой базы**: `payments` после наката несёт `kind`, `plan`, `messages_count`, `package_name`, `amount_value` |
| Исторические значения `payments.plan` печатаются так, как были проданы | 10-14 | test | ✓ VERIFIED | `payment_row.html:51` — непустой тариф печатается как есть; `test_the_purpose_of_a_payment_has_three_sources` жив и не тронут планом 05.1-10 |
| Ни одно значение, пришедшее от пользователя, не попадает в текст назначения платежа | 10 | test | ✓ VERIFIED | Назначение — константа; `test_..._is_not_a_substitution` подаёт `plan="basic"` и требует ту же строку |
| Формулировка назначения не заводится третьей | 10 | test | ✓ VERIFIED | Записей ровно две — константа и литерал шаблона, связаны тестом |
| Ревизия не изменяет `expires_at` ни одной ПЛАТНОЙ активной строки | 11 | test | ✓ VERIFIED | `test_a_paid_row_keeps_its_date_even_when_it_is_already_in_the_past`; на копии боевой базы платных строк 0 — условие выполняется пусто, и это записано пустотой |
| Необратимая правка данных не остаётся без следа | 11 | test | ✓ VERIFIED | Снимок ДО правки + поимённая строка журнала на каждую строку обеих групп |
| Ни одна строка требования не удаляется | 12 | test | ✓ VERIFIED | Пять VOID-строк на месте; ни одной удалённой |
| `Complete` не ставится требованию, условием которого названа непроведённая проверка | 12, 13 | judgment | ✓ VERIFIED | План 05.1-12 оставил BILL-05/07 открытыми; отметку поставил план 05.1-13 ПОСЛЕ приёмки |
| Ни одна команда не выполняется против боевого стека (кроме снятия дампа владельцем) | 13, 14 | judgment | ✓ VERIFIED | `just run` не запускался ни разу; накат шёл против `127.0.0.1:55433`; дамп снял владелец, и это было только чтение. Дамп и копия уничтожены, в репозиторий не попал ни один файл данных |
| Отметка не ставится по устному «выглядит нормально»: без разбивки по семи порогам приёмка не засчитывается | 13 | judgment | ⚠️ НАРУШЕНА — принято владельцем | Разбивки нет; отметка поставлена решением владельца. Не спрятано: названо трижды в сводке, в обеих строках прослеживаемости и в подвале реестра. Заведён override во frontmatter |
| **Текст о закрытии доступа не читается как потеря данных или как упрёк** | 05 | judgment | ✓ **ПОДТВЕРЖДЕНА ЧЕЛОВЕКОМ** (раунд 1: ⚠️ FLAGGED) | Владелец прочитал четыре редакции на живой странице и подтвердил все три критерия по каждой — 12 «да» из 12, 2026-08-21. Флаг `unverified-prohibition` СНЯТ: вердикт вынес человек, а не машина |
| Вердикт LLM по judgment-прохибиции не засчитывается зелёным | 14 | judgment | ✓ VERIFIED | Исполнитель только сверил, что четыре текста на экране — те самые четыре закрытых множества с сервера, и записал ответы человека |

---

### Найденные анти-паттерны

Гэп-закрытие тронуло **шесть файлов** (`git diff --stat b8a8990..HEAD -- app/ alembic/ tests/`: 631 вставка, 28 удалений) — ровно те, что объявлены планами 05.1-10 и 05.1-11. Ни одного файла сверх объявленного.

**Гейт долговых маркеров пройден:** по всем **69** файлам `app/`, `alembic/`, `tests/`, изменённым фазой (`8c4e420~1..HEAD`), греп `TBD|FIXME|XXX` даёт **0 вхождений**, греп `TODO|HACK|PLACEHOLDER` — **0 вхождений**.

| Файл | Строка | Паттерн | Серьёзность | Влияние |
|------|--------|---------|-------------|---------|
| `app/services/payment_service.py` | 349 (раунд 1) | «Подписка «None»» в кассе и в чеке | 🛑 **БЫЛО BLOCKER — ЗАКРЫТО** | Ветка читает константу; три свидетеля |
| `alembic/versions/0020_flat_subscription.py` | 143-149 (раунд 1) | Безусловный `UPDATE` в необратимом шаге | ⚠️ **БЫЛО WARNING — ЗАКРЫТО** | Предикат строгий; решение владельца записано словами |
| `app/services/payment_service.py` | 27, 381-388 | Недостижимая пакетная ветка несёт `f"Пополнение баланса: {package_name}"` — тот же класс, что закрытый CR-02 | ⚠️ WARNING | `KIND_PACKAGE` — **0 живых вхождений** по `app/` кроме собственного объявления; единственный вызывающий (`billing.py:342`) всегда подаёт `KIND_SUBSCRIPTION`. Сегодня недостижимо и человеку не видно; латентно — открытый путь в чек для первого же будущего вызывающего. Ревизия кода (WR-03) поднимает серьёзность против раунда 1 (там было ℹ️ INFO) |
| `alembic/versions/0020_flat_subscription.py` | 24-39, 413 | Откат роняет `has_free_access`, но перечень «трёх потерь», объявленный исчерпывающим и закреплённый тестом, называет три из четырёх | ⚠️ WARNING | Подтверждено проверяющим по коду: `op.drop_column(TABLE_SUBSCRIPTIONS, COLUMN_FREE_ACCESS)` на `:413`. Кому администратор выдал льготу, после отката и повторного наката восстановить нечем — ровно тот довод, которым ревизия обосновывает необратимость первых трёх (WR-04 ревизии кода) |
| `app/metrics.py` | 27-30, 49-52 | Справка обещает «людей с открытым доступом», запрос спрашивает только `is_active` | ⚠️ WARNING | После наката `0020` активная строка есть у КАЖДОГО — метрика тождественна «всего пользователей». Записано отложенным поимённо (WR-01) и отнесено к Фазе 6 |
| `app/pages/admin.py` | 498-585 | Привилегированный POST без сверки источника; молчаливый отказ при отсутствии строки | ⚠️ WARNING | Держит только атрибут куки. Записано отложенным (WR-04 реестра плана 05.1-12) |
| `app/pages/admin.py` | 72-82, 552-559 | Читатель подписки расходится с четырьмя остальными; вторая форма без `limit(1)` | ⚠️ WARNING | Обе формы недостижимы, пока стоит частичный уникальный индекс, но опираются на него МОЛЧА |
| `app/services/payment_service.py` | 679-688 | «Деньги взяты, товар не выдан» пишется уровнем `info` | ⚠️ WARNING | Не попадает ни в один алерт; относится к платежам эпохи до выката |
| `tests/test_application/test_no_metering_remains.py` | 209-242 | Инвариант «признак льготы читается в одном месте» обходит только один пакет | ⚠️ WARNING | Гейт зелен и создаёт впечатление закрытого вопроса |
| Прочие пункты ревизии кода (WR-05…WR-15, IN-01…IN-04) | — | — | ⚠️/ℹ️ | Записаны поимённо в `05.1-REVIEW.md` и (восьмёрка первого прогона) в `05.1-12-SUMMARY.md`. Молча не пропущен ни один |

**Три BLOCKER ревизии кода раунда 2 — вне предмета этой фазы, и это проверено, а не принято на слово.** Блокировка пользователя на страничном входе (`app/pages/auth.py`), `random.randint` для кодов сброса, cookie сессии без `secure` — ни один не касается ни цены, ни срока доступа, ни гейта доступа, ни денежного пути. Все три заведены отдельными todo (`.planning/todos/pending/`) решением владельца. Первый отнесён к Фазе 6 (её критерий 2 — блокировка и разблокировка пользователя).

---

### Требуется ручная проверка

Пять пунктов раунда 1 закрыты человеком. Ниже — **остаток формы приёмки**, названный самими артефактами фазы, а не обнаруженный проверкой заново. Ни один из них не опровергает достигнутую цель: это края и одно непросмотренное состояние.

#### 1. Состояние `paid` на 375px

**Что сделать:** открыть `/billing` на ширине 375px под учётной записью с ОПЛАЧЕННЫМ живым доступом.
**Ожидается:** строка цены не переносится и не обрезается; бейдж состояния стоит отдельной строкой; кнопка «Продлить доступ» сохраняет нажимаемую высоту 44px.
**Почему человек:** приёмка 2026-08-21 прошла на `trial`, `expired` и `comped`; `paid` в сессии не заводилось, и сводка плана 05.1-13 называет это прямо. Браузерной оснастки в проекте нет — `playwright`, `selenium`, `puppeteer` отсутствуют и в `pyproject.toml`, и в `uv.lock`.

#### 2. Длинное историческое имя пакета в истории платежей на 375px

**Что сделать:** посеять пять строк истории, среди них платёж эпохи счётной модели с длинным `package_name`, и открыть `/billing` на 375px.
**Ожидается:** горизонтальной прокрутки нет; подписи `Назначение` / `Сумма` / `Статус` читаются.
**Почему человек:** край 6 реестра неснятых краёв объявлен ВЫНЕСЕННЫМ ЧЕЛОВЕКУ именно в этот пункт приёмки, но история на пяти строках в сессии не сеялась — это записано и в сводке плана 05.1-13 (порог 3 «отдельно не подтверждён»), и в оговорке при самом BILL-07 в реестре требований.

#### 3. Пятизначная цена в строке цены на 375px

**Что сделать:** выставить `subscription_price` пятизначным значением и открыть `/billing` на 375px.
**Ожидается:** строка цены не переносится и не обрезается.
**Почему человек:** край 8 того же реестра, тоже вынесенный человеку: расчётный горизонтальный запас строки цены ≈9px. Приёмка шла на цене 3000 ₽, то есть край не был затронут.

---

### Сводка

**Цель фазы достигнута. Гэп раунда 1 закрыт, регрессий нет, все десять истин проверены по коду и по прогону.**

Единственный блокер прошлого раунда — «Подписка «None»» в кассе ЮKassa, в фискальном чеке и в кабинете мерчанта — устранён не заплаткой, а тем же приёмом, которым фаза уже отвечала на этот вопрос на соседней поверхности: назначение стало ПОСТОЯННОЙ строкой без мест вставки, и обе поверхности связаны тестом, а не соглашением. Свидетелей три, и они разного рода — тело вызова SDK, отсутствие места вставки при непустом тарифе и разбор AST; ни один не заменяет другой. Граница D-H при этом цела: исторический платёж с непустым тарифом по-прежнему печатает имя своей эпохи, и держит это живой тест, план 05.1-10 его не тронул.

Односторонняя дверь закрыта в правильном порядке — **решением человека до наката, а не кодом после него**. Владелец выбрал ветку `b-never-shorten` с записанным основанием («Необратимость дороже буквы»), ревизия получила строгий предикат, парный тест ПЕРЕИМЕНОВАН и требует теперь обратного прежнему, а строгость границы закреплена дважды: соседней точкой на секунду выше границы и утверждением об операторе внутри ИЗВЛЕЧЁННОГО текста SQL. Поимённый след заведён снимками до и после правки.

**Главное приобретение раунда — допущения стали числами.** Раунд 1 УТВЕРЖДАЛ, что боевая `subscriptions` пуста; прогон на копии боевой базы её ИЗМЕРИЛ: дальних бесплатных строк 0, платных 0, пользователей без активной строки 4 при 4 пользователях всего. Ревизия боя прочитана командой, а не взята из записи. Из этого следует факт, которого не было ни у кого до прогона: обе ветки решения CR-01 дают на сегодняшних боевых данных тождественный результат — решение владельца в силе, но наблюдаемых последствий не имеет. Условие «у плативших дата не изменилась» записано как **выполняющееся ПУСТО**, а не как проведённая поимённая сверка, и эта разница названа в артефакте прямо.

**Judgment-прохибиция получила настоящий вердикт.** Флаг `unverified-prohibition` раунда 1 снят не машинным перечитыванием, а человеком: четыре редакции текста о закрытии доступа прочитаны на живой странице, по каждой подтверждены все три критерия — 12 «да» из 12, ни одного отклонённого текста. Тексты сверены проверяющим с кодом дословно.

**Чего у приёмки нет — и почему это не спрятано.** Плановая форма («без разбивки по семи порогам приёмка не засчитывается») не выполнена: таблицы 7 × 4 нет, пороги 1, 3, 4 и 6 числами не сняты, состояние `paid` не заводилось. Исполнитель назвал это трижды и отказался дописывать таблицу машинным разбором — «пустая клетка честнее заполненной чужой рукой». Оркестратор дозамерил самый содержательный порог 7 (`badge.top >= priceLine.bottom`) числом в трёх состояниях: зазор ровно 0.0 везде, то есть бейдж начинается там, где кончается строка цены. Владелец объявил условие отметки наступившим. Это отступление здесь заведено ОВЕРРАЙДОМ, чтобы через фазу оно читалось со своим настоящим основанием, а не растворилось в прозе сводки.

**Что осталось человеку и почему статус не `passed`.** Три остаточных пункта — состояние `paid` и два края (длинное историческое имя пакета, пятизначная цена), — которые фаза САМА объявила вынесенными человеку и которые приёмочная сессия не затронула. Они не опровергают ни одного критерия приёмки и не блокируют следующую фазу; они блокируют только право написать «проверено» там, где никто не смотрел.

**Что стоит унести в следующий проход.** Три предупреждения касаются собственной защитной механики фазы, и все три подтверждены проверяющим по коду, а не приняты по ревизии: недостижимая пакетная ветка `create_payment` несёт непочиненную копию того же класса дефекта, ради которого закрывали CR-02; откат ревизии роняет `has_free_access`, а перечень «трёх потерь» — объявленный исчерпывающим и закреплённый тестом — называет три из четырёх; метрика активных пользователей после наката становится тождественна числу зарегистрированных. Ни одно из трёх сегодня человеку не видно, и потому ни одно не гэп — но каждое живёт ровно в том месте, где фаза объявила своё правило.

---

_Проверено: 2026-08-21T17:15:36Z_
_Проверяющий: Claude (gsd-verifier), раунд 2_
