---
phase: 05.1-edinaya-podpiska
plan: 14
type: execute
wave: 11
depends_on: ["05.1-11"]
gap_closure: true
files_modified:
  - .planning/phases/05.1-edinaya-podpiska/05.1-14-SUMMARY.md
autonomous: false
# BILL-09 («по истечении пробного срока доступ прекращается, и человеку СКАЗАНО СЛОВАМИ,
# что произошло и что оплатить») — половина, которая словами и выражается, а ассертом
# нет. BILL-10 («ровно ОДНА дата окончания») — то, что ревизия делает с датами трёх
# популяций боевой базы. Обе отметки поставлены по коду планом 05.1-12 и здесь НЕ
# пересматриваются; здесь закрывается то, чего код о себе доказать не может.
requirements: [BILL-09, BILL-10]
user_setup:
  - service: production-postgres
    why: "Дамп боевой базы достижим только командой против боевого стека, а исполнителю это запрещено прямо и без исключений. План 05.1-09 объявил этот прогон и ЗАБЛОКИРОВАЛ его именно здесь. Снять блок может только владелец"
    env_vars:
      - name: DATABASE_URL
        source: "Файл `.env` рядом с `docker-compose.prod.yml` — боевая база живёт ВНЕ compose-стека (сервиса базы в нём нет), и адрес читается только оттуда"
    dashboard_config:
      - task: "Снять дамп боевой базы и положить его ВНЕ репозитория; после прогона уничтожить и дамп, и копию"
        location: "Машина, с которой достижим боевой адрес базы"

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Текст о закрытии доступа прочитан ЧЕЛОВЕКОМ во всех четырёх редакциях, в которых продукт его произносит, и вердикт вынесен по каждой: это факт с продолжением, а не извещение о потере данных и не упрёк."
    - "Judgment-прохибиция плана 05.1-05 получила вердикт человека: машинное прочтение по ней НЕАВТОРИТЕТНО и в зелёный счёт не входит. Пункт закрывается тем же чтением, а не вторым, отдельным."
    - "Ревизия `0020` исполнена на КОПИИ боевой базы до того, как она применилась к бою: четыре условия наката проверены на настоящих данных, а не на посеянных."
    - "Классификация трёх популяций снята с копии ДО наката — после него отличить бывшего платящего от бывшего бесплатного нечем, колонку тарифа снимает та же ревизия."
    - statement: "Число активных бесплатных строк со сроком ДАЛЬШЕ пробного окна измерено на настоящих данных и сопоставлено с веткой, выбранной чекпойнтом плана 05.1-11."
      verification: backstop
  artifacts:
    - .planning/phases/05.1-edinaya-podpiska/05.1-14-SUMMARY.md   # вердикты и числа
  key_links:
    - "копия боевой базы → классификация трёх популяций ДО наката → сопоставление с веткой решения плана 05.1-11 → выкат ревизии 0020"
    - "`ACCESS_CLOSED_NOTICES` и панель раздела → человек → вердикт по judgment-прохибиции плана 05.1-05"
  prohibitions:
    - "Гейт доступа никогда не закрывает денежный путь."
    - "Прекращение доступа никогда не уничтожает данные пользователя."
    - "Денежный журнал не стирается и не переписывается задним числом (D-H)."
    - "Исторические значения `payments.plan` продолжают печататься так, как были проданы."
    - "Ни одна команда не выполняется против боевой базы, кроме снятия дампа, и снимает его владелец, а не исполнитель."
    - "Копия боевой базы не попадает в репозиторий ни одним файлом и уничтожается после прогона."
    - "Вердикт LLM по judgment-прохибиции не засчитывается зелёным ни при каком исходе."
---

<objective>
Закрыть три оставшихся пункта, которые может закрыть только человек: прочитать глазами
текст о закрытии доступа, вынести вердикт по judgment-прохибиции плана `05.1-05` и
исполнить ревизию `0020` на КОПИИ боевой базы до того, как она применится к бою.

Purpose: два из этих пунктов проверка вынесла человеку прямо, а третий — прогон на копии
— план `05.1-09` объявил и ЗАБЛОКИРОВАЛ: дампа, достижимого без обращения к боевому
стеку, не нашлось, `pg_dump` и `psql` на машине отсутствуют вовсе, и вместо прогона был
исполнен синтетический — на настоящем PostgreSQL, но с ПОСЕЯННЫМИ популяциями. Тот прогон
закрыл другую половину риска (диалект боевой СУБД), и это записано честно; он не видел ни
одной боевой строки и не поймал бы того, что ловит только настоящий дамп: нарушенных
инвариантов в исторических данных, дубликатов, пустых значений там, где модель их не
ждёт, объёма, на котором снятие колонки берёт долгую блокировку.

⚠️ ЭТОТ ПЛАН ИМЕЕТ СРОК. Бой стоит на `0019`, очередь невыкаченных пуста, и `0020`
применится ПЕРВОЙ на ближайшем выкате. Всё, что здесь измеряется на копии, после наката
измерить нечем.

⚠️ ЧЕТВЁРТЫЙ ПУНКТ РУЧНОЙ ПРОВЕРКИ ЗДЕСЬ НЕ ДУБЛИРУЕТСЯ. Решение по CR-01 — чекпойнт
задачи 1 плана `05.1-11`; этот план на него ССЫЛАЕТСЯ и продолжает его: задача 2 ниже
измеряет ту самую популяцию, о которой владельца спрашивали, и даёт последнюю возможность
пересмотреть решение до выката.

Output: вердикты человека по формулировкам, числа трёх популяций боевой базы и результат
наката на её копии.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md
@.planning/phases/05.1-edinaya-podpiska/05.1-VALIDATION.md
@.planning/phases/05.1-edinaya-podpiska/05.1-09-SUMMARY.md
</context>

## Artifacts this phase produces (план 05.1-14)

| Символ | Место | Вид |
|--------|-------|-----|
| вердикт по четырём редакциям текста о закрытии доступа | `05.1-14-SUMMARY.md` | запись решения человека |
| вердикт по judgment-прохибиции плана `05.1-05` | `05.1-14-SUMMARY.md` | запись решения человека |
| классификация трёх популяций боевой базы (числа, снятые ДО наката) | `05.1-14-SUMMARY.md` | измерение |
| результат наката `0020` на копии: четыре условия + `alembic current` | `05.1-14-SUMMARY.md` | измерение |
| сопоставление измеренной популяции с веткой решения плана `05.1-11` | `05.1-14-SUMMARY.md` | запись последствия |

Ни одного файла кода этот план не трогает.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 1: Прочитать текст о закрытии доступа и вынести вердикт по judgment-прохибиции</name>

  <read_first>
    app/pages/billing.py:51-62 (закрытое множество плашек причины: ДВЕ редакции — для того, кто никогда не платил, и для того, у кого кончился оплаченный срок),
    app/pages/billing.py:255-266 (какая из двух редакций попадает на экран и по какому признаку),
    app/templates/billing/balance.html:42-49 (тексты четырёх состояний панели — в том числе `trial` и `expired`),
    .planning/phases/05.1-edinaya-podpiska/05.1-VALIDATION.md § Manual-Only Verifications (строка о читаемости формулировки),
    .planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md § «Проверка прохибиций» (строка judgment-уровня и прямая оговорка о неавторитетности машинного вердикта) и § «Требуется ручная проверка» пункты 2 и 5
  </read_first>

  <what-built>
    Продукт произносит текст о закрытии доступа в ЧЕТЫРЁХ редакциях, и все четыре —
    закрытые множества с сервера, а не собранные в разметке строки: две плашки причины
    (для никогда не платившего и для того, у кого кончился оплаченный срок) и два текста
    панели (состояние пробного периода и состояние закрытого доступа). Параметр адреса
    `?expired=1` обработчиком НЕ читается — плашку выбирает сервер по факту оплат.
    Тесты проверяют наличие строки и суммы; понятность формулировки ассертом не
    выражается.
  </what-built>

  <how-to-verify>
    Прочитать ГЛАЗАМИ четыре текста — три на экране и, где состояние трудно
    воспроизвести, в исходнике, назвав это в записи:

    1. Плашка причины для человека, который НИКОГДА не платил: «Пробный период
       закончился, и доступ к системе закрыт. Оплатите подписку — объявления, расписания
       и группы сохранены, работа продолжится с того же места.»
    2. Плашка причины для человека, у которого кончился ОПЛАЧЕННЫЙ срок: «Оплаченный
       срок закончился, и доступ к системе закрыт. Оплатите подписку — объявления,
       расписания и группы сохранены, работа продолжится с того же места.»
    3. Текст панели в состоянии закрытого доступа: «Доступ закрыт. Оплатите подписку —
       работа продолжится с того же места.»
    4. Текст панели в состоянии пробного периода: «Пробный период. Когда он закончится,
       доступ закроется, пока подписка не оплачена.»

    Открыть `http://localhost:8000/billing?expired=1` в состоянии закрытого доступа и в
    состоянии пробного периода (как достигаются четыре состояния — см. план `05.1-13`,
    задача 1, пункт подготовки).

    ПРОЙДЕНО, ЕСЛИ по КАЖДОМУ из четырёх текстов человек отвечает «да» на три вопроса:
    - сказано ли, ЧТО произошло;
    - сказано ли, ЧТО сделать, чтобы продолжить;
    - читается ли это как ФАКТ С ПРОДОЛЖЕНИЕМ — не как извещение о потере данных и не
      как упрёк.

    ЭТИМ ЖЕ ЧТЕНИЕМ ЗАКРЫВАЕТСЯ JUDGMENT-ПРОХИБИЦИЯ ПЛАНА `05.1-05` — «текст о закрытии
    доступа не читается как потеря данных или как упрёк». Отдельной проверки она не
    требует и второй раз не проводится: предмет один. ⚠️ Машинное прочтение по ней
    НЕАВТОРИТЕТНО и зелёным не считается ни при каком исходе — вердикт выносит человек, и
    записывается именно его вердикт, а не согласие с чьим-либо разбором.

    ЧТО ЗАПИСАТЬ: по каждому из четырёх текстов — три ответа; отдельной строкой —
    вердикт по прохибиции («подтверждена» либо «отклонена») с обоснованием словами.
    Если хотя бы один текст отклонён — записать, ЧЕМ именно он читается неверно
    (потерей данных, упрёком или отсутствием продолжения), потому что это адрес правки
    копирайта, а не оценка.

    ⚠️ ПОСЛЕДСТВИЕ ОТКЛОНЕНИЯ НАЗВАНО ЗАРАНЕЕ. Отметка BILL-09 поставлена планом
    `05.1-12` по проверенному коду и здесь не пересматривается автоматически. Но если
    человек отклоняет формулировку, то половина требования «человеку сказано словами» не
    выполнена, и возврат BILL-09 в `In Progress` — отдельное решение владельца, которое
    записывается здесь же вместе с правкой копирайта как работа своего размера.
  </how-to-verify>

  <acceptance_criteria>
    - В SUMMARY приведена таблица из четырёх строк (по одной на редакцию текста) с тремя ответами в каждой.
    - Отдельной строкой записан вердикт по judgment-прохибиции плана `05.1-05`: «подтверждена» либо «отклонена», с обоснованием словами.
    - В SUMMARY прямо сказано, что вердикт вынес человек и что машинный разбор по этой прохибиции зелёным не считается.
    - Для каждого текста названо, прочитан он на экране или в исходнике; для прочитанных в исходнике названа причина.
    - При отклонении хотя бы одного текста названо, чем именно он читается неверно, и записано решение владельца о судьбе отметки BILL-09.
  </acceptance_criteria>

  <resume-signal>Напишите «подтверждено» с таблицей ответов, либо перечислите отклонённые тексты с указанием, чем каждый читается неверно</resume-signal>

  <reversibility rating="reversible">
    Чтение и вердикт. Ничего не меняет; при отклонении порождает правку копирайта,
    которая сама по себе обратима.
  </reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 2: Прогон ревизии `0020` на КОПИИ боевой базы и классификация трёх популяций</name>

  <precondition>
    Ревизия `0020` НЕ накачена на бой (`alembic current` = `0019`, факт от 2026-08-21 в
    `05.1-08-SUMMARY.md`), и текст ревизии приведён к решению владельца задачей 2 плана
    `05.1-11`. Прогон на копии имеет смысл только над ОКОНЧАТЕЛЬНЫМ текстом ревизии и
    только ДО выката: после наката на бой обе половины этой проверки теряют предмет.
  </precondition>

  <read_first>
    .planning/phases/05.1-edinaya-podpiska/05.1-09-SUMMARY.md § «Накат на копии боевой базы — 🚫 BLOCKED, и что сделано вместо» целиком (что именно было заблокировано, что исполнено взамен и чего тот прогон не доказывает),
    alembic/versions/0020_flat_subscription.py в редакции плана `05.1-11` (три популяции, порядок операций, тексты журнала наката),
    alembic/env.py (адрес базы берётся из переменной окружения и предпочитается значению из конфигурационного файла — этим и подменяется цель наката),
    docker-compose.prod.yml:1-30 (боевая база живёт ВНЕ compose-стека: сервиса базы в файле нет, адрес приезжает переменной окружения),
    docker-compose.yml:75-90 (мажорная версия PostgreSQL, на которой обязана подниматься копия),
    .planning/phases/05.1-edinaya-podpiska/05.1-VERIFICATION.md § «Требуется ручная проверка» пункт 4
  </read_first>

  <what-built>
    Ревизия `0020` правит ДАННЫЕ трёх популяций и необратима по данным: снимает тариф
    каждой подписки и две таблицы учёта сообщений. Она исполнялась на SQLite (парный
    тест, 18 утверждений) и один раз — на настоящем PostgreSQL с СИНТЕТИЧЕСКИМ посевом
    шести пользователей (план `05.1-09`). Ни одной боевой строки она не видела.
  </what-built>

  <how-to-verify>
    ⚠️ ДАМП СНИМАЕТ ВЛАДЕЛЕЦ. Исполнителю запрещено выполнять команды против боевого
    стека прямо и без исключений; именно на это план `05.1-09` и упёрся. Ни один шаг
    ниже не выполняется исполнителем самостоятельно.

    ШАГ 1 — ДАМП. Адрес боевой базы читается из переменной `DATABASE_URL` файла `.env`
    рядом с `docker-compose.prod.yml`: сервиса базы в боевом compose-файле нет вовсе,
    база внешняя. `pg_dump` на машине отсутствует, поэтому снимать дамп ОДНОРАЗОВЫМ
    контейнером той же мажорной версии PostgreSQL, что объявлена в `docker-compose.yml`,
    смонтировав каталог вывода ВНЕ репозитория. Дамп в репозиторий не кладётся ни одним
    файлом.

    ШАГ 2 — КОПИЯ. Поднять ВТОРОЙ одноразовый контейнер той же мажорной версии и
    восстановить в него дамп. Это и есть «копия боевой базы»; бой с этого момента не
    участвует ни в одной команде.

    ШАГ 3 — КЛАССИФИКАЦИЯ ДО НАКАТА. ⚠️ ЭТОТ ШАГ НЕОБРАТИМ ПО ВОЗМОЖНОСТИ: после наката
    колонка тарифа снята, и отличить бывшего платящего от бывшего бесплатного будет
    нечем. Снять на копии ТРИ числа и записать их:
    - активных строк с тарифом `free`, чей срок ДАЛЬШЕ, чем «сейчас + 5 суток», — это
      ровно та популяция, о которой владельца спрашивал чекпойнт плана `05.1-11`;
    - активных строк с ПЛАТНЫМ тарифом (срок им не трогается);
    - пользователей без активной строки подписки (им строку заводит ревизия).
    Плюс общее число пользователей и общее число строк подписок.

    ШАГ 4 — НАКАТ. Выставить `DATABASE_URL` на КОПИЮ и выполнить `alembic upgrade head`.

    ПРОЙДЕНО, ЕСЛИ выполняются все пять условий:
    1. команда завершается успешно;
    2. `alembic current` показывает `0020`;
    3. число активных строк подписок равно числу пользователей;
    4. у ранее плативших подписчиков дата окончания НЕ изменилась — сверять с числами
       шага 3 и поимённо, а не по количеству;
    5. журнал наката назвал ВСЕ ТРИ популяции числами, и эти числа совпали с
       классификацией шага 3.

    ШАГ 5 — СОПОСТАВЛЕНИЕ С РЕШЕНИЕМ. Сверить первое число шага 3 с веткой, выбранной
    чекпойнтом плана `05.1-11`:
    - число равно нулю → обе ветки дают на боевых данных ТОЖДЕСТВЕННЫЙ результат, и это
      записывается фактом: решение владельца остаётся в силе, но сегодня не имеет
      наблюдаемых последствий;
    - число больше нуля И выбрана ветка (а) «срок переставляется безусловно» → это
      ПОСЛЕДНЯЯ возможность пересмотреть решение до выката, и владельцу оно предъявляется
      вместе с числом и списком затронутых пользователей из поимённого следа. Пересмотр
      возвращает работу в план `05.1-11`;
    - число больше нуля И выбрана ветка (б) → записать, сколько строк ветка СОХРАНИЛА, —
      это и есть измеренная цена решения.

    ШАГ 6 — УНИЧТОЖЕНИЕ. Снести оба одноразовых контейнера и удалить дамп. Записать
    фактом, что и дамп, и копия уничтожены: копия боевой базы содержит адреса
    электронной почты и хеши паролей живых людей.

    ЕСЛИ ДАМП ВСЁ ЖЕ НЕДОСТИЖИМ — записать это ПРЯМО, как сделал план `05.1-09`, и
    назвать, от чего именно зависит разблокировка (доступ к боевому адресу базы, право
    выполнить снятие дампа, место под копию). Подменять прогон похожей проверкой
    запрещено: пункт объявлен единственной проверкой применимости ревизии к боевым
    ДАННЫМ, и любая подмена вернула бы зелёную галочку там, где проверки не было.
  </how-to-verify>

  <acceptance_criteria>
    - В SUMMARY записаны пять чисел классификации шага 3, снятых ДО наката, с указанием момента снятия.
    - В SUMMARY записан результат по каждому из пяти условий наката отдельной строкой, включая вывод `alembic current`.
    - Условие 4 подтверждено ПОИМЁННО (список идентификаторов пользователей и их дат до и после), а не количеством строк.
    - Числа журнала наката сопоставлены с классификацией шага 3, и совпадение либо расхождение названо.
    - Шаг 5 исполнен: первое число сопоставлено с веткой решения плана `05.1-11`, и последствие записано по одному из трёх названных исходов.
    - Записано фактом, что дамп и копия уничтожены, и что в репозиторий не попал ни один файл дампа (`git status --porcelain` пуст по файлам данных).
    - Если прогон не состоялся — в SUMMARY записано «🚫 BLOCKED» с указанием, от чего зависит разблокировка; подменяющей проверки нет ни одной.
  </acceptance_criteria>

  <resume-signal>Напишите «пройдено» с пятью числами и результатом по пяти условиям, либо «BLOCKED» с указанием, от чего зависит разблокировка</resume-signal>

  <reversibility rating="costly">
    Сам прогон обратим — копия одноразовая и уничтожается. Дорогой здесь ВОЗМОЖНОСТЬ, а
    не действие: классификация трёх популяций снимается только ДО наката на бой, и после
    выката её не восстановит ничто. Пропущенный прогон необратим по знанию, а не по
    данным.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| боевая база → дамп → копия | персональные данные живых людей (адреса электронной почты, хеши паролей) покидают боевой периметр |
| копия → репозиторий | файл дампа, попавший в репозиторий, попадает в историю навсегда |
| исполнитель → боевой стек | граница прямого запрета: ни одна команда исполнителя не имеет права дойти до боя |
| человек → judgment-прохибиция | вердикт по формулировке выносится человеком; машинное прочтение по ней неавторитетно |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-48 | Information Disclosure | дамп боевой базы вне боевого периметра | high | mitigate | Дамп снимает ВЛАДЕЛЕЦ, кладёт вне репозитория, восстанавливает в одноразовый контейнер и уничтожает вместе с копией; факт уничтожения записывается в SUMMARY, отсутствие файлов данных в репозитории проверяется приёмочным критерием |
| T-05.1-49 | Tampering | команда, адресованная бою вместо копии | high | mitigate | Цель наката подменяется переменной окружения, которую читает окружение Alembic; перед накатом адрес проверяется, и в SUMMARY записывается, что накат шёл на копию. Ни один шаг не выполняется исполнителем |
| T-05.1-50 | Repudiation | подмена непроведённого прогона похожей проверкой | high | mitigate | Прямой запрет в плане и обязательная запись «🚫 BLOCKED» при недостижимости дампа — тот же ход, которым план `05.1-09` не дал себе зазеленить непроведённую проверку |
| T-05.1-51 | Repudiation | зелёный вердикт по judgment-прохибиции от машины | medium | mitigate | Вердикт выносит человек; в SUMMARY записывается именно его ответ и прямая оговорка о неавторитетности машинного прочтения |
| T-05.1-40 | Denial of Service | долгая блокировка снятия колонки на боевом объёме | low | accept | Измеряется этим прогоном на копии: если снятие колонки на настоящем объёме окажется долгим, это будет видно по времени выполнения и записано числом |
| T-05.1-SC | Tampering | установка пакетов (npm/pip/cargo) | — | N/A | План не устанавливает ни одного пакета; используются готовые образы объявленной мажорной версии |
</threat_model>

<verification>
- Четыре редакции текста прочитаны человеком, по каждой — три ответа.
- Вердикт по judgment-прохибиции плана `05.1-05` вынесен человеком и записан.
- Классификация трёх популяций снята с копии ДО наката и записана числами.
- Пять условий наката проверены на копии; условие о неизменности дат платящих подтверждено поимённо.
- Измеренная популяция сопоставлена с веткой решения плана `05.1-11`.
- Дамп и копия уничтожены; в репозитории нет ни одного файла данных.

⚠️ **Чего этот план НЕ доказывает.** Прогон на копии не заменяет наблюдения за выкатом:
он говорит, что ревизия применима к сегодняшним боевым данным, и молчит о данных,
появившихся между снятием дампа и выкатом. Разрыв между этими двумя моментами обязан быть
назван в SUMMARY числом (сколько прошло времени), а не оставлен читателю.
</verification>

<success_criteria>
- Текст о закрытии доступа прочитан человеком во всех четырёх редакциях, и вердикт вынесен по каждой.
- Judgment-прохибиция плана `05.1-05` получила вердикт человека, а не машины.
- Ревизия `0020` исполнена на копии боевой базы до выката, и пять условий проверены.
- Три популяции классифицированы ДО наката — момент, после которого это невозможно, не упущен.
- Решение по CR-01 сопоставлено с измеренной популяцией, и последствие записано.
- При недостижимости дампа пункт назван заблокированным, а не закрыт подменяющей проверкой.
</success_criteria>

<output>
Create `.planning/phases/05.1-edinaya-podpiska/05.1-14-SUMMARY.md` when done
</output>
