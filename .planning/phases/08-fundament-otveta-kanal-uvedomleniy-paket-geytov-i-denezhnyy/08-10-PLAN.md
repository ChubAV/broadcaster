---
phase: 08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy
plan: 10
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - tests/test_pages/test_money_perimeter_gate.py
  - tests/test_infra/__init__.py
  - tests/test_infra/test_web_service_is_single_process.py
autonomous: true
requirements: [PAY-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Периметр опасных маршрутов собирается ОБХОДОМ, а не рукописным списком: гейт находит маршруты, ставящие задачу в очередь либо создающие платёж, и утверждает РАВЕНСТВО найденного множества объединению двух объявленных перечней"
    - "Третий опасный маршрут, добавленный будущей фазой, краснеет сам — он не попадёт ни в один перечень"
    - "На каждом маршруте периметра, где удержание требуется, оно СУЩЕСТВУЕТ и не зависит от клиентского атрибута: денежный путь держит ограничение схемы, путь рассылки — окно удержания в процессе"
    - "Запрет очереди запросов относится ТОЛЬКО к маршрутам периметра: осознанная очередь на маршруте редактора объявлений гейт не роняет"
    - "Неявное условие, на котором держится удержание пути рассылки, стало машинным: у боевого сервиса приложения не объявлено ни второго процесса, ни второй реплики"
    - "Гейт читает исходники и артефакт развёртывания текстом, не поднимая приложения и не импортируя ни одного его модуля"
    - "Зубы обоих гейтов доказаны: группа -k control подаёт изменённые копии и утверждает красный на нарушении"
  artifacts:
    - path: "tests/test_pages/test_money_perimeter_gate.py"
      provides: "Периметр опасных маршрутов, собранный обходом; проверка существования удержания; запрет очереди запросов на периметре"
      contains: "HOLD_REQUIRED"
    - path: "tests/test_infra/test_web_service_is_single_process.py"
      provides: "Гейт артефакта развёртывания: у сервиса приложения один процесс и одна реплика"
      contains: "docker-compose.prod.yml"
  key_links:
    - from: "tests/test_pages/test_money_perimeter_gate.py"
      to: "app/pages/history.py"
      via: "удержание пути рассылки — окно в процессе, открываемое до всех проверок"
      pattern: "_claim_retry_slot"
    - from: "tests/test_pages/test_money_perimeter_gate.py"
      to: "app/services/payment_service.py"
      via: "удержание денежного пути — ограничение схемы, названное константой имени"
      pattern: "OPEN_INTENT_INDEX_NAME"
    - from: "tests/test_infra/test_web_service_is_single_process.py"
      to: "docker-compose.prod.yml"
      via: "чтение блока сервиса приложения текстом"
      pattern: "web:"
  prohibitions:
    - requirement_id: PAY-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT выдавать клиентский атрибут блокировки за защиту: удержание обязано существовать на сервере НЕЗАВИСИМО от разметки, иначе двойное нажатие на денежном пути и на пути рассылки защищено ровно ничем"
    - requirement_id: PAY-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT оставить условие, на котором держится удержание, незаписанным: удержание в памяти процесса молча перестаёт работать в день, когда у сервиса появляется второй процесс, и человек об этом не узнает"
---

<objective>
Замкнуть периметр опасных маршрутов машинным гейтом и сделать МАШИННЫМ неявное
условие, на котором сегодня держится серверное удержание пути рассылки.

Purpose: критерий 5 фазы — «серверное удержание на денежном пути и на пути
рассылки существует НЕЗАВИСИМО от клиентского атрибута, а очередь запросов на
этих маршрутах запрещена гейтом». Веха повышает риск двойной отправки: узкое
окно между нажатием и перезагрузкой было де-факто защитой, и она убирается.
Output: гейт периметра, замкнутый на себя, и гейт артефакта развёртывания.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-CONTEXT.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-PATTERNS.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-05-SUMMARY.md
@tests/test_pages/test_impersonation_gate.py
</context>

## Artifacts this phase produces

| Артефакт | Где |
|---|---|
| `tests/test_pages/test_money_perimeter_gate.py` | новый файл гейта периметра |
| `HOLD_REQUIRED` — отображение «маршрут → чем именно держится» | там же |
| `HOLD_NOT_BUILT_YET` — отображение «маршрут → почему удержание пришлось бы построить» | там же |
| `PERIMETER_ROUTES` — число, собранное собственным обходом | там же |
| `tests/test_infra/__init__.py`, `tests/test_infra/test_web_service_is_single_process.py` | новый каталог и файл гейта артефакта развёртывания |

## Периметр, снятый обходом при планировании

Кандидаты — маршруты, ставящие задачу в очередь либо создающие платёж:

| Маршрут | Признак | Множество |
|---|---|---|
| обработчик подписки в `app/pages/billing.py` (объявление на строке 316) | создаёт платёж | `HOLD_REQUIRED` — держит ограничение схемы (план 08-03/08-05) |
| обработчик повтора отправки в `app/pages/history.py` (постановка задачи на строке 1031) | ставит задачу | `HOLD_REQUIRED` — держит окно удержания в процессе (`_claim_retry_slot`) |
| синхронизация групп WhatsApp в `app/pages/accounts.py` (постановка на строке 463) | ставит задачу | `HOLD_NOT_BUILT_YET` |
| синхронизация групп MAX в `app/pages/accounts.py` (постановка на строке 648) | ставит задачу | `HOLD_NOT_BUILT_YET` |
| постановка задачи подключения в `app/pages/accounts.py` (строка 738) | ставит задачу | `HOLD_NOT_BUILT_YET` |

Расширение периметра на три последних отвергнуто D-08 как расширение объёма: им
удержание пришлось бы ПОСТРОИТЬ, а не проверить. Но из ПЕРИМЕТРА они не
выпадают — они попадают во второй перечень с обоснованием, иначе «не проверено»
стало бы неотличимо от «не найдено».

<tasks>

<task type="auto">
  <name>Task 1: Гейт периметра опасных маршрутов, замкнутый на себя</name>
  <files>tests/test_pages/test_money_perimeter_gate.py</files>
  <read_first>
    - `tests/test_pages/test_impersonation_gate.py` (ЦЕЛИКОМ — ОБРАЗЕЦ: `test_every_mutating_route_is_classified` (равенство объединения найденному), форма перечня с обоснованием на КАЖДУЮ запись, `test_the_gate_imports_no_application_module`, группа `-k control`, раздел «⚠️ ЧЕГО ГЕЙТ НЕ ВИДИТ» в докстринге)
    - `app/pages/history.py` (строки 400-415 — реестр удержаний и абзац «Уборку делает `_claim_retry_slot`»; строки 538-585 — `_claim_retry_slot` и `_release_retry_slot`; строка 951 — где удержание открывается; строка 1031 — постановка задачи)
    - `app/pages/accounts.py` (строки 455-470, 640-655, 730-745 — три постановки задачи в очередь)
    - `app/pages/billing.py` (строки 316-400 — обработчик подписки: единственный маршрут, создающий платёж)
    - `app/services/payment_service.py` (состояние после плана 08-05 — `OPEN_INTENT_INDEX_NAME` и порядок «резерв → сеть»)
    - `app/templates/ads/form.html` (строки 60-70 — осознанная очередь запросов с обоснованием на восемь строк; маршрут НЕ денежный и НЕ рассылочный, и гейт «нигде нет очереди» покраснел бы зря)
  </read_first>
  <action>
Создать `tests/test_pages/test_money_perimeter_gate.py` — гейт множества по
синтаксическому дереву Python, по форме `test_impersonation_gate.py`.

Обход собирает КАНДИДАТОВ ПЕРИМЕТРА рекурсивным разбором `app/pages/**/*.py`:
функция под декоратором маршрута, в теле которой есть постановка задачи в
очередь (вызов с атрибутом `send_task`) либо вызов создания платежа. Множество
собирается ТЕМ ЖЕ обходом, которым проверяется правило, — этим гейт замкнут на
себя (механика G-12), и рукописный список этого не даёт: третий опасный маршрут,
добавленный будущей фазой, не попадёт ни в один перечень и уронит тест.

Два объявленных перечня, каждый — отображение «ключ маршрута → обоснование»,
по форме перечня `test_access_gate.py`:
- `HOLD_REQUIRED` — удержание требуется И существует; обоснование называет, ЧЕМ
  именно оно держится;
- `HOLD_NOT_BUILT_YET` — удержания нет, и это ЗАПИСАННОЕ решение (D-08): этим
  маршрутам его пришлось бы построить, а не проверить; обоснование называет
  это прямо.

Гейты:
- `test_the_number_of_perimeter_routes_is_the_declared_one` — число найденных
  равно `PERIMETER_ROUTES`;
- `test_every_perimeter_route_is_classified` — объединение двух перечней РАВНО
  множеству найденных;
- `test_the_two_sets_do_not_overlap`;
- `test_every_required_hold_exists_in_the_source` — для каждого маршрута из
  первого перечня в исходнике находится названное удержание: для пути рассылки —
  вызов открытия окна удержания в теле обработчика ДО постановки задачи; для
  денежного пути — вызов создания платежа в теле обработчика и объявление
  константы имени ограничения в модуле сервиса. Сообщение об отказе обязано
  называть, ЧТО именно не найдено;
- `test_no_perimeter_route_carries_a_queued_request_sync` — ни одна форма,
  чей адрес действия ведёт на маршрут периметра, не объявляет очередь запросов.
  ⚠️ Правило применяется ТОЛЬКО к маршрутам периметра: на форме редактора
  объявлений очередь стоит осознанно и с обоснованием на восемь строк, маршрут
  редактора ни денежный, ни рассылочный, и гейт «нигде нет очереди» покраснел бы
  зря;
- `test_the_gate_imports_no_application_module`.

Группа контроля (`-k control`):
- копия исходника с новым маршрутом, ставящим задачу в очередь и не объявленным
  ни в одном перечне, — гейт краснеет;
- копия, где у маршрута периметра снято открытие окна удержания, — гейт
  краснеет;
- копия разметки, где форма маршрута периметра объявила очередь запросов, —
  гейт краснеет;
- копия разметки, где очередь объявлена на маршруте ВНЕ периметра, — гейт
  остаётся ЗЕЛЁНЫМ (это и есть доказательство, что граница правила проведена);
- настоящее дерево — всё зелёное.

Докстринг обязан нести раздел «⚠️ ЧЕГО ГЕЙТ НЕ ВИДИТ»: постановку задачи,
спрятанную за вспомогательной функцией соседнего модуля, и создание платежа
через псевдоним импорта. Обе формы запрещаются тем же гейтом, потому что гейт,
который чего-то не видит, обязан требовать, чтобы этого и не было.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_money_perimeter_gate.py -v && uv run pytest tests/test_pages/test_money_perimeter_gate.py -k control -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: файл объявляет `HOLD_REQUIRED`, `HOLD_NOT_BUILT_YET` и `PERIMETER_ROUTES`
    - Behavior: `PERIMETER_ROUTES` равно 5, и объединение двух перечней равно множеству, найденному обходом
    - Behavior: каждая запись обоих перечней несёт непустую строку обоснования — утверждается тестом
    - Behavior: для обоих маршрутов первого перечня удержание найдено в исходнике по имени
    - Behavior: гейт краснеет на копии с новым неклассифицированным опасным маршрутом и остаётся зелёным на копии с очередью вне периметра
    - Source: `grep -c 'import app' tests/test_pages/test_money_perimeter_gate.py` == 0
    - Test command: `uv run pytest tests/test_pages/test_money_perimeter_gate.py -k control -v` собирает не менее пяти тестов и завершается кодом 0
  </acceptance_criteria>
  <done>Периметр собран обходом и замкнут на себя; граница правила очереди доказана обеими сторонами.</done>
</task>

<task type="auto">
  <name>Task 2: Гейт артефакта развёртывания — один процесс, одна реплика</name>
  <files>tests/test_infra/__init__.py, tests/test_infra/test_web_service_is_single_process.py</files>
  <read_first>
    - `docker-compose.prod.yml` (блок сервиса `web`, строки 91-115: `command`, `container_name`, `expose`, `healthcheck`; ни `--workers`, ни `deploy.replicas` сегодня нет)
    - `app/pages/history.py` (строки 400-415 — докстринг реестра удержаний, который НАЗЫВАЕТ это условие словами и нигде его не проверяет; именно этот пробел закрывает задача)
    - `tests/test_templates/test_htmx_inventory.py` (форма гейта, читающего файл-исходник и утверждающего его состав)
    - `tests/test_migrations/test_deploy_applies_migrations_before_serving.py` (ближайший в проекте гейт по артефакту развёртывания — стиль и расположение)
  </read_first>
  <action>
Создать каталог `tests/test_infra/` с `__init__.py` (по образцу прочих каталогов
суиты) и файл `test_web_service_is_single_process.py`.

Гейт читает `docker-compose.prod.yml` ТЕКСТОМ и работает по ОБЛАСТИ, а не по
всему файлу: извлекается блок сервиса приложения — от строки его объявления до
следующего объявления сервиса того же уровня отступа, — и утверждения делаются
внутри этой области. Область важна: другие сервисы файла имеют полное право
объявлять и число процессов, и число реплик, и запрет на весь файл был бы
неисполним и неверен.

⚠️ Разбор ведётся ТЕКСТОМ, а не библиотекой YAML. Основание — рамка вехи: ни
одной новой Python-зависимости. Библиотека сегодня доступна в окружении лишь
транзитивно, то есть её присутствие не объявлено и может исчезнуть с чужим
обновлением; гейт, падающий на импорте, защищает ровно ничего.

Гейты:
- `test_the_web_service_block_is_found` — область найдена и непуста. Без этого
  утверждения все последующие были бы вакуумно-зелёными при переименовании
  сервиса;
- `test_the_web_service_runs_a_single_process` — в области нет объявления числа
  рабочих процессов сервера приложений;
- `test_the_web_service_runs_a_single_replica` — в области нет объявления числа
  реплик;
- `test_the_hold_precondition_is_named_in_the_source` — докстринг реестра
  удержаний в слое страниц по-прежнему НАЗЫВАЕТ это условие словами: гейт и
  объяснение обязаны существовать вместе, иначе через один рефакторинг гейт
  останется без предмета.

Докстринг файла обязан выписать ПОЛНОЕ рассуждение, потому что этот гейт
защищает не код, а решение:
- сегодня удержание пути рассылки живёт в памяти процесса и РАБОТАЕТ, потому что
  боевой сервис — один контейнер с одним процессом сервера приложений;
- оно МОЛЧА перестанет работать в день, когда появится второй процесс или вторая
  реплика: два процесса не видят реестра друг друга, и второе нажатие пройдёт;
- собственный докстринг функции удержания это условие называет, но нигде не
  проверяет — до этого гейта условие было записанным, но не машинным;
- переезд удержания в общее хранилище отложен ОСОЗНАННО (D-07) с названным
  условием возврата, и ЭТОТ ГЕЙТ — ровно тот сигнал, который условие обнаружит;
- ⚠️ ЧЕГО ГЕЙТ НЕ ВИДИТ: развёртывание, выполненное не этим файлом (ручной
  запуск, оркестратор, второй файл композиции). Это принятая граница, и она
  выписана здесь, а не оставлена на догадку.

Группа контроля (`-k control`): копия файла композиции с объявленным числом
рабочих процессов в области сервиса приложения — гейт краснеет; копия с
объявленным числом реплик — краснеет; копия, где то же объявление стоит в
области ДРУГОГО сервиса, — гейт остаётся ЗЕЛЁНЫМ; копия с переименованным
сервисом — краснеет тест поиска области; настоящий файл — всё зелёное.
  </action>
  <verify>
    <automated>uv run pytest tests/test_infra/ -v && uv run pytest tests/test_infra/ -k control -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/test_infra/__init__.py` существует
    - Source: `grep -c 'import yaml' tests/test_infra/test_web_service_is_single_process.py` == 0
    - Behavior: область сервиса приложения найдена и непуста — тест поиска области зелёный
    - Behavior: в области нет объявления числа рабочих процессов и нет объявления числа реплик — оба теста зелёные на настоящем файле
    - Behavior: гейт краснеет на копии файла с объявлением числа процессов в области сервиса приложения и ОСТАЁТСЯ ЗЕЛЁНЫМ, когда то же объявление стоит в области другого сервиса
    - Behavior: гейт краснеет на копии с переименованным сервисом
    - Test command: `uv run pytest tests/test_infra/ -k control -v` собирает не менее пяти тестов и завершается кодом 0
    - Test command: `uv run pytest tests/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Неявное условие удержания стало машинным; область гейта проведена и доказана обеими сторонами.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → опасный маршрут | двойное нажатие приходит двумя независимыми запросами; клиентский атрибут блокировки защитой не является |
| артефакт развёртывания → работающая защита | удержание в памяти процесса верно ровно при одном процессе, и это свойство развёртывания, а не кода |
| разметка формы → рантайм htmx | очередь запросов превращает второе нажатие в отложенную вторую отправку вместо отброшенной |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-40 | Tampering | двойное нажатие на денежном пути обходит клиентскую блокировку | high | mitigate | удержание денежного пути — ограничение схемы (планы 08-03/08-05), существующее независимо от разметки; гейт утверждает его наличие в исходнике по имени |
| T-08-41 | Tampering | двойное нажатие на пути рассылки отправляет объявление дважды | high | mitigate | окно удержания открывается ДО всех проверок и сохраняется при успешной постановке; гейт утверждает его наличие в теле обработчика ДО постановки задачи |
| T-08-42 | Tampering | очередь запросов превращает отброшенное второе нажатие в отложенную вторую отправку | high | mitigate | запрет очереди на маршрутах периметра, доказанный обеими сторонами: краснеет на периметре, зелёный вне его |
| T-08-43 | Denial of Service | удержание молча перестаёт работать при втором процессе или второй реплике | high | mitigate | гейт по артефакту развёртывания, областно ограниченный блоком сервиса приложения; условие возврата решения D-07 названо прямо |
| T-08-44 | Repudiation | третий опасный маршрут, добавленный будущей фазой, оказывается незащищённым по умолчанию | high | mitigate | периметр собирается ОБХОДОМ, и объединение перечней обязано равняться найденному; неклассифицированный маршрут роняет тест |
| T-08-45 | Tampering | постановка задачи, спрятанная за вспомогательной функцией, невидима обходу | medium | mitigate | форма запрещена тем же гейтом; граница выписана в докстринге |
| T-08-SC | Tampering | установка пакетов npm/pip/cargo | high | accept | план не устанавливает ни одного пакета; гейт развёртывания намеренно не использует даже транзитивно доступную библиотеку разбора |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_money_perimeter_gate.py tests/test_infra/ -v` — зелёный
- `uv run pytest tests/test_pages/test_money_perimeter_gate.py tests/test_infra/ -k control -v` — зелёный
- `uv run pytest tests/ -q` — зелёный
</verification>

<success_criteria>
- Периметр собран обходом, объединение перечней равно найденному
- На обоих маршрутах, где удержание требуется, оно найдено в исходнике по имени
- Запрет очереди запросов ограничен периметром и доказан обеими сторонами
- Условие, на котором держится удержание, стало машинным

## Покрытие пакета гейтов этим планом

| Гейт | Что утверждает | Требование |
|---|---|---|
| G-18 | периметр опасных маршрутов классифицирован; удержание существует независимо от разметки; очередь запросов на периметре запрещена | PAY-02 |
| гейт D-07 | у боевого сервиса приложения один процесс и одна реплика | PAY-02 |

Каждый имеет свой случай в группе `-k control`, включая ОТРИЦАТЕЛЬНЫЙ случай
границы: правило не срабатывает там, где срабатывать не должно.
</success_criteria>

## Flagged planner assumptions (edge probe, unresolved)

Зондирование краёв вернуло по PAY-02 строку `unclassified — review manually`.
Записана допущением, а не выброшена: **предполагается, что «опасный маршрут»
определяется двумя признаками — постановкой задачи в очередь и созданием
платежа — и что этих двух достаточно.** Маршрут, необратимо меняющий состояние
без очереди и без платежа (например, удаление), в периметр не попадает и
удержания не получает. Граница выбрана вслед за D-08 и требует решения человека,
если появится третий класс необратимого действия.

<output>
Create `.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-10-SUMMARY.md` when done
</output>
