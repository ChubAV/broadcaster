---
phase: 08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - app/templates/includes/notice_area.html
  - app/templates/includes/notice_oob.html
  - app/templates/includes/htmx_error_banner.html
  - app/templates/base.html
  - app/templates/auth_base.html
  - app/pages/common.py
  - app/pages/htmx.py
  - tests/test_pages/test_shell.py
  - tests/test_pages/test_notices_surface.py
autonomous: true
requirements: [FOUND-06, QUAL-03, GATE-08]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "На каждой странице обоих шеллов существуют ровно две области уведомления: вежливая с role=status и aria-live=polite и настойчивая с role=alert и aria-live=assertive"
    - "При отсутствии кода уведомления разметки плашки нет вовсе — область присутствует пустой, а плашка не рисуется"
    - "Неизвестный код уведомления не рисует ничего: владелец ссылки не может напечатать пользователю сообщение о событии, которого не было"
    - "Внеполосный блок уведомления подменяет СОДЕРЖИМОЕ области, а не её узел — форма свопа объявлена явно и проверена работой на настоящем ответе respond()"
    - "Плашка отказа сервера и плашка обрыва связи приходят с сервера ЗАРАНЕЕ ОТРИСОВАННЫМИ и скрытыми; сценарий только снимает атрибут скрытия и разметки не собирает"
    - "Ответ 422 плашку аварии НЕ поднимает: канал различает ошибку заполнения формы и отказ сервера ПО КОДУ ответа"
    - "Обработчиков ошибок два, а не один: без второго обрыв связи неотличим от сломанной кнопки"
    - "Каждое из трёх новых включений подключено в оба шелла ровно по одному разу — источник один, второй копии не заводится"
  artifacts:
    - path: "app/templates/includes/notice_area.html"
      provides: "Две aria-live-области уведомления, единственный владелец их разметки"
      contains: "aria-live"
    - path: "app/templates/includes/notice_oob.html"
      provides: "Внеполосная форма уведомления для ответа htmx — подмена содержимого обеих областей"
      contains: "hx-swap-oob"
    - path: "app/templates/includes/htmx_error_banner.html"
      provides: "Скрытые заготовки плашек отказа сервера и обрыва связи плюс два обработчика событий"
      contains: "htmx:sendError"
  key_links:
    - from: "app/templates/base.html"
      to: "app/templates/includes/notice_area.html"
      via: "включение областей в основной шелл"
      pattern: "includes/notice_area.html"
    - from: "app/templates/auth_base.html"
      to: "app/templates/includes/notice_area.html"
      via: "включение областей в шелл авторизации — `?notice=password_reset_done` живёт на /login"
      pattern: "includes/notice_area.html"
    - from: "app/pages/common.py"
      to: "app/pages/notices.py"
      via: "Jinja-глобаль notice_for — единственный вход шаблона в реестр"
      pattern: "notice_for"
    - from: "app/pages/htmx.py"
      to: "app/templates/includes/notice_oob.html"
      via: "respond() приклеивает внеполосный блок к фрагменту, когда задан код уведомления"
      pattern: "notice_oob.html"
  prohibitions:
    - requirement_id: QUAL-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT поднимать плашку аварии на обычной ошибке заполнения формы: приученный игнорировать красное глаз перестаёт видеть настоящую аварию, и канал теряет ровно ту ценность, ради которой заводится"
    - requirement_id: FOUND-06
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT рисовать пустую рамку или подставлять значение параметра адреса вместо текста: отсутствие кода означает отсутствие плашки, а не плашку без слов"
---

<objective>
Завести видимую поверхность канала: две `aria-live`-области уведомления в обоих
шеллах, внеполосную форму подмены их содержимого и заранее отрисованные скрытые
заготовки плашек отказа сервера и обрыва связи.

Purpose: критерий 2 фазы («OOB-область подменяет СОДЕРЖИМОЕ, заведены вежливая
и настойчивая области, при отсутствии кода разметки нет вовсе») и критерий 3
(«плашка приходит с сервера заранее отрисованной скрытой заготовкой, а
обработчики только переключают атрибут»).
Output: три включения-владельца, подключённые в оба шелла; Jinja-глобаль входа в
реестр; приклейка внеполосного блока в `respond()`; расширенный `test_shell.py`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-CONTEXT.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-PATTERNS.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-01-SUMMARY.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-02-SUMMARY.md
@app/templates/includes/htmx_config.html
@app/templates/components/alert.html
</context>

## Artifacts this phase produces

| Артефакт | Где |
|---|---|
| `app/templates/includes/notice_area.html` | новое включение — две области |
| `app/templates/includes/notice_oob.html` | новое включение — внеполосная форма |
| `app/templates/includes/htmx_error_banner.html` | новое включение — заготовки плашек + два обработчика |
| `id="notice"` (вежливая, `role="status"`, `aria-live="polite"`) | `notice_area.html` |
| `id="notice-alert"` (настойчивая, `role="alert"`, `aria-live="assertive"`) | `notice_area.html` |
| `id="htmx-failure-server"`, `id="htmx-failure-network"` | `htmx_error_banner.html` |
| Jinja-глобаль `notice_for` | `app/pages/common.py` |
| `_notice_oob(code) -> str` и приклейка в `respond()` | `app/pages/htmx.py` |
| `tests/test_pages/test_notices_surface.py` | новый файл тестов |

Потребители кодов уведомления заводятся планом 08-06; реестр — планом 08-02.

<!-- planner-discipline-allow: innerHTML -->

<tasks>

<task type="auto">
  <name>Task 1: Две aria-live-области в обоих шеллах и вход шаблона в реестр</name>
  <files>app/templates/includes/notice_area.html, app/templates/base.html, app/templates/auth_base.html, app/pages/common.py, tests/test_pages/test_shell.py</files>
  <read_first>
    - `app/templates/base.html` (строки 60-80 — блок `{% if impersonation %}`: образец «нет ключа — нет разметки»; строки 205-240 — `<div data-main>`, `<header data-head>`, `<div data-body>` — место включения)
    - `app/templates/auth_base.html` (ЦЕЛИКОМ — второй шелл, `<section class="auth-card">` и `{% block content %}`)
    - `app/templates/includes/htmx_config.html` (шапка — абзац «ПОЧЕМУ ОДИН ФАЙЛ, А НЕ ДВА ЛИТЕРАЛЬНЫХ БЛОКА В ДВУХ ШЕЛЛАХ (D-01)»: правило владения, которое новые включения обязаны повторить)
    - `app/templates/components/alert.html` (макрос `alert(message, variant='error')` — `role` уже выводится из варианта, переучивать не нужно)
    - `app/templates/auth/login.html` (строки 8-12 — сегодняшнее чтение параметра адреса прямо в шаблоне: приём, который включение обобщает)
    - `app/pages/common.py` (строки 250-340, 360-400 — регистрация глобалей `env.globals`, стиль и порядок; строки 100-130 — предупреждение о перезаписи `env.globals` следующим приложением в процессе)
    - `app/pages/notices.py` (создан планом 08-02 — `notice_for`, `Notice`)
  </read_first>
  <action>
Создать `app/templates/includes/notice_area.html` — ЕДИНСТВЕННОГО владельца
разметки областей уведомления, и подключить его в оба шелла.

Содержимое включения:
- импорт макроса плашки (`{% from "components/alert.html" import alert %}`) —
  компоненту контекст вызывающего шаблона недоступен, значение приходит
  параметром (приём уже применён в `auth/login.html`);
- чтение кода из адресной строки одной строкой: `request.query_params.get('notice')`,
  и разрешение его через глобаль `notice_for` — ЗНАЧЕНИЕ ПАРАМЕТРА В РАЗМЕТКУ НЕ
  УХОДИТ НИКОГДА, оно только ВЫБИРАЕТ запись;
- вежливая область: `<div id="notice" role="status" aria-live="polite">`,
  содержащая вызов `alert(...)` ТОЛЬКО когда запись найдена и её вариант НЕ
  `error`;
- настойчивая область: `<div id="notice-alert" role="alert" aria-live="assertive">`,
  содержащая вызов `alert(...)` ТОЛЬКО когда запись найдена и её вариант `error`;
- Jinja-комментарий (⚠️ именно Jinja, НЕ HTML: HTML-комментарий приехал бы в
  документ и удвоил счёт вхождений, уронив гейты — урок `htmx_config.html`) с
  четырьмя абзацами: почему один файл на два шелла; почему областей ДВЕ и чем
  вежливая отличается от настойчивой; почему «нет кода — нет разметки» (умолчание
  «показать» запрещено прямо, как у полосы имперсонации); почему деление идёт ПО
  ВАРИАНТУ записи, а не по новой оси — макрос `alert()` уже выводит `role` из
  варианта, и вторая ось разошлась бы с первой.

⚠️ Узел области существует ВСЕГДА, даже пустой, и это не противоречит правилу
«нет кода — нет разметки»: правило относится к ПЛАШКЕ. Стабильный узел по `id`
нужен внеполосной подмене содержимого (задача 2), а плашки в нём при отсутствии
кода нет ни одной. Записать это абзацем прямо, иначе следующий читатель снимет
узел и сломает свою же подмену.

Подключить включение:
- `app/templates/base.html` — первым дочерним узлом `<div data-body>`, ДО
  `{% block content %}`: исход действия обязан читаться раньше содержимого
  раздела (тот же довод, по которому `error_message` в `billing/balance.html`
  стоит выше цены и даты);
- `app/templates/auth_base.html` — внутри `<section class="auth-card">`, после
  `<p class="auth-subtitle">` и ДО `{% block content %}`.

Зарегистрировать в `app/pages/common.py` глобаль
`templates.env.globals["notice_for"] = notice_for` (импорт из `app.pages.notices`),
рядом с существующими глобалями и в их стиле. Обратного импорта быть не должно:
`notices.py` не знает о `common.py`.

Дописать в `tests/test_pages/test_shell.py` группу утверждений об областях:
обе области присутствуют в ОБОИХ шеллах ровно по одному разу; каждая несёт свой
`role` и свой `aria-live`; при запросе без параметра ни в одной из них нет
плашки; при известном коде плашка появляется РОВНО В ОДНОЙ из двух по варианту
записи; при неизвестном коде разметки плашки нет ни в одной; включение
подключено в каждый шелл ровно один раз (по форме
`test_htmx_runtime_tag_has_single_source`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -v -k "notice or shell"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'includes/notice_area.html' app/templates/base.html` == 1 и `grep -c 'includes/notice_area.html' app/templates/auth_base.html` == 1
    - Source: `grep -c 'aria-live="polite"' app/templates/includes/notice_area.html` == 1 и `grep -c 'aria-live="assertive"' app/templates/includes/notice_area.html` == 1
    - Source: `grep -c 'notice_for' app/pages/common.py` ≥ 1
    - Source: в `app/templates/includes/notice_area.html` нет HTML-комментариев — `grep -c '<!--' app/templates/includes/notice_area.html` == 0
    - Behavior: `GET /profile` без параметра — тело содержит `id="notice"` и `id="notice-alert"` ровно по одному разу и НЕ содержит `class="alert` внутри ни одной из областей
    - Behavior: `GET /profile?notice=profile_saved` — текст записи `profile_saved` присутствует внутри области `notice`, а область `notice-alert` пуста
    - Behavior: `GET /billing?notice=payment_failed` — текст записи присутствует внутри области `notice-alert`, а область `notice` пуста
    - Behavior: `GET /profile?notice=%3Cscript%3E` — ни в одной области нет плашки, и подстроки `<script>` из параметра в теле нет
    - Behavior: `GET /login?notice=password_reset_done` — текст записи присутствует (шелл авторизации получает канал наравне с основным)
    - Test command: `uv run pytest tests/test_pages/test_shell.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Обе области существуют в обоих шеллах; плашка появляется только по известному коду.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Внеполосная форма уведомления и её приклейка в respond()</name>
  <files>app/templates/includes/notice_oob.html, app/pages/htmx.py, tests/test_pages/test_notices_surface.py</files>
  <read_first>
    - `app/templates/ads/includes/autosave_response.html` (ЦЕЛИКОМ — форма внеполосного ответа: три блока с `id`, четвёртый признак собран условием Jinja; здесь же — почему форма подмены УЗЛА сосуществует с требуемой FOUND-06 формой подмены СОДЕРЖИМОГО)
    - `app/pages/htmx.py` (создан планом 08-01 — `respond()`, `_with_notice`, `location_response`)
    - `app/pages/accounts.py` (строки 47-50 — `_connect_status`: рендер макроса через окружение Jinja вне обработчика; ближайший приём для сборки строки фрагмента)
    - `app/pages/notices.py` (создан планом 08-02)
    - `app/templates/includes/notice_area.html` (создан задачей 1 — `id` областей, которые подменяет форма)
  </read_first>
  <behavior>
    - Тест 1: `respond(request, redirect="/history", notice="retry_queued", fragment=<фабрика, отдающая HTMLResponse с телом "ROW">)` на запросе htmx возвращает тело, содержащее и `ROW`, и внеполосный блок с текстом записи `retry_queued`.
    - Тест 2: тот же вызов без `notice` возвращает тело РОВНО `ROW` — внеполосный блок не приклеивается.
    - Тест 3: внеполосный блок объявляет подмену СОДЕРЖИМОГО области, а не её узла, и целится в ту область, которая соответствует варианту записи.
    - Тест 4: внеполосный блок для варианта `error` целится в настойчивую область, для остальных — в вежливую; вторая область в блоке не присутствует.
    - Тест 5: `respond(..., notice=...)` при ответе, не являющемся HTML (`fragment` отдаёт `Response` с иным `media_type`), приклейку НЕ делает и поднимает `ValueError` — молчаливая порча тела запрещена.
    - Тест 6: без заголовка htmx `fragment` не вызывается вовсе, а код едет адресом (`?notice=retry_queued`).
  </behavior>
  <action>
Создать `app/templates/includes/notice_oob.html` — внеполосную форму
уведомления — и приклеить её в `respond()`.

Форма включения: ОДИН блок, целящийся в область по варианту записи, и он
объявляет подмену СОДЕРЖИМОГО области (`hx-swap-oob="innerHTML:#notice"` для
вежливой и `hx-swap-oob="innerHTML:#notice-alert"` для настойчивой), а не
подмену узла. Внутри блока — тот же вызов макроса `alert()`, что и в
`notice_area.html`; вторая копия разметки плашки не заводится.

⚠️ Jinja-комментарий обязан назвать границу: требование подмены содержимого
относится ТОЛЬКО к областям уведомления. Сегодняшние четыре внеполосных блока
редактора объявлений объявляют подмену УЗЛА, работают и фазой не правятся; гейт
«внеполосный блок обязан подменять содержимое» (план 08-08) применяется только к
областям уведомления, иначе он покраснел бы на работающем редакторе.

В `app/pages/htmx.py` дописать `_notice_oob(code: str) -> str` — рендер этого
включения через окружение Jinja по приёму `accounts.py::_connect_status`
(`templates.env.get_template(...).render(...)`), и приклейку в `respond()`:
когда задан `notice` И задан `fragment`, тело ответа фрагмента дополняется
отрендеренным блоком. Приклейка допустима ТОЛЬКО для ответа с типом
содержимого HTML; иной тип — `ValueError` с текстом, называющим причину. Молча
портить чужое тело нельзя.

Импорт окружения шаблонов делать ОТЛОЖЕННО внутри функции: модуль слоя ответа не
должен зависеть от порядка импорта страниц (та же причина, по которой отложен
импорт реестра).

Тесты писать в новый `tests/test_pages/test_notices_surface.py`, а не в
`test_shell.py`: там живут утверждения о ДОСТАВКЕ шелла, здесь — о СБОРКЕ
ответа.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_notices_surface.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/templates/includes/notice_oob.html` содержит обе строки подмены содержимого — `grep -c 'hx-swap-oob="innerHTML:#notice"' app/templates/includes/notice_oob.html` == 1 и `grep -c 'hx-swap-oob="innerHTML:#notice-alert"' app/templates/includes/notice_oob.html` == 1
    - Source: `grep -c 'notice_oob.html' app/pages/htmx.py` ≥ 1
    - Behavior: `respond` с `notice` и `fragment` отдаёт тело, содержащее и тело фрагмента, и внеполосный блок с текстом записи
    - Behavior: `respond` с `fragment` и без `notice` отдаёт тело, посимвольно равное телу фрагмента
    - Behavior: `respond` с `notice` и фрагментом не-HTML поднимает `ValueError`
    - Behavior: для записи варианта `error` во внеполосном блоке присутствует цель настойчивой области и отсутствует цель вежливой; для остальных вариантов — наоборот
    - Test command: `uv run pytest tests/test_pages/test_notices_surface.py -v` завершается кодом 0
    - Test command: `uv run pytest tests/test_pages/test_ads_editor.py -q` остаётся зелёным — форма внеполосного ответа редактора не тронута
  </acceptance_criteria>
  <done>Внеполосная форма существует, подменяет содержимое и проверена работой на настоящем ответе.</done>
</task>

<task type="auto">
  <name>Task 3: Плашки отказа сервера и обрыва связи, два обработчика, шелл-гейт G-23</name>
  <files>app/templates/includes/htmx_error_banner.html, app/templates/base.html, app/templates/auth_base.html, tests/test_pages/test_shell.py</files>
  <read_first>
    - `app/templates/includes/htmx_config.html` (хвост файла — единственный сегодняшний инлайн-сценарий проекта и абзац «ЧЕГО СКРИПТ НЕ ДЕЛАЕТ»; ⚠️ и абзац «ОГРАНИЧЕНИЕ ДЛЯ ТОГО, КТО БУДЕТ ПИСАТЬ ОБЩИЙ КАНАЛ ВИДИМОСТИ ОТКАЗОВ (QUAL-03)» — предписание, адресованное ИМЕННО этой задаче)
    - `.planning/research/SUMMARY.md` (§R-2 — почему обработчиков ДВА: `htmx:responseError` не срабатывает, когда ответа не было вовсе; и почему тосты через заголовок роняют ответ в 500 на первом русском тексте)
    - `tests/test_pages/test_shell.py` (строки 1478-1700 — `test_vendored_htmx_is_the_declared_artifact`, `test_auth_shell_carries_htmx_config`, `test_htmx_runtime_tag_has_single_source`, `test_history_cache_purge_touches_no_markup_sink` — образец гейта по исходнику включения)
    - `app/templates/components/alert.html` (классы плашки — вторая их копия в шелле запрещена)
    - `tests/test_pages/test_cookie_flags.py` (действующее утверждение о признаке cookie, повторяемое G-23 как ЗАПИСАННОЕ решение)
  </read_first>
  <action>
Создать `app/templates/includes/htmx_error_banner.html` — единственного владельца
заготовок плашек отказа и двух обработчиков событий, и подключить его в оба
шелла рядом с включением областей уведомления.

Разметка: ДВЕ заранее отрисованные заготовки, каждая со своим `id`
(`htmx-failure-server` и `htmx-failure-network`), классами плашки от общего
компонента, `role="alert"` и атрибутом скрытия. Тексты постоянные:
- отказ сервера — сообщает, что действие не выполнено и что повторить его можно
  через минуту, и НЕ называет ни код ответа, ни путь;
- обрыв связи — сообщает, что запрос до сервера не дошёл, и предлагает
  проверить соединение.

Сценарий — ДВА обработчика на `document.body`, и ровно два:
- на `htmx:responseError` — СНАЧАЛА сравнение кода ответа с 422 и ранний выход,
  и только затем снятие атрибута скрытия с заготовки отказа сервера. Это прямое
  исполнение предписания, оставленного Фазой 7 в `htmx_config.html`: помеченный
  неуспешным ответ валидации НЕ ЕСТЬ отказ сервера, и без этой ветки на каждой
  ошибке заполнения пользователь получал бы плашку аварии поверх корректно
  перерисованной формы;
- на `htmx:sendError` — снятие атрибута скрытия с заготовки обрыва связи. Без
  второго обработчика выключенная сеть неотличима от сломанной кнопки (R-2).

Сценарий обязан ТОЛЬКО переключать атрибут: он не читает ни одного значения из
пришедшего ответа, кроме кода состояния, и не собирает разметку — действующее
решение вехи v2.0 «сборка узлами DOM, не строкой». Четыре стока разметки,
перечисленные в `<acceptance_criteria>` этой задачи, в файле отсутствуют, и это
утверждается гейтом по исходнику включения, а не заявляется.

Jinja-комментарий (не HTML-комментарий) обязан нести абзацы: почему один файл на
два шелла; почему обработчиков два; почему различение идёт ПО КОДУ ответа и что
случится, если его снять; «ЧЕГО СЦЕНАРИЙ НЕ ДЕЛАЕТ» по форме
`htmx_config.html`; и что суита не исполняет ни строчки JS, поэтому утверждает
ДОСТАВКУ этих строк в документ, а действие проверяется глазами (пункт 8 ручного
UAT).

Дописать в `tests/test_pages/test_shell.py` группу G-23 — по одному абзацу
комментария на величину, в действующем стиле файла:
- обе заготовки присутствуют в ОБОИХ шеллах ровно по одному разу и приходят
  скрытыми;
- имена обоих событий присутствуют в исходнике включения;
- в исходнике включения ноль вхождений каждого из четырёх стоков разметки
  (`test_the_failure_banner_touches_no_markup_sink`);
- сравнение с кодом 422 в исходнике присутствует
  (`test_the_failure_banner_tells_a_validation_answer_from_a_crash`);
- включение подключено в каждый шелл ровно один раз
  (`test_failure_banner_has_single_source`);
- признак cookie равен `lax` — ЗАПИСАННОЕ решение, а не наблюдение;
- версия вендоренного htmx и каждая строка блока конфигурации уже утверждены
  существующими тестами файла — их НЕ дублировать, а сослаться на них абзацем.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py -v</automated>
    <human-check>Открыть любую страницу в браузере с включённым JS. (1) Остановить контейнер `web` и нажать любую кнопку, идущую через htmx, — плашка обрыва связи обязана появиться. (2) Вернуть контейнер, временно заставить маршрут отвечать 500 и повторить — обязана появиться плашка отказа сервера, а не плашка обрыва. (3) Отправить форму, отдающую 422, — плашки аварии быть НЕ должно. Это пункт 8 перечня ручного UAT; суита не исполняет ни строчки JS и доказать это не может.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'includes/htmx_error_banner.html' app/templates/base.html` == 1 и `grep -c 'includes/htmx_error_banner.html' app/templates/auth_base.html` == 1
    - Source: `grep -c 'htmx:responseError' app/templates/includes/htmx_error_banner.html` == 1 и `grep -c 'htmx:sendError' app/templates/includes/htmx_error_banner.html` == 1
    - Source: `grep -c 'innerHTML' app/templates/includes/htmx_error_banner.html` == 0
    - Source: `grep -c 'outerHTML' app/templates/includes/htmx_error_banner.html` == 0
    - Source: `grep -c 'insertAdjacentHTML' app/templates/includes/htmx_error_banner.html` == 0
    - Source: `grep -c 'document.write' app/templates/includes/htmx_error_banner.html` == 0
    - Source: `grep -c '422' app/templates/includes/htmx_error_banner.html` ≥ 1
    - Source: `grep -c '<!--' app/templates/includes/htmx_error_banner.html` == 0
    - Behavior: `GET /profile` и `GET /login` возвращают тела, каждое содержащее `id="htmx-failure-server"` и `id="htmx-failure-network"` ровно по одному разу, и оба узла несут атрибут скрытия
    - Test command: `uv run pytest tests/test_pages/test_shell.py -q` завершается кодом 0
    - Test command: `uv run pytest tests/test_pages/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Плашки приходят с сервера скрытыми, обработчиков два, различение 422 машинно утверждено.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| адресная строка → область уведомления | параметр приходит от владельца ссылки и рисуется на КАЖДОЙ странице проекта |
| ответ сервера → инлайн-сценарий | сценарий читает свойство ответа и меняет DOM на всех страницах |
| фрагмент ответа → внеполосная подмена | внеполосный блок способен изменить область за пределами цели свопа |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08 | Spoofing | `?notice=` рисует сообщение от имени приложения на любой странице | high | mitigate | значение сравнивается ЦЕЛИКОМ по закрытому реестру и в разметку не уходит; неизвестный код не рисует ничего — утверждается тестом с параметром `%3Cscript%3E` |
| T-08-12 | Tampering | XSS через текст записи, попадающий в область на КАЖДОЙ странице | high | mitigate | текст — литерал реестра, `autoescape=True` действует, разметка плашки собирается общим макросом; форма `\|safe` в шаблонах запрещена гейтом плана 08-09 |
| T-08-18 | Tampering | инлайн-сценарий собирает разметку строкой и становится стоком исполнения | high | mitigate | сценарий снимает атрибут и больше ничего; ноль вхождений четырёх стоков утверждается гейтом по исходнику включения |
| T-08-19 | Denial of Service | плашка аварии на каждой ошибке заполнения формы обучает глаз игнорировать красное | medium | mitigate | ранний выход по коду 422 в обработчике; наличие сравнения утверждается гейтом |
| T-08-20 | Information Disclosure | текст плашки отказа выносит наружу код ответа, путь или текст стороннего исключения | medium | mitigate | тексты заготовок постоянные, приходят с сервера и подстановок не содержат; сценарий читает из ответа ТОЛЬКО код состояния |
| T-08-21 | Tampering | внеполосный блок подменяет узел вместо содержимого и уносит область из документа | medium | mitigate | форма подмены содержимого объявлена явно в `notice_oob.html` и утверждается гейтом плана 08-08 (правило применяется только к областям уведомления) |
| T-08-SC | Tampering | установка пакетов npm/pip/cargo | high | accept | план не устанавливает ни одного пакета; ни одной новой зависимости фаза не вводит (R-1) |

**Канон, не минтуемый прохибицией (breadcrumb):** XSS и политика содержимого —
канон OWASP, покрывается `/gsd-secure-phase` и строками T-08-12/T-08-18.
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_shell.py tests/test_pages/test_notices_surface.py -v` — зелёный
- `uv run pytest tests/test_pages/ tests/test_templates/ -q` — зелёный
- Ручной обход пункта 8 UAT (см. `<human-check>` задачи 3) — записывается в UAT фазы
- `uv run python -m compileall -q app main.py tests` — код 0
</verification>

<success_criteria>
- Две области уведомления существуют в обоих шеллах, различаются `role` и `aria-live` и пусты без кода
- Внеполосная форма подменяет содержимое областей и проверена работой
- Заготовки плашек приходят с сервера скрытыми, сценарий только снимает атрибут
- Ответ 422 плашку аварии не поднимает — различение машинно утверждено
</success_criteria>

## Flagged planner assumptions (edge probe, unresolved)

Зондирование краёв вернуло `unclassified — review manually` по FOUND-06 и по
QUAL-03. Обе строки записаны допущениями, а не выброшены:

1. **FOUND-06.** Предполагается, что «при отсутствии кода разметки нет вовсе»
   относится к ПЛАШКЕ, а не к узлу области: пустой узел области остаётся на
   странице всегда, потому что подмена содержимого по `id` требует стабильного
   узла. Формулировка требования этого различения не проводит. Требует решения
   человека, если проверяющий прочтёт её буквально.
2. **QUAL-03.** Предполагается, что «отказ сервера» — это ЛЮБОЙ ответ 4xx/5xx,
   кроме 422, и что снятая плашка не возвращается автоматически по успешному
   следующему запросу (сценарий её только показывает). Автоматическое сокрытие
   потребовало бы третьего обработчика, а рамка вехи ограничивает число новых
   сценариев. Требует решения человека, если накопление плашек станет заметно.

<output>
Create `.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-04-SUMMARY.md` when done
</output>
