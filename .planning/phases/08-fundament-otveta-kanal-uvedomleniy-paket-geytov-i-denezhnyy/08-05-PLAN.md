---
phase: 08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy
plan: 05
type: tdd
wave: 2
depends_on: ["08-03"]
files_modified:
  - app/services/payment_service.py
  - tests/test_services/test_payment_intent_cap.py
  - tests/test_services/test_payment_service.py
  - tests/test_services/test_payment_concurrency.py
autonomous: true
requirements: [PAY-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Второй одновременный запрос подписочной оплаты отвергается ДО того, как двинулись деньги: строка-намерение вставляется раньше единственного обращения к ЮKassa, и отказ приходит от ограничения схемы"
    - "Отказ потолка не доходит до платёжного SDK ни одним вызовом — свойство, которое требует действующий тест, остаётся истинным уже для отказа схемы, а не только прикладного"
    - "Прикладной проверки потолка в коде больше нет: один вопрос решается в одном месте, и этим местом стала схема"
    - "Отказ ограничения различается ПО ИМЕНИ ограничения; чужой отказ поднимается наружу тем же объектом исключения, а не глотается"
    - "Текст исключения ограничения на экран не уходит: пользователь видит слова кода причины, а не внутренности СУБД"
    - "Просроченные намерения убираются лениво, на своём же пути — в начале создания платежа, без второго писателя на денежном пути и без периодической задачи"
    - "Срок давности применяется в Python, а не условием SQL: колонка отдаётся SQLite наивной, а PostgreSQL — осведомлённой, и сравнение в SQL разошлось бы ровно на одном диалекте"
    - "Строка, снятая по сроку давности либо отказом SDK, остаётся ОПЛАЧИВАЕМОЙ: expired не входит в терминальные статусы, поэтому заявка на обработку выигрывается на ней без единой правки условия"
    - "Отказ SDK после резерва гасит СВОЮ ЖЕ строку: локальная строка без удалённого платежа восстановима, удалённый платёж без локальной строки — нет"
  artifacts:
    - path: "app/services/payment_service.py"
      provides: "Порядок «резерв → сеть → дозапись», ленивая уборка, разбор отказа ограничения по имени, статус expired"
      contains: "STATUS_EXPIRED"
    - path: "tests/test_services/test_payment_intent_cap.py"
      provides: "Потолок как свойство схемы: отказ второго намерения, уборка, гашение резерва, зачисление по просроченной строке"
      contains: "def test_"
  key_links:
    - from: "app/services/payment_service.py::create_payment"
      to: "alembic/versions/0021_payments_open_intent_index.py"
      via: "разбор IntegrityError по имени ограничения uq_payments_open_subscription_intent"
      pattern: "uq_payments_open_subscription_intent"
    - from: "app/services/payment_service.py::_claim_payment"
      to: "app/services/payment_service.py::TERMINAL_STATUSES"
      via: "expired не входит в множество, поэтому заявка выигрывается на просроченной строке без правки условия"
      pattern: "TERMINAL_STATUSES"
  prohibitions:
    - requirement_id: PAY-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT принять деньги, не выдав доступ: ни один путь отказа — ни потолка, ни SDK, ни уборки — не имеет права оставить у ЮKassa живой платёж, которому в нашей базе не соответствует ПРИНИМАЕМАЯ обработчиком уведомления строка"
    - requirement_id: PAY-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT показать человеку внутренности СУБД: имя ограничения, текст драйвера и число незакрытых намерений принадлежат журналу, а не экрану"
---

<objective>
Перевести денежный путь на новый потолок: строка-намерение резервируется ДО
обращения к ЮKassa, прикладная проверка снимается, отказ приходит от ограничения
схемы и разбирается по его имени, просроченные намерения убираются лениво на
своём же пути.

Purpose: PAY-01 требует, чтобы потолок стал свойством СХЕМЫ. Под сегодняшним
порядком («проверка → сеть → вставка») проигравший гонку узнавал бы об отказе
уже ПОСЛЕ создания намерения у ЮKassa, и записать его было бы некуда: при
ненайденной строке обработчик уведомления возвращает `False`, ничего не
начисляя, — то есть буквально принятые деньги без выданного доступа.
Output: переписанный `create_payment`, ленивая уборка, разбор отказа ограничения,
новый файл тестов потолка и приведённые в соответствие действующие тесты.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-CONTEXT.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-PATTERNS.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-03-SUMMARY.md
@app/services/payment_service.py
</context>

## Artifacts this phase produces

| Артефакт | Где |
|---|---|
| `STATUS_EXPIRED = "expired"` | `app/services/payment_service.py` |
| `OPEN_INTENT_INDEX_NAME = "uq_payments_open_subscription_intent"` | там же — имя ограничения, по которому различается свой отказ |
| `_stale_subscription_intents(db, user_id, now)` | там же — переименованный и перецеленный `_open_subscription_intents` (D-06: селектор уборки) |
| `_expire_stale_intents(db, user_id, now) -> int` | там же — ленивая уборка на своём же пути (D-02) |
| `_reserve_subscription_intent(...)` | там же — вставка строки ДО вызова SDK (D-05) |
| `_is_open_intent_conflict(rejection) -> bool` | там же — разбор отказа по имени ограничения |
| `tests/test_services/test_payment_intent_cap.py` | новый файл тестов |

<!-- planner-discipline-allow: STATUS_EXPIRED -->
<!-- planner-discipline-allow: open_intents -->
<!-- planner-discipline-allow: _open_subscription_intents -->

<tasks>

<task type="tdd">
  <name>Task 1: Резерв до сети, ленивая уборка, гашение резерва при отказе SDK</name>
  <files>app/services/payment_service.py, tests/test_services/test_payment_intent_cap.py</files>
  <reversibility rating="costly">Возврат к порядку «сеть → запись» потребовал бы согласованной правки обработчика уведомления и обратной миграции nullability; решение записано владельцем (D-05).</reversibility>
  <precondition>Ревизия `0021` накатывается: `uv run pytest tests/test_migrations/test_0021_payments_open_intent_index.py -q` зелёная, и `app/models/payment.py` объявляет `yookassa_payment_id` допускающим `NULL` — без этого резервная строка не вставится вовсе.</precondition>
  <read_first>
    - `app/services/payment_service.py` (строки 26-52 — константы и `TERMINAL_STATUSES`; строки 40-52 — комментарий над `PENDING_INTENT_TTL_HOURS`; строки 115-160 — `_open_subscription_intents` и абзац «⚠️ СРОК ДАВНОСТИ ПРИМЕНЯЕТСЯ В PYTHON, А НЕ УСЛОВИЕМ SQL, И ЭТО НЕ ВКУС»; строки 164-460 — `create_payment` целиком, включая абзац «ЧТО ЗАКРЫЛО БЫ ОКНО СВОЙСТВОМ…» и комментарий T-05-49 над вызовом SDK; строки 478-520 — `_claim_payment`)
    - `app/pages/history.py` (строки 400-415 и 538-585 — `_RETRY_IN_FLIGHT`, `_claim_retry_slot`, `_release_retry_slot`: ИДИОМА ленивой уборки чужих просроченных окон на своём же пути и её обоснование «обхода реестра нет нигде, периодической задачи под него не заведено»)
    - `app/application/analytics/send_analytics.py::normalize_utc` (приведение времени, которым пользуется отбор по сроку давности)
    - `alembic/versions/0021_payments_open_intent_index.py` (создана планом 08-03 — имя индекса и его предикат)
  </read_first>
  <behavior>
    - Тест 1: у пользователя без незакрытых намерений создание подписочного платежа проходит, и в базе остаётся ровно одна строка с непустым `yookassa_payment_id`.
    - Тест 2: второе подписочное намерение того же пользователя отвергается `PendingIntentCapError`, и SDK не вызывается НИ ОДНИМ вызовом.
    - Тест 3: строка старше срока давности убирается в начале создания платежа, и после уборки новое намерение создаётся беспрепятственно.
    - Тест 4: уборка трогает ТОЛЬКО строки этого пользователя, только `kind='subscription'` и только `status='pending'`.
    - Тест 5: строка без времени рождения считается СВЕЖЕЙ и уборкой не трогается — снимать защиту по отсутствию данных не то умолчание.
    - Тест 6: отказ SDK ПОСЛЕ резерва оставляет строку в статусе `expired`, а не удаляет её и не оставляет `pending`; наружу поднимается `PaymentCreationError`.
    - Тест 7: погашенная отказом SDK строка не мешает следующей попытке оплаты того же пользователя.
    - Тест 8: `_claim_payment` выигрывает заявку на строке со статусом `expired` — условие через `TERMINAL_STATUSES` не правится ни одной строкой, и уведомление по старой ссылке зачисляется.
    - Тест 9: пакетный платёж сохраняет сегодняшний порядок «сеть → запись»: отказ SDK не оставляет строки в журнале.
    - Тест 10: срок давности применяется в Python — строка, чей `created_at` отдан наивным, обрабатывается так же, как осведомлённая.
  </behavior>
  <action>
Перестроить денежный путь в `app/services/payment_service.py`.

Константы: добавить `STATUS_EXPIRED = "expired"` и
`OPEN_INTENT_INDEX_NAME = "uq_payments_open_subscription_intent"`.
`TERMINAL_STATUSES` НЕ ТРОГАТЬ — `expired` в него не входит НАМЕРЕННО, и над
константой поставить абзац, называющий это решением: просроченное намерение
остаётся оплачиваемым, поэтому оно нетерминально, и `_claim_payment` принимает
его наравне с `pending` БЕЗ ЕДИНОЙ ПРАВКИ условия — потому что условие написано
через множество, а не через равенство `pending`.

Переписать комментарий над `PENDING_INTENT_TTL_HOURS` (D-04). Число 24 НЕ
МЕНЯЕТСЯ — оно уже прошло чекпойнт владельца, — но сегодняшняя запись («срок
давности — единственный сегодня выход из незакрытого намерения») перестала быть
правдой: выходом стала уборка. Новый смысл константы — окно, в течение которого
вторая оплата отвергается, то есть всё, что осталось от потолка на человеческом
пути. Форма комментария («ЗДЕСЬ ВАЖНО НЕ ЧИСЛО, А ТО, ЗАЧЕМ КОНСТАНТА ВООБЩЕ
СУЩЕСТВУЕТ») сохраняется; снятое объяснение переписывается, а не оставляется
рядом с новым.

Перецелить `_open_subscription_intents` в СЕЛЕКТОР УБОРКИ и переименовать в
`_stale_subscription_intents`: тот же запрос (строки пользователя с
`kind='subscription'` и статусом вне `TERMINAL_STATUSES`), но возвращает те, чей
`created_at`, приведённый `normalize_utc`, СТАРШЕ отсечки. Абзац «⚠️ СРОК
ДАВНОСТИ ПРИМЕНЯЕТСЯ В PYTHON, А НЕ УСЛОВИЕМ SQL» переносится дословно — он
остаётся верным и остаётся несущим. Правило «строка без времени рождения
считается СВЕЖЕЙ» сохраняется и в новой роли означает «уборкой не трогается».
Переименование — уточнение формулировки D-06, а не отступление от неё: имя
обязано называть работу. Обновить все ссылки на прежнее имя (в том числе в
тестах) грепом.

Добавить `_expire_stale_intents(db, user_id, now) -> int` — ленивую уборку по
идиоме `_claim_retry_slot`: получить просроченные строки селектором, одним
`UPDATE ... WHERE id IN (...)` перевести их в `STATUS_EXPIRED` с
`synchronize_session=False`, вернуть число тронутых. Докстринг обязан назвать
ДВА отвергнутых варианта и почему: периодическая задача Celery beat асинхронна
(человек, ждавший сутки, остался бы заперт до ближайшего тика) и вводит ВТОРОГО
ПИСАТЕЛЯ на денежный путь; отмена намерения у ЮKassa — второй сетевой вызов на
денежном пути, и её отказ снова запирает человека. Там же назвать ЦЕНУ размена:
строка, просроченная и не тронутая уборкой, до следующей попытки оплаты
остаётся `pending` и читается на экранах биллинга и админки как «в обработке».
Это принято сознательно.

Перестроить `create_payment` для `kind == KIND_SUBSCRIPTION` в порядок
«уборка → резерв → сеть → дозапись»:
1. `await _expire_stale_intents(db, user_id, now)`;
2. вставить строку-намерение с `yookassa_payment_id=None` и `status=STATUS_PENDING`
   внутри `async with db.begin_nested()` с `await db.flush()`, по форме
   `_extend_subscription` (строки 798-825); зафиксировать резерв `await db.commit()`
   ДО обращения к сети — резерв обязан пережить падение процесса, иначе он не
   потолок;
3. `_configure_yookassa()`, сборка `description` и `metadata`, `YooPayment.create`;
4. при отказе SDK погасить СВОЮ ЖЕ строку: `status=STATUS_EXPIRED`, коммит,
   затем существующая запись в журнал ключом `payment_create_failed` и
   `PaymentCreationError`;
5. при успехе дозаписать `yookassa_payment_id` и закоммитить; существующая запись
   `payment_created` сохраняется.

Для `kind == KIND_PACKAGE` порядок НЕ МЕНЯЕТСЯ («сеть → запись»), и это решение
обязано быть выписано абзацем, а не подразумеваться: ограничение схемы
существует ТОЛЬКО у подписочного намерения, поэтому только у него отказ способен
прийти ПОСЛЕ вызова SDK. У пакета такого отказа нет, рассуждение T-05-49 для него
остаётся верным дословно, и менять ему порядок значило бы расширять радиус
правки денежного пути без предмета.

Комментарий T-05-49 над вызовом SDK ЗЕРКАЛИТСЯ для подписочной ветки, а не
удаляется: под индексом опасность поменяла сторону — локальная строка без
удалённого платежа восстановима (её гасит собственная ветка отказа), удалённый
платёж без локальной строки — нет.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_payment_intent_cap.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'STATUS_EXPIRED' app/services/payment_service.py` ≥ 3
    - Source: `grep -c 'STATUS_EXPIRED' app/services/payment_service.py` в строке объявления `TERMINAL_STATUSES` отсутствует — `sed -n '/^TERMINAL_STATUSES/,+1p' app/services/payment_service.py | grep -c STATUS_EXPIRED` == 0
    - Source: в `create_payment` строка вставки резервной строки стоит ВЫШЕ строки `YooPayment.create` (проверяется номерами строк)
    - Source: `grep -c '_expire_stale_intents' app/services/payment_service.py` ≥ 2
    - Behavior: второе подписочное намерение того же пользователя поднимает `PendingIntentCapError`, и подменённый `YooPayment.create` не вызван ни разу
    - Behavior: отказ SDK после резерва оставляет ровно одну строку со статусом `expired` и `yookassa_payment_id IS NULL`
    - Behavior: `_claim_payment` возвращает `True` на строке со статусом `expired`
    - Behavior: пакетный платёж при отказе SDK не оставляет строки в журнале
    - Test command: `uv run pytest tests/test_services/ -q` завершается кодом 0
    - Build: `uv run python -m compileall -q app main.py tests` завершается кодом 0
  </acceptance_criteria>
  <done>Резерв встаёт до сети, уборка ленивая, погашенная строка остаётся оплачиваемой.</done>
</task>

<task type="tdd">
  <name>Task 2: Снятие прикладной проверки потолка и разбор отказа ограничения по имени</name>
  <files>app/services/payment_service.py, tests/test_services/test_payment_intent_cap.py</files>
  <read_first>
    - `app/services/payment_service.py` (состояние после задачи 1; строки 798-825 — `_extend_subscription`: ПРЯМОЙ прецедент разбора отказа ограничения — `db.begin_nested()`, `except IntegrityError as rejection`, логирование проигранной гонки и ОБЯЗАТЕЛЬНЫЙ `raise rejected_by` для чужого отказа)
    - `app/services/payment_service.py` (строки 89-114 — докстринг `PendingIntentCapError`: почему у него свой тип и почему его текст на экран не уходит)
    - `app/pages/billing.py` (строки 385-400 — ветка `except PendingIntentCapError` в обработчике `subscribe`: она НЕ ПРАВИТСЯ этим планом, тип исключения сохраняется ради неё)
    - `alembic/versions/0021_payments_open_intent_index.py` (имя ограничения)
  </read_first>
  <behavior>
    - Тест 1: прикладного условия «если есть незакрытые намерения — отказать» в `create_payment` больше нет; отказ приходит исключительно от ограничения.
    - Тест 2: отказ ограничения `uq_payments_open_subscription_intent` превращается в `PendingIntentCapError` с тем же фиксированным текстом, что и раньше.
    - Тест 3: ЧУЖОЙ `IntegrityError` (например, нарушение уникальности `yookassa_payment_id`) НЕ превращается в `PendingIntentCapError`, а поднимается наружу тем же объектом.
    - Тест 4: в текст `PendingIntentCapError` не подставляется ни имя ограничения, ни текст драйвера, ни число незакрытых намерений.
    - Тест 5: запись в журнал `subscription_intent_cap_reached` уровня `warning` делается ровно один раз на отказ и содержит `user_id`.
    - Тест 6: `_is_open_intent_conflict` распознаёт отказ и по полю диагностики драйвера (форма PostgreSQL, подаётся подделкой), и по тексту сообщения (форма SQLite).
  </behavior>
  <action>
Снять прикладную проверку потолка целиком и завести разбор отказа ограничения.

Удалить из `create_payment` блок `if kind == KIND_SUBSCRIPTION: open_intents =
...; if open_intents: ... raise PendingIntentCapError(...)`. Основание — буква
требования: потолок становится свойством СХЕМЫ, а не проверкой в коде. Оставить
обе значило бы завести второе место, где решается один вопрос.

Добавить `_is_open_intent_conflict(rejection: IntegrityError) -> bool`:
сначала поле диагностики драйвера
(`getattr(getattr(rejection.orig, "diag", None), "constraint_name", None)`
сравнивается с `OPEN_INTENT_INDEX_NAME` — форма PostgreSQL), при его
отсутствии — вхождение `OPEN_INTENT_INDEX_NAME` в `str(rejection.orig)` (форма
SQLite: сообщение об отказе уникального индекса называет его имя). Докстринг
обязан выписать, ПОЧЕМУ разбор идёт по ИМЕНИ, а не по факту отказа: чужой отказ,
принятый за свой, показал бы человеку «предыдущая оплата не завершена» там, где
на самом деле сломалось что-то другое, и настоящая поломка осталась бы
незамеченной.

В ветке резерва (задача 1) обернуть вставку по форме `_extend_subscription`:
`except IntegrityError as rejection:` → если `_is_open_intent_conflict(rejection)`,
записать `logger.warning("subscription_intent_cap_reached", user_id=user_id)`
(уровень `warning` и его обоснование — «это исход, по которому к нам придёт
человек» — сохраняются дословно) и поднять
`PendingIntentCapError("Предыдущее подписочное намерение ещё не завершено") from rejection`;
иначе поднять `rejection` наружу тем же объектом.

⚠️ Ключ `open_intents=len(open_intents)` из записи в журнал уходит вместе с
прикладной проверкой: числа незакрытых намерений теперь не считает никто, и
подставлять туда единицу «чтобы поле осталось» нельзя — это была бы неправда в
журнале, по которому разбирают денежные жалобы. Снятие поля назвать в
комментарии.

Переписать в докстринге `create_payment` абзац «ЧТО ЗАКРЫЛО БЫ ОКНО СВОЙСТВОМ, А
НЕ ФОРМУЛИРОВКОЙ, НАЗВАНО ПОИМЁННО И НАЗВАНО НЕСДЕЛАННЫМ»: он перечисляет ровно
объём этой фазы и обязан быть переписан НА СДЕЛАННОЕ, а не оставлен рядом с
закрывшим его кодом. Абзац «⚠️ СУЖАЕТ, А НЕ ДЕЛАЕТ НЕДОСТИЖИМЫМ, И ОСТАТОЧНЫХ
ОКОН ДВА, А НЕ ОДНО» переписать так же: окно гонки закрыто ограничением, окно
срока давности закрыто уборкой; назвать, что осталось (строка, просроченная и
не тронутая уборкой, читается на экранах как «в обработке») и что это принято.

Тип `PendingIntentCapError` и его фиксированный текст НЕ МЕНЯЮТСЯ: ветка
обработчика формы различает отказ по типу, и она этим планом не правится.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_payment_intent_cap.py tests/test_services/test_payment_concurrency.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'PendingIntentCapError' app/services/payment_service.py` — вхождений ровно два: объявление класса и единственное место возбуждения
    - Source: `grep -c 'open_intents' app/services/payment_service.py` == 0
    - Source: `grep -c '_is_open_intent_conflict' app/services/payment_service.py` ≥ 2
    - Behavior: второе намерение поднимает `PendingIntentCapError`, и его `str()` не содержит ни `uq_`, ни `UNIQUE`, ни цифр
    - Behavior: отказ уникальности по `yookassa_payment_id` поднимается наружу как `IntegrityError`, а не как `PendingIntentCapError`
    - Behavior: `_is_open_intent_conflict` возвращает `True` и для подделки с `orig.diag.constraint_name`, и для подделки, у которой имя ограничения только в тексте
    - Test command: `uv run pytest tests/test_services/ -q` завершается кодом 0
    - Build: `uv run python -m compileall -q app main.py tests` завершается кодом 0
  </acceptance_criteria>
  <done>Потолок решается ровно в одном месте — в схеме; чужой отказ наружу не глотается.</done>
</task>

<task type="auto">
  <name>Task 3: Привести действующие тесты денежного пути в соответствие с новым порядком</name>
  <files>tests/test_services/test_payment_service.py, tests/test_services/test_payment_concurrency.py</files>
  <read_first>
    - `tests/test_services/test_payment_service.py` (ЦЕЛИКОМ по грепу имён: `test_the_refusal_never_reaches_yookassa`, `test_a_second_subscription_intent_is_refused_before_the_money_moves`, `test_a_user_without_open_intents_pays_without_obstruction`, `test_an_intent_of_another_user_does_not_reach_over`, `test_a_stale_intent_does_not_block_a_new_one`, `test_a_terminal_payment_never_blocks_a_new_one`, `test_a_failed_payment_leaves_no_row_in_the_journal`, `test_the_refusal_leaves_its_own_trace`, `_open_intent`)
    - `tests/test_services/test_payment_concurrency.py` (ЦЕЛИКОМ — что именно там проверяется о гонке)
    - `app/services/payment_service.py` (состояние после задач 1-2)
  </read_first>
  <action>
Привести действующие тесты к новому порядку, НЕ ослабляя ни одного утверждения.

Обязательные изменения, каждое с обновлённым докстрингом, называющим ПРИЧИНУ
правки:
- `test_a_failed_payment_leaves_no_row_in_the_journal` — предмет РАЗДЕЛЯЕТСЯ на
  два теста, а не подправляется: для ПАКЕТА утверждение остаётся дословно (строки
  не остаётся), для ПОДПИСКИ заводится
  `test_a_failed_subscription_payment_leaves_its_reserve_expired` — строка
  остаётся, но в статусе `expired`. Переименовать пакетный тест так, чтобы имя
  называло предмет, и записать в докстринге, что разделение вызвано сменой
  порядка (D-05), а не удобством.
- `test_the_refusal_never_reaches_yookassa` — утверждение СОХРАНЯЕТСЯ дословно и
  становится сильнее: отказ теперь приходит от схемы, и SDK по-прежнему не
  вызывается ни одним вызовом. Докстринг дополнить одной строкой об этом.
- `test_a_second_subscription_intent_is_refused_before_the_money_moves`,
  `test_a_user_without_open_intents_pays_without_obstruction`,
  `test_an_intent_of_another_user_does_not_reach_over`,
  `test_a_terminal_payment_never_blocks_a_new_one` — предмет прежний, источник
  отказа новый; проверить, что каждый зелёный, и дополнить докстринги строкой о
  том, что отказ теперь принимает СУБД.
- `test_a_stale_intent_does_not_block_a_new_one` — предмет прежний, механизм
  новый: не «просроченное не считается», а «просроченное убирается». Докстринг
  переписать под механизм; утверждение результата не менять.
- `test_the_refusal_leaves_its_own_trace` — ключ записи в журнале прежний,
  поле `open_intents` снято; обновить ожидаемый состав полей.
- `_open_intent` и любые прочие ссылки на прежнее имя селектора — переименовать
  грепом.

Ни одно утверждение НЕ ОСЛАБЛЯЕТСЯ и ни один тест не удаляется. Если какой-то
из перечисленных тестов после правки становится дублем нового из
`test_payment_intent_cap.py`, он остаётся на месте: дубль утверждения дешевле
потерянного утверждения, а решение о слиянии — работа своего размера.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/ tests/test_pages/test_billing_subscription.py tests/test_pages/test_billing_payment_errors.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `uv run pytest tests/test_services/ -q` завершается кодом 0
    - Test command: `uv run pytest tests/test_pages/test_billing_subscription.py tests/test_pages/test_billing_payment_errors.py tests/test_pages/test_billing_section.py -q` завершается кодом 0
    - Source: `grep -rc '_open_subscription_intents' app/ tests/ | grep -v ':0' | wc -l` == 0 — прежнее имя нигде не осталось
    - Source: `grep -c 'test_the_refusal_never_reaches_yookassa' tests/test_services/test_payment_service.py` == 1 — тест не удалён
    - Source: в `tests/test_services/test_payment_service.py` присутствует `test_a_failed_subscription_payment_leaves_its_reserve_expired`
    - Test command: `uv run pytest tests/ -q` завершается кодом 0
    - Build: `uv run python -m compileall -q app main.py tests` завершается кодом 0
  </acceptance_criteria>
  <done>Суита денежного пути зелёная, ни одно утверждение не ослаблено, разделённые предметы названы своими именами.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| форма оплаты → сервис | две одновременные отправки одной кнопки приходят как два независимых запроса |
| сервис → ЮKassa | единственное обращение наружу на денежном пути; его отказ и его успех обрабатываются по-разному |
| СУБД → сервис | отказ ограничения приезжает текстом драйвера, содержащим внутренние имена |
| сервис → экран | текст отказа виден человеку, стоящему у кнопки оплаты |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-22 | Tampering | обход потолка двумя одновременными отправками | high | mitigate | потолок — частичный уникальный индекс; проверка и запись слились в один оператор вставки, зазора между ними не остаётся ВОВСЕ |
| T-08-14 | Information Disclosure | имя ограничения и текст драйвера уходят на экран | medium | mitigate | `PendingIntentCapError` несёт ФИКСИРОВАННЫЙ текст без подстановок; текст исходного исключения остаётся доступен через `__cause__` отладчику, а не странице (прецедент T-05-47) |
| T-08-23 | Denial of Service | человек, открывший оплату и не заплативший, запирается навсегда | high | mitigate | ленивая уборка в начале создания платежа переводит просроченное намерение в `expired`, который предикат индекса ИСКЛЮЧАЕТ; окно закрывается на пути самого пользователя, без второго писателя |
| T-08-24 | Repudiation | деньги приняты, доступ не выдан, молча | high | mitigate | `expired` не входит в терминальные статусы, поэтому `_claim_payment` выигрывает заявку на такой строке и `handle_webhook` начисляет; закреплено тестом №8 задачи 1 |
| T-08-25 | Tampering | чужой отказ ограничения принят за свой и проглочен | high | mitigate | различение по ИМЕНИ ограничения; чужой отказ поднимается наружу тем же объектом исключения (прецедент `_extend_subscription`) |
| T-08-26 | Denial of Service | резерв, не переживший падение процесса, перестаёт быть потолком | medium | mitigate | резерв фиксируется `commit` ДО обращения к сети; порядок утверждается приёмочным критерием по номерам строк |
| T-08-SC | Tampering | установка пакетов npm/pip/cargo | high | accept | план не устанавливает ни одного пакета; установочных задач нет |
</threat_model>

<verification>
- `uv run pytest tests/test_services/ -v` — зелёный
- `uv run pytest tests/ -q` — зелёный (полный прогон: денежный путь трогает и страницы)
- `grep -rc '_open_subscription_intents' app/ tests/` — ни одного вхождения
- `uv run python -m compileall -q app main.py tests` — код 0
</verification>

<success_criteria>
- Потолок решается ровно в одном месте — ограничением схемы
- Отказ потолка не доходит до платёжного SDK ни одним вызовом
- Просроченное намерение убирается на пути самого пользователя и остаётся оплачиваемым
- Чужой отказ ограничения не глотается
</success_criteria>

<output>
Create `.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-05-SUMMARY.md` when done
</output>
