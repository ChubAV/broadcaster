---
phase: 08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy
plan: 07
type: execute
wave: 4
depends_on: ["08-01", "08-06"]
files_modified:
  - tests/test_pages/test_htmx_gates.py
autonomous: true
requirements: [GATE-03, GATE-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Каждый обработчик под декоратором POST в слое страниц либо идёт через respond(), либо стоит в одном из двух явных перечней — и объединение трёх множеств РАВНО множеству найденных обходом обработчиков"
    - "Перечень «ещё не переведено» — убывающее число, и тестов на него ДВА: один краснеет на РОСТЕ и говорит «фаза вернула обработчик назад», второй краснеет на ПАДЕНИИ и говорит «число упало — опустите константу, это прогресс вехи». Штатное уменьшение счётчика не читается как регрессия"
    - "Критерий 1 фазы утверждается не буквальным равенством двух множеств, а тройкой G-1 + G-2 + HX_HEADER_READS == 1: буквальное равенство несовместимо с FOUND-04, потому что читатель заголовка ровно один, и обоснование замены выписано в плане абзацем"
    - "Перечень исключений и перечень непереведённого — ДВА разных списка, а не один: «никогда не пойдёт» и «ещё не пошёл» не перемешиваются"
    - "Признак htmx читается на проект ровно один раз, и это утверждается собственным обходом гейта, а не сводным счётчиком"
    - "Каждое место записи заголовка HX-* имеет правый операнд, не содержащий обращения к объекту запроса: значение заголовка не может прийти от клиента ни одним путём"
    - "Гейт читает исходник, а не собранное приложение, и не импортирует ни одного модуля приложения — поэтому его результат не зависит ни от порядка исполнения сюиты, ни от параллельного прогона"
    - "Зубы каждого гейта доказаны: группа -k control подаёт разборщику изменённые копии исходника и утверждает, что гейт краснеет на нарушении и зеленеет на настоящем дереве"
  artifacts:
    - path: "tests/test_pages/test_htmx_gates.py"
      provides: "G-1/G-2 (единственность решения) и G-13 (AST на правый операнд записи HX-*), инвентарные константы, группа контроля"
      contains: "NOT_YET_CONVERTED"
  key_links:
    - from: "tests/test_pages/test_htmx_gates.py"
      to: "app/pages/htmx.py"
      via: "гейт утверждает, что единственное чтение заголовка живёт здесь"
      pattern: "htmx.py"
    - from: "tests/test_pages/test_htmx_gates.py"
      to: "app/pages"
      via: "обход дерева разбора всех POST-обработчиков слоя страниц"
      pattern: "ast.walk"
  prohibitions:
    - requirement_id: GATE-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT выпустить гейт, зелёный ПО ПОСТРОЕНИЮ: обход тридцати шести обработчиков, не доказавший, что краснеет на нарушении, создаёт уверенность вместо проверки — и обнаруживается это в тот день, когда он пропускает настоящий пропуск"
---

<objective>
Написать гейт единственности решения (G-1/G-2) и гейт безопасности записи
заголовков ответа (G-13) — два самых несущих машинных правила фазы.

Purpose: критерий 1 фазы («признак htmx объявлен РОВНО ОДИН раз… и гейт
утверждает это машинно») и та часть критерия 4, которая закрывает разом кириллицу
в заголовке, инъекцию заголовка и открытый редирект.
Output: `tests/test_pages/test_htmx_gates.py` с инвентарными константами,
двумя перечнями и доказанными зубами.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-CONTEXT.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-PATTERNS.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-01-SUMMARY.md
@tests/test_pages/test_impersonation_gate.py
@tests/test_pages/test_htmx_response_contract.py
</context>

## Artifacts this phase produces

| Артефакт | Где |
|---|---|
| `tests/test_pages/test_htmx_gates.py` | новый файл гейтов |
| `POST_HANDLERS = 36` | там же — инвентарь, собранный собственным обходом |
| `NEVER_RESPONDS` — отображение «имя обработчика → обоснование» | там же |
| `NOT_YET_CONVERTED` — множество имён + `NOT_YET_CONVERTED_COUNT` | там же |
| `HX_HEADER_READS = 1`, `HX_HEADER_WRITES = 2` | там же |

## Поправка к исходным артефактам, найденная при планировании

**Обработчиков под декоратором POST в `app/pages/` — 36, а не 35.** D-14 называет
«ещё не переведено: 35». Обход по коду даёт 36: тридцать пять объявлений
`@router.post` плюс одно `@money_router.post` в `app/pages/billing.py:316`.
Ровно это лишнее объявление — ДЕНЕЖНЫЙ маршрут, и гейт, считающий по имени
объекта роутера, потерял бы именно его. Обход обязан ловить любой атрибут `.post`
на любом объекте, как это делает гейт имперсонации. Расхождение записать в
SUMMARY: число объявляется по ФАКТУ обхода, а не по CONTEXT.

**Машинный приёмник ЮKassa (`@router.post("/webhook")`) живёт в
`app/routes/billing.py`, то есть ВНЕ периметра `app/pages/`** и в перечень
исключений не попадает. Если после обхода `NEVER_RESPONDS` оказывается пустым,
он объявляется пустым ЯВНО, с абзацем-обоснованием — а не снимается: пустой
объявленный перечень и отсутствующий перечень читаются по-разному, и второе
означало бы, что вопрос никто не задавал.

## Почему буквальное равенство критерия 1 заменено на пару G-1 + G-2 и счётчик чтений

Критерий 1 ROADMAP просит машинно утвердить, что «множество обработчиков с
`RedirectResponse` СОВПАДАЕТ с множеством, читающим `HX-Request`». Буквальное
равенство этих двух множеств утверждать НЕЛЬЗЯ, и не потому, что это трудно, а
потому, что оно ПРОТИВОРЕЧИТ соседнему требованию той же фазы. FOUND-04 требует,
чтобы признак htmx читался на весь проект РОВНО ОДИН РАЗ — в `app/pages/htmx.py`.
Значит множество «читающих `HX-Request`» по построению состоит из одного
элемента и никогда не сможет совпасть с множеством из тридцати шести
обработчиков. Требование, взятое буквально, было бы невыполнимо ни в одной
реализации, удовлетворяющей FOUND-04.

Смысл критерия сохраняется контрапозицией, и он разбит на три машинных
утверждения, которые ВМЕСТЕ дают то же свойство «решение о форме ответа
принимается в одном месте»:

| Утверждение | Гейт | Что оно даёт от критерия 1 |
|---|---|---|
| признак htmx читается ровно в одном месте, и это `app/pages/htmx.py` | `test_the_header_is_read_in_exactly_one_place` (`HX_HEADER_READS == 1`) | «читающих заголовок» ровно один — правая часть равенства сведена к точке |
| каждый POST-обработчик либо идёт через `respond()`, либо стоит в одном из двух объявленных перечней | G-1 `test_every_post_handler_is_classified` | обработчика, решающего форму ответа МИМО слоя, не существует |
| обработчик, уже переведённый на слой ответа, не строит редирект сам | G-2 `test_no_converted_handler_builds_its_own_redirect` | у переведённого не остаётся ВТОРОГО решения о форме ответа |

Прочитанные вместе, три утверждения означают ровно то, чего просит критерий:
каждый обработчик, у которого есть форма ответа, получает её из единственного
места, а второго места, где эта форма решается, в проекте нет.

⚠️ G-2 СЕГОДНЯ ПРОХОДИТ ВАКУУМНО, И ЭТО НАЗВАНО, А НЕ СПРЯТАНО. До Фазы 9
множество `converted` пусто, поэтому утверждение истинно ни на чём. Гейтом на
пустоту оно при этом не является: его предмет собирается ТЕМ ЖЕ обходом, который
даёт 36, и обход этот проверяется собственным счётчиком — сломанный разбор даёт
красный, а не зелёный ноль. Красным G-2 становится в тот день, когда Фаза 9
переведёт первый обработчик, оставив ему собственный редирект.

Абзац с этой таблицей обязан быть перенесён в SUMMARY плана: `/gsd-verify-work`
читает критерий буквально и без него объявит его непокрытым.

<tasks>

<task type="auto">
  <name>Task 1: G-1/G-2 — гейт единственности решения с двумя перечнями</name>
  <files>tests/test_pages/test_htmx_gates.py</files>
  <read_first>
    - `tests/test_pages/test_impersonation_gate.py` (ЦЕЛИКОМ: докстринг с четырьмя обязательными свойствами; `test_every_mutating_route_is_classified` — равенство объединения объявленных множеств множеству найденных; `test_the_gate_imports_no_application_module`; `test_no_route_is_declared_in_a_form_the_gate_cannot_see`; `test_no_two_mutating_routes_collapse_into_one_key`; группа `-k control` в конце файла)
    - `tests/test_pages/test_access_gate.py` (строки 19-60 — «ПОЧЕМУ ГЕЙТ ПЕРЕЧНЯ ЧИТАЕТ ИСХОДНИК, А НЕ СОБРАННОЕ ПРИЛОЖЕНИЕ» и форма перечня с обоснованием на каждую запись)
    - `app/pages/htmx.py` (создан планом 08-01 — `is_htmx`, `respond`, `refuse`, `location_response`)
    - `app/pages/billing.py` (строка 316 — единственное объявление `@money_router.post`)
  </read_first>
  <action>
Создать `tests/test_pages/test_htmx_gates.py` — гейт множеств по синтаксическому
дереву Python, по форме `test_impersonation_gate.py`.

Обход: рекурсивный `app/pages/**/*.py` (⚠️ `**/*.py`, а не плоский — граница,
закрытая ревизией фазы 6 и обязательная к повторению), сбор каждой функции,
несущей декоратор с атрибутом `.post` НА ЛЮБОМ объекте роутера. Ключ записи —
пара «модуль + имя функции», чтобы столкновение имён не схлопывало две записи в
одну.

Три множества:
1. `converted` — обработчики, в теле которых есть вызов `respond(` (собирается
   ОБХОДОМ, а не объявляется);
2. `NEVER_RESPONDS: dict[str, str]` — обработчики, которые НИКОГДА не пойдут
   через слой ответа, с обоснованием на КАЖДУЮ запись, по форме перечня
   `test_access_gate.py`. Заполняется по факту обхода; если пуст — объявляется
   пустым словарём с абзацем-обоснованием над ним;
3. `NOT_YET_CONVERTED: frozenset[str]` — обработчики, которых Фазы 9-15 ещё не
   перевели, плюс `NOT_YET_CONVERTED_COUNT` — то же число константой.

Гейты:
- `test_the_number_of_post_handlers_is_the_declared_one` — число найденных равно
  `POST_HANDLERS`;
- `test_every_post_handler_is_classified` — объединение трёх множеств РАВНО
  множеству найденных. Обработчик, добавленный будущей фазой, не попадёт ни в
  одно и уронит тест вместо того, чтобы оказаться разрешённым по умолчанию;
- `test_the_three_sets_do_not_overlap`;
- убывающий счётчик отставания — ДВА теста, а не один, по форме G-22 плана
  08-09. Один текст отказа на два разных события лгал бы в одном из них:
  при штатном переводе обработчика Фазой 9 число УПАДЁТ, и единственное
  сообщение «фаза вернула обработчик назад» сообщило бы о регрессии, которой
  не было, — а приученный не верить тексту отказа читатель перестаёт читать
  его вовсе:
  - `test_the_backlog_never_grows` — `len(NOT_YET_CONVERTED)` НЕ БОЛЬШЕ
    `NOT_YET_CONVERTED_COUNT`; сообщение об отказе читается как «фаза вернула
    обработчик назад: переведённый обработчик снова не идёт через слой ответа»;
  - `test_the_backlog_matches_the_declared_count` — `len(NOT_YET_CONVERTED)`
    РАВЕН `NOT_YET_CONVERTED_COUNT`; сообщение об отказе читается как «число
    упало до N — опустите константу и запишите это прогрессом вехи», а не как
    поломка.
  ⚠️ Оба теста ЗЕЛЁНЫЕ на сегодняшнем дереве. Это число — машинный счётчик
  прогресса вехи, и каждое из двух его движений обязано иметь СВОЙ текст;
- `test_the_header_is_read_in_exactly_one_place` — число мест чтения строки
  заголовка htmx в `app/**/*.py` равно `HX_HEADER_READS = 1`, и это место —
  `app/pages/htmx.py`. Число утверждается СОБСТВЕННЫМ обходом гейта (D-13):
  сводный счётчик не поймал бы сломанный обход, а собственное число ловит и
  пустоту, и опечатку в разборе;
- `test_no_converted_handler_builds_its_own_redirect` (G-2) — обработчик из
  множества `converted` не имеет права строить редирект сам: в его теле нет
  `RedirectResponse(` и нет возбуждения исключения со статусом 302. Пока
  `converted` пусто, тест проходит вакуумно — и это НЕ гейт на пустоту: его
  предмет собирается тем же обходом, который даёт 36, и он краснеет в тот день,
  когда Фаза 9 переведёт первый обработчик, оставив ему собственный редирект;
- `test_the_gate_imports_no_application_module` — гейт читает исходники текстом;
- `test_no_post_route_is_declared_in_a_form_the_gate_cannot_see` — объявление
  вызовом (`add_api_route`) запрещено, потому что разборщику оно невидимо. Гейт,
  который чего-то не видит, обязан требовать, чтобы этого и не было.

Докстринг файла обязан нести все четыре свойства гейта проекта и ⚠️ раздел
«ЧЕГО ГЕЙТ НЕ ВИДИТ»: обработчик, объявленный вне `app/pages/` (машинные
приёмники в `app/routes/` — принятая граница, а не пропуск); обработчик,
собранный фабрикой; и вызов `respond(` через псевдоним импорта.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: файл объявляет `POST_HANDLERS`, `NEVER_RESPONDS`, `NOT_YET_CONVERTED`, `NOT_YET_CONVERTED_COUNT`, `HX_HEADER_READS`
    - Behavior: `POST_HANDLERS` равно числу, которое даёт обход, и это число 36 (проверено грепом при планировании: 35 объявлений `@router.post` + 1 `@money_router.post`)
    - Behavior: объединение трёх множеств равно множеству найденных обработчиков — тест `test_every_post_handler_is_classified` зелёный
    - Behavior: `HX_HEADER_READS == 1`, и единственное найденное место — `app/pages/htmx.py`
    - Source: `grep -c 'import app' tests/test_pages/test_htmx_gates.py` == 0
    - Source: файл объявляет ДВА теста счётчика отставания — `test_the_backlog_never_grows` и `test_the_backlog_matches_the_declared_count`
    - Source: сообщение об отказе `test_the_backlog_never_grows` содержит слова о возврате обработчика назад; сообщение об отказе `test_the_backlog_matches_the_declared_count` содержит слова об опускании константы и о прогрессе вехи — тексты РАЗНЫЕ
    - Source: план и SUMMARY несут таблицу сопоставления критерия 1 с тройкой G-1 + G-2 + `HX_HEADER_READS == 1`
    - Test command: `uv run pytest tests/test_pages/test_htmx_gates.py -v` завершается кодом 0
  </acceptance_criteria>
  <done>Единственность решения утверждена машинно; два перечня объявлены раздельно.</done>
</task>

<task type="auto">
  <name>Task 2: G-13 — AST на правый операнд записи заголовка HX-* и группа контроля</name>
  <files>tests/test_pages/test_htmx_gates.py</files>
  <read_first>
    - `tests/test_pages/test_htmx_response_contract.py` (ЦЕЛИКОМ — ПРЯМОЙ образец: `PROJECT_ROOT`/`APP_ROOT`, обход `ast.walk` по всем файлам `app/`, разбор ОБЕИХ форм записи статуса — `ast.Constant` и `ast.Attribute`, инвентарь-ноль именованной константой)
    - `app/pages/ads.py` (строки 515-525 — единственное СЕГОДНЯШНЕЕ место записи заголовка `HX-*`: f-строка с целочисленной подстановкой; образец разрешённой формы)
    - `app/pages/htmx.py` (создан планом 08-01 — `location_response`: второе место записи, чей операнд проверяется рантайм-помощником `_local_path`)
    - `.planning/research/SUMMARY.md` (§R-2 — почему кириллица в значении заголовка роняет ответ в 500)
    - `tests/test_pages/test_impersonation_gate.py` (строки 843-975 — форма группы `-k control`: разборщику подаются изменённые копии исходника во временном каталоге)
  </read_first>
  <action>
Дописать в `tests/test_pages/test_htmx_gates.py` вторую группу — гейт
безопасности записи заголовков ответа.

Обход: `ast.walk` по всем файлам `app/**/*.py`; собираются все места, где
значение присваивается ключу заголовка, начинающемуся с `HX-`, в обеих формах —
подписка (`response.headers["HX-…"] = …`) и ключ в литерале словаря заголовков
(`headers={"HX-…": …}`).

Гейты:
- `test_the_number_of_header_writes_is_the_declared_one` — число найденных мест
  равно `HX_HEADER_WRITES = 2` (запись адреса редактора объявлений в `ads.py` и
  единственная сборка ответа с адресом перехода в `app/pages/htmx.py`);
- `test_every_header_write_has_a_safe_right_operand` — правый операнд каждого
  места принадлежит разрешённому множеству форм: строковый литерал; f-строка,
  все подстановки которой суть обращения к атрибутам или именам (без вызовов и
  без индексации); либо имя, связанное с модульной константой того же модуля;
  либо параметр функции, чей модуль объявляет рантайм-проверку локального пути.
  Последняя форма объявляется в файле теста ПОИМЁННО, одной записью с
  обоснованием, а не разрешается классом: `app/pages/htmx.py::location_response`
  принимает значение параметром, и его безопасность доказывается не разбором
  дерева, а рантайм-проверкой, у которой есть собственные тесты (план 08-01);
- `test_no_header_write_touches_the_incoming_request` — в поддереве правого
  операнда НЕТ имени объекта запроса ни в одном из мест. Это и есть настоящее
  свойство безопасности: значение заголовка не может прийти от клиента ни одним
  путём, и им закрываются разом кириллица в заголовке (latin-1 у Starlette),
  инъекция заголовка и открытый редирект.

Группа контроля (`-k control`) — обязательна, зубы доказываются, а не заявляются:
- копия исходника, где обработчик пишет заголовок значением из строки запроса, —
  гейт краснеет;
- копия, где значение собрано конкатенацией с русским текстом, — гейт краснеет;
- копия, где добавлено третье место записи, — счётчик краснеет;
- копия, где снята зависимость обработчика от слоя ответа, — G-1 краснеет
  (контроль первой группы);
- настоящее дерево — все гейты зелёные.

Докстринг группы обязан назвать, ЧТО ИМЕННО закрывает правило (три угрозы
поимённо) и ЧЕГО оно не видит: значение, собранное вспомогательной функцией
соседнего модуля, разбором дерева не прослеживается — такие места обязаны
объявляться поимённо, и именно поэтому исключение выписано записью, а не классом.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -v && uv run pytest tests/test_pages/test_htmx_gates.py -k control -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: файл объявляет `HX_HEADER_WRITES = 2`
    - Behavior: `test_the_number_of_header_writes_is_the_declared_one` зелёный на настоящем дереве
    - Behavior: `test_no_header_write_touches_the_incoming_request` зелёный на настоящем дереве
    - Behavior: гейт краснеет на копии исходника, где значение заголовка берётся из строки запроса (доказано контролем)
    - Behavior: гейт краснеет на копии, где мест записи стало три (доказано контролем)
    - Test command: `uv run pytest tests/test_pages/test_htmx_gates.py -k control -v` собирает не менее пяти тестов и завершается кодом 0
    - Test command: `uv run pytest tests/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Правый операнд каждой записи заголовка доказуемо безопасен; зубы обоих гейтов доказаны.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| исходник приложения → гейт | гейт читает текст, а не собранное приложение; его вход — дерево репозитория |
| запрос клиента → значение заголовка ответа | правило гейта запрещает этот путь целиком |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | инъекция заголовка через правый операнд записи `HX-*` | high | mitigate | G-13 разбирает дерево и допускает только перечисленные формы операнда; поддерево операнда не имеет права содержать объект запроса |
| T-08-02 | Denial of Service | кириллица в значении заголовка роняет ответ в 500 | medium | mitigate | то же правило: значение либо литерал, либо f-строка без строкового пользовательского ввода; рантайм-помощник отвергает не-ASCII |
| T-08-05 | Tampering | открытый редирект через `HX-Location` | high | mitigate | то же правило плюс рантайм-проверка локального пути; исключение выписано поимённо и подкреплено собственными тестами |
| T-08-30 | Repudiation | гейт зелёный по построению и создаёт уверенность вместо проверки | high | mitigate | группа `-k control` подаёт разборщику изменённые копии исходника и требует красного на каждом из четырёх нарушений |
| T-08-31 | Tampering | обработчик, объявленный формой, невидимой разборщику | medium | mitigate | форма объявления вызовом запрещена собственным тестом; граница выписана в докстринге |
| T-08-SC | Tampering | установка пакетов npm/pip/cargo | high | accept | план не устанавливает ни одного пакета; установочных задач нет |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/test_htmx_gates.py -v` — зелёный
- `uv run pytest tests/test_pages/test_htmx_gates.py -k control -v` — зелёный, не менее пяти тестов
- `uv run pytest tests/ -q` — зелёный
</verification>

<success_criteria>
- Каждый POST-обработчик слоя страниц классифицирован, и объединение множеств равно найденному
- Число непереведённых объявлено, его рост и его падение имеют РАЗНЫЕ тексты отказа
- Критерий 1 сопоставлен с тройкой машинных утверждений таблицей, а не оставлен читателю
- Признак htmx читается ровно в одном месте — утверждено собственным обходом
- Правый операнд каждой записи заголовка `HX-*` доказуемо не приходит из запроса
- Зубы обоих гейтов доказаны группой контроля

## Покрытие пакета гейтов этим планом

| Гейт | Что утверждает | Требование |
|---|---|---|
| G-1 | каждый POST-обработчик идёт через слой ответа либо стоит в одном из двух перечней | GATE-03 |
| G-2 | переведённый обработчик не строит редирект сам | GATE-03 |
| G-13 | правый операнд записи `HX-*` — литерал, f-строка с непользовательскими подстановками либо поимённое исключение | GATE-07 |

Каждый из трёх имеет свой случай в группе `-k control`, доказывающий, что он
краснеет на нарушении и зеленеет на настоящем дереве.
</success_criteria>

<output>
Create `.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-07-SUMMARY.md` when done
</output>
