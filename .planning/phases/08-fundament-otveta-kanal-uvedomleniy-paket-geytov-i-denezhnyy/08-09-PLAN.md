---
phase: 08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy
plan: 09
type: execute
wave: 4
depends_on: ["08-04", "08-06"]
files_modified:
  - tests/test_templates/test_htmx_markup_security.py
  - tests/test_templates/test_htmx_inventory.py
autonomous: true
requirements: [GATE-07, GATE-08]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "В шаблонах нет ни одной конструкции, снимающей экранирование, и ни одной сборки безопасной разметки в коде приложения — оба инвентаря объявлены именованными нулями и утверждаются собственным обходом"
    - "Автоматическое экранирование окружения шаблонов включено, и это утверждается тестом, а не подразумевается"
    - "Значения, уезжающие в атрибут параметров запроса, проходят только через сериализацию в JSON; формы этого атрибута с исполняемым выражением и атрибуты обработчиков событий в разметке запрещены"
    - "Каждый опрашивающий фрагмент отнесён к одному из двух объявленных множеств — бессрочный опрос либо опрос до завершения — и объединение множеств РАВНО множеству найденных обходом"
    - "Опрос до завершения имеет доказанный останов: признак опроса стоит внутри условия шаблонизатора в том же теге, то есть ответ без условия признака не несёт"
    - "Число мест ручной сборки запроса в шаблонах объявлено убывающим счётчиком: гейт краснеет, когда оно ВЫРОСЛО, и отдельно сообщает, когда оно упало, — финальное утверждение о нуле принадлежит Фазе 15"
    - "Шаблон без единого опрашивающего фрагмента в гейт останова не попадает и его не роняет; пустое множество кандидатов даёт зелёный, а объявленное число остаётся утверждением, а не выводом из найденного"
    - "Два фрагмента с одинаковым содержимым остаются двумя записями и не схлопываются в одну: ключ записи — путь файла и позиция, а не текст"
    - "Обход шаблонов идёт по отсортированному списку путей, поэтому сообщение об отказе воспроизводимо от прогона к прогону"
  artifacts:
    - path: "tests/test_templates/test_htmx_markup_security.py"
      provides: "Гейты безопасности шаблонов: ноль конструкций снятия экранирования, ноль сборок безопасной разметки, правила атрибута параметров запроса"
      contains: "def test_"
    - path: "tests/test_templates/test_htmx_inventory.py"
      provides: "Гейт останова опроса и убывающий счётчик ручной сборки запроса"
      contains: "MANUAL_FETCH_PLACES"
  key_links:
    - from: "tests/test_templates/test_htmx_markup_security.py"
      to: "app/templates"
      via: "рекурсивный обход шаблонов с вырезанием комментариев обоих видов"
      pattern: "rglob"
    - from: "tests/test_templates/test_htmx_inventory.py"
      to: "app/templates"
      via: "инвентарь опрашивающих фрагментов и мест ручной сборки запроса"
      pattern: "POLL_PLACES"
  prohibitions:
    - requirement_id: GATE-08
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT записать клаузу о снятии ручной сборки запроса так, чтобы она краснела до Фазы 13: правило, красное по причинам, которых исполнитель не может устранить, обучает игнорировать красный цвет и обесценивает весь пакет"
    - requirement_id: GATE-07
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT ослабить нулевой инвентарь до «предупреждения»: снятие экранирования в шаблоне расширяет радиус межсайтового исполнения на ЛЮБУЮ область страницы, включая шапку и области уведомления, живущие теперь на каждом экране"
---

<objective>
Написать гейты безопасности шаблонов (ноль конструкций снятия экранирования, ноль
сборок безопасной разметки, правила атрибута параметров запроса) и две клаузы
шелл-гейта, живущие в инвентаре разметки: останов опроса и убывающий счётчик
ручной сборки запроса.

Purpose: та часть критерия 4, которая закрывает межсайтовое исполнение, и та
часть GATE-08, которая не относится к шеллу. Внеполосная подмена расширяет радиус
дефекта экранирования на ЛЮБУЮ область страницы, а области уведомления с этой
фазы живут на каждом экране обоих шеллов.
Output: новый файл гейтов безопасности разметки и две группы, дописанные в
действующий инвентарь.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-CONTEXT.md
@.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-PATTERNS.md
@tests/test_templates/test_htmx_inventory.py
</context>

## Artifacts this phase produces

| Артефакт | Где |
|---|---|
| `tests/test_templates/test_htmx_markup_security.py` | новый файл гейтов |
| `ESCAPE_ESCAPES = 0`, `MARKUP_BUILDERS = 0`, `REQUEST_PARAM_ATTRS = 0` | там же — именованные нули |
| `PERMANENT_POLLS`, `TERMINATING_POLLS` | `tests/test_templates/test_htmx_inventory.py` |
| `MANUAL_FETCH_PLACES = 6` | там же — убывающий счётчик прогресса вехи |

## Числа инвентаря, снятые при планировании

| Инвентарь | Значение сегодня |
|---|---|
| конструкции снятия экранирования в шаблонах | **0** |
| сборки безопасной разметки в коде приложения | **0** |
| атрибуты параметров запроса и атрибуты обработчиков событий в разметке | **0** |
| опрашивающие фрагменты (`POLL_PLACES`, уже объявлено) | **8** после вырезания комментариев (сырых вхождений 10) |
| места ручной сборки запроса в шаблонах | **6** в двух файлах |

Все три нуля объявляются ИМЕНОВАННЫМИ константами, а не подразумеваются
отсутствием теста — прецедент `SERVER_SIDE_VALIDATION_RESPONSES = 0` в
`tests/test_pages/test_htmx_response_contract.py`.

<tasks>

<task type="auto">
  <name>Task 1: Гейты безопасности шаблонов — три именованных нуля и их зубы</name>
  <files>tests/test_templates/test_htmx_markup_security.py</files>
  <read_first>
    - `tests/test_templates/test_htmx_inventory.py` (строки 36-45 — почему гейт разметки живёт в этом каталоге; строки 190-210 — регулярки с отрицательным lookbehind и вырезание комментариев обоих видов)
    - `tests/test_pages/test_htmx_response_contract.py` (строки 40-75 — форма именованного нуля: `SERVER_SIDE_VALIDATION_RESPONSES = 0` и обход, его подтверждающий)
    - `app/pages/common.py` (строки 30-40 — создание окружения шаблонов; где утверждается автоматическое экранирование)
    - `app/templates/includes/notice_area.html` и `app/templates/includes/notice_oob.html` (созданы планом 08-04 — области, живущие на каждой странице; именно они расширяют радиус дефекта экранирования)
    - `tests/test_templates/test_ads_form_security.py` (действующий файл безопасности шаблонов — стиль утверждений и соседство)
  </read_first>
  <action>
Создать `tests/test_templates/test_htmx_markup_security.py`.

Три инвентаря, каждый — ИМЕНОВАННЫЙ НОЛЬ, собранный собственным обходом с
вырезанием комментариев обоих видов ДО счёта:
1. `ESCAPE_ESCAPES = 0` — конструкции шаблонизатора, снимающие автоматическое
   экранирование значения. Обход ищет их по регулярке с отрицательным
   lookbehind, чтобы не совпасть внутри более длинного имени;
2. `MARKUP_BUILDERS = 0` — вызовы, объявляющие строку заведомо безопасной
   разметкой, в `app/**/*.py`;
3. `REQUEST_PARAM_ATTRS = 0` — атрибуты разметки, задающие дополнительные
   параметры запроса, и атрибуты, объявляющие обработчик события прямо в
   разметке. Точные имена искомых конструкций выписаны в `<acceptance_criteria>`
   этой задачи; в теле гейта они живут именованными регулярками с абзацем на
   каждую, называющим, ЧТО она закрывает.

Гейты:
- три теста «число равно объявленному нулю», по одному на инвентарь;
- `test_the_template_environment_escapes_by_default` — автоматическое
  экранирование окружения включено; утверждается ЧТЕНИЕМ свойства окружения, а
  не верой;
- `test_request_param_attributes_go_through_json_serialisation` — правило на
  вырост: когда атрибут параметров запроса появится, его значение обязано
  проходить через сериализацию в JSON. Пока инвентарь нулевой, тест утверждает
  ноль и НЕСЁТ правило в докстринге, чтобы следующая фаза не изобретала его
  заново;
- `test_the_gate_reads_sources_not_the_rendered_document` — гейт читает
  исходники, приложение не поднимает.

Группа контроля (`-k control`) — обязательна, иначе три нуля неотличимы от
сломанного обхода:
- копия дерева шаблонов с конструкцией снятия экранирования — гейт краснеет;
- копия с вызовом сборки безопасной разметки в коде — гейт краснеет;
- копия с атрибутом параметров запроса, чьё значение не сериализовано, — гейт
  краснеет;
- копия с атрибутом обработчика события в разметке — гейт краснеет;
- копия, где искомая конструкция стоит внутри комментария, — гейт остаётся
  ЗЕЛЁНЫМ (проза не участвует в инвентаре);
- настоящее дерево — все гейты зелёные.

Докстринг файла обязан назвать, ПОЧЕМУ эти нули стоят дороже, чем выглядят:
внеполосная подмена расширяет радиус дефекта экранирования с одного фрагмента на
ЛЮБУЮ область страницы, а области уведомления с этой фазы живут на КАЖДОМ экране
обоих шеллов. И раздел «⚠️ ЧЕГО ГЕЙТ НЕ ВИДИТ»: значение, объявленное безопасным
на стороне Python и уехавшее в шаблон готовой строкой, обходом шаблонов не
ловится — его закрывает второй инвентарь, и именно поэтому инвентаря ДВА, а не
один.
  </action>
  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_markup_security.py -v && uv run pytest tests/test_templates/test_htmx_markup_security.py -k control -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: файл объявляет `ESCAPE_ESCAPES = 0`, `MARKUP_BUILDERS = 0`, `REQUEST_PARAM_ATTRS = 0`
    - Behavior: `grep -rc '|safe' app/templates/ | grep -v ':0' | wc -l` == 0 — конструкция снятия экранирования отсутствует
    - Behavior: `grep -rc 'Markup(' app/ | grep -v ':0' | wc -l` == 0 — сборка безопасной разметки отсутствует
    - Behavior: `grep -rc 'hx-vals' app/templates/ | grep -v ':0' | wc -l` == 0 и `grep -rc 'hx-on:' app/templates/ | grep -v ':0' | wc -l` == 0
    - Behavior: `templates.env.autoescape` истинно — утверждено тестом
    - Behavior: гейт краснеет на каждой из четырёх изменённых копий и остаётся зелёным на копии, где конструкция стоит в комментарии
    - Test command: `uv run pytest tests/test_templates/test_htmx_markup_security.py -k control -v` собирает не менее шести тестов и завершается кодом 0
  </acceptance_criteria>
  <done>Три нуля объявлены именованными константами, их зубы доказаны, границы выписаны.</done>
</task>

<task type="auto">
  <name>Task 2: Гейт останова опроса и убывающий счётчик ручной сборки запроса</name>
  <files>tests/test_templates/test_htmx_inventory.py</files>
  <read_first>
    - `tests/test_templates/test_htmx_inventory.py` (ЦЕЛИКОМ — `POLL_PLACES = 8`, `CONDITIONAL_PLACES = 2`, `HX_GET_PLACES = 22`, регулярки и обход; сюда дописываются обе группы)
    - `app/templates/account_groups/partials/sync_result.html` (строка 50 — ОБРАЗЕЦ опроса до завершения: признак опроса стоит внутри условия шаблонизатора в том же теге, то есть ответ со статусом «готово» признака не несёт и опрос останавливается)
    - `app/templates/accounts/list.html`, `app/templates/dashboard.html`, `app/templates/admin/workers.html` (образцы БЕССРОЧНОГО опроса — обновление экрана, которое не завершается)
    - `app/templates/ads/form.html` и `app/templates/accounts/connect_tg_user.html` (шесть мест ручной сборки запроса — предмет убывающего счётчика; их снимают Фазы 12 и 13)
    - `tests/test_pages/test_impersonation_gate.py` (форма перечня с обоснованием на КАЖДУЮ запись и требование равенства объединения найденному множеству)
  </read_first>
  <action>
Дописать в `tests/test_templates/test_htmx_inventory.py` две группы.

**Группа останова опроса (G-21).** Множество опрашивающих фрагментов, собранное
обходом, делится на ДВА ОБЪЯВЛЕННЫХ множества, и их объединение обязано быть
РАВНО найденному — этим гейт замкнут на себя:
- `PERMANENT_POLLS: dict[str, str]` — бессрочные опросы, с обоснованием на
  КАЖДУЮ запись: обновление экрана, которое не завершается по своей природе;
- `TERMINATING_POLLS: dict[str, str]` — опросы до завершения, с обоснованием на
  каждую запись.

Гейты:
- `test_every_polling_fragment_is_classified` — объединение равно найденному;
  фрагмент, добавленный будущей фазой, не попадёт ни в одно множество и уронит
  тест вместо того, чтобы оказаться бессрочным по умолчанию;
- `test_every_terminating_poll_declares_its_stop` — у каждого опроса до
  завершения признак опроса стоит ВНУТРИ условия шаблонизатора в том же теге,
  то есть в ответе без условия признака нет и опрос останавливается сам;
- `test_the_two_poll_sets_do_not_overlap`;
- ⚠️ ключ записи — ПУТЬ ФАЙЛА ПЛЮС ПОРЯДКОВЫЙ НОМЕР вхождения, а не текст тега:
  два фрагмента с одинаковым содержимым обязаны остаться двумя записями и не
  схлопнуться в одну. Форма наследуется у `test_no_two_mutating_routes_collapse_into_one_key`;
- обход идёт по ОТСОРТИРОВАННОМУ списку путей, чтобы сообщение об отказе было
  воспроизводимо от прогона к прогону.

**Группа убывающего счётчика ручной сборки запроса (G-22).**
`MANUAL_FETCH_PLACES = 6` — число мест ручной сборки запроса в шаблонах, с
абзацем-обоснованием над константой:

⚠️ ЭТО НЕ СЛУЖЕБНАЯ КОНСТАНТА, А СЧЁТЧИК ПРОГРЕССА ВЕХИ, И КЛАУЗА НАМЕРЕННО
НЕПОЛНА. Финальное утверждение «мест не осталось ни одного» принадлежит FETCH-03
Фазы 15; физически позеленеть оно может только после Фазы 13, когда мастер
подключения переедет на фрагменты. Клауза пишется ЗДЕСЬ, чтобы правило
существовало до массовой правки, но утверждает она сегодняшнее число, а не ноль:
правило, красное по причинам, которых исполнитель не может устранить, обучает
игнорировать красный цвет и обесценивает весь пакет.

Два теста, а не один:
- `test_manual_request_assembly_never_grows` — найденное число НЕ БОЛЬШЕ
  объявленного; сообщение об отказе читается как «фаза вернула ручную сборку
  запроса назад»;
- `test_manual_request_assembly_matches_the_declared_count` — найденное число
  РАВНО объявленному; сообщение об отказе читается как «число упало до N —
  опустите константу и запишите это прогрессом вехи», а не как поломка.

Оба теста ЗЕЛЁНЫЕ на сегодняшнем дереве, и это обязательное свойство: план не
имеет права оставить суите красный тест.

Группа контроля (`-k control`) для обеих групп: копия дерева с новым
неклассифицированным опрашивающим фрагментом — краснеет; копия, где опрос до
завершения лишился условия, — краснеет; копия с седьмым местом ручной сборки
запроса — краснеет первый тест; копия с пятью местами — краснеет второй тест;
пустая копия дерева без единого опрашивающего фрагмента — гейт останова зелёный
(пустое множество кандидатов не роняет правило); настоящее дерево — всё зелёное.
  </action>
  <verify>
    <automated>uv run pytest tests/test_templates/test_htmx_inventory.py -v && uv run pytest tests/test_templates/test_htmx_inventory.py -k control -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: файл объявляет `PERMANENT_POLLS`, `TERMINATING_POLLS` и `MANUAL_FETCH_PLACES = 6`
    - Behavior: объединение двух множеств опросов равно множеству, найденному обходом (`POLL_PLACES`)
    - Behavior: `grep -rno 'fetch(' app/templates/ | wc -l` == 6 — объявленное число совпадает с фактом
    - Behavior: оба теста счётчика ЗЕЛЁНЫЕ на текущем дереве — ни один не оставлен красным
    - Behavior: гейт останова остаётся зелёным на копии дерева без единого опрашивающего фрагмента
    - Behavior: гейт краснеет на копии с седьмым местом ручной сборки и на копии с пятью
    - Test command: `uv run pytest tests/test_templates/test_htmx_inventory.py -v` завершается кодом 0
    - Test command: `uv run pytest tests/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Опросы классифицированы и их останов доказан; счётчик ручной сборки объявлен убывающим и зелёным.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| данные приложения → разметка страницы | внеполосная подмена делает целью ЛЮБУЮ область страницы, а области уведомления живут на каждом экране |
| разметка → рантайм htmx | атрибут параметров запроса и атрибут обработчика события — второй слой экранирования, который автоматическое экранирование шаблонизатора не закрывает |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Tampering | межсайтовое исполнение через снятие экранирования в шаблоне | high | mitigate | `ESCAPE_ESCAPES = 0` и `MARKUP_BUILDERS = 0`, оба собранные собственным обходом; автоматическое экранирование окружения утверждается тестом; зубы доказаны четырьмя копиями контроля |
| T-08-37 | Tampering | второй слой экранирования в атрибуте параметров запроса и исполняемое выражение в разметке | high | mitigate | `REQUEST_PARAM_ATTRS = 0` плюс правило «только через сериализацию в JSON», записанное на вырост; формы с исполняемым выражением и атрибуты обработчиков событий запрещены |
| T-08-38 | Denial of Service | опрос, который никогда не останавливается, множится на число открытых вкладок | medium | mitigate | опросы классифицированы двумя объявленными множествами; у опроса до завершения останов доказан положением признака внутри условия |
| T-08-30 | Repudiation | нулевой инвентарь неотличим от сломанного обхода | high | mitigate | три нуля — именованные константы собственного обхода; копия контроля с конструкцией внутри комментария доказывает, что обход и проза различаются |
| T-08-39 | Repudiation | клауза, красная по неустранимым причинам, обучает игнорировать красный цвет | medium | mitigate | счётчик ручной сборки объявлен убывающим и утверждает сегодняшнее число; финальное утверждение о нуле явно отнесено к Фазе 15 |
| T-08-SC | Tampering | установка пакетов npm/pip/cargo | high | accept | план не устанавливает ни одного пакета; установочных задач нет |

**Канон, не минтуемый прохибицией (breadcrumb):** межсайтовое исполнение и
политика содержимого — канон OWASP, покрывается `/gsd-secure-phase` и строками
T-08-12/T-08-37.
</threat_model>

<verification>
- `uv run pytest tests/test_templates/ -v` — зелёный
- `uv run pytest tests/test_templates/ -k control -v` — зелёный
- `uv run pytest tests/ -q` — зелёный, ни одного красного теста не оставлено
</verification>

<success_criteria>
- Три инвентаря безопасности объявлены именованными нулями и доказали зубы
- Автоматическое экранирование утверждается, а не подразумевается
- Опрашивающие фрагменты классифицированы, и объединение множеств равно найденному
- Счётчик ручной сборки запроса объявлен убывающим, зелёный сегодня, и его финальное утверждение отнесено к Фазе 15

## Покрытие пакета гейтов этим планом

| Гейт | Что утверждает | Требование |
|---|---|---|
| G-14 | ноль конструкций снятия экранирования и ноль сборок безопасной разметки | GATE-07 |
| G-15 | ноль атрибутов параметров запроса и обработчиков событий в разметке; правило сериализации на вырост | GATE-07 |
| G-21 | каждый опрашивающий фрагмент классифицирован; опрос до завершения имеет доказанный останов | GATE-08 |
| G-22 | места ручной сборки запроса не растут; число объявлено убывающим счётчиком | GATE-08 |

Каждый имеет свой случай в группе `-k control`.
</success_criteria>

## Flagged planner assumptions (edge probe, unresolved)

Зондирование краёв вернуло `unclassified — review manually` по GATE-07. Записано
допущением, а не выброшено: **предполагается, что «безопасность ответа»
измеряется тремя инвентарями по ИСХОДНИКАМ (шаблоны и код приложения) и не
включает проверку отрендеренного документа.** Значение, объявленное безопасным на
стороне Python и уехавшее в шаблон готовой строкой, обходом шаблонов не ловится;
второй инвентарь (сборки безопасной разметки в коде) закрывает известные формы,
но не произвольную. Требует решения человека, если появится третий способ внести
готовую разметку.

<output>
Create `.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-09-SUMMARY.md` when done
</output>
