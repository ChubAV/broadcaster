---
phase: 14-avtorizatsiya-na-htmx
plan: 04
type: tdd
wave: 4
depends_on: ["14-03"]
files_modified:
  - app/pages/auth.py
  - app/templates/auth/forgot_password.html
  - app/templates/auth/forgot_password_verify.html
  - app/templates/auth/includes/forgot_password_step.html
  - app/templates/auth/includes/forgot_password_verify_step.html
  - tests/test_pages/test_auth_transport.py
  - tests/test_pages/test_password_reset.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [SIGN-01, SIGN-02]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: sample_count 0, factor 1; confidence выведен, а не самооценён.
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Неизвестный адрес на `POST /forgot-password/send-code` отвечает 422 с «Пользователь с таким email не найден» и адресом в `value=` на обоих транспортах; с признаком — `<title>Забыли пароль — Broadcaster</title>` первым узлом (D-03, D-04)"
    - "Известный адрес открывает экран кода восстановления: 200 на обоих транспортах, с признаком — фрагмент с `<title>Код подтверждения — Broadcaster</title>` первым узлом и токеном восстановления скрытым полем; «код уже отправлен» в пределах минуты — 200 на экране кода (D-03, D-06, D-07)"
    - "Повтор кода восстановления — 200 и обе формы экрана с одним токеном и «Новый код отправлен на вашу почту.»; раньше минуты — 422 на экране кода; устаревшая ссылка — 200 и экран начала восстановления с «Ссылка устарела. Начните сброс пароля заново.» (D-03, D-06)"
    - "Под чужой личностью оба шага на пути htmx отвечают 204 и `HX-Location: /dashboard?notice=impersonation_forbidden` без тела, без htmx — 403, как сегодня; ни кода, ни письма не заводится (D-13, D-22)"
    - "Форма адреса и форма повтора — из макроса с целью `#auth-step`; без JS обе отправляются как сегодня (критерий 2, D-09)"
    - "Гейты: `NOT_YET_CONVERTED_COUNT` 5 → 3 и прочие числа поставлены прогоном"
    - statement: "В браузере восстановление для существующего ящика показывает экран кода без перезагрузки, настоящее письмо с кодом приходит — ручной UAT фазы (критерий 4, D-02)"
      verification: backstop
  artifacts:
    - "app/pages/auth.py — `forgot_password_send_code`, `forgot_password_resend_code` на выходах слоя; записи `forgot_password`, `forgot_password_verify` в `AUTH_SCREENS`"
    - "app/templates/auth/includes/forgot_password_step.html, forgot_password_verify_step.html — единственные источники разметки двух экранов"
  key_links:
    - "форма адреса → `forgot_password_send_code` → `respond_screen` → экран кода с токеном в скрытом поле обеих форм: ломается — путь без JS теряет токен"
    - "`forbid_when_impersonating` → `HtmxRefusal` → `location_response` на пути htmx: ломается — отказ под чужой личностью приходит JSON-телом в якорь"
  prohibitions:
    - statement: "восстановление пароля под чужой личностью остаётся запрещённым на всех шагах — фаза зависимость отказа не трогает (D-13, D-22)"
      status: flagged-unverified
      verification: none
    - statement: "подписанный токен восстановления не попадает в адрес, историю и заголовки перехода (D-08)"
      status: flagged-unverified
      verification: none
    - statement: "инвентарное число гейта ставится ПРОГОНОМ покрасневшего правила, а не вычитанием в уме (D-13 Фазы 8)"
      status: flagged-unverified
      verification: none
---

<objective>
Первая половина восстановления пароля на фрагментах: «почта → экран кода» и повтор кода — тем же
устройством, что регистрация в 14-02 (экран включаемым шаблоном, 422 на том же экране, 200 на
смене экрана, подмена всего якоря). Отказ под чужой личностью (D-13) на пути htmx утверждается
парой на двух переведённых шагах.

Purpose: восстановление — второй путь критерия 4 и половина девяти форм критерия 2. Форма
подтверждения кода восстановления переходит на макрос в плане 14-05 вместе со своим обработчиком —
окно названо в §scope_note.

Output: два переведённых обработчика, два экрана включаемыми шаблонами, пары отказа под чужой
личностью, сдвинутые перечни.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-avtorizatsiya-na-htmx/14-CONTEXT.md
@.planning/phases/14-avtorizatsiya-na-htmx/14-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**`app/pages/htmx.py`:** `redirect_internal` (14-01), `respond_screen`, `_respond_by_transport`
(14-02). **`app/pages/auth.py`:** `AUTH_STEP_RESPONSE_TEMPLATE`, `AuthScreen`, `AUTH_SCREENS`
(+ `forgot_password`, `forgot_password_verify` — этот план; `forgot_password_reset` — 14-05),
`_screen_markup`, `_screen_builders`. **Шаблоны:** `auth/includes/step_response.html`,
`login_step.html`, `register_step.html`, `register_verify_step.html`, `register_complete_step.html`,
`forgot_password_step.html`, `forgot_password_verify_step.html` (этот план),
`forgot_password_reset_step.html` (14-05); якорь `auth-step`. **Тесты:**
`tests/test_pages/test_auth_transport.py`; `RESPONSE_LAYER_EXITS`, `FULL_LOAD_CALL`,
`FULL_LOAD_HANDLERS`, `AUTH_MODULE`, `AUTH_HX_LOCATION_HANDLERS`, `DECISION_OWNER_D01_F14`,
`LIFTING_CONDITION_OWN_RESPONSE_RETURN`, `_full_load_callers`, `_hx_location_emitters` (14-06);
ветки пар `FULL_LOAD`, `SCREEN`, `ANONYMOUS`, `_PairCase.sets_session_cookie`, ключи `AUTH_*`
(`AUTH_FORGOT_SEND_CODE`, `AUTH_FORGOT_RESEND_CODE` — этот план). **Записи (14-07):** летописи
ROADMAP, REQUIREMENTS, PROJECT, окно 63, `14-UAT.md`.
</artifacts_this_phase_produces>

<scope_note>
## Что план СОЗНАТЕЛЬНО не делает (окно внутри фазы, в поставку не уходит)

- **Форма подтверждения кода восстановления** остаётся обычной формой
  (`action="/forgot-password/verify"`) до плана 14-05 — по тому же основанию, что окно 14-02:
  её обработчик ещё отвечает готовыми страницами. Эхо кода и синхронизация двух форм — 14-05.
- **Зависимость отказа под чужой личностью не трогается** (D-13): она уже отвечает двумя
  транспортами; план только утверждает её htmx-половину на переведённых шагах.
</scope_note>

<tdd_notes>
- Коммиты: RED — `test(14-04): …`, GREEN — `feat(14-04): …`, перечни — `test(14-04): record …`.
- RED-улика — как в 14-01 (`--junit-xml` → TAP в scratchpad). Целевое правило —
  `test_an_unknown_email_keeps_the_address_and_answers_422_on_both_transports`; RED засчитывается
  тремя совпадениями в выводе
  `uv run pytest tests/test_pages/test_auth_transport.py -q -p no:randomly -k unknown_email`:
  (1) `^FAILED tests/test_pages/test_auth_transport.py::test_an_unknown_email_keeps_the_address_and_answers_422_on_both_transports`;
  (2) итоговая строка вида `1 failed`; (3) причинный литерал `assert 200 == 422` — на дереве до
  правки неизвестный адрес отвечает готовой страницей со статусом 200 (`app/pages/auth.py:558-564`,
  проверено чтением).
</tdd_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Восстановление «почта → экран кода» и повтор кода на фрагментах якоря, отказ под чужой личностью на пути htmx</name>

  <files>
app/pages/auth.py,
app/templates/auth/forgot_password.html,
app/templates/auth/forgot_password_verify.html,
app/templates/auth/includes/forgot_password_step.html,
app/templates/auth/includes/forgot_password_verify_step.html,
tests/test_pages/test_auth_transport.py
  </files>

  <read_first>
- `app/pages/auth.py` после 14-03: помощники экранов, переведённые обработчики регистрации
  (образец); блок восстановления `:527-768` дерева до фазы (комментарий D-22, `forgot_password_page`,
  `forgot_password_send_code`, `forgot_password_resend_code`).
- `app/templates/auth/forgot_password.html`, `app/templates/auth/forgot_password_verify.html`;
  `app/templates/auth/includes/register_step.html` и `register_verify_step.html` в редакции 14-02
  (образец окна с обычной формой подтверждения).
- `app/dependencies.py:320-402` (`IMPERSONATION_REFUSED_LOCATION`, `forbid_when_impersonating`),
  `app/main.py:220-231` (`HtmxRefusal` → `location_response`).
- `tests/test_pages/test_impersonation.py:1-80` (`_seed_target`, `_enter`) и `:1078-1128` (отказ
  четырёх шагов без htmx).
- `tests/test_pages/test_password_reset.py:1-60`.
- `14-RESEARCH.md` §Карта выходов (строки `forgot_password_send_code`, `forgot_password_resend_code`,
  «четыре `forgot_*` под чужой личностью»), §Находка 6 (последняя строка).
  </read_first>

  <behavior>
RED (`tests/test_pages/test_auth_transport.py`):
1. `test_an_unknown_email_keeps_the_address_and_answers_422_on_both_transports` — 422, «Пользователь
   с таким email не найден», адрес в `value=`; с признаком — `<title>Забыли пароль — Broadcaster</title>`.
2. `test_the_recovery_email_step_answers_the_code_screen_on_both_transports` — известный адрес: без
   признака 200, `<!DOCTYPE`, `action="/forgot-password/verify"`; с признаком 200,
   `<title>Код подтверждения — Broadcaster</title>`, `hx-post="/forgot-password/resend-code"`,
   `name="token"`, заголовков перехода нет.
3. `test_a_repeated_recovery_step_within_a_minute_lands_on_the_code_screen` — 200, «Код уже отправлен.
   Подождите минуту перед повторной отправкой.».
4. `test_resending_the_recovery_code_redraws_both_forms_with_one_token` — посев кода старше минуты:
   200, «Новый код отправлен на вашу почту.», два скрытых `token` с одним значением.
5. `test_resending_the_recovery_code_within_a_minute_answers_422` — 422, «Подождите минуту перед
   повторной отправкой.».
6. `test_resending_with_a_stale_recovery_link_returns_to_the_start` — 200, «Ссылка устарела. Начните
   сброс пароля заново.», с признаком — `<title>Забыли пароль — Broadcaster</title>`.
7. `test_the_first_recovery_steps_are_refused_under_another_identity_on_both_transports` — админ под
   чужой личностью (`_seed_target`, `_enter` из `test_impersonation.py`): `send-code` и
   `resend-code` без признака → 403; с признаком → 204, `HX-Location ==
   "/dashboard?notice=impersonation_forbidden"`, пустое тело; строк `EmailVerificationCode` с
   назначением `password_reset` не прибавилось.
GREEN — обработчики и экраны.
  </behavior>

  <action>
**1. `app/pages/auth.py` (D-03, D-04, D-06, D-13, D-14, D-15).** Записи
`"forgot_password": AuthScreen(page="auth/forgot_password.html",
step="auth/includes/forgot_password_step.html", title="Забыли пароль — Broadcaster")` и
`"forgot_password_verify": AuthScreen(page="auth/forgot_password_verify.html",
step="auth/includes/forgot_password_verify_step.html", title="Код подтверждения — Broadcaster")`.
`forgot_password_send_code`: адрес не найден → `respond_field_error` экраном `forgot_password` с
прежним текстом и `email`; «код уже отправлен» → `respond_screen` экраном
`forgot_password_verify` с `email`, токеном восстановления и прежним текстом; успех →
`respond_screen` экраном `forgot_password_verify`. `forgot_password_resend_code`: токен негоден →
`respond_screen` экраном `forgot_password` с «Ссылка устарела. Начните сброс пароля заново.»;
раньше минуты → `respond_field_error` экраном `forgot_password_verify` с присланным токеном;
успех → `respond_screen` с новым токеном и `success`. Зависимость `forbid_when_impersonating`,
запросы, минута, срок, строки генерации кода через `secrets.randbelow`, отправка письма и тексты —
без изменений (D-13, D-15, CR-02). Прежние возвраты снимаются целиком (D-14). Комментарий D-22 над
блоком не трогается.

**2. Шаблоны.** `auth/includes/forgot_password_step.html` (новый): `<p class="auth-subtitle">
Восстановление пароля</p>`, алерт ошибки, блочный вызов
`form_wrapper(action='/forgot-password/send-code', target='#auth-step', swap='innerHTML')` с
`<div class="auth-form">`, полем email (`value=email or ''`) и кнопкой «Отправить код», ниже
«Вернуться ко входу». `auth/includes/forgot_password_verify_step.html` (новый) — по форме
`register_verify_step.html` в редакции 14-02: подзаголовок «Введите код из письма», алерты,
строка об отправленном коде, ОБЫЧНАЯ форма подтверждения (окно до 14-05), форма повтора
`form_wrapper(action='/forgot-password/resend-code', target='#auth-step', swap='innerHTML')` с
`<div class="auth-form auth-form--secondary">`, ссылка «Вернуться ко входу»; шапка называет общий
токен, подмену всего якоря и окно. Страницы — `extends`, прежний блок `title`, одно включение.

**3. Тесты** — правила `<behavior>`; адреса литералами первым аргументом `.post(…)`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_auth_transport.py tests/test_pages/test_impersonation.py tests/test_pages/test_reset_code_source.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_auth_transport.py -q -p no:randomly -k "unknown_email or recovery"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def forgot_password_send_code/,/^@router/' app/pages/auth.py | grep -c 'TemplateResponse'` возвращает `0`; то же для `forgot_password_resend_code` — `0`.
- `awk '/^async def forgot_password_send_code/,/^@router/' app/pages/auth.py | grep -c 'Depends(forbid_when_impersonating)'` возвращает `1`; то же для `forgot_password_resend_code` — `1`.
- `grep -c 'secrets.randbelow' app/pages/auth.py` возвращает `4`.
- `grep -c "target='#auth-step'" app/templates/auth/includes/forgot_password_step.html` возвращает `1`; в `forgot_password_verify_step.html` — `1`.
- `uv run pytest tests/test_pages/test_auth_transport.py -q -p no:randomly -k "refused_under_another_identity"` — правило исполнено и зелено.
  </acceptance_criteria>

  <done>
Человек восстанавливает пароль до экрана кода без перезагрузки; неизвестный адрес остаётся в поле;
повтор кода перерисовывает обе формы одним токеном; под чужой личностью шаги закрыты на обоих
транспортах.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Перечни первой половины восстановления и прежнее утверждение об ошибке адреса</name>

  <files>
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_password_reset.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py` — `NOT_YET_CONVERTED`, `FRAGMENT_RESPONSE_HANDLERS` и
  летописи после 14-03.
- `tests/test_pages/test_htmx_post_pairs.py` — посевы и случаи регистрации 14-02 (образец).
- `tests/test_templates/test_htmx_markup_gates.py` — записи экранов регистрации в кортежах
  вызывающих.
- `tests/test_pages/test_password_reset.py:33-45`.
  </read_first>

  <action>
Числа — ПРОГОНОМ покрасневшего правила, летопись «Фаза 14, план 14-04» с дословным выводом.

**1. `tests/test_pages/test_htmx_gates.py`.** Ключи `forgot_password_send_code` и
`forgot_password_resend_code` снимаются из `NOT_YET_CONVERTED` ПЕРЕВОДОМ;
`NOT_YET_CONVERTED_COUNT` 5 → 3; оба — в `FRAGMENT_RESPONSE_HANDLERS` с комментарием на запись;
`FRAGMENT_RESPONSE_HANDLERS_DECLARED` 21 → 23.

**2. `tests/test_pages/test_htmx_post_pairs.py`.** Ключи `AUTH_FORGOT_SEND_CODE`,
`AUTH_FORGOT_RESEND_CODE`; личность `ANONYMOUS`; посев — пользователь на уникальный адрес каждого
вызова (половин две, и вторая иначе попала бы в минуту между кодами). Случаи: адрес — успех
(`SCREEN`, метка `action="/forgot-password/verify"`), повтор — успех (`SCREEN`, метка «Новый код
отправлен»), повтор — устаревшая ссылка (`SCREEN`, метка `action="/forgot-password/send-code"`).
Комментарий: отказ под чужой личностью в реестр не входит — его половины утверждены правилом
`test_the_first_recovery_steps_are_refused_under_another_identity_on_both_transports`.
`POST_PAIR_CASES_DECLARED` 68 → 71 и `PAIRED_302_ASSERTIONS_DECLARED` — прогоном (ожидание:
второе не двигается).

**3. `tests/test_templates/test_htmx_markup_gates.py`.** `auth/includes/forgot_password_step.html` и
`auth/includes/forgot_password_verify_step.html` — в оба кортежа вызывающих с комментарием и
замером G-9 / G-11 / G-12; `MACRO_DEFINITION_SITES_CALLERS_DECLARED` 28 → 30,
`PARAMETRIC_SWAP_TARGETS_CALLERS_DECLARED` 13 → 15, `UNREACHABLE_TARGET_CALL_BLOCKS_MEASURED`
24 → 26 — прогоном.

**4. `tests/test_pages/test_password_reset.py` (D-03).** `test_send_code_unknown_email` ждёт 422
вместо 200; проверки текста не трогаются; комментарий-летопись «Фаза 14, план 14-04, D-03».
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_password_reset.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_inventory.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_markup_security.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_impersonation_gate.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_preserved.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_auth_transport.py tests/test_pages/test_password_reset.py tests/test_pages/test_impersonation.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^NOT_YET_CONVERTED: frozenset/,/^\)/' tests/test_pages/test_htmx_gates.py | grep -v '^\s*#' | grep -cE 'forgot_password_(send|resend)_code'` возвращает `0`.
- `grep -c 'AUTH_FORGOT_SEND_CODE' tests/test_pages/test_htmx_post_pairs.py` не меньше `2`; `grep -c 'Фаза 14, план 14-04' tests/test_pages/test_htmx_gates.py` не меньше `2`.
- `grep -c 'auth/includes/forgot_password_verify_step.html' tests/test_templates/test_htmx_markup_gates.py` не меньше `2`.
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q -p no:randomly -k "every_converted_handler_has_a_pair"` — зелено.
  </acceptance_criteria>

  <done>
Первая половина восстановления описана перечнями; числа поставлены прогоном; прежнее утверждение
об ошибке адреса следует D-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | поля `email`, `token`; признак htmx; cookie с признаком действующего лица |
| страничный слой → браузер | экран кода с токеном восстановления; эхо адреса в 422; переход отказа под чужой личностью |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-13 | Elevation of Privilege (перехват восстановления под чужой личностью) | `forgot_password_send_code`, `forgot_password_resend_code` | high | mitigate | Зависимость `forbid_when_impersonating` не меняется (D-13, D-22); правило отказа на обоих транспортах с проверкой «ни одной строки кода не заведено» |
| T-14-03 | Tampering (XSS через эхо адреса) | экран начала восстановления | high | mitigate | Автоэкранирование; разметочный гейт безопасности ответа |
| T-14-07 | Information Disclosure (токен восстановления в адресе) | экран кода | medium | mitigate | Токен только скрытым полем; ответ 200 без заголовков перехода |
| T-14-09 | Info Disclosure (перечисление адресов) | «Пользователь с таким email не найден» | medium | accept | Вне объёма (CONTEXT Deferred); текст переезжает дословно (D-15) |
</threat_model>

<verification>
- После каждой задачи — её `<verify>`; последняя команда задачи 2 — прогон гейтов волны.
- `uv run python -m compileall -q app main.py tests`; `graphify update .`.
</verification>

<success_criteria>
1. «Почта → экран кода» и повтор кода восстановления — на фрагментах якоря; ошибки — 422 с эхом.
2. Под чужой личностью шаги закрыты на обоих транспортах.
3. Перечни сдвинуты прогоном.
</success_criteria>

<output>
Create `.planning/phases/14-avtorizatsiya-na-htmx/14-04-SUMMARY.md` when done.
</output>
