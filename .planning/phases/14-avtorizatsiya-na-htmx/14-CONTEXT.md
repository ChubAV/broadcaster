# Phase 14: Авторизация на htmx - Context

**Gathered:** 2026-09-22
**Status:** Ready for planning

<domain>
## Phase Boundary

Фаза переводит девять форм авторизации и возврат из-под чужой личности на htmx. Главный
выигрыш — в ОШИБКЕ: неверный пароль или код больше не стирает заполненное. Требования —
**SIGN-01, SIGN-02, SIGN-03**.

**Внутри границы — десять обработчиков `app/pages/auth.py`** (весь остаток счётчика
`NOT_YET_CONVERTED`, `tests/test_pages/test_htmx_gates.py:272-281`, `NOT_YET_CONVERTED_COUNT = 10`
на `:638`):
- `login_submit`;
- `register_send_code`, `register_verify`, `register_resend_code`, `register_complete`;
- `forgot_password_send_code`, `forgot_password_verify`, `forgot_password_resend_code`,
  `forgot_password_reset`;
- `stop_impersonation`.

Также внутри: семь шаблонов `app/templates/auth/*.html`, шелл `app/templates/auth_base.html`
(только якорь шага, D-06), форма возврата в `app/templates/base.html:74`, слой ответа
`app/pages/htmx.py` (новый внутренний выход полной перезагрузки, D-11), тесты этих маршрутов
и перечни гейтов, которые держат обработчики поимённо.

**Вне границы, названо поимённо:**
- JSON-маршруты `app/routes/auth.py` (`/api/auth/*`);
- решения обработчиков (что проверять, в каком порядке, тексты, сроки, лимиты — D-15);
- набор атрибутов cookie (`_session_cookie_attrs`) и умолчание `cookie_secure`;
- `hx-push-url` на формах авторизации (решается Фазой 15, D-13 Фазы 11);
- защита входа от подделки запроса, перебора и перечисления адресов (см. Deferred).

</domain>

<decisions>
## Implementation Decisions

### Отказ по источнику у возврата — РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-09-22)

- **D-01:** **Голый `Response(status_code=403)` у `stop_impersonation` на провале
  `is_same_origin` ОСТАЁТСЯ и объявляется именованным изъятием.** Это то же решение, что
  D-08 Фазы 11, продлённое на случай, который Фаза 11 оставила этой фазе
  (`11-CONTEXT.md:159`). Основание и цена те же:
  - из интерфейса на htmx-пути отказ недостижим;
  - при редком сбое заголовков у прокси человек увидит общую плашку «Действие не выполнено»;
  - поддельная форма со стороннего сайта получает тот же 403, что и сегодня.

  Как это оформляется:
  - после перевода обработчик входит во вселенную правила G-2 и получает запись в
    `OWN_RESPONSE_EXITS` со ссылкой на D-08 Фазы 11 и на этот D-01;
  - `OWN_RESPONSE_EXITS_DECLARED` сейчас равно **13** (`test_htmx_gates.py:4847`). Сдвиг
    ожидается на 13 → 14, но число ставит прогон, а не прогноз (D-13 Фазы 8);
  - **окно 63** (`.planning/WINDOWS.md:80`): после этой записи решение о форме отказа
    принято для всех мест. Исполнитель проверяет это замером. Если выходов с нерешённой
    формой не осталось, строка окна переводится `open` → `waived` штатной командой реестра
    со ссылкой на оба решения владельца, а у окна ставится летопись. Если замер покажет
    иное, окно остаётся `open` с летописью, называющей остаток.

  Отвергнуто: отказ через слой ответа с новым кодом уведомления и переходом.
  — **Reversibility:** reversible.

### Ручной обход — РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-09-22)

- **D-02:** **Коды подтверждения в ручном обходе (критерий 4) приходят НАСТОЯЩИМ письмом.**
  На стенде настроен SMTP, и код берётся из реального ящика. Вариант «читать код из базы»
  отвергнут: обход проверяет путь целиком, включая доставку.
  - Предпосылки обхода, которые планировщик выписывает в UAT: рабочий SMTP на стенде;
    доступный ящик для регистрации нового адреса; ящик существующей учётной записи для
    восстановления пароля; учётная запись администратора для возврата из-под чужой
    личности.
  - Если на момент обхода письма не доходят, обход ОСТАНАВЛИВАЕТСЯ и вопрос уходит
    владельцу. Молча подставлять код из базы нельзя.
  - Машинная улика обхода — не приёмка: `result` и отметки заполняет человек.

### Ошибка заполнения: 422 и эхо введённого (SIGN-02) — принято по умолчанию при делегировании

- **D-03:** **Правило статуса.**
  - **422** — действие не состоялось и человек остаётся на ТОМ ЖЕ экране: неверный пароль,
    учётная запись заблокирована, адрес уже зарегистрирован, адрес не найден, неверный код,
    код истёк или попытки кончились, короткий пароль, повтор кода раньше минуты.
  - **200** — экран сменился: следующий шаг; ответ «код уже отправлен» ведёт на экран кода;
    «ссылка устарела» и «пользователь не найден» возвращают на начало пути.

  Выход — `respond_field_error()` (`app/pages/htmx.py:847`): код один на оба транспорта,
  путь без JS — та же страница. Тексты переезжают ДОСЛОВНО, включая `BLOCKED_LOGIN_ERROR`
  и «Осталось попыток: N». Правило `422` блока конфигурации уже `swap: true, error: true`,
  а плашка ошибки 422 пропускает (`htmx_error_banner.html:306`).
- **D-04:** **Эхо — всё введённое, кроме паролей.** Пароль не возвращается НИКОГДА, поле
  приходит пустым (та же линия, что D-08 Фазы 13).

  | Экран | Эхо после фазы | Сегодня |
  |---|---|---|
  | вход | email | теряется |
  | начало регистрации, начало восстановления | email | уже возвращается |
  | ввод кода (оба пути) | набранный код в `value=`, скрытый `token` — как сегодня | код теряется |
  | завершение регистрации | имя | теряется |
  | новый пароль | (только пароль — эха нет) | — |

  Эхо идёт ТОЛЬКО автоэкранированием шаблона: ни `|safe`, ни `Markup(` на этом пути
  (докстринг `respond_field_error`). Машинно на каждом пути ошибки и на обоих транспортах
  проверяется три вещи:
  - введённое значение стоит в `value=`;
  - строки пароля в теле ответа нет;
  - враждебное значение приходит экранированным (образец — план 11-18).
- **D-05:** **Отказ заблокированному — тот же текст, 422, email возвращается.** Порядок
  «проверка пароля → проверка блокировки → cookie» не меняется: отказ стоит ДО выдачи
  cookie (D-30, `auth.py:129-138`). Пара на обоих транспортах требует: cookie в ответе нет.

### Шаги внутри второго шелла — принято по умолчанию при делегировании

- **D-06:** **Смена шага — подмена ОДНОГО постоянного якоря карточки `auth_base.html`,
  `innerHTML`** (D-12 Фазы 9). Так идут пути «почта → код → имя и пароль», «почта → код →
  новый пароль» и возврат на начало. Якорь охватывает подзаголовок, плашки, ВСЕ формы экрана
  и нижние ссылки, а не одну форму. На это две причины:
  - блок `auth_subtitle` живёт в шелле вне блока `content`, и при подмене одной формы
    подзаголовок остался бы от прошлого шага;
  - на экране кода ДВЕ формы (подтверждение и повтор) с общим скрытым `token`, а повтор
    выдаёт НОВЫЙ токен (`auth.py:362`, `:759`). Обновиться обе обязаны вместе.

  Ошибка (D-03) перерисовывает тот же якорь. Разметка каждого экрана — один включаемый
  шаблон, общий для страницы и фрагмента (образец — `accounts/includes/max_connect_step.html`),
  поэтому разойтись им негде.
- **D-07:** **`<title>` меняется вместе с шагом.** Фрагмент шага несёт заголовок своего
  экрана. Исследователь сверяет по вендоренному htmx 2.0.10, что заголовок из ответа
  применяется при подмене `innerHTML` (и значение `ignoreTitle` в блоке конфигурации).
  Критерий 4 проверяет это руками.
- **D-08:** **`hx-push-url` не ставится (D-13 Фазы 11); своего адреса у шагов нет и не
  появляется.** В адресной строке остаётся страница, с которой начат путь: `/login`,
  `/register`, `/forgot-password`. Обновление страницы на шаге возвращает к началу пути —
  это принято. Сегодня обновление повторно отправляет POST через вопрос браузера о повторной
  отправке, и защиты это не давало. Токен в адрес не выносится: он попал бы в журналы и
  историю.
- **D-09:** **Девять форм и форма возврата идут через `form_wrapper`** (D-03 Фазы 9;
  `components/form_wrapper.html:187`). `method`/`action` сохраняются, и без JS каждая форма
  работает как сегодня (критерий 2). Индикатор и блокировка кнопки берутся из макроса.
  Изъятия из макроса допускаются только поимённым перечнем с обоснованием.

### Успех через границу шеллов (SIGN-03) — принято по умолчанию при делегировании

- **D-10:** **Успех форм авторизации — `HX-Redirect`, то есть полная загрузка.** Три таких
  выхода:
  - вход → `/dashboard` (ставит cookie);
  - завершение регистрации → `/dashboard` (ставит cookie);
  - новый пароль → `/login?notice=password_reset_done`.

  Последний идёт внутри второго шелла, но уходит тем же заголовком по буквальному тексту
  критерия 3 («`HX-Location` ТОЛЬКО у `/impersonation/stop`»). Код исхода едет параметром
  адреса и рисуется областью уведомления шелла (FOUND-05, `notices.PASSWORD_RESET_DONE`).

  Cookie ставится на ВОЗВРАЩАЕМЫЙ объект ответа (Landmine `10-CONTEXT.md:489-492`,
  `11-CONTEXT.md:448-449`). Пара на каждый выход:
  - без заголовка htmx — 302 и cookie, как сегодня;
  - с заголовком — `HX-Redirect` и cookie, тела-документа нет (`"<!DOCTYPE" not in`).
- **D-11:** **Внутренний выход полной перезагрузки заводится в `app/pages/htmx.py`.**
  `redirect_external` (`htmx.py:345`) для него не годится и НЕ расширяется: его закрытый
  перечень хостов ЮKassa — решение D-09 Фазы 11. Требования к новому выходу:
  - адрес только локальный (`_local_path`);
  - код исхода — параметром `?notice=`;
  - никакой кириллицы в заголовках;
  - путь деградации обязателен (FOUND-04).

  Гейт критерия 3:
  - `HX-Redirect` на внутренний адрес выдают только поимённо перечисленные обработчики
    авторизации;
  - `HX-Location` в `app/pages/auth.py` — только в `stop_impersonation`.
- **D-12:** **Возврат из-под чужой личности.** Три ветки:
  - успех — `HX-Location` на `/admin`, cookie перезаписывается через `set_session_cookie`
    на том же ответе 204 (прецедент — `admin_impersonate`, D-05 Фазы 10);
  - действующего лица нет — `HX-Location` на `/dashboard`;
  - администратора нет или он заблокирован — cookie снимается и уходит **`HX-Redirect`**
    на `/login`.

  **Посылка критерия 3 «не пересекающего границу шеллов» верна для двух веток из трёх.**
  Третья ветка уводит из `base.html` в `auth_base.html`. Летопись ставится у критерия 3 и
  у SIGN-03 в `.planning/REQUIREMENTS.md` по идиоме D-30/D-32, текст критерия не
  переписывается. Форма в `base.html:74` получает `hx-post` через макрос, а без JS остаётся
  настоящей POST-формой (её комментарий в шаблоне это требует). Исследователь проверяет:
  `HX-Location` подменяет `body` целиком, полоса имперсонации (она вне `[data-shell]`)
  уходит вместе с ним, а `<title>` становится заголовком `/admin`.

### Прочее — принято по умолчанию при делегировании

- **D-13:** **Отказы зависимостей не трогаются.** `forbid_when_impersonating` на четырёх
  шагах восстановления уже отвечает двумя транспортами (`HtmxRefusal`). Пары фазы покрывают
  его на htmx-пути.
- **D-14:** **Старое снимается сразу** (D-09 Фазы 8): возвраты `TemplateResponse` с
  ошибкой и `RedirectResponse` в десяти обработчиках.
  - Счётчик отставания вехи доходит до нуля: `NOT_YET_CONVERTED` пуст,
    `NOT_YET_CONVERTED_COUNT = 0` — именованный ноль с летописью, перечень не удаляется.
  - Остальные инвентарные числа двигаются явно и ставятся прогоном (D-13 Фазы 8). Среди них
    `test_hx_location_destinations.py`, `test_htmx_post_pairs.py`,
    `test_impersonation_gate.py`, `test_identifier_bounds.py`, перечни разметочных гейтов.
- **D-15:** **Фаза меняет транспорт, а не решения.** Неизменны:
  - тексты, порядок проверок, минута между кодами, пять попыток, срок кода;
  - источник кода `secrets` (CR-02);
  - проверка `is_blocked`;
  - запрет восстановления под чужой личностью (D-22);
  - набор атрибутов cookie.

  Исключения три, и все они — предмет фазы: статус 200 → 422 на ошибках (D-03), эхо
  (D-04), заголовки перехода на htmx-пути (D-10, D-12). Эхо и 422 по построению улучшают и
  путь без JS: это свойство сервера.

### Claude's Discretion

Владелец решил лично D-01 и D-02. Остальное (D-03…D-15) принято по умолчанию при
делегировании: эти решения стоят на решениях прошлых фаз и на рекомендациях разведки.

Свободно и не решено ни здесь, ни ROADMAP-ом:
- имя и место якоря шага в `auth_base.html`;
- раскладка включаемых шаблонов (`auth/includes/…` или иначе);
- имя и форма внутреннего выхода `HX-Redirect`: отдельная функция или параметр `respond()`;
- `autofocus` на поле с ошибкой после подмены;
- `hx-sync` между формами подтверждения и повтора кода на одном экране;
- куда ставится индикатор;
- разбиение на планы и волны.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Предмет фазы — код, который переписывается
- `app/pages/auth.py` — десять обработчиков (строки 115-427, 441-524, 550-814). Там же
  `set_session_cookie` / `clear_session_cookie` / `_session_cookie_attrs` (:68-107, не
  трогаются) и `BLOCKED_LOGIN_ERROR` (:62).
- `app/templates/auth_base.html` — второй шелл: `auth_subtitle` вне `content`, область
  уведомления (`includes/notice_area.html`) и плашка ошибки уже включены.
- `app/templates/auth/*.html` — семь экранов: `login`, `register`, `register_verify`,
  `register_complete`, `forgot_password`, `forgot_password_verify`, `forgot_password_reset`.
- `app/templates/base.html:74` — форма возврата из-под чужой личности.
- Тесты маршрутов: `tests/test_pages/test_registration.py`, `test_password_reset.py`,
  `test_blocked_user.py`, `test_impersonation.py`, `test_cookie_flags.py`,
  `test_reset_code_source.py`, `test_trial.py` (все в `tests/test_pages/`).

### Слой ответа и гейты
- `app/pages/htmx.py` — `is_htmx` (:166), `HtmxRefusal` (:188), `_local_path` (:207),
  `location_response` (:251), `redirect_external` (:345, НЕ расширяется — D-11),
  `respond` (:712), `respond_field_error` (:847).
- `app/templates/components/form_wrapper.html:187`, `components/field.html:7` (параметр
  `error`).
- `app/templates/includes/htmx_config.html` — `responseHandling`, правило `422`,
  `selfRequestsOnly`.
- `app/templates/includes/htmx_error_banner.html:306` — 422 без плашки.
- `app/pages/notices.py:137` — `PASSWORD_RESET_DONE`.
- `tests/test_pages/test_htmx_gates.py` — `NOT_YET_CONVERTED` (:265-281),
  `NOT_YET_CONVERTED_COUNT` (:638), `OWN_RESPONSE_EXITS_DECLARED` (:4847).
- `tests/test_pages/test_htmx_post_pairs.py`, `test_hx_location_destinations.py`,
  `test_impersonation_gate.py`, `test_identifier_bounds.py`, `test_htmx_response_contract.py`.
- `.planning/WINDOWS.md:80` — окно 63 (D-01).

### Разведка вехи (читать, НЕ править)
- `.planning/research/ARCHITECTURE.md:725-737` — почему `HX-Location` через границу шеллов
  теряет `<title>` и `<meta name="htmx-config">`; `:849` — `is_same_origin` на htmx-пути.
- `.planning/research/SUMMARY.md:91, 153, 205-207` — класс C «смена личности», порядок фаз.
- `.planning/research/FEATURES.md:351-367` — форма 422 и эхо без паролей.
- `.planning/research/PITFALLS.md:128-158` — открытый редирект и инъекция в заголовки
  (`?next=` сегодня нет и не заводится); `:271-307` — CSRF: `SameSite=Lax`, `HX-Request`
  не проверка безопасности.
- ⚠️ **Устаревшие посылки разведки, которые правятся летописями, а не переписыванием:**
  - «`#notice` в `auth_base.html` не приходит» (`SUMMARY.md:145`, `ARCHITECTURE.md:298-302`,
    ROADMAP §Phase 14 «Research») — ложно с Фазы 8: `auth_base.html` включает
    `notice_area.html` (`08-CONTEXT.md:470-471`);
  - «успех авторизации — `HX-Location`» (`ARCHITECTURE.md:216`, `FEATURES.md:494, 585, 645`,
    `PITFALLS.md:137`, `.planning/PROJECT.md:79, 253`) — обязывает ROADMAP с `HX-Redirect`.

### Решения прошлых фаз, действующие здесь
- `.planning/phases/07-obnovlenie-htmx-do-2-0-10-i-blok-konfiguratsii/07-CONTEXT.md` — **D-11** (`auth_base.html` чистит
  `htmx-history-cache`).
- `.planning/phases/08-fundament-otveta-kanal-uvedomleniy-paket-geytov-i-denezhnyy/08-CONTEXT.md` —
  **D-09** (старое снимается сразу), **D-12** (исход — в шелле, ошибка — у формы),
  **D-13** (инвентарь числом), **D-14** (два перечня), **D-15** (выходы `htmx.py`),
  **D-16** (пары `htmx_client`), FOUND-04, FOUND-05.
- `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-CONTEXT.md` —
  **D-03** (`hx-post` только из макроса), **D-06**, **D-12** (постоянный якорь + `innerHTML`),
  **D-14/D-15** (индикатор).
- `.planning/phases/10-rychag-components-modal-html/10-CONTEXT.md` — **D-01** (форма ответа
  по классу действия), **D-05** (`admin_impersonate`: cookie на ответе 204 с `HX-Location`),
  Landmine :489-492.
- `.planning/phases/11-massovyy-perevod-razdelov-pisma/11-CONTEXT.md` — **D-06** (422 + эхо),
  **D-08** (голый 403 — основание D-01), **D-09** (`redirect_external`), **D-13** (без
  `hx-push-url`), **D-14**, **D-16** (`HX-Retarget`/`HX-Reswap` только перечнем),
  Landmine :448-449.
- `.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-CONTEXT.md` —
  **D-08** (пароль не возвращается), линия «перенести, а не переизобрести».

### Рамки и требования
- `.planning/ROADMAP.md` §Phase 14 (:689-702) — четыре критерия; у критерия 3 появляется
  летопись (D-12).
- `.planning/REQUIREMENTS.md:57-59` — SIGN-01, SIGN-02, SIGN-03.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`respond_field_error()`** — ошибка заполнения с одним кодом на оба транспорта; тело
  собирает вызывающий.
- **`location_response()`** — 204 + `HX-Location` для трёх веток `stop_impersonation`,
  кроме ветки на `/login`.
- **`form_wrapper`, `field(error=…)`, `alert`, `button`** — макросы уже импортируются
  экранами авторизации.
- **Шаг включаемым шаблоном** (`accounts/includes/max_connect_step.html`) — одна разметка на
  страницу и фрагмент.
- **`set_session_cookie` / `clear_session_cookie`** — единственные точки cookie, возврат
  обязан ходить через них.

### Established Patterns
- Экраны `register.html` и `forgot_password.html` УЖЕ возвращают email (`value=email`).
  Потери сосредоточены на входе, на коде и на имени.
- Все ошибки сегодня — 200 через `TemplateResponse`, ни одного 422 в `auth.py`.
- Успехи с cookie — `RedirectResponse(302)` с `set_session_cookie` на том же объекте.

### Integration Points
- Якорь шага в `auth_base.html` — единственная правка шелла.
- Внутренний выход `HX-Redirect` в `htmx.py` — новый потребитель слоя ответа.
- `base.html:74` — форма возврата на всех страницах основного шелла.

### Landmines
- **Cookie не на том объекте** — тест, проверяющий только заголовок, зеленеет, а вход
  молча ломается. Пары обязаны утверждать cookie на htmx-ответе.
- **Повтор кода выдаёт новый токен.** Подмена только формы повтора оставила бы форму
  подтверждения со старым токеном (D-06).
- **Подзаголовок вне блока `content`** — подмена одной формы оставляет подзаголовок
  прошлого шага (D-06).
- **Менеджер паролей.** При входе и регистрации через `hx-post` и `HX-Redirect` браузер
  может не предложить сохранить пароль, потому что сохранение распознаётся по отправке формы
  и навигации. Исследователь проверяет поведение Chrome и Firefox, обход наблюдает его
  руками. Если сохранение ломается, это выносится владельцу, а не глушится молча.
- **Кириллица в `HX-*`** даёт 500 (`08-CONTEXT.md:486-488`). Код исхода едет латиницей.
- **Ночное окно админ-обзора** (`test_the_overview_error_number_matches_the_users_own_dashboard`)
  краснит полный прогон в 00:00–05:00 UTC. Это известный шум, а не отказ фазы.

</code_context>

<specifics>
## Specific Ideas

- Владелец выбрал рекомендованный вариант в обеих развилках и остальное отдал на умолчания.
- Линия фазы: **перенести, а не переизобрести**. Выигрыш — в ошибке, а не в успехе. Успех
  сознательно остаётся полной загрузкой, и это ничего не стоит.
- Ручной обход идёт с настоящими письмами (D-02). В нём, помимо четырёх путей критерия 4,
  наблюдается возврат из-под чужой личности и предложение сохранить пароль.

</specifics>

<deferred>
## Deferred Ideas

- **Проверка источника запроса на формах входа и регистрации.** Сегодня у девяти форм
  `is_same_origin` нет, защита — только `SameSite=Lax`. Подделка входа (вход жертвы в
  учётку атакующего) — отдельная работа по безопасности, не транспорт.
- **Ограничение перебора пароля на входе** — лимита попыток у `login_submit` нет. Новая
  возможность.
- **Перечисление адресов** — тексты «Пользователь с таким email не найден» и «Этот email уже
  зарегистрирован» раскрывают существование учётки. Тексты переезжают дословно (D-15).
- **`hx-push-url` и собственные адреса шагов** — Фаза 15 (критерий 3).

### Reviewed Todos (not folded)
- **`session-cookie-without-secure-flag.md`** (совпадение 0.4) — в объём не взят. Признак
  `secure` читается из настройки `cookie_secure` (CR-03, `app/config.py:88`), а фаза набор
  атрибутов не трогает (D-15). Остаток долга — умолчание `False` и HSTS в nginx.
- **`blocked-user-can-still-log-in.md`** — Фаза 12 адресовала его сюда (`12-CONTEXT.md:458-459`).
  Страничный вход уже отказывает заблокированному (`auth.py:133-138`, D-30). Фаза переносит
  отказ как есть и держит его парой на обоих транспортах (D-05). JSON-путь и путь рассылки —
  вне границы. Закрытие todo — отдельная уборка после сверки.
- **`password-reset-codes-are-predictable.md`** — по коду закрыт: `secrets.randbelow`, CR-02,
  свидетель `tests/test_pages/test_reset_code_source.py`. Фаза источник не трогает.
- **`full-suite-ads-editor-order-pollution.md`** (0.2) — не относится к фазе, совпадение по
  слову «post».

</deferred>

---

*Phase: 14-avtorizatsiya-na-htmx*
*Context gathered: 2026-09-22*
