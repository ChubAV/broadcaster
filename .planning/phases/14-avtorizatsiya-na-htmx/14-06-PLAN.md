---
phase: 14-avtorizatsiya-na-htmx
plan: 06
type: tdd
wave: 6
depends_on: ["14-05"]
files_modified:
  - app/pages/auth.py
  - app/templates/base.html
  - tests/test_pages/test_impersonation.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_pages/test_hx_location_destinations.py
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [SIGN-01, SIGN-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: sample_count 0, factor 1; confidence выведен, а не самооценён.
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Возврат из-под чужой личности с признаком htmx отвечает 204 и `HX-Location: /admin` без `HX-Redirect` и без тела; cookie сессии перезаписана НА ЭТОМ ЖЕ ответе токеном без признака действующего лица; следующий `GET /admin` отдаёт админку, а полосы имперсонации на страницах больше нет. Без признака — 302 на `/admin` и та же cookie, как сегодня (критерий 3, D-12)"
    - "Возврат без действующего лица отвечает `HX-Location: /dashboard` (без htmx — 302 на `/dashboard`) и НЕ выдаёт ни одного токена (D-12)"
    - "Возврат, когда учётной записи действующего лица нет или она заблокирована, отвечает 204 и `HX-Redirect: /login` (без htmx — 302 на `/login`), снимая cookie тем же набором атрибутов; админка после этого не открывается — это единственная ветка возврата, уходящая через границу шеллов (D-12, WR-02 фазы 6)"
    - "Возврат с чужого источника отвечает голым 403 без тела, без cookie и без заголовков `HX-*` на ОБОИХ транспортах — именованное изъятие, запись `OWN_RESPONSE_EXITS` в состоянии `DECISION_OWNER_D01_F14` (D-01)"
    - "Форма возврата в полосе `base.html` рождена макросом `form_wrapper` без цели (`hx-swap=\"none\"`), остаётся настоящей формой `method=\"post\" action=\"/impersonation/stop\"` и прижата вправо обёрткой `.impersonation-back` (D-09, D-12)"
    - "Гейт критерия 3: вызывающие выхода полной перезагрузки в страничном слое РАВНЫ {`login_submit`, `register_complete`, `forgot_password_reset`, `stop_impersonation`}; адрес каждого такого вызова — литерал локального пути; функции `app/pages/auth.py`, способные выдать `HX-Location`, РАВНЫ {`stop_impersonation`}; у каждого правила есть отрицательный контроль на синтетике (D-11)"
    - "Счётчик отставания вехи — ИМЕНОВАННЫЙ НОЛЬ: `NOT_YET_CONVERTED` пуст, но не удалён, `NOT_YET_CONVERTED_COUNT = 0` с летописью; `OWN_RESPONSE_EXITS_DECLARED` 13 → 14, `HX_LOCATION_DESTINATION_CALLS_DECLARED` 77 → 79 с `/admin` в карте назначений — поставлены прогоном (D-14)"
    - statement: "В браузере «ВЕРНУТЬСЯ В АДМИНА» приводит в админку: адресная строка `/admin`, вкладка — заголовок админки, полосы нет, cookie без признака действующего лица — ручной UAT фазы (критерий 4, D-12)"
      verification: backstop
  artifacts:
    - "app/pages/auth.py — `stop_impersonation` на выходах слоя (`respond`, `redirect_internal`) и своём 403"
    - "app/templates/base.html — форма возврата через `form_wrapper`"
    - "tests/test_pages/test_htmx_gates.py — именованный ноль отставания, запись `OWN_RESPONSE_EXITS` возврата, гейт критерия 3 с контролями"
  key_links:
    - "форма возврата (`hx-swap=none`) → `stop_impersonation` → `respond(redirect=\"/admin\")` → `set_session_cookie` на ВОЗВРАЩЁННОМ объекте → `HX-Location` → GET `/admin` с новой cookie: ломается — администратор остаётся под чужой личностью при правильном заголовке"
    - "ветка закрытого действующего лица → `redirect_internal(redirect=\"/login\")` → `clear_session_cookie`: ломается — заблокированный выходит со свежим суточным токеном"
    - "`_full_load_callers` / `_hx_location_emitters` ↔ `FULL_LOAD_HANDLERS` / `AUTH_HX_LOCATION_HANDLERS`: ломается — критерий 3 перестаёт быть машинным"
  prohibitions:
    - statement: "возврат не выпускает токен без признака действующего лица в предъявленном токене и не выпускает его закрытой учётной записи (D-12, WR-02)"
      status: flagged-unverified
      verification: none
    - statement: "форма отказа по источнику у возврата — голый 403, решение владельца D-01; отказ через слой ответа с новым кодом уведомления не вводится"
      status: flagged-unverified
      verification: none
    - statement: "инвентарное число гейта ставится ПРОГОНОМ покрасневшего правила, а не вычитанием в уме (D-13 Фазы 8)"
      status: flagged-unverified
      verification: none
---

<objective>
Последний обработчик вехи — возврат из-под чужой личности — переходит на слой ответа: две ветки
уходят `HX-Location` внутри основного шелла, третья (действующего лица нет или оно закрыто) —
`HX-Redirect` на `/login` через границу шеллов; отказ по источнику остаётся голым 403 решением
владельца D-01. Форма возврата в полосе получает `hx-post` через макрос. Счётчик отставания
вехи доходит до именованного нуля (D-14), и машинно утверждается критерий 3 (D-11).

Purpose: SIGN-03 — «`HX-Location` применяется только для `/impersonation/stop`» — становится
правилом, а не наблюдением; оговорки к буквальному тексту критерия (третья ветка и отказ
зависимости на шагах восстановления) записывает план 14-07 летописями.

Output: переведённый `stop_impersonation`, форма возврата на макросе, htmx-половины пар возврата,
запись собственного выхода, гейт критерия 3, именованный ноль, `/admin` в карте назначений.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-avtorizatsiya-na-htmx/14-CONTEXT.md
@.planning/phases/14-avtorizatsiya-na-htmx/14-01-SUMMARY.md
@.planning/phases/14-avtorizatsiya-na-htmx/14-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**`app/pages/htmx.py`:** `redirect_internal` (14-01), `respond_screen`, `_respond_by_transport`
(14-02). **`app/pages/auth.py`:** `AUTH_STEP_RESPONSE_TEMPLATE`, `AuthScreen`, `AUTH_SCREENS`,
`_screen_markup`, `_screen_builders`. **Шаблоны:** `auth/includes/step_response.html` и семь
экранов `auth/includes/*_step.html`; якорь `auth-step`. **Тесты:**
`tests/test_pages/test_auth_transport.py`; `RESPONSE_LAYER_EXITS`; в этом плане —
`FULL_LOAD_CALL = "redirect_internal"`, `FULL_LOAD_HANDLERS`, `AUTH_MODULE = "app/pages/auth.py"`,
`AUTH_HX_LOCATION_HANDLERS`, `DECISION_OWNER_D01_F14`, `LIFTING_CONDITION_OWN_RESPONSE_RETURN`,
помощники `_full_load_callers(sources)`, `_hx_location_emitters(sources, module=AUTH_MODULE)`;
ключ пар `AUTH_STOP_IMPERSONATION`. **Записи (14-07):** летописи ROADMAP, REQUIREMENTS, PROJECT,
окно 63, `14-UAT.md`.
</artifacts_this_phase_produces>

<tdd_notes>
- Коммиты: RED — `test(14-06): …`, GREEN — `feat(14-06): …`, перечни — `test(14-06): record …`.
- RED-улика — как в 14-01 (`--junit-xml` → TAP в scratchpad). Целевое правило —
  `test_the_return_over_htmx_keeps_both_the_location_and_the_admin_cookie`; RED засчитывается тремя
  совпадениями в выводе
  `uv run pytest tests/test_pages/test_impersonation.py -q -p no:randomly -k return_over_htmx`:
  (1) `^FAILED tests/test_pages/test_impersonation.py::test_the_return_over_htmx_keeps_both_the_location_and_the_admin_cookie`;
  (2) итоговая строка вида `1 failed`; (3) причинный литерал `assert 200 == 204` — на дереве до
  правки возврат отвечает перенаправлением 302 на `/admin`, и клиент, следующий
  перенаправлениям, приходит к странице админки со статусом 200 (`app/pages/auth.py:514-524`,
  проверено чтением).
</tdd_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Возврат из-под чужой личности — три ветки и отказ по источнику на обоих транспортах, форма возврата из макроса</name>

  <files>
app/pages/auth.py,
app/templates/base.html,
tests/test_pages/test_impersonation.py
  </files>

  <read_first>
- `app/pages/auth.py:441-524` (дерево до фазы) — `stop_impersonation` целиком с докстрингом.
- `app/pages/admin.py:1860-1895` — `admin_impersonate`: 204 с `HX-Location` и cookie на том же
  ответе (D-05 Фазы 10, прецедент).
- `app/templates/base.html:1-20` (импорты макросов), `:40-80` (полоса имперсонации и её
  комментарий «ВОЗВРАТ — НАСТОЯЩАЯ ФОРМА POST»); `app/static/css/app.css:515-532`
  (`[data-impersonation] .impersonation-back`).
- `tests/test_pages/test_impersonation.py:30-170` (`_register`, `_user`, `_seed_target`, `_enter`,
  `_stop`, `_session_cookies`, `_cookie_attrs`, `_act_of`), `:200-298` (тройная пара входа —
  образец), `:398-470`, `:745-900` (`RETURN_FORM`, правила полосы), `:1615-1664` (заблокированное
  действующее лицо).
- `app/pages/common.py` — `is_same_origin` (ветка без заголовков источника пропускает запрос).
- `14-RESEARCH.md` §Находка 5, §Карта выходов (строки `stop_impersonation`), §Pitfall 4, §Pitfall 8.
  </read_first>

  <behavior>
RED (`tests/test_pages/test_impersonation.py`):
1. `test_the_return_over_htmx_keeps_both_the_location_and_the_admin_cookie` — админ входит под
   пользователем без признака, затем `POST /impersonation/stop` с признаком htmx и следованием
   перенаправлениям: 204, `HX-Location == "/admin"`, `HX-Redirect` нет, тело пустое, в
   `set-cookie` есть `access_token`, `_act_of(response) is None`; `GET /admin` → 200; на `GET
   /dashboard` признака `data-impersonation` нет.
2. `test_the_return_without_an_actor_goes_to_the_dashboard_on_both_transports` — обычный
   пользователь: без признака 302 на `/dashboard` (адрес `"/impersonation/stop"` литералом в
   `.post(…)`), с признаком 204 и `HX-Location == "/dashboard"`; `access_token` в `set-cookie` нет
   ни на одном.
3. `test_a_closed_actor_is_logged_out_by_the_return_on_both_transports` — действующее лицо
   заблокировано во время имперсонации (и отдельным шагом — удалено): без признака 302 на `/login`,
   с признаком 204, `HX-Redirect == "/login"`, `HX-Location` нет; на обоих — снятие `access_token`
   (`max-age=0` либо истёкший `expires`) с тем же `path`, `httponly`, `samesite` и `secure`, что у
   установки; следующий `GET /admin` — не 200.
4. `test_a_cross_origin_return_is_refused_with_a_bare_403_on_both_transports` — заголовок
   `Origin: https://evil.example` под чужой личностью: 403, пустое тело, `access_token` в
   `set-cookie` нет, ни одного заголовка с приставкой `HX-` — на обоих транспортах (D-01).
5. `test_the_return_form_rides_the_macro_and_swaps_nothing` — на `GET /dashboard` под чужой
   личностью: `RETURN_FORM` найден, тег несёт `hx-post="/impersonation/stop"` и `hx-swap="none"`,
   форма стоит внутри `<div class="impersonation-back">`.
GREEN — обработчик и полоса.
  </behavior>

  <action>
**1. `app/pages/auth.py` — `stop_impersonation` (D-01, D-12, D-14).** Сверка источника остаётся
ПЕРВОЙ и отвечает прежним голым 403 (D-01); комментарий над ней дополняется: «именованное изъятие —
D-01 Фазы 14, продление D-08 Фазы 11; запись `OWN_RESPONSE_EXITS`». Действующего лица нет →
`return await respond(request, redirect="/dashboard")`. Учётной записи действующего лица нет или
она заблокирована → `response = await redirect_internal(request, redirect="/login")`,
`clear_session_cookie(response, settings)`, `return response` (оба прежних комментария ветки —
WR-02 и «админство не переспрашивается» — сохраняются). Успех → `response = await respond(request,
redirect="/admin")`, `set_session_cookie(response, create_access_token(admin.id,
settings.secret_key), settings)` на этом же объекте, запись журнала `impersonation_stop` прежняя,
`return response`. Докстринг: прежние абзацы не стираются (D-30/D-32); дописывается абзац о трёх
ветках на пути htmx — две уходят заголовком частичного перехода внутри основного шелла
(рантайм подменяет содержимое `body`, полоса уходит вместе с ним, вкладка получает заголовок
`/admin` — RESEARCH Находка 5), третья уводит в шелл авторизации полной загрузкой, потому что
фрагментом смену шелла не отдать; и о несущем порядке «ответ слоя → cookie на тот же объект»
(прецедент `admin_impersonate`). Импорт `respond` из `app.pages.htmx`.

**2. `app/templates/base.html` (D-09, D-12, Pitfall 8).** Импорт `form_wrapper` рядом с
соседними импортами макросов. Форма возврата становится
`<div class="impersonation-back">` с блочным вызовом `form_wrapper(action='/impersonation/stop')`
внутри (без цели — макрос печатает `hx-swap="none"`: ответ всегда переход), кнопка прежняя
(`<button type="submit" class="btn btn--ghost">` с подписью через `mono`). Класс прижатия вправо
переезжает с тега формы на обёртку, поэтому правило `app.css` не меняется. Комментарий
«ВОЗВРАТ — НАСТОЯЩАЯ ФОРМА POST» не стирается и дополняется летописью «Фаза 14, план 14-06»:
метод и адрес печатает макрос, без JS возврат работает как прежде, с JS ответ — заголовок
перехода.

**3. `tests/test_pages/test_impersonation.py`** — правила `<behavior>`; помощники модуля
переиспользуются, а не копируются.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_impersonation.py tests/test_pages/test_origin_guard_on_destructive_routes.py tests/test_pages/test_cookie_flags.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_impersonation.py -q -p no:randomly -k "return_over_htmx or without_an_actor or closed_actor or cross_origin_return or rides_the_macro"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def stop_impersonation/,/^# ---- Forgot Password/' app/pages/auth.py | grep -c 'RedirectResponse'` возвращает `0`; `awk '/^async def stop_impersonation/,/^# ---- Forgot Password/' app/pages/auth.py | grep -c 'Response(status_code=403)'` возвращает `1`.
- `awk '/^async def stop_impersonation/,/^# ---- Forgot Password/' app/pages/auth.py | grep -c 'await respond(request, redirect="/admin")'` возвращает `1`; `awk '/^async def stop_impersonation/,/^# ---- Forgot Password/' app/pages/auth.py | grep -c 'await redirect_internal(request, redirect="/login")'` возвращает `1`; `awk '/^async def stop_impersonation/,/^# ---- Forgot Password/' app/pages/auth.py | grep -c 'await respond(request, redirect="/dashboard")'` возвращает `1`.
- `grep -c "form_wrapper(action='/impersonation/stop')" app/templates/base.html` возвращает `1`; `grep -c '<div class="impersonation-back">' app/templates/base.html` возвращает `1`.
- `uv run pytest tests/test_pages/test_impersonation.py -q -p no:randomly` — зелено.
  </acceptance_criteria>

  <done>
Администратор возвращается в админку без перезагрузки документа с новой cookie; без действующего
лица — в кабинет без токена; закрытое действующее лицо выходит на экран входа полной загрузкой;
чужой источник получает голый 403.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Гейт критерия 3, запись собственного выхода возврата и именованный ноль отставания</name>

  <files>
tests/test_pages/test_htmx_gates.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py` — `NOT_YET_CONVERTED` и летопись счётчика (редакции
  14-01…14-05), `NEVER_RESPONDS` (форма «пустой объявленный перечень»), `:2440-2470`
  (`_sources_with`), `:2984-3055` (`DECISION_WAITS_FOR_THE_OWNER`, `DECISION_OWNER_D08`,
  `DECISION_OWNER_D15` — абзац «`stop_impersonation` — Фаза 14 — в это состояние НЕ ставится»),
  `:3619` (`_DECISION_REFERENCE`), `:4360-4480` (`_OwnResponseExit`, условия снятия, цена,
  `_OWN_RESPONSE_ORIGIN_GUARD`), `:4476-4850` (записи и летопись `OWN_RESPONSE_EXITS_DECLARED`),
  `:4850-4990` (правила полноты, обоснований, авторства).
- `app/dependencies.py:320-402`, `app/main.py:220-231` — второй отправитель `HX-Location` (вне
  модуля авторизации).
- `14-RESEARCH.md` §Находка 6 (строка о критерии 3), §Инвентарь гейтов (строка «новый гейт
  критерия 3»).
  </read_first>

  <behavior>
RED (контроли на синтетике `_sources_with`, модуль `AUTH_MODULE`):
1. `test_control_a_fifth_full_load_caller_reddens_the_criterion_three_gate` — синтетический
   обработчик модуля авторизации, зовущий `redirect_internal`: равенство вызывающих ломается с
   именем обработчика в отказе; до подмены — зелено.
2. `test_control_a_bare_respond_in_the_login_reddens_the_hx_location_gate` — в `login_submit`
   дописан `respond(request, redirect="/dashboard")` без фрагмента: множество функций, способных
   выдать `HX-Location`, становится шире `AUTH_HX_LOCATION_HANDLERS`.
3. `test_control_a_computed_full_load_address_reddens_the_literal_rule` — вызов
   `redirect_internal(request, redirect=target)` с переменной: правило литерала краснеет.
Правила: `test_only_the_named_auth_handlers_leave_by_a_full_load`,
`test_every_full_load_address_is_a_literal_local_path`,
`test_hx_location_in_the_auth_module_belongs_to_the_return_only`.
  </behavior>

  <action>
Числа — ПРОГОНОМ покрасневшего правила, летопись «Фаза 14, план 14-06» с дословным выводом.

**1. Именованный ноль (D-14).** `NOT_YET_CONVERTED` — пустой `frozenset()`: перечень НЕ удаляется
(форма `NEVER_RESPONDS`: «вопрос задан, и сегодня в нём никого нет»), внутри — комментарий о
снятии последнего ключа ПЕРЕВОДОМ. `NOT_YET_CONVERTED_COUNT` 1 → 0 — прогоном; летопись
«1 → 0, Фаза 14, план 14-06» и СВОДНАЯ ЛЕТОПИСЬ ФАЗЫ 14 «10 → 0» словами, а не ключами: вход
(14-01), начало регистрации и повтор кода (14-02), подтверждение кода и завершение регистрации
(14-03), начало восстановления и повтор кода (14-04), подтверждение кода и новый пароль (14-05),
возврат из-под чужой личности (14-06). «⚠️ ЧИСЛО УБЫВАЮЩЕЕ» сохраняется; добавляется: ноль
держится правилом «счётчик не растёт», и любой новый POST-обработчик мимо слоя краснит
классификацию.

**2. Собственный выход возврата (D-01).** Константа `DECISION_OWNER_D01_F14 = "РЕШЕНО
ВЛАДЕЛЬЦЕМ: решение владельца 2026-09-22, `14-CONTEXT.md` D-01 — голый 403 возврата из-под чужой
личности на провале сверки источника остаётся именованным изъятием; продление D-08 Фазы 11"` с
абзацем-основанием по форме `DECISION_OWNER_D15`; абзац у `DECISION_OWNER_D08` про «Фазу 14»
дополняется летописью (запись возврата стоит в СВОЁМ состоянии). Константа
`LIFTING_CONDITION_OWN_RESPONSE_RETURN = LIFTING_CONDITION_OWN_RESPONSE + ". ⚠️ …"`: Фаза 11 этот
выход не переводила — он оставлен Фазе 14 (`11-CONTEXT.md:159`); работа перевода исполнена планом
14-06; ФОРМУ отказа выбрал владелец (D-01 Фазы 14) — строка сохраняет «Фаза 11» и «ВЛАДЕЛЬЦУ» по
правилу обоснований. Запись `_OwnResponseExit(entry="app/pages/auth.py::stop_impersonation",
kind="Response(status_code=403)", reason=_OWN_RESPONSE_PRICE + ". " + _OWN_RESPONSE_ORIGIN_GUARD +
". ⚠️ СВОЁ У ЭТОГО ВХОДА: …", lifting_condition=LIFTING_CONDITION_OWN_RESPONSE_RETURN,
decision_state=DECISION_OWNER_D01_F14)`; «своё» называет: возврат прав не спрашивает (докстринг
обработчика), поэтому сверка стоит первой; формы-панели нет — полоса на каждой странице основного
шелла, ответ без цели; поддельная форма стороннего сайта получает тот же 403, что и до фазы; на
пути htmx немой 403 поднимает общую плашку «Действие не выполнено» (цена D-01).
`OWN_RESPONSE_EXITS_DECLARED` 13 → 14 — прогоном.

**3. Гейт критерия 3 (D-11).** Группа с шапкой: текст критерия 3 и SIGN-03; предмет правила —
модуль авторизации и выход полной перезагрузки страничного слоя; ГРАНИЦА названа прямо — отказ
зависимости `forbid_when_impersonating` на четырёх шагах восстановления уходит `HX-Location` из
`app/main.py` (обработчик `HtmxRefusal`) и этим правилом НЕ видится по построению, это второй
отправитель, записанный летописью критерия 3 (план 14-07, RESEARCH Находка 6). Константы
`FULL_LOAD_CALL = "redirect_internal"`, `AUTH_MODULE = "app/pages/auth.py"`,
`FULL_LOAD_HANDLERS = frozenset({…login_submit, …register_complete, …forgot_password_reset,
…stop_impersonation})`, `AUTH_HX_LOCATION_HANDLERS = frozenset({"app/pages/auth.py::stop_impersonation"})`.
`_full_load_callers(sources)` — ключи POST-обработчиков страничного слоя, зовущих
`FULL_LOAD_CALL` (голое имя и через модуль). `_hx_location_emitters(sources, module=AUTH_MODULE)` —
функции модуля с вызовом `respond(...)` без именованного аргумента фрагмента либо с вызовом
`location_response(...)`. Три правила из `<behavior>` (равенство с непустотой; каждый вызов
`FULL_LOAD_CALL` несёт `redirect=` строковым литералом, начинающимся с одного `/`; равенство для
`HX-Location`) и три контроля — двухшаговые (до подмены зелено, после — красно с именем).
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly -k "full_load or hx_location_in_the_auth or bare_respond_in_the_login or own_response"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `uv run python -c "import sys; from tests.test_pages.test_htmx_gates import NOT_YET_CONVERTED as n, NOT_YET_CONVERTED_COUNT as c; sys.exit(0 if n == frozenset() and c == 0 else 1)"` — код 0; `grep -c '^NOT_YET_CONVERTED: frozenset' tests/test_pages/test_htmx_gates.py` возвращает `1` (перечень объявлен, а не удалён).
- `grep -c 'entry="app/pages/auth.py::stop_impersonation"' tests/test_pages/test_htmx_gates.py` возвращает `1`; `grep -c '^DECISION_OWNER_D01_F14 = ' tests/test_pages/test_htmx_gates.py` возвращает `1`.
- `grep -c '^FULL_LOAD_HANDLERS' tests/test_pages/test_htmx_gates.py` возвращает `1`; `grep -c '^AUTH_HX_LOCATION_HANDLERS' tests/test_pages/test_htmx_gates.py` возвращает `1`.
- `uv run pytest tests/test_pages/test_htmx_gates.py -q -p no:randomly -k "control_a_fifth_full_load or control_a_bare_respond or control_a_computed_full_load"` — три контроля исполнены и зелены.
  </acceptance_criteria>

  <done>
Критерий 3 стал машинным правилом с отрицательными контролями; возврат — объявленный собственный
выход решением владельца; отставание вехи — именованный ноль.
  </done>
</task>

<task type="auto">
  <name>Задача 3: Пара возврата, `/admin` в карте назначений перехода, форма полосы среди вызывающих макроса</name>

  <files>
tests/test_pages/test_htmx_post_pairs.py,
tests/test_pages/test_hx_location_destinations.py,
tests/test_templates/test_htmx_markup_gates.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_post_pairs.py` — случаи `LOCATION` (образец), `POST_PAIR_CASES_DECLARED`,
  `PAIRED_302_ASSERTIONS_DECLARED` и летописи после 14-05.
- `tests/test_pages/test_hx_location_destinations.py:400-520` (летопись
  `HX_LOCATION_DESTINATION_CALLS_DECLARED`, карта `HX_LOCATION_DESTINATION_TEMPLATES`), `:939-1010`
  (правила карты и двойного исполнения).
- `app/pages/admin.py:499-552`, `app/templates/admin/overview.html:1-10` (страница `/admin`
  наследует `base.html`, инлайн-скриптов нет — RESEARCH Находка 5).
- `tests/test_templates/test_htmx_markup_gates.py` — кортеж вызывающих `form_wrapper` после 14-05,
  `UNREACHABLE_TARGET_CALL_BLOCKS_MEASURED`.
  </read_first>

  <action>
Числа — ПРОГОНОМ покрасневшего правила, летопись «Фаза 14, план 14-06» с дословным выводом.

**1. `tests/test_pages/test_htmx_post_pairs.py`.** Ключ `AUTH_STOP_IMPERSONATION`; случай «возврат
без действующего лица» (личность `user`, ветка `LOCATION`, `landing="/dashboard"`, посев без
подмен). Комментарий: успех и закрытое действующее лицо требуют cookie имперсонации и утверждены
тройными парами в `tests/test_pages/test_impersonation.py`; отказ по источнику — голый 403,
граница обхода. `POST_PAIR_CASES_DECLARED` 76 → 77 и `PAIRED_302_ASSERTIONS_DECLARED` — прогоном
(ожидание: +2 прежних утверждения 302 о возврате — замер планирования — плюс новые).

**2. `tests/test_pages/test_hx_location_destinations.py`.** В `HX_LOCATION_DESTINATION_TEMPLATES` —
`"/admin": "admin/overview.html"` с комментарием «Фаза 14, план 14-06: успех возврата из-под
чужой личности (D-12)»; `HX_LOCATION_DESTINATION_CALLS_DECLARED` 77 → 79 — прогоном (два вызова
`respond` без фрагмента в возврате: `/admin` и `/dashboard`). Правило двойного исполнения на цепи
шаблона `/admin` должно остаться зелёным; если покраснеет — останов и запись отступления.

**3. `tests/test_templates/test_htmx_markup_gates.py`.** `base.html` — в кортеж вызывающих
`form_wrapper` (только в перечень мест определения; цели у формы нет, в параметрические цели она
не входит) с комментарием «Фаза 14, план 14-06: форма возврата из-под чужой личности в полосе
шелла, `hx-swap="none"`, ответ — заголовок перехода»; `MACRO_DEFINITION_SITES_CALLERS_DECLARED`
31 → 32 и `UNREACHABLE_TARGET_CALL_BLOCKS_MEASURED` 28 → 29 (в слоте настоящая кнопка отправки) —
прогоном.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_hx_location_destinations.py tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_inventory.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_markup_security.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_impersonation_gate.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_preserved.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_auth_transport.py tests/test_pages/test_impersonation.py tests/test_pages/test_shell.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c '"/admin": "admin/overview.html"' tests/test_pages/test_hx_location_destinations.py` возвращает `1`.
- `grep -c 'AUTH_STOP_IMPERSONATION' tests/test_pages/test_htmx_post_pairs.py` не меньше `2`.
- `grep -c 'Фаза 14, план 14-06' tests/test_templates/test_htmx_markup_gates.py` не меньше `1`; то же в `tests/test_pages/test_hx_location_destinations.py` и `tests/test_pages/test_htmx_post_pairs.py`.
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q -p no:randomly -k "every_converted_handler_has_a_pair"` — зелено: у каждого из десяти обработчиков фазы есть пара.
  </acceptance_criteria>

  <done>
У всех десяти обработчиков фазы есть пары; `/admin` — объявленное назначение перехода; форма
полосы — среди вызывающих макроса; числа поставлены прогоном.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | cookie с признаком действующего лица; заголовки источника; признак htmx |
| страничный слой → браузер | `Set-Cookie` перевыпуска либо снятия; заголовки `HX-Location` / `HX-Redirect` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-10 | Spoofing / CSRF (поддельная форма возврата) | `stop_impersonation` | high | mitigate | Сверка источника первой, голый 403 на обоих транспортах (D-01); правило `test_a_cross_origin_return_is_refused_with_a_bare_403_on_both_transports`; гейт `test_origin_guard_on_destructive_routes.py` |
| T-14-11 | Elevation of Privilege (администратор остаётся под чужой личностью) | успех возврата на пути htmx | high | mitigate | Cookie на ВОЗВРАЩЁННОМ ответе `respond`; тройная пара: заголовок, cookie без признака действующего лица, `GET /admin` |
| T-14-12 | Elevation of Privilege (заблокированный администратор выходит со свежим токеном) | ветка закрытого действующего лица | high | mitigate | Ветка сохранена; `clear_session_cookie` на ответе `redirect_internal`; правило «снятие cookie и админка не открывается» на обоих транспортах |
| T-14-01 | Tampering (open redirect) | выходы возврата | medium | mitigate | Адреса — литералы, правило `test_every_full_load_address_is_a_literal_local_path`; `_local_path` |
| T-14-18 | Repudiation (изъятие без решения) | запись `OWN_RESPONSE_EXITS` возврата | low | mitigate | Состояние `DECISION_OWNER_D01_F14` называет D-01; правило авторства `_DECISION_REFERENCE` |
</threat_model>

<verification>
- После каждой задачи — её `<verify>`; последняя команда задачи 3 — прогон гейтов волны.
- В конце плана — полный прогон `uv run pytest tests/ -q` ОДИН раз: именованный ноль отставания
  и гейт критерия 3 — последние перечни вехи этой фазы; покрасневший тест, не названный здесь,
  правится той же дисциплиной и записывается в SUMMARY. Известный шум — ночное окно
  админ-обзора (CONTEXT Landmines).
- `uv run python -m compileall -q app main.py tests`; `graphify update .`.
</verification>

<success_criteria>
1. Три ветки возврата и отказ по источнику — на обоих транспортах, cookie на верном объекте.
2. Критерий 3 — машинное правило с контролями; `HX-Location` в модуле авторизации только у возврата.
3. Отставание вехи — именованный ноль; все числа поставлены прогоном.
</success_criteria>

<output>
Create `.planning/phases/14-avtorizatsiya-na-htmx/14-06-SUMMARY.md` when done.
</output>
