---
phase: 14-avtorizatsiya-na-htmx
plan: 05
type: tdd
wave: 5
depends_on: ["14-04"]
files_modified:
  - app/pages/auth.py
  - app/templates/auth_base.html
  - app/templates/auth/forgot_password_reset.html
  - app/templates/auth/includes/forgot_password_reset_step.html
  - app/templates/auth/includes/forgot_password_verify_step.html
  - tests/test_pages/test_auth_transport.py
  - tests/test_pages/test_password_reset.py
  - tests/test_pages/test_htmx_gates.py
  - tests/test_pages/test_htmx_post_pairs.py
  - tests/test_templates/test_htmx_markup_gates.py
autonomous: true
requirements: [SIGN-01, SIGN-02, SIGN-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: sample_count 0, factor 1; confidence выведен, а не самооценён.
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Неверный код восстановления отвечает 422 на обоих транспортах с «Неверный код. Осталось попыток: N» и набранным кодом в `value=`; истёкший или исчерпанный — 422 с прежним текстом; верный — 200 и экран нового пароля с `<title>Новый пароль — Broadcaster</title>` первым узлом фрагмента (критерий 1, D-03, D-04, D-07)"
    - "Короткий новый пароль отвечает 422 с «Пароль должен быть не менее 6 символов» без эха (строки пароля в теле нет) и новым подтверждённым токеном; устаревшая ссылка и исчезнувший пользователь — 200 и экран начала восстановления с прежними текстами (D-03, D-04)"
    - "Новый пароль без признака htmx — 302 на `/login?notice=password_reset_done`; с признаком — 204, `HX-Redirect: /login?notice=password_reset_done`, без `HX-Location`, пустое тело; cookie сессии не выдаётся; страница `/login` с этим кодом рисует «Пароль успешно изменён. Войдите с новым паролем.» областью уведомления шелла, и вход новым паролем проходит (критерий 3, D-10, FOUND-05)"
    - "Под чужой личностью подтверждение кода и новый пароль на пути htmx отвечают 204 и `HX-Location: /dashboard?notice=impersonation_forbidden`, без htmx — 403; пароль пользователя не меняется (D-13, D-22)"
    - "Обе формы экрана кода восстановления — из макроса с целью `#auth-step` и `hx-sync=\"closest #auth-step:drop\"`"
    - "Шелл окончателен: переходного блока подзаголовка в `auth_base.html` нет, каждый из семи экранов печатает ровно один подзаголовок сам, реестр `AUTH_SCREENS` накрывает все семь страниц `app/templates/auth/*.html` (D-06)"
    - "Гейты: `NOT_YET_CONVERTED_COUNT` 3 → 1 и прочие числа поставлены прогоном"
    - statement: "В браузере восстановление с кодом из настоящего письма доходит до `/login` с плашкой об изменении пароля, и вход новым паролем проходит — ручной UAT фазы (критерий 4, D-02)"
      verification: backstop
  artifacts:
    - "app/pages/auth.py — `forgot_password_verify`, `forgot_password_reset` на выходах слоя; запись `forgot_password_reset` в `AUTH_SCREENS`"
    - "app/templates/auth/includes/forgot_password_reset_step.html — единственный источник разметки экрана нового пароля"
    - "app/templates/auth_base.html — якорь без переходного блока подзаголовка"
  key_links:
    - "форма нового пароля → `forgot_password_reset` → `redirect_internal(..., notice=PASSWORD_RESET_DONE)` → `/login?notice=…` → область уведомления шелла ВНЕ якоря: ломается — исход смены пароля не доезжает до человека"
    - "`forbid_when_impersonating` на подтверждении и новом пароле → `HtmxRefusal` на пути htmx: ломается — администратор меняет пароль пользователя"
  prohibitions:
    - statement: "новый пароль не возвращается в ответ ни на каком исходе (D-04)"
      status: flagged-unverified
      verification: none
    - statement: "восстановление пароля под чужой личностью остаётся запрещённым (D-13, D-22)"
      status: flagged-unverified
      verification: none
    - statement: "инвентарное число гейта ставится ПРОГОНОМ покрасневшего правила, а не вычитанием в уме (D-13 Фазы 8)"
      status: flagged-unverified
      verification: none
---

<objective>
Вторая половина восстановления на фрагментах: «экран кода → новый пароль → вход с уведомлением».
Неверный код оставляет набранное, короткий пароль — токен без эха пароля, успех уходит полной
загрузкой на `/login` с кодом исхода, который рисует область уведомления шелла (D-10: последний
из трёх выходов полной перезагрузки идёт внутри второго шелла тем же заголовком). Это последний
экран авторизации — шелл освобождается от переходного блока подзаголовка.

Purpose: закрывает девять форм критерия 2 и пути критерия 4 на стороне сервера. Возврат из-под
чужой личности — 14-06.

Output: два переведённых обработчика, экран нового пароля, экран кода с обеими формами на макросе,
окончательный шелл, сдвинутые перечни.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-avtorizatsiya-na-htmx/14-CONTEXT.md
@.planning/phases/14-avtorizatsiya-na-htmx/14-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**`app/pages/htmx.py`:** `redirect_internal` (14-01), `respond_screen`, `_respond_by_transport`
(14-02). **`app/pages/auth.py`:** `AUTH_STEP_RESPONSE_TEMPLATE`, `AuthScreen`, `AUTH_SCREENS`
(+ `forgot_password_reset` — этот план; семь записей всего), `_screen_markup`, `_screen_builders`.
**Шаблоны:** `auth/includes/step_response.html`, `login_step.html`, `register_step.html`,
`register_verify_step.html`, `register_complete_step.html`, `forgot_password_step.html`,
`forgot_password_verify_step.html`, `forgot_password_reset_step.html` (этот план); якорь
`auth-step`. **Тесты:** `tests/test_pages/test_auth_transport.py`; `RESPONSE_LAYER_EXITS`,
`FULL_LOAD_CALL`, `FULL_LOAD_HANDLERS`, `AUTH_MODULE`, `AUTH_HX_LOCATION_HANDLERS`,
`DECISION_OWNER_D01_F14`, `LIFTING_CONDITION_OWN_RESPONSE_RETURN`, `_full_load_callers`,
`_hx_location_emitters` (14-06); ветки пар `FULL_LOAD`, `SCREEN`, `ANONYMOUS`,
`_PairCase.sets_session_cookie`, ключи `AUTH_*` (`AUTH_FORGOT_VERIFY`, `AUTH_FORGOT_RESET` — этот
план). **Записи (14-07):** летописи ROADMAP, REQUIREMENTS, PROJECT, окно 63, `14-UAT.md`.
</artifacts_this_phase_produces>

<tdd_notes>
- Коммиты: RED — `test(14-05): …`, GREEN — `feat(14-05): …`, перечни — `test(14-05): record …`.
- RED-улика — как в 14-01 (`--junit-xml` → TAP в scratchpad). Целевое правило —
  `test_a_short_new_password_answers_422_without_echo_on_both_transports`; RED засчитывается
  тремя совпадениями в выводе
  `uv run pytest tests/test_pages/test_auth_transport.py -q -p no:randomly -k short_new_password`:
  (1) `^FAILED tests/test_pages/test_auth_transport.py::test_a_short_new_password_answers_422_without_echo_on_both_transports`;
  (2) итоговая строка вида `1 failed`; (3) причинный литерал `assert 200 == 422` — на дереве до
  правки короткий новый пароль отвечает готовой страницей со статусом 200
  (`app/pages/auth.py:797-802`, проверено чтением).
</tdd_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Восстановление «код → новый пароль → вход с уведомлением» на фрагментах якоря, шелл без переходного блока</name>

  <files>
app/pages/auth.py,
app/templates/auth_base.html,
app/templates/auth/forgot_password_reset.html,
app/templates/auth/includes/forgot_password_reset_step.html,
app/templates/auth/includes/forgot_password_verify_step.html,
tests/test_pages/test_auth_transport.py
  </files>

  <read_first>
- `app/pages/auth.py` после 14-04; блок `forgot_password_verify` и `forgot_password_reset`
  (`:626-690`, `:771-814` дерева до фазы) — предмет перевода; комментарий FOUND-05 над выходом
  успеха сохраняется.
- `app/templates/auth_base.html` (редакция 14-01, переходный блок `auth_subtitle` в якоре),
  `app/templates/auth/forgot_password_reset.html`, `app/templates/auth/includes/forgot_password_verify_step.html`
  (редакция 14-04), `app/templates/auth/includes/register_verify_step.html` (редакция 14-03 —
  образец обеих форм на макросе).
- `app/pages/notices.py:130-140`, `:280-286` (`PASSWORD_RESET_DONE` и его текст).
- `tests/test_pages/test_password_reset.py:60-220`.
- `14-RESEARCH.md` §Карта выходов (строки `forgot_password_verify`, `forgot_password_reset`),
  §Pattern 2 (подзаголовок уезжает в экран).
  </read_first>

  <behavior>
RED (`tests/test_pages/test_auth_transport.py`):
1. `test_a_short_new_password_answers_422_without_echo_on_both_transports` — 422, «Пароль должен
   быть не менее 6 символов», строки пароля нет, скрытый `token` есть; с признаком —
   `<title>Новый пароль — Broadcaster</title>` первым узлом.
2. `test_a_wrong_recovery_code_keeps_the_typed_code_and_answers_422` — 422, «Неверный код. Осталось
   попыток: 4», код в `value=`; посев с `attempts=5` — 422, «Код истёк или превышено число попыток.
   Отправьте код заново.».
3. `test_a_right_recovery_code_opens_the_new_password_screen` — 200 на обоих транспортах, с
   признаком — `hx-post="/forgot-password/reset"` и `<title>Новый пароль — Broadcaster</title>`.
4. `test_stale_recovery_links_return_to_the_start_with_200` — подтверждение и новый пароль с токеном
   `x`: 200, «Ссылка устарела. Начните сброс пароля заново.».
5. `test_a_vanished_user_returns_to_the_start_of_recovery_with_200` — подтверждённый токен на адрес
   без пользователя: 200, «Пользователь не найден.».
6. `test_a_new_password_leaves_for_the_login_by_a_full_load_with_the_notice` — без признака 302,
   `location == "/login?notice=password_reset_done"` (адрес `"/forgot-password/reset"` литералом в
   `.post(…)`); с признаком 204, `HX-Redirect == "/login?notice=password_reset_done"`, без
   `HX-Location`, пустое тело; `access_token` в `set-cookie` нет ни на одном; `GET
   /login?notice=password_reset_done` рисует «Пароль успешно изменён. Войдите с новым паролем.» ДО
   `id="auth-step"`; вход новым паролем без признака → 302 на `/dashboard`.
7. `test_the_last_recovery_steps_are_refused_under_another_identity_on_both_transports` — админ под
   чужой личностью: подтверждение и новый пароль без признака → 403, с признаком → 204 и
   `HX-Location == "/dashboard?notice=impersonation_forbidden"`; хеш пароля пользователя прежний.
8. `test_both_recovery_code_forms_ride_the_anchor_and_drop_a_second_request` — обе формы экрана кода
   несут `hx-target="#auth-step"` и `hx-sync="closest #auth-step:drop"`.
9. `test_the_shell_leaves_the_subtitle_to_every_screen` — в исходнике `auth_base.html` нет
   `auth_subtitle`; ни одна страница `app/templates/auth/*.html` его не объявляет; в `AUTH_SCREENS`
   ровно семь записей, и их страницы — ровно множество `app/templates/auth/*.html` (анти-вакуум);
   `GET /login`, `GET /register`, `GET /forgot-password` несут ровно одно `class="auth-subtitle"`
   с непустым текстом.
GREEN — обработчики, экраны, шелл.
  </behavior>

  <action>
**1. `app/pages/auth.py` (D-03, D-04, D-10, D-13, D-14, D-15).** Запись `"forgot_password_reset":
AuthScreen(page="auth/forgot_password_reset.html", step="auth/includes/forgot_password_reset_step.html",
title="Новый пароль — Broadcaster")`. `forgot_password_verify`: токен негоден → `respond_screen`
экраном `forgot_password` с «Ссылка устарела. Начните сброс пароля заново.»; кода нет →
`respond_field_error` экраном `forgot_password_verify` с `email`, присланным `token`, `code=code` и
прежним текстом; неверный код — счёт попыток и `commit` прежние, затем `respond_field_error` с
эхом кода; успех — отметка, `commit`, `respond_screen` экраном `forgot_password_reset` с
подтверждённым токеном. `forgot_password_reset`: токен негоден и пользователь не найден →
`respond_screen` экраном `forgot_password` с прежними текстами; короткий пароль →
`respond_field_error` экраном `forgot_password_reset` с `email` и новым подтверждённым токеном,
без пароля; успех — хеш и `commit` прежние, затем `return await redirect_internal(request,
redirect="/login", notice=notices.PASSWORD_RESET_DONE)`; комментарий FOUND-05 над выходом
сохраняется и дополняется строкой «на пути htmx — полная загрузка тем же адресом (D-10)».
Зависимость отказа под чужой личностью не трогается (D-13). Прежние возвраты снимаются целиком
(D-14).

**2. `app/templates/auth/includes/forgot_password_verify_step.html` (D-06, Pattern 5).** Форма
подтверждения — блочный вызов `form_wrapper(action='/forgot-password/verify', target='#auth-step',
swap='innerHTML', sync='closest #auth-step:drop')` с `<div class="auth-form">`, скрытым `token`,
полем кода с `value=code or ''` и кнопкой «Подтвердить»; форма повтора получает тот же `sync`;
шапка — летопись «окно 14-04 закрыто планом 14-05» без стирания прежнего абзаца.

**3. `app/templates/auth/includes/forgot_password_reset_step.html` (новый) и
`app/templates/auth/forgot_password_reset.html`.** Экран: `<p class="auth-subtitle">Установите новый
пароль</p>`, алерт ошибки, строка «Email … подтверждён», блочный вызов
`form_wrapper(action='/forgot-password/reset', target='#auth-step', swap='innerHTML')` с
`<div class="auth-form">`, скрытым `token`, полем нового пароля без значения (параметры как
сегодня) и кнопкой «Сменить пароль». Страница — `extends`, прежний блок `title`, одно включение.

**4. `app/templates/auth_base.html` (D-06).** Из якоря снимается переходная строка подзаголовка
вместе с её блоком; якорь содержит только блок содержимого. Шапка шелла: перечень блоков — `title`
и `content`; абзац 14-01 о переходном блоке не стирается, а дополняется летописью «снят планом
14-05: все семь экранов печатают подзаголовок сами». В прозе шапки блок называется СЛОВАМИ
(«переходный блок подзаголовка»), а не своим именем: отсутствие имени проверяется грепом, а греп
читает и комментарии. Правило CSS для подзаголовка в карточке (14-01) не меняется — подзаголовок
экрана по-прежнему визуально под брендом.

**5. Тесты** — правила `<behavior>`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_auth_transport.py tests/test_pages/test_impersonation.py tests/test_pages/test_shell.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_auth_transport.py -q -p no:randomly -k "short_new_password or recovery_code or new_password_leaves or last_recovery_steps or subtitle_to_every_screen"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^async def forgot_password_verify/,/^@router/' app/pages/auth.py | grep -c 'TemplateResponse'` возвращает `0`; `awk '/^async def forgot_password_reset/,/^@router/' app/pages/auth.py | grep -cE 'TemplateResponse|RedirectResponse'` возвращает `0`.
- `awk '/^async def forgot_password_reset/,/^@router/' app/pages/auth.py | grep -c 'redirect_internal(request, redirect="/login", notice=notices.PASSWORD_RESET_DONE)'` возвращает `1`.
- `grep -c 'auth_subtitle' app/templates/auth_base.html` возвращает `0`; `grep -l 'auth_subtitle' app/templates/auth/*.html | wc -l` печатает `0`.
- `grep -c "sync='closest #auth-step:drop'" app/templates/auth/includes/forgot_password_verify_step.html` возвращает `2`.
- `uv run pytest tests/test_pages/test_auth_transport.py -q -p no:randomly` — зелено.
  </acceptance_criteria>

  <done>
Восстановление проходит целиком на фрагментах якоря и уводит на `/login` с плашкой об изменении
пароля; все семь экранов — включаемые шаблоны, шелл окончателен.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Перечни второй половины восстановления и прежние утверждения об ошибках кода и пароля</name>

  <files>
tests/test_pages/test_htmx_gates.py,
tests/test_pages/test_htmx_post_pairs.py,
tests/test_templates/test_htmx_markup_gates.py,
tests/test_pages/test_password_reset.py
  </files>

  <read_first>
- `tests/test_pages/test_htmx_gates.py` — `NOT_YET_CONVERTED`, `FRAGMENT_RESPONSE_HANDLERS`,
  летописи после 14-04.
- `tests/test_pages/test_htmx_post_pairs.py` — посевы и случаи 14-03/14-04; ветка `FULL_LOAD`.
- `tests/test_templates/test_htmx_markup_gates.py` — записи экранов восстановления 14-04.
- `tests/test_pages/test_password_reset.py:100-122`, `:185-220`.
  </read_first>

  <action>
Числа — ПРОГОНОМ покрасневшего правила, летопись «Фаза 14, план 14-05» с дословным выводом.

**1. `tests/test_pages/test_htmx_gates.py`.** Ключи `forgot_password_verify` и
`forgot_password_reset` снимаются из `NOT_YET_CONVERTED` ПЕРЕВОДОМ; `NOT_YET_CONVERTED_COUNT`
3 → 1 (остаток — возврат из-под чужой личности, 14-06); оба ключа — в
`FRAGMENT_RESPONSE_HANDLERS` с комментарием на запись; `FRAGMENT_RESPONSE_HANDLERS_DECLARED`
23 → 25.

**2. `tests/test_pages/test_htmx_post_pairs.py` (D-10).** Ключи `AUTH_FORGOT_VERIFY`,
`AUTH_FORGOT_RESET`; личность `ANONYMOUS`; посев — пользователь, код и токены на уникальный адрес
каждого вызова. Случаи: подтверждение — верный код (`SCREEN`, метка
`action="/forgot-password/reset"`), подтверждение — устаревшая ссылка (`SCREEN`, метка
`action="/forgot-password/send-code"`), новый пароль — успех (`FULL_LOAD`,
`landing="/login?notice=" + notices.PASSWORD_RESET_DONE`, без `sets_session_cookie`), новый пароль —
устаревшая ссылка (`SCREEN`), новый пароль — пользователь не найден (`SCREEN`, метка
«Пользователь не найден.»). `POST_PAIR_CASES_DECLARED` 71 → 76 и `PAIRED_302_ASSERTIONS_DECLARED`
— прогоном (ожидание: +1 прежнее утверждение 302 о новом пароле — замер планирования — плюс новые
утверждения `test_auth_transport.py`).

**3. `tests/test_templates/test_htmx_markup_gates.py`.** `auth/includes/forgot_password_reset_step.html`
— в оба кортежа вызывающих с комментарием и замером G-9 / G-11 / G-12;
`MACRO_DEFINITION_SITES_CALLERS_DECLARED` 30 → 31, `PARAMETRIC_SWAP_TARGETS_CALLERS_DECLARED`
15 → 16, `UNREACHABLE_TARGET_CALL_BLOCKS_MEASURED` 26 → 28 — прогоном; запись
`forgot_password_verify_step.html` пополняется строкой «обе формы экрана на макросе».

**4. `tests/test_pages/test_password_reset.py` (D-03).** `test_verify_wrong_code` и
`test_reset_short_password` ждут 422 вместо 200; проверки текста не трогаются;
комментарий-летопись «Фаза 14, план 14-05, D-03».
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_password_reset.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_pages/test_htmx_gates.py tests/test_templates/test_htmx_inventory.py tests/test_templates/test_htmx_markup_gates.py tests/test_templates/test_htmx_markup_security.py tests/test_pages/test_hx_location_destinations.py tests/test_pages/test_impersonation_gate.py tests/test_pages/test_identifier_bounds.py tests/test_pages/test_htmx_preserved.py tests/test_pages/test_htmx_post_pairs.py tests/test_pages/test_htmx_response_layer.py tests/test_pages/test_auth_transport.py tests/test_pages/test_password_reset.py tests/test_pages/test_reset_code_source.py tests/test_pages/test_shell.py -q -p no:randomly</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>

  <acceptance_criteria>
- `awk '/^NOT_YET_CONVERTED: frozenset/,/^\)/' tests/test_pages/test_htmx_gates.py | grep -v '^\s*#' | grep -c 'auth.py::'` возвращает `1` (остался только возврат из-под чужой личности).
- `grep -c 'AUTH_FORGOT_RESET' tests/test_pages/test_htmx_post_pairs.py` не меньше `2`; `grep -c 'Фаза 14, план 14-05' tests/test_pages/test_htmx_gates.py` не меньше `2`.
- `grep -c 'auth/includes/forgot_password_reset_step.html' tests/test_templates/test_htmx_markup_gates.py` не меньше `2`.
- `uv run pytest tests/test_pages/test_htmx_post_pairs.py -q -p no:randomly -k "every_converted_handler_has_a_pair or every_302_assertion"` — зелено.
  </acceptance_criteria>

  <done>
Все девять форм авторизации описаны перечнями; в отставании остался один обработчик — возврат;
прежние утверждения об ошибках кода и пароля следуют D-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → страничный слой | поля `token`, `code`, `password`; cookie с признаком действующего лица |
| страничный слой → браузер | эхо кода; подтверждённый токен; `HX-Redirect` на `/login` с кодом исхода |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-13 | Elevation of Privilege (смена пароля под чужой личностью) | `forgot_password_verify`, `forgot_password_reset` | high | mitigate | Зависимость отказа не трогается (D-13, D-22); правило на обоих транспортах с проверкой «хеш пароля прежний» |
| T-14-04 | Information Disclosure (новый пароль в ответе) | 422 нового пароля | high | mitigate | Пароль не передаётся в контекст; правило «строки пароля нет» |
| T-14-01 | Tampering (open redirect через код исхода) | `redirect_internal` с `notice=` | high | mitigate | Код из реестра (`_require_registered_notice`), адрес — литерал `/login`, `_with_notice` → `_local_path` |
| T-14-16 | Elevation (перебор кода восстановления) | `forgot_password_verify` | medium | mitigate | Счёт попыток и лимит прежние (D-15); правило «Осталось попыток: 4» |
</threat_model>

<verification>
- После каждой задачи — её `<verify>`; последняя команда задачи 2 — прогон гейтов волны.
- `uv run python -m compileall -q app main.py tests`; `graphify update .`.
</verification>

<success_criteria>
1. Неверный код — 422 с набранным; короткий пароль — 422 без эха; успех — полная загрузка на
   `/login` с плашкой; вход новым паролем проходит.
2. Под чужой личностью шаги закрыты на обоих транспортах.
3. Шелл окончателен, семь экранов — включаемые шаблоны; перечни сдвинуты прогоном.
</success_criteria>

<output>
Create `.planning/phases/14-avtorizatsiya-na-htmx/14-05-SUMMARY.md` when done.
</output>
