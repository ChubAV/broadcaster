---
phase: 14-avtorizatsiya-na-htmx
plan: 07
type: execute
wave: 7
depends_on: ["14-06"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/WINDOWS.md
  - .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md
autonomous: true
requirements: [SIGN-01, SIGN-02, SIGN-03]

estimate:
  # НЕКАЛИБРОВАННАЯ ПРОЕКЦИЯ: sample_count 0, factor 1; confidence выведен, а не самооценён.
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "У критерия 3 Фазы 14 в `.planning/ROADMAP.md` стоит летопись: посылка «`/impersonation/stop` не пересекает границу шеллов» верна для двух веток возврата из трёх — третья (действующего лица нет или оно закрыто) уходит `HX-Redirect` на `/login`; второй отправитель `HX-Location` — отказ зависимости `forbid_when_impersonating` на четырёх шагах восстановления, живущий вне модуля авторизации; успех нового пароля уходит `HX-Redirect` внутри второго шелла. Текст критерия не переписан (D-10, D-12, RESEARCH Находка 6)"
    - "У SIGN-03 в `.planning/REQUIREMENTS.md` стоит та же летопись; флажок `[ ]` и ячейка `Pending` не тронуты — отметку ставит закрытие фазы после верификации"
    - "У критерия 2 стоит летопись счёта: девять форм (вход 1, начало регистрации 1, экран кода регистрации 2, завершение 1, начало восстановления 1, экран кода восстановления 2, новый пароль 1) и десятая — возврат из-под чужой личности; счётчик отставания вехи — именованный ноль (D-14)"
    - "У строки Research раздела Фазы 14 стоит летопись: посылка «`#notice` в `auth_base.html` не приходит» ложна с Фазы 8; «успех авторизации — `HX-Location`» заменено `HX-Redirect`; у рамки вехи в `.planning/PROJECT.md` — та же летопись; записи разведки `.planning/research/*` не правятся"
    - "Окно 63 реестра `.planning/WINDOWS.md` переведено штатной командой реестра: при замере «14 мест голого 403, 14 записей `OWN_RESPONSE_EXITS`, 0 в состоянии ожидания владельца» — `waived` с причиной-летописью, называющей D-08 Фазы 11, D-15 Фазы 12 и D-01 Фазы 14; при ином замере — `open` с летописью, называющей остаток (D-01)"
    - "`.planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` существует в размеченной форме: `checks_declared: 7`, семь разделов `## Проверка N — …`, семь подзаголовков `### Отметка о закрытии проверки N` с пустыми таблицами, `status: testing`, `result: [pending]`; предусловия обхода выписаны (настоящий SMTP, ящики, админ, два браузера) вместе с правилом останова, если письма не доходят (D-02)"
    - "`uv run pytest tests/test_planning -q` зелено после каждой правки записи"
  artifacts:
    - ".planning/ROADMAP.md — летописи критериев 2 и 3 и строки Research раздела Фазы 14"
    - ".planning/REQUIREMENTS.md — летопись SIGN-03"
    - ".planning/PROJECT.md — летопись рамки вехи об успехе авторизации"
    - ".planning/WINDOWS.md — окно 63 переведено командой реестра"
    - ".planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md — артефакт ручного обхода критерия 4"
  key_links:
    - "летопись критерия 3 ↔ летопись SIGN-03: ломается — верификатор читает «`HX-Location` только у `/impersonation/stop`» буквально и находит второго отправителя без объяснения"
    - "замер окна 63 → команда реестра `windows waive`: ломается — окно закрыто словами без замера либо правкой таблицы в обход журнала JSON"
  prohibitions:
    - statement: "тексты критериев ROADMAP и требований не переписываются — рядом ставится летопись (идиома D-30/D-32)"
      status: flagged-unverified
      verification: none
    - statement: "машинная улика обхода — не приёмка: `result` и таблицы отметок заполняет человек; код подтверждения из базы вместо письма не подставляется (D-02)"
      status: flagged-unverified
      verification: none
    - statement: "состояние окна реестра меняется только командой реестра, а не правкой таблицы"
      status: flagged-unverified
      verification: none
---

<objective>
Записи фазы приводятся к сделанному: летописи у критериев 2 и 3 и у строки Research раздела
Фазы 14 в ROADMAP, у SIGN-03 в REQUIREMENTS и у рамки вехи в PROJECT — без переписывания самих
текстов; окно 63 реестра решается замером и штатной командой (D-01); ручной обход критерия 4
получает артефакт с предусловиями настоящих писем (D-02).

Purpose: буквальный текст критерия 3 и SIGN-03 («`HX-Location` ТОЛЬКО у `/impersonation/stop`,
не пересекающего границу шеллов») верен не целиком — у возврата три ветки, и у маршрутов есть
второй отправитель. Верификатор, читающий критерий без летописи, найдёт расхождение там, где
его нет по существу (D-12, RESEARCH Находка 6).

Output: пять абзацев летописи, решённое окно 63, `14-UAT.md`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-avtorizatsiya-na-htmx/14-CONTEXT.md
@.planning/phases/14-avtorizatsiya-na-htmx/14-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**`app/pages/htmx.py`:** `redirect_internal`, `respond_screen`, `_respond_by_transport`.
**`app/pages/auth.py`:** `AUTH_STEP_RESPONSE_TEMPLATE`, `AuthScreen`, `AUTH_SCREENS`,
`_screen_markup`, `_screen_builders`. **Шаблоны:** `auth/includes/step_response.html` и семь
экранов `auth/includes/*_step.html`; якорь `auth-step`. **Тесты:** `tests/test_pages/test_auth_transport.py`,
`RESPONSE_LAYER_EXITS`, `FULL_LOAD_CALL`, `FULL_LOAD_HANDLERS`, `AUTH_MODULE`,
`AUTH_HX_LOCATION_HANDLERS`, `DECISION_OWNER_D01_F14`, `LIFTING_CONDITION_OWN_RESPONSE_RETURN`,
`_full_load_callers`, `_hx_location_emitters`, ветки пар `FULL_LOAD` / `SCREEN`, `ANONYMOUS`,
`_PairCase.sets_session_cookie`, ключи `AUTH_*`. **Записи (этот план):** летописи критериев 2 и 3
и строки Research в ROADMAP, летопись SIGN-03 в REQUIREMENTS, летопись рамки вехи в PROJECT,
окно 63 в `.planning/WINDOWS.md`, файл `.planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Задача 1: Летописи критериев 2 и 3 и строки Research в ROADMAP, SIGN-03 в REQUIREMENTS, рамки вехи в PROJECT</name>

  <files>
.planning/ROADMAP.md,
.planning/REQUIREMENTS.md,
.planning/PROJECT.md
  </files>

  <read_first>
- `.planning/ROADMAP.md` — раздел `### Phase 14:` целиком (критерии 1–4, строки `**Plans**` и
  `**Research**`, перечень планов) и раздел `### Phase 13:` — летописи критериев после перечня
  (ОБРАЗЕЦ формы: полужирный заголовок, скобка «фаза, план, решения», абзац, «⚠️ ПОСЫЛКА …»,
  «Записи разведки (`.planning/research/*`) НЕ ПРАВЯТСЯ»).
- `.planning/REQUIREMENTS.md:50-60` (SIGN-01…03 и образец летописи FETCH-02 под требованием),
  `:145-160` (таблица трассировки).
- `.planning/PROJECT.md:77-80` (рамки вехи: «Авторизация — исключение по МЕХАНИЗМУ…») и строка
  таблицы решений «Авторизация переводится на htmx, но успех уходит `HX-Location`…».
- `14-CONTEXT.md` D-10, D-12, D-13, D-14 и §canonical_refs «Устаревшие посылки разведки»;
  `14-RESEARCH.md` §Находка 5, §Находка 6.
- `14-01-SUMMARY.md` … `14-06-SUMMARY.md` — фактические числа, поставленные прогонами
  (`NOT_YET_CONVERTED_COUNT`, `OWN_RESPONSE_EXITS_DECLARED`, `POST_PAIR_CASES_DECLARED`).
- `tests/test_planning/test_state_progress_matches_roadmap.py:103-140` — форма прозы строки
  `**Plans**`, которую правило читает (строка `**Plans**` этим планом НЕ правится).
  </read_first>

  <action>
Абзацы ставятся в раздел Фазы 14 ПОСЛЕ перечня критериев и ДО строки `**Plans**`, в форме летописей
Фазы 13; тексты критериев не трогаются. В прозе летописей не встречается оборот «N планов, из них M
исполнено» — его читает правило прозы плана.

**1. ROADMAP — «Летопись критерия 2: девять форм и десятая»** (Фаза 14, план 14-07, D-09, D-14).
Содержание: счёт девяти форм по экранам (вход 1; начало регистрации 1; экран кода регистрации 2 —
подтверждение и повтор; завершение регистрации 1; начало восстановления 1; экран кода
восстановления 2; новый пароль 1), десятая — форма возврата в полосе `base.html`; все через
макрос `form_wrapper`, без JS — прежние `method`/`action`; счётчик отставания вехи
`NOT_YET_CONVERTED_COUNT` прошёл 10 → 0 и стоит ИМЕНОВАННЫМ НУЛЁМ (число — из SUMMARY 14-06).

**2. ROADMAP — «Летопись критерия 3: `HX-Location` — у возврата, но не только у него; граница
шеллов — у двух веток из трёх»** (Фаза 14, план 14-07, D-10, D-12, D-13). Содержание: (а) успех
входа и завершения регистрации уходит `HX-Redirect` на `/dashboard` через границу шеллов, как
сказано; успех нового пароля уходит тем же заголовком на `/login?notice=password_reset_done`
ВНУТРИ второго шелла (D-10); (б) у `/impersonation/stop` три ветки: успех — `HX-Location` на
`/admin`, действующего лица нет — `HX-Location` на `/dashboard` (обе внутри основного шелла), а
действующего лица нет в базе или оно заблокировано — `HX-Redirect` на `/login`, то есть ЧЕРЕЗ
границу шеллов: посылка «не пересекающего границу шеллов» верна для двух веток из трёх (D-12);
(в) второй отправитель `HX-Location` — отказ зависимости `forbid_when_impersonating` на четырёх
шагах восстановления пароля под чужой личностью (`/dashboard?notice=impersonation_forbidden`),
живущий вне модуля авторизации (`app/main.py`, `app/dependencies.py`), сохранён решением D-13;
(г) машинно держится гейтом критерия 3 в `tests/test_pages/test_htmx_gates.py` (вызывающие
выхода полной перезагрузки поимённо, `HX-Location` в `app/pages/auth.py` — только у возврата),
граница гейта названа в его шапке. «⚠️ ПОСЫЛКА КРИТЕРИЯ НЕ БЫЛА ЛОЖНОЙ ЦЕЛИКОМ — ОНА НЕПОЛНА:
текст не переписан». «Та же летопись стоит у SIGN-03 в `.planning/REQUIREMENTS.md`».

**3. ROADMAP — «Летопись строки Research: две посылки разведки устарели»** (Фаза 14, план 14-07).
Содержание: «`#notice` в `auth_base.html` не приходит, там свой канал внутри формы» — ЛОЖНА с
Фазы 8: второй шелл включает общую область уведомления (`includes/notice_area.html`), и исход
смены пароля рисует она (FOUND-05); «успех авторизации — `HX-Location`» (разведка вехи,
`ARCHITECTURE.md`, `FEATURES.md`, `PITFALLS.md`) заменено критерием 3 и D-10 на `HX-Redirect`; замер
вендоренного 2.0.10 опроверг и тезис «`HX-Location` через границу шеллов теряет `<title>`»
(RESEARCH Находка 6). «Записи разведки (`.planning/research/*`) НЕ ПРАВЯТСЯ».

**4. REQUIREMENTS — под строкой SIGN-03** с отступом в два пробела по образцу летописи FETCH-02:
«**Летопись SIGN-03: `HX-Location` — у возврата, но не только у него**» (Фаза 14, план 14-07,
D-10, D-12, D-13) — содержание пп. 2(а)–(в) сжато и «Та же летопись стоит у критерия 3 Фазы 14 в
`.planning/ROADMAP.md`». Флажок `[ ]` строк SIGN-01…03 и ячейки `Pending` таблицы трассировки НЕ
трогаются.

**5. PROJECT — под пунктом рамки «Авторизация — исключение по МЕХАНИЗМУ, а не по охвату»** —
строка-летопись с отступом: «Летопись (Фаза 14, D-10): успех уходит `HX-Redirect` — полной
загрузкой, а не `HX-Location`; `HX-Location` остался у возврата из-под чужой личности; формулировка
рамки не была ошибкой — она устарела решением фазы». В строке таблицы решений «Авторизация
переводится на htmx, но успех уходит `HX-Location`…» в ячейку обоснования дописывается та же
летопись одной фразой; ячейка исхода («— Pending») не трогается — её правит закрытие фазы.

**6. Если `tests/test_planning` краснеет по чужой причине.** Правило
`test_the_machine_readable_progress_is_derived_from_the_roadmap` сверяет поля трекинга
`.planning/STATE.md`, которые ведёт оркестратор (память проекта «ROADMAP mark desyncs STATE
progress»): его отказ, не вызванный этим планом, записывается в SUMMARY дословно и передаётся
оркестратору; `.planning/STATE.md` этот план не правит.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning -q</automated>
    <fails_when>non-zero exit with a failing rule other than `test_the_machine_readable_progress_is_derived_from_the_roadmap` (tracking-owned, recorded verbatim in the SUMMARY if it alone fails), or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_planning -q -k "requirement or flag_and_the_status or prose_plan_counts"</automated>
    <fails_when>non-zero exit (pytest exits 5 when the -k selection is empty), or "failed" in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -c '^\*\*Летопись критерия 3: .*(Фаза 14, план 14-07' .planning/ROADMAP.md` возвращает `1`; то же для `Летопись критерия 2` и `Летопись строки Research` — по `1`.
- Тексты критериев не переписаны: `d=$(mktemp) && git diff "$(git merge-base HEAD master)" -- .planning/ROADMAP.md > "$d" && grep -c '^-  [1-4]\. ' "$d"` печатает `0` (пустой вывод — отказ git, а не согласие).
- `grep -c '^  \*\*Летопись SIGN-03: .*(Фаза 14, план 14-07' .planning/REQUIREMENTS.md` возвращает `1`; `grep -c '^- \[ \] \*\*SIGN-03\*\*' .planning/REQUIREMENTS.md` возвращает `1`; `grep -c '| SIGN-03 | Phase 14 | Pending |' .planning/REQUIREMENTS.md` возвращает `1`.
- `grep -c 'Летопись (Фаза 14, D-10)' .planning/PROJECT.md` не меньше `1`.
- `git diff --quiet "$(git merge-base HEAD master)" -- .planning/research/` — код 0: записи разведки не тронуты.
  </acceptance_criteria>

  <done>
Критерии 2 и 3, строка Research, SIGN-03 и рамка вехи несут летописи; тексты прежние; требования не
отмечены выполненными.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Окно 63 — замер и штатная команда реестра; артефакт ручного обхода с предусловиями настоящих писем</name>

  <files>
.planning/WINDOWS.md,
.planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md
  </files>

  <read_first>
- `.planning/WINDOWS.md` — шапка (команды реестра), строка окна 63 и запись окна 63 в журнале JSON
  ниже таблицы.
- `.claude/gsd-core/bin/lib/broken-windows.cjs:270-300` (`markWaived` — причина обязательна, окно
  должно быть открыто) и `:850-880` (проза НИЖЕ журнала JSON сохраняется командами реестра).
- `tests/test_pages/test_htmx_gates.py` — `OWN_RESPONSE_EXITS`, `DECISION_WAITS_FOR_THE_OWNER`,
  `_converted_with_own_response`, `_pages_sources` (после 14-06).
- `.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-UAT.md` — размеченная
  форма; её первая редакция — `git show 4740ba90:.planning/phases/13-master-podklyucheniya-telegram-po-qr-na-fragmentah/13-UAT.md`.
- `tests/test_planning/test_the_walkthrough_cannot_self_certify.py:50-250` — что правило считает
  (поле `status` первого блока, `checks_declared`, разделы `## Проверка N`, подзаголовки
  `### Отметка`, строки данных таблиц).
- `14-CONTEXT.md` D-01, D-02, §Specifics; `14-RESEARCH.md` §Находка 7 (пять наблюдений менеджера
  паролей), §Pattern 2 (A4), §Environment Availability; `14-VALIDATION.md` §Manual-Only Verifications.
  </read_first>

  <action>
**1. Окно 63 (D-01).** Замер — две команды, вывод дословно в SUMMARY:
`grep -rn "Response(status_code=403)" app/pages --include=*.py | grep -v "^app/pages/htmx.py" | wc -l`
(ожидание `14`: тринадцать мест записи окна плюс загрузка вложения Фазы 12) и
`uv run python -c "from tests.test_pages.test_htmx_gates import OWN_RESPONSE_EXITS as e, DECISION_WAITS_FOR_THE_OWNER as w, _converted_with_own_response as m, _pages_sources as s; print(len(e), sum(x.decision_state == w for x in e), len(m(s())))"`
(ожидание `14 0 14`). Если оба совпали — окно переводится ШТАТНОЙ командой реестра
`node .claude/gsd-core/bin/gsd-tools.cjs windows waive 63 "<причина>"`, где причина — летопись
одним абзацем: «Фаза 14, план 14-07: решение о ФОРМЕ отказа по источнику принято для ВСЕХ мест.
Замер: мест голого 403 в страничном слое — 14; записей `OWN_RESPONSE_EXITS` — 14, в состоянии
ожидания владельца — 0; отставание вехи — 0, то есть обработчиков вне слоя ответа не осталось.
Решения владельца: D-08 Фазы 11 (двенадцать записей), D-15 Фазы 12 (загрузка вложения), D-01
Фазы 14 (возврат из-под чужой личности). Машинный гейт продолжает считать выходы; окно закрыто
решением, а не работой». Если замер показал иное — окно ОСТАЁТСЯ `open`, а летопись с остатком
(какие места, в каком состоянии) пишется абзацем «Летопись окна 63 (Фаза 14, план 14-07)» НИЖЕ
журнала JSON — проза там сохраняется командами реестра; таблица и журнал руками не правятся.

**2. `.planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` (D-02).** Размеченная форма по первой
редакции `13-UAT.md`: frontmatter — `status: testing`, `phase: 14-avtorizatsiya-na-htmx`,
`requirement: [SIGN-01, SIGN-02, SIGN-03]`, `source: [14-VALIDATION.md, 14-RESEARCH.md]`,
`created`, `created_by: план 14-07 (D-02 — предусловия обхода выписывает планировщик)`,
`checks_declared: 7`. Раздел «**Предусловия обхода**»: рабочий SMTP на стенде; доступный ящик для
регистрации НОВОГО адреса; ящик СУЩЕСТВУЮЩЕЙ учётной записи для восстановления; учётная запись
администратора и пользователь для входа под ним; Chrome и Firefox с чистым профилем без
сохранённого пароля стенда. Правило останова дословно по D-02: если письма не доходят, обход
ОСТАНАВЛИВАЕТСЯ и вопрос уходит владельцу; код из базы не подставляется. `## Current Test` —
номер 1, `awaiting: user response`. `## Tests` — семь пунктов `### N. …` с `expected:` и
`result: [pending]`. Семь разделов `## Проверка N — …` с таблицей шагов «# | Шаг | Ожидаемый
наблюдаемый признак», строкой «**Почему человек.**» и подзаголовком `### Отметка о закрытии
проверки N` с таблицей «Дата | Браузер / ОС | Кто наблюдал | Наблюдённый признак» из шапки,
разделителя и ОДНОЙ ПУСТОЙ строки данных. Проверки:
1 — вход (критерий 4a): неверный пароль оставляет email, пароль пуст, вкладка «Вход — Broadcaster»;
заблокированный видит причину; верный — кабинет полной загрузкой, cookie `access_token` сменилась
(DevTools → Application → Cookies), вкладка — заголовок кабинета.
2 — регистрация (4b): новый адрес → экран кода без перезагрузки, адресная строка `/register`;
короткий пароль оставляет имя; завершение — кабинет с пробным сроком.
3 — подтверждение почты (4c): код приходит НАСТОЯЩИМ письмом; неверный код — «Неверный код.
Осталось попыток: N» и набранный код в поле; «Отправить код повторно» — новое письмо, обе формы
работают; обновление страницы на шаге возвращает к началу пути (D-08).
4 — восстановление пароля (4d): код из письма на существующий ящик → новый пароль → `/login` с
плашкой «Пароль успешно изменён. Войдите с новым паролем.» → вход новым паролем.
5 — возврат из-под чужой личности (D-12): «ВЕРНУТЬСЯ В АДМИНА» → адрес `/admin`, вкладка админки,
полосы нет, cookie без признака действующего лица.
6 — менеджер паролей (RESEARCH Находка 7): пять наблюдений в Chrome и Firefox, база сравнения —
вход и завершение регистрации с выключенным JavaScript; регрессия относительно пути без JS
выносится владельцу, а не глушится.
7 — вёрстка карточки авторизации (A4): подзаголовок сразу под брендом, промежутки как до фазы,
индикатор запроса у кнопки; 375px и десктоп.
Разделы `## Summary` (total 7, pending 7) и `## Gaps` (`[none yet]`). Машинной улики в файле нет;
если исполнитель её снимает, она идёт отдельным разделом `## Машинная улика обхода` БЕЗ
подзаголовков `### Отметка`, а `result` и таблицы отметок остаются человеку.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning -q</automated>
    <fails_when>non-zero exit with a failing rule other than `test_the_machine_readable_progress_is_derived_from_the_roadmap` (tracking-owned, recorded verbatim in the SUMMARY if it alone fails), or "no tests ran"</fails_when>
    <automated>uv run pytest tests/test_planning/test_the_walkthrough_cannot_self_certify.py -q</automated>
    <fails_when>non-zero exit, or "failed" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
    <fails_when>non-zero exit, or output lacking `"ok": true` (the ledger failed to parse after the change)</fails_when>
  </verify>

  <acceptance_criteria>
- Окно 63 решено по замеру: `node .claude/gsd-core/bin/gsd-tools.cjs windows status | python3 -c "import json,sys; d=json.load(sys.stdin); e=[x for x in d['ledger']['entries'] if x['id']==63][0]; t=open('.planning/WINDOWS.md',encoding='utf-8').read(); ok=(e['status']=='waived' and all(k in e['reason'] for k in ('D-08','D-15','D-01'))) or (e['status']=='open' and 'Летопись окна 63 (Фаза 14, план 14-07)' in t); sys.exit(0 if ok else 1)"` — код 0.
- `grep -c '^checks_declared: 7$' .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` возвращает `1`; `grep -c '^## Проверка [1-7] — ' .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` возвращает `7`; `grep -c '^### Отметка о закрытии проверки [1-7]$' .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` возвращает `7`.
- `grep -c '^status: testing$' .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` возвращает `1`; `grep -c '^result: \[pending\]$' .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` возвращает `7`.
- `grep -c 'SMTP' .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` не меньше `1`; `grep -c 'ОСТАНАВЛИВАЕТСЯ' .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` не меньше `1`.
  </acceptance_criteria>

  <done>
Окно 63 решено замером и командой реестра; обход критерия 4 готов к приёмке с выписанными
предусловиями настоящих писем, отметки ждут человека.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| записи планирования → верификатор и аудит вехи | критерии, требования и реестр окон читаются как контракт приёмки |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-14 | Repudiation (окно закрыто без замера) | окно 63 `.planning/WINDOWS.md` | low | mitigate | Две команды замера с выводом в SUMMARY до команды реестра; ветка «остаётся открытым» с летописью остатка; критерий приёмки проверяет состояние через журнал JSON |
| T-14-19 | Repudiation (самозаверение обхода) | `14-UAT.md` | low | mitigate | Нетерминальный `status`, пустые таблицы отметок, `result: [pending]`; правило `test_the_walkthrough_cannot_self_certify.py` |
| T-14-20 | Tampering (преждевременная отметка требований) | SIGN-01…03 | low | mitigate | План флажки и ячейки не трогает; `test_requirement_completion_follows_verification.py` краснеет на отметке раньше верификации |
</threat_model>

<verification>
- `uv run pytest tests/test_planning -q` — после каждой задачи (исполнители обычно отбирают суиту
  без `tests/test_planning/`, и только этот прогон видит дефекты записей).
</verification>

<success_criteria>
1. Летописи у критериев 2 и 3, строки Research, SIGN-03 и рамки вехи стоят; тексты не переписаны.
2. Окно 63 решено замером и штатной командой реестра.
3. `14-UAT.md` в размеченной форме с предусловиями D-02; отметки ждут человека.
</success_criteria>

<output>
Create `.planning/phases/14-avtorizatsiya-na-htmx/14-07-SUMMARY.md` when done.
</output>
