---
phase: 14-avtorizatsiya-na-htmx
plan: 07
subsystem: testing
tags: [records, roadmap, requirements, broken-windows, uat, chronicle]

requires:
  - phase: 14-avtorizatsiya-na-htmx
    provides: "14-01…14-06 — десять переведённых обработчиков, `NOT_YET_CONVERTED_COUNT = 0` именованным нулём, гейт критерия 3 с тремя двухшаговыми контролями, запись возврата в `OWN_RESPONSE_EXITS` (14 записей), `NOTICE_WRITE_PLACES = 0`"
  - phase: 13-master-podklyucheniya-telegram-po-qr-na-fragmentah
    provides: "форма летописи критериев (идиома D-30/D-32) и размеченная форма артефакта ручного обхода `13-UAT.md`"
provides:
  - "летопись критерия 2 в `.planning/ROADMAP.md` — девять форм замером и десятая, именованный ноль отставания"
  - "летопись критерия 3 — три успеха `HX-Redirect`, три ветки возврата, второй отправитель `HX-Location` вне модуля авторизации, граница гейта"
  - "летопись строки Research — три устаревшие посылки разведки"
  - "летопись SIGN-03 в `.planning/REQUIREMENTS.md` (флажок и ячейка `Pending` не тронуты)"
  - "летопись рамки вехи и ячейки обоснования в `.planning/PROJECT.md`"
  - "окно 63 реестра переведено `open` → `waived` штатной командой ПОСЛЕ замера"
  - "`14-UAT.md` — артефакт ручного обхода критерия 4 в размеченной форме, отметки ждут человека"
affects: [14-verification, 15-uprochnenie]

actuals:
  tokens: 13400
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - "летопись ставится рядом с текстом, который устарел, и называет ЗАМЕР, которым проверена (идиома D-30/D-32)"
    - "состояние окна реестра меняется только командой реестра и только после замера; таблица и журнал JSON руками не правятся"

key-files:
  created:
    - .planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md
  modified:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
    - .planning/WINDOWS.md

key-decisions:
  - "SIGN-01…03 НЕ отмечены Complete, хотя `requirements.ready-ids` объявил все три готовыми: отметку ставит закрытие фазы после верификации (собственный must-have плана; `test_requirement_completion_follows_verification.py` краснеет на отметке раньше вердикта `passed`)"
  - "Окно 63 переведено в `waived`, потому что ЗАМЕР совпал с условием снятия (14 мест голого 403 / 14 записей реестра / 0 в ожидании владельца), а не потому, что работа кончилась"
  - "Расхождения между сводкой 14-06 («второй отправитель живёт в `app/main.py`») и must-have плана («отказ зависимости на четырёх шагах восстановления») НЕТ: замером это одна и та же цепочка, и летопись называет все три её звена"
  - "Машинной улики в `14-UAT.md` не ставится вовсе: всё измеримое уже стои́т в суите, а раздел улики рядом с отметками размывал бы границу приёмки"

patterns-established:
  - "Летопись, называющая ЧИСЛО, называет и команду замера, которой число снято, — читатель может повторить замер, не веря прозе"

requirements-completed: [SIGN-01, SIGN-02, SIGN-03]

coverage:
  - id: D1
    description: "Летописи критериев 2 и 3 и строки Research стоят в разделе Фазы 14 ПОСЛЕ перечня критериев и ДО строки `**Plans**`; тексты критериев не переписаны"
    requirement: SIGN-03
    verification:
      - kind: other
        ref: "grep -c '^\\*\\*Летопись критерия 3: .*(Фаза 14, план 14-07' .planning/ROADMAP.md → 1 (то же для критерия 2 и строки Research)"
        status: pass
      - kind: other
        ref: "git diff $(git merge-base HEAD master) -- .planning/ROADMAP.md | grep -c '^-  [1-4]\\. ' → 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Летопись SIGN-03 стои́т под требованием с отступом; флажок `[ ]` и ячейка `Pending` таблицы трассировки НЕ тронуты"
    requirement: SIGN-03
    verification:
      - kind: other
        ref: "grep -c '^  \\*\\*Летопись SIGN-03: .*(Фаза 14, план 14-07' → 1; grep -c '^- \\[ \\] \\*\\*SIGN-03\\*\\*' → 1; grep -c '| SIGN-03 | Phase 14 | Pending |' → 1"
        status: pass
      - kind: unit
        ref: "tests/test_planning/test_requirement_completion_follows_verification.py (в прогоне 44 passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Летопись рамки вехи и ячейки обоснования в `.planning/PROJECT.md`; ячейка исхода («— Pending») не тронута"
    verification:
      - kind: other
        ref: "grep -c 'Летопись (Фаза 14, D-10)' .planning/PROJECT.md → 2"
        status: pass
    human_judgment: false
  - id: D4
    description: "Окно 63 переведено `open` → `waived` штатной командой реестра ПОСЛЕ двух замеров; причина называет D-08, D-15 и D-01"
    verification:
      - kind: other
        ref: "gsd-tools windows status → ledger.entries[63].status == 'waived' и все три решения в reason; open_count 76 → 75, waived_count 2 → 3"
        status: pass
      - kind: other
        ref: "gsd-tools windows status → \"ok\": true (реестр разбирается после правки)"
        status: pass
    human_judgment: false
  - id: D5
    description: "`14-UAT.md` в размеченной форме: `checks_declared: 7` == 7 разделов `## Проверка N` == 7 ПУСТЫХ таблиц `### Отметка`; `status: testing`, семь `result: [pending]`"
    verification:
      - kind: unit
        ref: "tests/test_planning/test_the_walkthrough_cannot_self_certify.py — 5 passed (три счёта сходятся, изъятия 11 и 12 не выросли)"
        status: pass
      - kind: other
        ref: "grep -c '^checks_declared: 7$' → 1; '^## Проверка [1-7] — ' → 7; '^### Отметка о закрытии проверки [1-7]$' → 7; '^status: testing$' → 1; '^result: \\[pending\\]$' → 7"
        status: pass
    human_judgment: false
  - id: D6
    description: "Ручной обход критерия 4 пройден человеком: вход, регистрация, подтверждение почты НАСТОЯЩИМ письмом, восстановление пароля, возврат из-под чужой личности, менеджер паролей, вёрстка карточки"
    requirement: SIGN-01
    verification: []
    human_judgment: true
    rationale: "Предмет плана — ФОРМА артефакта обхода, а не его прохождение. Семь проверок `14-UAT.md` закрываются глазами на приёмке фазы (D-02): доставка письма, рантайм подмены, заголовок вкладки, эвристики менеджера паролей и визуальное суждение сервером не измеряются"

duration: 50min
completed: 2026-09-22
status: complete
plan_head_before: cb9654f5047893f21a1866a17d9f8039fa378b71
commits: 4
---

# Phase 14 Plan 07: Летописи записей, окно 63 замером, артефакт ручного обхода — Summary

**Записи фазы приведены к сделанному без переписывания текстов: у критериев 2 и 3, у строки Research, у SIGN-03 и у рамки вехи стоят летописи, называющие замер; окно 63 реестра переведено в `waived` штатной командой ПОСЛЕ двух совпавших замеров (14 мест голого 403 / `14 0 14`); ручной обход критерия 4 получил `14-UAT.md` в размеченной форме с предусловиями настоящих писем и правилом останова D-02, где `result` и семь таблиц отметок ждут человека.**

## Performance

- **Duration:** ~50 min
- **Started:** 2026-09-22T19:40Z (приблизительно — момент диспетчеризации исполнителем не снят; первый твёрдый якорь — журнал плана, созданный в 20:25:55Z)
- **Completed:** 2026-09-22T20:31Z
- **Tasks:** 2
- **Files modified:** 5 (1 новый, 4 изменённых) — РОВНО файлы, объявленные планом; шестой (этот файл) — выход плана

## Accomplishments

- **ROADMAP, три летописи** стоят в разделе Фазы 14 после перечня критериев и до строки `**Plans**`, в форме летописей Фазы 13. Тексты критериев 1–4 не тронуты ни байтом (критерий приёмки печатает `0` снятых строк критериев в диффе от базы ветки).
- **REQUIREMENTS, летопись SIGN-03** — с отступом в два пробела по образцу летописи FETCH-02. Флажок `[ ]` и ячейка `Pending` таблицы трассировки НЕ тронуты (см. §Отметка требований отложена).
- **PROJECT** — летопись под пунктом рамки «Авторизация — исключение по МЕХАНИЗМУ, а не по охвату» и та же летопись одной фразой в ячейке обоснования строки решений; ячейка исхода («— Pending») не тронута: её правит закрытие вехи.
- **Окно 63** переведено `open` → `waived` командой реестра `windows waive 63` с причиной-летописью. Таблица и журнал JSON руками не правились; диффом это 7 строк: два счётчика шапки, метка времени, ячейка состояния строки, три поля записи журнала. Проза ниже журнала цела.
- **`14-UAT.md`** создан в размеченной форме: `checks_declared: 7`, семь разделов проверок, семь ПУСТЫХ таблиц отметок, `status: testing`, семь `result: [pending]`. Семь предусловий (П-1…П-7) и правило останова D-02 выписаны дословно.

## Task Commits

1. **Задача 1: летописи критериев 2 и 3, строки Research, SIGN-03 и рамки вехи** — `94ecef3d` (docs)
2. **Задача 2: окно 63 замером и командой реестра; артефакт ручного обхода** — `5079ab7e` (docs)

**Plan metadata:** `fee350ac` (docs — этот файл); трекинг STATE/ROADMAP/state.json — `2673b3a7`.

**`commits: 4` ИЗМЕРЕНО, а не рассказано:** `git rev-list --count cb9654f5..HEAD` = `4` на коммите трекинга, в который довложен и этот раздел (аменд собственного неотправленного коммита). База `plan_head_before` записана во frontmatter тем же прибором, которым её прочтёт `/gsd-verify-work`.

_План `type: execute`, обе задачи — записи. `task.is-behavior-adding` для них ложно (ни одного исходного файла вне `.planning/`), поэтому пары `test(14-07)` → `feat(14-07)` не изготовлялось: RED на прозе был бы вакуумом._

## Замеры, поставленные этим планом

Летопись, называющая число, называет и команду. Каждый замер ниже снят на дереве плана 14-07 и приведён дословно.

| Замер | Команда | Вывод |
|---|---|---|
| Девять форм авторизации | `grep -rn 'form_wrapper(' app/templates/auth/` | девять вызовов: `login_step`, `register_step`, `register_verify_step` (2), `register_complete_step`, `forgot_password_step`, `forgot_password_verify_step` (2), `forgot_password_reset_step` |
| Десятая форма | `grep -n 'form_wrapper' app/templates/base.html` | `:89` — `{% call form_wrapper(action='/impersonation/stop') %}` |
| Именованный ноль | `grep -n 'NOT_YET_CONVERTED_COUNT = ' tests/test_pages/test_htmx_gates.py` | `847:NOT_YET_CONVERTED_COUNT = 0` |
| Выходы полной перезагрузки и перехода | `grep -n 'redirect_internal(\|await respond(request, redirect=' app/pages/auth.py` | `:273` `/dashboard`, `:617` `/dashboard`, `:716` `respond /dashboard`, `:744` `redirect_internal /login`, `:748` `respond /admin`, `:1115` `/login?notice=password_reset_done` |
| Четыре шага восстановления под зависимостью | `grep -n 'forbid_when_impersonating' app/pages/auth.py` | `:790`, `:886`, `:966`, `:1058` |
| Второй отправитель `HX-Location` | `grep -n 'HtmxRefusal\|location_response' app/main.py` | `:224` `@app.exception_handler(HtmxRefusal)`, `:231` `return location_response(exc.location)` |
| Канал уведомления вне якоря | `grep -n 'include\|auth-step' app/templates/auth_base.html` | `:62` `notice_area.html`, `:67` `<div id="auth-step">` — область уведомления ВЫШЕ якоря, то есть вне него |
| **Окно 63, замер 1** | `grep -rn "Response(status_code=403)" app/pages --include=*.py \| grep -v "^app/pages/htmx.py" \| wc -l` | `14` |
| **Окно 63, замер 2** | `uv run python -c "…OWN_RESPONSE_EXITS…DECISION_WAITS_FOR_THE_OWNER…_converted_with_own_response…"` | `14 0 14` |

Оба замера окна совпали с условием снятия (D-01), поэтому окно переведено `waived`, а не оставлено `open` с летописью остатка. Реестр после правки разбирается: `windows status` → `"ok": true`, `open_count` 76 → 75, `waived_count` 2 → 3.

## Отметка требований отложена НАМЕРЕННО

`requirements.ready-ids` на этом плане отвечает дословно:

```
3/3 requirement(s) ready to mark complete
```

**`requirements.mark-complete` НЕ вызывался.** Основание — собственный must-have плана: «флажок `[ ]` и ячейка `Pending` не тронуты — отметку ставит закрытие фазы после верификации». Оно не декоративно: `tests/test_planning/test_requirement_completion_follows_verification.py::test_no_requirement_is_marked_complete_before_its_phase_verification_passed` краснеет на требовании со статусом `Complete`, у фазы которого нет `*-VERIFICATION.md` с вердиктом `passed`, — а у Фазы 14 его пока нет. Фаза 12 на этом уже спотыкалась и откатывала отметку.

Проверено после всех правок: `grep -n 'SIGN-0' .planning/REQUIREMENTS.md` показывает `- [ ] **SIGN-01**`, `- [ ] **SIGN-02**`, `- [ ] **SIGN-03**` и три строки `| SIGN-0N | Phase 14 | Pending |`.

## Расхождение источников, о котором предупреждал оркестратор, — разрешено замером

Сводка 14-06 называет второго отправителя `HX-Location` через `app/main.py` (обработчик `HtmxRefusal`), а must-have этого плана — через «отказ зависимости `forbid_when_impersonating` на четырёх шагах восстановления». **Противоречия нет: это одна цепочка, названная с разных концов.** Замер прошёл её целиком: признак читает `forbid_when_impersonating` (`app/dependencies.py`, `IMPERSONATION_REFUSED_LOCATION`), отказ едет исключением `HtmxRefusal` (`app/pages/htmx.py`), заголовок печатает `location_response` в обработчике исключения (`app/main.py:224-231`). Шапка гейта критерия 3 (`tests/test_pages/test_htmx_gates.py:6155-6167`) называет ровно эти три звена. Летопись выписывает все три, поэтому читатель, пришедший с любого конца, найдёт своё.

## Files Created/Modified

- `.planning/ROADMAP.md` — три летописи в разделе Фазы 14 (критерий 2, критерий 3, строка Research); тексты критериев и строка `**Research**` не переписаны
- `.planning/REQUIREMENTS.md` — летопись под SIGN-03 с отступом; флажки и таблица трассировки не тронуты
- `.planning/PROJECT.md` — летопись у рамки вехи и в ячейке обоснования строки решений
- `.planning/WINDOWS.md` — окно 63 `open` → `waived` командой реестра
- `.planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md` — новый артефакт ручного обхода критерия 4 (267 строк)

## Decisions Made

- Решения, отданные планом на усмотрение исполнителя, приняты так, как план их записал: содержание пяти абзацев летописи, форма предусловий обхода, состав семи проверок.
- Машинной улики в `14-UAT.md` не ставится вовсе. План разрешал отдельный раздел `## Машинная улика обхода`, но всё измеримое уже стои́т в суите и в сводках 14-01…14-06, а раздел улики рядом с пустыми отметками размывал бы границу «улика ≠ приёмка».
- В `14-UAT.md` заведено предусловие П-6 (возможность выключить JavaScript), которого план поимённо не называет. Без него пункт 5 Находки 7 — «база сравнения: повторить с выключенным JS» — неисполним, и регрессия менеджера паролей была бы неотличима от предсуществующего поведения.

## Deviations from Plan

### Дополнения (предмета плана не меняли)

**1. [Rule 2 — недостающее критичное] Предусловие П-6 «возможность выключить JavaScript»**
- **Found during:** задача 2
- **Issue:** проверка 6 требует базы сравнения — входа и завершения регистрации по пути без JS (Находка 7, пункт 5). Перечень предусловий плана её не называет, и обход без выключаемого JS дал бы наблюдение, из которого вывод о регрессии не следует.
- **Fix:** предусловие выписано строкой таблицы П-6 рядом с П-5 (чистые профили Chrome и Firefox).
- **Files modified:** `.planning/phases/14-avtorizatsiya-na-htmx/14-UAT.md`
- **Verification:** `tests/test_planning/test_the_walkthrough_cannot_self_certify.py` — 5 passed; три счёта остались 7 / 7 / 7
- **Committed in:** `5079ab7e`

**2. [Оформление] Строка `**Plans**` раздела Фазы 14 приведена к опознаваемой форме прозы**
- **Found during:** шаг записи трекинга (после задач 1 и 2)
- **Issue:** строка стояла в форме «7 планов», которую `test_the_prose_plan_counts_agree_with_the_marks` пропускает МОЛЧА — раздел был вне правила согласия прозы с отметками. План говорит, что задача 1 эту строку не правит, и задача 1 её не тронула.
- **Fix:** строка приведена к форме «7 планов, из них 7 исполнено» ПОСЛЕ того, как `roadmap.update-plan-progress` перевёл отметку плана 14-07 в `[x]`, с оговоркой поколения по идиоме D-30/D-32 — как у одноимённой строки Фазы 13. Раньше флипа отметки та же правка покраснила бы правило: проза сказала бы 7 при шести отметках.
- **Verification:** `uv run pytest tests/test_planning -q` — зелено после правки
- **Committed in:** коммит записи трекинга

**3. [Замер] Расхождение источников о втором отправителе `HX-Location` расхождением не оказалось**
- Разобрано выше отдельным разделом. Правки не потребовалось; летопись называет все три звена цепочки.

---

**Total deviations:** 3 (1 дополнение предусловием, 1 оформление строки прозы, 1 замер). Ни одного файла сверх пяти, объявленных планом, плюс штатные файлы трекинга. **Impact:** предмет плана не менялся; дополнение делает исполнимой проверку, которая иначе не давала бы вывода.

## Issues Encountered

- Ничего не покраснело. `uv run pytest tests/test_planning -q` даёт 44 passed на базе, после задачи 1 и после задачи 2 — одно и то же число: артефакт обхода вошёл в обе вселенные правил самозаверения (несущего и правила трёх счётов), поэтому объявленные изъятия 11 и 12 не сдвинулись и сдвигать их не пришлось.
- `test_the_machine_readable_progress_is_derived_from_the_roadmap` (правило трекинга, которое план разрешает записать дословно при отказе) НЕ отказало: `progress.completed_plans` сдвинут рукой до прогона, как требует память проекта.

## Known Stubs

None. Каждая летопись называет замер, которым проверена; ни одно число не перенесено из прогноза плана. `14-UAT.md` пуст в отметках НАМЕРЕННО — это его штатное состояние до приёмки, а не заглушка: `status: testing` нетерминален, и правило самозаверения такой артефакт не судит.

## Threat Flags

None. Новых поверхностей нет — план не трогает ни строки исходного кода.

- **T-14-14** (окно закрыто без замера): два замера сняты ДО команды реестра и приведены дословно выше; ветка «остаётся открытым» была готова и не понадобилась; состояние проверено через журнал JSON, а не по таблице.
- **T-14-19** (самозаверение обхода): `status: testing` нетерминален, семь таблиц отметок пусты, семь `result: [pending]`; правило `test_the_walkthrough_cannot_self_certify.py` зелено на всех пяти утверждениях.
- **T-14-20** (преждевременная отметка требований): `mark-complete` не вызывался; флажки и ячейки проверены после всех правок.

## User Setup Required

None — внешних служб план не настраивает. Предусловия ручного обхода (SMTP стенда, ящики, админ, два браузера) выписаны в `14-UAT.md` и обеспечиваются к моменту приёмки фазы, а не исполнения плана.

## Next Phase Readiness

- **Код фазы закончен планом 14-06, записи — этим планом.** Все семь планов исполнены; фаза НЕ закрыта: закрытие ставит `phase.complete` после верификации.
- **Что ждёт верификацию:** `/gsd-verify-work 14` — вердикт; затем отметка SIGN-01…03 и закрытие фазы. Ручной обход `14-UAT.md` — на приёмке, с настоящими письмами (D-02).
- **Что уходит дальше:** ручной обход пункта 9 сводного перечня (атрибуты, собранные Jinja-условием) и `hx-push-url` форм авторизации — Фаза 15 (D-08, D-13 Фазы 11).

---
*Phase: 14-avtorizatsiya-na-htmx*
*Completed: 2026-09-22*

## Self-Check: PASSED

- Созданные файлы на диске: `14-UAT.md` — FOUND; `14-07-SUMMARY.md` — FOUND.
- Коммиты в истории: `94ecef3d` — FOUND; `5079ab7e` — FOUND; `fee350ac` — FOUND; `2673b3a7` — FOUND.
- Число коммитов плана измерено: `git rev-list --count cb9654f5..HEAD` = `4`.
- Требования не отмечены: `grep -n 'SIGN-0' .planning/REQUIREMENTS.md` — три `- [ ]` и три `| Phase 14 | Pending |`.
- Цепь истории цела: `grep -c 'Last activity' .planning/STATE.md` = `38` (было 37 + одна новая); `git diff` STATE.md прочитан построчно, ни одной перенесённой строки верб не потерял и не переписал внутри цитат.
- `uv run pytest tests/test_planning -q` — 44 passed после задачи 1, после задачи 2 и после правок трекинга.
