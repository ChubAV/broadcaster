---
phase: quick-260825-of5-accounts-list-matches-canonical-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260825-of5-stranitsa-accounts-ne-sootvetstvuet-shab/260825-of5-NOTES.md
  - app/templates/accounts/list.html
  - app/templates/accounts/partial_cards.html
  - app/templates/accounts/partials/sync_status_card.html
  - app/static/css/app.css
  - tests/test_pages/test_responsive_markup.py
  - tests/test_routes/test_wa_sync_status.py
autonomous: true
requirements:
  - QUICK-ACCOUNTS-LAYOUT
estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Список `/accounts` рисуется КАРТОЧНОЙ СЕТКОЙ на всех ширинах — как раздел `isAccounts` канонического макета (`design/new_broadcaster_design.unpacked.html:878`), а не карточкой-обёрткой с шапкой колонок и строками-таблицами."
    - "Расхождения макета и реализации перечислены ПОИМЁННО в `260825-of5-NOTES.md`: у каждой оси — что говорит макет (с номером строки распакованного файла), что рисует приложение (с `файл:строка`) и вердикт «исправляется / сознательное отступление / вне объёма». Утверждений без адреса в таблице нет."
    - "Карточка аккаунта несёт ровно те элементы, что и карточка макета (строки 880-901): тонированная иконка канала 40px, подпись канала, mono-идентификатор, точка состояния справа, mono-блок «ключ — значение», ряд действий внизу."
    - "Тон иконки канала берётся из ЕДИНСТВЕННОГО существующего объявления тонов канала (`app/static/css/app.css:1386-1394`, заведено дашбордом). Второй копии тех же трёх тонов в файле не появляется."
    - "Все ТРИ поверхности раздела перевёрстаны ОДНИМ шагом и остаются взаимозаменяемыми: списочная страница, порция бесконечной прокрутки и блок подмены по опросу статуса. Разъехавшаяся поверхность не роняет страницу — она проявляется только после прокрутки или после первого опроса."
    - "Опрос статуса остаётся ТОЛЬКО на синхронизирующейся карточке (T-06-01): переверстка не переносит запрос обновления на каждую карточку сетки."
    - "Якорь подмены `account-row-{id}` сохранён дословно и стоит на ТОМ ЖЕ элементе, что и запрос обновления статуса; элемент заменяется целиком."
    - "Панель подтверждения удаления по-прежнему эмитится ДВУМЯ файлами из трёх и лежит ВНЕ заменяемого элемента (асимметрия Плана 11, T-11-04). Инвентаризации `ROW_DELETE_PLACES = 12`, `MODAL_IMPORTERS`, `MODAL_EVENT_NAMES`, `MODAL_PLACES` остаются на прежних числах и правок не требуют."
    - "Все ТРИ пути действия сохранены во всех ветках статуса: вход на экран групп аккаунта (D-02), удаление аккаунта и повтор синхронизации у ветки ошибки. Задача меняет ВИД раздела, а не отбирает у пользователя путь."
    - "Состав показанных величин не сокращён: шесть значений, стоявших колонками, названы своими ключами в карточке. Это сознательное отступление от макета — у него в карточке две строки, но его две строки суть его данные-примеры, а шесть значений приложения суть настоящие данные пользователя."
    - "Ни одного инлайнового hex и ни одного utility-класса в разметке раздела: оформление живёт классами в `app/static/css/app.css` на существующих переменных `:root`, новых цветовых констант не заводится. Цвет состояния, приходящий у макета из данных (`ac.color`), НЕ переносится — величины такого рода в модели `MessengerAccount` не существует."
    - "Выход раздела из обхода по строке-таблице ОБЪЯВЛЕН, а не молчалив: `MIGRATED_SECTIONS`, `ROWHEAD_PAGES`, `ROW_TEMPLATES_WITHOUT_HEADER` и оба объявленных числа опущены в ТОМ ЖЕ коммите с названной причиной — тем же приёмом, каким планы 02-07, 03-08, 04-05, 04-07 и задача 260825-m0b выводили свои разделы."
    - "Раздел не выпадает из проверок вместе со снятыми строками параметризации: у него появляется СВОЙ положительный примитив, закреплённый тестом, — ЧЕТВЁРТЫМ по образцу `test_schedules_summary_list_is_card_based`, `test_account_groups_list_is_card_based` и `test_ads_list_is_card_based`."
    - "Обещание «понятно, что означает каждое значение», которое держали подписи ячеек, переехало на ключи карточки и проверяется на ВСЕХ ТРЁХ поверхностях, а не только на списочной странице."
    - "Подзаголовок раздела взят из макета (`design/new_broadcaster_design.unpacked.html:1486`) — блок `page_subtitle` шелла переопределён, как это уже сделали пять других страниц."
    - "Мастера подключения (`accounts/connect_*.html`) и экран групп аккаунта не тронуты: они перевёрстаны Фазами 3 и 8 и в расхождение не входят."
    - "Разделы «История», «Тарифы» и админка сохраняют примитив строки-таблицы: `components/table.html` не правится, его прочие потребители не трогаются."
    - "`new_broadcaster_design.html` и `design/new_broadcaster_design.unpacked.html` не изменены ни одним байтом."
  artifacts:
    - ".planning/quick/260825-of5-stranitsa-accounts-ne-sootvetstvuet-shab/260825-of5-NOTES.md"
    - "app/templates/accounts/list.html"
    - "app/templates/accounts/partial_cards.html"
    - "app/templates/accounts/partials/sync_status_card.html"
    - "app/static/css/app.css"
    - "tests/test_pages/test_responsive_markup.py"
    - "tests/test_routes/test_wa_sync_status.py"
  key_links:
    - "Три поверхности раздела ↔ одна форма карточки: `accounts/list.html`, `accounts/partial_cards.html` и `accounts/partials/sync_status_card.html` рисуют ОДНУ карточку. Расхождение не роняет страницу — оно проявляется только после прокрутки или после первого опроса, и именно так уже терялась колонка даты подключения (WR-03)."
    - "Якорь `account-row-{id}` ↔ `hx-swap=\"outerHTML\"` блока подмены: идентификатор и запрос обновления обязаны стоять на ОДНОМ элементе. Перенос идентификатора на обёртку рвёт подмену молча."
    - "Панель подтверждения ↔ заменяемый элемент: панель лежит СНАРУЖИ, иначе после первого опроса в документе окажутся две панели с одним идентификатором (T-11-04)."
    - "Сентинел бесконечной прокрутки ↔ контейнер сетки: сентинел обязан остаться ПОСЛЕДНИМ элементом ТОГО ЖЕ контейнера, что и карточки, и растянуться на всю строку сетки — иначе он встанет очередной колонкой рядом с карточкой и прочтётся как ещё один аккаунт."
    - "Тон канала ↔ `[data-upbadge][data-channel]`: единственное объявление трёх тонов канала в проекте. Вторая копия разъедется молча."
    - "Инвентаризации строки-таблицы ↔ разметка: `ROWHEAD_PAGES`, `ROW_TEMPLATES_WITHOUT_HEADER` и `MIGRATED_SECTIONS` обязаны опуститься ТЕМ ЖЕ коммитом, что и снятие шапки колонок, иначе раздел остаётся либо со строками без шапки, либо с шапкой без строк."
---

<objective>
Привести раздел «Аккаунты» (`/accounts`) к каноническому макету: карточная сетка вместо
карточки-обёртки с шапкой колонок и строками-таблицами.

Purpose: раздел получил сегодняшнюю форму Планом 01-03, когда ОДНА форма списочной страницы —
карточка-обёртка, шапка колонок, строка-таблица — была применена ко всем пяти разделам сразу.
Макет описывает для «Аккаунтов» КАРТОЧНУЮ СЕТКУ (`design/new_broadcaster_design.unpacked.html:877-903`).
Три соседних раздела из этой пятёрки уже вышли (планы 02-07, 03-08, задача 260825-m0b);
«Аккаунты» — ЧЕТВЁРТЫЙ и ПОСЛЕДНИЙ: после него перечень разделов на примитиве строки-таблицы
пустеет, и это обязано быть названо, а не обнаружено молчаливым пропуском параметризации.

Output: аудит расхождений с адресами обеих сторон; карточная сетка на трёх поверхностях раздела;
раздел `app/static/css/app.css` на существующих переменных; объявленный выход из инвентаризаций
строки-таблицы и собственный положительный примитив взамен.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/source/broadcaster/CLAUDE.md
@/source/broadcaster/.planning/STATE.md
@/source/broadcaster/design/README.md

Прецедент — задача 260825-m0b, тот же класс работы для раздела «Объявления».
Читать ДО правок, форма шагов повторяется дословно:
@/source/broadcaster/.planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md

Коммиты прецедента (читать `git show`, а не пересказ):
- `78ccbe7` — карточная сетка вместо строки-таблицы + выход из инвентаризаций ОДНИМ коммитом
- `eba22ba` — падающие тесты перед правкой
- `4587554` — вторая половина работы
- `d490dbf` — документные артефакты

Стороны расхождения:
@/source/broadcaster/app/templates/accounts/list.html
@/source/broadcaster/app/templates/accounts/partial_cards.html
@/source/broadcaster/app/templates/accounts/partials/sync_status_card.html
@/source/broadcaster/app/pages/accounts.py

Эталон формы карточки (переносить приём, а не текст):
@/source/broadcaster/app/templates/ads/includes/ad_card.html
@/source/broadcaster/app/templates/ads/list.html
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Аудит расхождений макета и реализации</name>
  <files>.planning/quick/260825-of5-stranitsa-accounts-ne-sootvetstvuet-shab/260825-of5-NOTES.md</files>
  <read_first>
    - `design/README.md` — почему исходник макета грепать бесполезно и какой файл читать
    - `design/new_broadcaster_design.unpacked.html:877-903` — раздел `isAccounts` целиком
    - `design/new_broadcaster_design.unpacked.html:1486` — заголовок, подзаголовок и действие раздела
    - `design/new_broadcaster_design.unpacked.html:1809-1821` — данные-примеры аккаунтов
    - `.planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md` — форма таблицы аудита, которую задача повторяет
    - `app/templates/accounts/list.html`, `app/templates/accounts/partial_cards.html`, `app/templates/accounts/partials/sync_status_card.html`, `app/pages/accounts.py`
  </read_first>
  <action>
Написать аудит расхождений раздела «Аккаунты» против канонического макета по форме
`260825-m0b-NOTES.md` — тот файл читается как образец структуры, а не копируется текстом.

Сторона макета цитируется по `design/new_broadcaster_design.unpacked.html` с номером строки,
сторона приложения — по файлам репозитория в форме `файл:строка`. Утверждение без адреса в
таблицу не ставится: аудит существует для того, чтобы следующий читатель мог проверить каждую
строку сам, а не поверить автору.

Разделы файла — те же четыре, что у прецедента:

1. «Откуда взялось расхождение» — происхождение, а не оправдание. Раздел получил форму
   списочной страницы Планом 01-03, когда одна форма была применена ко всем пяти разделам
   сразу; Фаза 6 своим объёмом брала подключение аккаунтов и опрос статуса, а формой списка не
   занималась. Сказать это прямо и сразу оговорить, что вердикты ниже поставлены по макету, а
   не по истории.

2. Таблица расхождений — по одной строке на ось, колонки «Ось | Макет | Приложение | Вердикт».
   Оси перечислены ниже; ни одну не пропускать, у каждой вердикт ровно один из трёх:
   «исправляется — задача N», «сознательное отступление», «вне объёма».

   - Оболочка страницы и шапка раздела. Макет: заголовок, подзаголовок и действие раздела
     (строка 1486). Приложение: заголовок и действия рендерит шелл, подзаголовок не
     переопределён вовсе. Подзаголовок — исправляется задачей 3; блок шелла уже переопределяют
     пять других страниц, так что механизм существует и заводить нечего. Число действий шапки
     (у макета одно, у приложения три канала) — вне объёма: свести три маршрута подключения в
     одно действие значит завести экран выбора канала, то есть функциональную работу, а не
     вёрстку.
   - Раскладка списка. Макет: сетка `repeat(auto-fill, minmax(300px,1fr))` с зазором 13px
     (строка 878). Приложение: карточка-обёртка, шапка колонок и строки-таблицы. Исправляется.
   - Форма единицы списка. Макет: карточка строки 880 — отступ 20px, радиус 16px, колонка с
     зазором 15px. Приложение: строка-таблица из восьми ячеек. Исправляется.
   - Шапка карточки. Макет: тонированная плашка канала 40px с иконкой 19px (строка 882),
     подпись 14px/600 (строка 888), mono-подпись 11px под ней (строка 889), точка состояния 9px,
     прижатая вправо, с пульсацией у неподключённого (строка 891). Приложение: иконка канала,
     подпись канала и mono-идентификатор внутри ПЕРВОЙ ЯЧЕЙКИ строки, состояние — бейдж-пилюля
     в шестой ячейке. Форма исправляется. Имя и телефон аккаунта — сознательное отступление:
     таких полей у модели `MessengerAccount` нет, и выдумывать их нельзя; роль имени играет
     подпись канала, роль второй строки — идентификатор аккаунта.
   - Состав показанных величин. Макет: две строки, «статус» и «групп» (строки 894-895).
     Приложение: шесть значений — группы, расписания, успешность, последняя отправка, дата
     подключения, статус. Сознательное отступление, и причина та же, что у оси 5 прецедента:
     две строки макета суть его данные-примеры, а шесть значений приложения суть настоящие
     данные пользователя. Задача меняет ВИД раздела, а не отбирает у пользователя показанные
     величины — все шесть переезжают в блок «ключ — значение» карточки.
   - Цвет состояния. Макет красит и точку, и значение статуса величиной `ac.color` (строки 891,
     894), приходящей из данных-примеров. Сознательное отступление: величины такого рода в
     модели нет. Точка и значение красятся ПО СОСТОЯНИЮ — той же величиной, что несёт слово, —
     и цвет не остаётся единственным носителем смысла, потому что слово стоит рядом.
   - Пульсация точки. Макет: пульсация у неподключённого аккаунта (строка 891). Исправляется
     существующим кадровым набором `pulse` (`app/static/css/app.css:437`) — второго не заводится.
   - Действия единицы списка. Макет: два — растянутое «Настроить группы» и второе действие,
     зависящее от состояния (строки 898-899). Приложение: вход на экран групп, удаление во всех
     ветках и повтор синхронизации в ветке ошибки. Форма исправляется; состав — сознательное
     отступление: путь удаления и путь повтора остаются, задача чинит вид, а не отбирает путь.
   - Пустое состояние. У макета его нет — в его данных-примерах аккаунтов всегда пять.
     Приложение показывает приглашение подключить первый аккаунт. Сознательное отступление,
     сохраняется слово в слово.
   - Пагинация. У макета её нет; у приложения бесконечная прокрутка сентинелом. Сознательное
     отступление: механизм сохраняется, но сентинел переезжает внутрь контейнера сетки и
     обязан растягиваться на всю её строку.
   - Опрос статуса синхронизации. У макета его нет вовсе. Сознательное отступление: механизм
     сохраняется целиком, включая условность запроса (T-06-01) и асимметрию панели
     подтверждения (T-11-04).
   - Шкала отступов, радиусы, типографика. У макета — захардкоженные значения инлайн-стилями;
     у приложения те же величины уже выведены в переменные. Расхождения нет, вне объёма как ось
     правки: задача обязана лишь ПОЛЬЗОВАТЬСЯ существующими переменными.
   - Адаптивные брейкпоинты. Приложение перестраивает раздел на 860px правилами строки-таблицы,
     компенсируя скрывающуюся шапку подписями внутри ячеек. После правки сетка перестраивается
     САМА шириной трека, правила 860px к разделу не относятся, и подписи ячеек уступают место
     ключам карточки. Исправляется — и именно поэтому раздел обязан выйти из инвентаризаций
     строки-таблицы ТЕМ ЖЕ коммитом.

3. «Что обязано измениться в инвентаризациях тестов» — таблица «Инвентаризация | Адрес | Что
   делается». Перечислить поимённо: перечень разделов на примитиве строки-таблицы и его
   единственный потребитель-параметризация; перечень страниц с шапкой колонок и его объявленное
   число; перечень шаблонов, рисующих строку без шапки, его объявленное число и хвостовое
   утверждение про файл подмены; четыре проверки подписей ячеек раздела и проверка «подписи
   пришли из списка колонок»; две проверки синхронности трёх файлов, стоящие на объявлениях
   раскладки и списка колонок; регрессия ответа опроса статуса в `tests/test_routes/`.
   Отдельной строкой назвать инвентаризации, которые остаются на ПРЕЖНИХ числах и правок НЕ
   требуют: места строчного удаления, импортёры панели, имена событий панели, места панели.
   Причина, по которой они не двигаются, называется прямо: три файла раздела сохраняются как
   три файла, форма карточки в каждом пишется своя, и ни один путь удаления не снимается.

4. «Вне объёма, названо явно» и «Инварианты, которые правка обязана сохранить» — по образцу
   прецедента. В инварианты обязательно: неизменность обоих файлов макета; сохранение якоря
   подмены дословно; сохранение маршрутов, метода, имени события и идентификатора панели;
   условность запроса обновления статуса; асимметрия эмиссии панели между тремя файлами.

Аудит пишется ДО единой правки кода: его вердикты — вход задач 2 и 3, а не их пересказ.
  </action>
  <verify>
    <automated>test -f .planning/quick/260825-of5-stranitsa-accounts-ne-sootvetstvuet-shab/260825-of5-NOTES.md &amp;&amp; grep -c 'unpacked.html:' .planning/quick/260825-of5-stranitsa-accounts-ne-sootvetstvuet-shab/260825-of5-NOTES.md &amp;&amp; git diff --quiet -- new_broadcaster_design.html design/new_broadcaster_design.unpacked.html</automated>
  </verify>
  <done>Файл аудита существует, содержит четыре названных раздела, у каждой оси таблицы стоит адрес обеих сторон и ровно один вердикт из трёх, оба файла макета не изменены ни одним байтом.</done>
</task>

<task type="tracer" tdd="true">
  <name>Задача 2: Карточная сетка раздела на трёх поверхностях</name>
  <files>app/templates/accounts/list.html, app/templates/accounts/partial_cards.html, app/templates/accounts/partials/sync_status_card.html, app/static/css/app.css, tests/test_pages/test_responsive_markup.py, tests/test_routes/test_wa_sync_status.py</files>
  <read_first>
    - `260825-of5-NOTES.md` задачи 1 — вердикты по каждой оси
    - `git show 78ccbe7` — как прецедент выводил раздел из инвентаризаций ОДНИМ коммитом
    - `app/templates/ads/includes/ad_card.html` — форма карточки и её комментарии-инварианты
    - `app/static/css/app.css:2260-2430` — раздел карточной сетки объявлений: образец того, как раздел заводится на существующих переменных
    - `app/static/css/app.css:1376-1394` — единственное объявление тонов канала
    - `app/static/css/app.css:437` — существующий кадровый набор пульсации
    - `app/static/css/app.css:2008-2027` — существующий примитив «ключ — значение»
    - `tests/test_pages/test_responsive_markup.py:52-90` — перечень разделов на примитиве строки-таблицы и комментарии трёх предыдущих выходов
    - `tests/test_pages/test_responsive_markup.py:340-400` — положительный примитив прецедента, эталон нового теста
    - `tests/test_pages/test_responsive_markup.py:1318-1440` — четыре проверки подписей ячеек раздела
    - `tests/test_pages/test_responsive_markup.py:2678-2830` — страховочная сетка синхронности трёх файлов
    - `tests/test_pages/test_responsive_markup.py:3607-3680`, `:3753-3760`, `:3920-4034` — три инвентаризации и их объявленные числа
    - `tests/test_routes/test_wa_sync_status.py:127-141` — регрессия ответа опроса статуса
  </read_first>
  <behavior>
Тесты пишутся ПЕРВЫМИ и обязаны краснеть до правки разметки. Каждое утверждение ниже —
отдельная проверка; слитые в одну они перестают различать причину падения.

  - Положительный примитив раздела: страница `/accounts` отдаёт контейнер карточной сетки и
    карточку аккаунта, и НЕ отдаёт примитив строки-таблицы. Утверждение положительное — «у
    раздела есть СВОЙ примитив», а не «нет старого»: без положительной половины раздел просто
    выпал бы из проверок вместе со снятыми строками параметризации. Тест ЧЕТВЁРТЫЙ по образцу
    `test_schedules_summary_list_is_card_based`.
  - Каждое значение карточки названо своим ключом — на ТРЁХ поверхностях порознь: списочная
    страница, порция бесконечной прокрутки (`/accounts/partial?offset=0&limit=30`) и ответ
    опроса статуса (`/accounts/{id}/sync-status`). Три теста, а не один: поверхности ломаются
    порознь, и разметки третьей нет на первичной отрисовке вовсе.
  - Ключи считаются по числу веток статуса, а не проверяются на «встречается хотя бы раз».
    Посев — по одному аккаунту на каждую из трёх веток (`active`, `sync_failed`, `syncing`).
    Проверка «хотя бы раз» зеленела бы при потере ключа в двух ветках из трёх.
  - Опрос статуса висит ТОЛЬКО на синхронизирующейся карточке: у подключённого аккаунта адреса
    опроса на странице нет, у синхронизирующегося запрос стоит на элементе С ЯКОРЕМ. Проверка
    уже существует (`test_accounts_polling_only_on_syncing_row`) и обязана остаться зелёной без
    правок — если она требует правки, значит переверстка сдвинула якорь, и сдвинут он неверно.
  - Сентинел бесконечной прокрутки приезжает ПОСЛЕДНИМ элементом внутри контейнера сетки, а не
    после него: посев в 31 аккаунт, проверка положения признака сетки и сентинела в выдаче.
  - Регрессия ответа опроса статуса в `tests/test_routes/test_wa_sync_status.py` переписана на
    форму карточки: число групп приходит вместе со своим ключом. Оба носителя смысла — ключ и
    число — проверяются ОДНИМ утверждением, как это было сделано для ячейки: число без ключа
    тест пройти не должно.
  </behavior>
  <action>
**Разметка — три файла, одна форма карточки.**

Три файла раздела СОХРАНЯЮТСЯ как три файла, и форма карточки пишется в каждом своя. Объединение
их общим макросом здесь НЕ делается, и это решение, а не забывчивость: файл подмены получает
контекст от обработчика опроса (идентификатор, статус, тип канала, статистика), а не строку
списка, панель подтверждения он сознательно не эмитит, а перенос разметки в общий макрос сдвинул
бы места строчного удаления, импортёров панели и мест подтверждения — четыре инвентаризации,
которые эта задача обязана оставить на прежних числах. Синхронность трёх файлов держат тесты, и
задача 3 переводит их на ключи карточки.

В каждом из трёх файлов:

  - Объявления раскладки колонок и списка названий колонок снимаются вместе с импортом табличных
    примитивов строки и ячейки. Вызов шапки колонок и обёртки-карточки в списочном файле
    снимается тоже: у сетки шапки нет, и обёртка вокруг сетки — вторая рамка вокруг рамок.
  - На их месте объявляется список ключей карточки — ОДНО объявление на файл, посимвольно
    совпадающее в трёх файлах, по той же форме, что несли снятые объявления. Ключи: состояние,
    группы, расписания, успешность, последняя отправка, дата подключения. Порядок фиксирован и
    одинаков в трёх файлах; ключ берётся ИНДЕКСОМ из списка, вписанный на месте строкой ключ
    запрещён — он разъедется с соседним файлом при первом же переименовании.
  - Единица списка — элемент `article` с классом карточки и с сохранённым ДОСЛОВНО якорем
    подмены. Якорь и запрос обновления статуса стоят на ОДНОМ элементе, и запрос по-прежнему
    висит только у ветки синхронизации. Переносить якорь на обёртку или на дочерний элемент
    нельзя: подмена порвётся молча, а страница останется исправной с виду.
  - Шапка карточки: тонированная плашка канала с иконкой (макрос иконки вызывается без подписи,
    как в существующих вызовах раздела), подпись канала, mono-идентификатор аккаунта, точка
    состояния, прижатая вправо. Тон плашки приходит АТРИБУТОМ канала — тем же признаком, каким
    красится пилюля канала, — а не цветом глифа: иконка одного из каналов залита градиентом и
    осталась бы бесцветной.
  - Тело карточки: существующий примитив «ключ — значение» (`.kv` / `.kv__k` / `.kv__v`), по
    одной строке на ключ. Значения берутся из тех же выражений, что стояли в снятых ячейках, —
    дословно, включая подсказки успешности и форматирование дат через глобал пользовательской
    таймзоны. Ветки статуса сохраняют свои прочерки там, где значения нет.
  - Состояние показано ДВАЖДЫ и обоими носителями сразу: точкой с тоном и mono-значением со
    словом. Тон берётся по состоянию из существующих переменных тона (успех, предупреждение,
    опасность, приглушённый) через существующий макрос mono с его вариантом. Ветка синхронизации
    получает пульсацию существующим кадровым набором. Бейдж-пилюля состояния из разметки уходит:
    её место занимает пара «точка + значение», как у макета.
  - Подвал карточки: ряд действий. Вход на экран групп аккаунта стоит во ВСЕХ ветках статуса и
    во всех трёх файлах — условная ветка, скрывающая вход, оставила бы группы синхронизирующегося
    аккаунта недостижимыми. Форма удаления и форма повтора синхронизации переносятся дословно:
    маршрут, метод, имя события открытия панели и идентификатор панели не меняются ни на символ.
  - Панель подтверждения остаётся РЯДОМ с карточкой, а не внутри неё, и по-прежнему эмитится
    списочным файлом и файлом порции, но НЕ файлом подмены. Асимметрия сохраняется дословно.
  - Сентинел бесконечной прокрутки переезжает ВНУТРЬ контейнера сетки и остаётся её последним
    элементом. Его разметка в списочном файле и в файле порции остаётся идентичной.
  - Каждый инвариант выше сопровождается комментарием у места, а не в коммите: комментарий
    называет, ЧТО ломается при нарушении и ПОЧЕМУ этого не видно на глаз. Прежние комментарии
    инвариантов подмены и панели сохраняются — они не про строку-таблицу, а про механизм.

**Оформление — `app/static/css/app.css`.**

Завести раздел карточной сетки аккаунтов по образцу соседнего раздела карточной сетки
объявлений, в конце файла, с шапкой-комментарием, называющей задачу и строку макета.

  - Контейнер сетки: колонки автозаполнением с минимальной шириной трека, взятой у макета
    (строка 878), и зазором из существующей шкалы отступов. Автозаполнение, а не
    автовместимость: одна карточка на широком экране обязана остаться карточкой, а не
    растянуться на всю строку.
  - Сентинел внутри сетки растягивается на всю её строку — иначе он встанет очередной колонкой
    рядом с последней карточкой и прочтётся как ещё один аккаунт.
  - Карточка: поверхность, рамка, радиус и отступ существующими переменными; наведение повторяет
    приём соседней карточки объявления. Медиазапросов у раздела нет ни одного — он перестраивается
    шириной трека сам.
  - Плашка канала: геометрия задаётся новым классом, а ТОН — не заводится. Три тона канала уже
    объявлены ровно один раз (строки 1386-1394); плашка становится ВТОРЫМ потребителем того же
    объявления — селектор дописывается в существующее правило через запятую, с комментарием,
    почему второй копии тонов не появляется. Новых цветовых констант в файле не возникает ни одной.
  - Точка состояния: тона — существующие переменные тона, по одному классу-модификатору на тон;
    пульсация — существующий кадровый набор, второго не заводится.
  - Ряд действий: путь на экран групп забирает свободное место ряда, разрушительное действие не
    сжимается. Инлайновых hex в разметке раздела нет ни одного.

**Выход раздела из обхода по строке-таблице — ТЕМ ЖЕ коммитом.**

Молчаливого выхода не бывает: раздел, выпавший из обхода без объявления, перестаёт проверяться и
никто об этом не узнаёт. Повторить форму прецедента дословно:

  - Перечень разделов на примитиве строки-таблицы пустеет — «Аккаунты» были в нём последними.
    Пустой перечень оставлять параметризацией нельзя: пустая параметризация не краснеет, а
    молча ничего не проверяет. Перечень и его единственный потребитель снимаются, на их место —
    комментарий класса с названной причиной и перечислением ЧЕТЫРЁХ выходов по порядку (планы
    02-07, 03-08, задача 260825-m0b, эта задача). Перечень «все разделы, переведённые на
    дизайн-систему» становится самостоятельным и по-прежнему СОДЕРЖИТ «Аккаунты»: проверка
    отсутствия utility-классов раздела не ослабляется ни на шаг.
  - Из перечня страниц с шапкой колонок снимается вход списочной страницы раздела, объявленное
    число опускается на единицу с дописанной названной причиной, ветка посева этого входа,
    ставшая недостижимой, удаляется.
  - Из перечня шаблонов, рисующих строку без шапки, снимаются ДВА входа — файл порции и файл
    подмены, — объявленное число опускается на два с названной причиной, а хвостовое
    утверждение, требовавшее от файла подмены написанного вручную признака строки, снимается
    вместе с ними: оно существовало ради разрешителя перечня, из которого файл ушёл.
  - Четыре проверки подписей ячеек раздела и проверка «подписи пришли из списка колонок»
    снимаются: подписей ячеек в карточке нет, компенсировать скрывающуюся шапку нечем, потому
    что шапки нет. Взамен в этом же коммите встают три проверки «каждое значение названо своим
    ключом» на трёх поверхностях — то же обещание, тот же счёт по веткам статуса.
  - Проверка синхронности объявлений трёх файлов ПЕРЕВОДИТСЯ на список ключей карточки: она
    продолжает утверждать посимвольное совпадение объявления в трёх файлах, только объявление
    теперь другое. Проверка совпадения подписей по числу вхождений снимается в этом коммите и
    восстанавливается задачей 3 в терминах ключей — иначе счёт по веткам остался бы без
    покрытия дольше, чем на один шаг.
  - Проверки, стоящие на маршрутах и на панели подтверждения, правок НЕ требуют и обязаны
    остаться зелёными без единого касания. Если хоть одна из них требует правки — переверстка
    сдвинула контракт, и сдвинут он неверно.

Полный прогон обязателен: `just test`. Известная посторонняя краснота полного прогона —
`test_image_base_url_comes_from_app_settings` (открытый долг порядка исполнения сюиты,
`.planning/todos/pending/full-suite-ads-editor-order-pollution.md`); её чинить в этой задаче не
нужно и трогать нельзя.
  </action>
  <verify>
    <automated>cd /source/broadcaster &amp;&amp; uv run pytest tests/test_pages/test_responsive_markup.py tests/test_pages/test_htmx_preserved.py tests/test_routes/test_wa_sync_status.py tests/test_templates/test_components.py -q</automated>
  </verify>
  <done>Страница `/accounts`, порция прокрутки и ответ опроса статуса отдают карточки в сетке; три файла раздела объявляют список ключей посимвольно одинаково; опрос статуса остаётся только у синхронизирующегося аккаунта; панель подтверждения эмитится двумя файлами из трёх; три инвентаризации строки-таблицы опущены с названными причинами и объявленными числами в том же коммите; места строчного удаления, импортёры панели, имена событий и места панели остались на прежних числах; названные проверочные файлы зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Подзаголовок раздела и счёт ключей по веткам статуса</name>
  <files>app/templates/accounts/list.html, tests/test_pages/test_responsive_markup.py</files>
  <read_first>
    - `design/new_broadcaster_design.unpacked.html:1486` — подзаголовок раздела
    - `app/templates/base.html:215-217` — гнёзда заголовка и подзаголовка шелла
    - `app/templates/account_groups/list.html:26`, `app/templates/billing/balance.html:15` — два из пяти существующих переопределений подзаголовка
    - `tests/test_pages/test_responsive_markup.py` — страховочная сетка синхронности трёх файлов после правок задачи 2
  </read_first>
  <behavior>
  - Подзаголовок раздела приезжает на странице `/accounts` и приезжает ИЗ ШАБЛОНА страницы, а не
    из обработчика: ни один из обработчиков раздела ради него не правится.
  - Ключи карточки совпадают в трёх файлах раздела ПО СОСТАВУ и ПО ЧИСЛУ ВХОЖДЕНИЙ. Счёт
    вхождений, а не множество значений: в каждом файле три ветки статуса, и ключ, потерянный в
    ОДНОЙ из них, множества не меняет — две оставшиеся ветки его удержат. Именно так ключ и
    теряется на практике: правят одну ветку, а расходится весь раздел.
  - Ключ, вписанный на месте строкой вместо элемента списка ключей, краснеет отдельным
    утверждением: вписанный ключ разъедется с соседним файлом, и увидит это только тот, кто
    долистал до второй порции или дождался опроса.
  </behavior>
  <action>
Переопределить блок подзаголовка шелла в шаблоне списочной страницы раздела значением из макета
(строка 1486). Гнездо уже существует и уже используется пятью страницами — заводить нечего,
переопределяется существующий блок. Обработчики раздела не правятся ни один: контракт «страница →
шелл» на то и заведён.

Восстановить счёт ключей по веткам статуса — половину страховочной сетки, снятую задачей 2.
Проверка идёт по ИСХОДНИКАМ трёх файлов, а не по отрендеренной странице, и причина та же, по
которой так устроена вся сетка Плана 11: у файла подмены нет своего адреса в обходе страниц, а
расхождение проявляется только в момент подмены и только визуально.

  - Ключи считаются счётчиком вхождений по каждому файлу; расхождение любого файла с эталонным
    называется поимённо, с обеими сторонами и числами.
  - Отдельным утверждением краснеет ключ, вписанный строкой на месте вместо элемента списка
    ключей. Отрицательный просмотр назад в образце сохраняется: он отсекает подписи действий и
    подписи макросов, которые ключами карточки не являются.
  - Комментарий над проверкой называет, ЧТО ломается при потере ключа в одной ветке и почему
    это не видно на широкой ширине.

Прогнать полностью: `just test`. Известная посторонняя краснота полного прогона названа в
задаче 2 — не чинить и не трогать.
  </action>
  <verify>
    <automated>cd /source/broadcaster &amp;&amp; uv run pytest tests/test_pages/test_responsive_markup.py -q &amp;&amp; grep -c 'page_subtitle' app/templates/accounts/list.html</automated>
  </verify>
  <done>Подзаголовок раздела приезжает на `/accounts` из шаблона страницы; счёт ключей по трём файлам и трём веткам статуса восстановлен и краснеет на потере ключа в одной ветке; вписанный строкой ключ краснеет отдельным утверждением; ни один обработчик раздела не правился.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → `/accounts`, `/accounts/partial`, `/accounts/{id}/sync-status` | три поверхности раздела, каждая отдаёт данные аккаунтов текущего пользователя |
| данные аккаунта → шаблон | значения из БД и из внешнего мессенджера попадают в разметку карточки |
| браузер → `POST /accounts/{id}/delete`, `POST /accounts/{id}/retry-sync` | разрушительные и дорогие действия, доступные с карточки |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-of5-01 | Information Disclosure | `accounts_list`, `accounts_partial`, `_get_account_stats` (`app/pages/accounts.py`) | high | mitigate | Задача вёрстки обработчиков НЕ ПРАВИТ. Условие владения (`MessengerAccount.user_id == user.id`) и тройное условие владения в сборе статистики остаются нетронутыми; `app/pages/accounts.py` не входит в `files_modified` ни одной задачи — попытка правки обработчика есть признак выхода за объём. |
| T-of5-02 | Information Disclosure | карточка аккаунта, три файла | high | mitigate | Ни одно значение не выводится в обход автоэкранирования: фильтр «как есть» в разметке раздела не появляется ни разу. Цвет из данных (`ac.color` макета) НЕ переносится — инлайновые стили, собираемые из значений, в разметке раздела не заводятся; тон приходит классом или атрибутом канала. |
| T-of5-03 | Denial of Service | опрос статуса синхронизации (T-06-01) | high | mitigate | Запрос обновления остаётся ПОД УСЛОВИЕМ ветки синхронизации во всех трёх файлах. Вынос запроса на каждую карточку «ради единообразия разметки» превращает раздел в поток запросов каждые 5 секунд с каждой открытой вкладки. Закреплено существующей проверкой `test_accounts_polling_only_on_syncing_row`, которая обязана остаться зелёной без правок. |
| T-of5-04 | Tampering | панель подтверждения удаления (T-11-04) | medium | mitigate | Панель остаётся ВНЕ заменяемого элемента и эмитится двумя файлами из трёх. Две панели с одним идентификатором открывались бы одним событием, и фокус уходил бы в невидимую копию. Закреплено существующей проверкой синхронности трёх файлов. |
| T-of5-05 | Tampering | маршруты удаления и повтора синхронизации | medium | mitigate | Маршрут, метод, имя события и идентификатор панели переносятся ДОСЛОВНО. Инвентаризации мест строчного удаления, импортёров панели, имён событий и мест панели остаются на прежних числах — их изменение есть признак сдвинутого контракта, а не успешной вёрстки. |
| T-of5-06 | Denial of Service | сентинел бесконечной прокрутки | low | accept | Механизм прокрутки сохраняется как есть, размер порции не меняется. Задача переносит сентинел внутрь контейнера сетки и не трогает ни его условие, ни его предел. |

Установок пакетных менеджеров задача не содержит ни одной: правятся шаблоны, стилевой файл и
тесты. Гейт подлинности пакетов неприменим, угроза цепочки поставок в реестр не заводится.
</threat_model>

<verification>
1. `just test` — полный прогон. Единственная допустимая краснота — известный долг порядка
   исполнения сюиты (`full-suite-ads-editor-order-pollution.md`); любая другая красная проверка
   означает несделанную работу, а не посторонний шум.
2. `git diff --stat` — в изменённых значатся ровно семь файлов из `files_modified` и ни одного
   сверх: обработчики раздела, `components/table.html`, мастера подключения и оба файла макета
   не тронуты.
3. `git diff -- new_broadcaster_design.html design/new_broadcaster_design.unpacked.html` пуст.
4. Три поверхности раздела прочитаны глазами рядом друг с другом: форма карточки, порядок ключей
   и разметка сентинела совпадают.
</verification>

<success_criteria>
- Список `/accounts` — карточная сетка на всех ширинах; шапки колонок, обёртки-карточки и
  строк-таблиц в разделе не осталось ни одной.
- Три поверхности раздела взаимозаменяемы: то, что видит долиставший до второй порции и
  дождавшийся опроса, совпадает с тем, что видит открывший страницу.
- Ни один путь пользователя не потерян: вход на экран групп, удаление аккаунта и повтор
  синхронизации доступны в тех же ветках статуса, что и до правки.
- Выход раздела из инвентаризаций строки-таблицы объявлен с названными причинами и опущенными
  числами в том же коммите, что и снятие шапки колонок.
- У раздела есть СВОЙ положительный примитив и обещание «понятно, что означает каждое значение»
  на трёх поверхностях.
- Новых цветовых констант — ноль; инлайновых hex в разметке раздела — ноль; вторых копий тонов
  канала, кадровых наборов и примитива «ключ — значение» — ноль.
</success_criteria>

<output>
Создать `.planning/quick/260825-of5-stranitsa-accounts-ne-sootvetstvuet-shab/260825-of5-SUMMARY.md`
по завершении.
</output>
