# Отложенное: задача 260825-of5

Найдено при полном прогоне, ВНЕ объёма задачи. Правится отдельно — граница
объёма проходит по причине поломки, а не по её месту в выдаче.

## 1. Счётчики прогресса в `.planning/STATE.md` разошлись с `.planning/ROADMAP.md`

- **Где:** `tests/test_planning/test_state_progress_matches_roadmap.py::test_the_machine_readable_progress_is_derived_from_the_roadmap`
- **Что:** `progress.total_plans` и `progress.completed_plans` во frontmatter
  `.planning/STATE.md` записаны как `110`, а из отметок `.planning/ROADMAP.md`
  выводится `0`. Источником счёта планов объявлен ROADMAP, значит приводить надо
  ПОЛЕ, а не отметки.
- **Почему вне объёма:** обе стороны расхождения — документные артефакты
  планирования; ни `.planning/STATE.md`, ни `.planning/ROADMAP.md` задача не
  трогала (`git diff --stat` задачи — шесть файлов кода, ни одного из
  `.planning/`). Расхождение существовало ДО задачи: последняя правка обоих
  файлов — коммит `d490dbf` предыдущей задачи 260825-m0b.
- **Проверено:** проверка краснеет на HEAD задачи ровно тем же сообщением, что и
  до неё; ни одно из шести изменённых задачей файлов в её выводе не участвует.

## 2. Известный долг порядка исполнения сюиты — НЕ проявился

`test_image_base_url_comes_from_app_settings`
(`.planning/todos/pending/full-suite-ads-editor-order-pollution.md`) в обоих
полных прогонах задачи прошёл зелёным. Долг не закрыт и не трогался — записано
ради честности отчёта: отсутствие красноты в конкретном прогоне не есть
исправление плавающей проверки.
