---
phase: quick-baileys-v7-0-0-rc14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wa_worker/package.json
  - wa_worker/package-lock.json
autonomous: true
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The WA worker declares @whiskeysockets/baileys at exactly 7.0.0-rc14."
    - "The npm lockfile resolves the root dependency and installed Baileys package to 7.0.0-rc14 with registry integrity metadata."
    - "A clean lockfile install exposes every Baileys export currently imported by wa_worker/index.js."
  artifacts:
    - path: "wa_worker/package.json"
      provides: "Exact direct dependency pin for Baileys 7.0.0-rc14"
      contains: '"@whiskeysockets/baileys": "7.0.0-rc14"'
    - path: "wa_worker/package-lock.json"
      provides: "Reproducible npm resolution for Baileys 7.0.0-rc14 and its transitive graph"
      contains: '"version": "7.0.0-rc14"'
  key_links:
    - from: "wa_worker/package.json"
      to: "wa_worker/package-lock.json"
      via: "npm-generated root dependency and node_modules/@whiskeysockets/baileys records"
      pattern: '7\\.0\\.0-rc14'
    - from: "wa_worker/index.js"
      to: "node_modules/@whiskeysockets/baileys"
      via: "existing ESM default and named imports exercised by the smoke check"
      pattern: "@whiskeysockets/baileys"
---

<objective>
Upgrade the per-account WhatsApp worker from `@whiskeysockets/baileys` 7.0.0-rc.9 to the exact 7.0.0-rc14 release.

Purpose: Adopt the requested Baileys release while preserving reproducible npm resolution and confirming the worker's existing import contract remains available.
Output: Synchronized `wa_worker/package.json` and `wa_worker/package-lock.json` dependency metadata.
</objective>

<execution_context>
@/source/broadcaster/.codex/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@wa_worker/package.json
@wa_worker/package-lock.json

<interfaces>
`wa_worker/index.js` imports the Baileys default export plus `useMultiFileAuthState`, `fetchLatestBaileysVersion`, `DisconnectReason`, and `Browsers`; the post-upgrade smoke check must resolve this existing interface without changing `index.js`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the exact Baileys dependency and validate the resolved worker contract</name>
  <files>wa_worker/package.json, wa_worker/package-lock.json</files>
  <reversibility rating="reversible">The dependency and lockfile change can be reverted together without data migration or a published contract change.</reversibility>
  <action>From `wa_worker/`, run `npm install --package-lock-only --ignore-scripts --save-exact @whiskeysockets/baileys@7.0.0-rc14` so npm updates both the direct dependency declaration and the complete lock graph. Keep the dependency pinned exactly, retain npm registry `resolved` and `integrity` metadata, and do not hand-author those lockfile values. Review the diff so no unrelated top-level dependency specifier changes; retain transitive changes that npm requires to resolve Baileys 7.0.0-rc14. Do not modify `wa_worker/index.js` or any Python application files.</action>
  <verify>
    <automated>cd wa_worker &amp;&amp; npm ci --ignore-scripts --no-audit --no-fund &amp;&amp; node --input-type=module -e "import fs from 'node:fs'; const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); const lock=JSON.parse(fs.readFileSync('package-lock.json','utf8')); const want='7.0.0-rc14'; if(pkg.dependencies['@whiskeysockets/baileys']!==want || lock.packages[''].dependencies['@whiskeysockets/baileys']!==want || lock.packages['node_modules/@whiskeysockets/baileys'].version!==want) throw new Error('Baileys version metadata is not synchronized'); const mod=await import('@whiskeysockets/baileys'); for (const name of ['default','useMultiFileAuthState','fetchLatestBaileysVersion','DisconnectReason','Browsers']) if (!(name in mod)) throw new Error('Missing Baileys export: '+name);"</automated>
  </verify>
  <done>Both npm manifests pin and resolve `@whiskeysockets/baileys` 7.0.0-rc14, `npm ci` succeeds with lifecycle scripts disabled, and every Baileys export used by the current worker imports successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to repository lockfile | Third-party release metadata and transitive dependency resolution enter the trusted build inputs. |
| Installed Baileys module to WA worker | Third-party ESM exports must satisfy the interface consumed by `wa_worker/index.js`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-L8H-01 | Tampering | `wa_worker/package-lock.json` | high | mitigate | Generate the lock graph with npm, preserve registry integrity hashes, pin the direct version exactly, and prove clean installation from the resulting lockfile. |
| T-Q-L8H-02 | Denial of Service | Baileys ESM interface consumed by `wa_worker/index.js` | medium | mitigate | After clean installation, dynamically import the exact default and named exports the worker requires before accepting the upgrade. |
| T-Q-L8H-SC | Tampering | npm package resolution | high | mitigate | Retain the established `@whiskeysockets/baileys` package coordinate, resolve only the user-selected exact release, and disable lifecycle scripts during lock generation and verification. |
</threat_model>

<verification>
Run the task's clean-install and ESM import smoke command from a clean worktree. Confirm `git diff -- wa_worker/package.json wa_worker/package-lock.json` contains the requested direct version bump and npm-required lock graph changes only.
</verification>

<success_criteria>
- `wa_worker/package.json` declares `@whiskeysockets/baileys` as exactly `7.0.0-rc14`.
- The lockfile root and installed-package records both resolve exactly `7.0.0-rc14` with npm-generated integrity metadata.
- A clean npm install succeeds with scripts disabled and the existing worker import surface resolves.
- No implementation file is changed.
</success_criteria>

<output>
Create `.planning/quick/260803-l8h-baileys-v7-0-0-rc14/260803-l8h-SUMMARY.md` with `status: complete` when done.
</output>
