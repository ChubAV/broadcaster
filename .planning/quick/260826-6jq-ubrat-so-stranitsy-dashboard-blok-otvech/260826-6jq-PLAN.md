---
phase: quick-260826-6jq-dashboard-drops-worker-block
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/dashboard.html
  - app/templates/dashboard/includes/worker_row.html
  - app/static/css/app.css
  - app/pages/dashboard.py
  - app/pages/common.py
  - tests/test_pages/test_shell.py
  - tests/test_pages/test_responsive_markup.py
autonomous: true
requirements:
  - QUICK-DASH-WORKERS-REMOVE
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "На странице `/dashboard` карточки «Воркеры аккаунтов» больше нет: в отрендеренной разметке не остаётся ни строки перечня, ни её пустого состояния, ни подписи об обрезке."
    - "Пилюля состояния сессий в шапке шелла (`app/templates/base.html:222-224`) ОСТАЁТСЯ и продолжает показывать те же два числа на всех 26 маршрутах. Снимается перечень на дашборде, а не индикатор шелла."
    - "Подсистема воркеров НЕ ТРОНУТА: `wa_worker/`, `app/worker/`, `app/services/wa_container_manager.py`, `app/services/ops_state.py` и раздел админки «Воркеры» (`app/templates/admin/workers.html`, `app/templates/admin/includes/worker_row.html`) не изменены ни одним байтом. Снимается ПОКАЗ воркеров на дашборде, а не сами воркеры."
    - "Контракт шелла `get_shell_context` перестаёт собирать ПОАККАУНТНЫЙ перечень: ключи `sessions` и `sessions_truncated` из словаря уходят, ключи `sessions_online` и `sessions_total` остаются. Перечень читал ОДИН маршрут из 26, а собирался на всех — вместе с блоком уходит и отдельный запрос за строками на каждом рендере страницы."
    - "Число обращений к таблице messenger-аккаунтов на рендере дашборда УМЕНЬШАЕТСЯ на одно и по-прежнему не растёт вместе с числом аккаунтов пользователя."
    - "Мёртвых артефактов не остаётся: шаблон строки перечня удалён как файл, его правила в `app/static/css/app.css` сняты, имя его точки вычеркнуто из правила `@media (prefers-reduced-motion: reduce)`, а комментарий над этим правилом называет НОВОЕ число оставшихся имён."
    - "Константа `WORKER_ONLINE_STATUS` ОСТАЁТСЯ единственным объявлением предиката «онлайн»: её читает скалярный подзапрос счёта онлайн-аккаунтов, из которого посчитана пилюля шапки."
    - "`@keyframes pulse` не тронут — его зовут ещё три живых правила (`.session-dot.is-online`, точка карточки аккаунта, точка живой ленты)."
    - "Снятие ОБЪЯВЛЕНО тестами, а не молчаливо: в проекте появляются положительные запреты (перечня нет в разметке, перечня нет в контракте шелла, шаблона строки нет на диске, правил нет в стилях) — тем же приёмом, каким план 04-05 выводил `dashboard/includes/recent_send_card.html`."
    - "Утверждения о владении и о предикате «онлайн», которые держали снятые тесты, НЕ ТЕРЯЮТСЯ: чужой аккаунт не попадает в счёт пилюли, а из четырёх аккаунтов онлайном считаются ровно два — обе гарантии переезжают на числа агрегатов."
    - "`uv run pytest tests/ -v` зелёный целиком, а не только по правленым файлам."
  artifacts:
    - "app/templates/dashboard.html"
    - "app/static/css/app.css"
    - "app/pages/dashboard.py"
    - "app/pages/common.py"
    - "tests/test_pages/test_shell.py"
    - "tests/test_pages/test_responsive_markup.py"
  key_links:
    - "`get_shell_context` ↔ `app/templates/base.html:35,222-224`: агрегаты `sessions_online` / `sessions_total` читает ШАПКА на каждом маршруте. Сняв их заодно с перечнем, задача уронит пилюлю на всех 26 страницах — это и есть главная ловушка правки."
    - "`WORKER_ONLINE_STATUS` ↔ скалярный подзапрос `accounts_online` в `get_shell_context`: константа остаётся живой ПОСЛЕ снятия перечня. Удалённая заодно, она унесёт с собой предикат, из которого посчитана пилюля."
    - "`.worker-row__dot.is-online` ↔ правило `@media (prefers-reduced-motion: reduce)` (`app/static/css/app.css:712-714`): имя стоит В ДВУХ местах. Снятое только в одном, оно оставляет правило, адресованное несуществующему классу, — ровно тот мусор, который запрещает `test_the_reduced_motion_rule_names_only_living_classes`."
    - "`app/templates/dashboard/includes/worker_row.html` ↔ `app/templates/admin/includes/worker_row.html`: ДВА РАЗНЫХ ФАЙЛА с одинаковым именем. Удаляется первый; второй — строка таблицы админки, живая и потребляемая `admin/includes/workers_partial.html`."
    - "Снятые тесты ↔ помощники `_seed_messenger_account` и `_dashboard_user` (`tests/test_pages/test_shell.py:781-812`): помощники ОСТАЮТСЯ — их зовут тесты пилюли, которые не снимаются."
---

<objective>
Убрать со страницы дашборда карточку «Воркеры аккаунтов» вместе с плумбингом, который существует ТОЛЬКО ради неё.

Purpose: перечень отвечает на вопрос «какой канал отвалился», но собирается он контрактом шелла на КАЖДОМ из 26 страничных маршрутов, а читает его один. Убрав блок, задача снимает и это чтение — не «оптимизация», а снятие работы, которую никто больше не заказывает.

Output: дашборд без карточки воркеров; контракт шелла без поаккаунтного перечня; удалённый шаблон строки и его стили; тесты, которые ЗАПРЕЩАЮТ возврат блока и сохраняют утверждения, ради которых снятые тесты писались.

**ГРАНИЦА ЗАДАЧИ.** Снимается ПОКАЗ воркеров на дашборде. Сама подсистема (`wa_worker/`, `app/worker/`, `app/services/wa_container_manager.py`, `app/services/ops_state.py`) и раздел админки «Воркеры» не трогаются вовсе. Пилюля состояния сессий в шапке шелла остаётся: она отвечает «работает ли хоть что-то» с ЛЮБОЙ страницы и к дашборду не привязана.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

@app/templates/dashboard.html
@app/pages/dashboard.py
</context>

<tasks>

<task type="tracer">
  <name>Задача 1: снять карточку воркеров с дашборда и её плумбинг из контракта шелла</name>
  <files>app/templates/dashboard.html, app/templates/dashboard/includes/worker_row.html, app/static/css/app.css, app/pages/dashboard.py, app/pages/common.py</files>
  <action>
Один сквозной срез через все четыре слоя блока: разметка → шаблон строки → стили → сборка данных. Номера строк даны по текущему состоянию файлов; правь СВЕРХУ ВНИЗ внутри каждого файла, иначе номера уедут.

**1. `app/templates/dashboard.html` (188 строк).**
Снять строку 8 — импорт макроса строки перечня. Снять строки 58-102 целиком: комментарий блока (58-75) и саму карточку от `{{ card_open() }}` на строке 76 до `{{ card_close() }}` на строке 102 включительно.
ОСТАЮТСЯ нетронутыми: строка 39 (`{% set next_label, next_href = next_step %}` — пару читает блок графика на строке 184), импорты `card_open/card_close` (зовут блоки на 117/126 и 158/168), `empty_state` (строка 182), `mono` (строка 161), обёртка `data-dashpair` и всё, что ниже строки 103.

**2. `app/templates/dashboard/includes/worker_row.html` — удалить файл целиком** (46 строк). В проекте недостижимых шаблонов не оставляют (тот же приём, каким план 04-05 вывел `dashboard/includes/recent_send_card.html`). Четыре соседа в каталоге — `activity_chart.html`, `feed_row.html`, `metric_tile.html`, `upcoming_row.html` — остаются.
⚠️ НЕ ПУТАТЬ с `app/templates/admin/includes/worker_row.html`: это ДРУГОЙ файл, строка таблицы админки, живой и потребляемый `admin/includes/workers_partial.html`. Он не трогается.

**3. `app/static/css/app.css`.**
Снять строки 676-704: комментарий раздела (676-685) и семь правил перечня — `.worker-list`, `.worker-row`, `.worker-row:last-child`, `.worker-row__dot`, `.worker-row__dot.is-online`, `.worker-row__label`, `.worker-row__state`.
В строке 713 (перечисление селекторов правила `@media (prefers-reduced-motion: reduce)`) вычеркнуть имя точки перечня. Остаются ТРИ имени: `.brand-mark::after`, `.session-dot.is-online`, `.animate-fade-in`.
Комментарий над этим правилом (709-711) называет число оставшихся имён словом «четыре» — привести к «три» и дописать причину второго вычёркивания. Комментарий, называющий устаревшее число, лжёт следующему читателю ровно там, где он и написан против лжи.
НЕ ТРОГАТЬ: `@keyframes pulse` (строка 437) — его зовут `.session-dot.is-online` (673), правило на 1435 и `.acct-card__dot--live` (2515); блок пилюли `.session-pill` / `.session-dot` / `.session-label` (665-674) — это шапка шелла.

**4. `app/pages/common.py`.**
- Снять строки 607-619: константу потолка перечня вместе с её комментарием.
- Снять строки 692-729: комментарий, отдельный запрос за строками аккаунтов (`worker_rows`) и сборку списка словарей.
- В возвращаемом словаре снять ключ перечня (строка 787), признак обрезки вместе с его комментарием (строки 790-793) и комментарий 785-786, который объясняет соотношение перечня и агрегатов.
- ⚠️ Ключи `sessions_online` (788) и `sessions_total` (789) ОСТАЮТСЯ. Их читает пилюля шапки в `app/templates/base.html:35,222-224` на КАЖДОМ маршруте: снятые заодно, они уронят индикатор на всех 26 страницах.
- Докстринг `get_shell_context` (623-644): снять абзацы про перечень и про признак обрезки (635-643). Абзац про запрет перечисления Docker-контейнеров (628-633) ОСТАЁТСЯ — его держит живой тест `test_dashboard_render_path_never_touches_docker`.
- Константа `WORKER_ONLINE_STATUS` (605) ОСТАЁТСЯ: её читает скалярный подзапрос счёта онлайн-аккаунтов на строке 684. Её комментарий (601-604) обещает одно сравнение «для перечня и агрегата» — привести к единственному оставшемуся потребителю.
- Импорт `MessengerAccount` остаётся: он нужен подзапросам счётчиков (675-687).

**5. `app/pages/dashboard.py`.**
Снять строки 122-136 — три ключа контекста (перечень, признак обрезки, полное число) вместе с их комментариями. Остальные ключи словаря не трогать.
⚠️ Живой тест (`tests/test_pages/test_shell.py:1332`) запрещает этому модулю упоминать модель messenger-аккаунта и имя ключа счёта онлайн — запрет продолжает соблюдаться, и оставленный комментарий не должен его нарушить.

Проверка целостности после правки: `next_step` в `dashboard.py` остаётся, `check_is_admin` остаётся, обвязка ленты (`FEED_LIMIT`, `FEED_POLL_SECONDS`) остаётся.
  </action>
  <verify>
    <automated>test ! -f app/templates/dashboard/includes/worker_row.html && test -f app/templates/admin/includes/worker_row.html && uv run pytest tests/test_pages/test_dashboard.py -q</automated>
  </verify>
  <done>Файл шаблона строки перечня удалён, одноимённый файл админки на месте, страница `/dashboard` рендерится и весь набор `tests/test_pages/test_dashboard.py` зелёный.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: снять тесты снятого блока, сохранить их утверждения и объявить запрет</name>
  <files>tests/test_pages/test_shell.py, tests/test_pages/test_responsive_markup.py</files>
  <behavior>
    - Страница `/dashboard` у пользователя с двумя аккаунтами (одним активным) отдаёт 200, и в её разметке нет ни одного признака перечня воркеров, при этом пилюля сессий в шапке на месте.
    - `get_shell_context` для пользователя с одним активным аккаунтом отдаёт словарь БЕЗ ключей перечня и признака обрезки, но с `sessions_total == 1` и `sessions_online == 1`.
    - Ни один шаблон под `app/templates/dashboard/` не несёт признака строки перечня, а сам файл строки на диске отсутствует.
    - Стили проекта (в разбор берётся текст БЕЗ комментариев) не содержат селекторов снятого перечня.
    - Из четырёх посеянных аккаунтов со статусами `active`, `active`, `disconnected`, `sync_failed` агрегаты насчитывают четыре всего и два онлайн — предикат «онлайн» остаётся с одним объявлением.
    - Чужой аккаунт не попадает в счёт пилюли: у владельца одного аккаунта в разметке стоит единица.
    - На рендере дашборда СОБСТВЕННОГО запроса за строками аккаунтов больше нет вовсе, агрегаты по-прежнему едут скалярными подзапросами в общем round-trip счётчиков, и число обращений к таблице не растёт вместе с числом аккаунтов.
  </behavior>
  <action>
**A. `tests/test_pages/test_shell.py` — снять (номера строк по текущему файлу; правь снизу вверх или по именам).**
- помощники `_worker_rows` (865-882) и `_worker_states` (883-901);
- `test_dashboard_lists_each_account_with_its_worker_state` (902-944);
- `test_worker_list_is_capped_but_the_counts_are_not` (945-993);
- `test_worker_list_reports_no_truncation_when_it_fits` (994-1010);
- `test_shell_worker_list_carries_no_secrets` (1033-1059);
- `test_dashboard_worker_list_keeps_an_unrecognised_status_visible` (1060-1088);
- `test_dashboard_worker_list_shows_an_empty_state_without_accounts` (1089-1104);
- `test_dashboard_worker_row_has_one_definition` (1105-1126);
- `test_shell_worker_entries_expose_only_the_declared_keys` (1307-1330).

**НЕ СНИМАТЬ:** `_seed_messenger_account` (781-806) и `_dashboard_user` (809-812) — их зовут остающиеся тесты; три теста пилюли (815-863); `_identifiers_of` (1127-1152) и `test_dashboard_render_path_never_touches_docker` (1155-1187); `test_dashboard_page_has_no_second_source_of_the_sessions_number` (1332-1351).

**B. `tests/test_pages/test_shell.py` — переписать три теста, чтобы их утверждения не потерялись вместе с предметом.**

1. `test_dashboard_worker_list_excludes_another_users_account` (1011-1032) → переименовать в `test_dashboard_account_count_excludes_another_users_account`. Снять две строки, зовущие удалённый помощник (1027-1029); оставить посев обоих пользователей и утверждение о единице в разметке пилюли (1030). Владение чужой строкой остаётся проверенным — теперь на числе агрегата.

2. `test_shell_reads_worker_state_in_a_single_query` (1190-1277) → переименовать в `test_shell_makes_no_own_read_of_messenger_accounts`. Утверждение 1250-1253 ИНВЕРТИРОВАТЬ: собственных обращений за строками аккаунтов теперь НОЛЬ (список `own_read` пуст), и сообщение об ошибке обязано называть новую причину — отдельное чтение вернулось на путь рендера каждой из 26 страниц. Блок про агрегаты (1259-1271) и утверждение о неросте числа обращений (1272-1276) оставить дословно: это и есть оставшаяся гарантия. Докстринг переписать под новый предмет.

3. `test_shell_aggregate_is_derived_from_the_worker_list` (1279-1305) → переименовать в `test_shell_counts_accounts_and_online_accounts_by_one_predicate`. Посев из четырёх аккаунтов оставить как есть; утверждения 1300-1304 заменить на три: всего четыре, онлайн два, счётчик раздела аккаунтов равен четырём. Докстринг переписать: предмет — единственное объявление предиката «онлайн», а не согласие агрегата с перечнем.

**C. `tests/test_pages/test_shell.py` — обновить прозу секции.**
Заголовок секции (749-760) и комментарий внутри `DASHBOARD_RENDER_PATH` (769-773) объясняют предмет через поаккаунтное чтение. Модуль контекста шелла ОСТАЁТСЯ в перечне — в нём по-прежнему живёт чтение состояния аккаунтов агрегатами, то есть соблазн дописать «а давайте покажем, жив ли контейнер» стоит там же. Привести обе прозы к новому положению дел, перечень модулей не трогая.

**D. `tests/test_pages/test_shell.py` — дописать три положительных запрета** (рядом с остальными тестами секции). `PROJECT_ROOT` уже объявлен на строке 776, `get_shell_context` уже импортирован.

- `test_the_dashboard_carries_no_per_account_worker_block` — асинхронный, берёт `authed_client` и `db_session`, сеет владельцу два аккаунта (`active` и `disconnected`), запрашивает `/dashboard`, утверждает статус 200, отсутствие в ОТРЕНДЕРЕННОЙ разметке каждого из признаков перечня — атрибута строки, двух её классов и заголовка карточки — и присутствие признака пилюли шапки (иначе запрет пройдёт и на снесённом заодно индикаторе). Разбирается именно отрендеренный ответ: комментарии Jinja до разметки не доезжают, поэтому объяснение снятия, оставленное в шаблоне, тест не краснит.
- `test_the_shell_contract_carries_no_per_account_list` — асинхронный, сеет один активный аккаунт с непустыми учётными данными и строкой сессии, зовёт `get_shell_context` напрямую и утверждает: ключа перечня и ключа признака обрезки в словаре НЕТ, а `sessions_total` и `sessions_online` равны единице. Это же утверждение закрывает и границу секретов, которую держал снятый `test_shell_worker_list_carries_no_secrets`: перечня нет вовсе, протаскивать нечего.
- `test_the_dashboard_worker_row_template_is_gone` — синхронный, утверждает, что файла строки перечня на диске нет, и что ни один `*.html` под `app/templates/dashboard/` не содержит признака строки перечня. Второе утверждение — замена снятому `test_dashboard_worker_row_has_one_definition`: прежде он требовал ровно одного владельца разметки, теперь требует ни одного.

**E. `tests/test_pages/test_responsive_markup.py`.**
- `test_the_reduced_motion_rule_names_only_living_classes` (2044-2066): ожидаемое число имён — три, имя точки перечня из цикла убрать, докстринг дополнить вторым вычёркиванием (первое — заливка шкалы, план 05.1-04).
- Дописать `test_the_dashboard_worker_list_left_no_css_behind`: читает стили ЧЕРЕЗ существующий помощник `_css_without_comments()` (он же используется на строке 2076) и утверждает отсутствие обоих селекторов снятого перечня. Через помощник, а не сырым текстом: объяснение снятия, оставленное комментарием в стилях, тест краснить не должен.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py tests/test_pages/test_responsive_markup.py -q</automated>
  </verify>
  <done>Оба набора зелёные; в `test_shell.py` появились три новых теста-запрета и три переписанных теста, помощники посева и тесты пилюли на месте.</done>
</task>

<task type="auto">
  <name>Задача 3: зелёный прогон всего набора и обновление графа</name>
  <files>tests/, graphify-out/</files>
  <action>
Прогнать ВЕСЬ набор, а не только правленые файлы: снятие ключей контракта шелла задевает путь рендера всех 26 маршрутов, и красное могло уехать туда, куда правка не заглядывала (`tests/test_pages/test_admin_panel.py` держит собственный раздел «Воркеры» админки — он обязан остаться зелёным без единой правки, и это лучшее доказательство, что подсистема не тронута).

Если что-то краснеет — чинить ПО МЕСТУ, не расширяя объём: правка касается только дашборда и контракта шелла, и любое красное за их пределами означает, что задета лишняя строка, а не что тест устарел.

По правилу проекта (`CLAUDE.md`) после изменения кода обновить граф: `graphify update .` (только AST, без обращений к API).
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <done>`uv run pytest tests/ -q` зелёный целиком; `graphify update .` отработал; `git status` не показывает незакоммиченных правок вне перечисленных файлов и `graphify-out/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| контракт шелла → HTML на 26 маршрутах | словарь `get_shell_context` печатается в разметку каждой страницы; всё, что в него попало, попадает на экран |
| владелец → чужие messenger-аккаунты | выборка по `user_id` — единственное, что отделяет перечень аккаунтов одного пользователя от другого |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6jq-01 | Information Disclosure | `get_shell_context` | medium | mitigate | Снятие поаккаунтного перечня УБИРАЕТ поверхность целиком: словарь больше не несёт ни идентификаторов, ни типов, ни статусов аккаунтов. Закреплено `test_the_shell_contract_carries_no_per_account_list`. |
| T-6jq-02 | Denial of Service | путь рендера страниц | low | mitigate | Отдельный запрос за строками аккаунтов уходит с пути рендера каждой из 26 страниц вместе с потолком выборки. Неросту числа обращений оставлен живой тест `test_shell_makes_no_own_read_of_messenger_accounts`. |
| T-6jq-03 | Tampering | смежные модули воркеров | medium | mitigate | Объём ограничен пятью файлами показа; полный прогон набора (Задача 3) обязан оставить зелёными `test_admin_panel.py`, `test_worker/test_tasks.py` и `test_services/test_ops_state.py` БЕЗ единой правки — это и есть доказательство, что подсистема не задета. |
| T-6jq-04 | Information Disclosure | пилюля шапки шелла | low | accept | Агрегаты `sessions_online` / `sessions_total` остаются на экране осознанно: это два числа без идентификаторов, они существовали до задачи и в её объём не входят. |

Установок пакетов задача не делает — гейт легитимности пакетов не применяется.
</threat_model>

<verification>
1. `uv run pytest tests/ -q` — весь набор зелёный.
2. `git diff --stat` — изменены РОВНО семь файлов из `files_modified` (плюс удалённый шаблон строки), и ни одного файла из `wa_worker/`, `app/worker/`, `app/services/`, `app/templates/admin/`.
3. Ручная сверка глазами: `/dashboard` открывается, плитки, ближайшие отправки, живая лента и график на месте, карточки воркеров нет, пилюля сессий в шапке показывает прежние числа.
</verification>

<success_criteria>
- Карточки «Воркеры аккаунтов» на дашборде нет ни в разметке, ни в стилях, ни в шаблонах.
- Контракт шелла отдаёт агрегаты и не отдаёт поаккаунтный перечень.
- Подсистема воркеров и раздел админки не изменены.
- Весь набор тестов зелёный, и возврат блока теперь краснит четыре новых теста-запрета.
</success_criteria>

<output>
Create `.planning/quick/260826-6jq-ubrat-so-stranitsy-dashboard-blok-otvech/260826-6jq-SUMMARY.md` when done
</output>
