---
phase: quick-260825-m0b-ads-list-matches-canonical-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md
  - app/templates/ads/list.html
  - app/templates/ads/partial_cards.html
  - app/templates/ads/includes/ad_card.html
  - app/static/css/app.css
  - app/pages/ads.py
  - tests/test_pages/test_responsive_markup.py
  - tests/test_pages/test_htmx_preserved.py
autonomous: true
requirements:
  - QUICK-ADS-LAYOUT
estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Список `/ads` рисуется КАРТОЧНОЙ СЕТКОЙ на всех ширинах — как раздел `isAds` канонического макета (`design/new_broadcaster_design.unpacked.html:479`), а не строкой-таблицей с шапкой колонок."
    - "Расхождения макета и реализации перечислены ПОИМЁННО в `260825-m0b-NOTES.md`: у каждой строки — что говорит макет (с номером строки распакованного файла), что рисует приложение (с `файл:строка`) и вердикт «исправлено / сознательное отступление / вне объёма»."
    - "Карточка объявления несёт ровно те элементы, что и карточка макета (строки 481-508): обложка вложения, точка состояния, название, mono-подпись состояния справа, выдержка текста, полоса подвала с пилюлями каналов, mono-метаданные и действие «Изменить»."
    - "Пилюли каналов собраны на УЖЕ СУЩЕСТВУЮЩЕМ примитиве `data-upbadge` / `messenger_icon` (`app/static/css/app.css:1376-1394`, заведён дашбордом), а не на второй копии тех же правил."
    - "Число каналов карточки приходит ОДНИМ сгруппированным запросом на страницу выдачи внутри `_enrich_ads_with_stats` — по форме двух уже существующих там запросов. Запроса на объявление в цикле не появляется."
    - "Путь удаления объявления со списка СОХРАНЁН — это названное отступление от макета, а не недосмотр: макет кладёт удаление только в редактор, а План 01-03 специально удержал его на списке (WR-04, «на странице не оставалось ни одного пути удалить объявление»). Инвентаризации `ROW_DELETE_PLACES = 12`, `MODAL_IMPORTERS`, `MODAL_EVENT_NAMES`, `MODAL_PLACES` и `THUMB_CONSUMERS` остаются на прежних числах."
    - "Панель поиска макета (строки 468-477) реализована: поиск по названию И тексту объявления, счётчик найденного mono-подписью, сброс поиска."
    - "Поиск переживает бесконечную прокрутку: сентинел несёт действующие параметры отбора, и вторая порция отфильтрована так же, как первая. Приём перенесён ДОСЛОВНО из `app/templates/schedules/list.html:61`, второго способа проброса не заводится."
    - "Пустых состояния ДВА и они различаются — «поиск ничего не нашёл» (действие: сбросить поиск) и «объявлений нет вовсе» (действие: создать), по образцу `app/templates/schedules/list.html:64-79`."
    - "Ни одного инлайнового hex и ни одного utility-класса в разметке раздела: оформление живёт классами в `app/static/css/app.css` на существующих переменных `:root`, новых цветовых констант не заводится."
    - "Выход раздела из обхода по строке-таблице ОБЪЯВЛЕН, а не молчалив: `MIGRATED_SECTIONS`, `ROWHEAD_PAGES`, `ROW_TEMPLATES_WITHOUT_HEADER` и оба объявленных числа опущены в ТОМ ЖЕ коммите с названной причиной — ровно тем приёмом, каким планы 02-07, 03-08, 04-05 и 04-07 выводили свои разделы."
    - "Раздел не выпадает из проверок вместе со снятой строкой параметризации: у него появляется СВОЙ положительный примитив, закреплённый тестом, — по образцу `test_schedules_summary_list_is_card_based`."
    - "Редактор объявления (`app/templates/ads/form.html` и остальные `ads/includes/*`) не тронут: он перенесён из макета Фазой 2 (строки 520-709) и в расхождение не входит."
    - "Разделы «Аккаунты», «История», группы аккаунта и админка сохраняют примитив строки-таблицы: `components/table.html` не правится, его прочие потребители не трогаются."
    - "`new_broadcaster_design.html` и `design/new_broadcaster_design.unpacked.html` не изменены ни одним байтом."
  artifacts:
    - path: ".planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md"
      provides: "Итемизированный аудит расхождений макета и реализации с вердиктом по каждой строке"
      contains: "unpacked.html"
    - path: "app/templates/ads/includes/ad_card.html"
      provides: "Карточка объявления по макету: обложка, точка состояния, mono-состояние, выдержка, пилюли каналов, метаданные, действия"
      contains: "data-upbadge"
    - path: "app/templates/ads/list.html"
      provides: "Страница списка: панель поиска, счётчик, карточная сетка, два пустых состояния"
      contains: "data-ads-grid"
    - path: "app/templates/ads/partial_cards.html"
      provides: "Порция бесконечной прокрутки с тем же макросом карточки и тем же сентинелом с пробросом отбора"
      contains: "filter_params"
    - path: "app/static/css/app.css"
      provides: "Классы сетки и карточки объявления на существующих переменных :root"
      contains: "[data-ads-grid]"
    - path: "app/pages/ads.py"
      provides: "Поиск по названию и тексту, счётчик найденного, признак активного отбора, набор каналов объявления одним сгруппированным запросом"
      contains: "filter_params"
    - path: "tests/test_pages/test_responsive_markup.py"
      provides: "Положительный примитив раздела, подписи значений карточки, опущенные инвентаризации с названной причиной"
      contains: "test_ads_list_is_card_based"
  key_links:
    - from: "app/templates/ads/includes/ad_card.html (пилюля канала)"
      to: "app/static/css/app.css:1376-1394 — [data-upbadge] / [data-upbadge][data-channel] — ЕДИНСТВЕННОЕ объявление тона пилюли канала"
    - from: "app/templates/ads/list.html + partial_cards.html (сентинел)"
      to: "app/pages/ads.py::ads_partial — параметры отбора приезжают тем же приёмом, что и у расписаний"
    - from: "app/pages/ads.py::_enrich_ads_with_stats (каналы)"
      to: "app/models/schedule.py::Schedule.account_id → app/models/messenger_account.py::MessengerAccount.type — единственный источник канала объявления"
    - from: "tests/test_pages/test_responsive_markup.py::ROWHEAD_PAGES / ROW_TEMPLATES_WITHOUT_HEADER"
      to: "app/templates/ads/list.html + ads/includes/ad_card.html — снятие входов обязано идти ТЕМ ЖЕ коммитом, что и снятие шапки колонок"
---

<objective>
Привести страницу `/ads` в соответствие каноническому макету `new_broadcaster_design.html`.

Раздел `isAds` макета (`design/new_broadcaster_design.unpacked.html:466-518`) — это панель
поиска со счётчиком и КАРТОЧНАЯ СЕТКА `repeat(auto-fill, minmax(290px, 1fr))`. Приложение
рисует на этом месте строку-таблицу с шапкой колонок: раздел получил её Планом 01-03,
когда одна форма списочной страницы была применена ко всем пяти разделам сразу, а Фаза 2
своим объёмом взяла только редактор объявления и сводный список расписаний
(`02-CONTEXT.md:84`) и списка объявлений не касалась.

Purpose: пользователь видит раздел «Объявления» таким, каким его описывает единственный
источник истины дизайна вехи v2.0.

Output: аудит расхождений отдельным файлом, карточная сетка вместо строки-таблицы, панель
поиска со счётчиком и два пустых состояния, обновлённые инвентаризации тестов.

**В ОБЪЁМЕ:** списочная страница `/ads` — `app/templates/ads/list.html`,
`app/templates/ads/partial_cards.html`, `app/templates/ads/includes/ad_card.html`,
обработчики `ads_list` / `ads_partial` / `_enrich_ads_with_stats` в `app/pages/ads.py`,
классы раздела в `app/static/css/app.css` и тесты, закрепляющие форму этого списка.

**ВНЕ ОБЪЁМА, НАЗВАНО ЯВНО:**
- Редактор объявления (`app/templates/ads/form.html`, `ads/includes/autosave.html`,
  `autosave_response.html`, `preview.html`, `sched_card.html`, `summary.html`) — перенесён
  из макета Фазой 2 (строки 520-709 распакованного файла), общих расходящихся компонентов
  со списком у него нет. Единственная общая точка — макрос `thumb`, и он не правится.
- Прочие списочные разделы. `components/table.html` (`rowhead` / `row_open` / `cell`)
  НЕ ПРАВИТСЯ: у него остаются потребители в «Аккаунтах», «Тарифах» и четырёх подразделах
  админки, а макет предписывает карточную сетку только разделу объявлений.
- Заголовок и подзаголовок шапки шелла. Макет пишет подзаголовком «6 из 15 по тарифу
  Basic» — тарифных лимитов в продукте больше НЕТ (Фаза 05.1 сняла счётную модель целиком),
  и воскрешать их нельзя. Счётчик макета живёт в панели поиска, туда он и ставится.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/source/broadcaster/CLAUDE.md
@/source/broadcaster/.claude/CLAUDE.md
@/source/broadcaster/design/README.md

@/source/broadcaster/app/templates/ads/list.html
@/source/broadcaster/app/templates/ads/partial_cards.html
@/source/broadcaster/app/templates/ads/includes/ad_card.html
@/source/broadcaster/app/templates/schedules/list.html
@/source/broadcaster/app/templates/dashboard/includes/upcoming_row.html
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Аудит расхождений макета и реализации</name>
  <files>.planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md</files>
  <read_first>
    - `design/new_broadcaster_design.unpacked.html`, строки 466-518 — весь раздел `isAds`.
      Читать ОДНИМ чтением диапазона: файл 166 КБ, повторные чтения запрещены.
    - `app/templates/ads/list.html` (37 строк), `app/templates/ads/partial_cards.html`
      (8 строк), `app/templates/ads/includes/ad_card.html` (82 строки) — целиком.
    - `app/pages/ads.py`, строки 55-146 — `_enrich_ads_with_stats`, `ads_partial`, `ads_list`.
    - `app/templates/schedules/list.html` — образец УЖЕ ПРИНЯТОЙ в проекте формы
      «панель отбора + счётчик + карточный список + два пустых состояния».
  </read_first>
  <action>
Написать `260825-m0b-NOTES.md` — аудит расхождений, за которым идут задачи 2 и 3.

Файл открывается абзацем о происхождении расхождения: раздел получил форму
списочной страницы Планом 01-03, где одна форма была применена ко всем пяти
разделам сразу; Фаза 2 своим объёмом взяла редактор и сводный список расписаний
(`02-CONTEXT.md:84`) и списка объявлений не касалась. Это объяснение, а не
оправдание — вердикты ниже ставятся по макету.

Дальше — таблица со столбцами: `Ось | Макет | Приложение | Вердикт`. Каждая строка
обязана нести адрес по ОБЕИМ сторонам, и оба — в форме `файл:строка`: сторона макета
записывается как `unpacked.html:481`, сторона приложения — как
`ads/includes/ad_card.html:55`. Утверждение без адреса в таблицу не ставится.

Обойти ровно эти оси, ни одну не пропустить (пропуск объявляется строкой «расхождения
нет», а не отсутствием строки):

1. Оболочка страницы и шапка раздела — заголовок, действие шапки, подзаголовок.
2. Панель отбора: поиск по названию и тексту, кнопка сброса, счётчик найденного.
3. Раскладка списка: сетка против строки-таблицы, ширина трека, зазор.
4. Форма единицы списка: обложка вложения, точка состояния, название, подпись
   состояния, выдержка текста, подвал.
5. Состав показанных по объявлению полей — что показывает макет, что показывает
   приложение, чего нет у одной из сторон.
6. Пилюли каналов: есть ли они, откуда берутся данные, каким примитивом рисуются.
7. Бейджи и состояния: пилюля-бейдж против mono-подписи цвета состояния.
8. Действия единицы списка: «Изменить», «Удалить», где их кладёт макет.
9. Пустое состояние: сколько их у макета, сколько у приложения, чем различаются.
10. Пагинация / бесконечная прокрутка.
11. Шкала отступов, радиусы, типографика — со ссылкой на переменные `:root`
    в `app/static/css/app.css`, а не на hex макета.
12. Адаптивные брейкпоинты: что перестраивает раздел сегодня и что должно после правки.

Вердикт каждой строки — одно из трёх, и третий обязан нести причину:
`исправляется` (с номером задачи), `сознательное отступление` (с причиной),
`вне объёма` (с причиной).

Отступления, которые обязаны попасть в таблицу ИМЕННО как отступления, с причиной:

- **Удаление остаётся на карточке.** Макет (строка 705) кладёт удаление только в
  редактор. План 01-03 специально удержал его на списке (WR-04), потому что без него
  на странице не оставалось ни одного пути удалить объявление; снятие уронило бы
  `ROW_DELETE_PLACES` и четыре инвентаризации панели подтверждения. Задача чинит ВИД,
  а не отбирает путь.
- **Цвет точки состояния.** У макета цвет объявления произволен (`a.color` из
  данных-примеров) — источника такой величины в модели `Ad` нет. Точка красится
  по состоянию объявления, той же величиной, что несёт подпись рядом: выдуманных
  данных не заводится.
- **Слова состояний.** Макет показывает «на паузе» / «пауза» — это его данные-примеры.
  Словарь состояний объявления в продукте другой (опубликовано / черновик,
  `AD_STATUS_PUBLISHED`), и он остаётся.
- **Подзаголовок шапки.** Макет пишет «6 из 15 по тарифу Basic» — тарифных лимитов
  в продукте нет с Фазы 05.1. Счётчик ставится в панель поиска, как у макета.
- **Заполнитель обложки.** У макета обложка — штриховка с надписью про хранилище;
  у объявления без вложений показывается та же штриховка без выдуманной надписи.

Файл не редактирует ни `new_broadcaster_design.html`, ни распакованную копию.
  </action>
  <verify>
    <automated>test -f .planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md &amp;&amp; N=$(grep -cE '^\|' .planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md) &amp;&amp; [ "$N" -ge 14 ] &amp;&amp; grep -qE 'ads/includes/ad_card\.html:[0-9]+' .planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md &amp;&amp; grep -qE 'unpacked\.html:(4[6-9][0-9]|51[0-8])' .planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-NOTES.md &amp;&amp; git diff --quiet -- new_broadcaster_design.html design/new_broadcaster_design.unpacked.html &amp;&amp; echo AUDIT_OK</automated>
  </verify>
  <done>Файл аудита существует, несёт таблицу минимум из двенадцати осей плюс шапку и разделитель, цитирует и распакованный макет по строке, и файлы приложения по `файл:строка`; исходник макета и распакованная копия не изменены.</done>
</task>

<task type="tracer" tdd="true">
  <name>Задача 2: Карточная сетка вместо строки-таблицы</name>
  <files>app/templates/ads/includes/ad_card.html, app/templates/ads/list.html, app/templates/ads/partial_cards.html, app/static/css/app.css, app/pages/ads.py, tests/test_pages/test_responsive_markup.py</files>
  <read_first>
    - `app/templates/schedules/includes/schedule_row.html` — как выглядит УЖЕ ПРИНЯТАЯ
      в проекте карточка списка: макрос с явными параметрами, ключи `kv__k`, без примитива
      строки-таблицы.
    - `app/static/css/app.css`, строки 1370-1410 — примитив пилюли канала `data-upbadge`
      и его перестроение на 400px.
    - `tests/test_pages/test_responsive_markup.py`: строки 52-66 (`MIGRATED_SECTIONS`),
      2795-2845 (`test_schedules_card_names_each_value`, `test_schedules_partial_names_each_value`),
      3345-3360 (вход объявлений в `ROW_TEMPLATES_WITHOUT_HEADER`), 3486-3495
      (вход `ads/list.html` в `ROWHEAD_PAGES`), и оба теста инвентаризации —
      `test_rowhead_pages_all_have_a_parametrization_entry`,
      `test_row_templates_without_header_are_accounted_for`.
  </read_first>
  <behavior>
    - `GET /ads` с одним объявлением отдаёт контейнер сетки и карточку; шапки колонок
      в ответе нет.
    - Карточка несёт настоящие данные объявления: название, выдержку текста, адрес
      редактирования — тест `test_ads_card_renders_data` остаётся зелёным без правок.
    - Каждое значение карточки названо своим ключом; подписи ячейки таблицы в карточном
      списке нет (новый `test_ads_card_names_each_value`, зеркало теста расписаний).
    - Порция бесконечной прокрутки несёт ту же карточку и те же ключи, что и первая
      страница, — правка живёт в макросе, и тест это доказывает.
    - Объявление, у которого есть расписание с привязанным аккаунтом, показывает пилюлю
      этого канала со счётчиком; объявление без расписаний не показывает ни одной.
    - Форма удаления и панель подтверждения на карточке сохраняются: инвентаризации
      `ROW_DELETE_PLACES`, `MODAL_IMPORTERS`, `MODAL_EVENT_NAMES`, `MODAL_PLACES`,
      `THUMB_CONSUMERS` остаются на прежних числах и правок не требуют.
  </behavior>
  <action>
Сквозной срез: одно объявление проходит путь «запрос → обработчик → макрос карточки →
классы CSS» и рисуется карточкой макета из конца в конец.

**`app/pages/ads.py` — каналы объявления.** В `_enrich_ads_with_stats` добавить ТРЕТИЙ
сгруппированный запрос по форме двух уже стоящих там: `Schedule` внешним соединением
с `MessengerAccount` по `Schedule.account_id`, отбор `Schedule.ad_id.in_(ad_ids)`,
группировка по паре `Schedule.ad_id` и `MessengerAccount.type`, счёт строк. Результат
разложить в `ad.channels` — список пар «тип канала, число расписаний», в порядке
`tg_user`, `wa`, `max`, и только по известным типам: у отвязанного расписания тип
отсутствует, а ветка неизвестного типа в `messenger_icon` тянет класс удалённого
фреймворка (о чём предупреждает комментарий в `dashboard/includes/upcoming_row.html`).
Объявлению без расписаний присвоить пустой список — `Undefined` в шаблоне печатается
пустотой и молча спрячет ошибку имени. Запроса на объявление в цикле не появляется.

**`app/templates/ads/includes/ad_card.html` — карточка.** Макрос сохраняет имя и
сигнатуру `ad_card(ad, user=None)`; параметр раскладки колонок и константы `AD_COLS` /
`AD_COLUMNS` уходят вместе с примитивом строки. Импорты `row_open` / `row_close` / `cell`
снимаются; `modal` и `thumb` остаются.

Строение карточки повторяет строки 481-508 макета:
- корневой `article` с признаком карточки объявления и якорем строки, который несёт
  сегодняшняя строка (идентификатор `ad-row-{id}` НЕ переименовывать: это точка подмены);
- обложка — блок фиксированной высоты; при непустом `ad.images` внутрь идёт
  `thumb(ad.images[0], class=…, lazy=true)`, иначе остаётся штриховка-заполнитель
  из класса;
- шапка карточки: точка состояния, название объявления, mono-подпись состояния,
  прижатая вправо. Ветвление по состоянию сохраняется КАК ЕСТЬ — проверяется
  ОПУБЛИКОВАННОСТЬ (`AD_STATUS_PUBLISHED`), нераспознанное значение падает в черновик;
  причина сохранена комментарием на месте: обратная проверка обещала бы рассылку,
  которой планировщик не сделает;
- тело: выдержка текста в 80 символов с многоточием, полный текст до 200 символов
  в подсказке — состав показанных данных не меняется;
- подвал, отбитый линией: пилюли каналов, mono-метаданные, действия справа.

Пилюля канала собирается ТЕМ ЖЕ приёмом, что в `dashboard/includes/upcoming_row.html:56`:
обёртка с признаком `data-upbadge` и атрибутом канала, внутри `messenger_icon(тип,
size='', show_label=true)`, следом число расписаний этого канала. Вызов ограничен
известными типами. Второй копии тонов пилюли в CSS НЕ ЗАВОДИТСЯ — правила
`app/static/css/app.css:1376-1394` уже описывают их, и именно поэтому признак берётся
существующий, а не новый.

Метаданные подвала — те значения, что сегодня стоят колонками: число успешных отправок,
число расписаний, дата создания через `format_datetime_for_user`. Каждое значение
названо своим ключом тем же приёмом, каким названы значения карточки расписания
(`kv__k`), — на карточном списке шапки колонок нет, и без ключа значение остаётся
безымянным. Подпись ячейки таблицы в карточку НЕ переносится.

Действия: ссылка «Изменить» и форма удаления с панелью подтверждения — обе в подвале,
обе в сегодняшней форме. Ни маршрут, ни метод, ни имя события открытия панели, ни
идентификатор панели не меняются: их держат четыре инвентаризации в
`tests/test_templates/test_components.py`.

**`app/templates/ads/list.html` и `partial_cards.html`.** Вместо `card_open` / `rowhead` /
`card_close` — контейнер сетки с признаком `data-ads-grid`; карточки идут прямыми детьми.
Сентинел бесконечной прокрутки остаётся ПОСЛЕДНИМ элементом внутри ТОГО ЖЕ контейнера,
что и карточки, и его разметка в двух файлах остаётся идентичной — оба инварианта уже
записаны комментариями в этих файлах и обязаны в них остаться. Импорты `card_open`,
`card_close`, `rowhead`, `AD_COLS`, `AD_COLUMNS` снимаются.

**`app/static/css/app.css`.** Новый блок раздела: сетка
`repeat(auto-fill, minmax(290px, 1fr))` с зазором из шкалы, карточка (фон `--surface`,
рамка `--border`, радиус `--r-2xl`, перестроение рамки и подъём на наведении), обложка
фиксированной высоты со штриховкой на переменных фона, точка состояния, mono-подпись,
тело, подвал с верхней линией `--border-muted`. Все величины — существующие переменные
`:root`; новых цветовых констант и инлайновых hex не появляется ни в CSS, ни в разметке.
Точка и mono-подпись состояния красятся модификаторами по состоянию объявления
(опубликовано — `--ok`, черновик — `--warn`), а не значением из данных.

**`tests/test_pages/test_responsive_markup.py` — инвентаризации, ТЕМ ЖЕ коммитом.**
Молчаливое исчезновение раздела из обхода запрещено, и в этом файле уже есть четыре
прецедента, как выводить раздел: планы 02-07 (расписания), 03-08 (группы), 04-05 и
04-07 (дашборд и запись истории). Повторить их форму дословно:
- убрать объявления из `MIGRATED_SECTIONS`, добавив на их место комментарий класса
  «вышел планом …, список стал карточным, свой примитив закреплён тестом …»;
- убрать вход `ads/list.html` из `ROWHEAD_PAGES` с таким же комментарием и опустить
  объявленное число в `test_rowhead_pages_all_have_a_parametrization_entry` на единицу,
  дописав к цепочке «восемь → семь» названную причину;
- убрать вход `ads/includes/ad_card.html` из `ROW_TEMPLATES_WITHOUT_HEADER` (он больше
  не рисует строку вовсе) и опустить объявленное число в
  `test_row_templates_without_header_are_accounted_for` на единицу с такой же причиной.

Добавить два теста по образцу расписаний, оба с ПОЛОЖИТЕЛЬНЫМ утверждением:
`test_ads_list_is_card_based` — у списка есть свой контейнер и своя карточка (без него
раздел просто выпал бы из проверок вместе со снятой строкой параметризации), и
`test_ads_card_names_each_value` — каждый ключ проставлен во ВСЕХ карточках выдачи,
поэтому сеются два объявления и утверждается число вхождений, а не факт.
  </action>
  <verify>
    <automated>cd /source/broadcaster &amp;&amp; uv run pytest tests/test_templates/ tests/test_pages/test_responsive_markup.py tests/test_pages/test_htmx_preserved.py -q 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`/ads` отдаёт карточную сетку с настоящими данными объявления; пилюли каналов приходят из сгруппированного запроса; `tests/test_templates/`, `test_responsive_markup.py` и `test_htmx_preserved.py` зелёные целиком; инвентаризации панели подтверждения, удаления и миниатюр не потребовали правок.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Панель поиска, счётчик и два пустых состояния</name>
  <files>app/pages/ads.py, app/templates/ads/list.html, app/templates/ads/partial_cards.html, app/static/css/app.css, tests/test_pages/test_responsive_markup.py</files>
  <read_first>
    - `app/pages/schedules.py` — обработчики сводного списка и его паршала: как в проекте
      уже собираются отбор, `filter_params`, `filters_active` и счётчик `total`.
    - `app/templates/schedules/list.html`, строки 30-79 — панель отбора, счётчик,
      проброс отбора в сентинел, ДВА пустых состояния.
    - `design/new_broadcaster_design.unpacked.html`, строки 468-477 и 510-515 — панель
      поиска и пустое состояние поиска. Диапазон уже прочитан в задаче 1: заново файл
      НЕ открывать, взять выписку из `260825-m0b-NOTES.md`.
  </read_first>
  <behavior>
    - Запрос без поиска отдаёт все объявления пользователя и счётчик, равный их числу.
    - Поиск по подстроке названия находит объявление; поиск по подстроке ТЕКСТА находит
      его же — макет обещает обе оси одним полем.
    - Поиск не выводит за границу владения: чужое объявление не находится ни по названию,
      ни по тексту.
    - Поиск, не нашедший ничего, отдаёт пустое состояние со сбросом поиска, а НЕ
      приглашение создать объявление.
    - Пользователь без объявлений вовсе получает приглашение создать — второе, другое
      пустое состояние.
    - Сентинел второй страницы несёт действующий поиск, и вторая порция отфильтрована
      так же, как первая.
  </behavior>
  <action>
Поиск, счётчик и два пустых состояния — то, что макет держит панелью на строках 468-477
и пустым состоянием на 510-515.

**`app/pages/ads.py`.** В `ads_list` и `ads_partial` принять необязательный параметр
поиска. Строить общее условие отбора ОДНИМ выражением, используемым и выдачей, и
счётом: владение (`Ad.user_id == user.id`) плюс, при непустом поиске, совпадение
подстроки в названии ИЛИ в тексте без учёта регистра. Пустая и состоящая из пробелов
строка поиска означает «отбора нет» — она не пишется в параметры и не превращается
в условие: `?search=` и отсутствие ключа обязаны значить одно и то же (то же правило,
что записано в `components/filter_chips.html`).

Счётчик — отдельный `func.count()` по ТОМУ ЖЕ условию, а не длина отданной страницы:
страница ограничена `PAGE_SIZE`, и длина соврала бы на любой выдаче длиннее одной
страницы. Передать в шаблон: сами объявления, `total`, `filter_search`, `filter_params`
(словарь непустых параметров отбора) и `filters_active`. Существующее ограждение
владения и параметр компоновки, принимаемый и игнорируемый, не трогать — причина
последнего записана комментарием на месте и остаётся верной.

**`app/templates/ads/list.html`.** Над сеткой — панель поиска: форма `GET` на `/ads`
с полем поиска (заполнитель из макета — про название и текст объявления), кнопкой
применения и сбросом; счётчик найденного mono-подписью, прижатой вправо. Собирать
её существующими макросами библиотеки (`components/field.html`, `components/button.html`,
`components/mono.html`), новых компонентов не заводить: библиотека инвентаризуется
двумя тестами, и молчаливое пополнение краснеет. Панель рисуется ВСЕГДА, в том числе
при пустой выдаче, — иначе отфильтровавший всё до нуля не сможет снять отбор; это же
правило записано у расписаний.

Сентинел в ОБОИХ файлах получает проброс отбора — цикл по `filter_params` с приведением
к строке и экранированием для адреса, перенесённый ДОСЛОВНО из
`app/templates/schedules/list.html:61`. Разметка сентинела в `list.html` и
`partial_cards.html` остаётся идентичной. Потерянный проброс не роняет страницу — он
молча подмешивает неотобранные объявления к отобранным, поэтому свойство закрепляется
тестом, а не вниманием.

Два пустых состояния через `empty_state`, различающиеся действием: при активном
отборе — «объявления не найдены», действие ведёт на чистый `/ads`; без отбора —
сегодняшнее приглашение создать первое объявление, слово в слово как сейчас.

**`app/static/css/app.css`.** Панель поиска раздела — своим классом на существующих
переменных: фон `--surface`, рамка `--border`, радиус из шкалы, перенос по строкам на
узкой ширине. Новых цветовых констант не появляется.

**Тесты.** Дописать в `tests/test_pages/test_responsive_markup.py` рядом с прочими
тестами раздела: поиск по названию, поиск по тексту, отсутствие чужого объявления
в выдаче поиска, два различающихся пустых состояния и перенос поиска во вторую порцию
бесконечной прокрутки (утверждать наличие параметра поиска в адресе сентинела И состав
второй порции).

Финальный прогон — весь набор. Один провал в нём ИЗВЕСТЕН и этой задаче не принадлежит:
`test_image_base_url_comes_from_app_settings` краснеет только в полном прогоне из-за
порядка исполнения сюиты (открытый пункт `full-suite-ads-editor-order-pollution.md`,
подтверждён отложенным при закрытии вехи v2.0). Его не чинить и на него не отвлекаться;
любой ДРУГОЙ красный тест принадлежит этой задаче.
  </action>
  <verify>
    <automated>cd /source/broadcaster &amp;&amp; uv run pytest tests/test_pages/ tests/test_templates/ -q 2>&amp;1 | tail -15 &amp;&amp; just test 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Поиск находит по названию и по тексту, чужого не показывает и переживает бесконечную прокрутку; счётчик считает всю выдачу, а не страницу; пустых состояния два и они различаются; полный прогон зелёный, за единственным исключением известного `test_image_base_url_comes_from_app_settings`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → `GET /ads`, `GET /ads/partial` | параметр поиска приходит от пользователя и попадает в условие запроса и обратно в разметку |
| БД → шаблон карточки | название, текст и вложения объявления — данные, введённые пользователем, и они печатаются на странице |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m0b-01 | Information Disclosure | `ads_list` / `ads_partial` — условие отбора | high | mitigate | Поиск ДОПОЛНЯЕТ ограждение владения, а не заменяет его: `Ad.user_id == user.id` остаётся первым слагаемым общего условия, используемого и выдачей, и счётом. Закреплено тестом «чужое объявление не находится ни по названию, ни по тексту» (задача 3). |
| T-m0b-02 | Tampering | параметр поиска → SQL | high | mitigate | Условие строится выражениями SQLAlchemy с параметризацией, строковой сборки запроса не появляется ни в одной ветке. |
| T-m0b-03 | Information Disclosure | счётчик найденного | low | mitigate | Счёт идёт по ТОМУ ЖЕ условию, что и выдача, включая ограждение владения: счётчик, посчитанный шире выдачи, назвал бы пользователю число чужих записей. |
| T-m0b-04 | Tampering | название и текст объявления в карточке | medium | mitigate | Автоэкранирование Jinja2 включено и не обходится: ни `\|safe`, ни `Markup(` в правках не появляется — сплошной обход `UNSAFE_MARKERS` в `tests/test_templates/test_components.py` держит это по всем шаблонам. |
| T-m0b-05 | Denial of Service | `_enrich_ads_with_stats` — каналы | medium | mitigate | Каналы приходят ОДНИМ сгруппированным запросом на страницу выдачи по форме двух уже стоящих там; запроса на объявление в цикле не заводится. |
| T-m0b-06 | Denial of Service | поиск по подстроке текста | low | accept | Отбор по подстроке идёт без индекса. Принято: выдача ограничена владением одного пользователя и `PAGE_SIZE`, а число объявлений на пользователя измеряется десятками. |
| T-m0b-SC | Tampering | установка пакетов | n/a | n/a | Задача не устанавливает ни одного пакета: правятся шаблоны, CSS, обработчик и тесты. Аудит легитимности не требуется — предмета нет. |
</threat_model>

<verification>
1. `uv run pytest tests/test_templates/ -v` — зелёный после КАЖДОЙ задачи, не только в конце.
2. `uv run pytest tests/test_pages/test_responsive_markup.py -q` — зелёный, включая обе
   инвентаризации шапок и строк с опущенными числами.
3. `just test` — полный прогон; единственный допустимый красный —
   `test_image_base_url_comes_from_app_settings` (известный, отложенный, к задаче
   не относится).
4. `git diff --stat -- new_broadcaster_design.html design/new_broadcaster_design.unpacked.html`
   пуст: канонический макет и распакованная копия не тронуты.
5. `grep -rnE '#[0-9a-fA-F]{6}' app/templates/ads/list.html app/templates/ads/partial_cards.html app/templates/ads/includes/ad_card.html` не даёт совпадений: оформление живёт классами.
6. Ручной осмотр `/ads` на 1440 / 900 / 375 px: сетка перестраивается сама, ничего не
   уезжает за край и не обрезается.
</verification>

<success_criteria>
- `260825-m0b-NOTES.md` перечисляет расхождения поимённо, с адресами по обеим сторонам
  и вердиктом у каждой строки; сознательные отступления названы отступлениями, а не
  умолчаны.
- `/ads` рисует карточную сетку макета с панелью поиска, счётчиком, пилюлями каналов
  и двумя различающимися пустыми состояниями.
- Бесконечная прокрутка, удаление через панель подтверждения и показ миниатюр работают
  как прежде; ни одна инвентаризация тестов не опущена молча.
- Редактор объявления, прочие списочные разделы и `components/table.html` не тронуты.
</success_criteria>

<output>
Создать `.planning/quick/260825-m0b-stranitsa-ads-ne-sootvetstvuet-shablonu-/260825-m0b-SUMMARY.md`
по завершении. В сводке — ссылка на `260825-m0b-NOTES.md` и итог по каждой строке
аудита: исправлено / отступление с причиной / вне объёма.
</output>
