---
phase: quick-260826-9vv-dashboard-drops-activity-block
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/dashboard.html
  - app/templates/dashboard/includes/activity_chart.html
  - app/static/css/app.css
  - app/pages/dashboard.py
  - app/application/analytics/send_analytics.py
  - tests/test_pages/test_dashboard.py
  - tests/test_pages/test_responsive_markup.py
  - tests/test_application/test_send_analytics.py
autonomous: true
requirements:
  - QUICK-DASH-ACTIVITY-REMOVE
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "На странице `/dashboard` карточки «Активность за неделю» больше нет: в отрендеренной разметке не остаётся ни столбцов графика, ни подписей суток, ни заголовка карточки, ни её пустого состояния «За неделю отправок не было»."
    - "Соседи по странице ЖИВЫ и это проверено в том же тесте: плитки, пара «Ближайшие отправки» / «Живая лента» и опрос ленты на месте. Запрет, прошедший на снесённой заодно странице, запретом не является."
    - "Путь рендера дашборда перестаёт СТРИМИТЬ недельное окно `send_logs`: раскладка часов читала окно в семь суток батчами (`yield_per`) на каждой загрузке страницы, и это было самое дорогое чтение экрана. Уходит блок — уходит и чтение."
    - "Модуль аналитики теряет ровно две секции — «Heatmap активности» и «Столбцы активности за неделю» — и ни одной строки сверх них. `send_metrics`, `last_send_at`, `upcoming_sends`, `recent_feed`, фильтры истории, `history_count` и ось календарного месяца НЕ ТРОНУТЫ: их читают история, админка и биллинг."
    - "`_get_timezone_for_user` ОСТАЁТСЯ в `app/pages/common.py`: её зовут форматирование времени (строки 258, 313) и сам модуль аналитики (строки 805, 927). Снимается только её импорт в `app/pages/dashboard.py`, где после снятия блока потребителя не остаётся."
    - "`plural_ru` ОСТАЁТСЯ: её зовут одиннадцать шаблонов, включая шапку шелла `app/templates/base.html:172`. Подпись столбца графика была лишь одним из потребителей."
    - "Первое правило `@media (prefers-reduced-motion: reduce)` (`app/static/css/app.css:687-689`) НЕ ТРОНУТО и по-прежнему перечисляет три живых имени: `test_the_reduced_motion_rule_names_only_living_classes` ищет ПЕРВОЕ вхождение регуляркой и требует ровно трёх. Снимается отдельное правило графика на строках 1488-1490, имени графика в первом правиле никогда не было."
    - "Утверждения снятых тестов НЕ ТЕРЯЮТСЯ вместе с предметом: запрет инлайн-заливки (D-06, `style=\"background`) и запреты на возврат снятой сетки 7×24 (`data-heatscroll`, `data-heatcell`) переезжают в новые тесты-запреты."
    - "Мёртвых артефактов не остаётся: шаблон макроса удалён как файл, его правила и его собственное правило пониженной анимации сняты из стилей, его имена сняты из импортов модуля маршрута и модуля аналитики, а `tzinfo` уходит из импорта `send_analytics.py` вместе со своим единственным потребителем."
    - "Снятие ОБЪЯВЛЕНО тестами, а не молчаливо: появляются четыре положительных запрета — блока нет в разметке, шаблона нет ни на диске, ни под каталогом дашборда, правил нет в стилях, имён нет в модуле аналитики."
    - "`uv run pytest tests/ -q` зелёный целиком, кроме пред-существующего красного `tests/test_planning/test_state_progress_matches_roadmap.py` (запись 9 в `.planning/WINDOWS.md`), который НЕ чинится."
  artifacts:
    - "app/templates/dashboard.html"
    - "app/static/css/app.css"
    - "app/pages/dashboard.py"
    - "app/application/analytics/send_analytics.py"
    - "tests/test_pages/test_dashboard.py"
    - "tests/test_pages/test_responsive_markup.py"
    - "tests/test_application/test_send_analytics.py"
  key_links:
    - "`{% set next_label, next_href = next_step %}` (`app/templates/dashboard.html:38`) ↔ пустое состояние ближайших отправок (строка 95): пару читают ДВА блока, снимаемый и остающийся. Снятая заодно со своим вторым потребителем, она уронит страницу исключением Jinja у любого пользователя без расписаний."
    - "Комментарий на месте снятого блока в `dashboard.html` ↔ `test_dashboard_blocks_share_one_head_without_a_divider` (`tests/test_pages/test_responsive_markup.py:4638`): тест считает вхождения `data-blockhead` в СЫРОМ тексте шаблона и требует ровно двух. Слово `data-blockhead`, написанное в объяснении снятия, даст третье вхождение и покраснит тест на комментарии."
    - "Комментарий на месте снятого раздела в `app.css` ↔ тот же тест (строки 4629-4630): он ищет `[data-feedhead]` и `[data-heathead]` в СЫРОМ тексте стилей. Скобочные селекторы в объяснении запрещены — раздел называется прозой."
    - "`app/templates/dashboard/includes/activity_chart.html` ↔ соседи по каталогу `feed_row.html`, `metric_tile.html`, `upcoming_row.html`: удаляется ОДИН файл, три остаются живыми потребителями `dashboard.html` и `partial_feed.html`."
    - "`CHART_BUCKETS_PER_DAY` ↔ `tests/test_pages/test_responsive_markup.py:24`: константу импортирует ЧУЖОЙ тестовый файл. Снятая из модуля прежде, чем снят этот импорт, она уронит сбор всего набора `test_pages` на ImportError — отсюда порядок задач 2 и 3."
    - "`_page_body` (`tests/test_pages/test_dashboard.py:37`) ↔ пустые состояния страницы: после снятия графика на пустом дашборде остаются ДВА разных пустых состояния — «Ближайших отправок не запланировано» и «Отправок пока нет» из паршала ленты. `test_dashboard_empty_upcoming_block_has_its_own_text` требует не менее двух и остаётся зелёным без правки."
---

<objective>
Убрать со страницы дашборда карточку «Активность за неделю» вместе с плумбингом, который существует ТОЛЬКО ради неё.

Purpose: бар-чарт отвечает на вопрос «когда система работала», но платит за ответ самым дорогим чтением экрана — потоковой раскладкой недельного окна `send_logs` на каждой загрузке страницы. Владелец решил, что вопрос не стоит цены. Вместе с блоком уходит и чтение.

Output: дашборд без карточки графика; модуль маршрута без сбора данных для неё; удалённый шаблон макроса и его стили; модуль аналитики без двух секций, потерявших единственного потребителя; четыре теста-запрета на возврат блока.

**ГРАНИЦА ЗАДАЧИ.** Снимается ПОКАЗ недельной активности на дашборде и та арифметика, которая существует только ради него. Не трогаются: плитки суточных метрик (`send_metrics`), ближайшие отправки, живая лента, раздел истории, админка и биллинг — они читают ДРУГИЕ функции того же модуля. Ни один файл вне восьми перечисленных в `files_modified` меняться не должен.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

@app/templates/dashboard.html
@app/pages/dashboard.py
</context>

<tasks>

<task type="tracer">
  <name>Задача 1: снять карточку «Активность за неделю» со всех четырёх слоёв блока</name>
  <files>app/templates/dashboard.html, app/templates/dashboard/includes/activity_chart.html, app/static/css/app.css, app/pages/dashboard.py</files>
  <action>
Один сквозной срез через все слои блока: разметка страницы → шаблон макроса → стили → сбор данных в маршруте. Номера строк даны по текущему состоянию файлов; ВНУТРИ КАЖДОГО ФАЙЛА правь СНИЗУ ВВЕРХ, иначе после первого же удаления номера уедут.

**1. `app/templates/dashboard.html` (153 строки).**

- Снять строки 137-150 целиком: комментарий блока (137-143), ветку `{% if chart_view.peak %}` (144), вызов макроса (145), `{% else %}` (146), пустое состояние «За неделю отправок не было» (147-149) и `{% endif %}` (150).
- Снять строку 5 — импорт макроса графика.
- ОСТАЮТСЯ нетронутыми: строка 38 (`{% set next_label, next_href = next_step %}` — пару читает ВТОРОЙ потребитель, пустое состояние ближайших отправок на строке 95), импорты `card_open/card_close` (2), `empty_state` (3), `mono` (4), `metric_tile` (6), `upcoming_row` (7), обёртка `data-dashpair` (74-135), плитки (41-55), контейнер ленты (128-132) и закрывающий `</div>` стопки (152).
- На месте снятого оставить комментарий Jinja, называющий причину снятия и тесты, которые держат запрет.

⚠️ **ЗАПРЕЩЁННЫЕ СЛОВА В ЭТОМ КОММЕНТАРИИ.** `test_dashboard_blocks_share_one_head_without_a_divider` считает вхождения атрибута общей шапки блока в СЫРОМ тексте `dashboard.html` и требует ровно двух (`tests/test_pages/test_responsive_markup.py:4638`). Имя этого атрибута в объяснении даст третье вхождение и покраснит тест на собственном комментарии. Не писать его. По той же причине не писать в комментарии разметку элементов таблицы: `test_template_inventory` ищет её по сырому тексту каждого шаблона.

**2. `app/templates/dashboard/includes/activity_chart.html` — удалить файл целиком** (64 строки). Недостижимых шаблонов в проекте не оставляют — тем же приёмом план 04-05 вывел `dashboard/includes/recent_send_card.html`, а задача 260826-6jq — `dashboard/includes/worker_row.html`.
Три соседа по каталогу — `feed_row.html`, `metric_tile.html`, `upcoming_row.html` — остаются: их зовут `partial_feed.html` и сама страница.

**3. `app/static/css/app.css` (2532 строки).**

- Снять строки 1448-1490 одним куском: комментарий раздела (1448-1458), шесть правил графика (1459-1486) и отдельное правило пониженной анимации, адресованное столбцу (1488-1490).
- На месте снятого оставить комментарий, называющий причину и тест-запрет.
- ⚠️ **В КОММЕНТАРИИ НЕ ПИСАТЬ НИ ОДНОГО СКОБОЧНОГО СЕЛЕКТОРА.** Тот же `test_dashboard_blocks_share_one_head_without_a_divider` ищет два имени в атрибутных скобках по СЫРОМУ тексту стилей (строки 4629-4630) и краснеет на любом их появлении, включая объяснение. Раздел называется прозой: «бар-чарт недельной активности дашборда».
- НЕ ТРОГАТЬ: правила ленты выше (1435-1446), `[data-stack]` (1493) и раздел пары дашборда ниже (1494+).
- НЕ ТРОГАТЬ первое правило `@media (prefers-reduced-motion: reduce)` (687-689) и его комментарий (683-686): имени графика там никогда не было, а `test_the_reduced_motion_rule_names_only_living_classes` ищет ПЕРВОЕ вхождение правила регуляркой и требует в нём ровно трёх имён. Тронутое, оно покраснеет; снятое правило графика на 1488-1490 — второе по счёту и под регулярку не попадает.

**4. `app/pages/dashboard.py` (129 строк).**

- Снять строку 115 — ключ контекста с представлением графика.
- Снять строки 81-92 — комментарий сбора недельной активности и сам вызов; лишнюю пустую строку не оставлять, между `metrics` и блоком ближайших отправок остаётся одна.
- Снять из импорта модуля аналитики (5-11) две строки: 6 и 7. `recent_feed`, `send_metrics`, `upcoming_sends` ОСТАЮТСЯ.
- Снять из импорта `app.pages.common` (14-19) строку 15: после снятия блока у помощника зоны в этом модуле потребителя не остаётся. `check_is_admin`, `get_user_from_cookie`, `templates` ОСТАЮТСЯ.
- ⚠️ Сама функция `_get_timezone_for_user` в `app/pages/common.py` НЕ ТРОГАЕТСЯ: её зовут форматирование времени (`common.py:258` и `:313`) и модуль аналитики отложенным импортом (`send_analytics.py:805` и `:927`). Снимается импорт в маршруте, а не функция.
- Остальное не трогать: `dashboard_next_step` со своим докстрингом (25-61), `metrics`, `upcoming`, `feed`, `next_step`, `feed_poll_seconds`, `active_page`.

**Проверка целостности после правки.** Модуль аналитики на этой задаче НЕ ТРОГАЕТСЯ — `activity_chart` и `activity_heatmap` остаются в `send_analytics.py` до задачи 3, иначе `tests/test_pages/test_responsive_markup.py:24` уронит сбор всего набора `test_pages` на ImportError.
  </action>
  <verify>
    <automated>test ! -f app/templates/dashboard/includes/activity_chart.html && test -f app/templates/dashboard/includes/feed_row.html && uv run pytest tests/test_pages/test_dashboard.py -q</automated>
  </verify>
  <done>Шаблон макроса графика удалён, три соседа по каталогу на месте, страница `/dashboard` рендерится и весь набор `tests/test_pages/test_dashboard.py` зелёный (его два упоминания графика — отрицательные и остаются истинными).</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: снять тесты снятого блока, сохранить их утверждения и объявить запрет</name>
  <files>tests/test_pages/test_dashboard.py, tests/test_pages/test_responsive_markup.py</files>
  <behavior>
    - Пользователь с отправкой за последние сутки открывает `/dashboard`: статус 200, и в ОТРЕНДЕРЕННОЙ разметке нет ни одного признака бар-чарта — ни контейнера, ни столбца, ни доли, ни ряда подписей, ни заголовка карточки, ни подписи единицы измерения.
    - На той же разметке живы соседи: сетка плиток, обёртка пары, контейнер ленты с опросом.
    - На той же разметке нет инлайн-заливки: признак инлайн-цвета не встречается нигде на странице (переехавший запрет D-06).
    - Файла шаблона макроса нет на диске, и ни один `*.html` под каталогом дашборда (разбор БЕЗ комментариев Jinja) не несёт признаков разметки графика; при этом живой сосед по каталогу на месте.
    - В стилях (разбор БЕЗ комментариев) нет ни одного селектора графика, нет селекторов снятой прежде сетки 7×24, и при этом живы правила пары дашборда и строки ленты.
    - У пользователя без единой сущности дашборд по-прежнему ведёт к подключению канала, а у пользователя с заведённым расписанием призыва к действию на странице нет.
  </behavior>
  <action>
**A. `tests/test_pages/test_dashboard.py` — две точечные правки.**

1. `test_dashboard_empty_grid_is_replaced_by_an_empty_state` (499-506). Предмет теста — пустое состояние ВМЕСТО пустого графика — перестал существовать, но второе его утверждение остаётся единственной проверкой ПЕРВОЙ ветки D-40 на отрендеренной разметке (две соседние проверяют ветки `/ads/new` и `/ads`). Переименовать в `test_dashboard_empty_blocks_lead_to_connecting_a_channel`, снять строку 505, оставить строку 506, докстринг переписать под новый предмет: у пользователя без единой сущности призыв ведёт на подключение канала.

2. `test_dashboard_empty_state_has_no_action_when_everything_is_set_up` (552-568). Снять строку 568. Утверждение 567 (`empty__action` не в разметке) — настоящий предмет теста, оно остаётся; докстринг не трогать.

3. НЕ ТРОГАТЬ `test_dashboard_empty_upcoming_block_has_its_own_text` (571-580): он требует не менее двух РАЗНЫХ пустых состояний, и после снятия графика их по-прежнему два — «Ближайших отправок не запланировано» из страницы и «Отправок пока нет» из `dashboard/partial_feed.html:43`. Если тест всё же покраснеет, это факт страницы, а не устаревшее утверждение: чинить страницу, не тест.

**B. `tests/test_pages/test_responsive_markup.py` — снять (правь СНИЗУ ВВЕРХ).**

- `test_activity_chart_columns_are_fractional_not_fixed` (4580-4604) — предмет снят целиком. Его два запрета на возврат прежней сетки 7×24 (`data-heatscroll`, `data-heatcell`) ПЕРЕЕЗЖАЮТ в новый тест-запрет по стилям (пункт D): снятые заодно, они разрешат вернуть под видом графика ту самую сетку, ради снятия которой их и писали.
- `test_dashboard_chart_bars_carry_height_but_never_inline_colour` (1045-1067) — предмет снят. Его запрет инлайн-заливки (D-06) ПЕРЕЕЗЖАЕТ в новый тест-запрет по разметке (пункт D).
- `test_dashboard_activity_chart_renders_bars_without_table_elements` (1016-1042) — предмет снят. Его запрет на элементы таблицы НЕ теряется: `test_template_inventory` (3015) обходит ВСЕ шаблоны проекта по сырому тексту, и удалённый файл он больше не увидит, а любой новый увидит.
- Комментарий секции (1010-1014) — переписать: он утверждает, что часовая раскладка ОСТАЛАСЬ в модуле аналитики. После задачи 3 это перестанет быть правдой, а комментарий, называющий несуществующее, лжёт следующему читателю ровно там, где написан против лжи. Секция после снятия двух тестов остаётся при одном соседе — привести заголовок и прозу к тому, что в ней осталось.
- Строка 24 — снять импорт константы числа долей: после задачи 3 её в модуле не будет, а импорт уронит сбор всего набора.

**C. `tests/test_pages/test_responsive_markup.py` — переписать один тест.**

`test_dashboard_blocks_share_one_head_without_a_divider` (4606-4655):
- снять привязку `chart` (4620-4622) и утверждение 4639 — предмета больше нет;
- утверждение 4638 (`page.count(...) == 2`) НЕ ТРОГАТЬ: собственная шапка графика жила в его паршале, а не на странице, поэтому переписи страницы снятие блока не меняет;
- докстринг (4607-4616) и комментарий переписи (4632-4637) — привести к новому положению дел: пара «Ближайшие отправки» / «Живая лента» осталась одна, графика в перечислении больше нет;
- утверждения 4624-4630, 4648-4655 не трогать: они про примитив шапки и про `card_open(title=)` в самой странице, к графику отношения не имеют.

**D. `tests/test_pages/test_responsive_markup.py` — дописать три положительных запрета.**

Первый — рядом с остальными тестами страницы дашборда (после снятой секции), два других — рядом с `test_the_dashboard_worker_list_left_no_css_behind` (2069), где уже стоят помощники `_css_without_comments` (1840) и `_markup_without_comments` (1837).

1. `test_the_dashboard_carries_no_weekly_activity_block` — асинхронный, берёт `authed_client` и `db_session`, сеет отправку существующим помощником `_seed_send_log` (иначе запрет прошёл бы на пустых данных, при которых блок и раньше не рисовался — это и есть вакуумность), запрашивает `/dashboard`, утверждает статус 200 и:
   - ни одного признака графика в ОТРЕНДЕРЕННОЙ разметке — контейнера, столбца, доли, ряда подписей, заголовка карточки и подписи единицы измерения;
   - признак инлайн-заливки в разметке не встречается (переехавший запрет D-06);
   - живы соседи: сетка плиток, обёртка пары и контейнер ленты — без второй половины запрет прошёл бы и на снесённой заодно странице.
   Разбирается именно отрендеренный ответ: комментарии Jinja до разметки не доезжают, поэтому объяснение снятия, оставленное в шаблоне задачей 1, тест не краснит.

2. `test_the_activity_chart_template_is_gone` — синхронный: файла макроса на диске нет; ни один `*.html` под `app/templates/dashboard/` не содержит признаков разметки графика (каждый файл разбирается через `_markup_without_comments`); живой сосед `feed_row.html` на месте — иначе запрет прошёл бы на вычищенном каталоге.

3. `test_the_dashboard_activity_chart_left_no_css_behind` — синхронный, читает стили через `_css_without_comments()`: ни одного селектора графика; ни одного селектора снятой прежде сетки 7×24 (переехавшие запреты); при этом живы правило пары дашборда и правило строки ленты. Через помощник, а не сырым текстом: объяснение снятия, оставленное комментарием задачей 1, тест краснить не должен.

Признаки графика вынести в ОДИН модульный кортеж и читать его всеми тремя тестами. Три теста проверяют разные поверхности, но предмет у них один, и разъехавшиеся списки дали бы запрет, дырявый там, где список короче — тот же приём, каким задача 260826-6jq свела признаки перечня воркеров в `WORKER_LIST_MARKERS`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py tests/test_pages/test_dashboard.py -q</automated>
  </verify>
  <done>Оба набора зелёные; в `test_responsive_markup.py` появились три теста-запрета и один общий кортеж признаков, снятые тесты не оставили за собой ни одного утверждения без нового владельца.</done>
</task>

<task type="auto">
  <name>Задача 3: снять из модуля аналитики две секции, потерявшие потребителя, и прогнать весь набор</name>
  <files>app/application/analytics/send_analytics.py, tests/test_application/test_send_analytics.py</files>
  <action>
После задач 1 и 2 у секций «Heatmap активности» и «Столбцы активности за неделю» не остаётся ни одного потребителя, кроме их собственных тестов. Обход `grep -rn "activity_chart\|activity_heatmap\|HeatmapView\|ActivityChartView\|CHART_BUCKETS_PER_DAY" app/ tests/` обязан не находить ничего вне самих этих двух файлов ДО правки — если находит, значит задача 1 или 2 сделана не полностью, и снимать секции рано.

**Про обещание, которое здесь закрывается.** Комментарий маршрута и комментарий секции тестов обещали, что часовая раскладка «остаётся доступной Фазе 6». Фаза 6 закрыта и заархивирована (веха v2.0), `app/pages/admin.py:42-47` импортирует из модуля четыре ДРУГИЕ функции, и обещание истекло невыполненным. Держать ради него код, до которого нет ни одного пути, значит держать мусор с объяснением, а не без.

**1. `app/application/analytics/send_analytics.py` (правь СНИЗУ ВВЕРХ).**

- Снять строки 285-500 одним куском: обе секции целиком — `SHORT_WEEKDAYS`, `HEATMAP_YIELD_PER`, `HeatmapView`, `activity_heatmap`, `CHART_BUCKETS_PER_DAY`, `CHART_BUCKET_HOURS`, `ActivityChartView`, `activity_chart` вместе со всеми их комментариями. Верхняя граница — строка 284 (конец `last_send_at`), нижняя — строка 501 (`# --- Ближайшие отправки ---`), обе НЕ ТРОГАТЬ.
- Снять `tzinfo` из импорта на строке 47: он стоял ради одного параметра снятой раскладки. `datetime`, `timedelta`, `timezone` ОСТАЮТСЯ — их зовут окно суток, периоды истории и ось календарного месяца.
- Проверить импорты моделей после правки: `MessengerAccount`, `Group`, `Ad`, `Schedule`, `SendLog`, `User`, `calendar`, `case`, `func`, `select`, `Select` — все имеют потребителей в оставшихся секциях, снимать их не нужно. Если линтер укажет на неиспользуемое имя — снять именно его и назвать в SUMMARY.
- НЕ ТРОГАТЬ ничего ниже 501: пометки причин, `upcoming_sends`, живую ленту, фильтры истории, `history_count`, ось календарного месяца. Их читают история, админка и биллинг.

**2. `tests/test_application/test_send_analytics.py` (1325 строк, правь СНИЗУ ВВЕРХ).**

- Снять строки 1178-1325 — секцию свёртки в столбцы до конца файла: комментарий секции, помощник `_grid`, пять тестов свёртки, комментарий секции сквозного порядка и два сквозных теста.
- Снять строки 613-841 — секцию раскладки часов: комментарий секции, `SHORT_DAYS`, `MOSCOW`, `_filled_cells` и девять её тестов. Нижняя граница — строка 842 (`# --- upcoming_sends ---`), не трогать.
- Снять строки 196-215 — `test_activity_heatmap_ignores_records_from_the_future`: он стоит в секции окна суток, но предмет у него снимаемый. Соседний `test_send_metrics_ignores_records_from_the_future` (173-193) ОСТАЁТСЯ: он про плитки, которые никуда не уходят.
- Снять из импорта (19-38) пять имён: строки 25, 26, 27, 30, 31. `FAILED_STATUSES`, `HISTORY_PERIODS`, три статуса, `SendMetrics`, `UpcomingSend`, `apply_history_filters`, `history_count`, `history_filter_params`, `normalize_utc`, `send_metrics`, `upcoming_sends` ОСТАЮТСЯ.
- `ZoneInfo` в импорте ОСТАЁТСЯ: его зовут строки 119 и 524. `event` и `select` остаются тоже.
- Дописать четвёртый положительный запрет — `test_the_module_no_longer_carries_the_weekly_activity_surface`: синхронный, импортирует модуль аналитики и утверждает, что пяти снятых имён у него нет (`hasattr` ложен для каждого), а четыре живых — `send_metrics`, `upcoming_sends`, `recent_feed`, `history_count` — на месте. Проверка через объекты модуля, а не по тексту: объяснение снятия, если его оставят комментарием, запрет краснить не должно. Этим же запретом закрыт и модуль маршрута: несуществующее имя он импортировать не сможет, и любой страничный тест это увидит первым.

**3. Полный прогон.** Прогнать ВЕСЬ набор, а не только правленые файлы: снятые имена импортировали два чужих тестовых файла, и красное могло уехать туда, куда правка не заглядывала. `tests/test_pages/test_history.py`, `tests/test_application/test_send_analytics_month.py` и `tests/test_application/test_admin_uses_analytics.py` держат ДРУГИЕ функции того же модуля — они обязаны остаться зелёными без единой правки, и это лучшее доказательство, что граница задачи соблюдена.

Если что-то краснеет — чинить ПО МЕСТУ, не расширяя объём: правка касается дашборда и двух секций аналитики, и любое красное за их пределами означает, что задета лишняя строка, а не что тест устарел.

**Известное красное, которое НЕ чинится.** `tests/test_planning/test_state_progress_matches_roadmap.py::test_the_machine_readable_progress_is_derived_from_the_roadmap` красный до начала этой задачи: `.planning/ROADMAP.md` опустошён при архивации вехи, а `STATE.md` хранит 110/110. Заведён записью 9 в `.planning/WINDOWS.md` со статусом `open`. Зелёный прогон этой задачи означает «2270-ish passed, этот один по-прежнему failed, нового красного нет». Коммиты задачи не должны трогать `.planning/` ни одним байтом, кроме собственных артефактов самой задачи.

**4. По правилу проекта** (`CLAUDE.md`) после изменения кода обновить граф: `graphify update .` (только AST, без обращений к API).
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <done>`uv run pytest tests/ -q` зелёный целиком, кроме пред-существующего красного в `tests/test_planning/`; `graphify update .` отработал; `git status` не показывает незакоммиченных правок вне восьми файлов плана, артефактов задачи и `graphify-out/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| путь рендера `/dashboard` → HTML | всё, что маршрут положил в контекст, печатается в разметку страницы |
| владелец → журнал отправок `send_logs` | выборка по `user_id` — единственное, что отделяет активность одного пользователя от другого |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9vv-01 | Denial of Service | путь рендера `/dashboard` | medium | mitigate | Потоковое чтение недельного окна `send_logs` батчами по 1000 записей уходило на КАЖДУЮ загрузку дашборда и росло вместе с журналом пользователя. Снятие секции убирает поверхность целиком; закреплено `test_the_module_no_longer_carries_the_weekly_activity_surface`. |
| T-9vv-02 | Information Disclosure | контекст шаблона дашборда | low | mitigate | Ключ представления графика уходит из контекста: почасовой профиль активности владельца больше не печатается в разметку. Закреплено `test_the_dashboard_carries_no_weekly_activity_block` на отрендеренном ответе. |
| T-9vv-03 | Tampering | тесты, снимаемые вместе с предметом | medium | mitigate | Три запрета снятых тестов (инлайн-заливка D-06, два имени снятой сетки 7×24) переезжают в новые тесты-запреты, а не исчезают вместе с их предметом. |

Установок пакетов задача не делает: `pyproject.toml`, `uv.lock` и `package.json` в `files_modified` не входят, поэтому строка цепочки поставки в реестре отсутствует по построению.
</threat_model>

<verification>
1. `uv run pytest tests/ -q` — зелёный, кроме пред-существующего красного `tests/test_planning/test_state_progress_matches_roadmap.py`.
2. `test ! -f app/templates/dashboard/includes/activity_chart.html` — шаблон макроса удалён.
3. `git diff --stat` показывает ровно восемь файлов плана и ни одного сверх них; в частности `app/pages/common.py`, `app/templates/base.html`, `app/pages/admin.py` и `app/pages/history.py` не изменены ни одним байтом.
4. `grep -rn "activity_chart\|activity_heatmap\|HeatmapView\|ActivityChartView\|CHART_BUCKET" app/ tests/` не находит ничего.
5. `graphify update .` отработал.
</verification>

<success_criteria>
- Страница `/dashboard` рендерится со статусом 200 у пользователя с отправками и без них, и карточки «Активность за неделю» на ней нет ни в одном из двух случаев.
- Плитки, пара «Ближайшие отправки» / «Живая лента» и опрос ленты работают как прежде.
- Четыре положительных запрета в наборе: разметка, шаблон, стили, модуль аналитики.
- Ни одно утверждение снятых тестов не потеряно: три переехали поимённо.
- Полный прогон зелёный при единственном пред-существующем красном.
</success_criteria>

<output>
Create `.planning/quick/260826-9vv-ubrat-so-stranitsy-dashboard-blok-aktivn/260826-9vv-SUMMARY.md` when done
</output>
