# Roadmap: Broadcaster

## Milestones

- ✅ **v1.0 Baseline** — Фазы 1-6 (ретроспективная карта отгруженной системы) — [архив](milestones/v1.0-ROADMAP.md)
- ✅ **v2.0 Redesign** — Фазы 1-6 + 05.1 (отгружено 2026-08-25) — [архив](milestones/v2.0-ROADMAP.md)
- 🚧 **v2.1 HTMX-first** — Фазы 7-15 (роадмап составлен 2026-08-26)

## Phases

<details>
<summary>✅ v1.0 Baseline (Фазы 1-6) — ретроспективная карта</summary>

Шесть вертикальных пользовательских возможностей уже отгруженной системы. Это карта
текущего состояния, а не план работ: все перечисленные требования были доставлены
до введения GSD-процесса в проект.

- [x] Phase 1: Secure Access & Scheduling Profile
- [x] Phase 2: Advertisement Library
- [x] Phase 3: Messenger Accounts & Group Targeting
- [x] Phase 4: Scheduled Multi-Messenger Delivery
- [x] Phase 5: Subscription & Message Balance
- [x] Phase 6: Administration & Operations

Полные детали: [`milestones/v1.0-ROADMAP.md`](milestones/v1.0-ROADMAP.md)

</details>

<details>
<summary>✅ v2.0 Redesign (Фазы 1-6 + 05.1) — SHIPPED 2026-08-25</summary>

Нумерация начата заново с 1: фазы v1 были ретроспективной документацией уже
отгруженной системы, а не выполненными GSD-фазами.

- [x] Phase 1: Интерфейсный фундамент (13/13 планов) — completed 2026-08-10
- [x] Phase 2: Объявления и расписания (15/15 планов) — completed 2026-08-11
- [x] Phase 3: Группы аккаунта (12/12 планов) — completed 2026-08-13
- [x] Phase 4: Дашборд и история (12/12 планов) — completed 2026-08-15
- [x] Phase 5: Тарифы (30/30 планов) — completed 2026-08-25 *(модель тарификации ЗАМЕНЕНА фазой 05.1 решением владельца 2026-08-20; платёжные рельсы ЮKassa переиспользованы)*
- [x] Phase 05.1: Единая подписка (INSERTED, 14/14 планов) — completed 2026-08-21
- [x] Phase 6: Админ-панель (14/14 планов) — completed 2026-08-24

**Итого:** 7 фаз, 110 планов, 262 задачи, 41/41 требование `Complete` (+1 `VOID`).

**Десятичная фаза 05.1 (INSERTED)** вставлена после Фазы 5, а не вместо неё: владелец
остановил UAT фазы 5 на формулировке «получилась очень сложная для понимания система
тарифов». Фаза 5 не откатывалась и не переписывалась — её артефакты остаются историей
принятого и отменённого решения.

Полные детали: [`milestones/v2.0-ROADMAP.md`](milestones/v2.0-ROADMAP.md)

</details>

### 🚧 v2.1 HTMX-first (Фазы 7-15)

**Milestone Goal:** перевести всё письмо и все ручные обращения к серверу на htmx — так,
чтобы ни одно действие пользователя не перезагружало страницу и ни один шаблон не собирал
запрос руками. Деградация без JS сохраняется: каждая форма остаётся рабочим `method="post"`
с настоящим `action`, htmx только перехватывает.

**Нумерация продолжает v2.0 и НЕ начинается заново.** v2.0 закончилась Фазой 6, поэтому
веха открывается Фазой 7. Внутри вехи 9 фаз: 7…15.

**Отправная точка (снята по коду 2026-08-26):** 47 форм, через htmx не идёт ни одна;
36 POST-обработчиков; 127 `RedirectResponse`; 79 существующих `hx-*` атрибутов в 22 шаблонах,
из них `hx-post` — 2; 6 ручных `fetch()`; `HX-Request` во всей суите — **1 вхождение**
на 131 файл и ~1700 тестов.

- [x] **Phase 7: Обновление htmx до 2.0.10 и блок конфигурации** - единственный шаг вехи, который не проверяет ни один машинный тест; `responseHandling` в 1.9.10 физически отсутствует (completed 2026-08-28)
- [ ] **Phase 8: Фундамент ответа, канал уведомлений, пакет гейтов и денежный потолок** - 13 из 19 ловушек закрываются здесь, и в большинстве это ГЕЙТ, обязанный существовать до первой переведённой формы
- [ ] **Phase 9: Пилот на `account_groups` — сквозной контракт формы** - наименьшая поверхность, на которой контракт проверяется целиком: 2 обработчика, 1 шаблон, готовые `id`
- [ ] **Phase 10: Рычаг `components/modal.html`** - одна правка ⇒ 16 мест подтверждения / 10 потребителей
- [ ] **Phase 11: Массовый перевод разделов письма** - `schedules` + `ads` → `admin` → `accounts` последним; `billing` не первым
- [ ] **Phase 12: Загрузка изображений без `fetch()`** - параллельный поток: не касается ни `app/pages/`, ни оболочки
- [ ] **Phase 13: Мастер подключения Telegram по QR на фрагментах** - 5 обработчиков над живой сессией Telethon, минус ~150 строк JS
- [ ] **Phase 14: Авторизация на htmx** - осознанно последняя: второй шелл, смена cookie личности, сломанный вход блокирует весь ручной UAT
- [ ] **Phase 15: Упрочнение и сводный обход 47 форм** - закрытие инвентарей, парные `*_degrades_without_htmx`, девять именованных пунктов ручного UAT

## Phase Details

### Phase 7: Обновление htmx до 2.0.10 и блок конфигурации

**Goal**: интерфейс работает на htmx 2.0.10 с явно выписанной конфигурацией, и подмена вендоренного файла больше не может остаться незамеченной браузером пользователя
**Depends on**: Nothing (первая фаза вехи; поверх отгруженного состояния v2.0)
**Requirements**: FOUND-01, FOUND-02, FOUND-03, QUAL-05
**Success Criteria** (what must be TRUE):

  1. Вендоренный `app/static/js/htmx.min.js` несёт `version:"2.0.10"`, и тест утверждает версию по СОДЕРЖИМОМУ файла, а не по имени (машинно).
  2. Блок `<meta name="htmx-config">` из шести ключей стоит в `base.html` **и** `auth_base.html` перед подключением htmx, причём правило `422` — **выше** `[45]..`; каждая строка утверждается тестом, снятие любой роняет его (машинно).
  3. Подмена ЛЮБОГО вендоренного JS-файла меняет `?v=` на его теге: `_compute_asset_version()` считает хеш содержимого всех вендоренных файлов охвата, а не mtime одного `app.css` — вернувшийся пользователь не остаётся на 1.9.10 против сервера, говорящего на контракте 2.x (машинно).
  4. После выхода из системы снимков страниц в браузере не остаётся. Хранилища ДВА, и это два разных утверждения: свои снимки 2.0.10 держит в `sessionStorage` (переживает перезагрузку вкладки, НЕ переживает её закрытие) и при `historyCacheSize: 0` чистит их сам; `localStorage` — хранилище ВЫБЫВШЕГО 1.9.10, и остаток в нём есть предмет разовой миграционной очистки, приезжающей в оба шелла одним включением — **ручной UAT: суита не исполняет ни строчки JS**.
  5. Все 22 существующих `hx-get`-места работают на 2.0.10, и отдельно — каскад infinite scroll `hx-trigger="revealed"`, риск которого относится к изменившейся семантике самого срабатывания `revealed` в 2.0 (D-16), а запасной план на него — правка условия срабатывания через общий макрос (D-17) — **ручной UAT, пункты 1 и 2 перечня из девяти. «Зелёная суита» здесь не значит ничего.**

**Plans**: 7 планов — 3 исходных (волна 1 — планы 01 и 02 параллельно, волна 2 — план 03) плюс 4 плана закрытия разрывов после верификации (волна 1 — планы 04, 06 и 07 параллельно, волна 2 — план 05)
**Wave 1**

- [x] 07-01-PLAN.md — трассирующий срез: рантайм htmx 2.0.10, общий блок конфигурации в `includes/htmx_config.html`, оба шелла на нём, очистка ключа снимков истории в auth-шелле (FOUND-01, FOUND-02, QUAL-05)
- [x] 07-02-PLAN.md — `_compute_asset_version()` считает хеш содержимого охвата `**/*.css` + `**/*.js` вместо времени изменения `app.css`, плюс инвентарный гейт состава охвата (FOUND-03)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 07-03-PLAN.md — инвентарный гейт 22 мест `hx-get` числом по трём механизмам и артефакт ручного обхода `07-UAT.md` (FOUND-01, QUAL-05)

**Закрытие разрывов после верификации** *(`07-VERIFICATION.md` — `gaps_found`, 3/5; планы 01-03 не правятся и остаются записью о том, что отгружено)*

**Wave 1**

- [x] 07-04-PLAN.md — блокер критерия 4: миграционная очистка остатка 1.9.10 переезжает в `includes/htmx_config.html` и приезжает в ОБА шелла, гейт-пара переписывается под инвариант «ровно один раз в обоих», предусловие самоочистки рантайма объявляется явно, и пять документов приводятся в соответствие с артефактом (QUAL-05, FOUND-01)
- [x] 07-06-PLAN.md — контракт деградации `_compute_asset_version()` становится равен задекларированному (расчёт на импорте больше не может уронить приложение), охват перестаёт зависеть от регистра расширения, число мест доставки версии получает гейт (FOUND-03)
- [x] 07-07-PLAN.md — инвентарный гейт 22 мест считает вхождения атрибута и теги на исходнике с вырезанными комментариями вместо строк, и расцепляется с прозой основного шелла (FOUND-01)

**Wave 2** *(blocked on 07-04)*

- [x] 07-05-PLAN.md — правило `422` получает признак ошибки, и расхождение правила с телом ответа сервера получает инвентарный гейт по дереву разбора (FOUND-02)

**Research**: не требуется — поверхность ломающих изменений 1.x→2.x проверена побайтово и пуста (`hx-on`, `hx-ws`/`hx-sse`, расширения, `show:`, `hx-delete` — 0 вхождений каждый)

### Phase 8: Фундамент ответа, канал уведомлений, пакет гейтов и денежный потолок

**Goal**: контракт двойного ответа, канал обратной связи и машинные гейты существуют ДО того, как переведена первая форма — чтобы массовое переписывание не размножило дефект в 47 экземплярах
**Depends on**: Phase 7 (`responseHandling` и умолчания 2.x — предусловие ветвления по кодам)
**Requirements**: FOUND-04, FOUND-05, FOUND-06, FOUND-07, QUAL-03, GATE-01, GATE-03, GATE-04, GATE-05, GATE-06, GATE-07, GATE-08, PAY-01, PAY-02
**Success Criteria** (what must be TRUE):

  1. Признак htmx объявлен на проект РОВНО ОДИН раз: `app/pages/htmx.py::respond()` с обязательным аргументом `redirect=` (обработчик без пути деградации не собирается как вызов), и гейт утверждает, что множество обработчиков с `RedirectResponse` совпадает с множеством, читающим `HX-Request` (машинно, G-1/G-2).
  2. Пользователь получает обратную связь одним каналом: пять микро-контрактов (`?error=`, `?saved=1`, `?reset=`, `?retry=`, `?sched_error=`) сведены в один `?notice=` по КОДУ из закрытого реестра; OOB-область подменяет СОДЕРЖИМОЕ (`innerHTML:#notice`), заведены вежливая (`role="status"`) и настойчивая (`role="alert"`) области, при отсутствии кода разметки нет вовсе (машинно). Ценность независима от htmx и проверяема уже на сегодняшних редиректах.
  3. Отказ сервера, обрыв сети и просроченный доступ ВИДИМЫ и не мусорят экран: `require_access` и зависимость имперсонации на `HX-Request` отвечают `HX-Location`, а не 302 и не JSON (страница `/billing` больше не может свопнуться внутрь карточки); плашка приходит с сервера заранее отрисованной скрытой заготовкой, а `htmx:responseError` и `htmx:sendError` только переключают атрибут — действующее решение v2.0 «сборка узлами DOM, не строкой» не нарушено (машинно). **Фактическое срабатывание при 500 и при выключенной сети — ручной UAT, пункт 8 перечня.**
  4. Ни одна форма не может быть переведена мимо контракта: пакет из 13 машинных гейтов краснеет на нарушении — деградация (`method="post"` + непустой `action`, `hx-post` == `action` посимвольно, тег обязан быть `<form>`, `action` ведёт на маршрут с полным документом), своп (`hx-target` либо `hx-swap="none"`, цель существует, OOB несёт `id`, id не бывает целью и OOB-целью разом), граница Alpine, безопасность ответа (AST на правый операнд `response.headers["HX-*"]`, `|safe`/`Markup(` == 0, `hx-vals` только через `| tojson`), шелл. Фикстура `htmx_client` (`HX-Request: true` + `follow_redirects=True`) существует, и парный тест выглядит так: без заголовка → 302, с заголовком → 200 и `"<!DOCTYPE" not in response.text` (машинно).
  5. Второй одновременный запрос оплаты не создаёт второго незакрытого намерения: частичный уникальный индекс на `payments` построен (потолок стал свойством СХЕМЫ, а не проверкой в коде, которую две одновременные отправки проходят обе), серверное удержание на денежном пути и на пути рассылки существует НЕЗАВИСИМО от `hx-disabled-elt`, а `hx-sync` с `queue` на этих маршрутах запрещён гейтом (машинно).

**Plans**: 5/10 plans executed в 4 волнах

Plans:
**Wave 1**

- [x] 08-01-PLAN.md — слой ответа `app/pages/htmx.py`: единственное чтение `HX-Request`, `respond()` с обязательным `redirect=`, оба отказа зависимости на `HX-Location`, фикстура `htmx_client` (волна 1, tracer)
- [x] 08-02-PLAN.md — закрытый реестр уведомлений `app/pages/notices.py` из 14 кодов и его гейты (волна 1)
- [x] 08-03-PLAN.md — ревизия `0021`: зачистка, снятие `NOT NULL`, частичный уникальный индекс на `payments`; подпись статуса `expired` (волна 1, чекпойнт односторонней двери)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 08-04-PLAN.md — поверхность канала: две `aria-live`-области, внеполосная форма, скрытые заготовки плашек отказа и обрыва связи, гейт шелла (волна 2)
- [x] 08-05-PLAN.md — денежный путь: резерв до сети, ленивая уборка, разбор отказа ограничения по имени, снятие прикладной проверки (волна 2)

**Wave 3** *(blocked on Wave 2 completion)*

- [ ] 08-06-PLAN.md — перевод 12 редиректов на `?notice=`, снятие 4 читателей, 3 частных реестров и 5 мест отрисовки, греп-гейт снятых написаний (волна 3)

**Wave 4** *(blocked on Wave 3 completion)*

- [ ] 08-07-PLAN.md — гейты слоя ответа: G-1/G-2 (единственность решения) и G-13 (AST на правый операнд записи `HX-*`) (волна 4)
- [ ] 08-08-PLAN.md — гейты разметки: деградация G-3…G-6, своп G-7/G-9/G-10/G-11, граница Alpine G-12 (волна 4)
- [ ] 08-09-PLAN.md — гейты безопасности шаблонов G-14/G-15 и клаузы GATE-08: останов опроса G-21, убывающий счётчик ручной сборки запроса G-22 (волна 4)
- [ ] 08-10-PLAN.md — периметр опасных маршрутов G-18 и гейт артефакта развёртывания «один процесс, одна реплика» (волна 4)

**Research**: не требуется — все паттерны сняты с действующего кода (`_save_from_editor`, `_editor_redirect`, `_connect_status`, `autosave_response.html`), у гейтов прямые прецеденты (`test_access_gate.py`, `test_impersonation_gate.py`, `test_components.py`). ⚠️ Денежный узел PAY-01/PAY-02 требует ЯВНОГО решения в `/gsd-discuss-phase`: механизм серверного удержания и поведение при конфликте индекса
**UI hint**: yes — фаза вводит НОВУЮ видимую поверхность (две `aria-live`-области уведомлений и плашка отказа), а не только механику
**Примечания к охвату:**

- GATE-08 пишется здесь целиком, но его клауза `test_no_manual_fetch_remains` физически позеленеет только после Фазы 13; её финальное утверждение `fetch( == 0` принадлежит FETCH-03 в Фазе 15.
- PAY-01/PAY-02 стоят здесь, а не «перед `billing`» в Фазе 11, по трём причинам: гейт G-18 и так фундаментный; удержание на ПУТИ РАССЫЛКИ нужно раньше, чем переводится `history_retry`; и «раньше с запасом» дешевле, чем «ровно вовремя» на денежном узле. Требование пришло из долга v2.0 и втянуто в веху сознательно — htmx повышает его риск: узкое окно между кликом и перезагрузкой было де-факто защитой, веха его убирает, а `hx-disabled-elt` — клиентское свойство и защитой не является.

### Phase 9: Пилот на `account_groups` — сквозной контракт формы

**Goal**: контракт «фрагмент + OOB + notice + деградация + свойства качества» доказан целиком на наименьшей поверхности, и гейты Фазы 8 получают зубы прежде, чем начнётся массовая работа
**Depends on**: Phase 8
**Requirements**: FORM-02, FORM-09, QUAL-01, QUAL-02, QUAL-06
**Success Criteria** (what must be TRUE):

  1. Включение, выключение и удаление группы на `/accounts/{id}/groups` происходят без перезагрузки страницы: ответ — фрагмент по `id` конкретной строки плюс побочные области через `hx-swap-oob`; контейнер целиком не перерисовывается, позиция прокрутки не теряется (машинно: 200 без `<!DOCTYPE`, наличие `id`, наличие OOB-узлов).
  2. Обе формы остаются рабочими при выключенном JS: `method="post"`, непустой `action`, `hx-post` совпадает с `action` посимвольно, `action` ведёт на маршрут, отдающий полный документ (машинно, GATE-04 — «htmx только перехватывает» становится свойством ИСХОДНИКА, а не намерения).
  3. Свойства качества раздаёт ОДИН макрос-обёртка формы, а не ревью 47 форм поотдельности: `hx-disabled-elt` целью **только** `find button[type=submit]` (кнопка Отмены в панели подтверждения не блокируется никогда — записанное решение v2.0), `hx-indicator` одним классом в `app.css` с порогом видимости через `transition-delay` (машинно — наличие и цель; **визуально и как реальная защита от двойного клика — ручной UAT, пункт 6**).
  4. Своп действительно произошёл и ИМЕННО в тот элемент, OOB-счётчик приземлился куда задумано, а фокус после подмены вернулся на элемент с тем же `id` (клавиатура и скринридер не теряют место) — **ручной UAT, пункт 3 перечня; сервером это не доказуемо в принципе: httpx не свопает и не собирает OOB**.
  5. Проверено и записано, несёт ли `#group-row-N` атрибут `x-data`; если несёт — `x-data` поднят на обёртку прямо здесь, на двух формах, и гейт границы Alpine (узел с `x-data` не бывает целью `hx-target`) держит это дальше (машинно).

**Plans**: TBD
**Research**: не требуется — паттерн взят с действующего `autosave_response.html`, разметка списка уже несёт стабильные `id` и собирается макросом
**UI hint**: yes — впервые появляются видимые состояния «запрос идёт» и «кнопка занята», то есть новая визуальная поверхность на уже сделанном дизайне

### Phase 10: Рычаг `components/modal.html`

**Goal**: подтверждение удаления идёт через htmx во всех 16 местах одной правкой одного файла, и после N удалений в документе не остаётся N мёртвых диалогов
**Depends on**: Phase 9 (контракт доказан и подтверждён руками до правки, расходящейся на 10 потребителей)
**Requirements**: FORM-06
**Success Criteria** (what must be TRUE):

  1. Все 16 мест подтверждения удаления работают через htmx одной правкой `components/modal.html`; инвентарь мест утверждён ЧИСЛОМ, поэтому молчаливое появление или исчезновение места краснеет (машинно, расширенные `test_modal_guard_is_inherited_by_every_consumer` и `test_modal_site_inventory`).
  2. Ответ снимает осиротевшую панель подтверждения вторым OOB-узлом `hx-swap-oob="delete"` по её `id`: панель сознательно стоит СНАРУЖИ удаляемой строки, и без этого узла в документе копятся `role="dialog"` с живыми фокус-ловушками (машинно — присутствие узла; **фактическое снятие — ручной UAT, пункт 4 перечня, и это единственный вывод исследования, взятый из документации и не проверенный НИ НА ЧЁМ**).
  3. Панель закрывается существующим Alpine (`x-on:htmx:after-request`) — новых строк JS фаза не добавляет (машинно: диффом по `app/static/js/` и шаблонам).
  4. Все 10 потребителей `modal.html` продолжают удалять без JS: панель остаётся настоящей формой POST с непустым `action` (машинно, гейты деградации Фазы 8 держат наследование).

**Plans**: TBD
**Research**: не требуется — механизм `hx-swap-oob="delete"` описан документацией, а неопределённость закрыта именованным пунктом ручного UAT, а не разведкой

### Phase 11: Массовый перевод разделов письма

**Goal**: пользователь выполняет действия во всех продуктовых и админских разделах без перезагрузки страницы, и суита перестаёт быть зелёной по построению
**Depends on**: Phase 10 (и Phase 8 — денежный потолок обязан стоять до перевода форм `billing`)
**Requirements**: FORM-03, FORM-04, FORM-05, FORM-07, FORM-08, FORM-10, GATE-02
**Success Criteria** (what must be TRUE):

  1. 15 обработчиков правки на месте (класс A) возвращают фрагмент по `id` конкретной строки или карточки плюс побочные области через `hx-swap-oob`; 5 навигационных (класс B) отвечают `HX-Location`; `HX-Retarget` и `HX-Reswap` применены ПОИМЕННО в перечисленных случаях, а не как общая практика (машинно).
  2. `/billing/subscribe` уходит на внешний адрес ЮKassa заголовком `HX-Redirect`, а не `HX-Location` — иначе `selfRequestsOnly: true` заблокировал бы межсайтовый запрос и веха сломала бы работающий платёж (машинно — заголовок; **переход в ЮKassa на тестовом ключе — ручной UAT**).
  3. Создание сущности вставляет её в список `afterbegin`, не перерисовывая контейнер; ошибка валидации возвращает 422, перерисовывает форму и возвращает введённое эхом — пользователь не теряет заполненное (машинно — коды и содержимое; **позиция прокрутки — ручной UAT**).
  4. 160 существующих утверждений `status_code == 302` стали ПАРАМИ (без заголовка → 302, с `HX-Request` → 200 и без `<!DOCTYPE`) параметризованным обходом, а не были заменены и не превратились в 320 функций (машинно, счёт пар).
  5. Alpine переживает свап там, где раздел плотно им набит: раскрытая карточка расписания не схлопывается, три шаблона `accounts/*` не теряют состояние, слушатели не копятся после десятков действий без перезагрузки; `hx-push-url` действительно меняет адрес, а Back и F5 не предлагают повторить POST — **ручной UAT, пункты 5 и 7 перечня; пункт 5 назван самым острым остаточным риском вехи**.

**Plans**: TBD
**Research**: не требуется — паттерны сняты с действующего кода. ⚠️ При планировании назвать отдельным пунктом: совместное поведение `hx-push-url` с уже применяемым заголовком `HX-Push-Url` (`app/pages/ads.py:522`) не проверял никто
**Порядок внутри фазы (не переставлять):** `schedules` + `ads` → `admin` (три файла `admin/includes/*` обязаны получить `id` и макросы) → `accounts` **последним** (3 шаблона с `x-data`, 9 правок на действие, тройная копия разметки). `billing` — **не первым**, после отработки контракта и только поверх построенного в Фазе 8 потолка.

### Phase 12: Загрузка изображений без `fetch()`

**Goal**: изображение объявления прикрепляется htmx-фрагментом, и ни одна строка шаблона не собирает запрос руками
**Depends on**: Phase 8 (может вестись ПАРАЛЛЕЛЬНО Фазам 9-11: не касается ни `app/pages/`, ни оболочки)
**Requirements**: FETCH-01
**Success Criteria** (what must be TRUE):

  1. Загрузка идёт htmx-фрагментом в существующий `#media-strip`; `fetch()` и JS-массив `imagePaths` из `ads/form.html` исчезли, ключ приезжает скрытым полем с атрибутом `form="ad-form"` (машинно, ~70 строк JS минус).
  2. Успешно загруженные файлы остаются прикреплёнными, даже если часть отвалилась — сегодняшнее свойство сохранено СОЗНАТЕЛЬНО, при том что htmx шлёт все файлы ОДНИМ запросом, а не N (машинно, тест на частичный отказ).
  3. Отказ доступа на загрузке виден пользователю, а не молчит: гейт `get_current_user_id_with_access` отвечает JSON, а htmx на 4xx не свопает вовсе; принятое решение о том, КУДА переезжает маршрут (`app/routes/` ↔ `app/pages/`), отражено в ОБОИХ гейтах перечня — а не подогнано ради зелени (машинно).
  4. Пользователь видит, что загрузка идёт — бинарный `hx-indicator` без процентов (`UPLD-01` отложен в v2.2 осознанно: проценты требуют `htmx:xhr:progress` и конфликтуют с рамкой «минимум нового JS») — **ручной UAT**.

**Plans**: TBD
**Research**: **ДА, `--research-phase`** — три поведенческих отличия являются РЕШЕНИЯМИ, а не переносом: один запрос вместо N, судьба частичного успеха, переезд гейта доступа между двумя поверхностями

### Phase 13: Мастер подключения Telegram по QR на фрагментах

**Goal**: мастер подключения работает на HTML-фрагментах с состоянием на сервере — без `setInterval`, без пяти JSON-контрактов и без ручного переключения `hidden`
**Depends on**: Phase 10 (нужны готовый `respond()` и отлаженные гейты опроса); идёт ДО авторизации
**Requirements**: FETCH-02
**Success Criteria** (what must be TRUE):

  1. Пять `fetch()` из `accounts/connect_tg_user.html` (`start-qr`, `qr-status`, `complete`, `refresh-qr`, `verify-2fa`) исчезли вместе с `setInterval` и ручным переключением `hidden`; пять маршрутов отдают HTML-фрагменты шага, а не JSON (машинно).
  2. Опрос ведётся `hx-trigger` и ОСТАНАВЛИВАЕТСЯ ответом без `hx-trigger`, а не JS-таймером: каждый фрагмент с `hx-trigger="every "` имеет парный без него (машинно, обобщение существующего `test_sync_polling_stops`).
  3. Состояние мастера живёт на сервере, `session_id` едет скрытым полем, и серверная проверка владения сессией СОХРАНЕНА — подставленный чужой `session_id` отвергается (машинно).
  4. Живой сценарий проходит до конца: сканирование QR телефоном, обновление истёкшего кода, ввод 2FA — **ручной UAT над живой сессией Telethon; дешёвого отката нет**.
  5. Записано прямо: деградации без JS здесь нет **и сегодня** — мастер уже 100% JS-only. Веха на этом участке регрессировать не может, и аудит не вправе записать здесь ложный провал (документная запись в артефактах фазы).

**Plans**: TBD
**Research**: **ДА, `--research-phase`** — машина состояний над живой сессией Telethon; окно между `qr-status` и `complete` требует ПРОДУКТОВОГО решения, а не механической правки

### Phase 14: Авторизация на htmx

**Goal**: неверный код или пароль перестаёт стирать заполненную форму — заявленный выигрыш вехи именно в ОШИБКЕ, а не в успехе
**Depends on**: Phase 11 и Phase 13 (осознанно последняя из конверсионных: сломанный вход заблокировал бы ВЕСЬ ручной UAT остальных фаз)
**Requirements**: SIGN-01, SIGN-02, SIGN-03
**Success Criteria** (what must be TRUE):

  1. Неверный код подтверждения или пароль перерисовывает форму С СОХРАНЕНИЕМ введённого, а не отправляет пользователя заполнять заново: 422 + эхо-возврат в `value=`; свойство создаёт СЕРВЕР, htmx лишь подменяет присланное (машинно).
  2. Все 9 форм авторизации (вход, регистрация, подтверждение, повтор кода, восстановление пароля) идут через `hx-post` и остаются рабочими без JS (машинно, гейты деградации + парные тесты).
  3. Успех авторизации уходит `HX-Redirect` через границу шеллов `auth_base.html` → `base.html` (фрагментом смену личности и cookie сессии не отдать); `HX-Location` остаётся ТОЛЬКО у `/impersonation/stop`, не пересекающего границу шеллов (машинно).
  4. Вход, регистрация, подтверждение почты и восстановление пароля проходят в браузере целиком, включая смену cookie личности и `<title>` второго шелла — **ручной UAT**.

**Plans**: TBD
**Research**: **ДА, `--research-phase`** — взаимодействие смены cookie личности с частичным свапом через границу двух шеллов; `#notice` в `auth_base.html` не приходит, там свой канал внутри формы

### Phase 15: Упрочнение и сводный обход 47 форм

**Goal**: цель вехи закрыта инвентарями, а не ощущением: 47 из 47, `fetch(` == 0, девять пунктов ручного UAT закрыты поимённо
**Depends on**: Phases 7-14 (все конверсионные потоки)
**Requirements**: FORM-01, FETCH-03, QUAL-04, GATE-09, GATE-10
**Success Criteria** (what must be TRUE):

  1. Пользователь выполняет ЛЮБОЕ действие письма без перезагрузки страницы: все 47 форм идут через `hx-post`, инвентарь сведён числом, ни одна форма не осталась вне контракта (машинно, сводный обход всех 27 файлов шаблонов с формами).
  2. Ручной сборки запросов в шаблонах не осталось: `fetch(` в `app/templates/` == 0 — цель вехи закрыта тем же способом, каким греп-гейт закрыл возврат снятых имён тарификации (машинно, `test_no_manual_fetch_remains`).
  3. По КАЖДОЙ форме принято и записано решение о `hx-push-url` по конвенции трёх случаев (меняет ЧТО показано → `true`; меняет только данные → атрибута нет; адрес известен только серверу → заголовок `HX-Push-Url`), и гейт запрещает атрибут на маршрутах перечня «изменяет данные» (машинно + сводка решений в артефактах фазы).
  4. Тесты `*_degrades_without_alpine` получили ПАРНЫЕ `*_degrades_without_htmx`, а не были переименованы с подменой предмета — файл, чьё имя врёт про предмет, не появился (машинно, счёт пар).
  5. Девять именованных пунктов ручного UAT закрыты поимённо, включая известную слепую зону гейтов разметки — атрибуты, собранные Jinja-условием (`{% if %}hx-post{% endif %}`): либо такая форма запрещена гейтом, либо проверена глазами — **ручной UAT, пункт 9; границы каждого гейта выписаны в докстринге его файла, как уже сделано в `test_impersonation_gate.py`**.

**Plans**: TBD
**Research**: не требуется — фаза закрывает инвентари и обходы, новых механизмов не вводит

## Покрытие требований

Все 41 требование вехи v2.1 отображены ровно в одну фазу. Сирот нет, дублей нет.

| Категория | Требования | Фазы |
|---|---|---|
| Фундамент (7) | FOUND-01…03 → 7; FOUND-04…07 → 8 | 7, 8 |
| Формы письма (10) | FORM-02, FORM-09 → 9; FORM-06 → 10; FORM-03, 04, 05, 07, 08, 10 → 11; FORM-01 → 15 | 9, 10, 11, 15 |
| Свойства качества (6) | QUAL-05 → 7; QUAL-03 → 8; QUAL-01, 02, 06 → 9; QUAL-04 → 15 | 7, 8, 9, 15 |
| Снятие ручного JS (3) | FETCH-01 → 12; FETCH-02 → 13; FETCH-03 → 15 | 12, 13, 15 |
| Авторизация (3) | SIGN-01…03 → 14 | 14 |
| Верификация (10) | GATE-01, 03, 04, 05, 06, 07, 08 → 8; GATE-02 → 11; GATE-09, 10 → 15 | 8, 11, 15 |
| Денежный путь (2) | PAY-01, PAY-02 → 8 | 8 |

**Правило отображения агрегатов** (записано, чтобы мапинг не выглядел произвольным): требование
вида «на каждой форме» отнесено к фазе, где оно становится структурно ИСТИННЫМ — то есть где
появляется макрос или гейт, делающий его свойством построения; последующие фазы наследуют его
по конструкции, а гейты Фазы 8 не дают отступить. Требования вида «все 47» (FORM-01, FETCH-03,
QUAL-04, GATE-09, GATE-10) отнесены к Фазе 15: 47/47 наблюдаемо только в сводном обходе, и
планировщик Фазы 11 не должен пытаться перевести там же 9 форм авторизации.

## Параллельные потоки

Три потока не имеют общих файлов и могут вестись параллельно указанным порядком зависимостей:

- **Основной:** 7 → 8 → 9 → 10 → 11 → 14 → 15
- **Загрузка изображений (Фаза 12):** с момента готовности Фазы 8, параллельно 9-11
- **QR-мастер (Фаза 13):** после Фазы 10, параллельно 11; до Фазы 14

## Ручной UAT — девять именованных пунктов

Браузерного стенда в проекте нет и не заводится (Playwright отклонён: ~1700 тестов идут на
in-memory SQLite через `ASGITransport` без процесса и портов; стенд — своя веха, отложен как
`E2E-01`). Поэтому перечисленное закрывается ГЛАЗАМИ, а не тестом, и названо здесь поимённо,
а не спрятано за «покрыто тестами».

| # | Пункт | Фаза |
|---|---|---|
| 1 | Замена `htmx.min.js` вообще — суита не исполняет JS ни строчки | 7 |
| 2 | 22 существующих `hx-get`-места + каскад infinite scroll от изменившейся семантики срабатывания `revealed` в 2.0 (D-16) | 7 |
| 3 | Своп действительно произошёл и в тот элемент; OOB-область приземлилась куда задумано | 9 (впервые), 11 (каждый раздел) |
| 4 | `hx-swap-oob="delete"` действительно снимает осиротевшую панель подтверждения | 10 |
| 5 | Alpine после свапа: переинициализация, порядок относительно `htmx:afterSwap`, утечка слушателей | 11 (плюс проверка границы в 9) |
| 6 | `hx-indicator` / `hx-disabled-elt` визуально и как реальная защита от двойного клика | 9 |
| 7 | `hx-push-url` действительно меняет адрес; Back и F5 на переведённых экранах | 11 |
| 8 | Глобальный обработчик ошибок срабатывает — при 500 и при выключенной сети | 8 |
| 9 | Атрибуты, собранные Jinja-условием (`{% if %}hx-post{% endif %}`) — слепая зона гейтов разметки | 15 |

Сводное закрытие всех девяти — требование GATE-09, Фаза 15.

## Гранулярность — названное отступление

Настройка `granularity: standard` предполагает 4-6 фаз; в вехе их 9. Отступление сознательное,
основания названы:

1. **Объём.** 41 требование, 47 форм, 36 обработчиков, 9 из 14 роутеров. Веха v2.0 при тех же
   41 требовании шла 7 фазами.

2. **Жёсткие зависимости.** Фазы 7 и 8 — не «удобнее сначала», а условие достижимости:
   `responseHandling` в 1.9.10 физически отсутствует, а 13 из 19 ловушек закрываются гейтами,
   обязанными существовать ДО первой переведённой формы.

3. **Границы фаз = точки ручной приёмки** (`human_verify_mode: end-of-phase`). Слияние Фазы 7
   с Фазой 8 отложило бы ручную проверку обновления htmx до конца большой фазы, уже написанной
   поверх непроверенной семантики 2.x; слияние Фазы 9 с Фазой 10 расплескало бы правку на
   10 потребителей до подтверждения пилота.

4. **Однотребовательные фазы (10, 12, 13) тонкими не являются:** FETCH-02 — это пять
   обработчиков и серверная машина состояний, FORM-06 — 16 мест, FETCH-01 — три поведенческих
   решения. Единичность счёта — свойство гранулярности ТРЕБОВАНИЙ, а не объёма работы.

## Progress

**Execution Order:** 7 → 8 → 9 → 10 → 11 → 14 → 15, при этом 12 идёт параллельно с 9-11, а 13 — после 10 и до 14.

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 7. Обновление htmx до 2.0.10 | v2.1 | 7/7 | Complete    | 2026-08-28 |
| 8. Фундамент, гейты, денежный потолок | v2.1 | 5/10 | In Progress|  |
| 9. Пилот `account_groups` | v2.1 | 0/TBD | Not started | - |
| 10. Рычаг `modal.html` | v2.1 | 0/TBD | Not started | - |
| 11. Массовый перевод разделов | v2.1 | 0/TBD | Not started | - |
| 12. Загрузка изображений | v2.1 | 0/TBD | Not started | - |
| 13. QR-мастер Telegram | v2.1 | 0/TBD | Not started | - |
| 14. Авторизация | v2.1 | 0/TBD | Not started | - |
| 15. Упрочнение и сводный обход | v2.1 | 0/TBD | Not started | - |

## Перенесено незакрытым из v2.0

Веха v2.0 закрыта типом `override_closeout`: 15 открытых артефактов подтверждены как
осознанно отложенные. Пробелов в требованиях нет — перечисленное ниже к ним не относится
и **в состав v2.1 не входит**, за одним названным исключением.

- **Выкат очереди ревизий Alembic `0013`…`0020` на боевую базу** (D-26) — деплой-гейт
  владельца. Выкат кода и выкат очереди разделить нельзя. ⚠️ Веха v2.1 ложится поверх
  невыкаченной схемы; связи с htmx нет, но условие «отгруженное работает» не исполнено.

- **Три BLOCKER-долга безопасности** (`.planning/todos/pending/`): блокировка пользователя
  не действует на страничном маршруте, коды сброса пароля из `random.randint`, cookie
  сессии без флага `secure`. Последний соприкасается с вехой: единственная действующая
  защита от CSRF — `SameSite=Lax`, поэтому Фаза 8 утверждает `samesite == "lax"` как
  ЗАПИСАННОЕ РЕШЕНИЕ, а не наблюдение. Сам долг остаётся открытым.

- **Подписка на `payment.canceled` в кабинете ЮKassa** (D-27) — действие вне репозитория.
- **Три открытые debug-сессии**: `knowledge-base`, `max-worker-import-after-update`,
  `telegram-schedule-not-due`.

- **Адаптив админ-панели на 375px** не подтверждён — браузерного стенда в суите нет.
- ✅ **ВТЯНУТО В v2.1:** частичный уникальный индекс на `payments` и серверное удержание —
  требования PAY-01/PAY-02, Фаза 8. Основание втягивания: веха ПОВЫШАЕТ риск долга, убирая
  окно перезагрузки, которое было де-факто защитой.

Полный перечень с основаниями — `.planning/STATE.md` §Deferred Items,
`.planning/MILESTONES.md` §v2.0.

---

*Требования вехи — `.planning/REQUIREMENTS.md`. Разведка — `.planning/research/SUMMARY.md`.*
*Текущее состояние проекта — `.planning/PROJECT.md`. Роадмап v2.1 составлен 2026-08-26.*
