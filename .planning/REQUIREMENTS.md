# Requirements: Broadcaster — milestone v2.1 «HTMX-first»

**Defined:** 2026-08-26
**Core Value:** Надёжно выполнять периодические рекламные рассылки в группы нескольких мессенджеров по заданному пользователем расписанию.

**Цель вехи:** перевести всё письмо и все ручные обращения к серверу на htmx — так, чтобы ни одно действие пользователя не перезагружало страницу и ни один шаблон не собирал запрос руками.

**Отправная точка (снята по коду 2026-08-26):** 47 форм, из них через htmx не идёт **ни одна**; 36 POST-обработчиков; 127 `RedirectResponse`; 79 существующих `hx-*` атрибутов в 22 шаблонах, из них `hx-post` — 2; 6 ручных `fetch()`; `HX-Request` встречается во всей суите **1 раз** на 131 файл и ~1700 тестов.

---

## v2.1 Requirements

### Фундамент

- [x] **FOUND-01**: htmx обновлён с 1.9.10 до 2.0.10 заменой вендоренного файла, и тест утверждает версию по содержимому файла
- [x] **FOUND-02**: блок конфигурации htmx из шести ключей (`responseHandling` с правилом `422` **выше** `[45]..`, `historyRestoreAsHxRequest: false`, `allowNestedOobSwaps: false`, `reportValidityOfForms: true`, `historyCacheSize: 0`, явный `selfRequestsOnly: true`) стоит в `base.html` **и** `auth_base.html` перед подключением htmx, и каждая строка утверждается тестом
- [x] **FOUND-03**: `asset_version` пересчитывается при замене любого вендоренного JS-файла, а не только `app.css` — подмена `htmx.min.js` меняет `?v=` на его теге
- [x] **FOUND-04**: слой `respond()` в новом `app/pages/htmx.py` — единственное на проект объявление признака htmx; аргумент `redirect=` обязателен, поэтому обработчик без пути деградации не собирается как вызов
- [x] **FOUND-05**: реестр уведомлений `app/pages/notices.py` принимает КОД, а не текст; пять существующих микро-контрактов (`?error=`, `?saved=1`, `?reset=`, `?retry=`, `?sched_error=`) сведены в один `?notice=`
- [x] **FOUND-06**: OOB-область уведомлений подменяет СОДЕРЖИМОЕ (`hx-swap-oob="innerHTML:#notice"`), а не узел; заведены две области — вежливая (`role="status"`) и настойчивая (`role="alert"`); при отсутствии кода разметки нет вовсе
- [x] **FOUND-07**: гейт доступа `require_access` и зависимость имперсонации на `HX-Request` отвечают `HX-Location`, а не 302 и не JSON — иначе отказ выглядит как вложенная страница внутри карточки или как полная тишина

### Формы письма

- [ ] **FORM-01**: пользователь выполняет любое действие письма без перезагрузки страницы — все 47 форм идут через `hx-post`
- [ ] **FORM-02**: каждая форма остаётся рабочей без JS — сохраняет `method="post"` и непустой `action`, причём `hx-post` совпадает с `action` посимвольно, а `action` ведёт на маршрут, отдающий полный документ
- [ ] **FORM-03**: правка на месте (15 обработчиков класса A) возвращает фрагмент по `id` конкретной строки или карточки плюс побочные области через `hx-swap-oob`; контейнер целиком не перерисовывается
- [ ] **FORM-04**: навигационные действия (5 обработчиков класса B) возвращают `HX-Location`
- [ ] **FORM-05**: `/billing/subscribe` уходит на внешний адрес ЮKassa заголовком `HX-Redirect`, а не `HX-Location` — `selfRequestsOnly` заблокировал бы межсайтовый запрос
- [ ] **FORM-06**: подтверждение удаления работает через htmx во всех 16 местах одной правкой `components/modal.html`, и ответ снимает осиротевшую панель подтверждения вторым OOB-узлом с `hx-swap-oob="delete"`
- [ ] **FORM-07**: создание сущности вставляет её в список (`afterbegin`), не перерисовывая контейнер и не теряя позицию прокрутки
- [ ] **FORM-08**: ошибка валидации возвращает 422 с перерисовкой формы и эхо-возвратом введённого — пользователь не теряет заполненное
- [ ] **FORM-09**: единый макрос-обёртка формы раздаёт свойства качества, вместо того чтобы они проверялись ревью 47 форм поотдельности
- [ ] **FORM-10**: `HX-Retarget` и `HX-Reswap` применяются поимённо в перечисленных случаях, а не как общая практика — каждое применение делает поведение невидимым в шаблоне

### Свойства качества

- [ ] **QUAL-01**: двойное нажатие не создаёт второй записи — `hx-disabled-elt` на каждой форме письма, целью **только** `find button[type=submit]`; кнопка Отмены в панели подтверждения не блокируется никогда
- [ ] **QUAL-02**: пользователь видит, что запрос идёт — `hx-indicator` на каждой форме письма через один класс в `app.css` и один макрос, с порогом видимости через `transition-delay`
- [x] **QUAL-03**: отказ сервера и обрыв сети видимы пользователю — глобальные `htmx:responseError` **и** `htmx:sendError`; разметка плашки приходит с сервера заранее отрисованной скрытой заготовкой, скрипт только переключает атрибут, чем соблюдается действующее решение v2.0 «сборка узлами DOM, не строкой»
- [ ] **QUAL-04**: по каждой форме принято и записано решение о `hx-push-url` по конвенции трёх случаев: действие меняет ЧТО показано → `hx-push-url="true"`; меняет только данные → атрибута нет; адрес известен только серверу → заголовок `HX-Push-Url`
- [x] **QUAL-05**: снимки страниц не остаются в браузере после выхода из системы — `historyCacheSize = 0`
- [ ] **QUAL-06**: после подмены фрагмента фокус возвращается на элемент с тем же `id` — клавиатура и скринридер не теряют место

### Снятие ручного JS

- [ ] **FETCH-01**: загрузка изображения объявления идёт htmx-фрагментом вместо `fetch()` + JSON; успешно загруженные файлы остаются прикреплёнными, даже если часть отвалилась
- [ ] **FETCH-02**: мастер подключения Telegram по QR работает на HTML-фрагментах — опрос через `hx-trigger`, останов опроса ответом без `hx-trigger`, состояние мастера на сервере, `session_id` в скрытом поле при сохранённой серверной проверке владения
- [ ] **FETCH-03**: ручной сборки запросов в шаблонах не остаётся — `fetch(` в `app/templates/` равно 0

### Авторизация

- [ ] **SIGN-01**: 9 форм авторизации (вход, регистрация, подтверждение, повтор кода, восстановление пароля) идут через htmx
- [ ] **SIGN-02**: неверный код подтверждения или пароль перерисовывает форму с сохранением введённого вместо потери заполненного — заявленный выигрыш вехи на этом участке
- [ ] **SIGN-03**: успех авторизации уходит `HX-Redirect` через границу шеллов `auth_base.html` → `base.html`; `HX-Location` применяется только для `/impersonation/stop`

### Верификация

- [x] **GATE-01**: фикстура `htmx_client` (`HX-Request: true` + `follow_redirects=True`) существует, и каждый переведённый маршрут покрыт ПАРОЙ тестов: без заголовка → 302, с заголовком → 200 и `"<!DOCTYPE" not in response.text`
- [ ] **GATE-02**: 160 существующих утверждений `status_code == 302` становятся парами, а не заменяются — параметризованным обходом, а не 320 функциями
- [x] **GATE-03**: гейт единственности решения — каждый обработчик под `@router.post` идёт через `respond()` либо стоит в явном перечне исключений с обоснованием; множество обработчиков с `RedirectResponse` равно множеству, читающему `HX-Request`
- [x] **GATE-04**: гейты деградации — у каждого `<form>` с `hx-post` есть `method="post"` и непустой `action`; `hx-post` равен `action`; тег с `hx-post` обязан быть `<form>`; `action` ведёт на маршрут, отдающий полный документ
- [x] **GATE-05**: гейты свапа — на каждом `hx-post` есть `hx-target` либо `hx-swap="none"`; цель `hx-target` существует; каждый OOB-блок несёт `id`; один `id` не может быть одновременно `hx-target` и OOB-целью
- [x] **GATE-06**: гейт границы Alpine — узел с `x-data` никогда не является целью `hx-target`; множество целей собирается тем же обходом, чем гейт замкнут на себя
- [x] **GATE-07**: гейты безопасности ответа — AST-проверка, что правый операнд `response.headers["HX-*"]` есть литерал или f-строка с целыми (закрывает разом кириллицу в заголовке, инъекцию заголовка и открытый редирект); `|safe` и `Markup(` в шаблонах равны 0; `hx-vals` только через `| tojson`, формы `hx-vals='js:'` и `hx-on:` запрещены
- [x] **GATE-08**: `test_shell.py` утверждает версию htmx, каждую строку блока конфигурации, оба обработчика ошибок, обе `aria-live`-области и `samesite == "lax"` как записанное решение; гейт останова опроса — каждый фрагмент с `hx-trigger="every "` имеет парный без него; `test_no_manual_fetch_remains`
- [ ] **GATE-09**: девять именованных пунктов ручного UAT закрыты — замена вендоренного файла, 22 существующих `hx-get`-места и каскад infinite scroll, факт свапа и приземление OOB, снятие осиротевшей модалки, Alpine после свапа, `hx-indicator`/`hx-disabled-elt` визуально, `hx-push-url` с Back и F5, срабатывание глобального обработчика при 500 и при выключенной сети, атрибуты, собранные Jinja-условием
- [ ] **GATE-10**: тесты `*_degrades_without_alpine` получают парные `*_degrades_without_htmx`, а не переименовываются с подменой предмета

### Денежный путь

- [x] **PAY-01**: частичный уникальный индекс «не более одного незакрытого подписочного намерения» на `payments` построен — потолок становится свойством схемы, а не проверкой в коде, которую две одновременные отправки проходят обе
- [x] **PAY-02**: серверное удержание на денежном пути и на пути рассылки существует **независимо** от `hx-disabled-elt`, и `hx-sync` с `queue` на этих маршрутах запрещён гейтом — клиентское свойство защитой не является

---

## v2.2 Requirements

Признано ценным, отложено осознанно.

### Правка на месте

- **EDIT-01**: пользователь правит значение по клику прямо в строке, без перехода в форму — единственный паттерн, где деградация без JS требует отдельной проектной работы (кнопка `hx-get` без JS мертва, нужен `<a href>`). Брать после того, как 47 форм переведены

### Прогресс загрузки

- **UPLD-01**: пользователь видит процент загрузки крупного файла — требует JS-обработчика `htmx:xhr:progress`, что прямо конфликтует с рамкой «минимум нового JS». В v2.1 берётся бинарный `hx-indicator`

### Браузерный стенд

- **E2E-01**: сквозные проверки в настоящем браузере покрывают то, что не доказуемо сервером (факт свапа, поведение Alpine после подмены, визуальные свойства). Это своя веха со своим решением — живой uvicorn, реальная БД, бинарники в CI и в Docker-сборке, вторая суита и флак; протаскивать внутрь v2.1 нельзя, её премиса — «ни build-шага, ни новой инфраструктуры»

---

## Out of Scope

| Feature | Reason |
|---------|--------|
| `hx-boost` на навигацию | Веха закрывает ПИСЬМО; навигация — независимая ось, её смешение размыло бы критерий готовности. Переходы по ссылкам остаются полной перезагрузкой |
| Снятие Alpine.js | 14 шаблонов с `x-data` остаются; перенос клиентского состояния на сервер — отдельная работа. В вехе Alpine получает только правило границы (узел с `x-data` не бывает целью свапа) |
| `jinja2-fragments` и любая новая Python-зависимость | Пакет требует `{% block %}`, а проект принуждает МАКРОСЫ двумя действующими гейтами; и он не касается трудной части задачи — ветвления 36 разнородных выходов. Веха получает 0 новых зависимостей |
| Playwright и браузерный стенд | ~1700 тестов идут на in-memory SQLite через `ASGITransport` без процесса и портов; стенд — живой uvicorn, реальная БД, бинарники в CI, вторая суита и флак. Новая операционная поверхность на вехе, чья премиса «ни build-шага, ни новой инфраструктуры». Отложено как `E2E-01` |
| Тосты через заголовок `HX-Trigger` | Starlette кодирует значения заголовков в latin-1 — проверено исполнением в venv проекта: первый же русский текст роняет ответ в 500. Плюс не деградируют без JS |
| `hx-confirm` | Вызывает браузерный `confirm()`, выброшенный в v2.0 вместе с 14 местами; возврат отменил бы записанное решение |
| Оптимистичный UI | У htmx нет отката. Для рассылок цена прямая: пользователь думает, что группа выключена, а объявление туда уходит |
| Морфинг-свап (`idiomorph`, `alpine-morph`) | Новый вендоренный файл при действующем правиле «внешних ресурсов 0»; проблема Alpine закрывается правилом границы дешевле |
| Расширение `response-targets` | В htmx 2.x расширения — отдельные пакеты; задача закрывается `responseHandling` и точечным `HX-Retarget` без нового файла |
| Свап всей страницы на каждое действие | Теряет позицию прокрутки и гасит раскрытые карточки; отменяет смысл фрагментного контракта |
| Плашка на каждый успех | Спам уведомлениями обесценивает канал; плашка — для того, что пользователь иначе не увидит |
| Полный отказ от `RedirectResponse` | 127 редиректов остаются как путь деградации; они не «легаси», а вторая половина двойного ответа |
| Изменение протоколов отправки Telegram, WhatsApp и MAX | Рамка, сохранённая с v2.0 — работающие сценарии доставки не трогаем |

---

## Traceability

Заполнено при создании роадмапа 2026-08-26. Фазы вехи v2.1 — 7…15 (нумерация продолжает v2.0,
закончившуюся Фазой 6, и НЕ начинается заново).

| Requirement | Phase | Status |
|-------------|-------|--------|
| FOUND-01 | Phase 7 | Complete |
| FOUND-02 | Phase 7 | Complete |
| FOUND-03 | Phase 7 | Complete |
| FOUND-04 | Phase 8 | Complete |
| FOUND-05 | Phase 8 | Complete |
| FOUND-06 | Phase 8 | Complete |
| FOUND-07 | Phase 8 | Complete |
| FORM-01 | Phase 15 | Pending |
| FORM-02 | Phase 9 | Gaps Found |
| FORM-03 | Phase 11 | Pending |
| FORM-04 | Phase 11 | Pending |
| FORM-05 | Phase 11 | Pending |
| FORM-06 | Phase 10 | Pending |
| FORM-07 | Phase 11 | Pending |
| FORM-08 | Phase 11 | Pending |
| FORM-09 | Phase 9 | Gaps Found |
| FORM-10 | Phase 11 | Pending |
| QUAL-01 | Phase 9 | Gaps Found |
| QUAL-02 | Phase 9 | Gaps Found |
| QUAL-03 | Phase 8 | Complete |
| QUAL-04 | Phase 15 | Pending |
| QUAL-05 | Phase 7 | Complete |
| QUAL-06 | Phase 9 | Gaps Found |
| FETCH-01 | Phase 12 | Pending |
| FETCH-02 | Phase 13 | Pending |
| FETCH-03 | Phase 15 | Pending |
| SIGN-01 | Phase 14 | Pending |
| SIGN-02 | Phase 14 | Pending |
| SIGN-03 | Phase 14 | Pending |
| GATE-01 | Phase 8 | Complete |
| GATE-02 | Phase 11 | Pending |
| GATE-03 | Phase 8 | Complete |
| GATE-04 | Phase 8 | Complete |
| GATE-05 | Phase 8 | Complete |
| GATE-06 | Phase 8 | Complete |
| GATE-07 | Phase 8 | Complete |
| GATE-08 | Phase 8 | Complete |
| GATE-09 | Phase 15 | Pending |
| GATE-10 | Phase 15 | Pending |
| PAY-01 | Phase 8 | Complete |
| PAY-02 | Phase 8 | Complete |
| EDIT-01 | — (v2.2) | Deferred |
| UPLD-01 | — (v2.2) | Deferred |
| E2E-01 | — (v2.2) | Deferred |

> **Три последние строки — НЕ требования v2.1, и фазы у них нет по решению.** `EDIT-01`,
> `UPLD-01` и `E2E-01` объявлены разделом «v2.2 Requirements» отложенными осознанно; в счёт
> «41/41» они не входят. Строки заведены здесь, чтобы идентификатор, названный в теле файла,
> не выглядел потерянным при сверке таблицы: отсутствие строки неотличимо от забытого
> требования, а «Deferred» называет состояние прямо.

> **Сноска к `Gaps Found` Фазы 7.** Столбец состояния один и потому не различает «машинная
> половина закрыта, ручная не исполнена» и «предмет требования не достигнут». Для двух требований
> фазы разница существенна. **FOUND-01:** машинная половина закрыта и верифицирована (подлинность
> вендоренного артефакта измерена — размер, полный SHA-384, подстрока версии); поведенческая
> открыта — «22 места работают на 2.0.10» закрывается только глазами, пункт 2 обхода 07-UAT.md,
> WINDOWS.md #11. **QUAL-05:** `Gaps Found` — корректное состояние, а не недосмотр: требование
> заблокировано до приземления плана 07-04 (мера целилась в хранилище, которого 2.0.10 не
> касается) и повторного обхода по ИСПРАВЛЕННОЙ процедуре. Ни одно из двух не закрывается
> исполнителем плана.

### Фаза 9 — свидетельства к пересмотру состояний (заполнено планом 09-09)

⚠️ **СОСТОЯНИЙ ЭТОТ РАЗДЕЛ НЕ ПЕРЕВОДИТ, И ЭТО НЕ ОСТОРОЖНОСТЬ, А ЗАПРЕТ.** Столбец `Status`
таблицы выше и флажки в теле файла план 09-09 не трогал ни одной правкой — неприкосновенность
проверена сличением строк до и после (`git show HEAD:… | grep` против рабочего файла, оба вывода
пусты). Преждевременная отметка «выполнено» — ровно та ошибка, что породила третий пробел Фазы 9
и потребовала отката коммитом `183511c`. Перевод состояний делает ПОВТОРНАЯ ВЕРИФИКАЦИЯ — шаг,
который единственный видит и зелёную суиту, и заполненные таблицы отметок обхода, и этот раздел.

⚠️ **РАЗДЕЛ НАБРАН СПИСКОМ, А НЕ ТАБЛИЦЕЙ, И ЭТО МАШИННОЕ ТРЕБОВАНИЕ.** Проверка
неприкосновенности состояний сличает строки ТАБЛИЦЫ; табличные строки с теми же идентификаторами
требований попали бы в её выборку и покрасили бы собственный гейт плана. Списочная форма делает
это невыразимым.

Источник наблюдений — `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md`,
обход 2026-08-30, **Chrome / macOS**, стенд `UAT9-` (account_id=50, 40 групп, 30 активных).

- **FORM-02**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly` → `113 passed`; поимённо `-k "scroll_cursor_survives or no_op_delete_does_not_double"` → `2 passed, 111 deselected`.
  - **наблюдённый признак:** проверка **3.5** обхода: «Обход по четырём шагам (F5 → выписать последнюю из 30 строк → удалить из первой десятки → долистать вниз → сверка) проведён; дыр и повторов в списке не обнаружено». ⚠️ Выписанного названия строки (шаг 3.5.2) отметка НЕ несёт, а правило самой проверки требует его прямо: «Отметка „список выглядел нормально“ закрытием этой проверки не считается».
  - **вердикт:** `не заработано` — клетка наблюдения заполнена ЧАСТИЧНО, и по правилу раздела частичная строка даёт `не заработано` без выбора у исполнителя. Что закрывает: повторный проход 3.5 с выписанным названием.
- **FORM-09**
  - **машинная проверка:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q` → `69 passed`, включая правило самодостаточности класса индикатора (план 09-08) и правило единого источника линейки (план 09-07).
  - **наблюдённый признак:** — (прочерк). Единственность источника свойств качества глазами не наблюдается вовсе: это свойство ПОСТРОЕНИЯ разметки, и утверждается оно сканером дерева, а не экраном.
  - **вердикт:** `заработано`.
- **QUAL-01**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -k "repeated_delete"` → `2 passed, 111 deselected`.
  - **наблюдённый признак:** проверка **6.2** обхода: «На Slow 3G двойное нажатие даёт ОДИН запрос `toggle`; „Отмена“ во время запроса остаётся нажимаемой».
  - **вердикт:** `заработано` — обе клетки заполнены фактом, и условие исхода выполнено: столкновение с буквой критерия 3 роадмапа СНЯТО решением владельца (вариант «б»), форма тумблера живёт по общему умолчанию цели блокировки `find button[type=submit]`. ⚠️ Цена названа: наблюдение 6.2 снято с ПРЕЖНЕГО механизма (блокировка чекбокса); на пришедшей ему на смену отбрасывающей синхронизации `hx-sync="this:drop"` двойное нажатие глазами не наблюдалось, и 6.2 обязана быть переснята.
- **QUAL-02**
  - **машинная проверка:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -k "indicator"` → `4 passed, 65 deselected`.
  - **наблюдённый признак:** проверка **6.1** обхода: «Точка на Slow 3G появляется и держится; при снятом троттлинге 5-6 переключений подряд — не мигнула ни разу», и этим подтверждено допущение **A3** («ответ тумблера укладывается быстрее 300 мс»), прежде взятое из записанного решения без замеров.
  - **вердикт:** `заработано`.
- **QUAL-06**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -k "toggle_fragment_keeps_the_toggle_id"` → `1 passed, 112 deselected` (разметочная половина: `id="group-toggle-N"` стабилен и в первичной отрисовке, и во фрагменте ответа).
  - **наблюдённый признак:** измерение **3.3** обхода, фактический ответ — **«нет»**: `document.activeElement` при нетронутой мыши вернул `<body>`, фокус на тумблере не остался (Chrome / macOS, 2026-08-30).
  - **вердикт:** `заработано` — обе клетки заполнены фактом, и исход останова решения принадлежит к тем, что СНИМАЮТ конфликт: владелец выбрал вариант «б». ⚠️ Цена названа: возврат фокуса ПОСЛЕ снятия блокировки — рассуждение, а не измерение; проверка 3.3 обязана быть пройдена заново на изменённой разметке.

#### Второй круг закрытия (планы 09-10 … 09-12)

⚠️ **СОСТОЯНИЙ И ЭТОТ ПОДРАЗДЕЛ НЕ ПЕРЕВОДИТ.** Столбец `Status` таблицы выше и флажки в теле файла
план 09-12 не тронул ни одной правкой — неприкосновенность утверждена сличением, СПОСОБНЫМ
ПОКРАСНЕТЬ: число изменённых строк таблицы уходит в переменную, исход даёт `[ "$N" = "0" ]`, и рядом
стоит антивакуумное утверждение — дифф файла непуст ИМЕННО работой плана (по одной добавленной
строке-заголовку записи на каждое из четырёх требований). Перевод состояний делает ПОВТОРНАЯ
ВЕРИФИКАЦИЯ.

⚠️ **ПОДРАЗДЕЛ НАБРАН СПИСКОМ, А НЕ ТАБЛИЦЕЙ, ПО ТОЙ ЖЕ ПРИЧИНЕ, ЧТО И ПЕРВЫЙ КРУГ:** табличные
строки с теми же идентификаторами требований попали бы в выборку проверки неприкосновенности и
покрасили бы собственный гейт плана.

**ПОЧЕМУ КРУГ ВТОРОЙ.** Три отметки первого круга (**3.3**, **6.2** и обе строки консоли пункта 7)
сняты с механизма, которого на экране больше нет: решение владельца по D-06 ↔ QUAL-06 (план 09-09)
убрало блокировку чекбокса, планы 09-10 и 09-11 закрыли оба расхождения обхода. Отметка **3.5**
проведена по всем шагам, но не несла выписанного названия строки. Шаг «кнопка подтверждения после
ОТКАЗА запроса» (WARN-2 / WR-02) не проводился ни разу.

Источник наблюдений — `.planning/phases/09-pilot-na-account-groups-skvoznoy-kontrakt-formy/09-UAT.md`,
раздел «Второй круг закрытия», обход **2026-08-31**, **Chrome / macOS**, стенд `UAT9-` (два аккаунта:
крупный для 3.5-Б и с числом групп не больше размера страницы для 7.3). Прогоны колонки «машинная
проверка» выполнены ЗАНОВО на сегодняшнем дереве (HEAD `b8d76e5`), а не переписаны из сводок.

- **FORM-02**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "no_declared_include_selector or scroll_cursor_survives or include_target_exception"` → `5 passed, 112 deselected` (правило объявленных селекторов и три утверждения идиомы SP-1 из сводки плана 09-11 плюс правило выживания курсора прокрутки).
  - **наблюдённый признак:** проверка **3.5-Б** второго круга. Выписанное название последней видимой строки первой порции — **`UAT9-g048`**, дословно. До удаления: `отрисовано: 30`, `сентинел: true`, счётчик «21 активная из 32 групп». После удаления одной строки и долистывания до низа: `отрисовано: 31`, `дубли: []`, счётчик «20 активных из 31 группы». `UAT9-g048` присутствует и, при пустых `дубли`, встречается ровно один раз; тождество числа сходится.
  - **вердикт:** `заработано` — обе клетки заполнены фактом, и выписанное название, которого не хватило первому кругу, в отметке есть. ⚠️ **Цена названа явно:** поля `пропало` и `лишнее` сырого вывода в отметку не пошли — снимавший однострочник нёс свой захардкоженный эталон имён, а стенд оказался другим (оговорка **О-2** артефакта обхода). Закрытие держится на двух признаках, от эталона не зависящих: тождестве числа и пустых `дубли`.
- **QUAL-01**
  - **машинная проверка:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "blocking_target or self_removing_wrapper"` → `2 passed, 69 deselected` (правило достижимости цели блокировки плана 09-10 и его отрицательный контроль); `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "repeated_delete"` → `2 passed, 115 deselected`.
  - **наблюдённый признак:** проверка **6.2-Б** второго круга: Slow 3G, фильтр по `toggle`, два быстрых нажатия — ушёл **ОДИН** запрос. Число, а не оценка.
  - **вердикт:** `заработано` — обе клетки заполнены фактом. Наблюдение снято с НОВОГО механизма: второе нажатие отбрасывает клиент отбрасывающей синхронизацией, а не блокировка чекбокса, и оговорка первого круга «6.2 снята с прежнего механизма» этим закрыта.
- **QUAL-02**
  - **машинная проверка:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "indicator"` → `4 passed, 67 deselected`.
  - **наблюдённый признак:** проверка **6.3** второго круга, проведённая ВПЕРВЫЕ. После отказа запроса (Offline, панель `group-del-2249`, снято не закрывая и не переоткрывая панель): «Удалить» — `type: "submit"`, `disabled: false`, `ariaBusy: null`; «Отмена» — `type: "button"`, `disabled: false`, `ariaBusy: null`. **WARN-2 / WR-02 не воспроизвёлся:** `sending` вернулось в исходное по ЗАВЕРШЕНИЮ запроса, а не по успеху. Во время запроса (Slow 3G): «Удалить» — `disabled: true`, `ariaBusy: "true"`, «Отмена» — нажимаема и нажалась. Плашка обрыва связи поднялась именно как плашка сети (`плашкаСети: true`, `плашкаСервера: false`) и назвала расхождение экрана с сервером своим вторым предложением.
  - **вердикт:** `заработано` — обе клетки заполнены фактом, сведение двух владельцев признака занятости планом 09-08 наблюдением подтверждено. ⚠️ **Цена названа явно, и она двойная.** Первое: проверка снята в ДВЕ части — «в полёте» под Slow 3G, «после отказа» под Offline, потому что на Offline запрос падает мгновенно и наблюдать «пока запрос идёт» не на чем (оговорка **О-1**); ни один шаг не пропущен, изменён режим сети первой половины. Второе: сверх проверки наблюдено, что «Отмена», нажатая во время летящего запроса, закрывает панель, но САМ ЗАПРОС НЕ ОТМЕНЯЕТ — группа всё равно удаляется. На вердикт это не влияет (6.3 спрашивает о нажимаемости, и кнопка нажимаема), но записано именованным отложенным пунктом `DEF-09-02` с назначенной **Фазой 10**.
- **QUAL-06**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_fragment_keeps_the_toggle_id"` → `1 passed, 116 deselected` (разметочная половина: идентификатор тумблера стабилен и в первичной отрисовке, и во фрагменте ответа).
  - **наблюдённый признак:** проверка **3.3-Б** второго круга, фактический ответ — **«да»**: Tab до чекбокса, Space, мышь не тронута, `document.activeElement` вернул тот же тумблер `id="group-toggle-2231"` (`<input class="toggle__input" type="checkbox" name="is_active" …>`).
  - **вердикт:** `заработано` — обе клетки заполнены фактом, и рассуждение, записанное в шапке проверки В ОЖИДАНИИ измерения, подтвердилось измерением. ⚠️ **Цена названа явно:** ответ ОПРОВЕРГАЕТ ответ первого круга («нет», `<body>`), но опровергает на ДРУГОМ механизме — блокировки чекбокса больше нет вовсе, и терять фокус стало не на чем. То есть закрыт не тот же самый вопрос, а вопрос о сегодняшнем экране; оговорка первого круга «3.3 обязана быть пройдена заново» этим исчерпана.
- **Опорный признак обхода «ответ 200 И ЧИСТАЯ КОНСОЛЬ» — проверка 7.3, отдельной строкой, потому что одному требованию он не принадлежит**
  - **машинная проверка:** правило `test_no_caller_declares_a_blocking_target_its_form_cannot_have` (план 09-10) и правило `test_no_declared_include_selector_names_an_id_the_document_lacks` (план 09-11) зелены — оба вошли в прогоны FORM-02 и QUAL-01 выше.
  - **наблюдённый признак:** на аккаунте с числом групп не больше размера страницы, при очищенной консоли и без фильтра: переключён один тумблер — строки `... on hx-disabled-elt returned no matches!` НЕ появилось; удалена группа через панель подтверждения — строки `... on hx-include returned no matches!` НЕ появилось. Число строк о неразрешённом селекторе — **0**.
  - **исход:** **опорный признак ВОССТАНОВЛЕН**, и это наблюдение ЗАКРЫВАЕТ повторный проход 7.1 и 7.2, который сводки планов 09-10 и 09-11 прямо назвали недостающим («после правки строка в браузере повторно не наблюдалась»). ⚠️ **Остаток назван поимённо, а не умолчан:** единственная объявленная остаточная строка — запись перечня `INCLUDE_TARGET_EXCEPTIONS['group-list-sentinel']` с назначенной **Фазой 15** (второе следствие DIV-09-01). На стенде проверки 7.3 её положение экрана недостижимо, поэтому здесь ожидался и наблюдён ноль; она остаётся открытой и этим кругом не закрывается.

#### Третий круг закрытия (планы 09-13 … 09-15)

⚠️ **СОСТОЯНИЙ ЭТА ЗАПИСЬ САМА НЕ ПЕРЕВОДИТ.** Столбец `Status` таблицы выше и флажки в теле файла
задача 1 плана 09-16 не тронула ни одной правкой — неприкосновенность утверждена сличением,
СПОСОБНЫМ ПОКРАСНЕТЬ: число изменённых строк таблицы уходит в переменную, исход даёт
`[ "$N" = "0" ]`, и рядом стоит антивакуумное утверждение — дифф файла непуст ИМЕННО работой задачи
(`A=$(git diff --numstat … | awk '{s+=$1} END{print s+0}'); [ "$A" -gt 0 ]`). Печать счётчика
конвейером без сличения гейтом не является: пустой дифф прошёл бы её зелёным.

⚠️ **НО ПЕРЕВОД ЗДЕСЬ УЖЕ НЕ У ВЕРИФИКАЦИИ, А У СЛЕДУЮЩЕЙ ЗАДАЧИ ЭТОГО ЖЕ ПЛАНА — И ЭТО СМЕНА
ПРАВИЛА, А НЕ ЕГО НАРУШЕНИЕ.** Два первых круга ждали повторной верификации. Прогон 2026-08-31
(HEAD `496ae40`) рекомендацию ВЫНЕС, но исполнять её отказался прямым текстом: «отмечать требования
Complete внутри незакрытой фазы значило бы то же самое, что фаза себе запретила». Тупик («верификация
не переводит, потому что фаза не закрыта; планы не переводят, потому что это дело верификации»)
разрывает задача 3 плана 09-16 — ПОСЛЕ закрытия обоих пунктов `gaps` и ПО НИМ. Клетка `Status`
меняется только там, где ниже названы закрытый пункт `gaps` И зелёная команда сегодняшнего дерева;
рекомендация отчёта основанием НЕ является.

⚠️ **ПОДРАЗДЕЛ НАБРАН СПИСКОМ, А НЕ ТАБЛИЦЕЙ, ПО ТОЙ ЖЕ ПРИЧИНЕ, ЧТО ПЕРВЫЙ И ВТОРОЙ КРУГИ:**
табличные строки с теми же идентификаторами требований попали бы в выборку проверки
неприкосновенности состояний и покрасили бы собственный гейт плана. Списочная форма делает это
невыразимым.

**ПОЧЕМУ КРУГ ТРЕТИЙ.** Прогон верификации оставил ДВА пункта `gaps`: **CR-01** (курсор бесконечной
прокрутки чинился абсолютным числом, снятым в момент ОТПРАВКИ и применённым к сентинелу момента
ОТВЕТА) и **ложный инвариант прозы** «внеполосный узел с ненайденной целью молчит», на котором стоит
опорный признак обхода «200 и чистая консоль», наследуемый Фазами 10-15. Первый закрыт планом 09-13,
второй — планом 09-14. Планом 09-15 закрыты сверх того WR-02, WR-03, WR-04 и IN-03.

⚠️ **НАБЛЮДЕНИЙ ТРЕТИЙ КРУГ НЕ ЗАВОДИТ ВОВСЕ, И ЭТО НАЗВАНО, А НЕ УМОЛЧАНО.** Браузерный стенд не
поднимался, обход не проводился, ни одной новой отметки в `09-UAT.md` не появилось. Поэтому позиция
«наблюдённый признак» ниже ССЫЛАЕТСЯ на наблюдения второго круга (3.3-Б, 6.2-Б, 3.5-Б, 6.3, 7.3),
снятые **2026-08-31 ДО правок третьего круга**, и на каждой строке прямо сказано, обесценила ли эти
наблюдения правка. Наблюдение, снятое с изменённого механизма, есть ровно то, из-за чего понадобился
второй круг, — молчать об этом здесь нельзя.

Прогоны колонки «машинная проверка» выполнены **ЗАНОВО на сегодняшнем дереве, HEAD `1fabfef`**
(2026-08-31), и ни одно число не переписано из сводок планов 09-13 … 09-15: сводка описывает дерево
на момент своего плана, а свидетельство обязано описывать дерево на момент перевода.

- **FORM-02**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "scroll_cursor or interleaved_portion_and_delete or control_positive_the_reverse_order or degrades_without_htmx or both_delete_forms_post_the_same_field_names or portion_route_never_answers or trigger_form_body_lands_on_the_filtered_listing"` → `13 passed, 124 deselected`. В выборке: три правила курсора прокрутки, несущее правило чередования плана 09-13 и его положительный контроль обратного порядка, обе пары деградации (`test_toggle_degrades_without_htmx`, `test_delete_degrades_without_htmx`), правило равенства наборов полей двух форм удаления (WR-03, план 09-15) и четыре правила маршрута порции (WR-04, план 09-15).
  - **наблюдённый признак:** **3.5-Б второго круга — и он ОБЕСЦЕНЕН правкой третьего круга; это первое, что здесь надо сказать.** Отметка снималась на механизме АБСОЛЮТНОГО смещения с четвёртым внеполосным узлом починки курсора; план 09-13 решением владельца `keyset` снял из дерева ЦЕЛИКОМ и смещение, и этот узел, и скрытое поле `rendered_rows`. Сегодняшний механизм (сентинел несёт ключ последней отрисованной строки, маршрут порции читает `after_id`) глазами не наблюдался ни разу. Взамен пришло то, чего у наблюдения не было вовсе: `missing` пункта `gaps` требовал не отметки, а РЕГРЕССИОННОГО ТЕСТА на тот порядок, в котором отказ живёт, — и он исполнен переходом цвета `5de9c56` (RED: строки 31-60 приезжали вторично, курсор уезжал с `offset=60` на `offset=29`) → `0326694` (GREEN), причём тест между коммитами не тронут ни на символ. Не наблюдена и добавка `scroll-lock` (D10 сводки 09-13) — она не покрыта ни тестом, ни глазом.
  - **вердикт:** `заработано` — оба держателя требования сняты: пункт `gaps` про CR-01 закрыт планом 09-13 переходом цвета, замечание WR-03 закрыто планом 09-15 (`test_both_delete_forms_post_the_same_field_names` с антивакуумом плюс поведенческая половина `test_the_trigger_form_body_lands_on_the_filtered_listing`). Буква требования — `method="post"`, непустой `action`, посимвольное равенство `hx-post` и `action`, `action` на маршрут полного документа — машинная по существу и зелена. ⚠️ **Цена названа тремя пунктами, а не сглажена.** (1) Поведенческую половину, привязанную к FORM-02 самой фазой, сегодня держит ПРАВИЛО, а не глаз: 3.5-Б описывает снятый механизм, и повторное наблюдение на `keyset` не проводилось. (2) Добавка `scroll-lock` не проверена ничем. (3) Закрытие WR-05 (литерал `limit=30` ушёл из шаблона сентинела) **не держит ни одно правило** — это находка самого плана 09-15, сделанная обходом тестов: отсутствия литерала не утверждает ни один тест, а ближайший `test_page_shows_thirty_rows_and_a_sentinel` сверяет ОТРЕНДЕРЕННЫЙ адрес и остаётся зелёным и при параметре, и при вернувшемся литерале; на шести местах соседних экранов литерал живёт до сих пор. Регресс WR-05 сегодня НЕВИДИМ, и переводом состояния это не закрывается.
- **FORM-09**
  - **машинная проверка:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly` → `73 passed`; поимённо по перечням исключений и их числам `-k "macro_born or born_of_a_component_macro or disabled_elt_exception or number_of_fragment_routes or number_of_oob_blocks"` → `9 passed, 64 deselected`.
  - **наблюдённый признак:** — (прочерк), как и в первом круге: единственность источника свойств качества глазами не наблюдается вовсе — это свойство ПОСТРОЕНИЯ разметки, и утверждается оно сканером дерева, а не экраном. Обесценивать здесь нечего: наблюдения у требования нет по существу. ⚠️ Зато проверено соседнее: третий круг ДВИНУЛ два инвентарных числа — `OOB_BLOCKS` 10 → 9 (план 09-13, снятый четвёртый внеполосный узел) и `FRAGMENT_ROUTES_DECLARED` 12 → 11 (план 09-15, маршрут порции приобрёл путь деградации и вышел из множества), — и оба движения поставлены ПРОГОНОМ покрасневшего правила с записью летописи, а не вычитанием. Беззвучно ни одно число не уехало.
  - **вердикт:** `заработано` — ни одним пунктом `gaps` требование не держалось (отчёт верификации, §«Requirements Coverage»: `✓ SATISFIED`), машинный признак зелен на сегодняшнем дереве, и правки третьего круга его не обесценили.
- **QUAL-01**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_is_idempotent_on_a_repeated_post or no_op_delete_does_not_double_a_row or repeated_delete_is_harmless or toggle_honours_the_posted_state or two_toggles_and_a_submit or delete_response_is_indistinguishable"` → `10 passed, 127 deselected`; и `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "blocking_target or self_removing_wrapper or cancel_is_never_disabled or disabled_elt"` → `8 passed, 65 deselected` (правило достижимости цели блокировки плана 09-10, его отрицательный контроль, «Отмена не блокируется никогда» и перечень `DISABLED_ELT_EXCEPTIONS` с отдельно утверждаемым числом).
  - **наблюдённый признак:** 6.2-Б и 6.3 второго круга — оба УЦЕЛЕЛИ, и это проверено по дереву, а не предположено. Механизм, на котором снято 6.2-Б, третий круг не трогал: `group_row.html:178` по-прежнему передаёт макросу-обёртке `sync='this:drop'` и ПУСТУЮ цель блокировки. Предмет 6.3 (`type`, `disabled`, `ariaBusy` кнопок «Удалить» и «Отмена») тоже цел: возврат признака занятости по `x-on:htmx:after-request` в `components/modal.html` не изменён ни на символ. ⚠️ Названо и то, что изменилось РЯДОМ: план 09-13 дописал в `show()` и `hide()` той же панели по одному вызову `classList` (добавка `scroll-lock`), и этот дописанный оператор глазами не наблюдался.
  - **вердикт:** `заработано` — но ТОЛЬКО ПОСЛЕ ОТВЕТА ВЛАДЕЛЬЦА, и порядок здесь несущий, а не косметический. ⚠️ **До ответа в этой строке стояло `не заработано`**, и причина была не в машинном признаке, который зелен, а в ТРЕТЬЕМ условии перевода. Первые два выполнялись и тогда: пункт `gaps` про цену холостого пути закрыт планом 09-14, серверная идемпотентность планом 09-15 УСИЛЕНА (присвоение присланного значения идемпотентно по построению, тогда как прежняя слепая инверсия давала идемпотентность только в паре). Открытым оставалось третье: оговорка требования «`hx-disabled-elt` **на каждой форме письма**» на пилотной форме не выполнена — атрибута там нет вовсе, защиту несёт `hx-sync="this:drop"`. Отступление ОСОЗНАННОЕ (решение владельца `record-kind`, план 09-10; объявлено `DISABLED_ELT_EXCEPTIONS` с летописью и проверяемым полем `callers`), забытой работой оно не является. Но запись «записи `overrides` этот исход не требует и не заводит» писалась планом 09-09, КОГДА ФОРМА ЕЩЁ ЖИЛА ПО ОБЩЕМУ УМОЛЧАНИЮ ЦЕЛИ БЛОКИРОВКИ; снятие атрибута произошло ПОСЛЕ неё и той записью не покрыто. Закрыть этот зазор суждением исполнителя — то же самое, что закрыть дефект удалением записи о нём, поэтому он и не закрывался до ответа. **Ответ получен остановом задачи 2 плана 09-16 (`gate="blocking-human"`): ветвь `accept-override`, принял `chubav`, `2026-09-01`** — запись выше в этом же подразделе и блок `overrides` во frontmatter `09-VERIFICATION.md`. Третье условие удовлетворено РЕШЕНИЕМ ВЛАДЕЛЬЦА, а не суждением исполнителя, и клетка `Status` переводится по нему вместе с закрытым пунктом `gaps` и зелёной командой.
- **QUAL-02**
  - **машинная проверка:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "indicator"` → `4 passed, 69 deselected` (имя класса не рантаймовое, класс самодостаточен, порог видимости отдельным правилом, отрицательный контроль порога).
  - **наблюдённый признак:** 6.1 первого круга и 6.3 второго — оба УЦЕЛЕЛИ. Предмет наблюдения — класс `.form-busy` и его порог `.3s` в `app/static/css/app.css`; правка третьего круга в этом файле ровно одна и предмету посторонняя: правило `.is-modal-open { overflow: hidden }` плана 09-13. Селектор индикатора по-прежнему зашит в макросе-обёртке и не передаётся ни одним вызывающим.
  - **вердикт:** `заработано` — ни одним пунктом `gaps` требование не держалось, машинный признак зелен на сегодняшнем дереве, наблюдения второго круга правками третьего не обесценены.
- **QUAL-06**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_fragment_keeps_the_toggle_id"` → `1 passed, 136 deselected` (разметочная половина: `id="group-toggle-N"` стабилен и в первичной отрисовке, и во фрагменте ответа).
  - **наблюдённый признак:** 3.3-Б второго круга — УЦЕЛЕЛ. `group_row.html` третьим кругом правился (план 09-13 снял `has_sentinel` и объявление включаемых данных, план 09-15 добавил скрытое поле `search` в форму-триггер удаления), но САМ ТУМБЛЕР не тронут: `group_row.html:179` печатает тот же `id='group-toggle-' ~ group.id`. ⚠️ Остаток назван: поведенческая половина остаётся `backstop` и по сводке 09-15 (D13) закрывается только глазом человека — суита не поднимает ни Alpine, ни htmx.
  - **вердикт:** `заработано` — ни одним пунктом `gaps` требование не держалось, машинный признак зелен на сегодняшнем дереве, наблюдение второго круга правками третьего не обесценено.
- **Опорный признак обхода «ответ 200 И ЧИСТАЯ КОНСОЛЬ» — отдельной строкой, потому что одному требованию он не принадлежит**
  - **правило:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "oob_target or claim_about_a_missing_oob_target or claim_site_without_the_runtime_event or idle_delete_path_really_ships"` → `5 passed, 132 deselected`. Держат признак теперь ДВЕ вещи, обе заведённые планом 09-14: правило прозы `test_every_claim_about_a_missing_oob_target_names_the_runtime_event` со своим отрицательным контролем и запись `OOB_TARGET_EXCEPTIONS` с назначенной **Фазой 15**, отдельно утверждаемым числом `OOB_TARGET_EXCEPTIONS_DECLARED = 2` и ПОВЕДЕНЧЕСКИМ третьим утверждением идиомы SP-1 (`test_the_idle_delete_path_really_ships_the_recorded_nodes`), краснеющим в обе стороны — и когда записанный узел перестал приезжать, и когда приехал незаписанный.
  - **наблюдение:** 7.3 второго круга — ноль строк о неразрешённом селекторе. **ОБЕСЦЕНЕНО ЧАСТИЧНО, и это надо назвать.** Отметка снималась ДО того, как план 09-14 прочитал вендоренный рантайм и установил, что при ненайденной цели внеполосного узла htmx 2.0.10 не молчит, а пишет `console.error` (`htmx:oobErrorNoTarget` → `fe` → `ae(…, {error})` → `H` → `console.error`). Её ноль верен ДЛЯ СВОЕГО СТЕНДА — боевого пути со своей существующей группой, — но не для холостого. Дерево при этом тоже сдвинулось: план 09-13 снял четвёртый внеполосный узел целиком, поэтому объявленных отступлений ДВА, а не три, и число это поставлено прогоном, а не арифметикой.
  - **исход:** остаточные строки консоли **названы поимённо, а не умолчаны**: `group-row-{group_id}` и `group-del-{group_id}` — оба печатает `delete_response.html`, оба достижимы ХОЛОСТЫМ путём удаления (чужая, несуществующая, уже удалённая группа), то есть кнопкой «назад» и повторной отправкой формы, ради безвредности которых неотличимость D-04-A и заведена. Оба несут обоснование и назначенную **Фазу 15**. Третье объявленное отступление того же семейства — `INCLUDE_TARGET_EXCEPTIONS['group-list-sentinel']` — планом 09-13 ЗАКРЫТО записью летописи, а не удалено молча: правило стоит заряженным, и вернувшееся объявление немедленно поднимет число.
- **Два пункта `gaps` отчёта верификации — чем закрыт каждый и чем это воспроизводится**
  - **пункт 1, CR-01 (курсор прокрутки):** закрыт планом 09-13 сменой ФОРМЫ КОНТРАКТА по решению владельца (`keyset`, добавкой `scroll-lock`): сентинел несёт ключ последней отрисованной строки, маршрут порции читает `after_id`, четвёртый внеполосный узел и скрытое поле `rendered_rows` ушли из дерева — величины, которую можно снять в один момент и применить к узлу другого момента, в контракте не осталось, то есть класс отказа закрыт НЕВЫРАЗИМОСТЬЮ, а не обложен проверками. Утверждается правилами `test_an_interleaved_portion_and_delete_never_double_a_row` и `test_control_positive_the_reverse_order_keeps_the_document_whole`. Воспроизводится: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "interleaved_portion_and_delete or control_positive_the_reverse_order"`. Переход цвета: `5de9c56` → `0326694`.
  - **пункт 2, ложный инвариант тишины внеполосного узла:** закрыт планом 09-14 правкой ШЕСТИ мест прозы — не четырёх: число поставлено измерением по дереву, расхождение с отчётом записано в шапке перечня, а два места сверх плана суть ОБА файла области уведомления шелла, то есть общий канал обратной связи вехи, — и именованием остатка записью `OOB_TARGET_EXCEPTIONS`. Утверждается правилами `test_every_claim_about_a_missing_oob_target_names_the_runtime_event`, `test_the_number_of_oob_target_exceptions_is_the_declared_one`, `test_every_oob_target_exception_carries_a_reason_and_an_assigned_phase` и `test_the_idle_delete_path_really_ships_the_recorded_nodes`. Воспроизводится: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "oob_target or claim_about_a_missing_oob_target or claim_site_without_the_runtime_event or idle_delete_path_really_ships"`. Переходы цвета: `0134ef7` → `2898095` и `b5e3af3` → `571e45f`.
  - **что закрытием этих пунктов НЕ стало:** граница гейта G-2 и счётчика отставания НЕ расширена, и `NOT_YET_CONVERTED_COUNT` не двинулось (34 → 34). Это ИЗМЕРЕНИЕ плана 09-15, а не недосмотр: предмет гейта — только POST-обработчики, а маршрут порции объявлен GET, поэтому переведённый на слой ответа GET-вход счётчику невидим — как невидим и обратный случай, такой вход, вернувший себе собственное перенаправление. Расширение области обхода объявлено решением ВЕХИ и передано дальше нетронутым, с записью летописи у самой константы.

- **Решение владельца по отступлению QUAL-01 — ПРОДУБЛИРОВАНО ЗДЕСЬ, потому что отчёт верификации перегенерируется**
  - **ветвь:** `accept-override` — ответ владельца на останов задачи 2 плана 09-16, процитированный, а не выведенный. Ветви `amend-requirement` (правка текста требования) и `hold` (оставить QUAL-01 в `Gaps Found` до следующего прогона) НЕ выбраны. Следствие названо прямо: текст требования QUAL-01 не двигается ни на символ, и Фаза 15 продолжает сверять 47 форм по ПРЕЖНЕЙ букве.
  - **принял:** `chubav`, `2026-09-01`. ⚠️ **Провенанс имени назван, а не умолчан:** владелец ответил ИДЕНТИФИКАТОРОМ ВЕТВИ и имени рядом не набрал; значение снято исполнением `git config user.name` → `chubav` (почта `chubav@MacBook-Pro-Aleksandr.local`), то есть это личность отвечавшего на останов, а НЕ подпись, поставленная им самим. Записано видимой строкой затем, чтобы следующий читатель мог ИСПРАВИТЬ атрибуцию, а не унаследовать её молча. Пустым поле не оставлено и выдуманным не заполнено.
  - **что принято:** РОВНО ОДНО расхождение, и его граница названа. Исход требования достигнут и наблюдён — 6.2-Б: один запрос на два быстрых нажатия; 6.3: «Отмена» нажимаема и нажалась во время летящего запроса. Цель блокировки «только `find button[type=submit]`» исполнена БУКВАЛЬНО: печатаемых вхождений в дереве два, оба именно этой цели. Не выполнена ровно оговорка «`hx-disabled-elt` **на каждой форме письма**»: на пилотной форме тумблера атрибута нет вовсе, и защиту от второго нажатия несёт `hx-sync="this:drop"`. Серверная идемпотентность планом 09-15 УСИЛЕНА — присвоение присланного значения идемпотентно по построению, тогда как прежняя слепая инверсия давала идемпотентность только в паре.
  - **чем это НЕ БЫЛО ПОКРЫТО раньше — и это вся причина, по которой решение понадобилось:** отступление объявлено записью `DISABLED_ELT_EXCEPTIONS['components/form_wrapper.html']` с летописью из трёх строк и ПРОВЕРЯЕМЫМ полем `callers` (сканер вызывающих меряет дерево, и вызывающий, молча начавший или переставший передавать своё значение, краснеет), забытой работой оно не является. Но строка «записи `overrides` этот исход не требует и не заводит» писалась планом **09-09** — раздел «ЗАПИСЬ ИСХОДА D-06 ↔ QUAL-06» ниже, — КОГДА ФОРМА ТУМБЛЕРА ЕЩЁ ЖИЛА ПО ОБЩЕМУ УМОЛЧАНИЮ ЦЕЛИ БЛОКИРОВКИ. Снятие атрибута произошло планом **09-10** (решение владельца `record-kind`), то есть ПОСЛЕ неё, и той записью не покрыто. Принятое сейчас решение покрывает ИМЕННО ПОЛОЖЕНИЕ ПОСЛЕ 09-10.
  - **где живёт запись:** блок `overrides` во frontmatter `09-VERIFICATION.md`, набранный ДОСЛОВНО по образцу самого отчёта (§«Образец записи `overrides`») в полях `must_have` и `reason`, с заполненными `accepted_by` и `accepted_at`; потребованные при принятии уточнения добавлены ОТДЕЛЬНЫМИ КЛЮЧАМИ именно затем, чтобы образец остался дословным. Правка отчёта ТОЛЬКО ДОБАВЛЯЮЩАЯ: `git diff --numstat` показывает `65	0` — удалённых строк ноль. ⚠️ Счётчик `overrides_applied: 0` во frontmatter отчёта НАМЕРЕННО НЕ ТРОНУТ: его исправление было бы ИЗМЕНЕНИЕМ существующей строки, а прохибиция плана допускает только добавление (угроза T-09-16-02). Пересчитает его следующий прогон верификации — тот самый, который этот файл и перегенерирует.

- **ПЕРЕВОД СОСТОЯНИЙ — исполнен задачей 3 плана 09-16, и вот на чём стоит каждая клетка**
  - **FORM-02:** `Gaps Found` → `Complete`. Закрытый пункт `gaps` — первый (CR-01), закрыт планом 09-13 сменой формы контракта курсора; плюс замечание WR-03, закрытое планом 09-15. Команда признака: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "scroll_cursor or interleaved_portion_and_delete or control_positive_the_reverse_order or degrades_without_htmx or both_delete_forms_post_the_same_field_names or portion_route_never_answers or trigger_form_body_lands_on_the_filtered_listing"` → `13 passed, 124 deselected`.
  - **FORM-09:** `Gaps Found` → `Complete`. Ни одним пунктом `gaps` не держалось. Команда признака: `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly` → `73 passed`.
  - **QUAL-01:** `Gaps Found` → `Complete`. Закрытый пункт `gaps` — второй (цена холостого пути), закрыт планом 09-14; третье условие удовлетворено решением владельца `accept-override` от `2026-09-01`. Команда признака: `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "blocking_target or self_removing_wrapper or cancel_is_never_disabled or disabled_elt"` → `8 passed, 65 deselected`.
  - **QUAL-02:** `Pending` → `Complete`. Ни одним пунктом `gaps` не держалось. Команда признака: `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "indicator"` → `4 passed, 69 deselected`.
  - **QUAL-06:** `Pending` → `Complete`. Ни одним пунктом `gaps` не держалось. Команда признака: `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_fragment_keeps_the_toggle_id"` → `1 passed, 136 deselected`.
  - **чем основание НЕ является:** блок рекомендации состояний из frontmatter отчёта верификации основанием перевода не служил НИ ДЛЯ ОДНОЙ клетки и в качестве основания здесь не записан — его имя намеренно не набирается в этом файле даже как ссылка, потому что собственный гейт плана сличает добавленные строки на его отсутствие. Совпадение с ним по трём требованиям (FORM-09, QUAL-02, QUAL-06) есть следствие того, что машинный признак один и тот же, а НЕ того, что рекомендация исполнена. По FORM-02 и QUAL-01 отчёт рекомендовал ДЕРЖАТЬ `Gaps Found` — обе клетки всё равно переведены, потому что оба державших их пункта `gaps` закрыты ПОСЛЕ прогона отчёта (планами 09-13, 09-14, 09-15), а по QUAL-01 получено ещё и решение владельца. То есть перевод разошёлся с рекомендацией там, где расходятся факты, и совпал там, где совпадают признаки.
  - **флажки в теле файла не тронуты ни одной правкой:** их снимает закрытие фазы, а не этот план.
- **ЧЕГО ПЕРЕВОД СОСТОЯНИЙ НЕ ЗАКРЫВАЕТ — четыре открытых пункта, названных поимённо, чтобы перевод их не проглотил**
  - **(а) Закрытие WR-05 не держит НИ ОДНО правило.** Литерал `limit=30` из шаблона сентинела экрана групп ушёл (план 09-13), но отсутствия его не утверждает ни один тест: ближайший `test_page_shows_thirty_rows_and_a_sentinel` сверяет ОТРЕНДЕРЕННЫЙ адрес (`assert "limit=30" in sentinels[0]`) и остаётся зелёным и при параметре, и при вернувшемся литерале. Регресс сегодня НЕВИДИМ. На шести местах соседних экранов литерал живёт до сих пор: `ads/list.html`, `ads/partial_cards.html`, `accounts/list.html`, `accounts/partial_cards.html`, `schedules/list.html`, `schedules/partial_cards.html`. **Адресовано:** повторной верификации и Фазе 10 как естественный предмет правила «размер страницы не набирается в разметке».
  - **(б) Добавка `scroll-lock` не покрыта НИ ТЕСТОМ, НИ ГЛАЗОМ.** Правило `.is-modal-open { overflow: hidden }` и поднимающий его Alpine отгружены планом 09-13, но «список за открытой панелью стоит» тестом не снимается, а обходом не наблюдалось. Это D10 сводки 09-13, объявленный там `human_judgment`. **Адресовано:** обходу — открыть панель подтверждения на списке длиннее экрана и попробовать прокрутить.
  - **(в) Счётчик отставания `NOT_YET_CONVERTED_COUNT` СЛЕП К GET-ВХОДАМ В ОБЕ СТОРОНЫ,** и потому не двинулся (34 → 34) при переводе маршрута порции на слой ответа. Предмет гейта G-2 — только POST-обработчики. Невидим и обратный случай: GET-вход, вернувший себе собственное перенаправление, гейт не поймает. Граница НАЗВАНА записью летописи у самой константы и не расширена: расширение области обхода — решение ВЕХИ. **Адресовано:** решению вехи, не закрыто.
  - **(г) Отметка 3.5-Б обесценена ЦЕЛИКОМ.** Она снималась на механизме АБСОЛЮТНОГО смещения, который план 09-13 удалил из дерева вместе с четвёртым внеполосным узлом и полем `rendered_rows`. Механизм `keyset` глазами не наблюдался НИ РАЗУ. Поведенческую половину FORM-02 сегодня держит правило чередования с переходом цвета, а не глаз. **Адресовано:** обходу — повторный проход 3.5 на сегодняшнем механизме.
  - **почему это не помешало переводу и почему всё равно записано:** ни один из четырёх пунктов не является пунктом `gaps` и ни один не опровергает буквы переведённых требований — (а) и (в) суть отсутствие ПРАВИЛА там, где свойство сегодня верно, (б) и (г) суть отсутствие НАБЛЮДЕНИЯ. Но требование, переведённое в `Complete`, читается следующими фазами как ФАКТ, и молчание о том, что часть его признаков держится измерением, а не стоящим правилом, было бы ровно тем дрейфом, против которого заведены гейты. Записано здесь затем, чтобы повторная верификация нашла эти пункты открытыми, а не выводила их заново.

#### Четвёртый круг закрытия (планы 09-17 … 09-19)

⚠️ **СОСТОЯНИЯ ЭТИМ КРУГОМ НЕ ПЕРЕВЕДЕНЫ НИ ОДНИМ СИМВОЛОМ, И ЭТО ВЫВОД ИЗ СОБСТВЕННОЙ ИСТОРИИ ФАЗЫ,
А НЕ ОСТОРОЖНОСТЬ.** Перевод пяти клеток Фазы 9 в `Complete` внутри незакрытой фазы делался **ДВАЖДЫ
и ДВАЖДЫ откатывался** — коммитами `183511c` и `4fe89da`. Второй откат случился не от избытка
строгости: вердикт FORM-02 третьего круга опирался **поимённо** на закрытие WR-03, которое дерево
отменило **двумя коммитами позже** (ответ тумблера стирал строку поиска из скрытого поля формы
удаления — CR-02). Условие перевода — **вердикт `passed` ПОВТОРНОЙ ВЕРИФИКАЦИИ**, а не суждение
исполнителя о том, что разрывы закрыты. Все пять клеток читаются `Gaps Found` и до, и после этой
записи; флажки в теле файла не тронуты. **Непереведённые клетки — это решение, а не забытая работа.**

⚠️ **ССЫЛКА НА БЛОК РЕКОМЕНДАЦИЙ ОТЧЁТА ВЕРИФИКАЦИИ НЕ НАБИРАЕТСЯ НИ В ОДНОМ ВИДЕ.** Свидетельство
опирается на ЗАКРЫТЫЕ РАЗРЫВЫ, зелёные прогоны сегодняшнего дерева и НАБЛЮДЕНИЯ обхода, а не на то,
что отчёт посоветовал.

⚠️ **ПОДРАЗДЕЛ НАБРАН СПИСКОМ, А НЕ ТАБЛИЦЕЙ, ПО ТОЙ ЖЕ ПРИЧИНЕ, ЧТО ПЕРВЫЙ, ВТОРОЙ И ТРЕТИЙ КРУГИ:**
проверка неприкосновенности состояний сличает строки ТАБЛИЦЫ, и табличные строки с теми же
идентификаторами требований покрасили бы собственный гейт плана. Списочная форма делает это
невыразимым.

**ПОЧЕМУ КРУГ ЧЕТВЁРТЫЙ.** Прогон верификации (`09-VERIFICATION.md`, 2026-09-01, HEAD `d28dd78`)
оставил `status: gaps_found` с ДВУМЯ пунктами `gaps`, `behavior_unverified: 1` (SC4) и ТРЕМЯ
пунктами человеческой проверки. Оба пункта `gaps` — дефекты САМОГО КОНТРАКТА, а не местные помарки, и
оба внесены тем же кругом, который закрыл прошлый блокер. Закрыты они планами 09-17 и 09-18;
остаточная неопределённость ревизии (WR-04, WR-05) — планом 09-19; **половина, которой не даёт ни
один инструмент проекта, — эффект в живом браузере — закрыта ОБХОДОМ 2026-09-02.**

**ИСТОЧНИК НАБЛЮДЕНИЙ** — `09-UAT.md`, раздел «Четвёртый круг закрытия», обход **2026-09-02**,
Chrome (версия не названа) / ОС не названа, **на ПРОДЕ `https://broadcaster.all-torgi.ru`, аккаунт 53**
(прод и рабочее дерево смотрят в одну базу `192.168.0.9:5432/broadcaster`). Поколение дерева
**подтверждено тремя пробами в консоли, а не предположено**: правило `is-modal-open` в таблице стилей
→ `1`; `document.body.dataset.htmxFailureWired` → `"1"`; `includes('destroy()')` по отрисованной
разметке `/accounts/53/groups` → `true` (проза шапки `modal.html` лежит в Jinja-комментариях и при
отрисовке стирается, поэтому совпадение может прийти только из объекта `x-data`). **Три проверки из
трёх пройдены, расхождений НЕ найдено ни одного, стенд убран и это проверено машинно**
(`removed: groups=35 accounts=1`, затем `UAT9- rows: groups=0 accounts=0`).

Прогоны колонки «машинная проверка» выполнены **ЗАНОВО на сегодняшнем дереве, HEAD `2bd7ab3`**
(2026-09-02), и ни одно число не переписано из сводок планов 09-17 … 09-19: сводка описывает дерево
на момент своего плана, а свидетельство обязано описывать дерево на момент записи.

- **FORM-02**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "scroll_cursor or interleaved_portion_and_delete or control_positive_the_reverse_order or degrades_without_htmx or both_delete_forms_post_the_same_field_names or portion_route_never_answers or trigger_form_body_lands_on_the_filtered_listing or delete_form_render_sites or third_render_site"` → **`15 passed, 128 deselected`**. В выборке против третьего круга ДВА новых правила плана 09-18: `test_the_number_of_delete_form_render_sites_is_the_declared_one` (объявленное число мест отрисовки формы удаления — `3`; четвёртое, заведённое молча, обязано покраснеть) и `test_the_toggle_response_trigger_form_body_lands_on_the_filtered_listing` — ПОВЕДЕНЧЕСКОЕ правило третьего места, утверждающее не только НАБОР ИМЁН полей, но и ЗНАЧЕНИЕ. Различие несущее: на сломанном дереве имя поля БЫЛО, а величины не было, и правило на одних множествах имён зеленело бы ровно на сломанном дереве.
  - **наблюдённый признак:** **4.1, 4.2 и 4.3 четвёртого круга — все три, и это первое наблюдение FORM-02 на СЕГОДНЯШНЕМ дереве.** (1) Курсор `keyset` наблюдён ГЛАЗАМИ ВПЕРВЫЕ: адрес сентинела дословно `/accounts/53/groups/partial?after_id=2296&limit=30` — `after_id=` есть, `offset=` нет ни одного; сходимость изнутри (тумблер `2267`, сентинел `2296`, разница ровно `29` — тридцать строк порции при `PAGE_SIZE = 30` и `order_by(Group.id)`) подтверждает, что ключ взят у последней отрисованной строки. Сверка списка с самим собой расхождений не дала, счётчик сошёлся. (2) Путь деградации FORM-02 наблюдён БУКВАЛЬНО: при мёртвом Alpine форма удаления ушла обычным POST-ом, панель не открылась, документ перезагрузился, и приземление легло на `…/accounts/53/groups?search=UAT9-g020` — **фильтр уцелел**. (3) Ветвь `HX-Location` дала `204` с заголовком перехода и адрес `…?search=UAT9-needle`. ⚠️ **Названо и то, чего наблюдение НЕ дало:** название последней видимой строки владелец дословно не транскрибировал (оговорка **О-6** артефакта обхода), и у шагов 4.1.11 / 4.1.12 нет литеральных значений — вкладка закрыта до транскрипции (**О-7**); литералы НЕ сочинены. Слабость **О-7** компенсирована шагом 4.2.6, где тот же механизм измерен буквальными выражениями и на более опасной ветке.
  - **вердикт:** `заработано` — **ОБА держателя требования сняты, и каждый назван поимённо.** **CR-01** (добавка `scroll-lock` никогда не снимала признак `is-modal-open` на htmx-пути удаления) закрыт планом 09-17 ветвью `destroy-guard`, выбранной ВЛАДЕЛЬЦЕМ на останове `gate="blocking-human"`: метод `destroy()` объекта `x-data` снимает признак на ветви СНОСА узла под несущей проверкой `if (this.open)` — число путей снятия стало равно числу путей ухода узла. Воспроизводится правилом `test_the_scroll_lock_never_survives_the_teardown_of_the_panel` (ПОВЕДЕНЧЕСКИМ: объект `x-data` исполняется в интерпретаторе JS со стаб-документом) и переходом цвета `2f9875f` (RED, код прогона РОВНО `1`) → `00b6879` (GREEN), исполненным в отдельном отсоединённом рабочем дереве на `RED_SHA`, а не утверждённым парой коммитов; тело правила между коммитами не тронуто ни на байт. **CR-02** (ответ тумблера стирал строку поиска, отменяя починку WR-03 двумя коммитами позже) закрыт планом 09-18: строка поиска доезжает до ВСЕХ ТРЁХ мест отрисовки формы удаления, переход цвета `78c6622` (RED, код `1`) → `74e1de7` (GREEN). ⚠️ **Цена третьего круга — три пункта — СНЯТА НЕ ПОЛНОСТЬЮ, и остаток назван ниже отдельной позицией:** (1) поведенческая половина ТЕПЕРЬ наблюдена глазом (4.1 / 4.2 / 4.3), а не только правилом; (2) добавка `scroll-lock` ТЕПЕРЬ покрыта и правилом (план 09-17), и наблюдением (4.2.6); (3) закрытие WR-05 ТРЕТЬЕГО круга по-прежнему **не держит ни одно правило** — маршрутизировано `DEF-09-03`.
- **FORM-09**
  - **машинная проверка:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly` → **`73 passed`**; поимённо по перечням исключений и их числам `-k "macro_born or born_of_a_component_macro or disabled_elt_exception or number_of_fragment_routes or number_of_oob_blocks"` → **`9 passed, 64 deselected`**.
  - **наблюдённый признак:** **— (прочерк), и причина та же, что в первом и третьем кругах: наблюдения у требования нет ПО СУЩЕСТВУ.** Единственность источника свойств качества — свойство ПОСТРОЕНИЯ разметки, утверждаемое сканером дерева, а не экраном; обесценивать здесь нечего. ⚠️ Зато проверено соседнее, и это относится к обходу напрямую: четвёртый круг ЗАВЁЛ новое инвентарное число — `DELETE_FORM_RENDER_SITES_DECLARED = 3` (план 09-18), — и оно поставлено ПРОГОНОМ покрасневшего правила, а не арифметикой. Наблюдение 4.3.4 показало ЗНАЧЕНИЕ поля в третьем месте отрисовки (`'UAT9-g020'`), то есть подтвердило глазом ровно то, что новое правило утверждает машинно.
  - **вердикт:** `заработано` — ни одним пунктом `gaps` требование не держалось, машинный признак зелен на сегодняшнем дереве, и правки четвёртого круга его не обесценили: перечни исключений не выросли молча, а прибавившееся число объявлено с зубами.
- **QUAL-01**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_is_idempotent_on_a_repeated_post or no_op_delete_does_not_double_a_row or repeated_delete_is_harmless or toggle_honours_the_posted_state or two_toggles_and_a_submit or delete_response_is_indistinguishable or every_claim_about_the_toggle_names_the_mechanism or control_negative_a_toggle_claim"` → **`11 passed, 132 deselected`**; и `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "blocking_target or self_removing_wrapper or cancel_is_never_disabled or disabled_elt"` → **`8 passed, 65 deselected`**. Против третьего круга прибавилось правило прозы плана 09-18 о механизме идемпотентности тумблера с отрицательным контролем (`TOGGLE_CLAIM_SITES_MEASURED = 2`): проза, называвшая снятую форму (слепую инверсию) вместо действующей (присвоение присланного состояния), теперь краснеет.
  - **наблюдённый признак:** 4.1 и 4.3 четвёртого круга. Тумблер переключается и при живом Alpine (4.1.2, строка перерисовалась НА МЕСТЕ, позиция прокрутки не потеряна), и при мёртвом (4.3.3, ответ `200`, htmx жив) — то есть исход требования «двойное нажатие не создаёт второй записи» держит механизм, наблюдённый в обоих мирах. Отметки 6.2-Б и 6.3 второго круга при этом УЦЕЛЕЛИ по предмету: четвёртый круг правил `components/modal.html` (метод `destroy()`, план 09-17) и `group_row.html` (скрытое поле строки поиска в форме тумблера, план 09-18), но ни отбрасывающей синхронизации `hx-sync="this:drop"`, ни пустой цели блокировки, ни возврата состояния отправки по `x-on:htmx:after-request` не тронул — это записанное решение плана 09-18 («БОЛЬШЕ В ЭТОЙ ФОРМЕ НЕ МЕНЯЕТСЯ НИЧЕГО»).
  - **вердикт:** `заработано` — и **отступление от буквы «`hx-disabled-elt` на каждой форме письма» ПОВТОРНО НЕ ПЕРЕСМАТРИВАЕТСЯ.** Решение владельца ветвью `accept-override` принято **2026-09-01** и записано ДВАЖДЫ — блоком `overrides` во frontmatter `09-VERIFICATION.md` и подразделом третьего круга выше. Четвёртый круг его **наследует, а не переигрывает**: переоткрывать принятое решение исполнителем — то же самое, что закрывать дефект удалением записи о нём. Цель блокировки на пилотной форме тумблера недостижима по построению, защиту несёт `hx-sync="this:drop"`, отступление объявлено `DISABLED_ELT_EXCEPTIONS` с летописью и проверяемым полем `callers`.
- **QUAL-02**
  - **машинная проверка:** `uv run pytest tests/test_templates/test_htmx_markup_gates.py -q -p no:randomly -k "indicator"` → **`4 passed, 69 deselected`**; и правила плашек отказа плана 09-19 `uv run pytest tests/test_pages/test_shell.py tests/test_pages/test_history_retry.py -q -p no:randomly -k "failure_banner or retry_panel or unguarded_banner or guard_lives_on_the_node"` → **`10 passed, 193 deselected`**. Второй прогон — новый в этом круге: `FAILURE_BANNER_HANDLERS_MEASURED = 2`, ПОВЕДЕНЧЕСКИЙ замер регистраций (`test_the_failure_banner_registers_its_handlers_once_per_body`) с антивакуумом, правило места признака (`test_the_failure_banner_guard_lives_on_the_node_it_wires`) и отрицательный контроль накопления слушателей.
  - **наблюдённый признак:** **4.2.7 — 4.2.9 четвёртого круга, и это ЕДИНСТВЕННОЕ место, где остаточная неопределённость WR-05 закрывается по существу.** `document.body.dataset.htmxFailureWired` → `"1"`, и после **ТРЁХ** переходов `HX-Location` в одной сессии документа (документ не перезагружался — `window.__uat4` → `'жив'`) значение осталось `"1"`; `getEventListeners(document.body)['htmx:responseError'].length` → **`1`**, `['htmx:sendError'].length` → **`1`**. ⚠️ **Почему считалось именно ЧИСЛО СЛУШАТЕЛЕЙ, а не значение признака:** `dataset` выглядит одинаково `"1"` и когда обработчики повешены однажды на тот же узел, и когда узел тела сменился, признак потерялся, а скрипт отработал заново. Различает эти два положения только счёт слушателей. Ревизия назвала эту неопределённость прямо, и суита её не снимает: правило плана 09-19 исполняет сценарий со **стаб-узлом** и подмены самого узла тела не воспроизводит.
  - **вердикт:** `заработано` — ни одним пунктом `gaps` требование не держалось, машинный признак зелен на сегодняшнем дереве, а остаточная неопределённость WR-05 **измерена в браузере**, а не оставлена рассуждением. Отметки 6.1 первого круга и 6.3 второго уцелели по предмету: класс `.form-busy` и его порог `.3s` в `app/static/css/app.css` четвёртым кругом не тронуты — единственная правка этого файла в круге посторонняя предмету (летопись к абзацу `:947-960` о добавке `scroll-lock`, план 09-17).
- **QUAL-06**
  - **машинная проверка:** `uv run pytest tests/test_pages/test_account_groups.py -q -p no:randomly -k "toggle_fragment_keeps_the_toggle_id"` → **`1 passed, 142 deselected`** (разметочная половина: `id="group-toggle-N"` стабилен и в первичной отрисовке, и во фрагменте ответа); и правило тождества узла плана 09-17 `uv run pytest tests/test_templates/test_components.py -q -p no:randomly -k "delete_response_removes_the_very_node_that_owns_the_scroll_lock"` → **`1 passed, 53 deselected`**.
  - **наблюдённый признак:** **4.1.5 — 4.1.7 четвёртого круга, снятые ЗАНОВО, потому что строка группы третьим и четвёртым кругами правилась.** Ответ — **«ДА»**: `document.activeElement` вернул дословно `<input class="toggle__input" type="checkbox" name="is_active" id="group-toggle-2267" value="1" checked="">`, то есть после подмены строки фокус остался на элементе **С ТЕМ ЖЕ `id`**, что был до нажатия Space; мышь не тронута. Это поведенческая половина требования, объявленная `backstop` и закрываемая только глазом человека — суита не поднимает ни Alpine, ни htmx. ⚠️ Наблюдение снято ПОСЛЕ того, как `group_row.html` получил третье скрытое поле строки поиска (план 09-18): правка форму тумблера по предмету QUAL-06 не задела, и это подтверждено не чтением, а измерением.
  - **вердикт:** `заработано` — ни одним пунктом `gaps` требование не держалось; разметочная половина зелена на сегодняшнем дереве, поведенческая **наблюдена заново на дереве после всех правок круга**, и наблюдение первого круга («нет», `<body>`), снятое на снятом механизме, опровергнуто уже вторым кругом и подтверждено четвёртым.
- **ДВА ПУНКТА `gaps` ОТЧЁТА — отдельной позицией, потому что именно они держали `status: gaps_found`**
  - **`gap` 1 — CR-01: признак блокировки прокрутки умел подниматься и не умел сниматься на единственном пути, которым панель уходит из документа.** Чем закрыт: планом 09-17, ветвь `destroy-guard` (ВЫБРАНА ВЛАДЕЛЬЦЕМ на останове `gate="blocking-human"`, исполнитель её не выводил). Чем утверждается: ПОВЕДЕНЧЕСКИМИ правилами `test_the_scroll_lock_never_survives_the_teardown_of_the_panel` и `test_the_teardown_of_a_closed_panel_keeps_an_open_sibling_locked` плюс положительный контроль подъёма со сверкой имени класса по `app/static/css/app.css` и отрицательный контроль на подставленном выражении. Чем воспроизводится: `uv run pytest tests/test_templates/test_components.py -q -p no:randomly -k "scroll_lock or teardown_of_the_panel or teardown_of_a_closed_panel or panel_raises or delete_response_removes_the_very_node or without_a_teardown_path"` → **`5 passed, 49 deselected`** (HEAD `2bd7ab3`). Переход цвета: `2f9875f` (RED, код прогона РОВНО `1`) → `00b6879` (GREEN), исполнен в отдельном отсоединённом дереве на `RED_SHA`. ⚠️ **ПРАВИЛО ЗДЕСЬ ПОВЕДЕНЧЕСКОЕ — ОНО ИСПОЛНЯЕТ ОБЪЕКТ `x-data` в интерпретаторе JS со стаб-документом, — И ЭТО НЕСУЩЕЕ РАЗЛИЧИЕ.** Разметочное правило на его месте («в файле есть строка `classList.remove`») зеленело бы ВАКУУМОМ: строка снятия в `hide()` присутствовала и на сломанном дереве, а отказ жил в том, что `hide()` на htmx-пути **не зовётся вовсе** — узел уносится внеполосным снятием. **Наблюдением закрыт шагом 4.2.6:** панель открылась, её корень унесён, `HX-Location` перевёл без перезагрузки документа — и `contains('is-modal-open')` → `false`, `overflow` → `visible`.
  - **`gap` 2 — CR-02: ответ тумблера стирал строку поиска из скрытого поля формы удаления, отменяя починку WR-03 двумя коммитами после её приземления.** Чем закрыт: планом 09-18 — обработчик тумблера принимает строку поиска телом формы и проводит её через тот же `_clean_search`, оба выхода собирают адрес помощником `_screen_url`, форма тумблера несёт своё скрытое поле, а число мест отрисовки формы удаления объявлено (`DELETE_FORM_RENDER_SITES_DECLARED = 3`) с зубами. Чем утверждается: `test_the_toggle_response_trigger_form_body_lands_on_the_filtered_listing` (ПОВЕДЕНЧЕСКОЕ — тело формы ИЗ ОТВЕТА ТУМБЛЕРА приземляется на отфильтрованную выдачу), `test_the_number_of_delete_form_render_sites_is_the_declared_one` и правило третьего места, утверждающее ЗНАЧЕНИЕ, а не только набор имён. Чем воспроизводится: команда FORM-02 выше → **`15 passed, 128 deselected`**. Переход цвета: `78c6622` (RED, код `1`) → `74e1de7` (GREEN); вторая половина задачи 3 того же плана — `bb91db6` (RED) → `7504105` (GREEN). **Наблюдением закрыт шагом 4.3.4:** `document.querySelector('form[action*="/delete"] input[name="search"]').value` → `'UAT9-g020'`, а не пустая строка; и шагом 4.3.6 — приземление на `…?search=UAT9-g020`, то есть **фильтр уцелел на пути БЕЗ Alpine**, где починка WR-06 иначе не срабатывала бы в принципе.
  - **остаточная неопределённость ревизии — WR-04 и WR-05 — закрыта планом 09-19 и тоже переходами цвета:** окно правила панели повтора режется по СОБСТВЕННОЙ форме (`7d3073e` RED → `9cd6f32` GREEN), признак однократной регистрации переехал на ТОТ ЖЕ узел, к которому вешаются слушатели (`70ef917` RED → `efe9cb6` GREEN), запуск интерпретатора сведён в единственный источник `tests/conftest.py::run_node_script` (`0dfdd0a`).
- **ЧТО ЗАКРЫТИЕМ НЕ СТАЛО — четыре пункта, названных поимённо, чтобы круг их не проглотил**
  - **`DEF-09-03` — закрытие WR-05 ТРЕТЬЕГО круга не держит НИ ОДНО правило.** Литерал `limit=30` ушёл из шаблона сентинела экрана групп (план 09-13), но отсутствия его не утверждает ни один тест: ближайший `test_page_shows_thirty_rows_and_a_sentinel` сверяет ОТРЕНДЕРЕННЫЙ адрес и остаётся зелёным и при параметре, и при вернувшемся литерале. На **шести** местах соседних экранов литерал живёт до сих пор (`ads/list.html`, `ads/partial_cards.html`, `accounts/list.html`, `accounts/partial_cards.html`, `schedules/list.html`, `schedules/partial_cards.html`). Назначенная **Фаза 15** (GATE-09/GATE-10). ⚠️ Речь именно о WR-05 ТРЕТЬЕГО круга ревизии, а не о WR-05 четвёртого, который планом 09-19 ЗАКРЫТ — совпадение имён названо здесь, чтобы его не приняли за противоречие.
  - **`DEF-09-04` — вселенная гейта G-2 видит только POST-обработчики.** GET-маршрут порции невидим ему В ОБЕ СТОРОНЫ: и когда он ушёл из множества неконвертированных, и когда GET-вход вернул бы себе собственное перенаправление. Граница НАЗВАНА записью летописи у самой константы и **не расширена**: расширение области обхода — решение ВЕХИ, а не плана. Назначенная **Фаза 15**.
  - **`DEF-09-01` — `test_the_overview_error_number_matches_the_users_own_dashboard` краснеет от ЧАСОВ прогона** (окно 00:00-05:00 UTC), а не от кода: расхождение суточного окна `send_analytics.py DEFAULT_WINDOW=24h` с календарным путём. Предмет экранов `/admin` и `/dashboard`, которых фаза не трогала. Маршрутизирован **вне фазы**; летопись битых окон, запись **14**, — и она этим планом **НЕ трогается**, потому что её предмет ЖИВ.
  - **`DEF-09-02` — «Отмена» панели подтверждения закрывает панель, но летящий запрос НЕ отменяет.** Человек в этот момент считает, что отменил действие, а действие происходит. Назначенная **Фаза 10** (рычаг `components/modal.html` — фаза, которой макрос принадлежит). Четвёртым кругом не закрыт и закрываться не должен был: правка здесь задела бы все шестнадцать вызовов разом.
  - **все четыре МАРШРУТИЗИРОВАНЫ, ни один не поглощён и ни один не забыт.** Ни один из четырёх не является пунктом `gaps` и ни один не опровергает буквы требований: `DEF-09-03` и `DEF-09-04` суть отсутствие ПРАВИЛА там, где свойство сегодня верно, `DEF-09-01` — предсуществующий дефект соседнего экрана, `DEF-09-02` — находка обхода с назначенной фазой.
- **ЧЕГО ЭТОТ КРУГ НЕ СДЕЛАЛ — И ЭТО ЕГО СОБСТВЕННОЕ РЕШЕНИЕ, А НЕ ПРОПУСК**
  - **состояний не перевёл ни одним символом:** пять клеток Фазы 9 читаются `Gaps Found` и до, и после записи; условие перевода — вердикт `passed` повторной верификации.
  - **флажков в теле файла не тронул:** их снимает закрытие фазы, а не этот план.
  - **отметок прежних кругов обхода не переписал:** отметка, снятая с прежнего механизма, **названа обесцененной** в артефакте обхода, а не исправлена задним числом. Число удалённых строк отметок — ноль.
  - **`09-VERIFICATION.md` не правил вовсе:** отчёт перегенерируется прогоном верификации.
  - **ни одного файла приложения и суиты не тронул:** сличение диффа по обоим каталогам пусто — это собственный машинный критерий плана.

#### ЗАПИСЬ ИСХОДА D-06 ↔ QUAL-06 — ОРИГИНАЛ (план 09-09, 2026-08-30)

⚠️ **ЭТО ОРИГИНАЛ ЗАПИСИ, А ЕЁ ПОМЕЧЕННАЯ КОПИЯ ЛЕЖИТ В `09-VERIFICATION.md`.** Артефакт
верификации ПЕРЕГЕНЕРИРУЕТСЯ следующим прогоном проверки, и запись, положенная только туда,
исчезла бы вместе с ним — Фаза 15 унаследовала бы пустоту вместо решения владельца. При
расхождении оригинала и копии правым является настоящий файл — тем же правилом, которым
разрешается WARN-8.

- **Что было:** форма тумблера экрана групп передавала макросу-обёртке собственную цель
  блокировки `find input[type=checkbox]` (решение D-06, объявленное исключением с обоснованием и
  числом). Названная цена отступления — отмена QUAL-06 на том же пути.

- **Что измерено:** ответ проверки **3.3** — **«нет»**, `document.activeElement` вернул `<body>`
  (Chrome / macOS, 2026-08-30). Допущение **A1** («браузер снимает фокус с элемента, которому
  выставлен `disabled`») подтвердилось; конфликт оказался настоящим, а не выведенным из чтения
  кода.

- **Что решил владелец:** вариант **(б)** — отбрасывающая форма синхронизации
  `hx-sync="this:drop"` на форме тумблера ВМЕСТО блокировки. Основание, названное владельцем: это
  единственный вариант, закрывающий и QUAL-01, и QUAL-06 разом. Допущение **A4** прочитано по
  исходнику гейта Фазы 8 ДО выбора: запрещено слово `queue`, отмена (`abort`, `drop`, `replace`)
  названа незадетой прямым текстом, правило применяется только к пяти маршрутам периметра, и
  маршрута тумблера среди них нет.

- **Что применено:** `app/templates/account_groups/includes/group_row.html` — вызов
  макроса-обёртки перестал передавать собственную цель блокировки и передаёт `sync='this:drop'`;
  признак поставлен В ВЫЗОВЕ, а не в макросе, чтобы решение не раздалось сорока семи формам вехи.
  Перечень исключений цели блокировки приведён к новому положению дел следующей строкой летописи.

- **Цена, зафиксированная явно:** QUAL-06 на пути тумблера БОЛЬШЕ НЕ ОТМЕНЯЕТСЯ решением D-06 —
  но и не подтверждён наблюдением: возврат фокуса на изменённой разметке в браузере не
  измерялся ни разу. Отступления от буквы критерия 3 роадмапа на этой поверхности не осталось,
  записи `overrides` этот исход не требует и не заводит.

**Coverage:**

- v2.1 requirements: 41 total
- Mapped to phases: 41 ✓
- Unmapped: 0 — сирот нет, дублей нет: каждое требование отображено ровно в одну фазу

**Распределение по фазам** (нумерация продолжает v2.0, веха открывается Фазой 7):

| Фаза | Требования | Кол-во |
|---|---|---|
| 7. Обновление htmx до 2.0.10 и блок конфигурации | FOUND-01, FOUND-02, FOUND-03, QUAL-05 | 4 |
| 8. Фундамент ответа, гейты, денежный потолок | FOUND-04, FOUND-05, FOUND-06, FOUND-07, QUAL-03, GATE-01, GATE-03, GATE-04, GATE-05, GATE-06, GATE-07, GATE-08, PAY-01, PAY-02 | 14 |
| 9. Пилот `account_groups` | FORM-02, FORM-09, QUAL-01, QUAL-02, QUAL-06 | 5 |
| 10. Рычаг `components/modal.html` | FORM-06 | 1 |
| 11. Массовый перевод разделов письма | FORM-03, FORM-04, FORM-05, FORM-07, FORM-08, FORM-10, GATE-02 | 7 |
| 12. Загрузка изображений без `fetch()` | FETCH-01 | 1 |
| 13. QR-мастер Telegram на фрагментах | FETCH-02 | 1 |
| 14. Авторизация на htmx | SIGN-01, SIGN-02, SIGN-03 | 3 |
| 15. Упрочнение и сводный обход 47 форм | FORM-01, FETCH-03, QUAL-04, GATE-09, GATE-10 | 5 |

Требования вида «на каждой форме» отнесены к фазе, где они становятся структурно ИСТИННЫМИ
(появляется макрос или гейт, делающий свойство свойством построения); требования вида «все 47»
(FORM-01, FETCH-03, QUAL-04, GATE-09, GATE-10) отнесены к Фазе 15 — 47/47 наблюдаемо только
в сводном обходе. Обоснование каждого отнесения — `.planning/ROADMAP.md` §Покрытие требований.

---
*Requirements defined: 2026-08-26*
*Last updated: 2026-08-26 after milestone v2.1 definition — три решения владельца по эскалациям разведки внесены: QUAL-03 (заготовка с сервера + переключение атрибута вместо «ни строки JS»), QUAL-04 (решение по каждой форме вместо `hx-push-url` на всех), PAY-01/PAY-02 (денежный долг втянут в веху, поскольку веха повышает его риск).*
*Traceability filled: 2026-08-26 — роадмап v2.1, 9 фаз (7…15), 41/41 требование отображено.*
*Traceability updated: 2026-08-29 после Фазы 8 — три отложенных к v2.2 идентификатора (EDIT-01, UPLD-01, E2E-01) заведены строками со статусом Deferred; счёт v2.1 не изменился.*
