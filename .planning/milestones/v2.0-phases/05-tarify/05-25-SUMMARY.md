---
phase: 05-tarify
plan: 25
subsystem: billing
tags: [invariants, gate, ast, docstrings, ratchet, verification]
status: complete
requires:
  - "05-23: переписанное объявление `converted_remainder` (место находки WR-01)"
  - "05-24: снятое объявление «план только повышается» и тест-свидетель `test_neither_declaration_claims_the_plan_only_grows_at_the_apply_stage`"
provides:
  - "машинный гейт на класс «объявление утверждает то, чего код рядом с ним не делает»"
  - "реестр долга `declared_invariants_without_witness.txt` с храповиком на 37 записей"
  - "тест-свидетель охвата на четырёх местах находок раунда 6"
affects:
  - "app/application/billing/ — каждое объявление денежного пути обязано называть свидетеля"
  - "app/services/payment_service.py — то же"
tech-stack:
  added: []
  patterns:
    - "предмет проверки ВЫВОДИТСЯ из текста (заглавный прогон), а не из ручной разметки"
    - "множество проверяемых модулей выводится обходом каталога пакета"
    - "негативные контроли над синтетическими исходниками вместо правки настоящих файлов"
    - "реестр долга с потолком-константой (храповик на рост)"
key-files:
  created:
    - tests/test_application/test_declared_invariants.py
    - tests/test_application/declared_invariants_without_witness.txt
  modified:
    - app/application/billing/subscription_period.py
    - app/application/billing/plan_switch.py
    - app/application/billing/plan_usage.py
    - app/services/payment_service.py
decisions:
  - "Предмет гейта — ОБЪЕДИНЕНИЕ заглавного прогона (k=2) и маркера ⚠️; маркер предмет не сужает, поэтому снятие пометки перестало быть исходом"
  - "Два из четырёх фрагментов свидетеля охвата заменены на живые: волны 18-19 переписали абзацы; владелец у всех четырёх сохранён"
  - "Абзацы-предусловия о ценах выведены исходом A, а не B: ратчет запрещает помеченным абзацам попадать в реестр, и это сильнее ожидания плана"
  - "WITHOUT_WITNESS_CEILING = 37 — факт, не запас"
metrics:
  duration: "~2ч 20м"
  completed: 2026-08-18
actuals:
  tokens: 71000
  tasks: 3
  commits: 3
---

# Phase 05 Plan 25: Машинный гейт на объявления денежного пути — Summary

Класс «объявление утверждает то, чего код рядом с ним не делает», за который фаза получила
раунды 4, 5 и 6, перестал быть чтением глазами: гейт находит каждый абзац докстринга и блок
комментария денежного пути, несущий утверждение, и требует названного исполняемого свидетеля
либо записи в реестр долга; предмет выводится из ТЕКСТА, поэтому новый ложный абзац краснит
прогон сам.

## Что сделано

**Задача 1 — гейт** (`test(05-25)`, коммит `4354471`). Новый файл
`tests/test_application/test_declared_invariants.py`: помощник разбора абзацев с указанием
владельца (докстринги по синтаксическому дереву, блоки комментариев по строкам), признак отбора
из трёх исполнимых частей, выведение множества модулей обходом пакета, резолвинг имён тестов по
`FunctionDef`/`AsyncFunctionDef` (не грепом), реестр долга и три теста храповика. Реестр заведён
пустым, потолок — `0`.

**Задача 2 — пакет `app/application/billing/`** (коммит `cdaa3f2`). 50 абзацев рабочего списка.

**Задача 3 — `app/services/payment_service.py`** (коммит `04160ce`). 85 абзацев рабочего списка;
потолок реестра поставлен по факту.

## Замер на исполнении и отклонение от базы плана

**Команда замера из `<action>` задачи 1** (пять чисел: всего абзацев докстрингов, отобрано при
`k=2`, при `k=3`, несут маркер, объединение):

| момент | вывод команды | комментарий |
| --- | --- | --- |
| база плана (до волн 18-19) | `130 89 66 13 89` | снята при записи плана |
| до этого плана (после волн 18-19) | `145 102 81 22 102` | **отклонение ожидаемо** |
| после этого плана | `145 100 80 22 100` | два абзаца вышли из отбора исходом C |

Расхождение с базой плана объяснено: планы `05-22`, `05-23` и `05-24` добавили `15` абзацев
докстрингов и `9` вхождений маркера в те же четыре модуля — `capped_carryover` целиком (план
`05-22`), переписанное объявление `converted_remainder` о длине месяца (план `05-23`) и четыре
абзаца-опровержения снятого инварианта «план только повышается» в `payment_service.py` и
`plan_switch.py` (план `05-24`).

**Собственный вывод помощника по четырём модулям (докстринги + блоки комментариев), два вызова
ОДНОГО помощника с разным порогом:**

| величина | порог `2` | порог `3` |
| --- | --- | --- |
| отобрано объединением | `139` | `114` |
| только заглавный прогон | `139` | `113` |

Всего абзацев помощником — `203`, маркер несут `32`. При пороге `2` маркер не добавляет к
предмету ни одного абзаца (`139 == 139`) — он защёлка на будущее, а не второй источник; при
пороге `3` добавляет ровно один (`114` против `113`), то есть признаки действительно
объединяются, а не совпадают.

**Порог два обоснован прогоном, а не оценкой.** Адресная команда из `<action>` по абзацу `WR-03`
в `switch_is_refused` даёт `[(True, False)]` — при пороге два абзац отобран, при пороге три нет.
Обоснование сохранилось и после правок волны 19 (команда выполнена по НОВОМУ фрагменту, см. ниже).

## Замена фрагментов свидетеля охвата — предъявлена, а не сделана молча

Из четырёх фрагментов, названных планом, **два взяты дословно и два заменены**. Владелец не
менялся ни у одного — ключ остался тройным.

| # | модуль | владелец | фрагмент плана | фрагмент с дерева |
| --- | --- | --- | --- | --- |
| WR-01 | `subscription_period.py` | `converted_remainder` | `ДЛИНА МЕСЯЦА БЕРЁТСЯ` | **заменён** → `ДЛИНА КАЛЕНДАРНОГО МЕСЯЦА В ОТВЕТ НЕ ВХОДИТ` |
| WR-03 | `payment_service.py` | `_extend_subscription` | `ЧТО ИМЕННО ДЕЛАЕТ ПОДТВЕРЖДЁННЫЙ ПЛАТЁЖ` | дословно |
| WR-02 | `payment_service.py` | `_extend_subscription` | `ЧТО ПРОИСХОДИТ, КОГДА ОПЛАЧЕННЫЙ СРОК УЖЕ ИСТЁК` | дословно |
| WR-03 | `plan_switch.py` | `switch_is_refused` | `план только растёт` | **заменён** → `СОХРАНЕНИЕ действующего тарифа` |

**Причина замены названа, а не подразумевается.** План `05-23` переписал объявление
`converted_remainder` о длине месяца — старый фрагмент в этом владельце больше не встречается
(он остался только у `prorated_expiry`, то есть ключ по одному фрагменту дал бы тест, зелёный от
чужого абзаца). План `05-24` СНЯЛ из `plan_switch.py` сентенцию «план только растёт» — прогон
адресной команды по старому фрагменту вернул `[]`.

**Каждый новый фрагмент — ровно одно вхождение в своём модуле, и владелец совпадает с названным
планом.** Прогон помощника:

```
subscription_period.py :: converted_remainder :: 'ДЛИНА КАЛЕНДАРНОГО МЕСЯЦА В ОТВЕТ НЕ ВХОДИТ'
   маркер в абзаце: False | отобран: True | вхождений фрагмента в модуле: 1
payment_service.py :: _extend_subscription :: 'ЧТО ИМЕННО ДЕЛАЕТ ПОДТВЕРЖДЁННЫЙ ПЛАТЁЖ'
   маркер в абзаце: False | отобран: True | вхождений фрагмента в модуле: 1
payment_service.py :: _extend_subscription :: 'ЧТО ПРОИСХОДИТ, КОГДА ОПЛАЧЕННЫЙ СРОК УЖЕ ИСТЁК'
   маркер в абзаце: False | отобран: True | вхождений фрагмента в модуле: 1
plan_switch.py :: switch_is_refused :: 'СОХРАНЕНИЕ действующего тарифа'
   маркер в абзаце: False | отобран: True | вхождений фрагмента в модуле: 1
```

Порог по НОВОМУ фрагменту `WR-03`:
`uv run python -c "...'СОХРАНЕНИЕ действующего тарифа' in p..."` → `[(True, False)]`.

**Ни один из четырёх абзацев маркера не несёт** — это и есть причина, по которой предмет гейта не
сводится к разметке. Положение маркеров на живом дереве:

- `grep -n "⚠️" app/application/billing/subscription_period.py` → `65, 191, 250, 277, 285, 295, 315, 359, 374`, тогда как абзац `WR-01` живёт между `277` и `285`;
- `grep -n "⚠️" app/application/billing/plan_switch.py` → `56, 88`, тогда как абзац `WR-03` живёт между ними;
- `grep -n "⚠️" app/services/payment_service.py` → `134, 201, 227, 253, 297, 687, 710, 724, 734, 754, 828, 840, 849, 959, 980, 1036, 1064, 1107, 1192, 1232, 1305`, тогда как абзацы `WR-03` и `WR-02` стоят между `724` и `734` и между `754` и `828`.

Утверждение «гейт задержал бы находки раунда 6» внесено в исполняемый контракт:
`uv run pytest tests/test_application/test_declared_invariants.py -q -k "covers_the_round_six_findings"` → `1 passed`.

## RED основного гейта и его вывод

После задачи 1 `uv run pytest tests/test_application/test_declared_invariants.py -q` завершился
**ненулевым кодом**: `1 failed, 9 passed`. Сообщение:

```
E       AssertionError: объявления денежного пути без исполняемого свидетеля и без записи в реестр (135):
E           app/application/billing/plan_switch.py :: <module> :: ГРАНИЦЫ МОДУЛЯ (по образцу app/application/billing/subscript
E           app/application/billing/plan_switch.py :: switch_is_refused :: РЕШЕНИЕ ВЛАДЕЛЬЦА (C2/WR-02, чекпойнт плана 05-10, вариант `
...
E           app/services/payment_service.py :: _apply_extension :: ПОЛЕ ПОСТОЯННО ПО ПОСТРОЕНИЮ И ОСТАВЛЕНО НАМЕРЕННО: при мёрт
```

Рабочий список по модулям: `plan_switch.py` — `12`, `plan_usage.py` — `8`,
`subscription_period.py` — `30`, `payment_service.py` — `85`. **Итого `135`.**

После задач 2 и 3 тот же прогон — `10 passed`.

## Три исхода: перечни и равенство числом

**Равенство:** `96` (исход A) + `37` (исход B) + `2` (исход C) = **`135`** = длина рабочего списка.

### Исход A — назван свидетель (96 абзацев)

Полный перечень имён в диффе; ниже — те, о которых план поставил вопрос отдельно.

| абзац | закрепляющий тест | какое свойство закрепляет |
| --- | --- | --- |
| `converted_remainder` / `ДЛИНА КАЛЕНДАРНОГО МЕСЯЦА В ОТВЕТ НЕ ВХОДИТ` (**WR-01**) | `test_the_conversion_does_not_depend_on_the_length_of_the_month` | три базы с месяцами 28/31/30 дают ОДИН ответ |
| `switch_is_refused` / `СОХРАНЕНИЕ действующего тарифа` (**WR-03**) | `test_a_refused_payment_buys_days_of_what_it_paid_for`, `test_a_downgrade_is_refused_with_a_named_reason` | срок двинулся, план остался; цена отказа на стадии намерения |
| `_extend_subscription` / `ЧТО ИМЕННО ДЕЛАЕТ ПОДТВЕРЖДЁННЫЙ ПЛАТЁЖ` (**WR-03**) | `test_a_refused_payment_buys_days_of_what_it_paid_for`, `test_a_confirmed_higher_plan_is_applied_at_the_apply_stage` | отвергнутый переход всё равно покупает дни; разрешённый применяется |
| `_extend_subscription` / `ЧТО ПРОИСХОДИТ, КОГДА ОПЛАЧЕННЫЙ СРОК УЖЕ ИСТЁК` (**WR-02**) | `test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage`, `test_the_expired_period_writes_no_preserved_plan_warning`, `test_an_expired_period_lifts_the_recorded_refusal_and_leaves_no_trace` | план применяется; следа нет; истёкший срок снимает и ЗАПИСАННЫЙ отказ |
| `axis_percent` / `ДЕЛЕНИЕ ИДЁТ ТОЛЬКО ПРИ ЦЕЛОМ ЛИМИТЕ` | `test_plan_axes_zero_limit_does_not_divide_by_zero`, `test_plan_axes_unlimited_axis_reports_none_and_zero_percent` | нулевой лимит и безлимит дают ноль, а не деление на ноль |
| `axis_percent` / `ПОТОЛОК В СТО ПРОЦЕНТОВ` | `test_plan_axes_over_the_limit_is_not_an_error` | расход выше лимита даёт ровно `100`, а не исключение |
| `plan_axes` / `ВЛАДЕНИЕ — ПРЕДИКАТ ЗАПРОСА` | `test_plan_axes_groups_axis_counts_only_the_owners_groups`, `test_plan_axes_isolates_every_axis_from_another_user` | чужие группы и отправки в оси не попадают |
| `subscription_is_live` / порядок снятия признака | `test_the_liveness_is_sampled_before_the_date_moves` | признак снимается ДО сдвига срока (AST-ловушка) |
| `switch_is_refused` / `⚠️ У ПРАВИЛА ДВЕ СТАДИИ` | `test_both_stages_read_the_same_declaration_of_the_rule` | обе стадии читают одно объявление |
| `switch_is_refused` / `⚠️ ПРЕЖНЯЯ РЕДАКЦИЯ ДОБАВЛЯЛА` | `test_neither_declaration_claims_the_plan_only_grows_at_the_apply_stage`, `test_a_demotion_by_the_recorded_answer_leaves_its_own_downgrade_trace` | ни одно объявление не утверждает роста плана; понижение оставляет свой след |
| `prorated_expiry` / `⚠️ ПРЕДУСЛОВИЕ: price СТРОГО БОЛЬШЕ НУЛЯ` | `test_a_price_that_cannot_be_read_falls_back_to_the_whole_month` | вызывающий предусловие ПРОВЕРЯЕТ: нечитаемая цена уводит на откат |
| `converted_remainder` / `⚠️ ПРЕДУСЛОВИЕ: ОБЕ ЦЕНЫ СТРОГО БОЛЬШЕ НУЛЯ` | `test_an_unreadable_paid_plan_price_does_not_carry_the_whole_horizon` | нечитаемая цена уводит на `capped_carryover` |

Каждое названное имя проверено прогоном: `uv run pytest <файл> -q -k "<имя>"` собирает ровно один
тест. Совокупный прогон закрепляющих файлов —
`uv run pytest tests/test_application/test_declared_invariants.py tests/test_services/ tests/test_pages/test_billing_payment_errors.py tests/test_migrations/ -q` → `239 passed`.

### Исход C — абзац переписан и вышел из отбора (2 абзаца)

| абзац | что сделано |
| --- | --- |
| `plan_usage.py` / `<module>` / `ЧЕГО МОДУЛЬ НЕ ДЕЛАЕТ:` | заголовок-связка снят с заглавных: `Чего модуль не делает:` — утверждения абзац не нёс, он вводил перечень |
| `plan_switch.py` / `switch_is_refused` / `Различие … — ЦЕНА ОТКАЗА:` | то же: `— цена отказа:`; сами два пункта перечня остались в отборе и получили свидетеля |

### Исход B — записан долг (37 записей = потолок)

`WITHOUT_WITNESS_CEILING = 37`. **Это ЗАПИСАННЫЙ ДОЛГ, а не закрытие: столько объявлений
денежного пути сегодня не имеют исполняемого свидетеля.** Значение равно факту, не запасу:
`grep -v '^#' … | grep -c '::'` → `37`, `grep -oE 'WITHOUT_WITNESS_CEILING *= *[0-9]+' …` → `37`.

Разбивка по модулям: `payment_service.py` — `28`, `subscription_period.py` — `8`,
`plan_switch.py` — `1`, `plan_usage.py` — **`0`**.

**Индивидуальным решением в реестр не ушёл ни один абзац.** Все `37` записаны механическим
прогоном помощника печати реестра — с причиной, назначенной по владельцу и первым словам, и без
единой безымянной строки (`grep -v '^#' | wc -l` == `grep -c '::'` == `37`).

## Подсписок «ЗАПИСАНО ДОЛГОМ, ХОТЯ СВОЙСТВО МАШИННО ПРОВЕРЯЕМО» — ПЕРВАЯ ОЧЕРЕДЬ НА СНЯТИЕ

Таких абзацев **14**. У каждого свидетель МОЖЕТ быть написан сегодня, и не написан только потому,
что объём работы принадлежит не этому плану.

| абзац | что проверял бы тест |
| --- | --- |
| `subscription_period.py` / `<module>` / `ГРАНИЦЫ МОДУЛЯ` | греп по сырому тексту модуля: ни записи в БД, ни `Request`, ни Jinja — ровно то, что уже есть у `plan_switch.py` и `plan_usage.py` |
| `subscription_period.py` / `converted_remainder` / `ЭТО ТОТ ЖЕ АППАРАТ D-29` | разбор дерева: деление денег на цену встречается ровно в одном объявлении (`prorated_days`) |
| `subscription_period.py` / `capped_carryover` / `ЭТО АРИФМЕТИКА ВРЕМЕНИ` | разбор дерева функции: арифметики денег в теле нет |
| `subscription_period.py` / `capped_carryover` / `ЗАЖИМ ПО now` | разбор дерева: второй копии правила отсчёта D-04 в функции нет |
| `payment_service.py` / `_claim_payment` / `ЭТО COMPARE-AND-SWAP` | разбор дерева: условие и запись стоят в ОДНОМ `UPDATE`, а не проверкой с последующей записью |
| `payment_service.py` / `_mirror_claim` / `Значения ставятся set_committed_value` | разбор тела: присваивания атрибутов нет, используется `set_committed_value` |
| `payment_service.py` / `handle_webhook` / `ПОРЯДОК ПРОВЕРОК ЗНАЧИМ` | AST-ловушка того же вида, что у признака живости: событие → наличие строки → терминальный статус |
| `payment_service.py` / `handle_webhook` / `Проверка терминального статуса стоит ДО ветвления` | AST-ловушка порядка операторов |
| `payment_service.py` / `handle_webhook` / `ПРЕДМЕТ ПОКУПКИ РЕШАЕТ КОЛОНКА kind` | разбор дерева: `metadata` на этом пути не читается ни разу |
| `payment_service.py` / `_extend_subscription` / `ЗАПРОС АКТИВНОЙ ПОДПИСКИ ПОВТОРЯЕТ get_shell_context` | сверка двух деревьев запросов на дословное совпадение |
| `payment_service.py` / `_apply_extension` / `ЖУРНАЛ ВЕТКИ ОТКАЗА НАЗЫВАЕТ ТРИ ВЕЩИ` | разбор вызова `logger.warning`: три поля присутствуют РАЗОМ |
| `payment_service.py` / `<module>` / `ЗНАКОМЫЕ СОБЫТИЯ — КОНСТАНТАМИ SDK` | разбор дерева: строковых литералов событий в модуле нет |
| `payment_service.py` / `_apply_extension` / `База берётся ТЕМ ЖЕ объявлением` | разбор дерева: база сдвига и база, ушедшая в журнал, — одно выражение |
| `payment_service.py` / `_apply_extension` / `ПРАВИЛО ОБЪЯВЛЕНО ОДИН РАЗ И ЧИТАЕТСЯ ЗДЕСЬ` | разбор дерева: второй формулы отсчёта на денежном пути нет |

## Абзацы, ушедшие исходом B по причине «работа своего размера» (боевое окружение)

Их **3**, и каждый уже числится человеческой проверкой фазы:

| абзац | где свойство числится |
| --- | --- |
| `payment_service.py` / `_claim_payment` / `РАБОТАЕТ ОДИНАКОВО НА PostgreSQL И НА SQLite` | суита идёт на SQLite; боевой диалект PostgreSQL — человеческая проверка «первый настоящий выкат» |
| `payment_service.py` / `_extend_subscription` / `ВТОРАЯ АКТИВНАЯ СТРОКА С РЕВИЗИИ 0018` | частичный уникальный индекс живёт в невыкаченной ревизии `0018` — записанный долг фазы |
| `payment_service.py` / `<module>` / `Подписка на событие payment.canceled` | состояние кабинета ЮKassa (D-27) — записанный долг фазы, человеческая проверка |

Остальные `20` записей — доводы о выборе, записи решений и истории, заголовки перечней и
объяснения констант: закреплять тестом там нечего, и это названо в самой строке реестра.

## `plan_usage.py` разобран, а не ссыпан в реестр

Замер повторён на исполнении и приведён числом рядом с числом плана:

| величина | план | на исполнении |
| --- | --- | --- |
| `grep -c "⚠️"` | `0` | `0` |
| абзацев докстрингов | `15` | `15` |
| отобрано при пороге `2` | `8` | `7` (после исхода C у заголовка `ЧЕГО МОДУЛЬ НЕ ДЕЛАЕТ:`) |

**Все `7` вышли исходом A; в реестре модуля ноль записей.** Три машинно проверяемых утверждения —
условие деления, потолок процента, владение предикатом запроса — названы поимённо:
`grep -c 'ПОТОЛОК В СТО ПРОЦЕНТОВ' …txt` → `0`,
`grep -c 'ВЛАДЕНИЕ — ПРЕДИКАТ ЗАПРОСА' …txt` → `0`.
Все три имени резолвятся: `uv run pytest tests/test_application/test_plan_usage.py -q -k "<имя>"`
собирает ровно один тест по каждому.

Это ровно тот случай, ради которого план вывел модуль из механического прогона: правило «только
помеченные разбираются индивидуально» отправило бы в реестр все восемь его абзацев, и раунд 7
прочитал бы это пропуском.

## Зубы храповика показаны на живом дереве

Временный модуль `app/application/billing/temporary_ratchet_probe.py` с одним абзацем формы
`WR-01` — заглавный заголовок, БЕЗ маркера, теста не называет. Прогон
`uv run pytest tests/test_application/test_declared_invariants.py -q`:

```
КОД ВОЗВРАТА: 1
E       AssertionError: объявления денежного пути без исполняемого свидетеля и без записи в реестр (1):
E           app/application/billing/temporary_ratchet_probe.py :: <module> :: ДЛИНА МЕСЯЦА БЕРЁТСЯ У `add_one_month` ОТ ТОЙ ЖЕ БАЗЫ, А НЕ
1 failed, 9 passed
```

Модуль удалён; после удаления — `10 passed`.
`git status --porcelain --untracked-files=all app/application/billing/ | grep -c '^??'` → `0`.

**Это и есть мерка плана `05-19`, достигнутая:** НОВЫЙ модуль попал под гейт САМ, без единой
правки перечня, и НОВЫЙ ложный абзац покраснил прогон САМ, без чьей-либо пометки.

## Граница гейта — процитирована из его докстринга

> Он НЕ ЧИТАЕТ ТЕЛА ФУНКЦИИ и не сравнивает смысл абзаца с поведением. Абзац, называющий
> существующий, но НЕ ТОТ тест, гейт пропустит зелёным. Соответствие свидетеля предмету судится
> человеком — это объявленная граница средства, а не его дефект: претензия на большее была бы
> ровно тем классом, против которого он написан.
>
> Он НЕ СЧИТАЕТ записи реестра долга верными. Реестр он только ПЕРЕСЧИТЫВАЕТ.
>
> ХРАПОВИК ЛОВИТ ДОБАВЛЕНИЕ, А НЕ ПЕРЕФОРМУЛИРОВАНИЕ. Отпечаток записи берётся от текста абзаца,
> поэтому переписывание уже записанного абзаца выбивает запись из реестра и требует либо
> свидетеля, либо новой записи ВЗАМЕН старой. Число записей при такой замене не растёт, и
> храповик замену пропустит.

## Прогоны

| прогон | результат |
| --- | --- |
| `pytest tests/test_application/test_declared_invariants.py -q -k "reports or covers or accepts or ledger"` | `9 passed, 1 deselected` |
| `pytest tests/test_application/test_declared_invariants.py -q` (после задачи 1) | `1 failed, 9 passed` — RED, `135` абзацев |
| `pytest tests/test_application/test_declared_invariants.py -q` (после задачи 3) | `10 passed` |
| `pytest tests/test_application/ -q` | `222 passed` |
| `pytest …declared_invariants.py tests/test_services/ …billing_payment_errors.py tests/test_migrations/ -q` | `239 passed` |
| `pytest tests/ -q` | **`1767 passed`** |

Поведение не тронуто:
`git diff -U0 -- app/application/billing/ \| grep -cE "^[+-] *(return\|if \|for \|while \|[a-z_]+ =)"` → `0`;
`git diff -U0 -- app/services/payment_service.py \| grep -cE "^[+-] *(return\|if \|for \|while \|await \|[a-z_]+ =)"` → `0`;
ни один существующий тест не переписан (`git diff -U0 -- tests/ | grep -c '^-def test_\|^-async def test_'` → `0`);
ревизий Alembic не заведено, зависимостей не добавлено.

## Deviations from Plan

### 1. [Rule 1 — Bug] Отпечаток абзаца терял хвостовой пробел

- **Found during:** задача 2, прогон `test_the_ledger_holds_no_dead_entry`
- **Issue:** отпечаток — первые `60` символов абзаца — примерно в половине случаев обрывается
  в середине слова и оканчивается пробелом; разбор строки реестра снимает пробелы у каждой части,
  поэтому запись **не сходилась сама с собой** и объявлялась мёртвой. Это дефект самого гейта:
  средство против «объявление против кода» не сходилось с собственным объявлением.
- **Fix:** `paragraph_fingerprint` снимает хвостовой пробел (`.rstrip()`); причина названа в
  докстринге функции.
- **Files modified:** `tests/test_application/test_declared_invariants.py`
- **Commit:** `cdaa3f2`

### 2. [Rule 1 — Bug] Перенос строки рвал имя теста в приписке

- **Found during:** задача 3
- **Issue:** первая редакция вспомогательного скрипта переносила длинные имена тестов по слогам
  (`test_an_unreadable_paid_plan_price_does_not_carry_the` / `_whole_horizon`), и гейт справедливо
  не находил имени. Правка применялась к дереву и была откачена целиком (`git checkout --`)
  до повторного прогона — в коммит попал только исправленный результат.
- **Fix:** перенос без разрыва длинных слов; правка перепроверена прогоном гейта.
- **Files modified:** `app/services/payment_service.py`
- **Commit:** `04160ce`

### 3. [Rule 4 → решено по исполняемому критерию] Два абзаца-предусловия о ценах выведены исходом A, а не B

- **Found during:** задача 2
- **Issue:** `<action>` задачи 2 прямо назначал абзацам-предусловиям о положительных ценах
  **исход B** («требование к ВЫЗЫВАЮЩЕМУ»). Но оба абзаца несут маркер `⚠️`, а критерий приёмки
  той же задачи требует, чтобы тест храповика `..._holds_no_marked_paragraph` был зелёным — то
  есть чтобы помеченный абзац в реестр НЕ ПОПАЛ. Два места плана противоречат друг другу.
- **Решение:** приоритет отдан ИСПОЛНЯЕМОМУ критерию (Rule 1). Оба вышли исходом A, и свидетель
  нашёлся честный — не «первый попавшийся зелёный тест», а тот, что показывает: **вызывающий
  предусловие действительно проверяет**
  (`test_a_price_that_cannot_be_read_falls_back_to_the_whole_month` и
  `test_an_unreadable_paid_plan_price_does_not_carry_the_whole_horizon`).
- **Files modified:** `app/application/billing/subscription_period.py`
- **Commit:** `cdaa3f2`

### 4. [Отклонение от ожидаемого распределения плана — в сторону строгости] 96 абзацев исходом A вместо ожидаемых «порядка двадцати одного»

- **Issue:** план ожидал индивидуального решения только у помеченных абзацев и у четырёх мест
  находок (≈21), а остальные `114` отправлял в реестр механическим прогоном.
- **Что сделано вместо:** индивидуальное решение получили ВСЕ абзацы, у которых свидетель уже
  существует по имени, — это ровно тот принцип, которым план вывел `plan_usage.py` из
  механического прогона («свидетели у них уже написаны и называются по имени»). Реестр в итоге
  `37` записей вместо ожидавшихся `114`.
- **Почему это не расширение объёма:** поведение не тронуто ни строкой, ни один существующий тест
  не переписан, новых тестов не написано. Изменились только докстринги и потолок реестра — то
  есть работа осталась в границах задач 2 и 3, а записанный долг стал втрое меньше.

### 5. [Rule 3 — Blocking] `<verify>` задачи 2 не мог быть зелёным по построению плана

- **Issue:** `<verify>` задачи 2 — `uv run pytest tests/test_application/ -q` с кодом `0`, но
  основной гейт по построению плана остаётся КРАСНЫМ до задачи 3 (`payment_service.py` ещё не
  разобран), а сам гейт лежит в `tests/test_application/`. Два требования плана несовместимы.
- **Решение:** на задаче 2 прогон выполнен с точечным исключением ещё-красного основного гейта
  (`--deselect …::test_every_declared_invariant_names_an_executable_witness`) → `222 passed`,
  плюс проверено критерием задачи 2 по существу: гейт **не сообщает ни одного абзаца из
  `app/application/billing/`**. После задачи 3 тот же прогон без исключений — зелёный.

## Known Stubs

Нет. Заглушек, пустых значений и «coming soon» план не вводит: правятся объявления, добавляется
тест и текстовый реестр.

## Записанный долг, названный числом

`37` объявлений денежного пути сегодня не имеют исполняемого свидетеля. Из них `14` машинно
проверяемы и объявлены **первой очередью на снятие**, `3` перенесены в человеческую проверку,
`20` — доводы, записи решений и заголовки перечней, которым свидетеля не полагается вовсе.
Потолок может только снижаться: рост краснит прогон.

## Self-Check: PASSED

- `tests/test_application/test_declared_invariants.py` — FOUND
- `tests/test_application/declared_invariants_without_witness.txt` — FOUND
- `app/application/billing/temporary_ratchet_probe.py` — отсутствует (удалён намеренно), в git не попадал
- коммит `4354471` — FOUND
- коммит `cdaa3f2` — FOUND
- коммит `04160ce` — FOUND
