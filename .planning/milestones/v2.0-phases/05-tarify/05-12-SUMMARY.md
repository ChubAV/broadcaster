---
phase: 05-tarify
plan: 12
subsystem: infra
tags: [state-tracking, planning-artifacts, code-review-accounting, worktree, gsd-tooling]

# Dependency graph
requires:
  - phase: 05-tarify (план 05-11)
    provides: "Исполненное закрытие блокера `_apply_extension`: `switch_is_refused`, ключ журнала `subscription_plan_preserved`, регрессия стадии применения — имена, которыми запись §Deferred Items называет закрытие"
  - phase: 05-tarify (планы 05-07, 05-08)
    provides: "Закрытия `CR-01` и `CR-02` ревью раунда 1 — безопасное умолчание `X-Real-IP`, `_claim_payment` с `with_for_update()`, частичный уникальный индекс ревизии `0018`"
  - phase: 05-tarify (план 05-10)
    provides: "Таблица распоряжений по находкам код-ревью и чекпойнт семантики `upgrade-only` — вход сводной записи и записи §Decisions"
provides:
  - "`.planning/phases/05-tarify/05-STATE-PATCH.md` — готовая к применению замена пяти секций `.planning/STATE.md`, коммитящаяся в каталог фазы и переживающая снос worktree"
  - "`.planning/STATE.md` без устаревших 🔴: обе записи ревью раунда 1 сняты и переехали в §Deferred Items закрытыми строками со ссылкой на закрывшие планы и доказавшие регрессии"
  - "Единственная сводная запись отложенных находок код-ревью обоих раундов — каждая находка с РАУНДОМ и распоряжением"
  - "Запись §Decisions: семантика смены тарифа `upgrade-only` как решение фазы, а не как докстринг"
  - "Согласованные «Current Position» и frontmatter `progress`: `Plan: 12 of 12`, `63/64`"
affects: [05-verification-round-3, gsd-tooling-state-begin-phase, phase-06]

# Actuals (#2632)
actuals:
  tokens: 4000
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Артефакт, который обязан пережить снос worktree, пишется В КАТАЛОГ ФАЗЫ: файлы каталога фазы коммитятся планом в любом режиме исполнения, `.planning/STATE.md` — нет"
    - "Правка файла вне зоны коммита исполнителя делается по фактическому корню из `git rev-parse --git-common-dir`, а не по cwd: из worktree это главный рабочий каталог, чей дифф коммитит оркестратор"
    - "Номер находки код-ревью без указания РАУНДА — ложное указание, если файл ревью перезаписывался; проверяется счётом вхождений (`grep -Eo … | wc -l`), а не построчным `grep -c`"

key-files:
  created:
    - .planning/phases/05-tarify/05-STATE-PATCH.md
  modified:
    - .planning/STATE.md

key-decisions:
  - "Пара `WR-10` (раунд 1) / `WR-07` (раунд 2) получила ОДНО распоряжение одной физической строкой: причина одна (негодный `PLAN_LIMITS`), мест отказа два (`axis_percent` по ТИПУ, `json.loads` по ФОРМАТУ), ответ один. Два распоряжения на одну причину были бы тем же введением читателя в заблуждение, ради устранения которого план заведён"
  - "Устаревшие 🔴 не переписаны на месте, а ПЕРЕНЕСЕНЫ в §Deferred Items закрытыми строками: в таблице уже лежал точный образец (три записи Фазы 2), и §Blockers/Concerns перестаёт расти"
  - "Счёт 🔴 по файлу НЕ обнулён (остался 2): открытая запись о блокирующем вызове SDK обязана уцелеть, иначе закрытие дефекта подменяется удалением записи (T-05-60)"
  - "Отложенные находки собраны ОДНОЙ записью, а не восемнадцатью строками: бюджет файла 150 строк, и строка на находку обменяла бы один дефект на другой"
  - "`requirements-completed` оставлен ПУСТЫМ вопреки умолчанию шаблона — см. раздел «Требования не перемечены»"

patterns-established:
  - "Блокирующий человеческий чекпойнт ставится там, где у исполнителя нет пути с гарантированно переживающим worktree эффектом — и это доказано прогонами, а не предположением"
  - "Критерий «одно распоряжение на одну причину» проверяется парой встречных грепов: разнесение пары на две строки роняет один критерий в 0, дописанное второе упоминание роняет другой в 2"

# Требования этим планом НЕ перемечаются — см. раздел «Требования не перемечены».
# BILL-05/BILL-06/BILL-07 возвращает в Complete повторная верификация, а не этот план (прохибиция BILL-07).
requirements-completed: []

coverage:
  - id: D1
    description: "Ни одна 🔴 запись `.planning/STATE.md` больше не описывает закрытый дефект денежного пути как открытый: обе записи ревью раунда 1 сняты"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "grep -c '🔴 \\[Phase 5, CR-01\\]' .planning/STATE.md → 0; grep -c '🔴 \\[Phase 5, CR-02\\]' .planning/STATE.md → 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Закрытия названы планами, которые их исполнили — 05-07, 05-08, 05-11"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "grep -Ec '05-07|05-08|05-11' .planning/STATE.md → 6 (порог >= 3)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Блокер `_apply_extension` записан в операционный документ ИМЕНАМИ — функцией правила, ключом журнала, именем регрессии — а не словом «исправлено»"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "grep -c 'subscription_plan_preserved' .planning/STATE.md → 1"
        status: pass
    human_judgment: false
  - id: D4
    description: "Пара `WR-10` (раунд 1) / `WR-07` (раунд 2) несёт ОДНО распоряжение на двоих — оба номера в одной физической строке, третьего вхождения нет"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "grep -Eo 'WR-07|WR-10' .planning/STATE.md | wc -l → 2 (ровно 2); grep -Ec 'WR-10.*WR-07|WR-07.*WR-10' 05-STATE-PATCH.md → 1"
        status: pass
    human_judgment: false
  - id: D5
    description: "След открытого дефекта не стёрт заодно: запись о блокирующем вызове SDK (ревью раунда 2, CR-02) уцелела"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "grep -c '🔴' .planning/STATE.md → 2 (порог >= 1)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Бюджет файла соблюдён — операционный документ не разросся ценой записи находок"
    verification:
      - kind: other
        ref: "wc -l .planning/STATE.md → 125 (потолок 145)"
        status: pass
    human_judgment: false
  - id: D7
    description: "Артефакт написан В ФАЙЛ и закоммичен, а не обещан во frontmatter: `05-STATE-PATCH.md` переживает снос worktree"
    requirement: BILL-07
    verification:
      - kind: other
        ref: "git show c177c44 --numstat → 164 строки, .planning/phases/05-tarify/05-STATE-PATCH.md"
        status: pass
    human_judgment: false
  - id: D8
    description: "Требования не перемечены раньше вердикта верификации"
    requirement: BILL-07
    verification:
      - kind: other
        ref: "git diff --name-only -- .planning/REQUIREMENTS.md → пусто"
        status: pass
    human_judgment: false
  - id: D9
    description: "Читатель STATE.md, пришедший в проект впервые, понимает из §Blockers/Concerns действительное состояние денежного пути за одну минуту, а не собирает его из четырёх документов фазы"
    verification: []
    human_judgment: true
    rationale: "Backstop-пункт плана: понятность записи судится человеком, а не грепом. Греп доказывает, что записи ЕСТЬ и указывают на верные находки; он не доказывает, что сводная запись длиной в абзац читается за минуту"

# Metrics
duration: ~10min (возобновление после чекпойнта; задача 1 исполнена предыдущим агентом)
completed: 2026-08-16
status: complete
---

# Phase 05 Plan 12: Бухгалтерия фазы записана в STATE.md, а не обещана во frontmatter — Summary

**Пять секций `.planning/STATE.md` заменены по готовому патчу `05-STATE-PATCH.md`: обе устаревшие 🔴 записи ревью раунда 1 сняты и переехали в §Deferred Items закрытыми строками со ссылками на планы 05-07/05-08/05-11 и на доказавшие закрытие регрессии, блокирующий вызов SDK и сводное распоряжение по восемнадцати находкам обоих раундов записаны, семантика `upgrade-only` стала решением фазы, а «Current Position» перестала утверждать `Plan: 1 of 10`.**

## Исход применения к настоящему дереву — ПРИМЕНЕНО

**Шаг 7 задачи 1 (применение замены к `.planning/STATE.md` настоящего рабочего дерева) выполнен успешно: правки применены и закоммичены.** Доказательство — коммит `c177c44 docs(05-12): record phase accounting in STATE.md`, изменяющий `.planning/STATE.md` (16 строк добавлено, 12 удалено) вместе с созданием `05-STATE-PATCH.md` (164 строки). Отказа доступа не было; изображать успех не пришлось.

Дополнительно проверено этим агентом: копия `.planning/STATE.md` в worktree и файл `/source/broadcaster/.planning/STATE.md` главного рабочего каталога **побайтово идентичны** (`cmp` → код 0). Расхождение между деревьями было бы находкой само по себе — его нет.

## Сигнал возобновления и фактические значения шести проверок

**Сигнал возобновления, записанный дословно: «применено».**

Все шесть проверок `<verification>` задачи 2 перезапущены этим агентом по `.planning/STATE.md` **ГЛАВНОГО** рабочего каталога (`/source/broadcaster/.planning/STATE.md`, корень получен из `git rev-parse --git-common-dir` → `/source/broadcaster/.git`), а не по копии worktree:

| # | Проверка | Порог | Фактическое значение | Итог |
|---|---|---|---|---|
| 1 | `grep -c '🔴 \[Phase 5, CR-01\]' .planning/STATE.md` | 0 | **0** | ✓ |
| 2 | `grep -c '🔴 \[Phase 5, CR-02\]' .planning/STATE.md` | 0 | **0** | ✓ |
| 3 | `grep -Ec '05-07\|05-08\|05-11' .planning/STATE.md` | >= 3 | **6** | ✓ |
| 4 | `grep -Eo 'WR-07\|WR-10' .planning/STATE.md \| wc -l` | ровно 2 | **2** | ✓ |
| 5 | `wc -l .planning/STATE.md` | <= 145 | **125** | ✓ |
| 6 | `grep -c '🔴' .planning/STATE.md` | >= 1 | **2** | ✓ |
| + | `git diff --name-only -- .planning/REQUIREMENTS.md` | пусто | **пусто** | ✓ |

Значения сошлись с измеренными оркестратором до чекпойнта — дрейфа между предъявлением гейта и его снятием нет.

Прочие критерии приёмки задачи 2:
- `git diff --name-only -- .planning/STATE.md` → **пусто**: незакоммиченных остатков правки не осталось.
- `git log --oneline -1 -- .planning/STATE.md` → **`c177c44 docs(05-12): record phase accounting in STATE.md`** — коммит ЭТОЙ волны, а не `docs(phase-05): create phase plan`.

Дополнительно (критерий `<verification>` плана, за пределами шести):
- `grep -Ec 'раунд(а)? 1|раунд(а)? 2' .planning/STATE.md` → **7** (порог >= 2) — номера находок разведены по раундам.
- `git diff --name-only -- app/ tests/ alembic/ pyproject.toml` → **пусто** — план организационный, кода не касается.

## Критерии приёмки задачи 1, перепроверенные на `05-STATE-PATCH.md`

Артефакт закоммичен и потому неизменен; значения перепроверены, а не переписаны из отчёта:

| Критерий | Порог | Значение | Итог |
|---|---|---|---|
| `grep -Ec 'WR-10.*WR-07\|WR-07.*WR-10'` | == 1 | **1** | ✓ оба номера в одной физической строке |
| `grep -c 'WR-07'` | == 1 | **1** | ✓ второго распоряжения нет |
| `grep -c 'WR-10'` | == 1 | **1** | ✓ второго распоряжения нет |
| `grep -Ec 'WR-10 \(раунд 1\)\|WR-10 — раунд 1'` | == 1 | **1** | ✓ раунд назван |
| `grep -Ec 'WR-07 \(раунд 2\)\|WR-07 — раунд 2'` | == 1 | **1** | ✓ раунд назван |
| `grep -c '05-07'` / `grep -c '05-08'` / `grep -c '05-11'` | >= 1 каждый | **2 / 5 / 4** | ✓ |
| `grep -Ec 'раунд(а)? 1'` / `grep -Ec 'раунд(а)? 2'` | >= 2 каждый | **5 / 6** | ✓ |
| `grep -c 'subscription_plan_preserved'` | >= 1 | **1** | ✓ закрытие названо именем ключа |
| `grep -Eo 'WR-0[1-9]\|WR-10\|IN-0[1-7]\|CR-0[12]' \| wc -l` | >= 15 | **36** | ✓ счёт по ВХОЖДЕНИЯМ |
| Пять правок названы поимённо | 5 разделов | **13 вхождений имён секций** | ✓ |
| `git diff --name-only -- app/ tests/ alembic/` | пусто | **пусто** | ✓ |

Пара встречных критериев по `WR-10`/`WR-07` сработала так, как задумана: 1 / 1 / 1 означает ровно одно распоряжение на одну причину — разнесение на две строки дало бы 0 в первом, дописанное второе упоминание дало бы 2 во втором или третьем.

## Performance

- **Duration:** ~10 мин (возобновление после чекпойнта); задача 1 исполнена предыдущим агентом в этой же волне
- **Tasks:** 2 (1 `auto` + 1 `checkpoint:human-action`, снятый человеком)
- **Files modified:** 2 (1 создан — `05-STATE-PATCH.md`; 1 изменён — `.planning/STATE.md`)
- **Кода не тронуто:** 0 файлов в `app/`, `tests/`, `alembic/`, `pyproject.toml`

## Accomplishments

- **Гэп 2 отчёта `05-VERIFICATION.md` закрыт по существу, а не по форме.** Файл, объявленный в `files_modified` планов `05-08` и `05-10` и не изменённый ни одним, изменён и закоммичен: `git log -1 -- .planning/STATE.md` называет коммит этой волны.
- **Две ложные 🔴 записи перестали вводить читателя в заблуждение.** Строка о пустом `YOOKASSA_WEBHOOK_CLIENT_IP_HEADER` (закрыто планом 05-07: `X-Real-IP` — свойство `app/config.py`, запись в `docker-compose.prod.yml`, 10 регрессий через настоящий `ProxyHeadersMiddleware`) и строка о `select(Payment)` без блокировки (закрыто планом 05-08: `_claim_payment` с `rowcount == 1` и `with_for_update()`, частичный уникальный индекс ревизии `0018`, 22 теста) переехали в §Deferred Items закрытыми строками.
- **Блокер денежного пути наконец живёт в операционном документе.** Молчаливое понижение тарифа в `_apply_extension` до этого существовало только в `05-REVIEW.md` и в отчёте верификации; теперь §Deferred Items называет его закрытие именами: `switch_is_refused` (`app/application/billing/plan_switch.py`), ключ журнала `subscription_plan_preserved`, регрессия `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage` — с обязательной оговоркой, что вердикт `gaps_found` действует до повторной верификации.
- **Ловушка нумерации обезврежена.** `05-REVIEW.md` перезаписывался (`4989f7a` — раунд 1, `1d29360` — раунд 2), номера между раундами переназначены. Каждая запись патча называет РАУНД и СУТЬ; раунд 1 прочитан из git, поскольку на диске его нет.
- **Одна причина — одно распоряжение.** `WR-10` (раунд 1, негодный ТИП лимита роняет `axis_percent`) и `WR-07` (раунд 2, негодный ФОРМАТ документа роняет `json.loads` и отдаёт 500 на раздел) записаны одной строкой с общим ответом: типизированная запись плана, проверяемая при построении `Settings`.
- **Число невыкаченных ревизий исправлено с пяти на шесть.** Ревизия `0018` отгружена планом 05-08 в тот же день; «пять» стало неверным тем же способом, что и обе снятые 🔴 — та же секция, тот же день, та же цена.
- **Счёт 🔴 не обнулён (остался 2).** Открытая запись о блокирующем вызове SDK уцелела: закрытие дефекта не подменено удалением записи (T-05-60).

## Task Commits

1. **Task 1 (auto): готовая замена секций STATE.md — записанная в коммитящийся артефакт и применённая к настоящему дереву** — `c177c44` (docs), 2 файла, +180/−12
2. **Task 2 (checkpoint:human-action, gate=blocking): применить замену и подтвердить проверками** — собственного коммита не имеет: правка уже была применена и закоммичена задачей 1, гейт снят человеком сигналом «применено» после схождения шести проверок

**Метаданные плана:** коммит этого SUMMARY (`docs(05-12): complete phase accounting plan`).

## Files Created/Modified

- `.planning/phases/05-tarify/05-STATE-PATCH.md` **(создан, 164 строки)** — готовая к применению замена пяти секций: правка строки очереди ревизий, снятие двух 🔴, две новые записи §Blockers/Concerns (🔴 блокирующий вызов SDK + 🟡 сводное распоряжение), три строки §Deferred Items, запись §Decisions, «Current Position», frontmatter `progress`. Лежит в каталоге фазы намеренно: файлы каталога фазы коммитятся планом в любом режиме и переживают снос worktree.
- `.planning/STATE.md` — 125 строк (вход 121, потолок рабочего процесса 150, цель плана 145). Применены все пять правок; frontmatter и текст прогресса перестали спорить (`total_plans: 64 / completed_plans: 63` против прежних `62/58` и `52/52 (100%)`).

## Decisions Made

- **Маршрут записи изменён, а не объявление повторено третий раз.** `workflow.use_worktrees = true` закрывает исполнителю путь к `.planning/STATE.md`: `execute-plan.md` исключает файл из коммита плана в режиме worktree, `execute-phase.md` прямо запрещает его трогать, а шаг 5.7 оркестратора переносит только прогресс планов — произвольный текст §Blockers/Concerns из SUMMARY получателя не имеет. Поэтому твёрдым результатом сделан файл каталога фазы, а применение — правкой по фактическому корню из `git rev-parse --git-common-dir`.
- **Тип гейта — `checkpoint:human-action`, осознанное исключение из правила «не заводить человеческий чекпойнт там, где есть CLI или Write».** Оправдание не теоретическое: пропуск воспроизводился дважды (планы 05-08 и 05-10). Гейт превратил тихий пропуск в громкую остановку и снялся проверками, а не мнением.
- **Требования не перемечены.** См. отдельный раздел ниже.

## Требования не перемечены — и это исполнение прохибиции, а не пропуск

`.planning/REQUIREMENTS.md` не открывался на запись ни разу; `git diff --name-only -- .planning/REQUIREMENTS.md` пуст. `BILL-05`/`BILL-06`/`BILL-07` сняты с `Complete` 2026-08-16 именно потому, что фаза получила вердикт `gaps_found`, и однажды преждевременная отметка уже была поставлена и отозвана (коммит `2124d1e`). Отметку возвращает повторная верификация после подтверждения закрытия обоих гэпов — не план, который эти гэпы закрывает.

По той же причине поле frontmatter `requirements-completed` оставлено **пустым** вопреки умолчанию шаблона (шаблон предписывает копировать все ID из `requirements` плана — здесь это были бы `BILL-05` и `BILL-07`). Копирование заполнило бы машиночитаемое поле утверждением «требование выполнено» ровно там, где прохибиция `BILL-07` это запрещает, и дало бы инструментам основание проставить отметку автоматически. Работа по этим требованиям исполнена и описана блоком `coverage` (D1–D8); статус `Complete` присваивает верификация.

## Deviations from Plan

### 1. [Наблюдение — дефект инструментария, не плана] `gsd-tools query state.begin-phase` затёр курированную «Current Position» и был откачен оркестратором

- **Найдено:** оркестратором при возобновлении волны, между снятием чекпойнта и запуском этого агента.
- **Что произошло:** шаг `validate_phase` рабочего процесса `execute-phase.md` вызывает помощник `gsd-tools query state.begin-phase`. Помощник переписал §Current Position в `.planning/STATE.md` обратно на общий шаблон — `Phase: 05 (tarify) — EXECUTING` / `Plan: 1 of 12` / `Status: Executing Phase 05`, — уничтожив ровно ту курированную бухгалтерию, которую задача 1 только что записала. То есть инструмент воспроизвёл дефект класса «Plan 1 of 10» — тот самый, ради устранения которого этот план заведён.
- **Исправление:** оркестратор откатил файл `git checkout -- .planning/STATE.md`. Дерево чисто, курированный русский текст цел — подтверждено этим агентом: `git diff --name-only -- .planning/STATE.md` пуст, а строки 31–36 главного дерева несут `Plan: 12 of 12` и статус закрытия гэпов, а не шаблон.
- **Почему записано здесь:** `state.begin-phase` враждебен рукописному тексту §Current Position. Любой следующий прогон `execute-phase` по фазе 05 затрёт эту секцию снова, если запустится после записи и не будет откачен. Пункт адресован повторной верификации и сопровождению инструментария, а не коду проекта.
- **Границы:** правка была в главном дереве и откачена ДО запуска этого агента; ни одного коммита дефект не породил.

### 2. [Граница объёма] Задача 2 собственного коммита не имеет

Гейт был поставлен на случай, если применение задачей 1 не удастся. Оно удалось, поэтому работой задачи 2 остались только проверки и подтверждение — что прямо предусмотрено её `<action>` («Если задача 1 уже применила правки — работы нет, остаётся выполнить проверки и подтвердить»). Отсутствие коммита у задачи 2 — исполнение плана, а не отклонение от него.

---

**Итого отклонений:** 1 наблюдение по инструментарию (исправлено оркестратором до запуска этого агента), 1 предусмотренная планом граница. Автоисправлений по правилам 1–3 не потребовалось.
**Влияние на план:** объём не расширен. Ни строки кода, ни ревизии Alembic, ни правки `pyproject.toml` — проверено диффом.

## Issues Encountered

- **Из worktree главное дерево доступно только на чтение — и этого достаточно.** Шесть проверок задачи 2 обязаны идти по `.planning/STATE.md` ГЛАВНОГО каталога, а изолированный агент писать туда не вправе. Корень получен из `git rev-parse --git-common-dir` (`/source/broadcaster/.git`), проверки выполнены грепами по абсолютному пути, идентичность копий подтверждена `cmp`. Ни одной записи в главное дерево этот агент не сделал.
- **`graphify query` не использовался намеренно.** Плановые артефакты — не исходный код; графа в worktree нет (`graphify-out/` не отслеживается git и живёт в главном каталоге). Ориентирование шло Read/Grep, как предписано инструкцией на этот случай. Кода план не менял, поэтому `graphify update .` не требуется.

## Известные заглушки

Нет. Заглушек, TODO и пропущенных тестов план не оставил — кода он не касается вовсе.

## Threat Flags

Новой поверхности не добавлено. T-05-58 (утечка секретов в трекинговый документ) соблюдён: записи называют ИМЕНА переменных окружения и заголовков (`YOOKASSA_WEBHOOK_CLIENT_IP_HEADER`, `X-Real-IP`) и пути к значениям, но ни одного значения — ни ключа магазина, ни идентификатора, ни строки подключения. T-05-60 (закрытие дефекта подменяется удалением записи) снят проверкой `grep -c '🔴'` → 2. T-05-61 (преждевременная отметка требования) снят пустым диффом `REQUIREMENTS.md`.

## Перенесённые человеческие проверки — задачами НЕ становились

Восемь пунктов из `05-VERIFICATION.md` остаются человеческими и этим планом не автоматизировались: мобильная ширина 375px, настоящий платёж в тестовом магазине ЮKassa (D-26), статус «отклонён» у отменённого платежа (D-27), читаемость формулировок «в обработке» и отказа оплаты, проход первого настоящего уведомления через гард источника после выката, прохибиция `BILL-05`/fairness (`unresolved` до решения владельца), прохибиция `BILL-06`/transparency, и backstop-пункт D9 этого плана (понятность сводной записи за минуту). Браузерного и e2e-харнесса в проекте нет, боевого доступа к API ЮKassa нет.

## User Setup Required

Нет — новых переменных окружения, ключей конфига и внешних настроек план не вводит.

## Next Phase Readiness

- **Оба гэпа `05-VERIFICATION.md` закрыты работой:** гэп 1 — планом 05-11 (код), гэп 2 — этим планом (бухгалтерия). Фаза готова к ПОВТОРНОЙ верификации, но `Complete` ей не присвоена — вердикт `gaps_found` действует до неё.
- **Что должна сделать повторная верификация:** подтвердить закрытие обоих гэпов и вернуть `BILL-05`/`BILL-06`/`BILL-07` в `Complete`; отметку этот план не ставил намеренно.
- **Открытые пункты, унаследованные фазой:** блокирующий вызов SDK (ревью раунда 2, `CR-02`) — отложен с обоснованием, запись 🔴 в STATE.md; типизированная запись `PLAN_LIMITS` (`WR-10` раунда 1 / `WR-07` раунда 2) — отложена, работа своего размера, меняющая контракт `parsed_plan_limits` у трёх потребителей; очередь из шести невыкаченных ревизий `0013`…`0018` при боевой базе на `0012`; `graphify update .` в главном каталоге после вливания ветки плана 05-11.
- **Для сопровождения инструментария:** `gsd-tools query state.begin-phase` перезаписывает §Current Position шаблоном — см. «Deviations from Plan», п. 1.

## Self-Check: PASSED

Файлы (2/2 на месте):
- FOUND: `.planning/phases/05-tarify/05-STATE-PATCH.md` (164 строки, 19 595 байт)
- FOUND: `.planning/STATE.md` (125 строк) — и в worktree, и в главном каталоге, побайтово идентичны (`cmp` → 0)

Коммиты (1/1 в истории ветки): `c177c44` — `docs(05-12): record phase accounting in STATE.md`, найден `git log --oneline -1 -- .planning/STATE.md`

Критерии, проверенные командами этим агентом:
- Шесть проверок задачи 2 по ГЛАВНОМУ дереву → **0 / 0 / 6 / 2 / 125 / 2** — все сошлись (таблица выше)
- `git diff --name-only -- .planning/STATE.md` → пусто
- `git diff --name-only -- .planning/REQUIREMENTS.md` → пусто
- `git diff --name-only -- app/ tests/ alembic/ pyproject.toml` → пусто
- Одиннадцать критериев приёмки задачи 1 по `05-STATE-PATCH.md` → все сошлись (таблица выше)
- `git status --short` в worktree перед записью SUMMARY → пусто

Фаза `complete` этим агентом НЕ помечена и верификатор НЕ запускался: гейты фазы принадлежат оркестратору.

---
*Phase: 05-tarify*
*Completed: 2026-08-16*
