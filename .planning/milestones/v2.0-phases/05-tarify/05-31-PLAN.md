---
phase: 05-tarify
plan: 31
type: tdd
wave: 24
depends_on: ["05-28", "05-33"]
gap_closure: true
files_modified:
  - tests/test_planning/__init__.py
  - tests/test_planning/test_state_progress_matches_roadmap.py
  - justfile
  - .planning/STATE.md
autonomous: true
requirements: [BILL-05, BILL-06, BILL-07]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Расхождение `.planning/STATE.md` с ROADMAP закрыто МЕХАНИЗМОМ, а не значением: существует прогон, который краснеет в тот момент, когда `progress` перестаёт выводиться из ROADMAP, и он входит в `just test` — ту самую команду, которую в проекте запускают все"
    - "Механизм доказан на воспроизведении САМОЙ регрессии: синтетическая пара «ROADMAP на 78 исполненных / STATE на 73 исполненных» краснит помощника. Это ровно то состояние дерева, которое раунд 7 нашёл и назвал регрессией против закрытого гэпа 3 раунда 5"
    - "Счёт выводится ИЗ ROADMAP, а не сверяется с константой в тесте: перечень планов читается отметками `- [x]` / `- [ ]` разделов `### Phase N:`, поэтому появление следующего плана меняет ожидание само, а тест не требует правки"
    - "Сегодняшние значения `.planning/STATE.md` ИЗМЕРЕНЫ, а не приняты: приведены число, выведенное из ROADMAP, и число из frontmatter, и они равны. Расхождение раунда 7 (`73` против `78`) названо закрытым С УКАЗАНИЕМ коммита, который его закрыл, а не объявлено закрытым"
    - "Прозаические строки `stopped_at:` и `Progress:` называют состояние НА МОМЕНТ ЗАПИСИ, не заводят констант величины и не опережают событий: исполнение сокращённого закрытия раунда 7 названо СОСТОЯВШИМСЯ, а вердикт раунда 8 — НЕ названным вовсе, потому что верификация этого плана не производит. Запрет на константы величины, записанный планом `05-21` с причиной и подтверждённый решением D-33, соблюдается и этой волной"
    - "Все ТРИНАДЦАТЬ находок код-ревью раунда 7 (`CR-01`, `WR-01`…`WR-06`, `IN-01`…`IN-06`) получили распоряжение, и ни один номер не назван голым: каждое вхождение квалифицировано раундом, потому что под тем же номером в прежних раундах лежит другая находка"
    - "Решение D-33 не переоткрыто и не обойдено: машинная проверка ставится на МАШИННО ЧИТАЕМОЕ поле `progress` и на его объявленный источник, а не на прозу. Различие предметов названо в самом тесте, а не только в плане"
    - statement: "Человек, открывший `.planning/STATE.md` после этой волны, понимает состояние фазы и судьбу каждой находки раунда 7 без чтения отчётов; и не встречает утверждения, которое устареет от появления следующего плана. Неустареваемость и понятность судятся человеком, а не грепом — ровно потому, что отличить «число, которое устареет» от «числа, которое не устареет», машине нечем (D-33)"
      verification: backstop
  artifacts:
    - "`tests/test_planning/test_state_progress_matches_roadmap.py` — помощники вывода счёта из ROADMAP и чтения `progress`, сам гейт и негативные контроли над синтетическими парами, включая воспроизведение регрессии раунда 7"
    - "`justfile` — рецепт, запускающий этот гейт отдельно, чтобы шаг трекинга мог проверить себя ДО коммита, а не после верификации"
    - "`.planning/STATE.md` — распоряжения по тринадцати находкам раунда 7, запись вердикта раунда 7, приведённые §Current Position и `progress`"
  key_links:
    - "`.planning/ROADMAP.md` §Phase N (отметки планов) → помощник вывода счёта → frontmatter `progress` в `.planning/STATE.md`. Цепочка ОДНОСТОРОННЯЯ: ROADMAP объявлен единственным источником счёта планов правилом, которым закрыт гэп 3 раунда 5, и гейт закрепляет именно это правило, а не заводит новое"
    - "гейт → `just test`. Механизм, живущий вне команды, которую запускают, не механизм: коммит трекинга волн 19 и 20 обошёл STATE именно потому, что обойти было нечего"
    - "решение D-33 → объём гейта. D-33 отказал машинному гейту на ПРОЗЕ операционного документа с названной причиной; настоящий гейт стоит на поле, которое сам документ объявляет машинно читаемым и приводимым по ROADMAP"
  prohibitions:
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT закрывать регрессию трекинга правкой сегодняшних чисел: закрытие, не заводящее механизма, повторяет раунд 5 и обязано быть отвергнуто как закрытие"
      status: unresolved
      verification: test
    - requirement_id: BILL-05
      category: integrity
      statement: "MUST NOT заводить машинный гейт на ПРОЗУ операционного документа: решение D-33 отказало ему с названной причиной, и обход решения через расширение предмета есть отмена решения молча"
      status: unresolved
      verification: judgment
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT называть номер находки без раунда: между раундами номера переназначены, и голый номер указывает не на ту находку"
      status: unresolved
      verification: judgment
---

<objective>
**Регрессия трекинга закрывается МЕХАНИЗМОМ, и тринадцать находок раунда 7 получают
распоряжения.**

Раунд 7 нашёл `.planning/STATE.md` разошедшимся с ROADMAP — спустя ОДИН раунд после того, как
то же расхождение было закрыто правилом «единственный источник счёта планов — ROADMAP» (гэп 3
раунда 5). Причина установлена по дереву, а не угадана:

> трекинг-коммит волны 18 (`4430ef4`) правил и ROADMAP, и STATE; коммиты волн 19 и 20
> (`ada9500`, `1e80cb5`) правили только ROADMAP. То есть расхождение внесено инструментом
> трекинга, а не автором записи.

Значения к моменту планирования этой волны уже приведены коммитом `6ade68e`
(«docs(phase-05): update tracking after wave 21»). **И это ровно та форма закрытия, которая
уже подвела дважды.** Правка чисел не мешает следующему коммиту трекинга снова обойти STATE:
между «привели значения» и «сделали обход невозможным» лежит вся разница между раундом 5 и
раундом 7.

Поэтому предмет этого плана — механизм. Он устроен так:

- счёт планов **выводится из ROADMAP** отметками `- [x]` / `- [ ]` — то есть из объявленного
  единственного источника, а не из константы в тесте;
- он сверяется с машинно читаемым `progress` в `.planning/STATE.md` — с тем самым полем,
  которое §Current Position сама объявляет «машинно читаемым представлением того же счёта…
  приводится по ROADMAP»;
- сверка живёт в `tests/` и потому входит в `just test`. Коммит, тронувший ROADMAP и
  обошедший STATE, краснит прогон — то есть обойти становится нечего.

⚠️ **Решение D-33 этим НЕ переоткрывается.** D-33 отказал машинному гейту на ПРОЗЕ
операционного документа с названной причиной: отличить «константу величины, которая
устареет» от законного числа (даты, цены, номера ревизии, числа, квалифицированного раундом)
машине нечем. Настоящий гейт прозы не читает вовсе. Он читает ОДНО машинно читаемое поле и
сверяет его с ОДНИМ объявленным источником — предмет, для которого суждения не требуется.
Различие называется в докстринге теста, а не только здесь.

Вторая половина плана — распоряжения. `.planning/STATE.md` несёт собственную прохибицию «ни
одна находка не осталась без распоряжения»; раунд 7 принёс тринадцать находок, и без
распоряжений раунд 8 нашёл бы эту прохибицию нарушенной — ровно как раунд 7 нашёл нарушенной
прохибицию о счёте планов.

Purpose: закрытие, которое не заводит механизма, есть отсрочка, а не закрытие; и находка без
распоряжения теряется, даже когда она записана.
Output: гейт вывода счёта, рецепт для шага трекинга, тринадцать квалифицированных раундом
распоряжений и приведённая §Current Position.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-VERIFICATION.md
@.planning/phases/05-tarify/05-REVIEW.md
@.planning/phases/05-tarify/05-CONTEXT.md
</context>

<flagged_assumptions>

**Зонд краевых случаев: три требования фазы остаются `unclassified` / `unresolved`.**
Классификатор зонда работает по английским формулировкам, а BILL-05, BILL-06 и BILL-07
записаны по-русски. Поднимаются явными допущениями и остаются `unresolved`:

- **E-01 (BILL-05, `unresolved`)** — краевые состояния продления. Допущение: план кода
  продления не касается; он касается операционных записей и одного нового гейта над ними.
- **E-02 (BILL-06, `unresolved`)** — краевые состояния показа осей. Допущение: закрывается
  не кодом, а распоряжением по находке `IN-01` раунда 7 в задаче 3.
- **E-03 (BILL-07, `unresolved`)** — краевые состояния журнала платежей (усечение на
  `PAYMENT_LIST_CAP`). Допущение: закрывается распоряжением по `IN-05` раунда 7 в задаче 3.

Равенство: **3 поднятых == 3 записанных допущения == 0 внесённых в `must_haves.truths`**.

**Допущение о моменте измерения.** Числа `progress` меняются в ходе этого же закрытия: планы
`05-28`, `05-31` и `05-33` внесены в ROADMAP отметками, и два из трёх исполняются раньше
этого плана; четыре спланированных плана (`05-27`, `05-29`, `05-30`, `05-32`) сняты до
исполнения решением владельца (запись D-34), и их строки из ROADMAP удалены с названной
причиной.
Исполнитель снимает ОБА числа прогоном НА МОМЕНТ ИСПОЛНЕНИЯ и приводит `progress` к
выведенному значению; числа `78` и `73` из отчёта раунда 7 приводятся как ИСТОРИЯ, с
указанием коммита `6ade68e`, а не как ожидание.
</flagged_assumptions>

<artifacts_this_phase_produces>

| Артефакт | Где | Что это |
|---|---|---|
| `tests/test_planning/` | новый каталог с `__init__.py` | Дом для машинных проверок над операционными документами репозитория |
| `roadmap_plan_counts()` | `tests/test_planning/test_state_progress_matches_roadmap.py` | Вывод счёта планов из отметок ROADMAP по фазам |
| `state_progress()` | там же | Чтение `progress.total_plans` / `progress.completed_plans` из frontmatter STATE |
| `test_the_machine_readable_progress_is_derived_from_the_roadmap` | там же | Сам гейт |
| `test_the_helper_catches_the_round_seven_regression` | там же | Контроль воспроизведения регрессии раунда 7 на синтетической паре |
| `test_a_newly_added_unchecked_plan_raises_the_total` | там же | Контроль: добавленный неотмеченный план поднимает `total` и краснит устаревший STATE |
| `just tracking-check` | `justfile` | Рецепт запуска гейта отдельно от полной суиты |
| Реестр распоряжений раунда 7 | `.planning/STATE.md` §Blockers/Concerns | Тринадцать записей, каждая квалифицирована раундом |

Символов `app/` план не вводит и файлов `app/` не трогает.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — гейт вывода счёта планов из ROADMAP и его зубы на воспроизведении регрессии раунда 7</name>
  <reversibility rating="reversible">Новый файл тестов и один рецепт; откат — удаление обоих.</reversibility>
  <precondition>`.planning/ROADMAP.md` и `.planning/STATE.md` существуют, и разделы `### Phase N:` ROADMAP несут перечни планов отметками `- [x]` / `- [ ]`. Гейт, выводящий счёт из формата, которого нет, проверял бы собственный разбор.</precondition>
  <files>tests/test_planning/__init__.py, tests/test_planning/test_state_progress_matches_roadmap.py, justfile</files>
  <read_first>
    - .planning/ROADMAP.md (разделы `### Phase 1:`…`### Phase 6:` — формат перечня планов: заголовок `Plans:`, подзаголовки `**Wave N**` и строки `- [x] 05-01-PLAN.md — …`. Отметка `[x]` означает исполненный план, `[ ]` — запланированный. ⚠️ Строка `**Plans**: N/M plans executed` есть ПРОЗА и источником счёта не служит: она устаревает ровно тем способом, против которого записано правило)
    - .planning/STATE.md (frontmatter — блок `progress` с полями `total_phases`, `completed_phases`, `total_plans`, `completed_plans`, `percent`. Гейт читает ДВА поля из пяти и о прочих не утверждает)
    - .planning/STATE.md (§Current Position, абзац «⚠️ СЧЁТ ПЛАНОВ ЭТА СЕКЦИЯ НЕ НАЗЫВАЕТ — НАМЕРЕННО» — там записано правило дословно: «Единственный источник счёта планов — `.planning/ROADMAP.md`» и «Машинно читаемым представлением того же счёта остаётся frontmatter `progress` … приводится по ROADMAP». Гейт закрепляет ЭТО правило и не заводит нового)
    - .planning/phases/05-tarify/05-CONTEXT.md (решение D-33 — отказ от машинного гейта на ПРОЗЕ операционного документа с названной причиной. Докстринг теста обязан назвать различие предметов, иначе следующий читатель прочтёт гейт как отмену D-33)
    - tests/test_migrations/test_deploy_applies_migrations_before_serving.py (ОБРАЗЕЦ ФОРМЫ: тест над файлами репозитория вне `app/`; как объявляются `PROJECT_ROOT` и пути, как пишется абзац «⚠️ ЧЕГО ЭТОТ ФАЙЛ НЕ ДОКАЗЫВАЕТ», как утверждения делаются по свойству, а не по точному тексту)
    - justfile (строки 13-19 — рецепты `test` и `test-cov`: форма записи рецепта, стиль комментария-заголовка, вызов через `uv run pytest`)
  </read_first>
  <behavior>
    - `roadmap_plan_counts()` над живым ROADMAP возвращает по каждой фазе пару «исполнено / всего», выведенную из отметок; фаза без перечня планов даёт `(0, 0)` и не роняет разбор
    - `progress_divergence()` над ЖИВОЙ парой возвращает список расхождений: пустой при согласии, непустой с обоими числами при расхождении. ⚠️ КАКОЙ из двух исходов даёт сегодняшнее дерево, эта задача НЕ утверждает и утверждать не вправе: приведение `progress` — работа задачи 2, и фактический исход фиксируется числами в SUMMARY как её вход
    - Синтетическая пара «ROADMAP с 78 отмеченными планами / STATE с `completed_plans: 73`» — помощник сравнения возвращает расхождение, называющее ОБА числа
    - Синтетическая пара «ROADMAP, куда добавлен неотмеченный план / STATE прежний» — расхождение по `total_plans`
    - Синтетическая совпадающая пара — расхождения нет
    - Сообщение об отказе называет: выведенное число, записанное число и ФАЙЛ С ПОЛЕМ, которое надо привести
    - Гейт не читает ни одной строки ПРОЗЫ `.planning/STATE.md`: он разбирает только frontmatter
  </behavior>
  <action>
Завести каталог `tests/test_planning/` с пустым `__init__.py` и модулем
`test_state_progress_matches_roadmap.py`.

**Разбор ROADMAP.** `roadmap_plan_counts(text)` идёт по строкам, открывает раздел на
`### Phase N:` и считает в текущем разделе строки, начинающиеся с `- [x] ` и `- [ ] `.
Возвращает отображение «раздел → (исполнено, всего)». Разбор ведётся ПО ФОРМЕ СТРОКИ, а не по
точному тексту названия плана: правка формулировки строки не имеет права ронять тест — хрупкий
тест отключают, и вместе с ним отключается свойство.

**Чтение STATE.** `state_progress(text)` разбирает frontmatter (первый блок между `---`)
через `yaml.safe_load` — `PyYAML` уже в окружении — и возвращает пару
`(completed_plans, total_plans)`. Прозу документа функция не читает и не имеет права читать:
это граница, отделяющая настоящий гейт от того, от которого отказалось решение D-33.

**Сравнение.** Помощник `progress_divergence(roadmap_text, state_text)` возвращает список
расхождений, каждое — с именем величины, выведенным числом, записанным числом и указанием на
поле, которое надо привести. Пустой список означает согласие.

**Гейт.** `test_the_machine_readable_progress_is_derived_from_the_roadmap` читает ЖИВЫЕ
`.planning/ROADMAP.md` и `.planning/STATE.md` и требует пустого списка расхождений. Сообщение
об отказе называет оба числа и точное поле — иначе следующий автор «починит» не то.

**Зубы — контролями над СИНТЕТИЧЕСКИМИ парами, а не правкой настоящих файлов.** Три
контроля: воспроизведение регрессии раунда 7 (78 против 73); добавленный неотмеченный план
(рост `total`); совпадающая пара (расхождения нет). Синтетические тексты строятся в самом
модуле, как это сделано с `_SYNTHETIC_*` в `tests/test_application/test_declared_invariants.py`.

**Докстринг модуля обязан назвать ТРИ вещи**, по образцу
`tests/test_migrations/test_deploy_applies_migrations_before_serving.py`:

1. ЗАЧЕМ: расхождение внесено ИНСТРУМЕНТОМ трекинга, и названы коммиты — `4430ef4` (правил
   оба файла), `ada9500` и `1e80cb5` (правили только ROADMAP), `6ade68e` (привёл значения, но
   механизма не завёл). Механизм ставится там, где обход был возможен.
2. ЧЕГО ФАЙЛ НЕ ДОКАЗЫВАЕТ: он не судит ПРОЗУ — ни §Current Position, ни `stopped_at`, ни
   `Progress:`; он не утверждает, что ROADMAP верен; он утверждает только, что машинно
   читаемое поле выведено из объявленного источника.
3. ПОЧЕМУ ЭТО НЕ ОТМЕНА D-33: D-33 отказал гейту на прозе, потому что отличить константу
   величины от законного числа документа машине нечем. Здесь предмет другой — одно поле и
   один объявленный источник, — и суждения он не требует.

**Рецепт.** В `justfile` добавить рецепт `tracking-check`, запускающий этот файл тестов
отдельно (`uv run pytest tests/test_planning/ -q`), с однострочным комментарием-заголовком в
стиле соседних рецептов. Причина рецепта прикладная: шаг трекинга обязан иметь способ
проверить себя ДО коммита, а не после следующей верификации.

⚠️ **ЖИВОЙ ГЕЙТ МОЖЕТ ОКАЗАТЬСЯ КРАСНЫМ, И ЭТА ЗАДАЧА ЕГО НЕ ЗЕЛЕНИТ.** Значения приведены
коммитом `6ade68e`, но планы этой волны отмечаются в ROADMAP по мере исполнения, и на момент
исполнения `progress` может разойтись снова. Приведением `.planning/STATE.md` занимается
ЗАДАЧА 2, и файл `.planning/STATE.md` в `<files>` ЭТОЙ задачи не объявлен — значит, правки
его здесь не будет ни при каком исходе. Следствие для проверок: `<verify>` этой задачи
ИСКЛЮЧАЕТ узел живого гейта (`--deselect`), и ни один её критерий приёмки не требует ни
зелёного `just tracking-check`, ни зелёного полного прогона суиты — оба стоят у задачи 2,
которая их и производит. Прежняя редакция плана требовала обоих ЗДЕСЬ, при том что этот же
абзац прямо раскрывал, что зеленит их соседняя задача: критерий утверждал состояние, которое
задача рядом с ним не производит, — ровно тот класс, ради закрытия которого писана вся эта
волна.

Зубы этой задачи доказывают ТРИ СИНТЕТИЧЕСКИХ КОНТРОЛЯ, а не живое дерево: они краснеют и
зеленеют независимо от того, сошёлся ли сегодня `progress`. Вывод каждого приводится в
SUMMARY. Если живой гейт при первом же прогоне краснеет — это ЗАФИКСИРОВАННОЕ НАБЛЮДЕНИЕ, а
не отказ задачи: расхождение называется числами в SUMMARY и передаётся задаче 2 как её вход.

**Синтетические тексты живут под именами `_SYNTHETIC_*`** — тем же приёмом и с теми же
именами, что в `tests/test_application/test_declared_invariants.py`, — и ЛЮБОЕ число счёта
планов (в том числе `78`, `73`, `84`) стоит только внутри них. В теле самого гейта числовых
констант нет вовсе: он их ВЫВОДИТ. Это проверяется разбором дерева синтаксиса, а не грепом:
греп считает строки, а не их расположение, и на прозе докстринга даёт ложный отказ.

RED-коммит: `test(05-31): гейт вывода счёта планов из ROADMAP`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_planning/ -q --deselect tests/test_planning/test_state_progress_matches_roadmap.py::test_the_machine_readable_progress_is_derived_from_the_roadmap</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_planning/ -q --collect-only` показывает не менее четырёх собранных тестов (гейт плюс три контроля)
    - `uv run pytest tests/test_planning/test_state_progress_matches_roadmap.py::test_the_helper_catches_the_round_seven_regression -q` завершается кодом `0`, и в SUMMARY приведён вывод помощника с числами `78` и `73`
    - `uv run pytest tests/test_planning/test_state_progress_matches_roadmap.py::test_a_newly_added_unchecked_plan_raises_the_total -q` завершается кодом `0`
    - Совпадающая синтетическая пара расхождения не даёт: третий контроль завершается кодом `0`
    - Гейт ВЫВОДИТ счёт, а не сверяет константу — проверяется разбором синтаксиса, а не счётом строк: `python3 -c "import ast; t=ast.parse(open('tests/test_planning/test_state_progress_matches_roadmap.py',encoding='utf-8').read()); f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='test_the_machine_readable_progress_is_derived_from_the_roadmap']; assert f, 'гейт не найден'; bad=[n.value for n in ast.walk(f[0]) if isinstance(n,ast.Constant) and isinstance(n.value,int) and not isinstance(n.value,bool) and n.value not in (0,1)]; assert not bad, bad; print('гейт без констант счёта')"` печатает `гейт без констант счёта`. Разрешены только структурные `0` и `1` (индексы, длины); всякая иная числовая константа в теле гейта означает сверку с числом вместо вывода
    - Числа счёта планов стоят только в синтетических текстах: в SUMMARY приведён вывод `grep -nE '\b78\b|\b73\b|\b84\b' tests/test_planning/test_state_progress_matches_roadmap.py` вместе с диапазонами строк констант `_SYNTHETIC_*`, и каждый найденный номер строки лежит внутри одного из диапазонов. ⚠️ Это критерий-СВИДЕТЕЛЬСТВО, судимый человеком: расположение вхождений грепом не проверяется, и машинную половину утверждения несёт предыдущий критерий
    - Гейт не читает прозу: `grep -c 'Current Position' tests/test_planning/test_state_progress_matches_roadmap.py` даёт `0`; `grep -c 'stopped_at' tests/test_planning/test_state_progress_matches_roadmap.py` даёт `0`
    - Докстринг модуля называет все четыре коммита `4430ef4`, `ada9500`, `1e80cb5`, `6ade68e` и содержит фразу о том, что решение D-33 не отменяется
    - Рецепт `tracking-check` заведён и запускает ровно `tests/test_planning/`: `grep -A2 '^tracking-check:' justfile` показывает вызов `uv run pytest tests/test_planning/`. ⚠️ Критерия «`just tracking-check` завершается кодом `0`» у этой задачи НЕТ намеренно: код `0` требует сошедшегося `progress`, а приводит его ЗАДАЧА 2, чей файл (`.planning/STATE.md`) в `<files>` этой задачи не объявлен
    - ⚠️ Критерия «`uv run pytest tests/ -q` завершается кодом `0`» у этой задачи НЕТ по той же причине: новый гейт входит в полный прогон, и его цвет на живом дереве принадлежит задаче 2. В SUMMARY приводится ФАКТИЧЕСКИЙ цвет живого гейта на этот момент с числами — как вход задачи 2, а не как её отказ
  </acceptance_criteria>
  <done>Гейт заведён, счёт выводится из ROADMAP отметками и не содержит числовых констант в теле, зубы доказаны тремя синтетическими контролями (включая воспроизведение регрессии раунда 7), рецепт `just tracking-check` заведён и указывает на `tests/test_planning/`, прозу гейт не читает; цвет живого гейта зафиксирован числами и передан задаче 2</done>
</task>

<task type="auto">
  <name>Task 2: Значения и прозаические строки STATE приведены к сегодняшнему состоянию — без констант величины и без опережающих утверждений</name>
  <reversibility rating="reversible">Правка операционного документа; откат — возврат прежних формулировок, которые тут же названы.</reversibility>
  <precondition>Задача 1 исполнена: гейт существует и его вывод — источник чисел для этой задачи. Значения, набранные без прогона, есть ровно та форма закрытия, которая подвела в раундах 5 и 7.</precondition>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (frontmatter целиком и §Current Position целиком, строки 1-40 — оба абзаца-предупреждения о константах величины и о правках инструментом. ⚠️ ОБА СОХРАНЯЮТСЯ: они и есть записанное правило, а не украшение)
    - .planning/ROADMAP.md (§Phase 5 — состояние отметок на момент исполнения; и разделы прочих фаз, потому что `total_plans` есть сумма по ВСЕМ фазам, а не по текущей)
    - .planning/phases/05-tarify/05-VERIFICATION.md (четвёртый элемент `gaps:` — формулировка регрессии и её `missing:` дословно; там же названы коммиты-виновники)
    - .planning/phases/05-tarify/05-CONTEXT.md (решение D-33 — тир прохибиций документного класса; новая запись обязана не спорить с ним)
  </read_first>
  <action>
**Числа — прогоном, а не набором.** Снять `roadmap_plan_counts()` над живым ROADMAP на момент
исполнения и привести `progress.total_plans` и `progress.completed_plans` к выведенным
значениям. Поле `percent` пересчитывается по фазам, как и прежде, и остаётся тем, чем было.
В SUMMARY приводятся: выведенное число, прежнее значение поля и команда, которой снято
выведенное.

**Прозаические строки — инвариантом, а не числом, и БЕЗ ОПЕРЕЖЕНИЯ.** `stopped_at:` и
`Progress:` переписываются так, чтобы называть СОСТОЯНИЕ фазы на момент записи и то, чего
фаза ждёт (верификации раунда 8), и НЕ называть счёта планов. Формулировка держится приёма,
записанного планом `05-21` и подтверждённого D-33: перечень планов с отметками отдаёт
ROADMAP, а не автор записи.

⚠️ **Проза НЕ ИМЕЕТ ПРАВА объявить фазу закрытой и НЕ ИМЕЕТ ПРАВА назвать вердикт, которого
нет.** Этот план — последний из трёх, оставленных сужением объёма, поэтому строка вида
«закрытие раунда 7 ИСПОЛНЕНО» здесь ВЕРНА и допустима. Запрещено другое: утверждать, что
фаза ПРОВЕРЕНА, что гэпы ЗАКРЫТЫ ПО ВЕРДИКТУ или что раунд 8 что-либо показал. Вердикт
возвращает верификация, а не исполнение; плана, производящего вердикт, в этом закрытии нет.
Правильная форма называет состояние («сокращённое закрытие раунда 7 исполнено; фаза ждёт
верификации раунда 8»), а перечень планов с отметками отдаёт ROADMAP. Диапазон волн в прозе
не дублируется по той же причине, по какой не дублируется счёт планов: он меняется от
перепланирования, а документ не может держать его истинным.

⚠️ **Запись о сужении объёма, внесённая в §Blockers/Concerns при планировании этого
закрытия, НЕ СТИРАЕТСЯ и НЕ ПЕРЕПИСЫВАЕТСЯ.** Она называет четыре снятых плана (`05-27`,
`05-29`, `05-30`, `05-32`), причину снятия и решение D-34. Дыра в нумерации планов фазы
разрешена и ОБЪЯСНЕНА этой записью; её потеря вернула бы дыру без причины, а сквозная
перенумерация выживших порвала бы соответствие PLAN↔SUMMARY и все ссылки ROADMAP.

**Регрессия названа закрытой С УКАЗАНИЕМ ЧЕМ.** Отдельной строкой в §Blockers/Concerns:
расхождение раунда 7 (`completed_plans: 73` против ROADMAP `78`) приведено коммитом `6ade68e`,
а ПОВТОРЕНИЕ его закрыто механизмом — гейтом `tests/test_planning/test_state_progress_matches_roadmap.py`
и рецептом `just tracking-check`. Названа и причина, по которой одной правки чисел было бы
мало: коммиты трекинга волн 19 и 20 обошли STATE, и обход был возможен, потому что обходить
было нечего.

**Абзац о механизме — в §Current Position, рядом с записанным правилом.** Одна вставка,
которая говорит: правило «единственный источник счёта — ROADMAP» теперь ИМЕЕТ ПРИНУЖДЕНИЕ, и
названы предмет принуждения (поле `progress`), место (`tests/test_planning/`), команда
(`just tracking-check`) и ГРАНИЦА — проза этой секции по-прежнему судится человеком, потому
что решение D-33 отказало машинному гейту на ней с названной причиной. Граница называется
прямо, чтобы следующий читатель не принял гейт за отмену D-33.

⚠️ Ни один прежний абзац §Current Position не стирается. Оба предупреждения — о константах
величины и о том, что запись инструментом семейства `state.*` возвращает шаблонные строки, —
остаются дословно: второе описывает механизм, который уже однажды уничтожил записанный
вердикт (`1e62a71`), и его потеря вернула бы этот исход.
  </action>
  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_planning/test_state_progress_matches_roadmap.py::test_the_machine_readable_progress_is_derived_from_the_roadmap -q` завершается кодом `0` на живом дереве. ⚠️ Критерий стоит ЗДЕСЬ, а не у задачи 1: зелёным живой гейт делает приведение `progress`, а `.planning/STATE.md` объявлен в `<files>` ЭТОЙ задачи и только её
    - `just tracking-check` завершается кодом `0` — тем же основанием
    - `uv run pytest tests/ -q` завершается кодом `0`: новый гейт входит в полный прогон, и с этого момента полный прогон зелен
    - Расхождение, если оно было на входе задачи, названо ЧИСЛАМИ: в SUMMARY приведены значение, выведенное из ROADMAP, и значение поля ДО правки, даже если они совпали (совпадение — тоже измерение, и молчание о нём неотличимо от непроверенного)
    - Прозаические строки не называют счёта планов: `sed -n '/^stopped_at:/p;/^Progress:/p' .planning/STATE.md` и строка `Progress:` §Current Position не содержат сочетаний вида «N планов», «N/M» и «N of M» — проверяется грепом на цифры в этих двух строках и подтверждается в SUMMARY цитатой обеих строк целиком
    - Проза не опережает исполнения: обе строки приводятся в SUMMARY целиком, и ни одна не утверждает, что фаза ПРОВЕРЕНА, что гэпы закрыты ПО ВЕРДИКТУ или что раунд 8 что-либо показал — вердикт возвращает верификация, а плана, производящего вердикт, в этом закрытии нет. Критерий судится человеком по приведённым в SUMMARY строкам: отличить «состояние» от «опережающего утверждения» грепом нечем, и это ровно та причина, по которой решение D-33 отказало машинному гейту на прозе
    - Запись о сужении объёма закрытия раунда 7 на месте и не переписана: `grep -q 'сужен' .planning/STATE.md` завершается кодом `0`, и в SUMMARY приведена эта запись целиком
    - Оба прежних абзаца-предупреждения §Current Position на месте: `grep -c 'СЧЁТ ПЛАНОВ ЭТА СЕКЦИЯ НЕ НАЗЫВАЕТ' .planning/STATE.md` даёт `1` и `grep -c '1e62a71' .planning/STATE.md` даёт не менее `1`
    - §Current Position называет механизм и его границу: `grep -c 'just tracking-check' .planning/STATE.md` даёт не менее `1`, и рядом стоит фраза о том, что решение D-33 не отменяется
    - Запись о закрытии регрессии называет коммит `6ade68e` и коммиты-виновники `ada9500` и `1e80cb5`
    - `git diff -- .planning/STATE.md | grep -c '^-[^-]'` показывает удалённые строки ТОЛЬКО в пределах переписываемых `stopped_at:`, `Progress:`, `last_activity*` и полей `progress`; ни одна запись §Blockers/Concerns и ни один абзац §Current Position не удалён — подтверждается в SUMMARY перечнем удалённых строк
  </acceptance_criteria>
  <done>`progress` выведен прогоном и совпадает с ROADMAP, живой гейт и полный прогон зелены; прозаические строки называют состояние без констант величины и без утверждения о вердикте, которого верификация ещё не выносила; механизм и его граница записаны в §Current Position; запись о сужении объёма и ни одна прежняя запись не стёрты</done>
</task>

<task type="auto">
  <name>Task 3: Тринадцать находок код-ревью раунда 7 получают распоряжения, квалифицированные раундом</name>
  <reversibility rating="costly">Реестр распоряжений — документ, по которому принимаются следующие решения; находка без распоряжения теряется, даже будучи записанной, и обнаруживается только следующим раундом.</reversibility>
  <precondition>Планы `05-28` и `05-33` исполнены, их SUMMARY существуют: распоряжения «закрыт планом N» обязаны опираться на исполненный план, а не на намерение. ⚠️ Планы `05-29`, `05-30` и `05-32` СНЯТЫ до исполнения решением владельца (запись D-34 в `05-CONTEXT.md`), их SUMMARY не существует и существовать не будет — распоряжение, ссылающееся на любой из них, ссылалось бы на несостоявшееся.</precondition>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/phases/05-tarify/05-REVIEW.md (весь раздел `## Critical Issues`, `## Warnings` и `## Info` — тринадцать находок раунда 7: `CR-01`; `WR-01`…`WR-06`; `IN-01`…`IN-06`. Каждая нужна с её файлом и одной фразой предмета)
    - .planning/STATE.md (§Blockers/Concerns целиком — ОБРАЗЕЦ ДОСЛОВНЫЙ: как записана существующая строка находки, как квалифицируется раунд, какими словами оформляются три исхода — «закрыт планом N», «отложено с обоснованием», «записано без требования кода». Особое внимание записям, заведённым планами `05-14`, `05-16` и `05-26`)
    - .planning/phases/05-tarify/05-28-SUMMARY.md (исход `CR-01`: что именно сделано и чем закреплено)
    - .planning/phases/05-tarify/05-33-SUMMARY.md (исход `WR-01` — спор двух объявлений о статусе прохибиции `05-01`, закрытый приведением трёх абзацев к каноническому прочтению D-34)
    - .planning/phases/05-tarify/05-CONTEXT.md (запись D-34 целиком — там записано сужение объёма закрытия раунда 7 и принятый долг по щели гейта объявлений. Распоряжения по находкам, которые закрывали снятые планы, обязаны ССЫЛАТЬСЯ на эту запись, а не заводить рядом второе обоснование)
    - .planning/phases/05-tarify/05-VERIFICATION.md (`### Gaps Summary`, абзац «Записанный долг фазы» — перечень пунктов, НЕ вменяемых вердикту. Распоряжения по находкам, попадающим в этот перечень, обязаны на него ссылаться, а не заводить новый долг рядом)
  </read_first>
  <action>
Записать в §Blockers/Concerns ТРИНАДЦАТЬ распоряжений, каждое с квалификацией `(раунд 7)`
рядом с номером. Номер без раунда запрещён: под тем же номером в прежних раундах лежит другая
находка, и голая ссылка указывает не на тот дефект.

Распоряжения разносятся на три названные группы:

**Закрыты этим закрытием** — с указанием плана и того, чем закрытие закреплено:
`CR-01` (защита `_plan_price`, план `05-28`), `WR-01` (спор двух объявлений о статусе
прохибиции `05-01`, план `05-33`: три абзаца приведены к каноническому прочтению D-34).

**Приняты ДОЛГОМ решением владельца, а не оставлены молча** — `WR-02` (множество
проверяемых гейтом модулей), `IN-02` (постоянство поля `decided_by` в испускании
`subscription_plan_downgraded`), `IN-03` и `IN-04` (печатник реестра долга и разбор строки
реестра). Все четыре закрывались снятыми планами `05-29` и `05-30`; объём закрытия раунда 7
сужен решением владельца, и причина записана в D-34 арифметикой (раунд 6 — 17/20, раунд 7 —
19/24: знаменатель вырос от артефактов, произведённых предыдущим раундом закрытия). Каждая
запись обязана: назвать снятый план поимённо, СОСЛАТЬСЯ на D-34 вместо второго обоснования
рядом, и назвать форсирующий признак возврата к вопросу — находку того же класса в раунде,
который сам этих мест не правил. ⚠️ Ни одна из четырёх не записывается «закрытой»: они
записаны ПРИНЯТЫМИ, и это разные слова.

**Отложены с обоснованием, и обоснование названо, а не подразумевается** — `WR-03`, `WR-04`,
`WR-05`, `WR-06`. Все четыре суть переномерованные `WR-04`…`WR-07` раунда 6, уже отложенные с
обоснованием и названные `Gaps Summary` раунда 7 записанным долгом, не вменяемым вердикту.
Каждая запись обязана: назвать ОБА номера («`WR-03` раунда 7, он же `WR-04` раунда 6»),
повторить обоснование одной фразой и сослаться на существующую запись, а не завести вторую о
том же.

**Записаны без требования кода** — `IN-01` (истёкший либо выпавший из конфига план
показывается действующим правом; принуждения лимитов в системе нет по решению D-08, поэтому
цена — показ, а не доступ; интерфейсная работа вынесена за границу объёма планами `05-11` и
`05-13`), `IN-05` (`первый платёж` в усечённом журнале; `PAYMENT_LIST_CAP` и признак
`payments_truncated` названы), `IN-06` (монтирование `/var/run/docker.sock` в `web` —
ПРЕДШЕСТВУЕТ фазе и уже записано в §Blockers/Concerns как `WR-07` раунда 3 с распоряжением;
здесь оно КВАЛИФИЦИРУЕТСЯ раундом 7 как повторно найденное, а не заводится второй записью, и
владелец риска по-прежнему не назван — седьмой раунд подряд).

Отдельной строкой — **вердикт раунда 7**: `gaps_found`, `19/24`, дата прогона, и одной фразой
что именно закрыто (верхняя граница предоплаченного горизонта на обеих ветках, проверенная
перебором всех дней года) и что осталось человеку (перечень `human_verification` отчёта не
переписывается, а называется ссылкой).

⚠️ Ни одна прежняя запись §Blockers/Concerns не стирается и не переписывается. Устаревшая
запись ПЕРЕСКАЗЫВАЕТСЯ с названной причиной — прохибиция «закрытие дефекта удалением записи о
нём» соблюдается и здесь.

⚠️ Констант величины в новых записях быть не должно там, где величина устареет. Числа,
КВАЛИФИЦИРОВАННЫЕ РАУНДОМ («раунд 7 насчитал тринадцать находок», «`19/24`»), правилом не
задеваются: они описывают состоявшееся событие и от следующего раунда не устаревают — это
записано в §Current Position дословно.
  </action>
  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Все тринадцать номеров присутствуют и каждый квалифицирован раундом: `grep -oE '(CR-01|WR-0[1-6]|IN-0[1-6]) \(раунд 7\)' .planning/STATE.md | sort -u | wc -l` даёт `13`
    - Голых номеров раунда 7 не осталось: в добавленном диффе нет вхождения номера без соседней квалификации `(раунд 7)` — проверяется просмотром `git diff -- .planning/STATE.md` и подтверждается в SUMMARY счётом вхождений и счётом квалификаций (они равны)
    - Четыре отложенных несут ОБА номера: `grep -c 'он же WR-0. раунда 6' .planning/STATE.md` даёт `4`
    - `IN-06` не заводит второй записи: `grep -c 'WR-07 (раунд 3)' .planning/STATE.md` не увеличился относительно значения до задачи (оба значения в SUMMARY), а квалификация раундом 7 добавлена к существующей записи
    - Вердикт раунда 7 записан: `grep -c '19/24' .planning/STATE.md` даёт не менее `1`
    - Ни одна прежняя запись не удалена: `git diff -- .planning/STATE.md | grep '^-[^-]' | grep -c -E 'раунд[а]? [1-6]'` даёт `0`
    - `uv run pytest tests/ -q` завершается кодом `0`
  </acceptance_criteria>
  <done>Тринадцать находок раунда 7 разнесены на три группы с квалифицированными раундом распоряжениями; четыре отложенных несут оба номера и ссылку на прежнее обоснование; `IN-06` квалифицирован без второй записи; вердикт раунда 7 записан; прежние записи не тронуты</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| инструмент трекинга → `.planning/STATE.md` | Автоматическая запись может обойти файл целиком либо, наоборот, вернуть шаблонные строки и стереть записанный вердикт (`1e62a71`) |
| `.planning/ROADMAP.md` → `progress` | Единственный объявленный источник счёта; отсутствие принуждения делает вывод делом внимания |
| решение D-33 → объём нового гейта | Расширение гейта на прозу отменило бы решение молча |
| номер находки без раунда → находка | Между раундами номера переназначены; голый номер указывает не на тот дефект |
| §Blockers/Concerns → следующее решение | Находка без распоряжения теряется, будучи записанной |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-211 | Repudiation | Регрессия трекинга закрывается правкой сегодняшних чисел и повторяется в третий раз | high | mitigate | Задача 1: гейт выводит счёт из ROADMAP и входит в `just test`; зубы доказаны воспроизведением регрессии раунда 7 на синтетической паре |
| T-05-212 | Tampering | Гейт сверяет константу, записанную в тесте, а не выведенное значение: следующий план ломает тест, и тест правят числом | high | mitigate | Задача 1: счёт выводится отметками ROADMAP; критерий требует, чтобы числа встречались только внутри синтетических текстов контролей |
| T-05-213 | Repudiation | Гейт читают как отмену решения D-33, и следующий план заводит гейт на прозе | medium | mitigate | Задача 1: докстринг модуля называет различие предметов; задача 2: та же граница записана в §Current Position |
| T-05-214 | Tampering | Запись инструментом семейства `state.*` возвращает шаблонные строки и стирает §Current Position вместе с вердиктом | high | mitigate | Задача 2: оба абзаца-предупреждения сохраняются дословно; критерий требует их присутствия грепом, включая ссылку на `1e62a71` |
| T-05-215 | Repudiation | Находка раунда 7 остаётся без распоряжения, и раунд 8 находит нарушенной собственную прохибицию документа | high | mitigate | Задача 3: тринадцать номеров разнесены на три группы; критерий считает уникальные квалифицированные вхождения и требует `13` |
| T-05-216 | Spoofing | Голый номер находки указывает на другой дефект прежнего раунда | medium | mitigate | Задача 3: каждое вхождение квалифицировано раундом; счёт вхождений и счёт квалификаций сверяются и приводятся в SUMMARY |
| T-05-217 | Repudiation | Отложенная находка заводится ВТОРОЙ записью рядом с существующей, и обоснование расходится с прежним | medium | mitigate | Задача 3: четыре отложенных несут оба номера и ссылаются на существующую запись; `IN-06` квалифицируется без второй записи |
| T-05-218 | Denial of Service | Новый гейт краснеет в окне между коммитом плана и коммитом трекинга и блокирует прогон | low | accept | Это и есть предмет механизма: окно, в котором STATE не приведён, обязано быть видимым. Цена принята сознательно, а рецепт `just tracking-check` даёт шагу трекинга способ проверить себя ДО коммита |
| T-05-219 | Information Disclosure | В записи распоряжения попадает боевой адрес или значение секрета при описании находки `IN-06` | low | accept | Записи называют пути монтирования и имена сервисов; значений секретов ни одна не содержит. Принимается тем же порядком, что для существующей записи `WR-07` раунда 3 |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План не устанавливает пакетов: `PyYAML` уже в окружении, `git diff --name-only` не содержит `pyproject.toml` / `uv.lock`. Устанавливаемых пакетов ноль |
</threat_model>

<verification>

1. `uv run pytest tests/ -q` завершается кодом `0`.
2. `just tracking-check` завершается кодом `0`.
3. `grep -oE '(CR-01|WR-0[1-6]|IN-0[1-6]) \(раунд 7\)' .planning/STATE.md | sort -u | wc -l` даёт `13`.
4. `git diff -- .planning/STATE.md | grep '^-[^-]' | grep -c -E 'раунд[а]? [1-6]'` даёт `0`.
5. `git diff --name-only` содержит ровно четыре файла: два в `tests/test_planning/`, `justfile` и `.planning/STATE.md`. Ни одного файла `app/`.

</verification>

<success_criteria>

- Расхождение `progress` с ROADMAP краснит `just test` — механизм, а не значение.
- Зубы механизма доказаны воспроизведением ровно той регрессии, которую нашёл раунд 7.
- Счёт выводится из объявленного источника; тест не требует правки от появления следующего плана.
- Прозаические строки называют состояние без констант величины; решение D-33 не переоткрыто, и это сказано в двух местах.
- Тринадцать находок раунда 7 имеют распоряжения, квалифицированные раундом; ни одна прежняя запись не стёрта.

</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-31-SUMMARY.md` when done.

В SUMMARY привести: (1) вывод трёх синтетических контролей, включая числа `78`/`73`
воспроизведённой регрессии; (2) выведенное из ROADMAP значение `progress` на момент
исполнения, прежнее значение поля и команду, которой снято выведенное; (3) обе прозаические
строки целиком после правки — как доказательство отсутствия констант величины; (4) счёт
вхождений тринадцати номеров и счёт квалификаций `(раунд 7)` рядом, с указанием их равенства;
(5) перечень удалённых строк `.planning/STATE.md` целиком.
</output>
