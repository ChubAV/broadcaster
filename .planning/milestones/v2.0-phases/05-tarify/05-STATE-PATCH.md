# 05-STATE-PATCH — готовая к применению замена секций `.planning/STATE.md`

**План:** 05-12 (гэп 2 отчёта `05-VERIFICATION.md`)
**Дата подготовки:** 2026-08-16
**Вход:** `.planning/STATE.md`, 121 строка, mtime 16.08 11:09 — файл объявлен в
`files_modified` планов `05-08` и `05-10` и не изменён ни одним.

Файл лежит в каталоге фазы намеренно: файлы каталога фазы коммитятся планом в любом
режиме исполнения и переживают снос worktree. Даже если применение к рабочему дереву не
удастся, готовый текст остаётся под рукой, а не в теле отчёта.

Правок ровно пять, по разделу на каждую: **§Blockers/Concerns**, **§Deferred Items**,
**§Decisions**, **«Current Position»**, **frontmatter `progress`**. Ни один раздел
`STATE.md`, не названный ниже, не трогается; файл не перегенерируется целиком.

**Ловушка нумерации.** `05-REVIEW.md` перезаписывался: раунд 1 живёт только в git
(`git show 4989f7a:.planning/phases/05-tarify/05-REVIEW.md`), раунд 2 — на диске. Номера
находок между раундами ПЕРЕНАЗНАЧЕНЫ, и совпадение номера не означает совпадения
находки. Поэтому каждая запись ниже называет РАУНД и СУТЬ, а не голый номер.

---

## 1. §Blockers/Concerns — правка строки 87 (очередь невыкаченных ревизий)

**Удаляется** (фрагмент строки 87): `невыкаченный долг вырос до пяти ревизий: `0013`,
`0014`, `0015`, `0016`, `0017`.`

**Встаёт вместо этого** (остальное содержание строки — D-26, необратимость `0013`, guard
по `hostname`/`port`/`dbname` — не трогается):

> невыкаченный долг вырос до шести ревизий: `0013`, `0014`, `0015`, `0016`, `0017`,
> `0018`.

**Почему.** Число проверено по репозиторию, а не переписано из плана:
`ls alembic/versions/` даёт `0013`…`0018` при боевой базе на `0012`. Ревизия `0018`
(частичный уникальный индекс по активной подписке) отгружена планом `05-08` в тот же
день. Утверждение «пять» стало неверным ровно тем же способом, что и обе снятые ниже
🔴 записи — та же секция, тот же день, та же цена: единственный операционный документ
называет денежный долг меньшим, чем он есть.

## 2. §Blockers/Concerns — снятие двух устаревших 🔴 записей (строки 89 и 90)

**Удаляются ЦЕЛИКОМ** обе строки:

- строка 89 — `🔴 [Phase 5, CR-01] Перед выкатом обязателен YOOKASSA_WEBHOOK_CLIENT_IP_HEADER=X-Real-IP …`
- строка 90 — `🔴 [Phase 5, CR-02] Защита от двойного зачисления — check-then-act без блокировки …`

Обе переезжают в таблицу §Deferred Items закрытыми строками (раздел 4). Переносом, а не
переписыванием на месте: в таблице уже лежит точный образец — три закрытые записи Фазы 2
в одну строку каждая. Побочная выгода — секция §Blockers/Concerns перестаёт расти.

**Содержимое обеих записей ЛОЖНО на сегодня.** Строка 89 утверждает, что
`app/config.py:94` даёт пустое значение (сейчас `X-Real-IP`, строка 108) и что правки
docker-compose не нужны (запись добавлена, `docker-compose.prod.yml:27`). Строка 90
утверждает, что `select(Payment)` идёт без блокировки строки и что уникального индекса по
`user_id` нет — обе половины закрыты (`payment_service.py:291`, ревизия `0018`).

**Строка 88** (D-27, подписка на `payment.canceled`) остаётся как есть: по-прежнему верна
и по-прежнему открыта.

## 3. §Blockers/Concerns — две новые записи

**Встают после строки 88**, перед строкой `- Brownfield-риск: …`.

### 3.1. Новая 🔴 запись — блокирующий вызов SDK

> - 🔴 [Phase 5, ревью раунда 2 — CR-02] `YooPayment.create()` — синхронный HTTP-вызов без таймаута внутри `async def` (`app/services/payment_service.py:128-141`), а `docker-compose.prod.yml:87` не задаёт `--workers`: воркер один, и зависшее соединение с ЮKassa вешает всё приложение, включая `/health`. Та же причина шла в раунде 1 половиной `WR-05`. **Отложено с обоснованием** (план 05-10): перенос в поток меняет модель исполнения платёжного вызова и требует своего наблюдения за временем ответа, а таймаута SDK всё равно не предоставляет — один `to_thread` проблему зависшего TLS-соединения перекладывает, а не решает.

Заголовок называет РАУНД намеренно: `CR-02` раунда 1 — это двойное зачисление, закрытое
планом `05-08`, совсем другая находка. Запись не обнуляет счёт 🔴 по файлу: закрытие
дефекта не подменяется удалением записи, открытый дефект остаётся видимым.

### 3.2. Новая 🟡 запись — сводное распоряжение по находкам код-ревью

Одной записью, а не восемнадцатью строками на раунд: бюджет файла — 150 строк, и
строка на находку обменяла бы один дефект на другой. Записывается **одной физической
строкой**.

> - 🟡 [Phase 5, код-ревью — сводное распоряжение по обоим раундам] Ревью фазы ПЕРЕЗАПИСЫВАЛОСЬ, и номер без раунда указывает не на ту находку: раунд 1 читается только из git (`git show 4989f7a:.planning/phases/05-tarify/05-REVIEW.md`), раунд 2 лежит на диске (`05-REVIEW.md`, итог `critical: 2, warning: 9, info: 7`). **Закрыты планами:** CR-01 раунда 1 → `05-07`, CR-02 раунда 1 → `05-08`, CR-01 раунда 2 → `05-11` (три строки §Deferred Items). **Приняты в работу и исполнены:** WR-01 раунда 1 (`format_amount` на `NaN`/`Infinity`) → `05-09`; WR-04 раунда 1 (константы предмета покупки) → ранее; половина WR-05 раунда 1 про обработку ошибок SDK и IN-06 раунда 1 (кнопка «Продлить» у истёкшего `free`) → `05-10`. **Отложены с обоснованием:** CR-02 раунда 2 — отдельной 🔴 записью выше, там же названа его половина из раунда 1; WR-10 (раунд 1) и WR-07 (раунд 2) — одна причина, два места отказа, ОДНО распоряжение: негодный `PLAN_LIMITS` роняет `axis_percent` сравнением строки с числом (негодный ТИП лимита — первый номер) и `json.loads` в `parsed_plan_limits`, отдавая 500 на весь раздел тарифов (негодный ФОРМАТ документа — второй номер, первым НЕ покрыт), ответ один — типизированная запись плана, проверяемая при построении `Settings`, чтобы негодный конфиг падал на старте, а не на клике покупателя, и это работа своего размера: контракт `parsed_plan_limits` меняется у трёх потребителей; WR-03, WR-06, WR-08, WR-09 раунда 1 (SQLite-каскад ревизии `0017`, ответ 200 на необработанное уведомление, неподтверждённые `limit`/`offset`, отсутствие пере-запроса платежа у ЮKassa) и IN-01…IN-05 раунда 1 — причины названы планами `05-07`, `05-08` и `05-10`; раунд 2 целиком: WR-01 (тот же lost-update в `reset_free_monthly`), WR-02 и WR-03 (гард переоткрывается одной непроверенной переменной, аварийный выключатель не оставляет следа), WR-04 (`"object": null` даёт 500 и цикл повторов ЮKassa), WR-05 (`limit`/`offset` у `GET /api/billing/transactions`), WR-06 (`∞` в сайдбаре при нулевом балансе), WR-08 («первый платёж»/«продление» решается по усечённой карте — тот же дефект нёс свой номер и в раунде 1), WR-09 (осиротевший `pending` при неожиданной форме ответа SDK), IN-01…IN-07 (в том числе IN-07 — платёж с пустым планом записывается как `free`). Ни одна находка не осталась без распоряжения; ни одна из отложенных не входит в два `status: failed` гэпа `05-VERIFICATION.md`.

## 4. §Deferred Items — три новые строки таблицы

**Встают в конец таблицы**, форматом соседних закрытых записей Фазы 2
(`| Phase 2 | CR-01 — … | ✓ Закрыто планом 02-02 (2026-08-11) | 2026-08-10 |`).

> | Phase 5 | CR-01 ревью раунда 1 — гард источника вебхука читал адрес пира при пустом заголовке | ✓ Закрыто планом 05-07 (2026-08-16): безопасное умолчание `X-Real-IP` стало свойством `app/config.py`, значение закреплено записью в `docker-compose.prod.yml`, ветка адреса пира удалена целиком; доказано регрессией через настоящий `ProxyHeadersMiddleware` (`tests/test_routes/test_billing_webhook_proxy_headers.py`, 10 тестов). Организационной мерой запись быть перестала; человеческим остаётся РОВНО ОДИН пункт — боевой nginx ставит настроенный заголовок именно на маршруте вебхука | 2026-08-16 |
> | Phase 5 | CR-02 ревью раунда 1 — двойное зачисление, check-then-act без блокировки | ✓ Закрыто планом 05-08 (2026-08-16): `_claim_payment` — настоящий compare-and-swap с `rowcount == 1`, `with_for_update()` на выборке, приращение баланса на стороне СУБД, вторая активная строка подписки закрыта частичным уникальным индексом ревизии `0018`; доказано `test_payment_concurrency.py` и `test_0018_subscriptions_unique_user.py` (22 теста) | 2026-08-16 |
> | Phase 5 | CR-01 ревью раунда 2 — молчаливое понижение тарифа подтверждённым платежом в `_apply_extension` | ✓ Закрыто планом 05-11 (2026-08-16): правило `switch_is_refused` объявлено один раз (`app/application/billing/plan_switch.py`) и читается обеими стадиями, сохранение старшего тарифа пишет `subscription_plan_preserved` уровня `warning`, регрессия стадии применения — `test_a_confirmed_lower_plan_does_not_strip_the_higher_one_at_the_apply_stage`. ⚠️ Вердикт `05-VERIFICATION.md` раунда 2 — `gaps_found` — действует до повторной верификации: запись фиксирует исполненную работу, а не пройденную проверку | 2026-08-16 |

Закрытия названы ИМЕНАМИ — функцией правила, ключом журнала, именами регрессий, — а не
словом «исправлено»: имя проверяется грепом, «исправлено» не проверяется ничем.

## 5. §Decisions — одна новая запись

**Встаёт последней строкой списка**, форматом соседних записей `Phase 3: …`.

> - Phase 5: семантика смены тарифа — `upgrade-only` (чекпойнт плана 05-10): повышение разрешено и оплаченный остаток не сжигает, понижение при действующей подписке не предлагается и не принимается. Правило объявлено ОДИН раз (`app/application/billing/plan_switch.py`) и читается ОБЕИМИ стадиями — гардом формы `POST /billing/subscribe` и `_apply_extension` (план 05-11). Названная цена варианта: повышение отдаёт лимиты старшего тарифа на оставшиеся дни младшего бесплатно, и это принято сознательно — применения тарифных лимитов в системе нет (D-08).

Сегодня эта семантика живёт только в докстрингах и планах; решение фазы, стоящее денег,
обязано лежать там, где его читают до кода.

## 6. «Current Position» — приведение в соответствие с ROADMAP

**Удаляются** строки 31-36 (`Phase: 05 (tarify) — EXECUTING` … строка прогресса).

**Встаёт вместо этого:**

> Phase: 05 (tarify) — EXECUTING (закрытие гэпов раунда 2)
> Plan: 12 of 12
> Status: Исполнены планы 05-01…05-11; вердикт `05-VERIFICATION.md` раунда 2 — `gaps_found`. Гэп 1 (молчаливое понижение тарифа) закрыт планом 05-11, гэп 2 (бухгалтерия фазы) закрывается планом 05-12. Отметку `Complete` требованиям возвращает повторная верификация, а не эти планы.
> Last activity: 2026-08-16 — план 05-12: бухгалтерия фазы записана в STATE.md
>
> Progress: [███████████████████░] 63/64 планов (98%) — Фазы 1–4 из 6 завершены (67% фаз milestone v2.0); Фаза 5 — 11/12, план 05-12 исполняется

Сегодня там стоит `Plan: 1 of 12` — утверждение, ложное с 16.08. ROADMAP называет
`11/12 plans executed` и 12 планов фазы; текст STATE.md обязан согласоваться с ним, а не
спорить.

## 7. frontmatter `progress` — устранение расхождения с текстом

**Удаляется:** `completed_plans: 62` и `stopped_at: Phase 05 UI-SPEC approved`,
`last_activity_desc: Phase 05 execution started`.

**Встаёт вместо этого:**

> ```yaml
> stopped_at: Плана 05-12 задача 1 — бухгалтерия фазы записана в STATE.md
> last_updated: "2026-08-16T17:27:22.000Z"
> last_activity: 2026-08-16
> last_activity_desc: "План 05-12: бухгалтерия фазы записана в STATE.md"
> progress:
>   total_phases: 6
>   completed_phases: 4
>   total_plans: 64
>   completed_plans: 63
>   percent: 67
> ```

**Почему 64 и 63.** Сегодня frontmatter говорит `total_plans: 62 / completed_plans: 58`
в одном месте и `52/52 (100%)` строкой прогресса — обе цифры не могут быть верны
одновременно. Число берётся из ROADMAP: 13 + 15 + 12 + 12 + 12 = 64 плана заведено
(Фаза 6 — `TBD`, планов нет); исполнено 52 (Фазы 1–4) + 11 (Фаза 5 без 05-12) = 63.
После записи `05-12-SUMMARY.md` счётчик становится 64, а строка прогресса — `12/12`.

## Что этот патч НЕ делает

- **Требования не перемечает.** `.planning/REQUIREMENTS.md` не открывается на запись:
  `BILL-05`/`BILL-06`/`BILL-07` сняты с `Complete` 2026-08-16 именно потому, что фаза
  получила `gaps_found`, и однажды преждевременная отметка уже была поставлена и
  отозвана (коммит `2124d1e`). Отметку возвращает верификация.
- **Кода не касается.** Гэп 2 — организационный; `app/`, `tests/`, `alembic/` пусты в
  диффе плана.
- **Ручные проверки задачами не делает.** Семь пунктов `human_verification` и один
  `behavior_unverified_items` из `05-VERIFICATION.md` остаются человеческими: мобильная
  ширина 375px, настоящий платёж в тестовом магазине ЮKassa (D-26), статус «отклонён» у
  отменённого платежа (D-27), читаемость формулировок, проход первого настоящего
  уведомления через гард источника после выката и прохибиция `BILL-05`/fairness,
  остающаяся `unresolved` до решения владельца.

## Бюджет файла

Вход — 121 строка. Снимаются 2 длинных 🔴 абзаца, добавляются 2 записи
§Blockers/Concerns, 3 строки таблицы §Deferred Items и 1 запись §Decisions: итог —
125 строк при потолке рабочего процесса 150 и цели плана 145.
