---
phase: 05-tarify
plan: 14
subsystem: planning-records
tags: [gap-closure, state-md, code-review-rounds, finding-numbering, docker-socket, checkpoint-blocked]

# Dependency graph
requires:
  - phase: 05-tarify (план 05-13)
    provides: "Ответ чекпойнта `apply-after-expiry` и НАСТОЯЩИЕ имена закрытия CR-01 раунда 3 — `subscription_is_live`, `period_is_live`, четыре именованных теста; без них запись §Deferred Items называла бы плановые имена, а не исполненные"
  - phase: 05-tarify (план 05-12)
    provides: "`05-STATE-PATCH.md` — образец формата «что удаляется / что встаёт вместо» и доказанный маршрут «твёрдый артефакт → best-effort применение → блокирующий чекпойнт»"
  - phase: 05-tarify (ревью раунда 3, `164d210`)
    provides: "Таблица трёх раундов с коммитами и итогами (`05-REVIEW.md:64-84`), тринадцать находок и прямое указание записать WR-07 в STATE.md с владельцем"
provides:
  - "`.planning/phases/05-tarify/05-STATE-PATCH-R3.md` — готовая к применению замена СЕМИ секций `.planning/STATE.md`, закоммичена, переживает снос worktree"
  - "Реестр раундов ревью в форме, которой нечему устареть: каждый раунд адресуется своим неизменяемым коммитом, перечень отдаёт `git log --oneline`"
  - "Распоряжения по ВСЕМ тринадцати находкам раунда 3, каждое с номером в форме `<номер> (раунд 3)`"
  - "Запись владельца и распоряжения для `WR-07 (раунд 3)` — root-equivalent доступ к хосту у веб-слоя перестаёт быть «принятым никем конкретно»"
  - "Десять проверок с измеренными входными значениями — вход задачи 2"
affects: [05-verification-round-4, phase-06]

# Actuals (#2632)
actuals:
  tokens: 11200
  tasks: 1
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Указатель на перезаписываемый документ строится НЕ утверждением автора о его сегодняшнем содержимом, а парой «неизменяемый коммит на каждую версию + команда, отдающая полный перечень»: у такой конструкции нечему устареть при следующей перезаписи"
    - "Требование квалификации номера проверяется ИНВАРИАНТОМ (`число вхождений == число квалифицированных вхождений`), а не константой счёта: константа устаревает от первой перекрёстной ссылки и ещё раз от следующего раунда — тем же способом, каким устаревал чинимый указатель"
    - "Равенство подпирается проверками ПРИСУТСТВИЯ поимённо: `0 = 0` истинно, поэтому одно равенство удовлетворяется удалением обеих записей — то есть подменой закрытия дефекта стиранием его следа"
    - "Гейт по файлу-патчу ограничен строками записей (`^- 🔴`/`^- 🟡`), гейт по самому STATE.md идёт по всему файлу: пояснительная проза патча ОБЯЗАНА называть голый номер, объясняя правило, и без этой разницы правило самоаннулировалось бы"

key-files:
  created:
    - .planning/phases/05-tarify/05-STATE-PATCH-R3.md
  modified: []

key-decisions:
  - "Заголовок сводной записи правится с «по обоим раундам» на «по раундам 1 и 2»: раундов три, и «оба» — то же самое устаревающее утверждение, только в заголовке"
  - "Владелец `WR-07 (раунд 3)` оставлен видимым пробелом `⟨не назначен⟩`, а не подставлен наугад: «принято никем конкретно» — это диагноз ревью, и имя наугад закрыло бы запись, не закрыв находку"
  - "Цифры «Current Position» берутся из ROADMAP в момент применения, а не фиксируются патчем — то же правило, по которому реестр раундов заменил утверждение автора командой `git log`"
  - "Запись о CR-01 раунда 2 не переписана на «закрыто», а названа ПОЛОВИННОЙ с указанием плана, завершившего её: раунд 3 доказал, что закрытие само внесло регрессию"

patterns-established:
  - "Запись о закрытии называет функцию, вход правила, имена тестов и ФАКТИЧЕСКОЕ значение RED (`assert 'pro' == 'basic'`), а не слово «исправлено»: имя проверяется грепом, «исправлено» не проверяется ничем"
  - "Отклонения предыдущего плана читаются ДО написания записи о его закрытии: отклонение Rule 2 плана 05-13 (`normalize_utc` обоих операндов) добавило именованный тест, которого план не предусматривал, и запись обязана его называть"

requirements-completed: []

coverage:
  - id: E1
    description: "Реестр адресует все три раунда РАЗЛИЧНЫМИ неизменяемыми коммитами, включая новейший"
    requirement: BILL-07
    verification:
      - kind: other
        ref: "`grep -Eo 'git show (4989f7a|1d29360|164d210):' … | sort -u | wc -l` → **3** (порог 3)"
        status: pass
    human_judgment: false
  - id: E2
    description: "Перечень раундов, включая будущие, отдаёт команда, а не утверждение автора записи"
    requirement: BILL-07
    verification:
      - kind: other
        ref: "`grep -c 'git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md'` → **2** (порог >= 1)"
        status: pass
    human_judgment: false
  - id: E3
    description: "Все тринадцать находок раунда 3 несут распоряжение ВНУТРИ строки заголовка `код-ревью раунда 3`"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "`grep '🟡 \\[Phase 5, код-ревью раунда 3' … | grep -Eo 'CR-01 …|WR-0[1-7] …|IN-0[1-5] …' | sort -u | wc -l` → **13** (порог 13); перечислены поимённо в выводе"
        status: pass
    human_judgment: false
  - id: E4
    description: "Каждое вхождение `WR-07` в строках записей квалифицировано раундом — инвариант, а не константа"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "по `^- (🔴|🟡)`: голых `WR-07` — **3**, квалифицированных `WR-07 (раунд [0-9]+)` — **3**; равенство истинно"
        status: pass
    human_judgment: false
  - id: E5
    description: "Ни одна из двух записей `WR-07` не принесена в жертву счёту"
    requirement: BILL-06
    verification:
      - kind: other
        ref: "`grep -c 'WR-07 (раунд 2)'` → **4** (порог >= 1); `grep -c 'WR-07 (раунд 3)'` → **5** (порог >= 1)"
        status: pass
    human_judgment: false
  - id: E6
    description: "Закрытие CR-01 раунда 3 названо ИМЕНАМИ, сверенными по `05-13-SUMMARY.md`"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "`grep -c 'subscription_is_live'` → **3** (порог >= 1); `grep -c '05-13'` → **9** (порог >= 2)"
        status: pass
    human_judgment: false
  - id: E7
    description: "Ответ чекпойнта записан дословно, с идентификатором варианта"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "`grep -Ec 'apply-after-expiry|refuse-always'` → **2** (порог >= 1)"
        status: pass
    human_judgment: false
  - id: E8
    description: "Артефакт плана 05-12 не перезаписан; кода и требований план не касается"
    requirement: BILL-07
    verification:
      - kind: other
        ref: "`git diff --name-only -- .planning/phases/05-tarify/05-STATE-PATCH.md` → пусто; `-- .planning/REQUIREMENTS.md` → пусто; `-- app/ tests/ alembic/ pyproject.toml uv.lock` → пусто"
        status: pass
    human_judgment: false
  - id: E9
    description: "Десять проверок задачи 2 сошлись на `.planning/STATE.md` главного дерева"
    requirement: BILL-07
    verification: []
    human_judgment: true
    rationale: "Задача 2 — `checkpoint:human-action` `gate=blocking`, НЕ снята. Маршрут записи в главное дерево закрыт (см. «Исход применения»)"
  - id: E10
    description: "Читатель STATE.md, пришедший впервые, понимает состояние денежного пути за минуту и находит нужный раунд с первой попытки"
    requirement: BILL-07
    verification: []
    human_judgment: true
    rationale: "Backstop-пункт плана: понятность записи судится человеком, а не грепом"

# Metrics
duration: ~25min
completed: 2026-08-17
status: complete
---

# Phase 05 Plan 14: Реестр раундов ревью, которому нечему устареть, и распоряжения раунда 3 — Summary

**Указатель на раунды ревью перестал быть обязательством: вместо утверждения автора о том, какой раунд лежит на диске (устаревавшего трижды подряд), каждый раунд адресуется СВОИМ неизменяемым коммитом, а перечень всех раундов — включая ещё не написанные — отдаёт `git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md`. Все тринадцать находок раунда 3 получили распоряжение с номером в форме `<номер> (раунд 3)`, блокер денежного пути назван именами закрывших его функций и тестов, а root-equivalent доступ к хосту у веб-слоя перестал быть «принятым никем конкретно». Всё это записано В ФАЙЛ и закоммичено; применение к главному дереву снимает задача 2.**

## Состояние задач

| Задача | Тип | Состояние | Коммит |
|--------|-----|-----------|--------|
| Task 1: реестр раундов и распоряжения раунда 3 | `auto` | ✓ **complete** | `4bb2fe4` |
| Task 2: применить замену к STATE.md главного дерева и подтвердить проверками | `checkpoint:human-action`, `gate=blocking` | ✓ **complete** (оркестратором, см. ниже) | см. коммит STATE.md |

Задача 2 этим агентом НЕ закрывалась и закрыта быть не могла — см. «Исход применения»
ниже. Её снял ОРКЕСТРАТОР в главном рабочем каталоге после merge-back worktree — раздел
«ЗАДАЧА 2 — РАЗРЕШЕНИЕ ЧЕКПОЙНТА» в конце файла несёт фактические значения всех десяти
проверок и распоряжение владельца по несошедшейся первой.

## Первый пункт `missing:` закрыт СМЕНОЙ ФОРМЫ, а не повторной правкой значения

**Это отдельная строка намеренно, и без неё следующая верификация прочитает работу как
«уже сделано до вас».** Значение указателя уже поправил коммит `c440090` («point the
review-round entry at round 3») ДО начала этого плана — и именно поэтому правка значения
здесь не повторялась. Правка `c440090` воспроизвела ту же конструкцию, которая ломалась
дважды до неё: утверждение автора о том, какой раунд занимает файл сегодня. Раунд 4 сломал
бы её в четвёртый раз.

План заменил не значение, а **форму**. Сравнение по байтам:

| | Было (после `c440090`) | Стало |
|---|---|---|
| Новейший раунд | «на диске лежит раунд 3» — утверждение, ложное со следующей перезаписи | `git show 164d210:…` — неизменяемый коммит, наравне с прежними |
| Перечень раундов | закрытый список, который автор обязан дописывать | `git log --oneline -- …/05-REVIEW.md` — команда, отдающая и будущие раунды |
| Что устареет при раунде 4 | вся вводная часть записи | ничего |

Формулировка «на диске» уходит из файла целиком — проверка 3 задачи 2 (`grep -c 'на диске'`
→ 0) существует ровно для этого.

## Исход применения к настоящему дереву (шаг 9 задачи 1) — ОТКАЗАНО

**Это отдельная строка намеренно: отказ не подделывается и не изображается успехом.**

- **Разрешённый путь, названный полностью:** `/source/broadcaster/.planning/STATE.md` —
  главное рабочее дерево, вычисленное так, как предписал план:
  `git rev-parse --git-common-dir` → `/source/broadcaster/.git`, откуда корень главного
  дерева `/source/broadcaster`.
- **ЧТЕНИЕ по этому пути прошло.** Файл прочитан целиком и сверен: он БАЙТ В БАЙТ совпадает
  с копией в worktree на коммите `e6c14a3` — 125 строк, строка 91 без реестра по коммитам,
  распоряжений раунда 3 нет ни одного. То есть входные значения десяти проверок задачи 2,
  измеренные оркестратором, подтверждены независимо и на момент возврата остаются в силе.
- **ЗАПИСЬ по этому пути ОТКАЗАНА.** Попытка — правка раздела 1 патча (замена вводной части
  🟡 записи строки 91). Отказ пришёл от изоляции worktree, дословно:

  > `This agent is isolated in the worktree /source/broadcaster/.claude/worktrees/agent-a98eccb9318bdb956. Edit the worktree copy of this file instead of the shared-checkout path.`

- **Код отказа:** числового `errno` нет — это отказ уровня инструмента (`tool_use_error`),
  а не ошибка файловой системы. Записи не произошло ни на байт: `.planning/STATE.md`
  главного дерева остался в точности тем, каким был.
- **Попытки не тратились дальше.** Второй маршрут (перегенерация файла целиком через
  `Write`) не пробовался сознательно по двум причинам: он упёрся бы в ту же изоляцию, и он
  нарушил бы прямое правило патча «файл не перегенерируется целиком».
- **Ни одна git-команда против главного дерева не выполнялась** — ни `add`, ни `commit`,
  ни `status`.

**Это ЧЕТВЁРТЫЙ подряд воспроизведённый отказ этого маршрута** — планы `05-08` и `05-10`
объявили `.planning/STATE.md` в `files_modified` и не изменили его ни разу, план `05-12`
дошёл до цели только через тот же блокирующий гейт, и теперь `05-14`. Предсказание
`<flagged_assumptions>` плана подтвердилось буквально; устройство «твёрдый артефакт →
best-effort применение → блокирующий чекпойнт» отработало ровно так, как задумано:
**текст работы не потерян, потеряна только возможность применить его отсюда.**

## Task Commits

1. **Task 1 — патч написан и закоммичен:** `4bb2fe4`
   `docs(05-14): write the review-round ledger patch that outlives the next rewrite`
   — 1 файл, +331 строка (44 761 байт).
2. **Метаданные плана:** коммит этого SUMMARY.

`.planning/STATE.md` и `.planning/ROADMAP.md` этим агентом НЕ коммитились — режим worktree,
оркестратор пишет их централизованно после волны.

## Фактические значения ВСЕХ машинных критериев приёмки задачи 1

Каждая команда прогнана этим агентом по написанному файлу; записаны настоящие выводы, а не
ожидания.

| Критерий | Порог | Фактически | Итог |
|---|---|---|---|
| Файл `05-STATE-PATCH-R3.md` существует | есть | **есть**, 331 строка | ✓ |
| `grep -Eo 'git show (4989f7a\|1d29360\|164d210):' … \| sort -u \| wc -l` | == 3 | **3** | ✓ |
| `grep -c 'git log --oneline -- .planning/phases/05-tarify/05-REVIEW.md'` | >= 1 | **2** | ✓ |
| Находки раунда 3 ВНУТРИ строки заголовка, `sort -u \| wc -l` | == 13 | **13** | ✓ |
| Равенство квалификации по `^- (🔴\|🟡)`: голых `WR-07` vs `WR-07 (раунд [0-9]+)` | равны | **3 = 3** | ✓ |
| `grep -c 'WR-07 (раунд 2)'` | >= 1 | **4** | ✓ |
| `grep -c 'WR-07 (раунд 3)'` | >= 1 | **5** | ✓ |
| `grep -c 'subscription_is_live'` | >= 1 | **3** | ✓ |
| `grep -c '05-13'` | >= 2 | **9** | ✓ |
| `grep -Ec 'apply-after-expiry\|refuse-always'` | >= 1 | **2** | ✓ |
| `git diff --name-only -- .planning/phases/05-tarify/05-STATE-PATCH.md` | пусто | **пусто** | ✓ |
| `git diff --name-only -- .planning/REQUIREMENTS.md` | пусто | **пусто** | ✓ |
| `git diff --name-only -- app/ tests/ alembic/ pyproject.toml uv.lock` | пусто | **пусто** | ✓ |
| `git status --short` до коммита | только новый артефакт | **`?? 05-STATE-PATCH-R3.md`**, больше ничего | ✓ |

**Тринадцать номеров, извлечённых грепом из строки заголовка (не пересказ — вывод
команды):** `CR-01 (раунд 3)`, `IN-01 (раунд 3)`, `IN-02 (раунд 3)`, `IN-03 (раунд 3)`,
`IN-04 (раунд 3)`, `IN-05 (раунд 3)`, `WR-01 (раунд 3)`, `WR-02 (раунд 3)`,
`WR-03 (раунд 3)`, `WR-04 (раунд 3)`, `WR-05 (раунд 3)`, `WR-06 (раунд 3)`,
`WR-07 (раунд 3)`.

**Про равенство `3 = 3`.** Это ИНВАРИАНТ, а не константа: в строках записей патча `WR-07`
встречается трижды — заголовок 🔴 записи, перечень 🟡 записи и перекрёстная ссылка
распоряжения `WR-06 (раунд 3)` на отложенное распоряжение `WR-07 (раунд 2)`. Любая
константа величины («ровно 2») уже здесь была бы красной, а после раунда 4 отказала бы
снова — тем же способом, каким отказывал указатель, который этот план чинит. Счёт по ВСЕМУ
файлу патча даёт другие числа (пояснительная проза обязана называть голый номер, объясняя
само правило) — именно поэтому гейт патча ограничен строками `^- 🔴`/`^- 🟡`, а гейт самого
`STATE.md` идёт по всему файлу.

## Семь правок STATE.md, выписанных патчем

1. **§Blockers/Concerns, 🟡 сводная запись (строка 91)** — вводная часть заменена реестром
   раундов по коммитам плюс командой `git log`. Заголовок «по обоим раундам» → «по раундам
   1 и 2». Остаток строки от `**Закрыты планами:**` — не тронут ни на символ; пара
   WR-10 (раунд 1) / WR-07 (раунд 2) не разъединена.
2. **§Blockers/Concerns — новая 🔴 запись `WR-07 (раунд 3)`:** `docker-compose.prod.yml:91-92`
   монтирует `/var/run/docker.sock` в `web`, терминирующий `POST /api/billing/webhook`,
   чья аутентификация — IP из заголовка прокси и отключается одной переменной
   (`YOOKASSA_WEBHOOK_VERIFY_IP=false`). Названы три вещи, которые назвало ревью: находка
   ПРЕДШЕСТВУЕТ фазе 05, фаза УВЕЛИЧИЛА объём доходящего до этого процесса, режим —
   «принято никем конкретно». Распоряжение: отдельный воркер поверх очереди Redis либо
   фильтрующий прокси сокета; иначе — принятый риск с названным владельцем по образцу
   R-03-09. Существующая 🔴 запись о блокирующем вызове SDK НЕ снята.
3. **§Blockers/Concerns — новая 🟡 запись `[Phase 5, код-ревью раунда 3 — распоряжения]`:**
   тринадцать находок, каждая с квалифицированным номером, сутью и распоряжением; одной
   физической строкой ради бюджета файла.
4. **§Deferred Items, строка 119 (`CR-01` раунда 2):** закрытие названо ПОЛОВИННЫМ —
   подтверждено раундом 3 (шесть из семи must_haves плана 05-11 зелёные), седьмой
   фальсифицирован, регрессия внесена этим же закрытием и завершена планом 05-13. Оговорка
   о вердикте `gaps_found` СОХРАНЕНА: она оказалась пророческой.
5. **§Deferred Items — новая строка `CR-01` раунда 3:** закрыт планом `05-13` — именами.
6. **§Decisions, строка 80:** дописан исход на ИСТЁКШЕМ сроке — ответ владельца
   `apply-after-expiry` дословно, с идентификатором варианта и следствием для пользователя
   одной фразой. Новой записи не заведено: у одного решения одно место.
7. **«Current Position» + frontmatter `progress`:** приведены к ROADMAP (`13/14`, отсюда
   `65/66`), обе цифры, а не одна. Источником названа ROADMAP на момент применения, а не
   константа в патче — то же правило, что и в правке 1.

## Имена закрытия сверены по `05-13-SUMMARY.md`, а не по плану

Плановые имена могли сдвинуться при исполнении, поэтому запись §Deferred Items построена
на исполненных: `subscription_is_live` в `app/application/billing/subscription_period.py`
(а не рядом с правилом, как предлагало ревью — `plan_switch.py` держит AST-проверяемый
инвариант единственного импорта); обязательный keyword-only вход `period_is_live` правила
`switch_is_refused` без значения по умолчанию; `test_the_rule_cannot_be_asked_without_the_liveness_term`;
`test_the_liveness_is_sampled_before_the_date_moves` (AST-гейт `live=0 < move=1`);
`test_an_expired_period_lets_the_paid_plan_through_at_the_apply_stage` с фактическим RED
`assert 'pro' == 'basic'`; `test_the_expired_period_writes_no_preserved_plan_warning`;
суита 1665 → 1681.

**Оба отклонения плана 05-13 проверены на влияние на запись:**

- **Отклонение 1 (Rule 2, `normalize_utc` обоих операндов) — ВОШЛО в запись.** Оно
  добавило к закрытию именованный тест, которого план не предусматривал
  (`test_a_naive_now_does_not_break_the_liveness_term`), и закрыло реальную ловушку на
  денежном пути: naive-`now` поднял бы `TypeError`. Запись, умолчавшая бы о нём, описывала
  бы работу меньшую, чем исполненная.
- **Отклонение 2 (Rule 1, слово «истёк» только заглавными в докстринге) — в запись НЕ
  вошло.** Оно косметическое по отношению к предмету записи: поведение не менялось,
  имён не добавилось, порог теста не понижался.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 — недостающая критичная функциональность] Заголовок сводной записи правится вместе с её вводной частью**

- **Найдено при:** задаче 1, шаг 2 (замена ледовой части строки 91).
- **Что не так:** план предписал править «только вводное предложение записи, до слов
  `**Закрыты планами:**`». Но заголовок записи —
  `[Phase 5, код-ревью — сводное распоряжение по обоим раундам]` — стоит ПЕРЕД этим
  предложением и несёт ровно тот же дефект, который план чинит: «обоим» утверждает, что
  раундов два, а их три, и распоряжения раунда 3 уходят в отдельную запись. Оставленный
  как есть заголовок продолжал бы устаревать при каждом раунде — то есть план починил бы
  тело записи и оставил бы поломку в её шапке.
- **Исправление:** заголовок стал `[Phase 5, код-ревью — сводное распоряжение по раундам
  1 и 2]` — точное имя своего предмета, не зависящее от числа будущих раундов.
- **Проверено на самоаннулирование:** ни один гейт на этот заголовок не смотрит. Проверка
  тринадцати находок ищет `🟡 [Phase 5, код-ревью раунда 3` — другую строку; равенство
  `WR-07` считает по `^- 🔴`/`^- 🟡` и от текста заголовка не зависит.
- **Файл:** `.planning/phases/05-tarify/05-STATE-PATCH-R3.md`, раздел 1. **Коммит:** `4bb2fe4`.

**2. [Rule 2 — недостающая критичная функциональность] Цифры «Current Position» названы ИСТОЧНИКОМ, а не зафиксированы в патче**

- **Найдено при:** задаче 1, шаг 8.
- **Что не так:** план велел привести счётчик к ROADMAP и назвал ориентиры «планов фазы 14,
  планов milestone 66». ROADMAP на диске говорит `13/14` (план 05-14 ещё не засчитан
  исполненным), то есть `65/66`. Но патч применяется ПОЗЖЕ — возможно, уже после того, как
  оркестратор шагом 5.7 засчитает 05-14 и ROADMAP станет `14/14`. Константа в патче была бы
  верна ровно в один из двух моментов и ложна в другой — тот же класс дефекта, который весь
  этот план и чинит.
- **Исправление:** раздел 7 даёт готовый блок для сегодняшнего состояния ROADMAP (`13/14`,
  `65/66`) И правило: цифра берётся из ROADMAP в момент применения, с выписанным вторым
  вариантом (`14/14`, `66/66`, полная полоса) на случай, если ROADMAP уже обновлён.
- **Файл:** тот же, раздел 7.3. **Коммит:** `4bb2fe4`.

**3. [Rule 2 — недостающая критичная функциональность] Владелец `WR-07 (раунд 3)` оставлен видимым пробелом**

- **Найдено при:** задаче 1, шаг 3.
- **Что не так:** ревью требует записать находку «с владельцем», а владельца назначает
  человек. Подставить имя наугад значило бы закрыть ЗАПИСЬ, не закрыв находку, — притом что
  сегодняшний диагноз находки дословно «принято никем конкретно».
- **Исправление:** в записи стоит `Владелец: ⟨не назначен — назначает владелец проекта⟩` —
  видимый пробел, который нельзя пролистать. Отмечено в патче отдельным абзацем.
- **Файл:** тот же, раздел 2. **Коммит:** `4bb2fe4`.

**Итого отклонений:** три автоисправления, все Rule 2, все внутри единственного файла,
изменяемого планом. Объём не расширен; архитектурных решений (Rule 4) не потребовалось.
Ни одного отклонения класса Rule 1 (баг) и Rule 3 (блокирующая проблема) не встретилось.

### Границы объёма — соблюдены

Ни строки кода: `git diff --name-only -- app/ tests/ alembic/ pyproject.toml uv.lock` пуст.
Требования не перемечены: `git diff --name-only -- .planning/REQUIREMENTS.md` пуст —
`BILL-05`/`BILL-06`/`BILL-07` возвращает в `Complete` верификация, а не план, закрывающий
гэпы (прецедент отзыва — `2124d1e`). Артефакт плана 05-12 (`05-STATE-PATCH.md`) не
перезаписан: его дифф пуст. Ни одна находка раунда 3 не взята в работу — все получили
ЗАПИСЬ с распоряжением, ровно то, чего требует список `missing:`. Сокет Docker не
переносится и не проксируется: ревью само называет вынос работой своего размера.

## Issues Encountered

- **Маршрут записи в `.planning/STATE.md` главного дерева закрыт изоляцией worktree** — см.
  «Исход применения». Чтение по пути работает, запись отказана на уровне инструмента.
  Предсказано планом, отработано предусмотренным устройством, потерь текста нет.
- **Составные shell-команды с `&&` и подстановками отклоняются песочницей worktree**
  («too complex to verify that it stays inside the worktree»). Блок `<automated>` задачи 1
  — цепочка из четырёх `test`, соединённых `&&`, — целиком не прогонялся; вместо этого
  прогнан КАЖДЫЙ его конъюнкт по отдельности, и все четыре зелёные (существование файла;
  `3` различных коммита; равенство `3 = 3`; `13` находок). Логическое `И` четырёх истин —
  истина, но честнее записать, что прогонялись части, а не цепочка.

## Известные заглушки

Одна, намеренная и видимая: **владелец `WR-07 (раунд 3)` не назначен** —
`Владелец: ⟨не назначен — назначает владелец проекта⟩`. Это не недоделка исполнителя, а
предмет решения человека: назначение владельца риску severity high подделке не подлежит.
Пробел оставлен видимым специально, чтобы его нельзя было пролистать при применении патча.

Ни одного `TODO`/`FIXME`, ни одного пропущенного теста, ни одной невыполненной проверки
задачи 1 — все прогнаны с записанными фактическими значениями.

## Threat Flags

Новой поверхности не добавлено; план не тронул ни строки кода. Из `<threat_model>` плана:

- **T-05-69** (блокер денежного пути отсутствует в операционном документе) — митигация
  ЗАПИСАНА в патч (строка §Deferred Items с именами закрытия), ПРИМЕНЕНИЕ ждёт задачи 2.
- **T-05-70** (утечка секретов в трекинговый документ) — исполнено: записи называют ИМЕНА
  переменных (`YOOKASSA_WEBHOOK_VERIFY_IP`) и путь монтирования (`/var/run/docker.sock`),
  ни одного значения, ключа магазина, идентификатора или строки подключения.
- **T-05-71** (распоряжение уходит не той находке) — исполнено: тринадцать номеров
  квалифицированы раундом, инвариант равенства зелёный (`3 = 3`).
- **T-05-72** («закрытие» удалением записи) — исполнено: обе записи `WR-07` присутствуют
  поимённо, существующая 🔴 запись о вызове SDK не снята, счёт `^- 🔴` растёт с 1 до 2.
- **T-05-73** (root-equivalent доступ у веб-слоя) — `transfer` исполнен записью:
  распоряжение выписано, место владельца заведено; кодом не тронуто ничего.
- **T-05-74** (требование помечено `Complete` раньше вердикта) — исполнено: дифф
  `REQUIREMENTS.md` пуст.
- **T-05-SC** — ни одного пакета не установлено; дифф `pyproject.toml`/`uv.lock` пуст.

## Перенесённые человеческие проверки — задачами НЕ становились

Дословно из `<verification>` плана, ни один пункт не автоматизирован: мобильная ширина
375px (`behavior_unverified: 1` — браузерного и e2e-харнесса нет, разметка планом не
тронута ни на строку); настоящий платёж в тестовом магазине ЮKassa (D-26); первое
настоящее уведомление через боевой nginx после выката; статус «отклонён» у отменённого
платежа (D-27); читаемость формулировок «в обработке» и отказа оплаты; прохибиции
`BILL-05`/fairness, `BILL-05`/autonomy и `BILL-06`/transparency — все три остаются
`unresolved`/`judgment`.

## User Setup Required

Нет новых переменных окружения, ключей конфига, миграций и внешних настроек. Требуется
ровно одно человеческое действие — задача 2, ниже.

## Next Phase Readiness

- **Гэп 2 закрыт по существу в артефакте, но НЕ применён к операционному документу.** До
  снятия задачи 2 `.planning/STATE.md` по-прежнему не содержит ни одной находки раунда 3, а
  CR-01 раунда 3 по-прежнему не зафиксирован нигде, кроме `05-REVIEW.md`, отчёта верификации
  и этого патча.
- **Требования НЕ перемечены**, `requirements-completed` пуст — тем же основанием, что у
  планов 05-12 и 05-13: `Complete` возвращает повторная верификация.
- **Открытым остаётся:** владелец `WR-07 (раунд 3)`; блокирующий вызов SDK (`CR-02`
  раунда 2); типизированная запись `PLAN_LIMITS` (WR-10 раунда 1 / `WR-07 (раунд 2)` /
  `WR-06 (раунд 3)`); одиннадцать прочих находок раунда 3, записанных с распоряжением, но
  не исполненных; очередь невыкаченных ревизий `0013`…`0018` при боевой базе на `0012`.
- **STATE.md и ROADMAP.md этим агентом НЕ трогались** — режим worktree, оркестратор пишет
  их централизованно после волны.

## ✓ ЗАДАЧА 2 — РАЗРЕШЕНИЕ ЧЕКПОЙНТА (2026-08-17, оркестратор + владелец)

Патч применён к `.planning/STATE.md` ГЛАВНОГО рабочего каталога (`/source/broadcaster`)
оркестратором после merge-back worktree — разделами, в порядке 1 → 7. Ни один раздел, не
названный патчем, не тронут; файл не перегенерировался.

### Фактические значения всех десяти проверок

| # | Проверка | Вход | Порог | Факт | Исход |
|---|----------|------|-------|------|-------|
| 1 | вхождений `git show <commit>:` | 2 | 3 | **4** (различных — **3**) | ⚠ см. ниже |
| 2 | `git log --oneline -- …/05-REVIEW.md` | 0 | >= 1 | 1 | ✓ |
| 3 | `'на диске'` | 1 | 0 | 0 | ✓ |
| 4 | находки раунда 3 в 🟡 записи | 0 | 13 | 13 | ✓ |
| 5 | равенство `WR-07` = квалифицированных | 1 = 1 | истинно | **4 = 4** | ✓ |
| 6 | `WR-07 (раунд 2)` / `WR-07 (раунд 3)` | 1 / 0 | >= 1 / >= 1 | 2 / 2 | ✓ |
| 7 | `subscription_is_live\|05-13` | 0 | >= 2 | 5 | ✓ |
| 8 | строк `^- 🔴` | 1 | >= 2 | 2 | ✓ |
| 9 | `wc -l` | 125 | <= 145 | 128 | ✓ |
| 10 | дифф `.planning/REQUIREMENTS.md` | пусто | пусто | пусто | ✓ |

### Проверка 1 — фактическое значение и распоряжение владельца

`grep -Eo 'git show (4989f7a|1d29360|164d210):' .planning/STATE.md | wc -l` → **4**, тогда
как строка таблицы требовала ровно `3`. РАЗЛИЧНЫХ коммитов — **3**: `4989f7a`, `1d29360`,
`164d210`, все три названы, новейший наравне с прежними, то есть инвариант, ради которого
проверка существует, ВЫПОЛНЕН. Четвёртое вхождение — `git show 164d210:…` внутри 🟡 записи
распоряжений раунда 3 (строка 93), и его туда предписал сам патч разделом 3: запись
открывается командой чтения своего раунда.

Причина отказа названа прямо, потому что она поучительна: **проверка была сформулирована
КОНСТАНТОЙ ВЕЛИЧИНЫ, а не инвариантом, и первая же перекрёстная ссылка её обнулила — ровно
тот способ отказа, который этот план чинит в самом указателе на раунды.** Равноценный
критерий задачи 1 для файла патча с самого начала написан через `sort -u` именно по этой
причине («Считаются РАЗЛИЧНЫЕ коммиты, а не вхождения»); строка 1 таблицы для `STATE.md`
этот `sort -u` потеряла.

**Распоряжение владельца на чекпойнте: принять инвариант и починить ФОРМУ проверки.**
Строка 1 таблицы в `05-STATE-PATCH-R3.md` приведена к `… | sort -u | wc -l`, туда же
внесена датированная поправка с объяснением. Вариант «убрать перекрёстную ссылку, чтобы
счёт стал 3» ОТКЛОНЁН: это зеленение гейта удалением содержания — тот самый класс подмены,
который патч запрещает для `WR-07`, и читатель потерял бы маршрут к тексту раунда 3 из
самой записи его распоряжений.

### Сигнал возобновления

> применено — девять проверок сошлись; проверка 1 дала 4 вхождения при 3 различных
> коммитах, форма проверки исправлена на `sort -u`, содержание не сокращалось

---

## Исходное задание чекпойнта (сохранено как есть)

**Тип:** `checkpoint:human-action`, **`gate=blocking`**

**Что нужно сделать:** применить
`.planning/phases/05-tarify/05-STATE-PATCH-R3.md` к `.planning/STATE.md` в **ГЛАВНОМ**
рабочем каталоге (`/source/broadcaster`), разделами, в порядке 1 → 7, и подтвердить
десятью проверками. Патч написан так, чтобы применяющий не читал план: в каждом разделе
выписано, что удаляется (с номером строки) и что встаёт вместо этого, дословно, готовым к
копированию.

**Проверить перед началом:** `git rev-parse --git-dir` обязан отдать `.git`, а не путь
внутрь `.git/worktrees/…`.

**Десять проверок с измеренными входными значениями** — таблица в конце
`05-STATE-PATCH-R3.md`. Семь из них на входе КРАСНЫЕ (реестр коммитов 2 → 3; `git log`
0 → >= 1; «на диске» 1 → 0; находки раунда 3 0 → 13; `WR-07 (раунд 3)` 0 → >= 1; имена
закрытия 0 → >= 2; строки `^- 🔴` 1 → >= 2), три защитные и зелёные на входе (равенство
квалификации `1 = 1`; бюджет 125 <= 145; пустой дифф требований) — они ловят не
невыполнение работы, а её выполнение НЕ ТЕМ способом.

**Чего делать нельзя:** приводить счёт вхождений `WR-07` к прежнему значению удалением
записи. За номером стоят ДВЕ разные находки — `WR-07 (раунд 2)` (негодный формат
`PLAN_LIMITS`) и `WR-07 (раунд 3)` (сокет Docker у веб-слоя). Если проверка равенства не
сходится, ответ — **дописать раунд к голому номеру, а не удалить вхождение**.

**Коммит после применения:**
`docs(05): make the review-round ledger outlive the next rewrite`.

**Сигнал возобновления (ДОСЛОВНО из плана):**

> Введите «применено», если все десять проверок сошлись, либо назовите проверку, которая
> не сошлась, и её фактическое значение

**Если проверка не сошлась — план НЕ закрывается:** записывается фактическое значение и
причина, волна остаётся незавершённой.

## Self-Check: PASSED

Файлы (1/1 на месте, закоммичен):
- FOUND: `.planning/phases/05-tarify/05-STATE-PATCH-R3.md` (331 строка, 44 761 байт)

Коммиты (в истории ветки `worktree-agent-a98eccb9318bdb956`):
- FOUND: `4bb2fe4` — `docs(05-14): write the review-round ledger patch that outlives the next rewrite`

Не изменено (проверено пустым диффом): `.planning/phases/05-tarify/05-STATE-PATCH.md`,
`.planning/REQUIREMENTS.md`, `app/`, `tests/`, `alembic/`, `pyproject.toml`, `uv.lock`.
Удалённых файлов в коммите нет (`git diff --diff-filter=D HEAD~1 HEAD` — пусто).

Фаза `complete` этим агентом НЕ помечена, верификатор НЕ запускался, задача 2 НЕ закрыта:
гейты фазы и чекпойнт принадлежат оркестратору и человеку.

---
*Phase: 05-tarify*
*Task 1 completed: 2026-08-17 — Task 2 awaiting checkpoint: human-action*
