---
phase: 05-tarify
plan: 26
subsystem: planning-docs
tags: [decisions, prohibitions, code-review-dispositions, coverage, yookassa]

requires:
  - phase: 05-22
    provides: "ответ владельца `cap-one-month` и `capped_carryover` в ветке отката — материал решения D-31"
  - phase: 05-23
    provides: "опровергнутая прогоном премисса `IN-03` — материал распоряжения по находке"
  - phase: 05-24
    provides: "ответ владельца `record-wins`, ключ `subscription_plan_downgraded`, долг BILL-05 — материал решения D-32"
  - phase: 05-25
    provides: "машинный гейт на кодовую половину класса «объявление против кода» — кодовая половина решения D-33"
provides:
  - "D-31 — форма верхней границы переноса в ветке отката, записанная своим номером с ценой в числах"
  - "D-32 — разрешение конфликта `upgrade-only` × D-28, записанное своим номером с двойным принятым долгом"
  - "D-33 — тир прохибиций класса «объявление против кода»: машинный гейт коду, честный judgment документам"
  - "распоряжения по всем двенадцати находкам код-ревью раунда 6, каждое квалифицировано строкой «(раунд 6)»"
  - "девять пунктов записанного долга фазы, названные поимённо в §Deferred Items"
  - "раздел сверки `COVERAGE.md` после раунда 6 — матрица проверена, а не перегенерирована"
affects: [верификация раунда 7]

actuals:
  tokens: 25286
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Решение владельца возвращается в `05-CONTEXT.md` СВОИМ номером; SUMMARY плана, снявшего чекпойнт, номера не присваивает"
    - "Пара чисел одной величины сводится в сторону КОДА: остаётся то число, которое несёт докстринг"
    - "Тир прохибиции описывает существующее принуждение; при его отсутствии тир понижается с названной причиной, а не объявляется пятый раз"

key-files:
  created:
    - .planning/phases/05-tarify/05-26-SUMMARY.md
  modified:
    - .planning/phases/05-tarify/05-CONTEXT.md
    - .planning/STATE.md
    - .planning/phases/05-tarify/COVERAGE.md

key-decisions:
  - "D-31 (`cap-one-month`) — верхняя граница переноса в ветке отката: один календарный месяц. Цена названа числом: 22 780 ₽ дают 60 дней Pro вместо 396"
  - "D-32 (`record-wins`) — исполняется записанный факт сделки; `upgrade-only` перестаёт быть инвариантом обеих стадий и становится правилом стадии НАМЕРЕНИЯ"
  - "D-33 — граница между половинами класса «объявление против кода» проведена по ПРЕДМЕТУ: код получает машинный гейт, документы — честный judgment-tier"
  - "Обе пары чисел одной величины сведены в сторону кода: `395`/`396` и `≈141`/`140` — оставлено число раунда 6, потому что его же несут докстринг `capped_carryover` и комментарий ветки отката"
  - "`IN-03 (раунд 6)` записан как ОПРОВЕРГНУТЫЙ ПРОГОНОМ, а не закрытый как написано: ветка несущая, повторное требование её снять адресуется владельцу"

patterns-established:
  - "Девятый пункт долга (неуникальная нумерация угроз) разобран на два рода — столкновения и законный перенос, — потому что распоряжение у них общее, а природа разная"
  - "§Current Position и frontmatter `progress` не трогаются исполнителем в worktree: они принадлежат оркестратору волны"

requirements-completed: []

coverage:
  - id: D1
    description: "Оба решения владельца, снятые чекпойнтами волны 21, записаны в `05-CONTEXT.md` своими номерами по образцу D-28/D-29/D-30"
    requirement: BILL-05
    verification:
      - kind: manual
        ref: "grep -cE '^- \\*\\*D-3[1-9]:' .planning/phases/05-tarify/05-CONTEXT.md → 3"
        status: pass
    human_judgment: false
  - id: D2
    description: "Записанная цена D-30 и то, что отдаёт код в ветке отката, сведены; журнальный ключ назван верно"
    requirement: BILL-07
    verification:
      - kind: manual
        ref: "grep -c '396' → 2; grep -c '22 780' → 2; grep -c 'subscription_prorating_skipped' → 1; grep -c '395' → 0; grep -c '141' → 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "Все двенадцать находок раунда 6 несут квалифицированные раундом распоряжения, разнесённые на три группы"
    requirement: BILL-07
    verification:
      - kind: manual
        ref: "grep -oE '(CR|WR|IN)-0[0-9] \\(раунд 6\\)' .planning/STATE.md | sort | uniq -c → двенадцать номеров по одному разу"
        status: pass
    human_judgment: false
  - id: D4
    description: "Матрица `COVERAGE.md` сверена после раунда 6 и не перегенерирована"
    requirement: BILL-07
    verification:
      - kind: manual
        ref: "git diff -- COVERAGE.md | grep -c '^-[^-]' → 0; блок таблицы решений (строки 18-41) — 17 строк и до, и после"
        status: pass
    human_judgment: false
  - id: D5
    description: "Человек, читающий `05-CONTEXT.md` и §Blockers/Concerns после этой волны, понимает, что решено о деньгах и почему, не открывая ни одного отчёта верификации"
    verification: []
    human_judgment: true
    rationale: "Backstop-строка `must_haves`: понятность и неустареваемость судятся человеком, а не грепом"

duration: 40min
completed: 2026-08-18
status: complete
---

# Фаза 05 План 26: операционная половина раунда 6 — Summary

**Три решения владельца получили свои номера, записанная цена D-30 сведена с кодом в двух местах и по обеим парам чисел, двенадцать находок код-ревью раунда 6 получили квалифицированные раундом распоряжения, девять пунктов записанного долга названы поимённо, а тир документных прохибиций переведён в judgment честно — вместо пятого необеспеченного объявления `verification: test`.**

## Performance

- **Duration:** ~40 мин
- **Tasks:** 3
- **Files modified:** 3
- **Ни одной строки кода:** `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh pyproject.toml uv.lock` пуст на каждой из трёх задач

## Замеры, снятые ДО правки

План требовал повторить замер до правки, а не после. Фактический вывод совпал с ожидаемым по всем четырём командам:

| команда | ожидалось | фактически |
|---|---|---|
| `grep -c '≈141' .planning/phases/05-tarify/05-CONTEXT.md` | `1` | `1` |
| `grep -c '141' .planning/phases/05-tarify/05-CONTEXT.md` | `1` | `1` |
| `grep -n '140 дней Pro' .planning/phases/05-tarify/05-VERIFICATION.md` | строки `19`, `135`, `149`, `252` | строки `19`, `135`, `149`, `252` (счёт `4`) |
| `grep -n '140' .planning/phases/05-tarify/05-VERIFICATION.md` | те же четыре плюс `28`, `178`, `308` | те же четыре плюс `28`, `178`, `308` (счёт `7`) |

Дополнительно снято до правки: `grep -c '395' 05-CONTEXT.md` → `1`, `grep -c 'Reversibility'` → `4`, `grep -cE '^- \*\*D-[0-9]+:'` → `29`.

## Задача 1 — три решения своими номерами и запись D-30, сведённая с кодом

**Коммит `7936b1c`.**

### Выбранное состояние по каждой паре чисел

**Пара ветки ОТКАТА (`395` против `396`) — сведена ОДНИМ ЧИСЛОМ.** Выбрано состояние «одно число на пару»: `grep -c '395' .planning/phases/05-tarify/05-CONTEXT.md` → `0`, `grep -c '396'` → `2`. Прежнее число снято как измерение той же величины другого дня, и запись говорит об этом словами:

> «Первая редакция называла прежнюю выдачу ветки ОТКАТА числом на сутки меньшим — это та же величина, снятая прогоном раунда 5 от СВОЕГО сегодняшнего дня, тогда как прогон раунда 6 мерил от своего; сутки разницы даёт календарная длина месяцев между двумя датами отсчёта, а не второй дефект.»

**Пара ветки КОНВЕРСИИ (`≈141` против `140`) — сведена ОДНИМ ЧИСЛОМ тем же проходом.** `grep -c '141'` → `0`, `grep -c '≈141'` → `0`. Оценка на бумаге заменена измерением, и запись говорит об этом словами:

> «Первая редакция называла выдачу ветки КОНВЕРСИИ оценкой на бумаге со знаком «≈» и прогона рядом с собой не называла; отчёт раунда 6 предъявил ту же величину ВЫВОДОМ ПРОГОНА (`05-VERIFICATION.md:178`, строка «days of pro from now: 140») и журнальными числами (`remainder_days=365 → converted_days=110`), поэтому оценка заменена измерением.»

**Направление сведения выбрано не произвольно, и причина записана:** в обоих случаях оставлено число раунда 6, потому что его же несут докстринг `capped_carryover` (`app/application/billing/subscription_period.py:352-354`) и комментарий ветки отката (`app/services/payment_service.py:1236-1238`). Расхождение записи с кодом закрывается в сторону кода, а не наоборот.

### Журнальный ключ отката

Проверено чтением живого дерева, а не по отчёту:

- ветка ОТКАТА (`if price_from is None or price_to is None`) пишет `subscription_prorating_skipped` — `app/services/payment_service.py:1224`, поле `stage="convert_remainder"` на `:1230`;
- ветка КОНВЕРСИИ (`else`) пишет `subscription_remainder_converted` — `:1268`;
- ветка ОТКАЗА пишет тот же `subscription_prorating_skipped` с `stage="prorate_refused"` — `:1130`, `:1143`.

Запись D-30 теперь цитирует ключ верно, а прежнее название не стёрто, а названо ошибкой записи с указанием раунда и совпадения с `IN-04 (раунд 6)`. Рядом сказано, в какой ветке пишется второй ключ и что различает две ветки теперь ПОЛЕ `stage`, добавленное планом `05-22`, а не ключ. `grep -c 'subscription_prorating_skipped' .planning/phases/05-tarify/05-CONTEXT.md` → `1`.

### Цена ветки отката, названная числом

Пятый пункт `missing:` гэпа 1 закрыт дословно: в записи D-30 присутствуют числа `396`, `22 780` и `63 700`, названы ОБА входа в ветку (`price_to is None` — достижим одной правкой `.env`; `price_from is None` — `free` попадает сюда по построению), сказано, что ни один тест ветку не проверял, и названа форма границы, которую выбрал владелец, с новой величиной: 60 дней Pro, два календарных месяца, прейскурант 9 800 ₽.

### Сверка идентификаторов с SUMMARY планов

| решение | идентификатор в записи | идентификатор в SUMMARY плана |
|---|---|---|
| D-31 | `cap-one-month` | `cap-one-month` (`05-22-SUMMARY.md`, «Ответ дословно») |
| D-32 | `record-wins` | `record-wins` (`05-24-SUMMARY.md`, «Ответ владельца по чекпойнту задачи 1 (дословно)») |

Ветвь `upgrade-only-wins` НЕ выбрана, поэтому утверждения о сужении D-28 в записи нет — вместо него стоит прямое утверждение обратного: «Решение D-28 этим НЕ сужено и НЕ переоткрыто, и его запись не правится: сужается объявление `upgrade-only`, а не D-28». Это исполнение ключевой связи плана: решение, отменённое молча, есть та же потеря записи, что стёртая запись.

### Критерии приёмки задачи 1

| критерий | проверка | результат |
|---|---|---|
| `<verify>` | `test "$(grep -cE '^- \*\*D-3[1-9]:' 05-CONTEXT.md)" -ge 3` | `3` → код `0` |
| Части образца | `grep -c 'Reversibility'` | `4` → `7` (+3 ровно) |
| Журнальный ключ | `grep -c 'subscription_prorating_skipped'` | `1` |
| Цена числом | `grep -c '396'` | `2` (≥ 1) |
| Пара отката сведена | `grep -c '395'` | `0` |
| Пара конверсии сведена | `grep -c '141'` | `0` |
| Ни одна запись не стёрта | `grep -cE '^- \*\*D-[0-9]+:'` | `29` → `32` (+3) |
| `05-18-SUMMARY.md` не тронут | `git diff --name-only -- …05-18-SUMMARY.md` | пусто |
| Ни строки кода | `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` | пусто |

## Задача 2 — двенадцать распоряжений, решение о тире и девять пунктов долга

**Коммит `d9d9478`.**

### Двенадцать находок раунда 6

`grep -oE '(CR|WR|IN)-0[0-9] \(раунд 6\)' .planning/STATE.md | sort | uniq -c` — двенадцать номеров, каждый ровно по разу:

```
1 CR-01 (раунд 6)   1 WR-01 (раунд 6)   1 IN-01 (раунд 6)
1 WR-02 (раунд 6)   1 WR-03 (раунд 6)   1 IN-02 (раунд 6)
1 WR-04 (раунд 6)   1 WR-05 (раунд 6)   1 IN-03 (раунд 6)
1 WR-06 (раунд 6)   1 WR-07 (раунд 6)   1 IN-04 (раунд 6)
```

**Ни один номер не назван голым.** Проверено по телу самой записи, а не по всему файлу (в старых записях живут номера прежних раундов со своей квалификацией): в блоке раунда 6 встречается шестнадцать номеров находок, и все шестнадцать квалифицированы — двенадцать строкой `(раунд 6)`, четыре перекрёстные ссылки своими раундами (`CR-01 (раунд 5)`, `CR-02 (раунд 5)`, `IN-01 (раунд 5)`, `WR-07 (раунд 3)`). Голых — ноль.

**Три группы, сумма равна двенадцати (7 + 4 + 1):**

| группа | номера | счёт |
|---|---|---|
| закрыты планами волн 18-20 | `CR-01`, `WR-01`, `WR-02`, `WR-03`, `IN-01`, `IN-03`, `IN-04` | 7 |
| отложены с обоснованием, владелец-решение назван | `WR-04`, `WR-05`, `WR-06`, `WR-07` | 4 |
| записана без требования кода | `IN-02` | 1 |

**`IN-03 (раунд 6)` записан не как закрытый, а как ОПРОВЕРГНУТЫЙ ПРОГОНОМ** — это прямое требование волны. Запись называет: премисса «ветка недостижима, потому что `countdown_base` уже вернул `now_utc`» ложна (возврат `now_utc` не обходит ветку, а УДОВЛЕТВОРЯЕТ её условие); снятие ветки меняет ответ с «сегодня» на «сегодня плюс сутки» и покупает один день тарифа, за который не заплачено; ветка НЕСУЩАЯ; взят второй путь, названный самой находкой (`if base == now_utc` плюс комментарий). Распоряжение записано прямо: **находка требует ПЕРЕФОРМУЛИРОВКИ, и повторное требование «снять ветку» есть требование правки ПОВЕДЕНИЯ денежного пути, адресуемое владельцу, а не исполнителю.** Невыполненный критерий приёмки плана `05-23` («вхождений `return now_utc` — `0`») назван невыполнимым, а не замолчан.

### Формулировки о денежном пути совпадают с кодом — две пары

**Пара 1 — форма границы (D-31).**

| источник | текст |
|---|---|
| Документ, `.planning/STATE.md` §Decisions | «Верхней границей переноса объявлен ОДИН календарный месяц остатка: позднейшая из действующего срока и сегодняшнего дня, зажатая месяцем от сегодня» |
| Докстринг, `app/application/billing/subscription_period.py:337-338` | «Верхней границей переноса объявлен ОДИН календарный месяц остатка: позднейшая из действующего срока и сегодняшнего дня, зажатая месяцем от сегодня.» |

**Пара 2 — статус объявления `upgrade-only` (D-32).**

| источник | текст |
|---|---|
| Документ, `.planning/STATE.md` §Decisions | «`upgrade-only` есть правило стадии НАМЕРЕНИЯ: оно определяет, что ПРОДАЁТСЯ, а не то, чем кончается любой подтверждённый платёж» |
| Докстринг, `app/application/billing/plan_switch.py:96-97` | «То есть `upgrade-only` есть правило стадии НАМЕРЕНИЯ: оно определяет, что ПРОДАЁТСЯ, а не то, чем кончается любой подтверждённый платёж.» |

Обе формулировки взяты ДОСЛОВНО, а не пересказаны — расхождение двух формулировок одного утверждения и есть класс, за который фаза получила три раунда подряд.

### Решение о тире (D-33) и девять пунктов долга

Запись §Deferred Items ссылается на номер D-33 и называет явно: документные прохибиции планов `05-16`, `05-19`, `05-20`, `05-21` переведены в judgment-tier; кодовая половина осталась `test` и получила настоящий тест (`tests/test_application/test_declared_invariants.py`, план `05-25`, храповик на `37` записей, `14` из них машинно-проверяемые); граница проведена по ПРЕДМЕТУ. Отдельным предложением записано, что перевод тира НЕ снимает прохибицию — она остаётся `unresolved` и продолжает судиться человеком.

**Девять пунктов записанного долга** — по строке на пункт, каждая со своим распоряжением: (1) второе окно потолка намерений, (2) частичный уникальный индекс, (3) невыкаченная `0018`, (4) D-26, (5) D-08/BILL-02, (6) D-27, (7) отсутствие RED-коммитов у `05-15` и `05-17`, (8) мобильная проверка 375px, (9) неуникальная нумерация угроз.

**Пункт 1 несёт ВТОРОЕ следствие, вскрытое раундом 6,** — через окно срока давности достижимо изменение тарифа вниз, — и говорит, чем оно закрыто (ВИДИМОСТЬЮ: ключ `subscription_plan_downgraded`, ответ `record-wins`) и чем НЕ закрыто (само окно остаётся открытым; карточка продолжает обещать переход после окончания срока).

**Пункт 9 — вывод команды поиска дублей дословно:**

```
$ grep -ohE '^\| T-05-[0-9]+ \|' .planning/phases/05-tarify/05-*-PLAN.md | tr -d '| ' | sort | uniq -d
T-05-30
T-05-31
T-05-32
T-05-33
T-05-34
T-05-35
T-05-51
```

Разобран на два рода: шесть настоящих столкновений и один законный перенос (`T-05-51` — одна и та же угроза, перенесённая планами `05-11`, `05-13` и `05-15` из `05-10`; подтверждено чтением всех четырёх строк). Распоряжение по обоим родам одно и записано прямо: **исполненные планы НЕ ПРАВЯТСЯ.** Названо следствие (номер перестаёт адресовать угрозу однозначно) и то, чего следствием НЕ является (ни одна мера защиты не теряется — у каждой строки своя мера на месте). `grep -c 'T-05-51' .planning/STATE.md` → `1`, то же для `T-05-30`…`T-05-35`.

**Владелец риска `WR-07 (раунд 3)`** назван фактом, а не пропущен: шестой раунд без имени, распоряжение записано и проверено, не хватает человека.

### Критерии приёмки задачи 2

| критерий | проверка | результат |
|---|---|---|
| `<verify>` | `test "$(grep -c 'раунд 6' .planning/STATE.md)" -ge 12` | `13` → код `0` |
| Все двенадцать номеров | `grep -oE '(CR\|WR\|IN)-0[0-9] \(раунд 6\)' \| sort \| uniq -c` | двенадцать по одному разу |
| Ни одного голого номера | тот же греп по блоку раунда 6 | `16` вхождений, `16` квалификаций, голых `0` |
| Сумма трёх групп | подсчёт | `7 + 4 + 1 = 12` |
| Решение о тире | `grep -c 'judgment' .planning/STATE.md` | `4` (≥ 1) |
| §Current Position без констант величины | `grep -cE 'Plan: [0-9]+ of [0-9]+\|[0-9]+/[0-9]+ планов'` | `0` |
| Прежние записи не стёрты | `grep -c 'раунд 5'` до/после | `9` → `13` |
| Ни строки кода | `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` | пусто |

## Задача 3 — `COVERAGE.md` сверена, а не перегенерирована

**Коммит `4329b1f`.**

**Факт установлен грепом.** Фактический вывод `grep -rn "YooPayment\.\|SecurityHelper" app/` — шесть строк, из которых обращений к SDK три:

```
app/routes/billing.py:4      from yookassa.domain.common.security_helper import SecurityHelper   (импорт)
app/routes/billing.py:109    …берутся ИЗ SDK (`SecurityHelper`)…                                 (докстринг)
app/routes/billing.py:121    return SecurityHelper().is_ip_trusted(ip)                           (обращение)
app/routes/billing.py:146    …у `SecurityHelper` ровно два…                                      (докстринг)
app/services/payment_service.py:298  # `YooPayment.create`. Отказ, принятый ПОСЛЕ…               (комментарий)
app/services/payment_service.py:365  payment = YooPayment.create(                                (обращение)
```

Третье обращение — конфигурация создания (`"type": "redirect"` на `:369`, `"capture": True` на `:372`). **Единственным вызовом SDK остался тот же `YooPayment.create`**, и это записано ПРЯМО, а не оставлено выводиться из молчания. `KNOWN_EVENTS` по-прежнему содержит ровно два события (`:67-72`). Причина названа: планы волны меняли стадию ПРИМЕНЕНИЯ подтверждённого платежа и объявления о ней, а не обращения к API.

**Две строки пересмотрены явно.** `payments.find_one` — `OPT-OUT` в силе, причина дополнена вторым основанием из `WR-04 (раунд 6)`. `payments.cancel` — `OPT-OUT` в силе, причина дополнена ВТОРЫМ СЛЕДСТВИЕМ окна срока давности. Цитата из раздела:

> «Раунд 6 добавил к цене этого окна ВТОРОЕ СЛЕДСТВИЕ, и оно записывается здесь, хотя решение по строке не меняется: через окно срока давности достижимо изменение ДЕЙСТВУЮЩЕГО ТАРИФА ВНИЗ при живом старшем сроке (`WR-03 (раунд 6)`, воспроизведено прогоном).»

**Незатронутые строки названы незатронутыми прямо** формулировкой образца плана `05-21`; обе прежние оговорки (D-27 и D-26) остаются в силе дословно. Раздел заканчивается прямым утверждением: решение не изменилось ни по одной строке, матрица остаётся действительной без правок.

### Критерии приёмки задачи 3

| критерий | проверка | результат |
|---|---|---|
| `<verify>` | `test "$(grep -c 'раунда 6' COVERAGE.md)" -ge 1` | `4` → код `0` |
| `find_one` пересмотрен | `grep -c 'find_one'` до/после | `5` → `8` |
| `cancel` пересмотрен | `grep -c 'cancel'` до/после | `13` → `20` |
| Прежние разделы не стёрты | `git diff -- COVERAGE.md \| grep -c '^-[^-]'` | `0` |
| Матрица не перегенерирована | строки таблицы решений (`18-41`) до/после | `17` → `17` |
| Ни строки кода | `git diff --name-only -- app/ tests/ alembic/ entrypoint.sh` | пусто |

**Изменение счёта `grep -c '^| \`'` по ВСЕМУ файлу названо поимённо, а не замолчано:** `24` → `27`. Все три новые строки принадлежат таблице обращений к SDK нового раздела сверки (`YooPayment.create`, конфигурация создания, `SecurityHelper().is_ip_trusted`) — той же формы, что таблица раздела сверки после раунда 5. **Ни одна строка таблицы РЕШЕНИЙ не добавлена и не удалена:** проверено отдельно по блоку `18-41` — `17` и до, и после.

## Deviations from Plan

### 1. [Правило объёма волны] §Current Position и frontmatter `progress` НЕ правились

- **Найдено:** до задачи 2, при чтении объёма волны.
- **Что случилось:** задача 2 плана требовала привести §Current Position и `frontmatter.progress` к исполненному состоянию и показать расчёт в SUMMARY. Объём этой волны прямо запрещает исполнителю в worktree трогать учётную бухгалтерию оркестратора — статус фазы, счётчики прогресса, счёт планов, «Current Position», — потому что эти поля пишутся централизованно после слияния волны, и правка из worktree была бы либо перезаписана, либо дала бы конфликт.
- **Распоряжение:** правка не сделана; поля остаются оркестратору. **Свойство, которое требовал критерий приёмки, при этом проверено и держится:** `grep -cE 'Plan: [0-9]+ of [0-9]+|[0-9]+/[0-9]+ планов' .planning/STATE.md` → `0`. Запрет плана `05-21` соблюдён и этой волной — просто не правкой, а невмешательством. Содержательная часть, которую план законно поручал этому файлу (распоряжения, решения, долг), записана полностью.
- **Расчёт для оркестратора приводится здесь, чтобы он не терялся:** `ROADMAP.md` §«Phase 5: Тарифы» после этого плана содержит `26` планов, все с отметкой исполнения; строка сводной таблицы прогресса и поле `frontmatter.progress` считаются по нему.

### 2. [Rule 1 — Bug в примере плана] Ориентация образца `T-05-30` в плане перевёрнута относительно дерева

- **Найдено:** при исполнении задачи 2, установлением факта грепом.
- **Issue:** план называет образец так: «`T-05-30` в `05-05` — молча обрезанный потолком список платежей, а в `05-07` — подделка адреса пира в гарде уведомления». Дерево отдаёт обратное: в `05-05` под этим номером стоит `Spoofing` / `_webhook_client_ip` (подделка адреса пира, critical), в `05-07` — `Repudiation` / молча обрезанный список платежей (medium).
- **Fix:** в `.planning/STATE.md` записана УСТАНОВЛЕННАЯ ориентация, а не процитированная из плана. Сам факт столкновения (шесть настоящих дублей плюс один законный перенос) планом назван верно и подтверждён.
- **Files modified:** `.planning/STATE.md`

### 3. [Уточнение счёта] `Gaps Summary` раунда 6 называет СЕМЬ пунктов долга, а не восемь

- **Найдено:** при исполнении задачи 2, чтением `05-VERIFICATION.md:308-314`.
- **Issue:** план говорит «`Gaps Summary` раунда 6 называет восемь пунктов, не вменяемых фазе». Абзац «Что НЕ вменяется фазе» перечисляет семь: окно срока давности, частичный уникальный индекс, невыкаченная `0018`, D-26, D-08/BILL-02, D-27, отсутствие RED-коммитов у `05-15` и `05-17`. Восьмой пункт плана — мобильная проверка на 375px — приходит не из `Gaps Summary`, а из перечня человеческих проверок того же отчёта; сам план перечисляет его восьмым в `<flagged_assumptions>`.
- **Fix:** записаны все ДЕВЯТЬ пунктов, как требует план по числу, с точным указанием источника каждого. Расхождение не в составе, а в атрибуции источника, и оно записано здесь, чтобы раунд 7 не искал восьмого пункта в `Gaps Summary`.
- **Files modified:** `.planning/STATE.md`

## Known Stubs

Нет. План не производит кода; заглушек, недоведённых до источника данных компонентов и placeholder-текста в изменённых файлах нет.

## Перенесённые человеческие проверки — задачами не становились

- **Backstop-строка `must_haves`:** понятность и неустареваемость новых записей — судится человеком.
- Мобильная ширина 375px — план не трогает разметки.
- Настоящий платёж в тестовом магазине ЮKassa; первое настоящее уведомление после выката; первый настоящий выкат после прохождения очереди ревизий; статус «отклонён» у отменённого платежа (D-27).
- Владелец риска `WR-07 (раунд 3)` — шестой раунд без имени; названо фактом в §Blockers/Concerns.
- **Три judgment-tier прохибиции фазы (BILL-05, BILL-06, BILL-07) остаются `unresolved`** и этим планом не перемечались: право объявить их закрытыми принадлежит верификации раунда 7. Прохибиция BILL-05 в половине «MUST NOT менять действующий тариф вниз» остаётся `unresolved` **ПО РЕШЕНИЮ владельца (D-32)**, и это сказано прямо, а не оставлено в общем перечне.

## Task Commits

1. **Task 1: Два решения владельца получают номера; запись D-30 сводится с кодом** — `7936b1c` (docs)
2. **Task 2: `.planning/STATE.md` — распоряжения по двенадцати находкам, решение о тире, девять пунктов долга** — `d9d9478` (docs)
3. **Task 3: `COVERAGE.md` — сверка матрицы после раунда 6** — `4329b1f` (docs)

## Self-Check: PASSED

Файлы:

- `.planning/phases/05-tarify/05-CONTEXT.md` — FOUND
- `.planning/STATE.md` — FOUND
- `.planning/phases/05-tarify/COVERAGE.md` — FOUND
- `.planning/phases/05-tarify/05-26-SUMMARY.md` — FOUND

Коммиты:

- `7936b1c` — FOUND
- `d9d9478` — FOUND
- `4329b1f` — FOUND

Удалённых файлов ни в одном из трёх коммитов нет (`git diff --diff-filter=D --name-only HEAD~1 HEAD` пуст на каждом).
