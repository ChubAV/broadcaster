---
phase: 05-tarify
plan: 23
subsystem: payments
tags: [subscription, billing, decimal, pytest, tdd, docstring-invariant]

requires:
  - phase: 05-18
    provides: "`converted_remainder` — арифметика переноса остатка по деньгам (форма `convert-remainder`, D-30), чей докстринг и чинится здесь"
  - phase: 05-19
    provides: "заряд «ни одно объявление не утверждает того, чего код рядом не делает» — этот план закрывает три новых экземпляра того же класса"
  - phase: 05-22
    provides: "`capped_carryover` в том же модуле и раздел его таблицы решений в том же файле тестов — образец формы, которой следует новый раздел"
provides:
  - "прямая модульная таблица решений `converted_remainder` в файле, который владеет арифметикой: семь тестов, включая нижнюю границу, naive/aware и усечение неполного дня"
  - "машинно проверяемое утверждение независимости ответа от длины календарного месяца — три базы с длинами 28, 31 и 30 дают один ответ"
  - "докстринг `converted_remainder`, совпадающий с поведением функции: абзац о длине месяца приведён к тому, что она делает, и ссылается на закрепляющий тест ПО ИМЕНИ"
  - "усечение неполного дня переноса названо числом там, где объявлено обещание «остаток не сгорает»"
  - "прогоном опровергнутая находка `IN-03`: гард `converted_remainder` НЕ мёртв — он несущий, и это записано на его месте"
affects: [05-25, 05-26, верификация раунда 7]

actuals:
  tokens: 15653
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Утверждение докстринга о свойстве арифметики называет тест, который его закрепляет, ПО ИМЕНИ — читатель видит, чем оно проверено, не выходя из функции"
    - "Характеризующая таблица решений пишется по поведению, снятому ПРОГОНОМ функции, а не по её докстрингу: разошедшийся абзац правится ПОСЛЕ того, как поведение закреплено"

key-files:
  created: []
  modified:
    - tests/test_application/test_subscription_period.py
    - app/application/billing/subscription_period.py

key-decisions:
  - "Выбран второй путь пункта `missing:` гэпа 2 — абзац приведён к поведению, а НЕ поведение к абзацу. Величина, которую наблюдает покупатель, посчитана решением владельца D-30 именно этой арифметикой; менять её вслед за комментарием было бы решением планировщика о деньгах"
  - "Гард `if base <= now_utc: return now_utc` НЕ снят, а переписан на сравнение по равенству с комментарием о владельце зажима. Прогоном показано, что ветка несущая: без неё нулевой остаток проходит через нижнюю границу `prorated_days` и покупает один неоплаченный день. Это отклонение от буквы критерия приёмки и соблюдение границы плана «поведение не меняется» — подробности в разделе отклонений"
  - "Соседний абзац `prorated_expiry` о длине месяца оставлен ДОСЛОВНО и назван неприкосновенным прямо в новом тексте: там величина действительно входит в ответ, и правка «за компанию» ввела бы четвёртое неверное утверждение"
  - "`.planning/REQUIREMENTS.md` этим планом НЕ правится: BILL-05 снят с `Complete` вердиктом `gaps_found` по всей фазе и вернётся закрытием фазы, а не одного плана. Файл к тому же делится с параллельным агентом волны"

patterns-established:
  - "Таблица решений денежной арифметики проверяет ТОЧНЫМ числом ту величину, которой в ответе нет, и ДИАПАЗОНОМ ту, что входит множителем — правило раздела объявлено в его же шапке"
  - "Комментарий на месте ветки, признанной мёртвой чужим чтением, называет ПРОГОН, который её оживил, и тесты, которые её закрепляют"

requirements-completed: [BILL-05]

coverage:
  - id: D1
    description: "Ответ `converted_remainder` не зависит от длины календарного месяца: три базы с длинами месяца 28, 31 и 30 при остатке 365 дней дают одно число — 110 дней, равное `int(остаток_в_днях · old / new)`"
    requirement: BILL-05
    verification:
      - kind: unit
        ref: "tests/test_application/test_subscription_period.py#test_the_conversion_does_not_depend_on_the_length_of_the_month"
        status: pass
    human_judgment: false
  - id: D2
    description: "Живой оплаченный остаток никогда не конвертируется в ноль дней: один день `basic` по цене `pro` даёт ровно один день"
    requirement: BILL-05
    verification:
      - kind: unit
        ref: "tests/test_application/test_subscription_period.py#test_a_live_remainder_never_converts_to_zero_days"
        status: pass
    human_judgment: false
  - id: D3
    description: "Отсутствующий и истёкший `current` дают сегодня — воскрешения прошедшего срока нет, зеркало `next_expiry`"
    requirement: BILL-05
    verification:
      - kind: unit
        ref: "tests/test_application/test_subscription_period.py#test_a_missing_period_converts_to_today"
        status: pass
      - kind: unit
        ref: "tests/test_application/test_subscription_period.py#test_an_expired_period_converts_to_today"
        status: pass
    human_judgment: false
  - id: D4
    description: "Равные цены возвращают остаток себе В ЦЕЛЫХ ДНЯХ: неполный день переноса теряется, и это закреплено как настоящее поведение, а не как дефект"
    requirement: BILL-05
    verification:
      - kind: unit
        ref: "tests/test_application/test_subscription_period.py#test_equal_prices_return_the_remainder_in_whole_days"
        status: pass
    human_judgment: false
  - id: D5
    description: "naive и aware `current` одного момента дают один ответ на трёх смещениях (−40, 10, 365 дней) — SQLite против PostgreSQL"
    requirement: BILL-05
    verification:
      - kind: unit
        ref: "tests/test_application/test_subscription_period.py#test_a_naive_and_an_aware_date_give_the_same_conversion"
        status: pass
    human_judgment: false
  - id: D6
    description: "Аппарат симметричен: понижение тарифа при живом остатке даёт БОЛЬШЕ дней, чем было"
    requirement: BILL-05
    verification:
      - kind: unit
        ref: "tests/test_application/test_subscription_period.py#test_a_downgrade_converts_the_remainder_into_more_days"
        status: pass
    human_judgment: false
  - id: D7
    description: "Докстринг `converted_remainder` совпадает с поведением: абзац о длине месяца назван свойством и ссылается на закрепляющий тест по имени; усечение неполного дня названо на месте обещания; гард назван несущим и читающим ответ `countdown_base`"
    requirement: BILL-05
    verification:
      - kind: other
        ref: "grep -c 'КОНСТАНТОЙ 30' app/application/billing/subscription_period.py → 1 (было 2), и оставшееся вхождение принадлежит `prorated_expiry`, где оно верно"
        status: pass
    human_judgment: true
    rationale: "Совпадение объявления с поведением проверяется машиной только в части независимости от длины месяца (D1). Остальное — суждение читателя: абзац может быть верным и при этом непонятным, а именно понятность и есть предмет трёх раундов подряд"

duration: 32min
completed: 2026-08-18
status: complete
---

# Фаза 05 План 23: объявление `converted_remainder` и его таблица решений — Summary

**Единственная новая арифметика денежного пути получила прямые модульные тесты в файле, который ею владеет, и её докстринг перестал утверждать зависимость от календаря, которой у ответа нет: три базы с длинами месяца 28, 31 и 30 дают одно число, и теперь это утверждает тест, а не абзац.**

## Performance

- **Duration:** 32 min
- **Started:** 2026-08-18T06:56:00Z
- **Completed:** 2026-08-18T07:28:41Z
- **Tasks:** 2 из 2
- **Files modified:** 2

## Accomplishments

- **`converted_remainder` впервые импортирована файлом, который владеет её арифметикой.** До плана символ встречался в `tests/` ровно один раз — прозой в шапке раздела `capped_carryover`, добавленной планом 05-22, — и НИ ОДНОГО раза вызовом. Всё покрытие жило интеграционными утверждениями с допуском ±2 дня, который не видит ни усечения дня, ни расхождения февраля, ни разницы naive/aware (`IN-01` раунда 6).
- **Числа отчёта раунда 6 воспроизведены и закреплены.** Остаток 365 дней при переходе `basic` → `pro` даёт 110 дней при базах 2026-01-31 (28 дней месяца), 2026-03-01 (31) и 2026-04-01 (30); остаток 25 дней даёт 7 дней. Прогон снят до правки и повторён после — числа те же.
- **Абзац о длине месяца приведён к поведению и ссылается на тест ПО ИМЕНИ.** Утверждение «длина месяца берётся у `add_one_month`, а не константой 30» осталось в файле ровно одно — у `prorated_expiry`, где оно верно.
- **Усечение неполного дня названо числом на месте обещания.** Абзац о нижней границе теперь сам говорит, что до 23 ч 59 м уже оплаченного времени теряется, и что это цена хранения срока днями, а не решение о деньгах (`IN-02`).
- **Находка `IN-03` опровергнута прогоном и записана на месте ветки.** Гард `converted_remainder` не мёртв: снятие его меняет ответ для отсутствующего и истёкшего срока с «сегодня» на «сегодня плюс один день».

## Task Commits

1. **Task 1: Таблица решений `converted_remainder` в файле, который владеет арифметикой** — `4ce5df6` (test)
2. **Task 2: Абзац о длине месяца приведён к поведению, гард переписан, усечение названо** — `3de8541` (docs)

**Plan metadata:** см. коммит этого SUMMARY.

## Files Created/Modified

- `tests/test_application/test_subscription_period.py` — новый раздел «Перенос остатка ПО ДЕНЬГАМ: таблица решений»: шапка с предметом, причиной и правилом утверждений раздела; семь тестов (один из них параметризован тремя смещениями). `converted_remainder` добавлена в существующий блок импорта; `PRICE_PRO` и `PRICE_BASIC` переиспользованы — третьей копии прейскуранта не заведено (`grep -c "1490"` остался равен 3).
- `app/application/billing/subscription_period.py` — три правки ОДНОЙ функции `converted_remainder`: абзац о длине месяца (`:240`), фраза об усечении на месте обещания (`:227-235`), комментарий и сравнение по равенству на месте гарда (`:272-285`). Арифметика не тронута ни на символ.

## Decisions Made

1. **Абзац правится к поведению, а не поведение к абзацу.** Отчёт раунда 6 называл обе альтернативы. Вторая («если задумывалась зависимость от календаря») меняет величину, которую наблюдает покупатель, и требует решения владельца — план такого мандата не имеет и прямо объявляет границу «поведение `converted_remainder` не меняется».
2. **Гард сохранён и переписан на `==`.** Это второй из двух путей, названных `IN-03` дословно («or invert the guard to `if base == now_utc: return now_utc` and say why (nothing to convert)»), и единственный, совместимый с границей плана. См. раздел отклонений.
3. **Тест равных цен утверждает усечение, а не желаемое равенство.** `result == now + timedelta(days=(current - now).days)` плюс `result < current`. Утверждение `== current` было бы ложным.
4. **`REQUIREMENTS.md` не правится этим планом** — BILL-05 снят с `Complete` вердиктом `gaps_found` по фазе целиком; кроме того, файл делится с параллельным агентом волны (план 05-24).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug в предпосылке плана] Ветка `subscription_period.py:245-247` НЕ недостижима; критерий приёмки, требующий её снятия, невыполним без правки поведения**

- **Found during:** Task 2 (перед правкой, прогоном обеих форм функции)
- **Issue:** План, `05-VERIFICATION.md` и `IN-03` независимо утверждают, что ветка `if base <= now_utc: return now_utc` недостижима, потому что «`countdown_base` уже вернул `now_utc` в этом случае». Рассуждение обратное: возврат `now_utc` из `countdown_base` не обходит ветку, а УДОВЛЕТВОРЯЕТ её условие — ветка берётся. Прогон обеих форм на трёх входах:

  | `current` | `base == now_utc` | ветка берётся | ответ БЕЗ ветки |
  |---|---|---|---|
  | `None` | True | True | `now + 1 day` |
  | истёкший (−40 дней) | True | True | `now + 1 day` |
  | ровно `now` | True | True | `now + 1 day` |

  Снятие ветки пускает нулевой остаток через нижнюю границу `prorated_days` (`max(…, 1)`) и покупает ОДИН день тарифа, за который не заплачено. Это правка поведения, прямо запрещённая границей плана и truth №6 (`Ни одно поведение converted_remainder не изменено этим планом`).
- **Fix:** взят второй путь, названный `IN-03` дословно и `05-VERIFICATION.md` как «либо заменить её комментарием»: сравнение переписано на `if base == now_utc`, а на месте ветки поставлен комментарий, который (а) называет `countdown_base` владельцем зажима к сегодняшнему дню (D-04), (б) говорит, что проверка ЧИТАЕТ его ответ, а не решает об истечении заново, (в) называет ветку несущей, а не мёртвой, и (г) называет два теста задачи 1, которые её закрепляют. Второе независимое правило истечения из текста исчезло — то есть must_have truth №4 закрыт по существу, а не по букве.
- **Files modified:** `app/application/billing/subscription_period.py`
- **Verification:** `uv run pytest tests/test_application/test_subscription_period.py tests/test_pages/test_billing_payment_errors.py tests/test_services/test_payment_concurrency.py -q` → 135 passed; полная суита → 1754 passed. `base == now_utc` эквивалентно `base <= now_utc` по построению `countdown_base` (база либо равна `now_utc`, либо строго больше), поэтому поведение идентично.
- **Committed in:** `3de8541`
- **План велел поступить именно так:** «Если снятие ветки меняет ответ хоть в одном тесте, ветка НЕ была недостижимой… об этом сказать в SUMMARY прямо, а не подгонять тест». Тесты не подгонялись; ни одно ожидание задачи 1 не изменено (`git diff --name-only -- tests/` в задаче 2 пуст).

### Невыполненный критерий приёмки — назван прямо

- Критерий задачи 2 «число вхождений `return now_utc` в теле `converted_remainder` равно `0`» **НЕ выполнен и выполнен быть не мог**: он противоречит границе того же плана. Вхождение осталось одно. Второй путь того же пункта `missing:` выполнен полностью.
- План также предлагал в этом случае «править не ветку, а `countdown_base`». Этот путь **отвергнут**: `countdown_base` — единственное объявление правила D-04, его читают `next_expiry`, `prorated_expiry` и `capped_carryover`, и правка ради одного потребителя ввела бы ровно то расхождение, ради устранения которого модуль заведён. Плюс это правка поведения трёх функций денежного пути, то есть решение владельца.

**2. [Rule 3 — Фактическая поправка] Базовое значение `grep -c "converted_remainder"` в тестах равно 1, а не 0**

- **Found during:** Task 1 (снятие базовых значений)
- **Issue:** План и `IN-01` утверждают, что символ «не встречается в `tests/` ни разу». На момент исполнения он встречается один раз — прозой в шапке раздела `capped_carryover` (`tests/test_application/test_subscription_period.py:300`), добавленной планом 05-22 волной 18, то есть ПОСЛЕ снятия отчёта раунда 6.
- **Fix:** факт записан как есть; утверждение отчёта верно по существу (ноль ВЫЗОВОВ, символ не импортирован). После плана значение — 11 при пороге критерия 8.
- **Files modified:** нет (поправка учёта)
- **Verification:** `grep -n "converted_remainder" tests/test_application/test_subscription_period.py` до правки → одна строка, комментарий.
- **Committed in:** `4ce5df6`

---

**Total deviations:** 2 auto-fixed (1 × Rule 1, 1 × Rule 3)
**Impact on plan:** Объём плана не расширен ни на строку. Одна правка (снятие ветки) заменена на эквивалентную по цели и совместимую с границей плана; расхождение названо числами и таблицей прогона, а не эпитетом. Scope creep отсутствует: изменены ровно два объявленных файла.

## Verification Results

| Критерий | Результат |
|---|---|
| `uv run pytest tests/test_application/test_subscription_period.py -q` | 46 passed (было 37; прирост 9 ≥ 7) |
| `uv run pytest` трёх файлов задачи 2 | 135 passed, код 0 |
| Полная суита `uv run pytest tests/ -q` | **1754 passed**, код 0 (было 1733 после 05-21 / 05-22) |
| `grep -c "converted_remainder" tests/…` | 11 (порог 8; до плана 1, из них 0 вызовов) |
| `grep -c "1490" tests/…` | 3 — не изменилось |
| `grep -c "КОНСТАНТОЙ 30" app/…/subscription_period.py` | 1 (было 2); оставшееся — `:168`, принадлежит `prorated_expiry` |
| `grep -c "float(" app/…/subscription_period.py` | 0 |
| Тест закрепления назван по имени в коде | 1 вхождение; `pytest -k` собирает **ровно один** тест |
| Арифметика не тронута: `git diff -U0 … \| grep -c "^[+-].*prorated_days(\|^[+-].*Decimal("` | 0 |
| Задача 1 не тронула `app/` | `git diff --name-only -- app/` пуст |
| Ни один существующий тест не удалён | `git diff -U0 … \| grep -c "^-def test_"` → 0 |
| Alembic / пакеты | `pyproject.toml`, `uv.lock`, `alembic/` не тронуты |

**Числа, обязанные не измениться, — не изменились:** 110 дней при остатке 365 (на всех трёх длинах месяца), 7 дней при остатке 25.

**Цитата абзаца об усечении (`subscription_period.py:227-232`):**

> ⚠️ ЦЕНА ЭТОГО ОБЕЩАНИЯ НАЗВАНА ЗДЕСЬ ЖЕ, ЧИСЛОМ, А НЕ ЭПИТЕТОМ. Та же целая часть, что даёт нижнюю границу, ОТБРАСЫВАЕТ неполный день переноса: при равных ценах ответ равен сегодняшнему дню плюс ЦЕЛОЕ число дней остатка, и до 23 ч 59 м уже оплаченного времени теряется.

**Цитата комментария на месте гарда (`subscription_period.py:272-283`):**

> ЗАЖИМ К СЕГОДНЯШНЕМУ ДНЮ — РАБОТА `countdown_base` (D-04), И ВТОРОЙ КОПИИ ПРАВИЛА ИСТЕЧЕНИЯ ЗДЕСЬ НЕТ. […] ⚠️ ВЕТКА НЕСУЩАЯ, А НЕ МЁРТВАЯ, И СНЯТИЕ ЕЁ МЕНЯЕТ ПОВЕДЕНИЕ.

## TDD Gate Compliance

⚠️ **RED-гейта у этого плана нет, и это не упущение.** Задача 1 объявлена `tdd="true"`, но её тесты — ХАРАКТЕРИЗУЮЩИЕ: они закрепляют поведение существующей функции, а сам план прямо запрещает менять это поведение («ПОВЕДЕНИЕ `converted_remainder` НЕ МЕНЯЕТСЯ. Это граница плана, а не пожелание»). Тест, обязанный упасть, здесь означал бы правку арифметики денег. Поведение снято ПРОГОНОМ настоящей функции ДО написания тестов, а не переписано из докстринга, — именно поэтому таблица утверждает усечение неполного дня, которое докстринг обещанием отрицал.

Порядок задач тем не менее исполняет тот же заряд, что и RED→GREEN: свойство закреплено тестом (задача 1, `4ce5df6`) РАНЬШЕ, чем объявление о нём переписано (задача 2, `3de8541`). Обратный порядок дал бы четвёртое утверждение, проверенное чужими глазами.

Коммиты: `test(05-23)` → `docs(05-23)`. Гейта `feat(...)` нет, потому что план не добавляет поведения.

## Known Stubs

Нет. Заглушек, `TODO`, `FIXME`, пропущенных тестов и невыполненных `<verify>` план не оставил.

## Threat Flags

Нет. Новой сетевой, файловой или схемной поверхности план не вводит: изменены докстринг, комментарий, одна операция сравнения (эквивалентная прежней) и файл модульных тестов.

Диспозиции `mitigate` из `<threat_model>` применены:

- **T-05-145** (читатель «приводит арифметику в соответствие» с ложным абзацем) — свойство закреплено тестом на трёх длинах месяца ДО правки абзаца; абзац называет тест по имени.
- **T-05-146** (арифметика без прямых тестов) — таблица из семи тестов той же формы, что у `prorated_expiry`.
- **T-05-147** (правка объявления незаметно меняет поведение) — **сработала на практике**: попытка исполнить критерий «снять ветку» была бы ровно этой угрозой, и поймал её критерий неизменности чисел вместе с прогоном. Митигация подтверждена делом, а не декларацией.
- **T-05-148** (обещание объявлено шире, чем исполняется) — усечение названо числом на месте обещания и закреплено тестом равных цен.
- **T-05-149** (правка задевает верный абзац `prorated_expiry`) — абзац не тронут; более того, новый текст прямо называет его неприкосновенным и объясняет, чем он отличается.
- **T-05-SC** — пакеты не устанавливались, `pyproject.toml` и `uv.lock` не тронуты.

## Issues Encountered

**Предпосылка трёх независимых источников (плана, `05-VERIFICATION.md` и `IN-03` ревью) оказалась ложной.** Разрешено прогоном обеих форм функции до внесения правки, а не чтением. Урок для раунда 7 записывается прямо: «недостижимость» ветки, стоящей после функции с зажимом, была выведена рассуждением — та же ошибка класса, что и весь предмет плана, только допущенная в отчёте, а не в коде.

Прочих проблем нет. Полная суита прошла за 16 мин 39 с.

## Deferred / Not Addressed

Записанный долг фазы этим планом не закрывается и не вменяется: второе окно потолка намерений, частичный уникальный индекс, невыкаченная `0018`, D-26, D-08/BILL-02, D-27, отсутствие RED-коммитов у `05-15` и `05-17`, мобильная проверка на 375px, настоящий платёж в тестовом магазине ЮKassa. Три judgment-tier прохибиции фазы (E-01/BILL-05, E-02/BILL-06, E-03/BILL-07) остаются `unresolved`.

## User Setup Required

Нет — внешних сервисов план не касается.

## Next Phase Readiness

- План `05-25` строит машинный гейт на прохибицию «MUST NOT объявлять в докстринге инвариант, не назвав теста, который его закрепляет». `converted_remainder` теперь такому гейту УДОВЛЕТВОРЯЕТ: её ключевое утверждение называет тест по имени, и это имя собирается `pytest -k` в ровно один тест.
- Для верификации раунда 7 приготовлено: находка `IN-03` требует переформулировки — ветка не мёртвая. Если раунд 7 повторит требование «снять ветку», это будет требование правки ПОВЕДЕНИЯ, и его следует адресовать владельцу.
- Блокеров нет. `STATE.md`, `ROADMAP.md` и `REQUIREMENTS.md` этим агентом не трогались — записи волны за оркестратором.

## Self-Check: PASSED

- `.planning/phases/05-tarify/05-23-SUMMARY.md` — FOUND
- `tests/test_application/test_subscription_period.py` — FOUND (изменён, коммит `4ce5df6`)
- `app/application/billing/subscription_period.py` — FOUND (изменён, коммит `3de8541`)
- Коммит `4ce5df6` — FOUND в `git log`
- Коммит `3de8541` — FOUND в `git log`
- Рабочее дерево чистое; `STATE.md`, `ROADMAP.md`, `REQUIREMENTS.md` не тронуты
- Реестр `.planning/WINDOWS.md` этим агентом НЕ заведён намеренно: файл кросс-фазовый и общий, а волна исполняется двумя агентами параллельно — создание его в двух worktree дало бы add/add конфликт при слиянии. Отклонение записано в этом SUMMARY разделами «Deviations from Plan» и «Невыполненный критерий приёмки»

---
*Phase: 05-tarify*
*Completed: 2026-08-18*
