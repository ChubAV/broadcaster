---
phase: 05-tarify
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - app/pages/billing.py
  - app/pages/history.py
  - app/routes/billing.py
  - app/services/billing_service.py
  - app/constants.py
  - tests/test_pages/test_billing_section.py
  - tests/test_routes/test_billing.py
autonomous: true
requirements: [BILL-05, BILL-06, BILL-07]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Один экран /billing собирает все блоки раздела: текущий тариф и срок, четыре оси, планы из конфига, баланс и пакеты, история платежей — ни табов, ни второго маршрута (D-18)"
    - "История платежей строится по журналу Payment, а не по BalanceTransaction: критерий 3 требует сумму в рублях, и она есть только у Payment (D-14)"
    - "Баланс сообщений остаётся отдельным блоком экрана и не смешивается с осью «Отправок в месяц»: квота тарифа и купленный пакет — разные вещи (D-10)"
    - "Список платежей полный, без пагинации, с техническим потолком; при срабатывании потолка экран называет это явно, а не обрезает молча (D-17)"
    - "Чужие платежи на /billing не видны: владение проверено предикатом запроса, а не фильтром в шаблоне"
    - "Покупка пакета сообщений идёт настоящей формой POST с редиректом на confirmation_url и работает без JS (D-20)"
    - "Истёкшая подписка ничего не отключает: раздел честно сообщает, что срок истёк, и предлагает продлить (D-07)"
    - "GET /billing не пишет в БД ни при каких условиях, включая возврат пользователя с ЮKassa (D-05)"
    - "JSON-маршрут покупки пакета удалён вместе с переводом покупки на форму: он был единственным местом, отдававшим идентификатор платежа в браузер (D-24)"
    - "Гард источника на изменяющем запросе — один на проект: раздел истории и раздел тарифов зовут одну функцию, а не две копии одного правила"
    - statement: "Пользователь, вернувшийся с ЮKassa до прихода вебхука, понимает, что платёж ещё в обработке, и не считает, что оплата не прошла"
      verification: backstop
  artifacts:
    - "app/pages/billing.py — GET /billing со всеми блоками D-18 и POST /billing/purchase"
    - "app/services/billing_service.py — get_payment_history"
    - "app/constants.py — потолок списка платежей"
    - "tests/test_pages/test_billing_section.py — интеграционное покрытие раздела"
  key_links:
    - "plan_axes(nav_counts из request.state.shell) → контекст шаблона usage"
    - "get_payment_history(user_id, limit=потолок) → контекст шаблона payments"
    - "POST /billing/purchase → create_payment(kind='package') → 302 на confirmation_url"
    - "app/pages/common.py is_same_origin → и раздел истории, и обе формы оплаты"
  prohibitions:
    - requirement_id: BILL-07
      category: transparency
      statement: "MUST NOT молча обрезать список платежей при срабатывании технического потолка — пользователь обязан узнать, что он видит не всё"
      status: resolved
      verification: test
---

<objective>
Собрать раздел `/billing` целиком на серверной стороне: один маршрут отдаёт все пять
блоков экрана, обе оплаты идут настоящими формами POST, и мёртвая JSON-поверхность
покупки уезжает вместе со своим единственным потребителем.

Purpose: критерии 2 и 3 фазы. Сегодня `app/pages/billing.py` — 38 строк, отдающих
баланс и историю операций в штуках сообщений; ни подписки, ни осей, ни планов, ни
денежного журнала на экране нет.
Output: переписанный страничный роутер, `get_payment_history`, снос
`POST` JSON-покупки.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tarify/05-CONTEXT.md
@.planning/phases/05-tarify/05-RESEARCH.md
@.planning/phases/05-tarify/05-PATTERNS.md
@.planning/phases/05-tarify/05-01-SUMMARY.md
@.planning/phases/05-tarify/05-02-SUMMARY.md
@.planning/phases/05-tarify/05-03-SUMMARY.md
</context>

<!-- planner-discipline-allow: parsed_plan_limits -->
<!-- planner-discipline-allow: PurchaseRequest -->
<!-- planner-discipline-allow: /api/billing/purchase -->

<flagged_assumptions>
Строки edge-probe для **BILL-05**, **BILL-06** и **BILL-07** вернулись `unclassified` /
`unresolved` — классификатор англоязычный, требования написаны по-русски. Все три
подняты явно как флагированные допущения (полная таблица — план `05-01`) и не
отброшены; края выведены вручную из четырёх критериев ROADMAP и решений D-01…D-25 и
разложены по `must_haves.truths` планов `05-01` … `05-05`.

**Дискреционный выбор, названный явно.** «Что видит пользователь после возврата с
ЮKassa до прихода вебхука» отдан на усмотрение решениями фазы. Выбран **вариант строки
истории**: платёж уже создан со статусом «в обработке» и виден в списке платежей вместе
с датой и суммой; отдельная плашка на экране не заводится — она была бы вторым местом,
рассказывающим то же самое, и её пришлось бы гасить по приходе вебхука. Срок при этом
не сдвинут (D-05). Соответствующая истина помечена `verification: backstop`: понятность
формулировки не доказывается автотестом.

**Канон-отсылка:** «просмотр чужих платежей» и «покупка чужому пользователю» — канон
контроля доступа (OWASP), покрыты `<threat_model>` ниже и `/gsd-secure-phase`;
отдельные прохибиции не минтятся, хотя владение закреплено тестами.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Один гард источника на проект и журнал платежей в сервисе биллинга</name>
  <files>app/pages/history.py, app/services/billing_service.py, app/constants.py</files>
  <read_first>
    - app/pages/common.py (после плана 05-01 — функция `is_same_origin` и её докстринг с названной границей защиты)
    - app/pages/history.py (строки 340-380 — приватная копия гарда, которая уезжает; строка 950 — единственный её вызов; строки 745-775 — приём «потолок проверяется ДО конструирования и называет себя»)
    - app/services/billing_service.py (строки 133-165 — `get_balance_info` и `get_transaction_history`: сортировка по дате, форма limit/offset, форма возвращаемых записей)
    - app/models/payment.py (после плана 05-01 — состав колонок, включая `kind`, `plan`, `amount_value`, `status`, `created_at`, `confirmed_at`)
    - app/constants.py (строки 15-45 — форма именованной константы с выписанным обоснованием)
  </read_first>
  <behavior>
    - `app/pages/history.py` использует общий гард источника; второй копии правила в проекте нет
    - Существующие тесты повтора отправки (`tests/test_pages/test_history_retry.py`) продолжают проходить без правок
    - `get_payment_history(db, user_id, limit=N)` возвращает не более N записей, отсортированных по дате создания по убыванию
    - `get_payment_history` не возвращает платежи другого пользователя
    - `get_payment_history` возвращает платежи всех статусов — `pending`, `succeeded`, `canceled`
    - Есть способ узнать, что записей больше потолка, не вытаскивая их все
  </behavior>
  <action>
**Гард источника.** План `05-01` уже завёл `is_same_origin` в `app/pages/common.py`.
Здесь снимается его приватный дубликат: `app/pages/history.py` перестаёт объявлять свою
копию и зовёт общую функцию. Копия завела бы второй источник одного правила — ровно то,
чего решения фазы требуют избегать. Существующие тесты раздела истории править не
нужно: поведение не меняется, меняется место объявления.

**Журнал платежей.** В `app/services/billing_service.py`, рядом с
`get_transaction_history` и по его форме, объявить `get_payment_history(db, user_id,
limit)`: выборка `Payment` с предикатом владельца, сортировка по дате создания по
убыванию, ограничение `limit`. Сортировка **по дате, никогда по сумме**:
`amount_value` объявлена строкой (ЮKassa оперирует decimal-строками, и хранение во
`float` потеряло бы копейки), поэтому строковое сравнение дало бы «999.00 больше
1490.00». Арифметика по суммам в фазе не нужна вовсе — BILL-07 требует только показа.
Владение — предикатом запроса, а не фильтром у вызывающего.

Рядом объявить способ узнать общее число платежей владельца, не вытаскивая их: этого
требует потолок, который обязан **проверяться до конструирования списка** и называть
себя, а не обрезать молча — приём уже применён у выгрузки истории.

**Константа потолка.** В `app/constants.py`, по форме соседних констант с выписанным
обоснованием: потолок списка платежей — **200**. Обоснование в комментарии: платежей у
пользователя единицы в год (месячное продление плюс редкие пакеты), поэтому пагинация и
сентинел бесконечной прокрутки здесь были бы механикой ради механики; потолок остаётся
только чтобы запрос не был неограниченным, и 200 — это порядка семнадцати лет
ежемесячных продлений. Значение живёт в `app/constants.py`, потому что его обязаны
называть одинаково и обработчик, и тест, и Фаза 6.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_history_retry.py tests/test_services/test_billing_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_history_retry.py tests/test_services/test_billing_service.py -q` завершается с кодом 0
    - `grep -c 'def _is_same_origin' app/pages/history.py` == 0 — приватной копии больше нет
    - `app/pages/history.py` содержит `is_same_origin` как импортируемое имя
    - `app/services/billing_service.py` содержит `async def get_payment_history(`
    - `grep -c 'order_by(Payment.amount_value' app/services/billing_service.py` == 0
    - `app/constants.py` содержит именованную константу потолка списка платежей со значением 200
    - Поведение: `get_payment_history` при 5 платежах владельца и 3 платежах другого пользователя возвращает ровно 5
    - Поведение: `get_payment_history` возвращает записи в порядке убывания даты создания
    - Поведение: `get_payment_history` включает записи со статусами `pending`, `succeeded` и `canceled`
  </acceptance_criteria>
  <done>Гард источника объявлен один раз на проект; журнал денег доступен обработчику раздела с проверенным владением и явным потолком.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Переписать страничный роутер раздела «Тарифы»</name>
  <files>app/pages/billing.py, tests/test_pages/test_billing_section.py</files>
  <read_first>
    - app/pages/billing.py (весь файл после плана 05-01 — текущий контекст и добавленный обработчик подписки)
    - app/pages/dashboard.py (строки 64-145 — эталон страничного обработчика: гард входа, вызовы модулей, один `TemplateResponse`; строки 119-127 — чтение уже посчитанного шелла вместо второго запроса)
    - app/pages/history.py (строки 745-775 — потолок, проверяемый до конструирования и называющий себя параметром редиректа; строки 940-961 — порядок проверок изменяющего POST и PRG)
    - app/pages/common.py (строки 390-455 — контракт `nav_counts` и `quota`; строки 170-250 — глобалы `format_datetime_for_user`, `time_ago_for_user`, `plural_ru`)
    - app/application/billing/plan_usage.py (после плана 05-03 — сигнатура `plan_axes` и форма возвращаемых осей)
    - app/services/billing_service.py (после задачи 1 — `get_payment_history`, `get_balance_info`, `get_transaction_history`)
    - app/routes/billing.py (строки 45-66 — валидация индекса пакета по списку конфига: образец для валидации из формы)
    - app/config.py (после плана 05-01 — `parsed_plan_limits`, `parsed_message_packages`)
  </read_first>
  <behavior>
    - `GET /billing` неаутентифицированным → 302 на `/login`
    - `GET /billing` отдаёт контекст со всеми блоками: подписка (план и срок), четыре оси, планы из конфига, баланс, пакеты, история операций, история платежей
    - `GET /billing` не выполняет ни одной записи в БД
    - `GET /billing` у пользователя с истёкшей подпиской помечает её истёкшей и не отключает ничего
    - `GET /billing` показывает только платежи владельца
    - При числе платежей больше потолка контекст несёт явный признак срабатывания потолка
    - `POST /billing/purchase` с валидным индексом пакета и корректным Origin → 302 на `confirmation_url`
    - `POST /billing/purchase` с посторонним Origin → 403, платёж не создан
    - `POST /billing/purchase` с индексом вне диапазона → 302 на `/billing`, платёж не создан
    - `POST /billing/purchase` при `yookassa_enabled=false` → 302 на `/billing`, платёж не создан
    - При `yookassa_enabled=false` контекст `GET /billing` всё равно несёт планы и оси
  </behavior>
  <action>
Переписать `app/pages/billing.py` под один экран со всеми блоками. Порядок блоков сверху
вниз — текущий тариф и срок, четыре оси, карточки планов, баланс сообщений и пакеты,
история платежей; ни табов, ни второго маршрута. Табы в макете есть только у админки, и
заводить их здесь значило бы ввести новый компонент раньше срока.

**GET-обработчик.** Гард входа дословно как в `dashboard.py`: нет пользователя → 302 на
`/login`. Далее — вызовы уже готовых модулей, страница сама ничего не агрегирует:
`get_balance_info`, `get_transaction_history`, `plan_axes`, `get_payment_history`.
`plan_axes` получает `nav_counts` из `request.state.shell` через `getattr(request.state,
"shell", {})` — образец в `dashboard.py:119-127`; вторым запросом эти числа не считаются.
Лимиты для `plan_axes` берутся из записи текущего плана в `parsed_plan_limits`, план —
из `quota['plan']` шелла. Свойство `parsed_plan_limits` разбирается **один раз в
обработчике** и кладётся в контекст готовым списком: `@lru_cache` стоит на
`get_settings`, а не на свойстве, поэтому вызов из Jinja в цикле по трём планам парсил
бы JSON трижды за рендер. Прецедент строкой уже есть в этом файле для пакетов.

Подписка в контексте: план и срок берутся из `quota` шелла (`plan`, `expires_at`) —
эти поля уже читают строку `Subscription`. Признак «срок истёк» считается сравнением с
`datetime.now(timezone.utc)`, и `expires_at` перед сравнением проводится через
`normalize_utc`: колонка объявлена с таймзоной, но SQLite отдаёт её naive, а PostgreSQL
aware, и сравнение падает ровно на одном диалекте. **Истечение срока ничего не
отключает** — раз применения лимитов в системе нет, истёкшая подписка меняет только
показ; вводить принуждение по сроку в фазе, которая сознательно не вводит принуждение
по лимитам, значило бы завести два разных ответа на один вопрос.

Потолок списка платежей проверяется **до** конструирования списка: если общее число
платежей владельца больше константы потолка, в контекст кладётся явный признак, и
шаблон называет это пользователю. Тихая обрезка запрещена.

**GET не пишет в БД ни при каких условиях.** Обработчика возврата с ЮKassa не
заводится, ленивого `get_or_create_subscription` на рендере нет: шелл прямо запрещает
писать в БД при отрисовке страницы, а редирект браузера с `return_url` происходит и при
отказе — это самое лёгкое место, где раздел мог бы начать раздавать подписку бесплатно.
Пользователь, вернувшийся до прихода вебхука, видит свой платёж строкой истории в
статусе «в обработке».

**POST-обработчик покупки пакета.** Новый `POST /billing/purchase` в этом же
страничном роутере, рядом с обработчиком подписки плана `05-01` — обе оплаты живут в
одном месте и делят один порядок проверок. Порядок дословно как у повтора отправки:
`get_user_from_cookie` → нет пользователя → 302 на `/login`; `is_same_origin` → ложь →
403; `settings.yookassa_enabled` ложь → 302 на `/billing`; индекс пакета из формы
валидируется по длине `parsed_message_packages` — образец валидации уже есть в
`app/routes/billing.py:55-56`; вне диапазона → 302 на `/billing`. Затем
`create_payment(kind="package", ...)` с ценой и числом сообщений **из конфига по
индексу**, никогда из формы, и `RedirectResponse(status_code=302)` на
`confirmation_url`. Из формы приезжает только идентификатор — иначе покупатель
назначает себе цену.

`active_page` остаётся `"billing"` — его проставляют все роутеры раздела.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_section.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_billing_section.py -q` завершается с кодом 0
    - `app/pages/billing.py` содержит `plan_axes`, `get_payment_history` и `/billing/purchase`
    - `grep -c 'parsed_plan_limits' app/pages/billing.py` >= 1 и `grep -c 'parsed_plan_limits' app/templates/billing/balance.html` == 0 — свойство разбирается в обработчике, не в Jinja
    - `grep -Ec '(db|session)\.(add|commit|flush)\(' app/pages/billing.py` == 0 в теле GET-обработчика — страница ничего не пишет
    - Поведение: `GET /billing` неаутентифицированным отдаёт 302 с `Location: /login`
    - Поведение: `GET /billing` аутентифицированным отдаёт 200 и контекст несёт четыре оси
    - Поведение: `GET /billing` у пользователя с двумя своими и тремя чужими платежами показывает ровно два
    - Поведение: `GET /billing` у пользователя с `expires_at` в прошлом отдаёт 200 и признак истёкшего срока, не 403 и не редирект
    - Поведение: `GET /billing` при 201 платеже владельца и потолке 200 несёт в контексте признак срабатывания потолка
    - Поведение: `POST /billing/purchase` с валидным индексом и корректным Origin отдаёт 302 на `confirmation_url` мока
    - Поведение: `POST /billing/purchase` с посторонним Origin отдаёт 403 и не создаёт строк `payments`
    - Поведение: `POST /billing/purchase` с индексом 99 отдаёт 302 на `/billing` и не создаёт строк `payments`
    - Поведение: `POST /billing/purchase` при `yookassa_enabled=false` не создаёт строк `payments`
    - Поведение: `GET /billing` при `yookassa_enabled=false` отдаёт 200 и контекст всё равно несёт планы и четыре оси
  </acceptance_criteria>
  <done>Один маршрут отдаёт весь раздел, обе оплаты идут формами POST через один порядок проверок, и страница по-прежнему ничего не пишет в БД.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Снос JSON-поверхности покупки</name>
  <files>app/routes/billing.py, tests/test_routes/test_billing.py</files>
  <read_first>
    - app/routes/billing.py (весь файл после плана 05-01 — четыре JSON-входа и обработчик вебхука)
    - tests/test_routes/test_billing.py (весь файл — какие входы покрыты тестами и какие тесты уезжают вместе с маршрутом)
    - app/pages/billing.py (после задачи 2 — новый потребитель покупки)
    - .planning/phases/03-gruppy-akkaunta/03-CONTEXT.md (D-14 — прецедент решения о судьбе мёртвой JSON-поверхности, которому эта задача обязана следовать)
  </read_first>
  <behavior>
    - JSON-маршрут покупки пакета больше не отвечает: запрос к нему даёт 404/405
    - `PurchaseRequest` больше не объявлен
    - Обработчик вебхука на месте и по-прежнему защищён IP-гардом
    - Оставшиеся JSON-чтения (`GET /api/billing/balance`, `/packages`, `/transactions`) работают как раньше
    - Покупка пакета работает через форму раздела
  </behavior>
  <action>
<!-- planner-discipline-allow: /api/billing/purchase -->
Снести `POST /api/billing/purchase` вместе с моделью `PurchaseRequest` и его тестами.
После перевода покупки на форму у маршрута не остаётся ни одного потребителя: его
единственный вызов — сносимый скрипт шаблона (план `05-05`). Это тот самый маршрут,
который возвращал `yookassa_payment_id` прямо в браузер, — единственный «секрет»,
которым до этой фазы был защищён неаутентифицированный вебхук. Удаление потребителя
убирает и утечку, и мёртвый код. Ход тот же, что снос мёртвой JSON-поверхности групп в
Фазе 3.

**Три JSON-чтения остаются.** `GET /api/billing/balance`, `GET /api/billing/packages`
и `GET /api/billing/transactions` сохраняются: они читающие, покрыты тестами, и
удаление объявленного читающего API — отдельное решение о совместимости, а не
следствие перевода покупки на форму. Решение по ним зафиксировано этим планом как
«оставить», чтобы проект не набрал два разных ответа на один вопрос.

Обработчик вебхука в этом файле не трогать: он остаётся единственным изменяющим входом
`app/routes/billing.py` и сохраняет IP-гард плана `05-01`.

Тесты сносимого маршрута в `tests/test_routes/test_billing.py` удаляются вместе с ним —
не переписываются под новый путь: путь формы покрыт задачей 2. Тесты оставшихся трёх
чтений сохраняются без изменений.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes/test_billing.py tests/test_routes/test_billing_webhook_source.py tests/test_pages/test_billing_section.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_routes/test_billing.py tests/test_routes/test_billing_webhook_source.py tests/test_pages/test_billing_section.py -q` завершается с кодом 0
    - `grep -c 'PurchaseRequest' app/routes/billing.py` == 0
    - `grep -vc '^\s*#' app/routes/billing.py` — в файле нет исполняемого объявления маршрута покупки пакета (`grep -c '@router.post("/purchase")' app/routes/billing.py` == 0)
    - `grep -c '@router.post("/webhook")' app/routes/billing.py` == 1
    - `grep -Ec '@router.get\("/(balance|packages|transactions)"\)' app/routes/billing.py` == 3
    - Поведение: POST на JSON-маршрут покупки пакета отдаёт 404 или 405
    - Поведение: `GET /api/billing/balance` аутентифицированным по-прежнему отдаёт 200
    - Поведение: покупка пакета через форму раздела по-прежнему создаёт платёж и отдаёт 302
    - `uv run pytest tests/ -q` завершается с кодом 0
  </acceptance_criteria>
  <done>Единственный маршрут, отдававший идентификатор платежа в браузер, удалён вместе с тестами; читающий JSON-API сохранён одним решением.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → `POST /billing/purchase` | Недоверенный ввод: индекс пакета, Origin, cookie |
| браузер → `GET /billing` | Недоверенный ввод: cookie; ответ содержит денежный журнал владельца |
| приложение → браузер (тело ответа) | Утечка идентификатора платежа наружу — вектор подделки вебхука |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Information Disclosure | Просмотр чужих платежей на `/billing` | high | mitigate | Задачи 1 и 2: `Payment.user_id == user.id` предикатом запроса в `get_payment_history`, не фильтром в шаблоне; закреплено тестом «два своих, три чужих → показано два» |
| T-05-21 | Information Disclosure | Идентификатор платежа отдаётся в тело ответа и служит ключом подделки вебхука | high | mitigate | Задача 3: JSON-маршрут покупки, возвращавший `payment_id` браузеру, удалён; форма отдаёт 302 на `confirmation_url` и идентификатор наружу не попадает |
| T-05-22 | Tampering | Цена или число сообщений приезжают полем формы | high | mitigate | Задача 2: из формы приезжает только индекс пакета; цена и количество читаются из `parsed_message_packages` по индексу. Индекс вне диапазона → 302, никогда «умолчание» |
| T-05-23 | Spoofing | CSRF на форме покупки пакета | medium | mitigate | Задача 2: `is_same_origin` на изменяющем входе + существующая посадка cookie `samesite="lax"`; новая форма наследует посадку 13 существующих POST-форм |
| T-05-24 | Elevation of Privilege | Рендер страницы пишет в БД и выдаёт подписку в обход вебхука | high | mitigate | Задача 2: GET-обработчик не содержит записи (проверяется грепом); обработчик `return_url` не заводится; закреплено тестом плана `05-01` |
| T-05-25 | Denial of Service | Неограниченная выборка журнала платежей | medium | mitigate | Задача 1: `limit` равен константе потолка, потолок проверяется до конструирования и называет себя пользователю |
| T-05-26 | Repudiation | Пользователь вернулся с ЮKassa, платёж не виден — неясно, списаны ли деньги | medium | mitigate | Задача 2: строка `pending` создаётся при инициации оплаты и видна в истории с датой и суммой сразу после возврата |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | План новых пакетов не устанавливает; аудит легитимности в `05-RESEARCH.md` §Package Legitimacy Audit |
</threat_model>

## Artifacts this phase produces

**Файлы (новые):** `alembic/versions/0017_payment_kind_and_plan.py`,
`app/application/billing/__init__.py`, `app/application/billing/subscription_period.py`,
`app/application/billing/plan_usage.py`, `app/templates/billing/includes/plan_card.html`,
`app/templates/billing/includes/usage_meters.html`,
`app/templates/billing/includes/payment_row.html`,
`tests/test_application/test_subscription_period.py`,
`tests/test_application/test_plan_usage.py`,
`tests/test_pages/test_billing_subscription.py`, `tests/test_pages/test_billing_section.py`,
`tests/test_migrations/test_0017_payment_kind_and_plan.py`,
`tests/test_routes/test_billing_webhook_source.py`,
`.planning/phases/05-tarify/COVERAGE.md`

**Файл, удаляемый фазой:** `app/templates/billing/plans.html` (план `05-05`)

**Alembic revision id:** `0017` (`down_revision = "0016"`)

**Функции и макросы (новые):** `add_one_month`, `next_expiry`, `is_same_origin`,
`current_month_bounds_utc`, `sends_in_current_month`, `plan_axes`, `get_payment_history`,
`format_money_ru` (план `05-05`), Jinja-макросы `plan_card`, `usage_meter`, `payment_row`

**Поля моделей (новые):** `Payment.kind`, `Payment.plan`; `Payment.messages_count` становится nullable

**Ключи конфига (новые):** `Settings.plan_limits`, `Settings.parsed_plan_limits`,
`Settings.yookassa_webhook_verify_ip`, `Settings.yookassa_webhook_client_ip_header`

**Константы (новые):** потолок списка платежей в `app/constants.py`; идентификаторы и
подписи четырёх осей в `app/application/billing/plan_usage.py`

**Маршруты (новые):** `POST /billing/subscribe`, `POST /billing/purchase`
**Маршрут, удаляемый фазой:** JSON-покупка пакета в `app/routes/billing.py`

**CSS-атрибут (новый):** `[data-plans]`

<verification>
- `uv run pytest tests/ -v` зелёный целиком
- В проекте ровно одно объявление гарда источника
- `app/pages/billing.py` не содержит записи в БД в GET-обработчике
- Ни один маршрут проекта больше не отдаёт `yookassa_payment_id` в тело ответа
</verification>

<success_criteria>
- Один экран `/billing` отдаёт все пять блоков раздела
- История платежей показывает дату, сумму и статус только своих платежей
- Обе оплаты идут настоящими формами POST
- Потолок списка называет себя
</success_criteria>

<output>
Create `.planning/phases/05-tarify/05-04-SUMMARY.md` when done
</output>
