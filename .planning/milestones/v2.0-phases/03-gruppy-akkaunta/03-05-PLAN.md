---
phase: 03-gruppy-akkaunta
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - app/pages/account_groups.py
  - app/pages/common.py
  - app/templates/account_groups/list.html
  - app/templates/account_groups/partial_cards.html
  - app/templates/account_groups/includes/group_row.html
  - app/static/css/app.css
  - tests/test_pages/test_account_groups.py
autonomous: true
requirements: [GRP-04, GRP-06]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Пользователь находит группу по названию строкой поиска, и поиск переживает подгрузку следующей страницы (D-03)"
    - "Длинные списки подгружаются сентинел-паттерном по 30 строк за запрос (D-04)"
    - "Счётчик «N активных из M групп» считается отдельными запросами, а не по загруженной странице (D-04)"
    - "Пользователь удаляет группу через панель подтверждения, и её идентификатор исчезает из group_ids всех своих расписаний (GRP-06)"
    - "Экран пригоден к использованию на мобильных ширинах в пределах брейкпоинтов 860/900/1080 — критерий 5 фазы"
    - "Подпись строки показывает, в скольких расписаниях состоит группа, — пользователь видит цену удаления (D-08)"
    - "Нулевой список рендерит одно из трёх пустых состояний, различаемых по факту прошлой синхронизации и по наличию строки поиска (GRP-04 empty)"
    - "Порядок строк не меняется от переключения тумблера: сортировка задана возрастанием Group.id (GRP-05 ordering)"
    - "Переключение тумблера группы, не входящей ни в одно расписание, отрабатывает штатно и ничего кроме её is_active не меняет (GRP-05 empty)"
    - "Удаление одной группы не задевает соседние: группы с тем же именем в других аккаунтах остаются, из расписаний уходит ровно один идентификатор (GRP-06 adjacency)"
    - "Удаление последней группы аккаунта оставляет экран в пустом состоянии «Все группы удалены»; удаление группы, не входящей ни в одно расписание, отрабатывает штатно (GRP-06 empty)"
    - "После удаления оставшиеся строки сохраняют порядок по возрастанию Group.id (GRP-06 ordering)"
    - "Повторный запрос удаления уже удалённой группы отвечает перенаправлением на экран, не поднимает ошибки и ничего не меняет (GRP-06 idempotency)"
    - "Два последовательных запроса удаления одной группы: первый удаляет, второй остаётся без эффекта; чистка group_ids идемпотентна (GRP-06 concurrency)"
    - "При нуле групп линейка счётчика не рендерится вовсе — сообщение несёт пустое состояние, а не строка «0 активных из 0 групп» (E3 empty)"
    - "Линейка счётчика рендерится сервером вместе со страницей и не имеет промежуточного состояния загрузки; паршал прокрутки её не трогает (E3 loading)"
    - "Отказ подсчёта — это отказ рендера страницы существующим путём, а не линейка с выдуманными числами (E3 error)"
    - "Числа линейки приходят двумя выделенными запросами подсчёта, независимыми от загруженной страницы (E3 populated)"
    - "Все группы выключены — линейка честно показывает ноль активных рядом с надписью о пропуске при рассылке (E3 partial)"
    - "На узких ширинах правая надпись линейки переносится под число, разделительная линия схлопывается без обрезки (E3 overflow)"
    - "Русские склонения линейки верны для одного, двух и пяти: «1 активная из 1 группы» и «5 активных из 12 групп» (E3 zero-one-many)"
    - "В линейку не попадает пользовательский текст — обе строки собираются из чисел (E3 long-text)"
    - "Три пустых состояния списка различимы по копирайтингу: никогда не синхронизировано, все группы удалены, поиск ничего не нашёл (E4 empty)"
    - "Единственный индикатор подгрузки — сентинел; уже отрисованные строки во время подгрузки не мигают и не исчезают (E4 loading)"
    - "Отказ переключения возвращает строку в прежнем состоянии, отказ удаления оставляет строку на месте — состояние, не принятое сервером, не отображается (E4 error)"
    - "Строка списка несёт аватар в цвете мессенджера, имя, подпись «в N расписаниях», необязательную пометку «не найдена при синке», кнопку удаления и тумблер; выключенная строка приглушена (E4 populated)"
    - "Строка может нести оба состояния сразу — выключена и не найдена при синке; пометка и приглушение независимы и не маскируют друг друга (E4 partial)"
    - "Сотни групп реального TG-аккаунта разбиваются на страницы по 30, поиск остаётся инструментом управления объёмом (E4 overflow)"
    - "Ноль групп даёт пустое состояние, одна группа — одну строку и линейку «из 1 группы», много групп — постраничную подгрузку (E4 zero-one-many)"
    - statement: "Имя группы длиной 120 символов обрезается в одну строку с полным значением в title, а элементы управления не покидают строку — проверяется на ширине 320 пикселей"
      verification: backstop
    - "Пустая строка поиска отдаёт полный список; строка поиска рендерится и при пустом результате, чтобы фильтр всегда можно было сбросить (E5 empty)"
    - "Поиск — обычная GET-навигация с полным серверным рендером; введённое значение сохраняется в поле (E5 loading)"
    - "Негодные параметры запроса отклоняются существующим контрактом валидации; необычная строка поиска просто ничего не находит (E5 error)"
    - "Строка поиска протаскивается в адрес каждого сентинела в urlencode-виде: вторая страница поиска не может тихо вернуться к неотфильтрованному списку (E5 partial)"
    - "На ширинах до 860 пикселей строка поиска сворачивается за кнопку существующим поведением макроса (E5 overflow)"
    - "Длинная строка поиска прокручивается в поле нативно, эхо значения проходит через экранирование шаблонизатора (E5 long-text)"
    - "Панель подтверждения всегда называет удаляемую группу — пустого тела у неё не бывает (E6 empty)"
    - "Кнопка подтверждения отражает выполняющийся запрос и не допускает повторной отправки (E6 loading)"
    - "Отказ удаления закрывает панель и показывает сообщение уровня страницы, строка остаётся на месте (E6 error)"
    - "Строка последствий в панели рендерится полностью независимо от состояния группы: для группы вне расписаний текст остаётся верным (E6 partial)"
    - statement: "Длинное имя группы в теле панели подтверждения переносится не более чем на две строки и затем обрезается; панель не выходит за свою предельную ширину — проверяется на ширине 320 пикселей"
      verification: backstop
    - statement: "Тело панели подтверждения — единственный слот пользовательского содержимого; оно экранируется шаблонизатором и ограничено правилом переноса выше"
      verification: backstop
  artifacts:
    - path: "app/pages/account_groups.py"
      provides: "Паршал прокрутки, поиск, два запроса подсчёта, удаление группы"
      min_lines: 150
    - path: "app/templates/account_groups/partial_cards.html"
      provides: "Страница подгрузки с идентичной строкой и идентичным сентинелом"
      min_lines: 10
    - path: "app/static/css/app.css"
      provides: "Нумерованная секция стилей экрана групп аккаунта"
      contains: "data-group-row"
  key_links:
    - from: "app/templates/account_groups/list.html"
      to: "app/pages/account_groups.py"
      via: "сентинел вызывает паршал прокрутки с протащенным поиском"
      pattern: "groups/partial\\?offset="
    - from: "app/pages/account_groups.py"
      to: "app/repositories/schedule.py"
      via: "удаление группы чистит group_ids расписаний через remove_group_ids"
      pattern: "remove_group_ids"
---

<objective>
Экран групп аккаунта доводится до состава, описанного макетом и UI-SPEC: линейка «N активных из M групп», строка поиска, бесконечная прокрутка по 30 строк, удаление группы через панель подтверждения, три различимых пустых состояния, шапка аккаунта и собственная секция стилей с адаптивностью.

Purpose: срез плана 03-01 доказал архитектуру экрана, но у TG-аккаунта бывают сотни чатов — без поиска и постраничной подгрузки экран непригоден (D-03, D-04), а без удаления не закрыт GRP-06. Счётчик обязан считаться отдельными запросами: подсчёт по загруженной странице врёт ровно там, где цена вранья наибольшая (RESEARCH Pitfall 5).
Output: паршал прокрутки, поиск, два запроса подсчёта, маршрут удаления с панелью подтверждения, шапка аккаунта, три пустых состояния и новая нумерованная секция `app.css`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-gruppy-akkaunta/03-CONTEXT.md
@.planning/phases/03-gruppy-akkaunta/03-RESEARCH.md
@.planning/phases/03-gruppy-akkaunta/03-PATTERNS.md
@.planning/phases/03-gruppy-akkaunta/03-UI-SPEC.md
@.planning/phases/03-gruppy-akkaunta/03-01-SUMMARY.md
@.planning/phases/01-interfeysnyy-fundament/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Паршал прокрутки, поиск и честные счётчики</name>
  <read_first>
    - app/pages/account_groups.py — текущее состояние модуля после плана 03-01
    - app/pages/groups.py (строки 100-230) — контракт паршала: offset/limit/search, приём limit+1, состав контекста, сборка filter_params
    - app/templates/groups/list.html (строки 89-104) — сентинел с протаскиванием фильтров и комментарий-инвариант, который переезжает вместе с кодом
    - app/templates/groups/partial_cards.html — как страница подгрузки повторяет разметку строки и сентинела
    - app/templates/components/filters.html — сигнатура макроса filters и его поведение при сворачивании
    - app/templates/components/field.html — сигнатура макроса field
    - app/pages/groups.py (строки 45-60) — подсчёт вхождений групп в group_ids расписаний
    - .planning/phases/03-gruppy-akkaunta/03-UI-SPEC.md — раздел Copywriting Contract, подразделы Counter line, Group row и Search
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ (дополняют `tests/test_pages/test_account_groups.py`) и должны падать до реализации:
    - страница с 35 группами показывает 30 строк и сентинел, адрес сентинела содержит смещение 30 и предел 30
    - паршал по адресу сентинела отдаёт оставшиеся 5 строк и сентинела больше не содержит
    - страница с поиском по подстроке имени показывает только совпавшие строки
    - адрес сентинела на странице с поиском содержит строку поиска в urlencode-виде
    - паршал, вызванный с той же строкой поиска, отдаёт продолжение отфильтрованного списка, а не полного
    - паршал чужого аккаунта не отдаёт имён его групп
    - паршал без cookie-сессии отвечает перенаправлением на вход
    - паршал с отрицательным смещением и с пределом больше 100 отвергается контрактом валидации
    - линейка счётчика показывает число активных и общее число групп аккаунта, посчитанные по всей таблице, а не по 30 загруженным строкам
    - при 35 группах, из которых 32 активны, линейка показывает 32 и 35
    - склонения линейки верны для 1, 2 и 5 групп
    - паршал прокрутки линейку счётчика не содержит
    - подпись строки показывает число расписаний, в которые входит группа, и считает только расписания владельца
  </behavior>
  <files>app/pages/account_groups.py, app/templates/account_groups/list.html, app/templates/account_groups/partial_cards.html, app/templates/account_groups/includes/group_row.html, tests/test_pages/test_account_groups.py</files>
  <action>
Добавить в `app/pages/account_groups.py` вход `GET /accounts/{account_id}/groups/partial` с контрактом параметров, идентичным существующим паршалам проекта: `offset: int = Query(0, ge=0)`, `limit: int = Query(PAGE_SIZE, ge=1, le=100)`, `search: str | None = Query(None)`, а также `layout: str | None = Query(None)`, который принимается и игнорируется (D-15 Фазы 1). Аутентификация и проверка владения аккаунтом выполняются в этом входе заново — владение проверяется на КАЖДОМ входе.

Выделить внутренний построитель запроса, принимающий `user_id`, `account_id` и `search`: базовое условие — `Group.user_id == user_id` И `Group.account_id == account_id`; при непустой строке поиска добавляется `Group.name.ilike(pattern)`, где `pattern` собирается как bind-параметр, а не конкатенацией SQL. Сортировка — `order_by(Group.id)`. Фильтры по мессенджеру и по активности из старого раздела НЕ переносятся (D-03): у экрана ровно один параметр фильтрации.

Приём определения следующей страницы — `limit(limit + 1)` с последующим срезом и флагом `has_next`, как в существующих паршалах.

Счётчики (D-04): в обработчике СТРАНИЦЫ выполнить два скалярных запроса подсчёта по всей таблице для этого аккаунта — общее число групп и число групп с истинным `is_active`. Ни один из них не должен опираться на длину загруженной страницы. Паршал прокрутки счётчики не вычисляет и в контекст не кладёт.

Склонения. В `app/pages/common.py` добавить и зарегистрировать в `templates.env.globals` функцию русского склонения по числу — принимающую число и три формы («активная/активные/активных», «группа/группы/групп», «расписании/расписаниях») и возвращающую нужную. Общего хелпера склонений в репозитории нет, а линейка обязана печатать «1 активная из 1 группы», а не «1 активных из 1 групп» (UI-SPEC).

Подпись строки «в N расписаниях» (разрешение дискреции D-08 — показывать, потому что это объясняет цену удаления): для отрисованных групп посчитать вхождения их идентификаторов в `Schedule.group_ids` — обходом JSON-списков в Python, по образцу подсчёта в старом разделе, НО с обязательным ограничением выборки расписаний владельцем: аналог этого ограничения не имел, и переносить дефект нельзя. При нуле подпись читается «не в расписаниях». Статистика отправок по группе НЕ переносится (отложено).

Шаблоны. В `list.html` добавить строку поиска макросом `filters` с единственным полем `search`, кнопкой «Применить» и ссылкой сброса на адрес экрана без параметров; плейсхолдер — «Поиск по названию группы». Добавить сентинел ПОСЛЕДНИМ элементом внутри того же контейнера, что и строки, с адресом паршала, смещением, пределом 30 и протаскиванием строки поиска через urlencode. Создать `partial_cards.html` с ИДЕНТИЧНОЙ разметкой строки (вызов того же макроса) и ИДЕНТИЧНЫМ сентинелом. Комментарий-инвариант бесконечной прокрутки из старого раздела переносится вместе с кодом: сентинел остаётся последним элементом того же контейнера, разметка сентинела в обоих файлах совпадает, цикл проброса параметров переносится дословно с приведением к строке и urlencode.

Макрос строки дополнить подписью числа расписаний и пометкой «не найдена при синке» для групп с заполненной колонкой пропажи: mono-подпись плюс декоративная точка в цвете предупреждения, `title` с датой; декоративная точка помечается как скрытая от вспомогательных технологий. Массовые операции старого раздела не переносятся (D-03) — ни разметки выбора строк, ни соответствующей панели действий в новом экране не появляется.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/ tests/test_templates/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/account_groups.py` содержит `async def account_groups_partial(`
    - `grep -c 'Query(0, ge=0)' app/pages/account_groups.py` возвращает не менее 1 и `grep -c 'le=100' app/pages/account_groups.py` возвращает не менее 1
    - `grep -q 'ilike' app/pages/account_groups.py` — поиск идёт параметризованным сравнением
    - `grep -c 'func.count' app/pages/account_groups.py` возвращает не менее 2 — оба счётчика считаются запросами
    - страница с 35 группами отдаёт 30 строк и сентинел со смещением 30; паршал по этому адресу отдаёт 5 строк без сентинела
    - адрес сентинела на странице с поиском содержит закодированное значение поиска, и паршал с ним отдаёт отфильтрованное продолжение
    - при 35 группах, из которых 32 активны, HTML страницы содержит оба числа 32 и 35 в линейке счётчика
    - HTML паршала прокрутки чисел линейки счётчика не содержит
    - разметка сентинела в `list.html` и в `partial_cards.html` совпадает
    - `uv run pytest tests/test_pages/test_account_groups.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Экран выдерживает сотни групп: поиск и подгрузка по 30 строк работают вместе, поиск не теряется на второй странице, а числа линейки считаются по всей таблице и не зависят от того, сколько строк успело загрузиться.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Удаление группы с панелью подтверждения</name>
  <read_first>
    - app/pages/groups.py (строки 250-270) — существующий обработчик удаления: выборка, чистка group_ids, удаление, перенаправление
    - app/repositories/schedule.py (строки 41-53) — remove_group_ids: учёт JSON-колонки и ограничение владельцем через связь с объявлением
    - app/templates/groups/includes/group_row.html (строки 70-93) — форма-триггер с перехватом на самой форме и панель подтверждения РЯДОМ со строкой
    - app/templates/components/modal.html — сигнатура макроса modal
    - .planning/phases/03-gruppy-akkaunta/03-UI-SPEC.md — раздел Destructive confirmation: текст тела панели с двумя последствиями
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ (дополняют `tests/test_pages/test_account_groups.py`) и должны падать до реализации:
    - запрос удаления своей группы удаляет строку и отвечает перенаправлением на экран групп этого аккаунта
    - идентификатор удалённой группы исчезает из `group_ids` всех расписаний владельца
    - идентификаторы других групп в тех же расписаниях остаются на месте
    - группа с тем же именем в другом аккаунте того же пользователя не удаляется
    - запрос удаления группы, принадлежащей другому аккаунту, ничего не удаляет
    - повторный запрос удаления уже удалённой группы отвечает перенаправлением и не поднимает ошибки
    - удаление группы, не входящей ни в одно расписание, отрабатывает штатно
    - после удаления порядок оставшихся строк остаётся возрастающим по идентификатору
    - разметка страницы содержит панель подтверждения с именем группы, и панель ровно одна на группу
    - панель подтверждения находится вне элемента строки
    - форма-триггер удаления сохраняет действие и метод, то есть работает без Alpine
  </behavior>
  <files>app/pages/account_groups.py, app/templates/account_groups/includes/group_row.html, tests/test_pages/test_account_groups.py</files>
  <action>
Добавить вход `POST /accounts/{account_id}/groups/{group_id}/delete`. Аутентификация и владение — как во всех входах экрана: выборка группы тройным условием `Group.id == group_id`, `Group.user_id == user.id`, `Group.account_id == account_id`. При найденной группе — сначала `ScheduleRepository(db).remove_group_ids(user.id, {group.id})` (готовый метод учитывает JSON-колонку и ограничение владельцем; собственный обход расписаний писать запрещено), затем `await db.delete(group)` и `await db.commit()`. При ненайденной группе — ничего не менять. Ответ в обоих случаях — перенаправление на `/accounts/{account_id}/groups`, что и делает повторный запрос безвредным.

Разметка. Мгновенный крестик макета заменяется панелью подтверждения — правило Фазы 1. В макросе строки: форма-триггер с действием, ведущим на маршрут удаления, и перехватом отправки, навешенным на САМУ форму (без Alpine перехват не навешивается и форма уходит настоящим запросом на тот же маршрут). Панель подтверждения эмитится РЯДОМ со строкой, вне неё: панель позиционируется фиксированно, а внутри строки стала бы её колонкой; кроме того, панель обязана жить вне любого элемента, заменяемого опросом (урок Фазы 1). Панель ровно одна на группу.

Тело панели по UI-SPEC называет ОБА настоящих последствия: группа исчезнет из всех расписаний и следующая синхронизация вернёт её как новую. Второе следствие прямо вытекает из D-10 и обязано быть сказано пользователю до удаления, а не обнаружено после.

Кнопка подтверждения — разрушительного варианта; параметр метода передаётся макросу явно, хотя совпадает с умолчанием: маршрут и метод удаления — контракт с обработчиком, и он обязан читаться поиском по этому файлу, а не только по отрендеренной странице.

Комментарии-инварианты о расположении панели и о перехвате на самой форме переносятся из аналога вместе с кодом.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/ tests/test_templates/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/account_groups.py` содержит `async def account_groups_delete(`
    - `grep -q 'remove_group_ids' app/pages/account_groups.py` — чистка расписаний идёт готовым методом репозитория
    - запрос удаления своей группы удаляет строку и убирает её идентификатор из `group_ids` расписаний владельца
    - идентификаторы прочих групп в тех же расписаниях остаются
    - запрос удаления группы другого аккаунта не удаляет ни одной строки
    - повторный запрос удаления отвечает кодом 302 и не поднимает исключения
    - HTML страницы содержит ровно одну панель подтверждения на группу, и её идентификатор не встречается внутри элемента строки
    - форма-триггер удаления в HTML сохраняет метод отправки и адрес маршрута удаления
    - `uv run pytest tests/test_pages/test_account_groups.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Пользователь удаляет группу из списка аккаунта с явным подтверждением, знает оба последствия заранее, и группа корректно уходит из расписаний; повторный запрос безвреден, чужие данные недостижимы.</done>
</task>

<task type="auto">
  <name>Task 3: Шапка аккаунта, линейка счётчика, пустые состояния и секция стилей</name>
  <read_first>
    - app/templates/account_groups/list.html — текущее состояние страницы после задач 1 и 2
    - .planning/phases/03-gruppy-akkaunta/03-UI-SPEC.md — разделы Spacing Scale, Typography, Color, Copywriting Contract, Component Inventory, Responsive Contract целиком
    - app/static/css/app.css — нумерация секций, существующие токены, секция выбора групп как ближайший стилевой прецедент строк-карточек
    - app/templates/includes/messenger_icon.html — сигнатура и правило не вызывать для неизвестного типа
    - app/templates/components/empty_state.html, components/card.html, components/badge.html, components/mono.html, components/avatar.html — сигнатуры макросов
    - app/pages/common.py (строки 137-165) — format_datetime_for_user и разрешение таймзоны пользователя
    - tests/test_pages/test_responsive_markup.py — класс проверок адаптивной разметки, которым обязан пройти новый экран
  </read_first>
  <files>app/templates/account_groups/list.html, app/templates/account_groups/includes/group_row.html, app/pages/common.py, app/static/css/app.css, tests/test_pages/test_account_groups.py</files>
  <action>
Шапка экрана — карточка аккаунта из макета: плашка иконки мессенджера 48 на 48 в цвете канала, имя аккаунта заголовком, mono-строка идентичности, бейдж статуса существующим словарём статусов экрана аккаунтов, справа — действие «К аккаунтам». Кнопка «Синхронизировать всё» и плашка результата синка приходят планом 03-06; здесь шапка собирается без них, а mono-строка идентичности уже честна: при незаполненном времени последней синхронизации читается «синхронизация ещё не выполнялась», при статусе выполнения — «синхронизация идёт сейчас», иначе — «{телефон} · последняя синхронизация {N} назад». Отсутствующий телефон убирает и сегмент, и разделитель — осиротевшей точки в начале строки не бывает.

Относительное время. В `app/pages/common.py` добавить и зарегистрировать в `templates.env.globals` серверный хелпер «N назад», считающий разницу от текущего момента в таймзоне пользователя и возвращающий короткую русскую строку. Клиентских часов не заводить: таймзона пользователя уже решена существующими хелперами того же модуля.

Линейка-разделитель над списком: слева число активных и общее число со склонениями, справа — надпись макета о том, что выключенные группы пропускаются при рассылке, прописными. Между ними — тонкая заполняющая линия. При нуле групп линейка не рендерится вовсе: сообщение несёт пустое состояние.

Три различимых пустых состояния макросом `empty_state`, тексты — из UI-SPEC:
- синхронизации ещё не было и групп нет — «Групп пока нет» с подсказкой нажать «Синхронизировать всё»;
- синхронизация была, групп нет — «Все группы удалены» с подсказкой запустить синхронизацию, чтобы подтянуть чаты заново (согласуется с D-10);
- задана строка поиска и совпадений нет — «Группы не найдены» с действием сброса поиска.
Различаются по факту прошлой синхронизации и по наличию строки поиска.

Новая нумерованная секция `app/static/css/app.css` (одна, следующая по номеру) со стилями: раскладка шапки с переносом и действиями, прижатыми вправо; линейка счётчика с заполняющей линией; колонка строк с промежутком 8 пикселей; сама строка-карточка с колонкой имени, допускающей обрезку, и элементами управления справа; приглушённое состояние выключенной строки; блок пометки «не найдена при синке»; кнопка-иконка удаления 36 на 36 с разрушительным состоянием наведения — последняя добавляется только если эквивалентного отгруженного класса в файле нет, что нужно проверить чтением перед добавлением.

Значения берутся строго из UI-SPEC: шаг сетки 4/8/16/24, промежуточный шаг 12 пикселей допущен только для вертикального ритма между блоками одного экрана, размеры типографики 11/14/16, акцент разрешён исключительно для главного действия экрана, включённого положения тумблера, кольца фокуса и подписи выполняющейся синхронизации. Новых цветов, радиусов и ступеней размера не вводить; новых utility-классов не вводить.

Адаптивность (критерий 5 фазы): существующие брейкпоинты 860/900/1080 без добавления новых. До 860 пикселей шапка переносит действия на свою строку, строка поиска сворачивается за кнопку существующим поведением макроса. До 400 пикселей строка группы переносится в две строки — аватар с именем на первой, пометка и элементы управления на второй; горизонтальной прокрутки и обрезки нет. Каждый интерактивный элемент — не менее 36 пикселей по меньшей стороне; тумблер помещён в зону метки нужного размера. Фокус виден на каждом элементе, декоративные значки и точки скрыты от вспомогательных технологий, состояние никогда не передаётся одним лишь цветом или прозрачностью.

Тестами закрепить: три пустых состояния различимы по тексту, линейка при нуле групп отсутствует, склонения линейки верны для 1, 2 и 5, шапка при незаполненном времени синхронизации показывает соответствующий текст, а не выдуманное «0 минут назад».
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py tests/test_templates/test_components.py -q</automated>
    <human-check>Открыть экран групп аккаунта на ширинах 320, 860 и 1280 пикселей: шапка переносит действия без обрезки, строка группы читается и её элементы управления доступны, горизонтальной прокрутки нет.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'data-acct-head' app/static/css/app.css` и `grep -q 'data-group-row' app/static/css/app.css`
    - `grep -q 'data-acct-head' app/templates/account_groups/list.html`
    - страница аккаунта с незаполненным временем последней синхронизации содержит текст «синхронизация ещё не выполнялась»
    - страница аккаунта без групп и без прошлой синхронизации содержит заголовок «Групп пока нет»
    - страница аккаунта без групп с прошлой синхронизацией содержит заголовок «Все группы удалены»
    - страница с непустым поиском и нулём совпадений содержит заголовок «Группы не найдены» и действие сброса
    - страница с нулём групп не содержит текста линейки счётчика
    - страница с одной активной группой содержит строку «1 активная из 1 группы»
    - страница с пятью активными из двенадцати содержит строку «5 активных из 12 групп»
    - `uv run pytest tests/test_pages/test_responsive_markup.py tests/test_templates/test_components.py -q` завершается кодом 0 — запрет utility-классов и правила экранирования не нарушены
  </acceptance_criteria>
  <done>Экран выглядит и ведёт себя как описано в UI-SPEC: честная шапка, честная линейка счётчика, три различимых пустых состояния, собственная секция стилей и работоспособность на мобильных ширинах.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → паршал прокрутки | `account_id`, `offset`, `limit`, `search` приходят из адреса запроса |
| браузер → маршрут удаления | разрушительная операция, инициируемая из адреса |
| мессенджер → имя группы → HTML строки, тела панели и эха поиска | недоверенный внешний ввод в трёх местах рендера |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-19 | Elevation of Privilege | `GET /accounts/{account_id}/groups/partial` | high | mitigate | Аутентификация и владение аккаунтом проверяются в самом паршале, выборка ограничена `Group.user_id` И `Group.account_id`; тест «паршал чужого аккаунта не отдаёт имён» обязателен |
| T-03-20 | Elevation of Privilege | `POST /accounts/{account_id}/groups/{group_id}/delete` | high | mitigate | Тройное условие выборки; при несовпадении удаления не происходит и ответ неотличим от успешного перенаправления. Чистка расписаний выполняется методом репозитория, ограниченным владельцем |
| T-03-21 | Tampering | строка поиска | high | mitigate | Значение уходит bind-параметром в сравнение по шаблону, а не конкатенацией SQL; в адрес сентинела попадает в urlencode-виде; эхо значения в поле проходит экранирование шаблонизатора |
| T-03-22 | Denial of Service | параметры постраничной загрузки | medium | mitigate | Существующий контракт валидации: смещение неотрицательно, предел от 1 до 100; сверх предела запрос отвергается до обращения к БД |
| T-03-23 | Tampering | имя группы в теле панели подтверждения и в строке | medium | mitigate | Во все три места рендера уходит текст, не разметка; покрывается существующим обходом всех шаблонов на небезопасное экранирование |
| T-03-24 | Denial of Service | подсчёт вхождений в `group_ids` обходом в Python | low | accept | Обход выполняется для 30 отрисованных строк на страницу; агрегация по всей таблице расписаний не выполняется. Признано приемлемым тем же приёмом, что в отгруженном разделе |

Установок пакетов фаза не содержит — строка `T-{phase}-SC` неприменима.
</threat_model>

## Artifacts this phase produces

- Обработчики `account_groups_partial`, `account_groups_delete` в `app/pages/account_groups.py`
- Маршруты `GET /accounts/{account_id}/groups/partial`, `POST /accounts/{account_id}/groups/{group_id}/delete`
- Файл `app/templates/account_groups/partial_cards.html`
- Глобалы шаблонов: функция русского склонения по числу и серверный хелпер относительного времени в `app/pages/common.py`
- Новая нумерованная секция `app/static/css/app.css` с селекторами `[data-acct-head]`, `.count-rule`, `.count-rule__line`, `[data-group-list]`, `[data-group-row]`, `.group-row__name`, `.group-row__sub`, `.group-row--off`, `.group-row__mark`, `.icon-btn`
- Атрибуты раскладки `data-acct-head`, `data-group-list`

<verification>
- `uv run pytest tests/test_pages/test_account_groups.py -q` — зелено
- `uv run pytest tests/ -q` — суита не деградировала
- Числа линейки счётчика не зависят от того, сколько строк загружено
- Панель подтверждения удаления живёт вне строки и вне любого подменяемого блока
</verification>

<success_criteria>
- GRP-04 полностью: экран показывает только группы этого аккаунта, выдерживает сотни строк, имеет поиск и три осмысленных пустых состояния
- GRP-06 полностью: группа удаляется с подтверждением и корректно уходит из расписаний
- D-03, D-04, D-08 реализованы; критерий 5 фазы (мобильные ширины) закрыт разметкой и стилями
</success_criteria>

<output>
Create `.planning/phases/03-gruppy-akkaunta/03-05-SUMMARY.md` when done
</output>
