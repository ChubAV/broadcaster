# Phase 3: Группы аккаунта - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-11
**Phase:** 3-Группы аккаунта
**Areas discussed:** Судьба раздела «Группы», Семантика «отключено», Синк и его результат, Ручное добавление

---

## Судьба раздела «Группы»

| Option | Description | Selected |
|--------|-------------|----------|
| Снести целиком | /groups и пункт навигации удаляются, редирект на «Аккаунты» | ✓ |
| Редирект + оставить паршалы | Страница уходит, часть маршрутов переиспользуется | |
| Оставить оба раздела | Глобальный список параллельно с экраном аккаунта | |

**User's choice:** Снести целиком (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| /accounts/{id}/groups | Явный адрес внутри раздела «Аккаунты» | ✓ |
| /accounts/{id} | Экран аккаунта = его группы, как в макете | |
| /groups?account_id={id} | Старый адрес с обязательным фильтром | |

**User's choice:** /accounts/{id}/groups (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| Только макет + поиск | Список из макета плюс поиск по имени | ✓ |
| Строго по макету | Без добавлений | |
| Поиск + фильтр активности + bulk | Максимум старых возможностей | |

**User's choice:** Только макет + поиск (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| Бесконечная прокрутка | Сентинел-паттерн Фазы 1, по 30 за запрос | ✓ |
| Все сразу | Один рендер без пагинации | |

**User's choice:** Бесконечная прокрутка (рекомендация Claude)

---

## Семантика «отключено»

| Option | Description | Selected |
|--------|-------------|----------|
| Пропуск при отправке | Диспетчер не отправляет в выключенные группы | ✓ |
| Вычистка из расписаний | Выключение убирает группу из group_ids | |
| Только выбор в редакторе | Как сейчас; старые расписания продолжают слать | |

**User's choice:** Пропуск при отправке (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| Тихо, без записи в истории | Пропуск на этапе диспетчеризации, след в structlog | ✓ |
| Запись «пропущено» в истории | Новый статус SendLog | |

**User's choice:** Тихо, без записи в истории (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| Показывать выбранные с пометкой | Выключенные видны только если уже в этом расписании | ✓ |
| Показывать все с пометками | Все группы аккаунта с недоступными чекбоксами | |
| Скрывать как сейчас | Выключенная группа невидима даже в расписании | |

**User's choice:** Показывать выбранные с пометкой (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| Без подтверждения | Тумблер мгновенный, действие обратимо | ✓ |
| Предупреждать | Панель подтверждения при активных расписаниях | |

**User's choice:** Без подтверждения (рекомендация Claude)

---

## Синк и его результат

| Option | Description | Selected |
|--------|-------------|----------|
| Сводка-плашка на экране | «найдено N, новых M, ошибка…» + опрос для WA/MAX | ✓ |
| Только обновить список | Без сводки | |

**User's choice:** Сводка-плашка на экране (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| Возвращаются | Синк — полная переинвентаризация; удалённые вернутся | ✓ |
| Не возвращаются | Томбстоуны, удаление «навсегда» | |

**User's choice:** Возвращаются (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| Имена + пометка пропавших | Обновлять имена, помечать не найденные, не удалять | ✓ |
| Только добавлять новые | Как сейчас (only-add) | |
| Полная замена | Пропавшие удаляются автоматом | |

**User's choice:** Имена + пометка пропавших (рекомендация Claude)

| Option | Description | Selected |
|--------|-------------|----------|
| Колонка + без per-group кнопки | Миграция last_synced_at; per-group ↻ не делается | ✓ |
| Колонка + кнопка = синк аккаунта | Per-group кнопка запускает синк всего аккаунта | |
| Без колонки | Никакой миграции, шапка без «последней синхронизации» | |

**User's choice:** Колонка + без per-group кнопки (рекомендация Claude)

---

## Ручное добавление

| Option | Description | Selected |
|--------|-------------|----------|
| ID + название, без проверки | Создание записи без обращения к воркеру | |
| С проверкой через мессенджер | Валидация перед созданием | |
| **Other (свободный ответ)** | «У нас не будет ручного добавления! Убираем его!» | ✓ |

**User's choice:** Ручного добавления не будет — GRP-08 снимается с фазы, CTA «Добавить группу» из макета не реализуется, `POST /api/groups` удаляется.
**Notes:** Решение владельца принято на все три вопроса области (форма, UX, дубликаты) — область закрыта отменой возможности. ROADMAP.md (критерий 3) и REQUIREMENTS.md (GRP-08) требуют правки.

---

## Claude's Discretion

- Форма сводки синка и пометки «не найдена при синке»
- Подпись строки группы (в макете «N участников» — данных нет)
- Показ «в N расписаниях» в подписи группы
- Взаимодействие поиска с бесконечной прокруткой
- Мобильная раскладка в пределах брейкпоинтов макета
- Судьба GET/DELETE/PATCH /api/groups (проверить потребителей)
- Точка встраивания пропуска выключенных групп в диспетчеризацию

## Deferred Ideas

- Статистика отправок по группе — после Фазы 4 (агрегации SendLog)
- Bulk-операции над группами — отдельная задача при потребности
- Синк одной группы — требует расширения протоколов воркеров, вне v2.0
- Ручное добавление группы — отменено владельцем; возврат возможен отдельной фазой
