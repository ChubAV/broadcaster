---
phase: 03-gruppy-akkaunta
plan: 12
subsystem: planning
tags: [security-register, threat-model, alembic, validation-matrix, research, knowledge-graph]

# Dependency graph
requires:
  - phase: 03-gruppy-akkaunta
    provides: "регистр угроз после плана 03-09 (T-03-15/T-03-28 закрыты, T-03-36 заведена), исходники после планов 03-09/03-10/03-11, ревизия `0015_groups_unique_account_external.py` и её восемь тестов"
provides:
  - "Строка регистра T-03-37 — разрушительная ревизия 0015 получила собственный идентификатор угрозы с записанной дисциплиной применения"
  - "Дополненная формулировка T-03-06: причина сужения скоупа выборки со ссылкой на IN-09 (`fb14859`) и на комментарий в коде"
  - "Пометка об авторитетности регистра относительно двух запечатанных артефактов фазы (`03-02-SUMMARY.md:316`, `03-VALIDATION.md:57`)"
  - "Закрытый раздел переходящих находок, итоговая строка Security Audit Trail (37/36/1) и датированный Sign-Off"
  - "Пять дописанных строк матрицы `## Per-Task Verification Map` (03-09-01, 03-09-02, 03-10-01, 03-10-02, 03-11-01) и абзац-дополнение к «Покрытию карты»"
  - "Заголовок `## Open Questions (RESOLVED)` и три строки разрешения в `03-RESEARCH.md`"
  - "Перестроенный граф знаний `graphify-out/` — единственное перестроение за фазу"
affects: [шаг закрытия фазы 03, будущий прогон /gsd-validate-phase, будущий аудит безопасности, выкат ревизий 0013..0015 на целевую базу]

actuals:
  tokens: 6377
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Расхождение между регистром и запечатанным артефактом маркируется пометкой об авторитетности, а не правкой артефакта"
    - "Матрица валидации расширяется ТОЛЬКО дописыванием снизу; аддитивность доказывается счётом строк на `-` в `git diff HEAD -U0`"

key-files:
  created:
    - .planning/phases/03-gruppy-akkaunta/03-12-SUMMARY.md
  modified:
    - .planning/phases/03-gruppy-akkaunta/03-SECURITY.md
    - .planning/phases/03-gruppy-akkaunta/03-VALIDATION.md
    - .planning/phases/03-gruppy-akkaunta/03-RESEARCH.md

key-decisions:
  - "Номера строк ревизии 0015 записаны в регистр только после прогона по живому файлу: `_DROP_DUPLICATES` объявлен на :208-216, сама строка `DELETE FROM groups` — :210, исполнение — :224. Слипание объявления и исполнения в один диапазон было бы ровно той ложной формулировкой, которую запрещает прохибиция плана"
  - "Дисциплина применения для 0015 записана как ОБЯЗАТЕЛЬСТВО перед выкатом, а не как отчёт: целевая база остаётся на ревизии 0012, и запись это называет прямо"
  - "Раздел переходящих находок снят вместе с заголовком, а строка-заключение поставлена в конец раздела открытых угроз: пустой заголовок читался бы как «находки есть, но не записаны», а новый заголовок противоречил бы прямому указанию плана снять заголовок"
  - "`graphify update` выполнен по основному чекауту `/source/broadcaster`, а не по дереву воркtree: `graphify-out/` в `.gitignore:54-56`, в воркtree его нет, и перестроение там было бы уничтожено вместе с воркtree"

patterns-established:
  - "Гейт-совместимость формулировок: прозаическое упоминание идентификаторов (`R-03-01..R-03-08`) может сломать построчный `grep -c`-гейт того же файла — счёт ведётся по строкам, а не по вхождениям"

requirements-completed: []

coverage:
  - id: D1
    description: "Ревизия 0015 присутствует в регистре собственным идентификатором с категорией, severity, disposition и mitigation"
    verification:
      - kind: docs-gate
        ref: "grep -c 'T-03-37' .planning/phases/03-gruppy-akkaunta/03-SECURITY.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "Каждый номер строки, названный в T-03-37, подтверждается прогоном по живому файлу ревизии"
    verification:
      - kind: docs-gate
        ref: "grep -n 'DELETE FROM groups' + grep -n '_DROP_DUPLICATES' alembic/versions/0015_groups_unique_account_external.py"
        status: pass
    human_judgment: false
  - id: D3
    description: "Раздел переходящих находок снят, обе находки закрыты по существу, журнал принятых рисков не тронут"
    verification:
      - kind: docs-gate
        ref: "test \"$(grep -cE '^## Unregistered' 03-SECURITY.md)\" -eq 0; grep -c 'R-03-0' == 8"
        status: pass
    human_judgment: false
  - id: D4
    description: "Правка матрицы валидации чисто аддитивная: пять новых строк, ни одна существующая не изменена"
    verification:
      - kind: docs-gate
        ref: "git diff HEAD -U0 -- 03-VALIDATION.md | grep -c '^-' == 1"
        status: pass
    human_judgment: false
  - id: D5
    description: "Столбец Automated Command пяти новых строк посимвольно равен первой команде <automated> своей задачи-источника"
    verification:
      - kind: docs-gate
        ref: "alternation-grep по пяти идентификаторам задач == 5"
        status: pass
    human_judgment: false
  - id: D6
    description: "Открытые вопросы исследования помечены разрешёнными; исходные формулировки сохранены дословно"
    verification:
      - kind: docs-gate
        ref: "grep -qE '^## Open Questions \\(RESOLVED\\)$'; git diff HEAD -U0 показывает единственное удаление — прежний заголовок"
        status: pass
    human_judgment: false
  - id: D7
    description: "Граф знаний перестроен и наблюдает удаление app/repositories/group.py"
    verification:
      - kind: docs-gate
        ref: "grep -c 'app/repositories/group.py' graphify-out/graph.json == 0"
        status: pass
    human_judgment: false
  - id: D8
    description: "Формулировка T-03-37 не переоценивает реализованный контроль и не выдаёт обязательство за факт"
    verification: []
    human_judgment: true
    rationale: "Соответствие текста регистра фактическому положению — суждение аудитора: греп подтверждает наличие подстрок, но не то, что формулировка честна относительно невыкаченной миграции"

# Metrics
duration: ~30min
completed: 2026-08-13
status: complete
---

# Phase 03 Plan 12: Закрытие переходящих находок регистра угроз Summary

**Самая разрушительная миграция фазы перестала быть невидимой для аудита: ревизия 0015 получила собственный идентификатор угрозы T-03-37 с проверенными номерами строк и честной пометкой, что выкат ещё впереди; матрица Найквиста описывает фактический состав задач фазы, а граф знаний перестроен один раз и наблюдает удаление снесённого файла репозитория.**

## Performance

- **Duration:** ~30 min (из них 12 мин 27 с — фоновый прогон полной суиты)
- **Tasks:** 3
- **Files modified:** 3 планировочных, 0 файлов кода
- **Commits:** 3

## Accomplishments

- **Ревизия 0015 больше не невидима.** Единственная запись категории «уничтожение данных», T-03-09, сознательно ограничивает себя ревизией 0014 словами «только additive nullable, без data-migration», и до этого плана аудитор читал это как характеристику всей фазы. Теперь рядом стоит T-03-37 (high, mitigate, closed), называющая слияние строк через `UPDATE`, перезапись `schedules.group_ids`, `DELETE FROM groups` и построение уникального ограничения под `ACCESS EXCLUSIVE`.
- **Каждый номер строки в новой записи сверен с файлом перед записью, а не перенесён из плана.** `_DROP_DUPLICATES` объявлен на `:208-216`, сама строка `DELETE FROM groups` — `:210`, исполнение — `:224`, ограничение — `:226-229`, порядок операций `upgrade` — `:219-229`. Исходная находка называла диапазон `:210-224`, слипавший объявление с исполнением.
- **Дисциплина применения записана как обязательство, а не как отчёт.** Целевая база остаётся на ревизии `0012` (`.planning/STATE.md`, Blockers/Concerns), то есть ни 0013, ни 0014, ни 0015 не выкачены. Запись называет это прямо, чтобы будущий читатель не принял её за отчёт о применённой миграции.
- **Формулировка T-03-06 сверена с кодом и подтверждена верной.** Строка регистра уже описывала реализованный контроль (скоуп только по `account_id`); переписывать было нечего. Вместо этого добавлена причина сужения — правка IN-09 (`fb14859`) привела скоуп поиска в соответствие со скоупом ограничения `uq_groups_account_external`, потому что более широкое условие превращало безобидное расхождение данных в отказ синхронизации всего аккаунта.
- **Расхождение с запечатанными артефактами промаркировано, а не скрыто.** Пометка об авторитетности под таблицей регистра называет `03-02-SUMMARY.md:316` и `03-VALIDATION.md:57` поимённо и объясняет, почему они не правятся. Ни один PLAN и ни один SUMMARY планов 03-01..03-08 не изменён.
- **Матрица валидации описывает фактический состав задач фазы.** Дописаны пять строк реализационных задач планов закрытия пробелов; каждая несёт ПЕРВУЮ команду `<automated>` своей задачи-источника, сверенную с планом посимвольно. Правка чисто аддитивная — в `git diff HEAD -U0` по файлу ровно одна строка на `-`, и это заголовок diff.
- **Граф знаний перестроен один раз за фазу.** До перестроения `graph.json` содержал узлы для `app/repositories/group.py`, удалённого планом 03-11; после — ноль вхождений. 5690 узлов, 11938 рёбер, 350 сообществ.

## Task Commits

1. **Task 1: Ревизия 0015 получает собственный идентификатор угрозы** — `9d134d3` (docs)
2. **Task 2: Сверка T-03-06, закрытие раздела находок, журнал аудита** — `637fe8f` (docs)
3. **Task 3: Матрица валидации, открытые вопросы, перестроение графа** — `b0a0cfb` (docs)

## Files Created/Modified

- `.planning/phases/03-gruppy-akkaunta/03-SECURITY.md` — строка T-03-37, дополненная Mitigation строки T-03-06, пометка об авторитетности под таблицей регистра, снятый раздел переходящих находок и строка-заключение вместо него, третья строка Security Audit Trail (37/36/1), датированный Sign-Off с подтверждением каждой галочки
- `.planning/phases/03-gruppy-akkaunta/03-VALIDATION.md` — пять дописанных строк матрицы и абзац-дополнение к «Покрытию карты»
- `.planning/phases/03-gruppy-akkaunta/03-RESEARCH.md` — заголовок `## Open Questions (RESOLVED)`, вводный абзац и три строки разрешения
- `graphify-out/` (основной чекаут, gitignored) — перестроенный граф знаний

## Decisions Made

- **Арифметика счётчиков получена пересчётом по таблице, а не переносом из плана.** `grep -cE '^\| T-03-[0-9]+ \|'` даёт 37 строк, из них 36 оканчиваются статусом `closed` и одна (T-03-36) — открытая. Числа сошлись с ожиданием плана; расхождения, которое требовало бы остановки, не возникло.
- **`threats_open: 0` и `status: verified` во frontmatter не тронуты.** Единственная открытая угроза имеет severity `medium` — ниже настроенного порога `block_on: high`.
- **Пять команд матрицы сверены с планами-источниками, а не переписаны из таблицы плана.** У всех пяти задач-источников блок `<verify>` содержит больше одной команды; правило «берётся первая» применено, и каждая из пяти совпала с выписанной в плане. Расхождения, означавшего бы изменение плана-источника после планирования, не обнаружено.
- **Требование строки `03-11-01` — прочерк.** Поле `requirements` плана 03-11 пусто СОЗНАТЕЛЬНО (`03-11-PLAN.md:20`), и заимствовать чужой `GRP-*` значило бы приписать удалению мёртвого слоя закрытие требования.
- **Три разрешения исследования перепроверены прогоном по живому дереву перед записью:** `ls app/routes/` — файла групп нет; `app/pages/account_groups.py` существует; подпись «в N расписаниях» рендерится в `group_row.html:40`, ветка нуля — `:42`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Точность ссылки] Диапазон комментария в дополнении к T-03-06**

- **Найдено при:** Task 2
- **Проблема:** план предписывал сослаться на комментарий `group_resync.py:175-190`. Чтение файла показало, что комментарий-обоснование сужения скоупа занимает `:172-184`, а `:185-190` — это сама выборка `select(Group).where(...)`, не комментарий.
- **Исправление:** в регистр записаны оба подтверждённых диапазона раздельно — «обоснование записано комментарием `group_resync.py:172-184`, сама выборка — `:185-190`».
- **Почему это не отступление от замысла:** прохибиция №1 плана требует сверять каждую ссылку с файлом перед записью и прямо называет ложную ссылку худшей, чем отсутствующая. Запись непроверенного диапазона нарушила бы её.
- **Файлы:** `.planning/phases/03-gruppy-akkaunta/03-SECURITY.md`
- **Коммит:** `637fe8f`

**2. [Rule 3 - Блокирующее] Перестроение графа выполнено по основному чекауту**

- **Найдено при:** Task 3
- **Проблема:** `graphify-out/` находится в `.gitignore` (`:54-56`) и в дереве воркtree отсутствует. `graphify update .`, выполненный внутри воркtree, построил бы граф в каталог, который оркестратор уничтожает вместе с воркtree принудительным удалением. Гейт вернул бы код 0, не доказав ничего, — ровно та ложная зелень, против которой направлен этот план.
- **Исправление:** выполнено `graphify update /source/broadcaster`. Основной чекаут несёт то же дерево после слияния первой волны (обе проверки — отсутствие `app/repositories/group.py` — сошлись), а `graphify-out/` gitignored и потому загрязнить слияние не может.
- **Проверка результата:** до перестроения `grep -c 'app/repositories/group.py' graphify-out/graph.json` давал ненулевой счёт (узлы снесённого планом 03-11 файла оставались в графе); после — 0. Именно этот эффект план и называл смыслом задачи.
- **Побочное наблюдение:** флаг `--force` (нужный, когда перестроение даёт меньше узлов после удаления кода) не потребовался — суммарное число узлов выросло до 5690.
- **Файлы:** `graphify-out/` в основном чекауте (gitignored, в коммит не входит)
- **Коммит:** отражено в `b0a0cfb`

**3. [Rule 1 - Гейт] Формулировка Sign-Off ломала собственный гейт плана**

- **Найдено при:** Task 2
- **Проблема:** первая редакция абзаца Sign-Off содержала перечисление `R-03-01..R-03-08`. Критерий приёмки требует, чтобы `grep -c "R-03-0"` возвращал 8 (журнал принятых рисков не тронут), а `grep -c` считает СТРОКИ: добавленная прозаическая строка дала 9 и покрасила бы верный исход.
- **Исправление:** перечисление заменено на «те же восемь подписанных владельцем записей». Счёт вернулся к 8; журнал принятых рисков при этом не тронут ни одной строкой, что и утверждает гейт.
- **Файлы:** `.planning/phases/03-gruppy-akkaunta/03-SECURITY.md`
- **Коммит:** `637fe8f`

### Решение по форме, оставленное планом исполнителю

- **Раздел переходящих находок снят вместе с заголовком, без замещающего заголовка.** План требовал удалить раздел «целиком — вместе с заголовком» и заменить одной строкой-заключением, но при этом запрещал оставлять пустой заголовок. Заключение поставлено в конец раздела открытых угроз отдельным абзацем: новый заголовок противоречил бы прямому указанию, а абзац без заголовка выполняет обе части требования. Формулировка написана своими словами и слово `Unregistered` не цитирует — иначе греп-гейт `^## Unregistered` остался бы красным навсегда.

## Issues Encountered

- **Полная суита идёт 12 мин 27 с** — дольше одного окна выполнения команды; прогон выполнен фоново параллельно с подготовкой задачи 3. Результат: **1094 passed**, 0 failed, код 0. План кода не менял, и суита осталась в том же состоянии (после первой волны их 1094 против 1090 на момент 03-09).

## Known Stubs

None — заглушек не оставлено. План не менял ни строки кода приложения.

## Threat Flags

None — новой поверхности план не создаёт: ни один файл приложения им не правится. Единственная новая строка регистра, T-03-37, описывает УЖЕ СУЩЕСТВУЮЩУЮ ревизию, а не введённую этим планом.

## User Setup Required

None для самого плана. Отдельно напоминается зафиксированное регистром обязательство: **выкат ревизий 0013..0015 на целевую базу остаётся впереди** (целевая база на `0012`), и дисциплина применения для 0015 — одноразовая база, программный guard по `hostname`/`port`/`dbname`, проверенный на отказ, целевая база не адресуется ни одной командой — записана в T-03-37 как обязательство перед выкатом.

## Next Phase Readiness

- `03-SECURITY.md` открытых угроз выше порога `block_on: high` не содержит: единственная открытая T-03-36 имеет severity `medium`, `threats_open: 0` сохранено.
- Переходящих находок без идентификатора в регистре не осталось — раздела больше нет.
- Матрица `03-VALIDATION.md` покрывает 25 задач в 11 планах; будущий `/gsd-validate-phase` не читает пробела, который не может никому приписать.
- Граф знаний перестроен и актуален относительно всех правок фазы.
- Бухгалтерия закрытия фазы (статусы GRP-04..GRP-07 в `REQUIREMENTS.md`, строка фазы 3 в таблице покрытия `ROADMAP.md`) этим планом СОЗНАТЕЛЬНО не тронута — это работа шага закрытия фазы.
- Флаги планировщика перенесены без разрешения: детерминированный edge-пробник вернул GRP-04..GRP-07 как `unclassified / unresolved`. Настоящий план кода не касался и разрешить их не мог.

## Self-Check: PASSED

- Все три изменённых файла присутствуют, изменения зафиксированы; `03-12-SUMMARY.md` создан.
- Коммиты `9d134d3`, `637fe8f`, `b0a0cfb` найдены в истории ветки.
- Гейты задачи 1: `grep -c "T-03-37"` = 1; `grep -n "0015_groups_unique_account_external"` находит строку регистра; `grep -n 'DELETE FROM groups'` = `:210` и `grep -n '_DROP_DUPLICATES'` = `:208`/`:224` совпадают с записанным; `git diff HEAD` строку T-03-09 не показывает.
- Гейты задачи 2: `grep -cE '^## Unregistered'` = 0; `grep -c "R-03-0"` = 8; `threats_open: 0` на месте; `git status --porcelain` по каталогу фазы не показывает ни одного `03-0[1-8]-PLAN.md` и ни одного `03-0[1-8]-SUMMARY.md`; `git diff HEAD -- 03-VALIDATION.md` после задачи 2 был пуст; в Security Audit Trail три строки прогонов, и Threats Total последней (37) равен пересчитанному числу строк регистра; `uv run pytest tests/ -q` — код 0.
- Гейты задачи 3: пять строк матрицы — счёт 5; альтернация по командам — счёт 5; `git diff HEAD -U0 -- 03-VALIDATION.md | grep -c '^-'` = 1 (только заголовок diff); `grep -qE '^## Open Questions \(RESOLVED\)$'` — код 0; `grep -cE '^## Open Questions$'` = 0; в diff `03-RESEARCH.md` удалена ровно одна строка — прежний заголовок; `graphify update` — код 0, `grep -c 'app/repositories/group.py' graphify-out/graph.json` = 0.
- Пересчёт матрицы: 25 строк задач, 11 различных планов — числа абзаца-дополнения подтверждены, а не приняты на веру.

---
*Phase: 03-gruppy-akkaunta*
*Completed: 2026-08-13*
