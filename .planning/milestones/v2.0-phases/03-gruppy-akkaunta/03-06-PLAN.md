---
phase: 03-gruppy-akkaunta
plan: 06
type: execute
wave: 3
depends_on: ["03-04", "03-05"]
files_modified:
  - app/pages/account_groups.py
  - app/templates/account_groups/list.html
  - app/templates/account_groups/partials/sync_result.html
  - tests/test_pages/test_account_groups.py
  - tests/test_pages/test_htmx_preserved.py
autonomous: true
requirements: [GRP-07]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Пользователь запускает повторную синхронизацию кнопкой «Синхронизировать всё» и видит её результат, не покидая экрана (GRP-07, D-09)"
    - "Сводка синка показывает найдено, новых и обновлено имён; при непустом числе пропавших добавляется и оно (D-09)"
    - "Провал синхронизации показывает текст ошибки и следующий шаг вместо сводки (D-09)"
    - "Результат последнего синка виден при перезаходе на экран — он читается из аккаунта, а не из памяти запроса (D-09)"
    - "Фоновый синк WA и MAX добирается самоостанавливающимся опросом: атрибуты опроса присутствуют только в ветке выполнения и исчезают вместе с ней (D-09)"
    - "Кнопки повторной синхронизации отдельной группы на экране нет — протокола синхронизации одной группы у воркеров не существует (D-12)"
    - "Плашка результата и панели подтверждения живут вне подменяемого опросом элемента"
    - "Незаполненное время последней синхронизации даёт строку «синхронизация ещё не выполнялась» в строке идентичности, а не пустое место и не выдуманное «0 минут назад» (E1 empty)"
    - "Во время синхронизации строка идентичности читается «синхронизация идёт сейчас», подпись действия — «Синхронизация…», повторная отправка отклоняется существующим guard-ом (E1 loading)"
    - "Провал синхронизации показывает бейдж статуса существующим словарём экрана аккаунтов, а текст ошибки и следующий шаг несёт плашка (E1 error)"
    - "Обычный вид шапки — плашка иконки в цвете канала, имя заголовком, строка идентичности, бейдж статуса и два действия, всё из уже загруженного аккаунта без дополнительных запросов (E1 populated)"
    - "Отсутствующий телефон убирает и сегмент, и разделитель — строка идентичности не начинается с осиротевшей точки (E1 partial)"
    - "Шапка переносит действия на свою строку на узких ширинах; ничего не обрезается и не прокручивается вбок (E1 overflow)"
    - "Шапка ровно одна на экран и рендерится безусловно для аккаунта-владельца — вариантов «ноль» и «много» не существует (E1 zero-one-many)"
    - statement: "Имя аккаунта длиной 60 символов обрезается в колонке идентичности, не выталкивая бейдж статуса и действия — проверяется на ширине 320 пикселей"
      verification: backstop
    - "Нет сохранённого результата и синхронизация не идёт — плашка не рендерится вовсе; пустой оболочки плашки не бывает (E2 empty)"
    - "Строка «Синхронизация выполняется…» живёт в подменяемом блоке, атрибуты опроса объявлены только в ветке выполнения, и это закреплено парой тестов на продолжение и остановку опроса (E2 loading)"
    - "Сохранённая ошибка даёт плашку ошибки с инструкцией повтора; испорченная сохранённая строка результата приводит к отсутствию плашки, а не к стек-трейсу (E2 error)"
    - "Успешная сводка печатает три счётчика; число пропавших появляется только когда оно больше нуля (E2 populated)"
    - "Плашка отражает только последнюю ЗАВЕРШЁННУЮ синхронизацию: пока идёт новая, старая сводка остаётся, а состояние выполнения несёт подменяемый блок (E2 partial)"
    - "Счётчики сводки — ограниченные числа, плашка занимает полную ширину и переносит свой текст (E2 overflow)"
    - "Плашка рендерится в единственном числе — результаты не накапливаются стопкой (E2 zero-one-many)"
    - statement: "Длинная строка ошибки воркера переносится внутри плашки на несколько строк, не расширяя страницу и не обрезаясь — проверяется на ширине 320 пикселей со строкой в 300 символов"
      verification: backstop
  artifacts:
    - path: "app/templates/account_groups/partials/sync_result.html"
      provides: "Подменяемый блок статуса синхронизации с условным опросом"
      min_lines: 20
    - path: "app/pages/account_groups.py"
      provides: "Вход статуса синхронизации экрана и разбор сохранённого результата"
  key_links:
    - from: "app/templates/account_groups/partials/sync_result.html"
      to: "app/pages/account_groups.py"
      via: "условный адрес опроса ведёт на вход статуса того же экрана"
      pattern: "groups/sync-status"
    - from: "app/pages/account_groups.py"
      to: "app/application/accounts/group_resync.py"
      via: "разбор сохранённой строки результата защищённым парсером"
      pattern: "parse_sync_result"
---

<objective>
Результат синхронизации становится видимым: кнопка «Синхронизировать всё» в шапке, сводка-плашка «найдено N, новых M, обновлено имён K» либо текст ошибки, а для фоновых WA и MAX — самоостанавливающийся опрос статуса, не покидая экрана.

Purpose: замыкает GRP-07 со стороны пользователя. Результат читается из сохранённого на аккаунте значения, поэтому виден и при перезаходе (D-09). Опрос обязан быть самоостанавливающимся: атрибуты запроса и триггера ставятся ТОЛЬКО в ветке выполнения — иначе каждая открытая вкладка превращается в бесконечный поток запросов каждые пять секунд.
Output: подменяемый блок статуса `account_groups/partials/sync_result.html`, вход статуса экрана, плашка результата и действие запуска синхронизации в шапке, парные тесты продолжения и остановки опроса.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-gruppy-akkaunta/03-CONTEXT.md
@.planning/phases/03-gruppy-akkaunta/03-RESEARCH.md
@.planning/phases/03-gruppy-akkaunta/03-PATTERNS.md
@.planning/phases/03-gruppy-akkaunta/03-UI-SPEC.md
@.planning/phases/03-gruppy-akkaunta/03-04-SUMMARY.md
@.planning/phases/03-gruppy-akkaunta/03-05-SUMMARY.md
@.planning/phases/01-interfeysnyy-fundament/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Плашка результата синхронизации и действие запуска в шапке</name>
  <precondition>Планы 03-04 и 03-05 завершены: аккаунт получает заполненные `last_synced_at` и `last_sync_result` после синхронизации, а шапка экрана и секция стилей уже существуют.</precondition>
  <read_first>
    - app/pages/account_groups.py — состояние обработчика страницы после планов 03-01 и 03-05
    - app/templates/account_groups/list.html — состояние шапки и порядок блоков страницы
    - app/application/accounts/group_resync.py — parse_sync_result и форма JSON-строки результата
    - app/templates/components/alert.html — сигнатура макроса alert
    - app/templates/components/button.html — сигнатура button и набор доступных значков
    - .planning/phases/03-gruppy-akkaunta/03-UI-SPEC.md — раздел Copywriting Contract, подраздел Sync result plashka, и раздел Color (акцент разрешён только главному действию экрана)
  </read_first>
  <files>app/pages/account_groups.py, app/templates/account_groups/list.html, tests/test_pages/test_account_groups.py</files>
  <action>
Обработчик страницы читает `account.last_sync_result` через защищённый парсер `parse_sync_result` и кладёт в контекст разобранный словарь либо `None`. Испорченное значение обязано давать отсутствие плашки, а не исключение и не стек-трейс на экране.

В шапку экрана добавить главное действие — настоящую форму запроса на существующий вход синхронизации `POST /accounts/{account_id}/sync-groups` с кнопкой «Синхронизировать всё» основного варианта и значком обновления. Это единственное место экрана, которому разрешён акцентный цвет наряду с включённым тумблером, кольцом фокуса и подписью выполняющейся синхронизации. При статусе выполнения подпись читается «Синхронизация…», значок в состоянии вращения, повторная отправка отклоняется существующим guard-ом на стороне обработчика.

Кнопки синхронизации ОТДЕЛЬНОЙ группы на экране не появляется (D-12): протокола синхронизации одной группы у воркеров не существует, а протоколы фаза не трогает. Действия добавления группы вручную на экране тоже нет (D-13).

Плашка результата рендерится НАД списком и ПОД шапкой, макросом `alert`:
- успех — «Синхронизация завершена: найдено {found}, новых {new}, обновлено имён {renamed}» варианта успеха; сегмент «, не найдено {missing}» дописывается только при значении больше нуля, чтобы «не найдено 0» не рендерилось никогда;
- ошибка — «Синхронизация не удалась: {error}. Нажмите «Синхронизировать всё», чтобы повторить.» варианта ошибки;
- нет сохранённого результата — плашки нет вовсе; строку «синхронизация ещё не выполнялась» несёт строка идентичности шапки.

Плашка живёт ВНЕ элемента, который в задаче 2 будет заменяться опросом, и вне любой панели подтверждения: элемент, заменяемый целиком, приносит с каждым ответом свои дочерние блоки, и вложенная в него плашка дублировалась бы или исчезала. Плашка ровно одна: результаты не накапливаются.

Текст ошибки приходит из внешней системы и выводится обычным выводом под autoescape — готовая разметка макросу не передаётся.

Тестами закрепить: успешный результат печатает три числа; сегмент про не найденные отсутствует при нулевом значении и присутствует при ненулевом; сохранённая ошибка печатает текст ошибки и инструкцию повтора; испорченная строка результата даёт страницу без плашки и код ответа 200; шапка содержит форму запроса на вход синхронизации; на экране нет ни одного действия синхронизации, адресованного отдельной группе.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/ tests/test_templates/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'parse_sync_result' app/pages/account_groups.py`
    - `grep -q 'sync-groups' app/templates/account_groups/list.html` — действие запуска синхронизации присутствует
    - страница аккаунта с сохранённым успешным результатом содержит все три числа сводки
    - страница аккаунта с нулевым числом пропавших групп не содержит текста «не найдено 0»
    - страница аккаунта с ненулевым числом пропавших групп содержит сегмент с этим числом
    - страница аккаунта с сохранённой ошибкой содержит её текст и слово «повторить»
    - страница аккаунта с испорченной строкой результата отвечает кодом 200 и плашки не содержит
    - `grep -c 'groups/[^\"]*/sync' app/templates/account_groups/includes/group_row.html` возвращает 0 — действия синхронизации отдельной группы в строке нет
    - `uv run pytest tests/test_pages/test_account_groups.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Пользователь запускает повторную синхронизацию с экрана и видит её результат — сводку либо ошибку — не покидая экрана; результат переживает перезаход, потому что читается из аккаунта.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Самоостанавливающийся опрос статуса фоновой синхронизации</name>
  <read_first>
    - app/templates/accounts/partials/sync_status_card.html — эталон приёма целиком: комментарий-инвариант о механизме остановки, условие внутри открывающего тега, ветвление по статусу
    - app/pages/accounts.py (строки 661-693) — как обработчик рендерит паршал опроса из шаблона напрямую и что возвращает вне известных статусов
    - app/application/accounts/use_cases.py — get_sync_status_view и её DTO
    - tests/test_pages/test_htmx_preserved.py — парные тесты продолжения и остановки опроса как образец и как место дополнения
    - app/templates/account_groups/list.html — состояние страницы после задачи 1: куда встаёт подменяемый блок относительно плашки
    - .planning/phases/01-interfeysnyy-fundament/01-06-SUMMARY.md — четыре инварианта приёма и урок о панелях внутри подменяемого элемента
  </read_first>
  <behavior>
    Тесты пишутся ПЕРВЫМИ (дополняют `tests/test_pages/test_account_groups.py` и `tests/test_pages/test_htmx_preserved.py`) и должны падать до реализации:
    - страница аккаунта в статусе выполнения синхронизации содержит атрибуты запроса и триггера опроса
    - страница аккаунта в статусе активности атрибутов опроса НЕ содержит
    - страница аккаунта в статусе провала синхронизации атрибутов опроса НЕ содержит
    - вход статуса экрана для аккаунта в статусе выполнения возвращает разметку С атрибутами опроса
    - вход статуса экрана для аккаунта, перешедшего в активное состояние, возвращает разметку БЕЗ атрибутов опроса — это и есть механизм остановки
    - вход статуса экрана для чужого аккаунта не отдаёт разметки статуса
    - вход статуса экрана без cookie-сессии не отдаёт разметки статуса
    - панель подтверждения удаления не встречается внутри подменяемого блока
  </behavior>
  <files>app/pages/account_groups.py, app/templates/account_groups/partials/sync_result.html, tests/test_pages/test_account_groups.py, tests/test_pages/test_htmx_preserved.py</files>
  <action>
Создать `app/templates/account_groups/partials/sync_result.html` — МИНИМАЛЬНЫЙ подменяемый блок статуса: бейдж статуса и строка состояния выполнения, и ничего более. Плашка результата, панели подтверждения и список групп в него НЕ входят.

Атрибуты опроса — адрес запроса, триггер каждые пять секунд и режим замены элемента целиком — ставятся ВНУТРИ открывающего тега под условием статуса выполнения. Команды «стоп» у опроса не существует: он прекращается тем, что очередной ответ приходит без этих атрибутов. Идентификатор элемента и адрес запроса стоят на ОДНОМ элементе, и заменяется он целиком: перенос идентификатора на обёртку или на дочерний элемент рвёт подмену молча — страница остаётся исправной с виду, а обновление статуса перестаёт работать.

Комментарий-инвариант в шапке файла обязателен и переносится по смыслу с эталона: описать механизм остановки, назвать четыре инварианта приёма и сослаться на парные тесты продолжения и остановки опроса как на спецификацию этого файла.

Ветвление по статусу — тем же словарём, что у экрана аккаунтов: активен, провал синхронизации, выполняется.

Блок объявляет доступное для вспомогательных технологий вежливое оповещение, чтобы завершение синхронизации было объявлено без перехвата фокуса.

Добавить в `app/pages/account_groups.py` вход `GET /accounts/{account_id}/groups/sync-status`, отдающий этот блок: аутентификация, затем владение аккаунтом; вне известных статусов — пустой ответ. Рендер выполняется получением шаблона из окружения и его рендером напрямую, по образцу существующего входа статуса экрана аккаунтов; параметр компоновки принимается и игнорируется.

Встроить блок в страницу так, чтобы он стоял рядом с плашкой результата, а не внутри неё, и чтобы ни одна панель подтверждения не оказалась внутри него.

Обязательна ПАРА тестов: опрос присутствует при статусе выполнения и опрос отсутствует при остальных статусах. Одиночного теста присутствия недостаточно — именно отсутствие второго теста в прошлом позволяло вечному опросу проходить проверку.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_account_groups.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_preserved.py -q</automated>
    <automated>uv run pytest tests/test_pages/ tests/test_routes/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/account_groups.py` содержит `async def account_groups_sync_status(`
    - `grep -c 'hx-trigger' app/templates/account_groups/partials/sync_result.html` возвращает 1 — единственное объявление опроса
    - `grep -q 'syncing' app/templates/account_groups/partials/sync_result.html` — объявление опроса стоит под условием статуса
    - HTML страницы аккаунта в статусе выполнения содержит `hx-trigger`; HTML той же страницы в активном статусе `hx-trigger` не содержит
    - ответ входа статуса для активного аккаунта не содержит `hx-get`
    - ответ входа статуса для чужого аккаунта не содержит бейджа статуса этого аккаунта
    - идентификатор панели подтверждения удаления не встречается в теле `app/templates/account_groups/partials/sync_result.html`
    - `uv run pytest tests/test_pages/test_htmx_preserved.py -q` завершается кодом 0
  </acceptance_criteria>
  <done>Статус фоновой синхронизации WA и MAX добирается опросом, который сам себя останавливает; отсутствие опроса вне статуса выполнения закреплено отдельным тестом, а плашка и панели подтверждения остаются вне подменяемого блока.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → вход статуса синхронизации | `account_id` из адреса, запрос повторяется автоматически каждые пять секунд |
| сохранённая строка результата → разметка | текст ошибки внешней системы попадает в HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-25 | Elevation of Privilege | `GET /accounts/{account_id}/groups/sync-status` | high | mitigate | Аутентификация и владение аккаунтом проверяются в самом входе; для чужого аккаунта разметка статуса не отдаётся. Тест обязателен и пишется до реализации |
| T-03-26 | Denial of Service | автоматический опрос статуса | high | mitigate | Атрибуты запроса и триггера объявлены ТОЛЬКО в ветке статуса выполнения; остановка доказывается парой тестов — присутствие при выполнении и отсутствие при остальных статусах. Без парного теста вечный опрос проходит проверку незамеченным |
| T-03-27 | Tampering | текст ошибки из сохранённого результата | medium | mitigate | Вывод под autoescape, разметка макросу не передаётся; испорченное сохранённое значение даёт отсутствие плашки, а не исключение |
| T-03-28 | Denial of Service | повторный запуск синхронизации с экрана | medium | mitigate | Существующий guard по статусу выполнения на стороне обработчика синхронизации; подпись действия при выполнении отражает состояние, второй одновременный запуск не ставится |

Установок пакетов фаза не содержит — строка `T-{phase}-SC` неприменима.
</threat_model>

## Artifacts this phase produces

- Файл `app/templates/account_groups/partials/sync_result.html`
- Обработчик `account_groups_sync_status` и маршрут `GET /accounts/{account_id}/groups/sync-status`
- Плашка результата синхронизации и главное действие «Синхронизировать всё» в шапке экрана

## Planner assumptions (flagged, not resolved)

- Зондирование границ по GRP-07 вернуло строку `unclassified` («review manually»): категория границ этого требования классификатором не определена. Строка НЕ разрешена автоматически и остаётся открытым предположением планировщика — просмотр человеком при верификации фазы обязателен.

<verification>
- `uv run pytest tests/test_pages/test_account_groups.py tests/test_pages/test_htmx_preserved.py -q` — зелено
- `uv run pytest tests/ -q` — суита не деградировала
- Опрос объявлен ровно в одном месте и только под условием статуса выполнения
- Плашка результата и панели подтверждения находятся вне подменяемого блока
</verification>

<success_criteria>
- GRP-07 полностью: пользователь повторно синхронизирует группы аккаунта и видит результат синхронизации, не покидая экран
- D-09 и D-12 реализованы: сводка на экране, результат переживает перезаход, кнопки синхронизации отдельной группы нет
</success_criteria>

<output>
Create `.planning/phases/03-gruppy-akkaunta/03-06-SUMMARY.md` when done
</output>
