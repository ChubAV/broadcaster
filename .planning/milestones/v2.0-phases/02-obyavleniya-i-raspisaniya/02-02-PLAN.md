---
phase: 02-obyavleniya-i-raspisaniya
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/routes/uploads.py
  - app/pages/ads.py
  - app/routes/ads.py
  - app/pages/schedules.py
  - app/routes/schedules.py
  - tests/test_routes/test_uploads.py
  - tests/test_routes/test_ads.py
  - tests/test_routes/test_schedules.py
  - tests/test_pages/test_ads_image_ownership.py
  - tests/test_pages/test_schedule_ownership.py
autonomous: true
requirements: [ADS-05, ADS-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Загрузка файла с подделанным заголовком типа отклоняется: тип определяется по содержимому, а не по значению, присланному клиентом (CR-02)"
    - "Сохранение объявления с ключом изображения вне префикса владельца отклоняется на всех четырёх входах — страничных create/update и JSON-API create/update (WR-01)"
    - "Сохранение объявления с числом вложений больше `settings.max_images_per_ad` отклоняется сервером, а не только браузером (D-13)"
    - "Постановка расписания на чужое объявление отклоняется на обоих входах — страничном и JSON-API (CR-01)"
    - "Постановка расписания на чужой аккаунт мессенджера отклоняется на обоих входах (CR-01/D-20)"
    - "Гейт код-ревью `01-REVIEW.md` (status: issues_found, critical: 2) закрыт: CR-01, CR-02 и WR-01/T-10-04 более не воспроизводятся"
  artifacts:
    - path: "app/routes/uploads.py"
      provides: "Определение типа изображения по сигнатуре содержимого (CR-02)"
      contains: "def sniff_image"
    - path: "app/pages/ads.py"
      provides: "Проверка владения ключом изображения и серверный лимит вложений (WR-01, D-13)"
      contains: "def own_image_keys"
    - path: "tests/test_pages/test_ads_image_ownership.py"
      provides: "Регрессия WR-01 и D-13 на страничном слое"
      contains: "def test_"
    - path: "tests/test_pages/test_schedule_ownership.py"
      provides: "Регрессия CR-01/D-20 на обоих входах"
      contains: "def test_"
  key_links:
    - from: "app/pages/ads.py"
      to: "app/config.py"
      via: "лимит вложений читается из settings.max_images_per_ad, не из литерала"
      pattern: "max_images_per_ad"
    - from: "app/routes/ads.py"
      to: "app/pages/ads.py"
      via: "JSON-API переиспользует own_image_keys — один источник правила ключа"
      pattern: "own_image_keys"
---

<objective>
Закрыть три находки безопасности, унаследованные из Фазы 1, до того как начнётся переделка редактора (D-19).

`01-REVIEW.md` отгружен со `status: issues_found`, `critical: 2`. CR-01 (владение `ad_id`/`account_id` при постановке в расписание), CR-02 (тип загружаемого файла берётся из клиентского заголовка) и WR-01/T-10-04 (владение ключом изображения при сохранении объявления) решением UAT от 2026-08-10 перенесены в эту фазу. Пока они открыты, гейт `security_block_on: high` держит фазу.

D-19 требует закрыть их **отдельным ранним планом**, чтобы фиксы легли на стабильный код, а не на движущуюся цель: планы 02-04 и 02-05 переписывают ровно те же обработчики.

Здесь же исполняется D-13 — лимит числа вложений становится серверным правилом. Он попадает сюда не по смыслу интерфейса, а по смыслу доверия: клиентским данным в этой фазе перестают верить, и все четыре проверки живут в одной функции.

Purpose: снять блокирующий гейт безопасности и зафиксировать правила доверия к клиентским данным до переделки экранов.
Output: обе точки входа расписаний и все четыре точки записи вложений проверяют владение; тип файла определяется по содержимому.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-CONTEXT.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-PATTERNS.md
@.planning/phases/01-interfeysnyy-fundament/01-REVIEW.md
@.planning/phases/01-interfeysnyy-fundament/01-SECURITY.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Определять тип загружаемого изображения по содержимому (CR-02)</name>
  <files>app/routes/uploads.py, tests/test_routes/test_uploads.py</files>
  <read_first>
    - `app/routes/uploads.py` целиком (~80 строк) — `safe_filename` и его модульные регулярки на 12-38, проверка типа на 47-52, чтение содержимого на 55, лимит размера на 56-61, сборка ключа на 63-67
    - `tests/test_routes/test_uploads.py` — существующие тесты, включая `test_upload_key_stays_inside_user_prefix`
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md` §Code Examples 5 и §Don't Hand-Roll (обратный случай) — почему проверка пишется руками, а не библиотекой
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-UI-SPEC.md` §Error states — точная копия сообщения об отказе
  </read_first>
  <behavior>
    - Тест: загрузка байтов, начинающихся с сигнатуры JPEG, PNG, GIF87a, GIF89a или RIFF/WEBP, принимается.
    - Тест: загрузка содержимого SVG (`<svg`) с заголовком типа `image/png` отклоняется кодом 400.
    - Тест: загрузка произвольных не-изображений с любым заголовком типа отклоняется кодом 400.
    - Тест: `test_upload_key_stays_inside_user_prefix` остаётся зелёным — форма ключа не меняется.
  </behavior>
  <action>
Ввести в `app/routes/uploads.py` модульную таблицу сигнатур и чистую функцию `sniff_image(content: bytes) -> str | None`, возвращающую MIME-тип по первым байтам либо `None`. Распознаются ровно четыре формата, названные в `02-UI-SPEC.md` §Error states: JPEG (`FF D8 FF`), PNG (`89 50 4E 47 0D 0A 1A 0A`), GIF (`GIF87a` и `GIF89a`) и WebP (первые четыре байта `RIFF`, байты 8..12 равны `WEBP`; байты 4..8 — размер файла и произвольны). SVG в список не входит — именно он и есть вектор CR-02.

Переставить порядок операторов в обработчике: содержимое уже читается ниже по коду (строка 55), поэтому дополнительное чтение не нужно — нужно лишь поднять чтение выше и заменить проверку, опирающуюся на присланный клиентом заголовок типа (строки 47-52), на вызов `sniff_image`. Отказ оформить в стиле файла — `HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=...)`, — с текстом из UI-SPEC: «Не удалось загрузить: подойдут только изображения JPEG, PNG, WebP или GIF. Выберите другой файл.»

Проверка размера (`settings.max_image_size_mb`, строки 56-61) и построение ключа (`f"{user_id}/{uuid4().hex}_{safe_filename(...)}"`, строки 63-67) остаются нетронутыми: форма ключа — контракт, на который опирается Задача 2 и существующий тест префикса.

Стиль правки берётся с соседнего фикса Фазы 1 в этом же файле (`02-PATTERNS.md` §`app/routes/uploads.py`): модульная константа, маленькая чистая функция с докстрингом «почему», вызывающий код в остальном не тронут.

Библиотеку не вводить. `python-magic` тянет системный `libmagic` в Docker-образ; `imghdr` удалён из stdlib в Python 3.13; `Pillow` присутствует только транзитивно через `qrcode[pil]`, а `Image.open()` на недоверенном файле добавляет вектор decompression bomb ровно тому эндпоинту, который мы чиним. Обоснование записать комментарием «почему» рядом с таблицей сигнатур.

<!-- planner-discipline-allow: file.content_type -->
  </action>
  <verify>
    <automated>uv run pytest tests/test_routes/test_uploads.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/routes/uploads.py` содержит `def sniff_image`.
    - `grep -c 'file.content_type' app/routes/uploads.py` возвращает `0`.
    - `tests/test_routes/test_uploads.py` содержит тест, который отправляет содержимое SVG с заголовком типа изображения и утверждает код ответа 400.
    - `uv run pytest tests/test_routes/test_uploads.py::test_upload_key_stays_inside_user_prefix -q` завершается с кодом 0.
    - Команда из `<verify>` завершается с кодом 0.
    - В `pyproject.toml` не появилось ни одной новой зависимости: `git diff --stat pyproject.toml uv.lock` пуст.
  </acceptance_criteria>
  <done>Тип загружаемого файла определяется по первым байтам; SVG и не-изображения отклоняются кодом 400 с текстом из UI-SPEC; новых пакетов не добавлено.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Проверять владение ключом изображения и лимит вложений на всех четырёх входах (WR-01, D-13)</name>
  <files>app/pages/ads.py, app/routes/ads.py, tests/test_routes/test_ads.py, tests/test_pages/test_ads_image_ownership.py</files>
  <read_first>
    - `app/pages/ads.py` (строки 100-200) — `ads_create` (сборка `image_list` около строки 134) и `ads_update` (около строки 184), образец проверки владения через `WHERE` на 112-114
    - `app/routes/ads.py` (строки 1-90) — `CreateAdRequest.images` и `UpdateAdRequest.images`, `update_ad` с `repo.update(ad, **data.model_dump(exclude_unset=True))`
    - `app/routes/uploads.py` (строки 63-67) — форма ключа `{user_id}/{32 hex}_{safe_filename}`, источник правды для регулярки
    - `app/config.py` (строки 20-40) — `max_images_per_ad: int = 10` и `max_image_size_mb`, обе настройки уже существуют
    - `tests/test_routes/test_ads.py` (строки 160-190) — тест, который сегодня закрепляет приём чужих ключей и подлежит переписыванию
    - `.planning/phases/01-interfeysnyy-fundament/01-REVIEW.md` (строки 300-340) — точная формулировка WR-01 и регулярка ключа
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-UI-SPEC.md` §Error states — тексты отказов
  </read_first>
  <behavior>
    - Тест: сохранение объявления со списком, содержащим ключ с префиксом другого пользователя, отклоняется кодом 400 и объявление не изменяется.
    - Тест: сохранение объявления с внешним значением, начинающимся с `http`, отклоняется кодом 400.
    - Тест: сохранение объявления с числом вложений на одно больше `settings.max_images_per_ad` отклоняется кодом 400.
    - Тест: сохранение ровно `settings.max_images_per_ad` вложений своего префикса проходит и сохраняет порядок загрузки.
    - Тест: то же поведение на JSON-API `POST /api/ads` и `PATCH`/`PUT /api/ads/{id}`.
  </behavior>
  <action>
Ввести общую функцию `own_image_keys(values: list[str], user_id: int, max_images: int) -> list[str]` в `app/pages/ads.py` и переиспользовать её из `app/routes/ads.py` — единственный источник правила, чтобы страничный и JSON-слой не разъехались.

Правило ключа: регулярка `^\d+/[0-9a-f]{32}_[A-Za-z0-9._-]{1,100}$` плюс обязательное совпадение числового префикса с `user_id` вызывающего. Регулярка взята из `01-REVIEW.md:326` и подтверждается кодом загрузки (`app/routes/uploads.py:63-67`): `uuid4().hex` даёт ровно 32 шестнадцатеричных символа, `safe_filename` сводит имя к `[A-Za-z0-9._-]` длиной до 100. Значение, не прошедшее регулярку или лежащее вне префикса вызывающего, — отказ, а не молчаливое отбрасывание: молчаливое отбрасывание превратило бы попытку подмены в «успешное сохранение без картинки» и скрыло бы атаку от пользователя.

Лимит (D-13) — второй отказ той же функции: `len(keys) > max_images` даёт 400. Порог берётся из `settings.max_images_per_ad` (существует в `app/config.py:27` и сегодня не используется нигде), не из литерала. Второго источника истины для десятки не заводить.

Тексты отказов — из `02-UI-SPEC.md` §Error states: для чужого/некорректного ключа отказ формулируется как недоступное вложение; для превышения лимита — «Объявление не сохранено: вложений больше 10. Удалите лишние и нажмите «Сохранить».» (число подставляется из настройки).

Четыре точки подключения:
1. `app/pages/ads.py`, `ads_create` — на месте сборки списка ключей из формы (около строки 134);
2. `app/pages/ads.py`, `ads_update` — там же в обновлении (около строки 184);
3. `app/routes/ads.py`, `CreateAdRequest.images` — в обработчике создания, после того как известен `user_id`;
4. `app/routes/ads.py`, `UpdateAdRequest.images` — в обработчике обновления, до вызова `repo.update`.

Слоевые соглашения не смешивать (`02-PATTERNS.md` §`app/routes/schedules.py`): JSON-слой поднимает `HTTPException`; страничный слой в этом проекте отвечает редиректами, но для отказа по данным здесь уместен именно `HTTPException(400)` — страница редактора получит его как ошибку, а не как молчаливую потерю данных. Записать это решение комментарием «почему».

Переписать `tests/test_routes/test_ads.py` (строки 169-185): сегодняшний тест закрепляет **приём** чужого ключа, то есть фиксирует уязвимость как поведение. Он должен утверждать отказ.

Создать `tests/test_pages/test_ads_image_ownership.py` по образцу перекрёстной изоляции из `tests/test_pages/test_responsive_markup.py:280-290` (посев чужой записи с заведомо другим `user_id`, утверждение, что чужая запись не тронута).

**Диагностика уже сохранённых значений (RESEARCH Q4).** Функция отсекает чужие и внешние значения только на записи; строки, сохранённые до фикса, останутся в БД. Разовую чистку в этой фазе **не** делать — это правка данных, а не интерфейса, и она требует отдельного решения. Записать в SUMMARY диагностический запрос `SELECT id FROM ads WHERE images::text LIKE '%http%'` как задел для бэклога и отметить остаточный риск: внешние значения продолжают рендериться в истории и админке (`01-REVIEW.md:311-315`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_ads_image_ownership.py tests/test_routes/test_ads.py tests/test_pages/test_ads_editor.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/ads.py` содержит `def own_image_keys`.
    - `grep -c 'own_image_keys' app/routes/ads.py` возвращает значение не меньше `2` (создание и обновление).
    - `grep -c 'max_images_per_ad' app/pages/ads.py` возвращает значение не меньше `1`; `grep -nE '(>|<|=)\s*10\b' app/pages/ads.py` не показывает захардкоженного порога вложений.
    - `tests/test_pages/test_ads_image_ownership.py` существует и содержит тест превышения лимита.
    - `tests/test_routes/test_ads.py` больше не содержит утверждения о том, что чужой ключ сохраняется.
    - Команда из `<verify>` завершается с кодом 0.
  </acceptance_criteria>
  <done>Все четыре входа записи вложений проверяют префикс владельца и лимит из настроек; тест, закреплявший приём чужого ключа, переписан на отказ.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Проверять владение `ad_id` и `account_id` при постановке расписания на обоих входах (CR-01, D-20)</name>
  <files>app/pages/schedules.py, app/routes/schedules.py, tests/test_pages/test_schedule_ownership.py, tests/test_routes/test_schedules.py</files>
  <read_first>
    - `app/pages/schedules.py` (строки 160-220) — `schedules_create`: `ad_id` и `account_id` приходят из `Form(...)` на 169-170, пишутся в модель без запроса владения
    - `app/pages/schedules.py` (строки 270-330) — `schedules_update`: образец правильной проверки владения группами на 297-308 и её отсутствие для `ad_id`/`account_id` на 314-315
    - `app/routes/schedules.py` (строки 55-90) — проверка владения объявлением через `AdRepository.get_by_id_and_user` на 66-73; владение аккаунтом не проверяется вовсе
    - `app/models/messenger_account.py` — поле `user_id`, по которому проверяется владение аккаунтом
    - `tests/test_pages/test_responsive_markup.py` (строки 280-290) — эталон теста перекрёстной изоляции
    - `tests/test_routes/test_schedules.py` — существующие тесты JSON-API расписаний
  </read_first>
  <behavior>
    - Тест: страничный `POST` создания расписания с `ad_id` чужого объявления не создаёт расписание.
    - Тест: страничный `POST` создания расписания с `account_id` чужого аккаунта не создаёт расписание.
    - Тест: страничный `POST` обновления расписания, подменяющий `ad_id` или `account_id` на чужие, не изменяет запись.
    - Тест: `POST /api/schedules` с чужим `account_id` отвечает 404 и расписание не создаётся.
    - Тест: свои `ad_id` и `account_id` продолжают работать на обоих входах — существующие тесты остаются зелёными.
  </behavior>
  <action>
Ввести проверку владения обоими идентификаторами на обоих входах.

**Страничный слой (`app/pages/schedules.py`, `schedules_create` и `schedules_update`).** Владение проверять запросом, а не последующим `if`, — по образцу, который уже стоит строкой выше в этом же файле для групп (строки 297-308) и для самого расписания (строки 283-290): `select(Ad).where(Ad.id == ad_id, Ad.user_id == user.id)` и `select(MessengerAccount).where(MessengerAccount.id == account_id, MessengerAccount.user_id == user.id)`. Если любой из запросов не дал строки — расписание не создаётся и не изменяется. Ответ страничного слоя остаётся в его соглашении: редирект на `/schedules`. Сообщение пользователю — из `02-UI-SPEC.md` §Error states: «Аккаунт или объявление недоступны. Обновите страницу и попробуйте снова.»

Учесть, что `account_id` в схеме nullable с `ON DELETE SET NULL` (issue #35): пустое значение — законное состояние отвязанного расписания, и проверка владения применяется только к непустому значению.

**JSON-слой (`app/routes/schedules.py:67-73`).** Владение объявлением там уже проверяется через `AdRepository.get_by_id_and_user` — скопировать ровно эту форму для аккаунта: репозиторий или прямой запрос по `user_id`, затем `raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=...)`. Состав маршрутов и контракт JSON-API не менять — D-15 разрешает здесь единственную правку, и это она.

Соглашения слоёв не смешивать: JSON-слой ходит через репозитории и поднимает `HTTPException`, страничный — через прямой `select()` и отвечает редиректом.

Создать `tests/test_pages/test_schedule_ownership.py`, покрывающий страничный вход, и дополнить `tests/test_routes/test_schedules.py` тестами владения аккаунтом на JSON-входе. Форма теста — перекрёстная изоляция из `tests/test_pages/test_responsive_markup.py:280-290`: посеять чужую запись с заведомо другим `user_id` и утверждать, что она не тронута и новая запись не появилась.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_schedule_ownership.py tests/test_routes/test_schedules.py tests/test_pages/test_schedules_detached_account.py tests/test_routes/test_schedules_toggle_detached.py tests/test_pages/test_schedule_creation_path_exists.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MessengerAccount.user_id' app/pages/schedules.py` возвращает значение не меньше `2` (создание и обновление).
    - `grep -c 'Ad.user_id' app/pages/schedules.py` возвращает значение не меньше `4` (создание, обновление и существующие запросы).
    - `app/routes/schedules.py` содержит проверку владения аккаунтом по `user_id` перед записью `account_id`.
    - `tests/test_pages/test_schedule_ownership.py` существует и содержит тест для чужого `ad_id` и тест для чужого `account_id`.
    - Команда из `<verify>` завершается с кодом 0.
    - Полный прогон `uv run pytest tests/ -q` завершается с кодом 0.
    - Состав маршрутов `app/routes/schedules.py` не изменился: `grep -c '@router\.' app/routes/schedules.py` даёт то же число, что до правки.
  </acceptance_criteria>
  <done>Оба входа постановки расписания проверяют владение объявлением и аккаунтом; гейт код-ревью Фазы 1 закрыт; состав маршрутов JSON-API не изменён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → `POST /api/uploads/image` | Составной запрос целиком подконтролен отправителю: имя файла, заголовок типа, содержимое |
| браузер → `POST /ads/new`, `POST /ads/{id}/edit` | Список ключей вложений приходит скрытыми полями формы и полностью управляем клиентом |
| браузер → `POST /schedules/...` | `ad_id` и `account_id` приходят полями формы; `Schedule` не имеет собственного `user_id` |
| клиент API → `/api/ads`, `/api/schedules` | Тот же набор идентификаторов в теле JSON |
| приложение → S3 | Ключ объекта строится из значения, пришедшего от клиента |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Elevation of Privilege | `app/pages/schedules.py:314-315` — `ad_id`/`account_id` из формы пишутся без запроса владения (**CR-01**) | critical | mitigate | Запрос владения по `Ad.user_id` и `MessengerAccount.user_id` в обоих страничных обработчиках; без строки — отказ (Задача 3) |
| T-02-06 | Elevation of Privilege | `app/routes/schedules.py:67-73` — владение объявлением проверяется, владение аккаунтом нет (**CR-01/D-20**) | critical | mitigate | Копия существующей формы проверки объявления, применённая к аккаунту; 404 на чужой аккаунт (Задача 3) |
| T-02-07 | Tampering / Information Disclosure | `app/routes/uploads.py:47-52` — тип файла берётся из клиентского заголовка; SVG проходит и исполняется на origin хранилища (**CR-02**) | critical | mitigate | `sniff_image` по четырём сигнатурам содержимого; SVG в список не входит (Задача 1) |
| T-02-08 | Spoofing / Information Disclosure | `app/pages/ads.py:134,184` и `app/routes/ads.py:16,22` — чужой или внешний ключ изображения принимается (**WR-01 / T-10-04**) | high | mitigate | `own_image_keys`: регулярка формы ключа плюс совпадение префикса с `user_id`, отказ вместо молчаливого отбрасывания (Задача 2) |
| T-02-09 | Denial of Service | Обход браузерного ограничения в 10 вложений прямым POST (**D-13**) | medium | mitigate | Серверная проверка по `settings.max_images_per_ad` внутри `own_image_keys` (Задача 2) |
| T-02-10 | Information Disclosure | Значения `Ad.images`, сохранённые до фикса WR-01, остаются в БД и рендерятся в истории и админке | medium | accept | Правка данных вне границы фазы (интерфейс, не миграция данных). Диагностический запрос и остаточный риск записываются в SUMMARY как задел бэклога (`02-RESEARCH.md` §Open Questions Q4) |
| T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов. `sniff_image` написан руками намеренно: `python-magic` тянет `libmagic`, `imghdr` удалён в Python 3.13, `Pillow` доступна лишь транзитивно и добавила бы decompression bomb чинимому эндпоинту |
</threat_model>

<artifacts_this_phase_produces>
Символы, создаваемые **этим планом** (новые — не подлежат проверке на дрейф от источника):

| Вид | Символ / путь |
|-----|---------------|
| Функция | `sniff_image(content)` — `app/routes/uploads.py` |
| Функция | `own_image_keys(values, user_id, max_images)` — `app/pages/ads.py` |
| Файл теста | `tests/test_pages/test_ads_image_ownership.py` |
| Файл теста | `tests/test_pages/test_schedule_ownership.py` |

Настройки `max_images_per_ad` и `max_image_size_mb` **уже существуют** в `app/config.py:26-27` — этот план впервые начинает читать первую из них, но не создаёт её.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **ADS-05 / ADS-07 (edge-проба, строки 2 и 4).** Обе вернулись `unclassified` → `unresolved`. Краевые случаи, покрытые этим планом, выведены не из пробы, а из `01-REVIEW.md`, `01-SECURITY.md` и `02-UI-SPEC.md` E3/E4. Полный роллап семи строк — в `02-01-PLAN.md` §flagged_assumptions.
- **Уже сохранённые чужие и внешние значения в `Ad.images` (RESEARCH Q4).** Фикс действует только на записи. Наличие таких строк в проде неизвестно; разовая чистка сознательно вынесена за границу фазы (см. T-02-10).
- **Канон-отсылки, не минтившиеся как прохибиции (ADR-550 D6):** IDOR по `ad_id`/`account_id` и загрузка SVG под видом изображения — оба канон OWASP, покрываются `/gsd-secure-phase` и блоком `<threat_model>` выше; отдельные прохибиции не создаются.
</flagged_assumptions>

<verification>
- Три находки `01-REVIEW.md` (CR-01, CR-02) и `01-SECURITY.md` (WR-01/T-10-04) более не воспроизводятся; каждая закрыта регрессионным тестом.
- Состав маршрутов JSON-API не изменён (D-15).
- Форма ключа объекта хранилища не изменена — `test_upload_key_stays_inside_user_prefix` зелёный.
- Полная суита зелёная; страховочная сетка SC-3 из плана 02-01 зелёная.
- Новых зависимостей не добавлено.
</verification>

<success_criteria>
- [ ] `sniff_image` определяет тип по содержимому; SVG отклоняется
- [ ] `own_image_keys` подключён на всех четырёх входах записи вложений
- [ ] Лимит вложений читается из `settings.max_images_per_ad`
- [ ] Владение `ad_id` и `account_id` проверяется на страничном и JSON-входе
- [ ] Тест, закреплявший приём чужого ключа, переписан на отказ
- [ ] `uv run pytest tests/ -q` завершается с кодом 0
</success_criteria>

<output>
Create `.planning/phases/02-obyavleniya-i-raspisaniya/02-02-SUMMARY.md` when done
</output>
