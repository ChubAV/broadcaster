---
phase: 02-obyavleniya-i-raspisaniya
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - app/pages/schedules.py
  - app/templates/schedules/form.html
  - app/templates/schedules/list.html
  - app/templates/schedules/includes/schedule_row.html
  - tests/test_pages/test_shell.py
  - tests/test_pages/test_responsive_markup.py
  - tests/test_pages/test_schedules_detached_account.py
  - tests/test_routes/test_schedules_toggle_detached.py
  - tests/test_routes/test_schedules_profile_timezone.py
autonomous: true
requirements: [ADS-07, SCH-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Расписание настраивается ровно одним способом — в редакторе объявления; отдельных страниц создания и редактирования расписания в приложении не осталось"
    - "Мёртвого кода после переезда не остаётся: шаблон формы расписания удалён, обработчики сняты, ссылок на них в приложении нет"
    - "После сноса пользователь сохраняет возможность создать расписание — страховочная сетка SC-3 зелёная в этом коммите"
    - "Ни одна ссылка в приложении не ведёт на несуществующий маршрут: действие в шапке сводного списка и его пустое состояние ведут к объявлениям"
    - "Строка сводного списка ведёт в редактор объявления к нужному расписанию, а не на удалённую страницу редактирования расписания"
    - "Расписание, отвязанное от удалённого аккаунта, по-прежнему нельзя возобновить, и текст объяснения указывает на редактор объявления, а не на исчезнувшую форму (issue #35, SCH-05)"
    - "Пять тестов, ссылавшихся на удалённые маршруты, переписаны на новый путь и продолжают проверять то же поведение, а не удалены"
  artifacts:
    - path: "app/pages/schedules.py"
      provides: "Страничный слой расписаний без обработчиков создания и редактирования; список, фрагмент, переключение и удаление сохранены"
      contains: "schedules_toggle"
  key_links:
    - from: "app/templates/schedules/includes/schedule_row.html"
      to: "app/pages/ads.py"
      via: "переход из сводного списка в редактор объявления к конкретному расписанию"
      pattern: "/ads/"
---

<objective>
Снять отдельные страницы создания и редактирования расписания — вторая половина перехода, задуманного D-16.

Порядок здесь несущий, а не косметический. План 02-05 довёл расписания в редакторе до рабочего состояния; между 02-05 и этим планом оба пути жили одновременно. Только теперь старый путь можно снять, не оставляя пользователя без возможности создать расписание ни в одном коммите фазы. Страховочная сетка `tests/test_pages/test_schedule_creation_path_exists.py`, созданная планом 02-01, обязана быть зелёной и до, и после этого коммита — она сформулирована как дизъюнкция именно ради этого перехода.

D-14 требует удалить целиком: один способ настроить расписание, мёртвого кода не остаётся. Ссылки на удаляемые маршруты живут в девяти местах — четырёх внутри приложения и пяти в тестах; все девять перечислены в `02-RESEARCH.md` §Runtime State Inventory и закрываются здесь.

Purpose: завершить переезд настройки расписаний в редактор объявления, не оставив ни мёртвого кода, ни битых ссылок.
Output: единственный способ настроить расписание; девять ссылок переведены на новый путь.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-CONTEXT.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-PATTERNS.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-UI-SPEC.md
@.planning/phases/02-obyavleniya-i-raspisaniya/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Снять обработчики и шаблон, перевести внутренние ссылки на редактор объявления</name>
  <reversibility rating="reversible">Удаление страниц и шаблона обратимо возвратом файлов из истории git; схема и данные не затрагиваются. Оценка перенесена из D-16, где пользователь уже её зафиксировал.</reversibility>
  <precondition>Расписание создаётся и изменяется из редактора объявления: `uv run pytest tests/test_pages/test_editor_schedules.py -q` завершается с кодом 0 (результат плана 02-05)</precondition>
  <files>app/pages/schedules.py, app/templates/schedules/form.html, app/templates/schedules/list.html, app/templates/schedules/includes/schedule_row.html</files>
  <read_first>
    - `app/pages/schedules.py` (строки 119-163) — `schedules_new`, подлежит удалению целиком
    - `app/pages/schedules.py` (строки 166-215) — `schedules_create`: **не удалять**, обработчик обслуживает форму карточки из редактора (план 02-05)
    - `app/pages/schedules.py` (строки 218-267) — `schedules_edit`, подлежит удалению целиком
    - `app/pages/schedules.py` (строки 327-390) — `schedules_toggle` с блокировкой возобновления по issue #35 (комментарий на 344-346 ссылается на исчезающую форму) и `schedules_delete`
    - `app/templates/schedules/form.html` целиком — файл удаляется
    - `app/templates/schedules/list.html` (строки 12-40) — действие в шапке на 14, пустое состояние на 32-35
    - `app/templates/schedules/includes/schedule_row.html` (строки 68-75) — действие строки на 71
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md` §Runtime State Inventory (таблица девяти ссылок), §Common Pitfalls 12
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-UI-SPEC.md` §Copywriting Contract (действие в шапке сводного списка, действие строки), §Empty states
  </read_first>
  <action>
<!-- planner-discipline-allow: /schedules/new -->
<!-- planner-discipline-allow: schedules_new -->
<!-- planner-discipline-allow: schedules_edit -->

**Удалить.** Обработчики `schedules_new` (строки 119-163) и `schedules_edit` (строки 218-267) в `app/pages/schedules.py` снимаются целиком. Файл `app/templates/schedules/form.html` удаляется. Импорты, ставшие неиспользуемыми после удаления обработчиков, снимаются.

**Не удалять.** `schedules_create`, `schedules_update`, `schedules_toggle`, `schedules_delete`, `schedules_list` и `schedules_partial` остаются: первые четыре обслуживают формы карточки из редактора (план 02-05), последние два — сводный список. JSON-API `app/routes/schedules.py` не трогается вовсе — D-15 сохраняет его состав и контракт.

**Три ссылки внутри приложения.**
- `app/templates/schedules/list.html:14` — действие в шапке ведёт на страницу создания расписания. По UI-SPEC §Copywriting Contract вместо него ставится «К объявлениям» в приглушённом варианте со ссылкой на список объявлений: создание живёт в редакторе объявления.
- `app/templates/schedules/list.html:32-35` — пустое состояние ведёт туда же. Копия из UI-SPEC §Empty states: заголовок «Расписаний пока нет», подсказка «Расписания создаются в редакторе объявления», действие «К объявлениям» на список объявлений.
- `app/templates/schedules/includes/schedule_row.html:71` — действие строки ведёт на страницу редактирования расписания. Заменяется на «Открыть объявление» со ссылкой на редактор объявления этого расписания с параметром выбранного расписания — тем самым, который план 02-05 ввёл для разворачивания нужной карточки.

Полная переверстка этих трёх файлов в карточки — работа плана 02-07. Здесь правятся только ссылки, чтобы между двумя планами в приложении не осталось ни одного перехода на несуществующий маршрут.

**Путь восстановления для отвязанного расписания (Pitfall 12, SCH-05).** Блокировка возобновления по issue #35 — не дефект и остаётся как есть: расписание, потерявшее аккаунт после его удаления, нельзя возобновить, пока аккаунт не привязан заново. Но комментарий на строках 344-346 и текст, который видит пользователь, отсылают к «форме редактирования», которой больше нет. Переписать и комментарий, и пользовательский текст так, чтобы они указывали на редактор объявления. Без этого пользователь получает заблокированный тумблер без объяснения, куда идти. Этот случай — отвязанное после удаления аккаунта расписание — в UI-SPEC отдельно не назван; текст строится по образцу соседнего случая из §Error states, называя проблему и следующий шаг.

**Мягкой посадки не делать.** `02-RESEARCH.md` §Open Questions Q5 поднимает вопрос, не оставить ли удаляемые адреса постоянными редиректами ради открытых вкладок и закладок. D-14 отвечает: удалить целиком, мёртвого кода не остаётся. Решение не переоткрывается. Записать в SUMMARY как возможный последующий шаг, если после выката в логах появятся обращения на эти адреса.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_schedule_creation_path_exists.py tests/test_pages/test_editor_schedules.py tests/test_routes/test_schedules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/templates/schedules/form.html` завершается с ненулевым кодом — файл удалён.
    - `grep -c 'def schedules_new' app/pages/schedules.py` возвращает `0`.
    - `grep -c 'def schedules_edit' app/pages/schedules.py` возвращает `0`.
    - `grep -c 'def schedules_create' app/pages/schedules.py` возвращает `1`; `grep -c 'def schedules_toggle' app/pages/schedules.py` возвращает `1`; `grep -c 'def schedules_delete' app/pages/schedules.py` возвращает `1`.
    - `grep -rn 'schedules/new' app/` не возвращает ни одной строки.
    - `grep -rnE "schedules/\{\{? ?s?c?h?e?d?u?l?e?\.?i?d?[^/]*\}?\}?/edit" app/templates/` не возвращает ни одной строки.
    - `app/templates/schedules/includes/schedule_row.html` содержит `Открыть объявление` и ссылку, начинающуюся с `/ads/`.
    - `app/templates/schedules/list.html` содержит `К объявлениям`.
    - `grep -c '@router\.' app/routes/schedules.py` даёт то же число, что до правки — JSON-API не тронут.
    - Команда из `<verify>` завершается с кодом 0.
    - `uv run ruff check app/pages/schedules.py` (или существующий линтер проекта) не сообщает о неиспользуемых импортах.
  </acceptance_criteria>
  <done>Отдельные страницы создания и редактирования расписания сняты вместе с шаблоном; ни одна ссылка в приложении не ведёт на несуществующий маршрут; текст восстановления отвязанного расписания указывает на редактор объявления.</done>
</task>

<task type="auto">
  <name>Задача 2: Перевести пять тестов, ссылавшихся на удалённые маршруты, на новый путь</name>
  <files>tests/test_pages/test_shell.py, tests/test_pages/test_responsive_markup.py, tests/test_pages/test_schedules_detached_account.py, tests/test_routes/test_schedules_toggle_detached.py, tests/test_routes/test_schedules_profile_timezone.py</files>
  <read_first>
    - `tests/test_pages/test_shell.py` (строки 78-115) — список страниц оболочки, включая удалённый адрес на строке 85
    - `tests/test_pages/test_responsive_markup.py` (строки 260-290) — `test_schedules_card_renders_data` с утверждением на строке 276 и `test_schedules_toggle_route_unchanged`
    - `tests/test_pages/test_schedules_detached_account.py` (строки 85-105) — обращение к удалённой странице на строке 96
    - `tests/test_routes/test_schedules_toggle_detached.py` (строки 160-180) — ссылка на строке 171
    - `tests/test_routes/test_schedules_profile_timezone.py` (строки 20-40) — обращение к удалённой странице на строке 28
    - `app/templates/ads/includes/sched_card.html` — куда теперь ведёт путь, и как в нём отрисована таймзона (только для чтения)
    - `.planning/phases/02-obyavleniya-i-raspisaniya/02-RESEARCH.md` §Runtime State Inventory — таблица девяти ссылок с указанием действия по каждой
  </read_first>
  <action>
Переписать пять тестов **на новый путь**, а не удалить: каждый из них проверяет поведение, которое остаётся истинным, просто в другом месте интерфейса. Удаление теста вместе с маршрутом потеряло бы проверку.

- `tests/test_pages/test_shell.py:85` — убрать удалённый адрес из перечня страниц оболочки. Если перечень служит проверкой того, что каждая страница получает оболочку, добавить вместо него редактор объявления: он теперь и есть страница настройки расписаний.
- `tests/test_pages/test_responsive_markup.py:276` — утверждение о ссылке на страницу редактирования расписания заменить на утверждение о ссылке в редактор объявления с параметром выбранного расписания. Остальные утверждения теста (отрисовка реальных данных: заголовок объявления, время, маршрут переключения) не трогать — они и есть его ценность.
- `tests/test_pages/test_schedules_detached_account.py:96` — обращение к удалённой странице заменить обращением к редактору объявления этого расписания; проверяемое поведение (отвязанное расписание отображается и не даёт себя возобновить) сохранить.
- `tests/test_routes/test_schedules_toggle_detached.py:171` — то же.
- `tests/test_routes/test_schedules_profile_timezone.py:28` — обращение к удалённой странице заменить обращением к редактору. Таймзона теперь не выбирается в карточке расписания, а показывается моноширинной подписью только для чтения из профиля пользователя; переписать утверждение под это: подпись содержит таймзону профиля.

Ни один тест не должен быть ослаблен до проверки одного кода ответа. Форма утверждений — как в эталоне `tests/test_pages/test_responsive_markup.py:260-278`: посев реальных данных и утверждения на реальных строках и адресах маршрутов.

После правки в `tests/` не должно остаться ни одного обращения к удалённым адресам, а полная суита обязана быть зелёной.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py tests/test_pages/test_responsive_markup.py tests/test_pages/test_schedules_detached_account.py tests/test_routes/test_schedules_toggle_detached.py tests/test_routes/test_schedules_profile_timezone.py tests/test_pages/test_schedule_creation_path_exists.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'schedules/new' tests/` не возвращает ни одной строки.
    - `grep -rn '/edit' tests/ | grep schedules` не возвращает строк, обращающихся к удалённому маршруту редактирования расписания.
    - Каждый из пяти файлов сохранил прежнее число тестовых функций: `grep -c 'def test_' <файл>` не уменьшилось ни в одном.
    - `tests/test_routes/test_schedules_profile_timezone.py` утверждает наличие таймзоны профиля в разметке редактора.
    - Команда из `<verify>` завершается с кодом 0.
    - Полный прогон `uv run pytest tests/ -q` завершается с кодом 0.
  </acceptance_criteria>
  <done>Пять тестов переведены на редактор объявления без потери проверяемого поведения; обращений к удалённым адресам в суите не осталось.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| открытые вкладки и закладки пользователей → удалённые адреса | После сноса прежние адреса отвечают 404 |
| браузер → оставшиеся страничные маршруты расписаний | Обработчики создания, изменения, переключения и удаления сохраняются и продолжают принимать данные формы |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-29 | Denial of Service | Снос страниц оставляет пользователя без способа создать расписание | high | mitigate | Предусловие задачи 1 требует зелёного `tests/test_pages/test_editor_schedules.py`; страховочная сетка SC-3 обязана быть зелёной в этом же коммите (D-16) |
| T-02-30 | Denial of Service | Ссылки внутри приложения ведут на удалённые маршруты — 404 из сводного списка | medium | mitigate | Три внутренние ссылки переводятся на список объявлений и редактор в том же коммите, что и удаление (Задача 1) |
| T-02-31 | Denial of Service | Открытые вкладки и закладки пользователей отвечают 404 | low | accept | D-14 требует полного удаления. Мягкая посадка постоянным редиректом рассмотрена и отклонена; фиксируется как возможный последующий шаг при появлении обращений в логах |
| T-02-32 | Elevation of Privilege | Удаление обработчиков случайно снимает проверки владения с оставшихся | critical | mitigate | Приёмочные критерии фиксируют сохранение оставшихся обработчиков поимённо; тесты владения из плана 02-02 и 02-05 прогоняются в `<verify>` |
| T-02-33 | Repudiation | Тест удаляется вместе с маршрутом, и проверяемое поведение перестаёт проверяться | medium | mitigate | Приёмочный критерий требует, чтобы число тестовых функций ни в одном из пяти файлов не уменьшилось (Задача 2) |
| T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов |
</threat_model>

<artifacts_this_phase_produces>
Этот план **удаляет**, а не создаёт символы:

| Вид | Символ / путь | Судьба |
|-----|---------------|--------|
| Обработчик | `schedules_new` — `app/pages/schedules.py` | удаляется |
| Обработчик | `schedules_edit` — `app/pages/schedules.py` | удаляется |
| Шаблон | `app/templates/schedules/form.html` | удаляется |
| Маршрут | адрес создания расписания и адрес редактирования расписания | удаляются |

Символы, создаваемые фазой и используемые здесь, объявлены в планах-владельцах: параметр `?sched={id}` у `/ads/{id}/edit` — план 02-05.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **ADS-07, SCH-05 (edge-проба, строки 4 и 7).** Обе вернулись `unclassified` → `unresolved`. Краевые случаи, значимые здесь, выведены из `02-RESEARCH.md` §Runtime State Inventory (таблица девяти ссылок) и §Pitfall 12 (блокировка возобновления по issue #35), а не из пробы. Полный роллап семи строк — в `02-01-PLAN.md` §flagged_assumptions.
- **RESEARCH Q5 — мягкая посадка удалённых адресов.** D-14 требует полного удаления, решение не переоткрывается. Остаточный риск: у пользователей есть открытые вкладки и закладки, которые начнут отвечать 404. Зафиксирован как T-02-31 с диспозицией «принять» и как возможный последующий шаг в SUMMARY.
- **Текст восстановления для отвязанного расписания.** Именно этот случай — расписание, потерявшее аккаунт после его удаления, — в `02-UI-SPEC.md` §Error states отдельно не назван. Текст строится по образцу соседнего случая и должен называть проблему и следующий шаг. **Риск, если формулировка неудачна:** пользователь видит заблокированный тумблер без понятного пути восстановления.
</flagged_assumptions>

<verification>
- Отдельных страниц создания и редактирования расписания в приложении нет; шаблон формы удалён.
- Ни одной ссылки на удалённые маршруты ни в `app/`, ни в `tests/`.
- Страховочная сетка SC-3 зелёная в этом коммите — расписание по-прежнему создаётся из редактора.
- JSON-API расписаний не тронут; оставшиеся страничные обработчики на месте.
- Пять тестов переписаны, ни один не удалён и не ослаблен.
- Полная суита зелёная.
</verification>

<success_criteria>
- [ ] `schedules_new` и `schedules_edit` сняты, `app/templates/schedules/form.html` удалён
- [ ] Оставшиеся страничные обработчики расписаний на месте поимённо
- [ ] Три внутренние ссылки переведены на список объявлений и редактор
- [ ] Текст восстановления отвязанного расписания указывает на редактор объявления
- [ ] Пять тестов переписаны без потери числа тестовых функций
- [ ] `grep -rn 'schedules/new' app/ tests/` пуст
- [ ] `uv run pytest tests/ -q` завершается с кодом 0
</success_criteria>

<output>
Create `.planning/phases/02-obyavleniya-i-raspisaniya/02-06-SUMMARY.md` when done
</output>
