---
phase: 02
slug: obyavleniya-i-raspisaniya
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: 2026-08-11
---

# Phase 02 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

Основание закрытия: реестр угроз составлен на этапе планирования (все 15 планов несут `<threat_model>`); каждая диспозиция `mitigate` закрыта в SUMMARY соответствующего плана именованным тестом или проверкой; полный прогон `uv run pytest tests/ -q` — 895 passed, 0 failed (2026-08-11, подтверждён независимым повтором в 02-15). Выборочная L1-проверка оркестратора: проверки владения (`Ad.user_id == user_id`, `Group.user_id`, `MessengerAccount.user_id`) присутствуют в `app/pages/schedules.py` / `app/routes/schedules.py`; сигнатурная проверка типа загрузки (`sniff_image`, `_IMAGE_SIGNATURES`) — в `app/routes/uploads.py`; именованные регрессии (`test_foreign_ad_is_not_autosavable`, `test_toggle_from_the_list_leaves_a_foreign_schedule_alone`, `test_save_during_autosave_overlap_lands_in_one_published_ad` и др.) найдены в tests/.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| окружение процесса → конфигурация приложения | Файл `.env` рабочего каталога попадает в `Settings` при **каждом** конструировании (`app/config.py:78`) (02-01) | — |
| тестовый процесс → боевые учётные данные | Тестовые фикстуры сегодня наследуют реальные SMTP-параметры разработчика (02-01) | — |
| браузер → `POST /api/uploads/image` | Составной запрос целиком подконтролен отправителю: имя файла, заголовок типа, содержимое (02-02) | — |
| браузер → `POST /ads/new`, `POST /ads/{id}/edit` | Список ключей вложений приходит скрытыми полями формы и полностью управляем клиентом (02-02) | — |
| браузер → `POST /schedules/...` | `ad_id` и `account_id` приходят полями формы; `Schedule` не имеет собственного `user_id` (02-02) | — |
| клиент API → `/api/ads`, `/api/schedules` | Тот же набор идентификаторов в теле JSON (02-02) | — |
| приложение → S3 | Ключ объекта строится из значения, пришедшего от клиента (02-02) | — |
| клиент API → `PATCH`/`PUT /api/ads/{id}` | Значение состояния приходит из тела JSON и присваивается модели напрямую через `model_dump(exclude_unset=True)` (02-03) | — |
| БД → планировщик отправки | `collect_due_schedules` читает состояние объявления и решает, уйдёт ли рассылка в чужие группы (02-03) | — |
| ревизия `0013` → данные прода | `downgrade` необратимо теряет информацию о том, какие объявления были черновиками (02-03) | — |
| браузер → автосохранение | Тело запроса приходит с каждой паузы ввода; сервер обязан быть идемпотентным по правилу «последняя запись побеждает» (02-04) | — |
| имя загруженного файла → разметка редактора | Имя подконтрольно отправителю и участвует в подсказке плитки (02-04) | — |
| значение из настроек → контекст JavaScript | Базовый URL хранилища подставляется внутрь блока скрипта (02-04) | — |
| текст объявления → предпросмотр | Пользовательский текст рендерится в HTML-контексте панели предпросмотра (02-04) | — |
| браузер → страничные маршруты расписаний | Идентификаторы объявления, аккаунта, групп, дней и времён приходят полями формы (02-05) | — |
| поле возврата в форме → построение редиректа | Значение подконтрольно отправителю и участвует в решении, куда вернуть пользователя (02-05) | — |
| значения времени → вычисление следующего запуска | Строки времени попадают в разбор, который сегодня не защищён от неверного формата (02-05) | — |
| открытые вкладки и закладки пользователей → удалённые адреса | После сноса прежние адреса отвечают 404 (02-06) | — |
| браузер → оставшиеся страничные маршруты расписаний | Обработчики создания, изменения, переключения и удаления сохраняются и продолжают принимать данные формы (02-06) | — |
| браузер → параметры фильтрации и поиска `/schedules` | Значения фильтров и поисковая строка приходят из строки запроса (02-07) | — |
| идентификаторы групп расписания → разрешение имён | Массив идентификаторов может содержать значения, указывающие на чужие группы (02-07) | — |
| поисковая строка → пустое состояние | Термин поиска отображается пользователю в тексте «Расписания не найдены» (02-07) | — |
| браузер → `POST /ads/new` | Тело формы полностью подконтрольно отправителю: этот план ВВОДИТ в него новый идентификатор объекта (`ad_id`), то есть расширяет границу (02-08) | — |
| браузер → `POST /ads/{id}/edit` | Идентификатор приходит путём маршрута; тело содержит вложения и состояние (02-08) | — |
| браузер → `POST /api/uploads/image` | Ключ вложения порождается сервером, но возвращается клиенту и приезжает обратно телом формы (02-08) | — |
| аутентифицированный клиент → `POST /api/schedules` | Тело несёт `ad_id`, `account_id` и `group_ids`; `Schedule` не имеет собственного `user_id`, поэтому эти три значения задают не только содержание рассылки, но и её владельца (02-09) | — |
| аутентифицированный клиент → `PUT /api/schedules/{id}` | Тело несёт частичный патч; присутствие ключа `group_ids` меняет адресатов рассылки (02-09) | — |
| аутентифицированный клиент → `POST /api/schedules/{id}/toggle` | Переводит расписание в состояние, в котором планировщик начинает его выбирать (02-09) | — |
| расписание → воркер отправки → `SendLog` | `collect_due_schedules` итерирует `schedule.group_ids` как есть, `send_message_once` резолвит группу по первичному ключу — ниже по потоку владельца никто не перепроверяет (02-09) | — |
| аутентифицированный клиент → `POST /api/uploads/image` | Тело произвольного размера и произвольного содержимого; имя файла подконтрольно отправителю (02-10) | — |
| клиент → поля `images` формы редактора и тело JSON-API объявлений | Ключ вложения ОБЪЯВЛЯЕТСЯ клиентом; принадлежность и форма им не доказываются (02-10) | — |
| `Ad.images` → адрес изображения → карточка, история, админка, адаптеры мессенджеров | Сохранённое значение конкатенируется в адрес и передаётся дальше (02-10) | — |
| правка объявления → снапшот отправки | Правка `Ad.images` не должна дотягиваться до независимой колонки снапшота `SendLog` (02-11) | — |
| путь убирания вложения → хранилище объектов | Убирание ключа не должно вызывать удаление объекта: снапшоты истории ссылаются на тот же объект (D-12) (02-11) | — |
| исполнитель → база данных владельца | Применение ревизии — необратимая операция над данными; граница пересекается только по явному решению человека (02-12) | — |
| поднятое приложение → браузер человека | Наблюдения рендера выполняются человеком на его машине; в кодовую базу они попадают только записью исхода (02-12) | — |
| браузер → POST /ads/new | недоверенный ввод: тело формы, включая клиентский `ad_id` (02-13) | — |
| клиент → PUT /api/schedules/{id} | недоверенное JSON-тело аутентифицированного пользователя (02-14) | — |
| база → шаблоны карточек | строка расписания может нести None, записанный до фикса (02-14) | — |
| — | Новых границ доверия план не вводит: правится планировочный документ и выполняется прогон существующей суиты (02-15) | — |

---

## Threat Register

| Plan | Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|------|-----------|----------|-----------|----------|-------------|------------|--------|
| 02-01 | T-02-01 | Information Disclosure | `Settings` в тестовых фикстурах (12 модулей) | medium | mitigate | `_env_file=None` в каждом конструировании: суита перестаёт запускаться с боевыми SMTP-параметрами и боевым `s3_public_url` разработчика (Задача 1) | closed |
| 02-01 | T-02-02 | Denial of Service | `app/pages/common.py:36-38` — импорт модуля конструирует `Settings` | medium | mitigate | Привязка глобалов через `create_app`; импорт не падает `ValidationError` при отсутствии файла окружения (Задача 2) | closed |
| 02-01 | T-02-03 | Tampering | Отсутствие тестового покрытия `/ads/new` и `/ads/{id}/edit` | high | mitigate | `tests/test_pages/test_ads_editor.py` — экран, на котором строится вся фаза, перестаёт быть непокрытым (Задача 2). Без этого правки планов 02-04/02-05 нечем удержать | closed |
| 02-01 | T-02-04 | Elevation of Privilege | `GET /ads/{id}/edit` для чужого объявления | high | mitigate | Тест владения в `tests/test_pages/test_ads_editor.py` закрепляет существующую проверку `Ad.user_id == user.id` (`app/pages/ads.py:112-114`) как регрессионный контракт | closed |
| 02-01 | T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | Фаза не устанавливает ни одного пакета — `02-RESEARCH.md` §Package Legitimacy Audit: таблица пуста, `[SLOP]`/`[SUS]` отсутствуют. Гейт легитимности неприменим | closed |
| 02-02 | T-02-05 | Elevation of Privilege | `app/pages/schedules.py:314-315` — `ad_id`/`account_id` из формы пишутся без запроса владения (**CR-01**) | critical | mitigate | Запрос владения по `Ad.user_id` и `MessengerAccount.user_id` в обоих страничных обработчиках; без строки — отказ (Задача 3) | closed |
| 02-02 | T-02-06 | Elevation of Privilege | `app/routes/schedules.py:67-73` — владение объявлением проверяется, владение аккаунтом нет (**CR-01/D-20**) | critical | mitigate | Копия существующей формы проверки объявления, применённая к аккаунту; 404 на чужой аккаунт (Задача 3) | closed |
| 02-02 | T-02-07 | Tampering / Information Disclosure | `app/routes/uploads.py:47-52` — тип файла берётся из клиентского заголовка; SVG проходит и исполняется на origin хранилища (**CR-02**) | critical | mitigate | `sniff_image` по четырём сигнатурам содержимого; SVG в список не входит (Задача 1) | closed |
| 02-02 | T-02-08 | Spoofing / Information Disclosure | `app/pages/ads.py:134,184` и `app/routes/ads.py:16,22` — чужой или внешний ключ изображения принимается (**WR-01 / T-10-04**) | high | mitigate | `own_image_keys`: регулярка формы ключа плюс совпадение префикса с `user_id`, отказ вместо молчаливого отбрасывания (Задача 2) | closed |
| 02-02 | T-02-09 | Denial of Service | Обход браузерного ограничения в 10 вложений прямым POST (**D-13**) | medium | mitigate | Серверная проверка по `settings.max_images_per_ad` внутри `own_image_keys` (Задача 2) | closed |
| 02-02 | T-02-10 | Information Disclosure | Значения `Ad.images`, сохранённые до фикса WR-01, остаются в БД и рендерятся в истории и админке | medium | accept | Правка данных вне границы фазы (интерфейс, не миграция данных). Диагностический запрос и остаточный риск записываются в SUMMARY как задел бэклога (`02-RESEARCH.md` §Open Questions Q4) | closed |
| 02-02 | T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов. `sniff_image` написан руками намеренно: `python-magic` тянет `libmagic`, `imghdr` удалён в Python 3.13, `Pillow` доступна лишь транзитивно и добавила бы decompression bomb чинимому эндпоинту | closed |
| 02-03 | T-02-11 | Tampering | `UpdateAdRequest` — запись произвольной строки в состояние объявления через JSON-API | high | mitigate | Тип входного поля `Literal["draft", "published"] \ | closed |
| 02-03 | T-02-12 | Denial of Service | Публикация черновика при `next_run_at` в прошлом — залп всех пропущенных слотов в группы | high | mitigate | Пропуск реализован веткой со сдвигом `next_run_at` через `compute_next_run_at`, а не условием в `WHERE`; закреплено тестом `tests/test_application/test_collect_due_draft.py` | closed |
| 02-03 | T-02-13 | Tampering | Задача долетает до воркера после возврата объявления в черновик | medium | mitigate | Дублирующая проверка состояния в `send_message_once` рядом с существующим каскадом «нет объявления / аккаунт не активен» | closed |
| 02-03 | T-02-14 | Denial of Service | Ревизия `0013` выкатывается, пока `app/pages/schedules.py:131,240` ещё читают старый флаг | high | mitigate | Вариант (A) из §Pitfall 3: миграция и правка обоих фильтров одним коммитом; страховочная сетка `test_schedule_creation_path_exists.py` держит SC-3 | closed |
| 02-03 | T-02-15 | Denial of Service | Снятие колонки без правки схем Pydantic — 500 на четырёх маршрутах `/api/ads` | high | mitigate | `AdResponse` и `UpdateAdRequest` правятся тем же коммитом; живость закреплена тестом | closed |
| 02-03 | T-02-16 | Repudiation | `downgrade` ревизии `0013` теряет, какое объявление было черновиком | medium | accept | Принято пользователем в D-02 (оценка `one-way`). Потеря объявлена комментарием в ревизии в принятом стиле проекта; альтернатива — держать мёртвую колонку вечно | closed |
| 02-03 | T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов — `02-RESEARCH.md` §Package Legitimacy Audit пуст | closed |
| 02-04 | T-02-17 | Tampering | Хранимый XSS через имя загруженного файла в редакторе | high | mitigate | Разметка строится узлами DOM с присваиванием свойств; опасные способы вставки разметки остаются на нуле по проекту и проверяются по исходному тексту шаблона (Задача 3) | closed |
| 02-04 | T-02-18 | Tampering | Подстановка значения настроек в контекст JavaScript без кавычек-безопасной сериализации | medium | mitigate | Сериализация значения в JSON без окружающих кавычек — форма, запрошенная `01-REVIEW.md:625-626` (Задача 3) | closed |
| 02-04 | T-02-19 | Tampering | Текст объявления в предпросмотре | medium | mitigate | Автоэкранирование Jinja; предпросмотр рендерится сервером из записанного объекта, клиент его не собирает (Задача 1, Задача 2) | closed |
| 02-04 | T-02-20 | Denial of Service | Обход браузерного ограничения на число вложений прямым POST | medium | mitigate | Отказ на сервере через `own_image_keys` с порогом из настроек; скрытие плитки добавления — удобство, а не точка принуждения (Задача 2) | closed |
| 02-04 | T-02-21 | Elevation of Privilege | Автосохранение чужого объявления | high | mitigate | Владение внутри `WHERE` запроса на каждом обращении к объявлению; закреплено тестом (Задача 2) | closed |
| 02-04 | T-02-22 | Information Disclosure | Предпросмотр, собранный из тела запроса, показал бы отклонённые сервером данные | low | mitigate | Предпросмотр рендерится из перечитанного после коммита объекта (Задача 2) | closed |
| 02-04 | T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов; htmx 1.9.10 и Alpine 3.13.3 вендорены файлами с Фазы 1, внешних запросов ноль | closed |
| 02-05 | T-02-23 | Tampering | Открытый редирект через поле возврата в редактор | high | mitigate | Адрес редиректа строится сервером из идентификатора объявления уже проверенной на владение записи; значение поля используется только как признак происхождения (Задача 2) | closed |
| 02-05 | T-02-24 | Denial of Service | Значение времени вне формата часы-двоеточие-минуты роняет вычисление следующего запуска — ошибка 500 на прямой POST | medium | mitigate | Фильтрация значений по формату до приведения типов и до вызова вычисления (Задача 2) | closed |
| 02-05 | T-02-25 | Denial of Service | Нечисловые значения в списках идентификаторов групп и дней | medium | mitigate | Отбрасывание значений, не приводящихся к числу, до разбора (Задача 2) | closed |
| 02-05 | T-02-26 | Elevation of Privilege | Новый путь из редактора обходит проверки владения `ad_id` и `account_id` | critical | mitigate | Проверки из плана 02-02 распространяются на путь из редактора и закреплены тестом в `tests/test_pages/test_schedule_ownership.py` (Задача 2) | closed |
| 02-05 | T-02-27 | Tampering | Расписание сохраняется активным с пустым списком групп после смены аккаунта | medium | mitigate | Выключение при пустом результате проверки принадлежности групп (Задача 2, D-08) | closed |
| 02-05 | T-02-28 | Tampering | Заголовок объявления в теле окна подтверждения удаления | low | mitigate | Автоэкранирование Jinja: разметка в заголовке рендерится как текст (Задача 1, held-out проверка) | closed |
| 02-05 | T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов | closed |
| 02-06 | T-02-29 | Denial of Service | Снос страниц оставляет пользователя без способа создать расписание | high | mitigate | Предусловие задачи 1 требует зелёного `tests/test_pages/test_editor_schedules.py`; страховочная сетка SC-3 обязана быть зелёной в этом же коммите (D-16) | closed |
| 02-06 | T-02-30 | Denial of Service | Ссылки внутри приложения ведут на удалённые маршруты — 404 из сводного списка | medium | mitigate | Три внутренние ссылки переводятся на список объявлений и редактор в том же коммите, что и удаление (Задача 1) | closed |
| 02-06 | T-02-31 | Denial of Service | Открытые вкладки и закладки пользователей отвечают 404 | low | accept | D-14 требует полного удаления. Мягкая посадка постоянным редиректом рассмотрена и отклонена; фиксируется как возможный последующий шаг при появлении обращений в логах | closed |
| 02-06 | T-02-32 | Elevation of Privilege | Удаление обработчиков случайно снимает проверки владения с оставшихся | critical | mitigate | Приёмочные критерии фиксируют сохранение оставшихся обработчиков поимённо; тесты владения из плана 02-02 и 02-05 прогоняются в `<verify>` | closed |
| 02-06 | T-02-33 | Repudiation | Тест удаляется вместе с маршрутом, и проверяемое поведение перестаёт проверяться | medium | mitigate | Приёмочный критерий требует, чтобы число тестовых функций ни в одном из пяти файлов не уменьшилось (Задача 2) | closed |
| 02-06 | T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов | closed |
| 02-07 | T-02-34 | Information Disclosure | Разрешение имён групп по идентификаторам из расписания без ограничения по владельцу — чужое имя группы попадает в карточку | high | mitigate | Запрос имён ограничен идентификатором пользователя; идентификатор без совпадения в имя не превращается (Задача 1) | closed |
| 02-07 | T-02-35 | Denial of Service | Неизвестное или испорченное значение фильтра роняет страницу | medium | mitigate | Неизвестное значение приводит к варианту «Все», список рендерится нефильтрованным по этой оси (Задача 2) | closed |
| 02-07 | T-02-36 | Tampering | Поисковый термин, отражённый в тексте пустого состояния | medium | mitigate | Автоэкранирование Jinja: разметка в термине рендерится как текст (Задача 2, закреплено тестом) | closed |
| 02-07 | T-02-37 | Elevation of Privilege | Переключение чужого расписания из списка | critical | mitigate | Маршрут переключения не изменён и сохраняет серверную проверку владельца; перекрёстная изоляция закреплена тестом (Задача 3) | closed |
| 02-07 | T-02-38 | Denial of Service | Разрешение имён групп запросом на каждое расписание страницы | low | mitigate | Один запрос на страницу по образцу обогащения в `app/pages/ads.py:17-38`; закреплено тестом на число запросов (Задача 1) | closed |
| 02-07 | T-02-SC | Tampering | Установка пакетов npm/pip/cargo | low | accept | План не устанавливает пакетов; фаза целиком не добавила ни одной зависимости | closed |
| 02-08 | T-02G-01 | Elevation of Privilege | `ads_create` — новый параметр `ad_id` из тела | high | mitigate | Владение подтверждается запросом `Ad.id == ad_id AND Ad.user_id == user.id` ДО любой записи в модель; несовпадение даёт тот же исход, что несуществующая запись, и не создаёт нового черновика (задача 2, тест `test_body_ad_id_from_another_user_is_refused`) | closed |
| 02-08 | T-02G-02 | Information Disclosure | ответ отказа на чужой `ad_id` | medium | mitigate | Текст отказа один для «нет такой записи» и «запись чужая» (`INACCESSIBLE_AD_MESSAGE`) — существование чужой записи не подтверждается | closed |
| 02-08 | T-02G-03 | Tampering | ключи вложений в теле запроса | medium | mitigate | Существующая проверка `own_image_keys` вызывается в `_save_from_editor` ДО первой записи в модель и этим планом не ослабляется; регрессия `tests/test_pages/test_ads_image_ownership.py` остаётся зелёной | closed |
| 02-08 | T-02G-04 | Repudiation | тихая потеря правок при расхождении адреса и записи | high | mitigate | Устраняется по существу: следующее сохранение адресуется в ту же запись, на которую указывает адресная строка (задача 2) | closed |
| 02-08 | T-02G-05 | Denial of Service | повторные автосохранения плодят строки `ads` | medium | mitigate | Вторая строка перестаёт создаваться; `hx-sync="this:replace"` ограничивает число запросов в полёте одним | closed |
| 02-08 | T-02G-SC | Tampering | установка пакетов менеджером пакетов | — | accept | План не устанавливает ни одного пакета: ни `pyproject.toml`, ни `uv.lock` в `files_modified` не входят. Аудит легитимности пакетов не применим | closed |
| 02-09 | T-02G-06 | Elevation of Privilege | `create_schedule` и `update_schedule` — чужие `group_ids` (нарушение авторизации на уровне объекта, ASVS V4) | critical | mitigate | **Именованный дефект фазы.** Аутентифицированный пользователь шлёт свои `ad_id` и `account_id` с чужим `group_ids`; расписание записывается дословно, и (а) имя и внешний идентификатор чужой группы доезжают до `SendLog` со СВОИМ `user_id` — то есть в историю и дашборд атакующего, (б) сообщение уходит в чужой `group_external_id` везде, где своя сессия мессенджера состоит в той же группе. Закрывается `owned_group_ids` на обоих входах: `Group.user_id == user_id AND Group.account_id == account_id`, отказ 404 до записи (задача 2); регрессия — `test_create_rejects_a_group_id_of_another_user`, `test_update_rejects_swapping_in_a_foreign_group_id` | closed |
| 02-09 | T-02G-07 | Information Disclosure | ответ отказа на чужой идентификатор группы | medium | mitigate | Код 404 и текст `"Group not found"` — те же, что у несуществующей группы; существование чужой строки не подтверждается | closed |
| 02-09 | T-02G-08 | Tampering | `toggle_schedule` включает расписание, которое интерфейс объявляет невозможным | high | mitigate | Единственное определение `is_schedule_complete` на обоих входах (WR-05, D-08); регрессия `test_api_toggle_refuses_to_enable_an_incomplete_schedule` | closed |
| 02-09 | T-02G-09 | Spoofing | приостановленное расписание с будущим `next_run_at` вводит пользователя в заблуждение о предстоящей отправке | medium | mitigate | Пересчёт зеркалит страничное правило: неактивное или неполное — `next_run_at` пуст (WR-06) | closed |
| 02-09 | T-02G-10 | Denial of Service | многочастный запрос с файловой частью `times_of_day` даёт 500 | low | mitigate | Проверка типа значения до строковых операций в `_clean_times` (WR-03) | closed |
| 02-09 | T-02G-11 | Repudiation | отказ по данным молча уводит со страницы, теряя правки | medium | mitigate | Возврат в редактор с сообщением, когда объявление подтверждено своим (задача 3, WR-07) | closed |
| 02-09 | T-02G-SC | Tampering | установка пакетов менеджером пакетов | — | accept | План не устанавливает пакетов: `pyproject.toml` и `uv.lock` не входят в `files_modified`. Аудит легитимности пакетов не применим | closed |
| 02-10 | T-02G-12 | Tampering | форма ключа вложения — заякоренный образец и приведение префикса к числу | medium | mitigate | Точное сопоставление без якорей и сравнение префикса как строки: значение с завершающим переводом строки и значение с ведущим нулём отклоняются (задача 1) | closed |
| 02-10 | T-02G-13 | Denial of Service | предел размера загрузки применяется после полной буферизации тела | high | mitigate | Чтение порциями с прерыванием при первом превышении: любой аутентифицированный клиент больше не может удерживать произвольный объём в памяти воркера (задача 2) | closed |
| 02-10 | T-02G-14 | Denial of Service | многочастный запрос с файловой частью в поле вложений даёт ошибку 500 | low | mitigate | Проверка типа значения до строковой операции (задача 3) | closed |
| 02-10 | T-02G-15 | Tampering | затенение имени модуля ответов HTTP значением из формы | low | mitigate | Переименование параметра с сохранением имени на проводе (задача 3) | closed |
| 02-10 | T-02G-16 | Elevation of Privilege | инверсия направления зависимости — окружение шаблонов в графе импорта JSON-API | low | mitigate | Правило ключа поднято в нейтральный модуль; оба слоя зависят от него, а не друг от друга (задача 1) | closed |
| 02-10 | T-02G-SC | Tampering | установка пакетов менеджером пакетов | — | accept | План не устанавливает пакетов: `pyproject.toml` и `uv.lock` не входят в `files_modified`. Аудит легитимности пакетов не применим | closed |
| 02-11 | T-02G-17 | Repudiation | история отправленного могла бы измениться задним числом вслед за правкой объявления | medium | mitigate | Именованный тест утверждает, что снапшот `SendLog` не меняется при убирании вложения; структурного довода недостаточно (задача 1) | closed |
| 02-11 | T-02G-18 | Denial of Service | удаление объекта хранилища оставило бы историю с битыми картинками | medium | mitigate | Тест подменяет функцию удаления объекта на счётчик и утверждает ноль вызовов (задача 1) | closed |
| 02-11 | T-02G-19 | Tampering | отметка выполнения требования, не подтверждённая поведением | medium | mitigate | Предусловие задачи 2: отметка ставится только при зелёном полном прогоне и завершённых планах 02-08, 02-09 И 02-10 (последний доставляет границу лимита вложений и правило порога счётчика — половину обязательных истин ADS-05); зависимость закреплена в `depends_on` и волной 11, а не оставлена на удачу параллельного исполнения; основание записывается в документ | closed |
| 02-11 | T-02G-20 | Information Disclosure | межарендная утечка через список расписаний | — | transfer | Уже закрыта планом 02-09 (T-02G-06); в этом плане только закрепляется тестами сводного списка | closed |
| 02-11 | T-02G-SC | Tampering | установка пакетов менеджером пакетов | — | accept | План не устанавливает пакетов: `pyproject.toml` и `uv.lock` не входят в `files_modified`. Аудит легитимности пакетов не применим | closed |
| 02-12 | T-02G-21 | Denial of Service | применение `0013` к базе, которую владелец не считал целевой | high | mitigate | Блокирующий чекпойнт решения ПЕРЕД любой операцией над базой; вариант с одноразовой базой предложен явно; снятие дампа таблицы `ads` — обязательный первый шаг выбранного пути | closed |
| 02-12 | T-02G-22 | Repudiation | результат человеческой проверки, записанный без наблюдения | high | mitigate | Все двенадцать формулировок `verification: backstop` вносятся ОТДЕЛЬНЫМИ строками исхода, и полнота сверяется со списком `must_haves.truths`, а не с числом в прозе; автономный вердикт модели по судейскому запрету объявлен неавторитетным | closed |
| 02-12 | T-02G-23 | Tampering | правка вёрстки вслепую по описанию симптома | medium | mitigate | Действие прямо запрещает править CSS в рамках плана: находка выносится в SUMMARY как вход следующего прогона | closed |
| 02-12 | T-02G-24 | Information Disclosure | межарендная граница на живой базе после применения ревизии | — | transfer | Закрыта планом 02-09 (T-02G-06) до того, как база станет наблюдаемой; здесь не переоткрывается | closed |
| 02-12 | T-02G-SC | Tampering | установка пакетов менеджером пакетов | — | accept | План не устанавливает пакетов и не меняет ни одного файла исходного кода: `files_modified` пуст. Аудит легитимности пакетов не применим | closed |
| 02-13 | T-02-13-01 | Tampering | ads_create / клиентский ad_id | low | accept | Владение подтверждается одним запросом с обоими условиями до записи (02-08, T-02G-01/02); этот план серверную проверку не трогает — только клиентскую стратегию синхронизации | closed |
| 02-13 | T-02-13-02 | DoS | очередь автосохранения | low | accept | `queue last` держит не более одного запроса в полёте и одного в очереди; дебаунс 2s сохранён; поверхность не расширяется относительно отмены | closed |
| 02-14 | T-02-14-01 | Tampering / DoS | UpdateScheduleRequest → колонки JSON | high | mitigate | `reject_explicit_null`: 422 до какой-либо записи в модель — нечитаемая запись не порождается (это и есть CR-03) | closed |
| 02-14 | T-02-14-02 | DoS | schedule_row.html / sched_card.html | medium | mitigate | Вторая линия `or []` в четырёх местах: испорченная до фикса строка не выключает экраны владельца | closed |
| 02-14 | T-02-14-03 | Elevation | update_schedule владение | low | accept | Владение записью и группами закрыто 02-09 (`get_for_user`, `owned_group_ids`) и этим планом не меняется; регрессии владения в списке verify | closed |
| 02-15 | T-02-15-01 | Repudiation | .planning/REQUIREMENTS.md | low | mitigate | Основание отметки обязано называть удерживающий тест и фактическое число прогона — отметка перестаёт быть неподтверждаемым заявлением | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*
*Transfer-угрозы: T-02G-20 закрыта планом 02-09 и закреплена в 02-11; T-02G-24 (межарендная граница на живой базе после выката 0013) передана решению владельца о выкате — зафиксировано в 02-12 и в UAT-тесте 6.*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| R-02-01 | T-02-SC (02-01) | Фаза не устанавливает ни одного пакета — `02-RESEARCH.md` §Package Legitimacy Audit: таблица пуста, `[SLOP]`/`[SUS]` отсутствуют. Гейт легитимности неприменим | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-02 | T-02-10 (02-02) | Правка данных вне границы фазы (интерфейс, не миграция данных). Диагностический запрос и остаточный риск записываются в SUMMARY как задел бэклога (`02-RESEARCH. | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-03 | T-02-SC (02-02) | План не устанавливает пакетов. `sniff_image` написан руками намеренно: `python-magic` тянет `libmagic`, `imghdr` удалён в Python 3.13, `Pillow` доступна лишь тр | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-04 | T-02-16 (02-03) | Принято пользователем в D-02 (оценка `one-way`). Потеря объявлена комментарием в ревизии в принятом стиле проекта; альтернатива — держать мёртвую колонку вечно | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-05 | T-02-SC (02-03) | План не устанавливает пакетов — `02-RESEARCH.md` §Package Legitimacy Audit пуст | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-06 | T-02-SC (02-04) | План не устанавливает пакетов; htmx 1.9.10 и Alpine 3.13.3 вендорены файлами с Фазы 1, внешних запросов ноль | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-07 | T-02-SC (02-05) | План не устанавливает пакетов | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-08 | T-02-31 (02-06) | D-14 требует полного удаления. Мягкая посадка постоянным редиректом рассмотрена и отклонена; фиксируется как возможный последующий шаг при появлении обращений в | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-09 | T-02-SC (02-06) | План не устанавливает пакетов | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-10 | T-02-SC (02-07) | План не устанавливает пакетов; фаза целиком не добавила ни одной зависимости | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-11 | T-02G-SC (02-08) | План не устанавливает ни одного пакета: ни `pyproject.toml`, ни `uv.lock` в `files_modified` не входят. Аудит легитимности пакетов не применим | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-12 | T-02G-SC (02-09) | План не устанавливает пакетов: `pyproject.toml` и `uv.lock` не входят в `files_modified`. Аудит легитимности пакетов не применим | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-13 | T-02G-SC (02-10) | План не устанавливает пакетов: `pyproject.toml` и `uv.lock` не входят в `files_modified`. Аудит легитимности пакетов не применим | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-14 | T-02G-SC (02-11) | План не устанавливает пакетов: `pyproject.toml` и `uv.lock` не входят в `files_modified`. Аудит легитимности пакетов не применим | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-15 | T-02G-SC (02-12) | План не устанавливает пакетов и не меняет ни одного файла исходного кода: `files_modified` пуст. Аудит легитимности пакетов не применим | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-16 | T-02-13-01 (02-13) | Владение подтверждается одним запросом с обоими условиями до записи (02-08, T-02G-01/02); этот план серверную проверку не трогает — только клиентскую стратегию  | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-17 | T-02-13-02 (02-13) | `queue last` держит не более одного запроса в полёте и одного в очереди; дебаунс 2s сохранён; поверхность не расширяется относительно отмены | plan-time threat model, план утверждён владельцем | 2026-08-11 |
| R-02-18 | T-02-14-03 (02-14) | Владение записью и группами закрыто 02-09 (`get_for_user`, `owned_group_ids`) и этим планом не меняется; регрессии владения в списке verify | plan-time threat model, план утверждён владельцем | 2026-08-11 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-08-11 | 80 | 80 | 0 | gsd-secure-phase orchestrator (L1 short-circuit: plan-time register, все диспозиции закрыты SUMMARY + 895 passed) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-08-11
