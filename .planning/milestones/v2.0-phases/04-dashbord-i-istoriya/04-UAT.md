---
status: complete
phase: 04-dashbord-i-istoriya
source: [04-11-SUMMARY.md, 04-12-SUMMARY.md, 04-REVIEW-FIX.md]
round: 2
previous_round: 04-UAT-round-1.md
started: 2026-08-15T13:37:52Z
updated: 2026-08-15T13:44:27Z
coverage_mode: coverage
auto_covered: 14
---

## Раунд 2 — волна закрытия пробелов и фиксы код-ревью

Первый раунд UAT (95 проверок: 84 автоматических и 11 человеческих, все `pass`, файл `04-UAT-round-1.md`) закрывал **только** планы 04-01..04-10 — это прямо видно в его поле `source`. Позже легли:

- **04-11** — перечень messenger-аккаунтов с состоянием каждого в теле дашборда вместо одного числа (DASH-05). Проверка №48 первого раунда всё ещё описывает старую форму — «число воркеров онлайн».
- **04-12** — окно удержания повтора, переживающее ответ 302, и новый текст плашки `RETRY_BUSY` (HIST-04).
- **17 фиксов код-ревью WR-01…WR-17** (коммиты `3d0534d`…`06b22e7`), из которых человеку видны: текст плашки повтора (WR-02), граница удержания у самого брокера (WR-03), потолок перечня воркеров и точность счётчиков за ним (WR-06), вторая линия проверки баланса внутри `retry_send` (WR-07), нормализация фильтров на админских маршрутах истории (WR-09), верхняя граница окон метрик и графика (WR-11), размер страницы бесконечной прокрутки из контекста (WR-13), маркер обрыва внутри файла выгрузки (WR-15).

Восемь проверок ниже — это два человеческих утверждения из `coverage`-блоков планов 04-11 и 04-12 плюс шесть наблюдаемых следствий волны фиксов. Четырнадцать проверок с `source: automated` закрыты тестами и человеку не показываются.

## Current Test

[testing complete]

## Tests

### 1. Перечень воркеров на дашборде — живой экран
expected: Открыть дашборд на живом стеке с подключёнными messenger-аккаунтами. В теле страницы — блок «Воркеры аккаунтов»: ПЕРЕЧЕНЬ аккаунтов, по строке на аккаунт, а не одно число. У каждой строки точка состояния и слово состояния СОГЛАСОВАНЫ между собой и совпадают с тем, что показывает раздел `/accounts` для того же аккаунта. Выключить контейнер одного воркера — после обновления страницы именно его строка уходит в оффлайн, остальные не меняются. На ширине 320 px перечень читается без горизонтальной прокрутки. У пользователя без аккаунтов на месте перечня — пустое состояние «Каналы не подключены» с призывом подключить канал.
why_human: Коверейдж-утверждение 04-11 D9 (`human_judgment`). Автоматической проверки ширины в проекте нет, план объявил это утверждение backstop-ом. Совпадение экрана с `/accounts` и реакция на выключение контейнера проверяются глазами на живом сервере. Плюс WR-04: слово состояния теперь ветвится по булеву контракта шелла, а не по второму литералу — согласованность точки и слова видна только на экране.
result: pass

### 2. Два нажатия «Повторить» при ВЫКЛЮЧЕННОМ JavaScript
expected: Отключить JavaScript в браузере. В истории у неуспешной записи нажать «Повторить», затем сразу нажать ещё раз (форма отправляется без Alpine, страница перезагружается между нажатиями). В целевую группу мессенджера приходит РОВНО ОДНО сообщение, с баланса списывается РОВНО ОДИН платёж. Затем включить JavaScript обратно и убедиться, что панель подтверждения повтора не сломана и работает как прежде.
why_human: Коверейдж-утверждение 04-12 D7 (`human_judgment`). Главный ассерт — одна доставка в реальную стороннюю группу и одно списание реального баланса; ни то, ни другое внутрипроцессным тестом не наблюдаемо: клиент очереди в тестах подменён, а доставка и биллинг живут за границей веб-процесса. Отключения JavaScript автоматизация фазы не имеет. Сюда же ложатся WR-03 (удержание взводится ДО вызова брокера, освобождается только на `OperationalError`) и WR-07 (баланс перепроверяется уже внутри `retry_send`).
result: pass

### 3. Текст плашки «повтор уже запущен» на экране
expected: Спровоцировать плашку `RETRY_BUSY` (нажать «Повторить» второй раз по той же записи в пределах минуты). На экране читается: «Повтор этой отправки уже запущен или запускался только что — второй раз он не уйдёт. Повторите через минуту. Если повтор ушёл в очередь, результат появится новой записью истории.» Текст понятен, не обещает записи в истории безусловно и объясняет, что делать дальше.
why_human: WR-02. Тест ассертит два инварианта формулировки (отказ заявлен; обещание новой записи условное), но читаемость и достаточность текста для живого пользователя — решение владельца, а не ассерт.
result: pass

### 4. Мусорное значение фильтра в админской истории пользователя
expected: Открыть админскую историю конкретного пользователя и подставить в адрес заведомо мусорное значение оси — например `?status=<script>` или `?messenger=nonsense`. Экран НЕ пустеет: мусорная ось просто не применяется, записи показываются как без фильтра. Сырая подставленная строка нигде в разметке страницы не появляется. Поведение совпадает с пользовательской историей на том же мусоре.
why_human: WR-09. Три новых теста ловят регрессию (проверено откатом `admin.py`), но совпадение админского экрана с пользовательским и отсутствие визуального мусора проверяются глазами на живых данных.
result: pass

### 5. Бесконечная прокрутка истории и админской истории
expected: Пролистать историю вниз до конца на живых данных — записи подгружаются партиями, без дублей и без пропусков на стыках партий, прокрутка останавливается на последней записи. То же самое в админской истории пользователя, в обоих представлениях (таблица и карточки).
why_human: WR-13. Размер страницы переведён из четырёх литералов в контекст (`page_size`), причём в частичных ответах он равен тому `limit`, которым отобрана ИМЕННО эта партия. Расхождение с `next_offset` проявилось бы дублем или дырой на стыке — это видно только при живой прокрутке.
result: pass

### 6. Плитки метрик и график активности на боевом PostgreSQL
expected: На боевом стеке сверить четыре плитки за скользящие сутки и график активности с реальными данными. Запись с временем отправки В БУДУЩЕМ (если такая есть или её можно завести) в плитки и на график НЕ попадает. Дельта к предыдущим суткам считается осмысленно.
why_human: WR-11. Верхняя граница `sent_at <= now` добавлена в оба запроса; два теста сеют записи на `now`, `now+1s` и `now+2/3 суток`. На боевом PostgreSQL проверяется, что тип времени и таймзона не сдвигают границу иначе, чем в SQLite тестов.
result: pass

### 7. Обрыв выгрузки посреди потока
expected: Спровоцировать сбой во время потоковой выгрузки истории в CSV (например, оборвав соединение с БД в середине большой выгрузки). Скачанный файл СОДЕРЖИТ последней строкой маркер «ВЫГРУЗКА ОБОРВАНА — ФАЙЛ НЕПОЛНЫЙ», то есть обрыв заявлен внутри самого файла, а не остаётся молчаливо усечённым набором строк. Здоровая выгрузка такого маркера не содержит.
why_human: WR-15. Форма потокового ответа не позволяет сменить HTTP-код после первого фрагмента — это принятый техдолг (предупреждение в 04-VERIFICATION.md). Тест ассертит маркер, вызывая функцию маршрута напрямую, потому что через ASGI-клиент исключение внутри потокового тела роняет уже отданные фрагменты. Поведение на боевом стеке с реальной отменой — человеческая проверка.
result: pass

### 8. Смоук после волны из 17 фиксов
expected: Пройти по дашборду и истории после всей волны: обе страницы открываются без ошибок, живая лента тикает, фильтры и чипсы работают, выгрузка скачивается, повтор доступен только у неуспешных записей, копирование текста ошибки работает. Ничего из принятого в первом раунде не сломано.
why_human: 17 фиксов легли ПОСЛЕ приёмки первого раунда и после отчёта верификации. Суита зелёная (1420 passed), но регрессия в уже принятом поведении — ровно тот класс, ради которого раунд 2 и запускается.
result: pass

### 9. Пользователь читает с дашборда, КАКОЙ из его аккаунтов онлайн, а какой нет — перечнем, а не одним числом
expected: Пользователь читает с дашборда, КАКОЙ из его аккаунтов онлайн, а какой нет — перечнем, а не одним числом
result: pass
source: automated
coverage_id: D1
verification: tests/test_pages/test_shell.py#test_dashboard_lists_each_account_with_its_worker_state

### 10. Аккаунт с незнакомым статусом остаётся в перечне и показывает своё сырое значение
expected: Аккаунт с незнакомым статусом остаётся в перечне и показывает своё сырое значение
result: pass
source: automated
coverage_id: D2
verification: tests/test_pages/test_shell.py#test_dashboard_worker_list_keeps_an_unrecognised_status_visible

### 11. Пользователь без аккаунтов видит на месте перечня пустое состояние с призывом подключить канал
expected: Пользователь без аккаунтов видит на месте перечня пустое состояние с призывом подключить канал
result: pass
source: automated
coverage_id: D3
verification: tests/test_pages/test_shell.py#test_dashboard_worker_list_shows_an_empty_state_without_accounts

### 12. Аккаунты другого пользователя в перечень и в счёт не попадают
expected: Аккаунты другого пользователя в перечень и в счёт не попадают
result: pass
source: automated
coverage_id: D4
verification: tests/test_pages/test_shell.py#test_dashboard_worker_list_excludes_another_users_account

### 13. Ни учётные данные, ни строка сессии messenger-аккаунта на дашборд не выводятся
expected: Ни учётные данные, ни строка сессии messenger-аккаунта на дашборд не выводятся
result: pass
source: automated
coverage_id: D5
verification: tests/test_pages/test_shell.py#test_shell_worker_list_carries_no_secrets; tests/test_pages/test_shell.py#test_shell_worker_entries_expose_only_the_declared_keys

### 14. Число в пилюле шелла и перечень не могут разойтись: оба выведены из одного прочитанного списка
expected: Число в пилюле шелла и перечень не могут разойтись: оба выведены из одного прочитанного списка
result: pass
source: automated
coverage_id: D6
verification: tests/test_pages/test_shell.py#test_shell_aggregate_is_derived_from_the_worker_list; tests/test_pages/test_shell.py#test_dashboard_page_has_no_second_source_of_the_sessions_number

### 15. Поаккаунтное чтение стоит одно обращение и не растёт с числом аккаунтов (нет N+1 на пути рендера)
expected: Поаккаунтное чтение стоит одно обращение и не растёт с числом аккаунтов (нет N+1 на пути рендера)
result: pass
source: automated
coverage_id: D7
verification: tests/test_pages/test_shell.py#test_shell_reads_worker_state_in_a_single_query

### 16. Путь рендера дашборда, включая новый модуль-владелец чтения состояния воркеров, не обращается к Docker
expected: Путь рендера дашборда, включая новый модуль-владелец чтения состояния воркеров, не обращается к Docker
result: pass
source: automated
coverage_id: D8
verification: tests/test_pages/test_shell.py#test_dashboard_render_path_never_touches_docker

### 17. Два последовательных нажатия «Повторить» по одной записи ставят РОВНО ОДНУ задачу и списывают один платёж баланса
expected: Два последовательных нажатия «Повторить» по одной записи ставят РОВНО ОДНУ задачу и списывают один платёж баланса
result: pass
source: automated
coverage_id: D1
verification: tests/test_pages/test_history_retry.py#test_two_sequential_retries_queue_exactly_one_task

### 18. Пересекающиеся во времени запросы по-прежнему дают одну постановку — существующая защита не потеряна
expected: Пересекающиеся во времени запросы по-прежнему дают одну постановку — существующая защита не потеряна
result: pass
source: automated
coverage_id: D2
verification: tests/test_pages/test_history_retry.py#test_retry_of_a_busy_record_queues_no_second_task

### 19. Окно истекает: по прошествии срока та же запись снова повторяема (это окно, а не вечный замок)
expected: Окно истекает: по прошествии срока та же запись снова повторяема (это окно, а не вечный замок)
result: pass
source: automated
coverage_id: D3
verification: tests/test_pages/test_history_retry.py#test_retry_becomes_possible_again_after_the_cooldown_window

### 20. Запрос, не дошедший до очереди (черновик, выключенная группа, исчерпанный баланс, исключение), окна удержания не оставляет
expected: Запрос, не дошедший до очереди (черновик, выключенная группа, исчерпанный баланс, исключение), окна удержания не оставляет
result: pass
source: automated
coverage_id: D4
verification: tests/test_pages/test_history_retry.py#test_a_refused_retry_arms_no_cooldown; tests/test_pages/test_history_retry.py#test_a_balance_refusal_arms_no_cooldown; tests/test_pages/test_history_retry.py#test_retry_releases_the_slot_after_an_exception

### 21. Удержание ключуется по КОНКРЕТНОЙ записи: повтор соседней неуспешной записи не блокируется
expected: Удержание ключуется по КОНКРЕТНОЙ записи: повтор соседней неуспешной записи не блокируется
result: pass
source: automated
coverage_id: D5
verification: tests/test_pages/test_history_retry.py#test_retry_cooldown_is_keyed_per_record

### 22. Докстринг обработчика, комментарий реестра и плашка RETRY_BUSY описывают ровно ту гарантию, которую даёт код
expected: Докстринг обработчика, комментарий реестра и плашка `RETRY_BUSY` описывают ровно ту гарантию, которую даёт код
result: pass
source: automated
coverage_id: D6
verification: tests/test_pages/test_history_retry.py#test_retry_handler_description_matches_the_mechanism; tests/test_pages/test_history_retry.py#test_retry_busy_notice_states_the_real_guarantee; tests/test_pages/test_history_retry.py#test_retry_slot_registry_documents_its_limit

## Summary

total: 22
passed: 22
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
