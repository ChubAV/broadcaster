---
phase: 04-dashbord-i-istoriya
plan: 06
type: execute
wave: 4
depends_on: ["04-01", "04-05"]
files_modified:
  - app/pages/history.py
  - app/templates/history/list.html
  - app/templates/history/partial_cards.html
  - app/templates/history/includes/filter_chips.html
  - app/static/css/app.css
  - tests/test_pages/test_history.py
autonomous: true
requirements: [HIST-01]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Фильтры статуса, канала и периода выбираются чипсами из макета, и выбор применяется одним действием без кнопки «Применить» (D-29)"
    - "Чипсы — обычные ссылки: смена фильтра работает при выключенном JavaScript"
    - "Чипсы статуса покрывают все три значения журнала, включая отключённый аккаунт, а не два из трёх"
    - "Чипсы канала покрывают все три канала проекта"
    - "Фильтр по аккаунту сохранён и реализован выпадающим списком рядом с чипсами (D-29)"
    - "Варианты периода — сегодня, 7 дней, 30 дней, всё время; произвольного диапазона дат нет (D-30)"
    - "Период «сегодня» отсчитывается от локальной полуночи пользователя"
    - "Над списком показывается точное число найденного отдельным запросом с теми же фильтрами (D-31)"
    - "Число найденного совпадает с числом записей, которые вернёт выгрузка тех же фильтров"
    - "Активный набор фильтров переживает бесконечную прокрутку — разметка проброса фильтров в паршале и на странице остаётся идентичной"
    - "Пустой результат фильтров даёт отдельный текст со сбросом, отличный от текста «отправок вообще нет» (D-41)"
    - "Новый файл шаблона положен вне каталога компонентов, поэтому инвентаризация библиотеки компонентов не сдвигается"
  artifacts:
    - path: app/templates/history/includes/filter_chips.html
      provides: "Макрос группы чипсов-ссылок для фильтров истории"
      contains: "macro filter_chips"
    - path: tests/test_pages/test_history.py
      provides: "Собственный файл тестов истории (сегодня они размазаны по двум файлам)"
  key_links:
    - from: app/templates/history/list.html
      to: app/templates/history/includes/filter_chips.html
      via: "импорт макроса чипсов"
      pattern: "history/includes/filter_chips.html"
    - from: app/pages/history.py
      to: app/application/analytics/send_analytics.py
      via: "счётчик найденного считается history_count с теми же фильтрами, что список"
      pattern: "history_count"
---

<objective>
Фильтры истории переезжают на чипсы из макета, над списком появляется точное число найденного, а
пустой результат фильтров получает собственный текст со сбросом.

Purpose: HIST-01 и критерий 3 фазы. Точное число (D-31) — не украшение: оно честно говорит,
сколько строк уйдёт в выгрузку, и служит опорой потолка экспорта в плане 04-08.
Output: макрос чипсов-ссылок, линейка счётчика, отдельное пустое состояние, свой файл тестов.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashbord-i-istoriya/04-CONTEXT.md
@.planning/phases/04-dashbord-i-istoriya/04-RESEARCH.md
@.planning/phases/04-dashbord-i-istoriya/04-PATTERNS.md
@.planning/phases/04-dashbord-i-istoriya/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чипсы-ссылки для статуса, канала и периода</name>
  <precondition>`app/application/analytics/send_analytics.py` экспортирует `apply_history_filters`, `history_filter_params` и `HISTORY_PERIODS` (создано планом 04-01)</precondition>
  <files>app/templates/history/includes/filter_chips.html, app/templates/history/list.html, app/pages/history.py, app/templates/history/partial_cards.html, app/static/css/app.css, tests/test_pages/test_history.py</files>
  <read_first>
    - `app/templates/history/list.html` — текущая секция фильтров: обёртка `filters(...)`, четыре выпадающих списка, кнопки «Применить» и «Сбросить»; какие значения уже приходят в шаблон
    - `app/static/css/app.css` (строки 1372-1412) — примитив `.chip` / `.chip-set` / `.chip__input` / `.chip__label` / `.chip--on`; макроса чипсов в проекте НЕТ, есть только этот примитив
    - `app/templates/ads/includes/sched_card.html` (строки 145-210) — существующие потребители примитива с настоящими полями ввода
    - `app/templates/schedules/includes/schedule_row.html` (строка 110) — потребитель `.chip--on` только для показа
    - `app/templates/components/filters.html` — сигнатура обёртки `filters(id, action, method, open_on_desktop, label)`, которая остаётся для выпадающего списка аккаунта
    - `app/pages/history.py` — обработчики списка и паршала после переезда фильтров планом 04-01
    - `app/application/analytics/send_analytics.py` — `HISTORY_PERIODS` и семантика неизвестного значения
    - `design/new_broadcaster_design.unpacked.html` (строки 810-824) — группы чипсов и кнопка «СБРОСИТЬ»
    - `tests/test_pages/test_responsive_markup.py` (строки 1870-1885) — `test_template_inventory`: число файлов в каталоге компонентов фиксировано
  </read_first>
  <behavior>
    - Выбор чипса статуса меняет выборку: при статусе «Ошибка» в ответе остаются только записи неуспешных отправок.
    - Чипсы статуса покрывают три значения журнала: успешно, ошибка, аккаунт отключён — плюс вариант «все».
    - Чипсы канала покрывают три канала проекта плюс вариант «все».
    - Чипсы периода покрывают сегодня, 7 дней, 30 дней и «всё время».
    - Активный чипс несёт признак активности в разметке; остальные — нет.
    - Переход по чипсу сохраняет остальные активные фильтры в адресе.
    - Запись со старым пустым значением канала не попадает ни под один чипс конкретного канала и остаётся видимой при варианте «все».
    - Фильтр по аккаунту продолжает работать выпадающим списком и сохраняется при смене чипса.
    - Неизвестное значение фильтра в адресе не роняет страницу: ответ 200, фильтр не применён.
    - Разметка сохраняется при выключенном JavaScript: чипс — ссылка, а не элемент, требующий обработчика.
  </behavior>
  <action>
Создать `app/templates/history/includes/filter_chips.html` с макросом
`filter_chips(options, active, base_params, param_name)`: группа чипсов-ссылок, где каждый чипс —
`<a>` с классом чипса и признаком активности у выбранного, а адрес собирается из базовых
параметров фильтра с подменой одного значения. Шаблон кладётся в каталог включений раздела
истории, а НЕ в каталог компонентов: инвентаризация библиотеки компонентов фиксирует число
файлов, и новый файл там потребовал бы правки константы в том же плане. Докстринг макроса
обязан назвать это решение явно.

⚠️ Макроса чипсов, на который ссылается D-29, в проекте не существует: решение Фазы 2 о
переиспользовании относилось к обёртке сворачиваемых фильтров, а чипсы существуют только как
CSS-примитив с классами `.chip`, `.chip-set`, `.chip--on`. Макрос строится ПОВЕРХ этого
примитива и новых классов не вводит — utility-классы по проекту запрещены и проверяются тестом.

Форма выбрана ссылками, а не полями ввода с формой: базовый путь без JavaScript получается в
один клик вместо «выбрать плюс применить», а существующие потребители примитива с полями ввода
остаются нетронутыми.

Переверстать секцию фильтров в `app/templates/history/list.html`: три группы чипсов — статус,
канал, период; выпадающий список аккаунта остаётся внутри существующей обёртки фильтров с
кнопкой применения, потому что аккаунтов может быть много и чипсы переполнятся (D-29). Кнопка
«СБРОСИТЬ» по макету (строки 818-822) ведёт на адрес истории без параметров и отображается,
когда хотя бы один фильтр активен.

Наборы значений: статус — все, успешно, ошибка, аккаунт отключён (три значения журнала, а не два:
сегодняшний список знает только два и теряет отключённый аккаунт); канал — все, Telegram,
WhatsApp, MAX; период — сегодня, 7 дней, 30 дней, всё время из `HISTORY_PERIODS`. Подписи —
русские, значения — те же строки, что лежат в журнале и понимает фильтр.

В `app/pages/history.py` прокинуть в шаблон списка и паршала набор доступных значений и активные
значения фильтров. `_parse_account_id` остаётся на месте. Передавать пользователя в
`apply_history_filters`, чтобы период «сегодня» считался от локальной полуночи.

Обращение со старыми записями, у которых канал или группа пустые (Claude's Discretion): такая
запись не совпадает ни с одним чипсом конкретного канала и видна только при варианте «все»;
скрывать её из «всех» нельзя — это настоящая отправка. Решение зафиксировать в докстринге
макроса и закрепить тестом.

В `app/static/css/app.css` добавить правила чипса-ссылки поверх существующего примитива:
состояние наведения и активное состояние у элемента-ссылки (сегодня примитив рассчитан на
поле ввода внутри), и раскладку линейки групп чипсов на узких ширинах — переносом, а не
горизонтальной прокруткой.

Создать `tests/test_pages/test_history.py` и написать в нём тесты из блока behavior. Сегодня
тесты истории размазаны по разметочному файлу и файлу сохранности HTMX; новый файл — их
собственное место, существующие тесты в других файлах не переносить и не дублировать.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_history.py -k filters -x -q</automated>
    <automated>uv run pytest tests/test_pages/ tests/test_templates/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/history/includes/filter_chips.html` существует и содержит `macro filter_chips`
    - Каталог `app/templates/components/` содержит ровно 13 файлов — инвентаризация компонентов не сдвинута
    - `app/templates/history/list.html` содержит три вызова `filter_chips(` и сохраняет выпадающий список аккаунта
    - `GET /history?status=account_disconnected` возвращает 200 и только записи с этим статусом
    - `GET /history?messenger=max` возвращает 200 и только записи канала MAX
    - `GET /history?period=today` возвращает 200 и отсекает записи по локальной полуночи пользователя
    - `GET /history?period=не-такой-период` возвращает 200 и не применяет отсечку
    - `uv run pytest tests/test_pages/ tests/test_templates/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Статус, канал и период выбираются чипсами-ссылками, работают без JavaScript, покрывают все значения журнала и сохраняют остальные фильтры.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Точное число найденного и пустой результат фильтров</name>
  <files>app/pages/history.py, app/templates/history/list.html, app/static/css/app.css, tests/test_pages/test_history.py</files>
  <read_first>
    - `app/pages/history.py` — обработчик списка после задачи 1: где считать счётчик и что класть в контекст
    - `app/application/analytics/send_analytics.py` — сигнатура `history_count` из плана 04-01
    - `app/templates/history/list.html` (строки 36-56) — блок списка, сентинел бесконечной прокрутки и текущее пустое состояние
    - `app/templates/history/partial_cards.html` — разметка сентинела, ИДЕНТИЧНАЯ разметке в списке; правится один — синхронно правится второй
    - `app/pages/common.py` (строки 171-192) — `plural_ru` для склонения «N записей»
    - `app/templates/components/empty_state.html` — сигнатура пустого состояния с призывом к действию
    - `design/new_broadcaster_design.unpacked.html` (строки 826-830 и 867-872) — линейка со счётчиком и ссылкой выгрузки; пустое состояние «Ничего не найдено»
    - `tests/test_pages/test_htmx_preserved.py` (строка 209 и окрестности) — `test_infinite_scroll_keeps_filters`
  </read_first>
  <behavior>
    - Над списком отображается число найденного, склонённое по-русски.
    - Число совпадает с фактическим количеством записей, удовлетворяющих активному набору фильтров, а не с числом записей на странице.
    - При смене фильтра число меняется соответственно.
    - При пустом наборе фильтров число равно общему числу записей пользователя.
    - Пустой результат при активных фильтрах даёт текст «Ничего не найдено» с подсказкой изменить фильтры или период и кнопкой сброса.
    - Пустой результат без активных фильтров даёт прежний текст об отсутствии отправок вообще — это разные состояния.
    - Бесконечная прокрутка продолжает сохранять фильтры: разметка сентинела на странице и в паршале остаётся идентичной.
  </behavior>
  <action>
В `app/pages/history.py` в обработчике списка вызвать `history_count` с тем же набором фильтров,
что и основной запрос, и положить результат в контекст. Счётчик считается ОТДЕЛЬНЫМ запросом —
тот же приём, что применён к счётчикам на экране групп аккаунта. В паршале бесконечной прокрутки
счётчик не пересчитывается: он живёт над списком и подменой не затрагивается.

Добавить в `app/templates/history/list.html` линейку над списком по макету строки 826-830: слева
число найденного mono-шрифтом со склонением через `plural_ru`, посередине разделительная линия,
справа место под ссылку выгрузки. Саму ссылку выгрузки добавляет план 04-08 — здесь готовится
место и правило раскладки, но пустая ссылка не рендерится.

Развести два пустых состояния: при активном наборе фильтров и нулевом результате рендерится
«Ничего не найдено» с подсказкой «измените фильтры или период» и кнопкой сброса, ведущей на
адрес истории без параметров; при отсутствии активных фильтров остаётся прежний текст об
отсутствии отправок. Оба состояния рисуются существующим макросом пустого состояния — своей
разметки не заводить.

Разметку сентинела бесконечной прокрутки не трогать. Если правка всё же понадобится — она
обязана быть внесена одинаково в оба файла: инвариант «разметка сентинела здесь и в паршале
идентична» выписан комментарием в обоих и закреплён тестом сохранения фильтров.

В `app/static/css/app.css` добавить правила линейки счётчика: mono-подписи, разделительная линия
на всю оставшуюся ширину, перенос на узких ширинах.

Дописать тесты из блока behavior в `tests/test_pages/test_history.py`, включая прямое сравнение
числа счётчика с длиной полной выборки по тем же фильтрам — именно это сравнение делает обещание
«выгружен именно отфильтрованный результат» проверяемым в плане 04-08.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_history.py -q</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_preserved.py::test_infinite_scroll_keeps_filters -q</automated>
    <automated>uv run pytest tests/test_pages/ tests/test_templates/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/history.py` содержит вызов `history_count(`
    - `app/templates/history/list.html` содержит линейку счётчика над списком
    - `GET /history` с посеянными записями возвращает 200 и содержит число, равное количеству посеянных записей
    - `GET /history?status=fail` при отсутствии неуспешных записей возвращает 200 и содержит текст «Ничего не найдено» и ссылку сброса
    - `GET /history` у пользователя без единой записи возвращает 200 и содержит прежний текст об отсутствии отправок
    - `uv run pytest tests/test_pages/test_htmx_preserved.py::test_infinite_scroll_keeps_filters -q` завершается кодом 0
    - `uv run pytest tests/test_pages/ tests/test_templates/ -q` завершается кодом 0
  </acceptance_criteria>
  <done>Над списком стоит точное число найденного тем же набором фильтров, а пустой результат фильтров отличается от «отправок вообще нет».</done>
</task>

</tasks>

## Artifacts this phase produces

Символы, создаваемые ЭТИМ планом:

- Шаблон `app/templates/history/includes/filter_chips.html` и макрос `filter_chips`
- Линейка счётчика над списком истории и CSS-правила чипса-ссылки (модификатор поверх существующего примитива `.chip`)
- Файл тестов `tests/test_pages/test_history.py`

Остальные символы фазы перечислены в разделе «Artifacts this phase produces» плана 04-01.

## Decisions resolved in this plan

- **D-29 и несуществующий «макрос чипсов Фазы 2»:** решение Фазы 2 касалось обёртки
  сворачиваемых фильтров, а чипсы в проекте существуют только как CSS-примитив. Макрос
  чипсов-ссылок создаётся здесь поверх примитива, кладётся в каталог включений раздела истории и
  не сдвигает инвентаризацию библиотеки компонентов.
- **Старые записи с пустым каналом или группой (Claude's Discretion):** такая запись не
  совпадает ни с одним чипсом конкретного канала и видна при варианте «все»; из «всех» она не
  скрывается — это настоящая отправка.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → `GET /history` | значения фильтров приходят из query-строки и попадают в условия выборки |
| данные мессенджера → разметка карточки | имя группы и текст ошибки пришли извне |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-22 | Information Disclosure | счётчик найденного | high | mitigate | `history_count` вызывается с идентификатором текущего пользователя, условие владения стоит в базовом запросе счётчика; тест сравнивает число с выборкой того же пользователя |
| T-04-23 | Tampering | неизвестное значение чипса в адресе | low | mitigate | значение вне допустимого набора отсечки не применяет и не поднимает 500; закреплено тестом |
| T-04-24 | Information Disclosure | подстановка чужого идентификатора аккаунта в фильтр | medium | mitigate | фильтр по аккаунту сужает выборку внутри записей текущего пользователя; базовое условие владения снять невозможно, потому что оно стоит до применения фильтров |
| T-04-25 | Tampering | сборка адреса чипса из активных параметров | low | mitigate | значения приводятся к строке и экранируются для адреса тем же приёмом, что уже применён к пробросу фильтров в сентинеле прокрутки |
| T-04-SC | Tampering | установка пакетов npm/pip/cargo | low | accept | план не устанавливает пакетов |
</threat_model>

<verification>
- `uv run pytest tests/test_pages/ tests/test_templates/ -q` — зелено
- Каталог компонентов по-прежнему содержит 13 файлов
- Существующие гарантии сохранения фильтров при прокрутке зелены
</verification>

<success_criteria>
- Статус, канал и период выбираются чипсами и работают без JavaScript
- Фильтр по аккаунту сохранён
- Над списком стоит точное число найденного тем же набором фильтров
- Пустой результат фильтров отличается от отсутствия отправок вообще
- Раздел истории пригоден к использованию на мобильных ширинах
</success_criteria>

<output>
Create `.planning/phases/04-dashbord-i-istoriya/04-06-SUMMARY.md` when done
</output>
