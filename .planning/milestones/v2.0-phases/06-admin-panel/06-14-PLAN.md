---
phase: 06-admin-panel
plan: 14
type: execute
wave: 10
depends_on: ["06-13"]
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/test_pages/test_admin_panel.py
  - .planning/phases/06-admin-panel/06-14-SUMMARY.md
autonomous: true
# Единственный план фазы, правящий реестр требований по ИТОГУ, и единственный, где живут
# человеческие пункты приёмки. Он идёт последним намеренно: проверять вёрстку и живое
# поведение имеет смысл на собранной фазе, а не по частям.
requirements: [ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, ADMIN-07, ADMIN-08, ADMIN-09, ADMIN-10, ADMIN-11]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Все девять требований фазы отмечены в реестре ПО ПРОВЕРЕННОМУ КОДУ с указанием плана и команды, а не по факту исполнения плана."
    - "Счётчики реестра пересчитаны арифметикой по дереву, а не поправлены на глаз; журнал изменений файла дополнен строкой с номером плана и датой."
    - "Пять критериев успеха фазы перечислены в сводке с планом и командой у каждого."
    - "Полная суита проекта зелёная; трекинг счёта планов сходится с реестром."
    - "Ни одна из шести вкладок подраздела не потеряна: сквозная проверка обходит все шесть адресов и подтверждает наличие вкладок на каждом."
    - statement: "Все шесть подразделов пригодны к использованию на ширине 375 пикселей: горизонтального переполнения нет, вкладки и элементы управления достижимы."
      verification: backstop
    - statement: "Строка воркера на живом стенде показывает простой: воркер, убитый без штатного завершения, через 90 секунд перестаёт читаться живым."
      verification: backstop
    - statement: "Плашка недоступного источника логов рисуется на живом стенде с неподнятым мониторингом."
      verification: backstop
    - "Три допущения исследования, снимаемые только на стенде (имя контейнера службы канала telegram в метках, разбираемый формат журналирования в бою, видимость очередей воркеров из веб-процесса), проверены человеком и записаны фактом с датой."
  artifacts:
    - path: ".planning/phases/06-admin-panel/06-14-SUMMARY.md"
      provides: "Сводная приёмка фазы: пять критериев с планом и командой, итоги человеческих проверок, перечень несделанного"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/06-admin-panel/"
      via: "каждое из девяти требований отмечено с указанием закрывшего плана и команды-свидетеля"
      pattern: "ADMIN-0"
  prohibitions:
    - statement: "MUST NOT отмечать требование выполненным по факту исполнения плана: правило проекта требует отметки по ПРОВЕРЕННОМУ КОДУ, и отметка вперёд кода уже случалась здесь однажды — она превращает реестр из свидетельства в намерение"
      category: transparency
    - statement: "MUST NOT считать человеческие пункты закрытыми зелёной суитой: три поведения фазы недоказуемы автоматически в этом окружении, и молчаливое их закрытие означает, что о непроверенном отчитались как о проверенном"
      category: transparency
---

<objective>
Принять фазу: свести пять критериев успеха с планами и командами, закрыть три поведения,
которые в этом окружении доказуемы только человеком, отметить девять требований по
проверенному коду и назвать несделанное несделанным.

Purpose: фаза добавила шесть подразделов, новый путь аутентификации и действующую
блокировку. Часть её обещаний машинно не проверяется здесь ни при каком старании: Redis и
источник логов в среде разработки отсутствуют, браузерного стенда для измерения вёрстки в
суите нет. Эти три поведения обязаны попасть в человеческую приёмку явными пунктами, а не
раствориться в зелёной суите.

Output: отмеченные требования, пересчитанные счётчики, сводка приёмки с итогами
человеческих проверок и перечнем переходящих долгов.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-admin-panel/06-CONTEXT.md
@.planning/phases/06-admin-panel/06-VALIDATION.md
@.planning/phases/06-admin-panel/06-13-SUMMARY.md
</context>

## Artifacts this phase produces (план 06-14)

| Символ | Место | Вид |
|--------|-------|-----|
| сквозная проверка шести подразделов | `tests/test_pages/test_admin_panel.py` | тест-обход |
| отметки девяти требований и пересчитанные счётчики | `.planning/REQUIREMENTS.md` | записи реестра |
| сводка приёмки фазы | `.planning/phases/06-admin-panel/06-14-SUMMARY.md` | документ |

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Сквозная проверка фазы — шесть подразделов, ни одной потерянной вкладки</name>

  <files>
    tests/test_pages/test_admin_panel.py
  </files>

  <read_first>
    tests/test_pages/test_admin_panel.py (весь накопленный файл после планов 06-01, 06-05, 06-07, 06-08, 06-10),
    tests/test_pages/test_shell.py:100-125 (форма обхода админских адресов),
    tests/test_pages/test_responsive_markup.py:1900-1975 (форма обхода нескольких адресов с проверкой признаков разметки),
    app/pages/admin.py (итоговый перечень вкладок и все маршруты подраздела),
    .planning/phases/06-admin-panel/06-VALIDATION.md § Per-Task Verification Map целиком (какая команда какое требование доказывает)
  </read_first>

  <behavior>
    - Тест 1: обход всех шести адресов подразделов — каждый отвечает 200 администратору, и на каждом присутствуют вкладки со всеми шестью ссылками.
    - Тест 2: на каждом из шести адресов признак активной вкладки встречается ровно один раз и указывает на текущий подраздел.
    - Тест 3: на каждом из шести адресов раздел админ-панели подсвечен в навигации шелла.
    - Тест 4: обход всех шести адресов посторонним пользователем — каждый отвечает 403.
    - Тест 5: ни один из шести адресов не роняет 500 при недоступных Redis и источнике логов одновременно.
    - Тест 6: ни один шаблон и ни один маршрут проекта не ссылается на адрес снесённого справочника групп — страховочная сетка плана 06-01 по-прежнему зелёная.
  </behavior>

  <action>
    Дописать в файл тестов админ-панели сквозную проверку фазы: обход всех шести адресов
    одним параметризованным тестом с проверкой вкладок, подсветки и прав.

    ⚠️ Это НЕ повтор тестов отдельных подразделов. Те проверяют содержимое каждого; этот
    проверяет, что после тринадцати планов ни один подраздел не потерял вкладки, не сменил
    подсветку и не начал ронять 500 при отсутствующих внешних службах. Именно такие потери
    происходят в конце фазы, когда каждый план правил тот же файл.

    Отдельным случаем — одновременная недоступность обеих внешних служб: подразделы обязаны
    отвечать 200 с честными плашками, а не 500. В среде разработки обе службы отсутствуют
    по умолчанию, поэтому этот случай проверяется подменой клиентов, а не удачей окружения.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_admin_panel.py -q -x</automated>
    <automated>uv run pytest tests/test_pages -q</automated>
    <automated>just test</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_admin_panel.py -q` завершается кодом 0.
    - Сквозной обход параметризован шестью адресами — число случаев в отчёте равно шести на каждый из обходных тестов.
    - Тест одновременной недоступности внешних служб присутствует и проверяет все шесть адресов.
    - `just test` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Шесть подразделов проверены как ОДНО целое: вкладки на месте, подсветка одна, права
    везде, и ни один не падает, когда внешних служб нет.
  </done>

  <reversibility rating="reversible">
    Дополнение файла тестов.
  </reversibility>
</task>

<task type="auto">
  <name>Задача 2: Человеческая приёмка — три недоказуемых поведения и три допущения стенда</name>

  <files>
    .planning/phases/06-admin-panel/06-14-SUMMARY.md
  </files>

  <read_first>
    .planning/phases/06-admin-panel/06-VALIDATION.md § Manual-Only Verifications целиком (три поведения с инструкциями),
    .planning/phases/06-admin-panel/06-RESEARCH.md § Assumptions Log целиком (⚠️ строки о метке источника службы канала telegram, формате журналирования в бою и видимости очередей воркеров из веб-процесса),
    .planning/phases/06-admin-panel/06-RESEARCH.md § Environment Availability целиком,
    .planning/phases/05.1-edinaya-podpiska/05.1-13-PLAN.md (форма человеческой приёмки вёрстки на узкой ширине по названным порогам — образец),
    justfile (команды подъёма мониторинга и боевого стенда — их имена уходят в инструкции)
  </read_first>

  <action>
    Провести человеческую приёмку и записать её итоги фактами с датой.

    Первое — вёрстка шести подразделов на узкой ширине. Проверяются все шесть, а не один:
    у них разная раскладка (плитки, строки в две колонки состояния, строки очереди, строки
    логов, журнал платежей), и узкое место у каждого своё.

    Второе — простой воркера на живом стенде. Единственный способ увидеть, что стухший
    ключ живости читается мёртвым, — убить воркер без штатного завершения и подождать
    дольше порога. В суите это проверено на подменённых значениях; на стенде проверяется
    сам механизм.

    Третье — плашка недоступного источника логов. Мониторинг в среде разработки не поднят,
    и подраздел обязан показать именно плашку, а не пустой список.

    Заодно снять три допущения исследования, которые в этом репозитории не проверяются:
    фактическое имя контейнера службы канала telegram в метках источника, разбираемый ли
    формат журналирования в бою, и видны ли очереди воркеров из веб-процесса. Каждое
    записать фактом с датой и командой, которой оно снято; расхождение — отдельной строкой.

    Итоги всех шести проверок записать в сводку плана: что проверено, чем, когда и с каким
    результатом. Непройденный пункт записывается непройденным, а не переписывается в
    «частично».
  </action>

  <verify>
    <human-check>
      1. **Вёрстка на 375 пикселях.** Открыть каждый из шести подразделов на ширине 375 и
         проверить: нет горизонтального переполнения; вкладки достижимы (прокруткой,
         переносом или списком — форма выбрана исполнителем); в «Воркерах» обе колонки
         состояния читаются; строка очереди и строка лога не рвут раскладку; журнал
         платежей читается; блок инцидентов на «Обзоре» не наезжает на плитки; кнопки
         перезапуска и снятия задачи нажимаемы. Пройдено, если все шесть пригодны к
         использованию.

      2. **Простой воркера.** Поднять стенд, дождаться запуска воркера канала WhatsApp,
         убить контейнер БЕЗ штатного завершения, подождать дольше порога свежести и
         открыть «Воркеры». Пройдено, если строка показывает простой при пустой очереди и
         отключение при непустой, а не «в работе».

      3. **Плашка недоступного источника логов.** Открыть «Логи» при неподнятом мониторинге.
         Пройдено, если видна плашка с причиной и командой подъёма, а не пустой список.

      4. **Имя источника службы канала telegram.** Выполнить перечисление имён контейнеров
         на стенде и сверить с перечнем источников подраздела логов. Пройдено, если имя
         совпадает; расхождение записать и поправить словарь источников.

      5. **Формат журналирования в бою.** Убедиться, что переменная формата журналирования
         на боевом стенде даёт разбираемый сборщиком вывод. Пройдено, если у строк в
         источнике есть метка уровня.

      6. **Видимость очередей из веб-процесса.** Выполнить перечисление ключей очередей
         воркеров с того же адреса брокера, который читает веб-процесс. Пройдено, если
         ключи видны; иначе «Воркеры» и «Очередь» покажут пустоту и это НЕ ошибка кода.
    </human-check>
  </verify>

  <acceptance_criteria>
    - Сводка содержит результат каждой из шести человеческих проверок с датой и командой либо описанием действия.
    - Непройденные пункты названы непройденными и заведены переходящими долгами с адресатом.
    - Расхождение по имени источника службы канала telegram (если оно есть) записано отдельной строкой и закрыто правкой словаря источников.
    - Ни один из трёх пунктов раздела недоказуемых поведений не отмечен закрытым со ссылкой на зелёную суиту.
  </acceptance_criteria>

  <done>
    Три поведения, которые в этом окружении не доказываются машиной, проверены человеком и
    записаны фактами; три допущения исследования сняты или названы неснятыми.
  </done>

  <reversibility rating="reversible">
    Записи в сводке; кода задача не меняет.
  </reversibility>
</task>

<task type="auto">
  <name>Задача 3: Реестр требований по проверенному коду и сводка приёмки фазы</name>

  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/06-admin-panel/06-14-SUMMARY.md
  </files>

  <read_first>
    .planning/REQUIREMENTS.md целиком (⚠️ формулировки ADMIN-03…ADMIN-11, обе таблицы прослеживаемости, счётчики и журнал изменений файла; ⚠️ частичный вердикт ADMIN-02, внесённый планом 06-01 — его не трогать),
    .planning/ROADMAP.md § «Phase 6: Админ-панель» (пять критериев успеха — источник таблицы сводки),
    .planning/phases/06-admin-panel/06-VALIDATION.md § Per-Task Verification Map (какая команда какое требование доказывает — источник колонки «команда»),
    .planning/phases/06-admin-panel/ (все сводки исполненных планов — источник колонки «план»),
    .planning/phases/05.1-edinaya-podpiska/05.1-12-PLAN.md (форма отметки требований по проверенному коду и пересчёта счётчиков арифметикой по дереву — образец)
  </read_first>

  <action>
    Отметить девять требований фазы в реестре ПО ПРОВЕРЕННОМУ КОДУ. Правило записано в
    самом файле прошлыми планами и здесь исполняется буквально: отметка ставится не за
    исполненный план, а за проверенное поведение, и у каждой отметки называется команда,
    которой оно проверено. Требование, чьё поведение закрыто человеческой приёмкой задачи 2,
    отмечается со ссылкой на её результат, а не на суиту.

    Обе таблицы прослеживаемости привести в соответствие. Счётчики пересчитать арифметикой
    по дереву требований, а не поправить на глаз: расхождение счётчика с таблицей уже
    случалось в этом файле и закрывалось отдельным разбором.

    Дописать строку в журнал изменений файла: номер плана, дата, что изменилось и на каком
    основании. Частичный вердикт ADMIN-02, внесённый планом 06-01, НЕ трогать — он уже
    записан со своим основанием.

    В сводку плана свести приёмку фазы:
    — таблица пяти критериев успеха с планом и командой у каждого;
    — итоги шести человеческих проверок задачи 2;
    — перечень переходящих долгов: отсутствие автоповтора отправки на канале telegram
      (докстринг обещает то, чего в коде нет — заведено отдельным todo, в фазе не чинится),
      неподтверждённая подписка на событие отмены платежа во внешней платёжной системе,
      невыкаченная очередь ревизий, и всё, что человеческая приёмка назвала непройденным;
    — порядок выката: вход под пользователем едет на бой не раньше плана 06-13, признак
      транспортной защиты cookie включается не раньше подтверждения защищённого шаблона;
    — перечень решений владельца, принятых чекпойнтами фазы, со своими номерами.

    Обновить граф проекта после правок кода — командой проекта, а не вручную.
  </action>

  <verify>
    <automated>just test</automated>
    <automated>just tracking-check</automated>
    <automated>uv run pytest tests/test_planning -q</automated>
    <automated>grep -c 'ADMIN-0' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - Все девять требований ADMIN-03…ADMIN-11 отмечены в дереве требований и в таблице прослеживаемости.
    - У каждой отметки в сводке названы план и команда-свидетель.
    - Счётчики реестра сходятся с деревом — проверяется прогоном проверок планирования.
    - Журнал изменений реестра дополнен строкой с номером плана, датой и основанием.
    - Частичный вердикт ADMIN-02 в файле сохранён без изменений.
    - Сводка содержит таблицу пяти критериев успеха, итоги шести человеческих проверок, перечень переходящих долгов, порядок выката и перечень решений владельца с номерами.
    - `just test` завершается кодом 0.
    - `just tracking-check` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Реестр требований говорит правду о том, что закрыто и чем это доказано; сводка фазы
    отвечает на вопросы «чем закрыт каждый критерий», «что проверил человек» и «что осталось
    несделанным» — без необходимости читать четырнадцать сводок планов.
  </done>

  <reversibility rating="reversible">
    Правки документов планирования; кода задача не меняет.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| человеческая приёмка → запись о выполненности | утверждение о безопасности фазы, не подкреплённое машиной |
| реестр требований → будущие фазы | отметка «выполнено» читается как основание не проверять заново |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-ACC1 | Repudiation | отметка требования вперёд проверенного кода | medium | mitigate | Отметка ставится по проверенному коду с названной командой-свидетелем; правило записано в самом файле реестра и исполняется буквально |
| T-06-ACC2 | Repudiation | человеческий пункт, закрытый ссылкой на зелёную суиту | medium | mitigate | Три недоказуемых поведения перечислены поимённо; закрытие каждого требует записи с датой и описанием действия |
| T-06-ACC3 | Information Disclosure | выкат имперсонации до гейта запретов | high | mitigate | Порядок выката записан в сводке отдельным абзацем: механика не едет на бой раньше плана 06-13 |
| T-06-SC | Tampering | установка пакетов (npm/pip/cargo) | — | N/A | Установок нет |
</threat_model>

<verification>
- `just test` — полная суита зелёная.
- `just tracking-check` — трекинг сходится.
- `uv run pytest tests/test_planning -q` — проверки планирования зелёные.
- Человеческая приёмка шести пунктов проведена и записана фактами с датой.
- Девять требований отмечены с указанием плана и команды.
</verification>

<success_criteria>
- Шесть подразделов проверены как одно целое и не падают без внешних служб.
- Три недоказуемых машиной поведения проверены человеком и записаны.
- Три допущения исследования сняты на стенде или названы неснятыми.
- Девять требований отмечены по проверенному коду, счётчики сходятся.
- Сводка отвечает на «чем закрыт критерий», «что проверил человек» и «что осталось».
</success_criteria>

<output>
Create `.planning/phases/06-admin-panel/06-14-SUMMARY.md` when done
</output>
