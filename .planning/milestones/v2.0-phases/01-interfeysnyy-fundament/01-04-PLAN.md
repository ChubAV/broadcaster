---
phase: 01-interfeysnyy-fundament
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - app/templates/schedules/list.html
  - app/templates/schedules/form.html
  - app/templates/schedules/partial_cards.html
  - app/templates/groups/list.html
  - app/templates/groups/partial_cards.html
  - app/templates/components/filters.html
  - app/static/css/app.css
  - tests/test_pages/test_responsive_markup.py
autonomous: true
requirements: [UI-04, UI-05, UI-06]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Страницы расписаний и групп отрисовываются в новом шелле и содержат адаптивные примитивы"
    - "Заголовки и кнопки действий обоих разделов переехали в шапку шелла и не дублируются на странице"
    - "Бесконечная прокрутка расписаний и групп подгружает вторую страницу выдачи"
    - "Фильтры групп протаскиваются во вторую страницу выдачи и не сбрасываются"
    - "Сворачивание блока фильтров работает на мобильной ширине и не мигает при загрузке страницы"
    - "Тумблеры включения расписаний и групп отправляют те же запросы, что и до перевёрстки"
    - "Форма расписания сохраняет все атрибуты name и маршрут отправки"
    - "Все 393 существующих теста остаются зелёными"
    - statement: "Utility-классы удалённого CSS-фреймворка отсутствуют в пяти шаблонах обоих разделов"
      verification: backstop
  artifacts:
    - path: "app/templates/schedules/list.html"
      provides: "Список расписаний на компонентах с адаптивными примитивами"
      contains: "block page_title"
    - path: "app/templates/schedules/form.html"
      provides: "Форма расписания на макросах полей"
    - path: "app/templates/groups/list.html"
      provides: "Список групп с унифицированным блоком фильтров"
    - path: "app/templates/components/filters.html"
      provides: "Макрос сворачиваемого блока фильтров — единственный оставшийся сценарий Alpine после D-12"
      contains: "{% macro filters"
  key_links:
    - from: "app/templates/groups/list.html"
      to: "app/templates/groups/partial_cards.html"
      via: "идентичный сентинел с протаскиванием фильтров"
      pattern: "filter_params"
    - from: "app/templates/groups/list.html"
      to: "app/templates/components/filters.html"
      via: "{% from \"components/filters.html\" import filters %}"
      pattern: "components/filters.html"
  prohibitions:
    - statement: "MUST NOT терять цикл проброса фильтров в URL сентинела — вторая страница выдачи, молча потерявшая фильтр, показывает пользователю чужие данные вперемешку с отфильтрованными и выглядит при этом как исправно работающий список"
      category: safety
---

<objective>
Перевести разделы «Расписания» и «Группы» на дизайн-систему.

Purpose: два раздела с самым плотным набором интерактивных элементов после аккаунтов — тумблеры,
фильтры, бесконечная прокрутка с протаскиванием фильтров. Здесь же унифицируется последний оставшийся
сценарий Alpine.

Output: 5 мигрированных шаблонов, макрос сворачиваемых фильтров, дополненный `test_responsive_markup.py`.

Образец — раздел «Объявления» из Плана 03. Структура списочной страницы, форма сентинела и контракт
заголовка через блоки шелла берутся оттуда дословно и не переизобретаются.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-interfeysnyy-fundament/01-CONTEXT.md
@.planning/phases/01-interfeysnyy-fundament/01-02-SUMMARY.md
@.planning/phases/01-interfeysnyy-fundament/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Список расписаний (UI-04, UI-05, UI-06)</name>
  <files>app/templates/schedules/list.html, app/templates/schedules/partial_cards.html, tests/test_pages/test_responsive_markup.py</files>
  <read_first>
    - app/templates/schedules/list.html — целиком: импорт иконок (строка 5), список, сентинел (строка 67), тумблеры паузы и запуска
    - app/templates/schedules/partial_cards.html — целиком: сентинел, идентичный тому, что в list.html
    - app/templates/ads/list.html и app/templates/ads/partial_cards.html — результат Плана 03: эталон структуры, повторить дословно
    - app/templates/components/table.html, badge.html, toggle.html, button.html, empty_state.html — сигнатуры Плана 02
    - app/pages/schedules.py — строки 40-75: контекст списка и партиала
    - design/new_broadcaster_design.unpacked.html — строки 737-806: раздел «Расписания» в макете
  </read_first>
  <behavior>
    Дополнить `tests/test_pages/test_responsive_markup.py` параметром `schedules`:
    - `test_list_page_has_responsive_primitives[schedules]` — выдача `/schedules` содержит `data-row`
    - `test_list_page_no_utility_classes[schedules]`
    - `test_schedules_card_renders_data` — выдача содержит имя созданного расписания (ловит потерю
      контекста при переводе include в макрос: страница вернёт 200 с пустыми карточками, а не упадёт)
    Регрессия: `test_infinite_scroll_chain[schedules]` из Плана 03 остаётся зелёным.
  </behavior>
  <action>
Переверстать список расписаний по образцу `ads/list.html` из Плана 03.

Заголовок раздела и кнопку создания перенести в `{% block page_title %}` и `{% block page_actions %}`;
собственный заголовок из страницы удалить, иначе он задвоится с шапкой шелла.

Список — на примитивах `data-rowhead` / `data-row` / `data-grow` через макросы `table.html`. Состояние
расписания — через `badge` с явным вариантом (`success` для активного, `neutral` для приостановленного);
маппинг «домен → вариант» остаётся в шаблоне, потому что единого перечисления статусов в проекте нет.
Пустое состояние — через `empty_state`. Кнопки паузы, запуска, редактирования и удаления — через
`button` с иконками; иконки `icon_pause` / `icon_play` / `icon_edit` / `icon_delete` из
`includes/icons.html` переиспользуются как есть.

**Сентинел бесконечной прокрутки.** Остаётся последним элементом внутри того же контейнера, что и
карточки; `list.html` и `partial_cards.html` несут идентичный сентинел — правится один, синхронно
правится второй; целевой элемент неявный, атрибут цели не добавлять; параметр компоновки из URL убрать.

Тумблеры включения и паузы переводятся на макрос `toggle`, но **отправляют те же запросы на те же
маршруты тем же методом**, что и сейчас. Правило фазы — «новый вид, старые действия».

Utility-классы вырезаются полностью; оформление берётся из семантических классов `app.css`.
Инлайн-стили из макета не копировать.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_preserved.py -x -q</automated>
    <automated>just test</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/schedules/list.html` содержит `{% block page_title %}` и `{% block page_actions %}`
    - `app/templates/schedules/list.html` и `app/templates/schedules/partial_cards.html` оба содержат `hx-trigger="revealed"` и `hx-swap="outerHTML"`
    - `grep -c 'layout=cards' app/templates/schedules/list.html` == 0 и то же для `partial_cards.html`
    - `grep -c 'hx-target' app/templates/schedules/` == 0
    - `grep -rc 'bg-white\|text-gray\|rounded-lg\|border-gray\|divide-' app/templates/schedules/list.html app/templates/schedules/partial_cards.html | grep -v ':0' | wc -l` == 0
    - Оба файла содержат импорт из `components/`
    - Поведение: GET `/schedules` → 200, тело содержит имя созданного расписания и `data-row`
    - Поведение: запрос партиала расписаний со смещением 30 возвращает сентинел со смещением строго больше 30
    - Поведение: маршруты паузы и запуска расписания по-прежнему меняют состояние расписания
    - `just test` завершается с кодом 0
  </acceptance_criteria>
  <done>Список расписаний собран из компонентов, содержит адаптивные примитивы, цепочка прокрутки цела,
  действия над расписанием работают через прежние маршруты.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Форма расписания (UI-04)</name>
  <files>app/templates/schedules/form.html</files>
  <read_first>
    - app/templates/schedules/form.html — целиком, 189 строк: самая крупная форма раздела; выбор групп, дней недели, времени и таймзоны
    - app/pages/schedules.py — обработчики создания и редактирования: какие имена полей читаются
    - app/templates/components/field.html, button.html, toggle.html, card.html — сигнатуры Плана 02
    - tests/ — существующие утверждения, завязанные на точную разметку выбора: проверки присутствия атрибута выбранного значения относительно конкретных подстрок. Найти их до правки формы
    - design/new_broadcaster_design.unpacked.html — строки 558-600: блок расписания в редакторе макета
  </read_first>
  <behavior>
    Регрессия — существующие тесты страниц расписаний должны остаться зелёными без правок, кроме тех,
    что завязаны на порядок атрибутов в элементе выбора. Если такой тест ломается, он правится в том же
    коммите, что и шаблон, и правка отражается в SUMMARY.
  </behavior>
  <action>
Переверстать форму создания и редактирования расписания на макросы `field`, `select_field`,
`textarea_field`, `toggle` и `button`; группировку блоков — через `card_open` / `card_close`.

**Инварианты, которые нельзя терять — форма перестанет сохраняться молча:**
- атрибуты `name` всех полей переносятся дословно, обработчик читает именно их;
- `method` и `action` формы не меняются;
- скрытые поля переносятся как есть;
- множественный выбор групп и выбор дней недели сохраняют прежние имена и значения — это то, что
  превращается в записи расписания.

**Осторожно с элементами выбора.** В тестах проекта есть утверждения, проверяющие присутствие атрибута
выбранного значения относительно конкретной подстроки в разметке. Они сломаются, если `select_field`
расставит атрибуты в другом порядке. Найти такие утверждения **до** правки шаблона: либо сохранить
порядок атрибутов существующей разметки в макросе, либо синхронно поправить тест в том же коммите.
Молча оставить красный тест нельзя.

Известно, что Фаза 2 переносит настройку расписаний в редактор объявления (SCH-04, ADS-07) и эта форма
изменится. Работа частично переделается — принятый пользователем компромисс D-06.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/ -q</automated>
    <automated>just test</automated>
    <human-check>Создать расписание через форму и отредактировать его: все поля видны, выбор групп и дней работает, сохранение проходит, значения подставляются при повторном открытии.</human-check>
  </verify>
  <acceptance_criteria>
    - `app/templates/schedules/form.html` содержит импорт из `components/field.html`
    - `grep -c 'method="post"' app/templates/schedules/form.html` >= 1
    - `grep -rc 'bg-white\|text-gray\|rounded-lg\|border-gray' app/templates/schedules/form.html | grep -v ':0' | wc -l` == 0
    - Набор атрибутов `name` в форме совпадает с набором до правки (сверить выводом `grep -o 'name="[a-z_]*"' app/templates/schedules/form.html | sort -u` до и после)
    - Поведение: POST создания расписания создаёт запись; POST редактирования обновляет её
    - Поведение: GET формы редактирования подставляет сохранённые значения
    - `just test` завершается с кодом 0
  </acceptance_criteria>
  <done>Форма расписания собрана из макросов полей, сохранила все имена полей и маршрут, создание и
  редактирование расписаний работают.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Список групп и унификация сворачиваемых фильтров (UI-04, UI-05, UI-06)</name>
  <files>app/templates/groups/list.html, app/templates/groups/partial_cards.html, app/templates/components/filters.html, app/static/css/app.css, tests/test_pages/test_responsive_markup.py</files>
  <read_first>
    - app/templates/groups/list.html — целиком, 178 строк: блок фильтров на Alpine (строки 40 и 44), список, сентинел с протаскиванием фильтров (строка 141), тумблеры включения групп
    - app/templates/groups/partial_cards.html — целиком: сентинел с тем же циклом проброса фильтров
    - app/templates/history/list.html — строки 10 и 13: тот же паттерн сворачивания фильтров, будет переведён на новый макрос в Плане 05
    - app/templates/admin/user_history.html — третье применение того же паттерна, План 08
    - app/static/css/app.css — правило скрытия элементов с отложенной инициализацией, перенесённое Планом 01
    - app/templates/components/toggle.html, table.html, empty_state.html — сигнатуры Плана 02
    - app/pages/groups.py — строки 125-160: контекст списка и партиала, состав фильтров
    - design/new_broadcaster_design.unpacked.html — строки 711-736: раздел «Группы» в макете
  </read_first>
  <behavior>
    Дополнить `tests/test_pages/test_responsive_markup.py` параметром `groups`:
    - `test_list_page_has_responsive_primitives[groups]`
    - `test_list_page_no_utility_classes[groups]`
    - `test_groups_card_renders_data` — выдача `/groups` содержит имя созданной группы
    - `test_groups_filters_survive_pagination` — запросить партиал групп с фильтром и смещением;
      утверждать, что тот же фильтр присутствует в URL следующего сентинела
    Регрессия: `test_infinite_scroll_keeps_filters[groups]` из Плана 03 остаётся зелёным.
  </behavior>
  <action>
Переверстать список групп и создать макрос сворачиваемого блока фильтров.

**Макрос `components/filters.html`.** После D-12 из Alpine в проекте остаётся ровно один сценарий —
«свернуть/развернуть блок фильтров», применённый в трёх местах (группы, история, история пользователя
в админке). Вынести его в один макрос `filters(id, open_on_desktop=true)` с парным закрывающим
макросом или слотом для содержимого. Сегодня видимость управляется привязкой класса к состоянию, а
классы эти принадлежат удалённому фреймворку — заменить на семантические классы `app.css`
(`.filters`, `.filters--collapsed`, `.filters__toggle`) и добавить их в `app.css`. Новых
захардкоженных цветов не вводить — только токены `var(--...)` из Плана 01.

Правило скрытия элементов с отложенной инициализацией уже перенесено Планом 01 в `app.css`. Без него
свёрнутый блок фильтров мигает на долю секунды при загрузке страницы — проверить, что правило
действительно применяется к новому макросу.

**Сентинел с фильтрами — самое хрупкое место раздела.** Кроме обычных инвариантов (последний элемент
внутри того же контейнера, идентичен в `list.html` и `partial_cards.html`, неявная цель, параметр
компоновки убран) здесь есть цикл, протаскивающий текущие фильтры в URL следующей порции. Этот цикл
переносится **дословно**, вместе с приведением значений к строке и экранированием для URL. Если его
потерять, вторая страница выдачи вернёт нефильтрованные данные, а список при этом будет выглядеть
исправным.

Список групп — на примитивах `data-row`; тумблеры включения групп — на макрос `toggle` с сохранением
прежних маршрутов и методов; пустое состояние — через `empty_state`; иконка мессенджера —
существующий макрос `messenger_icon`, он переиспользуется как есть.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py -x -q</automated>
    <automated>uv run pytest tests/test_pages/test_htmx_preserved.py -x -q</automated>
    <automated>just test</automated>
    <human-check>Открыть /groups на ширине меньше 860px: блок фильтров свёрнут и не мигает при загрузке, разворачивается по нажатию. Применить фильтр и прокрутить список до подгрузки второй страницы — подгруженные группы соответствуют фильтру.</human-check>
  </verify>
  <acceptance_criteria>
    - `app/templates/components/filters.html` содержит `{% macro filters`
    - `app/static/css/app.css` содержит `.filters`
    - `app/templates/groups/list.html` содержит `components/filters.html`
    - `app/templates/groups/list.html` и `app/templates/groups/partial_cards.html` оба содержат `filter_params`
    - Оба файла содержат `hx-trigger="revealed"` и `hx-swap="outerHTML"`
    - `grep -c 'layout=cards' app/templates/groups/list.html` == 0 и то же для `partial_cards.html`
    - `grep -rc 'bg-white\|text-gray\|rounded-lg\|border-gray\|hidden sm:' app/templates/groups/ | grep -v ':0' | wc -l` == 0
    - Поведение: GET `/groups` → 200, тело содержит имя созданной группы и `data-row`
    - Поведение: запрос партиала групп с фильтром и смещением 30 возвращает сентинел, URL которого содержит тот же фильтр и смещение строго больше 30
    - Поведение: маршрут включения группы по-прежнему меняет её состояние
    - `just test` завершается с кодом 0
  </acceptance_criteria>
  <done>Список групп собран из компонентов, фильтры вынесены в переиспользуемый макрос без мигания при
  загрузке, протаскивание фильтров во вторую страницу выдачи сохранено.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (План 04)

**Новый макрос**
- `app/templates/components/filters.html::filters(id, open_on_desktop=true)` — единственный оставшийся сценарий Alpine после D-12; потребители: группы (здесь), история (План 05), история пользователя в админке (План 08)

**Новые CSS-классы (`app/static/css/app.css`)**
- `.filters`, `.filters--collapsed`, `.filters__toggle`

**Новые тесты (`tests/test_pages/test_responsive_markup.py`)**
- Параметры `schedules` и `groups` в `test_list_page_has_responsive_primitives` и `test_list_page_no_utility_classes`
- `test_schedules_card_renders_data`, `test_groups_card_renders_data`, `test_groups_filters_survive_pagination`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → фильтры групп | Значения фильтров приходят от клиента, попадают в запрос к БД и обратно в URL сентинела |
| браузер → маршруты тумблеров | Изменяющие действия над расписаниями и группами |
| данные групп и расписаний → HTML | Названия групп приходят из внешних мессенджеров и не контролируются приложением |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | цикл проброса фильтров в URL сентинела | high | mitigate | Цикл переносится дословно вместе с приведением к строке и экранированием для URL. Значение фильтра попадает в атрибут, поэтому autoescape Jinja2 обязан остаться включённым. Покрыто `test_groups_filters_survive_pagination` |
| T-04-02 | Tampering | названия групп из внешних мессенджеров в разметке | high | mitigate | Название группы — недоверенная строка из внешней системы. Выводится только через autoescape, ни один макрос не принимает готовую разметку. Покрыто `test_no_unsafe_escaping` из Плана 02 |
| T-04-03 | Elevation of Privilege | маршруты тумблеров расписаний и групп | high | mitigate | Маршруты, методы и серверные проверки владельца не меняются — меняется только оформление элемента управления. Поведенческие критерии проверяют, что состояние действительно меняется |
| T-04-04 | Information Disclosure | список групп и расписаний | high | mitigate | Фильтрация выборки по владельцу остаётся в обработчиках, план меняет только шаблон. Регрессия ловится полной суитой |
| T-04-05 | Denial of Service | бесконечная прокрутка групп | medium | accept | Ограничения `offset` и `limit` — существующее поведение обработчика; фаза их не меняет |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Пакеты не устанавливаются (D-02). Новых зависимостей нет; записей `[ASSUMED]`/`[SUS]`/`[SLOP]` в аудите легитимности нет |
</threat_model>

<verification>
1. `uv run pytest tests/test_pages/test_responsive_markup.py -x -q` — зелёный с новыми параметрами
2. `uv run pytest tests/test_pages/test_htmx_preserved.py -x -q` — страховочная сетка Плана 03 цела
3. `just test` — после каждой задачи; D-07 требует работоспособности после плана
4. Ручная проверка сворачивания фильтров на мобильной ширине и фильтрованной подгрузки
</verification>

<success_criteria>
- 5 шаблонов двух разделов собраны из компонентов, utility-классов не осталось
- Цепочки бесконечной прокрутки обоих разделов целы, фильтры групп переживают пагинацию
- Сворачиваемые фильтры вынесены в один макрос, применимый в трёх местах проекта
- Действия над расписаниями и группами работают через прежние маршруты
- Полная суита зелёная
</success_criteria>

<flagged_assumptions>
- **Edge-проб (UI-04, UI-05, UI-06)** — строки `unclassified` / `unresolved` из-за русскоязычного текста требований; предикаты взяты из `01-RESEARCH.md` §Validation Architecture.
- **`schedules/form.html` будет переделан в Фазе 2** — настройка расписаний переезжает в редактор объявления (SCH-04, ADS-07). Частичная переделка принята пользователем в D-06.
- **Существующие утверждения на порядок атрибутов элемента выбора** могут сломаться при переводе формы на макрос. План требует найти их до правки и поправить синхронно в том же коммите, а не оставлять красными.
- **`app.css` дополняется в этом плане** (классы фильтров). Это допустимо, потому что планы фазы исполняются последовательно волнами и параллельной правки одного файла не возникает; дополнения обязаны использовать токены Плана 01, а не новые захардкоженные значения.
</flagged_assumptions>

<output>
Создать `.planning/phases/01-interfeysnyy-fundament/01-04-SUMMARY.md` по завершении.
Зафиксировать: сигнатуру макроса `filters` (Планы 05 и 08 будут его вызывать) и любые правки
существующих тестов с указанием причины.
</output>
