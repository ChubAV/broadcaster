---
phase: 05.1-edinaya-podpiska
plan: 13
subsystem: billing
tags: [human-verdict, manual-acceptance, mobile-375, requirements-registry, gap-closure, uat-debt]

# Dependency graph
requires:
  - phase: 05.1-05
    provides: "Раздел подписки в четырёх состояниях панели и семь числовых порогов ручной приёмки на 375px — предмет проверки"
  - phase: 05.1-12
    provides: "Счётчики §Coverage (v2.0) на входе: закрытых 27, в работе 2, ожидающих 12, итог 41; BILL-05 и BILL-07 намеренно оставлены `In Progress`"
  - phase: 05.1-14
    provides: "Вердикт человека по формулировкам и ЯВНОЕ решение НЕ возвращать BILL-09 в `In Progress` — ветка возврата не исполняется"
provides:
  - "ВЕРДИКТ ВЛАДЕЛЬЦА о вёрстке раздела подписки на 375px, вынесенный в НАСТОЯЩЕМ браузере на настоящей отдаваемой сервером странице"
  - "BILL-05 и BILL-07 переведены `In Progress` → `Complete` в обеих таблицах реестра"
  - "Счётчики §Coverage (v2.0) после отметки: закрытых 29, в работе 0, ожидающих 12, итог 41"
  - "Перенесённый долг единственного открытого пункта UAT фазы 5 закрыт проверкой, а не вторым переносом"
affects: [ship-v2.0]

# Что этот план НЕ доказывает — вынесено в frontmatter, чтобы не потерялось
limitations:
  - "Вердикт вынесен ЦЕЛИКОМ («вёрстка не ломается»), а не разбивкой по семи порогам с числами: таблицы 7 × 4 у этой приёмки НЕТ, и плановый приёмочный критерий в этой части НЕ выполнен"
  - "Состояния `paid` и `comped` в сессии приёмки не заводились: владелец смотрел `trial` и `expired` (обе редакции)"
  - "Регрессии у этого свидетельства нет: следующая правка раздела потребует приёмки заново"

estimate:
  tokens: 20000
  tasks: 2

actuals:
  tokens: 6850    # chars/4 по реализованному диффу: 17872 симв. этой сводки + 9528 симв. добавленных строк реестра и deferred-items = 27400 / 4
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Ручная приёмка на боевой машине ведётся против ОТДЕЛЬНОЙ локальной базы с перекрытием ровно тех значений, что мешают старту, — боевой `DATABASE_URL` не переопределяется частично, а заменяется целиком"
    - "Экран, недостижимый браузером с машины, где поднято приложение, показывается через SSH-туннель — а не подменяется снимком или чтением исходника"
    - "Вердикт человека записывается В ТОЙ ФОРМЕ, в какой он вынесен; недостающая форма называется отступлением, а не дописывается исполнителем"

key-files:
  created:
    - .planning/phases/05.1-edinaya-podpiska/05.1-13-SUMMARY.md
  modified:
    - .planning/REQUIREMENTS.md

decisions:
  - "Приёмка вёрстки на 375px ПРОЙДЕНА (владелец, 2026-08-21): вёрстка не ломается — ничего не уезжает вбок, кнопки и суммы читаются, текст не наезжает"
  - "Владелец объявил условие отметки НАСТУПИВШИМ: BILL-05 и BILL-07 отмечаются по этому вердикту, несмотря на отсутствие разбивки по семи порогам"
  - "Ветка возврата BILL-09 в `In Progress` НЕ исполнялась: решения о возврате в `05.1-14-SUMMARY.md` нет — ни один текст не отклонён"
  - "Отсутствие таблицы 7 × 4 записано отступлением от плана, а не заполнено исполнителем: заполненная им таблица была бы машинным разбором на месте человеческого наблюдения"

requirements-completed: [BILL-05, BILL-07]

# Metrics
duration: ~25 мин (включая ручной просмотр владельцем через SSH-туннель)
completed: 2026-08-21

status: complete
---

# Phase 05.1 Plan 13: Ручная приёмка вёрстки на 375px и отметка BILL-05/BILL-07 — Summary

**Единственный открытый пункт UAT фазы 5 — отрисовка раздела подписки на 375px — закрыт ПРОВЕРКОЙ, а не вторым переносом: владелец сузил окно настоящего браузера до 375px на настоящей отдаваемой сервером странице и подтвердил, что вёрстка не ломается; по этому вердикту BILL-05 и BILL-07 переведены в `Complete`, и счётчики v2.0 стали 29 / 0 / 12 при итоге 41.**

⚠️ **Ни одной строки кода этот план не тронул.** Изменённых файлов два: эта сводка и `.planning/REQUIREMENTS.md`.
⚠️ **Ни одна команда против боевого стека не выполнялась.** Это записано ниже фактом, с перечислением того, что запускалось вместо неё.

## Performance

- **Duration:** ~25 мин
- **Completed:** 2026-08-21
- **Tasks:** 2 (первая — `checkpoint:human-verify`, исполнена владельцем; вторая — правка реестра)
- **Files created:** 1 (эта сводка). **Files modified:** 1 (`.planning/REQUIREMENTS.md`)

## Задача 1 — ручная приёмка `/billing` на 375px

### Вердикт

**ПРИЁМКА ПРОЙДЕНА. Вердикт вынес ВЛАДЕЛЕЦ, 2026-08-21.**

Дословно: раздел подписки открыт в настоящем браузере, окно сужено до **375px**, и вёрстка **не ломается** — ничего не уезжает вбок, кнопки и суммы читаются, текст не наезжает.

⚠️ **Исполнитель не выносил по этому пункту ни одного суждения и не выводил вердикт из кода.** Плановая прохибиция здесь соблюдена буквально: приёмку рендера может провести только человек, потому что браузерной оснастки в проекте нет — `playwright`, `selenium` и `puppeteer` отсутствуют и в `pyproject.toml`, и в `uv.lock`, а тесты читают CSS и разметку КАК ТЕКСТ и проверяют, что правило ОБЪЯВЛЕНО, а не что оно отрисовалось.

### Условия просмотра — записаны целиком, потому что путь был нестандартным

Плановая подготовка (`just run`, порт 8000, локальная база) исполнена **не буквально**. Замена сделана в сторону строгости — см. «Deviations from Plan», отступление 1. Устройство здесь не деталь, а условие достоверности вердикта:

- **сессия идёт НА БОЕВОЙ МАШИНЕ**, и `DATABASE_URL` в `.env` указывает на **БОЕВУЮ** базу. Поэтому `just run` как есть был бы посевом состояний в бой и **не запускался ни разу**;
- поднят **отдельный локальный PostgreSQL 18** в контейнере (`127.0.0.1:55433`); схема накачена `alembic upgrade head` до ревизии **`0020`**;
- приложение поднято против ЭТОЙ базы на **`127.0.0.1:8001`** с перекрытием ровно трёх значений:
  - `DATABASE_URL` — цель уведена с боя на локальную копию,
  - `WA_BRIDGE_URLS` — в `.env` значение записано не JSON-массивом, а простой строкой, и pydantic его не разбирает,
  - `SMTP_HOST=""` — гейт `settings.smtp_host` (`app/routes/auth.py:43`) требует подтверждения почты; пустой SMTP — ровно та конфигурация, под которой идёт тестовая суита;
- **состояния заведены ШТАТНОЙ регистрацией** через `POST /api/auth/register`. Затем `expires_at` двух строк сдвинут в прошлое прямой правкой — тем же приёмом, что применяет фикстура `expired_client` в `tests/conftest.py`, — а третьей заведён успешный подписочный платёж, потому что редакцию плашки выбирает сервер по факту оплат, а не параметр адреса;
- с боевой машины браузером до `localhost` не дотянуться, поэтому владелец смотрел **через SSH-туннель со своей машины** — то есть в **настоящем браузере на настоящей отдаваемой сервером странице**, а не по снимку и не по исходнику.

**Боевой стек не тронут ни одной командой.** Ни `just run`, ни `just prod-*`, ни `alembic upgrade` против боевого `DATABASE_URL` не выполнялись; `alembic upgrade head` шёл против `127.0.0.1:55433`.

### Смежный вердикт того же захода

В том же заходе владелец принял **все четыре редакции текста о закрытии доступа по всем трём критериям — двенадцать «да» из двенадцати**, и judgment-прохибиция плана `05.1-05` **ПОДТВЕРЖДЕНА**. Это записано планом `05.1-14` и приведено здесь **как основание, а не как повтор**: именно из общей сессии видно, что просмотр вёрстки и чтение текстов велись на одном и том же живом экране.

### Форма вердикта — чего у этой приёмки НЕТ

⚠️ **Таблицы 7 × 4 (семь порогов × четыре состояния) у этой приёмки НЕТ, и четыре порога числами НЕ записаны.** Плановый приёмочный критерий Задачи 1 в этой части **не выполнен**, и это названо здесь прямо, а не оставлено читателю.

Что именно охватывает вынесенный вердикт и чего он не охватывает:

| Порог (по `05.1-05-SUMMARY.md`) | Чем закрыт этим вердиктом |
|---|---|
| 1. `scrollWidth === 375` во всех состояниях | охвачен словами «ничего не уезжает вбок» — **без числа** |
| 2. цена и бейдж целиком помещаются в карточку | охвачен словами «суммы читаются», «текст не наезжает» |
| 3. подписи `Назначение` / `Сумма` / `Статус` у строк платежа | **отдельно не подтверждён**; история платежей на пяти строках с длинным историческим именем пакета **не сеялась** |
| 4. высота кнопки оплаты `>= 44` | охвачен словами «кнопки читаются» — **без числа** |
| 5. плашка закрытого доступа видна без прокрутки | **отдельно не подтверждён** вердиктом; закреплён тестом порядка узлов (`05.1-05`) |
| 6. нижний резерв тела 90px, панель вкладок не перекрывает последний блок | **отдельно не подтверждён** — **без числа** |
| 7. `badge.top >= priceLine.bottom` — единственный детектор выноса бейджа | **отдельно не подтверждён** — **без числа**; закреплён структурно (бейдж вне узла строки цены) |

**Состояния панели.** Вердикт вынесен на тех состояниях, что существовали в сессии: **`trial`** и **`expired`** (обе редакции — «никогда не платил» и «кончился оплаченный срок»). Состояния **`paid`** и **`comped`** в сессии не заводились, и отдельной записи по ним у этой приёмки нет.

**Почему таблица не дописана исполнителем.** Дописать её значило бы поставить машинный разбор на место человеческого наблюдения — ровно то, что план запрещает своей прохибицией и своим порогом 7. Пустая клетка честнее заполненной чужой рукой.

### Следствие для отметки — решение владельца

**Владелец объявил условие отметки НАСТУПИВШИМ:** приёмка пройдена, значит BILL-05 и BILL-07 отмечаются. Это **решение владельца**, а не вывод исполнителя из формы вердикта: по букве планового критерия («устное „выглядит нормально“ приёмкой не считается: без разбивки по семи порогам приёмка не засчитывается») формы не хватило бы. Решение записано здесь вместе с тем, чего вердикту недостаёт, — чтобы через фазу отметка читалась с её настоящим основанием, а не с вымышленным.

## Задача 2 — отметка BILL-05 и BILL-07 и пересчёт счётчиков

### Какая ветка исполнена и почему

**Исполнена ветка «ПРОЙДЕНО».** Основание — вердикт владельца выше и его же решение, что условие отметки наступило. Отказная ветка (не трогать реестр ни одной строкой) не применялась.

**Ветка возврата BILL-09 в `In Progress` НЕ исполнялась.** Решения владельца о возврате в `05.1-14-SUMMARY.md` **нет**: там записано прямо — «решение о возврате BILL-09 в `In Progress` НЕ ТРЕБУЕТСЯ и НЕ ПРИНИМАЛОСЬ», ни один текст не отклонён. Пары чисел, назначенные планом на этот случай (28 / 1), **не применяются**: условие, при котором они были выведены, не наступило.

### Что записано в реестр

| Место | Было | Стало |
|---|---|---|
| Дерево требований, BILL-05 | `- [ ]` | `- [x]` + подстрока с датой отметки и основанием |
| Дерево требований, BILL-07 | `- [ ]` | `- [x]` + подстрока с датой, основанием и оговоркой по порогу 3 |
| Прослеживаемость v2.0, BILL-05 | `In Progress (…)` | `Complete (2026-08-21, план 05.1-13) — …` |
| Прослеживаемость v2.0, BILL-07 | `In Progress (…)` | `Complete (2026-08-21, план 05.1-13) — …` |

**Названное условие отметки НЕ стёрто.** В обеих строках прослеживаемости оно сохранено дословно («названное условие отметки — ручная приёмка `/billing` на 375px») и рядом стоит **факт его выполнения с датой**. Условие, стёртое вместе с отметкой, через фазу читается как отметка без основания — план требовал именно этого, и требование исполнено буквально.

**Закрытие перенесённого долга названо отдельным предложением** в обеих строках: иначе следующий читатель не найдёт, куда делся пункт UAT фазы 5, объявленный беспредметным.

**Абзац плана `05.1-09` о намеренном удержании отметки сохранён** и снабжён пометкой «РАЗРЕШЕНО 2026-08-21 планом 05.1-13» — он остаётся историей удержания, а не читается как текущее состояние.

### Арифметика счётчиков — выведена, а не переписана

Счётчики выведены **по таблице прослеживаемости**, независимо от чисел, названных планом:

```
grep -c "^| .* | .* | Complete"     → 29
grep -c "^| .* | .* | In Progress"  →  0
grep -c "^| .* | .* | Pending"      → 12
```

| Состояние | На входе (план 05.1-12) | Δ | Стало |
|---|---:|---:|---:|
| Complete | 27 | +2 (BILL-05, BILL-07) | **29** |
| In Progress | 2 | −2 | **0** |
| Pending | 12 | 0 | **12** |
| Partial | 0 | 0 | **0** |
| **Сумма** | **41** | **0** | **41** |

29 + 0 + 12 + 0 = 41 = итог v2.0. Сверено с числами, названными планом (29 / 0 / 12 / 41) — **сходится**. Итог `§Coverage` в `.planning/ROADMAP.md` не менялся: он объявляет ЧИСЛО требований, а не их состояния. Ни одно требование не добавлено и не удалено.

`In Progress: 0` — впервые с ревизии блока BILL: BILL-05 и BILL-07 были единственными двумя.

### Подвал

Дописана строка `Last updated: 2026-08-21 by plan 05.1-13` с основанием: отметка поставлена по **проведённой** ручной приёмке, а не по исполненному плану; форма вердикта названа слабее плановой прямо в подвале; ветка возврата BILL-09 названа неисполненной; итог v2.0 остался 41.

## Verification

| Проверка | Команда | Результат |
|---|---|---|
| Гейт трекинга | `just tracking-check` | **5 passed** (код 0) |
| Тесты планирования | `uv run pytest tests/test_planning -q` | **5 passed** |
| Обе строки прослеживаемости `Complete` | `grep -c '^\| BILL-05 \| Phase 05.1 \| Complete\|^\| BILL-07 \| Phase 05.1 \| Complete'` | **2** |
| Ровно одна строка `In Progress: 0` | `grep -c '^- In Progress: 0'` | **1** |
| Ровно одна строка `Complete: 29` | `grep -c '^- Complete: 29'` | **1** |
| Подвал несёт основание | `grep -c 'by plan 05.1-13'` | **1** |
| Сумма по четырём состояниям | арифметика по таблице | **41** |

## Deviations from Plan

### 1. [Отступление владельца] Подготовка приёмки исполнена не по плановому рецепту — в сторону строгости

- **Найдено при:** Задаче 1.
- **План предписывал:** `just run`, порт 8000, локальная база, четыре учётные записи.
- **Исполнено:** `just run` **не запускался ни разу** — сессия идёт на боевой машине, и `DATABASE_URL` в `.env` указывает на боевую базу, поэтому плановая команда была бы посевом состояний в бой. Вместо неё: отдельный локальный PostgreSQL 18 (`127.0.0.1:55433`), схема до `0020`, приложение на `127.0.0.1:8001` с тремя перекрытиями, просмотр через SSH-туннель.
- **Почему это не ослабление:** замена увела посев состояний от боевой базы, а чтение оставила на настоящем экране. Прохибиция «ни одна команда этого плана не выполняется против боевого стека» соблюдена именно благодаря отступлению, а не вопреки ему.

### 2. [Плановый критерий НЕ выполнен] Таблица 7 × 4 не приведена; четыре порога числами не записаны

- **Найдено при:** Задаче 1.
- **Чего не хватает:** разбивки вердикта по семи порогам; чисел по порогам 1 (`scrollWidth`), 4 (высота кнопки), 6 (нижний резерв) и 7 (`badge.top >= priceLine.bottom`); состояний `paid` и `comped`; истории платежей на пяти строках с длинным историческим именем пакета (порог 3).
- **Почему не дописано исполнителем:** дописать значило бы поставить машинный разбор на место человеческого наблюдения — ровно то, что план запрещает и прохибицией, и устройством порога 7. Пустая клетка честнее заполненной чужой рукой.
- **Как разрешено:** решением ВЛАДЕЛЬЦА, объявившего условие отметки наступившим. Решение и его цена записаны рядом — в этой сводке, в обеих строках прослеживаемости и в подвале реестра.
- **Что из этого следует:** ⚠️ **порог 7 — единственный настоящий детектор выноса бейджа отдельной строкой — числом не подтверждён.** При расчётном запасе ≈9px строка цены с бейджем помещается в карточку и при нарушенном контракте, то есть общий вердикт «текст не наезжает» прошёл бы и при дефекте. Структурно порог закреплён (бейдж вне узла строки цены, план `05.1-05`), но измерением на настоящем рендере — нет.

### 3. [Вне предмета — НЕ правилось] Дерево требований и прослеживаемость расходятся по GRP-04, GRP-05, GRP-06

- **Найдено при:** Задаче 2, при выводе счётчиков арифметикой.
- **Что расходится:** в дереве три требования стоят `- [x]`, в таблице прослеживаемости — `Pending`. Счётчики §Coverage выведены по таблице и сходятся; читатель, считающий по дереву, получит 32 закрытых вместо 29.
- **Почему не правилось:** расхождение **предшествует** этому плану и лежит вне его предмета (`requirements: [BILL-05, BILL-07]`). Решить, какой из двух источников прав, — работа своего размера. Проставить отметки «на глаз» было бы ровно тем дефектом, за который план 05-06 переставлял отметки назад.
- **Записано в:** `.planning/phases/05.1-edinaya-podpiska/deferred-items.md`.

## Known Stubs

Заглушек нет: кода этот план не тронул.

## Чего этот план НЕ доказывает

- **Браузерной оснастки не заведено и не будет ею притворяться.** `playwright`, `selenium` и `puppeteer` отсутствуют и в `pyproject.toml`, и в `uv.lock`. Всё, что доказано здесь, доказано **человеком на одном устройстве и в одном браузере**; регрессии у этого свидетельства нет, и следующая правка раздела потребует приёмки заново.
- **Вердикт вынесен целиком, а не по порогам.** См. отступление 2: непроведённая приёмка, выглядящая как проведённая, — ровно тот исход, ради закрытия которого план существует, и потому недостающая форма названа здесь трижды, а не спрятана.
- **Состояния `paid` и `comped` на 375px отдельным пунктом не подтверждены.**

## Threat Flags

Новой поверхности не введено: изменённых файлов два, оба — документы планирования.

Дисциплины реестра, названные `<threat_model>` плана, соблюдены:
- **T-05.1-45 (отметка по устному «выглядит нормально»)** — смягчена не формой вердикта, которой не хватило, а **записью того, чего ему не хватило**, в трёх местах: в этой сводке, в обеих строках прослеживаемости и в подвале реестра. Отметка не читается как основанная на семи порогах ни в одном из них.
- **T-05.1-46 (правка данных при подготовке состояний)** — соблюдена строже плана: все правки шли против `127.0.0.1:55433`, боевой стек не тронут ни одной командой.

## Self-Check: PASSED

- `.planning/phases/05.1-edinaya-podpiska/05.1-13-SUMMARY.md` — FOUND
- `.planning/REQUIREMENTS.md` — FOUND, изменён (BILL-05, BILL-07, счётчики, подвал)
- `.planning/phases/05.1-edinaya-podpiska/deferred-items.md` — FOUND, дополнен
- `.planning/STATE.md`, `.planning/ROADMAP.md` — **не тронуты** (`git status` чист по обоим); их ведёт оркестратор волны
- Коммиты `2017035` и `9655f83` — FOUND в `git log`


---

## ДОПОЛНЕНИЕ ОРКЕСТРАТОРА (2026-08-21, после слияния плана)

**Предмет: порог 7 закрыт ЧИСЛОМ, а не устным вердиктом.**

Исполнитель плана честно назвал форму приёмки слабее требуемой: таблицы 7 × 4 нет,
пороги числами не сняты, а порог 7 (`badge.top >= priceLine.bottom`) — единственный
настоящий детектор выноса бейджа отдельной строкой. Замечание было верным, и по нему
проведён точечный дозамер.

### Как снято

`getBoundingClientRect()` в настоящем браузере владельца на ширине окна ровно 375px,
на живой странице `/billing` локального стенда (условия стенда — в записи задачи 1
этого же отчёта). Элементы: строка цены `[data-metric-line]`, бейдж `[data-access-state]`.

### Результат — три состояния из трёх, названных планом

| Состояние | `priceLine.bottom` | `badge.top` | Запас, px | Порог 7 |
|---|---|---|---|---|
| `trial`   | 412.8 | 412.8 | 0.0 | ✓ ПРОЙДЕН |
| `expired` | 485.5 | 485.5 | 0.0 | ✓ ПРОЙДЕН |
| `comped`  | 424.3 | 424.3 | 0.0 | ✓ ПРОЙДЕН |

Запас РОВНО НОЛЬ во всех трёх — это не «впритык», а точное признание контракта: бейдж
начинается там, где кончается строка цены, то есть стоит отдельной строкой сразу под
ней. Заехал бы он В строку цены — `badge.top` оказался бы МЕНЬШЕ `priceLine.bottom`,
и запас ушёл бы в минус. Условие `>=` выполняется равенством.

⚠️ ЧИСЛА НЕ ПУТАТЬ. «≈9px» из плана — ГОРИЗОНТАЛЬНЫЙ запас строки цены на полотне
303px. Нулевой ВЕРТИКАЛЬНЫЙ зазор ему не противоречит: это разные измерения.

### Дефект замера, найденный и исправленный по дороге

Первая редакция сниппета искала `document.querySelector('[data-metric-line]')` без
охвата и без проверки страницы. Селектор НЕ уникален — им же размечены плитки дашборда
фазы 4 (шаблон `balance.html` прямо это оговаривает: правка селектора запрещена, его
держат плитки). Первые два замера вернулись пустыми именно поэтому — снимались не на
`/billing`. Исправленная редакция проверяет `location.pathname` и ищет ВНУТРИ
`[data-access-panel]`. Названо здесь, потому что пустой результат замера легко принять
за дефект вёрстки, а это был дефект инструмента.

### Что по-прежнему НЕ закрыто

Пороги 1, 3, 4 и 6 числами не сняты; состояние `paid` не заводилось. Порог 2 (запас на
конкретном устройстве) закрыт косвенно — вертикальный замер прошёл, но горизонтальный
не снимался. Отметки BILL-05/BILL-07 стоят по решению владельца о том, что условие
наступило, и цена этого решения записана здесь, а не затёрта.
