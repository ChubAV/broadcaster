---
phase: 05.1-edinaya-podpiska
plan: 05
type: execute
wave: 4
depends_on: ["05.1-04"]
files_modified:
  - app/pages/billing.py
  - app/pages/common.py
  - app/templates/billing/balance.html
  - app/templates/billing/includes/payment_row.html
  - app/templates/billing/includes/plan_card.html
  - app/templates/billing/includes/usage_meters.html
  - app/static/css/app.css
  - app/constants.py
  - tests/test_pages/test_billing_section.py
  - tests/test_pages/test_responsive_markup.py
autonomous: true
requirements: [BILL-05, BILL-07, BILL-08, BILL-10]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "У пользователя без платежей блок истории показывает пустое состояние со словами, а не пустую таблицу и не отсутствующий блок (критерий 4)."
    - "format_amount печатает сумму с неразрывным пробелом как разделителем разрядов и перед знаком рубля; идентификатор платежа ЮKassa на экран не выводится ни в одной ветке строки платежа (критерий 4)."
    - "После удаления блока карточек тарифов правило нажимаемой высоты кнопки оплаты остаётся в app.css, и кнопка сохраняет высоту 44px (M3/M4 мобильного контракта)."
    - statement: "История платежей при равных значениях даты создания выводится в устойчивом порядке — объявлен вторичный ключ сортировки, а не порядок, зависящий от плана запроса."
      verification: backstop
    - statement: "Длинное значение имени пакета в исторической строке платежа (журнал сохраняется по D-H) не ломает вёрстку истории на 375px: ячейка назначения переносится внутри строки, а не даёт строке горизонтальную прокрутку."
      verification: backstop
    - "Экран подписки имеет ровно четыре состояния — trial, paid, expired, comped, — и каждое отличается от остальных СЛОВАМИ, а не только цветом; состояние вычисляет обработчик, а не разметка (UI-SPEC E1 populated)."
    - "Пустого состояния у панели доступа нет: отсутствие строки подписки рисуется как закрытый доступ, а не как пустая карточка; все чтения идут с умолчанием (UI-SPEC E1 empty)."
    - "Состояние бесплатного доступа при отсутствующей дате печатает «доступ открыт бессрочно» и не печатает дату; панель стоит целиком (UI-SPEC E1 partial)."
    - "Отказ создания платежа рисуется плашкой из закрытого множества причин; текст стороннего исключения на экран не выходит (UI-SPEC E1 error)."
    - "Бейдж состояния вынесен ОТДЕЛЬНОЙ строкой из строки цены: строка цены не умеет переноситься, и запаса в девять пикселей у неё нет (UI-SPEC E1 overflow, M1)."
    - "Все строки панели — константы контракта плюс форматирование суммы и даты; произвольной длины у панели не бывает (UI-SPEC E1 long-text)."
    - "Плашка о закрытом доступе рисуется по состоянию с сервера, а не по параметру адреса; параметр не читается обработчиком ни одной строкой (UI-SPEC E2 populated, A-5)."
    - "Приоритет плашек объявлен: сообщение об исходе только что совершённого действия вытесняет фоновую плашку о закрытом доступе; две плашки подряд не рисуются (UI-SPEC E2 error)."
    - "Плашка объявлена без запрета переноса и без фиксированной ширины: строка примерно в полтораста знаков переносится потоком (UI-SPEC E2 long-text)."
    - "Пустая история платежей печатает пару слов «Платежей пока нет» и «Здесь появятся оплаты доступа» (UI-SPEC E3 empty)."
    - "Заполненная история рисуется шапкой колонок и строкой на запись; макрос строки правится ровно в ветке назначения платежа (UI-SPEC E3 populated)."
    - "Назначение платежа имеет три источника: пустое поле тарифа даёт «Доступ к системе», заполненное — историческое имя тарифа, пакетный платёж — имя пакета либо родовую подпись; отсутствие даты даёт пустую ячейку, а строка рисуется целиком (UI-SPEC E3 partial)."
    - "Неизвестный статус платежа печатается моношрифтом как есть, а не прячется: спрятанное состояние платежа читается как «платежа не было» (UI-SPEC E3 error)."
    - "Сработавший потолок списка называет себя числом показанных и числом всего; набор ширин колонок инертен ниже 861px (UI-SPEC E3 overflow, M4)."
    - "Ноль платежей даёт пустое состояние, один и много — один и тот же примитив строки (UI-SPEC E3 zero-one-many)."
  artifacts:
    - app/templates/billing/balance.html               # переписан, четыре состояния
    - app/templates/billing/includes/payment_row.html  # ветка назначения платежа
    - app/pages/billing.py                             # access.state, ever_paid
  key_links:
    - "billing_page → access.state ∈ {comped, expired, trial, paid} → balance.html (готовый ответ, не пересчёт в Jinja)"
    - "settings.subscription_price → format_amount → строка цены (литерала суммы в разметке нет)"
    - "форма оплаты ← атрибут data-plan-cta ← правило нажимаемой высоты в app.css"
    - "NAV_ITEMS ключ billing → подпись раздела → сайдбар, нижние табы и заголовок страницы"
  prohibitions:
    - "Текст о закрытии доступа не имеет права читаться как потеря данных или как упрёк: закрытие формулируется как факт с продолжением — объявления, расписания и группы сохранены."
---

<objective>
Переписать раздел `/billing` в то, что остаётся после снятия матрицы тарифов: одно
число, дата окончания доступа, кнопка оплаты, история платежей. Плюс плашка причины.

Purpose: критерий 4 требует, чтобы человек видел дату и историю «и больше ничего не
считал». Сегодня экран несёт пять блоков, четыре из которых — счёт. Открытый пункт UAT
фазы 5 про вёрстку на 375px снят как беспредметный именно потому, что проверяемая
разметка заменяется здесь: долг не закрыт проверкой, а ПЕРЕНЕСЁН в этот план.

Output: обработчик, вычисляющий одно из четырёх состояний; разметка на ~90 строк вместо
267; правленый макрос строки платежа; удалённые карточка тарифа и метр оси; вырезанный
блок стилей карточек тарифов с сохранённым правилом нажимаемой высоты.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05.1-edinaya-podpiska/05.1-UI-SPEC.md
@.planning/phases/05.1-edinaya-podpiska/05.1-PATTERNS.md
@.planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md
@.planning/phases/05.1-edinaya-podpiska/05.1-04-SUMMARY.md
</context>

## Artifacts this phase produces (план 05.1-05)

| Символ | Место | Вид |
|--------|-------|-----|
| ключ контекста `access.state` со значениями `trial` / `paid` / `expired` / `comped` | `app/pages/billing.py` | ключ шаблона |
| признак `ever_paid` | `app/pages/billing.py` | ключ шаблона |
| атрибут `[data-access-panel]` | `app/templates/billing/balance.html` | атрибут разметки |
| подпись раздела `Подписка` | `app/pages/common.py` (`NAV_ITEMS`) | строка копирайта |

Удаляются: `app/templates/billing/includes/plan_card.html`,
`app/templates/billing/includes/usage_meters.html`, набор ширин и подписей колонок
истории операций в `balance.html`, блок правил карточек тарифов в `app.css`.

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Обработчик вычисляет одно из четырёх состояний</name>

  <files>
    app/pages/billing.py,
    app/pages/common.py,
    app/constants.py,
    tests/test_pages/test_billing_section.py
  </files>

  <read_first>
    app/pages/billing.py:60-130 (закрытое множество причин отказа оплаты и подпись карточки перехода),
    app/pages/billing.py:128-275 (обработчик страницы целиком: докстринг, чтения, сборка контекста),
    app/pages/common.py:123-131 (`NAV_ITEMS`, ключ раздела оплаты),
    app/services/billing_service.py:200-250 (`get_payment_history` и `count_payments` — что именно они возвращают и в каком порядке),
    app/services/payment_service.py:30-40 (константы вида платежа и статусов),
    app/constants.py:90-110 (потолок списка платежей и правило «обрезка называет себя»),
    .planning/phases/05.1-edinaya-podpiska/05.1-UI-SPEC.md § Page State Contract целиком, § Copywriting Contract → «Раздел и навигация», «Панель E1», «Уведомления E2»,
    .planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md § Вопрос 6
  </read_first>

  <behavior>
    - Тест 1: пользователь с живым сроком и без единого успешного подписочного платежа получает состояние `trial`.
    - Тест 2: пользователь с живым сроком и хотя бы одним успешным подписочным платежом получает состояние `paid`.
    - Тест 3: пользователь с истёкшим сроком получает состояние `expired`.
    - Тест 4: пользователь без строки подписки получает состояние `expired`, а не падение.
    - Тест 5: тело ответа при состоянии `expired` содержит объяснение и сумму, отформатированную глобалом; текст различается по признаку «платил когда-нибудь».
    - Тест 6: тело ответа при состоянии `expired` содержит те же слова независимо от того, был ли в адресе параметр редиректа — обработчик параметр не читает.
    - Тест 7: если одновременно есть сообщение об исходе действия и состояние `expired`, рисуется только сообщение об исходе.
    - Тест 8: подпись раздела в навигации, заголовок страницы и заголовок документа несут новое имя раздела.
  </behavior>

  <action>
    В `app/pages/billing.py` вычислять `access.state` в обработчике по объявленному
    порядку проверок: бесплатный доступ выигрывает у всех (ветку вводит план `05.1-08`
    вместе с колонкой), затем закрытый срок, затем «срок жив и успешных подписочных
    платежей нет» — пробный период, иначе — оплачено. Состояние считается ЗДЕСЬ, а не в
    Jinja: вторая копия правила в разметке разъехалась бы с гардом доступа молча.

    Признак «платил когда-нибудь» считать одним `count()` по журналу платежей с видом
    «подписка» и статусом «успешно». Считать его ТОЛЬКО на этой странице и НЕ уводить в
    шелл: шелл исполняется на 26 маршрутах, и запрос ради подписи виджета оплачивался бы
    на каждом рендере продукта. Следствие принято сознательно — виджет сайдбара пробный
    период от оплаченного не отличает.

    Вырезать из контекста шаблона всё, что теряет предмет: перечень тарифов, текущий
    тариф, оси потребления, сведения о балансе, журнал операций, перечень пакетов,
    идентификатор бесплатного тарифа, множество отвергнутых тарифов, подпись карточки
    перехода. Оставить: платежи, их общее число, потолок, признак срабатывания потолка,
    признак включённости платежей, готовую строку причины отказа, дату окончания доступа,
    состояние, признак «платил когда-нибудь», активный раздел. Сопутствующие импорты
    (`plan_axes`, чтения баланса и журнала операций) удалить.

    Убрать из закрытого множества причин отказа записи, потерявшие предмет: про
    несуществующий тариф, про несуществующий пакет и про переход на младший тариф; вместе
    с ними — подпись карточки перехода. Записи про неудачу начала оплаты, про отключённые
    платежи и про незавершённое предыдущее намерение оставить ДОСЛОВНО. Последняя верна
    при переоснованном потолке намерений (план `05.1-01`, D-I) без единой правки.

    Добавить две новые строки объяснения закрытого доступа — одна для человека, который
    никогда не платил, вторая для того, кто платил. Обе обязаны называть факт и
    продолжение: доступ закрыт, объявления, расписания и группы сохранены, работа
    продолжится с того же места. Формулировка «доступ закрыт» без продолжения читается как
    потеря данных, а это неправда — планировщик лишь сдвигает срок следующего запуска.
    Ни одна из строк не содержит числа: сумма приезжает форматированием настройки, длина
    пробного периода и порог предупреждения — подстановкой констант.

    Плашка о закрытом доступе рисуется по СОСТОЯНИЮ, а не по параметру адреса. Параметр,
    которым гейт помечает редирект, обработчик не читает ни одной строкой: тогда из
    адресной строки в разметку не уходит вообще ничего — это недостижимость, а не
    экранирование. Приоритет объявить: сообщение об исходе только что совершённого
    действия вытесняет фоновую плашку.

    В `app/pages/common.py` переименовать подпись раздела оплаты в `NAV_ITEMS` — отсюда её
    читают и сайдбар, и нижние табы, и заголовок страницы. Подзаголовок раздела заменить
    на формулировку про доступ и историю платежей.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_section.py -q -x</automated>
    <automated>uv run pytest tests/test_pages/test_billing_section.py -q -k "expired or state or trial or paid"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "query_params.get(\"expired\")\|request.query_params.get('expired')" app/pages/billing.py` не находит вхождений.
    - `grep -c 'plan_axes\|get_balance_info\|get_transaction_history\|parsed_message_packages' app/pages/billing.py` равно 0.
    - `grep -n '"state"' app/pages/billing.py` находит вычисление состояния ровно в одном месте.
    - `grep -rn '3000' app/templates/billing/ app/pages/billing.py` не находит литерала суммы вне ссылки на настройку.
    - `grep -n 'Тарифы' app/pages/common.py` не находит подписи раздела оплаты.
    - `uv run pytest tests/test_pages/test_billing_section.py -q -k "expired or state or trial or paid"` завершается кодом 0 и собирает не менее шести тестов.
  </acceptance_criteria>

  <done>
    Обработчик отдаёт разметке готовый ответ о состоянии, признак «платил когда-нибудь» и
    строку причины; параметра адреса не читает; закрытое множество причин отказа приведено
    к новой модели; раздел переименован в одном месте.
  </done>

  <reversibility rating="costly">
    Переименование раздела трогает подпись, читаемую тремя поверхностями шелла, и оседает
    в скриншотах, закладках и привычке пользователя. Возврат технически стоит одной
    строки, но восприятие продукта откатывается не правкой файла.
  </reversibility>
</task>

<task type="auto">
  <name>Задача 2: Разметка — панель на четыре состояния, история платежей, удаление матрицы</name>

  <files>
    app/templates/billing/balance.html,
    app/templates/billing/includes/payment_row.html,
    app/templates/billing/includes/plan_card.html,
    app/templates/billing/includes/usage_meters.html,
    app/static/css/app.css
  </files>

  <read_first>
    app/templates/billing/balance.html (весь файл, 267 строк — прежде всего блоки на строках 2-11, 38, 44-56, 69, 88-89, 94-126, 128-137, 139-146, 147-167, 168-202, 204-231, 232-266),
    app/templates/billing/includes/payment_row.html (весь файл, 66 строк — особенно ветка назначения платежа и параметры макроса),
    app/static/css/app.css:1125-1195 (плитки дашборда, блок карточек тарифов и правило нажимаемой высоты кнопки оплаты — прочитать ЦЕЛИКОМ до правки),
    app/templates/components/ (макросы alert, card, badge, mono, empty_state, rowhead/row/cell — что уже доступно),
    .planning/phases/05.1-edinaya-podpiska/05.1-UI-SPEC.md § Page State Contract, § Component Contracts C1 и C2, § Mobile Contract, § Deletion Contract целиком,
    .planning/phases/05.1-edinaya-podpiska/05.1-PATTERNS.md § Deletion Sites → `app/static/css/app.css:1153-1192` (правило разреза)
  </read_first>

  <action>
    Переписать `app/templates/billing/balance.html` в две карточки в вертикальной стопке.

    Первая карточка — панель доступа с атрибутом `[data-access-panel]`. Анатомия по
    контракту C1: заголовок и подзаголовок карточки, затем строка цены, затем бейдж
    состояния ОТДЕЛЬНОЙ строкой, затем строка даты моношрифтом, затем форма оплаты либо
    подпись вместо неё либо ничего. Цена печатается во ВСЕХ состояниях, включая
    бесплатный доступ: цена — свойство продукта, а не счёт этому человеку.
    Крупный элемент на экране ровно один, и это цена. Дата крупного кегля НЕ получает —
    иначе на экран вернулось бы второе крупное число, то есть арифметика, ради снятия
    которой фаза существует.

    ⚠️ Бейдж НЕ ставить внутрь строки цены. Строка цены объявлена без переноса, бейдж —
    без переноса и без сжатия; при полотне в 303 пикселя запас составляет около девяти, и
    у строки, которая не умеет переноситься, девять пикселей запасом не являются: при любом
    выходе за полотно она не сложится, а вылезет за карточку. Править сам селектор строки
    цены ЗАПРЕЩЕНО — его держат плитки дашборда фазы 4. Допустимая альтернатива выносу —
    строго ограниченное панелью правило переноса по образцу правила нажимаемой высоты.

    Форма оплаты — обычный POST на прежний адрес, БЕЗ единого скрытого поля: цену читает
    сервер. Атрибут, на котором висит правило нажимаемой высоты кнопки, ОБЯЗАТЕЛЕН и
    обязан переехать на эту единственную оставшуюся форму: потерять его — значит молча
    вернуть кнопку к 33 пикселям и заново открыть снятый пункт UAT фазы 5. При выключенных
    платежах гаснет КНОПКА, а не витрина: вместо формы печатается подпись с причиной, а
    цена и дата остаются — экран без цены и без даты читается как поломка.
    В состоянии бесплатного доступа кнопки нет вовсе, её место занимает подпись с
    причиной: кнопка предлагала бы купить то, что у человека уже есть бесплатно.

    Шкал прогресса на экране не остаётся ни одной — ни «сколько дней осталось», ни
    «сколько прошло пробного периода». Компонент шкалы в разметку раздела не
    импортируется.

    Вторая карточка — история платежей: шапка колонок и строка на запись, пустое состояние
    парой слов при нуле записей, строка о сработавшем потолке с числом показанных и числом
    всего. Набор ширин колонок платежей и подписи колонок не меняются. Блоки потребления,
    карточек тарифов, баланса сообщений, пакетов и истории операций удалить целиком вместе
    с их наборами ширин и подписей и вместе с импортами шкалы, карточки тарифа и метра оси.

    В `app/templates/billing/includes/payment_row.html` править РОВНО одну ветку —
    назначение платежа: пустое поле тарифа даёт подпись про доступ к системе, заполненное —
    историческое имя тарифа с заглавной буквы, платёж не подписочного вида — имя пакета
    либо родовую подпись. ⚠️ Историческая покупка обязана остаться собой: переименовать
    чужой прошлый платёж — значит соврать в денежном журнале. Параметр различения первого
    платежа и продления удалить из сигнатуры макроса и из вызывающего шаблона: ключом
    карты служило снимаемое поле, и после фазы карта начала бы врать. Состав печатаемых
    полей НЕ расширять ни на одно: идентификатор платежа ЮKassa не печатается нигде — ни
    значением, ни атрибутом, ни комментарием.

    Удалить файлы `app/templates/billing/includes/plan_card.html` и
    `app/templates/billing/includes/usage_meters.html`.

    В `app/static/css/app.css` удалить НЕПРЕРЫВНЫЙ блок правил карточек тарифов — от
    правила сетки карточек до правила вложенного элемента строки лимитов включительно —
    вместе с комментарием-заголовком этого блока.
    ⚠️ Правило нажимаемой высоты кнопки оплаты лежит ВПЛОТНУЮ к удаляемому блоку и носит
    то же префиксное имя. Оно ВЫЖИВАЕТ вместе со своим объясняющим комментарием: это
    единственное правило нажимаемой высоты и закрытая находка фазы 5.
    Правила плиток дашборда и их вложенных элементов НЕ удалять и НЕ править — их держат
    плитки дашборда фазы 4 и карточка пользователя в админке.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_section.py tests/test_pages/test_responsive_markup.py -q -x</automated>
  </verify>

  <acceptance_criteria>
    - `wc -l app/templates/billing/balance.html` показывает не более 120 строк.
    - `ls app/templates/billing/includes/` не содержит файлов карточки тарифа и метра оси.
    - `grep -c 'data-plans\|data-plan-name\|data-plan-limits' app/static/css/app.css` равно 0.
    - `grep -n 'data-plan-cta' app/static/css/app.css` находит правило с минимальной высотой 44px.
    - `grep -c 'data-plan-cta' app/templates/billing/balance.html` равно 1.
    - `grep -c 'data-metrics\|data-metric-line\|data-metric-value' app/static/css/app.css` не уменьшилось относительно состояния до правки.
    - `grep -c 'input type="hidden"' app/templates/billing/balance.html` равно 0.
    - `grep -rc 'progress' app/templates/billing/` равно 0.
    - `grep -c '<table' app/templates/billing/balance.html app/templates/billing/includes/payment_row.html` равно 0 для обоих файлов.
    - `grep -c 'first_payment_at' app/templates/billing/` равно 0.
    - `uv run pytest tests/test_pages/test_billing_section.py tests/test_pages/test_responsive_markup.py -q -x` завершается кодом 0.
  </acceptance_criteria>

  <done>
    На экране остаются ровно четыре вещи и плашка причины: цена, дата, кнопка, история.
    Правило нажимаемой высоты и правила плиток дашборда пережили разрез; идентификатор
    платежа ЮKassa не печатается; исторические платежи подписаны собой.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Регрессии раздела перенацелены, вёрстка на 375px принята числами</name>

  <files>
    tests/test_pages/test_billing_section.py,
    tests/test_pages/test_responsive_markup.py,
    app/services/billing_service.py
  </files>

  <read_first>
    tests/test_pages/test_billing_section.py (весь файл, 988 строк — прежде всего тесты осей на :222, :638-639, :791-792, :807, тесты прейскуранта на :891-900, тесты пакетов на :933-938 и регрессия печати идентификатора платежа),
    tests/test_pages/test_responsive_markup.py — тесты нажимаемой высоты кнопок оплаты и наличия атрибута на формах оплаты,
    app/services/billing_service.py:200-250 (`get_payment_history` и `count_payments` — порядок сортировки),
    .planning/phases/05.1-edinaya-podpiska/05.1-UI-SPEC.md § Mobile Contract целиком, включая блок ручной приёмки с семью числовыми пунктами,
    .planning/phases/05.1-edinaya-podpiska/05.1-VALIDATION.md § Manual-Only Verifications
  </read_first>

  <behavior>
    - Тест 1: две регрессии нажимаемой высоты — правило в стилях и атрибут на форме — зелены и указывают на ЕДИНСТВЕННУЮ оставшуюся форму.
    - Тест 2: тело `/billing` не содержит осей, карточек тарифов, пакетов и баланса сообщений ни в одном из четырёх состояний.
    - Тест 3: пустая история печатает обе строки пустого состояния.
    - Тест 4: сработавший потолок печатает число показанных и число всего.
    - Тест 5: строка платежа с неизвестным статусом печатает его как есть моношрифтом.
    - Тест 6: сортировка истории платежей устойчива при равных значениях даты создания.
    - Тест 7: сумма печатается с неразрывными пробелами, а не с обычными.
  </behavior>

  <action>
    Удалить из `tests/test_pages/test_billing_section.py` тесты, чей предмет исчез: оси
    тарифа, прейскурант, пакеты сообщений, баланс, история операций. Сохранить и оставить
    зелёными тесты журнала платежей, потолка, пустых состояний и регрессию, запрещающую
    печать идентификатора платежа ЮKassa: она СТАРШЕ этой разметки и обязана её пережить.

    Две регрессии нажимаемой высоты — про правило в стилях и про атрибут на форме — НЕ
    удалять, а ПЕРЕНАЦЕЛИТЬ: форм на разделе остаётся одна вместо трёх, и обе регрессии
    обязаны утверждать её.

    Добавить тесты по семи пунктам `<behavior>`. Тест устойчивости порядка (пункт 6)
    требует, чтобы у выборки истории платежей был объявлен ВТОРИЧНЫЙ ключ сортировки: при
    равных значениях даты создания порядок не имеет права зависеть от плана запроса. Если
    вторичного ключа в выборке нет — добавить его в `app/services/billing_service.py`
    рядом с существующим первичным, а не сортировать в шаблоне.

    Тест переноса длинного имени пакета в ячейке назначения (backstop UI-контракта E3)
    доказывается разметкой, а не браузером: ячейка назначения объявлена растягиваемой и
    правила усечения не имеет, а строка ниже 861 пикселя раскладывается правилами
    мобильного блока. Утверждение снять по исходнику стилей и разметки.

    Ручная приёмка на 375 пикселях остаётся человеческой: браузерного харнесса в проекте
    нет. Записать её в SUMMARY со всеми семью числовыми порогами UI-контракта и особо —
    с пунктом 7, потому что он единственный детектор выноса бейджа отдельной строкой:
    пункт про «помещается в карточку» при запасе в девять пикселей проходит и при
    нарушенном контракте.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_section.py -q -x</automated>
    <automated>uv run pytest tests/test_pages tests/test_services -q</automated>
    <human-check>
      Открыть `/billing` при ширине 375px по одному разу в каждом из состояний
      «пробный период», «оплачено», «доступ закрыт» и с историей платежей длиннее экрана.
      Пройдено, если: (1) ширина документа равна 375 во всех состояниях;
      (2) цена и бейдж состояния целиком помещаются в карточку;
      (3) у каждой строки платежа видны подписи назначения, суммы и статуса;
      (4) высота кнопки оплаты не меньше 44 пикселей;
      (5) в состоянии «доступ закрыт» плашка видна без прокрутки — то есть стоит первой;
      (6) фиксированная панель вкладок не перекрывает последний блок;
      (7) верхняя граница бейджа состояния не выше нижней границы строки цены — бейдж
      стоит на ОТДЕЛЬНОЙ строке. Пункт 7 — единственная настоящая проверка выноса.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_pages/test_billing_section.py -q -x` завершается кодом 0.
    - `grep -c 'plan_axes\|usage_meters\|packages' tests/test_pages/test_billing_section.py` равно 0.
    - `grep -n 'def test_the_screen_never_prints_the_yookassa_payment_id' tests/test_pages/test_billing_section.py` находит тест.
    - `grep -rn 'order_by' app/services/billing_service.py` в выборке истории платежей показывает не менее двух ключей сортировки.
    - Обе регрессии нажимаемой высоты присутствуют в `tests/test_pages/test_responsive_markup.py` и зелены.
    - `uv run pytest tests/test_pages tests/test_services -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Раздел закрыт тестами по своему новому предмету; ни одна из трёх регрессий, старших
    этой разметки, не потеряна; ручная приёмка на 375 пикселях объявлена числами, а не
    «на глаз».
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| адресная строка → разметка раздела | параметры запроса пересекают границу; правило раздела — в разметку уходит ГОТОВАЯ СТРОКА, а не значение параметра |
| строки журнала платежей → разметка | контекст несёт строки платежей целиком, включая ключ подделки уведомления об оплате |
| форма оплаты → сервер | изменяющий запрос; сверка источника обязательна |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-07 | Information Disclosure | `app/templates/billing/includes/payment_row.html` | high | mitigate | Правка касается ровно одной ветки макроса и не имеет права расширять состав печатаемых полей; действующая регрессия, запрещающая печать идентификатора платежа ЮKassa, обязана пережить переписывание и входит в критерии приёмки задачи 3 |
| T-05.1-21 | Injection (reflected) | параметр адреса, помечающий редирект гейта | medium | mitigate | Обработчик параметр НЕ читает ни одной строкой: плашка рисуется по состоянию с сервера. Это недостижимость, а не экранирование — подставлять нечего |
| T-05.1-22 | Tampering | форма оплаты без скрытых полей | high | mitigate | Форма перестаёт нести хоть одно поле; сверка источника и проверка включённости платежей остаются первыми проверками обработчика |
| T-05.1-23 | Information Disclosure | текст стороннего исключения на экране | medium | mitigate | Отказ SDK уходит в журнал ключом; на экран выходит одна из строк закрытого множества |
| T-05.1-10 | Repudiation | подписи исторических платежей | high | mitigate | Журнальные поля тарифа и имени пакета читаются и после фазы (D-H); разметка выбирает подпись по НАЛИЧИЮ поля, а не по эпохе — прошлая покупка остаётся собой |
| T-05.1-SC | Tampering | установка пакетов | — | N/A | План не устанавливает пакетов |
</threat_model>

<verification>
- `uv run pytest tests/test_pages tests/test_services tests/test_application -q` — зелёный.
- `grep -rc 'data-plans' app/static/css/app.css app/templates/` — ноль вхождений.
- `grep -n 'data-plan-cta' app/static/css/app.css app/templates/billing/balance.html` — по одному вхождению в каждом файле.
- Ручная приёмка на 375px по семи числовым пунктам записана в SUMMARY.

## Открытый пункт UAT фазы 5

Пункт «вёрстка раздела тарифов на 375px» снят как беспредметный вместе с заменяемой
разметкой. Долг ПЕРЕНЕСЁН в ручную приёмку этого плана: если новая вёрстка ляжет на
375px так же непроверенно, фаза унаследует ту же дыру под новым именем.
</verification>

<success_criteria>
- Экран показывает одно число, дату окончания доступа, кнопку оплаты и историю платежей — и ничего, что надо считать.
- Четыре состояния различаются словами, а не только цветом; состояние вычислено сервером.
- Ни одна из трёх регрессий, старших этой разметки, не потеряна.
- Правило нажимаемой высоты кнопки и правила плиток дашборда пережили разрез стилей.
</success_criteria>

<output>
Create `.planning/phases/05.1-edinaya-podpiska/05.1-05-SUMMARY.md` when done
</output>
