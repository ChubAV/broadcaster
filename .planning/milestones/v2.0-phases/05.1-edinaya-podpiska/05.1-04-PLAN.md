---
phase: 05.1-edinaya-podpiska
plan: 04
type: execute
wave: 3
depends_on: ["05.1-01", "05.1-03"]
files_modified:
  - app/pages/common.py
  - app/templates/base.html
  - app/static/css/app.css
  - app/pages/billing.py
  - app/templates/billing/balance.html
  - tests/test_pages/test_shell.py
  - tests/test_pages/test_responsive_markup.py
  - tests/test_pages/test_billing_section.py
  - tests/test_application/test_subscription_period.py
autonomous: true
requirements: [BILL-09, BILL-10]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "В последний день доступа виджет печатает «последний день», а не «осталось 0 дней» — правило P-6 UI-контракта, и эта ветка проверяется РАНЬШЕ ветки счёта дней (критерий 2)."
    - "После удаления шкалы из виджета сайдбара margin-top: auto остаётся действующим для нижнего блока на всех 26 страницах (D-J)."
    - "Четыре представления виджета доступа объявлены поимённо: бесплатный доступ, осталось N дней, до даты, доступ закрыт (UI-SPEC E4 populated)."
    - "Отсутствие ключа access в контексте шелла приводит к тому, что виджет НЕ рисуется; умолчание «закрыт» запрещено явно — ложное «доступ закрыт» из пустого словаря хуже отсутствия виджета (UI-SPEC E4 error)."
    - "У виджета доступа нет мобильной поверхности: [data-access] и [data-user] скрыты правилом внутри @media (max-width: 860px); ту же величину на телефоне несёт панель страницы подписки (UI-SPEC E4 overflow)."
    - "Самое длинное значение виджета помещается в 147px доступной ширины либо переносится внутри виджета, а не выходит за его границу: ни .access-plan, ни .access-value не объявлены white-space: nowrap и flex: none (UI-SPEC E4 long-text)."
    - statement: "Пользователь в последний день доступа (days_left == 0 при живом сроке) достижим: существует тест, показывающий это значение и печать «последний день» на поверхности виджета; если ветка окажется недостижимой, она снимается тем же планом."
      verification: backstop
    - "Порог предупреждения — именованная константа ACCESS_SOON_DAYS = 7: внутри порога виджет показывает дни, вне — дату; литерала 7 в копирайте нет (D-K, UI-SPEC E1/E4 overflow)."
    - "Виджет сайдбара СОХРАНЯЕТСЯ на своём месте и перецеливается на срок доступа; шкала потребления из него удаляется по прохибиции P-4 (D-J, UI-SPEC E4 populated)."
    - "Шелл читает строку подписки ОДНИМ запросом и не делает ни одного дополнительного: два чтения журнала сообщений уходят, ни одного нового не приходит."
    - "Виджет подписан тем, что действительно показывает: подпись «Доступ» соответствует сроку доступа, и величины, которой виджет не показывает, в подписи нет."
  artifacts:
    - app/pages/common.py            # ключ shell["access"] формы {open, expires_at, days_left}
    - app/templates/base.html        # виджет [data-access]
    - app/static/css/app.css         # .access-head / .access-plan / .access-value / .access-link
  key_links:
    - "get_shell_context → request.state.shell['access'] → base.html [data-access] на всех 26 страничных маршрутах"
    - "days_left(expires_at, now) ← app/application/billing/subscription_period.py (введена планом 05.1-01)"
    - "ACCESS_SOON_DAYS ← app/constants.py (введена планом 05.1-01)"
    - "[data-quota] margin-top:auto → [data-access] margin-top:auto — свойство переезжает вместе с селектором"
---

<objective>
Перевести контракт шелла с квоты сообщений на доступ: `get_shell_context` перестаёт
читать журнал списаний, отдаёт `access`, виджет сайдбара перецеливается на срок
доступа, шкала из него удаляется.

Purpose: это изменение уровня ШЕЛЛА, а не раздела — виджет рисуется на всех 26
страничных маршрутах, и `get_shell_context` — единственная точка, где строка подписки
уже прочитана бесплатно. Пока шелл читает `MessageBalance` и `BalanceTransaction`,
модели нельзя снять: их удаление даёт 500 на каждой странице продукта.

Output: `shell["access"] = {open, expires_at, days_left}`, виджет `[data-access]`
без шкалы, стили `.access-*`, инвертированные регрессии шелла.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05.1-edinaya-podpiska/05.1-UI-SPEC.md
@.planning/phases/05.1-edinaya-podpiska/05.1-PATTERNS.md
@.planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md
@.planning/phases/05.1-edinaya-podpiska/05.1-01-SUMMARY.md
</context>

## Artifacts this phase produces (план 05.1-04)

| Символ | Место | Вид |
|--------|-------|-----|
| ключ контекста `access` формы `{open, expires_at, days_left}` | `app/pages/common.py` | ключ шелла |
| атрибут `[data-access]` | `app/templates/base.html` | атрибут разметки |
| `.access-head`, `.access-plan`, `.access-value`, `.access-link` | `app/static/css/app.css` | CSS-классы |

⚠️ Ключ `is_comped` в словаре `access` вводит план `05.1-08` вместе с колонкой
`subscriptions.has_free_access`: её хранилища сейчас не существует. Это не сокращение
объёма, а точка поставки решения D-E — бесплатный доступ администратора приезжает
целиком (колонка, предикат, ключ шелла, бейдж виджета, админские поверхности) одним
планом, а не размазывается по четырём.

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Контракт шелла — quota уходит, access приходит</name>

  <files>
    app/pages/common.py,
    tests/test_pages/test_shell.py,
    tests/test_pages/test_responsive_markup.py,
    tests/test_application/test_subscription_period.py
  </files>

  <read_first>
    app/pages/common.py:500-560 (выборка строки подписки, оба чтения журнала сообщений, словарь `quota` в возврате),
    app/pages/common.py:1-30 (импорты `MessageBalance`, `BalanceTransaction`, `plan_usage`),
    app/application/billing/subscription_period.py (функции `access_is_open` и `days_left`, введённые планом 05.1-01),
    app/constants.py (константа `ACCESS_SOON_DAYS`, введённая планом 05.1-01),
    tests/test_pages/test_shell.py:455-475 (утверждение `data-quota`),
    tests/test_pages/test_responsive_markup.py:1495-1545 (утверждения о подписи виджета, о виджете на /profile и утверждение о чтении журнала списаний шеллом),
    .planning/phases/05.1-edinaya-podpiska/05.1-UI-SPEC.md § Component Contracts → C3 и § Deletion Contract → «Инвертируется, а не удаляется»,
    .planning/phases/05.1-edinaya-podpiska/05.1-PATTERNS.md § Deletion Sites → `app/pages/common.py:517-556`
  </read_first>

  <behavior>
    - Тест 1: у пользователя с живым сроком `shell["access"]["open"]` истинно, `expires_at` равен дате строки подписки, `days_left` — целое число.
    - Тест 2: у пользователя с `expires_at` в прошлом `shell["access"]["open"]` ложно.
    - Тест 3: у пользователя БЕЗ строки подписки `shell["access"]` присутствует, `open` ложно, `expires_at` равен `None`, `days_left` равен `None` — исключения нет.
    - Тест 4: `days_left` равен нулю у пользователя, чей срок истекает через несколько часов, и срок при этом ЖИВОЙ — ветка «последний день» достижима.
    - Тест 5 (инверсия существующей регрессии): в исходнике `app/pages/common.py` больше нет обращения к журналу списаний сообщений.
    - Тест 6: рендер любой страницы делает то же число запросов к базе, что и до правки, минус два.
  </behavior>

  <action>
    В `get_shell_context` удалить оба чтения журнала сообщений: выборку строки баланса и
    агрегат израсходованного по журналу операций, вместе с вычислениями `remaining`,
    `is_unlimited`, `period_start`, `used`, `limit`, `percent`. Выборку строки подписки
    (`ORDER BY expires_at DESC LIMIT 1`) ОСТАВИТЬ нетронутой — на ней стоит требование
    «ноль дополнительных запросов» UI-контракта C3.

    Заменить ключ `quota` на `access` со значением `{"open": …, "expires_at": …,
    "days_left": …}`. `open` вычисляется единственным предикатом `access_is_open`, а не
    выражением на месте: вторая копия правила доступа разъехалась бы с зависимостью
    `require_access` молча — ровно тот класс расхождения, за который раздел получил шесть
    раундов гэпов. `days_left` берётся у одноимённой функции того же модуля.
    Прежний литерал подстановки плана у пользователя без строки подписки удалить: у ключа
    `access` умолчаний нет, отсутствие строки выражается значениями `False` / `None`,
    а не выдуманным именем тарифа.

    Импорты моделей журнала сообщений из `app/pages/common.py` удалить. Импорт
    `plan_usage` и глобалы `plan_axis_order` / `plan_axis_labels` в ЭТОМ плане НЕ трогать:
    их читает разметка страницы подписки, которую переписывает план `05.1-05`, а сам
    модуль снимает план `05.1-07`. Удаление глобалов раньше разметки уронило бы рендер.

    Инвертировать утверждение о чтении журнала списаний шеллом
    (`tests/test_pages/test_responsive_markup.py:1531-1538`): было «модель журнала
    присутствует в исходнике шелла», стало «отсутствует». Инверсия, а не удаление: гейт
    охраняет ту же границу, изменилось направление.
    Перецелить утверждения о разметке виджета в `tests/test_pages/test_shell.py:465` и
    `tests/test_pages/test_responsive_markup.py:1512,1519-1522` — атрибут и подпись
    виджета меняются задачей 2 этого же плана, и обе задачи уходят одной волной.

    В `tests/test_application/test_subscription_period.py` добавить тесты `days_left`,
    включая обязательный тест достижимости нулевого значения при ЖИВОМ сроке. Если тест
    показывает, что ноль недостижим (граница отсекается округлением), то ветка
    «последний день» снимается ТЕМ ЖЕ планом на обеих поверхностях, а факт недостижимости
    записывается в SUMMARY: недостижимая ветка копирайта стареет так же, как недостижимая
    строка отказа.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_shell.py tests/test_application/test_subscription_period.py -q -x</automated>
    <automated>uv run pytest tests/test_pages -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'MessageBalance\|BalanceTransaction' app/pages/common.py` не находит вхождений.
    - `grep -c '"quota"' app/pages/common.py` равно 0; `grep -c '"access"' app/pages/common.py` равно 1.
    - `grep -n 'access_is_open' app/pages/common.py` находит ровно один вызов — предикат не переписан выражением на месте.
    - `grep -n 'plan_axis_order\|plan_axis_labels' app/pages/common.py` по-прежнему находит объявления глобалов (их снимает план 05.1-07).
    - `uv run pytest tests/test_application/test_subscription_period.py -q -k days_left` завершается кодом 0 и собирает тест с нулевым значением при живом сроке.
    - `uv run pytest tests/test_pages -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Шелл отдаёт вердикт доступа, дату и число полных дней одним уже сделанным запросом;
    журнала сообщений он не читает; регрессия, охранявшая обратное, инвертирована.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Виджет сайдбара — перецеливание на срок доступа, шкала удаляется</name>

  <files>
    app/templates/base.html,
    app/static/css/app.css,
    tests/test_pages/test_responsive_markup.py
  </files>

  <read_first>
    app/templates/base.html:55-95 (объяснительный комментарий о подписи виджета, сам виджет и следующий за ним блок пользователя),
    app/static/css/app.css:495-520 (сайдбар: ширина, padding, gap),
    app/static/css/app.css:556-570 (стили виджета квоты целиком, включая объявление margin-top),
    app/static/css/app.css:660-670 (правило prefers-reduced-motion и перечень имён в нём),
    app/static/css/app.css:468-490 (мобильный блок и правило скрытия виджета и блока пользователя),
    .planning/phases/05.1-edinaya-podpiska/05.1-UI-SPEC.md § Component Contracts → C3 целиком, § Copywriting Contract → «Виджет сайдбара E4», § Page State Contract → P-6, § UI Considerations строки E4,
    .planning/phases/05.1-edinaya-podpiska/05.1-PATTERNS.md § Deletion Sites → `app/templates/base.html:71-81`, `app/static/css/app.css:556-570`, `app/static/css/app.css:664`
  </read_first>

  <action>
    Переписать виджет в `app/templates/base.html`: атрибут `[data-quota]` заменить на
    `[data-access]`; атрибуты процента и даты, которые несут значения шкалы, удалить.
    Анатомия — две строки вместо трёх: строка `.access-head` с подписью слева и значением
    справа, затем ссылка `.access-link`. Дорожку и заливку шкалы удалить целиком.

    Подпись виджета — `Доступ`, класс `.access-plan` (13px, вес 600, sans — ровно то, чем
    сегодня объявлена подпись квоты). Значение — класс `.access-value` (моношрифт, 11px,
    межбуквенный интервал .06em). Порядок и кегли зафиксированы контрактом C3 потому, что
    от них считается доступная ширина значения: поменять их местами — сломать обмер, а не
    оформление. Ни одному из двух НЕ назначать `white-space: nowrap` и `flex: none` —
    именно это позволяет длинному значению перенестись внутри виджета вместо выхода за его
    границу.

    Четыре представления значения, ветки проверяются в объявленном порядке:
    (1) `days_left` равен нулю при ЖИВОМ сроке — печатается `последний день`, тон `warn`;
    эта ветка стоит ПЕРВОЙ среди счётных, иначе человек, у которого доступ ещё работает,
    прочитает «осталось 0 дней» и поймёт это как «уже кончилось»;
    (2) `days_left` от единицы до `ACCESS_SOON_DAYS` включительно — `осталось {N} {дней}`,
    тон `warn`, форма слова через глобал склонения, а не выписыванием;
    (3) `days_left` больше `ACCESS_SOON_DAYS` — `до {дата}` через глобал форматирования
    даты в зоне пользователя;
    (4) доступ закрыт — бейдж `закрыт`, тон `danger`.
    Ветку бесплатного доступа (бейдж `бесплатный`, тон `success`) вводит план `05.1-08`
    вместе с ключом `is_comped`.
    Если ключа `access` в контексте шелла нет вовсе — виджет НЕ рисуется. Умолчание
    «закрыт» запрещено прямо: ложное «доступ закрыт» из-за пустого словаря — худший из
    возможных дефектов этого виджета, он рисуется на 26 страницах.

    Ссылка виджета — `ПОДПИСКА И ОПЛАТА →`, адрес прежний.

    Объяснительный комментарий над виджетом (`app/templates/base.html:55-70`) —
    ПЕРЕПИСАТЬ, а не удалить: правило «подпись называет то, что виджет показывает»
    остаётся верным, у него меняется предмет. Блок пользователя ниже НЕ трогать.

    В `app/static/css/app.css` переименовать селекторы виджета в `.access-*` и
    `[data-access]`. ⚠️ Свойство `margin-top: auto` объявлено на селекторе виджета, а НЕ
    на блоке пользователя: оно обязано уехать вместе с переименованием, иначе блок
    пользователя оторвётся от низа сайдбара на всех 26 страницах. Правила дорожки и
    заливки шкалы удалить. Из перечня правила `prefers-reduced-motion` вычеркнуть имя
    заливки шкалы, оставив остальные четыре имени нетронутыми: правило, адресованное
    несуществующему классу, — мусор, который следующий читатель примет за живой.
    В мобильном блоке заменить имя атрибута виджета в правиле скрытия, сохранив само
    правило: мобильной поверхности у виджета нет и не появляется.

    Обновить утверждения разметки в `tests/test_pages/test_responsive_markup.py` на новые
    атрибут и подпись, включая проверку виджета на странице профиля. Добавить тест,
    утверждающий, что свойство прижатия к низу объявлено на селекторе виджета, а виджет
    и блок пользователя скрыты одним правилом мобильного блока.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_responsive_markup.py -q -x -k "shell or quota or access or sidebar"</automated>
    <automated>uv run pytest tests/test_pages -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'data-quota' app/templates/base.html` равно 0; `grep -c 'data-access' app/templates/base.html` больше нуля.
    - `grep -c 'quota-track\|quota-fill' app/static/css/app.css` равно 0.
    - `grep -c 'quota' app/static/css/app.css` равно 0.
    - `grep -n 'margin-top: auto' app/static/css/app.css` находит объявление внутри блока правил `[data-access]`.
    - `grep -n 'prefers-reduced-motion' -A 3 app/static/css/app.css` показывает перечень из четырёх имён без имени заливки шкалы.
    - `grep -c 'plural_ru' app/templates/base.html` больше нуля; литерал `дней` без вызова склонения в файле отсутствует.
    - `grep -c '\bprogress\b' app/templates/base.html` равно 0 — шкалы в виджете нет.
    - `uv run pytest tests/test_pages -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Виджет стоит на своём месте, подписан `Доступ`, показывает срок доступа четырьмя
    представлениями, шкалы не несёт, прижатие к низу сайдбара сохранено, мобильной
    поверхности не приобрёл.
  </done>

  <reversibility rating="reversible">
    Переименование атрибута и классов виджета обратимо правкой тех же трёх файлов;
    хранимого состояния и внешних контрактов решение не затрагивает.
  </reversibility>
</task>

<task type="auto">
  <name>Задача 3: Потребители ключа quota на странице подписки переведены на access</name>

  <files>
    app/pages/billing.py,
    app/templates/billing/balance.html,
    tests/test_pages/test_billing_section.py
  </files>

  <read_first>
    app/pages/billing.py:160-200 (чтение ключа шелла и всё, что из него выводится: идентификатор плана, срок, признак истечения),
    app/pages/billing.py:205-275 (сборка контекста шаблона),
    app/templates/billing/balance.html:30-130 (чтение ключа шелла и блок текущего тарифа и срока),
    .planning/phases/05.1-edinaya-podpiska/05.1-PATTERNS.md § «Чтение вердикта на странице»,
    .planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md § Вопрос 6 (инвентарь пяти блоков страницы),
    .planning/phases/05.1-edinaya-podpiska/05.1-UI-SPEC.md § Page State Contract (для понимания целевой формы — реализует её план 05.1-05)
  </read_first>

  <behavior>
    - Тест 1: `GET /billing` у пользователя с живым сроком отвечает 200 и содержит дату окончания доступа.
    - Тест 2: `GET /billing` у пользователя с истёкшим сроком отвечает 200.
    - Тест 3: `GET /billing` у пользователя БЕЗ строки подписки отвечает 200, а не 500.
    - Тест 4: регрессия `test_the_screen_never_prints_the_yookassa_payment_id` остаётся зелёной.
  </behavior>

  <action>
    Это задача переноса контракта, а не изменения информационной архитектуры страницы:
    она делает ровно столько, сколько нужно, чтобы страница пережила смену ключа шелла.
    Четыре состояния, копирайт и переписывание разметки принадлежат плану `05.1-05`.

    В `app/pages/billing.py` заменить чтение `quota` на чтение `access` из уже
    посчитанного контекста шелла. Идентификатор текущего тарифа больше не выводится из
    шелла: ключа с ним там нет. Значение `expires_at` брать из `access`; признак истечения
    брать из `access["open"]`, а не пересчитывать сравнением дат на месте — второе
    выражение того же правила разъехалось бы с первым.

    ⚠️ Комментарий `app/pages/billing.py:210-214`, утверждающий, что истечение срока
    ничего не отключает и применения лимитов в системе нет вовсе, ИСПРАВИТЬ, а не удалить
    молча: он был правдой при решении D-07 фазы 5 и перестал ею быть — теперь истечение
    прекращает доступ. Абзац обязан назвать отмену прямо.

    В `app/templates/billing/balance.html` заменить чтение ключа шелла на новый и убрать
    печать имени тарифа из блока текущего срока: печатать остаётся ДАТА и признак
    истечения. Все чтения — через доступ с умолчанием, чтобы отсутствие строки подписки
    рисовалось как закрытый доступ, а не роняло страницу. Остальные блоки разметки — оси,
    карточки планов, баланс, пакеты, история операций — в этой задаче НЕ трогать: их
    снимает план `05.1-05`, и снятие их здесь развело бы одну работу по двум планам.

    Обновить в `tests/test_pages/test_billing_section.py` только те утверждения, которые
    ломаются сменой ключа. Тесты про оси, прейскурант и пакеты НЕ трогать — они умирают
    планом `05.1-05` вместе со своим предметом.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_billing_section.py -q -x</automated>
    <automated>uv run pytest tests/test_pages tests/test_routes -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "shell.get(\"quota\"" app/pages/billing.py` равно 0.
    - `grep -c "quota" app/templates/billing/balance.html` равно 0.
    - `grep -n 'ИСТЕЧЕНИЕ СРОКА' app/pages/billing.py` находит абзац, называющий отмену прежнего утверждения, а не прежнее утверждение.
    - `uv run pytest tests/test_pages/test_billing_section.py -q -x` завершается кодом 0.
    - `uv run pytest tests/test_pages/test_billing_section.py -q -k yookassa_payment_id` завершается кодом 0.
    - `uv run pytest tests/test_pages tests/test_routes -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Страница подписки читает новый ключ шелла, рендерится во всех трёх состояниях строки
    подписки, включая её отсутствие, и не печатает ключ подделки уведомления.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `request.state.shell` → Jinja-разметка на 26 маршрутах | вердикт авторизации пересекает границу «сервер → разметка»; разметка гарантий не даёт |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-18 | Elevation of Privilege | вердикт доступа, живущий только в разметке | high | mitigate | Виджет ПОКАЗЫВАЕТ вердикт и ничего не решает; принуждение живёт в зависимостях `require_access` и `get_current_user_id_with_access` (планы 05.1-01 и 05.1-03), ASVS V4.1.1. Скрытая кнопка средством защиты не считается |
| T-05.1-19 | Tampering | второе выражение правила доступа в шелле | medium | mitigate | `get_shell_context` зовёт единственный предикат `access_is_open`, а не пересчитывает вердикт сравнением дат; ассерт на единственный вызов входит в критерии приёмки |
| T-05.1-20 | Information Disclosure | контекст страницы подписки несёт строки платежей целиком | medium | mitigate | Регрессия, запрещающая печать идентификатора платежа ЮKassa, обязана пережить правку разметки; её зелёность — критерий приёмки задачи 3 |
| T-05.1-SC | Tampering | установка пакетов | — | N/A | План не устанавливает пакетов |
</threat_model>

<verification>
- `uv run pytest tests/test_pages tests/test_services tests/test_application -q` — зелёный.
- `grep -rc 'data-quota' app/templates/ app/static/css/` — ноль вхождений.
- Ручная сверка: сайдбар на любой странице показывает подпись `Доступ`, значение и ссылку; блок пользователя прижат к низу.
</verification>

<success_criteria>
- Шелл не читает журнала сообщений и отдаёт вердикт доступа, дату и число полных дней.
- Виджет перецелен, шкала снята, прижатие к низу сохранено, мобильное поведение не изменилось.
- Страница подписки пережила смену ключа во всех трёх состояниях строки подписки.
</success_criteria>

<output>
Create `.planning/phases/05.1-edinaya-podpiska/05.1-04-SUMMARY.md` when done
</output>
