---
phase: 05.1-edinaya-podpiska
plan: 03
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - app/services/subscription_service.py
  - app/services/billing_cache.py
  - app/services/payment_service.py
  - app/worker/tasks.py
  - app/pages/history.py
  - app/dependencies.py
  - app/main.py
  - tests/test_billing_cache.py
  - tests/test_application/test_access_gate_scheduling.py
  - tests/test_worker/test_tasks.py
  - tests/test_pages/test_history_retry.py
  - tests/test_pages/test_access_gate.py
  - tests/test_e2e.py
autonomous: true
requirements: [BILL-09, BILL-10]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Гейт доступа проверяется в collect_due_schedules ДО постановки задачи на отправку: у просроченного пользователя список задач пуст, а next_run_at сдвигается вперёд. Вторая линия — retry_send не зовёт send_task у просроченного (критерий 2b)."
    - "Просроченный пользователь на POST /api/ads получает 402 или 403, а не 201; отказ приходит кодом, а не HTML-редиректом (ASVS V13, критерий 2b)."
    - "Ключ кэша вердикта доступа называется access:{user_id}, а не balance:{user_id}: сохранение имени при смене смысла значения дало бы минуту неверных вердиктов после выката."
    - "Подтверждённое уведомление об оплате инвалидирует кэш доступа в ПОДПИСОЧНОЙ ветке вебхука: оплативший получает доступ немедленно, а не через TTL кэша."
    - "Зависимость get_current_user_id остаётся нетронутой: она не принимает db и обслуживает в том числе незащищённые пути; отказ доступа живёт в соседней зависимости."
    - "Администратор без действующей подписки входит в админку и в JSON-маршруты админки: гейт доступа не висит ни на require_admin, ни на маршрутах app/pages/admin.py."
  artifacts:
    - app/services/subscription_service.py     # check_access
    - app/services/billing_cache.py            # check_access_cached, invalidate_access_cache
    - app/dependencies.py                      # get_current_user_id_with_access
    - tests/test_application/test_access_gate_scheduling.py
    - tests/test_billing_cache.py
  key_links:
    - "collect_due_schedules(check_limit=check_access_cached) ← app/worker/tasks.py:195"
    - "retry_send ← app/worker/tasks.py:442-445 (вторая линия гейта)"
    - "повтор из истории ← app/pages/history.py:975 (первая линия гейта)"
    - "handle_webhook подписочная ветка → invalidate_access_cache(user_id) — новая точка вызова"
    - "app/routes/{ads,accounts,schedules,history,uploads} ← Depends(get_current_user_id_with_access)"
  prohibitions:
    - "Прекращение доступа не имеет права уничтожать или делать недостижимыми данные пользователя: объявления, расписания и группы сохраняются, планировщик лишь сдвигает срок следующего запуска."
---

<objective>
Закрыть две оставшиеся поверхности применения отказа — JSON-API и путь отправки — и
перевести кэшированный гейт с баланса сообщений на доступ.

Purpose: гейт, закрывающий две поверхности из трёх, — это обход авторизации, а не
неполная функция. Путь отправки — самая дорогая из трёх при пропуске: истёкший доступ
продолжает рассылать по расписанию, и это потерянные деньги на боевом стенде, а не
косметика.

Output: `check_access` / `check_access_cached` / `invalidate_access_cache`,
`get_current_user_id_with_access`, подстановка нового гейта в трёх точках пути
отправки, инвалидация кэша в подписочной ветке вебхука.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md
@.planning/phases/05.1-edinaya-podpiska/05.1-PATTERNS.md
@.planning/phases/05.1-edinaya-podpiska/05.1-01-SUMMARY.md
</context>

## Artifacts this phase produces (план 05.1-03)

| Символ | Место | Вид |
|--------|-------|-----|
| `check_access(db, user_id) -> tuple[bool, str]` | `app/services/subscription_service.py` | функция |
| `check_access_cached(db, user_id, action) -> tuple[bool, str]` | `app/services/billing_cache.py` | функция |
| `invalidate_access_cache(user_id) -> None` | `app/services/billing_cache.py` | функция |
| ключ Redis `access:{user_id}` | `app/services/billing_cache.py` | ключ кэша |
| `get_current_user_id_with_access` | `app/dependencies.py` | FastAPI-зависимость |
| `tests/test_application/test_access_gate_scheduling.py` | новый | тест-файл |

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Кэшированный гейт доступа и его подстановка на пути отправки</name>

  <files>
    app/services/subscription_service.py,
    app/services/billing_cache.py,
    app/services/payment_service.py,
    app/worker/tasks.py,
    app/pages/history.py,
    tests/test_billing_cache.py,
    tests/test_application/test_access_gate_scheduling.py
  </files>

  <read_first>
    app/services/billing_cache.py (весь файл, 70 строк — сигнатура, ранний выход, ключ, TTL, инвалидация),
    app/application/scheduling/use_cases.py:125-200 (сигнатура `collect_due_schedules` с параметром `check_limit` и место его применения),
    app/worker/tasks.py:185-200 и :430-450 (обе точки подстановки),
    app/pages/history.py:965-985 (первая линия гейта повтора),
    app/services/payment_service.py:600-645 (подписочная и пакетная ветки `handle_webhook`, место существующего `invalidate_balance_cache`),
    app/application/billing/subscription_period.py (функция `access_is_open`, введённая планом 05.1-01),
    app/services/subscription_service.py (модуль, созданный планом 05.1-01),
    tests/test_billing_cache.py (весь файл, 61 строка — форма, которую надо сохранить при смене предмета),
    tests/test_application/test_scheduling_use_cases.py:1-60 (скелет посева с собственным движком),
    .planning/phases/05.1-edinaya-podpiska/05.1-PATTERNS.md § Shared Patterns 1 и 2,
    .planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md § Вопрос 2 (Поверхность 3) и § Ловушка 3
  </read_first>

  <behavior>
    - Тест 1: `check_access(db, user_id)` у пользователя с живым сроком возвращает `(True, "")`.
    - Тест 2: `check_access(db, user_id)` у пользователя с `expires_at` в прошлом возвращает `(False, <непустая причина>)`.
    - Тест 3: `check_access(db, user_id)` у пользователя БЕЗ строки подписки возвращает `(False, <непустая причина>)` и не поднимает исключения.
    - Тест 4: `check_access_cached(db, user_id, "ads")` возвращает `(True, "")` не обращаясь к базе — ранний выход по `action != "send"`.
    - Тест 5: при недоступном Redis `check_access_cached` идёт прямо в базу и отвечает тем же вердиктом.
    - Тест 6: `collect_due_schedules` с `check_limit=check_access_cached` у просроченного пользователя возвращает ПУСТОЙ список, и `next_run_at` расписания сдвинут вперёд относительно исходного.
    - Тест 7: тот же вызов у пользователя с живым сроком возвращает непустой список задач.
    - Тест 8: подтверждённое уведомление подписочной ветки зовёт инвалидацию кэша доступа ровно один раз для владельца платежа.
  </behavior>

  <action>
    В `app/services/subscription_service.py` объявить
    `async def check_access(db, user_id) -> tuple[bool, str]`: выбрать активную строку
    подписки пользователя тем же запросом, каким её выбирает `get_shell_context`
    (`Subscription.user_id == user_id`, `Subscription.is_active.is_(True)`,
    `ORDER BY expires_at DESC LIMIT 1`), и вернуть
    `(access_is_open(subscription, now), причина)`. Причина при отказе — короткая строка
    журнального свойства, а не текст для экрана: слова пользователю рисует разметка из
    закрытого множества (UI-контракт E2), и вторая формулировка того же отказа разъехалась
    бы с первой. Кортеж `(bool, str)` сохраняется ровно потому, что его форма — контракт
    параметра `check_limit`.

    В `app/services/billing_cache.py` объявить `check_access_cached(db, user_id, action)`,
    скопировав форму `check_balance_cached` целиком: ранний выход `if action != "send":
    return True, ""`, чтение из Redis, вычисление через `check_access`, запись `setex` c
    `get_settings().billing_cache_ttl`, деградация без Redis через существующий `_get_redis`.
    Ключ — `f"access:{user_id}"`. Переименование ключа обязательно и является предметом
    решения, а не оформления: сохранённое имя при изменившемся СМЫСЛЕ значения даёт минуту,
    в которой закэшированные ответы о балансе читаются как ответы о доступе. Рядом объявить
    `invalidate_access_cache(user_id)` по форме `invalidate_balance_cache`.
    Существующие `check_balance_cached` и `invalidate_balance_cache` в этом плане НЕ
    удалять: их снимает план `05.1-06` вместе с самим списанием. Порядок несущий — между
    введением нового гейта и снятием старого путь отправки обязан быть закрыт хотя бы одним
    из них, иначе окно между двумя коммитами оставляет рассылку без гейта вовсе.

    Заменить подставляемую функцию в трёх точках: `app/worker/tasks.py:195`
    (`check_limit=` у `collect_due_schedules`), `app/worker/tasks.py:442-445` (вторая линия
    в повторе отправки) и `app/pages/history.py:975` (первая линия повтора из истории).
    Поведение отказа не менять ни в одной: расписание не отправляет, `next_run_at`
    сдвигается вперёд, и после оплаты рассылка возобновляется сама — это уже правильное
    поведение для новой модели, и проектировать здесь нечего.

    В `app/services/payment_service.py` добавить вызов инвалидации кэша доступа в
    ПОДПИСОЧНУЮ ветку `handle_webhook` — после `await db.commit()` и до `return True`
    (сегодня вызов стоит только в пакетной ветке, после начисления сообщений, и
    подписочная возвращает `True` раньше). Без этого оплативший ждал бы до минуты, пока
    истечёт TTL, и всё это время видел бы «доступ закрыт» на всех страницах.

    Переписать `tests/test_billing_cache.py` на новый предмет, сохранив форму файла:
    четыре теста той же структуры, но про доступ. Создать
    `tests/test_application/test_access_gate_scheduling.py` по скелету
    `tests/test_application/test_scheduling_use_cases.py:1-60` — собственный движок
    `create_async_engine("sqlite+aiosqlite:///:memory:")` и `Base.metadata.create_all`, а не
    фикстура `db_session`. Файл обязан утверждать ОБА следствия отказа: пустой список задач
    И сдвиг `next_run_at` вперёд. Второе — прямое свидетельство того, что закрытие доступа
    не разрушает пользовательских данных: расписание сохраняется и переносится, а не
    удаляется и не отключается.
  </action>

  <verify>
    <automated>uv run pytest tests/test_billing_cache.py tests/test_application/test_access_gate_scheduling.py -q -x</automated>
    <automated>uv run pytest tests/test_worker tests/test_services -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'access:{user_id}' app/services/billing_cache.py` больше нуля; `grep -n 'f"balance:' app/services/billing_cache.py` по-прежнему находит СТАРЫЙ ключ (старая функция ещё жива и снимается планом 05.1-06).
    - `grep -n 'check_access_cached' app/worker/tasks.py app/pages/history.py` находит ровно три вхождения в исполняемом коде.
    - `grep -n 'def check_access' app/services/subscription_service.py` находит ровно одно объявление.
    - `grep -n 'invalidate_access_cache' app/services/payment_service.py` находит вызов внутри подписочной ветки `handle_webhook`.
    - `uv run pytest tests/test_application/test_access_gate_scheduling.py -q -x` завершается кодом 0 и файл содержит ассерт на сдвиг `next_run_at`.
    - `uv run pytest tests/test_worker tests/test_services -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Путь отправки закрыт вердиктом доступа во всех трёх точках; кэш переименован вместе со
    смыслом; оплативший получает доступ немедленно, а не через минуту.
  </done>

  <reversibility rating="reversible">
    Переименование ключа кэша обратимо: старые ключи истекают сами за TTL 60 с, хранимого
    состояния решение не создаёт.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Отказ доступа на JSON-API отдельной зависимостью</name>

  <files>
    app/dependencies.py,
    app/main.py,
    tests/test_pages/test_access_gate.py
  </files>

  <read_first>
    app/dependencies.py:25-85 (`get_current_user_id`, `get_current_user`, `require_admin` — три существующие формы),
    app/main.py:70-95 (порядок включения роутеров, включая обособленный `dashboard_feed_router` и комментарий на :87-90),
    app/routes/ads.py, app/routes/accounts.py, app/routes/schedules.py, app/routes/history.py, app/routes/uploads.py (места применения `Depends(get_current_user_id)`),
    app/routes/billing.py:14-40 (маршруты, которые НЕ имеют права закрыться),
    app/routes/auth.py:36,62 (маршруты, не использующие `get_current_user_id`),
    tests/test_pages/test_access_gate.py (файл, созданный планом 05.1-01 — заполняется группа `api`),
    .planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md § Вопрос 2 (Поверхность 2) и § Security Domain
  </read_first>

  <behavior>
    - Тест 1: просроченный пользователь на `POST /api/ads` получает 402 или 403 и НЕ 201.
    - Тест 2: тот же пользователь на `GET /api/billing/...` получает штатный ответ — денежные маршруты не закрываются.
    - Тест 3: тот же пользователь на `POST /api/auth/login` получает штатный ответ — вход не закрывается.
    - Тест 4: пользователь с живым сроком на `POST /api/ads` получает 201.
    - Тест 5: тело отказа — JSON с объяснением, а не HTML и не редирект.
    - Тест 6 (гейт перечня): множество модулей `app/routes/*`, применяющих зависимость с проверкой доступа, равно объявленному перечню; модуль, добавленный будущим планом без неё, роняет тест.
  </behavior>

  <action>
    В `app/dependencies.py` объявить
    `async def get_current_user_id_with_access(request, db=Depends(get_db), settings=Depends(get_settings)) -> int`
    по форме `require_admin` (`:73-82`): получить идентификатор через существующую
    `get_current_user_id`, затем спросить `check_access`; при отказе поднять
    `HTTPException(status_code=402, detail=<строка объяснения>)`. Код 402 выбран потому,
    что отказ по неоплате отличим от отказа по правам (403) — JSON-клиенту редирект
    бессмыслен, и различимость двух причин отказа стоит одного кода.

    ⚠️ `get_current_user_id` НЕ ТРОГАТЬ ни одной строкой: она не принимает `db`,
    обслуживает в том числе незащищённые пути, и добавление в неё проверки доступа
    расширило бы радиус на маршруты, которые обязаны оставаться открытыми.

    Применить новую зависимость ПЕР-РОУТЕРНО в `app/main.py` при включении
    `ads_router`, `accounts_router`, `schedules_router`, `history_router`, `uploads_router`
    через `dependencies=[Depends(get_current_user_id_with_access)]`. НЕ применять к
    `billing_router` (там живёт вебхук и чтения денежного раздела), к `auth_router` и к
    `dashboard_feed_router`.

    `dashboard_feed_router` оставить открытым СОЗНАТЕЛЬНО и записать решение абзацем в
    коде рядом с его включением: лента только читает, своего гарда входа она уже держит
    сама (`get_user_from_cookie`), а гейт доступа на бессрочном опросе стоил бы запроса к
    базе на каждый тик каждой открытой вкладки. Умолчание здесь запрещено: маршрут,
    выпавший из общей зависимости молча, — ровно та форма обхода, которую этот план
    закрывает.

    Заполнить группу `api` в `tests/test_pages/test_access_gate.py` по шести пунктам
    `<behavior>`. Тест перечня (пункт 6) читать ИСХОДНИК `app/main.py` синтаксическим
    разбором либо грепом по вызовам `include_router` — цель поймать маршрут, добавленный
    будущим планом без зависимости.
  </action>

  <verify>
    <automated>uv run pytest tests/test_pages/test_access_gate.py -q -x -k api</automated>
    <automated>uv run pytest tests/test_routes -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'def get_current_user_id_with_access' app/dependencies.py` находит ровно одно объявление.
    - Тело `get_current_user_id` в `app/dependencies.py` не изменилось: `git diff app/dependencies.py` не показывает правок в диапазоне её объявления.
    - `grep -c 'get_current_user_id_with_access' app/main.py` равно 5.
    - `grep -n 'dashboard_feed_router' app/main.py` показывает включение БЕЗ параметра `dependencies`, и рядом стоит объясняющий абзац.
    - `uv run pytest tests/test_pages/test_access_gate.py -q -k api` завершается кодом 0.
    - `uv run pytest tests/test_routes -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Просроченный пользователь получает кодом отказ на пяти JSON-роутерах, создающих
    ценность, и штатный ответ на денежных маршрутах и на входе; перечень закрытых роутеров
    закреплён машинным гейтом; `get_current_user_id` не тронута.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Вторая линия гейта в воркере и порядок «гейт до отправки»</name>

  <files>
    tests/test_worker/test_tasks.py,
    tests/test_pages/test_history_retry.py,
    tests/test_e2e.py
  </files>

  <read_first>
    tests/test_worker/test_tasks.py — все места патча `check_balance_cached` (шесть вхождений: :196, :222, :238, :255, :288, :703) и комментарий про списание на :924,
    tests/test_pages/test_history_retry.py:700-735 (утверждение порядка `assert body.index(...) < body.index("send_task(")` и отрицательные утверждения тела обработчика),
    tests/test_pages/test_history_retry.py — четыре патча `check_balance_cached` (:208, :889, :927, :961),
    tests/test_e2e.py:125-140 (патч `check_balance_cached`),
    app/worker/tasks.py:430-450 (вторая линия после правки задачей 1),
    app/pages/history.py:965-985 (первая линия после правки задачей 1)
  </read_first>

  <behavior>
    - Тест 1: `retry_send` у просроченного пользователя НЕ зовёт `send_task`.
    - Тест 2: `retry_send` у пользователя с живым сроком зовёт `send_task`.
    - Тест 3 (по синтаксическому дереву/исходнику): в теле обработчика повтора из истории вызов гейта доступа стоит РАНЬШЕ постановки задачи на отправку.
    - Тест 4: сквозной тест `tests/test_e2e.py` проходит с патчем нового гейта.
  </behavior>

  <action>
    Перецелить все патчи гейта в трёх тестовых файлах на новое имя функции, сохранив
    структуру тестов: шесть в `tests/test_worker/test_tasks.py`, четыре в
    `tests/test_pages/test_history_retry.py`, один в `tests/test_e2e.py`. Патч ставится на
    ту же точку импорта, откуда функция читается вызывающим модулем, — иначе патч не
    подменит вызов.

    Утверждение порядка в `tests/test_pages/test_history_retry.py:717` перецелить на новое
    имя, НЕ ослабляя его: тест обязан продолжать сравнивать позицию вызова гейта с позицией
    постановки задачи на отправку в теле обработчика. Перестановка двух операторов и в
    новой модели вернёт дефект молча, и этот тест — единственный сторож порядка.

    Добавить в `tests/test_worker/test_tasks.py` группу тестов, различимую по `-k access`,
    покрывающую пункты 1 и 2 `<behavior>` на второй линии повтора.

    ⚠️ Отрицательные утверждения `tests/test_pages/test_history_retry.py:730-731` о
    списании сообщений в теле обработчика в этом плане НЕ трогать: списание ещё живо, и
    инвертировать их до его снятия значило бы утверждать неправду. Их судьбу решает план
    `05.1-06`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_worker/test_tasks.py -q -x -k access</automated>
    <automated>uv run pytest tests/test_worker tests/test_pages/test_history_retry.py tests/test_e2e.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'check_access_cached' tests/test_worker/test_tasks.py` равно 6 или больше; `grep -c 'check_balance_cached' tests/test_worker/test_tasks.py` равно 0.
    - `grep -c 'check_balance_cached' tests/test_pages/test_history_retry.py` равно 0; `grep -c 'check_balance_cached' tests/test_e2e.py` равно 0.
    - В `tests/test_pages/test_history_retry.py` присутствует тест, сравнивающий индекс вызова гейта с индексом `send_task(` в теле обработчика, и он зелёный.
    - `uv run pytest tests/test_worker/test_tasks.py -q -k access` завершается кодом 0 и собирает не менее двух тестов.
    - `uv run pytest tests/test_worker tests/test_pages/test_history_retry.py tests/test_e2e.py -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Обе линии повтора отправки спрашивают вердикт доступа; порядок «гейт до отправки»
    остаётся закреплённым тестом по исходнику, а не соглашением.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON-клиент → `app/routes/*` (Bearer или cookie) | недоверенный вход; новая граница авторизации по доступу |
| Celery beat / worker → база (вне HTTP) | исполняется без пользователя и без сессии запроса; отказ здесь ничем не виден снаружи |
| приложение → Redis | кэш вердикта авторизации хранится вне процесса |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-02 | Elevation of Privilege | `collect_due_schedules` / `retry_send` / повтор из истории | critical | mitigate | Вердикт доступа подставляется во все три точки одной функцией; порядок «гейт до постановки задачи» закреплён тестом по исходнику; отказ сдвигает `next_run_at` и ничего не удаляет |
| T-05.1-14 | Elevation of Privilege | `app/routes/*` — маршрут без зависимости доступа | high | mitigate | Пер-роутерное применение перечнем + отрицательный гейт-тест, читающий `app/main.py` по исходнику. Ручное «не забыть» средством защиты не считается |
| T-05.1-15 | Elevation of Privilege | `app/pages/dashboard_feed.py` — выпадает из общей зависимости | medium | accept | Лента только читает и держит собственный гард входа; гейт доступа на бессрочном опросе стоил бы запроса на каждый тик. Решение записано абзацем рядом с включением роутера, а не молчанием |
| T-05.1-04 | Tampering | ключ `access:{user_id}` в Redis | medium | mitigate | Ключ переименован вместе со сменой смысла значения; TTL 60 с ограничивает окно; инвалидация вызывается после каждой записи `expires_at` — в подписочной ветке вебхука |
| T-05.1-16 | Denial of Service (product lockout) | `billing_router`, `auth_router` в `app/main.py` | high | mitigate | Оба включаются без зависимости доступа явно; тесты 2 и 3 группы `api` утверждают штатный ответ у просроченного пользователя |
| T-05.1-17 | Elevation of Privilege | админ без подписки не может войти в админку | medium | mitigate | Гейт не вешается ни на `require_admin`, ни на `app/pages/admin.py`; иначе администратор без подписки не смог бы выдать бесплатный доступ ни себе, ни другим |
| T-05.1-SC | Tampering | установка пакетов | — | N/A | План не устанавливает пакетов |
</threat_model>

<verification>
- `uv run pytest tests/test_pages tests/test_services tests/test_application tests/test_worker tests/test_routes -q` — зелёный.
- `uv run pytest tests/test_worker tests/test_migrations -q` — зелёный (волна касается воркера).
- `grep -rn 'check_access_cached' app/ | wc -l` — не менее четырёх (объявление + три точки подстановки).
</verification>

<success_criteria>
- Все три поверхности применения отказа закрыты: страницы (план 05.1-01), JSON-API и путь отправки (этот план).
- Ключ кэша переименован вместе со смыслом; инвалидация стоит в подписочной ветке.
- Перечни закрытых роутеров закреплены машинными гейтами на обеих поверхностях.
</success_criteria>

<output>
Create `.planning/phases/05.1-edinaya-podpiska/05.1-03-SUMMARY.md` when done
</output>
