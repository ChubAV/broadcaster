---
phase: 05.1-edinaya-podpiska
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03, BILL-04, BILL-05, BILL-06, BILL-07, BILL-08, BILL-09, BILL-10, BILL-11]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Пять требований блока BILL помечены VOID с названным основанием и не считаются ни выполненными, ни открытыми; долг BILL-02 закрыт СНЯТИЕМ ПРЕДМЕТА, и это записано словами, а не выведено из отсутствия строки."
    - "Четыре новых требования BILL-08…BILL-11 объявлены, распределены по фазе 05.1 и присутствуют в обеих таблицах прослеживаемости REQUIREMENTS.md."
    - "Счётчики v2.0 в ROADMAP §Coverage и в REQUIREMENTS.md пересчитаны и согласуются между собой и с деревом требований."
    - "`just tracking-check` зелёный: счёт планов STATE ↔ ROADMAP сходится после правки ROADMAP."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "ROADMAP §Phase 05.1 `**Requirements:**` ← перечень BILL-05, BILL-07, BILL-08, BILL-09, BILL-10, BILL-11"
    - "REQUIREMENTS.md таблицы прослеживаемости ← вердикты SURVIVES / VOIDED-BY-MODEL-CHANGE"
    - "tests/test_planning/test_state_progress_matches_roadmap.py ← §Progress ROADMAP и STATE.md"
---

<objective>
Привести блок требований BILL-* в соответствие с плоской моделью: пять требований
объявить VOID с основанием, четыре новых объявить и распределить, счётчики v2.0
пересчитать, строку `Requirements:` фазы 05.1 заполнить.

Purpose: ROADMAP прямо требует пересмотра, а не наследования. Пока пересмотр не
записан, фаза исполняется против требований, половина которых описывает отменённую
модель, а долг BILL-02 («система применяет тарифные лимиты») переживает фазу,
предмет которой исчез.

Output: правленые `.planning/REQUIREMENTS.md` и `.planning/ROADMAP.md`, зелёный
`just tracking-check`.
</objective>

<execution_context>
@/source/broadcaster/.claude/gsd-core/workflows/execute-plan.md
@/source/broadcaster/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md
</context>

## Artifacts this phase produces (план 05.1-02)

| Символ | Место | Вид |
|--------|-------|-----|
| `BILL-08` | `.planning/REQUIREMENTS.md` | идентификатор требования |
| `BILL-09` | `.planning/REQUIREMENTS.md` | идентификатор требования |
| `BILL-10` | `.planning/REQUIREMENTS.md` | идентификатор требования |
| `BILL-11` | `.planning/REQUIREMENTS.md` | идентификатор требования |

<tasks>

<task type="auto">
  <name>Задача 1: Вердикты по действующим BILL-* и объявление BILL-08…BILL-11</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    .planning/REQUIREMENTS.md (весь файл, 264 строки — оба перечня требований и обе таблицы прослеживаемости, включая строки 47-53, 115-117, 186-189, 202, 235-237, 256, 263),
    .planning/phases/05.1-edinaya-podpiska/05.1-RESEARCH.md § Phase Requirements — ревизия блока BILL-* (строки 59-84),
    .planning/phases/05.1-edinaya-podpiska/05.1-01-PLAN.md § Ревизия блока требований BILL-* (таблица вердиктов — источник истины для этой задачи),
    .planning/ROADMAP.md:419-441 (цель и пять критериев фазы — источник формулировок BILL-08…BILL-11)
  </read_first>

  <action>
    Проставить вердикт КАЖДОМУ из семи действующих требований блока, не удаляя ни одной
    строки: удаление стёрло бы историю решения, а вердикт её сохраняет.

    Пять требований помечаются `VOID (05.1)`: BILL-01, BILL-02, BILL-03, BILL-04, BILL-06.
    Каждое получает однострочное основание с указанием решения владельца, снявшего предмет:
    BILL-01 — D-A и D-F (тарифов Free/Basic/Pro не существует; часть «и текущую подписку»
    переезжает в BILL-10); BILL-02 — D-D (предмет снят; долг закрывается снятием предмета,
    а НЕ реализацией — это надо написать буквально, иначе `Partial` от 2026-08-15 переживёт
    фазу); BILL-03 — D-D (баланса сообщений нет; платёжная система остаётся, меняется
    предмет покупки); BILL-04 — D-D (списаний нет); BILL-06 — критерий 4 фазы требует
    обратного.

    Два требования оставить дословно и перевести в `Complete (05.1)` по факту исполнения
    фазы — отметку ставит НЕ этот план, а верификация фазы; здесь только фиксируется, что
    формулировка переживает смену модели: BILL-05 («может продлить текущую подписку») и
    BILL-07 («видит историю своих платежей»). Ни один символ их текста не меняется —
    вердикт NEEDS-REWORDING не применим ни к одному требованию блока, и это надо сказать
    явно.

    Объявить четыре новых требования с формулировками, взятыми из критериев фазы
    (ROADMAP:426-430), не пересказывая их своими словами:
    BILL-08 — доступ стоит одно число 3000 ₽/мес, ни аккаунты, ни группы, ни сообщения на
    сумму не влияют и ничем не ограничены (критерий 1);
    BILL-09 — первые пять дней бесплатно и в полном объёме, по истечении доступ
    прекращается и человеку сказано словами, что произошло и что оплатить (критерий 2);
    BILL-10 — ровно ОДНА дата окончания доступа, оплата сдвигает её вперёд, дата видна на
    странице подписки (критерии 3 и 4);
    BILL-11 — администратор может выдать бесплатный ДОСТУП, и это освобождает от оплаты
    доступа (критерий 5).

    Внести все четыре в ОБЕ таблицы прослеживаемости `REQUIREMENTS.md` со статусом
    `Pending` и фазой `Phase 05.1`. Пересчитать счётчики v2.0 (`Complete` / `Partial` /
    `Pending` / `Итого`) арифметически по получившемуся дереву, а не правкой чисел на
    глаз; в строке `Last updated` назвать план `05.1-02` и причину правки.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'VOID (05.1)' .planning/REQUIREMENTS.md` равно 5.
    - `grep -n 'BILL-08\|BILL-09\|BILL-10\|BILL-11' .planning/REQUIREMENTS.md` находит каждый идентификатор минимум трижды (объявление + две таблицы прослеживаемости).
    - `grep -n 'BILL-02' .planning/REQUIREMENTS.md` находит строку, содержащую формулировку про закрытие снятием предмета; статуса `Partial` у BILL-02 в файле не остаётся.
    - Тексты BILL-05 и BILL-07 совпадают с прежними посимвольно: `grep -n 'может продлить текущую подписку' .planning/REQUIREMENTS.md` и `grep -n 'видит историю своих платежей' .planning/REQUIREMENTS.md` находят вхождения.
    - Сумма `Complete + Partial + Pending` в счётчиках v2.0 равна объявленному `Итого`, и `Итого` равно фактическому числу строк требований v2.0.
    - `uv run pytest tests/test_planning/ -q` завершается кодом 0.
  </acceptance_criteria>

  <done>
    Каждое из одиннадцати требований блока BILL несёт вердикт: пять VOID с основанием,
    два SURVIVES дословно, четыре NEW с формулировкой из критерия. Счётчики сходятся.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Строка Requirements фазы 05.1 и §Coverage в ROADMAP</name>

  <files>.planning/ROADMAP.md</files>

  <read_first>
    .planning/ROADMAP.md:419-441 (заголовок фазы 05.1, строка `**Requirements**`, критерии),
    .planning/ROADMAP.md:543-588 (Phase 6 и раздел §Coverage со счётчиками и таблицей распределения по фазам),
    .planning/ROADMAP.md:293-300 (строка `**Requirements**` фазы 5 — она остаётся исторической записью и НЕ правится),
    .planning/REQUIREMENTS.md (после правки задачей 1 — источник новых счётчиков),
    tests/test_planning/test_state_progress_matches_roadmap.py:160-300 (что именно гейт выводит из ROADMAP)
  </read_first>

  <action>
    Заменить `**Requirements**: TBD (выводятся при планировании; BILL-* фазы 5 подлежат
    пересмотру, а не наследованию)` в заголовке фазы 05.1 на перечень
    `BILL-05, BILL-07, BILL-08, BILL-09, BILL-10, BILL-11`. Правку делать точечной заменой
    ЭТОЙ строки, а не перезаписью раздела: блок Notes фазы 05.1 несёт решения владельца
    D-A…D-K и является для фазы документом решений вместо отсутствующего CONTEXT.md.

    Строку `**Requirements**: BILL-05, BILL-06, BILL-07` фазы 5 НЕ трогать. Фаза 5 не
    откатывается и не переписывается — её артефакты остаются историей решения; вердикт VOID
    по BILL-06 живёт в `REQUIREMENTS.md`, а не в заголовке уже исполненной фазы.

    В §Coverage пересчитать итог и привести таблицу распределения по фазам: пять VOID
    выходят из счёта распределённых, четыре новых входят со ссылкой на фазу 05.1.
    Утверждение «Все N требований v2.0 распределены ровно по одной фазе» обязано остаться
    ПРАВДОЙ после пересчёта — если после вычета VOID и добавления новых оно перестаёт
    сходиться, чинить дерево, а не число.

    Строки `**Plans:**` и блок `Plans:` фазы 05.1 в этой задаче НЕ трогать: их заполняет
    планировщик при создании планов, и двойная правка развела бы счёт планов между STATE.md
    и ROADMAP.md, который проверяет `tests/test_planning/`.
  </action>

  <verify>
    <automated>uv run pytest tests/test_planning/ -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'Requirements' .planning/ROADMAP.md` в блоке фазы 05.1 показывает строку с шестью идентификаторами и без слова `TBD`.
    - `grep -c 'TBD' .planning/ROADMAP.md` не содержит вхождений внутри блока фазы 05.1 (строки заголовка фазы).
    - Строка `**Requirements**: BILL-05, BILL-06, BILL-07` фазы 5 присутствует в файле без изменений.
    - Итог §Coverage равен сумме строк таблицы распределения по фазам.
    - `uv run pytest tests/test_planning/ -q` завершается кодом 0.
    - `git diff --stat .planning/ROADMAP.md` показывает изменения только в блоке фазы 05.1 и в §Coverage.
  </acceptance_criteria>

  <done>
    Фаза 05.1 объявляет шесть требований поимённо; §Coverage сходится; трекинг зелёный;
    заголовок фазы 5 не тронут.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | План правит только планировочные документы; исполняемого кода, внешних входов и хранимых данных не касается |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-12 | Repudiation | `.planning/REQUIREMENTS.md` — удаление требования вместо пометки VOID | medium | mitigate | Ни одна строка требования не удаляется; вердикт проставляется рядом с текстом, и основание называет решение владельца. Долг BILL-02 закрывается записанной причиной, а не исчезновением строки |
| T-05.1-13 | Tampering | §Coverage ROADMAP — счётчик, разошедшийся с деревом | low | mitigate | Счётчики выводятся арифметикой по дереву; `tests/test_planning/` проверяет согласованность STATE ↔ ROADMAP на каждом прогоне `just test` |
| T-05.1-SC | Tampering | установка пакетов | — | N/A | План не устанавливает пакетов и не касается кода |
</threat_model>

<verification>
- `uv run pytest tests/test_planning/ -q` — зелёный.
- Ручная сверка: сумма счётчиков v2.0 в `REQUIREMENTS.md` совпадает с итогом §Coverage в `ROADMAP.md`.
</verification>

<success_criteria>
- Одиннадцать требований блока BILL несут вердикт; ни одно не пропало и ни одно не унаследовано молча.
- Фаза 05.1 объявляет свои шесть требований в ROADMAP.
- `just tracking-check` зелёный.
</success_criteria>

<output>
Create `.planning/phases/05.1-edinaya-podpiska/05.1-02-SUMMARY.md` when done
</output>
