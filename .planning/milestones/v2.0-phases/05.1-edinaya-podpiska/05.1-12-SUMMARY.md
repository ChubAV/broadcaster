---
phase: 05.1-edinaya-podpiska
plan: 12
subsystem: planning
tags: [requirements, traceability, coverage-counters, deferred-register, edge-cases, code-review]

# Dependency graph
requires:
  - phase: 05.1-10
    provides: "CR-02 закрыт — `SUBSCRIPTION_DESCRIPTION` есть назначение платежа, мест вставки не осталось; BILL-05/BILL-07 сознательно НЕ отмечены"
  - phase: 05.1-11
    provides: "CR-01 закрыт веткой `b-never-shorten` (решение владельца 2026-08-21) — ревизия 0020 подтягивает срок и никогда не укорачивает; применимость к боевым данным открыта до плана 05.1-14"
  - phase: 05.1 (проверка фазы)
    provides: "`05.1-VERIFICATION.md` — истины 1-4, проверенные по коду на всех четырёх уровнях: они и есть основание трёх отметок"
provides:
  - "Реестр требований, не отстающий от кода на три строки: BILL-08, BILL-09 и BILL-10 стоят `Complete` в ОБЕИХ таблицах"
  - "Основание каждой отметки — свидетельство проверки и команда прогона, а не факт исполненного плана"
  - "Счётчики §Coverage (v2.0), выведенные арифметикой; их внутреннее расхождение со строкой «Сумма» закрыто"
  - "Названный список отложенного: WR-01…WR-08 поимённо с причиной у каждого"
  - "Реестр четырнадцати неснятых краёв зонда с судьбой у каждого"
  - "Сверка двух независимых инструментов (ревизия кода ↔ отчёт проверки) с поимённо названными расхождениями"
affects: [05.1-13, 05.1-14, ship-v2.0, следующая фаза — Phase 6]

actuals:
  tokens: 8815    # chars/4 по единственному изменённому файлу `.planning/REQUIREMENTS.md` (35 261 знак); тот же счёт по одному диффу задачи 1 даёт ≈4 494
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Отметка требования ставится по ПРОВЕРЕННОМУ КОДУ, а не по исполненному плану, и основание называет ИСТИНУ отчёта проверки плюс команду прогона"
    - "Счётчики покрытия выводятся арифметикой по дереву, а не правятся на глаз: расхождение, дающее верную сумму при неверных слагаемых, иначе переживает пересчёт"
    - "Отложенное записывается ПОИМЁННО с причиной: молча пропущенное предупреждение через фазу неотличимо от закрытого"
    - "Два инструмента, смотревшие на один код независимо, сверяются, а не усредняются — пункт, увиденный одним, называется вместе с причиной, по которой второй его не видел"

key-files:
  created:
    - .planning/phases/05.1-edinaya-podpiska/deferred-items.md
  modified:
    - .planning/REQUIREMENTS.md

decisions:
  - "BILL-08/09/10 отмечены `Complete` в обеих таблицах по истинам 1-4 отчёта проверки; ни одна отметка не сослалась на исполненный план"
  - "BILL-05 и BILL-07 оставлены `In Progress`: условием их отметки названа ручная приёмка 375px, её проводит план 05.1-13"
  - "Внутреннее расхождение строки «Сумма» со строками состояний закрыто здесь же и названо в подвале отдельным предложением — иначе новые числа читались бы как опечатка"
  - "Ветка чекпойнта плана 05.1-11 прочитана из его SUMMARY: `b-never-shorten` (б). Обе зависящие от неё строки реестров записаны В ОДНОЙ редакции — той, что соответствует факту"
  - "Красный `just tracking-check` не чинился: он красен на счёте ПЛАНОВ, читает только STATE.md и ROADMAP.md, а их правка этому исполнителю запрещена прямо; вынесено в deferred-items.md"

requirements-completed: [BILL-08, BILL-09, BILL-10]

# Metrics
duration: ~25 мин
completed: 2026-08-21

status: complete
---

# Phase 05.1 Plan 12: Реестр требований и названный список отложенного — Summary

**Три требования, чью реализацию проверка фазы доказала по коду на всех четырёх уровнях, перестали числиться ожидающими: BILL-08, BILL-09 и BILL-10 отмечены `Complete` в обеих таблицах с основанием из отчёта проверки, а не из факта исполненных планов, — и вместе с ними записан решённым весь список, который этот прогон закрывать не будет.**

⚠️ **Ни одной строки кода этот план не тронул.** Его предмет — документы `.planning/`.

## Performance

- **Duration:** ~25 мин
- **Completed:** 2026-08-21
- **Tasks:** 2
- **Files modified:** 1 (`.planning/REQUIREMENTS.md`); создан 1 (`deferred-items.md`)

_Примечание к `actuals.tokens`._ Значение 8 815 снято как chars/4 по ЦЕЛИКОМ изменённому файлу, как предписывает шаблон сводки. Тот же счёт по одному диффу задачи 1 даёт ≈4 494. Оценка плана была 25 000 при `confidence: low` — план переоценён примерно втрое, и это записано без округления в сторону оценки: подгонка испортила бы калибровку следующих.

## Task Commits

1. **Задача 1: отметка BILL-08/09/10 и пересчёт счётчиков** — `b917258` (docs) — ровно один файл, `.planning/REQUIREMENTS.md`
2. **Задача 2: названный список отложенного** — коммит этой сводки (docs)

## Accomplishments

- **Реестр перестал отставать от кода на три строки.** BILL-08, BILL-09 и BILL-10 переведены `Pending` → `Complete` в ОБЕИХ таблицах — в дереве требований и в таблице прослеживаемости v2.0. Одной таблицы было бы недостаточно: расхождение двух представлений одного состояния есть ровно тот дефект, за который реестр уже правился планом `05-06`.
- **Основанием названо СВИДЕТЕЛЬСТВО, а не исполнение.** У каждой отметки в таблице стоит истина отчёта проверки и команда, которой это проверено, по образцу уже стоявшей строки BILL-11.
- **Реестр не опередил свидетельство ни на одну строку.** BILL-05 и BILL-07 не тронуты.
- **Счётчики выведены арифметикой,** и их прежнее внутреннее расхождение закрыто заодно.
- **Отложенное записано решённым, а не пропущенным молча:** восемь предупреждений поимённо с причиной, четырнадцать краёв с судьбой у каждого, четыре информационных пункта названы не тронутыми.

## Что именно записано в реестре

### Отметки (дерево требований + таблица прослеживаемости v2.0)

| Требование | Было | Стало | Основание (истина отчёта проверки) |
|------------|------|-------|-------------------------------------|
| BILL-08 | `Pending` | `Complete` (2026-08-21) | **Истина 1.** Цена — единственный литерал настройки `settings.subscription_price = "3000.00"` (`app/config.py:77`); читают его ровно три потребителя, и все три серверные (`billing.py:282`, `billing.py:356`, `balance.html:71` через `format_amount`); арифметики над ценой нет ни одной; тринадцать отрицательных грепов по `app/` дают ноль каждый; отказа по количеству нет ни на одном маршруте |
| BILL-09 | `Pending` | `Complete` (2026-08-21) | **Истина 2.** `TRIAL_DAYS = 5` (`app/constants.py:89`); `start_trial` объявлен ОДИН раз (`subscription_service.py:43`) и вызывается обоими путями регистрации (`pages/auth.py:336`, `routes/auth.py:73`); прекращение и слова закреплены сквозным тестом |
| BILL-10 | `Pending` | `Complete` (2026-08-21) | **Истины 3 и 4.** Частичный уникальный индекс `uq_subscriptions_active_user` объявлен В МОДЕЛИ (`subscription.py:22-30`), а не только в ревизии; `next_expiry` отсчитывает от позднейшей из двух точек; панель имеет ровно четыре состояния, и вычисляет их обработчик `_access_state` (`billing.py:143`), а не разметка |
| BILL-05 | `In Progress` | **`In Progress` — НЕ ТРОНУТО** | Условием отметки в той же таблице названа ручная приёмка `/billing` на 375px (перенесённый долг открытого пункта UAT фазы 5). Её никто не проводил; ставит её план `05.1-13` |
| BILL-07 | `In Progress` | **`In Progress` — НЕ ТРОНУТО** | Та же причина |

### Счётчики §Coverage (v2.0)

| Строка | Было | Стало |
|--------|------|-------|
| Complete | 24 | **27** (+BILL-08, BILL-09, BILL-10) |
| In Progress | 2 | 2 (без изменений) |
| Pending | 15 | **12** (−те же три) |
| Partial | 0 | 0 |
| Сумма | `23 + 2 + 16 + 0 = 41` | **`27 + 2 + 12 + 0 = 41`** |
| Итог | 41 | 41 — **не менялся** |

⚠️ **Внутреннее расхождение строки «Сумма» закрыто заодно, и это записано словами.** До правки строка суммы называла 23 закрытых и 16 ожидающих, тогда как строки состояний называли 24 и 15. Оба набора давали в сумме 41 — потому расхождение и пережило пересчёт плана `05.1-09` незамеченным: сходилась сумма, а не слагаемые. В подвале это названо отдельным предложением, иначе следующий читатель прочтёт новые числа как опечатку.

Итог §Coverage в `.planning/ROADMAP.md` **не тронут**: он объявляет ЧИСЛО требований, а не их состояния, и это число не менялось. Строки сверки с деревом проверены пересчётом и остались верными: в разделе «v2.0 Requirements» 42 строки требований = 41 распределённое + 1 VOID (BILL-06); в таблице прослеживаемости 43 = те же 42 плюс сохранённая строка GRP-08.

### Ни одна строка требования не удалена

`git diff` коммита `b917258`: **10 удалённых строк, 13 добавленных.** Из них **6 пар** несут идентификатор требования (BILL-08, BILL-09, BILL-10 — дважды каждый: строка дерева и строка прослеживаемости), и у каждой удалённой строки есть парная добавленная **с тем же идентификатором**. Остальные 4 удалённых — строки счётчиков, идентификатора требования не несущие. Осиротевших удалений нет.

## Отложено сознательно: WR-01…WR-08

_Раздел перенесён из плана `05.1-12` ДОСЛОВНО. Пересказанная причина через фазу читается как мнение исполнителя, а записанная — как решение владельца._

Решение владельца по этому прогону: закрыть блокер, одностороннюю дверь и реестр требований; восемь предупреждений ревизии кода оставить следующему проходу. Список записан ПОИМЁННО с причиной у каждого — не потому, что причины оправдывают отложение, а потому, что молча пропущенное предупреждение через фазу неотличимо от закрытого.

| # | Файл | Суть | Причина отложения |
|---|------|------|-------------------|
| **WR-01** ⭐ | `app/metrics.py:27-30`, `:49-52` | Справка метрики числа активных пользователей обещает «людей с ОТКРЫТЫМ доступом», а запрос спрашивает только про признак активности строки: ни срок, ни признак льготы в условие не входят | **Сильнейший кандидат следующего прохода.** После наката ревизии активная строка есть у КАЖДОГО (популяцию без строки населяет сама ревизия), поэтому метрика становится тождественна «всего пользователей» и на закрытие доступа не реагирует вовсе. Дежурный, читающий панель, получает неверный ответ на вопрос «скольких продукт сейчас пускает». Отложено потому, что правка требует решения — привести запрос к справке или переименовать метрику вместе со справкой, — а решение это относится к наблюдаемости, а не к предмету фазы |
| **WR-02** ⚠️ | `alembic/versions/0020_flat_subscription.py:48-53`; `tests/test_migrations/test_0020_flat_subscription.py:400-427` | Ревизия объявляет правило строгости границы срока, которого в её теле нет; парный тест этого не ловит, потому что проверяет два тождественно истинных факта | **Записана редакция ветки (б)** — она и выбрана на чекпойнте плана `05.1-11` (см. ниже). Половину закрывает план `05.1-11`: сравнение по сроку в ревизии появилось (`expires_at < :trial_expires_at`), абзац докстринга приведён к нему, а строгость получила два теста — соседнюю точку на секунду выше границы и утверждение об операторе внутри извлечённого текста SQL. **Вторая половина — вакуумность теста границы ПЛАТНОЙ строки — отложена** |
| **WR-03** | `app/services/payment_service.py:679-688` | Событие «деньги взяты, товар не выдан» пишется уровнем `info` и потому не попадает ни в один алерт, хотя это самый тяжёлый класс события на денежном пути | Отложено: событие относится к платежам, заведённым ДО выката (пакетная ветка вебхука), и правка требует решения об операторском действии — ручном возврате в кабинете ЮKassa, — которого в продукте нет. Поднять уровень без названного действия значит завести алерт, на который нечем ответить |
| **WR-04** ⭐ | `app/pages/admin.py:498-585` | Привилегированный POST выдачи бесплатного доступа не делает сверки источника, тогда как два соседних POST того же проекта её делают; и он же отвечает молчаливым редиректом при отсутствии строки подписки | **Второй сильнейший кандидат.** Это самое привилегированное действие админки после удаления, и сегодня межсайтовую отправку удерживает только атрибут куки — то есть свойство КУКИ, а не свойство маршрута: смена атрибута или добавление второго способа аутентификации снимет защиту молча. Готовый помощник в проекте уже есть. Отложено только по решению о составе этого прогона |
| **WR-05** | `app/services/payment_service.py:26`, `:169-170`, `:358-365` | Пакетная ветка создания платежа недостижима по построению: единственный вызывающий всегда передаёт подписочный предмет покупки | Отложено: снятие меняет сигнатуру функции денежного пути и перецеливает два теста, а предмет — чистота кода, а не дефект поведения. ⚠️ Ветка ВЕБХУКА к этому пункту не относится и снятию не подлежит: по ней приходят уведомления о платежах, заведённых до выката |
| **WR-06** | `app/pages/admin.py:498`; `app/templates/admin/user_detail.html:145` | Адрес маршрута тумблера остался от снятого признака безлимита СООБЩЕНИЙ, тогда как обработчик, колонка и кнопка уже говорят о доступе | Отложено: переименование трогает шаблон и тест, поведения не меняет. ⚠️ Отмечено, что греп-гейт снятых имён формально не покраснел лишь потому, что ищет более длинную подстроку, чем стоит в адресе |
| **WR-07** | `tests/test_application/test_no_metering_remains.py:209-242` | Инвариант «признак льготы читается ровно в одном месте» проверяется обходом только одного пакета, тогда как колонку читают ещё в двух модулях показа одним и тем же выражением | Отложено: закрывается либо расширением области обхода свидетеля с явным белым списком читателей показа, либо объявлением производной величины рядом с предикатом — обе правки своего размера. ⚠️ Сегодня гейт зелен и создаёт впечатление закрытого вопроса |
| **WR-08** | `app/pages/admin.py:72-82`, `:552-559` | Админский читатель активной подписки выбирает ДРУГУЮ строку, чем четыре остальных читателя проекта; вторая его форма вовсе без ограничения выборки | Отложено: обе формы недостижимы, пока стоит частичный уникальный индекс активной подписки. ⚠️ Но они единственные в проекте, кто опирается на индекс МОЛЧА, и снятие индекса будущей фазой обнаружится на админском экране, а не в прогоне |

Информационные пункты ревизии кода **IN-01, IN-02, IN-03 и IN-04** этим прогоном не тронуты и остаются записанными в `05.1-REVIEW.md`. Названы здесь поимённо намеренно: молчание о них сделало бы их неотличимыми от закрытых. Кратко — IN-01: отказ БД после успешного вызова SDK оставляет платёж без своей строки; IN-02: `balance.html` читает `access.days_left` атрибутом там, где соседние ветки читают через `.get()`; IN-03: заголовок источника вебхука берётся из настройки без проверки его пригодности; IN-04: полный прогон суиты не укладывается в 900 с.

### Сверка двух инструментов: ревизия кода ↔ отчёт проверки

Восьмёрка предупреждений `05.1-REVIEW.md` сверена с § «Найденные анти-паттерны» `05.1-VERIFICATION.md`. Два инструмента смотрели на один код независимо, и расхождения названы, а не усреднены.

**Совпали по существу (оба увидели):** WR-01 (`app/metrics.py`), WR-04 (`app/pages/admin.py:498-585`), WR-08 (`app/pages/admin.py:72-82`, `:552-559`), а из информационных — IN-02 и IN-03. Совпадение независимых прочтений здесь и есть их вес.

**Разошлись по СЕРЬЁЗНОСТИ — два пункта, и оба разошлись в одну сторону:**

| Пункт | Ревизия кода | Отчёт проверки | Чем разрешилось |
|-------|--------------|----------------|-----------------|
| Безусловный `UPDATE` в ветке П-о-2 ревизии `0020` | **CR-01 — Critical** (односторонняя дверь) | ⚠️ **WARNING** | Возобладала оценка ревизии: пункт вынесен человеку, владелец принял решение 2026-08-21, и план `05.1-11` закрыл его веткой `b-never-shorten`. Была бы принята оценка проверки — дверь закрылась бы сама на ближайшем деплое |
| Недостижимая пакетная ветка `create_payment` | ⚠️ **WR-05 — Warning** | ℹ️ **INFO** | Не разрешалось: пункт отложен в любом прочтении, разница в серьёзности на судьбу не повлияла |

**Разошлись по ЗЕРНИСТОСТИ — один пункт:** WR-04 в ревизии — один пункт о двух свойствах одного маршрута (отсутствие сверки источника И молчаливый отказ); в отчёте проверки это две отдельные строки (`:498-585` и `:561-571`). Расхождение формы, не существа.

**Увидела ТОЛЬКО ревизия кода — шесть пунктов:** WR-02, WR-03, WR-06, WR-07, а из информационных — IN-01 и IN-04.

⚠️ **Причина не одна, и её надо различать, а не сваливать в «проверка недосмотрела».** Проверка сканировала **`app/` и `alembic/`** — 37 файлов диапазона `8c4e420~1..HEAD`, и каталог `tests/` в её область НЕ входил. Поэтому:

- **WR-07 и вторая половина WR-02 лежали вне области сканирования ПО ПОСТРОЕНИЮ** — их предмет живёт в `tests/test_application/test_no_metering_remains.py` и `tests/test_migrations/test_0020_flat_subscription.py`. Это не недосмотр, а граница инструмента, и знать её важнее, чем зафиксировать промах.
- **WR-03 и WR-06 лежали ВНУТРИ области** (`app/services/payment_service.py`, `app/pages/admin.py` + шаблон) и всё же названы не были. Вот это — настоящее расхождение прочтений, и оно записано именно так.
- **Первая половина WR-02** (докстринг ревизии объявляет правило, которого нет в теле) тоже лежала внутри области: проверка читала `0020_flat_subscription.py` подробно и нашла в нём безусловный `UPDATE`, но расхождения ОБЪЯВЛЕНИЯ с исполняемым правилом не назвала.

**Увидела ТОЛЬКО проверка — ни одного пункта.** Каждая строка её таблицы анти-паттернов отображается в пункт ревизии кода. Две её строки — негативные находки, а не пункты: маркеры `TBD`/`FIXME`/`XXX` и `TODO`/`HACK`/`PLACEHOLDER` дали **0 вхождений** по 37 изменённым файлам.

## Реестр неснятых краёв (14)

_Раздел перенесён из плана `05.1-12` ДОСЛОВНО, с одной строкой в редакции выбранной ветки (край 13) и одной добавленной строкой о месте второго повышенного края._

Детерминированный зонд краевых случаев прогнан по идентификаторам требований фазы и вернул **14 применимых краёв, 0 снятых, 14 неснятых** (категории `unclassified`, `boundary`, `adjacency`, `empty` и смежные). Спецификации у фазы нет, поэтому ни `## Edge Coverage`, ни `## Prohibitions` зонду поданы не были, и ни один край не имеет права быть отброшенным молча. Отнесение каждого края к требованию ниже — реконструкция планировщика по категориям зонда; судьба названа у каждого.

| # | Край | Категория | Требование | Судьба |
|---|------|-----------|------------|--------|
| 1 | Продление у пользователя БЕЗ активной строки подписки | `empty` | BILL-05 | Допущение: ревизия `0020` заводит строку каждому; закреплено `test_a_user_without_a_subscription_row_is_given_one` |
| 2 | Оплата в момент, когда срок РАВЕН текущему | `boundary` | BILL-05 | Допущение: отсчёт идёт от позднейшей из двух точек, живость сравнивается строго; закреплено `tests/test_application/test_subscription_period.py` |
| 3 | Две доставки вебхука одного платежа внахлёст | `adjacency` | BILL-05 | Допущение: закреплено `test_overlapping_deliveries_extend_subscription_by_one_month` |
| 4 | Назначение подписочного платежа при ПУСТОМ поле тарифа | `empty` | BILL-05, BILL-07 | **ПОВЫШЕН до приёмочного критерия** — план `05.1-10`, задача 1 |
| 5 | Пустая история платежей | `empty` | BILL-07 | Допущение: пустое состояние печатается словами; закреплено тестами раздела подписки |
| 6 | Длинное историческое имя пакета в строке журнала на узком экране | `long-text` | BILL-07 | **ВЫНЕСЕН ЧЕЛОВЕКУ** — пункт 1 ручной приёмки, план `05.1-13` (порог «история не даёт горизонтальной прокрутки») |
| 7 | Историческая строка с НЕПУСТЫМ тарифом рядом с новой | `adjacency` | BILL-07 | Допущение: закреплено `test_the_purpose_of_a_payment_has_three_sources`; план `05.1-10` его не трогает |
| 8 | Цена, изменённая настройкой до пятизначной, в строке цены на 375px | `boundary` | BILL-08 | **ВЫНЕСЕН ЧЕЛОВЕКУ** — пункт 1 ручной приёмки, план `05.1-13`: расчётный запас строки цены ≈9px, и пятизначная цена его съедает |
| 9 | Историческая строка журнала с суммой, отличной от текущей цены | `unclassified` | BILL-08 | Допущение: журнал печатает сумму как продана (D-H); ревизия таблицу платежей не трогает |
| 10 | Момент истечения РОВНО в секунду сравнения | `boundary` | BILL-09 | Допущение: сравнение строгое в предикате живости подписки; закреплено тестами срока |
| 11 | Пользователь без строки подписки на входе гейта | `empty` | BILL-09 | Допущение: предикат отвечает «закрыто» при отсутствии строки; закреплено `tests/test_pages/test_access_gate.py` |
| 12 | Оплата ВНУТРИ пробного срока не сжигает остаток пробных дней | `adjacency` | BILL-09 | Допущение: отсчёт от позднейшей из двух точек; закреплено тестами срока |
| 13 | Срок бесплатной строки РАВЕН «момент исполнения + пробное окно» | `boundary` | BILL-10 | **ПОВЫШЕН ЧАСТИЧНО** — план `05.1-11`, задача 2, ветка (б): СТРОГОСТЬ сравнения закреплена двумя тестами (соседняя точка на секунду выше границы + утверждение об операторе внутри извлечённого текста SQL), а точное равенство остаётся ДОПУЩЕНИЕМ с названной причиной: момент исполнения снимается внутри наката и тесту недоступен, и причина записана в докстринге теста |
| 14 | Выдача льготы пользователю БЕЗ строки подписки | `empty` | BILL-11 | Допущение (backstop UI-контракта E5): операция не отдаёт 500; закреплено планом `05.1-09` |

Итого судеб: **2 повышено до приёмочных критериев · 2 вынесено человеку · 10 допущений**. Ни один край не отброшен.

**Где живут оба повышенных края.** Край 4 (назначение платежа при пустом поле тарифа) — план `05.1-10`, задача 1; он от ветки чекпойнта не зависит. Край 13 — план `05.1-11`, задача 2, и его редакция от ветки зависит (см. ниже).

### Какая ветка прочитана и откуда

**Прочитано из `05.1-11-SUMMARY.md`: ветка (б) — `b-never-shorten`.** Строка сводки: «**Выбранная ветка:** `b-never-shorten` — срок подтягивается, но НИКОГДА не укорачивается», решение владельца от 2026-08-21, там же приведено его основание дословно («Необратимость дороже буквы»).

Обе зависящие от ветки строки записаны выше **в одной редакции каждая** — той, что соответствует факту:

- **WR-02** — редакция ветки (б): половина закрыта планом `05.1-11`, вторая половина (вакуумность теста границы платной строки) отложена. Редакция ветки (а) («отложен целиком») **не записана**, потому что не соответствует факту.
- **Край 13** — редакция ветки (б): **повышен ЧАСТИЧНО**. Редакция ветки (а) («снят тождественно вместе с предметом») **не записана**: при (б) предмет никуда не делся — граница существует, и её строгость именно ПРОВЕРЕНА, а не устранена. Разница здесь не словесная: снятие вместе с предметом означало бы, что проверять стало нечего, а частичное повышение означает, что кто-то что-то проверил и назвал границу того, что проверено не было.

## Deviations from Plan

**Одно, и это не отступление от предписаний, а недостижимый приёмочный критерий — названный, а не обойдённый.**

**1. [Rule 3 — граница полномочий] Приёмочный критерий «`just tracking-check` завершается кодом 0» в worktree недостижим и не чинился**

- **Найдено в:** задача 1, плановая верификация.
- **Что красно:** `tests/test_planning/test_state_progress_matches_roadmap.py` — «поле `progress.completed_plans` во frontmatter `.planning/STATE.md` записано как 91, а из отметок `.planning/ROADMAP.md` выводится 93». Остальные 4 из 5 тестов каталога зелёные.
- **Почему это НЕ дефект этого плана:** гейт читает ровно два файла — `.planning/ROADMAP.md` и `.planning/STATE.md`. Греп `REQUIREMENTS` по `tests/test_planning/` даёт **ноль вхождений**: счётчиков требований он не читает вовсе. Дифф плана состоит из одного файла — `.planning/REQUIREMENTS.md` (`git diff --name-only`). Входы теста от базы worktree не отличаются ни на символ, значит на базе он красен тождественно. Разошлись счета ПЛАНОВ: ROADMAP отметил исполненными планы `05.1-10` и `05.1-11`, а машинно читаемое поле их ещё не увидело.
- **Почему не сведено здесь:** `.planning/STATE.md` и `.planning/ROADMAP.md` принадлежат оркестратору волны, и параллельному исполнителю в worktree их правка **запрещена прямо**. Правка была бы записана поверх при сведении, а при неудачном порядке — потеряна вместе с worktree.
- **Кто закрывает:** оркестратор волны шагом трекинга после слияния. Это ЗАЯВЛЕННАЯ цена механизма, а не его отказ: `.planning/STATE.md:154` записывает её словами — «в окне между коммитом плана и коммитом трекинга гейт КРАСНЫЙ — это и есть его предмет, окно обязано быть видимым».
- **Записано:** `.planning/phases/05.1-edinaya-podpiska/deferred-items.md`, пункт DEF-01.

⚠️ **План сам предупреждал ровно об этом,** и предупреждение оказалось точным: § `<verification>` называет границу трекинг-гейта прямо — «`tests/test_planning/` принуждает равенство СЧЁТА ПЛАНОВ и не читает счётчиков требований вовсе», а значит зелёный `tracking-check` НЕ доказывал бы верности счётчиков покрытия. Верность счётчиков здесь держится арифметикой приёмочных критериев и подвальной записью основания — и держится независимо от цвета этого гейта.

## Verification Results

| Проверка | Результат |
|----------|-----------|
| `grep -c '^- \[x\] \*\*BILL-0[89]\*\*\|^- \[x\] \*\*BILL-10\*\*'` | **3** — все три отметки стоят в дереве требований |
| `grep -c '^\| BILL-0[89] \| Phase 05.1 \| Complete\|^\| BILL-10 \| Phase 05.1 \| Complete'` | **3** — все три стоят в таблице прослеживаемости |
| `grep -c '^\| BILL-05 \| Phase 05.1 \| In Progress\|^\| BILL-07 \| Phase 05.1 \| In Progress'` | **2** — обе строки не тронуты |
| `grep -c 'Complete: 27'` · `'In Progress: 2'` · `'Pending: 12'` | **1 · 1 · 1** — каждая находит ровно одну строку |
| Строка «Сумма»: `27 + 2 + 12 + 0` | **41** — сходится и называет ТЕ ЖЕ числа, что строки состояний |
| `grep -c 'by plan 05.1-12'` | **1** — основание записано в подвале |
| `git diff --stat` по коммиту `b917258` | **ровно один файл** — `.planning/REQUIREMENTS.md` |
| Парность удалённых строк требований | **6 пар**, у каждой — тот же идентификатор; 4 оставшихся удаления суть строки счётчиков |
| `grep -c 'WR-0'` по этой сводке | **≥ 8** — все восемь названы поимённо |
| `grep -c 'IN-0'` по этой сводке | **≥ 1** — четыре информационных пункта названы |
| `uv run pytest tests/test_planning -q` | **4 passed, 1 failed** — падение на счёте ПЛАНОВ, разобрано в § Deviations и вынесено в `deferred-items.md` |
| `just tracking-check` | **rc=1** по той же причине; предмет — `STATE.md` ↔ `ROADMAP.md`, а не `REQUIREMENTS.md` |

## Требования

`requirements-completed: [BILL-08, BILL-09, BILL-10]` — отметка поставлена **этим планом** и **в самом реестре**, потому что предмет плана и есть реестр.

⚠️ `BILL-05` и `BILL-07` **НЕ отмечены** и отмечены здесь быть не могли: условием их отметки названа ручная приёмка `/billing` на 375px, её проводит план `05.1-13`, и он же — единственный правщик реестра после обоих человеческих пунктов.

## User Setup Required

None — внешней конфигурации план не требует.

## Known Stubs

None. Заглушек, пропущенных тестов и невыполненных `<verify>` план не оставил: обе автоматические проверки прогнаны, их результат приведён выше целиком, включая красный пункт — он назван, а не скрыт.

## Threat Flags

Новых поверхностей безопасности не появилось: план не тронул ни строки кода. По реестру угроз плана:

- **T-05.1-42** (отметка `Complete` вперёд свидетельства) — **СНЯТА**: каждая из трёх отметок несёт основание, названное истиной отчёта проверки и командой прогона; BILL-05 и BILL-07 сознательно не отмечены, и причина записана в той же таблице и в подвале.
- **T-05.1-43** (молча пропущенное предупреждение ревизии) — **СНЯТА**: восемь предупреждений перенесены поимённо с причиной, WR-01 и WR-04 названы сильнейшими кандидатами следующего прохода, четыре информационных пункта названы не тронутыми, и вдобавок проведена сверка двух инструментов с поимённым перечнем расхождений.
- **T-05.1-44** (реестр требований в репозитории) — остаётся **принятой**: ни ключей, ни персональных данных, ни платёжных реквизитов документ не содержит.
- **T-05.1-SC** (легитимность пакетов) — **неприменима**: план не устанавливает ни одного пакета.

## Next Phase Readiness

- **Реестр не отстаёт от кода и не опережает свидетельство.** Следующая фаза читает состояния как факт и может планировать поверх них.
- **Открыто и названо поимённо:** восемь предупреждений ревизии кода (сильнейшие — WR-01 и WR-04), четыре информационных пункта, десять допущений реестра краёв.
- **Ждёт человека:** ручная приёмка `/billing` на 375px и читаемость текста об истечении — план `05.1-13`; прогон ревизии `0020` на копии боевой базы — план `05.1-14`.
- **Ждёт оркестратора:** сведение `progress.completed_plans` в `.planning/STATE.md` с отметками `.planning/ROADMAP.md` (DEF-01).
- **Ревизия `0020` на бой НЕ накачена** — применимость BILL-09 и BILL-10 к БОЕВЫМ ДАННЫМ остаётся открытой до плана `05.1-14`. Отметки, поставленные здесь, относятся к проверенному КОДУ, и это записано в основании каждой из них.

## Self-Check

- `.planning/REQUIREMENTS.md` — FOUND
- `.planning/phases/05.1-edinaya-podpiska/deferred-items.md` — FOUND
- `.planning/phases/05.1-edinaya-podpiska/05.1-12-SUMMARY.md` — FOUND
- Коммит `b917258` — FOUND

## Self-Check: PASSED

---
*Phase: 05.1-edinaya-podpiska*
*Completed: 2026-08-21*
