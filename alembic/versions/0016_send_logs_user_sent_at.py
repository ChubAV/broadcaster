"""Составной индекс (user_id, sent_at) на send_logs

ЗАЧЕМ. Все запросы дашборда и истории имеют одну и ту же форму — «мои записи в
окне времени»: отбор по `user_id` плюс диапазон по `sent_at`. Существующие
индексы `ix_send_logs_user_id` и `ix_send_logs_sent_at` (app/models/send_log.py)
покрывают эти два условия ПО ОТДЕЛЬНОСТИ, и планировщик вынужден выбирать один
из двух, отбрасывая половину селективности: либо читает все записи одного
пользователя за всё время, либо все записи всех пользователей за окно. Составной
индекс закрывает оба условия одним проходом. Порядок колонок задан формой
запроса: равенство (`user_id`) идёт первым, диапазон (`sent_at`) — вторым, иначе
вторая колонка индекса перестаёт сужать поиск.

Имя индекса следует конвенции SQLAlchemy `ix_<table>_<cols>`, как и все
одиночные индексы этой таблицы. Одиночные индексы ревизия НЕ снимает: составной
не покрывает доступ по одному только `sent_at` и по `task_id`, а снятие было бы
отдельным решением с собственным обоснованием.

РАЗМЕН ПО БЛОКИРОВКАМ НАЗВАН ЯВНО — тот же разбор, что в ревизии 0015. На
PostgreSQL `CREATE INDEX` берёт на таблице блокировку `SHARE`, а она несовместима
с `ROW EXCLUSIVE` пишущих запросов: на время построения индекса запись в
`send_logs` стоит. Цена здесь выше, чем у ревизии 0015: `groups` растёт по числу
групп пользователя, а `send_logs` — по числу ОТПРАВОК, то есть это самая
растущая таблица системы, и она же принимает записи от Celery непрерывно.
Размен принят по сегодняшним объёмам: построение индекса по таблице такого
размера занимает доли секунды, а альтернатива — `CREATE INDEX CONCURRENTLY`,
который строит индекс, не блокируя запись, — требует ревизии БЕЗ транзакции
(`autocommit_block`) и умеет оставлять индекс в состоянии `INVALID` при обрыве,
что чинится вручную. Если таблица вырастет настолько, что окно записи станет
заметным, перевод этой ревизии на неблокирующее построение — готовый следующий
шаг, а не переоткрытие вопроса.

ИНДЕКС — ОПТИМИЗАЦИЯ, А НЕ ПРЕДУСЛОВИЕ. Ни один запрос дашборда и истории не
требует этой ревизии для КОРРЕКТНОСТИ: до её наката оба раздела обязаны работать
на существующих одиночных индексах и работают. Поэтому откладывание наката
ничего не ломает, а `downgrade` безопасен в любой момент — он снимает индекс и
не трогает ни одной строки.

Revision ID: 0016
Revises: 0015
"""
from alembic import op

revision = "0016"
down_revision = "0015"

INDEX_NAME = "ix_send_logs_user_id_sent_at"


def upgrade():
    op.create_index(INDEX_NAME, "send_logs", ["user_id", "sent_at"])


def downgrade():
    op.drop_index(INDEX_NAME, table_name="send_logs")
