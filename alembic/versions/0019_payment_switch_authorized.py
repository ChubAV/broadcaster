"""Записанный ответ гарда смены тарифа на платеже

ЗАЧЕМ. У правила смены тарифа две стадии. Стадия НАМЕРЕНИЯ решает, ПРОДАВАТЬ ли,
в момент нажатия кнопки; стадия ПРИМЕНЕНИЯ решает, что делать с УЖЕ УПЛАЧЕННЫМ, в
момент прихода уведомления от ЮKassa. Между этими двумя моментами лежит вся
сессия оплаты у ЮKassa, и состояние подписки за это время меняется — самим
пользователем, соседним подтверждённым платежом или просто временем.

До этой ревизии ответ первой стадии не сохранялся НИГДЕ. Вторая стадия поэтому
принимала решение ЗАНОВО, по перечитанной строке подписки, и выдавала не то, что
было продано. Дефект воспроизведён независимо в обе стороны:

  * платёж младшего тарифа, проданный при отсутствующей подписке, покупал полный
    календарный месяц действующего старшего — 9370 ₽ давали 121 день Pro
    списочной стоимостью 19 600 ₽, и повторов не ограничивало ничто;
  * зеркально: понижение, проданное на ИСТЁКШЕМ сроке (гард пропускает — защищать
    нечего), приходило на ОЖИВШИЙ период и не выдавалось вовсе — деньги взяты за
    тариф, который не выдан.

Предыдущие два подхода лечили не корень. Один объединил ОБЪЯВЛЕНИЕ правила, но не
его входные данные; второй объединил ФОРМУ входных данных, но не МОМЕНТ их
снятия. Корень в том, что ФАКТ СОСТОЯВШЕЙСЯ СДЕЛКИ нигде не записан, и колонка
`switch_authorized` записывает именно его: что было ПРОДАНО этому платежу.

ПОЧЕМУ NULLABLE И БЕЗ `server_default`. Существующие строки правила НЕ
СПРАШИВАЛИ, и выдать им значение значило бы записать факт, которого не было:
`false` — отказ, которого не случалось, `true` — разрешение, которого никто не
давал. NULL здесь честен и означает ровно «ответа нет»; для таких строк стадия
применения продолжает решать правилом по текущему состоянию, как и до ревизии.
Новые строки без ответа не заводятся — этого не даёт обязательный keyword-only
параметр `create_payment`, падающий TypeError у необновлённого вызывающего.

Три значения колонки, а не два: `true` — спросили и разрешило; `false` — спросили
и отвергло (через форму сегодня недостижимо, отказ возвращает 302 ДО создания
платежа, но колонка обязана уметь это выразить, иначе следующий писатель выразит
отказ через NULL); NULL — не спрашивали.

BATCH_ALTER_TABLE ЗДЕСЬ НЕ ПРИМЕНЯЕТСЯ, И ЭТО НЕ НЕДОСМОТР. Ревизии `0017` он
понадобился из-за `op.alter_column`, которого SQLite не умеет ВОВСЕ: batch-режим
пересоздаёт там таблицу целиком. Чистый `ADD COLUMN ... NULL` SQLite умеет сам,
и batch-режим пересоздавал бы таблицу платежей без единой причины — лишний риск
на таблице, которая является следом денег.

⚠️ ГДЕ ОНА В ОЧЕРЕДИ — СЕДЬМОЙ НЕВЫКАЧЕННОЙ (D-26). Боевая база решением
владельца стоит на ревизии `0012`; невыкачены `0013`, `0014`, `0015`, `0016`,
`0017`, `0018` и теперь эта. Читатель обязан узнать это ЗДЕСЬ, а не выводить из
планировочных документов: пока очередь не пройдена, колонки на бою НЕТ, и все
строки платежей там будут заведены без записанного ответа. Проход очереди идёт
в одну сторону: `0013_ad_status.py` необратима.

⚠️ ВЫКАТИТЬ КОД БЕЗ ЭТОЙ ОЧЕРЕДИ НЕЛЬЗЯ, И ПРИЧИНА НЕ В ТОМ, ЧТО НОВАЯ ФУНКЦИЯ
НЕ ДОЕДЕТ, А В ТОМ, ЧТО СТАРАЯ СЛОМАЕТСЯ. Разница между двумя формулировками —
это разница между отложенным удобством и остановленным приёмом денег, и вторая
формулировка верна.

Механизм. ORM-выборка СУЩНОСТИ выписывает в `SELECT` ПОЛНЫЙ перечень
отображённых моделью колонок, а не только те, которые читает вызывающий.
`Payment.__table__.columns` содержит `kind`, `plan` и `switch_authorized`; в
боевой схеме `0012` нет ни одной из трёх. Значит `UndefinedColumn` поднимает
КАЖДЫЙ путь, выбирающий сущность, — независимо от того, интересуется ли он
новыми колонками вообще:

  * `get_payment_history` (`app/services/billing_service.py:230-236`) — блок
    «История платежей», а с ним и ВЕСЬ раздел «Тарифы», отдаёт 500 любому
    вошедшему. BILL-06 и BILL-07 перестают выполняться не потому, что не
    реализованы;
  * `_open_subscription_intents` (`app/services/payment_service.py:136-148`) —
    потолок одновременных подписочных намерений;
  * `handle_webhook` (`app/services/payment_service.py:443-448`) — обработчик
    уведомления ЮKassa;
  * УЦЕЛЕЕТ `count_payments` (`app/services/billing_service.py:247-251`): он
    выбирает `count()`, а не сущность, и перечня колонок в его `SELECT` нет.
    Названо, чтобы перечень выше не читался как приблизительный.

ЦЕНА — ПРИЁМ ПАКЕТНЫХ ПЛАТЕЖЕЙ, РАБОТАВШИЙ ДО ЭТОЙ ФАЗЫ. В `handle_webhook`
выборка платежа `select(Payment).with_for_update()` стоит на строках 443-448,
а ветвление по предмету покупки — НИЖЕ: пакетная проверка на `:496`,
подписочная ветка на `:518`. Падение происходит ДО ветвления, поэтому платёж за
ПАКЕТ СООБЩЕНИЙ, к колонкам подписки отношения не имеющий, обрабатывается ровно
так же — никак.

Чем это кончается для денег: маршрут отвечает 500 (`app/routes/billing.py:200-202`
— необработанное исключение превращается в `HTTPException(500)`), ЮKassa
трактует 5xx как «попробовать позже», повторяет доставку ОГРАНИЧЕННОЕ число раз
и сдаётся. Деньги списаны, сообщения не начислены, платёж навсегда `pending`.
Это тот же класс отказа, который фаза лечит в `webhook_package_without_messages_count`
(`:496-503`), — только введённый в путь, который до неё работал.

Отсюда правило выката, читаемое как одно утверждение: ВЫКАТ КОДА И ВЫКАТ
ОЧЕРЕДИ МИГРАЦИЙ ОТДЕЛИТЬ ДРУГ ОТ ДРУГА НЕЛЬЗЯ. Не «желательно вместе» —
нельзя: код, приехавший на схему `0012`, отвечает 500 на деньги. Машинная
сверка отображённых колонок с ГОЛОВНОЙ ревизией репозитория живёт в
`tests/test_migrations/test_model_matches_head.py`; расхождение репозитория с
БОЕВОЙ схемой она не ловит и не может — это операционный факт D-26, и его место
в `.planning/STATE.md`.

⚠️ DOWNGRADE СИММЕТРИЧЕН ПО СХЕМЕ И ТЕРЯЕТ ДАННЫЕ. Он снимает колонку, а вместе с
ней — все записанные разрешения; восстановить их неоткуда, потому что второго
места, где хранится ответ гарда, по замыслу не существует. Названо прямо, тем же
способом, каким названа односторонность ревизии `0018`.

Литералы имён таблицы и колонки выписаны ЗДЕСЬ строками и НЕ импортированы из
`app.models` — правило ревизий `0013`/`0014`/`0017`/`0018`: ревизия описывает
схему на СВОЙ момент времени, и переименование атрибута задним числом не имеет
права изменить смысл давно выполненного шага.

Revision ID: 0019
Revises: 0018
"""
import logging

from alembic import op
import sqlalchemy as sa

revision = "0019"
down_revision = "0018"

TABLE_NAME = "payments"
COLUMN_NAME = "switch_authorized"

logger = logging.getLogger("alembic.runtime.migration")


def upgrade():
    op.add_column(
        TABLE_NAME,
        sa.Column(COLUMN_NAME, sa.Boolean(), nullable=True),
    )


def downgrade():
    """Снимает колонку. Записанные разрешения НЕ восстанавливает — их не вернуть.

    Схему откат возвращает полностью: до `0019` колонки не было, после отката её
    снова нет. Данные — нет: ответ гарда хранится только здесь, и второго места,
    из которого его можно перечитать, не существует по замыслу решения D-28.
    Предупреждение пишется в журнал, чтобы откатывающийся прочитал это на месте,
    а не в докстринге постфактум.
    """
    op.drop_column(TABLE_NAME, COLUMN_NAME)
    logger.warning(
        "0019 downgrade: column %s.%s dropped — every recorded switch "
        "authorization is gone with it, and there is no second place to read it "
        "back from. The apply stage falls back to re-deciding by the rule.",
        TABLE_NAME,
        COLUMN_NAME,
    )
